# OI-S6-BOUNCE-CLASS-UX

record_kind: owed_item

owner: relaunch / next m-2 touch

source: gate-day finding F-GATE-s6-1, the operator's two-bounce diagnosis round, and `s6-gateday/SITREP-orchestrator-planner-20260708-021139.md`.

target_surface: require `PHASE` and `CEREMONY_TIER`, or fold absent-context into the typed-required class. This is a registry/validate contract change outside the co-signed s6 set; the finish-line fence holds.

disposition_path: m-2 rules the shape (require-flag vs class fold), then a bounded build touch. The operator may author the live-store owed record post-relaunch, following the s2 Q2=(i) pattern.
