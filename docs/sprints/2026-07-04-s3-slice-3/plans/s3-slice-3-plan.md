# S3 Slice-3 Implementation Plan — the full form system

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (single Implementer seat; implementation starts ONLY on a live `DISPATCH IMPL` per the dispatch conditions). Steps use checkbox (`- [ ]`) syntax for tracking.

**PLAN_LOCK_ID:** `s3-slice-3-plan` · **DESIGN_LOCK_ID:** `s3-slice-3-design` (r4, pair-approved `s3-form-design-r4-review/DESIGN-REVIEW-implementer-20260704-182951.md`; references GRILL_LOCK_ID `s3-grill-s3-form`) · **Owner:** s3-form · **Date:** 2026-07-04 · **Rev:** r1
**Goal:** frank speaks the real protocol — the full m-2 FieldSpec registry replaces the S1 6-enum dialect end-to-end (fresh-store), the 62-check linter dissolves into registry validation + the lineage engine with a code-first disposition table, the FULL 243-fixture replay runs both legs green, and `schema_version` migration + the R2/GRILL_REQUIRED carries land — S3 exit gate green (E2).
**Architecture:** design doc = the spec of record; this plan cites it as `D-<n>` / `S3-<id>` (docs/sprints/2026-07-04-s3-slice-3/designs/s3-slice-3-design.md — **the CURRENT rev in-tree is the spec of record; the r4 pair design-review approve (`…-182951.md`, on r4 = main@291ab08) is the v2.8.5 lineage parent — the review surface moves with folds, the lineage parent does not** (the S2 r2-lineage lesson)). Promote-don't-rebuild throughout: the digest seed, ClassifyGateCategory semantics, the corpus walker + report-matches-generated pattern, the S2 crash harness, and the owed mechanism are consumed, never re-implemented.
**Tech stack:** Go ≥1.22, stdlib only (unchanged). `go test ./...` = the E2 umbrella; `-race` on engine/lineage/fieldspec where concurrency moves.

## Global constraints (every task inherits; violations are review blockers)

- Terminal enum byte-exact `{accepted, rejected, held}` · one pivot per mutation class · presence = committed — all unchanged; S3 adds NO mutation class (validate-path slice).
- **Fresh-store qualifier on every claim surface** (R-SCOPE condition 1, verbatim): "registry live end-to-end **(fresh-store)**"; "the S3 registry rides `store.Init`; registry evolution on an existing store awaits the §7 config-change record." The §7 record is OUT (master-ruled; `OI-S3-CONFIG-CHANGE` carries it).
- **Replay-label guardrails structural** (R-GUIDE Q2): report rows carry `context: live | reconstructed-observe`; the exact caught-label string; live-coverage tallies exclude reconstructed rows by construction. Never "live-caught on a Step-1 form."
- **R2 grain** (R-GUIDE Q3): load-time grammar rejection off the field's own flags; the negative set includes the **both-flags test column** (`gate_referenceable: false` ∧ `model_identity: true`); concrete `chosen_model`/bucket-proxy negatives = S4/m-4 (OUT).
- **D-10 grain:** GRILL_LOCK_ID dependent-required fires ONLY at `all_of(field:GRILL_REQUIRED == true, field:DESIGN_LOCK_ID present)` — grill-ordering shapes pass.
- **m-1 shapes are PROPOSALS pending fidelity** (dispatch condition 3): the §4 packet (tables/query shapes, PARENT parent_picker realization, lineage-field homes, migrator read-facade wrap point, JSON-in-string carrier confirm, no-new-record_kind confirm) is routed by the orchestrator (`s3-fidelity-m1/SITREP-orchestrator-planner-20260704-183833.md`); **`DISPATCH IMPL` is blocked until m-1's approve is on record in `.relays/s3/`**; a must-revise folds bounded (the S2 F-M1 pattern) before any dispatch.
- **No per-submit full-store rescan survives the live path** (F-P1, constraint 6): after Task 4, `store.Records()` callers on the live submit/commit path = zero (recovery + table-build only); the S3-P1 probe fixture pins it.
- **CQ-1 discipline:** no Step-1 live form requires an observe-owned field; every system/lineage/form-owned required (`EVIDENCE_TARGET` included) stays required.
- **I-PH on every new surface:** registry/predicate/migration/lineage errors ride `bounce.Format` field:class grain; the served schema payload carries no store/config path and no config value; the P1 grep family extends to all of them. `config.Load`'s path-embedding error class never reaches a seat surface.
- **S1+S2 invariant assertions stay byte-identical** with ONE sanctioned exception class, stated: unit tests of the DYING 6-enum API (`internal/fieldspec/fieldspec_test.go` construction/API lines; `test/replay/classmap.go` + its report) are superseded-by-design — their *invariant* assertions (seat-scope negatives, other→A, digest presence, re-render class) migrate assertion-equivalent onto the v2 surfaces and are flagged line-by-line in the migration commit for review. Everything else: mechanical call-site migration only.
- **Fill-time-authority claim wording everywhere:** tool-mediated confusion-resistance (affordance, not access) + D5 residual beside every exclusivity-shaped claim; the dissolution claim is proven by the executed replay, never asserted.
- Scope fence: the ROADMAP OUT list verbatim; no `../master` or `../extracted` write; **root `README.md` is NOT in this plan's file list** — the fresh-store-qualifier README line is an ASK to the orchestrator (Task 13; the orchestrator pre-flagged it — ASK early, never deviate).
- Commits: small, per green step, on `main`, message prefix `s3 IMPL:`.

## File structure (the decomposition of record; SCOPE_DIFF enumerates exactly these)

```
internal/fieldspec/registry.go + registry_test.go   # FieldSpec/NamedEnums/Registry v2 types + Load + ALL load-time checks (Task 1)
internal/fieldspec/registry.json                    # the v2 member content (default source; byte-exact locked enums) (Task 1)
internal/fieldspec/predicate.go + predicate_test.go # §5 AST, parser (load-rejects), evaluator + context (Task 2)
internal/fieldspec/canonical.go + canonical_test.go # canonical JSON-in-string marshal/parse per row Type (Task 3)
internal/fieldspec/render.go + render_test.go       # registry-driven Render + form digest (full context) (Task 4)
internal/fieldspec/validate.go + validate_test.go   # registry-driven Validate (full enum sweep, floors, digest, seat-scope) (Task 5)
internal/fieldspec/fieldspec.go                     # DELETE the 6-enum dialect (struct, Render, Validate, canGrant) after Tasks 4-5 land (Task 5)
internal/tables/tables.go + tables_test.go          # runtime tables: by-relay/by-dispatch/lineage-class/waiver + OnCommit (Task 6)
internal/obligation/obligation.go                   # Modify: CompleteAuto rides tables; BuildTables extends into tables pkg (Task 6)
internal/engine/loop.go                             # Modify: OnCommit table maintenance in completeTurn (Task 6)
internal/engine/submit.go                           # Modify: digest threading, registry Validate, lineage engine, tables (Tasks 5,6,7)
internal/lineage/lineage.go + lineage_test.go       # Rewrite: the §10c engine over tables; seven walks; authority floor kept (Task 7)
internal/channel/server.go + capability_test.go     # Modify: describe-grade per-seat form+digest response replacing static descriptions (Task 8)
internal/migrate/migrate.go + migrate_test.go       # migrator registry + read-time Apply + refusal legs (Task 9)
cmd/frank/main.go                                   # Modify: v2 registry assembly; describe wiring (Task 10)
test/fixtures/s3_*_test.go (per task)               # S3-* fixtures; iph/sweep extensions (Tasks 10,12)
test/replay/harness.go + dispositions.json + replay_test.go  # Rewrite: executed replay both legs; classmap.go + report.md DIE (Task 11)
docs/sprints/2026-07-04-s3-slice-3/results/disposition-table.md  # generated table of record (Task 11)
docs/sprints/2026-07-04-s3-slice-3/plans/s3-slice-3-plan.md      # this plan (pair-Planner-committed)
```

Fixture-id → task map: Task 1 {S3-G1..G5 (load-reject legs incl. the both-flags chain + mark-consistency), GRILL_REQUIRED/GRILL_LOCK_ID row data} · 2 {evaluator-grain atom legs + CQ-1 wrap + context parameter} · 3 {canonicalization no-aliasing} · 4 {S3-A1 render halves, digest full-context} · 5 {S3-A2..A4, S3-G6/G7, S3-RR1/RR1b, F-P4 enum sweep} · 6 {S3-P1 probe; S1/S2 suites green over the refactor} · 7 {S3-L1..L7, S3-RR1c} · 8 {S3-E2} · 9 {S3-V1..V5} · 10 {S3-E1, S3-V6, S3-RR2} · 11 {S3-D1..D5} · 12 {S3-RE, S3-SWEEP, I-PH extension}. **The disposition table + replay (Task 11) are not claimable green before Tasks 5+7 land** (they execute the real path).

---

### Task 1: Registry v2 — types, member content, Load with ALL load-time checks (D-1, D-10 row data, D-11 legs; S3-G1..G5)

**Files:** Create `internal/fieldspec/registry.go`, `registry_test.go`; replace `internal/fieldspec/registry.json` with the v2 shape.
**Interfaces produced:** `fieldspec.FieldSpec` — the D-1 struct verbatim (ID/Layer/Owner/Type/EnumSet/Options/GateReferenceable/ModelIdentity/SeatScope/RequiredWhen/VisibleWhen/FillConstraints/Consumers/LineageRole); `fieldspec.Registry{Version string, Provenance map[string]string, NamedEnums map[string][]string, Fields []FieldSpec}` + `ByID(id) (*FieldSpec, bool)`; `fieldspec.Load(path) (*Registry, error)` — parses v2 JSON then runs the LOAD-TIME check set, each failing with a typed, path-free error naming field + rule: (a) predicate atoms `field:`/`any_row:` naming a column with `GateReferenceable: false` ⇒ reject (R2 grammar, D-3); (b) `ModelIdentity: true ∧ GateReferenceable: true` ⇒ reject (mark consistency); (c) an `X-*` row with non-empty `Consumers` or `LineageRole != none`, or any predicate referencing an X- id ⇒ reject; (d) a concrete `slot_in` value in any predicate ⇒ reject (reserved shape); (e) unknown atom/op/owner/type/fill_constraints token ⇒ reject; (f) enum fields whose EnumSet names an absent named enum ⇒ reject. Member content: the D-1 Step-1 field-row set + named enums byte-exact (PHASE 11, AUTHORITY 9, tiers 5, E1..E4, §J2 A(8)/B(4) + `other`, grant 2, delivery_state 3 system-only) + the D-10 `GRILL_REQUIRED` row (guide shape verbatim: bool/agent_enum_pick/monotonic-RAISE-toward-yes/gate_referenceable TRUE/no-seat-denied-yes) + `GRILL_LOCK_ID` row (id_ref, lock-bearing dependent-required grain) + the observe-owned rows carrying the CQ-1 wrap + the grant/merge-gated SeatScope matrix admitting **operator + orchestrator-planner patterns only** (F-P3 fixed in data — `orchestrator-reviewer` and pair seats absent). `config.Load` keeps working unchanged (same member name; it delegates to `fieldspec.Load`).

- [ ] **Step 1: failing tests** — v2 member parses; ByID/enums byte-exact against the locked token lists (literal string asserts); S3-G1..G5: each load-reject leg above via minimal bad-registry JSON fixtures, error text asserts field + rule and greps path-free; the shipped member passes ALL checks; GRILL_REQUIRED/GRILL_LOCK_ID rows present with the exact guide/D-10 attributes. Run: `go test ./internal/fieldspec/ -v` — FAIL (types absent).
- [ ] **Step 2: implement types + Load + author the v2 member.** (The old 6-enum code stays compiling beside it until Task 5 deletes it — no dead-path window.)
- [ ] **Step 3: green + commit** `s3 IMPL: registry v2 data model + load-time checks + member content (S3-G1..G5)`.

### Task 2: The §5 bounded predicate evaluator (D-3; CQ-1 wrap; context parameter)

**Files:** Create `internal/fieldspec/predicate.go`, `predicate_test.go`.
**Interfaces produced:** `fieldspec.Predicate` (typed AST); `fieldspec.ParsePredicate(json.RawMessage, reg *Registry) (*Predicate, error)` — parse errors ARE the Task-1 load rejects (Load calls it per row); `fieldspec.EvalContext{Seat SeatMeta, Phase, Tier string, Slot string /* always "" Step-1 */, PresentLayers map[string]bool}`; `fieldspec.DefaultLayers() = {store,form,lineage}`; `(*Predicate).Eval(fields map[string]string, rows func(array, field string) []string, ctx EvalContext) bool` — pure boolean; atoms exactly `phase_in, ceremony_tier_gte, authority_in, seat_is, role_in, slot_in(reserved), record_kind_in, scan_result_in, claims_action, field:<id> {==,in,present}, any_row:<arr>.<f> {==,in,present}, layer_present:<layer∈{store,form,lineage,observe}>`; combinators `all_of/any_of/not`. Observe-owned rows evaluate under their stored `all_of(<base>, layer_present:observe)` wrap — with `DefaultLayers()` they are never required; a test context with `observe: true` fires them (the R-GUIDE Q2 reconstruction — Task 11 consumes this parameter).

- [ ] **Step 1: failing tests** — one leg per atom (true + false cases; `ceremony_tier_gte` over the 5-token order; `field:` in/==/present; `any_row:` over parsed row_arrays, no nesting); combinator nesting; CQ-1: an observe-wrapped predicate false under DefaultLayers, true under +observe; a form-owned required unaffected by the layers map (guardrail); tier variance: same field required at `large`, not at `small` (feeds S3-RR1b's rationale). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s3 IMPL: bounded predicate evaluator + CQ-1 layer wrap (D-3)`.

### Task 3: Canonical JSON-in-string carrier (D-2; grill Q1)

**Files:** Create `internal/fieldspec/canonical.go`, `canonical_test.go`.
**Interfaces produced:** `fieldspec.CanonicalMarshal(v any) (string, error)` — sorted keys, no insignificant whitespace, the ONE marshaler for structured header values; `fieldspec.ParseTyped(spec *FieldSpec, raw string) (TypedValue, error)` — per row Type: `row_array` ⇒ `[]map[string]string`, `object` ⇒ `map[string]string`, `address_list` ⇒ `[]string`, `bool/int/enum/string/id_ref/evidence_ref/text` ⇒ scalar validation; a non-canonical encoding of an equal value re-canonicalizes and byte-compares ⇒ typed violation `canonical-encoding` (no aliasing under the record checksum); parse failure ⇒ typed violation naming the field. `Headers` stays `map[string]string` (record.go untouched — the grill Q1 decision; zero m-1 envelope movement).

- [ ] **Step 1: failing tests** — marshal determinism (key order, whitespace); the aliasing leg (two encodings of one row_array ⇒ second rejected `canonical-encoding`); per-type parse legs incl. failures; record.Seal/Verify round-trip with a row_array header stays byte-stable. Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s3 IMPL: canonical JSON-in-string typed carrier (D-2)`.

### Task 4: Render v2 + the full-context form digest (D-4; S3-A1 render halves)

**Files:** Create `internal/fieldspec/render.go`, `render_test.go`.
**Interfaces produced:** `fieldspec.GrantState func(seat SeatMeta) bool` — the conditional-grant provider; a `ClosedGrantState` default (always false) until Task 7 wires the real one; `reg.Render(env RenderEnv, seat SeatMeta, phase, tier string, grants GrantState) (Form, string)` — iterates rows: VisibleWhen gates presence; Owner/FillConstraints → affordance (system ⇒ not rendered; monotonic ⇒ `[floor, MAX]` options; seat_scoped_enum ⇒ SeatScope[pattern] options only; agent_enum_pick ⇒ full set; free_text ⇒ text); `grant` rendered ONLY for seats the SeatScope matrix admits AND — for pair-planner patterns — only when `grants(seat)` is true (D-6 carry); the digest = SHA-256 over `CanonicalMarshal(struct{Form; ConfigDigest, SeatPattern, Phase, Tier string})` — **the canonical rendered form × (config digest, seat, phase, CEREMONY_TIER)** (r4 blocker fold); candidate-set *contents* (parent_picker lists) are delivered in the Form but EXCLUDED from the digested structure (a `DigestExempt` marker on the field's rendered entry — presence digested, contents not); `Render` takes the config digest via a `RenderEnv{ConfigDigest string}` (main threads `pinned.Digest`).

- [ ] **Step 1: failing tests** — S3-A1 render halves: operator + orchestrator-planner forms carry `grant` (dispatch-merge only under MERGE-GATE); **orchestrator-reviewer and pair forms carry NO grant and NO merge-gated AUTHORITY option** (F-P3 — assert against the v2 member, not a test registry); monotonic floor rendering `[floor,MAX]`; digest changes on: registry option flip, seat, phase, TIER (the r4 leg), config-digest change; digest UNCHANGED on candidate-set-contents change (the split, render grain); GrantState=false ⇒ pair-planner form omits grant, =true ⇒ present (provider-grain; the lineage-backed legs are Task 7's). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s3 IMPL: registry-driven render + full-context digest with candidate-set split (D-4)`.

### Task 5: Validate v2 — the full enum sweep, floors, live digest; the 6-enum dialect DIES (D-5; S3-A2..A4, S3-G6/G7, S3-RR1/RR1b; F-P4)

**Files:** Create `internal/fieldspec/validate.go`, `validate_test.go`; DELETE the old dialect from `internal/fieldspec/fieldspec.go` (struct/Render/Validate/canGrant/ClassifyGateCategory — semantics re-homed here); modify `internal/engine/submit.go` (thread `form_digest` from the payload; call the v2 Validate).
**Interfaces produced:** `reg.Validate(cand record.Record, seat SeatMeta, formDigest string, env RenderEnv, grants GrantState) []Violation` — required-set via Eval (DefaultLayers); **enum conformance for EVERY enum field** (PHASE, AUTHORITY, CEREMONY_TIER, EVIDENCE_TARGET, HUMAN_GATE_REQUIRED, gate_category, DESIGN_RECORD_KIND, DESIGN_REVIEW_VERDICT, grant, record_kind — F-P4 closed); seat-scope set-membership (out-of-scope option ⇒ `seat-scope` violation — the courier's re-validation half); monotonic floors: HUMAN_GATE_REQUIRED ≥ floor, gate_category known-A raise ⇒ effective A + `gate_category_raised: "true"` stamped into Headers (system computed — re-homed ClassifyGateCategory: A/B from the registry's §J2 named enums, `other`→A, the SINGLE §J2 source); typed parse per row (Task 3) violations; X-* passthrough untouched; system-owned payload fields ignored byte-for-byte (stamp wins — existing posture); digest check LIVE: `formDigest` empty OR ≠ current `Render` digest ⇒ violation class `re-render` (the dead `""` at submit.go:33 dies — the payload carries `form_digest`, threaded by SubmitHandler; an ABSENT payload digest is itself `re-render`, so no client can opt out); GRILL_REQUIRED monotonic OR/MAX across raisers (a `no` over a dispatch-raised `yes` context floor ⇒ raised, recorded like gate_category_raised — same mechanics); GRILL_LOCK_ID dependent-required at the D-10 grain.

- [ ] **Step 1: failing tests** — S3-A2 (hand-crafted submission carrying `grant`/`merge-gated` from reviewer/pair seat ⇒ `seat-scope` bounce pre-append); S3-A3 (forged FROM/ROLE/delivery_state payload values ignored byte-for-byte); S3-A4 (below-floor HUMAN_GATE pick bounces; known-A B-pick ⇒ A + `gate_category_raised` recorded, never delivered as B); F-P4 sweep (one unknown-token leg per enum field — ten legs, incl. `record_kind` unknown ⇒ reject); S3-G6 (GRILL_REQUIRED rendered per predicate; OR/MAX legs); S3-G7 (lock-bearing candidate `GRILL_REQUIRED: yes` + `DESIGN_LOCK_ID` present + no GRILL_LOCK_ID ⇒ bounce; the grill-ordering shape — yes, no lock fields — passes); S3-RR1 (tampered/garbage digest ⇒ `re-render`; absent digest ⇒ `re-render`); S3-RR1b (digest echoed from a different tier's render ⇒ `re-render`); the old-API deletion compiles the tree (assertion-equivalent migration of fieldspec_test.go invariant legs, flagged per the global constraint). Run — FAIL.
- [ ] **Step 2: implement + delete the dialect + migrate the flagged tests**; **Step 3: full suite green + commit** `s3 IMPL: registry-driven validate, live digest, full enum sweep; 6-enum dialect deleted (S3-A2..A4, G6/G7, RR1/RR1b)` (dialect deletion + test migration may ride a second reviewable commit).

### Task 6: Runtime tables — the hot path off full-store rescans (D-7; F-P1; S3-P1)

**Files:** Create `internal/tables/tables.go`, `tables_test.go`; modify `internal/obligation/obligation.go` (CompleteAuto over tables; BuildTables delegates), `internal/engine/loop.go` (completeTurn maintains tables + passes them), `internal/engine/submit.go` (classifyVerdict + validateRecordKind owed legs over tables), `internal/recover/recover.go` (phase-3 table build handed to the loop).
**Interfaces produced:** `tables.T{ByRelay, ByDispatch map[string][]record.Record; AcceptedParents map[string]string; Grants, Verdicts, Locks, MergeGates map[string][]record.Record; Waivers []record.Record; Completion (the S2 obligation indexes); ParkedLanes; OutcomeByIntake; ContentHash}`; `tables.Build(st *store.Store) (*T, error)` — ONE full read at recovery phase 3; `(*T).OnCommit(rec record.Record)` — incremental, loop-goroutine-only; `obligation.CompleteAuto(st, t *tables.T) error` — sources + completion checks read t, commits still via the store (the ONLY writes); `engine.Loop` holds `*tables.T` (constructed at recovery, passed at assembly), calls `OnCommit` after every `Store.Commit`, threads t everywhere the old code called `st.Records()`. **Invariant: after this task, `st.Records()` has zero callers on the live submit/commit path** (recovery + tables.Build + test code only).

- [ ] **Step 1: failing tests** — incremental = recomputed (after N mixed commits through the loop, `tables.Build` from scratch equals the maintained T — deep compare); S3-P1 probe (a counting `store.Records` wrapper: N submits through a live loop ⇒ Records-call count constant in N, not linear — the F-P1 pin); S1/S2 obligation fixtures (C7, H-r3, S2-O5, S2-K2 legs) green UNCHANGED over the refactor — the no-regression proof this refactor rides on. Run — FAIL.
- [ ] **Step 2: implement + refactor**; **Step 3: full suite + `-race ./internal/engine/ ./internal/tables/` green + commit** `s3 IMPL: runtime tables; live path off full-store rescans (F-P1, S3-P1)`.

### Task 7: The lineage engine — all seven §10c rows + the S1 carry (D-6; S3-L1..L7, S3-RR1c)

**Files:** Rewrite `internal/lineage/lineage.go` (+ `lineage_test.go`); modify `internal/engine/submit.go` (engine call carries tables + registry).
**Interfaces produced:** `lineage.Engine{Reg *fieldspec.Registry, T *tables.T}`; `(*Engine).Check(cand record.Record, meta seat.SeatMeta) *Bounce` — the S1 pessimistic `AuthorityBearing` superset KEPT verBATIM as the floor (never narrows); walks, each firing only for its class, blocking only for authority-bearing candidates: **L1 design-review** (DESIGN_LOCK_ID + kind=design-doc ⇒ parent chain: accepted approving DESIGN-REVIEW with matching DESIGN_DOC_ID → same-owner accepted DESIGN; kind=audit-record valid only when no same-owner DESIGN with that doc-id exists in T; kind=direct-override valid only from operator/orchestrator-planner-stamped seats); **L2 pair dispatch** (grant=dispatch-impl from a pair-planner-stamped seat ⇒ parent = accepted approving PLAN-REVIEW → parent = that planner's accepted PLAN whose TO includes the dispatched implementer; **absent parent = structural bounce for this class** — F-P6; operator/orchestrator-planner grants bypass the pair walk); **L3 non-addressee IMPL trap** (IMPL-phase action-claiming candidate must parent a dispatch whose TO includes the submitting seat); **L4 merge-claim** (merge-claim class ⇒ an earlier accepted same-DISPATCH_ID MERGE-GATE grant from the grantor set exists in T); **L5 OUT→IN flip** (ROW_TRUTH_CHECK: required + typed SCOPE_DIFF/FOLD_SCOPE rows ⇒ a path OUT in an earlier accepted same-DISPATCH_ID record flipping IN ⇒ drift bounce); **L6 visibility gate** (orchestrator-planner-stamped broad-SET candidate must carry the run reviewer in TO/CC unless T.Waivers holds an accepted operator-FROM record with non-empty ORCH_REVIEW_WAIVER); **L7 substrate** (parent resolution by RelayID or DispatchID over T — today's semantics, table-backed). `lineage.RealGrantState(t *tables.T) fieldspec.GrantState` — true iff an accepted approving PLAN-REVIEW parented to this planner's PLAN exists (committed records only; the render provider Task 4 stubbed).

- [ ] **Step 1: failing tests** — per row one green + one bounce leg (S3-L1..L7), graphs built by sequential loop submits (never store pokes); L2's edge-absence leg; the conditional-render legs: RealGrantState false ⇒ pair-planner form omits grant AND an api-submitted grant bounces (fill-time + set-membership halves); after the approving PLAN-REVIEW commits ⇒ re-render shows grant, submit passes the walk; S3-RR1c (a candidate-set/lineage-state change alone does NOT invalidate a held digest, and a stale-POSITIVE grant submit — state changed adversely after render — is rejected by the authoritative walk); L7 rebuild leg (tables rebuilt at recovery ⇒ identical verdicts). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: full suite green + commit** `s3 IMPL: the §10c lineage engine over tables; grant-narrowing carry landed (S3-L1..L7)`.

### Task 8: Serving the form — the describe-grade response (D-4 serving; S3-E2)

**Files:** Modify `internal/channel/server.go` (+ `capability_test.go`).
**Interfaces produced:** `tools/descriptions` becomes per-connection: for an authed seat, the response carries `{tools: [names], submit_schema: <the rendered Form>, form_digest: <digest>}` computed handler-side via `reg.Render(env, seat, req.Phase, req.Tier, grants)` — Step-1 wire grain: the describe call's request body carries `{phase, tier}` (render inputs; SeatMeta always from the connection, never payload); static `toolDescriptions()` dies; read-only surfaces describe `{project, read}` with no submit_schema; I-PH: the schema payload greps path-free (candidate sets fine).

- [ ] **Step 1: failing tests** — S3-E2 (scripted seat over a real socket: describe ⇒ form + digest; submit echoing that digest ⇒ accepted; describe-as-pair-seat shows no grant/merge-gated; describe on Diagnostics surface has no submit_schema; schema payload path-free grep); stale-echo leg through the real channel (digest from an earlier describe after a tier change ⇒ `re-render` frame). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s3 IMPL: describe-grade form serving over the socket (S3-E2)`.

### Task 9: schema_version migrators — registry, read-time apply, refusal legs (D-9; S3-V1..V5)

**Files:** Create `internal/migrate/migrate.go`, `migrate_test.go`; modify read-side consumers per the m-1-confirmed wrap point (proposal: a `migrate.Reader` facade in front of `store.Read`/`Records` consumers in engine + projections; store verbs untouched).
**Interfaces produced:** `migrate.Registry{Register(from int, fn func(record.Record) (record.Record, error)); Current int /* = 1 */}`; `migrate.Apply(reg *Registry, rec record.Record) (record.Record, error)` — rec.SchemaVersion == Current ⇒ identity; < Current ⇒ chain-walk to Current (view only, stored bytes never mutate); > Current ⇒ `ErrFutureVersion` (typed refusal); == 0 ⇒ `ErrUnversioned` (corrupt-class, distinct from ErrChecksum); chain gap ⇒ `ErrMigrationGap`. Production registers ZERO migrators (§9 verbatim; the additive-carrier decision means the major-bump trigger did not fire — stated in the package doc). Submit-side: the courier stamps SchemaVersion=Current unconditionally (payload value ignored — S3-V5).

- [ ] **Step 1: failing tests** — S3-V1 (a test-registered v1→v2 migrator: a v1 record reads as the v2 view; stored bytes byte-identical before/after); S3-V2 (future version ⇒ ErrFutureVersion, never silent coercion); S3-V3 (version 0 ⇒ ErrUnversioned); S3-V4 (registered v1→v2 but Current=3 with no v2→v3 ⇒ ErrMigrationGap); S3-V5 (submit payload claiming schema_version 99 ⇒ committed record carries Current); refusal error texts path-free. Run — FAIL.
- [ ] **Step 2: implement + wire the facade**; **Step 3: green + commit** `s3 IMPL: migrator registry + read-time apply + refusal legs, zero production migrators (S3-V1..V5)`.

### Task 10: Assembly + end-to-end (D-1/D-4 wiring; S3-E1, S3-V6, S3-RR2)

**Files:** Modify `cmd/frank/main.go`, `test/fixtures/main_assembly_test.go` (+ new `test/fixtures/s3_e2e_test.go`).
**Interfaces produced:** serve path threads `pinned.Registry` (v2) + `RenderEnv{pinned.Digest}` + `tables.T` + `lineage.Engine` + `RealGrantState` through recovery → loop → surfaces; `-init` unchanged (fresh-store posture — the R-SCOPE claim wording in the CLI/README-adjacent strings).

- [ ] **Step 1: failing tests** — S3-E1 (fresh store: a real relay in the full header vocabulary — ROLE/PHASE/AUTHORITY/DISPATCH_ID/PARENT/DESIGN_*/SUBJECT/body/X- — renders via describe, validates, commits, projects; a second relay parenting it walks L7); S3-V6 (a store genesis-pinned to a DIFFERENT registry, served with the v2 member ⇒ `Diagnostics` — reads serve, submit absent, operator report carries digests never paths — the positive disposition leg THROUGH main); S3-RR2 (restart-with-new-store: seat holds a gen-1 describe digest, conductor restarts on a fresh store with a changed registry ⇒ old-digest submit bounces `re-render`; fresh describe succeeds — assertion message carries the ratified wording). Run — FAIL.
- [ ] **Step 2: wire**; **Step 3: full suite green + commit** `s3 IMPL: assembly on registry v2 end-to-end (S3-E1, S3-V6, S3-RR2)`.

### Task 11: The disposition table + the FULL replay, both legs (D-8; S3-D1..D5)

**Files:** Create `test/replay/harness.go`, `test/replay/dispositions.json`; rewrite `test/replay/replay_test.go`; DELETE `test/replay/classmap.go` + `test/replay/report.md`; generated `docs/sprints/2026-07-04-s3-slice-3/results/disposition-table.md`.
**Interfaces produced:** `dispositions.json` — one row per relay-lint.py assertion anchor: `{anchor: "relay-lint.py:NNN[-NNN]", check, class_10: "10a|10b|10c|unmapped-named", disposition: "dissolved-form|dissolved-lineage|retained|obsolete", surface: "<registry row / L-row / fixture id>", context: "live|reconstructed-observe", obsolete_ground: "<closed-vocabulary token>|null", fixtures: [names]}` — the **:840-848/:850-868/:870-873 rows explicit** (grantor/phase halves → dissolved-form via grant SeatScope + MERGE-GATE visibility; TO-cardinality → retained as a form rule on grant-bearing records; duplicate merge-verdict-line → obsolete, prose-phrase surface); harness: per corpus fixture (the S1 walker pattern reused) a typed-equivalent constructor driving the REAL path (Render→SubmitHandler→lineage over a test store; root-mode fixtures via sequential submits); oracle = `check-relay-lint-fixtures.py` EXPECTED (146 entries): **96 expected-FAIL ⇒ caught (bounce class = the row) or obsolete (ground ∈ the closed vocabulary); 50 expected-OK ⇒ typed equivalent ACCEPTED**; observe-context rows evaluated under `PresentLayers+observe` and labeled with the exact guide string; the generator emits the results/ table + asserts: table-matches-inventory (every assertion anchor in relay-lint.py covered — a line-scan of the frozen file vs the json), report-matches-generated, zero `uncovered-*`, live-tally excludes reconstructed rows, every obsolete ground ∈ {vanished-markdown-channel classes, §8 strict-submit, one-channel-ROLE/FROM} (S3-D5 mechanical scan).

- [ ] **Step 1: failing tests** — S3-D1 (inventory coverage: every assertion anchor has a row; :840-873 present); S3-D2 (results/ table matches generated byte-exact); S3-D3 (all 96 FAIL-side caught-or-obsolete, zero uncovered, observe-context rows labeled exactly); S3-D4 (all 50 OK-side accepted — non-overblocking); S3-D5 (obsolete-ground vocabulary scan; fresh-store qualifier present in the table header text). Run — FAIL (harness absent; classmap tests deleted same commit).
- [ ] **Step 2: author dispositions.json (code-first off the frozen file) + build the harness + constructors** (largest task — commit per disposition family: grammar/enum/addressing → lineage families → the two legs green).
- [ ] **Step 3: full suite green + commit** `s3 IMPL: code-first disposition table + FULL replay, both legs green (S3-D1..D5)`.

### Task 12: Regression floor + sweeps (S3-RE, S3-SWEEP; I-PH extension)

**Files:** Modify `test/fixtures/iph_test.go`, `sweep_test.go` (+ assertions inventory diff-check vs `s2-close`).
**Interfaces produced / asserted:** S1+S2 suites green with invariant assertions byte-identical (diff-check against s2-close; the sanctioned fieldspec_test/classmap exception documented line-by-line in the migration commits); enum byte-exact greps still hold; **single-§J2-source grep: no literal A-set token list outside `registry.json`** (isAGateCategory is dead); I-PH extension: output capture across the FULL suite incl. registry load errors, re-render bounces, migration refusals, the served schema payload ⇒ zero store/config/outbox path-family hits; claim-sweep classes over every new string/doc: fresh-store qualifier beside registry-evolution mentions, replay-label wording, D5 beside exclusivity claims, "proven by the executed replay" framing.

- [ ] **Step 1: failing tests** (extended greps/sweeps red where surfaces missing); **Step 2: fix surfaces**; **Step 3: `go test ./...` FULL PASS + `go vet` + commit** `s3 IMPL: regression floor + claim/I-PH sweeps (S3-RE, S3-SWEEP)`.

---

### Task 13 (process, pair Planner — NOT Implementer work): gates, then dispatch

- [ ] **Step 1:** Implementer PLAN-REVIEW of this plan → approve required (fold + reissue otherwise; a narrow fix round re-parents its lock to the approving DESIGN-REVIEW `s3-form-design-r4-review-implementer`, never an intervening PLAN-REVIEW — the S2 lineage lesson, restated from the PROCEED-TO-PLAN).
- [ ] **Step 2:** m-1 fidelity verdict on the design-§4 packet (routed in parallel by the orchestrator) — **approve ON RECORD in `.relays/s3/` before ANY dispatch** (condition 3); a must-revise folds bounded into design+plan (S2 F-M1 pattern) + narrow re-review before dispatch.
- [ ] **Step 3:** ASK to `s3.orchestrator-planner` (rides the plan-completion SITREP): root `README.md` fresh-store-qualifier line (one claim-honest sentence) — in-fence ruling or defer; NOT edited without the ruling (the orchestrator pre-flagged this ASK).
- [ ] **Step 4:** All conditions green → pair Planner runs the mechanical `SCOPE_DIFF` over this plan's file list vs the dispatch fence → `all-in` required → delegated `DISPATCH IMPL` (TO `s3-form.implementer`, parent = the approving PLAN-REVIEW). Any deviation / OUT-touch / hard trigger ⇒ escalate to `s3.orchestrator-planner`, never through.
- [ ] **Step 5 (at S3 close, separate):** adversarial review panel → REVIEW-FOLD → S3 exit gate (every D-12 fixture green, E2) → SITREP to master → **operator S3-close sign-off** (merge never implied by green fixtures).

## Out of scope (verbatim fence)

The ROADMAP OUT list (MCP wire-up · observe/evidence Step-2 · routing execution Step-3 · S4 consumer-field content · TUI/runtime) · the §7 config-change record (master-ruled OUT; `OI-S3-CONFIG-CHANGE` carries it; a second item of that shape escalates fresh) · concrete `chosen_model`/bucket-proxy R2 negatives (S4/m-4) · root README.md absent an explicit fence ruling (Task 13 step 3) · any m-1 shape decision (PROPOSALS pending fidelity) · any `../master` or `../extracted` write · any locked-design amendment.

## Verification summary

`go test ./...` = the E2 umbrella; `-race` on engine/tables/fieldspec; the disposition table + replay report are committed artifacts under results/. Per-fixture red→green commands per task. Evidence level at S3 close: E2 (local fixtures green, fresh-store qualified); deploy/live-verify remain E3/E4, outside S3 (stated, not skipped); merge = the operator's separate S3-close gate.
