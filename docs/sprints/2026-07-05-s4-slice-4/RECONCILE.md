# Sprint s4 — reconciliation ledger

Append-only. Each entry: date, what was reconciled, agreement/disagreement/coverage-delta, evidence level, disposition.

## 2026-07-05 — s4 stood up: orchestrator onboarded (boot ACK recorded here per the s2/s3 pattern), scaffold + boots issued; AUDIT dispatch pending

- `s4.orchestrator-planner` booted per `../master/relays/boot/s4-boot-orchestrator-planner/SITREP-orchestrator-planner-20260705-000914.md`; work dispatch of record = `../master/relays/s4-dispatch/PLAN-orchestrator-planner-20260705-000914.md` (VP pre-handoff APPROVE, four watchpoints VP-W1..W4 folded inline).
- **Boot acknowledgment (the boot relay's required items, on record):** identity = `s4.orchestrator-planner`, RUN_ID `s4`; skill loaded = `orchestrator-planner` (+ protocol.md v2.8.8 + sprint-doc-setup); `frank/` reachable, sprint tree + `.relays/s4/` substrate live; the one-line boundary held (BRIDGE + §7 record against LOCKED contracts; guardrail surface stays exactly `submit`/`project`/`read`; the shim adds affordance, never authority); the guide(m-7)/fidelity(m-1)/consult(m-2) split; the F2 gate + OUT-escalation set; the E3-scoped-to-transport honesty line (done-state + `record_integrity` stay `self_reported` until Step-2); the §7 inheritance from the s3-scope-q1 ruling (m-7 guides · m-1 fidelity on `record_kind` · crash-matrix gains the class · existing store, never re-genesis); VP-W1 second-connect pre-constraint (one active channel per credential; reject or proven-dead recovery only; more = locked-contract touch = escalate).
- Onboarding evidence (orchestrator's own reads/runs this session): ARCHITECTURE §C4.1–C4.3 + owed-fixture ledger read (E1); m-7 design-of-record read — §2 loop, §7 trusted config (:109, the config-change record), §8 attach/pipe lifecycle + guardrail, §12 seam matrix, §13 fixtures (E1); m-1 design-of-record read — §4 invariants/DI family, §5 API, §6 system-field contract + conductor-internal provenance, §13.3 credential-lifecycle carry (E1); s3-scope-q1 ruling read (the five conditions; condition 4 = the split this slice inherits) (E1); all THREE sprint ledgers read in full (S1/S2/S3 RECONCILE.md) incl. the S1 grant-narrowing ratification, the S2 single-intake-writer + checksum-fail-stop convergent findings, the S3 fragility set F-P1..F-P6 and the five-chain close (E1); source read at the integration surface — `internal/channel/server.go` (whole), `internal/seat/binding.go` (whole), `cmd/frank/main.go` (whole, incl. channelTools/Describe/turnContextForSeat/mintSeat/operatorSubmit) (E1).
- **Baseline battery re-verified (E2, own uncached runs this session):** `go test -count=1 ./...` = **20 packages ok** + `go vet ./...` clean + `go test -race -count=1` green on channel/seat/engine/intake, at HEAD 50290e1 whose code surface = tag `s3-close` exactly (docs-only delta verified by `git diff --stat s3-close..HEAD` = 1 ledger file).
- **Onboarding probe surfaces recorded for the AUDIT dispatch** (orchestrator observations, E1 — to be independently audited by the pair, not pre-judged): (a) nudge delivery is BROADCAST to all connected clients (`server.Push`/`broadcast`) vs the locked per-recipient pipe write (m-7 §8.3) — S2's F-4 design-awareness note now becomes s4's build surface; (b) the `recovery-nudge` frame carries the ALL-seats pending list to every client, and the global `pending` slice is flushed to every newly-authed client and never cleared (cross-seat metadata + unbounded growth); (c) no second-connect constraint exists (`handle()` accepts any number of `session/connect` with the same credential — VP-W1 is unbuilt, as expected); (d) no credential rotation/revocation surface (m-1 §13.3 carry); (e) credential custody: `-mint` prints to stdout, `-operator-submit` takes `-credential` as a CLI flag (ps-visible — custody-posture doc surface); (f) `bufio.Scanner` default 64KB frame ceiling both sides of the socket (describe-grade form payloads + large `project` results are the risk class); (g) `safeError` collapses tool errors to a path-free bounce (I-PH-positive baseline) but `auth:*` errors and outcome JSON are distinct surfaces the I-PH matrix must cover.
- Scaffold: sprint tree + `.relays/s4/` substrate created (sprint-doc-setup); scaffold commit main@56a19ec (docs-only). Boot relays issued to `s4-wire.planner`, `s4-wire.implementer`, `s4.orchestrator-reviewer` (report-only; no work authority; all three lint-clean exact-file; root-mode clean, INDEX noise exempt per the s1/s2/s3 convention). Single-pair granularity chosen per the s1/s2/s3 precedent: the shim + lifecycle + §7 record share the channel/seat/engine surface — a second pair would collide, not parallelize.
- Standing state: operator hand-relays the three boots; the AUDIT dispatch follows (no operator hold stated in the s4 boot or dispatch — the S3 no-hold precedent; issuing after the boots are relayed so the pair's independent-audit geometry is clean).

## 2026-07-05 — s4-wire AUDIT dispatch issued (same session, s3 no-hold precedent)

- `s4-wire-audit/AUDIT-orchestrator-planner-20260705-010636.md` — paired independent audits, read-only, TO both pair seats, CC s4.orchestrator-reviewer + operator (broad-SET visibility). Scope: S1–S3 onboarding proof (own uncached battery); spec-to-exit-gate map (m-7 §8 + §7 :109, m-1 §4/§5/§6/§13.3, §C4.1/§C4.3, the s3-scope-q1 conditions); six 4-bucket verdicts; duplicate/reuse gate with named candidates (channel.Client, test/seatproc, -operator-submit/-mint, the S3 describe render, the crash harness, the owed mechanism, store.Init/phase-0); m-1/m-7 surface enumeration incl. the §7 mutation-class gains; eight claim-boundary probes (a)–(h) incl. broadcast-nudge vs per-recipient pipe, pending-flush leak, VP-W1 ground truth, custody posture, frame ceilings, the I-PH bridge-surface census, MCP fidelity vs the bespoke frame dialect; second-connect proven-dead ground truth; the honesty-line census [VP-W2]. OUT fence restated with name-and-escalate discipline. Lint: exact-file OK + root-mode OK (INDEX noise exempt), my runs.
- Standing state: operator hand-relays the three boots, then the AUDIT dispatch; pair audits return here for reconciliation; DESIGN dispatch follows reconciliation per the F2 lifecycle.

## 2026-07-05 — boot ACKs + paired audits reconciled: FULL AGREEMENT, zero contradictions; live-probe depth adopted; five fresh fragility findings + one operational finding adopted as design constraints; single-pair granularity confirmed on audit evidence; guide thread routed; DESIGN dispatched (GRILL_REQUIRED: yes)

**Boot ACKs (all three seats online, all lint-clean, my runs):** reviewer `boot/s4-boot-orchestrator-reviewer/SITREP-orchestrator-reviewer-20260705-011245.md` (identity, visibility-gate posture, E2 reachability checks); implementer `boot/s4-boot-s4-wire-implementer/SITREP-implementer-20260705-011355.md` (full acknowledgment checklist incl. VP-W1/E3-honesty/§7 inheritance; own battery run at boot); planner `boot/s4-boot-s4-wire-planner/SITREP-planner-20260705-012400.md` (full checklist). Trail notes, no action: the planner session self-reported initially mis-reading the orchestrator boot before locating its own — no writes under the wrong identity, transparent correction (recorded, welcome); the implementer minted a fresh `-ack` DISPATCH_ID vs the others reusing the boot id (both shapes acceptable, s1 precedent); the planner's audit return minted child id `s4-wire-audit-planner` parented to `s4-wire-audit` vs the implementer reusing the dispatch id (same disposition); pair audit returns CC'd per seat judgment (audits are pair-seat relays, not broad-SET — no reviewer-CC obligation; no action). Reviewer has not yet filed a RECONCILE on the audit dispatch — own-cadence per the visibility gate, not a hold.

**Audit inputs (both lint-clean, my runs):** implementer `s4-wire-audit/AUDIT-implementer-20260705-012253.md`; planner `s4-wire-audit/AUDIT-planner-20260705-013000.md`. Independence statements on both; the planner's directory-state check corroborates non-coordination. Battery: **three independent verifications at main@6987367** (implementer's, planner's, mine — each uncached + vet; planner + mine with race legs).

**Agreement (full):**
- **Zero blocking spec gaps both sides.** Every exit-gate line grounds in locked text (planner filed the complete line-by-line map). One planner grounding note ADOPTED: the VP-W1 one-active-channel rule and the E3-transport scoping live in the s4-dispatch/boot text, not in locked m-1/m-7 (which are silent on concurrent connections per credential) — proper pre-constraint by the dispatching authority; **DESIGN cites the s4-dispatch as the rule's source of record.**
- All six IN items **still-open** with matching promote-don't-rebuild inventories: `channel.Client` IS the shim's conductor-side half (dial/auth/describe/call/push); the S3 describe-grade render is whole server-side (the shim owes presentation + drift surfacing only); the S1/S2 crash harness takes the §7 class (never a parallel harness); the live owed mechanism carries the OI-S3-CONFIG-CHANGE disposition (S2 real-store discharge = the worked pattern); `store.Init` config pinning + phase-0 `ValidateGenesis` = the surface the §7 record mutates against; `test/seatproc` = stub only, not substrate.
- **All eight claim-boundary probes (a)–(h) CONFIRMED both sides** — the planner at live-socket E2 grain (scratch store outside the repo; probes P4–P10): second `session/connect` on a live in-use credential is ACCEPTED today (VP-W1 open, as expected); nudge broadcast observed live incl. `recovery-nudge` carrying the ALL-seats list to a non-recipient channel (metadata-grain cross-seat side channel; divergence from locked m-7 §8.3 per-recipient grain — the gate line is grounded, the code is not yet at the locked grain); global `pending` never cleared, replays to every future auth; no rotation/revocation (m-1 §13.3 carry); custody = mint-stdout + ps-visible CLI flag + plaintext-at-rest 0600 binding table; 64 KiB scanner ceiling both sides with **silent-connection-death** failure mode; I-PH: **zero path hits over 12,710 captured seat-facing bytes** across every existing surface class (live scan) — the un-fixtured class is the SHIM'S OWN diagnostics (Go's default dial error carries the socket path verbatim — the exact leak I-PH exists to prevent; shim must scrub/type its own errors).
- **MCP verdict identical both sides:** the S1–S3 socket dialect is JSON-RPC-*shaped*, NOT MCP (no `jsonrpc` member, int64 ids with 0-sentinel, bare-string errors, bare-array tools/list, custom describe + nudge methods). The shim translates (initialize handshake, JSON-Schema `inputSchema`, `tools/call` result shape, stdio framing); the conductor dialect stays private behind the shim; **no locked-contract touch** — m-7 §8.1's "conductor is an MCP server" is realized by the composite, exactly as the S3 m-7-consult Q3 anticipated ("MCP inherits at wire-up"). Confirm rides the guide thread.
- **Second-connect ground truth converged:** exactly two honest legs — reject-active-duplicate (typed, path-free; needs the serverConn to retain (a hash of) its resolved credential + a per-credential active-channel index under the server mutex — engine-internal, no new seat-facing surface, no binding-table shape change) and proven-dead recovery (prior conn left `s.clients` on close — kernel close-detection). A wedged-but-alive host is NOT provably dead → reject regime + documented operator remedy (kill the host session); heartbeat/ping would change the frame dialect → guide question, recommended defer. Implementer's plan-honesty line adopted verbatim: any plan claiming second-connect is already implemented is false.
- Spot checks at my seat (E1, this session): `fieldspec/validate.go:24` re-render class live (empty digest also bounces); `store/genesis.go:104-118` ValidateGenesis = static compare, no chain walk (a config change on an existing store today = permanently diagnostics-bricked absent re-genesis — precisely the ruled gap); `f11_test.go:303-317` = 11 classes, no config-change (the LOCKED F11 list :172 names it — the map owes the row).

**Deltas (complementary, resolved):**
- **IN-6 bucket label:** implementer `already-closed` (the mechanism — project/read over committed records — exists; no analytics surface) vs planner `still-open` (the chartered item IS the documentation). Same substance, no contradiction; resolved to **still-open at the chartered grain** (deliverable = the read-path doc + at most a trivial read-only stat), with the implementer's claim-honesty framing adopted (usage evidence = committed records/projections only; any aggregation/analytics = s5 OUT-touch).
- Describe-size measurements: implementer 2,222 B (virtual-overlay test) vs planner 2,413 B (live probe) — different render contexts, both ≈26× under the ceiling; adopted: today's forms are safe, the ceiling risk class is `read` record bodies / `project` growth / candidate-set growth (probe (f)).
- **Planner-only depth adopted as design constraints:** F-W1 silent frame-death (>64 KiB ⇒ both loops just exit — typed failure or raised/chunked framing is lifecycle-hardening work, not an afterthought); F-W2 client push-drop at 16 frames (shim must be poll-first; nudges never presented as reliable); F-W3 **darwin AF_UNIX ≈104-byte socket-path cap — the conductor itself failed to bind on a deep path in the planner's live run, and the default `<root>/frank.sock` join exceeds the cap for deep roots** (ops convention: short socket paths; pre-flight length check = one-line PLAN hardening candidate); F-W4 recovery-nudge staleness (the per-recipient redesign retires the global queue rather than patching it); F-W5 zero-ID dialect ambiguity (pinned in shim dialect handling).
- Implementer-only: the recommended implementation order (adopted as DESIGN input, non-binding); the go-overlay measurement technique (read-only discipline held — noted with approval).

**Granularity call — single pair CONFIRMED on audit evidence** (recorded as argued, per the operator's ask): the audits confirm the §7 record's exit legs run through the live wire (re-render bounce over the shim; OI discharge on the real store the wire-up stands up) and its backstop (before any store is persistent) IS the wire-up moment; the collision surfaces are real where they exist (main.go assembly, f11/applicability harness, README claim surface); and the §7-only bundle is small against the fixed onboarding cost both seats just paid. Escape hatch stands (split at DESIGN/PLAN if evidence flips) — nothing in the audits flips it.

**Question thread routed (operator-carried; reply lands in ../master/relays/ per the s1–s3 pattern):**
- `s4-guide-q1/SITREP-orchestrator-planner-20260705-013107.md` TO m-7.planner — six guide questions: per-recipient nudge grain (the locked §8.3 shape over the existing socket; retiring the global pending queue); §7 mutation-class semantics (config-member byte replacement vs record commit across the restart boundary; phase-0 genesis→config-change chain walk; F11 one-pivot + applicability row); §7 authorship shape (operator-channel submit — recommended — vs conductor-internal `system`-stamped commit); the frame-ceiling remedy class (raise/chunk/typed-failure — engine transport); heartbeat deferral confirm (reject + operator remedy; dialect untouched); the MCP-composite reading of §8.1 confirm (+ drift → `tools/list_changed` mapping, poll-hint-first posture).

**Disposition:** DESIGN dispatched — `s4-wire-design/DESIGN-orchestrator-planner-20260705-013107.md` TO s4-wire.planner (implementer CC'd as design-challenger; formal DESIGN-REVIEW goes TO the implementer per v2.8.5 lineage), carrying ten hard constraints from the reconciled audits + the provisional-pending-guide sections (Q1–Q6). **GRILL_REQUIRED: yes from the start** (the S2 lesson; the trigger profile is real on its own: still-open at medium tier; cross-domain boundary contracts — m-1 identity/credential/custody, the m-7 engine seam, host-runtime integration; hard-to-reverse decisions — the §7 on-store mutation shape + `record_kind` + phase-0 chain semantics, the second-connect regime, the shim dialect + custody posture; multiple downstream choices hanging on the guide thread). Grill fence: no reopening of c1–c6 operator-grilled locks; guide answers enter as resolved rows, never re-asked. The stale README fresh-store sentence is pre-flagged as a PLAN-time fence ASK (S1 ASK-1 / S2 / S3 precedent). Evidence: E1/E2 as cited throughout; my own runs of record.
