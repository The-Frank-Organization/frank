# S2 Slice-2 Implementation Plan — the thickened engine

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (single Implementer seat; implementation starts ONLY on a live `DISPATCH IMPL` per the dispatch conditions). Steps use checkbox (`- [ ]`) syntax for tracking.

**PLAN_LOCK_ID:** `s2-slice-2-plan` · **DESIGN_LOCK_ID:** `s2-slice-2-design` (r2, pair-approved `s2-core-design/DESIGN-REVIEW-implementer-20260704-021603.md`; references GRILL_LOCK_ID `s2-grill-s2-core`) · **Owner:** s2-core · **Date:** 2026-07-04 · **Rev:** r3 (r1 → r2 = plan-review blocker F1: the S2-K2 live-read three-layer decomposition, `s2-core-plan/PLAN-REVIEW-implementer-20260704-030434.md`; r2 → r3 = the m-1 fidelity folds F-M1-1..3, `s2-fidelity-m1/SITREP-implementer-20260704-034158.md`: exact canonical field homes + `system` provenance/reservation (Tasks 2/3/9/10), the two typed corruption read states (Tasks 3/11), store-root `config/` placement (Tasks 1/2/11); design r3 carries the same folds; r3 → r4 = narrow-review state-label fixes, `PLAN-REVIEW-implementer-20260704-040358.md`: spec-of-record repointed to the folded state with the lineage parent stated, Task 13.2 re-aimed at the post-fold m-1 narrow re-review, "m-1-approved" labels normalized to m-1-prescribed-pending-confirm)
**Goal:** the S1 conductor thickened to the locked engine grain — reified recovery phases 0–4 behind a two-capability split, single intake-writer over segmented journals, genesis-pinned config, checksum quarantine, drained-segment GC (off by default), and the generalized obligation projection whose owed-item instance surfaces and discharges `OI-S1-F11-SWEEP` — with the S2 exit gate green (E2).
**Architecture:** every S1 piece PROMOTED into its phase/mechanism slot (design D-1..D-9); no re-implementation of the already-closed inventory. Design doc = the spec of record; this plan cites it as `D-<n>` / `S2-<id>` (docs/sprints/2026-07-03-s2-slice-2/designs/s2-slice-2-design.md — **the CURRENT rev in-tree is the spec of record (substantive m-1 folds landed at r3 = main@da25aec; r4 = state-label normalization); the r2 pair design-review approve (`…-021603.md`, on design r2 = main@6e3b67f) remains the v2.8.5 lineage parent — the review surface moves with folds, the lineage parent does not**).
**Tech stack:** Go ≥1.22, stdlib only (unchanged from S1). `go test ./...` is the E2 umbrella; crash fixtures reuse the S1 child-SIGKILL harness.

## Global constraints (every task inherits; violations are review blockers)

- Terminal enum byte-exact `{accepted, rejected, held}`; one pivot per mutation class (F11); presence = committed — all unchanged from S1.
- **m-1 shapes are PROPOSALS pending fidelity:** this plan builds to the design-§4 proposed shapes; `DISPATCH IMPL` is blocked until m-1's approve is on record in `.relays/s2/` (dispatch condition 3); an m-1 must-revise folds into this plan (bounded fold + narrow re-review, S1 F-M1-1 pattern) BEFORE any dispatch.
- **Recovery reads ONLY the store** (guide constraint): no docs-file read in any recovery/genesis path; the engine never mints obligation records from out-of-store text.
- **Claim pins (blockers-by-construction in code, fixtures, docs, strings):** "exactly-once EFFECT" never unqualified exactly-once (m-7 :58); GC stated at locked strength (canonical records never GC'd at all; target = drained segments only per GRILL row 1); "guards recorded owed-items only" beside every projection claim (materialize-first); D5 residual beside every exclusivity-shaped claim; I-PH on every NEW surface (diagnostics report, incident text, GC output, owed projection artifact).
- **S1 invariant assertions stay byte-identical** (S2-RE): mechanical call-site migration only (inventory in Task 7/Task 2 steps); any S1 assertion edit is a review blocker.
- Promote-don't-rebuild: the audits' already-closed inventory is binding; re-implementing staging cleanup / RebuildProjections / Unconsumed / binding restore / gate.Complete semantics / record.Verify / the crash harness / WriteFileAtomic / content-hash dedupe is a duplicate-gate violation.
- Crashpoint discipline: every new registry name lands in the SAME commit as its live `crashpoint.Hit` site, and the F11 names-live-in-source file list extends in that commit (the pin stays green task-by-task).
- Scope fence: nothing from the ROADMAP OUT list; no `../master` or `../extracted` write; **root `README.md` is NOT in this plan's file list** — its S2 honesty delta is flagged as an ASK to the orchestrator (Task 13), the S1 ASK-1 precedent, not edited without an explicit fence ruling.
- Commits: small, per green step, on `main`, message prefix `s2 IMPL:`.

## File structure (the decomposition of record; SCOPE_DIFF enumerates exactly these)

```
internal/config/config.go + config_test.go        # pinned-config load + canonical manifest digest (Task 1)
internal/store/genesis.go (+ store_test.go ext)    # store.Init, genesis record, phase-0 validation core (Task 2)
internal/store/quarantine.go + quarantine_test.go  # checksum scan, evict, incident (Task 3)
internal/store/store.go                            # Modify: typed checksum error w/ relay id; redo append → segmented (Tasks 3,4)
internal/store/projections.go                      # Modify: canonical-driven rebuild (Task 6)
internal/intake/journal.go + journal_test.go       # Modify: segmented journal + rotation (Task 4)
internal/intake/writer.go + writer_test.go         # single intake-writer goroutine (Task 5)
internal/seat/binding.go (+ binding_test.go ext)   # Modify: reserved-name "system" mint-reject (Task 2, r3 F-M1-1)
internal/gate/derived.go + derived_test.go         # Modify: canonical-sufficient outbox body; refactor onto obligation engine (Tasks 6,8)
internal/obligation/obligation.go + _test.go       # the generalized obligation engine + runtime tables (Task 8)
internal/obligation/owed.go + owed_test.go         # owed-item instance + OPEN.md projection artifact (Task 9)
internal/gc/gc.go + gc_test.go                     # drained-segment collector, marker-first (Task 10)
internal/engine/ready.go + ready_test.go           # Ready/Diagnostics capabilities (Task 7)
internal/engine/loop.go, submit.go                 # Modify: Ready-gated construction; owed record_kind validation; obligation maintenance on commit (Tasks 7,8,9)
internal/recover/recover.go + recover_test.go      # Rewrite: the phase machine (Task 7)
internal/channel/server.go                         # Modify: surface-typed toolset (full vs read-only registry) (Task 7)
internal/crashpoint/crashpoint.go                  # Modify: new names, per owning task (Tasks 3,4,7,10)
cmd/frank/main.go                                  # Modify: assembly + -init + operator-submit client mode (Task 11)
test/fixtures/s2_*.go (per task)                   # S2-* fixtures; f11_test.go readSource list extensions
test/fixtures/applicability_map.go                 # the reviewed class×point applicability artifact (Task 12)
test/fixtures/f11_test.go, f9/f10/iph/sweep tests  # Modify: call-site migration ONLY + S2 sweep/I-PH surface extensions (Tasks 7,12)
test/replay/*, test/seatproc/testseat.go           # Modify: call-site migration only if signatures reach them (Task 7 inventory)
docs/sprints/2026-07-03-s2-slice-2/plans/s2-slice-2-plan.md    # this plan (pair-Planner-committed)
docs/sprints/2026-07-03-s2-slice-2/results/        # gate artifacts: sweep output, OI record + disposition copies (Tasks 12,13)
```

Fixture-id → task map: Task 1 {digest determinism (S2-V2 core)} · 2 {S2-V1, S2-V2} · 3 {S2-K1, S2-K2 recovery legs} · 4 {S2-W3} · 5 {S2-W1, S2-W2} · 6 {canonical-sufficiency legs of S2-X2/S2-O5} · 7 {S2-V3, S2-PM1, S2-PM2 + migration inventory} · 8 {S2-O5, S2-K2 **primitive leg only** (loop-executed disposition given an enqueued command)} · 9 {S2-O1, S2-O2, S2-O4} · 10 {S2-X1, S2-X2, S2-X3} · 11 {assembly re-run of V3/PM2, **S2-K2 live leg e2e** (read-surface wiring — r2 F1)} · 12 {S2-F11 (the OI-S1-F11-SWEEP discharge), S2-O3, S2-RE, S2-SWEEP, I-PH extension}. **S2-K2 live behavior is NOT claimable green before Task 11 lands** (r2 F1 rule).

---

### Task 1: Pinned config + canonical manifest digest (D-3)

**Files:** Create `internal/config/config.go`, `internal/config/config_test.go`.
**Interfaces produced:** `config.Load(members map[string]string) (*Pinned, error)` — members = named artifact paths; **at SERVE/recovery time the members are ALWAYS the store-root copies `<root>/config/fieldspec/registry.json` + `<root>/config/engine.json` (r3 F-M1-3) — operator-supplied outside paths are `store.Init` SOURCES only, never a phase-0 dependency**; `Pinned{Members map[string][]byte, Engine EngineConfig, Digest string}`; `EngineConfig{GCEnabled bool, SegmentRotateBytes int64}` parsed from the `engine` member (JSON: `{"gc_enabled": false, "segment_rotate_bytes": 4194304}` — GRILL row 2 defaults; a missing `engine` member is a load error, not a silent default); `config.Digest(members) string` = SHA-256 over the canonical manifest: member names sorted, each contributing `name + "\x00" + hex(sha256(bytes)) + "\n"` (D-3 deterministic-input sharpening). The registry member keeps loading through the existing `fieldspec.Load` (no registry refactor — S3-OUT).

- [ ] **Step 1: failing tests** — same member set in any map order ⇒ identical digest; one byte flipped in any member ⇒ digest changes; missing `engine` member ⇒ typed load error; defaults file parses to `{false, 4194304}`; digest string contains no member PATH (names only — I-PH grain). Run: `go test ./internal/config/ -v` — FAIL (package absent).
- [ ] **Step 2: implement.**
- [ ] **Step 3: green + commit** `s2 IMPL: pinned config + canonical manifest digest (D-3)`.

### Task 2: Genesis — store.Init + phase-0 validation core (D-3; S2-V1, S2-V2)

**Files:** Create `internal/store/genesis.go`; extend `internal/store/store_test.go`; modify `internal/seat/binding.go` + `binding_test.go` (r3).
**Interfaces produced:** `store.Init(root string, sources map[string]string) error` — (r3 F-M1-3) **materializes each source's bytes under `<root>/config/…` FIRST** (registry → `config/fieldspec/registry.json`, engine → `config/engine.json`; via `fsio.WriteFileAtomic`), computes the digest over the store-root copies, then commits the genesis record via the standard pivot as the FIRST canonical record — **the exact m-1-fixed shape (r3 F-M1-1):** `Envelope{RelayID: "genesis", DispatchID: "genesis", From: "system", Role: "system", DeliveryState: record.Accepted, SchemaVersion: 1}`, `Headers{"record_kind": "genesis", "config_digest": <digest>, "address_space_seed": <128-bit random hex + the literal operator address>, "created_ts": <ts>}` — NO `schema_version` header (envelope-only home); on an existing genesis ⇒ typed `ErrGenesisExists`, **nothing written, store byte-identical**; `store.Genesis() (record.Record, error)` — typed `ErrGenesisMissing`; `store.ValidateGenesis(pinned *config.Pinned) error` — recomputes over the STORE-ROOT config members, nil | `ErrGenesisMissing` | `ErrDigestMismatch{Want, Got}` (digest values, never paths). **Reserved provenance (r3 F-M1-1):** `seat.Mint("system", …)` ⇒ typed `ErrReservedSeatName`; no binding may hold the name, so no channel stamp can ever produce `From: system` — internal records alone carry it.

- [ ] **Step 1: failing tests** — S2-V1 (Init ⇒ `config/` members materialized + exactly one `records/genesis.json` with the exact m-1 envelope/header shape asserted field-by-field (incl. NO `schema_version` header), one canonical rename (counter); re-Init ⇒ `ErrGenesisExists` + `records/` AND `config/` hash-trees byte-identical before/after); S2-V2 (Init from sources, ValidateGenesis(store-root pinned) nil; mutate a STORE-ROOT config member ⇒ `ErrDigestMismatch`; mutating the original OUTSIDE source file after Init changes nothing — phase-0 reads only the store; mismatch error text carries both digests, no path); reserved-name legs (`Mint("system")` ⇒ `ErrReservedSeatName`, binding table unchanged; existing S1 mint legs untouched). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s2 IMPL: genesis record + Init idempotence + digest validation (S2-V1, S2-V2)`.

### Task 3: Quarantine replaces fail-stop — scan, evict, incident (D-5; S2-K1 + S2-K2 recovery legs)

**Files:** Create `internal/store/quarantine.go`, `internal/store/quarantine_test.go`; modify `internal/store/store.go` (typed error), `internal/crashpoint/crashpoint.go` (+2 names).
**Interfaces produced:** the two typed corruption states (r3 F-M1-2): `store.ErrChecksum{RelayID string}` returned by `Records()`/`Read()` on a corrupt record STILL PRESENT in `records/` (the detection event; Task 8 wires the live disposition), and `store.ErrQuarantined{RelayID, IncidentID, FailureClass string}` returned by `Read()` for a record already evicted to `quarantine/` (the stable post-disposition state — never `ErrChecksum` for it; `IncidentID` populated when the incident record exists); `Read("incident-<id>")` serves the HELD incident record normally; `Records()` + projection rebuilds operate over clean canonical records only (quarantined bytes never re-included); `store.ScanQuarantine() (evicted []string, err error)` — verifies every `records/*.json`; mismatch ⇒ `crashpoint.Hit("pre_quarantine_evict")`, one `os.Rename` into `quarantine/<relay_id>.json` (name-preserving), `crashpoint.Hit("post_quarantine_evict")`; `store.QuarantineOne(relayID string) (evicted bool, err error)` — the TARGETED single-record evict (same crashpointed rename; idempotent: already-evicted/absent ⇒ `(false, nil)`) — the primitive Task 8's loop turn and phase 1 both call; `store.CompleteIncidents() error` — for every `quarantine/` member with no incident record, commit ONE compound HELD-class incident record (`relay_id: "incident-<quarantined id>"`, `delivery_state: held`, headers `{record_kind: incident, quarantined_ref, failure_class: checksum-mismatch}` — §4.4 PROPOSAL) via the standard pivot — idempotent by the quarantined relay_id (the F2-fold file-backed durable intent: evicted-file-without-incident is the source fact); incident reason text flows through `bounce.Format` (I-PH). Crashpoint names `pre/post_quarantine_evict` + Hit sites land here; f11 `readSource` list gains `quarantine.go`.

- [ ] **Step 1: failing tests** — S2-K1 (corrupt one committed record's bytes ⇒ Scan evicts it to `quarantine/` + CompleteIncidents commits exactly one incident; run both again ⇒ no second incident, no error; child-SIGKILL at `pre/post_quarantine_evict` ⇒ re-run converges to exactly one evicted file + one incident); S2-K2 recovery legs (with one corrupt record among N: Scan+Complete succeed, the other N−1 records all `Read` clean, `Records()` post-scan returns N−1 with no error — the store never bricks; post-eviction `Read` of the quarantined id ⇒ `ErrQuarantined` carrying the incident id + `failure_class: checksum-mismatch` (r3 F-M1-2), and `Read("incident-<id>")` serves the held incident; incident text grep ⇒ no store-root path). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s2 IMPL: checksum quarantine + compound incident, crash-safe (S2-K1)`.

### Task 4: Segmented journals + rotation (D-2; S2-W3)

**Files:** Modify `internal/intake/journal.go` (+ tests), `internal/store/store.go` (`appendRedo` → segmented), `internal/crashpoint/crashpoint.go` (+2 names).
**Interfaces produced:** intake layout `journal/intake/<seq>.jsonl` (zero-padded 6-digit, highest = active; §4.7 PROPOSAL), redo mirror `journal/redo/<seq>.jsonl`; `Journal.Append` unchanged signature — internally appends to the active segment and rotates when the active segment exceeds `SegmentRotateBytes` (`crashpoint.Hit("pre_segment_rotate")`, create next segment, `crashpoint.Hit("post_segment_rotate")`); `Journal.ReadAll` reads all segments in sequence order (unchanged signature — S1 fixture call sites untouched); `Journal.Segments() ([]Segment, error)`, `Segment{Seq int, Path string, Entries []Cmd}` (GC consumes this in Task 10); rotation NEVER splits an entry. `intake.Open` migrates nothing: a legacy single `journal/intake.jsonl` is absent in every real store (repo-verified; no grandfathering — GRILL codebase row), so Open simply creates `journal/intake/` — but if the legacy file EXISTS (S1-era fixture dirs), Open returns a typed error naming the incompatibility (fail-closed, honest; no silent migration).

- [ ] **Step 1: failing tests** — S2-W3 (rotate-bytes set tiny (256) via config ⇒ N appends produce >1 segment; every entry present exactly once across segments in append order; child-SIGKILL at `pre/post_segment_rotate` ⇒ recovery reads all entries exactly once, active segment consistent); `Unconsumed` returns arrival order across a segment boundary; legacy-file presence ⇒ typed error. Run — FAIL.
- [ ] **Step 2: implement** (redo mirror: same rotation mechanics, same config value); **Step 3: green + commit** `s2 IMPL: segmented intake+redo journals, crash-safe rotation (S2-W3)`.

### Task 5: The single intake-writer (D-2; S2-W1, S2-W2)

**Files:** Create `internal/intake/writer.go`, `internal/intake/writer_test.go`.
**Interfaces produced:** `intake.NewWriter(j *Journal, cfg config.EngineConfig) (*Writer, error)` — initializes the monotonic `intake_id` counter + content-hash map from `j.ReadAll()` ONCE; `(*Writer).Run(ctx, out chan<- engine.Job)` — the ONE goroutine that touches the journal: consumes a **bounded** request channel, per request: dedupe by content-hash (return the existing id + a replayed-outcome path), assign `intake-%06d`, append+fsync (existing `post_intake_fsync` fires inside Append), then emit the `engine.Job` to `out` **in journal order**; `(*Writer).Submit(ctx context.Context, cmd Cmd) (reply <-chan engine.Outcome, intakeID string, err error)` — what channel handlers call; **handlers never touch any file** (m-7 :52 verbatim — the D-2 discipline). No public API on `Journal` is removed (S1 fixtures keep compiling); `Journal.Append` stays for the Writer's own use and single-writer test setup.

- [ ] **Step 1: failing tests** — S2-W1 (N=8 goroutines × M=25 submits through `Writer.Submit` concurrently ⇒ journal ids gap-free monotonic `intake-000001..NM` (duplicates deduped), journal file order = job emission order; run under `-race` — the S1 ReadAll+len race is structurally gone); S2-W2 / F9-concurrent (child process: concurrent multi-seat submits streaming through Writer→loop, SIGKILL mid-stream via `FRANK_TEST_CRASHPOINT=post_intake_fsync:K` ⇒ recovery re-enqueues exactly `intake − outcomes` in arrival order, zero re-emission of any outcome'd entry — the exactly-once-EFFECT wording in every assertion message). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + `go test -race ./internal/intake/ -v` PASS + commit** `s2 IMPL: single intake-writer, ordered + race-free (S2-W1, S2-W2)`.

### Task 6: Canonical sufficiency — derived records embed payloads; rebuild goes canonical-driven (D-6)

**Files:** Modify `internal/gate/derived.go` (outbox record body), `internal/store/projections.go` (`RebuildProjections`), tests.
**Interfaces produced:** the derived outbox record's `Body` = the OutboxItem JSON (the file payload becomes a pure function of the canonical record — D-6; the projection file is byte-regenerable from `Body`); `RebuildProjections` becomes canonical-driven: iterate committed records → ensure INDEX row / rendered `.md` / mailbox lines exist (recomputed via `DefaultProjectionIntents`) AND every outbox-record's `outbox/<item>.json` exists (regenerated from `Body`); surviving redo segments replayed first as the fast path; canonical wins on conflict; INDEX append-only preserved (S1 C5 assertions untouched).

- [ ] **Step 1: failing tests** — commit a gate-derived outbox item, then DELETE `journal/redo/` entirely AND the `outbox/*.json` file ⇒ `RebuildProjections` regenerates the outbox file **byte-identical** (incl. `created_ts` — it lives in the record body, not recomputed); same for a deleted rendered `.md` + mailbox line with no redo (S1 C5 held only with redo present — this is the strengthened leg); existing C5/F2-shape fixtures still green. Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s2 IMPL: canonical-sufficient derived records + canonical-driven rebuild (D-6)`.

### Task 7: The phase machine + Ready/Diagnostics split (D-1, D-4; S2-V3, S2-PM1, S2-PM2; the migration inventory)

**Files:** Create `internal/engine/ready.go` (+ test); rewrite `internal/recover/recover.go` (+ tests); modify `internal/engine/loop.go` (Ready-gated construction), `internal/channel/server.go` (surface-typed toolsets), `internal/crashpoint/crashpoint.go` (+phase names); migrate call sites (inventory below).
**Interfaces produced:** `engine.Ready` / `engine.Diagnostics` — unexported-field structs minted ONLY by the phase machine (r2 F1 split); `recover.Run(root string, pinned *config.Pinned) (*recover.Result, error)`; `Result{Ready *engine.Ready; Diag *engine.Diagnostics}` — exactly one non-nil; `(*Diagnostics).Report() string` — typed failure class + expected/got digests, built by `bounce.Format` (I-PH; never a path); `engine.New(st, handler, ready *engine.Ready)` — nil `ready` ⇒ panic in construction (the loop is unconstructible without phase 4); `intake.NewWriter` gains the same `ready` requirement; `channel.Surface` = `channel.FullSurface(ready, tools)` | `channel.ReadOnlySurface(diag, projectRead)` — `ServeAuthenticated(sock, mgr, surface)`; the read-only surface's rendered registry = exactly `{project, read}` (no `submit` entry exists — absence, not refusal). Phase order inside `Run`: 0 `ValidateGenesis` (fail ⇒ mint `Diagnostics`, return — phases 1+ never run) → 1 staging cleanup + `ScanQuarantine`+`CompleteIncidents` → 2 `RebuildProjections` → 3 `seat.Open` + build the obligation/runtime tables (Task 8 wires; until then binding only) → 3.5 `Unconsumed` re-enqueue → 3.6 derived-work completion (`gate.Complete`) → 4 mint `Ready`. Crashpoints `recovery_post_phase0` … `recovery_post_phase3_5`, `recovery_post_phase3_6` with Hit sites at each boundary; f11 `readSource` gains `recover.go`.
**Migration inventory (mechanical ONLY; every S1 invariant assertion stays byte-identical — S2-RE blocker):** call sites of `frankrecover.Run(root)` → `recover.Run(root, testPinned())` + unwrap `Result.Ready`: `test/fixtures/f11_test.go:104`, `test/fixtures/f9_c6_test.go`, `test/fixtures/f10_test.go`, `internal/recover/recover_test.go`, `test/fixtures/main_assembly_test.go`; every fixture store setup gains `store.Init(root, testDigest)` before first use (phase-0 now requires genesis unconditionally — no grandfathering); `engine.New(st, handler)` call sites gain the test `Ready` (minted via a recovery run in setup, or the test-only `engine.TestReady(t)` helper — helper lives in `ready.go` guarded by `testing.Testing()`): `internal/engine/loop_test.go`, `pipeline_test.go`, `fault_test.go`, `test/fixtures/f11_test.go:269`. A shared `test/fixtures/s2setup_test.go` helper (`initStore(t) root` = Init+digest) keeps the churn one-line per site.

- [ ] **Step 1: failing tests** — S2-V3 (store WITHOUT genesis, or with a wrong digest ⇒ `Run` returns `Diag`, `Ready` nil; `ServeAuthenticated` with the read-only surface: `tools/list` = exactly `["project","read"]`, a hand-crafted `submit` RPC ⇒ method-not-found class reject; operator surface serves `Report()`; store byte-identical after the whole exchange; report grep ⇒ digests present, zero path hits — BOTH halves in this one fixture); S2-PM2 (compile-level: `engine.New`/`NewWriter` with nil Ready panic — asserted via recover(); runtime probe: no socket listens before `Run` returns); S2-PM1 (child-SIGKILL at every `recovery_*` crashpoint during a recovery over a prepared store (records + unconsumed intake + a gate record missing its park) ⇒ rerun of `Run` converges: same committed set, projections complete, re-enqueue set exact, derived work completed exactly once — no lost/double intake or delivery across any boundary). Run — FAIL.
- [ ] **Step 2: implement + migrate the inventory** (one commit for the machine, one for the mechanical migration — reviewable apart).
- [ ] **Step 3: full suite green (`go test ./...` — every S1 fixture passing with byte-identical assertions) + commit** `s2 IMPL: recovery phase machine + Ready/Diagnostics split (S2-V3, S2-PM1, S2-PM2)` / `s2 IMPL: mechanical call-site migration (S1 assertions byte-identical)`.

### Task 8: The obligation engine + runtime tables + live-path sweep (D-6, D-7; S2-O5, S2-K2 live leg)

**Files:** Create `internal/obligation/obligation.go`, `internal/obligation/obligation_test.go`; modify `internal/gate/derived.go` (refactor onto it), `internal/engine/loop.go` (table maintenance + post-commit obligation turn), `internal/recover/recover.go` (phase-3 table build, phase-3.6 via the engine).
**Interfaces produced:** `obligation.Fact{Kind, Key string}`; `obligation.Class{Name string; Sources func(*store.Store) ([]Fact, error); Completed func(Fact, *Tables) bool; Complete func(Fact, *store.Store) error /* nil ⇒ human-only (owed) */}`; `obligation.Engine{Register(Class); Open(name string) []Fact; BuildTables(*store.Store) error; OnCommit(record.Record)}` — **sources are durable store facts: committed records normally; the CLOSED file-backed pair {quarantine/ member, gc-marker-named still-present segment} only** (r2 F2 verbatim; a third non-record source is a design change); `obligation.Tables{OutcomeByIntake, ContentHash, CompletionIndex, ParkedLanes, …}` built once at phase 3, maintained by `OnCommit` on the loop; `gate.Complete(st)` KEEPS its signature (S1 fixtures untouched) — internally delegates to the engine's park/outbox/incident instances; the loop runs the obligation turn post-commit in the same FIFO turn (replacing main.go's per-submit `gate.Complete` full scans — the F-3 hot path disappears; Task 11 rewires assembly). **The quarantine-disposition primitive (r2 F1 — the three-layer API, stated exactly):** `(*Loop).EnqueueQuarantine(relayID string)` — non-blocking, bounded in-memory internal queue drained between FIFO jobs on the loop goroutine; each drain calls `store.QuarantineOne(relayID)` (Task 3) + completes the incident via the obligation engine. The internal queue is deliberately NOT journaled: recovery phase 1 rescans and quarantines anyway, so durability rides recovery (the safety net), and no reader ever mutates — the reader only ENQUEUES. Task 11 wires the read handler to call this; Task 8 proves the primitive.

- [ ] **Step 1: failing tests** — S2-O5 (the same `Engine` instance completes: gate→park, gate→outbox, held→outbox, quarantine→incident — one mechanism, four registered classes; S1's C7 + H-r3 + the s1-close mirror-leg fixtures run UNCHANGED over the refactored `gate.Complete`); table maintenance (after N commits through the loop, `Open` sets match a from-scratch `BuildTables` recompute — incremental = recomputed); S2-K2 PRIMITIVE leg (r2: corrupt a committed record, call `loop.EnqueueQuarantine(relayID)` directly ⇒ the next loop turn evicts via `QuarantineOne` + commits the incident, all on the loop goroutine (single-writer held — asserted via the loop's own execution, no test-side store write); enqueue for an already-evicted id ⇒ no-op; next recovery finds nothing left to do; the live READ-surface leg is Task 11's, not claimable here). Run — FAIL.
- [ ] **Step 2: implement + refactor**; **Step 3: full suite green + commit** `s2 IMPL: obligation engine, runtime tables, live quarantine path (S2-O5, S2-K2)`.

### Task 9: The owed-item instance (D-7; S2-O1, S2-O2, S2-O4)

**Files:** Create `internal/obligation/owed.go`, `internal/obligation/owed_test.go`; modify `internal/engine/submit.go` (record_kind validation legs).
**Interfaces produced:** owed records ride the NORMAL submit path — header `record_kind: owed_item` with required payload headers `{owner, source, target_surface, disposition_path}` (§4.1 PROPOSAL); disposition = `record_kind: owed_disposition` + `disposes_owed: <owed relay_id>` (§4.2 — the `parks_gate` idiom); submit-time validation (in-loop, authoritative): unknown `record_kind` value ⇒ rejected; `owed_disposition` whose `disposes_owed` resolves to no committed owed record ⇒ rejected (parent-unknown class bounce); a SECOND disposition for an already-disposed owed id ⇒ rejected (the one-shot idiom — reads the first disposition on the serialized loop, the licensed §2.4 property); owed obligation class: source = committed `owed_item` records, completion = `owed_disposition`, **`Complete == nil` — no auto-completer exists** (materialize-first: only a record-authoring principal closes it); projection artifact `projections/owed/OPEN.md` (§4.9 PROPOSAL) — one row per open item `{relay_id, owner, target_surface, disposition_path}` — regenerated on owed/disposition commits + at phase 2 (canonical-sufficient); served to the operator via existing `read`/`project`.

- [ ] **Step 1: failing tests** — S2-O1 (operator-channel submit of an owed record ⇒ accepted; `OPEN.md` gains its row; `Open("owed")` contains it); S2-O2 (disposition commits ⇒ row gone + `Open` empty; second disposition ⇒ typed reject, store unchanged); S2-O4 (fresh store post-genesis ⇒ `OPEN.md` exists empty / projection serves an empty set without error); non-operator seat submitting `owed_item` — ACCEPTED like any record (any record-authoring principal may materialize an obligation; the OI record's operator authorship is a process fact, not an engine restriction — claim-honest: the engine does not pretend an authorship gate it doesn't have); artifact grep ⇒ no path families (I-PH). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s2 IMPL: owed-item record_kind + open-set projection (S2-O1, S2-O2, S2-O4)`.

### Task 10: GC — drained segments only, marker-first, off by default (D-8; S2-X1..X3)

**Files:** Create `internal/gc/gc.go`, `internal/gc/gc_test.go`; modify `internal/crashpoint/crashpoint.go` (+4 names), `internal/engine/loop.go` (GC pass as a loop mutation).
**Interfaces produced:** `gc.Drained(segs []intake.Segment, t *obligation.Tables) []intake.Segment` — a segment is drained iff non-active AND every entry has a committed outcome (intake) / every entry's record is committed (redo); `gc.Pass(st, tables, cfg)` — loop-executed, ONLY when `cfg.GCEnabled` (GRILL row 2: default false): compute drained set → commit ONE gc-marker record (`relay_id: "gc-<seq>"`, `Headers{record_kind: gc_marker}`, `system/system/accepted` envelope per the r3 internal-provenance convention, Body = the collected segment list JSON — canonical-sufficient; §4.6 m-1-approved shape; `crashpoint.Hit("pre_gc_marker")`/`"post_gc_marker"` around its pivot) → unlink each named segment (`pre/post_gc_unlink`); **marker-first: no unlink before the marker's pivot completes**; gc obligation class (F2 file-backed source): marker-naming-still-present-segments ⇒ complete the unlinks at phase 3.6 — crash anywhere mid-GC converges; the pass is enqueued post-open and after each rotation event. **Rendered projections are untouched at any setting — grill row 1, asserted** (the collector's reachable file set is `journal/{intake,redo}/<seq>.jsonl` only).

- [ ] **Step 1: failing tests** — S2-X1 (default config ⇒ `Pass` collects nothing ever; store byte-stable modulo new commits); S2-X2 (enabled + tiny rotation ⇒ only fully-drained segments unlinked; a segment holding ONE outcome-less entry survives; `records/` + `projections/` hash-trees byte-identical before/after; the pre-unlink assert: zero outcome-less entries in every collected segment); S2-X3 (child-SIGKILL at `pre_gc_marker` ⇒ nothing collected, no marker; at `post_gc_marker`/between unlinks ⇒ recovery completes exactly the marker's named set, no more; collected set = marker body exactly). Run — FAIL.
- [ ] **Step 2: implement**; **Step 3: green + commit** `s2 IMPL: drained-segment GC, marker-first, off by default (S2-X1..X3)`.

### Task 11: Assembly — main.go on the new spine (D-1/D-2/D-8 wiring)

**Files:** Modify `cmd/frank/main.go`, `test/fixtures/main_assembly_test.go`.
**Interfaces produced:** flag surface: `-root`, `-socket`, `-registry`, `-engine-config` — (r3 F-M1-3) the two config flags are **`-init`-time SOURCES only**: `-init` runs `store.Init(root, sources)` (materialize under `<root>/config/` → genesis; exit 0 / `ErrGenesisExists` exit 1 with its typed message); the SERVE path ignores outside config paths entirely and loads `config.Load` over the store-root members (phase-0 depends on nothing outside the store), and the operator client mode `-operator-submit <payload.json> -credential <cred>` (dials the socket via the existing `channel.DialAuthenticated`, calls `submit` with the file's JSON payload, prints the typed outcome — the documented one-liner the operator's Task-13 step uses; no new verb, no new protocol); serve path: `config.Load` → `recover.Run` → `Ready` ⇒ Writer + loop + FullSurface + GC-pass hook | `Diag` ⇒ ReadOnlySurface only; the per-submit `gate.Complete` call at old main.go:98 is GONE (the loop's obligation turn owns it); recovery nudge (`PendingDeliverySeats` + `QueuePush`) unchanged. **Read-handler quarantine wiring (r2 F1 — the live half of S2-K2):** the `Read` tool closure, on `store.ErrChecksum{RelayID}`, calls `loop.EnqueueQuarantine(err.RelayID)` and returns the typed path-free `checksum-mismatch` frame; on `store.ErrQuarantined` it returns the typed path-free `record-quarantined` frame carrying `relay_id`/`incident_id`/`failure_class` (r3 F-M1-2 — the two states are distinct at the channel surface too) — the handler enqueues, never mutates (the Task 8 primitive executes on the loop).

- [ ] **Step 1: failing tests** — assembly fixture extends: full path (init → serve → seat submits → outcome + delivery nudge) still green; diagnostics path (serve over a store with a wrong `-registry` content ⇒ read-only registry served, submit RPC absent, diagnostics on operator surface — S2-V3 re-asserted THROUGH main); `-init` twice ⇒ second exits nonzero with `ErrGenesisExists` text (path-free); `-operator-submit` round-trips a SITREP-class payload against a live conductor; **S2-K2 live leg e2e (r2 + r3)**: corrupt a committed record post-open ⇒ a seat's `read` over the channel returns the typed `checksum-mismatch` frame (grep: no path) AND the disposition subsequently commits on the loop (evicted file + incident record present); a RE-read of the same id after disposition ⇒ the `record-quarantined` frame with `incident_id` + `failure_class` (r3 F-M1-2); the record's next recovery is a no-op for it. Run — FAIL.
- [ ] **Step 2: rewire**; **Step 3: green + commit** `s2 IMPL: assembly on the phase machine + operator client mode`.

### Task 12: The applicability map + the full class×point sweep + gate fixtures (D-9; S2-F11, S2-O3, S2-RE, S2-SWEEP)

**Files:** Create `test/fixtures/applicability_map.go`, `test/fixtures/s2_sweep_test.go`; modify `test/fixtures/f11_test.go` (mutation arms: genesis, quarantine-disposition, gc-marker), `test/fixtures/iph_test.go` + `sweep_test.go` (new-surface extensions); results artifact `docs/sprints/2026-07-03-s2-slice-2/results/f11-sweep-report.md` (generated, committed).
**Interfaces produced:** `applicability_map.go` — the REVIEWED artifact: one row per mutation class × `crashpoint.Names()` entry, value ∈ {`crash-expected`, `clean-completion`}; **owed-item and owed-disposition appear as explicit named rows mapped to the submit-accept path** (reviewer carry-forward — visible, never folded into the class name); the sweep test drives EVERY cell: crash-expected ⇒ child SIGKILLs at that point + recovery converges (the f11 assert set); clean-completion ⇒ child exits 0 + store converged (asserted, not skipped). Report artifact = the cell matrix with outcomes (the OI-S1-F11-SWEEP discharge evidence).

- [ ] **Step 1: failing tests** — S2-F11 (the full sweep over {submit-accept, submit-reject, held, operator-verdict, park, outbox-enqueue, genesis, quarantine-disposition, gc-marker} × all registered names per the map; report generated); S2-O3 (end-to-end in a fixture store: operator-channel submit of the REAL OI-S1-F11-SWEEP payload — `owner: s1 (S2 slice)`, `source` citing s1 RECONCILE.md :160-161 + the guide ruling relay id, `target_surface: F11 full class×point sweep`, `disposition_path: S2 exit gate` — surfaces open; after the sweep report exists, a disposition record citing it commits; open set empties); S2-RE (`go test ./...` full suite; S1 assertion files diff-checked against s1-close for assertion-identity — mechanical-migration lines only); S2-SWEEP (claim-class grep extended over every NEW seat-/operator-facing string + this sprint's docs: exactly-once-EFFECT qualifier present, GC wording at locked strength, materialize-first beside projection mentions, D5 beside exclusivity claims, `bounced`/`submitted` still absent as value tokens); I-PH extension (output capture across the FULL suite incl. diagnostics reports, incident records, GC markers, OPEN.md ⇒ zero path-family hits). Run — FAIL.
- [ ] **Step 2: implement arms + map + report generation**; **Step 3: `go test ./...` FULL PASS + commit** `s2 IMPL: applicability map + full class-x-point sweep + gate fixtures (S2-F11, S2-O3, S2-RE, S2-SWEEP)`.

---

### Task 13 (process, pair Planner — NOT Implementer work): gates, the operator step, then dispatch

- [ ] **Step 1:** Implementer PLAN-REVIEW of this plan → approve required (fold + reissue otherwise).
- [ ] **Step 2:** m-1 fidelity — initial verdict RECEIVED (must-revise, `s2-fidelity-m1/SITREP-implementer-20260704-034158.md`) and FOLDED (F-M1-1..3, design/plan r3 @ main@da25aec); remaining: the **m-1 NARROW re-review of the folded lines** (review object = the post-fold design §4 + D-3/D-5 and plan Tasks 1/2/3/9/10/11 at the fold commit), routed via s2.orchestrator-planner — **m-1's approve on record in `.relays/s2/` REQUIRED before any dispatch** (dispatch condition 3).
- [ ] **Step 3:** ASK to `s2.orchestrator-planner` (in the plan-completion SITREP): root `README.md` honesty delta (S2 adds the projection + GC — one claim-honest paragraph) — in-fence ruling or defer to a later slice; NOT edited without the ruling (S1 ASK-1 precedent).
- [ ] **Step 4:** All conditions green → pair Planner runs the mechanical `SCOPE_DIFF` over this plan's file list vs the dispatch fence → `all-in` required → delegated `DISPATCH IMPL` (TO `s2-core.implementer`, parent = the approving PLAN-REVIEW). Any deviation/OUT-touch ⇒ escalate to `s2.orchestrator-planner`, never through.
- [ ] **Step 5 (IMPL-phase, sequenced):** after Task 11 lands, the OPERATOR executes the OI-S1-F11-SWEEP submit (guide Q2=(i)): `frank -operator-submit oi-s1-f11-sweep.json -credential <operator cred>` against the S2 acceptance store — operator-executed, never pair-authored; the committed record + (post-Task-12) its disposition are copied into `docs/sprints/2026-07-03-s2-slice-2/results/` as gate evidence.
- [ ] **Step 6 (at S2 close, separate):** adversarial review panel → REVIEW-FOLD → S2 exit gate (all §3 fixtures green, E2) → SITREP to master → **operator S2-close sign-off** (merge never implied by green fixtures).

## Out of scope (verbatim fence)

The ROADMAP OUT list (S3 registry/linter · MCP live-adapter · observe Step-2 · routing Step-3 · consumer schemas S4) · rendered-projection compaction (GRILL row 1 — dormant, documented) · the §7 config-change record (S3) · outbox drain/external send · README.md absent an explicit fence ruling (Task 13 step 3) · any m-1 shape decision (PROPOSALS pending fidelity) · any `../master` or `../extracted` write.

## Verification summary

`go test ./...` = the E2 umbrella; `go test -race` on intake/engine/obligation; the sweep report + OPEN.md legs are committed artifacts under results/. Per-fixture red→green commands are in each task. Evidence level at S2 close: E2 (local fixtures green); deploy/live-verify remain E3/E4, outside S2 (stated, not skipped); merge = the operator's separate S2-close gate.
