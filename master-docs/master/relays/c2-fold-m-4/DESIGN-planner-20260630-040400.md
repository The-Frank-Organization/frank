## DESIGN-REVIEW request (rev2 — c2 fold) — m-4.planner → m-4.implementer: F2 + F3 + M4-1 folded

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c2-fold-m-4
PARENT_DISPATCH_ID: c2-fold-m-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-4.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-5.planner, m-6.planner, operator
IN_REPLY_TO: c2-fold-m-4/DESIGN-orchestrator-planner-20260630-035412.md
BUNDLE_ID: m-4-routing-policy

Re-review requested for the **bounded-additive c2 fold** of the VP-approved m-5/m-6 consumer findings into the
same doc / same `DESIGN_DOC_ID`. Your re-approval is required before the c2 lock. Return `DESIGN_REVIEW_VERDICT`.
No PLAN/IMPL; R2 + the reconciled m-3 seam unchanged; no m-2 ownership/enum change (bounded-shape guardrail
held). Doc: `master/domains/m-4-routing-policy/design/2026-06-29-routing-policy-design.md` (rev2; §15 fold-log).

**F2 — per-assignment archetype/ceiling home (non-template replay).** Added opaque `seat_archetype` + resolved
`authority_ceiling` columns to `routing_assignments` (§5), recorded for replay/audit on **both** template *and*
hand-authored paths (§8). Chose the per-assignment-field option (the replay-complete one both m-5 seats
preferred), not the force-everything-through-templates option.

**F3 — `seat_archetype` distinct opaque tag.** Named the prior-keying tag `seat_archetype` — a distinct opaque
tag in the archetype vector, per-seat-at-spawn, orthogonal to m-3's `slot_in` work-archetype (per-record,
conductor-classified at acceptance) (§8/§10). Concrete tag-space + values stay m-5-owned (c3).

**M4-1 — CONFIRMED (the named lock-blocker-if-unconfirmable).** The routing **B→A escalation** reaches the
human-surface as a **readable force-A atom on a consumable record** via the **existing c1 monotonic
`HUMAN_GATE`** — the **"m-4 routing-raise"** the locked m-2 contract already names as a sanctioned monotonic
raiser (`m-2 form-schema §3`) — plus the §J `gate_category` A-set. **No new gate class.**
- Atom: `HUMAN_GATE_REQUIRED = raised` + routing `gate_category ∈ A-set` (`human_decision_required` /
  `routing_unavailable`), readable off the **accepted/consumable** routing record (not the `route_dispatch()`
  return). Ordinary routing stays category-B (no raise).
- Stamp path: rides m-1 `submit()` → stamped `FROM` + lineage-accepted; m-6 projects A-set to the human-surface.
  A/B membership + projection are m-6/§J config (per §J J2); m-4 names the requirement, not a new enum.
- R2-safe: the raise trigger is a **routing-outcome state**, not a model value → no model-derived predicate
  enters the gate.

**Attack points for re-review:** (1) is `seat_archetype` genuinely opaque + authority-bearing only within the
m-4 routing mechanism (no concrete Step-1 values, no m-2 change)? (2) does the M4-1 confirm truly ride the
existing c1 HUMAN_GATE/§J shape with **no new gate class**, and is the raise trigger model-free (R2)? (3) is the
fold strictly additive — R2 + the m-3 seam untouched? (4) any place the fold silently needs an m-2 micro-fold
(if so, it should STOP-and-flag, not absorb)?

On approve I report fold-complete to the orchestrator (M4-1 confirm already reported in parallel). No
self-advance to PLAN.

ACTIONS_GIT_REF: docs-workspace fold of approved consumer findings + this re-review-request relay; INDEX row
appended. No code/source/pcode edits.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo).
