## DESIGN — c6-fix-m-7 planner fold: all 7 findings applied (doc-only); implementer review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-7
PARENT_DISPATCH_ID: c6-fix-m-7
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: c6-fix-m-7/DESIGN-orchestrator-planner-20260702-204518.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: c6 pair slice folded — one-pivot-per-mutation, config author-set, intake single-writer, NF-S9 grain, README currency, F8 token-family; your approve completes the return

Partner — the 7 c6 findings routed to us are applied (doc-only; no mechanism change; §22 lock invariants + the byte-exact `{accepted, rejected, held}` enum + the sanctioned claim set preserved). Fold-log entry appended to design doc §21. Per-finding changelog:

| finding | fix landed | where |
|---|---|---|
| m-7-F1 (B, ◆) | HELD = ONE compound record embedding the candidate; `verify()` = ONE compound operator-verdict record whose presence IS the decision-scoped burn (no separate burn record); Phase-3 burn set derived from verdict records; new fixture **F11**: every mutation class = exactly one canonical record via one rename; crash injected at every syscall boundary finds no second canonical rename to crash between | design doc §6, §3, §5, §12 NF-S2, §13 F11 |
| x3-seam-byte-integrity-F2 (B) | trusted-config author set completed to **m-2/m-3/m-4/m-5/m-6** (m-5 archetype-registry section + m-2 declared section added); m-5's CQ-4b section confirm noted as riding the c6 COORD (m-5 CC'd) | §7, §12 S15 |
| m-7-F5 (M) | single **intake-writer task** (bounded channel from handlers; codex donor at the intake tier); "handlers never touch any file" now literally true | §1 diagram, §2.1, §2.2 |
| m-7-F6 (M) | NF-S9 parenthetical regrained: "only component that writes to **non-seat destinations**" (seat pipes/MCP channels are seat-facing — the old wording collided with §8.3 delivery) | §12 NF-S9 |
| m-7-F7 (M) | README status → **DESIGN-LOCKED / c4 CLOSED** with §22 + VP co-sign (`c4-design-m-7/RECONCILE-orchestrator-reviewer-20260702-040327`) pointers; origin note kept as history | README :3-17 |
| m-7-F9 (C) | F8 sweep classes widened with the **writability/reachability token family** (`non-lane-writable`, `lane-proof`, `seat-proof`, "no lane can"); §3 step 3's bare instance scoped in place (tool-surface grain, D5 reference) | §16, §3 step 3 |
| x2-claim-honesty-F8 (C) | README "under VP review" consumes-note replaced with the locked seam-matrix pointer (with m-7-F7 this brings the README fully current) | README consumes section |

**CTO-applied items verified present, NOT re-touched** (per the dispatch): NF-S6 two-axis split; NF-S7/§15 CQ-2/r4-fold-log `{self_reported, mixed}` widening; the §21 c6 fold-log entry; the anchor repoints.

**Your review hunt-list:** (a) does the m-7-F1 fold stay within the VP-ratified canonical resolution (one pivot per mutation; candidate-embedding; verdict-implies-burn) without changing CQ-6 base semantics or any §22 invariant? (b) is F11 biting (every mutation class enumerated)? (c) any of the 7 fixes drifting into mechanism change or claim-boundary regression? (d) did I anywhere re-touch a CTO-applied item?

DESIGN_REVIEW_VERDICT requested: approve | must-revise | reject-narrow | human-decision-required. On your approve, this relay + yours = the `c6-fix-m-7` pair completion return per the dispatch.

Not authorized / not claimed: no mechanism change, no design-lock reopen, no PLAN/IMPL/spike, no code/source/`pcode/`, no CQ reopened, no CTO-applied item re-touched.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/c6-fix-m-7/DESIGN-planner-20260702-205730.md` — run before handoff (result in inline pointer)
- `rg -n "candidate \+ a held|separate burn record|only socket-writing|m-6/m-3/m-4-authored|BOOTING" master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md master/domains/m-7-conductor-core/README.md` — zero stale-form hits post-fold
- byte-exact `{accepted, rejected, held}` + four sanctioned by-construction claims + confusion-resistant/D5 vocabulary: unchanged (the folds add scoping, never strength)
- `git -C pcode status --short` — clean (planner-run this session)

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: edited `master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md` (§1, §2.1, §2.2, §3, §5, §6, §7, §12 S2/S9/S15 + step-3 scoping, §13 F11, §16, §21 c6-fix entry) + `master/domains/m-7-conductor-core/README.md` (status + consumes) + this relay + one INDEX.md row timestamped 20260702-205730; no code/source/`pcode/` edits (pcode verified clean)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C pcode status --short` = clean)
Next requested action: operator relays to m-7.implementer for the adversarial pass; on approve the pair return goes to the orchestrator for the c6 verify sweep + VP close.
