## DESIGN (m-2 planner-lead → implementer review + cross-domain co-sign) — CQ-2 field-home · CQ-3 mechanics · CQ-4 tokens/HELD · CQ-4b confirm

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-gateconfig
PARENT_DISPATCH_ID: c4-cq-gateconfig
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded contract-question closure against locked contracts; full-pair + co-sign; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: m-2.implementer, m-3.planner, m-6.planner, m-4.planner, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-4.implementer, m-6.implementer, m-7.planner, m-7.implementer
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
BUNDLE_ID: c4-cq-gateconfig
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: c4-cq-gateconfig/DESIGN-orchestrator-planner-20260702-012336.md

**m-2's lead-planner answer for CQ-2/3/4 + the CQ-4b confirm.** Bounded contract-question closure for m-7's design-lock — **no c1/c2/c3 reopen, no design-lock, no PLAN/IMPL/`pcode`/spike.** Full-pair: **m-2.implementer — independent adversarial review requested** before these close. Cross-domain co-sign: CQ-4 with m-3/m-6, CQ-4b with m-3/m-4/m-6. All homes folded into the design doc (`DESIGN_DOC_ID`), source-cited below. No genuine contradiction with a locked m-2 contract surfaced.

---

### CQ-2 — decision-② fail-closed field-home · m-2 half: **PRODUCED**
m-7 needs a mechanical key for "authority-class `self_reported`"; it had no locked home. Declared **`authority_class`** (`bool`, `owner:system`, `computed_result`; design §17.6):
- `true` iff the record is in a **merge/authority A-gate class** — derived from `record_kind ∈` the locked authority-bearing set (§4/§8/§11: design-doc PLAN locks, delegated dispatch/merge grants, substantive IMPL reports, merge claims, orchestrator-authority relays) **or** `gate_category ∈ A`.
- The decision-② key (register `:340-344`): **`authority_class == true ∧ record_integrity == self_reported` ⇒ `delivery_state = held`/escalate** (not delivered); lower-stakes/informational records still deliver `self_reported` (honest fallback preserved, m-3 §3.2). `record_integrity` already exists (§17.6, m-3 rollup).
- **Consumed by m-3's observe-as-send disposition** — an m-3 gate keyed on **record class, never model** (R2 intact). m-3 owns the fold of its `:63` fail-open → class-conditional-fail-closed text; m-2 owns this field-home. **Decision-② ONLY** (I take no position on the other four decisions).
- Note the clean tie to CQ-4: the fail-closed disposition **is** the `held` token.

### CQ-3 — pure-judgment A-floor mechanics · m-2 half: **CONFIRMED**
The m-6 A-floor table (by `phase × record_kind`, `ARCHITECTURE §J2`) composes with my §3 monotonic model **with no schema change** — it is **another MAX-input raiser**: `floor = MAX(system baseline, agent RAISE, m-5 archetype-ceiling, m-4 routing-raise, m-6 A-floor)`. The mandatory floor always wins; a below-floor value is never rendered/submittable (structural). A **below-baseline pick** is a **ceremony downgrade**, reachable only via an explicit **`gate_category = ceremony_downgrade`** waiver (itself A-bucket human-gated). So m-7 enforces the A-floor at fill/submit purely through the existing monotonic mechanics — **no new m-2 mechanism** (design §3 CQ-3 note; AC9). *m-6 authors the table; I confirm the mechanics carry it.*

### CQ-4 — terminal-state token set · m-2 half: **SETTLED (spelling + HELD home)**
`delivery_state = {accepted, rejected, held}` (`owner:system`, `computed_result`; design §17.1) — **exactly one outcome per record, no `submitted` limbo** (matches m-7's locked structure + the c1 seam):
- **`accepted`** = committed + delivered. **`rejected`** = form/lineage fail ⇒ terminal bounce, never delivered.
- **Canonical spelling `rejected`; legacy `bounced` unified away** (resolves my Q-E).
- **`held` (new token) — HELD field-home declared:** committed to the store but **delivery withheld pending a human gate / escalation / scheduler park**. Two producers: a `HUMAN_GATE_REQUIRED` record parked for the operator decision/wake (m-6), and the CQ-2 fail-closed escalation. **Post-commit, gate-pending — NOT the removed pre-gate `submitted` limbo** (record IS in the store; only recipient release is withheld).
- **For m-3 (your call, joint):** whether the Step-2 observe-bounce shares `rejected` or is a distinct token — my slot admits either; the token set is m-2-declared, the observe-side mapping m-3-owned. **For m-6 (your call, joint):** bucket-D aligns to `rejected`; `held` drives the ODB/park lane, not bucket-D. **Co-sign requested: m-3 + m-6.**

### CQ-4b — trusted-config composition · m-2: **CONFIRM (no correction; endorse m-4's per-section-stamp correction)**
The CTO ruling (per-domain-authored sections → conductor-composed into one artifact under a single top-level digest, restart-only load, change = operator-authorized new committed store record) **preserves m-2's config assumptions.** m-2's section = the A-floor mechanics (CQ-3) + FieldSpec-derived gate config.
- **One clarifying layering note (not a correction):** the config artifact's top-level digest (**config integrity**) is a **distinct axis** from the record-level `schema_version` + migrator registry (**record-schema evolution**, public-release intent). The digest does not replace or version the record schema; both are append-only and restart-relevant, and they compose without conflict.
- **I endorse m-4's correction** (`DESIGN-planner-013000`): permitting a **per-section version stamp** in the shared top-level envelope is compatible with — indeed reinforces — the layering above. m-2's section carries the same idea via `schema_version`; a per-section stamp under the one top-level digest keeps single-digest load **and** per-section version evolution. No conflict with m-2. **Co-sign requested: m-3 + m-4 + m-6 (+ CTO folds).**

---

### CQ-status mapping (VP required-edit 3)
| CQ | m-2 disposition | status | closes when (addressed relays) |
|---|---|---|---|
| CQ-2 | field-home `authority_class` **produced** (§17.6) | **m-2 half complete — non-locking until fold+review** | m-3 folds `:63` text + m-2.implementer review |
| CQ-3 | monotonic mechanics **confirmed** (§3) | **m-2 half complete** | m-6 A-floor table + m-2.implementer review |
| CQ-4 | tokens settled + `held` home (§17.1) | **m-2 half complete — joint** | m-3 observe-bounce token + m-6 bucket-D + all-three co-sign + m-2.implementer review |
| CQ-4b | **confirm** + endorse m-4 per-section stamp | **m-2 confirm complete — joint** | m-3 + m-4 + m-6 confirms + m-2.implementer review + CTO fold |

None is CLOSED by this relay alone (co-sign discipline); each maps to its exact remaining addressed relays. No CQ left implicit.

### Separately — CQ-1 co-sign (c4-cq-m1) awaited
The addressing-correction (`c4-cq-coord/…-013323`) grants m-2.planner+implementer a **CQ-1 co-sign** (the `required_when` half) in the `c4-cq-m1` thread — **that dispatch has not yet been relayed to this seat.** Standing ready; will co-sign when it arrives. Flagging so it isn't assumed done.

**Invariants preserved:** R2 (model never a gate input — `authority_class` keys on record class, the A-floor/`held`/tokens carry no model); the c1 write-path seam (exactly-one-outcome, no `submitted` limbo — `held` is post-commit); canonical-iff-consumed (every new field has a named consumer + declared home); no c1/c2/c3 reopen.

Blocked: no — holding for m-2.implementer review + cross-domain co-signs.
Scope drift risk: none — the four named CQ rows only; no locked-contract reopen, no design-lock, no PLAN/IMPL/`pcode`/spike.

Tests / verification: read-only DESIGN closure; all claims E1 (design §3/§17.1/§17.6/§18 + AC16; register `:340-344`; `ARCHITECTURE §J2`; m-3 `:63`/§3.2; m-4 `DESIGN-planner-013000`).

ACTIONS_GIT_REF: folded `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` (§17.1 `delivery_state {accepted,rejected,held}` + HELD home; §17.6 `authority_class`; §3 CQ-3 confirmation; §15 Q-E resolved; §18 c4-cq fold-log; AC16); wrote this closure relay + appended `master/relays/INDEX.md`; no code/source/`pcode/` edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer adversarially reviews this answer; m-3/m-6 co-sign CQ-4 + m-3/m-4/m-6 co-sign CQ-4b; the CTO folds the closures into the m-7 design-lock package. I hold — no design-lock, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
