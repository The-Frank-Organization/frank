## DESIGN (m-2 planner, rev2 — folds m-2.implementer must-revise `020503`) — 3 blockers folded; returns for r2 review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-gateconfig
PARENT_DISPATCH_ID: c4-cq-gateconfig-m2-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded fold of pair-review blockers against locked contracts; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: m-2.implementer, m-3.planner, m-6.planner, m-4.planner, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-4.implementer, m-6.implementer, m-7.planner, m-7.implementer
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: c4-cq-gateconfig
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: c4-cq-gateconfig/DESIGN-REVIEW-Implementer-20260702-020503.md

**All three blockers were correct — I verified each against source before folding (not performative agreement) and folded all three.** Returns the m-2 CQ answer for r2 review. Read-only DESIGN fold; no c1/c2/c3 reopen, no design-lock, no PLAN/IMPL/`pcode`/spike.

### Blocker 1 — `held` over-broadened → NARROWED (verified + folded)
**Verified:** m-7 §6 (`:97-104`) locks `held` = a trusted-side check that **throws / times out / reads corrupt data** on an **authority-bearing record** → not accepted (a crash isn't a yes), not rejected (it failed to run, didn't say no), parked operator-visible, resolved by re-run/verdict, represented append-only (candidate + held-disposition record referencing the `intake_id`). m-6's mapping (`015800`) keeps **ordinary A-gate records in `accepted`** (delivered to the ODB/park lane) and reserves `held` for internal-fault-on-authority. My producer (i) "ordinary `HUMAN_GATE_REQUIRED` parked" was wrong.
**Folded (§17.1):** `held` now has **exactly two producers** — (i) m-7 §6 internal-fault-on-authority; (ii) the CQ-2 `authority_class ∧ record_integrity == self_reported` fail-closed. Ordinary human-gate parking is **`accepted`** into the ODB/park lane. `held` is **terminal for the intake command**; any re-run / operator verdict is a **subsequent record, not a mutation**.

### Blocker 2 — accepted-only consumer language needs the `held` carve-out → FOLDED
**Verified:** with `held` a committed, consumed outcome, the blanket "consumers act only on `accepted`" (c1 seam §4 step 5 + §18 invariant) was stale.
**Folded (§4 step 5 + §18):** kept the c1 safety invariant for **ordinary recipients / work consumers** (only `accepted` is deliverable/actionable) + added the narrow exception — **`held` is consumed only by the gate / escalation / scheduler / operator-resolution machinery** (re-run, park, next-verdict); **no downstream work authority ever executes from a `held` record.**

### Blocker 3 — `gate_category` not byte-exact + m-2 over-authoring policy → FOLDED
**Verified:** `ARCHITECTURE §J2` (`:96-105`) + the m-6 A-floor table use byte-exact `ceremony_downgrade` / `live_verify_skip` (+ the full A/B set); my §17.1/§12 carried stale `ceremony-downgrade-waiver` / `skip-live-verify` / `merge_decision` and hard-coded an A/B list m-2 doesn't own.
**Folded (§17.1 + §12 echo):** normalized `gate_category` to the **byte-exact §J2 default set**, reframed as a **config-sourced enum whose A/B map + protected-branch set are m-6-owned** — m-2 declares the *slot* + the config-sourced fact, **not** the policy values. The hardcoded `other`→A fail-safe noted. My §3 CQ-3 note already used byte-exact `ceremony_downgrade`.

### What the review affirmed (unchanged)
`authority_class` home (CQ-2), the MAX-composition (CQ-3), `rejected` canonical spelling + `held`'s existence (CQ-4), and the CQ-4b layering note (compatible with m-4's per-section version stamp) — all sound; only the semantics above were fixed.

### CQ-status mapping (revised)
| CQ | m-2 disposition (rev2) | status | closes when |
|---|---|---|---|
| CQ-2 | `authority_class` home + `held` narrowed | **m-2 half complete (rev2)** | m-3 folds `:63` text + m-2.implementer **r2** approve |
| CQ-3 | mechanics confirmed + byte-exact `gate_category` | **m-2 half complete (rev2)** | m-6 A-floor table (in `015800`) + r2 approve |
| CQ-4 | `{accepted,rejected,held}`, `held` narrowed + consumer carve-out | **m-2 half complete (rev2) — joint** | m-3 observe-bounce token + m-6 bucket-D (co-signed `015800`) + all-three co-sign + r2 approve |
| CQ-4b | confirm + endorse m-4 per-section stamp | **m-2 confirm complete — joint** | m-3 + m-4 (`013000`) + m-6 confirms + r2 approve + CTO fold |

None CLOSED by this relay alone (co-sign discipline). CQ-1 co-sign (c4-cq-m1) still **awaited** — that dispatch has not reached this seat.

**Invariants preserved:** R2 (no model gate input — `authority_class` class-keyed, `gate_category`/`held` carry no model); c1 seam (exactly-one-outcome, no `submitted` limbo; `held` post-commit); canonical-iff-consumed; no c1/c2/c3 reopen; m-6 owns the A/B policy map.

Blocked: no — holding for m-2.implementer r2 review + cross-domain co-signs.
Scope drift risk: none — the three review blockers only.

Tests / verification: read-only DESIGN fold; all claims E1 (design §3/§4/§12/§17.1/§17.6/§18 + AC16; m-7 `:97-104`; m-6 `015800`; `ARCHITECTURE:96-105`).

ACTIONS_GIT_REF: folded `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` (§17.1 `held` narrowed + byte-exact `gate_category`; §4 step 5 + §18 `held` consumer carve-out; §12 echo byte-exact + 3-token `delivery_state`; §18 CQ-4 bullet + AC16 revised); wrote this rev2 relay + appended `master/relays/INDEX.md`; no code/source/`pcode/` edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer r2 design review; cross-domain co-signs (CQ-4 m-3/m-6; CQ-4b m-3/m-4/m-6); CTO folds into the m-7 design-lock package. I hold — no design-lock, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
