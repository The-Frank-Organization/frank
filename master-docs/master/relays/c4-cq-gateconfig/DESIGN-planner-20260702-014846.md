## DESIGN — m-3.planner answer: CQ-2 fold PRODUCED · CQ-4 (m-3 half) SETTLED · CQ-4b CONFIRMED — requesting m-3.implementer adversarial review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-gateconfig
PARENT_DISPATCH_ID: c4-cq-coord
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded directed fold of operator decision ② + cross-domain token/config settle; no new operator gate
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-4.planner, m-6.planner, m-2.implementer, m-4.implementer, m-6.implementer, operator
IN_REPLY_TO: c4-cq-gateconfig/DESIGN-orchestrator-planner-20260702-012336.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: c4-cq-gateconfig — m-3 lead-planner answer for CQ-2/CQ-4/CQ-4b; adversarial review requested (closure-gate leg 2 per c4-cq-coord/…-013323); CQ-status-mapped

m-3.implementer — my lead-planner answer for the three m-3-touching CQ rows is below and folded into `design/2026-06-29-observe-evidence-design.md` (`DESIGN_DOC_ID: c2-design-m-3-observation-evidence`). Per `c4-cq-coord/…-013323` you now hold **TO** authority for the adversarial-review leg — respond read-only `PHASE: DESIGN-REVIEW`, parent to this relay, carry the same `DESIGN_DOC_ID`, return `DESIGN_REVIEW_VERDICT`. Scope guardrail honored: confirm-or-produce the named CQ rows **only**; no c1/c2 reopen, no design-lock (m-7's), no PLAN/IMPL/`pcode`/spike.

---

### CQ-2 — decision-② fail-closed fold · m-3 (fold) + m-2 (field-home) · **PRODUCED**
Operator decision ② (`READINESS-REGISTER.md` ②; `DESIGN-REVIEW-2026-07-01.md` §2A.7): the universal fail-**OPEN** (`self_reported` always delivers) becomes **class-conditional fail-CLOSED**.
- **Folded (m-3 text):** §2 (line 31), **§3.2 condition (d)** (line ~63), **§8 opaque-lane floor** (line ~130), §12 Resolved-decisions, §13 fixtures, §15 fold-log. Disposition: **`authority_class == true ∧ record_integrity == self_reported` ⇒ terminal `held`/escalate (resummon, J1 `hold_and_resummon`), NOT delivered**; non-authority/informational records still deliver `self_reported` (honest fallback preserved). m-7 §3/§6 executes this folded disposition (NF-S7 binds to it).
- **Field-home (m-2, co-owner) — already declared, reconciled:** m-2 §17.6 line 334 declares **`authority_class`** (`bool`·`owner:system`·`computed_result`; consumed by the m-3 observe-as-send gate), `true` iff `record_kind ∈` the authority-bearing set **or** `gate_category ∈ A`, mapped to `delivery_state = held` (line 271). My fold keys **exactly** on that field — no m-3-invented schema. **Never keyed on model (R2 preserved).**
- **CQ-status:** **corrected-by-artifact** (m-3 fold) + m-2 field-home **produced** (line 334). Foldable to `closed` once your review posts (closure-gate leg 2).

### CQ-4 — terminal-state token set · m-2 + m-3 + m-6 joint · **SETTLED (m-3 half)**
m-7 locked the structure {accepted, rejected, held} (exactly-one-outcome, no `submitted` limbo; §6/NF-S16). **The m-3 question — does the Step-2 observe-bounce share `rejected` or is it distinct — SETTLED:**
- **The Step-2 observe-bounce SHARES `rejected`** — when the check **runs and says no** (predicate observably false; declared-vs-observed integrity contradiction, §9; egress-fail, §7), the candidate relay is **not appended-as-accepted nor delivered**, but a **terminal `rejected` evidenced outcome record** naming the failing field IS committed for audit. This **unifies with m-7 §6 / line 74 / NF-S16** (every path reaches exactly one outcome — a pre-append control-flow bounce that recorded nothing would be the outcome-free exit NF-S16 forbids) and keeps the m-3 thesis (evidence, not vibes: the bounce leaves a durable evidenced trace).
- **`held` stays DISTINCT** — reserved for the check being **unable to run** in an authority class (CQ-2 decision ②) — "the check failed to run, it did not say no," m-7 §6 line 100 held-semantics exactly.
- **§2 refinement (flagged):** my locked §2 said "nothing is appended or delivered"; I refined it to "candidate **not appended-as-accepted nor delivered**; a terminal `rejected` evidenced outcome record IS committed for audit." This is a reconciliation with m-7 §6, **not** a security regression — the load-bearing property (the gated relay never reaches a recipient) is unchanged. Surfacing it explicitly rather than silently resolving, per the guardrail. **Adversarially test this is not a contract break.**
- Spelling (`rejected` vs legacy `bounced`, Q-E) + the `HELD` registry home are **m-2's** half (m-2 §17.6 lines 269–271 already declare `held`); bucket-D naming is **m-6's**. **CQ-status: closed (m-3 half); needs m-2 + m-6 co-sign** + your review to fully close.

### CQ-4b — trusted-config artifact composition · CTO arbitrates; m-3 CONFIRMS · **CONFIRMED (no correction)**
The CTO draft (per-domain-authored sections, conductor-composed into ONE artifact under a single top-level digest; restart-only trusted load; change = new committed store record w/ recomputed digest) **preserves every m-3 config-input assumption**:
- m-3's config inputs — the **egress/content rule-set** (§7, operator-configurable, fail-closed `other`→block), the **check-registry** membership (§4, vetted conductor-implemented procedures), the **opaque-lane floor + decision-② class→disposition map** — all assume: (a) **loaded once at trusted startup, restart-only** ✓; (b) **tamper-evident integrity check** ✓ (single top-level digest pinned to genesis covers my section); (c) **m-3-authored, not lane-writable** ✓ (authorship stays per-domain; this actually *closes* the config-lane-writable FATAL); (d) **operator-authorized, auditable change history** ✓ (change = committed append-only store record).
- **Cross-link:** the decision-② class→disposition map is m-3 disposition config that composes into the artifact and keys on m-2's `authority_class` — one artifact carries both cleanly. A **single** top-level digest (vs per-section) does **not** hurt m-3's fail-closed-critical egress config: restart-only + operator-authorized means no hot-path integrity concern. **No correction — CONFIRM.**
- **CQ-status: confirmed (no correction);** needs your review + the m-2/m-4/m-6 co-confirms to close.

---

### Adversarial targets for your review
1. Does the §3.2(d)/§8 fold key **exactly** on m-2's declared `authority_class` (§17.6:334), inventing no m-3 schema, and stay **R2-clean** (class-keyed, never model)?
2. Is the CQ-4 settle sound — is `rejected`-shared / `held`-distinct the right cut (check-ran-said-no vs check-couldn't-run), and does the §2 "nothing appended" → "candidate-not-delivered + terminal audit record" refinement **preserve the security property without reopening the locked exactly-one-outcome seam**?
3. Is the disposition split complete/mutually-exclusive across {non-authority-unobservable→`accepted`+self_reported | authority-unobservable→`held` | observably-false→`rejected`} — any record class that falls through?
4. CQ-4b: any m-3 config assumption a single-top-level-digest section-composition breaks that I missed?

Phase band DESIGN only — no PLAN/IMPL. On your approve I report the m-3 legs answered to the orchestrator; the joint CQ-4 still needs m-2 + m-6 co-sign and CQ-4b the peer confirms before the CTO folds into the m-7 design-lock package. I do not self-advance to PLAN.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd is a non-git docs workspace. Artifacts this turn: design-doc fold (§2, §3.2, §8, §12, §13, §15) + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
