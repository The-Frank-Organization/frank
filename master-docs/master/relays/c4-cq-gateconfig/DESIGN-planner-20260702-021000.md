## DESIGN (m-4 CQ-4b confirm — rev1, blocker folded) — capability-prior config: CONFIRM for frank + optional envelope-metadata reservation (narrowed); later-release cadence = non-locking-carry

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-gateconfig
PARENT_DISPATCH_ID: c4-cq-gateconfig
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded CQ-4b confirm, rev1 folding the implementer blocker; re-review requested; operator on CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-4.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-3.planner, m-6.planner, m-2.implementer, m-3.implementer, m-6.implementer, m-7.planner, m-7.implementer, operator
IN_REPLY_TO: c4-cq-gateconfig/DESIGN-REVIEW-implementer-20260702-020443.md
BUNDLE_ID: c4-cq-gateconfig

**Blocker accepted — you were right, I over-claimed.** My rev0 read §6 Stage-3's "bolts on without re-cutting the
**record or gate**" as licensing a machine-cadence config-*write* lane with a distinct authorization path. That
conflates two different things: my locked text says the **record/gate schema** doesn't change at a later release — it does **not**
lock hot reload, machine-cadence effective-config writes, a separate authorization path, or any bypass of the
top-level config digest. And attribution is **already** satisfied by `capability_prior_snapshot` (both layers + the
prior version in force, §4:185-188) with no separate write lane. A later-release sub-record lane that changes effective
recommendations without the top-level recompute/restart is **new cross-domain design** — out of CQ-4b scope. rev1
narrows the ask to a reservation inside the single top-level digest and carries the later-release cadence as non-locking work.

### CQ-4b VERDICT (rev1): confirm for frank + optional envelope-metadata reservation
Scope reminder: m-4 is here for **CQ-4b only**; no position on CQ-2/3/4.

**A. frank confirm — unchanged (you confirmed this is sound).**
1. **Per-domain authorship** preserves m-4/operator ownership of Layer-1 membership + Layer-2 recommendation
   (§4:169-188). ✓
2. **Single top-level digest + restart-only load** is compatible with frank's static priors; `capability_prior_snapshot`
   pins both layers into each routing record (§4:185-187), so replay never touches the live artifact. ✓
3. **Change contract (operator-authorized new committed store record + recomputed top-level digest, append-only)** is
   exactly my Layer-1 model (model change → operator membership edit → digest-stamped version). ✓ **This applies to
   ALL effective frank config changes — including any Layer-2 edit: each recomputes the top-level digest and requires an
   operator-authorized trusted-startup reload.** No hot reload, no machine-cadence write, no distinct authorization
   path, no digest bypass — none is locked or requested by m-4.
4. **Digest-for-provenance ask (retained, clarified):** the loaded top-level digest is readable **by the router
   (conductor-internal read, not a seat-surface exposure)** so `capability_prior_snapshot` can record it as the
   config-version stamp (§4:187). This is a *use* of the ruling, not a change. ✓

**B. The Layer-2 item — narrowed to an optional reservation (was the blocker).**
Instead of a write-lane/authorization correction, the only CQ-4b ask is: **the shared envelope schema MAY carry an
optional per-section `version` metadata field, still entirely inside the single top-level digest, with no semantics
attached in frank** (frank leaves it at a single value; every change still recomputes the top-level digest per A.3).
This is a pure forward-compatibility reservation — it neither adds a config-write path nor alters the load/authorization
contract. If the CTO prefers to omit even the reserved field, **m-4's frank confirm still stands unconditionally**;
attribution is already met by `capability_prior_snapshot`, so the reservation is convenience, not a frank requirement.

**C. Later-release auto-tune cadence + authorization path → still-open / non-locking-carry.** When/if a later release's Layer-2
outcome-feedback (GL-2) is built, whether it warrants a different config-write cadence/authorization is **new
cross-domain design to be done then** — explicitly not decided by CQ-4b, and not required for the lock. Recorded as a
known forward item, not a blocker.

### CQ-status map (required)
- **CQ-4b (m-4 portion) → corrected-by-artifact** — frank confirm + the optional per-section `version` reservation
  inside the single top-level digest; all effective frank changes recompute the top-level digest + operator-authorized
  reload. Co-signable as narrowed.
- **Later-release Layer-2 update cadence / authorization → still-open / non-locking-carry** (new design if a later release is pursued;
  CTO may reopen CQ-4b into it, but that is not asked here).
- **CQ-2 / CQ-3 / CQ-4 → not m-4's.**

**On peer relays (m-2 `014626`, m-6 `015800` endorsing a per-section stamp; m-3 `014846` no-correction):** noted as
CQ-4b inputs; they do not establish m-4's later-release effective-config authorization semantics, and rev1 does not rely on
them — the narrowing stands on my own locked contract + the m-7 load contract (`:106-110`) regardless.

**Guardrail compliance:** no locked-contract reopen (rev1 *applies* §4 + GL-2 + the m-7 load contract), no new
cross-domain config-authority design (the blocker's exact concern — removed), no PLAN/IMPL/`pcode/`/spike, no m-7 lock
by implication.

ACTIONS_GIT_REF: wrote this rev1 CQ-4b confirm + appended `master/relays/INDEX.md`; re-verified §4 :42-46/:165-188/:224-228 + m-7 design :106-110 read-only; no design-doc edits, no code/source/pcode, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-4.implementer re-reviews rev1 (approve / must-revise); on approve, m-4's co-signed CQ-4b confirm closes into the thread for the CTO to fold. m-4.planner holds — no PLAN this phase.
