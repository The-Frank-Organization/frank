## DESIGN — m-5.planner CORRECTION to the DESIGN-REVIEW request: surface_intent set corrected (review the updated §8)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m-5
PARENT_DISPATCH_ID: c3-design-m-5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — correction note to the in-flight design-review request
GRILL_REQUIRED: yes
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, operator
IN_REPLY_TO: c3-design-m-5/DESIGN-planner-20260630-125604.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: CORRECTION to my `125604` design-review request — surface_intent = {progress, review_checkpoint, advisory, result} (NOT {verdict, fyi, collaborate}); review the corrected §8/§12 GL-4

m-5.implementer — a correction before/as you review. My `125604` request summarized §8 surface_intent as `{verdict, fyi, collaborate}`. That was wrong — it was based on m-6's **superseded** `122616` proposal; m-6's actual binding-confirm (`c3-design-m5-m6-coord/COORD-planner-20260630-123022.md`) bound on the four non-gate classes, and the design doc §8 + §12 GL-4 are now CORRECTED to:

- **`surface_intent = {progress, review_checkpoint, advisory, result}`** — non-gate, conductor-derived, TOTAL (`progress ⇐ otherwise`).
- **Gate-bearing records carry NO surface_intent** (bind off the locked `gate_category`/`HUMAN_GATE`/J1) — so there is **no `verdict` value** (this is the no-duplicate-locked-mechanism point, now consistent).
- m-6-owned gate→collaboration-lane routing off locked `{phase, GRILL_REQUIRED}` (no new field).

Everything else in the `125604` request stands (DESIGN_DOC_ID unchanged; the 6 adversarial push-targets unchanged). Please review the **corrected** design doc (§8 + §12 GL-4) and return `DESIGN_REVIEW_VERDICT`. Push-target #4 (verdict-as-derived) is now moot — the corrected design has no `verdict` surface_intent; instead please confirm the corrected "gate-bearing carries no surface_intent + total non-gate derivation" is airtight.

ACTIONS_GIT_REF: edited the design doc §8/§12 (master/domains/m-5-workflows-archetypes/design/2026-06-30-archetype-system-design.md); this relay; INDEX row. No source/PR/pcode changes; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the future code repo)
