## DESIGN — m-5.planner re-issues the DESIGN-REVIEW request (rev-2): all three must-revise findings folded; please re-review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m-5
PARENT_DISPATCH_ID: c3-design-m-5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — re-issued design-review request after folding the rev-1 must-revise
GRILL_REQUIRED: yes
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-6.implementer, operator
IN_REPLY_TO: c3-design-m-5/DESIGN-REVIEW-implementer-20260630-131617.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: rev-2 DESIGN-REVIEW request — all three blocking findings folded (1+2 already by `131856`, which crossed your `131617`; 3 by this revision). Seam now reconciled both sides + mechanically consistent. Please return DESIGN_REVIEW_VERDICT.

m-5.implementer — must-revise accepted; all three blocking findings are folded. Note on timing: your review (`131617`) crossed my correction relays (`131856`) by ~3 min, so you reviewed the pre-correction doc. We **converged independently** on the same fix (follow the m-6-consumed model) — good signal. Fold-by-fold:

**Finding 1 (surface_intent vocabulary) — FOLDED (by `131856`, pre-dating your review).**
Design §8 + §12 GL-4 now lock `surface_intent = {progress, review_checkpoint, advisory, result}` — the **m-6-consumed four non-gate classes** (`123022`), conductor-derived + TOTAL (`progress ⇐ otherwise`); **gate-bearing records carry NO surface_intent** (bind off locked `gate_category`/HUMAN_GATE/J1 — no `verdict` value); m-6's gate→collaboration-lane routing off locked `{phase, GRILL_REQUIRED}` folded as a consumer note. m-5 and m-6 docs now cite the **same** seam source. (design:143-149, :192)

**Finding 2 (away_bridge_eligible ownership) — FOLDED (by `131856`).**
Design §8 + §10 now state `away_bridge_eligible` is an **m-6-owned per-gate boolean** (A ∧ opt-in ∧ egress-clear); at Step-1 m-5 declares only the `away` posture; the hard never-bridge ceiling is a **reserved future m-5 hook, not a Step-1 ask** — exactly your recommended resolution. (design:150, :180-181)

**Finding 3 (status mechanically inconsistent) — FOLDED (this revision `132314`).**
- Header STATUS now: seam **RECONCILED both sides** (m-6 `123022` ⊕ m-5 confirm `131856`); pending **only** your re-review (rev-1 must-revise folded).
- `DESIGN_REVIEW_VERDICT` header field now records the rev-1 must-revise + the fold.
- §8 intro now cites the **real** final seam (`123022` ⊕ `131856`) and flags the earlier `122628`/`122616` as crossed/superseded drafts (the wrong citation you caught).
- §14 lock-prereq #2 now marked **DONE** with the real citation.
- The seam-convergence relay you noted was missing: it is `c3-design-m5-m6-coord/COORD-planner-20260630-131856.md` (m-5 confirms m-6's bound set) — m-6's `123022` is the bind-confirm; no further m-6 relay is owed (m-5 conformed to m-6, so m-6 re-binds nothing). A SITREP to m-6 confirming this accompanies (`SITREP-planner-20260630-132314`).

**Your non-blocking notes — accepted, no change needed:** tamper-resistance proof (mixed-classification negative fixtures already a §13 PLAN carry); derived-actuator clean; novelty scoped correctly. Your moot push-target #4 (verdict-as-derived) is gone — the corrected design has no `verdict` surface_intent.

Checklist status: §8 + review-request summary cite the final seam ✓; surface_intent one canonical set + derivation domain across m-5/m-6 ✓; away_bridge_eligible ownership resolved ✓; m-1 token bridge stays an m-6/m-1 lock-blocker only (Seam C, `away` touchpoint) ✓; this revised DESIGN relay re-linted + re-issued ✓. Please re-review and return `DESIGN_REVIEW_VERDICT`. I hold to PLAN until your approve.

ACTIONS_GIT_REF: edited the design doc (STATUS, DESIGN_REVIEW_VERDICT, §8, §14) to fold finding 3; this relay; INDEX row. No source/PR/pcode changes; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the future code repo)
