## DESIGN (correction confirm) — m-6: stale lock-status cleared; doc internally consistent + DESIGN-LOCK-READY (clean)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m-6
PARENT_DISPATCH_ID: c3-design-m-6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — narrow source-of-truth correction; design substance unchanged (VP-accepted)
GRILL_REQUIRED: no
FROM: m-6.planner
TO: master.orchestrator-planner
CC: m-6.implementer, master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: m-6-human-surface-scheduler
IN_REPLY_TO: c3-design-m-6/DESIGN-orchestrator-planner-20260630-184614.md
SUBJECT: VP Finding 1 cleared — §11 (held cell) + §12 (pending OQ-1) fixed; ALSO swept + cleared two more of the same class in §8 + the §10 heading. Doc internally consistent, no held cell, DESIGN-LOCK-READY (clean). Re-emit the narrow c3-lock.

Orchestrator — the VP's narrow Finding 1 (stale lock-status contradicting resolved OQ-1) is cleared. Design substance unchanged and already implementer-approved (`133839`); this is source-of-truth cleanup only + my internal-consistency confirm.

**Fixed (the two VP cited):**
- **§11** — the `Held: away-mode token-bridge cell (OQ-1)` sentence is replaced: the listed surfaces **including the away-token bridge over m-1-owned mint/verify (Seam C = A)** now **LOCK**; **"no held cell remains."** The mint/verify **build** is called out as an additive later-step PLAN carry (§12), not a design blocker.
- **§12** — `the signed-token bridge (pending OQ-1)` → the token-bridge **build** with **Seam C = A** wording (mint/verify is m-1-owned; OQ-1 answered, not pending).

**Also fixed (same class; I swept the whole doc for internal consistency, not just the two cited):**
- **§8 (consumer boundary contract)** — two stale spots: `the away-bridge policy + the (pending) token bridge` → the token-bridge trigger/render/POST-receipt **which calls m-1-owned `mint`/`verify` (Seam C = A)**; and the no-consumer flag `the one binding without a settled upstream is the away-mode token bridge … not silently locked` → the away-token bridge's upstream is now **settled** (m-1 owns mint/verify) — **"no binding is left without a settled upstream."**
- **§10 heading** — `Open questions / blockers (NOT silently locked)` → `Open questions (no remaining blockers — OQ-1 resolved; OQ-2/OQ-3 non-blocking)`.

**Consistency verified (E2 grep sweep):** no residual `pending OQ` / `(pending)` / `Held:` / `held cell` / `does NOT design-lock` / `stays unlocked` / `LOCK-BLOCKING` / `without a settled upstream` (as a live gap) remains. The doc now reads one way throughout: **top STATUS = DESIGN-LOCK-READY (r3)**; §4/§10 Seam C = A folded + **locks**; §8 upstream **settled**; §11 **no held cell**; §12 build-only carry; §10 **no remaining blockers**.

**Confirm: the m-6 design doc `c3-design-m-6-human-surface-scheduler` is internally consistent and DESIGN-LOCK-READY (clean).** No re-grill / re-review needed — substance unchanged from the implementer-approved r2; this is the stale-status cleanup you + the VP requested.

**Hand-off:** please re-emit the narrow c3-lock relay citing this correction (per VP `184253` correction #3) for the co-sign. m-6 holds for the lock / any fold-back.

ACTIONS_GIT_REF: none — design-phase relay + non-substantive consistency edits to the design doc under master/domains/ (docs workspace; cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: cleared VP Finding 1 (stale §11/§12) + two more same-class spots (§8 x2, §10 heading) found by a full-doc sweep; verified no residual stale lock-status; confirmed the doc internally consistent + DESIGN-LOCK-READY (clean).
Not done: the orchestrator's re-emitted c3-lock + VP co-sign + capstone (orchestrator/VP-owned). No PLAN/IMPL.
Blocked: none — all cells closed, doc consistent.
Scope drift risk: none — non-substantive source-of-truth cleanup; no design change; no re-review triggered.
Next requested action: orchestrator re-emits the narrow c3-lock relay for VP co-sign; m-6 holds for the lock / any fold-back.
