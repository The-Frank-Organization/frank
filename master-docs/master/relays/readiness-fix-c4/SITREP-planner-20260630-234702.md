## SITREP — m-2 fold COMPLETE: routing FieldSpec mirrored to m-4's full record + conductor-computed field homes declared (Cluster 4 completion: 4b + 4c)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded bring-current schema mirror + field declarations; sibling confirms requested; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: master.orchestrator-planner
CC: m-4.planner, m-3.planner, m-5.planner, m-6.planner, master.orchestrator-reviewer, m-2.implementer, m-4.implementer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: readiness-fix-c4/DESIGN-orchestrator-planner-20260630-233740.md

**Cluster 4 completion folded — 4b + 4c done. No breaking domain constraint.** This follows the VP's correct revise (`232925`: 4a closed, but 4b was broader than the R2 trigger — the FieldSpec was stale vs m-4's full record — and the operator chose *complete the fold*). Bounded to §12/§17.3 + the named fields; **no routing-record contract change** (m-4-owned), **no tag-value micro-fold**, no other c2 change. Read-only DESIGN; no source/`pcode/` edits, no PLAN, no closure claim.

**4b — mirrored m-2's routing FieldSpec to m-4's LOCKED record (`m-4 …routing-policy…:200-210`).**
- **Enumerated the per-row `routing_assignments` fields** (m-4 `:203`): `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned|resolved` — **model-identity ⇒ non-gate-referenceable**, R2 held), `pin_mode` (`pinned|slot`), `declared_deviated:bool` (already per-row from the retype), `seat_archetype` (opaque, m-5-valued c3), `authority_ceiling` (**resolved-at-spawn, recorded per assignment** on both template + hand-authored paths — the F2 replay-completeness fold), `human_mode`/posture (see 4c). §17.3 + §12.
- **Added `deviation_reason_code`** (m-4 `:206`): `agent_enum_pick`, **`required_when any(routing_assignments.declared_deviated == true)`** — the *same grain/treatment* as `justified_deviation`, so the reason code can't be omitted where policy makes it load-bearing. (The field most coupled to the 4a deviation gate — folded with it, not deferred.)
- **Added `constraints` (m-4 `:207`) + `template_ref` (m-4 `:209`) as reserved-shape declarations** — shape only, **no Step-1 values** (same reserved-seam pattern as `slot_in`/`certification`); `template_ref` is `system`/`id_ref`, null in Step-1 (templates land with m-5, c3), `lineage_role: none`.

**4c — declared the conductor-computed fields' schema home (new §17.6 + §12 echoes).** So the m-3 write-allowlist resolves to an **m-2-declared set** (m-1 requirement) + canonical-iff-consumed holds. All three **non-gate-referenceable** (they are audit/delivery outputs, not gate inputs — consistent with R2):
- **`record_integrity {observed|self_reported|mixed}`** — `owner:system`/`computed_result`, the m-3 conductor rollup (`m-3 …:107-112`); pure function of the per-field `evidence_integrity` stamps; ODB/m-6/audit-consumed; not agent-writable.
- **`surface_intent {progress|review_checkpoint|advisory|result}`** — `owner:system`/`computed_result` (conductor-derived), **non-gate-bearing records only** (gate-bearing records bind off the locked `gate_category`/HUMAN_GATE, C2.4); m-5 owns the vocabulary, m-6 binds `(posture × surface_intent)`. Declared as a computed slot (like `record_integrity`) — the m-5/m-6 "no new *authored* m-2 field" position is honored; the registry still carries the computed-slot declaration.
- **`human_mode`/posture {interactive|away|unattended}` — HAS a clean home, NOT a gap.** It rides the **F2 per-assignment record** (declared as a per-row field of `routing_assignments`, §17.3; `ARCHITECTURE:187-188`; m-5 `:142`), so I declared it where it lives rather than papering it in as a duplicate top-level field. Per the dispatch's "if posture has no clean F2 slot, say so" — **it does have one; reporting that explicitly.**

**Boundary held (the CTO/VP clarification):** these are **additive FIELD declarations** (the fields consumers already require), explicitly **distinct from the forbidden tag-value micro-fold** — no tag-VALUE semantics entered the schema; `seat_archetype`/`slot`/`constraints`/`template_ref` stay opaque/reserved with no Step-1 values.

**Acceptance ("Cluster 4 reconciled") — m-2 side met:** §17.3/§12 now enumerate m-4 `:200-210`'s full record (per-row fields + `deviation_reason_code` + reserved `constraints`/`template_ref`); `record_integrity`/`surface_intent`/`posture` have declared homes; AC15 added. — E1, §17.3 :289–297 + §17.6 + §12 :178–182.

**Review edges (requested):**
- **m-4** — confirm the routing-record mirror matches your locked `:200-210` (per-row shape, `deviation_reason_code` grain, reserved `constraints`/`template_ref`).
- **m-3** — confirm `record_integrity` (+ the observe write-allowlist) home/typing.
- **m-5** — confirm `seat_archetype`/`authority_ceiling`/`human_mode`(posture) ride the F2 per-assignment home as declared, and the `surface_intent` vocabulary.
- **m-6** — confirm the delivery consumption of `record_integrity` + `(posture × surface_intent)`.

**Surfaced (honest, non-blocking — not folded, out of this dispatch's named scope):** the rest of the m-3 write-allowlist (`achieved_evidence`, per-field `evidence_integrity`, `*_RESULT`, `executable_claim_results`, `egress_scan_result`, `degradation_notes`, the routing-profile observed set) is **declared in §12 prose** but not as formal §17.3-style registry rows. The m-1 "allowlist resolves to an m-2-declared set" requirement is satisfied (record_integrity was the one missing rollup, now added), but if the CTO wants **row-level parity** for the full observe set, that's a small follow-up pass — flagging rather than silently scoping it in.

**Invariants preserved:** routing-record contract m-4-owned + unchanged (m-2 mirrors as FieldSpec); **R2 preserved** — new fields are declarations, the gate-referenceable allowlist still excludes model-identity fields, no predicate keys on model; reserved fields carry shape only, no Step-1 values; trust keyed to stamped seat `FROM`.

**Not done (by design):** Cluster 4 closes when m-4 confirms the mirror + m-3/m-5/m-6 confirm their fields + CTO re-verify + VP closure co-sign. No Step-1 PLAN opens until then; the phase transition itself stays the operator's to authorize.

Blocked: no — holding for the sibling confirms + CTO/VP re-verify.
Scope drift risk: none — §12/§17.3 + the named fields only; no routing-record contract change, no tag-value micro-fold, no R2 regression.

Tests / verification: read-only DESIGN fold; all claims E1 (source-cited to design §17.3/§17.6/§12/§18 + m-4 `:200-210` + m-3 `:107-112` + m-5 `:142` + m-6 `:113-116,130` + `ARCHITECTURE:187-188,349-352`).

ACTIONS_GIT_REF: folded `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` (§17.3 per-row enumeration + `deviation_reason_code`/`constraints`/`template_ref` rows; new §17.6 computed-field homes; §12 m-3/m-4 echoes + new m-5 line; §18 4b+4c fold-log; AC15); wrote this SITREP + appended `master/relays/INDEX.md`; no code/source/pcode edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-4 confirms the record mirror; m-3/m-5/m-6 confirm their field homes; then CTO re-verify + VP closure co-sign → Cluster 4 CLOSED → the full MUST gate is genuinely satisfied. m-2.planner holds — no self-advance to PLAN.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
