## SITREP — m-2 folded the CTO posture-home arbitration: standalone posture field REMOVED (rides `seat_archetype`); Cluster 4 m-2-side complete

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded fold applying a CTO cross-domain ruling; owners confirm; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: master.orchestrator-planner
CC: m-4.planner, m-5.planner, m-6.planner, master.orchestrator-reviewer, m-2.implementer, m-4.implementer, m-5.implementer, m-6.implementer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: readiness-fix-c4/DESIGN-orchestrator-planner-20260701-001537.md

**CTO ruling folded — I was corrected, and I accept it.** My 4b/4c fold declared `human_mode`/posture as a **standalone per-row field** of `routing_assignments`. That contradicted the c3 lock; the CTO arbitration (option 1, c3-lock-faithful) is correct. Read-only DESIGN fold applying a locked ruling; no source/`pcode/` edits, no PLAN, no closure claim.

**I re-verified the ruling against source (E1), not the SITREPs — the CTO is right:**
- `m-4 …routing-policy…:203` per-row set = `{seat, role, task_tag, declared_bucket, chosen_model, pin_mode, declared_deviated, seat_archetype, authority_ceiling}` — **no posture.**
- `m-5 …archetype-system-design…:142` (c3 LOCK): *"posture … rides the F2 per-assignment record home (**no new m-2 field**)."* And `:103`: `seat_archetype` carries **"default human-mode (posture)"** as an attribute.
- So posture rides `seat_archetype`; my standalone field *created* the drift it claimed to close. My earlier "posture has a clean F2 home (no gap)" was half-right (it does ride F2) but wrong on the mechanism (a **standalone field**, not `seat_archetype`).

**The bounded fix (exactly the three the dispatch specified):**
1. **Removed the standalone `human_mode`/posture per-row field** from §17.3 `routing_assignments`. The per-row set now matches m-4 `:203` exactly (no posture). §17.3 :292, §12 :179.
2. **Kept the posture value-enum `{interactive|away|unattended}`** as the m-5-owned delivery vocabulary m-6 binds (§17.6), **annotated** "derived from the recorded `seat_archetype` default-human-mode (m-5 `:103`/`:142`); NOT a `routing_assignments` field — m-6 resolves posture by reading `seat_archetype`." §17.6 :329.
3. **Folded m-5 Precision-2:** relabeled posture (and the posture/`surface_intent` vocab) from "opaque/reserved" to **"m-5-owned vocabulary, LOCKED at c3"** — the values ARE fixed, unlike `seat_archetype`/`slot_in`. §17.6 :329, §12 :181.

Propagated to AC15, §18 fold-log (the carve-out narrated as a correction), and the §12 m-4/m-5 echoes. Doc reads as final state (no internal contradiction).

**Acceptance ("Cluster 4 reconciled") — m-2 side met:** §17.3 no longer declares a standalone posture field (posture rides `seat_archetype`); the mirror follows m-4 `:203` un-extended; posture value-enum kept as m-5 vocabulary in §17.6. — E1, §17.3 :292 + §17.6 :329 + §12 :179–181.

**On the owners' confirms (dispatch asked them to confirm the ruling reconciles their positions):** the three clean confirms already landed (m-3 `record_integrity` `000930`; m-5 `seat_archetype`/`authority_ceiling`/`surface_intent` `235944`; m-6 delivery `235743`); m-4's carve-out `235500` is what the CTO arbitrated. My fold **honors m-5 `:142` as the governing lock** (not the looser confirm wording) — so m-5's `235944` value-enum blessing stands, and the standalone-field imprecision is removed. If m-5 instead intends the *resolved* posture to need its **own** recorded home distinct from `seat_archetype`, that is a c3-lock amendment to `:142` (separate lane), not this fix — I've folded option 1 as ruled; m-5 pushes there if I misread.

**Row-parity pre-PLAN SHOULD — tracked, not folded.** The CTO accepted m-3+m-6's row-level-parity ask (formal §17.3 rows for the remaining 7 observe fields) as a **pre-Step-1-PLAN SHOULD**, not a Cluster-4 blocker. Recorded in my doc as **§15 Q-F** — runs after Cluster 4 closes, before Step-1 PLAN opens. This is the same item I surfaced in `234702`; good to see it routed rather than scoped into the MUST gate.

**Invariants:** R2 held (posture is non-gate, never model); **no m-4 record-contract change**; **no `ARCHITECTURE` change**; `seat_archetype` stays opaque; **no c2/c3 reopen** (this *applies* the c3 lock `:142`, doesn't change it).

**Not done (by design):** Cluster 4 closes when m-4/m-5/m-6 confirm the ruling reconciles their positions + CTO re-verify (full mirror + all homes) + VP closure co-sign. No Step-1 PLAN opens until then; the phase transition stays the operator's to authorize.

Blocked: no — holding for the owners' ruling-confirms + CTO/VP re-verify.
Scope drift risk: none — exactly the CTO's three-item fix; no m-4 record change, no `ARCHITECTURE` change, no c3 reopen, no tag-value micro-fold.

Tests / verification: read-only DESIGN fold; all claims E1 (source-cited to design §17.3/§17.6/§12/§18 + m-4 `:203` + m-5 `:103`/`:142` + the arbitration `001537`).

ACTIONS_GIT_REF: folded `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` (removed standalone posture field §17.3; annotated posture-rides-`seat_archetype` + LOCKED-at-c3 relabel §17.6; §12 m-4/m-5 echoes; §18 carve-out + row-parity SHOULD; §15 Q-F; AC15); wrote this SITREP + appended `master/relays/INDEX.md`; no code/source/pcode edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-4/m-5/m-6 confirm the ruling reconciles their positions; CTO re-verify + VP closure co-sign → Cluster 4 CLOSED → full MUST gate satisfied. m-2.planner holds — no self-advance to PLAN.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
