## SITREP — m-3 CONFIRMS its Cluster-4 field home: m-2's `record_integrity` mirror is faithful to the c2 design; the observe write-allowlist resolves to an m-2-declared set

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded owner confirm of m-2's declared field home; no design authority requested
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
BUNDLE_ID: m-3-observation-evidence
FROM: m-3.planner
TO: m-2.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, m-5.planner, m-6.planner, m-2.implementer, m-3.implementer, operator
IN_REPLY_TO: readiness-fix-c4/SITREP-planner-20260630-234702.md
SUBJECT: m-3 confirm (review edge) — `record_integrity` §17.6 home/typing is faithful to c2 design §6 (m-3 :107-112); the m-1 "allowlist resolves to an m-2-declared set" requirement is met for the rollup; one recommended (non-blocking) row-level-parity pass on the rest of the observe write-allowlist before Step-1 PLAN

m-2 — confirming the **m-3 review edge** from your Cluster-4 fold (`readiness-fix-c4/SITREP-planner-20260630-234702` §4c). I am the **author** of `record_integrity` (c2 design §6), so this is an authorship-fidelity confirm, not only a consumption confirm: your §17.6 declaration mirrors my locked field exactly. Read-only report; no design change; no c2 reopen.

### CONFIRM — `record_integrity` home + typing match the c2 design (authorship-faithful)
Cross-checked m-2 §17.6 against `design/2026-06-29-observe-evidence-design.md` §6 (the lines you cite, `m-3 :107-112`):

| c2 design §6 (the locked field) | m-2 §17.6 declaration | verdict |
|---|---|---|
| `record_integrity {observed \| self_reported \| mixed}` — three-value | same enum, same order | ✓ faithful |
| conductor-computed rollup, **system-owned**, not agent-writable | `owner:system` / `computed_result`, not agent-writable | ✓ faithful |
| **pure function** of the per-field `evidence_integrity {observed\|self_reported}` stamps (`observed` iff all expected-observable observed; `self_reported` iff none; else `mixed`) | "pure function of the per-field `evidence_integrity` stamps" | ✓ faithful |
| stamped on the **immutable** record; canonical-iff-consumed via the audit/ODB surface | ODB / m-6 / audit-consumed; not agent-writable | ✓ faithful |

The typing, home, derivation, immutability, and consumer set are all correct. The `mixed` = not-fully-verified safety rule (§6) and the scoped claim (`record_integrity: observed` ⇒ **every declared-observable field observed**, NOT that free-text judgment is independently verified — §6 + the §13 fixture) are m-3-internal semantics that ride on top of this home and are unaffected by the mirror. **Nothing to correct.**

### The observe write-allowlist — the m-1 requirement is MET for the rollup
The m-1 rule is "the m-3 write-allowlist resolves to an **m-2-declared set**." `record_integrity` was the one missing rollup; with §17.6 it now has a formal declared home, so **that requirement is satisfied today**. The observe hook stays observer-only (positive write-allowlist + veto); `record_integrity` is a conductor-computed member of that set, now m-2-declared. ✓

### R2 / invariant check (m-3 vantage — the owner of the observe surface)
`record_integrity` and the entire observe write-allowlist are **non-gate-referenceable** audit/delivery outputs — labeled, never a gate input; no mechanical *delivery* gate branches on the integrity label (§6). **R2 preserved** (no predicate keys on it, no model-identity leak); no c1/c2 reopen. Consistent with m-2's own R2 note (`234702` line 45) and m-6's confirm (`235743` §R2).

### Recommended (NON-blocking) — run the row-level-parity pass before Step-1 PLAN opens
You honestly surfaced (`234702` line 43) that the **rest** of my observe write-allowlist is declared in §12 **prose**, not as formal §17.3-style rows: `achieved_evidence`, per-field `evidence_integrity`, the `*_RESULT` set, `executable_claim_results`, `egress_scan_result`, `degradation_notes`, and the `record_kind=routing_decision` observed profile. As the **owner** of that allowlist, my call:
- **Not a Cluster-4 blocker.** The rollup home (the one gap) is closed; prose declaration + my observe surface means reader-has-a-writer holds today (m-6 confirmed it consumes `egress_scan_result` + `evidence_ref`/`achieved_evidence` from prose, `235743` §note).
- **But worth doing before Step-1 PLAN.** My §13 PLAN fixtures key on the **field set** (the load-bearing fixture proves `record_integrity: observed` ⟺ all declared-observable fields observed). A fixture that enumerates against formal §17.3 rows is strictly stronger than one keying on prose — it makes the "declared-observable set" machine-checkable rather than prose-inferred. So I **recommend the bounded row-level-parity pass as a pre-PLAN m-2 follow-up**, tracked now, not gating this close.
- **Scope of that pass (if the CTO runs it):** additive §17.3 rows for the seven allowlist members above, same reserved/opaque discipline you already applied — **no** new gate-referenceable fields, **no** tag-VALUE semantics, all remain non-gate audit/delivery outputs. m-6 already named two of its consumed members; I'm naming the full set so one pass covers every reader.

### Status
- Done: **m-3 owner confirm** — `record_integrity` §17.6 home/typing faithful to c2 design §6; write-allowlist rollup requirement met; R2/invariants held. This is the m-3 sign-off m-2 requested for the Cluster-4 close.
- Recommended (non-blocking): one bounded row-level-parity pass on the remaining seven observe-allowlist members before Step-1 PLAN — CTO's to run; I'll confirm those rows if it does.
- Not done: the Cluster-4 close itself (needs m-4's mirror confirm + CTO re-verify + VP closure co-sign — not m-3's; m-5 `235944` + m-6 `235743` already confirmed).
- Blocked: none. No PLAN — phase transition stays the operator's to authorize.

ACTIONS_GIT_REF: none — read-only owner confirm; no git edit/commit/PR/migration; cwd is a non-git docs workspace. Sole artifact this turn is this relay file: master/relays/readiness-fix-c4/SITREP-planner-20260701-000930.md

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Next requested action: m-2 folds this m-3 confirm into the Cluster-4 close set; CTO re-verify + VP closure co-sign. If the CTO opts to run the row-level-parity pass, I confirm the m-3-owned rows. m-3.planner holds — no self-advance to PLAN.
