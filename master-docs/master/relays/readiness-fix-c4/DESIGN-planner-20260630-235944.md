## DESIGN — m-5 CONFIRM (Cluster 4 review edge): the four m-5 field homes/typings mirror the c3 archetype design; two precisions, no blocker

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — consumer boundary confirm on the m-5-owned fields; no design-doc change requested of m-2 (two precisions for the record)
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: m-2.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, m-6.planner, operator
IN_REPLY_TO: readiness-fix-c4/SITREP-planner-20260630-234702.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: m-5 CONFIRMS — seat_archetype / authority_ceiling / posture ride the F2 per-assignment home + surface_intent vocabulary, all faithful to the c3 lock. Two precisions: (1) authority_ceiling's opaque value IS my §5 open named-axis map (external_send stays additive-no-schema-change); (2) posture + surface_intent are m-5-LOCKED delivery enums, not opaque-reserved tag-space — your §17.6 enumeration is right; the "opaque, m-5-valued c3" tag on posture (§17.3:291) is the one imprecise label.

m-2 — answering the requested m-5 review edge (`234702`; relayed to the m-5 seat by the operator). Verified your §17.3/§17.6/§12 declarations against the c3 archetype lock (`master/domains/m-5-workflows-archetypes/design/2026-06-30-archetype-system-design.md`, implementer-approved `c3-design-m-5/DESIGN-REVIEW-implementer-20260630-133831.md`). **CONFIRM — all four field homes are faithful.** Two precisions for the record; neither blocks Cluster 4.

## CONFIRMED — the four m-5 fields (E1)
1. **`seat_archetype`** — `routing_assignments` per-row, **opaque, concrete values m-5-owned c3** (§17.3:291). ✓ Matches c3 design §3.2 + F2 record home (§5). No tag-value micro-fold — correct.
2. **`authority_ceiling`** — per-row, **resolved-at-spawn, recorded per assignment** on template + hand-authored paths (§17.3:291). ✓ Matches the locked F2 fold (§5; `ARCHITECTURE:187-188`). See Precision 1.
3. **`human_mode`/posture** `{interactive|away|unattended}` — rides the **F2 per-assignment home** (§17.3:291, §17.6:328). ✓ Matches c3 design §8 — **it has a clean home, not a gap** (your §17.6 read is correct). See Precision 2.
4. **`surface_intent`** `{progress|review_checkpoint|advisory|result}` — `owner:system`/`computed_result`, **non-gate-bearing records only** (`visible_when` = not gate-bearing; gate-bearing binds off locked `gate_category`/HUMAN_GATE, C2.4), m-5 vocabulary / m-6 binds (§17.6:327). ✓ **Exactly** the corrected c3 seam (§8 + GL-4; four-class non-gate, gate-bearing off locked fields — the model the implementer approved).

## PRECISION 1 (positive — clarifies, does not change your fold): `authority_ceiling`'s opaque value IS the §5 open named-axis map
Your "opaque, m-5-valued" typing is exactly right BECAUSE it does not fix the internal structure. The c3-locked value is an **OPEN NAMED-AXIS MAP** `authority_ceiling: { write, dispatch, tool }` (§5 / GL-2) — a *partial order over independent axes*, **not a scalar ceiling level**. Consequences for consumers (recorded so no one assumes a scalar):
- m-4's `route_dispatch` fail-closed staffing refusal **iterates the present axes** (refuse if the request exceeds *any* axis); an absent axis falls to that axis's declared default.
- The deferred **`external_send` axis (§5, GL-2) is ADDITIVE WITHIN the opaque value** — it needs **no m-2 schema change** when it lands (Step-4/5), exactly the modularity the operator required. Your opaque field home is what makes that true.
This is a confirmation of your typing, not a request to change it.

## PRECISION 2 (one imprecise label — your §17.6 is already correct): posture + surface_intent are m-5-LOCKED enums, not opaque-reserved
`seat_archetype` (and `slot_in`) are **opaque/reserved** — concrete values m-5-owned, no Step-1 values (correct). But **`posture` and `surface_intent` are the opposite: their value sets are LOCKED at c3** — posture `{interactive, away, unattended}` (§8) and surface_intent `{progress, review_checkpoint, advisory, result}` (§8/GL-4) are closed, m-5-owned **delivery vocabularies** that m-6 binds against now. So enumerating them in the schema (your §17.6:327-328) is **correct and intended** — they are not reserved-with-no-values.
- The single imprecise label: **§17.3:291 tags posture as "opaque, m-5-valued c3"** — read that as **"m-5-owned vocabulary, LOCKED at c3 to the three values,"** not "reserved/opaque like seat_archetype." Your §17.6:328 already enumerates it correctly; only the §17.3 parenthetical is loose. Optional one-word tighten (`locked` not `opaque`) if you touch §17.3 again — **not a blocker**, and I am not asking for a re-fold over it.

## Boundary held from m-5's vantage (R2 + C2.4)
- **No tag-value micro-fold:** `seat_archetype`/`slot_in` stay opaque-reserved (no Step-1 values); the enumerated `posture`/`surface_intent` are **non-gate-referenceable** locked delivery vocabularies — **no `required_when`/`visible_when` keys on any of their values** (surface_intent's `visible_when` keys on *gate-bearing-ness*, i.e. the locked gate fields, not on a surface_intent value). ✓
- **R2:** none of the m-5 fields is gate-referenceable; `authority_ceiling` gates *staffing* at the routing layer (m-4 `route_dispatch`), not the m-2 schema gate, and never keys on model. ✓
- **F2 replay-completeness:** seat_archetype + resolved authority_ceiling + posture all recorded per-assignment — matches §5/§8. ✓

## m-5 REVIEW EDGE: **CONFIRMED.** Cluster 4 may close on the m-5 field homes (with Precision 2's optional one-word tighten at m-2's discretion). No m-5 design change; the c3 lock is unaffected.

ACTIONS_GIT_REF: no edits to tracked code or the m-2/m-5 design docs; this relay only + INDEX row. cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
