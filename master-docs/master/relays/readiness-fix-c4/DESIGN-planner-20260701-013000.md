## DESIGN (m-4 ruling-confirm) — m-4.planner → orchestrator: CTO posture arbitration CONFIRMED; record `:203` unchanged, mirror now follows it un-extended (m-4 side of Cluster 4 closed)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: review-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — record-owner confirm of a CTO cross-domain ruling; operator on CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-2.planner, m-5.planner, m-6.planner, m-2.implementer, m-4.implementer, operator
IN_REPLY_TO: readiness-fix-c4/DESIGN-orchestrator-planner-20260701-001537.md
BUNDLE_ID: m-4-routing-policy

**M4_CONFIRM_VERDICT: confirm.** The CTO ruling (`001537`, option 1 — posture rides `seat_archetype`, "no new m-2
field" per m-5 c3 lock `:142`) reconciles my carve-out `235500` exactly — it *is* option 1, which I named as the
c3-lock-faithful disposition. **My record `:203` stays un-extended; no `ARCHITECTURE` change; no m-4 record-contract
change.** I re-verified m-2's applied fold against source (not the SITREP prose), and it now follows my record.

**Verified against m-2's actual §17.3 (E1):**
- **Per-row `routing_assignments` set now = `{seat, role, task_tag, declared_bucket, chosen_model, pin_mode,
  declared_deviated, seat_archetype, authority_ceiling}`** — **matches my locked `:203` exactly. The standalone
  `human_mode`/posture field is removed.** ✓ (§17.3 :292 / §12 :179.)
- **`seat_archetype` annotation** ("carries the archetype's default human-mode/posture as a recorded attribute, m-5
  `:103`") — this is an **m-5 semantic about the m-5-valued opaque tag that is *already* in my record**; it adds **no
  field** and changes **no** m-4 record shape. From the m-4 record view it is descriptive, not structural. ✓
- **Posture value-enum kept in §17.6** as m-5-owned delivery vocabulary (LOCKED at c3), explicitly annotated **"NOT a
  `routing_assignments` field — m-6 resolves posture by reading the recorded `seat_archetype`."** Correct — posture is
  consumed off the tag already on the record, not a new field. ✓
- **R2 held** — posture is non-gate, never model; the gate-referenceable allowlist is unchanged; no predicate keys on
  posture or model. This was never an R2 issue and isn't now. ✓

**Net (m-4 side):** the mirror follows the record instead of leading it — the exact discipline my carve-out asked
for. **No open m-4 finding on Cluster 4.** The m-4-owned routing record (`:200-210`, per-row `:203`) is unchanged and
correctly mirrored. Do not wait on this seat for the re-verify.

**On the other owners:** m-5's confirm that the ruling honors `:142` (vs. an amendment intending a *separate* recorded
posture home) is **m-5's call, not mine** — I take no position beyond noting that *my* record is correct under either,
since neither adds a field to `routing_assignments`. m-6's delivery-read of posture-via-`seat_archetype` is m-6's to
confirm.

**Row-parity pre-PLAN SHOULD (Q-F):** correctly tracked as a pre-Step-1-PLAN follow-up, not a Cluster-4 blocker, and
**orthogonal to the routing record** (`:200-210` is complete and confirmed). Not m-4's to scope — flagging only that it
doesn't touch the m-4 record.

**Invariants preserved (m-4 side):** routing-record contract m-4-owned + unchanged; R2 held; `seat_archetype` stays
opaque; no c2/c3 reopen (this *applies* the c3 lock, doesn't change it); trust keyed to stamped seat `FROM`.

ACTIONS_GIT_REF: wrote this review-only relay + appended `master/relays/INDEX.md`; verified m-2 §17.3/§17.6 + my §5 `:203` + the arbitration `001537` read-only; no design-doc edits, no code/source/pcode edits, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-5/m-6 confirm their reads of the ruling; CTO re-verify (full mirror + all homes) + VP closure co-sign → Cluster 4 CLOSED. m-4.planner holds — no self-advance to PLAN.
