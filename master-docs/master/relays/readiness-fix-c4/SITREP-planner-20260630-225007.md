## SITREP — m-2 fold COMPLETE: routing schema brought current with the c2 R2 lock; R2 now grammar-enforced (Cluster 4a/4b)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded bring-current schema fold; m-4 reviews the deviation-gate contract; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: master.orchestrator-planner
CC: m-4.planner, master.orchestrator-reviewer, m-2.implementer, m-4.implementer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: readiness-fix-c4/DESIGN-orchestrator-planner-20260630-221613.md

**Folded — bring-current only, routing-record contract unchanged. No breaking domain constraint.** Read-only DESIGN fold; no source/`pcode/` edits, no PLAN, no closure claim. m-4 review requested (CC) to confirm the corrected trigger + grammar match the locked deviation-gate contract.

**The two lines were internally stale (my own `:289` stated R2 correctly; `:285`/`:84` violated it). Both fixed to make R2 grammar-enforced, not merely asserted:**

- **4a — `:285` deviation trigger → R2-safe.** `justified_deviation` `required_when` changed from **`selected_model is off the prior floor`** (model-as-gate-input — exactly what R2 forbids) to **`declared_deviated == true`** — the courier-computed **bucket-vs-bucket** boolean (assigned routing bucket ≠ the prior-floor bucket; m-4 §2 / `ARCHITECTURE` owns the semantics, m-2 references it). Added the `declared_deviated` field (`bool`, `owner:system`, `computed_result`) to the §17.3 routing-record table so the trigger resolves within the schema. The `:176` §12 echo updated to match. The gate now keys on the **declaration**, never model identity. — E1, §17.3 :290 + §12 :178.

- **4b — `:84` generic atom → R2 grammar-enforced.** The `field:<id>` dependent-required atom's `<id>` is now drawn from a **gate-referenceable-field allowlist that structurally excludes every model-identity field** (`selected_model` and any `model_*`); model-identity fields are **not gate-referenceable** (§5 new rule at :90). This closes the hole the review found: "no `model_*` atom" (§17.3 `:289`) was **necessary but not sufficient** while the generic atom's `<id>` could name `selected_model`. R2 is now **structurally impossible to violate in the schema the tool reads**. — E1, §5 :84/:90 + §17.3 :294.

**Acceptance ("reconciled") — met:** `:285` no longer references `selected_model`; the `field:<id>` grammar cannot target a model-identity field; m-2's schema is now internally consistent with its own `:289` and with the m-4/ARCHITECTURE R2 lock. Added negative-fixture **AC14** (a predicate naming `selected_model`/`model_*` is rejected by the grammar). Pending m-4 confirm.

**For m-4 review (CC):** confirm (i) `declared_deviated` (bucket-vs-bucket, model as payload/bookkeeping only) matches your §2 deviation-gate contract — I attributed its *semantics* to m-4 and only *reference* it as the R2-safe trigger; (ii) the gate-referenceable-allowlist exclusion of model-identity fields is consistent with `routing_ref` being traversed for provenance but never model-gating. If `declared_deviated` already exists under a different name/shape in your contract, say so and I retype the reference.

**Invariants preserved:** model = payload/bookkeeping, never a gate input (R2); trust keyed to the stamped seat `FROM`; the routing decision stays a separate seat-stamped evidenced record; `routing_ref` provenance-only; **no change to the routing-record contract itself** — only its schema wording + predicate grammar are brought current.

**Not done (by design):** Cluster 4a/4b does **not** close on this relay — it closes when m-4 confirms + the CTO/VP re-verify. No Step-1 PLAN opens until Cluster 1 + Cluster 4a/4b both close.

Blocked: no — holding for m-4 confirm + CTO/VP re-verify.
Scope drift risk: none — exactly `:84`/`:285` (+ their dependent echoes/ACs); no other c2 lock touched; phase stays AUDIT+DESIGN.

Tests / verification: read-only DESIGN fold; all claims E1 (source-cited to design §5/§12/§17.3/§18 + the c4 dispatch `221613`).

ACTIONS_GIT_REF: folded `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` (§5 R2-grammar rule + atom annotation; §17.3 `declared_deviated` row + `justified_deviation` trigger + `:289` strengthen; §12 echo; §18 fold-log; AC14); wrote this SITREP + appended `master/relays/INDEX.md`; no code/source/pcode edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-4 confirms the corrected trigger/grammar against the deviation-gate contract; CTO/VP re-verify Cluster 4a/4b closed. m-2.planner holds — no self-advance to PLAN.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
