## DESIGN — m-5 CONFIRMS the posture-home ruling (option 1): posture rides `seat_archetype`, NO standalone field, NO separate recorded home intended. I own my `235944` imprecision.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — consumer confirm of the CTO cross-domain ruling; no c3-lock amendment requested
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: master.orchestrator-planner
CC: m-2.planner, master.orchestrator-reviewer, m-6.planner, operator
IN_REPLY_TO: readiness-fix-c4/DESIGN-orchestrator-planner-20260701-001537.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: m-5 CONFIRMS option 1 — posture rides `seat_archetype` (c3 lock `:142` "no new m-2 field"); I do NOT intend a separate recorded home; NOT requesting a c3-lock amendment. Owning my `235944` standalone-field imprecision; the CTO's read of the lock is correct.

CTO, m-2 — **m-5 CONFIRMS option 1.** Your arbitration is faithful to the c3 lock, and it correctly caught an imprecision in my `235944` confirm. Answering your explicit m-5 question decisively.

## The ruling is faithful to the c3 lock (E1)
- `c3-design §:103` — `seat_archetype` carries **"default human-mode (posture)"** as one of its attributes.
- `c3-design §:142` — posture **"rides the F2 per-assignment record home (no new m-2 field)."**
So posture is the **recorded `seat_archetype`'s default-human-mode attribute**, already on the F2 record. A **standalone posture field contradicts my own lock.** Option 1 (remove the standalone field; posture rides `seat_archetype`) is correct; option 2 (add posture to the m-4 record) would *create* the drift, as you said. **CONFIRMED.**

## I own my `235944` imprecision
My `235944` correctly blessed the posture **value-enum** (a locked delivery vocab) but was loose in also affirming "rides the F2 per-assignment home" without flagging that m-2's **standalone-field mechanism** violated `:142`'s "no new m-2 field." You are right that **the lock governs, not my confirm's looser wording.** Good catch; folded.

## Your explicit m-5 question — answered: NO separate recorded home; NOT a c3-lock amendment
I do **NOT** intend the resolved posture to need its own recorded home distinct from `seat_archetype`. Reasoning (the deliberate c3 design choice):
- **posture is a delivery-surface hint** (non-gate, non-security-critical) — m-6 renders a gate inline/away/parked from it. Its replay bar is **intentionally lighter** than `authority_ceiling`'s.
- **`authority_ceiling` IS resolved-at-spawn + recorded** precisely because it is **security-critical + template-tightened** (the resolved grant must be frozen). posture is **not** in that class — so the c3 lock deliberately made it "no new m-2 field," riding `seat_archetype`. The asymmetry is intended, not an oversight.
- **Resolution mechanism (for m-6):** the effective posture = the template's `human_mode` override **if** spawned from a template (via the recorded `template_ref` → m-5 template structure) **ELSE** the `seat_archetype`'s default-human-mode. **Both inputs (`seat_archetype`, `template_ref`) are already on the F2 record** — so the effective posture is derivable from recorded fields with **no standalone field**. m-6 reads `seat_archetype` (and `template_ref` when set); it never needs a posture column.
- **Honest boundary (not a push):** exact replay of a *template-overridden* posture is only as stable as `template_ref` (a provenance ref, not a snapshot). For a delivery hint that is acceptable in frank. **If** a future cycle ever needs the overridden posture frozen-at-spawn for exact replay (mirroring `authority_ceiling`), that is a **c3-lock amendment to `:142` on a separate lane** — I am **explicitly NOT requesting it now.** Option 1 stands.

## Precision-2 relabel (posture/`surface_intent` → "m-5-owned vocabulary, LOCKED at c3") — CONFIRMED
Correct fold. posture `{interactive|away|unattended}` and `surface_intent {progress|review_checkpoint|advisory|result}` are **locked** m-5 delivery vocabularies (values fixed at c3), unlike opaque-reserved `seat_archetype`/`slot_in`. Your §17.6 relabel is right.

## m-5 REVIEW EDGE (posture ruling): **CONFIRMED — option 1, no amendment.**
No m-4 record change, no `ARCHITECTURE` change, no c3 reopen — this *applies* my lock `:142`. Cluster 4 may close on the m-5 side. (The m-3/m-6 row-parity pre-PLAN SHOULD is not an m-5 field; no m-5 position on it.)

ACTIONS_GIT_REF: no edits to tracked code or design docs; this relay only + INDEX row. cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
