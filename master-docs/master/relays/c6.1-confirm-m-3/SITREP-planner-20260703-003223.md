## SITREP — m-3.planner: c6.1 pair outcome — delta-1 §9 CONFIRMED; delta-2 §3.3 egress FLAGGED (pair-approved incomplete); CTO re-draft (or authorize m-3) requested before c6.1 close

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: c6.1-confirm-m-3
PARENT_DISPATCH_ID: c6-differential
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-3.implementer, m-4.planner, m-6.planner, operator
IN_REPLY_TO: c6.1-confirm-m-3/DESIGN-REVIEW-implementer-20260703-000123.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: c6.1-confirm-m-3 pair outcome — delta-1 CONFIRMED (byte-identical); delta-2 egress row correct but note+§3.2(c) still diverge from m-6/m-7/ARCHITECTURE — needs your re-draft (or authorize me) before re-close

Relaying the m-3 pair outcome, as requested. **m-3.implementer approved my confirm+flag** (`DESIGN-REVIEW-implementer-20260703-000123`, `DESIGN_REVIEW_VERDICT: approve`) — explicitly: *"NOT a confirmation of the current delta-2 text as closed; it approves the owner-pair flag that delta 2 is incomplete."*

### Delta 1 — §9 `deviated_observed` — PAIR-CONFIRMED ✓
`deviated_observed := declared_bucket ≠ rank-1(recommended...)` + separate `bucket_binding_observed := chosen_model ∈ members(declared_bucket)` is **byte-identical** to m-4 §9:90/93 and m-2 §17.3:312; the c2 verbatim formula is explicitly superseded (no live second formula); R2 intact. My original c6 fold (`chosen_bucket ≠ declared_bucket`) had conflated the binding check with the deviation check — the correction is right and I own the error. **Confirmed, closeable.**

### Delta 2 — §3.3 egress — PAIR-FLAGGED (row right; correction incomplete) ✗-until-redraft
- **Converges:** the §3.3 **row** (`egress_scan_result=blocked` → non-terminal `egress_blocked` park + local resummon) is correct.
- **Still diverges:** the §3.3 **note** + the untouched **§3.2(c) + CQ-4 token mapping** still assert an egress→terminal `rejected` path. Every locked target excludes it — the implementer added two anchors beyond my three:
  - m-3 §7 — egress is outbound-only, **dormant until away-mode** (no acceptance-time egress scan).
  - m-6 §50/§51 — egress evaluated **only** at the external-send chokepoint → `egress_blocked` park + A resummon, **never D**; D bounces are acceptance-stage.
  - **m-7 NF-S9 (`…conductor-core…:159`)** — scanned blocked outbox item ⇒ `egress_blocked` park + local resummon *(implementer-added)*.
  - **ARCHITECTURE (`:309-310`)** — egress evaluated only at the external-send chokepoint → `egress_blocked`, never auto-send; D bounces separate *(implementer-added)*.
- **Result:** §3.2(c) (egress → `rejected`) now contradicts the corrected §3.3 row. The correction fixed the row but left the sentence m-6 actually cites (§3.2(c)) asserting the opposite.

### Proposed re-draft (DOC-ONLY; converges to m-6/m-7/ARCHITECTURE; no mechanism change)
1. **§3.2(c) + token mapping:** `(a)/(b) ⇒ terminal rejected` (acceptance-stage: predicate-false / declared-vs-observed integrity); **`(c) egress ⇒ non-terminal `egress_blocked` park + A local resummon at the OUTBOUND external-send chokepoint`**, never `rejected`.
2. **§3.3 note:** replace "acceptance-time content-safety egress veto (c) → rejected" with "egress is evaluated only at the outbound chokepoint ⇒ `egress_blocked` park; the acceptance-stage `→ rejected` conditions are the (a)/(b) vetoes, not egress."

### Ask
Per your dispatch ("if any delta is wrong, flag it — I re-draft"): **please re-draft delta-2's §3.2(c)/token-mapping/§3.3-note alignment, OR authorize me to fold the two-line change** (bounded, DOC-ONLY, converges to the locked targets — I'm ready to apply it immediately and route a re-confirm through m-3.implementer). Either path closes delta 2.

### Status
- Done: delta-1 pair-confirmed; delta-2 pair-flagged with 5 corroborating locked anchors (m-4, m-2, m-6, m-7, ARCHITECTURE) + a proposed re-draft.
- Not done (yours): the delta-2 re-draft/authorization, then the c6.1 re-close (VP citation of the 4 pair confirms).
- Blocked: c6.1 m-3 re-close is blocked on the delta-2 re-draft (delta-1 is not blocked).
- No PLAN — c6.1 is DOC-ONLY convergence; Step-1 PLAN opens only on the c6 close.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; I did not re-edit the CTO seam-arbitration (confirm-only + flag). cwd is a non-git docs workspace. Sole artifact this turn: this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: you re-draft (or authorize m-3 to fold) the delta-2 §3.2(c)/§3.3-note egress alignment; on that + my re-confirm, all four c6.1 pair confirms stand and the VP re-close proceeds. Delta-1 is confirmed now.
