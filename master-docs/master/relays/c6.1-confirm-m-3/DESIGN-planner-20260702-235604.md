## DESIGN — c6.1-confirm-m-3: delta-1 (§9) CONFIRMED byte-identical; delta-2 (§3.3 egress) FLAGGED — row correct, but note + §3.2(c) diverge from m-6; implementer adversarial review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6.1-confirm-m-3
PARENT_DISPATCH_ID: c6-differential
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, m-6.planner, operator
IN_REPLY_TO: c6.1-confirm-m-3/DESIGN-orchestrator-planner-20260702-233004.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: c6.1 owner-confirm — delta-1 §9 CONFIRM (byte-identical to m-4 §9:90/93 + m-2 §17.3:312); delta-2 §3.3 egress FLAG (row converges to m-6 §2:46, but the note + §3.2(c) still assert an egress→rejected path m-6/§7 exclude) — CTO re-draft requested

m-3.implementer — my owner-confirm of the two CTO-applied c6.1 corrections is below; **one confirm, one flag.** Please adversarially review both my confirm and my flag and return `DESIGN_REVIEW_VERDICT`. The CTO asked for extra scrutiny on the egress one — I gave it, and it does need a further re-draft.

### Delta 1 — §9 `deviated_observed` formula — **CONFIRM (byte-identical, converges, no contradiction)**
- **(a) converges + byte-correct:** the corrected §9 reads `deviated_observed := declared_bucket ≠ rank-1(recommended bucket for (role, task_tag))` + the separate `bucket_binding_observed := chosen_model ∈ members(declared_bucket)`. This is **byte-identical** to **m-4 §9** (`…routing-policy…:90` + `:93`) and **m-2 §17.3** (`…form-schema…:312`, "`declared_bucket ≠ rank-1(recommended)`"). ✓
- **My c6 fold was genuinely wrong** — it wrote `chosen_bucket ≠ declared_bucket`, which is the **binding** check, not the **deviation-from-floor** check. The correction properly separates the two atoms. Good differential catch.
- **(b) no contradiction:** the m-3-side-commitments paragraph is updated to match; the verbatim c2 block's old `chosen_model ∉ rank-1(...)` is explicitly marked *superseded*, so it reads as history, not a live second formula. **(c) R2 intact** — bucket-vs-bucket, `deviated_observed` non-gate-referenceable (m-2 §17.3:312 confirms), model identity never in a predicate.

### Delta 2 — §3.3 egress row — **FLAG (row correct; note + §3.2(c) do NOT converge to m-6)**
- **What converges (confirm this part):** the §3.3 **row** — `egress_scan_result=blocked` → **non-terminal `egress_blocked` park + local resummon**, not terminal `held` — is **correct** and matches **m-6 §2:46** ("egress-block … not a terminal token") + m-6 §82 (`egress_blocked` state) + m-7 NF-S9. My original c6 row (egress → `held`) was wrong; the park is right. ✓
- **What still diverges (the flag):** the correction's **note** says *"distinct from the acceptance-time content-safety egress veto (c), which is terminal `rejected` per §3.2."* That egress→rejected path **does not exist** in the locked model:
  - **m-3 §7** — egress is a **conductor-governed OUTBOUND scan, dormant until the first external send** (away-mode bridge). There is **no acceptance-time egress scan** to produce a `rejected`.
  - **m-6 §51** — "egress is evaluated **only** at the external-send chokepoint … the D bounces are evaluated at acceptance." Egress is **not** an acceptance-stage → `rejected` veto.
  - **m-6 §50** — explicitly reads **m-3 §3.2(c)/§7** as *that outbound chokepoint* → `egress_blocked` park + **A** local resummon, **"never a D author-bounce."**
- **Consequent internal contradiction (confirm criterion b fails as-is):** my **§3.2(c)** + the **CQ-4 token mapping** still group *"a failed egress scan (§7)"* under *"(a)/(b)/(c) … ⇒ terminal `rejected`."* That now contradicts the corrected §3.3 row **and** diverges from m-6 §50/§51. The CTO correction fixed the §3.3 row but left §3.2(c) — the sentence m-6 actually cites — asserting the opposite disposition.

### Proposed re-draft (CTO seam-arbitration, or authorize me to fold — DOC-ONLY, converges to m-6):
1. **§3.2(c) + token mapping:** pull egress out of the acceptance-stage `→ rejected` group. `(a)/(b) ⇒ terminal rejected` (predicate-false / declared-vs-observed contradiction, evaluated at acceptance); **`(c) egress ⇒ the non-terminal `egress_blocked` park + A local resummon at the OUTBOUND external-send chokepoint** (m-6 §50/§51, m-3 §7), never terminal `rejected`.**
2. **§3.3 note:** replace "the acceptance-time content-safety egress veto (c) → rejected" with: "egress is evaluated **only** at the outbound external-send chokepoint (m-3 §7 dormant-until-away-mode; m-6 §51) ⇒ non-terminal `egress_blocked` park; the acceptance-stage `→ rejected` conditions are the predicate-false / integrity (a)/(b) vetoes, **not** egress."

This makes §3.2(c) ↔ §3.3 ↔ m-6 §2:46/§50/§51 ↔ m-3 §7 all state one disposition. No mechanism change, no new acceptance-time scan invented (which would violate the c6.1 "no mechanism change" bound).

### Confirm summary
- **Delta 1 (§9):** CONFIRM — byte-identical to m-4 §9:90/93 + m-2 §17.3:312; converges; no contradiction; R2 intact.
- **Delta 2 (§3.3):** the **park disposition CONFIRMS**, but the correction is **incomplete** — its note + the untouched §3.2(c)/token-mapping still assert an egress→`rejected` path excluded by m-6 §50/§51 + m-3 §7, leaving §3.2(c)↔§3.3 contradictory. **Requesting the CTO re-draft** (or authorization for me to fold the two-line §3.2(c)/§3.3-note alignment).
- Sanctioned-claims / D5 vocab / byte-exact `{accepted, rejected, held}` — untouched by both deltas.

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/c6.1-confirm-m-3/DESIGN-planner-20260702-235604.md` — OK.
- Delta-1 alignment: `master/domains/m-4-routing-policy/design/2026-06-29-routing-policy-design.md:90,93`; `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md:312` — byte-match.
- Delta-2 evidence: m-3 §7 (dormant/outbound); `master/domains/m-6-human-surface-scheduler/design/2026-06-30-human-surface-scheduler-design.md:46,50,51,82` (egress = outbound chokepoint → `egress_blocked` park, never terminal / never D); m-3 §3.2(c) + §3.3 (current live divergence).
- `git status --short` — unavailable: cwd is not a git repo (docs workspace).

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; I did NOT re-edit the CTO-applied deltas (confirm-only; delta-2 flagged for CTO re-draft). cwd is a non-git docs workspace. Sole artifact this turn: this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-3.implementer adversarial review of this confirm+flag; then the CTO re-drafts the delta-2 §3.2(c)/§3.3-note alignment (or authorizes me), I re-confirm, and the c6.1 re-close proceeds. Delta-1 is confirmed now.
