## DESIGN-REVIEW request — m-3.planner → m-3.implementer: the frank observe/evidence design draft

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c2-design-m-3
PARENT_DISPATCH_ID: c2-design-m-3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-review request; the operator-grill items are already resolved (GRILL_LOCK c2-grill-m-3, folded §12)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, operator
IN_REPLY_TO: c2-design-m-3/DESIGN-orchestrator-planner-20260629-203747.md
SUBJECT: DESIGN-REVIEW request — full m-3 observe-as-send design draft (grill resolved, seam folded verbatim, m-5 reserved, F5 novelty stated); return approve | must-revise | reject-narrow | human-decision-required

m-3.implementer — design draft is ready for your adversarial design-review. This is the planner→implementer DESIGN-REVIEW request (Template I). Respond read-only `PHASE: DESIGN-REVIEW`, parent to this relay, carry `DESIGN_DOC_ID: c2-design-m-3-observation-evidence`, and return `DESIGN_REVIEW_VERDICT`. I do not advance to a design-doc PLAN until you approve (and not even then without orchestrator PROCEED-TO-PLAN — phase band is DESIGN only).

**Design doc:** `master/domains/m-3-observation-evidence/design/2026-06-29-observe-evidence-design.md`.

**What changed since your independent audit pass (your `observe_gate()`/`observe_result{}` strawman is adopted):**
- §3 API surface = your signature, with the closed R3 write-allowlist made explicit (§3.1) and the veto conditions enumerated (§3.2: integrity-labeling never vetoes; only observed-false predicates, the generic declared-vs-observed integrity veto, and egress do).
- §4 evidence ladder + **check-registry** — grill Q1 resolved to **conductor-owned registry, arbitrary lane code rejected** (conductor = TCB); the registry **unifies executable-claims with the m-5 archetype gates**; base checks active at Step-1, side-effecting/expensive operator-gated.
- §5 per-phase done-predicates + the **m-5 archetype-tag reservation** (candidate mappings surfaced, not closed; the two verifier-tamper-resistant invariants called out).
- §6 `evidence_integrity` — grill Q4 resolved: per-field two-value stays locked R3; a **conductor-computed three-value `record_integrity {observed|self_reported|mixed}`** stamped on the immutable record for human audit (pure function of per-field; `mixed`=not-fully-verified for any trust reduction).
- §7 egress — grill Q2 resolved: **always-block + hold-and-resummon**, no default auto-redact, operator-configurable rule set; promote the agent-scripts/claude-code engines; dormant until the first external send.
- §8 opaque-lane floor — grill Q3 resolved: **honest `self_reported` floor**; the routing-deviation check (§9) noted as NOT needing DI-5 (snapshot-provenance).
- §9 the m-3↔m-4 seam folded **verbatim** (reconciled both sides).
- §11 F5 precise-novelty (no overclaim over the partial existing primitives).
- §12 GRILL_LOCK c2-grill-m-3; §14 lock prerequisites.

**Please adversarially test, in particular:**
1. **§3.2 veto boundary** — is the line exactly right that *integrity-labeling never vetoes* but the *generic declared-vs-observed integrity veto* does? Any case where a `self_reported` record should have been blocked, or a labeled record wrongly blocked?
2. **§4 check-registry** — does "conductor-owned registry, no arbitrary lane code" actually cover the Step-1 done-predicates without a capability gap? Is unifying executable-claims with the m-5 archetype gates sound, or does it overload one mechanism?
3. **§6 `record_integrity`** — is the pure-function rollup + pessimistic-trust reduction watertight? Any way a `mixed` record is mistaken for `observed` by a consumer?
4. **§9 seam** — confirm the verbatim fold matches `COORD-RECONCILE-planner-20260629-193400.md`; flag any drift.
5. **§5 / §14** — is the m-5 reservation drawn at the right line (mechanism-only for m-3; tag-space for m-5)?
6. **F5 (§11)** — any residual overclaim vs the surveyed partial primitives?

Phase band: DESIGN only — no PLAN/IMPL/merge. On your approve I report design-complete to the orchestrator and hold for the c2 lock.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd is a non-git docs workspace. Artifacts this turn: the design doc (`master/domains/m-3-observation-evidence/design/2026-06-29-observe-evidence-design.md`) + this relay.

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
