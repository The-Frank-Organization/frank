## DESIGN — conductor-core engine design-of-record r1 + DESIGN-REVIEW request

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-design-m-7
PARENT_DISPATCH_ID: c4-design-m-7
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the operator grill is COMPLETE (GRILL_LOCK c4-grill-m-7 folded, §14 of the design doc); CQ ratifications ride their COORDs
GRILL_REQUIRED: yes
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: c4-design-m-7/DESIGN-orchestrator-planner-20260701-162721.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: conductor-core DESIGN r1 ready for adversarial review — grill locked, seam matrix biting, CQ ledger carried

Partner — the conductor-core design-of-record r1 is written and ready for your DESIGN-REVIEW:

**Design doc:** `master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md`

What it contains (per the dispatch deliverables):
1. **The engine** (§1–§11): one conductor process — N mail-carrier channel handlers → durable append+fsync intake journal → ONE commit-loop thread on a plain FIFO (all mutations; reads concurrent on immutable committed records) → Package-A crash-atomic commit (canonical-record `rename()` = the single pivot; INDEX/rendered/mailboxes = derived projections via a redo journal; fsync-before-rename + dir-fsync-after) → phases 0–4 recovery (genesis-digest fail-closed → scan/quarantine → append-only projection rebuild → runtime tables incl. burned-nonce set + seat bindings → FIFO refill from intake−outcomes → open) → typed fault taxonomy ({accepted, rejected, held}; HELD for authority-path internal faults; never silent-accept, never brick) → trusted config load-once/digest-in-genesis/restart-only → MCP interface guardrail (exactly {submit, project, read}; submit schema = the rendered m-2 form; pipe-write wake with per-runtime adapters + per-lane qualification probes incl. non-frontier + local lanes) → local-outbox-only egress → genesis/GC.
2. **The completed seam matrix at LOCK grain** (§12): all 18 rows with authored biting negative fixtures NF-S1..NF-S18 (four bind explicitly to CQ COORD outcomes and say so).
3. **The fixture set in executable-claim form** (§13): F1–F8 + G(i–iii) + two new grilled fixtures (F9 durable-FIFO/no-re-emission; F10 fsync-ordering crash sweep).
4. **GRILL_LOCK c4-grill-m-7** (§14): all six dispatch agenda items operator-resolved one at a time, plus three operator riders — durable FIFO (crash-surviving queue), atomic clear-on-pop (outcome records reference intake_id; re-enqueue set = intake−outcomes; zero stale re-emission), INDEX append-only even during recovery — and the wake-breadth rider (runtime-layer property + per-lane probe for Kimi/DeepSeek/GLM/MiniMax/Qwen/Ollama-class lanes).
5. **CQ ledger carried** (§15): CQ-1..CQ-7 per the dispatch + **NEW CQ-8** (INDEX derived-authority — the grilled Package-A pivot needs a targeted m-1 COORD to confirm the reading of m-1 §6; surfaced, not assumed). All design-LOCK blockers remain OPEN pending the orchestrator's COORDs; nothing resolved by me.
6. **F8 claim-sweep run** (§16): the doc's only "by construction" is the §2.4 serialized-loop double-accept kill, scoped, with the D5 residual stated beside it.

**Your review hunt-list** (dispatch + my ask): (a) over-reach — any sentence where m-7 authors policy rather than executing it; (b) claim-boundary regressions the sweep missed; (c) engine seams an implementer could not build to as written; (d) whether the biting fixtures actually bite (especially F9/F10 and the G-set); (e) the §3 pipeline ordering vs the locked m-1/m-2 seam text; (f) the new CQ-8 — real contract question or already within m-1's lock?

DESIGN_REVIEW_VERDICT requested: approve | must-revise | reject-narrow | human-decision-required.

Not authorized / not claimed: no design-LOCK (CQ gate open), no PLAN, no IMPL, no code/source/`pcode/`, no spike, no locked-contract reopen, no CQ resolved.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/c4-design-m-7/DESIGN-planner-20260701-170452.md` — run before handoff (result in inline pointer)
- `git -C pcode status --short` — clean (planner-run this session)

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created `master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md` + this relay + one appended INDEX.md row timestamped 20260701-170452; also patched the audit merge-artifact status line per VP carry-forward #1 (supersession cite, `.../audit/2026-07-01-audit-pair-reconcile.md:4`); no code/source/`pcode/` edits (pcode verified clean)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C pcode status --short` = clean)
Next requested action: operator relays this to m-7.implementer for DESIGN-REVIEW; in parallel the orchestrator's CQ COORDs run (CQ-8 added for m-1); on approve + CQ closure the design-lock package assembles.
