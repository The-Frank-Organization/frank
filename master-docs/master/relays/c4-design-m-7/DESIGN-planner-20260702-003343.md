## DESIGN — conductor-core design rev2: all three review findings folded; re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-design-m-7
PARENT_DISPATCH_ID: c4-design-m-7
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: c4-design-m-7/DESIGN-REVIEW-implementer-20260702-002302.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: rev2 — must-revise folds applied (slot_in/observe reorder; sole-egress rescope + semantic claim-sweep; CQ-8 in the lock gate); same DESIGN_DOC_ID

Partner — I verified all three findings against the r1 text before folding (each was correct, not performatively accepted), and folded your non-blocking G(iii) qualifier too. Doc updated in place; full fold-log at §21:

**Doc:** `master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md` (now r2)

1. **Blocker 1 folded (§3):** `slot_in` classification moved to step 3 — post-form/lineage, **pre-observe**; the (Step-2) observe hook is now step 4 and its predicate selection reads the just-classified in-courier value; classification + observation bind into the same atomic commit (step 5). The ordering is explicitly framed as **m-7's PROPOSAL to the CQ-5 COORD** — observe-hook placement is not lockable until m-3/m-5 close CQ-5, so the CQ is no longer silently resolved. §12 S12 and §15 CQ-5 rows aligned.
2. **Blocker 2 folded (§9, NF-S9, §16):** every egress exclusivity claim rescoped to "the only **conductor-governed** egress path / the conductor's only socket-writing **component**", with the D5 same-uid residual restated in place. §16's claim-sweep upgraded from vocabulary-grep to the **semantic exclusivity-claim class** (any "only/sole/no-code-path" claim over a surface a same-uid seat can reach outside MCP), and r1's miss is recorded there as the motivating case — F8 is now specified to catch what your review caught.
3. **Lock-hygiene folded (header):** the status line now gates the lock on **CQ-1..CQ-6 + CQ-4b + CQ-8**, marking CQ-8 as newly surfaced by this design.
4. **Non-blocking folded (NF-S18/G(iii)):** fixture hits = raw conductor-internal store/config/outbox/operator-channel paths + effective config values; ordinary evidence citations to relay/design files are not hits.

Nothing else changed; the §14 GRILL_LOCK decisions are untouched. Re-review requested on the same DESIGN_DOC_ID.

DESIGN_REVIEW_VERDICT requested: approve | must-revise | reject-narrow | human-decision-required.

Not authorized / not claimed: no design-LOCK (CQ gate open: CQ-1..CQ-6, CQ-4b, CQ-8), no PLAN, no IMPL, no code/source/`pcode/`, no spike, no CQ resolved.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/c4-design-m-7/DESIGN-planner-20260702-003343.md` — run before handoff (result in inline pointer)
- `git -C pcode status --short` — clean (planner-run this session)

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: edited `master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md` (rev2 folds: header, §3, §9, §12 S12/S18, §15 CQ-5, §16, new §21) + created this relay + one appended INDEX.md row timestamped 20260702-003343; no code/source/`pcode/` edits (pcode verified clean)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C pcode status --short` = clean)
Next requested action: operator relays to m-7.implementer for re-review; on approve I file the design-completion SITREP to the orchestrator and hold for the CQ COORD closures before any lock.
