## DESIGN — conductor-core design rev3: §1 diagram claim-boundary fix folded; full-doc semantic sweep re-run; re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-design-m-7
PARENT_DISPATCH_ID: c4-design-m-7
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: c4-design-m-7/DESIGN-REVIEW-implementer-20260702-003942.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: rev3 — the residual §1 diagram overclaim fixed; semantic sweep re-run over the whole doc; same DESIGN_DOC_ID

Partner — verified and folded. Your rg caught what my rev2 fold missed: the §1 overview diagram still said "local outbox = the only external egress" — precisely the semantic exclusivity class rev2's own §16 rule bans, sitting in the highest-scan spot in the doc.

**Doc:** `master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md` (now r3; fold-log §21 updated)

1. **§1 diagram line fixed:** now "local outbox = the conductor-governed external egress, behind the m-3 gate (D5 same-uid residual — §9)".
2. **Full-document semantic sweep re-run** (rg over the only/sole/no-code-path/never-reach classes, per your verification command): every remaining hit is (a) the licensed §2.4 by-construction claim with its residual in the same breath, (b) surface-scoped guardrail language ("seat-deliverable", "no tool can express"), (c) internal state-machine exhaustiveness over the conductor's own control flow (not a seat-reachable surface), or (d) the locked mechanism name "local-outbox-only external send" (§9 heading — the §2A/GRILL-LOCK D2b term of art), whose body paragraph carries the scoping + residual in place.
3. **Fold-log honesty note added (§21):** the rev2 claim "all exclusivity wording scoped" is true as of rev3, not rev2 — recorded so the lock package doesn't inherit a false statement.

Nothing else changed; §14 GRILL_LOCK untouched; your four no-objection confirmations (slot_in ordering as CQ-5 proposal, §9/NF-S9 wording, CQ-8 gate hygiene, G(iii) qualifier) carried as-is.

DESIGN_REVIEW_VERDICT requested: approve | must-revise | reject-narrow | human-decision-required.

Not authorized / not claimed: no design-LOCK (CQ gate open: CQ-1..CQ-6, CQ-4b, CQ-8), no PLAN, no IMPL, no code/source/`pcode/`, no spike, no CQ resolved.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/c4-design-m-7/DESIGN-planner-20260702-004224.md` — run before handoff (result in inline pointer)
- `rg -n "\bonly\b|\bsole\b|no code path|unbypassable|same-uid write-exclusion|sole-writer" master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md` — re-run post-fix; residual hits dispositioned as classes (a)–(d) above; zero unscoped exclusivity claims remain
- `git -C pcode status --short` — clean (planner-run this session)

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: edited `master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md` (§1 diagram line + §21 rev3 fold entry) + created this relay + one appended INDEX.md row timestamped 20260702-004224; no code/source/`pcode/` edits (pcode verified clean)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C pcode status --short` = clean)
Next requested action: operator relays to m-7.implementer for re-review; on approve I file the design-completion SITREP to the orchestrator and hold for the CQ COORD closures before any lock.
