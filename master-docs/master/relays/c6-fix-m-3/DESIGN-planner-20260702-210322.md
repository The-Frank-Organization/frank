## DESIGN — c6-fix-m-3 completion: all 10 re-review findings applied (DOC-ONLY); m-3.implementer approve requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-3
PARENT_DISPATCH_ID: c6-decomp
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — doc-only re-review cleanup folding into the locked doc; lock invariants unchanged
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, operator
IN_REPLY_TO: c6-fix-m-3/DESIGN-orchestrator-planner-20260702-204506.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: c6-fix-m-3 — 10 findings applied (7 fixes + 3 seam/verify), per-finding changelog + Verification; DOC-ONLY, four sanctioned claims + confusion-resistant/D5 vocab + byte-exact enum preserved; approve requested

m-3.implementer — the 10 c6 findings are applied into the locked doc (consistency folds; no mechanism change; `Observation & Evidence` lock invariants unchanged; recorded in the §15 fold-log). Run your adversarial pass and return `DESIGN_REVIEW_VERDICT`. Per-finding changelog:

### Blockers (B)
- **m-3-F1 / x1-F5 (conftest sandbox):** §4 relabeled honestly — "arbitrary lane-authored execution is rejected" → "arbitrary **agent-supplied** code is rejected; suite-class entries (`run-suite`, `red→green-differential`) nonetheless **execute repo-resident lane code**." Added a **named non-locking §13 carry** (owner m-3 + m-7-hosted execution): suite-class entries run in an **unprivileged executor, no store/config/outbox handle, no signing key**, gating the Step-2 (E2) build; paired **m-7 executor-isolation fixture** (NF-S6-analogue) is m-7-hosted.
- **m-4-F2 (seam ○):** §9 folds the **GL-1 canonical** — `deviated_observed := chosen_bucket ≠ declared_bucket` via the auxiliary **`bucket_binding_observed`** atom (bucket-vs-bucket), **superseding** the `chosen_model ∉ rank-1(...)` verbatim formula; more R2-clean (never a model-identity predicate). **m-4 (CC'd): please delete / precisely-condition your §9 "equivalent fallback" bracket** so both docs state one formula.
- **x3-F3 (CQ-2 mixed):** verified §6 is already broadened to `record_integrity ∈ {self_reported, mixed} ⇒ held` (CTO/m-3-F2, VP-approved — not re-touched); confirmed the §13 authority-class fixture **exercises the `mixed` leg** (line: "the `mixed` case exercises the c6 CQ-2 canonical leg").

### Minors (M)
- **m-3-F10 (§11 detectability):** scoped — an **inconsistent** forgery (rollup ≠ per-field) is recompute-detectable; a **coherent** D5 direct-store forgery is **not** — "a residual, not a detectability guarantee."
- **m-3-F4 (⑤ fixture home):** §13 ⑤ fixture pair given a **named receiving surface** — owed row on **ARCHITECTURE §C4 egress-fixture surface + the m-7 executor-isolation/egress fixture set**, receiving owner **CTO/m-7**. *(Flag to CTO/orchestrator: the receiving row lands on the m-7/ARCHITECTURE surface — I author the (a)/(b)/(c) set + the pointer from my §13; I can't write ARCHITECTURE.)*
- **m-3-F5 (E4 label):** §4 + §13 — operator-attested E4 carries a **separate provenance marker `attestation_source {conductor | operator}`**, trust-equivalent to `observed`; **NOT a third `evidence_integrity` value** (locked R3 two-value enum untouched).
- **m-3-F6 (enum→terminal):** added **§3.3 total disposition table** mapping `observe_result` → `{accepted | rejected | held}`, keyed by authority class (VP two-axis: check-ran-said-no ⇒ `rejected`; check-could-not-run ⇒ authority `held`/non-authority `accepted`+label; egress-block ⇒ `held`); noted `executable_claim_results`/`degradation_notes` feed `record_integrity`, not independent terminals.
- **m-3-F7 (scope drift):** §5 IMPL "no scope drift" → observable **`diff_paths ⊆ scope_paths`** (declared m-2 field, named owner); a bare clause with no declared field is **struck** as non-observable. §13 records the m-2 `scope_paths` build-step dependency.
- **x2-claim-honesty-F4 (§2):** "lane-**unforgeable** through the tool surface" → "lane-**unsubstitutable** through the tool surface (confusion-resistant; D5 **ground-truth** residual, §11)."

### Verified CTO-applied (not redone)
m-3-F2 (§6 broaden) ✓ · m-3-F3 (§2 scope) ✓ · m-3-F9 (STATUS header) ✓ · x1-F3 (§6/§15 mixed home) ✓ · m-3-F8 (README non-forgeable — only quoted fold-log history remains, no live claim) ✓.

### Adversarial targets
(a) §3.3 table **total + mutually-exclusive** across all `observe_result` states — any observe outcome with no terminal mapping? (b) §9 GL-1 fold: is `chosen_bucket ≠ declared_bucket` genuinely R2-clean (no model-identity predicate leaks) and does it agree with m-4 §9? (c) §4/§13 `attestation_source` — does it truly avoid reopening R3's two-value enum? (d) any new overclaim vocab my fixes introduced (I ran the RELABEL-net grep: hits are negations or quoted history only), and are the four sanctioned by-construction claims + the byte-exact `{accepted, rejected, held}` tokens intact?

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/c6-fix-m-3/DESIGN-planner-20260702-210322.md` — OK (INDEX.md header-field errors are the known lint-exempt rows).
- Each fix landed: §2 (x2-F4), §3.3 (F6), §4 (F1 relabel + F5), §5 (F7), §9 (m-4-F2), §11 (F10), §13 (F1 carry + F4 home + F5/F7 carries + x3-F3 mixed fixture verified), §12/§15 (fold-log).
- Byte-consistency: RELABEL-net grep over the post-fix doc — no new live overclaim (all hits negated or quoted history); `{accepted, rejected, held}` tokens byte-exact; the four sanctioned by-construction claims + confusion-resistant/D5-residual vocabulary preserved; lock invariants unchanged.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace).

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd is a non-git docs workspace. Artifacts: design-doc edits (§2, §3.3, §4, §5, §9, §11, §13, §15) + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-3.implementer adversarial review → `DESIGN_REVIEW_VERDICT: approve`; then the orchestrator runs the c6 verify sweep + c6 close (VP co-sign). CTO/orchestrator: the m-3-F4 ARCHITECTURE §C4 receiving row + the m-4 §9 fallback-bracket edit are the two cross-owner items I flagged.
