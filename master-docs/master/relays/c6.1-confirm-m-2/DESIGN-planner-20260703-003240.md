## DESIGN (m-2 planner, rev2 — folds m-2.implementer must-revise `000156`) — AC12 author-set echo propagated

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6.1-confirm-m-2
PARENT_DISPATCH_ID: c6.1-confirm-m-2-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — bounded within-doc byte-consistency propagation; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: m-2.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: c6.1-confirm-m-2
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: c6.1-confirm-m-2/DESIGN-REVIEW-implementer-20260703-000156.md

**Blocker correct — folded.** The three c6.1 deltas + the §12 echo were confirmed converged in r1 (`000156`); the one remaining gap was **AC12** (`:237`), a live acceptance criterion still scoped `routing_assignments` to "planner/orch-planner" without the c6.1 operator-on-template-spawn extension. Propagated. Within-doc byte-consistency only; no mechanism change, no delta re-edit, no c6 reopen.

### Blocker — AC12 author-set echo → PROPAGATED
`:237` AC12 now reads: *"`routing_assignments` seat-scoped to planner/orch-planner **(+ `operator` on `template_ref`-bearing template-spawn records only — m-4 §7/`:208`, m-4-F4; c6.1)**"* — byte-consistent with §17.3 `:309`, `declared_deviated` `:311`, and the §12 echo `:187`.

### Completeness sweep (so this class is fully closed)
Grepped every author-set occurrence in the doc — **all four** now carry the bounded operator extension: §12 echo `:187`, AC12 `:237`, §17.3 row `:309`, `declared_deviated` `:311`. The only other `deviation_reason_code`/author mention is the **§18 4b fold-log** (`:367`) — a *historical* fold record (accurate for what 4b did), explicitly not a live contract line per the r1 review's own AC-vs-fold-log distinction; left as-is.

### Unchanged (accepted in r1 `000156`)
- **Delta 1** `held` one-compound-record (converges m-7 §6 `:100`, F11 `:172`).
- **Delta 2** author-set +operator-on-template-spawn primary contract (converges m-4 `:208`, §7 `:266-272`).
- **Delta 3** `deviation_reason_code` config-sourced value-set (converges m-4 `:211`, §7 `:227-228`); shape mirrored, m-4 the value owner.
- Lock invariants intact: byte-exact `{accepted, rejected, held}` (`:126`/`:241`/`:282`); R2 grammar-enforced (`:99`/`:239`/`:313`/`:320`); confusion-resistant/D5 vocab; no PLAN/IMPL/`pcode`/spike/design-lock reopen.

No design-doc change in this rev2 beyond the single AC12 line.

Blocked: no — holding for m-2.implementer re-review.
Scope drift risk: none — one live acceptance-criterion echo propagated to the c6.1 author-set delta.

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/c6.1-confirm-m-2/DESIGN-planner-20260703-003240.md` — OK (below).
- `grep -n "planner/orch-planner\|seat-scoped to planner"` → all 4 occurrences (`:187`/`:237`/`:309`/`:311`) carry the operator-on-template-spawn extension; 0 stale.
- `git status --short` — unavailable: not a git repo (docs workspace; `pcode/` untouched).

ACTIONS_GIT_REF: edited `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` AC12 `:237` (author-set echo propagation); wrote this rev2 relay + appended `master/relays/INDEX.md`; no delta re-edit, no mechanism change, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer re-reviews the AC12 fix; on `approve` this `c6.1-confirm-m-2` pair-confirm returns to the orchestrator for the focused c6.1 re-close (VP). m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
