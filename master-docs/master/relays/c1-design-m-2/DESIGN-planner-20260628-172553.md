## Team m-2 — Forms & Determinism: DESIGN-REVIEW REQUEST

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c1-design-m-2
PARENT_DISPATCH_ID: c1-design-m-2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the four operator-owned decisions were resolved in the grill (GRILL_LOCK c1-design-m-2-grill); remaining items are non-blocking/future
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)
REPO: docs workspace (cwd); pcode/ is the future code repo — no code this phase

Current scope for the TO addressee: read-only DESIGN-REVIEW. No source/test edits, no implementation branches, no commits, no PRs. Respond with Template C (PHASE: DESIGN-REVIEW, FROM: m-2.implementer, TO: m-2.planner, PARENT_DISPATCH_ID: c1-design-m-2, same DESIGN_DOC_ID) returning DESIGN_REVIEW_VERDICT.

Design doc to review:
- DESIGN_DOC_ID: c1-design-m-2-forms-determinism
- Location / relay pointer: master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md (full artifact; this relay summarizes the review-critical pieces)
- Selected option: an SMTP-inspired three-layer relay envelope (certified system-owned envelope / typed FieldSpec headers / free body + X- overflow), carried as a bespoke FieldSpec registry (ownership · seat-scope · consumer · lineage-role first-class), borrowing JSON-Schema vocabulary only for the type/enum/required-when sublayer. Fill-time authority via owner-determined render (forbidden option absent from the seat form). One-FROM rule = DMARC-by-construction (no header From:; FROM is system-owned envelope only). Certification reserved + deferred (SPF-analog channel-stamp active via m-1; DKIM-analog signing dormant). Strict legacy (form-only submit; legacy markdown read-only). schema_version stamped + migration framework shipped with zero migrators. X- overflow = never-consumed/never-gate-input. required-when = bounded boolean over a closed atom set.
- Rejected alternatives: JSON-Schema-core + x- extensions as canonical source (buries the novel ownership/seat-scope/consumer layer in ignored extensions; validates post-hoc not fill-time); DKIM signing now (reverses m-1's TCB deferral, no consumer yet); transitional legacy-markdown-submission window (keeps the prose hole open, forces retaining the dissolved lexical checks); defer-all-versioning (invalidated by public-release intent).
- Boundary contract: m-2 declares the field SLOT; siblings own the fill/consume MECHANISM. m-1 system-owned envelope set (FROM/ROLE/PARENT/timestamps/schema_version/reserved certification) — system-filled PARENT strengthens lineage to forgery-robust. m-3 observe/evidence (ACTIONS_GIT_REF, FINAL_GIT_STATUS_SHORT, EVIDENCE_TARGET, achieved-evidence, per-phase done-predicate hook, target>achieved auto-flag). m-4 routing-record (role+model-per-dispatch, capability-priors, justified-deviation reusing DESIGN_RECORD_KIND shape). m-6 human-gate/email (HUMAN_GATE monotonic floor, gate→email bucket, Owner Decision Brief 7-field sub-schema). No field without a named consumer; none unrouted.
- Acceptance criteria draft: see design doc §14 (single FieldSpec source; fill-time authority demonstrated; ~32 prose checks deleted / ~17 form-validation / ~13 lineage with forgery-robust PARENT; X- provably non-gating; schema_version + migrator registry with zero migrators; every §12 consumer field consumer-validated before the joint lock).
- Operator decisions/defaults: GRILL_LOCK c1-design-m-2-grill (design doc §13) — carrier=bespoke FieldSpec; certification=reserve+defer; legacy=strict form-only; versioning=stamp+framework-zero-migrators; overflow=X-; required-when=bounded.
- Open questions: §15 — m-1 envelope-field split + certification null-reserved vs omitted (coordination relay); m-6 Owner-Decision-Brief field list; m-4 model-per-dispatch in-header vs separate routing relay; m-5 slot_in dependency direction. All non-blocking.

Review asks (adversarial — the VP carry-forwards): (1) does the dissolve/survive table (design doc §10) drop any check, or mis-class a prose-only check that actually carries semantics? (2) is the bounded required-when vocabulary (§5) genuinely sufficient to express every upstream conditional without escaping to Turing-completeness? (3) does the FieldSpec model leave any upstream authority gate unpreservable beyond the two named in GATE-1 (§11)? (4) is the one-FROM / certification-reserve split consistent with m-1's locked identity model?

Requested response: Template C — DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: created master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md (DESIGN_DOC_ID artifact) and this relay; both are docs-workspace artifacts, not code; cwd is not a git repo so a git ref is unavailable — files exist on disk.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
