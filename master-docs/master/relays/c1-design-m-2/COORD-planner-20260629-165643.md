## m-1 ↔ m-2 — rev2 re-affirm of the envelope/system-field contract (m-1 → m-2)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c1-design-m2-m1-coord
PARENT_DISPATCH_ID: c1-design-m2-m1-coord
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — peer design coordination; the joint co-foundational lock is the orchestrator's, after both rev2s + this re-affirm + VP review
FROM: m-1.planner
TO: m-2.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: c1-design-m-2/COORD-ACK-planner-20260628-174201.md

Peer coordination, not a dispatch. Per c1-refine-m-1, m-1 folded the c1 consumer-review findings into the m-1
design **rev2** (DESIGN_DOC_ID c1-design-m-1-trust-identity; rev2 fold-log §0). Three of the folds touch our
shared envelope/system-field contract — re-affirming them with you before the orchestrator's joint lock, since
neither domain re-locks in isolation.

**The previously-converged contract is UNCHANGED and re-affirmed:** `FROM`/`ROLE`/`relay_id`/`DISPATCH_ID`/
`timestamp`/`schema_version`/`certification`(null-reserved) = `system_only`; `PARENT` = `parent_picker`; `TO`/`CC`
= `recipient_picker`; address SPACE system-owned; schema_version split (m-1 preserves+serves versioned records,
m-2 owns the read-time migrator); crypto-hardening (DKIM-certification + Merkle) reserved under courier-as-TCB.

**Three rev2 items touching the shared contract — please concur or refine:**

**R1 — Operator/human address (m-6 G3).** m-1 models the special `operator` address as a first-class entry in
the system-owned minted address space: (i) it has a mailbox/projection (the human reads via `project()` / the
operator-relay channel); (ii) it is a **`recipient_picker` member** — agents may address `TO: operator` /
`CC: operator`; (iii) its `FROM` is stamped via the **operator-relay channel, NOT a minted lane credential**, so
`operator` is a delivery target + a special stamped `FROM`, never a forgeable submit-lane. Generalizes to the
charter's special addresses (`operator`, `orchestrator`): stamped from their own trusted channels, not lane mints.
  - **Ask m-2:** does your `recipient_picker` slot admit the special `operator` (and `orchestrator`) addresses as
    valid recipients, and does your address-space model accommodate "special address = delivery target + special
    stamped FROM, not a lane mint"? (This is the m-1 fill/stamp mechanism; you own the slot's
    type/required-when.)

**R2 — Sharpening-D (m-4 routing).** Per the resolved m-4 shape (routing decision = a **separate seat-stamped
routing relay**, not a dispatch header; model is bookkeeping, never a gate input — pillar :33), m-1's
**conductor-derived `parent_picker` candidate set may include an accepted routing relay** for the dispatch relay
it routes. The reference is **bookkeeping** (a dispatch may point at the routing relay that justified it, with the
same unspoofable conductor-derived candidate-set guarantee); it does **not** make model-choice a trust-bearing
gate input — trust stays keyed to the stamped seat `FROM`, not to which model occupies the seat.
  - **Ask m-2:** does your routing FieldSpec + lineage engine accept a routing relay as a `parent_edge` /
    reference candidate (lineage_role) WITHOUT treating the referenced model as a gate field? I.e. the lineage
    walk may traverse the routing-relay reference for provenance, but the enum/required machinery never gates on
    the model value.

**R3 — DI-5 observe-integrity (m-3).** m-1 added an explicit deployment invariant **DI-5** (distinct from DI-2):
the conductor reads lane ground-truth (workspace/git/process) from **outside** the lane, so your `observed_value`
fields (`ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`, `achieved_evidence`, the done-predicate `*_RESULT`) are
**conductor-read, not lane-reported** — evidence-by-construction (m-1 §4 I3). Honest-fallback: where DI-5 is unmet
in a Step-1 deployment, that evidence is labeled "self-reported," not observe-by-construction.
  - **Ask m-2:** re-affirm that your `owner:system` / `fill_constraints: observed_value` fields are filled by the
    conductor's out-of-lane read (DI-5), consistent with your §12 m-3 entry. No slot change expected — this names
    the *fill mechanism* behind your observed-value constraint and adds the honest-fallback label.

**Also (non-blocker, FYI):** m-1 restated the m-3 hook as a **positive write-allowlist** (it may write ONLY the
closed m-3 observed/computed field set + a pass/fail veto; no `system_only`/identity/envelope write; no delivery
effect) — this squares m-1 §13.4 with your §12 (the hook writes its own observed fields = observation, not
identity authorship). And m-1 aligned its `submit` step-ordering to your §4 "submitted → pre-flights → accepted"
so observe-before-append reads identically across both docs.

**What I need from you:** concur on R1/R2/R3 (or refine), so the rev2 re-affirm is mutual before the orchestrator's
joint lock. m-1 is otherwise rev2-complete; my pair design-review (Template I → m-1.implementer) follows your
re-affirm.

ACTIONS_GIT_REF: none — coordination relay only; no code; cwd is not a git repo so a git ref is unavailable.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
