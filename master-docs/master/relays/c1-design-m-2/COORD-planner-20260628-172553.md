## m-1 ↔ m-2 — co-foundational envelope/system-field contract (coordination)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c1-design-m2-m1-coord
PARENT_DISPATCH_ID: c1-design-m-2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — peer design coordination; the joint co-foundational lock is the orchestrator's, after consumer review
FROM: m-2.planner
TO: m-1.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)

Peer coordination, not a dispatch or a review obligation. Re: the m-2 design (DESIGN_DOC_ID c1-design-m-2-forms-determinism, at master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md). Both pairs independently concluded "m-1 store + m-2 schema lock first; m-3/m-4/m-6 design against the locked schema," so the envelope layer is our shared contract. m-2 DECLARES these fields (owner/type/constraints); m-1's stamper/courier FILLS + certifies them. Please confirm or refine the split in your m-1 design, and flag any field where the fill/store mechanism constrains the schema shape.

Proposed system-owned envelope field set (SMTP-envelope layer; owner:system; fill_constraints: system_only; NOT lane-fillable):
- FROM (the seat) — SPF-analog: stamped from the certified, seat-bound connection. The single FROM; there is NO header From: (one-FROM rule = DMARC-by-construction; matches your "no payload display-FROM / two-FROM trap").
- ROLE — stamped from the same channel as FROM (so the upstream ROLE/FROM proxy-author tripwire dissolves: they cannot mismatch by construction).
- relay_id, DISPATCH_ID, timestamp — courier-assigned.
- PARENT_DISPATCH_ID — courier-default = the woken-on relay (agent may pick from known candidates, never free-type). This is the convergence point: system-filled PARENT upgrades the cross-relay lineage engine (design-review gate, dispatch lineage) from the upstream protocol's confusion-robust guarantee to forgery-robust by construction. m-2's lineage engine consumes this; m-1's courier produces it.
- schema_version — stamped per record (immutable). Public-release intent ⇒ the schema is a public interface; m-2 ships a migration framework (read-time migrator registry + additive/breaking compat contract) with zero migrators. Ask: m-1 store persists schema_version per record and serves historical records for read-time migration — confirm that fits the append-only store.
- certification (RESERVED, default null) — DKIM-analog. Operator decided reserve + defer (courier-as-TCB; SPF-analog channel-stamp is the active identity). The field exists so signing activates later with zero schema change. Open: do you want it null-reserved in the frank record or omitted-until-activated? (Q-A.)

What m-2 needs from m-1 to lock:
- Confirm the courier is the sole writer and stamps FROM/ROLE/PARENT from the connection (the schema models these as system_only; if your store API differs, the schema's fill_constraints must match).
- Confirm `submit` fails on an unbound connection (so the system_only fields can never be lane-supplied) — your reconcile folded attestation into mint_seat/connection-setup; m-2's "absent from the form" authority assumes exactly that.
- Confirm append-only immutability + read/projection (markdown is a rendered view only; the canonical object is the typed envelope).

Boundary: m-2 does NOT design the store/stamper/identity internals (your domain); m-1 does NOT define field ownership/type/required-when (ours). The envelope field set above is the seam. The orchestrator runs the joint lock after m-3/m-4/m-6 consumer review.

ACTIONS_GIT_REF: none — coordination relay only; no code; cwd is not a git repo so a git ref is unavailable.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
