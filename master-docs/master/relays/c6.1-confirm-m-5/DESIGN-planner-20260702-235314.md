## DESIGN — c6.1-confirm-m-5 planner CONFIRM: the §9 observe-phasing correction converges to m-1 §5 + m-7 NF-S5/CQ-1(a), byte-correct, no contradiction, lock-invariants intact. I own the c6-F6 regression. Request implementer adversarial approve.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6.1-confirm-m-5
PARENT_DISPATCH_ID: c6.1-confirm-m-5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — owner-confirm of a CTO seam-arbitration correction already applied; doc-only; no re-edit
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
IN_REPLY_TO: c6.1-confirm-m-5/DESIGN-orchestrator-planner-20260702-233008.md
BUNDLE_ID: c6.1-confirm-m-5
SUBJECT: m-5 CONFIRMS the single §9 c6.1 delta (observe hook inert in Step-1; send-gate chokepoint by-design; predicates land Step-2) — converges to the cited locked targets, byte-correct, no internal contradiction, lock invariants intact. Owning my c6-F6 "every send observes" regression. Request `DESIGN_REVIEW_VERDICT`.

m-5.implementer — confirming the CTO's c6.1 seam-arbitration correction (1 delta to my doc, `master/c61-fix.diff` §9 `:163`; already applied — I am confirming, not re-editing). I verified against source, not the dispatch summary. **CONFIRM** on all three required criteria. Request your adversarial `DESIGN_REVIEW_VERDICT` (the operator's differential loop exists precisely so we don't re-close on CTO edits + sampling).

## The delta (exact)
§9 observe-invariant phasing bullet — my c6-F6 text said the gate mechanism "is always-on from Step-1 (**every send observes**)." Corrected to: the observe-as-send **send-gate is the locked m-3 chokepoint *by design***, but **in Step-1 the observe hook is inert** — no observe writer — so **Step-1 sends pass the chokepoint without an observe predicate**; the pipeline + done-predicates land at **Step-2**. Rest of the bullet unchanged.

## (a) Converges to the cited locked target — VERIFIED byte-faithful (E2)
- **m-1 §5 `:129`** (verbatim): *"**Step-1 records carry no observe gate.**"* + *"The m-3 observe-as-send gate (2c) … are the reserved additive Step-2 hook — m-3 builds at Step-2."* ✓
- **m-7 NF-S5 / CQ-1(a) step-gate `:155`**: *"a Step-1 IMPL … submit with no observe layer present ⇒ accepted without the observe-owned fields (their `required_when` is step-gated off) … CQ-1 CLOSED."* ✓
- So my c6-F6 "every send observes" from Step-1 **directly contradicted** both — it implied a Step-1 observe predicate that the locked step-gate says is absent. The correction is the accurate convergent statement. **I own the regression:** my c6-F6 §9 wording was the divergence the differential caught (pair-review + CTO sweep + VP-sampling missed it because the *atoms* were present; the *phasing claim* diverged). Confirmed corrected.

## (b) No contradiction elsewhere in my doc — CHECKED
- **§7 T1 cell `:123`** ("observe-as-send gate always-on (families bite from Step-2, §9)") — **consistent.** The doc uniformly distinguishes the **chokepoint-mechanism** (always-on, structural — "gate") from the **observe-predicate/hook** (inert Step-1, bites Step-2). §9's own closing sentence explicitly reconciles it: *"T1's 'observe-as-send always-on' names the gate mechanism (Step-1), not a claim that every family predicate is enforced pre-Step-2."* The T1 parenthetical already defers families to Step-2. No contradiction.
- **§4 confusion-resistance proof `:78`** — **consistent.** §4 is a phase-agnostic *design property* (the observe-as-send gate, when it runs, selects the invariant conductor-side ⇒ tamper/confusion-resistant); it makes **no Step-1 enforcement claim**. The corrected §9 phases the biting to Step-2, which §4 never contradicted. The observer-selected-control sanctioned claim is untouched.
- **§16 fold-log `:244`** — already phrased as "the observe-as-send *gate mechanism* is always-on from Step-1; … families bite from Step-2" (it never carried the "every send observes" over-claim), so it is **already consistent** with the correction. No fold-log re-edit needed (respecting the confirm-don't-re-edit constraint).

## (c) Lock invariants intact — CONFIRMED
No mechanism/field/lattice/tag-value change; F1 (slot_in classified-at-acceptance — the correction retains "Step-1 records the `slot_in` classification (F1)"), F2, R2, C2.4, GL-1..GL-6 all untouched. Four sanctioned by-construction claims preserved (observer-selected control unaffected); confusion-resistant/D5 vocab preserved; `{accepted, rejected, held}` enum byte-untouched. A pure phasing clarification.

## Scope notes
- **m-3 §3.3 egress extra-scrutiny caveat (dispatch `:25`)** is **NOT an m-5 delta** — it's m-3's disposition confirm (a different pair's slice in the shared `c61-fix.diff`). Out of my confirm scope; I do not confirm another pair's delta. (Read it; noting it is m-3's.)
- **m-5-F2** (posture away-trigger scope question, still owed from `c6-fix-m-5/SITREP-planner-20260702-210717`) is **untouched by c6.1** — this correction is purely the §9 observe-phasing. The m-5-F2 CTO scope ruling remains separately outstanding.

## DESIGN-REVIEW requested (adversarial)
Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`. Probe hardest at (b): does "observe-as-send gate always-on" in §7 T1 truly reconcile with "observe hook inert in Step-1" in §9, or is there a residual reader-trap needing a T1 wording tighten? Also: is the m-1 §5 / m-7 NF-S5 citation an exact convergence, or does the correction over- or under-state the locked step-gate?

Not authorized / not claimed: doc-only confirm; no re-edit (correction is CTO-applied), no mechanism change, no lock reopen, no unrelated-c6 reopen, no PLAN/IMPL/`pcode/`/spike, no m-5-F2 resolution.

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/c6.1-confirm-m-5/DESIGN-planner-20260702-235314.md` — (run below)
- m-1 §5 `:129` + m-7 NF-S5 `:155` read (E2 convergence anchors); m-5 §7 `:123` + §4 `:78` + §16 `:244` read (contradiction check); `master/c61-fix.diff` §9 slice read (1 m-5 delta, byte-exact).
- `git -C pcode status --short` — (run below)
- `git status --short` — unavailable: `fatal: not a git repository` (cwd is a docs workspace)

ACTIONS_GIT_REF: read-only confirm — no doc edit (the c6.1 correction is CTO-applied); wrote this DESIGN relay + appended `master/relays/INDEX.md`; no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-5.implementer files the adversarial `DESIGN_REVIEW_VERDICT`; then I return the `c6.1-confirm-m-5` completion relay (planner confirm + implementer approve) to master.orchestrator-planner + operator.
