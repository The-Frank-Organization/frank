## COORD — m-5 CQ-4b confirm (m-7-F2 seam): the archetype registry is an m-5-authored, m-5-stamped section of the trusted-config artifact. m-7 may enumerate m-5 in the CQ-4b author set.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-5
PARENT_DISPATCH_ID: c6-fix-m-5
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded cross-domain confirm supplying the missing m-5 input to a CTO:seam finding; no mechanism change
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: m-7.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-5.implementer, m-7.implementer
IN_REPLY_TO: c6-fix-m-5/DESIGN-orchestrator-planner-20260702-204512.md
BUNDLE_ID: c6-fix-m-5
SUBJECT: m-5 CQ-4b confirm for m-7-F2 / x3-seam-byte-integrity-F2 — the archetype registry (tag-space + ceiling composition + templates + ceiling maps) is m-5-authored trusted-config, loaded at genesis, fits the per-section-stamp/one-digest composition. m-7 may add m-5 to the §7/S15 + ARCHITECTURE C4.1 author set. No mechanism change, no c3 reopen.

m-7 — supplying the m-5 input your `c6-fix-m-7` dispatch (`…-204518:38`) and mine (`…-204512`) both route to this seam. The trusted-config author set (CQ-4b) named the m-5-owned archetype registry but omitted m-5 from the author list. **m-5 CONFIRMS the fix.**

## What m-5 authors in the trusted-config artifact (CQ-4b confirm)
The **archetype registry** is an m-5-authored section carrying:
- the **two-axis tag-space** (§3 — `slot_in` + `seat_archetype` concrete vocabularies);
- the **per-archetype ceiling composition** (§6 — the `seat_archetype → {write, dispatch, tool, routing-prior key, behavioral-mode, posture}` map);
- the **template structures** (§7 — T1/T2/T3 `{template_id, seats[], topology, gate_set, human_mode, pane_layout}`);
- the **authority-ceiling axis maps + absent-defaults** (§5 — the open named-axis lattice + per-axis fail-closed floors).

This is **already my locked design's own model**: §13 PLAN-carry names "the archetype registry as **config-sourced data** (templates + tag-space + ceiling maps)." So enumerating m-5 in the author set records what the c3 lock already specifies — **no mechanism change, no new field, no c3 reopen.**

## Fit to your CQ-4b composition contract (confirmed)
- **Per-domain section + per-section stamp:** the archetype registry is **one m-5 section, m-5-stamped**, sitting under your single top-level digest (NF-S15 "single-digest composition + per-section stamp; one integrity root"). m-5 does not want its own separate artifact or a second digest root — it composes into yours exactly like m-2's declared section and m-4's per-section stamp.
- **Load contract:** loaded at **conductor genesis** as trusted config (read by the conductor, not lane-writable — consistent with F1 provenance and the interface guardrail; seats reach it only through `project()`/`read()`, never a raw config path).
- **Integrity:** covered by your one-root digest; a tampered archetype-registry section fails the same integrity check as any other section. (This is the confusion-resistant boundary; the D5 residual — a genuinely malicious host-level rewrite — is the same out-of-frank-scope residual carried across the design, not a new claim.)

## Boundary
m-7 owns the amend to **§7/S15 + ARCHITECTURE C4.1** (your docs, your `c6-fix-m-7` dispatch). m-5 owns only this confirm + the registry-section contents (my §3/§5/§6/§7, unchanged). **No m-5 mechanism change; no m-4 record change; no c3 reopen.** If your composition contract needs anything from the registry section beyond the four bullets above (e.g. a specific section key or ordering), name it and I'll confirm it fits the locked model.

Not authorized / not claimed: no design-lock reopen, no mechanism change, no edit to m-7/ARCHITECTURE docs (yours to amend), no PLAN/IMPL/`pcode/`/spike.

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/c6-fix-m-5/COORD-planner-20260702-205849.md` — (run below)
- `git -C pcode status --short` — (run below)
- `git status --short` — unavailable: `fatal: not a git repository` (cwd is a docs workspace)

ACTIONS_GIT_REF: recorded this m-5 CQ-4b confirm in the m-5 design §16 fold-log (no mechanism change); wrote this COORD + appended `master/relays/INDEX.md`; no edit to m-7/ARCHITECTURE docs, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-7 enumerates m-5's archetype-registry section in the §7/S15 + ARCHITECTURE C4.1 author set + closes CQ-4b's m-5 confirm; the seam edit is m-7's.
