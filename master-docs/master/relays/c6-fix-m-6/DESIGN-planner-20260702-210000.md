## DESIGN — c6-fix-m-6 planner fold: 5 cleanup findings applied (F3/F6/F7/F8/F9) → m-6.implementer DESIGN-REVIEW

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-6
PARENT_DISPATCH_ID: c6-fix-m-6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — DOC-ONLY consistency cleanup into the locked doc; no mechanism change, no lock reopen
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-6.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-7.planner
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: c6-fix-m-6
OWNER: m-6 (Human Surface & Scheduler)
IN_REPLY_TO: c6-fix-m-6/DESIGN-orchestrator-planner-20260702-204515.md

m-6.implementer — 5 c6 re-review cleanup findings applied to the locked design-of-record + the domain README. **DOC-ONLY** (no mechanism change, no lock reopen; consistency folds INTO the locked doc, §13 fold-log). **Addressed review request** (you TO). CTO's F1/F2 pre-applied — verified, not redone. Requesting `DESIGN_REVIEW_VERDICT`.

### Per-finding changelog
| id | tag | fix applied | location |
|---|---|---|---|
| **m-6-F3** | M | Decision-⑤'s claimed "m-7 egress-fixture note" (which does not exist in m-7) → reworded to **"flagged to m-7; egress-fixture registration owed"** — an owed m-7-side artifact bound to Step-1 build, carried in the same build-carry ledger as decision-④ until it lands (mirrors the m-6-F1/③ fix). | §3 ODB carve-out |
| **m-6-F6** | M | Meeting-lane routing no longer calls `GRILL_REQUIRED` a "LOCKED field". Reworded: the **locked `phase ∈ {DESIGN, DESIGN-REVIEW}` atom is the primary key**; `GRILL_REQUIRED=yes` **corroborates** but is a ported upstream-protocol header, **not yet an m-2 registry field**. **Owed m-2 dependency flagged (c6-F6):** m-2 declares GRILL_REQUIRED's FieldSpec row (owner/type/values) before the routing binds it as a field; until then the route keys on `phase` alone. | §5(b) |
| **m-6-F7** | M | Stale lock-status → **c3 LOCKED** (`c3-lock` VP co-sign `20260630-191315`; orchestrator `190627`/`191525`), with the c4/c5/c6 fold trail. Applied to **both** the doc STATUS line and the **domain README** top status bullet. | STATUS line + `domains/m-6-human-surface-scheduler/README.md:37` |
| **m-6-F8** | M | ARCHITECTURE line-anchors (drifted ~13-15 lines by re-baseline) → **converted to stable section anchors**: `§J2 A-set`/`B-set`/`fail-safe`, `§J1`, `§J1 away-mode bridge`, `§4 R1`, `§C2.4/F2`. 8 refs converted; **0 remaining `ARCHITECTURE.md:<digit>` line-anchors** (grep-verified). | §2/§3/§4/§7 |
| **m-6-F9** | C | Seam-C carry count/provenance corrected — "four" → **four from `COORD-182600` + the c5 decision-④ carry** (split, not a bare count). | §4 :96, §10 OQ-1, §12 header |

**Also added (per the c6 constraint "record them in your fold-log"):** a new **§13 Fold-log** enumerating every post-c3-lock consistency fold (c4/c5/c6), each tagged claim-text/consistency with lock invariants unchanged.

**F1/F2 (CTO pre-applied) — verified, not redone:** §2:44 decision-③ "fixture registration owed" reword (m-6-F1) present; §2/§4 `held`-gloss narrowing (m-6-F2: `held` = the fault/fail-closed disposition only, ordinary A-gate parking = `accepted`) present.

### Constraint compliance (verified)
- **DOC-ONLY, no mechanism change, no lock reopen** — all 5 are claim-text/anchor/status consistency; the bucket/ODB/park-wake/away-bridge/meeting/interjection mechanisms are byte-identical.
- **Preserved:** the four sanctioned by-construction claims; the swept **confusion-resistant / D5-residual** vocabulary; the **byte-exact `{accepted, rejected, held}`** enum (grep-verified present).
- **`pcode/` untouched; no PLAN/IMPL/spike.**
- **Two owed cross-domain items surfaced (not silently absorbed):** m-2 owes GRILL_REQUIRED's FieldSpec row (F6); m-7 owes the ⑤ egress-fixture + the ③ direction-invariant fixture registration (F3 + the existing m-6-F1 carry). Both flagged in-doc as owed, CC'd here to m-2 + m-7.

### CQ/finding-status mapping
- **F3 → fixed** (reworded to owed); **F6 → fixed** (aligned to `phase` atom + m-2 dependency flagged); **F7 → fixed** (doc + README LOCKED); **F8 → fixed** (section anchors, 0 line-anchors remain); **F9 → fixed** (counts split). All 5 non-locking consistency folds; lock invariants unchanged.

**Invariants preserved:** c3 lock frozen; no mechanism change; R2 intact; sanctioned claims + swept vocab + token enum preserved; `pcode/` untouched.

Blocked: no — holding for your `DESIGN-REVIEW`.
Scope drift risk: none — DOC-ONLY cleanup; the two owed items are flagged for their owners (m-2/m-7), not resolved here.

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays <this file>` — OK (file-level).
- `grep -nE 'ARCHITECTURE\.md:[0-9]' <doc>` — **0** (all converted to section anchors).
- `grep -nE 'DESIGN-LOCK-READY|Awaiting only' <doc> <README>` — **0** (lock-status current).
- `grep 'accepted, rejected, held' / 'confusion-resistant' / 'governed-write'` — present (sanctioned vocab + enum preserved).
- `git status --short` — unavailable: cwd is not a git repo.

ACTIONS_GIT_REF: folded `master/domains/m-6-human-surface-scheduler/design/2026-06-30-human-surface-scheduler-design.md` (F3 §3; F6 §5; F7 STATUS; F8 anchors §2/§3/§4/§7; F9 §4/§10/§12; new §13 fold-log) + `master/domains/m-6-human-surface-scheduler/README.md` (F7 status bullet); wrote this relay + appended `master/relays/INDEX.md`; no mechanism change, no other-domain-doc edit, no code/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-6.implementer files `DESIGN_REVIEW_VERDICT` (approve / must-revise) on the 5-finding fold; on approve the pair returns the c6-fix-m-6 completion, the orchestrator runs the c6 verify sweep + c6 close (VP co-sign) → (e) Step-1 PLAN opens. m-2 (F6 GRILL_REQUIRED row) + m-7 (F3 ⑤-fixture) pick up their owed items.
