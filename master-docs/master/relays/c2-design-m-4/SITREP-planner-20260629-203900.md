## SITREP — m-4.planner → orchestrator: c2-design-m-4 DESIGN-COMPLETE (implementer approve); held for c2 lock

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: c2-design-m-4
PARENT_DISPATCH_ID: c2-design-m-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-4.implementer, operator
BUNDLE_ID: m-4-routing-policy

m-4 routing/policy DESIGN is complete and pair-approved. Reporting for the c2 lock; **holding — no self-advance
to PLAN** (design-lock is terminal this phase).

**Approved design:** `master/domains/m-4-routing-policy/design/2026-06-29-routing-policy-design.md`
(DESIGN_DOC_ID `c2-design-m-4-routing-policy`).
**Approving review:** `c2-design-m-4/DESIGN-REVIEW-implementer-20260629-203329.md` — `DESIGN_REVIEW_VERDICT:
approve` on rev1 (one prior `must-revise` round, three narrow findings folded; the non-blocking carry-forward
normalized). Operator approved the draft this cycle.

**What the design proves/specifies:**
- **R2 by construction** — the deviation comparison is bucket-vs-bucket on `declared_bucket` (does not read
  model identity); model payload is read only by an auxiliary observe-layer binding check; **no model-derived
  predicate enters the m-2 schema/authority/lineage gate or the work-dispatch header** (the only model-touching
  *gate* atom is the plain `declared_deviated` boolean).
- Fail-closed `route_dispatch()` (no silent default-model fallback); declared/versioned/**snapshotted** two-layer
  capability priors; the `routing_decision` FieldSpec as an m-2 consumer; the 3-staged policy + a later-release
  forward hook; the authority-ceiling cap (identity≠authority at the routing layer).
- **Routing templates** (operator GL-4) — pinned/slot/customizable, with a mechanical **no-bypass** rule
  (off-floor template assignments emit a self-contained recorded deviation, never informal prose).

**Operator GRILL_LOCK (durable, §0):** GL-1 two-layer bucket priors (deviation at bucket layer); GL-2 a later
release tunes recommendations only (membership operator-owned); GL-3 record-now/execute-Step-3 (declaration-honesty enforced,
execution-fidelity a documented Step-1→Step-3 boundary); GL-4 ships 1–3 routing templates at Step-1.

**m-3↔m-4 seam:** RECONCILED both sides (`c2-design-m3-m4-coord`). The bucket-vs-bucket form is m-3's preferred
construction; the additive observed field-set update is declared and flagged to m-3 (no seam re-negotiation).

**Two lock prerequisites — surfaced, not closed (for CTO/VP at the c2 lock):**
1. **m-5 disposition (required).** The prior table keys on an **opaque archetype-tag**; the concrete tag-space +
   authority-ceiling semantics + the routing-template *structures/lineup* (GL-4) are m-5's. The c2 m-4 lock must
   not finalize archetype-prior semantics without either a narrow m-5 consumer review of this draft OR an
   explicit reconcile reservation preserving m-5 ownership. m-5 pair not yet booted — route at boot.
2. **R2-boundary ratification (both pairs aligned).** Routing the silent-deviation block through m-3's generic
   declared-vs-observed integrity-veto (no model-derived `required_when`) is the cross-domain R2-preservation
   choice; m-3 and m-4 both recommend it; ratify at the lock.

**Cross-domain scope item already flagged (`c2-design-m-4/SITREP-planner-20260629-200500.md`):** the Step-1
templated-team-spawn (operator-directed) needs the m-5 structure/lineup + conductor-core pane-spawn decomposed
and folded into the roadmap (CTO/VP). m-4 owns only the routing-template model-assignment mechanism (designed).

**Holding** for orchestrator direction (the c2 lock, after the m-5 disposition + the R2-boundary ratification).
No PLAN/IMPL this phase.

ACTIONS_GIT_REF: report-only relay + the approved/normalized design doc under
`master/domains/m-4-routing-policy/design/`; INDEX row appended. No code/source/pcode edits.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo).
