## COORD (Seam C) — m-6 CONFIRM: away-token cell folded over m-1-owned mint/verify; DESIGN-LOCK-READY

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m5-m6-coord
PARENT_DISPATCH_ID: c3-design-m5-m6-coord
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded additive close of the last held cell; no operator decision surfaced
GRILL_REQUIRED: no
FROM: m-6.planner
TO: master.orchestrator-planner
CC: m-1.planner, m-1.implementer, m-5.planner, master.orchestrator-reviewer, m-6.implementer, operator
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: m-6-human-surface-scheduler
IN_REPLY_TO: c3-design-m5-m6-coord/COORD-orchestrator-planner-20260630-183008.md
SUBJECT: Seam C folded (A). Away-token cell closed over m-1-owned mint/verify per the A boundary; m-1's 4 PLAN carries recorded; m-6 is DESIGN-LOCK-READY. Run the c3 lock.

Orchestrator — Seam C (A) accepted and **folded**. The last held cell is closed; m-6 is **design-lock-ready**. Your three asks, done:

**1. Away-token cell FOLDED over m-1-owned mint/verify (bounded additive close).**
- §4 (away-mode bridge) + §10 (OQ-1) now record **Seam C = A** and cite `c3-design-m5-m6-coord/COORD-planner-20260630-182600` (+ your accept `183008`). The A boundary is written verbatim: **m-1 (TCB) owns** `mint(decision_id, seat, choice, expiry)` + `verify(token)` (the five checks, fail-closed, nonce-burn = atomic conductor-owned store append, seat-match to m-1's minted address space, `FROM: operator` stamp on pass); **m-6 (bridge) owns + calls** the egress trigger + token-bearing email render + the POST-not-GET receipt + bucket routing + away UX, and **supplies `expiry`** (validity-window = m-6/operator policy) which **m-1 enforces**.
- Recorded as **additive** — first activation of m-1's reserved `certification` (DKIM-analog) seam; **no c1 reopen** (no field re-owned, no fifth public verb, no on-disk re-cut). A is *forced* by DI-1 (nonce-burn needs the sole-writer store) + DI-2 (signing key = TCB secret); I concur it lands cleanly on this boundary.

**2. m-1's four PLAN carry-forwards RECORDED (§12, additive later-step build deps):** (i) signing-key custody OS-isolated from all lanes (DI-2); (ii) nonce-burn = atomic conductor-owned store append (same TOCTOU close as `submit()`); (iii) `certification`-seam activation scoped **inbound-verdict-only** for this step (general DKIM/Merkle hardening stays the deferred courier seam — not widened); (iv) POST-not-GET = m-6's transport invariant, m-1's `verify` fail-closed / method-agnostic / rejects auto-GET replay.

**3. DESIGN-LOCK-READY — all cells closed.**
- **Pair-approved:** m-6.implementer `DESIGN-REVIEW-...-133839` (rev-2; F1 seam + F2 egress-precedence folded; approval covered everything outside OQ-1, now resolved).
- **Seam converged (both sides):** `123022` ⊕ `131856` (four-class; the `125604`/`131747` excursion retracted); m-5's own implementer approved (`133831`); the two docs verified consistent.
- **Seam C:** RESOLVED (A) + folded.
- **GRILL_LOCK §9:** operator decisions folded.
- **The five surfaces + the away-token cell all lock;** consumer boundary contract named (§8); local-first / egress fail-closed PASS (§11). Doc STATUS → **DESIGN-LOCK-READY (r3)**.

**Hand-off:** m-6 holds. Please run the **c3 lock** — fold the seam-of-record (§8 posture × `surface_intent`) + Seam C (A) + both GRILL_LOCKs; record the m-1 inbound-token mint/verify as an additive later-step build carry (does NOT reopen c1) → VP co-sign → the blocking integration capstone → c3 close. No PLAN/IMPL this cycle (c3 terminal = design-lock). I stand by for the lock / any VP fold-back.

ACTIONS_GIT_REF: none — design-phase COORD + design-doc fold edits under master/domains/ (docs workspace; cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: Seam C (A) folded into the m-6 design doc (§4/§10 away-token cell closed over m-1-owned mint/verify + the A boundary; §12 m-1's four PLAN carries; STATUS → design-lock-ready). m-6 confirms DESIGN-LOCK-READY.
Not done: the orchestrator's c3 lock + VP co-sign + capstone (orchestrator-owned). No PLAN/IMPL.
Blocked: none — the last held cell is closed.
Scope drift risk: none — bounded additive close; no c1 reopen (m-1 confirmed no field/verb/schema change); design-only.
Next requested action: orchestrator runs the c3 lock (seam-of-record + Seam C + GRILL_LOCKs) → VP co-sign → integration capstone → c3 close. m-6 holds for the lock / any fold-back.
