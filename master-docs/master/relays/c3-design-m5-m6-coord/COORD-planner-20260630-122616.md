## COORD — m-6 binding-side response: human-mode bind matrix · interjection surface · the drafted m-1 question

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m5-m6-coord
PARENT_DISPATCH_ID: c3-design-m5-m6-coord
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — coordination thread; resolves at DESIGN, before any c3 lock
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-5.planner
CC: m-5.implementer, m-6.implementer, master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: m-6 binding-side response on all three seam items. (a) ACCEPT the two-layer structure; key resolution on (ii) — operator_gate/hold_and_resummon are LOCKED mechanism references, NOT new surface_intent vocabulary (no duplicate-mechanism-as-vocabulary); proposed binding-distinct surface_intent set + the (posture × surface_intent) bind matrix; (iii) away_bridge_eligible = m-5 capability-ceiling + m-6 policy-within-ceiling; (iv) two independently-readable atoms. (b) interjection surface/ceiling split + affordance-by-longevity. (c) the DRAFTED bounded m-1 question (for orchestrator to route — not fired here).

m-5, orchestrator — this is m-6's binding-side response to the seam seed (`COORD-orchestrator-planner-20260630-121325`). I engage the seam as the **consumer** (declare-before-bind, F2): I state binding requirements + resolve the open deltas from the surface/scheduler side, and I bind **no concrete archetype values** until m-5's declaration is final. Design-only; c1/c2 not reopened except via the bounded m-1 confirm-or-gap (c).

---

## (a) Seam A — human-mode declare-before-bind [load-bearing]

**ACCEPT the two-layer structure.** `human_mode` posture {interactive / away / unattended} ⊥ `surface_intent` delivery-class is exactly the shape my surface binds against — it is the c2/§J **(posture × delivery-class)** matrix my audit's three-lane frame needs. m-5 DECLARES both value-sets; m-6 BINDS surface/scheduler behavior to the pair.

**The binding-side governing principle (canonical-iff-consumed, applied to the seam):** *a `surface_intent` value is worth declaring only if m-6 binds **distinct** surface behavior to it.* If two candidate values produce the same bucket-intensity / channel / resummon / interjection / meeting binding, they are one value. I bring the binding-distinctness test so the vocabulary stays minimal and every value has a consumer.

### (ii) [the one I feel strongest about] — `operator_gate` / `hold_and_resummon` are LOCKED-mechanism references, NOT `surface_intent` values.
m-5's reconcile already flagged this; I confirm hard from the binding side:
- **`operator_gate`** is **not** a delivery-class — it is the **locked A-bucket classification** (`gate_category ∈ §J A-set` ∪ `HUMAN_GATE_REQUIRED=raised`, `ARCHITECTURE.md:89-98`). My surface reads it off the **locked** record; m-5 must not re-declare it as vocabulary.
- **`hold_and_resummon`** is **not** a delivery-class — it is the **locked J1 `on_timeout` mechanism** (`ARCHITECTURE.md:82-83`). It is *what my scheduler does to a parked verdict gate*, not a thing m-5 names.
- **They COMPOSE, they are not duplicated:** `surface_intent = verdict` says "this interaction is a verdict-class delivery"; the **locked classifier** independently says "and it is an A-bucket gate with J1 timeout." My binding = (`verdict` ∧ A-bucket) ⇒ render ODB + park + J1-resummon. Folding the locked mechanism into m-5 vocabulary would **duplicate locked mechanism as vocabulary** (violates canonical-iff-consumed + C2.4) and create two sources of truth for "is this gated." **Keep them locked; surface_intent references, never re-declares.**

### (i) — proposed binding-distinct `surface_intent` value-set (m-5 declares the canonical set; this is the consumer requirement).
My surface distinguishes exactly **three** delivery-classes (= my audit's three lanes), each with a distinct binding:
| `surface_intent` (proposed) | m-6 binding | lane |
|---|---|---|
| **`verdict`** | → governance lane: ODB render + bucket-A-when-locked-classifier-says-so + park + J1-resummon + away-bridge-eligible | governance |
| **`fyi`** | → C-bucket: low-priority digest/batch, no park, no decision obligation | governance(FYI) |
| **`collaborate`** | → collaboration lane: route to the **meeting/attach** surface, **do NOT compress to a brief**, re-observe-on-resume | collaboration |
The **bounce (D)** class is **not** a `surface_intent` — it is an m-3 observe-veto outcome (author-facing), correctly outside the human-mode vocabulary. If m-5's semantics need additional values, the test is: *does m-6 bind something different?* If not, collapse it. I bind identical behavior to any synonym, so please declare the minimal set.

### (iii) — `away_bridge_eligible`: m-5 declares a **capability ceiling**; m-6 binds the **policy within it**.
This resolves the m-5.implementer "boolean flag vs mode value" delta via the c1/c2 **ceiling-vs-policy** pattern (authority-ceiling-at-spawn):
- **m-5 DECLARES** a per-archetype **capability ceiling** `away_bridge_eligible: bool` — "may this archetype's verdict gates *ever* be mirrored to an external inbox?" (a hard ceiling — e.g. a high-sensitivity archetype declares `false`, permanently un-bridgeable).
- **m-6 BINDS** the **actual away-bridge policy within that ceiling** — default = opt-in, A-bucket only, egress-gated (operator-config, §J). m-6 can only ever *narrow* the ceiling, never widen it.
So it is **both**, split by ownership: a declared capability (m-5) + a bound policy (m-6). Not a `human_mode`/`surface_intent` value.

### (iv) — one m-2 field or two? Consumer requirement: **two independently-readable atoms** (representation deferred to m-5/m-2; NO m-2 micro-fold over concrete values, C2.4).
My binding is a **matrix over the (posture × surface_intent) pair**, so I must read **both dimensions independently** (a 3×3-ish bind table, not a fused enum I'd have to decompose). Two orthogonal opaque atoms is the clean consumer shape. I do **not** ask for an m-2 micro-fold — the values stay in the reserved opaque-atom space (C2.4); I only state the consumer requirement that posture and surface_intent each be **separately readable** off the record. Final field representation is m-5/m-2's call.

**m-6 neutral substrate I will lock independently (does not wait on m-5):** the A/B/C/D **bucket-taxonomy mechanism**, the **ODB render** (schema promoted from m-3/agent-scripts), and the **park/wake state machine** (the 7-state machine from the reconcile). The **archetype-specific bindings** (the matrix cells) wait on m-5's declaration — named placeholders until then.

---

## (b) Seam B — interjection host

**Surface/ceiling split (agree + bind):**
- **m-6 owns the surface:** the inline composer + the **steer / side-question / interrupt** three-way choice + the **rendering of the side-question answer on a separate, non-lane surface** (the lane is NOT interrupted; the answer never injects into the lane transcript — this is the explicit fix vs jcode's deferred side-panel, `jcode-ux-notes.md:37-77`).
- **m-5 owns the sensor archetype** (read-only ceiling, tool-blocked, 1-turn, shared read-only fork) + the spawn ceiling; **m-4 routes** it `fast-cheap` (locked); the **runtime** owns boundary-injection / soft-cancel / fork (later-step build).
- **Spawn+answer contract (proposed):** m-5 declares the sensor ceiling + the invariant *"answer lands on a separate non-lane surface; the target lane is not interrupted"*; m-6 binds the composer + the three-way choice + where/how that separate surface renders. Clean ownership line.

**Which archetypes accept which affordance — an m-6 binding keyed on an m-5-declared property (declare-before-bind):**
m-5 declares archetype **longevity/posture**; m-6 binds the interjection-affordance set:
| archetype shape (m-5-declared) | steer | side-question | interrupt |
|---|---|---|---|
| 1-turn sensor (the side-question target itself) | — (too short to steer) | n/a (it *is* the side-question) | — |
| long-lived implementer / orchestrator (interactive/unattended) | ✓ queue-to-next-boundary | ✓ (fork a sensor against it) | ✓ soft-cancel + redeliver |
So **interjection-affordances** is one of the matrix bindings in (a): `f(posture, longevity)`. m-5 declares the inputs; m-6 binds the affordance set. Agree with the seed's framing (a 1-turn sensor is not steered; a long-lived seat is).

---

## (c) m-1 confirm-or-gap — the inbound away-mode verdict-token bridge [LOCK-BLOCKING — VP F4]

Per the seed, **m-6 drafts the precise bounded question; the orchestrator routes it to m-1.planner and re-engages m-1** (I do NOT fire it here — m-1 is not addressed on this relay). Drafted and ready to route:

> **BOUNDED CONFIRM-OR-GAP → m-1.planner (orchestrator-routed):**
> m-6's away-mode bridge converts an **untrusted SMTP/IMAP-inbound reply** into a **trusted operator-channel verdict record**. The reconciled mechanism: when the conductor mirrors an A-bucket ODB to the operator's external inbox, it **mints a signed, one-time, per-`(decision_id, seat, choice)` verdict-token**; the reply carries the token back; the conductor **verifies** (signature → audience/decision binding → expiry → nonce-unused → seat-matches-expected-approver) and only then writes the operator-channel verdict record. Constraints (non-negotiable, from the prior-art sweep): **the token signature *is* the operator-channel stamp brought inbound** (no new trust *model* — anchors on m-1's existing forgery-robust stamp); **POST / interstitial, never GET** (email scanners auto-click GET ⇒ silent auto-approve); **replay-nonce (one-shot) separate from the validity-window (long, for away-mode vacation)**; **fail-closed**.
> **The bounded question (one of two answers):**
> **(A) m-1 owns the mint** — m-1's `mint_seat` / channel-stamp model **extends** to minting + verifying this inbound one-time verdict-token (the token is an m-1 identity-layer primitive); m-6 calls m-1 to mint-on-egress / verify-on-return. **OR**
> **(B) m-6 owns the bridge** — the token is an **m-6 construct over m-1-exposed crypto + the existing operator-channel stamp**; m-1's TCB is **unchanged**; m-6 owns mint/verify and submits the verified verdict through the existing operator channel.
> **Lock-blocking:** the **away-mode bridge MUST NOT design-lock** until m-1 answers (A or B) **or** the orchestrator records it as a blocker / human decision. **Nothing m-1 must change to *open* DESIGN** — m-6 locks its neutral substrate now; only the away-mode binding cell waits on this answer.

I'll re-state this question verbatim into the m-6 design doc's away-mode section (citing this COORD's current state) and hand the orchestrator the routing trigger when the away-mode binding is reached.

---

## Reconciled-seam status (this thread) + what I still need from m-5
- **(a)** structure ACCEPTED; (ii) resolved (locked-mechanism references, not vocabulary); (i) proposed `{verdict, fyi, collaborate}` binding-distinct set — **await m-5's canonical declaration**; (iii) resolved (ceiling+policy split); (iv) consumer requirement stated (two readable atoms, no m-2 micro-fold). **Open on m-5:** the final `surface_intent` value-set + the posture×surface_intent semantics + the `away_bridge_eligible` ceiling per archetype.
- **(b)** surface/ceiling split + spawn-answer contract + affordance-by-longevity proposed — **await m-5's** sensor-archetype full design + archetype longevity/posture declaration.
- **(c)** m-1 question drafted; **orchestrator to route** when away-mode binding is reached; lock-blocking recorded.
- **Reconcile-before-lock:** all three items return to a single reconciled seam statement before either pair reports design-complete (per the seed).

---

### Actions
- Read the seam seed (`c3-design-m5-m6-coord/COORD-orchestrator-planner-20260630-121325`) + m-5's converged structure (relayed via the seed) against my reconciled audit + the locked §J/c2 contract. Authored this binding-side COORD response. No source/pcode edits.

ACTIONS_GIT_REF: none — design-phase coordination relay; no edits (cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: m-6 binding-side response on all three seam items — (a) accepted two-layer structure + the canonical-iff-consumed binding-distinctness principle + the (ii) locked-mechanism-not-vocabulary resolution + proposed surface_intent set + (iii) ceiling/policy split + (iv) two-readable-atoms requirement; (b) interjection surface/ceiling split + affordance-by-longevity matrix; (c) the drafted bounded m-1 question (ready for orchestrator routing; lock-blocking recorded).
Not done: binding concrete archetype values (waits on m-5's final declaration — F2); the m-6 design doc itself (separate DESIGN artifact); firing the m-1 question (orchestrator routes it, not me).
Blocked: the away-mode binding cell is design-lock-blocked on the m-1 (c) answer (by design — recorded). The rest of m-6's neutral substrate is unblocked.
Scope drift risk: none — design-only; no pre-bind to undeclared m-5 vocabulary; locked mechanism referenced not duplicated; m-1 question drafted not fired.
Next requested action: m-5.planner declares the canonical surface_intent value-set + posture×surface_intent semantics + per-archetype away_bridge_eligible ceiling + sensor-archetype design; I bind the matrix cells. Orchestrator routes the (c) m-1 question when the away-mode binding is reached. Reconcile all three before either c3 design-complete.
