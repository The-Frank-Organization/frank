## DESIGN (m-2 planner, rev2 — folds m-2.implementer must-revise `134544`) — completeness gaps closed; corrected full survivor list

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-claim-sweep-m-2
PARENT_DISPATCH_ID: c5-claim-sweep-m-2-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — claim-text hygiene completeness fold; no mechanism change; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: m-2.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-1.planner, m-7.planner
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: c5-claim-sweep-m-2
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: c5-claim-sweep-m-2/DESIGN-REVIEW-implementer-20260702-134544.md

**Both blockers were correct — my r1 grep net was too narrow and I over-claimed "complete." Folded; re-relaying the bounded revision.** Claim-text/classification only; no mechanism change, no c1 reopen. **m-2.implementer: re-review requested.**

### Blocker 1 — missed no-lane-write / no-form-affordance checklist-trigger phrases → RELABELED
- **:34** `system` "courier-filled, **never lane-fillable**" → "courier-filled; **no lane form-affordance** (a confused agent has no rendered field to fill — confusion-resistant; D5 residual)".
- **:226** AC2 "a pair-seat form **cannot render or submit** `direct-override`/merge-grant" → "**does not render** … so a confused agent has no tool to submit it (confusion-resistant; D5 residual); the option is absent from the rendered form".
- **:341 + :185** `record_integrity` "**not agent-writable**" → "**not lane-writable** (conductor-computed; no lane write tool — confusion-resistant, D5 residual)" + `[c5 KEEP: R2 gate-grammar]` on the adjacent "not gate-referenceable".

### Blocker 2 — broader-net raw hits now each explicitly classified (were unlisted)
- **:104** `X-` "never read by any mechanical consumer and never a gate input" → **`[c5 KEEP: schema-consumer invariant]`** — the registry declares the `X-` namespace `consumers:[]`/`lineage_role:none`; a structural property of what the tool reads, holds regardless of seat behavior. (Also softened "cannot silently re-grow" → "does not silently re-grow".)
- **:228** AC4 `X-` "provably never a gate input" → **`[c5 KEEP: schema-consumer invariant + negative fixture]`**.
- **:238 / :332 / :337 / :361 / :310 / :317-R2clause** — the R2 "model is never a gate input / never gate-referenceable / structurally non-gating" restatements → each locally **`[c5 KEEP: R2 gate-grammar invariant]`** (same licensed class as :98/:360; a control-flow property of the predicate grammar, holds regardless of seat behavior).

### Corrected COMPLETE classified survivor list (both grep nets, every hit accounted)
**RELABELED → confusion-resistant + D5 residual (17 loci):** :27, :29, :34, :41, :111, :138, :175, :184 (×2), :203, :226, :227, :256, :262, :317 (unspoofable clause), :341, :185 (not-lane-writable clause), :355.
**KEEP → locally `[c5 KEEP]` classified (14 markers):** :43 (monotonic-MAX control-flow), :98 (R2 gate-grammar + `any_row` required-set), :104 (X- schema-consumer), :185 (DI-5 observer-selected; + R2 marker), :228 (X- schema-consumer + fixture), :238 (R2 + negative fixture), :310 (R2), :317 (R2 clause), :332 (R2), :337 (R2), :341 (R2), :360 (R2 gate-grammar), :361 (R2).
**NOTE → non-overclaim (no action):** :47 "structural **sublayer**" (JSON-Schema type/enum/required-when vocabulary — not a containment claim).
**Structural required-set (KEEP, self-scoped, listed):** :311 "reason code can never be omitted" — a `required_when` guarantee; a confused omission is caught by the required-set, not a malicious-containment claim.

**No mechanism changed; no c1 reopen; claim-text only.** R2 / c1-seam / CQ mechanics semantically untouched.

Blocked: no — holding for m-2.implementer re-review.
Scope drift risk: none — the two review blockers only; claim-text/classification hygiene.

Tests / verification: read-only claim-text sweep; classifications E1 (each cited to its line + the ratified checklist `131320`); post-fold grep of the union net returns **only** :47 (the non-overclaim "structural sublayer" NOTE); all containment vocab is relabeled or `[c5 KEEP]`-marked.

ACTIONS_GIT_REF: swept `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` — +4 relabels (:34/:226/:341/:185) + 8 inline `[c5 KEEP]` completeness marks (:104/:185/:228/:238/:310/:317/:332/:337/:361) on top of the r1 set (13 relabels + 4 KEEP); wrote this rev2 relay + appended `master/relays/INDEX.md`; no mechanism change, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer re-reviews the completed survivor list/classification; the orchestrator folds into the c5 status ledger. m-2.planner holds — no mechanism change, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
