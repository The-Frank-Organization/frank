## Team m-6 — c2 consumer-lens review of m-3 + m-4 (Human Surface & Scheduler) — INDEPENDENT PLANNER PASS

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: c2-consumer-review-m-6
PARENT_DISPATCH_ID: c2-consumer-review-m-6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — read-only consumer-review; operator items surfaced in findings
GRILL_REQUIRED: no
FROM: m-6.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-3.planner, m-4.planner, m-6.implementer, operator
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: c2 consumer review — reader-has-a-writer holds across m-3/m-4; ONE confirm-or-gap (M4-1, routing B→A escalation must be a stamped consumable atom) + 1 forward note. Independent planner pass; implementer challenge + reconcile to follow.

Phase: AUDIT / consumer-review (read-only). This is my **independent planner lead pass** per the pair method (m-6.planner leads; m-6.implementer challenges independently; reconcile into one m-6 relay). NOT the reconciled deliverable — that follows the implementer's independent challenge. No edits/branches/commits/PRs; no PLAN/IMPL. §J is LOCKED and is NOT reopened (it is the contract I check the c2 designs against).

---

### VERDICT (overall): **sufficient — with ONE confirm-or-gap (M4-1) + one forward note**

Every m-6 consumer surface on m-3 and m-4 has a writer (reader-has-a-writer holds), with a single nameable exception worth flagging pre-lock exactly as c1's consumer-review caught writer/reader mismatches: **M4-1** — the routing B→A escalation is, as written, a `route_dispatch()` return value (m-4 §3), not a readable atom stamped on a gate record the human-surface projector consumes (absent from the §5 FieldSpec). Likely already discharged by the c1-locked HUMAN_GATE layer, but m-4 should name the atom + the stamp path so my bucket projector can mechanically distinguish an absorbed routing gate from an escalated one. Notably, both c2 designs are *strongly* aligned with my surface — m-3 §7/§10 build the egress gate and the ODB evidence-bundle expressly FOR my away-mode bridge + ODB, and m-3 honors my c1 Q-B retype (`completed_proof` = conductor-observed `evidence_ref`, not lane free-text).

### Per-surface reader-has-a-writer table

| # | m-6 consumer surface | writer (E1) | verdict |
|---|---|---|---|
| S1 | observe-gate veto → gate→email bucket (D/A projection) | m-3 §3:43-57 (`observe_result`), §3.2:63, §10:146 (veto + bounce text) | **writer present** |
| S2 | egress / content-safety gate for away-mode bridge (§J1 first external send) | m-3 §7:111-117, observe_result `egress_scan_result` §3:56 | **writer present (built FOR my bridge)** |
| S3 | ODB evidence-summary schema (`completed_proof`=evidence_ref, residual-risk, recommendation, choices) | m-3 §10:146; m-4 §10:319-321 (routing recommendation+choices) | **writer present; honors c1 Q-B retype** |
| S4 | `routing` gate_category bucketing (B; escalate→A) | m-4 §J map (ARCH §J2:96), m-4 §10:319-321 | **writer present — but see M4-1** |
| S5 | routing A-escalation ODB content (recommendation + enumerated model choices) | m-4 §5:200-206 (snapshot/basis/assignments), §10:320 | **writer present** |
| S6 | §J forward: operator-config gate_category A/B map + protected-branch set | m-4 emits `gate_category`; map is m-6 config (ARCH §J2:89-92) — not foreclosed | **not foreclosed** |
| S7 | §J forward: away-mode external-inbox bridge (egress-gated) | m-3 §7:117 (dormant until my bridge = first external send) | **not foreclosed (actively enabled)** |

---

### Findings — m-3 (Observation & Evidence)

**(a) observe-gate veto → gate→email bucket — SUFFICIENT.** The `observe_result` (m-3 §3:43-57) exposes `veto {none|block_delivery}`, `predicate_result {pass|fail|blocked|degraded}`, a distinct `egress_scan_result {pass|blocked|not_applicable}`, and the bounce "names the exact failing field" (§2:31, §3.2:63); §10:146 promises me the **veto → bucket + bounce text** explicitly. That is enough to mechanically project the full A/B/C/D set:
- predicate-fail / non-egress `block_delivery` → return-to-author bounce → **D (lint-bounce class)** [input: veto + failing field + bounce text].
- declared-vs-observed integrity veto (silent deviation / bucket-binding mismatch, §3.2(b):63, §9:135) → return-to-author bounce → **D** [input: integrity-veto reason].
- `egress_scan_result = blocked` → `hold_and_resummon` → **A (reserved-to-human)** [input: the distinct egress atom — cleanly separable from a predicate bounce].
- `degraded` / `self_reported` floor (§8:125) → **delivers, labeled**; bucket from TO/CC + `record_integrity` per §J (C if CC-only-FYI, A if TO operator).
All four buckets have a writer; the egress block is a *distinct* atom from a predicate bounce, so I can route it to A rather than mis-bucketing it as a D return-to-sender. **Reader has a writer.**

**(b) egress gate for the away-mode bridge — SUFFICIENT (and built for me).** m-3 §7:111-117 puts a fail-closed scan at the conductor chokepoint (sole external sender ⇒ structurally unbypassable), policy **always-block + hold-and-resummon** aligned verbatim to locked J1 (ARCH §J1:84-87), dormant until **the m-6 away-mode external bridge — the first external send** (§7:117). `egress_scan_result` is the consumable result. This is exactly the gate on my away-mode door and is stronger than I require. **Reader has a writer.** (One forward note below on the flag-class.)

**(c) ODB evidence-summary schema — SUFFICIENT; honors my c1 Q-B.** m-3 §10:146 promotes the agent-scripts 7-part ODB (`maintainer-orchestrator/SKILL.md:53-69`) as an **evidence-bundle** — `completed_proof` / residual-risk / recommendation / exact-choices — with **the evidence atoms conductor-observed and the `record_integrity` label shown** (§6:104-107). This directly discharges my **c1 Q-B retype** (`completed_proof` → m-3 `evidence_ref`, NOT lane free-text) and strengthens my "never ask for a bare decision; show the proof and its integrity" ODB invariant (the shown `record_integrity` label is exactly the integrity signal my ODB surface should render). Ownership is clean: **m-3 owns the ODB evidence-bundle schema; each gate-raising domain fills its own choices/recommendation; m-6 projects to the operator surface.** **Reader has a writer.**

### Findings — m-4 (Routing & Policy)

**(d) `routing` gate_category bucketing — SUFFICIENT shape, but M4-1 confirm-or-gap.** m-4 §10:319-321 keeps `routing` a **category-B (orchestrator-absorbed)** gate (matches ARCH §J2:96), escalating to **A only on `human_decision_required` / `routing_unavailable`** (m-4 §3:154-156, fail-closed). The static map (routing→B) is my config (§J2) and m-4 consumes it without foreclosing — good. The escalation, however, is produced as a **`route_dispatch()` return** (§3:154-156) and **does NOT appear as a field on the `routing_decision` FieldSpec (§5:197-206)** — there is no `gate_category` atom and no `human_decision_required` / `routing_unavailable` status atom on the record my projector reads.

> **M4-1 (confirm-or-gap, to m-4.planner; CC orchestrator):** For my bucket projector to override B→A, the escalation outcome must reach the human-surface as a **readable force-A atom on a consumable gate record** (the natural carrier is the c1-locked HUMAN_GATE record raised on escalation, with `human_decision_required` true and/or a reason ∈ the A-set), **not solely a `route_dispatch()` return**. Otherwise an absorbed routing gate and an escalated one are indistinguishable to me, and `gate_category=routing` alone maps to B. This is precisely the c1 G2 shape (typed, readable escalation reason). **Most likely already discharged by the c1 human-gate layer + m-4 raising a HUMAN_GATE on escalation — in which case M4-1 closes as a confirm.** m-4: please name the atom + the stamp path (or confirm the HUMAN_GATE-on-escalation route). Per the dispatch's loop-in discipline + the c1 contract constraint (no unreviewed side-lock), I hold the binding coordination relay until the m-6 pair reconciles; m-4.planner sees this early on CC.

**(e) routing A-escalation ODB content — SUFFICIENT.** For the rare A-escalation m-4 §10:320 exposes recommendation + enumerated model choices; the record carries the materials: `capability_prior_snapshot` (both layers = the ranked buckets/members = my enumerated **choices**, §4/§5:201), `justified_deviation` + `deviation_reason_code` + per-row `basis` (the **recommendation** rationale, §4:181, §5:202-203), and `chosen_model` (the proposed pick, §5:200). That populates my ODB `recommendation` + typed-row `choices` ("what each does" = each bucket's `basis`/capability descriptor). **Reader has a writer.**

### §J forward requirements (S6/S7) — not foreclosed
- **operator-config gate_category A/B map + protected-branch set:** m-4 emits `gate_category`; the **map stays m-6 config** (ARCH §J2:89-92) and m-4 consumes it. Not foreclosed.
- **away-mode external-inbox bridge (egress-gated):** m-3 §7 is the chokepoint and is dormant until my bridge is the first external send — actively enabled, a clean reserved seam. Not foreclosed.

---

### Forward note (soft — NOT a pre-lock gap)
- **N1 — egress flag-class for the away-mode fallback notice.** When egress blocks an away-mode mirror send, the operator is "out," so the notify channel is the very channel that just blocked. My away-mode fallback (later m-6 work) must fall back to the **local in-app inbox** and may want to compose a *content-safe* "a gate is held, return to app" notice. The binary `egress_scan_result {pass|blocked}` (§3:56) is **sufficient to trigger the local fallback** (I do not need the class to fall back). Exposing the matched **flag-class** would only enable a richer safe notice — a nicety, not a blocker. Flagged as forward; my mechanism to design, m-3 does not foreclose it (§7 engines already produce match classes).

### c1 continuity (verified, not reopened)
- **G3 (operator/human address + operator-FROM) — FOLDED/CLOSED:** ARCH §2:38-40 (`operator` = first-class minted-address entry + special stamped FROM over the operator-relay channel, not lane-minted) + §4 R1:65 (`operator` is a `recipient_picker` member with a defined operator-FROM path). My c1 G3 is discharged in the locked contract.
- **c1 Q-B ODB retype — HONORED:** `completed_proof` = conductor-observed `evidence_ref` is exactly m-3 §10:146 / §6. No drift.

### Operator-judgment items
- **OJ-1 (forward, mine — flag only):** the away-mode-blocked fallback policy (local-inbox notify + content-safe notice composition) is m-6 later work; reserved, not decided here. No decision needed at this lock.
- **OJ-2:** none blocking the c2 lock from the m-6 consumer lens beyond M4-1's confirm.

### Actions
- Read m-3 design (`master/domains/m-3-observation-evidence/design/2026-06-29-observe-evidence-design.md`), m-4 design (`master/domains/m-4-routing-policy/design/2026-06-29-routing-policy-design.md`), locked ARCH §J — evidence E1.
- Wrote this independent planner consumer-review pass. ACTIONS_GIT_REF: no edits to any git repo; cwd is a non-git docs workspace.

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: independent planner consumer-review pass over m-3 + m-4; per-surface reader-has-a-writer verdict; M4-1 confirm-or-gap named; N1 forward note; c1 continuity verified.
Not done: the reconciled m-6 deliverable (awaits m-6.implementer's independent challenge); the binding M4-1 loop-in relay (held until pair reconcile, per no-side-lock).
Blocked: none.
Scope drift risk: none — read-only; §J not reopened; M4-1 routed as confirm-or-gap, not a unilateral contract edit.
Next requested action: operator relay m-6.implementer's independent challenge pass back to me; I reconcile into the single `c2-consumer-review-m-6` deliverable relay TO the orchestrator and, if M4-1 survives reconcile, fire the M4-1 coordination to m-4.planner CC orchestrator.
