## Team m-2 — Forms & Determinism: DESIGN-REVIEW REQUEST (rev2 — consumer-review fold)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c1-design-m-2
PARENT_DISPATCH_ID: c1-refine-m-2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design refinement; operator-judgment items (ODB on_timeout default; gate_category novel-member placement) surfaced in the doc §15 with proposed defaults
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, m-1.planner, master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)
REPO: docs workspace (cwd); pcode/ is the future code repo — no code this phase
IN_REPLY_TO: c1-refine-m-2/DESIGN-orchestrator-planner-20260629-164925.md

rev2 folds the c1 consumer-review findings (m-3/m-4/m-6, reconciled + VP-approved) into the design doc on the same DESIGN_DOC_ID. The design SHAPE is unchanged (orchestrator: endorsed) — these are field-schema refinements. Scope for the TO addressee: read-only DESIGN-REVIEW; respond with Template C (PHASE: DESIGN-REVIEW, FROM: m-2.implementer, TO: m-2.planner, PARENT_DISPATCH_ID: c1-design-m-2, same DESIGN_DOC_ID).

Folds (design doc §17 fold-log):
- G1 — HUMAN_GATE_REQUIRED render-affordance. Was tagged bare `owner:system` (→ "not shown"), so the agent could not RAISE it. Now a `monotonic` **hybrid** (like the pickers): system sets the floor, the agent selects within `[floor, MAX]` (RAISE-only), courier validates monotonic ≥ floor; the floor is the MAX across all raisers (system baseline, agent, m-5 archetype-ceiling, m-4 routing-raise). §3 hybrid clause + render semantics; AC9.
- G2 — gate→email bucket drivers. `human_gate_reason` is now a **closed enum** `gate_category` (A human-only: product_semantics/irreversible_write/merge_decision; B orch-absorbed: routing/sequencing/scope) — the bucket projection is a mechanical consumer, so its driver must be enum (canonical-iff-consumed). Added `delivery_state` {accepted|bounced} + `failing_edge` as `owner:system` computed slots for the bucket-D bounce. §17.1, §12 m-6; AC10. m-2 declares slots; m-6 owns the projection.
- Q-B — Owner Decision Brief ~10-field sub-schema. agent-scripts 7 + 3 retyped to the frank substrate (`subject_ref`=id_ref into store; `completed_proof`=evidence_ref pulled from m-3 observed evidence, never agent free-text; `choices`=row_array the operator reply selects within) + `gate_category` + 2 null-reserved scheduler fields (`decision_deadline`, `on_timeout`). Provenance auto-projects from the certified envelope. §17.2; AC11.
- Routing-relay record (Q-C resolved = a SEPARATE seat-stamped routing relay, not a dispatch header). `routing_assignments` (row_array, seat_scoped_enum planner/orch-planner — altitude-B; target seat/role + model from a closed model enum); `capability_prior_snapshot` (system, computed/observed, replay-complete); `justified_deviation` (free_text, required_when off-prior); `routing_record_kind` (own enum, DESIGN_RECORD_KIND shape not values); `outcome_feedback_ref` (system id_ref, null-reserved for a later release). Model stays out of the work relay's authority header. D-schema-half lineage is m-2's; the parent_picker candidate-set join is m-1's. §17.3; AC12.
- slot_in — RESERVED atom: shape only, no Step-1 values, no predicate branches on a concrete slot; m-5 owns later. §5; resolves §15 Q-D as reserved.

m-1 re-affirm sent in parallel (COORD-REAFFIRM-planner-165702): items 1–2 consume already-locked ownership unchanged; item 3 (routing-relay parent_picker join) is the one join needing m-1's explicit confirm.

Review asks (adversarial): (1) does the HUMAN_GATE hybrid actually make RAISE reachable while keeping lower-than-floor unrepresentable (not merely rejected)? (2) is `gate_category` a clean closed enum that mechanically drives the A/B bucket split with no free-text leak? (3) is `completed_proof`=m-3 `evidence_ref` (never agent-filled) airtight against an agent forging proof in the brief? (4) does the separate routing-relay + parent_picker join keep model fully out of every work-relay gate input? (5) any consumed field still lacking a named consumer, or any new field that should be `X-`/free-text instead of canonical?

Requested response: Template C — DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: revised master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md (DESIGN_DOC_ID artifact, rev2: §3/§5/§12/§14/§15 + new §17) and created this relay + the m-1 re-affirm; docs-workspace artifacts, not code; cwd is not a git repo so a git ref is unavailable — files exist on disk.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
