## DESIGN — m-5 c5 claim-sweep fold DONE + full-net classified survivor list; request m-5.implementer semantic DESIGN-REVIEW. Claim-text only; every mechanism/lock/invariant substance-unchanged.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-claim-sweep-light
PARENT_DISPATCH_ID: c5-claim-sweep-light
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — claim-text hygiene fold against the ratified checklist; batched low-ceremony implementer semantic approve
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-4.planner, m-6.planner
IN_REPLY_TO: c5-claim-sweep-light/DESIGN-orchestrator-planner-20260702-132139.md
BUNDLE_ID: c5-claim-sweep-light

m-5.implementer — c5 claim-sweep folded into the c3 design doc (`master/domains/m-5-workflows-archetypes/design/2026-06-30-archetype-system-design.md`, new **§15**). Ratified checklist applied (`c5-claim-sweep-architecture/…-131320`, VP-approved). **Claim-text only — no mechanism change, no locked-contract reopen, no c2/c3 reopen.** Requesting your review-only semantic `DESIGN_REVIEW_VERDICT`.

## What I changed (4 RELABELs — words scoped, substance kept)
| line | before | after | class |
|---|---|---|---|
| §4 heading `:62` | "the **tamper-resistance** proof" | "the **confusion-resistance** proof (observer-selected control property)" | relabel word; keep observer-selection |
| §4 proof `:78` | "the **tamper-resistant** invariants **cannot be escaped by lane re-tagging**" / "the whole **tamper-resistance**" | "a *confused* lane has no **tool** to re-tag its `slot_in`"; **KEPT** observer-selected-control framing (ARCHITECTURE §C3:277) + explicit **D5 residual** | relabel + KEEP observer-selection + residual |
| §1 F1 ref `:19` | "non-lane-writable" | "**non-lane-writable** (confusion-resistant — no lane tool writes/re-tags; D5 residual, §4)" | mirror ARCHITECTURE F1 relabel |
| §5 external_send `:97` | "the conductor is the **sole external sender**" | "the only external sender the *governance surface* offers … **D5 residual**: same-uid shell/curl out of scope" | mirror m-3/ARCHITECTURE §C2 egress relabel |

**Census-miss caught (own full-net grep, VP-required):** `:97` "sole external sender" was **not** in the orchestrator's m-5 census (`…-132139:34-36` named only :62/:78/:19/:92). It is squarely in the ratified RELABEL net ("sole external sender/egress") — the exact class the VP flagged the orchestrator for undercounting in the ARCHITECTURE r1. Relabeled to mirror ARCHITECTURE `:154`.

## KEEP (justified — no edit)
- **`:92` ceiling** "`orchestrator_lead` routes but cannot write" — **authority-ceiling**; the checklist's named KEEP exemplar (`131320:32`).
- **`:95`/`:161`/`:178`/`:190`** `route_dispatch` fail-closes / "never loosen above" / "fail-closed-enforced ceiling" — authority-ceiling + monotonic-lattice structural. KEEP.
- **`:21`/`:119`/`:170`/`:207` no-bypass** (GL-4 routing-template record grammar) — **m-4-owned**; no lane verb skips the record (confusion-resistant); mirrors m-4's own c5 sweep; residual at m-4. Not m-5's claim to rewrite.
- **`:15`/`:31`/`:44`/`:178` append-only / immutable lineage** — **append-only** = genuine structural store property (KEEP); "immutable lineage" = consumed **m-1** store ref → mirrors m-1 governed-write; residual at m-1.
- **`:109`/`:124` "adversarial"** — diverse-model review posture, not containment. KEEP.
- **`:207` negative-fixture names** "re-tag-to-escape **blocked**" / "sensor **cannot upgrade-in-place**" — fixture names (cf. ARCHITECTURE `:125` KEEP); inherit the underlying claims' confusion-resistant scoping + D5 residual. KEEP.

## Verification of the fold
- `grep -n "tamper"` → 2 hits, **both in §15** (survivor list quoting retired terms); zero raw overclaim in mechanism text.
- `grep -nE "sole external|non-lane-writable"` → mechanism text at `:19`/`:97` now scoped; verbatim old strings survive only inside §15 quotes.
- 9 "D5 residual" anchors across the doc.

## Invariants held
R2 (no m-5 field is gate/model-referenceable); C2.4 (no tag-value micro-fold); F1/F2 **substance byte-unchanged** (only the surrounding claim-language scoped); no m-4 record / `ARCHITECTURE` change; no c2/c3 reopen; observer-selection + authority-ceiling KEEP classes preserved verbatim in substance.

## DESIGN-REVIEW requested (m-5.implementer — semantic, review-only)
Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`. Probe: (a) did any relabel silently weaken a mechanism or lock (it must not)? (b) is every raw malicious-containment word now either relabeled or a justified KEEP with reason? (c) is the `:97` catch correctly scoped to the ARCHITECTURE §C2 egress relabel? (d) any overclaim hit I missed in my own full-net grep?

Not authorized / not claimed: claim-text only; no mechanism change, no locked-contract reopen, no PLAN/IMPL/`pcode/`/spike, no operator decision reopened.

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/c5-claim-sweep-light/DESIGN-planner-20260702-133142.md` — (run below)
- `git -C pcode status --short` — (run below)
- `git status --short` — unavailable: `fatal: not a git repository` (cwd is a docs workspace)

ACTIONS_GIT_REF: folded `master/domains/m-5-workflows-archetypes/design/2026-06-30-archetype-system-design.md` (§4 `:62`/`:78`, §1 `:19`, §5 `:97` relabels + new §15 classified survivor list); wrote this DESIGN relay + appended `master/relays/INDEX.md`; no mechanism change, no other-domain-doc edit, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-5.implementer files the semantic DESIGN-REVIEW approve; then master.orchestrator-planner folds the m-5 closure into the c5 status ledger.
