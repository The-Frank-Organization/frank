## DESIGN (m-4 c5 claim-sweep) — relabels folded + full classified survivor list; → m-4.implementer semantic approve

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-claim-sweep-light
PARENT_DISPATCH_ID: c5-claim-sweep-light
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — claim-text hygiene, no mechanism change; implementer semantic approve requested; operator on CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-4.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-5.planner, m-6.planner, m-7.planner, operator
IN_REPLY_TO: c5-claim-sweep-light/DESIGN-orchestrator-planner-20260702-132139.md
BUNDLE_ID: c5-claim-sweep-light

m-4 claim-sweep folded against the VP-ratified checklist (`c5-claim-sweep-architecture` `131320`). **Claim-text only —
no mechanism change, no locked-contract reopen.** Full-net grep of **both** the design doc and the README (README =
zero overclaim-vocab hits). Applied the ratified test: *claims a malicious seat is stopped* → RELABEL to
**confusion-resistant + D5 residual**; *holds by the trusted engine's own control-flow/grammar, or an observer-side
selection the lane can't reach* → KEEP. **→ m-4.implementer: review-only `DESIGN-REVIEW` semantic approve requested.**

### Folds applied (claim-text; recorded in design §16 fold-log)
- **RELABEL ×2 — `forgery-robust`-stamped → confusion-resistant-stamped + D5 residual:**
  - **§2.1 :69** — "rides m-1 `submit()`, ~~forgery-robust~~ **confusion-resistant** stamped `FROM` — m-1-owned;
    **D5 residual**: a malicious same-uid seat forging a store record is out of scope."
  - **§11 :360** — routing decision recorded as a ~~forgery-robust~~ **confusion-resistant**-stamped (m-1 stamp;
    **D5 residual**), lineage-gated relay. *(Basis: the `FROM` stamp rides m-1's `submit()`; m-4 does not own the
    stamp mechanism, so it mirrors m-1's relabel — cf. census `:360` / `:69` extra hit my full-net grep caught.)*
- **SCOPE ×1 — §12 :381–:387:** the execution-fidelity gap is now named a **documented frank residual (D5-class)** (a
  lane may serve a different model than the record declares; frank does not verify it); "Step-3 closes the gap
  **structurally**" is explicitly scoped to **Step-3 only — future standalone-runtime hardening, NOT a frank/Step-1
  claim.** *(Census `:384` scope item; the Step-3 "structural" property holds by the conductor's own control-flow at
  Step-3, so it is kept but fenced to the future step.)*

### Full classified survivor list (complete net — design doc + README)
| loc | claim | class | reason |
|---|---|---|---|
| §0 :39 | "makes R2 **structural** at the deviation comparison" | **KEEP** | R2 gate-grammar invariant (licensed class; holds by m-2 schema grammar) |
| §0 :41 | "R2-safe, but flag-only rather than **structural**" | **KEEP** | same R2 grammar context; accurate hedge |
| §2.1 :69 | "**forgery-robust** stamped `FROM`" | **RELABEL** | → confusion-resistant + D5 residual (rides m-1 stamp) |
| §2 :76 | "The `routing_decision` record — R2 **by construction**" | **KEEP** | R2 gate-grammar invariant — model-identity non-gate-referenceable by grammar (census KEEP; = ARCHITECTURE R2 KEEP) |
| §2 :78 | "the single sharpest **proof** obligation" | **KEEP** | design proof-obligation for the R2 grammar invariant, not a malice-containment claim |
| §4 :187 | "an immutable record **cannot be reconstructed** after the fact" | **KEEP** | replay-completeness rationale (why we snapshot), not a malice claim |
| §11 :360 | "**forgery-robust**-stamped, lineage-gated relay" | **RELABEL** | → confusion-resistant + D5 residual |
| §12 :381 | "the way the upstream protocol §10 documented deferred **forgery**" | **KEEP** | honest citation of a documented deferral (names a residual, doesn't claim containment) |
| §12 :384 | "Step-3 closes the gap **structurally**" | **SCOPE** | fenced to Step-3-only future; frank gap = documented D5-class residual |
| README | — | **CLEAN** | full-net grep: zero overclaim-vocab hits |

**Net: 2 RELABEL, 1 SCOPE, 7 KEEP.** The two RELABELs are the only *malicious-seat-stopped* claims; both were
m-1-stamp-derived (mirror m-1). The KEEPs are the licensed classes: **R2 gate-grammar** (the whole `structural/by
construction` set here is R2, not a malice claim), one replay-completeness fact, and one honest deferral citation.

**Guardrail compliance:** claim-text only; **no mechanism change** (R2, the deviation gate, the record shape, the
capability-prior layers, the m-3 seam — all unchanged); no locked-contract reopen; no PLAN/IMPL/`pcode`/spike; no
operator decision reopened.

ACTIONS_GIT_REF: folded `master/domains/m-4-routing-policy/design/2026-06-29-routing-policy-design.md` §2.1 :69 + §11 :360 relabels + §12 :381–:387 scope + new §16 c5 fold-log; wrote this relay + appended `master/relays/INDEX.md`; no code/source/`pcode`, no PLAN, no mechanism change.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-4.implementer files a review-only `DESIGN-REVIEW` semantic approve of the relabels + survivor list; the orchestrator folds the four light-domain closures into the c5 status ledger. m-4.planner holds — no PLAN this phase.
