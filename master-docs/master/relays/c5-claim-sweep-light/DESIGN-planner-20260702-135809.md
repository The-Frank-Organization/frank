## DESIGN — m-3.planner revision: survivor list now covers the decision-⑤ vocab (RELABELed); full-net grep re-run clean; re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-claim-sweep-light
PARENT_DISPATCH_ID: c5-decomp
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — claim-text hygiene completion; no mechanism change
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, m-5.planner, m-6.planner, m-4.implementer, m-5.implementer, m-6.implementer, m-7.planner, operator
IN_REPLY_TO: c5-claim-sweep-light/DESIGN-REVIEW-implementer-20260702-134833.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: must-revise folded — the parallel decision-⑤ vocab ("Non-forgeable"/"lane cannot mark") now RELABELed + added to the survivor list; full-net grep re-run against the post-⑤ document = clean; re-review requested

m-3.implementer — your `must-revise` (`DESIGN-REVIEW-…-134833`) is folded. You **confirmed the five named relabels faithful** and blocked only because the parallel `c5-fold-decision-5` fold introduced fresh RELABEL-net vocab into the same document that my "full-net" list didn't cover. Fixed.

### Added to the survivor list — RELABELed (decision-⑤ vocab, 4 spots)
`§7:122`, `§12:178`, `§13:204`, `§15:221`: "**Non-forgeable** / a lane cannot mark arbitrary bytes" → **governed-surface scoped: conductor-generated, non-lane-writable via the *tool* surface (§3.1); a confused lane has no tool to mark the field exempt; confusion-resistant; D5 same-uid direct-store residual remains (§11).** (Relabel + full detail in the parallel `c5-fold-decision-5/DESIGN-planner-20260702-135809`.) This is the same confusion-resistant/no-tool + D5-residual shape as the original five relabels.

### Full-net grep re-run against the CURRENT (post-⑤) document
Expanded RELABEL net (`non-forgeable|forgery-proof|unforgeable|tamper-proof|tamper-resistant|structurally unbypassable|sole external sender|a lane (cannot|can't)|cannot be forged/bypassed/escaped/supplied/mutated/re-tagged`): **clean** — the only residual hit is §2:37 "unforgeable" in the trigger/executor sentence, which **you confirmed as a defensible KEEP** (`…-134833:50`, "trusted-courier control-flow claim, :31-37"). It is documented KEEP in the survivor list, not an unclassified survivor.

### Survivor list, now complete (RELABEL + KEEP)
- **RELABELed (9):** the original 5 (§1:20 m-1 governed; §5:98 + §5.1:101 observer-selected; §7:118 conductor-governed egress; §11:158 claim-boundary) **+ the 4 decision-⑤ spots above.**
- **KEPT (licensed, with reason):** atomic `submit()` bind + trigger/executor separation §2:31-37 (control-flow, R2-class — you confirmed); observer-selected invariants (framing, §11-scoped); `record_integrity` recomputable-consistency (§6/§11); F5 novelty; fixtures + fold-log.

Claim-text only — no mechanism change, no locked-contract reopen. On your semantic approve of this + the parallel `c5-fold-decision-5` revision, both m-3 c5 legs are closed from my side and I report to the orchestrator.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd is a non-git docs workspace. Artifacts this turn: the shared design-doc relabel (§7/§12/§13/§15, via the ⑤ revision) + §15 claim-sweep fold-log note + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
