## DESIGN (Template I) — m-1.planner CQ closure for CQ-1 / CQ-6 / CQ-8 → m-1.implementer independent review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-m1
PARENT_DISPATCH_ID: c4-cq-m1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded confirm-or-correct against a locked contract; no operator decision surfaced
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-2.implementer, m-6.planner, m-6.implementer, m-7.planner, m-7.implementer
BUNDLE_ID: c4-cq-m1
OWNER: m-1 (Trust & Identity — store/provenance/binding)
DESIGN_DOC_ID: c1-design-m-1-trust-identity
IN_REPLY_TO: c4-cq-m1/DESIGN-orchestrator-planner-20260702-012453.md

m-1.implementer — full-pair rigor per the dispatch: I lead the confirm-or-correct for the three CQs on our store/provenance/binding surface; **independently review each** (approve / must-revise / reject-narrow / human-decision-required) against our **locked** c1 contract. **No locked-contract reopen, no new domain design, no PLAN/IMPL/`pcode`, no m-7 design-LOCK by implication.** I did **not** edit the locked design doc — a confirm is not a change. This is a read-only DESIGN closure; no source/`pcode/` edits.

---

### CQ-1 — phase-split required-set — resolution **(a) step-gate** — STATUS: **closed** (m-1 half; m-2 co-sign = schema half)

**Confirm (a): observe-owned `required_when` predicates are step-gated on observe-layer presence — absent in Step-1 ⇒ not required.** This is what our locked §5 Step-1 boundary requires: **Step-1 = store + form + lineage; the m-3 observe gate + the DI-5/I3 observed-value fill are the reserved additive Step-2 hook; "Step-1 records carry no observe gate."** (design §5:100-104, the `readiness-fix-c1` seam.) A Step-1 form gate therefore MUST NOT demand an observe-owned field, because Step-1 has no observe writer.

**Reject (b) conductor-side filler — it breaks our contract two ways:**
1. A Step-1 filler that actually reads lane ground-truth to fill observe-owned fields **is** the DI-5 out-of-lane observe read — which we explicitly reserved to Step-2. That re-opens the readiness-fix-c1 boundary. ✗
2. A Step-1 filler that supplies values **without** the out-of-lane read fabricates evidence — violating our honest-fallback discipline: without DI-5 an observed field is **"self-reported / confusion-resistant," NOT observe-by-construction** (design §4:92, §4 honest-fallback). Filling `ACTIONS_GIT_REF`/`FINAL_GIT_STATUS_SHORT`/`achieved_evidence` conductor-side with unverified values would ship dishonest evidence under the m-1 stamp. ✗

**Hard guardrail on (a):** the step-gate applies **only to observe-owned fields.** Every `system_only` / lineage / form-owned required field stays **fully required in Step-1** — the step-gate must not become a lever to relax non-observe requireds. Which fields are observe-owned, and how the step-gate expresses in FieldSpec (`required_when` conditioned on observe-layer presence), is **m-2's co-sign** (rides `c4-cq-gateconfig`). — E1: design §5:100-104, §4:92.

### CQ-6 — persisted seat-binding table + away-token sibling-burn — STATUS: **closed** (with one binding clarification; m-6 co-sign = park/wake edge)

**Confirm the persisted binding table.** Our contract already sites `seat ↔ {connection, credential}` in "the conductor's **private** binding table" / "conductor-owned files" (design §5:99, §13:138). Making it **survive conductor restart + re-attach without re-mint** is that same conductor-owned table made durable — consistent. **m-7 executes the persistence mechanism; m-1 owns credential generation / rotation / revocation** (our §13.3 PLAN carry: "seat-credential lifecycle — generation/epoch, rotation, revocation"). That ownership split is confirmed. — E1: design §5:99, §13:138, §13.3.

**Binding clarification (invariant, derivable from our contract — NOT a new design):** **re-attach-without-re-mint MUST re-verify credential possession over the reconnecting channel** (DI-2 + DI-3). Matching a persisted seat *name* is insufficient; the reconnecting party must prove it holds the persisted `C_seat`, else a conductor restart becomes an impersonation window (a sibling could claim a re-attach slot). Re-attach ≠ new mint — DI-4 still holds (no new seat bound, `mint_seat` stays conductor-only) — but re-attach inherits reject-unbound's "prove the credential, not the name." This falls straight out of DI-2/DI-3/reject-unbound (design §4:80-84), so m-7 must honor it; it is a clarification, not a contradiction of m-7's "re-attach without re-mint."

**Confirm the decision-scoped sibling-burn.** Extends my c3 Seam-C answer (away-token = m-1 TCB, one-time per `(decision_id, seat, choice)`, nonce-burn = an atomic conductor-owned store append; `COORD-planner-20260630-182600`). **Burn set = on the first valid redemption of any `(decision_id, seat, choice)`, atomically burn all sibling tokens sharing `(decision_id, seat)`** — i.e. all other choices for that seat's decision — so a decision cannot be answered twice with a different choice. The burn is **conductor-only (DI-1)** and **atomic (TOCTOU-closed, same discipline as `submit()`)**. Scope is bounded: it does **not** reach other `decision_id`s or other seats' tokens. **m-6 co-signs** the away-window park/wake lifecycle (the token's validity-window vs the one-shot nonce split — window = m-6 policy, nonce/burn = m-1). — E1: `COORD-planner-20260630-182600`, design §4:80-84.

### CQ-8 — INDEX derived-authority — STATUS: **closed** (confirmed consistent; layout UNCHANGED)

**Confirm m-7's reading: canonical records = truth; `INDEX.md` = a derived projection — consistent with our locked §6, layout unchanged.** Our §6 already states the **canonical object is the typed-envelope record**, the on-disk `.md` is a **"view/export only,"** records are **immutable + append-only**, and `INDEX.md` is **append-only** (design §6:110). INDEX-as-derived-projection (rebuilt from records at recovery; corrections appended, never rewritten) is the **same "canonical record = truth, rendered artifacts = views" principle we already apply to the `.md` files**, now extended to INDEX. This **strengthens I1** (records-as-truth, sole-writer, immutable) rather than changing the layout. "Corrections appended never rewritten" is exactly our append-only + immutable-record rule.

**Not a change to the locked on-disk layout.** The `<RELAY_ROOT>/<DISPATCH_ID>/<PHASE>-<ROLE>-<ts>.md` + append-only `INDEX.md` shape is untouched; only the **recovery-provenance semantics** (INDEX's *authority* = derived/rebuildable, not itself a source of truth) is made explicit — which was already implicit in "canonical record = truth." No field, verb, or on-disk change; non-re-cut path untouched. — E1: design §6:110.

---

### CQ-status mapping (VP required-edit 3)
- **CQ-1 → closed** — resolution **(a) step-gate**; m-1 half confirmed; m-2 co-sign (observe-owned field set + FieldSpec expression) rides `c4-cq-gateconfig`.
- **CQ-6 → closed** — persisted binding table + ownership split confirmed; **binding clarification** (re-attach re-verifies credential possession) is a binding PLAN invariant within our contract; decision-scoped sibling-burn `(decision_id, seat)` confirmed; m-6 co-sign (park/wake) rides its thread.
- **CQ-8 → closed** — confirmed consistent with locked §6; **on-disk layout UNCHANGED**, only INDEX recovery-provenance clarified.

**No genuine contradiction with the locked contract surfaced.** All three confirm cleanly; CQ-1 selects (a) and rejects (b) on contract grounds; CQ-6 adds one derivable re-verify invariant. No c1 reopen; no m-7 design-LOCK implied.

### Requested of you (m-1.implementer)
Independent DESIGN-REVIEW of each row: is (a) the only contract-supported CQ-1 answer? Is the CQ-6 re-attach re-verify invariant correctly derived (and does the `(decision_id, seat)` burn scope leak nothing)? Is CQ-8 truly layout-neutral? Return `DESIGN_REVIEW_VERDICT` + any must-revise. On your approve, I report the co-signed closure to the orchestrator for the m-7 design-lock fold.

ACTIONS_GIT_REF: wrote this CQ-closure design-review request + appended `master/relays/INDEX.md`; no design-doc edit (confirm-not-change), no code/source/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-1.implementer returns the independent DESIGN-REVIEW; on approve, m-1.planner reports the co-signed CQ-1/6/8 closure to master.orchestrator-planner for the m-7 design-lock fold.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
