## SITREP — m-6 CQ-6 co-sign is pair-approved; ONE hanging item = m-1's burn-model branch (in-scope per §2C, not droppable, not a reopen)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: c4-cq-m1
PARENT_DISPATCH_ID: c4-cq-m1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — report + fold-readiness recommendation; no design authority requested
GRILL_REQUIRED: no
FROM: m-6.planner
TO: master.orchestrator-planner
CC: m-1.planner, m-1.implementer, m-6.implementer, master.orchestrator-reviewer, operator, m-7.planner, m-7.implementer
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: c4-cq-m1
IN_REPLY_TO: c4-cq-m1/DESIGN-REVIEW-implementer-20260702-020447.md

Reporting CQ-6 fold-readiness from the m-6 co-sign side so the hanging item is explicit, not implicit. Read-only; no design change.

### m-6 CQ-6 side — COMPLETE + pair-approved
- **m-6 park/wake co-sign** (`DESIGN-planner-020100`) — the window(m-6)/nonce-burn(m-1) split + the `(decision_id, seat)` sibling-burn reconcile with my 7-state FSM (exactly-one wake; window-expiry never auto-resolves, J1).
- **m-6.implementer review** (`DESIGN-REVIEW-implementer-020447`) — **`approve`, no blockers.** The split, the exactly-one-wake mapping, J1, and the trigger(m-6)/burn(m-1) ownership are all confirmed correct.
- So of CQ-6's four base pieces — m-1 answer (`013500`), m-1.impl review (`020418` approve), m-6 co-sign, m-6.impl review — **all four are in and approved.**

### The one hanging item — the re-mint-supersedes branch (m-1.planner's call)
My co-sign added a derivable **re-mint-supersedes on resummon** rule (a resummon re-mints a fresh `(decision_id, seat, choice)` set and must **burn the superseded prior-cycle nonces**, so a stale-window reply fails closed). Both implementer reviews correctly parked it: I addressed the burn-model confirm to **m-1.planner** (m-1.impl was CC-only, so `020418` explicitly did not review it; `020447` flagged it as an m-1 confirmation point, not an m-6 burn rewrite).
- **This is in-scope, not optional polish.** It closes the **§2C MUST** "stale approval / sibling-token reuse bite NORMAL operator flows" (`DESIGN-REVIEW-2026-07-01.md:159-161`) — the same away-token restart/burn surface CQ-6 exists to settle. Dropping it would leave the stale-window hole open at the m-6 wake edge.
- **It is bounded, not a reopen.** m-1 need only pick the burn-model expression — **(i) supersede/burn the prior-cycle nonces on re-mint, or (ii) re-mint under a new `decision_id`.** Either closes the hole; I bind m-6's park/wake edge to whichever m-1 picks. No TCB re-ownership (mint/verify/burn stay m-1), no c1/c3 reopen.

### Recommendation (orchestrator's call — I report, you sequence)
Fold CQ-6 as **closed on the base answer + m-6 co-sign (all four pieces approved)**, with the re-mint-supersedes branch tracked as a **bounded m-1.planner confirm** that must land (+ its addressed m-1.impl review) before the away-bridge PLAN — i.e. CQ-6 is foldable into the m-7 design-lock package now, with the branch selection as a named non-locking carry, OR held until m-1 answers, at your discretion. Flagging so it neither hangs the m-7 lock silently nor closes with the §2C hole unaddressed.

### CQ-status mapping (VP required-edit 3)
- **CQ-6 (m-6 co-sign) → closed/approved** — pair-complete; window/nonce split + sibling-burn reconciled with the FSM; m-6.impl `approve`.
- **CQ-6 (re-mint-supersedes branch) → still-open / non-locking carry** — awaits m-1.planner's burn-model pick (i or ii) + addressed m-1.impl review; in-scope per §2C.

Blocked: none on the m-6 side — my co-sign is pair-approved and unconditional for the base answer.
Scope drift risk: none — report-only; the re-mint rule stays a bounded m-1 confirm (no TCB move, no reopen).

ACTIONS_GIT_REF: none — read-only SITREP; wrote this relay + appended `master/relays/INDEX.md`; no design-doc edit, no code/source/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-1.planner picks the re-mint-supersedes burn-model branch (i or ii); the orchestrator folds CQ-6 (base + m-6 co-sign approved) into the m-7 design-lock package with the branch as a tracked carry. m-6 holds.
