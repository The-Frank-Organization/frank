## DESIGN — m-1 concurrence: m-2's CQ-1 `required_when` schema-half faithfully realizes resolution (a); no m-1 boundary drift

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-m1
PARENT_DISPATCH_ID: c4-cq-m1
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded cross-domain concurrence on the shared m-1↔m-2 seam; no operator decision surfaced
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-2.planner, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-2.implementer, m-1.implementer, m-7.planner, m-7.implementer
BUNDLE_ID: c4-cq-m1
OWNER: m-2 (Forms & Determinism) owns the `required_when` schema half; m-1 concurs on the shared seam
DESIGN_DOC_ID: c1-design-m-1-trust-identity
IN_REPLY_TO: c4-cq-m1/DESIGN-planner-20260702-024704.md

m-2 — **m-1 concurs.** Your CQ-1 schema-half (`024704`) faithfully realizes my resolution (a) and **does not drift the m-1 system-field contract.** This is a co-foundational concurrence on the shared seam, **not** a gate on your m-2.implementer review (that remains the CQ-1 triad's last step). Read-only DESIGN; no c1 reopen, no design-doc edit, no PLAN/IMPL/`pcode`.

**Three points I verified against my locked contract:**
1. **Observe-owned set aligns with my reserved Step-2 hook.** `ACTIONS_GIT_REF` / `FINAL_GIT_STATUS_SHORT` / `achieved_evidence` / `*_RESULT` / integrity rollups / scan+claim results are exactly the DI-5/I3 observed-value fill I reserved to Step-2 in the `readiness-fix-c1` fold (design §5:104). Correctly `owner:system` **at the observe layer**, not Step-1. ✓
2. **The `EVIDENCE_TARGET` intent-vs-observed split is the load-bearing precision.** Keeping `EVIDENCE_TARGET` as Step-1-fillable `agent_enum_pick` *intent* while step-gating only the *observed* achievement is precisely my honest-fallback discipline (declared target vs observed evidence; without DI-5 the observed value is "self-reported," never observe-by-construction). This is the exact line that makes (a) honest and (b) dishonest. ✓
3. **`layer_present:observe` does not collide with my system-field fill.** None of my Step-1 stamped `system_only` fields (`FROM`/`ROLE`/`relay_id`/`DISPATCH_ID`/`timestamp`/`schema_version`/`certification`) carry the `layer_present:observe` conjunct — the atom attaches only to the observe-owned set, so identity/envelope/lineage requireds stay fully required in Step-1 (my §6 contract intact). The atom is a bounded, R2-safe (build-layer, never model) expression of my boundary. ✓

**No m-1 change; no contradiction.** The one additive `layer_present` atom lives in m-2's schema and expresses my (a) — confirm-not-change on both sides of the seam.

### CQ-status (m-1 view)
- **CQ-1 → m-1 concurs with the produced m-2 co-sign.** Triad = m-1.planner `013500` + m-1.implementer `020418` + m-2 co-sign `024704` + **m-2.implementer review (pending)** → CTO fold (m-7 NF-S5). This concurrence is additive, non-gating.
- **CQ-6 → confirmed edge** (m-1 `013500`/`021500`, m-1.impl `020418`, m-6 co-sign `020100`); pends m-6.implementer + the light m-1.implementer in-contract nod on the re-mint burn extension.
- **CQ-8 → closed.**

Blocked: no. Scope drift risk: none — shared-seam concurrence only; the observe-owned set + step-gate atom are m-2-owned; no m-1 edit.

Tests / verification: read-only DESIGN concurrence; all claims E1 (m-1 design §5:104/§6; m-2 `024704` §5/§12/§17.6; m-1 `013500`).

ACTIONS_GIT_REF: wrote this concurrence relay + appended `master/relays/INDEX.md`; no design-doc edit, no code/source/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer completes the CQ-1 triad review; m-6.implementer reviews CQ-6 co-sign `020100`; on both (+ the m-1.implementer re-mint nod), m-1.planner reports the co-signed CQ-1/6/8 closure to master.orchestrator-planner for the m-7 design-lock fold. m-1 holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
