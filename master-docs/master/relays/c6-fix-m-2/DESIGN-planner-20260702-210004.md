## DESIGN (m-2 planner-lead → implementer review) — c6-fix-m-2: 7 re-review findings folded

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-2
PARENT_DISPATCH_ID: c6-fix-m-2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — DOC-ONLY re-review cleanup against locked contracts; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: m-2.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-4.planner, m-5.planner
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: c6-fix-m-2
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: c6-fix-m-2/DESIGN-orchestrator-planner-20260702-204503.md

**c6 re-review cleanup — the 7 findings routed to m-2 are folded.** DOC-ONLY; no mechanism change; `Forms & Determinism` lock invariants unchanged; the four sanctioned by-construction claims + confusion-resistant/D5 vocab + the byte-exact `{accepted, rejected, held}` enum preserved. **m-2.implementer: adversarial review requested** (planner-fix → implementer-approve, the c6 return shape). Recorded in the design-doc §18 c6 fold-log.

### Per-finding changelog
| id | tag | fold | location |
|---|---|---|---|
| **m-2-F1** | B ◆ | Decision ③ RAISE-ONLY: typed `gate_category` **monotonic-toward-A** — `known_A` detector = system-floor MAX-raiser, affordance `[floor, A]`; effective home = `gate_category` (raised) + system-computed **`gate_category_raised: bool`**; ③ neg-fixture **AC18**; m-7 known-A/RAISE-ONLY NF fixture registered **owed** | design §17.1, §3 pattern, AC18 |
| **m-2-F10** | M | Canonicalized to **one id `gate_category`**; `human_gate_reason` retired (its "pick a reason" role is `gate_category`'s own `agent_enum_pick` fill) | §17.1, §12 echo, AC10 |
| **m-2-F5** | M | **Gate-referenceability first-class**: `gate_referenceable: bool` on the FieldSpec (default false; model-identity/computed-label/`X-` = false); §5 grammar keys on the field's own flag — the two KEPT by-construction claims now read declared data | §4, §5 |
| **m-2-F7** | M | README status rows swept to post-c4/c5: joint lock **CLOSED**, posture carve-out **CLOSED**, PLAN row → c6 gate | README rows |
| **m-2-F9** | C | §15 operator-judgment (i)/(ii) → **RATIFIED** (ARCH §J1/§J2), stable anchors; + `on_timeout` row + §17.5 echo | §15, §17.2, §17.5 |
| **m-5-F1** | B ◆ | m-2 slice: **propose-vs-stamp per-column ownership** in the §17.3 mirror — planner proposes `seat`/`role`/`seat_archetype` from a fill-time-pruned set; conductor stamps `seat_archetype`+`authority_ceiling` per-column (`system_only`) ⇒ a lane cannot self-elevate its ceiling (confused-deputy fix). m-4/m-5 own their sides (CC'd) | §17.3 |
| **m-5-F2** | M | Re-record lands in **m-5's §C4 ledger** (m-5/CTO write, not an m-2 doc). m-2 posture mirror already consistent (rides `seat_archetype`; §17.6 LOCKED-c3 value-enum) — **no m-2 doc change owed**; flagged to the ledger owner | (m-5 §C4) |

### Verified — not redone (CTO apply half, VP-approved `204236`)
m-2-F2 (CQ-2 `{self_reported, mixed}` §17.6/§17.1/AC16), m-4-F3 (`chosen_model` re-anchor + model-identity-mark exclusion §5/§17.3), m-5-F3 (`template_ref` "set when spawned from template; null otherwise" §17.3), m-2-F4 (`delivery_state` close §17.1), m-2-F6 (exempt ODB `model_name` slot §17.2), m-2-F8 (§8 sole-path scoping) — all confirmed present + consistent.

### Preservation check (c6 constraints)
- **Four sanctioned by-construction claims:** R2 gate-grammar `[c5 KEEP]` (now backed by first-class `gate_referenceable` — strengthened, not changed); observer-selected DI-5 `[c5 KEEP]`; authority-ceiling (reinforced by m-5-F1 propose-vs-stamp) — all intact.
- **Confusion-resistant/D5 vocab:** untouched (18 confusion-resistant / 17 D5-residual loci, c5-approved).
- **Byte-exact enum:** `delivery_state = {accepted, rejected, held}` unchanged; `gate_category` §J2 tokens byte-exact.
- **No mechanism change, no c1/c2/c3/c4 reopen, no design-lock, no PLAN/IMPL/`pcode`/spike.**

### Coordination notes (cross-domain seams, CC'd owners)
- **m-5-F1** — m-4 (routing record) + m-5 (archetype registry) own the propose/candidate-set + per-column stamp mechanism on their sides; my mirror reflects the ownership split only.
- **m-5-F2** — the away/posture-vocab non-locking carry is m-5's §C4 ledger entry; surfaced here so it isn't assumed folded on the m-2 side (it needs no m-2 edit).

Blocked: no — holding for m-2.implementer review.
Scope drift risk: none — the 7 routed findings only; DOC-ONLY.

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/c6-fix-m-2/DESIGN-planner-20260702-210004.md` — OK (below).
- Each m-2 fix landed: `gate_referenceable` ×5, `gate_category_raised` ×3, AC18 present, `human_gate_reason` residuals are retired-notes only, README rows CLOSED.
- Sanctioned claims + confusion-resistant/D5 counts + `{accepted, rejected, held}` preserved (greps clean).
- `git status --short` — unavailable: not a git repo (docs workspace; `pcode/` untouched).

ACTIONS_GIT_REF: folded `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` (§4 `gate_referenceable`; §5 grammar; §17.1 canonical `gate_category` + RAISE-ONLY; §15/§17.2/§17.5 RATIFIED; §17.3 propose-vs-stamp; AC10/AC18; §18 c6 fold-log) + `master/domains/m-2-forms-determinism/README.md` (status sweep); wrote this relay + appended `master/relays/INDEX.md`; no mechanism change, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer files `DESIGN_REVIEW_VERDICT: approve` (or must-revise); on approve I return the `c6-fix-m-2` completion relay to the orchestrator for the c6 verify sweep + c6 close. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
