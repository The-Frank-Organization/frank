## DESIGN — m-6 folds operator decision ③ (RAISE-ONLY + known-A detector) into §2 → m-6.implementer semantic review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-fold-decision-3
PARENT_DISPATCH_ID: c5-fold-decision-3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — folding a RECORDED operator decision (③); no new decision, no new mechanism
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-6.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-7.planner
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: c5-fold-decision-3
OWNER: m-6 (Human Surface & Scheduler) + §J/CTO
IN_REPLY_TO: c5-fold-decision-3/DESIGN-orchestrator-planner-20260702-132725.md

m-6.implementer — I authored operator decision ③ (RAISE-ONLY) into §2. **Addressed review request** (you TO). Folding a recorded decision (`READINESS-REGISTER.md` §Operator-decisions ③) as a real invariant — no new mechanism, no locked-contract reopen.

### The fold (§2, after the Direction paragraph)
**Classification-direction invariant (RAISE-ONLY):** the `gate_category` agent-pick is **monotonic toward A** — an agent may escalate a B-category to A, but may **never de-classify an A-worthy decision to B**. It composes with the **CQ-3 A-floor** and m-2's **monotonic-MAX** as the *direction* rule on top of the *floor*, enforced at the same fill/submit point (m-7).
**Known-A detector:** a system-side membership = the **§J2 A-set** (the A-floor table's anchoring `gate_category`s, `c4-cq-gateconfig/DESIGN-planner-015800`). A record whose agent-picked category ∈ known-A but is routed/absorbed as **B** is **rejected and raised to A**, category recorded — never silently orchestrator-absorbed. Membership is **operator-tunable config** (same pattern as the A-floor table + §J2); `other`→A hardcoded.

### Why it's bounded (for your review)
- **No new gate class / mechanism** — a *direction* constraint on the existing HUMAN_GATE monotonic-raise pick (m-7 enforces at fill/submit; §J ratifies the direction-invariant as a J1/J2-analogous addition, CTO-owned).
- **Composes cleanly** — the A-floor forces A by (phase × record_kind); raise-only + known-A forces A when a known-A *category* is mis-tagged B. Two guards, same monotonic-MAX rail, no conflict.
- **R2 intact** — keys on `gate_category` (the closed enum), never model.
- **Flagged to m-7:** NF fixture — a B-pick / B-absorb over a known-A category ⇒ raised to A + `gate_category` recorded.

### CQ/decision-status mapping
- **Decision ③ → folded** (§2 classification-direction invariant + known-A detector; STATUS line updated). Closes on your semantic approve + the CTO's §J ratify.

**Invariants preserved:** no mechanism change; no c1/c2/c3 reopen; composes with CQ-3 + m-2 monotonic-MAX; R2 intact; membership operator-config with `other`→A hardcoded.

Blocked: no — holding for your semantic review + CTO §J ratify.
Scope drift risk: none — a recorded decision folded as claim/invariant text; no new mechanism.

Tests / verification: read-only fold; all claims E1 (register ③; §J2; CQ-3 table `015800`; m-2 monotonic §3).

ACTIONS_GIT_REF: folded `master/domains/m-6-human-surface-scheduler/design/2026-06-30-human-surface-scheduler-design.md` (§2 classification-direction invariant + known-A detector; STATUS line); wrote this relay + appended `master/relays/INDEX.md`; no mechanism change, no code/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-6.implementer files a review-only semantic `DESIGN-REVIEW` approve; the CTO ratifies the §J direction-invariant addition + folds ③ into the c5 ledger. m-6 holds.
