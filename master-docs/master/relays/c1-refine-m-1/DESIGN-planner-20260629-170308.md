## Team m-1 — Trust & Identity: DESIGN-REVIEW REQUEST (rev2)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c1-refine-m-1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — read-only DESIGN-REVIEW of the rev2 fold; operator decisions unchanged from rev1 grill
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, m-2.planner, master.orchestrator-reviewer, operator
RUN_ID: master
PARENT_DISPATCH_ID: c1-refine-m-1
DESIGN_DOC_ID: c1-design-m-1-trust-identity
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
REPO: master/ (docs workspace; cwd is not a git repo; pcode/ is the future code repo)

Current scope for the `TO` addressee: read-only DESIGN-REVIEW of the **rev2 fold** (you already approved rev1 —
c1-design-m-1-review, 184021). No source/test edits, no branches, no commits, no PRs. AUDIT+DESIGN-only; terminal = design-lock.

Design doc to review (rev2):
- DESIGN_DOC_ID: c1-design-m-1-trust-identity
- Location: master/domains/m-1-trust-identity/design/2026-06-28-trust-identity-design.md (rev2 fold-log §0)
- Basis: dispatch c1-refine-m-1 (fold the VP-approved c1 consumer review — m-3/m-4/m-6); the design SHAPE was endorsed (rev1 approve) — rev2 is targeted folds, not a redesign.
- What changed in rev2 (only these — rev1 body otherwise intact):
  1. **DI-5 + I3 observe-integrity (§4):** a new deployment invariant DI-5 (conductor reads lane ground-truth from outside the lane) and a new property I3 (evidence-by-construction), made explicitly DISTINCT from DI-2 (DI-2 = credential confidentiality, not read-vantage), with the honest-fallback "self-reported" label. Echoed in §9/§11/§12(3b).
  2. **Operator/human address (§6, §7 m-6):** `operator` as a first-class minted-address-space entry — mailbox/projection + recipient_picker member + `FROM` stamped via the operator-relay channel (not a lane credential); generalizes to special addresses.
  3. **Sharpening-D (§6 parent_picker, §7 m-4):** an accepted routing relay may sit in the conductor-derived parent_picker candidate set as bookkeeping; model stays payload, never a gate input.
  4. **§13.4 #4 → positive write-allowlist:** the m-3 hook may write ONLY the closed observed/computed set + a pass/fail veto; no system_only/identity/envelope write; no delivery effect.
  5. **§5 submit ordering:** aligned to m-2 §4 "submitted → pre-flights → accepted" (observe before any append; atomic accept).
- m-1↔m-2 re-affirm: mutually re-affirmed (COORD 165643 + m-2 CONCUR 170032; m-2 REAFFIRM 165702 + m-1 CONCUR 170308). Shared contract unchanged; R1/R2/R3 + m-2's G2/ODB/routing-join all concurred.
- Boundary contract / acceptance criteria / GRILL_LOCK: §7 / §12 / §10 (operator decisions unchanged from rev1).
- Open questions: §11 — DI-2 + DI-5 concrete realization (fork-2 infra call → orchestrator at PLAN); identity≠authority; mailbox realization.

Specific challenge targets (adversarial review of the rev2 folds — try to break these):
1. **DI-5 distinctness (§4 I3).** Is DI-5 genuinely orthogonal to DI-2, or did I double-count? Is "evidence-by-construction" honestly conditional on an out-of-lane read path, with the right fallback when it's unmet? Any observe path where a lane can still spoof ground-truth despite DI-5?
2. **Operator-FROM stamping (§6).** Does routing operator-FROM through the operator-relay channel (not a lane mint) actually close forgery, or does it introduce a new trusted path that itself needs an invariant? Can a lane ever present `operator`?
3. **Routing-relay candidate (§6/R2).** Does admitting an accepted routing relay into parent_picker leak model identity into any gate, or open a free-typed-parent surface? Is "accepted routing relay" unambiguously conductor-derived?
4. **Write-allowlist (§13.4 #4).** Is the closed m-3 field set actually closed, and does the allowlist framing fully prevent the hook from becoming a second identity authority (no envelope/delivery write)?
5. **Ordering (§5).** Does the rev2 submit ordering preserve the rev1 I1/I2 proof (nothing observable appended before pre-flights pass; atomic accept; TOCTOU still closed at the design boundary)?
6. **Regression check:** does any rev2 fold weaken the rev1 approve? If not, confirm.

Requested response:
Use Template C (`PHASE: DESIGN-REVIEW`, `FROM: m-1.implementer`, `TO: m-1.planner`, `PARENT_DISPATCH_ID: c1-refine-m-1`, same `DESIGN_DOC_ID: c1-design-m-1-trust-identity`) and return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`.

ACTIONS_GIT_REF: no edits claimed beyond authoring this relay + the rev2 design-doc/README edits under master/domains/ (design artifacts, not source); cwd is not a git repo so a git ref is unavailable.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
