## SITREP — m-2 fold COMPLETE: `send()` reconciled to the CTO-arbitrated write-path seam (Cluster 1, m-2 side)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c1
PARENT_DISPATCH_ID: readiness-fix-c1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded design fold of a VP-co-signed seam; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-1.planner, m-1.implementer, m-2.implementer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: readiness-fix-c1/DESIGN-orchestrator-planner-20260630-221356.md

**m-2 side folded. No breaking domain constraint — I accept the arbitrated seam as-is.** Reporting for the Cluster-1 re-verify. Read-only DESIGN fold of a VP-co-signed seam; no source/`pcode/` edits, no PLAN, no closure claim.

**Reconciled against the canonical, not the initial proposal.** I carried the whole thread before folding: initial arbitration `221356` → CTO fold accepting the VP tightening `223018` → **VP `approve` `223751`** (canonical seam `ARCHITECTURE.md:58-65`). I folded `send()` to the VP-co-signed text.

**What I changed (m-2 fold split — §4 steps 4–5, formerly `send` :72-73):**
- Rewrote the write path to **pre-append validation → one atomic accept**: resolve/stamp (m-1) → **[pre-append, candidate held in-courier, nothing persisted]** (a) form-validation → (b) **cross-relay lineage gate** (candidate vs the persisted `accepted`-graph, walks `persisted ∪ {candidate}`, **no `submitted` limbo append**) → (c) **[RESERVED — Step-2] observe-as-send** (m-3) → **one atomic `accepted` append** on pass / **distinct terminal `rejected` evidenced record + bounce** on fail.
- **Removed the persisted `submitted`/attempted append** (the rev1 two-state mechanism, Finding 1a); the lineage gate now validates the in-courier candidate against the persisted `accepted`-graph — equivalent walk, no lane-writable limbo.
- **Reserved m-3 observe to Step-2** (2c) + named the build boundary **Step-1 = store + form + lineage** (Finding 1b; m-3 builds Step-2 per ROADMAP/ARCHITECTURE).
- Recast the failure record as a **terminal `rejected`** record (m-2's audit-of-attempts trail preserved as a *terminal* record, not limbo).
- Propagated the seam through every dependent slot: AC8 (§8), §11 point 1, the rev1 blocker-2 fold-log (marked SUPERSEDED), the §17.5 R3 ordering note; and added negative-fixture **AC13**.
- **Consumer-flagged rename (m-6):** `delivery_state` value token `bounced` → `rejected` to match the seam's terminal-`rejected` record (§17.1/§12) — **same m-6-consumed computed field**, value token only, no shape/bucket-projection change. Flagged here for m-6 awareness.

**Byte-consistency with m-1 (their `submit()` fold SITREP `224500`) — confirmed.** Both sides now read: resolve/stamp → pre-append (a) form → (b) lineage over `persisted ∪ {candidate}` (no limbo) → (c) reserved Step-2 observe → one atomic `accepted` / terminal `rejected`. The lineage-gate hook-point is explicitly present in both docs; Step-1 requires no m-3 in either. — E1, m-2 design §4:72-73 + §18 vs m-1 §5:100-104 + ARCHITECTURE:58-65.

**Invariants preserved (must-not-change list, all held):** sole-writer append-only store (I1); channel-stamped `FROM`; TOCTOU-closed single accept transaction; "authority blocks before dispatch" (now via the pre-append lineage gate, protocol.md:397/407); consumers act only on `accepted`. No new field except the m-1-owned store transaction semantics I consume; no schema re-cut.

**No breaking domain constraint surfaced.** The seam is strictly better for m-2: it keeps my lineage-gate-before-delivery guarantee (now as pre-append validation) **and** an auditable attempt trail (now a terminal `rejected` record), while dropping a persisted un-gated `submitted` state that only ever existed to let the lineage engine walk the graph — the in-courier `persisted ∪ {candidate}` walk gives the identical reachability without the limbo.

**Not done (by design):** Cluster 1 does **not** close on this relay. It closes only when (i) m-1 [done, `224500`] **and** m-2 [done, this relay] both fold byte-consistent, (ii) the CTO ARCHITECTURE/register fold is current [done, `223018`/`223751`], and (iii) the CTO re-verifies all three consistent. The CTO re-verify remains. No Step-1 PLAN opens until Cluster 1 + Cluster 4a/4b both close.

Blocked: no — holding for the CTO re-verify.
Scope drift risk: none — write-path seam only; no other c1 change; phase stays AUDIT+DESIGN.

Tests / verification: read-only DESIGN fold; all claims E1 (source-cited to design §4/§8/§11/§17/§18 + the thread `221356`/`223018`/`223751` + m-1 `224500`).

ACTIONS_GIT_REF: folded `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` (§4 steps 4–5 + AC8/§11/§17.1/§17.5 slot alignments + new §18 fold-log + AC13); wrote this SITREP + appended `master/relays/INDEX.md`; no code/source/pcode edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: CTO re-verifies Cluster 1 byte-consistency across m-1, m-2, ARCHITECTURE, and the register. m-2.planner holds — no self-advance to PLAN (cycle terminal = design-lock).
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
