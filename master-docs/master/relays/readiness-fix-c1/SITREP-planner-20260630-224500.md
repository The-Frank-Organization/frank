## SITREP — m-1 fold COMPLETE: `submit()` reconciled to the CTO-arbitrated write-path seam (Cluster 1, m-1 side)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c1
PARENT_DISPATCH_ID: readiness-fix-c1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded design fold of a VP-co-signed seam; operator on CC
GRILL_REQUIRED: no
FROM: m-1.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-1.implementer, m-2.planner, m-2.implementer, operator
DESIGN_DOC_ID: c1-design-m-1-trust-identity
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: readiness-fix-c1/DESIGN-orchestrator-planner-20260630-221356.md

**m-1 side folded. No breaking domain constraint — I accept the arbitrated seam as-is.** Reporting for the Cluster-1 re-verify. This is a read-only DESIGN fold of a VP-co-signed seam; no source/`pcode/` edits, no PLAN, no closure claim.

**Reconciled against the canonical, not the initial proposal.** I carried the whole thread before folding: initial arbitration `221356` → CTO fold accepting the VP tightening `223018` → **VP `approve` `223751`** (canonical seam now `ARCHITECTURE.md:58-65`). I folded to the **VP-co-signed** text, not the superseded first proposal. — E1.

**What I changed (m-1 fold split — `submit()` §5:100-104):**
- Rewrote `submit()` to the **one-atomic-accept, gates-as-pre-append-validation** ordering: resolve/stamp → **[pre-append, candidate held in-courier, nothing persisted]** (a) m-2 form-validation → (b) **m-2 cross-relay lineage gate** (candidate vs the persisted `accepted`-graph, walks `persisted ∪ {candidate}`, no limbo append) → (c) **[RESERVED — Step-2] m-3 observe-as-send** → **one atomic `accepted` append** on pass / **one terminal `rejected` evidenced record** on fail. **No persisted `submitted` limbo.**
- **Added the m-2 lineage-gate hook-point** (2b) that the readiness review found absent from m-1's ordering (Finding 1a) — supplied in-courier, forgery-robust via the system-filled `PARENT`.
- **Reserved m-3 observe to Step-2** (2c) + named the build boundary **Step-1 = store + form + lineage** (Finding 1b; m-3 builds Step-2 per ROADMAP:57-70 / ARCHITECTURE:120). I3 evidence-by-construction lands with m-3 at Step-2.
- **Dropped** "reads identically to m-2 §4" and the two-state framing; recast fail as a **terminal `rejected`** record (audit-of-attempts preserved as terminal, not limbo).
- Logged the fold in §0.b + marked the now-superseded rev2 fold-log item 5.

**Acceptance ("reconciled") — m-1 half met:** the `submit()` ordering is byte-consistent with `ARCHITECTURE.md:58-65`; the **lineage gate has an explicit hook-point present** (2b); **Step-1 requires no m-3** (observe reserved to Step-2). — E1, source design §5:100-104 + §0.b vs ARCHITECTURE:58-65.

**Invariants preserved (must-not-change list, all held):** sole-writer append-only store (I1); channel-stamped `FROM`; TOCTOU-closed single accept transaction (§13.2); "authority blocks before dispatch" (now via the pre-append lineage gate, protocol.md:397/407); consumers act only on `accepted`. No new field, no new verb, no on-disk re-cut; the token→SO_PEERCRED/mTLS non-re-cut path is untouched. — E1.

**No breaking domain constraint surfaced.** The seam is strictly compatible with the m-1 TCB: walking `persisted ∪ {candidate}` in-courier needs no lane-writable limbo state (honors DI-1), and the terminal `rejected` record is a conductor-owned append like any other (I1). The seam is, if anything, *cleaner* for m-1 than the superseded two-state path — it removes a persisted un-gated state I never wanted.

**Not done (by design):** Cluster 1 does **not** close on this relay — it closes only when (i) m-1 [done, this relay] **and** m-2 both fold byte-consistent, (ii) the CTO ARCHITECTURE/register fold is current [done, 223018/223751], and (iii) the CTO re-verifies all three consistent. m-2's `send()` fold + the CTO re-verify remain. No Step-1 PLAN opens until Cluster 1 + Cluster 4a/4b both close.

Blocked: no — holding for m-2's fold + the CTO re-verify.
Scope drift risk: none — write-path seam only; no other c1 change; phase stays AUDIT+DESIGN.

Tests / verification: read-only DESIGN fold; all claims E1 (source-cited to design §5/§0.b + ARCHITECTURE:58-65 + the thread 221356/223018/223751).

ACTIONS_GIT_REF: folded `master/domains/m-1-trust-identity/design/2026-06-28-trust-identity-design.md` (§5 `submit()` + §0.b fold-log + item-5 supersede note); wrote this SITREP + appended `master/relays/INDEX.md`; no code/source/pcode edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2 folds `send()` :72-73 to the same seam; CTO re-verifies Cluster 1 across m-1, m-2, ARCHITECTURE, and the register. m-1.planner holds — no self-advance to PLAN (cycle terminal = design-lock).
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
