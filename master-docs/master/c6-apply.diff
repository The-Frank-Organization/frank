--- a/CLAUDE.md	2026-07-02 19:49:04
+++ b/CLAUDE.md	2026-07-02 19:51:10
@@ -23,7 +23,8 @@
 **frank = "the conductor":** our own thin trusted courier (NOT built on jcode / claude-code — prior-art
 references only). Locked high-level decisions — altitude **B** (planner emits role+model per dispatch),
 policy **3-staged** (capability priors + justified deviation; benchmark feedback in a later release), identity
-**= seat-trust via channel-stamped FROM** (forgery-robust by construction). Full, dated design state in
+**= seat-trust via channel-stamped FROM** (confusion-resistant; D5 residual accepted;
+forgery-robust-*by-construction* = the D3-shelved wrap milestone, not the Step-1 claim). Full, dated design state in
 the pre-build design-state export (not vendored) — see the adaptive-routing pillar note.
 Shipped baseline = the upstream protocol (release corpus, not vendored).
 
@@ -41,7 +42,7 @@
 ## Domains (durable ownership)
 | Pair | Domain | Owns | Consumes (collision edges) |
 |---|---|---|---|
-| **m-1** | **Trust & Identity** (the TCB) | sole-writer append-only relay store, seat-stamper / channel-stamped FROM, store isolation, inline-gate placement | — *(foundational; everyone consumes the store + identity stamp)* |
+| **m-1** | **Trust & Identity** (the TCB) | sole-*governed*-writer append-only relay store (confusion-resistant; D5 accepted), seat-stamper / channel-stamped FROM, store isolation, inline-gate placement | — *(foundational; everyone consumes the store + identity stamp)* |
 | **m-2** | **Forms & Determinism** | declarative field schema (field·owner·type·required-when·enum), field-ownership model, linter → form-validation + lineage-engine, dynamic required-set | m-1 store |
 | **m-3** | **Observation & Evidence** | observe-as-send-gate, per-phase done-predicates, evidence ladder, executable claims, egress/content-safety gate | m-2 schema, m-1 isolation |
 | **m-4** | **Routing & Policy** | model→seat router, capability priors, routing record, justified deviation, benchmark + a later release loop | m-2 schema, m-1 identity |
--- a/master/ARCHITECTURE.md	2026-07-02 19:49:04
+++ b/master/ARCHITECTURE.md	2026-07-02 21:28:16
@@ -99,7 +99,8 @@
 - *Forward (m-6 scheduler + m-3 egress):* **away-mode external bridge** — when the operator declares "out," the
   conductor mirrors A-bucket gates to the operator's real inbox (e.g. Gmail) and accepts the email reply as the
   verdict. **Opt-in, not the default** (default = local in-app inbox); it is the *first external send*, so it is
-  gated by the **fail-closed egress scan** (secrets / PII / model-names) before anything leaves.
+  gated by the **fail-closed egress scan** (secrets / PII / model-names — the decision-⑤ typed-ODB
+  operator-facing model-name carve-out excepted, m-3 §7) before anything leaves.
 
 **J2 — `gate_category` (default set; operator-CONFIGURABLE).** The membership, the A/B mapping, and the
 protected-branch set are **operator-configurable policy** — a default ships, and the design accommodates
@@ -222,7 +223,7 @@
 - **Initial-release template lineup (proposed):** T1 **Solo**, T2 **Adversarial Pair**, T3 **Sensor** (the orchestrator+N-pairs
   "conductor" template deferred to c3).
 - **Sensor archetype** (the `/btw` side-question made spawnable): read-only ceiling, tool-blocked, single-turn,
-  shared read-only fork, non-interrupting parallel, routed `fast-cheap`. **Integrity split:** answer *content* =
+  shared read-only fork, non-interrupting parallel, routed `fast_cheap`. **Integrity split:** answer *content* =
   `self_reported`/advisory + never gate-bearing; observable *metadata* (tool-blocked, no-source-actions, 1-turn)
   = `observed`.
 
@@ -343,7 +344,7 @@
 The away-mode bridge converts **untrusted SMTP/IMAP-inbound → a trusted operator-channel verdict record**. The
 **first conditional-upstream-contract-check** (VP F3) resolved **(A) m-1 owns the mint/verify** (`182600`, accepted
 `183008`, folded `183345`). **A is forced** by two locked m-1 invariants: **DI-1** (one-shot nonce-burn = an atomic
-conductor-owned store append; lanes have no write path) + **DI-2** (the signing key is a TCB secret; m-6 custody = a
+conductor-owned store append; confused lanes have no store-write tool — D5 residual: same-uid direct store write out of scope, §C4.3) + **DI-2** (the signing key is a TCB secret; m-6 custody = a
 forbidden second identity authority). It is **additive** — the first activation of m-1's **already-reserved
 `certification` (DKIM-analog) field** (present-but-null; email is the channel-stamp-unavailable case that seam was
 reserved for): **no field re-owned, no fifth public verb, no schema/on-disk re-cut; c1 not reopened.**
@@ -432,7 +433,7 @@
   **`submit` / `project` / `read`**; `submit`'s input schema **IS** the rendered m-2 form (forbidden options absent);
   raw store/config/outbox/operator-channel paths + config *values* are absent from every seat surface. Delivery/wake =
   one `write()` onto the seat's pipe (per-runtime adapters). **Confusion-resistant, not adversarial isolation.**
-- **Trusted config.** Per-domain sections (m-6/m-3/m-4-authored) composed into one artifact under a **single top-level
+- **Trusted config.** Per-domain sections (**m-2/m-3/m-4/m-5/m-6-authored** — the CQ-4b full author set, incl. m-5's archetype registry + m-2's declared section, x3-F2 c6) composed into one artifact under a **single top-level
   digest** + per-section stamps (CQ-4b), loaded once at trusted startup, digest-pinned to genesis, restart-only, absent
   from seat surfaces. Plus local-outbox-only external send (behind the m-3 egress gate), store genesis + GC, persisted
   seat-binding + decision-scoped `(decision_id, seat)` sibling-burn (CQ-6 base).
@@ -446,9 +447,13 @@
 park/wake state machine + buckets/ODB. Every hosted row carries a biting negative fixture (§12) proving the guardrail.
 
 ### C4.3 Claim boundary (the line the NO-GO turned on)
-Step-1 = attach + interface guardrail = **CONFUSION-RESISTANT**. The **sole** licensed "by construction" claim is the
-serialized-loop kill of the two-honest-seats double-accept race (a control-flow property of the trusted engine). A
-same-uid, shell-bearing seat can write the store files directly — the documented **D5 accepted-risk**, out of scope;
+Step-1 = attach + interface guardrail = **CONFUSION-RESISTANT**. The licensed "by construction" claims are exactly the
+**four classes** the RECONCILE c5 kept-class records (not one): (1) the **serialized-loop kill** of the two-honest-seats
+double-accept race (a control-flow property of the trusted engine, m-7 §2.4); (2) the **R2 no-model-predicate gate
+grammar** — no model-derived predicate enters any gate (§C2.3); (3) the **observer-selected control properties** — the
+F1 archetype invariants selected conductor-side, not by the observed lane (§C3.1); (4) **authority-ceilings-at-spawn**
+(the §C3.1 ceiling vector). Everything else — attach + the interface guardrail — is confusion-resistant, not
+by-construction. A same-uid, shell-bearing seat can write the store files directly — the documented **D5 accepted-risk**, out of scope;
 adversarial containment / wrap / "sole-writer by construction" remain **shelved** (`GRILL-LOCK-deployment-fork-2026-07-01.md`).
 The design's semantic claim-sweep (§16) is clean.
 
@@ -457,6 +462,13 @@
 operator-gated runtime spikes (RUNTIME-RESEARCH §12). This lock grants no PLAN/IMPL/code/spike; **Step-1 PLAN** is the
 operator-opened gate (re-baseline step (e)), after (c) global claim-sweep + (d) §2C-at-build-step.
 
-**Integrated §2C away-bridge build-carry ledger (recorded c5, mechanism deferred to step (d) — one place a (d) builder inherits the away-token constraints):**
+**Integrated §2C build-carry ledger (recorded c5, mechanism deferred to step (d) — the one place a (d) builder inherits ALL §2C carries: away-token AND routing-lane):**
 - **`re-mint-supersedes`** (m-7 CQ-6, m-1-confirmed-fit) — a resummon-mint burns the superseded prior-cycle `(decision_id, seat)` nonces.
 - **Decision ④ away-token ROTATE + RE-OBSERVE** (RATIFIED c5, recorded m-1 `c5-fold-decision-4` + m-6 resummon edge, both impl-approved) — a refresh **rotates `decision_id` + burns prior nonces**; `verify` **re-observes current state and bounces the approval if it changed** since the operator last saw it (closes the stale-approval / TOCTOU window). **Dormant in Step-1** (no away-bridge/resummon exists yet); until (d), the base decision-scoped sibling-burn + m-6's never-auto-resolve-on-expiry FSM hold. Detailed design + fixture + adversarial review = a **step-(d) gate**, not design-locked here.
+- **R2 `gate_referenceable` per-column FieldSpec** (m-4/m-2, recorded c5) — the **attribute is now declared first-class** (m-2 §4 `gate_referenceable: bool`, default false, `c6-fix-m-2`); the per-column **negative fixtures** over `chosen_model` and single-family bucket-valued proxies (R2's no-model-predicate grammar tested at the **live field grain**, not the ghost `selected_model`) remain a **step-(d) gate**.
+- **Altitude-B per-row deviation grain** (m-4, recorded c5) — per-row `justified_deviation` / `deviation_reason_code` disposition so the silent-deviation veto is caught at **row grain, not record grain**. Value-set + fixtures = a **step-(d) gate**.
+- **Away-mode trigger expressibility (m-5-F2, CTO ruling c6):** the m-5 posture model cannot yet express the away-mode trigger — **recorded a non-locking step-(d) build-carry**; the trigger's posture-model expression is designed when the **away-bridge** is built (it rides the deferred away-token/away-bridge mechanism above — not a Step-1 blocker).
+
+**Owed Step-1-build fixtures (registration owed — recorded here so a (d)/Step-1 builder inherits them; flagged by the c6 pairs, receiving side CTO/m-7):**
+- **③ known-A / RAISE-ONLY direction-invariant NF fixture** (m-7-side; flagged owed by m-2 `c6-fix-m-2` + m-6 §2, §J-ratified) — a B-pick / B-absorb over a known-A category ⇒ **raised to A + `gate_category` recorded**, never silently orchestrator-absorbed. Bound to Step-1 build; not yet in m-7's locked design-of-record (which locked before the ③ fold).
+- **⑤ ODB model-name egress fixture** (m-3-authored (a)/(b)/(c) set, `c6-fix-m-3` §13; receiving on m-7 / this §C4) — a model-name in the **exempt ODB `model_name` slot** (decision-⑤ carve-out) passes the egress scan; a model-name **outside** the carve-out is scanned/blocked. Bound to Step-1 build.
--- a/master/domains/m-1-trust-identity/design/2026-06-28-trust-identity-design.md	2026-07-02 19:49:04
+++ b/master/domains/m-1-trust-identity/design/2026-06-28-trust-identity-design.md	2026-07-02 21:04:55
@@ -19,7 +19,7 @@
 
 ## 0.b readiness-fix-c1 fold (CTO-arbitrated `submit()`/`send()` write-path seam)
 
-Bounded MUST-before-Step-1-PLAN fix (`readiness-fix-c1`, build-readiness Cluster 1). Shared c1 collision → **CTO-arbitrated + VP-co-signed** (VP `approve` 20260630-223751; canonical seam `ARCHITECTURE.md:58-65`). **Scope = the write-path seam only; nothing else in c1 reopens.** m-1's fold (this doc):
+Bounded MUST-before-Step-1-PLAN fix (`readiness-fix-c1`, build-readiness Cluster 1). Shared c1 collision → **CTO-arbitrated + VP-co-signed** (VP `approve` 20260630-223751; canonical seam = ARCHITECTURE.md bullet **"Write path — reconciled seam (`readiness-fix-c1`)"**). **Scope = the write-path seam only; nothing else in c1 reopens.** m-1's fold (this doc):
 - **§5 `submit()` rewritten** to: resolve/stamp → **pre-append validation, candidate held in-courier, nothing persisted** [(a) m-2 form-validation → (b) **m-2 cross-relay lineage gate** vs the persisted `accepted`-graph → (c) **reserved Step-2** m-3 observe hook] → **one atomic `accepted` append** on pass / **one terminal `rejected` evidenced record** on fail. **No persisted `submitted` limbo.**
 - **Added the m-2 lineage-gate hook-point** (2b) that the readiness review found absent from m-1's ordering — validated in-courier via `persisted ∪ {candidate}`, no limbo append; the system-filled `PARENT` makes it confusion-resistant (D4; D5 residual, §0.c).
 - **Reserved the m-3 observe gate to Step-2** (2c) — **Step-1 = store + form + lineage**; resolves 1b (m-3 builds at Step-2 per ROADMAP).
@@ -35,6 +35,19 @@
 
 The honest-fallback labels this doc already carried (I2→confusion-resistant §4; I3→self_reported §4) are the correct pattern — the review endorsed them; the sweep extends that same discipline to the remaining strong-vocab lines. **No c1 reopen; no mechanism/design-lock change.** Every strong-vocab occurrence is classified in the `c5-claim-sweep-m-1` closure survivor list. Where a line below reads "by-construction / forgery-robust / sole-writer," read it through this note (D4 + D5, D3-shelved) even where local space kept the original mechanism phrasing.
 
+## 0.d c6-fix fold (re-review cleanup — 5 doc-only findings; VP-concurred CONDITIONAL-GO)
+
+Per `c6-fix-m-1` (`master/DESIGN-REREVIEW-2026-07-02.md` §5, ◆VP-ratified where marked). **Doc-only consistency folds INTO the locked doc — no mechanism change, lock invariants unchanged.** Preserves the four sanctioned by-construction claims (of which m-1's is **observer-selected control** — DI-5 read-vantage / `parent_picker`, untouched), the swept confusion-resistant/D5 vocabulary, and the byte-exact `{accepted, rejected, held}` enum.
+1. **m-1-F3 (blocker, ◆) — DI-1 dispositioned.** DI-1's wrap-grade form (no-lane-write / separate-uid / store-ACL) is a **D3-shelved wrap invariant**; **Step-1 I1 rides the m-7 interface guardrail** (store path absent from every seat tool surface, confusion-resistant) → §4 DI-1 line + §4 proof + §9 Step-1 row + §12 AC#1 (D4 guardrail fixture + D3 wrap-spike criterion).
+2. **m-1-F12 (C) — third persisted outcome `held`.** §5 `submit()`: engine-internal fault during commit → m-7 C4.1/NF-S6 two-axis disposition (authority-bearing → `held`, not `rejected`); outcome set is byte-exact `{accepted, rejected, held}`.
+3. **m-1-F4 (M) — DI-2 tests re-cut to the D4 grain.** §12 #3 + §13 #1 + §13.rev2 #1: wrap-grade "sibling must fail to read" → **tool-surface negative fixture** (no verb/path/credential-read in the seat surface); the isolation probe is D3-shelved.
+4. **m-1-F5 (M) — transport rationale rebuilt post-sweep.** §3: A ≻ B because A's minted-credential is the **non-re-cut foundation** the D3 wrap upgrade + SO_PEERCRED/mTLS path build on (not for a stronger Step-1 claim — both are confusion-resistant).
+5. **m-1-F6 (M) — ordered strike of stale `m-3 observe`.** §13.2 + §7: annotated `[Step-2+, reserved hook 2c]` + §5(2c)/CQ-1(a) cross-ref.
+
+**rev1 (implementer must-revise `210016`, both folded):** F3 also cleared the residual **§4 I1 "Mechanism:" line** (:95) — the wrap-grade store-ACL wording now sits only inside an explicit "D3-shelved wrap clause (NOT the Step-1 mechanism)"; the Step-1 mechanism is the m-7 guardrail. F5 also cleared the stale **§10 Option-B rejected-alternative** (:186) — B is now rejected as *not upgrade-bearing* (would need a re-cut), not on a by-construction contrast.
+
+Verified CTO-already-applied (not redone): F7 (§4 "three invariants"), F9 (no surviving `unspoofable`), F10 (ARCHITECTURE DI-1..DI-5 anchors), F1/F2 (README §Owns relabel).
+
 ## 1. Thesis (reframed to Step-1 confusion-resistant; see §0.c)
 
 A single **trusted courier (the conductor)** is the **sole *governed* writer** of an append-only relay store and stamps every relay's sender identity (`FROM`) from the **connection the relay arrived on**, never from a lane-supplied field. This yields **confusion-resistant SEAT identity (D4):** a *confused* agent has no tool to write the trusted field, so through the governed interface there is nothing to forge. **D5 residual:** a malicious same-uid process under attach can write the store directly or spoof — out of Step-1 scope; **"by-construction" is the shelved-wrap milestone (D3)** (§0.c), not the Step-1 claim.
@@ -66,14 +79,14 @@
 
 ## 3. Transport selection (operator grill) — Option A: minted per-seat credential
 
-The operator selected **Option A** (grill, §10): the conductor mints a per-seat credential at `mint_seat`, carried on an **isolated per-seat connection** a sibling lane cannot read; the store is writable only through the conductor's governed interface. This is the minimal backend that **licenses the confusion-resistant claim** (proved §4 for a *confused* agent; "forgery-robust by construction" is the shelved-wrap D3 milestone, §0.c; D5 residual: same-uid attach out of scope). Rejected: Option B (operator-attested — only confusion-resistant) and Option C (OS-peer/mTLS/SPIFFE now — premature; it is the *standalone* backend and the non-re-cut upgrade target). See §9.
+The operator selected **Option A** (grill, §10): the conductor mints a per-seat credential at `mint_seat`, carried on an **isolated per-seat connection** a sibling lane cannot read; the store is writable only through the conductor's governed interface. This is the minimal backend that **licenses the confusion-resistant claim** (proved §4 for a *confused* agent; "forgery-robust by construction" is the shelved-wrap D3 milestone, §0.c; D5 residual: same-uid attach out of scope). **Why A ≻ B under the sweep (m-1-F5):** at Step-1 *both* A and B are only **confusion-resistant** (the sweep voided A's old "licenses forgery-robust by-construction" edge), so A is chosen not for a stronger Step-1 claim but because the **minted-credential mechanism is the non-re-cut foundation the structural upgrade builds on** — the **D3 wrap upgrade** and the **SO_PEERCRED/mTLS/SPIFFE** path both extend A's minted-credential/isolated-connection substrate behind an unchanged `lookup(connection)→seat`, whereas B (operator-attested) is a dead-end that must be re-cut to reach structural strength. Rejected: Option B (operator-attested — confusion-resistant *and* not upgrade-bearing) and Option C (OS-peer/mTLS/SPIFFE now — premature; it is the *standalone* backend and the non-re-cut upgrade target). See §9.
 
-## 4. The two invariants — PROVED for Option A (the load-bearing section; VP hard requirement)
+## 4. The three invariants — PROVED for Option A (the load-bearing section; VP hard requirement)
 
 **Threat model:** a **malicious lane** (a compromised/adversarial seat process) that can reach the conductor directly. Goal: produce a relay attributed to a seat it does not own (forge `FROM`), or write the store directly.
 
-Four **deployment invariants** the transport must realize (the concrete realization is the orchestrator-flagged "fork-2 infra call" — named §11):
-- **DI-1:** lane processes have **no write access** to the store path.
+Five **deployment invariants** the transport must realize (the concrete realization is the orchestrator-flagged "fork-2 infra call" — named §11):
+- **DI-1 (dispositioned — c6-fix m-1-F3, ◆VP):** the *wrap-grade* form ("lane processes have no write access to the store path" via separate uid / store-dir ACL) is a **D3-shelved wrap invariant**, NOT a Step-1 realization. **Step-1 I1 rides the m-7 interface guardrail:** the store path is **absent from every seat's tool surface** — a *confused* seat has no tool/verb/path that reaches the store, so the only ingress is `submit()` (D4/confusion-resistant). D5 residual: a malicious same-uid process can still write the store directly — accepted Step-1 risk (§0.c). Step-1 I1 is **guardrail-borne, not a lane-excluding store ACL**.
 - **DI-2:** each seat's connection + credential is **OS-isolated** — a sibling lane cannot read another seat's credential or connection endpoint.
 - **DI-3:** credentials are **unguessable** (sufficient entropy).
 - **DI-4:** `mint_seat` is **conductor-only** — no agent-facing self-binding.
@@ -81,8 +94,8 @@
 
 ### I1 — governed-write / store-isolation (confusion-resistant; D5 residual, §0.c)
 **Claim:** the conductor is the only writer of the store (relay records, INDEX, mailboxes).
-**Mechanism:** the store lives on a path owned by the conductor with no write permission for lane processes (separate uid / `0700` / no path handle).
-**Proof:** a lane attempting a direct write is denied by the OS permission check (DI-1). The only ingress to the store is `submit()`, which stamps `FROM`. ∴ every record was written by the conductor. **QED, conditional on DI-1.**
+**Mechanism (Step-1, guardrail-borne — c6-fix m-1-F3):** the store path is **absent from every seat's tool surface** (the m-7 interface guardrail) — a *confused* seat has no tool/verb/path that reaches the store, so the only ingress is `submit()` (D4/confusion-resistant). **D3-shelved wrap clause (NOT the Step-1 mechanism):** the wrap realization — store on a conductor-owned path with no write permission for lane processes (separate uid / `0700` / no path handle) — is the D3 future backend that would make this structural; **D5 residual:** a malicious same-uid direct store write is an accepted Step-1 risk (§0.c).
+**Proof (Step-1, guardrail-borne):** a *confused* lane has **no tool/verb/path** in its surface that reaches the store (the m-7 interface guardrail, DI-1 Step-1 form). The only ingress to the store is `submit()`, which stamps `FROM`. ∴ every record a confused lane can produce was written by the conductor (confusion-resistant, D4). **D5 residual:** a malicious same-uid direct store write is out of Step-1 scope (§0.c); the OS-ACL/separate-uid version that would close it is the D3-shelved wrap invariant. **QED for the D4 claim, conditional on the m-7 guardrail.**
 
 ### I2 — channel-isolation (the confusion-resistance core; D5 residual, §0.c)
 **Claim:** `FROM := lookup(connection)` maps each `submit` to exactly one seat; no lane can cause a submit to be attributed to a seat it does not own.
@@ -109,10 +122,10 @@
 ## 5. API surface — `submit` / `project` / `read` / `mint_seat`
 
 - **`mint_seat(seat_address) → connection_credential`** — conductor-only, spawn-time. Provisions the seat's mailbox and binds `seat ↔ {connection, credential}` in the private binding table. **Attestation is folded here** (no public `attest()`; reconcile-resolved). [SMTP: account/mailbox provisioning + SASL credential]
-- **`submit(relay_body) over seat_connection → relay_id`** — the **sole *governed* write path** (confusion-resistant; D5 residual: same-uid direct store write out of scope, §0.c). A relay is **lane-proposed** (a candidate held **in-courier** — nothing persisted) and becomes **accepted** (stamped + appended + delivered) only after the pre-append gates pass; on a gate failure it is recorded as a **distinct terminal `rejected`** evidenced record — **there is no persisted `submitted` limbo state.** *(Reconciled to the CTO-arbitrated `readiness-fix-c1` seam, VP-approved 20260630-222352; byte-consistent with m-2 `send()` and `ARCHITECTURE.md:58-65`. Supersedes the prior "submitted → pre-flights → accepted, reads identically to m-2 §4" framing.)*
+- **`submit(relay_body) over seat_connection → relay_id`** — the **sole *governed* write path** (confusion-resistant; D5 residual: same-uid direct store write out of scope, §0.c). A relay is **lane-proposed** (a candidate held **in-courier** — nothing persisted) and becomes **accepted** (stamped + appended + delivered) only after the pre-append gates pass; on a gate failure it is recorded as a **distinct terminal `rejected`** evidenced record — **there is no persisted `submitted` limbo state.** *(Reconciled to the CTO-arbitrated `readiness-fix-c1` seam, VP-approved 20260630-222352; byte-consistent with m-2 `send()` and the ARCHITECTURE.md bullet **"Write path — reconciled seam (`readiness-fix-c1`)"**. Supersedes the prior "submitted → pre-flights → accepted, reads identically to m-2 §4" framing.)*
   1. **resolve + stamp:** resolve seat from the connection — **reject if unbound** (I2 reject-unbound) → stamp `FROM`/`ROLE` from the binding; fill the system envelope fields `relay_id`, `DISPATCH_ID` continuity, `timestamp`, `schema_version`, `certification = null`; `PARENT` via **parent_picker** (validate the agent's pick ∈ the conductor-derived candidate set, else default to the woken-on relay); validate `TO`/`CC` ∈ the minted address space (**recipient_picker**);
   2. **pre-append validation — nothing persisted, candidate held in-courier:** (a) m-2 **form-validation** (required-set, enums, seat-scope); then (b) m-2 **cross-relay lineage gate** — the lineage engine validates the candidate's edges against the **persisted `accepted`-graph** with the candidate supplied **in-courier** (it walks `persisted ∪ {candidate}`; it does **not** require append-as-`submitted` first — the limbo state is unnecessary). The system-filled `PARENT` (step 1) keeps this gate **confusion-resistant** (a confused lane has no tool to supply a false parent; D5 residual §0.c); then (c) **[RESERVED — lands Step-2] m-3 observe-as-send** done-predicate + the DI-5/I3 observed-value fill from the conductor's out-of-lane read — an **additive** hook-point **named in the ordering but NOT required for Step-1**. A failing gate **bounces** naming the failing field/edge; nothing enters the store as deliverable. **"Authority blocks before dispatch" is preserved**: an authority-bearing candidate is non-consumable until the lineage gate passes (protocol.md:397/407);
-  3. **atomic commit (conductor-owned):** on pass — **one atomic append of an `accepted` record** + INDEX row (sole *governed* writer — I1; D5 §0.c) and **deliver/project** into the `TO`/`CC` mailboxes (MDA), a single transaction that does not re-enter lane-controlled state between validation and append (TOCTOU closed — §13.2). On form/lineage fail — **one atomic append of a distinct terminal `rejected` evidenced record** + bounce with the failing field/edge (preserves m-2's audit-of-attempts as a *terminal* record while honoring m-1's "no un-gated deliverable state" — **not** a persisted `submitted` limbo). [SMTP: authenticated submission + milter policy + delivery]
+  3. **atomic commit (conductor-owned):** on pass — **one atomic append of an `accepted` record** + INDEX row (sole *governed* writer — I1; D5 §0.c) and **deliver/project** into the `TO`/`CC` mailboxes (MDA), a single transaction that does not re-enter lane-controlled state between validation and append (TOCTOU closed — §13.2). On form/lineage fail — **one atomic append of a distinct terminal `rejected` evidenced record** + bounce with the failing field/edge (preserves m-2's audit-of-attempts as a *terminal* record while honoring m-1's "no un-gated deliverable state" — **not** a persisted `submitted` limbo). **On an engine-internal fault *during* commit (m-1-F12) — the trusted machinery ran-and-broke / a trusted check could not run, distinct from a form/lineage validation failure — disposition follows m-7 C4.1 / NF-S6 (VP-amended two-axis):** an **authority-bearing** record becomes **`held`** (escalated, not `rejected`); a non-authority record is `rejected` or author-returned with the fault edge named. So the persisted-outcome set is the byte-exact **`{accepted, rejected, held}`** — `held` is m-7's disposition, surfaced here for `submit()` completeness (not an m-1 mechanism change). [SMTP: authenticated submission + milter policy + delivery]
 - **Step-1 build boundary (named):** **Step-1 = store + form + lineage** (I1 governed-write store + m-2 form-validation + m-2 cross-relay lineage gate). The m-3 **observe-as-send** gate (2c) + the DI-5/I3 observed-field fill are the **reserved additive Step-2 hook** — m-3 builds at Step-2 (`ROADMAP.md:57-70`; `ARCHITECTURE.md:120`), so "store + form + lineage without m-3" is now explicitly derivable. **I3 evidence (confusion-resistant; DI-5 observer-selected, §0.c) lands with m-3 at Step-2**; Step-1 records carry no observe gate. This is additive and non-destructive: the reserved hook changes no Step-1 field, verb, or on-disk shape.
 - **`project(seat_connection) → [relays addressed to caller]`** — read/inbox-poll, scoped to the caller's stamped identity (reads only its own mailbox = its `TO`/`CC`). Reuses the claude-code poll-and-inject pattern. [SMTP: IMAP/POP retrieval]
 - **`read(relay_id) → {record, schema_version}`** / store queries — serves canonical immutable records for the cross-relay **lineage engine** (m-2) and the **read-time migrator** (m-2). The store is **migration-agnostic**: it preserves + serves versioned records; it does not migrate. [SMTP: message fetch]
@@ -124,7 +137,7 @@
 **System-field contract** (m-2 *declares* the slot, m-1 *fills/certifies*; B1/B2/B3 acked in `c1-design-m2-m1-coord`):
 - **`system_only`** (stamped, never lane-fillable): `FROM` (seat; SPF-analog), `ROLE` (derived by the conductor from the seat binding — same source as `FROM`, so for a **governed** write they are consistent by the engine's own derivation, not by lane cooperation; the upstream protocol's ROLE/FROM tripwire dissolves; **D5 residual:** a malicious same-uid direct store write could author a mismatched record — out of scope, §0.c), `relay_id`, `DISPATCH_ID`, `timestamp`, `schema_version`, `certification` (null-reserved; DKIM-analog, §8).
 - **`parent_picker`:** `PARENT_DISPATCH_ID` — the conductor supplies the candidate parent set (the woken-on relay / active dispatch lineage) + default; the agent may only **select within it**; a free-typed parent is rejected. The candidate set being **conductor-derived** is an **observer-selected control property** (the set is chosen conductor-side; the lane can only *select within* it and cannot reach the selection) → this keeps m-2's lineage engine **confusion-resistant** (a confused lane has no tool to supply a false parent). **D5 residual:** a malicious same-uid direct store write is out of scope; the structural version is D3-shelved (§0.c).
-  - **Sharpening-D (rev2, m-4 half):** an **accepted routing relay** — m-2's separate seat-stamped routing record, per the resolved m-4 shape (*routing decision = a separate seat-stamped relay, not a dispatch header*) — **may appear in the conductor-derived candidate set** (`parent_picker` / reference) for the dispatch relay it routes. This reference is **bookkeeping**: it lets a dispatch point at the routing relay that justified it, with the same conductor-derived (unspoofable) candidate-set guarantee. It does **NOT** make model-choice a trust-bearing gate input — the model behind a seat stays **payload, never a gate field** (pillar :33). The candidate set carries *relay references*, not model identity; trust remains keyed to the stamped seat `FROM`, not to which model occupies the seat.
+  - **Sharpening-D (rev2, m-4 half):** an **accepted routing relay** — m-2's separate seat-stamped routing record, per the resolved m-4 shape (*routing decision = a separate seat-stamped relay, not a dispatch header*) — **may appear in the conductor-derived candidate set** (`parent_picker` / reference) for the dispatch relay it routes. This reference is **bookkeeping**: it lets a dispatch point at the routing relay that justified it, with the same conductor-derived candidate-set guarantee (observer-selected control property, mirroring the `parent_picker` candidate set above; confusion-resistant, **D5 residual** §0.c — a malicious same-uid direct store write could seed the accepted-graph the candidate set derives from). It does **NOT** make model-choice a trust-bearing gate input — the model behind a seat stays **payload, never a gate field** (pillar :33). The candidate set carries *relay references*, not model identity; trust remains keyed to the stamped seat `FROM`, not to which model occupies the seat.
 - **`recipient_picker`:** `TO`/`CC` — sender-selected, courier-validated against the **system-owned minted address space** (no delivery to a non-seat). Choosing recipients is not identity-forgery, so these are **not** `system_only`; `FROM`/`ROLE` stay `system_only`. The address SPACE itself is system-owned (m-1 mints seats); m-6 projects the addressing graph.
 - **Operator / human address (rev2, m-6 G3):** the special `operator` address (a charter-named address) is a **first-class entry in the system-owned minted address space**, with three properties:
   - (i) **mailbox / projection** — `operator` has a mailbox the human reads via `project()` / the operator-relay channel (so gate→email + Owner Decision Brief delivery, m-6, have a real delivery target);
@@ -135,7 +148,7 @@
 
 Principle: **m-1 owns the store + stamp MECHANISM; the sibling domain owns the field SLOT (m-2) or the consuming mechanism.** No primitive without a named consumer.
 - **m-2 (co-foundational, joint lock):** the system-field set (§6) + the governed-write append-only store its form-validator + lineage engine read. **System-filled `PARENT` is the convergence point** (confusion-resistant lineage; D5 §0.c). Lock together.
-- **m-3 (observe/evidence):** the **store-isolation boundary** — m-3's probe-from-outside-the-lane reuses the **same** isolation the stamper needs (DI-2) — plus the **`submit()` pre-send gate hook** where m-3's done-predicate runs before append. m-3's observed-value fields (`ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`, `achieved_evidence`) are system/observed-filled at `submit`.
+- **m-3 (observe/evidence):** the **store-isolation boundary** — m-3's probe-from-outside-the-lane reuses the **same** isolation the stamper needs (DI-2) — plus the **`submit()` pre-send gate hook** where m-3's done-predicate runs before append **[Step-2+, reserved hook 2c — NOT in the Step-1 ordering; §5 step (2c), CQ-1(a)]**. m-3's observed-value fields (`ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`, `achieved_evidence`) are system/observed-filled at the **Step-2 observe hook** (2c), not in Step-1.
 - **m-4 (routing):** the stamped store as the **write target for routing records** (a separate seat-stamped routing relay); a routing record's `FROM` = the router seat's stamped identity → dispatch authority is confusion-resistant (D5 §0.c). Routing relays ride `submit()` like any relay, and an accepted routing relay may sit in the dispatch's `parent_picker` candidate set as **bookkeeping** (Sharpening-D, §6) — the model behind a seat stays payload, never a gate input.
 - **m-6 (human surface/scheduler):** the **addressing graph (`TO`/`CC`) + seat-address space**, **including the first-class `operator` address** (mailbox/projection + `recipient_picker` member + operator-relay-stamped `FROM`, §6 G3); inbox = projection of the addressing graph; the mailbox / `project()` substrate backs m-6's operator inbox + gate→email buckets + Owner Decision Brief delivery.
 
@@ -147,7 +160,7 @@
 
 ## 9. Non-re-cut path (Step-1 → standalone)
 
-- **Step-1** ("automated operator-relay", rides existing runtimes): I1 via store-dir ACL excluding lanes (DI-1); I2 via minted-credential over an OS-isolated per-seat connection (DI-2/3/4 realized by OS process+file isolation — each seat a separate process; `C_seat` via that process's private env/stdin/fd; store + binding table in the conductor's address space / conductor-owned files); **I3/observe-integrity via DI-5** — the conductor reads each lane's workspace/git/process state from **outside** the lane (e.g. `git -C <lane-workspace> …`, a read-only mount, or an out-of-lane process probe), so observed fields are conductor-read, not lane-reported. DI-5 is part of the same deferred fork-2 infra call as DI-2 and is realized by the same out-of-lane vantage the isolation already needs.
+- **Step-1** ("automated operator-relay", rides existing runtimes): I1 **guardrail-borne** — the store path is absent from every seat's tool surface (the m-7 interface guardrail; DI-1 Step-1 form, confusion-resistant), **not** a lane-excluding store-dir ACL (that is the D3-shelved wrap form, §0.c); I2 via minted-credential over an OS-isolated per-seat connection (DI-2/3/4 realized by OS process+file isolation — each seat a separate process; `C_seat` via that process's private env/stdin/fd; store + binding table in the conductor's address space / conductor-owned files); **I3/observe-integrity via DI-5** — the conductor reads each lane's workspace/git/process state from **outside** the lane (e.g. `git -C <lane-workspace> …`, a read-only mount, or an out-of-lane process probe), so observed fields are conductor-read, not lane-reported. DI-5 is part of the same deferred fork-2 infra call as DI-2 and is realized by the same out-of-lane vantage the isolation already needs.
 - **Standalone** (later): I1 = store in the conductor's runtime; I2 attestation backend upgrades minted-token → **SO_PEERCRED / mTLS / SPIFFE-SVID**.
 - **Why no re-cut:** all writes already route through `submit()` over a per-seat connection; only `lookup(connection) → seat`'s **attestation backend** changes. The four verbs, the system-field contract (§6), the on-disk shape, and the stamp semantics are invariant. Strict extension ("build B so C extends the same bones").
 
@@ -172,7 +185,7 @@
 - identity != authority — m-1 owns identity + store only — source design-team (route to m-4/orchestrator)
 
 Rejected alternatives:
-- Option B (operator-attested) — only confusion-resistant, not by-construction; forfeits the frank differentiator (retained ONLY as the honest fallback label if a deployment cannot meet DI-2)
+- Option B (operator-attested) — rejected because it is **not upgrade-bearing**: at Step-1 both A and B are only confusion-resistant (the sweep voided A's old by-construction edge), but B would require a **re-cut** to reach structural strength, whereas A's minted-credential is the **non-re-cut foundation** the D3 wrap upgrade + SO_PEERCRED/mTLS/SPIFFE path build on (m-1-F5). B is retained ONLY as the honest fallback label if a deployment cannot meet DI-2.
 - Option C (OS-peer/mTLS/SPIFFE now) — premature; it is the standalone backend + the non-re-cut upgrade target, not the initial release Step-1
 - public attest() call — folded into mint_seat; submit() fails on unbound instead
 - payload display-FROM / two-FROM — DMARC trap; one FROM only
@@ -194,9 +207,9 @@
 
 ## 12. Acceptance criteria draft (for the eventual PLAN, not this phase)
 
-1. `submit()` is the only write path; a direct lane write to the store path fails (I1; DI-1 verified E2).
+1. `submit()` is the only write path. **D4 guardrail fixture (Step-1, m-1-F3):** no seat tool/verb/path in the seat's tool surface reaches the store path — enumerate the surface and assert the store is absent (I1 guardrail-borne, confusion-resistant). **D3-scoped wrap-spike criterion (shelved, not a Step-1 gate):** under a future wrap backend (separate uid / store-dir ACL), a direct lane write to the store path fails — verified only if/when the D3 wrap upgrade is built (§0.c). The Step-1 D5 residual (malicious same-uid direct write) is accepted, not tested-closed.
 2. `FROM`/`ROLE` stamped from the connection; a payload `FROM` is ignored; `submit` on an unbound connection is rejected.
-3. A sibling lane cannot read another seat's credential/connection (DI-2 verified E2).
+3. **D4 tool-surface negative fixture (m-1-F4):** no verb/path/credential-read for another seat's credential or connection is present in a seat's tool surface — a *confused* sibling has no tool to read it (DI-2, confusion-resistant). (The wrap-grade "sibling process *must fail* to read" isolation probe is the **D3-shelved** form; D5 residual: a malicious same-uid read is accepted, §0.c.)
 3b. Observed-value fields are conductor-read from outside the lane (DI-5/I3, observer-selected); a *confused* lane has no tool to substitute or intercept an observed value (verified E2; **D5 residual:** malicious same-uid source/path manipulation out of scope, §0.c); where DI-5 is unmet, the evidence is labeled "self-reported," not observe-by-construction.
 4. `PARENT` is `parent_picker` (agent pick validated ∈ the conductor candidate set; free-typed parent rejected); system-filled `PARENT` keeps the lineage gate **confusion-resistant** (D5 §0.c).
 5. `TO`/`CC` are `recipient_picker` (validated ∈ the minted address space); `FROM` stays `system_only`.
@@ -208,9 +221,8 @@
 
 m-1.implementer returned `DESIGN_REVIEW_VERDICT: approve`. Four refinements were raised as PLAN/E2 acceptance
 detail, explicitly **without reopening DESIGN** — recorded here so PLAN inherits them:
-1. **DI-2 tested as an isolation property, not asserted** — at PLAN/E2, a sibling lane must *fail* to read or
-   use another seat's credential/connection (the **confusion-resistant** claim becomes an E2 fact only here; "by-construction" stays the D3-shelved wrap milestone, §0.c).
-2. **`submit()` as one atomic conductor-owned commit** — seat resolution → envelope fill → m-3 observe →
+1. **DI-2 tested at the D4 tool-surface grain, not asserted (re-cut m-1-F4)** — at PLAN/E2, the fixture is a **tool-surface negative**: no verb/path/credential-read for another seat's credential/connection exists in a seat's surface (a confused sibling has no tool). The wrap-grade "sibling process must *fail* to read" isolation probe is the **D3-shelved** form, not a Step-1 gate; the **confusion-resistant** claim becomes an E2 fact at the D4 grain here (§0.c).
+2. **`submit()` as one atomic conductor-owned commit** — seat resolution → envelope fill → m-3 observe **[Step-2+, reserved hook 2c; NOT in the Step-1 ordering — §5 step (2c), CQ-1(a)]** →
    append → INDEX write → mailbox projection must not re-enter lane-controlled state between validation and
    append (close the TOCTOU at PLAN; not a DESIGN change to the four verbs).
 3. **Seat-credential lifecycle** — define generation/epoch, rotation, and revocation so a remint/recycle
@@ -234,9 +246,11 @@
 
 m-1.implementer returned `DESIGN_REVIEW_VERDICT: approve` on rev2 (no rev1 regression, no DESIGN reopen). Four
 further PLAN/E2 carry-forwards, recorded so PLAN inherits them:
-1. **DI-2 *and* DI-5 each tested as independent properties** — DI-2 as sibling credential/connection
-   confidentiality; DI-5 as the conductor's out-of-lane read-vantage. Both verified separately at E2 (identity +
-   evidence — Step-1 **confusion-resistant**; the by-construction versions are D3-shelved wrap milestones, §0.c).
+1. **DI-2 *and* DI-5 each tested as independent properties, at the D4 grain (re-cut m-1-F4)** — DI-2 as a
+   **tool-surface negative** (no sibling-credential read verb/path in the seat surface, confusion-resistant); DI-5 as the
+   conductor's out-of-lane read-vantage (observer-selected). Both verified separately at E2 at the D4 grain (identity +
+   evidence — Step-1 **confusion-resistant**; the wrap-grade isolation-probe and the by-construction versions are
+   D3-shelved milestones, §0.c).
 2. **Operator-relay channel isolation** (extends the DI family to the special-address path): lane processes must
    have **no write access to the operator-relay channel**, so a *confused* lane can never `submit` as `operator`
    (confusion-resistant). **D5 residual:** under attach (same uid) a *malicious* process can reach the
--- a/master/domains/m-1-trust-identity/README.md	2026-07-02 19:49:04
+++ b/master/domains/m-1-trust-identity/README.md	2026-07-02 21:11:07
@@ -4,11 +4,14 @@
 **Cycle c1 scope:** AUDIT + DESIGN of the Step-1 trust/identity foundation. Terminal = design-lock. No impl.
 
 ## Owns (the trusted core / TCB)
-The conductor's trusted core: the **sole-writer append-only relay store**, the **seat-stamper**
-(channel-stamped `FROM` = identity by construction), **store isolation**, and the placement of the inline
+The conductor's trusted core: the **sole *governed* writer** of the append-only relay store (through the
+governed `submit()` path; confusion-resistant; D5 accepted), the **seat-stamper** (channel-stamped `FROM` =
+conductor-stamped identity — confusion-resistant, GRILL-LOCK D4; forgery-robust-by-construction = the
+D3-shelved wrap milestone, not the Step-1 claim), **store isolation**, and the placement of the inline
 lint/form gate pre-delivery. frank's differentiator neither prior-art harness makes: a stamping courier that
-is the SOLE writer of the relay store and stamps `FROM` from the connection a relay arrived on, never a
-lane-supplied field.
+is the sole *governed* writer of the relay store (through the governed submit path; D5 direct-store residual
+accepted) and stamps `FROM` from the connection a relay arrived on, never a lane-supplied field. See the
+design doc §0.c D3/D4/D5 ladder + GRILL-LOCK D4-D5 for the full scoping.
 
 ## Boundaries
 - **Co-foundational with m-2** (Forms & Determinism): m-2 *declares* the system-owned envelope fields; m-1's
@@ -71,6 +74,18 @@
     reviewer ran a wider survivor scan than my census → no unclassified blocker). Consolidated completion reported
     (`SITREP-planner-20260702-141500`). Remaining is the CTO's ledger folds (c5 status ledger + integrated build-carry
     ledger). Holding — no self-advance to PLAN.
+  - **c6-fix (re-review cleanup, CONDITIONAL-GO; last gate before Step-1 PLAN):** applied the 5 doc-only findings
+    (`DESIGN-planner-20260702-210000`, fold-log §0.d) — **F3** DI-1 dispositioned (D3-shelved wrap; Step-1 I1 =
+    m-7-guardrail-borne; AC#1 = D4 fixture + D3 spike); **F12** third outcome `held` (m-7 C4.1/NF-S6; enum byte-exact
+    `{accepted, rejected, held}`); **F4** DI-2 tests re-cut to D4 tool-surface grain; **F5** transport A≻B rebuilt
+    (non-re-cut foundation); **F6** stale `m-3 observe` struck → `[Step-2+ hook 2c]`. Verified CTO-applied F7/F9/F10/F1-2.
+    No mechanism/lock change; four sanctioned by-construction claims + D5 vocab preserved. m-1.implementer r1 returned
+    **must-revise** (2 catches: §4 I1 "Mechanism:" line still wrap-grade; §10 Option-B stale by-construction contrast) —
+    both **folded rev1** (`DESIGN-planner-20260702-211200`; §4:95 Mechanism → guardrail-borne + D3-shelved clause;
+    §10:186 Option-B → not-upgrade-bearing). **m-1.implementer re-review = approve** (`211300`, no must-revise
+    remaining). **c6-fix-m-1 COMPLETE + pair-approved**; completion reported (`SITREP-planner-20260702-211800`).
+    Remaining is the orchestrator's c6 verify sweep + close (VP co-sign) across the 7 domain returns → then
+    (e) Step-1 PLAN opens. Holding — no self-advance.
 
 ## Layout
 - `audit/` — AUDIT artifacts.  `design/` — DESIGN docs + grill locks.
--- a/master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md	2026-07-02 19:49:04
+++ b/master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md	2026-07-02 20:59:46
@@ -54,6 +54,7 @@
   type:          string | enum | bool | int | address | address_list
                  | row_array | id_ref | evidence_ref | object | text
   enum_set:      named-enum | inline-list     # closed option set (header/agent_enum_pick)
+  gate_referenceable: bool                    # DEFAULT false (m-2-F5). true iff a required_when/visible_when predicate may key on this field's id (§5). Model-identity fields (chosen_model, §17.3), computed labels (record_integrity/surface_intent, §17.6), and the X- namespace are false-by-declaration — this is the first-class data the R2 gate-grammar KEEP + the X- schema-consumer KEEP invariants read (a predicate naming a false-flagged field is rejected by the grammar, §5)
   seat_scope:    map<seat-pattern, allowed-options>   # the fill-time-authority matrix
   required_when: <predicate>                  # §5 bounded vocabulary
   visible_when:  <predicate>                  # §5 (seat/phase/slot gating)
@@ -83,7 +84,7 @@
 atoms:  phase_in:[…]            ceremony_tier_gte:<tier>     authority_in:[…]
         seat_is:<pattern>       role_in:[…]                  slot_in:[…]        # RESERVED atom — shape only, no Step-1 values
         record_kind_in:[…]      scan_result_in:[…]           claims_action:<bool>
-        field:<id> <op> <value>          # dependent-required (op ∈ ==,in,present); <id> ∈ gate-referenceable allowlist — model-identity fields (selected_model, model_*) EXCLUDED (R2, readiness-fix-c4)
+        field:<id> <op> <value>          # dependent-required (op ∈ ==,in,present); <id> must have `gate_referenceable: true` (the first-class FieldSpec flag, §4, m-2-F5) — every model-identity-marked field is `gate_referenceable: false` (the live routing model field is `chosen_model`, §17.3; exclusion is the field's own flag, not a `model_*` name pattern) (R2, readiness-fix-c4)
         any_row:<array>.<field> <op> <value>   # bounded existential over a row_array's rows (decidable, finite; no nesting/loops); <field> obeys the same gate-referenceable allowlist (readiness-fix-c4)
         layer_present:<layer>            # closed context atom; <layer> ∈ {store, form, lineage, observe}; courier-evaluated from which fill-layers/writers are bound at runtime (CQ-1 step-gate)
 combinators: all_of | any_of | not        # bounded boolean; no loops, no calls
@@ -95,7 +96,7 @@
 - **Observe-owned set (the m-3 write-allowlist, §12 m-3 / §17.6):** `ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`, `achieved_evidence`, the `*_RESULT` set, per-field `evidence_integrity`, `record_integrity`, `executable_claim_results`, `egress_scan_result`, `degradation_notes`, `deviated_observed`, and the per-phase done-predicate observe-gate hook.
 - **Hard guardrail (m-1):** the step-gate applies **ONLY** to observe-owned fields. Every `system_only` / lineage / form-owned required field stays **fully required in Step-1** — the step-gate is not a lever to relax non-observe requireds. Note `EVIDENCE_TARGET` is `agent_enum_pick` **intent** (not observe-owned) — it stays Step-1-required; only the observed `achieved_evidence`/`*_RESULT` are step-gated. R2-safe: `layer_present` references build-layers, never model.
 
-**R2 grammar-enforcement — model is not gate-referenceable (readiness-fix-c4).** The generic `field:<id>` atom's `<id>` is drawn from a **gate-referenceable-field allowlist that structurally excludes every model-identity field** (`selected_model` and any `model_*`): model-identity fields are **not gate-referenceable**, so no `required_when`/`visible_when` predicate can key on the model — even through the generic dependent-required atom. This makes R2 (*model ≠ gate input*, m-1 coord) **structurally impossible to violate in the schema the tool reads**, not merely asserted — **[c5 KEEP: a trusted-engine gate-grammar invariant; no predicate atom references the model field, so the property holds regardless of seat behavior, not a malicious-containment claim]**: the absence of a `model_*` atom (§17.3 `:289`) was necessary but not sufficient while `<id>` could name `selected_model`; the exclusion closes that hole. Model stays payload/bookkeeping; trust stays keyed to the stamped seat `FROM`. The R2-safe deviation trigger keys on the **planner-declared** `declared_deviated` bit (`agent_enum_pick`, §17.3), never on model identity and never on the system-computed observe-side `deviated_observed`. The aggregate routing trigger `any(routing_assignments.declared_deviated == true)` is expressed by the bounded `any_row:` existential atom above — decidable over the finite `routing_assignments` rows, so a single deviating row in an altitude-B fan-out cannot escape the required justification.
+**R2 grammar-enforcement — model is not gate-referenceable (readiness-fix-c4).** The generic `field:<id>` atom's `<id>` is drawn from a **gate-referenceable-field allowlist that structurally excludes every model-identity-marked field** (the live routing model field is `chosen_model`, §17.3, flagged non-gate-referenceable — exclusion is by the model-identity mark, **not** a `model_*` name pattern, which `chosen_model` would not even match): model-identity fields are **not gate-referenceable**, so no `required_when`/`visible_when` predicate can key on the model — even through the generic dependent-required atom. This makes R2 (*model ≠ gate input*, m-1 coord) **structurally impossible to violate in the schema the tool reads**, not merely asserted — **[c5 KEEP: a trusted-engine gate-grammar invariant; no predicate atom references the model field, so the property holds regardless of seat behavior, not a malicious-containment claim]**: the absence of a `model_*` atom (§17.3 `:289`) was necessary but not sufficient while `<id>` could name a model-identity field (e.g. `chosen_model`); the exclusion (by model-identity mark) closes that hole. Model stays payload/bookkeeping; trust stays keyed to the stamped seat `FROM`. The R2-safe deviation trigger keys on the **planner-declared** `declared_deviated` bit (`agent_enum_pick`, §17.3), never on model identity and never on the system-computed observe-side `deviated_observed`. The aggregate routing trigger `any(routing_assignments.declared_deviated == true)` is expressed by the bounded `any_row:` existential atom above — decidable over the finite `routing_assignments` rows, so a single deviating row in an altitude-B fan-out cannot escape the required justification.
 
 **`slot_in` is a RESERVED atom for Step 1** (rev2 #5, VP-approved): the atom *shape* exists, but **no concrete archetype/slot enum values are defined** and **no required-when predicate may branch on a concrete slot** in Step 1. m-5 (Workflows & Archetypes) owns the tag-space + concrete slot semantics in a later cycle — same reserved-seam pattern as `certification` / Merkle hash-chain. (`(…, slot)` stays in the evaluator context so m-5 extends the same bones.)
 
@@ -116,13 +117,13 @@
 
 ## 8. Legacy strictness (Q2) — strict, form-only submit
 
-The form/courier is the **only** path to create a relay. Legacy upstream-protocol hand-authored markdown is **read-only history** (importable/projectable), **never submittable**. Any hand-authored-markdown submission path would reopen every prose hole the typed envelope dissolves (fences, bare tokens, ambiguous rows); strictness is what *permits the deletion* of the ~33 prose checks (§10a) rather than retaining them for a compat window.
+The form/courier is the only **governed/sanctioned** path to create a relay (confusion-resistant; **D5 residual:** a same-uid shell-bearing seat can write a store record directly — m-1 TCB scope, out of the schema layer). Legacy upstream-protocol hand-authored markdown is **read-only history** (importable/projectable), **never submittable**. Any hand-authored-markdown submission path would reopen every prose hole the typed envelope dissolves (fences, bare tokens, ambiguous rows); strictness is what *permits the deletion* of the ~33 prose checks (§10a) rather than retaining them for a compat window.
 
 ## 9. Schema versioning + migration framework (Q3) — ship the seam, zero migrators
 
 Public-release intent makes the schema a **public interface**, so versioning is first-class even though there is nothing to migrate yet:
 - `schema_version` — system-owned envelope field, stamped immutably on every record.
-- **Published compatibility contract:** *additive* (new optional header / new `agent_enum_pick` value / new `X-` field) = backward-compatible → **minor** bump, old readers ignore-unknown; *breaking* (remove/rename a field, narrow an enum, change ownership or required-when) = **major** bump → requires a registered migrator.
+- **Published compatibility contract:** *additive* (new optional header / new `agent_enum_pick` value / new `X-` field) = backward-compatible → **minor** bump, old readers ignore-unknown; *breaking* (remove/rename a field, narrow an enum, **widen a system-owned closed enum whose mechanical consumers switch exhaustively on it — e.g. `delivery_state` `{accepted, rejected, held}`, byte-exact-locked at three**, change ownership or required-when) = **major** bump → requires a registered migrator. (Widening an *open* `agent_enum_pick` judgment enum stays additive/minor above — the ignore-unknown rule holds only where no consumer switches exhaustively.)
 - **Migration mechanism:** records are immutable (m-1 append-only store), so migration is applied at **read/projection time** by a registry of version→version migrators (a chain upgrading an old record to the current view). the initial release ships the registry + chain-application + version-negotiation **with zero migrators registered**; the first breaking change registers the first migrator into an existing seam.
 - **Submission is always current-version** (strict form-submit; courier renders the current form), so version skew only appears on **read** of historical records. Parked lanes wake to the current form.
 - Cross-cutting: m-1 persists `schema_version` per record + serves records for read-time migration; m-6 scheduler wakes lanes to the current form.
@@ -183,8 +184,8 @@
 
 - **m-1 (co-foundational; confirmed by m-1 coord 173516):** `FROM` and `ROLE` are `owner:system` / `system_only`, stamped from one certified connection (a seat address *is* `owner.role`, so ROLE derives from the same binding as FROM — a confused agent has no seat-fillable ROLE to make them mismatch; confusion-resistant, D5 residual). `relay_id`, `DISPATCH_ID`, `timestamp` courier-assigned. `PARENT_DISPATCH_ID` = `parent_picker` (courier supplies the conductor-derived candidate set + default = the woken-on relay; agent may only select within it; a free-typed parent is rejected) — **this conductor-derived candidate set is what strengthens the lineage engine to more strongly confusion-resistant** (a confused agent has no tool to free-type a false parent; **D5 residual** on a shell-bearing seat, m-1 TCB scope). `schema_version` stamped immutably (m-1 preserves + serves versioned records; **migration is m-2's read-side concern — m-1 does not migrate**). `certification` **null-reserved** in every record. The **address space** (minted seat addresses / graph topology) is `owner:system`; but a relay's **`TO`/`CC` values** are `recipient_picker` (sender-selected, courier-validated ∈ the minted address space, then delivered) — choosing recipients is not identity-forgery, so TO/CC are **not** `system_only` (only `FROM`/`ROLE` are). Lock together.
 - **m-3 (observe/evidence):** `ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT` (`owner:system`, `fill_constraints: observed_value`), `EVIDENCE_TARGET` (`agent_enum_pick`, intent), `achieved_evidence` (`owner:system`, observed), per-phase **done-predicate** hook (a `required_when`/observe-gate input), executable-claim ref, egress/content-safety result. **target>achieved auto-flag** = a system-computed `*_RESULT`. All `observed_value` fields are **conductor-read from OUTSIDE the lane** (m-1 DI-5; the locked observe-as-send mechanism) — **[c5 KEEP: observer-selected control property — the observed lane cannot reach the conductor-side selection]** observer-captured, not lane-reported — and carry an **integrity label `evidence_integrity ∈ {observed, self_reported}`** (`observed` when DI-5 holds; `self_reported` honest-fallback where a Step-1 deployment cannot read out-of-lane). The m-3 observe hook is a **positive write-allowlist**: it may write ONLY this closed observed/computed set + a pass/fail veto — no `system_only`/identity/envelope write, no delivery effect (m-1 coord R3 + FYI). The ODB `completed_proof` (§17.2) inherits the `evidence_integrity` label. **Record-level rollup `record_integrity {observed|self_reported|mixed}`** (`owner:system`, `computed_result`; conductor rollup, m-3 `:107-112`) is declared in §17.6 — a pure function of the per-field stamps, ODB/m-6/audit-consumed, **not** gate-referenceable **[c5 KEEP: R2 gate-grammar invariant]**, **not lane-writable** (conductor-computed; no lane write tool — confusion-resistant, D5 residual) — so the whole m-3 write-allowlist resolves to an m-2-declared set (m-1 requirement + canonical-iff-consumed).
-- **m-4 (routing-record) — Q-C RESOLVED (rev2): a SEPARATE seat-stamped routing relay, not a dispatch header** (model is payload, never a work-relay gate input — pillar :33). Record fields **mirror m-4's locked record `:200-210`** (full spec §17.3; readiness-fix-c4 4b): `routing_assignments` (`row_array`, `seat_scoped_enum` planner/orch-planner — altitude-B; **per row:** `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned|resolved`, model-identity ⇒ non-gate-referenceable), `pin_mode`, `declared_deviated:bool`, `seat_archetype` (opaque, m-5 c3 — carries the archetype default posture, m-5 `:103`), `authority_ceiling` (resolved-at-spawn, recorded — F2)) — **no standalone posture field** (CTO `001537`; posture rides `seat_archetype`), `capability_prior_snapshot` (`owner:system`, computed/observed, replay-complete), `justified_deviation` (`free_text`, `required_when` `any(routing_assignments.declared_deviated == true)` — keyed on the **planner-declared** deviation bit (`agent_enum_pick`), never `selected_model` and never the computed observe-side `deviated_observed`; readiness-fix-c4), `deviation_reason_code` (`agent_enum_pick`, **same required-when grain** as `justified_deviation`), `routing_record_kind` (own named-enum, DESIGN_RECORD_KIND *shape* not values), `constraints` + `template_ref` (**reserved-shape**, no Step-1 values), `outcome_feedback_ref` (`owner:system`, `id_ref`, null-reserved a later release). The work dispatch references the *accepted* routing relay via `parent_picker` (m-1 owns that candidate-set half). m-4 owns the record contract; m-2 mirrors it — m-4 confirms the mirror.
-- **m-6 (human-gate/email) — rev2 refined (G1/G2/Q-B; full specs §17.1–17.2):** `HUMAN_GATE_REQUIRED` (`fill_constraints: monotonic` **hybrid** — system floor, agent RAISE-able within `[floor, MAX]`, floor = MAX across raisers; §3 G1). `human_gate_reason` = **config-sourced closed enum** `gate_category` (byte-exact §J2 set, **m-6-owned A/B map**: A e.g. `merge_to_protected`/`irreversible_write`/`residual_risk_acceptance`/`live_verify_skip`/`ceremony_downgrade`/`authz_security`/`product_semantics`/`scope_expansion`; B `merge_feature_to_feature`/`routing`/`sequencing`/`scope_within_bounds`; `other`→A fail-safe — full spec §17.1) — drives the gate→email bucket split mechanically (canonical-iff-consumed). `delivery_state` {accepted|rejected|held} + `failing_edge` = `owner:system` computed slots (so the bucket-D lint-bounce email states why; `rejected` = the readiness-fix-c1 terminal-bounce record; `held` = m-7 §6 internal-fault-on-authority + CQ-2 fail-closed, drives the bucket-A ODB/park lane — §17.1). The **Owner Decision Brief** ~10-field sub-schema (§17.2): agent-scripts 7 + retyped `subject_ref`(id_ref)/`completed_proof`(evidence_ref from m-3, never free-text)/`choices`(row_array) + `gate_category` + null-reserved `decision_deadline`/`on_timeout`. m-2 declares the slots; m-6 owns the bucket-projection + scheduler mechanism. **Conductor-derived `surface_intent {progress|review_checkpoint|advisory|result}`** (`owner:system`, `computed_result`, **non-gate-bearing records only**) is declared in §17.6 — m-5 owns the vocabulary, m-6 binds `(posture × surface_intent)` (`m-6 …:113-116`); gate-bearing records carry **no** `surface_intent` (bind off the locked `gate_category`/HUMAN_GATE, C2.4).
+- **m-4 (routing-record) — Q-C RESOLVED (rev2): a SEPARATE seat-stamped routing relay, not a dispatch header** (model is payload, never a work-relay gate input — pillar :33). Record fields **mirror m-4's locked record `:200-210`** (full spec §17.3; readiness-fix-c4 4b): `routing_assignments` (`row_array`, `seat_scoped_enum` planner/orch-planner — altitude-B; **per row:** `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned|resolved`, model-identity ⇒ non-gate-referenceable), `pin_mode`, `declared_deviated:bool`, `seat_archetype` (opaque, m-5 c3 — carries the archetype default posture, m-5 `:103`), `authority_ceiling` (resolved-at-spawn, recorded — F2)) — **no standalone posture field** (CTO `001537`; posture rides `seat_archetype`), `capability_prior_snapshot` (`owner:system`, computed/observed, replay-complete), `justified_deviation` (`free_text`, `required_when` `any(routing_assignments.declared_deviated == true)` — keyed on the **planner-declared** deviation bit (`agent_enum_pick`), never `selected_model` and never the computed observe-side `deviated_observed`; readiness-fix-c4), `deviation_reason_code` (`agent_enum_pick`, **same required-when grain** as `justified_deviation`), `routing_record_kind` (own named-enum, DESIGN_RECORD_KIND *shape* not values), `constraints` (**reserved-shape**, no Step-1 values) + `template_ref` (**reserved-shape**; set when spawned from a template, null otherwise — m-4 `:209`), `outcome_feedback_ref` (`owner:system`, `id_ref`, null-reserved a later release). The work dispatch references the *accepted* routing relay via `parent_picker` (m-1 owns that candidate-set half). m-4 owns the record contract; m-2 mirrors it — m-4 confirms the mirror.
+- **m-6 (human-gate/email) — rev2 refined (G1/G2/Q-B; full specs §17.1–17.2):** `HUMAN_GATE_REQUIRED` (`fill_constraints: monotonic` **hybrid** — system floor, agent RAISE-able within `[floor, MAX]`, floor = MAX across raisers; §3 G1). `gate_category` (canonical id, m-2-F10 — the retired `human_gate_reason` alias folded away) = **config-sourced closed enum** (byte-exact §J2 set, **m-6-owned A/B map**: A e.g. `merge_to_protected`/`irreversible_write`/`residual_risk_acceptance`/`live_verify_skip`/`ceremony_downgrade`/`authz_security`/`product_semantics`/`scope_expansion`; B `merge_feature_to_feature`/`routing`/`sequencing`/`scope_within_bounds`; `other`→A fail-safe — full spec §17.1) — drives the gate→email bucket split mechanically (canonical-iff-consumed). `delivery_state` {accepted|rejected|held} + `failing_edge` = `owner:system` computed slots (so the bucket-D lint-bounce email states why; `rejected` = the readiness-fix-c1 terminal-bounce record; `held` = m-7 §6 internal-fault-on-authority + CQ-2 fail-closed, drives the bucket-A ODB/park lane — §17.1). The **Owner Decision Brief** ~10-field sub-schema (§17.2): agent-scripts 7 + retyped `subject_ref`(id_ref)/`completed_proof`(evidence_ref from m-3, never free-text)/`choices`(row_array) + `gate_category` + null-reserved `decision_deadline`/`on_timeout`. m-2 declares the slots; m-6 owns the bucket-projection + scheduler mechanism. **Conductor-derived `surface_intent {progress|review_checkpoint|advisory|result}`** (`owner:system`, `computed_result`, **non-gate-bearing records only**) is declared in §17.6 — m-5 owns the vocabulary, m-6 binds `(posture × surface_intent)` (`m-6 …:113-116`); gate-bearing records carry **no** `surface_intent` (bind off the locked `gate_category`/HUMAN_GATE, C2.4).
 - **m-5 (workflows/archetypes; parameterizes m-2, review-on-CC):** `slot_in` reserved atom (§5; tag-space m-5-owned, c3); `seat_archetype` + `authority_ceiling` ride the **F2 per-assignment home** in `routing_assignments` (§17.3, opaque/m-5-valued c3); **posture rides `seat_archetype`, not a standalone field** (CTO `001537`; m-5 c3 lock `:142`). `posture` + `surface_intent` value-enums are **m-5-owned delivery vocabulary LOCKED at c3** (§17.6, m-6-bound) — *not* opaque/reserved (their values are fixed, unlike `seat_archetype`/`slot_in`). m-2 declares the shape/vocabulary; m-5 owns the values (no tag-value micro-fold).
 
 ## 13. GRILL_LOCK
@@ -231,14 +232,15 @@
 7. `ROLE` is `owner: system`, stamped from the same channel as `FROM`; any header rendering of `ROLE` is a read-only projection (rev1).
 8. Authority-bearing records (design-doc PLAN locks, delegated dispatch/merge grants, substantive IMPL reports, merge claims, orchestrator-authority relays) are non-deliverable/non-consumable until the lineage gate passes — the **pre-append form+lineage validation with one atomic `accepted` commit / terminal `rejected` on fail** (§4 steps 4–5, readiness-fix-c1 seam; no persisted `submitted` limbo); a form/lineage failure produces a terminal `rejected` record + bounce and is never delivered.
 9. `HUMAN_GATE_REQUIRED` is agent-**RAISE-able** (hybrid `monotonic`): a seat may raise the gate to or above the system floor but never lower it; the floor is the MAX across all raisers (rev2 G1). Negative fixture: a lower-than-floor write is rejected.
-10. `human_gate_reason`/`gate_category` is a **closed enum** that mechanically drives the gate→email bucket split (A human-only vs B orchestrator-absorbed); `delivery_state`+`failing_edge` are readable computed slots for the bucket-D bounce (rev2 G2).
+10. `gate_category` (canonical id, m-2-F10) is a **closed enum** that mechanically drives the gate→email bucket split (A human-only vs B orchestrator-absorbed); `delivery_state`+`failing_edge` are readable computed slots for the bucket-D bounce (rev2 G2).
 11. The Owner Decision Brief sub-schema (§17.2) is encoded with `completed_proof` as an m-3 `evidence_ref` (never agent free-text) and `choices` as a `row_array` the operator reply selects within (rev2 Q-B).
 12. The routing decision is a **separate seat-stamped routing relay** (§17.3) with `routing_assignments` seat-scoped to planner/orch-planner; model never appears in a work relay's authority header (rev2 Q-C). `slot_in` is a reserved atom with no Step-1 values (rev2 #5).
 13. **(readiness-fix-c1)** The write path has **no persisted `submitted` state**: form+lineage run **pre-append** over `persisted ∪ {candidate}` with the candidate in-courier; on pass exactly **one atomic `accepted` append** commits; on fail a **terminal `rejected` record** + bounce is produced. **Negative fixture:** no code path can leave a `submitted`-but-non-`accepted`/non-`rejected` record in the store; a lineage-failing authority record is never delivered/consumed and lands as terminal `rejected`. Step-1 requires **no** m-3 observe gate (observe is a reserved Step-2 hook).
-14. **(readiness-fix-c4)** R2 is **grammar-enforced**: the `field:<id>` predicate atom cannot reference a model-identity field, and `justified_deviation` keys on the **planner-declared** `declared_deviated` bit (`agent_enum_pick`, aggregated over `routing_assignments`), not `selected_model` and not the computed observe-side `deviated_observed`. **Negative fixture:** a required-when/visible-when predicate that names `selected_model` (or any `model_*`) is **rejected by the schema grammar** (not merely unused); model is provably not a gate input on any code path **[c5 KEEP: R2 gate-grammar invariant + negative fixture — a control-flow property of the predicate grammar, holds regardless of seat behavior]**. **Declare/observe split fixture (m-4 seam):** `declared_deviated` (declared, gate-side) and `deviated_observed` (system-computed, non-gate-referenced) are distinct fields, so the `(declared=false, observed=true)` silent-deviation veto has two independent bits to compare.
-15. **(readiness-fix-c4 4b+4c)** m-2's routing FieldSpec (§17.3/§12) **mirrors m-4's locked record `:200-210`** — full per-row `routing_assignments` fields + `deviation_reason_code` (same required-when grain as `justified_deviation`) + reserved-shape `constraints`/`template_ref`; and the conductor-computed fields `record_integrity`/`surface_intent` have **declared homes** (§17.6), so the m-3 write-allowlist resolves to an m-2-declared set and every consumed field has a registry entry (canonical-iff-consumed). **Posture is NOT a standalone field** — it rides the recorded `seat_archetype` (CTO arbitration `001537`; m-5 c3 lock `:142`); m-2 declares only the m-5-owned posture value-enum m-6 binds. **Fixtures:** no consumed field lacks a declaration; reserved/opaque fields (`seat_archetype`, `authority_ceiling`, `constraints`, `template_ref`, `slot_in`) carry **no Step-1 values**; no computed label (`record_integrity`/`surface_intent`) is gate-referenceable; no standalone posture field exists on `routing_assignments`. Confirmed by m-4 (record mirror) + m-3/m-5/m-6 (their field homes).
-16. **(c4-cq-gateconfig — m-7 lock CQs)** `delivery_state` is the closed `{accepted, rejected, held}` outcome token set (canonical `rejected`, no `bounced`, no `submitted` limbo); **`held`** has an m-2 home **narrowed to internal-fault-on-authority (m-7 §6) + the CQ-2 fail-closed disposition** — ordinary human-gate parking is `accepted`, not `held`. **Fixtures:** (a) an authority record whose trusted-side check throws/times-out ⇒ `held` (not accepted, not rejected), resolved by a subsequent record; (b) an `authority_class` record with `record_integrity = self_reported` ⇒ `held`/escalated, never silently delivered, while a lower-stakes `self_reported` record still delivers; (c) `held` is consumed only by gate/escalation/scheduler/operator-resolution machinery — no downstream work authority acts on it (c1 accepted-only invariant preserved for work consumers); (d) `gate_category` tokens are byte-exact with `ARCHITECTURE §J2` (`ceremony_downgrade`, `live_verify_skip`, …), config-sourced, m-6-owned map. The A-floor composes as a MAX-raiser (CQ-3, no new mechanism). Neither `held`, `authority_class`, nor the A-floor introduces a model gate input (R2). Closed jointly: CQ-4 (m-2/m-3/m-6), CQ-4b (m-2/m-3/m-4/m-6).
+14. **(readiness-fix-c4)** R2 is **grammar-enforced**: the `field:<id>` predicate atom cannot reference a model-identity field, and `justified_deviation` keys on the **planner-declared** `declared_deviated` bit (`agent_enum_pick`, aggregated over `routing_assignments`), not `selected_model` and not the computed observe-side `deviated_observed`. **Negative fixture:** a required-when/visible-when predicate that names `chosen_model` (or any field flagged model-identity) is **rejected by the schema grammar** (not merely unused); model is provably not a gate input on any code path **[c5 KEEP: R2 gate-grammar invariant + negative fixture — a control-flow property of the predicate grammar, holds regardless of seat behavior]**. **Declare/observe split fixture (m-4 seam):** `declared_deviated` (declared, gate-side) and `deviated_observed` (system-computed, non-gate-referenced) are distinct fields, so the `(declared=false, observed=true)` silent-deviation veto has two independent bits to compare.
+15. **(readiness-fix-c4 4b+4c)** m-2's routing FieldSpec (§17.3/§12) **mirrors m-4's locked record `:200-210`** — full per-row `routing_assignments` fields + `deviation_reason_code` (same required-when grain as `justified_deviation`) + reserved-shape `constraints`/`template_ref`; and the conductor-computed fields `record_integrity`/`surface_intent` have **declared homes** (§17.6), so the m-3 write-allowlist resolves to an m-2-declared set and every consumed field has a registry entry (canonical-iff-consumed). **Posture is NOT a standalone field** — it rides the recorded `seat_archetype` (CTO arbitration `001537`; m-5 c3 lock `:142`); m-2 declares only the m-5-owned posture value-enum m-6 binds. **Fixtures:** no consumed field lacks a declaration; the m-2 **registry declares no value-enums** for the reserved/opaque fields (`seat_archetype`, `authority_ceiling`, `constraints`, `template_ref`, `slot_in`) — their values are m-5-config-/template-sourced and **opaque to the schema gate** (so a template-spawned or F2 record MAY carry Step-1 record values for `template_ref`/`seat_archetype`/`authority_ceiling` per GL-4/m-5 §9/m-7 S11, while the schema still enumerates none — the no-micro-fold boundary; `constraints`/`slot_in` remain reserved-shape with no Step-1 values); no computed label (`record_integrity`/`surface_intent`) is gate-referenceable; no standalone posture field exists on `routing_assignments`. Confirmed by m-4 (record mirror) + m-3/m-5/m-6 (their field homes).
+16. **(c4-cq-gateconfig — m-7 lock CQs)** `delivery_state` is the closed `{accepted, rejected, held}` outcome token set (canonical `rejected`, no `bounced`, no `submitted` limbo); **`held`** has an m-2 home **narrowed to internal-fault-on-authority (m-7 §6) + the CQ-2 fail-closed disposition** — ordinary human-gate parking is `accepted`, not `held`. **Fixtures:** (a) an authority record whose trusted-side check throws/times-out ⇒ `held` (not accepted, not rejected), resolved by a subsequent record; (b) an `authority_class` record with `record_integrity ∈ {self_reported, mixed}` ⇒ `held`/escalated, never silently delivered (both the `self_reported` and the *partially*-unverified `mixed` case hold — CQ-2 canonical pessimistic reduction), while a non-authority `self_reported`/`mixed` record still delivers; (c) `held` is consumed only by gate/escalation/scheduler/operator-resolution machinery — no downstream work authority acts on it (c1 accepted-only invariant preserved for work consumers); (d) `gate_category` tokens are byte-exact with `ARCHITECTURE §J2` (`ceremony_downgrade`, `live_verify_skip`, …), config-sourced, m-6-owned map. The A-floor composes as a MAX-raiser (CQ-3, no new mechanism). Neither `held`, `authority_class`, nor the A-floor introduces a model gate input (R2). Closed jointly: CQ-4 (m-2/m-3/m-6), CQ-4b (m-2/m-3/m-4/m-6).
 17. **(c4-cq-m1 — CQ-1 step-gate, m-2 co-sign of m-1 resolution (a))** An observe-owned field's required predicate is `all_of(required_when, layer_present:observe)` (§5), so in Step-1 (no observe writer) it is **not required** — a form never demands an unfillable field. **Fixtures:** (a) a Step-1 form with a done-predicate/`achieved_evidence`/`*_RESULT` requirement does **not** block on those observe-owned fields (step-gated off); (b) every `system_only`/lineage/form-owned required field **still blocks** in Step-1 (guardrail — the step-gate touches only observe-owned fields); (c) `EVIDENCE_TARGET` (intent) stays Step-1-required; (d) `layer_present` never references model (R2). Closed jointly: CQ-1 (m-1 planner+impl + m-2 co-sign+impl).
+18. **(c6 m-2-F1 — operator decision ③ RAISE-ONLY)** `gate_category` is typed **monotonic-toward-A** (§17.1): the `known_A` detector (m-6/§J2 config) is a system floor MAX-raiser; a known-A record's affordance is constrained to `[floor, A]`. **Negative fixture:** an agent B-pick over a known-A record is **raised to A** and `gate_category_raised = true` is recorded — never delivered as B (no A→B de-classification on any code path). The m-7 known-A/RAISE-ONLY NF fixture is a **registered owed artifact** gated to Step-1 build. No model gate input (R2).
 
 ## 15. Open questions (non-blocking)
 
@@ -246,9 +248,9 @@
 - Q-B — **RESOLVED** (rev2, consumer review): the Owner Decision Brief is the ~10-field sub-schema encoded in §17.2 (agent-scripts 7 + 3 retyped to the frank substrate + `gate_category` + 2 null-reserved scheduler fields).
 - Q-C — **RESOLVED** (rev2, m-4 consumer review): the routing decision is a **separate seat-stamped routing relay** (§17.3), not a dispatch header; model stays payload.
 - Q-D — **RESERVED** (rev2 #5, VP-approved): `slot_in` keeps its atom shape but defines no Step-1 values and no predicate branches on a concrete slot; m-5 owns the tag-space in a later cycle.
-- Q-E — **RESOLVED (CQ-4, c4-cq-gateconfig):** `delivery_state` = `{accepted, rejected, held}`, canonical spelling `rejected` (legacy `bounced` unified away); `held` declared as the new consumed token's home (§17.1). The Step-2 observe-bounce token (share `rejected` vs distinct) is m-3's call in the CQ-4 joint settle; m-2's slot admits either. `ARCHITECTURE.md:287` `bounced`→`rejected` alignment now backed by the settled vocabulary.
+- Q-E — **RESOLVED (CQ-4, c4-cq-gateconfig):** `delivery_state` = `{accepted, rejected, held}`, canonical spelling `rejected` (legacy `bounced` unified away); `held` declared as the new consumed token's home (§17.1). The Step-2 observe-bounce token is **SETTLED**: m-3's CQ-4 settle **shares `rejected`** (m-3 §2 `:31`), so the token set is **closed at three** `{accepted, rejected, held}` — no fourth/distinct token. `ARCHITECTURE.md:287` `bounced`→`rejected` alignment is backed by the settled vocabulary.
 - Q-F — **ACCEPTED, tracked pre-Step-1-PLAN SHOULD (CTO `001537`; not a Cluster-4 blocker):** promote the remaining 7 m-3 observe fields (`achieved_evidence`, `evidence_integrity`, `*_RESULT` set, `executable_claim_results`, `egress_scan_result`, `degradation_notes`, `routing_decision` observed profile) from §12 prose into formal §17.3-style registry rows, so the §13 PLAN fixtures are machine-checkable. Bounded/additive/non-gate; runs after Cluster 4 closes, before Step-1 PLAN opens.
-- **Operator-judgment (rev2 — surface for the joint lock, HUMAN_GATE_REQUIRED:no on this dispatch):** (i) ODB `on_timeout` default when a human gate times out — **proposed default `hold_and_resummon`** (never auto-approve/auto-proceed; the safe default for an authority gate); (ii) `gate_category` enum membership — the A/B split is per the dispatch; where a *novel* category lands is operator-ratifiable at the lock.
+- **Operator-judgment (rev2 → RATIFIED, m-2-F9):** (i) ODB `on_timeout` default when a human gate times out — **RATIFIED `hold_and_resummon`** (never auto-approve/auto-proceed; the safe default for an authority gate) → `ARCHITECTURE §J1`; (ii) `gate_category` enum membership + the RAISE-ONLY / known-A direction (decision ③) — **RATIFIED** → `ARCHITECTURE §J2` (A/B map + `other`→A fail-safe) and §J1 (RAISE-ONLY). Both are stable ARCH section anchors (same convention as Q-A..Q-E); no longer "proposed."
 
 ## 16. DESIGN-REVIEW fold log
 
@@ -267,17 +269,17 @@
 
 ### 17.1 G2 — gate→email bucket drivers (m-6 gap; answers Q4)
 m-2 declares these slots; **m-6 owns the bucket-projection mechanism**:
-- `human_gate_reason` — `type enum`, `owner agent_enum_pick` (the raiser picks the reason), `enum_set = gate_category`. The bucket split is a mechanical consumer ⇒ the driver must be **enum, not free text** (canonical-iff-consumed).
-- `gate_category` — a **config-sourced closed enum** (m-2 declares the *slot* + that the rendered enum is config-sourced; **m-6 owns the A/B default map + protected-branch set** per `ARCHITECTURE §J2`, with the hardcoded `other`→A fail-safe). The **byte-exact §J2 default set** (CQ-3/NF-S8; not authored here — referenced):
+- **Canonical id = `gate_category` (m-2-F10; one field, one id).** `type enum`, `owner agent_enum_pick` (the raiser picks the category), `gate_referenceable: false`, a **config-sourced closed enum** (m-2 declares the *slot* + that the rendered enum is config-sourced; **m-6 owns the A/B default map + protected-branch set** per `ARCHITECTURE §J2`, with the hardcoded `other`→A fail-safe). The record-level field a raiser fills **and** the enum-type name are the **single id `gate_category`** — the id m-6/m-7/§J2/CQ-2/3/4 all consume. The earlier `human_gate_reason` field-id is **retired** (it was a second id for the same semantic — the "pick a reason" role is `gate_category`'s own `agent_enum_pick` fill). The bucket split is a mechanical consumer ⇒ the driver is **enum, not free text** (canonical-iff-consumed). The **byte-exact §J2 default set** (CQ-3/NF-S8; not authored here — referenced):
   - **A (reserved-to-human, bucket A):** `merge_to_protected` · `irreversible_write` · `residual_risk_acceptance` · `live_verify_skip` · `ceremony_downgrade` · `authz_security` · `product_semantics` · `scope_expansion`.
   - **B (orchestrator-absorbed, bucket B):** `merge_feature_to_feature` · `routing` · `sequencing` · `scope_within_bounds`.
   - **Fail-safe:** `other` → A (`human_decision_required`) — hardcoded, non-overridable.
   - (bucket C = CC-only FYI; bucket D = lint-bounce, driven by `delivery_state = rejected` below, not by `gate_category`.) *Tokens are byte-exact with `ARCHITECTURE:96-105` + the m-6 A-floor table; m-2 does not author the policy values.*
+  - **RAISE-ONLY typing (operator decision ③, ARCH §J1; m-2-F1) — monotonic-toward-A, the CQ-3 pattern (§3).** The `gate_category` pick is **not** a free full-set enum: an agent-pick may only **escalate toward A** (more operator oversight), never **de-classify** a known-A-worthy decision down to B. Modeled exactly like the HUMAN_GATE monotonic floor — a **`known_A` system floor** raises the effective category: the **known-A detector** (m-6/§J2-owned config, `owner:system`) is a **named MAX-raiser**; for a record it marks A-worthy, the seat's affordance is constrained to `[floor, A]` (the A members), so a B option is not rendered and, if API-submitted, rejected. The **effective/forced result has a home:** `gate_category` is the effective category (floor-raised); a system-computed `gate_category_raised: bool` (`owner:system`, `computed_result`, `gate_referenceable: false`) records *that* a below-floor (B-over-known-A) pick was raised, for the operator-visible audit. **③ negative fixture (AC18):** a B-pick over a known-A record ⇒ raised to A + `gate_category_raised = true` recorded, never delivered as B. **m-7 fixture owed:** the m-7 known-A/RAISE-ONLY NF fixture is a **registered owed artifact** (ledger entry parallel to §2C, gated to Step-1 build) — flagged to m-7, fixture registration owed (per re-review §5).
 - `delivery_state` — `type enum {accepted, rejected, held}`, `owner system`, `computed_result` (the §4 write-path **terminal-outcome token**, readable; **exactly one outcome per record, no `submitted` limbo** — mirrors m-7's locked outcome structure). **CQ-4 settled (c4-cq-gateconfig; m-2 spelling + homes, joint with m-3/m-6):**
   - **`accepted`** — committed to the store + delivered to recipient/consumers. **An A-gate record that passes form/lineage/gate is `accepted`** and delivered into the A-gate **ODB/park lane** (m-6) — ordinary `HUMAN_GATE_REQUIRED` parking is `accepted`, **not** `held` (m-6 `015800` mapping).
   - **`rejected`** — form/lineage fail ⇒ terminal bounce record; never delivered/consumed (readiness-fix-c1 seam). **Canonical spelling `rejected`; legacy `bounced` is unified away (resolves Q-E).**
-  - **`held`** (new token, CQ-4) — the **HELD field-home** a new consumed token needs (m-7 never invents schema). **Narrowed to the m-7 §6 / m-6 semantics (per m-2.implementer review `020503`):** `held` is the **internal-fault / check-could-not-run** outcome for an **authority-bearing record** — **not accepted** (a crash/timeout is not a yes; K8s `failurePolicy: Fail`), **not rejected** (the check *failed to run*, it did not *say no*) — parked **operator-visible** (m-6 A-surface), resolved by re-run or operator verdict. **Exactly two producers:** (i) a trusted-side check that **throws / times out / reads corrupt data** on an authority record (m-7 §6, `:97-104`); (ii) the **CQ-2 class-conditional fail-closed** disposition — `authority_class == true ∧ record_integrity == self_reported` (§17.6). **`held` is a *terminal outcome for the intake command*** — represented append-only as the candidate + a **held-disposition record referencing it + the `intake_id`**; any re-run / operator verdict is a **subsequent record, never a mutation** of the held record. **NOT the removed pre-gate `submitted` limbo** (the record IS committed; only recipient release is withheld pending resolution).
-  - **m-3 edge (CQ-4, m-3's call):** whether the Step-2 observe-bounce shares `rejected` or is a distinct token is m-3's to settle in this thread; m-2's `delivery_state` slot admits either (the token set is m-2-declared, the observe-side mapping m-3-owned). **m-6 edge (co-signed `015800`):** bucket-D aligns to `rejected`; `held` drives the **bucket-A ODB/park lane**, not bucket-D.
+  - **`held`** (new token, CQ-4) — the **HELD field-home** a new consumed token needs (m-7 never invents schema). **Narrowed to the m-7 §6 / m-6 semantics (per m-2.implementer review `020503`):** `held` is the **internal-fault / check-could-not-run** outcome for an **authority-bearing record** — **not accepted** (a crash/timeout is not a yes; K8s `failurePolicy: Fail`), **not rejected** (the check *failed to run*, it did not *say no*) — parked **operator-visible** (m-6 A-surface), resolved by re-run or operator verdict. **Exactly two producers:** (i) a trusted-side check that **throws / times out / reads corrupt data** on an authority record (m-7 §6, `:97-104`); (ii) the **CQ-2 class-conditional fail-closed** disposition — `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` (§17.6; `mixed` = a partially-unverified authority record, held pessimistically per the CQ-2 canonical). **`held` is a *terminal outcome for the intake command*** — represented append-only as the candidate + a **held-disposition record referencing it + the `intake_id`**; any re-run / operator verdict is a **subsequent record, never a mutation** of the held record. **NOT the removed pre-gate `submitted` limbo** (the record IS committed; only recipient release is withheld pending resolution).
+  - **m-3 edge (CQ-4, SETTLED):** m-3 settled it in the same CQ-4 thread — the Step-2 observe-bounce **shares `rejected`** (the check ran and said no; m-3 §2 `:31`). The `delivery_state` token set is therefore **closed at three** `{accepted, rejected, held}`; a distinct fourth/observe-specific token is **no longer admissible** (it would violate the byte-exact m-7 terminal-enum lock). The observe-side mapping onto `rejected` stays m-3-owned. **m-6 edge (co-signed `015800`):** bucket-D aligns to `rejected`; `held` drives the **bucket-A ODB/park lane**, not bucket-D.
 - `failing_edge` — `type id_ref|string`, `owner system`, `computed_result`, null unless `delivery_state = rejected` (names the failed lineage/form edge so the bucket-D bounce email states why). For `delivery_state = held`, the held-reason is carried by `gate_category` + the escalation/ODB ref, not `failing_edge`.
 
 ### 17.2 Q-B — Owner Decision Brief sub-schema (m-6 owns surface; m-2 encodes slots)
@@ -292,9 +294,10 @@
 | `recommendation` | free_text | free_text | the agent's recommendation + rationale |
 | `choices` | **row_array** | agent_enum_pick | enumerated options the operator reply selects within |
 | `subject_ref` | **id_ref** | system | id_ref into the relay store (the gated subject) |
+| `model_name` | string | **system** | conductor-generated/rendered model-identity string; **confidentiality-exempt** — the *one* typed ODB field decision ⑤ exempts from the confidentiality class (m-3 egress scan keys on `record_kind == ODB ∧ field == model_name ∧ destination == operator`); consumers `[egress_gate, human_surface]`; **non-gate-referenceable** (model-identity, R2) — declared here so canonical-iff-consumed holds (AC15) |
 | `gate_category` | enum | agent_enum_pick | = the §17.1 enum (one category set serves bucket-routing + the brief) |
 | `decision_deadline` | timestamp | system | **null-reserved** (scheduler) |
-| `on_timeout` | enum | system | **null-reserved** (scheduler); default `hold_and_resummon` — operator-judgment (§15) |
+| `on_timeout` | enum | system | **null-reserved** (scheduler); default `hold_and_resummon` — **RATIFIED (ARCH §J1, m-2-F9)** |
 
 Provenance (`FROM`/`PARENT`/`DISPATCH_ID`) **auto-projects from the certified envelope** — not a manual ODB field.
 
@@ -303,7 +306,7 @@
 
 | field | type | owner | fill_constraints / note |
 |---|---|---|---|
-| `routing_assignments` | row_array | **seat_scoped_enum** (planner / orch-planner only) | altitude-B fill-time authority. **Per-row fields (mirror m-4 `:203`, readiness-fix-c4 4b):** `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned\|resolved` — **model-identity ⇒ non-gate-referenceable**, R2), `pin_mode` (`pinned\|slot`), `declared_deviated:bool` (per-row; the gate trigger, §17.3/§5), `seat_archetype` (**opaque; concrete values m-5-owned, c3** — also **carries the archetype's default human-mode/posture** as a recorded attribute, m-5 `:103`), `authority_ceiling` (**resolved-at-spawn, recorded per assignment** for replay/audit on both template + hand-authored paths — the F2 fold; opaque, m-5-valued c3). **No standalone posture field** (CTO arbitration `001537`): posture is **not** its own `routing_assignments` field — it **rides `seat_archetype`** (m-5 c3 lock `:142`, "no new m-2 field"); m-6 resolves posture by reading the recorded `seat_archetype`. m-4 owns the record contract (`:203`, unchanged); m-2 mirrors it as FieldSpec |
+| `routing_assignments` | row_array | **seat_scoped_enum** (planner / orch-planner only) | altitude-B fill-time authority. **Per-row fields (mirror m-4 `:203`, readiness-fix-c4 4b):** `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned\|resolved` — **model-identity ⇒ non-gate-referenceable**, R2), `pin_mode` (`pinned\|slot`), `declared_deviated:bool` (per-row; the gate trigger, §17.3/§5), `seat_archetype` (**opaque; concrete values m-5-owned, c3** — also **carries the archetype's default human-mode/posture** as a recorded attribute, m-5 `:103`), `authority_ceiling` (**resolved-at-spawn, recorded per assignment** for replay/audit on both template + hand-authored paths — the F2 fold; opaque, m-5-valued c3). **Propose-vs-stamp per-column ownership (m-5-F1 seam; confused-deputy fix):** within a `routing_assignments` row the planner *proposes* `seat`/`role`/`seat_archetype` from a **fill-time-pruned candidate set** (fill-time authority, §3), and the **conductor *stamps* the resolved `seat_archetype` + `authority_ceiling` per-column** (`fill_constraints: system_only` on those two columns — system ownership *inside* the row) — so a lane cannot self-elevate its own ceiling by authoring the row; the ceiling that denied write authority is conductor-stamped and replay-complete. m-4 (routing record) + m-5 (archetype registry) own the propose/candidate-set + stamp mechanism; m-2 declares the per-column ownership split alongside the §2C per-column FieldSpec work. **No standalone posture field** (CTO arbitration `001537`): posture is **not** its own `routing_assignments` field — it **rides `seat_archetype`** (m-5 c3 lock `:142`, "no new m-2 field"); m-6 resolves posture by reading the recorded `seat_archetype`. m-4 owns the record contract (`:203`, unchanged); m-2 mirrors it as FieldSpec |
 | `capability_prior_snapshot` | object | system | `computed_result`/`observed_value` — the prior in effect at decision time, **replay-complete** |
 | `declared_deviated` | bool | **agent_enum_pick** (planner / orch-planner — **declared**, gate-side) | the routing seat's **declared** deviation bit — a plain boolean it asserts when deviating from the capability-prior floor; **per-row of `routing_assignments`** (altitude-B fan-out grain). The single model-*touching* trigger a gate may read, and it reads a **plain bit — no model comparison enters the gate** (m-4 §2.1 boxed R2 invariant). This is the `required_when` trigger (m-4 owns the contract; retyped per m-4 confirm `231200`) |
 | `deviated_observed` | bool | system | `computed_result` — the conductor **observe-side** bucket-vs-bucket computation (`declared_bucket ≠ rank-1(recommended)` vs `capability_prior_snapshot`); **m-3-owned observed evidence** (`evidence_integrity: observed`). **NOT gate-referenceable** and **not** what `justified_deviation` reads — carried only for replay-completeness + the `(declared=false, observed=true)` **silent-deviation integrity veto** (m-4 §2:106–122). Owned/computed by m-4/m-3; m-2 only carries it |
@@ -311,7 +314,7 @@
 | `deviation_reason_code` | enum | agent_enum_pick | machine-readable reason code paired with the `justified_deviation` narrative (mirror m-4 `:206`, readiness-fix-c4 4b). **required_when** `any(routing_assignments.declared_deviated == true)` — **same grain/treatment** as `justified_deviation`, so the reason code can never be omitted where policy makes it load-bearing |
 | `routing_record_kind` | enum | agent_enum_pick | own named-enum — **DESIGN_RECORD_KIND *shape*, not its values** |
 | `constraints` | object | agent_enum_pick / free_text | budget/latency/privacy — **reserved/forward** (mirror m-4 `:207`; **shape only, no Step-1 values**, non-gate — same reserved-seam pattern as `slot_in`/`certification`) |
-| `template_ref` | id_ref | system | set when spawned from a template (m-4 `:209`; templates land with m-5, **c3**), **null in Step-1**; `lineage_role: none` — reserved-shape declaration |
+| `template_ref` | id_ref | system | **set when spawned from a template; null otherwise** (m-4 `:209` verbatim — the source this row mirrors). Templates land with m-5 (**c3**) and are selectable in the **the initial release/Step-1 release** (GL-4, locked c2), so a template-spawned record **carries a `template_ref` value at Step-1**; `lineage_role: none` — reserved-*shape* declaration (the schema enumerates no values; the value is m-5-template-sourced, opaque to the gate) |
 | `outcome_feedback_ref` | id_ref | system | **null-reserved for a later release**; `lineage_role: none` |
 
 **D-schema-half:** the routing relay carries `accepted`/deliverable lineage semantics (§4) + its record-kind identity; **m-1 owns the `parent_picker` candidate-set half** (the work dispatch references the *accepted* routing relay) — joined via the m-1 COORD thread. Model stays **out** of the work relay's authority header. The work dispatch references the accepted routing relay via a **`routing_ref`** (`lineage_role: routing_ref`) — a **provenance** reference, validated against m-1's conductor-derived `parent_picker` candidate set (so a *confused* agent has no tool to supply a false ref — confusion-resistant; **D5 residual** on a shell-bearing seat, m-1 TCB scope), **traversed by the lineage engine for replay/provenance but never an authority parent and never model-gating**: the §5 predicate vocabulary has **no `model_*` atom** *and* (readiness-fix-c4) the generic `field:<id>` atom **cannot reference a model-identity field** (`selected_model`/`model_*` excluded from the gate-referenceable allowlist), so model cannot be a gate input even structurally — R2 is grammar-enforced, not merely asserted (m-1 coord R2) **[c5 KEEP: R2 gate-grammar invariant]**. Trust stays keyed to the stamped seat `FROM`, not which model occupies the seat.
@@ -323,7 +326,7 @@
 - **Q-C** routing-relay record → §17.3 (resolves §15 Q-C; m-4). AC12.
 - **slot_in** reserved → §5 (resolves §15 Q-D as reserved).
 - **m-1 re-affirm:** the joint envelope/system-field contract re-affirmed for the three rev2 touch-points — G2's `TO`/`CC` address interaction (recipient_picker space), the ODB `completed_proof` = m-3 `evidence_ref` dependency, and the routing-relay `parent_picker` join — via the COORD re-affirm relay. Neither domain re-locks in isolation.
-- **Operator-judgment surfaced (§15):** ODB `on_timeout` default (`hold_and_resummon`); `gate_category` enum membership (novel-category placement).
+- **Operator-judgment RATIFIED (§15, m-2-F9):** ODB `on_timeout` = `hold_and_resummon` (ARCH §J1); `gate_category` membership + RAISE-ONLY direction (ARCH §J2/§J1).
 - Resent for DESIGN-REVIEW (rev2) on the same DESIGN_DOC_ID.
 
 ### 17.5 m-1 rev2 re-affirm — mutual (COORD 165643 ↔ 165702)
@@ -339,7 +342,7 @@
 | field | type / owner | value owner | consumers | note |
 |---|---|---|---|---|
 | `record_integrity` | `enum {observed\|self_reported\|mixed}` · `owner:system` · `computed_result` | **m-3** (conductor rollup, `m-3 …:107-112`) | ODB (§17.2), m-6 delivery (`m-6 …:55,130`), human audit | pure function of the per-field `evidence_integrity` stamps (`observed` iff all expected-observable fields observed; `self_reported` iff none; else `mixed`); record-level rollup on every evidenced record; **not** gate-referenceable **[c5 KEEP: R2 gate-grammar invariant]**; **not lane-writable** (conductor-computed; no lane write tool — confusion-resistant, D5 residual) (m-3 write-allowlist) |
-| `authority_class` | `bool` · `owner:system` · `computed_result` | **m-2** (this schema) computes it; **m-3** consumes for the fail-closed disposition | m-3 observe-as-send gate (CQ-2), lineage engine | **CQ-2 field-home (c4-cq-gateconfig).** `true` iff the record is in a **merge/authority A-gate class** — derived from `record_kind ∈` the locked authority-bearing set (§4/§8/§11: design-doc PLAN locks, delegated dispatch/merge grants, substantive IMPL reports, merge claims, orchestrator-authority relays) **or** `gate_category ∈ A`. The mechanical key m-3 needs for decision-②: **`authority_class == true ∧ record_integrity == self_reported` ⇒ `delivery_state = held`/escalate** (not delivered); lower-stakes/informational records still deliver `self_reported` (honest fallback preserved). Never keyed on model (R2) |
+| `authority_class` | `bool` · `owner:system` · `computed_result` | **m-2** (this schema) computes it; **m-3** consumes for the fail-closed disposition | m-3 observe-as-send gate (CQ-2), lineage engine | **CQ-2 field-home (c4-cq-gateconfig).** `true` iff the record is in a **merge/authority A-gate class** — derived from `record_kind ∈` the locked authority-bearing set (§4/§8/§11: design-doc PLAN locks, delegated dispatch/merge grants, substantive IMPL reports, merge claims, orchestrator-authority relays) **or** `gate_category ∈ A`. The mechanical key m-3 needs for decision-②: **`authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` ⇒ `delivery_state = held`/escalate** (not delivered — `mixed` = a *partially*-unverified authority record, held pessimistically per m-3's reduction rule + the CQ-2 canonical two-axis resolution); non-authority records still deliver `self_reported`/`mixed` (honest fallback preserved). Never keyed on model (R2) |
 | `surface_intent` | `enum {progress\|review_checkpoint\|advisory\|result}` · `owner:system` · `computed_result` (conductor-derived) | **m-5** declares the vocabulary; **m-6** binds (`m-5 …:149`, `m-6 …:113-116`) | m-6 delivery (non-gate lane routing) | present on **non-gate-bearing records only** (`visible_when` = not gate-bearing — gate-bearing records bind off the locked `gate_category`/HUMAN_GATE, never `surface_intent`, C2.4); a conductor-derived label **like `record_integrity`** — "no new *authored* m-2 field", but the registry carries the computed-slot declaration so canonical-iff-consumed holds |
 | `human_mode` / posture | `enum {interactive\|away\|unattended}` — **m-5-owned delivery vocabulary, LOCKED at c3** (not opaque/reserved — the values are fixed) | **m-5** declares (`m-5 …:142`,`:103`) | m-6 delivery (posture × surface_intent bind) | **NOT a `routing_assignments` field** (CTO arbitration `001537`; m-5 c3 lock `:142` = "no new m-2 field"). Posture is the **recorded `seat_archetype` default-human-mode** (`m-5 …:103`) — m-6 resolves it by reading `seat_archetype` on the F2 record. m-2 declares only the value-enum (the delivery vocabulary m-6 binds), **no standalone field** |
 
@@ -364,13 +367,24 @@
 - **4b — mirrored m-2's routing FieldSpec to m-4's locked record `:200-210`.** Enumerated the per-row `routing_assignments` fields (`seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model`, `pin_mode`, `declared_deviated`, `seat_archetype`, `authority_ceiling` — the last two opaque/m-5-valued c3, riding the F2 per-assignment home; **posture is NOT a per-row field — it rides `seat_archetype`**, see the carve-out bullet below); added `deviation_reason_code` (`agent_enum_pick`, **same `any(...declared_deviated)` grain** as `justified_deviation`) and reserved-shape `constraints` + `template_ref` (§17.3, §12). Model-identity fields (`chosen_model`) stay **non-gate-referenceable** (R2 unchanged).
 - **4c — declared the conductor-computed field homes** `record_integrity` (m-3 rollup) + `surface_intent` (m-5 vocabulary / m-6 bind) in **new §17.6** + §12 echoes, so the m-3 write-allowlist resolves to an m-2-declared set (m-1 requirement) + canonical-iff-consumed holds. Both **non-gate-referenceable** computed labels.
 - **Posture carve-out — CTO arbitration `001537` folded (I was corrected; accepted).** My 4b/4c fold declared `human_mode`/posture as a **standalone per-row field** of `routing_assignments`. That was wrong: m-4's record `:203` has no posture, and m-5's c3 lock `:142` says posture "rides the F2 per-assignment record home (**no new m-2 field**)" — i.e. it rides `seat_archetype` (`:103` "default human-mode"). Folded the CTO ruling (option 1, c3-lock-faithful): **removed the standalone posture field** (§17.3); posture now rides the recorded `seat_archetype`, m-6 resolves it by reading that; **kept only the m-5-owned posture value-enum LOCKED at c3** as the delivery vocabulary m-6 binds (§17.6) — relabeled from "opaque/reserved" to "LOCKED at c3" (m-5 Precision-2). No m-4 record change, no `ARCHITECTURE` change, no c3 reopen (this *applies* `:142`).
-- **Boundary:** additive FIELD declarations (consumers already require them) — **not** a tag-value micro-fold; reserved/opaque fields carry shape only, no Step-1 values. **Review edges:** m-4 confirms the record mirror; m-3/m-5/m-6 confirm their computed/opaque field homes (AC15) — all four confirms received (`000930`/`235944`/`235743`/`235500`), the m-4 posture carve-out now CTO-arbitrated.
+- **Boundary:** additive FIELD declarations (consumers already require them) — **not** a tag-value micro-fold; the schema **enumerates no values** for reserved/opaque fields (records may still carry m-5-config-/template-sourced values, e.g. a Step-1 `template_ref`/F2 `seat_archetype`/`authority_ceiling`; `constraints`/`slot_in` stay shape-only). **Review edges:** m-4 confirms the record mirror; m-3/m-5/m-6 confirm their computed/opaque field homes (AC15) — all four confirms received (`000930`/`235944`/`235743`/`235500`), the m-4 posture carve-out now CTO-arbitrated.
 - **Row-parity pre-PLAN SHOULD (tracked, accepted `001537`):** m-3 (allowlist owner) + m-6 want formal §17.3-style rows for the remaining 7 observe fields (`achieved_evidence`, `evidence_integrity`, the `*_RESULT` set, `executable_claim_results`, `egress_scan_result`, `degradation_notes`, the `routing_decision` observed profile) to make the §13 PLAN fixtures machine-checkable. Bounded, additive, non-gate — **runs before Step-1 PLAN opens, after Cluster 4 closes** (not a Cluster-4 blocker; §15 Q-F).
 
 **c4-cq-gateconfig — m-7 design-lock CQ closures (bounded contract-question closure for m-7 Conductor-Core; NOT a c1/c2/c3 reopen).** m-2 leads its half of CQ-2/3/4, confirms CQ-4b; full-pair (m-2.implementer reviews) + cross-domain co-sign (CQ-4: m-2/m-3/m-6; CQ-4b: m-2/m-3/m-4/m-6). CQ-status mapped in the closure relay.
-- **CQ-2 (field-home) — PRODUCED.** Declared **`authority_class`** (`bool`, `owner:system`, `computed_result`; §17.6) as the mechanical key m-3 needs for operator decision ②: `authority_class == true ∧ record_integrity == self_reported ⇒ delivery_state = held`/escalate (not delivered); lower-stakes still deliver `self_reported`. Derived from `record_kind ∈` the locked authority-bearing set (§4/§8/§11) or `gate_category ∈ A`. m-3 owns the fold of its `:63` fail-open→class-conditional-fail-closed text; m-2 owns this home. **Decision-② only** (not the other four decisions).
+- **CQ-2 (field-home) — PRODUCED.** Declared **`authority_class`** (`bool`, `owner:system`, `computed_result`; §17.6) as the mechanical key m-3 needs for operator decision ②: `authority_class == true ∧ record_integrity ∈ {self_reported, mixed} ⇒ delivery_state = held`/escalate (not delivered — `mixed` = a partially-unverified authority record, held pessimistically per the CQ-2 canonical two-axis resolution); non-authority records still deliver `self_reported`/`mixed`. Derived from `record_kind ∈` the locked authority-bearing set (§4/§8/§11) or `gate_category ∈ A`. m-3 owns the fold of its `:63` fail-open→class-conditional-fail-closed text; m-2 owns this home. **Decision-② only** (not the other four decisions).
 - **CQ-3 (monotonic mechanics) — CONFIRMED.** The m-6 pure-judgment A-floor composes as **another MAX-input raiser** in the §3 monotonic model — floor wins, below-floor is a `gate_category = ceremony_downgrade` waiver — **no new m-2 mechanism** (§3 CQ-3 note; AC9).
-- **CQ-4 (terminal-state tokens) — SETTLED (m-2 half); `held` semantics narrowed per m-2.implementer review `020503`.** `delivery_state = {accepted, rejected, held}` (exactly-one-outcome, no `submitted` limbo). Canonical `rejected` (legacy `bounced` unified, resolves Q-E); **`held` field-home declared** (§17.1), **narrowed to the m-7 §6 / m-6 meaning**: internal-fault-on-authority (check throws/times-out/reads-corrupt on an authority record — not accepted, not rejected, operator-visible, resolved by subsequent record) **and** the CQ-2 fail-closed disposition — **not** ordinary human-gate parking (that is `accepted` into the ODB/park lane). `held` is terminal for the intake; resolution is a subsequent record, not a mutation. Consumer carve-out added to the c1 accepted-only invariant (§4 step 5, §18): `held` is consumed only by gate/escalation/scheduler/operator-resolution machinery, never downstream work authority. m-3 settles the observe-bounce token; m-6 aligned bucket-D (`015800`) — joint co-sign.
+- **CQ-4 (terminal-state tokens) — SETTLED (m-2 half); `held` semantics narrowed per m-2.implementer review `020503`.** `delivery_state = {accepted, rejected, held}` (exactly-one-outcome, no `submitted` limbo). Canonical `rejected` (legacy `bounced` unified, resolves Q-E); **`held` field-home declared** (§17.1), **narrowed to the m-7 §6 / m-6 meaning**: internal-fault-on-authority (check throws/times-out/reads-corrupt on an authority record — not accepted, not rejected, operator-visible, resolved by subsequent record) **and** the CQ-2 fail-closed disposition — **not** ordinary human-gate parking (that is `accepted` into the ODB/park lane). `held` is terminal for the intake; resolution is a subsequent record, not a mutation. Consumer carve-out added to the c1 accepted-only invariant (§4 step 5, §18): `held` is consumed only by gate/escalation/scheduler/operator-resolution machinery, never downstream work authority. m-3 settled the observe-bounce token (**shares `rejected`**, m-3 §2 `:31` — token set closed at three); m-6 aligned bucket-D (`015800`) — joint co-sign.
 - **CQ-4b (trusted-config composition) — CONFIRMED (no correction).** The CTO's section-composed single-top-level-digest, restart-only, append-only-change ruling **preserves m-2's config assumptions**: m-2's section = the A-floor mechanics (CQ-3) + FieldSpec-derived gate config. **One clarifying layering note:** the config artifact's top-level digest (config integrity) is a **distinct axis** from the record-level `schema_version`/migrator registry (record-schema evolution, public-release intent) — the digest does not replace or version the record schema; both are append-only, both restart-relevant, they compose without conflict. No correction to the ruling.
 - **Boundary:** confirm-or-produce the four named CQ rows only; **no c1/c2/c3 reopen, no design-lock (m-7's, gated on these), no PLAN/IMPL/`pcode`/spike.** No genuine contradiction with a locked m-2 contract surfaced — `held` and `authority_class` are additive, consistent with the c1 seam (exactly-one-outcome) and R2 (class-keyed, never model).
 
+**c6-fix-m-2 — re-review cleanup fold (`DESIGN-REREVIEW-2026-07-02`, CONDITIONAL-GO; DOC-ONLY, no mechanism change, lock invariants unchanged).** 7 findings routed to m-2; the four sanctioned by-construction claims + the confusion-resistant/D5 vocab + the byte-exact `{accepted, rejected, held}` enum are preserved.
+- **m-2-F1 (B, ◆ decision ③ RAISE-ONLY):** typed `gate_category` **monotonic-toward-A** — `known_A` detector = system-floor MAX-raiser; affordance constrained to `[floor, A]`; effective/forced result home = `gate_category` (raised) + system-computed `gate_category_raised: bool`; ③ negative fixture = AC18; the m-7 known-A/RAISE-ONLY NF fixture registered as an owed artifact (§17.1, §3 CQ-3 pattern).
+- **m-2-F10 (M):** canonicalized to **one record-level id `gate_category`**; the second id `human_gate_reason` retired (its "pick a reason" role is `gate_category`'s own `agent_enum_pick` fill) (§17.1, §12 echo).
+- **m-2-F5 (M):** made **gate-referenceability first-class FieldSpec data** — `gate_referenceable: bool` (default false; §4); the §5 predicate grammar now keys on the field's own flag (model-identity/computed-label/`X-` = false), so the two KEPT by-construction claims read declared data, not an out-of-band list.
+- **m-2-F7 (M):** README status rows swept to post-c4/c5 state (joint lock CLOSED, posture carve-out CLOSED, PLAN row → c6 gate) — see README.
+- **m-2-F9 (C):** §15 operator-judgment (i)/(ii) marked **RATIFIED** → ARCH §J1/§J2 (+ the `on_timeout` row + §17.5 echo); stable section anchors, no longer "proposed."
+- **m-5-F1 (B, seam — m-2 slice):** reflected **propose-vs-stamp per-column ownership** in the §17.3 mirror — planner proposes `seat`/`role`/`seat_archetype` from a fill-time-pruned candidate set; conductor stamps `seat_archetype` + `authority_ceiling` per-column (`system_only`), so a lane cannot self-elevate its ceiling (confused-deputy fix). m-4 (record) + m-5 (registry) own the propose/stamp mechanism — CC'd; their sides land in their docs.
+- **m-5-F2 (M, seam):** the runtime-away / posture-vocab split re-record lands in **m-5's §C4 integrated ledger** (m-5/CTO write; not an m-2 doc) — m-2's posture mirror (§17.3 rides `seat_archetype`; §17.6 LOCKED-c3 value-enum) is already consistent, so **no m-2 doc change is owed**; flagged for the m-5/CTO ledger owner.
+- **Verified, not redone (CTO apply half, VP-approved):** m-2-F2 (CQ-2 `{self_reported, mixed}` §17.6/§17.1/AC16), m-4-F3 (`chosen_model` re-anchor, model-identity-mark exclusion §5/§17.3), m-5-F3 (`template_ref` "set when spawned from template; null otherwise" §17.3), m-2-F4 (`delivery_state` close §17.1), m-2-F6 (exempt ODB `model_name` slot §17.2), m-2-F8 (§8 sole-path scoping) — all present + consistent.
+- **Boundary:** DOC-ONLY; no mechanism change; `Forms & Determinism` lock invariants unchanged; four sanctioned by-construction claims + confusion-resistant/D5 vocab + `{accepted, rejected, held}` byte-exact enum preserved; no PLAN/IMPL/`pcode`/spike.
+
--- a/master/domains/m-2-forms-determinism/README.md	2026-07-02 19:49:04
+++ b/master/domains/m-2-forms-determinism/README.md	2026-07-02 21:13:59
@@ -21,17 +21,18 @@
 | c1 | DESIGN | m-1 rev2 re-affirm | **mutual + closed** — both directions: m-1 `165643`+`170308` ↔ my `165702`+`170032` (R1 operator-addr, R2 routing_ref non-gating, R3 DI-5 evidence_integrity) |
 | c1 | DESIGN-REVIEW | round 3 (rev2) | **APPROVE** — `../../relays/c1-refine-m-2/DESIGN-REVIEW-Implementer-20260629-170241.md` (5 acceptance points at E1) |
 | c1 | SITREP | design-complete-rev2 → orchestrator | **sent** — `../../relays/c1-refine-m-2/SITREP-planner-20260629-170716.md` |
-| c1 | LOCK | joint m-1↔m-2 co-foundational lock | **ready / held** — contract mutually re-affirmed; awaits m-1 rev2 review + VP review; orchestrator runs it |
+| c1 | LOCK | joint m-1↔m-2 co-foundational lock | **✅ CLOSED** — contract mutually re-affirmed; Cluster 4 closed (full-record mirror + posture carve-out + computed-field homes); m-7 lock CQs (CQ-1/2/3/4/4b) m-2-approved; see the c4/c5 rows below |
 | c1 | READINESS-FIX-c1 | write-path seam fold (MUST-before-Step-1) | **✅ CLOSED** — `send()` §4 steps 4–5 reconciled to the VP-co-signed seam (pre-append form+lineage → atomic `accepted` / terminal `rejected`; no `submitted` limbo; Step-1 = store+form+lineage); byte-consistent w/ m-1 `224500`; CTO re-verified + VP closure co-sign `230335`. Report: `../../relays/readiness-fix-c1/SITREP-planner-20260630-225007.md` |
 | c1 | READINESS-FIX-c4 (4a) | routing R2 grammar fix | **✅ VP-approved** (`232925`) — `declared_deviated`(planner)/`deviated_observed`(observe) split, `any_row:` aggregate trigger, `field:<id>` model-identity exclusion. §18 + AC14 |
 | c1 | READINESS-FIX-c4 (4b+4c) | full-record mirror + computed-field homes | **folded** — §17.3 mirrors m-4 `:200-210` (per-row fields + `deviation_reason_code` + reserved `constraints`/`template_ref`); new §17.6 declares `record_integrity`/`surface_intent` homes. §18 + AC15. Report: `../../relays/readiness-fix-c4/SITREP-planner-20260630-234702.md`. 4 sibling confirms in |
-| c1 | READINESS-FIX-c4 (posture carve-out) | CTO arbitration `001537` — posture home | **folded (corrected)** — my standalone `human_mode`/posture per-row field was wrong (contradicted m-5 c3 lock `:142` "no new m-2 field"); **removed it** — posture rides `seat_archetype`; kept m-5 posture value-enum (LOCKED at c3) in §17.6; m-4 record un-extended. Report: `../../relays/readiness-fix-c4/SITREP-planner-20260701-012000.md`. Held for owner ruling-confirms + CTO/VP re-verify → Cluster 4 CLOSE |
+| c1 | READINESS-FIX-c4 (posture carve-out) | CTO arbitration `001537` — posture home | **folded (corrected)** — my standalone `human_mode`/posture per-row field was wrong (contradicted m-5 c3 lock `:142` "no new m-2 field"); **removed it** — posture rides `seat_archetype`; kept m-5 posture value-enum (LOCKED at c3) in §17.6; m-4 record un-extended. Report: `../../relays/readiness-fix-c4/SITREP-planner-20260701-012000.md`. **✅ Cluster 4 CLOSED** (owner ruling-confirms + CTO/VP re-verify complete; F2 propose-vs-stamp per-column mix folded in c6 m-5-F1) |
 | c1 | Q-F (SHOULD, tracked) | m-3 observe-field row-parity | **accepted, pre-PLAN** (CTO `001537`) — promote 7 §12-prose observe fields into formal §17.3 rows for machine-checkable §13 fixtures; after Cluster 4 closes, before Step-1 PLAN (doc §15 Q-F) |
 | c1 | Q-E | `delivery_state` token semantics | **✅ RESOLVED via CQ-4** — `{accepted, rejected, held}`, `bounced` unified to `rejected`; observe-bounce token = m-3's call in CQ-4 (doc §15 Q-E/§17.1) |
-| — | c4-cq-gateconfig | m-7 design-lock CQs (CQ-2/3/4/4b) | **✅ m-2 half APPROVED (r2)** — m-2.implementer `021604` approve (3 r1 blockers resolved: `held` narrowed to m-7 §6 internal-fault + CQ-2 fail-closed; `held` consumer carve-out; byte-exact §J2 `gate_category`). Answer `../../relays/c4-cq-gateconfig/DESIGN-planner-20260702-021108.md`; SITREP `SITREP-planner-20260702-021849.md`. **Joint legs remain (not m-2's):** CQ-4 m-3/m-6, CQ-4b m-3/m-4/m-6, CQ-2 m-3 disposition, then CTO fold |
+| — | c4-cq-gateconfig | m-7 design-lock CQs (CQ-2/3/4/4b) | **✅ m-2 half APPROVED (r2)** — m-2.implementer `021604` approve (3 r1 blockers resolved: `held` narrowed to m-7 §6 internal-fault + CQ-2 fail-closed; `held` consumer carve-out; byte-exact §J2 `gate_category`). Answer `../../relays/c4-cq-gateconfig/DESIGN-planner-20260702-021108.md`; SITREP `SITREP-planner-20260702-021849.md`. m-2's half approved first; the joint CQ closure + CTO fold now proceed **under the m-7 design-lock / c6 gate** (no m-2-blocking leg outstanding) |
 | — | c5-claim-sweep-m-2 | claim-text hygiene (malicious→confusion-resistant + D5) | **✅ COMPLETE + pair-approved (r2)** — m-2.implementer `140747` approve (both r1 blockers closed). 17 relabel loci (confusion-resistant + D5) + 14 `[c5 KEEP]`; union-net grep → only :47 (non-overclaim NOTE). Claim-text only, no mechanism change. Rev2 `../../relays/c5-claim-sweep-m-2/DESIGN-planner-20260702-135144.md`; SITREP `SITREP-planner-20260702-141054.md` → CTO folds into c5 status ledger |
 | — | c4-cq-m1 (CQ-1 co-sign) | `required_when` step-gate half | **✅ APPROVED + triad complete** — m-2.implementer `030145` approve (no must-revise); m-1 concurs `025200` (non-gating, no boundary drift). Co-sign `../../relays/c4-cq-m1/DESIGN-planner-20260702-024704.md`. CQ-1 evidence set complete (m-1 `013500`+`020418` + m-2 `024704`+`030145`) → **CTO fold** (m-7 NF-S5). New `layer_present:` §5 atom + AC17 |
-| c1 | PLAN | gated design-doc PLAN | **held** — Cluster 1 ✅; awaits Cluster 4 re-verify + m-7 CQ closures + operator phase-transition authorization + orchestrator PROCEED-TO-PLAN |
+| — | c6-fix-m-2 | re-review cleanup (7 doc-only findings) | **folded** — m-2-F1 (③ RAISE-ONLY `gate_category` monotonic-toward-A, AC18), F10 (canonical id `gate_category`), F5 (`gate_referenceable:bool` first-class), F7 (this README sweep), F9 (§15 RATIFIED→§J1/§J2), m-5-F1 slice (propose-vs-stamp per-column), m-5-F2 (m-5-ledger, no m-2 edit owed). Doc §18 c6 fold-log. **✅ COMPLETE + pair-approved** (m-2.implementer `211048`; r1 README gap folded r2). Completion `../../relays/c6-fix-m-2/SITREP-planner-20260702-211306.md` → CTO c6 verify sweep + c6 close |
+| c1 | PLAN | gated design-doc PLAN | **held** — Cluster 1 ✅, Cluster 4 ✅, m-7 CQs m-2-approved; awaits **c6 close (VP co-sign)** → then (e) Step-1 PLAN opens (operator-authorized) + orchestrator PROCEED-TO-PLAN |
 
 ## Layout
 - `audit/` — AUDIT artifacts (durable home; the relay under `master/relays/c1-audit-m-2/` is the lint-checked report of record).
--- a/master/domains/m-3-observation-evidence/design/2026-06-29-observe-evidence-design.md	2026-07-02 19:49:04
+++ b/master/domains/m-3-observation-evidence/design/2026-06-29-observe-evidence-design.md	2026-07-02 21:18:10
@@ -4,15 +4,15 @@
 RUN_ID: master
 CYCLE: c2
 OWNER: m-3 (Observation & Evidence) — design-lead m-3.planner
-STATUS: design-complete + `c2-fold-m-3` (F1+F3) applied; re-review pending before c2 lock. Built on the LOCKED c1 contract; does not reopen m-1/m-2.
+STATUS: LOCKED — c2-lock 20260630 (VP co-sign `c2-lock` 20260630-043859; close 044308). Post-lock folds applied + implementer-approved: `c2-fold-m-3` (040633), `c4-cq-gateconfig` (decision-② / CQ-4, 021724), `c4-cq-slotin` (CQ-5, 030205), and the two c5 folds `c5-fold-decision-5` + `c5-claim-sweep-light` (approved `140748` / `140749`). The `c6-fix-m-3` re-review cleanup is **implementer-approved (rev1, `211417`)**; the orchestrator c6-close (VP co-sign) is pending. Built on the LOCKED c1 contract; does not reopen m-1/m-2.
 GRILL_LOCK_ID: c2-grill-m-3 (folded in §12)
-DESIGN_REVIEW_VERDICT: approve (r1) — m-3.implementer, `c2-design-m-3/DESIGN-REVIEW-implementer-20260629-211003.md` (2 non-blocking carry-forwards folded: ODB cite → maintainer-orchestrator/SKILL.md:53-69 §10; record_integrity scope §6/§13). Re-review pending for `c2-fold-m-3` (F1+F3, §5.1 + §15 fold-log).
+DESIGN_REVIEW_VERDICT: approve (r1) — m-3.implementer, `c2-design-m-3/DESIGN-REVIEW-implementer-20260629-211003.md` (2 non-blocking carry-forwards folded: ODB cite → maintainer-orchestrator/SKILL.md:53-69 §10; record_integrity scope §6/§13). `c2-fold-m-3` re-approved (`DESIGN-REVIEW-implementer-20260630-040633`); the later folds are implementer-approved per the §15 fold-log.
 
 ---
 
 ## 1. Frame, scope, and what is already locked
 
-**The one job.** Make "done" **conductor-observed, not lane-asserted.** A relay is deliverable only after the trusted courier (the conductor), reading the lane's ground-truth from *outside* the lane, has run the phase-shaped done-predicate and stamped the result onto the outgoing record. Self-reported completion is **labeled, never trusted** — for a **non-authority** record it is delivered, stamped `self_reported`; for the **authority/merge (A-gate) class** an unobservable→`self_reported` record is **held/escalated, not delivered** (operator decision ②, §3.2(d)/§8).
+**The one job.** Make "done" **conductor-observed, not lane-asserted.** A relay is deliverable only after the trusted courier (the conductor), reading the lane's ground-truth from *outside* the lane, has run the phase-shaped done-predicate and stamped the result onto the outgoing record. Self-reported completion is **labeled, never trusted** — for a **non-authority** record it is delivered, stamped `self_reported`; for the **authority/merge (A-gate) class** a not-fully-verified record (`record_integrity ∈ {self_reported, mixed}`) is **held/escalated, not delivered** (operator decision ②, §3.2(d)/§8).
 
 **Locked c1 contract this design builds on (NOT reopened):**
 - **R3 / I3-DI-5 observe-integrity** (ARCHITECTURE.md:31-34, 70-71): every observed record carries `evidence_integrity {observed | self_reported}` (per-field, two-value); the m-3 hook is **observer-only** — a positive write-allowlist that writes only the closed m-3 observed/computed field set + a veto. The conductor reads lane ground-truth from outside the lane (DI-5).
@@ -34,7 +34,7 @@
 
 **Two pre-flights compose, order fixed** (matches m-1 §5 / m-2 §4): (1) **m-2 form-validation** (structure), then (2) **m-3 observe** (observable done-predicate). The bounce names which pre-flight failed. Running observe *inside* the atomic `submit()` closes the check→edit→send-stale TOCTOU: the observation that passed is the observation stamped.
 
-**Trigger ≠ execute is load-bearing.** If the agent ran the probe itself, the result would be lane-forgeable again. The agent triggers; the conductor executes from its outside-lane vantage and returns the real result — seen-by-agent (so it self-corrects, no submit-blind churn) AND unforgeable.
+**Trigger ≠ execute is load-bearing.** If the agent ran the probe itself, the result would be lane-forgeable again. The agent triggers; the conductor executes from its outside-lane vantage and returns the real result — seen-by-agent (so it self-corrects, no submit-blind churn) AND lane-**unsubstitutable** through the tool surface (confusion-resistant; D5 ground-truth residual, §11).
 
 ---
 
@@ -60,8 +60,21 @@
 
 **§3.1 — the closed write-allowlist (R3 observer-only).** The hook writes ONLY: `ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`, `achieved_evidence`, the `target>achieved` computed `*_RESULT`, per-field `evidence_integrity`, the computed `record_integrity` rollup, the per-claim `executable_claim_results`, the `egress_scan_result`, `degradation_notes`, the routing-profile observed set (§9), and a pass/fail **veto**. It writes **no** identity/envelope field (FROM/ROLE/PARENT/relay_id) and performs **no** delivery effect. "Observer-only" is enforced as a positive allowlist, not prose intent.
 
-**§3.2 — what may veto.** Delivery gates on *evidence integrity* **only class-conditionally** (operator decision ②, folded `c4-cq-gateconfig`): for a **non-authority** record a `self_reported` field still delivers — the honest fallback, §8 — but for the **authority/merge (A-gate) class** an unobservable→`self_reported` record is **held/escalated, not delivered** (condition (d)). The vetoing/holding conditions are: (a) a phase-shaped done-predicate the conductor can observe to be **false** (e.g. an action-claim with no matching git ref; a read-only report over a dirty tree); (b) the generic **declared-vs-observed integrity veto** (a lane-declared field contradicted by conductor observation — e.g. clean-tree-claimed/dirty-observed; the routing silent-deviation case, §9); (c) a failed **egress** scan (§7); (d) **class-conditional fail-closed (decision ②):** `authority_class == true ∧ record_integrity == self_reported` ⇒ the record is **held/escalated** (terminal `held`, resummon the operator — J1 `hold_and_resummon`), never shipped on an unverified authority claim. `authority_class` is m-2's declared key (`bool`·`owner:system`·`computed_result`, m-2 §17.6 — `record_kind ∈` the authority-bearing set **or** `gate_category ∈ A`; **never keyed on model**, R2 holds). Token mapping (CQ-4): (a)/(b)/(c) are the check **running and saying no** ⇒ terminal **`rejected`** (§2); (d) is the check **unable to run** in an authority class ⇒ terminal **`held`**. *Deviation-as-such* and *integrity-labeling-as-such* still never act; only silent-deviation (b) and authority-class-unobservability (d) do.
+**§3.2 — what may veto.** Delivery gates on *evidence integrity* **only class-conditionally** (operator decision ②, folded `c4-cq-gateconfig`): for a **non-authority** record a `self_reported` field still delivers — the honest fallback, §8 — but for the **authority/merge (A-gate) class** a not-fully-verified record (`record_integrity ∈ {self_reported, mixed}`) is **held/escalated, not delivered** (condition (d)). The vetoing/holding conditions are: (a) a phase-shaped done-predicate the conductor can observe to be **false** (e.g. an action-claim with no matching git ref; a read-only report over a dirty tree); (b) the generic **declared-vs-observed integrity veto** (a lane-declared field contradicted by conductor observation — e.g. clean-tree-claimed/dirty-observed; the routing silent-deviation case, §9); (c) a failed **egress** scan (§7); (d) **class-conditional fail-closed (decision ②):** `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` ⇒ the record is **held/escalated** (terminal `held`, resummon the operator — J1 `hold_and_resummon`), never shipped on a not-fully-verified authority claim. `authority_class` is m-2's declared key (`bool`·`owner:system`·`computed_result`, m-2 §17.6 — `record_kind ∈` the authority-bearing set **or** `gate_category ∈ A`; **never keyed on model**, R2 holds). Token mapping (CQ-4): (a)/(b)/(c) are the check **running and saying no** ⇒ terminal **`rejected`** (§2); (d) is the check **unable to fully verify** an authority-class record ⇒ terminal **`held`**. *Deviation-as-such* and *integrity-labeling-as-such* still never act; only silent-deviation (b) and authority-class not-full-verification (d) do.
 
+**§3.3 — total observe→terminal disposition (m-3-F6; keyed by authority class).** Every submit reaches exactly one terminal `{accepted | rejected | held}` (m-7 §6 / NF-S16, exactly-one-outcome). The `observe_result` sub-enums are **observe-layer signals, not independent terminals**: `executable_claim_results` and `degradation_notes` feed `record_integrity`/`achieved_evidence`, they do not map to a terminal token directly. The total map (realizing the VP two-axis, `DESIGN-REREVIEW-2026-07-02` §5):
+
+| observe condition | non-authority | authority-class |
+|---|---|---|
+| `predicate_result=pass`, `veto=none`, `egress=pass/n/a` | `accepted` | `accepted` |
+| `predicate_result=fail` (check **ran, observably false**) or `veto=block_delivery` from a declared-vs-observed contradiction (§9) | `rejected` | `rejected` |
+| `predicate_result=blocked \| degraded` (**no vantage** — opaque lane, §8; ⇒ fields `self_reported`/`mixed`) | `accepted` + `self_reported`/`mixed` label | **`held`** + escalate (§3.2(d): `record_integrity ∈ {self_reported, mixed}`) |
+| **conductor check machinery ran-and-broke** (internal fault — m-7 §6) | `rejected` / author-returned, fault edge named | **`held`** + escalate |
+| `egress_scan_result=blocked` (§7) | **`held`** + resummon (J1) | **`held`** + resummon (J1) |
+| `executable_claim_results ∈ {skipped, unsafe}` | *(not a terminal — marks that claim's field `self_reported`; the record's terminal follows `record_integrity` per the rows above)* | *(same)* |
+
+The two axes: **check-ran-said-no ⇒ `rejected`** (both classes); **check-could-not-run** (no vantage *or* machinery-broke) **⇒ authority `held` / non-authority `accepted`+label or `rejected`+fault-edge**; **egress-block ⇒ `held`** (both). Consistent with CQ-4 (`c4-cq-gateconfig`), decision ② (§3.2(d)/§8), and m-7 §6 internal-fault-`held`.
+
 ---
 
 ## 4. The evidence ladder + the check-registry (executable claims)
@@ -70,9 +83,9 @@
 - **E1** — read the cited `file:line` / schema / static source.  *(passive; initial release)*
 - **E2** — run a named local check: the test suite, lint, typecheck, a fixture.  *(initial release; see registry)*
 - **E3** — integration/runtime check (local stack, worker, DB/API).  *(incremental)*
-- **E4** — deployed/live proof — stays the explicit **LIVE-VERIFY** gate, often operator-observed (mirrors the upstream protocol "merge is not deploy; deploy is not live verification," `protocol.md:202`).
+- **E4** — deployed/live proof — stays the explicit **LIVE-VERIFY** gate, often operator-observed (mirrors the upstream protocol "merge is not deploy; deploy is not live verification," `protocol.md:202`). **Operator-attested E4 (m-3-F5):** when the operator — a trusted observer *outside* the lane — confirms live proof the conductor cannot itself derive, the per-field `evidence_integrity` stays the locked **two-value** enum (R3 not reopened); the record carries a **separate provenance marker `attestation_source {conductor | operator}`** (NOT a third integrity value). An operator-attested field is trust-equivalent to `observed` (a trusted outside-vantage), distinguished only by `attestation_source=operator` for honest audit — never `self_reported` (the lane did not assert it). Build-step definition, §13.
 
-**The check-registry (resolves grill Q1, §12).** Every check the conductor runs is a **vetted, conductor-implemented, parameterized procedure** the agent *selects*, never code the agent *supplies*. An "executable claim" = the agent declaring "claim X, verify by registry-check C(params)"; the conductor runs *its own* implementation of C → "the conductor ran the check, it passed." **Arbitrary lane-authored execution is rejected** — the conductor is the TCB; running lane code there is the worst place for arbitrary execution. Consequences:
+**The check-registry (resolves grill Q1, §12).** Every check the conductor runs is a **vetted, conductor-implemented, parameterized procedure** the agent *selects*, never code the agent *supplies*. An "executable claim" = the agent declaring "claim X, verify by registry-check C(params)"; the conductor runs *its own* implementation of C → "the conductor ran the check, it passed." **Arbitrary agent-*supplied* code is rejected** — the conductor is the TCB; the agent never selects arbitrary code, only a vetted registry entry. **Honest scope (m-3-F1 / x1-F5):** several suite-class entries (`run-suite`, `red→green-differential`) nonetheless **execute repo-resident lane code** (the project's own test suite *is* lane/repo code) — this is repo code the conductor *runs*, not conductor code, and not agent-*supplied* code. That execution is a **named non-locking carry** (§13): suite-class entries must run in an **unprivileged executor with no store / config / outbox handle and no signing key**, gating the Step-2 observe (E2) build; the paired executor-isolation fixture is **m-7-hosted** (NF-S6-analogue). Consequences:
 - **One mechanism unifies executable-claims with the m-5 archetype gates** (§5): the archetype invariants *are* registry entries (`run-suite`, `find-references`, `diff-shape`, `red→green-differential`, `test-files-unchanged`).
 - **The initial release ships the base conductor-owned checks active** (read-file, git-status, run-named-suite). Checks with **side effects or unbounded cost** run only under an **operator gate** (operator policy on which registry entries may auto-run). The registry is the **additive extension point** — new entries are an additive schema bump, not a new subsystem.
 
@@ -82,7 +95,7 @@
 
 **Base predicates are phase-shaped** (conductor-observable):
 - read-only (AUDIT/DESIGN/RECONCILE report): allowed relay artifact present + **no unauthorized source actions** (clean/expected tree).
-- IMPL: expected diff/artifacts exist + required checks green + no scope drift.
+- IMPL: expected diff/artifacts exist + required checks green + **scope observed as `diff_paths ⊆ scope_paths`** where `scope_paths` is a plan-declared named form field (named owner). *(m-3-F7: a bare "no scope drift" with no declared `scope_paths` field is **struck** — not conductor-observable — rather than asserted; the observable form is the declared-path-subset check, a build-step dependency on m-2 declaring `scope_paths`, §13.)*
 - MERGE / LIVE-VERIFY: the target merge-commit / deploy / live proof exists, as applicable.
 
 **Archetype add-ons parameterize the predicate via the opaque `slot_in` atom.** The done-predicate hook takes an opaque archetype-tag input that *selects* an invariant-set. **m-3 owns the predicate-execution mechanism + result shape; m-5 owns the tag-space + which invariants attach to which tag.** Candidate mappings **surfaced, not closed** (per VP c2-decomp Finding 4):
@@ -108,7 +121,7 @@
 
 - **Per-field `evidence_integrity {observed | self_reported}` is the only canonical integrity truth** — locked R3, two-value, NOT reopened. Set by the conductor per observed/computed field: `observed` when the conductor derived the value from outside the lane; `self_reported` when it could not and the lane supplied it.
 - **`record_integrity {observed | self_reported | mixed}` is a conductor-computed rollup**, stamped onto the immutable record for **human auditability** (and the Owner Decision Brief). It is a **pure function of the per-field stamps** — `observed` if all expected-observable fields are observed; `self_reported` if none are; `mixed` otherwise — so it can never drift from or be forged independently of the per-field detail (the auditor sees exactly what the conductor derived, frozen). It is **system-computed**, not a new agent-writable field; canonical-iff-consumed is satisfied (the audit/ODB surface is the consumer).
-- **Safety rule:** anywhere a trust/gate decision reduces the label to yes/no, **`mixed` is treated as not-fully-verified** (pessimistic for trust, informative for audit). No mechanical *delivery* gate branches on either label **for non-authority records** — integrity is labeled, not gated. The **one** class-conditional exception (operator decision ②, §3.2(d)/§8): `authority_class == true ∧ record_integrity == self_reported` ⇒ `held`/escalate — the disposition keys on m-2's declared `authority_class` + `self_reported`, never on model (R2). *(Open co-sign edge, flagged for m-2/CQ-4: whether `authority_class == true ∧ record_integrity == mixed` should also hold under this safety rule's pessimistic reduction — the folded key stays `== self_reported` as m-2 declared (§17.6:334) until reconciled, not silently broadened.)*
+- **Safety rule:** anywhere a trust/gate decision reduces the label to yes/no, **`mixed` is treated as not-fully-verified** (pessimistic for trust, informative for audit). No mechanical *delivery* gate branches on either label **for non-authority records** — integrity is labeled, not gated. The class-conditional exception (operator decision ②, §3.2(d)/§8): `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` ⇒ `held`/escalate — the disposition keys on m-2's declared `authority_class`, never on model (R2). *(The `mixed` leg is the VP-ratified **CQ-2 canonical** resolution of the edge this doc previously left open for the now-closed m-2/CQ-4 co-sign: per this section's own pessimistic rule (`mixed` = not-fully-verified anywhere a trust decision reduces to yes/no), a partially-verified authority record **holds** rather than delivers, consistent with §3.2(d)/§8. This widens the m-3 **disposition** only — `record_integrity` already ranges over `mixed` (m-2 §17.6), so m-2's field-home is unchanged; convergence assumes the parallel c6 token-alignment in m-2 §17.6 / m-7 §15 / ARCHITECTURE C4.2.)*
 - **Scope of the claim (m-3.implementer carry-forward):** `record_integrity: observed` means **every declared-observable field was observed** — it is NOT a claim that free-text judgment / agent rationale is independently verified. Per the locked boundary (observable → system-captured; judgment/unobservable → agent-asserted + cross-lane reviewed), prose judgment stays agent-asserted and review-gated; `record_integrity` ranges only over the observable field set. (Fixture in §13.)
 
 ---
@@ -136,11 +149,13 @@
 
 ## 9. The m-3↔m-4 seam (RECONCILED — folded verbatim)
 
-Reconciled both sides (`c2-design-m3-m4-coord/COORD-planner-20260629-192916.md` confirmed by `COORD-RECONCILE-planner-20260629-193400.md`). Folded verbatim:
+Reconciled both sides (`c2-design-m3-m4-coord/COORD-planner-20260629-192916.md` confirmed by `COORD-RECONCILE-planner-20260629-193400.md`). Folded verbatim (the c2 record; the `deviated_observed` formula is superseded by the GL-1 canonical, next):
 
+**GL-1 canonical fold (c6, m-4-F2 — SUPERSEDES the `chosen_model ∉ rank-1(capability_prior_snapshot)` formula in the verbatim block below).** `deviated_observed` is computed **bucket-vs-bucket**: `deviated_observed := chosen_bucket ≠ declared_bucket`, where `declared_bucket` is the planner-declared capability bucket and `chosen_bucket` is the conductor-observed bucket the chosen model resolves to — recorded as the auxiliary observe-atom **`bucket_binding_observed`** (the bucket binding the conductor actually observed). This is **strictly more R2-clean** than the original model-vs-rank1 phrasing: the comparison is bucket-vs-bucket, never a model-identity gate (model identity never enters any predicate). m-4 deletes / precisely-conditions its "equivalent fallback" bracket (m-4 §9, CC'd on this dispatch) so the two docs state one formula. Everything else in the seam (the silent-deviation block via the generic declared-vs-observed integrity veto; snapshot-provenance not DI-5; `outcome_feedback_ref` null-reserved the initial release / benchmark-sampled a later release) is unchanged.
+
 > `routing_decision.deviated` is a planner-declared boolean (gate-side; the only model-touching atom the m-2 schema gate reads, via `justified_deviation.required_when: declared_deviated==true`), cross-checked by a conductor-derived `deviated_observed := chosen_model ∉ rank-1(capability_prior_snapshot)` for `(role, task_tag)` (observe-side; an m-3 `record_kind=routing_decision` profile of the single R3 observe-atom). The check is observed on all routing decisions and rides on snapshot-provenance, not DI-5 (conductor-stamped snapshot ⇒ `evidence_integrity: observed`, including for opaque lanes). The four declared×observed cells deliver, EXCEPT silent deviation (declared=false, observed=true), which m-3 blocks via its generic declared-vs-observed integrity veto — an observe-layer bounce that prompts honest re-declaration, which then triggers m-2's existing justification requirement. No model-derived predicate enters the m-2 schema gate (R2 preserved) — escalated to orchestrator/VP for ratification at the c2 lock. Task-outcome evidence (`outcome_feedback_ref`) is null-reserved in the initial release and benchmark-sampled in a later release (sampled independent of `deviated`).
 
-**m-3-side commitments:** the routing observe-atom is a `record_kind=routing_decision` profile of the single R3 atom; the silent-deviation block is the generic integrity veto (clean-tree class), realized as an observe-layer bounce (not a form-mutation, not a model-derived `required_when`); deviation-check stamped on all routing decisions; the layer (capability-bucket vs model-instance) is m-4's to finalize and the mechanism is identical either way.
+**m-3-side commitments:** the routing observe-atom is a `record_kind=routing_decision` profile of the single R3 atom; the silent-deviation block is the generic integrity veto (clean-tree class), realized as an observe-layer bounce (not a form-mutation, not a model-derived `required_when`); deviation-check stamped on all routing decisions; the layer is **finalized to capability-bucket** by the GL-1 canonical above (`chosen_bucket ≠ declared_bucket` via the `bucket_binding_observed` atom), not model-instance — the mechanism is identical, now R2-clean by grammar (bucket-vs-bucket, never a model-identity predicate).
 
 ---
 
@@ -156,7 +171,7 @@
 
 ## 11. F5 — precise novelty (no overclaim over the partial existing primitives)
 
-What is **promoted, not built**: the E0–E4 vocabulary (upstream); the egress detection engines + fail-closed pattern (agent-scripts + claude-code); the gate/hook + permission-queue *pattern* (jcode/claude-code); the Owner Decision Brief template (agent-scripts). What is **genuinely new**: (1) observe-AS-send — observation as the fail-closed *send-gate* performed from *outside* the lane and bound atomically to the gated relay (surveyed systems audit post-hoc, gate pre-action without outcome-verify, or observe in-lane); (2) `evidence_integrity` as a stamped trust-label tied to that outside-vantage gate (VFA has signed receipts, not this); (3) the conductor-owned check-registry as a *governance* send-gate (RLVR/verifiable-keypoints use the same primitive as an *RL training reward*, not a runtime gate); (4) the egress gate as a *conductor-governed egress chokepoint* (the only egress the governance system offers), not prompt policy. **Claim boundary (c5 claim-sweep, ratified checklist `c5-claim-sweep-architecture/…-131320`):** we do **not** claim to verify which model actually served (model = payload, not trust-bearing); the deviation check is internal-consistency, not model-attribution forensics (§9). The observe-gate, `evidence_integrity`, and `slot_in` invariants are **confusion-resistant / observer-selected** — the invariant is chosen conductor-side, unreachable by the observed lane's *tool* surface — and are **not tamper-proof against the D5 residual**: under same-uid attach a shell-bearing seat can write store files directly, reach the network outside the conductor, or emit an operator-`FROM` (the documented GRILL-LOCK-deployment-fork D5 accepted-risks, out of scope). `record_integrity` is a **recomputable pure function** (an auditor recomputes it from the per-field detail), so a direct-store forgery is **detectable, not undetectable**. Egress **content** safety is best-effort/encoding-evadable (§7); egress **destination** control over the governed path is by-construction post-spike.
+What is **promoted, not built**: the E0–E4 vocabulary (upstream); the egress detection engines + fail-closed pattern (agent-scripts + claude-code); the gate/hook + permission-queue *pattern* (jcode/claude-code); the Owner Decision Brief template (agent-scripts). What is **genuinely new**: (1) observe-AS-send — observation as the fail-closed *send-gate* performed from *outside* the lane and bound atomically to the gated relay (surveyed systems audit post-hoc, gate pre-action without outcome-verify, or observe in-lane); (2) `evidence_integrity` as a stamped trust-label tied to that outside-vantage gate (VFA has signed receipts, not this); (3) the conductor-owned check-registry as a *governance* send-gate (RLVR/verifiable-keypoints use the same primitive as an *RL training reward*, not a runtime gate); (4) the egress gate as a *conductor-governed egress chokepoint* (the only egress the governance system offers), not prompt policy. **Claim boundary (c5 claim-sweep, ratified checklist `c5-claim-sweep-architecture/…-131320`):** we do **not** claim to verify which model actually served (model = payload, not trust-bearing); the deviation check is internal-consistency, not model-attribution forensics (§9). The observe-gate, `evidence_integrity`, and `slot_in` invariants are **confusion-resistant / observer-selected** — the invariant is chosen conductor-side, unreachable by the observed lane's *tool* surface — and are **not tamper-proof against the D5 residual**: under same-uid attach a shell-bearing seat can write store files directly, reach the network outside the conductor, or emit an operator-`FROM` (the documented GRILL-LOCK-deployment-fork D5 accepted-risks, out of scope). `record_integrity` is a **recomputable pure function** (an auditor recomputes it from the per-field detail), so an **inconsistent** forgery (rollup ≠ per-field detail) is **recompute-detectable**; a **coherent** direct-store forgery (a D5 same-uid seat writes a self-consistent rollup + per-field detail together) is **not** recompute-detectable — that is the D5 direct-store residual, **not a detectability guarantee**. Egress **content** safety is best-effort/encoding-evadable (§7); egress **destination** control over the governed path is by-construction post-spike.
 
 ---
 
@@ -171,12 +186,12 @@
 - questions asked operator: the 4 below (HUMAN_GATE_REQUIRED).
 
 Resolved decisions:
-- **Executable-claim execution surface** — *conductor-owned check-registry; arbitrary lane code rejected.* — reason: the conductor is the TCB; registry unifies with the m-5 archetype gates; base checks active the initial release, side-effecting/expensive ones operator-gated; registry is the additive extension point. — source: operator.
+- **Executable-claim execution surface** — *conductor-owned check-registry; arbitrary **agent-supplied** code rejected; suite-class **repo-resident lane code** may run only through the non-locking unprivileged-executor carry (§4 / §13, m-3-F1).* — reason: the conductor is the TCB; registry unifies with the m-5 archetype gates; base checks active the initial release, side-effecting/expensive ones operator-gated; registry is the additive extension point. — source: operator (execution-scope honesty per c6 m-3-F1/x1-F5).
 - **Egress fail-closed policy** — *always-block + hold-and-resummon; no default auto-redact; operator-configurable rule set; auto-redact only for operator-whitelisted classes later.* — reason: first external send is high-stakes; aligns with locked J1. — source: operator.
 - **Opaque-lane evidence floor** — *honest `self_reported` floor; **deliver-and-label for non-authority records, held/escalate (terminal `held`) for the authority/merge class** (decision ②, §8); full outside-vantage = standalone-runtime carry-forward.* — reason: locked DI-5 honest-fallback (class-conditional per decision ②); does not block the initial release. — source: operator.
 - **Record-level integrity label** — *a conductor-computed, system-owned three-value `record_integrity {observed|self_reported|mixed}` stamped on the immutable record for human audit; pure function of per-field stamps; `mixed` = not-fully-verified for any trust reduction.* — reason: operator requires human-auditable label; computed-and-stamped is more auditable than hand-set and respects canonical-iff-consumed (audit/ODB is the consumer). — source: operator (per-field two-value stays locked R3).
 - **ODB model-name egress carve-out (operator decision ⑤, RECORDED 2026-06-30; folded `c5-fold-decision-5` 2026-07-02)** — *exempt **only** the model-name field of a conductor-generated operator-facing ODB (destination = operator) from the **confidentiality** egress class; safety/content class + all other model-name egress still block; fail-closed default preserved; governed-surface scoped (conductor-generated, non-lane-writable field — no lane *tool* marks it; confusion-resistant, D5 direct-store residual §11); away-transport still m-6 away-bridge-gated; **R2 untouched** (egress-confidentiality, not gate-referenceability).* — reason: an operator reading their own ODB should see which model ran without the confidentiality scan blocking it, while peer-bias protection (R2) and all other egress stay intact. — source: operator (`READINESS-REGISTER.md` ⑤). Joint fold: m-3 (scan carve-out, §7) + m-6 (typed exempt-marked ODB field) + m-4 (R2-guard).
-- **Authority-class `self_reported` disposition (operator decision ②, RECORDED 2026-06-30; folded `c4-cq-gateconfig` 2026-07-02)** — *the universal fail-OPEN (`self_reported` always delivers) becomes **class-conditional fail-CLOSED**: `authority_class == true ∧ record_integrity == self_reported` ⇒ **held/escalated** (terminal `held`, resummon), not delivered; lower-stakes/informational records still deliver `self_reported` (honest fallback preserved).* — reason: an unverified authority/merge claim must not ship on self-report; matches the fail-closed egress posture on high-stakes paths (J1). — source: operator (`READINESS-REGISTER.md` ②; `DESIGN-REVIEW-2026-07-01.md` §2A.7). Key = m-2's declared `authority_class` (§17.6); disposition token = `held` (m-7 §6). R2 holds — class-keyed, never model. Reflected in §2, §3.2(d), §8.
+- **Authority-class `self_reported` disposition (operator decision ②, RECORDED 2026-06-30; folded `c4-cq-gateconfig` 2026-07-02)** — *the universal fail-OPEN (`self_reported` always delivers) becomes **class-conditional fail-CLOSED**: `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` ⇒ **held/escalated** (terminal `held`, resummon), not delivered; lower-stakes/informational records still deliver `self_reported` (honest fallback preserved).* — reason: a not-fully-verified authority/merge claim must not ship on self-report; matches the fail-closed egress posture on high-stakes paths (J1). — source: operator (`READINESS-REGISTER.md` ②; `DESIGN-REVIEW-2026-07-01.md` §2A.7). The `mixed` leg = the VP-ratified c6 **CQ-2 canonical** resolution (§6:111). Key = m-2's declared `authority_class` (§17.6); disposition token = `held` (m-7 §6). R2 holds — class-keyed, never model. Reflected in §2, §3.2(d), §8.
 
 Rejected alternatives:
 - arbitrary sandboxed agent code in the conductor — rejected: arbitrary execution adjacent to the TCB; large isolation subsystem; premature.
@@ -201,8 +216,11 @@
 - opaque-lane detection + the honest `self_reported` labeling; the standalone-runtime outside-vantage extension.
 - `slot_in` (work-archetype) conductor-classification: the pipeline point is **pinned by CQ-5** (`c4-cq-slotin`, co-signed) — post-form/lineage-gate, pre-observe-hook, atomic classify+observe bind (not a persisted `submitted` state); the remaining PLAN details are the immutability enforcement after acceptance and the non-lane-writability check; the done-predicate reads the conductor-classified value (§5.1).
 - negative fixtures: silent-deviation block; clean-tree-claim/dirty-observed veto; an opaque-lane `self_reported` case; a `mixed` rollup case; an egress fail-closed (hold-and-resummon) case; a registry-tamper case; and (m-3.implementer carry-forward) a fixture proving `record_integrity: observed` ⇒ all declared-observable fields observed, NOT that free-text judgment/rationale is independently verified.
-- (decision ⑤, `c5-fold-decision-5`) an **egress carve-out** fixture pair: an operator-facing conductor-generated ODB's model-name field **passes** the confidentiality scan (destination=operator), while (a) the **same model-name in any non-ODB record still blocks**, (b) a **secret/PII/auth-URL in any ODB field still blocks** (safety/content class unexempted), and (c) **lane-supplied "exempt-marked" content still blocks** (the exemption is governed-surface only — no lane *tool* marks the field; confusion-resistant, D5 direct-store residual §11). *(Flag to m-7: this is the egress-fixture update decision ⑤ needs.)*
-- (decision ②, `c4-cq-gateconfig`) an **authority-class-unobservable → `held`/escalate** fixture (`authority_class==true ∧ record_integrity==self_reported` ⇒ not delivered, terminal `held`, resummon) **paired** with a **non-authority-unobservable → `accepted`+`self_reported`-delivered** fixture (the class split bites); and a **Step-2 observe-bounce → terminal `rejected` evidenced audit-record** fixture (candidate not delivered; exactly-one-outcome per m-7 NF-S16).
+- (decision ⑤, `c5-fold-decision-5`) an **egress carve-out** fixture pair: an operator-facing conductor-generated ODB's model-name field **passes** the confidentiality scan (destination=operator), while (a) the **same model-name in any non-ODB record still blocks**, (b) a **secret/PII/auth-URL in any ODB field still blocks** (safety/content class unexempted), and (c) **lane-supplied "exempt-marked" content still blocks** (the exemption is governed-surface only — no lane *tool* marks the field; confusion-resistant, D5 direct-store residual §11). *(Owed receiving-side record (m-3-F4): this fixture pair is registered as an **owed row on the ARCHITECTURE §C4 egress-fixture surface + the m-7 executor-isolation/egress fixture set** — receiving owner **CTO/m-7**; m-3 authors the (a)/(b)/(c) set here and flags it, the m-7/ARCHITECTURE surface carries the landed row. Named non-locking carry, not a lock reopen — flagged to CTO in the `c6-fix-m-3` completion relay.)*
+- (decision ②, `c4-cq-gateconfig`) an **authority-class not-fully-verified → `held`/escalate** fixture (`authority_class==true ∧ record_integrity ∈ {self_reported, mixed}` ⇒ not delivered, terminal `held`, resummon — the `mixed` case exercises the c6 CQ-2 canonical leg) **paired** with a **non-authority-unobservable → `accepted`+`self_reported`-delivered** fixture (the class split bites); and a **Step-2 observe-bounce → terminal `rejected` evidenced audit-record** fixture (candidate not delivered; exactly-one-outcome per m-7 NF-S16).
+- **NAMED NON-LOCKING CARRY — suite-class executor isolation (m-3-F1 / x1-F5; owner m-3 + m-7-hosted execution).** Gates the **Step-2 observe (E2) build**: suite-class registry entries (`run-suite`, `red→green-differential`) execute **repo-resident lane code**, so they must run in an **unprivileged executor with no store / config / outbox handle and no signing key**; the paired **m-7 executor-isolation fixture** (NF-S6-analogue, proving the privilege separation) is m-7-hosted (the m-7 dispatch carries the receiving side). Recorded like decision-④ / re-mint-supersedes — a build-step gate, **not a lock reopen** (the `Observation & Evidence` lock invariants are unchanged; this hardens the honest §4 relabel).
+- **operator-attested provenance (m-3-F5 build-step).** Define `attestation_source {conductor | operator}` as a **separate provenance marker** on operator-attested E4 records — NOT a third value in the locked two-value `evidence_integrity` enum (R3 untouched); operator-attested ⇒ trust-equivalent to `observed`, tagged `attestation_source=operator` for audit. (§4.)
+- **`scope_paths` observable (m-3-F7 build-step).** The IMPL scope predicate `diff_paths ⊆ scope_paths` depends on m-2 declaring a named `scope_paths` form field (named owner); absent it the clause is struck as non-observable (§5). Build-step seam with m-2, not a mechanism change this cycle.
 
 ---
 
@@ -216,7 +234,8 @@
 
 ## 15. Fold-log
 - **`c2-fold-m-3` (20260630) — additive.** Folded m-5 consumer-lens findings (basis: `c2-consumer-reconcile`, VP-approved `RECONCILE-orchestrator-reviewer-20260630-034321.md`): **F1** — `slot_in` is conductor-owned, classified at work-record acceptance, immutable, non-lane-writable (§5.1); **F3** — `slot_in` = work-archetype axis only, `seat_archetype` is a separate axis owned outside m-3 (§5.1). Bounded/additive only — no concrete slot values, no `required_when`/`visible_when` on slot values, no m-2 ownership-category or predicate-vocabulary change; concrete tag-space + invariant selection stay m-5-owned (c3). Re-review: `c2-fold-m-3` → m-3.implementer (re-approval required before c2 lock).
-- **`c4-cq-gateconfig` (20260702) — decision-② fold + CQ-4 settle (bounded, directed).** Basis: orchestrator COORD `c4-cq-gateconfig/DESIGN-orchestrator-planner-20260702-012336` + addressing correction `c4-cq-coord/…-013323`; operator decision ② (`READINESS-REGISTER.md` ②) + `DESIGN-REVIEW-2026-07-01.md` §2A.7. **CQ-2 (fold):** §2 / §3.2 / §8 universal fail-OPEN → **class-conditional fail-CLOSED** — `authority_class ∧ record_integrity==self_reported` ⇒ terminal `held`/escalate (not delivered); non-authority still delivers `self_reported`. Keyed on m-2's declared `authority_class` (§17.6, line 334 — m-2 owns the field-home, m-3 owns this disposition fold; co-owner reconciled: m-2 already declared it + mapped to `held`). **CQ-4 (settle, m-3 half):** §2 refined — the Step-2 observe-bounce (predicate observably false / integrity-contradiction / egress-fail) **shares the terminal `rejected` token** (candidate not delivered; terminal evidenced record committed for audit), unifying with m-7 §6 / NF-S16 exactly-one-outcome; the `held` token stays **distinct** for authority-class unobservability (the check *couldn't run* vs *ran and said no* — m-7 §6 held-semantics); non-authority unobservable → `accepted`+`self_reported`. **CQ-4b (confirm):** the CTO section-composed single-top-level-digest config ruling preserves m-3's egress/evidence/registry config assumptions (restart-only trusted load, tamper-evident digest, per-domain authorship, change=committed store record) — the decision-② class→disposition map is m-3 config keyed on m-2's `authority_class`, composes into the artifact; confirmed, no correction. R2 preserved throughout (class-keyed, never model); no c1/c2 contract reopened (additive dispositions consistent with the exactly-one-outcome seam). Re-review: `c4-cq-gateconfig` → m-3.implementer (adversarial review — closure-gate leg per `…-013323`); cross-domain co-sign CQ-4 with m-2 + m-6. **Byte-consistency sweep (rev, per m-3.implementer `DESIGN-REVIEW-…-020509` must-revise — intended shape approved, stale text blocked):** patched the residual pre-fold lines a builder could read as the old behavior — §1:15 (`self_reported` "is delivered" → non-authority-only, authority→held), §1:19 + §5.1:101 + §13:200 (`submitted→accepted` two-state → the readiness-fix-c1 **no-`submitted`-limbo** terminal-outcome seam), §6:111 ("no delivery gate branches on either label" → non-authority-only + the one decision-② exception), §12:175 (opaque-lane "deliver-and-label" → class-conditional). Surfaced (not silently resolved): the `authority_class ∧ record_integrity==mixed` edge — folded key stays `== self_reported` as m-2 declared until the m-2/CQ-4 co-sign reconciles it (§6:111 note).
+- **`c4-cq-gateconfig` (20260702) — decision-② fold + CQ-4 settle (bounded, directed).** Basis: orchestrator COORD `c4-cq-gateconfig/DESIGN-orchestrator-planner-20260702-012336` + addressing correction `c4-cq-coord/…-013323`; operator decision ② (`READINESS-REGISTER.md` ②) + `DESIGN-REVIEW-2026-07-01.md` §2A.7. **CQ-2 (fold):** §2 / §3.2 / §8 universal fail-OPEN → **class-conditional fail-CLOSED** — `authority_class ∧ record_integrity==self_reported` ⇒ terminal `held`/escalate (not delivered); non-authority still delivers `self_reported`. Keyed on m-2's declared `authority_class` (§17.6, line 334 — m-2 owns the field-home, m-3 owns this disposition fold; co-owner reconciled: m-2 already declared it + mapped to `held`). **CQ-4 (settle, m-3 half):** §2 refined — the Step-2 observe-bounce (predicate observably false / integrity-contradiction / egress-fail) **shares the terminal `rejected` token** (candidate not delivered; terminal evidenced record committed for audit), unifying with m-7 §6 / NF-S16 exactly-one-outcome; the `held` token stays **distinct** for authority-class unobservability (the check *couldn't run* vs *ran and said no* — m-7 §6 held-semantics); non-authority unobservable → `accepted`+`self_reported`. **CQ-4b (confirm):** the CTO section-composed single-top-level-digest config ruling preserves m-3's egress/evidence/registry config assumptions (restart-only trusted load, tamper-evident digest, per-domain authorship, change=committed store record) — the decision-② class→disposition map is m-3 config keyed on m-2's `authority_class`, composes into the artifact; confirmed, no correction. R2 preserved throughout (class-keyed, never model); no c1/c2 contract reopened (additive dispositions consistent with the exactly-one-outcome seam). Re-review: `c4-cq-gateconfig` → m-3.implementer (adversarial review — closure-gate leg per `…-013323`); cross-domain co-sign CQ-4 with m-2 + m-6. **Byte-consistency sweep (rev, per m-3.implementer `DESIGN-REVIEW-…-020509` must-revise — intended shape approved, stale text blocked):** patched the residual pre-fold lines a builder could read as the old behavior — §1:15 (`self_reported` "is delivered" → non-authority-only, authority→held), §1:19 + §5.1:101 + §13:200 (`submitted→accepted` two-state → the readiness-fix-c1 **no-`submitted`-limbo** terminal-outcome seam), §6:111 ("no delivery gate branches on either label" → non-authority-only + the one decision-② exception), §12:175 (opaque-lane "deliver-and-label" → class-conditional). Surfaced (not silently resolved): the `authority_class ∧ record_integrity==mixed` edge — folded key stayed `== self_reported` as m-2 declared until reconciled. *(Resolved c6 per the VP-ratified **CQ-2 canonical** — authority-class `mixed` now also `held` under the §6 pessimistic rule; the closed CQ-4 is superseded as the vehicle. See §6:111 / §3.2(d).)*
 - **`c4-cq-slotin` (20260702) — CQ-5 observe-pipeline co-sign (bounded, additive).** Basis: orchestrator CQ-5 dispatch + poke (`c4-cq-slotin/…-012537`, `…-024102`); m-5.planner answer `c4-cq-slotin/DESIGN-planner-20260702-014506` (m-5.implementer approved `…-020448`). **Co-signed the observe-pipeline half:** the conductor classifies `slot_in` at work-record acceptance **post-form/lineage-gate, PRE-observe-hook, atomic-bind-with-observation** matches — and is *required* by — locked m-3 §5.1/§2 (the observe done-predicate is keyed on `slot_in`; §3.1 allowlist excludes `slot_in` from the hook's writes ⇒ a distinct conductor step must set it pre-observe; the atomic bind is the §2 TOCTOU closure). **Pins** the previously-open §5.1/§13 pipeline point (was "a PLAN detail"). No m-3 contract change beyond recording the pin; immutability + non-lane-writability enforcement stay PLAN. R2/C2.4 preserved (`slot_in` selects an observe-invariant, never keys a model or an m-2 predicate). Co-sign relay + m-3.implementer review = the m-3 legs of the CQ-5 triad.
 - **`c5-fold-decision-5` (20260702) — decision-⑤ ODB model-name egress carve-out (owner-authored, joint).** Basis: orchestrator COORD `c5-fold-decision-5/DESIGN-orchestrator-planner-20260702-132752`; operator decision ⑤ (`READINESS-REGISTER.md`). **m-3 half (§7):** a typed, narrow confidentiality-class exemption — `record_kind==ODB` (operator-facing, conductor-generated) ∧ `field==model_name` (m-6's exempt-marked field) ∧ `destination==operator`, exempt from the **confidentiality** class only; safety/content class + all other model-name egress unchanged; fail-closed default preserved; governed-surface scoped (conductor-generated, non-lane-writable field — no lane *tool* marks it; confusion-resistant, D5 direct-store residual §11); away-transport still m-6 away-bridge-gated; non-away renders local. **R2 untouched** (egress-confidentiality, not gate-referenceability). §12 decision + §13 egress-fixture pair added; m-7 flagged for the egress-fixture update. Joint close needs all three co-confirms (m-3 scan + m-6 ODB + m-4 R2-guard). Semantic review: `c5-fold-decision-5` → m-3.implementer.
 - **`c5-claim-sweep-light` (20260702) — claim-text hygiene (no mechanism change).** Basis: orchestrator batched COORD `c5-claim-sweep-light/DESIGN-orchestrator-planner-20260702-132139`; ratified checklist `c5-claim-sweep-architecture/…-131320` (VP-approved). **RELABELed** (malicious-seat-containment overclaims → confusion-resistant + D5 residual): §1:20 m-1 "sole-writer" → *governed* sole-writer within the governance surface (mirror m-1); §5:98 "verifier-tamper-resistant invariants" → *observer-selected (confusion-resistant)*; §5.1:101 "the tamper-resistance the observe story depends on" → *confusion-resistance* (no lane *tool* rewrites the tag; D5 residual); §7:118 egress "sole external sender / structurally unbypassable" → *conductor-governed egress chokepoint* + content-safety best-effort/encoding-evadable + D5 residual; §11:158 "structural chokepoint (sole external sender)" → *conductor-governed* + a consolidated **claim-boundary** paragraph (confusion-resistant/observer-selected, D5 accepted-risks, `record_integrity` recomputable⇒forgery-detectable). **KEPT** (licensed classes): the atomic `submit()` bind (control-flow property of the trusted courier, R2-class); the trigger/executor separation + §2:31-37 (trusted-courier control-flow, implementer-confirmed KEEP); the observer-selected done-predicate/invariants (framing, now globally scoped by §11); F5 novelty/comparison claims; the fixture names (§13) + fold-log. **Rev (per m-3.implementer `DESIGN-REVIEW-…-134833` must-revise): the parallel `c5-fold-decision-5` fold introduced fresh RELABEL-net vocab ("Non-forgeable" / "a lane cannot mark") that this sweep must also cover — now RELABELed at §7:122 / §12:178 / §13:204 / §15:221 to *governed-surface scoped: no lane tool marks the field; confusion-resistant; D5 direct-store residual §11*. Full-net grep re-run against the post-⑤ document: clean.** No mechanism change, no locked-contract reopen. Survivor list: `c5-claim-sweep-light/DESIGN-planner-…`; m-3.implementer semantic review = the closure leg.
+- **`c6-fix-m-3` (20260702) — re-review cleanup, DOC-ONLY (no mechanism change; lock invariants unchanged).** Basis: `c6-fix-m-3/DESIGN-orchestrator-planner-20260702-204506`; source of record `master/DESIGN-REREVIEW-2026-07-02.md` §5 + appendix (VP-concurred CONDITIONAL-GO). **Applied (m-3 domain judgment):** **m-3-F1 / x1-F5** — §4 relabeled honestly (agent supplies no arbitrary code, but suite-class entries *execute repo-resident lane code*) + a named non-locking §13 carry (unprivileged executor, no store/config/outbox handle, no signing key; m-7-hosted executor-isolation fixture). **m-4-F2 (seam)** — §9 folds the GL-1 canonical `deviated_observed := chosen_bucket ≠ declared_bucket` via the `bucket_binding_observed` atom (bucket-vs-bucket, supersedes the model-vs-rank1 verbatim formula; more R2-clean; m-4 conditions its 'equivalent fallback' bracket, CC'd). **x3-F3** — verified §6 `{self_reported, mixed}` broaden (CTO/m-3-F2) + §13 fixture exercises the `mixed` leg. **m-3-F10** — §11 detectability scoped (inconsistent forgery recompute-detectable; coherent D5 forgery is not — a residual, not a guarantee). **m-3-F4** — §13 ⑤ egress fixture given a named receiving surface (ARCHITECTURE §C4 + m-7 fixture set, receiving owner CTO/m-7). **m-3-F5** — §4/§13 operator-attested E4 gets a separate `attestation_source {conductor|operator}` provenance marker (NOT a third `evidence_integrity` value; R3 untouched). **m-3-F6** — §3.3 total observe→terminal `{accepted|rejected|held}` disposition table, keyed by authority class (VP two-axis). **m-3-F7** — §5 IMPL "no scope drift" → observable `diff_paths ⊆ scope_paths` (declared m-2 field) or struck. **x2-F4** — §2 "lane-unforgeable" → "lane-**unsubstitutable** through the tool surface" (+ D5 ground-truth residual). **Verified (CTO-applied, not redone):** m-3-F2 (§6 broaden), m-3-F3 (§2 scope), x1-F3 (§6/§15 mixed home), m-3-F8 (README). **m-3-F9 (header) — corrected in c6-rev1** (per m-3.implementer `DESIGN-REVIEW-…-210904`): the STATUS header had cited the superseded c5 `must-revise` relays `134834 / 134833`; retargeted to the approving `140748 / 140749` + the `c6-fix-m-3` entry. **§12 echo aligned:** the resolved-decision "arbitrary lane code rejected" → "arbitrary agent-supplied code rejected; suite-class repo-resident lane code only via the §4/§13 carry" (byte-consistency with the m-3-F1 §4/§13 relabel). Preserved: the four sanctioned by-construction claims, the confusion-resistant/D5-residual vocabulary, and the byte-exact `{accepted, rejected, held}` enum. Review: `c6-fix-m-3` → m-3.implementer (`DESIGN_REVIEW_VERDICT`).
--- a/master/domains/m-3-observation-evidence/README.md	2026-07-02 19:49:04
+++ b/master/domains/m-3-observation-evidence/README.md	2026-07-02 21:19:01
@@ -30,9 +30,11 @@
 - AUDIT: **reconciled** — independent planner + implementer passes, zero disagreements (verdict recommended-next); `c2-audit-m-3/RECONCILE-planner-20260629-185757.md`.
 - **c4-cq-gateconfig (2026-07-02):** re-engaged (m-3 in TO) for the bounded CQ closure. **CQ-2** decision-② fold PRODUCED (§2/§3.2(d)/§8: fail-OPEN → class-conditional fail-CLOSED `authority_class ∧ self_reported ⇒ held`; keyed on m-2's declared `authority_class` §17.6:334). **CQ-4** m-3 half SETTLED (Step-2 observe-bounce **shares `rejected`**; `held` distinct for authority-class unobservability; unifies m-7 §6/NF-S16). **CQ-4b** CONFIRMED (section-composed single-digest config preserves m-3 egress/evidence/registry assumptions; no correction). Answer relay `c4-cq-gateconfig/DESIGN-planner-20260702-014846.md` → m-3.implementer. Review `DESIGN-REVIEW-implementer-20260702-020509` = **must-revise** (intended CQ shape APPROVED; blocked on stale byte-consistency text only). **Revision folded** `DESIGN-planner-20260702-021030.md` — 6 stale lines patched (fail-open at §1:15/§6:111/§12:175; `submitted→accepted` limbo at §1:19/§5.1:101/§13:200); `authority_class ∧ mixed` edge flagged for m-2 co-sign, key left `== self_reported`. Re-reviewed **`DESIGN-REVIEW-implementer-20260702-021724` = approve** (both blockers folded, R2 intact, `mixed` edge preserved open). **m-3 pair legs COMPLETE** — closure relayed `SITREP-planner-20260702-022243.md` → orchestrator. Remaining (NOT m-3): CQ-4 m-2+m-6 co-sign; CQ-4b m-2/m-4/m-6 confirms; CTO fold into m-7 design-lock; the `authority_class ∧ mixed` co-sign edge. **CQ-5 co-sign FILED** in `c4-cq-slotin` (`DESIGN-planner-20260702-024732`): the post-form/lineage · PRE-observe-hook · atomic-bind `slot_in` ordering matches + is *required* by m-3 §5.1/§2 (done-predicate keys on `slot_in`; §3.1 excludes it from the hook's writes ⇒ distinct pre-observe conductor step); pins the open §5.1/§13 pipeline point. **CQ-5 m-3 legs COMPLETE** — m-3.implementer approved first-pass (`DESIGN-REVIEW-implementer-20260702-030205`). All four triad legs present (m-5 ✓ · m-5.impl ✓ · m-3 co-sign ✓ · m-3.impl ✓) → **CQ-5 foldable** by orchestrator; completion relayed `SITREP-planner-20260702-030651`. No PLAN.
 
+**c6-fix-m-3 (2026-07-02):** 10 re-review cleanup findings applied DOC-ONLY (no mechanism change; lock invariants unchanged). 7 fixes — F1/x1-F5 (§4 honest relabel + §13 unprivileged-executor carry), m-4-F2 (§9 GL-1 bucket-vs-bucket `deviated_observed` seam fold), F10 (§11 detectability scoped), F4 (§13 ⑤ fixture named receiving surface), F5 (§4/§13 `attestation_source` provenance marker, R3 untouched), F6 (§3.3 total observe→terminal disposition table), F7 (§5 scope-drift → `diff_paths ⊆ scope_paths` or struck), x2-F4 (§2 unforgeable→unsubstitutable). x3-F3 + 5 CTO-applied verified. Completion relay `c6-fix-m-3/DESIGN-planner-20260702-210322` → m-3.implementer. Review `…-210904` = **must-revise** (all 10 findings confirmed non-blockers; blocked on 2 byte-consistency echoes I didn't touch: STATUS header cited superseded c5 must-revise IDs; §12 "arbitrary lane code" echo). **Rev1** `…-211206` — header retargeted `134834/134833`→`140748/140749`; §12 aligned to §4/§13; fold-log m-3-F9 corrected. **Re-review `…-211417` = approve — c6-fix-m-3 pair-COMPLETE.** Completion relayed `SITREP-planner-20260702-211815` → orchestrator (ready for the c6 verify sweep + close/VP co-sign → then Step-1 PLAN opens). Cross-owner items (NOT m-3): m-4 §9 fallback bracket + CTO ARCHITECTURE §C4 ⑤-fixture row. No PLAN.
+
 **c5 legs BOTH CLOSED (m-3 side), 2026-07-02** — reported to orchestrator `c5-decomp/SITREP-planner-20260702-141120`. Both approved after one shared wording revision (parallel-fold "Non-forgeable" → governed-surface/no-tool + D5 residual): claim-sweep-light approve `c5-claim-sweep-light/…-140749`; decision-⑤ approve `c5-fold-decision-5/…-140748`. Decision-⑤: all three owner halves now pair-approved (m-3 scan + m-4 R2-guard + m-6 ODB) → joint close/ledger fold is the orchestrator's; m-7 flagged for the egress fixture.
 
-**c5-fold-decision-5 (2026-07-02):** operator decision ⑤ (ODB model-name egress carve-out) — m-3 egress half folded (§7): typed **confidentiality-class-only** exemption for a conductor-generated operator-facing ODB's model-name field (destination=operator); safety/content + all other model-name egress unchanged; non-forgeable; away-transport still m-6-bridge-gated; **R2 untouched**. §12/§13(fixture)/§15 added; m-7 flagged for the egress fixture. Fold relay `c5-fold-decision-5/DESIGN-planner-20260702-133443` → m-3.implementer. Review `…-134834` = **must-revise** (carve-out substance approved; blocked only on fresh "Non-forgeable" RELABEL-net vocab). **Revised** `…-135809` — relabeled all 4 spots (§7/§12/§13/§15) → governed-surface/no-tool + D5 residual. Re-review requested. Joint close needs m-6 (ODB) + m-4 (R2-guard) co-confirms. No PLAN.
+**c5-fold-decision-5 (2026-07-02):** operator decision ⑤ (ODB model-name egress carve-out) — m-3 egress half folded (§7): typed **confidentiality-class-only** exemption for a conductor-generated operator-facing ODB's model-name field (destination=operator); safety/content + all other model-name egress unchanged; governed-surface scoped (no lane tool marks the field; confusion-resistant; D5 direct-store residual); away-transport still m-6-bridge-gated; **R2 untouched**. §12/§13(fixture)/§15 added; m-7 flagged for the egress fixture. Fold relay `c5-fold-decision-5/DESIGN-planner-20260702-133443` → m-3.implementer. Review `…-134834` = **must-revise** (carve-out substance approved; blocked only on fresh "Non-forgeable" RELABEL-net vocab). **Revised** `…-135809` — relabeled all 4 spots (§7/§12/§13/§15) → governed-surface/no-tool + D5 residual. Re-review requested. Joint close needs m-6 (ODB) + m-4 (R2-guard) co-confirms. No PLAN.
 
 **c5-claim-sweep-light (2026-07-02):** claim-text hygiene folded (no mechanism change). 5 RELABELs → confusion-resistant + D5 residual (§1:20 m-1 sole-writer→governed; §5:98/§5.1:101 tamper-resistant→observer-selected/confusion-resistant; §7:118 + §11:158 egress "sole external sender/structurally unbypassable"→conductor-governed chokepoint + a consolidated §11 claim-boundary). KEEPs: atomic-bind control-flow (R2-class), observer-selected invariants, F5 novelty, fixtures. Survivor list + fold `c5-claim-sweep-light/DESIGN-planner-20260702-133039`. Review `…-134833` = **must-revise** (5 named relabels faithful; blocked because the parallel ⑤ fold added fresh RELABEL-net vocab the sweep must also cover). **Revised** `…-135809` — ⑤ vocab RELABELed + added to the survivor list; full-net grep re-run post-⑤ = clean (only §2:37 residual is the implementer-confirmed KEEP). Re-review requested. No PLAN.
 
--- a/master/domains/m-4-routing-policy/design/2026-06-29-routing-policy-design.md	2026-07-02 19:49:04
+++ b/master/domains/m-4-routing-policy/design/2026-06-29-routing-policy-design.md	2026-07-02 21:07:47
@@ -3,18 +3,22 @@
 **DESIGN_DOC_ID:** c2-design-m-4-routing-policy
 **Cycle/phase:** c2 / DESIGN (terminal = design-lock; no PLAN/IMPL this phase)
 **Owner:** m-4 (Routing & Policy) — design-lead m-4.planner; adversarial review m-4.implementer
-**Status:** DESIGN-COMPLETE — m-4.implementer DESIGN-REVIEW **approve** on rev1
+**Status:** LOCKED-at-c2 (design-of-record) — m-4.implementer DESIGN-REVIEW **approve** on rev1
 (`c2-design-m-4/DESIGN-REVIEW-implementer-20260629-203329.md`); rev1 folded the three must-revise findings
 (F1 precise R2 invariant + locked bucket-vs-bucket form + bucket-binding atom §2; F2 `deviation_reason_code`
 `required_when` §5; F3 template no-bypass invariant §7) and the non-blocking carry-forward (leftover membership
 shorthand in §0/§9 normalized). **rev2 — c2 fold** (`c2-fold-m-4` 20260630): folded the VP-approved m-5/m-6
 consumer-lens findings (F2 per-assignment `seat_archetype`+`authority_ceiling`; F3 `seat_archetype` distinct
 opaque tag; **M4-1 CONFIRMED** — B→A escalation via the existing c1 monotonic HUMAN_GATE, no new gate class) —
-bounded-additive, see §15 fold-log. **Held for the c2 lock** pending the orchestrator/VP R2-boundary
-ratification + the m-5 concrete-semantics reservation (c3). Builds on the VP-approved
+bounded-additive, see §15 fold-log. **LOCKED at the c2 lock** — the R2-boundary is **RATIFIED**
+(readiness-fix-c1/c4 + the design-review reconciles); the m-5 concrete-semantics reservation is **carried to c3**
+(m-5-owned). **c5 folds:** claim-sweep (§16) + decision-⑤ R2-guard (§17). **c6 re-review cleanup** (`c6-fix-m-4`;
+`master/DESIGN-REREVIEW-2026-07-02.md` CONDITIONAL-GO): §5/§7/§13 build-carry + template-authoring + reason-code
+sourcing + status/§J reconcile (see §18 c6 fold-log). Builds on the VP-approved
 `c2-audit-m-4` reconcile and the
 locked c1 contract (`master/ARCHITECTURE.md` R2/§5/§J). The m-3↔m-4 seam is RECONCILED both sides
-(`c2-design-m3-m4-coord`); one R2-boundary item is escalated to orchestrator/VP for ratification at the c2 lock.
+(`c2-design-m3-m4-coord`); the one R2-boundary item **was escalated to orchestrator/VP and RATIFIED at the c2 lock**
+(readiness-fix-c1/c4 + the design-review reconciles).
 **Sources of record:** `c2-audit-m-4/RECONCILE-planner-20260629-190200.md` (the audit), the reconciled seam
 statement (`c2-design-m3-m4-coord/COORD-planner-20260629-192916.md`), this cycle's operator design-grill.
 
@@ -135,9 +139,10 @@
 *declaration* is enforced for all lanes; the identity of the *serving* model stays untrusted bookkeeping by
 design (§12).
 
-> **R2-boundary item escalated to orchestrator/VP (both pairs aligned):** routing the silent-deviation block
+> **R2-boundary item — RATIFIED at the c2 lock (both pairs aligned):** routing the silent-deviation block
 > through m-3's generic integrity-veto (not a model-derived `required_when`) is the cross-domain R2-preservation
-> choice; ratify at the c2 lock. Neither m-3 nor m-4 will accept a model-derived schema predicate.
+> choice; **ratified** (readiness-fix-c1/c4 + the design-review reconciles). Neither m-3 nor m-4 accepts a
+> model-derived schema predicate.
 
 ---
 
@@ -200,10 +205,10 @@
 | field | owner / fill | type | notes |
 |---|---|---|---|
 | `routing_record_kind` | agent_enum_pick (routing set) | enum | record-kind discriminator (own values) |
-| `routing_assignments` | seat_scoped_enum → planner / orch-planner | row_array | altitude B; per row: `{ seat, role, task_tag, declared_bucket, chosen_model (pinned\|resolved), declared_deviated:bool, pin_mode:{pinned\|slot}, seat_archetype (opaque; m-5-valued, c3), authority_ceiling (resolved-at-spawn, recorded) }`. **F2 fold:** `seat_archetype` + resolved `authority_ceiling` are recorded **per assignment** for replay/audit on **both** the template *and* hand-authored (non-template) staffing paths — the ceiling that denied write authority is replay-complete *on the record*, not only enforced (§8). Both are **opaque** (concrete values m-5-owned, c3). |
+| `routing_assignments` | seat_scoped_enum → planner / orch-planner **(+ `operator` on `template_ref`-bearing template-spawn records only — §7, m-4-F4)** | row_array | altitude B; per row: `{ seat, role, task_tag, declared_bucket, chosen_model (pinned\|resolved), declared_deviated:bool, pin_mode:{pinned\|slot}, seat_archetype (opaque; m-5-valued, c3), authority_ceiling (resolved-at-spawn, recorded) }`. **F2 fold:** `seat_archetype` + resolved `authority_ceiling` are recorded **per assignment** for replay/audit on **both** the template *and* hand-authored (non-template) staffing paths — the ceiling that denied write authority is replay-complete *on the record*, not only enforced (§8). Both are **opaque** (concrete values m-5-owned, c3). |
 | `capability_prior_snapshot` | system (`computed_result`) | object | both layers, replay-complete (§4) |
 | `justified_deviation` | free_text | text | **required_when `any(routing_assignments.declared_deviated == true)`** (§2) |
-| `deviation_reason_code` | agent_enum_pick | enum | machine-readable reason (SR-26-2 "reason code" + the `justified_deviation` narrative); **required_when `any(routing_assignments.declared_deviated == true)`** — same grain/treatment as `justified_deviation`, so the reason code can never be omitted where the policy says it is load-bearing |
+| `deviation_reason_code` | agent_enum_pick | enum | machine-readable reason (SR-26-2 "reason code" + the `justified_deviation` narrative); **required_when `any(routing_assignments.declared_deviated == true)`** — same grain/treatment as `justified_deviation`, so the reason code can never be omitted where the policy says it is load-bearing. **Value-set = config-sourced enum (m-4-F5, c6): default-seeded + operator-configurable + hardcoded `other` fail-safe** (the §J2 `gate_category` pattern). **Shipped default vocabulary:** `capability_gap` · `cost_budget` · `latency_budget` · `bucket_unavailable` · `operator_directive` · `experiment` (a later release-reserved) · `other` (fail-safe ⇒ the `justified_deviation` free-text carries the reason). m-2 §17.3 mirrors the value-set (flagged). |
 | `constraints` | agent_enum_pick / free_text | object | budget/latency/privacy — **reserved/forward** (§6) |
 | `outcome_feedback_ref` | system / id_ref | id_ref | **null-reserved** (a later release); links the downstream observe-atom |
 | `template_ref` | system / id_ref | id_ref | set when spawned from a template (§7); null otherwise |
@@ -219,8 +224,9 @@
 - **Stage 1 — capability-prior floor.** The policy engine suggests the rank-1 recommended bucket per
   `(role, task_tag)`; the seat-form renders it as the default (a slot resolves to a member of that bucket).
 - **Stage 2 — justified deviation (ships in the initial release).** The planner may pick off-floor → declares
-  `declared_deviated = true` → the form requires `justified_deviation` + `deviation_reason_code`. Recorded,
-  attributable, auditable, human-overridable.
+  `declared_deviated = true` → the form requires `justified_deviation` + `deviation_reason_code` (the reason code
+  drawn from the config-sourced value-set seeded in §5 — default + operator-configurable + `other` fail-safe).
+  Recorded, attributable, auditable, human-overridable.
 - **Stage 3 — outcome feedback (later-release forward hook only).** The benchmark consumes m-3 observed outcomes (via
   `outcome_feedback_ref`) to retune **Layer 2 recommendations only** (GL-2). Non-gradient (bandit/tally) — but
   per §11(b) the **differentiator is the persisted, auditable decision+update artifact, not the non-gradient
@@ -257,6 +263,22 @@
 **reusable, pre-filled routing decision** that changes **nothing** in the locked contract or R2 (it is a
 record, not a gate input). **Operator-customizable** (save/edit), config-sourced like the priors.
 
+**Template-spawn authoring model (m-4-F4, c6 — decided before the GL-4 build step).** A template-spawn
+`routing_decision` is authored **`FROM = operator`** on the operator-relay channel: the operator selecting the
+template is the authorizing act, and at spawn there may be **no orchestrator-planner seat yet** (the template
+*creates* the team, its orchestrator seats included). The `routing_assignments` **seat-scope is widened to admit
+`operator` as an authoring seat on `template_ref`-bearing records only** — ordinary hand-authored routing stays
+planner / orch-planner scoped. **`declared_deviated` stays a *declared* (agent/operator-owned) bit — it is NOT
+re-typed system/computed on the template path;** re-typing it would reintroduce the readiness-fix-c4
+declared-vs-observed conflation the m-3 silent-deviation seam depends on (the declared bit must remain an
+independent agent assertion, cross-checked by the conductor's observed bit). The pre-declared template
+justification is snapshotted into the emitted record (the no-bypass invariant above). **Propose-vs-stamp holds
+(seam):** the template/operator *proposes* role/archetype from a fill-time-pruned candidate set; the conductor
+*stamps* the resolved `seat_archetype` + `authority_ceiling` per-column. **Cross-domain mirror required (flagged
+to the orchestrator — m-4 records the decision, does not write those docs):** m-2 §17.3 (widen the
+`routing_assignments` seat-scope to admit `operator` on `template_ref`-bearing records) + m-7 S11 (template-spawn
+author = operator-relay).
+
 **Ownership boundary:** m-4 = the template's model-assignment mechanism + record emission on spawn. **m-5** =
 the template structures (seats/panes/gate-set/read-only-ness) + the shipped 1–3 lineup. **Conductor-core** =
 opening/naming the panes + boot-relay delivery. Roadmap sequencing of the initial-release scope addition is the
@@ -314,8 +336,8 @@
 with the `chosen_model ∉ rank-1` membership phrasing in the reconciled statement above as its equivalent
 fallback. This refinement (the bucket-binding atom + the locked bucket-vs-bucket form) is **additive to the
 m-4-declared observed set** — m-4 owns *which* fields are observed, m-3 owns *how* — so it needs no seam
-re-negotiation; flagged to m-3 at design-complete. Seam status: **reconciled both sides**; open only the
-R2-boundary ratification at the lock.
+re-negotiation; flagged to m-3 at design-complete. Seam status: **reconciled both sides**; the R2-boundary
+ratification is **complete** (c2 lock; readiness-fix-c1/c4 + the design-review reconciles).
 
 ---
 
@@ -335,11 +357,19 @@
   m-4 **raises the c1-locked monotonic `HUMAN_GATE_REQUIRED`** on the routing record — this is exactly the
   **"m-4 routing-raise"** the locked m-2 contract already names as a sanctioned monotonic raiser
   (`m-2 form-schema §3`: floor = MAX across {system, agent, m-5 archetype-ceiling, **m-4 routing-raise**}) — and
-  stamps the escalation **reason in the §J A-set** as a `gate_category` on the record.
-  - **The atom (readable + stamped on a *consumable* record):** `HUMAN_GATE_REQUIRED = raised` + the routing
-    escalation `gate_category ∈ A-set` (`human_decision_required` / `routing_unavailable`). m-6 reads these two
-    off the **accepted, consumable** routing record to distinguish an **escalated** routing gate from an
-    **absorbed** (category-B, no raise) one — it does **not** depend on the `route_dispatch()` return value.
+  stamps a **force-A `gate_category`** on the record.
+  - **The atom (readable + stamped on a *consumable* record):** `HUMAN_GATE_REQUIRED = raised` + a **force-A
+    `gate_category`**. The routing-outcome states that **trigger** the raise are `human_decision_required` /
+    `routing_unavailable` — these are **routing-outcome states, NOT §J2 `gate_category` members** (`human_decision_required`
+    is §J2's `other`→A disposition label; `routing_unavailable` is not in §J2). **Force-A resolution (m-4-F7 /
+    x3-seam-byte-integrity-F1, c6):** the escalation's category-A is **guaranteed today** by the hardcoded
+    **`other` → A fail-safe** in ARCHITECTURE §J2 (an unclassified routing-outcome escalation ⇒ human-only) — **no
+    §J2 change is required for correctness.** A **distinct explicit `routing_escalation` A-member** in the §J2
+    default map is *recommended* for precise classification/telemetry and is routed as a **c6 cross-domain carry**
+    (CTO §J2 edit + m-2/m-6 confirm) **before Step-1 PLAN** — a clarity improvement, not a design-lock dependency.
+    m-6 reads `HUMAN_GATE_REQUIRED = raised` + the force-A category off the **accepted, consumable** routing record
+    to distinguish an **escalated** routing gate from an **absorbed** (category-B, no raise) one — it does **not**
+    depend on the `route_dispatch()` return value.
   - **The stamp path:** the routing record rides m-1 `submit()` → stamped `FROM` + lineage-accepted → the raised
     `HUMAN_GATE_REQUIRED` + A-set reason are stamped on the consumable record; m-6 projects A-set to the
     human-surface (Owner Decision Brief: recommendation + enumerated model choices). The A/B category membership
@@ -401,10 +431,21 @@
    disposition (observe-layer bounce, same integrity class as silent-deviation, §2) is the intended grain vs a
    record-level rollup. m-4 DESIGN-internal; does not affect the gate (no model predicate either way).
 3. **m-5 seam disposition** — the concrete archetype-tag + ceiling semantics + template structures/lineup
-   (lock prerequisite, §10). Pending m-5 boot.
+   (lock prerequisite, §10). **RESOLVED/carried** — m-5 booted and confirmed through c4/c5; the concrete
+   semantics are **reserved to c3 (m-5-owned)**, and the c2 m-4 lock preserves that ownership.
 4. **Record-level `evidence_integrity` rollup** — m-3 DESIGN-internal (their reconcile flagged it); does not
    affect the m-4 per-field contract.
-5. **R2-boundary ratification** — orchestrator/VP at the c2 lock (§2; both pairs aligned).
+5. **R2-boundary ratification** — **RESOLVED** — ratified at the c2 lock (readiness-fix-c1/c4 + the design-review
+   reconciles; §2; both pairs aligned); no model-derived schema predicate.
+6. **§2C build-carry (routing lane) — non-locking, gated to Step-1 build (step-(d)) (m-4-F1 / x1-fatal-resolution-F1, c6).**
+   Two routing-lane carries are owed at **Step-1 PLAN**, not at the design lock, and are recorded in the §2C
+   build-carry ledger (ARCHITECTURE §C4 + RECONCILE step-(d) + README): **(i) R2 `gate_referenceable` per-column** —
+   the FieldSpec `gate_referenceable: bool` set **false** on `chosen_model` (and any model-identity column), with
+   **negative fixtures** over `chosen_model` *and* single-family-bucket proxies (a bucket whose only member is one
+   model must not become a model-name gate by proxy); **(ii) altitude-B per-row deviation grain** — the
+   `any(routing_assignments.declared_deviated == true)` aggregate fixture across a multi-seat fan-out (no deviating
+   row escapes justification). Both are **build/fixture** obligations, not record-shape or lock changes; the m-2
+   `gate_referenceable` flag that backs (i) is already landed (m-4-F3, m-2 §5/§12).
 
 ---
 
@@ -433,9 +474,11 @@
   per-seat-at-spawn, orthogonal to m-3's `slot_in` work-archetype (per-record, conductor-classified at
   acceptance) (§8/§10). Concrete tag-space + values stay m-5-owned (c3).
 - **M4-1 (m-6) — CONFIRMED.** The routing B→A escalation reaches the human-surface as a readable force-A atom on
-  a consumable record via the **existing c1 monotonic `HUMAN_GATE` "m-4 routing-raise"** + the §J `gate_category`
-  A-set — **no new gate class**, R2-safe (trigger = routing-outcome state, not a model value). Atom + stamp path
-  named in §10 → m-6. (This was the named lock-blocker-if-unconfirmable; it is confirmed through the existing
+  a consumable record via the **existing c1 monotonic `HUMAN_GATE` "m-4 routing-raise"** + a **force-A
+  `gate_category`** — **no new gate class**, R2-safe (trigger = routing-outcome state, not a model value). The
+  force-A category is guaranteed by §J2's hardcoded **`other`→A fail-safe** today; an explicit `routing_escalation`
+  §J2 A-member is a **c6 cross-domain carry** (CTO §J2 + m-2/m-6 confirm, §10), not a lock dependency. Atom + stamp
+  path named in §10 → m-6. (This was the named lock-blocker-if-unconfirmable; it is confirmed through the existing
   shape.)
 
 ## §16. c5 claim-sweep fold-log (claim-text hygiene — no mechanism change)
@@ -455,6 +498,13 @@
   trusted engine's own control-flow, the licensed class (same as ARCHITECTURE R2 KEEP). **§4 :187** "immutable
   record cannot be reconstructed" = replay-completeness fact, not a malice claim. **Authority-ceiling** statements
   (routes-but-no-write-tool, §8) = confusion-resistant enforcement, kept.
+- **KEEP — late-classified (m-4-F8; missed by the original static grep, token line-wrapped across :127–:128).**
+  **§2 snapshot-provenance :127–:129** "it is `observed` whenever the snapshot is conductor-stamped (by
+  construction, since the floor is the conductor's own policy)" = **observer-side selection the lane cannot reach**
+  (the conductor stamps its own snapshot from its own policy ⇒ the deviation check is `observed` without lane
+  vantage) — trusted-engine control-flow / observer-selected-control class, NOT a malice/write-exclusion claim, so
+  KEEP (wording unchanged). Re-run the c5 sweep greps in **whitespace-tolerant** form (tolerant of line-wrapped
+  `(by\nconstruction`) so line-split tokens are no longer missed.
 
 ## §17. c5 decision-⑤ fold — ODB model-name egress carve-out: R2-guard (m-4 half)
 
@@ -478,3 +528,44 @@
 consistent with the identity≠authority / human-override design, and is categorically distinct from a `model_*`
 predicate in a machine gate. **Peer-bias protection intact** — no gate gains a model input. m-4 co-confirm complete;
 carve-out closes with m-3 (scan) + m-6 (ODB) co-confirms.
+
+## §18. c6 re-review cleanup fold-log (doc-only — no mechanism change, lock invariants unchanged)
+
+Applied the c6 re-review findings routed to m-4 (`c6-fix-m-4`; `master/DESIGN-REREVIEW-2026-07-02.md`
+CONDITIONAL-GO, VP-concurred). **Doc-only consistency folds into the locked doc**; Routing & Policy lock invariants
+(R2, the deviation gate, the record shape, the four sanctioned by-construction claims, the confusion-resistant/
+D5-residual vocabulary) are **unchanged**. Per-finding:
+- **m-4-F1 / x1-fatal-resolution-F1 (B) — §2C build-carry deferral marker.** Added §13 item 6: the two routing-lane
+  build-carries owed at Step-1 PLAN — (i) R2 `gate_referenceable` per-column FieldSpec bool on `chosen_model` +
+  single-family-bucket negative fixtures; (ii) altitude-B per-row deviation grain. Non-locking, recorded in the §2C
+  ledger; the m-2 flag backing (i) is already landed (m-4-F3).
+- **m-4-F4 (B, ◆) — template-spawn authoring model.** §7: `FROM = operator` on the operator-relay channel;
+  `routing_assignments` seat-scope widened to admit `operator` on `template_ref`-bearing records **only**;
+  `declared_deviated` **stays declared** (not re-typed system/computed — the rejected option would reintroduce the
+  readiness-fix-c4 declared-vs-observed conflation). Cross-domain mirror (m-2 §17.3 + m-7 S11) flagged to the
+  orchestrator.
+- **m-4-F5 (B, ◆) — `deviation_reason_code` value-set.** §5/§6: declared **config-sourced enum** (default-seeded +
+  operator-configurable + `other` fail-safe, §J2 pattern); seeded the shipped default vocabulary
+  (`capability_gap` · `cost_budget` · `latency_budget` · `bucket_unavailable` · `operator_directive` ·
+  `experiment` · `other`). m-2 §17.3 value-set mirror flagged.
+- **m-4-F6 (H) — status hygiene.** Header → **LOCKED-at-c2** (+ c5/c6 fold notes); §2 box + §9 R2-boundary marked
+  **RATIFIED**; §13.3 (m-5 seam) + §13.5 (R2-boundary) marked **RESOLVED** with refs.
+- **m-4-F7 / x3-seam-byte-integrity-F1 (H/M) — §J gate_category byte-reconcile.** §10/§15: the escalation stamps a
+  **force-A `gate_category`** guaranteed by §J2's `other`→A fail-safe (no false claim of a non-existent A-member);
+  `human_decision_required`/`routing_unavailable` re-stated as routing-outcome **triggers**, not §J2 members; an
+  explicit `routing_escalation` A-member routed as a c6 cross-domain carry (CTO §J2 + m-2/m-6 confirm) before Step-1.
+- **m-4-F9 (M) — ARCHITECTURE §J1 ⑤ carve-out:** already satisfied by the CTO gov-surface apply (same line as
+  m-6-F5; `ARCHITECTURE §J1:99-102` already annotates the decision-⑤ typed-ODB model-name carve-out) — **verified,
+  not redone.**
+- **m-4-F8 (M) + m-4-F3 (B) — CTO-applied, verified not redone:** F8 late-classified KEEP (§16 :500–506);
+  F3 R2 `chosen_model` re-anchor landed in m-2 §5/§12 (`gate_referenceable` flag).
+
+**Cross-domain mirrors flagged to the orchestrator (m-4 records the decision; does not write those docs):** m-2
+§17.3 (template `operator` seat-scope + `deviation_reason_code` value-set) + m-7 S11 (template author) + the CTO
+§J2 explicit `routing_escalation` A-member (optional clarity carry).
+
+**rev1 (implementer must-revise fold):** (a) **F4 §5 primary FieldSpec row** — the `routing_assignments` owner cell
+now carries the same conditional `operator`-on-`template_ref`-records exception as §7 (the two seat-scope sources no
+longer compete); (b) **F6 header** — the live "one R2-boundary item is escalated … for ratification at the c2 lock"
+sentence past-tensed to "was escalated and RATIFIED at the c2 lock" (no resolved item described as currently
+escalated).
--- a/master/domains/m-4-routing-policy/README.md	2026-07-02 19:49:04
+++ b/master/domains/m-4-routing-policy/README.md	2026-07-02 21:08:26
@@ -151,5 +151,22 @@
   (`…/SITREP-planner-20260702-135001.md`). Global closure needs m-3+m-6 implementer reviews + CTO fold of all three
   halves. m-4 holding. No PLAN.
 
+- C6 RE-REVIEW CLEANUP c6-fix-m-4 (8 doc-only findings → the design-of-record): **folded, → implementer approve**
+  (20260702). Doc-only, no mechanism change, lock invariants unchanged. **B-blockers:** F1/x1 §2C build-carry
+  deferral marker (§13 item 6 — R2 gate_referenceable per-column + single-family-bucket fixtures + altitude-B per-row
+  grain, Step-1-gated); F4 template-spawn authoring = **`FROM=operator`**, seat-scope widened for `template_ref`
+  records, **`declared_deviated` stays declared** (rejected the system/computed re-type — would reintroduce the c4
+  conflation); F5 `deviation_reason_code` = config-sourced enum + seeded default vocabulary. **Hygiene:** F6 status →
+  **LOCKED-at-c2** + R2-boundary RATIFIED (§2/§9/§13); F7/x3 §J `gate_category` byte-reconcile (force-A via §J2
+  `other`→A fail-safe; explicit `routing_escalation` A-member = cross-domain carry). **Verified not-redone:** F8/F3
+  (CTO-applied), F9 (ARCHITECTURE §J1 already carries the ⑤ carve-out). §18 c6 fold-log added. Cross-domain mirrors
+  (m-2 §17.3, m-7 S11, CTO §J2) flagged to orchestrator. Closure: `c6-fix-m-4/DESIGN-planner-20260702-210000.md` →
+  m-4.implementer adversarial review. On approve → c6-fix-m-4 completion to orchestrator → c6 close (VP co-sign) → (e)
+  Step-1 PLAN opens. **m-4.implementer `must-revise`** (`c6-fix-m-4/DESIGN-REVIEW-implementer-20260702-210453.md`) —
+  two valid catches: rev0 fixed §7 but left the primary §5 FieldSpec row + one header sentence with stale text.
+  **rev1 folds both** (`c6-fix-m-4/DESIGN-planner-20260702-211200.md`): §5 `routing_assignments` owner cell now carries
+  the `operator`-on-`template_ref`-records exception (matches §7); header R2 status past-tensed to RATIFIED. All other
+  rev0 folds confirmed. Reissued to m-4.implementer for re-review. No PLAN yet.
+
 ## Layout
 - `audit/` — AUDIT artifacts.  `design/` — DESIGN docs + grill locks.
--- a/master/domains/m-5-workflows-archetypes/design/2026-06-30-archetype-system-design.md	2026-07-02 19:49:04
+++ b/master/domains/m-5-workflows-archetypes/design/2026-06-30-archetype-system-design.md	2026-07-02 20:58:43
@@ -4,7 +4,7 @@
 RUN_ID: master
 CYCLE: c3
 OWNER: m-5 (Workflows & Archetypes) — design-lead m-5.planner
-STATUS: **design-complete** — m-5.implementer DESIGN-REVIEW **approve** (`c3-design-m-5/DESIGN-REVIEW-implementer-20260630-133831.md`); m-5↔m-6 COORD seam **RECONCILED both sides** (m-6 `123022` ⊕ m-5 `131856`; both domain docs matched on the four-class non-gate seam after m-6 withdrew the crossed `131747` path). **Pending the orchestrator/VP c3 design-lock.** Builds on the LOCKED c1+c2 contract (`master/ARCHITECTURE.md` §1–§C2); does NOT reopen m-1..m-4.
+STATUS: **design-complete** — m-5.implementer DESIGN-REVIEW **approve** (`c3-design-m-5/DESIGN-REVIEW-implementer-20260630-133831.md`); m-5↔m-6 COORD seam **RECONCILED both sides** (m-6 `123022` ⊕ m-5 `131856`; both domain docs matched on the four-class non-gate seam after m-6 withdrew the crossed `131747` path). **LOCKED per c3-lock (VP co-sign 20260630-191315).** Builds on the LOCKED c1+c2 contract (`master/ARCHITECTURE.md` §1–§C2); does NOT reopen m-1..m-4.
 GRILL_LOCK_ID: c3-grill-m-5 (folded in §12; operator decisions of 20260630)
 DESIGN_REVIEW_VERDICT: approve — m-5.implementer `c3-design-m-5/DESIGN-REVIEW-implementer-20260630-133831.md` (rev-1 `131617` + rev-2 `132748` must-revise folded; cross-domain seam cleared — both docs on the four-class non-gate model)
 
@@ -85,7 +85,7 @@
 ```text
 authority_ceiling: { write: <lvl>, dispatch: <lvl>, tool: <lvl> }   # initial release
 ```
-Each axis is its own monotonic lattice; consumers (m-4 `route_dispatch`, the F2 per-assignment record) **iterate present axes**; an axis **absent** on an older record falls to that axis's **declared absent-default** (read-time-migrator-friendly; consistent with the locked append-only store + zero-migrator versioning).
+Each axis is its own monotonic lattice; consumers (m-4 `route_dispatch`, the F2 per-assignment record) **iterate present axes**; an axis **absent** on an older record falls to that axis's **declared absent-default** (read-time-migrator-friendly; consistent with the locked append-only store + zero-migrator versioning). **Every axis MUST declare an absent-default, and it is that axis's most-restrictive floor (fail-closed): `write → read_only`, `dispatch → none`, `tool → none`** (and the deferred `external_send → none`, below). A record-absence therefore reads as the **minimum grant on that axis, never an escalation** — monotonic-tightening-consistent, so an old or partial record can only ever be *under*-authorized, never silently widened.
 
 **The three initial-release axes (partial order — NOT a single ladder):**
 1. **write:** `read_only < write_scratch < write_feature < merge_feature < merge_protected`(A-gated). Merge folds in as the top tiers.
@@ -102,14 +102,14 @@
 
 | seat_archetype | write | dispatch | tool | routing-prior (m-4 bucket) | behavioral mode | default human-mode (posture) |
 |---|---|---|---|---|---|---|
-| sensor | read_only | none | none | fast_cheap | answer-once | away/side (§8) |
+| sensor | read_only | none | none | fast_cheap | answer-once | away |
 | solo_worker | write_feature | none | exec | coding | execute | interactive |
 | implementer | write_feature | none | exec | top_tier_coding | execute | interactive |
-| planner | read_only | route(design-author) | read | strong_reasoning | plan/design | interactive |
+| planner | read_only | route | read | strong_reasoning | plan/design | interactive |
 | reviewer | read_only | none | read | strong_general | adversarial | interactive |
 | orchestrator_lead | read_only | route | read | strong_reasoning | decompose/route | interactive |
 
-(`actuator` = a derived class: `write_feature`+`exec` + heavy gates, §7.2.) Ceilings are MAX points; templates tighten. The routing-prior is the m-4 capability-prior bucket keyed on `seat_archetype` (m-4-owned; m-5 supplies the key).
+(`actuator` = a derived class: `write_feature`+`exec` + heavy gates, §7.2.) Ceilings are MAX points; templates tighten. The routing-prior is the m-4 capability-prior bucket keyed on `seat_archetype` (m-4-owned; m-5 supplies the key). Dispatch cells carry only lattice tokens (`none < route`, §5); the `planner`'s `route` is scoped to design-author dispatch by the T2 template (§7), a template tightening expressed outside the axis value, not a distinct dispatch point.
 
 ---
 
@@ -120,7 +120,7 @@
 
 | # | template | seats / topology | panes | gate-set | read-only-ness | model slots |
 |---|---|---|---|---|---|---|
-| **T1 solo** | 1 `solo_worker` (or role-specific `implementer`); no peer | 1 | by `slot_in`; observe-as-send always on | mutating, bounded by ceiling | 1 (coding bucket) |
+| **T1 solo** | 1 `solo_worker` (or role-specific `implementer`); no peer | 1 | by `slot_in`; observe-as-send gate always-on (the `slot_in` invariant *families* bite from Step-2, §9) | mutating, bounded by ceiling | 1 (coding bucket) |
 | **T2 adversarial_pair** | `planner` ↔ `implementer`; review/fold lineage | 2 | + design/plan/review/fold by phase; `slot_in` work-invariant | planner read/design-leaning | planner=strong_reasoning, implementer=top_tier_coding (diverse-model adversarial) |
 | **T3 sensor** | 1 `sensor`; non-interrupting side-branch, NOT in delivery lineage | 1 side-surface | none (read-only ⇒ no delivery/merge gate) | strict read-only, tool-blocked, 1-turn | fast_cheap |
 
@@ -160,6 +160,7 @@
 The archetype system DESIGN is Step-1-rideable as a **recorded governance contract**; ceiling ENFORCEMENT is **tiered**:
 - **Step 1 (ride existing runtimes):** archetype + ceiling DECLARED + recorded per-assignment (F2) + enforced **best-effort by host config** — real on claude-code's agent-type `tools[]`/`disallowedTools[]` allowlist (`agentToolUtils.ts:122-225`); partial/host-dependent elsewhere. The governance VALUE (recorded, auditable, fail-closed routing-refusal) holds regardless.
 - **Steps 4-5 (standalone runtime):** the conductor enforces ceilings + topology uniformly (ROADMAP Step-5 "a human picks a workflow at spawn that sets the authority ceiling"). The literal single-action actuator (§7.2) + the `external_send` axis (§5) + uniform tool-blocking specifically need this.
+- **Observe-invariant phasing (mirrors the ceiling tiering — resolves the §7-T1-vs-§9 read):** the **observe-as-send gate mechanism is always-on from Step-1** (it is the locked m-3 send-gate; every send observes). The per-`slot_in` observe-invariant **families** (§4/§7 — refactor `test-files-unchanged`, bugfix `red→green`, …) **bite from Step-2**, when m-3's observe pipeline + done-predicates land. Step-1 records the `slot_in` classification (F1) and rides the always-on gate; the invariant families enforce as m-3 ships them — the same **declared-now / enforced-as-the-tier-lands** shape as the ceiling above. So T1's "observe-as-send always-on" (§7) names the *gate mechanism* (Step-1), not a claim that every `slot_in` family predicate is enforced pre-Step-2.
 - **No re-cut:** the ceiling is already recorded per-assignment (F2) as an open named-axis map (§5); standalone enforcement reads the same fields. **Dependency routed to the orchestrator** as a later-step build concern.
 
 ---
@@ -195,7 +196,7 @@
 
 Rejected / deferred:
 - literal single-action actuator seat in the initial release — deferred (not ride-runtime-enforceable; Step-4/5).
-- `external_send` as an initial-release seat axis — deferred (no initial-release consumer; conductor sole-sender); reserved modular.
+- `external_send` as an initial-release seat axis — deferred (no initial-release consumer; conductor = the only external sender the *governance surface* offers, D5 residual); reserved modular.
 - conductor/N-pair template in the initial release — deferred to Step-5 (no initial-release consumer).
 - any m-2 `required_when`/`visible_when` over concrete tag values — rejected (C2.4 no micro-fold).
 
@@ -217,11 +218,12 @@
 ## 15. c5 claim-sweep fold (claim-text hygiene — NO mechanism change, NO locked-contract reopen)
 Applied the ratified confusion-resistant checklist (`c5-claim-sweep-architecture/DESIGN-orchestrator-planner-20260702-131320`, VP-approved) to this doc under `c5-claim-sweep-light`. **Claim-text only; every mechanism, lock (GL-1..GL-6), and F1/F2 invariant is byte-unchanged in substance.** Full-net classified survivor list:
 
-**RELABELED → confusion-resistant + D5 residual (4):**
+**RELABELED → confusion-resistant + D5 residual (5):**
 - **§4 heading (`:62`)** "tamper-resistance proof" → "confusion-resistance proof (observer-selected control property)" — keeps the observer-selection substance; drops the malicious-containment word.
 - **§4 proof (`:78`)** "tamper-resistant invariants cannot be escaped by lane re-tagging" / "the whole tamper-resistance" → "a *confused* lane has no tool to re-tag its `slot_in`"; **KEPT** the observer-selected-control framing (the licensed KEEP class, ARCHITECTURE §C3:277) + added the explicit **D5 residual** (a genuinely malicious seat writing a false store record / reaching a shell under attach is out of the initial release scope, bounded by m-1 isolation + Step-4/5 tool-ceiling).
 - **§1 F1 ref (`:19`)** "non-lane-writable" → scoped to confusion-resistant (no lane tool writes/re-tags) + D5 residual pointer; mirrors ARCHITECTURE F1 relabel.
 - **§5 external_send (`:97`)** "the conductor is the **sole external sender**" → "the only external sender the *governance surface* offers" + D5 residual (same-uid shell/curl out of scope); mirrors the m-3 chokepoint / ARCHITECTURE §C2 egress relabel. *(This hit was not in the orchestrator's m-5 census; caught by the required own full-net grep.)*
+- **§12 Rejected/deferred (`:198`)** the hyphenated shorthand "conductor **sole-sender**" → "conductor = the only external sender the *governance surface* offers, D5 residual"; same claim class as §5 `:97`, matched by extending the sweep net to the hyphenated forms ("sole-sender", "sole sender"). *(Surfaced in the c6 seam-byte-integrity pass; the c5 regex net did not catch the hyphenated shorthand.)*
 
 **KEEP (justified — no edit):**
 - **§5 ceiling (`:92`)** "an `orchestrator_lead` routes but cannot write" — **authority-ceiling** (the dispatch axis grants no write *tool*); the checklist's named KEEP exemplar (`131320:32`).
@@ -231,4 +233,20 @@
 - **§6/§7 (`:109`,`:124`)** "adversarial" (reviewer / T2 adversarial_pair) — diverse-model **review posture**, not a security-containment claim. KEEP.
 - **§9 negative-fixture names (`:207`)** "re-tag-to-escape attempt **blocked** by conductor-classification" / "sensor that **cannot upgrade-in-place**" — negative **fixture** names (cf. ARCHITECTURE `:125` KEEP): they name what the fixture asserts (the observer-selected classification property + the sensor authority-ceiling); inherit the same confusion-resistant scoping + D5 residual as the underlying claims they test. KEEP.
 
-Invariants: R2 held (no m-5 field is gate/model-referenceable); C2.4 held (no tag-value micro-fold); F1/F2 substance unchanged; no m-4 record / `ARCHITECTURE` change; no c2/c3 reopen. m-5.implementer semantic-approve pending under `c5-claim-sweep-light`.
+Invariants: R2 held (no m-5 field is gate/model-referenceable); C2.4 held (no tag-value micro-fold); F1/F2 substance unchanged; no m-4 record / `ARCHITECTURE` change; no c2/c3 reopen. m-5.implementer semantic-approve **recorded** — verdict approve (`c5-claim-sweep-light/DESIGN-REVIEW-implementer-20260702-134809`).
+
+---
+
+## 16. c6-fix-m-5 fold (re-review cleanup — DOC-ONLY; NO mechanism change, NO design-lock reopen)
+Applied the `c6-fix-m-5` dispatch (`c6-fix-m-5/DESIGN-orchestrator-planner-20260702-204512`; source `master/DESIGN-REREVIEW-2026-07-02.md` §5 + appendix, VP-concurred CONDITIONAL-GO). **Consistency folds INTO the locked doc; lock invariants (GL-1..GL-6, F1/F2, R2, C2.4) unchanged.** Preserves the four sanctioned by-construction claims, the swept confusion-resistant / D5-residual vocabulary, and the byte-exact `{accepted, rejected, held}` enum.
+
+**Applied (m-5-owned, my domain judgment):**
+- **m-5-F6 (M, cross-doc-contradiction — §7 T1 vs §9):** T1's "observe-as-send always on" read as contradicting §9's tiered/best-effort Step-1 framing. **Fix:** annotated §7 T1 cell + added a §9 phasing bullet — the observe-as-send *gate mechanism* is always-on from Step-1; the per-`slot_in` observe-invariant *families* bite from **Step-2** (when m-3's observe pipeline lands), mirroring the ceiling's declared-Step-1 / enforced-later tiering. No new mechanism; a phase annotation only.
+- **m-5-F7 (C/OBS, gap — §5):** §5 mandated per-axis absent-defaults but declared one only for the deferred `external_send` axis. **Fix:** folded one sentence into §5 — every axis MUST declare an absent-default = its most-restrictive floor (fail-closed): `write→read_only`, `dispatch→none`, `tool→none` (`external_send→none`); a record-absence reads as the minimum grant, never an escalation. Makes explicit what GL-2's monotonic-tightening already implied; no lattice change.
+
+**Coordinated (seam, addressed COORD not a self-edit):**
+- **m-7-F2 / x3-seam-byte-integrity-F2 (B, CTO:seam — trusted-config author set omits m-5):** the trusted-config artifact (CQ-4b: per-domain sections, each stamped, one top-level digest) named the m-5-owned archetype registry but omitted m-5 from its author set. **m-5 CQ-4b confirm** filed to m-7 (`c6-fix-m-5/COORD-planner-20260702-…`): the archetype registry (§3 tag-space + §6 ceiling composition + §7 templates + §5 ceiling maps) is an **m-5-authored, m-5-stamped section** of the artifact, loaded at genesis (already my §13 config-sourced-data carry) — no mechanism change, consistent with the c3 lock. m-7 amends its §7/S15 + ARCHITECTURE C4.1 (their doc, their dispatch `c6-fix-m-7`).
+
+**Verified CTO-already-applied (did NOT redo):** m-5-F4 (status header → LOCKED per c3-lock, `:7`); m-5-F5 (§6 token normalization — tokens lower_snake_case/consistent); x2-claim-honesty-F5 + x3-seam-byte-integrity-F6 (§12 `:198` hyphenated "sole-sender" relabel + §15 survivor-list entry, `:225`). All present and correct.
+
+**Flagged out-of-scope (NOT applied — scope confirm requested from CTO):** **m-5-F2** (posture-vocab away-trigger) is named in the dispatch's canonical-resolution guidance but `owner: CTO:seam`, absent from the dispatch's "Your findings" table + the "3 findings" framing, and absent from the CTO-already-applied list. posture is a **locked c3 vocabulary** and the away-mode trigger touches the m-6-owned `away_bridge`/Seam-C boundary — so I did not re-record it unbidden. If the m-5 posture-vocab slice is mine, route it as a bounded seam COORD and I will apply within the locked lattice + coordinate with m-6.
--- a/master/domains/m-6-human-surface-scheduler/design/2026-06-30-human-surface-scheduler-design.md	2026-07-02 19:49:04
+++ b/master/domains/m-6-human-surface-scheduler/design/2026-06-30-human-surface-scheduler-design.md	2026-07-02 20:58:10
@@ -3,7 +3,7 @@
 DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
 OWNER: m-6 (Human Surface & Scheduler) — pair: `m-6.planner` (lead) + `m-6.implementer` (adversarial design-reviewer)
 PHASE: DESIGN (c3) — design-lock is terminal; no PLAN/IMPL this cycle
-STATUS: DESIGN-LOCK-READY (r3) — m-6.implementer APPROVED (`133839`, rev-2); seam converged (`123022`⊕`131856`); **Seam C RESOLVED (A)** — away-token cell folded over m-1-owned mint/verify + locked (`182600`/`183008`); GRILL_LOCK §9 folded; all cells closed. **c4-cq-gateconfig:** CQ-3 (pure-judgment A-floor table, canonical home `c4-cq-gateconfig/DESIGN-planner-20260702-015800`), CQ-4, CQ-4b — m-6 legs **pair-approved** (`024620`); the CQ-4 terminal-token alignment (`delivery_state {accepted,rejected,held}`; `bounced` retired → `rejected`; `held`→bucket A) is **folded into §2/§4** here so the c3 lock consumes clean tokens. **c5:** claim-sweep relabels folded (§2/§4/§11, claim-text only, `c5-claim-sweep-light`); **operator decision ③ (RAISE-ONLY + known-A detector) folded into §2** (`c5-fold-decision-3`); **decision ④ (ROTATE+RE-OBSERVE) recorded as a non-locking §2C/§12 build-carry** (mechanism at step d, `c5-fold-decision-4`); **decision ⑤ (ODB model-name egress carve-out) folded into §3** (joint m-3/m-4, `c5-fold-decision-5`). Awaiting only the orchestrator's c3 lock (seam-of-record + Seam C + GRILL_LOCKs) → VP co-sign → close. No PLAN/IMPL (c3 terminal = design-lock).
+STATUS: **c3 LOCKED** (`c3-lock` VP co-sign `RECONCILE-orchestrator-reviewer-20260630-191315`; orchestrator lock `190627`/`191525`) — pair-approved `133839` (rev-2); seam converged (`123022`⊕`131856`); **Seam C RESOLVED (A)** — away-token cell folded over m-1-owned mint/verify + locked (`182600`/`183008`); GRILL_LOCK §9 folded; all cells closed. **c4-cq-gateconfig:** CQ-3 (pure-judgment A-floor table, canonical home `c4-cq-gateconfig/DESIGN-planner-20260702-015800`), CQ-4, CQ-4b — m-6 legs **pair-approved** (`024620`); the CQ-4 terminal-token alignment (`delivery_state {accepted,rejected,held}`; `bounced` retired → `rejected`; `held`→bucket A) is **folded into §2/§4** here so the c3 lock consumes clean tokens. **c5:** claim-sweep relabels folded (§2/§4/§11, claim-text only, `c5-claim-sweep-light`); **operator decision ③ (RAISE-ONLY + known-A detector) folded into §2** (`c5-fold-decision-3`); **decision ④ (ROTATE+RE-OBSERVE) recorded as a non-locking §2C/§12 build-carry** (mechanism at step d, `c5-fold-decision-4`); **decision ⑤ (ODB model-name egress carve-out) folded into §3** (joint m-3/m-4, `c5-fold-decision-5`). Post-lock review-driven consistency folds — c4 CQ (`c4-cq-*`), c5 sweep+decisions (`c5-*`), c6 cleanup (`c6-fix-m-6`) — recorded in the §13 fold-log; **lock invariants unchanged**. No PLAN/IMPL (c3 terminal = design-lock).
 BASIS: VP-approved reconciled audit `c3-audit-m-6` (`RECONCILE-planner-20260630-054107`); dispatch `c3-design-m-6/DESIGN-orchestrator-planner-20260630-121325`
 SEAM: m-5↔m-6 resolved in `c3-design-m5-m6-coord` — **m-6 bind `123022` ⊕ m-5 confirm `131856`** (the converged FOUR-class model below; the interim `125604`/`131747` `{verdict,fyi,collaborate}` excursion crossed `123022` and was **retracted both sides** per m-5 SITREP `132314` + m-5.implementer must-revise `131617`)
 GRILL_LOCK: §9 (operator decisions, 2026-06-30) — referenced from this DESIGN_LOCK
@@ -34,23 +34,23 @@
 
 | Bucket | Trigger (LOCKED writer) | Reaches operator? | Surface behavior | Capture/return |
 |---|---|---|---|---|
-| **A — verdict-required** | `gate_category ∈ §J A-set` (`ARCHITECTURE.md:94`) ∪ `HUMAN_GATE_REQUIRED=raised` (incl. **m-4 routing-raise**, `m-4 …design.md:339-344`) ∪ `egress_scan_result=blocked` (`m-3 …design.md:116-122`) | **YES** — primary decision queue | **ODB** (§3); park; J1 resummon; non-suppressible (monotonic floor) | bounded-choice (buttons) → operator-FROM verdict relay |
-| **B — orchestrator-absorbed** | `gate_category ∈ §J B-set` (`:96`) | **No** (→ A only if a monotonic raiser promotes it) | **live local digest, non-interrupting** (GRILL_LOCK G3) | none (orchestrator resolves) |
+| **A — verdict-required** | `gate_category ∈ §J A-set` (`ARCHITECTURE.md §J2 A-set`) ∪ `HUMAN_GATE_REQUIRED=raised` (incl. **m-4 routing-raise**, `m-4 …design.md:339-344`) ∪ `egress_scan_result=blocked` (`m-3 …design.md:116-122`) | **YES** — primary decision queue | **ODB** (§3); park; J1 resummon; non-suppressible (monotonic floor) | bounded-choice (buttons) → operator-FROM verdict relay |
+| **B — orchestrator-absorbed** | `gate_category ∈ §J B-set` (`ARCHITECTURE.md §J2 B-set`) | **No** (→ A only if a monotonic raiser promotes it) | **live local digest, non-interrupting** (GRILL_LOCK G3) | none (orchestrator resolves) |
 | **C — CC-FYI** | operator on **CC** not TO (`README.md:49-59`; export `:37`) | **Informational** | low-priority FYI feed; batchable | none (CC creates no obligation) |
 | **D — observe-bounce** | observe-veto: predicate-fail / integrity-veto / form-bounce → `delivery_state=rejected` + `failing_edge` (`m-2 …design.md:256-258`; `m-3 …design.md:61-63`) | **No — author-facing** | returns to the **authoring seat** with the named failing edge | author repairs + resubmits |
 
 **Direction (DQ-3 resolved at grill):** D **stays in the taxonomy** (it is a delivery outcome with a locked writer) but is **tagged author-facing** and **never enters the operator decision queue** (unless the failed edge is itself an A-gate). Only A (alert) and C (notification) reach the operator — the alert-vs-notification split is a property of the **bucket grammar** (it keys on the locked writer + TO/CC direction, not on seat assertion).
 
-**Classification-direction invariant (operator decision ③, RAISE-ONLY — folded c5, `READINESS-REGISTER.md` §Operator-decisions ③).** The `gate_category` agent-pick is **monotonic toward A**: an agent may escalate a B-category to A (more operator oversight), but may **never de-classify an A-worthy decision down to B**. This composes with the CQ-3 A-floor and m-2's monotonic-MAX as the *direction* rule on top of the *floor*, enforced at the same fill/submit point (m-7). **Known-A detector:** a system-side membership list of categories that force **≥A regardless of agent pick** — = the §J2 A-set (the A-floor table's anchoring `gate_category`s, `c4-cq-gateconfig/DESIGN-planner-20260702-015800`). A record whose agent-picked category ∈ known-A but is routed/absorbed as B is **rejected and raised to A**, category recorded — never silently orchestrator-absorbed. The membership is **operator-tunable config** (same pattern as the A-floor table + §J2), with the `other`→A fail-safe hardcoded. This closes the most direct operator-not-surfaced vector (an A-worthy decision mis-tagged B). *(m-7 NF fixture: a B-pick / B-absorb over a known-A category ⇒ raised to A + `gate_category` recorded; §J ratifies the direction-invariant as a J1/J2-analogous addition — no new gate class, no new mechanism, a direction constraint on the existing HUMAN_GATE monotonic-raise pick.)*
+**Classification-direction invariant (operator decision ③, RAISE-ONLY — folded c5, `READINESS-REGISTER.md` §Operator-decisions ③).** The `gate_category` agent-pick is **monotonic toward A**: an agent may escalate a B-category to A (more operator oversight), but may **never de-classify an A-worthy decision down to B**. This composes with the CQ-3 A-floor and m-2's monotonic-MAX as the *direction* rule on top of the *floor*, enforced at the same fill/submit point (m-7). **Known-A detector:** a system-side membership list of categories that force **≥A regardless of agent pick** — = the §J2 A-set (the A-floor table's anchoring `gate_category`s, `c4-cq-gateconfig/DESIGN-planner-20260702-015800`). A record whose agent-picked category ∈ known-A but is routed/absorbed as B is **rejected and raised to A**, category recorded — never silently orchestrator-absorbed. The membership is **operator-tunable config** (same pattern as the A-floor table + §J2), with the `other`→A fail-safe hardcoded. This closes the most direct operator-not-surfaced vector (an A-worthy decision mis-tagged B). *(m-7 enforcement flagged, fixture registration owed: the direction-invariant NF fixture — a B-pick / B-absorb over a known-A category ⇒ raised to A + `gate_category` recorded — is an owed m-7-side artifact bound to Step-1 build, NOT yet registered in m-7's design-of-record (locked 2026-07-02, before this ③ fold); until it lands this doc does not claim a registered m-7 fixture. §J ratifies the direction-invariant as a J1/J2-analogous addition — no new gate class, no new mechanism, a direction constraint on the existing HUMAN_GATE monotonic-raise pick.)*
 
-**Terminal-token → bucket map (CQ-4 settled: `delivery_state {accepted, rejected, held}`, `bounced` retired — m-2 §17.1 `014626`; m-7 §6 enum; m-6.impl approve `024620`).** `accepted` → normal delivery (into the addressed mailbox; if the record is an A-gate → ODB **park**, §3/§4). `rejected` → **bucket D** author-return (`failing_edge` names the acceptance-stage veto). `held` → **bucket A** — the ODB/park A-lane (operator-visible, **non-suppressible**), covering the post-commit gate-pending hold **and** the m-7 §6 internal-fault-on-authority disposition; **`held` never routes to bucket D.** An egress-block on an already-`accepted` A-gate's away-mirror is a distinct outbox event (`egress_scan_result=blocked` → `egress_blocked`, §4 → A local resummon), not a terminal token — the precedence below is unchanged.
+**Terminal-token → bucket map (CQ-4 settled: `delivery_state {accepted, rejected, held}`, `bounced` retired — m-2 §17.1 `014626`; m-7 §6 enum; m-6.impl approve `024620`).** `accepted` → normal delivery (into the addressed mailbox; if the record is an A-gate → ODB **park**, §3/§4). `rejected` → **bucket D** author-return (`failing_edge` names the acceptance-stage veto). `held` → **bucket A** — the ODB/park A-lane (operator-visible, **non-suppressible**), the fault / fail-closed disposition only (m-7 §6 internal-fault-on-authority + CQ-2 class-conditional fail-closed); ordinary `HUMAN_GATE_REQUIRED` A-gate parking is `accepted`, **not** `held` (m-2 §17.1); **`held` never routes to bucket D.** An egress-block on an already-`accepted` A-gate's away-mirror is a distinct outbox event (`egress_scan_result=blocked` → `egress_blocked`, §4 → A local resummon), not a terminal token — the precedence below is unchanged.
 
 **Egress-failure precedence — A vs D, no double-assignment [resolves m-6.implementer must-revise F2 `131702`].** Both an egress block and an observe-veto are "vetoes" in m-3's general sense, so the buckets key on the **`failing_edge` + pipeline stage** to keep them mutually exclusive:
 - **D (author bounce)** = a relay rejected at **acceptance**: `delivery_state=rejected`, `failing_edge ∈ {form-validation, lineage, observe-predicate, declared-vs-observed integrity-veto}` (m-3 §3.2(a)(b)). The relay never delivered; the **author** repairs.
 - **A (operator, local resummon)** = an **already-accepted A-gate** whose **external away-mirror** was blocked by the egress scan: `failing_edge = egress` at the conductor-governed egress chokepoint (m-3 §3.2(c)/§7; governance-surface, not system-level sole-egress — D5 residual). The gate is **valid and still parked** awaiting the operator's verdict; only its external delivery was blocked ⇒ it **parks locally + raises an A resummon** (the `egress_blocked` state, §4) — **never auto-redact/send, never a D author-bounce** (the author did nothing wrong).
 - **Precedence:** egress is evaluated **only** at the external-send chokepoint on an **already-A** gate; the D bounces are evaluated at **acceptance**. Different stage + different `failing_edge` ⇒ a single external-send egress failure resolves to **A (local resummon)**, never D. Local-only relays never reach the egress stage. (An egress-blocked **B**-digest mirror simply **stays local** — no external send, no operator decision, since B is orchestrator-absorbed.)
 
-**Surface realization (Step-2 mechanism / Step-4 UX):** buckets = **saved queries over tags, not exclusive containers** (an item may be `gate`+`m-4`+`urgent`); Gmail-taxonomy preset; additive-score→sort (oldest/stalled first). The **§J A/B map + protected-branch set are operator-config** (`ARCHITECTURE.md:89-92`); `other`→A fail-safe inherited (`:98`), never surface-overridden.
+**Surface realization (Step-2 mechanism / Step-4 UX):** buckets = **saved queries over tags, not exclusive containers** (an item may be `gate`+`m-4`+`urgent`); Gmail-taxonomy preset; additive-score→sort (oldest/stalled first). The **§J A/B map + protected-branch set are operator-config** (`ARCHITECTURE.md §J2`); `other`→A fail-safe inherited (`ARCHITECTURE.md §J2 fail-safe`), never surface-overridden.
 
 ---
 
@@ -58,12 +58,12 @@
 
 **Render (promoted 7-field bundle + locked evidence):** plain-language change + who-benefits · why-now · **`completed_proof` = conductor-observed `evidence_ref`** with **`record_integrity {observed|self_reported|mixed}` shown** (`m-3 …design.md:107-112`) · tradeoffs/residual-risk/scope · **opinionated recommendation** (agent-scripts `:69`) · **exact bounded choices**. Plus envelope/provenance (`subject_ref`, FROM/TO/PARENT/DISPATCH_ID, thread context — `m-2 …design.md:271-276`) and the actionable-brief enrichments (blast-radius, severity tier, length-capped headline).
 
-**Model-name egress carve-out (operator decision ⑤ — folded c5, `c5-fold-decision-5`; joint m-3 scan + m-6 ODB + m-4 R2-guard).** When an ODB legitimately surfaces a model-name to the operator (e.g. a routing/deviation brief), m-6 renders it in a **typed, exempt-marked field** (`record_kind = ODB`, operator-facing, field = model-name) so m-3's egress scan applies the **confidentiality-class carve-out only here** — every other external send still blocks model-names, and the **safety/content class is NOT exempted**. Transport gating is unchanged: **non-away, the ODB renders locally and never egresses**; **away-mode, the away-bridge opt-in + egress gate still govern the send** (the carve-out only relaxes the *confidentiality* scan on this one operator-facing field, not the opt-in or the safety scan). **R2 is UNTOUCHED** — the model-name stays bookkeeping/payload in the render, **never a gate input** (this is an *egress-confidentiality* relaxation, not a *gate-referenceability* change; m-4 co-confirms). *(m-7 egress-fixture note: the ODB model-name field passes the confidentiality carve-out; all other model-name egress still blocked.)*
+**Model-name egress carve-out (operator decision ⑤ — folded c5, `c5-fold-decision-5`; joint m-3 scan + m-6 ODB + m-4 R2-guard).** When an ODB legitimately surfaces a model-name to the operator (e.g. a routing/deviation brief), m-6 renders it in a **typed, exempt-marked field** (`record_kind = ODB`, operator-facing, field = model-name) so m-3's egress scan applies the **confidentiality-class carve-out only here** — every other external send still blocks model-names, and the **safety/content class is NOT exempted**. Transport gating is unchanged: **non-away, the ODB renders locally and never egresses**; **away-mode, the away-bridge opt-in + egress gate still govern the send** (the carve-out only relaxes the *confidentiality* scan on this one operator-facing field, not the opt-in or the safety scan). **R2 is UNTOUCHED** — the model-name stays bookkeeping/payload in the render, **never a gate input** (this is an *egress-confidentiality* relaxation, not a *gate-referenceability* change; m-4 co-confirms). *(flagged to m-7; egress-fixture registration owed — the ⑤ carve-out fixture (the ODB model-name field passes the confidentiality carve-out; all other model-name egress still blocked) is an owed m-7-side artifact bound to Step-1 build, NOT yet registered in m-7's design-of-record; carried in the same build-carry ledger as decision-④ until it lands. Mirrors the m-6-F1/③ "fixture registration owed" fix, §2.)*
 
 **Capture (GRILL_LOCK G2) — buttons + elaborate-more, never a bare decision:**
 - The bounded choices render as **buttons** (one per enumerated choice). The choice-set is a **self-describing, capability-scoped gate** realized as m-2 **`agent_enum_pick`** (the adaptive-routing pillar note) — the operator can pick **only a legal verdict**.
-- A picked button → an **operator-FROM verdict relay** (R1, `ARCHITECTURE.md:65`) that wakes the parked lane (§4). Optional **free-text note rides *with* a choice** — never a new hidden choice, never a gate-lowering.
-- **J1 throughout:** `hold_and_resummon`; **refresh-before-resummon** (agent-scripts `:57` — never re-ask an answered/stale gate; re-observe ground-truth); **never auto-approve** (`ARCHITECTURE.md:82-83`).
+- A picked button → an **operator-FROM verdict relay** (R1, `ARCHITECTURE.md §4 R1`) that wakes the parked lane (§4). Optional **free-text note rides *with* a choice** — never a new hidden choice, never a gate-lowering.
+- **J1 throughout:** `hold_and_resummon`; **refresh-before-resummon** (agent-scripts `:57` — never re-ask an answered/stale gate; re-observe ground-truth); **never auto-approve** (`ARCHITECTURE.md §J1`).
 
 **"Elaborate more" = a context-preserving collaboration fork (operator-initiated governance→collaboration bridge):**
 - When the operator chooses **elaborate more** instead of a verdict button, the conductor **forks the parked lane's stream/context** into an **interactive elaboration session** — seeded with the lane's **running context** (shared context/prompt-cache; the `runForkedAgent` prior art, `jcode-ux-notes.md:48-53`) so the operator elaborates with full context, no re-explaining.
@@ -81,19 +81,19 @@
 **The 7-state machine (on the durable store):**
 `active → parked_waiting_human` (A-gate accepted, ODB emitted) `→ resummon_due` (cadence point, no valid reply) `→ replied_pending_validation` (operator reply captured; form/lineage/choice + operator-FROM channel validated) `→ resumed` (valid reply appended + delivered to the woken lane; **m-3 re-observe on wake**). Plus `bounced_repair` (an acceptance-stage veto → `delivery_state=rejected` terminal → bucket D author-return; the FSM label stays m-6-local, the terminal token is `rejected`) and `egress_blocked` (away-mode outbound scan blocked → park locally + resummon, **never auto-redact/send**).
 
-A `delivery_state=held` terminal (post-commit gate-pending, incl. the m-7 §6 internal-fault-on-authority hold) is **not an m-6 park/wake FSM transition** — it is an m-7-owned runtime disposition that m-6 **renders on bucket A** (operator-visible, non-suppressible, never auto-accept) and resolves by re-run or operator verdict. The 7-state machine stays as above (CQ-4 alignment: `held`→A surface, `rejected`→D).
+A `delivery_state=held` terminal (the fault / fail-closed disposition only — m-7 §6 internal-fault-on-authority + CQ-2 class-conditional fail-closed; ordinary A-gate parking is `accepted`, **not** `held`) is **not an m-6 park/wake FSM transition** — it is an m-7-owned runtime disposition that m-6 **renders on bucket A** (operator-visible, non-suppressible, never auto-accept) and resolves by re-run or operator verdict. The 7-state machine stays as above (CQ-4 alignment: `held`→A surface, `rejected`→D).
 
-**Resummon cadence (GRILL_LOCK G4):** two timers (no-response vs answered-but-stalled); **escalate the SUMMON CHANNEL** (local → louder local → away-bridge if opted-in) → **never escalates to a verdict** (J1). **No hard deadline by default**; the gate waits indefinitely for the operator rather than ever auto-approving. Optional per-gate block-ceiling (a gate may make itself more conservative — block, not approve; monotonic, `ARCHITECTURE.md:83`).
+**Resummon cadence (GRILL_LOCK G4):** two timers (no-response vs answered-but-stalled); **escalate the SUMMON CHANNEL** (local → louder local → away-bridge if opted-in) → **never escalates to a verdict** (J1). **No hard deadline by default**; the gate waits indefinitely for the operator rather than ever auto-approving. Optional per-gate block-ceiling (a gate may make itself more conservative — block, not approve; monotonic, `ARCHITECTURE.md §J1`).
 
 **Away-mode external-inbox bridge (GRILL_LOCK G1; opt-in; the one new design):**
 - **Trigger:** operator declares **`away` posture** (m-5-declared, the activation condition).
-- **Destination policy (operator-chosen):** **A-gates mirror live** + **B-items mirror as a frequent digest** (operator-config interval, **default sub-daily**); **C stays local**. Opt-in; default = local in-app inbox (`ARCHITECTURE.md:84-87`).
+- **Destination policy (operator-chosen):** **A-gates mirror live** + **B-items mirror as a frequent digest** (operator-config interval, **default sub-daily**); **C stays local**. Opt-in; default = local in-app inbox (`ARCHITECTURE.md §J1 away-mode bridge`).
 - **First external send ⇒ egress-gated fail-closed** by m-3 §7 (`m-3 …design.md:116-122`); any block → `egress_blocked` → local resummon.
 - **`away_bridge_eligible` = an m-6-owned per-gate boolean**, defaulted by the locked policy (A-bucket ∧ opt-in ∧ egress-clear); m-5 declares only the `away` posture (seam-converged `123022`⊕`131856`). A hard per-archetype never-bridge ceiling is a **reserved future m-5 hook** (§10 OQ-3), not needed for the initial release.
 - **✅ RESOLVED (Seam C = A) — the inbound verdict-token bridge.** The bridge converts **untrusted SMTP/IMAP-inbound → a trusted operator-channel verdict record** via a conductor-minted **signed, one-time, per-`(decision_id, seat, choice)` token**; the token signature **is** the operator-channel stamp brought inbound (no new trust *model*; **POST/interstitial, never GET**; replay-nonce ≠ validity-window; fail-closed). **m-1 answered (A)** (`c3-design-m5-m6-coord/COORD-planner-20260630-182600`; orchestrator-accepted `183008`): the mint/verify surface is **TCB-owned**, *forced* by **DI-1** (nonce-burn = an atomic conductor-owned store append; confused lanes have no store-write *tool* — D5 residual: same-uid direct store write out of scope, per m-1's c5 relabel) + **DI-2** (signing key = a TCB secret; m-6 custody would be a forbidden second identity authority). It is the **first activation of m-1's reserved `certification` (DKIM-analog) field** — additive, zero schema change, no new public verb, **not a c1 reopen** (email is the channel-stamp-unavailable case that seam was reserved for). **The A boundary:**
   - **m-1 (TCB) owns:** `mint(decision_id, seat, choice, expiry)` on egress → conductor-signed one-time token (key OS-isolated from lanes, DI-2); `verify(token)` on return → the **five checks** (sig → audience → expiry → nonce-unused → seat-matches-expected), **fail-closed**, nonce-burn = atomic conductor-owned store append (DI-1), seat-match to m-1's minted address space; on pass, stamp `FROM: operator` on the operator-relay channel.
   - **m-6 (bridge, outside the TCB) owns + CALLS the above:** the egress **trigger** + email render carrying the minted token, the **POST-not-GET** receipt endpoint, bucket routing, away-mode UX; m-6 **supplies** `expiry` (**validity-window = m-6/operator policy**), m-1 **enforces** it.
-  The away-mode token cell now **locks** with the rest of §4 (the four additive later-step build carries are in §12).
+  The away-mode token cell now **locks** with the rest of §4 (the additive later-step build carries are in §12 — **four** from `COORD-182600` + the **c5 decision-④** carry).
 
 ---
 
@@ -102,7 +102,7 @@
 **(a) Email-governance surface (governance lane UX).** A calm, keyboard-driven, **low-chrome bucketed local inbox** over the relay store; buckets = §2, ODB = §3. **Looks (proof requirement):** **jcode negative-look** — no GUI noise/filler, **no persistent panels that don't earn their pixels**, explicitly **not** the deferred `/btw` side-panel (`jcode-ux-notes.md`); **codex positive-look** — stable-scrollback + transient-tail (calm under load), **Tab-queues-while-busy** composer (never drop operator input), reserved space (`codex-notes.md:34-79`). Digest/batch/throttle vocabulary distinguished; the **A-gate is the non-suppressible `critical` class** (monotonic floor, cannot be silenced).
 
 **(b) Meeting-collaboration surface (the under-served lane — DQ-4 resolved).** The export: route **design/open/grill** gates to the **meeting/attach** surface and **MUST NOT compress them to a brief**; "PROTECT the collaboration lane as deliberately as the governance one" (`:51`). Contract locked now:
-- **Gate→lane routing (m-6-owned, off LOCKED fields, no new field):** a gate-bearing record with `phase ∈ {DESIGN, DESIGN-REVIEW}` ∨ `GRILL_REQUIRED=yes` → **collaboration/meeting lane** (conversational, not compressed); otherwise (verdict-shaped) → **governance/ODB lane**. (Seam-converged; reuses the conductor-derivation discipline on locked atoms.)
+- **Gate→lane routing (m-6-owned, off LOCKED atoms, no new field):** a gate-bearing record with `phase ∈ {DESIGN, DESIGN-REVIEW}` (the locked `phase` atom is the **primary key**) → **collaboration/meeting lane** (conversational, not compressed); otherwise (verdict-shaped) → **governance/ODB lane**. `GRILL_REQUIRED=yes` **corroborates** the meeting-lane route, but it is **not yet an m-2 frank registry field** — it is a ported the upstream protocol protocol header (agent-raised). **Owed m-2 dependency (c6-F6):** m-2 declares GRILL_REQUIRED's FieldSpec row (owner/type/values — the ported the upstream protocol header) before the routing binds it as a field; until then the route keys on the locked `phase` atom alone. (Seam-converged; reuses the conductor-derivation discipline on locked atoms.)
 - **Re-observe-on-resume (export (E), `:49`):** a meeting / an "elaborate more" fork is an out-of-band channel ⇒ conductor state may be stale ⇒ **re-observe before the woken lane acts; newest authoritative** (the m-3 observe-gate re-fired on resume).
 - **Attach mechanism DEFERRED to Step-3/4 runtime** (the live-session attach + the fork primitive are runtime-owned); this doc locks the **routing + no-compress + re-observe contract**, not the attach plumbing.
 
@@ -118,7 +118,7 @@
 
 ## 7. The m-5 seam resolution (cite COORD converged state)
 
-Resolved in `c3-design-m5-m6-coord` — **m-6 bind `123022` ⊕ m-5 confirm `131856`** (the converged model; the interim `125604`/`131747` `{verdict,fyi,collaborate}` excursion crossed `123022` and was retracted both sides — m-5.implementer must-revise `131617` + m-5 SITREP `132314`). **m-5 DECLARES; m-6 BINDS** (declare-before-bind, F2). The two-layer vocabulary: **`human_mode` posture {interactive, away, unattended}** (on the locked F2 per-assignment home, `ARCHITECTURE.md:187-188`) ⊥ **`surface_intent` {progress, review_checkpoint, advisory, result}** (**conductor-derived**, total, **non-gate** records only — like `record_integrity`; **no new m-2 field**, C2.4 holds). Gate-bearing records carry **no** `surface_intent` — they bind off the **locked** `gate_category`/HUMAN_GATE/J1 (no duplicate-mechanism-as-vocabulary).
+Resolved in `c3-design-m5-m6-coord` — **m-6 bind `123022` ⊕ m-5 confirm `131856`** (the converged model; the interim `125604`/`131747` `{verdict,fyi,collaborate}` excursion crossed `123022` and was retracted both sides — m-5.implementer must-revise `131617` + m-5 SITREP `132314`). **m-5 DECLARES; m-6 BINDS** (declare-before-bind, F2). The two-layer vocabulary: **`human_mode` posture {interactive, away, unattended}** (on the locked F2 per-assignment home, `ARCHITECTURE.md §C2.4/F2`) ⊥ **`surface_intent` {progress, review_checkpoint, advisory, result}** (**conductor-derived**, total, **non-gate** records only — like `record_integrity`; **no new m-2 field**, C2.4 holds). Gate-bearing records carry **no** `surface_intent` — they bind off the **locked** `gate_category`/HUMAN_GATE/J1 (no duplicate-mechanism-as-vocabulary).
 
 **The m-6 bind matrix (over (posture × surface_intent) + the locked gate fields):**
 | `surface_intent` | m-6 binding | lane |
@@ -154,7 +154,7 @@
 
 ## 10. Open questions (no remaining blockers — OQ-1 resolved; OQ-2/OQ-3 non-blocking)
 
-- **OQ-1 [RESOLVED — Seam C = A] — the m-1 confirm-or-gap (away-mode inbound verdict-token).** m-1 answered **(A): m-1 (TCB) owns mint/verify; m-6 owns the bridge over it** (`COORD-planner-20260630-182600`; orchestrator-accepted `183008`). *Forced* by DI-1 + DI-2; additive — first activation of m-1's reserved `certification` seam; **no c1 reopen**. The away-token cell is folded in §4 and now **locks**. The A-boundary + m-1's four PLAN carries are recorded in §4/§12.
+- **OQ-1 [RESOLVED — Seam C = A] — the m-1 confirm-or-gap (away-mode inbound verdict-token).** m-1 answered **(A): m-1 (TCB) owns mint/verify; m-6 owns the bridge over it** (`COORD-planner-20260630-182600`; orchestrator-accepted `183008`). *Forced* by DI-1 + DI-2; additive — first activation of m-1's reserved `certification` seam; **no c1 reopen**. The away-token cell is folded in §4 and now **locks**. The A-boundary + the build carries (**four** from `COORD-182600` + the **c5 decision-④** carry) are recorded in §4/§12.
 - **OQ-2 [m-5 seam follow-up, non-blocking] — the "elaborate more" fork posture.** Does the context-preserving elaboration fork inherit the parent `seat_archetype`/`human_mode`, or run under an `interactive`/`review_checkpoint` collaboration posture (read-only ceiling)? Bounded COORD note to m-5; does not block the m-6 lock (the fork primitive is runtime-owned; the surface is m-6's).
 - **OQ-3 [operator-declined / reserved] — write-capable elaboration fork is DECLINED** (operator 2026-06-30: the elaboration fork stays read-only; its only output is the decision relayed back). A hard per-archetype **never-bridge ceiling** (beyond content-egress) remains a reserved future hook — not needed for the initial release.
 
@@ -174,9 +174,22 @@
 - The §J config surface (gate_category map / protected-branch / away opt-in / egress whitelist).
 - The Step-4 TUI realization (codex stack; notmuch-style tag/saved-search buckets; mutt additive-score sort).
 
-**Inbound verdict-token mint/verify (Seam C = A; additive later-step build carries, from `COORD-planner-20260630-182600`):**
+**Inbound verdict-token mint/verify (Seam C = A; additive later-step build carries — the **four** from `COORD-planner-20260630-182600` below, + the **c5 decision-④** carry as the final bullet):**
 - **m-1:** signing-key custody OS-isolated from all lanes (DI-2); the key never resides in an m-6 / any lane process.
 - **m-1:** nonce-burn = an atomic conductor-owned store append (verify's nonce-unused → burn closes the same TOCTOU as `submit()`).
 - **m-1:** `certification`-seam activation scoped **inbound-verdict-only** for this step; general DKIM/Merkle courier-hardening stays the deferred seam (do not widen here).
 - **m-6:** POST-not-GET is m-6's transport invariant (anti-scanner); m-1's `verify` stays fail-closed / method-agnostic and rejects any auto-GET-derived replay. m-6 supplies `expiry` (validity-window policy); m-1 enforces it.
 - **m-6 resummon edge (operator decision ④, ROTATE + RE-OBSERVE — recorded §2C build-carry, `c5-fold-decision-4`; NOT design-locked here):** a resummon triggers **rotate `decision_id` + burn prior-cycle nonces** (m-1-owned burn) and a **`verify` re-observe that bounces the approval if state changed** since the operator last saw it — closing the stale-approval / TOCTOU window. The 7-state FSM's refresh transition carries it. **Mechanism + fixture + full-pair adversarial review are owed at build-step (d)** (before park/wake or the away-bridge ships), not design-locked here; this **subsumes/pairs with** the m-7 `re-mint-supersedes` carry + my CQ-6 co-sign (`c4-cq-m1/DESIGN-planner-20260702-020100`). **Residual (dormant in Step-1):** until (d) builds it, the base `(decision_id, seat)` sibling-burn + the never-auto-resolve-on-expiry FSM (§4) hold.
+
+---
+
+## 13. Fold-log (post-c3-lock, review-driven consistency folds — lock invariants unchanged)
+
+The c3 design-lock (`c3-lock` VP co-sign `20260630-191315`) is the frozen contract. The folds below are **claim-text / consistency** consequences of downstream cycles applied INTO the locked doc; **no mechanism, no lock invariant changed.**
+- **c4-cq-gateconfig** (pair-approved `024620`): CQ-3 pure-judgment A-floor table authored (canonical home `c4-cq-gateconfig/DESIGN-planner-20260702-015800`; NF-S8 binds there); CQ-4 terminal-token alignment (`delivery_state {accepted, rejected, held}`; `bounced`→`rejected`; `held`→bucket-A fault-disposition, ordinary A-gate parking = `accepted`) folded §2/§4; CQ-4b config-composition confirm.
+- **c4-cq-m1** (pair-approved): CQ-6 away-token park/wake co-sign + the re-mint-supersedes carry (§12).
+- **c5-claim-sweep-light** (approve `134742`): 3 relabels (`sole-writer`/`sole-external-send`→governed-write/conductor-governed-egress + D5 residual), 1 DI-1 mirror, 1 "structural"→bucket-grammar tighten (§2/§4/§11); claim-text only, four sanctioned by-construction claims + `{accepted, rejected, held}` preserved.
+- **c5-fold-decision-3** (approve `134743`): decision ③ RAISE-ONLY classification-direction invariant + known-A detector (§2). *(m-6-F1: the m-7 NF fixture is "registration owed", CTO-applied §2.)*
+- **c5-fold-decision-4** (approve `134744`): decision ④ ROTATE+RE-OBSERVE recorded as a non-locking §2C/§12 build-carry (mechanism at step d).
+- **c5-fold-decision-5** (approve `134745`): decision ⑤ ODB model-name egress carve-out (§3); R2 untouched.
+- **c6-fix-m-6** (this pass): **F3** — decision-⑤ m-7 note reworded to "fixture registration owed" (§3). **F6** — meeting-lane routing keys on the locked `phase` atom; `GRILL_REQUIRED` corroborates but is an owed m-2 registry declaration (§5). **F7** — STATUS + domain README updated to c3 LOCKED. **F8** — ARCHITECTURE line-anchors converted to stable section anchors (`§J1`, `§J2 A-set`/`B-set`/`fail-safe`, `§4 R1`, `§C2.4/F2`) to survive re-baseline line drift. **F9** — Seam-C carry counts split (four `COORD-182600` + one c5 decision-④) at §4/§10/§12. **F1/F2** were CTO-applied pre-dispatch (§2:44 ③-fixture-owed, §2/§4 `held`-gloss narrowing) — verified, not redone.
--- a/master/domains/m-6-human-surface-scheduler/README.md	2026-07-02 19:49:04
+++ b/master/domains/m-6-human-surface-scheduler/README.md	2026-07-02 21:14:21
@@ -34,10 +34,12 @@
 = Step 4. c3 designs the contracts now; build is later ("designed early, executed later").
 
 ## Status
-- **c3 DESIGN-LOCK-READY** (`c3-design-m-6`) — pair-**approved** (`DESIGN-REVIEW-...-133839`) + seam **converged** (`123022`⊕`131856`) + **Seam C RESOLVED (A)**: the away-token cell is folded over **m-1-owned mint/verify** (`182600`/`183008`) + locked; m-1's 4 PLAN carries recorded (§12); GRILL_LOCK §9 folded. **All cells closed.** Confirmed design-lock-ready to orchestrator (`COORD-planner-20260630-183345`). **VP-revise Finding 1 (narrow: stale §11/§12 lock-status) cleared** — swept + fixed §11/§12 + two more same-class spots (§8 ×2, §10 heading); doc internally consistent, confirmed clean (`DESIGN-planner-20260630-184921`) + **implementer re-approved post-correction** (`DESIGN-REVIEW-implementer-20260630-185424`, no blockers). Awaiting only the orchestrator's re-emitted c3 lock (seam-of-record + Seam C + GRILL_LOCKs) → VP co-sign → close. No PLAN/IMPL (c3 terminal = design-lock).
-- **c3 DESIGN doc r2 — both must-revise blockers folded; re-review requested** (`c3-design-m-6`) — `design/2026-06-30-human-surface-scheduler-design.md`. Implementer must-revise `131702`: **F1 (seam stale)** resolved by m-5's retraction of `125604` → converged **FOUR-class `123022`⊕`131856`** (doc reverted; my spurious `131747` bind-confirm WITHDRAWN); **F2 (egress A/D double-assignment)** folded an explicit `failing_edge`+stage precedence rule (§2). rev-2 review requested (`DESIGN-planner-20260630-133146`). **Held:** away-mode token-bridge cell (OQ-1), pending orchestrator-routed m-1 confirm-or-gap. Design-complete gates on: implementer rev-2 approve + m-1 answer.
-- **c3 full-domain audit RECONCILED** (`c3-audit-m-6`) — planner `053651` + implementer `053053` independent passes reconciled into `RECONCILE-planner-20260630-054107.md` (F4). Verdict: **promote-and-bind** (thin local-first projection over locked m-1..m-4, not a new gate system). One divergence resolved (away-mode inbound verdict trust → DESIGN-phase confirm-or-gap with m-1). Two c3-DESIGN COORDs queued: **m-5** human-mode vocabulary (binding, declare-before-bind) + **m-1** inbound verdict-token bridge (confirm-or-gap). Awaiting orchestrator accept → c3 DESIGN (GRILL-gated).
-- **c3 full-domain audit dispatched** `c3-audit-m-6` (20260630-051950) — m-6 has no prior design-of-record.
+- **c3 LOCKED** (`c3-lock` VP co-sign `RECONCILE-orchestrator-reviewer-20260630-191315`; orchestrator lock `190627`/`191525`) — pair-**approved** (`DESIGN-REVIEW-...-133839`) + seam **converged** (`123022`⊕`131856`) + **Seam C RESOLVED (A)**: the away-token cell folded over **m-1-owned mint/verify** (`182600`/`183008`); the COORD-182600 build carries recorded (§12); GRILL_LOCK §9 folded. **All cells closed.** Post-lock review-driven consistency folds — **c4** CQ-3/4/4b + CQ-6 (`024620`), **c5** claim-sweep + decisions ③/④/⑤ (`134742`–`134745`), **c6** cleanup F3/F6/F7/F8/F9 (`c6-fix-m-6`) — folded into the doc §13 fold-log; **lock invariants unchanged**. No PLAN/IMPL (c3 terminal = design-lock).
+### History (superseded by the c3 lock — chronological trail, all gates since cleared)
+*(These bullets are a past-tense progression log; the current state is the `c3 LOCKED` bullet above. Nothing below is a live/pending gate.)*
+- **c3 DESIGN doc r2 — both must-revise blockers folded (historical)** (`c3-design-m-6`) — `design/2026-06-30-human-surface-scheduler-design.md`. Implementer must-revise `131702`: **F1 (seam stale)** resolved by m-5's retraction of `125604` → converged **FOUR-class `123022`⊕`131856`** (doc reverted; the spurious `131747` bind-confirm WITHDRAWN); **F2 (egress A/D double-assignment)** folded an explicit `failing_edge`+stage precedence rule (§2). rev-2 was **subsequently approved** (`DESIGN-REVIEW-implementer-20260630-185424`); the then-**Held** away-mode token-bridge cell (OQ-1) was **resolved Seam C = A** (m-1 answer `182600`/`183008`). Both gates cleared; the doc is now **c3 LOCKED** (see above).
+- **c3 full-domain audit RECONCILED (historical)** (`c3-audit-m-6`) — planner `053651` + implementer `053053` independent passes reconciled into `RECONCILE-planner-20260630-054107.md` (F4). Verdict: **promote-and-bind** (thin local-first projection over locked m-1..m-4, not a new gate system). One divergence resolved (away-mode inbound verdict trust → DESIGN-phase confirm-or-gap with m-1). Two c3-DESIGN COORDs (m-5 human-mode vocabulary + m-1 inbound verdict-token bridge) **were subsequently accepted and run**; c3 DESIGN completed and **locked**.
+- **c3 full-domain audit dispatched (historical)** `c3-audit-m-6` (20260630-051950) — m-6 had no prior design-of-record at the time.
 
 ## Layout
 - `audit/` — c3 AUDIT artifacts.  `design/` — c3 DESIGN docs + grill locks.
--- a/master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md	2026-07-02 19:49:04
+++ b/master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md	2026-07-02 21:05:38
@@ -15,7 +15,7 @@
 
 **Claim boundary (LOCKED, held in every sentence of this doc):** Step-1 = attach + interface guardrail = **CONFUSION-RESISTANT** (`master/GRILL-LOCK-deployment-fork-2026-07-01.md` D1–D5). The **single** "by construction" claim in this design is the serialized-loop kill of the two-honest-seats double-accept race (§2.4) — a control-flow property of the trusted engine. A malicious same-uid code-executing seat can write the store files directly; that is the documented D5 accepted-risk, out of scope. No sentence in this doc claims "unbypassable / sole-writer by construction / same-uid write-exclusion" (§16 claim-sweep).
 
-**Locked contracts consumed (never reopened):** m-1 verbs/stamp/store shape + DI family; m-2 FieldSpec/fill-time authority/write-path seam (`master/ARCHITECTURE.md:58-66`); m-3 observe-as-send + allowlist + egress; m-4 route_dispatch/R2; m-5 archetypes/ceilings/templates; m-6 buckets/ODB/park-wake/away-bridge. Genuine contract questions ride the CQ ledger (§15) as orchestrator-fired COORDs — never silent reinterpretation.
+**Locked contracts consumed (never reopened):** m-1 verbs/stamp/store shape + DI family; m-2 FieldSpec/fill-time authority/write-path seam (`master/ARCHITECTURE.md §3 "Write path — reconciled seam"`); m-3 observe-as-send + allowlist + egress; m-4 route_dispatch/R2; m-5 archetypes/ceilings/templates; m-6 buckets/ODB/park-wake/away-bridge. Genuine contract questions ride the CQ ledger (§15) as orchestrator-fired COORDs — never silent reinterpretation.
 
 ## 1. The engine at a glance
 
@@ -26,9 +26,9 @@
    │  per-seat MCP channel (stdio pipe / bearer token)   ← identity = the channel (m-1)
    ▼
  [A] CHANNEL LAYER      N concurrent per-seat handlers; present the 3-tool registry;
-                        stamp-source; write commands to the intake journal; await outcome
-   ▼ append+fsync
- [B] INTAKE JOURNAL     durable, append-only; the FIFO's persistent half (§2.2)
+                        stamp-source; hand commands to the intake-writer; await outcome
+   ▼ bounded channel → SINGLE intake-writer task (append+fsync; §2.1)
+ [B] INTAKE JOURNAL     durable, append-only, one writer; the FIFO's persistent half (§2.2)
    ▼ in arrival order
  [C] COMMIT LOOP        ONE thread; plain FIFO; runs EVERY mutation start-to-finish:
                         submit validate+append, verify check-and-burn, park/wake, slot_in
@@ -49,31 +49,31 @@
 ## 2. Process/concurrency model (GRILL Q2 — locked)
 
 ### 2.1 One writer, many readers
-- **Channel handlers ([A]) are mail carriers only**: they authenticate the channel (m-1 binding), accept a command, append it to the intake journal, and wait. They never touch store files. Donor: codex's single-owner writer task + bounded-channel + oneshot-acked barriers (`references/codex/codex-rs` rollout recorder), elevated from per-file to global; the LMAX single-writer principle.
+- **Channel handlers ([A]) are mail carriers only**: they authenticate the channel (m-1 binding), accept a command, and **hand it over a bounded in-process channel to the single INTAKE-WRITER task**, then await the outcome. **Handlers never touch any file** — the intake journal has exactly ONE writer: the dedicated intake-writer task, which appends+fsyncs entries in channel-arrival order and feeds the commit loop (c6 m-7-F5 fix: the single-writer discipline is now stated at the intake tier too, resolving the apparent "handlers never touch store files, yet who appends intake?" gap). Donor: codex's single-owner writer task + bounded-channel + oneshot-acked barriers (`references/codex/codex-rs` rollout recorder) — applied at BOTH tiers (intake-writer for the journal; commit loop for the store); the LMAX single-writer principle.
 - **The commit loop ([C]) is one thread** consuming a **plain FIFO** (operator-grilled: no priority classes in the initial release; claude-code's priority-dequeue recorded as a rejected-for-now future knob). Every mutation runs to completion before the next begins.
 - **Reads are concurrent and lock-free** because committed records are immutable — `project()`/`read()` never race with anything. Readers of append-in-progress projection files see a shorter prefix (safe). Form renders are concurrent reads too: the render is advisory, the loop's in-critical-section validation is authoritative (locked m-2 §3/m-1 §13.2 — stale renders are caught at submit; TOCTOU stays closed).
 
 ### 2.2 The durable FIFO (operator riders, grilled)
-- Every command is **appended + fsynced to the intake journal BEFORE entering the in-memory FIFO**; the seat's ack arrives only with a typed outcome. Power loss loses at most a command not yet fsynced to intake — and the seat, holding an un-acked candidate on a persistent session, retries; content-hash request keys (jcode idempotency-replay donor) dedupe the retry.
+- Every command is **appended + fsynced to the intake journal (by the single intake-writer task, §2.1) BEFORE entering the in-memory FIFO**; the seat's ack arrives only with a typed outcome. Power loss loses at most a command not yet fsynced to intake — and the seat, holding an un-acked candidate on a persistent session, retries; content-hash request keys (jcode idempotency-replay donor) dedupe the retry.
 - **Clear-on-pop is atomic with the outcome commit** (operator rider — the stale-re-emission failure mode is structurally closed): every intake entry has an `intake_id`; every outcome record (`accepted`/`rejected`/`held`) references the `intake_id` it consumes; "consumed" is durably recorded by the same §4 pivot that records the outcome — no separate mark-consumed write exists to be missed. Recovery re-enqueues exactly `intake − outcomes` in arrival order (§5 phase 3.5). At-least-once intake, exactly-once effect, zero stale re-emission.
 
 ### 2.3 What waits on the loop
-Step-2's observe probe runs **inside** the atomic submit (locked m-3 §2 — the passing observation binds to the gated record). A slow probe stalls the loop; the design's answer is **hard per-check timeouts → internal fault → HELD** (§6), never accept-without-observing, never hang. Cost accepted and recorded: during a probe other submits queue (live-team volume: 261 records/4 days).
+Step-2's observe probe runs **inside** the atomic submit (locked m-3 §2 — the passing observation binds to the gated record). A slow probe stalls the loop; the design's answer is **hard per-check timeouts → internal fault → §6 disposition** (class-conditional per CQ-2: authority-bearing → `held`; non-authority → `rejected`/author-return), never accept-without-observing, never hang. Cost accepted and recorded: during a probe other submits queue (live-team volume: 261 records/4 days).
 
 ### 2.4 The licensed by-construction claim (scoped exactly)
 Two honest racing seats submit records consuming the same one-shot authority (away-token choice, delegated dispatch, merge grant). Because ALL mutations — `submit()`'s read-validate-append and `verify()`'s check-and-burn — execute on one single-threaded loop, each to completion, there exists **no interleaving** in which both pass the unused-check before either burns. The second command's validate step reads the first's committed burn. This holds regardless of seat behavior — it is the trusted engine's own control flow. **This control-flow property is the only "by construction" claim this design makes.** It does not constrain a code-executing seat writing store files directly (D5 accepted-risk).
 
 ## 3. The commit pipeline — executing the locked write-path seam
 
-`submit()` on the loop, in the locked order (`master/ARCHITECTURE.md:58-66`; m-1 §5; m-2 §4):
+`submit()` on the loop, in the locked order (`master/ARCHITECTURE.md §3 "Write path — reconciled seam"`; m-1 §5; m-2 §4):
 1. **resolve + stamp** — seat from channel binding (reject unbound); stamp `FROM`/`ROLE`; fill system envelope (`relay_id`, `DISPATCH_ID`, `timestamp`, `schema_version`, `certification=null`); validate `PARENT` ∈ conductor-derived candidate set; `TO`/`CC` ∈ minted address space.
 2. **pre-append validation, candidate in-courier, nothing persisted** — (a) m-2 form-validation (required-set, enums, seat-scope); (b) m-2 lineage gate over `persisted ∪ {candidate}` (blocking edges only for authority-bearing records).
-3. **`slot_in` classification — the CLOSED CQ-5 ordering** (m-5 `c4-cq-slotin/DESIGN-planner-20260702-014506` + m-5.impl `020448` + m-3 co-sign `024732` + m-3.impl `030205`): conductor-classified **at acceptance, post-gate / pre-observe / atomic-bind** — after the form/lineage gates, before the observe hook, so the (Step-2) observe predicate selection reads the just-classified in-courier value, and the classification is bound into the same atomic commit as the observation it selects. Immutable, non-lane-writable (locked F1); required by locked m-5 §4. *(History: r1 had classify-after-observe — contradiction caught in review; r2 proposed this ordering to the CQ-5 COORD; the COORD closed on exactly it — lock-bearing as of r5.)*
+3. **`slot_in` classification — the CLOSED CQ-5 ordering** (m-5 `c4-cq-slotin/DESIGN-planner-20260702-014506` + m-5.impl `020448` + m-3 co-sign `024732` + m-3.impl `030205`): conductor-classified **at acceptance, post-gate / pre-observe / atomic-bind** — after the form/lineage gates, before the observe hook, so the (Step-2) observe predicate selection reads the just-classified in-courier value, and the classification is bound into the same atomic commit as the observation it selects. Immutable, non-lane-writable **at the tool surface** (no verb exists for a lane to set or change it; D5 same-uid residual per §8.4 — locked F1's claim grain); required by locked m-5 §4. *(History: r1 had classify-after-observe — contradiction caught in review; r2 proposed this ordering to the CQ-5 COORD; the COORD closed on exactly it — lock-bearing as of r5.)*
 4. **[Step-2 hook, inert in Step-1]** m-3 `observe_gate()` under the §2.3 timeout, its predicate selected by the step-3 classification, its write-allowlist mechanically enforced (writes outside the closed set = internal fault).
 5. **atomic commit** — §4: stage (record carrying classification + observation) → checksum → fsync → **rename pivot** → dir-fsync → redo-journal-driven projections (INDEX row, rendered `.md`, mailbox entries) → outcome references `intake_id`.
 6. **deliver** — one `write()` per recipient pipe (§8.3). On gate failure at 2 or 4: **one atomic append of a terminal `rejected` evidenced record** (same pivot mechanics) + bounce naming the failing field/edge. On internal fault: `held` (§6).
 
-`verify(token)` on the same loop: m-1's five checks (sig → audience → expiry → nonce-unused → seat-match) → **nonce-burn = a committed burn record via the same pivot**, sibling-burn per decision scope (CQ-6's m-1 COORD confirms the decision-scoped burn set) → on pass, stamp `FROM: operator` via the operator channel. Double-redemption is killed by §2.4.
+`verify(token)` on the same loop: m-1's five checks (sig → audience → expiry → nonce-unused → seat-match) → on pass, **ONE compound operator-verdict record** committed via the same single §4 pivot, stamped `FROM: operator` via the operator channel — **the verdict record's presence IS the decision-scoped `(decision_id, seat)` burn** (m-7-F1, VP-ratified: burn+verdict merged into one canonical record; no separate burn record, no second rename). The burned-nonce set is *derived* from committed operator-verdict records. Double-redemption is killed by §2.4.
 
 ## 4. Crash-atomic multi-file commit — the named pivot (GRILL Q1 — locked)
 
@@ -90,14 +90,14 @@
 - **Phase 0 — validate genesis:** genesis record present + config digest matches (§7). Mismatch/missing ⇒ serve read-only diagnostics, accept nothing, summon operator. Fail-closed at the root.
 - **Phase 1 — scan canonical records:** per-record checksum. Torn file in `staging/` ⇒ delete (never committed — routine). Checksum-fail on a *committed* record ⇒ **quarantine** (`store/quarantine/` + a HELD-class incident record); never silent-skip (Kafka interior-corruption lesson), never fail-stop the store (§2A "never brick").
 - **Phase 2 — rebuild projections:** replay redo journal; ensure every committed record's INDEX row / rendered `.md` / mailbox entries exist; canonical wins; idempotent (ARIES property: recovery of the recovery is safe); INDEX repairs are appended, never rewritten (§4).
-- **Phase 3 — restore runtime tables:** burned-nonce set (burn records — a crash can never resurrect a consumed token), parked-lane states (m-6 7-state records; the store IS the checkpointer, locked m-6 §4), seat-binding table (§11).
+- **Phase 3 — restore runtime tables:** burned-nonce set (derived from committed operator-verdict records, whose presence implies the decision-scoped burn — a crash can never resurrect a consumed token, and there is no separate burn record to diverge from its verdict; m-7-F1), parked-lane states (m-6 7-state records; the store IS the checkpointer, locked m-6 §4), seat-binding table (§11).
 - **Phase 3.5 — refill the FIFO:** re-enqueue `intake − outcomes` in arrival order (§2.2). Entries with outcomes can never re-emit.
 - **Phase 4 — open:** accept `submit`/`verify`. During 1–3: reads of committed records may be served; zero authority consumption (a half-restored burn set is how stale approvals replay).
 
 ## 6. Internal-fault disposition + terminal-state enum (GRILL Q4+Q5 — locked)
 
 **One rule:** a trusted-side check that **throws, times out, or reads corrupt data** yields a typed outcome — never silent-accept, never a brick:
-- **authority-bearing record → `HELD`**: not accepted (a crash is not a yes — K8s `failurePolicy: Fail`), not rejected (the check *failed to run*, it did not *say no* — the distinction is preserved in the type), parked operator-visible (m-6 A-surface), resolved by re-run or operator verdict. Represented append-only: the candidate + a held-disposition record referencing it and the `intake_id`.
+- **authority-bearing record → `HELD`**: not accepted (a crash is not a yes — K8s `failurePolicy: Fail`), not rejected (the check *failed to run*, it did not *say no* — the distinction is preserved in the type), parked operator-visible (m-6 A-surface), resolved by re-run or operator verdict. Represented append-only as **ONE compound canonical record**: the held-disposition record **embeds the candidate** (full candidate bytes inside the disposition record) and references the `intake_id` — one record, one §4 rename pivot, no second canonical write (m-7-F1, VP-ratified one-pivot-per-mutation; supersedes the two-record "candidate + disposition referencing it" shape, whose two renames opened a crash window between them).
 - **non-authority record → bounce** to author with the fault-edge named.
 - **the engine keeps serving**: fault handling touches only the sick record; the fault path is minimal and self-excluding (its own machinery passes through no gate that could HELD it — the K8s wedge lesson).
 
@@ -106,7 +106,7 @@
 
 ## 7. Trusted config load + integrity (§2A.2; CQ-4b consumer)
 
-- The policy-config artifact (m-6/m-3/m-4-authored: gate_category maps, the CQ-3 A-floor table, egress rules, capability priors, archetype registry) — **composition CLOSED via CQ-4b (CTO ruling, all-domain confirms):** **per-domain sections, conductor-composed into one artifact with a single top-level digest**, plus **m-4's per-section version-stamp inside the artifact** (CTO integration item 1 — compatible with the single-digest ruling; a section stamp aids per-domain change attribution without adding a second integrity root). The artifact is **conductor-owned**, read **once at trusted startup**, top-level-digest-verified against the genesis chain: genesis carries the initial config digest; a legitimate config change is itself a committed store record (operator-authorized, carrying the new digest) so config history is auditable append-only.
+- The policy-config artifact — **full author set (c6 x3-F2 fix): m-2, m-3, m-4, m-5, m-6** — per-domain sections: gate_category maps + the CQ-3 A-floor table (m-6), egress rules (m-3), capability priors (m-4), **the archetype registry (m-5 — the previously omitted author of a section this doc already named)**, and **m-2's declared schema section** (the registry slice config carries). m-5's CQ-4b section confirm: **OBTAINED** — `master/relays/c6-fix-m-5/COORD-planner-20260702-205849.md` (the registry = one m-5-authored, m-5-stamped section under the single top-level digest; contents = tag-space + ceiling composition + templates + axis maps, matching m-5's locked §13 "registry as config-sourced data"; loaded at genesis; no c3 reopen). **Composition CLOSED via CQ-4b (CTO ruling, all-domain confirms):** **per-domain sections, conductor-composed into one artifact with a single top-level digest**, plus **m-4's per-section version-stamp inside the artifact** (CTO integration item 1 — compatible with the single-digest ruling; a section stamp aids per-domain change attribution without adding a second integrity root). The artifact is **conductor-owned**, read **once at trusted startup**, top-level-digest-verified against the genesis chain: genesis carries the initial config digest; a legitimate config change is itself a committed store record (operator-authorized, carrying the new digest) so config history is auditable append-only.
 - **No hot reload in the initial release** (restart-only; grilled under Q2's simplicity rule) — no agent turn can cause a re-read or mutate effective config (codex donor: config-by-value, no reload machinery on the agent path). Digest mismatch at startup ⇒ Phase-0 fail-closed.
 - **Absent from every seat surface**: not a tool, not a resource, no config *path or value* in any seat-deliverable schema/description/prompt/tool-result/projection (fixture F7/G(iii); implementer's strengthening folded). Donors: codex untrusted-layer denylist + config-lock replay validation.
 
@@ -148,28 +148,28 @@
 
 | # | contract (owner) | doc/section | m-7 obligation (engine §) | biting negative fixture | CQ |
 |---|---|---|---|---|---|
-| S1 | store append + stamp (m-1) | m-1 design §5 :97-107, §6, §4; CQ-6 BASE triad | §3 pipeline; §4 pivot; §11 **persisted seat-binding table + re-attach credential proof (CQ-6 base, closed)** | **NF-S1 (bound to CQ-6 BASE):** submit over an unbound channel ⇒ reject, nothing staged; submit with payload-supplied `FROM`/`ROLE` ⇒ stamped values win, payload ignored byte-for-byte; kill -9 between pivot and projections ⇒ recovery yields exactly one INDEX row + full mailboxes, zero duplicates; conductor restart ⇒ re-attach with the same credential resolves to the same seat, no re-mint | CQ-6 **CLOSED (base)** |
-| S2 | away-token verify/nonce-burn, Seam C (m-1) | m-6 design §4 :85-88; m-1 §C3.4; CQ-6 BASE triad | §3 verify; §2.4; **decision-scoped `(decision_id, seat)` sibling-burn, atomic inside the loop (CQ-6 base, closed)** | **NF-S2 (=F1, bound to CQ-6 BASE):** two `verify(token)` for the same nonce enqueued concurrently ⇒ exactly one burn record after both complete; the loser fail-closes typed; resolving one choice burns the decision-scoped sibling set; replay after restart ⇒ still burned (Phase-3). *(`re-mint-supersedes` is NOT part of this fixture — §2C build-carry, unreviewed add-on, per the VP-approved CQ-6 re-scope.)* | CQ-6 **CLOSED (base)** |
+| S1 | store append + stamp (m-1) | m-1 design §5 (API surface), §6, §4; CQ-6 BASE triad | §3 pipeline; §4 pivot; §11 **persisted seat-binding table + re-attach credential proof (CQ-6 base, closed)** | **NF-S1 (bound to CQ-6 BASE):** submit over an unbound channel ⇒ reject, nothing staged; submit with payload-supplied `FROM`/`ROLE` ⇒ stamped values win, payload ignored byte-for-byte; kill -9 between pivot and projections ⇒ recovery yields exactly one INDEX row + full mailboxes, zero duplicates; conductor restart ⇒ re-attach with the same credential resolves to the same seat, no re-mint | CQ-6 **CLOSED (base)** |
+| S2 | away-token verify/nonce-burn, Seam C (m-1) | m-6 design §4 :85-88; m-1 §C3.4; CQ-6 BASE triad | §3 verify; §2.4; **decision-scoped `(decision_id, seat)` sibling-burn = the presence of ONE compound operator-verdict record, atomic inside the loop (CQ-6 base, closed; one-pivot-per-mutation per m-7-F1)** | **NF-S2 (=F1, bound to CQ-6 BASE):** two `verify(token)` for the same nonce enqueued concurrently ⇒ exactly one operator-verdict record after both complete (its presence = the burn); the loser fail-closes typed; resolving one choice burns the decision-scoped sibling set (implied by the same single record); replay after restart ⇒ still burned (Phase-3 derives the burn set from verdict records). *(`re-mint-supersedes` is NOT part of this fixture — §2C build-carry, unreviewed add-on, per the VP-approved CQ-6 re-scope.)* | CQ-6 **CLOSED (base)** |
 | S3 | form render / fill-time authority (m-2) | m-2 design §3, §4 | §8.2 | **NF-S3:** a pair-seat's rendered `submit` schema contains no `direct-override`/merge-grant option (schema introspection); a hand-crafted MCP call supplying the forbidden enum value anyway ⇒ authoritative validation rejects at §3 step 2 (render is advisory, loop is authoritative) | — |
-| S4 | form-validation + lineage gate (m-2) | m-2 §4 steps 4–5; `master/ARCHITECTURE.md:58-66` | §3 step 2 | **NF-S4 (=F5):** validator forced to throw on an authority-bearing candidate ⇒ outcome `held`, author-visible, loop alive, next FIFO entry processes; no store path exists where the candidate is deliverable | — |
-| S5 | phase-split required-set (m-1+m-2+m-7) | `master/DESIGN-REVIEW-2026-07-01.md` §2A.5 | §3 step 2a executes the **CQ-1(a) STEP-GATE** (closed: observe-owned `required_when` predicates are step-gated on observe-layer presence) | **NF-S5 (bound to CQ-1(a)):** a Step-1 IMPL action-report submit with no observe layer present ⇒ accepted without the observe-owned fields (their `required_when` is step-gated off); the SAME submit with the Step-2 observe layer present ⇒ those fields required + conductor-filled | CQ-1 **CLOSED** |
-| S6 | observe-as-send hook (m-3) | m-3 design §2, §3 | **§3 step 4** (predicate selected by the step-3 classification — closed CQ-5 ordering); §2.3 timeout | **NF-S6:** a (Step-2) observe hook attempting to write outside its closed allowlist (e.g. `FROM`) ⇒ internal fault, `held`, allowlist violation logged; hook timeout ⇒ `held`, never accept | CQ-5 **CLOSED** |
-| S7 | decision-② fail-closed (m-3 + op-②) | m-3 fold (CQ-2 triad) + `master/DESIGN-REVIEW-2026-07-01.md` §2A.7 | §3/§6 execute the **closed CQ-2 disposition: class-conditional fail-closed for authority-class `self_reported`, disposition token = `held`** (m-3 fold + m-2 field-home landed) | **NF-S7 (bound to CQ-2):** an authority-class record whose evidence is `self_reported` ⇒ outcome `held` (never delivered-as-accepted, never silent); a non-authority `self_reported` record still delivers labeled (honest-fallback preserved) | CQ-2 **CLOSED** |
+| S4 | form-validation + lineage gate (m-2) | m-2 §4 steps 4–5; `master/ARCHITECTURE.md §3 "Write path — reconciled seam"` | §3 step 2 | **NF-S4 (=F5):** validator forced to throw on an authority-bearing candidate ⇒ outcome `held`, author-visible, loop alive, next FIFO entry processes; no store path exists where the candidate is deliverable | — |
+| S5 | phase-split required-set (m-1+m-2+m-7) | `master/DESIGN-REVIEW-2026-07-01.md` §2A.5 | §3 step 2a executes the **CQ-1(a) STEP-GATE** (closed: observe-owned `required_when` predicates are step-gated on observe-layer presence) | **NF-S5 (bound to CQ-1(a)):** a Step-1 IMPL action-report submit with no observe layer present ⇒ accepted without the observe-owned fields (their `required_when` is step-gated off); the SAME submit with the Step-2 observe layer present ⇒ those fields required + conductor-filled | CQ-1 **CLOSED** |
+| S6 | observe-as-send hook (m-3) | m-3 design §2, §3 | **§3 step 4** (predicate selected by the step-3 classification — closed CQ-5 ordering); §2.3 timeout | **NF-S6:** a (Step-2) observe hook attempting to write outside its closed allowlist (e.g. `FROM`) ⇒ internal fault, allowlist violation logged, §6/CQ-2 class-conditional disposition (authority-bearing ⇒ `held`; non-authority ⇒ `rejected`/author-return); an authority-class hook timeout ⇒ `held`, never accept (a non-authority hook timeout ⇒ `rejected`/author-return per §6 — distinct from a non-authority *unobservable/no-vantage* record, which is `accepted`+`self_reported` per locked m-3) | CQ-5 **CLOSED** |
+| S7 | decision-② fail-closed (m-3 + op-②) | m-3 fold (CQ-2 triad) + `master/DESIGN-REVIEW-2026-07-01.md` §2A.7 | §3/§6 execute the **closed CQ-2 disposition: class-conditional fail-closed for authority-class `record_integrity ∈ {self_reported, mixed}`, disposition token = `held`** (m-3 fold + m-2 field-home landed; c6-widened to include `mixed`) | **NF-S7 (bound to CQ-2):** an authority-class record whose `record_integrity ∈ {self_reported, mixed}` ⇒ outcome `held` (never delivered-as-accepted, never silent); a non-authority `self_reported`/`mixed` record still delivers labeled (honest-fallback preserved) | CQ-2 **CLOSED** |
 | S8 | pure-judgment A-floor (m-2/m-6) | CQ-3 triad (m-6 table + m-2 mechanics); §2A.6 | enforce the **closed CQ-3 (phase × record_kind) A-floor table** at §3 step 2a via the existing monotonic MAX (no new m-2 mechanism); CEREMONY_TIER monotonic | **NF-S8 (bound to CQ-3):** a submit picking below the table's floor for its (phase × record_kind) ⇒ floor wins (monotonic MAX), `gate_category=ceremony_downgrade` auto-set; a table-absent (phase × record_kind) pair ⇒ the `other`→A fail-safe | CQ-3 **CLOSED** |
-| S9 | egress gate (m-3) | m-3 §7 | §9 chokepoint | **NF-S9:** an outbox item flagged by the scan ⇒ blocked + `egress_blocked` park + local resummon; **no conductor-owned code path** sends unscanned bytes externally (the outbox drain is the conductor's only socket-writing component — a governance-surface claim; the D5 same-uid seat residual per §9 applies) | — |
+| S9 | egress gate (m-3) | m-3 §7 | §9 chokepoint | **NF-S9:** an outbox item flagged by the scan ⇒ blocked + `egress_blocked` park + local resummon; **no conductor-owned code path** sends unscanned bytes externally (the outbox drain is **the conductor's only component that writes to non-seat destinations** — seat pipes/MCP channels are seat-facing and out of this claim's grain (c6 m-7-F6 reword); a governance-surface claim; the D5 same-uid seat residual per §9 applies) | — |
 | S10 | routing record + route_dispatch (m-4) | m-4 §3, §5 | §3 acceptance; host fail-closed refusal | **NF-S10:** no acceptable route ⇒ `routing_unavailable`/`human_decision_required`, never a silent default; engine gate evaluation over a routing record never reads `chosen_model` (R2 grammar — m-2 AC14 hosted) | — |
 | S11 | template/pane spawn (m-4/m-5) | m-4 GL-4 §7; m-5 §7 | spawn panes (existing multiplexer), deliver boot relays, record ceiling per assignment | **NF-S11:** an off-floor template slot spawn emits a `routing_decision` with `declared_deviated=true` + snapshotted justification (no-bypass); spawn above any ceiling axis ⇒ fail-closed refusal | — |
 | S12 | `slot_in` classification (m-3/m-5) | CQ-5 triad (m-5 + m-3 co-sign); m-3 §5.1; m-5 §4 | §3 step 3 — the **closed CQ-5 ordering: classify at acceptance, post-gate / pre-observe / atomic-bind** (m-3/m-5-confirmed; required by locked m-5 §4) | **NF-S12 (bound to CQ-5):** a lane-supplied `slot_in` value in a submit payload ⇒ ignored; the conductor-classified value is on the committed record, bound in the same atomic commit as the observation it selected, and immutable; a re-tag attempt post-acceptance has no verb to express it | CQ-5 **CLOSED** |
 | S13 | scheduler park/wake (m-6) | m-6 §4 | §11 | **NF-S13:** kill -9 with three lanes parked ⇒ Phase-3 restores all three `parked_waiting_human` states from store records alone; wake before the verdict record commits is impossible (wake is pipe-write sequenced after commit) | — |
 | S14 | buckets + ODB delivery (m-6) | m-6 §2, §3 | mailbox projection + ODB render slots | **NF-S14:** a bucket-D-class bounce never appears in the operator mailbox (author-facing only); an A-gate record is non-suppressible (appears regardless of digest/batch config) | CQ-4 (token) |
-| S15 | trusted config (authors m-6/m-3/m-4; composition CTO-ruled) | CQ-4b ruling + confirms; GL D2b; §2A.2 | §7 — the **closed CQ-4b composition: per-domain sections → conductor-composed, single top-level digest, m-4 per-section version-stamp inside** | **NF-S15 (=F7, bound to CQ-4b):** top-level digest mismatch at startup ⇒ Phase-0 refuses authority ops, serves diagnostics; a per-section stamp change without a matching top-level digest change ⇒ mismatch (one integrity root); seat-side `read`/`project`/`submit` results grepped for the config path or any effective config value ⇒ zero hits | CQ-4b **CLOSED** |
+| S15 | trusted config (authors m-2/m-3/m-4/m-5/m-6 — c6 x3-F2 full set; composition CTO-ruled) | CQ-4b ruling + confirms (m-5 section confirm OBTAINED: `c6-fix-m-5/COORD-planner-20260702-205849`); GL D2b; §2A.2 | §7 — the **closed CQ-4b composition: per-domain sections → conductor-composed, single top-level digest, m-4 per-section version-stamp inside** | **NF-S15 (=F7, bound to CQ-4b):** top-level digest mismatch at startup ⇒ Phase-0 refuses authority ops, serves diagnostics; a per-section stamp change without a matching top-level digest change ⇒ mismatch (one integrity root); seat-side `read`/`project`/`submit` results grepped for the config path or any effective config value ⇒ zero hits | CQ-4b **CLOSED** |
 | S16 | terminal-state tokens (m-2/m-3/m-6) | CQ-4 triad; m-2 §17.1 Q-E | §6 — the **closed CQ-4 byte-exact set `{accepted, rejected, held}`**; `rejected` shared across rejection classes; `held` distinct; bucket-D aligned; stale `bounced` retired | **NF-S16 (bound to CQ-4):** state-machine exhaustiveness — no code path exits without exactly one of the byte-exact `{accepted, rejected, held}`; a grep of the engine's emitted tokens finds no `bounced` value token | CQ-4 **CLOSED** |
-| S17 | integrated architecture (CTO/VP) | `master/ARCHITECTURE.md:17-66, :287-337` | execute in locked order; surface, never arbitrate | **NF-S17:** composite — NF-S1..S16 all green constitutes the integration fixture; any m-7 doc text contradicting a locked m-1..m-6 sentence = review blocker | — |
+| S17 | integrated architecture (CTO/VP) | `master/ARCHITECTURE.md` §1 (the integrated model) + §C4.1–C4.3 (the engine × six contracts + claim boundary) | execute in locked order; surface, never arbitrate | **NF-S17:** composite — NF-S1..S16 all green constitutes the integration fixture; any m-7 doc text contradicting a locked m-1..m-6 sentence = review blocker | — |
 | S18 | deployment-fork claim boundary (operator) | GL D1–D5; `master/DESIGN-REVIEW-2026-07-01.md` §2 | §2.4 scope; §8.4 residual; §16 sweep | **NF-S18 (=F8+G):** G(i) registry enumeration = exactly {submit, project, read}, zero path-bearing tools/resources; G(ii) scripted confused-seat "edit the store file directly" turn ⇒ no tool can express it; only store effect via submit; G(iii)+F6 grep of every seat-deliverable surface for **raw conductor-internal** store/config/outbox/operator-channel paths + **effective config values** ⇒ zero hits (ordinary evidence citations to relay/design *files* are not hits — rev2 qualifier, prevents false positives); F8 claim-sweep (§16) clean | — |
 
 ## 13. Fixture set (executable-claim form; conductor-registry checks at build time)
 
-F1=NF-S2 (double-verify) · F2 crash-free commit shape (submit ⇒ exactly one record + one INDEX row + one rendered `.md` + expected mailboxes, all checksums valid) · F3 staging-crash (kill -9 pre-pivot ⇒ no committed record, staging cleaned, intake entry re-enqueued once, no duplicate on re-run) · F4 projection-crash (kill -9 post-pivot ⇒ projections rebuilt, authority not re-consumed, INDEX append-only preserved) · F5=NF-S4 (validator-throw ⇒ held) · F6+G(i–iii)=NF-S18 (guardrail negatives) · F7=NF-S15 (config mismatch) · F8=§16 (claim-sweep) · **F9 (new, grilled):** durable-FIFO — kill -9 with 5 enqueued commands (2 outcomes reached) ⇒ recovery re-enqueues exactly the 3 without outcomes, in arrival order; zero re-emission of the 2 · **F10 (new):** fsync-ordering — simulated power-cut between record-fsync and dir-fsync, and between rename and projection writes ⇒ recovery converges to the correct store both times.
+F1=NF-S2 (double-verify) · F2 crash-free commit shape (submit ⇒ exactly one record + one INDEX row + one rendered `.md` + expected mailboxes, all checksums valid) · F3 staging-crash (kill -9 pre-pivot ⇒ no committed record, staging cleaned, intake entry re-enqueued once, no duplicate on re-run) · F4 projection-crash (kill -9 post-pivot ⇒ projections rebuilt, authority not re-consumed, INDEX append-only preserved) · F5=NF-S4 (validator-throw ⇒ held) · F6+G(i–iii)=NF-S18 (guardrail negatives) · F7=NF-S15 (config mismatch) · F8=§16 (claim-sweep) · **F9 (new, grilled):** durable-FIFO — kill -9 with 5 enqueued commands (2 outcomes reached) ⇒ recovery re-enqueues exactly the 3 without outcomes, in arrival order; zero re-emission of the 2 · **F10 (new):** fsync-ordering — simulated power-cut between record-fsync and dir-fsync, and between rename and projection writes ⇒ recovery converges to the correct store both times · **F11 (new, c6 m-7-F1):** one-pivot-per-mutation — enumerate every mutation class {submit-accept, submit-reject, held (compound, candidate embedded), operator-verdict (compound, burn implied), park/wake transition, outbox enqueue, genesis, config-change, GC marker} and assert each commits **exactly one canonical record via exactly one rename pivot**; a crash injected at every syscall boundary of every mutation class leaves the store in either fully-committed or not-committed state — **no mutation class has a second canonical rename to crash between**.
 
 ## 14. GRILL_LOCK
 
@@ -215,7 +215,7 @@
 | CQ | owner | resolution (closed) | closing legs | lands in |
 |---|---|---|---|---|
 | CQ-1 phase-split required-set | m-1+m-2 | **(a) STEP-GATE** — observe-owned `required_when` step-gated on observe-layer presence | m-1 `013500` + m-1.impl `020418` + m-2 co-sign `024704` + m-2.impl `030145` | §3 step 2a; NF-S5 |
-| CQ-2 decision-② fail-closed | m-3 (step-c) + m-2 | class-conditional **fail-closed** for authority-class `self_reported`; disposition token = **`held`**; m-2 field-home landed | m-3 fold + m-2 `014626` + m-3.impl `021724` + m-2.impl `021604` | §3/§6; NF-S7 |
+| CQ-2 decision-② fail-closed | m-3 (step-c) + m-2 | class-conditional **fail-closed** for authority-class `record_integrity ∈ {self_reported, mixed}`; disposition token = **`held`**; m-2 field-home landed (c6: `mixed` leg added) | m-3 fold + m-2 `014626` + m-3.impl `021724` + m-2.impl `021604` | §3/§6; NF-S7 |
 | CQ-3 A-floor table | m-2+m-6 | (phase × record_kind) table, m-6-authored; rides existing monotonic MAX (no new m-2 mechanism) | m-6 `015800` + m-2 mechanics + m-2.impl + m-6.impl `024620` | §3 step 2a; NF-S8 |
 | CQ-4 terminal tokens | m-2+m-3+m-6 | byte-exact **`{accepted, rejected, held}`**; `rejected` shared / `held` distinct; bucket-D aligned; `bounced` retired | m-2/m-3/m-6 + all impl incl. `024620` | §6; NF-S16 |
 | CQ-4b config composition | CTO | per-domain sections → conductor-composed, **single top-level digest** + m-4 per-section version-stamp inside (`022000`) | CTO ruling + m-2/m-3/m-4/m-6 confirms + all impl | §7; NF-S15 |
@@ -230,6 +230,7 @@
 
 Swept: the only "by construction" phrase in this design is §2.4's serialized-loop double-accept kill (control-flow, explicitly scoped, with its D5 residual stated in the same breath). The guardrail is described as confusion-resistant at every mention (§0, §8.1, §8.4, §12 S18); the D4 claim-set language is used verbatim; D5 accepted-risks (config / store-write / operator-FROM under same-uid attach) are restated in §0/§8.4/§9. No "unbypassable", no "sole-writer by construction", no "same-uid write-exclusion" anywhere.
 **rev2 strengthening (from implementer blocker 2):** the sweep now targets **semantics, not just vocabulary** — any exclusivity claim ("only egress", "only writer", "no code path", "sole X") over a surface a same-uid seat can reach outside MCP is a violation unless scoped to the conductor-governed surface WITH the D5 residual stated beside it. r1's §9/NF-S9 unqualified "only egress / only socket writer" was exactly such a miss (caught in review, fixed at rev2); fixture F8 is specified to grep for the exclusivity-claim class, not merely the three banned phrases.
+**c6 strengthening (m-7-F9):** the sweep's specified classes now ALSO include the **writability/reachability token family** — `non-lane-writable`, `lane-proof`, `seat-proof`, "no lane can …", "a lane cannot …" — which makes the same implicit strength claim through a different vocabulary. Any such token over state a same-uid seat can reach outside MCP is a violation unless scoped to the tool/governance surface (no verb exists) with the D5 residual stated or referenced beside it. §3 step 3's previously bare `non-lane-writable` was the motivating instance (now scoped in place).
 
 ## 17. Non-re-cut path (Step-1 → standalone)
 
@@ -241,7 +242,7 @@
 
 ## 19. Acceptance criteria draft (for the eventual operator-opened PLAN — NOT this phase)
 
-1. F1–F10 + NF-S1..S18 pass as conductor-registry checks (E2).
+1. F1–F11 + NF-S1..S18 pass as conductor-registry checks (E2). (F11 = the c6 one-pivot-per-mutation fixture; §22's "F1–F10/G" line records the c4 lock-time set as history — the PLAN gate is THIS line, which includes every later-added fixture.)
 2. kill -9 at any single point in the §3 pipeline yields, after recovery, a store byte-equivalent to either "command fully applied" or "command still queued" — never anything else (crash-point sweep harness).
 3. A seat's full MCP surface enumerates to exactly three tools; the G-fixtures pass against every shipped seat archetype form.
 4. The claim-sweep (F8) passes over all shipped docs.
@@ -266,13 +267,15 @@
 
 **r4 fold (CQ-gate closure, per `master/relays/c4-cq-coord/RECONCILE-orchestrator-planner-20260702-031533.md` + the CQ-6 re-scope `...-032227.md` + VP approve `RECONCILE-orchestrator-reviewer-20260702-032843.md`):**
 1. §15 ledger flipped to CLOSED with resolutions + closing legs; the gate line updated (gate satisfied).
-2. NF-Sx bindings landed: NF-S5→CQ-1(a) step-gate; NF-S7→CQ-2 fail-closed `held`; NF-S8→CQ-3 table + `other`→A fail-safe; NF-S16→CQ-4 byte-exact tokens; NF-S15→CQ-4b composition (single top-level digest + per-section stamp = one integrity root); NF-S12→CQ-5 confirmed ordering; NF-S1/NF-S2→**CQ-6 BASE only** (re-mint-supersedes explicitly excluded from the fixture and never presented as pair-approved); §4→CQ-8 (layout unchanged, m-1-confirmed).
+2. NF-Sx bindings landed: NF-S5→CQ-1(a) step-gate; NF-S7→CQ-2 fail-closed `held` (authority-class `{self_reported, mixed}`, c6-widened); NF-S8→CQ-3 table + `other`→A fail-safe; NF-S16→CQ-4 byte-exact tokens; NF-S15→CQ-4b composition (single top-level digest + per-section stamp = one integrity root); NF-S12→CQ-5 confirmed ordering; NF-S1/NF-S2→**CQ-6 BASE only** (re-mint-supersedes explicitly excluded from the fixture and never presented as pair-approved); §4→CQ-8 (layout unchanged, m-1-confirmed).
 3. CTO integration items applied: (i) m-4 per-section version-stamp inside the single-digest artifact → §7 + NF-S15; (ii) byte-exact `{accepted, rejected, held}` with the `bounced` value token retired — swept §6/§12/§14 ("bounce" survives only as the author-return verb); (iii) m-3's exactly-one-outcome framing checked and confirmed non-expanding → §6.
 4. `re-mint-supersedes` recorded as a §2C build-carry (§20) with its dormancy-in-Step-1 residual stated.
 5. Status line → DESIGN-LOCK-READY r4; DESIGN_LOCK block written at §22.
 
 **r5 fold (m-7.implementer lock-package review `c4-design-m-7/DESIGN-REVIEW-implementer-20260702-034630.md`, verdict must-revise — one stale-fold blocker, verified correct):** §3 step 3 still carried the r2-era "m-7's PROPOSAL / not lockable until CQ-5 closes / may renumber" language inside lock-bearing pipeline text, contradicting the r4-closed CQ-5; and §12 S6 still pointed the observe hook at the pre-rev2 "§3 step 2c". Folded: §3 step 3 rewritten as closed CQ-5 text (post-gate / pre-observe / atomic-bind) citing the four closing legs, with the r1→r2→closure history compressed into a history note; S6 repointed to §3 step 4 with the CQ marked CLOSED; §20's "future DESIGN_LOCK_ID" made current (non-blocking note, folded). The "proposal" wording surviving in THIS §21 fold-log (rev2 entry above) is historical narration of what rev2 did, not lock-bearing design text. No mechanism changed; no other section touched.
 
+**c6 fold (re-review cleanup; VP c6-decomp amendment `c6-decomp/RECONCILE-orchestrator-reviewer-20260702-192059.md` + c6-apply review `c6-apply/RECONCILE-orchestrator-reviewer-20260702-203146.md`):** doc-only consistency corrections to this locked doc, no mechanism change, §22 LOCKED CONTENT unchanged. (i) **NF-S6 internal-fault disposition split on two axes** (authority-class × whether the trusted check could run) — authority-bearing internal fault ⇒ `held` (never silent `rejected`), non-authority ⇒ `rejected`/author-return, distinct from the non-authority *unobservable/no-vantage* record which is `accepted`+`self_reported` (m-7-F3, per the VP-ratified c6-decomp two-axis wording). (ii) **NF-S7 + the §15 CQ-2 ledger row + the r4 fold-log NF-S7 summary widened** from authority-class `self_reported` to `record_integrity ∈ {self_reported, mixed}`, so m-7's CQ-2 fixture converges with the m-2/m-3 CQ-2 canonical (VP-caught token-convergence miss, c6-apply). (iii) **Stale byte-level cites repointed** to stable section-name anchors (the `ARCHITECTURE.md` write-path seam + `m-1 design §5`), which drifted when c5 edited those files (m-7-F4). The serialized-loop two-honest-seats double-accept kill and every §22 lock invariant are untouched.
+
 ## 22. DESIGN_LOCK (assembled; effective on m-7.implementer lock-package approve + VP co-sign)
 
 ```text
@@ -306,3 +309,13 @@
 GRANTS NOTHING: no PLAN, no IMPL, no code/pcode, no spike; Step-1 PLAN remains a
   separate operator-opened gate (re-baseline step (e))
 ```
+
+**c6-fix-m-7 fold (pair slice of the c6 re-review punch-list, dispatch `master/relays/c6-fix-m-7/DESIGN-orchestrator-planner-20260702-204518.md`; doc-only, no mechanism change, §22 lock invariants unchanged; for m-7.implementer review):**
+1. **m-7-F1 (B, ◆ VP-ratified) — one pivot per mutation:** the HELD outcome is now ONE compound canonical record embedding the candidate (§6); `verify()` now commits ONE compound operator-verdict record whose presence IS the decision-scoped burn — no separate burn record (§3, §5 Phase-3, NF-S2); new fixture **F11** asserts every mutation class commits exactly one canonical record via one rename (§13). Burn *semantics* (decision-scoped, atomic, CQ-6 base) unchanged; only the record shape (1 compound vs 2 records with a crash window between renames).
+2. **x3-F2 (B) — trusted-config author set completed:** m-2 + m-5 added (m-5's archetype-registry section was named but its author omitted); §7 + S15; m-5's CQ-4b section confirm subsequently OBTAINED (`c6-fix-m-5/COORD-planner-20260702-205849`).
+3. **m-7-F5 (M) — intake single-writer discipline:** handlers hand commands over a bounded channel to ONE intake-writer task (codex donor applied at the intake tier too); "handlers never touch any file" is now literally true; §1 diagram + §2.1 + §2.2.
+4. **m-7-F6 (M) — NF-S9 grain fix:** "only socket-writing component" → "only component that writes to NON-SEAT destinations" (seat pipes/MCP channels are seat-facing sockets; the old wording contradicted §8.3 delivery).
+5. **m-7-F7 + x2-claim-honesty-F8 (M/C) — README brought current:** status = DESIGN-LOCKED / c4 CLOSED with §22 + VP co-sign pointers; the stale "under VP review" consumes-note replaced with a pointer to the locked seam matrix.
+6. **m-7-F9 (C) — F8 sweep widened:** the writability/reachability token family (`non-lane-writable`, `lane-proof`, `seat-proof`, "no lane can") added to §16's specified sweep classes; §3 step 3's bare instance scoped in place (tool-surface grain + D5 reference).
+CTO-applied c6 items (NF-S6 two-axis, CQ-2 `{self_reported, mixed}` widen, §21 c6 entry, anchor repoints) verified present and NOT re-touched.
+**c6-fix r2 (implementer review `c6-fix-m-7/DESIGN-REVIEW-implementer-20260702-210010.md`, must-revise — both findings verified + folded):** (i) §19 AC1 widened to **F1–F11** so the c6 fixture is inside the eventual Step-1 PLAN gate; §22's "F1–F10/G" line is EXPLICITLY the c4 lock-time historical set (the certificate is not retro-edited) — the PLAN gate is §19 AC1, which includes every later-added fixture; (ii) README hosts+executes line widened to `record_integrity ∈ {self_reported, mixed}` matching the CQ-2 canonical (the c6-dispatched README lines 47-49 miss). Also folded post-review: the m-5 CQ-4b section confirm recorded as OBTAINED (§7/S15; `c6-fix-m-5/COORD-planner-20260702-205849`) + the `archetype_registry` section-key/canonical-ordering answer relayed (`c6-fix-m-7/COORD-planner-20260702-210344`); ARCHITECTURE C4.1's stale author-set line flagged to the CTO (their doc).
--- a/master/domains/m-7-conductor-core/README.md	2026-07-02 19:49:04
+++ b/master/domains/m-7-conductor-core/README.md	2026-07-02 21:05:38
@@ -1,13 +1,21 @@
 # m-7 — Conductor-Core (the runtime substrate)
 
-**Status: BOOTING — scope decomposition under VP review (`design-review` → `conductor-core` standup, 2026-07-01).**
-The 7th standing pair, created in the re-baseline after the adversarial design review (`DESIGN-REVIEW-2026-07-01.md`,
-NO-GO) found that **the running program the six policy domains ride on was never designed or owned** — Step-1 *is*
-"build the conductor core," and there was nothing to build to. m-7 owns that substrate.
+**Status: DESIGN-LOCKED — c4 CLOSED 2026-07-02.** The conductor-core design-of-record is locked:
+`design/2026-07-01-conductor-core-design.md` (DESIGN_LOCK_ID `c4-design-m-7-lock`, §22 lock block;
+GRILL_LOCK `c4-grill-m-7`; pair-approved r3 design + r5 lock package; **VP co-sign
+`master/relays/c4-design-m-7/RECONCILE-orchestrator-reviewer-20260702-040327`**; CQ gate satisfied —
+all eight design-LOCK CQs closed, CQ-6 on the base with `re-mint-supersedes` a §2C build-carry).
+c6 doc-only re-review folds applied per `master/DESIGN-REREVIEW-2026-07-02.md` (fold-log: design doc §21).
+Step-1 PLAN remains a separate operator-opened gate.
 
+*(Domain origin, kept for history: the 7th standing pair, created in the 2026-07-01 re-baseline after the
+adversarial design review found the running program the six policy domains ride on was never designed or
+owned. m-7 owns that substrate.)*
+
 Seats: `m-7.planner` (design-lead) · `m-7.implementer` (adversarial design-reviewer). Domain dir layout per the
-charter (`audit/`, `design/`). First cycle = audit the substrate requirements + design the conductor-core
-design-of-record covering `DESIGN-REVIEW` §2A.
+charter (`audit/`, `design/`). c4 (audit + design, CLOSED) delivered the substrate audit
+(`audit/2026-07-01-substrate-audit.md`, `audit/2026-07-01-audit-pair-reconcile.md`) and the locked
+design-of-record covering `DESIGN-REVIEW-2026-07-01.md` §2A.
 
 ## The one-line boundary
 **Conductor-core owns the ENGINE (how things run); the six domains own the CONTRACTS (what is valid / required /
@@ -22,7 +30,7 @@
   recovered; no lost governance approval, no bricked store; corrupt-record quarantine.
 - **Internal-FAULT disposition** — a trusted-side check that throws / times out / hits a corrupt record yields a
   distinct **held / fail-closed** outcome for authority records (never silent-accept, never unrecoverable-brick).
-- **Trusted CONFIG load + integrity** — loads the (m-6/m-3/m-4-authored) policy config artifact **once at trusted
+- **Trusted CONFIG load + integrity** — loads the (m-2/m-3/m-4/m-5/m-6-authored — c6 x3-F2 full set) policy config artifact **once at trusted
   startup**, integrity-checked; config is conductor-owned and **not in any seat tool surface**.
 - **Attach/pipe lifecycle + the INTERFACE-GUARDRAIL enforcement** — the persistent-seat pipe lifecycle; the seat tool
   surface exposes **only** `submit()`/`project()`/`read()`; raw store/config filesystem paths are **absent** from it
@@ -37,7 +45,7 @@
   (`DESIGN-REVIEW` §2B); the mechanism runs here.
 - **Phase-split required-set** (`§2A.5`): m-1/m-2 own which fields are required-when; m-7 must not demand
   observe-owned fields in a Step-1 that has no observe writer — a joint m-1/m-2/m-7 fix.
-- **m-3** — observe gate (Step-2 hook) + **decision-② fail-closed** on authority-class `self_reported`: m-3 owns the
+- **m-3** — observe gate (Step-2 hook) + **decision-② fail-closed** on authority-class `record_integrity ∈ {self_reported, mixed}` (c6-widened per the CQ-2 canonical; design doc §12 S7): m-3 owns the
   observe/evidence *contract*; m-7 hosts the hook and enforces the class-conditional fail-closed at the gate.
 - **Pure-judgment A-floor** (`§2A.6`): m-2/m-6 own the `gate_category`/`HUMAN_GATE`/`CEREMONY_TIER` floor *contract*;
   m-7 enforces the mandatory floor at fill/submit.
@@ -45,8 +53,9 @@
 
 ## Consumes (collision edges)
 All six policy contracts (it is the host). Foundational upstreams: m-1 store API + m-2 schema. **Collision arbitration
-+ the substrate↔policy seam split stay with CTO+VP** (this README's ownership split is the CTO's proposed
-decomposition, under VP review before the pair designs against it).
++ the substrate↔policy seam split stay with CTO+VP.** (The ownership split proposed here was VP-approved at the
+conductor-core standup and is now realized in the locked design-of-record — the hosted-contract seam matrix,
+design doc §12, is the authoritative per-seam statement.)
 
 ## Explicitly NOT conductor-core (stays policy-domain-owned)
 Model→seat routing decisions (m-4), archetype/ceiling semantics (m-5), gate→email bucketing + ODB + park/wake policy
--- a/master/READINESS-REGISTER.md	2026-07-02 19:49:04
+++ b/master/READINESS-REGISTER.md	2026-07-02 19:53:00
@@ -13,6 +13,8 @@
 
 ## VERDICT: GO-WITH-FIXES  (unanimous — all 7 lenses)
 
+> **[SUPERSEDED WHERE IT CONFLICTS — 2026-07-02]** This GO-WITH-FIXES verdict and the "What the review CONFIRMED works" list below are superseded by `DESIGN-REVIEW-2026-07-01.md` wherever they conflict with it. The NO-GO retraction at the foot of this file is **not** scoped only to the certification block: in particular the away-token "replay/forgery/expiry closed" line and the "unforgeable by lanes" claim in the CONFIRMED-works list do **not** stand — see the annotation on that list.
+
 The design-of-record is **sound in the large and as a standalone-runtime target**; the core composition seams trace
 clean; **nothing is no-go or unbuildable.** But Step-1 must not open PLAN on the store until **Cluster 1** is reconciled,
 and the identity/governance posture needs the **operator decisions** in Clusters 2/5/6. Two recurring root causes:
@@ -244,6 +246,9 @@
 ---
 
 ## What the review CONFIRMED works (adversarially checked, clean)
+
+> **[SUPERSEDED WHERE IT CONFLICTS — 2026-07-02]** Two items in this list are overturned by `DESIGN-REVIEW-2026-07-01.md`: (1) the **away-token "replay/forgery/expiry closed"** line is superseded by FATAL #10 (sibling-token replay) → CQ-6 base + decision-④ rotate+re-observe, an honestly-deferred **(d) build-carry** with adversarial review still owed — it is *not* closed; (2) the **"local operator-FROM unforgeable by lanes"** claim is superseded by the **D4/D5 claim-set** — the honest posture is lane-unforgeable through the tool surface (confusion-resistant; D5 direct-store residual accepted), not unforgeable by construction. The remaining items stand.
+
 - **observe-as-send TOCTOU** genuinely closed by placement (inside `submit()`, trigger≠execute) — the mechanism most
   primed for fault; clean. `m-3 …:31,35`; `m-1 …:92-94`.
 - **J1 never-auto-approve** airtight on *every* path (timeout / resummon-exhaustion / egress-block / away-mode). `m-6 …:60,76,78`.
--- a/master/README.md	2026-07-02 19:49:04
+++ b/master/README.md	2026-07-02 20:09:01
@@ -15,7 +15,7 @@
 unbypassable" claim; **conductor-core** (the running program: serialization / crash-atomicity / config-integrity /
 recovery) is **nobody's domain and has no design doc** yet Step-1 IS "build the conductor core"; policy config is
 **lane-writable under attach** (one Bash line = total governance bypass); altitude-B grain breaks the routing veto;
-the Step-1/Step-2 split dead-ends the write path; R2 is keyed on a **ghost field** (`chosen_model` leaks); pure-judgment
+the Step-1/Step-2 split dead-ends the write path; R2 under-specified/untestable at schema grain (VP-corrected — not a confirmed `chosen_model` leak); pure-judgment
 A-categories silently absorb; and the **five operator decisions were never folded into the docs** (② fail-closed vs
 still-locked fail-OPEN m-3 text). **The bones are sound — it's a bounded re-baseline (weeks), not a rewrite.**
 **Design-review dispatch CLOSED (2026-07-01, VP-approved `design-review/…-150235`)** — NO-GO concurred; deployment fork
@@ -30,7 +30,7 @@
   per-domain-section single-digest trusted config, local-outbox-only send, seat-binding base — folded into
   `ARCHITECTURE.md` **§C4** + `RECONCILE.md` **c4**. **8 CQs closed full-pair** (6 pairs re-engaged; the gate/config
   cluster took an r1→r2 revise cycle; closure gate held throughout); grilled (`c4-grill-m-7`); **claim boundary held —
-  confusion-resistant, sole licensed "by construction" = the §2.4 serialized-loop double-accept kill.** *The NO-GO's
+  confusion-resistant; m-7's headline by-construction claim = the §2.4 serialized-loop double-accept kill (one of the four licensed classes — §C4.3).** *The NO-GO's
   core defect — the running substrate was nobody's domain — is corrected.*
 - **(c) ✅ COMPLETE — global claim-sweep + decision-folds** (`c5` CLOSED 2026-07-02; VP close-approve `c5-decomp/…-143205`).
   All 7 design-of-record docs (m-1..m-6 + `ARCHITECTURE`) swept to the honest line — adversarial-strength claims
@@ -39,7 +39,7 @@
   (`{accepted,rejected,held}`; `bounced` retired). Decisions **③** raise-only (+ §J), **⑤** ODB carve-out, **④** away-token
   **recorded as §2C build-carry** folded; 6 lanes owner+implementer-approved (3 must-revise→approve cycles). Folded into
   `RECONCILE.md` **c5**. **The docs now tell the honest story end-to-end.**
-- **(d)** §2C at its build step (the `re-mint-supersedes` + decision-④ away-token carries + altitude-B land there);
+- **(d)** §2C at its build step (the `re-mint-supersedes` + decision-④ away-token carries + the routing-lane **R2 `gate_referenceable`-per-column** & **altitude-B per-row** carries land there);
   **(e)** THEN Step-1 PLAN.
 
 **Step-1 PLAN remains the operator-opened gate (re-baseline step e). (a)/(b)/(c) done; (d)'s items are deferred
@@ -73,7 +73,7 @@
 
 What c3 locked (detail: `ARCHITECTURE.md` §C3; the two design docs):
 - **m-5** — an **archetype = one governed expansion-slot** binding {topology + gate-set + ceiling-at-spawn +
-  observe-invariants + routing-prior}; `lower_snake_case` tag-space; observer-selected tamper-resistant invariants (F1);
+  observe-invariants + routing-prior}; `lower_snake_case` tag-space; observer-selected control-property invariants (F1);
   3-axis open-map ceiling vector + modular `external_send`; T1/T2/T3 + sensor-full; `actuator` = derived class.
 - **m-6** — a promote-and-bind human surface: A/B/C/D buckets; ODB render+capture + the read-only elaborate-more fork;
   the 7-state park/wake machine; the opt-in egress-gated away-bridge; meeting-lane routing; the interjection host.
@@ -136,10 +136,10 @@
 
 ## Decisions (c1 — LOCKED)
 - **Org / Roadmap / both c1 reconciles** — LOCKED (VP-approved); own-the-gate-first; `C1_PRODUCT_SCOPE = B`.
-- **Transport** — Option A minted per-seat credential = forgery-robust by construction.
+- **Transport** — Option A minted per-seat credential = confusion-resistant (GRILL-LOCK D4; forgery-robust-by-construction = the D3-shelved wrap milestone, not the Step-1 claim).
 - **Schema carrier** — bespoke FieldSpec registry; strict form-only submit; `X-` overflow; zero-migrator versioning.
 - **Shared seam** — R1 operator/special-address; R2 routing = separate seat-stamped relay (model never a gate input);
-  R3 observe-integrity (`evidence_integrity`). PARENT (m-1) strengthens lineage (m-2) → forgery-robust.
+  R3 observe-integrity (`evidence_integrity`). PARENT (m-1) strengthens lineage (m-2) → more strongly confusion-resistant (D5 residual).
 - **identity ≠ authority** — ratified (`ARCHITECTURE.md` §5).
 - **§J operator-judgment** — RATIFIED (`ARCHITECTURE.md` §J): J1 `on_timeout = hold_and_resummon` (never auto-approve);
   J2 `gate_category` default set, **operator-configurable** (forward m-6/config); merge split by target branch +
--- a/master/RECONCILE.md	2026-07-02 19:49:04
+++ b/master/RECONCILE.md	2026-07-02 20:08:56
@@ -2,6 +2,13 @@
 
 Orchestrator reconciliation of paired audits/designs. Incoming reports are E0 until reconciled here against
 file:line evidence. Append per cycle.
+
+> **Claim-boundary / supersession note (applies to all pre-c5 entries below).** Strength vocabulary in the c1–c3
+> historical sections — "forgery-robust by construction", "sole-writer", "closes all three by construction",
+> "confusion-robust → forgery-robust" — predates the fork/relabel decision and now reads **per the c5 relabel**:
+> the honest Step-1 claim is **confusion-resistant** (GRILL-LOCK D4; D5 accepted direct-store residual), with the
+> four licensed by-construction classes enumerated in `ARCHITECTURE.md` §C4.3. The historical entries are preserved
+> verbatim (append-only discipline); this banner supersedes their strength wording. See RECONCILE c5 / D4.
 
 ---
 
@@ -319,7 +326,9 @@
   fields never accepted as FROM). **② was c4** (CQ-2). **③ RAISE-ONLY** A/B + known-A detector (m-6 §2 + **CTO-ratified
   §J**). **⑤** ODB model-name egress carve-out (m-3 scan + m-6 ODB + m-4 R2-guard; confidentiality-scoped only, R2 untouched).
   **④ away-token ROTATE + RE-OBSERVE — RECORDED as a non-locking §2C build-carry** (m-1/m-6 + the integrated §C4 ledger);
-  **mechanism / fixture / adversarial review = a step-(d) gate**, not folded/locked.
+  **mechanism / fixture / adversarial review = a step-(d) gate**, not folded/locked. **Routing-lane §2C (m-4/m-2), RECORDED
+  non-locking:** R2 `gate_referenceable` per-column FieldSpec + `chosen_model`/single-family-bucket-proxy negative fixtures,
+  and altitude-B per-row `justified_deviation`/`deviation_reason_code` grain — mechanism + fixtures = a **step-(d) gate**.
 - **§2A.6 A-floor + §2A.7 decision-②** guardrails intact (c4 CQ-3/CQ-2).
 
 **Byte-consistency (CTO cross-doc, VP-verified clean).** Zero unclassified raw overclaim survives; terminal-token vocabulary
@@ -334,5 +343,6 @@
 
 **Authority boundary.** No mechanism change (claim-text + recorded-decision folds only), no design-lock reopen, no PLAN /
 IMPL / code / `pcode/` / spike granted. **Re-baseline status: (a) ✅ (b) ✅ (c) ✅.** **(d)** §2C-at-build-step (the
-away-token / away-bridge mechanisms — decision ④ + m-7 `re-mint-supersedes` — land at their build step) and **(e) Step-1
+away-token / away-bridge mechanisms — decision ④ + m-7 `re-mint-supersedes` — **and the routing-lane R2 `gate_referenceable`-per-column
++ altitude-B per-row carries** — land at their build step) and **(e) Step-1
 PLAN** (operator-opened) remain. Pairs stood down; next is the operator's call.
