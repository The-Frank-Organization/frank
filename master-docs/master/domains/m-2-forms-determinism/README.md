# m-2 — Forms & Determinism (domain charter)

**Pair:** `m-2.planner` (design-lead) · `m-2.implementer` (adversarial design-reviewer).
**Owns (the relay ENVELOPE):** the declarative field schema (field · owner · type · required-when · enum-set · consumer); the field-ownership model (system-filled / seat-scoped-enum / agent-enum-pick / free-text); the linter's refactor from prose-ambiguity defenses into **form-validation** (intra-relay) + a **lineage engine** (cross-relay).
**Thesis:** relays stop being hand-authored markdown and become a form the courier renders from typed input — "fill the form, don't write the prose" — enforcing authority at FILL (a forbidden option is absent from your seat's form), not post-hoc.

**Consumes (collision edges):** m-1 store (persists/renders the form; supplies FROM/PARENT system-fill). **Co-foundational:** m-2 schema + m-1 store API lock first; m-3/m-4/m-6 design against the locked schema.
**Boundary (m-2 declares the SLOT; siblings own the MECHANISM):** m-3 observe/evidence fills evidence fields; m-4 routing supplies routing-record values; m-6 human-surface consumes the HUMAN_GATE/email-projection fields.

## Status
| Cycle | Phase | Artifact | State |
|---|---|---|---|
| c1 | AUDIT | planner + implementer independent audits | **done**, reconciled + VP-approved (`../../RECONCILE.md`) |
| c1 | DESIGN | design doc `c1-design-m-2-forms-determinism` | **done** — `design/2026-06-28-form-schema-design.md`; GRILL_LOCK folded |
| c1 | DESIGN-REVIEW | round 1 | **must-revise** (narrow; both blockers correct) — `../../relays/c1-design-m-2/DESIGN-REVIEW-Implementer-20260628-173310.md` |
| c1 | DESIGN | doc rev1 (fold) | **done** — ROLE=system-owned; two-state delivery (lineage-before-deliver); + m-1 refinements (TO/CC recipient_picker, null-reserved certification). See doc §16 |
| c1 | DESIGN | m-1 co-foundational coordination | **converged** — proposal `COORD-planner-172553`, m-1 reply `COORD-planner-173516`, ack `COORD-ACK-planner-174201` |
| c1 | DESIGN-REVIEW | round 2 | **APPROVE** — `../../relays/c1-design-m-2/DESIGN-REVIEW-Implementer-20260628-175833.md` (both blockers resolved at E1) |
| c1 | SITREP | design-complete (rev1) → orchestrator | **sent** — `SITREP-planner-180444` |
| c1 | DESIGN | rev2 fold (consumer review m-3/m-4/m-6) | **done** — dispatch `c1-refine-m-2`; folded G1 (HUMAN_GATE hybrid), G2 (gate→email enum), Q-B (ODB ~10-field), routing-relay record (Q-C), slot_in reserved. See doc §17 |
| c1 | DESIGN | m-1 rev2 re-affirm | **mutual + closed** — both directions: m-1 `165643`+`170308` ↔ my `165702`+`170032` (R1 operator-addr, R2 routing_ref non-gating, R3 DI-5 evidence_integrity) |
| c1 | DESIGN-REVIEW | round 3 (rev2) | **APPROVE** — `../../relays/c1-refine-m-2/DESIGN-REVIEW-Implementer-20260629-170241.md` (5 acceptance points at E1) |
| c1 | SITREP | design-complete-rev2 → orchestrator | **sent** — `../../relays/c1-refine-m-2/SITREP-planner-20260629-170716.md` |
| c1 | LOCK | joint m-1↔m-2 co-foundational lock | **✅ CLOSED** — contract mutually re-affirmed; Cluster 4 closed (full-record mirror + posture carve-out + computed-field homes); m-7 lock CQs (CQ-1/2/3/4/4b) m-2-approved; see the c4/c5 rows below |
| c1 | READINESS-FIX-c1 | write-path seam fold (MUST-before-Step-1) | **✅ CLOSED** — `send()` §4 steps 4–5 reconciled to the VP-co-signed seam (pre-append form+lineage → atomic `accepted` / terminal `rejected`; no `submitted` limbo; Step-1 = store+form+lineage); byte-consistent w/ m-1 `224500`; CTO re-verified + VP closure co-sign `230335`. Report: `../../relays/readiness-fix-c1/SITREP-planner-20260630-225007.md` |
| c1 | READINESS-FIX-c4 (4a) | routing R2 grammar fix | **✅ VP-approved** (`232925`) — `declared_deviated`(planner)/`deviated_observed`(observe) split, `any_row:` aggregate trigger, `field:<id>` model-identity exclusion. §18 + AC14 |
| c1 | READINESS-FIX-c4 (4b+4c) | full-record mirror + computed-field homes | **folded** — §17.3 mirrors m-4 `:200-210` (per-row fields + `deviation_reason_code` + reserved `constraints`/`template_ref`); new §17.6 declares `record_integrity`/`surface_intent` homes. §18 + AC15. Report: `../../relays/readiness-fix-c4/SITREP-planner-20260630-234702.md`. 4 sibling confirms in |
| c1 | READINESS-FIX-c4 (posture carve-out) | CTO arbitration `001537` — posture home | **folded (corrected)** — my standalone `human_mode`/posture per-row field was wrong (contradicted m-5 c3 lock `:142` "no new m-2 field"); **removed it** — posture rides `seat_archetype`; kept m-5 posture value-enum (LOCKED at c3) in §17.6; m-4 record un-extended. Report: `../../relays/readiness-fix-c4/SITREP-planner-20260701-012000.md`. **✅ Cluster 4 CLOSED** (owner ruling-confirms + CTO/VP re-verify complete; F2 propose-vs-stamp per-column mix folded in c6 m-5-F1) |
| c1 | Q-F (SHOULD, tracked) | m-3 observe-field row-parity | **accepted, pre-PLAN** (CTO `001537`) — promote 7 §12-prose observe fields into formal §17.3 rows for machine-checkable §13 fixtures; after Cluster 4 closes, before Step-1 PLAN (doc §15 Q-F) |
| c1 | Q-E | `delivery_state` token semantics | **✅ RESOLVED via CQ-4** — `{accepted, rejected, held}`, `bounced` unified to `rejected`; observe-bounce token = m-3's call in CQ-4 (doc §15 Q-E/§17.1) |
| — | c4-cq-gateconfig | m-7 design-lock CQs (CQ-2/3/4/4b) | **✅ m-2 half APPROVED (r2)** — m-2.implementer `021604` approve (3 r1 blockers resolved: `held` narrowed to m-7 §6 internal-fault + CQ-2 fail-closed; `held` consumer carve-out; byte-exact §J2 `gate_category`). Answer `../../relays/c4-cq-gateconfig/DESIGN-planner-20260702-021108.md`; SITREP `SITREP-planner-20260702-021849.md`. m-2's half approved first; the joint CQ closure + CTO fold now proceed **under the m-7 design-lock / c6 gate** (no m-2-blocking leg outstanding) |
| — | c5-claim-sweep-m-2 | claim-text hygiene (malicious→confusion-resistant + D5) | **✅ COMPLETE + pair-approved (r2)** — m-2.implementer `140747` approve (both r1 blockers closed). 17 relabel loci (confusion-resistant + D5) + 14 `[c5 KEEP]`; union-net grep → only :47 (non-overclaim NOTE). Claim-text only, no mechanism change. Rev2 `../../relays/c5-claim-sweep-m-2/DESIGN-planner-20260702-135144.md`; SITREP `SITREP-planner-20260702-141054.md` → CTO folds into c5 status ledger |
| — | c4-cq-m1 (CQ-1 co-sign) | `required_when` step-gate half | **✅ APPROVED + triad complete** — m-2.implementer `030145` approve (no must-revise); m-1 concurs `025200` (non-gating, no boundary drift). Co-sign `../../relays/c4-cq-m1/DESIGN-planner-20260702-024704.md`. CQ-1 evidence set complete (m-1 `013500`+`020418` + m-2 `024704`+`030145`) → **CTO fold** (m-7 NF-S5). New `layer_present:` §5 atom + AC17 |
| — | c6-fix-m-2 | re-review cleanup (7 doc-only findings) | **folded** — m-2-F1 (③ RAISE-ONLY `gate_category` monotonic-toward-A, AC18), F10 (canonical id `gate_category`), F5 (`gate_referenceable:bool` first-class), F7 (this README sweep), F9 (§15 RATIFIED→§J1/§J2), m-5-F1 slice (propose-vs-stamp per-column), m-5-F2 (m-5-ledger, no m-2 edit owed). Doc §18 c6 fold-log. **✅ COMPLETE + pair-approved** (m-2.implementer `211048`; r1 README gap folded r2). Completion `../../relays/c6-fix-m-2/SITREP-planner-20260702-211306.md` → CTO c6 verify sweep + c6 close |
| — | c6.1-confirm-m-2 | differential seam-convergence confirm (3 deltas) | **confirmed** — CTO seam-arbitration byte-verified (E2) against locked targets: `held` one-compound-record (m-7 §6/F1), routing author-set +operator-on-template-spawn (m-4 §7/F4), `deviation_reason_code` config-sourced value-set (m-4 §5/§6/F5); + fixed a §12-echo propagation gap. **✅ COMPLETE + pair-approved** (m-2.implementer `003501`, r2; r1 §12-echo + r2 AC12-echo gaps folded). 3 deltas byte-verified (E2): `held` one-compound-record (m-7 §6/F1), routing author-set +operator-on-template-spawn (m-4 §7/F4), `deviation_reason_code` config-sourced (m-4 §5/§6/F5). Completion `../../relays/c6.1-confirm-m-2/SITREP-planner-20260703-004725.md` → orchestrator c6.1 re-close (VP) |
| — | s1-fidelity-m2 | F3 fidelity edge — s1 consuming-surface usage vs the locked m-2 envelope | **✅ APPROVE (usage-fidelity only)** — m-2.implementer `.relays/s1/s1-fidelity-m2/SITREP-implementer-20260703-171027.md`: §J2 byte-custody, grant narrowing, `held` usage, R1 replay classification, MVP FieldSpec instantiation all faithful to the locked design; contract unchanged. m-2.planner on CC (not the review seat) |
| — | s3-guide-q1 | m-2 as S3 domain guide — 3 confirm items answered **from locked text** | **✅ ANSWERED** — `../../relays/s3-guide-q1/SITREP-planner-20260704-173000.md`: Q1 GRILL_REQUIRED FieldSpec = `bool`/`agent_enum_pick`(monotonic RAISE-toward-yes)/`gate_referenceable:true` (m-6 §5 route; R2-safe); Q2 step-gated replay lands in "caught" under reconstructed observe-context (no third bucket, w/ live-vs-reconstructed label guardrail); Q3 flag-based negative grain S3-sufficient (concrete `chosen_model`/bucket-proxy negatives defer to S4/m-4). **No amendment to locked text, no master escalation.** 1 bounded S3-owned choice (GRILL_LOCK_ID dependent-required?) + 1 S4/m-4 boundary flag surfaced. **✅ Guide answers consumed faithfully at the S3 exit gate** (`.relays/s3/s3-exit-gate/SITREP-orchestrator-planner-20260704-213740.md`, E2 @ `s3-form-impl@fe7308e`): Q1 shape live + the pair's lock-bearing dependent-required (both legs fixtured); Q2 implemented verbatim w/ the no-live-tally guardrail as a structural `LiveCaughtCount` exclusion; Q3 both-flags model-identity chain at load-time. No guide-finding |
| — | s3-exit-gate | S3 EXIT GATE report of record (m-2 in TO as guide) | **✅ GREEN at E2 — VERDICT merge-blocked, operator's close/merge decision.** Full FieldSpec registry live end-to-end (fresh-store), the 62-check dissolution EXECUTED not asserted (115 anchor rows, `:840-873` rowed, zero uncovered), all owed §C4 carries discharged. No action owed to m-2 (no finding surfaces); the operator holds S3-close + merge (`s3-merge-gate/MERGE-GATE-...-213741`) |
| — | s4-wire-impl | m-2 micro-consult (guide) — confirm 5 owed FieldSpec rows for the F-GATE-2 fold | **✅ CONFIRMED** — `../../relays/s4-wire-impl/SITREP-planner-20260705-224500.md`: `owner`/`source`/`target_surface`/`disposition_path` = `free_text`/`text`, `disposes_owed` = `id_ref`, all `gate_referenceable:false`, gated on `record_kind` (∈`owed_item`/`owed_disposition`) — verified against `engine/submit.go` enforcement + the live registry. **RECOMMENDED** the dedicated `record_kind_in:` §5 atom over the generic `field:record_kind==` form (grammar-valid either way; fold not held). Free-text render-scope lean confirmed locked-consistent (seat_scope is enum-only). **Routed 2 out-of-fence items** to s4.orch-planner (owed-authoring intake-authority since record_kind is `system_only`; future `owner`-as-`address` / owed-id picker). No amendment, no master escalation |
| — | s5-fidelity | § m-2 section of s5's 11-Q Slice-5 fidelity packet (hub-and-spoke via master) | **✅ ANSWERED** — `../../relays/s5-fidelity/SITREP-planner-20260706-032000.md` → master. **Q1** GRILL_REQUIRED fully already-closed (both legs live @`67ee23e`; owed-#3 → empty); **Q2** exact `named_enums` delta for `routing_escalation` (A-member, MINOR, pending m-4); **Q4** migrator ruling confirmed **+ the `:126` line: Q2 add=MINOR vs Q6 `record_kind` ODB member=MAJOR** (master's Q6×Q4 reconcile crux); **Q7** `scope_paths` struck-until-Step-2; **Q9** async — `authority_class`=`bool` (correction) + `record_integrity` enum, m-3 owns sub-enum values; **Q10** async — `authority_ceiling`=`object` reserved-shape + accepts_interjection/template = m-5 config. No c1-lock reopen |
| — | s5-escalations | § m-2 of s5's escalation bundle (M-1 blocker + M-3 batch) | **✅ ANSWERED** — `../../relays/s5-escalations/SITREP-planner-20260706-053000.md` → master. **M-1 (blocks s5-a PLAN): BLESSED idiom (i)** `visible_when {layer_present:observe}` as a pure step-gate on the ~10 lane-fillable consumer rows — verified renders-off in Step-1 (render.go `renderable()`), the **only** in-grammar option (observe = sole non-Step-1 layer), annotated as step-gate proxy (not observe-owned); option (ii) does NOT return to master+VP. **M-3 (a)–(k) confirmed** — (b)/(c)/(h)/(i)/(j) are defect fixes my locked design mandates (code→contract compliance); **(e) discovered `record_kind` retyped `system_only`→`seat_scoped_enum`**, making my s4-wire fill-time-authority item actionable (recommend operator-only owed scope, m-1 confirms intent). No amendment, no c1-lock reopen |
| s6 | DESIGN-AMENDMENT | m-2 transport-codec amendment (the FIRST c1 amendment: F6/F7/F2/F13/F12) | **✅ COMPLETE + pair-approved (implementer `190100`, r2)** — doc `design/2026-07-06-s6-transport-codec-amendment.md` (DESIGN_DOC_ID `s6-amend-m-2-transport-codec`). **ONE canonical envelope codec** kills the render↔validate↔gate↔deliver divergence class: F6 silent-drop (single decode; Header list = recipient truth in EVERY projection), F7 CC-deadlock (gate decodes via codec), F2 dead-edge parent (consumes m-1 fork; dissolves under conductor-computed PARENT), F13 three-layer authorization (membership/`seat_scope`/per-kind; `genesis` seat-scope-rejected), F12 waiver bound to m-1's F17 record. **r1 must-revise → rev1 folded** (3 blockers all correct, 2 caught me contradicting my own s5 rulings). Amends c1 §2/§4/§12 via review-driven path — **no c1 fold until VP co-sign**. Completion `../../relays/s6-amend-m-2/SITREP-planner-20260706-190600.md` → master. **Held** for master integration (m-1/m-2/m-7 docs + seams) + VP co-sign; then s6 build slice. Seams declared: F2←m-1 fork, F12↔m-1 F17, `Envelope.To`/digest→m-7 |
| s6 | DESIGN-ADDENDUM (B-2) | typed BOOT form (operator-requested boot-stage leg) | **✅ COMPLETE + pair-approved (implementer `202400`, r1 clean)** — §11 of the amendment doc. **Token RULED: `PHASE: SITREP` + lifecycle-gated form, NO new `BOOT` phase** (shared-vocab untouched; no `record_kind` member = MAJOR; no §J2; no §5-atom; no seat-verb). Minimal honest set = `charter_loaded` (bool, `self_reported`) + `dispatch_status` (`{read,awaiting}`); identity = channel-stamped FROM (not a seat field). Un-bounceable by construction (A-1 digest + branch-A no-parent + codec enum). Carry-forward: any `boot_ack` marker must be system-derived/non-gate-ref/never-seat-filled. Completion `../../relays/s6-amend-m-2/SITREP-planner-20260706-203000.md` → master. Folds into the s6 integration package. Seams: B-1 lifecycle/roster→m-7, B-3 activation→m-1 |
| — | s6-fidelity-m2 | s6 BUILD design r2 reviewed vs the m-2 amendment (m-2.implementer; m-2.planner on CC) | **✅ approve-conditional for PLAN** (`.relays/s6/s6-fidelity-m2/SITREP-implementer-20260707-012246.md`) — conditions F-S6-M2-1..4 (seven transport rows + boot separate; waiver render-absence via `visible_when {seat_is}` + submit-rejection; `seat_mint`/`waiver_retraction` three-layer; B-2 as written) carried into the PLAN. **Correction folded**: render-absence of free_text IS expressible via `visible_when {seat_is}` (not `seat_scope`) — my earlier "not expressible" was over-general; see memory [[frank-seat-render-scoping]]. No m-2.planner action owed (CC) |
| c1 | PLAN | gated design-doc PLAN | **✅ OPENED — Step-1 build underway.** All pre-PLAN gates closed (Cluster 1/4, m-7 CQs, c6, c6.1); Step-1 PLAN opened → the build runs in `frank/` (relays under `.relays/`). m-2's design-of-record is **locked + frozen**; the m-2 pair now serves the build as **fidelity reviewer** (s1, ✅) + **domain guide** (s3, ✅ above), never re-cutting the locked contract |

## Layout
- `audit/` — AUDIT artifacts (durable home; the relay under `master/relays/c1-audit-m-2/` is the lint-checked report of record).
- `design/` — DESIGN docs + grill locks (later, after PROCEED).
