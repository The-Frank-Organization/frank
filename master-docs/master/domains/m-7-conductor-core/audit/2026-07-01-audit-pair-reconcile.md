# m-7 c4 AUDIT pair-reconcile — merged seam matrix + unified CQ list

**Artifact** · Owner: m-7 · Cycle c4 · 2026-07-01 · Basis: `master/relays/c4-audit-m-7/AUDIT-planner-20260701-160243.md` ⊕ `master/relays/c4-audit-m-7/AUDIT-implementer-20260701-155145.md`, routed by `master/relays/c4-audit-m-7/RECONCILE-orchestrator-planner-20260701-160639.md`.
**Status:** planner-authored merge; **CONFIRMED by m-7.implementer** — `master/relays/c4-audit-m-7/RECONCILE-implementer-20260701-161137` (convergence confirmed; supersedes the earlier "confirmation pending" line per the c4-design dispatch VP carry-forward #1). Convergence-confirm + merge, not dispute resolution.
**Scope discipline:** no CQ resolved here; no locked contract reopened; no design started. Biting negative fixtures are *completed* at DESIGN-lock; this artifact fixes the converged inventory + the fixture set.

## 1. Reconciliation log (per protocol: agree / disagree / different coverage / operator decision needed)

| point | disposition |
|---|---|
| PRIMARY_BUCKET still-open; entire §2A substrate net-new; no existing conductor (duplicate gate run both sides) | **agree** (independent) |
| Promote-parts donor set (the upstream contract layer; jcode write discipline + connection binding; claude-code mailbox+lock+re-read; codex serialized writer + ToolExposure + config-lock; SQLite/rename/fsync/Maildir) | **agree** (independent; planner adds jcode idempotency-replay + claude-code mark-read-after-deliver/priority-dequeue; implementer adds jcode resume/takeover rejection + channel-subscription dual-index projection prior art — merged, no conflict) |
| Claim boundary: by-construction licensed ONLY for the serialized-loop double-accept kill; no adversarial sole-writer/unbypassable claims; claim-sweep fixture required | **agree** (independent, near-verbatim) |
| Interface guardrail: conductor-as-MCP, exactly `{submit, project, read}`, raw store/config/outbox paths absent; confusion-resistant only | **agree** (implementer adds **outbox** and **operator-channel** paths to the absent-set and "no config value in prompt/tool-result surface" — folded into the fixture, strengthens) |
| Over-reach discipline: every hosted contract mapped to a policy owner; m-7 never authors policy | **agree** |
| Bucket vocabulary: planner filed `already-closed: none` + `product-overlapped: none material` (promote-parts listed inside recommended framing); implementer filed the promote-parts under `already-closed` and the ownership boundary under `product-overlapped` | **different coverage — vocabulary only, zero substantive delta.** Merged reading (orchestrator-framed): PRIMARY still-open; the locked m-1..m-6 *contract shapes* and donor *components* are promoted (never rebuilt) inside the net-new engine; the hosted domains are a boundary discipline, not an overlap. No operator decision needed |
| Fixture set | **complementary — merged §3** (implementer's 8 DESIGN fixtures ⊕ planner's 3-part guardrail negative + live-store E2 probes as the running counter-example) |
| CQ set | **complementary — merged §4** (planner CQ-1..CQ-6 ⊕ implementer's four overlap hazards; hazards map onto CQ-1/CQ-2/CQ-4/CQ-6 + one new CQ-7) |

Residual divergence: **none found** (per the routing relay's expectation).

## 2. MERGED SEAM MATRIX (converged inventory; biting negatives completed at DESIGN-lock)

Planner rows S1–S16 ⊕ implementer rows (two additional: integrated-architecture, claim-boundary) ⊕ implementer fixtures F1–F8 ⊕ planner guardrail fixture G(i–iii). "PF/NF: design" = fixture authored at DESIGN-lock per the matrix requirement.

| # | contract (owner) | doc/section | m-7 execution obligation | positive fixture | negative fixture | CQ? |
|---|---|---|---|---|---|---|
| S1 | store append + channel-stamped FROM (m-1) | `master/domains/m-1-trust-identity/design/2026-06-28-trust-identity-design.md` §5 :97-107, §6, §4 | four verbs; stamp from channel binding; one atomic accepted-append + INDEX + mailbox deliver in the loop; terminal `rejected` on fail; serve `read` | **F2**: accepted submit ⇒ exactly one canonical record, one rendered relay, one INDEX row, expected mailbox projections | **F3**: crash after staging, before commit marker ⇒ no accepted record visible; staging removed/quarantined. **F4**: crash after canonical commit, before projections ⇒ startup rebuilds INDEX/mailboxes from canonical records, no duplicated authority consumption | CQ-6 (binding persistence) |
| S2 | away-token mint/verify + nonce-burn, Seam C (m-1) | m-1 §C3.4 refs; `master/domains/m-6-human-surface-scheduler/design/2026-06-30-human-surface-scheduler-design.md` §4 :85-88, §12 | execute `verify()` five checks; nonce-burn = atomic conductor append **in the same critical section as submit**; sibling-burn per `master/DESIGN-REVIEW-2026-07-01.md` §2C | **F1**: two simultaneous `verify(token)` enter the loop ⇒ one commits burn, the other observes already-burned and fails closed | PF/NF: design (replayed-token + expired-token rejects) | CQ-6 |
| S3 | form render + fill-time authority (m-2) | `master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md` §3, §4 steps 1–6 | render trusted-side per (seat, phase, tier, slot); forbidden options absent; validate constrained picks; courier-fill system fields | PF: design (below-floor HUMAN_GATE write rejected — m-2 AC9 fixture, hosted) | NF: design (pair-seat form cannot render/submit merge-grant — m-2 AC2 fixture, hosted) | — |
| S4 | form-validation + lineage gate (m-2) | m-2 §4 steps 4–5, §10b/§10c, §11; `master/ARCHITECTURE.md:58-66` | (a) form-validation then (b) lineage walk over `persisted ∪ {candidate}` in-courier pre-append; blocking edges only for authority-bearing records; bounce names failing field/edge | PF: design (authority record blocked until lineage passes; no `submitted` limbo observable) | **F5**: form validator **throws** on an authority-bearing relay ⇒ typed held/fail-closed, author-visible; never accepted, never process-bricking | — |
| S5 | phase-split required-set (m-1+m-2, joint w/ m-7) | m-1 §5 :104; m-2 §4 step 4c; `master/DESIGN-REVIEW-2026-07-01.md` §2A.5 | Step-1 form gate must not demand observe-owned fields with no Step-1 writer | PF/NF: design (Step-1 IMPL report accepted without observe-owned fields OR with conductor-filled values — per CQ-1 resolution) | NF: design | **CQ-1** |
| S6 | observe-as-send hook (m-3) | `master/domains/m-3-observation-evidence/design/2026-06-29-observe-evidence-design.md` §2, §3 | host `observe_gate()` inside atomic submit (Step-2 hook named now, inert Step-1); enforce positive write-allowlist; bind passing observation to gated record | PF: design | NF: design (hook writing outside allowlist = violation — m-1 §13.4 fixture, hosted) | CQ-5 (ordering) |
| S7 | decision-② fail-closed on authority-class `self_reported` (m-3 + operator decision ②) | m-3 §3.2 :63 (currently universal fail-open); `master/DESIGN-REVIEW-2026-07-01.md` §2A.7 | execute class-conditional fail-closed at the gate for authority-class records | PF/NF: design — **requires CQ-2 resolution first** (field home + names) | NF: design | **CQ-2** |
| S8 | pure-judgment A-floor (m-2/m-6) | `master/DESIGN-REVIEW-2026-07-01.md` §2A.6; anchors m-2 §3 monotonic floor + `master/ARCHITECTURE.md` §J2 | enforce mandatory HUMAN_GATE floor by (phase × record_kind) at fill/submit; CEREMONY_TIER monotonic-with-floor (below-baseline ⇒ `gate_category=ceremony_downgrade`) | PF/NF: design — **requires CQ-3 table owner** | NF: design | **CQ-3** |
| S9 | egress / content-safety gate (m-3) | m-3 §7 :116-122 | host fail-closed scan at the sole-external-send chokepoint (conductor outbox); dormant until away bridge | PF/NF: design | NF: design | — |
| S10 | routing record + route_dispatch fail-closed (m-4) | `master/domains/m-4-routing-policy/design/2026-06-29-routing-policy-design.md` §3, §5, §7 | sequence routing-record acceptance; host fail-closed refusal; never a silent default model; engine gates never read model identity (R2 preserved at execution) | PF: design (no-acceptable-route ⇒ `routing_unavailable`, never a default) | NF: design (m-2 AC14 model-predicate-rejected fixture, hosted) | — |
| S11 | template spawn + pane-spawn (m-4/m-5; conductor-core executor) | m-4 §0 GL-4 + §7; `master/domains/m-5-workflows-archetypes/design/2026-06-30-archetype-system-design.md` §7, §10 | open/name panes via existing multiplexer (Step-1); deliver boot relays; record per-assignment `seat_archetype`+`authority_ceiling` at spawn (F2 home = m-4 record); Step-1 ceiling = host-config best-effort, stated bound | PF: design (off-floor template slot records a deviation — m-5 §13 fixture, hosted) | NF: design | — |
| S12 | `slot_in` conductor-classification at acceptance (m-3/m-5) | m-3 §5.1 :100-103 (pipeline point "a PLAN detail"); m-5 §4 | classify work-archetype at acceptance inside the commit loop, immutable, non-lane-writable; done-predicate reads it | PF/NF: design (re-tag-to-escape blocked — m-5 §13 fixture, hosted) | NF: design | **CQ-5** |
| S13 | scheduler park/wake 7-state machine (m-6) | m-6 §4 :71-89 | persist transitions as store records (store = checkpointer); wake = one pipe write; re-observe-on-wake sequencing; resummon timers | **F4** (shared with S1): parked-lane state recoverable from store alone after crash | NF: design | — |
| S14 | delivery/projection buckets + ODB (m-6) | m-6 §2 :31-49, §3 | deliver accepted records into TO/CC mailboxes; project buckets off locked fields; render ODB slots | PF: design | NF: design | CQ-4 (bucket-D token) |
| S15 | trusted config artifact (m-6/m-3/m-4 author; m-7 loads) | `master/GRILL-LOCK-deployment-fork-2026-07-01.md` D2b; `master/DESIGN-REVIEW-2026-07-01.md` §2A.2; §J2 | load once at trusted startup; integrity-check (digest into genesis/runtime metadata); expose internally to policy engines; absent from every seat tool surface; fail-closed on mismatch | PF: design (clean load ⇒ digest stamped) | **F7**: config digest mismatch at startup ⇒ authority-bearing submit/verify refused; diagnostic/status projection still served | **CQ-4b** (artifact composition/format owner) |
| S16 | terminal-state token set (m-2/m-3/m-6) | m-2 §17.1 Q-E :266; `master/ARCHITECTURE.md:287` held un-swapped | the engine's closed record-state enum {accepted, rejected, held, …} executes whichever token set the owners settle; bucket-D naming follows | PF/NF: design — **requires CQ-4 resolution** | NF: design | **CQ-4** |
| S17 | integrated architecture-of-record (CTO/VP) | `master/ARCHITECTURE.md:17-66, :287-337` | execute the typed-envelope store + human-surface/away-token architecture in the locked order; no direct lane path into store/config/outbox; collision arbitration stays CTO/VP — m-7 surfaces questions, never arbitrates silently | (composite of rows above) | (composite) | — |
| S18 | deployment-fork claim boundary (operator grill-lock) | `master/GRILL-LOCK-deployment-fork-2026-07-01.md` :30-49, :65-88; `master/DESIGN-REVIEW-2026-07-01.md` §2 | build the interface guardrail + trusted config + serialized commit + crash recovery + local-outbox as CONFUSION-RESISTANT mechanisms; sweep every adversarial-strength claim | **G(i)**: enumerate a seat's presented tool registry ⇒ exactly {submit, project, read}, zero path-bearing tools/resources | **F6/G(ii,iii)**: seat instructed to edit raw store/config/outbox ⇒ no such tool exists; only store effect is via `submit()`; store/config path strings + config values absent from every seat-deliverable projection/prompt/tool-result. **F8 (claim-sweep)**: final design text contains no "unbypassable / sole-writer by construction / same-uid write-exclusion" claim for Step-1 attach | — |

## 3. Merged fixture set (DESIGN inherits; completed + made biting at DESIGN-lock)

F1 double-verify serialization (S2) · F2 crash-free multi-file commit shape (S1) · F3 staging-crash recovery (S1) · F4 projection-crash rebuild without authority re-consumption (S1/S13) · F5 validator-throw ⇒ held/fail-closed (S4) · F6+G(i–iii) guardrail negatives incl. outbox/operator-channel paths and config values (S18) · F7 config-digest mismatch fail-closed with diagnostics (S15) · F8 claim-sweep (S18). Plus the planner's live-store E2 probes as the standing counter-example baseline (unserialized INDEX interleaving; two-step non-atomic seat commits).

## 4. UNIFIED CQ LIST (owners agreed; NOTHING resolved here; resolution = targeted COORD at DESIGN)

| CQ | question | policy owner | source (both audits) | blocks |
|---|---|---|---|---|
| **CQ-1** | phase-split required-set: step-gate observe-owned `required_when` predicates on observe-layer presence OR define a Step-1 conductor-side filler; not yet visible in locked m-1/m-2 text | m-1 + m-2 (joint COORD w/ m-7) | planner S5; implementer m-3 row note | **design-LOCK** |
| **CQ-2** | decision-② class-conditional fail-closed: fold into locked m-3 §3.2/§8/§12 + concrete m-2 field home/names for "authority-class `self_reported`" so m-7 can enforce mechanically. **Foregrounded**: this is the "decision recorded but never folded" pattern the NO-GO named | m-3 (fold owned by re-baseline step c, CTO-sequenced) + m-2 (field home) | planner CQ-2/S7; implementer hazard 1 | **design-LOCK** |
| **CQ-3** | pure-judgment A-floor table by (phase × record_kind): needs a locked policy author; m-7 owns only enforcement | m-2 + m-6 | planner CQ-3/S8 | **design-LOCK** |
| **CQ-4** | terminal-state token set: Q-E `bounced`→`rejected` unification + the new HELD fault state + m-6 bucket-D naming — the engine's closed state enum forces this open SHOULD-fix | m-2 + m-3 + m-6 | planner CQ-4/S16; implementer hazard 3 | **design-LOCK** |
| **CQ-4b** | trusted-config artifact composition/format (one artifact vs per-domain files) + load contract | m-6/m-3/m-4 (authors) → m-7 consumes | planner S15 | **design-LOCK** |
| **CQ-5** | `slot_in` classification ordering inside the commit pipeline (classify-at-acceptance point relative to observe-predicate selection) | m-3 + m-5 | planner CQ-5/S12; implementer m-3 row note | **design-LOCK** (mild — joint ordering statement, no policy change) |
| **CQ-6** | persisted seat-binding table + away-token sibling-burn/restart semantics (`master/DESIGN-REVIEW-2026-07-01.md` §2C): m-7 surfaces the table + consume-once transaction; m-1/m-6 own token semantics | m-1 (+ m-6 policy edges) | planner CQ-6/S1-S2; implementer hazard 4 | **design-LOCK** |
| **CQ-7** | row-parity for the remaining 7 observe fields (m-2 §15 Q-F tracked SHOULD): m-7 flags, does not invent; cleaner Step-1 fixtures if landed before PLAN | m-2 (+ m-3/m-6 confirms) | implementer hazard 2 | **neither** (tracked pre-Step-1-PLAN SHOULD; not a c4 design gate) |

**Nothing blocks design-START.** CQ-2 and CQ-4 are the foregrounded design-LOCK blockers (per the orchestrator's routing relay); all CQs resolve via targeted COORD or their existing owners' tracked items — never silent reinterpretation by m-7.

## 5. Planner's four cross-questions — status

Answered by m-7.implementer in their reconcile half (pending at this artifact's write time): (a) absence-claim confirmation; (b) §4.1 by-construction scope concur/dissent; (c) seam-row re-bucketing; (d) over-reach check on the planner artifact §7 design questions. The implementer's audit already implicitly concurs on (a) (independent absence evidence, `AUDIT-implementer-…:87-92`) and (b) (":34 — the one claim still licensed as by construction"); explicit confirmation rides their relay.
