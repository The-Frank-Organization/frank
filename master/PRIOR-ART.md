# Prior-art & landscape register

**The durable home for frank's prior-art reads: landscape comparisons, harness references, and novelty-claim verdicts.**
Maintained by `master.orchestrator-planner` (same ownership rule as `ROADMAP.md`); append a dated section per landscape event — never rewrite a past verdict, supersede it.
Standing posture for every entry (charter rule 3): **port principles, don't copy systems.**

Created 2026-07-19, consolidating four auto-memory notes (the 2026-07-07 landscape digest; the opencode/pi/omp Stage-4 pointer; the AXI note; the deepagents read). Sibling research docs: `master/RUNTIME-RESEARCH.md` + `master/RUNTIME-RESEARCH-GPT.md` (2026-07-06, the runtime/engine prior-art study — Temporal/Restate/DBOS-class engines, the pi/opencode interface work, the `model × provider × serving` catalog unit); the reference-clone inventory lives at `references/README.md`.

---

## 1. The 2026-07-07 nine-lane landscape comparison (novelty-claim verdicts)

*Provenance: a 17-agent ultracode run (9 Sonnet research lanes + per-lane adversarial fact-check), operator-requested. The full report was delivered in-session and its digest lived in an ephemeral scratchpad; **this section is reconstructed from the surviving digest** — verdict-grade, not citation-grade. Re-verify any load-bearing claim before citing it externally.*

Verdicts vs frank's spec:

- **M4 routing-as-governance-record = the strongest survivor.** No system found combining a per-dispatch auditable routing record + a declared capability-prior floor + a justified-deviation register. Routers (RouteLLM / NotDiamond / Martian / OpenRouter) are pure optimization; gateways (Portkey / LiteLLM) audit config changes only; banking MRM applies to LLMs only by analogy at whole-model grain. (SR 11-7 rescinded April 2026 → a principles-based Fed/FDIC/OCC framework — matches the spec's "SR 26-2" framing.)
- **M2 fill-time authority** (the forbidden option absent from the rendered form) — **no analog found anywhere.** The M5 five-tuple archetype-binding claim also survives.
- **M3 observe-as-send:** closest shipped analog = Claude Code Stop/PreToolUse hooks (a real hard block on an external check, but per-session/opt-in/untyped). `observed` vs `self_reported` typed evidence tagging found nowhere. "Deterministic rails / harness engineering" is now common 2026 industry writing — the thesis is convergent wisdom; the enforced typed-schema formalization is frank's.
- **M1 identity:** "infrastructure-assigns-identity, the agent cannot self-assert" is 2026 industry consensus (AWS AgentCore Identity, Entra Agent ID, SPIFFE/SPIRE, IETF WIMSE draft) — but all are IAM/credential brokers, not a courier stamping FROM on the store that IS the comms medium. The claim is re-scoped: **convergent, differentiated by mechanism.** arXiv 2509.18415 independently articulates identity+lineage (federated-crypto variant, unimplemented); CapSeal (arXiv 2604.16762) = the same affordance-removal philosophy applied to secrets.
- **M7 engine mechanics = commoditized** (Temporal / Restate / DBOS / Orkes; AMQ ships maildir tmp→fsync→rename for agent mail, active July 2026; AgentDir ships derived-projections-from-canonical-envelopes). The justification for bespoke = governance fused inside the commit critical section, not durability per se.
- **Naming collision:** "Conductor" = Orkes/Netflix Conductor (32k★, marketed 2026 as a durable engine "for AI Agents") AND conductor.build (a Mac multi-Claude-Code app — the exact product space). *(Mitigated by the 2026-07-03 `frank` product naming; "the conductor" survives as the internal engine name only.)*
- **Fact-check catches (kept as method lessons):** Ruflo/claude-flow "federation" = roadmap-presented-as-shipped; the MS "agents cannot self-assert identity" quote = fabricated; arXiv A2H ≠ Twilio A2H (same acronym, different projects).

**Watch-list (as of 2026-07-07):** Microsoft Agent Governance Toolkit (Apr 2026, ~4.7k★ — DID identity + pre-action gate + Merkle audit + require_approval; the closest single analog, its evidence schema currently unbacked) · Ona/Gitpod guardrails (acquired by OpenAI Jun 2026) · the ESCALATE.md spec (default-deny-on-timeout) · Auth0 CIBA + Bedrock AgentCore · Twilio Labs A2H one-shot approval links · Paperclip (org-chart-of-agents control plane).

## 2. Harness references (the coding-agent-runtime layer — Step-3/4 build inputs)

These are inputs to the m-9/m-10 harness work (Division II), NOT to the conductor. The recurring caveat applies to all of them: they spawn/coordinate agents in-process or ungoverned — the exact topology frank rejected; mine the tool/permission/lifecycle **contracts**, never the process model.

### 2.1 opencode (`references/opencode`) — the Stage-4 completeness checklist
A TS/Bun/Effect monorepo; study `packages/opencode/src/`, with the design "why" in `specs/v2/*.md` (session, tools, config, provider-policy, lifecycle — read those before the source). **Start from the digest: `references/opencode-notes.md`** (siblings: `codex-notes.md`, `jcode-ux-notes.md`). Clone state as of 2026-07-19: HEAD `ba4b8e21f` (default branch `dev`; pulled +70 commits from the 07-14 `571e7b852` baseline), working tree now fully populated. NOTE: `references/opencode-notes.md` was written against the 07-14 baseline — re-verify file paths against the new HEAD when the stage opens.
Harness-necessary refs: the tool surface (`src/tool/`, each tool a `.ts` + paired `.txt` prompt — exactly frank's split) · the permission model (`src/permission/`, the closest external analog to frank's gate→decision surface) · session lifecycle/turn processor · provider/model abstraction (relevant to altitude-B role+model-per-dispatch) · MCP, LSP, HTTP-first server. Already-planned features it ships: subagents (`tool/task.ts`) · worktree isolation · git snapshot/checkpoint/revert (`src/snapshot/`, the closest ref to crash-atomic commit + recovery) · a park/wake analog (`background/job.ts`) · single-writer sync (`src/sync/` — rhymes with the serialized commit loop + the m-1 sole-writer store) · skills, plan mode, todos, a structured-question tool.
**Re-skim 2026-07-19 (+70, subagent survey):** of the load-bearing v2 specs, ONLY `specs/v2/provider-policy.md` changed materially (`103f76462` — provider policy enforcement now first-class with a dedicated `policy.ts` + test suite); session/tools/lifecycle/config specs unchanged — good news for the (B) contract port. Permission hardening (granular auto-approve file-only-vs-all, MCP tool permission enforcement, denial-continuity semantics) · identity preservation across tool boundaries (partial tool-call identity buffering, provider tool-input identity) · background subagents on by default with nesting-depth limits · and one architecture signal: `a15afbe8f` "bye bye orchestrator" — they REMOVED an orchestrator plugin layer while jcode doubled down on one.

### 2.2 pi / oh-my-pi (`references/pi`, `references/oh-my-pi`)
pi (`pi-monorepo`, @mariozechner) ≈ frank's Step-3/4 shape minus governance; the operator-ruled relationship is **(B) port-contracts-build-fresh** — lift pi's interface *shape* as spec, write frank's own impl with governance seams from line one (NOT vendor, NOT clean-room). omp (@can1357 fork) adds `packages/catalog` — a model-descriptor/resolver layer generated from models.dev + provider discovery, directly relevant to the `model × provider × serving` routable unit (RUNTIME-RESEARCH §6.5; realized in m-8's factual lane catalog). pi's ecosystem gravity is rising: Moonshot's kimi-code vendors `pi-tui` (§2.5).
**Re-skim 2026-07-19 (omp +847 commits since 07-14, now `39c95e5e2`):** frank-relevant feature deltas — **per-project model ROLES** (a shipped role→model mapping; altitude-B convergence) · retry fallback chains with id-prefixed wildcards (routing-adjacent) · **automatic credential rotation for invalidated OAuth tokens** (m-8 credential-lifecycle prior art) · unified structured subagent execution · full OTel log+metric export · the catalog actively maintained (provider/model pruning, new lanes). **Process datapoint (battle-reports-thesis adjacent):** omp is now developed BY an agent fleet — ~35% of the 847 commits are agent-authored (`roboomp`/`robomp-bot`), 86 merges land via `eval/pr-*` gates, `farm/*` branches — a live agent-driven dev process. **⚠ CORRECTED 2026-07-23 (§2c sweep) — the earlier claim here ("merge gate is EVALS, not governance/evidence… a weaker-trust regression-shaped mechanism; watch how it fails") was WRONG and inverted the conclusion.** omp's `CONTRIBUTING.md` gates merges on **human review + a human-written explanation** — *"AI agents are welcome as tools, not as unattended contributors… you must review every changed file"* — and no eval-gated auto-merge exists in CI (`.github/workflows/ci.yml` is a standard build/test matrix). They independently landed on **a human must attest**, which is a **convergence toward frank's evidence philosophy**, not a counterexample to it. The real remaining delta: theirs is *prose* attestation; frank's is structured/executable claims with an evidence ladder.
**Upstream pi re-skim 2026-07-19 (+59, subagent survey):** the dominant theme is the MODEL CATALOG AS A PUBLISHED EXTERNAL ARTIFACT — generated model data split into per-provider `data.json`, published to R2, explicit generation pipeline (`a9f6a315`/`056c8cbb`; 30+ providers reduced to stubs) — which strengthens the F63 case exactly: a remotely-published catalog is precisely the thing whose digest must be mechanically verified against the shipped registry (drift fails closed). Also: UUIDv7 identity generation centralized into `ai/utils` as the canonical source across packages (identity-at-the-generation-layer, still intra-harness) · xAI device OAuth · new llama.cpp/HF integrations.

### 2.3 AXI (`references/axi`; installable `axi` skill) — agent-CLI ergonomics
A **benchmarked** 10-principle standard for CLIs that agents drive via shell (token budget as a first-class constraint; evidence: gh-axi 100%/$0.050/3-turns vs GitHub-MCP 87%/$0.148/6-turns). The ten: token-efficient output (TOON) · minimal default schemas + `--fields` · truncation + `--full` · pre-computed aggregates · definitive empty states · structured errors + exit codes (fail loud on unknown flags, no interactive prompts) · ambient-context session hook · content-first · contextual next-step disclosure · self-identifying help.
Frank adjudication (2026-07-15): on-point for the native conductor tool + frank CLI output, **orthogonal to the MVP architecture** → parked as `FRANK-HARDENING-BACKLOG.md` **H-13** (cheap bits → the m-9 native-tool DESIGN; fuller conformance → the Step-4 CLI). Convergence-validation: fail-loud-not-drop ≈ reject-not-drop; idempotent-mutation ≈ replay-by-content-hash; no-interactive-prompts ≈ the operator direct route.

### 2.4 deepagents (`references/deepagents`) — LangChain's harness (added 2026-07-19)
MIT monorepo (~264MB): core lib + `dcode` CLI coding agent + Talon host + ACP/Zed connector + evals; "batteries-included agent harness" on LangGraph → `create_agent` → opinionated middleware; explicitly "inspired by Claude Code."
What it ships: middleware stack (todos/planning · filesystem over pluggable backends [state/disk/sandbox/store] · sync subagents with isolated context · **async subagents as background runs on remote Agent Protocol servers returning task IDs** · summarization + offload · skills · memory · HITL via LangGraph interrupts with per-tool `interrupt_on` + path-glob predicates · provider profiles). Talon = a long-running host (channel adapters/WhatsApp, cron, per-conversation serialization) — alpha, with a security disclaimer conceding the gap ("channel access = direct access to the operator's agent, model credentials, MCP tools, and local host resources").
**Verdicts:** (a) security stance is the opposite pole — "trust the LLM; enforce at the tool/sandbox level"; no inter-agent identity, no evidence discipline, no governed comms (LangSmith = observability, not evidence). (b) **Validates the Step-3 reframe** — the harness layer is commodity (M7-class); deepagents occupies m-9/m-10 ground; the governed relay plane stays unoccupied; M1–M4 verdicts unchanged. (c) Stage-4 prior art: the middleware seam design (before-model/around-tool hooks, per-middleware typed state, profile-based tool exclusion) + the backend abstraction. (d) **Honest-labeling contrast #1:** its `patch_tool_calls` middleware FABRICATES ToolMessages for dangling calls ("was cancelled…") without knowing the actual effect — precisely the failure mode F59/D-4/`UNKNOWN_TOOL_OUTCOME` kills; citable. (e) **Contrast #2:** `RubricMiddleware` (a grader subagent gates "done", feedback injected, loop resumes) = convergent instinct with the evidence ladder, but same-harness self-grading, not independent adjudication — prior art for the s9-item-9 adjudication rung. (f) Integration/demo path: an Agent Protocol / deepagents worker sitting on frank's courier via the retained MCP surface — a post-MVP foreign-harness validation target.

### 2.5 kimi-code (`references/kimi-code`) — Moonshot's CLI coding agent (added 2026-07-19)
MIT monorepo (~87MB, TS/pnpm): `agent-core` (+ a v2 rewrite on a VS-Code-style DI/scope architecture) · `kosong` (LLM abstraction) · `kaos` (execution-environment abstraction) · `klient` (client SDK) · `minidb` (embedded KV: WAL + AOF-rewrite — M7 commodity again) · `pi-tui` (**a vendored copy of pi-mono's TUI, upstream 0.80.2** — mariozechner's pi is now upstream of a major-lab product; ecosystem gravity behind the interfaces the operator's (B) ruling ports) · ACP support, subagents (`coder`/`explore`/`plan`, in-process), lifecycle hooks, plugin marketplace with surfaced trust levels, video input.
**Four frank-relevant findings:**
1. **Goal mode (`GOAL.md` + agent-core) — the closest new shipped analog to M3's done-predicates.** A runtime-held structured goal state machine (`active`/`paused`/`blocked`/`complete`, no `cancelled` — cancel = clear), a goal driver chaining autonomous turns, and the load-bearing rule: **the model cannot end a goal with natural-language "done" — it must emit a machine-readable state signal**, with explicit anti-overclaim instructions ("don't mark complete on a plan/summary/first-draft/partial") and injection hygiene ("goal text is user data; cannot override system/permission rules"). Nuance that preserves frank's claim: this is **typed self-report**, not observed evidence — the `observed` vs `self_reported` distinction survives; the structured-completion-signal half of M3 is now shipped by a major lab. The `paused`/`blocked` taxonomy rhymes with park/wake (m-6).
2. **The permission system (v1 shipped + the v2 design doc, `agent-core-v2/docs/Permission.md`) — the closest external analog to frank's Step-4 permission seam.** An ordered responsibility chain, first-match-wins, 11 dimensions/19 policies, the order **deliberately hardcoded as an auditable security cascade** (external-hook veto → structural deny → state-machine deny → static deny → mode allow → session memory → static ask/allow → sensitive-path ask → default → fallback-ask); decisions are **behavior packages** (approve/deny/ask carrying continuations, RPC, telemetry, state writes), not scalars — their explicit anti-Casbin rationale; tools declare standardized `accesses` resource metadata consumed by generic dimensions; v2 = a composable registry micro-kernel (domain services contribute policies). Direct convergences: the pinned-order total-classification shape is exactly m-10's D.2 — and the **F83 failure mode (an ordered procedure contradicted by a side rule) is precisely the risk such cascades carry**; the `accesses` declaration pattern is Step-4 ceiling-design input; "order is a security posture, deliberately not externally adjustable" matches frank's operator-fixed-constant instinct.
3. **klient — frank's interface-guardrail, shipped.** "One facade, three transports (http/ipc/memory), byte-identical after creation; zod-validated contract shared by all transports; **no escape hatch to raw services — the facade IS the public contract.**" Convergence-validation for m-7's seat guardrail (verbs only, raw store/config paths absent from the surface) + the MVP shared-client 3-way seam.
4. **Governance plane: still absent.** Approval is single-user HITL; no inter-agent identity, no evidence discipline; subagents are in-process (the rejected topology). M1–M4 verdicts unchanged; the register's central thesis (harness = commodity, governed relay plane unoccupied) gains a third confirming vendor in two weeks (deepagents, kimi-code, + the July field).

### 2.6 jcode (`references/jcode`) — the swarm/task-DAG turn (promoted from the adjacent list, re-skim 2026-07-19)
@1jehuang's Rust harness (notes: `references/jcode-ux-notes.md`, now stale — +953 commits since 07-14, HEAD `50c3ed88`; 159 of them swarm-related). **jcode has become the closest STRUCTURAL analog to the frank ecosystem vision in any harness reference:** the swarm module (docs `SWARM_ARCHITECTURE.md` + `SWARM_TASK_GRAPH.md`, largely implemented) reframes multi-agent work as a **task DAG as the primary abstraction, agents as fungible workers** that execute/decompose/verify nodes — with mode-gated bounded recursion (one-level fan-out by default; `swarm-deep` roots may spawn at depth under a live-worker budget + absolute member cap), subtree ownership via `report_back_to_session_id` ancestry (an agent may stop only its own subtree), reparent-on-churn, a single per-swarm coordinator slot owning the one shared `VersionedPlan`, **inter-agent comms (broadcast/DMs/channels)**, plan verbs (`propose/approve/reject/assign/task-control`), worktree-manager-owned integration, daemon-snapshot crash recovery, and design language of "gates," "completion/coverage guarantees," and a "bias budget."
**Deep read (2026-07-19): `references/jcode-swarm-notes.md`** — a 10-row convergence table (engine-enforced gate coverage `UncoveredSiblings` ≈ the VP's census discipline · `StaleGateScope` ≈ byte-bound voiding · artifact-or-nothing ≈ no-NL-done · `what_i_did_not_check` ≈ honest non-coverage · owner-partitioned append-only mutations ≈ the m-1/m-2 store/forms design · validated-ops-only interface with the gates-unbypassable argument ≈ the conductor guardrail · dataflow-not-chat comms ≈ the relay plane), the absent-trust-half analysis, and the strategic read. Spawned backlog **H-15** (conductor-computed census + staleness gates — the F71/F74/F75/F80/F81 class the VP hand-catches, proven mechanizable by jcode's engine rules).
**The honest verdict:** this is org-of-agents coordination discipline — plans, approvals, bounded authority-by-construction (subtree stop rights ≈ a structural authority ceiling) — built with NO trust layer: identity = session ids in one daemon's memory, completion = self-report, comms = ungoverned channels, approval = agent-to-agent with nothing stamped or evidenced. The field is converging on frank's PROBLEM (the coordination shapes are arriving) while still not building frank's ANSWER (identity + evidence + governed comms). Sharpens the M1–M4 framing from "absent everywhere" to "coordination-shaped analogs now exist; trust-mechanism analogs still don't." Also in the delta: request-provenance reporting in discovery, goal/todo intention-alignment scoring, and live-Claude-Code-session takeover from the picker.

### 2.7 AMQ / amq-squad / agent-scripts delta (re-skim 2026-07-19, subagent survey)
**AMQ** (`references/agent-message-queue`, +25 → `927d825`): the maildir courier is maturing operationally, in frank-adjacent directions — a presence-liveness split (`notifier_live` verified-wake-process-identity vs `recent_activity`), **session mailbox isolation with cross-session reads refused**, corrupt messages moved to a DLQ (never left in the inbox), wake-lock lifecycle states + doctor diagnostics. Still no identity stamping beyond handle+thread routing.
**amq-squad** (+87 → `0da5d15`) — **the first reference to ship trust-plane machinery, and the closest live analog to frank's operator-gate/authority axis:** (a) **Ed25519-SIGNED authorization envelopes for verified human approval** (v2.21.0), with **consumer-side end-to-end revalidation** — signature + trust state + exact action/target + durable gate + receipt + namespace generation; (b) a **terminal operator-gate lifecycle** (requesters close/withdraw gate generations; answered/closed/withdrawn stop escalating; blocked/failed stay attention-bearing — a gate-attention model m-6 should read); (c) durable delivery receipts on AMQ sends; (d) task-lifecycle atomicity via journals + outbox intents; (e) **claim-once, resumable goal delivery** (an exactly-once-consume convergence with F59's ticket semantics); (f) safe-wait rules where deliberate overrides require a reason + a durable audit record.
**Frank deltas that survive:** amq-squad's authority is per-action human-approval provenance — NOT agent identity (no seat trust, no confusion-resistant FROM), NOT evidence discipline (no observed-vs-self-reported), no forms/lineage/routing planes; its trust base is consumer-side crypto revalidation where frank's is the courier-as-TCB. Watch closely — this is the same local ecosystem that triggered the July publication scare, now building on the authority axis specifically. Study seams: m-6 (gate lifecycle/attention), m-5/m-10 (durable-gate + receipt), F59 (claim-once).
**Priority timeline (dated from both sides' git, 2026-07-19):** the operator's flag-plant artifact is **`references/agentic-dev-team-skills`** = the public repo `iwnlcern/agentic-dev-team-skills` @ `f468288` — the six role skills (agent-pair ×2, orchestrator ×2, design-grill, sprint-doc-setup), the shared 436-line `protocol.md`, and `relay-lint` + its fixture matrix; **authored 2026-07-09 03:12 -0700, published 2026-07-10**; discloses the baseline protocol only (frank appears in two product-level README lines linking the public repo — no courier mechanism internals). The **blog followed 2026-07-14**. amq-squad's signed-envelope integration commit (`b975e8f`, incl. `authorization_envelope_test.go` +1,070 lines) = **2026-07-15 14:08 +0300**, v2.21.0 merged 18:23 same day. Lineage: a non-cryptographic "high-risk action authorization guard" (#355) = 07-09; operator-gate notifications = 06-22; release co-sign gate = 06-30. Read: the operator's public protocol claim predates their strongest trust-plane artifact by **five days** (skill 07-10 → envelopes 07-15), with the blog as a second independent timestamp — and the AUTHORED timestamp (07-09 03:12 -0700) is contemporaneous with their non-crypto guard's merge that same morning, so at that grain the two tracks are independent. The protocol-class claim is uncontested; their approval-gate THEME has June lineage but is human-HITL-gate-class, not governed-protocol-class. Causation unprovable, priority cleanly datable from both ledgers.
**agent-scripts** (+108 → `0cd887d`): operational glue, one pattern worth m-8/m-1 eyes — **all secrets routed through 1Password under an operator TCC gate, bare `--account` denied** (an operator-held credential broker in shipping practice); plus Codex-first delegation routing and GitHub-quota etiquette rules. **zellij** (+26): pure terminal infra, nothing governance- or coordination-shaped.

### 2.8 codex delta (re-skim 2026-07-19, subagent survey; +425 → `678157acaa`)
Three frank-relevant threads in an otherwise product-heavy period (audio, realtime V3, TUI perf): (a) **permission instructions became hash-keyed world-state sections** (`82b294c73c` — re-emitted when model-visible content changes, CRLF-normalized before hashing) and **permission/policy/approval messages now ride the MODEL CATALOG** (`eb80df0317`) — policy-carried-by-digest-keyed-artifacts, an F58-adjacent shape on the m-8/m-4 seam; (b) **credential-lifecycle hardening**: serialized MCP OAuth refreshes with a distributed lock + atomic read-refresh-write, failing on stale credentials instead of continuing (`6962a2ecae`), credential-store pinning at startup — m-8 prior art; (c) multi-agent settings consolidated under `agents.*` while task STATE simplified (agents report name+status only, task context dropped — the opposite direction from jcode's DAG). SessionEnd lifecycle hooks + session-id-keyed telemetry; no DAG/state-machine machinery.

### 2.9 jinn (`references/jinn`) — the cross-harness org control plane (added 2026-07-19)
@hristo2612's `jinn` (MIT-licensed npm `jinn-cli`, beta; ~41MB, TS/pnpm; 1,279 commits 2026-03-06 → 07-19, actively shipping — v0.27.0 days old). "Run your AI agents as a company": a **local gateway daemon** turning heterogeneous agent CLIs on `$PATH` (Claude Code, Codex, Grok, Hermes, Pi, Antigravity) into "employees" — YAML personas with rank/department/`reportsTo` hierarchy — plus a durable Todo ledger, graph Workflows (sequential/conditional/parallel/switch, per-phase engine+model, first-class human approval gates, idempotent runs with durable history), cron/webhook/poll triggers, chat + web dashboard, and **a built-in MCP as the single typed company surface with capability-scoped tool discovery** ("bus, not brain" — no AI logic of its own).
**Why it matters — the FIRST reference occupying frank's actual product layer:** everything before it is single-harness (jcode, deepagents, kimi) or courier-only (AMQ); jinn is a **cross-harness org/control plane** — the same layer as the frank dev-team-ecosystem vision, and even the same integration posture (a typed tool surface foreign harnesses attach to ≈ frank's MCP-retained-for-foreign-harnesses). Shipped frank-principle convergences: **reviewer-closes-not-producer** ("completion is earned through review, not assumed the moment a session ends" — the independent-close half of M3, in a product) · **graded oversight levels** (TRUST/VERIFY/THOROUGH) with **irreversibility-gated human escalation** ("only money, irreversible actions, public actions, or legal/security risk route up to you" — the H-11 capability-model instinct) · **activity receipts** (delegations/callbacks/todo changes as structured durable events) · **persisted restart-safe callbacks binding delegation to a Todo** (durable dispatch discipline).
**The trust base is the gap, again:** identity = YAML personas + gateway session tracking (nothing stamped, nothing confusion-resistant); receipts are gateway-logged self-report, not observed evidence; inter-employee content is ungoverned; the whole trust model is "one local daemon the single operator trusts" — no seat credentials, no evidence typing, no governed relay store. M1–M4 stand. **Narrative pressure is real though:** "conducts your agents," org charts, review discipline, approval gates — the *story* space of "agents as a governed company" is being actively occupied by a polished, install-in-two-commands product. Sharpest register datapoint yet for publication cadence.

### 2.10 paperclip (`references/paperclip`) — the governance-deepest occupant (added 2026-07-19; promoted from the §1 watch-list)
`paperclipai/paperclip` (MIT, ~228MB TS monorepo, 2026-02-16 → 07-18, very active; Node server + React UI). "If OpenClaw is an employee, Paperclip is the company": a cross-harness org control plane (OpenClaw/Claude Code/Codex/Cursor/Bash/HTTP — "if it can receive a heartbeat, it's hired") with org charts, ticket-based work, per-agent monthly budgets with atomic checkout+enforcement, goal alignment (every task traces to the company mission), multi-company isolation, immutable audit log with full tool-call tracing, and "governance with rollback" (enforced approval gates, revisioned config, rollback).
**Why it outranks jinn on our axes — real trust-plane machinery, the first in any reference:**
- **`low_trust_review` containment presets** (`doc/LOW-TRUST-PRESETS.md`): explicit trust boundaries for hostile/prompt-injected input (untrusted PRs, external tickets), resolved by INTERSECTION across agent/project/issue policy sources — narrower wins, **fail-closed** on ambiguity; requires the sandbox driver + isolated workspace or the run fails closed; low-trust output is never auto-promoted into higher-trust context; config changes from low-trust work must pass higher-trust review/promotion. A genuine trust GRADIENT with promotion paths.
- **MCP access governance** (`doc/MCP-ACCESS-GOVERNANCE.md`): application/connection/catalog/profile separation, tool **risk classification in a catalog**, allow/deny profiles **bound at agent/project/routine/issue/company scope**, an approval flow with trust rules, runtime slots, and a call-event audit log — an m-5-shaped capability plane (ceiling-at-scope) + an m-8/m-4-shaped risk-classed catalog, shipped.
**What survives for frank:** their threat model is hostile-INPUT containment (malice at the boundary) where frank's is confusion-not-malice among trusted-but-fallible seats — complementary, not overlapping; identity is control-plane-internal (DB entities + heartbeats — no cryptographic seat identity, no confusion-resistant FROM); their audit observes tool calls THEIR gateway mediates, but work claims/completion remain self-reported tickets; inter-agent content is ungoverned threads; no forms/determinism/lineage plane; no routing-as-governance. Still: **this is the closest single product analog to frank overall** — study seams: m-5 (scope-bound profiles ≈ ceiling-at-spawn), m-3 (call-event log vs evidence typing), the Step-4 permission system (their intersection/fail-closed resolver), H-12 (their sandbox-required-or-fail-closed rule).

### 2.11 Other adjacent clones
Per `references/README.md`: claude-code, the Fugu report — consult the per-clone notes files where they exist. (Reference clones bulk-updated 2026-07-19: codex +425, jcode +953, omp +847, opencode +70, pi +59, amq-squad +87, agent-scripts +108, zellij +26, agent-message-queue +25; notes files predate the update.)
**Register/inventory housekeeping (2026-07-23):** three cloned dirs were absent from this register — `gajae-code` (now §2.12), `amq-noc` and `claw-code` (both dormant, no commits in the 07-19→07-23 window; no entry owed yet). **`references/README.md` is STALE** — it still describes a 4-repo, cycle-c1-era clone target (jcode / claude-code / agent-scripts / zellij) while ~20 repos are cloned; **this register, not that README, is the inventory of record.**

### 2.12 gajae-code (`references/gajae-code`) — a pi-lineage FORK, not a new bloodline (added 2026-07-23)
CLI `gjc`, MIT, by Yeachan-Heo; Rust (`crates/`: PTY, AST, shell, git-daemon) + Bun/TS (`packages/`) monorepo. **Its own `NOTICE.md` + `docs/porting-from-pi-mono.md` state plainly that it is a rebranded downstream fork of `oh-my-pi`/pi-mono** — package scopes literally renamed `@mariozechner/pi-*` → `@gajae-code/*`. **File it under the pi lineage (§2.2), NOT as an independent entrant** — the +303-commits-since-07-19 velocity is real but reads as feature/product breadth, not governance depth: Telegram/Discord/Slack bridges, a `computer-use` desktop tool, an `rlm` research REPL, a fixed `deep-interview → ralplan → ultragoal` planning workflow.
**The two frank-relevant bits:** (1) an **`auth-broker`/`auth-gateway` credential boundary** (`docs/auth-broker-gateway.md`) — broker-only writer, redacted snapshots, opaque refs handed to clients — a mild convergence with the m-1 secret boundary + m-8 connector-credential contract; worth a citation, not a threat. (2) `gjc team` tmux-backed parallel workers with lease/claim/journal semantics — fenced nudges, a "proven-absent shutdown authority record," **fails closed on restart/unknown outcome**, at most two immutable journaled attempts. A genuine fail-closed/journaling discipline, but a single ad-hoc feature guard, not a systemic evidence ladder. No byte-bound lock, no honest-outcome vocabulary, no channel-stamped identity, no ceiling-at-spawn; "authority" appears only loosely/locally (reply-ID, tool-selection, MCP-collision).

## 2b. The 2026-07-19 sweep synthesis (one week of field motion, 9 repos, ~2,600 commits)

Three cross-repo currents, each landing on a frank seam:
1. **The provider/catalog plane is hardening everywhere** — codex sources permission/policy messages from the model catalog; opencode made provider policy first-class (the ONE v2 spec that changed); pi externalized its catalog to published R2 artifacts. The field is converging on policy-riding-catalog-artifacts, which makes frank's F63 digest-verified-catalog and the m-8 factual-lane-catalog design MORE load-bearing, not less.
2. **Identity is being hardened intra-harness only** — opencode across tool boundaries, pi at the generation layer (UUIDv7 canonical source), AMQ at session-mailbox isolation. Nobody crosses a process/trust boundary with stamped identity. M1's mechanism differentiation stands.
3. **Multi-agent structure is diverging, trust is not** — jcode built a gated task-DAG engine (§2.6, the structural half of frank's constitution), codex SIMPLIFIED agent state (name+status only), opencode deleted an orchestrator layer, amq-squad shipped signed human-approval envelopes (§2.7, the first trust-plane machinery). The coordination layer is contested and unsettled; the cross-boundary trust layer has exactly one occupant building toward it (amq-squad, per-action-approval-shaped, not identity/evidence-shaped).
Watch-list additions from the sweep: **amq-squad** (promoted — closest live analog on the authority axis) · jcode's DAG engine as a mechanization donor (H-15) · pi's published-catalog pipeline (F63 pressure) · **jinn** (§2.9 — the first CROSS-HARNESS org control plane; occupies frank's product layer and narrative space, trust plane untouched) · **paperclip** (§2.10, promoted from the §1 watch-list — the governance-deepest occupant: trust-gradient containment presets + scope-bound MCP access governance + immutable call audit; hostile-input threat model, complementary to frank's confusion-not-malice; the closest single product analog overall).
**The corrected competitive map after the full sweep:** the org/control-plane layer is now a three-way race (jinn = polish, paperclip = governance depth, jcode = coordination mechanics), the courier/authority layer has one fast mover (amq-squad), and the specific compound frank builds — courier-as-TCB × stamped seat identity × typed observed-vs-self-reported evidence × forms/lineage determinism × routing-as-governance, ACROSS heterogeneous harnesses — still has zero occupants. But every individual ingredient now exists somewhere in shipping form; the compound is the moat, and the window for claiming it publicly is measured in weeks, not months.

## 2c. The 2026-07-23 sweep synthesis (4-day delta; 4 parallel subagent surveys; ~1,900 commits across 11 repos)

Deltas since the 07-19 baseline: **oh-my-pi ~973** · gajae-code +303 (a fork of omp — §2.12) · codex +222 · deepagents +137 · paperclip +87 · jcode +56 · opencode +54 · pi +89 · agent-message-queue +19 · kimi-code +6 · axi +6 (docs only).

**THE HEADLINE — paperclip has SHIPPED two of frank's five governance pillars while ours are still designed.** This is the most important correction to our competitive self-assessment:
- **Non-forgeable identity (shipped, theirs):** `issue.comments.create_human_attributed` — a plugin may attribute a comment to a human **only after the host independently re-verifies** that user is an active human company member; "a plugin can never forge attribution to an arbitrary or inactive user." Agent-JWT tamper/claim-forgery rejection is separately tested. This IS frank's channel-stamped-`FROM` principle, at the plugin/host boundary.
- **Authority ceilings (shipped, theirs):** a deny-by-default `low_trust_review` preset + a narrow **audited one-hop exception** — a standard-trust child may post one comment to its **direct parent only**, grandparent/sibling explicitly excluded, low-trust prose quarantined from propagating upward. Route-enforced with red-team regression tests. Coarser than frank's ceiling (named presets vs. a general ceiling-at-spawn primitive) but running.
- **What frank still uniquely holds:** byte-bound design locks · genuinely tamper-EVIDENT evidence (paperclip's is a DB audit log append-only *by convention* — no delete route, but **no hash chaining**, so a privileged write could rewrite history undetectably) · evidence-gated phase transitions · **crash-honesty — paperclip has NO "unknown" concept at all.** Crash-honesty is now the cleanest single differentiator.

**Two independent convergences VALIDATE crash-honesty** (cite these at external review — independent arrival is evidence the shape is right): pi's `AgentHarness` doc specifies persist-before-commit → notify-after, and **a post-commit hook failure does NOT roll back** (rejects with code `hook`), plus immutable turn snapshots so readers never see in-flight state. AMQ shipped **tri-state `TreeRelation{Same,Different,Unknown}`** + "authority de-laundering" + fail-closed `.amqrc` provenance — an explicit *unknown* rather than a guess. **The counter-example:** kimi-code `71bcfba54` silently drops "vacuous" empty-assistant messages that wedge sessions, self-healing via a logged anomaly instead of surfacing the fault — exactly what frank's design forbids.

**Two competitors' BUGS validate the fixed tool-dispatch constant** (vs. pattern-matched command allow-lists): omp `09d02c641` — `git status\nrm file.txt` rode a `git *` allow-rule because newline normalization ran *before* the approval matcher; deepagents `THREAT_MODEL.md` **T13** — the allow-list validates only the **first token**, so `python3`/`bash`/`env`/`xargs` grant unrestricted execution (they honestly label it "an ergonomic auto-approve heuristic, not a security boundary"). Two independent confirmations that the free-text-pattern shape is broken; frank's 8-name constant is empirically vindicated.

**Parent→child authority prior art (bears directly on B21's nested orchestrator-team + the monotone-non-increasing ceiling — designed BEFORE these were read; independent arrival, now with three benchmarks):** codex **role-scoped spawn** (`multi_agent_v2` promoted to **Stable**; test `configured_agent_roles_control_spawn_agent_type` — agent roles control what child type may be spawned) · paperclip's audited parent-only hop (above) · omp's workspace-root/AGENTS.md inheritance + **quiescence barrier** (a subagent's final yield with pending owner jobs is a PAUSE, not completion — a lifecycle problem m-9 will hit once async tool execution exists).

**Other frank-seam signals:** codex binds MCP tool calls to the **exact tool-catalog revision the model step saw, rejecting calls if the catalog changed mid-flight** (`65f8bf6853`) — a staleness guard aimed at the same class as the tool-dispatch constant + F59 one-shot ticket · codex `ReviewDecision::Denied` now carries a structured rejection **reason** threaded through command/patch/network/MCP/delegated flows (a *narrowing* of frank's honest-outcome differentiator — per-call, no append-only record or evidence ladder) · codex multi-agent state is a durable **world-state section** diffed/restored across history rewrites with a legacy-compat field (schema-versioning-on-resume) · **opencode `specs/v2/*.md` had ZERO changes** — the design-of-record we intend to port contracts from is stable, de-risking that plan; and "bye bye orchestrator" is still **unreversed and un-replaced** · pi renamed `packages/orchestrator` → `packages/server` (vocabulary note: pi's "orchestrator" was NEVER a governance/authority plane, just IPC/RPC supervision — no overlap with the conductor) · **jcode's gates did NOT deepen** (last touched 07-12) but are confirmed **mechanical**: `DagError::UncoveredSiblings`/`StaleGateScope` are typed Rust variants with unit tests, and an empty `what_i_did_not_check` is *machine-consumed* (it spawns critique-gate child nodes), not narrated — still the closest mechanical analogue · deepagents has **no** multi-agent org / identity / ceiling primitive, and notably **removed** a SHA-256 MCP-config trust store in favour of interactive approval (moved AWAY from a cryptographic control).

**Adopt-list (prior art worth stealing):**
1. **Host-side re-derivation of caller-supplied actor IDs** (paperclip) — never trust a claimed actor; re-check server-side. Benchmark m-1's stamper against this.
2. **The audited narrow NON-TRANSITIVE exception** (paperclip) — a model for bounded cross-scope relays without opening a general hole.
3. **A living, versioned, git-tracked `THREAT_MODEL.md`** with a threat-ID table (severity · verification status · code pointers), amended in the open whenever a gap is found (deepagents) — frank's threat reasoning currently lives only in relay history; this deserves a standing artifact.
4. **omp's sandbox architecture** — 4-zone filesystem ownership, per-slot UID/GID isolation (`omp-N` users, setgid pools), credentials never entering the sandboxed slot via a proxying git transport (HMAC RPC). **The best prior art seen for the H-12 gap** — read before any Step-4 sandbox work.

**Corrected competitive map (2026-07-23):** the compound frank builds — courier-as-TCB × stamped seat identity × typed observed-vs-self-reported evidence × forms/lineage determinism × routing-as-governance, ACROSS heterogeneous harnesses — **still has zero occupants.** But paperclip now occupies two of the five pillars in shipped form (identity non-forgeability, authority ceilings), so the honest claim narrows: frank's defensible core is **byte-bound locks + tamper-evident (hash-chained) evidence + crash-honesty/"unknown" + cross-harness reach**. Paperclip is the one repo to read every batch.

## 2d. The 2026-07-26 sweep synthesis (3-day delta; 4 parallel lanes — 3 repo surveys + 1 web; ~1,800 commits across 15 repos)

Deltas since 07-23: oh-my-pi 657 · gajae-code 199 · jinn 157 · jcode 153 · codex 82 · deepagents 63 · opencode 52 · pi 44 · paperclip 43 · AMQ 31 · kimi-code 15 · amq-squad 6. (Our own public skills repo's +2 = the v2.8.8.2/.3 pushes; agent-scripts/axi noise.)

**THE HEADLINE — pillar-by-pillar contestation jumped a gear: four of frank's five pillars now have at least one shipping analog somewhere.** The compound still has zero occupants, but the ingredient window is closing visibly faster than the 07-23 read said:
- **Evidence typing — CONTESTED for the first time, by AMQ.** `e9cbd34` `amq trace` (schema `amq/trace/v1`): every leg carries typed `evidence`/`no_evidence`/`error`; every non-evidence leg MUST carry `detail` + `next_step`; the docs refuse inference in frank's exact register (notification success "is never inferred from wake state, a mailbox file, or a delivery receipt"; file presence "does not prove the historical directory-sync result"). **SUPERSEDES the §2.7 "no evidence concept" line.** What AMQ lacks: identity attribution on the evidence and any chain. **The JOIN — typed evidence × stamped identity — is now the differentiator, not the typing alone.**
- **Stamped provenance — jinn's "trust plane untouched" verdict (§2.9) is RETIRED.** Server-stamped `created_by` (`d70c0d9d`), reserved author namespaces `operator`/`system`/`session:`/`cron:*` with collision-skipping (`6f8e746e`), authority matched on the `(author, authorKind)` PAIR (`9df4adb6`), and `create_work_item` **structurally refusing caller-supplied provenance as "spoofable"** (`work-item-tools.ts:194`) — the closest anyone has come to the stamped FROM, control-plane-internal, no crypto, single-daemon trust base. Two-lane human-vs-agent authority (operator PUT `human:true` vs allowlisted note-required agent POST) + per-field edit matrices.
- **Crash-honesty — fragments arriving empirically, still nobody's typed-unknown-as-evidence.** gajae-code `0c25d133`: atomic prepare/ready/commit session-identity rotation whose post-commit fault surfaces as **`committed-degraded`** (not success, not rollback) — the explicit third outcome, in a shipping store; `de7ae1a3`: a typed rename-outcome domain where unrecognized codes are **refused, not guessed**. paperclip `665408c6d`/`7014e46e5`: STRUCTURAL crash classification (explicitly rejecting regex-on-error-strings because stdout may quote an agent *discussing* failures) + wake-the-responsible-agent-quoting-its-own-report — converging on don't-infer-what-wasn't-observed without a persisted unknown type. jcode `038d029f`: spawn fails loudly when the RESOLVED model differs from requested ("refusing to silently use a different model" — observed-vs-self-reported, arrived at empirically); `06e16d53`: a starvation watchdog for continuations that vanish with no log line. **Frank's hold narrows to: the unknown as a first-class evidence type inside a governed ladder, not an ad-hoc recovery.**
- **Session versioning — a race formed within THREE DAYS of the operator choosing the direction.** codex `05f000263b` ships **`rollout_lineage`**: thread forks referencing a frozen source-history prefix, child-owned records only, `latest`/`lastTurnId`/`beforeTurnId` fork boundaries, compressed-rollout materialization — plus `5c94796dc9`/`963316583b` per-thread filesystem locks (single-writer ownership, competing resume rejected). deepagents `ca86fbecc`: schema-versioned per-thread/per-subagent JSONL transcripts with content-hash `revision` identity, atomic writes, capped `.bak` retention — from the trust-the-LLM pole. jinn `47b2f586`/`90ddfdd1`: content-hashed instance migration BUNDLES (manifest + per-file `baseSha256`/`targetSha256` + completion receipts). Academic: **OpenRath (arXiv 2606.19409)** makes Session a first-class branchable/inspectable/replayable runtime value — fork/merge/replay as explicit operations with lineage metadata — **read before the Step-4 versioning design locks.** (Letta "Trajectory", 07-23, is cross-harness session *normalization for learning* — explicitly not portability/versioning/repair; not our lane.) Durability nuance: codex compression now `sync_all()`s (3 sites) but the recorder + rollout-trace still carry zero fsync/checksum — the §2c best-effort verdict narrows, doesn't fall.
- **Routing-as-governance — still ZERO occupants, and now carrying a named ANTI-position.** amq-squad v2.24.0's model-routing policy is **explicitly advisory** ("never mutates an existing profile, never overrides an operator choice, never gates readiness or launch") — a deliberate counter-stance, quotable as the contested-thesis marker. And omp *shipped then REMOVED* per-call model selection in the task tool (`1c8d9db6d` → `9f8aa87db`, replaced by coarse config-held `effort`) — the field testing agent-held routing authority and pulling it back. M4 stands, uniquely.

**Register corrections owed by our own decisions (claims discipline):** the §2c "corrected competitive map" listed *"tamper-EVIDENT (hash-chained) evidence"* among frank's defensible core. **That claim is retired as of the 2026-07-26 D1 re-scope** (operator-ratified direction: chaining removed from the session log as malice-only/bivpak-hostile; relay-store chaining was already deferred at m-1). The honest core after this sweep: **the compound/JOIN (typed evidence × stamped identity × governed relay, cross-harness) · byte-bound design locks · typed-unknown-as-evidence · fill-time authority/forms-determinism · routing-as-governance.** Do not let external copy claim hash-chaining.

**Reclassification:** gajae-code (§2.12 "a pi-lineage FORK, not a new bloodline") is **SUPERSEDED → an independent lineage.** Still syncs upstream, but now owns a native durability subsystem with no omp counterpart (own issue stream, own maintainers, own release cadence), and its window work is the most frank-relevant of the three pi-lineage repos: the `committed-degraded` rotation, typed outcome domains, identity-bound tombstone receipts gated on a full comparand tuple (`a11fd9f3` — and `504bbf11`'s producer/checker authority-split bug is a cautionary tale for any two-sided identity comparand), fail-closed consensus caps that floor counts from disk so a wiped index cannot under-count (`41d6850c`), and a typed `executionMode` where the field is authoritative and prose is compatibility-only (`726bd31d`).

**Other verdicts tested and HELD:** opencode `specs/v2/*.md` — **zero changes again** (port plan de-risked twice running); orchestrator still dead. jcode — gates deepened (plan-level intent gate `172363d9`, with a serde-alias schema-migration pattern worth reading for versioned forms), **still zero trust/identity**; its new `jcode-harness-api` (versioned NDJSON envelope, forward-compat `Unknown` variants, schema snapshot tests) converges on our seam shape without auth. paperclip — **no hash chain, no unknown type** (activity_log schema untouched); nearest new thing to a ceiling is task-scoped egress grants (`7f766526a`, per-issue k8s network policy). deepagents T13 landed in-tree (`f7922795d`) stating verbatim that the first-token allow-list is "an ergonomic auto-approve heuristic, not a security boundary" and "textual command classification cannot robustly constrain execution" — the field writing frank's fixed-dispatch-constant argument itself; meanwhile Auto mode went GA and a `yolo` cycle shipped (loosening), and the fabricated-ToolMessage path is unchanged. kimi-code — typed-error-driven goal continuation (`0cef160c4`, inspects `LOOP_MAX_STEPS_EXCEEDED` not the coarse `failed`); permission cascade untouched; the silently-dropped wedged-message counter-example stands. codex also shipped trusted-plugin script attribution (`84fa68b429` — provenance for *code paths*, analytics-facing; nearest approach to actor identity, stops short) and a Guardian V2 feature flag with no visible implementation (watch).

**Web lane (primary-source-verified; UNVERIFIED items excluded from verdicts):** MS Agent Governance Toolkit quiet at v4.1.0 — but note "Escrow signing support in ProofOfOutcome" + Ed25519 agent registration in its changelog (an outcome-shaped primitive; the never-claim-"first-governance-platform" rule stands). **NEW ENTRANT: Galileo "Agent Control"** (Apache-2.0; `@control()` decorator → deny/steer/warn/log/allow per step; central Control Store for fleets; pluggable evaluators; positioning line "runtime governance should be infrastructure, not a product moat") — the closest structural competitor found this sweep; launch date UNRESOLVED (blog says Jul 6; secondary coverage says March; press URL 403s). A2A v1.0 shipped **Signed Agent Cards** — JWS over *capability metadata*, not per-message provenance; the stamped-FROM claim is untouched (a secondary "v1.2 / Linux Foundation" claim CONTRADICTED by the primary site — not entered). draft-klrc-aiagent-auth rev-03 (Jul 6) — still an individual submission, not WIMSE-adopted. agentsh/ProofRail/Sponsio: no verifiable movement. jinn: publicly dark — no launch/funding signal anywhere, while privately +157 commits (watch the gap between those two facts). **Narrative space:** paperclip openly claims "the orchestration layer for ZERO-HUMAN COMPANIES"; its origin story (20+ ungoverned Claude Code tabs, no recoverable state after reboot) is literally frank's problem statement sold without the trust layer — and **no funded player is claiming the narrative** (July platform moves — Gemini Enterprise, Workspace Agents billing, Claude Cowork — are platform-tier, not agents-as-a-company). NVIDIA "OpenShell/NemoClaw": secondary-only, UNVERIFIED, needs a dedicated pass. arXiv: **ContextNest (2607.02116)** — SHA-256 hash-chained version histories + deterministic selectors + point-in-time reconstruction for agent knowledge stores (forms/lineage-adjacent); **2607.20827** — untrusted evidence still sways agent action selection in ~2.4% of comparisons (empirical backing for observed-vs-self-reported); "Governing Actions, Not Agents" (2606.26298) + "From Agent Traces to Trust" (2606.04990) — June items adjacent to routing-as-governance and evidence tracing, verify-then-cite per §3 rule 3.

**Adopt-list additions (build-reference mandate §4 applies — read at design time, provenance notes owed):**
1. **codex `rollout_lineage` + per-thread writer locks** — read BEFORE the Step-4 session-versioning design; it is the shipped half of the direction the operator chose 2026-07-26, three days before codex shipped it.
2. **OpenRath (arXiv 2606.19409)** — the session-as-governed-object formalization; same read-before-lock.
3. **gajae-code's prepare/ready/commit rotation + `committed-degraded` + typed rename-outcome domain** — crash-honesty at the session store, the closest external implementation of the explicit third outcome.
4. **omp's asymmetric allow/deny matching** (`85110c366` — deny/prompt match ANY `&&`/`||`/`;` segment; allow requires whole-command match and never applies to compound lines) — a rule the tool-dispatch constant's bash lane should state explicitly.
5. **pi's ETag catalog revalidation** (`b1c444d9`) — freshness/provenance on a remote catalog artifact; F63-adjacent.
6. **jinn's refuse-caller-supplied-provenance + reserved author namespaces** — benchmark m-1's stamper against both (companion to §2c adopt-item 1).
7. **AMQ's mandatory `detail` + `next_step` on every non-evidence leg** — evidence-ladder ergonomics worth stealing wholesale.

**Corrected competitive map (2026-07-26):** the compound — courier-as-TCB × stamped seat identity × typed observed-vs-self-reported evidence × forms/lineage determinism × routing-as-governance, ACROSS heterogeneous harnesses — **still has zero occupants.** But the per-pillar picture moved from "every ingredient exists somewhere" (07-23) to **"four of five pillars have shipping analogs, and the fifth has a published anti-position"** — and the session-versioning direction acquired a major-lab implementation within 72 hours of our choosing it. The window language in §2b ("weeks, not months") was right and is now shorter. Paperclip remains the read-every-batch repo; **AMQ and jinn join it** on the trust-plane axis; gajae-code joins on crash-honesty. Fleet-process datapoint: omp's agent-authored share this window ≈ 20% (`roboomp` 132/657) with human PR-merge as the only gate — down from ~35% at §2.2's read, consistent with their human-attestation posture.

## 3. Standing rules for this register

1. New landscape event → a dated subsection here (append-only in spirit; supersede, don't silently edit verdicts).
2. Auto-memory carries ONLY a pointer to this doc — never a second copy of its content (drift kills one of them).
3. A verdict cited in a governing doc or externally must be re-verified at source first if it came from §1's reconstructed digest.

## 4. Build-reference mandate (operator directive, 2026-07-19)

**The operator's rule:** when we build a surface that a reference repo has already implemented well, we take their implementation as REFERENCE — read the code, port the mechanism — not just the abstract principle. This refines charter rule 3 ("port principles, don't copy systems"): the *system* stays ours (frank's architecture, Go, the governance plane); the *mechanisms* get ported from the best shipped implementation with provenance. Discipline: (a) every ported mechanism gets a provenance note in the consuming design doc (repo · path · commit at read time); (b) no verbatim vendoring without a license check at porting time (the corpus is MIT/Apache but verify per repo); (c) reference reads happen at DESIGN time so the pattern lands in owner bytes, not silently at IMPL time.

**The operative mapping (reference implementation → frank build surface; carried into the stage-4/5 dispatches and the Step-4 kickoff):**

| frank surface | reference implementations to read at design time |
|---|---|
| m-9 worker (stage-4 DESIGN → T4 build) | opencode (the Stage-4 completeness checklist, §2.1) · deepagents middleware seam — before-model/around-tool hooks, per-middleware typed state, profile-based tool exclusion + the pluggable filesystem-backend abstraction (§2.4c) · kimi `klient` contract-driven three-transport facade (§2.5) · AXI ergonomics for the native relay tool (§2.3, H-13) |
| m-10 control plane (stage-5 DESIGN → T4 build) | deepagents async-subagents-as-background-runs returning task IDs + Talon's per-conversation serialization/cron host patterns (§2.4) · jcode's engine-enforced gate rules as the H-15 mechanization donor (§2.6) |
| Step-4 permission system | paperclip policy resolver — intersection/narrower-wins/fail-closed + scope-bound MCP governance + sandbox-required-or-fail-closed (§2.10; the deepening-menu lane 1 read) · kimi permission micro-kernel + ordered cascade (§2.5) · codex hash-keyed world-state permission sections (§2.8) |
| F63 / catalog plane | pi's published-catalog artifact pipeline (§2.2) · codex catalog-carried policy messages (§2.8) |
| m-6 surfaces (Step-4+) | jcode-notify-email (§2.6, unexamined — read before m-6 design reopens) · amq-squad envelope consumer-side revalidation (§2.7) vs the F59/m-6 gate lifecycle |
| Foreign-harness validation (post-MVP) | a deepagents/Agent Protocol worker on frank's retained MCP surface (§2.4f) |

Anti-adoption rails (what we explicitly do NOT port, with the citable contrast): deepagents' fabricated ToolMessages for dangling calls (§2.4d — the F59/`UNKNOWN_TOOL_OUTCOME` counter-case) · same-harness self-grading as evidence (§2.4e — the adjudication rung stays independent) · trust-the-LLM/sandbox-only enforcement (§2.4a — frank's plane is the governed relay, not the sandbox).

## 5. External audit + source additions (2026-07-20, GPT-5.6 Sol Pro via operator)

An external audit of frank-dev `main@6e4d657` (full text with the operator; dispositions + H-16..H-25 in `FRANK-HARDENING-BACKLOG.md`). Its landscape verdict matches §2b's: the topology is convergent, the commit-integrated governance plane is the differentiation. Adopted positioning + claim discipline recorded in the backlog entry. New sources to verify-then-cite (per §3 rule 3 — these arrived via the audit, each needs one verification read before a public claim leans on it):
- **OpenAI Harness Engineering (2026-02)** — repo-as-system-of-record, mechanical architecture enforcement, few blocking merge gates (explicit throughput tradeoff frank deliberately inverts for authority-class work; the reversible-vs-irreversible risk partition should become product-organizing — H-11/H-12 adjacency).
- **Towards a Science of Scaling Agent Systems (arXiv 2512.08296)** — 260 configs: architecture effects range +80.8% to −70% by task shape; architecture-task alignment decides the SIGN. Anti-universal-org-chart evidence; feeds H-25 + any future topology router (m-4 adjacency).
- **MAST (arXiv 2503.13657)** — 14 failure modes / 3 classes over 1,600+ traces; frank maps cleanly (contracts/store → system design; identity/lineage/authority → misalignment; observe-as-send/review → verification). Citable taxonomy for the product narrative.
- **SEMAP (arXiv 2510.12120)** — the closest academic neighbor to the SKILLS layer (behavioral contracts over A2A); frank's courier goes further (admission control over identity/lineage facts). CodeTeam (arXiv 2606.22082) — closest repo-generation topology analog; its 34-42% pass rates = orchestration ≠ solved.
- **Microsoft Agent Governance Toolkit (v4.1.0, 2026-06)** — the broad product-class comparator; candid LIMITATIONS.md (per-action not sequence; audit records attempts not outcomes; empty-policy default-allow). Frank narrower + stronger on the commit-integrated axis; NEVER claim "first governance platform" against this.
- **Proof of Execution (arXiv 2607.05397)** — planning/enforcement/effect/recordkeeping as separate authority planes; trace completeness depends on EXCLUSIVE effector credentialing → adopted as the H-17 census's organizing law. **Governing Dynamic Capabilities (arXiv 2603.14332)** — one unverifiable interior agent breaks downstream verification (the m-8/m-9/m-10 chain risk, formalized). **Intent-Governed Tool Authorization (arXiv 2606.22916)** — intent may only NARROW static authority (matches fill-time authority + raise-only gates).
- **Mechanism standards for the H-20/H-18 ladder:** NIST reference-monitor definition (the three properties; our honest claim = relay-plane only) · DSSE + SPIFFE + SLSA (signed evidence/provenance, standard not home-grown) · OPA's PDP/PEP split (vocabulary for the census rows) · MCP spec's untrusted-annotations + host-consent obligations · A2A task-state = carrier not trust.
- **Products:** agentsh (whole-harness execution mediation — the H-12/H-21 placement pattern) · ProofRail (cumulative chain thresholds) · Sponsio (temporal-contract enforcement) — sequence-level governance is commercializing; frank's edge = trajectories over SYSTEM-DERIVED lineage, not SDK self-report.

## 6. The compaction lane (2026-07-20, m-9 stage-4 flags — source: `stage4-m9/SITREP-planner-20260720-213000`)

- **Governance Decay (arXiv 2606.22528):** compaction dropping a standing policy/constraint pushes prohibited-action violations 0% → 30–59%; pinning restores 0%. **Frank's validation:** authorization lives OUTSIDE the model context (F59 tickets + run manifest + seat identity, re-checked authoritatively every dispatch) — compaction-induced confusion about authority CANNOT cause an unauthorized effect; the worker's Tier-0 pinning + §7.3 post-rewrite survival check protect the residual confusion layer. Governance-outside-the-summarizer = a first-class, literature-backed safety property.
- **The 8-harness compaction survey** (opencode `cd46f22d5` · deepagents `ae96a90` · kimi `df689955` · axi `fbfe6d2` · codex `678157acaa` · claude-code `6a25909` · jcode `9e4c00f7` · oh-my-pi `39c95e5e2` · pi `94373d81`): tool-result eviction = the cheapest coding-native reversible win (the filesystem is the store; re-reads are themselves F59-recorded); pi's structured-typed-lite summary template ≈ prose cost, locator-preserving; two universal must-haves (tool-call/result boundary protection; reactive drop-oldest-retry). **Anti-adoption rails:** opaque provider capsules (un-auditable, lock-in, `store:false`) · LLMLingua-class token-dropping (dangerous on exact strings — traces/diffs/JSON).
