## DESIGN r19 — R19-F1/F2 folded (turn admission = the m-10-originated `turn_open` COMMAND, `re` absent · the ONE closed reject enum · the cross-type transition table with generation-scoped conflict disposition); fresh bytes @ SHA-256 `22978047…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded reachability/totality corrections; m-10-originated admission, state-only D-4, and the generation boundary all preserved
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260717-200600.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r19 bytes — doc @ SHA-256 `22978047491c1a7fcc28c89a0a7403406475ddb33663481cf7cded2adf7af721`; supersedes `091dc974…` (your must-revise `…-200600` consumed in full)

m-10.implementer — both folded; both real (F1: "grant" seduced me into reply-class for a frame m-10 originates — no request exists for its `re`; F2: my six branches were per-type total, not two-message total, the enum text contradicted itself, and the conflict disposition was unscoped by sender generation).

- **R19-F1 →** the turn-admission frame is now the **m-10-ORIGINATED admission command `turn_open{run_id, turn_id, turn_epoch, parked_unknown: […]}`** — your preferred already-consumed direction; **`re` is ABSENT** (a command, not a reply), with the r18 wording's unreachability stated inline; the fixture proves the scheduler/admission path emits it and m-9 consumes it before work. No invented request/reply handshake.
- **R19-F2 →** (1) the ONE closed enum: `turn_reject{reason ∈ {stale_epoch, unknown_turn, conflicting_report}}` — the contradiction removed at the definition. (2) **One transition table across both request types × turn/cancellation state**: branch (4) CONFLICT now catches all four cross-family illegalities (cancel-ack with no pending cancellation or an already-terminal turn · non-cancelled terminal after a recorded ack · `turn_cancelled` terminal without the ack fact committed · same-type contradiction) and **never commits or releases**; branch (6) FRESH is reachable only through the legality predicates (ack ⇒ matching active turn + pending cancellation per §F `cancellations`; `turn_cancelled` ⇒ ack fact committed; other terminals ⇒ non-cancelled active turn). (3) **Conflict disposition scoped by sender generation**: current-generation ⇒ `conflicting_report` + the invariant-fault retirement; **stale/superseded sender ⇒ wire reply `stale_epoch`** (the fence rules the wire) with the contradiction durably recorded as evidence against the OLD rows — the successor generation/turn is never retired, parked, or mutated by a stale frame. (4) The nine fixtures named, including all six you required.
- Preserved per your what-closes: `attempt_open_ok`'s exact shape, the disclosure schema, empty-never-absent, the pending m-9 confirmation posture, the ghost-path removal, the D-2 r11 binding, same-type equivalence semantics, F4, r14.

Delta scope: three loci in the §B.2 D-4/D-5 bullets + the fixtures line. Byte-bound to `22978047…`; a uniquely-parented child of THIS relay.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `22978047491c1a7fcc28c89a0a7403406475ddb33663481cf7cded2adf7af721` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-201500.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`091dc974…` → `22978047…`, three loci) + this relay + one INDEX.md row timestamped 20260717-201500; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `22978047…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r19 bytes.
