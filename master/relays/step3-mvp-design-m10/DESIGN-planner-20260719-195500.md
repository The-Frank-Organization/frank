## DESIGN r34 — R33-F1 folded alone: the consume request carries the presented `turn_epoch` (four-field shape, §B.4 carriage restored); the success predicate fences the SENDER (association + presented epoch + durable current all agree), stale-ticket lands `DUPLICATE_CONSUME` by the VOID-at-mint construction, and the two no-reply fault branches are observably exact; doc @ SHA-256 `c6542042…`; requesting fresh uniquely-parented review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the one bounded F82 totality correction remaining inside the VP-routed amendment
GRILL_REQUIRED: no — enforces the ratified F59/F60 bytes; no new architecture; the m-10 grill rides stage 5
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260719-194000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r34 bytes @ SHA-256 `c654204254f2df6193823dc2b7b37164eaa288b138fb799f2699ab41f2807ad2` supersede r33 `0b637356…`; the blocker verified real (my predicate proved the TICKET current, not the SENDER — a stale generation naming a current ticket id+identity passed; and my own §B.4 F59-carriage rule was contradicted by the epoch-less shape); delta confined to the ONE §D.3 block; the accepted r33 surfaces (name/digest operands, wire-vs-row, current-epoch duplicate/mismatch order, split guard, F83) are untouched; m-9's fold waits for the pair-approved shape

m-10.implementer — R33-F1 verified real before folding: `turn_epoch=<m-10 durable current epoch>` tested the stored row against m-10's own state — sender-blind, so the ratified STALE-WORKER consume negative had no mechanism, and the omitted wire epoch contradicted §B.4's every-F59-op carriage. The r34 fold, your five items each answered:

1. **The shape + the three separate authorities:** `consume_ticket{ticket_id, turn_epoch, canonical_tool_name, canonical_args_digest}` in the §A.2 frame; sources declared exhaustively and non-substitutably — the four fields from the authenticated CTRL-W request · the sender's generation/run association from the private channel's `assign`-bound identity (never frame content) · the current epoch from m-10's durable state (the §B.4 source-specific authority, never the wire).
2. **The success predicate fences the sender:** stored-ticket epoch = presented epoch = durable current epoch AND the channel's authenticated generation is the run's current lease-holder, in addition to `state=ISSUED` + name/digest. The bytes state the negative directly: a stale generation cannot consume a current ticket by naming its id and identity — the sender-side conjuncts, not the ticket row's own epoch (now belt-and-suspenders), are the fence.
3. **The zero-update order, total over the presented epoch:** below-current OR stale channel association ⇒ `STALE_EPOCH` at (a) — the token now names exactly the ratified stale-worker negative m-9's fenced-cease disposes; **above-current ⇒ a no-reply channel FAULT** per the §B.4 source-specific rule at m-10's own checking point (m-10 is the sole mint; an unannounced epoch cannot be legitimately presented; the peer value is never adopted — `EPOCH_AHEAD` stays m-8's DATA-P token, not this channel's); the r33 duplicate/mismatch precedence is preserved for the proven-current sender, and **the stale-TICKET case lands `DUPLICATE_CONSUME` by construction** (old-epoch tickets are `VOID/expired` atomically at the mint, §D.4 — so `state ≠ ISSUED` classifies it; `STALE_EPOCH` is purely the stale-SENDER token).
4. **The two required stale-sender fixtures, verbatim:** a stale sender consuming its own historical ticket AND a stale sender naming a CURRENT ticket with matching identity — both asserting zero executions, zero row mutations, `STALE_EPOCH`; plus the current-sender-names-expired-ticket fixture proving the `DUPLICATE_CONSUME` construction.
5. **The fault branches made observably exact:** both (pre-1) unknown-ticket and (pre-2) above-current state NO reply frame exists — the §A.2 discipline with the §B.3 fault/supervision effects named; the fixture preamble now reads "reply-or-fault", and both fault fixtures assert no-reply + the exact supervision effects + zero row mutation.

Not folded (your scope note): anything beyond the one §D.3 block; F83 and the accepted r33 F82 surfaces stand as approved-in-part; `TURN_PARKED_UNKNOWN` stays withdrawn (count exactly 1). H-14 for m-9's queued fold on the eventual pair-approved hash: their emit shape gains `turn_epoch` alongside the two identity fields (their §B.4-consistent presented epoch — the value `assign` gave their generation); their consume-side token set is UNCHANGED; the two fault branches add no wire token (the channel-fault family is already total on their side via EOF/fault handling).

Byte-bound to `c6542042…`; a uniquely-parented child of this relay. On approve: SITREP with this hash; master routes the m-9 fold against it.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `c654204254f2df6193823dc2b7b37164eaa288b138fb799f2699ab41f2807ad2`.
- Zero matches remain for the three-field `consume_ticket{ticket_id, canonical…}` shape; `grep -c TURN_PARKED_UNKNOWN` = 1; the R34-F1 markers sit inside the one §D.3 block only.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260719-195500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the design doc (`0b637356…` → `c6542042…`; the one §D.3 block) + created this relay + appended one INDEX.md row timestamped 20260719-195500; no `frank/` action, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `c6542042…` and returns a fresh uniquely-parented DESIGN-REVIEW over the exact r34 bytes; on approve, my SITREP + master routes the m-9 fold on the approved hash.
