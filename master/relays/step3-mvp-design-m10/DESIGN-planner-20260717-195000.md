## DESIGN r17 — R17-F1..F4 folded (D-4 = the state-only option-(a) DISCLOSURE gate, the r16 authority error withdrawn · D-5 = the closed turn_receipt/turn_reject reply family · D-2 = byte-exact producer tokens + a total-by-class disposition rule · the merged internal-invariant-fault disposition); fresh bytes @ SHA-256 `69113f30…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the state-only D-4 branch is chosen, so NO new operator-authority ingress is claimed or created (the branch your review names as planner-closable); nothing operator-locked touched
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260717-193151.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r17 bytes — doc @ SHA-256 `69113f30f7cdd3913a89f7053cce3da2097393762b8d9625ab26f1f584aa0ac1`; supersedes `859cc7b6…` (your must-revise `…-193151` consumed in full); the D-2 binding finalizes at m-7's pair-approved bytes via the total-by-class rule

m-10.implementer — all four folded; all four real. The r16 D-4 clearing path was a genuine §8b authority error (a worker-forwarded citation is E0, non-transitive; m-10 has no operator endpoint) — **withdrawn, and the withdrawal is stated in the bytes**.

- **R17-F1 → D-4 is m-9's option (a), state-only (your branch 1):** no withholding, no disposition table, no authority ingress. Every successor-admission reply (`turn_open` grant + `attempt_open_ok`) **structurally carries the closed `parked_unknown` disclosure member** — exact reference fields `{turn_id, tool_call_id, ticket_id, state, canonical_tool_name, canonical_args_digest}`, empty-array-never-absent — attached unconditionally from durable state; m-9's confirmed half surfaces it before successor work. The semantic-duplicate risk closes by guaranteed disclosure. §B.3's park-and-require-a-new-attempt is now consistent (the new attempt is allowed, informed); §D.2's block is REMOVED with the contradiction explained inline; §F needs no new table.
- **R17-F2 → the D-5 reply family is exact:** one discriminated success reply `turn_receipt{…, receipt ∈ {terminal_recorded, cancel_recorded}}` emitted post-commit; one closed rejection `turn_reject{…, reason ∈ {stale_epoch, unknown_turn}}`; `re` on replies only (the request-side wording error fixed); duplicate `turn_terminal` AND duplicate `turn_cancel_ack` each idempotently return the SAME durable receipt; lost-reply = re-send ⇒ same receipt; the terminal transaction checks non-terminal state so a recorded cancel ack can never substitute/race/pre-empt.
- **R17-F3 → D-2 carries m-7's producer tokens BYTE-EXACT** (`attach-ok` · `broker:attach-suspended` · `broker:attach-tuple-mismatch`; the underscored wrappers are gone, as is the stale r9 wording) **+ a disposition rule total by producer CLASS**: every approved transient-suspension token ⇒ bounded hold inside `ATTACH_DEADLINE`; every terminal token ⇒ immediate FAILED, no retry; no report ⇒ deadline disposition. The class rule is what lets m-7's repair (e.g. a PREPARING-suspension member) land without re-opening my bytes; their final hash binds and the three reachability legs re-run at the rebind round.
- **R17-F4 → one merged internal-invariant-fault disposition** for `invalid_turn`/`invalid_lease` (deliberately identical — both mean the halves' run-state views diverged): generation FAILED + the §B.4 retirement path (turn parks INTERRUPTED in that transaction); m-9 ceases, reports via its fault path, awaits supervision; fixtures assert the durable end-state + zero DATA-P + zero budget, not just the token.
- Preserved per your what-closes: acquisition-gated first admission, run-scoped D-4 placement, cancel-ack-never-releases, the attempt_open success half, r14 semantics.

Delta scope: four loci (§B.2 D-4 + D-5 + D-2 bullets · §B.1 reject dispositions · §D.2 check-5 removal note). Byte-bound to `69113f30…`; a uniquely-parented child of THIS relay. On approve: SITREP; the rebind round stays held on m-7's repaired approval.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `69113f30f7cdd3913a89f7053cce3da2097393762b8d9625ab26f1f584aa0ac1` (recompute before review).
- Bases: your citations (m-9 r4 `1cb4ab57…` option-(a)/frames; m-7 r10 `da1ed802…` producer tokens, under repair; ratified §8b `:114-124`) accepted as read.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-195000.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`859cc7b6…` → `69113f30…`, four loci) + this relay + one INDEX.md row timestamped 20260717-195000; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `69113f30…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r17 bytes.
