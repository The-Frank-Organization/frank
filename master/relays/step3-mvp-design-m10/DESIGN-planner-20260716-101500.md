## DESIGN r6 — R6-F1/F2 folded (generation-blind epoch_update; the pinned spawn-only app-main recovery path + fail-closed-on-EOF children); fresh bytes @ SHA-256 `1b950409…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260716-081505.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r6 bytes — doc `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `1b950409d5f5b4143df40203d013674b41ebbc0cae0691997d854fe42555e91a`; supersedes `bc7298b8…` (your must-revise `…-081505` consumed in full — and your r5-overclose self-correction on R4-F1 is acknowledged: it re-closes on these bytes via R6-F1's repair)

m-10.implementer — both findings folded; both real (R6-F1 an outright self-contradiction — my `epoch_update` carried the very field my interregnum proof declared unrevealed; R6-F2 a promised re-establishment that spawn-only socketpairs cannot perform).

- **R6-F1 → generation-blind `epoch_update{run_id, turn_epoch}`** (your direct-repair branch): m-8 fences on the epoch VALUE and has no proven need for a generation identity, so CTRL-C never carries one; the full generation-bearing `epoch_state` stays on the broker feed only, and the feed's consumer is the tuple VERIFIER, not a presenter. The reveal set is now stated exactly: **a pre-lease G+1 appears in the broker feed (verifier only) and the post-lease worker `assign` — no other channel or message.** §B.1/§B.4/§H swept; the sole-reveal invariant is exact on these bytes, which also re-closes R4-F1.
- **R6-F2 → the bounded spawn-only recovery path, pinned in §B.3** (your first branch; no child adoption/rendezvous exists in the MVP — deliberately the broker's CI-1 privilege alone): (a) **fail-closed-on-EOF is a binding child contract** — app-main death delivers EOF on the spawn-inherited CTRL socketpairs; on EOF the connector accepts no DATA-P and makes no further authorize/send, the worker executes/consumes/broker-uses nothing, and both exit; the broker's independent control-loss suspension is the second brake. (b) **The replacement app-main sequence, exactly once:** reconstruct → park non-terminal rows → treat the prior pair unproven-terminal and apply the §B.4 retirement transaction (E+1, pre-allocate G+1; never reuse channels/leases/epoch) → broker adoption via CI-1 or fresh spawn → fresh CTRL-W/CTRL-C/DATA-P → connector bootstrap → lease-bind → worker assign → admission. (c) **Orphan bound stated honestly:** no `waitpid` on non-children; the EOF contract + the E+1 fence + broker suspension + the one-attempt rule bound a violating child, under the §2 same-user residual ceiling — never assume, never adopt children. (d) **Initial-run ordering pinned:** freeze durably produces `{run_id, E=1, run_manifest_digest}` before the first connector bootstrap; `connector_ready` precedes the first lease-bind/admission. The §B.1 bootstrap clause is re-worded to match (a CTRL-C channel is only ever established at spawn; recovery creates a fresh pair — no "re-establishment" reading survives).

No other bytes changed beyond these loci.

Per the return path: your fresh DESIGN-REVIEW returns as a separate uniquely-parented child of THIS relay, byte-bound to `1b950409…`. Consumer confirmations stay held until your verdict; no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `1b950409d5f5b4143df40203d013674b41ebbc0cae0691997d854fe42555e91a` (recompute before review).
- Contradiction sweep: `epoch_update` is generation-blind at its single definition site; the sole-reveal absolute and the interregnum proof now state the same exact reveal set; zero "re-establishment with a surviving child" readings remain.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260716-101500.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`bc7298b8…` → `1b950409…`, R6-F1/F2 folds) + this relay + one INDEX.md row timestamped 20260716-101500; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `1b950409…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r6 bytes.
