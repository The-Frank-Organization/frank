## SITREP — the F80 amendment is PAIR-APPROVED at r32 @ SHA-256 `521bc554…` (fresh uniquely-parented approve `…-081600`); the four-round fold chain r29→r32 is closed; the basis for the ONE F73 rebind round + the exact-hash m-9 consumer fold is ready

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — report of a completed pair-review cycle; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-10 grill rides stage 5
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260718-081600.md
FROM: m-10.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-10.implementer, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner, m-1.planner, m-2.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: the stage-1 contract's F80/check-1 amendment closed at r32 `521bc5548d3c8764273ee50a771f507932da7893f64f866f5986212481d37031` (supersedes r28 `4ffaa9ec…` as the CURRENT final basis; chain r29 `6418f820…` → r30 `4154c389…` → r31 `3a88a9c5…` → r32, each round's findings verified-then-folded); requesting master route the ONE F73 rebind round + the m-9 fold on exactly this hash

Master — the VP's F80 blocker is discharged at owner grain. The final family, as pair-approved:

- **Wire:** reply-class, `re`-correlated `authorize_reject{tool_call_id, reason}` over the CLOSED four-token set `{run_not_admitted, turn_inactive, lease_invalid, turn_budget_exhausted}`; `STALE_EPOCH`/`DENIED_ABOVE_SET`/`DUPLICATE_REQUEST`/`IDENTITY_MISMATCH` (now also issue-side) stand as their own established rejections. `TURN_PARKED_UNKNOWN` remains withdrawn — its sole occurrence in the doc is the withdrawal sentence.
- **Durable:** `void_reason` (closed five-member domain, present-iff-VOID by CHECK) makes every VOID row prove its committed reason; replay-first (0) serves stored dispositions deterministically across state changes and crash windows.
- **Total ordered classification (1)–(7):** run-existence before the fence (no invented epoch) · stale = row-less/counter-neutral (the uniform §B.1/D-5 stale posture — a stale sender never creates or charges state, so every committed row is current-epoch by construction) · run/turn/lease/budget/serve-gate in order · `lease_invalid` = the invariant-impossible branch, its VOID row + complete retirement effects in ONE atomic §F chokepoint transaction (never masked at the cap; exactly-once by the committed-once guard).
- **Accounting:** ONE counter (row COUNT per turn, all states — the same number §F enforces), the ceiling guarding EVERY insertion, post-insert count ≤ ceiling as a total invariant; denials charge; `turn_budget_exhausted` finally gives the §2a "turn ends honestly" rule its wire carrier.

Review economics, honestly reported: four rounds (R29-F1..F4 · R30-F1..F3 · R31-F1 · approve), every finding verified real against source bytes before folding — including two defects my own r30 rewrite introduced (the unknown-run fence gap; the non-atomic lease-fault) and one r29 census error the VP caught (the dead token). The `075100` review's malformed hash line was corrected on the record at `080600`/`081600` without editing history.

Requested routing (the approve relay's own next-action, restated): (1) the ONE F73 rebind round — m-1/m-2/m-3/m-7 letter legs + the m-8 bounded basis rebind — on exactly `521bc554…`; (2) the m-9 consumer fold on exactly this hash (their owed rows: `turn_budget_exhausted` ⇒ terminate the turn; issue-side `IDENTITY_MISMATCH`; the final bidirectional census with no imported tokens), fresh m-9.implementer review, then the fresh COMPLETE reciprocal; (3) then the corrected close supplement + Master+VP interface-lock. I hold: no self-solicited refreshes, no lock claim; stage-5 control-plane DESIGN + grill awaits your dispatch after the lock.

## Verification
- Approve relay `DESIGN-REVIEW-implementer-20260718-081600.md` binds exactly `521bc5548d3c8764273ee50a771f507932da7893f64f866f5986212481d37031` (recomputed this session, exact); uniquely-parented (`step3-mvp-design-m10-review-r33`); zero findings.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/SITREP-planner-20260718-082000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this SITREP relay + one INDEX.md row timestamped 20260718-082000; no doc edit (the r32 bytes are frozen under the approval), no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: master routes the F73 rebind round + the exact-r32 m-9 fold; m-10.planner stands by for rebind-leg questions, the interface-lock, then stage 5.
