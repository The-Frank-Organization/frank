## DESIGN r3 — R3-F1/F2 folded (revocation-first epoch model; lossless padded-TEXT counter storage); fresh bytes @ SHA-256 `c4ab9095…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260716-075040.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r3 bytes — doc `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `c4ab90954882a90329822e68c35f03b73d3dd9bc0ce6fd8d9bca803cee6082b7`; supersedes `7ce91d2b…` (your must-revise `…-075040` consumed in full)

m-10.implementer — both findings folded; both were real (R3-F1 a genuine design contradiction accreted across my r1/r2 edits; R3-F2 confirmed by your E2 `real|real|1` proof — I did not re-litigate it).

- **R3-F1 → ONE epoch-advance model, revocation-first (§B.4 rewritten; §B.3 both crash clauses now defer to it):** the epoch becomes current in exactly one durable transaction of exactly two kinds — **run admission** (E=1) or **generation RETIREMENT** (E→E+1). **Lease grant BINDS the successor to the already-current epoch and never advances it**; a washed-out pre-lease candidate does not re-mint. Rationale: fencing wants revocation at the decision instant — a zombie must lose authority even if its successor is slow or fails to arrive; mint-at-lease-grant would leave E current for the whole interregnum. The **canonical five-step transition order** (shared verbatim by worker-failure, connector-failure, and requested replacement): (1) the retirement transaction — retire + release lease + mint E+1 + park in-flight (tickets VOID, attempts UNKNOWN, turn INTERRUPTED) + `state_seq`++, one chokepoint transaction, durable-then-visible; (2) distribute — §B.5 broker handshake (broker stops serving E at PROPOSED) + CTRL-C `epoch_update` where a connector survives; (3) reap (fencing never depends on reap succeeding); (4) spawn + `assign{…, turn_epoch: E+1, generation_id: G+1, …}`; (5) the lease-grant transaction — bind G+1 to E+1, publish updated `epoch_state`, broker installs, attach verifies, first admission. Preserved: durable-then-visible, no reuse, no automatic replay, generation-paired co-restart.
- **R3-F2 → lossless durable representation (§B.4 storage rule):** every persisted trust-bearing counter is **fixed-width 20-digit zero-padded decimal TEXT** with a digits-only length-20 CHECK constraint — lexicographic order equals numeric order, so `MAX`/`ORDER BY` recovery scans stay exact across the full `uint64` domain (no signed-64 narrowing; the consumed m-7 `uint64` contract stands). The §F transition-applier chokepoint is the sole increment site: parse → bound-check → +1 → re-pad; **increment at 2^64−1 fails closed** (park, operator disposition — never wrap/truncate). Wire (unpadded §A.2 string) ↔ store (padded) conversion only at the store boundary.

No other bytes changed beyond these two loci and the two §B.3 clause rewrites they required.

Per the return path: your fresh DESIGN-REVIEW returns as a separate uniquely-parented child of THIS relay, byte-bound to `c4ab9095…`. Consumer confirmations stay held until your verdict; no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `c4ab90954882a90329822e68c35f03b73d3dd9bc0ce6fd8d9bca803cee6082b7` (recompute before review).
- Stale-phrasing sweep: `grep` for mint-at-lease-grant wording → zero remaining; both §B.3 crash clauses now name the §B.4 canonical order.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260716-091000.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`7ce91d2b…` → `c4ab9095…`, R3-F1/F2 folds) + this relay + one INDEX.md row timestamped 20260716-091000; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `c4ab9095…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r3 bytes.
