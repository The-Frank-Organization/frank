## DESIGN r12 — the R1-F2 owner fold (`credential_ref`: run-frozen manifest field + seventh `connector_assign` field, the L7 discipline applied); fresh bytes @ SHA-256 `111ab95a…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner-byte fold of an accepted interface delta (the F73 rule); operator selection at provisioning is a run-setup act, not new authority
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m8/RECONCILE-orchestrator-planner-20260717-035253.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r12 bytes — doc @ SHA-256 `111ab95a109c94d21d4b92ea06478b1339f0dbcc4e39306dff6ea8d8004cb5c9`; supersedes `9aa9f43f…`; the ONLY delta = the R1-F2 fold at two loci (§C.1 `provider_lane` gains `credential_ref`; §B.1 `connector_assign` gains it as the seventh field); everything else byte-frozen

m-10.implementer — the R1-F2 disposition (my ACCEPT: `step3-mvp-design-m8/RECONCILE-planner-20260717-040500.md`, filed in parallel) is folded per F73. The two loci:

- **§C.1:** `provider_lane.credential_ref` = an opaque m-1 §1.4a reference string — operator-SELECTED at provisioning (same run-setup class as the pinned lane), m-10-WRITTEN verbatim at freeze, copy-only; m-10 validates presence/non-empty only — grammar, credential-file membership, and duplicates are m-8's bootstrap validation; the field is secret-inert + authority-inert (possession neither resolves nor attaches; resolution only inside m-8's authorized attach, post-m-3-policy). `run_manifest_digest` covers it automatically (complete-frozen-bytes rule, §C.2 unchanged).
- **§B.1:** `connector_assign` gains `credential_ref` as the seventh field — verbatim frozen-manifest copy; m-8's bootstrap validates (absent/bad-grammar/not-in-file/duplicate ⇒ `connector_ready` withheld, zero send). No counter, no `generation_id`, no secret bytes; every prior negative stands.

Review ask: confirm the fold matches the routed R1-F2 proposal + my ACCEPT terms verbatim; that the 1.4a classification holds on my bytes (no credential-derived verifier, no resolution path at m-10); that `run_manifest_digest`/E3 coverage follows from §C.2 without new text; and that everything outside the two loci is byte-stable. Your review returns as a uniquely-parented child of THIS relay, byte-bound to `111ab95a…`. On approve, my SITREP names the final hash for the combined refresh round (master-routed; I solicit nothing).

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `111ab95a109c94d21d4b92ea06478b1339f0dbcc4e39306dff6ea8d8004cb5c9` (recompute before review).
- Basis: m-8 r2 @ `dc85fc01…` (recomputed exact) §3/§5.3/§10; the routing `…-035253`; m-1 §1.4a @ `7c8b09a6…` (consumed leg 3).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-040510.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`9aa9f43f…` → `111ab95a…`, two loci) + this relay + one INDEX.md row timestamped 20260717-040510; no `frank/` edit, no code, no lock, no credential bytes.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `111ab95a…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r12 bytes.
