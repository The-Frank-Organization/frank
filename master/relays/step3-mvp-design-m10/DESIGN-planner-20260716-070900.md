## DESIGN — m-10 stage-1 owned contract: the app-IPC/supervision interface + the run-manifest/dispatch-seam (8-name carrier · F59 one-shot ticket protocol · durable app-state schema · §6 wake contract) — final bytes @ SHA-256 `b971ea2f…`; requesting m-10.implementer pair review of THESE exact bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-orchestrator-planner-20260716-041640.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: pair-review request — the stage-1 contract is authored; doc = `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `b971ea2f342f950c73808903ea12378c7f024099cdd9270d571cf0943bbdf880`; any byte revision triggers a fresh review

m-10.implementer — the stage-1 DESIGN dispatched in `step3-mvp-design-m10/…-041640` (+ supplement `…-043530`, consumed: m-3 joins the consumer set on `run_manifest_digest`; the m-2 F58-components confirmation is owed reciprocally when m-2's bytes land) is authored. **Review THESE exact bytes:**

**Doc:** `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md`
**SHA-256:** `b971ea2f342f950c73808903ea12378c7f024099cdd9270d571cf0943bbdf880`
**Basis:** the ratified MVP amendment r7 @ `2f75f2a1…` (§2/§2a/§2b/§4/§6/§7/§10), the reframe packet @ `2d240eb6…` (preserved rails), the m-10 charter delta (F44 fold).

### What the contract pins (owner summary)
- **§A topology + IPC:** m-10 = connection BROKER, never traffic hub — CTRL-W (m-10↔worker), CTRL-C (m-10↔connector, no provider payload), DATA-P (m-9↔m-8 DIRECT; m-10 creates the socketpair, holds no endpoint after spawn). Length-prefixed JCS-JSON frames, `v`/`seq`/`re` envelope, `FRAME_MAX`, reject-unknown-type / ignore-unknown-field. As-if-process-separated throughout. Bounded queues; only wake/advisory frames droppable.
- **§B supervision:** worker-generation state machine; the two per-run leases (one-active-worker; active-turn); fail-closed crash disposition to UNKNOWN/PARTIAL/INTERRUPTED, park-and-require-new-attempt, never silent replay, never auto provider resend; `turn_epoch` = per-run monotonic uint64, minted atomically WITH the lease grant at the store chokepoint, durable-then-visible (crash ⇒ at worst a skipped number, never reuse), distributed via CTRL-C push (m-8), the ordered `state_seq` broker feed (m-7 gate, F64), and direct store checks (CTRL-W); **high-water-mark rule** at every checking point (reject below max-seen; adopt-and-record above — exact-match rejection is non-conforming); race-disposition notes for the confirmers.
- **§C manifest/dispatch-seam:** manifest schema v1 carrying the operator-ratified 8 canonical names (F58 staging: null digests pre-lock → locked identity vector at interface-lock → `release_binding` at the F63 post-build event); exact canonical-SET equality over tool IDENTITY, aliases normalized first, absent/malformed/not-exact ⇒ deny-all fail-closed; immutable per `run_id`, byte-identical reload verified; `run_manifest_digest` = SHA-256 over the complete frozen JCS bytes at run freeze, never changes for a run (the m-3 confirmation target).
- **§D F59 tickets (my half):** durable one-shot ticket `{run_id, turn_id, turn_epoch, tool_call_id, canonical_tool_name, canonical_args_digest}`, UNIQUE per assembled call; atomic single-transaction consume (exactly-once); typed rejections `DUPLICATE_CONSUME`/`STALE_EPOCH`/`IDENTITY_MISMATCH`; crash windows per the §7 rule (ISSUED dies with turn/epoch → VOID; CONSUMED-without-outcome parks `UNKNOWN_TOOL_OUTCOME`); **denials are recorded VOID rows (typed reason, digest-only, never payload) and count toward the §2a per-turn tool-call ceiling** (closes the unbounded denial-loop hole).
- **§E wake:** at-most-once scheduling via durable `UNIQUE(relay_id)`; push best-effort/advisory (F61); recovery = m-9 durable rediscovery; m-10 never touches the conductor. Non-gating stretch.
- **§F store:** single SQLite (WAL, `synchronous=FULL`), sole-writer m-10 module, PRIVATE file (all consumers via frames/feed, never the file — the verbs-not-raw-store-paths guardrail applied app-side); `PRAGMA user_version` schema versioning + forward-only migrations from day one; the **single-transition-chokepoint rule** (binding), which also enables the deferred optional forensic events journal (toggle + retention + export; NOT built in MVP; operator-directed 2026-07-16).
- **§G–§I:** boundary negatives (no seat, no credential bytes, no provider payload on CTRL/broker, no policy authored — the 8 names are the operator's, the Step-4 ceiling is m-5's into this same seam); consumer confirmation map (m-9 · m-8 · m-7 · m-3; m-2 reciprocal); named deferrals incl. TOON at the presentation layer (routed to m-9 + Step-4 AXI/H-13).

### Review guidance (adversary these hardest)
1. Above-set/zero-dispatch + authorized==executed against the §10 annex rows (the F59 negatives: duplicate consume, stale epoch, mutated args, both crash windows).
2. The broker-feed shape vs F64's per-verb/per-push gate needs (m-7 will confirm; you review first).
3. Any residue of policy authorship, seat capability, credential-byte handling, or provider payload on a CTRL channel.
4. The F58 staging honesty (names-only now; nothing claims the build identity pre-lock; release binding lands at F63, not before).
5. As-if-process-separated consistency (nothing depends on module co-residency).

Per the dispatch return path: your DESIGN-REVIEW returns as a **separate, uniquely-parented CHILD of this relay**; any byte revision to the doc receives a **fresh** review. After your verdict I return the report-only SITREP to master naming the approved bytes + hash; consumer confirmations route on master's direction; the Master+VP interface-lock is the gate — no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `b971ea2f342f950c73808903ea12378c7f024099cdd9270d571cf0943bbdf880` (recompute before you review; a mismatch means the bytes moved — stop and ask).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260716-070900.md` — run at filing; result reported inline to the operator.
- Amendment hash independently recomputed this session: `2f75f2a1df00c4e330fc052bb06023547b3cfd1fe323c1e9cb0e43b9550e6e1d` (exact match).

ACTIONS_GIT_REF: design-only — authored the design doc (`b971ea2f…`) + this relay + one INDEX.md row timestamped 20260716-070900; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes the doc hash, adversarially reviews the final bytes, and returns DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required) as a uniquely-parented child of THIS relay.
