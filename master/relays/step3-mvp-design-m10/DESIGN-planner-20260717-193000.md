## DESIGN r16 — R16-F1..F4 folded (D-2 = observed acquisition via the worker's attach_result on the m-7 D-3 taxonomy · D-4 = the run-scoped ADMISSION predicate with operator disposition · D-5 = m-9's exact two shapes, cancel-ack never releases the lease · the attempt_open_reject no-row half); fresh bytes @ SHA-256 `859cc7b6…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded interface-totality corrections inside ratified ownership; nothing operator-locked touched
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260717-191809.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r16 bytes — doc @ SHA-256 `859cc7b69c982e892c87a21b97cb04113558d0c89cf5aa2736e1c50725271e21`; supersedes `fe65062f…` (your must-revise `…-191809` consumed in full); the D-2 taxonomy binding is by-name pending m-7's repaired D-3 bytes, exactly per your gate note

m-10.implementer — all four findings folded; all four real (my r15 batch consumed the routed *summaries* faithfully but not the source contracts' exact halves — D-2 proved the wrong fact, D-4 gated the wrong scope at the wrong point, D-5 rewrote frames that were pinned, F4 left the reject half as prose).

- **R16-F1 → §B.2 attach-acquisition gate:** the worker→m-10 CTRL-W `attach_result{generation_id, turn_epoch, result}` with `result` bound BY NAME to m-7's D-3 producer taxonomy (`attach_ok` — emitted only after the worker holds its capability · `attach_suspended` — transient · `attach_tuple_mismatch` — terminal). First-turn admission gates on **receipt of `attach_ok`**, never on installed feed state (verifier readiness ≠ acquisition). Mapping per your required-return: suspended ⇒ bounded hold inside `ATTACH_DEADLINE`; tuple-mismatch ⇒ **immediate FAILED, no retry**; missing/wedged ⇒ the deadline disposition. The taxonomy binding **re-verifies at m-7's fresh approval** (their r9 is must-revise) — stated in the bytes, so the leg cannot be mistaken for stable early.
- **R16-F2 → §B.2 run-scoped ADMISSION predicate (the state-owner branch, done right):** while ANY parked `UNKNOWN_TOOL_OUTCOME`/`PARTIAL_TOOL_EFFECT` row exists in the run, **no successor `turn_open` and no `attempt_open` is admitted** — typed `RUN_PARKED_UNKNOWN{row refs}` reply — until a **named operator disposition** (a durable operator-authorized record per §8b; never an m-10/worker self-clear) transitions the parked row to a terminal disposed state and clears the predicate. The §D.2 check is re-scoped to RUN grain and explicitly demoted to defense-in-depth.
- **R16-F3 → §B.2 turn-terminal consumption, m-9's exact shapes:** `turn_terminal{run_id, turn_id, turn_epoch, terminal, attempts_summary_ref?}` (their closed enum) + `turn_cancel_ack{run_id, turn_id, turn_epoch, partial_disposition}` — **cancel-ack records and never terminalizes/releases**; only the matching `turn_terminal{terminal: turn_cancelled}` transaction (composition, not replacement) terminalizes + releases the lease; duplicate terminal ⇒ idempotent same-reply; typed stale-epoch/unknown-turn rejections; the crash cut preserved (receipt-without-commit ⇒ in-flight, parks per §B.3).
- **R16-F4 → §B.1 `attempt_open_reject{attempt_id, reason ∈ {stale_epoch, invalid_turn, invalid_lease}}`:** reply-class, `re`-correlated, NO row committed; m-9 consumption pinned (no DATA-P, no budget charge; stale_epoch = ordinary fence outcome; the other two = named fail-closed supervision dispositions); the four T4 fixtures named (success · each reason · lost-reply · commit-before-ack).
- Preserved untouched, per your what-closes: the `attempt_open_ok` success ordering, the budget rule, the r14 `rejected_local` semantics, the CI-1 citation.

Delta scope: three loci (§B.2 three bullets rewritten/replaced · §B.1 reject half · §D.2 check (5) re-scoped). Byte-bound to `859cc7b6…`; a uniquely-parented child of THIS relay. On approve: SITREP; the rebind round stays held on m-7's repaired bytes per your gate note.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `859cc7b69c982e892c87a21b97cb04113558d0c89cf5aa2736e1c50725271e21` (recompute before review).
- Bases: m-9 r4 @ `1cb4ab57…` (`:35-38,128-130,144-149`), m-8 r6 @ `ab63f6eb…` (`:74-91`), m-7 r9 @ `ed66e038…` (D-3 names; under revision) — your citations accepted as read.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-193000.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`fe65062f…` → `859cc7b6…`, three loci) + this relay + one INDEX.md row timestamped 20260717-193000; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `859cc7b6…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r16 bytes; the D-2 taxonomy leg re-verifies at m-7's fresh approval before any rebind.
