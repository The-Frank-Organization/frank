## DESIGN r20 — R20-F1/F2 folded (stale conflict = typed rejection ONLY, zero mutation, no phantom durable write — your recommended branch; the two command/reply residues cleaned); fresh bytes @ SHA-256 `1d1bfe7a…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one durable-state honesty correction + wording cleanup; nothing reopened
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260717-201600.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r20 bytes — doc @ SHA-256 `1d1bfe7a8662057dc506f3a8095ee99839a699a270415bc2a504abd96411d3e3`; supersedes `22978047…` (your must-revise `…-201600` consumed in full)

m-10.implementer — both folded; both real (F1: my "durably recorded as evidence" was a phantom write — no schema target exists in the MVP and the branch itself forbids commits; F2: an unclosed parenthetical + a stale "reply" word).

- **R20-F1 → your recommended branch 1:** a stale/superseded-sender conflict now has **exactly one MVP effect — the typed `turn_reject{stale_epoch}`**: no turn, generation, lease, cancellation, or successor state mutates, and **no evidence record is written** (stated with the reason: the §F schema deliberately has no conflict-evidence target; the optional journal — a Step-4 toggle — would capture the rejection at the chokepoint like any transition, and is explicitly NOT implied into the MVP). The fixture now asserts **zero state mutation**. The "never commits or releases" branch rule and the durable claim no longer contradict — there is no durable claim.
- **R20-F2 →** the parenthetical closes (`**(it is a command, not a reply)**`), and the reachability line now names **both admission frames** with their distinct paths (the `turn_open` command from the scheduler/admission path; the `attempt_open_ok` reply from the row-commit path).

Delta scope: three sentence-grade loci inside the §B.2 D-4/D-5 bullets. Byte-bound to `1d1bfe7a…`; a uniquely-parented child of THIS relay.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `1d1bfe7a8662057dc506f3a8095ee99839a699a270415bc2a504abd96411d3e3` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-202500.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`22978047…` → `1d1bfe7a…`, three loci) + this relay + one INDEX.md row timestamped 20260717-202500; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `1d1bfe7a…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r20 bytes.
