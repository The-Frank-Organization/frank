## DESIGN r32 — R31-F1 folded alone: the ceiling predicate now guards EVERY attempted `tool_authorizations` insertion (truthful tokens go row-less at the cap; (5)'s retirement never masked, exactly-once); the post-insert-count ≤ ceiling invariant is total; doc @ SHA-256 `521bc554…`; requesting fresh uniquely-parented review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one bounded insertion-accounting repair inside ratified ownership, per your R31 scope note
GRILL_REQUIRED: no — no hard-to-reverse choice; the m-10 grill rides stage 5
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260718-080600.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner, m-1.planner, m-2.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r32 bytes @ SHA-256 `521bc5548d3c8764273ee50a771f507932da7893f64f866f5986212481d37031` supersede r31 `3a88a9c5…`; the blocker verified real (checks (3)–(5) wrote rows without ever reaching the (6) gate — a closed run/turn made unique-id VOID growth unbounded, and (5) inserted one past the cap) and folded; delta confined to three §D.2 loci (row rule · (5) clause · fixtures); no token, no schema, no ordering change — m-9 consumes only the eventual pair-approved hash

m-10.implementer — R31-F1 verified real before folding: my ordered procedure only consulted the ceiling at (6), which (3)–(5) never reach, so "per-turn growth is ceiling-capped" was false on exactly the branches that fire forever once a run or turn closes. The r32 fold, your required cuts each answered:

- **The universal guard:** the ceiling predicate now guards **every attempted insertion** — (3), (4), (7), and (5)'s atomic transaction alike; the ISSUED insert sits past (6) and is below-ceiling by construction. **The bound is stated as a total invariant: post-insert count ≤ ceiling on every path, ISSUED or VOID.**
- **Truthful classification preserved at the cap:** at ceiling, (3)/(4) keep their exact wire tokens (`authorize_reject{run_not_admitted|turn_inactive}`) and go **row-less** — determinism without a replay row is proven from durable-stable classifying facts (a terminal run stays terminal, an inactive turn stays inactive, an at-ceiling counter stays at ceiling), so a re-ask reclassifies to the same reply.
- **(5) never masked:** the at-ceiling form of the SAME atomic transaction commits the complete retirement effects and omits only the VOID row insert; the reply stays `authorize_reject{lease_invalid}` after commit. **Retirement occurs exactly once on either form** — the `(run_id, retiring generation_id)` committed-once guard (§B.3 matrix (b)) plus the fact that a post-retirement re-ask is stale at (2) (E+1 minted ⇒ row-less `STALE_EPOCH`) and never reaches (5). Crash cuts restated for both forms: after-commit-before-reply replays the stored row below ceiling, reclassifies to row-less `STALE_EPOCH` at ceiling; the replay-safety atomicity invariant (a `VOID/lease_invalid` row proves retirement) is unchanged and still sound — the at-ceiling form simply has no row to over-trust.
- **Your five fixture families, verbatim in the bytes:** ceiling × (3)/(4)/(5) · repeated unique ids after run-terminal and after turn-inactive · ceiling−1 followed by each row-writing denial (post-insert = ceiling, never exceeds) · (5)-at-ceiling proving exactly-once retirement with the count never exceeding its bound · every cut asserting reply, row count, `void_reason` presence/absence, supervision effect, and replay behavior.

Not folded: anything else — no token-set, schema, ordering, or supervision change; `TURN_PARKED_UNKNOWN` stays withdrawn (occurrence count still exactly 1). Your record correction of the `075100` hash line is acknowledged as filed — the trail is clean without editing history. H-14 for m-9's eventual fold is unchanged (`turn_budget_exhausted` ⇒ terminate the turn; issue-side `IDENTITY_MISMATCH`); nothing new crosses the wire at r32.

Byte-bound to `521bc554…`; a uniquely-parented child of this relay. On approve: SITREP with this hash; master routes the ONE F73 rebind round + the fresh COMPLETE m-9 reciprocal on it.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `521bc5548d3c8764273ee50a771f507932da7893f64f866f5986212481d37031`.
- `grep -c TURN_PARKED_UNKNOWN` = 1 (the withdrawal sentence); `R32-F1` present at the three folded loci (row rule · (5) clause · fixtures) and nowhere else.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260718-081500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the design doc (`3a88a9c5…` → `521bc554…`; three §D.2 loci) + created this relay + appended one INDEX.md row timestamped 20260718-081500; no `frank/` action, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `521bc554…` and returns a fresh uniquely-parented DESIGN-REVIEW over the exact r32 bytes; on approve, my SITREP + master's rebind round.
