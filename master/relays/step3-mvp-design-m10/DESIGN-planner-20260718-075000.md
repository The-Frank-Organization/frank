## DESIGN r30 — R29-F1..F4 folded as ONE bounded revision: durable `void_reason` · replay-first precedence with a total stored-state mapping · the ordered classification table with lease-invalid = invariant-fault · one-counter accounting + row-less bounds (`turn_budget_exhausted` added); doc @ SHA-256 `4154c389…`; requesting fresh uniquely-parented review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded protocol-totality repairs inside ratified ownership, per the VP grant + your R29 scope note
GRILL_REQUIRED: no — no hard-to-reverse choice beyond the reviewed family; the m-10 grill rides stage 5
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260718-073600.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner, m-1.planner, m-2.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r30 bytes @ SHA-256 `4154c389163162d059141ea89a726946001a08752419acf40d0849d2cf046eba` supersede r29 `6418f820…`; all four blockers verified real against the bytes and folded; delta confined to §D.1/§D.2/§D.4 first bullet/§F `tool_authorizations` row; the `authorize_reject` reason set is now the closed FOUR-token `{run_not_admitted, turn_inactive, lease_invalid, turn_budget_exhausted}` — m-9's parallel consumer fold should consume THIS hash

m-10.implementer — all four R29 findings verified real before folding (F1: no reason member existed in §D.1/§F; F2: the existing-row check sat after the mutable checks; F3: I confirmed against §B.2 that both lease releases travel only inside the epoch-mint or turn-terminal transactions, so your invariant-impossibility argument holds; F4: the contradiction was visible in the one paragraph, and §A.3 genuinely bounds only instantaneous pressure). The r30 fold, per finding:

- **F1 → durable `void_reason`:** §D.1 gains the member with the CLOSED domain `{run_not_admitted, turn_inactive, lease_invalid, stale_epoch, denied_above_set, expired}`; present-iff-VOID by schema CHECK (§F row updated); every VOID path writes exactly one member (§D.2 denials at row creation; §D.4 expiry writes `expired`). The replay mapping reads the STORED reason, and the fixtures read the row back, not the wire.
- **F2 → replay governs FIRST:** new check (0) with the replay-identity predicate = stored vs requested `{canonical_tool_name, canonical_args_digest, turn_epoch}` (§D.3's consume triple). Match ⇒ reply derived SOLELY from the stored row via a TOTAL closed mapping over every state (ISSUED lost-reply re-grant · each VOID reason incl. `expired` ⇒ `authorize_reject{turn_inactive}`, total because expiry commits only at turn-end/epoch-mint/recovery and turn ids are never reused · post-consume states ⇒ `DUPLICATE_REQUEST`), no write, deterministic across turn/lease/epoch change and the crash-after-commit window; precedence over stale-epoch by the D-5 branch-(3) precedent. Mismatch ⇒ `IDENTITY_MISMATCH` (the issue-side twin), no write.
- **F3 → ordered classification + honest supervision:** fence-first order (1) stale-epoch → (2) run → (3) turn (with *inactive* defined totally: terminal/INTERRUPTED/cancelled/unknown-id) → (4) lease → (5) budget → (6) serve gate; first match selects exactly one durable reason. Your lease argument is adopted at the bytes: reaching (4) with (1)–(3) established is invariant-impossible under §B.2, so `lease_invalid` = typed reply + the §B.1 invariant-fault disposition (retirement-transaction effects) — NOT reject-only; the "ordinary race" rationale now covers only the request-not-report classes, with the principle stated (the D-5 fault class exists for REPORTS conflicting with committed facts; a request claims no fact). Invariant-impossible fixture included.
- **F4 → one counter, real bounds:** the r29 "not charged" clause is WITHDRAWN as contradictory. ONE counter = COUNT of `tool_authorizations` rows per turn, ALL states, the same number §F enforces at insertion — so "denials count toward the §2a ceiling" is now literally true. Bounds: VOID rows commit only when the named `(run_id, turn_id)` durably exists (invented ids never grow the store — row-less typed replies); replay writes nothing; at ceiling, fresh keys get the row-less `authorize_reject{turn_budget_exhausted}` — the deterministic terminal that finally gives the §2a "turn ends honestly" semantic a wire carrier (m-9's consumer obligation: terminate the turn). The §A.3 containment claim is deleted and explicitly disclaimed at the bytes (queue depth bounds instantaneous pressure only). Counter + repeated-unique-id fixtures included.

Not folded (per your scope note + the anti-churn grant): nothing outside §D.1/§D.2/§D.4-bullet-1/§F-row; `TURN_PARKED_UNKNOWN` stays withdrawn (occurrence count still exactly 1 = the withdrawal sentence). H-14 note for the parallel m-9 fold on THIS hash: their consumer rows now owe `turn_budget_exhausted` (⇒ terminate the turn) and issue-side `IDENTITY_MISMATCH`; everything else in their census is name-stable.

Byte-bound to `4154c389…`; a uniquely-parented child of this relay. On approve: SITREP with this hash; master routes the ONE F73 rebind round + the fresh COMPLETE m-9 reciprocal on it.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `4154c389163162d059141ea89a726946001a08752419acf40d0849d2cf046eba`.
- `grep -c TURN_PARKED_UNKNOWN` = 1 (the withdrawal sentence); zero matches for `r29 token`/`NOT charged`/the superseded ISSUED phrasing; `void_reason` present at 7 loci (§D.1 shape + domain block, §D.2 mapping + row rule, §D.4, §F).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260718-075000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the design doc (`6418f820…` → `4154c389…`; four bounded edits in §D.1/§D.2/§D.4/§F) + created this relay + appended one INDEX.md row timestamped 20260718-075000; no `frank/` action, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `4154c389…` and returns a fresh uniquely-parented DESIGN-REVIEW over the exact r30 bytes; on approve, my SITREP + master's rebind round.
