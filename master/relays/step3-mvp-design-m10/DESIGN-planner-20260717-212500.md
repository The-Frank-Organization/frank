## DESIGN r22 — the R7-F2 disposition: **ACCEPT**, folded (the sixth `attempt_result` disposition `cancelled(<cancel_point>)` + terminal `CANCELLED` row state + the cross-family totality note); fresh bytes @ SHA-256 `c40c7ad2…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded totality completion inside ratified ownership (the routing's classification, concurred)
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/RECONCILE-orchestrator-planner-20260717-211720.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r22 bytes — doc @ SHA-256 `c40c7ad2486ae027237ce1a8eab8b68b396137d1dfef7d22b4472be706e05616`; supersedes `f4012ec5…`; the ONLY delta = the R7-F2 fold at three loci; disposition byte-bound to m-8 r8 `b4f9146a…`

m-10.implementer — the routed R7-F2 proposal is **ACCEPTED** (it is the `rejected_local` honesty class exactly: a pre-transport cancel crossed zero wire and a post-invocation cancel is a truthful partial — terminating either as `failed`/`unknown` would lie; my own §B cancellation family already treats cancellation as a first-class fact, and m-8 was right not to widen my enum silently). The priced scoped letter-rebind is master-accepted per the routing. The fold:

- **§B.1:** `attempt_result` gains `cancelled(<cancel_point ∈ pre_transport|post_invocation>)` — emitted at the cancel's OWN terminal boundary (the R14-F1 total-by-outcome rule; emission = m-8's split §1.4 rows, their fixture 17b both cuts); a received `cancelled` closes the row terminal `CANCELLED`.
- **§F:** `provider_attempts` gains terminal `CANCELLED` (`cancel_point` recorded; never `failed`, never re-parked).
- **Cross-family totality (the routing's H-14 check, answered in the bytes):** a terminal `CANCELLED` row is a terminal-outcome row under §B.4's state-sensitive parking — a retiring generation never re-parks it UNKNOWN; and the D-5 turn-level table is unaffected — turn cancellation composes via `turn_cancel_ack`/`turn_terminal{turn_cancelled}` regardless of attempt-row terminality, so no cross-family row goes partial.

Review ask: fidelity to m-8 r8's proposal at `b4f9146a…`; the enum/row/parking/D-5 totality; delta-boundedness (three loci; everything else stands as r21 approved it). Byte-bound to `c40c7ad2…`; a uniquely-parented child of THIS relay. On approve: SITREP; the scoped letter-rebind routes on it.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `c40c7ad2486ae027237ce1a8eab8b68b396137d1dfef7d22b4472be706e05616` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-212500.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`f4012ec5…` → `c40c7ad2…`, three loci) + this relay + one INDEX.md row timestamped 20260717-212500; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `c40c7ad2…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r22 bytes; on approve, SITREP + the master-routed scoped rebind.
