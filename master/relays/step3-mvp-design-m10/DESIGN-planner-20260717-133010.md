## DESIGN r13 — the R3-F1 owner fold (`rejected_local` fifth disposition + terminal `REJECTED_LOCAL` row state) + the two routed L8 cosmetic loci; fresh bytes @ SHA-256 `68c9890f…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded protocol-totality completion (the F73 ladder); no topology/policy/operator-locked choice touched
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m8/RECONCILE-orchestrator-planner-20260717-131153.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-7.planner, m-9.planner, m-3.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r13 bytes — doc @ SHA-256 `68c9890f26e23d46b04f55a176efceceeb0952d3bb1c744a3853846d06c2d5f5`; supersedes `111ab95a…`; delta = FOUR loci (two R3-F1: §B.1 disposition + §F row state; two L8: §B.4 "seven-field" + §B.5 header r8 hash); everything else byte-frozen

m-10.implementer — the R3-F1 disposition (my ACCEPT: `step3-mvp-design-m8/RECONCILE-planner-20260717-133000.md`, parallel) + the routed L8 batch, folded:

- **§B.1:** `attempt_result` disposition set gains `rejected_local(<m-8 reject_reason>)` — the honest terminal for a deterministic local pre-stream reject arriving AFTER `attempt_open` (the durable row exists; parking a deterministic reject UNKNOWN would be an accounting lie; UNKNOWN/PARTIAL stay reserved for genuine unknowns). Reason tokens m-8-owned, disjoint from m-3's (`denied` stays policy-only); no stream precedes, resolver never invoked, zero transport counters; a `rejected_local` result terminates the row `REJECTED_LOCAL`.
- **§F:** `provider_attempts` gains the terminal `REJECTED_LOCAL` state (R3-F1 annotation inline).
- **L8-a (§B.4 step 4):** "six-field" → "seven-field" (stale since the r12 `credential_ref` fold).
- **L8-b (§B.5 header):** the superseded m-7 r6 citation (`f072bd99…`) → their final r8 `ab0ed428…` (historical BASIS citations left byte-frozen per the r12 review's provenance rule).

Review ask: confirm the R3-F1 fold matches m-8's r4 proposal + my ACCEPT terms; that `REJECTED_LOCAL` cannot be confused with, or absorb, any UNKNOWN/PARTIAL semantics (the §B.3 park discipline untouched); that no m-3 token surface is crossed; and that the delta is exactly the four loci. Byte-bound to `68c9890f…`; a uniquely-parented child of THIS relay. On approve, my SITREP names the final hash; the re-affirm batch is master-routed.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `68c9890f26e23d46b04f55a176efceceeb0952d3bb1c744a3853846d06c2d5f5` (recompute before review).
- Basis: m-8 r4 @ `168c24b7…` (recomputed exact); the routing `…-131153`; m-7 r8 @ `ab0ed428…` (the refresh-leg basis).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-133010.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`111ab95a…` → `68c9890f…`, four loci) + this relay + one INDEX.md row timestamped 20260717-133010; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `68c9890f…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r13 bytes.
