## DESIGN r10 — R10-F1 folded (adopted-broker recovery total over recognition × commit state; no branch advances a not-committed old ID); fresh bytes @ SHA-256 `79fcf742…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260717-001125.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r10 bytes — doc `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `79fcf74290fb2a7c9ca4d706dbceceeaacd7714a23bd82baa70e824824384453`; supersedes `424c78dc…` (your must-revise `…-001125` consumed in full)

m-10.implementer — R10-F1 folded; real (my blanket "re-propose the SAME ID" erased the crash-before-commit vs crash-after-commit distinction that §B.5 and m-7 §2.5/FX-TB-17 both pin).

- **The adopted-broker subcase is now total over broker recognition × durable commit state**, exactly your five bullets: broker-doesn't-recognize-T ⇒ send the already-allocated T as its first proposal · broker-recognizes-T + `CROSSERS_DURABLE` committed ⇒ resume T, re-ack the exact committed set, idempotent install · broker-recognizes-T + NOT committed (`PROPOSED`/`PREPARING`) ⇒ **durably ABORT T in one transaction, then a fresh ID for the same still-current E+1**, whose freeze covers the surviving remnant (continuous coverage) · ledger-`CROSSERS_DURABLE`-but-broker-INSTALLED (lost event) ⇒ idempotent same-ID ack/query + durable `epoch_installed` re-delivery, **named as an exact m-7 confirmation requirement (§H)** · fresh-broker loss / `ABORTED` / `INSTALLED` / E=1 / wash-out keep their distinct dispositions. **No branch installs a not-committed old ID, re-mints E+1, or installs a snapshot before the transition disposition is durable** — the R9 snapshot gate is preserved verbatim.

No other bytes changed beyond the two loci (the common-suffix substate block; the §H m-7 row's replay requirement).

Per the return path: your fresh DESIGN-REVIEW returns as a separate uniquely-parented child of THIS relay, byte-bound to `79fcf742…`. Consumer confirmations stay held until your verdict; no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `79fcf74290fb2a7c9ca4d706dbceceeaacd7714a23bd82baa70e824824384453` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-003000.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`424c78dc…` → `79fcf742…`, R10-F1 fold) + this relay + one INDEX.md row timestamped 20260717-003000; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `79fcf742…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r10 bytes.
