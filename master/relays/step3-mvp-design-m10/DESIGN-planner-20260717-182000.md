## DESIGN r14 — R14-F1 folded (attempt_result emission is total by outcome; the pre-freeze `rejected_local` leg is now reachable); fresh bytes @ SHA-256 `a2663a79…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded timing correction inside the routed result seam; nothing operator-locked touched
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260717-181004.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-7.planner, m-9.planner, m-3.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r14 bytes — doc @ SHA-256 `a2663a7964fb23e5c92eeb3b3ecf530b80c1b3108813a398941724fd6a25c5b7`; supersedes `68c9890f…` (your must-revise `…-181004` consumed in full); delta = the ONE §B.1 emission-timing sentence

m-10.implementer — R14-F1 folded; real (the inherited "at the authorize/send boundary" sentence made the new disposition unreachable — all three m-8 local rejects fire pre-freeze, so a literal build would emit nothing and the row would fall to absence⇒UNKNOWN, exactly the lie the fold was meant to kill).

- **The fix, your exact required-return:** §B.1 now pins `attempt_result` emission **at the outcome's OWN terminal boundary, total by outcome, never universally authorize/send-bound** — `rejected_local` immediately on the deterministic pre-freeze validation/translation failure (before the typed DATA-P return completes), `attempt_id`/`turn_epoch` carried from the accepted request envelope; `denied` stays the Authorize leg; `sent_completed`/`transport_failed`/`unknown` stay at their send/stream terminal. The normal-path exception is explicit: a received `rejected_local` closes the row terminal on its own with **no m-9 `attempt_stream_end` expected**; only a genuinely missing/conflicting result or a crash window parks UNKNOWN per §B.3.
- Delta scope: the one §B.1 sentence (its clause expanded); §F/`REJECTED_LOCAL`, the L8 loci, and everything else stand as r13 reviewed them.

Byte-bound to `a2663a79…`; a uniquely-parented child of THIS relay. On approve, my SITREP names the final hash; the re-affirm batch stays master-routed.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `a2663a7964fb23e5c92eeb3b3ecf530b80c1b3108813a398941724fd6a25c5b7` (recompute before review).
- Basis: your finding's m-8 r4 citations (`:46-47,76-80,127` — the three pre-freeze reject points) accepted as read; m-8 r4 @ `168c24b7…` unchanged.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-182000.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`68c9890f…` → `a2663a79…`, one locus) + this relay + one INDEX.md row timestamped 20260717-182000; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `a2663a79…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r14 bytes.
