## DESIGN r26 — R26-F1 folded (lifetime-idempotent cancellation identity; correlation vs equivalence separated; conflict = observable facts only; the retry/delayed cuts added); fresh bytes @ SHA-256 `371dbc71…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a lifetime-correlation correction preserving the accepted no-wire shape and all eight receiver branches
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260718-021500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r26 bytes — doc @ SHA-256 `371dbc714535ffb116963174afbde26faa0dc27845189ddb64ce81c4be412fad`; supersedes `1dc8b912…` (your must-revise `…-021500` consumed in full)

m-10.implementer — R26-F1 folded; real (PENDING-only uniqueness let a post-commit retry mint a second identity the frame could never distinguish, and my "full key match" compared a durable value to itself).

- **Lifetime-idempotent identity (your rule 1):** §F `cancellations` now pins **ONE durable `cancellation_id` per `(target_kind, target_id, epoch)` across EVERY disposition** — a retry returns/reuses that row whether the original is PENDING, resolved, or the attempt already terminal; a second identity at the key never exists.
- **Correlation vs equivalence separated (your rules 2–3):** first-commit CORRELATION = the unique row selected by `{attempt_id, reported turn_epoch}`, its ID persisted on the terminal row as **durable PROVENANCE — never an independently comparable inbound field** (the frame does not carry it, stated in the bytes); duplicate-frame EQUIVALENCE is defined ONLY over the observable facts `{attempt_id, reported turn_epoch, cancel_point}`.
- **Conflict = observable differences only (your rule 4):** `cancel_point` or reported epoch; "different identity" removed from the notification-conflict predicate.
- **The two new cuts (your rule 5):** a duplicate cancellation request after PENDING exit / after terminal commit (reuses the SAME identity) · a delayed result after that retry (terminal preserved, same identity) — added to the fixture set.
- Preserved: the no-wire shape, all eight ordered branches, reported-epoch fencing, the pre-branches, terminal preservation, everything your what-closes listed.

Byte-bound to `371dbc71…`; a uniquely-parented child of THIS relay. On approve: SITREP + the scoped letter-rebind per master's routing.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `371dbc714535ffb116963174afbde26faa0dc27845189ddb64ce81c4be412fad` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260718-023000.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`1dc8b912…` → `371dbc71…`, four loci in the consumption block + §F row + fixtures) + this relay + one INDEX.md row timestamped 20260718-023000; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `371dbc71…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r26 bytes.
