## DESIGN r28 — the batched m-9-seam return: item (1) ACCEPTED + folded (equivalence = `{terminal}`, H-14-verified total) · items (2)/(3) CONFIRMED · the m-3 r4 letter rebind @ `009df607…` batched in; fresh bytes @ SHA-256 `4ffaa9ec…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded owner disposition + comparator confirmations inside ratified ownership
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/RECONCILE-orchestrator-planner-20260718-051544.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r28 bytes — doc @ SHA-256 `4ffaa9ecb82a9e7e7b4b0e9ca82cf19de91247ed11e2672a55707692465fc406`; supersedes `db199b0d…`; this relay is ALSO the disposition return to master (CC'd) per the `051544` batch — items verbatim, one revision, one review

m-10.implementer — the `051544` batch, dispositioned and folded as one r28 (master CC'd — this relay is the return):

1. **Item (1) — ACCEPT, folded:** `turn_terminal` is consumed as m-9's r6 shape `{run_id, turn_id, turn_epoch, terminal}` (the drop noted with its rationale in the bytes), and the D-5 equivalence predicate narrows to **`{terminal}` alone**. **The H-14 addendum verified at the fold and stated in the bytes:** the table stays TOTAL under the narrower key — no legality or conflict predicate ever consumed `attempts_summary_ref?` (equivalent = same `terminal`; conflict = different `terminal`; the cross-family rows key on terminal VALUES) — so nothing under-distinguishes; no finding to return.
2. **Item (2) — CONFIRM, pinned in the same bytes:** my `turn_cancel_ack` comparator consumes exactly the closed turn-OUTPUT domain **`{none, partials_committed_labeled}`**; the tool-effect axis stays on `tool_calls` + the D-4 disclosure; `cancel_point` stays a separate attempt-row provenance fact — never folded into the comparator.
3. **Item (3) — CONFIRM (no bytes needed):** m-9's two-view / count-once / bare-closure⇒UNKNOWN / loss-≠-cancel consumption composes with my §B.1 as-is; the worker neither carries nor compares `cancellation_id`, exactly my R26 observable-facts rule.
4. **The m-3 r4 letter rebind, batched per the anti-churn rule:** m-3's bytes recomputed this session @ **`009df6070c35f6c2…`** (exact match to the routed hash) — my m-3 leg (policy_digest production semantics · freeze-time pinned_lane equality · the E0/attempt substrate · the resolved turn_epoch encoding) **re-affirms by this letter at their r4**, consumer basis = my r28 `4ffaa9ec…`. The remaining sweep (m-7 leg-2 · m-3's re-void self-rule · m-8's basis letter-cite) routes on master's direction at my final hash.

Delta scope: two loci (the §B.2 D-5 frame shape + the equality-predicate sentence). Byte-bound to `4ffaa9ec…`; a uniquely-parented child of THIS relay. On approve: the closure machinery (m-9's SITREP → my reciprocal → the stage-3 close) proceeds per master's sequencing.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `4ffaa9ecb82a9e7e7b4b0e9ca82cf19de91247ed11e2672a55707692465fc406`; m-3 r4 recomputed `009df607…` (exact); m-9 r6 @ `1611009c…` per the routing.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260718-053000.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`db199b0d…` → `4ffaa9ec…`, two loci) + this relay + one INDEX.md row timestamped 20260718-053000; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `4ffaa9ec…` and returns a fresh uniquely-parented DESIGN-REVIEW; master routes the sweep + the m-9 closure machinery on the approve.
