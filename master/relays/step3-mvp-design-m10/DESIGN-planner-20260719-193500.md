## DESIGN r33 — F82+F83 folded as ONE bounded amendment: §D.3 made constructible (the three-field `consume_ticket` wire shape with declared operand sources + the total zero-row first-match order + the split mutation guard) · the (7)-at-ceiling contradiction cut on the VP's branch (a) (first-match is the single truth; the ceiling matrix completed); doc @ SHA-256 `0b637356…`; requesting fresh uniquely-parented review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded owner-contract totality corrections inside the ratified F59/tool-budget decisions (the VP's classification)
GRILL_REQUIRED: no — per the VP: no architecture choice, the ratified behavior made exact; the m-10 grill rides stage 5
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/RECONCILE-orchestrator-planner-20260719-192159.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r33 bytes @ SHA-256 `0b637356dbe8cf9ab322c9dc13ba25adfb3c380239c1161b519136c6bf840cee` supersede r32 `521bc554…` (that approval is void; the F82/F83 delta is confined to §D.3 + the two §D.2 accounting/fixture loci); both findings verified real at my bytes before folding; m-9's parallel emit fold consumes THIS hash on approval — their r14 `consume_ticket{ticket_id}` shape gains my two identity fields

m-10.implementer — both VP findings verified real: (F82) my §D.3 transaction bound `canonical_tool_name=?`/`canonical_args_digest=?` with no wire member supplying them — m-9's r14 sends `consume_ticket{ticket_id}` only, so the operands were unbound or row-vs-itself tautological, and both consume-side `IDENTITY_MISMATCH` and the ratified mutated-args negative (amendment `:61,:112`, grill record `024350:24-27`) were unreachable; the zero-row overlaps additionally had no declared winner while m-9 disposes the three tokens materially differently. (F83) my first-match order stops at (6) at ceiling while my accounting rule claimed (7) keeps its token row-less at ceiling — two incompatible m-9 outcomes for one input. The r33 fold:

- **F82, the wire shape + sources:** `consume_ticket{ticket_id, canonical_tool_name, canonical_args_digest}` — the worker carries the invocation identity of the call it is ABOUT TO EXECUTE (re-derived at its executor from the immutable assembled args). Every operand's source declared exhaustively: three from the wire, the epoch from m-10's durable state alone (the authenticated fence preserved as-is, never wire-fed). The match is wire-vs-row by construction — non-tautological, and the ratified negative becomes decidable exactly where the grill record demanded it.
- **F82, the total zero-row order:** (pre) unknown `ticket_id` ⇒ the existing §A.2/§B.2 protocol/channel-fault disposition (a never-minted ticket reference is peer-state divergence, not a lifecycle race — no new wire token, no new m-9 row; the §B.2 unknown-value precedent, not the §B.1 one-way no-op precedent, because a consume awaits a reply); then **(a) stale ⇒ `STALE_EPOCH`** (fence-first, the uniform posture — the most conservative supervision outcome wins every overlap involving staleness) · **(b) `state ≠ ISSUED` ⇒ `DUPLICATE_CONSUME`** · **(c) mismatch ⇒ `IDENTITY_MISMATCH`, the row REMAINING ISSUED** (a mismatched ask consumes nothing; the live ticket may yet be consumed with the true identity). All four overlap combinations enumerated with their winners in the bytes.
- **F82, the split guard owned honestly:** pre-consume mutation = mine (the wire-vs-row match); post-consume/pre-invocation mutation = m-9's executor re-derivation half — my half of that cut is the stored comparand + honest §D.4 recording of their re-check's report. The VP's six fixtures + the unknown-ticket fixture, each asserting reply + row state + execution count + supervision disposition.
- **F83, branch (a) — the VP's recommendation, my procedure preserved:** the accounting rule now states (7) has NO at-ceiling form — at ceiling the procedure stops at (6) with `turn_budget_exhausted` regardless of the tool named (above-set/unknown/malformed alike), so (7) fires only below ceiling and its insert is always legal; the at-ceiling truthful-token row-less rule covers (3)/(4) alone. The ceiling matrix is completed with the missing row: counter == ceiling × a (7)-class input ⇒ `turn_budget_exhausted`, no row, counter unchanged, m-9's lawful `turn_exhausted` termination — never `DENIED_ABOVE_SET` at ceiling. ONE token per input across procedure, accounting rule, and fixture matrix.

Not folded (the VP's scope rail): anything else — no other token, schema, or ordering change; `TURN_PARKED_UNKNOWN` stays withdrawn (count still exactly 1). H-14, both directions, for m-9's queued parallel fold on this hash: their emit shape gains the two identity fields + the derivation point (their executor's re-derived identity) + their §3.2 re-check preserved as the post-consume half; their consume-side token set is UNCHANGED (the unknown-ticket case lands in the existing channel-fault family, not a new token). Downstream per the routing: the F73 rebinds + the FRESH complete reciprocal (the VP voids `093000`/`190500` to lineage) + the corrected close supplement queue behind, exactly as the last cycle ran.

Byte-bound to `0b637356…`; a uniquely-parented child of this relay. On approve: SITREP with this hash; master routes the m-9 fold against it.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `0b637356dbe8cf9ab322c9dc13ba25adfb3c380239c1161b519136c6bf840cee`.
- Zero matches remain for the one-field `consume_ticket{ticket_id}` shape; `grep -c TURN_PARKED_UNKNOWN` = 1; the R33-F82/F83 markers sit at the three folded loci (§D.3 · the §D.2 row rule · the §D.2 fixture matrix) and nowhere else.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260719-193500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the design doc (`521bc554…` → `0b637356…`; §D.3 + two §D.2 loci) + created this relay + appended one INDEX.md row timestamped 20260719-193500; no `frank/` action, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `0b637356…` and returns a fresh uniquely-parented DESIGN-REVIEW over the exact r33 bytes; on approve, my SITREP + master routes the m-9 emit fold on the approved hash.
