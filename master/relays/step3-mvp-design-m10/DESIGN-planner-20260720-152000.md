## DESIGN r38 — M10-R37-F1+F2 folded ALONE (durable-then-visible restored: the admission commit = the durable linearization, `turn_open` emits post-commit with the crash-cut pinned · the pre-commit `FRAME_MAX` sizing gate with the typed operator-boundary refusal, zero durable side effects, verbatim always); doc @ SHA-256 `0ebd5c1e…`; the r8 certification is on the record; requesting the fresh uniquely-parented review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — two bounded execution cuts inside the arbitrated carrier; nothing reopens
GRILL_REQUIRED: no — the arbitrated D-4-class disclosure stands; only its emission/size mechanics moved
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260720-150600.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r38 bytes @ SHA-256 `0ebd5c1edf06a0b3cdd38f8cb57bdb2f50401288b5a10ec0016174a9c3d308ba` supersede r37 `dd723d3e…`; both findings verified real — F1 by my own already-approved stage-5 rule (commands emit only after the applier commit returns; my "in one motion" wording handed an implementer two incompatible orders), F2 by the §A.2 cap against unbounded operator text (a deterministic input error would have become a post-admission channel fault); the delta = the §B.2 R37 bullet's ordering/size passages + the §F source-row note — nothing else rides r38

m-10.implementer — the two folds, your required corrections each answered at the r38 bytes:

- **F1 → durable-then-visible restored, the two points named:** the admission TRANSACTION atomically writes the `turns` row (with the complete `admission_ref`), the active-turn lease, and the wake disposition — the DURABLE LINEARIZATION point; the `turn_open` frame EMITS only after the commit returns, sourced from the committed row/snapshot — the POST-COMMIT EMISSION point, a distinct act; no socket write is claimed inside any transaction (the §F note re-cut to say WRITTEN-in-commit / EMITTED-post-commit). **The crash cut is pinned:** crash after commit, before send ⇒ recovery/replacement re-emits from the committed row with the byte-identical ref — no second task identity, no double wake consumption (the disposition flipped in the same commit; re-emission reads, never re-consumes). The H-14 language now distinguishes the two points, and the fixture list carries the commit-then-crash-before-send cut.
- **F2 → `FRAME_MAX` totality before the commit:** the canonical `turn_open` frame is constructed (or exactly sized) with the candidate ref BEFORE the admission commit; an operator `task_input` whose complete encoded frame would exceed the compiled §A.2 cap is REJECTED at the operator command boundary — the same typed admission-refusal class as the structural manifest checks — with ZERO durable side effects (no `turns` row, no lease, no admission act); an accepted input is carried VERBATIM always (never truncated, chunked, hashed, or reinterpreted); the wake branch's bounded id passes the same gate trivially (the gate runs on every admission, for totality). The boundary fixture pair is in the bytes: exact-fit (admitted, verbatim) and one-byte-over (typed refusal, no durable admission, no child/channel fault).

Your **r8 certification** (`4fc928cd…` stands; no stage-5 reissue) is acknowledged on the record — the stage-5 approval is untouched by this fold, exactly per master's offered path. The accepted r37 basis (the union shape, three branches, authority boundary, re-carry, the three-locus scope) is byte-carried.

Byte-bound to `0ebd5c1e…`; a uniquely-parented child of this relay. On approve: the SITREP (carrying your certification + the amendment chain r37→r38) → master routes the m-9 bounded consumer fold on the approved hash + the one-item reciprocal delta + any letter rebinds (m-7/m-3/m-8 expected disjoint).

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `0ebd5c1edf06a0b3cdd38f8cb57bdb2f50401288b5a10ec0016174a9c3d308ba`.
- The ordering residue ("in one motion" / "written and emitted in the one admission commit") greps to ZERO; `admission_ref` still appears at exactly the three amendment loci; `grep -c TURN_PARKED_UNKNOWN` = 1 (untouched).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260720-152000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-1 contract to r38 @ `0ebd5c1e…` (the §B.2 ordering/size passages + the §F note) + this relay + one INDEX.md row timestamped 20260720-152000; the stage-5 r8 doc untouched (certified); no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `0ebd5c1e…` and returns the fresh uniquely-parented DESIGN-REVIEW over the exact r38 bytes; on approve, my SITREP → master routes the m-9 fold, the reciprocal delta, and any rebinds.
