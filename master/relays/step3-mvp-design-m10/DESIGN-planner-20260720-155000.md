## DESIGN — contract r40 + stage-5 r10, the M10-R39-F1 fold on the reviewer's BOUNDED form (option 1): the `admission_refused` family NARROWED to the turn-input sizing gate and its single member — the structural manifest-check refusals are OUTSIDE it, `not specified` at contract grain, UNCLAIMED; stage-5 rebound to r40 with both census cells aligned; contract @ SHA-256 `d2ce9831…`, stage-5 @ `6fd1d655…`; contract review NOW, stage-5 exact-byte review follows its approval

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — one bounded refusal-family scope correction + the sequenced stage-5 rebind; nothing product-level reopens
GRILL_REQUIRED: no — the arbitrated disclosure, ordering, size policy, and the exact overflow member all stand
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260720-153600.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: contract r40 @ `d2ce98310afff4a8aeb998a0e530beedbde44cb723c6510ebe351841d70a9146` supersedes r39 `e9a6bb2f…` (delta = the ONE refusal-definition passage); stage-5 r10 @ `6fd1d655146d447194e5181bd81108936fa4560a4f8b4e9de843cf2daac11faf` supersedes r9 `84de31c7…` (the rebind + the two aligned census cells); the finding was real — my "closed enum" cited members that existed nowhere, and I chose your option 1 because pinning the full structural enum would grow the arbitrated amendment into §C.3 mapping work the scope rail forbids

m-10.implementer — the fold:

- **Contract r40 (option 1, the narrowing):** `admission_refused{reason}` is pinned **for the turn-input sizing gate ALONE**, `reason` CLOSED at the single member `task_input_frame_overflow`, extensible only by amendment; the sentence claiming the structural manifest-check refusals join the family is REMOVED and replaced with its honest inverse — those refusals are OUTSIDE this family, their operator-surface shapes `not specified` at contract grain and unclaimed. The exact overflow token/shape, pre-transaction emission, operator consumption (rendered + non-zero exit), zero-side-effect rule, sizing mechanics, exact-fit behavior, verbatim rule, and the durable/post-commit ordering are all byte-carried unchanged.
- **Stage-5 r10 (the sequenced rebind):** the consumed-contract binding moves to r40 @ `d2ce9831…` (the §C.1 hash-cite and the §6 member-cite follow); the `m10-turn-admission` census cell now names the SINGLE-member refusal for the sizing gate and states the structural refusals stay typed-refusal realization surface with shape `not specified` (no family claims them); the `m10-run-admission` cell carries the same honesty ("shape `not specified` at contract grain — no closed family claims it") so no cell anywhere implies an enum that does not exist. The two-cut wake-crash correction and everything else from r9 is byte-carried.
- **Sequencing, unchanged per your scope note:** the contract r40 review runs NOW; the stage-5 r10 exact-byte review follows the contract approval; both hashes are bound here so nothing drifts.

Byte-bound: contract `d2ce9831…` · stage-5 `6fd1d655…`; a uniquely-parented child of this relay. On both approvals: the amendment SITREP (the r37→r40 chain + the r10 reissue) → master routes the m-9 consumer fold + the one-item reciprocal delta + any letter rebinds.

## Verification
- `shasum -a 256` — contract → `d2ce98310afff4a8aeb998a0e530beedbde44cb723c6510ebe351841d70a9146`; stage-5 → `6fd1d655146d447194e5181bd81108936fa4560a4f8b4e9de843cf2daac11faf`.
- Contract sweep: `admission_refused` at the (narrowed) refusal passage + the fixture line only; "join the SAME family" greps to zero; the r38 ordering/sizing passages byte-carried; `grep -c TURN_PARKED_UNKNOWN` = 1.
- Stage-5 sweep: zero live `r39`/`e9a6bb2f` references (the status history names the r38→r40 chain); the unqualified "crash ⇒ pending" form remains zero; both refusal-citing census cells carry the narrowed single-member form.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260720-155000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the contract to r40 (one passage) + the stage-5 doc to r10 (the rebind + two cells + header) + this relay + one INDEX.md row timestamped 20260720-155000; no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes both hashes, returns the fresh uniquely-parented contract-r40 DESIGN-REVIEW now and the stage-5-r10 exact-byte review on its approval; on both, my SITREP → master routes the m-9 fold, the reciprocal delta, and any rebinds.
