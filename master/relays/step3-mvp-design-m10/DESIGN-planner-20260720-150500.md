## DESIGN r37 — the master-arbitrated `admission_ref` amendment folded ALONE (the D-4-class state-only disclosure on `turn_open`: the closed two-kind ref — wake `relay_id` · verbatim operator `task_input` — never absent, replacement re-carries byte-identically; the §F `turns` row named as the emission source); doc @ SHA-256 `dd723d3e…`; requesting the fresh uniquely-parented review + the stage-5 r8 shape-genericity certification in the same pass

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — the operator concurred with the arbitration in-session (`145111` records it); the operator gates at the stage-6 lock
GRILL_REQUIRED: no — a bounded totality/disclosure completion in the D-4/check-1/outcome-record class; no new architecture choice
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/RECONCILE-orchestrator-planner-20260720-145111.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r37 bytes @ SHA-256 `dd723d3e49919850a8183b1e20d94833bcb492d696ff5f97b7d658b1949ff09c` supersede r36 `0240e874…` (that approval is void; the fourth F73 run of the cycle, priced and accepted by the arbitration); the delta = the §B.2 D-4 shape line + ONE new §B.2 bullet + the §F `turns` row note — nothing else rides r37 per the scope rail; the gap was real at my own bytes (the binding stored at :278, never on the wire; the worker's only legal surface is a frame)

m-10.implementer — the fold, the arbitration's requirements each answered:

- **The member, three branches covered:** `turn_open{run_id, turn_id, turn_epoch, admission_ref, parked_unknown}` — `admission_ref` REQUIRED and NEVER absent (every `turns` row has the column by construction, so no empty form exists to pin), carrying the SAME value the admission commit writes durably. **The closed two-kind shape:** `{kind: wake_relay, relay_id}` — the worker obtains the task content through its OWN seat `read` (the legal surface; an id, not a path; I-PH intact) · `{kind: operator_input, task_input}` — the operator's task text VERBATIM as durably stored; **the CTRL-W frame itself is the named legal surface** (app-originated operator-authored content — not provider payload, §G.3 untouched; not secret, the m-1 census; the `app_event` precedent for content-bearing CTRL-W frames). **The replacement branch re-carries the same ref byte-identically** (re-read from the durable row at re-admission — task identity stable across generations by construction, the arbitrated property).
- **The discipline:** the D-4 pattern verbatim — state-only, attached from durable state in the one admission commit, no authority transfer (the ref LOCATES; the wake read runs through the worker's own capability). H-14 both directions: emission = the admission commit; consumption = m-9's queued r5 consumer fold on this hash's approval (their §2.6/census row — their stage-4 realizes against the amended frame in the same motion, per the escalation).
- **Census + fixtures, in the bytes:** the wake branch (ref == the consumed `wake_schedule` row's `relay_id`) · the operator-input branch (verbatim-stable) · the replacement re-carry (byte-identical across generations) · never-absent on every `turn_open`.
- **The stage-5 r8 disposition (the arbitration's item 3, my assessment at the bytes — YOUR certification requested in this same review):** my r8's `turn_open` citations are SHAPE-GENERIC — §6's one-active-turn bullet says the D-4 member is attached in the admission transaction (true and unchanged; the same transaction now also attaches `admission_ref`, an addition not a contradiction); the `m10-turn-admission` census row cites "the `turn_open` command emitted post-commit" with no member enumeration; the non-effect rationale routes `turn_open` to its owning row; no r8 sentence claims a CLOSED member list for the frame. My disposition: **certification, not re-issue** — the r8 approval stands, with the realization note that the admission transaction's snapshot now also carries the ref (a build-time detail inside the already-approved one-transaction shape). If you refute, the r8 batch-fold runs instead; say which in your verdict.

Byte-bound to `dd723d3e…`; a uniquely-parented child of this relay. On approve: SITREP (incl. your r8 certification); master routes the m-9 bounded consumer fold + the one-item reciprocal delta + any letter rebinds (m-7/m-3/m-8 expected disjoint — one member on one CTRL-W frame).

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `dd723d3e49919850a8183b1e20d94833bcb492d696ff5f97b7d658b1949ff09c`.
- `grep -c admission_ref` = 3 (the shape line · the R37 bullet · the §F emission-source note) — exactly the arbitrated delta; `grep -c TURN_PARKED_UNKNOWN` = 1 (the standing withdrawal sentence, untouched).
- The stage-5 r8 citation sweep run this session (§6:71 · census :120 · rationale :147 · fixture :166): no closed-shape claim found.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260720-150500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-1 contract to r37 @ `dd723d3e…` (the three admission_ref loci) + this relay + one INDEX.md row timestamped 20260720-150500; the stage-5 r8 doc untouched pending your certification; no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `dd723d3e…` and returns the fresh uniquely-parented DESIGN-REVIEW over the exact r37 bytes + the r8 shape-genericity certification (or refutation); on approve, my SITREP → master routes the m-9 fold, the reciprocal delta, and any rebinds.
