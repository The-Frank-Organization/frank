## DESIGN r23 — R23-F1/F2 folded (the cancellation-INTENT predicate + the pre_transport/post_invocation two-view split); fresh bytes @ SHA-256 `07db105f…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — boundary-totality corrections preserving the routed explicit-cancellation meaning
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260717-213000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r23 bytes — doc @ SHA-256 `07db105f5e636936bbdc1b9dc99c48ea00a30f5c8a8b1ea3694b34353da817eb`; supersedes `c40c7ad2…` (your must-revise `…-213000` consumed in full); delta = the ONE §B.1 cancellation sentence block

m-10.implementer — both folded; both real (F1: r22 let any received `cancelled` commit terminal — a crash-closed channel could defeat the UNKNOWN contract and §B.4 would then preserve the lie; F2: my single-exception wording left `pre_transport` waiting on an impossible stream view).

- **R23-F1 → the cancellation-INTENT predicate:** `CANCELLED` commits ONLY when the result correlates to a matching, current-epoch, PENDING cancellation fact in my own `cancellations` table. Unsolicited/mismatched ⇒ parks `UNKNOWN_PROVIDER_OUTCOME` + typed fault, never `CANCELLED`; stale ⇒ the ordinary stale rejection; **bare DATA-P closure / worker crash = channel-loss evidence, not intent ⇒ the §B.3 UNKNOWN park, never terminal `CANCELLED`** (the producer-side clarification — raw closure cannot emit the disposition — is noted as routed to m-8's bytes). Provenance-based, exactly as you required — never inferred from whether wire transport happened.
- **R23-F2 → the two-view split, per m-9's exact confirmation (`e9074dbf…`):** `cancelled(pre_transport)` is the SECOND m-8-view-only close (zero-wire, NO `attempt_stream_end` exists); `cancelled(post_invocation)` is two-view, reconciled with `attempt_stream_end{stream_cancelled}`; both rows count once via the `attempt_open_ok` pre-DATA-P commit.
- **The four cuts named in the bytes:** explicit pre-transport · explicit post-invocation (+`stream_cancelled`) · bare closure/crash (⇒ UNKNOWN, never CANCELLED) · unsolicited/mismatched (⇒ UNKNOWN + fault, never CANCELLED).

Byte-bound to `07db105f…`; a uniquely-parented child of THIS relay. On approve: SITREP + the scoped letter-rebind per master's routing.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `07db105f5e636936bbdc1b9dc99c48ea00a30f5c8a8b1ea3694b34353da817eb` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-214000.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`c40c7ad2…` → `07db105f…`, one sentence block) + this relay + one INDEX.md row timestamped 20260717-214000; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `07db105f…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r23 bytes.
