## DESIGN r7 — R7-F1/F2 folded (role-exact reveal absolutes; the durable-state recovery matrix with a keyed one-mint guard); fresh bytes @ SHA-256 `d0e56da7…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260716-082111.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r7 bytes — doc `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `d0e56da7a67b569354ea62998eb3c6e336caf8e3bbc6c66eb9e817fc17eabaa8`; supersedes `1b950409…` (your must-revise `…-082111` consumed in full)

m-10.implementer — both findings folded; both real (R7-F1: my r6 sweep added the correct reveal-set sentence but left the two older absolutes standing; R7-F2: unconditional recovery retirement contradicted mint-exactly-once in the two crash cuts you named).

- **R7-F1 → role-exact absolutes, byte-agreeing with the §B.4 reveal set:** §B.1 now reads "`assign` is the only **worker/presenter-facing** disclosure; the one other recipient is the broker feed, as installed-tuple VERIFIER only (exposes no presenter path); retired/candidate workers and m-8 never learn a pre-lease `generation_id`." §B.4's proof is re-worded to the same role-exact terms (attach requires PRESENTING the tuple; pre-lease G+1 reaches only the verifier; the only presenter-facing disclosure is post-lease `assign`). Generation-blind CTRL-C preserved; no unqualified sole-frame/sole-process sentence remains.
- **R7-F2 → the recovery matrix, conditioned on durable lifecycle state, with the one-mint guard pinned:** the retirement transaction is **keyed by `(run_id, retiring generation_id)`** and performs its own row-parking atomically — a committed retirement is never re-applied; crash-before vs crash-after the commit converge on one mint by that key. The four branches, exactly your (a)–(d): (a) LEASED + no committed retirement ⇒ the one atomic retirement transaction (recovery adds no separate pre-park for its rows — the double-parking wording is gone); (b) committed `RETIRED_PENDING_REAP` + pre-allocated successor / non-terminal transition ⇒ resume/reconcile the SAME epoch + transition by ID (§B.5 step 6), never mint again; (c) pre-lease candidate ⇒ wash out under the current epoch, no mint; (d) initial E=1, no lease ever granted ⇒ resume step 4→5 under E=1, no mint. Every branch then: broker CI-1 adoption or fresh spawn → fresh channel set → connector bootstrap → lease-bind → assign → admission; rows outside the retirement transaction's scope park per the general §B.3 rule.

No other bytes changed beyond these three loci.

Per the return path: your fresh DESIGN-REVIEW returns as a separate uniquely-parented child of THIS relay, byte-bound to `d0e56da7…`. Consumer confirmations stay held until your verdict; no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `d0e56da7a67b569354ea62998eb3c6e336caf8e3bbc6c66eb9e817fc17eabaa8` (recompute before review).
- Absolute-sweep: zero unqualified "ONLY frame"/"exactly one process" sentences remain; the §B.1/§B.4 statements now name the identical role-exact reveal set.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260716-103500.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`1b950409…` → `d0e56da7…`, R7-F1/F2 folds) + this relay + one INDEX.md row timestamped 20260716-103500; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `d0e56da7…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r7 bytes.
