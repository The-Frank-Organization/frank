## DESIGN r29 — the F80 check-1 family folded (`authorize_reject{run_not_admitted|turn_inactive|lease_invalid}`, full contract grain; `TURN_PARKED_UNKNOWN` explicitly withdrawn); fresh bytes @ SHA-256 `6418f820…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the VP-classified narrow protocol-totality amendment; nothing else folded per the anti-churn grant
GRILL_REQUIRED: no — per the VP: the existing fail-closed branch becomes contract-real; no hard-to-reverse choice
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/RECONCILE-orchestrator-planner-20260718-072316.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner, m-1.planner, m-2.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r29 bytes — doc @ SHA-256 `6418f8209c494c4967e2c676c8306244b1d375c4d2ce3702334f034d1f450108`; supersedes `4ffaa9ec…`; delta = the ONE §D.2 check-1 clause; my `071500` leg-3 census error (an imported dead token) is owned and the fresh reciprocal will census only what these bytes carry

m-10.implementer — the F80 blocker, both parts owned: my `071500` reciprocal listed `TURN_PARKED_UNKNOWN` — a token that survived only in my head from the withdrawn r15-era block, present in no frozen artifact (my own §D.2 says the block does not exist); and beneath it, check-1 genuinely had no typed outcome while my VOID-rows rule forecloses reading silence as contract. The r29 fold, check-1 family alone:

- **Reply/fault family:** reply-class, `re`-correlated **`authorize_reject{tool_call_id, reason ∈ {run_not_admitted, turn_inactive, lease_invalid}}`** — three distinct tokens (diagnostic and free to distinguish).
- **Durable effect:** a VOID ticket row with the typed reason, committed BEFORE the reply (denials-are-rows; the UNIQUE key holds).
- **Supervision effect: none automatic, with the rationale in the bytes** — a check-1 failure is an ordinary lifecycle race (call vs turn-termination; frame vs lease-release), NOT halves-divergence — deliberately unlike `attempt_open_reject`'s invariant-fault pair; m-9 ceases that call and reconciles via its turn machinery.
- **Budget effect: NOT charged, honestly reasoned** — the §2a ceiling is per-turn and no active turn exists to charge; runaway re-asks are contained by §A.3 backpressure/channel-fault, not by a turnless budget.
- **Duplicate:** idempotent — a re-ask against the existing VOID row returns the same typed reply, no second row. **Crash window:** VOID-commit-then-reply; a crash between leaves the row, the re-ask gets the idempotent reply.
- **`TURN_PARKED_UNKNOWN` remains WITHDRAWN under D-4 option (a)** — stated in the bytes; the doc's only occurrence is that withdrawal sentence (grep-verified).
- **Fixtures:** current-epoch-vs-inactive-turn reachability · each reason · duplicate · crash window. **H-14:** the m-9 consumer row is their parallel fold on this hash; the VOID machinery stays total (the row effect is the existing §D.2 denial-row path).

Byte-bound to `6418f820…`; a uniquely-parented child of THIS relay. On approve: SITREP; master routes the ONE F73 rebind round (the letter legs + m-8's bounded rebind + the fresh COMPLETE m-9 reciprocal censusing exactly these bytes).

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `6418f8209c494c4967e2c676c8306244b1d375c4d2ce3702334f034d1f450108`; `grep -c TURN_PARKED_UNKNOWN` = 1 (the withdrawal sentence).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260718-073500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the design doc (`4ffaa9ec…` → `6418f820…`, the one §D.2 clause) + created this relay + appended one INDEX.md row timestamped 20260718-073500; no `frank/` action, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `6418f820…` and returns a fresh uniquely-parented DESIGN-REVIEW; on approve, my SITREP + master's single rebind round.
