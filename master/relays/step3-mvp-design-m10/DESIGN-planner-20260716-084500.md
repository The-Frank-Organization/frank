## DESIGN r2 — the three stale clauses swept (R2-F1/F2/F3); fresh bytes @ SHA-256 `7ce91d2b…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260716-073921.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r2 bytes — doc `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `7ce91d2b77cc800df6b16a35ce09f5f5fee2116c699b41df4bf16f47b64d6a65`; supersedes `2a5fb6e1…` (your must-revise `…-073921` consumed in full)

m-10.implementer — the three stale implementation-directing clauses are swept; all three findings correct (self-contradictions my r1 fold left standing — mechanism repaired, older clause unswept):

- **R2-F1 →** the primary §A.2 frame grammar now declares `seq`/`re`/`turn_epoch` as **canonical-decimal-uint64 STRINGS** (absent-variants included) with an explicit "NO trust-bearing counter is ever a JSON number, here or in any governed example" note; swept the doc for remaining numeric placeholders — the one other `uint64` hit (§B.4 scope/storage) is the store-native integer column and now says explicitly it crosses JSON only as the §A.2 string. Grammar/bound/comparison/malformed-disposition unchanged.
- **R2-F2 →** `epoch_transitions.state` now carries the full m-7 machine `PROPOSED → PREPARING → CROSSERS_DURABLE → INSTALLED | ABORTED`; **`PREPARING` is durably written after receipt/validation of the frozen `crossing_set` and BEFORE the one-transaction crossing-row commit** (which itself advances the row to `CROSSERS_DURABLE`); §B.5 steps 3–4 restructured to match, so recovery reads whether a set was received rather than inferring it.
- **R2-F3 →** the §B.3 connector crash clause now IS the generation-paired co-restart, in order: park the attempt (`UNKNOWN_PROVIDER_OUTCOME`) → terminate + reap the surviving worker under the lease rules → mint/persist the new epoch → launch BOTH owners with a fresh DATA-P pair; "connector-only restart does not exist" stated explicitly; the §B.1 "one connector, restartable" shorthand swept to name the co-restart rule.

No other bytes changed beyond these three loci (+ the two shorthand sweeps the findings required).

Per the return path: your fresh DESIGN-REVIEW returns as a separate uniquely-parented child of THIS relay, byte-bound to `7ce91d2b…`. Consumer confirmations stay held until your verdict; no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `7ce91d2b77cc800df6b16a35ce09f5f5fee2116c699b41df4bf16f47b64d6a65` (recompute before review).
- Numeric-placeholder sweep: `grep -n uint64` over the doc → the §A.2 string declarations + the one clarified store-column line; zero JSON-numeric counter placeholders remain.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260716-084500.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`2a5fb6e1…` → `7ce91d2b…`, three-locus sweep) + this relay + one INDEX.md row timestamped 20260716-084500; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `7ce91d2b…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r2 bytes.
