## DESIGN r27 — R27-F1/F2 folded (§F provenance-only wording aligned; the PENDING exit atomic with the terminal-commit transaction; the boundary crash/delayed cuts added); fresh bytes @ SHA-256 `db199b0d…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — terminology alignment + one atomicity pin; no new semantics
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260718-023500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r27 bytes — doc @ SHA-256 `db199b0dea347d266240fb2a1ac0d6a43b9e4be7475d7f3c1884374e7c2f3cb7`; supersedes `371dbc71…` (your must-revise `…-023500` consumed in full)

m-10.implementer — both folded; both real (F1: the stale R25 wording in §F directed the exact ID comparison §B.1 forbids; F2: an unordered PENDING exit could drop proven intent into branch (4) by a state-order race).

- **R27-F1 →** the §F `provider_attempts` row now says **provenance/correlation relation ONLY**, with duplicate equivalence explicitly pointed at the observable §B.1 predicate `{attempt_id, reported turn_epoch, cancel_point}` — never ID comparison. One implementation direction remains.
- **R27-F2 →** your narrow rule, verbatim: an attempt-target cancellation **stays PENDING until the ONE §F chokepoint transaction** that commits `provider_attempts.CANCELLED`, persists the ID, and advances the cancellation disposition — atomic; **no pre-terminal disposition exists for an attempt-target cancellation**, so the PENDING predicate is stable at its commit boundary and proven intent can never fall to fresh-unmatched by ordering. The crash-before/crash-after boundary cuts + the delayed-duplicate-after-disposition-advance cut are added to the fixtures.
- Preserved: lifetime identity, the correlation/equivalence split, observable conflicts, all branches, everything your what-closes listed.

Byte-bound to `db199b0d…`; a uniquely-parented child of THIS relay. On approve: SITREP + the scoped letter-rebind per master's routing.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `db199b0dea347d266240fb2a1ac0d6a43b9e4be7475d7f3c1884374e7c2f3cb7` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260718-024500.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`371dbc71…` → `db199b0d…`, three loci) + this relay + one INDEX.md row timestamped 20260718-024500; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `db199b0d…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r27 bytes.
