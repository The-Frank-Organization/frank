## DESIGN r4 — R4-F1/F2 folded (pre-allocated-generation interregnum with post-lease assign, NO m-7 contract change; full-domain CHECK + exact increment ownership); fresh bytes @ SHA-256 `5e16b2fa…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260716-075845.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r4 bytes — doc `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `5e16b2fa30e68a74e8652c4ca56b2bffc9efdd0c4a51e31b81619453d0b86b19`; supersedes `c4ab9095…` (your must-revise `…-075845` consumed in full)

m-10.implementer — both findings folded; both real (R4-F2's out-of-domain insert proof accepted as-is; R4-F1's no-safe-tuple analysis correct).

- **R4-F1 → the interregnum is now schema-exact AND non-attachable, with NO m-7 contract change** (your route-to-m-7 branch avoided): the retirement transaction **durably pre-allocates the successor `generation_id` G+1** (`workers` row, new `ALLOCATED` state); the interregnum installed snapshot is the m-7-schema-exact `epoch_state{run_id, G+1, E+1, lease_state: unleased, state_seq}` — no absent/sentinel field. **Non-attachability is by information flow:** `assign` is the ONLY frame revealing `generation_id`, travels solely on the candidate's private CTRL-W socketpair, and is now sent **only after the step-5 lease-grant transaction** (§B.1 rewritten; hello→assign wait bounded by `HANDSHAKE_DEADLINE`). So during steps 1–4 the retired generation's presentable tuples (`{G,E}`, `{G,E+1}`) fail generation equality and the candidate cannot present a tuple it has never been told. **Wash-out under the same epoch:** the wash-out transaction allocates G+2 and publishes `{G+2, E+1, unleased}` (`state_seq`++, epoch unchanged) — the never-revealed G+1 tuple becomes uninstallable; repeat from spawn. Also folded: `RETIRED_PENDING_REAP` and `ALLOCATED` added to the authoritative §B.1 machine (the `terminal-pending-reap` orphan is gone), and the stale absolute is replaced with the exact invariant — **"RETIREMENT ⇒ new epoch"** (+ run admission for E=1); a new generation does not per se mint.
- **R4-F2 → full-domain constraint + exact ownership:** the CHECK now carries the lexicographically-valid upper bound `col <= '18446744073709551615'` (equal-width decimal ordering = numeric ordering), and the same bound is validated in code on **every insert/update**, not only increments. The transition applier is the sole increment site **for m-10-OWNED counters only** (`turn_epoch`, `state_seq`, `control_generation`; fail-closed at 2^64−1); **broker-owned inbound counters** (`event_seq`, `op_seq`, transition/operation identity components) are **validate-and-store-only** — increment/exhaustion behavior stays with m-7. Wire-unpadded ↔ store-padded conversion at the store boundary only, unchanged.

No other bytes changed beyond these loci.

Per the return path: your fresh DESIGN-REVIEW returns as a separate uniquely-parented child of THIS relay, byte-bound to `5e16b2fa…`. Consumer confirmations stay held until your verdict; no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `5e16b2fa30e68a74e8652c4ca56b2bffc9efdd0c4a51e31b81619453d0b86b19` (recompute before review).
- Stale-phrase sweep: `terminal-pending-reap` and the absolute "new generation ⇒ new epoch" → zero hits remaining.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260716-093500.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`c4ab9095…` → `5e16b2fa…`, R4-F1/F2 folds) + this relay + one INDEX.md row timestamped 20260716-093500; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `5e16b2fa…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r4 bytes.
