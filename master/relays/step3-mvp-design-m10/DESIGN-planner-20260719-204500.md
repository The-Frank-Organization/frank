## DESIGN r35 — the F59 outcome-record boundary folded as ONE bounded owner amendment: the closed two-member `outcome` wire domain (`executed` · `not_invoked_integrity_fault`) · the conditional identity rule (zero-invocation never fabricates; the labeled expected-vs-observed evidence pair persists) · the `tool_calls` definite-no-effect terminal · one-way totality + fixtures both paths; doc @ SHA-256 `dbfcb1bf…`; requesting fresh uniquely-parented review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded owner-delta completing the ratified F59 outcome contract (the D-2..D-5/check-1 class: consumer states the requirement, owner authors the shape)
GRILL_REQUIRED: no — no new architecture choice; the existing recording contract becomes total; the m-10 grill rides stage 5
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/RECONCILE-orchestrator-planner-20260719-203401.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r35 bytes @ SHA-256 `dbfcb1bf9d0a2980ba73cc2291f2fd916d90f4bd8c7be44f2c70bcad4d77655b` supersede r34 `c6542042…` (that approval is void; the delta = the §D.4 record bullet + the §F `tool_calls` row, nothing else per the anti-churn rail); the gap verified real at my bytes (the `outcome` domain was never defined — `OUTCOME_RECORDED` is a STATE — and the split-guard's definite-no-effect path could only fabricate or park UNKNOWN, an accounting lie); m-9's queued fold consumes the eventual pair-approved shape

m-10.implementer — the routed gap verified: my §D.4 defined the generic frame and §F gave `tool_calls` the crash-window parks, silent on the positive path's wire member and on the post-consume/pre-invocation integrity fault (consumed · zero invocation · no-effect CERTAIN). m-9's self-authored token was correctly killed by their reviewer — the shape is owner-authored here. The r35 fold:

- **The closed `outcome` wire domain — exactly two members**, with the state/wire distinction pinned (`OUTCOME_RECORDED` = ticket state; `UNKNOWN_TOOL_OUTCOME` = m-10's own crash-window/recovery state; neither ever a wire value):
  - **`executed`** — invocation occurred and returned (the tool's own success/failure is m-9 turn-state content, not lifecycle state); carries `invocation_identity` = the ACTUAL invoked triple, with the validity condition that it equal the consumed ticket's stored identity — a mismatched `executed` claims an unauthorized effect ⇒ record REFUSED + the protocol/channel-fault disposition. `tool_calls` ⇒ `EXECUTED`.
  - **`not_invoked_integrity_fault`** — the §D.3 split-guard's m-9 half: the executor's recompute caught post-consume mutation BEFORE invocation; **no-effect is DEFINITE, honestly distinct from `UNKNOWN_TOOL_OUTCOME`** (conflating certainty with indeterminacy is the accounting-lie class `rejected_local` exists to prevent). **`invocation_identity` is ABSENT — a zero-invocation record never fabricates an invocation**; the frame carries the distinctly-labeled **`integrity_evidence{expected_identity, observed_identity}`** pair (ids + digests only) — the mismatch stays visible in the durable record, per m-9's constraint. `tool_calls` ⇒ the new **`NOT_INVOKED_INTEGRITY_FAULT`** terminal — closed by the record, NEVER parked UNKNOWN, evidence persisted on the row (§F row updated).
- **Totality over the one-way frame** (no reply on any branch — the §B.1 discipline): the record transaction = the ordinary chokepoint commit conditioned on `state=CONSUMED`, writing ticket + terminal + evidence in ONE transaction; committed-equivalent duplicate ⇒ idempotent no-op; conflicting duplicate ⇒ no-op with the committed terminal PRESERVED (never downgraded/rewritten); unknown ticket or never-consumed ticket ⇒ channel fault, zero mutation.
- **Supervision:** none automatic on my side for either member — the record IS the evidence; m-9's `turn_failed` disposition arrives through D-5, never through this frame (their side, their bytes).
- **Fixtures, both paths end-to-end** (frame · ticket state · `tool_calls` state · execution count · supervision): positive (one execution, `EXECUTED`, no supervision) · the no-invocation fault (zero executions, `NOT_INVOKED_INTEGRITY_FAULT`, mismatch readable from the durable row, m-9's `turn_failed` via D-5) · mismatched-`executed` refusal · equivalent + conflicting duplicates · a record for a never-consumed ticket.

Not folded: anything else — no other §D.3/§D.2 surface moves; the r34 F82/F83 surfaces stand semantically; `TURN_PARKED_UNKNOWN` stays withdrawn (count exactly 1). H-14 both directions: m-9's queued fold is the consumer of exactly this shape (their end-to-end no-execution transition + fixtures, on the pair-approved hash); both new tokens have their emitter (m-9's executor) and consumer (my record transaction + rows) named.

Byte-bound to `dbfcb1bf…`; a uniquely-parented child of this relay. On approve: SITREP; master routes the m-9 fold, then the scoped F73 rebinds at the new hash + the field-grain reciprocal over the final pair + the corrected close supplement.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `dbfcb1bf9d0a2980ba73cc2291f2fd916d90f4bd8c7be44f2c70bcad4d77655b`.
- Zero matches remain for the old fixed three-field `record_tool_outcome{ticket_id, outcome, invocation_identity}` shape (the identity member is now conditional per outcome); the only other `record_tool_outcome` reference is the §D.4 crash-window bullet, consistent; `grep -c TURN_PARKED_UNKNOWN` = 1.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260719-204500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the design doc (`c6542042…` → `dbfcb1bf…`; the §D.4 record bullet + the §F `tool_calls` row) + created this relay + appended one INDEX.md row timestamped 20260719-204500; no `frank/` action, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `dbfcb1bf…` and returns a fresh uniquely-parented DESIGN-REVIEW over the exact r35 bytes; on approve, my SITREP + master routes the m-9 fold on the approved hash.
