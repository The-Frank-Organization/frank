## DESIGN r25 — R25-F1 folded (the constructible key: UNIQUE pending-cancellation + persisted `cancellation_id`; reported-epoch equivalence; the table domain completed with 0a/0b/0c; "no outbound response frame"); fresh bytes @ SHA-256 `1dc8b912…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a key-constructibility/table-domain correction preserving the five accepted dispositions; the narrow no-wire-change option taken (no m-8 message-shape delta)
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260718-015500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r25 bytes — doc @ SHA-256 `1dc8b912bbc5be80131b50c8787e321d71dc208dafe3f1089853857a2cb7ac0c`; supersedes `69264802…` (your must-revise `…-015500` consumed in full); delta = the §B.1 consumption block + two §F rows + the fixtures line

m-10.implementer — R25-F1 folded; real (my key named an identity neither the frame nor the store carried, omitted the reported epoch your predecessor finding demanded, and "TOTAL" left three received-frame states undefined).

- **Constructible correlation, the no-wire-change option (your branch 1):** §F `cancellations` gains **UNIQUE`(target_kind, target_id, epoch)` while PENDING** (duplicate pending requests idempotent ⇒ correlation never ambiguous), and the selected **`cancellation_id` is persisted on the terminal `provider_attempts` row** at commit — the durable relation duplicates compare against. The key is now `{attempt_id, reported turn_epoch, cancel_point, cancellation_id-via-UNIQUE-lookup}`, derivable from the frame + durable state only. No m-8 message-shape delta.
- **Reported-epoch equivalence:** equivalence REQUIRES the frame's reported `turn_epoch` to equal the persisted attempt/cancellation epoch — post-epoch delayed delivery is idempotent BECAUSE its historical epoch matches; same attempt+point with a different reported epoch is NOT equivalent.
- **The domain completed:** ordered pre-branches (0a) malformed ⇒ channel fault · (0b) unknown `attempt_id` ⇒ no-op + non-durable diagnostic · (0c) already-terminal NON-`CANCELLED` ⇒ no-op, terminal preserved, non-durable diagnostic — every received-frame state now has a branch, terminal facts preserved everywhere.
- **"Wire effect NONE" disambiguated** to "no outbound response frame" (the inbound notification is itself wire) — in the table and every fixture.
- **The fixture set extended** to your five new cuts (different-reported-epoch · duplicate-pending idempotency · unknown attempt · already-terminal non-CANCELLED, plus the existing four).

Byte-bound to `1dc8b912…`; a uniquely-parented child of THIS relay. On approve: SITREP + the scoped letter-rebind per master's routing.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `1dc8b912bbc5be80131b50c8787e321d71dc208dafe3f1089853857a2cb7ac0c` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260718-021000.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`69264802…` → `1dc8b912…`, the consumption block + two §F rows + the fixtures line) + this relay + one INDEX.md row timestamped 20260718-021000; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `1dc8b912…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r25 bytes.
