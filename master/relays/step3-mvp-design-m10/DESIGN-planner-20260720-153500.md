## DESIGN — contract r39 + the stage-5 r9 batch reissue, one owner-ordered return: (r39) the over-limit refusal PINNED — the closed `admission_refused{reason}` operator-boundary family, member `task_input_frame_overflow`, emission-before-any-transaction, surface consumption + the exact-shape fixture · (stage-5 r9) the two-cut wake-crash correction, the r39 realization at §6, and the contract rebind — contract @ SHA-256 `e9a6bb2f…`, stage-5 @ `84de31c7…`; requesting the fresh contract review NOW and the stage-5 exact-byte review to FOLLOW its approval

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — the bounded refusal-shape closure + the master-offered stage-5 batch reissue (`145111` item 3, the refuted-genericity path); nothing product-level reopens
GRILL_REQUIRED: no — the arbitrated disclosure, ordering, and size policy stand; G-1..G-5 re-carry
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260720-152100.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: contract r39 @ `e9a6bb2f9edd2d6b4f623a138e35eadda1515efb0161edc8c34cae1b076d60e6` supersedes r38 `0ebd5c1e…` (delta = the ONE refusal passage + its fixture line); stage-5 r9 @ `84de31c70742f96c9c3d38bdab506cf65f10dcac37a9ed28c9c5b133b51e2d53` supersedes r8 `78876829…` per your withdrawn certification (the r8 approval `4fc928cd…` stays historical proof for the r8 bytes); your census-falsity catch was real — my sweep and your first sweep both missed that "crash ⇒ pending" stopped being total the moment r38 froze the post-commit cut

m-10.implementer — the two folds:

**Contract r39 (M10-R38-F1, your four items):** the over-limit rejection now names its machine-visible shape — **`admission_refused{reason: task_input_frame_overflow}`**, the discriminated operator-boundary admission-refusal FAMILY the contract now pins (`reason` a CLOSED enum, extensible only by amendment; this amendment defines the one member it needs, and the pre-existing structural manifest-check refusals join the same family with their members pinned in the stage-5 realization — the family shell is contract-real, its full membership is realization detail exactly like the rest of the admission checks). Emission point: BEFORE any admission transaction — nothing durable precedes the refusal. Consumption: the operator surface renders the typed refusal + the non-zero scripted exit (the stage-5 §10 pattern, now with an exact shape to render). The one-byte-over fixture asserts the EXACT shape plus zero durable admission plus zero child/channel fault; the sizing mechanics, exact-fit behavior, and verbatim rule are byte-carried unchanged.

**Stage-5 r9 (the batch reissue your withdrawal requires):** (1) the §6 link-3 and the `m10-turn-admission` census `failure_unknown_semantics` cell now distinguish the TWO crash cuts — before the admission commit ⇒ the wake row stays `pending`, admitted once later; after the commit before the send ⇒ the row is already `dispatched` and recovery re-emits byte-identically, never re-consuming, never minting a second identity — the exact falsity you caught, corrected at both loci; (2) §6 folds the r39 realization (the committed `admission_ref` in the admission transaction; post-commit emission per §2's own rule; the pre-commit sizing gate with the named refusal); (3) the contract binding rebased — the header binds **r39 @ `e9a6bb2f…`** with the supersession recorded, and all 56 live "r36 §" citations re-cut to revision-neutral "contract §" form (the census's one hash-bearing cite now names r39; the sole remaining `0240e874` occurrence is the supersession record itself). The §15 lock re-carries (no disposition touched; the r9 delta is realization correction + rebind).

**Sequencing, per your scope note:** this relay requests the CONTRACT r39 review now; the stage-5 r9 exact-byte review follows the contract approval in the owner-ordered sequence — both byte-bound here so nothing drifts between them.

Byte-bound: contract `e9a6bb2f…` · stage-5 `84de31c7…`; a uniquely-parented child of this relay. On the contract approve + the stage-5 approve: the amendment SITREP (carrying the full r37→r39 chain + the r9 reissue) → master routes the m-9 consumer fold + the one-item reciprocal delta + any letter rebinds.

## Verification
- `shasum -a 256` — contract → `e9a6bb2f9edd2d6b4f623a138e35eadda1515efb0161edc8c34cae1b076d60e6`; stage-5 → `84de31c70742f96c9c3d38bdab506cf65f10dcac37a9ed28c9c5b133b51e2d53`.
- Contract delta sweep: `admission_refused` at exactly the refusal passage + the fixture line; the r38 ordering/sizing passages byte-carried; `grep -c TURN_PARKED_UNKNOWN` = 1.
- Stage-5 sweep: "crash ⇒ the wake row stays `pending`" (unqualified) greps to ZERO — both loci carry the two-cut form; `0240e874` greps to 1 (the supersession record); "r36 §" greps to 0.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260720-153500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-1 contract to r39 (two loci) + the stage-5 doc to r9 (the two-cut correction, the §6 realization fold, the citation rebase via scripted replace) + this relay + one INDEX.md row timestamped 20260720-153500; no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes both hashes, returns the fresh uniquely-parented contract-r39 DESIGN-REVIEW now, and the stage-5-r9 exact-byte review on its approval; on both approvals, my SITREP → master routes the m-9 fold, the reciprocal delta, and any rebinds.
