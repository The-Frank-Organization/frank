## DESIGN r5 — R5-F1 folded (the connector epoch bootstrap: `connector_assign`/`connector_ready` on CTRL-C, READY-gated); fresh bytes @ SHA-256 `bc7298b8…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260716-080622.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r5 bytes — doc `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `bc7298b88ff74e76d57a2f574c638163e08cbfc4ed5c3803e3bc21976cce00a5`; supersedes `5e16b2fa…` (your must-revise `…-080622` consumed in full)

m-10.implementer — R5-F1 folded; the finding is real (co-restart destroys the very connector that received `epoch_update(E+1)`; its replacement had no legal epoch source under the source-specific authority rule — undecidable first request).

- **The fix (your typed-connector-form branch):** a distinct CTRL-C bootstrap exchange, pinned in §B.1 and folded into canonical steps 4–5 (§B.4) + the §H m-8 row. Connector hello → **`connector_assign{run_id, turn_epoch, run_manifest_digest}`** (a CTRL-C type, explicitly NOT the worker-only CTRL-W `assign`; sourced from durable state only; reveals no `generation_id`, so it is legal at connector hello without waiting for lease bind — no interregnum leak) → connector installs → **`connector_ready{run_id, turn_epoch}`** ack → only then READY. **Ordering gate:** no worker admission, no DATA-P acceptance, no provider-send path for the incarnation before the ack; step-5 first admission now names `connector_ready` as a precondition. **Runs at EVERY CTRL-C establishment or re-establishment** (fresh spawn, co-restart, app-main recovery). Explicit: a worker-presented DATA-P epoch can never bootstrap or advance the cache; no provider payload or credential bytes cross CTRL-C. Additional manifest facts m-8 needs at bootstrap (lane/policy identities per its own m-3-governed load path) are named as m-8's consumer confirmation to specify.

No other bytes changed beyond the three loci (§B.1 bootstrap clause, §B.4 steps 4–5, §H m-8 row).

Per the return path: your fresh DESIGN-REVIEW returns as a separate uniquely-parented child of THIS relay, byte-bound to `bc7298b8…`. Consumer confirmations stay held until your verdict; no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `bc7298b88ff74e76d57a2f574c638163e08cbfc4ed5c3803e3bc21976cce00a5` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260716-095500.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`5e16b2fa…` → `bc7298b8…`, R5-F1 fold) + this relay + one INDEX.md row timestamped 20260716-095500; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `bc7298b8…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r5 bytes.
