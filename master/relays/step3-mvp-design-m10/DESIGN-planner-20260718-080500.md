## DESIGN r31 — R30-F1..F3 folded as ONE bounded revision: existence-cut before the fence · stale = row-less/counter-neutral (uniform stale posture; `stale_epoch` leaves the `void_reason` domain) · `lease_invalid` = ONE atomic chokepoint transaction (VOID row + retirement effects inseparable); doc @ SHA-256 `3a88a9c5…`; requesting fresh uniquely-parented review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded protocol-totality repairs inside ratified ownership, per your R30 scope note
GRILL_REQUIRED: no — no hard-to-reverse choice beyond the reviewed family; the m-10 grill rides stage 5
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260718-075100.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner, m-1.planner, m-2.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r31 bytes @ SHA-256 `3a88a9c5552cb6ad18727ad494e1d7ad8a609d40f5f37dcbccda8f6ca60ae636` supersede r30 `4154c389…`; all three blockers verified real (F2 confirmed against §B.1's no-row/no-charge stale posture verbatim) and folded; delta confined to §D.1 domain + §D.2; the `authorize_reject` reason set is UNCHANGED at four — m-9 consumes only the eventual pair-approved hash. RECORD NOTE: your `075100` §Verification "recomputed r30" line carries a malformed 78-hex string (the r30 head spliced onto the r29 tail) — your binding statement at the body's opening carries the correct full `4154c389…`, so the review binds; please carry the corrected line in the fresh review so the trail stays byte-exact

m-10.implementer — all three R30 findings verified real before folding (F1: check (1) genuinely had no comparator input for an unknown run while three loci promised the result; F2: I re-read §B.1 and the D-5 stale branch — `attempt_open_reject{stale_epoch}` is "NO row committed … no attempt-budget charge" verbatim, and my "turn exists" predicate let a stale frame charge even the current turn; F3: the two durable acts were separate and my own replay no-supervision rule foreclosed the repair). The r31 fold, per finding:

- **F1 → the existence cut precedes the fence:** new check (1) — unknown `run_id` ⇒ `authorize_reject{run_not_admitted}`, row-less, counter-neutral, **no invented epoch** (the branch fires precisely because the fence is undefined; fence-first for known runs unweakened). Fixture: zero row, zero counter change, decided before any fence read.
- **F2 → stale is row-less and counter-neutral:** check (2) stale ⇒ `STALE_EPOCH` with ZERO durable mutation, matching the uniform stale posture (§B.1 no-row/no-charge · D-5 zero-mutation · §B.4 every-CTRL-W fencing · §G). Because every row-writing classification sits BELOW the fence, **every committed row is current-epoch by construction** — the ordering, not the "turn exists" predicate, is what closes your different-epoch-turn attack. Consequence carried through: **`stale_epoch` leaves the `void_reason` domain** (now five members — no `VOID/stale` row can exist) and the replay mapping drops that arm with an explicit totality note. Fixtures: stale naming its own historical turn AND stale naming an existing different-epoch turn, both asserting successor state/rows/counters unchanged.
- **F3 → your option (a), the atomic transaction:** `lease_invalid` (now check (5)) commits the VOID row AND the complete retirement-transaction effects in **ONE §F chokepoint transaction**, reply only after — **the row and the retirement effects cannot exist separately**. Both crash cuts pinned: before ⇒ nothing durable, deterministic reclassification re-runs the transaction; after-commit-before-reply ⇒ replay (0) serves the stored rejection and recovery proceeds via §B.3 branch (b), with replay's no-supervision rule proven SAFE by the atomicity invariant (a `VOID/lease_invalid` row existing at all proves retirement committed with it). Fixtures: crash immediately before and after the boundary, asserting exact generation/lease/epoch/turn/`void_reason`/reply state. I chose (a) over your (b) fault-disposition-state alternative because the retirement transaction is already one atomic multi-row §F transaction — one INSERT joins it with no new state machine.

Checks renumbered 1–7 (run-exists · fence · run-admitted · turn-active · lease · budget · serve-gate); the row rule, supervision totality, and fixture list renumbered with it. Not folded: anything outside §D.1's domain sentence + §D.2; the four-token `authorize_reject` set, the counter definition, and the ceiling semantics stand as r30 defined them; `TURN_PARKED_UNKNOWN` stays withdrawn (occurrence count still exactly 1). H-14 note for m-9's eventual fold (pair-approved hash only): their consumer rows are unchanged from the r30 relay's note — `turn_budget_exhausted` (⇒ terminate the turn) + issue-side `IDENTITY_MISMATCH`; no new outbound token was added at r31.

Byte-bound to `3a88a9c5…`; a uniquely-parented child of this relay. On approve: SITREP with this hash; master routes the ONE F73 rebind round + the fresh COMPLETE m-9 reciprocal on it.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `3a88a9c5552cb6ad18727ad494e1d7ad8a609d40f5f37dcbccda8f6ca60ae636`.
- `grep -c TURN_PARKED_UNKNOWN` = 1 (the withdrawal sentence); zero matches for the r30 residues (`VOID/stale_epoch` mapping arm · the six-member domain · the old "(1)–(4) and (6)" row rule · "any r30 token" · "lands on (5)").
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260718-080500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the design doc (`4154c389…` → `3a88a9c5…`; §D.1 domain sentence + the §D.2 procedure) + created this relay + appended one INDEX.md row timestamped 20260718-080500; no `frank/` action, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `3a88a9c5…` and returns a fresh uniquely-parented DESIGN-REVIEW over the exact r31 bytes (with the corrected r30 hash line on the record); on approve, my SITREP + master's rebind round.
