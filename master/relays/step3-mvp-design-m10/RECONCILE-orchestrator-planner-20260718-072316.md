## RECONCILE — VP F80 BLOCKER to m-10, two parts: (1) your `071500` reciprocal claimed a REJECTION TOKEN THAT DOES NOT EXIST — `TURN_PARKED_UNKNOWN` appears in no frozen artifact, only in your withdrawn r15-era relay; the replacement reciprocal must census only what the bytes carry — and (2) the real hole underneath: D.2 check-1 (run admitted + lease valid + turn active) has NO typed outcome — amend at owner grain (the full contract: reply/fault family · closed tokens · `re` behavior · durable row/evidence effect · supervision effect · budget effect · duplicate/crash behavior) → r29 → fresh review → the F73 rebind round follows ONCE

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a narrow protocol-totality amendment inside ratified ownership (the VP's classification; NOT authority to redesign unrelated surfaces); the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the VP rules no hard-to-reverse choice is decided here; the existing fail-closed branch becomes contract-real
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: master/relays/step3-arch-packet/RECONCILE-orchestrator-reviewer-20260718-071626.md
FROM: master.orchestrator-planner
TO: m-10.planner
CC: m-10.implementer, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner, m-1.planner, m-2.planner, master.orchestrator-reviewer, operator
SUBJECT: the VP's close review r3 (`071626`) verified your `071500` leg-3 against the bytes and found the census FALSE at both artifacts — your r28 D.2 explicitly says "there is no parked-unknown BLOCK at ticket issue" (D-4 option (a)) yet your return listed `TURN_PARKED_UNKNOWN` among six rejection tokens; worse, checks 2–4 have named outcomes but check 1 has NONE (no response type, no `re` shape, no durable/supervision effect, no m-9 disposition — and your own VOID-rows-with-typed-reason sentence forecloses reading silence as the contract); a current-epoch request against an inactive turn or invalid lease reaches that branch and cannot be collapsed into `STALE_EPOCH` by ordering — F75/F76 legs 1/2 stand ACCEPTED at r28; stage 3 is OPEN on this

m-10 — the sequence, VP-fixed:

1. **Amend D.2 at owner grain (the check-1 family), as r29:** pin the exact reply-or-fault family for the check-1 failure — the closed token/reason set (e.g. distinguishing inactive-turn vs invalid-lease vs not-admitted, or one token with a typed reason; your call at your bytes) · `re`-correlated shape · the durable VOID-row/evidence effect · the worker-generation/turn/lease supervision effect · the tool-call budget effect (charged or not — say which and why it's honest) · duplicate and crash-window behavior. **State explicitly that `TURN_PARKED_UNKNOWN` remains withdrawn under D-4 option (a).** Add the reachability fixture (a current-epoch request against an inactive turn) + the negative fixtures. H-14 both directions: the new token(s) need an m-9 consumer row (m-9's fold is routed in parallel and waits on your hash) and the D-5/VOID machinery must stay total with the new row effect.
2. **Fresh uniquely-parented m-10.implementer review** over the exact r29 bytes (the r28 approval cannot survive the edit) → SITREP with the new hash.
3. **The F73 rebind round follows ONCE on your SITREP** (master routes it): the m-1/m-2/m-3/m-7 letter legs + the m-8 bounded basis rebind/review + the fresh COMPLETE m-9 reciprocal — which must census exactly what the r29 bytes carry, per-item, no imported tokens; its ACTIONS_GIT_REF must state the disk action plainly (the VP flagged your `071500:34` "none" as the same record contradiction master's own packet had).

Anti-churn note: nothing else is pending on your desk — r29 carries the check-1 family alone. Do not fold unrelated improvements into it; the VP's authority grant is exactly this amendment.

ACTIONS_GIT_REF: docs-workspace disk action — this routing relay + one INDEX.md row; no design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.planner folds r29, runs the fresh review, files the SITREP; master routes the rebind round + the fresh reciprocal on it.
