## DESIGN r1 — R1–R4 folded (all four verified against sources before folding); fresh bytes @ SHA-256 `2a5fb6e1…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — per the dispatch; operator gates return at the Master+VP interface-lock
GRILL_REQUIRED: no — per the dispatch; my grill rides the stage-5 m-10 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260716-072454.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-8.implementer, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r1 bytes — doc `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` @ SHA-256 `2a5fb6e117c9f5067c29d95b3f5fb89bbddecd6381436b43af9584a16212d2d4`; supersedes `b971ea2f…` (your must-revise `…-072454` consumed in full)

m-10.implementer — your must-revise is folded. Each finding was **verified against its cited source before folding** (m-7 r6 recomputed `f072bd99…` exact; m-3 r2 recomputed `51495e81…` exact; RFC 8785 integer range confirmed): all four are real defects; no pushback on any.

### Fold map (finding → new bytes)
- **R1 (broker CI contract) →** §A.1: the broker channel is now the CI-1 dial-in control listener — m-10 as CLIENT, fcntl-lock → durable `control_generation` advance → connect ordering, spawn-pipe token handoff; socketpair-inheritance scoped to m-9/m-8 only. §A.2: unknown-field evolution scoped per message family; the broker event/ack family is CLOSED (reject unknown fields). §B.1: `assign` gains `generation_id` + `broker_worker_endpoint` (CI-2). §B.4: **epoch authority is source-specific** — only m-10-sourced updates advance any authority cache; a peer-presented epoch never does; peer-ahead ⇒ typed `EPOCH_AHEAD` hold/verify via m-10, never adopt; high-water-mark scoped to the m-10-sourced stream only. NEW §B.5: m-10's full half of the epoch-transition handshake (durable-publish → propose with `epoch_transition_id` → frozen `crossing_set` → CROSSERS_DURABLE one-transaction rows → ack → completions → recovery-by-ID with resume-or-abort, broker-loss `unknown-outcome` disposition, at-most-one-non-terminal-per-run) + the CI-3 recording protocol (`broker_events` UNIQUE(nonce,event_seq), same-ack-on-duplicate). §F: four new row families (`broker_control`, `broker_events`, `epoch_transitions`, `crossing_ops`). §H: the m-7 confirmation row now names CI-1/CI-2/CI-3 on exact bytes.
- **R2 (m-3/F63 vector) →** §C.1: manifest gains `policy_digest` (m-3-produced, m-10-written) and a distinct `tool_catalog_digest` (m-9 catalog build); the m-8 lane digest renamed `lane_catalog_digest` (conflation removed); `release_binding` is now a CLOSED schema — `bound_at_ref` + build-digests XOR `release_digest` (shared-conductor-client coverage rule named; app/provider vertical only per F65); freeze-time `pinned_lane == provider_lane.lane_id` equality check added (reject at freeze). §C.3: the release check is now four exact comparisons (binding validity · tool-catalog-vs-shipped-registry · policy-digest presence/load equality · lane-catalog fact).
- **R3 (DATA-P restart) →** §A.1: DATA-P pinned **generation-paired** — bound to the (worker generation, connector incarnation) pair; either owner's death ⇒ **co-restart of both** under the new epoch with a fresh socketpair, prior attempt parked; named accepted cost; private-listener rendezvous = named Step-4 evolution (§I). §B.1: NEW payload-free attempt-observation messages — m-9 `attempt_open`/`attempt_stream_end` (CTRL-W), m-8 `attempt_result` (CTRL-C), m-9 `app_event` → `pending_app_events` — the sole writer now has a truthful observation path for `provider_attempts` without touching DATA-P (also satisfies m-3 §6's m-10 row).
- **R4 (JCS uint64) →** §A.2: every trust-bearing counter (`turn_epoch`, `seq`, `re`, `state_seq`, `control_generation`, `event_seq`, `op_seq`, transition/operation identity components) crosses JSON as a canonical decimal STRING, pinned grammar `^(0|[1-9][0-9]*)$`, value < 2^64, numeric comparison; violation ⇒ malformed. Applied uniformly (frames, feed, exports).
- BASIS updated to name both consumed sibling contracts by path + hash.

### Unchanged
Broker-not-hub topology, JCS encoding, SQLite store discipline (FULL/private-file/user_version/chokepoint/deferred journal), the 8-name gate + F58 staging, F59 ticket semantics + denial rows + ceiling accounting, §E wake, §G negatives, TOON §I note. Your "what passes" list was left byte-stable except where a finding required otherwise.

Per the return path: your fresh DESIGN-REVIEW returns as a separate uniquely-parented child of THIS relay, byte-bound to `2a5fb6e1…`. Consumer confirmations stay held until your verdict; no self-declared lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `2a5fb6e117c9f5067c29d95b3f5fb89bbddecd6381436b43af9584a16212d2d4` (recompute before review).
- Source verification performed this session: m-7 r6 → `f072bd996da0c85b1be9b67fad880e7395ab03de7142cb87fc6864d4f67a100e` (exact); m-3 r2 → `51495e81cd906d548e7a601659a440e353888d7abe06c2a99f1cc4271fecdd44` (exact); amendment r7 → `2f75f2a1…` (exact, prior).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260716-081500.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`b971ea2f…` → `2a5fb6e1…`, R1–R4 folds) + this relay + one INDEX.md row timestamped 20260716-081500; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `2a5fb6e1…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r1 bytes.
