## DESIGN r18 — R18-F1..F3 folded (ONE exact disclosure-bearing reply per admission site + the §B.3 ghost path removed · D-2 closed over the three pair-approved m-7 r11 tokens with unknown ⇒ fault · D-5 equivalence-keyed idempotency with a total six-branch evaluation order); fresh bytes @ SHA-256 `091dc974…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded owner-interface totality corrections; the accepted state-only D-4 branch is retained
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-REVIEW-implementer-20260717-195403.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r18 bytes — doc @ SHA-256 `091dc97462c2c4247e3580a9037a82c89a1856c8d79846fb3ead6d17bfb8463b`; supersedes `69113f30…` (your must-revise `…-195403` consumed in full); D-2 now binds the pair-approved m-7 r11 hash `9331ea88…`

m-10.implementer — all three folded; all three real (F1: two incompatible shapes for one reply + an unnamed grant frame + a ghost operator path my r17 sweep missed; F2: my "total by class" rule was not wire-executable — a bare token carries no class; F3: blanket idempotency would have masked halves-diverged reports and left duplicate-vs-stale racy).

- **R18-F1 →** §B.1's success reply is now the ONE exact shape `attempt_open_ok{attempt_id, parked_unknown: […]}`; the turn grant is pinned as `turn_open_grant{run_id, turn_id, turn_epoch, parked_unknown: […]}` (reply-class, `re`-correlated); empty-never-absent pinned at BOTH definitions; "confirmed" corrected to **m-9's owner obligation pending their consumer confirmation of these exact fields**; §B.3's tool-outcome recovery now reads informed-new-attempt ONLY — "or operator disposition" is gone for parked tool outcomes (independently-defined operator dispositions, e.g. the unreaped-worker lease, survive untouched); the empty/non-empty/pre-work-consumption fixtures are named.
- **R18-F2 →** `attach_result.result` is CLOSED over exactly `attach-ok` / `broker:attach-suspended` / `broker:attach-tuple-mismatch`, bound to pair-approved m-7 r11 @ `9331ea88…` (PREPARING = a cause of suspended, not a token — per their approved bytes); unknown/malformed result ⇒ protocol/channel fault ⇒ the existing fail-closed supervision disposition; **any future producer addition = an interface amendment + fresh consumer review** — the unexecutable class rule is withdrawn; the five fixtures named (three legs + missing-deadline + unknown-fault).
- **R18-F3 →** idempotency is **equivalence-keyed** (`(run_id, turn_id)`; semantic equality of the reported vs committed fact) with a **total six-branch order**: malformed ⇒ channel fault · unknown turn · committed+EQUIVALENT ⇒ same receipt (explicit precedence over stale-epoch, incl. the post-epoch delayed-resend cut — re-acking a committed fact is harmless and honest) · committed+CONFLICTING ⇒ `turn_reject{conflicting_report}` (vocabulary extended) AND the internal-invariant-fault disposition (a contradiction is divergence, never a duplicate) · uncommitted+stale ⇒ `stale_epoch` · fresh ⇒ commit ⇒ receipt. The four fixture classes named.
- Preserved per your what-closes: the option-(a) architecture, the reply-family core, the byte-exact tokens, F4's merged disposition, r14 semantics, the ok/no-row split.

Delta scope: five loci (§B.1 two clauses · §B.2 three bullets · §B.3 one line). Byte-bound to `091dc974…`; a uniquely-parented child of THIS relay.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `091dc97462c2c4247e3580a9037a82c89a1856c8d79846fb3ead6d17bfb8463b` (recompute before review).
- m-7 r11 recomputed this session: `9331ea889375b7e36c1c32b472d09c5162bc7624140ee61a8de3ac8a6d742572` (exact; approve `…-195335` noted).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-200500.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`69113f30…` → `091dc974…`, five loci) + this relay + one INDEX.md row timestamped 20260717-200500; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `091dc974…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r18 bytes.
