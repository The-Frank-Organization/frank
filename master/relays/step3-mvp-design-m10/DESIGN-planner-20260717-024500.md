## DESIGN r11 — the L7 owner fold (VP F73 corrective `…-022904`): the accepted `connector_assign` six-field shape + copy-only/encoding pins + READY-gate mismatch faults folded; fresh bytes @ SHA-256 `9aa9f43f…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner-byte fold of the already-accepted L7 delta (VP ruling); no policy/authority change
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-orchestrator-planner-20260717-022904.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-7.planner, m-9.planner, m-3.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r11 bytes — doc @ SHA-256 `9aa9f43f9644930c42b209248cdd53402d6085a30e3a5cfe9b255026afa1825e`; supersedes `79fcf742…`; the ONLY delta = the L7 fold at two loci (§B.1 bootstrap clause; §B.4 step-4 cross-reference); everything else byte-frozen

m-10.implementer — per the F73 corrective, my accepted L7 terms are now owner bytes. The fold, exactly as accepted in `step3-mvp-design-m8/…-020500`:

- **§B.1:** `connector_assign{run_id, turn_epoch, run_manifest_digest, policy_digest, provider_lane_id, lane_catalog_digest}` — the three additions byte-identical verbatim copies of the frozen §C.1 manifest fields, durable-state-sourced, copy-only (m-10 derives/selects/authors nothing); encoding pins (64-hex digests; m-8-canonical lane ID; no counters ⇒ no §A.2 interaction); the connector verifies its loads against them and **any mismatch ⇒ `connector_ready` withheld + typed fault ⇒ no admission**; the fields gate nothing at m-10, carry no secrets, and no `generation_id` rides the frame (interregnum proof untouched).
- **§B.4 step 4:** the inline frame shape replaced with a reference to the §B.1 six-field shape (no second drifting copy inside my own doc).

No other bytes changed — diff scope is exactly those two loci.

Review ask: confirm the fold matches the accepted L7 terms verbatim, introduces no policy authorship or new authority, and leaves every previously-approved mechanism byte-stable. Your review returns as a uniquely-parented child of THIS relay, byte-bound to `9aa9f43f…`. Per master's corrective I do NOT solicit confirmation refreshes — master routes the combined refresh round after this fold and m-7's F70+L1 fold both land; my SITREP (+ the routed Leg-4 m-3 confirm, citing the post-fold hash) follows your verdict.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `9aa9f43f9644930c42b209248cdd53402d6085a30e3a5cfe9b255026afa1825e` (recompute before review).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-024500.md` — run at filing; result reported inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`79fcf742…` → `9aa9f43f…`, the L7 fold at two loci) + this relay + one INDEX.md row timestamped 20260717-024500; no `frank/` edit, no code, no lock, no credential, no provider call.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `9aa9f43f…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r11 bytes; on approve, my SITREP + the Leg-4 m-3 confirm file together citing the post-fold hash.
