## DESIGN r15 — the BATCH fold: all five items ACCEPTED and folded as ONE revision (attempt_open_ok · D-2 attach gate · D-4 parked-UNKNOWN gate · D-5 turn-terminal half · the CI-1 citation cosmetic), each with its H-14 reachability note inline; fresh bytes @ SHA-256 `fe65062f…`; requesting fresh uniquely-parented m-10.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m10
PARENT_DISPATCH_ID: step3-mvp-design-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded totality completions inside ratified ownership (the routing's classification, concurred per item); nothing operator-locked touched
GRILL_REQUIRED: no — my grill rides the stage-5 control-plane DESIGN
DESIGN_DOC_ID: step3-mvp-design-m10-ipc-manifest-seam
IN_REPLY_TO: step3-mvp-design-m10/DESIGN-orchestrator-planner-20260717-185818.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r15 bytes — doc @ SHA-256 `fe65062f5226706694ad491871c91d35e9da7c5d8fbd26f49f505cfb7c29e3e4`; supersedes `a2663a79…`; ALL FIVE routed items dispositioned ACCEPT (none refined/rejected) and folded at four loci; everything else byte-frozen

m-10.implementer — the `185818` batch, dispositioned item-by-item (all ACCEPT — each is a totality completion of a seam I already own) and folded as one revision:

1. **`attempt_open_ok` (m-8 R5-F1c) — §B.1:** durable reply-class ack emitted ONLY after the `provider_attempts` row commits (§F chokepoint, durable-then-visible); m-9 issues DATA-P only on the ack — the row-existence claim under R14-F1 becomes structural. Budget rule pinned: a parked row counts toward §2a; a no-row reject does not. **Reachability:** emission = the row-commit path; consumption = m-9's DATA-P issue gate. (The two m-8 notifications noted, zero bytes: reason-enum widening is theirs by my r14 text; the epoch-class attempt-inert rule leaves parked rows to my retirement machinery, which is exactly what §B.4 already does.)
2. **D-2 attach gate (m-9 half r4) — §B.2:** the first `turn_open` reply carries `attach_state ∈ {ready, held(<typed reason>)}` off my control-session/feed state — the F70 suspended floor surfaced as typed supervision state; `held` past `ATTACH_DEADLINE` ⇒ the §B.1/§B.3 FAILED disposition (wash-out/retirement), never an infinite hold. **Reachability:** emission = turn admission off installed broker state; consumption = m-9 turn start; the wedged case exits via the existing disposition machinery.
3. **D-4 parked-UNKNOWN gate (m-9 half r4) — §D.2 check (5):** my pick as state owner — **the gate blocks at ticket ISSUE, unconditionally**: no ticket issues for a turn holding an `UNKNOWN_TOOL_OUTCOME`/`PARTIAL_TOOL_EFFECT` row; typed `TURN_PARKED_UNKNOWN` rejection, recorded; the turn parks `INTERRUPTED`. Worker-independent by construction (it sits in my check chain, not in worker behavior). **Reachability:** any post-park `authorize_tool_call` hits it.
4. **D-5 turn-terminal half (m-9 half r4) — §B.2:** consuming `turn_terminal`/`turn_cancel_ack` = ONE chokepoint transaction writing the `turns` terminal record AND releasing the active-turn lease, `re`-correlated confirm; crash between receipt and commit ⇒ the turn stays in-flight and parks per §B.3 — never a silently-released lease. **Reachability:** emission = m-9's §2.9 pinned family; consumption = the transaction.
5. **Cosmetic — §A.1:** the CI-1 citation now names m-7's final r8 `ab0ed428…` (third instance of the L8 class).

Delta scope: four loci (§A.1 citation · §B.1 attempt-observation clause · §B.2 two new bullets · §D.2 check chain). Everything else stands as r14 approved it.

Review ask: per-item fidelity to the routed asks; the H-14 reachability lens on every new member (each has its emission AND consumption path stated at the folded bytes); the D-4 pick's consistency with §B.3/§B.4 (park discipline unchanged, no silent replay path opened); the D-5 transaction's lease-release atomicity against §B.2's invariants; delta-boundedness. Byte-bound to `fe65062f…`; a uniquely-parented child of THIS relay. On approve, my SITREP names the final hash; the rebind round is master-routed.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` → `fe65062f5226706694ad491871c91d35e9da7c5d8fbd26f49f505cfb7c29e3e4` (recompute before review).
- Bases: m-8 r6 @ `ab63f6eb…` (R5-F1c + the two notifications), m-9 half r4 @ `1cb4ab57…` (D-2/D-4/D-5), m-7 r8 @ `ab0ed428…` (the citation) — per the routing relay.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m10/DESIGN-planner-20260717-190500.md` — run at filing; result inline.

ACTIONS_GIT_REF: design-only — revised the design doc (`a2663a79…` → `fe65062f…`, four loci) + this relay + one INDEX.md row timestamped 20260717-190500; no `frank/` edit, no code, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer recomputes `fe65062f…` and returns a fresh uniquely-parented DESIGN-REVIEW over the r15 bytes; on approve, SITREP + the master-routed rebind round.
