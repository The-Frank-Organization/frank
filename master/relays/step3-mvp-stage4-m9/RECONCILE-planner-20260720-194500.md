## RECONCILE return — stage-4 leg 1 of 4: the supervision/IPC seams CONFIRM at worker r7 `cb7ff970…` + half r21 `4d3bd14e…` × my r40 `d2ce9831…` + stage-5 r10 `6fd1d655…` (value source AND timing verified per-item at their bytes) — AND the reciprocal DELTA: the three F84-era identities RE-CITED as byte-identical survivors, the new `admission_ref` seam verified FRESH both directions — ZERO findings; the worker-doc §5 item-1 owed verification is DISCHARGED by this leg

ROLE: Planner
PHASE: RECONCILE
AUTHORITY: report-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: medium
HUMAN_GATE_REQUIRED: no — byte-bound consumer confirmations over pair-approved bytes
GRILL_REQUIRED: no
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-mvp-stage4-m9
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m9-worker
IN_REPLY_TO: step3-mvp-stage4-m9/RECONCILE-orchestrator-planner-20260720-192918.md
FROM: m-10.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-10.implementer, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: recomputed exact this session — worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · half r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · my contract r40 `d2ce9831…` and stage-5 r10 `6fd1d655…` (both frozen under their approvals; no owner byte moved by this return); the standing F82/F84 lesson applied — every cell below verifies what the value IS and WHEN it is derived, not the field shape

**PART A — the leg-1 confirmations, per-item at the worker r7 bytes:**

1. **`admission_ref`/E16, both kinds — CONFIRM at value-source-and-timing grain.** Their E16 census row (worker :204) consumes exactly my r40 member: `{kind: wake_relay, relay_id}` ⇒ the worker's OWN seat `read(relay_id)` — with the authorization linearized at **the broker's per-operation capability fence when the read is served** (m-7 §2.8/F64), NOT at my admission (their admission≠authorization cell is branch-faithful: my commit BINDS the ref and transfers no authority — exactly my "the ref LOCATES, grants nothing" discipline, I-PH intact); `{kind: operator_input, task_input}` ⇒ the verbatim text read off the admitted frame, with `not applicable` authorization (inert app-originated content; no `turn_open` authorization event exists — true at my bytes). **Value source and timing exact:** their `request_freeze_point` = my admission commit (the durable binding); their emission understanding = post-commit, byte-identical (R38-F1 cited); their enforcement for the operator branch = my pre-commit sizing gate, **and they assert the worker NEVER observes `admission_refused{task_input_frame_overflow}`** — correct: my refusal fires pre-frame at the operator boundary, so only in-`FRAME_MAX` objectives ever reach a frame. Missing/malformed ref ⇒ fail-closed (the member is REQUIRED at my bytes — consistent).
2. **The Tier-0 re-materialization on byte-identical re-emit — CONFIRM.** Their §7 replacement semantics (worker :88/:155–156) rebuild the objective from the re-carried ref — never from a transcript reload — and their E16 replay cell states it: a replacement re-carries the byte-identical ref ⇒ the SAME objective re-materializes, no second task identity, idempotent. That is my R38-F1 re-emission rule consumed exactly (the value's source = my committed `turns` row; the timing = re-admission).
3. **`parked_unknown` flag-1 handling — CONFIRM, and their owed note DISCHARGES here.** The worker's §5 item 1 realizes the D-4 disclosure via the lifecycle §2.6 two-gate consumer over the exact member set and asked master to verify the m-10 consumer-confirmation at routing — this leg is that routing: the member set at their bytes is exactly my §B.2 closed row shape (byte-carried through r37–r40, which touched the D-4 section only to ADD `admission_ref` alongside `parked_unknown`), and the two-gate pre-work sequencing is the confirmed realization from the stage-3 reciprocals. Discharged; no residue.
4. **The process skeleton vs my supervision rows — CONFIRM, compatible split, residual honest.** Their §1.4: spawn-inherited CTRL sockets; on CTRL-W EOF the worker signals its tool process-group (`setpgid`/`killpg`, SIGTERM→grace→SIGKILL), reaps within `TOOL_TERMINATE_DEADLINE`, parks unproven children `UNKNOWN_TOOL_OUTCOME`/`PARTIAL_TOOL_EFFECT` (never "completed"), then EXITS — no reconnect, no surviving-generation rediscovery: my §B.3 fail-closed-on-EOF consumed from the child side. The division of reaping is clean against my census: I reap THEM (my `m10-child-terminate` row, `waitpid` the independent observer); they reap their tool children inside their own boundary. **The daemon-escape residual (`setsid`/double-fork) is named honestly at their bytes as an ungoverned local effect** (amendment §3/H-12, Step-4 deferral) — the same acceptance class as my census's bash-ambient row; no conflict, no overclaim.

**PART B — the reciprocal DELTA over half r21 × my r40 (the prior three-identity reciprocal re-bound; only the new member verifies fresh):**

- **The F84-era items RE-CITE as byte-identical survivors:** the three identities (#1 frozen authority at mint · #2 the point-A pre-consume comparand on the four-field consume wire · #3 the point-B pre-invocation recompute), the two derivation points, and the three mutation fixtures stand at r21 exactly as verified in my `223500` three-identity reciprocal — their r20/r21 status preserves the survivor citations, and my r37–r40 delta touched §B.2's D-4 bullet set, the §F `turns` row, and the sizing gate ONLY: §D.3/§D.4 are byte-identical from r36 into r40. Re-cited, not re-verified — per your delta form.
- **The new member's seam, verified FRESH both directions:** their half §2.2 consumer bullet (:91) consumes presence + shape + discipline exactly as r40 carries them — REQUIRED-never-absent · the closed two-kind union · written in my admission commit (the durable linearization) with post-commit emission · the byte-identical replacement re-carry (task identity stable across generations) · the §1.6/§2.5 fence and ADMITTED gate governing the member unchanged · the never-observes-the-refusal statement (my R40-F1 boundary) — and their scope line splits ACQUISITION to the worker doc's E16 (named, not designed, in the half — the clean two-artifact split). Their §5 census (:262) enumerates `turn_open{…, admission_ref, parked_unknown}` and binds my r40 at the exact hash; their §7 binding row likewise. **H-14 closes both directions:** my emission (the admission commit + post-commit send, my r40 §B.2/§F rows) ↔ their consumption (the half's presence/discipline row + the worker's E16 acquisition row). No orphan, no imported token.

**ZERO findings. Leg 1 CONFIRMS and the reciprocal delta CONFIRMS at the named hashes; nothing re-opens from this seat.**

## Verification
- Recomputed this session: worker r7 `cb7ff970…45` · half r21 `4d3bd14e…bd` (both exact, matching their cited approvals) · my r40 `d2ce9831…` + stage-5 r10 `6fd1d655…` (frozen; untouched by this return).
- Loci verified by line: worker E16 :204 (the full 20-field row) · :156 objective acquisition · :88/:155 replacement semantics · :163 §5 item 1 · :32 §1.4 EOF/teardown; half :91 (the consumer bullet) · :262 (the census) · :313 (the binding row) · the r20/r21 status survivor citations.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-stage4-m9/RECONCILE-planner-20260720-194500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this confirmation relay + one INDEX.md row timestamped 20260720-194500; no design-doc edit (all four artifacts stay frozen), no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: master carries this leg + the reciprocal delta into the stage-6 lock packet; m-10.planner stands by for any remaining cross-leg questions, then the stage-6 Master+VP interface-lock.
