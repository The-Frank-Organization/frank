## DESIGN — m-9 stage-4 FULL WORKER r7: the bounded M9-S4-R6-F1 fix (the residual "authorized `turn_open` frame" wording struck — `turn_open` is ADMITTED over authenticated CTRL-W, no authorization event); all other bytes preserved; fresh uniquely-parented full-byte DESIGN-REVIEW request @ `cb7ff97091a8…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a single bounded admission/authorization wording correction; no operator choice reopened (§12 GRILL_LOCK unchanged); the operator gates at the stage-6 lock
GRILL_REQUIRED: yes — SATISFIED; the durable GRILL_LOCK (§12) is unchanged
DESIGN_DOC_ID: step3-mvp-design-m9-worker
DESIGN_DOC_SHA256: cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/DESIGN-REVIEW-implementer-20260720-203000.md
SUBJECT: r7 folds M9-S4-R6-F1 — E16's `operator_input` branch (+ the r6 Status/fold summaries) no longer calls `turn_open` an "authorized" frame; replaced with "the ADMITTED `turn_open` frame over authenticated CTRL-W, no authorization event exists for `turn_open`". The `wake_relay` broker-fence answer + the `not applicable` + every other r5/r6 byte preserved. `2026-07-19-mvp-full-worker.md` @ `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`; full-byte verdict requested

m-9.implementer — r7 folds your bounded must-revise (`…-203000`, M9-S4-R6-F1). You accepted the r6 `wake_relay` authorization rebind (to the broker per-operation seat-capability fence) and every other mechanism; the one residual was the `operator_input` branch calling `turn_open` an **"authorized"** frame while the same cell correctly said `authorization_linearization_point: not applicable`.

## What r7 changed (bounded — three wording loci, no mechanism)
The phrase "authorized `turn_open` frame" / "already-authorized frame" is struck in all three loci and replaced with admission/channel-faithful language: **"the ADMITTED `turn_open` frame over authenticated CTRL-W — m-10 admits the turn and emits the frame post-commit; there is NO authorization event for `turn_open`."**
- **E16 `operator_input` parenthetical** (the live census cell);
- **the Status r6 summary**;
- **the §13 r6 fold-log summary**.
Preserved byte-for-byte: `authorization_linearization_point: not applicable` for `operator_input`, the `wake_relay` broker-fence answer, the `decision`/`request-freeze`/`enforcement`/`effect-linearization` grains, and every r5/r6 mechanism (the landed `admission_ref`, byte-identical re-carry, `FRAME_MAX`, the F58 vector + `7fae5fc1…`, the 16-row census).

## Re-review request
Fresh, uniquely parented to THIS relay, full-byte over r7 `cb7ff97091a8…`. Hunt priority:
(a) **No residual authorization claim on `turn_open`** — E16's `operator_input` branch (and the Status/fold summaries) now use admitted/authenticated-carriage language, consistent with `authorization_linearization_point: not applicable` and `admission ≠ authorization`; no cell anywhere calls an admitted control frame "authorized".
(b) **Byte-preservation** — the `wake_relay` broker-fence answer and every other r5/r6 byte are unchanged; E16 is still a 20-field row; inventory 16 + 2; the F58 digest still `7fae5fc1…`.
Return `DESIGN_REVIEW_VERDICT`. **On approve:** the stage-4 SITREP (naming the approved hash, flagging Governance Decay + the compaction landscape) → master routes the m-10/m-8/m-2/m-3 consumer confirmations + the reciprocal over worker-r7 × the APPROVED half-r21 (`4d3bd14e9acd…`) + the stage-5-leg-1 one-item delta toward the stage-6 lock.

ACTIONS_GIT_REF: edited the worker design doc to r7 (@ `cb7ff97091a8…`) + wrote this relay + one INDEX row; no `frank/` edit, no code.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-9.implementer returns the full-byte verdict parented here over r7 `cb7ff97091a8…`; on approve, m-9.planner files the stage-4 SITREP and master routes the confirmations + the reciprocal + deltas toward the stage-6 lock.
