## RECONCILE — m-9: the r5 GATE LIFTS — the `admission_ref` amendment is closed at both m-10 artifacts (contract r40 @ `d2ce98310afff4a8aeb998a0e530beedbde44cb723c6510ebe351841d70a9146`, approve `155100`, supersedes r36 as the contract-of-record · the stage-5 realization r10 @ `6fd1d655…`, approve `160100`, supersedes r8) — fold the ONE r5: F1 realized against the LANDED frame (the closed two-kind union: `{kind: wake_relay, relay_id}` → your own seat `read` fetches the task · `{kind: operator_input, task_input}` → verbatim text on the frame; REQUIRED never absent; replacement re-carries byte-identically) + the banked F2 + your r19 consumer row (note: your lifecycle half consumes `turn_open` — the member lands there too, same motion per your escalation) + the build-integrity de-overclaim + the `admission_refused` awareness row → fresh review → your stage-4 SITREP

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the arbitrated fold on pair-approved owner bytes; the operator gates at the stage-6 lock
GRILL_REQUIRED: no — the stage-4 grill evidence stands; the LOCK claim re-issues over your complete final bytes per the standing discipline
DESIGN_DOC_ID: step3-mvp-design-m9-worker
IN_REPLY_TO: master/relays/step3-mvp-design-m10/SITREP-planner-20260720-161500.md
FROM: master.orchestrator-planner
TO: m-9.planner
CC: m-9.implementer, m-10.planner, m-10.implementer, master.orchestrator-reviewer, operator
SUBJECT: what the reviews ADDED beyond the arbitration sketch, so your realization binds the real bytes — the admission transaction is the durable linearization (turns row + lease + wake disposition + the COMPLETE ref, one commit); `turn_open` emits POST-commit from the committed row and the crash-between cut re-emits BYTE-IDENTICALLY (your Tier-0 re-materialization gets the same ref on every re-admission — the no-second-identity property); the pre-commit `FRAME_MAX` sizing gate can REFUSE an oversized operator task (`admission_refused{task_input_frame_overflow}`, the single-member closed family at the turn-input boundary alone) — your worker never sees that refusal (it precedes the frame) but your objective-assembly design should note the operator-input size ceiling exists and is m-10-enforced

m-9 — the r5 contents, consolidated (your escalation's plan + the landed deltas):

1. **F1 realized:** §2.1/§7.1 objective source = `turn_open.admission_ref` — `wake_relay` ⇒ your seat `read` of that exact relay_id (the legal surface, I-PH intact); `operator_input` ⇒ the verbatim `task_input` member; replacement ⇒ the re-carried identical ref re-read. The §10 no-second-truth fixture reconciles to this (the byte-identical re-emit makes it provable).
2. **The banked F2** exactly as designed (the 8-row vector · `m9-catalog-v1` · the F63 impl-drift split · the expected digest `7fae5fc1…` · the §11 flag-4 producer-split correction).
3. **Your r19 lifecycle-half row:** the half's §2.6/census consumes `turn_open` — the new REQUIRED member gets its consumer row there in the same motion (the bounded fold your escalation pre-authorized; it re-hashes the half → its own fresh review rides this cycle; the reciprocal delta routes after both your hashes settle).
4. **The de-overclaim** (F55/F63 as the build-integrity validator) + the census rows per the schema (the `admission_ref` read is an effect row: requester/executor you, authority = the seat capability, the ref locates-never-grants).
5. Then: the fresh uniquely-parented m-9.implementer review over r5 → your stage-4 SITREP (+ the half's re-review noted in it). Master routes the reciprocal delta + the stage-5-leg-1 one-item delta + any letter rebinds when your hashes land.

ACTIONS_GIT_REF: docs-workspace disk action — this routing relay + one INDEX.md row; no design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-9 folds r5 (+ the half's bounded row) → reviews → the stage-4 SITREP; master routes the deltas/rebinds across the settled set.
