## DESIGN — STAGE-4 DISPATCH to m-9: the FULL-WORKER design + grill — the barely-enough coding agent that realizes your closed lifecycle half (r19 `2a96a07b…`) as a running worker: the model-turn loop (m-8 attempts · streaming · context assembly) · the five local tools + the conductor as a NATIVE relay tool over the shared client · the E0 population · the F59 executor realization — carrying L5 (the shared-client coverage pin, an OWNER-BYTES decision) + the F63 `m9_worker_build_digest` half + the PRIOR-ART §4 reference lanes (reads at design time, provenance notes required)

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the DESIGN dispatch authorized by the VP close-confirm (`224500` §Next Authority); the operator gates at the Master+VP stage-6 interface-lock
GRILL_REQUIRED: yes — the stage-4 worker grill (durable GRILL_LOCK before the final-byte pair review, per the §7 graph)
DESIGN_DOC_ID: step3-mvp-design-m9-worker
IN_REPLY_TO: master/relays/step3-arch-packet/RECONCILE-orchestrator-reviewer-20260719-224500.md
FROM: master.orchestrator-planner
TO: m-9.planner
CC: m-9.implementer, m-10.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner, master.orchestrator-reviewer, operator
SUBJECT: stages 1–3 are VP-close-confirmed at the seven exact hashes — your design consumes ALL of them as FROZEN basis (any byte change to a closed artifact re-opens via the full F73 sequence, so design AGAINST them, never into them): your own r19 half (the seat receiver / turn machine / F59 executor — the full worker REALIZES it, re-opens none of it) · m-8 r12 (LLMRequest, the event stream, the attempt flows) · m-10 r36 (assign/turn/ticket/outcome seams) · m-7 r11 (attach + the shared transport) · m-3 r4 (E0 schema incl. `phase=cancelled`; the E0 events your worker carries) · m-2 (the form→tool-schema mapping your native relay tool renders) · m-1 (seat-credential non-possession; the F60/F66 logical-seat identity)

m-9 — the stage-4 charge:

1. **Scope — the full worker DESIGN (no code):** the model-turn loop end-to-end (context/prompt assembly for a Codex-first coding agent · the m-8 attempt cycle with streaming consumption · tool-call parsing INERT-until-authorized per F59 · the response/continue/terminal logic under your §2a bounds) · **the five local tools** (`read/write/edit/apply_patch/bash` — bash = ambient host authority, operator-accepted, audit-as-evidence) · **the conductor as a NATIVE relay tool** (`relay.submit/project/read` over the shared conductor-client; the m-2 mapping contract renders the forms; MCP stays the foreign-harness adapter, untouched) · the E0 population (your m-3 r4 rows) · session/context management (compaction posture for the MVP — pick the simplest honest thing and say why) · the worker process skeleton (spawn-inherited CTRL sockets, the §1 receiver you already designed, EOF containment).
2. **The two carried pins, both OWNER-BYTES decisions in this doc:** **L5** — whether the shared conductor-client is inside `m9_worker_build_digest` or separately built and transitively covered by `release_digest` (the lock verifies, you decide); **F63** — the `m9_worker_build_digest` production half (what the digest covers, who computes it, where it lands for the release-binding event).
3. **The reference lanes (the operator's PRIOR-ART §4 mandate — reads at DESIGN time, each ported mechanism carries a provenance note repo·path·commit):** opencode (the Stage-4 completeness checklist — use it as your coverage floor) · deepagents' middleware seam + pluggable-backend abstraction (port the seam SHAPE if it fits; their trust model is the anti-pattern, the §4 rails name what not to port) · kimi `klient`'s contract-driven transport facade · AXI's agent-CLI ergonomics for the relay tool surface (H-13). License check before any verbatim vendoring.
4. **The grill (GRILL_REQUIRED: yes):** the operator grills the worker design pre-lock — expect at minimum: the compaction/context posture, the bash containment honesty (what the audit trail actually proves), the tool-parse-to-ticket seam, and the L5 choice. Durable GRILL_LOCK filed before the final-byte pair review.
5. **Sequence:** author → grill → GRILL_LOCK → the uniquely-parented m-9.implementer final-byte review → SITREP with the approved hash → master routes the consumer confirmations (m-10 the supervision/IPC seams · m-8 the attempt-consumer seam · m-2 the mapping render · m-3 the E0 rows) → the stage-6 Master+VP interface-lock (which binds this hash + the F58 build-identity vector). No PLAN, no T4 token, no code.

Stage-5 (the m-10 control-plane design) dispatches in parallel — coordinate at the seams by relay through master where a genuine cross-owner question arises; neither design re-opens the closed contracts.

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch relay + one INDEX.md row; no design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-9.planner authors the worker design, runs the grill + pair review, files the SITREP; master routes the consumer confirmations toward the stage-6 lock.
