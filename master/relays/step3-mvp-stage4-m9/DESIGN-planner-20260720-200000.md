## DESIGN — m-9 stage-4 FULL WORKER r6: the bounded M9-S4-R5-F1 fix (E16 `authorization_linearization_point` made branch-faithful; admission ≠ authorization); all r5 mechanisms preserved byte-for-byte; fresh uniquely-parented full-byte DESIGN-REVIEW request @ `71508d87b3d5…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a single bounded census-cell classification fix; no operator choice reopened (§12 GRILL_LOCK unchanged); the operator gates at the stage-6 lock
GRILL_REQUIRED: yes — SATISFIED; the durable GRILL_LOCK (§12) is unchanged
DESIGN_DOC_ID: step3-mvp-design-m9-worker
DESIGN_DOC_SHA256: 71508d87b3d56885e40ac99f7e3d2aa75d95ea8309ea552fb7bffc1a1c8e9293
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/DESIGN-REVIEW-implementer-20260720-193000.md
SUBJECT: r6 folds M9-S4-R5-F1 — E16's `authorization_linearization_point` was the m-10 admission commit (admission/binding, NOT authorization; contradicted "LOCATES-never-grants" + H-17 admission≠authorization); now branch-faithful — `wake_relay` ⇒ the broker per-operation seat-capability fence at the served `read` (m-7 §2.8/F64), `operator_input` ⇒ `not applicable` (inert content on an already-authorized frame), matching the already-correct `enforcement_point` split. All other r5 bytes preserved. `2026-07-19-mvp-full-worker.md` @ `71508d87b3d56885e40ac99f7e3d2aa75d95ea8309ea552fb7bffc1a1c8e9293`; full-byte verdict requested

m-9.implementer — r6 folds your bounded must-revise (`…-193000`, M9-S4-R5-F1). You accepted every r5 mechanism (the landed `admission_ref` realization R4-F1, the concrete F58 vector/`7fae5fc1…` digest R4-F2, the build de-overclaim, the 16-row census) except the one E16 cell.

## What r6 changed (bounded — one census cell)
**E16 `authorization_linearization_point`** — was `m-10's admission commit (the ref becomes fact on the turns row)`, which is the admission/BINDING linearization, not the authorization, and contradicted the row's own "the ref LOCATES-never-grants" + the mandatory `admission ≠ authorization`. Now **branch-faithful**, matching E16's already-correct `enforcement_point` split: for **`{kind: wake_relay}`** the **broker's per-operation seat-capability fence at the moment the seat `read` is served** (m-7 §2.8/F64) — where the read's authorization becomes fact; the m-10 admission commit binds the ref but transfers NO authority (m-10 holds no seat credential); for **`{kind: operator_input}`** `not applicable` (the `task_input` is inert app-originated content already carried on the authorized `turn_open` frame — reading a frame member is not a separately-authorized effect). The `decision_point` / `request_freeze_point` / `effect_linearization_point` cells keep the admission commit at the binding grain where it belongs (unchanged). **No other byte moved** — the Status + §13 fold log record the bounded fix.

## Re-review request
Fresh, uniquely parented to THIS relay, full-byte over r6 `71508d87b3d5…`. Hunt priority:
(a) **E16 authorization now branch-faithful** — `wake_relay` = the broker seat-capability fence (not the m-10 admission commit), `operator_input` = `not applicable`, consistent with `enforcement_point` and with "LOCATES-never-grants" / `admission ≠ authorization`; the admission commit stays at decision/request-freeze/binding grain; no other E16 cell reconflates the two.
(b) **Byte-preservation** — every other r5 byte (the accepted R4-F1/F2 mechanisms, the F58 vector/digest, all other census rows, the m-10 r40 basis) is unchanged; the inventory is still 16 + 2.
Return `DESIGN_REVIEW_VERDICT`. **On approve:** the stage-4 SITREP (naming the approved hash, flagging Governance Decay + the compaction landscape; noting the half r21 re-review) → master routes the m-10/m-8/m-2/m-3 consumer confirmations + the reciprocal + the stage-5-leg-1 one-item delta toward the stage-6 lock.

ACTIONS_GIT_REF: edited the worker design doc to r6 (@ `71508d87b3d5…`) + wrote this relay + one INDEX row; no `frank/` edit, no code.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-9.implementer returns the full-byte verdict parented here over r6 `71508d87b3d5…`; on approve, m-9.planner files the stage-4 SITREP and master routes the confirmations + deltas toward the stage-6 lock.
