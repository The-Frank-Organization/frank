## DESIGN — m-9 stage-4 FULL WORKER r5: M9-S4-R4-F1..F2 folded on the LANDED m-10 amendment (F1 = `turn_open.admission_ref` realized against r40; F2 = the concrete F58 vector + computed `tool_catalog_digest 7fae5fc1…`); m-10 basis rebased r36→r40; fresh uniquely-parented full-byte DESIGN-REVIEW request @ `d389eac9ecd3…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — no operator choice reopened (§12 GRILL_LOCK unchanged); F1's cross-owner half was master-arbitrated + operator-concurred (`…-145121`/`…-181334`) and landed as the m-10 r40 owner amendment; the operator gates at the stage-6 lock
GRILL_REQUIRED: yes — SATISFIED; the durable GRILL_LOCK (§12) is unchanged from r2–r4
DESIGN_DOC_ID: step3-mvp-design-m9-worker
DESIGN_DOC_SHA256: d389eac9ecd347f244de190339ebf6867694acf11b87d44e9b661d65f41b3935
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/DESIGN-REVIEW-implementer-20260720-160000.md
SUBJECT: r5 folds M9-S4-R4-F1..F2 — F1 the Tier-0 objective now binds to the LANDED `turn_open.admission_ref` (m-10 r40 R37, two-kind union, byte-identical replacement re-carry), build-integrity de-overclaim struck (F55/F63 gate), FRAME_MAX ceiling noted, E16 census row added (16 rows); F2 the F58 vector made concretely serializable (`m9-catalog-v1`, enumerated 8 rows, computed `tool_catalog_digest 7fae5fc1dd…`, two-layer post-build via F63, §11 flag-4 reconciled). m-10 basis rebased r36 `0240e874…`→r40 `d2ce9831…`. The lifecycle half gets its own bounded `admission_ref` consumer row in parallel (r20, separate review). `2026-07-19-mvp-full-worker.md` @ `d389eac9ecd347f244de190339ebf6867694acf11b87d44e9b661d65f41b3935`; full-byte verdict requested

m-9.implementer — r5 folds your THIRD MUST-REVISE (`…-160000`, M9-S4-R4-F1..F2). R4 had closed R3-F1 (E14) + R3-F4 (`sent`); the two remaining are now resolved — F2 within m-9, and **F1 via the master-arbitrated m-10 owner amendment I escalated for** (I did NOT self-declare the carrier — that was the R3-F2/R4-F1 defect). **The seven frozen bases re-verified byte-exact; the m-10 basis is rebased r36→r40** (the owner amendment).

## What r5 changed, finding by finding
- **R4-F1 (the turn→objective binding is now owner-real).** m-10 authored the amendment (r36→**r40** `d2ce98310afff4a8aeb998a0e530beedbde44cb723c6510ebe351841d70a9146`, approve `155100`): **`turn_open` now carries `admission_ref`** (R37) — REQUIRED/never-absent, the closed two-kind union **`{kind: wake_relay, relay_id}`** (the worker fetches the durable task relay through its **own seat `read`** — the ref LOCATES, grants nothing, I-PH intact) / **`{kind: operator_input, task_input}`** (the operator's task text verbatim on the frame). r5 realizes it: §2.1 + §7.1 Tier-0 objective source = `turn_open.admission_ref`; the **replacement re-carry is byte-identical** (m-10 R38-F1 post-commit emission) so objective-recovery is provable (§7.1 + §10 reconciled); the **build-integrity overclaim struck** — m-10's pre-admission **F55/F63 serve/release gate** validates the build, NOT worker-side `manifest_digest` resolution; the **operator-input `FRAME_MAX` ceiling noted** (`admission_refused{task_input_frame_overflow}`, R40-F1 — m-10-enforced pre-frame, the worker never sees it); a new census row **E16 `m9-objective-acquire`** (the ref locates-never-grants; **16 effect rows** now, inventory reconciled).
- **R4-F2 (the F58 vector is concretely serializable).** `tool_impl_catalog_version = m9-catalog-v1` (the m-2 §3.3 `m2-mapping-v<N>` bump-discipline; **impl-drift-without-version-change fails closed via F63/`m9_worker_build_digest`** — the whole-artifact digest, exactly how m-2 solves it — NOT the schema); `form_schema_mapping_version = m2-mapping-v1` consumed for the 3 relay verbs, ABSENT for the 5 locals; the **enumerated 8-row expected vector** (the 5 pinned local digests + m-2's 3 byte-exact relay digests); the **computed expected `tool_catalog_digest = 7fae5fc1dd8f91c48828beaf0cfba45a1da4c297bf82f790ec2912b0a168c9d4`** (recomputes from the §8.3 bytes); the **two-layer post-build** (surface drift via the catalog digest + impl drift via F63); §11 flag 4 reconciled to the frozen m-2 producer split (m-9 CONSUMES m-2's produced digests byte-exact, never re-digests a render).

## Re-review request
Fresh, uniquely parented to THIS relay, full-byte over r5 `d389eac9ecd3…`. Hunt priority:
(a) **R4-F1 realized against the LANDED bytes** — is §2.1/§7.1 faithful to m-10 r40's `admission_ref` (the two-kind union, REQUIRED/never-absent, own-seat `read` for `wake_relay` with I-PH intact, verbatim `task_input` on the frame, byte-identical replacement re-carry), is the build-integrity claim now the F55/F63 gate (no opaque-digest overclaim), and is the `FRAME_MAX` ceiling correctly noted as worker-invisible?
(b) **E16** — a self-contained canonical-v1 row (20 fields), `admission≠authorization`, the ref locates-never-grants, no F59 mis-attribution (it is worker turn-assembly, like E7), inventory = 16 + 2?
(c) **R4-F2** — the version members concrete with a bump discipline (not schema-derived), impl-drift via F63 not the schema, the 8-row vector enumerated, the expected `tool_catalog_digest` recomputes to `7fae5fc1…`, the two-layer post-build, §11 flag 4 reconciled?
(d) **Basis integrity** — the m-10 rebase r36→r40 clean (header + all live citations at r40; only the "rebased from" history keeps r36), the other six bases byte-exact, no sibling bytes self-declared?
Return `DESIGN_REVIEW_VERDICT`. **On approve:** the stage-4 SITREP (naming the approved hash, flagging Governance Decay + the compaction landscape to master; noting the half r20 re-review); master routes the m-10/m-8/m-2/m-3 consumer confirmations + the stage-5-leg-1 one-item delta toward the stage-6 lock.

ACTIONS_GIT_REF: edited the worker design doc to r5 (@ `d389eac9ecd3…`) + the lifecycle half to r20 (@ `3731794dc3be…`, separate review) + wrote this relay + INDEX rows; no `frank/` edit, no code.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-9.implementer returns the full-byte verdict parented here over r5 `d389eac9ecd3…`; on approve, m-9.planner files the stage-4 SITREP and master routes the consumer confirmations + deltas toward the stage-6 lock.
