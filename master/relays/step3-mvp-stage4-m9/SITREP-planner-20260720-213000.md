## SITREP — m-9 stage-4 FULL WORKER design APPROVED (worker r7 `cb7ff970…` + lifecycle half r21 `4d3bd14e…`, both pair-approved zero findings): the barely-enough coding-agent-on-the-governed-courier is DESIGN-complete on m-9's side. Flagging Governance Decay + the compaction landscape to master (PRIOR-ART + FRANK-HARDENING-BACKLOG); naming the consumer confirmations + reciprocal + stage-5-leg-1 delta master routes toward the stage-6 lock.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report of a pair-approved design outcome; the operator gates at the stage-6 Master+VP interface-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m9-worker
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/DESIGN-REVIEW-implementer-20260720-211500.md
FROM: m-9.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-9.implementer, m-10.planner, m-10.implementer, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-mvp-stage4-m9/SITREP-planner-20260720-213000.md
SUBJECT: stage-4 full-worker DESIGN pair-approved on both m-9 docs; the owed-to-master flags (Governance Decay + compaction landscape) + the routing set (m-10/m-8/m-2/m-3 consumer confirmations, the worker-r7 × half-r21 reciprocal, the stage-5-leg-1 one-item delta) toward the stage-6 lock

master — the m-9 stage-4 full-worker DESIGN is **pair-approved on both docs, zero findings**, after five bounded review rounds (r2→r7) + one escalated cross-owner arbitration (the `admission_ref` amendment you routed to m-10). Handing back for the consumer-confirmation + reciprocal round toward the stage-6 lock.

## Approved bytes
- **Worker (full worker):** `2026-07-19-mvp-full-worker.md` **r7 @ `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`** (approve `step3-mvp-stage4-m9/DESIGN-REVIEW-implementer-20260720-211500`, zero findings).
- **Lifecycle half:** `2026-07-17-mvp-lifecycle-half.md` **r21 @ `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`** (approve `step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260720-203100`, the bounded `admission_ref` consumer fold).
- **Consumed frozen bases (re-verified byte-exact):** m-10 **r40** `d2ce9831…` (the `turn_open.admission_ref` amendment) · m-8 r12 `4b670a79…` · m-7 r11 `9331ea88…` · m-3 r4 `009df607…` · m-2 `83d8e63e…` · m-1 `7c8b09a6…` · H-17 schema `ea173abc…`. F58 expected `tool_catalog_digest = 7fae5fc1dd8f91c48828beaf0cfba45a1da4c297bf82f790ec2912b0a168c9d4` (independently recomputed by the reviewer).

## What the design delivers (the MVP coding agent on the governed courier)
The process skeleton + model-turn loop + five local tools (`read`/`write`/`edit`/`apply_patch`/`bash`) + the conductor as a native relay tool + the **uniform F59 executor** (all 8 tools, one central settlement, no carve-outs) + E0 population + the **governed 3-tier compaction** + the L5=B / F63 / F58 build-identity. The `admission_ref`-bound Tier-0 objective, the three-identity/two-derivation-point F84 guard, the honest bash/teardown residual, and the 16-row + 2-rationale H-17 census all landed. **No PLAN, T4 token, code, credential, or provider action** — DESIGN only.

## Owed-to-master FLAGS (per the design's SITREP obligation) — for the PRIOR-ART register + FRANK-HARDENING-BACKLOG
1. **Governance Decay (arXiv 2606.22528) — a hardening/architecture-validation finding.** A context compactor that drops a standing policy/constraint pushes prohibited-action violations from **0% → 30–59%**; pinning the constraint restores 0%. The load-bearing implication for frank: its **authorization governance already lives OUTSIDE the model context** (F59 tickets + the run manifest + seat identity, re-checked authoritatively every dispatch), so a compaction-induced *confusion* about authority **cannot** cause an unauthorized effect — frank is **structurally immune to the worst failure mode** the paper describes. The worker's Tier-0 pinning (policy/objective/constraints authoritative every call, never through the summarizer, + the §7.3 post-rewrite survival check) protects the residual **confusion layer** (the user constraints/objective the model acts on). **This validates frank's governance-outside-the-summarizer architecture as a first-class safety property** — recommend it lands in `master/PRIOR-ART.md` (the compaction lane) + `master/FRANK-HARDENING-BACKLOG.md` (as a proven-by-literature architectural strength, not just friction).
2. **The compaction landscape — PRIOR-ART material.** An 8-harness field survey (opencode `cd46f22d5` · deepagents `ae96a90` · kimi `df689955` · axi `fbfe6d2` · codex `678157acaa` · claude-code `6a25909` · jcode `9e4c00f7` · oh-my-pi `39c95e5e2` · pi `94373d81`) + an arxiv/industry sweep converged on: **tool-result eviction** (Anthropic context-editing `clear_tool_uses` / claude-code microcompact) as the cheapest coding-native reversible win (the filesystem is the store — re-read on demand, itself F59-recorded); the **structured-typed-lite summary template** (pi `compaction.ts`) at ≈prose cost but locator-preserving; two universal must-haves (tool-call/result boundary protection; reactive drop-oldest-retry on overflow). frank's governed 3-tier (pinned / evict / summarize) + the explicit anti-adoption of **opaque provider capsules** (un-auditable + lock-in + `store:false`) and **LLMLingua-style token-dropping** (dangerous on exact strings — traces/diffs/JSON). Recommend the survey + the anti-adoption rails land in `master/PRIOR-ART.md`.

## The routing set master carries toward the stage-6 lock
- **Consumer confirmations owed (the four the design names, §11):** **m-10** (supervision/IPC + the `parked_unknown` flag 1 + the `admission_ref`/E16 realization against r40) · **m-8** (the attempt-consumer + the `stream_lost`/no-`attempt_stream_end` enum flag 2 + the **§6.1 disposition set** + the **`sent`=`attempt_started` observability**) · **m-2** (the mapping render + parity flag 4 + the **§8.3 relay-tool digest/mapping-version consumption** — m-9 consumes m-2's produced `6bb7bbf4…`/`be5c41ec…`/`a84645cb…` + `m2-mapping-v1` byte-exact) · **m-3** (the **§6.1 total E0 table** + the **`sent` token semantics** [m-9 proposes `sent`=observed `attempt_started`, meaning "connector authorized+started", NOT a wire-crossing claim] + the `phase` tokens).
- **The one cross-owner OPEN carry (F4 fallback, named not built):** if m-3 requires `sent` to mean a true provider-wire-crossing, that escalates to an **m-8-authored wire-crossing carrier amendment** (m-8 r12 carries no per-attempt wire-crossing fact to m-9). Flagged for the m-3/m-8 confirmations to resolve.
- **The reciprocal:** the fresh complete reciprocal over the settled **worker-r7 × half-r21** pair (the half's r19→r21 `admission_ref` rebind into the worker header rides this — I kept the worker self-basis at r19 per the reviewer's separate-lineage framing; the rebind is yours to route).
- **The stage-5-leg-1 delta:** my stage-5 supervised-counterparty confirmation (`step3-mvp-confirm-m9/RECONCILE-planner-20260720-140000`) gains the one-item delta over the new pair (the `admission_ref` seam) — you named this to route with the rest.

ACTIONS_GIT_REF: docs-workspace disk action — this SITREP + one INDEX.md row; the approved worker doc UNCHANGED at `cb7ff970…`, the approved half UNCHANGED at `4d3bd14e…`; no `frank/` action, no code.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: master folds the two flags into PRIOR-ART + the hardening backlog and routes the m-10/m-8/m-2/m-3 consumer confirmations + the worker-r7 × half-r21 reciprocal + the stage-5-leg-1 delta toward the stage-6 Master+VP interface-lock (which binds these hashes + the F58 build-identity vector). m-9 holds for that round.
