## DESIGN — m-9 stage-4 FULL WORKER r3: the M9-S4-R2-F1..F4 must-revise folded (census→self-contained canonical rows + push/wake split; the transcript no-second-truth correction; the F58 catalog build; the total E0 mapping); fresh uniquely-parented full-byte DESIGN-REVIEW request @ `060b460814d7…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — no operator choice reopened; the §12 GRILL_LOCK stands unchanged; all four folds are m-9-owned completeness/conformance corrections; the operator gates at the Master+VP stage-6 interface-lock
GRILL_REQUIRED: yes — SATISFIED; the durable GRILL_LOCK (§12) is unchanged from r2
DESIGN_DOC_ID: step3-mvp-design-m9-worker
DESIGN_DOC_SHA256: 060b460814d749e1a87af656810596ae2b8949486ba01836512a81f8616f174a
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/DESIGN-REVIEW-implementer-20260720-133815.md
SUBJECT: r3 folds M9-S4-R2-F1..F4 — F1 §11.5 census now 15 self-contained canonical-v1 rows (E1–E15, push/wake split, all 20 fields, counts reconciled); F2 the "durable transcript" corrected to the in-memory derived turn-scoped artifact (no second canonical truth, lifecycle r19 §2.6); F3 the m-9-owned F58 tool-catalog build added (§8.3); F4 §6.1 made a total executable disposition→E0 mapping (the `sent` emission point + the total terminal table + the no-emission/conditional cuts). Seven frozen bases re-verified byte-exact, none re-opened. `master/domains/m-9-model-runtime/design/2026-07-19-mvp-full-worker.md` @ SHA-256 `060b460814d749e1a87af656810596ae2b8949486ba01836512a81f8616f174a`; full-byte verdict requested

m-9.implementer — r3 folds your MUST-REVISE (`…-133815`, M9-S4-R2-F1..F4) in one revision. Every fold is m-9-owned; **no operator choice is reopened** (the §12 GRILL_LOCK is byte-unchanged), the six ratified grill decisions carry, and **the seven frozen bases are re-verified byte-exact and re-opened by none** (m-9 r19 `2a96a07b…`, m-8 r12 `4b670a79…`, m-10 r36 `0240e874…`, m-7 r11 `9331ea88…`, m-3 r4 `009df607…`, m-2 `83d8e63e…`, m-1 `7c8b09a6…`; H-17 schema `ea173abc…`).

## What r3 changed, finding by finding
- **F1 (§11.5 census — canonical + effect-complete).** Every effect is now a **self-contained canonical-v1 row with all 20 fields** — no "as E1" shorthand, no inherited/collapsed cells, no absent cells. Expanded: **E2** (was "As E1 except"), **E3** (was a mega-collapsed inherited clause), **E7** (added `policy_owner/policy_artifact`; split `decision_point`/`enforcement_point` and the authorization/effect linearization), **E9/E10/E12** (split `requester`/`executor` + `decision_point`/`enforcement_point`, added `policy_owner/policy_artifact`), **E11** (added the credential-holder + authorization-linearization cells), **E13** (added the request-freeze + authorization-linearization cells). **E14 split into two families** per F87: **E14** = `m9-wake-forward` (the worker→m-10 `wake_forward` schedule-insertion), **E15** = `m9-push-receive` (the inbound conductor→seat push, its own read/serve effect). **Inventory stated + reconciled:** 15 effect rows (E1–E15) + 2 non-effect rationales (handshake, compaction); the stale "13 effect rows" in §13 corrected (r2 was 14: E1–E14). A structurally-inapplicable cell carries `not applicable` (the schema's `ambient`/`none (self-reported)` precedent for legal honest values; documented in the §11.5 intro); a contract-silent cell carries exactly `unknown`/`not specified`/`residual`.
- **F2 (no second durable truth).** §2.1/§7.1: "the durable turn transcript" is corrected to the **in-memory, turn-scoped, derived** artifact (dies at the turn terminal/park, same custody class as the §2.8 replay-envelope) — preserving lifecycle r19 §2.6's no-second-truth rule. §7.1 now names the **canonical readable sources** (the worker's own authored conductor records via `project{view: audit}` + `read`; fresh admission), the **derived-only representation + digest**, the **replacement/crash no-reload** semantics (a crash starts a fresh turn — there is no reload boundary at which a stale summary could replay or a nondeterministic provider summary re-run), and the **exact Tier-0 comparison input** (the canonical-JCS pinned block re-materialized from the manifest instructions + admitted objective/constraints, byte-compared at §7.3). The §11.5 compaction non-effect rationale is made exact on derivation + reload.
- **F3 (§8.3 — the m-9-owned F58 catalog build).** Added at design grain: the per-tool identity vector `{canonical_name, tool_schema_digest, tool_impl_version, form_schema_mapping_version}` with **field applicability defined** (local tools author their own arg schema + digest, `form_schema_mapping_version = not applicable`; relay tools' schemas are **consumed from m-2 without re-owning them** — m-9 digests the m-2-rendered schema, `form_schema_mapping_version` = m-2's version); the **canonical encoding** (sort by `canonical_name` → JCS → SHA-256 ⇒ `tool_catalog_digest`); the T4 catalog-version producer; the **expected pre-build vector** the stage-6 lock binds; the **post-build registry verification** (the running registry is re-hashed + verified against the locked digest, m-10 §C.1, fail-closed). Bound into F63 (§8.2) consistently — `tool_catalog_digest` is an F63 output; the shared client is a material.
- **F4 (§6.1 — total executable E0 mapping).** §6.1 is now a total disposition→event mapping: the **`sent` non-terminal emission point pinned** (once, at the wire-crossing; never on a no-wire attempt), the **total terminal table** over the m-8 r12 dispositions (completed/observed-failure/policy-deny/local-reject/both cancellation cuts/live-loss-conditional), the **two no-emission cuts** (worker crash; DATA-P epoch-inert) and the **one conditional cut** (live-loss vs retirement) — `deny_reason` present iff `denied`, no event fabricated on any no-emission cut. Reflected in the **E12** census row + the **§10** fixtures; routed for m-3 (`sent` point + `phase` tokens) / m-8 (disposition set) confirmation (§11 flags 4/5).

## Re-review request
Fresh, uniquely parented to THIS relay, full-byte over r3 `060b460814d7…`. Hunt priority:
(a) **F1 assemblability + coverage** — is EVERY row (E1–E15) a standalone canonical-v1 row with all 20 fields (only `unknown`/`not specified`/`residual`/`not applicable` where honest), are the push/wake families split without erasing either boundary, do all inventory counts agree, and is `admission≠authorization` / `bypass=alternate-cause-not-failure-to-record` honored throughout?
(b) **F2 no-second-truth** — does §2.1/§7.1 introduce NO durable m-9 canonical store, name the exact canonical readable sources + the derived-only digest + the crash/replacement no-reload rule + the exact Tier-0 comparison input, and stay faithful to lifecycle r19 §2.6?
(c) **F3 catalog build** — is the F58 vector + field applicability (local mapping-version absent; relay consumed-from-m-2) + canonical encoding + expected vector + post-build verification complete at design grain, consuming m-2 without re-owning it, and bound into F63 consistently?
(d) **F4 total E0 mapping** — is the disposition→(`sent`, terminal, emission) mapping total and executable, is the `sent` emission point fixed, are the no-emission + conditional cuts correct (no fabrication), and does E12 + §10 match?
(e) **Basis integrity** — the seven frozen bases + the H-17 schema consumed byte-exact, no sibling's bytes self-declared, no closed contract re-opened.
Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the SITREP naming the approved hash (flagging Governance Decay + the compaction landscape to master for the PRIOR-ART register + FRANK-HARDENING-BACKLOG); master routes the m-10/m-8/m-2/m-3 consumer confirmations toward the stage-6 Master+VP interface-lock.

ACTIONS_GIT_REF: edited the worker design doc to r3 (@ `060b460814d7…`) + wrote this relay + one INDEX.md row; no `frank/` edit, no code.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-9.implementer returns the full-byte verdict parented here over r3 `060b460814d7…`; on approve, m-9.planner files the SITREP and master routes the consumer confirmations toward the stage-6 lock.
