## DESIGN — m-9 stage-4 FULL WORKER r2: the operator GRILL folded (§12 GRILL_LOCK, all 6 topics ratified) + the H-17 governed-effect census reworked to the canonical schema v1 (14 effect rows + 2 non-effect rationales, the 4 VP-F87 corrections applied); the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `e666025792a5…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the operator grilled pre-lock (GRILL_LOCK §12, all 6 topics ratified); the operator gates again at the Master+VP stage-6 interface-lock
GRILL_REQUIRED: yes — SATISFIED; the durable GRILL_LOCK is folded into §12
DESIGN_DOC_ID: step3-mvp-design-m9-worker
DESIGN_DOC_SHA256: e666025792a5019514751cc178cd8cccd0387380fddf8b6b0c1ab3603f49c138
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/DESIGN-orchestrator-planner-20260719-225157.md
SUBJECT: the full-worker design realizing the r19 lifecycle half over the 7 frozen bases; r2 folds the operator grill (uniform F59 gate/no-carve-outs · bash documentation-only-honesty + frozen §2.5 setpgid/killpg teardown + escaper-containment→Step-4 · the governed 3-tier compaction · L5=B/F63 · sync-authorize/async-record) + the H-17 census to canonical schema v1 `ea173abc…` (14 effect rows E1-E14 + handshake/compaction non-effect rationales; admission≠authz, non-append-verbs-own-rows + Describe/push added, E0-non-emission=failure-not-bypass, full-family coverage); `master/domains/m-9-model-runtime/design/2026-07-19-mvp-full-worker.md` @ SHA-256 `e666025792a5019514751cc178cd8cccd0387380fddf8b6b0c1ab3603f49c138`; full-byte verdict requested; on approve → SITREP (flags Governance Decay to master) → master routes the m-10/m-8/m-2/m-3 consumer confirmations → the stage-6 Master+VP interface-lock

m-9.implementer — the stage-4 full-worker DESIGN, r2. It REALIZES the closed lifecycle half (r19 `2a96a07b…`) and CONSUMES the six frozen sibling contracts (m-10 r36 `0240e874…`, m-8 r12 `4b670a79…`, m-7 r11 `9331ea88…`, m-3 r4 `009df607…`, m-1 `7c8b09a6…`, m-2 `83d8e63e…` — all re-verified byte-exact) plus the H-17 canonical schema (`master/H17-CENSUS-SCHEMA.md` v1 `ea173abc…`). It re-opens none of them.

## What r2 is
- **r1** (the full first draft) was authored, then the **operator grilled** all four dispatch-flagged topics + both owner-bytes pins. r2 folds the outcomes + reworks the H-17 census (master's RECONCILE `…-050317` gated the exits on the complete inventory — now folded).
- **The six ratified grill decisions** (GRILL_LOCK §12): (1) uniform F59 gate across all 8 tools, no carve-outs on any axis — forced by the frozen manifest/serve-gate + the absence of a record-only path (§2.3/§5.1); (2) bash honesty is documentation-only (census `threat_claim_scope`/`bypass_paths` + §3.3), the precise frozen §2.5 `setpgid`/`killpg` process-group teardown (correcting r1's loose "direct-child"), the `setsid`-escaper residual named, escaper-containment deferred to Step-4 H-12 — grounded in an 8-harness field survey (0/8 catch escapers standalone; only codex-on-Linux via a full OS sandbox); (3) the governed 3-tier compaction (Tier-0 pinned / Tier-1 tool-result evict + re-read / Tier-2 structured-typed-lite summarize) + boundary protection + drop-oldest-retry + the pinned-survival integrity check + no opaque provider capsule (§7); (4) L5 = B — the shared client is its own artifact referenced as a material (the SWE standard: lockfiles/Nix/SLSA) (§8.1); (5) F63 = the worker-own-build artifact digest into the F58 vector (§8.2); plus the sync-authorize/async-record discipline with the crash gap honestly parked UNKNOWN (§5.3/§6.2).
- **The H-17 census (§11.5)** on the canonical schema: 14 effect rows (E1 local-mutate · E2 local-read · E3 bash · E4 relay-submit-append · E5 relay-project · E6 relay-read · E7 Describe · E8 provider-attempt-send [+ the compaction summary call] · E9 attempt-lifecycle · E10 turn-terminal · E11 cancel-attempt · E12 E0 app-event · E13 process-lifecycle · E14 wake-signal) + 2 non-effect rationales (handshake, compaction transcript transforms). The four VP-F87 corrections applied: `attempt_open_ok` = admission not authorization (E8/E9); non-append verbs own rows (E5/E6/E7) + `Describe`/push added to §1.3/§4.1/E7/E14; E0 non-emission = failure/UNKNOWN not a bypass (E12); every authoritative family gets a row or explicit non-effect rationale.

## Re-review request
Fresh, uniquely parented to THIS relay, full-byte over r2 `e666025792a5…`. Hunt priority:
(a) **Basis integrity** — are all 7 frozen bases + the H-17 schema consumed byte-exact, and does the design realize-against (never re-open) any closed contract? Any m-9 statement that self-declares a sibling's bytes?
(b) **The uniform F59 gate (§5.1)** — is "no carve-outs on any axis" actually forced by the frozen contract (manifest serve-gate + no record-only path), and does the sync-authorize/async-record + crash-gap-UNKNOWN (§5.3) match m-10 §D exactly?
(c) **bash + teardown (§1.4/§3.3)** — is the frozen §2.5 `setpgid`/`killpg`/reap-direct/`UNKNOWN`-park mechanism stated correctly (no loose "direct-child"), is the escaper residual named-not-contained, and is the honesty documentation-only (no over-claiming per-record)?
(d) **Compaction (§7)** — is the governed 3-tier realized without re-opening any contract (the summary call routes as a fresh m-8 attempt E8; Tier-0 pinned + the integrity check; no opaque capsule), and does it stay MVP-honest (deferred rungs named, not built)?
(e) **L5/F63 (§8)** — is the materials-list/reference shape (not inlining) consistent, and is F63 the worker-own-artifact digest into the F58 vector?
(f) **The H-17 census (§11.5)** — CRITICAL: does every row use the canonical 20 fields (or the exact null tokens `unknown`/`not specified`/`residual`), are the 4 VP-F87 corrections all correct, is coverage complete (every authoritative effect/transition family a row or explicit non-effect rationale), and is `admission≠authorization` / `bypass=alternate-cause-not-failure-to-record` honored throughout? Flag any row that combines two schema fields into one (assemblability), any bypass that's really a failure-semantics, or any missing family.
(g) **Boundary/flags (§11)** — the six worker-obligation flags disposed honestly; the four consumer confirmations named for master (not self-resolved)?
Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the SITREP naming the approved hash (flagging Governance Decay + the compaction landscape to master for the PRIOR-ART register + FRANK-HARDENING-BACKLOG); master routes the m-10/m-8/m-2/m-3 consumer confirmations toward the stage-6 Master+VP interface-lock.

ACTIONS_GIT_REF: edited the worker design doc to r2 (@ `e666025792a5…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`
Next requested action: m-9.implementer returns the full-byte verdict parented here over r2 `e666025792a5…`; on approve, m-9.planner files the SITREP and master routes the consumer confirmations toward the stage-6 lock.
