## RECONCILE — stage-4 dispatch SUPPLEMENT to m-9 (additive, one requirement; re-opens nothing): the design doc must carry a CENSUS ROW for every governed effect it touches — H-17, adopted from the 2026-07-20 external audit as a stage-6 lock input

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an additive statement requirement on the already-issued dispatch; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the stage-4 grill stands as dispatched
DESIGN_DOC_ID: step3-mvp-design-m9-worker
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/DESIGN-orchestrator-planner-20260719-225157.md
FROM: master.orchestrator-planner
TO: m-9.planner
CC: m-9.implementer, master.orchestrator-reviewer, operator
SUBJECT: this asks you to STATE what you are already designing, not to design differently — for each governed effect your worker touches (the F59 tool dispatch · the provider attempt via m-8 · the relay verbs · the E0 emission), the design names in one row: the enforcement point · the exclusive credential/capability holder · the request freeze point · the authorization + effect linearization points · the outcome observer + canonical record · the KNOWN BYPASS PATHS (bash's ambient authority named honestly, per the accepted residual) · the failure/unknown semantics · the replay/idempotency mechanism; master assembles these rows into the machine-readable TCB/effect census at the stage-6 lock

m-9 — one addition to the `225157` charge, nothing else moves. The closed contracts already contain almost every answer (the F59 chain, the m-8 freeze→authorize→attach→send, your own EOF/crash rows) — the row format just makes them assemblable and makes "governed" a bounded claim. Where a row's honest answer is "no exclusive credential — ambient" (bash), write exactly that; the census exists to name residuals, not to hide them.

ACTIONS_GIT_REF: docs-workspace disk action — this supplement relay + one INDEX.md row; no design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: fold the census-row requirement into the working design; no separate return owed — the rows arrive with your design SITREP.
