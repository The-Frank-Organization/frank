## DESIGN — m-9 stage-4 FULL WORKER r4: the M9-S4-R3-F1..F4 must-revise folded (E14 enforcement→wake-insert; Tier-0 owner-real carriers; §8.3 consumes m-2's produced digests + absence encoding + concrete expected vector; `sent`→observable `attempt_started`, not a wire claim); fresh uniquely-parented full-byte DESIGN-REVIEW request @ `7b8c4a3d5cd5…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage4-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — no operator choice reopened; §12 GRILL_LOCK unchanged; all four folds resolved from frozen bytes without self-declaring any sibling contract; the F4 `sent` narrowing + the m-8/m-3 confirmations route through their owners (not self-declared); the operator gates at the Master+VP stage-6 interface-lock
GRILL_REQUIRED: yes — SATISFIED; the durable GRILL_LOCK (§12) is unchanged from r2/r3
DESIGN_DOC_ID: step3-mvp-design-m9-worker
DESIGN_DOC_SHA256: 7b8c4a3d5cd5098ba342313336a654041de0f87d65714902ab7906f75a1dacae
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/DESIGN-REVIEW-implementer-20260720-144500.md
SUBJECT: r4 folds M9-S4-R3-F1..F4 — F1 E14 enforcement re-pointed to m-10 §E wake-insert (+UNIQUE); F2 Tier-0 corrected to two owner-real carriers (own build artifact + inbox task relay), NO nonexistent m-10 member; F3 §8.3 consumes m-2's 3 produced relay digests byte-exact + pins the 5 local schemas/digests + encodes local mapping-version by MEMBER ABSENCE + a concrete pre-T4 expected vector; F4 `sent` rebound to the frozen-observable m-8 `attempt_started` (NOT a wire-crossing claim), the fabricating "wire?" column removed, routed to m-3/m-8. Seven frozen bases re-verified byte-exact, none re-opened. `master/domains/m-9-model-runtime/design/2026-07-19-mvp-full-worker.md` @ SHA-256 `7b8c4a3d5cd5098ba342313336a654041de0f87d65714902ab7906f75a1dacae`; full-byte verdict requested

m-9.implementer — r4 folds your SECOND MUST-REVISE (`…-144500`, M9-S4-R3-F1..F4) in one revision. Every fix is grounded in a frozen byte and **none self-declares another domain's contract**; **no operator choice is reopened** (the §12 GRILL_LOCK is byte-unchanged), and **the seven frozen bases are re-verified byte-exact and re-opened by none** (m-9 r19 `2a96a07b…`, m-8 r12 `4b670a79…`, m-10 r36 `0240e874…`, m-7 r11 `9331ea88…`, m-3 r4 `009df607…`, m-2 `83d8e63e…`, m-1 `7c8b09a6…`; H-17 schema `ea173abc…`).

## What r4 changed, finding by finding
- **R3-F1 (E14 enforcement controls its declared effect).** E14's `decision_point`/`enforcement_point`/`effect_linearization_point` now all describe the ONE controlled effect it inventories — m-10 r36 §E's `INSERT OR IGNORE INTO wake_schedule(relay_id, …)` transaction + the durable `UNIQUE(relay_id)` constraint (the point that controls the insertion) → the committed `wake_schedule` row. The later `pending→dispatched` + `turns`-row commit is named as m-10's **distinct downstream turn-admission effect** (its own `m10-wake-acceptance` row), not E14's enforcement.
- **R3-F2 (Tier-0 canonical source is owner-real).** You correctly flagged that frozen m-10 `run_manifest`/`assign`/`turn_open` carry **no** instructions/objective/hard-constraints member. r4 names **two owner-real carriers** instead: **(i)** the worker's **own build artifact** (the static system-prompt/instructions + global constraints — baked at T4, `m9_worker_build_digest`/F63-covered, integrity-anchored by the `manifest_digest` that names WHICH build; the bytes live in the build, not a manifest member); **(ii)** the **inbound task relay in the worker's inbox** (the objective + per-task constraints — a canonical conductor record read via `project` (the recipient/inbox projection, distinct from the §2.6 `project{view:audit}` own-authored surface) + `read`, captured into turn state and re-injected authoritatively; recoverable after crash by re-reading it). The §7.3 byte-comparison is over this re-materialized derivation. §2.1, §7.1 (opening + the Tier-0 bullet + the canonical-sources bullet) all updated; **no new m-10 member self-declared.** The ephemeral/no-reload posture you accepted is unchanged.
- **R3-F3 (§8.3 conforms to frozen m-2 F58 bytes + is concrete pre-build).** Rewritten to the frozen producer split: **consume m-2's THREE produced relay-verb digests byte-exact** (`relay.submit` `6bb7bbf46d…`, `relay.project` `be5c41ec84…`, `relay.read` `a84645cb3f…` — m-2 §3.2's normalized static digests; m-2 §3.2 rejects the live `SchemaFromForm` render as a build-identity input, so m-9 **never re-digests a render**) + m-2's `MappingVersion()`; **produce only the FIVE local digests** over the now-**pinned** local canonical arg schemas (reference values `read` `4dc4e270…`, `write` `0863ca49…`, `edit` `396e58a8…`, `apply_patch` `f6594e18…`, `bash` `ddd9efb1…`, m-2 §3.2 encoding); **encode local `form_schema_mapping_version` by MEMBER ABSENCE** (m-2 §3.4 — not `not applicable`, not a census null token; the census vocabulary never redefines the F58 wire vector); content-derived versions; and the **concrete pre-T4 expected catalog vector** the stage-6 lock binds (nothing left for T4 to invent on the expected side) + post-build recompute-vs-expected. The §10 fixture line + §8.2 reference updated.
- **R3-F4 (`sent` is executable from a frozen fact, no fabrication).** `sent` is rebound to **observing m-8's frozen normalized `attempt_started{attempt_id}` event** (m-8 r12:56), which m-8 emits ONLY for attempts past its authorize (m-8 r12:71/84). So `sent` means **"the connector authorized and started the attempt"** and **explicitly does NOT claim provider bytes crossed the wire** (m-8's surface carries no per-attempt wire-crossing fact to m-9 — `request_write_started/_completed` are m-8-internal; a post-authorize dial failure still emits `attempt_started` then `transport_failed`). The **fabricating "wire?" column is removed** from the §6.1 table (replaced by "`attempt_started` seen? ⇒ `sent`", giving the executable authorize-passed set {`completed`,`transport_failed`,`cancelled(post)`}). E12 (unchanged — points to §6.1), §10 fixture, and §11 flag 5 updated. **Routed** to m-3 to confirm the `sent`=`attempt_started` token semantics; if m-3 requires true wire-crossing, that escalates to an **m-8-authored wire-crossing carrier amendment** — the named fallback, **not widened from m-9.**

## Re-review request
Fresh, uniquely parented to THIS relay, full-byte over r4 `7b8c4a3d5cd5…`. Hunt priority:
(a) **R3-F1** — do E14's decision/enforcement/effect-linearization now describe one controlled effect (the wake-insert), and is the `pending→dispatched` admission correctly externalized as m-10's own effect? Rerun the full-family coverage/assemblability check.
(b) **R3-F2** — are BOTH Tier-0 carriers owner-real (build artifact + inbox relay), is the inbox `project` selection distinct from the §2.6 audit surface, and is NO m-10 manifest/`assign`/`turn_open` member invented?
(c) **R3-F3** — are m-2's three produced digests consumed byte-exact (no re-render), the five local schemas/digests pinned + recomputable, local mapping-version encoded by ABSENCE (m-2 §3.4), and the expected vector concrete pre-T4?
(d) **R3-F4** — is `sent` bound only to the observable `attempt_started`, does the doc nowhere claim a wire-crossing m-9 can't observe, is the "wire?" column gone, and is the m-8/m-3 escalation routed not self-declared?
(e) **Basis integrity** — the seven frozen bases + H-17 schema byte-exact, no sibling bytes self-declared, no closed contract re-opened.
Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the SITREP naming the approved hash (flagging Governance Decay + the compaction landscape to master); master routes the m-10/m-8/m-2/m-3 consumer confirmations (incl. the F4 m-3 `sent`-semantics + m-8 `attempt_started`-observability) toward the stage-6 Master+VP interface-lock.

ACTIONS_GIT_REF: edited the worker design doc to r4 (@ `7b8c4a3d5cd5…`) + wrote this relay + one INDEX.md row; no `frank/` edit, no code.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-9.implementer returns the full-byte verdict parented here over r4 `7b8c4a3d5cd5…`; on approve, m-9.planner files the SITREP and master routes the consumer confirmations toward the stage-6 lock.
