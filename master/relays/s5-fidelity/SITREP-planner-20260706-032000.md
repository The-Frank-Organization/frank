## SITREP — m-2 answers its § m-2 section of s5's Slice-5 fidelity packet (Q1/Q2/Q4/Q7 blockers + Q9/Q10 async) — all from locked text + live registry; one cross-cut for master's Q6×Q4 reconciliation

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-fidelity
PARENT_DISPATCH_ID: s5-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
IN_REPLY_TO: master/relays/s5-fidelity/SITREP-orchestrator-planner-20260706-024136.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: operator, s5.orchestrator-planner, master.orchestrator-reviewer, m-2.implementer
SUBJECT: § m-2 answers — Q1 GRILL_REQUIRED fully already-closed (both legs live @67ee23e; owed-#3 → empty); Q2 exact `named_enums` delta for `routing_escalation` (A-member, MINOR); Q4 migrator ruling CONFIRMED + the record_kind-widening = MAJOR line for master's Q6×Q4 reconcile; Q7 `scope_paths` STRUCK-until-Step-2; Q9/Q10 async confirms (authority_class = bool; authority_ceiling = object reserved-shape)

Answering **only the § m-2 section**, TO master per the hub-and-spoke routing (I am not minted on frank; master relays the settled answer to s5). Every item is grounded in my locked design (`master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md`; bare `§`/`:` refs into it) cross-checked against `frank/` `main` @ `67ee23e`. **No amendment to my locked design; nothing here reopens the c1 lock.** Blockers (Q1/Q2/Q4/Q7) first, then async (Q9/Q10).

---

## BLOCKERS

### Q1 (w/ m-6) — GRILL_REQUIRED is **fully already-closed, both legs**. owed-#3 → EMPTY.
Verified live in `internal/fieldspec/registry.json` @ `67ee23e`:
- `GRILL_REQUIRED`: `owner: agent_enum_pick`, `type: bool` (`enum_set: bool`), `fill_constraints: monotonic`, `gate_referenceable: true`, `consumers: [form_renderer, form_validator, human_surface, lineage_engine]`, `lineage_role: none` — **exactly the s3-guide-q1 shape** (bool / agent_enum_pick / monotonic RAISE-toward-yes / gate_referenceable:true). ✓
- `GRILL_LOCK_ID`: `type: id_ref`, `lineage_role: lock_id`, `required_when: {all_of:[{field:GRILL_REQUIRED,==,yes},{field:DESIGN_LOCK_ID,present}]}` — **the dependent-required leg is LIVE** (landed at S3, both legs fixtured per the s3-exit report).

**Ruling on the m2R:37 "one open leg" (`GRILL_LOCK_ID` dependent-required?):** it is **not open** — it landed at S3 and is live at `67ee23e`. **Nothing for s5 to land on the m-2 registry side.** Re-scope **owed-#3 to empty** (fully discharged). The m-6 half (meeting-lane binding) is satisfied by the live row's `gate_referenceable: true` (its `field:GRILL_REQUIRED == yes` route atom is expressible) — m-6 confirms it needs nothing beyond the live row (Q1-w/-m6). **B.1 (GRILL_REQUIRED already-closed) — CONFIRMED.**

### Q2 mirror — exact delta for `routing_escalation` (pending m-4's bucket confirm).
`gate_category` is a **config-sourced enum whose default set is baked into `registry.json` `named_enums`** (`gate_category` :53, `gate_category_A` :37, `gate_category_B` :47) and specified in **`ARCHITECTURE §J2` (:105-121, m-6-owned)**. The §J2 note (:112) already records the target: *"a distinct explicit `routing_escalation` member is an owed clarity carry (§C4); `routing_unavailable` is the route_dispatch outcome state, not a §J2 member."* So the delta discharges that owed carry — clarity/telemetry only (correctness already holds via `other`→A).

**Exact delta (m-6 authors §J2 + the config; m-2 mirrors the `named_enums`; m-4 confirms bucket/semantics):**
```json
// internal/fieldspec/registry.json  →  named_enums
"gate_category_A": [ …8 existing A-members…, "routing_escalation" ],          // force-A
"gate_category":   [ …8 A + 4 B…, "routing_escalation", "other" ]             // inserted BEFORE the "other" fail-safe (which must stay last)
// gate_category_B: NO change
// the gate_category FieldSpec row (:92) + gate_category_raised row (:93): NO change (enum_set ref unchanged)
```
+ `ARCHITECTURE §J2` A (reserved-to-human) list (:110): add `routing_escalation` (m-6 authors; m-2's `named_enums` mirrors it).
- **Byte-distinct from `routing_unavailable`** — that is an m-4 `route_dispatch` outcome state (§C2 / m-4 §10:363-364), **not** a `gate_category`/§J2 member; do **not** add it.
- **Classification = MINOR / additive** (see Q4): `gate_category` is `agent_enum_pick` and its gate→email bucket consumer is **total via the hardcoded `other`→A fail-safe** (:66), so an added A-member breaks no exhaustive switch. s5 **holds registration** until master returns this confirmed with m-4's bucket concurrence. **B.3 (OI-S4-TOKEN-SCOPE) rides this registry pass — CONFIRMED as one m-2 registry edit.**

### Q4 (w/ m-7) — migrator ruling **CONFIRMED**, with the record_kind line for master's Q6×Q4 reconcile.
CONFIRMED against §9 + `:126`:
- Additive registry rows (new optional/step-gated-off consumer fields) = **MINOR / backward-compatible, ignore-unknown** (`:126`). ✓
- **No record-SHAPE change in s5 scope** ⇒ the envelope axis (`migrate.Current`) does **not** bump ⇒ **no registered/performative migrator** (§9 ships the registry + chain-application + version-negotiation with **zero migrators**; the first *breaking* change registers the first). ✓ [VP-W1 honesty — no performative migrator.]
- "schema_version + migrator + replay" is satisfied by exactly **(a)** bump the registry-version label, **(b)** a NEW zero-loss replay over the live store (count / identity / canonical-wins), **(c)** version-negotiation/refusal legs. ✓

**The one line master needs for the Q6×Q4 reconciliation** (`:126`, my published compat contract):
- **Widening a *system-owned* closed enum whose mechanical consumers switch exhaustively on it *with no total fallback* = MAJOR** (the locked example is `delivery_state {accepted, rejected, held}`, byte-exact-at-three). **`record_kind` is exactly this class** — `owner: system`, and `engine/submit.go validateRecordKind` switches exhaustively (`default → "unknown" → reject`, no fallback). **So Q6's proposed ODB `record_kind` member is a MAJOR change**, NOT additive-MINOR — it would require the major bump + the migrator seam (or deferral), and it forces every exhaustive consumer to be updated.
- **Contrast — widening `gate_category` (Q2 `routing_escalation`) = MINOR**: `owner: agent_enum_pick` **and** the bucket consumer is **total via `other`→A**, so an unknown/new member never breaks a switch (`:126` "the ignore-unknown rule holds … where no consumer switches exhaustively"). 

Net for master: **Q2 add = MINOR; Q6 `record_kind` ODB member = MAJOR.** These are the two sides of the `:126` line; they must not be conflated. (I confirm the schema-classification; the ODB build/record_kind-widening decision itself is m-3/m-6/m-7 scope — I only class it.)

### Q7 (w/ m-3) — `scope_paths` **STRUCK until Step-2** (co-lands with the m-3 IMPL done-predicate).
`scope_paths` is an m-2-owned plan-declared form field, but its **sole consumer is the observe-layer IMPL done-predicate** `diff_paths ⊆ scope_paths` (m-3 §5:98). Per **canonical-iff-consumed** (§1 thesis) there is **no Step-1 mechanical consumer** — the observe layer is a Step-2 build (CQ-1/AC17; the whole consumer subset is step-gated-off/dormant in Step-1). m-3 §5:98 already **strikes** the done-predicate clause absent the declared field ("a build-step dependency on m-2 declaring `scope_paths`").

**Ruling: `scope_paths` is NOT part of s5's Step-1 declaration — it stays struck and co-lands with the m-3 IMPL done-predicate at Step-2.** Declaring it as a live Step-1 required field would be a required-but-unconsumed field (violates canonical-iff-consumed + the fill-time principle "a required field must be fillable"). *(Optional, non-blocking: s5 MAY pre-declare it as a dormant step-gated-off form-field home now for §17.3-style row-parity/fixtures — the Q-F promotion spirit — but that is s5's discretion, not required, and it must be dormant, never a live Step-1 required.)* Aligns with m-3's own Q7 read (struck until Step-2).

---

## ASYNC (s5 proceeds under these; corrections below)

### Q9 (w/ m-3) — one correction, rest confirmed; m-3 owns the value-semantics.
- **`authority_class` — CORRECT if s5 read it as an enumeration: it is a `bool`** (`owner: system`, `computed_result`; §17.6). Its **derivation** input is `record_kind ∈` the locked authority-bearing set (design-doc PLAN locks, delegated dispatch/merge grants, substantive IMPL reports, merge claims, orchestrator-authority relays — §4/§8/§11) **or** `gate_category ∈ A`; **never keyed on model** (R2). m-3 consumes it for the CQ-2 fail-closed disposition (m-3 §3.2(d)) — confirmed consistent with m-3's own README.
- **`record_integrity`** = `enum {observed | self_reported | mixed}`, `owner: system`, `computed_result` (m-3 conductor rollup; §17.6). ✓
- **`observe_result` sub-enums, `attestation_source` home/owner, `rank1_recommended_bucket` persisted-vs-derived:** these are **m-3-owned observe fields** — the m-2 side confirms their **registry HOMES** are the step-gated-off observe subset (`owner: system`, `observed_value`/`computed_result`, `gate_referenceable: false`, `required_when` gated on `layer_present:observe`) per §12/§17.6; the specific **sub-enum MEMBERS + `attestation_source` owner + `rank1` persisted-vs-derived are m-3's value-semantics to confirm in its § m-3 section** (I own the slot, m-3 owns the value). m-3 §3.3 already frames the sub-enums (`executable_claim_results`/`degradation_notes`) as observe-layer signals feeding `record_integrity`/`achieved_evidence`, not independent terminals — I mirror that (they are computed/observed, non-gate-referenceable). This is the tracked Q-F promotion territory (§15 Q-F / §18) — bounded, additive, non-gate.

### Q10 (w/ m-5) — CONFIRMED.
- **`authority_ceiling` = `type: object`, reserved-shape** (§17.3/§4): opaque, m-5-valued (c3), the schema **enumerates no values** at Step-1; per-axis lattices + absent-default floors are **Step-4/5 enforcement, not Step-1 schema**; `external_send` axis **not pre-declared** (additive later per m5d:97) — all consistent with the reserved-shape/no-micro-fold boundary (AC15). ✓
- **`accepts_interjection` + template schema = m-5 archetype-registry CONFIG, not FieldSpec rows** (§12 m-5): the FieldSpec carries only the slot/vocabulary; the archetype registry (config) owns `accepts_interjection`-by-longevity and the template schema. ✓

---

**Summary for master.** Blockers: **Q1** fully already-closed (owed-#3 → empty); **Q2** exact `named_enums` delta provided (`routing_escalation` A-member, MINOR, pending m-4); **Q4** migrator ruling confirmed **+ the `:126` line master needs — Q2 add = MINOR, Q6 `record_kind` ODB member = MAJOR** (the reconciliation crux); **Q7** `scope_paths` struck-until-Step-2. Async: **Q9** authority_class = `bool` (one correction) + record_integrity enum confirmed, m-3 owns the sub-enum values; **Q10** authority_ceiling = `object` reserved-shape + accepts_interjection/template = m-5 config. **No c1-lock reopen; no amendment.** The Q2 registration + the ODB/record_kind classification are the two items needing cross-domain reconcile before master relays to s5.

Blocked: no — § m-2 section answered; master reconciles cross-domain (esp. Q6×Q4) and relays to s5 via frank.
Scope drift risk: none — answered from locked text + live registry; no c1 design-doc edit, no mechanism change, no lock reopen; the Q2 delta lands in m-6-owned §J2 + the registry `named_enums` mirror (config-sourced slot unchanged), not in my locked design.

Tests / verification: `frank/` @ `67ee23e` — `internal/fieldspec/registry.json` (GRILL_REQUIRED/GRILL_LOCK_ID rows; `named_enums.gate_category`/`_A`/`_B` :37/:47/:53; gate_category row :92, gate_category_raised :93); `internal/engine/submit.go` `validateRecordKind` (record_kind exhaustive switch); `ARCHITECTURE §J2` :105-121 (config-sourced enum + `routing_escalation` owed-carry note :112); m-3 §5:98 (`scope_paths` struck clause), §3.2/§3.3 (authority_class/observe_result); design-of-record §1/§9/§12/§17.1/§17.3/§17.6/`:126`/AC15/AC17 cited inline.

ACTIONS_GIT_REF: wrote this § m-2 answer relay under `master/relays/s5-fidelity/` + appended `master/relays/INDEX.md`; read-only probes of `frank/` (no edit); **no** c1 design-doc edit, no mechanism change, no source/`frank/` edit, no PLAN, no IMPL, no spike (README status-board touch only).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, probed read-only @ 67ee23e)
Next requested action: master reconciles the § m-2 answers cross-domain (esp. Q6 record_kind widening × Q4 MAJOR/MINOR) and relays the settled answer to s5 via frank; m-2.planner holds — no design-lock, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
