## SITREP — m-4 section answer to s5-fidelity: Q2 (blocker) CONFIRMED + Q8 (async) CONFIRMED; both match the locked m-4 design-of-record

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-fidelity
PARENT_DISPATCH_ID: s5-fidelity
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
IN_REPLY_TO: s5-fidelity/SITREP-orchestrator-planner-20260706-024136.md
FROM: m-4.planner
TO: master.orchestrator-planner
CC: operator, s5.orchestrator-planner, master.orchestrator-reviewer, m-4.implementer
BUNDLE_ID: s5-fidelity

**Scope.** Read-only confirm of the two m-4 sections. No new design; both answers are quotations of the locked,
c2-reviewed m-4 design-of-record (`2026-06-29-v3-routing-policy-design.md`, DESIGN_DOC_ID `c2-design-m-4-routing-policy`).
No m-4 record-shape change; no `frank/` edit. Master reconciles + relays to s5 via frank (I am not minted there).

---

### Q2 (CTO-driven, blocker — gates s5 registration [VP-W5]) — **CONFIRMED, all four sub-parts**

The CTO's proposal is *exactly* the m-4-recommended c6 cross-domain carry (`§10:367-369`). Confirming:

1. **Token `routing_escalation` is correct.** ✔ It is the §J2 `gate_category` A-member that names the routing
   escalation. m-4 stamps a **force-A `gate_category`** on the *consumable, accepted* routing record when it raises
   `HUMAN_GATE_REQUIRED` (`§10:360-361`); `routing_escalation` is the precise classification label for that stamp.

2. **Byte-distinct from `routing_unavailable`.** ✔ And they live in **different namespaces**, which is the stronger
   guarantee: `routing_unavailable` is a **`route_dispatch()` routing-outcome state** (`§10:362-364`), never a §J2
   `gate_category` member — it is one of the *triggers* of the raise, not the classification of it. `routing_escalation`
   is the §J2 `gate_category` member. Also byte-distinct from `human_decision_required` (the other raise-trigger,
   which is §J2's `other`→A **disposition label**, `§10:363`). No collision on any of the three strings.

3. **A-bucket placement = force-A.** ✔ Correctness **already holds today** via the hardcoded `other`→A fail-safe in
   ARCHITECTURE §J2 (an unclassified routing-outcome escalation ⇒ human-only, `§10:364-367`) — so this registration is
   **clarity/telemetry, not a correctness dependency**. With the explicit member, `routing_escalation`→A directly
   instead of via `other`→A; both yield force-A. This is a strict precision improvement, no behavior change.

4. **The A/B-map delta — ownership, so master sequences it right.** m-4 confirms the **semantics + bucket**
   (A-member, force-A, byte-distinct). m-4 does **not** author the map itself: per `§10:376-377` the A/B category
   membership + email projection are **§J config**. So the two dependent legs s5's registration waits on are:
   - **CTO / ARCHITECTURE §J2** — add `routing_escalation` to the default `gate_category` A/B map (force-A). *(CTO-owned edit.)*
   - **m-2 registry mirror** — add `routing_escalation` to the `gate_category` enum + A/B map delta (the m-2 §Q2-mirror in the packet). *(m-2-owned.)*

   m-4 introduces **no new m-4-owned enum or FieldSpec** for this — the routing record already carries only "a readable
   force-A atom" (`§10:377`); the `gate_category` enum is the §J2/registry home. Value-ownership ≠ record-home holds:
   m-4 owns the *requirement*, §J2/registry own the *enum value*.

**Net for s5:** register `routing_escalation` as an A-member (force-A), byte-distinct from `routing_unavailable`.
s5's registration unblocks once the CTO §J2 edit + the m-2 registry mirror land — those, not m-4, are the gating legs.

### Q8 (confirm-async) — **CONFIRMED, no correction; s5's read is correct**

`deviation_reason_code` = **config-sourced value-set** — matches `§5:211` + `§6:228` verbatim (m-4-F5, c6; c6.1-confirmed):
- **m-2 declares the SLOT** (the FieldSpec row): `agent_enum_pick`, `required_when any(routing_assignments.declared_deviated == true)`
  — same grain/treatment as `justified_deviation`, so the reason code can never be omitted where policy says it's load-bearing.
- **Values are config-sourced** (default vocab in config, **not** registry) — exactly the §J2 `gate_category` precedent s5 cites.
- **Hardcoded `other` fail-safe** ⇒ the `justified_deviation` free-text carries the reason when no code fits.
- **Shipped default vocabulary:** `capability_gap · cost_budget · latency_budget · bucket_unavailable · operator_directive ·
  experiment` (v3.1-reserved) · `other` (fail-safe). m-2 §17.3 mirrors the value-set (flagged to m-2).

No R2 exposure: `deviation_reason_code` is a reason enum, not a model-identity field, and its `required_when` reads the
gate-side `declared_deviated` atom (not any model value). s5 proceeds as assumed — **no correction**.

---

**Nothing else in the packet is m-4's** — the Q6 `record_kind` widening × Q4 MAJOR/MINOR reconcile, `scope_paths`,
the observe sub-enums, `slot_in`, `authority_ceiling` concrete shape, and the known-A detector home are m-2/m-3/m-5/m-6/m-7.
m-4 remains holding after this reply; no PLAN action taken (Step-1 conductor-core PLAN is the CTO's to open per the roadmap).

## Verification
- `grep -n "routing_escalation" 2026-06-29-v3-routing-policy-design.md` → §10:367-369 (recommended A-member carry), matches CTO Q2 proposal.
- `grep -n "config-sourced" …design.md` → §5:211, §6:228 (`deviation_reason_code` value-set), matches Q8.
- relay-lint: run below (`--relay-root master/relays/s5-fidelity`).
- No `frank/` edit, no code, no registry/§J2 edit (those are m-2/CTO — flagged, not authored here).

ACTIONS_GIT_REF: wrote this relay + appended `master/relays/INDEX.md`; no design-doc edit (both answers already locked in `2026-06-29-v3-routing-policy-design.md`), no ARCHITECTURE/m-2 edit (flagged to owners), no `frank/`/code, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` on `main` @ `67ee23e` (post-`s4-close`), clean.
Next requested action: master reconciles + relays the settled Q2/Q8 answers to s5 via frank; s5 registers `routing_escalation` once the CTO §J2 edit + m-2 registry mirror land. m-4.planner holds.
