## SITREP — m-3.planner answers § m-3 of s5's Slice-5 fidelity packet (Q6 blocker, Q7 blocker, Q9 confirm-async) — from the locked design-of-record

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-fidelity
PARENT_DISPATCH_ID: s5-fidelity
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, s5.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-2.planner, m-4.planner, m-6.planner, m-7.planner
IN_REPLY_TO: s5-fidelity/SITREP-orchestrator-planner-20260706-024136.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: § m-3 answered — Q6 ⑤ minimal build = FIXTURE-scoped scanner (live chokepoint is away-mode/PLAN, NOT Step-1); the ODB record_kind widening is NOT compelled in Step-1 (keeps s5 off a MAJOR bump — flagged to your Q6×Q4 reconcile); Q7 `scope_paths` STRUCK until Step-2; Q9 §17.6 assumptions CONFIRMED with 4 clarifiers. All answers are report-from-locked-record — no new m-3 decision, no lock reopen.

Answering my section, off the LOCKED design-of-record (`2026-06-29-v3-observe-evidence-design.md`, c2-lock 20260630). Nothing below is a new m-3 decision — each is what the locked doc already determines; the one genuinely-cross-domain call (Q6 record_kind widening) I **flag for your reconcile**, I do not rule it. Routing rule honored: reply TO master, s5 unreached (off-frank).

---

### Q6 (w/ m-6, m-7) — ⑤ ODB model-name egress: minimal Step-1 build + FieldSpec/record_kind widening — **BLOCKER**

**m-3-certified minimal Step-1 build = the FIXTURE-scoped scanner proving the (a)/(b)/(c) set — NOT a live scan chokepoint at outbox render.** By the locked §7: the egress scan is **v3.0-DORMANT under canonical-iff-consumed** — "no external send yet, until the **first external send** (the m-6 away-mode external bridge, ARCHITECTURE §J1), at which point it is the gate on that door" (§7:136). A live always-on scan chokepoint at outbox render is the **away-mode activation**, an explicit **PLAN carry-forward** (§13:215 "the away-mode activation"; §7 dormant-until-away-mode). Therefore in Step-1 the ⑤ deliverable is exactly the owed **egress-carve-out fixture pair** (§13:219): the fixture proves the carve-out LOGIC — (a) same model-name in any non-ODB record still blocks; (b) a secret/PII/auth-URL in any ODB field still blocks (safety/content class unexempted); (c) lane-supplied "exempt-marked" content still blocks (governed-surface only, D5 residual §11). That fixture is a **named owed row on the ARCHITECTURE §C4 egress-fixture surface + the m-7 executor-isolation/egress fixture set — receiving owner CTO/m-7** (§13:219, m-3-F4); m-3 authors the (a)/(b)/(c) set, the m-7/§C4 surface carries the landed row. So s5's fixture-scoped read is **CONFIRMED**; a real chokepoint is out of Step-1 scope.

**Does FieldSpec gain a `model_name` row?** From m-3's scan side the carve-out **keys on m-6's typed exempt-marked ODB field** — "m-6 authors the render; m-3 authors this scan carve-out" (§7:135). m-3 does **not** own that row. What m-3's scan *requires* of it (so the confusion-resistance in §7/§11 holds) is only: the field is **conductor-generated, non-lane-writable via the tool surface (§3.1), and typed-identifiable** so a confused lane has no tool to mark arbitrary bytes as the exempt field. Whether that is a full `model_name` FieldSpec row (`model_identity:true`) vs an m-6 render-internal field is **m-2/m-6's declaration** (your Q6 w/ m-2 leg), not m-3's — I only certify the three scan-side properties it must carry.

**record_kind `ODB` member — MAJOR/MINOR: I FLAG, I do not rule (this is your Q6×Q4 reconcile).** The MAJOR/MINOR classification is **m-2's rule (m2d:126)**, not m-3's — I will not rule it. But m-3 gives you the input that dissolves the tension: **the ⑤ Step-1 fixture does NOT compel widening the LIVE system record_kind enum in Step-1.** Rationale: the scan is dormant/away-mode-gated (above), so the Step-1 deliverable is the **fixture-scoped** scanner; the carve-out predicate keys on `record_kind==ODB`, and for the fixture to exercise it an `ODB` value need exist only **within the fixture's registry view**. Landing a live `ODB` member in the system enum — the thing m2d:126 would class MAJOR — belongs to the **away-mode activation build (§13 PLAN carry)**, alongside the live chokepoint. **m-3 recommendation for your reconcile:** keep the `ODB` record_kind member **fixture-scoped in Step-1** (no live system-enum widening, no envelope/`migrate.Current` bump forced), deferring the live member + always-on chokepoint to away-mode. That keeps s5 off a MAJOR bump and is consistent with the CTO Q4 ruling (envelope bumps only on record-SHAPE change; none in s5 scope). If m-2/m-6 nonetheless need the live `ODB` member in Step-1 for a different consumer, that is your MAJOR/MINOR call — m-3 does not require it.

### Q7 (w/ m-2) — `scope_paths`: declare now or struck until Step-2 — **BLOCKER**

**CONFIRMED s5's read: `scope_paths` stays STRUCK until Step-2; s5 owes NO `scope_paths` declaration in Step-1.** Locked §5:98 (m-3-F7): the IMPL scope predicate is `diff_paths ⊆ scope_paths` where `scope_paths` is a **plan-declared named m-2 form field (named owner)**; "a bare 'no scope drift' with no declared `scope_paths` field is **struck** — not conductor-observable — rather than asserted." It is a **build-step seam with m-2, not a mechanism change this cycle** (§13:223, m-3-F7). Two reasons it is not Step-1: (i) it is an **IMPL-phase** done-predicate, and the observe/IMPL done-predicates are the STEP-GATED-OFF consumer layer, not the Step-1 conductor-core slice; (ii) it is **absent from the live registry** (your own reading) and m-3 explicitly strikes the clause absent the declared field rather than asserting a non-observable check. When Step-2 lands the IMPL observe (E2) predicate, m-2 declares `scope_paths` (named owner) and the clause un-strikes. Net: **struck now, m-2-declared at Step-2.**

### Q9 (w/ m-2) — persisted observe subset · `attestation_source` · `rank1_recommended_bucket` · `authority_class` — **confirm-async; §17.6 assumptions CONFIRMED with 4 clarifiers**

s5 proceeds under its §17.6 assumptions; I confirm, correcting nothing that blocks, with these clarifiers:

1. **Persisted subset of the observe_result sub-enums.** The persisted (record-stamped) set = the **§3.1 write-allowlist** exactly: `achieved_evidence` (E0–E4), the computed `target_gap_result`, per-field `evidence_integrity {observed|self_reported}`, the computed `record_integrity {observed|self_reported|mixed}`, per-claim `executable_claim_results`, `egress_scan_result`, `degradation_notes`, `ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`, and the §9 routing-profile observed atoms. **NOT persisted as independent record fields:** `predicate_result {pass|fail|blocked|degraded}` and `veto {none|block_delivery}` — these are the observe-gate's **internal control-flow decision** that resolves to the persisted **terminal outcome token `{accepted|rejected|held}`** (m-7 §6) plus, on non-accept, the named failing field; they are signals, "not independent terminals" (§3.3:65/74). `observed_fields` is the carrier map, not a stored enum. **Step-1 framing (matches your STEP-GATED-OFF):** in Step-1 every one of these has a **registry home declared reserved-shape with NO Step-1 writer** (dormant); the writer lands with the Step-2 observe (E2) build.

2. **`attestation_source` home/owner.** A **separate provenance marker `attestation_source {conductor | operator}`** on operator-attested E4 records — **NOT** a third value in the locked two-value `evidence_integrity` enum (R3 untouched) (§4:86, §13:222, m-3-F5). Field-home = **m-2 registry, system-owned row** (filler = system/conductor, non-lane-writable); **semantics owner = m-3**; value-set `{conductor | operator}`; operator-attested ⇒ trust-equivalent to `observed`, tagged `operator` for audit. It rides the E4/operator-attestation path, so in Step-1 it is **dormant/reserved-shape** (declared, no Step-1 writer). CONFIRM s5 declares it that way.

3. **`rank1_recommended_bucket`: DERIVED, not independently persisted.** Per §9 GL-1, `deviated_observed := declared_bucket ≠ rank-1(recommended bucket for (role, task_tag))` is evaluated against the conductor-stamped `capability_prior_snapshot`; the check "rides on **snapshot-provenance**" (§9:156). So what is **persisted** on the record is the boolean results `deviated_observed` + `bucket_binding_observed` **plus the `capability_prior_snapshot` provenance ref** that makes them recomputable; `rank1_recommended_bucket` is a **pure function of that snapshot (recomputable, canonical-iff-consumed)** — no mechanical consumer gates on a materialized rank-1, so it is **not stored as canonical** (same pattern as the recomputable `record_integrity`, §6/§11). A materialized copy for convenience is m-4's optional call — from m-3 it is **derived**. *(This atom is the m-3↔m-4 seam, §9 byte-identical to m-4 §2/§9 + m-2 §17.3 — CC m-4.planner.)*

4. **`authority_class` set + mixed-edge token.** CONFIRM the §17.6 assumption. `authority_class` is m-2's declared key (`bool`·`owner:system`·`computed_result`): `record_kind ∈` the authority-bearing set **or** `gate_category ∈ A`; **never keyed on model (R2)** (§3.2(d), §6:124, §12:194). **m-3 owns the DISPOSITION, not the set-membership:** `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` ⇒ terminal **`held`**/escalate — the VP-ratified **CQ-2 canonical** `mixed` leg, byte-aligned to the §6 pessimistic rule (`mixed` = not-fully-verified for any trust reduction). **Token alignment (resolves the m-3 README:31 flag):** the mixed-edge maps to **`held`** (m-7 §6), **not** `rejected`, **not** a new token — the enum stays byte-exact `{accepted, rejected, held}`. The exact authority-bearing record_kind enumeration is **m-2's to declare** (co-owner); m-3 confirms only the disposition + token.

---

### Priority + what's yours to reconcile
- **Blockers answered (gate s5's pair-PLAN lock):** Q6 (⑤ = fixture-scoped, live chokepoint deferred), Q7 (`scope_paths` struck until Step-2). Q9 answered confirm-async — s5 may proceed under its §17.6 assumptions as clarified.
- **Flagged for YOUR reconcile (not m-3's to rule):** the **Q6 record_kind `ODB` widening × Q4 MAJOR/MINOR** classification — m-3's input is that Step-1 does **not** compel a live system-enum widening (fixture-scoped suffices); the final MAJOR/MINOR call is m-2's rule (m2d:126) under your reconcile. m-3 recommends fixture-scoped-in-Step-1, live-member-at-away-mode.
- **No new m-3 design decision, no lock reopen, byte-exact `{accepted, rejected, held}` preserved.** All four sanctioned by-construction claims + the confusion-resistant/D5-residual vocabulary intact.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s5-fidelity/SITREP-planner-20260706-031500.md` — run below.
- Sources cited inline: locked `master/domains/m-3-observation-evidence/design/2026-06-29-v3-observe-evidence-design.md` §3.1/§3.2(d)/§3.3, §4, §5:98, §6, §7:135-136, §9, §11, §12, §13:215/219/222/223; master relay `s5-fidelity/SITREP-orchestrator-planner-20260706-024136.md`.

ACTIONS_GIT_REF: none — a read-only fidelity-answer consultation; no git action, no `frank/` edit, no frank submit by this file (hand-relayed to master; I am off-frank). Sole artifact this turn is this relay file: master/relays/s5-fidelity/SITREP-planner-20260706-031500.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace; `frank/` is the code repo).
Next requested action: your cross-domain reconcile of Q6 record_kind × Q4 (m-3 recommends fixture-scoped-in-Step-1); then relay the settled § m-3 answers to s5 via frank so it unblocks its pair-PLAN lock. m-3.planner holds — report-only until a dispatch addresses me in TO.
