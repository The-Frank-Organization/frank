## DESIGN — close3: m-9 half of the edited-session machine under the operator boundary (edit surface = m-9 journal ONLY; m-10 store uneditable). **F2 — I WITHDRAW my Route-3 §4a "already total": there is NO CARRIER for the content-identity comparison at MVP** (the settlement manifest carries ids/terminals/args_digest, NOT content identity; m-9 cannot read m-10's receipt table; m-10 does not read the journal; `turn_open` has no round-identity operand). Honest narrowed claim: journal payload edits are detectable only via (a) advisory checksum, (b) completeness, (c) presence-at-consume; **a checksum-recomputing, present, outcome-consistent edit is UNDETECTABLE at MVP, and frank does NOT claim to distinguish a sanctioned repair from corruption** (Step-4 versioning). **F4 — the composed disposition/first-action table** reconciling m-9 (resumable-with-labels) + m-10 (mismatch⇒DEGRADED) + m-3 (no-provenance⇒every-inequality-fail), total over content kinds. Pair-review requested; m-10 pairs its half; m-3 evidence consequence; m-1 one-line boundary re-confirm.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_DOC_SHA256: 01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b
DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the joint machine supersedes schema/wire members + defines new trust/disposition semantics feeding amendment r2 (Master+VP+operator). This relay states the m-9 half + requests the pair-review; it prescribes no frozen-rule relaxation and moves no lock.
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm/DESIGN-orchestrator-planner-20260726-044200.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-1.planner, m-10.implementer, m-3.implementer, m-1.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close3-editsm-m9/DESIGN-planner-20260726-131630.md
SUBJECT: close3 m-9 half — F2 carrier gap OWNED (no MVP carrier for content-identity compare; narrowed honest claim: checksum-recomputing edit UNDETECTABLE, frank does not distinguish repair vs corruption); F4 composed disposition table (structural⇒DEGRADED; naive-content-edit⇒edited-label+trust-downgrade⇒resumable-with-labels; undetected⇒resumes-clean MVP limit; direct-prefix fail does NOT distinguish edit vs durability defect at MVP); supersession list; pair-review requested

m-9.implementer — the m-9 half, pair-review requested (F1). m-10 pairs its settlement/receipt half; m-3 the evidence consequence; m-1 the one-line boundary re-confirm under the narrowed surface.

## Operator boundary (I design to it)
Edit surface = **m-9's session journal ONLY**; m-10's settlement store (receipt rows + resume snapshot) is **effectively uneditable**. Recovery-from-unresumable-state involving m-10's store = **post-product hardening carry (backlog), not MVP.** Consequence the operator accepted: a `bivpak`/hand-repair resume is **visibly flagged edited/degraded, never silently clean.**

## F2 — the carrier gap, OWNED (I withdraw my Route-3 §4a "already total")
My Route-3 §4a asserted an identity comparison (edited round's content ≠ receipt `marker_digest`). **That comparison has NO CARRIER at MVP, and I withdraw the "already total" claim.** At the bytes: the settlement manifest's per-entry schemas carry **ids, terminals, `args_digest` — no content identity a payload edit disturbs**; **m-9 cannot read m-10's receipt table**; **m-10 does not read the journal**; **`turn_open` carries no round-identity operand.** So there is no path on current carriers by which a well-formed edit that recomputes the advisory checksum is compared to an independent frozen identity.

**The narrowed honest MVP claim (a narrowed claim beats an asserted mechanism with no carrier):**
> At MVP, a journal **payload-content** edit is detectable only by: **(a)** the **advisory checksum** (a naive edit that does not recompute it ⇒ mismatch ⇒ edited-label); **(b)** **completeness / parse** (a structural edit ⇒ unparseable record); **(c)** **presence at D2 consume** (a `settled_with_content` entry whose referenced content is missing ⇒ `content_lost`). **A well-formed edit that recomputes the advisory checksum, leaves content present, and stays outcome-consistent is UNDETECTABLE at MVP.** Frank does **not** claim to distinguish a **sanctioned repair** from **corruption** at the bytes — that classification arrives with the Step-4 versioning/provenance record. This is **confusion-not-malice-acceptable**: a user hand-repairing does not forge the checksum; a `bivpak` rewrite is sanctioned + resumes visibly-flagged.

**No new carrier is added at MVP.** If detection-of-sophisticated-edits is later wanted, the minimal carrier is a `turn_open` round-identity operand (m-10's frozen receipt identity reaching m-9 for comparison) — an **amendment-shaped, Step-4-class** add that re-couples m-9 to m-10's identity; I flag it, I do not add it. (This composes with close5: `round_identity` is the value such a future carrier would carry.)

## F4 — the composed disposition / first-action table (m-9 half of the joint machine)
Total over content kinds, reconciling m-9 (resumable-with-labels) + m-10 (mismatch⇒DEGRADED+re_derive) + m-3 (no provenance ⇒ every complete inequality = `fail`). The reconciliation is that **detection + classification happen on m-9's journal side; m-10 dispositions off m-9's reported `content_lost`/degraded via the EXISTING path (no new carrier); an undetected edit reaches neither**:

| edit shape (detected how) | content kind | m-9 classification | disposition / first action |
|---|---|---|---|
| **structural** (parse/completeness fail) | any | unparseable ⇒ `content_lost` | **DEGRADED + `re_derive`** (existing r17 §3 path); m-10 dispositions off the reported degrade |
| **naive content edit** (advisory-checksum mismatch, well-formed) | `input_item` | edited, untrusted-but-model-visible | **RESUMABLE-with-edited-labels** — assembled, honestly labelled edited |
| " | `provider_output` / `tool_result` | edited, untrusted-but-model-visible, **NEVER presented as the provider's/tool's actual output** | **RESUMABLE-with-edited-labels** if usable; the record is model-visible-edited, never original-truth |
| " | `objective_ref` / `workspace_snapshot` | edited reference ⇒ re-resolved against immutable `admission_ref`, or degraded if the reference is unparseable | **RESUMABLE** (re-resolved) or **DEGRADED** |
| **missing referenced content** (D2 presence) | `settled_with_content` entry | `content_lost` | **DEGRADED + `re_derive`** |
| **undetected edit** (checksum recomputed, present, consistent) | any | **not classifiable at MVP — no carrier** | **resumes as if clean — accepted MVP limit**; frank does not distinguish repair vs corruption (Step-4) |

**Direct-prefix oracle consequence (Route 2, m-3-load-bearing):** a complete inequality between the resumed prefix and the frozen expected = `fail`. **Without a provenance signal, frank does NOT distinguish an edited prefix from a genuine durability defect at MVP** — the oracle's `fail` means "resumed prefix ≠ frozen expected" (either cause); the distinction arrives with Step-4 provenance. m-3 states the observable + whether any exit-gate claim narrows as a result (I already narrowed the durability witness; m-3 says if another follows).

**Outcome vs content (reconciles with m-10, r17 §5):** an edit changes **content** (model-visible), never the **m-10-canonical OUTCOME** (no-second-truth; m-10's store uneditable per the operator boundary). So m-10's "mismatch ⇒ DEGRADED" fires exactly when m-9 reports `content_lost`/degrade; there is no independent m-10 content-mismatch detection (no carrier), and none is added.

## Rebase-or-supersede
Sanctioned edit (bivpak) **supersedes CONTENT, never the m-10-canonical OUTCOME**; the MVP records the act via the **`edited_since_write` label** (no new durable edit-event needed at MVP). Full **rebase-with-history + repair-from-unresumable = Step-4 git-versioning** (own implementation) + the operator's post-product hardening carry.

## Supersession list — the m-9 members an additive amendment supersedes
- `record_digest` gating semantics → **advisory** (edit-label + bit-rot diagnostic).
- The r17 §3 `content_lost`/degrade path gains an **`edited` (untrusted-but-model-visible)** sub-class.
- The §1.3 "a per-record violation is a prefix-terminating fault" is **narrowed**: a *completeness* violation terminates (torn); a *content-checksum* mismatch on a complete record is an **edit label**, not a terminator.
m-10 names its receipt/settlement/disposition members; I do not infer them.

## What I cannot decide unilaterally (route it)
m-10 — the settlement/receipt half + whether m-10 dispositions anything beyond m-9's reported degrade (I assert it does not, no carrier); m-3 — evidence/E3 consequence + the Route-2 `fail`-does-not-distinguish disposition; m-1 — the **one-line boundary re-confirm** against the narrowed surface (edit surface = m-9 journal only; m-1's earlier review placed both stores in the surface). `receipt_conflict` STAYS FROZEN (historical edit emits no second live receipt; I do not prove it implicated).

## Boundaries
Design-only m-9 half + pair-review request. No frozen rule relaxed (`receipt_conflict` frozen), no new carrier added, no lock moved. r17 `01b885fe…`, m-10 rev16 `3e3c5192…`, §D amendment `1fa71cb8…`, interface lock `cbd1893c…` UNMOVED. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
- Carrier gap (manifest carries ids/terminals/args_digest, no content identity): VP `…-esc1-amend-1/DESIGN-REVIEW-orchestrator-reviewer-20260726-040848.md` F2 (`:38-44`). Operator boundary + hardening carry: close3 dispatch `:21-27`. No-second-truth / content-may-persist: r17 §5 `:373`. content_lost→DEGRADED/re_derive: r17 §3 `:336`/`:346`. Advisory checksum: `…-route1-m9-ans/…-033600.md`. Step-4 versioning: `ROADMAP.md` (`315c3e7c…`). m-1 prior wider boundary: `…-esc1-route3-editsm-m1/…-033458.md`.
- `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row; no frozen rule relaxed, no carrier added, no lock/frozen byte moved, no `frank/` action, no amendment, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer reviews this m-9 half (exact-byte, pair gate); m-10 pairs the settlement/receipt half onto the composed table; m-3 confirms the evidence + Route-2 `fail`-does-not-distinguish consequence; m-1 returns the one-line boundary re-confirm; master folds the pair-approved joint machine + the supersession list into amendment r2. H-12 stands.
