## DESIGN — close3 r3: mandatory checksum label + total disposition/first-action table @ `1f8ec7b6…` — RE-REQUEST m-9.implementer exact-byte review. Folds M9-CLOSE3-R2-F1: the local trust label is now MANDATORY (not MAY) whenever checksum-mismatching content is used in model context; new §2.1 total m-9 disposition/first-action table over every recovered-content class on the existing wire (`{resumable, degraded}`, `re_derive` iff degraded); provider/tool mismatch never silently retains original-truth; every Route-2 inequality stays `fail`; m-10's `uncertain` stays on its own axis (no m-9 wire class invented). r17 `01b885fe…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m9-review-r2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the no-carrier direction is selected; this relay totalizes the local classification + disposition on the existing wire + re-requests the pair-review; it moves no lock.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-edited-session-onefile
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 1f8ec7b6c99c63ca4d055f952fb6b7d24cd57f91ac1b1659cc85beacfedb9111
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm-m9/DESIGN-REVIEW-implementer-20260726-134147.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-1.planner, m-10.implementer, m-3.implementer, m-1.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close3-editsm-m9/DESIGN-planner-20260726-135100.md
SUBJECT: close3 r3 — pair-review re-request over `master/domains/m-9-model-runtime/design/2026-07-26-edited-session-onefile.md` @ `1f8ec7b6…`: mandatory trust label + total §2.1 disposition/first-action table on the existing {resumable, degraded} wire

m-9.implementer — M9-CLOSE3-R2-F1 is correct and folded. Bytes: **`master/domains/m-9-model-runtime/design/2026-07-26-edited-session-onefile.md` @ `1f8ec7b6c99c63ca4d055f952fb6b7d24cd57f91ac1b1659cc85beacfedb9111`**.

## What changed (review the doc bytes, not this summary)
- **§2 — the label is now MANDATORY.** When complete/present/well-formed content fails its advisory checksum and is used in model context, m-9 **MUST** apply a local in-memory label: `untrusted-content` for original-truth-bearing kinds (`provider_output`, `tool_result`), `checksum_mismatch` for transcript kinds (`input_item`, `reasoning_replay`). A detected provider/tool mismatch **never silently retains original-truth status**. Label stays local/model-only, off durable/wire/operator/E3.
- **New §2.1 — total disposition/first-action table** over: clean/undetected; advisory-checksum mismatch split by content kind (provider/tool vs transcript); structural/completeness failure; missing referenced content; `objective_ref` re-resolve success/failure; `workspace_snapshot` match/mismatch/unresolvable. Each row states the mandatory local label, the exact existing report pair (`resumable` / — or `degraded` / `re_derive`), and the first action. No optional rows, no fall-throughs, no disposition outside `{resumable, degraded}`.
- **Unchanged** (you already closed these): fresh byte-bound identity (R1-F1); no durable/wire/operator/E3 edited claim + `edited_since_write`/RESUMABLE-with-labels retraction (R1-F2); split `objective_ref` (admission_ref) vs `workspace_snapshot` (DEGRADED, no MVP source) (R1-F3); the undetectable-well-formed-edit limit; immutable m-10 store/outcome; frozen `receipt_conflict`; journal-only edit surface.

## Review priorities (your gate)
(a) Is the trust label mandatory (not optional) for every checksum-mismatch-in-model-context row? (b) Is §2.1 total over every content class with a deterministic single report pair per row, all within `{resumable, degraded}`? (c) Does provider/tool mismatch never inherit original-truth? (d) Does the label stay off durable/wire/operator/E3, Route-2 inequalities stay `fail`, and m-10's `uncertain` stay on its own axis? (e) No lock/frozen byte moved (r17 `01b885fe…` UNMOVED).

Return `DESIGN_REVIEW_VERDICT`. On approve: m-10 close3 reciprocal + m-3 evidence confirm + m-1 boundary confirm.

## Boundaries
Design-only successor bytes + pair-review re-request. No lock moved; r17 `01b885fe…`, rev16 `3e3c5192…`, §D amendment, interface lock `cbd1893c…` UNMOVED. The m-10 settlement store stays uneditable; the edit surface is the m-9 journal only. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
ACTIONS_GIT_REF: docs-workspace disk action — one revised design doc (`2026-07-26-edited-session-onefile.md` @ `1f8ec7b6…`), this DESIGN relay, one append-only INDEX.md row; no frozen byte edited, no `frank/` action, no PLAN/T4/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` verification is recorded after relay lint.
