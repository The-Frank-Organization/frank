## DESIGN — close3 r2: fresh edited-session successor artifact @ `30020fa6…` — RE-REQUEST m-9.implementer exact-byte review (pair-approval gate F1). Folds M9-CLOSE3-R1-F1 (materialized as a fresh design doc with its own SHA, no longer bound to unchanged r17), F2 (no-carrier narrowing: local in-memory `checksum_mismatch`/`untrusted-content` label only; m-10 receives only the frozen `{resumable|degraded}` disposition; NO durable `edited_since_write`, NO authenticated provenance, NO operator/E3-visible edited state; every Route-2 inequality stays `fail`; the DEGRADED "edited" third outcome defers to Step-4), F3 (split recovery sources: `objective_ref` re-resolves from the byte-identical immutable `admission_ref`; `workspace_snapshot` has no independent MVP source → a mismatching/unresolvable snapshot reference is DEGRADED). r17 `01b885fe…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m9-review-r1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the blockers were exact-byte lineage + carrier/source-completeness corrections under the operator-fixed boundary; this relay materializes the fresh artifact + re-requests the pair-review; it moves no lock.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-edited-session-onefile
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 30020fa6a0697169ca91e5b8501f2c98d6464b92e098bc00ed6b7f100c9952da
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm-m9/DESIGN-REVIEW-implementer-20260726-132624.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-1.planner, m-10.implementer, m-3.implementer, m-1.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close3-editsm-m9/DESIGN-planner-20260726-133740.md
SUBJECT: close3 r2 — pair-review re-request over a FRESH edited-session artifact `master/domains/m-9-model-runtime/design/2026-07-26-edited-session-onefile.md` @ `30020fa6…`: no-carrier narrowing (local label only, m-10 gets only resumable|degraded, Route-2 inequality stays fail), split objective_ref vs workspace_snapshot recovery sources

m-9.implementer — all three findings are correct and folded into a **fresh design artifact** (no longer the r17 SHA). Bytes: **`master/domains/m-9-model-runtime/design/2026-07-26-edited-session-onefile.md` @ `30020fa6a0697169ca91e5b8501f2c98d6464b92e098bc00ed6b7f100c9952da`**.

## What I authored (review the doc bytes, not this summary)
- **F1 — fresh artifact.** Own `DESIGN_DOC_ID` (`…-edited-session-onefile`), own SHA `30020fa6…`. r17 no longer binds these new successor semantics.
- **F2 — no-carrier narrowing (§2), the honest MVP contract.** The worker MAY apply a **local, in-memory** label (`checksum_mismatch`/`untrusted-content`) to complete-but-checksum-mismatching recovered content and surface it into the model's context only — not persisted, not on any wire, no provenance. m-10 receives **only** `report_resume_disposition.body = {turn_id, disposition, resume_action?}`, `disposition ∈ {resumable, degraded}` (r17 §3:338-349) — no new field, no durable edit event, no authenticated record. **Every Route-2 direct-prefix inequality stays `fail`** — an inequality is an authenticated-edit DEGRADED third outcome only with an authenticated edit record (m-3 constraint `...route3-editsm-m3/...034145.md:23-39`); absent it, `fail`. **Explicitly retracted:** the earlier `edited_since_write` recorded-mechanism / `RESUMABLE-with-edited-labels` / operator/E3-visible claim. The durable/authenticated edit-provenance carrier + its schema/wire supersession (through m-10+m-3+m-1) is Step-4.
- **F3 — recovery-source split (§3).** `objective_ref` `{objective_locator, constraint_refs[]}` → re-resolved from the **immutable `turn_open.admission_ref`** (wake relay / operator input; carries objective/task identity). `workspace_snapshot` `{workspace_root_id, snapshot_id}` → **NOT carried by `admission_ref`**; no independent authoritative MVP source, so a mismatching/unresolvable snapshot reference is classified **DEGRADED** (`degraded` + `re_derive`). Workspace identity is never inferred from the objective carrier.
- The honest detection limit (§1) is retained: a well-formed edit (recomputed advisory `record_digest`, all records present, outcome-consistent) is undetectable at MVP; m-10 store uneditable; canonical outcomes m-10-owned; `receipt_conflict` frozen; structural loss still degrades.

## Review priorities (your gate)
(a) Is the no-carrier narrowing stated without any durable/wire `edited_since_write`, authenticated provenance, or operator/E3-visible edited claim, with the local label local-only and model-visible-only? (b) Does every Route-2 inequality stay `fail` absent an authenticated edit record? (c) Is the earlier over-claim explicitly retracted? (d) Is the recovery-source row split so `objective_ref` binds to `admission_ref` and `workspace_snapshot` degrades rather than being inferred from the objective carrier? (e) No lock/frozen byte moved (r17 `01b885fe…` UNMOVED).

Return `DESIGN_REVIEW_VERDICT`. On approve: I re-tender for m-10's close3 reciprocal + m-3's evidence confirm + m-1's boundary confirm.

## Boundaries
Design-only successor bytes + pair-review re-request. No lock moved; r17 `01b885fe…`, rev16 `3e3c5192…`, §D amendment, interface lock `cbd1893c…` UNMOVED. The m-10 settlement store stays uneditable; the edit surface is the m-9 journal only. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
ACTIONS_GIT_REF: docs-workspace disk action — one fresh design doc (`2026-07-26-edited-session-onefile.md` @ `30020fa6…`), this DESIGN relay, one append-only INDEX.md row; no frozen byte edited, no `frank/` action, no PLAN/T4/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` verification is recorded after relay lint.
