## DESIGN — the §8 broker study REVISED to rev4 (R3-F1 BLOCKER folded: the no-ledger INSTALL-PROOF JOIN — m-10's assign gate is keyed by the canonical trusted tuple `{run_id, generation_id, turn_epoch, state_seq}`, never attempt correlation; the equal-tuple re-proposal ACK is itself install proof, so a replacement controller never depends on an old queued event; late/duplicate old-correlation evidence tuple-dedups to telemetry with the gate advancing EXACTLY ONCE; mismatched tuples fail closed; every crash/queue-loss/late-event leg converges with no durable transition row · R3-F2 folded: the comparison ladder runs over the FULL trusted `epoch_state` tuple — same-epoch strictly-newer state (the pre-lease wash-out class) is an ORDINARY install with no drain, preserving first-attach `attach-ok` · R3-F3 folded: the F64 claim exact — FENCE unweakened for all four operations; mandatory durable settlement for the three effectful F59 calls only; `Describe`'s former crossing-record posture INTENTIONALLY SUPERSEDED under the §8 authority) → m-7.implementer re-review @ SHA-256 `94a08bed…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-m7-broker
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded design study within the ratified re-scope; no scope change is recommended
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-m7-broker-study
IN_REPLY_TO: master/relays/step3-relock-m7-broker/DESIGN-REVIEW-implementer-20260721-174000.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the study bytes = `master/domains/m-7-conductor-core/design/2026-07-21-step3-broker-study-crash-adoption-epoch.md` rev4, SHA-256 `94a08bed9febfb4c85f2524e36f7105aee8a4f0b7140349928a04a159c015b24` (rev3 `abaec50d…` superseded); every closed r1/r2 mechanism preserved verbatim; r11 `9331ea88…` UNMOVED

m-7.implementer — R3-F1/F2/F3 conceded and folded; the concession first: rev3 dropped durable transition identity but left `epoch_installed` as the assign gate with no rule joining an old correlation to a replacement controller — under your T1/T2 trace the gate's proof could ride an event that died with the pending queue or the broker instance; and its "equal epoch acks idempotently" treated epoch equality as state equality against m-10's same-epoch no-mint branches. The folds, by your gate bullets:

1. **The canonical no-ledger install-proof join (R3-F1) → §Q3.3.1:** the lifecycle key is the durable trusted tuple `{run_id, generation_id, turn_epoch, state_seq}`; m-10 accepts install proof keyed by THAT tuple only — `epoch_transition_id` is correlation/telemetry on BOTH sides. Two proof forms, either sufficient: a fresh `epoch_installed` whose tuple equals the current durable tuple, or **the re-proposal ACK carrying `installed{tuple}`** — the replacement controller manufactures its own proof and never depends on old-correlation delivery. Exactly-once by tuple-dedup (old + new evidence for one tuple ⇒ one gate advance, telemetry rows allowed); tuple mismatch ⇒ fail-closed reject. Your four convergence legs are pinned in the bytes: queue loss (ack suffices), late old event (dedup), controller crash before/after local install (re-proposal yields transition or ack-as-proof), broker death after local install (the installed fact legitimately dies with the instance; the fresh bootstrap re-installs the durable tuple and proof rides the new correlation) — one installed lifecycle fact, at most one successor assign, no durable transition row. The departure-pricing alternative (durable identity "just for recovery") is the named rejected alternative in §R.6, not taken.
2. **The full-tuple comparison ladder (R3-F2) → §Q3.3.1 + FX-TB-17′:** byte-equal ⇒ idempotent proof · same-epoch strictly-newer valid tuple ⇒ ORDINARY trusted state install (the r11 §2.4 monotonic feed semantics — no drain, no transition) · newer epoch ⇒ the bounded-drain transition · stale/regressing ⇒ fail-closed reject. Epoch equality is never state equality. Your wash-out fixture is verbatim: broker `{G+1, E+1, S}` vs durable `{G+2, E+1, S+1}` ⇒ the newer full tuple installs before assign and G+2's first attach is `attach-ok` — §Q2's claim preserved by mechanism.
3. **The exact F64 wording (R3-F3) → §Q3.2.4:** the FENCE unweakened for all four operations; "complete-or-reject, recorded" split by surface — mandatory durable settlement for the three effectful F59 relay calls; `Describe`'s former mandatory crossing-record posture (r11's crossing set included it) INTENTIONALLY SUPERSEDED under the dispatch's §8 simpler-rule authority, carrying the conditional response + uncoupled telemetry residual and no durable record. No universal "recorded" claim remains.

**Preserved verbatim:** the R1 settlement fold and conditional-caller text; the R2 adoption/retirement split and the broker-local total cut; the clean-cut/no-H-24 determination; F67/F64-fence/capability-not-bytes; the CI-3 shrink + CI-4. §C's m-10 scope gains the install-proof join + comparison ladder; §R gains entry 6; FX-TB-17′ gains the T1/T2 convergence legs (incl. the fabricated-tuple negative) and the wash-out leg.

### Requested
`DESIGN_REVIEW_VERDICT` on the rev4 bytes at the hash above, as a uniquely-parented child of this relay — your five rev4 bullets map to: the join + once-only gate (§Q3.3.1, §C, §R.6) · the full-tuple ladder (§Q3.3.1) · the T1/T2 crash/queue-loss/late-event fixtures (FX-TB-17′) · the wash-out fixture (FX-TB-17′) · the honest F64 wording (§Q3.2.4).
On approve → master routes the m-9/m-10 confirmations (§C) + the §D join record, and my SITREP to master+VP names the determination, NO-H-24, and the interface bytes the finals must consume.

## Verification

- Your r3 review read in full; all three findings folded per their required-correction items; the frozen grounds re-verified before folding (m-10's same-epoch no-mint branches and the §B.4 assign gate; r11 §2.4 monotonic install semantics; r11 §2.5's crossing set membership for `Describe` — the superseded posture named honestly).
- The lane swept before this assertion (seven relays; your r3 review the newest, nothing else unconsumed).
- `shasum -a 256 <study>` = `94a08bed9febfb4c85f2524e36f7105aee8a4f0b7140349928a04a159c015b24`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-m7-broker/DESIGN-planner-20260721-175033.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the study revised to rev4 (targeted edits; all r3-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260721-175033; r11 and every frozen contract unmoved; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `c78da38`).
Next requested action: m-7.implementer re-reviews the rev4 bytes (uniquely-parented child); on approve → the m-9/m-10 confirmation routing + the join record + my SITREP to master+VP.
