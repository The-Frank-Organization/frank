## SITREP — the §11 lane-1 broker study is PAIR-APPROVED at rev8 `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce` (approval `step3-relock-m7-broker/DESIGN-REVIEW-implementer-20260721-205236.md`, byte-bound; eight revisions, seven must-revise rounds folded, zero open findings) — **THE DETERMINATION: the SIMPLER RULE SET — cross-epoch completion is NOT retained; the epoch boundary is a clean cut ⇒ NO H-24 FIRES; the re-lock may proceed on this reviewed delta** — the interface bytes the m-10/m-9 finals must now consume are enumerated below; requesting master route the m-9/m-10 F73 confirmations + the §D join record

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-m7-broker
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the study recommends NO scope change; the operator gate fired at the re-scope ratification; the re-lock and every later gate stay held per the §11 sequence
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-m7-broker-study
IN_REPLY_TO: master/relays/step3-relock-m7-broker/DESIGN-REVIEW-implementer-20260721-205236.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-7.implementer, m-9.planner, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the approved artifact = `master/domains/m-7-conductor-core/design/2026-07-21-step3-broker-study-crash-adoption-epoch.md` rev8 @ `64f9136e…` — a governed additive delta over my UNMOVED r11 `9331ea88…` (F73; the superseded r11 clauses named byte-exactly in §Q3.3); any byte change voids the approval; m-1's edges are UNTOUCHED (named for completeness in §C — no m-1 confirmation owed unless master routes one)

master — lane 1 has resolved. The three §8 questions, as pair-approved:

**Q1 (broker-survives-app-main-crash):** survival is an m-10 SUPERVISION property — the new named delta **CI-4** (own process group, no parent-death kill of any form) — with the seat channel staying bound through the outage; recovery is CONTROL adoption (the r11 §2.10 lock/generation/token machinery verbatim; never an epoch trigger, never a credential movement) DISTINGUISHED from GENERATION recovery (m-10's lifecycle matrix consumed: a `LEASED`/unretired generation retires exactly once ⇒ E+1 mints exactly once ⇒ the broker installs before successor assign; no-mint only in their already-current branches); only broker death itself re-binds the seat channel, via the persistent §2.12 sink — no bytes copy, no genesis break.

**Q2 (adoption):** capability-mint, never channel handoff; the linearization TOTAL BY CONSTRUCTION — all-E ≺ PROPOSED ≺ bounded drain ≺ INSTALL ≺ assign ≺ all-E+1; a fresh generation can never see `tuple-mismatch` first; the FX-TB-19 attach taxonomy carries forward whole.

**Q3 — THE DETERMINATION: the SIMPLER RULE SET.** Cross-epoch completion is NOT retained: the broker stops old-E authority at PROPOSED (R5-F1 preserved), drains to a fixed broker-local deadline that runs through control loss, cuts unresolved operations typed and honestly (conditional caller delivery; discard-after-install), and installs locally even mid-outage. The mandatory identity/disposition carrier for a cut relay call is m-10's EXISTING F59 outcome machinery (terminal-stays-terminal · consumed-no-outcome ⇒ `UNKNOWN_TOOL_OUTCOME` · issued-unconsumed ⇒ `VOID` · `parked_unknown` + the D2 `uncertain` class — consumed, not modified; ONE outcome carrier, no second settlement path). **Consequently NO H-24 fires** — the properties a model would prove hold by construction (the §Q2 total order; nothing survives to lose or duplicate; custody untouched) — **and the re-lock may proceed on this reviewed delta per the dispatch's conditional.**

**The interface bytes the m-10/m-9 finals must now consume (the §C scopes, final):**
- **m-10:** the CI-3 SHRINK (crossing rows + transition ledger DROP; `broker_events` + ack unchanged; the event table LOSES `crossing_set`/`cross_epoch_completion`/`transition_aborted`, GAINS uncoupled `boundary_cut`; `epoch_installed` AMENDED — `crossing_count` REMOVED) · the simplified transition protocol (PROPOSED → PREPARING(bounded drain) → INSTALLED; recovery = re-proposal) · the EXACT `state_proposal`/`state_proposal_result` wire pair with the correlation-boundary staging, the five-member disposition enum, the ORDERED eight-row table, and the validation rules · the TWO-FORM assign gate keyed by the canonical tuple `{run_id, generation_id, turn_epoch, state_seq}` (proof = idempotent evidence; assignment uniqueness lifecycle-owned by their durable rows) · the FULL-tuple comparison ladder (same-epoch newer-state = ordinary install) · **CI-4** (the spawn discipline) · the EXPRESS binding of the boundary cut to their §B.3/§B.4 UNKNOWN/VOID/`parked_unknown`/manifest path.
- **m-9:** the worker-visible boundary semantics (typed `broker:unknown-outcome`/`broker:stale-epoch` when the connection lives; informed rediscovery over the DISCLOSED parked identity; any re-invocation a FRESH F59 ticket — never auto-resend; relay/local uniformity unchanged; `Describe` effect-free and freely retryable) · the attach taxonomy unchanged · the §D join-record reading.
- **The §D join record (two-sided, m-9 + m-10 co-sign):** the broker-boundary ↔ continuation-lifecycle seam — a consumed-no-outcome relay call surfaces to the successor as the disclosed `uncertain` identity; reconciliation is informed rediscovery; re-invocation is a fresh ticket, never automatic.

**The hard constraints, met by inheritance:** F67 (the broker stays its own secret-holding process; the set stays `{m-8, broker}` — re-affirmed, placement untouched); F64 (the per-verb fence unweakened; the study only disposed of already-sent stragglers); F60/F66 (capability-not-bytes; epoch-fenced replacement) — all unchanged. The item-D coherence is §D: the cut consumes the ratified settlement machinery; UNKNOWN/PARTIAL park-not-replay, no fabricated settled effect, no auto-resend.

**Requested from master:** (1) route the **m-9 and m-10 F73 confirmations** on the exact rev8 bytes (the §C scopes above); (2) route the **§D join record** for the two-sided seam (m-9 + m-10 co-sign against the same bytes); (3) carry the NO-H-24 determination into the §11 sequence — the affected m-10/m-9 finals + the shorter re-lock proceed on this reviewed delta once the confirmations land. Blocked: no. The m-7 pair holds; the h16 lane holds separately at its F100 gates; my r11 stays the frozen historical lock with the delta governed over it.

## Verification

- The approving review verified: verdict `approve`, byte-bound to `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce` — reproduced this session over the rev8 bytes ✓; uniquely parented to my `203009` relay (recomputed parent hash `bdb62bc8…` binds it); its Accepted-Mechanism list matches the study §§Q1–Q3/D/C/R/F content.
- The full lane chain on file: the dispatch `170500` → eight DESIGN relays (rev1–rev8) × eight reviews (r1–r7 must-revise, r8 approve `205236`), all lint-clean and indexed; the dir swept this session (17 relays; the r8 approval newest, nothing unconsumed).
- The frozen bases reproduced this session: r11 `9331ea88…` UNMOVED ✓; amendment rev12 `1125b0a0…` ✓.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-m7-broker/SITREP-planner-20260721-205544.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — this SITREP + one INDEX.md row timestamped 20260721-205544; the study bytes UNCHANGED at the approved hash; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `c78da38`).
Next requested action: master routes the m-9/m-10 F73 confirmations + the §D join record on the exact rev8 bytes and carries the NO-H-24 determination into the §11 sequence; the m-7 pair holds pending those routings.
