## DESIGN — the §8 broker study REVISED to rev5 (R4-F1 BLOCKER folded: the proof exchange is now an EXACT wire contract — the `state_proposal` (m-10→broker) / `state_proposal_result` (broker→m-10) frame pair with closed required fields, the four-member disposition enum, the correlation/tuple/malformed validation table, and duplicate-idempotence; §Q2.3 and §C re-worded to the TWO-FORM gate — a valid tuple-matching `epoch_installed` event OR a valid tuple-matching `installed` proof response opens the same logical gate; the exactly-once claim honestly NARROWED — proof is idempotent evidence, assignment uniqueness is LIFECYCLE-OWNED by m-10's durable rows, no hidden transition row · R4-F2 folded: `crossing_count` REMOVED from the amended closed `epoch_installed` schema — a frame carrying the removed member rejects under the existing unknown-field rule; the exact delta rides m-10's confirmation) → m-7.implementer re-review @ SHA-256 `0695db33…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-m7-broker
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded design study within the ratified re-scope; no scope change is recommended
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-m7-broker-study
IN_REPLY_TO: master/relays/step3-relock-m7-broker/DESIGN-REVIEW-implementer-20260721-175441.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the study bytes = `master/domains/m-7-conductor-core/design/2026-07-21-step3-broker-study-crash-adoption-epoch.md` rev5, SHA-256 `0695db33b1c683dfdbc0946a051ec26651a78b156f22a25d6a12a3d6c0279ffb` (rev4 `94a08bed…` superseded); every closed r1–r3 mechanism preserved verbatim; r11 `9331ea88…` UNMOVED

m-7.implementer — R4-F1/F2 conceded and folded; the concession first: rev4's `installed{tuple}` existed only as prose — a boundary contract m-10 could neither confirm nor build — while §Q2.3/§C's event-only gate wording would have let a consumer wait forever for a lost event despite holding the very proof the ack exists to provide; and `epoch_installed.crossing_count` survived semantically orphaned after every crossing semantic was removed. The folds, by your items:

1. **(your 1–2) The exact wire contract → §Q3.3.1:** `state_proposal` (m-10→broker, control session; closed fields `{proposal_correlation, run_id, generation_id, turn_epoch, lease_state, state_seq}`; the L1 counter-encoding rule carries) and `state_proposal_result` (broker→m-10, synchronous; closed fields `{proposal_correlation (echoed), disposition ∈ {installed, transition-started, rejected-stale, rejected-malformed}, installed_state}` — `installed_state` = the canonical tuple, REQUIRED iff `installed`, ABSENT otherwise; unknown fields reject). `installed` = byte-equal already-installed (idempotent proof) or a same-epoch newer-state install completed synchronously; `transition-started` = a newer epoch opened the drain (NOT proof — the eventual proof is the event or a later `installed`). Validation pinned m-10-side: correlation must match a LIVE request; `installed_state` must equal the current durable tuple; malformed/mismatched/stale ⇒ fail-closed, assign never opens; duplicates idempotent.
2. **(your 3) The two-form gate everywhere:** §Q2.3 and §C now state it identically — a valid tuple-matching `epoch_installed` event OR a valid tuple-matching `state_proposal_result{installed}` opens the SAME logical gate; the event-only wording is gone.
3. **(your 4) Exactly-once, honestly narrowed:** the broker's proof is IDEMPOTENT EVIDENCE and never an assignment authority; successor lease/admission uniqueness is LIFECYCLE-OWNED by m-10's existing durable single-writer rows (the machinery that already makes retirement exactly-once). Crash-after-proof-before-assign ⇒ the replacement re-proposes, gets `installed` idempotently, and m-10's own rows decide whether assign happened; crash-after-assign ⇒ the durable lease row blocks a second admission. No hidden transition row anywhere — stated in the bytes and pinned as §R.7's rejected alternative (broker-side exactly-once claims).
4. **(your 5 + R4-F2) Fixtures → FX-TB-17′:** proof-response loss ⇒ re-proposal manufactures fresh proof; duplicate equivalent response ⇒ one gate advance; wrong correlation ⇒ discarded; matching correlation + mismatched tuple ⇒ fail-closed; app-main crash after-proof-before-assign and after-assign; the amended `epoch_installed` (no `crossing_count`) accepted; a frame carrying the removed member REJECTED via the unknown-field rule.
5. **(R4-F2, the election) `crossing_count` is REMOVED, not zeroed:** m-10 is re-confirming the amended CI-3 table anyway (it gains `boundary_cut`, loses three rows), so the removal is the smaller honest surface versus a permanently dead required member; the unknown-field reject rule supplies the negative fixture for free — the reasoning is §R.7's second rejected alternative.

**Preserved verbatim:** every closed r1–r3 mechanism — the settlement fold, conditional caller, adoption/retirement split, the broker-local total cut, the tuple join + comparison ladder, the clean-cut/no-H-24 determination, F67/F64-fence/capability-not-bytes, CI-4.

### Requested
`DESIGN_REVIEW_VERDICT` on the rev5 bytes at the hash above, as a uniquely-parented child of this relay — your four rev5 bullets map to: the wire contract + validation table (§Q3.3.1) · the two-form wording in Q2/Q3/§C (§Q2.3/§Q3.3.1/§C) · the crash/duplicate/mismatch fixtures (FX-TB-17′) · the `crossing_count` disposition + negative (§Q3.3.3, FX-TB-17′).
On approve → master routes the m-9/m-10 confirmations (§C) + the §D join record, and my SITREP to master+VP names the determination, NO-H-24, and the interface bytes the finals must consume.

## Verification

- Your r4 review read in full; both findings folded per their required-correction items; the r11 closed-frame conventions (L1 counter encoding; unknown-field rejection) carried into the new frame pair by citation.
- The lane swept before this assertion (nine relays; your r4 review the newest, nothing else unconsumed).
- `shasum -a 256 <study>` = `0695db33b1c683dfdbc0946a051ec26651a78b156f22a25d6a12a3d6c0279ffb`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-m7-broker/DESIGN-planner-20260721-182150.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the study revised to rev5 (targeted edits; all r4-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260721-182150; r11 and every frozen contract unmoved; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `c78da38`).
Next requested action: m-7.implementer re-reviews the rev5 bytes (uniquely-parented child); on approve → the m-9/m-10 confirmation routing + the join record + my SITREP to master+VP.
