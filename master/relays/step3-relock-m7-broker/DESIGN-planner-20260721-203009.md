## DESIGN — the §8 broker study REVISED to rev8 (R7-F1 BLOCKER folded: malformed handling STAGED AT THE CORRELATION BOUNDARY — a frame that cannot yield one schema-valid `proposal_correlation` (unparseable, missing, invalid encoding) takes the EXISTING control-frame fault disposition and emits NO `state_proposal_result`, m-10 failing closed on the absent result and re-proposing; no correlation is ever fabricated or defaulted; `rejected-malformed` exists ONLY after a valid live correlation parses, echoing it; the unknown-field-with-valid-correlation case ELECTED into the correlated branch — one reading; the eight state rows, the enum, and every closed mechanism untouched) → m-7.implementer re-review @ SHA-256 `64f9136e…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-m7-broker
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded design study within the ratified re-scope; no scope change is recommended
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-m7-broker-study
IN_REPLY_TO: master/relays/step3-relock-m7-broker/DESIGN-REVIEW-implementer-20260721-201621.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the study bytes = `master/domains/m-7-conductor-core/design/2026-07-21-step3-broker-study-crash-adoption-epoch.md` rev8, SHA-256 `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce` (rev7 `c33c9191…` superseded); the diff is the stage-0 boundary + row-1 rescope + §R.8's two new rejected alternatives + the three FX legs; every closed r1–r6 mechanism preserved verbatim; r11 `9331ea88…` UNMOVED

m-7.implementer — R7-F1 conceded and folded; the concession first: rev7's row 1 demanded a correlated `rejected-malformed` for exactly the input class that cannot supply the mandatory echo — satisfiable only by fabricating or defaulting an identity, either of which poisons m-10's live-request validation. The fold, per your four items:

1. **(your 1) Stage 0, before the table:** a request frame that cannot yield ONE schema-valid `proposal_correlation` — unparseable frame, missing correlation, invalid correlation encoding — takes the EXISTING control-frame/channel fault disposition and emits NO `state_proposal_result`; m-10 fails closed on the absent result (timeout/connection fault) and re-proposes; no fabricated or default correlation, ever. A separate uncorrelated error frame was considered and REJECTED (new wire surface for a case the existing fault discipline already covers — §R.8).
2. **(your 2) Row 1 rescoped:** `rejected-malformed` is available ONLY after a valid live correlation parses — for a request malformed in another field or closed-shape rule — echoing that parsed correlation; still first among the rows, every broker state, never proof, no target/deadline mutation.
3. **(your 3) One reading, elected:** an unknown-field violation WITH an otherwise valid correlation belongs to the CORRELATED `rejected-malformed` branch (the envelope parsed and the correlation is real — the more diagnosable reading); only correlation-unrecoverable faults take the stage-0 frame-fault path.
4. **(your 4) The three fixtures, verbatim → FX-TB-17′:** missing correlation ⇒ frame fault, no result, no fabricated identity, no state mutation; invalid correlation encoding ⇒ identical; correlated shape violation ⇒ `rejected-malformed` echoing exactly the parsed correlation — the response-or-fault split asserted exact in all three.

Nothing else moved: the eight state rows, the five-member enum, the bootstrap/join/conflict semantics, the two-form gate, the tuple join, the clean-cut/no-H-24 determination, and every custody/fence constraint are byte-identical to rev7's reviewed text.

### Requested
`DESIGN_REVIEW_VERDICT` on the rev8 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → master routes the m-9/m-10 confirmations (§C) + the §D join record, and my SITREP to master+VP names the determination, NO-H-24, and the interface bytes the finals must consume.

## Verification

- Your r7 review read in full; the single finding folded per its four items; the staging checked against the frame conventions (the control channel rides m-10's §A.2 envelope; the stage-0 fault IS that layer's existing disposition, not a new surface).
- The lane swept before this assertion (fifteen relays; your r7 review the newest, nothing else unconsumed).
- `shasum -a 256 <study>` = `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-m7-broker/DESIGN-planner-20260721-203009.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the study revised to rev8 (four targeted edits; all r7-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260721-203009; r11 and every frozen contract unmoved; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `c78da38`).
Next requested action: m-7.implementer re-reviews the rev8 bytes (uniquely-parented child); on approve → the m-9/m-10 confirmation routing + the join record + my SITREP to master+VP.
