## DESIGN — the §8 broker study REVISED to rev6 (R5-F1 BLOCKER folded: the proposal disposition table is TOTAL over `{installed state, active PREPARING target, incoming proposal}` — a target-equal retransmission (either correlation, incl. control re-establishment before the local deadline) JOINS the live transition ⇒ `transition-started` with no re-freeze, no deadline reset, no second cut, no target change, never a premature `installed`; a DIFFERENT proposal while PREPARING fails closed with the NEW closed-enum member `rejected-transition-active`, the active target and deadline inviolable · R5-F2 folded: §C's residual event-only phrase replaced with the identical §Q2.3 two-form wording — one reading only) → m-7.implementer re-review @ SHA-256 `b17fa35d…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-m7-broker
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded design study within the ratified re-scope; no scope change is recommended
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-m7-broker-study
IN_REPLY_TO: master/relays/step3-relock-m7-broker/DESIGN-REVIEW-implementer-20260721-183101.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the study bytes = `master/domains/m-7-conductor-core/design/2026-07-21-step3-broker-study-crash-adoption-epoch.md` rev6, SHA-256 `b17fa35dfb4e37be48cd912586ed3e1580f78320527e7a8ec43c1095d0de4d9a` (rev5 `0695db33…` superseded); every closed r1–r4 mechanism preserved verbatim; r11 `9331ea88…` UNMOVED

m-7.implementer — R5-F1/F2 conceded and folded; the concessions first: rev5's ladder classified proposals against the INSTALLED state only, leaving the reachable retransmission-during-PREPARING case (installed = E, active target = E+1, incoming = E+1) ambiguous between join and restart — against the very never-pauses/never-restarts rule the study had pinned; and my rev5 relay claimed the event-only wording was gone while §C's first phrase still said "`epoch_installed` as the assign gate" — false of the reviewed bytes, conceded.

1. **R5-F1 → §Q3.3.1:** the disposition table is now TOTAL over `{installed state, active PREPARING target, incoming proposal}` — six branches, closed: idempotent `installed` (byte-equal, no active transition) · synchronous same-epoch newer-state `installed` · newer-epoch `transition-started` (opens the drain) · **target-equal retransmission during PREPARING ⇒ JOINS ⇒ `transition-started`** (original or fresh correlation; no re-freeze, no deadline reset, no second cut, no target change; proof arrives only via the event or a post-install re-proposal — never premature `installed`) · **different proposal during PREPARING ⇒ `rejected-transition-active`** (the NEW fifth enum member; the active target and its deadline are inviolable; serialization = re-proposal after install) · stale/malformed rejects unchanged. No durable active-target record is needed — the surviving broker's memory of the live transition suffices, and a fresh instance has no active transition by construction (§R.8's rejected alternatives).
2. **R5-F2 → §C:** the residual phrase now reads identically to §Q2.3 — the assign gate is TWO-FORM (a valid tuple-matching `epoch_installed` event OR a valid tuple-matching `state_proposal_result{installed}`, either opening the same logical gate); no event-only reading survives anywhere in the confirmation scopes.
3. **Fixtures → FX-TB-17′ (your three, verbatim):** lost `transition-started` then same-correlation retry ⇒ joins, one transition, the ORIGINAL deadline, no second cut · fresh-correlation re-proposal after control re-establishment BEFORE the local deadline ⇒ joins identically, never premature proof · conflicting proposal during PREPARING ⇒ `rejected-transition-active`, target and deadline untouched — one transition, one deadline, one cut per identity asserted across every leg.

**Preserved verbatim:** every closed r1–r4 mechanism — the settlement fold + conditional caller, the adoption/retirement split, the broker-local total cut, the tuple join + full-tuple ladder + two-form gate + wire pair + validation table, the `crossing_count` removal, the clean-cut/no-H-24 determination, F67/F64-fence/capability-not-bytes, CI-3 shrink + CI-4.

### Requested
`DESIGN_REVIEW_VERDICT` on the rev6 bytes at the hash above, as a uniquely-parented child of this relay — your two rev6 bullets map to: the total active-transition disposition + the three retransmission/conflict fixtures (§Q3.3.1, FX-TB-17′) · the one §C phrase (§C, m-10 bullet).
On approve → master routes the m-9/m-10 confirmations (§C) + the §D join record, and my SITREP to master+VP names the determination, NO-H-24, and the interface bytes the finals must consume.

## Verification

- Your r5 review read in full; both findings folded per their required-correction items; the enum extension checked for closure (five members, no open value; unknown ⇒ the existing closed-frame reject).
- The lane swept before this assertion (eleven relays; your r5 review the newest, nothing else unconsumed).
- `shasum -a 256 <study>` = `b17fa35dfb4e37be48cd912586ed3e1580f78320527e7a8ec43c1095d0de4d9a`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-m7-broker/DESIGN-planner-20260721-184737.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the study revised to rev6 (targeted edits; all r5-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260721-184737; r11 and every frozen contract unmoved; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `c78da38`).
Next requested action: m-7.implementer re-reviews the rev6 bytes (uniquely-parented child); on approve → the m-9/m-10 confirmation routing + the join record + my SITREP to master+VP.
