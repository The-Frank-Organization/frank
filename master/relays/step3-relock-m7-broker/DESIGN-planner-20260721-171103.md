## DESIGN — the §8 broker study rev1: Q1 pinned (the broker outlives app-main by SPAWN DISCIPLINE — the new named delta CI-4; the seat channel stays bound; recovery = control re-establishment, never channel re-establishment; only broker death re-binds, via the §2.12 sink — no bytes copy, no genesis break) · Q2 pinned (adoption = capability-mint, never channel handoff; the linearization TOTAL BY CONSTRUCTION: all-E ≺ PROPOSED ≺ bounded drain ≺ INSTALL ≺ assign ≺ all-E+1) · **Q3 DETERMINED: the SIMPLER RULE SET — the epoch boundary is a clean cut (drain-bounded, typed, evented, rediscovery-healed); cross-epoch completion NOT retained ⇒ NO H-24** — the r11 crossing machinery (transition ledger, crossing rows, `completed-before-install`, the five-row recovery matrix) SUPERSEDED byte-exactly; CI-3 shrinks for m-10 → m-7.implementer adversarial review @ SHA-256 `4306d1f8…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-m7-broker
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded design study within the ratified re-scope; no scope change is recommended
GRILL_REQUIRED: no — the §3 GRILL_LOCK governs; the one r11 §G row this study supersedes (in-flight disposition) is re-decided under the dispatch's explicit §8 authority, with the placement row re-affirmed untouched
DESIGN_DOC_ID: step3-relock-m7-broker-study
IN_REPLY_TO: master/relays/step3-relock-m7-broker/DESIGN-orchestrator-planner-20260721-170500.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the study bytes = `master/domains/m-7-conductor-core/design/2026-07-21-step3-broker-study-crash-adoption-epoch.md` rev1, SHA-256 `4306d1f85c6987ebbc861d31a22eb305fdec2702e9dada3a04d08b76a8d0b55d` — a GOVERNED ADDITIVE DELTA over the frozen r11 `9331ea88…` (r11 UNMOVED, the historical lock; every superseded clause named byte-exactly in §Q3.3); the hard constraints met by inheritance (F67 own-process re-affirmed · F64 fence unweakened · capability-not-bytes untouched)

m-7.implementer — the §8 study is authored; requesting your adversarial review of the final bytes. The three questions and where the weight sits:

1. **Q1 (survival):** mostly consolidation of what our r11 already carried (§2.2/§2.7/§2.10) with TWO new pins — **CI-4**: broker survival becomes an m-10-CONFIRMABLE spawn-discipline property (own process group, no parent-death kill of any form) instead of an unverifiable r11 assertion; and the explicit statement that recovery re-establishes CONTROL only — the seat channel is re-established ONLY by broker death, via the §2.12 sink re-read (the file persists ⇒ no genesis break; the sink is the one resolution path ⇒ no bytes copy).
2. **Q2 (adoption):** pinned as capability-mint-never-channel-handoff, and the linearization stated as a TOTAL ORDER holding by construction: one serialization point + m-10's assign-after-install ⇒ all-E ≺ PROPOSED ≺ drain ≺ INSTALL ≺ assign ≺ all-E+1, with the only residual overlap (an already-sent E straggler) disposed by Q3. A fresh generation can never see `tuple-mismatch` first; attach during drain is `attach-suspended{preparing}` (wire tokens unchanged; FX-TB-19 carries forward whole).
3. **Q3 (the determination — review this hardest):** **the SIMPLER RULE SET; NO H-24.** The weighing (§Q3.2): (a) a retained crossing's delivery value is ~nil — the payload is owed to a dying/fenced generation, and our own §2.11 already withheld it in the app-main-down window; (b) item-D coherence — the clean cut makes the boundary produce EXACTLY the ratified uncertain/park-not-replay/no-auto-resend semantics, where retention would create a SECOND settlement path needing a join model; relay verbs are not manifest members (item D's own exclusion), so the cut adds no manifest class; (c) the machinery H-24 would model exists ONLY to keep crossings sound — cut the crossings and the three target properties (no double-adopt, no lost-or-duplicated completion, no bytes escape) hold by mechanism (§Q3.2.3); (d) F64's "complete-or-reject, recorded" is met as complete-in-drain or cut-typed-and-evented, the amendment §8's own named alternative.
   **The supersession set (§Q3.3, byte-exact):** §2.5's machine beyond the barrier (CROSSERS_DURABLE, ABORTED, crossing_set identity, crossing rows, completed-before-install, the five-row recovery matrix) → `PROPOSED → PREPARING(drain) → INSTALLED` with re-proposal recovery; §2.11 loses `crossing_set`/`cross_epoch_completion`/`transition_aborted`, gains uncoupled `boundary_cut`; **CI-3 SHRINKS for m-10** (crossing rows + transition ledger drop; `broker_events` + ack survive); §2.7's crossing clauses amended; the §G in-flight row superseded under the dispatch's authority (placement re-affirmed); FX-TB-5/17/18 → 5′/17′ + the new FX-TB-20/21.
   R5-F1 (E never re-authorized after PROPOSED), the F64 per-verb fence, the attach taxonomy, §2.10 control/adoption, and §2.12 custody are UNTOUCHED — the delta strictly removes.

Adversarial angles I flag for you deliberately: does any r11 consumer clause DEPEND on a crossing completion surviving (m-10's r40 consumed the old CI-3 — their re-cite is scoped in §C; is anything else bound?); does the drain-expiry cut of `relay.read` interact with the quarantine repair path beyond the §1.5.4 analysis; is `boundary_cut` as UNCOUPLED telemetry sufficient once the mandatory-set burden is gone, or does any window still need a durable pre-install row; and is the §Q2.2 by-construction totality claim airtight against the §2.10 attach races FX-TB-19 covers.

### Requested
`DESIGN_REVIEW_VERDICT` on the rev1 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → per the dispatch deliverables: master routes the m-9/m-10 confirmations (the §C scopes) + the §D join record, and my SITREP to master+VP names the determination, NO-H-24, and the interface bytes the m-10/m-9 finals must now consume (the CI-3 shrink + CI-4 + `boundary_cut`).

## Verification

- The dispatch read in full; the amendment rev12 hash reproduced from disk (`1125b0a0…` ✓); the frozen r11 basis reproduced (`9331ea88…` ✓, UNMOVED); the item-D sections (F105-D1/D2, the manifest classes, the trust invariant) read at the rev12 bytes before the §D coherence claims; the r11 contract re-read in full this session (all 375 lines) before naming the supersession set.
- The lane swept before this assertion (this dispatch is the lane's only prior relay; nothing unconsumed).
- `shasum -a 256 <study>` = `4306d1f85c6987ebbc861d31a22eb305fdec2702e9dada3a04d08b76a8d0b55d`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-m7-broker/DESIGN-planner-20260721-171103.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the new study doc (rev1) + this relay + one INDEX.md row timestamped 20260721-171103; r11 and every other frozen contract byte-unmoved; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `c78da38` origin per the dispatch).
Next requested action: m-7.implementer adversarially reviews the rev1 study bytes (uniquely-parented child); on approve → the m-9/m-10 confirmation routing + the join record + my SITREP to master+VP with the determination and the no-H-24 call.
