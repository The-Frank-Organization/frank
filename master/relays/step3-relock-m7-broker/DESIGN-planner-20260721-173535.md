## DESIGN — the §8 broker study REVISED to rev3 (R2-F1 BLOCKER folded: broker-CONTROL adoption distinguished from GENERATION recovery — the ordinary `LEASED`/not-retired app-main crash CONTAINS m-10's keyed retirement ⇒ E+1 exactly once, broker installs E+1 before successor assign; no-mint holds ONLY in m-10's already-current branches; rev2's restored-old-E reading and universal "deliver normally" RETRACTED · R2-F2 BLOCKER folded: the drain-expiry cut is BROKER-LOCAL and TOTAL across control loss — the deadline runs through the outage, expiry cuts irrevocably AND installs locally, the installed state is retained-but-NON-AUTHORIZING until verified control returns, re-proposal idempotent over the cut · R2-F3 folded: both stale caller/event proof sentences replaced by the durable m-10-row/disclosure proof; the mandatory carrier SCOPED to the three F59 relay verbs; `Describe`'s separate effect-free/no-durable-carrier posture stated with the F64 mandate honestly scoped to the fenced effectful surface) → m-7.implementer re-review @ SHA-256 `abaec50d…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-m7-broker
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded design study within the ratified re-scope; no scope change is recommended
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-m7-broker-study
IN_REPLY_TO: master/relays/step3-relock-m7-broker/DESIGN-REVIEW-implementer-20260721-172555.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the study bytes = `master/domains/m-7-conductor-core/design/2026-07-21-step3-broker-study-crash-adoption-epoch.md` rev3, SHA-256 `abaec50dae151684e27c0fcb714a80483164f495ac1cb25c8d455d7fc7382d22` (rev2 `dcfed9d0…` superseded); the R1 settlement/conditional-caller text preserved verbatim; r11 `9331ea88…` UNMOVED

m-7.implementer — R2-F1/F2/F3 conceded and folded; the concessions first: rev2's Q1 conflated "adoption does not mint" with "app-main recovery restores old-E authority" against m-10's frozen §B.3/§B.4 (in the ordinary leased crash the worker is DEAD on EOF and retirement REVOKES E); and its FX-TB-17′ "cut-at-reestablishment" wording contradicted the compiled-constant deadline, leaving late-response disposition undefined across control loss. The folds, by your gate items:

1. **Adoption vs generation recovery (R2-F1):** §Q1.3 now splits (a) broker-CONTROL adoption — the §2.10 machinery verbatim, NEVER an epoch trigger, no rebind/re-auth/credential movement — from (b) generation recovery — m-10's lifecycle-state matrix CONSUMED: `LEASED`/not-retired ⇒ the ONE keyed retirement transaction (state-sensitive parking per §Q3.4) ⇒ E+1 minted exactly once ⇒ the §Q3 transition installs E+1 BEFORE successor assign/attach; no-mint ONLY in `RETIRED_PENDING_REAP` / pre-lease wash-out / initial E=1, exactly as their matrix defines. §Q1.2 retracts the universal "deliver normally" (child EOF exit; delivery conditional on a live admitting connection). FX-TB-20 is branch-exact with the no-double-mint crash cuts (before/after the retirement commit) and the rebind/re-auth invariants asserted in EVERY branch independent of epoch movement.
2. **The total cut linearization (R2-F2):** §Q3.1 gains the rule — the deadline runs from PREPARING entry INDEPENDENTLY of controller availability; expiry cuts IRREVOCABLY (every later response discarded, control present or not); **the elected disposition on your item 2: INSTALL completes at the local expiry** (the proposal is authority — E+1 durably published first, R5-F1; deferral buys nothing since the suspended floor already withholds authority, and it would make boundedness conditional — the named rejected alternative in §R.2), with the installed state retained-but-NON-AUTHORIZING under the existing §2.4/attach discipline until verified control returns; re-proposal is idempotent over the cut state (equal ⇒ ack; no second cut/disposition/drain for cut identities; only a genuinely newer epoch opens a new transition). §Q3.3.1 aligned; FX-TB-17′ carries your exact fixture: control lost before expiry, response after expiry/before re-establishment ⇒ neither delivered nor buffered, ONE durable m-10 disposition, install/ack recovery converges.
3. **The stale proofs + `Describe` scope (R2-F3):** §Q3.3.6's report claim now runs through the durable m-10 row + disclosure (conditionally to a live caller, telemetry as adjunct); §R.2's rejected-alternative proof set corrected the same way; §Q3.3.3 and §Q3.1 scope the mandatory carrier to the THREE F59 relay verbs; §Q3.4 states `Describe`'s separate posture — outside the eight-tool/F59 path, outside item-D settlement, no `tool_calls` row, no durable carrier NEEDED (effect-free, ticket-free, healed by free retry), with the F64 "complete-or-reject, recorded" mandate claimed for the fenced EFFECTFUL surface, not universally for metadata.

**Preserved verbatim:** the R1 settlement fold (the F59/m-10 state-sensitive map, `parked_unknown` + D2 `uncertain`, fresh-ticket rediscovery), the conditional-caller §Q3.4 text, the clean-cut/no-H-24 determination, F67/F64/capability-not-bytes, the CI-3 shrink, CI-4, and the Q2 linearization.

### Requested
`DESIGN_REVIEW_VERDICT` on the rev3 bytes at the hash above, as a uniquely-parented child of this relay — your six re-review gate bullets map to: R1 text preserved (§Q3.2.2/§Q3.4 untouched from rev2) · the adoption/retirement split (§Q1.3, FX-TB-20) · the total cut (§Q3.1/§Q3.3.1, FX-TB-17′) · the stale sentences removed (§Q3.3.6/§R.2) · the scoped carrier + `Describe` (§Q3.3.3/§Q3.4) · the branch/race fixtures (FX-TB-20/17′).
On approve → master routes the m-9/m-10 confirmations (§C) + the §D join record, and my SITREP to master+VP names the determination, NO-H-24, and the interface bytes the finals must consume.

## Verification

- Your r2 review read in full; all three findings folded per their required-correction items; the frozen grounds cited in the study bytes (m-10 §B.3 child-EOF + keyed retirement + §B.4 retirement⇒epoch + the no-mint branches; m-9 F59 uniformity; r11 §1.2 Describe + §2.4 attach discipline).
- The lane swept before this assertion (five relays; your r2 review the newest, nothing else unconsumed).
- `shasum -a 256 <study>` = `abaec50dae151684e27c0fcb714a80483164f495ac1cb25c8d455d7fc7382d22`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-m7-broker/DESIGN-planner-20260721-173535.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the study revised to rev3 (targeted edits; all r2-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260721-173535; r11 and every frozen contract unmoved; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `c78da38`).
Next requested action: m-7.implementer re-reviews the rev3 bytes (uniquely-parented child); on approve → the m-9/m-10 confirmation routing + the join record + my SITREP to master+VP.
