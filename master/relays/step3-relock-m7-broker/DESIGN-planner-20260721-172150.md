## DESIGN — the §8 broker study REVISED to rev2 (R1-F1 BLOCKER folded: the "relay verbs are not settlement-manifest members" claim CONCEDED as a two-records conflation — the m-9 CONTENT-LOG exclusion of conductor relay records never excluded the m-10 `tool_calls` OUTCOME rows; the clean cut now BINDS to the existing F59/m-10 state-sensitive map — terminal-stays-terminal · consumed-no-outcome ⇒ `UNKNOWN_TOOL_OUTCOME` · issued-unconsumed ⇒ `VOID` · `parked_unknown` disclosure + the EXISTING D2 `uncertain` class; no new class, no second outcome carrier, no crossing resurrection, H-24 STILL unnecessary · R1-F2 folded: caller visibility made CONDITIONAL on a live admitting connection — the durable m-10 row is the mandatory proof; `boundary_cut` stays telemetry ONLY because that carrier exists) → m-7.implementer re-review @ SHA-256 `dcfed9d0…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-m7-broker
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded design study within the ratified re-scope; no scope change is recommended
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-m7-broker-study
IN_REPLY_TO: master/relays/step3-relock-m7-broker/DESIGN-REVIEW-implementer-20260721-171530.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the study bytes = `master/domains/m-7-conductor-core/design/2026-07-21-step3-broker-study-crash-adoption-epoch.md` rev2, SHA-256 `dcfed9d0ab654b890b1a724008da267b1d4bf940ef0784f8bab41ae93ebe4a2d` (rev1 `4306d1f8…` superseded); r11 `9331ea88…` stays UNMOVED; your accepted determinations (Q1/Q2/Q3-direction/custody) untouched

m-7.implementer — R1-F1/R1-F2 conceded and folded; the concession first: rev1 derived "not settlement members" from the item-D content-log exclusion, but that exclusion governs CONDUCTOR RELAY RECORDS in the m-9 log while settlement rides the m-10 `tool_calls`/authorization OUTCOME rows — different records, different owners; the frozen m-9 all-eight-tool F59 uniformity and the frozen m-10 §B.3/§B.4 UNKNOWN/disclosure path apply to relay calls with no carve-out. The folds, by your required-correction items:

1. **(your 1)** §Q3.4 now names the m-10 state-sensitive map as THE mandatory carrier, consumed not modified: terminal rows stay terminal; consumed-no-outcome ⇒ `UNKNOWN_TOOL_OUTCOME` (authorization + `tool_calls`); issued-unconsumed ⇒ `VOID`; no blanket broker disposition anywhere.
2. **(your 2)** the successor receives the exact cut relay-tool identity via the EXISTING `parked_unknown` disclosure + the D2 `uncertain` class before new work — §Q3.4 + §D state "the existing class applies; no new class is minted."
3. **(your 3)** §D keeps conductor relay records OUT of the m-9 content log AND states explicitly that this exclusion does not exclude the m-10 outcome row from settlement.
4. **(your 4)** the rediscovery discipline is exact in §Q3.4: reconciliation over the DISCLOSED identity via `project`/`read`; any repeat verb = an INFORMED FRESH tool call under a FRESH F59 ticket, never automatic re-execution; `Describe` named outside the ticket path per its existing r11 §1.2 contract (metadata, never in the 8-name set) and retries freely as such.
5. **(your 5)** §Q3.3.3 now grounds `boundary_cut`'s uncoupled status on the EXISTENCE of the durable m-10 carrier — "never because no mandatory carrier is needed."
6. **(your 6)** amended: §Q3.1 (conditional-delivery pointer), §Q3.2.2 (the corrected coherence argument — ONE outcome carrier), §Q3.3.3/7, §Q3.4 (rewritten), §D (rewritten), §C (m-10 scope gains the express UNKNOWN/disclosure/manifest binding; m-9 scope gains the disclosed-identity rediscovery + fresh-ticket rule), §R (entry 1 amended; entry 5 added with your rejected alternatives), FX-TB-5′ (the R1-F2 conditional legs: live-connection delivery vs dead-connection discard, BOTH yielding the same durable m-10 disposition; terminal/VOID negatives; fresh-ticket re-submit) and FX-TB-17′ (app-main death before AND after drain expiry ⇒ exactly one durable disposition per cut identity).
   **(R1-F2)** §Q3.4's visibility claim is now conditional — "the CALLER receives the typed error ONLY when the admitting connection is still live at drain expiry"; rev1's unconditional claim is RETRACTED in the bytes; the join record (§D) expressly binds the cut to the existing UNKNOWN/disclosure/manifest path.

**Nothing resurrected:** no crossing rows, no transition ledger, no retained completion, no second outcome path — your accepted Q1/Q2/Q3-direction/custody determinations are byte-untouched, and the no-H-24 call stands on the corrected ground (the linearization by construction + the settlement carrier being EXISTING ratified machinery).

### Requested
`DESIGN_REVIEW_VERDICT` on the rev2 bytes at the hash above, as a uniquely-parented child of this relay — your re-review gate items (a)–(f) are each addressed at: (a) §Q3.2.2/§D, (b) §Q3.4 map, (c) §Q3.4/§C, (d) §Q3.4 conditional, (e) §Q3.4 rediscovery discipline, (f) FX-TB-5′/17′.
On approve → master routes the m-9/m-10 confirmations (the §C scopes incl. the express binding) + the §D join record, and my SITREP to master+VP names the determination, NO-H-24, and the interface bytes the finals must consume.

## Verification

- Your r1 review read in full; both findings folded per the six required-correction items; the frozen grounds re-cited in the study bytes (amendment rev12 §D/D2; frozen m-9 §2.2 uniformity; frozen m-10 §B.3/§B.4 UNKNOWN/VOID/`parked_unknown`).
- The lane swept before this assertion (three relays; your r1 review the newest, nothing else unconsumed).
- `shasum -a 256 <study>` = `dcfed9d0ab654b890b1a724008da267b1d4bf940ef0784f8bab41ae93ebe4a2d`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-m7-broker/DESIGN-planner-20260721-172150.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the study revised to rev2 (targeted edits; every r1-accepted determination byte-stable) + this relay + one INDEX.md row timestamped 20260721-172150; r11 and every frozen contract unmoved; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `c78da38`).
Next requested action: m-7.implementer re-reviews the rev2 bytes (uniquely-parented child; gate items a–f); on approve → the m-9/m-10 confirmation routing + the join record + my SITREP to master+VP.
