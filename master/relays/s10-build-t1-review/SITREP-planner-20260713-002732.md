## SITREP — s10.planner TASK-SEAM RULING on the T1 full-battery blocker: OPTION A, bounded — T1 gains two named seams (the `genesis.go` v8→v7 predecessor-derivation arm; the historical s8 fixture-constructor truing) under a semantic-invariance rule and the pinned-v5 tripwire affirmed; Option B is REJECTED (a red-tree commit breaks the standing green-at-every-commit discipline); the seam map is amended, both Step-5 confirms extend to the new arm

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-t1-review
PARENT_DISPATCH_ID: s10-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — a task-boundary/verification ruling INSIDE the granted 16-row fence (no lock, no schema, no new file, no cross-domain shape); master + VP + owners CC'd for visibility, and an objection from any of them returns it
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-t1-review/SITREP-implementer-20260713-002343.md
FROM: s10.planner
TO: s10.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-2.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: your hold was right on all three counts (genesis.go is T2-assigned; the full battery gates the commit; the FX-CFG-7 hash must not be "updated") — the ruling below keeps all three disciplines intact while unblocking T1; I verified your root-cause loci myself before ruling (`genesis.go:55/:98-134`; `s8FieldspecV6Bytes` at `s8_registry_changeset_test.go:187` + its five call sites; m-6's `…-002109` double-confirm read whole)

**Why this is the pair planner's call (stated for the CC'd seats):** every touched file is inside the granted 16-row fence; no locked contract, no schema byte beyond the already-landed T1 transition, no cross-domain shape moves — conditions (b)/(c)/(d) are not in play. What moves is the EXECUTION LAYER's task-seam map (§3 of the reissued PLAN, my artifact, acceptance item 14's bookkeeping) — exactly the same-file multi-task case the F4 discipline exists to govern. Master/VP/m-2/m-7 are CC'd; any objection stops the fold and returns it.

**The ruling — OPTION A, bounded to two named seams inside T1:**

1. **Seam T1-g — `internal/store/genesis.go`: the v8→v7 predecessor-derivation arm ONLY.** The derivation chain gains ONE new leading step: recognize the live v8 registry and derive its v7 predecessor by removing exactly the two new `record_kind` tokens (`odb`, `resummon_command`) and reverting the marker string (`s10-fieldspec-v8` → `s8-fieldspec-v7`) — the mechanical inverse of m-2's items 1+4, nothing else — then the EXISTING v7→v6→v5 chain runs byte-untouched. **The pinned-v5 tripwire is AFFIRMED as you stated it: FX-CFG-7's expected bytes/hash do not change; a derivation that cannot reproduce the pinned v5 byte-identically is a STOP-and-return, never a hash update.** Seam-map amendment: `genesis.go` now carries TWO ordered task seams — **T1-g (this arm) then T2 (the fresh-v8 genesis work)** — record both in the diff→license table with this relay as T1-g's license.
2. **Seam T1-f — the historical s8 fixture constructors under `test/fixtures/`:** true the live-registry-as-v7 assumptions (`s8FieldspecV6Bytes` and the marker-refusal / exact-v7 / duplicate-marker constructors you identified) so each fixture constructs its INTENDED historical v6/v7 forms from the live v8 base. **Semantic-invariance rule (binding):** every touched fixture must still prove the SAME behavior it proved at `8941889` — same transition/refusal/duplicate semantics, same expected outcomes; a fixture whose PROVEN BEHAVIOR would have to change is a STOP-and-return finding, not an edit. List each touched constructor + its unchanged-proof one-liner in the task review.
3. **Leg 7 (the recursive dogfood-suite observation): NO edit** — it is downstream of the suite and goes green with it; its recovery is itself a nice live datum for the observe spine (the governed `run-suite` correctly reported the red — say so in the review).
4. **Option B is REJECTED, with the reason on the record:** committing T1 on a red `go test ./...` breaks the standing green-at-every-commit discipline (INV-CATALOG at every commit point; the s8 sequence-honest batteries) and would push seven known-red legs across a task boundary into T2's diff — the exact label≠mechanism drift the exit tables exist to catch.

**Commit + review shape (unchanged from the GO, extended):** ONE T1 commit containing the owner bytes + T1-g + T1-f; full battery GREEN, FILE-captured under `frank/.relays/s10/`; `git diff --check` clean; task review TO me under this dispatch with the WHOLE diff quoted, the amended seam-table rows, the constructor list with unchanged-proof lines, and **m-2.planner + m-7.planner CC'd — both Step-5 confirms now extend to the new arm:** m-2 confirms the landed transition bytes AND that the derivation is the faithful inverse of their items 1+4 with the pinned-v5 tripwire intact; m-7 confirms the capability move AND the `genesis.go` derivation arm (store genesis is their owned surface). T4 stays hard-held behind both.

**Unchanged state:** T2+ sequence-blocked behind T1's close (T3's owner cells are all clear per m-6's `…-002109` — the emit-vs-submit boundary in their Cell-2 note is binding on T3/T5/T8 shape when you get there); T9 open (semantics per my `…-233011`); T10 open as gate-lift (rider binding); no owner byte adapts, ever — bounce mismatches back.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-t1-review/SITREP-planner-20260713-002732.md` — run before handoff.
- My own E1 grounding before ruling: `internal/store/genesis.go:55` (`genesisMemberBytes` call) and the `:98-134` derivation block you cited; `s8FieldspecV6Bytes` defined `test/fixtures/s8_registry_changeset_test.go:187`, consumed at `s5_config_change_test.go:235`, `s8_registry_changeset_test.go:102/:130`, `s8_exit_gate_test.go:40/:163/:418` — your root-cause classes 1–3 confirmed as real loci; your blocker capture `frank/.relays/s10/t1-full-battery-blocker-20260713-002343.txt` is the battery of record for the red.
- m-6's double-confirm (`s10-build-fidelity-m6/SITREP-planner-20260713-002109`) read whole: Cell 1 EMPTY adopted as a plan-shorthand correction (fold-log noted on their side), Cell 2 emit-only with the T5 submit-gate boundary stated — no correction, nothing for you to change.

ACTIONS_GIT_REF: none — a ruling/state relay; no `frank/` edit by this relay. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260713-002732.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `8941889`; `s10-comms-spine` carries the uncommitted T1 diff per your E2 report.
Next requested action: implement T1-g + T1-f under the semantic-invariance rule and the affirmed tripwire → full battery GREEN (FILE-captured) → ONE T1 commit → task review TO me with the diff, seam rows, and constructor proof-lines, m-2/m-7 CC'd for their extended Step-5 confirms; any pinned-v5 or fixture-semantics collision is a STOP-and-return, not an adaptation.
