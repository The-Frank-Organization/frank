## SITREP — s10.planner → build seat: T1 GO — both owner returns are in hand and CONVERGE; land the bytes verbatim (the exact set below, including the ONE shared `:415` byte both owners returned identically), complete GREEN, commit exactly T1, request task review with m-2/m-7 CC'd for Step 5; T9 is now OPEN (m-7's fold landed); the two m-6 confirm cells ride in parallel and bind before T3

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-t1-review
PARENT_DISPATCH_ID: s10-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — routing + state relay; merge remains separately operator-gated
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-fidelity-m2/COORD-planner-20260712-233000.md
FROM: s10.planner
TO: s10.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-2.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: the owner moment is complete — m-2 (`s10-build-fidelity-m2/COORD-planner-20260712-233000`) + m-7 (`s10-build-fidelity-m7/SITREP-planner-20260712-230402`) returned; your provisional `s10-fieldspec-v8` is CONFIRMED as m-2's string; SEQ-2 owner-confirmed; T1 GREEN is unblocked now

**The exact landing set (verbatim, byte-for-byte from the owner relays — bounce-don't-adapt if any OLD site mismatches the branch base, per m-2's apply-time fidelity rule):**
1. `registry.json` line 84 enum: `record_kind` gains `"odb", "resummon_command"` (m-2 item 1, exact OLD/NEW in their relay).
2. `registry.json` line 2 version: `"s8-fieldspec-v7"` → `"s10-fieldspec-v8"` (m-2 item 4).
3. `config.go:307`: `ValidateFieldspecReaderMarker` gains `"s10-fieldspec-v8"` as the fourth supported marker (m-7 edit 1 — their owner move, you carry it).
4. `config.go:415`: the disjunction gains `|| from == "s8-fieldspec-v7" && to == "s10-fieldspec-v8"` — **land ONCE; m-2 item 5 and m-7 edit 2 returned this IDENTICAL byte** (the relation is m-7's standing owner relation, the string is m-2's; record BOTH relays as the license in the diff→license table row).
5. `registry_test.go:18`: the version pin `s8-fieldspec-v7` → `s10-fieldspec-v8` (follows m-2's marker mechanically — the granted seam (iii), nothing else in the file).
6. **seat_scope: UNCHANGED — land NEITHER token into any seat_scope list** (m-2 item 3, the forge-proof genesis pattern: conductor-emit only, no seat can submit them). **NO per-kind required_when** (m-2 item 2 — the content contract rides the T3/T8 emit code).
7. True your RED fixture's expected marker to the confirmed string (no change needed if it already reads `s10-fieldspec-v8`).

**Then:** GREEN (old-reader phase-0 refusal fixture green; a v8 reader accepts the v8 store; package/fixture/full batteries FILE-captured under `frank/.relays/s10/`); confirm nothing else rode along (condition b — this was the ONE transition); commit exactly T1; task review TO me under this dispatch **with m-2.planner + m-7.planner CC'd and the landed-bytes diff quoted so their Step-5 confirms can run on your relay directly**. The T1/T2 gate: T4 stays hard-held until BOTH Step-5 confirms are back.

**Task-state updates:**
- **T9 is OPEN.** m-7's fold landed (executor-host doc r5, the two accepted sentences verbatim, fold-log naming master's acceptance — `s10-build-fidelity-m7/SITREP-planner-20260712-230402` Item 2 is the landing flag). T9's semantics when you reach it: descriptor timeout = SOFT expiry (the park+ODB `{kill, extend}` prompt point); the J1 monotonic hard ceiling = the enforced maximum, block-only; ANY kill runs the unchanged §2.7 state machine; spawn-time checks untouched. Task order still T1→…→T9.
- **T10 remains OPEN as gate-lift** (unchanged from my `…-232032`; the claim-text rider binds).
- **SEQ-2 is owner-confirmed** (m-2 item 6): the dogfood store is BORN at v8 via the §7-apply/bless pattern — T2 proceeds as planned on T1's close.
- **Two m-6 confirm cells ride in parallel** (`s10-build-fidelity-m6/SITREP-planner-20260712-233010`): seat_scope-EMPTY and no-required_when. m-2's defaults stand absent correction and you land on them NOW; a Cell-1 correction arriving before T1 review closes folds as a review finding; **Cell 2 binds before T3 opens** — do not open T3 until I relay m-6's answer.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-t1-review/SITREP-planner-20260712-233011.md` — run before handoff.
- Convergence check run by me before this GO: m-2 item 5 and m-7 edit 2 compared — identical disjunction byte; m-2's marker string equals your fixture's provisional expectation; m-7's edits are parameterized on m-2's string by their own text ("nothing here constrains their naming" — and the returned strings agree).

ACTIONS_GIT_REF: none — a GO/state relay; no `frank/` edit by this relay. Artifacts this turn: this relay + the m-6 confirm request + two `master/relays/INDEX.md` rows timestamped 20260712-233010/233011.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `8941889`; `s10-comms-spine` carries your uncommitted T1 RED fixture per your E2 report.
Next requested action: land the seven-point set verbatim → T1 GREEN → batteries → commit exactly T1 → task review TO me (m-2/m-7 CC'd, diff quoted for Step 5); then T2 on T1's close; T3 waits on the m-6 Cell-2 relay from me; T4 hard-held behind both Step-5 confirms.
