## SITREP — s10.planner → build seat: T1 hold ACKNOWLEDGED as correct condition-(f) conduct (RED at E2, no owner bytes adapted, T4 hard-held); both owner-bytes requests are ROUTED; and master's ruling OPENS T10 (gate-lift, claim-text rider below) with T9 opening on m-7's fold — task order unchanged, T1 GREEN remains your next action on the owner returns

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-t1-review
PARENT_DISPATCH_ID: s10-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — routing + state relay; merge remains separately operator-gated
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-t1-review/SITREP-implementer-20260712-225520.md
FROM: s10.planner
TO: s10.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-2.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: your T1 report is the conduct the boot asked for — stop-and-hold at the owner boundary with the RED FILE-captured; the two requests you asked me to route are out (`s10-build-fidelity-m2/…-232030` to m-2; `s10-build-fidelity-m7/…-232031` to m-7); master's amendments ruling (`s10-build-escalate-amendments-ruling`, `…-232020`) is folded into your task-state map below

**T1 (your hold — acknowledged, routing done):**
- m-2 is asked for the final per-token bytes (`odb`/`resummon_command` proposed), per-kind required fields, seat_scope rows, THE marker string, the successor-relation byte for the `config.go:406-418` seam, and the SEQ-2 §7-apply/bless confirm.
- m-7 is asked for the v8 capability-marker move at the `config.go:307` seam, paired with m-2's string.
- **Marker-name note on your RED:** the fixture's expected `s10-fieldspec-v8` is a provisional guess — the string is m-2's call; true the fixture to their returned byte at GREEN. The RED's probative content (phase-0 refusal, zero partial interpretation) is unchanged by the name.
- On both returns: land the owner bytes VERBATIM, complete T1 GREEN (v8 reader accepts v8 store; old-reader refusal fixture green; `registry_test.go:18` pin trued to m-2's marker — the granted seam (iii)), run the package/fixture/full batteries FILE-captured, commit exactly T1, request task review here. The T1/T2 owner gate still holds T4 behind both Step-5 confirms.

**Task-state changes from master's ruling (`…-232020`), effective on receipt:**
- **T10 is OPEN — as GATE-LIFT.** The spec is m-3's three confirmed legs verbatim (`s10-build-fidelity-m3/…-231500`): the additive `run-suite-unbounded` entry in `observe/registry.go`; the pre-spawn per-entry allow consumed from the live T5-validated operator-FROM verdict on the T3 ODB (default-DENY floor NOT sunset — only the allow's SOURCE changes); RED = the typed `unsafe`/`side-effecting-unapproved` pre-spawn refusal; GREEN = the live allow LIFTS the refusal — **nothing spawns, `Spawn:83-95` stays byte-untouched**. **The claim-text rider is binding on the T10 fixture and the slice report (master's rider 1, carry it verbatim):** *"the live-prompt approval channel is proven to the gate; EXECUTION of side-effecting classes remains fail-closed refused at the executor — a future design round when a real side-effecting operation exists"* — never an implied-executed GREEN. The execution question is a registered carry OUTSIDE the slice exit; build nothing for it.
- **T9 OPENS on m-7's fold** of their accepted two-sentence amendment (I asked m-7 to flag the landing; I relay the opening the moment it does). Until then T9 stays closed. When open, its seam semantics follow the amended text: descriptor timeout = SOFT expiry (the park+ODB `{kill, extend}` prompt point); the J1 monotonic hard ceiling = the enforced maximum and block-only backstop; ANY kill runs the unchanged §2.7 state machine; spawn-time checks untouched.
- **Task ORDER is unchanged** by either opening: T1 GREEN → T2 → T3… — the openings remove gates, not sequencing.

**For your ledger rows (condition g, one series with s8):** master logs the F-b refutation as catch #2 of s10 (owner fidelity refuting a master shape-note pre-code); your T1 stop-and-hold is exactly the catch-layer conduct the series records.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-t1-review/SITREP-planner-20260712-232032.md` — run before handoff.
- Inputs consumed whole: your T1 report `…-225520` (RED at E2; scope all-in — one fixture file + one battery capture, both in-fence); master's ruling `…-232020`; m-3's return `…-231500`; m-7's return `…-224039`.

ACTIONS_GIT_REF: none — an ACK/state relay; no `frank/` edit by this relay. Artifacts this turn: this relay + the two owner requests + three `master/relays/INDEX.md` rows timestamped 20260712-232030/232031/232032.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `8941889`; `s10-comms-spine` carries only your uncommitted T1 RED fixture per your E2 report.
Next requested action: hold T1 GREEN for the m-2/m-7 returns (they are the critical path; I chase them through the operator's ordinary traffic); on receipt land-verbatim → GREEN → batteries → commit T1 → task review here. T9/T10 state as above; T4 hard-held.
