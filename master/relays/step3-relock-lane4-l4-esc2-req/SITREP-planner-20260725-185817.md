## SITREP — ESCALATION 2 (LOW SEVERITY, PRE-MATERIALIZATION ADVISORY): master's esc1 correction sent me back through my own filed bytes to see whether the inference had propagated. It largely had not — `context_digest` appears ZERO times across all twelve artifacts and `marker_digest` is correctly attributed — but **one clause in `xit-dur-1/input.json` presupposes that `log_prefix_digest` is derivable**, which is exactly the open Q1/Q2 question. It breaches no hold (no definition, no recipe, no value), and the capture requirement it trails is correct under any resolution. But master is about to FREEZE these bytes, so the call belongs to master before materialization rather than to me after it. **Recommendation: materialize as-is (option A).** Not a blocker; I am not asking for a pause.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-lane4-l4-esc2-req
PARENT_DISPATCH_ID: step3-relock-lane4-l4-esc1-disp
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the operator hand-relays; master rules A or B before materializing `xit-dur-1`. The other eleven artifacts are unaffected either way.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
IN_REPLY_TO: master/relays/step3-relock-lane4-l4-esc1-disp/DESIGN-orchestrator-planner-20260725-184324.md
FROM: l4.planner
TO: master.orchestrator-planner
CC: l4.implementer, operator, master.orchestrator-reviewer, m-9.planner, m-3.planner
SUBJECT: esc2 — one `xit-dur-1` clause presupposes `log_prefix_digest` derivability; rule A (materialize as-is) or B (hold xit-dur-1 for a corrected generation) before freezing

## The correction accepted first, because it is what prompted this check

Master is right on both points, and I take them without qualification. §1.5 defines **`marker_digest`**; §7 never states that `log_prefix_digest` is that same value. I wrote the identity as though it were stated when it was my inference — the same class of error my own escalation existed to refuse. And the two members are not one problem: one has a plausible unconfirmed referent, the other has nothing at all.

The reason this matters beyond the record is that **the artifacts, not the relay prose, are what get frozen**. So I went back through the filed bytes rather than assuming the error had stayed in the escalation.

## What the sweep found

Searched all twelve filed artifacts for both held members and for the inferred identity:

| check | result |
|---|---|
| `context_digest` occurrences across all twelve artifacts | **0** — the member with no referent was never presupposed anywhere |
| `marker_digest` occurrences | **1**, in `xit-dur-1`, attributed to `round_marker` — correct, and NOT equated to `log_prefix_digest` |
| `log_prefix_digest` occurrences | **1**, in `xit-dur-1` — the clause below |
| the inferred `marker_digest` = `log_prefix_digest` identity, asserted anywhere in artifact bytes | **absent** — it appeared only in the esc1 relay prose master already corrected |

**The one clause**, in `xit-dur-1/input.json` under `capture_requirements`:

> "the recovered valid prefix after recovery, so `log_prefix_digest` is derivable from frozen bytes"

## Why this is not a hold breach, stated precisely so master can check it rather than take my word

The hold is: *do not author the two members in any form — no literal, no derivation, no slot, no placeholder.* This clause does none of those. It states **no value**, **no recipe**, and **no slot**; it names the member and asserts a property of it. The artifact defines nothing about `log_prefix_digest` and the fixture's expectation vector for it lives in the manifest, which is held and unwritten.

What the clause *does* carry is an unproven **presupposition of derivability** — the precise thing Q2 is open on. If m-9 confirms Q1, the clause becomes true as written. If m-9 answers that no recipe exists and one must be authored, the clause is premature in bytes that are by then frozen and equality-confirmed.

The capture requirement it trails — *capture the recovered valid prefix after recovery* — is **correct and necessary under every resolution**, because no expectation about the resumed prefix is checkable without it. Only the trailing rationale is at risk.

## The two options, and my recommendation

**A — materialize as-is (RECOMMENDED).** The clause is a rationale sentence inside a capture requirement, not a definition, not a value, and not a gate input. No leg's pass/fail turns on it. m-9's answer will either confirm it or retire it, and retiring one rationale clause is a natural part of whatever generation folds m-9's answer into the manifest anyway. Cost: zero.

**B — hold `xit-dur-1` only** for a corrected proposal generation trimming the clause to the bare capture requirement; the other eleven materialize now. Cost: one additional propose/materialize/equality cycle of hand-relay, to delete a sentence that changes no outcome.

**I recommend A.** B spends a full generation of the operator's hand-relay budget on a rationale clause with no gate consequence, which is a poor trade against the standing cost rule that chose file relays at B22 in the first place. I raise it only because master is about to freeze the bytes, and a defect I saw and did not name would surface at `l4.implementer`'s content review as exactly that.

**If master rules A**, I ask that this relay be treated as the record that the clause is **known-provisional**, so its later removal is a folded correction rather than a fresh finding against me.

## Standard SITREP block

Phase: DESIGN authoring, lane 4 — HELD on the two esc1 members, awaiting `…-l4-esc1-resume`
Current artifact: `master/relays/step3-relock-lane4-l4-propose-1/DESIGN-planner-20260725-183149.md` (twelve envelopes, filed, awaiting materialization)
Claims:
- `context_digest` appears 0 times across all twelve filed artifacts — evidence E1 — source: exhaustive search of the twelve authored artifact bodies at this relay's stamp
- exactly one clause, in `xit-dur-1`, presupposes `log_prefix_digest` derivability — evidence E1 — source: the same search; the clause is quoted verbatim above
- the corrected `marker_digest` identity error did NOT reach artifact bytes; it existed only in esc1 relay prose — evidence E1 — source: the same search
- the clause breaches no part of the esc1 hold — evidence E1 — source: the hold's own terms (no literal, derivation, slot or placeholder) compared against the clause text
Actions:
- read-only search of previously filed artifact content; no artifact byte authored, altered or re-proposed — ACTIONS_GIT_REF: no edits to any artifact or governed file; this relay file plus one appended INDEX.md row are the only disk actions
FINAL_GIT_STATUS_SHORT: unavailable — cwd `/Users/jack/Programming/harness` is not a git repository (the governance docs workspace); no `frank/` action taken.
Done: the post-correction sweep of all twelve filed artifacts for both held members and the inferred identity
Not done: `STEP-3-EXIT-FIXTURES.json` (proposal 2) — correctly held on the two esc1 members
Blocked: nothing by this relay; I am not requesting a pause on materialization
Scope drift risk: none — this proposes no new artifact, no change to §7, and no change to any of the other eleven artifacts
Tests / verification: exact-file `relay-lint` clean on this relay; the twelve envelopes were previously verified to round-trip byte-exactly from the proposal relay itself
Next requested action: master rules **A** (materialize as-is, this relay standing as the known-provisional record) or **B** (hold `xit-dur-1` for a corrected generation), then proceeds with `…-l4-materialize-1`. I remain held on the two esc1 members and rejoin only via `step3-relock-lane4-l4-esc1-resume`.

## Boundaries

This relay authors nothing, proposes no artifact, re-proposes nothing, materializes nothing, freezes nothing, moves no locked byte, requests no owner-fidelity, issues no PLAN or T4 token, touches no `frank/` path, claims no E3, and permits no external use. It does not author, define, or supply either held member in any form, and it does not resume the held flow. Kickoff `4f3928ec…`, plan rev13 `60daac08…`, interface lock `cbd1893c…` UNMOVED. **H-12 hard-blocks external use.**
