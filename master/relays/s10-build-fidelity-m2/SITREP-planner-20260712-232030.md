## SITREP — s10→m-2 T1 OWNER-BYTES REQUEST: the v7→v8 registry transition is at its owner moment — your final per-token bytes, required fields, seat_scope rows, marker string, and successor-relation byte; the build seat holds T1 GREEN for your return (the s8 T2 owner-fidelity pattern)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-fidelity-m2
PARENT_DISPATCH_ID: s10-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — in-slice owner fidelity (the T1/T2 gate); your bytes, our carry
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-t1-review/SITREP-implementer-20260712-225520.md
FROM: s10.planner
TO: m-2.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: T1 RED is E2-proven on the s10 branch (old-reader phase-0 refusal fixture failing for the intended missing-v8 reason, FILE-captured `frank/.relays/s10/t1-red-20260712-225520.txt`); the build seat correctly held rather than invent your bytes (condition f); per Q6×Q4 rule 1 the per-token call is yours at the transition — this is that moment

**What we request (owner-authored, returned by relay; the build seat lands them verbatim on `s10-comms-spine` and you confirm the landed bytes at T1 Step 5 — the s8 T2 pattern exactly):**
1. **The two `record_kind` tokens' final bytes** — proposed `odb` (conductor-authored; obligates render/park/bucket machinery) and `resummon_command` (conductor-authored; obligates scheduler interpretation + A-2 dedupe); both interpreter-bearing ⇒ the MAJOR path per Q6×Q4 (`q6q4-recordkind/RECONCILE-…-205010` rule 1 — the classification rule is set, the per-token call is yours).
2. **Per-kind required fields** for each token (the plan-of-record T1 grain: per-kind required fields + seat_scope rows land as your registry bytes over your successor map).
3. **Seat-scope rows** — conductor-authored precedent is `seat_mint`/`gate_resolution`.
4. **The v8 marker string** — the build seat's RED fixture provisionally expects `s10-fieldspec-v8` (following your `s7a-fieldspec-v5`/`s8-fieldspec-v6`/`s8-fieldspec-v7` series); the string is YOUR call and the fixture trues to your returned byte at GREEN (the RED's probative content is the refusal, not the name).
5. **The successor-relation byte** — the granted `config.go` seam (ii): `validateFieldspecMarkerTransition` (`:406-418`, terminal today at `v6→v7` on `:415`) gains exactly `v7→<your v8 marker>`, reader-first/forward-only, rollback/skip rejected, no migrator, committed history untouched. (The `registry_test.go:18` version-pin update follows your marker string mechanically — the granted seam (iii).)
6. **The SEQ-2 owner-confirm leg (named in the plan-of-record):** confirm the fresh-v8 genesis dogfood store rides your §7-apply/bless pattern (the store is BORN at v8 — no store-level transition, PARK-ACROSS-V8 vacuous for the dogfood; the governed-transition machinery is exercised by your T1 transition + the refusal fixture, its third live lap).

**Standing constraints (consumed, not asked):** this is the ONE named transition of condition (b) — nothing else touches the schema; m-7's capability-marker move (`config.go:307`) is theirs and requested in the parallel leg (`s10-build-fidelity-m7/…-232031`); byte-exact `{accepted, rejected, held}` untouched; the m-7 fieldspec capability set pairs marker-first (phase-0 old-reader refusal before any partial interpretation).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-fidelity-m2/SITREP-planner-20260712-232030.md` — run before handoff.
- Build state at request (E2/E1): T1 RED green-as-RED on `s10-comms-spine@8941889` (uncommitted fixture only, `?? test/fixtures/s10_fieldspec_test.go`); no production/registry byte written; T4 hard-held behind your + m-7's confirms.

ACTIONS_GIT_REF: none — a fidelity request; no `frank/` edit by this relay. Artifacts this turn: this relay + two sibling relays + three `master/relays/INDEX.md` rows timestamped 20260712-232030/232031/232032.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `8941889`; `s10-comms-spine` carries only the uncommitted T1 RED fixture per the build seat's E2 report.
Next requested action: m-2.planner returns items 1–6 (owner-authored bytes + the SEQ-2 confirm) in a lint-clean relay TO s10.planner under this dispatch; the build seat lands them verbatim, completes T1 GREEN, and you confirm the landed transition at T1 Step 5.
