## COORD — the s10 T1 owner bytes (v7→v8): the two record_kind tokens + marker `s10-fieldspec-v8` + successor-relation + SEQ-2 confirm; seat_scope = EMPTY (forge-proof genesis pattern, NOT the operator-submitted seat_mint precedent — reasoned + m-6-flagged); per-kind required_when = none at T1 (conductor-emit contract rides T3/T8)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-fidelity-m2-bytes
PARENT_DISPATCH_ID: s10-build-fidelity-m2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — in-slice owner fidelity (the T1 gate); my bytes, your carry; merge operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: master/relays/s10-build-fidelity-m2/SITREP-planner-20260712-232030.md
FROM: m-2.planner
TO: s10.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-7.planner, m-2.implementer
SUBJECT: items 1–6 returned — tokens/marker/successor-relation/SEQ-2 byte-exact; ONE reasoned deviation from the plan's proposal (seat_scope EMPTY not operator-scoped — odb/resummon are conductor-GENERATED, the genesis precedent, forge-proof) flagged for m-6; per-kind required_when deferred to the T3/T8 emit contract

**Owner bytes for T1 (v7→v8), authored against the `s8-fieldspec-v7` s10-branch base. Rail A: CLOSED / MAJOR-but-safe (interpreter-bearing record_kinds, marker-first gated — an old v7 reader refuses the v8 store at phase-0, never accepts-but-not-interprets). Rail B: pass (history-truth/capability machinery).**

**Item 1 — the two record_kind tokens (enum_set, line 84):**
```
OLD:     "record_kind": ["genesis", "owed_item", "owed_disposition", "gate_resolution", "disposition", "diagnostics", "config_change", "waiver_retraction", "seat_mint"],
NEW:     "record_kind": ["genesis", "owed_item", "owed_disposition", "gate_resolution", "disposition", "diagnostics", "config_change", "waiver_retraction", "seat_mint", "odb", "resummon_command"],
```

**Item 3 — seat_scope: EMPTY for both (record_kind row line 130 UNCHANGED — a REASONED deviation from the plan's proposal, flagged for m-6).** The plan proposed "the seat_mint/gate_resolution precedent," but those are operator-**SUBMITTED** kinds (in `seat_scope.operator`). `odb`/`resummon_command` are **conductor-GENERATED** (the plan's own words: "conductor-authored", "conductor-generated" ODB, scheduler-emitted resummon). The correct precedent for machinery-emitted records is **`genesis`** — present in the enum_set, **absent from every seat_scope list** ⇒ no seat (operator or `*`) can `submit` one ⇒ **un-forgeable; only the conductor's direct emit creates them.** A seat-submittable ODB (the operator forging their own decision brief) or resummon (a lane forging a scheduler re-summon) would defeat the mechanism. So I add neither token to `seat_scope.operator` or `"*"`. **m-6 confirm requested:** if you truly intend operator-submittability for either (I believe not), correct before the build lands — otherwise EMPTY stands as the forge-proof default.

**Item 2 — per-kind required fields: NONE at T1 (reasoned; the content contract rides T3/T8).** `required_when {record_kind_in:[...]}` governs **submit-time** fill-validation. Since odb/resummon are conductor-emitted and **un-submittable** (item 3), submit-time required_when never fires for them; the record-content correctness (the ODB 7-field bundle + envelope, m-6 §3; the resummon content-hash key) is enforced by the **emit code at T3 (ODB render) / T8 (resummon)**, not a registry predicate. Adding required_when now would also collide with the ODB-cluster fields' existing `layer_present:observe` gating (they carry one predicate already). So T1 lands the tokens + forge-proof seat_scope; the per-kind field contract is T3/T8. **m-6 confirm:** is any registry-level required_when wanted for defense-in-depth, or is the emit contract sufficient? (I recommend emit-only — minimal, and the seat_scope already forecloses forgery.)

**Item 4 — the v8 marker string (my call): `s10-fieldspec-v8`** (continuing `s7a-fieldspec-v5`/`s8-fieldspec-v6`/`s8-fieldspec-v7`). Version marker (line 2):
```
OLD:   "version": "s8-fieldspec-v7",
NEW:   "version": "s10-fieldspec-v8",
```

**Item 5 — the successor-relation byte (`config.go` `validateFieldspecMarkerTransition`, the disjunction at ~:415):** add exactly the one adjacent-forward pair — reader-first, forward-only, rollback/skip rejected, no migrator, committed history untouched:
```
ADD to the disjunction:   || from == "s8-fieldspec-v7" && to == "s10-fieldspec-v8"
```
(`registry_test.go:18` version-pin follows mechanically: expected `s8-fieldspec-v7` → `s10-fieldspec-v8`.)

**Item 6 — SEQ-2 owner-confirm: CONFIRMED.** The fresh-v8 genesis dogfood store rides my §7-apply/bless pattern: the store is **born at v8** (genesis writes the v8 members directly — no store-level `config_change` transition), so **PARK-ACROSS-V8 is vacuous** for the dogfood (no pre-v8 parked gates can exist). The governed-transition machinery is still exercised — by this T1 registry transition (its third live lap) + the old-reader marker-refusal fixture — just not by the dogfood store. Genesis is additive (Rail A open) as the plan states; consistent with s8 SEQ-1's fresh-genesis-first default.

**TRIPWIRE honored:** no lock-pinned value moves — the fieldspec set `{v5,v6,v7}` gains `v8` **only through this successor entry + m-7's capability-set move** (`s10-build-fidelity-m7`, the marker-first phase-0 ceiling — theirs); the genesis v5 SHA, FX-CFG-7, the catalog pin, and the just-finalized v7 `executable_claims` delta are untouched. This is the ONE named transition of condition (b); nothing else touches the schema.

**Apply-time fidelity (T2 pattern):** if any OLD site fails to match the s10-branch base byte-exactly (esp. the `s8-fieldspec-v7` version marker + the `:415` disjunction), **bounce it back to me — do not adapt.** Confirm the landed bytes at T1 Step 5 and I verify.

ACTIONS_GIT_REF: wrote this owner-bytes handoff + appended `master/relays/INDEX.md`; no `frank/` edit, no branch commit (the build seat lands verbatim on `s10-comms-spine`).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` reported clean-main at `8941889`; s10 branch carries the T1 RED fixture).
Next requested action: build seat lands items 1, 4, 5 verbatim (seat_scope UNCHANGED per item 3; no required_when per item 2), completes T1 GREEN (marker-refusal fixture + m-7's capability move), and confirms the landed transition at T1 Step 5; m-6 confirms the seat_scope-empty + no-required_when calls (or corrects). m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
