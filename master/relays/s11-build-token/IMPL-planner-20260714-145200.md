## IMPL DISPATCH — s11 comms-thicken build authorized: delegated same-owner token on the r3-approved pair PLAN; all-in 15-row fence at named-seam grain; T6 OPEN and builds to the integrated §B contract; T5/T10 dispatched-but-CLOSED on g2/dc; T8 = eight items under the ruled seams; T1-first straight-through under B11

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s11-build-token
PARENT_DISPATCH_ID: s11-build-plan-review-r3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e); no merge authority is granted here
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s11-comms-thicken-plan-s11.3
IN_REPLY_TO: master/relays/s11-build-plan/PLAN-REVIEW-implementer-20260714-145028.md
FROM: s11.planner
TO: s11.implementer
BUNDLE_ID: m-6-human-surface-scheduler
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner, m-5.planner
SUBJECT: delegated implementation dispatch for the s11 comms thicken — parented to your approving PLAN-REVIEW r3 (`s11-build-plan-review-r3`, approve, all-in), issued under the dispatch's conditions (a)–(g) and the m-6 review's five token conditions verbatim; the fence is the 15-row block below at the named-seam grain; T5/T10 carry their gate holds through dispatch; build T1-first now

All delegated-dispatch preconditions are met: conditional dispatch authority runs through the orchestrator delegation (`s11-dispatch/PLAN-orchestrator-planner-20260714-023000`, `DELEGATED_DISPATCH_AUTHORITY: yes`, conditions (a)–(g)) → the m-6 approving review's five token conditions (`s11-plan-m6/PLAN-REVIEW-implementer-20260714-024712` — the token issues inside THIS fresh pair's own loop, never from the guide) → my boot (`master-boot-s11-planner`); your PLAN-REVIEW returned **approve** (`s11-build-plan/PLAN-REVIEW-implementer-20260714-145028`) on my directly addressed r3 PLAN; the mechanical SCOPE_DIFF below is **all-in** with no hard trigger against the dispatched scope; and the PARENT chain points to that approving review (`s11-build-plan-review-r3`), whose parent is my pair-Planner PLAN (`s11-build-plan/PLAN-planner-20260714-144500`) addressed to you. I therefore issue the delegated token.

DISPATCH IMPL

**The fence (token condition 3 — the exact 15-row all-in block; file/dir-wide rows do NOT erase the named-seam licenses):**

SCOPE_DIFF:
- frank/internal/engine/ -> in
- frank/internal/obligation/ -> in
- frank/internal/store/projections.go -> in
- frank/internal/store/genesis.go -> in
- frank/internal/bounce/ -> in
- frank/internal/tables/ -> in
- frank/internal/migrate/ -> in
- frank/internal/crashpoint/ -> in
- frank/internal/recover/ -> in
- frank/internal/config/config.go -> in
- frank/internal/fieldspec/registry_test.go -> in
- frank/internal/executor/executor.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/ -> in
- frank/.relays/s11/ -> in
SCOPE_DIFF_RESULT: all-in
HARD_TRIGGER_SCAN: none against the dispatched T1–T4/T6–T9/T11-prep scope — the trust-critical surfaces are each master-ruled and owner-gated at a named seam (the g1 8a contract `s11-8a-joint-review/RECONCILE-…-035210` §B; the executor split ruling `s11-build-escalate-fence/RECONCILE-…-143010`); no registry/schema change is licensed (`registry.json` NOT in fence — condition b); no locked-contract edit is licensed to you; T5/T10 stay gate-held; the store write path is m-1-locked and out of fence; merge operator-only.

**Named-seam licenses (binding — a diff outside a task's named seam is a deviation relay, not a fold):**
- `engine/` — the m-6 comms surface per the task grain (FSM, ODB, resummon, prompter, wake, submit-side verdict classification). Same-file two-task edits are each their own named unit in task order, recorded in the diff→license table.
- `obligation/` — ODB/envelope/`gate_category` consumption (T1–T3) + the item-6 builder site `obligation.go:190-226` (T8).
- `store/projections.go` — ONLY the B/C/D saved-query read-side projections (T1–T3). `store/genesis.go` — ONLY T8 item 9. **The store write path (`store.go`, `lock.go`, `quarantine.go`) is m-1-locked, NOT in fence — any perceived need STOPS and returns through me.**
- `bounce/` — the bucket-D author-return formatting + the two reason tokens' rendering (T3/T4/T6).
- `tables/` — T8 items 4–5 only.
- `migrate/` — T6 ONLY (the §9 read-time migrator extension + the `Apply` deep-clone obligation; m-2 fidelity by relay).
- `crashpoint/` + `recover/` — T6 ONLY (the crash-replay boundary + the durable re-issue-intent recovery replaying to the SAME replacement identity; m-7 fidelity by relay; recovery semantics beyond the re-issue intent byte-untouched).
- `config.go` — ONLY T9's G4 cadence loci (the two timers onto the §J operator-config surface; **no config value may ever mean auto-approve** — only channel escalation or the optional per-gate conservative block-ceiling).
- `registry_test.go` — assertion adjacency ONLY if a fixture forces it; **`registry.json` is NOT in this fence** (g1 §C: no registration owed; any perceived registry/schema need = condition-(b) master escalation BEFORE work).
- `executor.go` — **TWO distinct seams, never merged** (RULING A): **(T8, open now, g2-independent)** `finalizeRun` (`:295-323`) + the `if preserve { cleanup = false }` call-sites (`:190-292`), the behavior-preserving move of preserve/cleanup ownership to ONE locus and nothing else; **(T5, only after g2 completes to master)** the fork spawn/feed/retire under the verbatim g2 ceiling. **Byte-untouched under both:** `Spawn` + its double refusal · every fault token (`executor-start-fault`, `executor-survivor`, `executor-timeout`) · the `exitGreen`/`expectGreen` verdict logic · `RungReached`/`Timing` values. T8's seam lands first; any other executor diff STOPS.
- `main.go` — composition-root wiring only (G4 config handoff; bucket/digest surface registration; the item-5 snapshot injection if required).
- `test/fixtures/` — the T7 matrix + the ③ known-A NF fixture + the T6 §B fixtures + the s8/s9/s10 regression suites kept green.
- `.relays/s11/` — evidence captures only, not governance relays.
- **`internal/observe/` is NOT in this fence** (master-ruled, stands): no m-3 seam, no s11 edit, ever — the deferred arbiter is a post-Step-2 m-7+m-3 carry.

**Task state at dispatch (the live gate state):**
- **OPEN: T1 → T2 → T3 → T4 → T6 → T7 → T8 → T9 → T11 prep**, straight-through in order, one commit boundary per task (T8: one commit per item), each task's diff reconciling to its named seam in the diff→license table.
- **T6 builds to the integrated §B contract byte-exactly** (`s11-8a-joint-review/RECONCILE-…-035210` — g1 CLOSED): the three byte-distinct records (stale reply candidate `rejected`/D + `failing_edge: stale_choice_set`, NO `resumed` wake, old decision NOT resolved · migration fault `held`/A + `stale_schema`, operator-visible, non-suppressible · replacement gate/ODB under a NEW decision identity, resummon keys `(same seat, NEW decision_id, restarted cadence_slot series)`); one serialized outcome OR a durable re-issue intent whose recovery replays to the SAME replacement identity — crash-replay fixture at the stale-detect/re-issue boundary; `π = {value → label}` with value AND label decision-bearing, order not; relabel trips stale; reorder + added-representational-column GREEN; migrate-then-validate at wake; un-migratable → `held`+escalated+`stale_schema`, never dropped, never auto-resolved; π snapshotted BEFORE any migrator; `migrate.Apply` deep-clones; the guard on the live `classifyVerdict` path against a guarded migrated view, never the live registry; either-side failure fails closed. **The v1 honesty rail: zero migrators and no guard fire exist at `d91fcfb` — these are build obligations; no fixture may pretend past what it proves.** Where any doc byte contradicts §B, §B governs.
- **T8 = EIGHT items** (RULING B rescope on the record), card order (1, 3, 4, 5, 6, 7, 8, 9), battery green between items, both mechanical tables reconciling after each: item 6 = the five operator-addressed sites (`approval.go:238-254` · `expiry.go:232-248` · `resummon.go:261-277` · `odb.go:73-109` · `obligation.go:190-226`; `loop.go:387/:411` are a DIFFERENT mechanism — touching them under item 6 is out-of-seam), with item 3 first and a fresh five-site census recorded post-item-3; item 5 = ONLY the per-emit build (`Emit` → `outcomeForContentHash` → `resummon.go:229-234`) + the scheduler snapshot-supply seam (constructor/state + `main.go` injection if required), dedupe outcome byte-identical — **every other `tables.Build` site is evidence-only; if the named seam cannot supply the snapshot behavior-preservingly, STOP and report the causal insufficiency to me for an amended reviewed plan before touching another build site** (your r3 approval boundary, binding). The exit package says exactly: "T8: eight of nine — item 2 rescoped by master (`s11-build-escalate-fence/RECONCILE-…-143010`), carried post-Step-2 to m-7+m-3."
- **T5 CLOSED** until the g2 OQ-2 ceiling completes to master (m-5.implementer review still out); **T10 CLOSED** until the m-3+m-6 design-cell ritual returns. If a gate lands mid-slice, the task slots in at the next commit boundary and I relay the opening; otherwise the task exits **report-and-hold with its acceptance OPEN** (FINDING-4) — never a `t.Skip` stub, never proceed-on-assumption, never claimed complete.
- **T4's `egress_blocked` stays fixture-scoped:** the state + local-park-resummon semantics land; the away-mirror send is UNBUILT (step-(d)); never auto-redact/send.
- Any owner mismatch, locked-contract change, cross-domain change, registry/schema need, out-of-fence diff, or un-fenced locus named by an owner return ⇒ STOP, report, hold — return through me (the fence-union rule: an owner return naming un-fenced loci is an escalation trigger, not an in-fence assertion).

**Conditions (a)–(g), verbatim binding:** (a) unique sub-DISPATCH_IDs (the `s11-build-*` chain; batteries FILE-captured under `frank/.relays/s11/`); (b) registry/schema changes beyond the named returns → master BEFORE work; (c) locked-contract changes → owning pair + master, never silent; (d) cross-domain → master; (e) merge operator-only via `HUMAN_MERGE_AUTHORIZATION` at grant time; (f) blocked lanes report and hold; (g) the dogfood evaluation extends the s8+s9+s10 ledger as ONE series — the s11 catch ledger already carries **#1** (the r1 executor-license contradiction, pre-code) and **#2** (the r2 locus-table truth errors, pre-code).

**Standing disciplines (day-one):** TDD with RED-first negatives per task; sequence-honest FILE-captured RED/GREEN batteries; byte-exact `{accepted, rejected, held}`; R2 (the model field stays payload/render-only, never a gate input); Rail-A/Rail-B per task as the plan-of-record fixes them; I-PH over every new seat-visible surface (buckets, FSM, fork, ODB); the ten INV-CATALOG laws green at every commit point; both mechanical tables (consumption→supply · diff→license) maintained as you go; label→mechanism sweep + verify-the-summary-line ride my end-of-slice review; named seams for same-file multi-task edits. Branch **`s11-comms-thicken` off `main@d91fcfb`** (`s9-close`); execution under `superpowers:executing-plans` task-by-task; **B11 — NO per-task review relays; blockers TO me as they arise; ONE end-of-slice adversarial review by me at completion.**

**Not granted (token condition boundaries):** merge (condition e); lock/contract changes; T5 work before g2 completes to master; T10 work before the dc return; any `observe/` or store-write-path or `registry.json` edit; any `tables.Build` site beyond the named item-5 seam; any file/seam outside the block above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s11-build-token/IMPL-planner-20260714-145200.md` — run before handoff (result in the inline pointer).
- Lineage: PARENT `s11-build-plan-review-r3` → your PLAN-REVIEW **approve** (`FROM: s11.implementer`, all-in mechanical check, no hard trigger) → its parent `s11-build-plan` → my pair-Planner PLAN r3 (`PLAN-planner-20260714-144500`, FROM s11.planner, TO s11.implementer). The m-6 review's five token conditions are each satisfied: (1) parented to this pair's own approved `s11-build-*` review, consuming `s11-comms-thicken-plan` (sha `6af929c3…d02d3d`) as plan-of-record; (2) g1 closed → T6 open to §B, g2/dc preserved as hard locks on T5/T10; (3) the mechanical scope/fence reconciliation above + the fence-union/mid-amendment rule; (4) merge operator-only; (5) the STOP/route rules restated.

ACTIONS_GIT_REF: none — a dispatch/authorization relay; no `frank/` edit, no code, no commit by this file. The build begins in the s11.implementer session on `s11-comms-thicken` off `main@d91fcfb`. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260714-145200.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `d91fcfb` (`s9-close`).
Next requested action: operator carries this to s11.implementer; the build runs T1→T4 → T6 → T7→T9 straight-through with evidence under `frank/.relays/s11/`; I stand by for blocker relays, relay the T5/T10 openings if g2/dc land mid-slice, run the end-of-slice adversarial review, and author the merge-decision relay TO the operator at slice exit. No merge without HUMAN_MERGE_AUTHORIZATION.
