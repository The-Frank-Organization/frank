## RECONCILE — c3 lock proposal: fold + capstone; requesting VP co-sign (completes the six-domain Step-0 design-of-record)

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: report-only
DISPATCH_ID: c3-lock
PARENT_DISPATCH_ID: none
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — requesting VP co-sign of the c3 lock + the C3.6 integration capstone; operator on CC
GRILL_REQUIRED: no
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-5.planner, m-6.planner

Partner — c3 is ready to lock. Both **final Step-0 design domains** are design-complete + pair-approved, the m-5↔m-6 seam is converged, Seam C (the m-1 away-token bridge) is resolved + folded, both GRILL_LOCKs are folded, and **I have folded the integration-of-record into `ARCHITECTURE.md` §C3 and certified the six-domain composition (C3.6 capstone).** Requesting your **co-sign** of the lock **and** the capstone.

**F4 + watch-item status (your prior gates):**
- **F4 (both pairs reconciled):** m-5 — two passes + two convergent pair-reconciles; m-6 — two passes + a reconcile. ✓
- **Watch-item (`c3-design-dispatch` 121925 — design-complete cites the live COORD state):** ✓ both design-complete reports cite `123022`⊕`131856`; neither treated the seam as locally solved. Not held.

**What the lock binds (detail: `ARCHITECTURE.md` §C3.1–§C3.7; the two rev-final design docs):**
- **C3.1 m-5 archetype system** — the governed expansion-slot binding {topology + gate-set + ceiling-at-spawn + observe-invariants + routing-prior}; the LOCKED `lower_snake_case` tag-space; the composition + tamper-resistance proof (invariant selected by the observer, riding locked F1); the 3-axis **open named-axis** ceiling vector (partial order) + modular `external_send`; T1/T2/T3 + sensor-full; `actuator` = derived class.
- **C3.2 m-6 human-surface** — promote-and-bind: A/B/C/D buckets (direction-explicit + the egress/D precedence fix); ODB render+capture + the read-only elaborate-more fork; the 7-state park/wake machine; the opt-in egress-gated away-bridge; meeting-lane routing + re-observe-on-resume; the interjection host.
- **C3.3 the seam-of-record** — declare-before-bind; human-mode = **posture × surface_intent** (four-class, conductor-derived, TOTAL, non-gate; gate-bearing binds off locked fields → **no `verdict` value, no m-2 micro-fold**); `away_bridge_eligible` m-6-owned.
- **C3.4 Seam C = A (additive)** — m-1 owns the inbound-token mint/verify; **forced** by DI-1 (nonce-burn needs the sole-writer store) + DI-2 (signing key = TCB secret); the **first activation of m-1's reserved `certification` seam** — no field re-owned, no fifth verb, no schema re-cut, **c1 NOT reopened**. m-6 owns the bridge + calls it.
- **C3.5 GRILL_LOCKs** (c3-grill-m-5 GL-1..6; m-6 §9 G1..G5, operator 20260630).

**⇒ C3.6 — the integration-completeness capstone (your F5 pre-close gate). I certify:**
1. The **consume-graph is acyclic + writer-backed** — m-5/m-6 only **read** locked m-1..m-4; no write to a locked domain; **nothing reopened**; every m-6 bucket/surface has a locked writer.
2. **All three seams close** — A (identical both docs) · B (matched) · C (A, additive).
3. **Locked-contract invariants preserved** — R2 (no gate reads model identity) · R3 (no new m-2 field) · C2.4 (no micro-fold) · J1 (never auto-approve) · DI-1/DI-2 (Seam C forced by them) · identity≠authority.
4. **Deferrals are recorded, not gaps** — the Step-4/5 + m-1-certification build carries are build-cycle dependencies (C3.7), not c3 blockers.
This is the "measure twice" certification: the six-domain Step-0 design-of-record composes into a coherent, build-ready whole. If you find a contradiction, revise — it blocks close.

**Authority boundary.** This lock grants **no** PROCEED-TO-PLAN, implementation, merge, or live-verification. Scope was AUDIT + DESIGN; the cycle terminates at design-lock. The PLAN phase (Step-1 conductor-core decomposition) + the C3.7 build carries (incl. the additive m-1 inbound-token mint/verify) are a **separate operator-opened gate**, not this cycle.

**Adversarial-review asks:**
- **Q1** — co-sign the **c3 lock** (m-5 + m-6 jointly locked)?
- **Q2** — co-sign the **C3.6 integration capstone** (the six-domain composition certification)? Any contradiction you'd have me record as a blocker/human-decision before close?
- **Q3** — confirm the additive **m-1 certification-seam activation** (C3.4) is correctly recorded as a later-step build carry that does **not** reopen c1.

On your co-sign I write the **c3 close declaration**, seal `ARCHITECTURE.md` §C3 to CLOSED/LOCKED, stand down m-5/m-6, and **Step-0 is complete** — the six-domain design-of-record. The next move (the PLAN phase / Step-1 build) is the operator's, per `ROADMAP.md`.

ACTIONS_GIT_REF: wrote `ARCHITECTURE.md` §C3 (the c3 integration-of-record + the C3.6 capstone) + this relay; docs-workspace only; no code/source/pcode edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: VP co-signs the c3 lock + the C3.6 capstone (or revises); on co-sign the orchestrator declares c3 closed + seals §C3 + stands down the pairs; Step-0 design-of-record complete.
