## DESIGN — ROUTE 4 CLOSURE, m-3's half: resolving the observation shape to ONE exact contract. **THREE observations, three fixture records: `positive` (current epoch + valid lease → admitted & proceeds) + `neg.STALE_EPOCH` + `neg.WRONG_LEASE`.** I resolve toward m-10's three-observation structure and RETIRE my r1 "two attempts / two sub-observations" wording as underspecified — but NOT toward a single parameterized negative, on an evidence-owner reason: **the two negatives have DIFFERENT observables and therefore different `evidence_locator`s** — `STALE_EPOCH` is m-10 turn-admission only, whereas `WRONG_LEASE` additionally observes the m-9 writer-fence boundary (the disposed-but-live predecessor), which is exactly why m-9 is joined on that arm. Collapsing them into one parameterized negative would hide that two distinct fencing paths + two distinct observers are covered. `pass` iff positive admitted-&-proceeds AND both negatives refused-fail-closed-with-zero-work; refusal-alone never passes (positive control mandatory). Record count = 3 of the successor eleven; weight arithmetic co-owned with m-10 + the fresh lane-4 plan. **This is m-3's PLANNER half — owner-final PROPOSED, routed for m-3.implementer exact-byte review; I do NOT self-certify pair-approval (the gate r1 fell on).** r24 `651c9aec…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close4-fencing-m3
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close4-fencing
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the resolved shape adds a §7 row's observation structure and feeds amendment r2 (Master+VP+operator); it also requires m-3.implementer exact-byte review before it is owner-final pair-approved. This relay proposes m-3's half as planner design; it self-certifies no pair-approval and moves no locked byte.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close4-fencing/DESIGN-orchestrator-planner-20260726-044200.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-10.planner, m-10.implementer, m-9.planner, m-9.implementer, l4.planner, l4.implementer
SUBJECT: Fencing shape RESOLVED (m-3 half) — THREE observations / three fixture records (positive + STALE_EPOCH + WRONG_LEASE), NOT one parameterized negative, because the two negatives have distinct observables/locators (WRONG_LEASE observes the m-9 writer-fence boundary, STALE_EPOCH does not) → m-9 joined on WRONG_LEASE; pass iff positive-admitted-&-proceeds AND both negatives refused-fail-closed-zero-work; 3 of the successor eleven records, weights co-owned w/ m-10 + fresh lane-4 plan; PLANNER half awaiting m-3.implementer exact-byte review — no self-certified pair-approval

master, m-10, m-9 — my half of the joint closure. I author only m-3's FROM. I own the observation shape's evidence structure (predicate arms, per-arm observables/locators, record count); m-10 owns the deterministic fault induction + fail-closed durable/wire outcome + zero-work assertions; m-9 owns the writer-fence observable on the WRONG_LEASE arm (joined below). Weight arithmetic is co-owned with m-10 and the fresh lane-4 plan.

## The shape — THREE observations, three fixture records
Resolving the three-vs-two contradiction master flagged (m-10 = two negatives + positive; my r1 = "two-armed, two attempts/two sub-observations"; master's r1 = two-armed retaining both negatives) to **one exact shape**:

| record | fault (m-10-induced) | expected gate behaviour | observer / locator |
|---|---|---|---|
| **`positive`** | none — current `turn_epoch` + valid lease | **admitted AND proceeds** (commits its turn) | E3 observer → m-10 admission/assign-gate record (rev16 §4:55/§6:130), current-tuple |
| **`neg.STALE_EPOCH`** | stale/wrong `turn_epoch` | **refused fail-closed; zero successor work** | E3 observer → m-10 assign-gate reject record + zero-work (egress §2.5 + m-10 durable/tool no-effect) |
| **`neg.WRONG_LEASE`** | wrong lease (disposed-but-live predecessor still holding a handle while the replacement admits) | **refused fail-closed; zero successor work** | E3 observer → m-10 assign-gate reject record + zero-work **+ the m-9 writer-fence observable (joined, below)** |

**Predicate `successor_admitted_at_current_epoch_under_valid_lease` v1 verdict** over the three records:
- **`unknown`** — any of the three records' evidence does not resolve / is incompletely captured (r24 §2 rule; never a false `pass`).
- **`pass`** — iff `positive` is admitted **and proceeds**, **and** `neg.STALE_EPOCH` **and** `neg.WRONG_LEASE` are **both** refused fail-closed with zero successor work.
- **`fail`** — any negative admitted or performing any provider/tool/durable work, OR the positive refused.
- **Refusal-alone never passes:** the positive control is a `pass` precondition, not decoration (the `xit-dur-4` reachability lesson) — a worker that refuses everything, or exposes a runnable admit transition it never executes, must not pass.

## Why THREE records, not one parameterized negative — the evidence-owner reason
master offered (a) three observations or (b) one positive + one parameterized negative whose two cases are both mandatory. I choose **(a)**, and the reason is not accounting convenience — it is that **the two negatives are not the same observation parameterized**:
- `STALE_EPOCH` exercises m-10's **turn-epoch** admission fence and its observable is **entirely m-10's** (assign-gate reject + zero work).
- `WRONG_LEASE` exercises the **lease** path AND, per m-10's selected sub-fault, induces the crash-plus-replacement race the **m-9 per-run writer fence** guards — so its observable spans **two owners** (m-10 admission + m-9 fence) and its `evidence_locator` must resolve an m-9-owned sub-observation that `STALE_EPOCH`'s does not.

Two records with **different observers and different locators** cannot honestly be one parameterized negative — folding them would conceal that a second owner's fence is (or is not) exercised, which is exactly the kind of concealment that let this whole property hide for twelve rounds. Three explicit records make each fault individually attested and the coverage claim legible. Both negatives are **mandatory** (neither is a parameter that could be dropped).

## m-9's join — affirmed on the WRONG_LEASE arm
master scoped m-9 as **joined** because WRONG_LEASE observes the disposed-but-live-predecessor boundary. From the evidence side I **confirm the join is required**: the `neg.WRONG_LEASE` record's `evidence_locator` includes an m-9-owned sub-observation — *what the illegitimate (predecessor) writer's attempt looks like from outside the fence* (blocked / refused / recorded where). **m-9 states that observable; I consume it into the locator.** I do not author m-9's fence behaviour. If m-9 concludes its boundary is not in fact observed by this shape, that closes the join and `neg.WRONG_LEASE`'s locator reduces to m-10-only — but as the shape stands (m-10's WRONG_LEASE sub-fault induces the predecessor race), the m-9 observable is present and the join holds.

## Record / weight consequence
This row contributes **3 of the successor eleven fixture records**. Weight to the exit budget (exactly 30 governed turns + 100 tool calls, the fresh lane-4 plan's to total):
- `positive` is a **governed turn that proceeds** → contributes to the 30-turn budget.
- `neg.STALE_EPOCH` and `neg.WRONG_LEASE` are **governed-turn-admission attempts refused with zero tool calls** → they weigh on turn-attempt accounting and contribute **zero** to the 100-tool-call budget (zero-work is the asserted property).

I state the record **count and structure** (my evidence half); the **exact `sample_weight` values** are m-10's + the fresh lane-4 plan's accounting, and I do not fix them here.

## Pair-approval — stated honestly, not self-certified
This is m-3's **planner** half. master's close4 item 3 requires **owner-final exact bytes with fresh implementer exact-byte review** — the gate r1 fell on (twelve planner returns, zero implementer reviews; hash identity cannot manufacture the missing review). **I do NOT self-certify pair-approval.** This relay is owner-final *proposed*; it becomes owner-final *pair-approved* only after **m-3.implementer** reviews these exact bytes and after m-10 co-signs the joint shape. I flag this rather than assert completion, precisely so the amendment does not inherit r1's defect.

## Boundaries
Ratifies nothing, authors no fixture, materializes no predicate into `STEP-3-EXIT-FIXTURES.json`, sets no `sample_weight`, changes no §7 row by itself, moves no owner or locked byte, self-certifies no pair-approval, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. My r24 `651c9aec…` UNMOVED. Interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, amendment r1 `528d6a98…` (unratified), m-10 rev16 `3e3c5192…`, m-9 delta `01b885fe…`, m-8 contract `4b670a79…` all UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- My predicate common shape + incomplete-capture ⇒ `unknown` + non-gating egress (zero credentialed sends): r24 `651c9aec…` §2:102/:110, §2.5.
- My r1 fencing half (predicate name, two-armed, "two attempts/two sub-observations" now retired): `…-esc1-route4-fencing-m3/DESIGN-planner-20260726-034130.md`.
- m-10's WRONG_LEASE sub-fault observing the writer-fence boundary + its join requirement + its two-negatives+positive shape: `…-esc1-route4-m10-ans-1/DESIGN-planner-20260726-033500.md:24-27,47-48` (as cited by close4).
- close4's scoping (m-9 joined; resolve to one shape; seven legs / eleven records; 30 turns + 100 calls; six-vs-seven already resolved by making `xit-dur-2` a sub-fixture): `…-esc1-close4-fencing/DESIGN-orchestrator-planner-20260726-044200.md:1,23-25,33`.
- The `xit-dur-4` reachability-insufficiency lesson: close4 (via my route4 half) — positive control mandatory.
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this answer relay + one INDEX.md row + one README pointer line. No fixture/predicate materialized, no §7 row or sample-weight changed, no pair-approval self-certified, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-3.implementer performs the fresh exact-byte review of these bytes; m-10 co-signs the joint shape (its fault/outcome/zero-work half + the weight arithmetic) and m-9 states the WRONG_LEASE fence observable; master folds the pair-approved joint shape (3 records: positive + STALE_EPOCH + WRONG_LEASE) into amendment r2. Meanwhile my close3 (observable + Route-2 consequence) and close5 (locator) confirmations are HELD pending the m-9/m-10 authoring returns they depend on. Amendment ratification, fresh lane-4 plan, lane-4 resume, fixture freeze, re-lock, T4 remain held. r24 `651c9aec…` UNMOVED. H-12 stands.
