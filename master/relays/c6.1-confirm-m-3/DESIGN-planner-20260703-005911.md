## DESIGN — c6.1-confirm-m-3 delta-2 FOLDED (egress = non-terminal park across §3.2(c)/token-map/§3.3/§7); re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6.1-confirm-m-3
PARENT_DISPATCH_ID: c6-differential
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, m-6.planner, operator
IN_REPLY_TO: c6.1-confirm-m-3/DESIGN-orchestrator-planner-20260703-005455.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: delta-2 folded (CTO-authorized) — egress never a terminal token; §3.2(c)/token-map/§3.3-note/§7 + c4 fold-log echo aligned to m-6/m-7/ARCHITECTURE; re-review the delta

m-3.implementer — the CTO **accepted the flag** ("the catch is correct, the miss is mine") and **authorized me to fold** the delta-2 re-draft (`DESIGN-orchestrator-planner-20260703-005455`). Folded — the convergence principle: **egress is never a terminal token (not `held`, not `rejected`) — it is the non-terminal `egress_blocked` park.** Re-review the delta; return `DESIGN_REVIEW_VERDICT`. Delta-1 (§9) already stands pair-confirmed (your `…-000123`).

### Delta-2 fold (5 touch-points, all one disposition)
1. **§3.2(c) + CQ-4 token mapping** (`:63`) — `(a)/(b)` acceptance-stage (predicate-false / declared-vs-observed integrity) ⇒ terminal `rejected`; **`(c) egress ⇒ non-terminal `egress_blocked` park + A local resummon at the OUTBOUND external-send chokepoint (§7, m-6 §50/§51) — never a terminal token (not `rejected`, not `held`)`**; `(d)` authority-class not-full-verification ⇒ terminal `held`.
2. **§3.3 note** (`:76`) — dropped the "acceptance-time content-safety egress veto (c) → rejected" phrasing (which I flagged + the CTO wrote); now: egress evaluated **only** at the outbound chokepoint ⇒ `egress_blocked`; the acceptance-stage `→ rejected` are the (a)/(b) vetoes, not egress. Anchored to m-6 §2:46/§50/§51, m-7 NF-S9, ARCHITECTURE :309-310.
3. **§7 policy** (`:133`) — clarified the J1 "hold-and-resummon" park **is the non-terminal `egress_blocked` state (§3.3), NOT the terminal `held` token**; egress is never a terminal outcome.
4. **c4 fold-log echo** (`:237`) — **egress-fail removed from the `rejected` group** (it was in the original c4 CQ-4 settle; c6.1-superseded), marked with the correction. *(Caught in my own spot-check — the fix-all-echoes discipline; it would also have failed the CTO's `egress→rejected` spot-verify.)*
5. **§15 fold-log** — new `c6.1-confirm-m-3` entry recording delta-1 confirm + delta-2 fold.

The §3.3 **table row** (`:73`) already mapped egress → non-terminal `egress_blocked` park (CTO's correct row-fix) — unchanged.

### Adversarial targets
(a) Is there any **live** `egress → rejected` or `egress → held` **terminal** mapping left? (My spot-grep: the remaining `egress`+`held`/`rejected` hits are (i) the authority-class-unobservable → `held` decision-② text that only *analogizes* to "the fail-closed egress posture" — not an egress→held mapping; (ii) the "egress auto-redact … rejected" rejected-*alternative*; (iii) negated/non-terminal phrasings. Confirm none is a live egress→terminal.)
(b) Does §3.2(c) now agree with §3.3 + §7 + the table row (one disposition, no internal contradiction)?
(c) Byte-exact `{accepted, rejected, held}` enum + the four sanctioned claims still intact; no mechanism change / no new token / no lock reopen?

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/c6.1-confirm-m-3/DESIGN-planner-20260703-005911.md` — OK.
- Spot-grep `egress.*(rejected|held|terminal)` over the doc: §3.2(c) `:63`, §3.3 `:73`/`:76`, §7 `:133` all state **non-terminal `egress_blocked`**; §8 `:144` + §12 `:194` are the authority-class-`held` decision-② *analogy* ("matching the fail-closed egress posture"), not egress→terminal; `:198` is a rejected-*alternative*; `:237` fold-log now marks egress-fail c6.1-superseded out of the `rejected` group.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace).

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd is a non-git docs workspace. Artifacts: design-doc delta (§3.2(c) `:63`, §3.3 note `:76`, §7 `:133`, c4 fold-log `:237`, §15 c6.1 entry) + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-3.implementer re-review the delta-2 fold → `DESIGN_REVIEW_VERDICT: approve`; then I return the closing `c6.1-confirm-m-3` to the orchestrator (delta-1 confirmed + delta-2 folded/approved) → CTO spot-verify + VP c6.1 re-close.
