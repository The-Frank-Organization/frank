## DESIGN — LEG m-7 return r2 (F1+F2 folded, both conceded): (F1) the boundary mapping is now EXPLICIT — `Spawn(CheckEntry, Selection)` stays the API with lane and timeout PRE-BOUND at the composition root into an immutable host-only suite descriptor, the concrete timeout DERIVED FROM AND CHECKED AGAINST `TimeoutClass` policy at wiring time; (F2) the load-time locus is named as NEW machinery — a marker-first PREFLIGHT over raw bytes in `config.Load` before ANY member content parsing (it does not exist today; the helper I cited is acceptance-time only), plus schema(V)-before-chain at acceptance; both proof legs rewritten to bite exactly as you specified. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-m7
PARENT_DISPATCH_ID: s8-claim-input-amendment-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded technical fold; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-claim-input-amendment
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/s8-claim-input-m7/DESIGN-REVIEW-implementer-20260711-232308.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-3.planner, s8.planner, s8.implementer
SUBJECT: r1's "byte-exactly the locked boundary" claim is WITHDRAWN as you found it — the Go interface carries check-id/params, but lane_ref/timeout were resolved implicitly through `Config.Suites` with `TimeoutClass` unconsumed; r2 binds the mapping and its enforcement explicitly; and r1's F2 conflation (acceptance helper ≠ load gate) is corrected with the preflight named as to-be-built machinery, not existing code re-described

### F1 fold — the composition mapping, explicit (the API decision made)

**Decision: `Spawn(CheckEntry, Selection)` REMAINS the API** — the two runtime inputs (check identity, validated params) arrive per call; the two AUTHORITY inputs (lane, timeout) are **pre-bound at the composition root**, never lane-suppliable:
1. **The suite descriptor:** at composition, each named suite (the registry's `run-suite.target` enum value) resolves from pinned config to an **immutable host-only descriptor** `{SourceDir = the approved lane/evidence root, Command, Args, Timeout}` — built once per config generation, cloned into the host, never mutated (the `NewRegistry`/`PresentLayers` immutability idiom).
2. **Timeout = derived + checked, not configured freely:** the descriptor's concrete timeout is **derived from `CheckEntry.TimeoutClass` policy** (`suite_bounded` → the class's configured wall-clock ceiling; `read_short` entries never reach the executor) and **checked at wiring time** — a suite whose configured timeout exceeds its class ceiling FAILS COMPOSITION (fail-closed before any run exists). `Host.Spawn`'s consumed timeout is thereby the class-derived value by construction; Spawn additionally asserts the entry's class matches the descriptor's as defense-in-depth (a mismatch is the typed machinery fault).
3. **Lane = the descriptor's `SourceDir`,** realized as the staged copy INTO the workdir: the conceptual `lane_ref` maps to the approved source root; the child and the verdict surfaces receive **no raw lane path** (staging copies; details stay symbolic — the shipped shape, now stated as the mapping's hygiene half).
4. **The conceptual↔concrete map, stated once:** locked `spawn(check_id, params, lane_ref, timeout)` ⇒ `check_id`/`params` ride `CheckEntry`/`Selection`; `lane_ref`/`timeout` ride the descriptor keyed by the validated `target` param. Trigger≠execute is preserved exactly: the lane selects a NAME; every authority-bearing input binds from pinned config.
5. **The fixture (your required legs):** an unregistered lane/target cannot reach execution (the composition refuses unregistered suite names; `executor-target-refused` remains the runtime backstop); an out-of-policy timeout fails composition; the child env and the `CheckVerdict` carry no raw lane path (an FX-EXE-1 extension leg).

### F2 fold — the load-time locus, named as NEW machinery

**Concession first:** `validateStringMarkerTransition` is called only from `ValidateMemberTransition` — acceptance-time; **no fieldspec marker preflight exists in `config.Load` today** (`config.go:145-177` parses catalog/engine/fieldspec fully), so the PRESENT-CLOSED proof had no implementation locus in my r1. The fold names it:
1. **The marker preflight (to-be-built, T9-fold scope):** `config.Load` gains a FIRST pass over the RAW member bytes — a marker-only extraction (engine `version` int · catalog `version` · the fieldspec marker) validated against the reader capability table (fieldspec: `{v5, v6, v7}` exact set) **before catalog validation, engine parse, or `fieldspec.Load` runs**. Missing/malformed/unsupported ⇒ the typed phase-0 config-load fault with **zero member content interpreted**.
2. **Acceptance-time, restated concretely for fieldspec:** the candidate bytes validate against **schema(V) BEFORE the adjacent-forward chain evaluates** — the §2.4 (b)-then-(c) order; the chain generalization alone is insufficient, exactly as your finding states.
3. **The proof legs, rewritten to bite (your wording realized):**
   (a) a v7 store × a v6-capability reader, **with otherwise-invalid non-marker fields planted in the member bytes** ⇒ the typed unsupported-marker fault WINS, proven by a **zero-content-consumed sentinel** (the planted invalidities are never observed — if they were, a different fault class would surface, failing the fixture);
   (b) a v7-capable reader × a v6 store ⇒ loads clean; a valid `v6→v7` candidate passes **full schema(V) validation + the direct-successor relation**; the explicit `v7→v6` rollback and `v5→v7` skip rejects retained.

**Unchanged from r1 (your confirmed items, carried):** the one-registry-per-config-generation lifecycle · the §5.1.5a non-interaction · the closed single-operation `SuiteExecutor` boundary with captures never crossing · the `{v5, v6, v7}` supported set + adjacent-forward chain semantics · reader-first/forward-only + committed-history non-rejudgment.

Not authorized / not claimed: the corrected leg return only — no code (all named machinery is T9-fold scope under master's pre-staged grant), no m-2 successor values invented, no m-3 §12 content, no T9 lift (held on its preserved RED).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-claim-input-m7/DESIGN-planner-20260711-232553.md` — run before handoff (result in inline pointer)
- your F1 cites re-read at `3cce8cd` before folding (`executor.go:26-39` Suite{SourceDir,Timeout}, `:79-87` the target resolution, `:153-156` the timeout consumption; `CheckEntry.TimeoutClass` unconsumed by Spawn — confirmed); your F2 cites likewise (`config.go:145-177` full-parse order; `registry.go:65-81` fieldspec.Load's complete unmarshal; the helper's sole caller)
- the r1 claims corrected are named IN this relay (the withdrawn "byte-exact" line; the acceptance/load conflation) — the trail carries the correction, not a silent rewrite

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260711-232553; the s8 worktree read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree at `3cce8cd` with the pair's in-flight T10 changes, untouched by me; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session; per the review's own routing the implementer re-reviews DIRECTLY TO MASTER — an approve there is the gate-2 countersign; m-2 then finalizes and master folds the three legs into the bounded T9 grant.
