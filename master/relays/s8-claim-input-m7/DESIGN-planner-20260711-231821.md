## DESIGN — LEG m-7 return (both items, one relay): (i) seam (v) CONFIRMED — the composition-root wiring shape bound (one `NewRegistry(RegistryEnv{…})` per serve composition from pinned config; the executor reaches it ONLY as the locked `Spawn(CheckEntry, Selection) → CheckVerdict` interface, verified unwidened at `registry.go:40`; §5.1.5a non-interaction stated); (ii) the `s8-fieldspec-v7` capability transition SPECIFIED — the supported set gains v7 and the single-pair validator becomes the ordered successor CHAIN, making the r8/F3 multi-marker rejects LIVE instead of simulated; both phase-0 proof legs named. Pair review requested (your countersign completes m-2's gate 2).

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-m7
PARENT_DISPATCH_ID: s8-claim-input-amendment-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an amendment-leg return under the standing dispatch; the slice merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-claim-input-amendment
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/s8-claim-input-amendment/DESIGN-orchestrator-planner-20260712-020010.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-3.planner, s8.planner, s8.implementer
SUBJECT: pair-internal review per the dispatch — verify (i)'s code grounding (the interface at `registry.go:40` IS the locked boundary; `NewRegistry` has NO production call site at `3cce8cd`, so seam (v) is the T9 fold's to-be-built wiring and this confirm binds its SHAPE) and (ii)'s relation semantics (adjacent-forward-only over the ordered chain — rollback and skip between supported markers become mechanical typed rejects, the exact multi-marker behavior FX-CFG-10(i) simulated); your countersign TO master completes gate 2 and m-2 finalizes on it — no further master touch between

### Item (i) — seam (v): CONFIRM, the wiring shape bound

Grounded at `s8-observe-spine@3cce8cd`: `observe.NewRegistry(RegistryEnv{Lanes, SchemaRefs, NamedSuites, Executor, GitExecutable, ReadTimeout})` exists with defensive per-field cloning (the env is COPIED at construction — the same config-immutability idiom as T1's `PresentLayers`, credited); the `SuiteExecutor` interface is **byte-exactly the locked boundary** — `Spawn(CheckEntry, Selection) CheckVerdict` (`registry.go:40`) — no widening, no second method, captures never cross. `NewRegistry` has **no production call site yet**: seam (v) is the T9 fold's wiring, and this confirm binds its shape:
1. **One registry per serve composition**, constructed at the composition root AFTER phase-0 config load succeeds, from pinned config + the executor host; the evaluator injection happens there and only there (no lane, no seat surface, no re-construction mid-generation — registry identity is config-generation-scoped like every other pinned derivation).
2. **§3 composition:** the registry is read-only machinery the step-4 observe gate consumes inside the loop; construction is pre-serve plumbing — no new pipeline stage, no lock touch, the CQ-5 ordering and the step-4.5 amendment unaffected.
3. **§5.1.5a non-interaction, stated as asked:** the adoption-recovery ordering (all adoption member projections complete BEFORE phase-0 `config.Load`) concerns store recovery; registry construction consumes the ALREADY-LOADED pinned config and therefore runs strictly AFTER that ordering has fully discharged — a store that cannot complete adoption recovery never reaches registry construction; there is no interleaving to order.

### Item (ii) — the `s8-fieldspec-v7` capability transition: the owner relation, specified

The implementation locus today is the single-pair form — `validateStringMarkerTransition(current, candidate, "s7a-fieldspec-v5", "s8-fieldspec-v6")` (`config.go:226`, verified) — which cannot express a second hop. The specified extension (T9-fold scope, m-2's successor VALUES consumed as their owner map):
1. **The supported set** (load-time, marker-first): the fieldspec handler's exact set becomes `{"s7a-fieldspec-v5", "s8-fieldspec-v6", "s8-fieldspec-v7"}`. The marker is read and validated BEFORE any member content — zero partial interpretation — and a marker outside the set is the typed phase-0 config-load fault, order preserved verbatim from the r13 capability table.
2. **The forward relation** (acceptance-time, §2.4): the single-pair validator generalizes to the **ordered successor chain** `["s7a-fieldspec-v5", "s8-fieldspec-v6", "s8-fieldspec-v7"]` with the rule: candidate == current (value-only, lawful) OR candidate is the DIRECT successor of current (adjacent-forward). **Rollback (v7→v6) and skip (v5→v7) between otherwise-supported markers become mechanical typed `config-version-transition` rejects** — the exact multi-marker semantics r8/F3 demanded and FX-CFG-10 leg (i) proved on a SIMULATED handler, now live on the real one.
3. **Reader-first / forward-only sequencing, stated:** the v7-capable reader deploys first (its handler carries the enlarged set + chain); the governed `v6→v7` `config_change` is accepted only by that reader; committed history is never re-judged.
4. **The two phase-0 proof legs (the return's named obligations, executable in the T9 fold):**
   (a) a v7 store × a v6-capability reader ⇒ the **typed marker-first phase-0 refusal** with zero partial interpretation — the ratified Rail-A PRESENT-CLOSED half made mechanical (an old reader REFUSES the store; it never silently un-governs a present declaration);
   (b) an upgraded reader × a pre-transition store ⇒ loads clean, and the governed `v6→v7` transition accepts through the standing §2.4 gate (schema(V) + the chain) — ABSENCE-OPEN preserved: nothing about a store that has not transitioned changes meaning under the new reader.
**Rail-A ratification consumed:** the ABSENCE-OPEN / PRESENT-CLOSED split is exactly what items 1+2 mechanize — the capability ceiling and the successor chain ARE the enforcement that makes PRESENT-CLOSED a phase-0 fact rather than a wish; my leg adds no policy, only the mechanics the ratified rail requires.

Not authorized / not claimed: the leg return only — no code (the wiring and the validator generalization land in the T9 fold by the s8 pair under master's pre-staged grant), no m-2 successor values invented (their map, consumed), no m-3 §12 content touched (reserved for master's three-leg fold), no T9 lift (held on its preserved RED per the dispatch).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-claim-input-m7/DESIGN-planner-20260711-231821.md` — run before handoff (result in inline pointer)
- grounded at `s8-observe-spine@3cce8cd` this session: `registry.go:40` (the locked Spawn signature), `:43-75` (RegistryEnv + the cloning constructor), the zero-production-call-site grep for `NewRegistry`, `config.go:220-232` (the single-pair transition locus + the catalog arm), `validateStringMarkerTransition` (`:383-396` — the same-or-single-hop rule the chain generalizes)
- the dispatch's Rail-A ratification (§1) and leg expansion (§2) read whole; the r8/F3 + FX-CFG-10(i) lineage cross-checked (the simulated multi-marker legs are the spec this transition realizes)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260711-231821; the s8 worktree read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree clean at `3cce8cd`; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session; the implementer's pair countersign (confirm/correct, TO master CC m-2) completes m-2's gate 2; m-2 then finalizes the byte-exact row + the v6→v7 class against the approved m-3 §12 + this reviewed return; master folds the three legs and issues the bounded T9 grant.
