## DESIGN — m-10 WEIGHT RE-ADJUDICATION from the corrected two-actor WRONG_LEASE sequence (VP2 F5; folds my planner-only concurrence `f3e0c2ae…`). My prior blanket `{0,0}` was WRONG: verified at the approved fencing object `a9ca1952…` §3, `neg.WRONG_LEASE` stages actor **B (legitimate replacement) that IS assigned + `turn_open`'d** (then would-blocks at `session.lock`, zero downstream work) — so a `turn_open` DOES occur and the `0/0` premise ("neither assign nor turn_open") is false for that record. Corrected m-10-domain ruling: **`neg.STALE_EPOCH` = {governed_turns: 0, tool_calls: 0}** (stale actor rejected pre-assign, no turn_open); **`neg.WRONG_LEASE` = {governed_turns: 1, tool_calls: 0}** (B's admitted-then-fenced turn opens ONE governed turn, zero tool calls). 12-record coupling (2a) re-affirmed with its precondition. The aggregate 30/100 rebalance is joint m-10+m-3+l4. For m-10.implementer exact-byte review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — re-adjudicates the m-10-domain turn/call facts of the fencing records for pair review; sets no §7 row, freezes no aggregate weight (joint), moves no byte. The §7/sample-weight supersession + ratification stay Master+VP+operator via the fresh lane-4 plan.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: pending-implementer-review
DISPATCH_ID: step3-relock-lane4-esc1-close-vp2-m10-weight-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-vp2
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-vp2/DESIGN-orchestrator-planner-20260726-153807.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close-vp2-m10-weight-1/DESIGN-planner-20260726-154900.md
SUBJECT: m-10 weight re-adjudication — from the approved `a9ca1952…` §3 two-actor sequence: STALE_EPOCH {0,0}, WRONG_LEASE {governed_turns:1, tool_calls:0} (actor B assigned+turn_open'd then fenced = one governed turn); folds concurrence `f3e0c2ae…` (2a 12-record re-affirmed, 2b `0/0` corrected); aggregate 30/100 joint with m-3+l4; requesting m-10.implementer APPROVE

m-10.implementer — the m-10 weight re-adjudication, folding my planner-only concurrence `f3e0c2ae…` and correcting its `0/0` premise per VP2 F5. Verified at the approved fencing object bytes before ruling.

## What F5 corrected (verified at `a9ca1952…` §3)
The approved WRONG_LEASE fixture is a **two-actor sequence**, and my prior `{0,0}` concurrence rested on a premise the approved bytes contradict:
- **Actor A — disposed predecessor:** m-10 admission gate rejects it **pre-assign — no `assign`, no `turn_open`** (`a9ca1952…` §3 row A). Never reaches the worker.
- **Actor B — legitimate current replacement:** it **IS assigned + `turn_open`'d**, then attempts `session.lock`; `flock` would-blocks (predecessor A retains the OFD); B performs **no recovery-read, no journal attach, no write, no provider/tool/conductor work**, and emits the bounded fault (`a9ca1952…` §3 row B).
My concurrence `f3e0c2ae…` assumed "neither assign nor `turn_open` occurs" for the refused record — **true for A, false for B.** A `turn_open` **is** emitted to B. Corrected below.

## The corrected m-10-domain ruling (turn/call facts I own)
| fixture record | governed_turns | tool_calls | m-10-domain rationale |
|---|---|---|---|
| **`positive`** | rides `xit-dur-1` | rides `xit-dur-1` | admits + proceeds (a governed turn), counted in the durability leg it rides (2a) |
| **`neg.STALE_EPOCH`** | **0** | **0** | the stale actor is rejected at m-10's admission gate **pre-assign** — no `turn_open` is emitted, no `turns` row, zero work |
| **`neg.WRONG_LEASE`** | **1** | **0** | actor **B is legitimately assigned + `turn_open`'d by m-10** (the continuation-admission transaction commits B's `turns` row + lease + `turn_open` at the current epoch) ⇒ **one governed turn is OPENED**; B then fences at the D1 `session.lock` (m-9-owned) and performs zero recovery/tool/provider/conductor work ⇒ **0 tool calls**. The failure is worker-side, **after** m-10's governed admission — so the governance machinery IS exercised, which is exactly what a "governed turn" counts. |

**The correction, stated plainly:** `neg.WRONG_LEASE` consumes **1 governed turn** (B's admitted-then-fenced turn), not 0. Only `neg.STALE_EPOCH` (and actor A within WRONG_LEASE) is a true pre-assign refusal at `{0,0}`. My blanket `0/0` was wrong on the approved sequence and is withdrawn.

## 2a — the 12-record coupling: RE-AFFIRMED (F5 does not touch the positive)
F5 corrected the **negative** actors; the **positive** arm is unchanged. The fencing positive still rides the regenerated `xit-dur-1` fixture (a positive resume admits a successor at the current epoch/lease = my assign-gate observation, rev16 §4:55/§6:130), under the **binary precondition** I named: the regenerated fixture must **expose the assign-gate-open-at-the-current-tuple observation** (not merely "a resume happened"/content fidelity), with **hard-stop + retender-13** on failure (never a silent cardinality change). The 12-record count stands (10 + the two negatives, positive riding `xit-dur-1`).

## Aggregate 30/100 — JOINT with m-3 + l4 (I bind only the m-10-domain facts)
I bind the **per-record m-10-domain facts** above: STALE_EPOCH opens 0 governed turns; WRONG_LEASE opens **1** governed turn via B's `turn_open`; both open 0 tool calls; the positive rides `xit-dur-1`'s governed turn. **The aggregate exactly-30-turns / 100-calls rebalance is the joint m-10+m-3+l4 composition** (the fresh lane-4 plan totals it) — and it must now account for the **+1 governed turn** the corrected WRONG_LEASE record contributes, which my prior `0/0` had hidden. I do not unilaterally set the aggregate; I supply the corrected m-10 input to it.

## Boundaries
Design-only, my re-adjudication half, for pair review. No §7 row set, no aggregate weight frozen, no fixture authored, no `xit-dur-1` regenerated, no amendment, no byte moved, lane 4 not resumed. rev16 `3e3c5192…`, fencing object `a9ca1952…`, r1 amendment `528d6a98…` (unratified), r2 `ed7e22af…` (preserved-unratified), interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…` UNMOVED. The §7/sample-weight supersession + fresh lane-4 plan + ratification stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- F5 corrected sequence verified at `master/domains/m-9-model-runtime/design/2026-07-26-fencing-observable-onefile.md` @ `a9ca1952…` §3 (row A pre-assign reject/no turn_open; row B assigned + `turn_open`'d + fenced, zero downstream work; §4 the cardinality/record/weight is m-10+m-3+l4's) — before ruling.
- rev16 `3e3c5192…` §4:55 (atomic lease-at-current-epoch admission commits the `turns` row + lease + `turn_open`) / §6:130 (two-form assign gate) — UNMOVED.
- Folded concurrence `f3e0c2ae…` (`…-resign-m10-concur-1/DESIGN-planner-20260726-150400`): 2a re-affirmed, 2b `0/0` corrected to STALE_EPOCH{0,0}/WRONG_LEASE{1,0}. VP2 F5 dispatch: `…-close-vp2/DESIGN-orchestrator-planner-20260726-153807`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close-vp2-m10-weight-1/DESIGN-planner-20260726-154900.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this re-adjudication relay + one INDEX.md row; no §7 row/aggregate weight/fixture/amendment authored, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte reviews this re-adjudication; m-3 + l4 compose the aggregate 30/100 from the corrected per-record facts (WRONG_LEASE +1 governed turn); both pairs approve; master folds the pair-approved weight into the fresh lane-4 plan / r3. rev16 UNMOVED. H-12 stands.
