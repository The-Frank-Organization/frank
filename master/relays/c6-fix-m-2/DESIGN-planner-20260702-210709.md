## DESIGN (m-2 planner, rev2 — folds m-2.implementer must-revise `210401`) — README sweep gap + finding-id typo closed

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-2
PARENT_DISPATCH_ID: c6-fix-m-2-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — DOC-ONLY README status fix; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: m-2.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-4.planner, m-5.planner
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: c6-fix-m-2
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: c6-fix-m-2/DESIGN-REVIEW-implementer-20260702-210401.md

**Both blockers were correct — README sweep was incomplete. Folded; re-relaying.** The design-doc fold (F1/F5/F9/F10 + the m-5-F1/F2 slices) was accepted in `210401`; the only gap was m-2-F7's README rows. DOC-ONLY; no mechanism change.

### Blocker 1 — m-2-F7: stale README row :31 → SWEPT
The `c4-cq-gateconfig` row still asserted "**Joint legs remain (not m-2's):** CQ-4 m-3/m-6, CQ-4b m-3/m-4/m-6, CQ-2 m-3 disposition, then CTO fold" — contradicting the CLOSED status the same table now carries above it. Reworded to: *"m-2's half approved first; the joint CQ closure + CTO fold now proceed **under the m-7 design-lock / c6 gate** (no m-2-blocking leg outstanding)."* Preserves the historical m-2-first fact; removes the stale "joint legs remain" current-state gate.

### Blocker 2 — finding-id typo → FIXED
README c6-fix row: `F5-F1 slice` → **`m-5-F1 slice`** (+ added the `m-5-F2` no-m-2-edit note for exactness; c6 tracking keys on exact finding ids).

### Unchanged (accepted in r1 review `210401`)
All six other findings + the design-doc fold — m-2-F1 (RAISE-ONLY `gate_category` + `gate_category_raised` + AC18), m-2-F10 (canonical id), m-2-F5 (`gate_referenceable` first-class), m-2-F9 (§15 RATIFIED), m-5-F1 m-2-slice (propose-vs-stamp), m-5-F2 (m-5-ledger, no m-2 edit) — and the preservation of the byte-exact enum / CQ-2 `{self_reported, mixed}` / R2 grammar / c5 confusion-resistant vocab were all confirmed non-regressed. No design-doc change in this rev2 — README only.

Blocked: no — holding for m-2.implementer re-review.
Scope drift risk: none — the two named README items only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/c6-fix-m-2/DESIGN-planner-20260702-210709.md` — OK (below).
- README row :31 no longer says "joint legs remain"; the c6 row reads `m-5-F1 slice` (typo gone); grep of `master/domains/m-2-forms-determinism/README.md` for "joint legs remain" and "F5-F1" → 0 hits.
- `git status --short` — unavailable: not a git repo (docs workspace; `pcode/` untouched).

ACTIONS_GIT_REF: edited `master/domains/m-2-forms-determinism/README.md` (c4-cq-gateconfig row de-staled; c6 row id typo fixed + m-5-F2 note); wrote this rev2 relay + appended `master/relays/INDEX.md`; no design-doc change since `210004`; no mechanism change, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer re-reviews the README fix; on `approve` I return the `c6-fix-m-2` completion relay to the orchestrator for the c6 verify sweep + c6 close. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
