## PLAN — s10 pair PLAN: the m-6 plan-of-record (r2, T1–T11) ADOPTED byte-verbatim; the s10 execution layer mapped (seats, fidelity legs, the T1/T2-before-T4 owner-fidelity gate, relay roots, sub-DISPATCH scheme); the 12-row all-in fence restated as the mechanical block of record with anchors re-verified live; plan-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s10-build-plan
PARENT_DISPATCH_ID: master-boot-s10-planner
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the T11 live dogfood ODB verdicts are the operator's own (operator-FROM by locked design); the slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e). No other human leg in this plan.
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.1
IN_REPLY_TO: boot/master-boot-s10-planner/SITREP-orchestrator-planner-20260712-205020.md
FROM: s10.planner
TO: s10.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: the s10 same-owner loop opens — adopt-verbatim PLAN consuming the m-6 plan-of-record per the boot (`master-boot-s10-planner/SITREP-…-205020`) and the m-6 gate-clearance token (`s10-plan-m6-impl/IMPL-planner-20260712-194423`); review → PLAN_REVIEW_VERDICT; on approve with an all-in mechanical check I issue the delegated same-owner token under `s10-build-impl`

s10.implementer — this is the s10 slice's own pair PLAN, the s8 pattern exactly. `PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.1` names the instrument: the m-6 r2 plan-of-record byte-verbatim (sha256 below) + this execution layer + the token's 12-row fence. Nothing in the plan-of-record is re-cut; every named question is already answered upstream (SEQ-2, PARK-ACROSS-V8, Q6×Q4). Your review is the delegated-dispatch precondition.

### 1. Adoption + design-record lineage

**Adopted verbatim, all 11 tasks:** `master/domains/m-6-human-surface-scheduler/plan/2026-07-12-s10-comms-spine-plan.md` — `sha256 f3e508bd356920ebf6a38b815cd44a19e29d750f900090a0a5cb8249e40f5777` (verify independently). This includes its Global Constraints, the two answered named questions (SEQ-2 = fresh-genesis dogfood at v8; PARK-ACROSS-V8 = sequencing-avoidance, the 8a freeze/re-issue branch is s11 and NOT built), the file/seam map, the boundary contract, the out-of-scope lines, and the 10 acceptance criteria. The r1 PLAN is superseded and non-token-bearing; r2 (`s10-plan-m6/PLAN-planner-20260712-193605`) is the reviewed plan of record.

**Design-record lineage (the s8 §2 reading, restated for owner s10):** this relay carries `DESIGN_DOC_ID` as context only and intentionally carries **no** `DESIGN_LOCK_ID`/`DESIGN_RECORD_KIND` — owner s10 locks no design. The EFFECTIVE lock is m-6 c3 (`c3-design-m-6-human-surface-scheduler`, §13 folds included), whose design-review lineage lives in the m-6 chain (the m-6 PLAN r2 parents to the approving c3 DESIGN-REVIEW; the m-6 token `s10-plan-m6-impl/IMPL-planner-20260712-194423` carries that lineage). Locked contracts are **consumed, never re-designed** here; any lock change routes to the owning pair via the amendment path + master (condition c).

**Consumed, re-derived nothing:** the Q6×Q4 resolution (`q6q4-recordkind/RECONCILE-orchestrator-planner-20260712-205010`) — the `odb` + `resummon_command` interpreter-bearing tokens ride ONE governed v7→v8 fieldspec transition (T1, MAJOR-but-safe, marker-first old-reader refusal, no migrator, history un-reclassified); this IS condition (b)'s one named transition. The s5-built park/wake primitives (`completePark`, wake-on-operator-verdict, durable-inbox liveness, crash-safe re-issued wake) are the floor we LIFT, never re-invent.

### 2. Execution mapping (the s10 layer over the adopted tasks)

- **Build seat:** you, `s10.implementer` — sole code writer on the slice (sole-writer discipline per seat). You write the literal Go + exact test bodies against the live tree at the plan's stated grain.
- **Per-task review gate:** me, `s10.planner` — the plan-of-record's "gated by s10.planner's task review" is this pair's arrangement (no separate s10.reviewer seat was staffed). Each task closes on my review of your FILE-captured evidence before the next opens; review requests ride `s10-build-t<N>-review` sub-dispatches.
- **Fidelity by relay, to the standing sessions (never fresh seats, never spawned):** m-2.planner — the T1 registry transition bytes (the s8 T2 owner-fidelity pattern; the final per-token byte + required-field call is theirs). m-7.planner — the T1 capability-marker move (theirs alone) + the T8 scheduler/timer + A-2 dedupe + store seams. m-3.planner — the T6 re-observe/J1 contract + the T9/T10 sunset re-pointing of their check-policy hook (operator-ruling-authorized, m-3 fidelity in-slice). m-1.planner — only if an addressing/mailbox seam moves (none anticipated). m-6.planner GUIDES by relay throughout (mechanism/seam questions route there); m-6 does not direct the build.
- **The T1/T2 owner-fidelity gate (binding, before T4):** m-2's transition + m-7's capability move land and are owner-confirmed at T1/T2 **before any park fixture (T4+) opens**. An owner contradiction at T1/T2 escalates to master BEFORE T4 (condition b). No park machinery lands on an unconfirmed schema foundation.
- **Branch/base:** `s10-comms-spine` off `frank/ main@8941889` (`s8-close`) — the worktree exists at `~/.config/superpowers/worktrees/frank/s10-comms-spine`, verified at `8941889`.
- **Relay roots:** master-visible legs (this loop, escalations, fidelity, the merge-decision relay) under `master/relays/` `s10-build-*`; pair-internal build legs under `frank/.relays/s10/` per sprint-doc-setup; every substantive relay lints clean before handoff.
- **Sub-DISPATCH scheme (condition a):** unique per leg — `s10-build-plan` (this loop), `s10-build-impl` (the token), `s10-build-t<N>-review`, `s10-build-t<N>-fold`, `s10-build-fidelity-m<N>`, `s10-build-escalate-*`, `s10-build-exit`. Never a shared thread id.
- **Order:** vertical-slice-first exactly as adopted — T1→T2 (transition + fresh-v8 foundation, owner-gated) → T3→T4→T5→T6→T7→T8 (the thinnest end-to-end A-gate spine) → T9→T10 (the two operator sunsets) → T11 (EXIT LEG 3 + hardened battery + the merge-decision relay).
- **Disciplines (day-one, the s8-institutionalized exit items — named in acceptance from the start, not discoveries):** TDD with RED-first negatives per task/row; frequent commits; sequence-honest FILE-captured batteries under `frank/.relays/s10/` (never pipe-counted); label→mechanism sweep + verify-the-summary-line in every review (state counts and selector semantics only from re-executed output); named seams recorded for any same-file multi-task edits; the two mechanical tables — consumption→supply reconciliation and diff→license fence-row reconciliation — maintained as we go and green at exit; INV-CATALOG's ten laws green at every commit point. Execution runs under `superpowers:executing-plans` task-by-task.

### 3. Binding carries (verbatim force, restated by reference)

- **Conditions (a)–(g)** exactly as carried in `s10-plan-m6-impl/IMPL-planner-20260712-194423` §conditions: (a) unique sub-DISPATCH_IDs; (b) any registry/schema change beyond the ONE named T1 transition → master BEFORE work; (c) locked-contract changes → owning pair via amendment + master, never silent; (d) cross-domain shape changes → master (the T9/T10 sunset re-pointing of m-3's check-policy hook is authorized by the operator sunset ruling with m-3 in-slice fidelity; anything beyond that hook → master); (e) merge operator-only, the executor acts only on a `HUMAN_MERGE_AUTHORIZATION` grant at grant time; (f) a blocked/model-stopped lane reports and holds; (g) the dogfood evaluation extends the s8 ledger as ONE series.
- **Global constraints** (every task): byte-exact `{accepted, rejected, held}` — parking is `accepted`, `held` is fault-only, `rejected`→bucket D; R2 — the model is never a gate input (the ⑤ model-name field is payload/render only, typed + exempt-marked, egress fixture pair registration s9/s11 NOT s10); Rails A/B stated per new surface (each adopted task carries its Rail-A open/closed call inline — build to them); I-PH over every new seat-visible surface, swept at T11; egress stays fixture-scoped (local-only comms, live chokepoint dormant); no migrator, committed history never re-classified.
- **Both s10 sunsets gate the slice exit** (operator ruling verbatim): s10 does not close while the silent auto-kill (T9) or the static-only `side_effecting` gate (T10) survives. The T9 backstop is block-only — it may KILL, never auto-extend/approve (J1 never-auto-approve).
- **Out of scope (do NOT build):** B/C/D bucket projections; the elaborate-more fork; the full 8a freeze/re-issue branch (sequencing-avoidance + the recorded co-signed floor only); the away-bridge / Seam-C token / decision-④ rotate+re-observe (step-(d)); E3/E4; egress live-chokepoint activation + the ⑤ fixture pair registration; the TUI/email-client UX (Step-4).

### 4. The fence (the mechanical block of record for the token)

Restated verbatim from the m-6 token at seam/package grain — exact new-file paths resolve WITHIN the named packages on-frank; any file outside → escalate to master per (b)/(d), no `minor`/`integration-hook` exception (the s8 fence discipline produced seven clean catches; it is load-bearing). I re-verified every anchor live this session at `frank@8941889`: all 8 file anchors + `internal/engine/`, `internal/observe/`, `test/fixtures/` exist (`frank/.relays/s10/` is created by first use).

SCOPE_DIFF:
- frank/internal/fieldspec/registry.json -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/predicate.go -> in
- frank/internal/fieldspec/render.go -> in
- frank/internal/store/genesis.go -> in
- frank/internal/obligation/obligation.go -> in
- frank/internal/engine/submit.go -> in
- frank/internal/engine/loop.go -> in
- frank/internal/engine/ -> in
- frank/internal/observe/ -> in
- frank/test/fixtures/ -> in
- frank/.relays/s10/ -> in
SCOPE_DIFF_RESULT: all-in

Task→seam map as carried by the token: T1 `fieldspec/` (registry.json + validate/predicate/render); T2 `store/genesis.go`; T3 ODB render + T8 A-2 dedupe = new units under `engine/`; T4 `obligation/obligation.go` (`completePark`); T5 `engine/submit.go` (`gate_resolution`/operator-FROM guard); T6 `observe/` re-fire + `engine/submit.go` wake path; T7 exactly-once + T8 scheduler = `engine/loop.go`; T9/T10 sunset hooks = `observe/read_file_worker.go` + `engine/loop.go` (`read_file_worker.go` resolves within `internal/observe/`); fixtures + slice relays in the last two rows.

### 5. Acceptance criteria

Criteria 1–10 of the adopted plan-of-record stand verbatim (EXIT LEG 3 live on the fresh v8 dogfood store; MAJOR-but-safe v7→v8 with the old-reader phase-0 refusal RED fixture green; A-gates-only park with byte-exact terminals; validated operator-FROM reply + frozen choice-set + single-resolution guard; local re-observe on wake with m-3 fidelity; exactly-once crash-safe wake both crash legs; deterministic A-2-deduped resummons with m-7 fidelity, escalate channel never verdict, no hard deadline; both sunsets demonstrated gone with the block-only backstop; Rails A/B + I-PH + the two mechanical tables green; SEQ-2 + PARK-ACROSS-V8 executed as stated with the s11 8a branch not built). Plus, binding as sequencing: **the T1/T2 owner-fidelity gate holds before T4** — m-2/m-7 confirmation in hand or escalation to master, never proceed-on-assumption. Slice exit ends with the merge-decision relay TO the operator (I author it; condition e governs the grant).

### 6. What I request from you

1. Review this PLAN against the plan-of-record (verify the sha256 yourself), the m-6 token + its fence, the Q6×Q4 resolution, the boot, and c3's spine sections (§2 terminal-token→bucket map, §3 ODB render/capture, §4 FSM + G4 cadence, §9 GRILL_LOCK). Adversarial posture: your job is to find where this execution layer mis-states an upstream binding, not to accept my summary — verify the summary lines against the sources.
2. Run your own mechanical scope check over §4 (the plan-of-record's file/seam map reconciled against the 12 rows) and confirm anchors against the live tree.
3. Return `PLAN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` in a lint-clean relay, `FROM: s10.implementer`, `TO: s10.planner`, `DISPATCH_ID: s10-build-plan-review`, `PARENT_DISPATCH_ID: s10-build-plan`, with your mechanical check in the body.
4. On approve with an all-in result and no hard trigger, I issue the delegated same-owner token under `s10-build-impl`, parented to your approving review, restating the block and conditions (a)–(g). The token remains the only thing that starts code — nothing in this PLAN authorizes an edit.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-plan/PLAN-planner-20260712-201530.md` — run before handoff (result in the inline pointer).
- Grounding this session (E1): plan-of-record sha256 computed; the 11 fence anchors verified on disk at `frank@8941889`; the s10-comms-spine worktree confirmed at `8941889`; boot + token + Q6×Q4 + c3 §§2–4/9 read in full.

ACTIONS_GIT_REF: none — a PLAN relay; no `frank/` edit, no code, no commit, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-201530.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `8941889` (`s8-close`).
Next requested action: operator carries this to s10.implementer; adversarial plan-review → `PLAN_REVIEW_VERDICT` under `s10-build-plan-review`; on approve + all-in I issue the delegated token under `s10-build-impl`; a must-revise returns the exact finding and I fold or escalate.
