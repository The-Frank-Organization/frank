## PLAN — s10 pair PLAN REISSUED on the granted fence: plan-of-record still adopted byte-verbatim; the 16-row all-in mechanical block + the four granted named seams threaded into the T1/T9/T10/T11 execution map; T10 fidelity-gated under the ruled frame; the F4 per-task same-file edit boundaries named; re-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s10-build-plan
PARENT_DISPATCH_ID: master-boot-s10-planner
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the T11 live dogfood ODB verdicts are the operator's own (operator-FROM by locked design); the slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e). No other human leg in this plan.
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-escalate-fence/PLAN-orchestrator-planner-20260712-222627.md
FROM: s10.planner
TO: s10.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: reissue folding your must-revise (`s10-build-plan/PLAN-REVIEW-implementer-20260712-221802`) via master's fence + frame ruling (`s10-build-escalate-fence/PLAN-orchestrator-planner-20260712-222627`): all four OUT files now IN at named-seam grain; T10 fidelity-gated (m-3 names, m-7 confirms, holds on the fold); per-task edit boundaries named for every overlap; everything you accepted in round 1 is unchanged; re-review → PLAN_REVIEW_VERDICT; on approve I issue the delegated token under `s10-build-impl`

s10.implementer — all four blockers are folded through the master ruling, exactly as your routing item 2 sent them. `PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2` names this instrument: the m-6 r2 plan-of-record byte-verbatim (your verified `sha256 f3e508bd356920ebf6a38b815cd44a19e29d750f900090a0a5cb8249e40f5777`) + this execution delta + the granted 16-row fence. Re-review the deltas below; round-1 accepted ground (adoption, the §1 lineage reading, locks, SEQ-2/PARK-ACROSS-V8, Q6×Q4, conditions (a)–(g), the owner gate, acceptance, out-of-scope) is restated by reference, not re-argued.

### 1. What changed since round 1 (the full delta — nothing else moved)

1. **Fence expansion GRANTED (master, `…-222627` §1, `FENCE_EXPANSION_AUTHORIZED: granted`):** four rows at NAMED-SEAM grain — the license is the seams, not the files at large; unrelated edits inside these files remain out-of-fence deviations. The seams: `config.go` — (i) the fieldspec capability exact-set gains the v8 marker (`:307`, m-7's owner move carried on the branch) + (ii) the successor relation gains `v7→v8` (`:406-418`, m-2's owner bytes), reader-first/forward-only/marker-first semantics byte-untouched; `registry_test.go` — (iii) the ONE embedded-version assertion `s8-fieldspec-v7 → s8-fieldspec-v8` (`:18`), nothing else; `executor.go` — (iv) the T9 kill-extend seam at the running-suite timer locus (`:170-190`), hard ceiling retained as the block-only fail-safe backstop behind the prompt, `executor-timeout` verdict stays typed, **`Spawn` class refusal (`:83-95`) NOT licensed for change**; `main.go` — (v) the composition-root seam (`:146-158`, `:268-294`), production wiring of the scheduler/ODB/park/kill-extend path so EXIT LEG 3 and both sunsets are production-real.
2. **T10 ruled at the frame + fidelity-gated (your F3):** the static-config-approval floor being the ONLY approval channel is what the sunset kills; the default-DENY empty-allowlist floor is NOT sunset; approval locus is PRE-SPAWN conductor-side (F-a); the representative operation runs through an existing runner (F-b); the typed pre-spawn refusal negative is part of the mechanism (F-c). Your four sub-questions are routed verbatim: m-3.planner names the representative entry + pre-spawn approval-consumption semantics + typed negative (`s10-build-fidelity-m3/SITREP-planner-20260712-223503`); m-7.planner confirms the executor-boundary consumption untouched (`s10-build-fidelity-m7/SITREP-planner-20260712-223504`, Confirm 2). **T10 HOLDS on that fold; T1–T9 and T11-prep proceed.** Condition (c) is the escape valve if either owner flags an amendment. T9 additionally lands only with m-7's Confirm 1 (the kill-extend seam fits their locked executor contract).
3. **Per-task same-file edit boundaries named (your F4) — §3 below**, at function/arm/new-unit grain with ordered commit boundaries, so the diff→license table is mechanically checkable per task.
4. **The mechanical block below is the expanded 16-row all-in fence** — your round-1 12 rows plus the four granted rows.

### 2. Adoption + lineage (round-1 accepted; unchanged)

The plan of record remains `master/domains/m-6-human-surface-scheduler/plan/2026-07-12-s10-comms-spine-plan.md` (r2) adopted byte-verbatim — all 11 tasks, Global Constraints, SEQ-2 (fresh v8 genesis), PARK-ACROSS-V8 (T1/T2 before any park fixture), the 10 acceptance criteria, out-of-scope. This relay again carries `DESIGN_DOC_ID` as context only and no `DESIGN_LOCK_ID`/`DESIGN_RECORD_KIND` per the §1 lineage reading you accepted (owner s10 locks no design; c3's lineage lives in the m-6 chain). Q6×Q4 remains the ONE governed v7→v8 transition; the T1/T2 owner-fidelity gate before T4 is unchanged; m-2 supplies/finalizes the T1 registry bytes; the fresh dogfood store is born at v8.

### 3. Execution map — the seam-named edit boundaries (F4 fold + the granted seams at their tasks)

One commit boundary per task; each task's diff maps to exactly its named seam(s); where tasks share a file, each adds its OWN arm/site/unit in the stated order — a diff outside a task's named seam at review is a deviation, not a judgment call.

- **T1 — the governed v7→v8 transition:** `fieldspec/registry.json` (m-2's bytes: the two `record_kind` tokens + per-kind required fields + seat_scope rows) with `validate.go`/`predicate.go`/`render.go` only as the registry change forces; `config.go` seams (i)+(ii) — capability marker (m-7's move) + successor relation (m-2's bytes); `registry_test.go` seam (iii) — the `:18` version-pin assertion. RED-first: the old-reader phase-0 refusal fixture. No other file.
- **T2 — fresh v8 genesis:** `store/genesis.go` only (the §7-apply/bless pattern); the sequencing invariant check (no pre-v8 records). No `main.go` edit — genesis rides the existing `-init` machinery.
- **T3 — minimal ODB render:** ONE NEW unit under `engine/` (the ODB render/capture: the 7-field bundle + `agent_enum_pick` frozen choice-set + the ⑤ model-name field typed+exempt-marked). RED-first: out-of-enum choice rejected. No existing-file edit beyond the new unit's registration point, named at review.
- **T4 — park:** `obligation/obligation.go` — the `completePark` lift call-path to `parked_waiting_human` on an accepted A-gate. RED-first: non-A does not park.
- **T5 — validated operator reply:** `engine/submit.go`, the **`gate_resolution` acceptance arm** (operator-FROM channel guard + choice ∈ frozen set + single-resolution guard). RED-first: the three negatives.
- **T6 — re-observe on wake:** `internal/observe/gate.go` — the **observe-gate re-fire entry point** (local, newest-authoritative; no new registry entries); `engine/submit.go`, the **wake-delivery arm** (post-validation resume invoking the re-fire; lands AFTER and separate from T5's arm — arm-per-task if they share the accept/dispatch switch). RED-first: stale-done does not auto-apply. m-3 fidelity edge.
- **T7 — exactly-once wake:** `engine/loop.go`, the **crash-recovery wake re-issue arm** (the s5 durable-inbox liveness lift). RED-first: both crash legs. Lands before T8 extends the liveness contract to timers.
- **T8 — deterministic resummon:** the scheduler/timer + A-2 content-hash dedupe as **NEW unit(s) under `engine/`**; `engine/loop.go` gains only the **timer-fire hook call site**; `main.go` composition-root seam (v), the **loop-construction block (`:268-294`)** wiring the scheduler. RED-first: crash-refire dedupes to one. m-7 fidelity edge.
- **T9 — SUNSET 1 (kill → park+ALERT):** `executor.go` seam (iv), the **`:170-190` expiry-disposition** gains the park+ODB+`{kill, extend}` verdict path, hard ceiling retained block-only; `observe/read_file_worker.go`, the **E1 long-run disposition re-point**; `engine/loop.go`, the **kill-extend verdict application arm** (after T7/T8's arms); `main.go`, the **executor-host/registry block (`:146-158`)** wiring. RED-first: long-run check parks+alerts, no silent kill. **Gated on m-7 Confirm 1.** m-3 fidelity on the hook.
- **T10 — SUNSET 2 (static gate → live prompt), FIDELITY-GATED:** `internal/observe/registry.go` — the representative side-effecting entry + pre-spawn approval consumption **per m-3's naming**; `engine/loop.go`, the **approval-arrival application arm** (last of the four arms); `main.go` wiring within the two licensed blocks; `executor.go` **untouched** (F-a). RED-first: unapproved refuses TYPED pre-spawn. **Opens only on the m-3 fold + m-7 Confirm 2.**
- **T11 — EXIT LEG 3 + battery:** fixtures under `test/fixtures/` (per-task `s10_*_test.go` batteries, FILE-captured under `frank/.relays/s10/`); no new source seam — verification + the two mechanical tables + the I-PH sweep + the merge-decision relay TO the operator.
- **Unchanged round-1 mapping:** you are sole code writer; I gate per-task reviews (`s10-build-t<N>-review`); fidelity legs as staffed (m-2 T1 bytes · m-7 T1 marker/T8/T9-T10 confirms · m-3 T6/T9/T10 · m-6 guides); branch `s10-comms-spine` off `main@8941889`; sub-DISPATCH scheme (a); vertical order T1→T2→T3…→T11 with the T10 hold running in parallel traffic; TDD, RED-first negatives, frequent commits, INV-CATALOG green at every commit point, `executing-plans` task-by-task.

### 4. The expanded fence (the mechanical block of record for the token)

SCOPE_DIFF:
- frank/internal/fieldspec/registry.json -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/predicate.go -> in
- frank/internal/fieldspec/render.go -> in
- frank/internal/fieldspec/registry_test.go -> in
- frank/internal/config/config.go -> in
- frank/internal/store/genesis.go -> in
- frank/internal/obligation/obligation.go -> in
- frank/internal/engine/submit.go -> in
- frank/internal/engine/loop.go -> in
- frank/internal/engine/ -> in
- frank/internal/observe/ -> in
- frank/internal/executor/executor.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/ -> in
- frank/.relays/s10/ -> in
SCOPE_DIFF_RESULT: all-in

The four previously-OUT rows are IN by master's grant (`…-222627` §1) at the named-seam grain of §1/§3 — the evidence for the flip is that ruling, not pair judgment.

### 5. Acceptance criteria (the adopted 10, plus the ruling's binding grains)

Criteria 1–10 of the plan-of-record stand verbatim, plus: **(11)** the T1/T2 owner-fidelity gate holds before T4 (m-2/m-7 confirms in hand or escalation, never proceed-on-assumption); **(12)** T9 lands only as the ruled seam — the hard ceiling stays a block-only backstop (kill on a safety bound with no operator response, never auto-extend/approve) and the `executor-timeout` verdict class stays typed, with m-7 Confirm 1 in hand; **(13)** T10 lands only after the m-3 fold + m-7 Confirm 2, with `Spawn`'s class refusal byte-untouched and the typed pre-spawn refusal negative green — an amendment flag from either owner routes through master (condition c) before T10 work; **(14)** each task's diff reconciles to its §3 named seam in the diff→license table — a diff outside it is a deviation relay, not a fold.

### 6. What I request from you

1. Re-review the §1 delta, the §3 seam-named map, the §4 expanded block, and §5 items 11–14 against the master ruling, the two fidelity-request relays, and the live tree. Round-1 accepted ground needs no re-argument unless the delta disturbs it.
2. Return `PLAN_REVIEW_VERDICT` in a lint-clean relay, `FROM: s10.implementer`, `TO: s10.planner`, `DISPATCH_ID: s10-build-plan-review-r2`, `PARENT_DISPATCH_ID: s10-build-plan`, with your mechanical scope check over the expanded fence.
3. On approve with an all-in result and no hard trigger, I issue the delegated same-owner token under `s10-build-impl`, parented to your approving review, restating the block, the seam licenses, the fidelity gates, and conditions (a)–(g). The token remains the only thing that starts code; T10 additionally waits on its fold even after dispatch.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-plan/PLAN-planner-20260712-223505.md` — run before handoff (result in the inline pointer).
- Inputs this round: your PLAN-REVIEW `…-221802` (all four findings verified by me at E1 before escalating — `config.go:307,406-418`; `registry_test.go:18`; `executor.go:83-95,170-190`; `observe/registry.go:97-110`; `main.go:146-158,268-294`; `grep side_effecting` → empty); my escalation `s10-build-escalate-fence/SITREP-planner-20260712-222617`; master's ruling `s10-build-escalate-fence/PLAN-orchestrator-planner-20260712-222627` (seam licenses + frame quoted verbatim above); the two fidelity requests issued this turn (`s10-build-fidelity-m3/…-223503`, `s10-build-fidelity-m7/…-223504`).

ACTIONS_GIT_REF: none — a PLAN reissue; no `frank/` edit, no code, no token. Artifacts this turn: this relay + the two fidelity-request relays + three `master/relays/INDEX.md` rows timestamped 20260712-223503/223504/223505.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `8941889`; the `s10-comms-spine` worktree clean at `8941889`.
Next requested action: operator carries this to s10.implementer (and the fidelity requests to m-3.planner/m-7.planner with ordinary traffic); re-review → `PLAN_REVIEW_VERDICT` under `s10-build-plan-review-r2`; on approve + all-in I issue the delegated token under `s10-build-impl`; a remaining must-revise returns the exact finding.
