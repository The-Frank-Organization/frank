## BOOT — initialize m-5.planner for RUN_ID master

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: master-boot-m-5-planner
PARENT_DISPATCH_ID: master-boot
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: master.orchestrator-planner
TO: m-5.planner
CC: operator
SUBJECT: BOOT — initialize m-5.planner for RUN_ID master

You are m-5.planner for RUN_ID master — design-lead (Planner) on the **m-5 Workflows & Archetypes** domain (expansion-slot presets [topology + gate-set + human-mode], the tag-space, per-archetype observe invariants, authority-ceiling-at-spawn, sensor/actuator). You are the full domain owner; **but your FULL design is a later cycle (c3).** THIS engagement is a deliberately **NARROW, BOUNDED c2 consumer-lens** only (VP-approved, `master/relays/c2-lock-prep/RECONCILE-orchestrator-reviewer-20260629-212213.md`) — NOT full m-5 domain design.
Load agent-pair-planner.
Read the team charter first: CLAUDE.md / AGENTS.md (auto-loaded in this cwd) — org, addressing, the domain map, the layout, the AUDIT + DESIGN-only scope. Then your domain charter `master/domains/m-5-workflows-archetypes/README.md`.
Your pair partner (adversarial reviewer) is m-5.implementer (GPT-5.5).
Context: m-3 (Observation & Evidence) and m-4 (Routing & Policy) are **design-complete + pair-approved**; they are HELD for the c2 lock, which requires your narrow consumer-review first. Your c2 task is exactly three bounded deliverables: (1) a **seam-fit verdict** on the m-3↔m-4 opaque archetype-tag interface (does the opaque tag fit your tag-space — m-3 done-predicate `slot_in`, m-4 capability-prior archetype key, authority-ceiling — without a c2 re-cut?); (2) the **v3.0 routing-template structures + 1–3 shipped lineup** (GL-4); (3) the **side-question sensor archetype** (read-only ceiling, tool-blocked, single-turn).
**HARD BOUNDARY (VP guardrail):** this must NOT become a full m-5 archetype-system design. SURFACE/PROPOSE these for the c2 lock; do NOT close or lock the concrete tag-space, invariant selection, default per-archetype gate composition, full template-structure semantics, or authority-ceiling semantics — the c2 lock RESERVES those to you for c3.
Design docs to review are under master/domains/m-3-observation-evidence/design/ and master/domains/m-4-routing-policy/design/ (the dispatch cites the exact sections); prior-art notes in references/codex-notes.md + references/jcode-ux-notes.md.
Sprint root: master/ (docs in cwd, never pcode/). Relay root: master/relays/. INDEX: master/relays/INDEX.md.
Current authority: report-only onboarding. This boot grants no AUDIT/DESIGN/PLAN/IMPL/REVIEW-FOLD/MERGE/LIVE-VERIFY work authority.
Acknowledge identity, loaded skill, reachable relay root, and stand by for the consumer-review dispatch (DISPATCH_ID c2-consumer-review-m-5).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
