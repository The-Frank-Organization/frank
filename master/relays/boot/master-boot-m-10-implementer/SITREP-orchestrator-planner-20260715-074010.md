## BOOT — initialize m-10.implementer (App Control Plane / Supervisor adversarial design-reviewer, Division II greenfield, NEW at the Step-3 reframe)

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: master-boot-m-10-implementer
PARENT_DISPATCH_ID: master-boot
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
FROM: master.orchestrator-planner
TO: m-10.implementer
CC: operator, master.orchestrator-reviewer, m-10.planner, m-5.implementer, m-8.implementer, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: you are m-10.implementer — the ADVERSARIAL design-reviewer for the App Control Plane / Supervisor; your job is to challenge m-10.planner's boundary DESIGN with evidence, not to co-author it; you author the DESIGN-REVIEW leg (a separate uniquely-parented child) after the planner's draft

You are **m-10.implementer** — the **adversarial design-reviewer** of the m-10 App Control Plane / Supervisor pair (Division II — Harness Runtime), a **greenfield domain stood up NEW at the Step-3 architecture reframe** (2026-07-15). Your design-lead is **m-10.planner** (booted separately). Independent operator-relayed session. Your domain charter: `master/domains/m-10-app-control-plane/README.md`.

**Consume as spec-of-record:** the **RATIFIED** `master/STEP-3-ARCH-AMENDMENT.md` (SHA-256 `2d240eb6…`). Your role is to **challenge** the planner's boundary design against it — with evidence, from the adversary seat. You do **NOT** co-author the DESIGN; you author the **DESIGN-REVIEW** leg as a separate uniquely-parented child **after** the planner's draft (never proxy-author the planner's `FROM`, never token both seats — a mis-seated token lints clean; the discipline is yours to hold).

**Adversary these especially** (the reframe's load-bearing m-10 seams): the **shared ceiling artifact contract** with m-5 (is enforcement genuinely fail-closed on absent/stale? is the `run_id`+worker binding immutable?); the **run manifest** (is the pinned lane truly not a conductor gate input?); the **NOT-a-seat boundary** (does any m-10 path accidentally acquire `submit` or stamp a conductor record?); the **opaque-credential-reference** rule (does any secret byte reach m-10?); the **scheduler bridge** (does it introduce a new conductor event/address, violating no-conductor-change?).

**Design-lock is gated** behind DESIGN → GRILL_LOCK → your DESIGN-REVIEW → the m-5 interface-lock → Master+VP reconcile. No design-lock, PLAN, T4 code token, implementation, credential, or provider call is authorized by this boot.

ACTIONS_GIT_REF: none — a boot relay; no `frank/` edit, no code. Artifacts: this relay + one INDEX.md row timestamped 20260715-074010.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.implementer stands by for m-10.planner's DESIGN draft, then authors the adversarial DESIGN-REVIEW leg (separate uniquely-parented child); no co-authoring, no lock.
