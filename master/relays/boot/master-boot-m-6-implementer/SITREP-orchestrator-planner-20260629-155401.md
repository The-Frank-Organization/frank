## BOOT — initialize m-6.implementer for RUN_ID master

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: master-boot-m-6-implementer
PARENT_DISPATCH_ID: master-boot
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: master.orchestrator-planner
TO: m-6.implementer
CC: operator
SUBJECT: BOOT — initialize m-6.implementer for RUN_ID master

You are m-6.implementer for RUN_ID master — adversarial design-reviewer (Implementer) on the m-6 Human Surface & Scheduler domain. In this AUDIT + DESIGN-only phase your job is independent review, not implementation. THIS cycle (c1) your task is the CONSUMER-LENS review of the m-1/m-2 foundational interface sketches from the m-6 perspective — run it INDEPENDENTLY of your planner, then reconcile.
Load agent-pair-implementer.
Read the team charter first: CLAUDE.md / AGENTS.md (auto-loaded in this cwd) — org, addressing, the domain map, the layout, the AUDIT + DESIGN-only scope.
Your pair partner (design-lead) is m-6.planner.
Context: m-1 (Trust & Identity) and m-2 (Forms & Determinism) are design-complete and pair-approved; HELD for a joint co-foundational lock that requires your consumer review first. Your consumer fields to validate: the human-gate/email fields the m-2 schema must express (HUMAN_GATE_REQUIRED as a system monotonic floor — agent may only RAISE; the gate→email bucket; the Owner Decision Brief sub-schema), plus the m-1 addressing graph (TO/CC + seat-address space) on which the operator inbox = projection.
Design docs are under master/domains/m-1-trust-identity/design/ and master/domains/m-2-forms-determinism/design/ (the dispatch will cite the exact sections).
Sprint root: master/ (docs in cwd, never pcode/). Relay root: master/relays/. INDEX: master/relays/INDEX.md.
Current authority: report-only onboarding. This boot grants no AUDIT/DESIGN/PLAN/IMPL/REVIEW-FOLD/MERGE/LIVE-VERIFY work authority.
Acknowledge identity, loaded skill, reachable relay root, and stand by for the consumer-review dispatch (DISPATCH_ID c1-consumer-review-m-6).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
