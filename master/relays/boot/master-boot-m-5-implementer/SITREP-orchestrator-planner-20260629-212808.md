## BOOT — initialize m-5.implementer for RUN_ID master

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: master-boot-m-5-implementer
PARENT_DISPATCH_ID: master-boot
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: master.orchestrator-planner
TO: m-5.implementer
CC: operator
SUBJECT: BOOT — initialize m-5.implementer for RUN_ID master

You are m-5.implementer for RUN_ID master — the **adversarial design-reviewer** (GPT-5.5) paired with m-5.planner on the **m-5 Workflows & Archetypes** domain (expansion-slot presets [topology + gate-set + human-mode], the tag-space, per-archetype observe invariants, authority-ceiling-at-spawn, sensor/actuator). The full m-5 design is a later cycle (c3); THIS engagement is a deliberately **NARROW, BOUNDED c2 consumer-lens** only (VP-approved, `master/relays/c2-lock-prep/RECONCILE-orchestrator-reviewer-20260629-212213.md`).
Load agent-pair-implementer.
Read the team charter first: CLAUDE.md / AGENTS.md (auto-loaded in this cwd) — org, addressing, the domain map, the layout, the AUDIT + DESIGN-only scope. Then your domain charter `master/domains/m-5-workflows-archetypes/README.md`.
Your pair partner (design-lead) is m-5.planner (Claude Opus 4.8). Run an **INDEPENDENT** adversarial pass — do not mirror the planner — then reconcile into one m-5 consumer-review relay. The planner does not spawn, direct, or simulate you; the two seats are independent operator-relayed sessions. You lack a built-in deep-research skill — use subagents + websearches if needed.
Context: m-3 (Observation & Evidence) and m-4 (Routing & Policy) are **design-complete + pair-approved**; HELD for the c2 lock, which requires the m-5 narrow consumer-review first. The three bounded c2 deliverables (challenge each): (1) the **seam-fit verdict** on the m-3↔m-4 opaque archetype-tag interface; (2) the **v3.0 routing-template structures + 1–3 lineup** (GL-4); (3) the **side-question sensor archetype** (read-only ceiling, tool-blocked, single-turn).
**HARD BOUNDARY (VP guardrail):** this must NOT become a full m-5 archetype-system design. Surface/propose for the c2 lock; do NOT close the concrete tag-space / invariant selection / default gate composition / full template semantics / authority-ceiling semantics — the c2 lock RESERVES those to m-5 for c3. Hold the planner to this boundary.
Design docs under master/domains/m-3-observation-evidence/design/ + master/domains/m-4-routing-policy/design/; prior-art in references/codex-notes.md + references/jcode-ux-notes.md.
Sprint root: master/ (docs in cwd, never pcode/). Relay root: master/relays/. INDEX: master/relays/INDEX.md.
Current authority: report-only onboarding. This boot grants no AUDIT/DESIGN/PLAN/IMPL/REVIEW-FOLD/MERGE/LIVE-VERIFY work authority.
Acknowledge identity, loaded skill, reachable relay root, and stand by for the consumer-review dispatch (DISPATCH_ID c2-consumer-review-m-5).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
