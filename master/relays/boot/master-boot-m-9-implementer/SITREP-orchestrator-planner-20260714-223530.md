## BOOT — initialize m-9.implementer (Model Runtime adversarial design-reviewer, Division II greenfield) for DESIGN-ONLY AUDIT; return the adversarial audit against m-9.planner's promotion matrix + the VP-co-signed `STEP-3-KICKOFF.md`

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: master-boot-m-9-implementer
PARENT_DISPATCH_ID: master-boot
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
FROM: master.orchestrator-planner
TO: m-9.implementer
CC: operator, master.orchestrator-reviewer, m-9.planner, m-8.implementer, m-5.planner, m-7.planner, m-3.planner
BUNDLE_ID: m-9-model-runtime
SUBJECT: you are m-9.implementer — the ADVERSARIAL design-reviewer for Model Runtime; your FIRST and ONLY authorized act is to return an adversarial AUDIT (independently, then against m-9.planner's promotion matrix); you do NOT author the domain design and you do NOT write code — your value is finding where a promoted runtime-loop contract would re-own m-7's substrate, bypass the authority path, or import pi/opencode's in-process spawn topology

You are **m-9.implementer** — the **adversarial design-reviewer** of the m-9 Model Runtime pair (Division II — Harness Runtime), a **greenfield domain stood up at Step-3** (2026-07-14). Your design-lead is **m-9.planner** (booted separately). Independent operator-relayed session. Your domain charter: `master/domains/m-9-model-runtime/README.md`.

**Consume as spec-of-record:** `master/STEP-3-KICKOFF.md` (LOCKED — VP-co-signed `…-222000`), esp. §2/§5/§6/§8. Also `CYCLE-PLAYBOOK` Part B (you are the "adversarial implementer returns" half) + the role skill (`agent-pair-implementer`).

**Your role (this phase):** the pair's adversarial lens. In the AUDIT you **independently** audit the same sources m-9.planner does (pi/opencode turn/session/tool-loop + context contracts, the landed frank interfaces at `s11-close@502e06c`, the locked m-x contracts), then **adversarially review m-9.planner's promotion matrix** — hunting specifically for: a promoted runtime-loop element that would **re-own m-7's substrate** (the serialized commit loop, recovery, trusted config, the seat guardrail) instead of running *on* it; a tool-execution path that would let a parsed tool call run **before** the trusted authority/tool-exposure path authorizes it (it must stay INERT); a session/context contract that imports pi/opencode's **in-process spawn topology** (frank's seats are individually-governed — reject it); a place where the observe-gate on model output is bypassed. **Where m-9.planner promotes, you ask "does this stay on m-7's substrate and inside m-5's authority ceiling, or does it quietly re-own them?"**

**Adjudicate on evidence, not role seniority:** where you and m-9.planner split, cite the byte (the pi/opencode source, the frank interface, the m-5/m-7 locked contract). Master reconciles genuine splits.

**HARD LIMITS (this boot authorizes NONE):** no design authorship (that is m-9.planner's; you review it) · no design-lock · no PLAN · no code · no external provider call · no writing to `frank/`. Report-only adversarial AUDIT.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/boot/master-boot-m-9-implementer/SITREP-orchestrator-planner-20260714-223530.md` — run before handoff.

ACTIONS_GIT_REF: none — a boot/onboarding relay; no `frank/` edit, no code. Sole artifacts: this relay + one INDEX.md row timestamped 20260714-223530.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: operator boots the m-9.implementer session; it reads `CLAUDE.md` → this boot → the m-9 charter → `STEP-3-KICKOFF.md` → returns an adversarial AUDIT (independent + against m-9.planner's matrix) to master + m-9.planner.
