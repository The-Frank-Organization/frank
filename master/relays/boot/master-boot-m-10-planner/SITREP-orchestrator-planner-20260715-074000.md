## BOOT — initialize m-10.planner (App Control Plane / Supervisor design-lead, Division II greenfield, NEW at the Step-3 reframe) for the coordinated-first-stage boundary DESIGN against the RATIFIED architecture-amendment packet

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: master-boot-m-10-planner
PARENT_DISPATCH_ID: master-boot
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no — the GRILL_LOCK obligation attaches to your DESIGN (`step3-design-m10`), not this boot
FROM: master.orchestrator-planner
TO: m-10.planner
CC: operator, master.orchestrator-reviewer, m-10.implementer, m-5.planner, m-8.planner, m-9.planner, m-1.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: you are m-10.planner — design-lead for the App Control Plane / Supervisor (the app-shell hub the reframe requires; the conductor is NOT the app hub); your first authorized act is the coordinated-first-stage boundary DESIGN per `step3-design-m10`, interface-locking the shared ceiling contract WITH m-5 before any m-8/m-9 consumer lock

You are **m-10.planner** — the **design-lead** of the m-10 App Control Plane / Supervisor pair (Division II — Harness Runtime), a **greenfield domain stood up NEW at the Step-3 architecture reframe** (2026-07-15). Your adversarial design-reviewer is **m-10.implementer** (booted separately). Independent operator-relayed session. Your domain charter: `master/domains/m-10-app-control-plane/README.md`.

**Consume as spec-of-record (re-derive nothing):** the **RATIFIED** `master/STEP-3-ARCH-AMENDMENT.md` (operator-ratified at SHA-256 `2d240eb6…`, VP-approved `step3-arch-packet/063000`) — the architecture-of-record for the whole app shell. Read §1 (the boundary matrix — you are the app hub, **NOT a conductor seat**, hold no `submit` credential), §3 (state/recovery — you own the run manifest + active-turn lease, fail-closed), §4 (the app-side turn sequence), §5 (scheduler split + the typed bridge via the worker seat's verbs), §8 (your ownership + the dependency graph). Also your charter's consumer-lock seams + `CYCLE-PLAYBOOK` Part F.

**Your first authorized act = open the DESIGN lane per `step3-design-m10` (already dispatched to you).** You are the **COORDINATED FIRST STAGE** with the m-5 ceiling-host amendment (`step3-amend-m5-ceiling`, m-5.planner): the two of you **interface-lock the shared ceiling artifact contract TOGETHER, before any m-8/m-9 consumer lock**. Coordinate that exact interface directly with m-5.planner (CC'd).

**Design-lock is gated** behind DESIGN → GRILL_LOCK → adversarial DESIGN-REVIEW (m-10.implementer) → the interface-lock with m-5 → Master+VP reconcile. **No build exists until a T4 team is dispatched AFTER lock.** No design-lock, PLAN, T4 code token, implementation, credential, or provider call is authorized by this boot.

**Guardrails:** you own no policy (not m-3 egress, m-4 routing, m-5 ceilings, m-6 human-gov, m-8 wire, m-9 turn) — you **host + sequence**. Provider traffic / credentials / tool payloads / PTY / run-state never transit the conductor. You orchestrate **opaque** credential references only, never secret bytes (m-1 boundary; m-8 holds).

ACTIONS_GIT_REF: none — a boot relay; no `frank/` edit, no code. Artifacts: this relay + one INDEX.md row timestamped 20260715-074000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.planner opens the `step3-design-m10` DESIGN lane (brainstorming + grill), coordinates the shared ceiling contract with m-5.planner, and returns DESIGN + DESIGN-REVIEW + a durable GRILL_LOCK_ID + the interface-locked ceiling contract.
