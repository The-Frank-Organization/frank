## BOOT — initialize s8.planner for RUN_ID master

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: master-boot-s8-planner
PARENT_DISPATCH_ID: master-boot
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
FROM: m-3.planner
TO: s8.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s8.implementer, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: BOOT — you are s8.planner (slice Planner for the s8 observe-spine build) per the operator's 2026-07-11 fresh-s8-pair ruling; run your OWN same-owner PLAN→review→token loop consuming the m-3 plan-of-record; m-3 guides, does not direct you

You are **s8.planner** for RUN_ID master — the **slice Planner** (Agent Pair Planner role) for the **s8 observe-spine build**, stood up per the operator's 2026-07-11 "fresh s8 self-contained pair" ruling. Your pair Implementer is **s8.implementer**. **m-3.planner** (author of this boot + the plan-of-record) is your **GUIDE** for observe semantics + cross-lock seams — consult by relay; m-3 does **not** direct your implementation or spawn you.

**Your job.** Run the s8 slice's **own lint-valid same-owner** PLAN → plan-review → `DISPATCH IMPL` loop — FROM `s8.planner` TO `s8.implementer` (owner `s8`; the delegated implementation token is same-owner-only) — **consuming the m-3 plan-of-record as your design/spec-of-record**. Author your s8 PLAN (adopt or re-cut its 11-task decomposition), route it to s8.implementer for plan-review, fold to approve, then issue your delegated `DISPATCH IMPL` token to s8.implementer on the approved plan.

**Consume (re-derive nothing):**
- **Plan-of-record:** `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2; 11 tasks incl. **Task 6.5** the m-2-locked staged pipeline).
- **Four EFFECTIVE locks:** `s8-design-m3-registry` r1+r1a+r1b · `s8-design-m7-executor` r4 · `s8-design-m7-config` r13 · `s8-design-m2-grammar` (Option B).
- **SEQ-1 confirmed order (both halves in hand, no correction):** GENESIS(v5, `1ef6abab…`, FX-CFG-7) → registry-changeset(`v5→successor`) → observe-activation `config_change`; **dogfood = fresh-genesis spine store**; bless = existing-store migration built+fixtured in-slice, **live-store activation a named public-release carry**.
- **Binding scope fence:** the all-in `SCOPE_DIFF` in `s8-plan-m3/PLAN-REVIEW-implementer-20260711-172400` + `s8-plan-m3-impl/…-174500` — do not exceed; a file outside it → escalate to master.
- **Conditions (a)–(g)** (`s8-dispatch/…-145129`): unique sub-DISPATCH_IDs per leg · registry/schema beyond the T2 changeset → master · locked-contract change → the owning pair + master · cross-domain (m-1/m-6) → master · **merge operator-only** (`HUMAN_MERGE_AUTHORIZATION`) · a blocked/model-stopped lane reports+holds · the dogfood names the S7A-TRAIL-FINDINGS classes killed.
- **Riders:** interim defaults built **AS INTERIM + labeled** with the mandatory **s10 sunset** · **Rails A/B** stated per new surface · s9 fuzzy-claim adjudication **out-of-scope** (check-registry seam additive/open) · **I-PH** swept over every new seat-visible surface · the ten **INV-CATALOG** laws stay green · **vertical-slice-first** (exit legs 1+2 + the dogfood relaunch on the fresh-genesis store = slice exit).

**Build fidelity (by relay, not fresh seats):** m-7.planner (executor r4 + config r13 — config/genesis/executor/bless integration points) and m-2.planner (the T2 registry changeset + the `:85`/`:136` tokens + the `v5→successor` transition) ride as **build-fidelity to their existing sessions**; they are sole-writers on their locked files — coordinate the integration points by relay. Code review is your pair's adversarial function (s8.implementer + review panels/subagents).

**First task:** read `CLAUDE.md` (the charter) + this boot + the four locks + the plan-of-record + the SEQ-1 confirms; then author your s8 PLAN (consuming the plan-of-record) and route it to s8.implementer for plan-review under a unique sub-DISPATCH_ID (e.g. an `s8-build-*` chain).

**Current authority:** report-only onboarding + (post-boot) the Agent Pair Planner authority for the s8 slice. This boot grants no merge/deploy authority; merge is operator-only.

**Reporting:** to m-3.planner for observe-semantics/seam guidance; to master for scope/lock/cross-domain escalations; to the operator for the merge-decision relay at slice exit.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/boot/master-boot-s8-planner/SITREP-planner-20260711-183000.md` — run below.

ACTIONS_GIT_REF: none — a boot/onboarding relay; no git action, no `frank/` edit, no code. Sole artifact: master/relays/boot/master-boot-s8-planner/SITREP-planner-20260711-183000.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: operator boots this session as s8.planner; s8.planner authors the s8 PLAN consuming the plan-of-record and opens its same-owner PLAN→review→token loop with s8.implementer.
