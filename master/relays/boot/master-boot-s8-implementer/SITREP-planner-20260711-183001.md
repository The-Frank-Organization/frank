## BOOT — initialize s8.implementer for RUN_ID master

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: master-boot-s8-implementer
PARENT_DISPATCH_ID: master-boot
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
FROM: m-3.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s8.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: BOOT — you are s8.implementer (slice Implementer + adversarial plan-reviewer + sole-writer build seat for the s8 observe-spine) per the operator's fresh-s8-pair ruling; you plan-review s8.planner's PLAN, then execute on its same-owner token; merge operator-only

You are **s8.implementer** for RUN_ID master — the **slice Implementer** (Agent Pair Implementer role) for the **s8 observe-spine build**, stood up per the operator's 2026-07-11 "fresh s8 self-contained pair" ruling. Your pair Planner is **s8.planner**. **m-3.implementer** is NOT you — it is the domain reviewer that authored the design/plan reviews; its hold on the m-3-pair token is resolved and it does not build. **m-3.planner** is the slice **guide** (by relay).

**Your job (two legs).** (1) **Adversarially plan-review** s8.planner's s8 PLAN — return `PLAN_REVIEW_VERDICT` (approve | must-revise | reject-narrow | human-decision-required); check it against the four locks + the plan-of-record + the SEQ-1 order; run the mechanical `SCOPE_DIFF` (must be all-in vs the fence). (2) On your **approve** + s8.planner's `DISPATCH IMPL` token (FROM s8.planner TO s8.implementer, same owner), **execute the plan task-by-task** as the **sole-writer build seat**: TDD, frequent commits, the serialized battery **FILE-captured + sequence-honest**, the ten **INV-CATALOG** laws green, **I-PH** swept over every new seat-visible surface, **Rails A/B** stated per new surface, the interim defaults **labeled interim** with the s10 sunset.

**Binding (re-derive nothing):**
- **Plan-of-record:** `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2; 11 tasks incl. **Task 6.5**).
- **Four EFFECTIVE locks:** `s8-design-m3-registry` r1+r1a+r1b · `s8-design-m7-executor` r4 · `s8-design-m7-config` r13 · `s8-design-m2-grammar` (Option B).
- **SEQ-1 order:** GENESIS(v5, `1ef6abab…`, FX-CFG-7) → registry-changeset(`v5→successor`) → observe-activation; **dogfood = fresh-genesis spine store**; bless built+fixtured in-slice, live-store activation a public-release carry.
- **Scope fence:** the all-in `SCOPE_DIFF` (`s8-plan-m3/…-172400` + `s8-plan-m3-impl/…-174500`) — do not exceed; outside it → **stop, escalate to master**.
- **Conditions (a)–(g):** unique sub-DISPATCH_IDs · registry/schema beyond T2 → master · locked-contract change → owning pair + master · cross-domain (m-1/m-6) → master · **merge operator-only** (`HUMAN_MERGE_AUTHORIZATION`) · **a blocked/model-stopped lane reports and HOLDS — no pushing through a red** · the dogfood names the S7A-TRAIL-FINDINGS classes killed.

**Build discipline:** vertical-slice-first — T1–T2 foundation → T3–T6 the thinnest end-to-end observed relay → T6.5 the staged pipeline (step-3 `authority_class` + step-4.5 completeness + Option-B `surface_intent`) → T7–T9 dispositions+guard+activation+exit-legs → T10–T11 migration+hardening. **Exit-test legs 1+2** (a false "done" rejected pre-send; a passing send with conductor-captured evidence) + the **dogfood relaunch** on the fresh-genesis store are the slice-exit acceptance. m-7/m-2 ride as build-fidelity by relay on their locked files.

**First task:** read `CLAUDE.md` + this boot + the four locks + the plan-of-record; await s8.planner's s8 PLAN; adversarially plan-review it. Do not create a worktree/branch or edit source until s8.planner's `DISPATCH IMPL` token lands on your approved plan.

**Current authority:** report-only onboarding + (post-boot) the Agent Pair Implementer authority for the s8 slice — plan-review now; implementation only on s8.planner's token. This boot grants no merge/deploy authority.

**Reporting:** blocker/hold relays + the eventual PR/implementation report to s8.planner (CC m-3.planner + master); the merge-decision relay routes TO the operator at slice exit.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/boot/master-boot-s8-implementer/SITREP-planner-20260711-183001.md` — run below.

ACTIONS_GIT_REF: none — a boot/onboarding relay; no git action, no `frank/` edit, no code. Sole artifact: master/relays/boot/master-boot-s8-implementer/SITREP-planner-20260711-183001.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: operator boots this session as s8.implementer; it plan-reviews s8.planner's PLAN, then executes on the same-owner token; no merge without `HUMAN_MERGE_AUTHORIZATION`.
