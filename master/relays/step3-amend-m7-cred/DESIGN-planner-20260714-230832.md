## DESIGN — the Step-3 m-7 credential/trusted-config owner amendment **r1** is authored (`2026-07-15-step3-credential-contract.md` + `GRILL_LOCK_ID: step3-amend-m7-cred-grill-r1` folded): the load-bearing decision is the **governed-BINDING / out-of-store-MATERIAL split** — an engine-v5 `provider_bindings` section (zero secret bytes, zero secret digests) riding the existing version machinery, with material at `<root>/secrets/<id>` (0600) loaded phase-0.5 into an opaque type that only the host's wire-attach can open — because §7 `config_change` bodies enter the append-only store, so any credential-in-member design writes secrets into permanent history ON EVERY ROTATION; the split makes the violation UNREPRESENTABLE. Your adversarial DESIGN-REVIEW requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m7-cred
PARENT_DISPATCH_ID: step3-audit-reconcile
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the design routes (not resolves) the operator provisioning decision per the cue; no credential action, no E3 call, no lock claimed
GRILL_REQUIRED: yes
DESIGN_DOC_ID: step3-amend-m7-cred-credential-contract
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/step3-amend-m7-cred/DESIGN-orchestrator-planner-20260715-005530.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-8.planner, m-3.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the doc is at `master/domains/m-7-conductor-core/design/2026-07-15-step3-credential-contract.md` — §1 pins the claim ceiling FIRST (confusion-resistance; the D5 same-uid residual stated; no OS-secrecy claim); the grill's four inferable items are resolved from the bytes with sources recorded (§11), and the ONE operator-owned item (provisioning + first-E3-go) is ROUTED as a prepared HUMAN_GATE decision surface, not resolved — exactly what the cue ordered; three review dimensions I flag for your adversarial pass below

**The shape in six sentences (full text in the doc):**
1. **§2 the split:** binding = engine **v5** `provider_bindings` rows (`credential_id`/`provider_id`/`endpoint_allowlist`/`secret_source`-ref/`generation`) — governed, digest-chained, §7-transitioned, no member-set change so no bless machinery; material = `<root>/secrets/<credential_id>` (0600, symlink-refused, operator-provisioned), outside the digest chain, loaded once at a new **phase 0.5** (post-digest, pre-serve), absent/malformed ⇒ typed `credential-material-unavailable`, serve never starts.
2. **§4 endpoint authority runs one direction:** the LANE names the endpoint, the credential can only AUTHORIZE it — never supply it (the m-8 audit's F-2: pi's stored credential silently replaces the model endpoint; the fusion this kills); the sender requires four-way agreement (lane.provider == binding.provider ∧ endpoint ∈ allowlist ∧ egress-authorized ∧ ref matches) else typed refusal, zero network send.
3. **§5 rotation:** material swap + restart moves ZERO governed bytes (no store/genesis break by construction); binding changes are ordinary §7 v5 edits; optional generation-bump provenance.
4. **§6 redaction is three independent layers:** the opaque `Secret` type (every serialization path yields `[credential:<id>]`; raw bytes reachable only by the host's wire-attach), attach-AFTER-final-authorization (the scanned/captured envelope structurally cannot contain the value — my hosted half of the §1(a) no-post-authorization-mutation rule, the attach being the sole named exception, reconciled with m-3's amendment), and the planted-canary census law extending the s7 path-hygiene family.
5. **§7 the operator decision surface:** which providers/accounts · the provisioning acts · binding ratifications · the first-E3-use go — routed as a HUMAN_GATE artifact gating the V1/V2 E3 legs, untouched by this design's close.
6. **§9/§10:** the kickoff §1(c) negatives as six named fail-closed legs (N1–N6) + eight fixture families (FX-CRED-1..8, incl. the joint m-3 ordering fixture).

**Where I want your adversarial pressure specifically (not exhaustive — your lens, your findings):**
- **The phase-0.5 insertion** — a new load phase in the trusted path is the kind of machinery my own r13 grilled others for; is the placement (post-digest, pre-serve) actually forced, and are its failure semantics complete (partial-load, multi-credential, one-bad-one-good)?
- **The `Secret` type's boundary honesty** — "unexported + package boundary" is a Go-convention wall, not a capability wall; §1 states the D5 ceiling, but check whether any §6 sentence quietly claims more than the type system delivers.
- **The §6.2 exception's byte-grain** — "the host's auth-attach is the sole post-authorization mutation" must reconcile with m-3's final-wire-vs-pre/post grill BEFORE close; if their grill lands final-wire-only, my attach placement IS the final wire and the wording must not imply two authorities.

Consumer-packet status (B14): m-8/m-9 DESIGNs are authoring in parallel; §12 names the four open items that block CLOSE but not your review. Per the cue, on your approve I report amendment-design-complete to master (CC the VP) carrying the GRILL_LOCK_ID; no close/lock until master+VP reconcile all three amendments.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` parenting to this relay.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-amend-m7-cred/DESIGN-planner-20260714-230832.md` — run before handoff (result in inline pointer)
- grounded this session: `STEP-3-KICKOFF.md` read in full (§1(b), §4 no-secrets-in-catalog, §6.4, the B14 amendment) · the m-3 sibling cue read (the ordering/exception seam) · `step3-audit-m-8/AUDIT-planner-…-224500` (F-2/F-4 + the consumer matrix) · `frank/internal/egress/rules.go:15-50` · the r13 config machinery + §7/bless sections re-read at the s8 doc · `frank/` main at `502e06c` (s11-close)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the design doc + this relay + one INDEX.md row timestamped 20260714-230832)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`)
Next requested action: operator carries this to the m-7.implementer session for the adversarial DESIGN-REVIEW; in parallel the doc is readable by m-1/m-8/m-3 from CC for their seam reviews; on approve I report amendment-design-complete to master carrying `step3-amend-m7-cred-grill-r1`.
