## DESIGN — credential-contract **r2**: F1–F6 folded whole, each at the mechanism grain. The two that reshaped the design: **F2 — request-chosen `credential_ref` is REMOVED FROM EXISTENCE** (no adapter/runtime/request credential field remains; the sender DERIVES the unique binding from governed state — provider match + optional `lane_ids` scope — with zero-match and two-match both typed refusals, so the two-account wrong-credential path is closed by derivation, not by a tautological ref==ref check); and **F1 — "phase 0.5" is now an executable protocol** (post-`RunWithProcessor`, pre-sender: all-or-nothing temp-set load → single immutable-vault publication → only then sender construction — no-sends-during-replay is STRUCTURAL because the sender is greenfield and does not exist earlier). Your re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m7-cred
PARENT_DISPATCH_ID: step3-audit-reconcile
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the operator provisioning/first-E3 gates stay routed, untouched by the folds
GRILL_REQUIRED: yes
DESIGN_DOC_ID: step3-amend-m7-cred-credential-contract
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: step3-amend-m7-cred-grill-r2
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/step3-amend-m7-cred/DESIGN-REVIEW-implementer-20260714-231621.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-8.planner, m-3.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: all six conceded on their own reasoning — F2's is the one I name against myself on the trail: I designed the §4a governed-derivation discipline for OTHERS twice this window (lane_vcs "capability cannot be inferred, it must be declared"; the s9 ruling "the fact gates the branch") and then let MY OWN contract's most security-relevant selection ride an ungovemed request field; the review's "checking that the ref equals the binding selected by that same ref is tautological" is exactly right and the field no longer exists. The grill lock is superseded r1→r2 (three resolutions ADDED, none reversed)

**The folds, finding by finding (doc = `2026-07-15-step3-credential-contract.md` at r2):**

1. **F1 → §2.3.** Pinned to the real bytes: existing hosts build at `main.go:137-164`, recovery (`RunWithProcessor`, which owns `ValidateGenesis`) at `:259` — so the protocol is a **startup insertion in the Ready branch, not a recovery hook, and no existing host ever receives credentials**. All-or-nothing: temp set → complete-set validation → publish exactly once (immutable vault) → sender constructed from the published vault → serve. One-bad-among-N ⇒ no partial set, no sender, no socket, no serve, one path-free typed disposition (a declared-but-unhonorable binding = trusted-config integrity failure, phase-0 posture). **Zero bindings is lawful** — pre-Step-3 stores serve untouched, provider sends refuse typed. The three requested fixtures are FX-CRED-1.
2. **F2 → §4a + §2.1.** The request-side ref is deleted; selection = governed derivation (unique row by `provider_id` + optional `lane_ids`); ambiguity and absence both refuse typed; the single-writer statement names m-7's binding rows keyed by m-8's lane identity; `secret_source.id` removed (the file name IS `credential_id`); the auth-attachment shape is the binding's closed-enum `auth_profile`, never adapter headers; the two-account/same-provider/same-endpoint negative is FX-CRED-9.
3. **F3 → §3.** Descriptor-relative `openat(O_NOFOLLOW)` under the already-open root; all checks (`fstat`: regular · 0600 · owner-uid · size ≤4096; parent 0700 — the pair flagged for m-1's ruling) on the **open descriptor**, killing resolve-then-open; the byte grammar exact (≤1 terminal LF/CRLF stripped; then empty/CR/LF/NUL/out-of-profile-charset all reject — header injection dies at load); nine FX-CRED-2 legs incl. swap-during-load.
4. **F4 → §4.** Structured rules (https-only · lowercase-ASCII host · explicit/default-normalized port · no userinfo · segment-boundary path prefix · rules never carry query/fragment); **canonical-form-only URLs** (dot-segments and encoded-slash reject outright); the **final-wire freeze** before authorization with the attach as the sole post-freeze change; **automatic redirects DISABLED in v1** (redirect-with-credential is unrepresentable, a 3xx is a terminal m-8 event); **every retry attempt re-frozen + re-authorized + re-attached** (policy = the m-9/m-3 packet; the per-attempt-authorization invariant = mine). Five new FX-CRED-3 legs.
5. **F5 → §6 + §1.** The package and capability API are named: `internal/provider/vault`, immutable post-publication, **no raw-byte accessor exists; the sole capability is `Vault.Attach(req, credentialID)`**, called from one site. Safe representations are ENUMERATED AND TESTED (fmt verbs · json · TextMarshaler/BinaryMarshaler asserted-absent · structured logging), and §1's claim is narrowed to exactly that: accidental flow through the enumerated interfaces is confusion-resistant; **deliberate in-package misuse/reflection is review-visible and canary-detectable, not type-impossible** — the same-uid/same-code residual stated at the ceiling, not buried. The post-attach request object is never logged/captured/evidenced (named rule + census).
6. **F6 → §6.4 + §5.** The visibility matrix (8 items × 9 surfaces) — material and concrete paths forbidden everywhere outside vault/wire; `credential_id` lawful in config/history/diagnostics/the redaction token and nowhere near the wire or seats; **whole binding rows lawful exactly in member bytes + §7 history and NEVER in projections/seat/evidence** (N6 restated at that grain); the canary sweeps raw+base64+hex with the reference token approved; `generation` demoted to an operator assertion with gate-enforced monotonicity and **no rotation-proof claim**.

**Unchanged:** the §2 split, root_file-only v1, restart-effective rotation, attach-after-authorization, the §7 routed operator decision, the B14 posture (§12 — your Consumer-packet boundary section is adopted verbatim: an intermediate technical re-review may confirm these folds; final approval waits for the live m-8/m-9 packet + m-1/m-3 confirmations in a new final-review relay).

Return `DESIGN_REVIEW_VERDICT` parenting to this relay — per your own boundary, an approve here is the **technical fold-confirmation**, not the final amendment approval.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-amend-m7-cred/DESIGN-planner-20260714-232405.md` — run before handoff (result in inline pointer)
- F1's ground re-verified before folding: `cmd/frank/main.go:137-169` + `:259` (`RunWithProcessor` call) and `internal/recover/recover.go:29-41` (`ValidateGenesis` inside recovery) read this session; the review read in full; the r2 doc self-checked against each Required-fold clause line by line

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r2 design doc + this relay + one INDEX.md row timestamped 20260714-232405)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`)
Next requested action: operator carries this to the m-7.implementer session for the technical re-review of the folds; on its approve I hold the amendment at DRAFT-complete and report to master (CC the VP) with `step3-amend-m7-cred-grill-r2`, pending the B14 consumer packet for the final-review leg.
