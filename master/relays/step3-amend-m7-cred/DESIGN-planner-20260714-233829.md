## DESIGN — credential-contract **r3**: F7–F10 folded whole. The census finding forced the honest mechanism: `secrets` is NOT a thirteenth exact-live-equality row (a conditional directory cannot inhabit an exact-equality family) — it is a **catalog `s8-v1`→`s8-v2` FORBIDDEN-ONLY entry** with staged never-red activation (catalog s8-v2 ∥ engine v5, either order, both singular §7 records, genesis untouched). The r2 free-string `Attach(req, credentialID)` is REMOVED for the same reason r2 removed the request ref — it reintroduced in-host substitution one boundary later — replaced by **`Derive → Selected (opaque, freeze-bound) → Attach`**. One r2 claim RETRACTED by name: "traversal unrepresentable in the name" was false at exactly `.` and `..`. Your technical re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m7-cred
PARENT_DISPATCH_ID: step3-audit-reconcile
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the operator provisioning/first-E3 gates stay routed, untouched
GRILL_REQUIRED: yes
DESIGN_DOC_ID: step3-amend-m7-cred-credential-contract
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: step3-amend-m7-cred-grill-r3
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/step3-amend-m7-cred/DESIGN-REVIEW-implementer-20260714-232820.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-8.planner, m-3.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the F9 concession names the pattern for the ledger — this is the SECOND time in one amendment that a selection authority I evicted from an outer boundary crept back one layer in (r1: the request chose the credential; r2: the host call site could); the fix each time is the same discipline at a finer grain (make the choice a governed derivation and make its result an unforgeable value, so substitution has no representable form) — and F8's dot-ID catch corrects an actual false claim of mine, retracted on the trail

**The folds (doc at r3, `2026-07-15-step3-credential-contract.md`):**

1. **F7 → §2.5.** The r13 twelve-row exact-live-equality census is UNTOUCHED — `secrets` enters as a **`forbidden_only` entry** (`{id: "secrets", forbidden: "/secrets/"}`) in a **catalog `s8-v2`** schema step riding the existing catalog machinery (v2 descriptor arm + the `s8-v1`→`s8-v2` adjacent marker hop + the §5.2 singular arm + the §4 drift law's same-changeset source-artifact rule). The leak-scan law consumes rows ∪ forbidden_only. **Staged, never-red activation:** catalog s8-v2 and engine v5 are independent singular §7 records in either order — the forbidden-only law asserts bytes-in-outputs, never live state, so no intermediate reds and no atomic two-member machinery is introduced. The directory is **operator-created at provisioning** (0700); genesis untouched; zero-binding and no-genesis-break claims survive verbatim.
2. **F8 → §3.** Component-wise descriptor confinement per the landed `fs_worker.go` posture (and explicitly NOT leaning on the flock, which locks `conductor.lock`, not a directory): root dir FD → `openat(rootfd, "secrets", O_DIRECTORY|O_NOFOLLOW|O_CLOEXEC)` + fstat the DIR descriptor → `openat(dirfd, id, O_RDONLY|O_NOFOLLOW|O_NONBLOCK|O_CLOEXEC)` + fstat the leaf (regular-file reject reachable because the FIFO can no longer block the open) → **`.`/`..` explicitly rejected** (the r2 charset claim retracted — it admitted both) → **bounded 4097-byte descriptor read** (growth after fstat cannot unbound the read; st_size is advisory). Four new FX-CRED-2 legs (parent-symlink · dot-ID · FIFO-no-writer · grow-after-stat).
3. **F9 → §4a.6 + §6.1 + §6.1a.** `Derive(lane, frozenEndpoint) → Selected` (opaque, package-constructed, **freeze-bound** — it refuses attachment to any bytes other than the freeze it was derived for) and `Attach(req, Selected)`; the free-string parameter no longer exists. **§6.1a pins the exact wire bytes**: `bearer` ⇒ `Authorization: Bearer <material>`; `x_api_key` ⇒ `X-Api-Key: <material>`; reserved set `{Authorization, X-Api-Key, Proxy-Authorization}` matched case-insensitively, **any pre-freeze occurrence refuses typed — never overwrite, cross-profile identical**; attach adds exactly one header; **`Request.Host` empty-or-byte-equal to the frozen authority**. Five new negatives (N11/N12 + FX-CRED-7 legs).
4. **F10 → §2.4.** The canonical engine-v5 descriptor delta + a nine-row composition-validation table, both-surface enforced (the lane_vcs posture): `provider_bindings` **optional, present-empty REJECTED** (zero bindings has one representation); closed row keys; `credential_id` unique + dot-rejected; non-empty deduplicated allowlist; closed enum profile; `lane_ids` absent≠empty; generation non-negative + gate-monotone; **rows sorted by credential_id** (the descriptor-rows precedent); typed timing per row. **Cross-member reference validation is the one explicitly-provisional row** (v1 default: selection-time refusal; upgradeable to composition-time when m-8's catalog load surface lands — §12.6, B14). Nine new FX-CRED-5 legs incl. lawful zero-binding v5. §12.5 is discharged: PLAN invents nothing.

**Unchanged:** everything your Confirmed section closed (F1–F6 grains), the §7 routed operator decision, the B14 posture — an approve here remains the technical fold-confirmation, not the final amendment approval.

Return `DESIGN_REVIEW_VERDICT` parenting to this relay.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-amend-m7-cred/DESIGN-planner-20260714-233829.md` — run before handoff (result in inline pointer)
- grounds re-verified before folding: `store/lock.go:43-53` (flock = `conductor.lock`, the F8 premise) · `observe/fs_worker.go:209-274` (the component-wise posture cited as the pattern) · the r13 census section (`2026-07-11-s8-config-host.md:65-90`, twelve closed rows) · `config.go:263-284` (the per-version transition gate F10 rides) · the review read in full; the r3 doc self-checked clause-by-clause against each Required fold

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r3 design doc + this relay + one INDEX.md row timestamped 20260714-233829)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`)
Next requested action: operator carries this to the m-7.implementer session for the technical re-review; on its approve I report DRAFT-complete to master (CC the VP) carrying `step3-amend-m7-cred-grill-r3`, with the final-review leg held for the B14 consumer packet.
