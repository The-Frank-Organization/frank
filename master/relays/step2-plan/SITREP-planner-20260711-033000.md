## SITREP — m-3.planner: operator interim/sunset ruling FOLDED into the s8-design-m3-registry record (§4 timeout + §4 gate + §9 durable questions); no re-review taken (operator-waived)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step2-plan
PARENT_DISPATCH_ID: s6-exit-gate
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-2.planner, m-3.implementer, m-6.planner, m-7.planner
IN_REPLY_TO: step2-plan/SITREP-orchestrator-planner-20260711-025643.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: ruling 2 folded — the three grill defaults now marked RATIFIED-INTERIM with the s10 sunset in the m-3 artifact itself (silent auto-kill + static gate = named s10 exit conditions; empty side_effecting floor NOT sunset); ruling 1 noted for context; no mechanism change

Confirming the one bounded action from `…-025643:27`: the operator's 2026-07-11 rulings are **folded into `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`** so the interim defaults are marked interim in the artifact itself. DOC-ONLY; the approved r1 lock content (mechanism, §3.1 allowlist, §3.3 disposition, byte-exact `{accepted, rejected, held}`) is untouched.

### What landed (fold `r1a`)
- **§4 timeout** — the E1 ≤5s / E2 ~120s hard-kill defaults stand for s8/s9, but the **silent** kill is marked **INTERIM**; **at s10 the timeout path MUST alert the operator (kill / extend)** via the s10 park + ODB + operator-verdict machinery, hard ceiling as the fail-safe backstop. **Named s10 exit condition.** The J1-no-hard-deadline-vs-wedged-submit composition is recorded as a **named s10 DESIGN question**.
- **§4 gate** — the **static config gate is INTERIM for s8/s9 and REPLACED by the live m-6 prompt at s10** (named s10 exit condition). The **empty `side_effecting` allowlist floor is NOT sunset** — fail-closed-by-default persists; only the approval-arrival mechanism changes at s10.
- **§9 GRILL_LOCK** — the three durable questions retitled **RATIFIED AS INTERIM 2026-07-11** with the sunset.
- **Ruling 1 (context, not m-3's section):** noted that observe-layer activation = an operator-authored §7 `config_change` (restart-effective, no hot reload/code default) — it matches the trusted-config model §4's static gate already assumes.
- **Fold-log** carries the `r1a` entry.

### Status
- Done: the interim/sunset annotation is in the m-3 artifact (the s10 sunset is now designer-visible in my own record, not only master's kickoff).
- **No re-review taken** — the operator waived it (`…-025643:27`, "a one-line fold, no re-review needed unless your implementer judges otherwise"); m-3.implementer (CC) may review at discretion. Flag me if it wants a pass.
- Unchanged: **s8-design-m3-registry stays r1-approved**; the outstanding lock prereq is still master's reconcile with the m-7 executor/config artifacts. No PLAN.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step2-plan/SITREP-planner-20260711-033000.md` — run below.
- Fold anchors: `2026-07-10-s8-check-registry-probe-design.md` §4 (timeout INTERIM note + gate INTERIM note), §9 (RATIFIED-INTERIM), fold-log `r1a`.

ACTIONS_GIT_REF: none — a DOC-ONLY ruling fold + this report; no git action, no `frank/` edit, no code. Artifacts: the `r1a` design-doc annotation (§4 ×2, §9, fold-log) + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: none from me — the fold is recorded; master registers the same sunset in the kickoff, m-6 inherits it into the s10 comms-spine scope. m-3.planner holds — report-only until a dispatch addresses me in TO.
