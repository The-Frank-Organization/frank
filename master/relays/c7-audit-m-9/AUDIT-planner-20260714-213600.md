## AUDIT — m-9 Model Runtime audit/promotion matrix AUTHORED (kickoff §6 step 1); routed to m-9.implementer for the adversarial AUDIT return

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: c7-audit-m-9
PARENT_DISPATCH_ID: master-boot-m-9-planner
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: m-9.planner
TO: m-9.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-8.planner, m-5.planner, m-7.planner, m-3.planner
BUNDLE_ID: m-9-model-runtime
SUBJECT: the m-9 design-only AUDIT artifact is authored — `master/domains/m-9-model-runtime/audit/2026-07-14-model-runtime-promotion-matrix.md` — 30 dispositioned candidates (pi + opencode turn/session/tool-loop/compaction contracts × the landed frank interfaces × the locked m-x contracts), each PROMOTE / ADAPT-WITH-GOVERNANCE / REJECT with the governance seam named; your adversarial return is the next act

**Artifact of record:** `master/domains/m-9-model-runtime/audit/2026-07-14-model-runtime-promotion-matrix.md` (this relay is the routing cover; the matrix is the payload).

**What it contains, in one breath:** §1 duplicate/already-built gate — the m-9 surface is GREENFIELD at `frank@502e06c` (exhaustive sweep, zero turn-loop/provider/session/compaction code) while ten adjacent landed mechanisms are enumerated as consume-never-rebuild; §2 the promotion matrix across seven areas (A turn-loop, B session/context, C tool-call path, D steering/cancel, E error/retry/timeout, F extensibility-hook bypass surface, G spawn topology); §3 the protocol-form 4-bucket deliverable + risks/reject gates; §4 the seam obligations surfaced for the m-8 pair; §5 six questions carried to your return + DESIGN/GRILL (Q1 process placement and Q4 retry×final-wire-auth are the grill-candidate class).

**Load-bearing dispositions you should attack hardest:**
- C2/C4 (REJECT execute-on-parse + opt-in gating; replacement = inert-parse → governed request → fail-closed m-5 ceiling check → unprivileged executor; plus the m-8 no-execution-capability seam requirement).
- F1–F7 (REJECT the entire seat-reachable mutation-hook surface — pi `onPayload`/`getApiKey`/stream-options patch, opencode options-bag `provider.ts:1029-1054`, plugin middleware — per kickoff §1a/§1b/§1c).
- D1/C3/E2 (product-overlap calls: frank mailbox = the only follow-up queue; landed approval/held/park = the only ask machinery; wire retry/idempotency = m-8's, m-9 keeps only recorded turn-level disposition).
- A2/A5/B1/B3 (PROMOTE calls: coalescing one-turn-per-session lock; store-is-truth loop state; append-only history + compaction-as-recorded-entry; compaction mechanics with the governed re-shape — the summarizer turn is itself a governed send).
- The spawn-topology correction: pi's subagents are separate OS processes — the reject rationale is ANONYMOUS/UNGOVERNED spawn (no seat identity, no ceiling-at-spawn), not "in-process"; verify this correction survives your read (your boot's hunt list says "in-process spawn topology").

**Evidence honesty:** three read-only research lenses mined pi/opencode/frank; I spot-checked the load-bearing anchors myself (method note in the artifact). Rows tagged [E1-lens] were not independently re-opened by me — they are named re-verification targets for your return. Treat lens-derived rows as E0-until-you-verify per protocol.

**Your return (per your boot `master-boot-m-9-implementer/…-223530`):** independent audit + adversarial review of this matrix — hunt (a) promoted rows that smuggle a governance-seam crossing, (b) rejected rows that discard load-bearing function the design will secretly need back, (c) any row that re-owns m-7 substrate / m-5 ceiling / m-8 contract / m-3 observe property, (d) runtime-loop elements that could run a tool call before the authority path authorizes, (e) missing candidates (what pi/opencode contract did I not even list?). §3's risks block-list and §5's questions are yours to sharpen or overturn.

4-bucket verdict (full form in the artifact §3):
PRIMARY_BUCKET: still-open
still-open: the entire m-9 turn/session/context/tool-request runtime + authority_ceiling ENFORCEMENT (header slots landed, zero enforcement code).
already-closed: the ridden substrate — commit loop + intake writer, system-verb handler pattern, ServiceWhileBlocked + expiry, observe gate + Env.Evaluate, ApprovalPrompter, park/wake + PushTo, egress outbox, trusted config + digest, seat surface + FROM stamp (artifact §1 table).
product-overlapped: follow-up queue (frank mailbox) · ask subsystem (landed approval/held) · wire retry/cancel/finish vocab (m-8) · spawn (m-5 + Step-3 carry) · transport/credentials (m-7 §1b) · provider-request egress (m-3 §1a) · facts catalog (m-8).
recommended-next: your adversarial AUDIT return → pair reconcile → DESIGN (kickoff §6 step 2).

Boundary contract: in the artifact §3 (writes = the audit artifact only; consumers = your return, the reconciled matrix, the m-8 pair via §4, the three owner-amendment consumer reviews).

HARD LIMITS still standing (boot): no design-lock, no PLAN, no T4 code token, no implementation, no external provider call, no writing to frank/. This relay carries no dispatch token and no authority beyond the AUDIT phase.

RELAY_LINT: run before handoff (`python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/c7-audit-m-9/AUDIT-planner-20260714-213600.md`).

ACTIONS_GIT_REF: no frank/ edits; artifacts = the audit matrix file + this relay + one INDEX.md row (cwd docs workspace, not a git repo)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ untouched at 502e06c (s11-close)
Next requested action: operator relays this to the m-9.implementer session for the adversarial AUDIT return; m-8's audit runs concurrently; interface-locks + impl stay serialized per kickoff §2.
