## DESIGN — l4's weight re-adjudication from the CORRECTED sequence. **The `0/0` premise fails for `neg.WRONG_LEASE`, and the aggregate does NOT still compose: it becomes 31 governed turns / 100 tool calls, violating the ratified EXACTLY-30 invariant.** I verified this at the approved contract `a9ca1952…` rather than at master's characterization — which is precisely the check I failed to run before concurring the first time, and I own that below. **The finding that matters most is not the arithmetic: the weight depends entirely on which ACTOR the record is scoped to, and that same scoping choice decides whether m-9 has any observable in the fixture at all.** m-10 and m-3 cannot rule the weight without simultaneously ruling the actor scope, and §3 of the approved contract says so in terms. Both branches are computed below so whichever way they rule, the arithmetic is already on record. **Lane 4 stays HELD; this authors nothing.** This return is planner-authored and requires `l4.implementer` exact-byte review before it may enter r3.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: review-only
DISPATCH_ID: step3-relock-lane4-l4-weight-readjud-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-vp2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — this is one input to amendment r3 (Master+VP+operator). It concurs at design level only, composes no amendment, resumes no held member, and authors no record.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-vp2/DESIGN-orchestrator-planner-20260726-153807.md
FROM: l4.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-3.planner, m-3.implementer, m-1.planner, l4.implementer
SUBJECT: Weight re-adjudication — actor-B scoping yields 31/100 and breaks the invariant; actor-A-only yields 30/100 but closes m-9's join; the weight and the actor scope are ONE question, and no rebalance may weaken a scenario to preserve a number

## First, the correction to my own prior return — stated precisely, not generally

In `…-l4-concur-1` I concurred on the `0/0` accounting and reasoned: *"a refused admission produces neither a governed turn nor a tool call."*

**That principle is correct and survives unchanged.** What was wrong was my **classification** of `neg.WRONG_LEASE` as a refused admission. It is not one, on the approved contract.

The reason I got it wrong is worth naming exactly, because it is the same failure class this lane keeps catching: **I verified my own arithmetic at my own bytes — I parsed my filed weight table rather than restating it — but I took the *event classification* from master's prose description and never opened the counterparty's approved contract.** Verifying half the inputs at the bytes is not verifying at the bytes. Master's r2 had the actors reversed, and my concurrence inherited the reversal because I only checked the half I authored. I have read `a9ca1952…` directly for this return.

## What the approved contract actually says

`master/domains/m-9-model-runtime/design/2026-07-26-fencing-observable-onefile.md` @ `a9ca1952c87098e4…`, §3, is explicit that this is a **two-actor sequence with two distinct consequences on two distinct surfaces**:

| actor | admission outcome | work performed | weight consequence |
|---|---|---|---|
| **A** — disposed predecessor generation | m-10's admission gate rejects it **pre-assign**; **no `assign`, no `turn_open` is ever emitted to it**; "it never reaches the worker" | none | genuinely `{0, 0}` — my original premise holds here exactly |
| **B** — legitimate current replacement | it **is assigned + `turn_open`'d**, then blocks at `session.lock` because the still-live predecessor retains the open file description | "**no** recovery-read, **no** journal attach, **no** write, **no** provider call, **no** tool call, **no** conductor verb"; emits a bounded fault | a governed turn **was opened**; **zero** tool calls |

So on actor B a `turn_open` demonstrably occurs. B was **admitted**, not refused. `{0,0}` is not available for a record scoped to B without asserting that a turn which was opened was not opened.

## The finding that governs the rest: weight and actor scope are ONE question

§3's closing sentence is load-bearing and I do not think it should pass unremarked in the weight discussion:

> "If the final N.b fixture is instead defined to observe **only** actor A's pre-worker admission rejection, then there is **no m-9 observable in it** and m-9's join to that fixture closes."

That means the two live options are not "what weight do we assign" but "**what is this record for**":

- Scope it to **B** (retain the m-9 writer-fence sub-observation, which is the thing `neg.WRONG_LEASE` was created to carry) ⇒ a governed turn is consumed.
- Scope it to **A only** (a pure m-10 admission rejection) ⇒ `{0,0}` holds, but m-9 has no observable in the fixture and its join closes.

**m-10 and m-3 cannot rule the weight independently of that choice**, and the choice has a consequence — m-9's join closing — that sits outside the accounting frame entirely. I flag it rather than assume the scoping, because picking the weight first and letting the scope follow would decide an m-9 join by arithmetic convenience. That inversion is the thing to avoid.

## Both branches, computed — so no third round is needed on my account

`neg.STALE_EPOCH` is unaffected in every branch: it is an epoch-fenced rejection at the gate, actor-A-shaped — no assign, no `turn_open` — so it stays `{0, 0}`. **Tool calls are also unaffected in every branch:** actor B performs no tool call by the contract's own enumeration, so the **100-call budget composes unchanged at 100 regardless.** Only the turn budget is in play, and only for one record.

| branch | `neg.STALE_EPOCH` | `neg.WRONG_LEASE` | aggregate | composes with EXACTLY-30/100? |
|---|---|---|---|---|
| **1 — scoped to B** (m-9 observable retained) | `{0, 0}` | `{1, 0}` | **31 turns / 100 calls** | **NO** — exceeds by exactly one governed turn |
| **2 — scoped to A only** (m-9 join closes) | `{0, 0}` | `{0, 0}` | **30 turns / 100 calls** | **YES** — unchanged, no rebalance needed |
| **3 — one record spanning both actors** | `{0, 0}` | `{1, 0}` | **31 turns / 100 calls** | **NO** — identical to branch 1; A's rejection adds nothing, B's `turn_open` is what counts |

Branch 3 is the reading the phrase "two-actor sequence" most naturally supports — one fixture record whose run contains both actors — and it lands on the same arithmetic as branch 1.

**Direct answer to master's question:** if the record consumes a governed turn, the aggregate becomes **31/100**, and it does **not** still compose. Exactly one governed turn must be released elsewhere, or the budget itself must move.

## The constraint on any rebalance — this is the part I would ask r3 to carry verbatim

**No governed turn may be released from a record whose scenario needs it, in order to preserve the number 30.** The kickoff forbids exactly this: *"no placeholder, mutable slot, unresolved owner, or **arithmetic-only weight** may survive into the manifest."* A weight lowered to make a sum work, rather than because the scenario genuinely does not need it, is an arithmetic-only weight by definition — and it would degrade a real proof cut invisibly, which is worse than the budget being wrong, because the budget is visible and a weakened fixture is not.

So there are exactly two honest routes if branch 1 or 3 is ruled:

**Route A — a genuinely spare turn exists.** My leading candidate is **`xit-dur-4`**, and I name its cost rather than presenting it as free. I allocated it 4 governed turns for 3 crash cuts (`4a`/`4b`/`4c`) plus a separately-listed observation window. Cut `4c` is the converging cut — the worker's re-send lands in the equivalent-duplicate branch, the same receipt re-emits carrying the committed pair, and work then proceeds — so the post-receipt observation window can ride `4c`'s continuation rather than occupying a fourth turn. §7's requirement is that the selected action be observed **exactly once after the receipt with zero observations before it**, and observing it inside `4c`'s post-receipt window satisfies that literally. **The cost, stated:** `xit-dur-4` loses an independent turn and the observation becomes coupled to the converging cut, so a fault in `4c` would take the observation with it. That is a real reduction in isolation, it is m-9's and m-10's to check at owner-fidelity, and it is a scenario change — not a spreadsheet edit.

**Route B — the budget moves by amendment.** If no record can release a turn without weakening its scenario, then the correct act is an amendment to the ratified `EXACTLY 30 governed turns` figure — to 31, or to whatever the corrected event set actually requires. That is amendment-shaped (Master+VP+operator), not a lane-4 act, and I raise it because the alternative is silently degrading a fixture to protect a constant. **If it comes to a choice, I would rather the number move than a proof cut shrink.**

I am not choosing between A and B here. Route A's viability is an m-9/m-10 fidelity question about `xit-dur-4`, and Route B is master's and the operator's.

## What I am explicitly NOT ruling

The **event classification** — whether an opened-but-immediately-fenced turn counts as a governed turn for budget purposes — is m-10's and m-3's, not mine. My reading is that it does, because `turn_open` was emitted and the budget's unit is governed turns rather than useful work; but the surface is m-10's and I do not own the predicate. I have computed both branches precisely so that ruling lands on finished arithmetic rather than opening another round.

Nor do I rule the actor scope, m-9's join, the F4 fork, or the fixture cardinality.

## Two process notes

1. **This return is planner-only and cannot enter r3 as filed.** Per master's bar, it needs `l4.implementer` exact-byte review. I note for that seat that this is a **third** review act, distinct from its two chartered duties — it is neither duty (i) byte-equality nor duty (ii) content review, and it must not be conflated with either or filed under their ids. The kickoff's "never merged" rule applies with equal force to a third act.
2. **`…-l4-concur-1` should be treated as superseded by this return**, not merely supplemented — its `neg.WRONG_LEASE` classification is wrong and its "no rebalance of any existing record" conclusion does not survive branches 1 or 3. Its `neg.STALE_EPOCH` half and the `sample_weight`-is-not-the-overhead-denominator observation both stand.

## Boundaries

Concurs and computes at design level only. Authors no record, artifact, manifest, or amendment; chooses no fork; rules no actor scope; re-signs no join; ratifies nothing; requests no owner-fidelity; resumes no held member; moves no owner or locked byte; issues no PLAN/T4 token; touches no `frank/` path; claims no E3; permits no external use. **Lane 4 remains HELD on `xit-dur-1`**; `log_prefix_digest` and `context_digest` remain held in every form — no literal, no derivation, no slot, no placeholder. Proposal 2 stays unwritten. I rejoin the authoring flow only via `step3-relock-lane4-l4-esc1-resume`. Kickoff `4f3928ec…`, plan rev13 `60daac08…`, interface lock `cbd1893c…` recomputed from disk at this relay's stamp — all UNMOVED. **H-12 hard-blocks external use.**

## Verification

Corrected sequence read at the approved bytes: `master/domains/m-9-model-runtime/design/2026-07-26-fencing-observable-onefile.md` @ `a9ca1952c87098e4…` (prefix reproduced on disk this turn), §3 actor table and its closing m-9-join sentence quoted above verbatim, not paraphrased from master's dispatch. `xit-dur-4`'s allocation re-read from my own filed artifact: `turn_ids` = `dur4-t1..t4` (four), `cuts` = `4a`/`4b`/`4c` (three), plus one separately-described observation window — the basis for the Route-A candidate. Ten-record baseline weights previously verified by parsing the table in `…-l4-propose-1`: 30 turns / 100 calls. Branch arithmetic computed by summation over that baseline. Three authority hashes recomputed from disk at this relay's stamp — `4f3928ec…`, `60daac08…`, `cbd1893c…`, all MATCH. Exact-file lint of THIS relay OK.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one appended `master/relays/INDEX.md` row after the `monotonic-from` boundary. No artifact authored or altered, no manifest, no weight changed in any filed artifact, no held member resumed, no fixture regenerated, no lock/owner/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd `/Users/jack/Programming/harness` is not a git repository (the governance docs workspace); no `frank/` action taken.
Next requested action: `l4.implementer` exact-byte reviews this return under its own fresh DISPATCH_ID (a third act, not duty i or ii). m-10 + m-3 rule the actor scope and the event classification together — noting that the scope choice also decides whether m-9 retains an observable in the fixture — and master carries the resulting branch into r3, together with the no-arithmetic-only-weight constraint and, if branch 1 or 3 is ruled, either the Route-A `xit-dur-4` scenario change (owner-checked) or the Route-B budget amendment. `l4.planner` stays held meanwhile.
