## SITREP — the ONE end-of-slice adversarial review is COMPLETE: the slice is faithful (my independent battery green; three read-only reviewer lenses — FSM/J1 8/8, sunsets 4/4, test-quality all-proves-its-label with the historical truing byte-count-guarded); ONE must-fix (the expiry prompter's missing operator-FROM check) + ONE accepted-optional + named carries; fold requested, then the merge-decision relay goes to the operator

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-slice-review
PARENT_DISPATCH_ID: s10-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the fold is pair-internal; merge remains operator-only and follows the fold + the batched owner confirms
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-impl/IMPL-implementer-20260713-022915.md
FROM: s10.planner
TO: s10.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-2.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: whole-slice review of `s10-comms-spine@9722744` (11 commits over `8941889`) — everything I verified I verified by re-execution, not from your transcripts; the review finds the build faithful to the plan-of-record, every ruling, both sunsets, the rider, and the fence at named-seam grain; one small must-fix protects the never-auto-approve invariant's depth; fold it, capture, commit, report — then I author the merge-decision relay

### What I verified myself (E2/E1, re-executed)
- **Branch/battery:** 11 task commits, merge-base exactly `8941889`, clean tree; MY OWN `go test ./... -count=1` — every package green (fixtures 117s, invariants, all replay suites).
- **Fence at seam grain:** per-commit diff→license reconciled against your table — all-in; `Spawn` byte-identical by extracted-function diff; `config.go` = exactly the two owner edits; `registry.json` = exactly the two owner hunks (seat_scope untouched); `main.go` = three hunks inside the two licensed blocks; T10 commit has zero executor diff.
- **Tripwire:** both `1ef6abab…` v5 pins byte-identical (the diff's sole occurrence is inside your committed T1 blocker transcript); FX-CFG-7 assertions untouched (constructor-source swap only).
- **Rider:** present verbatim as the fixture constant `s10LiveApprovalClaimBoundary` and in the evidence file.
- **RED classification:** T1/T2/T4/T5 behavioral REDs (T4's is exemplary — it captures the unguarded non-A park actually happening); T7–T10 REDs are compile-failures against new API — resolved as acceptable because the lens verified each GREEN suite carries real behavioral negatives (below).

### Lens results (three read-only reviewer lenses, findings consumed whole)
- **FSM/J1 (8/8 hold):** frozen-at-emit choices (`odb.go:88`, mutate-after-emit rejected); operator-FROM on the channel-stamped seat meta not a spoofable header (`submit.go:49,463`); single-resolution guard on the rebuilt table (`submit.go:488-492`); never-auto-approve on EVERY failure branch (`expiry.go:91,97,101` → Kill; `approval.go:84,90,97,105` → Denied); re-observe-holds-stale with durable park intact (`submit.go:510-527`); exactly-once wake is crash-atomic-by-construction (resolution + delivery one commit; `fsm.go:27-31` dominance; idempotent derivations); dedupe hashes exactly (seat, decision, cadence-slot) (`resummon.go:158-166`); terminals byte-exact, parking rides `accepted`; R2 clean (`model_name` written by render, read by nothing decision-side).
- **Sunsets (4/4 hold):** soft-expiry prompts before any kill; the hard ceiling can ONLY kill — including when a disposition tries to return Extend past it; the kill state machine moved verbatim into `finalizeRun`; T10's typed default-deny sits BEFORE the executor switch, and an APPROVED entry still refuses with the distinct `side-effecting-execution-refused` token with the spy-executor at zero calls — the approval gate demonstrably lifts while execution stays fail-closed, exactly the rider's boundary; no static allowlist remains; I-PH clean (prompt inputs deliberately drop `path`/`lane_ref`; internal errors map through `safeReason`); the `main.go` reorder is required (prompters need the writer) and behavior-preserving.
- **Test-quality (all proves-its-label):** the v8-refusal fixture plants garbage content and proves zero interpretation; the exit fixture counts observations exactly 2 and wakes exactly 1; the historical v7 reconstructors are BYTE-COUNT-GUARDED (they self-fail if the v7→v8 diff ever grows); `s5_wire3`'s added verdict body is a correctly-classified new-requirement adaptation, not a weakened assertion.

### Findings to fold (then this review is green)
**MUST-FIX MF-1 — mirror the operator-FROM check in the expiry prompter.** `ApprovalPrompter.existingDecision`/`Apply` require `From=="operator"` (`approval.go:113,142`); the expiry equivalents (`expiry.go:124,139-158`) trust any Accepted `resolves_gate` record. Safe today (the only writer of that shape is operator-gated at `submit.go:463`), but the two prompters encode different trust for the same record and the invariant they guard is J1 never-auto-approve itself. Fold: add the explicit From check + ONE behavioral negative (a non-operator-authored Accepted resolution must NOT yield Extend). In-fence (`internal/engine/`), Rail A closed, no owner surface.
**ACCEPTED-OPTIONAL AO-1:** one-line comment at `observe/registry.go:203` marking the approved→`side-effecting-execution-refused` branch as the deliberate registered-carry no-op (prevents a future misread as a live path). Your discretion to fold with MF-1.
**CLAIM-TEXT CT-1 (fold report text only):** your report's "pinned-v5 SHA-256 remains exactly `943f07…`" cites the CATALOG pin; the fieldspec v5 pin is `1ef6abab…` (verified intact). Correct the label in the fold report; no code.

### Named carries + notes (recorded, NO action this slice)
- `submit.go:499` choice-validation is ODB-presence-contingent — ordering makes it unreachable today; s11 carry: consider fail-closed require-ODB for any HUMAN_GATE resolution.
- `allow-for-entry` standing scope is OWNER-SPECIFIED (m-3's leg 2 named `allow-once`/`allow-for-entry`) — noted as answered, not a gap.
- Re-entrant `callHandler` depth under ServiceWhileBlocked (bounded in practice) + the clean-shutdown dropped-result goroutine — engineering notes for m-7's ledger.
- T9's three-part claim spans two test names (coverage complete) — cosmetic.

### Fold shape
FOLD_SCOPE rows: `frank/internal/engine/expiry.go` + its test + (optional) `frank/internal/observe/registry.go` comment + the battery capture — all in-fence, findings-named. One fold commit; full battery green FILE-captured; fold report here with `FOLD_SCOPE` above `ACTIONS_GIT_REF` per protocol. On its receipt + the batched owner confirms (requested in the sibling relay `s10-build-owner-confirms/…-024153`), I author the merge-decision relay TO the operator.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-slice-review/SITREP-planner-20260713-024152.md` — run before handoff.
- Everything in §"What I verified myself" re-executed this session in the s10 worktree; the three lens reports received in full and spot-checked against their cited loci (`odb.go:88`, `expiry.go:91-104`, `approval.go:113,142`, `registry.go:195-204`, `resummon.go:158-166`).

ACTIONS_GIT_REF: none — a review relay; no `frank/` edit by this relay. Artifacts this turn: this relay + the owner-confirms relay + two `master/relays/INDEX.md` rows timestamped 20260713-024152/024153.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the s10 worktree clean at `9722744` per my fresh status check after the battery run.
Next requested action: fold MF-1 (+AO-1 at your discretion, CT-1 in the report text), one commit, battery captured, fold report TO me; in parallel the owners return their batched confirms; on both I author the merge-decision relay TO the operator. No merge without HUMAN_MERGE_AUTHORIZATION.
