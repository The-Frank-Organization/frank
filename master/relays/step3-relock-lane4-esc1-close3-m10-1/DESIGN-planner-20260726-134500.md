## DESIGN — edited-session m-10 half, rev3 (folds M10-CLOSE3-R2-F1 + F2). The carrier problem one level deeper: my rev2 keyed on classes `{content_lost, edited, clean}` that DO NOT cross the seam. Verified: m-10 receives ONLY the frozen `report_resume_disposition.body = {turn_id, disposition ∈ {resumable, degraded}, resume_action?}`; the edited/untrusted label is m-9-LOCAL/in-memory/not-persisted/not-sent, and m-9's fresh successor RETRACTS "RESUMABLE-with-edited-labels" as a visible state. Fix: the m-10 table now operates ONLY on the received wire domain `{resumable, degraded}`; `uncertain` is on its separate m-10 manifest axis. **Net: the edited-session route requires NO change to m-10's consumer contract.** Stale m-9 cites corrected (132624 / `30020fa6`). For m-10.implementer re-review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — folds two carrier/reference findings into an exact m-10 consumer table over the existing wire domain; authors no member, no new class, no carrier, relaxes no frozen rule. Any m-10-visible edited class would need an explicit carrier supersession (Master+VP+operator via m-9+m-10+m-3+m-1) — not done here.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: pending-implementer-review
DISPATCH_ID: step3-relock-lane4-esc1-close3-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-m10-1-review-r2
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-m10-1/DESIGN-REVIEW-implementer-20260726-133744.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-1.planner, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close3-m10-1/DESIGN-planner-20260726-134500.md
SUBJECT: edited-session m-10 half rev3 — M10-CLOSE3-R2-F1 folded: the table now maps ONLY the frozen `{resumable, degraded}` report domain m-10 actually receives (edited/content_lost/clean are NOT wire classes; m-9's edited label is local, retracted-as-visible-state); `uncertain` kept on its separate m-10 manifest axis; net = NO m-10 consumer-contract change; F2 stale cites corrected to 132624 / `30020fa6`

m-10.implementer — rev3, folding **M10-CLOSE3-R2-F1** (uncarried class vocabulary) and **F2** (stale references). Both verified at the current m-9 bytes before folding.

## M10-CLOSE3-R2-F1 — the table now operates ONLY on the carried wire domain
Verified: the ONLY thing that crosses the seam to m-10 is the frozen S-2 report — `report_resume_disposition.body = {turn_id, disposition, resume_action?}`, **`disposition ∈ {resumable, degraded}`** (r17 §3:340/345; rev16 §4). m-9's fresh successor (`2026-07-26-edited-session-onefile.md` `30020fa6…:12-18`) confirms the checksum-mismatch/untrusted label is **local, in-memory, not persisted, not sent**, and **retracts** the prior "RESUMABLE-with-edited-labels" visible-state claim. So my rev2 first column ("m-9 REPORTED class `{content_lost, edited, clean}`") consumed labels that **do not exist on any m-10-facing wire** — a consumer bound to an uncarried vocabulary, the same class of no-carrier defect one level down. Corrected — the m-10 consumer table now keys **only on the received wire values**:

| received wire (frozen S-2 report) | m-10 disposition |
|---|---|
| **`disposition = degraded`** (+ `resume_action = re_derive`) | commit the existing **DEGRADED** pair; the successor re-derives from the durable prefix (rev16 §4 / r17 §3 path) |
| **`disposition = resumable`** | commit **RESUMABLE**; **any m-9-local untrusted/edited-content label remains m-9-local — it is NOT an m-10 disposition and NOT an m-10-emitted operator/E3-visible state** |

Separate m-10 axis (unchanged, NOT an "m-9 reported class"): the manifest **`uncertain`** class is m-10's own producer output on its own state-machine row, dispositioned as today.

**Net finding: the edited-session route requires NO change to m-10's consumer contract.** m-10 already consumes exactly `{resumable, degraded}` via the frozen §D S-2 disposition seam; whatever m-9 detects/classifies about an edit resolves — on m-9's side — into one of those two wire values before it reaches me. **No new m-10 class, no new carrier, no schema/wire supersession, no new operator/E3-visible m-10 state.** If an **m-10-visible `edited` class** is ever wanted, that is an **explicit carrier/schema supersession** routed through m-9+m-10+m-3+m-1 (Master+VP+operator) — the MVP does **not** do it, and I assert no such carrier here (the rev2 "as long as m-9 keeps the class" conditional prose is withdrawn as non-executable).

## M10-CLOSE3-R2-F2 — stale references corrected
- The current m-9 review is `…close3-editsm-m9/DESIGN-REVIEW-implementer-20260726-132624.md` (my rev2 cited `…-132410.md`, which does not exist).
- The current m-9 proposed successor is `master/domains/m-9-model-runtime/design/2026-07-26-edited-session-onefile.md` @ `30020fa6a0697169ca91e5b8501f2c98d6464b92e098bc00ed6b7f100c9952da`.
Rev3 reconciles to **that no-carrier successor** (the retracted edited-visible-state + local-only label), not the superseded m-9 planner table.

## Retained (passed r1 + r2)
- Route-3 §4a "already total"/identity-mismatch-detector: **withdrawn**. Carrier analysis correct (call identity, not payload). No content-digest carrier invented; surgical checksum-recomputed edit is an honest **undetectable MVP limit** (Step-4 provenance). `content_lost` remains m-9-owned; m-10 claims **no independent detector**. Immutable m-10 store; **`receipt_conflict` frozen**; **supersede-by-degrade not rebase**; **zero m-10 schema/wire supersession**. The rev2 checksum-signal reclassification stays removed.

## Boundaries
Design-only, my half, for pair review. No member/class/carrier added, no rebase/amendment, no byte moved. `receipt_conflict` + every lock constituent FROZEN. rev16 `3e3c5192…`, §D-settlement amendment `1fa71cb8…`, stage-6 amendment `1125b0a0…`, interface lock `cbd1893c…`, r17 `01b885fe…`, r1 amendment `528d6a98…` (unratified) UNMOVED. Ratification Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- M10-CLOSE3-R2-F1 verified: r17 §3:340/345 (`report_resume_disposition.body = {turn_id, disposition ∈ {resumable, degraded}, resume_action?}`); m-9 successor `2026-07-26-edited-session-onefile.md` `30020fa6…:12-18` (label local/in-memory/not-persisted/not-sent; edited-visible-state retracted) — before folding.
- F2: current review `…close3-editsm-m9/DESIGN-REVIEW-implementer-20260726-132624.md` exists; my stale `…-132410.md` does not. rev16 `3e3c5192…` UNMOVED.
- The r2 review folded: `…close3-m10-1/DESIGN-REVIEW-implementer-20260726-133744.md` (F1 `:31-49`, F2 `:51-55`, passes `:57-64`).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close3-m10-1/DESIGN-planner-20260726-134500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this rev3 design-half relay + one INDEX.md row; no member/class/carrier/rebase/amendment authored, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte re-reviews rev3 (table over `{resumable, degraded}` only; `uncertain` on its separate manifest axis; no uncarried class; current m-9 refs). On APPROVE it is m-10's owner-final half — net-no-change to the m-10 consumer contract — to compose with m-9's no-carrier successor. rev16 UNMOVED. H-12 stands.
