## DESIGN — edited-session CLOSURE, m-10 half, CORRECTED (for m-10.implementer exact-byte review). I WITHDRAW my Route-3 §4 over-claim: verified false at the bytes — the manifest/match key carry CALL identity + `args_digest`, NO content digest, so a payload edit leaves the identity-match intact and the frozen conjunction does NOT detect it. Honest position: **m-10 owns DISPOSITION, not detection; m-10 holds NO content-integrity carrier; detection (and its honest MVP limit) is m-9's over the journal it owns.** No m-10 schema/wire member is added or superseded (I do NOT invent a content-digest carrier — the asserted-mechanism-with-no-carrier the VP rejected). `receipt_conflict`-not-implicated + supersede-by-degrade (not rebase) STAND. Composed table + m-3/Route-2 consequence below. Pair-approval requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — the corrected m-10 half authors no new member and relaxes no frozen rule; it withdraws a false claim and states an honest disposition contract for exact-byte pair review. Ratification of the composed contract into amendment r2 is Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: pending-implementer-review
DISPATCH_ID: step3-relock-lane4-esc1-close3-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm/DESIGN-orchestrator-planner-20260726-044200.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-1.planner, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close3-m10-1/DESIGN-planner-20260726-131200.md
SUBJECT: edited-session m-10 half CORRECTED — Route-3 §4 "already total / identity mismatch detects the edit" WITHDRAWN (no content-digest carrier; call-identity match survives a payload edit); m-10 = disposition only, detection is m-9's; NO m-10 member superseded; composed table + honest MVP detection limit + m-3/Route-2 consequence — requesting m-10.implementer exact-byte APPROVE for the owner-final joint contract

m-10.implementer — this is the corrected m-10 half of the edited-session contract, for your exact-byte review (F1: owner-final + pair-approved, not planner-only). It **withdraws a false claim I made** and states the honest disposition contract.

## The withdrawal, stated exactly (VP F2, verified at the bytes)
My Route-3 (`…-route3-m10-ans-1:39-43`) claimed the edited-session trust semantics were **"already total"** because "an edit breaks Conjunct B (identity-exact match) ⇒ `content_lost`." **That is false, and I verified it at the bytes:**
- The manifest tool entry is `{run_id, turn_id, tool_call_id, args_digest, terminal, void_reason?}` (rev16 §1:22) — **call identity + `args_digest`, no content digest.**
- m-9's Conjunct B matches **identity-exact on `{run_id, source turn_id, tool_call_id|attempt_id}` (+ `args_digest` for tools)** (r17 §3:334) — the CALL, not the content bytes.
- A **payload edit rewrites the tool_result/provider_output CONTENT** and leaves `{run_id, turn_id, tool_call_id, args_digest}` **unchanged** ⇒ Conjunct B **still matches** ⇒ the edited content would be **trusted**. The frozen conjunction does **not** detect a payload edit. `turn_open` carries no round-identity/content operand; m-10 cannot read m-9's journal; m-9 cannot read m-10's receipt table. **The identity comparison I asserted has no carrier.** §4a "already total" is withdrawn.

## The honest position (F2): m-10 owns DISPOSITION, not detection
- **m-10 holds NO content-integrity witness.** My manifest/receipts carry ids, `args_digest`, terminals — never a content digest. Under the operator's ruling my settlement store is **uneditable** ("keep it effectively uneditable"), but it also **carries no content digest**, so m-10 cannot be the comparison actor **even in principle**. I do **not** add one — inventing a content-digest carrier is exactly the asserted-mechanism-with-no-carrier the VP rejected, and it would also re-open the co-signed receipt for a member the confusion-not-malice model does not need.
- **Detection of journal edits is m-9's**, over the journal m-9 owns, via the signals m-9 owns: content **absence** (stage-2 no-match), the **advisory per-record checksum**, **record grammar / valid-prefix** recovery, and the **D1 file-identity binding** to `{run_id, run_manifest_digest}`. The **honest MVP detection limit** — which edits are detectable vs undetectable — is **m-9's to enumerate over its journal**, not mine to assert.
- **m-10's contribution is the DISPOSITION** consuming m-9's reported outcome. Nothing here is a detector.

## The composed disposition/first-action table (F4), m-10 rows (total over m-9's reported states)
| content state at resume (m-9 detects) | m-10 disposition | first action |
|---|---|---|
| call `settled_with_content` + content present + m-9 integrity signals OK | **RESUMABLE** (consumed) | normal governed loop *(the honest gap lands here — see below)* |
| call `settled_with_content` + content **ABSENT** | **`content_lost` → DEGRADED** | `re_derive` from the durable recovered prefix; edited/absent content never trusted, never reconstructed |
| call `settled_with_content` + content present but **m-9 integrity signal FAILS** (checksum / grammar / file-identity) | **`content_lost` → DEGRADED** | `re_derive`; if no degraded path exists ⇒ fail-closed run-terminal |
| call not `settled_with_content` / `uncertain` | **uncertain** (existing manifest class) | surfaced; no auto-resend/re-execute |

m-10's rule is exact and total over m-9's reported outcome: **DEGRADED iff m-9 reports `content_lost`; otherwise RESUMABLE.** m-10 independently flags nothing (no carrier). This reconciles the two returns the VP flagged (m-9 "resumable-with-labels" vs m-10 "mismatch⇒DEGRADED"): the reconciliation is that **m-10's DEGRADED is driven by m-9's `content_lost`, not by an m-10-side detector** — there is only one detector (m-9's) and one disposer (m-10's).

## The honest narrowed claim (the VP invited it; I state it plainly)
At MVP, frank detects-and-degrades a journal edit **only when the edit breaks a detectable integrity signal** (content absence, advisory-checksum mismatch, record-grammar violation, or a file-identity break). A **wholesale rewrite** (`bivpak`-class) or a naive hand-repair typically breaks at least one of these and therefore **resumes visibly flagged DEGRADED** — matching the operator's accepted outcome. A **surgical payload edit that recomputes the advisory checksum, stays grammar-valid, and preserves file identity is UNDETECTABLE at MVP** and consumes as clean. This is **accepted under the confusion-not-malice threat model** (crafting a byte-consistent fake history is malice, out of scope), and the **authenticated-provenance / session-versioning path** that would flag or authorize such an edit is the **named Step-4 carry** (`ROADMAP.md`; the recovery-from-unresumable-state path is `FRANK-HARDENING-BACKLOG.md`, post-product). **I do not claim frank distinguishes a sanctioned repair from corruption** — it cannot at the bytes. The exact detectable/undetectable boundary is m-9's to state over its detection signals; I state only that m-10's guarantee ("never silently promote edited bytes as prior truth") holds **exactly to the extent m-9's detection reports `content_lost`**, and no further.

## m-3 / Route-2 consequence (load-bearing, per master)
Without an authenticated provenance record, m-3's Route-2 direct-prefix predicate cannot tell a sanctioned edit from corruption, so **every detectable divergence is treated uniformly as `content_lost`/DEGRADED** — frank makes **no sanctioned-vs-corruption classification at MVP** (that arrives with Step-4 versioning). m-3 confirms the resulting observable + the Route-2 consequence; my table above is consistent with a uniform-degrade-on-detectable-divergence predicate.

## Retained from Route-3 (unchanged, and now primary)
- **`receipt_conflict` NOT implicated** — the derivation stands: an edited historical log emits **no second live `content_ready` frame**; edit-handling is the resume-reconciliation seam, never the live receipt-ingress seam. `receipt_conflict` stays frozen.
- **MVP supersedes-by-degrade, does NOT rebase.** My snapshot/receipts are immutable; an authorized rebase-across-stores that re-anchors identity to sanctioned new bytes is the Step-4 carry. This is now the **primary** disposition, not a fallback.

## Schema/wire supersession — m-10 half: NONE
The disposition is fully covered by existing frozen members: manifest `{settled_with_content, determinate_no_resume, uncertain}`, `resume_disposition ∈ {PENDING, RESUMABLE, DEGRADED}`, `resume_action = re_derive`, the `content_lost`-via-DEGRADED path. **This route supersedes no m-10 receipt/manifest/trust/disposition/wire member**, and — correctly this time — because m-10 owns disposition not detection, not because "it was already total." (The round-identity member's fate is Route 5's, independent per my Route-5 half.)

## The joint seam + what I ask of you (m-10.implementer)
The single joint contract = m-9's detection half (the signals + the honest detectable/undetectable limit + `content_lost` production) composed with this m-10 disposition half at the existing §3/§4 `content_lost` → DEGRADED handoff — **no new wire, no new member.** Please exact-byte review THIS m-10 half for: the withdrawal is complete and honest (no residual "m-10 detects the edit" claim survives); the disposition table is total over m-9's reported states; no content-carrier is invented; no m-10 member is superseded; `receipt_conflict`/immutability/degrade-not-rebase stand; and the narrowed claim is stated as a limit, not hidden. On your APPROVE this is m-10's owner-final half; m-9's half + m-3's confirm + m-1's boundary re-confirm compose the joint contract master folds into r2.

## Boundaries
Design-only, my half, for pair review. `receipt_conflict` + every lock constituent stay FROZEN; no member added/superseded, no carrier invented, no rebase authored (Step-4 named), no amendment, no byte moved. rev16 `3e3c5192…`, §D-settlement amendment `1fa71cb8…`, stage-6 amendment `1125b0a0…`, interface lock `cbd1893c…`, m-9 delta `01b885fe…`, r1 amendment `528d6a98…` (unratified) UNMOVED. New trust/disposition semantics + ratification stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- rev16 `3e3c5192…` §1:22 (tool entry = `{run_id, turn_id, tool_call_id, args_digest, terminal, void_reason?}` — no content digest), §2:30 (evidence property #1), §4:53/55 (immutable `resume_snapshot`, no UPDATE; `resume_disposition`/`resume_action`) — UNMOVED.
- m-9 r17 `01b885fe…` §3:334 (Conjunct B call-identity match), §3:336 (`content_lost` → DEGRADED), §3:360 (`re_derive`) — the joint detection half's basis.
- The withdrawn over-claim: `…-esc1-route3-m10-ans-1/DESIGN-planner-20260726-033900.md:39-43`. VP F2/F4 + carrier gap: `…-esc1-amend-1/DESIGN-REVIEW-orchestrator-reviewer-20260726-040848.md`. Operator boundary ruling (store uneditable) + recovery carry: this close3 dispatch `…-044200` (§8b-cited). Step-4 carries: `ROADMAP.md`; `FRANK-HARDENING-BACKLOG.md`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close3-m10-1/DESIGN-planner-20260726-131200.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this corrected-half design relay + one INDEX.md row; no member superseded, no carrier/rebase/amendment authored, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte reviews this corrected m-10 half; on APPROVE it is m-10's owner-final half of the joint edited-session contract. m-9 authors its detection half (with the honest detectable/undetectable limit) pair-approved; m-3 confirms the observable + Route-2 consequence; m-1 re-confirms the narrowed boundary (store uneditable). Master composes the pair-approved halves into amendment r2. `receipt_conflict` frozen; rev16 UNMOVED. H-12 stands.
