## DESIGN — edited-session m-10 half, rev2 (folds M10-CLOSE3-R1-F1). My concrete "checksum failure → content_lost/DEGRADED" row RECLASSIFIED an m-9 detection signal — the exact detection-ownership error I was correcting. Verified at m-9's bytes: a checksum mismatch on PRESENT, WELL-FORMED content is m-9-class **edited/untrusted-but-model-visible → RESUMABLE-with-edited-labels**, NOT content_lost (record_digest is ADVISORY; only structural/missing ⇒ content_lost). Fixed: the table now keys PURELY on m-9's REPORTED CLASS vocabulary, reclassifying no signal; the "bivpak/naive-repair necessarily DEGRADED" claim is REMOVED. For m-10.implementer re-review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — folds one cross-owner classification finding into an exact m-10 disposition half for pair review; authors no member, relaxes no frozen rule. Ratification into r2 is Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: pending-implementer-review
DISPATCH_ID: step3-relock-lane4-esc1-close3-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-m10-1-review-r1
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-m10-1/DESIGN-REVIEW-implementer-20260726-132515.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-1.planner, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close3-m10-1/DESIGN-planner-20260726-133200.md
SUBJECT: edited-session m-10 half rev2 — M10-CLOSE3-R1-F1 folded: the disposition table now consumes m-9's REPORTED CLASS (content_lost / edited / clean) without reclassifying any signal; checksum-mismatch-on-present-content is m-9's `edited`⇒RESUMABLE-with-edited-labels, not m-10-DEGRADED; bivpak-necessarily-DEGRADED removed; class vocabulary robust to m-9's corrected successor

m-10.implementer — rev2 of the m-10 half, folding **M10-CLOSE3-R1-F1**. I verified the finding at m-9's bytes before folding (`…close3-editsm-m9/DESIGN-planner-20260726-131630.md`): m-9's owner table classes a naive checksum-mismatch on **present, well-formed** content as **`edited` (untrusted-but-model-visible) → RESUMABLE-with-edited-labels** (`:43-44`), reserves `content_lost`/DEGRADED for **structural/completeness fail or missing referenced content** (`:42`/`:46`), and narrows `record_digest`/checksum to **advisory** edit-labelling (`:57-59`). My rev1 concrete row mapped "checksum failure ⇒ content_lost/DEGRADED" — that **reclassified an m-9 detection signal**, contradicting m-9's owner class and re-owning the very job I had just withdrawn. Corrected.

## The fix — the table keys on m-9's REPORTED CLASS, never on a signal
m-10 owns disposition, not detection. So the rev2 table dispositions **purely off m-9's reported class label** and reclassifies **nothing**:

| m-9 REPORTED class (m-9's determination, over its journal) | m-10 disposition | first action |
|---|---|---|
| **`content_lost`** (m-9's: structural/completeness fail, or missing referenced content at D2 consume) | **DEGRADED** | `re_derive` from the durable recovered prefix; if no degraded path exists ⇒ fail-closed run-terminal |
| **`edited` / untrusted-but-model-visible** (m-9's: complete, present, advisory-checksum-mismatched content) | **RESUMABLE-with-edited-labels** — m-10 admits the successor; the **edited label + trust-downgrade ride m-9's class**; m-10 adds **no independent detection and no independent trust**; the content is model-visible-edited, **never presented as the provider's/tool's actual output** | normal governed loop under m-9's edited label |
| **clean / no reported divergence** (incl. the undetected checksum-recomputed rewrite) | **RESUMABLE** | normal governed loop — the **accepted MVP limit** (frank does not distinguish sanctioned repair from corruption; Step-4 provenance) |
| manifest **`uncertain`** (existing m-10 class, unchanged) | **uncertain** | surfaced; no auto-resend/re-execute |

**m-10's rule, exact and total over m-9's class set:** disposition is a **function of m-9's reported class only** — `content_lost ⇒ DEGRADED`; `edited ⇒ RESUMABLE-with-edited-labels`; `clean ⇒ RESUMABLE`. m-10 **independently detects and reclassifies nothing** (no carrier; §4a withdrawn in rev1, and now the last residual signal-classification — the checksum row — is removed too). This is the honest content of "one jointly-authored total table": **one detector+classifier (m-9), one disposer (m-10), no double classification.**

## Removed: the "bivpak/naive-repair necessarily DEGRADED" claim
rev1 said a naive repair or `bivpak` rewrite "normally resumes visibly flagged DEGRADED." **Withdrawn.** Per m-9's bytes, a present, well-formed, checksum-mismatched edit resumes **RESUMABLE-with-edited-labels** (visibly *edited*, not *degraded*); only structural/missing lands in DEGRADED. So m-10 does **not** predict which disposition a given real-world edit produces — it **consumes m-9's class**. The operator's "never silently clean" holds via m-9's *edited label* (for detectable non-structural edits) and DEGRADED (for structural/missing) — both m-9-classed — with the surgical checksum-recomputed edit the named undetectable MVP limit.

## Robust to m-9's corrected successor (the reconciliation target)
m-9's current return is itself **must-revise** (m-9.implementer found the `edited_since_write`/operator/E3 visibility label has **no durable/wire carrier** in those bytes). My rev2 table therefore binds only m-9's **class vocabulary** `{content_lost, edited, clean}` and the disposition function over it — **not** any specific edited-label carrier (which is m-9's to settle). As long as m-9's corrected pair-approved successor keeps that class set, my disposition function holds unchanged; if m-9's successor renames/re-scopes a class, my map re-binds by name at that point. I assert none of m-9's carrier — I consume its class.

## Retained from rev1 (all passed the reviewer)
- The Route-3 §4a "already total"/identity-mismatch-detector claim stays **withdrawn**.
- Carrier analysis correct: manifest/match-key ids + `args_digest` = call identity, not payload identity.
- m-10 adds **no content-digest carrier**, claims **no independent detector**, keeps its store **immutable**, leaves **`receipt_conflict` frozen**.
- Surgical checksum-recomputed edit honestly **undetectable at MVP**; sanctioned-vs-corruption **Step-4**.
- **Supersede-by-degrade, not rebase**, for the `content_lost` branch. **Schema/wire supersession, m-10 half: NONE.**

## Boundaries
Design-only, my half, for pair review. `receipt_conflict` + every lock constituent stay FROZEN; no member added/superseded, no carrier invented, no rebase authored, no amendment, no byte moved. rev16 `3e3c5192…`, §D-settlement amendment `1fa71cb8…`, stage-6 amendment `1125b0a0…`, interface lock `cbd1893c…`, m-9 delta `01b885fe…`, r1 amendment `528d6a98…` (unratified) UNMOVED. Ratification stays Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- M10-CLOSE3-R1-F1 verified at m-9's bytes `…close3-editsm-m9/DESIGN-planner-20260726-131630.md:42-47,57-59` (checksum-mismatch-present ⇒ `edited`/RESUMABLE-with-edited-labels; structural/missing ⇒ `content_lost`; record_digest advisory) before folding.
- rev16 `3e3c5192…` §1:22 (call identity, no content digest), §4:53/55 (immutable snapshot, `resume_disposition`/`resume_action`) UNMOVED. m-9 r17 `01b885fe…` §3:336/346 (`content_lost`→DEGRADED/`re_derive`).
- The r1 review folded: `…close3-m10-1/DESIGN-REVIEW-implementer-20260726-132515.md` (F1 `:31-45`, passes `:49-57`). m-9's concurrent must-revise (edited-label carrier gap): `…close3-editsm-m9/DESIGN-REVIEW-implementer-20260726-132410.md`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close3-m10-1/DESIGN-planner-20260726-133200.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this rev2 design-half relay + one INDEX.md row; no member superseded, no carrier/rebase/amendment authored, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte re-reviews rev2 (the table consumes m-9's class without reclassifying any signal; no bivpak-DEGRADED claim; class-vocabulary-bound, not carrier-bound). On APPROVE it is m-10's owner-final half, to reconcile with m-9's corrected pair-approved successor. rev16 UNMOVED. H-12 stands.
