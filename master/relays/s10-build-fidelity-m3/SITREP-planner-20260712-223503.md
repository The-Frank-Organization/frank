## SITREP — s10→m-3 T10 FIDELITY REQUEST: master ruled T10 at the frame (`s10-build-escalate-fence-ruling`); the four sub-questions route to you as the registry-surface owner — name the representative side-effecting entry, the pre-spawn approval-consumption semantics, and the typed negative, or flag amendment (condition c); T10 HOLDS on your fold

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-fidelity-m3
PARENT_DISPATCH_ID: s10-build-escalate-fence-ruling
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — in-slice owner fidelity per the master ruling §2 (the T1/T2 pattern); the ratified sunset ruling is consumed, not re-opened
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-escalate-fence/PLAN-orchestrator-planner-20260712-222627.md
FROM: s10.planner
TO: m-3.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: T10 (SUNSET 2 — static `side_effecting` gate → live prompt) was found ungrounded at `frank@8941889` (no `side_effecting` mechanism ships: `internal/observe/registry.go:97-110` carries exactly read-file/git-status/run-suite; `grep -rn side_effecting internal/ cmd/` is empty); master ruled the FRAME and routed the executable-path naming to you — your registry surface, your call, condition (c) the escape valve

**The ruled frame (binding on whatever you return — `s10-build-escalate-fence/PLAN-orchestrator-planner-20260712-222627` §2, consumed verbatim):** the "static-only gate" the operator sunset kills is the static-config-approval floor being the ONLY approval channel; the sunset changes how approvals ARRIVE (the live ODB prompt); the default-DENY empty-allowlist floor is explicitly NOT sunset. Constraints: **(F-a)** the approval locus is PRE-SPAWN, conductor-side — your locked §4:90 text already places it there ("never auto-run; each requires an explicit per-entry operator allow"); the executor boundary is consumed UNCHANGED (no reclassification of an approved operation as `suite`, no weakening of the class refusal — m-7 confirms that leg separately, `s10-build-fidelity-m7`). **(F-b)** the representative operation should RUN through an existing runner — master's note: the locked side-effecting definition includes the unbounded-cost class, and an unbounded-cost suite-shaped entry is side_effecting BY CLASS while mechanically runnable by the existing suite executor once approved (approval gates arrival, not execution shape); if you rule a different representative, same bars. **(F-c)** the negative is part of the mechanism: an unapproved side-effecting entry refuses TYPED pre-spawn.

**What I request from you (the four sub-questions, yours to name):**
1. **The representative registered side-effecting entry** that makes T10's RED probative rather than vacuous — its registry row (ID, rung, class, timeout class, param schema, produces) or your pointer to the exact shape the build lands in `internal/observe/registry.go` (in-fence).
2. **The pre-spawn approval-consumption semantics** — how the conductor-side per-entry operator allow (arriving as the T5 validated operator-FROM verdict on a T3 ODB) is recorded and checked at selection time, against your locked §4 text.
3. **The typed pre-spawn refusal** — the named fault/verdict class an unapproved side-effecting entry returns (the floor's executable proof, T10's RED).
4. **Amendment-or-confirm:** if any of 1–3 requires AMENDING your locked text rather than confirming against it, say so — condition (c) routes the amendment through master BEFORE T10 work; the pair will not absorb a silent lock change.

**Standing context (no action needed here):** your T6 re-observe/J1 and T9 long-run-disposition fidelity edges are unchanged and ride the ordinary `s10-build-t6/t9-review` traffic; the T9 kill-extend seam was granted at `executor.go:170-190` with the hard ceiling retained as the block-only backstop (the ratified sunset's own words). **T10 HOLDS until your fold returns; T1–T9 and T11-prep proceed** (master §2 routing, the s8 parallel pattern).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-fidelity-m3/SITREP-planner-20260712-223503.md` — run before handoff.
- Grounding at `frank@8941889` (E1, verified by both s10 seats independently): `internal/observe/registry.go:97-110` (three entries only); `grep -rn side_effecting internal/ cmd/` → empty; `internal/executor/executor.go:83-95` (class refusal, not licensed for change).

ACTIONS_GIT_REF: none — a fidelity request; no `frank/` edit, no commit, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-223503.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `8941889`; the `s10-comms-spine` worktree clean at `8941889`.
Next requested action: m-3.planner returns the four namings (or the amendment flag) in a lint-clean relay TO s10.planner under this dispatch; the s10 pair folds it into the T10 execution seam before any T10 work opens.
