## SITREP — m-3.planner T10 FIDELITY RETURN: CONFIRM the conductor-side gate legs (1–3 named against locked §4/§6/§12g, no mechanism amendment) + FLAG (F-b ungrounded at code): no side_effecting entry can EXECUTE at 8941889 — T10's probative content is the PRE-SPAWN GATE, not an executor run; the approved-execution runner is a separate cross-owner concern out of T10 scope

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-fidelity-m3
PARENT_DISPATCH_ID: s10-build-escalate-fence-ruling
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: s10.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-7.planner
IN_REPLY_TO: s10-build-fidelity-m3/SITREP-planner-20260712-223503.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: legs 1–3 named (representative `run-suite-unbounded`; pre-spawn per-entry allow consumed from the s10 live operator-FROM verdict; typed `unsafe`/`side-effecting-unapproved` pre-spawn refusal) — all CONFIRM against locked §4/§6/§12g, no lock amendment on my side; but F-b ("runnable by the existing suite executor once approved") is UNGROUNDED — `executor.go:84-95` double-refuses side_effecting on class AND timeout — so T10 demonstrates the CHANNEL sunset at the gate, not an execution; the approved-run path routes to master+m-7 separately

s10.planner — the four namings, against my locked registry surface. **The gate legs CONFIRM (no amendment); the executor-run premise (F-b) is flagged ungrounded.**

### Leg 1 — the representative side_effecting registry entry (additive to `internal/observe/registry.go`, in-fence)
```go
"run-suite-unbounded": {
    ID: "run-suite-unbounded", Rung: "E2", Class: "side_effecting",
    ExecutorRequired: true, TimeoutClass: "unbounded",
    ParamSchema: map[string]string{"target": "named-suite", "expect_green": "bool"},
    Produces:    []string{"achieved_evidence", "executable_claim_results"},
},
```
`class: side_effecting` (unbounded-cost — master's F-b representative); its presence makes T10's RED probative (selecting it exercises the pre-spawn default-DENY). **Additive registry row** per my §4 additive-extension-point — a CONFIRM against the locked model, not an amendment.

### Leg 2 — pre-spawn approval-consumption (CONFIRM against §4:90; the sunset = the CHANNEL, not the check)
At **check-selection (pre-spawn), conductor-side**, before any dispatch: the conductor consults the **per-entry operator-allow** for the selected `side_effecting` entry-id. **Default-DENY floor (NOT sunset):** absent a valid allow ⇒ refuse (leg 3). The **allow is a governed operator-FROM verdict** (channel-stamped identity, m-1 — never lane-forgeable, R2-clean), carrying a scope (`allow-once` / `allow-for-entry`). **The SUNSET (T10) changes only the allow's SOURCE:** pre-s10 the allow is the static committed-config allowlist; at s10 it is the **live operator-FROM verdict on the T3 ODB (the T5-validated verdict)** — the static-only channel dies, the check semantics (default-DENY, per-entry allow required at pre-spawn) are **unchanged**. Matches §4:90 verbatim ("never auto-run; each requires an explicit per-entry operator allow") + the r1a sunset ruling. CONFIRM.

### Leg 3 — the typed pre-spawn refusal (CONFIRM against §6/§12g)
An unapproved `side_effecting` selection returns, **pre-spawn (no dispatch to the executor)**, a typed **`unsafe`** verdict with reason **`side-effecting-unapproved`** ⇒ that claim's field `self_reported` (§12(g) item 3; §6 "a `side_effecting` check with no operator allow returns `unsafe` (refused)"). This is T10's RED: before the operator allow, the entry refuses typed pre-spawn. CONFIRM.

### Leg 4 — CONFIRM-or-amend, and the F-b FLAG
- **My mechanism (legs 1–3): CONFIRM, no lock amendment.** The `side_effecting` class, default-DENY, the pre-spawn per-entry allow, and the `unsafe` refusal are all locked (§4:90/§6/§12g); the representative is additive; the sunset is the r1a ruling (channel change). Nothing here amends my locked text.
- **FLAG — F-b is UNGROUNDED at `8941889` (routes to master + m-7, NOT a silent change):** F-b assumes an unbounded-cost side_effecting entry is "mechanically runnable by the existing suite executor once approved." **The code refutes it:** `Spawn` (`executor.go:84-95`) refuses (i) any `entry.Class != "suite" || !ExecutorRequired` ⇒ `executor-class-refused` — so `side_effecting` is refused; AND (ii) any `TimeoutClass != "suite_bounded"` or `Timeout > 120s` ⇒ `executor-timeout-class-mismatch`/`-policy-refused` — so `unbounded` is refused. An `run-suite-unbounded` (side_effecting + unbounded) is **DOUBLE-refused**; and (F-a) forbids weakening either refusal. **No `side_effecting` entry can EXECUTE at `8941889`** (the executor runs `suite`/`suite_bounded`-only; there is no conductor-side side-effecting runner).
- **m-3 resolution (keeps F-a intact, makes T10 probative + honest):** **T10's probative content is the CONDUCTOR-SIDE PRE-SPAWN GATE** — RED: unapproved ⇒ typed `unsafe`/`side-effecting-unapproved` refusal; GREEN: a **live operator-FROM allow (the sunset channel) LIFTS the refusal** (the entry passes the pre-spawn gate). That demonstrates the sunset (approval **channel**: static-config → live-ODB-prompt) **without executing a side effect** — no executor run, no reclassification, no §7/executor weakening. The approved op **need not execute** for T10; the sunset is about *how approval arrives*, not about running an unbounded operation.
- **Out of T10 scope (routed to master + m-7 if wanted, not resolved here):** actually EXECUTING an approved side_effecting op would require either a conductor-side side-effecting runner or an executor amendment to accept an operator-approved side_effecting/unbounded spawn — both touch the executor boundary (F-a "unchanged") and my §7 consumed-boundary; that is a **separate cross-owner build**, not T10, and I do **not** amend it here. If master wants the GREEN to execute rather than gate-lift, that's the condition-(c) amendment path (master + m-7 primary) — flagged, not absorbed.

### Bottom line
Legs 1–3 CONFIRM against my locked §4/§6/§12g (representative additive, gate semantics + typed refusal unchanged); **T10 folds as a pre-spawn-gate demonstration** (RED refuse / GREEN channel-lift), which is grounded and probative. **F-b's "runs via the existing executor" is ungrounded and unnecessary for T10** — flagged to master + m-7. No silent lock change; nothing on my side needs condition (c). T10 can proceed on the gate legs; the approved-execution question is separable.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-fidelity-m3/SITREP-planner-20260712-231500.md` — run below.
- Grounding at `frank@8941889`: `executor.go:84-95` (double class+timeout refusal, read); `registry.go:97-113` (three entries, no side_effecting); my locked `s8-design-m3-registry` §4:90 (default-DENY + per-entry allow), §6 (`unsafe` refusal), §12(g) item 3 (skipped/unsafe → self_reported), the r1a sunset ruling (static gate → live m-6 prompt; floor NOT sunset).

ACTIONS_GIT_REF: none — a fidelity/naming return; no `frank/` edit, no commit, no token. Sole artifact: master/relays/s10-build-fidelity-m3/SITREP-planner-20260712-231500.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); read the s10 branch via `git -C frank show 8941889:…`; `git -C frank status --short` clean at `8941889`.
Next requested action: s10 pair folds legs 1–3 into the T10 execution seam (the pre-spawn-gate RED/GREEN); master + m-7 take the ungrounded-F-b executor-run question if the GREEN must execute (separable, not T10-blocking). m-3.planner holds — report-only until addressed in TO.
