## DESIGN — rev2: F1–F5 folded → m-1.implementer fresh review @ SHA-256 `734df728…` (claims narrowed to honest; classes owner-real; at-rest descriptor-grain; one spec-mismatch route named, not taken)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m1
PARENT_DISPATCH_ID: step3-relock-dag-m1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the revision stays inside the released TCB scope (class (ii) emptied rather than a manifest surface added; the redaction claim narrowed to the ratified ceiling)
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner
DESIGN_DOC_ID: step3-relock-dag-m1
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-relock-dag-m1/DESIGN-REVIEW-implementer-20260721-234730.md

m-1.implementer — all five findings were correct; F1 and F4 were the important ones (an overclaimed boundary and an ownerless surface are exactly what this lane exists to prevent). Fresh bytes @ SHA-256 **`734df728ccdd15eee675ef09c05b55023b1c27c298efcf3f2ea77de33cd3bc74`** (rev2 header note; rev1 `41385dfc…` superseded).

### The folds
1. **F1 → §2.1 the claim SPLIT:** structural ONLY where structure exists (typed object routes — credential/secret-holder/replay/authority objects have no writer input variant); **open content gets the honest confusion-not-malice ceiling, not a byte-absence guarantee** — governed paths inject nothing, sentinels test accidental absence, and deliberate same-UID extraction (a `bash cat` of the credential file into a tool result) is OUTSIDE any guarantee until H-12, stated where it bites. **The spec-mismatch route is NAMED, not taken:** I read the ratified "no S-A/S-B in the log" row as ceiling-scoped (coherent with the amendment's own residual); if master/VP intend absolute, it routes up — scanning is not claimed either way.
2. **F2 → §2.2 the authority-object family completed:** **F59 tickets** (the ratified "no credential, no ticket, no epoch grant" made the m-1 rule), USE capabilities, broker control tokens, connector/control handles, **any future authority-tagged type — fail-closed evolution** (the writer gate is over SOURCE OBJECT TYPES; unrecognized ⇒ non-loggable); credential references separately classed non-secret/non-authorizing/**non-loggable**; `turn_epoch`/generation ids preserved as attribution facts.
3. **F3 → §2.3 descriptor-grain** (the m-7 §2.12 symmetry, cited): verified 0700 parent; `O_CREAT|O_EXCL|O_NOFOLLOW` creation; `O_NOFOLLOW|O_CLOEXEC` reopen; regular-file/owner/exact-mode/link-count-1/dev-inode verified ON the open descriptor; replacement races reject; segment rotation in the verified dir + dir durability; **containment on the opened object + resolved ancestry, never lexical path**; the RED battery in §5 (symlinked parent, swapped file, hard link, mode drift, race).
4. **F4 → §1 owner-real:** **TWO positive classes + deny-all** (the "three classes" phrasing retired); **class (ii) EMPTY for the MVP** — no m-10 manifest env member exists and I invent no producer surface (a future need = a master-routed m-10 schema addition); class (i) **enumerated exactly, no `LC_*` wildcard** (`LANG` + `LC_ALL` = `C.UTF-8` only), each var with a pinned source + value constraint — **dedicated empty tool-HOME** (kills ambient default-credential discovery at the env grain, honestly bounded: absolute-path reads remain the residual), dedicated per-run TMPDIR, fixed approved-toolchain PATH with containment constraints.
5. **F5 → §1 the digest ruling corrected:** the no-oracle-by-shape claim **WITHDRAWN** — a whole-set digest IS a confirmation oracle for one unknown member; safety comes from the secret-free input (now fully system-sourced with class (ii) empty); a secret-bearing value would make the digest a secret-derived artifact (noncompliant provisioning, undetectable by content — stated). **One canonical preimage bound:** JCS over ONE `{name: value}` object, construction-time duplicate rejection, UTF-8-valid values (non-UTF-8 = pre-spawn typed reject), so m-9 and the E3 observer hash the same logical set. The parent-env RED leg unchanged.
6. **Preserved (your bar item 6):** frozen `7c8b09a6…` unmoved; K6 exclusion; F57-narrow; construct-from-empty; producer-first parking (§4 criteria-only, nothing attached).

### Requested
Fresh uniquely-parented `DESIGN_REVIEW_VERDICT` on the exact bytes at `734df728…`. On approve → the SITREP to master (bytes + hash; parked halves named). Producer confirmations + the §D co-sign stay parked regardless.

## Verification
- Folds cross-checked item-by-item against your revision bar 1–6 and the cited producer evidence loci (the m-9 record union + at-rest rules; the m-10 manifest absence).
- `shasum -a 256 <doc>` = `734df728…`; frozen base + released dispatch hashes reproduced unchanged.
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/step3-relock-dag-m1/DESIGN-planner-20260721-235452.md` — OK (below).

ACTIONS_GIT_REF: rewrote the delta doc (rev2; frozen contract untouched) + this relay + INDEX row; no code, no `frank/`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched by this seat.
Next requested action: m-1.implementer reviews the fresh bytes; on approve I SITREP to master; producer halves stay parked until their pair-approved bytes land. m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
