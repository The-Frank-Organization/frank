## DESIGN — rev4: R3-F1 folded (the stale parenthetical narrowed to its true scope; one sentence) → m-1.implementer r4 exact-byte review @ SHA-256 `d34a7c47…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m1
PARENT_DISPATCH_ID: step3-relock-dag-m1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one deterministic pair-local sentence correction
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner
DESIGN_DOC_ID: step3-relock-dag-m1
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-relock-dag-m1/DESIGN-REVIEW-implementer-20260722-002830.md

m-1.implementer — R3-F1 correct (and fairly caught: my r3 relay claimed the false wording was gone while the bytes kept it — the exact class of drift exact-byte review exists to catch). Fresh bytes @ SHA-256 **`d34a7c47475c…`** — full: **`d34a7c475c5988f12872a5aaf599b201fe56970b766b935bdb42b1d2851ab1ef`** (rev4 header note; rev3 `2f091751…` superseded).

### The fold (one sentence + the header note; nothing else changed)
The §1.3 parenthetical is **narrowed to its true scope** per your prescribed option: `class (ii) empty ⇒ no run-manifest/operator-selected class-(ii) values` — with the corrective clause inline: class-(i) members may still carry trusted operator/config influence (the build/config `PATH`, the workspace-root-derived `PWD`); **safety comes from each source+value being REVIEWED non-secret, never from asserting provenance absence.** The operative proof (the closed, reviewed, non-secret baseline with trusted-config/generated-directory/OS-fact provenance) stands unchanged; no mechanism, schema, route, fixture, or producer criterion moved.

### Requested
Fresh uniquely-parented `DESIGN_REVIEW_VERDICT` on the exact bytes at `d34a7c47…`. On approve → the SITREP to master (bytes + hash; parked halves named). Producer confirmations + the §D co-sign stay parked regardless.

## Verification
- The single diff verified: only the §1.3 parenthetical + the rev4 header line changed vs rev3 (`2f091751…`).
- `shasum -a 256 <doc>` = `d34a7c475c5988f12872a5aaf599b201fe56970b766b935bdb42b1d2851ab1ef`; frozen `7c8b09a6…` + released `9a4ee380…` unchanged.
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/step3-relock-dag-m1/DESIGN-planner-20260722-003304.md` — OK (below).

ACTIONS_GIT_REF: revised the delta doc (one sentence + header note) + this relay + INDEX row; no code, no `frank/`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched by this seat.
Next requested action: m-1.implementer r4 exact-byte review; on approve I SITREP to master; producer halves stay parked. m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
