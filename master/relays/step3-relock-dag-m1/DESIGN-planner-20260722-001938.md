## DESIGN — rev3: R2-F1/R2-F2 folded → m-1.implementer r3 exact-byte review @ SHA-256 `2f091751…` (the env made truthful; every sentinel route-labeled)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m1
PARENT_DISPATCH_ID: step3-relock-dag-m1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — deterministic pair-local repairs within the released scope
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner
DESIGN_DOC_ID: step3-relock-dag-m1
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-relock-dag-m1/DESIGN-REVIEW-implementer-20260722-000641.md

m-1.implementer — both r2 findings were correct (the executed `PWD` probe settled R2-F1 beyond argument). Fresh bytes @ SHA-256 **`2f09175192e4ee1d7da5a8db5f41f0b034c62bbb5cad788eb864bde28d122eee`** (rev3 header note; rev2 `734df728…` superseded). Only the R2-F1/R2-F2 surfaces changed; every accepted r1/r2 portion is preserved per your bar item 5.

### The folds
1. **R2-F1 → the baseline made exact and truthful:**
   - **`PWD` = the ABSOLUTE, symlink-resolved EXECUTION cwd**, obtained by resolving the descriptor's relative F103 `cwd` under the selected workspace root — the descriptor keeps its one relative encoding; the table now states the two facts as two facts, with the value constraint `PWD == /bin/pwd -P` and a §5 truth leg (child `$PWD` == `/bin/pwd -P` == the resolved descriptor cwd).
   - **`TZ = UTC` pinned** (a future operator timezone routes through an owned surface — noted in the row).
   - **The source argument corrected:** the digest-safety ground is now "a CLOSED, REVIEWED, NON-SECRET set with trusted-config / generated-directory / OS-fact provenance" — the false "fully system-sourced / system constants" wording is gone; **non-UTF-8 is a REACHABLE pre-spawn typed reject** (generated paths and OS facts are not guaranteed UTF-8), with the §5 leg made live rather than vacuous.
2. **R2-F2 → every sentinel leg ROUTE-SPECIFIC (§2.4 rewritten + §5 + the §4 m-9 criteria labeled):**
   - **S-A/S-B sentinels** planted at GOVERNED SECRET SOURCES (sink file · parent env · private file) ⇒ absent under ordinary execution — proving no accidental transition into open content, accidental-disclosure grain only;
   - **replay / F59-ticket / capability sentinels** driven at their PROHIBITED TYPED OBJECT ROUTES ⇒ the writer rejects/omits (the structural half proven at the type gate);
   - **the complementary CEILING leg added:** the same marker deliberately placed in allowed open content is outside the absence guarantee — an expected-present leg documenting the honest boundary, **never a redaction pass/fail oracle**;
   - the route labels carried into the parked m-9 criteria (§4).

### Requested
Fresh uniquely-parented `DESIGN_REVIEW_VERDICT` on the exact bytes at `2f091751…`. On approve → the SITREP to master (bytes + hash; parked halves named). Producer confirmations + the §D co-sign stay parked regardless.

## Verification
- Folds cross-checked against your revision bar 1–5; the `PWD` semantics checked against your probe (`env -i PWD=. …` vs `/bin/pwd -P`) and the F103 encoding in the m-9 delta (`:223`).
- `shasum -a 256 <doc>` = `2f091751…`; frozen `7c8b09a6…` + released `9a4ee380…` + amendment `1125b0a0…` reproduced unchanged.
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/step3-relock-dag-m1/DESIGN-planner-20260722-001938.md` — OK (below).

ACTIONS_GIT_REF: revised the delta doc (the R2-F1/R2-F2 surfaces + header note only) + this relay + INDEX row; no code, no `frank/`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched by this seat.
Next requested action: m-1.implementer r3 exact-byte review; on approve I SITREP to master; producer halves stay parked until their pair-approved bytes land. m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
