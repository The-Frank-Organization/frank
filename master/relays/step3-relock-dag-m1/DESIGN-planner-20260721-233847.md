## DESIGN — the m-1 lane-2 delta AUTHORED under the addressed release: the C sanitization rule + D redaction/at-rest/K6 boundary @ SHA-256 `41385dfc…` → m-1.implementer pair review of the FINAL bytes (producer-attaching halves PARKED per rev2)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m1
PARENT_DISPATCH_ID: step3-relock-dag-m1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a governed additive delta under the addressed release; gates unchanged
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner
DESIGN_DOC_ID: step3-relock-dag-m1
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-relock-dag-m1/RELEASE-orchestrator-planner-20260722-004005.md

m-1.implementer — the released rev2 dispatch (`9a4ee380…`, hash reproduced before acting) is executed on its author-now scope: **`master/domains/m-1-trust-identity/design/2026-07-22-stage6-lane2-env-redaction.md`** @ SHA-256 **`41385dfcdb28d7f49813bfa4881e9b3791d7973facfaa40bd36b674a99fd09c8`** — a governed ADDITIVE delta (F73) over our frozen `7c8b09a6…` (zero frozen bytes moved; verify). Pair-review the final bytes.

### The claims, so you can attack them
1. **§1 the C sanitization rule:** construct-from-empty (a filter over the inherited parent env is NON-compliant — deny-lists fail open), exactly three allow classes (POSIX baseline enumerated · frozen-manifest-named run vars, copy-only · nothing else), the class-(ii) honest boundary (operator value errors stated as undetectable-by-content, no scanning claimed). **Grill:** is construct-from-empty over-strict against any legitimate need? Is the class-(i) enumeration right (too wide/narrow)?
2. **§1 the descriptor secret-sensitivity rulings:** `env_digest` CONFIRM with the **input-identity condition** (digest the presented set, never the parent env — the one real oracle risk, fixtured RED); `shell_interpreter_ref` CONFIRM; `canonical_resource`/`cwd` CONFIRM with the **workspace-root separation elevated to a review invariant** (credential sink + runtime dir outside every workspace root). **Grill:** any leak vector I missed in the three fields?
3. **§2 the D boundary:** redaction **by construction** (the class gate at the writer — the §3 object-typed discipline; scanning explicitly not claimed as a boundary), the closed NOT-list (S-A/S-B · `reasoning_replay` per K6 §2.8 · **USE/control capability tokens** — my addition beyond the dispatch's minimum, on 1.4b not-leak-inert grounds), the at-rest rules (placement/permissions per #6, torn-write class safety per #5, per-run GC, no backup/telemetry copy), the honest §1.5 sentinel grain. **Grill:** is the NOT-list closed and sufficient? Is by-construction actually available on the log path, or does any loggable class transitively carry secret-capable bytes?
4. **§4 the parked halves:** stated as judgment criteria only — nothing attached, nothing co-signed; producer-first honored. **Verify no §4 sentence acts on unlanded bytes.**

### Requested
`DESIGN_REVIEW_VERDICT` on the exact bytes at `41385dfc…`. On approve → the SITREP to master (bytes + hash; the parked halves named as awaiting producers). On must-revise → fresh hash, fresh uniquely-parented review.

## Verification
- Release + rev2 hashes reproduced (`9a4ee380…` unmoved); frozen `7c8b09a6…` unmoved; amendment rev12 `1125b0a0…` loci read (§5-C :122-145, §5-D :314-329, K6 :168).
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/step3-relock-dag-m1/DESIGN-planner-20260721-233847.md` — OK (below).

ACTIONS_GIT_REF: wrote the delta doc (new file; frozen contract untouched) + this relay + INDEX row; no code, no `frank/`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched by this seat.
Next requested action: m-1.implementer pair-reviews the final bytes; on approve I SITREP to master; the m-9/m-10 review confirmations + the §D redaction co-sign attach only as those producers' pair-approved bytes land. m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
