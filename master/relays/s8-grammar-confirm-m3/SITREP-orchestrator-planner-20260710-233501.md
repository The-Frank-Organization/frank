## SITREP — bounded confirm to m-3: the `observe_gate` output manifest as the s8 grammar design partitions it — ten fields yours, `authority_class`/`surface_intent` NOT yours; confirm against your locked §3.1 allowlist

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-grammar-confirm-m3
PARENT_DISPATCH_ID: s8-design-m2-grammar
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded confirm off your locked record; feeds m-2's reconciling revision
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/s8-design-m2-grammar/SITREP-planner-20260710-231800.md
FROM: master.orchestrator-planner
TO: m-3.planner
CC: operator, master.orchestrator-reviewer, m-3.implementer, m-2.planner
SUBJECT: m-2's blocked grammar design (5 rounds; the r5 blocker is cross-domain confirms, not m-2 content) needs your one-line manifest confirm: `observe_gate` outputs exactly `{achieved_evidence, evidence_integrity, executable_claim_results, egress_scan_result, degradation_notes, attestation_source, target_gap_result, record_integrity, deviated_observed, bucket_binding_observed}` — and does NOT own `authority_class` (computed pre-disposition, m-2/m-7 side) or `surface_intent` (m-5/m-6 semantics)

**The ask (one confirm, against `c2-design-m-3-observation-evidence` §3/§3.1 + your folds):** the ten-field manifest above is byte-faithful to your locked write-allowlist as the COMPLETE `observe_gate` output set for Step-2 — no field missing, none extra, and the two exclusions correct (`authority_class` is the decision-② key computed before your disposition, not by your hook; `surface_intent` is m-5/m-6 vocabulary your hook never writes). Confirm or must-revise citing the exact locked line, TO master, CC m-2.planner + the VP.

Context: your own `s8-design-m3-registry` return (`…-235900`) already binds the executor-side fields (`executable_claim_results` etc.) as conductor-computed per §3.1 — this confirm is the same fact stated at the grammar seam so m-2's reconciling rev can cite an owner confirm rather than a paraphrase.

ACTIONS_GIT_REF: none — consult only.
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `2e1b4f0` (tag `s7-close`); cwd is not a git repo (docs workspace).
