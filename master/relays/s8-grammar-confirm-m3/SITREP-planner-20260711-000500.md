## SITREP — m-3.planner: CONFIRM the ten-field `observe_gate` manifest + both exclusions (byte-faithful to locked §3.1/§3/§9 for the Block-A observe-manifest partition); 2 precision notes for m-2's grammar rev

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-grammar-confirm-m3
PARENT_DISPATCH_ID: s8-design-m2-grammar
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-2.planner, m-3.implementer
IN_REPLY_TO: s8-grammar-confirm-m3/SITREP-orchestrator-planner-20260710-233501.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: CONFIRM — the 10-field manifest is byte-faithful to my locked write set as the Block-A observe-manifest partition, and both exclusions (`authority_class`, `surface_intent`) are correct; 2 precision notes so m-2's rev doesn't read the ten as the entirety of §3.1's hook-write surface

**CONFIRM.** The ten-field manifest is byte-faithful to my locked write set (§3.1 + §3 signature + §9) as the **complete m-3-owned partition of the Block-A observe manifest** (`registry.json:147-158`), and both exclusions are correct. No must-revise.

### The ten — all m-3-owned observe_gate outputs
`achieved_evidence` · `evidence_integrity` · `executable_claim_results` · `egress_scan_result` · `degradation_notes` · `attestation_source` · `target_gap_result` · `record_integrity` · `deviated_observed` · `bucket_binding_observed` — each is in my locked §3.1 observer-only write-allowlist (`…2026-06-29…:61`), the §3 `observe_result` signature, or the §9 routing-profile observed set (`deviated_observed` + `bucket_binding_observed`), all `owner: system`. Conductor-written, lane-unwritable (§3.1 positive allowlist + s5-b (h) guard).

### Both exclusions correct
- **`authority_class` NOT mine.** It is the **decision-② key, computed *before* my disposition** — `record_kind ∈` authority-bearing set **or** `gate_category ∈ A` (§3.2(d); m-2's declared `bool·owner:system·computed_result`). My hook **reads** it to choose `held` vs deliver; it does **not** write it. Corroborated in-tree: `registry.json:155` is the only Block-A row with **`owner: computed`** (the other eleven are `owner: system`) — the grammar already encodes the distinction. ✓ exclude.
- **`surface_intent` NOT mine.** m-5/m-6 surface/archetype vocabulary; absent from my §3.1 allowlist; my hook never writes it. ✓ exclude.

### 2 precision notes (for m-2's rev — not a manifest correction)
1. **The ten are the Block-A partition, not the entirety of §3.1's hook-write surface.** My §3.1 allowlist *also* has the hook write **`ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`** (conductor-observed git provenance — DI-5) and the **pass/fail `veto`**. Those are **envelope/control-flow, not Block-A registry rows**, so they are correctly *outside* the grammar's observe-manifest — but m-2's rev should read the ten as "the Block-A observe fields m-3 owns," **not** as "the observe hook writes only these ten." (`predicate_result`/`veto`/`observed_fields` from the §3 signature are likewise control-flow/carriers → the terminal token, not persisted Block-A rows — consistent with my s5-fidelity Q9 answer.)
2. **Two of the ten are dormant/defaulted in Step-2** — ownership unchanged, disposition scoped: `egress_scan_result` is m-3's, but the egress **scan is dormant until away-mode** (§7), so in Step-2 it is `not_applicable`; `attestation_source` is m-3's (the §4/§13 **m-3-F5** provenance marker — a conductor-set addition to the literal §3.1:61 enumeration, which predates that fold), defaulting to `conductor` in Step-2 since the `operator`-attested E4 path is Step-3+.

This is the same fact my `s8-design-m3-registry` return (`…-235900`) bound executor-side (§6: the conductor computes `executable_claim_results`/`achieved_evidence`/`target_gap_result`; the executor writes nothing), now stated at the grammar seam. No c2 reopen; byte-exact.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-grammar-confirm-m3/SITREP-planner-20260711-000500.md` — run below.
- Anchors: locked `…2026-06-29…:61` (§3.1 allowlist), §3 signature, §9 (routing-profile observed set), §3.2(d) (`authority_class` = decision-② key), §4/§13 (`attestation_source` m-3-F5), §7 (egress dormant-until-away-mode); frank `2e1b4f0` `registry.json:147-158` (12 Block-A rows; `authority_class` uniquely `owner: computed`).

ACTIONS_GIT_REF: none — a bounded confirm off my locked record; no git action, no `frank/` edit, no code. Sole artifact: master/relays/s8-grammar-confirm-m3/SITREP-planner-20260711-000500.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `2e1b4f0` (tag `s7-close`).
Next requested action: m-2's grammar rev may cite this owner confirm for the ten-field observe-manifest partition + the two exclusions; note the §3.1-superset (ACTIONS_GIT_REF/FINAL_GIT_STATUS_SHORT/veto envelope+control-flow) so the observe-hook write surface isn't under-read. m-3.planner holds — report-only.
