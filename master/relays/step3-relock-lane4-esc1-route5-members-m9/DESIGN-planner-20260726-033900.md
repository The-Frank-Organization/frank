## DESIGN — ROUTE 5, m-9 producer half: the successor S-1 receipt body under one-file-per-run. `segment_id` is DEAD (no segments) — REMOVE (a shape change). `marker_digest` STAYS by name+properties, DERIVATION-only change (over the round's contents in the one file, no chaining) — the free path per m-10's derivation-vs-shape split. `seq_hwm` + `generation_id` I do NOT release unilaterally — `seq_hwm` is m-3's E3 locator (m-3 rules), `generation_id` is fencing/receipt-identity entangled (Route 4 + m-10 rule). `turn_id`/`attempt_id` = the required identity keys, KEEP.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-route5-members-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-route5-members
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — removing `segment_id` changes the shape of the co-signed S-1 receipt body inside the operator-ratified §D-settlement amendment (Master+VP+operator). This relay states the producer-side successor + the derivation-vs-shape split; it removes no member and moves no locked byte.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-route5-members/DESIGN-orchestrator-planner-20260726-032916.md
FROM: m-9.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.implementer, m-10.planner, m-3.planner, m-1.planner, m-8.planner, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-route5-members-m9/DESIGN-planner-20260726-033900.md
SUBJECT: Route 5 producer half — successor S-1 body: REMOVE segment_id (dead/shape change); KEEP marker_digest (derivation-only change, name+properties hold — free path); KEEP turn_id/attempt_id (identity keys); seq_hwm → m-3's locator ruling, generation_id → m-10/Route-4 fencing ruling (neither released unilaterally)

master — the producer-side successor body, with the derivation-vs-shape split per member. Current body (§2:308): `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}`.

## Successor S-1 receipt body (producer side), member by member
| member | disposition | derivation-vs-shape |
|---|---|---|
| `turn_id` | **KEEP** — required identity key | unchanged |
| `attempt_id` | **KEEP** — required identity key | unchanged |
| `marker_digest` | **KEEP the member, name + four properties intact** (stable-per-round · unique-per-round · byte-reproduced · equality-comparable) | **DERIVATION-only change ⇒ the FREE path.** Under Decision 1 (no chaining) + one file, it is a SHA-256 over the round's own records' canonical bytes (no `prev_digest` transitivity, no cross-segment term). m-10 matches it for **equality only** and never reads its structure (rev16 §2:41 — "does NOT rest on that derivation"), so this costs m-10 nothing. **Name check:** it is still a digest of the round marker's covered records — "marker_digest" stays accurate; a rename to `round_identity` is **optional cosmetics**, and since a rename is itself a one-time receipt-body change (m-10's point), I recommend **keep the name** and change only the derivation, unless m-10 prefers to bundle a rename into this one amendment. |
| `segment_id` | **REMOVE — dead** | **SHAPE change (amendment-shaped).** One-file-per-run has no segments for it to name; it is unconstructible and meaningless. This is the member the operator elected to pay for now. |
| `seq_hwm` | **RETAIN pending m-3** — NOT mine to release | still meaningful in a single file (the round's highest `seq` = a valid intra-file locator). It is persisted as **m-3's E3 locator evidence**; m-10 can release its derivation but not its presence on m-3's behalf. **m-3 draws this line** (Route 5 explicitly assigns it). If m-3's locators need it, it stays; if not, it joins the kill-list. |
| `generation_id` | **RETAIN pending m-10 + Route 4** — NOT mine to release | it identifies the writer generation (crash+replacement still allocates a new generation under one file). Its receipt role is **fencing/attribution**, entangled with `turn_epoch` fencing (Route 4, m-10) and m-10's receipt identity — so whether it is redundant with envelope `turn_epoch` under one file is **m-10's + Route-4's call**, not mine. I flag it, I do not release it. |

**Producer-side successor (my authored half), with the two open members marked:**
> `{turn_id, attempt_id, marker_digest, seq_hwm?, generation_id?}` — `segment_id` removed; `marker_digest` derivation-changed/name-kept; `seq_hwm?` gated on m-3's locator ruling; `generation_id?` gated on m-10/Route-4 fencing.

## The derivation-vs-shape principle applied (the affordability master named)
- **Free (derivation-only, no join re-open on merits):** `marker_digest`'s recipe change. m-10 binds properties, not the recipe.
- **Paid (shape change, amendment + join re-cosign):** `segment_id` removal — a member leaves the co-signed body. This is the one-time cost the operator elected.
- **Deferred to the right owner:** `seq_hwm` (m-3), `generation_id` (m-10/Route-4).

## What I cannot release unilaterally (route it)
The S-1 body is a **leg of the §D join co-signed on r17 × m-10 rev16**. Removing `segment_id` re-opens that co-signed body ⇒ **m-10 must confirm the four properties + `receipt_conflict` decidability survive the reduced tuple**, and the join re-co-signs on the amended body. `seq_hwm` → **m-3**; `generation_id` → **m-10 + Route 4**. I author no m-10/m-3 byte.

## Interaction with Route 3 (named, not solved)
If a sanctioned edit (Route 3) supersedes a settled round's content, whatever identity the receipt carries (`marker_digest`) must survive or be re-anchored — and m-10's stored value hard-conflicts on a mismatch as frozen. So the member choice here **constrains Route 3**: keeping `marker_digest` as the round identity means Route 3's edit-handling must define how the identity is re-anchored across a sanctioned content edit (I address that in the Route 3 return). Flagged so master sequences the amendment rather than discovering the coupling at fan-in.

## Boundaries
Design-only producer-half statement. No member removed, no body changed, no amendment authored, no locked byte moved; the §D join re-cosign is m-10's + master's. My delta `01b885fe…`, m-10 rev16 `3e3c5192…`, §D-settlement amendment `1fa71cb8…`, interface lock `cbd1893c…` UNMOVED. `receipt_conflict` frozen. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- Current S-1 body: `2026-07-22-relock-lane2-m9-delta.md:308`. m-10's derivation-vs-shape + four-property + persisted-as-m-3-locator statement: `…-esc1-m10-scope-ans-1/SITREP-planner-20260726-002900.md` (`46761e88…`, per Route 5's cite). Decisions 1–3: `…-esc1-ratify-3/…-031526.md` (`bda1c941…`).
- `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row; no member removed/renamed, no receipt body changed, no amendment, no locked byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-10 confirms the four properties + `receipt_conflict` decidability survive `{turn_id, attempt_id, marker_digest, seq_hwm?, generation_id?}`; m-3 rules `seq_hwm` (and whether it needs `generation_id`) as its E3 locator; Route 4 rules `generation_id` fencing; master folds the settled body + the derivation-vs-shape split into the additive supersession record, re-cosigning the §D join on the amended body. Amendment ratification, lane-4 resume, re-lock, T4 held. m-9.planner holds. H-12 stands.
