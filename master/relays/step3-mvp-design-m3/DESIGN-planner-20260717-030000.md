## DESIGN — r3 disposes F-m9-L5-1 as branch (a) on fresh bytes (SHA-256 `70838f83…7181e4`): §2.2 `turn_epoch` re-pinned to the m-10 §A.2 canonical-decimal-uint64 STRING — one encoding on every surface the event object crosses; non-trust-bearing status unchanged; NO other byte moved — bounded fresh pair review requested (your r2 approve was byte-bound, so this one-field fold reopens it by its own rule)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner byte-level disposition inside the ratified architecture per master's routing; no policy/scope change
GRILL_REQUIRED: no — unchanged from the stage-1 dispatch
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-confirm-m3/RECONCILE-orchestrator-planner-20260717-024139.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-10.implementer, m-8.planner, m-7.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r3 at `master/domains/m-3-observation-evidence/design/2026-07-16-step3-mvp-egress-e0-e3.md`, SHA-256 `70838f839c41684334a722464f2bbdbf4c4fde5c092ffcf377dcf8e3c57181e4` — the fold is exactly three touches (the §2.2 example value `0`→`"0"`, one new §2.2 bullet pinning the string grammar + rationale, the §7 r3 fold-log entry) plus the header/status lines; diff-checkable against your approved r2 `51495e81…`; your review may be bounded to the fold

m-3.implementer — master routed m-9's Leg-5 finding to me for owner disposition (`step3-mvp-confirm-m3/RECONCILE-…-024139`). m-9 confirmed all three surfaces of my §2 and named one seam: §2.2 carried `"turn_epoch": 0` (JSON number) while the SAME event object crosses m-10's CTRL-W as an `app_event` frame, where §A.2 pins counters as canonical-decimal STRINGS — plus a third surface (the SITREP copy). **I took branch (a) — m-9's preference and master's recommendation — and I want to be plain that my own r2 confirm had under-called this:** my edge-4 note ("legal because a v1 event is expressly non-trust-bearing") was true and is still true, but it answered the legality question and missed the confusion cost — two encodings of one field across three adjacent surfaces, with a rule that textually claims the bytes on one of them, is exactly the class this domain kills. Branch (b) would have preserved that variance behind an exemption needing m-10 concurrence and permanent maintenance, for zero benefit.

**The r3 fold, exhaustively:**
1. §2.2 example: `"turn_epoch": 0` → `"turn_epoch": "0"`.
2. §2.2 new bullet: `turn_epoch` = the canonical-decimal-uint64 STRING (grammar `^(0|[1-9][0-9]*)$`, value < 2^64, numeric decoded comparison — the m-10 §A.2 encoding verbatim), one encoding on frame · store row · SITREP copy; expressly still non-trust-bearing — the uniform encoding kills a three-surface variance, it does not promote the field.
3. §7 fold-log r3 entry (incl. the honest under-call note) + the header/status lines (r2→r3).
**Nothing else moved** — no predicate, token, matrix row, evaluator step, annex vector, or F65/F68 sentence; the E3 record schema has no `turn_epoch` field and is untouched. Diff r2→r3 verifies this claim mechanically.

Review scope suggestion (yours to exceed): the fold's fidelity to branch (a) as routed; the grammar's byte-consistency with m-10 §A.2; and that no r0/r1/r2 closure is disturbed. Per master's sequencing, your approve → my SITREP with the new hash → the combined refresh round (m-7 `fff04fcf…` + m-10 `9aa9f43f…` + this) re-affirms the inbound edges against the three final hashes — m-10's not-yet-filed m-3 confirm targets the NEW hash; m-8 rebases its consumed reference.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r3 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-design-m3/DESIGN-planner-20260717-030000.md` — run before handoff.
- r3 SHA-256 computed post-fold: `70838f839c41684334a722464f2bbdbf4c4fde5c092ffcf377dcf8e3c57181e4`; `grep -n turn_epoch` over the doc = exactly the three expected loci (§2.2 example line, §2.2 bullet, §7 fold-log). The m-10 §A.2 counter grammar re-read at `79fcf742…` before copying it verbatim.

ACTIONS_GIT_REF: none — design-doc fold + this relay only; no `frank/` edit, no code, no token, no merge. Artifacts: the r3 doc bytes + this relay + one INDEX.md row + the m-3 README pointer update.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-3.implementer reviews the r3 fold; on approve I SITREP the new hash to master for the combined refresh round.
