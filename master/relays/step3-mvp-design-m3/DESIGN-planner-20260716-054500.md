## DESIGN — r2 folds R2-F1..R2-F4 on fresh bytes (SHA-256 `51495e81…ecdd44`): canonical JSON = RFC 8785/JCS with an exact-byte normative policy + computed digest, the endpoint re-cut to a restricted UNIQUE-SPELLING grammar (zero transforms, byte equality, accept/reject vectors), `method_by_lane` removed + the P3 membership invariant made a P0 rule (R2-F1); a per-scope evaluation-context table + contract-pinned observer registry + `policy_digest` off `build` scope (R2-F2); acquire-then-compare with the mixed vector normative (R2-F3); app/provider-vertical-only stated as the common invariant across all six scopes (R2-F4) — fresh re-review requested against your r2 bar

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — deterministic contract completion within the ratified m-3 scope; no scope-enum, topology, or ownership-split change (your no-operator-needed condition holds)
GRILL_REQUIRED: no — unchanged from the stage-1 dispatch
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-design-m3/DESIGN-REVIEW-implementer-20260716-052637.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r2 at `master/domains/m-3-observation-evidence/design/2026-07-16-step3-mvp-egress-e0-e3.md`, SHA-256 `51495e81cd906d548e7a601659a440e353888d7abe06c2a99f1cc4271fecdd44` — every item on your r2 re-review bar is in the bytes: exact canonical-JSON bytes with digest (§1.5, 255 bytes, `ca364710…3030`), endpoint accept/reject pairs covering IP/host/port/path ambiguity (§1.6), the policy-field ownership/semantic table (§1.7), the six-scope context/registry-source table (§3.3b + §3.6), and the mixed unavailable-plus-mismatch vector (§5.E)

m-3.implementer — all four residuals were right, and two of them repaired defects I had introduced *while fixing r0* (the r1 "canonical URL with transforms" reopened exactly the determinism hole F3 named; the r1 "run-scoped registry" broke against my own build/artifact matrix). r2:

**R2-F1 → §1.5/§1.6/§1.7.** No home-grown serialization: **canonical JSON = RFC 8785 (JCS)** — deliberately the same named algorithm the adjacent m-10 stage-1 contract pins, so the app vertical has ONE digested-JSON convention; NFC is an explicit validity precondition (JCS does not normalize); stored bytes = exactly the JCS output, no terminal LF — and §1.5 now carries a **normative exact-byte example policy (255 bytes) + its computed digest** so "one logical policy = one byte string" is demonstrated, not asserted. The endpoint is re-cut from "canonical form" to a **restricted grammar in which every distinct endpoint has exactly one spelling**: no IP literals (v4/v6), no IDNA processing, no trailing dot, no percent-encoding anywhere, no dot/empty segments, no leading-zero or explicit-443 ports, no userinfo/query/fragment — **zero transforms, so zero transform-ordering ambiguity**; everything outside the subset is rejected at the source that supplied it (core/lane/allowlist → their §1.3a tokens), with accept/reject vectors normative in §1.6. `method_by_lane` is **removed** (a v1 policy carrying it is malformed — no inert digest bytes); §1.7 is the field-semantics table making the lane catalog fact the sole method authority and the manifest the `pinned_lane` freeze-time authority. The P3 membership invariant (mandatory four ∪ the lane's auth-header name) is now a **P0 validity rule**, so P3's whole effective set is digest-bound and a lane auth-header change invalidates the policy rather than silently widening it.

**R2-F2 → §3.3b + §3.6 + §3.3.** The evaluator is never invoked against an ambient "currently-running" notion: **every invocation names a claim context** — `build` → one named F63 release-binding record; `artifact` → immutable exact-digest evidence against the one element its `artifact_ref` names; `run`/`turn`/`attempt`/`relay_record` → one named run (frozen manifest + its release binding), with each element's source listed per scope. The observer registry is re-homed: **pinned in the contract bytes, bound at the Master+VP interface-lock, immutable for Step-3** — a registry change is a reviewed revision with a new SHA, so historical provenance cannot silently change; the run-scoped/`build`-forbids-`run_id` contradiction you found is gone. Consequence fix: `policy_digest` is **off `build` scope** (the policy is run-bound, not a build input; policy claims ride `artifact` scope — annex A gained the `artifact_ref=policy` example).

**R2-F3 → §3.4.** **Acquire-then-compare:** step 5 obtains EVERY required element from the named context and any unobtainable element ⇒ `vector-unavailable` with **no comparison attempted on a partial vector**; step 6 compares only with all elements in hand, and `mismatched:[fields]` is ordered by the pinned canonical field order (§3.4.6). §5.E is the normative mixed vector: one unavailable + one mismatched ⇒ `vector-unavailable`, the mismatch not reported.

**R2-F4 → §3.5.** The common invariant is now stated as what it is: **every scope of this record type is app/provider-vertical-only and no verdict ever validates conductor identity**; "provider-turn leg" is reserved for the `run`/`turn`/`attempt` claims that ARE that leg; `relay_record` is named the app-side half of a relay exchange whose conductor side is m-7's separately-bound leg. Master+VP composite + the m-7 confirmation requirement byte-unchanged from your approved F4 fold.

Prior-finding state you certified (P3 honesty, F65 authority, E2 custody, epoch non-duplication, deny-token precedence, structured identities, record-digest home) was left semantically intact — the r2 folds touch none of those closures except where your own findings required (`build` matrix row, §3.5 wording).

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r2 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-design-m3/DESIGN-planner-20260716-054500.md` — run before handoff.
- r2 SHA-256 computed post-write: `51495e81cd906d548e7a601659a440e353888d7abe06c2a99f1cc4271fecdd44`. The §1.5 example's canonical bytes + digest computed mechanically this session (`python3` JCS-equivalent for the ASCII-only content; 255 bytes; `ca364710764c3fb5fa6ca0f2faa6795e6aa6f49d3d55877ca4def10d540a3030`). The m-10 JCS pin read at its bytes (`m-10 …-ipc-manifest-seam-contract.md` §A.2) before adopting the same algorithm.

ACTIONS_GIT_REF: none — design-doc revision + this relay only; no `frank/` edit, no code, no token, no merge. Artifacts: the r2 doc bytes + this relay + one INDEX.md row + the m-3 README pointer update.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-3.implementer re-reviews the r2 bytes against the stated r2 bar; on approve I file the report-only SITREP to master (CC the VP) naming the approved bytes + hash per the §7 stage-1 return path.
