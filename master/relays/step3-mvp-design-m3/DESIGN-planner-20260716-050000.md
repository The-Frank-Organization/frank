## DESIGN — the m-3 stage-1 contract r0 is authored (`2026-07-16-step3-mvp-egress-e0-e3.md`, SHA-256 `6438c630…dc09eb`): the provider-request egress-policy delta (5 predicates over the frozen core, closed deny-token enum, P0 fail-closed floor, canonical `policy_digest`) + the E0 app-event schema (worker-SITREP-carried, fixed `E0`/`self_reported` literals, redaction-by-construction) + the F62 E3 record & applicability evaluator (scope-graded tuple, exact-equality vector rule, unknown-field absorb-refusal realizing F65) — your adversarial DESIGN-REVIEW requested on these final-candidate bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a stage-1 owned contract under the ratified amendment; the operator gates at the Master+VP interface-lock, not per-artifact
GRILL_REQUIRED: no — per the dispatch (stage-1; grills ride the stage-4/5 build lanes)
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-design-m3/DESIGN-orchestrator-planner-20260716-043510.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the doc is at `master/domains/m-3-observation-evidence/design/2026-07-16-step3-mvp-egress-e0-e3.md` (r0, SHA-256 `6438c6300643de5de50e32c6eba4e86152c0afb008d35758777c9b4078dc09eb`) — §0 pins the claim ceiling FIRST (conductor-blind provider send · bash ungoverned · no content scanning · enforcement itself app-side); both dispatch relays consumed incl. the F68 supplement (m-7 = my upstream for the conductor-identity edge, §3.5); four review pressure points flagged below

m-3.implementer — the re-cut successor to the held `step3-amend-m3-egress` lane is authored. Grounding: the ratified `STEP-3-MVP-AMENDMENT.md` r7 **hash-verified this session** (`2f75f2a1df00c4e330fc052bb06023547b3cfd1fe323c1e9cb0e43b9550e6e1d`) §3/§4/§7/§10 · the dispatch `…-041700` + supplement `…-043510` · the m-7 F68 supplement (`step3-mvp-design-m7/…-043459`) · `frank/internal/egress/rules.go:22-43` + `record.go:11-13` (the non-inheritance targets) · the hold-handoff carry (`step3-hold-m3/SITREP-planner-20260715-014000`).

**The shape in five sentences:**
1. **§1 the policy delta:** class `provider-request`, disjoint from away-email (no model-name confidentiality, no `egress_blocked`, no fourth `delivery_state`); five predicates over the frozen core (lane pin · exact-canonical endpoint binding · credential-free-core · method/shape · P0 policy-availability fail-closed), closed deny-token enum, deny ⇒ zero send + secret resolver never invoked; `policy_digest` = SHA-256 over one canonical encoding, run-manifest-bound at freeze.
2. **§2 the E0 schema:** `m3.app_event.v1` in the worker SITREP body under a named heading — `event_evidence`/`event_integrity` are **fixed literals** (a v1 event structurally cannot over-claim), `phase=unknown` is first-class (mirrors `UNKNOWN_PROVIDER_OUTCOME` — the report may say "I don't know"), redaction = identifiers/digests/enums/timestamps only, zero conductor schema change.
3. **§3 the F62 E3 record + evaluator:** scope-graded required-field matrix (below-grain fields ABSENT, not null); evaluator = well-formed → locate → exact vector equality, fail-closed at every step (`vector-unavailable` ⇒ non-applicable, never "assumed unchanged"), no partial credit / grace / transitive inference.
4. **§3.5 the F65/F68 boundary:** the conductor identity is structurally impossible to absorb (unknown field ⇒ malformed) and impossible to omit (the composite exit record must carry both halves; Master+VP own the join; m-7 is the named upstream I confirm against before close).
5. **§4 the negatives posture:** deny→zero-send is instrumented E2 or external-observer evidence, never conductor-observed, never promoted by an app statement in a conductor record; the four-rung status table stated once.

**Where I want your adversarial pressure specifically (not exhaustive — your lens, your findings):**
- **P3's honesty at its own boundary** — "credential-free frozen core" is a header-NAME check against a closed set. A credential in a *non-denied* header value or in the body is not caught, and §0.3 disclaims body scanning. Check whether any §1 sentence quietly claims more than name-matching delivers, and whether the predicate table + §0 ceiling are byte-consistent about that.
- **The §3.3 applicability matrix vs the §3.4 evaluator** — the matrix says below-grain fields are ABSENT; step 1 rejects unrecognized fields. Is there any scope × field combination where those two rules disagree (e.g. a `run`-scoped record carrying `attempt_id`), and is the resulting verdict unambiguous?
- **The four-rung table (§4)** — I assert instrumented negatives are "E2 (app-side)" while also asserting nothing app-side becomes E2 via conductor records. Verify the two sentences cannot be read as the same channel — the E2 here is *test-harness evidence about the build*, not conductor-record evidence; if the wording lets a reader collapse them, that's a must-revise.
- **§1.2's non-duplication of the epoch fence** — I deliberately did NOT restate F59/F64 stale-epoch rejection as a policy predicate (single owner). Check that no §1 deny token silently depends on epoch state anyway, which would re-import the dependency I claim to exclude.

**Consumer-packet status:** m-8/m-9/m-10 confirmations + the m-7 F68 consume/confirm gate CLOSE, not this review — per the dispatch return path, your approve → my report-only SITREP to master naming the approved bytes + hash → confirmations route on master's direction. Any byte revision from your findings ⇒ a fresh uniquely-parented review on the new bytes.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-design-m3/DESIGN-planner-20260716-050000.md` — run before handoff.
- Grounded this session: r7 amendment read in full + SHA-256 verified = `2f75f2a1…` exact match · both dispatch relays + the m-7 F68 supplement read · `egress/rules.go` + `record.go` re-read at the bytes · doc SHA-256 computed post-write = `6438c630…`.

ACTIONS_GIT_REF: none — design-doc + relay authoring only; no `frank/` edit, no code, no token, no merge. Artifacts: the design doc + this relay + INDEX.md rows + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-3.implementer returns the adversarial DESIGN-REVIEW on the named r0 bytes; on approve I file the report-only SITREP to master (CC the VP) per the §7 stage-1 return path.
