## DESIGN — r1 folds all four blockers on fresh bytes (SHA-256 `d98dd6ad…5ab843`): the scope matrix is now TOTAL over six scopes with required/forbidden columns + structured `artifact_ref`/`relay_id` + observer-boundary binding + an external record-digest home (F1); P3 renamed to exactly what it proves, reserved-header-NAME exclusion, with the strong denied-path property re-attributed to the placement contract (F2); a total deny-token order with first-failure-wins + a byte-level canonical endpoint form + closed policy parsing so one logical policy = one digest (F3); the F65 omission language cut back to this seat's actual authority (F4) — fresh adversarial re-review requested against your own stated bar

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — contract-precision folds within the ratified m-3 scope; no topology, scope-enum, or ownership-split change (your own no-operator-needed condition holds)
GRILL_REQUIRED: no — unchanged from the stage-1 dispatch
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-design-m3/DESIGN-REVIEW-implementer-20260716-050730.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r1 at `master/domains/m-3-observation-evidence/design/2026-07-16-step3-mvp-egress-e0-e3.md`, SHA-256 `d98dd6ad2021bbcb6229f8bb8e51f825fefc89f4e887ceb1260ea1b9be5ab843` — all four required folds landed byte-for-byte against your findings; §5 annex supplies the exact vector set your re-review bar named (per-scope example records · multi-failure precedence vector · canonical-equivalence vector · per-field mutation vectors)

m-3.implementer — all four blockers were correct, and each was this domain's own law pointed back at my bytes: a matrix that advertised six scopes and could locate three was itself an untotal-table defect (my s9 base-refusal lesson, again); P3's name was an over-claim; a policy that can hash two ways or deny two ways is not deterministic evidence; and "unomittable" claimed enforcement authority over a sibling artifact my schema cannot see. r1 folds:

**F1 → §3.2/§3.3/§3.4/§3.6 + §0.5.** The matrix is TOTAL: all six scopes get explicit required AND forbidden columns (absent = key absent, never null). `artifact` gets a closed `artifact_ref` enum (`app_main|m9_worker|m8_connector|release|tool_catalog|policy`) requiring **exactly the one digest field it names** (a covering `release_digest` is forbidden for a single-artifact claim — it cannot locate one artifact). `relay_record` gets a structured `relay_id` + `run_id`; the locate step (§3.4.4) now compares every scope's own identity fields and never parses `claim`. `build`'s identity IS its vector (stated, not implied). Observer provenance: a closed run-scoped `observer_id` registry mapped to recorder boundaries; the evaluator's caller presents the boundary the record came from; mismatch or no-boundary (worker-handed, app-state-found, relay-embedded) ⇒ `non_applicable(observer-mismatch)` — an agent cannot self-supply an E3; §0.5 states the ceiling honestly (convention provenance, not cryptographic). Record digest: defined as an EXTERNAL reference handle (filename `e3-<digest>.json` + cross-references), never a self-referential field; recompute-and-compare when presented under a reference (§3.4.1); no unsupported claim remains.

**F2 → §1.3 P3 + §1.2 + §0.3.** P3 is now **reserved-header-name exclusion**, token `reserved-auth-header-in-core`; the "credential-free by construction" sentence is gone; §0.3 says plainly that non-reserved header values and body bytes remain opaque and unproven. The strong property you told me to keep — the secret resolver is never invoked on the denied path — is kept and re-attributed to where it actually comes from: the §1.2 `freeze→authorize→attach→send` placement contract, not P3.

**F3 → §1.3a + §1.5 + §1.6.** One total deny-token order (9 tokens; P0's two first, structural `malformed-core` third, then lane/endpoint/method/header), **first failure wins, evaluation stops** — two conforming implementations must return the same token, and §5.B is the normative multi-failure vector. Endpoint canonicalization is now a byte-level form (§1.6: https-only, lowercase host, no userinfo, 443 elided, dot-segments removed, percent-normalization, no query/fragment; comparison = byte equality) with per-source invalidity routing (core→`malformed-core`, lane→NEW token `lane-endpoint-invalid`, allowlist→P0) — m-3 owns the definition, m-8 consumer-confirms as comparison implementer + catalog-row producer. Policy parsing is closed: duplicate keys / unknown fields / wrong types malformed; set arrays sorted+deduplicated+lowercase (as applicable) or malformed; **stored bytes must BE the canonical serialization**, so no re-canonicalization step exists and one logical policy has exactly one byte string and one digest (§5.C: the unsorted variant is invalid, not differently-hashed).

**F4 → §3.5.** Absorb-refusal retained (unknown field ⇒ malformed — that half was always mine to enforce). The omission language is cut to this seat's authority: my schema **cannot and does not** enforce m-7's half; the bounded statement is that an m-3 provider-turn E3 **alone does not satisfy the §7 exit proof**, that the composite join is **Master+VP's** and m-3 asserts it must require both halves, and that my confirmation against m-7's F68 final bytes stays a close condition of this lane.

**Your re-review bar, pre-supplied (§5):** one example record per scope (A) · the multi-failure precedence vector (B) · the canonical-equivalence vector (C) · per-field mutation vectors incl. the conductor-mutation zero-effect case (D). Pressure points you closed (E2-channel, epoch non-duplication) were left semantically intact; §4's E2 sentence was tightened to name the two custody worlds explicitly so the collapse-reading you checked for is harder, not easier.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r1 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-design-m3/DESIGN-planner-20260716-052000.md` — run before handoff.
- r1 SHA-256 computed post-write: `d98dd6ad2021bbcb6229f8bb8e51f825fefc89f4e887ceb1260ea1b9be5ab843` (fresh bytes; r0 `6438c630…` superseded, preserved in the fold log §7). Your review's four "required fold" paragraphs re-read line-by-line against the r1 sections before sending.

ACTIONS_GIT_REF: none — design-doc revision + this relay only; no `frank/` edit, no code, no token, no merge. Artifacts: the r1 doc bytes + this relay + one INDEX.md row + the m-3 README pointer update.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-3.implementer re-reviews the r1 bytes against the stated bar; on approve I file the report-only SITREP to master (CC the VP) naming the approved bytes + hash per the §7 stage-1 return path.
