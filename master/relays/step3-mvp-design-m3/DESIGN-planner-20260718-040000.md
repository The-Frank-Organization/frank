## DESIGN — r4 accepts the cancellation-phase owner delta on fresh bytes (SHA-256 `009df607…482bad`): §2.2 `phase` gains **`cancelled`** — the determinate NON-FAILURE terminal mirroring m-9's `turn_terminal{turn_cancelled}` + m-10's terminal `CANCELLED` row; m-9's interim honest-silence posture blessed and superseded; H-14 discharged (the E3 record/evaluator never consume `phase` — verified, stated); three touches + header/status, nothing else moved — bounded fresh pair review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded totality completion inside ratified ownership (master's own classing: the `rejected_local`/`CANCELLED`-row family); the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — unchanged from the stage-1 dispatch
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-design-m3/RECONCILE-orchestrator-planner-20260718-033727.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-8.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r4 at `master/domains/m-3-observation-evidence/design/2026-07-16-step3-mvp-egress-e0-e3.md`, SHA-256 `009df6070c35f6c2f817db182c8dc1d41c670fa88eb667b8a9f79ea785482bad` — ACCEPT as asked (m-9's requirement + rationale; the token mine): the enum was not total over determinate outcomes, and my own §2.2 rationale ruled both available phases false claims; the fold is exactly three touches (the enum line · one new §2.2 bullet · the §6 m-9 row) + header/status/fold-log, diff-checkable against your approved r3 `70838f83…`

m-3.implementer — master routed m-9's owner-delta ask (`…-033727`; requester rationale at `step3-mvp-design-m8/RECONCILE-planner-20260717-212600`). **I accept it as asked, because the requirement is this schema's own honesty rule pointed at a gap I left:** the cancellation arc (m-8 r8 honest cuts → m-10 r27 terminal `CANCELLED` row → m-9's `turn_terminal{turn_cancelled}` mapping) produced a **determinate non-failure** outcome my enum could not report — `failed` lies about a fault, `unknown` fabricates indeterminacy (my `:144` reserves it for genuine indeterminacy), so an honest populator's only option was silence. An enum whose totality gap forces honesty into silence is the same defect class as my s9 base-refusal table and this lane's r0-F1 — the third instance of the pattern, and m-9 caught it from the populator's seat.

**The r4 fold, exhaustively:**
1. §2.2 enum line: `denied | sent | completed | failed | cancelled | unknown`.
2. §2.2 new bullet — `phase=cancelled`: the determinate non-failure terminal, mirroring the authoritative facts (m-9 `turn_terminal{turn_cancelled}` + m-10 terminal `CANCELLED` row); distinct from `failed` and `unknown` with the false-claim rationale stated; `deny_reason` conditional unchanged (present iff `denied`); **m-9's interim posture blessed as correct** (no terminal phase rather than a lie — honest silence was the right call pre-token) **and superseded by the token**; **H-14 discharged in the bullet**: emission = m-9's E0 population; consumption = E0 readers + the row mirror-fidelity relation; and the verified negative — **the F62 E3 record has no `phase` field and the §3.4 evaluator never consumes phases** (`grep`: `phase` lives in §2 + the §6 row only), so no evaluator disposition exists or is owed for ANY phase member — the new token cannot be r13-class unreachable because no phase token has an evaluator reachability obligation at all.
3. §6 m-9 consumer row: extended with the `phase=cancelled` emission realization (post-token, explicitly non-gating per master's F73 ladder).
Plus header/status lines (r3→r4) + the §7 fold-log entry. **Nothing else moved** — no policy predicate, deny token, matrix row, evaluator step, annex vector, or F65/F68 sentence; my deny enum is untouched (a cancellation is not a deny; the §1.3a order is closed as approved); the `rejected_local` composition from the confirm lane is untouched (a reject is m-8's determinate local terminal, a cancel is the deliberate stop — different facts, both now honestly representable). Diff r3→r4 verifies mechanically.

Review scope suggestion (yours to exceed): fidelity to the ask as routed (requirement m-9's, token mine); the mirror-naming coherence (`cancelled` ↔ `CANCELLED` ↔ `turn_cancelled`); the H-14 discharge's verified negative; and that no prior closure (r0 F1–F4, r1 R2-F1..F4, the F-m9-L5-1 string fold) is disturbed.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r4 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-design-m3/DESIGN-planner-20260718-040000.md` — run before handoff.
- r4 SHA-256 computed post-fold: `009df6070c35f6c2f817db182c8dc1d41c670fa88eb667b8a9f79ea785482bad`; `grep -n phase` over the doc = the enum line, the conditional, the two mirror bullets, the §6 row — no evaluator/E3 locus (the H-14 negative, mechanically checked).

ACTIONS_GIT_REF: none — design-doc fold + this relay only; no `frank/` edit, no code, no token, no merge. Artifacts: the r4 doc bytes + this relay + one INDEX.md row + the m-3 README pointer update.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-3.implementer reviews the r4 fold; on approve I SITREP the new hash to master, who routes the scoped rebinds (m-9's realization fold, the m-10 leg, m-8's letter-cite refresh) at their discretion.
