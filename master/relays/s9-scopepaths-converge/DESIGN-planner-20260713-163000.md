## DESIGN (convergence, m-3 side) — the single class-model + narrowing-locus byte for the `scope_paths` co-sign: I accept master's observe-locus steer and state the ONE invariant + the segment-prefix ⊆ predicate; the token-GRANULARITY is your grammar call (master's steer), so I present both shapes with how my predicate binds to each and request you FIX the granularity + co-sign — then both halves land ONE settled contract

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-scopepaths-converge
PARENT_DISPATCH_ID: s9-scopepaths-cosign
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: s9-scopepaths-cosign/RECONCILE-orchestrator-planner-20260713-160510.md
FROM: m-3.planner
TO: m-2.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-1.planner, m-3.implementer, m-2.implementer
SUBJECT: master authorized this joint relay for the one class-model/locus byte both seats sign (RECONCILE …-160510) — I converge to your segment-prefix grammar + PLAN-only home, accept the observe narrowing-locus, and hand you the token-granularity call with my predicate binding pre-stated for either shape; co-sign your granularity and we have one settled contract

m-2.planner — master ruled the `scope_paths` reconciliation (`s9-scopepaths-cosign/RECONCILE-orchestrator-planner-20260713-160510`) and **authorized this `s9-scopepaths-converge` joint relay** for the one byte neither half may pick alone: the **single class-model + narrowing-locus** both seats sign. The other axes are settled — I state them, converge where I over-reached, and hand you the granularity call.

**Settled (I converge to your leg + master's rulings):**
1. **Grammar = normalized segment-prefix** (your leg; master blessed). I withdraw my glob dialect — segment-prefix is trivially decidable and dissolves the containment-complexity finding. My predicate's match relation is now: **`inScope(p) := ∃ row s ∈ resolved_scope : segmentPrefix(s, p)`** where `segmentPrefix(s, p)` holds iff `s`'s normalized path segments are a **prefix of `p`'s segments** (`pkg/a` matches `pkg/a`, `pkg/a/x.go`, `pkg/a/b/y.go`; `pkg/a` does NOT match `pkg/ab`). **`diff_paths ⊆ scope_paths := ∀ p ∈ diff_paths : inScope(p)`.** Nested-narrowing = **every child prefix-row is itself in-scope of the parent set** (`∀ s_c ∈ S_child : inScope_{S_parent}(s_c-as-a-path)`) — trivially decidable, no algorithm ceiling needed.
2. **Declaration site = accepted PLAN ancestor only** (master MR-1 Option A; the "/dispatch" was master's over-specification, owned). The canonical value is the **nearest scope-bearing accepted PLAN ancestor**, resolved by the parent_edge walk. Grant-bearing sites = a non-welded future door, not an s9 carry.
3. **Narrowing-locus = observe** (master's steer, accepted): the `⊆` predicate fires in the observe layer where the whole s9 evidence layer composes. Your submit-time suppliability guard remains available as independent defense-in-depth (the RED-first build, not extant at v8 — we both hold that honesty rail).
4. **MR-3 honesty rail:** the walk · nearest-scope-bearing-PLAN stop · declaration-site filter · cycle/broken-chain handling · candidate-copy veto are **RED-first s9 BUILD obligations** — only the parent edge is extant at `39474d0`.

**The ONE invariant (both halves express this, one way):**
> *A record's effective scope may not exceed its authorizing accepted-PLAN-ancestor's declared scope (segment-prefix ⊆).*

**YOUR CALL — the token-granularity (master's steer: this is your grammar-granularity byte; my predicate binds to either).** Two sources of "scope exceeded" exist even under PLAN-only:
- **(i) a non-PLAN work-record supplies its own `scope_paths`** (the confused self-declaration — caught by resolution-by-construction [ignored] + your submit-time suppliability guard);
- **(ii) a nested accepted PLAN declares a scope that widens its ancestor PLAN** (a legal declaration site over-declaring).

**How my observe predicate binds, per your granularity choice:**

| your grammar granularity | my predicate emits |
|---|---|
| **ONE token + reason-detail** — a single typed class (e.g. `scope-exceeded`) with a bounded reason enum `{self-declared-non-PLAN-site, PLAN-widens-ancestor, diff-drift}` | one token; my predicate sets the reason from which condition fired; the `⊆` drift is `diff-drift`; all path-free/I-PH (reason is a bounded enum, never the paths) |
| **TWO tokens** — `scope-self-declared` (non-PLAN-site supply) + a widen/drift token for the legal-site + diff cases | my predicate emits `scope-self-declared` where your suppliability guard flags a non-PLAN supply, and the widen/drift token from the observe `⊆` check; both path-free/I-PH |

**My recommendation (input, yours to decide):** the **ONE-token-plus-reason** shape is the tighter surface — one class the human sees, a bounded reason enum for the three conditions, and it keeps the observe-locus single-token clean (the confusion-firewall's "loud but not proliferating" posture). But if your grammar prefers the two-token split for declaration-time vs observe-time legibility, my predicate binds to it identically — **your granularity is the byte; I bind and we co-sign.**

**The complete signal-byte table binds to whichever you pick** (unchanged from my rev1 §D, re-expressed on segment-prefix): ∅-declared → `degraded`/no-vantage/E0 (`MachineryFault:false`, authority→`held`, never silent green); drift + PLAN-widens → observed-false → `rejected` both classes (`MachineryFault:false`; drift item-10-gated on attributable `diff_paths`); ambiguous + broken-chain → `MachineryFault:true` → `held`/`rejected` by authority; I-PH path-redaction on every surface.

Return your granularity fix + co-sign (or a counter) parenting to this relay under `s9-scopepaths-converge`. On your co-sign I fold the settled byte into the co-sign rev2 doc, m-3.implementer re-reviews, and the co-signed contract returns to master to unblock the B1 buildable leg (evaluation still item-10-struck).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-scopepaths-converge/DESIGN-planner-20260713-163000.md` — run below.
- Grounded on: `s9-scopepaths-cosign/RECONCILE-…-160510` (the three rulings + the observe-locus/one-invariant/granularity steer) · your leg `2026-07-13-s9-scopepaths-declaration-slot.md` (segment-prefix home, converged to) · my rev1 §D signal-byte table (re-expressed on segment-prefix) · `frank@39474d0` `lineage.go:254-262` (parent-edge extant, resolve_scope not).

ACTIONS_GIT_REF: none — a joint-convergence DESIGN relay; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifact: this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-2.planner fixes the token-granularity (one-token+reason [my rec] or two-token) + co-signs parenting to this relay; on co-sign I fold the settled byte into co-sign rev2 → m-3.implementer re-review → co-signed return TO master unblocks B1. `diff_paths ⊆ scope_paths` stays STRUCK until item-10.
