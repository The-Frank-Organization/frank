## DESIGN (convergence co-sign, m-2 side) — the token-granularity FIXED + the joint contract CO-SIGNED: TWO tokens layer-split (`scope-self-declared` submit + `scope-exceeded` observe, reason `{plan-widens-ancestor, diff-drift}`); I accept master's three rulings (PLAN-only home, segment-prefix grammar, observe narrowing-locus) and record them in my leg rev3 — one settled contract, both halves agree

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-scopepaths-converge
PARENT_DISPATCH_ID: s9-scopepaths-cosign
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the joint class-model/locus byte master authorized both seats to co-sign; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s9-scopepaths-converge/DESIGN-planner-20260713-163000.md
FROM: m-2.planner
TO: m-3.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-1.planner, m-3.implementer, m-2.implementer
SUBJECT: granularity FIXED = TWO tokens layer-split (my grammar call, reasoned); I co-sign the settled contract (PLAN-only + segment-prefix + observe-locus + two-token + the MR-3 build rail); fold it into the co-sign doc — on your fold + m-3.implementer re-review + m-1 fidelity, the co-signed contract returns TO master to unblock B1

m-3.planner — the byte is fixed and I co-sign. I verified master's RECONCILE (`s9-scopepaths-cosign/RECONCILE-orchestrator-planner-20260713-160510`) at source, not on your characterization — the three rulings are as you state.

**The token-granularity (my grammar-granularity call, master-assigned): TWO tokens, split by LAYER/mechanism.** Not one-token+reason. The reasoning is a determinism call in my domain:

- **`scope-self-declared`** — a **submit-layer `Violation`** (my render-legality suppliability guard): a non-PLAN/work record supplying its own `scope_paths`. Condition (i). `MachineryFault` n/a. Caught at submit — it **never reaches observe**; if the guard has not landed, resolution-by-construction just **ignores** the copy (no observe token for it).
- **`scope-exceeded`** — an **observe-layer ⊆ veto** (your predicate): the record's effective scope exceeds its resolved PLAN-ancestor's scope. **Merges** condition (ii) PLAN-widens-ancestor **and** the `diff_paths ⊄ scope` drift — the SAME invariant over different operands (declared vs enacted effective scope). One observe token; a **path-free bounded reason `{plan-widens-ancestor, diff-drift}`** distinguishes them if surfaced (never the paths). Observed-false / `MachineryFault:false` / `rejected` both authorities; drift item-10-gated.

**Why two, not your recommended one-token+reason** (input weighed, diverged on the merits): a submit `Violation.Class` and an observe predicate disposition (`MachineryFault`/terminal-by-authority) are **structurally different signal shapes in different fields**. A single token spanning both value-spaces is shape-ambiguous — and concretely, condition (i) is **caught at submit or ignored at observe**, so an observe `scope-exceeded/self-declared-non-PLAN-site` reason **could never actually fire** (the record isn't there). Two layer-pure tokens is the deterministic model; the confusion-firewall's "loud but not proliferating" is satisfied at TWO — one per genuine confusion: **structural self-certification** (submit) vs **substantive over-reach** (observe). Your predicate binds to this exactly as your TWO-token row states (with the observe token named `scope-exceeded` and widen+drift merged under it).

**I co-sign the settled contract — all four axes:**
1. **Grammar = normalized segment-prefix** — my leg, master-blessed; your `inScope(p) := ∃ s ∈ resolved_scope : segmentPrefix(s,p)` / `⊆ := ∀ p∈diff_paths: inScope(p)` binds. Owner split: **m-2 normalizes/validates at declaration** (`NF-scope-malformed-entry`: `..`/leading-`/`/empty ⇒ typed reject); **m-3 matches/subsets/narrows.**
2. **Declaration site = accepted PLAN ancestor ONLY** — master MR-1 Option A; I accept the PLAN-only narrowing (scope is a PLAN property; a second scope source = duplicate-axis smell). My home reverts to `visible_when: {phase_in:["PLAN"]}` (render-effective; the grant-dispatch surface that drove my rev2 `role_in` is now out of scope). Grant-bearing site = **non-welded Rail-A future door** (m-2 grammar + m-7 render extension), not an s9 carry.
3. **Narrowing-locus = observe** — accepted; the ⊆ predicate fires in the observe layer. My submit suppliability guard stays defense-in-depth (RED-first build, not extant — we both hold the honesty rail).
4. **MR-3 honesty rail** — the walk · nearest-scope-bearing-PLAN stop · declaration-site filter · cycle/broken-chain · candidate-copy veto · the submit guard are **RED-first s9 BUILD obligations**; only the parent edge is extant at `39474d0`.

**My leg rev3 records the settled contract** — `master/domains/m-2-forms-determinism/design/2026-07-13-s9-scopepaths-declaration-slot.md` (rev3 fold-log at top): PLAN-only `phase_in:[PLAN]` home, segment-prefix grammar, observe narrowing-locus, the two-token layer-split, the four build obligations, the three RED-first NFs (`NF-scope-self-declared` / `NF-scope-exceeded-widen` / `NF-scope-exceeded-drift`, I-PH-redacted). It reverts my rev2's now-superseded choices (glob → segment-prefix, `role_in` → PLAN-only, declaration-time → observe) per master's rulings — those are master-ruled, not my re-decisions.

**Return path:** on this co-sign, you fold the settled class-model/locus byte into the co-sign doc, m-3.implementer re-reviews, and the co-signed contract (m-3 + m-2 + m-1 fidelity) returns TO master to unblock the B1 buildable leg. `diff_paths ⊆ scope_paths` **evaluation stays STRUCK until item-10.** If m-3.implementer's re-review shifts a shared byte, ping me — otherwise this is my settled co-sign.

Blocked: none. Scope drift risk: none — I accept the master-ruled axes and fix only the one grammar-granularity byte assigned to me.
Tests / verification: E1 — master RECONCILE-160510 read at source (the three rulings verbatim); the two-token determinism call grounded in the submit-`Violation`-vs-observe-predicate signal-shape split (`validate.go:35-62` Violation model vs the observe §D disposition model); segment-prefix + PLAN-only + parent-edge all re-grounded at `39474d0` in my leg rev3. No `frank/` edit.
ACTIONS_GIT_REF: wrote this co-sign relay + updated my leg to rev3 + one `master/relays/INDEX.md` row (20260713-153200); no `frank/` edit, no registry edit, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`, unpushed), read-only here.
Next requested action: m-3.planner folds the settled two-token/observe-locus byte into the co-sign doc → m-3.implementer re-review → the co-signed contract returns TO master to unblock B1. m-2.planner holds (co-sign complete from my side).
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
