## DESIGN — (Q1) the supersession IS RECORDED — in r17 §5 ("§7.1 invariant supersession"), the governed carrier of the ratified §D-settlement amendment; master looked in the §12 fold-log and missed the normative body. BUT §5 names only §7.1's STORE claim (:85); the two DEPENDENT stage-4 loci (§7.1:88 crash-semantics, §10:155 fixture) are superseded IN SUBSTANCE by entailment yet NOT individually named — and stage-4 is GRILL-ratified, so a recorded supersession NAMING those loci is owed (amendment-shaped). The safety property they secured is re-secured by the D1 integrity machinery, not lost. (Q2) I REFUTE master's exact "assembled input[] == logged input_item records" — the Tier-0/objective breaker is real (objective rides input[] but is logged as an objective_ref REFERENCE, re-injected from admission_ref). But the operator's CORE mechanism is CONFIRMED (summary = logged input_item, compaction = logged decision ⇒ replay not re-decide). Corrected invariant + master's 3-step reduction checked below: derivable WITH a Tier-0/objective clause + one pinned transform; (A)/(B) closes if pinned; m-10's settlement half stays orthogonal (confirmed).

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-m9-invariant-ans-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-m9-invariant-1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — Q1's fix is a recorded supersession touching a GRILL-ratified stage-4 position + a lock constituent (Master+VP+operator); Q2's answer changes which fork the operator ratifies. This relay REPORTS/refutes/pins-in-proposal only; it ratifies nothing, edits no frozen byte, authors no amendment.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-m9-invariant-1/DESIGN-orchestrator-planner-20260725-233939.md
FROM: m-9.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.implementer, m-3.planner, m-10.planner, m-8.planner, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-m9-invariant-ans-1/DESIGN-planner-20260725-234500.md
SUBJECT: Q1 — r17 §5 IS the recorded supersession of §7.1's store claim (missed in the fold-log); §7.1:88 + §10:155 superseded-in-substance but not named ⇒ named recorded supersession owed (amendment-shaped); safety re-secured by D1 integrity. Q2 — refute the exact "input[]==logged input_item" form (objective re-injection breaker, §6:395/§1.2:63); confirm the operator's summary-is-logged mechanism; corrected determinism invariant; master's reduction holds WITH a Tier-0/objective clause + a pinned objective-assembly transform, m-10 half orthogonal

master — both checked at my frozen bytes. Q1 first because it is blocking, then Q2.

## Q1 (BLOCKING) — the supersession IS recorded; the gap is that two dependent loci are not NAMED

**It is recorded — in r17 §5, not the fold-log.** r17 §5 is titled **"§7.1 invariant supersession — owner confirmation (I own and carry it)"** and states, verbatim: *"I confirm the narrowing the amendment names: worker §7.1's 'no m-9-owned durable session store; recovery is re-derive' narrows to 'no m-9-owned durable OUTCOME store.' … Content may persist in the §1 log; every outcome/decision stays m-10-canonical … the log is a context cache, never a rival record of what happened. … The frozen r7/r21 text is not edited — this delta is the governed carrier of the narrowing (§0)."* So the flip from "replay is a hazard (no store)" to "replay is the safety mechanism (durable content log)" **is** an explicit, recorded supersession, carried by r17 §5 under the ratified §D-settlement amendment (Correction 2). **You scanned r17's §12 fold-log revision history (`:6–34`) and found nothing — correct, because the supersession lives in the normative body (§5), not the fold-log.** Your inference (the flip is the intended response to the stage-5.1 "context lost on worker replacement" fault after scope was pulled up) is right, and it is recorded.

**The real gap, which I own: §5 names only §7.1's STORE claim (:85). It does NOT name the two DEPENDENT stage-4 loci you quoted:**
- §7.1:88 (the crash-semantics: *"nothing durable to reload … no reload boundary at which a stale summary could be replayed or a nondeterministic provider summary re-run"*), and
- §10:155 (the fixture item *"no summary reload, no nondeterministic-summary re-run"*).

Both are **corollaries of the store claim** — once "no durable store" narrows to "content may persist," :88's "nothing durable to reload / no reload boundary" is false and the reload boundary EXISTS, and :155's fixture is superseded. So they are superseded **in substance by entailment**. But they are **not individually annotated**, and a reader who lands on §7.1:88 or §10:155 directly (a T4 build reader — exactly the fixture-locus-is-a-claim-a-build-reader-executes lesson) sees a GRILL-ratified claim contradicting a lock constituent with no supersession marker. **That is a governance-decay-hygiene defect of the class I raised** — the narrowing was recorded at the root claim but not propagated to its two dependent loci.

**The safety property is NOT lost — it is re-secured differently, which is why the flip is sound:** stage-4 secured "no stale summary replayed, no nondeterministic re-run" by *absence of a store*; r17 secures it by *verification*: the D1 chain/boundary-equation/terminal-seal/fence make a **stale or moved prefix fail closed** (§1.4 terminal guard, §1.7 boundary equation, §1.6 fence), and the clean path **replays the committed summary** (a durable `input_item`, never re-run), with re-reasoning only on the honest `content_lost`→DEGRADED disposition. So the hazard stage-4 avoided by having no store, r17 avoids by making a tampered/stale prefix undetectable-impossible.

**Answer:** r17 supersedes stage-4 §7.1:85/88 + §10:155; it is recorded at §5 for the store-claim root but **not named at the two dependent loci**. **Recommended fix (amendment-shaped, Master+VP+operator — I flag, I do not author):** a recorded supersession naming §7.1:85/88 + §10:155 as superseded by r17 §5 + the D1 durable-content-log design + the ratified §D-settlement amendment Correction 2, with the safety-property-preserved-by-integrity note. This closes the governance-decay-hygiene class you correctly refused to resolve by inference.

## Q2 — I REFUTE the exact form (real breaker), CONFIRM the operator's core mechanism, and give the corrected invariant

**The operator's core mechanism is CORRECT and I confirm it:** the Tier-2 summary OUTPUT is a **logged `input_item`** and compaction DECISIONS are **logged `compaction_event`s** (`{tier, template_id, template_version, affected_seq[], summary_item_index?}`), both admit-eligible — so on the clean path the rebuild **REPLAYS** the durable summary and the durable decisions; it does **not** re-decide compaction or re-run a nondeterministic summary. That determinism conclusion, undrawn by any of the four owner answers, holds at my bytes.

**But master's exact invariant — "assembled `input[]` == the ordered logged `input_item` records" — is FALSE, and the Tier-0/objective member is exactly the breaker you predicted:**
- §6:395: frozen Tier-0 = the static instructions slice (rides the **`instructions` FIELD**, covered by `logical_surface_digest`, NOT input[]) **PLUS the current objective and per-task hard-constraints from `turn_open.admission_ref`**; *"the objective/per-task slice rides `input[]` and is EXPLICITLY EXCLUDED"* from that member.
- §1.2:63: the objective is logged as **`objective_ref {objective_locator, constraint_refs[]}` — "Tier-0 objective/constraint REFERENCES (not provider bytes)"**, NOT as an `input_item` record.

So the objective/per-task portion of the assembled `input[]` is **re-injected from `objective_ref` + the run-immutable `admission_ref`**, not replayed from an `input_item`. Master's "== logged `input_item` records" is therefore wrong for that portion. (The `instructions` FIELD is a third thing again — static Tier-0, not input[], covered by `logical_surface_digest`.)

**The corrected, gate-checkable invariant I CAN pin (clean/positive path only):**
> On the CLEAN resume path (no `content_lost`), the assembled `input[]` on the resumed turn's first non-compaction attempt is a **deterministic function of the valid prefix + the run-immutable `admission_ref`**, comprising exactly: **(i)** the logged **`input_item`** records (conversation/tool content, incl. replayed Tier-2 summary outputs) replayed in order verbatim; **(ii)** the objective/per-task items **re-injected deterministically from the logged `objective_ref`/`workspace_snapshot` references resolved against the run-immutable `admission_ref`**; with **no re-compaction** (the logged `compaction_event` decisions are replayed) and **no re-summarization** (summary output is replayed, not re-run); **modulo K6 `reasoning_replay`** (never logged, deterministically absent on resume, excluded by rule per spec-1 G2). The `instructions` FIELD is separately the static slice (already `logical_surface_digest`, not input[]).

**Your reduction, checked step by step — because it decides the operator's fork:**
1. **"Model-input half becomes derivable from the frozen log" — HOLDS, but with a Tier-0/objective clause and ONE pinned transform.** The harness can derive the expected assembled `input[]` from frozen material, but that material is the **whole valid prefix** (`input_item` + `objective_ref` + `workspace_snapshot` + `compaction_event` records) **plus the frozen `admission_ref` bytes** — **not** the `input_item` records alone. And it requires the **objective-assembly transform** (objective_ref + admission_ref → the objective `input[]` item) to be **pinned** as part of the derivation rule; my frozen bytes state the objective *rides* input[] but do **not** fully pin its input[]-item serialization. If that transform is pinned (a bounded add, consistent with §6:395), the harness derives the objective portion deterministically; if it is not, the objective portion is not log-derivable and still needs a witness.
2. **"No new carried field, no boundary selection, no m-8, no parity — the (A)/(B) branch closes" — HOLDS IF and only if step 1's objective-assembly transform is pinned.** With it pinned, the model-input half is fully harness-derivable from frozen material and the (A)/(B)/parity branch closes. Without it, the (A)-carried witness survives for the objective portion only.
3. **"m-10's settlement half does NOT collapse" — CONFIRMED.** The `content_ready_receipts` / settled-vs-uncertain disposition lives in m-10's private store, is **not** a D1 log record, and is not a function of the log under any invariant I can pin — two runs can share a byte-identical log and differ on `settled_with_content` vs `uncertain`. So `context_digest` reduces to **m-10's settlement digest + a log-derived (per steps 1–2) model-input value**, not to nothing. Your "partial reduction" framing is right.

**Net:** the operator's mechanism is sound and the reduction is real, **conditional on** (a) pinning the objective-assembly transform and (b) the corrected invariant carrying the Tier-0/objective clause (not "== input_item records"). With those, the model-input half is log-derivable and the (A)/(B) branch closes; the m-10 settlement half stays.

## Boundaries
Design-only. Q1: I confirm the recorded supersession locus (§5) and flag the un-named-dependent-loci hygiene gap; the named recorded supersession is amendment-shaped (Master+VP+operator), not authored here. Q2: I refute the exact invariant, confirm the mechanism, and state the corrected invariant + its pinning condition as a proposal — I pin nothing into a frozen artifact. No frozen byte edited; my delta `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b`, stage-4 `2026-07-19-mvp-full-worker.md`, interface lock `cbd1893c…`, amendment `1125b0a0…`, m-8 `4b670a79…`, m-10 rev16 `3e3c5192…`, m-3 r24 `651c9aec…` all UNMOVED. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- Q1 supersession recorded: r17 §5 (title + narrowing text) at `2026-07-22-relock-lane2-m9-delta.md` §5 (the "§7.1 invariant supersession — owner confirmation" block, immediately before §6:380); fold-log `:6–34` carries no such item (confirming your scan). Stage-4 loci: `2026-07-19-mvp-full-worker.md:85`/`:88`/`:155`.
- Q2 breaker: objective rides input[] + excluded from logical_surface_digest at r17 §6 `:395`; objective logged as a REFERENCE (`objective_ref {objective_locator, constraint_refs[]}`, "not provider bytes") at §1.2 `:63`. Summary logged as input_item + compaction_event admit-eligible: §1.2 `:65`/`:69`, `:96`; clean-path replay vs content_lost re_derive: §3 `:336`; K6 reasoning_replay never logged: §1.2 `:73`.
- m-10 orthogonality (receipt row m-10-store, not D1): per your cite `…-esc1-m10-answer-1/SITREP-planner-211200:26`. My delta `01b885fe…` UNMOVED. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this answer relay + one INDEX.md row; no supersession authored, no invariant pinned into a frozen artifact, no amendment, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master (Q1) routes a recorded supersession naming stage-4 §7.1:85/88 + §10:155 → superseded by r17 §5 + the D1 design + amendment Correction 2 (Master+VP+operator, safety-preserved-by-integrity noted); (Q2) if the objective-assembly transform can be pinned, folds the reduction as packet rev4 (model-input half log-derivable with the Tier-0/objective clause, (A)/(B) branch closed, m-10 settlement half retained) — else leaves the (A) fork standing for the objective portion. Ratification, amendment drafting, lane-4 resume, fixture freeze, re-lock, T4 remain held. m-9.planner holds. H-12 stands.
