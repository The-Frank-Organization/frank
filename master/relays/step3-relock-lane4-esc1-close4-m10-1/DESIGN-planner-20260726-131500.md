## DESIGN — fencing CLOSURE, m-10 half: ONE exact observation shape (VP F6.2). Resolve three-vs-two to **one predicate, three observations**: 1 admitted-current positive control + 1 negative arm PARAMETERIZED over TWO MANDATORY sub-faults (STALE_EPOCH-class · WRONG_LEASE-class), both mandatory because they exercise DISTINCT frozen reject mechanisms (assign-gate tuple-mismatch vs lease-not-held). Record consequence: 3 fixture records on the one new §7 row; sample-weight = 0 governed turns + 0 tool calls (a fenced successor does zero work by construction), so the leg does NOT disturb the 30-turns/100-calls balance. m-9's WRONG_LEASE join is its call. For m-10.implementer exact-byte review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — resolves the m-10 half of the observation shape for exact-byte pair review; it adds no §7 row and freezes no fixture. The §7 row + sample-weight rebalance + ratification is Master+VP+operator via the fresh lane-4 plan.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: pending-implementer-review
DISPATCH_ID: step3-relock-lane4-esc1-close4-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close4-fencing
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close4-fencing/DESIGN-orchestrator-planner-20260726-044200.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close4-m10-1/DESIGN-planner-20260726-131500.md
SUBJECT: fencing observation shape resolved — ONE predicate, ONE positive + ONE parameterized negative arm with TWO mandatory sub-faults (STALE_EPOCH-class, WRONG_LEASE-class); 3 records on the new §7 row; sample-weight 0 turns/0 calls (fenced = zero work); m-9 owns the WRONG_LEASE join; requesting m-10.implementer APPROVE

m-10.implementer — the m-10 half of the single observation shape, reconciling my Route-4 "two negatives + one positive" with m-3's "two-armed predicate." For your exact-byte review (F1 pair-approval).

## The contradiction, and the resolution
My Route-4 return named **two** deterministic negative sub-faults + **one** positive; m-3's predicate `successor_admitted_at_current_epoch_under_valid_lease` was **two-armed**. These were two names for what must be **one** shape. Resolve to:

**ONE predicate, THREE observations = one positive control + one negative arm PARAMETERIZED over two mandatory sub-faults.**
- **Observation P (positive control):** install-proof whose trusted tuple **exactly matches** the current durable `{run_id, generation_id, turn_epoch, state_seq}` ⇒ the two-form assign gate opens exactly once ⇒ successor admits at the current epoch under a fresh lease ⇒ proceeds. Proves **discrimination**.
- **Observation N.a (STALE_EPOCH-class, MANDATORY):** install-proof carrying a `turn_epoch`/`state_seq` **below the current durable tuple** ⇒ tuple-mismatch ⇒ assign gate rejects fail-closed (rev16 §6:130).
- **Observation N.b (WRONG_LEASE-class, MANDATORY):** a **disposed generation whose lease was released at retirement** attempts to bind/admit ⇒ lease-not-held ⇒ fail-closed (single-writer, rev16 §4:55 + §B.4).

## Why BOTH negative sub-faults are mandatory (not collapsible to one)
They are **not the same fence** — they exercise **two distinct frozen reject mechanisms**, and dropping either leaves a distinct admission-fencing path untested:
- **N.a** is refused by the **two-form assign gate's tuple comparison** (`turn_epoch`/`state_seq` mismatch — the `state_seq`-closed stale-during-transition race, §6:130).
- **N.b** is refused by the **active-turn lease's single-writer invariant** (the disposed predecessor never holds the current lease, §4:55/§B.4).
A successor could pass one gate and be caught only by the other; a leg that exercised only one would certify only half the fencing. So the negative arm is **parameterized with two mandatory cases**, not a single case — this is the honest reconciliation of "two-armed" (m-3) and "two negatives" (me): **one arm, two mandatory parameter values.**

## Record + sample-weight consequence (the arithmetic master asked for)
- **Records:** the one new §7 row carries **3 fixture records** — P, N.a, N.b — each an independent observation, all three mandatory (a pass requires all three; N.a and N.b are not alternatives). This fits the successor **seven-legs / eleven-records** frame as **one leg contributing three records**; the exact per-row record tally across all seven legs is the fresh plan's to total, but this leg's contribution is fixed at **3**.
- **Sample-weight:** **0 governed turns + 0 tool calls.** A **fenced** successor (N.a/N.b) performs zero work by construction (the gate precedes all work — Route-4 item 4, observed not reachable), so it creates no governed turn and no tool call. The **positive control** (P) admits a successor, but the observation terminates at **admission** (the discrimination point); it need not run a full governed turn or tool call to prove the gate opened. So **this leg contributes 0 to the 30-turns / 100-tool-calls balance** — it must not consume budget from that accounting. I flag this explicitly so the fresh lane-4 plan does not misattribute turns/calls to the fencing leg; if l4 needs P to run a full turn for an independent reason, that is l4's plan choice, not a requirement of this predicate.

## m-9's WRONG_LEASE join — its call, not mine to state
Observation **N.b explicitly induces the disposed-but-live-predecessor boundary** the per-run writer fence guards, so m-9 **is joined** at N.b (master scoped this). What the illegitimate writer's attempt looks like **from the log-writer seam** (blocked / refused / recorded where) is **m-9's to state or close with reason** — m-10 attests the **admission-gate** outcome (no successor turn/lease/assign, recorded reject) but cannot attest another seat's writer fence. My N.b contract stands two-owner even if m-9 concludes its boundary is not observed by the final shape; if m-9 joins, its writer-fence observable composes at N.b.

## Boundaries
Design-only, my half, for pair review. No §7 row added, no fixture frozen, no sample-weight committed, no predicate typed (m-3's), no amendment, no byte moved. rev16 `3e3c5192…`, stage-6 amendment `1125b0a0…`, interface lock `cbd1893c…`, r1 amendment `528d6a98…` (unratified), lane-4 plan (successor pending) UNMOVED. The §7 row + weight rebalance + fresh plan + ratification stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- rev16 `3e3c5192…` — UNMOVED; §4:55 (atomic lease-at-current-epoch, single-writer), §6:130 (two-form assign gate keyed by the trusted tuple, tuple-mismatch fail-closed, `state_seq` closes the stale race), §B.4 (retire + release-lease).
- My Route-4 half (two negatives + positive; zero-work observed): `…-esc1-route4-m10-ans-1/DESIGN-planner-20260726-033500.md`. m-3's two-armed predicate + fork deferral: `…-esc1-route4-fencing-m3/DESIGN-planner-20260726-034130.md`. VP F6 + successor 7-legs/11-records: `…-esc1-amend-1/DESIGN-REVIEW-orchestrator-reviewer-20260726-040848.md:64-74`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close4-m10-1/DESIGN-planner-20260726-131500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this design-half relay + one INDEX.md row; no §7 row/fixture/weight/predicate/amendment authored, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte reviews this shape; m-3 confirms the predicate carries the one-positive/two-mandatory-negative arm + `observer_id`/`evidence_locator`; m-9 joins N.b or closes with reason; both pairs approve. Master folds the pair-approved fencing contract + the 3-record/0-weight consequence into the fresh lane-4 plan for r2. rev16 UNMOVED. H-12 stands.
