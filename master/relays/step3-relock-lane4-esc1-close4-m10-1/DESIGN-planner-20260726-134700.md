## DESIGN — fencing m-10 half, rev3 (folds M10-CLOSE4-R2-F1 + F2 + F3). F1: `neg.WRONG_LEASE` is now an explicit TWO-ACTOR sequence — actor A (disposed predecessor) rejected at m-10 admission pre-assign; actor **B (legitimate replacement)** assigned + turn_open'd, then its `flock(LOCK_EX|LOCK_NB)` on `session.lock` WOULD-BLOCKs (A still holds the OFD) ⇒ B does zero downstream work + emits the bounded m-9 fault. The m-9 observable is B's failed acquisition, NOT A's write (rev2 conflated them). F2: I bind only the CLOSED facts (positive = non-zero governed turn; both negatives = zero successor work + zero tool calls) and leave each negative's exact governed-turn `sample_weight` UNSET for the joint m-3+m-10+l4 rebalance. F3: stale m-9 refs corrected (132623 / `a9ca1952`). For m-10.implementer re-review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — folds the two-actor observable + the weight-boundary + reference findings into the exact m-10 half for pair review; adds no §7 row, freezes no fixture, commits no negative weight. The §7 row + rebalance + ratification is Master+VP+operator via the fresh lane-4 plan.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: pending-implementer-review
DISPATCH_ID: step3-relock-lane4-esc1-close4-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close4-m10-1-review-r2
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close4-m10-1/DESIGN-REVIEW-implementer-20260726-133745.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close4-m10-1/DESIGN-planner-20260726-134700.md
SUBJECT: fencing m-10 half rev3 — F1 folded: neg.WRONG_LEASE two-actor sequence with actor B (legitimate replacement's session.lock would-block failure) as the m-9 observable, not the predecessor's write; F2: only closed facts bound (positive = governed turn; negatives = zero work + zero tool calls), negative governed-turn sample_weight left to joint m-3+m-10+l4 rebalance; F3: refs corrected to 132623 / `a9ca1952`

m-10.implementer — rev3, folding **M10-CLOSE4-R2-F1/F2/F3**, all verified at m-9's current bytes before folding.

## M10-CLOSE4-R2-F1 — `neg.WRONG_LEASE` as an explicit TWO-ACTOR sequence (actor B is the m-9 observable)
Verified at m-9's fresh successor (`2026-07-26-fencing-observable-onefile.md` `a9ca1952…:21-29`) + review (`…close4-fencing-m9/DESIGN-REVIEW-implementer-20260726-132623.md:43-54`): under advisory `flock`, the disposed predecessor (A) **holds** the lock, so A's own write is **not** the would-block observable — the observable is the **legitimate replacement (B)** failing to acquire it. My rev2 assigned the m-9 event to A's write attempt — the actor conflation. Corrected to the exact two-actor controlled sequence:

| step | actor | event | owner / locator | work |
|---|---|---|---|---|
| **1** | **A — disposed predecessor** | stale admission attempt at the current durable tuple/lease | **m-10** admission gate ⇒ **reject pre-assign**, no `assign`, no `turn_open`; A never reaches the worker | zero (m-10-attested) |
| **2** | **B — legitimate current replacement** | assigned + `turn_open`'d, then `flock(LOCK_EX\|LOCK_NB)` on the dedicated `session.lock` **would-blocks** because A still holds the open-file description ⇒ B performs **no** recovery-read / journal attach / write / provider / tool / conductor work and emits the **bounded m-9 fault** | **m-9** writer-fence observable (B's failed acquisition + zero downstream work) | zero (m-9-attested) |

**m-10 attests step 1** (A's admission rejection) — owner-final on my side. **m-9 attests step 2** (B's `session.lock` would-block + zero work) — its writer-fence observable; I consume it into `neg.WRONG_LEASE`'s locator, I do **not** author it, and it composes once m-9's corrected pair-approved successor lands. **If the final fixture observes only actor A, the m-9 join CLOSES** and `neg.WRONG_LEASE` reduces to the m-10-only admission rejection (still mandatory) — I retain no nonexistent m-9 event.

## M10-CLOSE4-R2-F2 — bind only the CLOSED facts; leave negative governed-turn weight to the joint rebalance
My rev2 both disclaimed whole-leg weight AND fixed the negatives to "zero governed turns" — a contradiction, and m-3's bytes (`…close4-fencing-m3/DESIGN-planner-20260726-131130.md:48-53`) say the two refused admission attempts **"weigh on turn-attempt accounting"** with the exact `sample_weight` **co-owned** with m-10 + the fresh plan. Corrected — I bind ONLY what is closed:
- **`positive`:** a **non-zero governed turn** (admits and proceeds, contributes to the 30-turn budget).
- **`neg.STALE_EPOCH`, `neg.WRONG_LEASE`:** each refused fail-closed with **zero successor work and zero tool calls** (the gate precedes all work; observed not reachable).
- **Left UNSET (joint m-3+m-10+l4 rebalance):** how a **refused admission attempt** is represented in the **governed-turn `sample_weight`** — zero successor work proves zero tool calls + no admitted successor turn, but does **not** by itself settle whether a refused attempt counts as a turn-attempt in the sample accounting. I do **not** unilaterally bind `0 governed turns` for the negatives; that is the fresh lane-4 plan's joint accounting call (or a separately jointly-approved rule).

## M10-CLOSE4-R2-F3 — stale references corrected
Current m-9 review: `…close4-fencing-m9/DESIGN-REVIEW-implementer-20260726-132623.md` (my rev2 cited `…-132409.md`, which does not exist). Current m-9 proposed successor: `master/domains/m-9-model-runtime/design/2026-07-26-fencing-observable-onefile.md` @ `a9ca1952c87098e498c9826eee9297aae5617d6ec6e6c5c58a3f090217ea9850`. Reconciled to those bytes.

## Retained (passed r1 + r2)
- **Three independently locatable mandatory records** (`positive` + `neg.STALE_EPOCH` + `neg.WRONG_LEASE`), parameterized-negative shape **withdrawn** (m-3's evidence-owner shape).
- The **positive admits, proceeds, commits its turn**, contributes to the governed-turn budget.
- **`STALE_EPOCH`** (tuple-mismatch, §6:130) and **`WRONG_LEASE`** (lease-not-held, §4:55/§B.4) are **distinct m-10 rejection mechanisms**.
- Both refused negatives = **zero successor work + zero tool calls**; three records on one new §7 row (seven-leg/eleven-record frame); **m-10 does not attest m-9's writer-fence mechanism**.

## Boundaries
Design-only, my half, for pair review. No §7 row added, no fixture frozen, **no negative governed-turn weight committed**, no predicate typed (m-3's), no amendment, no byte moved. rev16 `3e3c5192…`, m-3 r24 `651c9aec…`, r17 `01b885fe…`, stage-6 amendment `1125b0a0…`, interface lock `cbd1893c…`, r1 amendment `528d6a98…` (unratified) UNMOVED. The §7 row + rebalance + fresh plan + ratification stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- M10-CLOSE4-R2-F1 verified at m-9 successor `2026-07-26-fencing-observable-onefile.md` `a9ca1952…:21-29` (actor B = legitimate replacement's `session.lock` would-block; A holds the OFD; zero downstream work) + m-9 review `…-132623.md:43-54` — before folding.
- F2 verified at m-3 `…close4-fencing-m3/DESIGN-planner-20260726-131130.md:48-53` (refused attempts weigh on turn-attempt accounting; sample_weight co-owned). F3: `…-132623.md` exists, `…-132409.md` does not.
- rev16 `3e3c5192…` §4:55 / §6:130 / §B.4 UNMOVED. The r2 review folded: `…close4-m10-1/DESIGN-REVIEW-implementer-20260726-133745.md` (F1 `:31-47`, F2 `:49-55`, F3 `:57-59`, passes `:61-68`).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close4-m10-1/DESIGN-planner-20260726-134700.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this rev3 design-half relay + one INDEX.md row; no §7 row/fixture/weight/predicate/amendment authored, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte re-reviews rev3 (two-actor sequence with actor B as the m-9 observable; no unilateral negative governed-turn weight; current m-9 refs). m-3 confirms per-record `observer_id`/`evidence_locator`; m-9 reconciles its actor-B writer-fence successor; the joint m-3+m-10+l4 rebalance sets the negative weights; both pairs approve. Master folds the pair-approved fencing contract into the fresh lane-4 plan for r2. rev16 UNMOVED. H-12 stands.
