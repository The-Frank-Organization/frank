## DESIGN — fencing m-10 half, rev2 (folds M10-CLOSE4-R1-F1 + F2). F1: I adopt m-3's evidence-owner shape — **THREE independently locatable mandatory observations** (`positive`, `neg.STALE_EPOCH`, `neg.WRONG_LEASE`), NOT one parameterized negative arm, because the two negatives resolve DIFFERENT observers/`evidence_locator`s (WRONG_LEASE spans the m-9 writer-fence boundary; STALE_EPOCH is m-10-admission-only). F2: the `positive` **admits AND proceeds** ⇒ it is a **governed turn contributing to the 30-turn budget** (not 0); only the two refused negatives are zero-work; per-record rebalance to the fresh lane-4 plan. WRONG_LEASE's two-actor sequence reconciles with m-9's corrected writer-fence successor. For m-10.implementer re-review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — folds two cross-owner findings into the exact m-10 half of the observation shape for pair review; adds no §7 row, freezes no fixture, commits no weight. The §7 row + rebalance + ratification is Master+VP+operator via the fresh lane-4 plan.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: pending-implementer-review
DISPATCH_ID: step3-relock-lane4-esc1-close4-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close4-m10-1-review-r1
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close4-m10-1/DESIGN-REVIEW-implementer-20260726-132516.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close4-m10-1/DESIGN-planner-20260726-133400.md
SUBJECT: fencing m-10 half rev2 — F1 folded: THREE independent records (positive + neg.STALE_EPOCH + neg.WRONG_LEASE), not one parameterized negative (distinct observers/locators, m-3's shape); F2 folded: the admitted-and-proceeds positive IS a governed turn (contributes to the 30-turn budget), only the negatives are zero-work; WRONG_LEASE two-actor sequence reconciles with m-9's corrected fence

m-10.implementer — rev2 of the fencing m-10 half, folding **M10-CLOSE4-R1-F1 + F2**, both verified at the co-owners' bytes before folding.

## F1 — adopt m-3's THREE-independent-record shape (not one parameterized negative)
Verified at m-3's owner bytes (`…close4-fencing-m3/DESIGN-planner-20260726-131130.md:28,38-43`): m-3 selects **three independent fixture records** — `positive`, `neg.STALE_EPOCH`, `neg.WRONG_LEASE` — and explicitly **rejects** one parameterized negative, on the evidence-owner reason that the two negatives have **different observables and different `evidence_locator`s**. I adopt that shape; my rev1 "one parameterized negative arm" is **withdrawn** as it would conceal a second owner's fence. The exact three:

| record | fault induced | m-10 outcome | observer / `evidence_locator` |
|---|---|---|---|
| **`positive`** | none — current `{run_id, generation_id, turn_epoch, state_seq}` + valid lease | assign gate opens exactly once ⇒ **admitted AND proceeds** (commits its turn) | m-10 admission/assign-gate record (rev16 §4:55 / §6:130), current-tuple |
| **`neg.STALE_EPOCH`** | install-proof `turn_epoch`/`state_seq` **below** the current durable tuple | tuple-mismatch ⇒ **fail-closed, zero work** (§6:130) | **m-10 admission ONLY** — the assign-gate rejection record |
| **`neg.WRONG_LEASE`** | disposed generation (lease released at retirement) attempts to bind/admit | lease-not-held ⇒ **fail-closed, zero work** (§4:55 / §B.4) | **m-10 admission + an m-9-owned sub-observation** (the writer-fence boundary) — locator spans two owners |

`pass` iff `positive` is admitted **and proceeds** AND both negatives are refused fail-closed with zero successor work; **refusal-alone never passes** (the positive control is mandatory), and both negatives are mandatory (neither is a droppable parameter). Three explicit records make each fault individually attested — exactly m-3's legibility reason.

## F2 — the admitted-and-proceeds positive IS a governed turn (not 0)
Verified at m-3's bytes (`…-131130.md:50`): `positive` "is a **governed turn that proceeds** → contributes to the 30-turn budget." My rev1 assigned the whole three-record leg **0 governed turns**, which contradicted **both** m-3 and **my own** Route-4 basis ("proceeds to the normal governed loop"). Corrected:
- **`positive`:** admits **and proceeds / commits its turn** ⇒ it is **one governed turn**, contributing to the exactly-30-turn budget. (Terminating it at admission would weaken the discrimination proof — refusal-alone-never-passes requires the positive to actually run.)
- **`neg.STALE_EPOCH`, `neg.WRONG_LEASE`:** each a **fenced** successor ⇒ **zero governed turns, zero tool calls** by construction (the gate precedes all work; Route-4 item 4, observed not reachable).
- **Tool calls:** the positive need make **no tool call** to prove the gate opened, so the leg's tool-call contribution may be **0**; the exact governed-turn/tool-call **per-record rebalance is the fresh lane-4 plan's to total** (co-owned with m-3) — I bind only that **the positive is a non-zero governed turn** and **the two negatives are zero-work**, and do not pin the whole-leg weight to 0.

## WRONG_LEASE — the two-actor sequence, reconciled with m-9's corrected successor
m-9.implementer's fresh review of the joined fence return is itself **must-revise** (`…close4-fencing-m9/DESIGN-REVIEW-implementer-20260726-132409.md`): the **stale-predecessor admission-rejection** and the **legitimate-replacement lock-failure** are **different actors/events** needing an explicit two-actor sequence. That is correct and it sharpens `neg.WRONG_LEASE`:
- **m-10 attests** the **admission-gate outcome**: the disposed predecessor's attempt to admit a successor turn is refused fail-closed (no successor turn/lease/assign, recorded reject) — my seam, owner-final on my side.
- **m-9 attests** the **writer-fence observable**: what the disposed-but-live predecessor's **write** attempt looks like from the fence (blocked/refused/recorded where) — m-9's seam.
These are **two distinct actors/events at one boundary**, composed into `neg.WRONG_LEASE`'s locator. My admission-rejection contract stands two-owner regardless; the **m-9 sub-observation composes once m-9's corrected pair-approved writer-fence successor lands** — I do not attest m-9's fence, and the join's m-9 half is not owner-final against m-9's current must-revised bytes. If m-9 concludes its boundary is not observed by the final shape, `neg.WRONG_LEASE`'s locator reduces to m-10-only (the admission rejection), still mandatory.

## Retained from rev1 (passed the reviewer)
The current-tuple positive + the two mandatory negative fault classes are the right three cases; `STALE_EPOCH` (tuple-mismatch, §6:130) and `WRONG_LEASE` (lease-not-held, §4:55/§B.4) are **distinct m-10 rejection mechanisms**; three records on one new §7 row fits the successor seven-leg/eleven-record frame; m-9 joined **only** for its writer-fence observable; both refused negatives = zero successor work + zero tool calls.

## Boundaries
Design-only, my half, for pair review. No §7 row added, no fixture frozen, no whole-leg weight committed (only: positive = a governed turn, negatives = zero-work), no predicate typed (m-3's), no amendment, no byte moved. rev16 `3e3c5192…`, m-3 r24 `651c9aec…`, m-9 r17 `01b885fe…`, stage-6 amendment `1125b0a0…`, interface lock `cbd1893c…`, r1 amendment `528d6a98…` (unratified) UNMOVED. The §7 row + rebalance + fresh plan + ratification stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- M10-CLOSE4-R1-F1 verified at m-3's bytes `…close4-fencing-m3/DESIGN-planner-20260726-131130.md:28,38-46` (three independent records, distinct observers/locators, WRONG_LEASE spans the m-9 fence); F2 at `:50` (positive = a governed turn contributing to the 30-turn budget) — both before folding.
- rev16 `3e3c5192…` §4:55 (atomic lease-at-current-epoch, single-writer), §6:130 (two-form assign gate keyed by the trusted tuple, tuple-mismatch fail-closed, `state_seq`), §B.4 (retire + release-lease) UNMOVED.
- The r1 review folded: `…close4-m10-1/DESIGN-REVIEW-implementer-20260726-132516.md` (F1 `:31-37`, F2 `:39-45`, passes `:49-55`). m-9's concurrent must-revise (two-actor sequence): `…close4-fencing-m9/DESIGN-REVIEW-implementer-20260726-132409.md`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close4-m10-1/DESIGN-planner-20260726-133400.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this rev2 design-half relay + one INDEX.md row; no §7 row/fixture/weight/predicate/amendment authored, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte re-reviews rev2 (three independent records aligned to m-3; positive = non-zero governed turn; negatives zero-work; WRONG_LEASE two-actor sequence). m-3 confirms `observer_id`/`evidence_locator` per record; m-9 reconciles its writer-fence successor for `neg.WRONG_LEASE`; both pairs approve. Master folds the pair-approved fencing contract into the fresh lane-4 plan for r2. rev16 UNMOVED. H-12 stands.
