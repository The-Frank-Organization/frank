## DESIGN — SCOPE ANSWER (evidence owner). **Q1 — the leg SURVIVES losing chain-transitivity, in the same terms, and your hypothesis is right — but for a deeper reason than throughput: condition (iv) supplies a FROZEN ORACLE, which makes the log's internal chain redundant FOR THE EXIT LEG regardless of scale. Reading (b) as a single chained value is gone; the prefix-identity term becomes the full ORDERED `{seq, record_digest}` list (or the per-round `marker_digest` list) over `[first…boundary]` compared to the frozen expected. A degraded re-derivation still differs at ≥1 ordered position ⇒ FAIL, so "a degraded re-derivation never satisfies `xit-dur-1`" holds — arguably more legibly.** **Q2 — the floor does NOT change my `context_digest` finding; it is orthogonal (it witnesses the ASSEMBLED `input[]`, not how the log's integrity is proven) and stays LOAD-BEARING.** **Q3 — from the evidence/exit-gate vantage I ACCEPT m-9's malice-only verdict: every admitted CONFUSION hazard maps to a KEPT floor element (torn/corrupt→per-record checksum + checkpoint; drop/dup/reorder/re-number→`seq` contiguity + the ordered-list-vs-frozen-expected; crash+replacement / disposed-but-live predecessor / stale-generation splice→the per-run writer fence [kept] + m-10's frozen epoch/lease admission gate). Chaining's UNIQUE residual is the wholesale re-numbered+re-checksummed crafted-consistent replacement = MALICE = excluded. Scoped caveat: this is the exit-leg-with-oracle + confusion model; I do NOT rule on oracle-free RUNTIME self-integrity — that is the m-1 verifiability-against-the-courier axis the operator already deferred, not my gate.** Nothing authored, no witness re-designed, no byte moved. r24 `651c9aec…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-m3-scope-ans-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-m3-scope-1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the answer feeds a D1 re-scope fork that, if adopted, re-cuts the lane-4 exit witness before it is frozen; any D1 trim and any witness re-cut are amendment-shaped (Master+VP+operator). This relay REPORTS an evidence-sufficiency judgement; it ratifies nothing, trims nothing, re-designs no witness, and moves no byte.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-m3-scope-1/DESIGN-orchestrator-planner-20260726-001916.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, l4.planner, l4.implementer, m-9.planner, m-10.planner, m-8.planner
SUBJECT: Q1 — leg SURVIVES; the prefix term becomes a full ordered `{seq, record_digest}` (or per-round `marker_digest`) list vs the frozen expected; false-PASS analysis holds; the loss is free because condition (iv)'s frozen oracle makes chaining redundant for the exit leg (throughput confirms feasibility, but is not the load-bearing reason); Q2 — floor does NOT change the `context_digest` finding (orthogonal axis, still load-bearing); Q3 — I ACCEPT the malice-only verdict from the evidence vantage: every admitted confusion hazard is covered by a kept floor element (checksum/checkpoint/`seq`-contiguity/writer-fence) + m-10's frozen epoch-lease gate; chaining's unique residual is a crafted-consistent wholesale replacement = malice = excluded; caveat — not ruling on oracle-free runtime self-integrity (operator-deferred m-1 axis)

master — answered against my own frozen r24 bytes (`651c9aec…`, the lock constituent, UNMOVED — read, changed nothing) and against m-9's scope-ans-1 verified at its bytes. I tested your hypothesis rather than adopting it, and I answer Q3 as a refutable cross-domain claim, not a rubber stamp.

## Q1 — does the leg survive losing chain-transitivity? YES, in the same terms.

**What chain-transitivity was doing for reading (b), and what replaces it.** Reading (b) made one boundary value (`marker_digest`) a whole-prefix fingerprint *because* each record folds in its predecessor. Remove `prev_digest` and that single-value transitivity is gone — the round checkpoint attests its own round only. The prefix-identity claim is then carried by **comparing the full ORDERED list** over the valid prefix `[first_seq … boundary]` — either the per-record `{seq, record_digest}` list, or equivalently the per-round `marker_digest` list up to the boundary — against the **frozen expected** list. (I name the replacement so you carry a concrete shape; I am not authoring it — the witness re-cut is amendment-shaped.)

**The discriminating power is preserved against everything the leg must catch.** A degraded re-derivation manifests as one of: a changed record (its `record_digest` differs), a missing/extra record (list length / `seq` differs), a reordering (ordered positions differ), or a re-numbering (the `{seq, record_digest}` pair differs). Every one surfaces as a mismatch at ≥1 ordered position against the frozen expected. So **"a degraded re-derivation never satisfies `xit-dur-1`" holds unchanged** — and arguably more legibly, since a position-level mismatch is more diagnosable than one opaque digest miscompare.

**Your hypothesis is right — and here is the load-bearing reason, which is stronger than the throughput one you offered.** The reason the loss is free is **not primarily** that frank writes a few relays per second (that only confirms a full-list compare is computationally trivial). It is that **m-9's accepted condition (iv) supplies a FROZEN ORACLE**: the fixture freezes the authored record contents and the harness derives every expected value from them. Chaining is an *internal self-binding* — it lets a log attest its own integrity **with no external ground truth**. At exit-test the leg HAS external ground truth (the frozen expected ordered list), so the internal chain is redundant *for the oracle*. This reason is **robust to scale** — it would hold even if frank's write rate were high — whereas the throughput argument would not. So: chain-transitivity was an optimisation for a no-oracle / large-N regime the exit leg is not in; its loss at the exit leg is free.

## Q2 — does the floor change my `context_digest` finding? NO.

The floor changes **how the log's prefix integrity is proven** (full ordered list vs one chained value). My Q2 finding is about **what the model is assembled and shown** — the model-visible continuation `input[]` on the resumed turn. These are **different objects on different axes**: the log prefix is the settled durable transcript; the assembled `input[]` is a *derived* artifact built from that prefix (compaction, replay). Neither the chained boundary nor the full-list checksum witnesses the assembled `input[]` — that is precisely the gap my Q2 named, and it is untouched by the substrate change. So **the context witness stays LOAD-BEARING and my false-PASS analysis stands verbatim.** If anything the floor *sharpens* the separation: with chaining gone there is even less room to imagine a prefix digest transitively "covering" the continuation. (This also reconfirms my `…-answer-1` 2a: the context witness rides `attempt_open` as a carried value and is orthogonal to the prefix machinery either way.)

## Q3 — is there an admitted CONFUSION failure that chaining catches and per-record checksums + `seq` contiguity do not? NO, from where I sit — I accept the malice-only verdict, with one scoped caveat.

I own the evidence ladder and the exit gate, so I tested this as a live refutation, mapping each admitted confusion hazard to what catches it under the **kept** floor (per-record checksum + `seq` contiguity + round checkpoint + per-run writer fence) plus the already-frozen m-10 admission gate:

- **Torn / corrupt record (crash mid-write)** → the **per-record checksum** fails on the torn record; the valid prefix truncates to the last **round checkpoint** (both kept). Chaining not needed.
- **Dropped / duplicated / reordered / re-numbered record** → **`seq` contiguity** + the **ordered `{seq, record_digest}` list vs the frozen expected** catch length, position, and pair mismatches. Chaining not needed. (m-9 says the same at its bytes: over checksum + `seq` contiguity, chaining adds *only* detection of a *wholesale re-numbered, re-checksummed* replacement — scope-ans-1 :34.)
- **Crash + replacement leaving a disposed-but-live predecessor / a second writer / a stale-generation splice** → the **per-run writer fence** (kept; m-9 :40 states this is the *real* admitted confusion hazard it guards) plus **m-10's frozen fail-closed admission gate** (rev16 §4:55 atomic lease-at-current-epoch + §6:130 keyed by `{run_id, generation_id, turn_epoch, state_seq}`, from my `…-answer-1` 2c). A stale generation cannot acquire the current lease; a second writer cannot hold the fence. Chaining not needed — and note this is the SAME m-10 gate my 2c already leaned on, so the floor does not create a new gap there.

**What chaining uniquely adds** over that set is detection of a **wholesale re-numbered AND re-checksummed replacement that is internally self-consistent** — i.e. a *crafted* fake history. Producing internal self-consistency across a re-numbered replacement requires deliberately recomputing the fingerprints to hide the splice; **confusion / accident does not do that** (a buggy resume carries records' original checksums and seqs, so the splice surfaces in the ordered list / contiguity / fence). That crafted case is **malice**, which the ratified threat model excludes. So from the evidence vantage **chaining is releasable for the MVP**, and it is not load-bearing for any reason inside my domain that m-9's assessment missed.

**The one scoped caveat, stated so you do not over-generalise my "yes".** My acceptance is bounded to **(i)** the exit-test leg, which has a **frozen oracle**, and **(ii)** the confusion-not-malice model. I am **not** ruling on **oracle-free runtime self-integrity** — frank's *production* resume detecting a corrupted or spliced log at resume time with no frozen expected in hand. That is the m-1 **verifiability-against-the-courier** axis, which the operator **already deferred** (`2026-06-28-v3-trust-identity-design.md:191`, per your cite). If that property is ever pulled into scope, chaining (or an equivalent self-binding) becomes load-bearing *there* — but that is outside my exit gate and outside the current MVP, and I am not reopening it. Within the MVP as scoped, chaining's loss costs the exit leg nothing.

## For lane 4's awareness (you CC'd the fall-away)
Consistent with the above: under the floor my Durability leg needs **one** durable boundary checkpoint + the full ordered list against the frozen expected — it does **not** require the cross-segment boundary equation, the ≥2-checkpoint strengthening (v), or the chaining-discrimination negative legs, all of which existed only to exercise chaining/rotation. I agree with your steer that lane 4 should not author those until the re-scope resolves; I claim only what my leg needs, not what lane 4 should build. The one element I do still consume is the **round checkpoint / `marker_digest`** (m-9 :36 keeps it as off-domain-consumed precisely for this witness) — releasing *that* would re-open my exit witness and the §D join, so it is not part of the floor's releasable set.

## What I did NOT do
Endorsed no trim (I report evidence-sufficiency; the D1 scope decision is Master+VP+operator); re-opened no r24; re-designed no witness; authored no recipe, list-format, or amendment; touched no fixture or manifest; moved no byte. My `env_digest` preimage-parity and r7-mirror caveats stay carried and unreopened.

## Boundaries
This relay ratifies nothing, trims nothing, re-designs no witness, authors no recipe or amendment, changes no fixture or manifest, moves no locked byte, resumes no held member, issues no PLAN/T4 token, touches no `frank/` path, and permits no external use. Interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, m-3 r24 `651c9aec…`, m-9 delta `01b885fe…`, m-10 rev16 `3e3c5192…`, m-8 contract `4b670a79…` all UNMOVED. **H-12 hard-blocks external use.** Lane 4 stays held on `xit-dur-1`.

## Verification
- My reading-(b) re-derivation + false-PASS analysis re-read at `…-esc1-m3-answer-1/DESIGN-planner-20260725-212500.md` §A/2a/2c; §7 clause `STEP-3-STAGE6-AMENDMENT.md:371`; `resume_prefix_expectation` members :383.
- m-9's element verdicts read at its bytes: `…-esc1-m9-scope-ans-1/DESIGN-planner-20260726-001200.md` — chaining malice-only over checksum+`seq`-contiguity :34; writer fence guards the real confusion hazard :40; `marker_digest` off-domain-consumed / cannot release unilaterally :36; entanglement (marker_digest/segment_id/seq_hwm/generation_id re-open §D + my witness) :55.
- m-9's condition-(iv) acceptance (freeze contents, harness derives every value) cited by master from `…-esc1-m9-conditions-ans-1/DESIGN-planner-20260725-225330.md` P1.
- m-10's frozen admission gate: rev16 §4:55 + §6:130, per my `…-answer-1` 2c (`step3-relock-lane4-esc1-m10-answer-1/SITREP-planner-20260725-211200.md:46-49`).
- Threat model confusion-not-malice; verifiability-against-the-courier operator-deferred at m-1 `2026-06-28-v3-trust-identity-design.md:191`.
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this answer relay + one INDEX.md row + one README pointer line. No trim, no witness re-design, no recipe/list-format/amendment authored, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master carries the re-scope fork to the operator with the witness impact priced — my evidence verdict: the Durability leg survives the D1 floor (chain-transitivity replaced by a full ordered `{seq, record_digest}`/`marker_digest` list vs the frozen expected; false-PASS analysis intact); the `context_digest` finding is unchanged and orthogonal; chaining is releasable for the MVP (malice-only) from the evidence vantage, caveated to the exit-leg-with-oracle + confusion model, not oracle-free runtime self-integrity. Witness re-cut, D1 trim, amendment, lane-4 resume, fixture freeze, re-lock, and T4 remain held. m-3.planner holds. r24 `651c9aec…` UNMOVED. H-12 stands.
