## SITREP (stop-work status handoff) — `step3-amend-m3-egress` STOPPED and preserved; nothing to unwind (I parked at grounding, no design authored); the reframe must disposition the policy-owner↔enforcement-host interface + the honest-evidence consequence of the conductor losing direct vantage on an app-side send

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-hold-m3
PARENT_DISPATCH_ID: step3-arch-reframe
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report-only stop-work status handoff; the reframe re-dispatches, this relay does not re-design
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-amend-m3-egress-provider-request
IN_REPLY_TO: master/relays/step3-hold-m3/RECONCILE-orchestrator-planner-20260715-013520.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-8.planner, m-7.planner, m-1.planner, m-9.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: STOP acknowledged and complete — no artifact byte to preserve (design not opened past intake+grounding; no doc, no grill-lock, no review, no lock); the handoff's substance is the four dependencies below, chief among them that moving the send app-side removes the conductor's direct vantage, so the send outcome is a connector ATTESTATION (E0/self_reported by my ladder), not a conductor observation

m-3 — **stop-work acknowledged; `step3-amend-m3-egress` is halted at the earliest possible point.** I read the reframe consequence in the sibling `step3-hold-m8` (CC) before your hold arrived and had already parked the whole amendment on the stale `m-7-hosted` premise; your hold confirms and formalizes it. No semantic edit, grill loop, consumer confirmation, or move toward lock/PLAN/impl occurred. No source/credential/provider-call/external-send/live-store action. VP guardrail carried: I will **not** port the away-email outbox mechanism or its denial vocabulary into the provider-send path.

### 1. Current artifacts + paths — NONE authored under the amendment
- **No design doc.** The intended path `master/domains/m-3-observation-evidence/design/2026-07-15-step3-provider-request-egress.md` **does not exist** — I stopped before writing it.
- **No `GRILL_LOCK`.** The grill opened on agenda item #1 only (final-wire-vs-pre/post); the operator held it as the architecture seam, then this reframe subsumed it. No lock byte produced.
- **Nothing to preserve or rewrite.** The only work product this session is read-only grounding (below). There is no old-framing artifact to quarantine as provisional audit input — a clean stop.

### 2. Current verdict / lock state — DESIGN not opened past intake
No `DESIGN_DOC` content, no DESIGN-REVIEW requested to m-3.implementer, no `DESIGN_LOCK`, no PLAN, no token. Nothing to unwind.

### 3. Dependencies the reframe must disposition (the substance of this handoff)
- **(a) The policy-owner ↔ enforcement-host interface — the relocated grill #1.** As dispatched, my gate was an in-process conductor observe-gate and the "where does authorization sit" choice (final-wire vs pre/post) was mine to grill. Under the reframe it is **not an m-3 grill question** — it is a cross-process topology decision the architecture packet owns: m-3 authors the provider-send **policy + evidence contract**; an **app-side** enforcer (m-8 connector / control plane) applies it at the last pre-wire boundary the conductor does not host. The packet must pin the **shape of that interface**: is m-3's policy a *pre-computed authorization the connector consults*, or a *decision surface the connector invokes at the wire*? That choice (not I) now fixes what "final authorization point / no post-authorization mutation" means, because the mutation window is now a **process boundary**, not an in-process call ordering.
- **(b) What a conductor relay records about an app-side send — I concur with your guardrail and sharpen it.** Record the **governed decision + evidence summary** (authorized/denied · reason · evidence rung · lane/intake key) as an ordinary relay record; **never** the request bytes, the streamed response, or the credential. That keeps my domain's evidence ownership without the conductor ever touching the wire payload.
- **(c) THE load-bearing m-3 consequence — observe-as-send degrades across the new boundary.** My domain thesis is *"done" is conductor-observed, not lane-asserted; an unobservable input degrades.* Moving the send app-side **removes the conductor's direct vantage on the send.** The connector's "I sent / I denied / it streamed" is therefore a **lane attestation, not a conductor observation** — by my evidence ladder it floors at **E0 / `self_reported`** unless independently corroborated. The reframe must disposition the **attestation contract**: what the app-side enforcer must return for the conductor to record an *honest* evidence summary, and the explicit rule that an uncorroborated connector attestation is degraded evidence, never an E-promotion. This is the single most important thing the topology change does to my domain, and it must be a named input to the replacement dispatch — not discovered after lock.
- **(d) The typed-denial mapping survives but must be RE-DERIVED, not ported.** A denial still maps to exactly one existing `delivery_state` ∈ `{accepted, rejected, held}` (no fourth), and no `egress_blocked` away-email park. But *which* state a denial takes must be re-derived in the app-side topology (who records it, when, from whose attestation), consistent with your VP guardrail against porting the away-email vocabulary.

### 4. Reusable grounding (provisional audit input for the reframe — topology-independent facts)
Established this session and durable across the re-cut:
- **The three existing `delivery_state` values are `{accepted, rejected, held}`** (`frank/internal/record/record.go:11-13`) — the "no fourth token" floor has a concrete target set.
- **The away-email classifier that must NOT be inherited** is `frank/internal/egress/rules.go:22-43`: `ClassSafety` on secret markers, `ClassConfidentiality` on `gpt-`/`claude-`/`llama`/`model_name=` — the model-name confidentiality rule that would reject every provider request, plus the `egress_blocked` local-park FSM in `egress.go`. Named so the reframe can point at exactly what "do not silently port" refers to.
- **The locked floors are topology-independent** and survive: denial ⇒ zero provider network send · no-send emits no provider-wire event · one existing `delivery_state` per intake. These carry into the replacement dispatch unchanged.

### 5. Cross-lane
The m-7 credential r-lane (`step3-amend-m7-cred`) is provisional audit input only per your hold; I make no claim on it. m-8/m-9 DESIGNs never materialized (parallel authoring, now held/re-cut) — there was no consumer packet to preserve; the reframe resets that set.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-hold-m3/SITREP-planner-20260715-014000.md` — run below.
- Grounding this session (read-only): `STEP-3-KICKOFF.md` §1a/§7 · `frank/internal/egress/{rules,egress,render}.go` · `frank/internal/record/record.go:11-24` · `step3-amend-m7-cred/DESIGN-planner-20260714-230832.md` (host seam) · the sibling `step3-hold-m8` reframe (CC). No `frank/` write, no code, no external action.

ACTIONS_GIT_REF: none — a report-only stop-work handoff; no `frank/` edit, no commit, no token, no merge. Sole artifacts: this relay + one `master/relays/INDEX.md` row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`); no worktree opened for this lane.
Next requested action: master reconciles this handoff into the `step3-arch-reframe` packet; m-3 holds report-only and resumes only on the operator-ratified architecture-amendment packet + a refreshed consumer audit + a replacement single-author design dispatch addressed to m-3.planner. I will not resume on the correction's prose alone.
