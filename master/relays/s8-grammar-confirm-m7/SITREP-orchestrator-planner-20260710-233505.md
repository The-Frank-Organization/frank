## SITREP — bounded confirm to m-7: the s8 grammar design's pipeline placements — `authority_class` computed pre-m-3-disposition, and the profile-aware completeness stage post-observe/pre-append — confirm against your locked §3 order

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-grammar-confirm-m7
PARENT_DISPATCH_ID: s8-design-m2-grammar
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded confirm off your locked record; feeds m-2's reconciling revision; rides beside your two open design lanes without displacing them
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/s8-design-m2-grammar/SITREP-planner-20260710-231800.md
FROM: master.orchestrator-planner
TO: m-7.planner
CC: operator, master.orchestrator-reviewer, m-7.implementer, m-2.planner
SUBJECT: m-2's blocked grammar design needs your placement confirm in the locked form → lineage → slot_in → observe → append order: (1) where `authority_class` is computed — before the m-3 disposition consumes it (the decision-② key); (2) where the NEW profile-aware completeness check runs — post-observe, pre-append, as the single stage that knows which producer profile a record carries; confirm or correct against `c4-design-m-7-conductor-core` §3 + the CQ-5 ordering

**The ask (one confirm, against your locked §3 steps + the s6 amendment):** m-2's design places (1) `authority_class` computation ahead of the m-3 disposition (so decision-②'s `held`/`rejected`/degrade logic reads a computed value, never a lane-supplied one) and (2) a **profile-aware completeness** stage after the observe outputs land and before the atomic append — the one place that validates "this record's producer profile has its complete field set" (the per-producer partition m-2's r5 settled on). Confirm both placements are hosted exactly there in your engine order — or state the corrected placement — TO master, CC m-2.planner + the VP.

Workload note: this is a confirm off your locked record, sized minutes — it does not displace `s8-design-m7-config`, `s8-design-m7-executor`, or your s7b pair-review duty; if sequencing pinches, say so and I re-order.

ACTIONS_GIT_REF: none — consult only.
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `2e1b4f0` (tag `s7-close`); cwd is not a git repo (docs workspace).
