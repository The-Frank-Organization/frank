## SITREP — m-7 placement confirm for the s8 grammar design: BOTH CONFIRMED against locked §3 — (1) `authority_class` is a step-3 acceptance-time classification (post-gate, pre-observe, atomic-bind; engine-computed, never lane-writable at the tool surface), consumed by the step-4/step-6 disposition; (2) profile-aware completeness hosts as a step-4.5 in-loop gate (post-observe, pre-§4-commit) — with four hosting constraints and ONE lock-discipline note: a new pipeline stage is a §3 AMENDMENT, which the s8 m-7 design set will carry formally

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-grammar-confirm-m7
PARENT_DISPATCH_ID: s8-design-m2-grammar
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded confirm off the locked record; feeds m-2's reconciling revision
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: s8-grammar-confirm-m7/SITREP-orchestrator-planner-20260710-233505.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: m-2.planner, master.orchestrator-reviewer, operator, m-7.implementer
SUBJECT: confirm ×2 with the locked cites (§3 steps at `:69-74`; NF-S6/S7 at `:156`) — m-2's "computed value, never lane-supplied" is exactly the locked step-3 grain (the CQ-5 closed ordering + the D5-scoped non-writability claim); the completeness stage's post-observe placement is not just compatible but FORCED (observe-owned fields land at step 4, so any earlier check reads a partial record); the amendment vehicle is named so the locked §3 stays truthful

**Placement (1) — `authority_class` pre-disposition: CONFIRM, at the step-3 classify point exactly.** The locked order (§3, `:71`): classification happens **at acceptance, post-gate / pre-observe / atomic-bind** (the closed CQ-5 ordering), and the step-4 disposition machinery reads the just-classified in-courier value. `authority_class` is the key NF-S6/S7 (`:156`) already disposition on (authority-class fault/timeout ⇒ `held`; non-authority ⇒ `rejected`/author-return; distinct from non-authority unobservable ⇒ `accepted`+`self_reported`) — so it is a member of the step-3 acceptance-time classification family: **conductor-computed after the form/lineage gates, before the observe hook, bound into the same atomic commit, with no verb at the tool surface for a lane to set or change it** (the locked claim grain: non-lane-writable *at the tool surface*, D5 same-uid residual stated per §8.4). m-2's parenthetical — "reads a computed value, never a lane-supplied one" — is this grain verbatim. One composition note: if the grammar ever lets `authority_class` interact with `slot_in`/`gate_category` semantics, the s5 ③ precedent binds (a raise REWRITES the token; the lattice is a map, never an enum index) — m-2's face, named here so it cannot fall between the designs.

**Placement (2) — profile-aware completeness post-observe/pre-append: CONFIRM, as step 4.5.** The placement is not merely compatible — it is **forced by the locked order**: observe-owned fields land at step 4 (`:72`), so a per-producer-profile completeness check anywhere earlier validates a partial record; and acceptance (`:73`, the §4 pivot) is the only mutation point, so the check must precede it. It hosts as the **last in-courier gate**, inside the same serialized critical section, immediately before the §4 stage/checksum/fsync/rename sequence begins. Four hosting constraints ride the confirm (each an existing engine law applied, none new):
1. **Pure and deterministic** over the assembled in-courier record + the pinned config — no I/O, no vantage, no clock — so recovery re-execution through the shared handler is byte-identical (the §4 locus principle the s6 design pinned).
2. **Failure = one terminal typed reject through the same single-pivot path** — §3 step 6's "gate failure at 2 or 4" mechanics extend to 4.5 (one atomic append of a terminal `rejected` evidenced record + a bounce naming the failing field, path-free per I-PH); never a partial append; the 1:1 intake↔outcome law (`TestLawIntakeOutcomeOneToOne`) holds unchanged.
3. **Read-only over the classification and observe outputs** — it reads the step-3 value and the step-4 outputs; it never rewrites either; the CQ-5 atomic-bind is untouched.
4. **Profile determination is grammar, not engine** — which profile a record carries is m-2's derivation over the record; the engine guarantees only the ORDER, the purity, and the single-outcome disposition.

**The lock-discipline note (the one thing master should carry into the reconcile):** step 4.5 is a **new stage in the locked §3 enumeration** — hosting-compatible with every engine law, but a pipeline addition all the same, and the locked design changes only by review-driven amendment (Cardinal rule 1). The amendment vehicle: **the s8 m-7 design set carries it formally** — `s8-design-m7-config` will name the §3 step-4.5 addition (with the four constraints above as its content) so the reconciled locks the s8 PLAN consumes match the locked record byte-for-byte. m-2 designs against the placement as confirmed here; the amendment lands with my artifact, not silently.

Not authorized / not claimed: placement confirm + hosting constraints only — no m-2 grammar content authored (the profile partition, the completeness semantics, and the reason vocabulary are theirs), no §3 amendment yet (named as owed, carried by my s8 artifact), no change to the two open m-7 design lanes' scope.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-grammar-confirm-m7/SITREP-planner-20260710-234643.md` — run before handoff (result in inline pointer)
- locked lines re-read this session: §3 steps 1–6 whole (`:69-74` — the step-3 CQ-5 text, the step-6 gate-failure mechanics), NF-S6/NF-S7 (`:156` — the two-axis disposition + CQ-2 class-conditional fail-closed)
- consulted for the composition notes: the s5 ③ raise-rewrites precedent; the s6 shared-handler locus principle; the s7 row-9 law grain

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260710-234643)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `2e1b4f0`)
Next requested action: master feeds this to m-2's reconciling revision (CC'd here); the §3 step-4.5 amendment rides `s8-design-m7-config` per the note; my two design lanes and the s7b duty proceed unchanged — no sequencing pinch from this confirm.
