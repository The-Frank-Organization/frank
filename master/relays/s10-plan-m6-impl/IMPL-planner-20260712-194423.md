## IMPL DISPATCH — s10 comms-spine build authorized: delegated token on the r2-approved plan; all-in SCOPE_DIFF; Q6×Q4 transition consumed; SEQ-2/PARK-ACROSS-V8 owner-fidelity gated in-slice before park; conditions (a)–(g) binding; operator boots the fresh s10 slice team

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s10-plan-m6-impl
PARENT_DISPATCH_ID: s10-plan-m6-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator BOOTS the fresh s10 slice-team sessions this dispatch names (ratified staffing); slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time; the LIVE dogfood ODB verdicts are the operator's own (operator-FROM by locked design)
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan
FROM: m-6.planner
TO: m-6.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-3.planner, m-7.planner, m-7.implementer, m-1.planner
IN_REPLY_TO: s10-plan-m6/PLAN-REVIEW-implementer-20260712-193841.md
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: delegated implementation dispatch for the s10 comms spine — parented to the approving PLAN-REVIEW r2 (approve), Q6×Q4 transition consumed as the T1 authorization, all-in SCOPE_DIFF preserved, conditions (a)–(g) binding; the operator boots the fresh slice team (s10.planner/s10.implementer) that executes under m-6 guidance

All delegated-dispatch preconditions from `s10-dispatch/PLAN-orchestrator-planner-20260712-205011` are met: orchestrator conditional dispatch authority granted (`DELEGATED_DISPATCH_AUTHORITY: yes`), the pair PLAN-REVIEW returned **approve** (r2, `s10-plan-m6/PLAN-REVIEW-implementer-20260712-193841`), the mechanical SCOPE_DIFF is **all-in** with no hard trigger, and the PARENT chain points to that approving PLAN-REVIEW, whose parent is my pair-Planner PLAN (r2) addressed to the Implementer. I therefore issue the delegated token.

DISPATCH IMPL

**Consumed (required carry — no stale status propagates into build):**
- **Plan of record:** `master/domains/m-6-human-surface-scheduler/plan/2026-07-12-s10-comms-spine-plan.md` (11 tasks T1–T11). The **superseded r1 PLAN is non-token-bearing**; r2 (`s10-plan-m6/PLAN-planner-20260712-193605`) is the reviewed plan of record.
- **The Q6×Q4 transition authorization:** `q6q4-recordkind/RECONCILE-…-205010` — the `odb` + `resummon_command` interpreter-bearing tokens ride **ONE governed v7→v8 fieldspec transition** (T1), MAJOR-but-safe (marker-first old-reader refusal; no migrator; history un-reclassified). This IS the "one named transition" of condition (b); no other schema change is authorized.
- **SEQ-2 answered:** fresh-genesis dogfood at v8 (T2). **PARK-ACROSS-V8 answered:** sequencing-avoidance (T1/T2 before any park fixture); the full 8a freeze/re-issue branch is s11, NOT built.

**SEQ-2 / PARK-ACROSS-V8 owner-fidelity — the in-slice gate BEFORE any park fixture (T4).** The transition owner actions are in-slice by their owners: **m-2 executes the T1 registry transition** (the s8 T2 owner-fidelity pattern) and **m-7 executes the capability-marker move** (theirs alone). These land at **T1/T2, before T4 opens** — the PARK-ACROSS-V8 sequencing invariant is therefore also the owner-fidelity gate: **no park machinery lands on an unconfirmed schema foundation.** If m-2 or m-7 finds the fresh-v8-genesis / v7→v8 transition does not fit their successor-map / §7-apply / capability pattern, **T1/T2 escalates to master BEFORE T4** (condition b) — the build does not proceed to park on a contradicted foundation.

**Conditions binding on this dispatch (a)–(g), verbatim from the orchestrator delegation:**
(a) unique sub-DISPATCH_IDs per build leg (the `s10-plan-m6` chain); (b) registry/schema changes beyond the ONE named T1 transition → escalate to master BEFORE work; (c) any change to a locked design contract → the owning pair via the amendment path + master, never a silent edit; (d) cross-domain shape changes → master (note: the T9/T10 sunset re-pointing of m-3's check-policy hook is **authorized** by the operator sunset ruling with m-3 in-slice fidelity — not an unauthorized shape change; any move beyond that hook → master); (e) **merge is operator-only** — the merge-decision relay goes TO the operator, the executor acts only on a `HUMAN_MERGE_AUTHORIZATION` grant at grant time; (f) a blocked/model-stopped lane reports and holds — no pushing through a red; (g) the dogfood evaluation extends the s8 ledger as one series (the catch-layer progression + the reader-with-no-writer class count).

**Staffing + the human boot leg (reconciling the pair-chain token with the fresh-team ratification).** This token is the lint-valid pair-chain authorization (the pinned `s10-plan-m6` loop — FROM m-6.planner TO the pair Implementer of record). The **actual build is carried by the operator-booted FRESH slice team** the plan names — **`s10.planner`** + **`s10.implementer`** (the s8 `sN.planner`/`sN.implementer` convention; sole-writer discipline per seat) — executing the plan-doc under **m-6 guidance by relay**, with **m-2** (T1 registry transition), **m-7** (T1 capability marker + T8 scheduler/timer + A-2 dedupe + store seams), **m-3** (T6 re-observe/J1 + T9/T10 sunset-hook fidelity), and **m-1** (only if an addressing/mailbox seam moves — none anticipated) as fidelity by relay. **B10 is not in effect** (fresh team, not the domain pair auto-building). **Operator action (the HUMAN_GATE leg): boot `s10.planner` + `s10.implementer`**; execution then runs task-by-task under `executing-plans`/`subagent-driven-development` with m-6 guiding by relay. If the ratified staffing routes build-seat authority differently than this pair-chain token implies, redirect at boot — the lint constrains the token to the same-owner pair, so the operational routing is the operator's.

**Build discipline:** vertical-slice-first (T1–T2 the transition + fresh-v8 foundation → T3–T8 the thinnest end-to-end A-gate spine: ODB render → park → validated operator reply → local re-observe on wake → exactly-once wake → deterministic resummon → T9–T10 the two operator sunsets → T11 EXIT LEG 3 + hardened battery); TDD per task, RED-first negatives per row/task, frequent commits; byte-exact `{accepted, rejected, held}` + R2 preserved; **Rails A/B stated per new surface**; **I-PH swept over every new seat-visible surface** (ODB render, FSM/resummon outputs); the two mechanical tables (consumption→supply · diff→license) green; both s10 sunsets demonstrated gone. **Exit-test leg 3 (a parked lane wakes on reply) live on the fresh v8 dogfood store is the slice-exit acceptance.**

Scope-fence note (seam/package grain — off-frank plan; exact new-file paths resolve WITHIN these packages on-frank, anything OUTSIDE → master per (b)/(d); anchors verified live by m-6.implementer at `frank@8941889`). Task→path map: T1 `fieldspec/` (registry.json + validate/predicate/render); T2 `store/genesis.go`; T3 ODB render + T8 A-2 dedupe = new units under `engine/`; T4 `obligation/obligation.go` (`completePark`); T5 `engine/submit.go` (`gate_resolution`/operator-FROM guard); T6 `observe/` re-fire + `engine/submit.go` wake path; T7 exactly-once + T8 scheduler = `engine/loop.go`; T9/T10 sunset hooks = `observe/read_file_worker.go` + `engine/loop.go`; fixtures + slice relays.
SCOPE_DIFF:
- frank/internal/fieldspec/registry.json -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/predicate.go -> in
- frank/internal/fieldspec/render.go -> in
- frank/internal/store/genesis.go -> in
- frank/internal/obligation/obligation.go -> in
- frank/internal/engine/submit.go -> in
- frank/internal/engine/loop.go -> in
- frank/internal/engine/ -> in
- frank/internal/observe/ -> in
- frank/test/fixtures/ -> in
- frank/.relays/s10/ -> in
SCOPE_DIFF_RESULT: all-in
HARD_TRIGGER_SCAN: none — no schema beyond T1; no locked-contract reopen (consumes locks); no unauthorized cross-domain shape (T9/T10 sunset hooks are operator-ruling-authorized with m-3 fidelity); merge operator-only; no scope expansion (B/C/D + full 8a branch + away-bridge fenced to s11/step-(d)).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s10-plan-m6-impl/IMPL-planner-20260712-194423.md` — run below.
- Lineage: PARENT `s10-plan-m6-review-r2` → PLAN-REVIEW **approve** (FROM m-6.implementer) → its parent `s10-plan-m6` → my pair-Planner PLAN r2 (FROM m-6.planner, TO m-6.implementer), whose own parent `c6-fix-m-6-review-r2` is the approving c3 DESIGN-REVIEW. SCOPE_DIFF fence + all-in above.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace; `frank/` main clean at `8941889`).

ACTIONS_GIT_REF: a dispatch/authorization relay; no `frank/` edit, no code, no commit by this file. The build begins in the operator-booted s10 slice sessions on a branch off `frank/ main@8941889`. Sole artifact this turn: `master/relays/s10-plan-m6-impl/IMPL-planner-20260712-194423.md` + one INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `8941889` (`s8-close`), off-session here
Next requested action: operator boots the s10 slice team (`s10.planner` + `s10.implementer`); the build runs T1→T11 under the fence + conditions (a)–(g), with T1/T2 owner-fidelity (m-2 transition + m-7 capability) gating before T4 park; m-6 guides by relay, stands by for blocker relays, and handles the PR/implementation report → the merge-decision relay TO the operator at slice exit. No merge without `HUMAN_MERGE_AUTHORIZATION`.
