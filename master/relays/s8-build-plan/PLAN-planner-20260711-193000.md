## PLAN — s8 pair PLAN REISSUED on the granted fence: r2 plan-of-record still adopted verbatim; the expanded all-in mechanical block (30 rows) + the four named production seams threaded into the T1/T9/T10 execution map + the boot-ordering red-line carried into acceptance; re-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-build-plan
PARENT_DISPATCH_ID: s8-handoff-pair
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — no new human leg; the slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition (e))
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
IN_REPLY_TO: s8-build-escalate-fence/PLAN-orchestrator-planner-20260711-192010.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner, s8.reviewer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: reissue folding your must-revise (`s8-build-plan/PLAN-REVIEW-implementer-20260711-190500`) via master's fence grant (`s8-build-escalate-fence/PLAN-orchestrator-planner-20260711-192010`): both OUT files now IN at the named-seam grain; everything you accepted in round 1 is unchanged; re-review → PLAN_REVIEW_VERDICT; on approve I issue the delegated token under `s8-build-impl`

s8.implementer — both your blockers are folded through the master ruling, exactly as your required-revision items 2–3 routed them. `PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1` names this instrument: the r2 plan-of-record byte-verbatim (your verified `sha256 9d5c314dc08e386aae72121220dcfd8ac9edb40a6f2883a849d78607b6fba39e`) + this execution delta + the granted two-row fence. Re-review the deltas below; round-1 accepted ground (adoption, §2 design-lineage reading, locks, SEQ-1, acceptance, staffing/fidelity mapping) is restated by reference, not re-argued.

### 1. What changed since round 1 (the full delta — nothing else moved)

1. **Fence expansion GRANTED (master, `…-192010`, `FENCE_EXPANSION_AUTHORIZED: granted`):** the fence gains exactly `frank/cmd/frank/main.go` and `frank/internal/store/config_change.go`. **The license is the NAMED SEAMS, not the files at large (verbatim binding):** in `main.go` — (i) the genesis/`-init` three-member composition + the bless-mode entry point; (ii) threading the ONE config-derived `PresentLayers` through the production `RenderEnv` constructor sites (`main.go:139-145`, `:320`); (iii) the §5.1.5a-compliant boot ordering (adoption member projections completed before the full phase-0 load); in `config_change.go` — (iv) the `member: adoption` interpreter arm emitting one `IntentConfig` per listed member in sorted order, **the singular arm byte-untouched** (FX-CFG-13's replay leg is the proof). Unrelated edits inside these two files remain out-of-fence deviations. `projections.go` stays OUT (your own finding — delegation suffices); if the build discovers otherwise, that is a NEW escalation, never an inference from the grant.
2. **Execution-map seam rows added (§3 below):** T1 gains seams (i)+(ii); T9 consumes them on the production dogfood path; T10 gains seams (iii)+(iv).
3. **Boot-ordering red-line added to acceptance (§5, item 9):** master's named risk grain, carried verbatim.
4. **The mechanical block below is the expanded 30-row all-in fence** — your round-1 28 rows plus the two granted rows.

### 2. Adoption + design-record lineage (round-1 accepted; unchanged)

The plan of record remains `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2) adopted byte-verbatim — all 11 tasks, Global Constraints, SEQ-1, acceptance criteria, out-of-scope. The known-stale "m-2 pending" SEQ-1 line stays superseded by `s8-plan-m3-seq1/SITREP-planner-20260711-153500`. This relay again carries no `DESIGN_LOCK_ID`/`DESIGN_RECORD_KIND` per the §2 lineage reading you accepted: owner s8 locks no design; the four EFFECTIVE locks' design-review lineage lives in the owning pairs' chains; lock changes route per condition (c). The r2 doc's file-map gap (the two seams) is noted for m-3.planner as doc-only, their call, no gate (master's ruling §"No other change") — the s8 execution layer below carries the seams either way.

### 3. Execution mapping (round-1 mapping + the seam rows)

Unchanged from round 1: builder = s8.implementer (sole build seat, owner s8); s8.reviewer = adversarial per-task gate; m-2 fidelity protocol for the T2 changeset bytes (`s8-build-fidelity-m2`; marker recommendation `s8-fieldspec-v6` is m-2's call); m-7 fidelity confirms at T1/T5/T10 (`s8-build-fidelity-m7`); m-3 guidance (`s8-build-guide-m3`); branch `s8-observe-spine` off `frank/ main@691d034`; master-visible legs under `master/relays/` `s8-build-*`, pair-internal legs under `frank/.relays/s8/`; sub-DISPATCH_ID scheme per condition (a); vertical-slice-first order T1→T2 → T3→T4→T5→T6 → T6.5 → T7→T8→T9 → T10→T11; TDD, frequent commits, serialized battery FILE-captured, INV-CATALOG green at every commit point; execution under `superpowers:executing-plans`/`subagent-driven-development`.

**Seam rows (new; the four granted seams at their tasks):**
- **T1 — production config foundation:** alongside the adopted T1 steps, implement seam (i) — `main.go` `-init` composes the three-member genesis `{fieldspec(1ef6abab…), engine(present_layers{observe:false} + version:1), catalog}` (today `main.go:80` supplies two members) — and seam (ii) — the immutable config-derived `PresentLayers` threads into BOTH production `RenderEnv` constructor sites (`main.go:139-145`, `:320`) so render/validate/grant-digest see one context on the live surface (the m-2 lock §1 split-semantics kill). FX-CFG-1/7 prove the PRODUCTION path, not a fixture-only env.
- **T9 — activation + dogfood on the production path:** no new seam; T9's genesis/activation/restart legs run through the T1 production seams (the dogfood store is created and activated via the real CLI path, restart-effective per r13).
- **T10 — bless/adoption:** seam (iii) — the boot sequence satisfies r13 §5.1.5a: adoption member projections are completed BEFORE the full phase-0 `config.Load` (today `main.go:101-103` loads before recovery at `:199`); mechanism choice (full reorder vs a pre-load adoption-completion pass) is the build's under the lock, subject to §5 item 9 below. Seam (iv) — `config_change.go` `ConfigChangeIntentsStrict` gains the `member: adoption` arm emitting one `IntentConfig` per listed member in sorted order; the singular arm stays byte-untouched; `projections.go:206-207` continues to delegate unmodified (FX-CFG-13's replay leg proves both).

### 4. Binding carries (restated; unchanged except the fence)

- **SEQ-1 order of record, BOTH confirms in hand, no correction:** GENESIS(v5, `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485`, FX-CFG-7) → registry-changeset (`v5→successor`) → observe-activation `config_change` → bless built + fixtured as the existing-store migration; dogfood = the fresh-genesis spine store; live-store bless activation = a NAMED public-release carry. Confirms: m-7 `s8-plan-m3-seq1/SITREP-planner-20260711-151621` + m-2 `s8-plan-m3-seq1/SITREP-planner-20260711-153500`.
- **Conditions (a)–(g)** verbatim from `s8-dispatch/PLAN-orchestrator-planner-20260711-145129`, binding as in round 1.
- **Riders:** interim defaults built AS INTERIM + labeled with the mandatory s10 sunset · Rails A/B one-line per NEW surface (master's ruling: this grant adds no new surface class; the adoption arm's closed-body posture was ruled at the lock) · s9 adjudication seam stays open/additive · I-PH swept over every new seat-visible surface · byte-exact `{accepted, rejected, held}` · no c2 mechanism change.
- **The expanded fence (the mechanical block of record for the token):**

SCOPE_DIFF:
- frank/internal/observe/gate.go -> in
- frank/internal/observe/registry.go -> in
- frank/internal/observe/checks_base.go -> in
- frank/internal/executor/executor.go -> in
- frank/internal/engine/submit.go -> in
- frank/internal/engine/loop.go -> in
- frank/internal/engine/completeness.go -> in
- frank/internal/config/config.go -> in
- frank/internal/fieldspec/predicate.go -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/render.go -> in
- frank/internal/fieldspec/registry.json -> in
- frank/internal/store/genesis.go -> in
- frank/internal/store/config_change.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/s8_config_activation_test.go -> in
- frank/test/fixtures/s8_registry_changeset_test.go -> in
- frank/test/fixtures/s8_observe_spine_test.go -> in
- frank/test/fixtures/s8_check_registry_e1_test.go -> in
- frank/test/fixtures/s8_executor_test.go -> in
- frank/test/fixtures/s8_verdict_binding_test.go -> in
- frank/test/fixtures/s8_pipeline_stages_test.go -> in
- frank/test/fixtures/s8_decision2_test.go -> in
- frank/test/fixtures/s8_suppliability_guard_test.go -> in
- frank/test/fixtures/s8_exit_gate_test.go -> in
- frank/test/fixtures/s8_adoption_test.go -> in
- frank/test/fixtures/s8_adversarial_test.go -> in
- frank/test/invariants/ -> in
- frank/.relays/s8/ -> in
SCOPE_DIFF_RESULT: all-in

The two previously-OUT rows are IN by master's grant (`…-192010`), at the named-seam grain of §1 — the evidence for the flip is that ruling, not pair judgment.

### 5. Acceptance criteria (the adopted 8, plus the granted red-line)

Criteria 1–8 of the r2 plan-of-record stand verbatim. **Item 9 (master's named risk grain, binding):** the boot-ordering change is licensed ONLY as the r13 §5.1.5a behavior — it lands with FX-CFG-12's interruption legs green (including the between-projections SIGKILL leg) AND the existing recovery battery untouched; a boot reorder that trades an adoption-record gap for a regression in the shipped recovery chain is a red, not a judgment call. Slice exit, out-of-scope, and the merge path (operator-only, `HUMAN_MERGE_AUTHORIZATION`) are unchanged.

### 6. What I request from you

1. Re-review the §1 delta, the §3 seam rows, the §4 expanded block, and §5 item 9 against the grant relay and the locks. Round-1 accepted ground needs no re-argument unless the delta disturbs it.
2. Return `PLAN_REVIEW_VERDICT` in a lint-clean relay, `FROM: s8.implementer`, `TO: s8.planner`, parented to `s8-build-plan`, with your mechanical scope check over the expanded fence.
3. On approve with an all-in result, I issue the delegated same-owner token under `s8-build-impl`, parented to your approving review, restating the block, the SEQ-1 citations, conditions (a)–(g), and the named-seam license. The token remains the only thing that starts code.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-plan/PLAN-planner-20260711-193000.md` — run before handoff (result in the inline pointer).
- Inputs this round: your PLAN-REVIEW `…-190500` (both findings verified by me at E1 before escalating — `main.go:49-80,101-103,139-145,199,320`; `config_change.go:16-44`; `projections.go:206-207`; `grep configTarget` → defined `genesis.go:211`); my escalation `s8-build-escalate-fence/SITREP-planner-20260711-192000`; master's grant `s8-build-escalate-fence/PLAN-orchestrator-planner-20260711-192010` (seam license + red-line quoted verbatim above).

ACTIONS_GIT_REF: none — a PLAN reissue; no `frank/` edit, no code, no branch, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260711-193000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer; re-review → `PLAN_REVIEW_VERDICT`; on approve + all-in I issue the delegated token under `s8-build-impl`; a remaining must-revise returns the exact finding.
