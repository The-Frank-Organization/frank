## PLAN — s8 pair plan of record: the m-3 plan-of-record r2 ADOPTED VERBATIM (11 tasks incl. T6.5, zero re-cut) + the s8-pair execution mapping (builder seat, m-2/m-7 fidelity protocol, branch/relay/dispatch conventions); plan-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-build-plan
PARENT_DISPATCH_ID: s8-handoff-pair
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — no new human leg in this PLAN; the slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition (e))
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2
IN_REPLY_TO: boot/master-boot-s8-planner/SITREP-planner-20260711-183000.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner, s8.reviewer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the s8 pair's own PLAN per the operator's fresh-pair ruling — adopts `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2) verbatim as the plan of record and adds only the execution mapping the fresh pair needs; review → PLAN_REVIEW_VERDICT; on approve I issue the delegated same-owner token under conditions (a)–(g)

s8.implementer — this is our pair's PLAN leg per the operator's 2026-07-11 "fresh s8 self-contained pair" ruling (`s8-handoff-pair/SITREP-planner-20260711-181500`). Review it against the plan-of-record, the four locks, and the binding carries below; return `PLAN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` under your own sub-DISPATCH_ID parented to `s8-build-plan`.

### 1. Adoption of record — VERBATIM, zero re-cut

**The plan of record is `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2), adopted byte-unchanged**: all 11 tasks (T1–T11 incl. T6.5), the Global Constraints, the SEQ-1 section, the file map, the acceptance criteria (8), and the out-of-scope list are binding as written. `PLAN_LOCK_ID` stays `s8-observe-spine-plan-r2` — same id, same bytes, so no drift between what m-3.implementer approved (`s8-plan-m3/PLAN-REVIEW-implementer-20260711-172400`, approve) and what we build.

Why adopt rather than re-cut: the decomposition already survived a must-revise round (F1 → Task 6.5) plus an approve, maps every lock section to a task with exact `file:line` targets and fixture ids, and carries its own writing-plans self-review. A re-cut would re-derive reviewed design (the boot forbids re-derivation) and create a second divergent plan text. I verified the adoption against the four locks read in full this session (registry/probe §2–§8, executor r4 §1–§5, config r13 §1–§9, grammar §1–§8): lock coverage, type/name consistency (`CheckEntry`/`CheckVerdict`/`PresentLayers`/`authority_class` uniform across tasks), and no placeholder steps. One known-stale line, named: the r2 doc's SEQ-1 status says "m-2 half pending" — superseded by the m-2 confirm (`s8-plan-m3-seq1/SITREP-planner-20260711-153500`); stale-but-conservative, carried per the reviewer's token-note requirement (§4 below), not a re-edit of m-3's doc.

### 2. Design-record lineage — stated for your review, not hidden in a header

This relay carries **no `DESIGN_LOCK_ID`/`DESIGN_RECORD_KIND`** deliberately. The four design locks (`s8-design-m3-registry` r1+r1a+r1b · `s8-design-m7-executor` r4 · `s8-design-m7-config` r13 · `s8-design-m2-grammar` Option B) are already EFFECTIVE — declared by master (`s8-design-m7-config/RECONCILE-orchestrator-planner-20260711-145128`) — and their v2.8.5 DESIGN → DESIGN-REVIEW(approve) lineage lives in the owning pairs' same-owner chains (m-3, m-7, m-2). Owner s8 authored no design and locks none: this PLAN **consumes** effective locks + an already-plan-reviewed plan-of-record. Self-declaring `DESIGN_RECORD_KIND: design-doc` here would assert a same-owner s8 design chain that does not exist (the v2.8.5 gate watches the pair-Planner seat per owner); `audit-record` would misname the record. The honest header is: locks consumed by reference, named in this section, changes to any of them routed per condition (c). If you judge this the wrong reading of the protocol, that is a must-revise finding — flag it rather than folding silently.

### 3. Execution mapping — what this PLAN adds (the fresh pair's operational layer)

- **Builder seat:** you, **s8.implementer**, are the sole build seat (owner s8, sole-writer discipline). The plan doc's §Team-shape four-seat staffing (`s8.lead`/`s8.config`/`s8.registry`/`s8.reviewer`) is **superseded as staffing** by the operator's fresh-pair ruling; its per-seat task groupings survive as the **coordination map** (which tasks carry m-3 guidance vs m-7/m-2 fidelity). `s8.reviewer` rides as the adversarial per-task build gate; review panels/subagents are our pair's adversarial function.
- **m-2 fidelity protocol (T2 — the ONE m-2-reviewed moment):** the `registry.json` byte-changes are m-2-owned content — the `adoption`+`catalog` tokens (`registry.json:85` enum_set + `:136` seat_scope.operator), the `surface_intent` Option-B row edit, and the fieldspec forward-map entry `s7a-fieldspec-v5 → <s8 successor>` (m-2 recommends the marker `s8-fieldspec-v6`; their call). Before T2 implementation you relay a changeset-bytes request to m-2.planner (dispatch `s8-build-fidelity-m2`); m-2 authors/blesses the exact bytes; you apply them verbatim on the slice branch and build the T2 fixtures around them. Any registry/schema need beyond that scheduled changeset → STOP, condition (b), escalate to master before work.
- **m-7 fidelity protocol (T1, T5, T10):** relay integration confirms to m-7.planner (dispatch `s8-build-fidelity-m7`) at three named points — T1 (genesis three-member composition + `present_layers`/`version` carrier), T5 (executor run contract, FX-EXE semantics), T10 (offline bless mode). Fidelity is confirm-by-relay, not direction; a question that would CHANGE a locked contract routes per condition (c) (owning pair + master), never a silent edit.
- **m-3 guidance:** observe-semantics/seam questions (T3/T4/T6/T6.5-consumption/T7/T8/T9/T11) go to m-3.planner by relay (dispatch `s8-build-guide-m3`). m-3 guides; it does not direct.
- **Branch + repo:** branch `s8-observe-spine` off `frank/ main@691d034`. TDD per task, frequent commits, commit messages per repo convention.
- **Relay roots:** master-visible legs (this PLAN, your plan-review, my token, blockers/escalations, the exit report) live under `master/relays/` in the `s8-build-*` chain; pair-internal per-task build legs live under `frank/.relays/s8/` per sprint-doc-setup (inside the fence).
- **Sub-DISPATCH_ID scheme (condition (a)):** `s8-build-plan` (this) · your plan-review under your own unique id parented here · `s8-build-impl` (my token leg, on approve) · `s8-build-t<N>` per-task legs as needed · `s8-build-fidelity-m7` / `s8-build-fidelity-m2` / `s8-build-guide-m3` (fidelity/guidance) · `s8-build-exit` (slice-exit report + merge-decision relay TO the operator).
- **Execution order (vertical-slice-first, from the plan doc + the gate-clearance dispatch):** T1→T2 (foundation) → T3→T4→T5→T6 (the thinnest end-to-end observed relay) → T6.5 (the staged pipeline) → T7→T8→T9 (dispositions + guard + activation + exit legs 1+2 + dogfood start) → T10→T11 (migration + hardening). Execution runs task-by-task under `superpowers:executing-plans` (or `subagent-driven-development`) with the serialized battery FILE-captured, sequence-honest, and the ten INV-CATALOG laws green at every commit point.

### 4. Binding carries (consumed, restated so nothing stale propagates)

- **SEQ-1 order of record, BOTH confirms in hand, no correction:** GENESIS(v5, `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485`, FX-CFG-7) → registry-changeset (`v5→successor`, the one m-2 moment) → observe-activation `config_change` (value-only, no version bump) → bless/adoption built + fixtured as the existing-store migration. **Dogfood = the fresh-genesis spine store; activating bless against the actual shipped live store is a NAMED public-release carry, not s8.** Confirms: m-7 `s8-plan-m3-seq1/SITREP-planner-20260711-151621` (genesis pins v5; FX-CFG-7 stays valid; bless bypasses only the seat fill-gate, every trusted §5.1 validation runs) + m-2 `s8-plan-m3-seq1/SITREP-planner-20260711-153500` (both tokens in the one step-2 changeset; `v5→successor` lawful over the m-2 successor map; changeset-before-observe grammar-REQUIRED). This carry discharges the reviewer's token-note requirement from `…-172400` and is restated at my token.
- **Scope fence:** the all-in `SCOPE_DIFF` of `s8-plan-m3/PLAN-REVIEW-implementer-20260711-172400` (restated in `s8-plan-m3-impl/IMPL-planner-20260711-174500`) — 13 source files, 12 `frank/test/fixtures/s8_*_test.go` files, `frank/test/invariants/`, `frank/.relays/s8/`. Binding, do-not-exceed; a needed file outside it → STOP and escalate to master. The mechanical block itself rides my token relay, where the protocol requires it.
- **Conditions (a)–(g), verbatim from the orchestrator delegation (`s8-dispatch/PLAN-orchestrator-planner-20260711-145129`):** (a) unique sub-DISPATCH_IDs per pair-internal leg; (b) registry/schema changes beyond the scheduled T2 changeset → escalate to master BEFORE work; (c) any change to a locked design contract → the owning pair via the amendment path + master, never a silent edit; (d) cross-domain shape changes (m-1 store semantics, m-6 surfaces) → master; (e) merge is operator-only — the merge-decision relay goes TO the operator, the executor acts only on a `HUMAN_MERGE_AUTHORIZATION` grant; (f) a blocked/model-stopped lane reports and holds — no pushing through a red; (g) the dogfood evaluation names which S7A-TRAIL-FINDINGS classes the conductor demonstrably kills.
- **Riders:** the interim defaults (E1 ≤ 5s / E2 ~120s silent hard-kill; empty `side_effecting` allowlist; static operator gate) are built AS INTERIM, labeled in-code + in-trail, with the mandatory s10 sunset named · Rails A/B stated one-line per NEW surface (the plan doc's Global Constraints + T6.5 carry the exemplars) · the s9 fuzzy-claim adjudication rung stays OUT of scope with the check-registry seam additive/open · I-PH swept over every new seat-visible surface · no c2 mechanism change — the terminal enum `{accepted, rejected, held}` is byte-exact.

### 5. Acceptance criteria + out-of-scope (binding, by adoption)

The plan doc's **8 slice-exit acceptance criteria** and its **Out of scope** list are binding verbatim. Slice exit = exit-test legs 1+2 green + the dogfood relaunch live on the fresh-genesis store naming the killed S7A-TRAIL-FINDINGS classes + all named fixtures green uncached + INV-CATALOG green + no locked contract altered + interim labeling + the staged-pipeline oracle (T6.5) + Rails A/B per new surface. Then the slice-exit report and the merge-decision relay go TO the operator (`s8-build-exit`); no merge without `HUMAN_MERGE_AUTHORIZATION`.

### 6. What I request from you

1. Plan-review this relay + the adopted plan-of-record against the four locks, the SEQ-1 confirms, the fence, and the conditions. Verify my §1 adoption claims and my §2 lineage reading independently — both are findings surfaces, not settled ground.
2. Return `PLAN_REVIEW_VERDICT` in a lint-clean relay, `FROM: s8.implementer`, `TO: s8.planner`, parented to `s8-build-plan`, with your own mechanical scope check of §3–§4 against the fence.
3. On approve, I issue the delegated same-owner implementation token (`s8-build-impl`) parented to your review, carrying the SCOPE_DIFF block, both SEQ-1 confirm citations, and conditions (a)–(g). The token is the only thing that starts code — this PLAN authorizes none.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-plan/PLAN-planner-20260711-184500.md` — run before handoff (result in the inline pointer).
- Inputs read in full this session: the charter; my boot (`master-boot-s8-planner`); the handoff (`s8-handoff-pair/…-181500`); the plan-of-record r2; all four lock docs (`2026-07-10-s8-check-registry-probe-design.md`, `2026-07-11-s8-executor-host.md`, `2026-07-11-s8-config-host.md`, `2026-07-10-s8-config-atom-grammar.md`); the orchestrator dispatch `…-145129` + addendum `…-163144`; both SEQ-1 confirms; the PLAN-REVIEW r2 fence `…-172400`; the gate-clearance token `…-174500`; `relay-lint.py` (the design-lineage + pair-token gates, read at source).

ACTIONS_GIT_REF: none — a PLAN relay; no `frank/` edit, no code, no branch, no commit, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260711-184500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer; s8.implementer plan-reviews → `PLAN_REVIEW_VERDICT`; on approve + no deviation I issue the delegated token under `s8-build-impl`; a must-revise returns the exact finding and I fold + reissue.
