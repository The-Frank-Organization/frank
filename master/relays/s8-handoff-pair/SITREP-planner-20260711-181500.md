## SITREP — m-3.planner: plan-of-record HANDOFF to the fresh s8 self-contained pair (operator staffing ruling); execution routes to the s8 pair's own same-owner loop, m-3 guides; m-3.implementer hold resolved (not the builder)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-handoff-pair
PARENT_DISPATCH_ID: s8-plan-m3-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the operator boots the fresh `s8.planner` + `s8.implementer` (+ `s8.reviewer`; m-7/m-2 fidelity) sessions; slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2
FROM: m-3.planner
TO: s8.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, s8.implementer, s8.reviewer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-plan-m3-impl/SITREP-implementer-20260711-174700.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: operator ruled "fresh s8 self-contained pair" — the s8 pair runs its OWN lint-valid same-owner PLAN→review→token loop consuming this plan-of-record; m-3 is the design/plan author + guide, NOT the builder; the m-3-pair token (…-174500) is gate-clearance of record, superseded as the execution dispatch by the s8 pair's own token

**Routing resolved.** m-3.implementer held correctly (`…-174700`) — a lint-forced same-owner m-3-pair token cannot silently convert the standing m-3.implementer session into the s8 build seat. **Operator ruling (2026-07-11): fresh s8 self-contained pair.** The s8 pair (`s8.planner` + `s8.implementer`, owner `s8`; `s8.reviewer` adversarial; m-7/m-2 as build-fidelity) runs its **own** PLAN → plan-review → `DISPATCH IMPL` loop **within owner s8** (lint-valid same-owner), **consuming this m-3 plan-of-record as its design/spec input**. m-3 (me) = design + plan-of-record author + **guide by relay**; **m-3.implementer = pair reviewer, NOT the builder** — its hold is resolved, stand down from the block.

**Token status (honest trail):** the m-3-pair `DISPATCH IMPL` (`s8-plan-m3-impl/…-174500`) is the **gate-clearance of record** — it proves every delegated-dispatch precondition passed (orchestrator authority, PLAN-REVIEW r2 approve, both SEQ-1 confirms, all-in SCOPE_DIFF, lineage). It is **NOT** the operative execution dispatch under this ruling; the **s8 pair's own same-owner token is** what starts code. m-3.implementer does not act on `…-174500`.

### What the s8 pair CONSUMES (the plan-of-record + binding inputs — re-derive nothing)
- **Plan-of-record:** `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2; 11 tasks incl. **Task 6.5** the staged pipeline). Adopt or re-cut the decomposition, but the scope/exit/fence/conditions below are binding.
- **Four EFFECTIVE locks:** `s8-design-m3-registry` r1+r1a+r1b · `s8-design-m7-executor` r4 · `s8-design-m7-config` r13 · `s8-design-m2-grammar` (Option B). Re-derive none.
- **SEQ-1 confirmed order (both halves in hand, no correction):** GENESIS(v5, `1ef6abab…`, FX-CFG-7) → registry-changeset(`v5→successor`) → observe-activation `config_change`; **dogfood = fresh-genesis spine store**; bless = existing-store migration built+fixtured in-slice, **live-store activation a named public-release carry**. (m-7 `s8-plan-m3-seq1/…-151621` + m-2 `…-153500`.)
- **Binding scope fence:** the all-in `SCOPE_DIFF` from the PLAN-REVIEW r2 (`s8-plan-m3/…-172400`) + restated in `…-174500` — do not exceed; a file outside it → escalate to master.
- **Conditions (a)–(g)** (orchestrator delegation `s8-dispatch/…-145129`): unique sub-DISPATCH_IDs per leg · registry/schema beyond the T2 changeset → master · locked-contract change → owning pair + master · cross-domain (m-1/m-6) → master · **merge operator-only** (`HUMAN_MERGE_AUTHORIZATION`) · blocked lane reports+holds · dogfood names the S7A-TRAIL-FINDINGS classes killed.
- **Riders:** interim defaults built AS INTERIM + labeled with the mandatory s10 sunset · Rails A/B stated per new surface · s9 fuzzy-claim adjudication out-of-scope (check-registry seam kept additive/open) · I-PH swept over every new seat-visible surface · the ten INV-CATALOG laws stay green · vertical-slice-first (exit legs 1+2 + the dogfood relaunch are slice-exit).

### m-3's ongoing role (guide)
I stand by for the s8 pair's design/plan questions on the observe semantics, blocker relays, and cross-lock seam clarifications — by relay, not by directing implementation. I do not spawn or drive the s8 pair. The s8 pair's PLAN + plan-review are theirs to run; where their PLAN needs a locked-contract interpretation, route the question to the owning pair (m-3 for observe/registry/probe/disposition; m-7 executor/config; m-2 grammar).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-handoff-pair/SITREP-planner-20260711-181500.md` — run below.
- Inputs cited: the operator staffing ruling (this turn); the routing-hold `s8-plan-m3-impl/…-174700`; the m-3-pair token `…-174500`; the plan-of-record + the four locks + the SEQ-1 confirms + the PLAN-REVIEW r2 fence.

ACTIONS_GIT_REF: none — a handoff/guidance report; no git action, no `frank/` edit, no code, no token issuance by this file. Sole artifact: master/relays/s8-handoff-pair/SITREP-planner-20260711-181500.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: operator boots the fresh `s8.planner`/`s8.implementer`(+`s8.reviewer`) sessions; the s8 pair runs its own PLAN → plan-review → same-owner `DISPATCH IMPL` consuming this plan-of-record; m-3 guides by relay and handles the eventual PR/implementation report + the merge-decision relay TO the operator at slice exit.
