## DESIGN — ITEM A CORRECTED + RE-ROUTED: `master/STEP-3-INTERFACE-LOCK.md` NEW external SHA-256 `cbd1893c7b1ed6a88aa4668ff6e333e037f6898069594b74f758918ad8913636` (supersedes the voided `3e99edd0…`). Folds VP+F73 r1: F1 record §6 replaced with the ratified §5.3 five edges at FULL literal paths (no `…/`/`same file`/expansion note); F2 corrected to **38 distinct files / 42 semantic rows** + the env-parity clause restored to exactly `env_digest-parity accepted disposition`; F3 the D7/`relay.submit` architecture consolidation COMPLETED + a full source-fold inventory below. Lane 4 remains HELD until this corrected record passes VP+F73. No new operator ratification (faithful realization of ratified rev7).

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-item-a
PARENT_DISPATCH_ID: step3-relock-item-a
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the correction is a faithful realization of the already-ratified rev7 `3443f73d…`; no new operator ratification is required (per VP r1 gate disposition). VP + F73 review of the corrected record completes item A.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-item-a
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-item-a/DESIGN-REVIEW-orchestrator-reviewer-20260727-150000.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-1.planner, m-1.implementer, m-2.planner, m-2.implementer, m-3.planner, m-3.implementer, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer
SUBJECT: VP + F73 re-review interface-lock record `cbd1893c7b1ed6a88aa4668ff6e333e037f6898069594b74f758918ad8913636` — the three r1 findings folded (literal §6 edges; 38 files/42 rows + exact env-parity clause; D7/relay.submit consolidation done + source-fold inventory); lane 4 held

## What changed vs `3e99edd0…` (now VOID)
Your r1 passed F73 (all 38 hashes recompute, additive, no owner byte moved) and returned three findings. All three folded:

- **R1-F1 (edge shorthand regressed).** You are right — I reintroduced the `…/`/`same file`/expansion-note indirection that r7–r9 removed. **Record §6 is now the ratified amendment §5.3 verbatim at full literal source AND target paths** — the four-half m-1 selector (distinct m-9-D and §D-co-sign source halves), m-9 C (§7/§9/§11), m-9 B (§8/§11), m-9 §9 receipts, m-10 producer — no abbreviation, no `same file`, no expansion note.
- **R1-F2 (count + clause identity).** The record now states **38 distinct byte-bound files across 42 semantic `{role,path,clause}` rows** (37 single-row files + the lane-2 close's 5 rows). The env-parity `carried_source` clause is restored to **exactly `env_digest-parity accepted disposition`**; the locus parenthetical is removed and **not relocated** (the m-1 `owner_base` hash captures the locus; §7 keeps only lineage). This transmittal makes no "37 files"/"one row per file" claim.
- **R1-F3 (source-fold GATE).** The D7/`relay.submit` consolidation was **owed post-ratification, not lane-4** — I mis-scoped it from a stale rev4 memory; ratified rev7 §3 lists it in the source-fold manifest and §7 has no lane-4 deferral. **It is now DONE:** `master/ARCHITECTURE.md:537` folds the §D-settlement resume additions (run-wide restore + `MAX_PARKED_ROWS_PER_RUN=512` + `parked_unknown_capacity_exceeded` run-terminal + `turn_failed` zero-attempt supersession) into the D7 durability bullet, and `:539`'s "**OWED**" clause is replaced with "**CONSOLIDATED 2026-07-27**" naming the `relay.submit` `canonical_resource` cell (`"relay.submit:" || SHA-256(JCS(target))`, totaled by `form_digest`). Full inventory below.

## Post-ratification source-fold — exact inventory (all durable, bundle→record-lock repoint + status)
- `ROADMAP.md` — §11 sequence steps 3–4 repointed + a 2026-07-27 milestone; stale "not started" marked superseded.
- `master/README.md` — top banner + line-164 board repointed to lane-2-closed / item-A ratified+authored+in-review (historical banner at :11 preserved).
- `master/ARCHITECTURE.md` — bundle→record repoint (items A/B/E §538–539) **+ the D7/`relay.submit` consolidation (§537 + §539)**.
- `master/domains/m-1-trust-identity/README.md`, `master/domains/m-2-forms-determinism/README.md`, `master/domains/m-3-observation-evidence/README.md` — "re-engage at the extraction bundle" → the record-lock + current status.
- `master/CYCLE-PLAYBOOK.md` (:408) — worked-example mechanism repointed (historical narrative preserved past-tense).
- `master/STEP-3-ITEM-A-RECIPE.md` — marked **WITHDRAWN** (header banner → the record-lock; r3 `06e6956e…` not built).
Residual bundle mentions across all eight are now past-tense/superseded only; each names the record-lock. Historical relays/ledger untouched (append-only). *(The `frank/` repo is untouched throughout.)*

## What I ask the VP (+ F73) to check
- Record §6 = ratified §5.3, full literal paths, four-half m-1 selector intact?
- 38 files / 42 rows stated; env-parity clause exactly `env_digest-parity accepted disposition`, locus not relocated?
- D7/`relay.submit` consolidation actually discharged (no residual "OWED") + the source-fold inventory complete?
- All 38 constituent hashes still recompute against the corrected record (only §6/§5/preamble prose changed; no row hash moved)?

## Boundaries
Lane 4 (Master+VP re-lock + exit-fixtures freeze), lane 5 (T4) remain HELD until this corrected record passes VP+F73. No owner/frozen/amendment byte moved (the record only names them). No PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action. **H-12 hard-blocks external use.**

## Verification
Corrected `master/STEP-3-INTERFACE-LOCK.md` external SHA-256 `cbd1893c7b1ed6a88aa4668ff6e333e037f6898069594b74f758918ad8913636` (the prior `3e99edd0…` is VOID — do not cite as current). Record now: 38 distinct constituent md paths; all 37 inline row-hashes recompute 0-mismatch; no `…/`/`same file`/bare-suffix in §6. `master/ARCHITECTURE.md` scanned: no residual "OWED into this architecture-of-record". Ratified contract `3443f73d…` UNMOVED; ratification relay `cabae8bd…` UNMOVED. Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — (a) corrected `master/STEP-3-INTERFACE-LOCK.md` (§6 edges literal, §5 clause, preamble count); (b) the post-ratification source-fold completed across the eight files inventoried above incl. the `master/ARCHITECTURE.md` D7/`relay.submit` consolidation; (c) this relay + one INDEX.md row. No owner/frozen/amendment byte moved, no re-lock issued (lane 4), no `frank/` action, no new ratification, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: VP + F73 re-review `cbd1893c…` (literal §6 edges; 38/42 counts + exact env-parity clause; consolidation discharged + source-fold complete; hashes recompute); on approve item A CLOSES → lane 4 (Master+VP re-lock over `cbd1893c…` + exit-fixtures freeze) → lane 5 (T4). No new operator ratification required. H-12 stands.
