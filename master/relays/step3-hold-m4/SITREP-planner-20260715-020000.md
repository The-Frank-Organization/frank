## SITREP — m-4 bounded status handoff on the HELD `step3-amend-m4-routing`: artifacts, state, and the split-boundary dependencies the reframe must disposition

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-hold-m4
PARENT_DISPATCH_ID: step3-hold-m4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded stop-work status handoff; reports state, proposes no design
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-amend-m4-routing-exact-lane
IN_REPLY_TO: step3-hold-m4/RECONCILE-orchestrator-planner-20260715-013540.md
FROM: m-4.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-4.implementer, m-2.planner, m-8.planner
BUNDLE_ID: m-4-routing-policy

**Stop-work acknowledged.** `step3-amend-m4-routing` is halted — no further design edits, grill, paired review, consumer
confirmation, lock, PLAN, or implementation. No source/credential/provider-call/external-send/merge/live-store action
taken; no `frank/` edit, no code. This is a bounded status report; it proposes no re-design of the split.

### 1. Current artifacts + paths
- **No m-4-authored artifact bytes exist under `step3-amend-m4-routing/`.** I had not yet filed an amendment DESIGN doc
  or a GRILL_LOCK — the grill was mid-flight (conversational with the operator, per [[no-inline-question-tools]]). So
  there is nothing to preserve-vs-rewrite in the dispatch dir; **this handoff is the durable record of the provisional
  grill decisions** (below).
- The held cue: `step3-amend-m4-routing/DESIGN-orchestrator-planner-20260715-005540.md`.
- Consumed context (read-only): `STEP-3-KICKOFF.md` §3/§4; m-8 charter + `m-8 audit 2026-07-14`; my locked design-of-record
  `master/domains/m-4-routing-policy/design/2026-06-29-v3-routing-policy-design.md` §2/§5/§10.
- Grill state also recorded in my working memory ([[m4-routing-record-shape]]), marked FROZEN/provisional.

### 2. Current verdict / lock state
DESIGN phase, **grill in progress** — Q1 settled, Q2 mid-grill. **No** DESIGN doc, **no** GRILL_LOCK, **no** DESIGN-REVIEW
requested, **no** DESIGN_LOCK, **no** PLAN. The amendment was **already blocked** before this hold: its consumer packet
(kickoff §6.4/B14) required m-8's lane-contract DESIGN/DESIGN-REVIEW/GRILL + the m-2 FieldSpec review, neither of which
had been consumed (m-8 is now itself stopped, `step3-hold-m8`). No consumer confirmations closed.

### 3. The salvageable governed-decision-record work (provisional, against the old framing)
Confirming the hold's "largely salvage" read — this is governance-plane / topology-independent:
- **Q1 (representation) — settled with the operator:** the governed routing decision references the exact lane as
  `lane_ref` (m-8 immutable lane ID, **authoritative**) + `catalog_snapshot_digest` (**authoritative**) +
  `lane_tuple` = `{model_id, provider_id, serving_profile_id, compat_mode}` as a **non-authoritative readable mirror**;
  conflict rule = digest-resolved lane wins, tuple flagged unverified (reuses the s6 Sharpening-D verified-when-provable
  pattern). All **payload**; `routing_assignments.gate_referenceable_columns` stays `["declared_deviated"]` (R2 intact).
- **Q2 (snapshot pinning) — mid-grill:** the exact-lane binding itself is mandatory for **no-silent-fallback**; the
  operator trimmed the whole-catalog-snapshot **heavy archival/resolution machinery to the Step-3 carry ledger** (a
  later deep-audit feature), keeping only a **tiny catalog-version fingerprint** (or reserve-the-field-deferred). Fully
  dropping even the fingerprint would reopen the VP+operator-ratified kickoff §4 "minimum pinning" — flagged as a
  master+VP call, not mine.

### 4. Unresolved findings + the dependencies the reframe must disposition
The dispatched record conflated the **governed decision** (stays conductor, m-4/m-2) with the **executable run binding +
invocation state** (now app-side). The split turns on these, which the ratified packet must set — I name them, I do not
resolve them:
1. **Where the exact four-axis lane binding lives — the load-bearing split-boundary.** Does the *governed decision
   record* pin the exact executable lane (with app-side invocation constrained to run *exactly* that), or does the
   decision pin at a higher policy level (e.g., capability bucket / abstract lane) with the concrete lane resolved by
   app-side invocation state? **The invariant the split must preserve either way:** the human-auditable, no-silent-fallback
   guarantee (V3) — the exact lane that will run must be governed/auditable *before* it runs; if resolution moves app-side,
   that guarantee has to be re-anchored at the app-side boundary, not lost. My Q1 lane-reference construction lands on the
   conductor decision record under the first reading; under the second it partly moves.
2. **R2 non-gate-referenceability must hold on BOTH sides of the split.** Wherever lane/model identity is recorded —
   conductor decision record and/or app-side invocation state — it stays non-gate-referenceable payload; the reframe must
   not let app-side binding become a gate input. This constraint is not negotiable and is currently guaranteed only for the
   conductor-side FieldSpec (allowlist `["declared_deviated"]`); an app-side binding needs its own equivalent guarantee.
3. **What the app-side binding consumes from the m-8 lane catalog.** The app-side invocation needs concrete lane facts
   (endpoint/compat) from m-8's catalog; the boundary must define what app-side reads from m-8 vs. what the governed
   decision already pinned. This is entangled with m-8's own reframe (connector app-side; m-8 = likely connector actuator /
   last-pre-wire enforcement host) — the two lanes must be dispositioned together.
4. **The §12 "record doesn't change between steps" tension is now compounded by the split:** part of the record stays
   (governed decision), part moves (app-side invocation) — the amendment's *home* and its *boundary against app-side
   invocation state* are set by the ratified packet, not by me.

### 5. Cross-lane
Noted: the m-7 credential r3 lane + its three paired reviews are provisional audit input only (no r4/lock). My amendment's
m-2 FieldSpec review and m-8 consumer packet never opened; nothing owed back to those lanes until re-dispatch.

## Verification
- `ls master/relays/step3-amend-m4-routing/` — only the orchestrator cue `DESIGN-…-005540.md`; **no m-4-authored file** (nothing to preserve-vs-rewrite).
- Grill decisions cross-recorded in [[m4-routing-record-shape]] (marked FROZEN/provisional against the old framing).
- No `frank/` edit, no code, no PLAN, no lock; relay-lint run below.

ACTIONS_GIT_REF: wrote this status relay + appended `master/relays/INDEX.md`; no design-doc/GRILL_LOCK authored (grill was mid-flight), no `frank/`/code, no PLAN, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: master folds this into the Step-3 architecture reframe; m-4 holds — no further design until the operator-ratified architecture-amendment packet + refreshed consumer audit + a replacement single-author design dispatch land.
