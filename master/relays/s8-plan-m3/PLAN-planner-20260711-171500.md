## PLAN — s8 observe-spine PLAN r2: F1 folded (Task 6.5 = the step-3 `authority_class` + step-4.5 completeness + Option-B `surface_intent` staged pipeline, owner split) + the `…-163144` addendum (SEQ-1 steer · Rails A/B · s9 seam); re-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-plan-m3/PLAN-REVIEW-implementer-20260711-155230.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r2 fold of F1 (the missing m-2-locked staged pipeline — now Task 6.5 with the named owner split; decision-② T7 consumes its computed authority_class) + the s8-dispatch addendum (fresh-genesis-first steer, Rails A/B build-time criterion, s9 seam kept open); SEQ-1 pre-token gate preserved (m-7 confirmed, m-2 pending); re-review → PLAN_REVIEW_VERDICT

m-3.implementer — F1 confirmed correct and folded; the addendum (`s8-dispatch/…-163144`) folded alongside. Revised plan: `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2). Re-review → `PLAN_REVIEW_VERDICT`.

### F1 (blocker) — the missing staged pipeline — FOLDED as **Task 6.5**
You were right: I keyed decision-② (T7) on computed `authority_class` and relied on Option-B `surface_intent`, but assigned no executable task producing them. Added **Task 6.5 — the m-2-locked staged pipeline** (`s8-design-m2-grammar:64-77`/`:91-94`/`:138-140`):
- **step-3 `authority_class` compute** (in `submit.go`, after `slot_in`/gate-category classify, before the observe hook) — so T7 reads a **computed** value, never absent/lane-supplied. **T7 now explicitly `Consumes: T6.5`.**
- **step-4.5 profile-aware completeness** (new `frank/internal/engine/completeness.go`, **m-7-hosted**) + **Option-B `surface_intent` derivation** — non-gate-bearing only, `progress ⇐ otherwise`, **absent** on gate-bearing; validates producer manifests before append.
- **owner split named:** m-2 = the `authority_class` formula + registry amendment (rides T2); m-5/m-6 = the `surface_intent` producer/profile contract (already locked in m-2 grammar §8:198-199 — consumed, not authored); m-7 = hosts step-4.5; m-3 = consumes for disposition + observe-output completeness.
- **oracle (F1 item 4):** a test proving **m-3 cannot write** `authority_class` or `surface_intent`, matching the m-2 fixture oracle; producer-outside-manifest → rejected.
- Threaded into the file map (`submit.go` step-3, new `completeness.go`, `loop.go:142-159`), milestone deps (T6.5 after T3–T6, before T7), acceptance criteria 7, and the self-review lock-coverage (m2-grammar → T1/T2/**T6.5**).

### The `…-163144` addendum — FOLDED
- **SEQ-1 steer (addendum 1):** fresh-genesis-first is now the operator-ratified default (matches my answer + m-7's confirm). Named the bless moment: **T10 builds + fixtures the bless mechanism in-slice; activating it against the actual shipped live store is the public-release-migration NAMED CARRY (not s8)**. Fill-gate exposure stated either way.
- **Rails A/B (addendum 2):** added as the **build-time review criterion** in Global Constraints; every new surface carries a one-line Rail-A open/closed choice + a Rail-B pass (no adversarial-only mechanism; confusion-grade wording). Acceptance criterion 8. T6.5 states its rails inline (`surface_intent` additive/open; `authority_class` gating closed/fail-closed).
- **s9 seam (addendum 3):** s8 stays deterministic E1+E2; the s9 fuzzy-claim adjudication rung is out-of-scope; the check-registry stays **additive/open (Rail A)** so a future adjudication `CheckEntry` doesn't reopen the spine.

### Preserved from your accepted shape
- **SEQ-1 pre-token gate kept:** no delegated token until BOTH confirms in hand; a correction reissues affected tasks. **m-7 half CONFIRMED** (`…-151621`, both legs, no correction); **m-2 half pending.**
- The vertical slice T3–T6 order, the handler-side pre-append observe (`loop.go:142-159`), the T7 timeout/no-vantage fixture pair, the T5 executor boundary, and the §6.1 I-PH conductor-side enforcement — all unchanged.
- Your all-in SCOPE_DIFF fence honored; the r2 additions are `frank/internal/engine/completeness.go` (new, step-4.5) + `frank/test/fixtures/s8_pipeline_stages_test.go` (new) — both inside/adjacent your fence; `submit.go`/`loop.go` already in-fence. No file outside your set.

### Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-plan-m3/PLAN-planner-20260711-171500.md` — run below.
- Anchors: `s8-design-m2-grammar:64-77,91-94,138-140,198-199` (the staged pipeline + the m-5/m-6 surface_intent contract); the plan-review `…-155230` (F1 + the SCOPE_DIFF fence); the addendum `s8-dispatch/…-163144`; the m-7 SEQ-1 confirm `…-151621`; frank `submit.go`/`loop.go:142-159`, `main@691d034`.

ACTIONS_GIT_REF: none — plan revision; no `frank/` edit, no code, no commit. Artifacts: the r2 plan-doc deltas (Revisions line, SEQ-1 steer, Global-Constraints Rails A/B + s9 seam, file map, Task 6.5, T7 consumes-line, out-of-scope, acceptance 7+8, self-review) + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: m-3.implementer re-review the r2 fold → `PLAN_REVIEW_VERDICT`. On approve + BOTH SEQ-1 confirms in hand, I issue the delegated `DISPATCH IMPL` token per conditions (a)–(g) and the operator boots the slice team. A remaining must-revise returns the exact task + finding.
