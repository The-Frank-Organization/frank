## PLAN — s8 OBSERVE-SPINE slice plan authored (four locks mapped, SEQ-1 answered, 11 tasks / 4 milestones); m-3.implementer PLAN-REVIEW requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — plan-review leg; the two human legs (session boot, operator-only merge) ride the build, not this review
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-design-m3-registry/DESIGN-REVIEW-implementer-20260710-235210.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the s8 observe-spine PLAN is authored (consumes the 4 EFFECTIVE locks, re-derives nothing; SEQ-1 answered + routed to m-7/m-2; vertical-slice-first; exit legs 1+2; interim defaults labeled w/ s10 sunset) — adversarial PLAN-REVIEW → PLAN_REVIEW_VERDICT; on approve my delegated token dispatches the build under the named conditions

m-3.implementer — per the delegated PLAN dispatch (`…-145129`), I authored the s8 observe-spine slice PLAN: **`master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md`**. This is the phase-opener slice (kickoff order-of-record position 2). Adversarial plan-review, please → return `PLAN_REVIEW_VERDICT` (approve | must-revise | reject-narrow | human-decision-required). On approve, my **delegated token** (this `s8-plan-m3` chain, unique sub-DISPATCH_IDs) dispatches the build under conditions (a)–(g); the operator boots the named slice team.

### What the PLAN carries
- **Scope of record** = the kickoff r4 s8 bullet verbatim: activation → `observe_gate()` in the serialized commit path (post-form/lineage + post-`slot_in`, pre-append) → one E1 read-check + one E2 registry check under the isolated executor with hard timeouts → conductor-side stamps → typed false-done bounces / observed true-done accepts → the decision-② legs → the s5-b (h) guard → adversarial fixtures. **Exit-test legs 1+2 live here; dogfood relaunch on this slice's store.**
- **Four EFFECTIVE locks consumed, re-derived none:** m3-registry r1+r1a+r1b (T3,4,6,7,8,11) · m7-executor r4 (T5,11; FX-EXE-1..6) · m7-config r13 (T1,2,9,10; FX-CFG-1..15) · m2-grammar (T1,2; Option B + the registry changeset).
- **SEQ-1 answered explicitly + routed to m-7/m-2** (`s8-plan-m3-seq1/SITREP-planner-20260711-153000`): order = GENESIS(v5, `1ef6abab…`, FX-CFG-7 unchanged) → registry-changeset as the fieldspec `v5→successor` transition → observe-activation `config_change`; **bless/adoption = existing-store migration, NOT the dogfood path; dogfood = the fresh-genesis spine store** (sidesteps the §5.1 deadlock). The offline-bless fill-gate exposure is STATED (pre-serve bootstrap, not `:136`-gated) and pending the m-7/m-2 confirm — a **named gate before T2/T10 lock**.
- **11 tasks / 4 milestones**, writing-plans-disciplined (exact `frank/` file:line targets, interface signatures, the fixture each discharges, TDD/commit steps, no placeholders): A config+changeset (T1,2), B the vertical observe spine (T3 hook · T4 E1 · T5 executor+E2 · T6 verdict binding), C dispositions+guard+activation (T7 decision-②/timeout · T8 suppliability guard · T9 activation+dogfood+exit legs), D migration+hardening (T10 bless · T11 adversarial+I-PH+INV-CATALOG-green).
- **Global constraints** carry the ratified-interim defaults **labeled interim with the mandatory s10 sunset**, the governed-activation rule, the v1 handle-surface ceiling (no OS-sandbox work), the §6.1 I-PH contract, the battery/trail discipline, operator-only merge.
- **Team shape** (fresh slice team, sole-writer per seat): `s8.lead` (m-3-guided), `s8.config` (m-7 fidelity), `s8.registry` (m-2), `s8.reviewer`; B10 not in effect.
- **Delegation conditions (a)–(g)** restated as binding on the token I issue.

### Adversarial targets (push hardest here)
- (a) **SEQ-1 correctness:** is GENESIS-then-transition the right read of the genesis §5 hash-pin vs the changeset's `v5→successor`, or does the changeset need to precede genesis (forcing FX-CFG-7's hash to move)? Is fresh-genesis-for-dogfood the right call vs blessing the shipped store? (I've routed it to m-7/m-2 — flag if my proposed order is wrong on its face.)
- (b) **Vertical-slice integrity:** does T3–T6 actually stand up the thinnest end-to-end observed relay before the taxonomy thickens (T7+), or did I bury a hidden dependency (e.g., does the exit-leg-1 false-done bounce need decision-② from T7 before T3 can accept/reject)?
- (c) **Lock fidelity:** does any task silently change a locked contract (the §3.1 allowlist, the §3.3 disposition, the closed `CheckVerdict`, the byte-exact enum)? Delegation condition (c) forbids it — check T2 (registry changeset) and T5 (executor) especially.
- (d) **No timeout→accepted regression:** T7 must keep timeout on the machinery-fault edge (never non-authority `accepted`+`self_reported`) — the exact r1 F1 correction; confirm the plan encodes it as a test, not prose.
- (e) **Right-sizing:** are the tasks reviewer-gateable units (each independently rejectable), or should any split/merge? Is T5 (executor, m-7 co-owned) correctly a build-fidelity integration point rather than m-3 authoring m-7's content?

### Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-plan-m3/PLAN-planner-20260711-154500.md` — run below.
- Inputs: the four locks (m-7 executor r4 `2026-07-11-s8-executor-host.md`; m-7 config r13 `2026-07-11-s8-config-host.md`; m-2 grammar `2026-07-10-s8-config-atom-grammar.md`; my `2026-07-10-s8-check-registry-probe-design.md` r1+r1a+r1b); the reconcile `s8-design-m7-config/RECONCILE-…-145128`; the dispatch `…-145129`; frank `main@691d034`.

ACTIONS_GIT_REF: none — plan authoring; no `frank/` edit, no code, no commit (the build starts only after your approve + my delegated token). Artifacts: the plan doc `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` + the SEQ-1 COORD + this relay + INDEX rows.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (tag `s7-close`+; post-s7b).
Next requested action: m-3.implementer adversarial PLAN-REVIEW → `PLAN_REVIEW_VERDICT`. On approve (and the SEQ-1 m-7/m-2 confirms in hand), I issue the delegated `DISPATCH IMPL` token per conditions (a)–(g) and the operator boots the slice team. A must-revise returns the exact task + finding; I fold and reissue.
