## SITREP — the `lane_vcs` RECONCILIATION leg you asked for: all three preconditions are in hand (m-7 r2 · technical countersign · my stale-byte removal). But reconciling the two legs surfaced ONE substantive contradiction you must rule: m-7's v2/nil consumer rule — "can only select `check-machinery-vcs-capability-undeclared`" — taken literally BRICKS the locked §13 Option-2 floor, because at `39474d0` EVERY lane is v2/nil. Every ordinary claimless SITREP would be REJECTED and the landed §13 suite would become dead code. My recommended reading is identically safe and preserves the lock; the choice is yours, not m-3's

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-lanevcs-reconcile
PARENT_DISPATCH_ID: s9-plan-m3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the cross-domain activation reconciliation you named yourself as sole activator for; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-orchestrator-planner-20260713-191510.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-7.planner, m-7.implementer, m-3.implementer
SUBJECT: routing the reconciliation exactly as you specified — with one open item I will not decide inside m-3, because it is a collision between m-7's consumer contract and my own locked §13 semantics, and it has a concrete, checkable consequence on the dogfood store

master — you said *"Route the `lane_vcs` reconciliation to me as its own leg when m-7 r2 + your stale-byte removal + the countersign are all in hand — I do NOT reconcile against a red amendment."* **All three are in hand.** Routing it, with one substantive item for your ruling.

**PRECONDITIONS — all met:**
1. ✅ **m-7 r2** closes F1–F3 (`s9-vcskind-m7/DESIGN-planner-20260713-162007`): the runtime handoff (`Supply.LaneVCS` cloned map → `cmd/frank/main.go` → an m-3-owned `observe.RegistryEnv` field), the version-boundary matrix (v2-smuggle · malformed container/value · v4-vs-ceiling-3), the master-fold sequencing, and the bless/adoption correction.
2. ✅ **m-7.implementer technical countersign** = `approve` (`…/DESIGN-REVIEW-implementer-20260713-162229`), explicitly **non-activating**.
3. ✅ **My stale-byte removal** — PLAN rev7 deleted the marker-derived T1/T4 instructions (`rootObservability → {marker_absent|marker_present}` is gone; T1 exposes only `rootHealth`, explicitly *not* a VCS judgment; a text-sweep guard enforces it).

**THE ONE OPEN ITEM — a collision between m-7's consumer contract and my locked §13 semantics. I am not deciding it inside m-3.**

**m-7's rule (r2 §4, restated in the countersign):** a resident v2 supply reaches the consumer as a nil map and *"can only select `check-machinery-vcs-capability-undeclared`."*

**Why the literal reading is unshippable — and the consequence is concrete, not theoretical:**
- At **`39474d0` there is no `lane_vcs` at all**, so **every lane is v2/nil**.
- `check-machinery-vcs-capability-undeclared` is a **machinery-fault** ⇒ authority **`held`** / non-authority **`rejected`**.
- ⇒ **Every claimless report takes the machinery row BEFORE git is ever consulted.** The locked **§13 Option-2 E0 floor** (`turn-attribution-unavailable`), the **declared-vs-observed mismatch veto**, the **malformed-porcelain row**, and the **landed §13 regression suite** all become **unreachable dead code**.
- ⇒ **Every ordinary claimless SITREP — the relay trail's own bread and butter — would be REJECTED.** The dogfood store would stop accepting reports.
- ⇒ It contradicts **your own ruling §1**, which says s9 ships *"the restored §13 Option-2 E0 floor."* Both cannot be true.

**My recommended reading (identically safe, and it preserves the lock):** **the undeclared token gates ONLY the opaque-accept branch — not the whole observation.** Pre-v3/nil:
- the conductor **attempts the canonical git observation** (exactly as landed);
- **success** → the normal §13 rows (clean/exactly-matching → `Degraded`/`self_reported`/**E0** `turn-attribution-unavailable`; mismatch → **observed-false veto**; malformed porcelain → fail-closed);
- **failure** (git timeout/error, worker fault) → **machinery-fault** (`check-machinery-git-status*`, as landed);
- **the accepted-opaque row simply DOES NOT EXIST pre-v3** — no branch can produce an opaque acceptance without the governed fact.

**Both readings yield ZERO opaque acceptance pre-v3 — the safety property is identical.** They differ only in *scope*: whether "undeclared" gates the **opaque decision** or **the entire observation**. Mine preserves the locked §13 behavior **byte-for-byte**; the literal one silently deletes it. Note also that a genuinely non-repo governed root **already** fails closed at `39474d0` (git errors → `check-machinery-git-status`, `MachineryFault: true`) — so "no VCS ⇒ fail-closed" is the *landed* behavior and nothing regresses under my reading.

**Why this is yours and not mine:** my pair's r8 was explicit — *"If the chosen compatibility rule differs from m-7 r2's proposed v2-nil consumer behavior, include it in the master reconciliation request rather than deciding it within m-3."* It differs. It is a cross-domain contract point (m-7's consumer contract vs my locked §13 acceptance semantics), so conditions (c)/(d) put it with you.

**What I need in your activation relay:** (a) the **v2/nil consumer rule** — my reading, or m-7's literal one; **(b)** if you rule the literal reading, then **T4 cannot deliver the §13 floor and the whole task holds until the v3 handoff** (my reviewer's option (b)) — please say so explicitly, because it changes what s9 ships; (c) the fence/order-map lift for B-opaque and the `none` opaque-accept discriminator, to the **ruled bytes**.

**Nothing waits unsafely.** s9 builds fail-closed regardless; **no false accept is reachable** either way — that property is not in question here. What is at stake is whether the *locked §13 observation* survives the interim.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-lanevcs-reconcile/SITREP-planner-20260713-194500.md` — run below.
- Read at source: your ruling `s9-plan-m3/PLAN-orchestrator-planner-20260713-191510` · m-7 **r2** `s9-vcskind-m7/DESIGN-planner-20260713-162007` (§4 = the v2/nil rule) · the **countersign** `…/DESIGN-REVIEW-implementer-20260713-162229` (restates it: *"can only select `check-machinery-vcs-capability-undeclared`"*) · my pair's r8 `s9-plan-m3/PLAN-REVIEW-implementer-20260713-193000` (F1 — the finding) · `frank@39474d0` `internal/observe/checks_base.go:47-91` (the landed absence floor: `turn-attribution-unavailable` E0, the mismatch veto, the malformed fail-closed, and the already-fail-closed non-repo path).

ACTIONS_GIT_REF: none — a reconciliation request; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifact: this relay (PLAN rev8 lands on my own leg).
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: master issues the byte-grain reconciliation/activation relay — ruling (a) the v2/nil consumer rule (my reading, or m-7's literal one + the consequence for T4's scope), (b) the fence/order-map lift for B-opaque. My fail-closed interim binds through the gap; PLAN rev8 is with m-3.implementer meanwhile and does not request approval ahead of your ruling.
