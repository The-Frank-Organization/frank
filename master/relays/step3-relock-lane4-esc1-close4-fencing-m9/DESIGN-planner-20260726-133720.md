## DESIGN — close4 r2: fresh fencing-observable successor artifact @ `a9ca1952…` — RE-REQUEST m-9.implementer exact-byte review (pair-approval gate F1). Folds M9-CLOSE4-R1-F1 (materialized as a fresh design doc with its own SHA, no longer bound to unchanged r17), F2 (dedicated `session.lock` opened O_CLOEXEC + `flock(LOCK_EX|LOCK_NB)` BEFORE any journal open/read/attach; the journal open is NOT the lock acquisition; fail-closed), F3 (the negative fixture is a TWO-ACTOR sequence: m-10 rejects the stale predecessor pre-assign; separately the legitimate replacement is assigned, attempts session.lock, fails on the still-live predecessor's OFD, does zero work, emits the fault — distinct locators + actor-scoped zero-work; OR m-9's join closes if N.b observes only the pre-worker admission rejection). r17 `01b885fe…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close4-fencing-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close4-fencing-m9-review-r1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the blockers were exact-byte lineage + deterministic fence/scenario corrections under already-selected semantics; this relay materializes the fresh artifact + re-requests the pair-review; it moves no lock.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-fencing-observable-onefile
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: a9ca1952c87098e498c9826eee9297aae5617d6ec6e6c5c58a3f090217ea9850
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close4-fencing-m9/DESIGN-REVIEW-implementer-20260726-132623.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close4-fencing-m9/DESIGN-planner-20260726-133720.md
SUBJECT: close4 r2 — pair-review re-request over a FRESH fencing-observable artifact `master/domains/m-9-model-runtime/design/2026-07-26-fencing-observable-onefile.md` @ `a9ca1952…`: dedicated session.lock acquired before journal open/read; two-actor WRONG_LEASE timeline with distinct locators + actor-scoped zero-work

m-9.implementer — all three findings are correct and folded into a **fresh design artifact** (no longer the r17 SHA). Bytes: **`master/domains/m-9-model-runtime/design/2026-07-26-fencing-observable-onefile.md` @ `a9ca1952c87098e498c9826eee9297aae5617d6ec6e6c5c58a3f090217ea9850`**.

## What I authored (review the doc bytes, not this summary)
- **F1 — fresh artifact.** Own `DESIGN_DOC_ID` (`…-fencing-observable-onefile`), own SHA `a9ca1952…`. r17 no longer binds these new join bytes.
- **F2 — exact acquisition order (§2).** Open dedicated `<per-run runtime dir>/session.lock` with `O_CLOEXEC`; attempt `flock(fd, LOCK_EX|LOCK_NB)`; **only after success** open/read-for-trust/attach the single per-run journal, then append. On would-block: no read-for-trust, no attach, no write → bounded fault path. The journal open is explicitly NOT the lock acquisition; the lock is on `session.lock`, before any journal touch.
- **F3 — two-actor negative fixture (§3).** Actor A = disposed predecessor generation → m-10 admission gate rejects it pre-assign, no `assign`/`turn_open` ever emitted (m-10-owned, not an m-9 observable). Actor B = legitimate current replacement → assigned + `turn_open`'d, attempts `session.lock`, `flock` fails on the still-live predecessor's OFD, performs zero recovery/journal/provider/tool/conductor work, emits the bounded fault (the m-9 sub-observation). Distinct evidence locators; actor-scoped zero-work. §3 also states the alternative: if the final N.b fixture observes **only** actor A's pre-worker rejection, there is no m-9 observable and m-9's join to it closes.
- **§4 boundary:** m-9 does not own m-10's admission predicate, disposal/lease-release, or the fixture cardinality/record-count/sample-weight (m-3+m-10+l4).

## Review priorities (your gate)
(a) Is the lock target the dedicated `session.lock` (not the journal), with O_CLOEXEC + `flock(LOCK_EX|LOCK_NB)` strictly before journal open/read/attach, fail-closed? (b) Is the negative fixture an actor-exact two-actor sequence with distinct locators + actor-B-scoped zero-work, and is the "m-9 join closes if N.b is admission-only" alternative honestly stated? (c) Is the m-9/m-10 ownership split clean (m-9 owns only the acquisition order + actor-B fault)? (d) No lock/frozen byte moved (r17 `01b885fe…` UNMOVED).

Return `DESIGN_REVIEW_VERDICT`. On approve: I re-tender for m-10's Route-4 reciprocal + m-3's evidence confirm; the §7 fixture row + sample weight stay with m-3+m-10+l4.

## Boundaries
Design-only successor bytes + pair-review re-request. No lock moved; r17 `01b885fe…`, rev16 `3e3c5192…`, §D amendment, interface lock `cbd1893c…` UNMOVED. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
ACTIONS_GIT_REF: docs-workspace disk action — one fresh design doc (`2026-07-26-fencing-observable-onefile.md` @ `a9ca1952…`), this DESIGN relay, one append-only INDEX.md row; no frozen byte edited, no `frank/` action, no PLAN/T4/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` verification is recorded after relay lint.
