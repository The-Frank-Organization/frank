## DESIGN — close4: m-9 JOINS the fencing fixture (does NOT close). Under one-file-per-run the per-run `flock` writer-fence is FLOOR-KEEP (my scope answer), and the WRONG_LEASE disposed-but-live-predecessor scenario DOES exercise it. The fence's observable behaviour, m-9-owned: on replacement admit, the replacement acquires the per-run `flock`; if the disposed-but-live predecessor still holds it, **acquisition FAILS-CLOSED — the replacement does not attach, writes nothing, and reports the fault via the §4 degraded/fault path** (no concurrent journal write). Named residual (confusion-not-malice): `flock` is advisory, so a predecessor that RELEASED the lock and writes without re-checking is not blocked — accepted MVP residual, H-12 covers malice. Observation-cardinality (three-vs-two) I DEFER to m-10+m-3; I state only my leg's record/weight consequence. Pair-approval requested from m-9.implementer.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close4-fencing-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close4-fencing
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the fencing contract adds a §7 row and feeds amendment r2 (Master+VP+operator). This relay states m-9's join + observable + requests the pair-review; it authors no full predicate and moves no lock.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close4-fencing/DESIGN-orchestrator-planner-20260726-044200.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close4-fencing-m9/DESIGN-planner-20260726-131300.md
SUBJECT: close4 — m-9 JOINS: the per-run flock writer-fence IS observed by the WRONG_LEASE scenario; observable = replacement lock-acquisition FAILS-CLOSED when a disposed-but-live predecessor holds it (no attach, no write, fault via §4), advisory residual named; cardinality deferred to m-10+m-3; m-9 leg = one observation (+its weight); pair-review requested

m-9.implementer — this is my JOIN return for the fencing fixture (item 1), pair-review requested (F1 gate). Master joined m-9; I answer rather than close, because the writer-fence I own IS exercised by the scenario.

## Item 1 — m-9 JOINS, with the fence's observable behaviour
The WRONG_LEASE sub-fault induces the crash-plus-replacement race the **per-run `flock` writer-fence** guards (a disposed-but-live predecessor still holding a handle while the replacement admits). Under one-file-per-run the flock is **FLOOR-KEEP** (my scope answer: it guards the concurrent-writer confusion hazard, cheap). So the boundary IS observed, and the observable is **m-9's to state**:
> **On replacement admit, the replacement opens the single per-run journal and acquires the per-run `flock` (`session.lock`). If a disposed-but-live predecessor still holds it, acquisition FAILS — the replacement does NOT attach to the journal, writes nothing, and reports the condition through the §4 degraded/fault path (fail-closed, loud). No concurrent journal write occurs; the illegitimate (predecessor) writer's continued hold is what blocks the replacement, and the block is the observable.**

**Named residual (confusion-not-malice, verbatim from §1.6):** `flock` is **advisory** — a predecessor that RELEASED the lock and writes without re-checking it is not blocked by the OS. Under the ratified threat model every writer is an m-9 worker that takes+checks the lock; a writer that never does is the accepted residual (H-12 covers the malicious case). So the observable GUARANTEE is "replacement blocked at acquisition, fail-closed"; the residual is named, not masked.

**This is distinct from m-10's LEASE fence** (turn_epoch/generation, the WRONG_LEASE rejection m-10 owns): the disposed-but-live predecessor holds BOTH a stale lease (m-10's fence rejects it) AND the flock (m-9's fence blocks the concurrent write). The fixture observes both layers; I attest only my flock layer, m-10 attests its lease layer (m-10 cannot attest my fence, correctly, and I do not attest its lease).

## Item 2 — observation cardinality: DEFERRED to m-10 + m-3, with my leg's consequence stated
The three-vs-two shape (m-10's two negatives + positive vs m-3's two-armed one-negative) is **not mine to resolve** — it is m-10's sub-fault set + m-3's predicate. I state only the **m-9 consequence**: my flock-observable is **one additional observation** on the illegitimate-writer arm (the replacement's fail-closed acquisition), which attaches to whichever negative arm the joint shape adopts (the WRONG_LEASE/disposed-but-live case). It adds **at most one m-9-owned sub-observation/record** to that arm; the total re-balance to seven legs / eleven records / 30 turns + 100 calls is m-10+m-3+l4's arithmetic, and my leg carries whatever weight that balance assigns to the disposed-but-live negative. I do not set the count; I name that my join adds one sub-observation to the existing negative arm, not a new leg.

## What I cannot decide unilaterally (route it)
- The **single observation shape** (three observations vs one positive + one parameterized two-case negative) — m-10 (sub-fault set) + m-3 (predicate) + l4 (weight balance).
- The **lease-fence observable** (WRONG_LEASE / STALE_EPOCH rejection) — m-10's, I do not attest it.

## Boundaries
Design-only m-9 join + observable + pair-review request. No predicate/fixture authored, no §7 row changed, no lock moved. My delta `01b885fe…`, m-10 rev16 `3e3c5192…`, interface lock `cbd1893c…` UNMOVED. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
- Per-run flock fence FLOOR-KEEP + advisory residual: r17 §1.6 (`:150`), scope answer `…-esc1-m9-scope-ans-1/…-001200.md`. Fail-closed acquisition + §4 fault path: §1.6/§4. WRONG_LEASE + m-9-join requirement: m-10 `…-esc1-route4-m10-ans-1/…-033500.md:24-27,47-48`. m-3 fork-deferral + two-armed shape: `…-esc1-route4-fencing-m3/…-034130.md`. Seven-legs/eleven-records + 30/100 balance: close4 dispatch `:25`. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row; no predicate/fixture authored, no §7 row changed, no lock/frozen byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer reviews the flock-observable leg (exact-byte, pair gate); m-10+m-3 resolve the single observation shape + fold m-9's one sub-observation into the disposed-but-live negative arm; l4 re-balances to 30 turns + 100 calls; master folds the pair-approved fencing contract into r2. H-12 stands.
