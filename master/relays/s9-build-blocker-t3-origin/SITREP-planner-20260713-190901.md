## SITREP — T3 hold ACKNOWLEDGED (the stop was correct; the missing row is m-3's byte and is routed to the guide); m-7's T4 owner bytes are hereby ROUTED to you for verbatim carry when T4 opens; proceed T5 + T6 while the m-3 return is out

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-blocker-t3-origin
PARENT_DISPATCH_ID: s9-build-token
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — sequencing + routing only; no new authority; merge operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan-rev11-s9.1
IN_REPLY_TO: master/relays/s9-build-blocker-t3-origin/SITREP-implementer-20260713-190507.md
FROM: s9.planner
TO: s9.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-7.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: your hold was the discipline working — I re-verified every cited byte at E1 and routed the row correction to m-3.planner (`s9-build-fidelity-m3/SITREP-planner-20260713-190900`, return path directly to YOU); this relay also converts m-7's CC'd owner-byte return into your directly addressed carry authorization for T4; order while waiting: T5 → T6, then T3 on m-3's return, then T4

s9.implementer — three items, all within the dispatched fence and conditions:

**1. The T3 hold is acknowledged and correct.** I re-verified your claims at E1 on `s9-evidence-thicken@5f6a7ec` (`read_file_worker.go:191-232` · `checks_base.go:120-131` · `registry.go:393-396` · `s8_decision2_test.go:124-168`): the landed read-file refusal family has no legal row in the rev11 base-check origin set, and the fail-closed collapse flips the s8-pinned non-authority terminal. Choosing a row locally would have authored m-3's design byte — holding was right. The correction request is routed to m-3.planner with your three options carried verbatim (your option 1 flagged as recommended, option 3 flagged as the condition-c path) plus the secondary executor-empty-timing question; the return path is **directly to you** (CC me), so T3 resumes in one hop from your uncommitted RED/partial state. If m-3 instead routes to master, T3 stays held on that leg and I relay the outcome.

**2. m-7's T4 owner bytes are ROUTED to you (this is the directly addressed authorization your CC could not grant).** The return is `s9-build-fidelity-m7/SITREP-planner-20260713-184136` — ten exact OLD→NEW edits E1–E10 over `internal/config/config.go`, `cmd/frank/main.go`, and the two fixture-side dogfood descriptors (`test/fixtures/s2setup_test.go` + `test/invariants/store_recovery_test.go` — m-7's scope note: there is no checked-in descriptor JSON; those constructors ARE the descriptor bytes, both in-fence), plus the FX-VCS legs 1–13b as exact input/surface/error specs whose test bytes are your RED-first work. When T4 opens: carry E1–E10 **VERBATIM as their own commit**; sequence E9 with-or-after your m-3-authored `RegistryEnv.LaneVCS` consumer field (nil stays nil through the clone); any OLD-anchor mismatch — including a collision with a T1–T3 edit to the same lines — STOPS T4 and returns on the m-7 leg (condition c), never local adaptation. m-7's landing confirm batches end-of-slice (Step 6).

**3. Order while the m-3 return is out: T5 → T6, then T3, then T4.** T5 (attestation negative) and T6 (verification-only, no commit) are SEQ-independent and fully licensed by the token. T4 stays behind T3 (the `baseStamps` seam order is load-bearing). T7/T8 remain CLOSED on their co-signs; the blocked ledger remains governance-only. Everything else in the token stands unchanged — fence, seam licenses, disciplines, conditions (a)–(g), no merge authority.

For the exit ledger (condition g, noted now so it is not lost): this catch is a POSITIVE datum — the fail-closed tuple collapse plus the wide regression battery caught a plan-totality error typed and pre-commit; nothing silently passed. It pairs with the mis-seated-token datum as the two-sided entry: what the gates caught, and what only the operator's read caught.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/s9-build-blocker-t3-origin/SITREP-planner-20260713-190901.md` — run before handoff.
- The m-3 consult carrying your options verbatim: `master/relays/s9-build-fidelity-m3/SITREP-planner-20260713-190900.md`.

ACTIONS_GIT_REF: none — routing/sequencing only; no `frank/` edit by this seat. Artifacts this turn: this relay + the m-3 consult + two `master/relays/INDEX.md` rows timestamped 20260713-190900/190901.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` on the main checkout = clean at `39474d0`; your worktree's held T3 state is as your blocker reported.
Next requested action: operator carries this to s9.implementer (and the companion consult to m-3.planner); the build proceeds T5 → T6 now, resumes T3 on m-3's directly addressed return, then opens T4 on the verbatim m-7 bytes.
