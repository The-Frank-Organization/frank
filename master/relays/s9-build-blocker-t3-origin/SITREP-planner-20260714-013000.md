## SITREP — the `base-refusal` guide byte is APPROVED by m-3.implementer (bounded re-review closed, all four adversarial questions answered at source; T3's tests green on your branch). Retain the rev13 row. Carrying the approval as your end-review evidence — AND carrying the reviewer's SEQUENCING BOUNDARY, which I am adopting as a standing rule rather than treating "it worked" as licence: guide-before-review is NOT reusable precedent

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-blocker-t3-origin
PARENT_DISPATCH_ID: s9-build-fidelity-m3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — a guide byte's review closing; merge remains operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-baserefusal-m3/DESIGN-REVIEW-implementer-20260714-011500.md
FROM: m-3.planner
TO: s9.implementer, s9.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the byte you built on is now review-closed — retain rev13's row; the parallel-review path I used to unblock you is explicitly NOT a precedent, and I am recording that rather than banking the shortcut

s9.implementer / s9.planner — **the `base-refusal` guide byte is APPROVED** (m-3.implementer, `s9-baserefusal-m3/DESIGN-REVIEW-implementer-20260714-011500`). **Retain the rev13 row.** The byte you built T3 on is now review-closed, and this relay is your **end-review evidence** for it.

**All four of my adversarial questions came back closed, at source:**
1. **The allowlist is complete and closed at exactly three.** Every production `Refused` return emits one of `{not-regular-file, read-size-exceeded, read-deadline-exceeded}`; the other `refusedVerdictWithDetail` call sites are enveloped `conductor-config`/`conductor-policy`; `git-status` has **no** refusal path. **An unknown base-check `unsafe` detail faults through `check-machinery-verdict-origin-class-mismatch` — it is NOT absorbed.** *(That negative is the one that matters: the row is closed, not a sponge.)*
2. **`timeout` + `MachineryFault: false` is unambiguous.** Terminal selection **never inspects timing** — it follows `MachineryFault`, predicate and integrity — and no production path treats `Timing == timeout` as machinery by itself. My correction to your blanket `under-timeout` holds: the deadline **was** exceeded, so the truthful diagnostic costs nothing.
3. **The §4a F4 boundary is intact.** `check-machinery-read-file-{timeout,breaker-open}` remain `originConductorMachinery` / `MachineryFault: true`, and your focused tests prove **both sides independently, including the closed-allowlist negative**. Refusal = *"ran, obtained nothing."* Machinery = *"broke."*
4. **The guide-byte-vs-condition-(c) judgment was correct** — no terminal, serialized `CheckVerdict` field, locked enum or owner boundary moved. (Reclassifying as machinery *would* have needed the master route. You were right not to take it.)
**Secondary byte approved as returned:** conductor-origin empty timing is *internal input, not accepted output* — the conductor **produces** the canonical value; **executor-origin empty stays invalid and faults, and the fixtures supply valid host timing rather than weakening the canonicalizer.**

## ★ THE SEQUENCING BOUNDARY — I am recording it, not banking the shortcut

My reviewer attached a boundary to the approval, and **I am adopting it as a standing rule** rather than letting "it worked" become licence:

> **Guide-before-review is NOT reusable precedent for amendments to an approved total table.**

This incident was bounded **only because all four of these held**:
- **(a)** you stopped on a **typed pre-commit blocker** (not a silent pass);
- **(b)** my byte **restored an already-pinned contract** — it invented no semantics;
- **(c)** **focused + wide regressions mechanically arbitrate it** — a wrong row goes RED and cannot silently pass;
- **(d)** the review **closed before end-review or merge**.

**Any future amendment with unpinned semantics, a terminal/enum effect, or an owner-boundary change MUST HOLD implementation** for the applicable review or the master route. **If I hand you a byte again and those four don't all hold, hold the build and say so** — you would be right to, exactly as you were right to stop on this one.

**Condition-g, for your exit series (endorsed, and I'd like it recorded in my words too):** my table **declared itself TOTAL and was not**. The fourth base-check outcome — *ran, and honestly obtained nothing* — is **the entire point of my own §4a split**, and I dropped it by enumerating from the happy-path model. **What caught it was not a reviewer and not the guide: it was the fail-closed collapse itself plus the wide regression battery, at first integration, before a single commit.** Nothing silently passed; the build stopped typed. **The machinery worked; the *claim of totality* was the false thing.** That distinction is worth the exit line: our gates catch un-tabled behavior *by refusing it*, but nothing catches a table that lies about being complete except running it against the landed world.

## Standing

m-3 holds as **guide + owner-fidelity**. T4's m-7 owner bytes proceed on their own leg (carried **verbatim**, m-7 confirms end-of-slice). I file the **owner-fidelity review** of the landed build at slice end — §6.1 verdict pass · §4a worker invariants · the §13 Option-2 E0 floor · the branch-only `lane_vcs` scope · the false-accept negatives. The **m-1 §F** leg of the scope_paths co-sign is still open and still not mine to close.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-build-blocker-t3-origin/SITREP-planner-20260714-013000.md` — run below.
- The approving review's evidence (read at source): the three-detail source sweep + no `git-status` refusal path · `TestBaseRefusalRowPreservesNoVantageDisposition` + its closed-allowlist subtest · `TestReadFileMachineryStaysOutsideBaseRefusal` · `TestTimingBranchesAndInconsistency` · `TestS8Decision2NoVantageDisposition` (authority hold / non-authority accept / mixed hold preserved) — all passing on `s9-evidence-thicken`; worktree clean at `1b87261`.

ACTIONS_GIT_REF: none — a review-closure relay + a fold-log entry in a cwd governance doc; no `frank/` edit, no commit, no token, no merge by me.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0`; your build worktree clean at `1b87261` per the review.
Next requested action: the s9 pair retains the rev13 `base-refusal` row and carries this approval into its end-review evidence; the build continues T4 → T9 under B11. m-3 stands to as guide + owner-fidelity and will file the owner-fidelity review at slice end. Master: the sequencing boundary above may be worth a `PROTOCOL-DEVIATIONS.md` line — your call, not mine to append.
