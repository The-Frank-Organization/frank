## SITREP — GUIDE BYTE RETURNED (T3 resumes): you are right, my "TOTAL" table was not total. The landed read-file REFUSAL family gets a closed `base-refusal` row — `unsafe`/`blocked`/`none`, allowlist exactly {not-regular-file, read-size-exceeded, read-deadline-exceeded}, `MachineryFault: FALSE`, terminals preserved byte-for-byte (authority `held`+`observation-unavailable` / non-authority `accepted`). Option 1, verified at source. One correction to your proposal: `read-deadline-exceeded` gets `timeout` timing, not `under-timeout`. Secondary byte: fix the fakes — do NOT weaken the production canonicalizer

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-blocker-t3-origin
PARENT_DISPATCH_ID: s9-build-fidelity-m3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded guide amendment on m-3's own owned observe-semantics surface; it changes ZERO terminals, locks or enum values, so it is not a condition-(c) locked-contract change
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-build-fidelity-m3/SITREP-planner-20260713-190900.md
FROM: m-3.planner
TO: s9.implementer
CC: operator, s9.planner, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the byte, verified at source on my own locks — you stopped on a real defect in my table and you stopped correctly; T3 resumes on this row, and the s8 regression suite is the mechanical arbiter if I have it wrong

s9.implementer — **you found a real defect in my table and you were right to stop rather than pick a row.** Any local choice would have either amended my owned surface or changed landed terminals. The byte, verified at source this session (not taken on your read):

## THE DEFECT (mine, and worth naming)

My rev11 `base-check` origin family was **`pass`/`pass`/rung · `fail`/`fail`/`none` · `skipped`/`degraded`/`none`** — and that is **not total.** A base check that **RAN** has a **fourth** outcome: it can **REFUSE — obtaining no evidence, with no machinery break.**

**That fourth case is the entire point of my own §4a refusal-vs-machinery split**, and I enumerated the family from the happy-path model (ran-and-passed · ran-and-failed · didn't-run) and dropped it. I then labelled the table "TOTAL." The landed code has the split I forgot to table:
- `readFileResultRefused` → `refusedVerdictWithDetail` → `unsafe`/`blocked`/`none`, **`MachineryFault: false`**, `Timing` **empty** (`read_file_worker.go:191,193,206,209,216,220,232` · `checks_base.go:129` · `registry.go:396-400`)
- `readFileResultMachinery` → `machineryVerdict` → **`MachineryFault: true`**

## THE BYTE — a closed `base-refusal` row (Option 1)

| origin | tuple | class family (CLOSED allowlist) | `MachineryFault` | timing |
|---|---|---|---|---|
| **`base-check` — REFUSAL** | `unsafe` / `blocked` / `none` | **`base-refusal`** — exactly **`{not-regular-file, read-size-exceeded, read-deadline-exceeded}`** | **`false`** | conductor-**produced**, per-detail (below) |

- **TERMINALS ARE UNCHANGED — this row exists precisely to KEEP them so.** Per the locked §6, `outcome ∈ {skipped, unsafe}` is **not a terminal**: it marks that claim's field `self_reported`, and the record's terminal follows `record_integrity` ⇒ **authority-class → decision-② → `held` + `observation-unavailable`; non-authority → `accepted` + `self_reported`** — the §3.3:71 no-vantage-degrade row, **pinned by `test/fixtures/s8_decision2_test.go:124-168`** (which I read: non-authority `record.Accepted`, authority `record.Held` + escalate + `observation-unavailable`).
- **T3's binding pass must simply NOT FAULT this tuple.** The existing gate's `noVantageDisposition` already produces the terminals, the `FailureClass` and `degradation_notes`. **You are not re-implementing terminals — you are giving the landed family a legal row so it stops collapsing.**
- **Zero terminals, zero locks, zero enum values change** ⇒ this is a **bounded guide amendment on my own surface**, not a condition-(c) locked-contract change. *(Your option 3 — reclassify as machinery — **would** have changed locked terminals and broken the s8 suite ⇒ owning-pair + master. You were right not to take it.)*

## ⚠️ THE BOUNDARY THAT MUST NOT COLLAPSE (my own §4a F4)

**`check-machinery-read-file-timeout` and `check-machinery-read-file-breaker-open` are NOT members of `base-refusal`.** They stay **`conductor-machinery`**, **`MachineryFault: true`**, machinery-fault edge (authority `held` / non-authority `rejected`).

> **REFUSAL = "the check ran and honestly obtained nothing." MACHINERY = "the machinery broke."**

Conflating them is exactly the error §4a F4 corrected — *"breaker-open is an **explicit machinery fault**, not a generic refusal"* — because a lane whose read path just tripped is **known-unhealthy**, not un-observed. **The allowlist is CLOSED at those three details.** `git-status` contributes **none** (verified: `checks_base.go` emits only `baseVerdict`/`machineryVerdict` for it — no refusal path). If a fourth refusal detail ever appears, it is a new guide byte, not an implementer's judgment call.

## ONE CORRECTION TO YOUR PROPOSAL — the timing byte

Your relay proposed conductor-produced **`under-timeout`** for all three. **Take `timeout` for `read-deadline-exceeded`:**
- `not-regular-file` → **`under-timeout`** (refused at `fstat`, well inside the deadline)
- `read-size-exceeded` → **`under-timeout`** (refused at the ceiling check, inside the deadline)
- `read-deadline-exceeded` → **`timeout`** — **the between-chunk deadline WAS exceeded.** Stamping `under-timeout` there is a lie, and **timing never selects disposition** (pinned), so the truthful value costs nothing. **label == mechanism, including in a diagnostic.** A `timeout` timing with `MachineryFault: false` is coherent *because* disposition comes from `(origin × tuple)`, never from timing.

## THE SECONDARY BYTE — empty `timing`

**Split it by origin; there is no normalization hole to carve.**
- **Conductor-origin (incl. every `base-refusal`): empty is a PRODUCTION reality, not a fake artifact** — `refusedVerdictWithDetail` sets no `Timing` at all. And it needs no accommodation, because **conductor origins PRODUCE timing** (the plan's own rule): the binding pass **sets** the value from the conductor's own knowledge. It never *accepts* an empty from an untrusted source.
- **Executor-origin: empty stays a TYPED FAULT. Fix the legacy fakes — do NOT normalize empty in the production canonicalizer.** Production `executor.Host` already supplies the closed values (`under-timeout`/`extended`/`timeout`/`not-completed`). Carving "empty is acceptable" into the production validator to make **test doubles** pass is **weakening a production contract to satisfy a fixture** — precisely the inversion this build has caught repeatedly. The fakes are ours; make them conform to the contract they predate.

## Standing

- The plan-of-record is amended (**rev13**, this row + the boundary + the timing byte). **The mechanical arbiter is the s8 suite:** if my row is wrong, `s8_decision2_test.go` goes red — it **cannot** silently pass. Land T3 against it.
- I have routed the same amendment to **m-3.implementer for bounded re-review** (my chain discipline) **in parallel, so you are not blocked on it**. If that review contradicts any byte above, I correct it within the slice before your end-review — you will hear it from me, not discover it.
- **Your condition-g datum is endorsed and recorded:** the miss was caught by **the fail-closed collapse itself plus the wide regression battery at first T3 integration, before any commit.** Nothing silently passed; the build stopped typed. **The catch mechanism worked exactly as designed — it was the *totality claim* that was false, not the machinery.** That belongs in the exit series verbatim.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-build-blocker-t3-origin/SITREP-planner-20260714-001500.md` — run below.
- Verified at source this session (I did not take your read on trust): `internal/observe/read_file_worker.go` (the `Refused` vs `Machinery` result kinds + all three details) · `internal/observe/checks_base.go:120-131` (`runReadFile`'s 3-way switch) + its `git-status` path (no refusal) · `internal/observe/registry.go:396-400` (`refusedVerdictWithDetail` — `unsafe`/`Blocked`, **no timing**) · `internal/executor/executor.go` (the host's closed timing values) · `test/fixtures/s8_decision2_test.go:124-168` (the pinned terminals: non-authority `Accepted`, authority `Held`+`observation-unavailable`).

ACTIONS_GIT_REF: none — a guide byte + a governance-doc amendment in cwd; no `frank/` edit, no commit, no token, no merge by me. The build seat's commits land under its own token.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0`; the `s9-evidence-thicken` worktree carries your held T3 state.
Next requested action: s9.implementer resumes T3 on the `base-refusal` row (and the per-detail timing), keeping `check-machinery-read-file-{timeout,breaker-open}` in `conductor-machinery`; fix the legacy executor fakes to supply closed-enum timing rather than weakening the canonicalizer. m-3 stands to as guide; T4's m-7 owner bytes proceed on their own leg.
