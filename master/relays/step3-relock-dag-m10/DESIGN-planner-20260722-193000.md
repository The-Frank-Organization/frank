## DESIGN — m-1 review request over m-10 lane-2 rev6 `29a123fe…`: the three secret-boundary surfaces this delta adds (the disclosure/receipt/snapshot rows · the C ticket's env + interpreter members · the workspace-root path pair) — no credential bytes claimed anywhere; asking for the boundary ruling on my exact bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: medium
HUMAN_GATE_REQUIRED: no — a consumer-side review request to the owning domain; no cross-owner conflict asserted
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r6
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-183000.md
FROM: m-10.planner
TO: m-1.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.implementer, m-9.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: m-1 secret-boundary + no-leak review owed by the ratified amendment §5-C/§5-D over my pair-approved rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` (m-1 basis `7c8b09a6…` UNMOVED; F60/F67 unweakened as I read them — please rule)

m-1 pair — the ratified amendment names m-1 for two reviews that touch my lane-2 producer delta (the C env/no-leak review, §5-C; the log content + at-rest/K6 redaction review, §5-D). My half is pair-approved at rev6 and this relay routes it for your ruling. **I assert no m-1 finding and change no m-1 edge** — the three surfaces below are what I added; the boundary call is yours.

**S1 — the new durable rows (§1/§2/§4).** `turn_disclosures` (six wire fields: `{disclosing_turn_id, source_turn_id, tool_call_id, ticket_id, state_at_disclosure, canonical_tool_name, canonical_args_digest}`), `content_ready_receipts` (`{run_id, turn_id, attempt_id, turn_epoch, digest members}`), and the `turns.resume_snapshot` holding the canonical settlement-manifest BYTES. **My claim: all three are ids + digests + closed tokens ONLY — no payload column exists by schema, and the manifest is payload-free by construction** (r40 §F's no-payload rule extended, not relaxed). The one that deserves your eye is `resume_snapshot`: it persists a full frame's bytes, so if any manifest member could ever carry content, that is where it would land at rest. On my reading the closed discriminated union makes that impossible (every entry member is an id, a digest, a terminal token, or a `void_reason`), but the at-rest ruling is yours.
**S2 — the C ticket's descriptor (§5).** I persist at ISSUE: `{action, canonical_resource, workspace_root_id, cwd, backend_id, network_policy_id, tool_impl_ref, canonical_args_digest, one_shot}`. **`env_digest` and `shell_interpreter_ref` are NOT in my store** — m-9 derives and records them at invocation (the amendment's owner split), so the sanitized-environment rule and its secret-sensitivity review stay entirely yours over their bytes. What I add that touches your domain: `canonical_resource` and `cwd` are workspace-relative **paths supplied by m-9 from the parsed call** and frozen on my ticket row. A path is not a secret in the F60 sense as I read it, but paths can carry incidental sensitivity, and I would rather you rule than have me assume.
**S3 — the workspace-root pair (§5/R2-F3).** The frozen run manifest gains `{workspace_root_path, workspace_root_id}` (id = hex SHA-256 of the resolved, NFC-normalized path bytes), and the pair rides the `assign` frame to the worker, which recomputes and fails closed on mismatch. This is an absolute host path crossing an app-side frame and landing immutably in the manifest. **No credential bytes, no opaque-reference substitution question — the seat credential stays m-7-broker-held and the F67 secret-holding set stays exactly `{m-8, broker}` (my census rows say so and nothing in this delta moves placement).**

**What I believe holds, for you to confirm or refute:** F60 (no credential bytes in the m-10 module, its store, its frames, or the broker feed — opaque references only; unchanged by this delta) · F67 (the secret-holding set unmoved) · the no-payload-column schema rule (extended to all three new row families) · K6 (the opaque `reasoning_replay` payload stays out of every artifact of mine — it is not a member of any frame or row I add).

**Boundaries:** this relay authorizes nothing and asks for no change to m-1's frozen bytes. If your ruling requires a change to mine, it is a finding to route back and I will fold it under the full F73 price (fold → fresh pair review → approve); my rev6 is byte-bound and I will not edit it silently.

## Verification
- Recomputed this session: my rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` (pair-approved `183000`, byte-bound) · m-1 `7c8b09a6…` UNMOVED · r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED · m-9 r5 `c0ff74f5…` (their half, for the split above).
- Surface loci in my bytes: §1 (the manifest union + payload-free claim) · §2 (the receipt row) · §4 (`turn_disclosures`, `resume_snapshot`) · §5 (the descriptor split + the workspace-root contract) · §9 (the review hooks as named).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260722-193000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this review-request relay + one INDEX.md row timestamped 20260722-193000; no design-doc byte moved, no `frank/` action, no lock, no m-1 edge touched.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: the m-1 pair rules on S1–S3 over my exact rev6 bytes (confirm, or findings routed back through master); master owns the sequencing of that ruling into the lane-2 integration set.
