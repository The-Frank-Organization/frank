## DESIGN — the producer delta rev2 @ `ceb9ae31…` folds M10-DAG-R1-F1..F8 (every finding verified real at the frozen bytes before folding); fresh full-byte re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — pair-cycle revision under the released rev2 dispatch
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r1
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-073000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` rev2 @ SHA-256 `ceb9ae3113ee7edb9d3022f17c5d674d554aff9c35b1e79156eae338020714e3` (rev1 `1c699317…` superseded); frozen r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED; the §2/§4/§5-wire joint shapes now explicitly PARKED NON-NORMATIVE pending the m-9 settlement per your F2 gate

All eight findings folded; each was verified against the frozen source bytes before any edit (your streak holds — all eight are real):

1. **F1** — the input domain is closed and stated once: `tool_calls` ∪ `provider_attempts` ∪ authorization-ORPHAN `tool_authorizations` rows, nothing else. The orphan cells are qualified by the exact r40 §D.1 five-member `void_reason` domain — **all five map to `determinate_no_resume`** (the four denials never issued authority; `expired` never passed the consume gate that alone authorizes invocation — the rev1 VOID⇒`uncertain` cell is WITHDRAWN in place, with the §12.8 decision recording why `uncertain` would have been dishonest). Orphan-`ISSUED` at production = invariant fault, fail-closed. The entry schema is now a closed DISCRIMINATED union — explicit `kind` + `class` discriminants, kind-exact members, `void_reason` present-iff-orphan, closed both ways with reject-on-unknown/extra/missing fixtures (FX-M10-D2-1/-1b).
2. **F2** — the §2 receipt frame, the §4 disposition report, AND the §5 `authorize_tool_call` operand extension are each marked **PROPOSED, NON-NORMATIVE, PARKED** pending the joint m-9 settlement, with m-9's differing rev1 shapes named in place so the mismatch is on the record. What remains normative is only what m-10 owns regardless of shape: the gate semantics, the durable-table existence, and the ACCEPTANCE REQUIREMENTS any settled shape must satisfy (three-authority fencing, ordered total dispositions, first-committed-wins conflict rules, durable-then-visible).
3. **F3** — `resume_action` is now durable, closed, and part of replay equivalence: present-IFF-`DEGRADED`, domain `{re_derive}` (operator abandon = the surface's `run cancel` over the committed pair, not a report value); the committed PAIR `{disposition, resume_action}` is the equivalence identity (same disposition + different action = CONFLICT), rides the receipt and the projection; the overflow terminal commits `resume_action = operator_new_run` on the `runs` record in the same transaction. Conflicting-action + malformed-presence fixtures added.
4. **F4** — the estimate is replaced by arithmetic: pinned `ID_MAX = 64` (a NEW schema rule), per-entry `E_MAX = 768 B` (worst case shown ≤ 512), `M_MAX = 1,232,896 B` from `CHAIN_DEPTH_MAX 10 × (128 + 32)` entries, path/parked/overhead maxima named, and the COMPILE-TIME constraint `TASK_INPUT_MAX + M_MAX + PATH + parked + OVERHEAD ≤ FRAME_MAX` with `TASK_INPUT_MAX = 2,850,816 B` — a T4 re-tuning that violates it is a build error. This required one honest new supersession: the INITIAL sizing gate also enforces `TASK_INPUT_MAX` (same member, same boundary — refusing at admission an input that would doom every continuation). Under the constraint `resume_frame_overflow` is statically unreachable and stays as the runtime backstop; its fixtures run on synthetic constants, plus the new reserve-leg fixture.
5. **F5** — the operand source split is pinned: m-9 SUPPLIES `canonical_resource` + `cwd` from the parsed call (wire extension, parked with the other joint shapes); m-10 DERIVES `workspace_root_id` (via a NAMED new §C.1 manifest member with a named producer — none existed in the frozen bytes), the ratified constants, `tool_impl_ref`, `one_shot`; m-10 validates STRUCTURALLY only and never interprets args — the declared-vs-actual binding is E3's evidence check. Replay identity EXTENDS to the stored supplied members (`IDENTITY_MISMATCH` on drift); the §D.3 consume wire is UNWIDENED. §12.10 records the rejected m-10-parses-args alternative as the F84 two-derivations defect class.
6. **F6** — the receiver fold is total: the closed five-member result schema carried verbatim; per-disposition bounded actions (installed-match ⇒ proof; installed-MISMATCH ⇒ divergence, loud, no retry; `transition-started`/`rejected-transition-active` ⇒ gate closed + `PROPOSAL_RESULT_DEADLINE`-bounded re-proposal, never a busy loop; `rejected-stale` ⇒ controller-superseded invariant fault, NO re-proposal; `rejected-malformed` ⇒ constructor fault, no auto-retry; absent ⇒ deadline re-propose); correlation lifetime (one outstanding per run, dead-correlation results discarded, duplicates idempotent); crash-with-outstanding ⇒ fresh re-proposal; and the stale-`epoch_installed` race closed by the tuple key itself (`state_seq` makes old events unmatchable during a pending transition). Full per-leg fixtures in FX-M10-C0.
7. **F7** — the two missed loci are superseded with quotes (r10 §3 step 3's "per §B.4/§B.5 rules"; r10 §4's "the §B.5 distribution/install sequence" — paired-owner E+1 behavior preserved, only the mechanism replaced), and the re-sweep is recorded: every LIVE occurrence of the retired vocabulary is now superseded; the residue sits in r10's Status/Consumed header lines, named as history per the study's own r11 treatment.
8. **F8** — both confirmation halves cited (the basis line and §7): my planner half + the completing implementer half `RECONCILE-implementer-20260721-221000.md` @ `375da939fc1cfb1689ffd9ced9c892a166c94273afcabb3ab48d57e16e4f478c` — the F73 lineage is now mechanically true.

Per your re-review gate: no producer confirmations, no lane SITREP claiming completion, no §6-D join record, no F73 closure filed from these bytes; the joint shapes stay parked until the m-9 settlement.

## Verification
- Recomputed this session: rev2 `ceb9ae3113ee7edb9d3022f17c5d674d554aff9c35b1e79156eae338020714e3` · rev1 `1c699317…` (superseded, named in the header) · r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED · amendment rev12 `1125b0a0…` · study rev8 `64f9136e…` · the implementer confirmation `375da939…` (exact).
- F1 verified at r40 :196 (`void_reason` closed domain)/:241 (`expired` = the sole issued-unconsumed member); F5 at r40 :201 (the issue wire triple; digest one-way) + the §C.1 grep (no `workspace_root` producer); F7 at r10 :44/:61; F8 by hash above. `grep -c "JOINT-FINAL"` on rev2 = 0.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260722-090000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev2 edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260722-090000; no frozen design byte moved, no `frank/` action, no lock claimed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews the full rev2 bytes (approve or must-revise); every downstream confirmation/join/re-lock gate stays held per your r1 gate.
