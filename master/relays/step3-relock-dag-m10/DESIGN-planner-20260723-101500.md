## DESIGN — TWO artifacts filed for adversarial review: (A) the §D-settlement fold rev7 @ `8ce78381…` on master's four rulings, and (B) the NEW bounded B/E carriage row rev1 @ `67f947e4…` — deliberately separate so m-3's binding is not coupled to the §D amendment's ratification cycle

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — folding master-issued rulings + a master-signalled bounded carriage row; the two operator-ratification items (the D-4 relabel, the new terminal) are carried as ruled-and-pending, never self-ratified
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/RECONCILE-orchestrator-planner-20260723-001500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: review both — (A) `2026-07-22-stage6-lane2-producer-delta.md` **rev7 @ `8ce78381f901861322f28deb80ac2c7b88f4c6213c49dc6dd01783a525d696bb`** (rev6 `29a123fe…` superseded) · (B) `2026-07-23-lane2-be-carriage-row.md` **rev1 @ `67f947e42b85dc22167e0d47675cb7d6ba24d7aaecce910c9ff4e418c5e480d8`** (new artifact); frozen r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED; producers bound at m-8 r5 `c0b7b488…` + m-9 r7 `f191c69c…` (recomputed exact this session; r5/r6 VOID)

Master ruled all four escalations (`…-20260722-230000`, confirmed with my branch-6 correction accepted at `…-20260723-001500`) and separately signalled the B/E carriage now that both producers are settled (`step3-relock-dag-m9/…-20260723-093000`). Two artifacts, filed together but reviewable independently.

### (A) rev7 — the §D-settlement fold
1. **Ruling (1), D-4 relabel:** carried as the ruled disposition — *Gate 1 delivers the disclosure guarantee; Gate 2 is a fail-closed validator + drift-detector over MVP-unreachable states* — with the comparator bytes kept and Gate 2's **validation** explicitly still reachable. **It takes effect on operator ratification via master's bounded amendment; I do not self-ratify it**, and the doc says so in those terms.
2. **Ruling (3), the big one:** the rev2–rev6 D-4 scoping is **WITHDRAWN** and run-wide carriage is **RESTORED** on both gates, with r40 `:72`'s worker-independence named as the reason no consumer-side repair was taken. Bounded by `MAX_PARKED_ROWS_PER_RUN = 512` at the single growth site (the retirement transaction commits **in full** — fencing is never traded for a bound — and the same transaction commits `runs.state = FAILED` + `stop_reason = parked_unknown_capacity_exceeded` if the cap would be exceeded). `turn_disclosures` kept for byte-identical `turn_open` replay, retained for the active turn only. Arithmetic recomputed throughout: `PARKED_MAX` 327,680 · `ADMISSION_REF_ENC_MAX` 2,564,096 · `FRAME_CONTENT_BOUND` 3,704,832. **New second compile-time assertion** covering `attempt_open_ok` (m-9's check (i) — that frame had no sizing gate at all).
3. **Settled seams folded normative:** S-1 receipt frame (envelope-carried fencing operands pinned REQUIRED-never-absent, the durable column split, evidence-tuple equivalence, **equivalent-duplicate ordered BEFORE stale-sender per my own frozen §D.4 precedent — my rev6 had these reversed**, `receipt_conflict` as a fail-closed detector, the delayed-first-arrival case recorded as honest over-surfacing); S-2 report + receipt + **`disposition_conflict` carrying the committed pair**; S-3 five families (relay cell held); S-4/S-5 confirmed with m-9's notes recorded.
4. **FX-M10-D4 leg (b) CORRECTED** — the `changed` fixture was unbuildable; the fixture battery now asserts the reachable `equal` leg, the reachable **validation** legs, and the drift-detector legs behind a test-only injected mutation. New FX-M10-CAP (cap/terminal, incl. the off-by-one and the forbidden-truncation negative) and FX-M10-ACK-BOUND (a build-level negative test — a violating cap must fail to compile).
5. **m-1's CONFIRM folded** with their binding carrier condition as explicit negatives (no fault, refusal, or terminal of mine names `workspace_root_path` or `session_log_path`), and **m-9's basis rebound r5 → r7** with their §2/§3/§7 seam semantics verified byte-unchanged across the rebase rather than assumed. §12 gains decisions 21–23; the superseded scoping decision is marked, not deleted.

### (B) The B/E carriage row — a separate bounded artifact
Master's instruction was explicit that this is separate from rev7, and the reason is right: coupling m-3's binding to the §D amendment's ratification cycle would block their lane close for nothing. It carries the three producer digests on `provider_attempts` at the exact attempt identity — **verbatim, never re-derived** (I hold neither producer's bytes and no hashing call exists on the carriage path); **NULL for absence, never a placeholder** (m-8's P1 matrix makes absence a legitimate outcome class, and m-3 reads it as `unknown`); written once inside the carrier's own applier transaction, immutable, with a differing second value a typed conflict where the first stands; **evidence, never authority** — no member gates any transition. §5 names the one open seam honestly: m-9's r7 says `logical_surface_digest` "rides m-9 → the m-10 attempt row" without naming the frame, so I name `attempt_open` as the carrier (the only m-9→m-10 frame at attempt identity before the request) and flag it as an m-9 confirmation item rather than authoring their frame choice.

**Review asks:** (a) verify every rev7 supersession quote and the recomputed arithmetic against the frozen r40/r10 bytes and master's ruling text — the arithmetic has now been wrong twice at this seat, so please recompute rather than check my sums; (b) confirm the ruled-but-unratified items are carried as pending everywhere they appear, with no sentence that reads as settled; (c) adversarial pass on the restored run-wide carriage for any place the withdrawn scoping still leaks through (I swept, but that class of residue has survived my sweeps before); (d) the B/E artifact against m-8 r5 §2/§3 and m-9 r7 §6 at value-source-and-timing grain, especially the absence semantics and the `attempt_open` carrier claim.

## Verification
- Recomputed this session: rev7 `8ce78381f901861322f28deb80ac2c7b88f4c6213c49dc6dd01783a525d696bb` · B/E rev1 `67f947e42b85dc22167e0d47675cb7d6ba24d7aaecce910c9ff4e418c5e480d8` · m-8 r5 `c0b7b48846b05091af1c814481bc0e4449cf455f8c3e616ee5f9634b1e04be21` · m-9 r7 `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3` · r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED · rev6 `29a123fe…` (superseded, named in the header).
- Arithmetic: 1,232,896 + 327,680 + 4,096 + 65,536 = 1,630,208 ⇒ `ADMISSION_REF_ENC_MAX` = 4,194,304 − 1,630,208 = **2,564,096**; actual-maxima bound = 823,296 + 247,808 + 2,564,096 + 4,096 + 65,536 = **3,704,832**; ack assertion ≈ 1,024 + 327,680 = **328,704** (7.8% of `FRAME_MAX`).
- Stale-reference sweep: `PARKED NON-NORMATIVE` and `c0ff74f5` survive only in the header's fold history and §9's explicitly-labelled superseded quote; `MAX_LEGAL_FRAME` only in the header history and the marked-superseded §12.16.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260723-101500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev7 edits to `2026-07-22-stage6-lane2-producer-delta.md`, the NEW `2026-07-23-lane2-be-carriage-row.md`, this relay, and one INDEX.md row timestamped 20260723-101500; no frozen design byte moved, no `frank/` action, no lock claimed, no amendment authored, no ratification implied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer reviews both artifacts (approve or must-revise, independently per artifact); on the B/E approval I return its exact hash to master for routing to m-3; on the rev7 approval master folds it into the bounded §D-settlement amendment → VP → operator, and the §D join co-signs with m-9 after both pairs' folds.
