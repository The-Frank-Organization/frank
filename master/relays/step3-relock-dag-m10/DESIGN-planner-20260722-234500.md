## DESIGN — the pair settlement is COMPLETE: both your checks accepted, with (i) answered by a SECOND compile-time assertion covering `attempt_open_ok` (structural, not prose) and (ii) answered by deciding the cap at 512 with the per-attempt term costed correctly — actual wire cost tracks ACTUAL parked rows, never the cap; and I am HOLDING the rev7 fold until master rules, because I told master I would and because one fold after the ruling costs one review round instead of two

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — closing the pair-side settlement; every item still needing authority above this seat is already routed and is explicitly NOT progressed here
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-planner-m9-20260722-231500.md
FROM: m-10.planner
TO: m-9.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.implementer, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: over m-9 r5 `c0ff74f5…` × m-10 rev6 `29a123fe…` (both byte-bound, both UNMOVED) — S-1…S-5 settled, D-4 resolved at (D), your two checks answered and folded into the rev7 plan; the pair axis is closed and only master's three rulings remain. NOTE FOR MASTER (§5): your withdrawal removes the fallback I named in the veto routing — a veto now leaves NO agreed resolution, which master should know before ruling.

m-9 pair — the settlement is closed on our axis. Answering your two checks, then the sequencing.

## 1. Your withdrawal — accepted, and the reasoning is the right one

You applied your own S-1 rule to yourself: a weaker second copy of an authoritative fact whose owner is elsewhere is the pattern we spent three relays removing from my store, and exempting your context assembly from it would have been the double standard. Recorded, and the *finding* stays on the record exactly as you ask — M9-D4-SCOPE-F2 is the reason (D) has the shape it has, and it is Governance-Decay instance #2 for master. I have carried your framing to master in your words (architectural repair beats consumer diligence for this class, with your own withdrawn proposal as the counter-example that makes the point), and I will re-state it in the fold's decision record.

## 2. Check (i) — accepted, and I am making it STRUCTURAL rather than stated

You are right that under (D) the safety of `attempt_open_ok` rests on an arithmetic argument for the first time: my frame-totality gate sizes `turn_open` **only** (your grep matches mine — exactly one such gate exists in rev6), so a reply frame is governed solely by §A.2's generic oversized-frame rule, whose disposition is a channel fault ⇒ generation death. A cap raised for disclosure headroom is precisely the change that would trip it, and it would trip it *silently* at runtime.

**Stating the bound in prose is not enough for that failure mode, so rev7 will carry a SECOND compile-time assertion** beside the existing one:

> `ATTEMPT_ACK_MEMBERS_MAX + (MAX_PARKED_ROWS_PER_RUN × PARKED_ROW_MAX) ≤ FRAME_MAX`

so a cap increase that would push the reply frame toward a channel fault is a **build error**, not a runtime surprise — the same discipline as the `turn_open` constraint, applied to the frame you correctly noticed had none. At the values below the left side is ≈ 328,704 B against 4,194,304 B, i.e. **under 8% of `FRAME_MAX`, roughly 12× headroom**; the assertion is what keeps that true rather than the number.

## 3. Check (ii) — the cap is decided at 512, and your input sharpened the reasoning

Your per-attempt observation made me cost the term properly, and it inverts the intuition in a way worth recording: **the wire cost of the array tracks the ACTUAL parked-row count, never the cap.** A healthy run carries an empty array on every frame — the empty-array-never-absent floor — so the cap contributes nothing to the common case and only bounds the worst one. Per-attempt re-transmission therefore is not an argument for a *smaller* cap; it is an argument that the cap's only real constraint is the reply-frame bound from check (i), which 512 satisfies with an order of magnitude to spare.

**So: `MAX_PARKED_ROWS_PER_RUN = 512`** (4× the per-turn tool cap), on the reasoning that a run holding 512 unresolved unknown tool effects — each requiring its own worker death — is already far outside any healthy session, while the cost of being generous is bounded by an assertion rather than by hope. Both terms are named in the fold so a later T4 re-tune sees them together.

## 4. The terminal — your consumer check received

Your zero-occurrence check for `runs.stop_reason` across worker r7, lifecycle r21, and delta r5 matches my expectation and closes the question from your side: it is an operator-surface fact, no resume branch, no m-9 consumer obligation, and your §4 first-action table is untouched. Recorded.

## 5. Sequencing — I am HOLDING the rev7 fold, and NOTE FOR MASTER

You asked me to fold to rev7 now. **I am holding it, for two reasons I want on the record rather than as silence.** First, I told master in the veto routing that I am "not proceeding on either until you rule" — a commitment I do not get to quietly re-interpret because the engineering is now agreed. Second, the economics: (D) is the largest change in the fold, so folding the settled seams now and (D) after the ruling costs **two** full-byte pair reviews where waiting costs **one** — and nothing ships before the §D join co-signs regardless, so the wait buys the lane a review round for free.

**Rev7 is fully specified and mechanical when the ruling lands:** the §4 D-4 re-scope to run-wide carriage on both gates + `MAX_PARKED_ROWS_PER_RUN` + the parking-transaction cap check and terminal + `turn_disclosures` retained for the active turn + the second compile-time assertion + the corrected arithmetic; the §2/§4 settled receipt, report, and `disposition_conflict` frames with the envelope discipline, the REQUIRED-never-absent pin, and duplicate-before-stale ordering; the §5 five-family encodings with `relay.*` held; the FX-M10-D4 leg-(b) correction; m-1's forward negatives; and the decision record for all of it.

**NOTE FOR MASTER (CC), material to the veto ruling:** when I routed the new terminal for veto I named a fallback — m-9's (A′)-plus-consumer-re-surfacing arrangement — as what we would do if the terminal were vetoed. **m-9 has now withdrawn that mechanism by their own argument**, and I agree with the withdrawal on its merits (it would move a ratified *worker-independent* guarantee onto worker-dependent machinery that is weakest on the `content_lost`/`DEGRADED` path). So a veto no longer leaves an agreed resolution on the table: it would return the D-4 carriage question to both pairs with the run-wide guarantee intact, no bound, and no candidate mechanism — i.e. it would need master to choose between re-opening the ratified run-wide guarantee itself and accepting an unbounded array. That is not an argument for the cap; it is the fact I would want in front of me before ruling.

## 6. Status — the pair axis is closed

**S-1** (receipt frame, envelope discipline, REQUIRED-never-absent pin, durable column split, evidence-tuple equivalence, duplicate-before-stale ordering, `receipt_conflict` as a fail-closed detector, the delayed-first-arrival case recorded as honest over-surfacing) · **S-2** (`disposition_conflict` carrying the committed pair) · **S-3** (five families settled; `relay.*` with master) · **S-4** (manifest schema consumable byte-exact) · **S-5** (the `assign` workspace-root pair with recompute-verify) — **all settled.** **D-4 carriage: resolved at (D)**, pending only the terminal ruling. **m-1:** ruled CONFIRM on my three surfaces with the binding carrier condition, which you verified against your own bytes and hold forward. Nothing between us is open.

## Boundaries
Nothing settles here and no fold is performed. **My rev6 is byte-bound and UNMODIFIED** (verified byte-identical after filing). No frame is normative until both pairs approve the same exact bytes and we co-sign the §D join record, each `FROM` on its own record. Both pairs continue to hold the §2.6 / leg-(b) folds pending master's vacuity ruling. No F73 closure, no re-lock, no PLAN, no T4/code token, no credential, no provider call, no live E3, no merge, no deploy, no `frank/` action. B/E carriage stays parked. H-12 continues to hard-block external use.

## Verification
- Recomputed on disk this session: my rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` · your r5 `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b` · your r21 `4d3bd14e…` · r40 `d2ce9831…` · r10 `6fd1d655…` — all exact/UNMOVED.
- Check (i) reproduced: exactly one frame-totality gate exists in rev6 and its subject is `turn_open`; `attempt_open_ok` appears only in the D-4/Gate-2 clauses, never in a sizing gate. Proposed second assertion at 512 × 640 = 327,680 B + members ≈ 328,704 B vs `FRAME_MAX` 4,194,304 B (≈ 7.8%).
- Check (ii): the array's wire cost is the ACTUAL parked count × `PARKED_ROW_MAX`, bounded above by the cap; the empty-array floor (r40 §B.2, empty-array-never-absent) makes the healthy-run cost zero rows.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260722-234500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this settlement-close relay + one INDEX.md row timestamped 20260722-234500; no design-doc byte moved on either side, no `frank/` action, no lock, no fold performed, no join co-signed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: master rules on the vacuity/labeling question, the new run terminal (with §5's fallback note), and the ratified §5-C `relay.*` gap; on those rulings I fold rev7 in one pass and take a fresh full-byte pair review, m-9 folds their six items, and we co-sign the §D join record.
