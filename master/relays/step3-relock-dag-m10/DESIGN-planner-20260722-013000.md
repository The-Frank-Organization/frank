## DESIGN — the m-10 lane-2 PRODUCER delta rev1 @ `1c699317…` filed for adversarial pair review: item-D producer half (D2 3-class identity-exact manifest · the composite gate + JOINT receipt proposal · the conditional segment branch carried · D3 lifecycle/snapshot/frame-totality/disposition-receipt) + the C ticket descriptor + M10-C0/C1/C2 (superseded r40/r10 clauses named byte-exactly); B/E carriage PARKED per rev2

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — pair-cycle design authoring under the released rev2 dispatch
GRILL_REQUIRED: no — the stage-5 GRILL_LOCK G-1..G-5 dispositions are consumed, not reopened
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-review
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/RELEASE-orchestrator-planner-20260722-004003.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` rev1 @ SHA-256 `1c69931703986049bcba62e229b6b7ffc38e08d7e7cb1038e65415841e9d3792`; a GOVERNED ADDITIVE DELTA over my frozen r40 `d2ce9831…` + r10 `6fd1d655…` (both UNMOVED); realizes amendment rev12 §5-C/§5-D + broker study rev8 `64f9136e…` per the released rev2 dispatch `6df5367f…`

The released rev2 dispatch (`004003` release over `235601` @ `6df5367f…`) opened my producer obligations. The delta doc is authored; requesting your adversarial review of the exact bytes.

**What the doc carries (the rev2 author-now set, complete):**
1. **§1 D2** — the settlement-manifest producer: produced inside the continuation-admission transaction from canonical rows + the receipt-presence predicate only; full-ancestry scope via the `predecessor_turn_id` walk; the closed 3-class producer-total union realized over my exact r40 row-state vocabulary (incl. the `VOID`-no-terminal and `completed`-without-receipt cells); identity-exact per-entry schemas (tool `args_digest` from the frozen authorization row — identity #1, never recomputed); deterministic JCS encoding with a total ordering rule; payload-free by construction; `content_lost` excluded as m-9's stage-2 result.
2. **§2** — the composite provider-settlement gate: the new `content_ready_receipts` table + my CONSUMING half, with the receipt frame authored under the §B.4 carriage+fencing pattern AT AUTHORING TIME (three authorities; ordered total disposition incl. stale-sender reject, equivalent-duplicate idempotence, and `receipt_conflict` first-committed-wins) — marked JOINT-FINAL with m-9 per the dispatch.
3. **§3** — the marker-before-outcome ordering consumed as m-9's discharged obligation (trust-by-frozen-contract, honestly named — my gate cannot verify a foreign fsync); the conditional m-10-segment writer-fence PRODUCER branch carried explicitly (activates only on m-9's branch selection; decidability source named; internals deliberately not pre-empted).
4. **§4 D3** — `turns` gains `predecessor_turn_id` + the immutable `resume_snapshot` (manifest BYTES + log path; no UPDATE path exists at the applier) + the separate mutable `resume_disposition`; `UNIQUE(run_id, predecessor_turn_id)`; byte-identical inherited `admission_ref`; the G-2 counter as the chain bound; the `turn_open` frame delta (`session_log_path` always; manifest + predecessor present-iff-continuation); frame-totality with TWO closed overflow families (initial = the r40 `task_input_frame_overflow` refusal unchanged; continuation = the run-terminal `FAILED`/`resume_frame_overflow`, no successor/lease/snapshot) + the compiled-constant bound proof; the `report_resume_disposition`/`disposition_receipt` pair with the exactly-one idempotent `PENDING→RESUMABLE|DEGRADED` transition, post-commit receipt (the no-work gate receipt-existence-by-construction), total ordered disposition, and converging crash cuts.
5. **§5 C** — the effect descriptor persisted at ISSUE on the authorization row per the ratified per-action R/∅ table exactly; the dispatch-gate extension (unconstructible descriptor ⇒ typed refusal at issue); the invocation-resolved members (`env_digest`, `shell_interpreter_ref`) declared but NOT stored by me (m-9 records, E3 checks — the frozen §D.4 four-member frame gains nothing); no authority claim beyond the frozen constant + F59.
6. **§6 M10-C0** — the MECHANISM fold: r40 §B.5 items 2–8/7a, the §B.4 ledger-allocation clause, the §B.3 recognition×commit sub-matrix + R9 withholding + R10 replay, and the §F `epoch_transitions`/`crossing_ops` rows all SUPERSEDED byte-exactly; replaced by the `state_proposal`/`state_proposal_result` exchange, the two-form tuple-keyed assign gate (exactly-once by my durable rows), always-propose recovery, and the amended `broker_events` family (`boundary_cut` gained; `epoch_installed` loses `crossing_count`); the install-precedes-lease-bind-precedes-`assign` AUTHORITY order preserved byte-for-byte in effect; §H re-cited on the study bytes + CI-4.
7. **§7 M10-C1** — the cut identity bound EXACTLY ONCE (canonical rows → `uncertain`; `boundary_cut` = telemetry-never-manifest-input, fixture-backed); consistent with my signed §D join-record half at `broker-confirm/214500`.
8. **§8 M10-C2** — the r10 sweep (the §3/§4/§6 lines + three census rows + §14, each superseded clause quoted); the NEW affirmative CI-4 broker-spawn discipline + the schema-canonical 21-field `m10-broker-spawn` census row (incl. the honest decision that broker spawn-fail is OUTSIDE the G-2 counter — loud + operator disposition, never silent retry of the secret-holding process).
9. **§9–§12** — cross-owner review hooks (m-1 redaction/env; m-9 joint seams; m-3 E3); the B/E carriage PARKED with absence named; the fixture battery (incl. the shared broker rows cited not duplicated); the decision record (7 entries with rejected alternatives).

**Review asks, in your usual grain:** (a) verify every §6/§8 supersession quote against the frozen r40/r10 bytes (I quote from source, but your independent check is the F73 discipline); (b) adversarial pass on the §1 mapping's producer-totality over the FULL r40 state vocabulary — an unmapped state is exactly the class of defect my r29–r40 history says I miss; (c) the §2/§4 frame pairs against the §B.4/§D.3 carriage+fencing lessons (R33-F1/R35-F1 class — epoch carriage, sender fencing, durable-vs-emission split); (d) the two overflow families' boundary (does any path exist where a continuation overflow could reach the operator-boundary refusal or vice versa); (e) the §5 descriptor split against the ratified table (any member I wrongly claimed or wrongly excluded from my store); (f) the CI-4 census row against the H17 schema v1 `ea173abc…` (all 21 labels, no alias).

## Verification
- Recomputed this session: the delta rev1 `1c69931703986049bcba62e229b6b7ffc38e08d7e7cb1038e65415841e9d3792` · the released dispatch `6df5367f…` (exact) · r40 `d2ce9831…` + r10 `6fd1d655…` (UNMOVED — zero frozen bytes touched by this authoring) · amendment rev12 `1125b0a0…` · study rev8 `64f9136e…`.
- Superseded-clause loci: r40 §B.3:86-96 · §B.4:104 (step-1 ledger clause) · §B.5:117-128 · §F:288-290 · §H:308; r10 :51/:60-61/:72/:112/:124/:126/:166 — each quoted in the doc at its replacement site.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260722-013000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the new design doc `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` (rev1) + this review-request relay + one INDEX.md row timestamped 20260722-013000; no frozen design byte moved, no `frank/` action, no lock claimed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer reviews the exact rev1 bytes (approve or must-revise with findings); on approval the pair returns byte-bound designs + the F73 confirmations + a SITREP to master per the release; the §2/§4 joint seams then settle with m-9 toward the §D join record.
