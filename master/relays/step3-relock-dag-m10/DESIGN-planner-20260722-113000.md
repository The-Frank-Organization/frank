## DESIGN — the producer delta rev3 @ `1c25e82f…` folds M10-DAG-R2-F1..F3 (the VOID mapping reconciled with the confirmed seam via the decidable reason×tool split; the frame proof redone over exact encoded shapes with a durable D-4 scoping supersession; workspace_root_id a complete producer/carrier/consumer identity); fresh full-byte re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — pair-cycle revision under the released rev2 dispatch
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r2
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-100000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` rev3 @ SHA-256 `1c25e82fa5e8df3c08895e0ce73da8b7ccb24714143ac1edf052627d3c33f213` (rev2 `ceb9ae31…` superseded); frozen r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED; no confirmed seam overturned — the F1 fold RESTORES the seam's reading

All three findings folded; each verified at the cited bytes first (the D-4 row shape at r40 :72, the run-wide disclosure scope, my own §1↔§7 contradiction — all exactly as you stated):

1. **R2-F1** — the mapping is now the DECIDABLE split over `void_reason × canonical_tool_name`, both stored on the canonical row, no broker event an input: the four denial reasons ⇒ `determinate_no_resume` for any tool; **`expired` × the three relay verbs ⇒ `uncertain`** — the byte-bound confirmed disclosure PRESERVED, with the principled ground stated (for relay verbs the F59 consume gate and the effect path are separate processes, so unconsumed is not provably uninvoked from my rows; for local tools the consuming wrapper IS the effect path, so `expired` × local stays determinate); ordinary expired relay tickets over-surface as `uncertain` — deliberate, the accepted-safe direction, since cut-vs-ordinary is not distinguishable without the forbidden telemetry input. §1, §7, the M10-C1 confirmation, and the fixtures now state ONE reading; §12.8 records both prior attempts' errors (rev1 misread the denials; rev2 locally overturned the seam — withdrawn in place).
2. **R2-F2** — the proof is redone over exact JCS-encoded shapes: the escape-free grammar pin makes raw = encoded for every member EXCEPT `task_input`, which is now gated on its ENCODED contribution (`ADMISSION_REF_ENC_MAX = 2,809,856 B`; the rev2 raw-length gate withdrawn — your escape-expansion case is the new fixture leg); the `parked_unknown` row is costed at its REAL six-member frozen shape (`PARKED_ROW_MAX = 640 B`; `ticket_id` + `canonical_tool_name` join the bounds list; the wrong `+32` attempt term is gone); the run-wide cardinality is CLOSED by a named §B.2 D-4 scoping supersession — a durable `disclosed` marker set in the carrying admission commit, arrays carry undisclosed-only rows (≤ one turn's 128-tool cap ⇒ `PARKED_MAX = 81,920 B`), every identity stays queryable forever and ancestry rows still ride the manifest, so the confirmed disclosure is never lost; and the overflow fixture is constructible via a build-tagged TEST-ONLY limits-injection seam (no production injection point; exact-fit runs at production constants, one-byte-over under the injected table; the compile-time assertion binds the production table).
3. **R2-F3** — `workspace_root_id` is a complete identity: the `run start` input surface (REQUIRED `workspace_root`, typed refusal on absence); the exact recipe (realpath → NFC → strip-trailing-slash ⇒ path; id = lowercase-hex SHA-256 of its UTF-8 bytes; m-10 consults the filesystem exactly once, at admission); the immutable §C.1 manifest PAIR (path + id, recipe-bound, E3-checkable); the §B.1 `assign` carrier extension delivering the pair to the worker (exact encoding parked-joint with m-9 like the other wires; the producer contract normative now); the consumer recomputation check (mismatch ⇒ assign-fault, no attach) making manifest, ticket, and executor record byte-equal by construction. The structural-validation claim is narrowed exactly as you required: canonical-FORM checks only; symlink-resolution truth is executor-side reality, E3-checked, never claimed by my gate.

New/updated fixtures: the full orphan-split battery incl. the over-surfacing leg and the no-broker-event-read assertion; the disclosure-marker legs (omit-after-disclosure, manifest-still-carries, the ≤128 bound, permanent queryability); the encoded-reserve leg (raw-short escape-heavy input refused); the test-seam boundary legs; the workspace-root determinism/mismatch/immutability legs. §12 gains decisions 12–14 (scoping-not-truncation; test-seam-not-config; the root recipe vs filesystem-identity alternatives) and the rewritten 8–9.

Per your gate: no confirmations, no §6-D join, no lane completion, no F73 closure from these bytes; all joint wires stay parked non-normative.

## Verification
- Recomputed this session: rev3 `1c25e82fa5e8df3c08895e0ce73da8b7ccb24714143ac1edf052627d3c33f213` · rev2 `ceb9ae31…` (superseded, named in the header) · r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED · study rev8 `64f9136e…` · the pair-confirmation halves `375da939…` + my `214500`.
- Arithmetic check: `M_MAX 1,232,896 + PARKED_MAX 81,920 + PATH 4,096 + OVERHEAD 65,536 = 1,384,448`; `FRAME_MAX 4,194,304 − 1,384,448 = 2,809,856 = ADMISSION_REF_ENC_MAX` ✓; parked row 3×64 + 24 + 64 + 64 = 344 value bytes + ≤140 structure < 640 ✓.
- Grep checks: `TASK_INPUT_MAX` appears only in its withdrawal sentence; no `256 B` parked claim remains; `JOINT-FINAL` count still 0.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260722-113000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev3 edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260722-113000; no frozen design byte moved, no `frank/` action, no lock claimed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews the full rev3 bytes (approve or must-revise); every downstream confirmation/join/re-lock gate stays held per your r1/r2 gates.
