## DESIGN — producer delta rev11 @ `8a26fd01…` folds M10-DAG-R10-F1 (verified at rev10's own lines 84/152): my rev10 R9-F1 fold was an INCOMPLETE SWEEP — I corrected the S-1 sites and stopped, but the same §D join gate governs S-2/S-4/S-5 and two live assertions still collapsed it; both fixed, S-3 stated on its own status, mechanism + fixtures byte-for-byte unchanged

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — completing a settlement-status correction; no mechanism or product choice changes
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r10
DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-182000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` **rev11 @ SHA-256 `8a26fd014da6598fea3a5c0407fe7c85b723b9befc6677a7bbe096cf60795d47`** (rev10 `56124f42…` superseded); basis stays pair-approved m-9 r12 `04422965…`; frozen r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED

R10-F1 verified at rev10's own lines 84/152, and it is real — my rev10 fold was an **incomplete sweep**, which is the honest name for it:

**What I got wrong.** R9-F1 was one fact: the §D join gate makes every §D-settled frame non-normative until both pairs approve *and* the co-sign lands. That gate governs **S-1, S-2, S-4, and S-5** together — but I corrected only the **S-1** sites (header, §2, §9 seam-list) and stopped. Two live assertions still collapsed the same gate:
- §4 line 84: the S-2 report/receipt/conflict frames read "SETTLED … NORMATIVE at this revision";
- §9 line 152: the seam-list swept "S-1 through S-5 are all SETTLED and folded normative above" — a direct internal contradiction with the S-1 label two clauses earlier, and it also wrongly folded **S-3** into the join gate.

**What rev11 changes — only those two assertions.** §4's S-2 label is now EXACT-FOLDED / JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D co-sign, citing the same r12 `:299`/`:473`/`:511` gate as S-1. §9's blanket sentence is replaced with seam-specific truth: S-1/S-2/S-4/S-5 exact-folded but joint-pending until the join; **S-3 stated on its own status** — its five families folded, its `relay.*` cell held for m-2's ratified shape (item (4)), explicitly **not** swept into the join gate. I left line 97 ("SETTLED with m-9 for FIVE action families") untouched: that is S-3's descriptor-encoding agreement, the C-item, distinct from the D-item receipt/manifest frames the §D join covers — you did not flag it, and it is accurate.

**The lesson, recorded in the header.** A status correction is a claim about *every* site the status appears, not the first one; the join gate is one fact governing four seams, and I swept one seam's label. The r12 basis, the widened complement-of-equivalence mechanism, the detector label, the R7/R8 folds, the D-4 restore, and every fixture are byte-for-byte from rev10 — only the two remaining status assertions moved.

**Review ask:** confirm no live design assertion (not scoped-historical lineage) calls any of S-1/S-2/S-4/S-5 normative before the join; that S-3 is stated on its own status and not under the join gate; and that the mechanism and fixtures are byte-identical to the rev10 you passed.

## Verification
- Recomputed this session: rev11 `8a26fd014da6598fea3a5c0407fe7c85b723b9befc6677a7bbe096cf60795d47` · rev10 `56124f42…` (superseded, named in the header) · m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` (the join gate re-read at `:299`/`:473`/`:511`) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED.
- Live-normative sweep re-run over the whole document: the phrases "NORMATIVE at this revision" and "all SETTLED and folded" now appear ONLY in the rev11 header's description of what it corrected (line 3), nowhere as a live operative claim; §4 S-2 and §9 both read joint-pending; §9 states S-3 separately.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260724-183500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev11 status-wording edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260724-183500; no frozen design byte moved, no mechanism/fixture byte changed, no `frank/` action, no lock, no amendment authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews rev11; on pair-approval this is the exact-folded §D producer half master carries into the bounded §D-settlement amendment → VP → operator, with S-1..S-5 going normative only at the §D join co-sign after.
