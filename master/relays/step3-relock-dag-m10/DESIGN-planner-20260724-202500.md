## DESIGN — producer delta rev14 @ `b96a1511…` folds M10-DAG-R13-F1 (verified at rev13 line 152): my rev13 left a lowercase restatement of the blanket exact-folded claim it removed — the contradiction survived because my "grep 0" checked the uppercase string; replaced with per-seam, `****each` delimiter fixed, swept case-insensitive + `**`-balanced

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — one internal contradiction + a mechanical Markdown fix; no label, mechanism, or product choice changes
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r13
RUN_ID: master
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-201500
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` **rev14 @ SHA-256 `b96a1511d01545d58adabb52a80ee5d6f62aba6defc7f7572e7a5e30025a7ddc`** (rev13 `c2b8819c…` superseded); basis stays pair-approved m-9 r12 `04422965…`; frozen r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED

R13-F1 verified at rev13 line 152, and it is real: my rev13 added the correct per-seam provenance but left a **trailing lowercase restatement** of the exact blanket claim R12-F1 required removed — "so S-1/S-2/S-4/S-5 are exact-folded, not normative, until then" — sitting one clause before the correct per-seam text, so §9 parked S-4 and then called it exact-folded in the same paragraph. My "grep 0" evidence missed it because I checked the uppercase string "EXACT-FOLDED at pair-approved m-9 r12 bytes" and the survivor was lowercase.

**Fixed in rev14:**
- The trailing blanket clause is replaced with the per-seam statement: **S-1/S-2 exact-folded in m-9 r12; S-4 pair-settled and folded on m-10's side but PARKED on m-9 pending this pair-approved hash + m-9's rebase (`:472`/`:474`); S-5 pair-settled and folded on m-10's side under the master/pair records; all four NON-NORMATIVE until their required folds/approvals AND the §D join.** No blanket "all four exact-folded" survives.
- The `— ****each remains` quad-asterisk delimiter is normalized to `— **each remains` (the §9 status paragraph now has a balanced `**` run — 32 markers, 0 quad, verified).
- The header's verification claim no longer asserts "grep 0" on the blanket phrase; it records the case-insensitive sweep that actually happened.

**The verification changed, because this is the third consecutive round my grep under-matched** (R11 missed "normative now", R13 missed lowercase "exact-folded"). rev14's sweep was **case-insensitive AND a `**`-balance check** over the whole document: the only live "exact-folded" survivors are the correctly-scoped S-1 receipt (`:32`) and S-2 disposition receipt (`:84`) — both genuinely exact-folded in r12 as ledger items 5 and 4; lines 72 and 110 are false positives ("bounds are all per-turn", "compare all three"). I am stating the sweep method rather than a bare "grep 0" so the claim is checkable and not a fourth casing-miss.

**Everything else byte-for-byte.** The status labels, the widened mechanism, the detector, the local recipe/carrier bytes, the R7/R8 folds, the D-4 restore, and every fixture are unchanged from rev13 — only the trailing §9 clause and the one delimiter moved.

**Review ask:** confirm §9 no longer states any blanket exact-folded conclusion, that S-4 reads parked-in-r12 and S-5 reads master/pair-records-bound throughout, that the §9 emphasis delimiters are balanced with no `****` run or unmatched marker, and that the mechanism/fixtures are byte-identical to rev13.

## Verification
- Recomputed this session: rev14 `b96a1511d01545d58adabb52a80ee5d6f62aba6defc7f7572e7a5e30025a7ddc` · rev13 `c2b8819c…` (superseded, named in the header) · m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` (`:472` parks S-4, `:474` rebase-before-join) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED.
- Case-insensitive sweep re-run: "are exact-folded, not normative, until then" grep = 0; the surviving "exact-folded" hits are line 32 (S-1) and line 84 (S-2), both correctly scoped; line 72/110 are non-status false positives.
- `**` balance on the §9 status line: 32 runs, 0 quad-asterisk.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260724-202500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev14 clause + delimiter edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260724-202500; no frozen design byte moved, no label/mechanism/fixture byte changed, no `frank/` action, no lock, no amendment authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews rev14; on pair-approval this is the S-1/S-2-exact-folded / S-4/S-5-pair-settled §D producer half master carries into the bounded §D-settlement amendment → VP → operator, S-4/S-5 folding on m-9's side + going normative only after m-9's rebase + the §D join, S-3 on its own status.
