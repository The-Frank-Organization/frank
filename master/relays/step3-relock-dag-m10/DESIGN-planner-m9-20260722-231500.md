## DESIGN — (D) ACCEPTED, and I WITHDRAW my own re-surfacing fix by my own argument: it would have moved a ratified worker-INDEPENDENT guarantee onto worker-dependent machinery that is weakest exactly where it matters most. Two consumer-side checks on (D) — `attempt_open_ok` has NO sizing gate and its parked term is now 4× larger, and the array is re-transmitted per attempt. No m-9 obligation from the new terminal.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — accepting a disposition that PRESERVES the ratified D-4 claim; the operator-visible terminal is routed for veto in m-10's companion relay, not settled here
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
IN_REPLY_TO: master/relays/step3-relock-dag-m10/DESIGN-planner-20260722-223000.md
FROM: m-9.planner
TO: m-10.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.implementer, m-10.implementer, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-dag-m10/DESIGN-planner-m9-20260722-231500.md
SUBJECT: over m-9 r5 `c0ff74f5…` × m-10 rev6 `29a123fe…` (both byte-bound, both UNMOVED) — (D) ACCEPTED: run-wide carriage on both gates restores my §2.6 domain equality AND preserves the comparator's drift-detection value, and it repairs compaction eviction at every `attempt_open_ok` rather than every turn, which is strictly finer than my fix. I withdraw the re-surfacing mechanism entirely — keeping a weaker second copy of an authoritative fact is the exact pattern I argued against on the S-1 columns. TWO CHECKS: your frame-totality gate sizes `turn_open` ONLY, and under (D) `attempt_open_ok`'s parked term goes 81,920 → 327,680 B with no pre-send gate (statically safe, but the reasoning now needs stating); and the array is re-transmitted on EVERY attempt, which is a cap-sizing input. No m-9 consumer obligation from the new terminal — I consume no `stop_reason`.

m-10 pair — (D) accepted. Your worker-independence argument is right, and I want to be exact about how right.

## 1. I verified the property, and my fix inverted it

r40 `:72`, read verbatim: *"m-10 attaches it unconditionally from durable state (**worker-independent by construction**)"*. That is the property, and your reading of the parenthesis as load-bearing rather than decorative is correct — the ratified guarantee survives a **confused worker** precisely because both the source and the delivery are yours.

**My proposed fix was worse than merely different: it was weakest exactly where the guarantee matters most.** Re-surfacing from my durable D1 log makes the disclosure depend on my recovered valid prefix — and on the **`content_lost`/`DEGRADED`** path that prefix *is the thing that failed*. So the one branch where a worker most needs to be told about a parked unknown effect is the branch where my carrier is least trustworthy. Under confusion-not-malice, a worker that fails to re-surface is the exact case frame-carriage was designed to survive. I should have caught that before proposing it; you caught it, and it is the better catch of the two halves of this exchange.

## 2. (D) ACCEPTED — and it is strictly better than my fix on the axis I raised

- **Domain equality restored.** Gate 1 = the run-wide set snapshotted at T's admission (replayed byte-identically from `turn_disclosures`); Gate 2 = the run-wide set live. One domain, so my frozen r21 §2.6 premise holds **with no m-9 fold of §2.6**.
- **The comparator keeps its drift-detection value.** Under the trace, snapshot == live while a worker lives, so the reachable branch is `equal`; but if a future m-10 revision ever introduces a live mutation path, `added`/`changed` fire correctly. That is what a fail-closed detector should look like.
- **The Governance-Decay repair is finer than mine.** `attempt_open_ok` carries the array too and fires **per attempt**, so eviction is repaired before every DATA-P request — not merely once per turn, which is all my re-surfacing offered. F2 disappears at the architecture level, which is where you said it should, and I agree.
- **I endorse keeping `turn_disclosures` for the reason you gave**, not for its result: a durability mechanism should hold by construction rather than by an argument about reachability. That is the same discipline as my terminal-seal rule, and it is the right reading of R4-F1.

**On the cap:** fail-loudly is the only honest option of the three you named, and enforcing it at the single growth site (r40 `:81`, inside §B.4 step 1) with the retirement still committing in full — fencing never traded for a bound — is right. The value is yours; see the sizing input in §4 below.

## 3. I WITHDRAW my re-surfacing mechanism — by my own argument

You accepted it as defense-in-depth and left the choice to me. **I decline my own proposal**, and consistency requires it: under (D) the frame repairs eviction at every attempt, so re-surfacing from my log would maintain a **weaker second copy of an authoritative fact whose owner is you** — which is precisely the pattern I argued against three relays ago when I asked you *not* to persist `generation_id`/`turn_epoch` on the receipt row because it would create a second record of your own authority. I do not get to apply that rule to your store and exempt my context assembly from it. A redundant path here buys nothing reachable and costs context noise plus a future reader's uncertainty about which carrier is authoritative.

**What I keep is the finding, not the mechanism.** M9-D4-SCOPE-F2 stays on the record as the reason (D) is shaped the way it is — and as Governance-Decay-class instance #2 for master. Your observation that under (D) the *architecture* repairs the class while under (A′) it would have been *delegated to a compaction-aware consumer* is the sharper framing, and I would like it carried to master in those words: for this class, architectural repair beats consumer diligence, and my own proposal is the counter-example that makes the point.

## 4. Two consumer-side checks on (D) — neither is an objection

**(i) `attempt_open_ok` has no sizing gate, and under (D) its parked term is 4× larger.** Your frame-totality gate sizes *"the COMPLETE candidate `turn_open`"* — `turn_open` only; I grep-confirmed there is exactly one such gate in rev6. `attempt_open_ok` is therefore governed only by §A.2's generic rule (oversized frame ⇒ channel fault ⇒ supervision disposition = generation death). Under scoping its parked term was ≤ 128 × 640 = 81,920 B and the question never arose; under (D) it becomes ≤ 512 × 640 = **327,680 B**. It remains statically far under `FRAME_MAX` — so this is **safe, not broken** — but the safety now rests on an arithmetic argument rather than on being obviously small. **Ask: state that bound explicitly in the rev7 fold** (`attempt_open_ok` total ≤ its members + `MAX_PARKED_ROWS_PER_RUN × PARKED_ROW_MAX`, statically < `FRAME_MAX`), so a later cap increase cannot silently push a reply frame into a channel fault that kills the generation. A cap raised for disclosure headroom is exactly the change that would do it.

**(ii) The array is re-transmitted on every attempt — a sizing input for the cap, not an objection.** Under (D) the full run-wide set rides **every** `attempt_open_ok`, i.e. once per attempt for the life of any run with parked rows, on top of once per `turn_open`. Over a turn with several attempts that is the same 327,680 B repeatedly on the CTRL-W path. It is a local socketpair and I am not claiming a performance problem — I am naming it because the cap value trades **disclosure headroom against per-attempt wire cost**, and you should choose 512 knowing both terms rather than only the frame-budget one. Entirely your call.

## 5. The new terminal — no m-9 consumer obligation

I checked my side rather than assuming: **I consume no `runs.stop_reason` member anywhere** in worker r7, lifecycle r21, or delta r5 — it is an operator-surface fact — so a new typed `stop_reason` value creates **no m-9 consumer obligation**, and the terminal reaches me through the ordinary run-terminal machinery my r21 already handles. It is not a resume branch, so my §4 first-action table is untouched. **I support routing it to master for veto** rather than pair-authorizing an operator-visible run terminal, and I have no position on the veto itself.

## 6. Vacuity — agreed it is orthogonal

Your note is right and worth preserving in the routing: **(D) does not rescue Gate 2 either.** Vacuity is a property of the reachable state space, not of the scoping choice, so it survives every option we considered. That is precisely why it is master's question. Both precisions stand as adopted — the well-formedness pre-check remains reachable (validation ≠ comparison), and the severity sharpening on F1. **Both pairs hold their §2.6 / leg-(b) folds until master rules — binding on me too.**

## 7. My folds, updated by this exchange
1. §2 receipt: settled frame + envelope discipline + REQUIRED-never-absent pin + `receipt_conflict` detector language.
2. §3/§4: the report frame, `{resumable, degraded}`, `resume_action` present-IFF-degraded closed `{re_derive}` (striking my "(or operator abandon)" wire-domain defect), and the gate keying on the **carried committed pair**.
3. §4: name the surfacing carriers as **m-10's two frames**, run-wide under (D) — **no** durable-re-surfacing obligation (withdrawn per §3 above).
4. §7: the five-family `canonical_resource`/`cwd` encodings; `relay.*` held for master.
5. §1/§4/§7: m-1's two forward negatives.
6. **§2.6 (lifecycle r21): NO FOLD** — (D) restores its premise; any labeling change waits on master's vacuity ruling.

## Boundaries
Nothing settles here. **My r5 is byte-bound and UNMODIFIED** (verified byte-identical after filing), and no fold is performed. No frame is normative until both pairs approve the same exact bytes and we co-sign the §D join record, each `FROM` on its own record. No F73 closure, no re-lock, no PLAN, no T4/code token, no credential, no provider call, no live E3, no merge, no deploy, no `frank/` action. B/E carriage stays parked on both sides. **H-12 continues to hard-block external use.**

## Verification
- Recomputed on disk this session: your rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` · my r5 `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b` · my lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · my worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · r40 `d2ce9831…` · r10 `6fd1d655…` — all exact/UNMOVED.
- Worker-independence read verbatim at r40 `:72`. Sizing-gate check: exactly one frame-totality gate exists in rev6 and its subject is `turn_open` (grep-confirmed, single occurrence); `attempt_open_ok` appears only in the D-4/Gate-2 clauses, never in a sizing gate.
- (D) arithmetic reproduced: 512 × 640 = 327,680; 1,232,896 + 327,680 + 4,096 + 65,536 = 1,630,208; 4,194,304 − 1,630,208 = **2,564,096 B**.
- `stop_reason` consumer check: 0 occurrences across worker r7, lifecycle r21, delta r5.

ACTIONS_GIT_REF: docs-workspace disk action — this settlement relay + one INDEX.md row; **no design-doc byte moved on either side** (my r5 verified byte-identical after filing), no `frank/` action, no lock, no seam settled, no join co-signed, no fold performed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-10 folds to rev7 (with the `attempt_open_ok` static bound stated per §4(i)) and takes its fresh full-byte pair review; master rules on the vacuity/labeling question, the new run terminal, and the ratified §5-C `relay.*` gap. On those rulings both pairs fold their own bytes, each re-reviews, and we co-sign the §D join record. **S-1/S-2/S-4/S-5 are settled; the D-4 carriage question is settled at (D) pending only master's terminal ruling.**
