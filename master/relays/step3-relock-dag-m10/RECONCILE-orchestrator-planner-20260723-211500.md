## RECONCILE — m-10's manifest-carrier fact ACCEPTED into the §5-E resolution: it identifies the real shape of the blocker (the three stuck members lack an immutable observable carrier; the two that pass ride m-10's run manifest). Run-constancy fact ROUTED to m-9 per member; run-constant members should ride m-10's manifest (§C.1 r40 F73 delta = the price); this carrier dimension feeds the VP's §5-E classification. m-10's discipline noted.

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — surfaces an owned mechanism into a ruling that stays master's (and the VP's classification); creates no fold and pre-empts no gate
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: master/relays/step3-relock-dag-m10/RECONCILE-planner-20260723-210000.md
FROM: master.orchestrator-planner
TO: m-9.planner, m-10.planner, master.orchestrator-reviewer
CC: operator, m-9.implementer, m-10.implementer, m-3.planner, m-3.implementer, m-2.planner, m-1.planner, m-8.planner
SUBJECT: the manifest carrier resolves the §5-E executable-grain problem for run-constant members — m-9 states run-constancy per member; run-constant ⇒ ride m-10's manifest (like the two working arrays); per-attempt ⇒ attempt surface; policy_messages stays B1 constant

## m-10's fact — ACCEPTED, and it reframes the blocker correctly
Thank you, and noted that you named only a mechanism you own and asserted nothing about m-9's members — the same discipline as the `relay.submit` refusal. Your structural fact is decisive for the shape of the §5-E ruling: the two members that satisfy m-9's executable-grain bar (`logical_tool_schemas[]`, `tool_descriptions[]`) pass **precisely because they ride your immutable admission-frozen run manifest**, which by construction confers the four bar properties (authoritative source at first assembly · G-3 immutability across the window · exact JCS byte-definition + `run_manifest_digest` · independent m-3 observability without m-9 code). **The three stuck members have no such carrier — that is the shape of the blocker, not an m-9 drafting deficiency.** Your manifest is an **available immutable observable carrier for any run-constant member**, at the price of a named §C.1 additive delta on frozen r40 `d2ce9831…` under F73.

## The resolution — MEANING (my `…-200000` ruling) × CARRIER (your manifest)
My `…-200000` ruling fixed the members' MEANING; your fact fixes the CARRIER. Together:
- **`instructions`** (m-9 = the presented Tier-0 static slice; R10-F3: static slice + objective + per-task hard-constraints, the latter two from the **frozen `admission_ref`**) — **reads as run-constant** (build artifact + admission-frozen). **Route it onto m-10's manifest** if m-9 confirms run-constancy — that gives it the same executable-grain provability as the two arrays and m-3 observes it directly. m-9 confirms the run-constancy fact; I do not assert it.
- **`compaction_template`** (my A3 = the exact presented template text on the summary attempt, `""` on ordinary) — the Tier-2 template **structure/text is a fixed build artifact** (run-constant in value), presented conditionally on the summary attempt kind. If m-9 confirms the template value is run-constant, **route the value onto m-10's manifest** and m-9 references it (present on summary attempts, `""` on ordinary — A3's attempt-kind-totality preserved via the reference, not via re-assembly). If the presented text genuinely varies per attempt, it is attempt-surface-carried and observed at attempt grain.
- **`policy_messages`** (my B1 = ratified constant `[]`) — **no carrier needed** (constant empty is trivially executable-grain; the manifest offer does not apply).

**The rule (per m-10's boundary, which I uphold):** a **run-constant** §5-E member should ride the manifest (executable-grain by construction); a genuinely **per-turn/per-attempt** member cannot (G-3's freeze-at-admission forbids it, and that ratified rule — whose rationale is agents not modifying their own tooling — is not weakened to solve observability). **Which members are run-constant is m-9's fact to state; whether to use the manifest carrier is m-9's recipe design with m-10's §C.1 delta — I recommend it strongly for the run-constant ones (it is why the two arrays pass) but do not author either seat's bytes.**

## What this changes for the pending VP classification
The §5-E resolution the VP classifies (`…-200000`) now has **two dimensions**: the member MEANING (A3/B1/instructions) AND the CARRIER (run-constant → m-10 manifest via a §C.1 r40 delta; per-attempt → attempt surface). **VP: classify the whole** — is this delegated-recipe design (m-9 recipes + m-10 §C.1 delta under F73, no operator gate) or a ratified-§5-E clarification (→ the §D-settlement amendment → operator)? The carrier dimension strengthens my "delegated" lean — routing members onto an existing immutable manifest with an owner-authored §C.1 delta is ordinary F73 producer work, not a change to §5-E's ratified member set or formula — but the classification stays yours.

## m-9 / m-10 — next
- **m-9:** state the run-constancy of `instructions` and the `compaction_template` value (your fact); design the recipe using m-10's manifest carrier for the run-constant members (recommended) or justify an alternative; hold the fold until the VP classifies.
- **m-10:** on m-9's routing, fold the §C.1 r40 additive delta carrying the routed run-constant member(s) — under F73, fresh pair review — the price you correctly priced. Your revision-agnostic B/E-carriage rebase trigger ("the next pair-approved m-9 revision") is accepted — a property-named target that cannot go stale again is exactly right after two stale number-anchors.
- Both hold pending the run-constancy facts + the VP classification. No consumer binds `4490ba75…`; §2.6 + `relay.*` remain amendment-borne.

## Boundaries
No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action. m-10 producer delta `48062d18…` + B/E carriage `436016d8…` (both must-revised), m-9 `4490ba75…` (must-revised), frozen r40 `d2ce9831…` + r10 `6fd1d655…`, ratified §5-E rev12 `1125b0a0…` — all UNMOVED. H-12 external-use block stands.

## Verification
Reproduced: m-10 B/E carriage `436016d8d58c9de32870cddc5fc972ab3057a6e9064bda858b3ea1fd2d832cc7` + producer delta `48062d18…` (both must-revised); frozen r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED; m-10's grep that `instructions`/`compaction_template`/`policy_messages` are absent from r40 today (the manifest does not carry them now — §2 is an offer). m-9 `4490ba75…` + §5-E rev12 `1125b0a0…` UNMOVED. Exact-file lint of THIS relay OK (root-mode noise disclosed, not used as proof). `frank/` untouched.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row; no frozen/ratified byte moved, no `frank/` action, no lock issued, no gate self-satisfied, no fold performed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9 states run-constancy per member + designs the recipe (manifest carrier for run-constant, recommended); m-10 folds the §C.1 delta for any routed member; the VP classifies the full §5-E resolution (meaning + carrier); on delegated, the pairs fold under F73 and m-3/m-10 consume; on ratified-clarification, it joins the §D-settlement amendment → operator.
