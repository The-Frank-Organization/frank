## DESIGN — the §D producer rev16 @ `3e3c5192…` folds M10-DAG-R15-F1: the `parked_unknown_capacity_exceeded` cap terminal now carries the ratified Correction-2 no-revival acceptance predicate (packet `1fa71cb8…` §2.2 rules 3+4) at both operative sites; all rev15-passed bytes preserved; §D-seam frame normativity still JOINT-PENDING until the two-sided join

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — the operator ratification already fired (recorded §8b at `…170000`); this folds the ratified packet's complete cap-terminal lifecycle into the owner artifact, no new operator choice is open at this seat
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r15
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260725-183000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` **rev16 @ SHA-256 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6`** (rev15 `4cf52859…` ancestry-only, superseded); binds the operator-ratified §D-settlement amendment packet `1fa71cb8…`; frozen r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED; r21 byte-frozen

I folded **M10-DAG-R15-F1** (`step3-relock-dag-m10-review-r15/DESIGN-REVIEW-implementer-20260725-183000`). I verified the finding real against the ratified packet bytes before folding: `master/STEP-3-STAGE6-SETTLEMENT-AMENDMENT.md` @ `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b`, Correction 2 §2.2 rule 3 lines 36–37 ("no successor generation, same-run continuation, lease, snapshot, or revival") + rule 4's acceptance predicate ("the full retirement batch … the single FAILED/stop-reason terminal … no successor/lease/snapshot/revival … the durable operator projection rendering the terminal + the complete queryable parked set" + "MAX_PARKED_ROWS_PER_RUN cannot be implemented as a 512-row storage cap"). **The finding was correct:** rev15's cap-terminal clause (`:72`) and `FX-M10-CAP` (`:160`) committed the `runs.state = FAILED` + `stop_reason` transition and asserted only *no successor generation*; the complete no-continuation/no-lease/no-snapshot/no-revival language existed ONLY on the DISTINCT `resume_frame_overflow` continuation-sizing terminal (`:59`), which cannot prove the cap terminal's own ratified lifecycle. That is exactly the amendment's timeless-fold rule — a fresh successor folds the packet's complete terminal semantics + acceptance predicate, not only the pre-ratification mechanism's status flip.

**What moved (two operative sites; everything else byte-identical to rev15):**

1. **§4 cap-terminal clause (`:72`)** — after "every parked identity still queryable afterward," it now states the ratified rule verbatim: **the cap terminal admits NO revival — no successor generation, no same-run continuation, no lease, no snapshot, no revival; any next work is an ordinary NEW run**; and clarifies that **`MAX_PARKED_ROWS_PER_RUN` is an ENFORCEMENT threshold at the parking write, NOT a 512-row storage cap** — the full retirement batch commits and the complete parked set stays queryable even past 512 (§2.3). It then states this terminal is **DISTINCT from `resume_frame_overflow`** (they share the no-revival/new-run-only lifecycle but arise at different loci — cap-vs-continuation-sizing — and neither's fixture proves the other's).

2. **`FX-M10-CAP` (`:160`)** — leg **(ii)** (512 parked + 1-row retirement ⇒ post-commit 513 ⇒ terminal) now observes the **FULL ratified acceptance predicate in the ONE transaction**: the complete retirement batch (all parked rows committed) · the SINGLE `runs.state=FAILED` + `stop_reason=parked_unknown_capacity_exceeded` terminal · NO successor generation, NO same-run continuation, NO lease, NO snapshot, NO revival · the durable operator projection rendering the terminal + the COMPLETE queryable parked set — and it does **NOT reuse the `resume_frame_overflow` fixture** (the cap terminal's lifecycle is proven on its own bytes). Leg **(iii)** (the multi-row overshoot from 510) now additionally asserts **no successor/lease/snapshot/revival is created** and adds that the batch's post-commit count legitimately exceeding 512 *proves `MAX_PARKED_ROWS_PER_RUN` is an enforcement threshold, not a 512-row storage cap*.

**Preserved, deliberately (per your "preserve all passed bytes"):** the packet-bound RATIFIED status of Corrections 1+2, the basis binding `1fa71cb8…`, the two compile-time frame assertions (§4 `ADMISSION_REF_ENC_MAX + …` + the §D3 frame-totality assertion; `PARKED_MAX = 327,680`, `FRAME_CONTENT_BOUND = 3,704,832 B`), the run-wide carriage on both frames, the 512 threshold + full-batch/no-prefix/no-truncation rules, the Correction-3 citation-only posture (§5/§9 name m-2's ratified `relay.submit` cell `5ec7a3d2…`, fold left to m-9's §7), and the **§D-seam FRAME normativity (S-1/S-2/S-4/S-5) UNCHANGED — still JOINT-PENDING** until this successor's pair approval AND the §D two-sided join co-sign. `resume_frame_overflow`'s own no-successor/no-revival language (`:59`) is left intact and distinct.

**Sweep discipline applied (the recurring lesson):** a case-insensitive concept sweep for the no-revival predicate confirmed it now lands on the operative cap clause (`:72`) and both `FX-M10-CAP` legs (`:160`), distinct from `resume_frame_overflow` (`:59`); `**`-balance is even (790 delimiters); the one quad-asterisk run is the backtick-quoted `` `****each` `` ancestry literal, not a live delimiter.

**Review ask:** confirm the cap-terminal clause + `FX-M10-CAP` now carry the ratified no successor-generation/continuation/lease/snapshot/revival predicate + the durable-projection observation, the cap-vs-`resume_frame_overflow` distinction is honest and neither fixture proves the other's terminal, the enforcement-threshold-not-storage-cap clarification is correct, and the packet-bound status + two compile-time assertions + §D join gate + all other bytes are byte-identical to rev15.

## Verification
- Recomputed this session: rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` · rev15 `4cf52859087aed8f00a7c25585c1fb102c503d2ebb176d1d2123427e56605094` (ancestry, named in the header) · ratified packet `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b` + bound m-2 cell `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f` (both from the §8b ratification record) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED; r21 `4d3bd14e…` byte-frozen.
- R15-F1 verified real at packet §2.2 rule 3 lines 36–37 + rule 4 acceptance predicate + the "cannot be implemented as a 512-row storage cap" clause, before the fold.
- No-revival concept sweep: the predicate lands on `:72` (cap clause) + `:160` (`FX-M10-CAP` legs ii/iii), distinct from `:59` (`resume_frame_overflow`); `**` balance even (790); one backtick-quoted quad literal only.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260725-201500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev16 §4 cap-terminal clause + two `FX-M10-CAP` legs + header fold-log edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260725-201500; the ratified amendment packet + m-2 cell stay byte-exact (not edited); no frozen design byte moved, no compile-time assertion or §D-seam status byte changed, no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer reviews rev16; on pair-approval this is m-10's settled §D producer successor — one of the two conditions for the §D two-sided join (m-9's batched successor, now at r14 `514f8855…`, is the other); once both are pair-approved the §D join co-signs with m-1 redaction, the lane-2 DAG closes, then item A (bundle) → lane 4 (re-lock) → lane 5 (T4). H-12 stands.
