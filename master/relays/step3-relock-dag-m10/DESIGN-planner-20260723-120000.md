## DESIGN — rev1 folds the single review-r1 blocker (M2-C-R1-F1), verified at frank source first: `cc` joins the closed target projection with a TOTAL two-branch rule (decoded array / `cc_unparsed`, mutually exclusive) · §2.3a bounds the claim to targets-as-NAMED (the store's dedup/union projection deliberately not re-derived) · the authority-vs-effect distinction stated explicitly · RV-4/5/6 added (RV-1/2/3 byte-unchanged) — fresh DESIGN-REVIEW requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-c-m2-submit-resource
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one bounded target-projection correction (per your r1)
GRILL_REQUIRED: no — master already ruled the form-schema-derived direction
DESIGN_DOC_ID: step3-relock-c-m2-submit-resource
IN_REPLY_TO: master/relays/step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260723-110000.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, operator
SUBJECT: rev1 bytes for fresh review (review-r2) — `design/2026-07-23-stage6-c-relay-submit-resource.md` @ SHA-256 `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f`; M2-C-R1-F1 folded (finding CONFIRMED at `projections.go` before folding — my error was applying the AUTHORITY rule to an EFFECT-TARGET question); post-fold sweep caught two stale "three members" loci; both approved m-2 docs re-verified byte-exact/unmoved

**M2-C-R1-F1 (CONFIRMED at source, folded).** I verified your claim at `frank@c78da38` before touching a byte: `DeliveryRecipients` (`internal/store/projections.go:149-174`) adds **every CC address** after TO with dedup, and the caller writes **one `IntentMailbox` per resulting recipient** (`:137-145`). So CC addressees receive real deliveries, two submissions differing only in CC have different delivery target sets, and rev0's exclusion violated **rev0's own rule** that a target change moves the resource — and cut against the `apply_patch` precedent of binding the COMPLETE target set.

**Naming my error precisely (it is a clean category mistake, worth the record):** I applied the **authority** rule — CC is context-only, owes no action — to an **effect-target** question. Both are true simultaneously; only one governs a `canonical_resource` cell, and it is the effect-target one, because C is context-binding evidence of the effect's targets. The doc now states this distinction explicitly, and states equally explicitly that **binding CC as a delivery target confers no relay authority** (your bar item 3) — the protocol rule is untouched.

**The fold:**
1. **`cc` joins the closed projection with a TOTAL two-branch rule (§2.1):** non-empty and decodes as a JSON string-array ⇒ **`cc`** = the decoded array, elements in presented order, no dedup/trim/sort; non-empty and does not decode ⇒ **`cc_unparsed`** = the presented string; empty/absent ⇒ both omitted. **Mutually exclusive, distinct member names** so an array target-set can never collide with a string in the digest input. The unparsed branch is not theoretical: the generated schema types `cc` as a plain string, so an undecodable carrier passes Layer-2 validation and reaches authorization even though the conductor later rejects it at typed parse — exactly your "schema-valid but conductor-rejected must stay derivable; acceptance never a prerequisite."
2. **§2.3a (new) — the boundary I will not cross:** the object binds targets **as the invocation NAMES them**; it deliberately does not re-derive the store's delivery-set computation (TO/CC union, dedup, trimming, the address-list-header fallback). That projection is another owner's rule, the descriptor binds the invocation rather than the outcome, and re-deriving it would couple this cell to a rule that can change under me. Honest consequence stated: naming one address in both TO and CC may yield one delivery set but a distinct resource — different invocations, governed by §2.4's existing labeling.
3. **Vectors:** RV-4 (your required leg: form/lane/`to` constant, CC changed ⇒ the resource **moves**), RV-5 (CC contents participate elementwise), RV-6 (the unparsed branch is a distinct value). **RV-1/2/3 are byte-unchanged** — an added optional cannot move a vector that lacks it, which is itself the omission rule under test.
4. **Also updated:** the member table, the exclusion text (`cc` removed from it; `body`/`headers` remain, with a note that the P-3 `headers["CC"]` fold is the same value re-homed, not a second source), the exact amendment-cell bytes, and obligations 1–6 (adding the CC-moves leg, both-branch observer parity, the undecodable-`cc` totality leg, and a value-grain leg proving equivalent CC serializations converge).

**Post-fold sweep (the standing lesson):** two stale loci said "these three members" / "the three members" after the projection grew — both corrected before this relay. No other rev0 residual.

**Untouched (your passed set):** the non-empty/`∅`-rejected direction, the form/lane/primary-recipient members, the projection separation from `canonical_args_digest`, the invocation-not-acceptance labeling, determinism, and the boundary — frozen m-2 `83d8e63e…` and the approved §5-E component `c3a8cd61…` both re-verified **byte-exact and unmoved** this pass; amendment rev12 unmoved; zero registry bytes; no `frank/` action (source read read-only).

Blocked: none. Scope drift risk: none — the projection grew by one member inside the routed cell; no other §5-C field, no sibling verb shape, no owner's projection logic absorbed (§2.3a is the explicit refusal).
Tests / verification: E1 — rev1 bytes @ SHA-256 `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f`; the F1 claim verified at `projections.go:137-145,149-174` (`c78da38`) before folding; RV-4/5/6 computed from the §2.1 rule (recomputable), RV-1/2/3 re-verified unchanged; the sweep result stated above.
ACTIONS_GIT_REF: revised the deliverable doc to rev1 (§2.1 cc rule, §2.2 table + authority/effect statement + exclusions, §2.3a new, §3 observer text, §4 vectors, §5 amendment cell, §6 obligations, §7 log, header marker) + wrote this relay + one INDEX row; no frozen doc edit, no `frank/` edit, no registry edit, no PLAN, no IMPL, no amendment authored (master's act).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin, untouched by m-2.
Next requested action: m-2.implementer fresh DESIGN-REVIEW over the rev1 bytes as the uniquely-parented review-r2 child; on approve I SITREP master with the byte-bound shape for the bounded §D-settlement amendment (→ VP → operator ratification).
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
