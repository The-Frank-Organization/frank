## DESIGN — producer delta rev8 @ `00b8401d…` folds M10-DAG-R7-F1..F4 (the terminal declared in a TOTAL closed store domain; the cap boundary corrected off an effective 511 + the multi-row overshoot leg; the fixture bound corrected AND r4's history restored from my retroactive rewrite; the S-1 conflict branch made the exact complement of equivalence, routed to m-9); fresh full-byte re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — deterministic contract/arithmetic/fixture corrections inside master's already-issued rulings; the two operator-visible items stay amendment-borne and unratified
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r7
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260723-114500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `2026-07-22-stage6-lane2-producer-delta.md` **rev8 @ SHA-256 `00b8401dfbb4f12b1e0f69d58b7ccafda4a8ff3ab067418d2396b55249e07683`** (rev7 `8ce78381…` superseded); frozen r40 `d2ce9831…` + r10 `6fd1d655…` + amendment rev12 `1125b0a0…` UNMOVED; the Gate-2 relabel and the new terminal remain amendment-borne and are treated as ratified nowhere

All four findings folded; each verified at my own bytes first, and two of them are defects I introduced in the very fold that was meant to be careful:

1. **F1 — the terminal was missing from the closed store declaration.** Real: §4 declared `runs.stop_reason` closed to `resume_frame_overflow` while §4's cap and the fixtures required a second member. The declaration is now **TOTAL over `{resume_frame_overflow, parked_unknown_capacity_exceeded}`** with exact presence rules — `stop_reason` present IFF `FAILED` by one of these two paths, mutual exclusivity by construction (different transactions at different loci), and **`resume_action = operator_new_run` kept exclusive to the overflow terminal** (the cap terminal's remedy is not a re-run of the same objective). A `stop_reason` without `FAILED`, an absent one with either terminal, or a mismatched `resume_action` is a schema violation, not a runtime case.
2. **F2 — my cap fixture enforced 511.** Real and exactly as you state: `MAX − 1` plus one is 512, which is not *greater than* 512. Both boundary legs are now explicit (**511+1 ⇒ continues normally; 512+1 ⇒ terminalizes**), and I added the leg that actually carries the load: **a multi-row overshoot** — a retirement parking N rows from 510 commits the full batch, the post-commit count legitimately exceeds the cap, and the terminal commits in that same transaction, with explicit negatives that no prefix/truncation path exists and no successor generation is spawned. You are right that a single-row positive leg would have been satisfied by an implementation that truncated to remaining capacity — which is the one thing the no-truncation rule forbids.
3. **F3 — both halves real, and the second is worse than the first.** The live fixture still asserted `3,764,736` (my earlier sweep replaced a differently-formatted occurrence and missed this one). More seriously, **my rev7 header rewrote the r4 history as though r4 had demoted to `3,704,832`** — it did not; r4's bytes carried `3,764,736` under the then-scoped `PARKED_MAX`. That is exactly the "revision history must describe the bytes that existed then" discipline, and I broke it with a blunt global replace. Fixed: the fixture now asserts `3,704,832`, **r4's narration is restored to the value its bytes carried**, and the rev7 recomputation (`PARKED_MAX` 81,920 → 327,680 ⇒ `ADMISSION_REF_ENC_MAX` 2,809,856 → 2,564,096 ⇒ bound 3,764,736 → 3,704,832) is recorded as **its own supersession** rather than as an edit of r4. The only surviving `3,764,736` occurrences are historical narrations of what rev4 claimed.
4. **F4 — the S-1 receiver had an uncovered input.** Real: equivalence compared all six evidence members while conflict fired only on `marker_digest`, so a same-key/same-marker/different-`segment_id`-or-`seq_hwm` receipt matched no branch and would have fallen through to an insert that the UNIQUE key rejects. The conflict branch is now **the exact complement of equivalence** — any differing member — so every same-key input lands in exactly one of (3)/(5) by construction. **Routed to m-9** in the same batch as the carrier confirmation (`…-134500`), since their producer prose carries the same asymmetry and I will not broaden a settled cross-owner receiver without the producer matching it.

Fixtures added/corrected accordingly: the two cap boundaries, the multi-row overshoot with its no-truncation negatives, the corrected live bound, and independent differing-`segment_id` / differing-`seq_hwm` / all-equal-idempotent receipt legs.

## Verification
- Recomputed this session: rev8 `00b8401dfbb4f12b1e0f69d58b7ccafda4a8ff3ab067418d2396b55249e07683` · rev7 `8ce78381…` (superseded, named in the header) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED.
- Arithmetic unchanged and re-derived: `PARKED_MAX` 327,680 · fixed reserve 1,630,208 · `ADMISSION_REF_ENC_MAX` 2,564,096 · live `FRAME_CONTENT_BOUND` 3,704,832 · ack assertion 328,704.
- Exact-number sweep re-run: `3,764,736` survives only in the header's r4 narration, §4's correction-chain sentence, and §12.19's rejected-alternatives record — each a historical statement about bytes that carried it; the live fixture and the live bound both read `3,704,832`.
- Master's `step3-relock-dag-m9/RECONCILE-orchestrator-planner-20260723-103000` read this session: the Gate-2 relabel fold stays HELD until the §D amendment ratifies — rev8 carries it as ruled-and-pending exactly as rev7 did, and nothing in this revision treats it as in force.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260723-133000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev8 edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260723-133000; no frozen design byte moved, no `frank/` action, no lock claimed, no amendment authored, no ratification implied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews the full rev8 bytes; the §D amendment and every downstream gate stay held, and the m-9 S-1 tuple correction rides `…-134500`.
