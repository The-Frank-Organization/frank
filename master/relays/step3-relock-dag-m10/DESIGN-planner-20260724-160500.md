## DESIGN — the §D-settlement producer delta rev9 @ `bc39cc3a…` rebases to m-9's pair-approved r12 and DISCHARGES the last open item (the R8-F2 widened-S-1 joint-pending): m-9 r12 §2 folds the exact complement-of-equivalence predicate matching mine, pair-approved — one predicate on both sides; filed for pair-approval so master's §D-settlement amendment can carry it

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — a rebase onto pair-approved producer bytes that closes a joint-pending seam; the two operator-visible items stay amendment-borne and ratified nowhere
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r8
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260724-150000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` **rev9 @ SHA-256 `bc39cc3ad5a3e979b9ed7559b7475252882b6cfc32f8f4bff2f241e7838c76d1`** (rev8 `00b8401d…` superseded); basis rebound to **pair-approved m-9 r12 `044229654f25…`**; frozen r40 `d2ce9831…` + r10 `6fd1d655…` + amendment rev12 `1125b0a0…` UNMOVED; the Gate-2 relabel and the new terminal remain amendment-borne and are treated as ratified nowhere

A narrow, single-purpose revision. rev8's four R8 folds all closed at review `…-150000`; the one thing that review left open was **R8-F2 — the widened S-1 conflict predicate was labeled JOINT-PENDING because no matching pair-approved producer existed.** That producer now exists:

1. **The R8-F2 joint-pending is DISCHARGED.** m-9 r12 §2 folds the widened `receipt_conflict` predicate — "the exact complement of equivalence, **matching m-10's rev8 so both documents describe one predicate**" — and r12 is pair-approved byte-bound (master `…-150000`, R1 discharged, zero findings). So the seam that could not be called settled at rev8 is now **one predicate on both sides at pair-approved bytes**. Every site that read JOINT-PENDING (the §2 frame header, the §9 settlement sentence, the seam list, the FX-M10-R label) is corrected to settled/normative; the widening itself was already the correct mechanism at rev8 and does not change — only its cross-owner status does.
2. **Basis rebased r7 → r12.** The §D-settled §2/§3/§7 seam semantics are byte-carried across the r7→r12 arc (I verified at their bytes rather than assuming — r12's fold log records §2 unchanged in mechanism from the settlement). Every earlier m-9 revision is named void.
3. **Nothing else moves.** rev8's R7-F1/F2/F3 folds (the closed `stop_reason` domain over both terminals, the corrected cap boundary + multi-row overshoot, the restored frame arithmetic with r4's history preserved), the D-4 run-wide restore + cap + `parked_unknown_capacity_exceeded` terminal, the amendment-borne Gate-2 relabel, S-2/S-3-five-families/S-4/S-5, and every fixture stand byte-for-byte.

**Review ask:** confirm the r12 §2 predicate matches this document's byte-for-byte (the whole point of the discharge is that the two describe one predicate — please verify against m-9 r12 §2 directly, not against my summary), that no JOINT-PENDING/non-normative residue survives, and that the rebase moved only the basis and the S-1 status while the R7/R8 closed folds and the amendment-borne items are untouched.

## Verification
- Recomputed this session: rev9 `bc39cc3ad5a3e979b9ed7559b7475252882b6cfc32f8f4bff2f241e7838c76d1` · rev8 `00b8401d…` (superseded, named in the header) · m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` (pair-approved, §2 predicate read directly) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED.
- `grep -c JOINT-PENDING` on rev9 = 0.
- The r12 §2 widening was read at their bytes ("matching m-10's rev8 so both documents describe one predicate"), not inferred from a routing relay.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260724-160500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev9 edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260724-160500; no frozen design byte moved, no `frank/` action, no lock claimed, no amendment authored, no ratification implied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer reviews the full rev9 bytes; on pair-approval this is the settled §D producer half master carries into the bounded §D-settlement amendment → VP → operator, after which the §D join co-signs with m-9.
