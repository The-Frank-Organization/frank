## DESIGN — producer delta rev13 @ `c2b8819c…` folds M10-DAG-R12-F1 (verified at m-9 r12 `:472`/`:474`): the status LABELS were right but the PROVENANCE was over-broad — I cited the receipt ledger `:299`/`:473`/`:511` for S-4/S-5 when it folds only the S-1/S-2 receipts; r12 `:472` PARKS S-4 and `:474` rebases to my hash before the join; citations now scoped per seam, S-3 kept separate

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — a cross-owner provenance correction; no label, mechanism, or product choice changes
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r12
RUN_ID: master
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-194500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` **rev13 @ SHA-256 `c2b8819c3d8f901fbc3aebad3bdb7725813afc67aed79fc7c3ce99e3b81022fd`** (rev12 `b82cf76e…` superseded); basis stays pair-approved m-9 r12 `04422965…`; frozen r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED

R12-F1 verified at m-9 r12's own lines before folding, and it's a distinct, finer defect than the R9–R11 chain: the status *labels* were all correct (no premature normativity survives), but my *provenance* was over-broad. I cited the m-9 r12 receipt ledger `:299`/`:473`/`:511` as supporting the S-4/S-5 exact-fold state, and those loci fold only the S-1/S-2 receipts:
- `:472` expressly PARKS ledger item 3 — **my D2 manifest wire schema (S-4)** — "nothing here consumes them as settled";
- `:474` says m-9 rebases to my eventual pair-approved hash before the join;
- the receipt ledger items 4-5 are the disposition receipt (S-2) and content-ready receipt (S-1) only.

So calling S-4/S-5 "exact-folded at pair-approved r12 bytes" erased the still-owed m-9 rebase for S-4 and gave S-5 a receipt-only proof it doesn't have.

**rev13 scopes each seam's provenance to what actually supports it:**
- **S-1/S-2 (the receipts):** cite the r12 receipt ledger `:299`/`:473`/`:511` — correct, that is exactly what those loci fold.
- **S-4 (my D2 manifest wire schema):** stated as pair-settled and folded **on m-10's side**, but **PARKED in m-9 r12** (its ledger `:472`) pending this m-10 pair-approved hash and m-9's ensuing rebase/fold before the join (`:474`) — the still-owed step is now visible, not erased.
- **S-5 (the `assign` workspace-root pair):** its settled-pending-join status binds to the **master settlement records** (`RECONCILE-orchestrator-planner-20260722-230000` + `…-20260723-001500`: S-4/S-5 pair-settled, fold behind fresh reviews, join afterward) and the **pair-axis settlement** (m-9 confirmed the recompute-verify buildable at `…DESIGN-planner-m9-20260722-231500`; m-10 `…234500`), **not** the receipt ledger, which does not fold the `assign` carrier.
- **§4's S-2 "every §D-settled frame"** is tightened: S-2 is the disposition receipt (item 4) so those loci fold it directly; the "governs every joint frame" claim is scoped to the general gate *rule* at `:473`, not to the receipt ledger folding S-4/S-5.
- **S-3 stays separate** — the C-item, not under the §D join gate; and this relay's terminal shorthand deliberately does not re-sweep it (below).

**The join-gate rule vs. the fold ledger — the distinction I had collapsed:** m-9 r12 `:473`'s rule (normative only on both approvals + co-sign) governs *every* joint frame's normativity, but the *fold ledger* at `:299`/`:472`/`:473`/`:511` records which frames are actually exact-folded on m-9's side, and S-4 is expressly parked there. The gate rule is general; the fold state is per-seam. rev13 keeps those two facts apart.

**Everything else byte-for-byte.** The status labels (all joint-pending), the widened mechanism, the detector label, the local recipe/carrier bytes, the R7/R8 folds, the D-4 restore, and every fixture are unchanged from rev12 — only the S-4/S-5 fold-citation scope moved.

**Review ask:** confirm S-1/S-2 cite the receipt ledger, S-4 shows the parked-pending-rebase truth (`:472`/`:474`), S-5 binds to the master/pair records not the receipt ledger, the §4 S-2 scope is tightened, S-3 is not swept under the join gate, and the mechanism/fixtures are byte-identical to rev12.

## Verification
- Recomputed this session: rev13 `c2b8819c3d8f901fbc3aebad3bdb7725813afc67aed79fc7c3ce99e3b81022fd` · rev12 `b82cf76e…` (superseded, named in the header) · m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` (`:472` parks S-4, `:474` rebase-before-join, `:299`/`:473`/`:511` receipts — all read directly) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED.
- The over-broad claim "S-1/S-2/S-4/S-5 are EXACT-FOLDED at pair-approved m-9 r12 bytes" is removed (grep 0); S-4 now reads parked-in-r12, S-5 bound to the master/pair records.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260724-200000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev13 provenance edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260724-200000; no frozen design byte moved, no label/mechanism/fixture byte changed, no `frank/` action, no lock, no amendment authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews rev13; on pair-approval this is the exact-folded (S-1/S-2) / pair-settled (S-4/S-5) §D producer half master carries into the bounded §D-settlement amendment → VP → operator, S-4/S-5 normative only after m-9's rebase/fold + the §D join co-sign, S-3 tracked on its own status.
