## DESIGN — producer delta rev12 @ `b82cf76e…` folds M10-DAG-R11-F1 (verified at rev11 line 110): the incomplete sweep recurred at the GREP-PATTERN level — my rev11 sweep matched "NORMATIVE at this revision"/"all SETTLED" and missed the synonym "normative now", leaving a third S-5 site; fixed with the m-10-local recipe named normative and the joint carrier excluded; §9 delimiter fixed; swept by CONCEPT this time

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — the last live settlement-status assertion + one mechanical markdown fix; no mechanism or product choice changes
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r11
RUN_ID: master
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-191500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` **rev12 @ SHA-256 `b82cf76edd0c0641fe0c9ca971b1b87ed0c00fc18c85d01a82815824c3be889f`** (rev11 `8a26fd01…` superseded); basis stays pair-approved m-9 r12 `04422965…`; frozen r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED

R11-F1 verified at rev11 line 110, real, and the interesting part is *why* my rev11 sweep missed it: I grepped the two exact phrasings I had used ("NORMATIVE at this revision", "all SETTLED and folded") and §5 said "**normative now**" — a synonym my pattern did not cover. The incomplete-sweep defect recurred one level down, at the grep *pattern* rather than the site list.

**The finding, fixed.** §5's S-5 `assign` carrier said "the producer contract (this recipe, this carrier) is **normative now**" — advancing the S-5 writer-to-reader contract before the same §D join §9 holds pending, and the parenthetical "this carrier" made it a joint-contract claim, not a local-recipe one. rev12 gives S-5 the same status as §9 (EXACT-FOLDED / JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D co-sign), and takes the separation you invited: the **m-10-LOCAL recipe** (run-start `workspace_root` input · realpath/NFC/SHA-256 derivation · the immutable §C.1 manifest pair) is named as the one independently-normative part, and the **joint `assign` carrier** (member encoding + worker recompute-verify seam) is explicitly EXCLUDED from that local claim and rides the join.

**The §9 markdown delimiter** (`join gate**.** Their`) is fixed to `join gate.** Their` — the mechanical defect you noted, not a semantic one.

**How I swept this time — by concept, not by my own prior phrasings.** I ran a synonym-inclusive pattern (`normative now|is normative|are normative|folded normative|now settled|all settled|are settled`) over the whole document and checked every hit: line 110 (fixed); line 30 (the composite gate's evidence property, "NORMATIVE regardless of the frame shape below" — an m-10-LOCAL property grounded in ratified amendment F105-D2-R11, explicitly decoupled from the joint frame, legitimately normative, left); line 97 (S-3's five-family descriptor encodings, the C-item, not under the §D join, left — you confirmed this at r10); line 156 (the B/E carriage discharge note — m-8/m-9 producer bytes settled, not a §D-seam claim, left). No live §D-seam normativity assertion survives.

**Everything else byte-for-byte.** The r12 basis, the widened complement-of-equivalence mechanism, the not-producible detector label, the local recipe/carrier BYTES, the R7/R8 folds, the D-4 restore, and every fixture are unchanged from rev11 — only §5's S-5 status wording and the §9 delimiter moved.

**Review ask:** confirm no live design assertion calls any of S-1/S-2/S-4/S-5 normative before the join (including §5's S-5 now); that the m-10-local recipe is separated cleanly from the excluded joint carrier; that line 30's gate property and line 97's S-3 are correctly distinct from the join gate; and that the mechanism and fixtures are byte-identical to the rev11 you passed.

## Verification
- Recomputed this session: rev12 `b82cf76edd0c0641fe0c9ca971b1b87ed0c00fc18c85d01a82815824c3be889f` · rev11 `8a26fd01…` (superseded, named in the header) · m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` (join gate at `:299`/`:473`/`:511`) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED.
- Concept-level normativity sweep re-run: the only live §D-seam status statements now read joint-pending (§2/§4/§5/§9); the surviving "settled/normative" hits are line 30 (local gate property, decoupled), line 97 (S-3 C-item), line 156 (B/E producer-bytes discharge), and the header's self-description — each verified not a §D-seam claim.
- `**.**` occurs once, in the header describing the fix; the live §9 delimiter is corrected.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260724-193000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev12 status-wording + delimiter edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260724-193000; no frozen design byte moved, no mechanism/fixture byte changed, no `frank/` action, no lock, no amendment authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews rev12; on pair-approval this is the exact-folded §D producer half master carries into the bounded §D-settlement amendment → VP → operator, with S-1..S-5 going normative only at the §D join co-sign after.
