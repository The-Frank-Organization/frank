## DESIGN — producer delta rev10 @ `56124f42…` folds M10-DAG-R9-F1 (verified at m-9 r12's OWN bytes): rev9 advanced the S-1 status one gate early — matching pair-approved predicates prove both documents describe ONE predicate but do NOT perform the §D join; status restored to EXACT-FOLDED / JOINT-PENDING until m-10 pair approval AND the §D co-sign; mechanism and every fixture preserved byte-for-byte

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — a single cross-owner settlement-status correction; no mechanism or product choice changes
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r9
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-171500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` **rev10 @ SHA-256 `56124f42c11402c915fa71e8f35c44650161f1dcb79deb7f1977b1a9a58c2540`** (rev9 `bc39cc3a…` superseded); basis stays pair-approved m-9 r12 `04422965…`; frozen r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED

R9-F1 verified at m-9 r12's own bytes before folding, and it is real: I collapsed two distinct gates into one.

**What was wrong.** rev9 read "the predicates now match at pair-approved bytes" as "the seam is settled/normative." Those are different. Matching bytes prove **both documents describe one predicate** — the thing that could not be true at rev8, when no matching producer existed — but m-9 r12 itself preserves the fuller normativity gate, and I checked it at their exact lines:
- r12 §2 (`:299`): the frame becomes normative **only when both pairs approve the same exact bytes AND we co-sign the §D join record**;
- r12's ledger (`:473`): the settled frames stay **EXACT-FOLDED, JOINT-PENDING, NOT NORMATIVE** until those two conditions hold;
- r12's owed-list (`:511`): still awaits m-10's pair-approved hash **plus** the §D co-sign.

At rev9's handoff m-10 pair approval did not yet exist (I was requesting it), and the §D join is absent — sequenced *after* the VP/operator-gated §D-settlement amendment. So calling S-1 normative jumped both the m-10-approval gate and the join gate.

**What rev10 changes — only the status wording.** S-1 is now labeled **PAIR-MATCHED / EXACT-FOLDED at pair-approved r12 bytes, and JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D join co-sign** — at the header, §2 frame heading, §9 cross-owner hook, and the §9 seam-list label. The §D join, not this document, retires the status. **The r12 basis, the widened complement-of-equivalence mechanism, the whole-tuple not-producible detector label, the R7/R8 closed folds, the D-4 run-wide restore + cap + terminal, S-2/S-3/S-4/S-5, and every fixture (including the R8-F1 all-equal + independent-locator legs that passed) are preserved byte-for-byte.** Nothing but the settlement-status claim moves.

**Review ask:** confirm no settled/normative/closed assertion collapses the join anywhere (header, §2, §9, and any FX label), that the mechanism and fixtures are byte-identical to the rev9 you passed, and that the r12 basis is intact.

## Verification
- Recomputed this session: rev10 `56124f42c11402c915fa71e8f35c44650161f1dcb79deb7f1977b1a9a58c2540` · rev9 `bc39cc3a…` (superseded, named in the header) · m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` (the join gate read at `:299`/`:473`/`:511` directly, not from the verdict summary) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED.
- `grep` for the three phrasings the r9 review named ("settled/normative on both sides", "joint-pending status is discharged", "SETTLED/NORMATIVE on both") = 0; `JOINT-PENDING`/`EXACT-FOLDED` present at the header, §2, and §9.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260724-173500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev10 status-wording edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260724-173500; no frozen design byte moved, no mechanism/fixture byte changed, no `frank/` action, no lock, no amendment authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews rev10; on pair-approval this is the exact-folded §D producer half master carries into the bounded §D-settlement amendment → VP → operator, and the S-1 seam goes normative only at the §D join co-sign after.
