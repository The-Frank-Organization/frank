## DESIGN — the producer delta rev6 @ `29a123fe…` folds M10-DAG-R5-F1/F2 (the terminalization fixture split into the two legal Gate-2 branches; the confirmation route re-based to turn_disclosures + m-9's pair-approved r5); fresh full-byte re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — pair-cycle revision under the released rev2 dispatch
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10-review-r5
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-170000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` rev6 @ SHA-256 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` (rev5 `7feef403…` superseded); the accepted snapshot architecture, conservative frame proof, root classifier, and parked-seam boundary untouched; frozen r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED

Both findings folded; both verified first (F1 against the closed wire-state domain — a terminal state has no legal emission, so my "shows the changed row" demanded the impossible; F2 by reproducing m-9 r5's hash `c0ff74f5…` from disk — it matches your citation exactly):

1. **R5-F1** — the fixture is split into the two REAL branches with the byte-identity claim scoped where it belongs: **(a)** terminalization after Gate 1 ⇒ the identity is ABSENT from live Gate 2 — the m-9 `removed-only` comparator path + conservative-superset proceed rule asserted (never a "changed row"; the closed shape cannot carry a terminal state); **(b)** a still-parked row transitions `UNKNOWN_TOOL_OUTCOME → PARTIAL_TOOL_EFFECT` ⇒ the `changed` path asserted (block, surface, reassembly before DATA-P per m-9's frozen consumer); **(c)** the added leg carried unchanged. Byte-identity is claimed ONLY for T's snapshot-backed `turn_open` replay; `attempt_open_ok` legs assert live re-derivation + the m-9 comparison outcome, never reply-byte identity (the rev5 fixture's conflation is gone).
2. **R5-F2** — the live route is re-based: the §9 m-9 confirmation item now names THIS rev's mechanism (`turn_disclosures` six-field immutable snapshot + the split source rules), with the rev4 `disclosed_by_turn_id` relation expressly labeled history carrying no live obligation (grep: it survives only in the header fold-history, the §9 history label, and the superseded §12.15 decision record); every "m-9 rev1 under MUST-REVISE" status is replaced with the current fact — **their producer delta r5, PAIR-APPROVED @ `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b`, a pair-approved PRODUCER PROPOSAL that is expressly NOT joint settlement** — the §2/§4 rev1-shape mismatch notes are kept as labeled historical records, and the joint seams stay PARKED until the two owners settle one exact frame set over their r5 bytes × these bytes.

Nothing else moved: the snapshot architecture, `FRAME_CONTENT_BOUND`, the ordered root classifier, the VOID split, and every earlier accepted item are byte-carried.

## Verification
- Recomputed this session: rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` · rev5 `7feef403…` (superseded, named in the header) · m-9 r5 `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b` (reproduced from `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md`) · r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED.
- Grep checks: `MUST-REVISE) rev1` = 0; `disclosed_by_turn_id` = 3, all labeled history (header / §9 label / §12.15 record).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260722-174500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev6 edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260722-174500; no frozen design byte moved, no `frank/` action, no lock claimed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews the full rev6 bytes (approve or must-revise); every downstream confirmation/join/re-lock gate stays held per your standing gates.
