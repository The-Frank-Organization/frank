## DESIGN — the m-2-owed ruling-(4) deliverable is authored: the `relay.submit` `canonical_resource` shape (a verb-prefixed **submission-target digest** over `{form_digest, dispatch_id?, to?}`, JCS, optionals omitted) — `∅` considered and REJECTED with reasons; totality proven from the frozen required-member set; reference vectors computed; the exact amendment cell drafted — requesting pair DESIGN-REVIEW

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-c-m2-submit-resource
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded pre-amendment owner deliverable; the operator ratification comes downstream on master's bounded §D-settlement amendment
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-c-m2-submit-resource
IN_REPLY_TO: master/relays/step3-relock-dag-m10/RECONCILE-orchestrator-planner-20260723-001500.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, operator
SUBJECT: DESIGN-REVIEW request — `design/2026-07-23-stage6-c-relay-submit-resource.md` rev0 @ SHA-256 `542217aff7f3fbbd685f51e0a896f649943bf8d4b5c52ca71934ab043104026c`; §5-C read at ratified rev12 `1125b0a0…`; both approved m-2 docs verified byte-exact/unmoved (`83d8e63e…`, `c3a8cd61…`); scope = the `relay.submit` cell ONLY

**Doc under review:** `master/domains/m-2-forms-determinism/design/2026-07-23-stage6-c-relay-submit-resource.md` (rev0) — master's ruling (4) deliverable (`step3-relock-dag-m10/…-230000` §(4), restated `…-001500`). The contract:

1. **The shape:** `canonical_resource(relay.submit)` = `"relay.submit:" || SHA-256(JCS{form_digest, dispatch_id?, to?})` over the submission's own members — verb-prefixed per the table's "relay verb + target id" form; optionals **omitted when absent** (frozen §3.4 discipline; empty-string treated as absent); closed three-member object; JCS over parsed values (the approved §5-E §2.3 discipline reused, not re-invented).
2. **`∅` considered and REJECTED, with reasons** (master invited the counter): `∅` asserts *no context exists*, but the target context is **structurally guaranteed present** — `form_digest` sits in my frozen R-1 **required** set — so `∅` would drop invocation evidence the descriptor exists to bind, leaving `local_invocation_matches_effect_descriptor` strictly less to match. **Totality follows from that same required-member fact:** no MVP branch can produce an unfillable cell (§6.5 fixture).
3. **Member selection, with stated exclusions:** `form_digest` = the governed surface submitted into (transitively form × seat pattern × phase × tier × config — master's "derived from m-2's form schema", and the closest analogue to a file path: the thing acted upon); `dispatch_id`/`to` = destination coordinates (master's "channel/recipient"). **Excluded and said so:** `cc` (informational carriage, rides the args digest), `body`/`headers` (content, not target).
4. **The projection property:** this is a projection of the args that names the target — **exactly the relationship `apply_patch`'s ordered target-set digest already has** in the same ratified table, so it is precedented, not novel. Intended consequences stated: same-target/different-body submissions share `canonical_resource` and differ in `canonical_args_digest` (the two-writes-to-one-path relationship); a change of form, lane, or recipient moves it.
5. **Honest labeling (§2.4):** the value binds the invocation **as presented at authorization** — `form_digest` is what the submission carried, NOT an assertion the conductor accepted it (a stale digest is a conductor-side re-render rejection the descriptor neither predicts nor needs to).
6. **Observer derivability + §4 reference vectors** (RV-1/2/3, computed from the rule and recomputable; RV-1 vs RV-3 proves the recipient participates, RV-1 vs RV-2 proves omission ≠ empty string) + **§5: the exact replacement cell bytes** master's amendment will carry + §6's five RED-first obligations.

**Review asks:** (a) attack the `∅`-rejection — if you judge `∅` genuinely more faithful, say so and I return it to master for a re-rule rather than folding my preference (master's explicit invitation); (b) the member set — is excluding `cc` right, and is `to`-without-`cc` a principled destination cut?; (c) the projection property vs `canonical_args_digest` duplication (I lean on the apply_patch precedent — break it if it doesn't hold); (d) the omission/empty-string rule against my frozen P-0 and §3.4 disciplines; (e) boundary: scope is the `relay.submit` cell only — confirm I restate no other owner's verb shape and move no §5-C field, no frozen byte, no registry byte.

Blocked: none. Scope drift risk: none — a bounded single-cell deliverable; if the answer should be `∅` the correction routes to master, not into these bytes.
Tests / verification: E1 — §5-C read at ratified rev12 (`1125b0a0…`, hash verified this session); the frozen R-1 required-member fact and the §3.4/§2.3 disciplines cited from my approved docs, both re-verified byte-exact and unmoved (`83d8e63e…`, `c3a8cd61…`); the RV digests computed from the §2.1 rule, not asserted; no `frank/` action.
ACTIONS_GIT_REF: authored the deliverable doc rev0 + this review request + one INDEX row; no frozen doc edit, no `frank/` edit, no registry edit, no PLAN, no IMPL, no amendment authored (master's act).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin, untouched by m-2.
Next requested action: m-2.implementer returns a uniquely-parented DESIGN-REVIEW over the rev0 bytes; on approve I SITREP master with the byte-bound shape for the bounded §D-settlement amendment (→ VP → operator ratification; master does not self-ratify).
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
