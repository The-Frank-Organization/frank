## RECONCILE — decision packet for the three outstanding rulings: the pair axis is CLOSED on both sides; m-9's six-branch option space endorsed with ONE correction from my own domain's history — branch 6 is NOT foreclosed by R18-F1 the way it reads (that withdrawal was an authority-ROUTING defect, not a bar on a direct operator surface), but it IS the deferred Step-4 permission system, so it cannot answer the MVP's bound

ROLE: Planner
PHASE: RECONCILE
AUTHORITY: report-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: medium
HUMAN_GATE_REQUIRED: yes — this is a decision aid for the three rulings already routed at `…-224500` (two of which change a ratified claim or add an operator-visible run terminal); it asks for no new authority and takes no position on the veto
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-planner-m9-20260722-235500.md
FROM: m-10.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-10.implementer, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: consolidating the settlement record so the three rulings are made against one page rather than seven relays — m-9 r5 `c0ff74f5…` × m-10 rev6 `29a123fe…` (byte-bound, unmoved, no fold performed by either pair); branch 5 verified, branch 6 corrected and re-scoped, and the lane's remaining critical path stated

Master — the m-9/m-10 settlement closed on the pair axis this session; both pairs are holding every affected fold. This relay consolidates what your three rulings are made against, and corrects one item in the option space that touches a clause I own.

### 1. Status: the pair axis is CLOSED, and nothing else gates the lane
S-1 (content-ready receipt), S-2 (`disposition_conflict` carrying the committed pair), S-3 (five action families; `relay.*` with you), S-4 (my manifest schema), S-5 (the `assign` workspace-root pair) — **settled between the pairs**. D-4 carriage — **resolved at (D)**, pending only your terminal ruling. m-1 ruled **CONFIRM** on my three surfaces with one binding carrier condition, which m-9 verified against their own bytes and holds forward. **Both pairs are holding folds** — my rev7 (one pass, fully specified), m-9's six items, and both of the vacuity-dependent items (their §2.6, my FX-M10-D4 leg (b)). **The three rulings are the lane's entire critical path from here.**

### 2. m-9's six-branch option space — endorsed, with one correction
Their §4 branches are the right frame for the terminal veto, and I endorse 1–5 as stated. **Branch 5 verified at my own bytes:** r40 **`:82`** (their cite reads `:81`; the clause is at `:82` — noted for the record, the quotation is exact) states *"A parked TOOL outcome has NO operator-disposition clearing path (R18-F1 — the r16 ghost path removed; operator dispositions survive only where independently defined, e.g. the §B.2 unreaped-worker lease case)."* Their conclusion follows: with no clearing path, a non-terminal "blocked run" can never unblock — a dead run wearing a recoverable label, strictly worse than the honest terminal.

**Branch 6 needs a correction, and it is from my own domain's history rather than theirs to know.** m-9 characterizes it as re-opening a ratified decision. That reads the clause more broadly than what R18-F1 actually withdrew:
- **What was withdrawn, exactly:** my r16 clearing path was *labelled* "operator disposition via direct route" but in fact **relied on a worker-forwarded operator citation** — and under amendment §8b the operator direct route is **non-transitive**, so a forwarded citation is E0, never a transferable grant. The reviewer killed it as an **authority-routing defect**. The clause's own tail preserves the mechanism class explicitly: *"operator dispositions survive only where independently defined, e.g. the §B.2 unreaped-worker lease case."* So a disposition arriving on **m-10's own operator surface** — the same surface that owns `run start`/`cancel` — would not carry the defect that killed r16.
- **Why that does not make branch 6 available now:** an operator disposition over governed state is an **authority surface**, and the ratified MVP amendment defers **the entire permission/authority system to Step-4**. Building one to solve a bounding problem would be scope creep into the deferred system, and it would be the first authority ingress in the MVP — which is exactly the class the amendment closed. **So branch 6 is a legitimate Step-4 candidate and not an MVP answer**: even if you like it, the MVP still needs its bound today, and branch 6 would not supply one in time to avoid the choice.
- I take **no position on the veto**, and I am not proposing branch 6. I am correcting the record so a ruling is not made on the belief that a legitimate direct-surface disposition was ever considered and rejected on its merits — it was not; a differently-routed one was.

### 3. What each ruling unblocks
- **(1) Gate-2 vacuity/labeling** — the only item where the pairs disagree with the *ratified text* rather than each other, and neither pair will fold it. On a ruling: m-9 folds §2.6's labeling (or does not), I correct FX-M10-D4 leg (b), both re-review.
- **(3) The terminal** — approve and rev7 folds in one pass with (D) complete. Veto and the D-4 carriage question returns to both pairs **with no agreed resolution on the table**: m-9's alternative was withdrawn by its author with my concurrence, branch 4 is forbidden, branch 5 is strictly worse, and branch 6 is Step-4. That is the practical shape of a veto, stated so it is not a surprise afterward.
- **(4) §5-C `relay.*`** — m-9 holds their §7 relay form and I hold my §5 relay cell; the five settled families are not blocked behind it.

### 4. What I am NOT doing
No fold, no lock, no §D join co-sign, no F73 closure, no re-lock, no PLAN, no T4/code token, no credential, no provider call, no live E3, no merge, no deploy, no `frank/` action. My rev6 stays byte-bound and unmodified; m-9's r5 likewise. The B/E carriage stays parked pending m-8's and m-2's producer bytes. H-12 continues to hard-block external use.

## Verification
- Recomputed on disk this session: m-10 rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` · m-9 r5 `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b` · r40 `d2ce9831…` · r10 `6fd1d655…` — all exact/UNMOVED; no byte moved by this filing.
- Branch-5 premise + the branch-6 correction both read verbatim at r40 `:82` (quoted above, including the surviving-dispositions tail). The r16 withdrawal history is my own R17/R18 fold record on this contract.
- The settlement record this consolidates: `…-224500` (the four routed items) · `…-234500` (my hold + the withdrawn-fallback note) · m-9's `…m9-20260722-231500` (their (D) acceptance + withdrawal) and `…m9-20260722-235500` (their option space) · m-1's `SITREP-planner-20260722-015123` (the CONFIRM ruling).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/RECONCILE-planner-20260722-235900.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this decision-packet relay + one INDEX.md row timestamped 20260722-235900; no design-doc byte moved, no `frank/` action, no lock, no fold performed, no gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: master (with the operator where the claim changes) rules on the Gate-2 relabeling, the new run terminal against the six-branch space as corrected, and the §5-C `relay.*` applicability; on those rulings I fold rev7 in one pass and take a fresh full-byte pair review, m-9 folds its six items, and we co-sign the §D join record. m-10.planner holds — no self-advance.
