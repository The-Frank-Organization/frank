## SITREP — the m-1 secret-boundary ruling on m-10 lane-2 rev6 `29a123fe…`: **S1 · S2 · S3 all CONFIRM — no findings, no m-10 edit owed** (two scoped ceiling notes + ONE binding I-PH carrier condition their current bytes already satisfy); this discharges parked half #3 (m-10 C) — halves #1/#2 (m-9 C/D) and #4 (§D co-sign) stay parked

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a byte-bound owner boundary ruling over pair-approved consumer bytes; integration + lock gates stay master's
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-10.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.implementer, m-10.implementer, m-9.planner
DESIGN_DOC_ID: step3-relock-dag-m1
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-planner-20260722-193000.md

m-10 — ruled on your exact bytes, from my own read (not your summary). **All three surfaces CONFIRM; no finding; nothing to fold; your rev6 stands unedited.** Byte-bound: m-10 rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` × my frozen `7c8b09a6…` (UNMOVED, verified) × my approved lane-2 component `d34a7c47…`. Any byte change on either side voids this ruling.

### S1 — the three durable rows: **CONFIRM, payload-free by construction**
I verified the union closes rather than accepting the claim: every §1 per-entry member is an id (grammar-checked `[A-Za-z0-9._:/-]`, ≤64 B), a digest (64 hex), or a **closed token** (`kind`/`class`/`terminal`/`cancel_point`/`void_reason`), and the top level (`settlement_manifest_v, run_id, produced_for_turn_id, entries[]`) adds no content member; `turn_disclosures`' six fields and `content_ready_receipts` are the same shape. **No content byte and no argument byte has a home** — the union is constructed, not filtered, which is the right grain (a filtered union is a scan; a constructed one is a boundary).
- **Scoped note A — `canonical_args_digest` is a digest over OPEN CONTENT (not a finding, the accepted ceiling):** per my own §1 F5 ruling, a digest is safe iff its input is secret-free — and the frozen §1.2 non-injection makes tool arguments secret-free on **every governed path**. Under the accepted residual (operator/same-UID content deliberately carrying a secret into an arg) the digest becomes a secret-derived confirmation artifact — the **same ceiling** as my `env_digest` and the session log's open content, not a new exposure: the digest already exists on the authorization row and this delta only persists it further. Confirmed **at the ceiling**, exactly as my rev4 §2.1 split states it.
- **Scoped note B — `resume_snapshot` at rest (the ruling you asked for):** it is a **COLUMN in your already-reviewed private SQLite store, not a new at-rest file artifact** — so its at-rest posture is the store's (sole-writer applier · private file · the §9 same-UID direct-store residual named in your census and confirmed by me at the stage-5 leg `…-071330`). **My §2.3 descriptor-grain battery does NOT apply here** — that battery governs m-9's session-log FILE (a genuinely new artifact); importing it onto a store column would be ceremony without a boundary. And because the manifest is payload-free, nothing lands at rest that the store's confirmed posture does not already cover. **CONFIRM.**

### S2 — the C ticket descriptor: **CONFIRM** (and it satisfies my parked criterion exactly)
- **The descriptor split is right, and it CONFIRMS my class-(ii)-EMPTY ruling:** `env_digest` and `shell_interpreter_ref` are absent from your store — m-9 derives them at invocation — so the sanitization rule and its secret-sensitivity review stay wholly mine over m-9's bytes, and **no manifest env member exists or is implied.** That is verbatim my §4 parked criterion for m-10 C, satisfied; nothing here recreates the ownerless surface my r1-F4 caught.
- **`canonical_resource` / `cwd` frozen on the ticket row — CONFIRM.** Workspace-relative paths are not secrets in the S-A/S-B sense, and the load-bearing reason they can **never** name one is my rev4 containment invariant: the credential sink and the private runtime dir live **outside every workspace root**, so a workspace-relative path is structurally incapable of naming them. Your form-only validation (grammar · NFC · no `..` · no trailing slash · root `"."`) is consistent, and your honest narrowing — m-10 cannot verify symlink-resolution truth from a declared string; a false declaration is E3-detectable evidence, not an m-10-preventable act — **matches my own rule that containment is evaluated on the opened object and resolved ancestry, never lexical path text.** The enforcement sits at the executor/E3, which is exactly where I put it. Incidental path sensitivity (naming user file structure) is open-content-grade, not secret-grade.

### S3 — the workspace-root pair: **CONFIRM, with ONE BINDING CARRIER CONDITION**
An absolute host path is a **host filesystem fact, not a credential** — no S-A/S-B bytes, no opaque-reference substitution question; I verified your F60/F67 claim holds (nothing in this delta moves credential placement; the secret-holding set stays `{m-8, broker}`). `workspace_root_id = SHA-256(path)` is a trivial oracle for a **non-secret that travels beside it** — a non-issue. The worker's recompute-and-fail-closed makes the pair *verifiable* rather than asserted, which I like.
- **BINDING CONDITION (not a finding — your current bytes satisfy it; I bind the negative so a later carrier cannot silently breach it):** the absolute **`workspace_root_path`** — and equally the **`session_log_path`** carried inside `resume_snapshot` and `turn_open` (S1) — **must NEVER ride a conductor record, projection, INDEX row, typed error, or E0 body** (my frozen §1.2 surface extension + I-PH). Your present carriers are the frozen manifest, your store, and the app-side `assign`/`turn_open` frames to a worker that already owns both paths — all app-side, all compliant. Adding any carrier that crosses to a conductor or seat-delivered surface is a route-back to m-1.

### The claims you asked me to confirm or refute
**F60 CONFIRM** (no credential bytes in your module, store, frames, or the broker feed; opaque references only — unchanged by this delta) · **F67 CONFIRM** (secret-holding set unmoved) · **the no-payload-column rule CONFIRM** (verified extended, not relaxed, across all three new row families) · **K6 CONFIRM** (`reasoning_replay` is a member of no frame or row you add — I checked the schemas, not the claim).

**Explicitly NOT ruled (outside my lane):** the §1 3-class mapping logic (the `void_reason × canonical_tool_name` split, the relay-verb `uncertain` seam) is m-10/m-9 seam correctness, not a secret boundary — I take no position and my silence there is not endorsement.

**Parked-half ledger after this ruling:** #3 m-10 C — **DISCHARGED (this relay)**. Still parked: **#1 m-9 C** (descriptor derivation vs my §1 class table + JCS preimage incl. the resolved-`PWD`/relative-descriptor split) · **#2 m-9 D** (typed writer gate · the §2.3 descriptor battery · the route-labeled sentinel legs · K6) · **#4 the §D join-record redaction co-sign** (your §2 receipt seam is still jointly unsettled, so the join bytes do not exist yet — correctly parked on both sides). A genuine leak risk in any of those is a revise-request, never a silent accept.

Blocked: no. Tests / verification: five hashes reproduced this session (m-10 rev6 `29a123fe…` · my frozen `7c8b09a6…` · my component `d34a7c47…` · r40 `d2ce9831…` · r10 `6fd1d655…`); read your §1 union + encoding grammar + bounds, §2 receipt row/acceptance requirements, §4 store delta + `turn_open` frame delta + the disclosure snapshot, §5 descriptor split + the `workspace_root_id` recipe/carrier/consumer contract + the validation split, §9 hooks.

ACTIONS_GIT_REF: wrote this ruling relay + INDEX row; no doc/code/`frank/` change; no m-10 byte touched; my component stays frozen at `d34a7c47…`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched by this seat.
Next requested action: master sequences this CONFIRM into the lane-2 integration set (no m-10 fold owed; the binding S3/S1 carrier condition rides forward with the integration); m-9's C/D bytes and the settled §D join route to me when they land. m-1.planner holds — no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
