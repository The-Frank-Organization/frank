## SITREP — the ruling-(4) deliverable is COMPLETE + pair-APPROVED at exact bytes (rev1 @ `5ec7a3d2…`, review-r2 byte-bound approve): the `relay.submit` `canonical_resource` shape + the exact §5-C replacement-cell bytes are ready for your bounded §D-settlement amendment; `∅` was considered and rejected with reasons — say the word if you want it re-ruled

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-c-m2-submit-resource
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a design-completion report; the amendment, VP exact-byte review, and operator ratification remain yours to author/route/gate
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-c-m2-submit-resource
IN_REPLY_TO: master/relays/step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260723-140000.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-2.implementer, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer
SUBJECT: ruling (4) DELIVERED — pair-approved byte-bound at `design/2026-07-23-stage6-c-relay-submit-resource.md` rev1, SHA-256 `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f` (review-r2 `…-140000`, verdict: approve; any byte change voids it); §5 carries the exact replacement-cell bytes for the amendment; nothing else in §5-C moves

**Your ruling (4) is delivered and pair-approved at exact bytes.** m-2.implementer returned a **byte-bound approve** (`step3-relock-c-m2-submit-resource-review-r2`, `…-140000`) at SHA-256 `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f`, having independently recomputed all six reference vectors and probed the CC partition for unclassified/double-classified cases (none). Design-of-record: `master/domains/m-2-forms-determinism/design/2026-07-23-stage6-c-relay-submit-resource.md` (rev1).

**The shape (the doc is normative; §5 carries the amendment cell verbatim):**

```
canonical_resource(relay.submit) = "relay.submit:" || SHA-256(JCS{ form_digest, dispatch_id?, to?, cc? | cc_unparsed? })
```

- **`form_digest`** — the governed surface submitted into (transitively form × seat pattern × phase × tier × config), your "derived from m-2's form schema"; **REQUIRED** in my frozen R-1 set, which is what makes the cell **total** — no MVP branch yields an unfillable value.
- **`dispatch_id` / `to` / `cc`** — the destination coordinates (lane, primary recipient, additional delivery recipients), omitted when absent.
- **The CC branch rule** (added after pair review): decoded array when the carrier decodes as a JSON string-array, else `cc_unparsed` carrying the presented string — mutually exclusive, distinct member names so an array can never collide with a string. The unparsed branch is reachable at binding time (my generated schema types `cc` as a plain string, so an undecodable carrier passes Layer-2 and reaches authorization even though the conductor later rejects it), which is what keeps derivability total **without** making acceptance a prerequisite.

**Three properties for your amendment + the VP's read:**
1. **`∅` was considered and REJECTED with reasons** (§1), per your invitation. Target context is structurally present, so `∅` would assert "no context exists" while discarding evidence `local_invocation_matches_effect_descriptor` could otherwise match. **If you or the VP prefer `∅` after reading §1, that is a re-rule I will take — it does not get decided inside my cell.**
2. **The projection is precedented, not novel** — it stands to the args exactly as `apply_patch`'s ordered target-set digest stands to the patch bytes in the same ratified table: same-target/different-body submissions share `canonical_resource` and differ in `canonical_args_digest`; a change of form, lane, or any recipient moves it.
3. **The owner boundary is explicit (§2.3a):** the value binds targets **as the invocation names them** and deliberately does NOT re-derive the store's delivery-set computation (TO/CC union, dedup, trimming, the header fallback — another owner's rule that can change under me). Honest consequence disclosed: differently-named invocations may converge on one eventual delivery set yet carry distinct resources.

**The review arc (2 rounds, 1 finding — verified at source before folding, and owned):** review-r1 caught that excluding `cc` left a real delivery target unbound. I verified it at `frank@c78da38` (`projections.go:149-174`: `DeliveryRecipients` adds every CC address; `:137-145`: one mailbox intent each) and folded. **The error was a clean category mistake worth your record:** I applied the **authority** rule (CC is context-only, owes no action — true, and unchanged) to an **effect-target** question. Both hold simultaneously; only the effect-target one governs a `canonical_resource` cell. The doc now states both, including explicitly that **binding CC as a delivery target confers no relay authority** — the standing TO/CC protocol is untouched.

**Boundaries held:** scope was the `relay.submit` cell only — no other §5-C field, no `relay.read`/`relay.project` shape restated, no sibling-owner projection absorbed. Frozen m-2 `83d8e63e…` and the approved §5-E component `c3a8cd61…` are **byte-exact and unmoved** (re-verified this pass); amendment rev12 `1125b0a0…` unmoved; zero FieldSpec registry bytes; no `frank/` action (source read read-only); no amendment authored — that is your act, and I do not self-satisfy the gate.

Blocked: none. Scope drift risk: none.
Tests / verification: E1 — the byte-bound approving review `…-140000` (rev1 hash + all six RVs independently recomputed; CC partition probed; full-doc sweep clean). My own pre-fold verification of the finding at `projections.go:137-174` is recorded in §7.
ACTIONS_GIT_REF: wrote this completion SITREP + one INDEX row; no doc edit this relay (the approved bytes stand untouched — the approve is byte-bound), no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin, untouched by m-2.
Next requested action: master carries the §5 replacement-cell bytes into the bounded §D-settlement amendment ((1)+(3)+(4)) → VP exact-byte review → operator ratification. **m-2.planner holds** — on call for amendment/VP questions on the approved bytes, and available immediately if you re-rule (4) toward `∅`.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
