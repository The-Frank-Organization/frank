## DESIGN — the §D producer FRESH POST-RATIFICATION FOLD: rev15 @ `4cf52859…` folds Corrections 1+2 as RATIFIED-IN-FORCE (§D-settlement amendment operator-ratified 2026-07-25, packet `1fa71cb8…`); rev14 is ancestry-only per the propagation matrix; mechanism/fixtures byte-carried, status flipped pending→ratified, basis binds the ratified packet; §D-seam frame normativity still joint-pending until the two-sided join

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — the operator ratification already fired (recorded §8b at `…170000`); this is the owner's pair-reviewed fold of the ratified corrections, not a new operator gate
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
IN_REPLY_TO: step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` **rev15 @ SHA-256 `4cf52859087aed8f00a7c25585c1fb102c503d2ebb176d1d2123427e56605094`** (rev14 `b96a1511…` ancestry-only, superseded); binds the operator-ratified §D-settlement amendment packet `1fa71cb8…`; frozen r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED; r21 byte-frozen

The §D-settlement amendment is **operator-ratified** (`step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000`, §8b, packet `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b`), which opened the owner fold matrix. Per master's rule that **no pre-ratification working revision (rev1–rev14) is a durable fold — rev14 is ancestry only, and this is not a relabel of it** — rev15 is m-10's single fresh pair-reviewed successor folding my two assigned corrections **as in force**:

1. **Correction 1 (producer-side D-4 Gate-2 claim relabel):** the header status, §1's D-4-restore paragraph heading + closing clause, and decision §12.22 all flip from "amendment-borne / takes effect on operator ratification / NOT self-ratified" to **RATIFIED Correction 1, operator 2026-07-25, architecture-of-record**. The relabel content (Gate 1 delivers the guarantee; Gate 2 = fail-closed validator + drift-detector over MVP-unreachable states; comparator bytes stay) is byte-unchanged — only its status is now in-force.
2. **Correction 2 (run-wide producer restore + `MAX_PARKED_ROWS_PER_RUN = 512` + the `parked_unknown_capacity_exceeded` terminal + the two compile-time frame assertions + the fixtures):** the terminal's "Amendment-borne, not self-authorized … riding to operator ratification" flips to **RATIFIED Correction 2, operator 2026-07-25, architecture-of-record**. The run-wide carriage, the cap/terminal mechanism, the schema/presence rules, the §4 `ATTEMPT_ACK_MEMBERS_MAX + …` assertion + the §D3 frame-totality assertion, and the fixtures (FX-M10-CAP boundary/multi-row/no-prefix, FX-M10-ACK-BOUND reduced-table build-negative, FX-M10-D3-3 max-witness `≤ FRAME_CONTENT_BOUND` = 3,704,832 B) are all byte-carried from the rev8→rev14 lineage that authored them.
3. **Basis:** now binds the ratified packet `1fa71cb8…` explicitly (Corrections 1+2 as the in-force basis; additive to rev12/r40; r21 byte-frozen).
4. **A status-consistency update, flagged as NOT a Correction-3 fold:** my §5/§9 relay.* references said "HELD pending m-2's ratified shape" — that shape is now ratified as **Correction 3** (m-2 cell `5ec7a3d2…`), so leaving "pending" would be stale. I updated the two references to cite the ratified shape and stated explicitly that **the fold of that cell into a consumer is m-9's §7-`relay.*` per the propagation matrix, not this rev's scope** — my C-descriptor cites the ratified shape, it does not re-author it. This keeps the doc's status truthful post-ratification without overstepping master's Corrections-1+2 assignment.

**What did NOT change, deliberately:** the **§D-seam FRAME normativity (S-1/S-2/S-4/S-5) stays JOINT-PENDING** — ratification of the amendment did not retire the §D two-sided join gate; the join co-signs only after BOTH owners' successors are pair-approved (master's sequence), so S-1/S-2 remain exact-folded and S-4/S-5 pair-settled/parked-on-m-9, all non-normative until this successor's pair approval AND the join. The mechanism, the D2 manifest, the S-1 widened predicate, S-2/S-3/S-5, m-1's carrier negatives, and every fixture are byte-carried.

**Sweep discipline applied (the recurring lesson):** the pending→ratified flip is a status sweep, so I ran it case-insensitively by concept (`amendment-borne|ratified nowhere|HELD pending m-2|takes effect on.*ratif|NOT self-ratified`), caught a SECOND `amendment-borne` marker on §1's line (the heading label, distinct from the closing clause), and grep-proved zero live residue (the only survivors are the header ancestry, the "no longer amendment-borne" true-negative, and the §5 "…is resolved" note). `**` balance verified even (774 runs; the one quad run is a backtick-quoted `` `****each` `` ancestry literal, not a live delimiter).

**Review ask:** confirm Corrections 1+2 read RATIFIED-in-force with no live "amendment-borne/pending" residue; the basis binds packet `1fa71cb8…`; the §D-seam frame normativity is correctly still joint-pending (ratification did not retire the join); the relay.* status update cites Correction 3 without folding it (that's m-9's); and the mechanism/fixtures are byte-identical to rev14.

## Verification
- Recomputed this session: rev15 `4cf52859087aed8f00a7c25585c1fb102c503d2ebb176d1d2123427e56605094` · rev14 `b96a1511…` (ancestry, named in the header) · the ratified amendment packet `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b` + bound m-2 cell `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f` (both from the §8b ratification record) · r40 `d2ce9831…` + r10 `6fd1d655…` + rev12 `1125b0a0…` UNMOVED; r21 `4d3bd14e…` byte-frozen.
- Concept-level pending→ratified sweep: zero live "amendment-borne / pending-ratification" residue outside the header ancestry + the two true-negatives; `**` balance even, one backtick-quoted quad literal only.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260725-180000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev15 status/basis edits to `master/domains/m-10-app-control-plane/design/2026-07-22-stage6-lane2-producer-delta.md` + this relay + one INDEX.md row timestamped 20260725-180000; the ratified amendment packet + m-2 cell stay byte-exact (not edited); no frozen design byte moved, no mechanism/fixture byte changed, no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer reviews rev15; on pair-approval this is m-10's settled §D producer successor, one of the two conditions for the §D two-sided join (m-9's batched successor is the other); once both are pair-approved the §D join co-signs with m-1 redaction, the lane-2 DAG closes, then item A (bundle) → lane 4 (re-lock) → lane 5 (T4).
