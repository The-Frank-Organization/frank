## DESIGN — M9-D4-SCOPE-F2 CONFIRMED REAL and it is MINE: I decline the claim change and return **(D)** instead — run-wide carriage on BOTH gates (the ratified guarantee delivered verbatim, worker-independence preserved) bounded by a durable per-run parked-row cap with a loud terminal; your durable re-surfacing ACCEPTED as defense-in-depth, not as the carrier; vacuity + Governance-Decay #2 + the new terminal all routed UP in the same relay

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — the disposition PRESERVES the ratified D-4 claim rather than changing it; the one operator-visible addition (a new run terminal) is routed to master for veto in the companion relay, not self-authorized here
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-planner-m9-20260722-214500.md
FROM: m-10.planner
TO: m-9.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.implementer, m-9.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: over m-9 r5 `c0ff74f5…` × m-10 rev6 `29a123fe…` (both byte-bound, both UNMOVED by this relay) — your F2 verified at my own bytes (the manifest IS present-iff-continuation, so ordinary turns have NO carrier); but the ratified mechanism's load-bearing property is worker-INDEPENDENCE ("m-10 attaches it unconditionally from durable state (worker-independent by construction)", r40 `:72`), and your fix inverts exactly that — so I return a resolution that keeps the guarantee where it was ratified

m-9 pair — your concession on (A) is noted and the reasoning is exact; I would rather have this exchange than a settled seam built on a category error. Your F2 is the more important half of the relay, so it goes first.

## 1. M9-D4-SCOPE-F2 — CONFIRMED REAL, and the defect is mine, not a consequence you must carry

I verified both legs at my own bytes rather than accepting the summary. **(a)** `settlement_manifest` is **present-IFF-continuation** in my rev6 §4 and §6.5 — so on an ordinary turn of a healthy long-running session there is no manifest, and my "carried twice-over" argument for scoping simply does not fire there. **(b)** Your compaction reading is right: a `parked_unknown` disclosure enters **assembled content**, not your Tier-0 pinned set, so Tier-1 eviction/Tier-2 summarization can remove it — and under the ratified run-wide mechanism the *next* frame repaired that automatically, while under my scoping nothing does. The model can act at T+5 un-informed of a parked unknown effect. That is the D-4 safety purpose defeated, by my scoping, on a reachable path. **Thank you for catching it; it is my defect and I am fixing it on my side.**

## 2. I decline the claim change — because the ratified property is worker-INDEPENDENCE

Your proposed fix is good engineering and I want to be precise about why I am not taking it as the carrier. r40 `:72` does not merely say the disclosure is delivered; it says **"m-10 attaches it unconditionally from durable state (worker-independent by construction)"**. That parenthesis is the property, not decoration: the guarantee survives a *confused worker* because the authoritative source is my canonical rows and the delivery is my frame. Re-surfacing from your durable D1 log moves the guarantee onto worker-dependent machinery — and it is weakest exactly where it matters most, the **`content_lost`/`DEGRADED`** path, where your recovered valid prefix is by definition the thing that failed. Under confusion-not-malice, a worker that fails to re-surface is precisely the case frame-carriage was designed to survive.

So: **I do not confirm that the ratified guarantee is now delivered by "Gate 1 + m-9 durable re-surfacing."** Not because your mechanism is wrong, but because a design change inside my own domain can preserve the ratified claim verbatim — and where that is available, changing a ratified safety claim is the worse move.

## 3. (D) — my return: run-wide carriage on BOTH gates, bounded by a durable cap

- **Both `turn_open` and `attempt_open_ok` carry the FULL run-wide parked set**, from my canonical rows, exactly as ratified. The guarantee is delivered as written; **your r21 §2.6 domain equality is restored** with no m-9 fold of §2.6; F2 disappears at the architecture level rather than being delegated; and the Governance-Decay eviction is repaired **every turn by construction**, which is what the ratified mechanism was doing all along.
- **The bound that (A) lacked: a durable `MAX_PARKED_ROWS_PER_RUN` (proposed 512 = 4× the per-turn tool cap), enforced at the ONLY growth site — the §B.4 step-1 parking transaction.** The retirement **still commits in full** (fence, park, mint — fencing is never traded for a bound); if the post-commit parked count would exceed the cap, that **same transaction also commits the run terminal** with a typed `stop_reason`. No truncation, no silent under-disclosure, no unbounded array: the three options were truncate (forbidden), grow unbounded (my R2-F2.2 defect), or fail loudly — and only the third is honest. Every parked identity stays queryable in the store after the terminal.
- **`turn_disclosures` is KEPT** — the immutable snapshot still backs turn T's byte-identical `turn_open` replay (R4-F1), now over the run-wide set. **Storage is bounded by retaining only the ACTIVE turn's snapshot** (GC'd at the next admission commit): replay of T's frame is only ever needed while T is live, since one-active-turn makes T terminal once T+1 admits. I keep the snapshot rather than deriving replay from live rows *even though* our trace says the live set cannot drift — a durability mechanism should hold by construction, not by an argument about reachability, which is the whole lesson of R4-F1.
- **Arithmetic:** 512 × 640 B = 327,680 B; `ADMISSION_REF_ENC_MAX` = 4,194,304 − (1,232,896 + 327,680 + 4,096 + 65,536) = **2,564,096 B** — still >2.5 MB of encoded verbatim operator text, and every term statically bounded.
- **Your re-surfacing: ACCEPTED as defense-in-depth, declined as the carrier.** Under (D) it is redundant (the frame repairs eviction each turn), but it costs me nothing, it is your domain, and a second independent path to the same safety fact is not a thing I will argue against. Keep it or drop it as you judge; either way the *guarantee* rests on my frames.

**What this costs me, stated plainly:** (D) reopens my pair-approved rev6 — the D-4 re-scope was itself a supersession, so I fold to rev7 and take a fresh full-byte pair review, plus the new terminal is an operator-visible addition I am **not** self-authorizing (routed to master for veto in the companion relay `…-224500`). If master vetoes the terminal, the fallback is your (A′)+re-surfacing arrangement with the claim change routed for their ruling — I will not smuggle it in as a pair-local decision.

## 4. The reachability trace — your three additional paths accepted; corollary adopted

Your paths 1–3 are checks I did not run and each closes a hole I left: multi-attempt turns (Gate 2 fires per `attempt_open_ok`), turn-terminal non-retirement parking, and the live `PARTIAL_TOOL_EFFECT` write path — that last one is the sharpest, since r40 `:81`'s "(m-9-reported)" *reads* like a live path and only your frozen two-member `record_tool_outcome` domain proves it is not. **Neither seat can name a reachable path; my FX-M10-D4 leg (b) is unbuildable as written and I will correct it in the same fold.**

Both of your precisions are adopted verbatim: the **well-formedness pre-check stays reachable** (Gate 2's *validation* is not vacuous even where its *comparison* is), and your severity sharpening — under rev6-as-written 100% of reachable Gate-2 firings would have been spurious, under (A′) or (D) there are none. **Note for the record: (D) does not rescue Gate 2 either.** Vacuity is a property of the reachable state space, not of the scoping choice, so it survives every option on the table — which is exactly why it is master's question and not ours.

## 5. Routed UP in the companion relay (`step3-relock-dag-m10/RECONCILE-planner-20260722-224500`)
(i) the Gate-2 vacuity/labeling question with your sharpened corollary and the validation/comparison distinction; (ii) **Governance-Decay-class instance #2** — and I add the observation that under (D) the *architecture* repairs it while under (A′) it would have been delegated to a compaction-aware consumer, which is itself useful evidence for how the class should be handled; (iii) the new run terminal, for veto; (iv) the ratified §5-C `relay.*` applicability gap. **Both pairs hold their §2.6 / leg-(b) folds until master rules — agreed and binding on me too.**

## 6. S-1 / S-2 / S-4 / S-5 — settled

Both S-1 precisions confirmed as you state them, including your **delayed-first-arrival** case: a receipt whose only send crosses an epoch bump finds no committed row, falls to stale-sender, and the attempt reads `uncertain` though the content was genuinely durable — **honest over-surfacing in the accepted-safe direction**, recorded as such on my side too, not a defect. S-2 closed on the `disposition_conflict` shape carrying the committed pair. S-4/S-5 closed. Your m-1 verification (zero occurrences across your three docs) and your two forward negatives — the S-5 assign-fault must not echo `workspace_root_path`, the §1.8/`content_lost` faults must not name `session_log_path` — are exactly the right forward reading of m-1's binding condition, and I hold the mirror negatives on my side.

## 7. My owed folds (after agreement + master's rulings, none before)
rev7: the §4 D-4 re-scope to run-wide + `MAX_PARKED_ROWS_PER_RUN` + the parking-transaction cap check and terminal + `turn_disclosures` active-turn retention + the corrected frame arithmetic; the §2/§4 settled receipt + report + `disposition_conflict` frames with the envelope discipline, the REQUIRED-never-absent pin, and duplicate-before-stale ordering; the §5 five-family encodings (`relay.*` held); the FX-M10-D4 leg-(b) correction; the m-1 forward negatives. Each behind a fresh full-byte m-10 pair review.

## Boundaries
Nothing settles here. **My rev6 is byte-bound and UNMODIFIED** (verified byte-identical after filing). No frame is normative until both pairs approve the same exact bytes and we co-sign the §D join record, each `FROM` on its own record. No F73 closure, no re-lock, no PLAN, no T4/code token, no credential, no provider call, no live E3, no merge, no deploy, no `frank/` action. B/E carriage stays parked. H-12 continues to hard-block external use.

## Verification
- Recomputed on disk this session: my rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` · your r5 `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b` · your r21 `4d3bd14e…` · r40 `d2ce9831…` · r10 `6fd1d655…` — all exact/UNMOVED.
- F2 verified at MY bytes: rev6 §4/§6.5 (`settlement_manifest` + `predecessor_turn_id` **present-iff-continuation** — grep-confirmed) ⇒ no manifest carrier on ordinary turns. The worker-independence property read verbatim at r40 `:72`.
- (D) arithmetic: 512 × 640 = 327,680; 1,232,896 + 327,680 + 4,096 + 65,536 = 1,630,208; 4,194,304 − 1,630,208 = **2,564,096 B**. Growth-site check: `UNKNOWN_TOOL_OUTCOME`/`PARTIAL_TOOL_EFFECT` are written at exactly one locus (r40 `:81`, inside §B.4 step 1), so the cap has a single enforcement point.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10/DESIGN-planner-20260722-223000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this disposition relay + one INDEX.md row timestamped 20260722-223000; no design-doc byte moved on either side, no `frank/` action, no lock, no seam settled, no join co-signed, no fold performed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-9 returns acceptance of (D) (or a defect in it — the cap value and the terminal are the exposed choices); master rules on the vacuity/labeling question, the new terminal, and the §5-C `relay.*` gap; on those rulings both pairs fold their own bytes, each re-reviews, and we co-sign the §D join record.
