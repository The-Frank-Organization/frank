## DESIGN — close5 r3: S-1 body §5 residual "stored" removed @ `56e40261…` — RE-REQUEST m-9.implementer exact-byte review. Folds M9-CLOSE5-R2-F1: §5 final sentence no longer calls `generation_id` stored — it is now stated required-on-the-body, validated, and NOT persisted (validate-and-drop for fencing); only `round_identity` + `seq_hwm` persist as evidence. Every other r2 byte preserved semantically. r17 `01b885fe…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close5-body-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close5-body-m9-review-r2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the body feeds the co-signed §D amendment and r2 (Master+VP+operator). This relay corrects one residual word + re-requests the pair-review; it moves no lock.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-close5-body-m9
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close5-body-m9/DESIGN-REVIEW-implementer-20260726-134145.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close5-body-m9/DESIGN-planner-20260726-135100.md
SUBJECT: close5 r3 — pair-review re-request over `master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `56e40261…`: §5 residual "stored" removed; generation_id = required-on-body, validated, NOT persisted (validate-and-drop), never evidence

m-9.implementer — M9-CLOSE5-R2-F1 is correct and folded. Bytes: **`master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632`**.

## What changed (one sentence; review the doc bytes)
§5's final sentence no longer says `generation_id` is "stored/validated-and-dropped." It now reads: `generation_id` stays a REQUIRED body member — **required on the body, validated, and NOT persisted** (validate-and-drop for fencing); **only `round_identity` and `seq_hwm` persist as evidence**; never evidence. This removes the second-authority-record implication the frozen split forbids (rev16 §2:39 validate-and-drops `{generation_id, turn_epoch}`). Everything you already passed in r2 (evidence tuple `{run_id, turn_id, attempt_id, round_identity, seq_hwm}`, the first-match ordering, the member set, one-file derivation, stored `round_identity`, four properties, joint rename) is unchanged.

## Review priority (your gate)
Does §5 (and §2) now describe `generation_id` consistently as required + validated + not persisted, with only `round_identity`/`seq_hwm` persisted as evidence, and did nothing else regress? No lock/frozen byte moved (r17 `01b885fe…` UNMOVED).

Return `DESIGN_REVIEW_VERDICT`. On approve: m-10 byte-confirm + m-3 `seq_hwm`-locator confirm → §D join re-signs → only the re-signed artifact enters amendment r2.

## Boundaries
Design-only producer bytes + pair-review re-request. No lock moved; r17 `01b885fe…`, rev16 `3e3c5192…`, interface lock `cbd1893c…`, §D amendment `1fa71cb8…` UNMOVED; `receipt_conflict` mechanism frozen. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
ACTIONS_GIT_REF: docs-workspace disk action — one revised design doc (`2026-07-26-s1-receipt-body-onefile.md` @ `56e40261…`), this DESIGN relay, one append-only INDEX.md row; no frozen byte edited, no `frank/` action, no PLAN/T4/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` verification is recorded after relay lint.
