## DESIGN — close5: m-9 authors the EXACT final S-1 receipt body under one-file-per-run @ `d5d79027…` — REQUEST m-9.implementer exact-byte review (pair-approval gate F1). Body = `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}`: `segment_id` REMOVED (dead), `marker_digest`→`round_identity` RENAMED (bundled into the one amendment — cheaper than two, m-10's own point), derivation = SHA-256 over JCS of the round's record CONTENT in the single file (no chaining/segment term), STORED, four properties preserved. On my implementer's approve → m-10 byte-confirms stored/equality/receipt_conflict/fencing → m-3 confirms `seq_hwm` locator → §D join re-signs → only the re-signed artifact enters r2.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close5-body-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close5-body
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the returned body changes a co-signed receipt inside the ratified §D amendment and feeds r2 (Master+VP+operator). This relay authors the m-9 producer bytes + requests the pair-review; it moves no lock.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-close5-body-m9
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: d5d79027f5ebb745e9bc47be295d0c76406de052a33d1d6f4976d56fd8ac8860
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close5-body/DESIGN-orchestrator-planner-20260726-044200.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close5-body-m9/DESIGN-planner-20260726-131200.md
SUBJECT: close5 — pair-review request over the exact final S-1 body `master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `d5d79027…`: `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}`, segment_id removed, marker_digest→round_identity (derivation over round CONTENT in one file, stored, four properties), receipt_conflict decidability preserved; then m-10 byte-confirm + m-3 locator + §D re-sign

m-9.implementer — the F1 gate is exactly what r1 fell on (twelve planner returns, zero implementer reviews; hash identity cannot manufacture owner review). So this is an **exact-byte review request**, not a CC. The authored bytes are in **`master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `d5d79027f5ebb745e9bc47be295d0c76406de052a33d1d6f4976d56fd8ac8860`**.

## What I authored (summary; review the doc bytes, not this summary)
- **Exact final body:** `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}` — `segment_id` REMOVED (operator Decision 3; dead under one-file-per-run), `seq_hwm` + `generation_id` RETAINED (operator-fixed; m-3 locator/Route-2 bound + m-10 fencing).
- **Rename `marker_digest` → `round_identity`, bundled into this one amendment.** The derivation changes regardless; the old name implied the gone chained-marker structure; `round_identity` honestly names the opaque round identity m-10 binds by properties. Bundling with the segment_id removal = one body change, not two (m-10's affordability point). Joint call: I propose `round_identity`; m-10 concurs or keeps `marker_digest`.
- **Derivation:** `round_identity` = SHA-256 (lc hex) over the JCS of the ordered array of the round's records' §1.3 canonical CONTENT (first_seq…last_seq), **excluding the advisory `record_digest`**, **no `prev_digest`** (chaining removed), **no segment term**. STORED (rev16 §2:39 — receipt_conflict decidable only if stored). Four properties preserved (stable/unique/byte-reproduced/equality-comparable).
- **`receipt_conflict` decidability:** preserved over the reduced tuple; removing `segment_id` doesn't weaken it (`round_identity` functionally determines the round, `seq_hwm` its committed-end).

## Review priorities (your gate)
(a) Is the body member-set exactly the operator-fixed set (segment_id out, seq_hwm + generation_id in), with turn_id/attempt_id keys? (b) Is the `round_identity` derivation total + observer-reproducible under one-file-per-run, excluding the advisory record_digest and with no chain/segment term? (c) Does `receipt_conflict` decidability survive the reduced tuple? (d) Is the rename honest + correctly scoped as a joint call bundled into the one amendment? (e) No lock/frozen byte moved (r17 `01b885fe…` UNMOVED; this is a fresh successor body for r2).

Return `DESIGN_REVIEW_VERDICT`. On approve: I re-tender for m-10's byte-confirm (stored/equality/receipt_conflict/fencing) + m-3's `seq_hwm`-locator confirm; then the §D join re-signs on the exact body; only the re-signed artifact enters amendment r2.

## Boundaries
Design-only producer bytes + pair-review request. No lock moved; r17 `01b885fe…`, m-10 rev16 `3e3c5192…`, interface lock `cbd1893c…`, §D amendment `1fa71cb8…` UNMOVED; `receipt_conflict` mechanism frozen (only the operator-decided tuple shape changes). No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
- Authored doc `d5d79027f5ebb745e9bc47be295d0c76406de052a33d1d6f4976d56fd8ac8860` (this session). Supersedes body at r17 §2:308. Operator Decision 3 (member set): `…-esc1-ratify-3/…-031526.md` (`bda1c941…`). m-10 conditional-rename + reciprocal/re-sign requirement: `…-esc1-route5-m10-ans-1/…-033700.md`. m-3 seq_hwm REQUIRED: `…-esc1-route5-members-m3/…-034100.md`. rev16 §2:39/§2:41 (stored + equality-only). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this review-request relay + the new design doc `2026-07-26-s1-receipt-body-onefile.md` + one INDEX.md row; no lock/frozen byte moved, no `frank/` action, no amendment, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the exact-byte verdict over `d5d79027…`; on approve m-10 byte-confirms + m-3 confirms the locator + the §D join re-signs; master binds only the re-signed artifact in r2. H-12 stands.
