## DESIGN — close5 r2: S-1 body §5 corrected — `generation_id`/`turn_epoch` are validate-and-drop fence operands, NOT receipt evidence @ `790ddce5…` — RE-REQUEST m-9.implementer exact-byte review (pair-approval gate F1). Folds M9-CLOSE5-R1-F1: reduced evidence tuple = `{run_id, turn_id, attempt_id, round_identity, seq_hwm}`; frozen first-match ordering (equivalent-duplicate → sender/epoch fencing → receipt_conflict on non-equivalent EVIDENCE) preserved; a generation-only difference is not a receipt-evidence conflict. Body member set + one-file derivation + stored round_identity + segment removal UNCHANGED (you already passed them). r17 `01b885fe…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close5-body-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close5-body-m9-review-r1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the returned body changes a co-signed receipt inside the ratified §D amendment and feeds r2 (Master+VP+operator). This relay corrects the producer bytes + re-requests the pair-review; it moves no lock.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-close5-body-m9
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 790ddce5ff63fce0c7b1c603ee27482d8ddfcbb586be0d7323cd0f8c826edbdf
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close5-body-m9/DESIGN-REVIEW-implementer-20260726-132622.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close5-body-m9/DESIGN-planner-20260726-133700.md
SUBJECT: close5 r2 — pair-review re-request over the corrected S-1 body `master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `790ddce5…`: §5 rewritten so generation_id/turn_epoch are validate-and-drop fence operands and the reduced evidence tuple is `{run_id, turn_id, attempt_id, round_identity, seq_hwm}`

m-9.implementer — your M9-CLOSE5-R1-F1 is correct and folded. The corrected bytes are in **`master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `790ddce5ff63fce0c7b1c603ee27482d8ddfcbb586be0d7323cd0f8c826edbdf`**.

## What changed (review the doc bytes, not this summary)
- **§5 rewritten.** `generation_id` (and envelope `turn_epoch`) are now stated as **validate-and-drop fence operands, NOT receipt evidence**, matching the exact frozen predicate on both sides (r17 §2:310-317; rev16 §2:39/:41). The **reduced evidence tuple** is exactly `{run_id, turn_id, attempt_id, round_identity, seq_hwm}`. The frozen **first-match ordering** is spelled out: (1) equivalent duplicate on the evidence tuple; (2) sender/epoch fencing on `generation_id`/`turn_epoch` — a generation/epoch difference is fenced here and **is not** a receipt-evidence conflict; (3) `receipt_conflict` only on a same-key non-equivalent **evidence** tuple.
- **§2 table** annotates the `generation_id` row as a validate-and-drop fence operand (not evidence).
- **UNCHANGED** (you already passed these): the operator-fixed member set (`segment_id` out, `seq_hwm` + `generation_id` in, `turn_id`/`attempt_id` keys); the one-file `round_identity` derivation over the round's canonical content excluding the advisory `record_digest`, no chain/segment term; STORED; four properties; the honest rename-as-joint-call.

## Review priorities (your gate)
(a) Is the reduced evidence tuple exactly `{run_id, turn_id, attempt_id, round_identity, seq_hwm}`, with `generation_id`/`turn_epoch` excluded from equivalence and conflict? (b) Is the first-match ordering the frozen order (equivalent-duplicate → sender/epoch fencing → receipt_conflict-on-evidence)? (c) Is "a generation-only difference is not a receipt-evidence conflict" stated unambiguously? (d) Did nothing you already approved regress? (e) No lock/frozen byte moved (r17 `01b885fe…` UNMOVED).

Return `DESIGN_REVIEW_VERDICT`. On approve: I re-tender for m-10's byte-confirm + m-3's `seq_hwm`-locator confirm; then the §D join re-signs on the exact body; only the re-signed artifact enters amendment r2.

## Boundaries
Design-only producer bytes + pair-review re-request. No lock moved; r17 `01b885fe…`, m-10 rev16 `3e3c5192…`, interface lock `cbd1893c…`, §D amendment `1fa71cb8…` UNMOVED; `receipt_conflict` mechanism frozen (only the operator-decided tuple shape changes). No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
ACTIONS_GIT_REF: docs-workspace disk action — one revised design doc (`2026-07-26-s1-receipt-body-onefile.md` @ `790ddce5…`), this DESIGN relay, one append-only INDEX.md row; no frozen byte edited, no `frank/` action, no PLAN/T4/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` verification is recorded after relay lint.
