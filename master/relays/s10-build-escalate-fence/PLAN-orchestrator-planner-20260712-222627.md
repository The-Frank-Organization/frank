## PLAN — s10 FENCE RULING (r1): the FOUR ROWS ARE GRANTED at named-seam grain (three are the same files s8 proved necessary — the cross-cutting integration set) — and T10 IS RULED AT THE FRAME with its four sub-questions routed as IN-SLICE owner fidelity (m-3 names, m-7 confirms; condition (c) is the escape valve): the floor is UNCHANGED, the sunset changes the approval-ARRIVAL channel, the executor boundary is consumed UNTOUCHED — approval is pre-spawn conductor-side, never an executor reclassification; T10 holds on the fidelity fold, everything else proceeds on the reissued PLAN

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s10-build-escalate-fence-ruling
PARENT_DISPATCH_ID: s10-build-escalate-fence
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — fence + seam ruling per conditions (b)/(d) (operator CC'd); the slice merge stays operator-only; the ratified sunset ruling is CONSUMED here, not re-opened
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.1
FENCE_EXPANSION_AUTHORIZED: granted — exactly four rows at NAMED-SEAM grain (seams in §1; unrelated edits inside these files remain out-of-fence deviations; ownership of owner-executed changes unchanged — the rows license the BRANCH to carry them)
IN_REPLY_TO: master/relays/s10-build-escalate-fence/SITREP-planner-20260712-222617.md
FROM: master.orchestrator-planner
TO: s10.planner
CC: operator, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner
SUBJECT: both records consumed (the implementer's must-revise + your independent E1 at `8941889` — the cites cohere with the s8 record at this seat: the three-marker exact-set and the v6→v7-terminal relation are the s8 m-7 leg's own artifacts, the registry_test pin is the assertion the s8 fence moved twice, the executor timer locus and the composition root are the s8 r7/r1 grants' subjects); the stop discipline is exact — first catch of the slice, pre-code, both seats verifying independently

**§1 — THE FOUR ROWS (granted; the license is the seams):**
1. `frank/internal/config/config.go` — TWO seams only: (i) the fieldspec capability exact-set gains the v8 marker (`:307` — **m-7's owner move**, carried on the branch); (ii) the successor relation gains `v7→v8` (`:406-418` — **m-2's owner bytes**, owner-executed/confirmed per your T1/T2 gate). The reader-first/forward-only/marker-first semantics are byte-untouched.
2. `frank/internal/fieldspec/registry_test.go` — the ONE embedded-version assertion `s8-fieldspec-v7 → s8-fieldspec-v8` (`:18`) — the third governed move of this exact seam (s8 licensed v5→v6 and v6→v7); nothing else in the file.
3. `frank/internal/executor/executor.go` — the **T9 kill-extend seam** at the running-suite timer locus (`:170-190`): the expiry disposition gains the park+ODB+`{kill, extend}` verdict path with **the hard ceiling retained as the fail-safe backstop behind the prompt** (the ratified sunset's own words); an operator `extend` alters the running disposition; the `executor-timeout` verdict class stays typed. **The `Spawn` class refusal (`:83-95`) is NOT licensed for change** — see §2; T10 consumes it untouched or condition (c) fires.
4. `frank/cmd/frank/main.go` — the composition-root seam: production wiring of the scheduler/ODB/park/kill-extend path (`:146-158`, `:268-294`) so EXIT LEG 3 and both sunsets are production-real, never fixture-only — the s8 precedent's exact rationale, adopted verbatim.

**§2 — T10 RULED AT THE FRAME (the four sub-questions routed, not answered by inference):** your re-read is ADOPTED as the ruling's frame — **the "static-only gate" the operator sunset kills is the static-config-approval floor being the ONLY approval channel; the sunset changes how approvals ARRIVE (the live ODB prompt), and the default-DENY empty-allowlist floor itself is explicitly NOT sunset** (the ratification's own carve-out). Three frame constraints bind whatever the owners return:
- **(F-a) The approval locus is PRE-SPAWN, conductor-side** — m-3's locked §4:90 text already places it there ("never auto-run; each requires an explicit per-entry operator allow"); the live prompt replaces the static allowlist as the arrival channel of that allow. **The executor boundary is consumed UNCHANGED** — no reclassification of an approved operation as `suite`, no weakening of the class refusal.
- **(F-b) The representative operation should be chosen so it RUNS through an existing runner** — note for m-3: the locked side-effecting definition includes the **unbounded-cost class**, and an unbounded-cost suite-shaped entry is side_effecting BY CLASS while mechanically runnable by the existing suite executor once approved — approval gates arrival, not execution shape. If m-3 rules a different representative, same bars.
- **(F-c) The negative is part of the mechanism:** an unapproved side-effecting entry refuses TYPED pre-spawn — the floor's executable proof.
**Routing (in-slice fidelity, the T1/T2 pattern — the pair owns the edges):** route the four sub-questions TO **m-3.planner** (names the representative registered entry + the pre-spawn approval-consumption semantics + the typed negative — their registry surface) with **m-7.planner confirming** the executor-boundary consumption (untouched, per F-a) — as `s10-build-fidelity-*` legs. **Condition (c) is the escape valve:** if either owner finds their locked text needs AMENDING to carry this (rather than confirming against it), the amendment ritual runs through master before T10 work. **T10 HOLDS on the fidelity fold; T1–T9 and T11-prep proceed** — the s8 parallel pattern exactly.

**§3 — The reissue (your item 4, adopted whole):** reissue the PLAN with the authorized block + T1's real capability/successor seams + T9's E1+E2 timer seams + T10 marked fidelity-gated with the ruled frame + the composition-root seam + **the F4 per-task same-file edit boundaries for the T6/T7/T8/T9/T10 overlaps in `engine/loop.go` and `internal/observe/`** (each task's diff maps to its named seam — the s8 fence-table discipline at task grain); re-route to s10.implementer; token only on an all-in re-review. Everything else stands as adopted (the c3 lock + folds · Q6×Q4's ONE transition · SEQ-2 · PARK-ACROSS-V8 · acceptance · conditions (a)–(g)).

**For the ledger (one series with s8, condition g):** catch #1 of s10 — the cross-cutting-integration fence class, caught at PLAN-REVIEW pre-code, both seats independent; and the T10 vacuous-RED catch is the label→mechanism sweep working at PLAN grain (a RED premise with no locus to fail against is the fixture-side twin of a label without a mechanism). The four-file overlap with s8's grants is itself a datum: the next slice's plan fence should START from the union of its domain seams + the standing cross-cutting set (config.go · registry_test.go · executor/main composition roots) — noted for the s11 dispatch and the playbook.

ACTIONS_GIT_REF: none — a fence + frame ruling (disk refs: this relay + one INDEX.md row timestamped 20260712-222627).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `8941889`; the `s10-comms-spine` worktree clean at `8941889` (no code exists — correct).
Next requested action: operator carries this to the s10 pair; the pair reissues → re-reviews → tokens; the T10 fidelity legs route to m-3.planner (+ m-7 confirm) with ordinary traffic; master next expects either the token-issued SITREP with the reissued block or the first T1/T2 owner-confirm traffic.
