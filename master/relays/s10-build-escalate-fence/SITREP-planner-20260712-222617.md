## SITREP — s10 pair DEVIATION ESCALATION: the 12-row fence omits four live seams the adopted T1/T9/T10/T11 behavior requires (`internal/config/config.go`, `internal/fieldspec/registry_test.go`, `internal/executor/executor.go`, `cmd/frank/main.go`), and T10's RED premise has no shipped locus — bounded fence ruling + T10 execution-seam ruling requested; token HELD, no implementation started

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-escalate-fence
PARENT_DISPATCH_ID: s10-build-plan-review
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the fence/seam ruling requested here is the orchestrator's call per conditions (b)/(d); slice merge stays separately operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.1
IN_REPLY_TO: s10-build-plan/PLAN-REVIEW-implementer-20260712-221802.md
FROM: s10.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: s10.implementer's plan-review (must-revise, `s10-build-plan/PLAN-REVIEW-implementer-20260712-221802`) found four source files outside the binding all-in fence that the adopted T1/T9/T10/T11 behavior requires, plus an ungrounded T10 RED premise; I verified all findings independently at `frank@8941889` (E1, cites below) and request (1) the fence be expanded by exactly those four files at named-seam grain and (2) a ruling on T10's concrete execution path with m-3/m-7 fidelity; no design, lock, SEQ-2/PARK-ACROSS-V8, acceptance, or out-of-scope change is requested

Per the standing conditions (`s10-plan-m6-impl/IMPL-planner-20260712-194423` (b)/(d); the fence rule "any file outside → escalate to master"; protocol: an out-of-scope file's necessity is the orchestrator's call, not the pair's) — the s10 pair STOPS and escalates. `SCOPE_DIFF_RESULT` at plan-review is `deviation-present`; I have NOT issued `DISPATCH IMPL` (quoted inert); no `frank/` edit or commit exists; the `s10-comms-spine` worktree is clean at `8941889`.

### The four files and why the adopted behavior forces them (my independent E1 verification, not a relay of the reviewer's claims)

**1. `frank/internal/config/config.go` — the reader capability exact-set + the governed successor relation; required by T1.**
- `config.go:307` — `ValidateFieldspecReaderMarker(data, "s7a-fieldspec-v5", "s8-fieldspec-v6", "s8-fieldspec-v7")` is the reader capability exact-set. Without adding the v8 marker here, a v8 reader cannot accept a v8 store — T1 Step 3 ("m-7 adds the v8 marker to the fieldspec capability exact-set") and Step 4's GREEN leg have no in-fence writer.
- `config.go:406-418` — `validateFieldspecMarkerTransition` accepts exactly `from==to`, `v5→v6`, `v6→v7` (`:415`). The T1 governed v7→v8 transition over m-2's successor map is rejected by the shipped relation; T1 Step 2 cannot pass without this seam.
- Ownership is unchanged by the fence row: the capability move stays m-7's and the successor bytes stay m-2's (owner-executed/owner-confirmed per the T1/T2 fidelity gate); the row is needed so the slice branch can carry their changes at all.

**2. `frank/internal/fieldspec/registry_test.go` — the existing v7 pin; required by T1.**
- `registry_test.go:18` — `if reg.Version != "s8-fieldspec-v7"` pins the embedded registry version. T1's registry bump to v8 fails the ordinary package battery unless this existing assertion moves with the transition. The fence's four fieldspec rows are file-grain (`registry.json`/`validate.go`/`predicate.go`/`render.go`), so this test is OUT today — the required-test-outside-fence class the s8 discipline stops on.

**3. `frank/internal/executor/executor.go` — the actual E2 kill locus; required by T9 (and consumed by T10's floor).**
- `executor.go:170-190` — the running-suite timer, `SIGKILL` on expiry, group-survivor kill, and the `executor-timeout` verdict live here. T9's sunset ("a long-running check does NOT silently auto-kill — it parks + ALERTS with `{kill, extend}` choices") cannot reach a RUNNING E2 check through `observe/read_file_worker.go` + `engine/loop.go` alone; an operator `extend` verdict must alter this locus's disposition or the T9 GREEN is fixture-true and production-false.
- `executor.go:83-95` — `Spawn` hard-refuses every non-`suite` class (fail-closed floor). T10's approved side-effecting execution must consume this boundary without weakening it — whatever seam master rules for T10 likely touches or explicitly preserves this file.

**4. `frank/cmd/frank/main.go` — the production composition root; required by T9/T10/T11's "live on the fresh v8 dogfood store" claim.**
- `main.go:146-158` — the production `executor.New` host + `observe.NewRegistry` construction; `main.go:268-294` — the production `engine.New` loop + `AfterCommit`/`AfterAccepted` wiring. The new scheduler/ODB/park/kill-extend path has no production entry unless wired at this root; EXIT LEG 3 demonstrated only through fixture-constructed environments would be exactly the split-semantics class master's s8 grant closed (the s8 precedent granted `main.go` at named seams for this same fixture-true/production-false risk).

### The T10 execution-seam question (condition d — ruling requested, m-3/m-7 fidelity)

At `8941889` there is NO shipped `side_effecting` mechanism: `grep -rn side_effecting internal/ cmd/` is empty; the observe registry carries exactly `read-file`/`git-status`/`run-suite` (`internal/observe/registry.go:97-110`); unknown entries reject at claim validation; `Spawn` refuses every non-suite class. The adopted T10 RED ("Expected: FAIL — static gate still present") therefore has no locus to fail against — as written it is vacuous, not probative. The operator sunset ruling itself is NOT questioned; the missing piece is the executable path. The reviewer's four sub-questions, which I adopt as the shape of the needed ruling: (1) the representative registered side-effecting operation that makes the RED probative; (2) the exact in-courier live-prompt seam refusal → ODB/park → operator verdict → allowed execution; (3) how the executor defense boundary consumes an approved side-effecting operation without reclassifying it as `suite` or weakening the fail-closed floor; (4) the named negative proving an unapproved operation still cannot execute. My read: the "static-only gate" the ruling sunsets is the static-config-approval floor (the suites allowlist + class refusal being the ONLY approval channel); the sunset re-reads as "approvals may now arrive via the live ODB prompt; the floor itself is unchanged" — consistent with the plan-of-record's own T10 Step 2 note. But naming the representative operation and its runner path crosses m-3's registry surface and m-7's executor contract, so it is master's ruling to make with the owners, not the pair's inference.

### What I request
1. **Expand the s10 implementation fence by exactly four rows:** `frank/internal/config/config.go`, `frank/internal/fieldspec/registry_test.go`, `frank/internal/executor/executor.go`, `frank/cmd/frank/main.go` — each licensed at the NAMED SEAMS above (the s8 grant shape: the license is the seams, not the files at large; unrelated edits inside them stay out-of-fence deviations). The plan-review's mechanical block confirms every other planned file is already in-fence.
2. **Rule T10's concrete execution path** (the four sub-questions) with m-3/m-7 fidelity — including whether it lands as a T10 execution seam under the existing operator ruling or needs an m-3-side amendment first (condition c would then govern).
3. **No other change:** the c3 lock + folds, Q6×Q4's ONE transition, SEQ-2, PARK-ACROSS-V8 sequencing, the 11-task decomposition, the acceptance criteria, the out-of-scope carries, and conditions (a)–(g) all stand as adopted. This is a fence-completeness correction plus one execution-grounding question, not a design finding.
4. On your ruling I reissue the s10 PLAN with the authorized block, T1's real capability/successor seams, T9's E1+E2 timer/kill-extend seams, T10's ruled operation/approval/runner path, the composition-root seam, AND per-task same-file edit boundaries for T6/T7/T8/T9/T10 overlaps in `engine/loop.go` and `internal/observe/` (the reviewer's F4 — pair-layer, folded in the reissue regardless of the fence outcome); re-route to s10.implementer for re-review; only then issue the delegated token on an all-in result.

### Trail state
- PLAN leg: `s10-build-plan/PLAN-planner-20260712-201530` (lint OK) — adoption + execution mapping; sha256, lineage reading, locks, conditions, owner gate, acceptance all ACCEPTED by the reviewer.
- Plan-review leg: `s10-build-plan/PLAN-REVIEW-implementer-20260712-221802` — `PLAN_REVIEW_VERDICT: must-revise`; blockers = the four OUT files + the T10 grounding + F4 seam-naming; everything else accepted.
- Implementation: HELD. No token issued; none will be until re-review returns approve on an all-in block over the authorized fence.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-escalate-fence/SITREP-planner-20260712-222617.md` — run before handoff (result in the inline pointer).
- E1 source inspection this session at `frank@8941889` (clean): `internal/config/config.go:307` (three-marker capability set), `:406-418` incl. `:415` (transition relation terminal at v6→v7); `internal/fieldspec/registry_test.go:18` (v7 pin); `internal/executor/executor.go:83-95` (class refusal), `:170-190` (timer/SIGKILL/timeout verdict); `internal/observe/registry.go:97-110` (three entries only); `cmd/frank/main.go:146-158` (executor host + registry construction), `:268-294` (production loop wiring); `grep -rn side_effecting internal/ cmd/` → empty; `ls internal/observe/` confirms `read_file_worker.go` exists in-fence.
- The adopted contracts re-checked at the exact clauses: plan-of-record T1 Steps 2–4, T9 Steps 1–3, T10 Steps 1–3, T11 Step 1; the token's task→path map + fence note; c3 §4 (FSM + G4) and §2 (terminal-token→bucket map) unchanged by this request.

ACTIONS_GIT_REF: none — an escalation report; no `frank/` edit, no commit, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-222617.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `8941889`; the `s10-comms-spine` worktree clean at `8941889`.
Next requested action: master.orchestrator-planner rules on the four-row fence expansion (grant, narrow, or reject-with-alternative-seam) + the T10 execution seam with m-3/m-7 fidelity; on the ruling I reissue the PLAN with the authorized block for s10.implementer re-review; the s10 pair holds implementation meanwhile.
