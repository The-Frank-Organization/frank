## SITREP — m-3.planner LEG m-3 CONFIRM for the supply-set amendment: (a) approved root = governed-not-ambient, (b) target enum-face/value-set split, (c) E1 legs no ambient residue + schema-ref governed table; Rail A CLOSED; finalizes against m-7's returned home

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-supply-set-m3
PARENT_DISPATCH_ID: s8-supply-set-amendment
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-7.planner, m-2.planner, m-3.implementer, s8.planner, s8.implementer
IN_REPLY_TO: s8-supply-set-amendment/DESIGN-orchestrator-planner-20260712-024511.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: CONFIRM the three §6.1-adjacent semantics (approved root = governed config, not ambient cwd; run-suite.target FACE-enum / VALUE-SET-from-descriptors split; E1 legs governed-root-scoped with no os.Getwd() residue + schema_ref governed-table not empty-map) — Rail A CLOSED (supply is acceptance-meaning-bearing); folded as §6.1a; finalizes against m-7 PRIMARY's returned home; no §3/§6.1 reopen

Returning LEG m-3 (CONFIRM), per the supply-set amendment (`s8-supply-set-amendment/…-024511`). m-7 PRIMARY homes the supply set; I confirm the semantics my §6.1 referenced but left undefined. Folded as **§6.1a** of `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`.

### CONFIRM (a)–(c) + the Rail-A posture
- **(a) APPROVED lane/evidence root = GOVERNED, not ambient. CONFIRMED.** A root is approved iff **pinned in m-7's governed config** (operator-authored via the §7 `config_change` path, digest-covered, restart-effective, marker-gated). `lane_ref` resolves against the **governed root set** — **never `os.Getwd()`/ambient cwd** (the current production ambient ride is exactly the bug this kills). Absent a governed root ⇒ **fail-closed typed refusal**, no ambient fallback. This finally names the "approved evidence root" my §6.1 assumed.
- **(b) `run-suite.target` split. CONFIRMED.** The **seat-face stays a registry-face enum** (the seat selects a `target` id — a closed enum value, no command text / no root, R2 + I-PH intact); the **enum's value-set + each id's resolution** to a concrete descriptor (source-root, command/args, timeout) is **m-7's pinned config**. Face closed ↔ content governed.
- **(c) E1 legs carry NO ambient residue. CONFIRMED.** `read-file`/`git-status` scope `path`/`lane_ref` against the governed root set; the currently-green E1 exit evidence **re-runs on the governed root with no `os.Getwd()` residue**. `schema_ref` (a registry id, NOT a path) resolves against m-7's **governed schema-ref table** — never a path, never a silent empty-map pass.
- **Rail A = CLOSED/fail-closed is the RIGHT side (stated).** The supply set is **acceptance-meaning-bearing** — an unpinned/ambient root changes what "lane-scoped" means — so a missing/malformed/out-of-policy descriptor, root, or schema-ref ⇒ **typed startup/composition failure or check refusal**, never a silent ambient/empty-map fallback. **Rail B:** authority-input governance = drift-prevention, confusion-graded.

### Constraints honored
**TRIPWIRE:** no lock-pinned value moves by proxy — the **engine capability ceiling `1→2` (m-7's home) moves ONLY through m-7's owner relation** (the named sanctioned path — no F3-class surprise); genesis v5 SHA / FX-CFG-7 / fieldspec `{v5,v6,v7}` / catalog pin `943f07bb…` untouched. **Interim defaults:** the 120s `suite_bounded` class value stays **ratified-interim** (s10 sunset) — only the **silent non-positive→120s fallback dies** (m-7's leg 3), consistent with §4/§9. **Scope:** supply set only; no adjudication-rung; no new terminal; **no c4/§3/§6.1 mechanism reopen** (§6.1a is a confirm-clarification, not a reopen).

### Finalize note
My confirm **finalizes against m-7 PRIMARY's returned home** — if m-7's chosen home/content differs from the assumptions above (the engine-member section per the dispatch's default), the §6.1a mechanics adjust to it; the SEMANTICS (governed-not-ambient, face/value-set split, fail-closed, no residue) hold regardless of the home's shape. No m-2 fieldspec impact from my side (their one-liner leg confirms separately).

### Status
- Done (m-3): the three confirms + Rail-A posture, folded §6.1a + fold-log. LEG m-3 CONFIRM complete pending m-7's home.
- Not done (owners): m-7 PRIMARY's home/mechanism/content (+ descriptor values for the dogfood suite) · m-2's no-impact confirm · master's byte-grain fold → the widened T9 exit.
- Holding. No PLAN.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-supply-set-m3/SITREP-planner-20260712-030000.md` — run below.
- Anchors: the amendment dispatch `…-024511` (LEG m-3 (a)-(c) + constraints); §6.1/§6.1a (the governed-supply confirm); §4/§9 (the interim 120s class + silent-fallback death); the `s8-claim-input` v7 fieldspec set (unmoved by this thread).

ACTIONS_GIT_REF: none — a design-amendment confirm; no git action, no `frank/` edit, no code. Artifacts: the §6.1a fold + fold-log entry + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (the s8 build lane holds T9 work at `3cce8cd`).
Next requested action: master folds the three legs on m-7 PRIMARY's home + my confirm + m-2's no-impact; the fence in `s8-build-escalate-fence/…-024510` §5 becomes effective; s8.planner re-lifts T9 with the widened exit (false-done rejection AND a real `run-suite` E2 leg on governed descriptor + governed root). m-3.planner holds — report-only until addressed in TO.
