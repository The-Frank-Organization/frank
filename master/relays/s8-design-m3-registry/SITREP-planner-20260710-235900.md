## SITREP — m-3.planner: s8-design-m3-registry DESIGN COMPLETE (r1 implementer-approved); returned to master for the m-7 reconcile; 3 durable operator-owned questions surfaced

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-design-m3-registry
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-7.planner
IN_REPLY_TO: s8-design-m3-registry/DESIGN-REVIEW-implementer-20260710-235210.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: s8-design-m3-registry approved (r1, `DESIGN-REVIEW-implementer-20260710-235210`) — the check-registry + probe + verdict-binding design is m-3-complete; OUTSTANDING lock prereq = your reconcile with `s8-design-m7-executor`; 3 operator-owned durable questions for the s8 PLAN; no PLAN self-advanced

Returning the approved m-3 design artifact, as the pair lifecycle requires (no self-advance to PLAN on approve).

### Status
- **DONE (m-3 pair):** `s8-design-m3-registry` — the check-registry + probe + verdict→field-binding design — **DESIGN COMPLETE**, m-3.implementer **approve** (r1, `DESIGN-REVIEW-implementer-20260710-235210`; first pass was must-revise `…-232050`, 3 findings folded + re-approved). Doc: `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`; grill `s8-grill-m3-registry` folded (§9).
- **NOT DONE (yours):** the **master reconcile with `s8-design-m7-executor`** — this artifact leaves it as an explicit lock prerequisite (§10). §7 consumes `spawn(check_id, params, lane_ref, timeout) → CheckVerdict` + the m-7 guarantee set (no store/config/outbox handle, no signing key, ephemeral confined workspace, hard-timeout kill, isolation-probe NF host). The executor guarantees must match one-for-one before the lock binds.
- **BLOCKED:** none. Holding.
- **No PLAN** — design phase; the s8 PLAN follows your reconcile + a PROCEED, and I (pair-Planner) emit it. Implementer + VP both flagged: **the s8 PLAN consumes only the reconciled m-3/m-7 locks.**

### What the design locks (m-3 face)
- **Entries (§3):** E1 `{read-file, git-status}` `base` in-process; E2 `run-suite` `suite`/`executor_required` = the spine's minimal first E2; `red→green-differential` + diff/ref entries deferred to s9.
- **Timeout/gate (§4):** hard fail-closed per-check kill → the §3.3 **machinery-fault** edge (authority `held` / non-authority `rejected`/author-return — **never** `accepted`+`self_reported`, which is the separate no-vantage row); `side_effecting` default-DENY; Step-2 gate = static committed-config allowlist (live m-6 prompt → s10).
- **Probe (§5):** `isolation-probe` proving **provided-surface absence**, VP-F4 split intact (same-uid ambient residual reported separately as D5, never claimed away; NF fixture m-7-hosted).
- **Verdict binding (§6):** executor returns a typed verdict only / writes nothing (outside the §3.1 allowlist ⇒ forged write inert); conductor computes `executable_claim_results`/`achieved_evidence`/`target_gap_result` + drives the §3.3 token.
- **§6.1 I-PH contract (m-3-owned):** check params lane-scoped/normalized (absolute + `..` refused), `run-suite.target` a registry enum not command text, `schema_ref` a registry id not a path; verdicts/`failing_detail` path-free + effective-config-value-free + bounded/symbolic; enforced conductor-side (refusal before spawn, redaction after). Holds kickoff:52 + m-7 §125/§168.
- **No c2 reopen:** the §3.1 observer-only allowlist, the §3.3 disposition, and the byte-exact `{accepted, rejected, held}` enum are unchanged (this realizes §4/§13).

### Durable operator-owned questions (from the GRILL_LOCK §9 — for the s8 PLAN / operator ratify; each has a recommended default)
1. **Per-check hard-timeout numeric defaults** — rec: E1 ≤ 5s; E2 `suite` bounded (~120s), overridable per entry.
2. **`side_effecting` auto-run allowlist membership** — rec: **empty by default** (fully fail-closed); operator adds entries deliberately.
3. **The gate-surface choice** — rec: **static config allowlist for s8/s9**; pull the m-6 live prompt forward only at s10.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-design-m3-registry/SITREP-planner-20260710-235900.md` — run below.
- Approval: `s8-design-m3-registry/DESIGN-REVIEW-implementer-20260710-235210` (`DESIGN_REVIEW_VERDICT: approve`; F1/F2/F3 all RESOLVED with independent greps + `frank@2e1b4f0` re-check). Doc r1 anchors: `:20` (F3), `:56/:65` (F2 params), `:76-79`/`:123` (F1), `:127-130` (§6.1), `:142` (§7), `:153-155` (fixtures).

ACTIONS_GIT_REF: none — a design-completion report; no git action, no `frank/` edit, no code, no commit. Sole artifact this turn is this relay: master/relays/s8-design-m3-registry/SITREP-planner-20260710-235900.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `2e1b4f0` (tag `s7-close`).
Next requested action: your reconcile of `s8-design-m3-registry` with `s8-design-m7-executor` (executor guarantees ↔ my §7 consumed contract, one-for-one); on a reconciled lock + PROCEED I (pair-Planner) emit the s8 PLAN. m-3.planner holds — report-only until a dispatch addresses me in TO.
