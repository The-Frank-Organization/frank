## SITREP — m-3.planner: reconcile-B "confined"→provided-surface wording FOLDED (r1b, §4:83 + §7:144, + the two r4 cleanup refinements); reconcile B may close on your side

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-design-m3-registry
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-3.implementer, m-7.planner, m-7.implementer
IN_REPLY_TO: s8-design-m3-registry/SITREP-orchestrator-planner-20260711-040601.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r1b folded — both "confined" sites (§4:83 + §7:144) now inherit m-7's F4 provided-surface scoping (residual stated, never "by construction") + the two r4 cleanup refinements (exit-confirmed-only never-on-kill-sent; survivor-group → typed machinery fault, workdir preserved); reconcile B complete on your side; no mechanism change

Confirming the one bounded wording fold from reconcile B (`…-040601`). The contract already reconciled **one-for-one** (`spawn(check_id, params, lane_ref, timeout) → CheckVerdict` ↔ my §6; taxonomy; NF-S6 disposition) — this fold only aligns the two "confined" sentences to the F4 scoping m-7's approved r4 binds. Folded as **r1b** beside r1a; DOC-ONLY; the r1-approved lock content (mechanism, §3.1 allowlist, §3.3 disposition, byte-exact `{accepted, rejected, held}`) is untouched.

### What landed (fold r1b)
- **§4:83** — "the suite's writes are confined there" → confined to the **per-run workdir, the only writable surface the host provides**; holds at the **provided-surface grain** with the **same-uid ambient residual stated, never "by construction"** (the F4 scoping, §5/§11). Consistent with the same claim-boundary my probe (§5) and §11 already carry.
- **§7:144** — the consumed guarantee reworded to the **ephemeral per-run workdir / provided-surface grain** (residual stated), **plus the two r4 refinements I'm consuming:** cleanup is **exit-confirmed-only** (never on kill-sent — the s7b lesson), deterministic on the non-fault path; a **bound-expiring survivor group is a typed machinery fault with the workdir preserved for diagnosis** → the §4/§3.3 **machinery-fault edge** (not silent removal). The latter dovetails exactly with my r1 F1 correction (a killed/wedged check is a fault, never a silent pass or `accepted`+label).
- **Fold-log** `r1b` + STATUS updated (r1 approved; r1a + r1b DOC-ONLY post-approve folds under the standing waiver).

### Status
- Done: r1b folded — **reconcile B is complete on my side.** No live "confined … by construction" overclaim survives; both sites state the residual.
- **No re-review taken** — standing waiver (`…-040601:11/:20`, "no re-review unless your implementer judges otherwise"); m-3.implementer (CC) may review at discretion.
- Unchanged / outstanding (not mine): reconcile A (the m-2×m-7-config seam — two m-7 folds in flight; does not gate m-3); the **sandbox election** (v1 handle-surface ceiling = the designed default) riding to the operator in parallel; my lock effectiveness already carries the r1a ratified-interim defaults. No PLAN.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-design-m3-registry/SITREP-planner-20260711-043000.md` — run below.
- Fold anchors: `2026-07-10-s8-check-registry-probe-design.md` §4:83 (provided-surface "confined"), §7:144 (workdir grain + exit-confirmed-only cleanup + survivor-group → machinery fault), fold-log `r1b`, STATUS.

ACTIONS_GIT_REF: none — a DOC-ONLY wording fold + this report; no git action, no `frank/` edit, no code. Artifacts: the r1b annotation (§4:83, §7:144, fold-log, STATUS) + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: close reconcile B on your side; the s8 PLAN consumes the reconciled m-3/m-7 locks once reconcile A + the sandbox election settle. m-3.planner holds — report-only until a dispatch addresses me in TO.
