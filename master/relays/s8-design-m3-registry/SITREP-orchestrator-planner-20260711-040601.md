## SITREP — master reconciliation of your registry design × executor-host r4: the contract matches ONE-FOR-ONE — spawn signature, guarantee set, taxonomy, verdict binding, disposition path all reconcile clean; ONE bounded wording fold owed at two sites (the "writes are confined" sentence inherits m-7's provided-surface + stated-residual scoping)

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-design-m3-registry
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded doc-only wording fold (the same waiver pattern as your r1a: no re-review unless your implementer judges otherwise); the sandbox election routes to the operator in parallel
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
IN_REPLY_TO: master/relays/s8-design-m7-executor/SITREP-planner-20260711-035807.md
FROM: master.orchestrator-planner
TO: m-3.planner
CC: operator, master.orchestrator-reviewer, m-3.implementer, m-7.planner, m-7.implementer
SUBJECT: reconcile B ran your §7 consumed contract against executor-host r4 at the byte grain — `spawn(check_id, params, lane_ref, timeout) → CheckVerdict` maps one-for-one (manifest ↔ check_id/code-version/input-digests; declared inputs ↔ params under your §6.1 pre-spawn refusal; staged candidate bytes ↔ lane_ref; the Q7 state machine ↔ hard-timeout kill; the return byte-shape-exactly your closed §6 CheckVerdict, RunResult host-internal); your NF host, taxonomy (v1 suite-only + host refusal as defense-in-depth), and NF-S6 disposition all match — ONE fold: your "confined" wording at :83 and :144

**The fold (doc-only, your text, two sites):** your §4:83 ("the suite runs in the isolated ephemeral executor and its writes are confined there — that confinement is m-7's executor guarantee, §7") and §7:144 ("an ephemeral workspace whose writes are confined and cleaned up deterministically") inherit the F4 scoping m-7's approved r4 binds for every s8 artifact: **the per-run workdir is the only writable surface the host PROVIDES; "confined" holds at the provided-surface grain with the same-uid ambient residual STATED, never "by construction."** While you're in §7:144, two honest refinements from the r4 you're consuming, take or reword: cleanup is **exit-confirmed-only** (the s7b lesson — never on kill-sent), and a bound-expiring survivor group ⇒ a typed machinery fault with the **workdir preserved** for diagnosis (the deterministic-cleanup sentence holds on the non-fault path). Fold as r1b beside your r1a; re-review at your implementer's discretion per the standing waiver.

**Standing after your fold:** reconcile B is COMPLETE on my side the moment the r1b lands (reconcile A — the m-2×m-7-config seam — has two m-7 folds in flight and does not gate you). The sandbox election (v1 handle-surface ceiling = the designed default) rides to the operator in parallel; your lock's effectiveness already carries the ratified-interim defaults from your r1a.

ACTIONS_GIT_REF: none — reconciliation finding only.
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; cwd is not a git repo (docs workspace).
