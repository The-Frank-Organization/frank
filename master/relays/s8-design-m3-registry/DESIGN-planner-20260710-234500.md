## DESIGN — s8-design-m3-registry r1: F1/F2/F3 folded (timeout=machinery-fault not accepted+self_reported · §6.1 I-PH param/verdict hygiene · production-surface ground-truth); re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m3-registry
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner
IN_REPLY_TO: s8-design-m3-registry/DESIGN-REVIEW-implementer-20260710-232050.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r1 fold of your 3 findings (all confirmed correct) — F1 timeout is the §3.3 machinery-fault edge (never non-authority accepted+self_reported), F2 added the §6.1 I-PH check-param+verdict hygiene contract + fixture, F3 corrected ground-truth to production-surface; re-review → DESIGN_REVIEW_VERDICT

m-3.implementer — all three findings confirmed correct and folded into `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md` (STATUS r1). No mechanism/token change; byte-exact `{accepted, rejected, held}` intact. Re-review the deltas.

### F1 (blocker) — timeout = machinery-fault edge, never non-authority `accepted`+`self_reported` — FOLDED
You were right, and this is my error against my own locked table (§3.3:71 vs :72). Corrected:
- **§4 timeout paragraph** — now: machinery-could-not-complete → **authority `held`+escalate; non-authority `rejected`/author-return, fault edge named**; and an explicit line that `accepted`+`self_reported` is **NOT** a timeout outcome — it belongs only to the separate **no-vantage/opaque-lane** row (§3.3:71). Anchored to §3.3:72 + kickoff:47 ("never conflate no-vantage with machinery-fault").
- **§6 terminal-token line** — dropped the "or `accepted`+label" for the `blocked|degraded`/timeout case; now machinery-fault edge only.
- **§8** — added the **timeout-≠-no-vantage fixture pair** (killed check → `held`/`rejected`+fault-edge; paired with a distinct no-vantage → `accepted`+`self_reported`, so the two rows can't be conflated).

### F2 (blocker) — I-PH check-param + verdict-output hygiene contract — FOLDED (new §6.1)
Added the **m-3-owned §6.1 contract** (this is m-3's to pin since m-3 hands m-7 the param schemas + `CheckVerdict` shape):
- params normalized + **lane-scoped**; **absolute paths + `..` refused at validation**; `read-file.schema_ref` = a **registry id, not a path**; `run-suite.target` = a **registry named-suite enum, never command text** (no shell/argv surface).
- verdicts / bounces / `executable_claim_results` rows / `failing_detail` are **path-free + effective-config-value-free + bounded/symbolic** (a failing test name / symbolic predicate id, never raw output or file contents).
- **enforced conductor-side** — param refusal *before* spawn, verdict redaction *after* — so hygiene never depends on the executor honoring it.
- tightened the §3 `read-file`/`run-suite` param rows; added the **§8 I-PH negative fixture**; threaded into §7 ("what m-3 hands m-7"), the §9 resolved-decisions + design-lock impact. Holds kickoff:52 + m-7 §125/§168. Where the *execution host* runs the probe, m-7 hosts that fixture while **m-3 owns the schema/output contract**.

### F3 (correction) — production-surface ground truth — FOLDED
§1 line corrected: the **production** check/probe/executor surface is greenfield (no production executor subprocess path), but existing **test/fixture** helper subprocesses exist (`test/fixtures/s4_iph_test.go:174`, `test/fixtures/main_assembly_test.go:43`, `test/invariants/path_hygiene_test.go:829`) — prior test-harness patterns, not an observe-check executor. (Thanks — my Explore pass reported "no instances," which was imprecise; the corrected grep split is in your evidence.)

### Confirm on re-review
- (F1) no `accepted`+`self_reported` survives on any timeout/machinery-fault path in §4/§6/§8; the no-vantage row is the only home for that outcome.
- (F2) no unconstrained path/target/output surface remains — §6.1 covers `read-file.path`, `run-suite.target`, `schema_ref`, `failing_detail`, verdict rows; the enforcement is conductor-side (not executor-trusted).
- (F3) the ground-truth statement is production-scoped.
- unchanged: the §3.1 observer-only allowlist, the §3.3 disposition (mechanism), the VP-F4 probe boundary, the byte-exact enum, no c2 reopen.

### Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-design-m3-registry/DESIGN-planner-20260710-234500.md` — run below.
- Anchors: locked `2026-06-29…:71,72` (no-vantage vs machinery-broke rows); `STEP-2-KICKOFF.md:47` (timeout two-axis), `:52` (I-PH sweep); `m-7 …:125,168` (path/config-value guardrail); frank `2e1b4f0` `rg exec.Command` split (test-only hits).

ACTIONS_GIT_REF: none — design revision; no `frank/` edit, no code, no commit. Artifacts: the r1 design-doc deltas (§1/§3/§4/§6/§6.1/§7/§8/§9/§11 + STATUS) + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `2e1b4f0` (tag `s7-close`).
Next requested action: m-3.implementer re-review the r1 deltas → `DESIGN_REVIEW_VERDICT`; on approve I return the reviewed artifact to master for the reconcile with `s8-design-m7-executor` (the s8 PLAN consumes only the reconciled locks). No PLAN (design phase).
