## DESIGN — s8-design-m3-registry authored (check-registry + probe + verdict-binding; grill folded); DESIGN-REVIEW requested from m-3.implementer

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m3-registry
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner
IN_REPLY_TO: s8-design-m3-registry/DESIGN-orchestrator-planner-20260710-214611.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: sole-authored `s8-design-m3-registry` — the 5 required faces (suite-class entries + E1 family · timeout+operator-gate · adversarial probe w/ VP-F4 split · verdict→§3.1-field binding · m-7 boundary contract) + grill folded; adversarial DESIGN-REVIEW → return `DESIGN_REVIEW_VERDICT`

m-3.implementer — the s8 design dispatch (`…-214611`) tasked me sole author of `s8-design-m3-registry`, realizing the locked §4/§13 check-registry carry. Design doc authored: **`master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`**. Grill run + folded (§9, `s8-grill-m3-registry`). Please adversarial-review and return `PHASE: DESIGN-REVIEW` / `DESIGN_REVIEW_VERDICT`.

### What it covers (the 5 required faces, all against the locked c2 record + verified frank ground truth)
1. **Suite-class entries + the E1 family (§3):** E1 `{read-file, git-status}` `class:base` in-process (no executor, no repo-code); E2 `{run-suite}` `class:suite`/`executor_required` as the spine's minimal first E2; `red→green-differential` + diff/reference entries **deferred to s9**. Param schemas per entry.
2. **Timeout + operator gate (§4):** hard per-check wall-clock kill, **fail-closed**; on-expiry = machinery-could-not-complete → **§3.3 fault edge** (authority `held`; non-authority `rejected`+fault-edge), NOT the predicate-false `rejected`. `side_effecting` = **default-DENY**; base+suite auto-active; Step-2 gate surface = **static committed config allowlist** under the composite digest (live m-6 prompt deferred to s10).
3. **Adversarial probe (§5):** `isolation-probe` tries store/config/outbox/signing, all must fail. **VP-F4 split honored:** proves **provided-surface absence**; the same-uid ambient residual is **reported separately as the D5 accepted-risk, never claimed away**. NF fixture m-7-hosted.
4. **Verdict binding (§6):** executor returns a **typed verdict only, writes nothing** (no store handle, no §3.1 allowlist entry ⇒ a forged verdict is inert); the **conductor** computes `executable_claim_results`/`achieved_evidence`/`target_gap_result` and drives the §3.3 terminal token. Trigger≠execute + observer-only preserved.
5. **Boundary contract (§7):** names `s8-design-m7-executor` as the consumed executor host (`spawn()` + the guarantee set); I do **not** author m-7's content; master reconciles the two before the s8 PLAN.

### Adversarial targets (where to push hardest)
- (a) **Does the verdict binding leak any write path to the executor?** I claim the executor is outside the §3.1 allowlist so a forged field-write is inert (§6) — confirm there's no back-door (e.g., via `executable_claim_results` row content) that lets executor-returned bytes land as an observed field unmediated.
- (b) **Is the probe's honesty scope exactly VP-F4?** Confirm §5 claims only *provided-surface absence* and reports the same-uid ambient residual separately — no "unbypassable/tamper-proof/no-possible-reach" overclaim survives (§11 claim-boundary).
- (c) **Timeout disposition:** is a timeout correctly the *fault edge* (couldn't-run) and not conflated with predicate-false `rejected`? Check §4 vs §3.3.
- (d) **No c2 reopen:** confirm this only *realizes* §4/§13 — the mechanism, the §3.1 observer-only allowlist, the §3.3 disposition, and the byte-exact `{accepted, rejected, held}` enum are unchanged; and that the s8-spine cut (E1+one E2; red→green→s9) matches the kickoff.
- (e) **Grill completeness:** are the three still-operator-owned durable questions (timeout numerics, `side_effecting` allowlist membership, gate surface) the right residuals, each with a defensible recommended default — or did I bury a decision that should be operator-owned (or resolve one I left open)?

### Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-design-m3-registry/DESIGN-planner-20260710-231500.md` — run below.
- Ground truth: frank @ `s7-close` `2e1b4f0` — `registry.json:147-158` (dormant observe rows), `predicate.go:17-23` (DefaultLayers omits observe), `submit.go:53-104` (open hook slot), `channel/server.go:26-31` (3-verb surface, no store/config/outbox handle), `egress.go:84-89` (⑤ carve-out), check/executor/probe = greenfield. Locked `c2-design-m-3-observation-evidence` §2/§3.1/§3.3/§4/§5/§13.

ACTIONS_GIT_REF: none — design authoring; no `frank/` edit, no code, no commit. Artifacts: the design doc `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md` + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `2e1b4f0` (tag `s7-close`).
Next requested action: m-3.implementer adversarial DESIGN-REVIEW → `DESIGN_REVIEW_VERDICT`; on approve I return the reviewed artifact to master for the reconcile with `s8-design-m7-executor` (the s8 PLAN consumes only the reconciled locks). No PLAN emitted (design phase; PLAN follows the reconcile + a PROCEED).
