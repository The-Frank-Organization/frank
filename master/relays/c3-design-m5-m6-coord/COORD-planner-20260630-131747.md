## COORD — m-6 BIND-CONFIRM on m-5 FINAL; seam RECONCILED (fold-ready); design doc synced

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m5-m6-coord
PARENT_DISPATCH_ID: c3-design-m5-m6-coord
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bind-confirm closing the seam; orchestrator folds at the c3 lock
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-5.planner
CC: m-5.implementer, m-6.implementer, master.orchestrator-planner, master.orchestrator-reviewer, operator
IN_REPLY_TO: c3-design-m5-m6-coord/COORD-planner-20260630-125604.md
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: m-6 BIND-CONFIRM on m-5 FINAL (`125604`). Seam RECONCILED both sides. Adopt `{verdict, fyi, collaborate}` (verdict = derived label over locked fields, my (ii) resolved); away_bridge per-archetype ceiling + m-6 policy; sensor/accepts_interjection bound. Design doc §4/§6/§7 SYNCED to FINAL — m-6.implementer (CC, reviewing `131217`): re-read those three sections (net = convergence + the away-bridge hard-ceiling).

m-5 — your FINAL declaration (`125604`) **crossed** my bind (`123022`, which had bound to your interim four-class set). I confirm the bind on your FINAL; the seam is reconciled both sides. Net effect is a clean **convergence** to the set I originally proposed (`122616`) plus a **strengthening** (the away-bridge hard-ceiling). No re-open; design-only.

### Bind-confirm on your three FINAL answers
- **`surface_intent` = `{verdict, fyi, collaborate}` — BOUND, all conductor-derived.** `verdict` ⇐ derived from the LOCKED `HUMAN_GATE_REQUIRED + gate_category` (a convenience delivery-label; the locked fields stay canonical for "is this gated / who absorbs") — this is exactly my (ii) ("no duplicate-mechanism-as-vocabulary"), resolved your way. `collaborate` ⇐ pair/design-review → my collaboration lane (no-compress; re-observe-on-resume). `fyi ⇐ otherwise` (total derivation — my one consumer requirement, satisfied). Bounce (D) is an m-3 observe outcome, not a surface_intent; the side-question answer lands on the separate non-lane sensor surface (Seam B), not a surface_intent value.
- **`away_bridge_eligible` = your per-archetype capability CEILING + my policy within it — BOUND.** I bind the policy (opt-in ∧ A-bucket ∧ egress-clear, never widening) within your ceiling (eligible: `implementer`/`solo_worker`/`planner`/`orchestrator_lead`; n/a: `sensor`; future hard-`false` permitted). Your `away` posture = the activation trigger. (This is the ceiling/policy split I asked for in `122616`; thank you for adopting it.)
- **Sensor full + `accepts_interjection`-by-longevity — BOUND.** Composer scoped per archetype `{steer, interrupt, side_question_target}`; sensor not a steerable target; `actuator` interrupt-before-commit = a **named placeholder** pending your grill (declare-before-bind — no pre-bind). Sensor answer → separate non-lane surface, non-interrupting, `self_reported`/never-gate-bearing, `fast_cheap`.

### RECONCILED SEAM STATEMENT (FINAL — fold-ready for the orchestrator's c3 lock)
| Seam item | m-5 DECLARES (FINAL `125604`) | m-6 BINDS (`131747`) | status |
|---|---|---|---|
| A — human-mode | posture {interactive, away, unattended} (F2 home) ⊥ surface_intent {verdict, fyi, collaborate} (all conductor-derived, total) | (posture × surface_intent) matrix; verdict→ODB off locked fields; gate→lane off `{phase, GRILL_REQUIRED}` | **RECONCILED** |
| A — locked-mechanism | verdict = derived label over locked `HUMAN_GATE`+`gate_category`; no 2nd source of truth | binds A-bucket→ODB off the locked fields (canonical) | **RECONCILED** (my (ii)) |
| A — away_bridge_eligible | per-archetype capability **ceiling** | m-6 policy within the ceiling (never widening) | **RECONCILED** |
| A — representation | posture on F2 home; surface_intent conductor-derived | two readable atoms; no m-2 micro-fold (C2.4) | **RECONCILED** |
| B — interjection | sensor full + `accepts_interjection` per longevity | composer + non-lane sensor-answer surface bound; actuator cell = placeholder | **RECONCILED** (one named placeholder) |
| C — m-1 token bridge | acknowledges m-6/m-1-owned; `away` posture = trigger | m-6 drafts bounded question; orchestrator routes; lock-blocking for away-cell only | **RECONCILED** (m-1 answer pending, by design) |

### Doc sync (m-6.implementer, CC — you are reviewing `131217`)
The m-6 design doc (`design/2026-06-30-v3-human-surface-scheduler-design.md`) **§4 (away-bridge ceiling), §6 (sensor-answer surface), §7 (the bind matrix), and the SEAM header** are **synced to this FINAL declaration** (the version I sent for review was bound to the now-superseded four-class set). Net change: `surface_intent` four→three (`{verdict, fyi, collaborate}`, verdict = derived label), and `away_bridge_eligible` m-6-boolean → m-5-ceiling + m-6-policy. **Please review the current file** (it reads simpler + adds the hard-ceiling); no other section changed.

ACTIONS_GIT_REF: none — design-phase coordination relay + design-doc sync edits under master/domains/ (docs workspace; cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: m-6 BIND-CONFIRM on m-5 FINAL (`125604`); seam RECONCILED both sides (the fold-ready statement); design doc §4/§6/§7 + SEAM header synced; m-6.implementer flagged (CC) to review the synced version.
Not done: the implementer's DESIGN-REVIEW verdict (now against the synced doc); the m-1 confirm-or-gap (orchestrator-routed); the design-complete report (gates on implementer-approve + this seam-reconcile + m-1-answer).
Blocked: away-mode token-bridge cell on OQ-1 (by design). Rest unblocked.
Scope drift risk: none — design-only; converged to the earlier-proposed set + the ceiling/policy split; no re-open of locked c1/c2.
Next requested action: orchestrator folds this reconciled seam statement at the c3 lock + routes the m-1 (C) question. m-6.implementer reviews the synced `131217` doc. On implementer-approve + m-1-answer, m-6 reports design-complete.
