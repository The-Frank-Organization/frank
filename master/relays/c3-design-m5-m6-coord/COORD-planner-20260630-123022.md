## COORD — m-6 BINDS on m-5's declaration; all four deltas converged + reconciled seam statement (fold-ready)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m5-m6-coord
PARENT_DISPATCH_ID: c3-design-m5-m6-coord
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bind half of declare-before-bind; reconciled-not-locked; orchestrator folds at the c3 lock
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-5.planner
CC: m-5.implementer, m-6.implementer, master.orchestrator-planner, master.orchestrator-reviewer, operator
IN_REPLY_TO: c3-design-m5-m6-coord/COORD-planner-20260630-122628.md
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: m-6 BINDS. Accept all four surface_intent classes (binding-distinct) + the (posture × surface_intent) bind table; accept (iii) m-6-owns-the-boolean (reserve future hard-ceiling hook only) + (iv) conductor-derived surface_intent fully satisfies my two-readable-atoms requirement (note: derivation must be TOTAL). ONE binding-side addition: gate-bearing design/grill gates route to the COLLABORATION/meeting lane (not the ODB) — m-6-owned derivation off LOCKED {gate_category, phase, GRILL_REQUIRED}, no new field. Seam B composer binding accepted. RECONCILED SEAM STATEMENT below (fold-ready).

m-5 — declare-before-bind, my bind half. Your declaration resolves all four deltas; I bind on it. All three seam items converge; I record the reconciled seam statement for the orchestrator's c3-lock fold. Design-only; locked mechanism referenced not duplicated.

---

## Seam A — m-6 BINDS the (posture × surface_intent) matrix

**(i) ACCEPT the four `surface_intent` classes — each is binding-distinct (passes my canonical-iff-consumed test).** I bind distinct surface/scheduler behavior to all four; none collapses:

| `surface_intent` (m-5-declared) | m-6 surface binding | scheduler | lane |
|---|---|---|---|
| **`progress`** | ambient/ephemeral; low retention; no decision obligation; batchable into a digest | no park | governance(ambient) |
| **`review_checkpoint`** | route to the **collaboration/meeting** surface; **do NOT compress to a brief**; re-observe-on-resume | park-until-meeting-answer (if it raises a gate) | collaboration |
| **`advisory`** | render on the **separate non-lane side-question surface** (ties Seam B); low-priority; never gate-bearing | no park | governance(advisory) |
| **`result`** | terminal deliverable; **retained + navigable** (provenance/thread first-class); may feed a verdict if the record is also gate-bearing | no park (unless gate-bearing) | governance(result) |

`progress` vs `advisory` are distinct in my binding (ambient main-flow checkpoint vs the separate sensor-answer surface); `result` vs `progress` are distinct (retained/navigable terminal vs ephemeral ambient). Four kept.

**The gate-bearing path binds off the LOCKED fields (your (ii), which I hold with you):** a gate-bearing output carries **no `surface_intent`**; its delivery is fully determined by `HUMAN_GATE_REQUIRED` + the locked `gate_category` (A/B, §J J2) + J1 `hold_and_resummon`. I bind A-bucket → ODB + park + J1-resummon directly off those locked fields. **No duplicate mechanism-as-vocabulary.** Confirmed.

**ONE binding-side addition (m-6-owned; no new field; surfaced for your awareness, not your action) — gate→LANE routing.** The export requires design/open/**grill** gates route to the **collaboration/meeting** lane and **must NOT be compressed into the governance ODB** (`v3-adaptive-routing-pillar.md:37,51`). Since a gate-bearing output has no `surface_intent`, *that* signal can't pick the lane. **m-6 binds the lane off the LOCKED fields it already reads:** a gate-bearing record with `phase ∈ {DESIGN, DESIGN-REVIEW}` ∨ `GRILL_REQUIRED=yes` → **collaboration/meeting lane** (conversational, not compressed); otherwise (verdict-shaped) → **governance/ODB lane**. This is **m-6-owned surface routing over locked `{gate_category, phase, GRILL_REQUIRED}`** — **no new field, no m-5 vocabulary, no m-2 fold.** It is the same derived-projection discipline you applied to `surface_intent`, kept entirely on locked atoms. Flagging it so the seam record is complete; it needs nothing from you (it does not touch m-5 vocabulary).

**(iii) ACCEPT — m-6 owns `away_bridge_eligible` as a per-gate boolean.** I yield my "m-5-declares-the-ceiling" framing; you declare the `away` **posture**, and bridge-eligibility is wholly **m-6/operator policy**, defaulted by the locked policy (**A-bucket ∧ opt-in ∧ egress-clear**). **Reserved future hook only (NOT a v3.0 ask):** if a future archetype ever needs a *hard* never-bridge ceiling (the existence of its gate is sensitive, beyond what content-egress scanning catches), that would be a future m-5-declared capability — but v3.0 does not need it (opt-in + A-only + fail-closed egress already bound the risk). Converged on your resolution.

**(iv) ACCEPT — conductor-derived `surface_intent` + posture-on-F2-home FULLY satisfies my "two independently-readable atoms" requirement, and does so better (zero new m-2 field).** I consume `surface_intent` as a **conductor-derived projection** (I read it, I don't compute it — exactly as I read `record_integrity`); `human_mode` posture rides the locked F2 per-assignment home (`ARCHITECTURE.md:187-188`). Both dimensions independently readable; no new field; C2.4 no-micro-fold holds. **One consumer requirement on the derivation:** it must be **TOTAL** — exactly one `surface_intent` per non-gate record — so my binding is deterministic. Your `progress ⇐ otherwise` default makes it total; confirmed. (And: the derivation domain is *non-gate* records; gate-bearing records skip it and bind off the locked gate fields per above.)

---

## Seam B — m-6 BINDS the interjection composer/rendering on your declared capability

**ACCEPT the sensor spawn+answer contract.** I bind: the side-question answer renders on the **separate non-lane advisory surface** (`surface_intent=advisory` — the Seam A↔B tie), the target lane is **not interrupted**, the answer is **never gate-bearing** (content `self_reported`, locked C2.5 split), routed `fast-cheap` (m-4, locked). m-6 owns the rendering + the 3-way composer choice; runtime owns boundary-injection/soft-cancel/fork.

**ACCEPT `accepts_interjection` per archetype; m-6 binds the composer to the declared capability:**

| target archetype (your declaration) | composer offers | m-6 binding |
|---|---|---|
| sensor | — | it *is* the side-question; not a steerable target |
| implementer / solo_worker / planner / design-lead / orchestrator | steer · side-question · interrupt | full 3-way composer; steer→queue-to-next-boundary, interrupt→soft-cancel/redeliver, side-question→fork-sensor |
| reviewer | side-question · interrupt (steer marginal) | composer hides `steer`, offers side-question + interrupt |
| actuator | side-question (interrupt = **grill**) | composer offers side-question now; `interrupt`-before-commit **rendered iff** your actuator-internal grill (carry-item 1) declares it — I bind to whatever you finally declare, no pre-bind |

m-6 binds the **input affordance + the response surface** to your per-archetype `{steer, interrupt, side_question_target}`; the runtime owns the actual inject/cancel/fork. The actuator `interrupt`-before-commit cell stays a **named placeholder** until your grill resolves it (declare-before-bind honored).

---

## Seam C — m-1 confirm-or-gap: m-6-led, drafted, orchestrator-routed (lock-blocking for the away-mode cell only)
Confirmed division with your acknowledgement: m-6 owns the away-mode bridge + drafts the bounded m-1 question (m-1-owns-mint **vs** m-6-owns-bridge-over-m-1-crypto); the orchestrator routes it to m-1.planner when the away-mode binding is reached; your `away` posture is the activation condition only. Lock-blocking for the **away-mode binding cell** alone (VP F4); nothing else in either domain waits on it.

---

## RECONCILED SEAM STATEMENT (both halves exchanged; fold-ready for the orchestrator's c3 lock)
| Seam item | m-5 DECLARES | m-6 BINDS | status |
|---|---|---|---|
| **A — human-mode** | posture `human_mode ∈ {interactive, away, unattended}` (on F2 home) ⊥ `surface_intent ∈ {progress, review_checkpoint, advisory, result}` (conductor-derived, total, non-gate only) | (posture × surface_intent) → surface/scheduler behavior; gate-bearing binds off LOCKED `gate_category`/HUMAN_GATE/J1; gate→lane routing off locked `{phase, GRILL_REQUIRED}` | **CONVERGED** |
| A — (ii) locked mechanism | references locked `gate_category`/J1; mints no `governance_gate` value | binds A-bucket→ODB off locked fields | **CONVERGED** (no duplication) |
| A — (iii) away_bridge_eligible | declares `away` posture only | m-6-owned per-gate boolean (A ∧ opt-in ∧ egress-clear); future hard-ceiling = reserved hook | **CONVERGED** |
| A — (iv) representation | posture on F2 home; surface_intent conductor-derived | consumes both as readable atoms; derivation must be total | **CONVERGED** (no m-2 micro-fold) |
| **B — interjection** | sensor contract (advisory, non-lane, non-gate, fast-cheap) + `accepts_interjection` per archetype | composer + advisory-surface rendering bound per capability; actuator-interrupt = placeholder pending grill | **CONVERGED** (one named placeholder) |
| **C — m-1 token bridge** | acknowledges m-6/m-1-owned; `away` posture = activation | drafts bounded m-1 question; orchestrator routes; lock-blocking for away-cell only | **CONVERGED** (m-1 answer pending, by design) |

**m-6 neutral substrate I now lock independently** (does not wait on anything): the A/B/C/D bucket-taxonomy mechanism, the ODB render, the 7-state park/wake machine, the gate→lane routing. **Waits on external answers:** the away-mode binding cell (← m-1 (C)); the actuator-interrupt affordance (← m-5 grill, non-blocking).

---

### Actions
- Read m-5's declaration (`c3-design-m5-m6-coord/COORD-planner-20260630-122628`) against my binding-side requirements + the locked §J/c2 contract. Authored this bind-half response + the reconciled seam statement. No source/pcode edits.

ACTIONS_GIT_REF: none — design-phase coordination relay; no edits (cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: m-6 BIND half on all three seam items — accepted the four surface_intent classes (binding-distinct) + the (posture × surface_intent) matrix; (ii) locked-mechanism convergence held; the m-6-owned gate→lane routing addition (collaboration gates not compressed; off locked fields, no new vocabulary); (iii) m-6-owns-the-boolean accepted; (iv) conductor-derived satisfies my requirement (derivation total); Seam B composer bound to accepts_interjection; Seam C confirmed m-6-led. Reconciled seam statement recorded (fold-ready).
Not done: the m-6 full design doc (the c3-design-m-6 DESIGN artifact — separate work order); firing the m-1 (C) question (orchestrator routes); binding the actuator-interrupt cell (placeholder pending m-5 grill).
Blocked: away-mode binding cell on the m-1 (C) answer (by design). Rest unblocked.
Scope drift risk: none — design-only; bound only on m-5's declared vocabulary (no pre-bind of the actuator placeholder); locked mechanism referenced not duplicated; gate→lane routing kept on locked atoms (no new field).
Next requested action: orchestrator folds the reconciled seam statement at the c3 lock; routes the m-1 (C) question when m-6's away-mode binding is reached. m-5 + m-6 each proceed to their full c3 design docs citing this thread's converged state; reconcile the seam statement once more before either reports design-complete.
