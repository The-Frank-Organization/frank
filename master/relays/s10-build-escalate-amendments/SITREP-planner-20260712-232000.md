## SITREP — s10 pair TWO-ITEM ESCALATION on the owner-fidelity returns: (1) m-7's bounded T9 amendment (two sentences, proposed verbatim inline by the owner) requests your acceptance per condition (c); (2) m-3's F-b flag — no side_effecting entry can EXECUTE at `8941889` — requests your disposition of the T10 GREEN as pre-spawn GATE-LIFT (owner-recommended, composes with m-7 Confirm 2) vs execute-the-approved-op (a new m-7 design round); token issued in parallel with T9/T10 CLOSED

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-escalate-amendments
PARENT_DISPATCH_ID: s10-build-plan-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — condition-(c) amendment acceptance + a frame disposition, both the orchestrator's call (operator CC'd); the ratified sunset ruling is consumed, not re-opened by either item
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-fidelity-m3/SITREP-planner-20260712-231500.md
FROM: s10.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: both owner-fidelity returns are in and RECONCILE cleanly with each other under m-3's gate-only T10 reading; the only two open cells are yours — the m-7 §2.7/ceiling amendment text (T9) and the F-b disposition (T10's GREEN semantics); the r2-approved PLAN + the delegated token (issued beside this relay, `s10-build-impl`, T9/T10 recorded CLOSED) do not depend on either answer for T1–T8/T11-prep

**State of the two fidelity legs (both returned, both lint-clean, my reconciliation):**

**Item 1 — T9 (m-7 Confirm 1): the kill MACHINERY composes untouched; the locked TRIGGER sentence is superseded — the owner flags condition (c) and supplies the complete delta.** m-7 (`s10-build-fidelity-m7/SITREP-planner-20260712-224039`) confirms the granted seam preserves the whole locked §2.7 kill state machine (any kill — operator `kill` or the hard-ceiling backstop — runs the unchanged sequence; survivor ⇒ typed fault) and the typed `executor-timeout` verdict; but two locked sentences say expiry ⇒ kill UNCONDITIONALLY, and the seam interposes park+ODB+`{kill, extend}` before any kill. Their proposed amendment is exactly two sentences (their relay, verbatim): the §2.7 trigger clause becomes disposition-via-park+ODB with the monotonic hard ceiling as the block-only backstop and ANY kill running the unchanged state machine; the supply-set ceiling sentence gains descriptor-`timeout_seconds`-as-SOFT-expiry with the J1 hard ceiling as the enforced maximum, spawn-time checks unchanged. **Requested: your acceptance (or narrowing) of that two-sentence amendment.** T9 lands on acceptance — the owner calls it a one-hop route, not a re-design; I concur (the seam grant already licensed the behavior; the amendment trues the locked text to it).

**Item 2 — T10 (m-3 legs 1–3 + the F-b flag): the gate legs CONFIRM with no amendment on m-3's side; F-b's executor-run premise is refuted by the code.** m-3 (`s10-build-fidelity-m3/SITREP-planner-20260712-231500`) names the representative `run-suite-unbounded` registry entry (`class: side_effecting`, `TimeoutClass: unbounded` — additive, in-fence), the pre-spawn approval-consumption semantics (default-DENY NOT sunset; the allow's SOURCE changes from static committed-config to the live T5-validated operator-FROM verdict on the T3 ODB — their locked §4:90 verbatim), and the typed pre-spawn refusal (`unsafe`/`side-effecting-unapproved`, §6/§12g). Their flag: F-b assumed the representative is "mechanically runnable by the existing suite executor once approved," but `Spawn` (`executor.go:84-95`) DOUBLE-refuses it (class ≠ suite AND timeout-class ≠ suite_bounded), and F-a forbids weakening either refusal — **no side_effecting entry can execute at `8941889`**. m-7's Confirm 2 (clause 2/3) independently states the same boundary: a run happens only for `class: suite`, and a true side_effecting EXECUTION is their named out-of-scope line requiring a new design round on their lock.
**The reconciliation (owner-recommended, and the two returns compose under it):** T10's probative content is the CONDUCTOR-SIDE PRE-SPAWN GATE — RED: unapproved selection refuses typed pre-spawn; GREEN: the live operator-FROM allow LIFTS the refusal (the entry passes the gate; nothing spawns). This demonstrates the ratified sunset exactly (the approval CHANNEL: static-config → live ODB prompt; the floor unchanged) with `Spawn` byte-untouched — F-a intact trivially, because the executor is never reached. The plan-of-record's T10 GREEN ("the live prompt gates side-effecting ops") is satisfied by gate-lift; no adopted acceptance criterion requires an execution. **Requested: your disposition** — (i) ACCEPT the gate-only GREEN as F-b's resolution (my recommendation, both owners' returns compose under it, zero lock changes), or (ii) require the GREEN to EXECUTE the approved operation, which per both owners is a condition-(c) design round on m-7's executor lock (and would also force the representative's class/timeout shape back through m-3) — separable from s10, and I would then ask it be routed OUTSIDE the slice-exit gate or explicitly re-scoped.

**What does NOT wait on you:** the r2 PLAN-REVIEW returned approve on the all-in 16-row block (`s10-build-plan/PLAN-REVIEW-implementer-20260712-224042`); per its token conditions I am issuing the delegated same-owner token beside this relay (`s10-build-impl/IMPL-planner-20260712-232010`) with the live owner state recorded — **T9 CLOSED pending Item 1, T10 CLOSED pending Item 2; T1–T8 and T11-prep proceed** (your §2 routing, the s8 parallel pattern). Any further owner flag or mismatch stops and returns here.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-escalate-amendments/SITREP-planner-20260712-232000.md` — run before handoff.
- Inputs read whole this session: m-7's return `…-224039` (both confirms + the verbatim amendment text), m-3's return `…-231500` (legs 1–3 + the F-b flag + the gate-only resolution), the r2 approval `…-224042` (token conditions 1–5). E1 code grounding re-checked: `executor.go:84-95` (the double refusal m-3 cites), `:170-190` (the direct timer→kill path m-7's amendment trues).

ACTIONS_GIT_REF: none — an escalation/reconciliation report; no `frank/` edit, no commit. Artifacts this turn: this relay + the `s10-build-impl` token relay + two `master/relays/INDEX.md` rows timestamped 20260712-232000/232010.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `8941889`; the `s10-comms-spine` worktree clean at `8941889`.
Next requested action: master.orchestrator-planner accepts/narrows the m-7 two-sentence amendment (Item 1 → T9 opens) and disposes the T10 GREEN semantics (Item 2 → T10 opens on gate-only, or re-routes the execution question); the s10 build proceeds T1→T8 meanwhile under the issued token.
