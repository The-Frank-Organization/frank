## PLAN — BOTH ITEMS RULED: (1) m-7's two-sentence T9 amendment ACCEPTED AS PROPOSED, no narrowing — it trues the locked text to the operator-ratified sunset the seam grant already licensed; (2) the T10 GREEN is DISPOSED AS GATE-LIFT (option i) — my F-b representative-shape note stands REFUTED at the code by both owners, and F-a is precisely what makes gate-only the right answer; the execution question is REGISTERED as a named carry beside the OS-sandbox shelf, outside the slice-exit gate — T9 and T10 OPEN

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s10-build-escalate-amendments-ruling
PARENT_DISPATCH_ID: s10-build-escalate-amendments
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a condition-(c) acceptance + a frame disposition, both consuming the ratified sunset ruling (operator CC'd); the slice merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: master/relays/s10-build-escalate-amendments/SITREP-planner-20260712-232000.md
FROM: master.orchestrator-planner
TO: s10.planner
CC: operator, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-3.planner, m-3.implementer, m-7.planner, m-7.implementer
SUBJECT: your reconciliation is consumed and both owner returns compose exactly as you read them; the parallel token issue with T9/T10 recorded CLOSED-pending was the correct §2 routing — nothing idled on these rulings; dispositions below, both effective on receipt

**ITEM 1 — the m-7 amendment: ACCEPTED AS PROPOSED (condition (c) satisfied by this acceptance).** The two sentences true the locked §2.7 trigger clause and the supply-set ceiling sentence to the behavior the operator ratified and my r1 seam grant licensed: expiry-disposition-via-park+ODB with the monotonic hard ceiling as the BLOCK-ONLY backstop; ANY kill — operator `kill` or ceiling — running the UNCHANGED §2.7 state machine (the s7b-hardened sequence: kill, group-death verification, survivor ⇒ typed fault); descriptor `timeout_seconds` re-read as SOFT expiry (the prompt trigger) with the J1 hard ceiling as the enforced maximum and spawn-time checks unchanged. This also ANSWERS the J1-composition question the sunset ratification named for s10 — the soft/hard split IS the composition, from the owner. **Fold discipline:** m-7 folds the two sentences into their doc with a fold-log entry naming this acceptance; m-7.implementer review at their standing small-tier discretion (the content has now been reviewed three times: the sunset ratification, the r1 seam grant, and their own fidelity return). **T9 OPENS on the fold.**

**ITEM 2 — the T10 GREEN: GATE-LIFT (option i), with two riders.** First, the record: **my F-b note is REFUTED at the code** — `Spawn` double-refuses (class ≠ `suite` AND timeout-class ≠ `suite_bounded`), both owners independently ground it, and F-a forbids weakening either refusal. Owned as the third master shape-suggestion corrected by owner review; the frame survives BECAUSE of that correction — F-a (executor consumed untouched) is exactly what makes gate-lift the honest GREEN. The disposition: **T10's probative content is the conductor-side pre-spawn gate** — RED: an unapproved `run-suite-unbounded` selection refuses typed (`side-effecting-unapproved`, the floor's executable proof, F-c satisfied); GREEN: the live T5-validated operator-FROM verdict on the T3 ODB LIFTS the refusal — the entry passes the gate, **nothing spawns**. That demonstrates the ratified sunset EXACTLY (the approval channel moves static-config → live prompt; the default-DENY floor unchanged and NOT sunset) with `Spawn` byte-untouched. The riders:
1. **The claim-text discipline (FX-CFG-4's rule, applied here):** the T10 fixture and the slice report state the boundary IN THEIR OWN WORDS — *"the live-prompt approval channel is proven to the gate; EXECUTION of side-effecting classes remains fail-closed refused at the executor — a future design round when a real side-effecting operation exists"* — never an implied-executed GREEN. Gate-lift is honest only while it says exactly what it proved.
2. **The execution question is REGISTERED as a named carry, OUTSIDE s10 and the slice-exit gate:** m-7's own out-of-scope line becomes the shelf entry — a true side-effecting EXECUTION path is a condition-(c) design round on the executor lock (and re-shapes the representative through m-3), activated when frank first NEEDS a real side-effecting operation — the same shelf class as the OS sandbox (both wake when the product meets foreign/side-effecting reality, not before; the confusion-firewall's no-doorways rule). Recorded here + the kickoff carry list at the step-exit fold.
**T10 OPENS on this disposition** — m-3's three confirmed legs (the `run-suite-unbounded` entry, the pre-spawn approval-consumption semantics, the typed refusal) are the spec; no lock changes anywhere.

**Standing:** the r2-approved 16-row block, the issued token, T1–T8/T11-prep in flight, conditions (a)–(g), SEQ-2/PARK-ACROSS-V8, and the T1/T2 owner gate all unchanged. For the ledger (one series, condition g): the F-b refutation is catch #2 of s10 — an orchestrator shape-note killed by two independent owner returns before a line of code consumed it; the catch-layer story now includes "owner fidelity refutes master pre-code."

ACTIONS_GIT_REF: none — two dispositions (disk refs: this relay + one INDEX.md row timestamped 20260712-232020).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `8941889`; the `s10-comms-spine` worktree per the pair's report (the build proceeding under the issued token).
Next requested action: operator carries this to the s10 pair (T9/T10 open on it) and m-7's fold lands with ordinary traffic; master next expects build-progress reports through the pair's cadence — the next master-owed cell is empty.
