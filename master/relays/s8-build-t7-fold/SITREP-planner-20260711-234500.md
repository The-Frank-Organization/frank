## SITREP — s8.planner T7 fold review VERDICT: APPROVED — F1/F2/F3 folded faithfully at 590afae; the E1 machinery row is now lock-true at both rungs; T7 close waits only on the m-7 T5 fidelity verdict

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-t7-fold-verdict
PARENT_DISPATCH_ID: s8-build-t7-fold
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — fold-review verdict under the standing `s8-build-impl` token; no new authority; merge remains operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T7
IN_REPLY_TO: frank .relays/s8/s8-build-t7-fold/REVIEW-FOLD-REPORT-implementer-20260711-233000.md (worktree s8-observe-spine)
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: fold approved on my independent verification — a killed or broken E1 check can no longer reach accepted+self_reported, missing-file citations reject observed-false, the escape guard survives, the locked CheckVerdict shape is untouched; T8 stands approved; T7 closes on the m-7 fidelity verdict; T9/T10 still hard-gated on the catalog bytes

FOLD_REVIEW_VERDICT: approved — F1, F2, F3 all closed as specified. T7's pair-grain review is now complete; its CLOSE remains conditioned solely on m-7's pending T5 executor-fidelity verdict (`s8-build-fidelity-m7/COORD-implementer-20260711-220000`), per the standing condition.

### Verified at my seat (worktree `s8-observe-spine@590afae`)

1. **Fold scope held:** the `59f01df..590afae` diff touches exactly the FOLD_SCOPE files (`checks_base.go`, `registry.go`, the two named fixtures, `.relays/s8/` bookkeeping) — all inside the 29-entry fence; `gate.go` needed no change, as the fold shape anticipated.
2. **F1 closed:** `git-status` now splits `ctx.Err()` (→ `check-machinery-git-status-timeout`, timing `timeout`) from other command failure (→ `check-machinery-git-status`), both through `machineryVerdict` — outcome `skipped`, never the policy-only `unsafe`. `machineryFaultDetail` flags both symbolic families {`executor-`, `check-machinery-`}, so the gate's fault edge (authority `held`+escalate / non-authority `rejected` naming `observe-machinery-fault`) now covers both rungs. The locked m-7 `CheckVerdict` boundary shape is byte-untouched — the flag stays m-3-internal, exactly as required.
3. **F2 closed:** `read-file` splits `os.IsNotExist` (observed absence → `Fail`, `read-file-absent`) from other IO errors (→ `check-machinery-read-file`).
4. **F3 closed with the escape guard intact:** the final component may be genuinely absent (Lstat-confirmed ENOENT) — then the PARENT is symlink-resolved and containment-checked; an existing or broken escape symlink at the final component still refuses. A valid missing-file citation now lands `rejected`/`observed-false`.
5. **The test seam is safe:** `GitExecutable`/`ReadTimeout` are conductor-constructed `RegistryEnv` values, normalized at registry construction (empty → `git`, non-positive → the ratified 5s default), unreachable from lane params — no I-PH surface change; it lets the fixture drive the REAL `CommandContext` timeout path deterministically rather than mocking it, which is the stronger proof.
6. **Decision-row pairing:** each new machinery leg is paired with a non-authority no-vantage control that stays `accepted` — the two rows can no longer re-conflate silently, at either rung.
7. **Batteries re-run at my seat:** focused `TestS8(Decision2|CheckRegistry|ReadFile|GitStatus|E1)` green; full uncached `go test ./... -count=1` → 25 packages ok, zero FAILs; worktree clean at `590afae`. INV-CATALOG bytes unchanged by the fold (no new counted site), consistent with the report.

### Standing gates (unchanged)
- **T7 close** = m-7's T5 executor-fidelity verdict, still unanswered. An exact correction folds on this chain; more than exact → condition (c) stop.
- **T9/T10** stay hard-gated on the m-7-reviewed s8-v1 catalog bytes (`…-213500`), also still unanswered.
- **Operator:** the two m-7 requests are the ONLY things now standing between this build and T9 — everything T1–T8 at pair grain is approved. Re-flagging the carry as the single critical-path item.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t7-fold/SITREP-planner-20260711-234500.md` — run before handoff (result in the inline pointer).
- Read/run at my seat: the full fold diff (`checks_base.go` classification splits + `scopedPath` parent-resolution logic; `registry.go` env seams, normalization, `machineryFaultDetail`); the paired fixture legs; focused + full batteries (25 ok, zero FAIL); `git status --short` clean.

ACTIONS_GIT_REF: none — a fold-review verdict; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260711-234500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine` clean at `590afae`; `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer AND carries the two pending m-7 requests to m-7.planner (the sole critical path); on the m-7 fidelity verdict T7 closes; on the catalog bytes T9 begins under the standing token.
