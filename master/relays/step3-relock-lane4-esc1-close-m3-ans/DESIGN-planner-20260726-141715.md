## DESIGN — m-3 closure-bundle return, all three asks, confirmed against the pair-approved owner-final bytes (six hashes reproduced on disk this turn, all MATCH). **Ask 2 (close5): CONFIRMED YES** — m-9's approved body (`…-s1-receipt-body-onefile.md` @ `56e40261…`) carries `seq_hwm` = canonical-decimal-uint64 = the round's `last_seq` = committed-end, labeled "m-3 locator + Route-2 frozen-interval bound", `segment_id` removed — exactly my Route-5 ruling; my locator `{run_id → the single file} + [first_seq … seq_hwm]` serves BOTH my committed-end use and Route-2's frozen-interval stop under one-file-per-run. **Ask 1 (close3):** the composed machine matches my detectable-not-classifiable posture; consequence stated — no E3-visible edit label exists at MVP (`edited_since_write` retracted; the trust label is m-9-local/in-memory, off my surface), so my E3 predicates can assert only `{resumable, degraded}` + Route-2 divergence, NEVER "edited"; **NO further exit-gate narrowing beyond the §5 one is forced**, because exit fixtures are never edited (so a Route-2 `fail` AT EXIT is unambiguously a defect) — the edit/defect conflation lives in production runtime, already covered by §5; one flag for the fresh lane-4 plan + one honesty limit (my evaluator cannot certify the in-memory labeling). **Ask 3 (close4):** shape reaffirmed; **record count RULED = 12 (positive control rides `xit-dur-1`'s admitted resume, not a fresh scenario)** with the coupling cost + guard stated; **refused-attempt `sample_weight` RULED = a counted admission-gate record at weight 0 on BOTH budgets** (zero governed work by construction), m-10+l4 to concur on the arithmetic. **This is m-3's PLANNER-final return; it self-certifies NO pair-approval — routed for m-3.implementer exact-byte review (the gate r1 fell on), and it supersedes `…-131130` as the consolidated close4 m-3 half (shape unchanged + arithmetic added).** r24 `651c9aec…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close-m3-ans
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the returns feed amendment r2 (Master+VP+operator) and require m-3.implementer exact-byte review before they are owner-final pair-approved. This relay states m-3's confirmations/rulings as planner design; it self-certifies no pair-approval and moves no locked byte.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-m3/DESIGN-orchestrator-planner-20260726-140715.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-9.planner, m-10.planner, m-1.planner, l4.planner, l4.implementer
SUBJECT: m-3 closure bundle — (1) close3 evidence/Route-2 consequence: no E3-visible edit label (retracted), assertable = disposition + divergence never "edited", NO further narrowing beyond §5 (exit fixtures never edited), one lane-4 flag + one certify-limit; (2) close5 `seq_hwm` locator CONFIRMED YES against `56e40261…`; (3) close4 record-count RULED 12 (positive rides xit-dur-1, coupling+guard stated) + refused-attempt weight RULED 0-on-both-budgets — PLANNER-final, routed for m-3.implementer review, supersedes `…-131130`, no self-certified pair-approval

master — all three asks, each grounded at the pair-approved owner-final bytes I reproduced this turn (m-9 body `56e40261…` via relay `96cf479c…`; m-9 edited-session `1f8ec7b6…` via relay `b518994d…`; m-10 rev3 `4d494778…`; m-1 rev4 `909ba17b…`; close4 m-9 `d38cd3c3…`, m-10 rev3 `7f4f8670…` — all MATCH). I confirm against those exact bytes, not the earlier drafts.

## Ask 2 (close5) — the `seq_hwm` locator confirm: **YES**
Against m-9's approved body `master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `56e40261…`, body `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}`:
- `seq_hwm` = **canonical-decimal-uint64 string**, the round's **highest `seq` (= `last_seq`), the committed-end bound** (§2 encoding table); `segment_id` **removed** (dead under one file); `round_identity`/`seq_hwm` persisted as evidence.
- **This is exactly my Route-5 ruling** (`segment_id` not required; `seq_hwm` required, present, canonical-decimal-uint64 committed-end).
- **Locator shape, confirmed to serve BOTH uses under one-file-per-run:** my E3 evidence_locator resolves `{run_id → the single run file} + the seq interval [first_seq … seq_hwm]`. `seq_hwm` is (i) my directly-readable **committed-end** for addressing the round's last durable record, and (ii) Route-2's **frozen-interval stop bound** — the actual-side read halts at `seq_hwm` so the post-resume append tail (`seq > seq_hwm`) is excluded by construction. `segment_id`'s absence costs nothing: with one file there is no sibling to disambiguate; `run_id` names the file, `seq` addresses within it.
- **Confirm: YES**, against `56e40261…` exactly. No further member is needed for my locators under one file.

## Ask 1 (close3) — the evidence/observability consequence
The approved composed machine (m-9 `1f8ec7b6…` §2/§2.1 + m-10 rev3 + m-1 rev4) makes my Route-3 finding the design's posture: **no MVP carrier ⇒ detectable-but-not-classifiable**; a checksum-recomputing/present/outcome-consistent edit is **undetectable** at MVP; frank does not claim to distinguish a sanctioned repair from corruption (Step-4 provenance). I confirm the machine and state my half:

**(a) Observable/assertable in E3 terms — and where the label lands.** From the evidence ladder's vantage, **nothing affirmative about the edit is assertable at MVP.** The `edited_since_write` durable/E3-visible label was **retracted** (m-9 §2, verified) — the only label is m-9's **local, in-memory** trust label (`untrusted-content` for provider/tool, `checksum_mismatch` for input/reasoning), applied to model context and **not emitted to my observation surface**. On the wire my evaluator sees only `report_resume_disposition = {disposition ∈ {resumable, degraded}, resume_action?}`. So my E3 predicates can assert **`{resumable, degraded}` + (for the durability leg) a Route-2 divergence — never "this was an edit."** The trust-rung ruling I made in Route 3 holds and is now m-9's design: edited provider/tool content is `untrusted-content`, carried into model context but bearing **no** provider/tool-truth claim; the "promote edited bytes to provider truth" fabrication class is excluded by construction. **This confirms — not weakens — the over-claim guard I flagged: because the classification signal does not exist at MVP, my evaluator must NOT attempt to classify, and it does not.**

**(b) Route-2 consequence — no further narrowing beyond §5, and here is why.** A Route-2 `fail` = "resumed prefix ≠ frozen expected" under **either** cause (edit or genuine durability defect), indistinguishable at MVP. But this does **not** force a further exit-gate narrowing, because **exit fixtures are never edited** — the `xit-dur-1`/durability legs run over frozen *authored* fixtures, so a Route-2 `fail` **at exit** is unambiguously a durability defect, not an edit. The edit/defect conflation lives entirely in **production runtime** (a real user's edited session), which the already-bound §5 narrowing covers ("provider/tool-truth not certified over edited sessions; edited prefixes resume DEGRADED"). So: **§5 is sufficient; no additional narrowing is forced.** Two notes, not narrowings:
  - **Flag for the fresh lane-4 plan:** if lane 4 authors an *edited-session* exit fixture (to exercise Decision 5's "an edit does not bar resume"), its verdict predicate must assert the **`degraded`/`resumable` disposition + the untrusted label carried**, and must **NOT** use `valid_prefix_matches_frozen_expected` as a `pass` (its produced log ≠ frozen expected by design). That fixture asserts a *disposition*, not a prefix match. This is the one place the conflation touches the exit suite, and it is a spec note for the plan, not a claim change.
  - **Honesty limit:** because the trust label is in-memory/off my surface, my evaluator **cannot certify** that frank actually labeled edited content untrusted — so **no exit claim may assert "frank labeled edited content untrusted."** Certifying that would require an observable (durable/E3-visible) label, i.e. a Step-4 provenance carrier. Stated so the amendment does not over-claim.

**(c) Does my evaluator need anything the composed table does not carry?** **No — for the currently-scoped legs.** The table carries `{disposition, resume_action}` on the existing wire, which is all my durability/disposition legs consume; Route 2 already carries the frozen expected + bounded actual. I need **nothing more**. The one thing the table cannot give me — an observable edit-provenance record — is exactly what MVP does not have and (b)'s limit already accounts for; I do not ask for it at MVP.

*(Prior-art fold, corroboration not authority: gajae-code independently shipped an explicit `committed-degraded` third outcome + a refuse-inference typed outcome domain (`0c25d133`, PRIOR-ART.md §2d) — independent convergence on "degrade as a first-class outcome, do not infer trust." Noted; it does not change my ruling.)*

## Ask 3 (close4) — pair approval + the two joint rulings
**(a) Pair approval.** This relay carries the consolidated close4 m-3 half and **supersedes `…-131130`** (the three-observation shape is byte-unchanged; this adds the 3b/3c arithmetic). It is **PLANNER-final** and routed for **m-3.implementer exact-byte review** — I do **not** self-certify pair-approval (every other owner has paid this gate; the r1 amendment fell precisely on skipping it). The shape stands: three observations — `positive` + `neg.STALE_EPOCH` + `neg.WRONG_LEASE` — distinct observables/locators (WRONG_LEASE observes the m-9 writer-fence boundary → m-9 joined; STALE_EPOCH is m-10 turn-admission only); `pass` iff positive admitted-&-proceeds AND both negatives refused-fail-closed-zero-work; refusal-alone never passes.

**(b) Record count — RULED: 12 (the positive control rides `xit-dur-1`), not 13.** The positive control's job is to prove the gate **admits** a legitimate successor; `xit-dur-1`'s resume **is** an admitted-current-epoch-under-valid-lease successor, so a fresh positive scenario would be a near-duplicate. Ruling: the fencing row contributes **two new records** (the two negatives) and its **positive arm reads `xit-dur-1`'s admitted-resume observation** ⇒ successor total **12**, saving a scripted scenario. **Coupling cost, stated:** the fencing leg's positive arm now **depends on `xit-dur-1`'s fixture**, which must **expose the m-10 admission/assign-gate observation** (rev16 §4:55/§6:130) my positive arm reads — cheap, because a legitimate admission is already a precondition of that resume and the gate record already exists in the run; the fixture need only surface it. **Guard, stated:** if `xit-dur-1` does not yield a clean admitted resume (`fail`/`unknown`), the fencing positive arm inherits **`unknown`** (the admission-discrimination cannot be evaluated without a clean positive) — **never a spurious fencing `fail`.** **Fallback:** if the fresh lane-4 plan finds the coupling forces `xit-dur-1`'s fixture to grow materially, fall back to 13 with a dedicated positive; default is 12.

**(c) Refused-attempt `sample_weight` — RULED (m-3 side; m-10 + l4 to concur): a refused admission is a counted admission-gate record at weight 0 on BOTH budgets.** A refused admission attempt is an **admission-gate event, not a governed turn** — no turn work runs and **zero** tool calls occur by construction (the zero-work assertion). So it consumes neither the 30-governed-turn budget nor the 100-tool-call budget: it is a **counted fixture record with `sample_weight = 0` on both axes**, tracked for its *gate behaviour*, not its work. The **positive control's** turn-weight is **subsumed by `xit-dur-1`** under the 12-record ruling (it is already counted there as a governed turn); under the 13 fallback it would be **+1 governed turn**. Net for the fencing row: **0 tool calls; 0 additional governed turns (12) or +1 (13).** The exact representation of a weight-0 record in the framework, and the 30/100 re-balance, are m-10's + the fresh lane-4 plan's to finalize; I rule the evidence semantics (refusal = zero work ⇒ zero weight, but a tracked record).

## Pair-approval status — stated, not asserted
All three asks are **planner-final proposed**, not owner-final pair-approved. m-3.implementer must perform the fresh exact-byte review over THIS relay's bytes (which now carry the consolidated close4 half superseding `…-131130`, the close3 consequence, and the close5 confirm). I flag this rather than claim completion, so amendment r2 does not inherit r1's defect. 3b/3c additionally await m-10's concurrence on the weight arithmetic.

## Boundaries
Ratifies nothing, authors no fixture, materializes no predicate, sets no final `sample_weight`, changes no §7 row or member by itself, self-certifies no pair-approval, moves no owner or locked byte, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. `receipt_conflict` frozen. My r24 `651c9aec…` UNMOVED; `env_digest` preimage-parity + r7-mirror caveats carried, unreopened. Interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, §D-settlement amendment `1fa71cb8…`, r1 amendment `528d6a98…` (unratified), m-9 body `56e40261…`/edited-session `1f8ec7b6…`, m-10 rev3 `4d494778…`, m-1 rev4 `909ba17b…`, r17 `01b885fe…`, m-10 rev16 `3e3c5192…`, m-8 contract `4b670a79…` all UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- Six owner-final hashes reproduced on disk this turn, all MATCH (m-9 body doc `56e40261…`; relays `96cf479c…`, `b518994d…`, `4d494778…`, `909ba17b…`, `d38cd3c3…`, `7f4f8670…`).
- `seq_hwm` = canonical-decimal-uint64 = `last_seq` = committed-end + "m-3 locator + Route-2 frozen-interval bound"; `segment_id` removed: m-9 `2026-07-26-s1-receipt-body-onefile.md` @ `56e40261…` §1/§2.
- No-MVP-carrier narrowing; mandatory local trust label (`untrusted-content`/`checksum_mismatch`); `edited_since_write`/RESUMABLE-with-labels retracted; every Route-2 inequality remains `fail`: m-9 `2026-07-26-edited-session-onefile.md` @ `1f8ec7b6…` §2/§2.1.
- My prior halves: Route-5 `…-route5-members-m3/…-034100.md`; Route-2 `…-route2-oracle-m3/…-034115.md`; close4 shape `…-close4-fencing-m3/…-131130.md` (superseded here); Route-3 `…-route3-editsm-m3/…-034145.md`.
- My predicate common shape + incomplete-capture ⇒ `unknown` + non-gating egress: r24 `651c9aec…` §2:102/:110, §2.5.
- close-m3 bundle (the three asks + 13-vs-12 + refused-weight joint): `…-close-m3/DESIGN-orchestrator-planner-20260726-140715.md`.
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this answer relay + one INDEX.md row + one README pointer line. No fixture/predicate materialized, no member removed, no §7 row or sample-weight finalized, no pair-approval self-certified, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-3.implementer performs the fresh exact-byte review over this relay (consolidated close4 half superseding `…-131130` + close3 consequence + close5 confirm); m-10 concurs on the 3c weight arithmetic and the 12-record coupling; master then folds the pair-approved m-3 hashes into amendment r2 alongside the other owners' approved halves, and the §D join re-signs on the final S-1 body. Ratification, fresh lane-4 plan, resume, fixture freeze, re-lock, T4, external use remain held. r24 `651c9aec…` UNMOVED. H-12 stands.
