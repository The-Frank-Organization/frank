## DESIGN — amendment **r2** routed for VP exact-byte review: `master/STEP-3-D1-RESCOPE-AMENDMENT.md` @ **`ed7e22af77ac5e24b0591a2edeb14c2cac1a8f9dc25c4deb92150f59aec91b77`**, superseding rejected r1 `528d6a98…` with **all nine VP1 findings folded.** The structural difference from r1, stated up front: **§0 binds ONLY pair-approved owner-final artifacts** — every normative input carries its implementer exact-byte approval, the §D join is **mutually re-signed** over the final body (both halves + consistency confirm), and the two joint arithmetic rulings carry **m-10's AND l4's explicit concurrences.** Nothing planner-only is normative; the route-round returns are downgraded to cited evidence. **10 of 11 cited full hashes recomputed on disk this turn and matched; the eleventh is r1's own hash, cited as superseded (its bytes were replaced by r2 in the same file, per the plan-revision precedent).**

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: review-only
DISPATCH_ID: step3-relock-lane4-esc1-amend-2
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-amend-vp-1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — VP exact-byte review then operator ratification; master proposes and may not self-ratify.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-amend-1/DESIGN-REVIEW-orchestrator-reviewer-20260726-040848.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-1.planner, m-1.implementer, m-3.planner, m-3.implementer, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, l4.planner, l4.implementer
SUBJECT: Amendment r2 `ed7e22af…` for exact-byte review — all VP1 findings folded: pair-approved-only binding set (F1) · honest no-carrier claim replacing "already total" (F2) · operator edit-surface ruling + BR-INV′ (F3) · one composed total disposition machine, no universal edited⇒degraded (F4) · authored S-1 body + re-signed §D join (F5) · fencing shape with m-9 joined + three observations (F6) · SEVEN legs / TWELVE records stated directly (F7) · §7 inherits §5's risk acceptance (F8) · "relaxes no receipt_conflict rule" (F9); plus edited_since_write retraction, m-3's honesty limits, the 12-record hard-stop precondition, and no tamper-evidence claim per PRIOR-ART §2d

## Finding-by-finding disposition of VP1

| finding | r2 disposition |
|---|---|
| **F1** unapproved inputs | §0 binds twelve pair-approved artifacts, each with its approval relay named; route-round planner returns are cited evidence only |
| **F2** no carrier | §4a states the honest narrowed claim — no carrier exists, none added; checksum-recomputing edits undetectable, documented; both prior "already total" claims were withdrawn by their own authors in the approved successors |
| **F3** edit surface | operator-ruled journal-only; m-10's store effectively uneditable; m-1 BR-INV′ rev4 pair-approved; recovery = post-product carry |
| **F4** non-composing returns | §4b is ONE total machine from the approved docs: structural/missing ⇒ degraded; well-formed checksum-mismatch ⇒ resumable + in-memory label; undetected ⇒ clean (accepted); **no universal edited⇒degraded** — the pair review killed that claim and r2 carries the corrected grain |
| **F5** premature body | §2 binds the m-9-AUTHORED body `56e40261…`, m-10's byte-confirm `92c9b3a8…`, and the mutually re-signed §D join (`ef72c732…` × `9f8c290f…`, consistency `edd2351e…`), naming the superseded fragment |
| **F6** open owner + shape | m-9 JOINED (fence observable `d38cd3c3…`); shape = three independently-locatable observations, both negatives mandatory for distinct reject mechanisms |
| **F7** stale cardinality | **seven legs / twelve records stated directly**; no revived historical defect; the 12-count is precondition-bound with a hard-stop-13, m-10 + l4 concurring (`f3e0c2ae…`, `234daf6f…`) |
| **F8** §7 safety overclaim | §7 now inherits §5's risk acceptance verbatim: completeness + fence prove record completeness and single-writer identity ONLY; the absence-based stale-summary claim is superseded, not re-secured |
| **F9** boundary wording | "relaxes no `receipt_conflict` rule" — in §4d and §8 |

## Also carried, beyond the findings
- **`edited_since_write` is RETRACTED** in the approved bytes — the label is in-memory only, so §4c adds m-3's pair-approved honesty limit: **no exit claim may assert "frank labeled edited content untrusted."**
- **No tamper-evidence claim anywhere** — the claim is formally retired (PRIOR-ART.md §2d); §1 says so explicitly.
- The **fresh-plan propagation list** now includes the Status-line `:3` sweep (your VP4 note), the `xit-dur-1` regeneration exposing the assign-gate observation, and the edited-fixture disposition-not-prefix-match spec note.
- The operator's **named re-entry test** for chaining and the **post-product recovery carry** are recorded so neither is re-derived nor silently lost.

## What I would review hardest
1. **§0's approval-chain integrity** — that each named approval genuinely covers the exact cited artifact (the m-9 doc hashes ride their approving relays' `DESIGN_DOC_SHA256`).
2. **§4b/§4c consistency** — the composed machine and the honesty limits against `1f8ec7b6…`'s exact bytes.
3. **§6's precondition** — whether "expose the assign-gate-open-at-the-current-tuple observation" is stated tightly enough to be the binary test m-3's hard-stop requires.

## Boundaries
Routes a proposal. Ratifies nothing, edits no lock constituent, moves no lock hash, relaxes no `receipt_conflict` rule, adds no carrier, authors no fixture/manifest, patches no plan byte, resumes no held member, issues no PLAN/T4, touches no `frank/` path, permits no external use. All §0 governing hashes UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- r2 @ `ed7e22af77ac5e24b0591a2edeb14c2cac1a8f9dc25c4deb92150f59aec91b77` (measured after writing). **10/11 cited full hashes recomputed and matched this turn**; the eleventh is r1 `528d6a98…`, cited as superseded — bytes replaced in-file per the plan-revision precedent, recorded in the VP1 review for posterity.
- The twelve §0 artifacts + approvals verified present; the two m-9 doc hashes verified via prefix + their approving relays.
- Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch + one INDEX.md row + the r2 rewrite of `master/STEP-3-D1-RESCOPE-AMENDMENT.md` (master's own unratified proposal file). No lock constituent edited, no owner byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: the **VP** performs exact-byte review of r2 at `ed7e22af…`. On approval the **operator** ratifies; master then authors the fresh lane-4 plan revision for VP review, the resume binds that approved hash, and lane 4 authors the two negative fencing records + regenerates `xit-dur-1` per §6/§8. Fixture freeze, re-lock, T4, external use remain held.
