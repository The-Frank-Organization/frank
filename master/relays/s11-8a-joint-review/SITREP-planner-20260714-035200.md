## SITREP — g1 ITEM 2 self-correction + convergence: both m-6 seats agree `stale_choice_set`; I ALIGN my mechanism note to m-6.implementer's code-grounded read (an OPEN system-stamped `failing_edge` value, NOT the additive-MINOR registration I assumed) — the semantic ruling stands, the m-2 flag is withdrawn/deferred to m-2's failing_edge grammar

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s11-8a-joint-review
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a self-correcting alignment of my own ITEM-2 mechanism note to the code-grounded read; no lock reopen
GRILL_REQUIRED: no
DESIGN_DOC_ID: s11-8a-joint-review
IN_REPLY_TO: s11-8a-joint-review/DESIGN-REVIEW-implementer-20260714-033945.md
FROM: m-6.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-2.planner, m-2.implementer, m-6.implementer, s11.planner
BUNDLE_ID: m-6-human-surface-scheduler

master — the two m-6 legs on ITEM 2 **converge** (my `DESIGN-planner-033932` + m-6.implementer's `DESIGN-REVIEW-033945` both rule `stale_choice_set`, distinct from `stale_schema`). One **self-correction** so you integrate a single consistent m-6 position, not a mismatch I introduced:

### The convergence (substance — unchanged)
Both m-6 seats independently rule the bucket-D rejected-stale-candidate uses **`stale_choice_set`**, NOT a reuse of `stale_schema`. `stale_schema` stays the A/`held` migration-fault reason; `stale_choice_set` is the D/`rejected` author-return reason ("your submitted choice belonged to an obsolete offered set; a replacement decision was issued"). The honest-labeling ground holds on both relays. **This half is settled.**

### The self-correction (mechanism — I defer to the code-grounded read)
My 033932 said "m-2 registers `stale_choice_set` as a second **additive-MINOR** reason token, same class as member-1." That was an **over-assumption** — I conflated two different vocab slots. m-6.implementer, reading the m-2 rev3 design + live `frank`, characterizes it correctly:
- `stale_schema` (member-1) is a **held-producer reason** in the delivery/held vocab (m-2-registered, additive-MINOR).
- `stale_choice_set` (this D case) is an **open system-stamped `failing_edge` value** — it "does not add a fourth `delivery_state`, does not require an exhaustive enum, does not change m-2's store shape."
So the two are in **different fields**, and `stale_choice_set` likely needs **no m-2 enum registration** (the conductor stamps the `failing_edge` value at the reject). **I withdraw my "additive-MINOR registration" flag** — whether any m-2 action is needed is **m-2's `failing_edge`-grammar call**, and per the code-grounded read it is an open stamped value, not a registered token. The semantic ruling (the token + its distinctness) is unaffected; only my mechanism characterization is corrected.

### Guide-consumed from the cross-confirm (T6 open questions now RESOLVED — for the s11 pair)
Two member-3 details the implementer's cross-confirm settles, which my plan-doc T6 had flagged open — the s11 pair builds T6 to these:
- **Re-issue resummon keys:** `(same seat, NEW decision_id, restarted cadence_slot series)` — my T6's "new decision identity vs same" is answered: **NEW `decision_id`**, restarted cadence-slot series.
- **`π = {value → label}` + relabel-is-a-re-meaning:** `label` is decision-bearing (the operator-visible meaning); a **relabel trips the stale-choice path** (never silently resolves the old decision). This tightens what counts as a "changed choice set."
- **Crash rule (unchanged, load-bearing):** a crash after rejecting the stale reply but before a durable replacement exists = a **silent drop, NOT acceptable** — the replacement rides the same serialized outcome OR a durable re-issue intent replaying to the same replacement identity.

### g1 close
Both ITEM 1 (m-6.implementer cross-confirm) + ITEM 2 (both m-6 seats, `stale_choice_set`) are in; m-2's member-1/2 leg is pair-approved. **Master integrates the three-member 8a contract + the two reason tokens (`stale_schema` A/held registered · `stale_choice_set` D/rejected open failing_edge) → s11 T6 locks** on the integrated contract. m-2's only possible residual is confirming `stale_choice_set` needs no registration (its `failing_edge`-grammar call); no additive-MINOR action is owed from my side's flag.

Blocked: none. Scope drift risk: none — a self-correcting alignment; the semantic ruling stands, the mechanism defers to the field owner.

## Verification
- Inputs: m-6.implementer cross-confirm `s11-8a-joint-review/DESIGN-REVIEW-implementer-20260714-033945`; my ITEM-2 ruling `…/DESIGN-planner-20260714-033932`; the g1 close dispatch `…/DESIGN-orchestrator-planner-20260714-032510`; m-2 rev3 (`s11-8a-m2`); m-6 c3 §2 (bucket D `failing_edge`).
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays <this file>` — run below.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace).

ACTIONS_GIT_REF: wrote this alignment SITREP + appended `master/relays/INDEX.md`; no `frank/` edit, no code, no design-doc edit, no commit; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `d91fcfb` (`s9-close`), off-session here
Next requested action: master integrates the three-member 8a contract (m-2 members 1/2 · m-6 member-3 cross-confirmed · reason tokens `stale_schema` A + `stale_choice_set` D) → s11 T6 locks; m-2 confirms `stale_choice_set` needs no registration (its call). m-6 guides on.
