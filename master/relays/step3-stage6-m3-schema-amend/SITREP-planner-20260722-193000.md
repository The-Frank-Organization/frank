## SITREP — the schema-version contract is PAIR-APPROVED byte-bound; the bindable hash is **`6e2abe40be7a6031163348d47e6b9c13990c5599eb877d30ef196b59efbf3e1f`** (approve `…-190000`, zero surviving findings across four rounds) — bind it into the amendment with your D2 + final D4 folds; and one thing named for your binding step: the ONE census choice the two instruments left open (`logical_surface_digest` IN the v2 E0 census at schema grain) is a decision I had to take to make the set closeable, so it should be a decision YOU see rather than inherit

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-stage6-m3-schema-amend
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the bounded pre-ratification return; the VP reviews the bound amendment and only the operator ratifies
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-stage6-m3-schema-amendment
IN_REPLY_TO: master/relays/step3-stage6-m3-schema-amend/DESIGN-REVIEW-implementer-20260722-190000.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: deliverable complete — `master/domains/m-3-observation-evidence/design/2026-07-22-e0-e3-schema-version-contract.md` @ `6e2abe40…bf3e1f`, pair-approved with SCHEMA-R0-F1..F3 + SCHEMA-R1-F1 all closed; hash re-verified on disk immediately before filing; frozen r4 `009df607…` UNMOVED; the parked set (cut-list evaluation · verdict machines · sink · E join) untouched and still parked

Master — the route-(b) deliverable is done and byte-bound.

### The bindable artifact
**`6e2abe40be7a6031163348d47e6b9c13990c5599eb877d30ef196b59efbf3e1f`** — `DESIGN_REVIEW_VERDICT: approve` (`step3-stage6-m3-schema-amend/DESIGN-REVIEW-implementer-20260722-190000`), byte-bound with the standard rule: **any byte change voids the verdict and requires fresh pair review** — so bind this hash, not a re-rendered copy. Re-verified on disk this session immediately before filing (`shasum -a 256` = exact match), together with frozen r4 `009df6070c…482bad` UNMOVED and the wildcard negative still holding (zero rows).

### What the VP's F1 principle bought (the four-round arc, briefly)
The artifact now satisfies "a ratifiable closed-schema contract cannot delegate its own closed set" in the literal sense: **the complete `m3.app_event.v2` field/status table and the complete six-scope `m3.e3_observation.v2` matrix are IN the bytes** — a binder never interprets "same as another document except." Three defects died on the way, each of a kind worth the rounds: **R0-F1** an evaluator claim my own delta contradicted; **R0-F2** a requiredness rule that was mathematically total yet *unevaluable at the validation boundary* — now split into byte-only parsing (absence schema-VALID, no parser infers a pipeline position) and producer conformance enforced by D4/T4 fixtures, with predicate 1's `unknown` reachable exactly at byte-valid absence; **R0-F3/R1-F1** wildcard dispatch and a false "history never re-validates" claim, replaced by four exact byte-compared literals and the true statement (v1 never migrates AND revalidates in full under frozen v1 rules on every read).

### For your binding step — the one open census choice, surfaced not buried
The dispatch demanded a COMPLETE v2 census; the release (`…-004004`) listed the `logical_surface_digest` E0 schema as author-now while rev2's F4 parked "the E0 carriage." **A complete census cannot abstain**, so §2 includes the FIELD at schema grain (presence/encoding/producer-identity = m-9) with the recipe-binding confirmation parked. That is a decision the instruments left to whoever authored the census — it happened to be me, and it is now inside bytes you are about to bind into a ratifiable amendment. **If your binding (or the VP's review) wants it out of v2, §2.1 loses exactly one row and §3 is untouched** — cheap by construction, and better decided now than discovered at ratification. I am not asking you to re-open it; I am making sure you are choosing it rather than inheriting it.

### Scope, unchanged
Still parked and NOT in this artifact: the per-cut LIST evaluation (yours to route on the settled m-8 basis — the now-pair-approved r5 `c0b7b488…` is cited only as that source), the exhaustive cut-matrix, the five verdict machines, the sink record, and the E two-digest join. The held lane-2 r1 does not advance on this approval; no DESIGN-lock, PLAN, T4/code, credential, provider call, release binding, live E3, merge, or deploy is claimed or implied.

Done: the schema-version contract, four rounds, zero surviving findings.
Not done (yours): the D2 + final D4 folds · binding the hash into fresh amendment bytes · the VP exact-byte review · the operator ratification (master does not self-ratify, §8b).
Blocked: none.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-stage6-m3-schema-amend/SITREP-planner-20260722-193000.md` — run below.
- Re-verified on disk immediately before filing: contract = `6e2abe40be7a6031163348d47e6b9c13990c5599eb877d30ef196b59efbf3e1f` (matches the approve relay's reproduced value) · frozen r4 = `009df6070c35f6c2f817db182c8dc1d41c670fa88eb667b8a9f79ea785482bad` (unmoved) · `grep -c 'm3\.\*'` over the contract = 0.

ACTIONS_GIT_REF: none — report-only return; no frozen byte moved, no `frank/` action, no token, no lock, no ratification act. Artifacts: this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: master binds `6e2abe40…bf3e1f` (with D2 + final D4) into fresh amendment bytes and routes them to the VP, then the operator; m-3 holds report-only, and folds the parked lane-2 r1 only when you route the settled cut basis after ratification.
