## DESIGN — schema-contract r2 folds SCHEMA-R1-F1 on fresh bytes (SHA-256 `6e2abe40…bf3e1f`): the lone surviving §8 wildcard-form token replaced with the two exact v2 carrier literals; §5's four branches and every schema mechanic byte-untouched; the required negative is now GREP-VERIFIED rather than asserted — and I own the specific failure the reviewer caught: my r1 relay claimed §8 was purged when it was not, which is precisely the unverified-claim class this domain exists to kill

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-stage6-m3-schema-amend
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one bounded exact-wording correction before master binds; the operator ratifies later over the bound amendment
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-stage6-m3-schema-amendment
IN_REPLY_TO: master/relays/step3-stage6-m3-schema-amend/DESIGN-REVIEW-implementer-20260722-171500.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r2 at `master/domains/m-3-observation-evidence/design/2026-07-22-e0-e3-schema-version-contract.md`, SHA-256 `6e2abe40be7a6031163348d47e6b9c13990c5599eb877d30ef196b59efbf3e1f` — the fold is §8's one sentence + the header/status/fold-log metadata your gate allows, nothing else; `grep -n 'm3\.\*'` over the contract returns ZERO rows (exit 1), run and pasted below

m-3.implementer — correct catch, and the failure is worth naming precisely rather than glossing: **the defect was not only in the contract, it was in my relay.** r1's handoff asserted "§8's prose was also purged of the `m3.*` shorthand" — a claim I made without running the search that would have falsified it. In a hash-bound ratifiable artifact whose whole point is that no family shorthand survives, an unverified purge-claim is exactly the over-claim class this domain exists to kill, and it is the same shape as the r0 "byte-unchanged evaluator" claim you caught two rounds ago. The correction here is one sentence; the discipline correction is that this fold's negative is **grep-verified, not asserted**.

**The r2 fold, exhaustively:**
1. **§8** — the wildcard-form token replaced with the two exact v2 carrier literals: *"The two exact v2 carrier literals — `m3.app_event.v2` and `m3.e3_observation.v2` — are each INDEPENDENT of every m-8 carrier-version label"*; the independence statement, the digest-VALUE compatibility rule, and the D4-source citation (`c0b7b488…`) are otherwise unchanged.
2. Header/status/fold-log metadata (r1→r2), per your re-review gate's allowance.
**Nothing else moved** — §5's four exact branches, the §2 E0 table, the §3 six-scope matrix, the §4 two-layer split, §6's `model_surface_digest` exclusion, §7's actor table, and §9's narrowed claim are byte-identical to the r1 you rechecked as closing.

**The verified negative (the thing r1 asserted and r2 proves):**
```
$ grep -n 'm3\.\*' master/domains/m-3-observation-evidence/design/2026-07-22-e0-e3-schema-version-contract.md
$ echo $?
1
```
Zero rows. A ratification binder reading this contract encounters exactly four version literals and no family form anywhere.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r2 bytes at the SHA above. On approve the hash goes to master for amendment binding (D2 + final D4 master's folds) → VP exact-byte review → operator ratification; nothing else advances on these bytes.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-stage6-m3-schema-amend/DESIGN-planner-20260722-174500.md` — run before handoff.
- r2 SHA-256 computed post-fold: `6e2abe40be7a6031163348d47e6b9c13990c5599eb877d30ef196b59efbf3e1f`. The wildcard search run over the contract (zero rows, exit 1, pasted above); the two exact v2 literals confirmed present at §8.

ACTIONS_GIT_REF: none — contract revision + this relay only; no frozen byte moved, no `frank/` action, no token, no ratification act. Artifacts: the r2 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer returns the final re-review on the r2 bytes; on approve I hand the byte-bound hash to master for binding → VP → operator ratification.
