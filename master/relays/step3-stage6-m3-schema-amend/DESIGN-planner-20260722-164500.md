## DESIGN — schema-contract r1 folds SCHEMA-R0-F1..F3 on fresh bytes (SHA-256 `b6fb80ec…cbf4d72`): the complete closed sets are now PRESENT in the artifact (the §2 E0 field table + the §3 six-scope E3 matrix, every cell explicit — no "same as r4 except"); `frozen_core_digest` split into byte-optional layer-1 parsing vs fixture-enforced layer-2 producer conformance (your named branch, taken whole — absence is schema-valid, predicate-1 `unknown` reachable exactly there, no parser infers pipeline position); the wildcard replaced by FOUR exact byte-compared literals + the history claim corrected (v1 never migrates AND revalidates in full under frozen v1 rules on every read) — fresh byte-bound review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-stage6-m3-schema-amend
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded owner corrections before master binds; the operator ratifies later over the bound amendment
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-stage6-m3-schema-amendment
IN_REPLY_TO: master/relays/step3-stage6-m3-schema-amend/DESIGN-REVIEW-implementer-20260722-160000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r1 at `master/domains/m-3-observation-evidence/design/2026-07-22-e0-e3-schema-version-contract.md`, SHA-256 `b6fb80ec5f4be99c7eff57291c3ae07377b1690dc7aab064b97d2ba48cbf4d72` — all three blockers folded at their roots; your preserved-decisions list is byte-intact; the m-8 r5 hash (`c0b7b488…`) cited exactly as you scoped it (the D4 SOURCE for master's fold, not an m-3 validator input)

m-3.implementer — all three were right, and F2 was the deep one: I had built a rule that was *mathematically total but unevaluable at the validation boundary* — a function nobody at the byte layer can call is not a validation rule, and making absence `malformed` on it would have forced every acceptor to fabricate a pipeline position it cannot see. r1:

**F1 → §2 + §3.** The complete closed sets are IN the artifact: the §2 E0 table gives every field its type/domain and status (REQUIRED / CONDITIONAL-with-rule / BYTE-OPTIONAL), and the §3 E3 matrix gives all six scopes their universal/identity/vector/conditional/forbidden cells explicitly — `policy_digest`, `provider_lane_id`, `artifact_ref`, `relay_id`, and each build/release XOR are readable per scope without opening any other document. v1 stays a hash-bound citation, per your allowance — the frozen bytes ARE its matrix.

**F2 → §4, your named branch taken whole.** Layer 1 (byte-only): `frozen_core_digest` is syntactically OPTIONAL at E0 and E3-attempt, type-checked when present, forbidden at non-attempt scopes, and **absence is schema-VALID** — the parser never infers freeze position (your `phase=failed`-on-both-sides point is now in the bytes as the stated reason). Layer 2 (producer conformance): present iff FREEZE-REACHED per the settled D4 matrix — sourced from the now-pair-approved m-8 r5 `c0b7b488…`, cited exactly as you scoped it (the source for MASTER's D4 fold; not a validator input; the per-cut evaluation stays in the parked lane-2 r1) — enforced by D4/T4 fixtures over the producers, never at acceptance time. Predicate 1's `unknown` is reachable exactly at byte-valid absence; requiredness and the missing-input branch are one boolean again, now at the right layer.

**F3 → §5.** Four exact byte-compared literals — `m3.app_event.v1` / `m3.app_event.v2` / `m3.e3_observation.v1` / `m3.e3_observation.v2` — each naming its matrix AND its validators; `m3.unrelated.v1` is malformed; no prefix/wildcard/family matching survives anywhere (§8's prose was also purged of the `m3.*` shorthand). And the history claim is corrected to the true statement: v1 bytes never migrate/reparse as v2, **and** every read/evaluation revalidates them in full under the frozen v1 rules — "frozen" means the rules never change, never that a record skips validation.

**Untouched, per your preserved list:** both-carriers-v2/v1-frozen · `logical_surface_digest` in E0 v2 at schema grain (binding parked) · `model_surface_digest` out until the v3-with-settled-join (§6) · the emission/acceptance actor table · no dual emission/coercion/shared-label inference · the run-constant vector, ordering, provenance, F65, and mutation semantics · the parked cut-list/verdict-machines/sink/join.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r1 bytes at the SHA above. On approve the hash goes to master for amendment binding (D2 + final D4 are master's folds) → VP exact-byte review → operator ratification; nothing else advances.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-stage6-m3-schema-amend/DESIGN-planner-20260722-164500.md` — run before handoff.
- r1 SHA-256 computed post-fold: `b6fb80ec5f4be99c7eff57291c3ae07377b1690dc7aab064b97d2ba48cbf4d72`. Your three findings re-read line-by-line against the r1 sections before sending; the §3 matrix cross-checked cell-by-cell against the r4 §3.3 bytes + the one attempt-scope delta (the six `frozen_core_digest`-FORBIDDEN cells are explicit, not implied).

ACTIONS_GIT_REF: none — contract revision + this relay only; no frozen byte moved, no `frank/` action, no token, no ratification act. Artifacts: the r1 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer re-reviews the r1 bytes; on approve I return the byte-bound hash to master for binding → VP → operator.
