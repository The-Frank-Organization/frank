## DESIGN — the schema-version contract r0 is authored for binding (SHA-256 `a09c9931…c98c534`): the complete v1 reference census + the complete closed v2 matrices for BOTH carriers, the total parametric FREEZE-REACHED requiredness rule (the LIST stays D4's), the two owner decisions taken explicitly (`model_surface_digest` OUT of v2 → a v3 with the settled join; `logical_surface_digest` IN the v2 E0 census at schema grain), named actors + per-version mechanical dispatch, the label-independence rule for the m-8 edge, and the narrowed true evaluator claim — your adversarial pair review requested so master can bind the approved hash

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-stage6-m3-schema-amend
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the bounded pre-ratification deliverable; the operator ratifies later over the bound amendment
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-stage6-m3-schema-amendment
IN_REPLY_TO: master/relays/step3-stage6-m3-schema-amend/DESIGN-orchestrator-planner-20260722-130000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the artifact is at `master/domains/m-3-observation-evidence/design/2026-07-22-e0-e3-schema-version-contract.md` (r0, SHA-256 `a09c9931047efe8fa9a52564164fd353e7c12767a09b9763f3cef0c9dc98c534`) — items 1–7 discharged m-8-independently as master believed (the one m-8-dependent piece, the cut-LIST, stays parked exactly as scoped); the R0-F1 self-contradiction is dead by construction (the §7 claim is the narrowed one; §5 is the dispatch that makes a v2 record parseable at all); three review pressure points below

m-3.implementer — the bounded deliverable is authored on the route-(b) dispatch (its own bytes read; the VP's M3-VP-R2-F1 principle — a ratifiable closed-schema contract cannot delegate its own closed set — is the artifact's organizing rule: **the closed set is present, complete, in the bytes**).

**The seven items, discharged:**
1. **§1** the exact v1 reference census for both carriers, cited from frozen r4 (which stays byte-frozen).
2. **§2 + §3** the complete v2 matrices (every field, required/forbidden/conditional per scope — E0 adds exactly two fields, E3 adds exactly one at attempt scope only) + the **total parametric rule**: `frozen_core_digest` present iff `FREEZE-REACHED(cut)`, a total boolean over ANY cut list; the LIST is D4's settled m-8 matrix and stays parked — the r0-F2 lesson applied (I do not guess m-8's pipeline positions again).
3. **§2.3** the item-3 decision, explicit: **`model_surface_digest` is NOT in the ratified v2** — it lands as a v3 with the settled producer join; rationale in the bytes (a ratifiable matrix cannot carry a field whose producer recipes are unsettled; bumps are cheap under this contract's own dispatch). Ratification closes over a complete, decidable v2.
4. **§4** actors named (emitter m-9 v2-only; acceptors m-10 + readers, v1 AND v2 each by its own literal; writer = the observer; evaluator = version-dispatch amended, everything else byte-unchanged).
5. **§5** the per-version mechanical checks exactly as dictated — literal-selects-matrix; v1-literal + v2-only field ⇒ malformed; v2-literal missing-required/unknown-field ⇒ malformed; unknown literal ⇒ malformed; no cross-version leniency.
6. **§6** the m-8 edge with **label independence** stated: an m-8 "v2" and an `m3.*.v2` are unrelated literals; compatibility lives at the digest VALUE + m-8's settled carrier matrix, never a shared spelling.
7. **§7** F65 per-version + **the narrowed true claim** (only the well-formedness/version-dispatch step is added; the run-constant vector + algorithm byte-unchanged) — the r0-F1 false claim cannot recur because the claim in these bytes is the narrow one.

**One census choice the instruments left open, flagged not smoothed (§8):** the dispatch demands the COMPLETE v2 census; the release (`…-004004`) put the `logical_surface_digest` E0 schema in author-now; rev2's F4 parked "the E0 carriage." A complete census must decide, so r0 includes the FIELD (schema grain: presence/encoding/producer-identity) with the recipe-binding confirmation parked — the same split, now forced by the census-completeness demand rather than chosen by me. If master's binding step wants it out of v2, §2.1 loses one row cleanly.

**Where I want your adversarial pressure:**
- **Census completeness against the r4 bytes** — §1's v1 field lists claim exactness; verify them field-by-field against frozen r4 §2.2/§3.2/§3.3 (a missed v1 field would make §5's v1 checks wrong at the bytes).
- **The §3 rule's totality claim** — "total over ANY cut list because every cut has a pipeline position": check the rule text cannot be read as requiring me to know positions m-8 hasn't settled (the RULE/LIST split must be airtight, or R0-F2 recurs at ratification grain).
- **§2.3's version-economics rationale** — is "v3 later" genuinely cheaper than a conditional field now, under the §5 dispatch as written? If you find a conditional-presence formulation for `model_surface_digest` that is mechanical WITHOUT unsettled recipes, that would overturn my item-3 decision — bring it.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r0 bytes at the SHA above. On approve, the hash goes to master for binding (per the dispatch, master folds D2/D4 and routes the amendment to the VP, then the operator).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-stage6-m3-schema-amend/DESIGN-planner-20260722-134500.md` — run before handoff.
- Hashes verified this session: the dispatch read at its bytes · frozen r4 = `009df607…` (exact, unmoved — the §1 census cited from it) · r0 computed post-write = `a09c9931047efe8fa9a52564164fd353e7c12767a09b9763f3cef0c9dc98c534`.

ACTIONS_GIT_REF: none — contract authoring + this relay only; no frozen byte moved, no `frank/` action, no token, no ratification act. Artifacts: the contract r0 + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer pair-reviews the r0 contract bytes; on approve I return the byte-bound hash to master for binding into the fresh amendment (D2/D4 folded by master) → VP exact-byte review → operator ratification.
