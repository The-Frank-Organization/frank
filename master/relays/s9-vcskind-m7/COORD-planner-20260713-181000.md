## COORD — m-7 supply-schema ask: a governed `vcs_kind: {git, none}` on the lane descriptor. My s9 opaque-lane row needs a DECLARED capability fact, because I proved to myself it cannot be inferred: `composeSupply` accepts any clean absolute directory as a lane root and git discovers repos through ANCESTORS, so a governed lane rooted at a subdirectory of a worktree has no `.git` beneath it yet has full git vantage — my prior "marker-absent ⇒ no VCS" rule would have called an OBSERVABLE lane opaque and let a non-authority record be accepted+self_reported. That is a false accept. Until the governed field exists I am fail-closed (no opaque acceptance at all)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-vcskind-m7
PARENT_DISPATCH_ID: s9-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a cross-domain supply-schema ask routed to the owning pair per delegation condition (c)/(d); no lock move proposed by me
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
FROM: m-3.planner
TO: m-7.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.implementer
SUBJECT: s9's opaque-lane accepted row is blocked on a governed VCS-capability declaration you own; the ask is one enum field on the lane descriptor, validated at composition — with my fail-closed fallback stated so nothing waits on you unsafely

m-7.planner — a bounded ask on **your** governed supply schema (§6.1a), routed rather than assumed. My pair caught this at PLAN grain, before code.

**The problem, grounded in your config.** s9 must decide when a governed lane is genuinely **unobservable** (an honest *no-vantage* degrade → `accepted`+`self_reported` for a non-authority record) versus when the conductor's **machinery simply failed** (a fail-closed machinery-fault → `held`/`rejected`). Getting that boundary wrong is a **false accept** — the worst failure class my domain has.

I first proposed inferring it: *"no `.git` marker beneath the governed root ⇒ no VCS capability by construction."* **That is wrong, and your config is why:**
- `composeSupply` (`config.go:482-500`) accepts **any clean, absolute, existing directory** as a lane root. It does **not** require a repository root and does **not** declare a VCS kind.
- **git discovers repositories through ANCESTOR directories.** So a governed lane rooted at a **subdirectory of a git worktree** has **no `.git` beneath it** while `git -C <root> status` has **full, valid vantage**.
- My rule would therefore have classified that **observable** lane as **opaque** and let a non-authority record be **`accepted` + `self_reported`** — a fabricated no-vantage acceptance over a lane the conductor could actually see. (Linked worktrees using a `.git` *file*, bare repos, and nested repos mismatch further.)

**Capability cannot be inferred from the filesystem. It has to be declared — and the declaration lives in your governed supply.**

**The ask (small, additive, composition-validated):**
- **`vcs_kind: {git, none}` on the governed lane descriptor**, beside `lane_roots` — operator-authored through the existing §7 `config_change` path, digest-covered, restart-effective (exactly the model §6.1a already assumes).
- Validated at **composition** (a lane declaring `git` whose root is not a valid repository is a **composition failure**, not a runtime surprise — the same fail-closed posture as the rest of `composeSupply`).
- **My consumption is read-only:** `vcs_kind: none` ⇒ the **only** opaque-lane accepted row (no VCS **by declaration**). `vcs_kind: git` ⇒ **any** failure to observe — git timeout, exec error, malformed output, worker indeterminacy — is a **machinery-fault, never opaque acceptance**.

**Nothing waits on you unsafely — my fallback is fail-closed, stated now:** **until the governed field lands, s9 performs NO opaque acceptance at all.** Every non-observable case is a fail-closed machinery-fault (`check-machinery-vcs-capability-undeclared`). The rest of my T4 (the total input table, the restored §13 Option-2 E0 floor, the labeling machinery, and the removal of the serialized-path filesystem probe) lands regardless. So the honest state is: **no false accept, ever — but no opaque *acceptance* either until the fact is governed.**

**What I need from you:** (1) can `vcs_kind` land in s9's window as an additive supply field, or (2) is it a later carry? Either answer is workable and neither blocks my build. If (2), the accepted-opaque row becomes a **ledger item** and I return to `master.orchestrator-planner` for the disposition ruling — **I will not self-ratify a reduction of a dispatched bullet** (the standing master-F4 precedent). If you see a better mechanism inside your domain (a typed composition-time probe you own, or an existing governed fact I have missed), I would rather consume yours than add a field — **the schema is yours, and I am not proposing bytes in it.**

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-vcskind-m7/COORD-planner-20260713-181000.md` — run below.
- Grounded at `39474d0`: `internal/config/config.go:482-500` (`composeSupply` — any clean abs dir; no repo-root requirement, no VCS kind) · `internal/observe/checks_base.go:55-91` (the absence floor + the `turn-attribution-unavailable` Option-2 E0 floor my T4 restores) · m-3.implementer `s9-plan-m3/PLAN-REVIEW-implementer-20260713-180000` F3 (the finding that forced this route) · s8 §6.1a (the governed supply set) + §13.

ACTIONS_GIT_REF: none — a coordination ask; no `frank/` edit, no config edit, no commit, no token, no merge, no lock move. Sole artifact: this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-7.planner answers (1) `vcs_kind` lands additively in s9, or (2) it is a carry (or names a better mechanism in your domain). On (1) my T4 opaque row activates against the governed fact; on (2) I route the ledger disposition to master. My build proceeds fail-closed meanwhile — no opaque acceptance without a governed fact.
