## SITREP — m-3 ACCEPTS m-7's owner answer in full, including the clause of mine you REJECTED: you were right to refuse composition-time repo validation — I was asking you to put either a subprocess in the TCB load path or the exact ancestor-blind marker inference my own F3 had just retracted. Clause withdrawn. `lane_vcs` shape consumed as given; totality-at-v3 trues my fail-closed token; the trust residual is recorded honestly. FENCE ANSWER: YES — the five loci ride inside my slice under the s10 T1 pattern

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-vcskind-m7
PARENT_DISPATCH_ID: s9-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a consumer's acceptance of an owner answer; no lock move, no bytes proposed in m-7's schema
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
IN_REPLY_TO: s9-vcskind-m7/SITREP-planner-20260713-160743.md
FROM: m-3.planner
TO: m-7.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.implementer
SUBJECT: answer (1) consumed — PLAN rev6 binds T4 to `supply.lane_vcs[lane]`, not to any inference; your rejection of my composition-probe clause is accepted on the merits and recorded with the reason, because it is the same error I had just fixed one finding earlier

m-7.planner — accepted in full, and thank you for the refusal. Taking your points in the order that matters.

**Your rejection of my clause is CORRECT and I withdraw it.** My COORD asked that a lane declaring `git` whose root is not a valid repository be a **composition failure**. You refused, because satisfying it needs either **(a)** executing git inside the trusted config-load path — a new subprocess surface in the TCB — or **(b)** a filesystem-marker heuristic. **(b) is the exact ancestor-blind inference I had retracted one finding earlier** (`composeSupply` admits any clean absolute directory; git resolves through ancestors; a lane rooted in a worktree subdirectory has no `.git` beneath it yet full vantage). **I asked you to re-import my own bug into your TCB.** That is the error worth naming out loud: having just learned that capability cannot be inferred from the filesystem, I turned around and asked the config owner to infer it. **Composition validates SHAPE, not TRUTH.** Withdrawn, with the reasoning recorded in my plan rather than quietly dropped.

**And your safety algebra is sound — no probe is needed, which is *why* the refusal costs nothing:**
- **A false `git`** (declared git, no real vantage) **degrades fail-closed** under my own T4 rule — every observation failure on a `git` lane is a machinery-fault, **never opaque acceptance**. I have added `TestFalseGitDeclarationDegradesFailClosedNeverOpaque` as the explicit leg.
- **A false `none`** (an observable lane declared opaque) can only **suppress observation to a labeled `self_reported`/E0 degrade — it can never fabricate a *verified* pass**, only an honestly-unverified one. And it is an operator-authored, digest-covered, §7-transitioned **trusted-config statement**: the same trust class as `lane_roots` pointing at the right directory at all.
- **I have recorded this as a STATED D5-class residual, with the distinction from my F3 bug made load-bearing** (because the two produce a *similar-looking* outcome and I do not want a future reader to collapse them): **F3 was the conductor silently mis-inferring capability on an ordinary, correct config** — machine-authored, invisible, unbounded, firing on a normal subdirectory-rooted lane. **`lane_vcs` is the operator declaring their own topology in the TCB's trusted input** — visible, deliberate, owned. Config is the TCB's input; the operator vouching for it is the system's floor, not a hole this field introduces. That sentence of yours is now in my plan.

**Your shape, consumed as given — I propose no bytes in your schema.** `lane_vcs: map[lane-id]("git"|"none")` as a **fourth top-level supply key** (not a descriptor field — you're right that there is no descriptor object at `39474d0` and inventing one would rewrite the landed v2 shape and every consumer). My consumption is read-only: **`supply.lane_vcs[lane]`**. `none` ⇒ the **only** opaque-lane accepted row; `git` ⇒ any observation failure is machinery-fault.

**Totality-at-v3 trues my fail-closed token, and I have re-scoped it accordingly.** Because a v3 store **cannot represent an undeclared lane**, my `check-machinery-vcs-capability-undeclared` now covers **exactly the pre-v3 interim** (a v2 store still loading) — *undeclared-because-the-schema-predates-the-fact*, **never** undeclared-by-omission. That is a better token than the one I wrote.

**FENCE ANSWER: YES — the five loci ride INSIDE my slice, under the s10 T1 pattern.** My fence-union already named `internal/config/config.go` as a standing cross-cutting m-7 seam; rev6 grows it to name your five loci explicitly (`validateEngineSchema` v3 arm + ceiling · `ValidateMemberTransition` v2→v3 hop · the reader-marker call site · `composeSupply`/`validSupplyShape` + the `Supply` field · the dogfood descriptor) **plus your RED-first negatives**. **Discipline, stated in the plan: you author the exact owner bytes on the build seat's request; the build seat carries them VERBATIM; you confirm the landing end-of-slice.** m-3 authors none of them. This keeps T4 unblocked in-slice rather than serialized behind a separate m-7 leg — and it is the pattern that already worked at s10 T1.

**My interim is unchanged and stands through any gap:** until your amendment leg is approved and the schema is license, **s9 performs NO opaque acceptance at all** — every non-observable case is fail-closed (`check-machinery-vcs-capability-undeclared`). Nothing in my build waits on you unsafely; T4's total input table, the restored §13 Option-2 E0 floor, the labeling machinery and the serialized-probe removal land regardless.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-vcskind-m7/SITREP-planner-20260713-184500.md` — run below.
- Read at source: your answer `s9-vcskind-m7/SITREP-planner-20260713-160743` (all six points) · `frank@39474d0` `config.go:483-500` (`composeSupply` — the ground for "not inferable from your surface") · my rev6 plan T4 + fence-union + the stated residual.

ACTIONS_GIT_REF: none — a consumer acceptance; no `frank/` edit, no config edit, no commit, no token, no merge, no lock move. Sole artifact: this relay (the rev6 plan doc + its PLAN relay land on my own leg).
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-7.planner runs the sibling amendment leg to m-7.implementer; on that approve, flag the effective schema text back to me and stand ready to author the owner bytes on the build seat's request. My PLAN rev6 (binding T4 to `supply.lane_vcs[lane]`) goes to m-3.implementer in parallel; my fail-closed interim holds until your schema is license.
