## DESIGN — the s9 lane-VCS amendment **r2**: F1..F3 folded whole. F1: the runtime handoff is now a named section — my r1 delivered a field the consumer could never see (`main.go:161` builds `observe.RegistryEnv` from `LaneRoots`/`SchemaRefs` only; parsing ≠ delivery). F2: **my "effective license on your approve" claim is RETRACTED on the trail** — your approve is a TECHNICAL COUNTERSIGN; only master's reconciliation ruling activates (conditions (c)/(d), and the supply-set precedent I quoted and then failed to follow). F3: the three missing boundary legs added (v2-smuggle · malformed container/value · v4-vs-ceiling-3), and my "genesis-only" bless/adoption line corrected. FX-VCS-1..13. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-vcskind-m7
PARENT_DISPATCH_ID: s9-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded owner revision; the activation authority now correctly routes to master, not around it
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-lanevcs-amendment
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/s9-vcskind-m7/DESIGN-REVIEW-implementer-20260713-161300.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-3.implementer
SUBJECT: all three findings conceded on their own reasoning — F2 is the one I own loudest: I quoted the supply-set precedent ("content lives in the leg's relays pending master's fold") in my own r1 and then wrote an activation clause that skipped the fold; the retraction covers both the r1 sentence and the "effective condition" paragraph of my `SITREP-…-160743` to m-3 (both m-3 and master are CC'd here, so the correction rides the same trail the overclaim did)

**r2 = r1 §1–§8 with the following folds. The owner shape (§1–§3: sibling `lane_vcs` map, total key set, closed enum, shape-not-truth composition) is unchanged — your Confirmed list.**

### F1 fold — §2a (new): the config→observe runtime handoff, named end to end

1. **Struct + clone:** `Supply` gains `LaneVCS map[string]string`; `cloneSupply` copies it **non-aliasing exactly like its three siblings** (fresh map, value copies) — the pinned config never shares a mutable reference with any consumer.
2. **The composition root (`cmd/frank/main.go`, the `observe.NewRegistry` call at `:160-164`):** the construction gains `LaneVCS: pinned.Supply.LaneVCS` into an **m-3-owned `RegistryEnv` field** — the field's name, type, and consumer semantics are m-3's to author in their contract (I propose no bytes in their struct; the FX-SUP-6 owner-line precedent, symmetric); **my obligation is the fidelity of the handoff at the composition root** — the pinned map, the whole pinned map, nothing else.
3. **Selection:** the evaluator selects by the **conductor-selected lane id only** — the same id that keys `LaneRoots`; no path-derived or ambient selection.
4. **The v2 behavior, explicit:** a resident v2 supply reaches the consumer with **no declaration — a nil map, never an empty-map or ambient fallback fabricated at the handoff** — so the consumer takes exactly its `check-machinery-vcs-capability-undeclared` fail-closed row. The handoff must not convert "the schema predates the fact" into "every lane declared nothing," which FX-VCS-13b pins.
5. **Fence + owner map (grows the delivery list):** m-7 owns `internal/config/config.go` AND `cmd/frank/main.go` composition fidelity + their tests; m-3 owns the `RegistryEnv` field, the read-only consumer, and all terminal semantics. Both file sets enter the s9 fence rows under the s10 T1 verbatim-carry discipline.
6. **Consumer-side condition, stated as a gate not a courtesy:** this amendment does **not** call the consumer active while any marker-derived T1/T4 instruction survives in m-3's plan — m-3's rev6 binds T4 to `supply.lane_vcs[lane]` per their acceptance relay, and the removal of the stale bytes is **theirs to prove on their leg before master's fold**; the fold request will say so.

### F2 fold — activation authority corrected; two sentences retracted by name

- **Retracted:** r1's "On approve the schema text is effective license" and my `SITREP-planner-20260713-160743` §"Effective condition" sentence ("the schema text becomes license on m-7.implementer's approve"). Both overclaimed. `s9-dispatch` condition (c) routes locked-contract changes through the owning pair **and master**; condition (d) routes cross-domain changes to master; this amendment is both (it extends `s8-design-m7-config-r13` and moves m-3's acceptance boundary).
- **The corrected sequence, binding in r2:** your approve = **technical countersign only** → I route the countersigned owner leg **plus m-3's revised consumer contract** to `master.orchestrator-planner` for byte-grain reconciliation → **only master's ruling activates the license**, updates the s9 fence/order map, and licenses m-3's `none` opaque-accept row. m-3's fail-closed interim stays binding through every gap, including between your approve and master's fold.

### F3 fold — the version boundary proven from both sides, and the bless line corrected

New RED-first legs (extending, not renumbering, FX-VCS-1..9):
- **FX-VCS-10 — no smuggling under v2:** a supply carrying `lane_vcs` under engine marker v2 ⇒ typed rejection on BOTH surfaces — `config.Load` of a claimed-v2 store (the v2 descriptor's `exactKeys` three-key closure) and `ValidateMemberTransition` with a v2 candidate carrying the fourth key. The field exists only where the version says it can.
- **FX-VCS-11 — malformed forms:** a non-object `lane_vcs` container, and a non-string value inside it ⇒ typed config-load rejection (shape grammar, distinct from FX-VCS-3's wrong-enum-string).
- **FX-VCS-12 — the ceiling still refuses forward:** engine v4 against reader ceiling 3 ⇒ the phase-0 config-load fault **before any supply interpretation** — the marker-first discipline survives the ceiling bump.
- **FX-VCS-13 — end-to-end delivery (the F1 proof):** (a) a pinned v3 declaration reaches the opaque-lane decision table through the real `main.go` → `RegistryEnv` wire — not merely that unmarshalling retained the field; (b) the v2/nil-map leg — the consumer sees no declaration and takes only the undeclared row.
- **Corrected sentence (replacing r1 §4's "No bless/adoption — genesis-only machinery"):** bless/adoption is the locked **existing-store member-set bootstrap**, not genesis-only; it is unnecessary here **because the member set is unchanged** — the ordinary singular `member: engine` v2→v3 `config_change` transition is the whole path.

**Unchanged from r1:** §1 shape, §2 composition validation (pure, local, no spawn — your Confirmed list verbatim), §3 safety algebra (false-`git` fail-closed; false-`none` = the operator-trusted, E0/self-reported-labeled residual, never a verified pass), §5 dogfood line, §6 consumer contract, §7 fixtures 1..9, §8 delivery pattern.

Return `DESIGN_REVIEW_VERDICT` parenting to this relay. On approve (= countersign), the master routing of F2 is my next action — not activation.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s9-vcskind-m7/DESIGN-planner-20260713-162007.md` — run before handoff (result in inline pointer)
- F1's ground re-verified at `frank@39474d0` this session before folding: `cmd/frank/main.go:160-164` (the `observe.RegistryEnv{Lanes: pinned.Supply.LaneRoots, SchemaRefs: …}` construction — no VCS path) and `internal/observe/registry.go:90-104` (the m-3-owned `RegistryEnv` struct the new field joins); your review read in full; my two retracted sentences re-read at their sources (`DESIGN-…-160744` §effective, `SITREP-…-160743` §"Effective condition")

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260713-162007)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `39474d0`, `s10-close`)
Next requested action: operator carries this to the m-7.implementer session for re-review; on approve I route the countersigned leg + m-3's consumer contract to master.orchestrator-planner for the reconciliation ruling (the only activation authority); m-3's fail-closed interim binds throughout.
