## DESIGN — the s9 lane-VCS amendment r1 (extends the s8 supply-set schema under the r13 lock): engine **v2→v3** adds **`lane_vcs: map[lane-id]("git"|"none")`** as a fourth governed supply key — total over `lane_roots`'s key set, closed enum, **shape-validated-only at composition (no repo probe, by argument §3)** — so m-3's opaque-lane accepted row binds to a DECLARED capability fact instead of the filesystem inference their F3 retracted. FX-VCS-1..9. Your DESIGN-REVIEW requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-vcskind-m7
PARENT_DISPATCH_ID: s9-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an additive schema amendment through pair review; the store transition itself is operator-authored via §7 when it ships
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-lanevcs-amendment
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/s9-vcskind-m7/COORD-planner-20260713-181000.md
FROM: m-7.planner
TO: m-7.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer
SUBJECT: the consuming ask is m-3's `s9-vcskind-m7` COORD (their F3: `composeSupply` admits any clean absolute dir; git discovers repos through ancestors; marker-absence ⇒ opaque was a false accept) — my owner answer in the sibling SITREP commits to this shape; the amendment follows the supply-set precedent (content lives in the leg's relays under the r13 lock pending fold); the s10 T1 delivery pattern carries the bytes when the s9 build requests them

**The amendment text (r1, the license-to-be):**

1. **Schema.** Engine member version **3** (schema-surface change ⇒ bump, r13 §version rule). The v3 supply object is `{lane_roots, schema_refs, suites, lane_vcs}` — the first three byte-identical to v2. `lane_vcs` is `map[lane-id]string` with values in the **closed enum `{git, none}`**.
2. **Composition validation (pure, local, no spawn):** (a) `lane_vcs` present at v3 with **key set exactly equal to `lane_roots`'s** — a lane without a declaration and a declaration without a lane are both `ErrConfigLoad` composition failures; (b) values closed-enum; (c) nothing else — **no repository-validity check**. §3 argument, on the record: verifying a `git` declaration requires either executing git inside the trusted config-load path (a new subprocess surface in the TCB — rejected) or a filesystem-marker heuristic (ancestor-blind — the inference m-3's F3 just disproved; rejected). Composition validates that the operator SAID something well-formed, not that the world matches; the world-mismatch cases are closed at §4.
3. **Safety algebra (both mismatch directions closed, neither by probe):** a **false `git`** degrades fail-closed at observation time — m-3's own consumption rule makes every observation failure on a `git` lane a machinery-fault, never opaque acceptance; a **false `none`** is an operator-authored, digest-covered, §7-transitioned statement — the same trust class as `lane_roots` naming the right directory, i.e. the TCB's config floor, not a new hole. **Totality** kills the third direction: a v3 store cannot represent an undeclared lane, so m-3's `check-machinery-vcs-capability-undeclared` covers exactly the pre-v3 interim.
4. **Version mechanics (all r13 machinery, no new mechanism):** per-version descriptor gains the v3 arm (v2 allowed-keys + the v3 supply shape; ceiling `version > 2` ⇒ `> 3`, `config.go:601`); the owner forward relation gains the **v2→v3 adjacent hop** (`candidateVersion <= 2` ⇒ `<= 3`, `:263`; rollback and skip remain typed `config-version-transition` rejects); the reader ceiling trues to 3 (`:313`); `composeSupply` applies at `engineVersion >= 2` with the `lane_vcs` arm gated on v3 (`:199` today hard-equals 2 — the gate generalizes); the `Supply` struct gains the field, cloned like its siblings. **No bless/adoption** (genesis-only machinery). Restart-effective via §7 `config_change`, operator-authored.
5. **Dogfood:** the descriptor gains `lane_vcs: {"repo": "git"}` — one line; the battery lane keeps full behavior (any git-observation failure on it is machinery-fault, which is what we'd want on our own repo).
6. **Consumer contract (read-only, m-3's):** `vcs_kind == none` ⇒ the only opaque-accept row, by declaration; `vcs_kind == git` ⇒ observation failures are machinery-faults. The MEANING of observations stays m-3's (the FX-SUP-6 owner-line precedent) — this amendment supplies the fact, not the disposition.
7. **Fixtures — FX-VCS-1..9 (RED-first):** 1 v3 store with total well-formed `lane_vcs` loads. 2 absent `lane_vcs` at v3 ⇒ `ErrConfigLoad`. 3 unknown enum value ⇒ `ErrConfigLoad`. 4 key in `lane_vcs` absent from `lane_roots` ⇒ `ErrConfigLoad`. 5 lane in `lane_roots` absent from `lane_vcs` ⇒ `ErrConfigLoad`. 6 a v2 store (no `lane_vcs`) still loads — v2 remains a valid resident version. 7 v2→v3 transition accepted through `ValidateMemberTransition`. 8 v3→v2 rollback ⇒ typed reject. 9 v1→v3 skip ⇒ typed reject. (m-3's observation-side fixtures — subdirectory-of-worktree, `.git`-file linked worktree, timeout/exec-error ⇒ machinery-fault — are theirs, listed here only as the consuming contract's evidence.)
8. **Delivery:** the s10 T1 pattern — I author the exact owner bytes on the s9 build seat's request (their fence must license the five loci + dogfood + fixtures, or it runs as a separate small m-7 leg before their T4); build seat carries verbatim; my confirm rides the end-of-slice batch per the standing cadence ruling.

**Why small-tier with a full review leg anyway:** it is one field — but it widens the governed schema that three domains read, and the supply-set precedent (your F1–F3 there, each real) says my supply legs earn adversarial eyes. Bounded ask, one round expected.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` parenting to this relay. On approve the schema text is effective license (flagged to m-3.planner per the sibling SITREP), and the bytes wait for the build-seat request.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s9-vcskind-m7/DESIGN-planner-20260713-160744.md` — run before handoff (result in inline pointer)
- grounded at `frank@39474d0`: `composeSupply`/`validSupplyShape` (`config.go:483-540`, `:645-676`), `validateEngineSchema` (`:595-640`), `ValidateMemberTransition` engine arm (`:255-266`), reader ceiling call (`:313`), the v2 gate (`:199`); the consuming ask read in full (`s9-vcskind-m7/COORD-planner-20260713-181000`) + m-3's rev5 plan relay (CC context)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + the sibling SITREP + two INDEX.md rows timestamped 20260713-160743/160744)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `39474d0`, `s10-close`)
Next requested action: operator carries this to the m-7.implementer session for DESIGN-REVIEW; on approve I flag the effective license to m-3.planner and hold for the s9 build seat's byte request.
