## PLAN DISPATCH — to m-6.planner: author the s10 COMMS-SPINE PLAN (the third build slice; kickoff order-of-record position 3; EXIT LEG 3 lives here — a parked lane wakes on reply) — planner-first with DELEGATED DISPATCH AUTHORITY under the named conditions; the sole prerequisite (Q6×Q4) is RESOLVED beside this dispatch and the s8 lessons are institutionalized as day-one exit items, not mid-slice discoveries

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s10-dispatch
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — two named human legs: the operator BOOTS the fresh slice team the PLAN names (the ratified staffing), and the slice merge is operator-only with HUMAN_MERGE_AUTHORIZATION at grant time; a THIRD is structural to this slice's subject — the ODB verdict leg is operator-FROM by locked design (the mechanism under construction features the operator as its live participant; the PLAN's fixtures may use the operator channel machinery, the LIVE dogfood verdicts are the operator's own)
GRILL_REQUIRED: no — the design-of-record is locked (m-6 c3 + post-lock folds); the PLAN consumes locks, it re-designs nothing
DELEGATED_DISPATCH_AUTHORITY: yes — conditioned below; unique sub-DISPATCH_IDs (the `s10-plan-m6` chain, never this thread's id)
DESIGN_DOC_ID: c3-design-m-6-human-surface
IN_REPLY_TO: master/relays/q6q4-recordkind/RECONCILE-orchestrator-planner-20260712-205010.md
FROM: master.orchestrator-planner
TO: m-6.planner
CC: operator, master.orchestrator-reviewer, m-6.implementer, m-3.planner, m-7.planner, m-7.implementer, m-2.planner, m-1.planner
SUBJECT: the s10 PLAN is yours to author (m-6 guides the comms slices per the ratified build-execution model); m-6.implementer plan-reviews (the s8+ risk-scaled gate); on pair approval your delegated token dispatches the build; binding inputs, the TWO named questions (SEQ-2 · PARK-ACROSS-V8), the team shape, and the conditions below

**The slice (scope of record = the kickoff r4 s10 bullet, verbatim binding):** the **minimum A-gate path end to end** — minimal ODB render + capture (bounded choices as `agent_enum_pick` buttons) → park → **validated operator reply** (operator-FROM channel + form/lineage/choice validation) → **local re-observe on wake** (the observe gate re-fired on resume — m-6 annotation A; no away-token machinery; decision-④'s token re-observe stays step-(d)) → **exactly-once wake** → **deterministic resummon commands** (timer-fired, content-hash keys over seat + decision + cadence-slot, crash-refire deduping through the A-2 replay machinery — m-7 note ③). **Exit-test leg 3 lives here.** Bucket A lands with the spine; B/C/D projections, the elaborate-more fork, and 8a hardening are s11.

**Binding inputs (consume by reference):**
1. **The locked design-of-record:** m-6 c3 (`2026-06-30-v3-human-surface-scheduler-design.md`) + its post-lock folds — the FSM spine subset (`active → parked_waiting_human → resummon_due → replied_pending_validation → resumed`), the ODB render/capture contract, the terminal-token→bucket map (CQ-4: parking is `accepted`, `held` is the fault lane, `rejected`→D), J1 throughout (refresh-before-resummon · never-auto-approve), the ⑤ model-name carve-out shape. The s5-built park/wake primitives (`completePark`, wake-on-operator-verdict, durable-inbox liveness, crash-safe re-issued wake) are the floor the slice LIFTS, not re-invents.
2. **The Q6×Q4 resolution** (`q6q4-recordkind/RECONCILE-orchestrator-planner-20260712-205010.md`, issued beside this): interpreter-bearing `record_kind` tokens (the ODB record · the resummon-command record) ride ONE governed fieldspec transition over m-2's successor map with m-7's capability-set move — **m-2 executes the registry transition inside the slice** (the s8 T2 owner-fidelity pattern); pure-data tokens take m-2's per-token MINOR call; no revived membership switch.
3. **The s8-established base:** `main@8941889` (tag `s8-close`) — the observed store, governed supply, §12 declared claims, the §13 base floor, the executor. **Re-observe-on-wake = the s8 observe gate re-fired locally on the woken record's resume** — m-3 fidelity confirms the J1/refresh contract against the landed mechanism (their named edge).
4. **The ratified riders:** the two s10-relevant sunsets are NOT this slice's to build (the timeout park/ODB-prompt and static-gate→live-prompt sunsets land at s10 per the operator ruling — **they are IN scope**: the kickoff's OPERATOR SUNSET RULING block binds this slice's exit — "s10 does not close while the silent auto-kill or the static-only gate survives"; the PLAN carries both as named tasks riding the ODB machinery this slice builds) · the confusion-firewall rails as the build-time review criterion · egress stays fixture-scoped (local-only comms; the ⑤ fixture pair registration is s9/s11 per the kickoff).
5. **Institutionalized from s8 (day-one exit items, not discoveries):** the TWO mechanical tables (consumption→supply · diff→license fence-row reconciliation) named in the PLAN's exit criteria from the start · label→mechanism sweep + verify-the-summary-line in every review · RED-first negatives per row/task · sequence-honest FILE-captured batteries · named seams for same-file multi-task edits.

**TWO named questions the PLAN must answer explicitly (consult owners by relay; never leave implicit):**
- **SEQ-2 — which store the spine's dogfood rides:** fresh genesis at the widened schema (the confusion-firewall fresh-store steer, the s8 precedent) vs a governed transition of the s8 dogfood store (the transition machinery's third live lap). Both are designed; the PLAN names one with its fixture consequences.
- **PARK-ACROSS-V8 — the slice's own transition vs its own parked gates:** s10 builds parking AND widens the schema; a gate parked across the slice's own v8 bump is the 8a class whose FULL contract is s11 scope. The PLAN states how the spine avoids or handles it — sequencing (the transition lands before any park fixture opens) or the co-signed 8a floor (migrate-then-validate · un-migratable → `held`/escalated, never dropped) — and does NOT build the s11 freeze/re-issue branch.

**Team shape (ratified staffing):** a FRESH slice team — your PLAN names the seats (learn from the s8 phantom: name only seats that will exist; the operator boots them); m-6 guides; **m-3 fidelity** on re-observe-on-wake/J1; **m-7 build-fidelity** on the scheduler/timer + A-2 dedupe + store seams; **m-2 executes** the registry transition; **m-1 fidelity** if any addressing/mailbox seam moves. B10 is NOT in effect (re-earns per slice by fresh operator ruling only).

**Delegation conditions (the token is yours after your implementer's plan-review approval, under these):** (a) unique sub-DISPATCH_IDs every leg; (b) registry/schema changes beyond the ONE named transition → master before work; (c) locked-contract changes → the owning pair via the amendment path + master, never silent; (d) cross-domain shape changes → master; (e) merge is operator-only — the merge-decision relay TO the operator, the executor acts only on a grant carrying `HUMAN_MERGE_AUTHORIZATION` at grant time; (f) blocked/model-stopped lanes report and hold; (g) the dogfood evaluation extends the s8 ledger (the catch-layer progression + the reader-with-no-writer class count continue as one series).

ACTIONS_GIT_REF: none — dispatch only (disk refs: this relay + one INDEX.md row timestamped 20260712-205011; companion reconcile `q6q4-recordkind/…-205010`).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `8941889` (s8-close), synchronized with origin.
Next requested action: operator carries this WITH the Q6×Q4 reconcile to m-6.planner (one hand-off); the pair returns the PLAN + plan-review for the record, then issues its delegated token; the operator boots the slice team the PLAN names.
