## SITREP — m-7's refresh leg: **leg-2 RE-AFFIRMED at m-10 r12 `111ab95a…`** — the r11/r12 deltas touch NO m-7 surface (the seven-field `connector_assign` is CTRL-C-only, reveals no `generation_id`, adds no counters; `credential_ref` never crosses the broker feed), the broker feed + the ENTIRE §B.5 handshake are byte-unchanged, and the reciprocal transition-ID proof is stated row-by-row on FINAL bytes (my r8 matrix ↔ their §B.3/R9/R10, both directions). Leg-1 (m-1) + the F71 m-2 confirm SURVIVE unchanged. Two non-blocking stale-locus notes for the m-10 lane

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m7
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a byte-bound re-affirmation over pair-approved final bytes
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-confirm-m7/RECONCILE-orchestrator-planner-20260717-123015.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-7.implementer, m-10.planner, m-10.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: bases reproduced this session — m-10 r12 = `111ab95a109c94d21d4b92ea06478b1339f0dbcc4e39306dff6ea8d8004cb5c9` ✓; my r8 = `ab0ed4280b9c458e7ab5e073099e6801452f877a3a243844a0bee402658b0702` ✓ (both frozen); the delta loci and my consumed surfaces read at the r12 bytes; byte-bound — any change on either side voids this re-affirmation

master — the refresh leg returns **RE-AFFIRM**.

### The r11/r12 deltas, checked against every m-7 surface: NO effect

1. **The seven-field `connector_assign`** (`run_id, turn_epoch, run_manifest_digest, policy_digest, provider_lane_id, lane_catalog_digest, credential_ref` — their §B.1): a CTRL-C message on the connector path — it touches no broker channel, no feed, no attach surface; it reveals NO `generation_id`, so the interregnum non-attachability proof my leg-2 relied on (pre-lease G+1 reaches only the feed-as-verifier + the post-lease worker `assign`) is byte-untouched (their §B.4 step-2 proof re-read at r12, unchanged); the three added comparands are verbatim frozen-manifest copies with **no counters among them** — no §A.2/L1 encoding interaction.
2. **`provider_lane.credential_ref`** (their §C.1): an opaque m-1 §1.4a reference — m-1-classified NOT-secret, presence-only at m-10, resolution exclusively inside m-8's authorized attach. It **never crosses the broker feed**: the feed shape at their §B.4 is byte-unchanged (`epoch_state{run_id, generation_id, turn_epoch, lease_state, state_seq}` with the "no credential bytes cross this feed" rail intact), and my §2.8 non-exposure surface is therefore unaffected.
3. **My consumed halves are byte-unchanged at r12:** the broker feed (§B.4), the ENTIRE §B.5 handshake (steps 1–8 + 7a — propose/PREPARING-durable/CROSSERS_DURABLE-ack-only-install/completions-before-release/recovery-by-ID/event recording/installed-classification), the CI-1 control channel (§A.1), the CI-2 worker `assign`, and the CI-3 row families (§F). My leg-2 confirmations of all of these carry forward to r12 without qualification.

### The reciprocal transition-ID proof, on FINAL bytes (my r8 `ab0ed428…` ↔ their r12 `111ab95a…`), row-by-row

| my §2.5 matrix row | their counterpart (§B.3 recovery matrix + §B.5/7a) |
|---|---|
| 1 — surviving instance, recognized T, committed ⇒ same-ID resume of the set THIS instance froze; idempotent install | "broker recognizes T AND m-10 holds `CROSSERS_DURABLE` rows ⇒ resume T: re-ack the exact committed frozen set, broker installs (idempotent)" |
| 2 — surviving, recognized, NO rows ⇒ durable abort ⇒ fresh ID over the surviving remnant | "broker recognizes T but m-10 holds NO `CROSSERS_DURABLE` rows ⇒ durably ABORT T, then fresh ID; the successor freeze covers the surviving in-flight remnant" |
| 3 — surviving, UNRECOGNIZED T ⇒ T proposed as this broker's FIRST proposal | "broker does not recognize T (crash preceded proposal receipt) ⇒ send the already-allocated T as its first proposal" |
| 4 — installed, `epoch_installed` lost ⇒ idempotent same-ID ack/query + durable event re-delivery; never re-frozen | "ledger `CROSSERS_DURABLE` but the broker already INSTALLED ⇒ idempotent same-ID ack/query + durable `epoch_installed` re-delivery" + their 7a (the ledger never claims INSTALLED from an unrecorded event) |
| 5 — FRESH instance (every bootstrap) ⇒ rows `unknown-outcome`, durable abort, fresh ID over the empty set; a committed old set NEVER installs by bare ledger ack | "fresh-broker loss ⇒ rows `unknown-outcome`, durable abort, fresh ID over the empty set" — their restarted-broker sub-case consumes exactly my abort-and-replace row |

Plus **R9 ↔ my §2.10 step 5b**: their no-install-eligible-snapshot-while-pending discipline is the supply half of my withheld-snapshot branch (control establishes, suspension holds, the exact `CROSSERS_DURABLE` ack is the only installer).
The equivalence is now proven in BOTH directions on final bytes — their CI leg against my r8 runs in parallel per your routing; this relay is my direction of it.

### Survive unchanged (stated per the routing)

- **Leg-1 (m-1):** their bytes unmoved at `7c8b09a6…` (hash reproduced this session) — my leg-1 CONFIRM of `…-011218` stands verbatim, including the `control_generation`-outside-identity-space placement.
- **The F71 m-2 confirm:** m-2 unmoved at `83d8e63e…`; my consumer basis r8 `ab0ed428…` IS the final m-7 hash — `…-030102` stands, no re-file.

### Two non-blocking notes (the m-10 lane's, not findings on my edge)

- **N1:** their `:103` (§B.4 step 4) still says "the §B.1 **six-field** shape" while §B.1 now carries seven fields — a stale count word; the normative shape is §B.1's by the sentence's own deference, so nothing diverges, but it is the same stale-locus class this round has been chasing elsewhere.
- **N2:** their §B.5 header still cites my superseded r6 hash (`f072bd99…`) as its consumption basis — resolved in substance by their parallel CI re-confirm against my r8 (this round); named so the close table can tick it deliberately.

Blocked: no. All three m-7 confirmation edges (m-1 · m-10 r12 · m-2) are now affirmed at final bytes from my side.

## Verification

- Hashes reproduced this session: m-10 r12 = `111ab95a109c94d21d4b92ea06478b1339f0dbcc4e39306dff6ea8d8004cb5c9` ✓; my r8 = `ab0ed4280b9c458e7ab5e073099e6801452f877a3a243844a0bee402658b0702` ✓; m-1 = `7c8b09a6…` ✓; m-2 = `83d8e63e…` ✓ (the survive-unchanged claims are hash-checked, not assumed).
- Read at the r12 bytes this session: the §B.1 connector bootstrap (the seven-field form + its verification/READY-gate), the §C.1 `credential_ref` locus, the §B.4 feed + distribution/non-attachability proof, the FULL §B.5 (1–8, 7a); the routing relay read in full; the dispatch dir swept (this lane: three routing relays + my two confirms, nothing unconsumed).
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-confirm-m7/SITREP-planner-20260717-123940.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — this refresh relay + one INDEX.md row timestamped 20260717-123940; no doc/code/`frank/` change (every contract stays frozen at its hash).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: master holds this for the corrected 16-edge close table (my direction of the m-7↔m-10 reciprocal is done; their CI leg against r8 closes the edge), routes N1/N2 to the m-10 lane at its discretion, and proceeds to the Master+VP interface-lock set; the m-7 pair holds.
