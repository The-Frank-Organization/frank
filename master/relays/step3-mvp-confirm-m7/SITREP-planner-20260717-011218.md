## SITREP — m-7's BOTH confirmation legs: **CONFIRM m-1 @ `7c8b09a6…`** (the broker realizes custody UNDER their approved semantics — every §3 obligation checked, the two-counter law honored with `control_generation` explicitly placed OUTSIDE identity space) + **CONFIRM m-10 @ `79fcf742…`** (CI-1/2/3 realized faithfully; R9/R10 are consistent strengthenings my approved bytes already support — NO conflict finding). Two non-blocking notes routed, neither changing any frozen byte

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m7
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — byte-bound consumer confirmations per the routing; confirmation ≠ lock
GRILL_REQUIRED: no — per the routing relay (the F67 grill is DONE; GRILL_LOCK in my approved record)
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-confirm-m7/RECONCILE-orchestrator-planner-20260717-010010.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-7.implementer, m-1.planner, m-1.implementer, m-10.planner, m-10.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: both legs verified against the ACTUAL approved bytes this session — hashes reproduced (m-1 `7c8b09a6…` ✓, m-10 `79fcf742…` ✓, my own `f072bd99…` ✓ still frozen), both docs read in full at those bytes; each confirmation is byte-bound — any change on either side voids it

master — both legs return CONFIRM.

### Leg 1 — m-1 @ `7c8b09a6289f117b86133bb391e02c50e26476b252c56c17cfa52a4794ff944c`: **CONFIRM — the broker contract realizes S-B custody UNDER their approved semantics**

Checked point-by-point against their §3 m-7 obligation row, at their bytes:
1. **The complete §1.4b fenced surface:** their "three canonical relay verbs AND typed `Describe` (request operations) + the per-push fence at forward time" is exactly my §2.4/§2.8 closed surface; `Describe` stays metadata on both sides (never a relay verb, dispatch name, or relay-acceptance authority).
2. **The locator/capability split:** my §2.3 capability claims exactly their §1.4b (intentionally authorizing, NOT leak-inert, no offline credential meaning, no principal) and my contract satisfies no §1.4a reference requirement by pointing at it; my §2.12 sink is exactly their §2.4-pinned "ONE broker-private, permission-checked 0600 credential file" — their census obligations (rotation overwrites at the path grain via atomic rename; no log/dump/backup surface; deletion on decommission) are carried by my §2.12 + FX-TB-8/13, and my broker never reads the binding table (their preserved negative).
3. **The lifecycle mapping (their §2.3):** `bound` = my broker's authenticated connection; worker generations never bind; conductor-invisible replacement with my F64 fence as the compensating control — verbatim my §2.1/§2.4.
4. **Rotation (their §2.4):** force-close → re-read the overwritten sink → re-authenticate; capabilities and epochs untouched — my §2.7 rotation row, cell-identical.
5. **The §2.7 five-event matrix:** my broker-side column matches every cell, including their MR2 broker-restart row (ALL old connection-scoped tokens die; reattach at the UNCHANGED epoch only through the authenticated restart protocol with FRESH material — my §2.3 lifetime + §2.10 attach) and the overlap row (either order ⇒ exactly one re-bind + one epoch install; no coupling — my §2.7 union row + FX-TB-7).
6. **The two-counter law (their §2.2), with the one reading addressed HEAD-ON:** my §2.10 `control_generation` is NOT a third counter in their §2.2 sense — it counts CONTROLLER SESSIONS (an m-10 supervision-internal handover ordinal), not worker generations and not identity epochs; it appears nowhere in identity space, has no stamp/principal/binding consequence, never touches `turn_epoch` or the mint-generation, and no value of it can cause, require, or imply a `seat_mint`.
   The Standing Identity-Seam Rail holds: my broker adds no stamp, no identity transform, no new principal class, and no home-grown crypto primitive (the fcntl lock + peer-PID probe are stock OS facilities used as liveness/ownership witnesses, not identity machinery).
7. **Their §5 fixture legs** (reference-without-authority, capability replay after advance/restart, stale-epoch `Describe`, the sink census, the overlap fixture) are covered by my FX-TB-1/6/7/8/13 at the mechanism grain — the identity-semantic EXPECTED RESULTS stay m-1's, as their matrix requires.

### Leg 2 — m-10 @ `79fcf74290fb2a7c9ca4d706dbceceeaacd7714a23bd82baa70e824824384453`: **CONFIRM — CI-1/2/3 realized faithfully; R9/R10 are faithful counterparts; the recording/control rows are the store half my broker writes into. NO conflict finding.**

1. **CI-1 (their §A.1):** dial-in listener with m-10 as client; fcntl lock BEFORE the durable `control_generation` advance, then connect and present `{token, generation}`; spawn-pipe token handoff, rotation only at spawn, adoption presents the existing token — my §2.10, byte-faithful.
2. **CI-2 (their §B.1):** `assign{run_id, turn_epoch, manifest_digest, generation_id, broker_worker_endpoint}` — both requested fields present; their post-lease-bind assign ordering + the role-exact reveal rule (the broker feed is the tuple VERIFIER and exposes no presenter path) STRENGTHEN my §2.10 attach (a pre-lease candidate cannot learn G+1, so the interregnum tuple is non-attachable by information flow).
3. **CI-3 (their §F/§B.5):** `broker_control` (one row, generation advanced per controller start, token rotated at spawn) · `broker_events` (my CLOSED §2.11 family consumed not restated; `UNIQUE(broker_instance_nonce, event_seq)`; ack-after-durable-commit; same-ack-on-duplicate) · `epoch_transitions` (my full `PROPOSED → PREPARING → CROSSERS_DURABLE → INSTALLED | ABORTED` machine; at most one non-terminal per run; queryable by ID) · `crossing_ops` (keyed `{epoch_transition_id, operation_id}`; my exact state enum incl. `completed-before-install`/`aborted-attempt`; one-transaction abort) — all four families exactly as my CI-3 named them.
   Their §B.5 steps 1–8 + 7a mirror my §2.5/§2.11 clause-for-clause; their added durable `PREPARING` ledger substate (written before the crossing-row commit, so recovery READS whether a frozen set was received) is a supply-side refinement consistent with my machine — it changes no m-7 byte.
4. **R9 (pending-transition bootstrap/adoption order): CONFIRM as a consistent strengthening.** When a non-terminal or `ABORTED` transition rides the current epoch, m-10 supplies NO install-eligible snapshot at bootstrap/adoption and reconciles by ID first, with only the exact `CROSSERS_DURABLE` ack installing E+1.
   My approved bytes already carry the receiving half: my §2.10 step-5 "install it" is conditional on a SUPPLIED snapshot — absence lands in my §2.4 fail-closed floor (suspended), and my §2.5 reconciliation names the `CROSSERS_DURABLE`-ack-only install for the committed case. R9 pins the supply-side discipline that rides that floor; no m-7 byte moves.
5. **R10 (lost-install replay): CONFIRM — my bytes provide exactly the required behavior in both sub-cases.** Same broker instance: `epoch_installed` is an uncoupled event in my bounded pending-resend queue — re-delivered with its UNCHANGED key until acked (my §2.11 at-least-once + key-dedup), and a same-ID re-ack/query is idempotent (my §2.5).
   Restarted broker instance: the same-ID resume re-acks the exact committed frozen set, the install is idempotent in effect, and the fresh instance emits `epoch_installed` under its new instance nonce — m-10's 7a dedup-idempotent commit advances the ledger either way, never claiming `INSTALLED` from an unrecorded event.
6. **The feed + epoch-authority halves:** their §B.4 source-specific authority rule quotes my §2.4 ("never derives, guesses, or defaults"); the interregnum `epoch_state{G+1, E+1, unleased}` is schema-exact against my feed shape (lease_state is a first-class field, no sentinel).

### Two non-blocking notes (routed, NOT findings; no frozen byte changes on either side)

- **N1 (additive hardening option, build-grain):** my §2.10 attach verifies the presented tuple against the installed `epoch_state`; m-10's non-attachability argument for the interregnum is information-flow (the pre-lease G+1 reaches no presenter). A build MAY additionally gate attach on `lease_state == leased` (their §B.4 step-5 ordering guarantees it at every legitimate attach) as belt-and-suspenders; neither contract requires it, both permit it — named so the build lane sees it deliberately rather than by accident.
- **N2 (a latent nit in MY OWN approved bytes, flagged for the interface-lock round rather than silently folded):** m-10's §A.2 counter-encoding rationale (JCS interoperable integers cap at 2^53−1) applies in principle to my §3.2 stamp's `config_generation: <uint64>` field, which is currently a JSON number in my approved schema.
   Practically unreachable (it counts accepted config changes), and my consumer rule already rejects malformed stamps — but if Master+VP want domain-exact consistency with the m-10 R4 string rule, that is a BYTE CHANGE to my approved contract requiring a fresh uniquely-parented pair review; I do not make it unilaterally.

Blocked: no. Both outbound legs are returned; the m-7 pair holds for the inbound confirmations OF my bytes (m-10's CI leg + m-9/m-2/m-3/m-1) and the interface-lock round.

## Verification

- Hashes reproduced this session before reading: m-1 doc = `7c8b09a6…` ✓; m-10 doc = `79fcf742…` ✓; my contract = `f072bd996da0c85b1be9b67fad880e7395ab03de7142cb87fc6864d4f67a100e` ✓ (frozen since the r6 approval — no byte has moved).
- Both approved docs read IN FULL at those bytes this session (m-1: §§1–5 incl. the rev1–rev3 fold notes; m-10: §§A–I incl. §A.1 CI-1, §B.1 CI-2, §B.3 recovery matrix, §B.4/§B.5, §F rows, §H); the routing relay read in full; the dispatch dir swept (this lane: the routing relay only, nothing unconsumed).
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-confirm-m7/SITREP-planner-20260717-011218.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — this confirmation relay + one INDEX.md row timestamped 20260717-011218; no doc/code/`frank/` change (every contract stays frozen at its approved hash).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: master folds both CONFIRMs into the confirmation table (the m-7↔m-10 edge closes when m-10's CI leg returns), routes N1 to the build lane and N2 to the Master+VP interface-lock round, and holds the m-7 pair for inbound confirmations or lock findings. m-7.planner holds — no self-advance.
