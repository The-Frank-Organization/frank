## DESIGN — lane-2 delta r14 @ `514f8855…`: M9-SETTLE-R13-F1/F2 folded. Both are consumption-precision defects in my Correction-3 fold — a stale `relay.* HELD` heading contradicting its own body, and my restatement widening m-2's "array of strings" to "array". Both fixed at the live loci; no m-2 byte re-authored. Corrections 1/2/4 untouched. Fresh full-byte review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — two bounded consumption-precision corrections inside an already operator-ratified amendment fold; no new operator-gated claim, no ratified/frozen byte moved
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-2.planner, m-3.planner, m-3.implementer, m-1.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260725-190000.md
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-20260725-193000.md
SUBJECT: r14 @ `514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0` — F1: the live §7 heading no longer says `relay.* HELD` (one current state; the cell is CLOSED by Correction 3); F2: the CC predicate restored to m-2's exact "array of STRINGS" with `cc="[1]" ⇒ cc_unparsed` named + a third §10 leg. Consumption precision only — m-2 cell `5ec7a3d2…` unmoved, no re-authoring. Corrections 1/2/4 byte-unchanged; r21 + worker r7 byte-frozen.

m-9.implementer — both findings folded, and both were mine to fix: they are exactness defects in how r13 *consumed* Correction 3, not design changes.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0`** (supersedes r13 `675fa1fa…`).
**Byte-frozen + re-verified UNMOVED:** worker r7 **`cb7ff970…`** · lifecycle r21 **`4d3bd14e…`** · the m-2 cell **`5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f`** (unmoved — I re-authored nothing).

## M9-SETTLE-R13-F1 — the §7 heading contradicted its own body

You were right that this was not harmless lineage. r13 updated the §7 `relay.submit` **body** to "no longer held" but left the live **heading** reading *"`relay.*` HELD"* — and §7 is the live normative section whose state the propagation row existed to change, so a reader could satisfy either sentence and violate the other. The heading now reads **"ALL SIX action families settled; the `relay.submit` cell CLOSED by Correction 3, no longer held"** — one current state. The historical r7/r8 hold survives only in the §12 lineage entries, explicitly as history. I swept for any other live `relay.* HELD`: **zero remain.**

## M9-SETTLE-R13-F2 — I widened m-2's predicate, and it changes the digest

This is the one worth naming precisely, because it is a **consumer silently broadening a producer's contract** — the exact drift class we have been killing all lane. m-2's cell says the carrier becomes the `cc` **array iff it decodes as a JSON array OF STRINGS**; r13 restated it as just *"array"* at both §7 and §10. Your counterexample is exact: the schema-valid carrier `cc="[1]"` **parses as an array but not an array of strings**, so m-2's recipe must produce `cc_unparsed:"[1]"` while r13's words admitted `cc:[1]` — **different JCS bytes, different `canonical_resource` for the same invocation.**

Fixed at both live loci:
- **§7** now states *"decodes as a JSON **array of strings**"*, with the `"[1]"` case named as selecting `cc_unparsed`, never `cc`.
- **§10** now carries **three** CC legs, not two: array-of-strings ⇒ `cc`; **the non-string-array `cc="[1]"` ⇒ `cc_unparsed`** (asserting it selects `cc_unparsed`, never `cc`); empty/absent ⇒ both omitted — each to distinct JCS bytes.

**No m-2 byte moved.** The cell `5ec7a3d2…` is unmoved; I now consume its predicate exactly rather than a widened paraphrase of it.

## Untouched (your accepted portions)
Corrections **1** (Gate-2 relabel consumer-side, r21 §2.6 comparator byte-frozen), **2** (run-wide restore ratified + the `parked_unknown_capacity_exceeded` run-terminal consumer posture, no resume branch), and **4** (the explicit `turn_failed` supersession, non-generalizing) are **byte-unchanged**. So are the §5-E recipes, S-1's complement, §9's three-state ledger (items 4/5 EXACT-FOLDED, JOINT-PENDING), the m-1 carrier-scoped negatives, and §8. r21 and worker r7 re-hash unchanged.

## Re-review priorities (your gate)
(a) **F1** — is there exactly one current state for §7's `relay.submit` cell, with no other live `relay.* HELD`?
(b) **F2** — does the live §7 derivation and every §10 leg say "array of strings", and does the `cc="[1]"` fixture assert `cc_unparsed`?
(c) **No over-correction** — confirm I changed only the two defect loci and re-authored no m-2 byte, and that Corrections 1/2/4 + the preserved sections are byte-identical to r13.

Return `DESIGN_REVIEW_VERDICT`. On approve I return the hash to master; the **§D two-sided join co-signs once m-10's successor is also pair-approved**, and I bind m-10's pair-approved successor hash there, not ancestry rev14.

## Verification
- r14 `514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0`; supersedes r13 `675fa1fa30ba03e25453f64ff7092cbbbf5b79712555cb5c546216a25f36bfbf`.
- Byte-frozen/ratified + UNMOVED: worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · m-2 `relay.submit` cell `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f` · ratified amendment `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b`.
- The m-2 CC predicate re-read at the cell §2.1 this session: *"non-empty and decodes as a JSON array of strings ⇒ `cc` … does NOT so decode ⇒ `cc_unparsed`"* — the exact predicate r14 now consumes.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r14 (@ `514f8855…`) + this relay + one INDEX.md row; **no frozen/ratified byte moved** (worker r7, lifecycle r21, the amendment, and the m-2 cell all re-verified byte-exact), no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, no m-2/m-10 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r14 `514f8855…`; on approve m-9.planner returns the hash to master, and once m-10's successor is also pair-approved the §D two-sided join (+ m-1 redaction) co-signs, closing the lane-2 interface DAG → item A → lane 4 → lane 5. H-12 stands.
