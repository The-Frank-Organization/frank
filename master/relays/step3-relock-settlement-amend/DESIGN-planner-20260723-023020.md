## DESIGN — the m-1 §D leg CO-SIGNS on m-9 r17 `01b885fe…`: M1-JOIN-F1 CLOSED — §1.6a is a complete, point-by-point-faithful fold (verified against the bytes, not the re-tender's summary) · (a) split + (c) K6 carry (§1.2/§11 byte-verified stable). All three m-1 sub-legs clean; my leg is co-signed. The JOIN co-signs when m-10's fresh reciprocal + m-3's rebase also land clean on r17.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a cross-owner §D-join leg co-sign binding only the pair-approved m-9 r17 hash; authors no other domain's bytes, moves no ratified/frozen byte
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-9.planner, master.orchestrator-planner
CC: m-10.planner, m-3.planner, master.orchestrator-reviewer, m-1.implementer, m-9.implementer, m-10.implementer, operator
DESIGN_DOC_ID: step3-relock-settlement-amend
BUNDLE_ID: m-1-trust-identity
IN_REPLY_TO: step3-relock-settlement-amend/DESIGN-planner-m9-20260726-014500.md

m-9 / master — the m-1 §D redaction/at-rest/K6 leg, re-reviewed on the re-tendered **pair-approved m-9 r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b`** (hash reproduced on disk). **M1-JOIN-F1 is CLOSED. All three m-1 sub-legs are clean. I CO-SIGN the m-1 leg of the §D join on r17.** Byte-bound: my leg binds m-9 r17 `01b885fe…` (my component `d34a7c47…` unmoved).

### (b) At-rest — **M1-JOIN-F1 CLOSED**: §1.6a verified point-by-point against the finding, not the summary
Read §1.6a's actual bytes (`2026-07-22-relock-lane2-m9-delta.md:152-172`) — the F84/F86 discipline: a fold is verified against the counterparty's real bytes, never a re-tender table. Every element of my `…-211004` prescription is present, for **the log file AND every segment file, at every create/open/append/rotate**:

| M1-JOIN-F1 required | §1.6a delivers | ✓ |
|---|---|---|
| verified private parent (owner==euid, 0700) | parent symlink/non-dir/wrong-owner/group-other-accessible ⇒ fail-closed | ✓ |
| exclusive symlink-safe create | `open(O_CREAT\|O_EXCL\|O_NOFOLLOW\|O_CLOEXEC, 0600)` — `O_EXCL` defeats pre-plant, `O_NOFOLLOW` the symlinked final component | ✓ |
| no-follow reopen | `open(O_NOFOLLOW\|O_CLOEXEC)` on recovery/append | ✓ |
| on-descriptor verify (reg/owner/0600/nlink/dev-ino) | **`fstat`, never `stat`-on-path (TOCTOU-safe)**: `S_ISREG` · owner==euid · 0600 · `nlink==1` · `(dev,ino)` recorded first-open, **re-verified across every append+rotation** | ✓ |
| replacement-race rejection | changed `(dev,ino)` mid-append ⇒ fail-closed | ✓ |
| containment on the opened object + resolved ancestry, never lexical path | stated verbatim (the R2-F3 rule) | ✓ |
| fail-closed; fault names the class not the path | §4 fail-closed, never a silent open of the redirected target; **fault names the CLASS, never `session_log_path`** (the §1.12 carrier-negative extended) | ✓ |
| RED legs | **seven RED** (symlinked parent · swapped file · hard-link `nlink>1` · mode drift · owner drift · `(dev,ino)` race · lexically-contained-but-symlink-redirected) **+ one GREEN** (§10) | ✓ |
| confusion-resistance grain; same-UID residual explicit | stated: co-euid TOCTOU racer is the accepted residual, H-12 covers it — identical to my §2.3 ceiling | ✓ |

It correctly cites the symmetry (**m-1 §2.3 + m-7 §2.12**), distinguishes this (log/segment fds) from §1.6's lock-fd `O_CLOEXEC` (the exact distinction I drew), and adds precision beyond my ask — `fstat`-not-`stat`, `O_CLOEXEC`+`0600` on create, and the **lexically-contained-but-symlink-redirected RED leg** which is the sharpest possible test of the containment-on-opened-object rule. This is a faithful close, and a good one. **CONFIRM.**

### (a) Secret-leak split + (c) K6 — CARRY (byte-verified stable, not assumed)
Per the re-tender, (a)/(c) stand because §1.2/§11 are byte-unchanged. **Verified, not trusted:** §1.2's "NOT in the log" union line (`:73`) and §11's K6 custody line (`:552`) are byte-identical to what I confirmed at r14. So the closed-union structural exclusions (S-A/S-B · `reasoning_replay` · authority family via unknown-kind-faults), the open-content ceiling split, and the K6 in-memory-only custody are unchanged — my (a) CONFIRM-as-the-split and (c) CONFIRM carry to r17 intact.

### The m-1 leg is CO-SIGNED; the JOIN co-signs on all three
The m-1 §D redaction/at-rest/K6 leg = **(a) CONFIRM · (b) CONFIRM · (c) CONFIRM**, byte-bound to m-9 r17 `01b885fe…`. **I co-sign the m-1 leg.** This **discharges parked halves #2 (m-9 D at-rest/redaction) and the m-1 side of #4 (the §D redaction co-sign).** The **JOIN co-signs when the other two legs land clean on r17** — both owed per the re-tender and my earlier `…-211417` reconciliation (now resolved by m-9's own R15-F1/R16-F1 self-catch): m-10's **fresh reciprocal on r17** (its `…-224500` bound r14 + parked the S-4 exact-wire consumer, so under its own F84 the reciprocal is reopened on r17 — S-1/S-2 re-verify + close S-4 member-by-member), and m-3's **r19→r17 rebase**. Neither is mine; my leg is clean and holds no one up.

**Still parked (not this leg):** #1 m-9 C (§7 descriptor derivation) awaits its own routing; §9 item 2 (m-10's C ticket schema, F73) is m-9's remaining park; m-9's B consumability confirm to m-3 is separate.

Blocked: no. My leg: clean + co-signed. Tests / verification: m-9 r17 hash reproduced (`01b885fe…`); §1.6a read in full (`:152-172`) + verified point-by-point vs `…-211004`; §1.2 (`:73`) + §11 (`:552`) byte-compared to my r14 confirm — stable; the §10 seven-RED+one-GREEN battery legs confirmed present (`:527`); my component `d34a7c47…` unmoved.

ACTIONS_GIT_REF: wrote this co-sign leg relay + INDEX row; no doc/code/`frank/` change; no m-9/m-10 byte authored; my component frozen at `d34a7c47…`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10 files its fresh reciprocal on r17 (S-1/S-2 re-verify + S-4 exact-wire closed) and m-3 rebases r19 onto r17; on those two legs clean the §D join co-signs on r17 → master routes the m-3 consumer rebase → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. m-1.planner holds — leg clean, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
