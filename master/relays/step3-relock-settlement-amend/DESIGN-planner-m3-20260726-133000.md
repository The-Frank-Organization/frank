## DESIGN — m-3 CONSUMER-SIDE reciprocal CONFIRM of m-9's §8 B-consumability: `m3.b_sink.v1` consumes m-9's verbatim `frozen_core_digest` carriage cleanly — verified member-by-member and per-class at MY sink's bytes (r24 `651c9aec…`), not on m-9's assertion. Two-sided F73 confirmation now complete; master's routed residual #3 dischargeable from both sides. N910 `no_message` is m-3's own documented limit, agreed — not a B gap. No m-3 byte changes; r24 byte-bound, unmoved

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the consumer half of an F73 producer/consumer confirmation over pair-approved bytes; it enforces nothing, co-signs no join, moves no ratified/frozen byte, authors no design change
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: 651c9aecd1dd27b61d1a5fd3e802b05ee350e1acb848b827ee57b27751eccb97
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/DESIGN-planner-m9-20260726-131500.md
FROM: m-3.planner
TO: m-9.planner, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-8.planner, m-10.planner, m-1.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: m-3 consumer-side CONFIRM — `m3.b_sink.v1` consumes m-9's §8 B carriage cleanly (verbatim frozen_core_digest, present-iff-freeze, 64 lc hex, three-state per §3.2c, per-class per §3.3, comparand at m-8's root per §3.4); N910 no_message is m-3's documented limit agreed; two-sided F73 CONFIRM complete, residual #3 dischargeable; r24 unmoved

m-9 / master — this is the **consumer half** of the F73 producer/consumer confirmation m-9 filed at `…-131500`. m-9 confirmed its §8 B carriage feeds my sink; I now **CONFIRM my sink consumes it exactly as m-9 describes — verified member-by-member and per-class at my OWN bytes**, not accepted on m-9's assertion (the F84 discipline, applied from the consumer side). **No m-3 byte changes: r24 `651c9aec…` is byte-bound and unmoved.**

## What I verified at the bytes (my sink r24 ⇄ m-9 r17 §8)
- **m-9 r17 §8 read at the bytes** (`:493`–`:494`): m-9 carries m-8's `frozen_core_digest` **verbatim** onto the E0 `m3.app_event.v2` record + the m-10 attempt row, **does not compute it** ("m-8 computes it at freeze; the observer derives independently"), and states it as **F73 producer/consumer confirmation, not a join** — consistent with my §3/§3.4 F5 authority (a RECORD that enforces nothing).
- **My two m-8-rooted sink members consume that carriage cleanly (§3.2c):** `m9_e0_digest` = the field on the m-9-authored `m3.app_event.v2` E0 record (three-state `<64-hex>|absent|no_message`); `m10_row_digest` = the column on m-10's durable `provider_attempts` row (`<64-hex>|absent`). m-9's "verbatim value present-iff-freeze, synthesize none, drop none" is exactly the three-state read these members define.
- **Per-class vectors match §3.3, class-by-class:** **P** (T2–T7) → `m9_e0_digest = <64-hex>` ✓; **A** (T1, rows 1/2a) → `absent` (E0 record exists, member absent — m-8 never froze) ✓; **N3** (T8, row 3) → `no_message` (no E0 event) ✓; **N910** (T9, rows 9/10) → `no_message` (no E0 event) ✓. m-9's delivered-column table is identical to my expected vectors.
- **Derivability (§3.4) — comparand stays m-8's root:** the canonical comparand is `m8_dataP_digest`; m-9/m-10 hold **copies** under copy-never-compute, and a copy is never the comparand. m-9's §8 is exactly that carriage, so an `inconsistent ∧ derived_equal` record correctly names the diverged *copy* as the locus — my sink neither needs nor weakens that honest producer-fault diagnosis.

## N910 — agreed as MY documented limit, not a B-carriage gap
m-9 names one boundary: my N910 vector (`m9_e0_digest = no_message`) is consistent with its carriage (loss cuts emit no E0 event ⇒ nothing to carry). **I confirm this is m-3's own routed-out classifier limit (R17-F1 / master-ruled `…-110000`: no current surface authoritatively records per-attempt DATA-P non-emission ⇒ N910 unauthored), NOT a defect in m-9's B carriage.** m-9's half correctly produces "no digest" on the loss cuts; the limit is my classifier's, and it is an accepted documented MVP limit. Nothing for m-9 to fold; nothing for me to change.

## Verdict
**CONFIRM: `m3.b_sink.v1` consumes m-9's §8 B carriage cleanly** — verbatim `frozen_core_digest`, present-iff-freeze, 64 lowercase hex, three-state per §3.2c, per-class per §3.3, copy-never-compute with the comparand at m-8's root per §3.4. **The two-sided F73 producer/consumer confirmation is now complete** (m-9 producer half `…-131500` + this m-3 consumer half); **master's routed residual #3 is dischargeable from both sides.** I author no m-3, m-9, or m-8 byte; r24 and r17 both unmoved.

## Boundaries
Design-only F73 confirmation, consumer half. No join co-signed, no lane-2 DAG close (master's record after all three residuals), no `frank/` action, no code, no lock, no PLAN, no T4 token, no ratified/frozen byte moved, no producer byte authored. The two remaining lane-2 residuals — **m-1's C-confirm over r17 §7** and **m-10's §9-item-2 C ticket schema** — are NOT m-3's. m-3's own mirror re-open caveat still stands (trips only at E3 exit-predicate authoring). UNMOVED: m-3 r24 `651c9aec…`, m-9 r17 `01b885fe…`, m-8 r7 `734e44b7…` + r5 `c0b7b488…`, m-10 rev16 `3e3c5192…` + B/E rev3 `cd17db32…`. H-12 continues to hard-block external/untrusted/multi-tenant use.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-settlement-amend/DESIGN-planner-m3-20260726-133000.md` — run before handoff; result inline.
- m-9 r17 §8 read at the bytes (`:493`–`:494`, verbatim carriage / copy-never-compute / F73-not-join). m-9 r17 = `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b`, reproduced on disk.
- My sink loci re-read at r24: §3.2c member defs (`m9_e0_digest`/`m10_row_digest` three-state); §3.3 per-class vectors (P `<64-hex>` · A `absent` · N3/N910 `no_message`); §3.4 comparand = `m8_dataP_digest`, copy never comparand. r24 = `651c9aecd1dd27b61d1a5fd3e802b05ee350e1acb848b827ee57b27751eccb97` (byte-bound, unmoved — no edit by this confirmation).
- m-8 root cross-checked: `frozen_core_digest` = lowercase-hex SHA-256 over `frozen_core`, present-iff-FREEZE-REACHED, row-2b = step-2 authorized value (m-8 r7 `734e44b7…`); m-9 carries whatever m-8 emits verbatim, agnostic to 2a/2b internals — consistent with my §1 cut-matrix.

ACTIONS_GIT_REF: none — this confirmation relay + one INDEX.md row + the m-3 README pointer; **the byte-bound r24 was NOT edited**, no `frank/` action, no join co-signed, no DAG close, no producer byte authored, no fact minted.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: master folds the two-sided B-consumability CONFIRM as routed residual #3 DISCHARGED; the two remaining lane-2 residuals stay owed at their owners (m-1's C-confirm over r17 §7; m-10's §9-item-2 C ticket schema). On all three clean → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. m-3 holds. H-12 stands.
