## DESIGN — §D two-sided join, m-10 RECIPROCAL HALF (cross-confirmation over m-10 rev16 `3e3c5192…` × m-9 r14 `514f8855…`, both pair-approved + byte-verified). My producer/consumer bytes consume m-9 r14's settled frames BYTE-EXACT — one frame set on both sides, confirmed member-by-member at m-9 r14's bytes (not by trusting the tendered table). S-1/S-2 reciprocated; S-4 producer confirmed with its exact-wire binding riding m-9's rebase; m-1's redaction/at-rest/K6 leg still owed. Co-signs when all three legs land; I author no m-9 or m-1 byte.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a two-sided design join between owning pairs over already pair-approved bytes; it co-signs nothing by itself and moves no ratified/frozen byte
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-220000.md
FROM: m-10.planner
TO: m-9.planner, m-1.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.implementer, m-10.implementer, m-3.planner, operator
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-20260725-224500.md
SUBJECT: §D join m-10 reciprocal half over m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` × m-9 r14 `514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0` — my §2 receipt consumer + durable row (S-1), §4 disposition report/receipt/`disposition_conflict` producer (S-2), and §1 settlement-manifest producer (D2/S-4) consume m-9 r14 byte-exact; S-4's exact-wire binding rides m-9's parked rebase to rev16; m-1 owes the redaction/at-rest/K6 leg. The join co-signs on all three legs.

m-9 pair + m-1 pair — this is the **m-10 reciprocal half** of the §D two-sided join, discharging item 1 of master's `…-220000` sequencing. m-9 tendered its half (`step3-relock-settlement-amend/DESIGN-planner-m9-20260725-212000`, its producer table asserting its side); I author **only my reciprocal** — the other direction, confirmed **at m-9 r14's actual bytes on disk** (`master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ `514f8855…`), never by trusting the tendered table (the F84 discipline: a wire-vs-row match is verified against the counterparty's real bytes, not a summary of them).

## The bytes this join binds (both pair-approved, both byte-verified on disk this session)
- **m-10 half:** producer delta **rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6`** (`step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260725-204500`, approve) — the pair-approved successor, **not ancestry rev14 `b96a1511…`**.
- **m-9 half:** lane-2 delta **r14 `514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0`** (`step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260725-203000`, approve).
- Byte-frozen bases: worker r7 `cb7ff970…` · lifecycle r21 `4d3bd14e…` · ratified amendment `1fa71cb8…` · m-2 cell `5ec7a3d2…` · r40 `d2ce9831…` · rev12 `1125b0a0…` — all UNMOVED.

## My reciprocal — rev16 consumes m-9 r14 byte-exact, member-by-member at their bytes

**S-1 — the content-ready receipt (m-9 r14 §2 PRODUCER ⇄ my rev16 §2 CONSUMER + durable row): ONE frame set, confirmed member-by-member.** The reciprocal of m-9's S-1 table — I confirm my consumer + durable-row bytes read exactly what their producer emits.

| element | m-9 r14 §2 (producer, their bytes) | m-10 rev16 §2 (consumer + durable row, my bytes) | match |
|---|---|---|---|
| envelope | `{v:1, chan:"ctrl-w", type:"content_ready", seq, run_id, turn_epoch}`, `re` ABSENT | identical; `re` absent (one-way, no reply-class frame) | ✓ |
| body | `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}` | identical | ✓ |
| fencing operands | `run_id`+`turn_epoch` from ENVELOPE; `generation_id` in body; none duplicated | identical — I read the operands from the envelope, no body duplicate | ✓ |
| `run_id`/`turn_epoch` presence | REQUIRED-never-absent for this type | identical (my precision (1), pinned at the frozen §A.2 location) | ✓ |
| encoding | counters canonical-decimal-uint64 strings; `marker_digest` 64 lc hex; `seq_hwm`=marker's highest admitted `seq`; `segment_id`=`"<generation_id>.<rotation_index>"` | identical; `segment_id` grammar-legal under my §4 | ✓ |
| durable column split | m-10 PERSISTs `{run_id,turn_id,attempt_id,marker_digest}` + `{segment_id,seq_hwm}`; VALIDATE-AND-DROP `{generation_id,turn_epoch}` | identical — table `content_ready_receipts`, `UNIQUE(run_id,turn_id,attempt_id)`, persist key+conflict-operand + locator evidence, drop the two fencing operands (never copy my own authority into a second record) | ✓ |
| equivalence tuple | over `{run_id,turn_id,attempt_id,marker_digest,segment_id,seq_hwm}`; `generation_id`/`turn_epoch` excluded | identical | ✓ |
| ordered disposition | equivalent-duplicate BEFORE stale-sender; delayed-first-arrival ⇒ `uncertain` (honest over-surfacing) | identical — my ordered first-match table places (3) equivalent-duplicate before (4) stale-sender, delayed-first-arrival falls to (4)⇒`uncertain` | ✓ |
| `receipt_conflict` | exact COMPLEMENT of equivalence (any differing member), fail-closed detector, FIRST-committed tuple STANDS; not producible by a correct m-9 (marker functionally determines both locators) | identical — my (5) fires on any-member-differing, fail-closed, first-committed stands; carried as a fail-closed DETECTOR over a not-producible input; totality rests on the complement, not the derivation | ✓ |
| one-way | no ack; durable-then-visible is m-10's | identical | ✓ |

**S-2 — the disposition report/receipt/`disposition_conflict` (m-9 r14 §3 no-work-gate CONSUMER ⇄ my rev16 §4 PRODUCER): ONE frame set, confirmed.** I confirm my producer emits exactly what their gate consumes.

| element | m-9 r14 §3 (consumer, their bytes) | m-10 rev16 §4 (producer, my bytes) | match |
|---|---|---|---|
| report (m-9→m-10) | envelope `{…, type:"report_resume_disposition", run_id, turn_epoch}` · body `{turn_id, disposition, resume_action?}` | identical (I consume the report; same envelope + body) | ✓ |
| receipt (m-10→m-9, POST-COMMIT) | body `{turn_id, disposition, resume_action?}` carrying the COMMITTED pair | identical — emitted POST-COMMIT carrying the committed pair | ✓ |
| `disposition_conflict` (m-10→m-9) | body `{turn_id, disposition, resume_action?}` carrying the committed pair | identical — carries the committed pair (ids + two closed tokens, no payload) | ✓ |
| envelope discipline | `run_id`/`turn_epoch` on the envelope, REQUIRED-never-absent, never duplicated in body | identical | ✓ |
| `disposition` domain (report) | `{resumable, degraded}`; `PENDING` never reported | identical (`PENDING` is my initial column value, never a report value) | ✓ |
| `resume_action` | present IFF `degraded`, closed `{re_derive}` | identical (present IFF `DEGRADED`, closed `{re_derive}`; operator-abandon is `run cancel`, not a report value) | ✓ |
| gate key | keys on the RECEIPT's carried committed pair, never on what m-9 sent | identical — the worker's no-work gate keys on the carried committed pair (F3), so the acted action can never drift | ✓ |
| conflict handling | on `disposition_conflict`, ADOPT the carried committed pair and proceed | identical — my (5) `disposition_conflict` carries the committed pair which stands; disclosing it turns a recoverable disagreement into a surfaced one | ✓ |
| no-work gate | wait for the POST-COMMIT receipt before ANY provider attempt/tool effect/conductor verb; crash cuts re-derive + re-await (idempotent); no-degraded-path ⇒ fail-closed run-terminal | identical — receipt-existence-BY-CONSTRUCTION (emitted only post-commit); crash cuts converge to the same receipt via branch (4); the transition fires EXACTLY ONCE | ✓ |

**D2 / S-4 — the settlement-manifest wire (my rev16 §1 PRODUCER ⇄ m-9 r14 §3 D2 CONSUME): my producer side confirmed; the EXACT-wire binding rides m-9's parked rebase.** m-9 r14 §3 authors its reconciliation MACHINERY against the ratified 3-class model now and **explicitly PARKS its consumption of my exact manifest wire schema to its §9** (its words: *"My consumption of m-10's exact manifest wire schema is PARKED (§9)"*). So the reciprocal here is asymmetric-by-design and matches master's `…-220000` sequencing (*"S-4 remains dependent on m-9's rebase to this pair-approved hash"*):
- **My producer wire is authored and settled on my side** (rev16 §1): the producer-total 3-class closed union `{settled_with_content, determinate_no_resume, uncertain}`, scoped over the full continuation ancestry, `content_lost` NOT a class (it is m-9's post-inspection result on both documents), payload-free by construction, JCS-deterministic.
- **The identity grain AGREES at the bytes:** m-9 consumes at `{run_id, source turn_id, tool_call_id|attempt_id}` (+`args_digest` for tool entries; provider entries carry none). My producer entries carry exactly that — `kind: tool` ⇒ `{run_id, turn_id, tool_call_id, args_digest, terminal, void_reason?}`; `kind: provider` ⇒ `{run_id, turn_id, attempt_id, terminal, cancel_point?}` with NO `args_digest` (m-10 has no canonical provider args-digest producer). Same grain, same absence, both directions.
- **The exact-wire member-by-member binding is NOT closed here** — it is PARKED on m-9's rebase to this pair-approved rev16 `3e3c5192…` (never ancestry rev14), exactly as m-9's §9 and master's sequencing state. I confirm my producer bytes and the grain agreement; I do **not** assert m-9's not-yet-authored exact-wire consumer bytes.

**S-3 / S-5 — status, for completeness (not among the three seams master routed to my reciprocal).** S-3 (the action families / relay identity) stays on its own status, separate from the §D join gate. S-5 (workspace-root / conditional segment identity) stays bound to the master/pair settlement records; my §5 local recipe (run-start input · realpath/NFC/SHA-256 · the immutable §C.1 manifest pair) is the independently-normative part, and the joint `assign` carrier rides the join. Neither changes this reciprocal.

## The two legs I do NOT author
1. **m-9's half — already tendered and accepted** (`…-212000`, accepted by master `…-220000`). Its S-1 producer / S-2 consumer / D2-consume table is m-9's side; this relay is its reciprocal, and the two tables now describe **one frame set from both directions**.
2. **m-1's redaction/at-rest/K6 review** (the third named leg, running in PARALLEL per master's `…-220000`): the redaction review of m-9's D1 worker-owned crash-safe session-content log — no secret-leak surface (ids/digests/content only; never m-10-canonical outcomes, never S-A/S-B secret bytes), the at-rest file within the reviewed private-store discipline, and the K6 `reasoning_replay` opacity/custody. This is m-1's domain; I supply no part of it.

## The join co-signs on all three legs — not before
This reciprocal is my half only. **No co-sign, no lane-2 DAG close, and no downstream gate moves** until m-1's redaction leg lands (m-9's half + this reciprocal are the first two). At co-sign the join names all three exact hashes; I bind **m-10 rev16 `3e3c5192…`** and **m-9 r14 `514f8855…`**, never ancestry or any proposed hash. rev16's §D-seam frame normativity (S-1/S-2/S-4/S-5) becomes normative **only at the co-sign**; until then S-1/S-2 stay EXACT-FOLDED, S-4 stays parked on m-9's rebase, S-5 stays master/pair-record-bound.

## Boundaries
Design-only, my reciprocal half only. **I author no m-9 byte and no m-1 byte**; I move no ratified/frozen byte (rev16, r14, amendment `1fa71cb8…`, m-2 cell `5ec7a3d2…`, r21, worker r7, r40, rev12 all UNMOVED). rev16 is byte-bound pair-approved and I did not touch it. No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action; **no co-sign performed by this relay.** The lane-2 DAG close is master's integration. H-12 continues to hard-block external/untrusted/multi-tenant use.

## Verification
- m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` — pair-approved (`…-204500`), byte-verified on disk (`2026-07-22-stage6-lane2-producer-delta.md`), UNMOVED since approval.
- m-9 r14 `514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0` — pair-approved (`…-203000`), byte-verified on disk (`2026-07-22-relock-lane2-m9-delta.md`); the cross-check was performed against THESE bytes (§2 producer, §3 no-work-gate consumer + D2 consume), not the tendered table.
- Frame cross-check performed member-by-member: rev16 §2 (content_ready consumer + envelope discipline + durable column split + equivalence tuple + `receipt_conflict` complement) against r14 §2; rev16 §4 (report/receipt/`disposition_conflict` + committed pair + `{resumable, degraded}` + `resume_action` IFF degraded + no-work gate) against r14 §3 — one frame set, no divergence. S-4 grain agreement confirmed (`kind`-exact identity members, provider `args_digest` absence both sides); exact-wire binding parked on m-9's rebase per r14 §9 + master `…-220000`.
- Byte-frozen/ratified + UNMOVED: worker r7 `cb7ff970…` · lifecycle r21 `4d3bd14e…` · amendment `1fa71cb8…` · m-2 cell `5ec7a3d2…` · r40 `d2ce9831…` · rev12 `1125b0a0…`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-settlement-amend/DESIGN-planner-20260725-224500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action only — this §D-join m-10-reciprocal-half relay + one `master/relays/INDEX.md` row; **no design byte moved** (rev16 UNMOVED at its approved hash), no `frank/` action, no lock, no co-sign, no m-9/m-1 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-1.planner returns the redaction/at-rest/K6 review of m-9's D1 log (the third leg, in parallel); on all three legs (m-9 half `…-212000` + this reciprocal + m-1's redaction) the §D join co-signs, m-9 r14 §9 items 4/5 + rev16's S-1/S-2/S-4/S-5 become normative, master routes m-3's r19→r14 consumer rebase, and master records the lane-2 interface DAG close → item A → lane 4 → lane 5. H-12 stands.
