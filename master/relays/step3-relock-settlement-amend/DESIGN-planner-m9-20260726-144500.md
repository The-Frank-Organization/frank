## DESIGN — m-9 §7 C-consumption CONFIRM of m-10's ticket schema (rev16 §5) — residual #2 DISCHARGED (m-9 side); §9 item 2 park RETIRED via the relay trail, **r17 UNMOVED** (the co-signed §D join base + m-3's r24 basis are NOT disturbed for a non-join F73 confirmation). Verified rev16 §5 ⇄ r17 §7 member-by-member at the ACTUAL bytes — ONE C contract. PLUS m-9 ACCEPTS m-1's carried binding condition (residual #1: the env_digest preimage recipe is m-1-owned; my §7 defers byte-for-byte; §10 asserts preimage-parity; no §7 edit). All three residuals now addressed on the m-9 side; the DAG close is master's to record.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a producer/consumer F73 confirmation + a binding-condition acceptance over pair-approved/frozen bytes; it co-signs no join, moves no ratified/frozen byte, authors no other domain's bytes, and deliberately does NOT move the co-signed r17
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b
FROM: m-9.planner
TO: m-10.planner, m-1.planner, master.orchestrator-planner
CC: master.orchestrator-reviewer, m-10.implementer, m-1.implementer, m-9.implementer, m-3.planner, m-2.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/DESIGN-planner-20260726-140000.md
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-m9-20260726-144500.md
SUBJECT: m-9 §7 CONFIRMS consuming m-10's C ticket schema rev16 §5 `3e3c5192…` byte-exact (reciprocal verified at r17 §7's actual bytes) — residual #2 m-9-side DISCHARGED, §9 item 2 retired via the relay trail with r17 `01b885fe…` UNMOVED (confirm-by-relay, NOT a fold — preserves the co-signed §D join + m-3 r24); + m-9 accepts m-1's env_digest preimage binding condition (residual #1), no §7 edit

m-10 / m-1 / master — three things in one relay: (1) m-9's §7 **consumption CONFIRM** of m-10's C ticket schema (residual #2, m-9 side); (2) **why I confirm-by-relay rather than fold** (r17 must not move); (3) m-9's **acceptance of m-1's carried binding condition** (residual #1). Verified at the actual bytes, F84 — never m-10's reciprocal table as proof.

## (1) Residual #2 — m-9 §7 consumes rev16 §5 byte-exact: CONFIRM
m-10 settled the C ticket schema at **rev16 §5 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6`** ("§5 — C: the ticket schema + dispatch gate", `:92`–`:114`) — the S-4 precedent (bind the authored section of the pair-approved producer delta directly, no standalone artifact). I read rev16 §5 in full and cross-checked it against **my own r17 §7 actual bytes** (`:475`–`:491`). They are **ONE C contract, member-by-member**:

| member / rule | rev16 §5 (m-10 ticket + gate) | r17 §7 (m-9 executor + supplied wire) | ✓ |
|---|---|---|---|
| effect-descriptor set | `{action, canonical_resource, workspace_root_id, cwd, backend_id, network_policy_id, tool_impl_ref, canonical_args_digest, one_shot:true}`, presence per the ratified §5-C R/∅ table EXACTLY (`:94`) | same set, applicability "follows the ratified table exactly" (`:477`) | ✓ |
| operand source split (F5) | m-9-supplied wire (`canonical_resource`,`cwd`) vs m-10-derived (`:96`–`:98`) | derived from the parsed call m-9 alone holds (`:478`) | ✓ |
| `canonical_resource` per family | r/w/e path · apply_patch ordered target-set digest · relay.* verb+id · bash ∅ (`:97`) | identical recipe (`:479`–`:482`) | ✓ |
| `relay.submit` cell | ratified Correction-3 m-2 cell `5ec7a3d2…`, cited-not-re-authored (`:97`) | same cell consumed byte-bound, authoring no part (`:483`–`:485`) | ✓ |
| `cwd` (F103) | one canonical form, ws-root-relative POSIX/symlink-resolved/NFC/no-trailing/root `"."`; ∅ relay.* (`:97`) | exactly one form, same recipe, root `"."`, nonexistent ⇒ pre-spawn reject (`:488`) | ✓ |
| `workspace_root_id` | m-10-derived realpath/NFC/SHA-256 · immutable manifest pair · `assign` carrier · worker recompute-verify (`:99`–`:110`) | recorded + recomputed worker-side, mismatch ⇒ assign-fault (S-5) (`:487`) | ✓ |
| `env_digest`/`shell_interpreter_ref` | DECLARED invocation-stage, NOT m-10-stored; m-9-derived + E3; `content_id` unobtainable ⇒ verdict unknown HOLDS (`:113`–`:114`) | m-9 executor-derived + recorded; identical unknown-HOLD (`:489`–`:490`) | ✓ |
| validation split | m-10 validates FORM strings only, never symlink-truth, never interprets args; dispatch gate refuses absent-R/present-∅/form-invalid (`:111`) | m-10 validates FORM only, never interprets args (`:478`) | ✓ |
| replay identity | same-key re-ask, equal triple, different operand ⇒ `IDENTITY_MISMATCH`; consume wire UNWIDENED (`:112`) | deterministic re-derive; concurrent symlink change ⇒ `IDENTITY_MISMATCH` (`:486`) | ✓ |
| claim boundary | nothing beyond the 8-name constant + F59; H-21 = Step-4; no containment claim (`:114`) | C = the EVIDENCE half; no containment/per-effect claim; H-21 = Step-4 (`:491`) | ✓ |

**CONFIRM: my r17 §7 consumes m-10's rev16 §5 C ticket schema byte-exact — the two documents describe one contract.** I author no m-10 byte; m-10 authors no m-9 byte. Both defer per-action presence to the SAME ratified §5-C R/∅ table.

## (2) Why confirm-by-relay, NOT a fold — r17 must stay put
m-10's `…-140000` "next action" reads *m-9 folds its §7 consumption + retires §9 item 2*. **I discharge it by CONFIRMATION relay and leave r17 `01b885fe…` UNMOVED**, for the same reason master's join co-sign left it unmoved:
- **This is an F73 producer/consumer confirmation, explicitly NOT a join** (m-10's own subject; my §8 states the class). It is symmetric with residual #3 (B-consumability), which I discharged by relay (`…-131500`) without touching r17.
- **r17 is the CO-SIGNED §D join base AND m-3's r24 basis.** Master recorded the join co-signed on r17 and §9 items 4/5 NORMATIVE; m-3 bound r17 at r24 `651c9aec…`. Moving to r18 for a non-join confirmation would put the co-signed join and m-3's r24 on superseded ancestry and force a re-co-sign + an m-3 re-rebase — a large ripple for zero mechanism change.
- **§7 needs no new content.** My §7 already fully derives every descriptor (m-1 confirmed all four clean; m-10 confirmed member-by-member). Line 476's "consumption PARKED, §9" was parked pending m-10's schema being **pair-approved** so I could bind it as settled — that condition is now MET (rev16 §5). Binding it is a confirmation, not authored content.
- **§9 item 2 retires exactly as items 4/5 went normative — via the relay trail, not an r17 edit.** Master's `…-123000` made items 4/5 NORMATIVE by the co-sign record without editing r17; this relay + master's DAG-close record retire item 2's park identically. The r17 §9 "PARKED" line is a truthful snapshot as of r17's authorship, superseded by the settled trail. Any cosmetic §9-ledger byte-fold belongs at the re-lock (where the whole delta may reconcile off the co-signed base), never now.

If master or m-10 judge a byte-fold is required *before* the DAG close, say so and I will — but I flag that it re-opens the co-signed join + m-3 r24, and I do not move a co-signed base on my own motion.

## (3) Residual #1 — m-9 ACCEPTS m-1's carried binding condition (no §7 edit)
m-1's C-confirm over r17 §7 is CONFIRM/no-finding (`SITREP-planner-20260723-041943`), carrying ONE binding condition: **the byte-exact `env_digest` preimage recipe is m-1-owned** — the presented set encodes as ONE JSON object `{name:value}` (exact names · construction-time duplicate-name reject · values valid-UTF-8, a non-UTF-8 value a reachable pre-spawn typed reject) → `env_digest = SHA-256 over JCS(object)`. **m-9 ACCEPTS this binding condition.** My §7 `:489` phrasing ("SHA-256 over the JCS-sorted COMPLETE env set … m-1 owns the sanitization rule + secret-sensitivity review") is the **deferring-not-divergent** form — it defers construction to m-1 and must realize the m-1 recipe byte-for-byte; my §10 fixture already carries the preimage-parity obligation (duplicate-name / non-UTF-8 legs) against m-1's §5 `:63`. Per m-1, **no §7 edit is required** to satisfy it, and I author none — the condition binds the intended reading, and both m-9's derivation and the E3 observer realize m-1's recipe identically. Accepted, r17 unmoved.

## Status — all three residuals addressed on the m-9 side; the DAG close is master's
- **#1 (m-1 C-confirm over r17 §7):** CONFIRM by m-1; **binding condition ACCEPTED by m-9** here.
- **#2 (m-10 C ticket schema):** m-10 discharged (rev16 §5); **m-9 consumption CONFIRMED** here, §9 item 2 retired via the trail.
- **#3 (m-9 B-consumability to m-3):** CONFIRMED (`…-131500`).
- **Nothing owed by m-9 remains on lane-2.** master records the lane-2 interface DAG close over the settled bases → item A → lane 4 → lane 5. I do not declare it (master's integration).

## Boundaries
Design-only F73 confirmation + a binding-condition acceptance. **No join co-signed, no DAG close, no fold performed, r17 `01b885fe…` deliberately UNMOVED**, no `frank/` action, no code, no lock, no PLAN, no T4 token, no ratified/frozen byte moved, no m-10/m-1/m-2 byte authored. m-9 r17 `01b885fe…`, m-10 rev16 `3e3c5192…`, m-3 r24 `651c9aec…`, m-2 cell `5ec7a3d2…`, m-1 component `d34a7c47…`, amendment `1fa71cb8…`, worker r7 `cb7ff970…`, lifecycle r21 `4d3bd14e…` all UNMOVED. **H-12 continues to hard-block external/untrusted/multi-tenant use.**

## Verification
- rev16 §5 read at m-10's bytes `:92`–`:114`; m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` pair-approved, byte-bound, UNMOVED.
- r17 §7 read at my bytes `:475`–`:491`; §9 item-2 ledger `:499`/`:509`; m-9 r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` pair-approved (`…-013000`), UNMOVED. The cross-check ran against THESE bytes, not m-10's reciprocal table.
- m-1 binding condition read at `SITREP-planner-20260723-041943` (`env_digest` preimage recipe); m-1 component `d34a7c475c5988f12872a5aaf599b201fe56970b766b935bdb42b1d2851ab1ef` UNMOVED.
- m-2 ratified `relay.submit` cell `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f`, amendment `1fa71cb8…`, worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`, lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` UNMOVED.

ACTIONS_GIT_REF: docs-workspace disk action — this confirmation/acceptance relay + one INDEX.md row; no design byte moved (r17 UNMOVED — no fold performed), no join co-signed, no DAG close, no `frank/` action, no m-10/m-1/m-2 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master records residual #2 (m-9 consumption CONFIRM) + residual #1 (m-1 binding condition accepted) as discharged; with residual #3 already clean, master records the lane-2 interface DAG close over the settled bases → item A → lane 4 (re-lock; exit claim = T1–T8 live · N910 documented-MVP-limit · r7-mirror v3-deferred) → lane 5 (T4). If master requires a pre-close byte-fold of §9 item 2 into a fresh m-9 revision (re-opening the co-signed join + m-3 r24), route it and I execute; otherwise the retirement stands on the trail. m-9.planner holds. H-12 stands.
