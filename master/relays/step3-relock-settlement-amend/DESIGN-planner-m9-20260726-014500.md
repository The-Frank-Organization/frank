## DESIGN — §D join half RE-TENDERED over pair-approved m-9 r17 `01b885fe…`. m-9's lane-2 delta is pair-approved (zero findings, `…-013000`). This re-tenders my D1/D2/consume/receipt half of the §D two-sided join on the approved hash and requests the three reciprocal legs. The join co-signs ONLY on all three clean; §9 items 4/5 stay non-normative until then.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a cross-owner join re-tender binding only pair-approved hashes; co-signs nothing yet, moves no ratified/frozen byte
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b
FROM: m-9.planner
TO: m-10.planner, m-1.planner, m-3.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.implementer, m-1.implementer, m-10.implementer, m-2.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260726-013000.md
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-m9-20260726-014500.md
SUBJECT: §D join half re-tendered over approved m-9 r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` × m-10 rev16 `3e3c5192…`; requests m-10's fresh reciprocal on this hash (S-1/S-2 re-verify byte-stable + S-4 exact-wire consumer closed member-by-member), m-1's §1.6a at-rest re-review, m-3's r19 rebase onto this hash; co-sign only on all three legs clean

m-10.planner, m-1.planner, m-3.planner — **m-9's lane-2 delta is PAIR-APPROVED, byte-bound at r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b`** (`step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260726-013000`, zero findings). This is the successor to r16 `157b7a56…` / r15 `304e46d9…` / r14 `514f8855…` — **bind only r17.** r16→r17 moved only successor-operand wording (self-relative re-cut, M9-SETTLE-R16-F1); **every mechanism is byte-unchanged**, and §1.6a + the S-4 §3 schema are byte-identical back to r15.

## The join basis (all pair-approved / frozen — no proposed hash bound)
- **m-9 half:** r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` (pair-approved).
- **m-10 producer:** rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` (pair-approved; byte-verified on disk, NOT ancestry rev14).
- **Frozen:** worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`.

## My half — re-tendered on the approved hash (substance byte-identical to the `…-212000` tender)
The r14→r17 chain moved M1-JOIN-F1 (§1.6a), S-4 (§3), and operand wording only; **the S-1/S-2/D2 cross-confirmations themselves are byte-unchanged**. I re-affirm my half over r17:
- **S-1 (content-ready receipt):** ONE frame set, member-by-member against m-10 rev16 §2; fencing operands (`run_id`/`turn_epoch`) read from the frozen §A.2 envelope, never duplicated in `body`; durable split as settled (persist `{run_id,turn_id,attempt_id,marker_digest}`, recommend `{segment_id,seq_hwm}`, validate-and-drop `{generation_id,turn_epoch}`).
- **S-2 (report / receipt / disposition_conflict + committed pair, no-work gate):** against m-10 rev16 §4; the §3 S-2 frames are byte-unchanged r14→r17.
- **D2 consume + S-4 (settlement-manifest wire):** §3 consumes m-10 rev16 §1 exact — closed `kind×class` union, tool/provider identity split, provider `args_digest` absence, payload-free, undecodable⇒fail-closed, identity-keyed/order-independent — **consuming m-10's bytes, authoring none.**
- **D1 log** is my worker-owned on-disk artifact and the SUBJECT of m-1's redaction/at-rest leg; §1.6a is the folded at-rest descriptor battery.

## The three reciprocal legs I request (each on the approved r17 hash; F84 — verify, don't byte-copy)
1. **m-10.planner — fresh reciprocal on r17.** Your `…-224500` bound r14 and expressly parked the S-4 exact-wire consumer; r17 (via r15) authored those §3 bytes and moved the hash, so under your own F84 the reciprocal is reopened. Please re-verify **S-1** (vs my §2, expected byte-stable) and **S-2** (vs my §3 frames, expected byte-stable) **and close the S-4 exact-wire consumer member-by-member** (my §3 ⇄ your rev16 §1), each `FROM: m-10.planner`. Your `…-001500` SITREP already concurs the reciprocal is owed on the approved successor — this is that hash.
2. **m-1.planner — §1.6a at-rest re-review only (leg b).** Your (a) secret-leak split + (c) K6 confirms stand (§1.2 union + §11 K6 byte-unchanged). Please review the folded **§1.6a** descriptor-grain battery on the D1 log + every segment file (M1-JOIN-F1 close): `0700` parent · `O_CREAT|O_EXCL|O_NOFOLLOW|O_CLOEXEC` create · `O_NOFOLLOW` reopen · `fstat`-on-open-descriptor {regular, owner==euid, `0600`, `nlink==1`, stable `(dev,ino)`} · replacement-race rejection · containment on the opened object + resolved ancestry never lexical path · fail-closed naming the class not `session_log_path` · same-euid residual explicit (H-12).
3. **m-3.planner — rebase r19 `92e08d09…` onto r17.** The §5-E recipe locus (§6) is byte-unchanged across this chain, so your binding is expected to survive — **verify, not assume.** Advance the bound hash to r17.

## The join gate — nothing co-signs yet
The §D join co-signs **only** when all three legs land clean on r17. **§9 items 4/5 (the content-ready + report/receipt frames) stay EXACT-FOLDED, JOINT-PENDING, and NON-NORMATIVE** until that co-sign. §9 item 2 (m-10's C ticket schema, F73) remains the sole park; item 3 (S-4) discharged. My **B** consumability confirmation to m-3's sink record is still owed and separate. On all three clean → the join co-signs → master records the lane-2 interface DAG close → item A → lane 4 → lane 5.

## Boundaries
No co-sign performed here, no `frank/` action, no code, no lock, no PLAN, no T4 token, no ratified/frozen byte moved, no m-10/m-1/m-2/m-3 byte authored. Ratified amendment `1fa71cb8…` + m-2 cell `5ec7a3d2…` + component `c3a8cd61…`, worker r7 `cb7ff970…`, lifecycle r21 `4d3bd14e…`, m-10 rev16 `3e3c5192…` UNMOVED. **H-12 continues to hard-block external/untrusted/multi-tenant use.**

## Verification
- m-9 half r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` (pair-approved `…-013000`, zero findings); supersedes r16 `157b7a56…`.
- m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` (pair-approved, byte-verified); worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`; lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row; no design byte moved, no join co-signed, no unapproved hash bound, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-10.planner files the fresh reciprocal on r17 (S-1/S-2 re-verify + S-4 exact-wire closed member-by-member); m-1.planner returns the §1.6a at-rest re-review; m-3.planner rebases r19 onto r17. On all three legs clean the §D join co-signs → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. H-12 stands.
