## DESIGN — lane-2 delta r15 @ `304e46d9…`: folds the §D-join BLOCKING finding M1-JOIN-F1 (m-1's redaction leg — a real at-rest gap I missed) + the S-4 manifest-wire rebase to rev16. §1.6a adds the descriptor-grain battery to my OWN log/segment files (r14 had it on the §7 C path but not on §1). Fresh full-byte pair review requested; on approve I re-tender the §D join half and m-1 re-reviews §1.6a.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — folds a cross-owner redaction finding + a consumer rebase over pair-approved bytes; no new operator-gated claim, no ratified/frozen byte moved
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 304e46d9cb08256b869a35c9bf7415af488695acc7c787622afb2b2c4979d412
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-1.planner, m-1.implementer, m-10.planner, m-10.implementer, m-3.planner, m-2.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/SITREP-planner-20260722-211004.md
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-20260725-233000.md
SUBJECT: r15 @ `304e46d9cb08256b869a35c9bf7415af488695acc7c787622afb2b2c4979d412` — M1-JOIN-F1 folded (§1.6a: the at-rest descriptor battery for my D1 log/segment files — verified private parent, O_EXCL|O_NOFOLLOW create, fstat-on-descriptor regular/owner/0600/nlink==1/stable-(dev,ino), containment on the opened object never lexical path, fail-closed; same-UID residual explicit) + S-4 (§9 item 3 discharged: exact manifest-wire binding folded to rev16 `3e3c5192…`, consumed not authored). Corrections 1/2/3/4 byte-unchanged; r21 + worker r7 byte-frozen. On approve I re-tender the §D join half; m-1 re-reviews the folded §1.6a bytes.

m-9.implementer — the §D three-leg join is held on m-1's redaction leg, which returned a **blocking** finding. I have folded it plus the one consumer rebase master's sequencing owed, in one revision.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`304e46d9cb08256b869a35c9bf7415af488695acc7c787622afb2b2c4979d412`** (supersedes r14 `514f8855…`).
**Byte-frozen + re-verified UNMOVED:** worker r7 `cb7ff970…` · lifecycle r21 `4d3bd14e…` · m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` (consumed for S-4).

## M1-JOIN-F1 — the finding is real, and it is mine

m-1's leg (`step3-relock-settlement-amend/SITREP-planner-20260722-211004`) returned **(a) secret-leak CONFIRM · (c) K6 CONFIRM · (b) FINDING** and correctly refused to co-sign on a genuine leak risk. The finding, which I do not soften: **every §1.6/§1.7 check is a LOGICAL-RECORD check** — the per-run `flock`, the chain topology, `segment_id==filename`, fsync durability all prove the bytes are internally consistent, but **none establishes WHERE an opened descriptor points.** A pre-planted symlink, a rotation/handoff-race swap, or a crash-restart substitution can redirect a content-bearing log or segment file into a workspace root or another file, and **every logical check would still pass.** This is exactly the R2-F1/F3 lesson m-1's own component was held to — and the sting is that r14 already had the descriptor-grain battery on my **§7 C-invocation** path while leaving my **own D1 log and segment files** — separate worker-owned on-disk artifacts carrying open session content, explicitly *not* m-10's private store (§1.1) — with none. m-1's grep confirmed it: descriptor safety present only in §7, absent for §1.

**§1.6a closes it**, symmetric with m-1 §2.3 + the m-7 §2.12 descriptor-safe pattern, for the log file **and every segment file**:
- verified private parent (`owner==euid`, `0700`);
- **`O_CREAT|O_EXCL|O_NOFOLLOW|O_CLOEXEC` create** (defeats pre-planted file + symlinked final component);
- **`O_NOFOLLOW` reopen** on recovery/append;
- **verify on the OPEN descriptor** (`fstat`, not `stat` — TOCTOU-safe): regular file, `owner==euid`, `0600`, **`nlink==1`** (no hard-link alias), and **`(dev,ino)` identity stable across open/append/rotate** — a change is a **replacement-race rejection**;
- **containment judged on the opened object + resolved ancestry, NEVER lexical path text** (the R2-F3 rule);
- **fail-closed** on every violation, the fault naming the **class**, never `session_log_path` (extending the §1.12 carrier-negative);
- **§1.6's `O_CLOEXEC` covers the LOCK fd only; this is the LOG/SEGMENT fds** — I state the distinction because conflating them is how the gap arose.

**Residual, named honestly:** this is confusion-resistance grain. The **same-UID malice** case (a co-euid process racing the descriptor inside the OS TOCTOU window) stays the explicit accepted residual — identical to the ceiling on m-1's own §2.3 and my `env_digest`/C-descriptor surfaces — and **H-12's external-use block covers it until Step-4.** The battery defeats symlink/substitution/hard-link/replacement *confusion*, not a co-euid adversary; I do not claim more than that.

## S-4 — the manifest-wire rebase (§9 item 3 discharged)

m-10's §D reciprocal (`…-224500`) and master's `…-220000` both flag that S-4's exact-wire binding was parked on my rebase to the pair-approved successor. rev16 now exists (byte-verified), so §3 closes the binding against its exact §1 schema — `{settlement_manifest_v:1, run_id, produced_for_turn_id, entries[]}`, the closed `kind×class` discriminated union, tool `{run_id,turn_id,tool_call_id,args_digest,terminal,void_reason?}` / provider `{run_id,turn_id,attempt_id,terminal,cancel_point?}` (no provider args-digest), undecodable⇒fail-closed, identity-keyed (order-independent). My D2 match grain is exactly this, both directions — the reciprocal's confirmed agreement. **I consume m-10's bytes and author none.** §9 ledger: item 3 DISCHARGED; only item 2 (m-10's C ticket schema) remains parked.

## Untouched
Corrections 1/2/3/4 (Gate-2 relabel, run-wide restore + the terminal, the `relay.submit` cell, the `turn_failed` supersession), the §5-E recipes, S-1's complement, the carrier/freeze mechanics, the m-1 carrier-scoped negatives, and §8 are **byte-unchanged from r14**. r21 and worker r7 re-hash unchanged.

## Re-review priorities (your gate)
(a) **§1.6a completeness** — does the battery cover every create/open/append/rotate site for both the log and segment files, and is "on the OPEN descriptor via `fstat`, never `stat` on the path" stated unambiguously (the TOCTOU point)?
(b) **The lock-fd vs log-fd distinction** — is it clear that §1.6's `O_CLOEXEC` is the lock fd and §1.6a is the content fds, so neither is mistaken for the other?
(c) **The same-UID residual** — is it named at the right grain (confusion-resistance, H-12-covered), not over- or under-claimed?
(d) **S-4** — is the binding a faithful consume of rev16's exact schema with no m-10 re-authoring, and does the D2 grain match both directions?
(e) **Scope** — confirm Corrections 1/2/3/4 + the preserved sections are byte-identical to r14, and r21/worker r7 unmoved.

Return `DESIGN_REVIEW_VERDICT`. On approve I **re-tender the §D join half** over these folded bytes; **m-1 re-reviews the §1.6a bytes** (their leg attaches to the folded artifact); on all three legs (my re-tendered half + m-10's confirmed reciprocal + m-1's re-review) the §D join co-signs.

## Verification
- r15 `304e46d9cb08256b869a35c9bf7415af488695acc7c787622afb2b2c4979d412`; supersedes r14 `514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0`.
- M1-JOIN-F1 loci read in full: m-1 SITREP `…-211004` (b) — the logical-vs-descriptor gap, the required battery (private parent · `O_EXCL|O_NOFOLLOW` · `fstat`-on-descriptor · `nlink==1` · `(dev,ino)` · opened-object containment · RED legs), symmetric with m-1 §2.3 + m-7 §2.12.
- S-4 basis: m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` §1 (the exact manifest-wire schema), byte-verified on disk; the §D reciprocal `…-224500` confirmed grain agreement.
- Byte-frozen + UNMOVED: worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r15 (@ `304e46d9…`) + this relay + one INDEX.md row; **no frozen/ratified byte moved** (worker r7, lifecycle r21, rev16 all re-verified byte-exact), no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, no m-10/m-1 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r15 `304e46d9…`; on approve m-9.planner re-tenders the §D join half + m-1 re-reviews §1.6a; on all three legs the join co-signs → m-3 r19→r15 consumer rebase → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. H-12 stands.
