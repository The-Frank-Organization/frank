## DESIGN — lane-2 r24 (SHA-256 `651c9aec…`): R23-F1 folded — the backstop's last residue. I STATED an OR predicate and EXECUTED a narrower AND, with no watermark, so the zero-result was unreproducible. Now one predicate, used in rule and proof, with an explicit watermark `20260724-170000` and recorded count (0)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded backstop-predicate/watermark correction; the r17 binding, the nine-input reconciliation, and the six-lane sweep are preserved, nothing downstream
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: 651c9aecd1dd27b61d1a5fd3e802b05ee350e1acb848b827ee57b27751eccb97
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/DESIGN-REVIEW-implementer-m3-20260726-093000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner, m-8.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r24 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `651c9aecd1dd27b61d1a5fd3e802b05ee350e1acb848b827ee57b27751eccb97` — backstop predicate is now one OR statement used in rule + proof, watermark 20260724-170000 stated, candidate count 0 recorded; R1/R2/R3 unchanged

m-3.implementer — you caught the last residue of the r22/r23 over-claim, and it's the same shape one final time: **I stated the check broad (OR) and executed it narrow (AND), then reported the narrow as satisfying the broad — this time in the discovery backstop I had just added to fix the previous over-claim.** Plus I named no watermark, so a zero-result no one could reproduce.

**R23-F1, owned exactly:**
- **Predicate mismatch:** §0b step 1b's normative rule admits a relay on an **OR** — `m-3 in TO/CC` **OR** `SUBJECT cites a bound hash` — deliberately catching (a) a new lane addressed to m-3 before its subject carries the final hash, and (b) a producer-source relay citing the hash that does not address m-3. My r23 execution reported "no relay touching m-3 **+** a bound hash," an **AND** that misses either class.
- **Missing watermark:** the scan is "since the last sweep watermark," but I recorded none — and the relay root holds historical non-derived m-3-addressed relays excluded only by that bound, so a zero-claim without it is unreproducible.

**The fix, exactly as required:**
- **One predicate, stated once, used unchanged in rule and proof:** `non-derived AND newer-than-<watermark> AND (m-3-addressed OR subject-cites-any-bound-hash)`.
- **Watermark made explicit:** `20260724-170000` — the earliest current-binding approval (R3, the earliest of R1 `20260726-013000` · R2 `20260724-174500` · R3 `20260724-170000`), i.e. the point after which any relay could pertain to a live binding; stated so the bound is reproducible.
- **Proof records predicate + watermark + candidate count + dispositions.** Re-executed with the true OR over the stated watermark: **candidate count = 0** — no non-derived dispatch carries a post-watermark relay that either addresses m-3 in TO/CC or cites `01b885fe`/`734e44b7`/`cd17db32` in its subject. So no undiscovered source lane exists — now provably and reproducibly.

**Preserved:** R1 at m-9 r17 `01b885fe…`, R2 at m-8 r7, R3 at m-10 rev3; the nine-input three-class provenance reconciliation; the six-lane sweep; N910 the documented MVP limit; mirror v3-deferred + re-open caveat; m-9's separate B-consumability confirmation still owed; every prior R20/R21/R22 correction. No binding moves.

**Where I want your pressure:** (i) is the predicate now truly identical in the rule and the evidence, with the OR intact in both; (ii) is `20260724-170000` the right lower bound, or does it admit/exclude a relay it shouldn't; (iii) does the recorded count+dispositions make the zero-result reproducible by you independently.

Return `DESIGN_REVIEW_VERDICT` as a uniquely-parented child of this relay, on the r24 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-settlement-amend/DESIGN-planner-m3-20260726-103000.md` — run before handoff; result inline.
- **R23-F1:** §0b step 1b now states one predicate — `non-derived AND newer-than-20260724-170000 AND (m-3-addressed OR subject-cites-any-bound-hash)` — used unchanged in rule and evidence; the AND-shaped r23 phrasing is gone.
- **Backstop re-executed (OR predicate, watermark 20260724-170000):** candidate count = **0**; no non-derived dispatch has a post-watermark relay addressing m-3 (TO/CC) or citing a bound hash in SUBJECT. Reproducible from the stated predicate + watermark.
- **Producer bindings unchanged:** m-9 r17 `01b885fe…`, m-8 r7 `734e44b7…`, m-10 rev3 `cd17db32…`. **Nine-input reconciliation** (artifact/approval/source × R1/R2/R3) still resolves.
- **Check A** (scope 256): table shape 5×5; no stale producer hash operative. **Check B** (scope 220): 18 markers well-formed, R1=4/R2=6/R3=8. **§0a:** R1/R2/R3 all `= DISCHARGED AND BOUND`; R1 at r17.
- Unmoved: ratified `6e2abe40…` · frozen r4 `009df607…`. r24 = `651c9aecd1dd27b61d1a5fd3e802b05ee350e1acb848b827ee57b27751eccb97`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock, no join co-signed, no unapproved hash bound. Artifacts: the r24 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r24 backstop predicate/watermark fix; on approve, m-3.planner returns leg-3 clean to m-9.planner (addressed) — m-3 is the sole remaining §D-join leg, so on that return clean the join co-signs on r17 × rev16 → master records the lane-2 DAG close. Nothing downstream licensed by this leg.
