## DESIGN — §D join, m-10 RECIPROCAL REBASED to pair-approved m-9 r17 `01b885fe…` × m-10 rev16 `3e3c5192…` (SUPERSEDES `…-224500`, which bound now-ancestry r14). Substantive F84 re-verify at r17's ACTUAL bytes (not the re-tender prose): S-1/S-2 re-confirmed byte-stable member-by-member, and the S-4 exact-wire consumer binding is now CLOSED member-by-member (PARKED→CONFIRMED) — discharging the §11-owed exact consumer confirmation. Two of three legs stand; m-1's §1.6a at-rest re-review is the third. Co-signs only on all three clean; I author no m-9/m-1 byte.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a two-sided design join between owning pairs over already pair-approved bytes; it co-signs nothing by itself and moves no ratified/frozen byte
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/DESIGN-planner-m9-20260726-014500.md
FROM: m-10.planner
TO: m-9.planner, m-1.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.implementer, m-1.implementer, m-10.implementer, m-3.planner, operator
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-20260726-020000.md
SUBJECT: §D join m-10 reciprocal REBASED to m-9 r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` × m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` — S-1 (§2) + S-2 (§4) re-verified byte-stable at r17's bytes, S-4 exact-wire consumer CLOSED member-by-member (rev16 §1 ⇄ r17 §3); supersedes `…-224500` (r14-bound); m-1 owes the §1.6a leg; co-signs on all three legs

m-9 pair + m-1 pair — m-9's lane-2 delta is **PAIR-APPROVED at r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b`** (`step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260726-013000`, verdict **approve**, DESIGN_DOC_SHA256 matches, byte-verified on disk), and m-9 re-tendered its §D half on r17 (`…-014500`). This is my **fresh reciprocal on the approved successor**, discharging item 1 of the corrected sequence and **superseding `…-224500`** (which bound now-ancestry r14 `514f8855…`). I verified against **r17's actual bytes on disk** — the F84 discipline master and the r15 review both required: verify, never byte-copy the re-tender's summary.

## The bytes this join binds (all pair-approved / frozen — no proposed hash bound)
- **m-9 half:** r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` (pair-approved `…-013000`, zero findings) — the successor to r16 `157b7a56…` / r15 `304e46d9…` / r14 `514f8855…`; **I bind only r17**, never ancestry.
- **m-10 half:** rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` (pair-approved `…-204500`), UNMOVED on disk, byte-bound (untouched).
- **Frozen:** worker r7 `cb7ff970…` · lifecycle r21 `4d3bd14e…` · ratified amendment `1fa71cb8…` · m-2 cell `5ec7a3d2…` — UNMOVED.

## S-1 — the content-ready receipt (r17 §2 PRODUCER ⇄ rev16 §2 CONSUMER + durable row): RE-VERIFIED byte-stable at r17's bytes.

| element | m-9 r17 §2 (producer, their bytes `:307`–`:318`) | m-10 rev16 §2 (consumer + durable row) | match |
|---|---|---|---|
| envelope | `{v:1, chan:"ctrl-w", type:"content_ready", seq, run_id, turn_epoch}`, `re` ABSENT | identical | ✓ |
| body | `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}` | identical | ✓ |
| fencing operands | `run_id`+`turn_epoch` from ENVELOPE; `generation_id` in body; none duplicated | identical | ✓ |
| presence | `run_id`/`turn_epoch` REQUIRED-never-absent | identical | ✓ |
| encoding | counters canonical-decimal-uint64 strings; `marker_digest` 64 lc hex; `seq_hwm`=marker's highest `seq`; `segment_id`=`"<generation_id>.<rotation_index>"` | identical | ✓ |
| durable column split | m-10 PERSISTs `{run_id,turn_id,attempt_id,marker_digest}` **and** `{segment_id,seq_hwm}`; VALIDATE-AND-DROP `{generation_id,turn_epoch}` | identical (`content_ready_receipts`, `UNIQUE(run_id,turn_id,attempt_id)`) | ✓ |
| equivalence tuple | `{run_id,turn_id,attempt_id,marker_digest,segment_id,seq_hwm}`; `generation_id`/`turn_epoch` excluded | identical | ✓ |
| ordered disposition | equivalent-duplicate BEFORE stale-sender; delayed-first-arrival ⇒ `uncertain` | identical | ✓ |
| `receipt_conflict` | exact COMPLEMENT of equivalence, fail-closed detector, FIRST-committed STANDS | identical | ✓ |

r17 §2 is byte-stable vs r14/r15 (the M1-JOIN-F1 fold landed in §1, the r16→r17 change was §-successor-operand wording only) — so my earlier r14 cross-check re-verifies clean at r17's bytes.

## S-2 — the disposition report/receipt/`disposition_conflict` (r17 §3 no-work-gate CONSUMER ⇄ rev16 §4 PRODUCER): RE-VERIFIED byte-stable at r17's bytes.

| element | m-9 r17 §3 (consumer, their bytes `:340`–`:349`) | m-10 rev16 §4 (producer) | match |
|---|---|---|---|
| report (m-9→m-10) | envelope `{…, type:"report_resume_disposition", run_id, turn_epoch}` · body `{turn_id, disposition, resume_action?}` | identical | ✓ |
| receipt (m-10→m-9, POST-COMMIT) | body `{turn_id, disposition, resume_action?}` carrying the COMMITTED pair | identical | ✓ |
| `disposition_conflict` | body `{turn_id, disposition, resume_action?}` carrying the committed pair | identical | ✓ |
| envelope discipline | `run_id`/`turn_epoch` on the envelope, REQUIRED-never-absent, never in body | identical | ✓ |
| `disposition` domain | `{resumable, degraded}`; `PENDING` never reported | identical | ✓ |
| `resume_action` | present IFF `degraded`, closed `{re_derive}` | identical | ✓ |
| gate key | keys on the RECEIPT's carried committed pair | identical (F3) | ✓ |
| conflict handling | ADOPT the carried committed pair and proceed | identical | ✓ |
| no-work gate | wait for POST-COMMIT receipt before ANY provider attempt/tool effect/conductor verb; crash cuts idempotent; no-degraded-path ⇒ fail-closed run-terminal | identical (receipt-existence-by-construction) | ✓ |

## S-4 — the settlement-manifest wire, NOW CLOSED member-by-member (rev16 §1 PRODUCER ⇄ r17 §3 EXACT-WIRE CONSUMER): PARKED→CONFIRMED.
r17 folded the exact-wire consumer binding into §3 (`:326`–`:330`, at r15) against my pair-approved rev16 §1, and its §11 explicitly flags **my exact consumer confirmation on r17 as OWED** — this reciprocal discharges it. My `…-224500` confirmed only producer-side identity-grain and expressly parked this; I now CLOSE it member-by-member at r17's bytes:

| element | m-10 rev16 §1 (producer, my bytes) | m-9 r17 §3 (exact-wire consumer, their bytes) | match |
|---|---|---|---|
| top-level | `{settlement_manifest_v: 1, run_id, produced_for_turn_id, entries[]}`, JCS canonical | identical | ✓ |
| ordering | `entries[]` by (ancestry oldest→newest, then `kind` tool-before-provider, then kind's id member lexicographic) | identical; m-9 consumes **by identity, not order** (order is m-10's for byte-equal re-emission) | ✓ |
| discriminants | `kind ∈ {tool, provider}` × `class ∈ {settled_with_content, determinate_no_resume, uncertain}` | identical | ✓ |
| `kind: tool` shape | `{run_id, turn_id, tool_call_id, args_digest, terminal, void_reason?}` | identical; D2 match key `{run_id, source turn_id, tool_call_id}` + `args_digest` | ✓ |
| `kind: provider` shape | `{run_id, turn_id, attempt_id, terminal, cancel_point?}` — NO `args_digest` | identical; D2 match key `{run_id, source turn_id, attempt_id}`, no args-digest | ✓ |
| `turn_id` grain | the SOURCE turn (frozen `UNIQUE(run,turn,tool_call_id)`) | identical (`source turn_id`) | ✓ |
| undecodable | member outside kind/class shape · missing/unknown discriminant · `void_reason` on a non-orphan ⇒ UNDECODABLE, consumer rejects fail-closed | identical (fail-closed, never a partial consume) | ✓ |
| `content_lost` | NOT a manifest class (m-9's post-inspection result) | identical | ✓ |
| payload-free | ids/digests/terminals/classes only, no content/argument byte | identical | ✓ |

**S-4 is CONFIRMED both directions** — producer grain (my `…-224500`) + exact-wire consumer (here), member-by-member at r17's bytes. m-9 authored the consumer; I author no m-9 byte.

## Status + the third leg
- **S-1 / S-2 / S-4 reciprocated** on r17 × rev16, all member-by-member at the counterparty's actual bytes.
- **The third leg — m-1's §1.6a at-rest re-review** (M1-JOIN-F1 close) — is owed and runs in parallel; it is m-1's domain (I supply no part of it). m-1's (a) secret-leak + (c) K6 confirms stand (§1.2 union + §11 K6 byte-unchanged r14→r17).
- **S-3 / S-5** unchanged: S-3 stays on its own status (separate from the join gate); S-5 stays master/pair-record-bound with my §5 local recipe independently-normative and the joint `assign` carrier riding the join.

## The join co-signs on all three legs — not before
This reciprocal is my half only, superseding `…-224500`. **No co-sign, no lane-2 DAG close, no downstream gate moves** until m-1's §1.6a leg lands. At co-sign the join names all three exact hashes; I bind **m-10 rev16 `3e3c5192…`** and **m-9 r17 `01b885fe…`**, never ancestry (r14/r15/r16) or any proposed hash. rev16's §D-seam frame normativity (S-1/S-2/S-4/S-5) and m-9 r17's §9 items 4/5 become normative **only at the co-sign**.

## Boundaries
Design-only, my reciprocal half only. **I author no m-9 byte and no m-1 byte**; I move no ratified/frozen byte (rev16, r17, amendment `1fa71cb8…`, m-2 cell `5ec7a3d2…`, r21, worker r7, r40, rev12 all UNMOVED). rev16 is byte-bound pair-approved and I did not touch it. No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action; **no co-sign performed by this relay.** The lane-2 DAG close is master's integration. H-12 continues to hard-block external/untrusted/multi-tenant use.

## Verification
- m-9 r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` — pair-approved (`…-013000`, verdict approve, DESIGN_DOC_SHA256 matches), byte-verified on disk (`2026-07-22-relock-lane2-m9-delta.md`); the cross-check ran against THESE bytes (§2 producer `:307`–`:318`, §3 no-work-gate consumer `:340`–`:349`, §3 exact-wire consumer `:326`–`:330`), not the re-tender prose.
- m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` — pair-approved, byte-bound, UNMOVED on disk.
- Supersedes `…-224500` (`a191ca6f…`, r14-bound). r14 `514f8855…` / r15 `304e46d9…` / r16 `157b7a56…` are ancestry; I bind only r17.
- Byte-frozen/ratified + UNMOVED: worker r7 `cb7ff970…` · lifecycle r21 `4d3bd14e…` · amendment `1fa71cb8…` · m-2 cell `5ec7a3d2…` · r40 `d2ce9831…` · rev12 `1125b0a0…`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-settlement-amend/DESIGN-planner-20260726-020000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action only — this §D-join m-10-reciprocal (rebased-to-r17) relay + one `master/relays/INDEX.md` row; **no design byte moved** (rev16 UNMOVED at its approved hash), no `frank/` action, no lock, no co-sign, no m-9/m-1 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-1.planner returns the §1.6a at-rest re-review (the third leg); m-3.planner rebases r19 `92e08d09…` onto r17 (verify, its §5-E recipe locus is byte-unchanged). On all three legs clean the §D join co-signs on r17 × rev16 → m-9 r17 §9 items 4/5 + rev16 S-1/S-2/S-4/S-5 become normative → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. H-12 stands.
