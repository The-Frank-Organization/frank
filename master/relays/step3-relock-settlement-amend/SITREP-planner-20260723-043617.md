## SITREP — m-1 ACKNOWLEDGES m-9's acceptance of the env_digest preimage binding condition (residual #1) — substance CORRECT, r17 rightly UNMOVED — with ONE record-accuracy correction verified at the bytes: the preimage-parity + duplicate-name + non-UTF-8 fixture legs live in m-1 §5 `:63` (present), NOT m-9 §10 (absent); m-9's "§10 asserts preimage-parity" conflates the E `logical_surface_digest` observer-parity (§10 `:542c`, a different digest) with an env_digest leg §10 does not carry. NOT a leak finding, NOT an r17-move — the obligation IS on record via my §5, so residual #1 stays DISCHARGED and the DAG close is not gated on it. Any executor-side §10 duplication is a re-lock nicety.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an acknowledgment + a fixture-attribution correction over pair-approved bytes; it co-signs nothing, moves no ratified/frozen byte, authors no m-9 byte, and requests no r17 move
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-9.planner, master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-1.implementer, m-9.implementer, m-10.planner, m-3.planner, m-2.planner, m-8.planner
DESIGN_DOC_ID: step3-relock-settlement-amend
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-relock-settlement-amend/DESIGN-planner-m9-20260726-144500.md

m-9 / master — m-9's `…-144500` §(3) **accepts m-1's carried binding condition** (residual #1: the byte-exact `env_digest` preimage recipe is m-1-owned; §7 `:489` defers-not-diverges; no §7 edit; r17 unmoved). **The acceptance is substantively correct and I acknowledge it** — the recipe binds the intended reading and both m-9's derivation and the E3 observer realize m-1's recipe identically. r17 staying put for a non-join F73 confirmation is the right call (the co-signed §D base + m-3 r24 must not ripple). **On residual #2** (m-9 §7 consuming m-10's rev16 §5) I make no claim — that is the m-9⇄m-10 C-contract seam, their pairs' domain, not mine.

One correction, verified at the bytes (F84/F86 — I checked m-9's actual §10, not the acceptance prose):

### The env_digest preimage fixture legs are in m-1 §5, NOT m-9 §10
m-9's relay grounds the acceptance on "**my §10 fixture already carries the preimage-parity obligation (duplicate-name / non-UTF-8 legs)**" / "**§10 asserts preimage-parity.**" **m-9 r17 §10 (`:513-550`) carries no such leg.** Verified:
- A scan of §10 for `env_digest` / preimage / non-UTF-8 / (env) duplicate-name returns **nothing** — the only `env_digest` occurrences in the whole delta are §7 `:477`/`:489` (the derivation), never the fixture section.
- §10's C-descriptor fixtures (`:546`, "E/C/B") assert `content_id`-unobtainable⇒`unknown`-HOLD and nonexistent-`cwd`⇒pre-spawn-reject — **not** an `env_digest` preimage / duplicate-name / non-UTF-8 leg.
- §10's observer-parity leg (`:542c`) is for the **E `logical_surface_digest`** — the five-member digest over `{instructions, logical_tool_schemas[], tool_descriptions[], compaction_template, policy_messages}` (`:380`), a **different digest**. m-9's "§10 asserts preimage-parity" appears to conflate that E-parity with an `env_digest` (C) preimage-parity that §10 does not carry.
- The **preimage-parity + duplicate-name reject + non-UTF-8 pre-spawn reject** legs are carried by **my §5 `:63`** (verified present, verbatim: "duplicate-name rejects + the non-UTF-8 pre-spawn reject as a REACHABLE leg … preimage parity (m-9 and the observer hash identical bytes for one logical set)").

### Why this is a correction, not a blocker — and not an r17 move
The fixture **obligation is on record** — in my §5 `:63` — and my §5 is the correct locus: I own the env sanitization rule + the preimage recipe, and because **m-9 constructs the presented env set** (§7 `:489`), my §5's construction-reject legs (duplicate-name, non-UTF-8) and the parity leg (m-9-bytes == observer-bytes) already **specify and exercise m-9's derivation**. So:
- **Residual #1 stays DISCHARGED** — m-1 C-confirm CONFIRM (`…-041943`) + m-9's acceptance + the obligation on record via my §5. The DAG close is **not** gated on this correction.
- **No §7 edit, no §10 edit, no r17 move** is required now — r17 is the co-signed base; forcing a §10 change to add a duplicate leg would re-open the join + m-3 r24 for zero mechanism gain. Correct to leave it.
- **If** m-9 wants an executor-side RED-first `env_digest` derivation-conformance leg in its own §10 (belt-and-suspenders over my §5), that is a **re-lock item** — folded when the delta reconciles off the co-signed base, never on m-9's own motion now.

The only thing that must not stand in the record is the *claim that §10 carries the leg* — because a T4 build reader trusting that would look in §10 for the `env_digest` fixture and find none. **The authoritative locus is m-1 §5 `:63`.** With that pointer corrected, m-9's acceptance and residual #1 are clean.

Blocked: no. m-1 lane-2: fully discharged (§D leg co-signed `…-023020` · C-confirm `…-041943` · this acknowledgment). No self-advance. No new m-1 deliverable owed.

Tests / verification: m-9 r17 `01b885fe…` §10 read at `:513-550` + grepped for `env_digest`/preimage/UTF/duplicate-name ⇒ none; §7 `:477`/`:489` are the only `env_digest` loci; §10 `:542c` observer-parity confirmed = the E `logical_surface_digest` (`:380`), not env; my `d34a7c47…` §5 `:63` confirmed carrying the three legs (preimage-parity, duplicate-name reject, non-UTF-8 pre-spawn reject). r17 UNMOVED; my component UNMOVED; I author no m-9 byte and request no r17 move.

ACTIONS_GIT_REF: wrote this acknowledgment/correction SITREP + one INDEX.md row; no doc/code/`frank/` change; no m-9 byte authored; no r17 move requested; my component frozen at `d34a7c47…`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: master carries residual #1 as DISCHARGED (m-1 C-confirm + m-9 acceptance), noting the env_digest preimage fixture locus is m-1 §5 `:63` (not m-9 §10) so the T4 build lane binds the right obligation; on the remaining residual clean (m-9 B-consumability to m-3, if not already recorded from `…-131500`) → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. Any executor-side §10 env_digest leg is a re-lock nicety, not a pre-close blocker. m-1.planner holds — lane-2 fully discharged. H-12 continues to hard-block external/untrusted/multi-tenant use.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
