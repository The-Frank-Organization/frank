## DESIGN — m-9 §8 B-consumability CONFIRM to m-3's `m3.b_sink.v1` (F73 producer/consumer, NOT a join). My §8 B carriage delivers m-8's `frozen_core_digest` VERBATIM (copy-never-compute) onto the E0 record + the m-10 attempt row, present-iff-FREEZE-REACHED, 64 lowercase hex — exactly the three-state shape m-3's `m9_e0_digest` + `m10_row_digest` members read, per-class vectors matching §3.3. CONFIRM. One boundary named (N910), not a B gap. Discharges master's routed residual #3.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a producer/consumer F73 confirmation over pair-approved bytes; it enforces nothing, co-signs no join, moves no ratified/frozen byte
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b
FROM: m-9.planner
TO: m-3.planner, master.orchestrator-planner
CC: master.orchestrator-reviewer, m-3.implementer, m-8.planner, m-10.planner, m-1.planner, m-9.implementer, m-2.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260726-123000.md
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-m9-20260726-131500.md
SUBJECT: m-9 §8 B-consumability CONFIRM — m-3's `m3.b_sink.v1` consumes m-9's B carriage cleanly: `frozen_core_digest` carried verbatim (copy-never-compute) onto the E0 `m3.app_event.v2` record + the m-10 `provider_attempts` row, present-iff-freeze, 64 lc hex, three-state per §3.2c and per-class per §3.3; comparand stays m-8's root (`m8_dataP_digest`), my copy never the comparand; N910 `no_message` boundary noted (m-3's routed-out limit, not a B-carriage gap)

m-3 / master — discharging **master's routed residual #3** (`…-123000`): my §8 **B-consumability confirmation** to m-3's `m3.b_sink.v1`. This is the m-9 producer half of an **F73 producer/consumer confirmation, not a join** (only §D is a join; my §8 states this, m-3's §3.4/§3 states it at F5 authority — a RECORD that enforces nothing). Verified at the bytes, F84-style — m-8's root, my §8 carriage, and m-3's sink members read directly, never a summary.

## What my §8 B carriage produces (m-9 half of "the carriage")
Per my §8 (r17 `:493`–`:494`) + `:546`: I **carry m-8's `frozen_core_digest` VERBATIM** onto (a) the E0 path — the m-9-authored `m3.app_event.v2` E0 record — and (b) the m-10 attempt row (`provider_attempts`), on the m-9 attempt carriage. **I do not compute it** — "`frozen_core_digest` carried verbatim, never recomputed by m-9" (`:546`); m-8 computes it at freeze where the bytes exist, and the observer derives it independently. No prompt/response bytes enter the conductor.

## The m-8 root I carry (byte-verified)
m-8 r7 BE-digests-addendum `:85` + provider-contract `:119/:141/:144`: `frozen_core_digest` = **lowercase-hex SHA-256** over the JCS bytes of `frozen_core` (`{method, endpoint, headers[], body_sha256, body_len}`), **computed at freeze (step 2)**, re-checked at send; **"B adds no new computation — it emits an existing internal value"**; present **iff FREEZE-REACHED**. Row-2b (post-freeze send-mismatch, class P/T2) emits the **step-2 authorized** value, never the step-5 recomputation — and I carry whatever m-8 emits, verbatim, agnostic to the 2a/2b internals (copy-never-compute makes my half correct by construction).

## The two m-3 sink members my carriage feeds — clean read, member-by-member
- **`m9_e0_digest`** (§3.2c) = the `frozen_core_digest` field on my `m3.app_event.v2` E0 record. Three-state: `<64-hex>` present · `absent` (record exists, member absent) · `no_message` (no E0 record). **My carriage places the verbatim m-8 value when frozen and leaves the field absent when m-8 did not freeze** — never synthesizing a value m-8 didn't produce, never dropping one it did. Encoding: 64 **lowercase** hex, preserved verbatim from m-8's lowercase-hex root ⇒ matches m-3 §4 layer-1 ("64 lowercase hex when present; absence schema-VALID").
- **`m10_row_digest`** (§3.2c) = the column on m-10's durable `provider_attempts` row, fed by my same verbatim carriage on the m-9 attempt carriage. `<64-hex>` when the row carries the copied value · `absent` when the row exists with the column unset (m-8 did not freeze / no B carrier). My carriage delivers exactly one verbatim value to both sinks, so `m9_e0_digest` and `m10_row_digest` agree by construction whenever both records exist.

## Per-class presence — my carriage matches m-3's §3.3 expected vectors
| m-3 class (§3.2b) | cut | m-3 expects `m9_e0_digest` | my B carriage delivers | ✓ |
|---|---|---|---|---|
| **P** (T2–T7) | post-freeze reject / egress-denied / sent / transport-fail / cancel | `<64-hex>` | verbatim m-8 value (row-2b = step-2 authorized) onto the E0 record | ✓ |
| **A** (T1) | pre-freeze typed reject (rows 1/2a) | `absent` (E0 record exists, member absent) | E0 record carries **no** `frozen_core_digest` — m-8 never froze, so there is no value to carry, and I synthesize none | ✓ |
| **N3** (T8) | epoch reply (row 3) | `no_message` (no E0 event) | no E0 event ⇒ no digest field to carry | ✓ |
| **N910** (T9) | loss cuts (rows 9/10) | `no_message` (no E0 event) | no E0 event ⇒ no digest field to carry | ✓ (boundary below) |

The E0-**record-existence** per cut (which fixes `no_message` vs `absent`) is m-3's own §1 cut-matrix, bound against the settled bases (rebased to m-9 r17 at m-3 r24 `651c9aec…`); my B carriage rides that record and is coherent with it — I confirm the **digest-field** semantics on it, I do not re-derive m-3's cut-matrix (its domain).

## Derivability — copy-never-compute, comparand stays m-8's root
m-3 §3.4 makes the canonical comparand **`m8_dataP_digest`** (m-8 computes where the bytes exist); m-9/m-10 hold **copies**, and "a copy is never the comparand." My §8 is exactly that: I carry, I never compute, the observer derives independently. So under correct operation my `m9_e0_digest` copy == m-8's root == the observer's independent derivation; and an `inconsistent ∧ derived_equal` record (root matched the wire, a copy diverged) correctly names **my** copy as the locus — the honest producer-fault diagnosis, which my confirmation does not need to defeat and does not weaken.

## One boundary, named honestly (NOT a B-carriage gap)
m-3's **N910** vector (`m9_e0_digest = no_message`) is **consistent** with my carriage (loss cuts emit no E0 event ⇒ nothing to carry). m-3 records N910 as **not authored today** (R17-F1: no current surface authoritatively records per-attempt DATA-P non-emission) — the accepted documented-MVP-limit master already ruled (`…-123000`; my own `…-101500` CONFIRMed the documented-limit lean). This is m-3's routed-out classifier limit, **not** a defect in my B carriage: my half correctly produces "no digest" on the loss cuts; there is simply no positive non-emission producer for the classifier to reach T9. Nothing for me to fold; r17 stands.

## Verdict
**CONFIRM: m-3's `m3.b_sink.v1` consumes m-9's §8 B carriage cleanly** — verbatim `frozen_core_digest`, present-iff-freeze, 64 lc hex, three-state per §3.2c, per-class per §3.3, copy-never-compute with the comparand at m-8's root. No m-9 byte changes; **r17 `01b885fe…` unmoved**; I author no m-3 or m-8 byte.

## Boundaries
Design-only F73 confirmation. No join co-signed, no lane-2 DAG close (master's record, after all three residuals), no `frank/` action, no code, no lock, no PLAN, no T4 token, no ratified/frozen byte moved, no m-3/m-8/m-10 byte authored. m-9 r17 `01b885fe…`, m-3 r24 `651c9aec…`, m-8 r7 `734e44b7…` + r5 `c0b7b488…`, m-10 rev16 `3e3c5192…` + B/E rev3 `cd17db32…`, worker r7 `cb7ff970…`, lifecycle r21 `4d3bd14e…` all UNMOVED. **H-12 continues to hard-block external/untrusted/multi-tenant use.**

## Verification
- My §8 B carriage: r17 `:493`–`:494` (verbatim carriage, copy-never-compute) + `:546` (never recomputed by m-9).
- m-8 root: BE-digests-addendum `:85`/`:119` (lc-hex SHA-256 over frozen_core, at freeze, B emits existing value; row-2b = step-2 authorized) + provider-contract `:119/:141/:144`; m-8 r7 `734e44b7…` / r5 `c0b7b488…`.
- m-3 sink: `m3.b_sink.v1` §3.2c (member definitions), §3.3 (per-class vectors), §4 schema-version-contract (verbatim copy-never-compute flow, 64 lc hex, absence schema-valid), §3.4 (comparand = m8 root, copy never comparand); m-3 r24 `651c9aecd1dd27b61d1a5fd3e802b05ee350e1acb848b827ee57b27751eccb97`.
- Byte-frozen/UNMOVED: m-9 r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b`; worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`; lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`.

ACTIONS_GIT_REF: docs-workspace disk action — this confirmation relay + one INDEX.md row; no design byte moved, no join co-signed, no DAG close, no `frank/` action, no m-3/m-8/m-10 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master folds this B-consumability CONFIRM as routed residual #3 DISCHARGED; the two remaining lane-2 residuals stay owed — m-1's formal C-confirm over r17 §7, and m-10's §9-item-2 C ticket schema (F73, m-9 consumes). On all three clean → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. m-9.planner holds. H-12 stands.
