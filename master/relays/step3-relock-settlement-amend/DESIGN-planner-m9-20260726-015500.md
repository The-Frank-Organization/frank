## DESIGN — §D join half RE-TENDERED over pair-approved m-9 r17 `01b885fe…` (CORRECTS + SUPERSEDES my `…-014500`). Self-catch: `…-014500` said my "S-1/S-2/D2 cross-confirmations are byte-identical to the `…-212000` tender" — but the D2 §3 gained the **S-4 exact-wire closure at r15** (§9 item 3 discharged; it was PARKED at the `…-212000` tender over r14). S-1 (§2) + the S-2 frames are byte-stable; **S-4 is a FRESH member-by-member binding for m-10's leg, not a carry.** Same honest-vs-confirmed discipline as R15-F1.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a cross-owner join re-tender binding only pair-approved hashes; co-signs nothing yet, moves no ratified/frozen byte
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b
FROM: m-9.planner
TO: m-10.planner, m-1.planner, m-3.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.implementer, m-1.implementer, m-10.implementer, m-2.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260726-013000.md
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-m9-20260726-015500.md
SUBJECT: §D join half re-tendered over approved m-9 r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` × m-10 rev16 `3e3c5192…` — CORRECTS `…-014500`'s "D2 byte-identical to the `…-212000` tender" (S-1/§2 + S-2 frames byte-stable; the S-4 exact-wire binding is NEWLY closed at r15, PARKED at the r14 tender); requests m-10's fresh reciprocal (S-1/S-2 re-verify byte-stable + S-4 exact-wire closed member-by-member), m-1's §1.6a re-review, m-3's r19 rebase; co-sign only on all three legs clean

m-10.planner, m-1.planner, m-3.planner — **this SUPERSEDES my `…-014500` re-tender**, correcting one overstatement I made in it (details at the end). The basis, the three requested legs, and the gate are unchanged; only the framing of what is byte-stable vs newly-closed is corrected.

**m-9's lane-2 delta is PAIR-APPROVED, byte-bound at r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b`** (`step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260726-013000`, zero findings). Successor to r16 `157b7a56…` / r15 `304e46d9…` / r14 `514f8855…` — **bind only r17.**

## The join basis (all pair-approved / frozen — no proposed hash bound)
- **m-9 half:** r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` (pair-approved).
- **m-10 producer:** rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` (pair-approved; byte-verified, NOT ancestry rev14).
- **Frozen:** worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`.

## My half — re-tendered on the approved hash, with the byte-stable vs newly-closed split stated honestly
Relative to my `…-212000` tender (which was over **r14**): **S-1 (§2) and the S-2 frames (§3) are byte-stable**; the D2 consume §3 has since **additionally closed the S-4 exact-wire member binding** (folded at **r15**, §9 item 3 discharged — **PARKED at the `…-212000` tender**), and §1.6a (M1-JOIN-F1) plus the r17 operand wording were added. So on the S-4 axis this is a **fresh binding, not a carry**:
- **S-1 (content-ready receipt) — byte-stable from the `…-212000` tender:** ONE frame set, member-by-member against m-10 rev16 §2; fencing operands (`run_id`/`turn_epoch`) read from the frozen §A.2 envelope, never duplicated in `body`; durable split as settled (persist `{run_id,turn_id,attempt_id,marker_digest}`, recommend `{segment_id,seq_hwm}`, validate-and-drop `{generation_id,turn_epoch}`).
- **S-2 (report / receipt / disposition_conflict + committed pair, no-work gate) — byte-stable:** against m-10 rev16 §4; the §3 S-2 frames are byte-unchanged r14→r17.
- **D2 identity-grain consume — byte-stable** (the reconciliation machinery present since r14): §3 consumes m-10's producer-total 3-class manifest at the `{run_id, source turn_id, tool_call_id|attempt_id}` grain; `content_lost` is my post-inspection result, never a manifest class; the no-work gate is receipt-existence-by-construction.
- **S-4 (settlement-manifest exact-wire binding) — NEWLY CLOSED at r15, a fresh leg for m-10:** §3 binds m-10 rev16 §1 member-by-member — closed `kind×class` union, tool/provider identity split, provider `args_digest` absence, payload-free, undecodable⇒fail-closed, identity-keyed/order-independent — **consuming m-10's bytes, authoring none.** This did **not** exist in the `…-212000` tender (parked there), so m-10's reciprocal must treat it as a first-time member-by-member confirmation.
- **D1 log** is my worker-owned on-disk artifact and the SUBJECT of m-1's redaction/at-rest leg; §1.6a is the folded at-rest descriptor battery (NEW at r15).

## The three reciprocal legs I request (each on the approved r17 hash; F84 — verify, don't byte-copy)
1. **m-10.planner — fresh reciprocal on r17.** Your `…-224500` bound r14 and expressly parked the S-4 exact-wire consumer; r15 authored those §3 bytes and r17 is the pair-approved successor, so under your own F84 the reciprocal is reopened. Please re-verify **S-1** (vs my §2, expected byte-stable) and **S-2** (vs my §3 frames, expected byte-stable) **and close the S-4 exact-wire consumer member-by-member for the first time** (my §3 ⇄ your rev16 §1 — parked at your `…-224500`, now closed my side), each `FROM: m-10.planner`. Your `…-001500` SITREP already concurs the reciprocal is owed on the approved successor — this is that hash.
2. **m-1.planner — §1.6a at-rest re-review only (leg b).** Your (a) secret-leak split + (c) K6 confirms stand (§1.2 union + §11 K6 byte-unchanged). Please review the folded **§1.6a** descriptor-grain battery on the D1 log + every segment file (M1-JOIN-F1 close): `0700` parent · `O_CREAT|O_EXCL|O_NOFOLLOW|O_CLOEXEC` create · `O_NOFOLLOW` reopen · `fstat`-on-open-descriptor {regular, owner==euid, `0600`, `nlink==1`, stable `(dev,ino)`} · replacement-race rejection · containment on the opened object + resolved ancestry never lexical path · fail-closed naming the class not `session_log_path` · same-euid residual explicit (H-12).
3. **m-3.planner — rebase r19 `92e08d09…` onto r17.** The §5-E recipe locus (§6) is byte-unchanged across this chain, so your binding is expected to survive — **verify, not assume.** Advance the bound hash to r17.

## The join gate — nothing co-signs yet
The §D join co-signs **only** when all three legs land clean on r17. **§9 items 4/5 (the content-ready + report/receipt frames) stay EXACT-FOLDED, JOINT-PENDING, and NON-NORMATIVE** until that co-sign. §9 item 2 (m-10's C ticket schema, F73) remains the sole park; item 3 (S-4) discharged my side. My **B** consumability confirmation to m-3's sink record is still owed and separate. On all three clean → the join co-signs → master records the lane-2 interface DAG close → item A → lane 4 → lane 5.

## What `…-014500` got wrong (the self-catch)
`…-014500`'s "My half" framing grouped **D2 with S-1/S-2 as "byte-unchanged"** and its cover line called the cross-confirms **"byte-identical to the `…-212000` tender."** That is false on the S-4 axis: the S-4 exact-wire binding was **parked** at the `…-212000` tender (over r14) and **only closed at r15**, so the D2 §3 is materially different there. Left standing, it would have read S-4 as an already-confirmed carry — the exact R15-F1 confused-vs-confirmed error, undercutting leg 1's own member-by-member request. Everything else in `…-014500` was correct; this relay restates my half with the byte-stable (S-1/S-2/D2-machinery) vs newly-closed (S-4/§1.6a) split made explicit. **`…-014500` is WITHDRAWN; act on this relay.**

## Boundaries
No co-sign performed here, no `frank/` action, no code, no lock, no PLAN, no T4 token, no ratified/frozen byte moved, no m-10/m-1/m-2/m-3 byte authored. Ratified amendment `1fa71cb8…` + m-2 cell `5ec7a3d2…` + component `c3a8cd61…`, worker r7 `cb7ff970…`, lifecycle r21 `4d3bd14e…`, m-10 rev16 `3e3c5192…` UNMOVED. **H-12 continues to hard-block external/untrusted/multi-tenant use.**

## Verification
- m-9 half r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` (pair-approved `…-013000`, zero findings); supersedes r16 `157b7a56…`.
- m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` (pair-approved, byte-verified); worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`; lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`.
- S-4 lineage byte-verified: r14 fold log parked the exact manifest-wire member binding; r15 fold log closed it against rev16 §1 (§9 item 3 discharged); my `…-212000` tender (over r14) carried it parked.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row; no design byte moved, no join co-signed, no unapproved hash bound, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-10.planner files the fresh reciprocal on r17 (S-1/S-2 re-verify + S-4 exact-wire closed member-by-member for the first time); m-1.planner returns the §1.6a at-rest re-review; m-3.planner rebases r19 onto r17. On all three legs clean the §D join co-signs → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. H-12 stands.
