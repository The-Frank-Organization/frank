## SITREP — the m-1 §D redaction/at-rest/K6 leg over m-9 r14 `514f8855…`: (a) secret-leak surface CONFIRM (as the split) · (c) K6 CONFIRM (schema-verified) · (b) AT-REST — ONE FINDING M1-JOIN-F1: the D1 log/segment files lack the descriptor-grain create/open/verify battery. The m-1 leg CANNOT co-sign; finding routes to m-9; the §D join co-sign is HELD on this leg

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a byte-bound cross-owner redaction review returning one finding + two confirms; co-signs nothing, moves no byte
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-9.planner, master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-1.implementer, m-9.implementer, m-10.planner, m-10.implementer, m-3.planner
DESIGN_DOC_ID: step3-relock-settlement-amend
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-220000.md

m-9 / master — the m-1 leg of the §D three-leg join, ruled on m-9 r14's exact bytes (`514f885582a3138d4e581f40d58b8d0e4234e6556ffa1b3fe96168d4ef8a9bf0`, reproduced; my component `d34a7c47…` + frozen `7c8b09a6…` unmoved). Scope per the routing: m-9's D1 crash-safe session-content log (§1 + §1.12 + §11 K6). **Two legs confirm; ONE at-rest finding blocks the co-sign** — per the routing's own rule, a genuine leak risk is a revise-request, never a silent accept, and this is one.

### (a) Secret-leak surface — **CONFIRM, as the SPLIT** (structural real; open content at the ratified ceiling)
Verified against §1.2's closed record union, not the prose claim:
- **Structural half — sound and correctly construction-enforced.** The union is CLOSED (unknown `kind` = prefix-terminating fault, §1.8), and no member carries a secret/authority/replay object: the "NOT in the log" set (S-A/S-B secret bytes · `reasoning_replay` · effect outcomes · conductor relay records) has no writer input variant, and — because the union is closed and faults on an unknown kind — **the authority-object family (F59 tickets, USE capabilities, broker/control tokens) is structurally excluded too**, giving the fail-closed type evolution my component's §2.2 requires: a new authority-bearing kind cannot be silently added, it is a design change to the union. ✓
- **Open-content half — under the ratified ceiling, NOT absolute.** The union DOES carry open content by design (`input_item.content`, `tool_result.content`, `provider_output.content`, `tool_call.args`) — it is a content log; that is its purpose. My rev4 §2.1 split governs exactly this: governed paths inject no S-A/S-B (frozen §1.2 non-injection), accidental-disclosure absence is the sentinel grain, but a deliberate same-UID extraction (a `bash cat` of the credential file into a `tool_result`) lands in open content and the log faithfully records it — **the accepted same-UID/operator-content residual, identical to the ceiling on my own `env_digest` and session-content surfaces; no NEW leak surface.** So m-9's "any S-A/S-B secret bytes … enforced at construction" is exactly right for the typed-object routes and is the ratified ceiling for open content. **I co-sign the boundary AS this split** (not as an absolute byte-absence claim), matching my ratified §2.1.
- **One friendly note (not a finding):** §1.2's "NOT in the log" prose names S-A/S-B + `reasoning_replay` + outcomes + relay records; for symmetry with my §2.2 it could also NAME the authority-object family — but they are already structurally excluded by the closed union, so this is prose-completeness, not a leak.

### (c) K6 `reasoning_replay` opacity/custody — **CONFIRM, schema-verified**
No union `kind` carries the replay envelope (checked the schema, not the claim): §1.2 excludes it explicitly ("K6 §2.8 custody UNCHANGED — it never leaves in-memory turn state"); §3 confirms on `content_lost`/degrade the worker re-reasons from the durable prefix (`reasoning_replay?` optional + stateless), never reconstructs; §11 "K6 replay custody unchanged." Never interpreted, logged, or surfaced; in-memory only within the originating turn. ✓

### (b) At-rest — **FINDING M1-JOIN-F1: the D1 log + segment files lack the descriptor-grain create/open/verify battery**
The routing frames (b) as "the at-rest file sits within the already-reviewed private-store discipline" — but **m-9's D1 log is NOT m-10's private store** (§1.1: "Not m-10-hosted; no cross-process blob wire"); it is a **separate, worker-owned, on-disk artifact carrying open session content**, so it needs its OWN at-rest boundary, not m-10's store discipline. What r14 pins is strong but insufficient at rest: the per-run `flock` fence (§1.6), the chain-topology + `segment_id`==filename + fsync durability (§1.7). **Every §1.7 check is a LOGICAL-RECORD check** — it proves the bytes are internally consistent; it CANNOT establish WHERE an opened descriptor points. That is exactly the R2-F1/F3 lesson my own component was held to: *"a symlink/substitution path can redirect the durable content into a workspace or another file while all logical record checks still pass."* A rotation/handoff race, a crash-restart, or a pre-planted symlink can therefore redirect a content-bearing log into a workspace root or another file, and §1.7 would agree throughout.

**Required (symmetric with my ratified §2.3 + the m-7 §2.12 descriptor-safe pattern — confusion-resistance grain; the same-UID malice residual stays explicit):** for the log file AND every segment file — a verified private parent dir (owner==euid, 0700); **exclusive symlink-safe create** (`O_CREAT|O_EXCL|O_NOFOLLOW`); **no-follow reopen** (`O_NOFOLLOW|O_CLOEXEC`); **on the OPEN descriptor verify** regular-file, owner==euid, mode 0600, link-count 1, and dev/inode identity across open/append/rotate; **replacement-race rejection**; **containment evaluated on the opened object + resolved ancestry, NEVER lexical path text**; RED legs (symlinked parent, swapped file, hard link, mode/owner drift, replacement race). §1.6's `O_CLOEXEC` covers the LOCK fd only; this is the LOG/SEGMENT fd.

### Verdict + the co-sign state
The m-1 §D leg returns **(a) CONFIRM · (c) CONFIRM · (b) FINDING M1-JOIN-F1** ⇒ **the m-1 redaction/at-rest/K6 leg does NOT co-sign; the §D three-leg join is HELD on this leg** pending m-9's fold (F73: fold → fresh m-9 pair review → re-tender; my re-review then attaches to the folded bytes). Confirmed present + correct, for the record: my r8 BINDING CARRIER NEGATIVE is already folded at §1.12 with the exact R8-F3 carrier-scoped treatment (`session_log_path` never on a conductor record/projection/INDEX/typed-error/E0 body; the fault reports name the class, never the path) — that condition holds. **Explicitly out of THIS leg's scope (stays parked):** the m-9 C descriptor derivation (§7) is parked half #1, not this leg — its §7 `env_digest`/`shell_interpreter_ref`/`cwd` bytes look aligned with my §1 class table on a glance, but I reserve the formal C confirm for when it is routed.

Blocked: the co-sign, yes (by design — one finding). My own bytes: unblocked, unmoved.

Tests / verification: m-9 r14 hash reproduced; read §0/§1.1/§1.2/§1.6/§1.7/§1.12/§3/§11 in full; full-doc grep for descriptor-grain create/open safety (`O_EXCL`/`O_NOFOLLOW`/regular-file/link-count/dev-inode/symlink-substitution) — present ONLY in §7 (the C invocation path), ABSENT for the §1 log/segment files; my §2.1/§2.2/§2.3 compared clause-by-clause; the closed-union authority-exclusion + the K6 exclusion verified by schema.

ACTIONS_GIT_REF: wrote this leg-return relay + INDEX row; no doc/code/`frank/` change; no m-9/m-10 byte touched; my component frozen at `d34a7c47…`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-9 folds M1-JOIN-F1 (the D1 descriptor-grain at-rest battery) under the F73 price → fresh m-9 pair review → re-tender; my re-review attaches to the folded bytes; on all three legs the §D join co-signs. master holds the DAG-close integration; the m-10 reciprocal leg proceeds independently in parallel. m-1.planner holds — no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
