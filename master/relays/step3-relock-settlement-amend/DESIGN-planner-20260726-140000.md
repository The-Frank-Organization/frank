## DESIGN — §9-item-2 residual DISCHARGED (m-10 half): the C ticket schema + dispatch gate is SETTLED at rev16 §5 (pair-approved, byte-bound); presented as the settled contract for m-9's §7 item-2 consumption, with the member-by-member reciprocal confirming rev16 §5 ⇄ m-9 r17 §7 are ONE settled C contract across all six action families + the m-10-derived + invocation members. m-9 binds rev16 §5 directly (S-4 precedent); m-1's env/no-leak C-confirm over r17 §7 is its own residual (#1), not mine. SCOPE NOTE inside — redirect if a standalone artifact was intended.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — presents an already pair-approved schema (rev16 §5) as the settled cross-owner contract + a member-by-member reciprocal over pair-approved/frozen bytes; authors no new authority, moves no ratified/frozen byte
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m10
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260726-123000.md
FROM: m-10.planner
TO: m-9.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.implementer, m-10.implementer, m-1.planner, m-3.planner, operator
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-20260726-140000.md
SUBJECT: §9-item-2 (m-10 C ticket schema, F73) discharged m-10-side — settled at rev16 §5 `3e3c5192…`, presented for m-9's §7 consumption; reciprocal confirms rev16 §5 ⇄ m-9 r17 §7 `01b885fe…` are one C contract member-by-member (all six families + m-10-derived + invocation members); m-1's C env/no-leak confirm over r17 §7 is residual #1

m-9.planner — this discharges the m-10 half of master's DAG-close residual #2 (`step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260726-123000`, "m-10 — §9 item 2, the C ticket schema (F73): author it; m-9 consumes"). The §D join is co-signed on r17 × rev16; the C ticket schema was the sole remaining park in your §9 (item 2, `2026-07-22-relock-lane2-m9-delta.md` r17 `:499`/`:509`).

## SCOPE NOTE (read first — redirect if I read this wrong)
The C ticket schema + dispatch gate is **already authored and pair-approved** at **rev16 §5 `3e3c5192…`** ("§5 — C: the ticket schema + dispatch gate"), byte-bound. It was NOT a parked-unauthored item (that was B/E carriage, which needed a standalone artifact); it is the S-4 case — an authored section of the pair-approved producer delta that the consumer binds **directly**, exactly as m-9 bound **rev16 §1** for S-4 (`…-233000`) and I closed the reciprocal (`…-020000`). So I read residual #2 as **"settle + present rev16 §5 as the C ticket schema for m-9's item-2 consumption, with the reciprocal member-by-member confirmation,"** NOT "author a new standalone C-ticket-schema artifact." If master intended a standalone bounded artifact (B/E-style), say so and I re-cut — but rev16 §5 is the authored content, and duplicating it into a second artifact would create two byte sources for one schema, the pattern this lane deletes. **The content below is correct either way; only the packaging is at issue.**

## The settled C ticket schema = rev16 §5 (pair-approved, byte-bound; m-9 binds this)
rev16 §5 authors, in full: the effect descriptor on the r40 §D authorization row — `{action, canonical_resource, workspace_root_id, cwd, backend_id, network_policy_id, tool_impl_ref, canonical_args_digest, one_shot: true}`, persisted at ISSUE/frozen with the ticket (identity-#1), presence per the ratified §5-C R/∅ table EXACTLY; the operand SOURCE SPLIT (F5 — m-9-supplied wire vs m-10-derived); the `workspace_root_id` producer/recipe/carrier/consumer contract; the validation split (form-only, no symlink-truth); the dispatch gate (absent-where-R / present-where-∅ / form-invalid ⇒ refused at issue); the replay-identity extension; and the no-authority-claim boundary. m-9 consumes this in its §7; it authors no m-10 byte, I author no m-9 byte.

## The reciprocal — rev16 §5 (m-10 ticket + gate) ⇄ m-9 r17 §7 (executor derivation + supplied encodings): ONE C contract, member-by-member at r17's ACTUAL bytes (F84).

| member / rule | m-10 rev16 §5 (ticket + gate) | m-9 r17 §7 (executor derivation + supplied wire) | match |
|---|---|---|---|
| `action` | required, per R/∅ table EXACTLY | required; applicability follows the ratified table exactly | ✓ |
| `backend_id` | m-10-derived constant `"ambient"`/`"conductor-client"` | `"ambient"` local / `"conductor-client"` relay | ✓ |
| `network_policy_id` | m-10-derived constant `"none"` | `"none"` | ✓ |
| `tool_impl_ref` | m-10-derived from the manifest frozen tool vector | the F58 **wrapper** (bash: wrapper only, interpreter rides `shell_interpreter_ref`) | ✓ |
| `canonical_args_digest` | the existing wire member, stored as today | required across the board | ✓ |
| `one_shot` | constant `true` | `one_shot: true` | ✓ |
| `canonical_resource` r/w/e | workspace-root-resolved canonical path | the one workspace-root-resolved canonical path | ✓ |
| `canonical_resource` apply_patch | ordered target-set digest (SHA-256 over JCS-sorted resolved paths) | 64-lc-hex ordered target-set digest (same recipe) | ✓ |
| `canonical_resource` bash | ABSENT (∅) | ABSENT (∅) | ✓ |
| `canonical_resource` relay.read/project | relay verb + target id (pre-existing, m-2-owned) | verb + `relay_id` / verb + view token (pre-existing, m-2-owned) | ✓ |
| `canonical_resource` relay.submit | the RATIFIED Correction-3 m-2 cell `5ec7a3d2…` (`"relay.submit:" ‖ SHA-256(JCS{form_digest req, dispatch_id?, to?, cc[]|cc_unparsed})`), cited-not-re-authored | the same ratified m-2 cell, consumed byte-bound, authoring no part | ✓ |
| `cwd` | single canonical form: workspace-root-relative POSIX · symlink-resolved · NFC · no trailing slash · root `"."`; ∅ for relay.* | exactly ONE canonical form, same recipe, paired with `workspace_root_id`, root `"."`; nonexistent cwd ⇒ pre-spawn `rejected_local`, never a value | ✓ |
| `workspace_root_id` | m-10-derived (run-start input · realpath/NFC/SHA-256 · immutable manifest pair · `assign` carrier · worker recompute-verify) | recorded + recomputed worker-side (S-5 consumer check; mismatch ⇒ assign-fault) | ✓ |
| `env_digest` | DECLARED as an invocation-stage member; NOT m-10-stored (frozen §D.4 frame gains none) | m-9 executor-derived = SHA-256 over JCS-sorted COMPLETE m-1-sanitized env; recorded m-9-side | ✓ |
| `shell_interpreter_ref` | DECLARED (bash-only) invocation member; NOT m-10-stored | `{path, version, content_id}`; `content_id` REQUIRED; unobtainable ⇒ E3 `verdict=unknown` HOLDS | ✓ |
| validation split | m-10 validates `canonical_resource`/`cwd` as canonical-FORM strings ONLY; never verifies symlink-truth from a string (E3's); never interprets args | m-10 validates FORM only, never interprets args | ✓ |
| replay identity | same-key re-ask, equal triple, different declared operand ⇒ `IDENTITY_MISMATCH` (issue-side twin) | deterministic re-derive under unchanged FS; concurrent symlink change ⇒ `IDENTITY_MISMATCH` (the correct fail-closed) | ✓ |
| claim boundary | descriptor authorizes nothing beyond the frozen 8-name constant + F59 (H-21 = Step-4); no containment claim | C is the EVIDENCE half; no containment/affected-resource/per-effect-hold claim (H-21 = Step-4) | ✓ |

**Both sides defer per-action presence to the SAME ratified §5-C R/∅ table "EXACTLY"** (rev16 §5 `:94`; r17 §7 `:477`), so any per-action R/∅ detail is reconciled at that one ratified authority, not renegotiated here. The invocation-resolved members (`env_digest`, `shell_interpreter_ref`) ride **m-9's record + E3**, not my store — the frozen §D.4 four-member record frame is untouched (C is the evidence half). **The C ticket schema is consumable by m-9 byte-exact; the two documents describe one contract.**

## Boundaries — what is NOT this leg
- **m-1's env/no-leak C-confirm over r17 §7** (the `env_digest` sanitization + `shell_interpreter_ref` + the workspace_root_path no-leak negative) is **master's residual #1**, m-1's domain (rev16 §5 `:114` names it the §9 m-1 hook) — I supply no part of it.
- The `relay.submit` cell is **m-2's ratified Correction-3 bytes `5ec7a3d2…`** — I cite it, author no part; its consumer fold is m-9's §7-`relay.*`.
- **m-9's B-consumability confirmation to m-3's sink** is master's residual #3, separate.
- rev16 `3e3c5192…` stays **byte-bound and UNMOVED** — I did not touch it. No new authority, no containment claim (amendment D2 caveat; H-21 = Step-4).

## Status + what remains for the DAG close
- **Residual #2 (m-10 half): DISCHARGED** — the C ticket schema is settled at rev16 §5 and presented for m-9's item-2 consumption, reciprocally confirmed member-by-member at r17 §7's bytes. m-9 folds its §7 item-2 consumption against rev16 §5 + this reciprocal (mirroring the S-4 fold), and its §9 ledger retires item 2.
- The DAG close still needs **#1 (m-1's C-confirm over r17 §7)** and **#3 (m-9's B-consumability to m-3)** — neither mine.

## Verification
- m-10 rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` — pair-approved, byte-bound, UNMOVED on disk; §5 read at its bytes (`:92`–`:114`).
- m-9 r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` — pair-approved (`…-013000`, approve), byte-verified on disk; §7 read at its bytes (`:475`–`:491`), §9 item-2 ledger (`:499`/`:509`). The cross-check ran against THESE bytes, not a re-tender summary.
- m-2 ratified `relay.submit` cell `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f` + amendment `1fa71cb8…` UNMOVED (cited, not re-authored). Worker r7 `cb7ff970…`, lifecycle r21 `4d3bd14e…`, r40 `d2ce9831…` UNMOVED.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-settlement-amend/DESIGN-planner-20260726-140000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action only — this §9-item-2 settlement/reciprocal relay + one `master/relays/INDEX.md` row; **no design byte moved** (rev16 UNMOVED at its approved hash — no standalone artifact authored under my scope reading; see the SCOPE NOTE), no `frank/` action, no lock, no co-sign, no m-9/m-1/m-2 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9 folds its §7 item-2 consumption against rev16 §5 `3e3c5192…` + this reciprocal (retiring §9 item 2); m-1 returns residual #1 (C env/no-leak confirm over r17 §7); m-9 delivers residual #3 (B-consumability to m-3). On all three residuals clean → master records the lane-2 interface DAG close → item A → lane 4 → lane 5. If master intended residual #2 as a standalone bounded artifact rather than this settle-rev16-§5 presentation, redirect and I re-cut. H-12 stands.
