## DESIGN — lane-2 delta r13 @ `675fa1fa…`: THE final batched revision, held for exactly this gate, folding all four ratified §D-settlement corrections. This is the m-9 row of the propagation matrix. Fresh full-byte pair review requested; §D join co-sign follows both owner successors.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-settlement-amend
PARENT_DISPATCH_ID: step3-relock-settlement-amend
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — folds owner/consumer sides of an already operator-ratified amendment; no new operator-gated claim is authored, and the ratified artifacts stay byte-exact
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 675fa1fa30ba03e25453f64ff7092cbbbf5b79712555cb5c546216a25f36bfbf
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-2.planner, m-3.planner, m-3.implementer, m-1.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000.md
RELAY_PATH: master/relays/step3-relock-settlement-amend/DESIGN-planner-20260725-181500.md
SUBJECT: r13 @ `675fa1fa30ba03e25453f64ff7092cbbbf5b79712555cb5c546216a25f36bfbf` — Correction 1 (Gate-2 relabel consumer-side, §4), Correction 2 (run-wide restore now ratified + the `parked_unknown_capacity_exceeded` terminal consumer posture, §4), Correction 3 (m-2's ratified `relay.submit` cell consumed byte-bound, §7 — `relay.*` no longer held), Correction 4 (r7's "descriptive/owner-clarification" framing REPLACED by the amendment's explicit supersession, §6). §2.6 is folded here, no longer a held item; only the §D join co-sign remains. r21 + worker r7 byte-frozen.

m-9.implementer — the amendment is operator-ratified, the propagation matrix is open, and this is **the final batched m-9 revision that has been held since r8 for exactly this gate.** One successor, all four corrections, per master's matrix row.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`675fa1fa30ba03e25453f64ff7092cbbbf5b79712555cb5c546216a25f36bfbf`** (supersedes r12 `04422965…`).
**Consumed byte-bound (verified on disk this session):** ratified §D-settlement amendment **`1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b`** + the bound m-2 `relay.submit` cell **`5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f`**.
**Byte-frozen + re-verified UNMOVED:** worker r7 **`cb7ff970…`** · lifecycle r21 **`4d3bd14e…`**.

## The four folds, each the side master's matrix assigned me

1. **Correction 1 — Gate-2 relabel, CONSUMER-side (§4).** Gate 1 (`turn_open`) delivers the disclosure guarantee; Gate 2 (`attempt_open_ok`) is a **fail-closed validator + drift-detector over MVP-unreachable states.** I record it at my consumer seam because my frozen r21 §2.6 two-gate consumption reads against this claim — but **the comparator bytes stay byte-frozen**; only the operator-visible claim is corrected. This is the ratified supersession of the D-4 two-gate claim, not a re-design.
2. **Correction 2 — run-wide restore + the new terminal (§4).** The run-wide D-4 disclosure on both gates is now the **ratified** mechanism (was "master's ruling (3)"). The new operator-visible terminal **`parked_unknown_capacity_exceeded`** is recorded with its m-9 consumer posture: a **run-terminal, no resume branch, no first-action-table entry** — the same zero-`stop_reason` finding, now against a named terminal. I author no part of it; I consume it as terminal through the ordinary run-terminal path.
3. **Correction 3 — the `relay.submit` cell, CONSUMED byte-bound (§7). `relay.*` is no longer held.** I consume m-2's ratified cell verbatim: `canonical_resource(relay.submit)` = `"relay.submit:" || SHA-256(JCS({form_digest REQUIRED, dispatch_id?, to?, cc[]|cc_unparsed}))`, total by the R-1 `form_digest` requirement, with the `cc`/`cc_unparsed` mutual-exclusion and absence-by-omission rules as m-2 authored them. **I author no part of the cell** — my derivation is just "compute the digest from the presented submission members, prefix the verb."
4. **Correction 4 — the `turn_failed` supersession, REFRAMED (§6).** This is the one I most want you to check, because it **corrects my own earlier framing.** At r7 I carried r21 `:115`'s "after the bounded attempt(s)" as an *owner clarification* — asserting the clause was "descriptive, not a precondition." **The VP corrected that (SETTLE-VP-R1-F3)** and the amendment governs it now: it does **not** claim r21 already meant this; it **explicitly supersedes** the after-attempt restriction **for exactly the one pre-attempt assembly-refusal branch**, effective on ratification, **non-generalizing** (any other zero-attempt `turn_failed` use stays a fresh amendment), r21 byte-frozen. §6 now binds that verbatim and demotes my r7 rationale (machinery-class fit, `turn_denied` wrong class, no other member fits) to *design rationale* — the authority is the ratified supersession, not a descriptive-clause claim.

## Scope + what stays held
§10 gains three legs (the relay cell; the Correction-2 terminal consumer posture; the zero-attempt `turn_failed` supersession, asserted **non-generalizing**). **§9 items 4/5 stay EXACT-FOLDED and JOINT-PENDING** — the amendment ratifies the *corrections*, not the two-sided §D join, which co-signs only after **both** owner successors are pair-approved; I **rebase to m-10's pair-approved successor hash before the join, never to its ancestry rev14.** **§2.6 is folded here and is no longer a held item** — the only remaining held m-9 work is the §D join co-sign itself. Everything else from r12 is byte-unchanged (the §5-E recipes, S-1's complement, §9's ledger structure, the carrier/freeze mechanics, the m-1 negatives, §8). **r21 and worker r7 stay byte-frozen and unedited.**

## Re-review priorities (your gate)
(a) **Correction 4's reframing** — is the "explicit supersession, not descriptive" framing bound faithfully to the amendment, and is the non-generalization stated strongly enough that no future zero-attempt `turn_failed` use can claim cover?
(b) **Correction 3** — is the `relay.submit` cell consumed exactly as m-2 authored it, with no m-9 re-authoring, and is the totality-by-`form_digest` claim m-2's, not mine?
(c) **Correction 1 vs the frozen comparator** — does the relabel touch only the claim and leave r21 §2.6's bytes and my consumption of them intact?
(d) **Correction 2 terminal** — is "run-terminal, no resume branch, no first-action entry" correct, and does it add no m-9 obligation?
(e) **Scope** — confirm §5-E recipes, S-1, §8, the m-1 negatives, and the carrier/freeze are byte-unchanged from r12, and that r21/worker r7 are unmoved.

Return `DESIGN_REVIEW_VERDICT`. On approve I return the hash to master; the **§D two-sided join (m-9 ⇄ m-10 + m-1 redaction) co-signs once m-10's successor is also pair-approved**, then the lane-2 interface DAG closes.

## Verification
- r13 `675fa1fa30ba03e25453f64ff7092cbbbf5b79712555cb5c546216a25f36bfbf`; supersedes r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`.
- Ratified consumed: amendment `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b` (Corrections 1–4 + the propagation matrix m-9 row) · m-2 `relay.submit` cell `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f` (§2.1 recipe read in full).
- Byte-frozen + UNMOVED: worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`. Correction-4 amendment text read at `STEP-3-STAGE6-SETTLEMENT-AMENDMENT.md` §"Correction 4" (the VP-corrected "explicit supersession" framing).
- m-10's successor is a **separate** matrix row (its live rev14 `b96a1511…` is ancestry, not a durable fold); I bind its **pair-approved** successor hash at the join, not before.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r13 (@ `675fa1fa…`) + this relay + one INDEX.md row; **no frozen/ratified byte moved** (worker r7, lifecycle r21, the amendment, and the m-2 cell all re-verified byte-exact), no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, no m-2/m-10 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r13 `675fa1fa…`; on approve m-9.planner returns the hash to master, and once m-10's successor is also pair-approved the §D two-sided join (+ m-1 redaction) co-signs, closing the lane-2 interface DAG → item A (bundle) → lane 4 (re-lock) → lane 5 (T4). H-12 stands.
