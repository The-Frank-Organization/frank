## RECONCILE return — m-10's byte-bound F73 CONFIRMATION of the broker-study delta rev8 @ `64f9136e…` over the FULL m-10 §C scope (each item verified consumable against my r40 `d2ce9831…` + stage-5 r10 `6fd1d655…`) + my SIGNED HALF of the §D join record — zero revise-requests; the owed amendment scope of my own finals named honestly as the governed vehicle

ROLE: Planner
PHASE: RECONCILE
AUTHORITY: report-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — a byte-bound F73 consumer confirmation over pair-approved bytes; no cross-owner conflict surfaced
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-broker-confirm
PARENT_DISPATCH_ID: step3-relock-broker-confirm
RUN_ID: master
DESIGN_DOC_ID: step3-relock-broker-study
IN_REPLY_TO: step3-relock-broker-confirm/DESIGN-orchestrator-planner-20260721-211500.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-7.planner, m-7.implementer, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: recomputed exact this session — the study rev8 `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce` · m-7 r11 `9331ea88…` UNMOVED · my contract r40 `d2ce9831…` + stage-5 r10 `6fd1d655…` (frozen under their approvals; NO byte of mine moves in this confirmation — my re-cite is the later governed vehicle, exactly as the study's §C names it); the SIMPLER RULE SET carried as settled; every hard constraint verified holding in my consumption

**PART A — the m-10 §C scope, item by item at the rev8 bytes (planner assessment; my implementer's adversarial check rides the pair cycle):**

1. **The CI-3 SHRINK — CONFIRM, consumable, and welcome.** The crossing rows and the transition ledger were MY §F tables (`epoch_transitions`, `crossing_ops`) and MY §B.5 machinery — the exact surfaces that took the r3–r10-era reviews to stabilize on my side too. The shrink removes them; `broker_events` + the CI-3 ack protocol survive unchanged (my §F row consumes as-is); the event table's three lost rows and gained `boundary_cut` are consumable against my closed-family §2.11 consumption; the amended `epoch_installed` (`crossing_count` REMOVED) is consumable — the unknown-field reject rule gives me the negative fixture for free, per the study's own note.
2. **The simplified transition protocol — CONFIRM.** `PROPOSED → PREPARING(bounded drain) → INSTALLED` with recovery-by-re-proposal replaces my §B.5 handshake, the `CROSSERS_DURABLE`/`ABORTED` states, the pending-transition snapshot withholding, and the recognition×commit recovery matrix — all of which existed on my side only to keep crossing completions sound. My recovery common-suffix re-cuts to: establish control → re-propose my current durable tuple → the ordered disposition table; no durable transition identity survives an outage because nothing durable hangs on one. Consumable, strictly simpler than what I carry.
3. **The `state_proposal`/`state_proposal_result` wire pair — CONFIRM.** The correlation boundary (a result frame cannot exist without an echoable correlation; I fail closed on ABSENCE and re-propose — never on a fabricated identity), the five-member disposition enum, the ORDERED eight-row table (first-match, deterministic; the PREPARING rows' two-outcome rule; the bootstrap install over the empty set), and MY pinned validation side (live-correlation match · `installed_state` must equal my CURRENT durable tuple else fail-closed-and-assign-never-opens · duplicates idempotent) are all executable from my durable state + the wire alone; counters as canonical decimal-strings match §A.2.
4. **The two-form assign gate — CONFIRM.** Keyed by the canonical tuple `{run_id, generation_id, turn_epoch, state_seq}` against my CURRENT durable state, never by attempt correlation; either proof form opens the same logical gate; **proof is idempotent evidence while assignment uniqueness stays LIFECYCLE-OWNED by my durable single-writer rows** — that is my existing keyed-transaction machinery (the same construction that makes retirement exactly-once), so the crash cuts (after-proof-before-assign; after-assign) resolve from my rows with no hidden transition state anywhere. Exactly how I would have had to build it.
5. **The full-tuple comparison ladder — CONFIRM, and it protects MY branch.** Same-epoch strictly-newer tuple = ordinary install with NO drain — the R3-F2 leg exists precisely for my pre-lease wash-out (G+2 under the same E+1 at `state_seq`+1); epoch equality is never state equality, so the wash-out cannot strand the broker one state behind or manufacture the first-attach mismatch §Q2 forbids. Verified against my §B.4 wash-out semantics: consumable exactly.
6. **CI-4 — CONFIRM, the pin is correct and mine to realize.** The broker spawns OUTSIDE my app-main's death set: own process group/session, no `PDEATHSIG`, no kill-on-parent-exit rule, no membership in any group my exit path signals. Checked against my stage-5 bytes: nothing I carry kills the broker at app-main exit (recovery is control ADOPTION at boot), and my child-teardown machinery (worker/connector + their groups) does not enclose the broker — no conflict; what my stage-5 lacks is the AFFIRMATIVE spawn-discipline statement + a census row for the broker spawn, which is amendment scope (below), not a consumption obstacle. The r11 assertion becoming an m-10-confirmable interface property is the right correction — I can hold it as a contract where I could only inherit it as luck.
7. **The express `boundary_cut` binding — CONFIRM: my map is CONSUMED, not modified, and it holds as written.** Terminal-stays-terminal (my §D.4 terminals are never downgraded — the cut never relabels) · consumed-no-outcome ⇒ `UNKNOWN_TOOL_OUTCOME` on the authorization AND `tool_calls` rows (my §B.3/§B.4 state-sensitive parking, verbatim) · issued-unconsumed ⇒ `VOID` (my §D.4 expiry mapping) · the successor's `parked_unknown` disclosure + the ratified item-D `uncertain` manifest membership for cut relay identities — the EXISTING class, no new class, ONE outcome carrier (my rows), ONE effect truth (the conductor record). The `Describe` carve-out (effect-free, ticket-free, no `tool_calls` row, healed by free retry) is consistent with my rows — `Describe` never enters my F59 path, so no durable carrier of mine is owed or claimed.

**The hard constraints, verified in my consumption:** F67 — the secret-holding set stays exactly `{m-8, broker}`; my census's credential rows say precisely this and nothing in the delta moves placement ✓. F64 — the per-verb fence unweakened; the delta disposes only of already-sent stragglers ✓. F60/F66 — capability-not-bytes, epoch-fenced replacement; adoption never mints, the seat channel never rebinds, S-B never moves ✓. Item-D coherence — park-not-replay, no fabricated settled effect, no auto-resend; my never-silent-replay rule and the cut's discard-after-install compose without residue ✓.

**The owed amendment scope, named honestly (the governed vehicle the study's §C anticipates — NOT executed here; my artifacts are frozen under their approvals):** my contract's §B.5 (the handshake + `CROSSERS_DURABLE`/`ABORTED` + the recovery-matrix transition legs), the §F `epoch_transitions`/`crossing_ops` rows, the §B.3 matrix common-suffix (the R9 snapshot-withholding + the R10 lost-install replay, both dissolved by re-proposal recovery), and the §H CI-3 delta re-cite to this study's bytes; my stage-5's broker-spawn CI-4 realization + census row. One bounded amendment when master opens the affected-m-10-finals lane; the full F73 price accepted as ever.

**PART B — my SIGNED HALF of the §D join record (the broker-boundary ↔ continuation-lifecycle seam; against exactly rev8 `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce`; m-9's half rides their own FROM per channel-stamping):**

As the lifecycle/settlement-manifest PRODUCER, m-10 binds the broker's clean cut to the existing machinery as follows, and to nothing else:
- A relay tool call cut at the epoch boundary lands in my state-sensitive map exactly as any other in-flight: **a consumed ticket with no recorded outcome parks `UNKNOWN_TOOL_OUTCOME`** on the matching authorization row AND `tool_calls` row; **an issued-unconsumed ticket follows the `VOID` mapping**; **a terminal row stays terminal** — the cut introduces no blanket disposition and the broker is not an outcome authority over my rows.
- **The successor turn receives the exact parked relay-tool identity BEFORE new work** through the existing `parked_unknown` disclosure on both admission frames and the item-D settlement manifest's **`uncertain`** class — the existing class, no new manifest member, no receipt, no m-9 content-log record.
- **Reconciliation is INFORMED REDISCOVERY** — the successor reconciles the disclosed identity against conductor truth through the worker's OWN seat capability (`project`/`read`); my rows carry the app-side disposition, the conductor record carries the effect truth, and neither is fabricated from the other.
- **Any re-invocation is a FRESH F59 ticket through my full §D.2 issue procedure — never automatic**: the broker never re-issues a cut submit, my machinery never auto-resends, and the content-hash replay at the conductor recovers truth for an informed re-submit without re-execution.
- ONE outcome carrier (my durable rows), ONE effect truth (the conductor record), zero second settlement paths.

Signed against the rev8 bytes by m-10.planner (this relay's channel-stamped FROM); my implementer's adversarial pass rides the pair cycle on this return.

## Verification
- Recomputed this session: the study rev8 `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce` (exact) · m-7 r11 `9331ea88…` (unmoved) · my r40 `d2ce9831…` + stage-5 r10 `6fd1d655…` (frozen; zero bytes of mine moved by this return).
- Scope loci verified at the study by section: §Q3.3.4 (the CI-3 shrink + my-confirmation-as-vehicle) · §Q3.3.1 (the protocol + the eight-row table + my validation side) · §Q2.3 (the two-form gate) · §Q3.3.1's ladder paragraph (the R3-F2 wash-out leg) · §Q1.1 (CI-4) · §Q3.4/§D (the binding + the `Describe` posture) · §C (my scope list, matched item-for-item).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-broker-confirm/RECONCILE-planner-20260721-214500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this confirmation + join-record relay + one INDEX.md row timestamped 20260721-214500; no design-doc byte moved (the study, r11, my r40, and my stage-5 r10 all stay frozen), no `frank/` action, no lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer runs the adversarial check on this assessment + join-record half (per the dispatch's pair cycle) and returns the pair's byte-bound confirmation to master; on that return master pairs it with m-9's, carries the NO-H-24 determination into the §11 sequence, and opens the affected-m-10-finals lane for my named amendment scope; m-10.planner stands by.
