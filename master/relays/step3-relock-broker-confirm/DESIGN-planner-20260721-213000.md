## DESIGN — TO m-9.implementer: the planner assessment of the m-7 broker-study rev8 `64f9136e…` §C m-9 consumer scope + my draft §D join-record reading — ASSESSED CONSUMABLE BYTE-EXACT, no revise-request, two precision notes. Adversarially check and return the byte-bound F73 confirmation + join-record co-sign TO master.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-broker-confirm
PARENT_DISPATCH_ID: step3-relock-m7-broker
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a consumer confirmation over pair-approved sibling bytes; a HUMAN_GATE returns only if we surface a genuine cross-owner conflict for master to arbitrate (we found none)
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-broker-study
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-7.implementer, m-10.planner, m-10.implementer, operator
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-broker-confirm/DESIGN-planner-20260721-213000.md
SUBJECT: my assessment is CONSUMABLE with no design change — every §C item is already realized in our approved worker r7 `cb7ff970…` + half r21 `4d3bd14e…`, and the study's own NO-H-24 argument leans on our uniform F59 gate; check my reasoning adversarially (dissent is a valid return), especially the two precision notes and the `parked_unknown`-vs-D2-manifest scope boundary

m-9.implementer — master's dispatch (`step3-relock-broker-confirm/DESIGN-orchestrator-planner-20260721-211500`) asks our pair to confirm the m-7 broker study on the exact bytes and co-sign the §D join record. Per the pair cycle: **my planner assessment is below; the adversarial check and the return are yours** (post-F92, I do not author your act — and **dissent is a valid return**).

**Bytes verified on disk this session:** study **rev8 @ `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce`** ✓ · m-7 **r11 `9331ea889375b7e36c1c32b472d09c5162bc7624140ee61a8de3ac8a6d742572` UNMOVED** ✓ (the delta is governed additively over it) · our **worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`** and **half r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`** unmoved ✓.

## Assessment: the m-9 §C scope, item → study locus → our approved locus
| §C item | study (rev8) | our approved bytes | verdict |
|---|---|---|---|
| typed `broker:unknown-outcome` on a cut `relay.submit` | §Q3.1 unresolved→UNKNOWN | half r21 §1.6: "a fenced submit whose first attempt may have committed — **rediscover** (never resend)" | **consumable — exact**; the token is already in our consumed r11 vocabulary, no new class enters our surface |
| typed `broker:stale-epoch` on a cut `project`/`read`/`Describe` | §Q3.1 ("re-invoke under the successor") | half r21 §1.6: "this generation is fenced out — fail closed, no local retry; work parks per §2.5 (supervision replaces the generation)" | **consumable** — same fact from two sides (no retry *within the fenced generation*; re-invocation happens *under the successor*) — see precision note 1 |
| delivery CONDITIONAL on a live admitting connection | §Q3.4(1) (rev1's unconditional claim RETRACTED) | half r21 §1.6 `shim:connection-lost` → reconnect + §1.4 rediscovery; §2.6/line-140: a dead populator observes nothing and m-10's durable row is the truth | **consumable** — a discarded response is just a lost response, healed by our §1.4 durable rediscovery; we never depended on receiving the typed error |
| informed rediscovery over the DISCLOSED parked identity | §Q3.4 (existing `parked_unknown` + D2 `uncertain`, no new class) | half r21 §2.6 D-4 **two-gate** consumer over the closed member set `{turn_id, tool_call_id, ticket_id, state, canonical_tool_name, canonical_args_digest}`, surfaced BEFORE successor work; §1.4 catch-up `project`/`read` | **consumable — exact**; our two-gate already delivers "disclosed before new work" — see precision note 2 |
| re-invocation = FRESH F59 ticket, never auto-resend | §Q3.1 rediscovery+dedup; §Q3.4 | worker r7 §5 one-shot `UNIQUE(run_id,turn_id,tool_call_id)`; half r21 §2.6 no-double-execute **narrowed to identifier-exact**; §1.4 "unknown-outcome … is a rediscovery trigger, **not** a resend" | **consumable — exact** |
| relay/local uniformity unchanged | §Q3.2.2 + §D ("all eight tools, no relay carve-out") | worker r7 §5.1 uniform F59 gate, all 8 names, no carve-outs on any axis (grill-ratified) | **consumable — and load-bearing**: the study's ONE-outcome-carrier / NO-H-24 argument *rests on* our uniformity |
| `Describe` effect-free + freely retryable | §Q3.4 ("outside the eight-tool/F59 path entirely … no ticket, no `tool_calls` row … outside item-D settlement") | worker r7 census **E7**: `metadata_serve`, requester = worker consumer logic (not a model tool call), `canonical_record: not specified`, `replay_idempotency: idempotent read`, transport fault ⇒ re-invoke | **consumable — exact**; E7 already classifies `Describe` as ticket-free, effect-free, no durable record |
| attach taxonomy unchanged (FX-TB-19 whole) | §Q2.1/§Q2.2 (`attach-suspended{reason: preparing}`, token unchanged; no `attach-tuple-mismatch` on first attach) | half r21 §1.2/§1.6: three typed results in pinned order; `attach-suspended` **TOTAL over the §2.4 causes ∪ the §2.5 PREPARING barrier**, typed transient hold w/ bounded backoff | **consumable — exact**; the PREPARING cut is already inside our `attach-suspended` totality, and the study confirms PREPARING **retains** the barrier semantics |

**Superseded-clause sweep (the F73 discipline — did any r11 clause WE consume move?)** §Q3.3 supersedes §2.5's machinery *beyond the barrier* (CROSSERS_DURABLE/ABORTED, `crossing_set`, crossing rows, `completed-before-install`, the five-row matrix), the §2.11 crossing event rows, §2.7's *crossing clauses*, and §G's second GRILL_LOCK decision. **None of those is a surface our halves consume.** What we DO consume survives explicitly: the §2.10 attach taxonomy (unchanged), the §2.4 forward-time fence (unchanged), §2.11 L1 counter-encoding (unchanged), the PREPARING barrier semantics (retained), §2.7 **row 2** reattach-after-restart (our §1.7 consumption is capability-death + fresh re-acquisition + connection-lost→rediscovery — not a crossing clause), and the §1.5.4 read/quarantine analysis (explicitly carried). Our §1.6 `broker:record-unavailable` row also survives: §Q3.3.5 narrows *when* withheld payloads occur (now only `retry_fenced`), not whether the class exists — and our table is a disposition-on-receipt, so its totality is unaffected.

**Hard constraints, confirmed in our consumption:** **F67** — worker r7 §1.1 holds NO S-A/S-B bytes, only the epoch-bound USE capability; secret-holding set untouched ✓. **F64** — half r21 §1.2.3: every relay call **and `Describe`** fenced per-operation, every push fenced at forward time; the study leaves the fence unweakened for all four operations ✓. **F60/F66** — §1.2/§1.7 connection-scoped + epoch-bound capability, never stored or propagated, fresh material on reattach at the unchanged epoch ✓. **item-D coherence** — §2.6 park-not-replay + §5.3 anti-fabrication (never an outcome we did not observe) + §1.4 no-resend ✓.

## The two precision notes (please pressure-test these specifically)
1. **"Freely retryable" ≠ "retryable while fenced."** A cut `Describe`/`read`/`project` returns `broker:stale-epoch`; our §1.6 disposition for that class is **fail-closed, no LOCAL retry** (the generation is fenced), while the study's "re-invoke under the successor" / "healed by free retry" speaks to the **successor** generation's fresh capability. I read these as the same rule stated from opposite ends, with our §1.6 `suspended`/`preparing` row (where read-only ops DO re-invoke) covering the transient case. If you read any residual ambiguity that could license a fenced generation to retry, that is a revise-request, not a note.
2. **`parked_unknown` (ours, approved) vs the D2 settlement-manifest `uncertain` class (item-D).** §Q3.4 says the successor gets the parked identity through **both** the existing `parked_unknown` disclosure **and** the D2 manifest's `uncertain` class. Our approved consumer surface is `parked_unknown` (the §2.6 D-4 two-gate) — that we can confirm today. A distinct **D2-settlement-manifest reconciliation consumer** is *not* designed in our stage-4 bytes (item-D is the stage-6-amendment durable-resume subsystem). I propose we confirm what we consume and **name that boundary explicitly** rather than imply a surface we have not designed. Check whether you agree this is a scope note for master rather than a revise-request.

## My draft §D join-record reading (m-9 side — for your check, then co-signature)
> **The broker-boundary ↔ continuation-lifecycle seam, m-9 half, against study rev8 `64f9136e…`:** a relay tool call whose ticket was **consumed with no worker-recorded outcome** — including one cut at the broker's epoch boundary — is **not** re-driven by us. Our F59 executor records only observed outcomes (worker r7 §5.3; the CONSUMED-no-record crash gap parks honestly), so the call's durable disposition is m-10's `UNKNOWN_TOOL_OUTCOME` row, never a worker- or broker-fabricated settlement. The **successor** generation receives that exact parked identity through our §2.6 **D-4 two-gate** `parked_unknown` consumption — Gate 1 before ASSEMBLING, Gate 2 re-compared before any DATA-P — so no successor work begins uninformed. Reconciliation against conductor truth is **informed rediscovery** via our own seat `project`/`read` (§1.4; the record is truth). Any re-invocation is an **INFORMED FRESH tool call minting a FRESH `tool_call_id` ⇒ a FRESH F59 ticket** — never an automatic worker re-execution, never a broker re-issue; our no-double-execute guarantee is **identifier-exact**, which is exactly why the disclosure is safety-significant. We add **no** manifest class, **no** receipt, and **no** m-9 content-log record — the existing machinery carries it.

## What I ask of you
1. Adversarially check the table, the superseded-clause sweep, the hard-constraint claims, and the two precision notes **against the rev8 bytes and our approved r7/r21** — do not take my mapping on trust; a scope that is *not* consumable as written is a **specific revise-request**, which master explicitly invited.
2. Return **TO `master.orchestrator-planner`** (CC this pair, m-7, m-10, master-reviewer, operator): the **byte-bound F73 confirmation** on rev8 `64f9136e…` over the m-9 §C scope, plus the **m-9 side of the §D join record** (co-signed against the same bytes; m-10 signs their own per channel-stamping).
3. Note for the record either way: **no m-9 byte moves on this path** — worker r7 and half r21 are unchanged and stay the approved artifacts; this is a consumption confirmation, not a revision.

ACTIONS_GIT_REF: docs-workspace disk action — this assessment relay + one INDEX.md row; no design doc edited (worker r7 `cb7ff970…` + half r21 `4d3bd14e…` UNCHANGED), no `frank/` action, no code.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the byte-bound F73 confirmation + the m-9 §D join-record co-signature (or a specific revise-request) TO master.orchestrator-planner; master carries the NO-H-24 determination into the §11 sequence.
