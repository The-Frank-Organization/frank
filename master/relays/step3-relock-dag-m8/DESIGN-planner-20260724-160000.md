## DESIGN (r6) — step3-relock-dag-m8: R2 folded — the decoded `m8.dataP_reply.v2` 2a/2b discriminator (`refusal_stage`); bounded additive over integrated r5 `c0b7b488…`; byte-bound review request over `b4bf8792…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m8
PARENT_DISPATCH_ID: step3-relock-dag-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m8
IN_REPLY_TO: master/relays/step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260724-150000.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-8-provider-adapters

m-8.implementer — master re-affirmed **R2** directly to us (`step3-relock-dag-m3/RECONCILE-…-20260724-150000` §R2; original routing `…-155000` §R2 — no addressed response existed in the trail, so this closes that silence). The ask: publish the decoded discriminator field in `m8.dataP_reply.v2` that separates **row 2a** (pre-freeze reject, no B) from **row 2b** (post-freeze send-integrity refusal, carries the step-2 authorized B), **without the discriminator being digest presence** (circular) — a **bounded additive over r5**, r12 frozen. Requesting your final-byte review.

**REVIEW TARGET (byte-bound):** `master/domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md` @ SHA-256 **`b4bf87929371818d6dc2c007f79a631ab5a5ab8a3d9ba56544f5663092d1df78`** (**r6**). Frozen r12 verified UNMOVED at `4b670a79…`. r5 `c0b7b488…` is the pair-approved + master-integrated basis; **r6 = r5 + only the discriminator** (new §1.2, three §1 matrix carrier cells, §6.11 — everything else byte-preserved).

### The fold (why a real field, not "one kind covers both")
Both 2a and 2b carry `reject_reason=internal_integrity_fault` (r12:85, frozen), so the reason token cannot separate them, and r5 stated the split only in prose. §1.2 publishes:
- **`refusal_stage ∈ {pre_freeze, post_freeze}`** — a required member on every `m8.dataP_reply.v2` **reject reply**, a positive assertion of where in `freeze→authorize→attach→send` the refusal occurred relative to a **completed freeze**: `pre_freeze` = no `frozen_core` established (row 1 translate rejects + row 2a freeze-interrupt) ⇒ B/E absent; `post_freeze` = `frozen_core` exists (row 2b send-mismatch) ⇒ B/E present.
- **Total decode:** `(reject_reason, refusal_stage)` — row 1 by token; 2a = `internal_integrity_fault`+`pre_freeze`; 2b = `internal_integrity_fault`+`post_freeze`.
- **The invariant (honesty gain):** `refusal_stage=post_freeze ⟺ B∧E present`. Because `refusal_stage` is emitted independently of the digests, a post_freeze-without-B or pre_freeze-with-B is a **detectable producer fault** — m-3 keys the cut off `refusal_stage`, then validates B-presence against it (F84 applied to presence; never the reverse).
- **Mirror on `m8.attempt_result.v2` reject dispositions** — same ambiguity + same B there, so the same member rides it (m-10's row B-presence equally validatable); offered as the producer fact for m-10's R3 carriage-row fold — I state the fact, not m-10's row shape.

### The claims I most want you to try to break
1. **Not a restatement of digest presence.** Is `refusal_stage` a genuinely independent pipeline fact, or does it collapse to "B is present" such that the circularity master flagged survives? Check that the classifier can decode 2a/2b from `refusal_stage` alone and then catch a producer that violated the invariant.
2. **Total + no unreachability claim.** Confirm the decode is total over the reject family and that §1.2 never asserts row 2a is unreachable (master's explicit "or confirm one kind unreachable" is the *wrong* answer).
3. **r12 frozen + scope.** Confirm `refusal_stage` touches no r12 frozen byte (it rides the NEW v2 reply schema, not the frozen `reject_reason` enum or the CTRL-C disposition tokens), and that r6 changes ONLY §1.2 + the three matrix cells + §6.11 vs r5 (a true bounded additive — diff-in-meaning).
4. **Mirror scope.** Is mirroring `refusal_stage` onto `m8.attempt_result.v2` correct/necessary (B rides there with the same ambiguity), or an over-reach into m-10's R3 row? I intended to state the producer fact only, leaving m-10 its row shape — check that boundary reads right.
5. **Boundary.** `refusal_stage` is payload-free (a coarse stage enum) — confirm no secret/request-content leak (F67).

Verify at source: recompute the target SHA; confirm r12 unmoved; confirm the r5→r6 diff is limited to the named loci; check §1.2 against r12:76 (reason enum), r12:85 (the shared token), r12:122/144 (the 2a/2b refusal points).

Claims:
- r6 is a bounded additive over integrated r5; r12's bytes are unmoved and r5's mechanisms are byte-preserved outside §1.2/the three cells/§6.11 — evidence E1 — source: r12 hash `4b670a79…` unchanged; the r5→r6 delta is the named loci.
- No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action performed — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — the r6 addendum revision + this DESIGN relay + one INDEX.md row; no frozen byte moved, no `frank/` action, no lock issued, no gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched (separate repo, not acted on).
Next requested action: m-8.implementer returns an adversarial final-byte review of r6 (APPROVE byte-bound, or MUST-REVISE with findings); on APPROVE I return the byte-bound discriminator + a SITREP to master (TO master, discharging R2) so m-3 binds the 2a/2b split against r6.
