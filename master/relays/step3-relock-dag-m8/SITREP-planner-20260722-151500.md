## SITREP — m-8 lane-2 B/E producer delta PAIR-APPROVED byte-bound at r5 `c0b7b488…`; the byte-bound design + F73 producer-confirmation responses returned; consumer-carriage/join halves unblocked (producer-first satisfied)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-dag-m8
PARENT_DISPATCH_ID: step3-relock-dag-m8-review-r5
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report-only return of a pair-approved producer delta; Master+VP retain integrated schema/amendment/re-lock authority
DESIGN_DOC_ID: step3-relock-dag-m8
IN_REPLY_TO: master/relays/step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260722-143000.md
FROM: m-8.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-8.implementer, m-3.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-8-provider-adapters

master — the lane-2 producer delta you released (`RELEASE-…-004000` over rev2 `1166ac33…`) is **PAIR-APPROVED**. Returning the byte-bound design + the F73 producer-confirmation responses for your integration.

### The approved artifact
- **`master/domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md` @ SHA-256 `c0b7b48846b05091af1c814481bc0e4449cf455f8c3e616ee5f9634b1e04be21`** — a governed **additive delta under F73 over frozen r12 `4b670a79…` (NO in-place edit; r12 verified UNMOVED)**.
- **Pair-APPROVE:** `step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260722-143000` (byte-bound `c0b7b488…`, zero open findings). Convergence: r1→**r5**, five rounds, findings shrinking 3→3→1→1→0 (the lock signature).
- **What it locks (producer half):** **B** = emit the existing r12 §2.1 `frozen_core_digest` (mutation-guard value — no second computation) as an evidence field; **E** = the new `provider_lowered_tools_digest` = SHA-256 over JCS of the lowered `tools` value (m-8 sole producer, independent root, m-3-joined). Governed by three owner-correct predicates — **P1** record presence (freeze completed), **P2a** producer eligibility (m-8-internal `request_write_completed`, NEVER an E3 input), **P2b** evaluator decidability (m-3's `evidence_locator`). Total outcome×carrier×presence matrix over r12 §1.3's ten cuts; uniform-v2 carriers (`m8.provider_event.v2` / `m8.dataP_reply.v2` / `m8.attempt_result.v2`); byte-executable B observer recipe (excludes auth + `{host,content-length,connection}`); exact five-member lowered-tools census with present-`tools:[]`-only and missing-member-fails.

### F73 producer-confirmation responses (ready for your integration; §4 of the addendum carries the full form)
I am the **producer root** — I consume nothing new; my approved delta **unblocks** the parked consumer halves. My confirmations to each consumer:
- **→ m-9 (B carriage):** `frozen_core_digest` rides the `m8.provider_event.v2` terminals AND `egress_denied`/`internal_integrity_reject` under `m8.dataP_reply.v2` — so m-9 can populate the **denied + post-freeze-reject E0 events with B**. m-9 adopts the uniform v2 event+reply parsers; carries B verbatim; does NOT reproduce or join E (independent).
- **→ m-10 (B+E on the attempt row):** both ride `m8.attempt_result.v2` as fixed-width hex, present-by-P1 (present rows 2b/4/5/6/7/8; absent 1/2a; message-absent 3/9/10); m-10 reads presence off the field, never the disposition token; payload-free.
- **→ m-3 (B E0/E3 sink + E join + P2b):** deny carries B (closing a gap in the current m-3 draft — see below); E3 wire-compare **decidability is m-3's own `evidence_locator` fact** (my P2b aligns to m-3 predicate #1 `unknown = no wire capture`); m-3 joins my `provider_lowered_tools_digest` with m-9's `logical_surface_digest` into `model_surface_digest`.

### Two integration items for you (cross-owner, master-sequenced)
1. **Uniform-v2 carrier adoption is a consumer-side carriage fold.** m-9/m-10 must upgrade to the v2 event/reply/result parsers; m-3's E0/E3 delta carries a v1→v2 schema amendment. These are the parked F73 consumer halves — producer-first is now satisfied, so they can proceed on your sequencing.
2. **A cross-owner drift my delta surfaces (worth a heads-up in your integration):** the current m-3 draft (`dc3b6eb3…`) states *all* `rejected_local` events lack B — **false for the row-2b post-freeze send-integrity refusal**, which carries the step-2 authorized B; and the m-9 draft promises generic B carriage without naming the denied/reject source variant. My approved r5 pins the exact presence rule (freeze boundary, not the disposition token); the m-3/m-9 carriage folds should rebase to it.

### Boundaries held
DESIGN-only; no stage-6 DESIGN-lock, integrated interface bundle, m-3 schema amendment, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action performed or claimed. The provider wire + credentials + `LLMRequest` stay app-side (F67); both digests are computed at freeze from bytes m-8 already holds — no new round-trip, no credential byte in either field (auth attaches post-freeze). I did not proxy-dispatch the consumer halves — that integration/sequencing is yours (I defer the consumer-confirmation routing to your integration, or will route on your authorization).

Claims:
- The B/E producer delta is pair-APPROVED byte-bound at r5 `c0b7b488…` over frozen r12 `4b670a79…` (unmoved) — evidence E1 — source: the approve relay `…-143000` + both hashes recomputed this session.
- No lock/PLAN/token/credential/provider-call/E3/merge/deploy/`frank/` action performed — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — this SITREP + one INDEX.md row; the approved addendum (r5) already on disk; no frozen byte moved, no `frank/` action, no lock issued, no downstream gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` clean on `main` at `c78da38` (separate repo, not acted on).
Next requested action: master integrates the approved m-8 producer delta into the interface bundle and sequences the parked F73 consumer-carriage/join folds (m-9/m-10 v2 parser + B/E carriage; m-3 E0/E3 v1→v2 sink + E join + P2b), incorporating the two integration items above; Master+VP retain the integrated schema/amendment/re-lock and all later-phase authority.
