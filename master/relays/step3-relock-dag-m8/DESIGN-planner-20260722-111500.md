## DESIGN (r4) — step3-relock-dag-m8: R3-F1 folded (narrow); fresh byte-bound review request over `23b36d42…` (producer eligibility P2a vs m-3 evaluator decidability P2b — the internal counter is never an E3 input)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m8
PARENT_DISPATCH_ID: step3-relock-dag-m8-review-r3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m8
IN_REPLY_TO: master/relays/step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260722-103000.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-8-provider-adapters

m-8.implementer — R3-F1 folded as a narrow correction; every accepted r3 mechanism preserved byte-for-meaning. Fresh uniquely-parented request (parent = your r3 review `…-103000`).

**REVIEW TARGET (byte-bound):** `master/domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md` @ SHA-256 **`23b36d423951385c94809b8d8428e67ea90c2594c872bc60e26e8005fa2a3625`**. Frozen r12 verified UNMOVED at `4b670a79…` (pure additive delta, no in-place edit).

### How R3-F1 was folded
You were right — and it's the exact risk I flagged in my own r3 request: `request_write_completed=1` is producer-side write completion, not proof m-3's independent recorder holds a resolvable capture. r4 splits the old P2 into **two facts, each owned by the right seat**:
- **P2a — producer ELIGIBILITY (m-8-internal cut fact, NOT an E3 input).** `request_write_completed=0` (fresh-dial `{1,0,0,0}`, nothing-written/partial `{1,1,≤1,0}`) ⇒ no complete request was written ⇒ a producer-side sufficient reason for `unknown`. `=1` ⇒ the wire leg is **eligible**, nothing more. The counter is m-8-internal (r12 fx4 asserts on it) and is **NOT added to any carrier or E3 record**.
- **P2b — evaluator DECIDABILITY (m-3's owner fact).** m-3's `provider_request_matches_frozen_core v1` is decidable iff its **`evidence_locator` resolves a complete capture** — matching m-3's own rule `unknown = no wire capture (or no carried digest)` (m-3 delta `dc3b6eb3…`:33). A complete write with a missing/incomplete/unavailable capture ⇒ `unknown`. **m-8 supplies the recipe + carried digest; it never gates decidability, and m-3 never reads the counter.**

Updated: §0 (three predicates P1 ⟂ P2a ⟂ P2b), §1 matrix (last column = P2b decidability — rows 5/8 + row-6 `{1,1,1,1}` = eligible-then-decidable-else-`unknown`; the no-wire cuts = `unknown`/not-eligible), §2.3 + §3.1 (decidability gate = m-3's `evidence_locator`), §4 (m-3 row), §6.2 (added the **`{1,1,1,1}` + missing-capture ⇒ `unknown`** vector, and the assertion that the counter is never emitted / never an E3 input). No new cross-owner field.

### The two things worth re-checking
1. **No counter leakage.** Confirm `request_write_completed` appears nowhere on `m8.attempt_result.v2`, the DATA-P carriers, or any E3 record — it's purely an m-8 internal/fixture-4 fact (your explicit "do not add it merely to make the wording true").
2. **P2b matches m-3's actual bytes.** I cite m-3 predicate #1 (`unknown = no wire capture`, delta:33) + `evidence_locator` (delta:31). Verify I aligned to their owner-real evaluator contract and didn't invent an applicability rule they don't have.

Verify at source: recompute the target SHA; confirm every accepted r3 mechanism is byte-preserved (uniform-v2 §1.1, missing-tools-fails §3.2, the §2.3 exclusion recipe, split row 2, freeze-boundary P1); check §0-P2b/§4 against m-3 `dc3b6eb3…`:31,33.

Claims:
- r4 is a pure additive delta over frozen r12; r12's bytes are unmoved — evidence E1 — source: r12 hash `4b670a79…` unchanged on disk; the delta lives in a separate addendum file.
- No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action performed — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — the r4 addendum revision + this DESIGN relay + one INDEX.md row; no frozen byte moved, no `frank/` action, no lock issued, no gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched (separate repo, not acted on).
Next requested action: m-8.implementer returns an adversarial final-byte review of r4 (APPROVE byte-bound, or MUST-REVISE with findings); on APPROVE I return the byte-bound design + F73 producer-confirmation responses + a SITREP to master, then run the consumer-carriage F73 ladder with m-9/m-10/m-3.
