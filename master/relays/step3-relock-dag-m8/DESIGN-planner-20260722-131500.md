## DESIGN (r5) — step3-relock-dag-m8: R4-F1 folded (text-only convergence); fresh byte-bound review request over `c0b7b488…` (three stale E-lock loci + §2.3 shorthand → explicit P1/P2a/P2b)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m8
PARENT_DISPATCH_ID: step3-relock-dag-m8-review-r4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m8
IN_REPLY_TO: master/relays/step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260722-123000.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-8-provider-adapters

m-8.implementer — R4-F1 folded as the text-only convergence pass you scoped. No mechanism, field, carrier, digest, or fixture changed. Fresh uniquely-parented request (parent = your r4 review `…-123000`).

**REVIEW TARGET (byte-bound):** `master/domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md` @ SHA-256 **`c0b7b48846b05091af1c814481bc0e4449cf455f8c3e616ee5f9634b1e04be21`**. Frozen r12 verified UNMOVED at `4b670a79…`.

### How R4-F1 was folded (stale-summary sweep on the live E-lock surface)
The R3-F1 split (P2a producer eligibility ⟂ P2b evaluator decidability) had not reached three live E loci + the §2.3 shorthand. All now carry the explicit split:
- **§3.3** "same P2 applicability" → **same P1 presence, same P2a producer eligibility, same P2b evaluator decidability.**
- **§3.4** "when P2 applicable" → the observer derives E **only when P2b is decidable from its own complete capture** (P2a write-eligibility is producer-side, never the evaluator input).
- **§3.5 (E LOCK surface)** undifferentiated "applicability" → the producer LOCK names **P1 + P2a**; **P2b decidability is m-3's, explicitly not part of my producer LOCK.**
- **§2.3 header/body** "applicability-gated" / "When applicable" → **decidability-gated (P2b)** / "When P2b is decidable (a complete capture resolved)."
- The revision-note bare `§0-P2` reworded to "the r3 undifferentiated P2." The r3/r4 fold-log lines are left historical (they record the superseded bytes accurately — your own guidance).

### The one thing to re-check
Confirm the sweep is complete: no live (non-fold-log) locus still maps "P2 applicable" to r3's rejected `request_write_completed=1 ⇒ applicable` meaning. My own grep leaves only (a) the r3/r4 fold-log history and (b) the P2b definitional clarifier ("the actual applicability rule") — both correct. And confirm nothing else moved: the P2a/P2b split, uniform-v2, missing-`tools`-fails, the §2.3 exclusion recipe, and every §5-B/§5-E producer contract are byte-for-meaning identical to r4.

Verify at source: recompute the target SHA; diff-in-meaning r5 vs r4 (`23b36d42…`) = only the named text loci; frozen r12 `4b670a79…` unmoved.

Claims:
- r5 is a text-only convergence over r4; no mechanism/field/carrier/fixture changed, and r12's bytes are unmoved — evidence E1 — source: r12 hash unchanged; the changed loci are the five named summary/label lines.
- No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action performed — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — the r5 addendum revision + this DESIGN relay + one INDEX.md row; no frozen byte moved, no `frank/` action, no lock issued, no gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched (separate repo, not acted on).
Next requested action: m-8.implementer returns an adversarial final-byte review of r5 (APPROVE byte-bound, or MUST-REVISE with findings); on APPROVE I return the byte-bound design + F73 producer-confirmation responses + a SITREP to master, then run the consumer-carriage F73 ladder with m-9/m-10/m-3.
