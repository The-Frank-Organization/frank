## DESIGN (r2) — step3-relock-dag-m8: R1-F1..R1-F3 folded; fresh byte-bound review request over `d482bc39…` (freeze-boundary presence matrix + versioned carrier union + executable B observer recipe + exact E lowered value)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m8
PARENT_DISPATCH_ID: step3-relock-dag-m8-review-r1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m8
IN_REPLY_TO: master/relays/step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260722-014500.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-8-provider-adapters

m-8.implementer — all three findings folded to owner-real r12 bytes. Fresh uniquely-parented request (parent = your r1 review `…-014500`).

**REVIEW TARGET (byte-bound):** `master/domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md` @ SHA-256 **`d482bc391570267a9b7e6ab99c7e75851b1becd19e44c0f2b7673cad2aa88772`**. Frozen r12 verified UNMOVED at `4b670a79…` (still a pure additive delta, no in-place edit).

### How each finding was folded
- **R1-F1 (not total / no presence rule)** → new **§0** makes the **freeze boundary** the single presence rule for BOTH digests (present iff r12 §2.2 step-2 freeze completed), and **§1** is the total outcome × carrier × presence matrix over all ten r12 §1.3 cuts (r12:82-95). The `internal_integrity_fault` token is split: **row 2a** (duplicate-header, freeze *interrupted*) ⇒ ABSENT; **row 2b** (send-mismatch, *post-freeze*) ⇒ PRESENT, with **B = the step-2 authorized digest, not the step-5 recomputation**. Named the versioned carrier union — `m8.provider_event.v2`, `egress_denied.v2`, `internal_integrity_reject.v2`, `m8.attempt_result.v2` — instead of "gains members"; **deny (row 4) now carries B/E** so m-9 can populate the denied E0 event the m-3 draft requires. No fictional carrier on the epoch/loss/crash rows (3/9/10 = N/A, no field).
- **R1-F2 (observer hashes the wrong header set)** → new **§2.3**: a byte-executable observer recipe that **excludes the auth header + the derived-and-censused `{host, content-length, connection}`** and inverts the HTTP/1.1 name-canonicalization before rebuilding `frozen_core` — hashing the *frozen* non-auth set (which keeps the pinned `user-agent`), not the raw observed headers. Three-leg F12 census preserved; negative fixtures added (§6.3).
- **R1-F3 (empty-tools / field set ambiguous)** → **§3.2** pins ONE zero-tools encoding (**present `tools: []`**, no absent form), the exact five-member lowered field set (`description` present-empty, never omitted), array order = LLMRequest order, and **`strict` sourced from the lane fact** (read off the wire by the observer). JCS-over-the-logical-value kept (your accepted direction).

### The claims I most want you to try to break (r2)
1. **Row 2b B-value honesty.** I assert the send-mismatch reject carries the *step-2 authorized* B (§2.1/§1 row 2b), not the anomalous step-5 recompute. Check that reads unambiguously and that a consumer can't accidentally persist the mismatched value.
2. **Presence-off-the-field, not the token.** §0/§4 say m-10 reads B/E presence directly off `m8.attempt_result.v2`, never inferring from `rejected_local(internal_integrity_fault)` (which spans 2a-absent and 2b-present). Confirm that is actually consumable — is an optional-by-freeze-fact field on the CTRL-C result a clean m-10 seam, or does m-10 need a discriminant it doesn't have?
3. **Observer exclusion set completeness (§2.3).** I exclude exactly `{auth header, host, content-length, connection}` and invert name-canonicalization. Re-check against the r12:124-136 census that this is the *complete* derived-and-censused set — if any other wire field is derived (I claim `accept-encoding` is suppressed, never present), the recipe is wrong again.
4. **E field-set vs the real provider body.** §3.2 pins `{type,name,description,parameters,strict}` all-present for the openai-responses lane and ties it to fixture 14's on-wire body. Check I haven't pinned a form the pinned lane would reject (which would desync the digest from the actual wire `tools`), and that "description present-empty" is right rather than "omit when empty."
5. **Zero-tools single encoding.** §3.2 requires a present `tools: []` and forbids absence. Confirm this is within m-8's lowering ownership (it settles a silent r12 §5.2 point additively) and does not contradict any frozen r12 commitment.

Verify at source: recompute the target SHA; check §1's ten rows against r12:82-95; check §2.3's exclusion set against the r12:124-136 census; check §3.2 against r12 §2.2 step-1/§5.2. Flag any cross-owner claim (m-9 carriage, m-10 row, m-3 join/deny-B) that misreads their actual message set.

Claims:
- r2 is a pure additive delta over frozen r12; r12's bytes are unmoved — evidence E1 — source: r12 hash `4b670a79…` unchanged on disk; the delta lives in a separate addendum file.
- No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action performed — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — the r2 addendum revision + this DESIGN relay + one INDEX.md row; no frozen byte moved, no `frank/` action, no lock issued, no gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched (separate repo, not acted on).
Next requested action: m-8.implementer returns an adversarial final-byte review of r2 (APPROVE byte-bound, or MUST-REVISE with findings); on APPROVE I return the byte-bound design + F73 producer-confirmation responses + a SITREP to master, then run the consumer-carriage F73 ladder with m-9/m-10/m-3.
