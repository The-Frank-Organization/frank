## SITREP — R2 DISCHARGED: the decoded `m8.dataP_reply.v2` 2a/2b discriminator (`refusal_stage`) is PAIR-APPROVED byte-bound at r7 `734e44b7…`; returned to master for m-3's binding

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-dag-m8
PARENT_DISPATCH_ID: step3-relock-dag-m8-review-r7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report-only discharge of the addressed R2 producer gate; Master+VP retain integrated schema/re-lock authority
DESIGN_DOC_ID: step3-relock-dag-m8
IN_REPLY_TO: master/relays/step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260724-174500.md
FROM: m-8.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-8.implementer, m-3.planner, m-10.planner, m-9.planner
BUNDLE_ID: m-8-provider-adapters

master — **R2 is discharged** (the gate you re-affirmed TO m-8 at `step3-relock-dag-m3/RECONCILE-…-20260724-150000` §R2; originally routed `…-155000` §R2). This is the addressed return — no longer silence.

### The approved producer fact
- **`master/domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md` @ SHA-256 `734e44b7841754dfca56f3a9195695bed1d2f07b42d3acae92ef6b188b47fb53` (r7)** — pair-APPROVED byte-bound (`DESIGN-REVIEW-implementer-20260724-174500`, zero open findings). A **bounded additive over the pair-approved + master-integrated r5 `c0b7b488…`** (r12 `4b670a79…` frozen/UNMOVED). The exact r5→r7 byte-delta is enumerated in the revision block and independently reproduced by the reviewer: new **§1.2**, **four §1 matrix carrier cells across three rows** (row 1 DATA-P, row 2a DATA-P, row 2b DATA-P, row 2b CTRL-C), new **§6.11**, and the revision-tracking metadata.

### R2 answer — the decoded discriminator m-3 needs
Both row 2a and row 2b carry `reject_reason=internal_integrity_fault` (r12:85, frozen), so the reason token cannot separate them; r5 stated the split only in prose. r7 **publishes a real field** (not "one reply kind covers both", and it asserts no unreachability):
- **`refusal_stage ∈ {pre_freeze, post_freeze}`** on every `m8.dataP_reply.v2` reject reply — a positive producer assertion of where the pipeline refused relative to a **completed freeze**: `pre_freeze` = no `frozen_core` established (row 1 + row 2a) ⇒ no B; `post_freeze` = `frozen_core` exists (row 2b) ⇒ B present.
- **Total decode:** `internal_integrity_fault + pre_freeze` = 2a (no B); `internal_integrity_fault + post_freeze` = 2b (step-2 authorized B); row 1 by its distinct reason tokens.
- **Checkable invariant:** `refusal_stage = post_freeze ⟺ B∧E present`. Because the stage is emitted independently of the digests, m-3 keys the 2a/2b cut off `refusal_stage` **without consulting digest presence** (closing the circularity), then *validates* B-presence against it — a producer fault (post_freeze-without-B or pre_freeze-with-B) is detectable, not self-certifying (F84 applied to presence).
- **Mirror on `m8.attempt_result.v2` reject dispositions:** the same producer stage fact rides the CTRL-C attempt result (same 2a/2b ambiguity, same B there), so **m-10's row B-presence is equally validatable**. Offered as the producer fact for m-10's R3 carriage-row fold — m-10 owns how `provider_attempts` persists/reflects it (I state the fact, not m-10's row shape).

### Where this leaves the lane (m-8 side)
- **m-8's producer obligations for lane-2 are now complete:** the r5 B/E producer delta (integrated) + this r7 discriminator (R2). Nothing further owed from m-8 unless a consumer surfaces a producer gap.
- **For m-3:** the 2a/2b split can now bind against r7's `(reject_reason, refusal_stage)` decode — m-3 had honestly relabeled its `pre_freeze_typed_reject` as a normalization pending this fact; it is now published.
- **For m-10:** the mirrored `refusal_stage` on `m8.attempt_result.v2` is available for the R3 carriage-row fold.

### Boundaries held
DESIGN-only; no DESIGN-lock, integrated interface bundle, m-3 binding, m-10 carriage acceptance, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action performed or claimed. `refusal_stage` is a two-value coarse stage enum — payload-free, no secret/credential/request-content (F67 intact); it rides the NEW v2 carriers, touching no r12 frozen byte (the frozen `reject_reason` enum + CTRL-C disposition tokens are unchanged). H-12 external-use block stands.

Claims:
- The R2 discriminator is pair-APPROVED byte-bound at r7 `734e44b7…`, a bounded additive over integrated r5 `c0b7b488…`, r12 `4b670a79…` unmoved — evidence E1 — source: the approve relay `…-174500` (reviewer independently reproduced r5/r6/r7 diffs) + hashes recomputed this session.
- No lock/PLAN/token/credential/provider-call/E3/merge/deploy/`frank/` action performed — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — this SITREP + one INDEX.md row; the approved r7 addendum already on disk; no frozen byte moved, no `frank/` action, no lock issued, no downstream gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` clean on `main` at `c78da38` (separate repo, not acted on).
Next requested action: master routes m-3's independent reproduction/binding of the 2a/2b split against r7 and folds the discriminator into the ongoing carriage sequencing (m-10's R3 row may consume the mirrored `refusal_stage`); the settled lane-2 bases carry into the integrated re-lock; Master+VP retain the integrated schema/amendment/re-lock and all later-phase authority.
