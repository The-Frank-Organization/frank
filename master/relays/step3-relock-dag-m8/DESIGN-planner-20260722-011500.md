## DESIGN — step3-relock-dag-m8: m-8 lane-2 B/E producer delta authored; requesting your final-byte pair review (governed additive delta over frozen r12, no in-place edit)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m8
PARENT_DISPATCH_ID: step3-relock-dag-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m8
IN_REPLY_TO: master/relays/step3-relock-dag-m8/RELEASE-orchestrator-planner-20260722-004000.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-8-provider-adapters

m-8.implementer — the master's addressed release landed (`RELEASE-…-004000`, over the rev2 dispatch `1166ac33…`). We are the **producer-first root** for the two lane-2 evidence digests. I authored the delta as a **governed additive delta under F73 over our FROZEN r12 `4b670a79…` — no in-place edit** (r12's bytes are unmoved). Requesting your adversarial final-byte review.

**REVIEW TARGET (byte-bound):** `master/domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md` @ SHA-256 **`322b4b8554afda4e87d1dc832a3bafe0b18b9c81b9ebffc68389dd0007b5b17c`**. Review exactly those bytes; a byte change voids the request.

### What the delta says (so you can attack it, not reconstruct it)
- **Item B — `frozen_core_digest` (F5, producer confirmation, NOT a join).** The recipe is UNCHANGED from r12 §2.1 (lowercase-hex SHA-256 over the JCS bytes of `frozen_core`); in r12 it was internal-only (the mutation guard). The delta **emits** that same value on **both** surfaces — the DATA-P terminal (m-9→E0) and the CTRL-C `attempt_result` (m-10→`provider_attempts`→E3). My artifact is my producer confirmation; m-3 authors the sink; I co-sign no join.
- **Item E — `provider_lowered_tools_digest` (F4, INDEPENDENT root).** NEW digest = lowercase-hex SHA-256 over the **JCS-canonical serialization of the lowered `tools` value** (the `tools[]` array as we lowered it in `translate()`, r12 §5.2). Rides alongside B on both surfaces. Independent — does NOT flow through m-9; m-3 joins it with m-9's `logical_surface_digest` into `model_surface_digest`. Empty-tools ⇒ canonical `[]` digest, always present.

### The claims I most want you to try to break
1. **JCS-over-value vs byte-slice (E).** I defined `provider_lowered_tools_digest` as JCS over the *logical* lowered `tools` value (re-canonicalized), NOT a byte-substring of the wire body — so an observer parsing the wire `tools` derives the same digest regardless of the wire body's incidental serialization. Verify this is (a) actually observer-derivable, and (b) not in conflict with `frozen_core_digest`'s byte-exact `body_sha256` basis (they measure different things — is that stated cleanly, or does it read as a contradiction?).
2. **Both-surfaces carriage.** I claim m-10 is never on DATA-P, so the digests MUST ride the CTRL-C `attempt_result` for m-10 to persist them, AND the DATA-P terminal for m-9 to carry B into E0. Check that against our frozen r12 §1.2 terminal + §1.4 `attempt_result{attempt_id,turn_epoch,disposition}` — is adding two hex fields to each a clean additive delta, and is m-10's confirmation the right owner for the `attempt_result` field-add?
3. **Freeze-vs-send population (B).** I say the digest exists for every attempt that reached freeze (freeze precedes authorize), but observer-derivation applies only to attempts that SENT. Check the pre-send-rejected case reads honestly (record-only, no E3 wire leg) and does not overclaim.
4. **Boundary/secret absence.** Both digests are computed at freeze from bytes we already hold; the auth header attaches AFTER freeze and is never in `frozen_core` or the tools slice. Confirm no credential byte and no payload can reach either digest field (§D + fixture 6).
5. **No new computation smuggled into B.** B must add zero new computation (emit-only). Confirm §A.2 does not silently redefine the r12 §2.1 value.

Verify at source: recompute the target SHA before reviewing; check my r12 §2.1/§2.2/§5.2 citations against the frozen r12 bytes; check the amendment §5-B/§5-E/§6 citations against rev12 `1125b0a0…`. If any cross-owner claim (m-9 carriage, m-10 row, m-3 join) misreads their actual message set, flag it — that is exactly the class of lapse the last four rounds caught.

Claims:
- The B/E delta is a pure additive delta over frozen r12; r12's bytes are unmoved — evidence E1 — source: r12 hash `4b670a79…` unchanged on disk; the delta lives in a separate addendum file.
- No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action performed — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — the addendum design doc + this DESIGN relay + one INDEX.md row; no frozen byte moved, no `frank/` action, no lock issued, no gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched (separate repo, not acted on).
Next requested action: m-8.implementer returns an adversarial final-byte review (APPROVE byte-bound, or MUST-REVISE with findings); on APPROVE I return the byte-bound design + F73 producer-confirmation responses + a SITREP to master.
