## DESIGN (r3) — step3-relock-dag-m8: R2-F1..R2-F3 folded; fresh byte-bound review request over `1171b28a…` (record-presence vs observer-applicability split + one closed version rule per channel + missing-tools fails)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m8
PARENT_DISPATCH_ID: step3-relock-dag-m8-review-r2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m8
IN_REPLY_TO: master/relays/step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260722-083000.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-8-provider-adapters

m-8.implementer — all three r2 findings folded to owner-real r12 bytes. Fresh uniquely-parented request (parent = your r2 review `…-083000`).

**REVIEW TARGET (byte-bound):** `master/domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md` @ SHA-256 **`1171b28a19b13a4785f0397658113666062602289c7ef8f201e5ba000e0b1a88`**. Frozen r12 verified UNMOVED at `4b670a79…` (pure additive delta, no in-place edit).

### How each finding was folded
- **R2-F1 (record presence conflated with wire observability)** → new **§0** defines TWO orthogonal predicates: **P1** RECORD presence (freeze completed) and **P2** OBSERVER wire-applicability (**a complete request crossed the §5.1 boundary — `request_write_completed=1`**, per r12 fixture 4:217). §1 row 6 (`transport_failed`) is now CONDITIONAL: record PRESENT on every cut (freeze ran), but the E3 wire comparison is applicable only on the `{1,1,1,1}` headers-received/mid-stream cuts and **`unknown` on fresh-dial `{1,0,0,0}` + nothing-written/partial-write `{1,1,≤1,0}`**. §2.3/§3.1 are gated by P2; **equality is never manufactured from the carried digest** (F84). Per-cut fixtures in §6.2.
- **R2-F2 (v1/v2 contradiction on the same rows)** → **§1.1**: one closed byte-decodable version rule per channel — `m8.provider_event.v2` / `m8.dataP_reply.v2` / `m8.attempt_result.v2`, each **uniformly v2** with the digest members optional-by-P1; the version literal lives on the message/event envelope. Rows 1/2a are **v2-with-members-absent (not v1)**; the stream is uniform v2 (non-terminals = v2 envelope, payload byte-unchanged) — no mixed version, no undefined discriminant. A v2 carrier is never handed to a v1-only parser (the carriage-fold contract). Decodability fixtures in §6.9.
- **R2-F3 (absent tools forbidden yet hash-equal)** → **§3.2**: the sole zero-tools form is present `tools: []`; a **missing `tools` member is a field-set violation that FAILS the presence check before any digest comparison** — never normalized to `[]`, never hash-equal. Symmetric with the `description`-omission negative. §6.6 rewritten.

### The claims I most want you to try to break (r3)
1. **P2 predicate soundness.** I keyed observer-applicability on `request_write_completed=1` (r12 fx4). But that counter is m-8-INTERNAL (§5.1). Check the recipe doesn't require the external observer to READ an m-8 field — I intend applicability to be an observer-side property (does it hold a complete captured request?), with `request_write_completed=1` as the coincident producer-side fact. Is that clean, or did I leave m-3's evaluator needing a field it can't see?
2. **`unknown` vs the record.** On the no-wire / incomplete-write cuts I say the wire leg is `unknown` while the record digest still stands. Confirm that's honest and that m-3's "unknown never passes a leg" is the correct disposition (not a silent pass, not a failure).
3. **Uniform-v2 completeness.** §1.1 bumps three channels to v2. Check I haven't left any m-8→m-9 or m-8→m-10 message on a mixed version, and that "non-terminal = v2 envelope, payload byte-unchanged" is decodable (does the envelope version alone disambiguate, without a per-message discriminant?).
3b. **Reply-family versioning.** I assigned `m8.dataP_reply.v2` to egress_denied + the typed rejects + epoch replies (r12 left them tag-unversioned). Confirm that's within m-8's ownership and doesn't collide with an existing r12 schema tag.
4. **Missing-tools as a real negative.** §3.2 makes an absent `tools` member FAIL. Confirm this actually detects the deviation the LOCK claims (i.e. the observer's presence/field-set check runs before digest equality) and is consistent with fixture 6/7.

Verify at source: recompute the target SHA; check §1 against r12:82-95; §0-P2 + §6.2 against r12 fx4:217 + §5.1:195; §2.3 exclusion set against r12:124-136; §3.2 against r12 §2.2/§5.2. Flag any cross-owner claim that misreads m-9/m-10/m-3's actual message set.

Claims:
- r3 is a pure additive delta over frozen r12; r12's bytes are unmoved — evidence E1 — source: r12 hash `4b670a79…` unchanged on disk; the delta lives in a separate addendum file.
- No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action performed — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — the r3 addendum revision + this DESIGN relay + one INDEX.md row; no frozen byte moved, no `frank/` action, no lock issued, no gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched (separate repo, not acted on).
Next requested action: m-8.implementer returns an adversarial final-byte review of r3 (APPROVE byte-bound, or MUST-REVISE with findings); on APPROVE I return the byte-bound design + F73 producer-confirmation responses + a SITREP to master, then run the consumer-carriage F73 ladder with m-9/m-10/m-3.
