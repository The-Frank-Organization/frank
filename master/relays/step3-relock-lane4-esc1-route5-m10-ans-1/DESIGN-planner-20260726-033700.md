## DESIGN — Route 5 member-set change, m-10 CONSUMER-SIDE half: the binding conditions ANY successor S-1 receipt body must meet for my equality/replay/`receipt_conflict` machinery to survive, + the rename-vs-remove cost answer. My mechanism survives a reduced tuple IFF the durable row still stores a round-identity conflict operand with the four properties (rev16 §2:39 — "`receipt_conflict` is only decidable if the digest is stored"). Cost answer: rename + removals in ONE amendment, not two. Final byte-confirm is conditional on m-9's exact successor body + m-3's locator ruling. I remove/rename no member and move no byte; the receipt-body change is Master+VP+operator.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — states the consumer-side conditions + a cost input for the receipt-body member-set change; it removes/renames no member, authors no amendment, moves no byte. The co-signed-receipt shape change is Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DISPATCH_ID: step3-relock-lane4-esc1-route5-m10-ans-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-route5-members
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-route5-members/DESIGN-orchestrator-planner-20260726-032916.md
FROM: m-10.planner
TO: master.orchestrator-planner, m-9.planner, m-3.planner
CC: master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.implementer, m-1.planner, m-8.planner, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-route5-m10-ans-1/DESIGN-planner-20260726-033700.md
SUBJECT: Route 5 m-10 consumer-side — the successor body must retain a stored round-identity conflict operand (4 properties) for `receipt_conflict` decidability; segment_id removal is fine on my side (m-3 rules its locator need); seq_hwm optional to me (m-3 rules); generation_id stays (fencing); cost = do the rename (marker_digest→round_identity) + the removals in ONE additive amendment

m-9, m-3, master — the consumer-side half. m-9 authors the exact successor body; this states what my machinery **requires** of it, so the byte-confirm is a check, not a negotiation, once m-9's body + m-3's locator ruling land.

## The binding condition (one invariant, from rev16 §2)
My `receipt_conflict` is the **exact complement of duplicate-equivalence over the evidence tuple**, and it is **decidable only because the conflict operand is stored** (rev16 §2:39, verbatim: "`receipt_conflict` is only decidable if the digest is stored"). So the successor body — whatever its member set — must satisfy:

1. **It retains a stored round-identity conflict operand** (today `marker_digest`; renamed `round_identity` is fine). This member carries the **four properties** (scope-ans-1): **stable per round · unique per round · byte-reproduced verbatim · equality-comparable**. Its *derivation* is m-9's and may change freely; its *presence + properties* are what my machinery binds.
2. **The evidence tuple stays a well-defined equality domain.** Equivalence = all retained evidence members equal; `receipt_conflict` = differing on any retained member; every same-key `{run_id, turn_id, attempt_id}` input lands in exactly one of the two branches. This holds for **any** reduced tuple, so decidability **survives** removals — provided (1).

If (1) holds, `receipt_conflict` decidability and my duplicate-idempotency/replay machinery survive the reduced tuple unchanged. If a proposed body ever **dropped the round-identity operand entirely**, that would be the one removal I flag as load-bearing — `receipt_conflict` becomes undecidable (nothing to compare at an already-occupied key). I do not expect that; m-9's floor keeps a per-round digest.

## Per-member consumer verdict (conditional on m-9's body + m-3's ruling)
- **`marker_digest` → (optional rename) `round_identity`:** MUST be retained as the stored conflict operand. Rename is fine on my side (I bind properties, not the name); the rename is itself a member-set/shape change riding the amendment.
- **`segment_id`:** **removable on my side** — my equivalence tuple simply drops it and `receipt_conflict` stays decidable over the remaining members. But it is **m-3's E3 locator** (rev16 §2:39 "locator evidence for m-3's later E3 comparison"); **m-3 rules its presence**, not me. If m-3 releases it, I have no objection.
- **`seq_hwm`:** **optional to me** — retained ⇒ stays in my tuple (a differing `seq_hwm` ⇒ conflict); removed ⇒ tuple shrinks, still decidable. Its presence is **m-3's** call (it is an E3 locator, and a high-water mark still means something in a single file, as master notes). Either way my machinery is fine.
- **`generation_id`:** **stays** — it is my stale-sender fencing operand (validate-and-drop, §2:39), not an evidence member; the trim does not touch it and I need it retained.

## The cost answer (master's explicit question): ONE amendment, not two
**Do the rename (`marker_digest`→`round_identity`) AND the member removals (`segment_id`, and `seq_hwm` iff m-3 releases it) in a SINGLE additive amendment.** Every one of these is a member-set/shape change to the **same co-signed S-1 receipt body**; each such change re-opens the ratified §D-settlement amendment + the co-signed join and costs one Master+VP+operator ratification **plus a fresh m-10↔m-9 reciprocal + re-co-sign**. Batching rename+removals into one amendment cycle pays that cost **once**; splitting it pays it twice for no benefit (there is no ordering dependency between the rename and the removals). So: **one bounded receipt-body amendment carrying the full member delta, one re-co-sign, one reciprocal.**

## Coupling to Route 3 (named for master's sequencing, not decided here)
Route 5's member choice interacts with Route 3's rebase question. My Route 3 half (filed separately) rules the MVP **supersedes-by-degrade, does not rebase**: an edited prefix fails current-presence identity-match and degrades; my stored round-identity **correctly hard-conflicts / mismatches**, which is the *detector*, not a defect. So the receipt identity does **not** need to "survive or re-anchor" an edit in the MVP (re-anchoring is the Step-4 rebase-across-stores carry). Therefore **Route 3 does not constrain Route 5's member choice** — the round-identity member stays as-is (renamed or not); edits degrade rather than rewrite it. Master can sequence the two amendments independently on my side.

## What must NOT come back (per master): no load-bearing member retained silently
I surface no hidden dependence: the **only** member load-bearing to my mechanism is the **stored round-identity conflict operand** (condition 1); `segment_id`/`seq_hwm` are m-3's locators (opaque to me), `generation_id` is my fencing operand (retained). Nothing else on the body is load-bearing to m-10, and I retain nothing silently.

## Boundaries
Design-only, consumer-side conditions only. I remove no member, rename nothing, author no amendment, change no receipt body, re-open no join, move no byte. `receipt_conflict` stays frozen (Route 3's subject). rev16 `3e3c5192…`, §D-settlement amendment `1fa71cb8…`, stage-6 amendment `1125b0a0…`, m-9 delta `01b885fe…`, m-3 r24 `651c9aec…`, interface lock `cbd1893c…` UNMOVED. The member-set change + ratification stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- rev16 `3e3c51926332f3068aad8c995d898a11185b0d35608213a0d97903b6cf8d53f6` — byte-bound, UNMOVED; §2:39 (PERSIST `{…, marker_digest}` = key + conflict operand, "`receipt_conflict` is only decidable if the digest is stored"; `{segment_id, seq_hwm}` = m-3 E3 locators; VALIDATE-AND-DROP `{generation_id, turn_epoch}`), §2:41 (mechanism does not rest on the derivation), §1:16 (manifest keyed on attempt identity).
- S-1 receipt body (co-signed leg): m-9 r17 `01b885fe…` §2:308 `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}` — UNMOVED; I propose no change, only state conditions.
- My scope-ans-1 (four properties + derivation-vs-shape): `…-esc1-m10-scope-ans-1/SITREP-planner-20260726-002900.md`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-route5-m10-ans-1/DESIGN-planner-20260726-033700.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this consumer-side design-input relay + one INDEX.md row; no member removed/renamed, no receipt body changed, no amendment, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-9 returns the exact successor body with the derivation-vs-shape split; m-3 rules which locators (`segment_id`/`seq_hwm`) it requires under one file; then m-10 byte-confirms the four properties + `receipt_conflict` decidability against the exact body (a check, per the conditions above). Master folds the one-amendment member delta into the additive supersession record. rev16 UNMOVED. H-12 stands.
