## DESIGN — c6-fix-m-5 planner fold: m-5-F6 + m-5-F7 applied (doc-only), m-7-F2 confirm filed, CTO-applied edits verified, m-5-F2 scope-flagged. Request implementer adversarial approve.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-5
PARENT_DISPATCH_ID: c6-fix-m-5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — doc-only re-review cleanup folds INTO the locked doc; lock invariants unchanged; batched pair review
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner
IN_REPLY_TO: c6-fix-m-5/DESIGN-orchestrator-planner-20260702-204512.md
BUNDLE_ID: c6-fix-m-5
SUBJECT: c6-fix-m-5 — 2 doc-only findings applied (m-5-F6 §7/§9 observe-invariant phasing; m-5-F7 §5 per-axis absent-defaults), m-7-F2 CQ-4b confirm filed to m-7 (`COORD-…-205849`), CTO-applied edits verified not-redone, m-5-F2 flagged out-of-scope. Request `DESIGN_REVIEW_VERDICT`.

m-5.implementer — c6 re-review cleanup folded into the locked c3 design doc (new **§16**). **DOC-ONLY: no mechanism change, no design-lock reopen** (review-driven consistency folds into the locked doc per the dispatch; GL-1..GL-6 / F1 / F2 / R2 / C2.4 unchanged). Requesting your adversarial `DESIGN_REVIEW_VERDICT`.

## Per-finding changelog
| id | tag | fix | location |
|---|---|---|---|
| **m-5-F6** | M | observe-as-send **gate mechanism** is always-on Step-1; the per-`slot_in` invariant **families** bite from **Step-2** (m-3 pipeline) — mirrors the ceiling tiering, resolves the §7-T1-vs-§9 read | §7 T1 cell `:123` + new §9 phasing bullet |
| **m-5-F7** | C | every axis MUST declare an absent-default = its most-restrictive **fail-closed floor** (`write→read_only`, `dispatch→none`, `tool→none`, `external_send→none`); record-absence = minimum grant, never escalation | §5 `:88` |
| **m-7-F2** | B (seam) | m-5 **CQ-4b confirm** filed: archetype registry = m-5-authored, m-5-stamped section under m-7's one-digest artifact; loaded at genesis (my §13 config-sourced-data carry) | `c6-fix-m-5/COORD-planner-20260702-205849.md` → m-7 |

## CTO-already-applied — VERIFIED, not redone
- **m-5-F4** status header → "LOCKED per c3-lock (VP co-sign 20260630-191315)" (`:7`). ✓
- **m-5-F5** §6 table token normalization — tokens `lower_snake_case`/consistent (`fast_cheap`, `top_tier_coding`, `strong_reasoning`, …). ✓
- **x2-claim-honesty-F5 / x3-seam-byte-integrity-F6** §12 `:198` hyphenated "sole-sender" → governance-surface egress + D5 residual, recorded in §15 survivor list (`:225`, count bumped 4→5). ✓

## m-5-F2 — FLAGGED out-of-scope (not applied)
`m-5-F2` (posture-vocab away-trigger) appears in the dispatch's **canonical-resolution guidance** but is `owner: CTO:seam`, **absent from the "Your findings" table**, absent from the "3 findings" framing, and absent from the CTO-already-applied list. posture is a **locked c3 vocabulary** and the away-mode trigger touches the m-6-owned `away_bridge`/Seam-C boundary — I did **not** re-record a locked vocab out-of-lane on an ambiguous assignment. Recorded in §16; the completion relay asks the CTO to confirm scope (or route it as a bounded seam COORD, whereupon I apply within the locked lattice + coordinate with m-6).

## Invariants held
No mechanism change; F1/F2 substance unchanged; R2 held (no m-5 field gate/model-referenceable); C2.4 held (no tag-value micro-fold); the four sanctioned by-construction claims preserved; swept confusion-resistant/D5 vocab preserved; `{accepted, rejected, held}` enum byte-untouched; no m-4 record / `ARCHITECTURE` edit; no c2/c3 reopen. m-5-F6/F7 both make **explicit what the lock already implied** (phase-tiering; monotonic-tightening fail-closed floor) — they add no new lattice, field, or predicate.

## DESIGN-REVIEW requested (adversarial)
Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`. Probe: (a) does the m-5-F6 phasing bullet actually resolve the §7-vs-§9 contradiction without weakening the observe-as-send lock, or does it introduce a new gap (e.g. what enforces the families in Step-1)? (b) is the m-5-F7 fail-closed floor set (`read_only`/`none`/`none`/`none`) correct and lattice-consistent, or does any axis have a non-bottom safe default? (c) is the m-5-F2 out-of-scope call right, or should I have applied it? (d) is the m-7-F2 CQ-4b confirm faithful to the c3 lock (no overreach into m-7's composition contract)? (e) any byte-inconsistency or sanctioned-claim regression introduced by the edits?

Not authorized / not claimed: doc-only; no mechanism change, no design-lock reopen, no m-7/ARCHITECTURE edit, no PLAN/IMPL/`pcode/`/spike, no operator decision reopened.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/c6-fix-m-5/DESIGN-planner-20260702-205849.md` — (run below)
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/c6-fix-m-5/COORD-planner-20260702-205849.md` — (run below)
- `git -C pcode status --short` — (run below)
- `git status --short` — unavailable: `fatal: not a git repository` (cwd is a docs workspace)

ACTIONS_GIT_REF: folded `master/domains/m-5-workflows-archetypes/design/2026-06-30-v3-archetype-system-design.md` (§5 `:88` absent-defaults; §7 T1 `:123` + §9 observe-invariant phasing bullet; new §16 c6 fold-log); filed `c6-fix-m-5/COORD-planner-20260702-205849.md` (m-7 CQ-4b confirm); wrote this DESIGN relay + appended `master/relays/INDEX.md`; no mechanism change, no other-domain-doc edit, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-5.implementer files the adversarial `DESIGN_REVIEW_VERDICT`; then I return the `c6-fix-m-5` completion relay (planner fold + implementer approve) to master.orchestrator-planner + operator, flagging the m-5-F2 scope question.
