## DESIGN (c6-fix-m-4 rev1 — both must-revise findings folded) — §5 FieldSpec row + header R2 status now consistent; → m-4.implementer re-review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-4
PARENT_DISPATCH_ID: c6-fix-m-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — doc-only rev1 folding the two must-revise findings; re-review requested; operator on CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-4.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
IN_REPLY_TO: c6-fix-m-4/DESIGN-REVIEW-implementer-20260702-210453.md
BUNDLE_ID: c6-fix-m-4

**Both must-revise findings accepted and folded — you were right on both; I fixed §7 but left two live surfaces
still saying the old thing.** Doc-only, no mechanism change. → m-4.implementer: re-review requested.

### rev1 fixes
1. **F4 — primary §5 FieldSpec row now matches §7.** The `routing_assignments` owner cell was still
   `seat_scoped_enum → planner / orch-planner` with no operator exception, competing with the §7 fold. Fixed: the
   cell now reads `planner / orch-planner (+ operator on template_ref-bearing template-spawn records only — §7,
   m-4-F4)`. §5 and §7 no longer disagree; the m-2 mirror stays flagged to m-2 (I did not edit it).
2. **F6 — stale live R2 status in the header removed.** The header sentence "one R2-boundary item is escalated to
   orchestrator/VP for ratification at the c2 lock" was live status text contradicting the §13.5/§2/§9 RESOLVED
   marks. Fixed to past tense: "the one R2-boundary item **was escalated to orchestrator/VP and RATIFIED at the c2
   lock** (readiness-fix-c1/c4 + the design-review reconciles)."

Both recorded in the §18 c6 fold-log rev1 note.

### Unchanged from rev0 (you confirmed these)
F1/x1 (§13.6 §2C build-carry marker), F5 (`deviation_reason_code` config-sourced value-set), F7/x3 (§10/§15 §J
force-A reconcile via `other`→A, no false A-member), F8/F9/F3 (verified CTO-applied, not redone). No mechanism change;
the four sanctioned by-construction claims + confusion-resistant/D5 vocabulary preserved; cross-domain mirrors
(m-2 §17.3, m-7 S11, CTO §J2) remain flagged to their owners.

## Verification
- `grep -nE "R2-boundary item is escalated|for ratification at the c2 lock"` — **0 hits** (stale live R2 status gone).
- `grep -nc "operator\` on \`template_ref\`-bearing template-spawn records only"` — **1** (§5 row exception present, matching §7).
- `grep -niE "held for the c2 lock|pending m-5 boot|gate_category ∈ A-set \(.human_decision_required"` — **0 hits** (rev0 reconciles still clean).
- No mechanism change; record shape unchanged; `pcode/` untouched.

ACTIONS_GIT_REF: folded design §5 `routing_assignments` owner cell + the header R2 status sentence + §18 rev1 note; wrote this relay + appended `master/relays/INDEX.md`; no ARCHITECTURE/m-2/m-7 edits (flagged), no code/`pcode`, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-4.implementer re-reviews rev1 (approve / must-revise); on approve I return the c6-fix-m-4 completion to the orchestrator. m-4.planner holds — no PLAN this phase.
