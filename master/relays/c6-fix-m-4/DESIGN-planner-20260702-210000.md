## DESIGN (c6-fix-m-4 planner fold) — 8 re-review findings applied to the Routing & Policy design-of-record; → m-4.implementer adversarial approve

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-4
PARENT_DISPATCH_ID: c6-fix-m-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — doc-only re-review cleanup folds into the locked doc; implementer adversarial approve requested; operator on CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-4.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
IN_REPLY_TO: c6-fix-m-4/DESIGN-orchestrator-planner-20260702-204509.md
BUNDLE_ID: c6-fix-m-4

c6 re-review cleanup folded into `master/domains/m-4-routing-policy/design/2026-06-29-v3-routing-policy-design.md`.
**Doc-only — no mechanism change, no design-lock reopen; lock invariants (R2, deviation gate, record shape, the four
sanctioned by-construction claims, the confusion-resistant/D5-residual vocabulary) unchanged.** → m-4.implementer:
adversarial `DESIGN-REVIEW` requested (`approve` closes the c6-fix-m-4 return).

### Per-finding changelog
| id | tag | fix applied | location |
|---|---|---|---|
| **m-4-F1 / x1-fatal-resolution-F1** | B | Added §13 item 6 — **§2C build-carry deferral marker**: (i) R2 `gate_referenceable` per-column FieldSpec bool on `chosen_model` + single-family-bucket negative fixtures; (ii) altitude-B per-row deviation grain. Non-locking, Step-1-build-gated, recorded vs the §2C ledger; notes the m-2 flag backing (i) already landed (m-4-F3). | §13 (new item 6) |
| **m-4-F4** ◆ | B | §7 — **template-spawn authoring model**: `FROM=operator` (operator-relay), `routing_assignments` seat-scope widened to admit `operator` on `template_ref`-bearing records only; **`declared_deviated` stays *declared*** (rejected the system/computed re-type — it would reintroduce the readiness-fix-c4 declared-vs-observed conflation). Propose-vs-stamp split stated. m-2 §17.3 + m-7 S11 mirror flagged to orchestrator. | §7 |
| **m-4-F5** ◆ | B | §5/§6 — `deviation_reason_code` **value-set = config-sourced enum** (default-seeded + operator-configurable + `other` fail-safe, §J2 pattern); seeded default vocabulary (`capability_gap`·`cost_budget`·`latency_budget`·`bucket_unavailable`·`operator_directive`·`experiment`·`other`). m-2 §17.3 mirror flagged. | §5 :206, §6 Stage-2 |
| **m-4-F6** | H | Status header → **LOCKED-at-c2** (+ c5/c6 fold notes); §2 box + §9 R2-boundary → **RATIFIED**; §13.3 (m-5 seam) + §13.5 (R2-boundary) → **RESOLVED** with refs. | :6, :13, §2 box, §9, §13 |
| **m-4-F7 / x3-seam-byte-integrity-F1** | H/M | §10/§15 — **§J `gate_category` byte-reconcile**: escalation stamps a **force-A `gate_category`** guaranteed by §J2's `other`→A fail-safe (no false claim of a non-existent A-member); `human_decision_required`/`routing_unavailable` re-stated as routing-outcome **triggers**, not §J2 members; explicit `routing_escalation` A-member routed as a c6 cross-domain carry (CTO §J2 + m-2/m-6 confirm). | §10 :333-350, §15 M4-1 |
| **m-4-F9** | M | **Verified already satisfied** — ARCHITECTURE §J1:99-102 already annotates the decision-⑤ typed-ODB model-name carve-out (same line as m-6-F5, CTO gov-apply). Not redone (ARCHITECTURE is CTO-owned). | ARCHITECTURE §J1 (verify) |
| **m-4-F8 (M) + m-4-F3 (B)** | — | **CTO-applied, verified not redone:** F8 late-classified KEEP (§16 :500-506); F3 R2 `chosen_model` re-anchor in m-2 §5/§12 (`gate_referenceable` flag). | §16 / m-2 |

New **§18 c6 fold-log** records all of the above in-doc.

### Cross-domain mirrors flagged to the orchestrator (m-4 records the decision; does NOT write those docs)
- **m-2 §17.3** — widen `routing_assignments` seat-scope to admit `operator` on `template_ref`-bearing records (F4); mirror the `deviation_reason_code` value-set (F5).
- **m-7 S11** — template-spawn author = operator-relay (F4).
- **CTO ARCHITECTURE §J2** — optional explicit `routing_escalation` A-member (F7 clarity carry; correctness already holds via `other`→A).

### Domain judgment calls (for your adversarial scrutiny)
1. **F4 chose `FROM=operator` over the conductor-system fill-class** specifically to keep `declared_deviated` a *declared* agent bit — the system/computed re-type would collapse the c4 declare/observe geometry the m-3 silent-deviation seam depends on. Please pressure-test that this is the right invariant to protect.
2. **F7 leans on the `other`→A fail-safe for correctness** and defers the explicit A-member as a non-locking carry — verify the force-A guarantee truly holds today with zero §J2 edit, and that I did not overclaim a §J2 member.

## Verification
- `grep -niE "held for the c2 lock|ratify at the c2 lock|pending m-5 boot|open only the r2-boundary"` — **0 hits** (stale pre-lock statuses cleared).
- `grep -nE "gate_category ∈ A-set \(.human_decision_required"` — **0 hits** (the byte-inconsistent A-member assertion is gone).
- `grep -ncE "confusion-resistant|D5 residual|by construction"` — **9** (swept c5 vocabulary + the four sanctioned by-construction claims preserved).
- Each of the 8 findings landed (changelog above); no mechanism change; record shape unchanged; `{accepted,rejected,held}` enum not touched (not an m-4 field); `pcode/` untouched.
- `relay-lint` this relay — expected OK.

ACTIONS_GIT_REF: folded design §5/§6/§7/§10/§13/§15 + status header + §2 box + §9 + new §18 c6 fold-log; verified F8/F3/F9 already applied (not redone); wrote this relay + appended `master/relays/INDEX.md`; no ARCHITECTURE/m-2/m-7 edits (flagged for their owners), no code/`pcode`, no PLAN, no mechanism change.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-4.implementer adversarial `DESIGN-REVIEW` (approve / must-revise); on approve I return the c6-fix-m-4 completion to the orchestrator with the changelog + the flagged cross-domain mirrors. m-4.planner holds — no PLAN this phase.
