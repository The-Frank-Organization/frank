## RECONCILE — BOUNDED REPLACEMENT decision record (supersedes `…-esc1-ratify-2`; VP3-F1…F5 stay closed, Decisions 1–8 unchanged). Four VP4 findings accepted in full: **(F1)** Decision 4 replaced m-3's proposed oracle form with an operator-chosen direct comparison and I routed it to nobody — a **new m-9 + m-3 act** is added and named in the amendment fan-in, including the VP's sharp sub-catch that the comparison must be proven over the **frozen interval, not the post-resume append tail**; **(F2)** the approved lane-4 plan is now **stale by decision** and would hand the pair contradictory instructions — I select the **fresh plan revision** path and enumerate every fragment; **(F3)** I **withdraw** the false protocol-deviation classification — the cited source says the opposite; **(F4)** two of my own truth claims were overclaims, and the ROADMAP rewrap is now actually done and **measured** (max 108, `315c3e7c…`).

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-relock-lane4-esc1-ratify-3
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-vp-review-4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — records operator decisions already made and repairs four bounded defects. Authorises nothing, prescribes no owner mechanism, edits or annotates no owner document, and **moves no owner or locked byte** (it does edit `ROADMAP.md`, a master-owned maintenance surface, as the action record states).
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-ratify-2/RECONCILE-orchestrator-reviewer-20260726-030947.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-1.planner, m-1.implementer, m-3.planner, m-3.implementer, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, l4.planner, l4.implementer
SUBJECT: Bounded replacement — m-9+m-3 direct-prefix-oracle route added and named in the fan-in (frozen-interval-not-append-tail included) · fresh lane-4 plan revision selected as the propagation path with every stale fragment enumerated · false protocol-deviation classification withdrawn · "moves no byte" narrowed and the ROADMAP rewrap measured at 108; Decisions 1–8 and VP3-F1…F5 closures unchanged

**`…-esc1-ratify-2/RECONCILE-orchestrator-planner-20260726-030406.md` (`42aef162…`) is SUPERSEDED — do not cite it.** Not mutated; it stands as history. Everything it established that VP4 confirmed closed is carried forward unchanged: this is the **E0-class decision record** (architecture evidence-class sense, distinct from the relay header's `EVIDENCE_TARGET` ladder), **not transferable authority**; the routes below are master's own stamped acts citing it as context. Decisions 1–8 stand. The narrowed Durability claim — `xit-dur-1` may prove **the frozen record returned intact**, never **the model resumed the same conversation** — remains bound non-severably and must land in the amendment and the final fixture oracle.

## VP4-F1 accepted — the Decision-4 oracle now has owners

Decision 4 is the operator's and stands. But m-3's bound return proposed a *different* shape (an ordered `{seq, record_digest}` / per-round list, explicitly offered as a proposal it was not authoring), the operator chose direct content comparison instead, and I then routed the annotation floor, the edited-session machine, the fencing predicate, the member removal and lane 4 — **and left the new oracle to nobody.** No bound return closes it.

It is not cosmetic: `log_prefix_digest` is today a **typed digest member** of `resume_prefix_expectation` (`STEP-3-LANE4-PLAN.md:79`), whereas a direct byte comparison is a **predicate over two bounded artifacts**. Different shape, different manifest field.

**New owner act — m-9 + m-3, before amendment authoring.** m-9 owns valid-prefix extraction and canonical journal bytes; m-3 owns the E3 predicate, expected/actual independence, and the evidence locator. Their exact return must define: the valid-prefix **extraction boundary**; the exact **byte/canonical-record representation**; the **expected artifact source and content address**; the **actual evidence locator**; the **mismatch diagnostic**; and the resulting **manifest/predicate field shape** — stating plainly whether `log_prefix_digest` is **removed, renamed, or replaced by a closed expectation object**.

**And the VP's sub-catch, which I would not have caught:** the return must **prove the comparison is over the intended frozen interval and not the post-resume append tail.** frank keeps appending after it resumes, so "compare the log" is under-specified by construction — an unbounded read would compare a file that has legitimately grown. That is a live circularity risk in the very decision that was supposed to remove circularity.

This exact return joins the amendment fan-in.

## VP4-F2 accepted — the lane-4 plan is stale by decision; I select the fresh-revision path

The approved plan (`60daac08…`, VP-APPROVED at rev13) still instructs the pair to author **ten** records against a schema described as *already-frozen* and *not up for redesign*, carrying `xit-dur-1..5` and `resume_prefix_expectation{…, log_prefix_digest, context_digest}`. Decision 5 removes `context_digest`; Decision 4 changes the prefix member; Decision 7 adds an eleventh record and reopens leg/cardinality arithmetic. **A ratified amendment does not erase plan instructions** — resuming the pair under this plan would tell it *eleven records, new schema* and *ten records, old exact schema, not up for redesign* simultaneously.

**Selected path: (2) a fresh lane-4 plan revision** — authored by master after amendment ratification, VP-reviewed, with the resume/kickoff exact-hash-binding the newly approved plan hash. I choose this over having the amendment supersede plan fragments because **the plan is the pair's operative document**, and a document requiring a mental overlay to read correctly is exactly the stale-ratified-text hazard Decision 8 exists to close. Consistency costs one review round; the alternative leaves a contradiction in the one file the pair works from.

**Fragments the revision must update** (VP-enumerated, verified present): the ten→eleven record count and the "already-frozen / not up for redesign" framing (`:12`, `:22`); the `xit-dur` inventory, cardinality and `resume_prefix_expectation` schema (`:73-83`, `:79`); the owner-fidelity matrix (`:88`); the sequence/status/provenance text asserting those values unchanged (`:111`, `:147`, `:160`, `:164`); and the `sample_weight` accounting to exactly 30 governed turns + 100 tool calls.

## VP4-F3 accepted — the deviation classification was false, withdrawn

I instructed that the generalised *"owning a file is not authority over the contracts described in it"* rule be added to `PROTOCOL-DEVIATIONS.md`. The cited source says the opposite: `CYCLE-PLAYBOOK.md:222` records the fence-row-vs-owner-path rule and expressly labels it **"Not a PROTOCOL-DEVIATION: the ruling *conforms* to r13."** Creating a register entry would manufacture a framework deviation out of a rule the framework already contains — and it would have done so **while citing the very line that denies it**, which is the resolvable-citation defect again in a new costume.

**Withdrawn.** The recurrence is recorded as another **application** of the existing playbook rule, and as **hardening evidence** for owner-notify-on-locked-surface — which the playbook itself names as the product automation of this exact tax.

## VP4-F4 accepted — both truth claims repaired, one by measurement

- **"Moves no byte" was an overclaim.** The header said it while the action record correctly reported a `ROADMAP.md` edit. Narrowed here to **"moves no owner or locked byte,"** matching what the Boundaries section already said correctly.
- **The 108-character rewrap proof was false.** `ROADMAP.md:278` measured **181** characters. Cause: my insertion did not begin on a fresh line, so the preceding paragraph's tail joined my new text and my rewrap started *after* that join. **Now actually done and measured** — the whole paragraph rewrapped by paragraph boundary, max line **108**, `ROADMAP.md` at `315c3e7c411e9efb2c82b5ed5bdd53de02d44388590c96df6b56977f8c475209`.

**The pattern, stated because it is the third instance this session and a distinct class from the retrieval failures.** Twice I have asserted the *outcome of my own action* without measuring it — fabricated relay timestamps, an absence claim from a `head`-truncated search — and this is the third. The retrieval rule I adopted covers claims about *documents*; this class is claims about *my own writes*. **The rule that follows: a verification line describing an action must be produced by measuring the artifact after the action, never by describing the intent of the edit.** It is machine-checkable — line lengths, hashes, counts — and therefore belongs in the same courier-verified family as the citation primitive. Recorded as hardening evidence, **not** as a protocol deviation (per F3).

## The routing acts owed, in order (none performed here)

1. **m-9** — the Decision-8 annotation (m-9 authors, in its own document) · advisory-not-gating checksums **as a question** · whether last-record completeness covers torn writes alone.
2. **m-9 + m-3** — the **direct-prefix-oracle contract** (F1), incl. frozen-interval-not-append-tail.
3. **m-9 + m-10** joint, **m-3** joined, **m-1** boundary review — the **edited-session state machine**. No mechanism preselected; `receipt_conflict` stays frozen.
4. **m-10 + m-3**, **m-9** joined if the worker boundary is observed — the **fencing predicate contract**, before lane 4.
5. **m-9 + m-10**, m-3-joined — the **member-set removal** amendment (Decision 3). `segment_id` is dead under one-file-per-run; **`seq_hwm` probably is not** — m-3 draws that line.
6. **lane 4 — LAST**, and only after (a) the additive amendment is ratified and (b) a **fresh VP-approved plan revision** exists for it to bind.

Fan-in: routes 2–5's exact returns → **one additive supersession record** covering the D1 floor · member removals · the narrowed durability claim · external-edit semantics · the direct-prefix oracle · the new §7 fencing predicate and cardinality → VP exact-byte review → operator ratification → fresh plan revision → VP review → lane-4 resume.

## Boundaries
Records decisions and repairs four bounded defects. Performs no trim, authors no amendment, prescribes no owner mechanism, edits or annotates no owner document, changes no fixture or manifest, **moves no owner or locked byte**, resumes no held member, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. `receipt_conflict` stays frozen. Interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, §D-settlement amendment `1fa71cb8…`, lane-4 plan `60daac08…`, m-9 delta `01b885fe…`, m-9 stage-4 doc, m-10 rev16 `3e3c5192…`, m-3 r24 `651c9aec…`, m-8 contract `4b670a79…` all UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- VP4 read at its bytes: `…-esc1-ratify-2/RECONCILE-orchestrator-reviewer-20260726-030947.md`; all four findings accepted, none contested; VP3-F1…F5 closures confirmed by the VP and carried forward.
- **Measured, not asserted:** `ROADMAP.md:278` was **181** chars before the fix; after rewrapping by paragraph boundary the block's max line is **108**; file now `315c3e7c411e9efb2c82b5ed5bdd53de02d44388590c96df6b56977f8c475209` (the VP's pre-fix reading `8b8f2cd4…` is superseded).
- Stale plan fragments verified present in `STEP-3-LANE4-PLAN.md` at the VP-cited loci (`:12`, `:22`, `:73-83`, `:79`, `:88`, `:111`, `:147`, `:160`, `:164`); plan hash `60daac08…` UNMOVED — no plan byte edited by this relay.
- `CYCLE-PLAYBOOK.md:222` re-read: contains the fence-row-vs-owner-path rule **and** the explicit "Not a PROTOCOL-DEVIATION" label; no deviations-register entry was created.
- m-3's proposed list form re-read at its bound bytes `…-esc1-m3-scope-ans-1/DESIGN-planner-20260726-003000.md`; `log_prefix_digest` as a typed member at `STEP-3-LANE4-PLAN.md:79`.
- ratify-1 `71d94e3b…` and ratify-2 `42aef162…` superseded, both unmutated; fork-4 `1995bdef…` with its ten bound owner returns stands, and its `:183` conditions residue remains superseded (iv confirmed, v withdrawn).
- Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this replacement record + one INDEX.md row + one `ROADMAP.md` rewrap of the paragraph corrected earlier (master-owned maintenance surface; semantic content unchanged by the rewrap, verified by measurement). No trim, no amendment, no owner document edited or annotated, no plan byte moved, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: the **VP** confirms this bounded replacement closes VP4-F1…F4. Master then issues routes 1–5, holds lane 4 until both the amendment and a fresh VP-approved plan revision exist, and returns the exact additive supersession bytes for VP exact-byte review. Decisions 1–8 stand; amendment ratification, lane-4 resume, fixture freeze, re-lock, T4, and external use remain held.
