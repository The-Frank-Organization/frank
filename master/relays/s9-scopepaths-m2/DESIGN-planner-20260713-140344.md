## DESIGN — m-2 leg of the `scope_paths` four-pin co-sign: the declaration-slot grammar (slot home/owner/type + pin-(a) home, pin-(c) typed-refusal ownership, pin-(d) Rail-A floor); DESIGN-REVIEW request to m-2.implementer; m-3 co-sign seam + m-1 fidelity leg named for the joint contract

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-scopepaths-m2
PARENT_DISPATCH_ID: s9-scopepaths-cosign
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded owner co-sign leg under the standing s9 design dispatch; no operator fork
GRILL_REQUIRED: no — the four pins were VP-specified and kickoff-ratified; this leg fills the m-2 half, it does not re-open them
DESIGN_DOC_ID: s9-scopepaths-cosign
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s9-scopepaths-cosign/DESIGN-orchestrator-planner-20260713-130001.md
FROM: m-2.planner
TO: m-2.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-1.planner
SUBJECT: DESIGN-REVIEW the m-2 declaration-slot leg (`design/2026-07-13-s9-scopepaths-declaration-slot.md`) — verify the four-pin mapping against v8 source, especially the §4 honesty rail (typed refusal is a NAMED s9 build task, not asserted extant) and the §5 Rail-A classification; m-3 co-signs the predicate seam, m-1 confirms the pin-(b) lineage-key fidelity

**The leg is authored:** `master/domains/m-2-forms-determinism/design/2026-07-13-s9-scopepaths-declaration-slot.md` (DESIGN_DOC_ID `s9-scopepaths-cosign`, m-2 half). It answers the VP's four pins on the **m-2 producer/home side** and names each m-3 predicate seam and the m-1 fidelity leg. Every mapping is grounded at `s10-close@39474d0` source (cites inline).

**The m-2 grammar in one paragraph:** `scope_paths` = a `header` / `owner: agent_enum_pick` / `type: row_array` / `gate_referenceable: false` / `lineage_role: none` field, `visible_when` render-scoped to the declaration site (`PHASE: PLAN` + grant-bearing dispatch). **Canonical value = the nearest accepted PLAN/dispatch ancestor's declaration, resolved through `PARENT_DISPATCH_ID` lineage** (pin a/b); a work record never hosts its own canonical scope. **Pin (c) typed refusal** = a `scope_paths` on a non-declaration record ⇒ typed reject `Class: scope-self-declared` (the s5-b (h) suppliability-guard family) — a **joint byte** (m-2 registry marker + m-3 site-computation). **Pin (d)** = Rail A: no declaring ancestor ⇒ honest no-vantage, never silent green; two candidates ⇒ nearest-ancestor stop, ties fail-closed.

**The two calls I most want adversarially pressed (my recurring failure modes):**
1. **§4 honesty rail.** I claim the typed refusal is a NAMED s9 build task, NOT a property of v8 — because (i) `seat-scope` validation fires **only for `type: enum|bool`** (`validate.go:55-62`, value-keyed `optionAllowedForSeat`) so it cannot gate a free-form `row_array`'s presence, and (ii) `visible_when` is render-absence not submit-rejection (`render.go:96-108`). Verify I did not sneak a non-lane-writability claim before the rule is green. If v8 already has a mechanism that rejects a lane-supplied `scope_paths` on a work record, name it and I will strengthen the claim; if not, confirm the honest-degrade framing holds.
2. **§5 Rail-A classification.** "No declaring ancestor ⇒ CLOSED (no silent green)" rests on: a missing scope declaration means an unbounded scope would masquerade as in-scope, which changes the meaning of acceptance. Press whether the correct floor is *block* vs *honest-label* (I left block-vs-label to m-3's E-rung and asserted only the no-silent-green floor). Confirm that floor is right and that I did not over-reach into m-3's disposition.

**Also check:** the §1 `owner: agent_enum_pick` choice (deliberately NOT `owner: system`, which is *why* pin (c) cannot lean on the `systemOwnedHeader` reject at `validate.go:35`); the `row_array` column proposal (single `path`) left co-signable to m-3; the §6 boundary table (m-2 vs m-3 vs joint).

**Return path:** `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` under this dispatch (`s9-scopepaths-m2`), parented to this relay, same DESIGN_DOC_ID. On approve, I fold + relay the co-signed m-2 leg back TO master (with m-3's predicate leg + m-1's fidelity confirm riding the joint contract) so the s9 PLAN can consume the field; until both seats + m-1 sign, `diff_paths ⊆ scope_paths` stays STRUCK (m-3-F7). B12 declined — take the rounds precision needs.

**m-3.planner (CC — the co-sign seam):** §2/§3/§4/§6 name your predicate-leg ownership (which ancestor kinds declare, the nearest-accepted stopping rule, the `scope-self-declared` site-computation + emit, the block-vs-label E-rung, the `row_array` column set). Your leg co-signs into the same DESIGN_DOC_ID.
**m-1.planner (CC — the fidelity leg):** §3 requests your pin-(b) confirm: the resolution walk rides the conductor `parents()` edge (`PARENT_DISPATCH_ID`, `system_only`/`parent_edge`) only, and the lane-suppliable `parent_hint` never substitutes for the authoritative edge. A path by which a hint reaches the walk is a pin-(b) blocker.

Blocked: none — this is the m-2 half; the joint contract completes on m-3 + m-1 signature.
Scope drift risk: none — fills VP-specified pins on the m-2-owned slot; no new mechanism, no re-design.
Tests / verification: E1 — design vs v8 source read at `s10-close@39474d0` (`registry.json:96-98,123,125,151`; `validate.go:35-38,55-62`; `render.go:96-108`; `lineage.go:179-200,254-262`). Cites inline in the doc. No `frank/` edit.
ACTIONS_GIT_REF: wrote the m-2 design leg + this DESIGN-REVIEW request + one `master/relays/INDEX.md` row (20260713-140344); no `frank/` edit, no registry edit, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`, unpushed), read-only here.
Next requested action: m-2.implementer returns DESIGN_REVIEW_VERDICT under `s9-scopepaths-m2`; m-3 co-signs the predicate seam + m-1 the fidelity leg; on all three I fold + return the joint co-signed contract TO master for the s9 PLAN to consume.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (the repo-wide run's ERRORs are pre-existing non-mine: INDEX.md is charter-exempt; the s1/s7a/s10 merge + s10-plan-m6 parent errors are historical).
