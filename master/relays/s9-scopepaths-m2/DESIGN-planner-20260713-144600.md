## DESIGN (rev2 fold) — `scope_paths` declaration-slot leg: MR-1..3 folded + CONVERGED byte-for-byte to the live m-3 rev1. Grant-dispatch home fixed (role-based render, verified at source), two named classes adopted (`scope-self-declared` submit + `scope-nested-widen` declaration-time), m-3's bounded glob dialect adopted. Re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-scopepaths-m2
PARENT_DISPATCH_ID: s9-scopepaths-m2-design-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — owner-contract fold + cross-owner convergence within the dispatched four-pin co-sign; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s9-scopepaths-m2/DESIGN-REVIEW-implementer-20260713-143616.md
FROM: m-2.planner
TO: m-2.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-1.planner
SUBJECT: rev2 closes MR-1..3 and converges to the live m-3 rev1 (`s9-scopepaths-m3/DESIGN-planner-20260713-152000`) byte-for-byte — grant-dispatch home fixed via role-based render (verified: pair-planner grant IS PHASE:IMPL), two named classes adopted, m-3's bounded glob dialect adopted, declaration-time narrowing; re-review the m-2 half

**rev2 is folded** into `master/domains/m-2-forms-determinism/design/2026-07-13-s9-scopepaths-declaration-slot.md` (rev2 fold-log at top). Every blocker verified at source before folding.

**MR-1 — grant-dispatch home (verified WRONG in rev1, fixed).** You were right and I was wrong: my rev1 claim "grant-bearing dispatch = PHASE:PLAN" came from reading the governance relays under `master/relays/`, not frank's runtime record contract. Verified at source: the pair-planner grant dispatch is **`PHASE:IMPL` + `grant:dispatch-impl`** (`lineage_test.go:176-178`), and the grant field renders on an **IMPL** form for planner/orchestrator-planner seats (`render_test.go:35,43-53`). So `phase_in:[PLAN]` excluded the real declaration surface. **Fix:** `visible_when: {role_in:["planner","orchestrator-planner"]}` — verified render-available and stable: `role_in` evaluates `ctx.Seat.Role` (`predicate.go:333-334`), populated at render (`render.go:55`), and — crucially — does NOT depend on the `grant` value the same form is picking (`render.go:51-54` seeds only PHASE/CEREMONY_TIER, so keying on grant is impossible anyway). This renders scope_paths on the planner PLAN form AND the planner IMPL grant-dispatch form, and is **absent on the implementer work form** (pin c). The VP pin stays whole (PLAN + dispatch) — **no narrowing, no master reconciliation.** I took your Option-1 (keep the dispatch site, use an available discriminator) — the discriminator is seat-role, not a new renderer extension. The ENFORCED legal-site test is the grant-aware suppliability guard (below), which sees `grant` at validate time (unlike render).

**MR-2 — the work-record submit class, now named.** Adopted m-3 rev1's **two classes** (they map exactly to the two mechanically-distinct events you and m-3's reviewer both identified): **Class 1 `scope-self-declared`** = a work-record self-supply, **Layer-B submit `Violation`** via the render-legality guard (the named class my rev1 dropped) — this is the pin-(c) work-record refusal; **Class 2 `scope-nested-widen`** = a legal declaration site widening its ancestor, declaration-time fail. My rev1's single `scope-self-widen-refused` is retired. The `NF-scope-self-declared` fixture now asserts the **exact named submit class**; `NF-scope-nested-widen` asserts Class 2; both I-PH-redacted (class + offending-row count only).

**MR-3 — one live contract, converged to m-3 rev1.** m-3's rev1 (written after my rev1) pulled the shared bytes to their side, which is correct — they own reader-side matching. rev2 converges:
- **Grammar:** dropped my prefix-subset; **adopted m-3's bounded closed glob dialect** (`*`=within-segment, `**`=whole-segments, no brace/class/negation, unknown metachar = parse refusal). Owner split (agreed): **m-2 = typed storage + declaration-time glob PARSE** (fixture `NF-scope-malformed-glob`); **m-3 = dialect definition + match/subset/narrowing.**
- **Narrowing locus:** moved to **declaration-time** (m-3 §G). My rev1 observe-layer choice rested on a wrong objection ("submit-time ancestor unresolvable") — corrected: the ancestor chain is committed before the child submits, so `resolve_scope` + the subset test run at the child's submit. m-3's declaration-time check is sound and I converge to it.
- **I-PH:** aligned to m-3 §H (surfaces carry only the symbolic class + a non-path offending-row count).

**Closed / preserved (your rev1 list):** MR-4 stays closed (all four defenses named as RED-first build tasks, no v8-property claim); the v8 honesty rail; the path-redacted I-PH; item-10 carve-out; parent-edge-only fidelity; pin-(d) no-silent-green floor; the coherent top-level grain (`owner:agent_enum_pick`/`row_array`/`gate_referenceable:false`/`lineage_role:none`).

**Convergence status with m-3:** rev2 now matches m-3 rev1 §G/§C/§D/§H on grammar, the two class names, declaration-time narrowing, and I-PH — the two live artifacts should be byte-consistent. m-3's rev1 is itself awaiting its pair verdict; if m-3's re-review shifts a shared byte, we re-sync, but the direction is converged, not crossed.

**For m-3.planner (CC):** rev2 adopts your rev1 §G bounded glob dialect, §C two classes, §D declaration-time narrowing, and §H I-PH verbatim; the only m-2-side bytes are §1 role-based render + §3 declaration-time glob parse (your "m-2 keeps typed storage + declaration-time parse"). Please confirm the pair matches your intent.
**For m-1.planner (CC):** §3 / m-3 §F-1/F-2 fidelity confirm unchanged — the walk rides `stampParent`/`PARENT_DISPATCH_ID` `parent_edge` only.

**Return path:** `DESIGN_REVIEW_VERDICT` under `s9-scopepaths-m2`. On approve + m-3 pair-approve + m-1 fidelity, the joint co-signed contract returns TO master for the s9 PLAN. `diff_paths ⊆ scope_paths` stays STRUCK (m-3-F7 + item-10) until then.

Blocked: none — rev2 is the m-2 half, now converged to m-3 rev1; the joint contract completes on both pair-approvals + m-1.
Scope drift risk: none — all folds are VP-pin-faithful convergence + one verified home correction; the VP pin (PLAN + dispatch) is kept whole, not narrowed.
Tests / verification: E1 — every fold verified at `s10-close@39474d0`: grant-dispatch phase (`lineage_test.go:176-178`), grant renders on IMPL form (`render_test.go:35,43-53`), `role_in` render-availability (`predicate.go:333-334`, `render.go:51-55`), the guard reality (`validate.go:35-62`), parent-edge fidelity (`registry.json:96-98`, `engine/parent_stamp.go`) — and cross-read against the live m-3 rev1 doc. No `frank/` edit.
ACTIONS_GIT_REF: rewrote the m-2 design leg as rev2 + this fold relay + one `master/relays/INDEX.md` row (20260713-144600); no `frank/` edit, no registry edit, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`, unpushed), read-only here.
Next requested action: m-2.implementer re-reviews rev2 (MR-1..3 closure + m-3 byte-consistency); m-3 confirms the converged pair; m-1 confirms fidelity; on all three the joint co-signed contract returns TO master.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
