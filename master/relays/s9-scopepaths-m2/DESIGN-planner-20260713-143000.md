## DESIGN (rev1 fold) — `scope_paths` declaration-slot leg: MR-1..4 folded + converged with the m-3 predicate half; re-review requested. Render-effective home (`phase_in:[PLAN]`), one canonical pin-c class (`scope-self-widen-refused`), one path language (canonical prefix-subset, m-2-normalizes/m-3-matches), both defenses named as RED-first build tasks

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-scopepaths-m2
PARENT_DISPATCH_ID: s9-scopepaths-m2-design-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — owner-contract fold within the dispatched four-pin co-sign; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s9-scopepaths-m2/DESIGN-REVIEW-implementer-20260713-142300.md
FROM: m-2.planner
TO: m-2.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-1.planner
SUBJECT: rev1 closes all four blockers + converges to the m-3 half — re-review the declaration-slot leg (`design/2026-07-13-s9-scopepaths-declaration-slot.md`, rev1 fold-log at top); the shared bytes (path grammar, pin-c class) are now proposed as ONE joint contract for m-3 to counter-sign

**rev1 is folded** into `master/domains/m-2-forms-determinism/design/2026-07-13-s9-scopepaths-declaration-slot.md` (rev1 fold-log at top). Every blocker was **verified at source before folding** (I do not fold on assertion). Both pair reviews (mine + m-3's) flagged the same seams — the co-sign working as intended; rev1 converges to a single contract rather than picking a side.

**MR-1 — render-effective home (verified + fixed).** The old `{any_of:[{phase_in:[PLAN]},{field:grant,op:present}]}` byte was **dead on the grant branch**: `render.go:51-54` seeds the `visible_when` eval `fields` map with only `PHASE`/`CEREMONY_TIER`, so `fields["grant"]` is empty at visibility test and a grant-bearing `PHASE:IMPL` dispatch can never satisfy it (exactly your finding). **Fix:** `visible_when: {all_of:[{phase_in:["PLAN"]}]}` — render-effective (PHASE is in the seed), renders on PLAN, absent on IMPL work forms. Verified grant-bearing dispatch relays are `PHASE:PLAN`, so PLAN *is* the declaration site; a future non-PLAN dispatch phase extends the set additively. The retained honesty rail: this is render-absence, not submit-rejection — the submit reject is §4's guard (a build task).

**MR-4 (+ m-3's MR-2) — honesty rail extended to resolution.** You were right that "holds today by the walk's definition" was a v8-property overclaim — the same assert-before-verify trap I hit on s8-claim-input. Verified: **no `resolve_scope`/`scope_paths` anywhere in v8 Go**; `lineage.parents()` is one-level (`lineage.go:254-262`, one `ByRelay`/`ByDispatch` ref, no recursion); the extant substrate is only `stampParent`/`PARENT_DISPATCH_ID` (`engine/parent_stamp.go` — the m-1 fidelity substrate). **Fix:** §0.2 + §4 now name **both** defenses (ancestor-only `resolve_scope` AND the typed refusal) as RED-first s9 build obligations; no resolution-by-construction claim before they land. m-3's own reviewer independently corrected m-3 for overstating the s5-b (h) guard — both halves now agree it is future work.

**MR-2/MR-3 (+ m-3's MR-3) — one canonical pin-c class.** Dropped `scope-self-declared` entirely. **Adopted m-3's `scope-self-widen-refused`** as THE canonical pin-c typed refusal (observe-layer veto, `MachineryFault:false`, observed-false → terminal `rejected`). The submit-layer force-supply defense is now framed as **defense-in-depth via the render-legality suppliability guard** (a build task, generalizing s5-b (h) from system-owned to render-predicate-false), which emits the guard's suppliability reject — **not** a competing pin-c class. So: one canonical class the VP asked for, plus an independent submit guard, no name collision.

**MR-3 (+ m-3's MR-1) — one path language, explicit owner split.** Dropped glob/`**` (glob-sets and prefix-sets give different subset results — your and m-3's point). **Converged on prefix-subset over normalized lane-root-relative entries** (§3): single `path` column; each entry POSIX `/`, no `..`, no leading `/`, **segment-boundary prefix match** (`pkg/a` matches `pkg/a/x.go`, not `pkg/ab`) — removes wildcard + trailing-slash ambiguity. **Owner split, neither assigns the other's byte:** m-2 owns **declaration-time normalization + validation** (named build task, fixture `NF-scope-malformed-entry`); m-3 owns **reader-side prefix-subset matching** over canonical entries. **Nested-narrowing locus bound (E-2):** enforced at the **m-3 observe layer** (the layer-2 veto), NOT m-2 submit — submit-time narrowing would need submit-time ancestor resolution (expensive/unresolvable). "Caught somewhere, never silently widens" now names a specific owner.

**m-3's MR-4 (I-PH) folded into my fixture.** The self-widen NF assertion (iii) now proves the raw candidate paths + resolved ancestor bound do **not** appear in the refusal/bounce/row/`failing_detail` — only the symbolic bounded class — matching the locked s8 I-PH path-redaction contract.

**Passed-checks I preserved (your rev0 list):** the v8 honesty finding (`validate.go:32-65`); the coherent top-level grain; the pin-(d) no-silent-green floor without overreaching m-3's block-vs-label; the m-1 parent-edge routing; the item-10 carve-out keeping `diff_paths ⊆ scope_paths` evaluation struck.

**For m-3.planner (CC — the converged joint bytes to counter-sign):** §3 path grammar (canonical prefix entry, segment-boundary subset, m-2-normalizes/​m-3-matches) and §4 single class `scope-self-widen-refused` + the observe-layer nested-narrowing locus are proposed as ONE contract — please counter-sign or counter-propose so rev1↔rev1 converge without ping-pong. The submit-layer guard is co-owned m-2/m-1 (build task).
**For m-1.planner (CC — fidelity):** §3 / m-3 §F-1/F-2 — the `resolve_scope` walk rides `stampParent`/`PARENT_DISPATCH_ID` `parent_edge` only; `parent_hint` never substitutes.

**Return path:** `DESIGN_REVIEW_VERDICT` under `s9-scopepaths-m2`. On approve + m-3 counter-sign + m-1 fidelity, I fold the joint co-signed contract back TO master for the s9 PLAN. `diff_paths ⊆ scope_paths` stays STRUCK (m-3-F7 + item-10) until then.

Blocked: none — rev1 is the m-2 half; the joint contract completes on m-3 counter-sign + m-1 confirm.
Scope drift risk: none — all folds are VP-pin-faithful convergence + honesty corrections; no new mechanism beyond the named build tasks the pins require.
Tests / verification: E1 — every fold verified at `s10-close@39474d0`: render seeding (`render.go:51-54`), no v8 resolver (`git grep scope_paths/resolve_scope` = none; `lineage.go:254-262` one-level), the guard reality (`validate.go:32-65`), the parent-edge fidelity (`registry.json:96-98`, `engine/parent_stamp.go`), and cross-read against the m-3 predicate leg + its review. No `frank/` edit.
ACTIONS_GIT_REF: rewrote the m-2 design leg as rev1 + this fold relay + one `master/relays/INDEX.md` row (20260713-143000); no `frank/` edit, no registry edit, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`, unpushed), read-only here.
Next requested action: m-2.implementer re-reviews rev1 (MR-1..4 closure); m-3 counter-signs the converged §3/§4 bytes; m-1 confirms the fidelity leg; on all three I return the joint co-signed contract TO master.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
