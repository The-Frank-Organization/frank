## PLAN — s7b delta implementation dispatch under the delegated authority: all four conditions verified (the master grant's `DELEGATED_DISPATCH_AUTHORITY: yes` renewed in `PLAN-…-234637` · your `s7b-plan-delta-review` approve · mechanical `SCOPE_DIFF_RESULT: all-in`, no hard trigger · this relay parents to that review's unique ID); the token stands bare below

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s7b-impl-dispatch-2
PARENT_DISPATCH_ID: s7b-plan-delta-review
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the delegated conditions are met; m-1 fidelity, VP integration, and the operator merge gate (with `HUMAN_MERGE_AUTHORIZATION` at grant time) remain downstream
GRILL_REQUIRED: no
DESIGN_RECORD_KIND: audit-record
PLAN_LOCK_ID: s7b-close-once-plan-delta
IN_REPLY_TO: master/relays/s7b-dispatch/PLAN-REVIEW-implementer-20260710-235301.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: build the delta on the preserved `s7b-close-once@5c678b4` exactly per `s7b-close-once-plan-delta` (`PLAN-planner-…-234943`) + your approving review (`PLAN-REVIEW-…-235301`) under the master grant (`PLAN-orchestrator-planner-…-234637`); report as `s7b-impl-2` TO me for the pair review

DISPATCH IMPL

m-7.implementer — the delegated conditions are satisfied and verified at my seat: (1) master's follow-up grant carries `DELEGATED_DISPATCH_AUTHORITY: yes`; (2) your `s7b-plan-delta-review` returned `PLAN_REVIEW_VERDICT: approve`; (3) its mechanical diff is `all-in` with no hard trigger and no unresolved operator decision; (4) this dispatch parents to that review's unique ID, whose parent is my delta PLAN addressed to you.

SCOPE_DIFF:
- internal/crashpoint/crashpoint.go -> in
- test/invariants/path_hygiene_test.go -> in
- test/invariants/testmain_test.go -> in
- test/fixtures/main_assembly_test.go -> in
SCOPE_DIFF_RESULT: all-in

**Binding shape notes carried from your review:** the crashpoint change is the exact granted block-after-kill (`select {}` after the SIGKILL — no new `syscall.Kill`-failure contract, current platform assumptions preserved); the invariants child short-circuit lives in the granted owner file (`testmain_test.go`), not a fifth surface; the fixtures short-circuit covers exactly the four censused markers, final set listed in your report. **Acceptance = the unchanged r2 bar whole at the delta tip + the delta's focused proofs** (the crashpoint consumers · the hardened `TestLawPathHygiene` · both race stations), all file-captured, sequence-honest. Any additional child marker, harness failure, wider path, acceptance change, or pin stops and reports.

Not authorized / not claimed beyond the token: no merge, tag, push, fidelity, or integration authority; the return path runs your `s7b-impl-2` report → my `s7b-impl-2-review` → master → m-1 fidelity → VP integration → the operator merge gate (which lifts only `OI-S7A-CLOSE-ONCE-RACE` as the live-channel blocker).

ACTIONS_GIT_REF: none — authority grant only; the delta commits are yours from here (disk refs: this relay + one INDEX.md row timestamped 20260711-002534)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean at `2e1b4f0`; the s7b worktree preserved at `5c678b4`, untracked `.relays/s7b/` only)
Next requested action: operator carries this to the m-7.implementer session; the delta builds on `5c678b4`; your `s7b-impl-2` report returns TO me.
