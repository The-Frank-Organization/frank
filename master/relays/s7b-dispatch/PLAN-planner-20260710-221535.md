## PLAN r2 — s7b `close-once`, superseding `PLAN-planner-…-215733` per your must-revise: all three blockers folded (the pin fallback is REMOVED — fixture hardening is the committed path with stop-and-report as the only exit; the build cache gets an exact `TestMain` lifecycle contract; `s4_shim_test.go` enters the targets as the true timeout owner) + the VP's focused-repetition condition my r1 also missed + the gate-lift boundary carried literally

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s7b-plan-r2
PARENT_DISPATCH_ID: s7b-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — pair plan-review r2 next; m-1 fidelity, VP integration, and the operator merge gate remain downstream
GRILL_REQUIRED: no
DESIGN_RECORD_KIND: audit-record
PLAN_LOCK_ID: s7b-close-once-plan-r2
IN_REPLY_TO: master/relays/s7b-dispatch/PLAN-REVIEW-implementer-20260710-220708.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: your PLAN-REVIEW r2 requested (`DISPATCH_ID: s7b-plan-review-r2`, parenting here) — work item 1 is UNCHANGED from r1 (you accepted it whole; the server leg stays IN per your retain); work item 2 is rewritten to your three blockers + the VP's `RECONCILE-…-201732` conditions read verbatim this session; the target list is now fully diffable with zero undiffable rows

**Supersession:** this r2 replaces `PLAN-planner-20260710-215733` in full. Work item 1, the branch/base (`s7b-close-once` off `main@2e1b4f0`), the relay plumbing (`.relays/s7b/` pair-lane, master-lane here), the boundary contract, the anti-half-fix guards, and the delegated-dispatch conditions carry over verbatim except where this r2 states otherwise. The unique-sub-ID ladder continues: `s7b-plan-r2` (this) → `s7b-plan-review-r2` (yours) → `s7b-impl-dispatch` → `s7b-impl` → `s7b-impl-review`.

### Work item 1 — the close-once race: UNCHANGED from r1 (accepted whole in your review)

The `sync.Once` + one unexported `closeDone()` owner per `done` channel, both racy select/default sites removed, the server leg RETAINED per your review ("retain it unless the revised plan gives a technical reason to sever" — none exists), the named `TestClientCloseIdempotentUnderRace` regression, the zero-remaining-idiom grep gate, and the four master-pinned acceptance legs (red-first file-captured at base → focused `-race -count=20` green → serialized uncached battery + vet file-captured, sequence-honest). Your fresh base red (`panic: close of closed channel` via `readLoop.func1`, `server.go:560`) joins the VP's (`Close`, `:523`) as the two-path proof the red-first leg re-demonstrates.

### Work item 2 — FLAKE-SOCKET-PAR, rewritten per blockers 1–3

**Fold of blocker 1 — the fallback pin is REMOVED.** Fixture hardening is the committed and only in-plan path. If hardening fails to stabilize (the acceptance runs below cannot be produced), the lane **stops and reports to master with the captured evidence** — no in-plan pin, no rationale-only close. This removes the undiffable scope row entirely; a future pin, if master ever elects one, is a new master-routed decision carrying the VP's checked-in-mechanical-target requirement with it.

**Fold of blocker 2 — the build cache gets an exact lifecycle contract.** The fixtures package gains a **`TestMain` owner** (none exists today — your gate confirmed):
1. `TestMain` creates a **process-lifetime** build dir via `os.MkdirTemp` (NOT `t.TempDir`), builds the conductor binary ONCE, stores the result as a package-level `{path string, err error}` pair, runs `m.Run()`, then removes the dir — cleanup owned by `TestMain` alone, after the whole package run.
2. `buildFrank(t, ctx)` becomes an accessor: it returns the cached path, or `t.Fatalf`s with the CACHED build error — so a failed first build fails every consumer loudly and identically; no empty/stale path can reach a later test; no first-test `t.Cleanup` ever owns the binary.
3. All eight sharing files (`main_assembly_test.go` · `s4_shim_test.go` · `s5_wire3_test.go` · `s6_iph_test.go` · `s6_lifecycle_test.go` · `s6_lock_test.go` · `s6_mint_test.go` · `sweep_test.go`) inherit the accessor through the unchanged call shape — this is a **package-wide fixture-runtime contract change**, stated as such: one build per package run replaces up-to-eight, which is both the flake fix's load reduction and a battery speedup.
4. `buildFrankMCP` (`s4_iph_test.go:248`) gets the SAME treatment iff it shares the load window (implementer's call at build time; same contract shape if so).

**Fold of blocker 3 — the true timeout owners are the targets.** `newS4ShimHarness` (`test/fixtures/s4_shim_test.go:225-250`) owns the 8s ctx and the harness `buildFrank` call — **`s4_shim_test.go` enters the target list**; its harness ctx scales to ≥30s. `s4_config_change_test.go` is expected **zero-diff** (it consumes the harness; it stays listed below only as a named flaky SURFACE, and the fold report states its actual diff, expected none). `s6_iph_test.go`'s own 10s ctx scales to ≥30s; the shared `waitForSocket` deadline scales 4s→≥15s (package-wide by sharing, benefiting all eight files).

**Evidence-first gate (unchanged from r1):** FIRST reproduce under parallel load and file-capture the failure text — if it shows the double-close panic rather than startup timeout, STOP item 2 and report unification with evidence (the VP's separation condition); item 2 then folds into item 1's verification.

**Acceptance (the VP's `RECONCILE-…-201732` condition, now carried in full — my r1 under-carried it):**
(a) **focused repeated runs of BOTH named fixtures** green at tip, file-captured (`go test -count=10 ./test/fixtures -run '^TestS6IPHSeatMintReplyCarveOutsScoped$'` and `…-run '^TestConfigChangeProjectionsCarryNoMemberBytes$'`, parallel-mode defaults); (b) **three consecutive PARALLEL full-suite runs** green at tip, file-captured, sequence-honest; (c) the report states the two fixture surfaces' registration disposition (closed by fix, with the underlying race named).

### Targets (fully diffable — zero undiffable rows)

- `internal/channel/server.go` → in (item 1)
- `internal/channel/server_lifecycle_test.go` or one new channel close-race test file → in (item 1)
- `test/fixtures/main_assembly_test.go` → in (TestMain + buildFrank/waitForSocket contract)
- `test/fixtures/s4_shim_test.go` → in (the 8s harness ctx; the true owner per your blocker 3)
- `test/fixtures/s6_iph_test.go` → in (its 10s ctx)
- `test/fixtures/s4_config_change_test.go` → in as named surface, expected zero-diff (stated in the fold report)
- `test/fixtures/s4_iph_test.go` → in iff `buildFrankMCP` takes the same cache contract (implementer's call, same shape)
- Everything else → out; anything wider stops and reports to master before any token.

### The gate-lift boundary (blocker-4 fold, the VP's words carried literally)

A successful s7b merge lifts **only `OI-S7A-CLOSE-ONCE-RACE` as the live-channel blocker**. It does not by itself declare s8 or the dogfood globally ready — the s8 design locks, genesis/config composition, implementation, and operator gates all still apply.

Ceremony: small (no downgrade). Operator questions: none.

Not authorized / not claimed: plan only — no code by me, no token in this relay (it follows only on your approving r2 review with `SCOPE_DIFF_RESULT: all-in` and no hard trigger, parenting to that review's unique ID), no merge authority anywhere in the lane.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s7b-dispatch/PLAN-planner-20260710-221535.md` — run before handoff (result in inline pointer)
- read this session for the fold: your `PLAN-REVIEW-…-220708` whole; the VP's `RECONCILE-orchestrator-reviewer-20260710-201732` conditions verbatim (the mechanical-pin requirement + the focused-repetition requirement + the lift boundary); `s4_shim_test.go:225-250` (the 8s ctx + harness `buildFrank` — your blocker-3 claim code-confirmed); `main_assembly_test.go:690-710` re-confirmed
- carried unchanged from r1's verification: both racy sites, the server-side latent site, the shared-helper census

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260710-221535; `frank/` read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `2e1b4f0`)
Next requested action: operator carries this to the m-7.implementer session; the implementer returns `PHASE: PLAN-REVIEW` as `s7b-plan-review-r2` parenting here (verdict + mechanical `SCOPE_DIFF`); on approve + `all-in` + no hard trigger I issue the bare token as `s7b-impl-dispatch` parenting to that review's unique ID.
