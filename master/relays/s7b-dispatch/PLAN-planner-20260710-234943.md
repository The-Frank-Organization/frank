## PLAN — the s7b delta under the granted follow-up scope: block-after-kill in the crashpoint (2 lines), the `test/invariants` hardening contract (cached build + ≥30s ctx + ≥15s socket wait + its OWN child-mode short-circuit — the pivot-law children re-exec through the new TestMain too), and the fixtures `TestMain` short-circuit over the four enumerated child markers; then the UNCHANGED r2 acceptance re-runs at the new tip

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s7b-plan-delta
PARENT_DISPATCH_ID: s7b-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — pair plan-review next; the unchanged downstream gates stand (m-1 fidelity, VP integration, operator merge)
GRILL_REQUIRED: no
DESIGN_RECORD_KIND: audit-record
PLAN_LOCK_ID: s7b-close-once-plan-delta
IN_REPLY_TO: master/relays/s7b-dispatch/PLAN-orchestrator-planner-20260710-234637.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: your PLAN-REVIEW requested (`DISPATCH_ID: s7b-plan-delta-review`, parenting here) — the delta builds ON `s7b-close-once@5c678b4` (items 1+2 stay as committed); one child-marker discovery beyond the grant text is folded into the spec (the invariants package's OWN TestMain needs the same short-circuit for `FRANK_S7_PIVOT_CHILD`, or the pivot law's children rebuild the conductor per re-exec — the exact amplification class the grant kills)

**Base:** the delta commits land on `s7b-close-once` at `5c678b4` (preserved per master's grant; the close-once fix and the fixtures hardening stay as committed). **Ladder:** `s7b-plan-delta` (this) → `s7b-plan-delta-review` (yours) → `s7b-impl-dispatch-2` (my token) → `s7b-impl-2` (your report) → `s7b-impl-2-review` (my pair review).

### Leg 1 — `internal/crashpoint/crashpoint.go`: block-after-kill

In `Hit`, the firing branch becomes:
```go
if count == nth {
    _ = syscall.Kill(os.Getpid(), syscall.SIGKILL)
    select {}
}
```
The crashing goroutine can never execute past the crashpoint; the process state at SIGKILL delivery is byte-exactly the state at the Hit, at any load. **No API, name list, counting, or trace change** — the diff is the `select {}` line (plus nothing). No red-first is owed on this leg (the race needs load to fire; master accepted the mechanism record `s7b-blocker-assess` as the evidence and the fix as strictly correctness-improving); the proof obligation is instead: **the whole existing crashpoint-consuming battery stays green** (the F10 family, the s2 crash-window fixtures, the s7 pivot law — their children must still die at their crashpoints exactly as before).

### Leg 2 — `test/invariants` hardening (the FLAKE class's third member)

Mirror the fixtures contract in the invariants package:
1. A **new small build-cache owner** (`test/invariants/testmain_test.go` or equivalent): `TestMain` with a process-lifetime `os.MkdirTemp` dir, ONE cached `{path, err}` build of `cmd/frank`, cleanup after `m.Run()`; `buildFrankBinary(t, ctx)` becomes the accessor (cached path, or `t.Fatalf` with the cached error) — same failure semantics as the fixtures contract.
2. **The invariants TestMain short-circuits in child mode:** when `FRANK_S7_PIVOT_CHILD=1`, skip the build and run `m.Run()` directly — `TestLawOnePivotPerMutation`'s children re-exec the test binary, and without the skip each child would pay the build the grant exists to remove.
3. `test/invariants/path_hygiene_test.go`: the live-capture ctx **20s → ≥30s**; `waitForSocketPath` **5s → ≥15s**. Nothing else in the file moves (the discovery/census/carve-out machinery is byte-untouched — it is s7-reviewed surface).

### Leg 3 — `test/fixtures/main_assembly_test.go`: the child-mode short-circuit

`TestMain` skips both binary builds (and the cache-dir creation) when any of the package's **four enumerated child markers** is `"1"`: `FRANK_F10_CHILD` · `FRANK_F11_CHILD` · `FRANK_F11_TRACE_CHILD` · `FRANK_F9_CHILD` (enumerated by grep at `5c678b4` this session; the report lists the final set it ships). Child processes then run their child branch without paying two `go build`s each. The accessor's behavior in a skipped-build child: any test-function caller fails loudly with a "child-mode: no cached binary" error — children only ever run their env-guarded branches, which never call the accessor; the loud error is the tripwire if that assumption ever breaks.

### Acceptance (the r2 bar UNCHANGED, re-run whole at the new tip, plus the delta's own proof)

1. The r2 bar exactly: focused `-count=10` of both named fixtures · **three consecutive PARALLEL full-suite runs** · the full serialized uncached battery + vet — all file-captured, sequence-honest.
2. Item-1's two race stations re-run green at the new tip (`-race -count=20`, channel + MCP).
3. The delta's own proof: focused `-count=10` green on `TestF10AtomicWriteCrashWindows`, the s2 crash-window family, and `TestLawOnePivotPerMutation` (the crashpoint consumers), plus `TestLawPathHygiene` focused `-count=10` (the hardened third member).

### Fence (the grant's, verbatim + the one folded discovery)

`internal/crashpoint/crashpoint.go` (block-after-kill only) · `test/invariants/path_hygiene_test.go` + the new small build-cache owner file · `test/fixtures/main_assembly_test.go`. The invariants short-circuit lives in the new owner file (in-fence as "its small build-cache owner"). Anything else — including any further harness surprise in the parallel runs — stops and reports, as before.

Ceremony: small. Operator questions: none.

Not authorized / not claimed: plan only — no code by me, no token here (it follows on your approving review with `SCOPE_DIFF_RESULT: all-in`, no hard trigger, as `s7b-impl-dispatch-2` parenting to that review), no acceptance change (master pinned it), no merge authority.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s7b-dispatch/PLAN-planner-20260710-234943.md` — run before handoff (result in inline pointer)
- code grounding at `5c678b4` this session: `crashpoint.go` `Hit`/`target` whole, `fsio.go:39-40` (the one-syscall window), the child-marker census (grep, both packages: F10/F11/F11_TRACE/F9 in fixtures; S7_PIVOT in invariants), `path_hygiene_test.go`'s 20s ctx + 5s wait + per-run build, the fixtures `TestMain`/accessor shape (`main_assembly_test.go:32-60`)
- master's grant read whole; the delta stays inside its three named surfaces + the owner file

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260710-234943)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s7b worktree preserved at `5c678b4`, untracked `.relays/s7b/` only)
Next requested action: operator carries this to the m-7.implementer session; the implementer returns `s7b-plan-delta-review` (verdict + mechanical `SCOPE_DIFF`); on approve + `all-in` + no hard trigger I issue `s7b-impl-dispatch-2` and the delta builds on the preserved branch.
