## PLAN — s7b implementation dispatch under the delegated authority: all four conditions verified satisfied (the master grant `DELEGATED_DISPATCH_AUTHORITY: yes` · your r2 approve · mechanical `SCOPE_DIFF_RESULT: all-in`, no hard trigger · this relay parents to your approving review's unique ID); the token stands bare below

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s7b-impl-dispatch
PARENT_DISPATCH_ID: s7b-plan-review-r2
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the delegated-dispatch conditions of `s7b-dispatch/PLAN-orchestrator-planner-20260710-193159.md` are met in full; m-1 fidelity, VP integration, and the operator merge gate (with `HUMAN_MERGE_AUTHORIZATION` at grant time) remain downstream
GRILL_REQUIRED: no
DESIGN_RECORD_KIND: audit-record
PLAN_LOCK_ID: s7b-close-once-plan-r2
IN_REPLY_TO: master/relays/s7b-dispatch/PLAN-REVIEW-implementer-20260710-221859.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: build to the r2 plan exactly — binding docs: `PLAN-planner-…-221535` (the r2 plan, `s7b-close-once-plan-r2`) + your approving `PLAN-REVIEW-…-221859` (all-in scope) + the master grant `PLAN-…-193159` + the VP conditions `RECONCILE-…-201732`; branch `s7b-close-once` off `main@2e1b4f0`; pair relays in `.relays/s7b/`; report as `s7b-impl` TO me for the adversarial pair review

DISPATCH IMPL

m-7.implementer — the delegated conditions are satisfied and verified at my seat this session: (1) the master s7b grant carries `DELEGATED_DISPATCH_AUTHORITY: yes`; (2) your `s7b-plan-review-r2` returned `PLAN_REVIEW_VERDICT: approve`; (3) its mechanical `SCOPE_DIFF_RESULT: all-in` with no hard trigger and no unresolved operator decision; (4) this dispatch parents to that review's unique ID, whose parent is my r2 PLAN addressed to you. Any deviation from here re-engages master rather than improvising — your own review's stop conditions (parallel failure unifying with the close panic · hardening failing to stabilize · any wider path) bind verbatim.

**Scope = your all-in diff exactly, restated mechanically** (the two channel-test rows as alternatives; the zero-diff named surface needs no gratuitous edit):

Row qualifiers per your review: the two channel-test rows are alternatives (touch one, not necessarily both); `s4_config_change_test.go` is the named acceptance surface with expected zero diff; `s4_iph_test.go` is in iff `buildFrankMCP` takes the same cache contract.

SCOPE_DIFF:
- internal/channel/server.go -> in
- internal/channel/server_lifecycle_test.go -> in
- internal/channel/new_close_race_test.go -> in
- test/fixtures/main_assembly_test.go -> in
- test/fixtures/s4_shim_test.go -> in
- test/fixtures/s6_iph_test.go -> in
- test/fixtures/s4_config_change_test.go -> in
- test/fixtures/s4_iph_test.go -> in
SCOPE_DIFF_RESULT: all-in

**Acceptance = the r2 plan's two chains exactly** (the four master-pinned race legs incl. red-first at base; the VP's two-fixture focused repetition + three consecutive parallel full-suite runs, all file-captured, sequence-honest). **Fences = the r2 plan's** (no registry, engine, store, or seat-surface change; stop-and-report beyond them).

Not authorized / not claimed by this relay beyond the token: no merge, no tag, no push, no fidelity or integration verdict; the return path runs your `s7b-impl` report → my `s7b-impl-review` → master → m-1 fidelity (close-ordering vs the auth window, scoped) → VP integration → the operator merge gate.

ACTIONS_GIT_REF: none — authority grant only; the branch/commits are yours from here (disk refs: this relay + one INDEX.md row timestamped 20260710-223025)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `2e1b4f0`)
Next requested action: operator carries this to the m-7.implementer session; the build begins at `2e1b4f0` per the r2 plan; your IMPL report returns as `s7b-impl` TO me.
