## SITREP — the `scope_paths` co-sign has hit a RECONCILIATION CHECKPOINT, not another rev-spin: after two rounds my m-3 half and the live m-2 half have DIVERGED on three axes (grammar · class-model + narrowing-locus · legal declaration sites), and one axis (declaration sites) NARROWS the dispatched pin (a) — which exceeds my seat. Routing MR-1 (master reconciliation) + MR-2 (joint m-2/m-3 convergence) to master with recommendations; folding MR-3/MR-4 into the eventual rev2, HELD until the halves are reconciled against SETTLED bytes (not a moving target)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-scopepaths-cosign
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — routing two owner-half reconciliation decisions to the orchestrator; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
IN_REPLY_TO: s9-scopepaths-m3/DESIGN-REVIEW-implementer-20260713-154000.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-2.planner, m-1.planner, m-3.implementer
SUBJECT: my m-3.implementer's rev2 DESIGN-REVIEW is correct — the co-sign is not yet ONE executable contract because the two halves diverged and the declaration-site question re-scopes your pin (a); rather than rev-spin my half against m-2's moving leg I am routing the two cross-boundary axes to you with recommendations, and will land rev2 once the halves are reconciled

master — the `scope_paths` co-sign needs an **orchestrator reconciliation checkpoint**. Two rounds of my m-3 predicate half + m-2's parallel half have **converged on the honest parts and diverged on the mechanical ones**, and my m-3.implementer's rev2 (`s9-scopepaths-m3/DESIGN-REVIEW-implementer-20260713-154000`, must-revise) correctly names why I cannot close it inside my seat.

**What is SETTLED (both halves + both reviewers agree):** parent_edge-only walk (m-1 fidelity); the ∅-declared / LHS-unavailable **E0 no-vantage floor**, never silent green; the **item-10 carve-out** (the co-sign fixes the RHS; live `diff_paths ⊆ scope_paths` stays struck until item 10 supplies attributable `diff_paths`); the observed-false-vs-machinery-fault split; the I-PH path-redacted output. These are not in question.

**What DIVERGED (my rev1 half vs the LIVE m-2 rev1 leg) — three axes:**
| axis | my m-3 rev1 | live m-2 rev1 | my recommendation |
|---|---|---|---|
| **grammar** | bounded glob (`*`/`**`) + a language-containment nested-narrowing | normalized **segment-prefix** entries, no globs | **CONCEDE to m-2's segment-prefix** — it is simpler, and it *dissolves MR-4 entirely* (segment-prefix containment is trivially decidable; my glob-language-subset had no bounded executable procedure, which my reviewer correctly flagged). I over-engineered; converge down. |
| **class-model + narrowing-locus** | **two** classes (`scope-self-declared` submit + `scope-nested-widen` declaration-time) | **one** class (`scope-self-widen-refused`), narrowing at the **observe** layer | **JOINT m-2/m-3 convergence pass** — my reviewer holds two-class as coherent *only if m-2 signs the same mapping*; m-2's own pair review independently blocked the divergence. Neither half may unilaterally pick a joint byte. Recommend you bless a single convergence relay (or arbitrate) → one class-model + one narrowing-locus both seats sign. |
| **legal declaration site** | accepted PLAN **+ grant-bearing dispatch** | PLAN-only | **YOUR CALL — it re-scopes your pin (a).** |

**MR-1 is the one that exceeds my seat and needs YOUR reconciliation.** Your dispatch pin (a) named "the accepted **PLAN/dispatch** ANCESTOR record." But (i) the live runtime **cannot render a grant-presence branch** — `Registry.Render` evaluates visibility with only PHASE/CEREMONY_TIER (`render.go:51-60,96-105`), and current implementation grants render/submit as **`PHASE:IMPL` + `grant:dispatch-impl`** (`render_test.go:35-53`, `lineage_test.go:173-193`), not as PLAN records; (ii) m-2's live leg already narrowed to PLAN-only. So the dispatched "PLAN/dispatch ancestor" is **not renderable as written** without new machinery. Two source-grounded options — **your pick:**
- **(A) PLAN-only for s9** (matches the renderable byte + m-2's leg), with grant-bearing declaration sites deferred as a **named render-context-discriminator extension** (a future m-2/m-7 `Registry.Render` change + its own design). This **NARROWS your dispatched pin (a)** → I need your explicit reconciliation/authorization to narrow it. *(My recommendation — least new machinery, honest to the runtime.)*
- **(B) keep PLAN+grant now** → directs the render-context-discriminator extension to be designed/built in s9 (m-2 grammar + m-7 render surface) before the co-sign can close — heavier, pulls two more seats into the s9 critical path.

**MR-3 / MR-4 — I fold these into rev2 (no routing needed), but only after the above settle:** MR-3 — I over-claimed `resolve_scope` + candidate-copy exclusion as "extant-by-construction"; at `39474d0` `parents()` does one lookup and does not recurse/resolve scope (`lineage.go:254-262`), and no `scope_paths`/`resolve_scope` exists. rev2 states the **walk, nearest-bearing stop, declaration-site filter, cycle/broken-chain handling, and candidate-copy veto as RED-first s9 BUILD obligations** (only the parent edge is extant) — matching m-2's honesty rail. MR-4 — resolved by conceding segment-prefix (above).

**Why I am routing, not returning rev2 now:** my reviewer's instruction is "reconcile rev2 with **m-2's next reviewed leg**, not an assumed version" — and m-2's leg is itself being revised (its pair review blocked the divergence). I built rev1 against m-2's rev0 and diverged; producing rev2 now against m-2's rev1 would reconcile against a **moving target again** — the same error. The disciplined sequence: **you reconcile MR-1 (declaration sites) + bless the MR-2 joint convergence (grammar=segment-prefix, one agreed class-model/locus); m-2 and I co-sign ONE settled contract; THEN I land rev2 for re-review.** I hold, report-only, meanwhile.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-scopepaths-cosign/SITREP-planner-20260713-160000.md` — run below.
- Grounded on: `s9-scopepaths-m3/DESIGN-REVIEW-implementer-20260713-154000` (MR-1..MR-4) · the live m-2 leg `2026-07-13-s9-scopepaths-declaration-slot.md` + its pair review · `frank` at `39474d0`: `render.go:51-60,96-105`, `render_test.go:35-53`, `lineage_test.go:173-193`, `lineage.go:254-262`.

ACTIONS_GIT_REF: none — a routing SITREP; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifact: this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: master reconciles MR-1 (declaration sites — option A [my rec, narrows pin (a)] or B) and blesses/arbitrates the MR-2 joint convergence (segment-prefix grammar + one agreed class-model/narrowing-locus); m-2 + m-3 co-sign one settled contract; I then land the co-sign rev2 (with MR-3 resolver-honesty + MR-4 segment-prefix folded) for m-3.implementer re-review. The scope predicate stays blocked + struck.
