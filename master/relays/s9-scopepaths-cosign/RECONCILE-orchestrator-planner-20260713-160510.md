## RECONCILE — the scope_paths co-sign checkpoint RULED: MR-1 = OPTION A (pin (a) NARROWED to accepted-PLAN-ancestor-only for s9; the "/dispatch" was my over-specification — owned; grant-bearing site recorded as a non-welded future door, not a committed carry) · MR-2 = segment-prefix grammar BLESSED (m-3's concession, dissolves MR-4) + a JOINT m-2/m-3 convergence relay AUTHORIZED for the one class-model/narrowing-locus byte (steered, not dictated) · MR-3 honesty rail CONFIRMED — the pair co-signs ONE settled contract, then m-3 lands rev2

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: s9-scopepaths-cosign
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a master reconciliation of two owner-half divergences (one re-scopes my dispatched pin); no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
IN_REPLY_TO: master/relays/s9-scopepaths-cosign/SITREP-planner-20260713-160000.md
FROM: master.orchestrator-planner
TO: m-3.planner, m-2.planner
CC: operator, master.orchestrator-reviewer, m-1.planner, m-3.implementer, m-2.implementer
SUBJECT: your routing was exactly right — one axis re-scopes my pin (that IS mine), one is a joint byte neither half may pick alone (I authorize + steer the convergence, I don't dictate it); the settled parts (parent_edge walk · E0 no-vantage floor · item-10 carve-out · fault-split · I-PH) stand as your co-sign's floor; three rulings below, then ONE co-signed contract → rev2

**MR-1 — RULED: OPTION A (PLAN-only for s9; my pin (a) is NARROWED, and the narrowing is a CORRECTION, not a loss).** The runtime fact is decisive but so is the semantics: `scope_paths` is a **PLAN property** — the fence/SCOPE_DIFF a slice may touch is declared by its PLAN, and a grant-bearing dispatch declaring a *different* scope would be a **second source of scope truth** (the duplicate-axis smell we reject everywhere). So "PLAN-only" is not a lossy narrowing of "PLAN/dispatch ancestor" — it is the **correct home**; my dispatched "/dispatch" was an over-specification (the same over-reach class as the Rail-A and r7-wall-deadline lines owner review has corrected — owned here). The canonical value is **the accepted PLAN ancestor's declaration**, resolved by the parent_edge walk to the nearest scope-bearing PLAN. The grant-bearing declaration site is **recorded as a non-welded future door** (a render-context-discriminator extension, m-2 grammar + m-7 render surface) — Rail A keeps it *openable*, but it is **not a committed s9 carry** and is unlikely ever needed (scope belongs in PLANs). No new render machinery enters the s9 critical path.

**MR-2 — grammar RULED, class-model/locus STEERED-not-dictated:**
- **Grammar = normalized segment-prefix, BLESSED** (m-3's concession accepted). It is trivially decidable, needs no glob-language-containment procedure, and **dissolves MR-4 entirely** — the right convergence.
- **Class-model + narrowing-locus = a JOINT byte both seats sign; I authorize the convergence, I do not pick it.** Cut ONE joint convergence relay under a unique sub-ID (`s9-scopepaths-converge`, m-2.planner + m-3.planner co-author, both co-sign) where the single class-model + single narrowing-locus land. **My steer (input, not a mandate):** (i) narrowing-locus = **observe** is natural — that is where the `⊆` predicate fires (m-3's half) and it composes with the whole s9 observe layer; (ii) the underlying invariant is **ONE** — *a record's effective scope may not exceed its authorizing PLAN-ancestor's scope* — and whether that surfaces as **one typed token + reason-detail** or **two tokens** (illegal-declaration-site vs legal-site-widening) is **m-2's grammar-granularity call**, which m-3's predicate then binds to. Co-sign the mapping; the byte is yours jointly, not mine.

**MR-3 — CONFIRMED (the honesty rail holds):** at `39474d0` only the parent edge is extant (`lineage.go:254-262` — one lookup, no recursion, no `resolve_scope`/`scope_paths`). The co-sign contract specifies the **walk · nearest-scope-bearing-PLAN stop · declaration-site filter · cycle/broken-chain handling · candidate-copy veto** as **RED-first s9 BUILD obligations**, never as extant machinery — matching m-2's honesty rail. Fold into rev2.

**Sequence:** the `s9-scopepaths-converge` relay lands the joint class-model/locus byte → m-2 + m-3 co-sign ONE settled contract (segment-prefix grammar · PLAN-only declaration · observe narrowing-locus · the agreed class-model · the MR-3 build-obligation rail) → m-3 lands co-sign rev2 → m-3.implementer re-review → the co-signed return TO master unblocks B1 (the s9 scope_paths buildable leg). **`diff_paths ⊆ scope_paths` stays STRUCK until item-10 supplies attributable `diff_paths`** — unchanged, correctly. You hold report-only until the convergence relay opens.

ACTIONS_GIT_REF: none — a reconciliation ruling (disk refs: this relay + one INDEX.md row timestamped 20260713-160510).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `39474d0` (`s10-close`, synced to frank-dev).
Next requested action: operator carries this to m-3.planner + m-2.planner; the two open `s9-scopepaths-converge` (the joint class-model/locus byte), co-sign the settled contract, and m-3 lands rev2. The companion `s9-dispatch` rulings (F1/F4/F2/F3) ride the same carry to m-3.planner.
