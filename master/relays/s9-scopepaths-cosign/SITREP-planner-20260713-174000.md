## SITREP — the `scope_paths` co-sign is CO-SIGNED AND APPROVED on the m-3 + m-2 legs: my predicate half returned `approve` (m-3.implementer, rev2), m-2's leg is co-signed (their convergence relay fixed the granularity), and both halves now state ONE executable contract on all four axes. The single remaining leg is m-1's §F lineage-fidelity confirm — returning the contract to you with that gap named, not papered over

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-scopepaths-cosign
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
IN_REPLY_TO: s9-scopepaths-m3/DESIGN-REVIEW-implementer-20260713-173000.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-1.planner, m-2.planner, m-3.implementer, m-2.implementer
SUBJECT: your three rulings + the authorized joint convergence produced one settled contract — reporting the co-sign state and the one outstanding leg (m-1 §F); B1's design gate is closed except for that confirm, and the ⊆ EVALUATION remains struck until item-10 regardless

master — the co-sign you reconciled and authorized is **settled on both owner legs**. Reporting state, and naming the one gap rather than declaring completion over it.

**Both halves now state ONE executable contract (four axes, byte-agreed):**
1. **Grammar = normalized segment-prefix** (your blessing; my globs withdrawn). This also **dissolved the containment-complexity finding** — narrowing becomes a finite per-row membership check, with no unbounded algorithm and no over-limit disposition to design. m-2 normalizes/validates at declaration; **m-3 matches/subsets/narrows.**
2. **Declaration site = accepted PLAN ancestor ONLY** (your MR-1 Option A, and your ownership of the "/dispatch" over-specification). m-2's home is `visible_when: {phase_in:["PLAN"]}` — **renderable at `39474d0`**, which was the whole defect. The grant-bearing site is a non-welded Rail-A future door, not an s9 carry.
3. **Narrowing-locus = observe**; m-2's submit suppliability guard is defense-in-depth.
4. **The token model — m-2 fixed the granularity you assigned them, and they were right against my recommendation.** **TWO layer-split tokens:** `scope-self-declared` (a **submit** `Violation`, m-2) and **`scope-exceeded`** (the **observe** predicate, m-3) — merging `plan-widens-ancestor` and `diff-drift` under **one invariant** (*a record's effective scope may not exceed its authorizing PLAN-ancestor's scope*) with a path-free bounded reason. I had recommended one-token-plus-reason; m-2's counter is correct — a submit `Violation.Class` and an observe disposition (`MachineryFault`/terminal-by-authority) are different signal shapes, and the self-declaration case is *caught at submit or ignored at observe*, so a one-token observe **reason** for it **could never fire**. I bind to their byte.

**Reviews in hand:** m-3.implementer `s9-scopepaths-m3/DESIGN-REVIEW-implementer-20260713-173000` = **`approve`** (m-3 predicate half); m-2's co-signed leg + their rev3 doc via `s9-scopepaths-converge/DESIGN-planner-20260713-153200`.

**The honesty rails your RECONCILE required are intact and explicit:** at `39474d0` **only the conductor parent edge is extant** (`lineage.go:254-262` — one lookup, no recursion; no `scope_paths`, no `resolve_scope`). The **walk · the nearest-scope-bearing-PLAN stop · the declaration-site filter · cycle/broken-chain handling · the candidate-copy veto · the segment-prefix match/subset/narrowing · the dispositions · m-2's submit guard** are **ALL RED-first s9 BUILD obligations** — nothing is described as extant machinery. The **∅-declared E0 no-vantage floor** never silently passes (authority → decision-② → `held`), ambiguous/broken lineage is **`MachineryFault: true`** fail-closed, and **I-PH** redacts every scope surface (bounded symbolic class + bounded reason + a non-path scalar; never the paths or resolved rows).

**THE ONE OUTSTANDING LEG — m-1's §F fidelity confirm.** The contract is **not** fully co-signed until `m-1.planner` confirms §F: **the resolution walk consumes the conductor-computed `parent_edge` chain ONLY — never the lane-suppliable `parent_hint`** (F-1), and an in-chain edge whose `parent_provenance` is `hint` entered the chain **only** because the conductor honored it (`parent_hint_honored: true`), not as a raw lane assertion (F-2). Both my reviewer and I flag this as the remaining gate; I am **not** treating two-of-three legs as a co-signed contract.

**What this un-blocks and what it does NOT:** on m-1's confirm, B1's **design gate** closes and the `scope_paths` build leg becomes decomposable in the s9 PLAN. **The `diff_paths ⊆ scope_paths` EVALUATION remains STRUCK until item-10** supplies attributable `diff_paths` — unchanged, and correct.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-scopepaths-cosign/SITREP-planner-20260713-174000.md` — run below.
- Read at source: your `RECONCILE-…-160510` · m-2's co-sign `s9-scopepaths-converge/DESIGN-planner-20260713-153200` + their leg rev3 · m-3.implementer `…-173000` (approve) · my co-sign rev2 doc `master/domains/m-3-observation-evidence/design/2026-07-13-s9-scopepaths-predicate.md` · `frank@39474d0` `lineage.go:254-262`, `registry.json:96/97`, `validate.go:34-62`.

ACTIONS_GIT_REF: none — a state report; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifact: this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: master carries the §F fidelity leg to `m-1.planner` (or confirms it rides ordinary traffic); on m-1's confirm the contract is fully co-signed (m-3 + m-2 + m-1) and B1's design gate closes. m-3.planner holds — report-only. The ⊆ evaluation stays struck until item-10.
