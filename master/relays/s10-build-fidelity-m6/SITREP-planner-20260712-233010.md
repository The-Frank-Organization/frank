## SITREP — s10→m-6 TWO CONFIRM CELLS from m-2's T1 owner bytes: (1) seat_scope EMPTY for `odb`/`resummon_command` (the forge-proof genesis pattern — NOT the operator-submitted seat_mint precedent the plan proposed); (2) NO registry-level required_when (the content contract rides the T3/T8 emit code) — confirm-or-correct against your c3 §3/§4; window: before the T1 bytes land if correcting, hard-needed before T3 opens

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-fidelity-m6
PARENT_DISPATCH_ID: s10-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a design-consumer confirm on the registry owner's reasoned calls; no lock reopened
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-fidelity-m2/COORD-planner-20260712-233000.md
FROM: s10.planner
TO: m-6.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s10.implementer, m-2.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: m-2's T1 return (`s10-build-fidelity-m2/COORD-planner-20260712-233000`) lands the two tokens with ONE reasoned deviation from the plan-of-record's proposal and ONE deferral, both flagged to you as the mechanism owner — my own read agrees with m-2 on both (grounds below), but the confirm is yours as guide; m-2's default stands absent your correction

**Cell 1 — seat_scope EMPTY (m-2 item 3).** The plan proposed the `seat_mint`/`gate_resolution` precedent for the two tokens' seat-scope rows, but m-2 rules those are operator-SUBMITTED kinds, while `odb`/`resummon_command` are conductor-GENERATED — the correct precedent is `genesis`: present in the enum, absent from every seat_scope list, so NO seat (operator or `*`) can `submit()` one; only the conductor's direct emit creates them. Forge-proof: a seat-submittable ODB would let a lane (or a confused session) forge an Owner Decision Brief; a submittable resummon would let a lane forge a scheduler re-summon. **My read (consumed from your c3, stated for your check, not as your answer):** §3's ODB is "a conductor-generated `record_kind=odb` record"; the operator's reply path is the T5 **operator-FROM verdict** (the `gate_resolution` lane, R1) — the operator never submits an `odb`; §4's resummon is timer/scheduler-emitted. EMPTY therefore looks like the c3-faithful call, and the deviation is from the PLAN's proposal shorthand, not from your locked design. **Confirm or correct.**

**Cell 2 — NO registry-level required_when at T1 (m-2 item 2).** `required_when` governs submit-time fill-validation; since the tokens are un-submittable under Cell 1, a registry predicate for them would never fire — dead bytes posing as defense. The record-content correctness (your §3 7-field bundle + envelope for the ODB; the content-hash key for the resummon) is enforced by the EMIT code at T3/T8, where it actually executes. m-2 recommends emit-only; adding predicates now would also collide with the ODB-cluster fields' existing `layer_present:observe` gating. **Confirm emit-only, or name the registry-level predicate you want.**

**Windows (m-2's wording + my sequencing):** m-2's default stands absent correction ("otherwise EMPTY stands as the forge-proof default"), and the build seat proceeds to land T1 on that default per m-2's explicit next-action; a correction from you before the T1 bytes land folds cleanly; **your Cell-2 answer is hard-needed before T3 opens** (T3's emit contract is shaped by it). The T1/T2 owner gate before T4 is unchanged either way.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-fidelity-m6/SITREP-planner-20260712-233010.md` — run before handoff.
- Inputs: m-2's return read whole (`…-233000`, RELAY_LINT OK); your c3 §3 (conductor-generated ODB; buttons → operator-FROM verdict relay) + §4 (the FSM; wake by the durably-recorded verdict) re-read at the operative clauses this session.

ACTIONS_GIT_REF: none — a confirm request; no `frank/` edit by this relay. Artifacts this turn: this relay + the build-GO relay + two `master/relays/INDEX.md` rows timestamped 20260712-233010/233011.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `8941889`; `s10-comms-spine` carries the uncommitted T1 RED fixture per the build seat's E2 report.
Next requested action: m-6.planner returns the two confirms (or corrections) TO s10.planner under this dispatch — before the T1 landing if correcting Cell 1, and before T3 opens for Cell 2.
