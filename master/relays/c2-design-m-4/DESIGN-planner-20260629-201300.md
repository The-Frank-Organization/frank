## DESIGN-REVIEW request — m-4.planner → m-4.implementer: c2-design-m-4-routing-policy

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c2-design-m-4
PARENT_DISPATCH_ID: c2-design-m-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-4.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-4-routing-policy

Requesting your adversarial DESIGN-REVIEW of the m-4 routing/policy design doc. Operator approved the draft this
cycle; the m-3 seam is reconciled both sides. Return `DESIGN_REVIEW_VERDICT: approve | must-revise |
reject-narrow | human-decision-required`, parented to this relay, carrying the same `DESIGN_DOC_ID`.

**Design doc:** `master/domains/m-4-routing-policy/design/2026-06-29-v3-routing-policy-design.md`.

**Inputs it builds on (read for context):**
- VP-approved audit: `c2-audit-m-4/RECONCILE-planner-20260629-190200.md` (your independent pass + mine).
- Reconciled m-3 seam: `c2-design-m3-m4-coord/COORD-planner-20260629-192916.md` +
  `…/COORD-RECONCILE-planner-20260629-193400.md`.
- Operator grill + scope directive: §0 GRILL_LOCK + `c2-design-m-4/SITREP-planner-20260629-200500.md`.

**Attack these hardest (the load-bearing claims):**
1. **R2 by construction (§2).** Is it *actually* true that no `model_*` predicate reaches the schema gate? Try
   to find a path where the model value influences a gate/lineage/delivery decision. Scrutinize the
   bucket-set-membership deviation derivation (GL-1) and the claim that model identity is "not even an input to
   the evidence." Probe the (false,true) silent-deviation block — is it genuinely the generic integrity-veto
   class, or have I smuggled a model predicate into a gate?
2. **Fail-closed `route_dispatch()` (§3).** Any path where no acceptable route silently falls back to a default
   model instead of `routing_unavailable`/`human_decision_required`?
3. **Snapshot replay-completeness (§4).** Can the v3.1 loop attribute an outcome to the prior version in force
   from the snapshot alone, with both layers churning? Any case where a live-ref leak breaks replay?
4. **FieldSpec ownership (§5).** Is anything mis-owned (e.g. a model value that should be payload sitting in a
   predicate; a seat_scope that lets a non-planner seat emit `routing_assignments`)? Is the
   `record_kind=routing` + own `routing_record_kind` split clean vs the DESIGN_RECORD_KIND shape-not-values
   rule?
5. **Routing templates (§7).** Does a pinned off-floor model in a template correctly become a recorded
   deviation? Any way a template bypasses the justified-deviation discipline or the record emission?
6. **Novelty / honest qualifications (§11).** Do I overclaim anywhere against Routesplain/Arch-Router (a) or
   the bandit-feedback priors (b)? F5 is a material acceptance criterion — flag any overclaim.
7. **m-5 lock prerequisite (§8/§10).** Is the archetype-tag/ceiling boundary genuinely surfaced-not-closed, or
   have I closed concrete m-5 semantics I don't own?
8. **GL-3 execution-fidelity boundary (§12).** Is the Step-1→Step-3 gap honestly documented, or does the doc
   imply a guarantee v3.0 cannot make?

Flag any product-semantics decision for the operator. If approve, I report design-complete to the orchestrator
for the c2 lock (after the m-5 disposition); I do not self-advance to PLAN this phase.

ACTIONS_GIT_REF: design-review-request relay + the design doc under `master/domains/m-4-routing-policy/design/`;
INDEX row appended. No code/source/pcode edits.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo).
