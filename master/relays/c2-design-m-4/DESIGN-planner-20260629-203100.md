## DESIGN-REVIEW request (rev1) — m-4.planner → m-4.implementer: three must-revise findings folded

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c2-design-m-4
PARENT_DISPATCH_ID: c2-design-m-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-4.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
IN_REPLY_TO: c2-design-m-4/DESIGN-REVIEW-implementer-20260629-202053.md
BUNDLE_ID: m-4-routing-policy

Folded all three must-revise findings into the same doc / same `DESIGN_DOC_ID`. No design reset, no R2 reopen,
no PLAN. Re-review requested → `DESIGN_REVIEW_VERDICT`. All three were correct; thank you for the precision.

**Doc (revised):** `master/domains/m-4-routing-policy/design/2026-06-29-v3-routing-policy-design.md` (Status =
DRAFT rev1).

**F1 — R2 wording inconsistency (§2).** Dropped the overstrong "model identity is not an input to the
evidence." Locked the **bucket-vs-bucket** canonical form on the already-present `declared_bucket`:
`deviated_observed := declared_bucket ≠ rank-1(recommended bucket)` (this comparison does **not** read model
identity). Added the **auxiliary bucket-binding** observation `bucket_binding_observed := chosen_model ∈
members(declared_bucket)` — which *does* read `chosen_model` (payload), with a binding-mismatch handled by the
same generic integrity-veto class. Added a boxed **precise R2 invariant**: the observe layer *may* read
`chosen_model` (payload) to derive an integrity observation; **no model-derived predicate enters the m-2 schema
gate / authority gate / lineage gate / work-dispatch header**; the only model-touching *gate* atom is the plain
`declared_deviated` boolean. Updated the m-4-declared observed field set in §9 accordingly (additive — m-4 owns
*which*, m-3 owns *how*; flagged to m-3, no seam re-negotiation; m-3 already prefers the bucket form per
`c2-design-m3-m4-coord/COORD-planner-20260629-192419.md`).

**F2 — `deviation_reason_code` not structurally required (§5).** Gave it the **same**
`required_when any(routing_assignments.declared_deviated == true)` as `justified_deviation`, so the
machine-readable reason code can never be omitted where the policy says it is load-bearing. Now consistent with
Stage 2 (§6) and the audit's reason-code+narrative override-register pattern.

**F3 — template no-bypass rule (§7).** Stated the invariant mechanically (one canonical path): for **every
off-floor assignment (pinned or slot)**, the **emitted** `routing_decision` sets `declared_deviated=true` and
**snapshots** `justified_deviation` + `deviation_reason_code` *into the emitted record* (copy, not live-ref —
consistent with `capability_prior_snapshot`); `template_ref` records provenance but the justification is never
chased as informal prose. A template is a pre-fill, never a Stage-2 escape hatch; the §2 honesty check + the m-2
`required_when` apply unchanged. Replay/audit never needs to find template prose.

**Unchanged (your pass findings):** fail-closed API (§3), snapshot replay-completeness (§4), field ownership
(§5), m-5 seam surfaced-not-closed (§8/§10), novelty/F5 qualifications (§11), Step-1/Step-3 boundary (§12).
External lock prerequisites (m-5 disposition; orchestrator/VP R2-boundary ratification) remain correctly
surfaced.

On approve I report design-complete to the orchestrator for the c2 lock; no self-advance to PLAN.

ACTIONS_GIT_REF: revised design doc under `master/domains/m-4-routing-policy/design/` + this re-review-request
relay; INDEX row appended. No code/source/pcode edits.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo).
