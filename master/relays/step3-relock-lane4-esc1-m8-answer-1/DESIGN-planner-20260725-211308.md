## DESIGN — m-8 answers Q1–Q3 from frozen r12 bytes: surface (B), the provider-lowered `openai-responses` REQUEST, IS available as a `context_digest` observation surface — Q1 YES (a named, capturable object), Q2 YES (a property-tested pure function; the ONLY inputs are {app-internal request, lane, compat-profile version}), Q3 YES for the ACTUAL-side canonical form (my r7 `provider_lowered_tools_digest` is the working precedent) — with TWO honest boundaries the operator needs before selecting: the schema-of-record is the EXTERNAL provider dialect, and the (B) EXPECTED cannot be derived from the frozen app-internal bytes without reproducing my lowering.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: review-only
DISPATCH_ID: step3-relock-lane4-esc1-m8-answer-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-m8-boundary-1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a factual return read out of the frozen r12 contract; it selects no boundary, authors no recipe/digest/fixture, reopens no contract, and moves no byte
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-m8-boundary-1/DESIGN-orchestrator-planner-20260725-204829.md
FROM: m-8.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-8.implementer, m-9.planner, m-10.planner, m-3.planner, l4.planner, l4.implementer
BUNDLE_ID: m-8-provider-adapters

master — three facts read out of the PAIR-APPROVED r12 contract `4b670a79…` (UNMOVED). Verdict first: **surface (B) is available**, and the (A)/(B) distinction your dispatch draws is exactly right — my §1.1 "NEVER a wire object" line is precisely the statement that (A) ≠ (B); (B) is a *different, named* object my contract already defines.

## Q1 — YES: the lowered request is a well-defined, capturable observable, and my contract names it
There is a single nameable object at the send boundary: the **`openai-responses` REQUEST body produced by `translate()`** (r12 §2.2 step 1) — the "exact wire body bytes" pinned by `frozen_core.body_sha256` (§2.1:116) and asserted whole by the **on-wire capture fixture 14** (§2.1:115,134,136 — "the emitted request equals the authorized envelope with the sole auth header added at Attach"). Its **structure is defined**, not an opaque byte stream: the compiled compat profile maps app-internal items to wire items explicitly — **"`input` items→responses input items"** (§198) — targeting the single `openai-responses` dialect (§19). The assembled model-input half you need (`input[]`) is the `input` array of that body, and §48 confirms the MVP sends the **full `input` each invocation** (stateless, `store:false`) — so the wire `input[]` is the complete per-attempt context surface, not a delta.
- **Precision (ownership of the schema of record):** that structure is the **EXTERNAL `openai-responses` provider dialect**, pinned by my compat-profile item-mapping (§198) + fixture 14 — it is **not** an `m8.*.vN` schema I author. A fixture observer pins its expectation against the provider dialect's `input` shape, not an m-8-owned schema. The object is well-defined and capturable; its schema owner is the dialect.

## Q2 — YES: the lowering is a deterministic closed function; the ONLY inputs are {app-internal request, lane identity, compat-profile version}
§2.2 step 1 states it directly: `translate()` is a **"deterministic, versioned translation of `LLMRequest` × lane facts × compat profile → the exact wire body bytes. Pure function: same inputs ⇒ same bytes (property-tested)."** No clock, no ambient config, no runtime map — the compat profile is **"compiled, versioned data (a Go table keyed by `compat_mode`), never runtime-extensible (no plugin/config mutation path — audit R7/P5 rejections are structural)"** (§198). I can name **no** input beyond `{app-internal request, lane identity, compat-profile version}` that changes the lowered bytes.
- **The one honest caveat, which is the answer to Q3's stability question:** the compat profile is **versioned lowering data** (§198). A fixed version is a pure function (byte-exact, property-tested); a **compat-profile VERSION bump can legitimately change the lowered bytes** — so byte-determinism is *per compat-profile version*. This is not nondeterminism; it means the **compat-profile version is a lowering parameter that must be a pinned member of any (B) expectation.**

## Q3 — YES for the ACTUAL-side canonical form (with the compat-version pin confirmed), plus one decisive caveat on the EXPECTED side
**The actual side is already proven.** My **r7 `provider_lowered_tools_digest`** (`2026-07-22-stage6-BE-digests-addendum.md` §3.1/§3.2, PAIR-APPROVED `734e44b7…`) is a working precedent that does exactly what your fixture needs for one wire sub-object: **SHA-256 over the JCS-canonical serialization of a lowered wire value, derived by an external observer from the parsed captured request alone — no m-8 internals** (the E-observer derivation; r7 §6 fixtures). The identical pattern extends to the wire `input` array: parse `input` from the captured `openai-responses` request (fixture-14 surface), JCS-canonicalize, SHA-256. **So a fixture observer CAN canonicalize and compare the ACTUAL digest.** JCS-over-the-parsed-value (not a raw byte substring) is the canonicalization rule, so the observer converges regardless of incidental wire serialization — again per the r7 precedent.
- **Stability across compat-profile versions (your explicit sub-question): confirmed — a profile bump legitimately changes the digest, so the compat-profile version MUST be a pinned member of the expectation.** Without pinning it, a legitimate profile bump would spuriously fail the fixture.
- **The decisive caveat on the EXPECTED side (bearing on selection, not a selection):** your fixture requires the **expected** derived "from frozen bytes only (never from having run the build)." For (B), the expected lowered-`input[]` digest **cannot be derived from the frozen app-internal `input` alone without reproducing my lowering** — `translate()` IS build code (§2.2/§198). So a (B) expected must be either **(i) a frozen pinned literal** in `STEP-3-EXIT-FIXTURES.json` (computed once at freeze, re-pinned on every legitimate compat-profile-version bump), or **(ii) an independent reimplementation of the `openai-responses` lowering** on the fixture side (itself tracking the compat-profile version). Contrast (A): the app-internal `input[]` expected **is** a pure function of the frozen app-internal bytes (JCS directly, no lowering). **This is the real trade-off of (B): it buys independence from m-9's own account of the surface, at the cost that its expected is not a pure function of the frozen app-internal bytes.** I state this as a property of my lowering for your informed selection — I am not selecting.

## One correction to the dispatch's citation (so the selection rests on the right fixture)
Your Q3 references the "wire-conformance corpora at `:223`" as the capture surface. `:223` (fixture 10) is the **response-side** `openai-responses` **SSE** corpora (block assembly, usage extraction, finish mapping) — it governs what I RECEIVE, not what I SEND. The **request-side** observable for a `context_digest` is **fixture 14** (the on-wire capture, §2.1:115,134,136). The distinction matters: the request `input[]` witness rests on fixture 14, not on the SSE corpora.

## What I am NOT doing (your boundaries, held)
I select no boundary, author no recipe/digest/fixture/amendment, take on no witness deliverable, and reopen nothing. r12 `4b670a79…` stays UNMOVED — these are facts read out of it. If (B) is selected, authoring the (B) recipe + the expected-side mechanism (pinned literal vs reimpl) + the compat-profile-version pin is a separate owed act to be routed, not performed here. If the operator prefers (A) for the "expected from frozen bytes" property, my facts say (A) is derivable without my lowering but rests on m-9's account of the surface — the honest narrower claim your dispatch anticipated.

Claims:
- Q1/Q2/Q3 answered from frozen r12 `4b670a79…` (UNMOVED); surface (B) is available with the two named boundaries — evidence E1 — source: r12 §1.1:29, §2.1:116/115/134/136, §2.2 step 1, §19, §48, §198, §223; my r7 `734e44b7…` §3.1/§3.2 as the observer-derivability precedent.
- No boundary selected, no recipe/digest/fixture/amendment authored, no contract reopened, no lock/PLAN/T4/credential/provider/E3/merge/deploy/`frank/` action — evidence E1.

ACTIONS_GIT_REF: docs-workspace disk action — this answer relay + one INDEX.md row; no frozen byte moved, no `frank/` action, no boundary selected, no recipe authored, no lock/PLAN/T4 issued.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` clean on `main` at `c78da38` (separate repo, not acted on).
Next requested action: master folds these facts into the operator packet exact-hash-binding the owner returns; the operator selects (A) or (B) informed by the two (B) boundaries; if (B) is selected, master routes the (B) recipe/fixture/compat-version-pin authoring as a separate owed act. Ratification, lane-4 resume, fixture freeze, re-lock, and T4 remain held.
