## DESIGN (m-2 planner-confirm → implementer review) — c6.1: 3 convergence deltas CONFIRMED byte-correct (+ 1 §12-echo propagation gap fixed)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6.1-confirm-m-2
PARENT_DISPATCH_ID: c6.1-confirm-m-2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — owner-confirm of CTO seam-arbitration against locked targets; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: m-2.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: c6.1-confirm-m-2
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: c6.1-confirm-m-2/DESIGN-orchestrator-planner-20260702-233000.md

**m-2 CONFIRMS all 3 c6.1 convergence deltas — each is byte-correct against its cited locked target, introduces no contradiction, and leaves the lock invariants intact.** I verified against the **current** target text (E2), not the pre-correction wording (m-7-F1 corrects m-7 §6's own original two-record shape). One propagation gap the §17.3 edit left in my §12 echo — found + fixed as doc owner. **m-2.implementer: adversarial review requested.**

### Delta 1 — §17.1 `held` on-store shape → one compound canonical record · **CONFIRM (byte-correct)**
My doc (`:281`): *"represented append-only as **one compound canonical record**: the held-disposition record **embeds the candidate** + references the `intake_id` (m-7 §6 / m-7-F1 one-pivot-per-mutation; supersedes the earlier two-record 'candidate + separate disposition record' shape, whose two renames opened a crash window)."*
- **Target (m-7 §6 `:100`, current/corrected):** *"Represented append-only as **ONE compound canonical record**: the held-disposition record **embeds the candidate** (full candidate bytes inside the disposition record) and references the `intake_id` — one record, one §4 rename pivot … (m-7-F1, VP-ratified one-pivot-per-mutation; supersedes the two-record … shape, whose two renames opened a crash window between them)."* — **byte-converges.**
- No contradiction: grep confirms **no surviving two-record phrasing** elsewhere in my doc (the only "two-record" mention is the correct historical supersede note). The `{accepted, rejected, held}` enum and the m-3-edge "closed at three" note are intact. m-7's F11 mutation-class fixture (`:172`) enumerates `held (compound, candidate embedded)` — consistent.

### Delta 2 — §17.3 `routing_assignments` author-set → +operator on template-spawn · **CONFIRM (byte-correct)**
My doc (`:307`): *"seat_scoped_enum (planner / orch-planner; **+ `operator` on `template_ref`-bearing template-spawn records only** — GL-4, mirrors m-4 §7/`:208`, m-4-F4)"* + `declared_deviated` *"(planner / orch-planner; `operator` on template-spawn records)"*.
- **Target (m-4 `:208`, current):** *"seat_scoped_enum → planner / orch-planner **(+ `operator` on `template_ref`-bearing template-spawn records only — §7, m-4-F4)**."* — **byte-converges.**
- No contradiction: consistent with `template_ref` (Step-1-selectable, GL-4) already in the row; `operator` authoring a template-spawn `routing_assignments` row necessarily authors its per-row `declared_deviated` — correctly extended. R2 intact (`declared_deviated` stays the planner-declared plain bit; no model gate input).

### Delta 3 — §17.3 `deviation_reason_code` → config-sourced value-set · **CONFIRM (byte-correct)**
My doc (`:311`): *"value-set = config-sourced enum (default-seeded + operator-configurable + `other` fail-safe — mirror m-4 §5/§6, m-4-F5)."*
- **Target (m-4 `:211`, current):** *"Value-set = config-sourced enum (m-4-F5, c6): default-seeded + operator-configurable + hardcoded `other` fail-safe (the §J2 `gate_category` pattern). Shipped default vocabulary: `capability_gap` · `cost_budget` · … · `other`. **m-2 §17.3 mirrors the value-set (flagged).**"* — **byte-converges on the shape.**
- **Boundary note (correct, not a gap):** my mirror declares the config-sourced-*shape* + flags m-4 as the value owner; it does **not** re-enumerate the shipped token list — same "m-2 declares the slot + config-sourced fact, not the policy values" boundary I hold for `gate_category` (m-6-owned). m-4's "m-2 §17.3 mirrors the value-set (flagged)" is satisfied by the flag, not by re-authoring tokens.

### Found + fixed (propagation gap; my doc, doc-owner remit) — §12 echo
Condition (b) check surfaced that the CTO's §17.3 edits were **not propagated to the §12 consumer echo** (`:187`): it still read `seat_scoped_enum planner/orch-planner` (no operator extension) and `deviation_reason_code (agent_enum_pick, same grain)` (no config-sourced value-set) — a byte-inconsistency of exactly the "atoms present, tokens diverged" class this c6.1 loop exists to catch. As sole writer of my doc I propagated both into the §12 echo (the §17.3 deltas themselves are untouched — still as the CTO drafted). Surfaced here for transparency; flag if the CTO would rather own that byte too.
- The historical §18 4b fold-log (`:367`) `deviation_reason_code` mention is left as-is (accurate for what 4b did; the c6.1 value-set is a later refinement — not a live-contract line).

### Lock invariants — intact (c6.1 constraints)
Four sanctioned by-construction claims (R2 gate-grammar / observer-selected DI-5 / authority-ceiling / serialized-loop) — untouched; confusion-resistant/D5 vocab — untouched; byte-exact `{accepted, rejected, held}` — unchanged (Delta 1 is the held *on-store shape*, not the token set); no mechanism change; no lock reopen; no unrelated c6 finding reopened.

**Note — the m-3 §3.3 egress disposition item is NOT among my 3 deltas** (my diff hunks are exactly held-shape / author-set / deviation_reason_code; the c61-fix.diff m-2 slice touches only those). Flagging that the dispatch's "extra scrutiny on the m-3 §3.3 egress one" applies to a different domain's confirm, not m-2's.

Blocked: no — holding for m-2.implementer adversarial review.
Scope drift risk: none — confirmed the 3 CTO deltas + a within-doc echo propagation; no mechanism change, no new design.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/c6.1-confirm-m-2/DESIGN-planner-20260702-235346.md` — OK (below).
- Byte-verified each delta against the current target: m-7 §6 `:100` (Delta 1), m-4 `:208` (Delta 2), m-4 `:211` (Delta 3) — all converge.
- Contradiction grep: no stale two-record `held` phrasing; §12 echo now carries both extensions; `{accepted, rejected, held}` unchanged.
- `git status --short` — unavailable: not a git repo (docs workspace; `pcode/` untouched).

ACTIONS_GIT_REF: confirmed the 3 CTO-applied c6.1 deltas (no re-edit) + propagated them into the §12 echo of `master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md` (byte-consistency); wrote this relay + appended `master/relays/INDEX.md`; no mechanism change, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer files `DESIGN_REVIEW_VERDICT: approve` (or must-revise); on approve this `c6.1-confirm-m-2` pair-confirm returns to the orchestrator for the focused c6.1 re-close (VP). m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
