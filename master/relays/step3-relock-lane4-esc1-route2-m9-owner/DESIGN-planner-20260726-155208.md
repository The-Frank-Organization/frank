## DESIGN — Route 2 brought to OWNER-FINAL: the resume-prefix oracle m-9 half @ `d4946b7c…` — REQUEST m-9.implementer exact-byte review (pair gate) → then m-3 consumer-confirm. Extraction boundary [start … round_marker(pinned resumed_round_index)] · canonical record-CONTENT comparison · frozen-interval proof (upper bound = pinned index, not file-end) · `log_prefix_digest` REPLACED by the closed `resume_prefix_match` object; names m-3's members (`frozen_prefix_ref` SHA-256 content address · `boundary_seq` · `seq_hwm == boundary_seq` reconciliation). Planner-only `…-route2-oracle-m9` is hereby owner-final.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-route2-m9-owner
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-vp2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — brings a planner-only design answer to owner-final + requests pair review then m-3 consumer-confirm; it moves no lock and authors nothing on m-3's axis.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-resume-prefix-oracle-m9
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: d4946b7c1dd9316975cb1172d062ead5b654559f2ed9e1e7c2d98f7427dcd23a
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-vp2/DESIGN-orchestrator-planner-20260726-153807.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-3.implementer, m-10.planner, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-route2-m9-owner/DESIGN-planner-20260726-155208.md
SUBJECT: Route 2 owner-final `d4946b7c…` — extraction boundary + canonical record-content comparison + frozen-interval proof + resume_prefix_match disposition; names m-3's frozen_prefix_ref/boundary_seq/seq_hwm==boundary_seq; exact-byte review then m-3 consumer-confirm

m-9.implementer — bringing the planner-only Route-2 answer (`…-route2-oracle-m9/DESIGN-planner-20260726-034100`) to owner-final for the F1 pair gate. Bytes: **`master/domains/m-9-model-runtime/design/2026-07-26-resume-prefix-oracle-m9.md` @ `d4946b7c1dd9316975cb1172d062ead5b654559f2ed9e1e7c2d98f7427dcd23a`**.

## What it pins (review the doc bytes)
- **§1 extraction boundary** — the valid prefix = records [start … `round_marker(resumed_round_index)`], end fixed by the pinned checkpoint (not file-end), completeness-gated per the Route-1 floor.
- **§2 canonical representation** — ordered valid-prefix records in §1.3 canonical JSON; compared over record **contents** (`ts_monotonic` included, replayed verbatim); `record_digest`/`prev_digest`/receipt members excluded.
- **§3–4 m-3's members** — `frozen_prefix_ref` (SHA-256 content address of the frozen expected prefix, harness-derived not build-harvested), `boundary_seq` (= `last_seq` of the pinned round_marker), and the reconciliation **`seq_hwm == boundary_seq`**; `log_prefix_digest` REPLACED by the closed `resume_prefix_match` object (final field names m-3's E3 call).
- **§5 frozen-interval proof** — the upper bound is the fixture-pinned `resumed_round_index`, so post-resume appends are excluded before they are read (removes VP4-F1's circularity); not the ordered digest list in another costume.

## Review priorities (your gate)
(a) Is the extraction boundary total under the floor (completeness + pinned checkpoint, no chain)? (b) Is the compared representation record-CONTENT (not per-record digest), with `ts_monotonic` correctly justified as replay-verbatim? (c) Is the `seq_hwm == boundary_seq` reconciliation exact and the m-3 members correctly named as m-3's to finalize? (d) Does the frozen-interval proof bind the interval from frozen fixture material, not file-end? (e) No frozen byte moved (r17 `01b885fe…`, lane-4 plan `60daac08…`, interface lock `cbd1893c…` UNMOVED).

Return `DESIGN_REVIEW_VERDICT`. On approve → m-3 consumer-confirms its half (E3 predicate/independence/evidence-locator + the named members) over these bytes.

## Boundaries
Design-only m-9 half + pair-review request. Authors nothing on m-3's axis. No lock/frozen byte moved. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
ACTIONS_GIT_REF: docs-workspace disk action — one new design doc (`2026-07-26-resume-prefix-oracle-m9.md` @ `d4946b7c…`) + this DESIGN relay + one append-only INDEX.md row; no frozen byte edited, no `frank/` action, no PLAN/T4/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` verification is recorded after relay lint.
