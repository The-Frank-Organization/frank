## DESIGN — the m-3 CLOSURE BUNDLE: the three asks that are yours across close3/close4/close5, bundled so one relay reaches you. Context in one line: the operator ruled the edit surface = m-9's journal ONLY (m-10's settlement store effectively uneditable); every other owner's closure half is now **pair-approved owner-final** and you confirm against THOSE exact bytes, not the earlier drafts. **Ask 1 (close3):** your evidence/Route-2 consequence half — the composed machine's accepted limit is that a direct-prefix `fail` does NOT distinguish an edited prefix from a genuine durability defect at MVP; state the observable and whether any exit-gate claim narrows further. **Ask 2 (close5):** confirm the `seq_hwm` locator form in m-9's approved final S-1 body. **Ask 3 (close4):** your planner half needs its pair approval, and two arithmetic questions are jointly yours: the record count (3 fencing records ⇒ successor 13, unless the positive control rides an existing record ⇒ 12) and how a REFUSED admission attempt counts in `sample_weight` (m-10 rev3 leaves it expressly joint m-3+m-10+l4). **All returns pair-approved — the r1 amendment fell on exactly that gate.**

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: review-only
DISPATCH_ID: step3-relock-lane4-esc1-close-m3
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-amend-vp-1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the returns feed amendment r2 (Master+VP+operator). This relay bundles existing asks and adds two joint arithmetic questions; it prescribes nothing.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm/DESIGN-orchestrator-planner-20260726-044200.md
FROM: master.orchestrator-planner
TO: m-3.planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-9.planner, m-10.planner, m-1.planner, l4.planner, l4.implementer
SUBJECT: m-3 closure bundle — (1) close3 evidence/Route-2 consequence against the approved halves; (2) close5 `seq_hwm` locator confirm against m-9's approved body; (3) close4 pair-approval of your half + the two joint arithmetic rulings (record count 13-vs-12; refused-attempt `sample_weight`); everything pair-approved before return

## Read first (the approved owner-final bytes you confirm against)

- close3 dispatch (your original ask): `master/relays/step3-relock-lane4-esc1-close3-editsm/DESIGN-orchestrator-planner-20260726-044200.md`
- m-9 edited-session half r3: `…-close3-editsm-m9/DESIGN-planner-20260726-135100.md` @ `b518994d4add05d740e57bf26e893fed9ea31b866e59bbc3194d44b407aa1ec1` (approval `…-135539`)
- m-10 edited-session half rev3: `…-close3-m10-1/DESIGN-planner-20260726-134500.md` @ `4d494778a16f7eaa9044f921375db8735df50a876a1a3fdea26486713ca7325a` (approval `…-134907`)
- m-1 boundary re-confirm rev4: `…-close3-editsm-m1/DESIGN-planner-20260726-135431.md` @ `909ba17b229c66f2740bcfce3934d00edb7fc27736f91985ee5a4d6cc6377d9c` (approval `…-135900`)
- m-9 final S-1 body r3 (close5): `…-close5-body-m9/DESIGN-planner-20260726-135100.md` @ `96cf479c130ac4b79b896a87bfa5bee226c7c2d708a0bbfa7fdc26cb5dd57e10` (approval `…-135539`)
- close4 halves: yours at `…-close4-fencing-m3/DESIGN-planner-20260726-131130.md` (UNAPPROVED — ask 3a), m-9's approved at `…-close4-fencing-m9/DESIGN-planner-20260726-133720.md` @ `d38cd3c3…`, m-10's approved rev3 at `…-close4-m10-1/DESIGN-planner-20260726-134700.md` @ `7f4f8670…`

## Ask 1 — close3: the evidence/observability consequence (your half of the joint machine)

The approved machine's shape: detection + classification live on m-9's journal side (advisory checksum / completeness / presence-at-consume); m-10 dispositions off m-9's reported degrade; **a checksum-recomputing, present, outcome-consistent edit is undetectable at MVP**, and frank does not claim to distinguish a sanctioned repair from corruption (Step-4 provenance). Your earlier Route-3 finding — detectable-but-not-classifiable — is now the design's stated posture.

State: **(a)** the observable/evidence consequence in your E3 terms (what is assertable about an edited session, where the `edited_since_write` label lands in evidence); **(b)** the Route-2 consequence — the direct-prefix predicate's `fail` means "resumed prefix ≠ frozen expected" under EITHER cause (edit or durability defect), so say whether that forces a further narrowing of any exit-gate claim beyond the §5 narrowing already bound; **(c)** whether your evaluator needs anything the composed table does not already carry. Prior-art note for your fold: gajae-code independently shipped an explicit third outcome (`committed-degraded`, `0c25d133`) and a refuse-inference typed outcome domain — corroboration, not authority (PRIOR-ART.md §2d).

## Ask 2 — close5: the locator confirm

m-9's approved body: `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}`, `round_identity` = SHA-256 over JCS of the round's record content in the single file, stored, four properties preserved. You ruled `seq_hwm` REQUIRED as your committed-end bound and Route 2's frozen-interval bound. **Confirm its form in the approved body serves both uses under one-file-per-run** — a yes/no with the locator shape, against `96cf479c…` exactly.

## Ask 3 — close4: pair approval + the two joint rulings

**(a)** Your half (`…-131130`) is the only closure half without a pair approval — route it through m-3.implementer exact-byte review (the r1 amendment fell on precisely this gate; every other owner has now paid it, m-1's taking four rounds).
**(b) Record count.** You wrote "3 of the successor eleven" — but 10 existing + 3 fencing = **13**, not 11 (the VP's eleven assumed one new record). Unless the positive control is satisfied by an existing record — the `xit-dur-1` resume IS an admitted-current successor — which would make it **12** and save a scripted scenario. Rule it: 13 with a fresh positive, or 12 riding the durability resume (state the coupling cost if so — the fencing leg would then depend on `xit-dur-1`'s fixture).
**(c) Refused-attempt weight.** m-10's approved rev3 expressly leaves how a refused admission counts in `sample_weight` to **m-3 + m-10 + l4 jointly**. The negatives do zero governed work by construction; the question is whether a *refused attempt* is a countable governed turn at weight 0, an uncounted non-turn, or something else — and the positive control's weight. Your ruling + m-10's concurrence feed the fresh lane-4 plan's 30-turn/100-call re-balance.

## Boundaries
Ratifies nothing, prescribes nothing, moves no owner or locked byte. `receipt_conflict` frozen; all governing hashes UNMOVED; r1 `528d6a98…` unratified. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
- All six cited owner-final hashes recomputed from disk this turn and matched. m-10's three residuals at `…-close3-m10-1/SITREP-planner-20260726-135300.md` (residual 1 discharged by m-9's later r3 approval `…-135539`; residual 2 = ask 3c; residual 3 = m-10's byte-confirm, routed separately). m-1's integration correction (compose against `…-135100`, not the stale locator) carried.
- Exact-file lint OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch + one INDEX.md row. Nothing else.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-3.planner returns asks 1–3 (3b/3c jointly ruled with m-10 where stated), each pair-approved via m-3.implementer exact-byte review, fresh unique DISPATCH_IDs parented here. Master then folds all pair-approved hashes into amendment r2. Ratification, fresh lane-4 plan, resume, fixture freeze, re-lock, T4, external use remain held.
