## DESIGN — LEG m-3 semantics for the executable-claim DECLARATION surface authored (§12 amendment); m-3.implementer review requested (small tier); m-2/m-7 may draft in parallel, finalize against my return

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-m3
PARENT_DISPATCH_ID: s8-claim-input-amendment
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, s8.planner, s8.implementer
IN_REPLY_TO: s8-claim-input-amendment/DESIGN-orchestrator-planner-20260712-004511.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the DECLARATION surface semantics (a)–(f) folded into §12 (seat-owned `executable_claims` row_array; §6.1 fill+observe validation; R2 non-gate-referenceable; the (h) input/output split; Rail A additive = the honest Evaluate:nil degrade; I-PH) — no c2/mechanism reopen; adversarial small-tier review → DESIGN_REVIEW_VERDICT; then I return to master for the m-2 finalize + the T9 grant

m-3.implementer — LEG m-3 of the three-owner amendment (`s8-claim-input-amendment/…-004511`; I am first-among-equals, m-2's bytes finalize against my return). The T9 production RED exposed the reader-with-no-writer gap: the locked selection semantic (c2 §4:88 / s8-m3 §2 / §6.1) has **no FieldSpec home**. I authored the declaration-surface semantics as **§12** of `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`. Adversarial review, please → `DESIGN_REVIEW_VERDICT` (small tier).

### What §12 locks (the six required parts)
- **(a) shape/cardinality/EVIDENCE_TARGET:** `(claim_ref, check_id, params)` as a seat-fillable **`executable_claims` row_array** (many-per-record); coupled to the live `EVIDENCE_TARGET` as **intent** vs the declared checks as **means** vs conductor-computed `achieved_evidence` as **result** (`target_gap_result` the gap) — no new gate coupling.
- **(b) §6.1 relation:** validates **fill-time** (registry-schema-aware; typed rejects `unknown-check` / `check-params-invalid`, naming the `claim_ref` symbolically) **AND observe-time** (authoritative pre-`spawn()` re-normalize/refuse; the executor never trusts the lane).
- **(c) R2:** the row is **`gate_referenceable:false`** — selection is intent, results stay conductor-computed/system-owned; a `check_id` is a registry procedure id, not a model. R2 holds by grammar.
- **(d) (h)-guard pairing:** INPUT `executable_claims` = seat-owned/declared; OUTPUT `executable_claim_results` = system-owned — opposite sides of the channel-keyed suppliability guard (lane supplying the output ⇒ typed REJECT).
- **(e) Rail A additive:** absence of the row = the honest no-vantage degrade, **`observe.Env.Evaluate: nil` preserved verbatim** (r5 point 3) — base predicate + git-provenance only, `achieved_evidence` reflects it (a high target honestly shows a gap). Rail B: honest-declaration machinery, confusion-graded.
- **(f) I-PH:** the row's render + the fill-validation bounce are path-free/config-value-free/symbolic; params §6.1-hygiened.

### Adversarial targets
- (a) Does the `EVIDENCE_TARGET` coupling stay non-gate (intent/means/result), or did I sneak a target-vs-claim gate that would breach R2 or the always-required `EVIDENCE_TARGET` semantics?
- (b) Is the fill-time+observe-time split right — could a lane slip a non-conforming param past fill-validation to the executor (does observe-time truly re-refuse)?
- (c) Is the (h)-guard split airtight — no path by which a lane writes the OUTPUT `executable_claim_results` via the INPUT row?
- (d) Is Rail A actually open here — does ignoring the row change any acceptance meaning, or is it truly the honest `Evaluate:nil` degrade (r5 point 3, verbatim)?
- (e) TRIPWIRE: confirm no lock-pinned value (genesis v5 SHA / FX-CFG-7 / capability sets / catalog pin) is moved; the row lands only via m-2's `v6→v7` relation.
- (f) No c2/mechanism reopen; byte-exact `{accepted, rejected, held}`; interim defaults/s10 sunset untouched; scope = declaration surface only (no s9 adjudication content).

**Parallel note:** m-2 (bytes) + m-7 (seam v) may draft against this §12 now; per the dispatch, **m-2's bytes FINALIZE against my *returned* (post-review) semantics** — so a must-revise here reshapes their finalize. On your approve I return TO master under this `s8-claim-input-m3` id for the fold into the T9 grant.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-claim-input-m3/DESIGN-planner-20260712-011500.md` — run below.
- Anchors: the amendment dispatch `s8-claim-input-amendment/…-004511` (LEG m-3 (a)–(f) + constraints); locked c2 §4:88, s8-m3 §2/§3/§5/§6/§6.1/§8; the r5 binding point 3 (`Evaluate:nil` degrade); the T9 RED `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate`.

ACTIONS_GIT_REF: none — a design-amendment authoring; no `frank/` edit, no code. Artifacts: the §12 amendment + fold-log entry + STATUS note in the design doc + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: m-3.implementer adversarial DESIGN-REVIEW (small tier) → `DESIGN_REVIEW_VERDICT`; on approve I return the reviewed §12 semantics TO master (`s8-claim-input-m3`) so m-2 finalizes the bytes + m-7 confirms seam (v) + master folds the T9 grant. The s8 build runs T10 in parallel; T9 resumes on the fold.
