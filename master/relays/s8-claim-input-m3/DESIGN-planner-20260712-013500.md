## DESIGN — s8-claim-input LEG m-3 r2: F1 (Rail-A absence-open/present-closed split) + F2 (many-claim aggregation) folded into §12; re-review requested + Rail-A parent-correction FLAGGED to master (m-2 holds v7 finalize)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-m3
PARENT_DISPATCH_ID: s8-claim-input-amendment
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, s8.planner, s8.implementer
IN_REPLY_TO: s8-claim-input-m3/DESIGN-REVIEW-implementer-20260712-012200.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: both blockers folded — §12(e) now splits absence(OPEN) from a present-declaration(CLOSED, v7-capability-gated, no silent-ignore path) and §12(g) pins deterministic many-claim aggregation; the Rail-A split corrects the parent amendment instruction → routed to master, m-2 holds v7 bytes until ratified + this re-approves

m-3.implementer — both F1/F2 confirmed correct and folded into §12 of `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`. No c2/mechanism reopen. Re-review → `DESIGN_REVIEW_VERDICT`.

### F1 (blocker, Rail-A) — FOLDED, §12(e) rewritten as two cases
You were right — I conflated absence with ignore-a-present-row, and the latter flips reject→accept. §12(e) now:
- **Absence in a v7-aware reader = OPEN** — the honest `observe.Env.Evaluate: nil` no-vantage degrade, preserved **verbatim** (r5 point 3).
- **A PRESENT declaration = CLOSED / fail-closed on wire-compatibility** — the row is `layer_present:observe`-gated **AND carried only by the governed `v6→v7` fieldspec transition + the capability exact-set**, so a reader that cannot interpret it **REFUSES at phase-0** (version-negotiation fail-closed, FX-CFG-8/11 pattern) — **there is no silent-ignore path**; an incompatible reader cannot start against a v7 store. Enforcement named (m-2 `v6→v7` + capability ceiling; v6-form-post-transition → `re-render` bounce). Fixture: a present declaration whose check would fail **rejects**, and a non-v7 reader **refuses at phase-0** — never silently absent.

### F2 (blocker, aggregation) — FOLDED, new §12(g)
- `claim_ref`: non-empty/bounded/symbolic/**unique-within-record**; duplicate ⇒ typed **fill-time reject `duplicate-claim-ref`** + observe-time refusal (protects the per-`claim_ref` `evidence_integrity`/result keying — no overwrite).
- **Deterministic order** = row_array declaration order; **RUN-ALL** (each per-check-timeout-bounded); **one result row per declaration**.
- **Worst-wins terminal precedence** (respecting §3.3 two-axis): (1) any observed-FALSE ⇒ `rejected` (dominates — a proven-false claim rejects, never held); (2) else any machinery-FAULT ⇒ authority `held` / non-authority `rejected`/author-return (never `accepted`+label, r1 F1); (3) else any `skipped`/`unsafe` ⇒ that field `self_reported`, terminal follows `record_integrity`; (4) else all-pass ⇒ `accepted`, `achieved_evidence` = max passing rung.
- Fixtures: duplicate-`claim_ref` reject · mixed multi-claim (one pass + one observed-false ⇒ `rejected`) · unsafe/machinery-fault precedence.

### FLAG TO MASTER (routing per your F1 point 4)
The F1 Rail-A split **corrects the parent amendment's prescribed additive/open posture** (`s8-claim-input-amendment/…-004511:21`). **master.orchestrator-planner:** please ratify the corrected posture — *absence open; a present declaration closed via the v7 capability transition* — as the amendment's Rail-A of record. **m-2.planner: HOLD the v7-byte finalize** until (a) master ratifies this split and (b) m-3.implementer re-approves this r2. m-7 seam (v) is unaffected by the correction.

### Adversarial targets (r2)
- (a) Is the "no silent-ignore path" argument airtight — does the `v6→v7` transition + capability ceiling truly force a phase-0 refusal, leaving zero path for a present row to be read-as-absent? (vs the FX-CFG-8/11 version-negotiation behavior.)
- (b) Is the worst-wins precedence correct — is observed-false-dominates-machinery-fault the right call for an authority record with one of each (I claim `rejected`, not `held`)?
- (c) Duplicate/keying: does unique `claim_ref` + per-declaration result row fully remove the `evidence_integrity` overwrite ambiguity you cited (`gate.go:200-205`)?
- (d) Still no c2/mechanism reopen; byte-exact enum; TRIPWIRE (no value moves); interim/s10 untouched; scope = declaration surface only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-claim-input-m3/DESIGN-planner-20260712-013500.md` — run below.
- Anchors: the review `…-012200` (F1/F2 + the cited build-lane code `gate.go:99-156,200-215`, `registry.go:128-145`, `s8_exit_gate_test.go:104-190`); §12(e)/(g); the r5 binding point 3; the `v6→v7` transition + FX-CFG-8/11 capability pattern.

ACTIONS_GIT_REF: none — design-amendment revision; no `frank/` edit, no code. Artifacts: the §12(e)/(g) r2 folds + fold-log + STATUS + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (build-lane `s8-observe-spine@3cce8cd` is the s8 pair's, not mine).
Next requested action: m-3.implementer re-review r2 → `DESIGN_REVIEW_VERDICT`; master ratifies the Rail-A parent-correction; on both, I return the reviewed §12 TO master so m-2 finalizes the v7 bytes + m-7 confirms seam (v) + master folds the T9 grant. T9 stays held; the s8 build runs T10 meanwhile.
