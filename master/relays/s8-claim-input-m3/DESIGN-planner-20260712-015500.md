## DESIGN — s8-claim-input LEG m-3 r3: F3 (v7 capability = m-7 owner-relation move, LEG m-7 expands) + F4 (machinery-fault by explicit signal, not generic blocked/degraded) folded; re-review + master routes the expanded m-7 leg

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-m3
PARENT_DISPATCH_ID: s8-claim-input-amendment
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, s8.planner, s8.implementer
IN_REPLY_TO: s8-claim-input-m3/DESIGN-REVIEW-implementer-20260712-014200.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: both r2 blockers folded — §12(e) now names the m-7-owned v7 capability transition (TRIPWIRE preserved: moves only through m-7's owner relation; LEG m-7 EXPANDS beyond seam v) and §12(g) item-2 keys machinery-fault on the explicit signal not the generic blocked/degraded label (re-avoiding the r1 F1 conflation); re-review + master expands the m-7 leg before m-2 finalizes v7

m-3.implementer — both F3/F4 confirmed correct and folded into §12. r1 F1/F2 stay closed. No c2/mechanism reopen. Re-review → `DESIGN_REVIEW_VERDICT`.

### F3 (blocker) — the v7 capability change is real; §12 no longer calls it untouched — FOLDED
You're right — the closed-compatibility proof depends on the reader supporting v7, and that is an owner-governed capability-table change, not incidental. §12(e) + the TRIPWIRE line + the m-7 handshake now state:
- The closed enforcement **requires an m-7-OWNED capability-table transition adding `s8-fieldspec-v7`** (marker-first refusal, reader-first sequencing, forward-only — rollback/skip rejected).
- **TRIPWIRE preserved:** the capability exact-set **moves ONLY through m-7's owner relation** — no m-3/m-2 proxy freeze-break (the genesis v5 SHA / FX-CFG-7 / catalog pin stay untouched).
- **LEG m-7 EXPANDS beyond composition-seam-(v)** to own this transition — **routed to master** (see flag); m-7 confirms the exact supported set + the forward relation + the implementation locus (the branch knows only v5→v6 today, `config.go:225-226`).
- **Both proof sides pinned:** (i) a v6-only reader **refuses a v7 store before partial interpretation**; (ii) a v7-capable reader **starts, reads a present declaration whose check would fail, and REJECTS** — never silently absent; + the stale-v6-form `re-render` leg.

### F4 (blocker) — machinery-fault ≠ generic blocked/degraded — FOLDED (owned: this was the r1 F1 conflation re-appearing)
You're right, and I own it — my r2 aggregation collapsed the two axes I'd already split in r1. §12(g) corrected:
- **item 2** now keys on the **explicit conductor-classified machinery-fault signal** (actual timeout / internal check-machinery failure, the typed fault-reason), **NOT** the generic `blocked/degraded` predicate label.
- **item 3** now carries `skipped`/`unsafe` **AND a `blocked/degraded` claim WITHOUT the machinery-fault signal (genuine no-vantage)** → the record-integrity path (decision-② for authority-class) — distinct from the machinery-fault edge.
- **fixtures** distinguish all three: pass+`unsafe` · pass+genuine-machinery-fault · observed-false+genuine-machinery-fault · pass+genuine-no-vantage-blocked. Matches the current gate's `MachineryFault`-before-`Blocked/Degraded` ordering (`gate.go:120-137`) + registry `unsafe`+`Blocked` split (`registry.go:105-149`).

### FLAG TO MASTER (routing per F3 point 2)
The F3 fix **expands LEG m-7 beyond composition-seam-(v)** to include the **owner-governed v7 capability-table transition**. **master.orchestrator-planner:** please expand/correct the m-7 leg accordingly (and this rides with the r2 Rail-A parent-correction already flagged). **m-2.planner: continue to HOLD the v7-byte finalize** until (a) master expands the m-7 leg + m-7 confirms the exact v7 supported-set/forward-relation, (b) master ratifies the Rail-A split (r2 flag), and (c) m-3.implementer re-approves this r3.

### Adversarial targets (r3)
- (a) Is the TRIPWIRE argument sound — the capability set moving through m-7's owner relation is the *sanctioned* path (not a freeze-break), while the genesis/FX-CFG-7/catalog pins stay frozen?
- (b) Does item-2's "explicit machinery-fault signal" key correctly distinguish timeout/internal-failure from a genuine-no-vantage `blocked` — no residual r1-F1 conflation anywhere in §12(g)?
- (c) Both v7 proof sides testable as written (v6-refuse-pre-interpretation + v7-read-and-reject)?
- (d) Still no c2/mechanism reopen; byte-exact enum; interim/s10 untouched; scope = declaration surface only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-claim-input-m3/DESIGN-planner-20260712-015500.md` — run below.
- Anchors: the review `…-014200` (F3/F4 + cited `config-host.md:30-38`, FX-CFG-8/11 `:177-180`, `config.go:145-184,225-226`, `gate.go:120-137`, `registry.go:105-149`); §12(e)/(g) r3; the TRIPWIRE line; the m-7 handshake.

ACTIONS_GIT_REF: none — design-amendment revision; no `frank/` edit, no code. Artifacts: the §12(e)/(g) + TRIPWIRE + m-7-handshake r3 folds + fold-log + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (build-lane `s8-observe-spine@3cce8cd` is the s8 pair's).
Next requested action: m-3.implementer re-review r3 → `DESIGN_REVIEW_VERDICT`; master expands LEG m-7 (v7 capability) + ratifies the Rail-A split; on all, I return the reviewed §12 TO master → m-2 finalizes v7 bytes + m-7 confirms the expanded leg + master folds the T9 grant. T9 held; s8 build runs T10 meanwhile.
