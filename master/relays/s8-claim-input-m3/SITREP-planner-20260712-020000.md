## SITREP — m-3.planner RETURNS the approved LEG m-3 semantics (§12, r3 implementer-approved) to master; two master gates owed (ratify Rail-A parent-correction + EXPAND LEG m-7 for the v7 capability) before m-2 finalizes v7 + T9

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-claim-input-m3
PARENT_DISPATCH_ID: s8-claim-input-amendment
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-3.implementer, s8.planner, s8.implementer
IN_REPLY_TO: s8-claim-input-m3/DESIGN-REVIEW-implementer-20260712-016200.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: LEG m-3 (executable-claim declaration surface) APPROVED r3 and returned — deterministic + fail-closed + no c2 reopen; two owed master actions (Rail-A parent-correction ratify + LEG m-7 expansion to the v7 capability transition) gate m-2's v7 finalize; m-2's bytes finalize against §12

Returning the approved m-3 semantics leg, as the amendment dispatch (`s8-claim-input-amendment/…-004511`) directs. **LEG m-3 = APPROVED** by m-3.implementer at r3 (`…-016200`, `DESIGN_REVIEW_VERDICT: approve`; the loop was r1 must-revise → r2 must-revise → r3 approve). The semantics live in **§12** of `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`.

### What §12 locks (the finalized declaration surface)
- **Shape/cardinality:** `(claim_ref, check_id, params)` seat-owned `executable_claims` **row_array**, many-per-record; `claim_ref` non-empty/bounded/symbolic/**unique** (duplicate ⇒ typed reject + observe-time refusal).
- **§6.1 validation:** registry-schema-aware **fill-time** + authoritative **observe-time** pre-spawn refusal.
- **R2:** input non-gate-referenceable (selection = intent); conductor-observed output drives disposition.
- **(h)-guard:** seat-owned `executable_claims` input ↔ system-owned `executable_claim_results` output.
- **Aggregation (§12g):** canonical row order, **run-all**, one result row/declaration, **worst-wins precedence** — observed-false→`rejected` > **explicit** machinery-fault-signal→`held`/`rejected` > skipped/unsafe **and genuine no-vantage-blocked**→record-integrity path > all-pass→`accepted`@max-rung.
- **Rail A (§12e):** absence = OPEN (honest `Evaluate:nil` degrade); a **present declaration = CLOSED/fail-closed** via the governed `v6→v7` transition + the m-7 v7 capability ceiling (no silent-ignore path).
- **Scope:** no s9 adjudication, no new terminal, byte-exact `{accepted, rejected, held}`, interim defaults/s10 sunset untouched, TRIPWIRE honored.

### TWO owed MASTER actions (both surfaced during review; both gate m-2's v7 finalize)
1. **Ratify the Rail-A parent-correction (r2/F1).** The parent dispatch prescribed additive/open (`…-004511:21`); §12(e) corrects it to **absence-open / present-closed**. Please ratify this as the amendment's Rail-A of record.
2. **EXPAND LEG m-7 beyond composition-seam-(v) (r3/F3).** The closed-compatibility proof **requires an m-7-owned capability-table transition adding `s8-fieldspec-v7`** (marker-first refusal, reader-first, forward-only). LEG m-7's dispatch scoped only seam (v); it must expand to own this capability transition. m-7 then confirms: the exact supported set, the forward relation, the implementation locus (the branch knows only v5→v6 today, `config.go:225-226`), and both phase-0 proof legs. TRIPWIRE preserved — the set moves **only through m-7's owner relation**, no proxy break.

### Downstream sequence (for your reconcile)
master ratify (1)+(2) → **m-7 confirms the expanded capability leg** → **m-2 finalizes the v7 bytes against §12** (the seat-owned `executable_claims` row_array, `layer_present:observe`, `gate_referenceable:false`, registry-schema-aware fill-validation) → **master reconciles the three legs + issues the bounded T9 fold/grant**. **T9 stays held; the s8 build runs T10 in parallel.** The exit condition is unchanged and executable: `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate` turns GREEN on the real production path once the declaration writer + evaluator land, or the slice does not exit.

### Status
- Done (m-3): the declaration-surface semantics — **APPROVED + returned.** m-3's leg of the amendment is complete pending the external reconcile.
- Not done (yours + owners): the two master actions above; m-7's capability confirm; m-2's v7 byte-finalize; the T9 grant.
- Holding. No PLAN (design amendment).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-claim-input-m3/SITREP-planner-20260712-020000.md` — run below.
- Approval: `s8-claim-input-m3/DESIGN-REVIEW-implementer-20260712-016200` (F3/F4 closed, full amendment check passed). §12(e)/(g) + the TRIPWIRE line + the expanded m-7 handshake in the design doc.

ACTIONS_GIT_REF: none — a design-completion return; no git action, no `frank/` edit, no code. Sole artifact: master/relays/s8-claim-input-m3/SITREP-planner-20260712-020000.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (build-lane `s8-observe-spine@3cce8cd` is the s8 pair's).
Next requested action: master ratifies the Rail-A parent-correction + expands LEG m-7 (v7 capability); m-7 confirms; m-2 finalizes v7 against §12; master reconciles + folds the T9 grant. m-3.planner holds — report-only until addressed in TO.
