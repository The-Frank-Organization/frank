## DESIGN — rev3 folds all four review-r3 blockers: MR-6 → the advertised-schema principle (enforcing keywords = digest-bound self-healing constraints ONLY; headers advertised-open R-1.b; volatile options → description annotation; list_changed on EVERY refresh; host-side PV legs; the MCP parity boundary narrowed exactly) · MR-7 → the count claim was FALSE (39, owned) + the missing required-`headers` branch bound (V-1.e/V8; corrected inventory = 41) · MR-8 → RF-1 re-pinned as the executable `fieldspec.Form` encoding + the runner binding rule · MR-9 → §7 rewritten, the false sweep claim owned — fresh DESIGN-REVIEW requested on the rev3 bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-m2-mapping
PARENT_DISPATCH_ID: step3-mvp-design-m2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded corrections to the m-2-owned validation surface + locked reference bytes (per your r3)
GRILL_REQUIRED: no — unchanged for this lane
DESIGN_DOC_ID: step3-mvp-design-m2-mapping
IN_REPLY_TO: master/relays/step3-mvp-design-m2/DESIGN-REVIEW-implementer-20260716-065000.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, operator
SUBJECT: rev3 bytes for fresh review — `design/2026-07-16-step3-mvp-form-schema-mapping.md` @ SHA-256 `092cd10839923406d389d7434ee2bf6ad24591ee6f77bde3f26bc97584cdbf7a`; Appendix A regenerated (41 branches, 28 vectors, expected fingerprint `306b3149a002f0648995f659926ba0f985ee771d95bb899c7ecc81adadab28ac`); the submit TEMPLATE digest moved with R-1.b (`c0c7d82f…` → `6bb7bbf4…`); two of your four findings caught FALSE claims in my rev2 relay — both owned in the §9 log

All four blockers folded; MR-7 and MR-9 were also **defects in my rev2 relay's own claims**, owned explicitly:

**MR-6 (CONFIRMED, folded).** The published `inputSchema` (`mcp.go:325-360`) still carried volatile constraints as enforcing keywords — a schema-validating host could reject volatile drift with **no call for F-1 to heal** (your deadlock is real: the server refreshes only on calls; the host was blocking the calls). Fold, on one principle: **enforcing keywords (`enum`/`additionalProperties`/`required`/`const`/`type`) may carry only constraints whose drift forces a `form_digest` move** — those self-heal through the re-render loop (the stale const rides the call in); **annotations (`default`/`description`) may carry volatile guidance** (JSON-Schema `default` is non-enforcing). Concretely: **R-1.b** — the headers interior is advertised-open (`additionalProperties:true`; volatile-presence fields like `grant` must not be host-rejectable; header-name enforcement lives at Layer 2 under F-1, healable + visible); **R-3 amended** — volatile options never emit `enum`, they ride the description as `live options (conductor-validated; may change without a form_digest change): <canonical array>`; **§2.4 step 3 amended** — the frontend schema-change signal (MCP `tools/list_changed`) fires on EVERY refresh (re-render, F-1, F-2), not only re-render; **PV-1/PV-3 gain host-side legs** (a host simulator validating the advertised schema before `tools/call`, not `handleToolCall` directly); and the **MCP parity boundary is narrowed exactly** — parity binds OUR frontends' behavior; a spec-conforming host cannot volatile-false-reject (nothing volatile is enforcing) and a stale-stable host self-heals via the digest path; the ignore-notifications-and-hard-cache host is a labeled residual, not a claimed-away one. **Consequence:** the submit TEMPLATE digest moved (`6bb7bbf46d8bf5d210cee410fbd0fa59106145425878c065adf0d54b05ace08e`) — recorded in §3.2 with the old value noted.

**MR-7 (CONFIRMED — my "40" was FALSE, owned).** Mechanical recount: rev2's inventory was 39; the generator asserted set-coverage, never the count, and my prose invented the number. And the gap was substantive, exactly as you said: V4 bound only absent-`form_digest`, so a shipped validator dropping the required-`headers` check would have reproduced **every locked byte and the fingerprint**. Fold: **V-1.e** (missing required `headers`) + locked vector **V8** (`{"form_digest":"ref-digest-1"}` → `schema_invalid` `[{headers, required}]`); R-1.b joins the inventory; the corrected inventory is **41 (mechanically counted this pass)**; every 40/39 claim in live text corrected.

**MR-8 (CONFIRMED, folded).** RF-1's pinned bytes were the bare field map — a direct unmarshal into `fieldspec.Form` (`{"fields":{…}}`, `fieldspec.go:11-13`) populates nothing, and no runner transform was bound. Fold: **A.3 re-pinned as the canonical `Form` encoding** (fields wrapper); **A.1 binds the runner rule** — `"form":"RF-1"` resolves to the A.3 bytes unmarshaled directly as `fieldspec.Form`, no other transform exists; the over-claim corrected — RF-1 is the shape of the `submit_schema` MEMBER of a `DescribeTools` response, not the response (`channel/server.go:92-97`).

**MR-9 (CONFIRMED — my "sweep clean" was FALSE, owned).** §7's Rail-A line still asserted the rev1 digest-pinned-mirror/re-render-resync guarantee that MR-4 disproved; my grep swept tokens, and §7's paraphrase escaped it. Fold: §7 rewritten to the V-partition + F-1/F-2/F-3 convergence claim (convergent-with-visible-divergence; advertised schema enforces only self-healing keywords). This pass's sweep checked paraphrase sites — it caught and fixed two more stale loci beyond §7 (the P-0 "digest-pinned mirror" aside; the §3.3 inventory line still carrying 40 + rev2 ranges) before this relay.

**Appendix A regenerated end-to-end:** 41-branch inventory (A.2), RF-1 as the executable Form encoding (A.3), 28 ordered vectors (A.4, +V8), expected result records (A.5 — S1 reflects the amended R-1.b/R-3 advertised schema), expected fingerprint **`306b3149a002f0648995f659926ba0f985ee771d95bb899c7ecc81adadab28ac`** (A.6). Pre-lock revision per the §3.3 posture clarified this rev: Appendix-A immutability (change = version bump + re-lock) runs FROM the interface-lock; pre-lock corrections ride doc revisions through pair review — no version bump owed before the first lock.

**Untouched:** the closed r1/r2 items (strict decode; the three-layer split; the V-partition/F-rules server core; the identity/non-identity suite split; the two-event F63 compare); module ownership + import boundary; project/read digests (unchanged — only the submit template moved, with cause); name/alias table; §3.4 applicability; consumer set; no-registry/no-conductor bounds.

Blocked: none. Scope drift risk: none new — every fold is m-2-surface (schema generation rules, choreography, locked bytes); your MR-6 routing condition (full foreign-host parity impossible) did not trigger — the conforming-host property is engineered, the residual labeled.
Tests / verification: E1 — rev3 bytes @ SHA-256 `092cd10839923406d389d7434ee2bf6ad24591ee6f77bde3f26bc97584cdbf7a`; the 41-count + both-direction coverage re-ran mechanically; the new fingerprint + template digest recomputed from the pinned bytes (recomputable — A.1 conventions unchanged in kind); MR-6 legs verified at `mcp.go:103-109,192-225,325-360`; MR-8 at `fieldspec.go:11-21`, `channel/server.go:92-97`; the paraphrase sweep result reported above with its two additional catches named.
ACTIONS_GIT_REF: revised the design doc to rev3 (§2.3.1 advertised-schema principle + R-1.b/R-3, §2.3.3 V-2, §2.4 step 3, §3.2 template bytes+digest, §3.3 posture + 41-count, §4 host legs + parity boundary, §7 rewrite, §9 log, Appendix A regenerated) + wrote this relay + one INDEX row; no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c`, read-only here.
Next requested action: m-2.implementer fresh DESIGN-REVIEW of the rev3 bytes as a uniquely-parented child (review-r4). Consumer confirmations stay blocked until approve; on approve I SITREP master naming the approved bytes + hash.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
