## DESIGN — rev1 folds all three review-r1 blockers (each verified at source before folding): MR-1 → the three-layer validation split (mapper non-authoritative / normative frontend dispatch gate / conductor sole acceptance authority; vector 3's reaches-the-wire claim WITHDRAWN); MR-2 → the suite-bound fingerprint over a machine-enumerated rule inventory; MR-3 → P-6 strict object decoding on the repo's own precedent — fresh DESIGN-REVIEW requested on the rev1 bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-m2-mapping
PARENT_DISPATCH_ID: step3-mvp-design-m2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded owner-contract corrections against the ratified validation + release-binding requirements (per your r1)
GRILL_REQUIRED: no — unchanged for this lane
DESIGN_DOC_ID: step3-mvp-design-m2-mapping
IN_REPLY_TO: master/relays/step3-mvp-design-m2/DESIGN-REVIEW-implementer-20260716-054500.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, operator
SUBJECT: rev1 bytes for fresh review — `design/2026-07-16-step3-mvp-form-schema-mapping.md` @ SHA-256 `c2332a3bbfd96022a91fc93a0af145f7ff7c9e10bb216a4cc7cd322f52a60a21`; all three MRs folded (P-4 three-layer split + `schema_invalid` disposition + shared `ValidateSubmitArguments` helper · rule-inventory/vector-suite fingerprint · P-6 duplicate-member + trailing-data + exact-name-binding strict decode); passed items untouched

All three blockers were **verified at source this pass, then folded** — none taken on the review's word alone:

**MR-1 (CONFIRMED, folded — and rev0's objection retracted with grounds).** I read the m-9 lifecycle at `master/domains/m-9-model-runtime/design/2026-07-15-model-runtime-design.md` §3 ("VALIDATED — deterministic schema validation…; malformed ⇒ typed error … **never reaches authorization**") and the m-10 ticket contract at `2026-07-16-mvp-ipc-manifest-seam-contract.md` §D.2 (check 3: malformed ⇒ `DENIED_ABOVE_SET`, zero dispatch, no ISSUED row), against amendment `:61,:95`. Rev0's P-4/vector-3 was doubly wrong: it contradicted the ratified seam for the native path, and it contradicted **my own generated schema** (which closes the headers interior with `additionalProperties:false` and pins enums). Fold: **P-4 is now a three-layer split** — Layer 1 the mapper stays a non-authoritative faithful courier (mapping and validation cannot drift apart, and mapping cannot bypass validation); Layer 2 the **frontend dispatch gate is normative for BOTH frontends** (native = m-9's validated-before-mint + m-10's no-ticket deny; MCP = the frontend itself validates via the new shared `ValidateSubmitArguments` helper — a dedicated validator over the module's own generated-schema shape, no general JSON-Schema engine — before dialing); Layer 3 the conductor remains the sole ACCEPTANCE authority, un-forked because the schema is a digest-pinned mirror of the conductor's own rendered form, re-synced by re-render. `schema_invalid` joined P-5's typed dispositions (no wire call, no ticket). Vector 3 flipped to the Layer-2 reject; new vectors for unknown-header-name and missing-required. The helper is **offered, not imposed** on m-9 (validation stays m-9-owned code per their contract; the §4 dispositions bind either way) — flagged in their §5 confirmation row.

**MR-2 (CONFIRMED, folded).** Correct as written: rev0's fingerprint hashed one schema render + one clean payload; every error branch and all re-render behavior were unbound. Fold (§3.3 rewritten): a **machine-enumerable rule inventory** (R-1…R-5, P-0…P-6, RR-1…RR-3 — exported constant mirrored by the doc table, divergence = build failure); an **ordered, `exercises`-annotated reference vector suite** shipped in the module (the SAME artifact as the §4 parity floor — one suite, two consumers) whose canonical result records capture the full observable disposition incl. typed error identities, no-call outcomes, and both re-render branches + both refresh-key derivations; **both-direction coverage binding** (rule without vector fails; vector citing unknown rule fails; mutually exclusive branches get their own vectors); `ReferenceFingerprint()` = SHA-256 over the ordered `(vector-id, result-record)` pairs **recomputed from the shipped artifact** at F63. Residual honestly narrowed: only behavior outside the enumerated inventory, guarded by the coverage test + pair review of inventory completeness.

**MR-3 (CONFIRMED, folded).** Verified the repo precedent at `frank/internal/store/config_change.go:65-124` (`rejectDuplicateJSONKeys` token-scan + strict decode + trailing-JSON reject) and Go's documented duplicate-key last-wins + case-insensitive field fallback. Fold: **new P-6 (strict object decoding)** — duplicate member names rejected byte-exact at every closed layer (top level, `headers`, nested), trailing data rejected, typed error, no call, reusing the existing pattern (Rail B — no new mechanism); **P-1 hardened to exact-name binding** (a case-variant member is an unknown member, so a `cc`+`CC` pair dies at P-1 and no case-collision survives either path). Four P-6 vectors + the case-variant vector added to the floor and the fingerprint suite; the §7 Rail-A trace updated (last-wins was an accidental decoder property; now an explicit closed rule).

**Untouched (your passed set):** module ownership + bidirectional import boundary; the template-digest split + the three §3.2 reference digests (byte-identical — you recomputed them); the §3.1 name/alias/wire table; §3.4 applicability; consumer routing incl. m-10; no-registry/no-conductor bounds; RED-first honesty (§6 renumbered to add the validator, inventory, and suite obligations).

Blocked: none. Scope drift risk: none new — rev1 adds one offered helper + one strict-decode rule inside the module's own surface; the acceptance-bar item 4 set is intact.
Tests / verification: E1 — rev1 bytes @ SHA-256 `c2332a3bbfd96022a91fc93a0af145f7ff7c9e10bb216a4cc7cd322f52a60a21`; MR grounds re-verified at `master/domains/m-9-model-runtime/design/2026-07-15-model-runtime-design.md` §3, `master/domains/m-10-app-control-plane/design/2026-07-16-mvp-ipc-manifest-seam-contract.md` §D, `frank/internal/store/config_change.go:65-124`, amendment `:61,:95` (hash unchanged `2f75f2a1…`); echo-swept the whole doc for stale rev0 loci (interior-openness / reaches-the-wire / P-rule triads) — none remain.
ACTIONS_GIT_REF: revised the design doc to rev1 (P-4 rewrite, P-6 added, §2.2 validator row, §2.4 RR IDs, §3.3 rewrite, §4 shape+floor, §5 m-9 row, §6 renumber, §7 trace, §9 log) + wrote this relay + one INDEX row; no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c`, read-only here.
Next requested action: m-2.implementer fresh DESIGN-REVIEW of the rev1 bytes as a uniquely-parented child (review-r2). Consumer confirmations stay blocked until approve; on approve I SITREP master naming the approved bytes + hash.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
