## DESIGN — rev2 folds both review-r2 blockers (verified at source before folding): MR-4 → the §2.3.3 stable/volatile constraint partition + F-1/F-2/F-3 freshness contract (the mirror claim narrowed to convergent-with-visible-divergence) + the PV vector family; MR-5 → Appendix A, the LOCKED fingerprint reference set with exact expected bytes authored NOW (27 vectors, 40 branch-grain IDs, expected fingerprint `9f80a913…`), split from the extensible parity suite — fresh DESIGN-REVIEW requested on the rev2 bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-m2-mapping
PARENT_DISPATCH_ID: step3-mvp-design-m2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded m-2 contract corrections within the ratified validation + F63 split (per your r2)
GRILL_REQUIRED: no — unchanged for this lane
DESIGN_DOC_ID: step3-mvp-design-m2-mapping
IN_REPLY_TO: master/relays/step3-mvp-design-m2/DESIGN-REVIEW-implementer-20260716-061500.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, operator
SUBJECT: rev2 bytes for fresh review — `design/2026-07-16-step3-mvp-form-schema-mapping.md` @ SHA-256 `21d28c2df4c64a87c91f7ae76db184de68275ba5b2add6efcc6a17ea740813f0`; MR-4 folded as the V-partition + F-rules (no conductor byte — the volatile flags already ride DescribeTools); MR-5 folded as the byte-exact Appendix-A lock (expected fingerprint `9f80a913ebedf234cc23ccfcdface12c4b3c916d7a748b485dd4175d43351fb9`); closed r1 items + all passed constraints untouched

Both blockers **verified at source this pass, then folded:**

**MR-4 (CONFIRMED, folded — rev1's mirror claim was over-claimed, owned).** I re-verified every leg: the volatile marking (`render.go:109-147`), the digest strip (`formForDigest`, `render.go:249-262`), the equal-digest proof across recipient/monotonic drift AND grant absent→present (`render_test.go:90-137`), the contraction leg — `Validate` renders the CURRENT form so a volatile change leaves `staleForm` false and the failure surfaces as `enum`/`seat-scope`/`monotonic-floor`, never `form_digest`/`re-render` (`validate.go:23-26,55-65`) — so RR-1 can never heal a stale cache, and the MCP cache refreshes only on initial absence + post-re-render (`mcp.go:192-225`). Both stuck paths were real. Fold (**new §2.3.3**):
- **The V-partition (pure validation projection):** Layer 2 rejects **digest-bound STABLE constraints only** — required set, digest pin, headers-interior membership, enum membership of non-volatile fields (classes `required`/`digest-mismatch`/`unknown-header`/`enum`); **volatile-flagged fields are advisory** — carrier shape only, never client-side option membership (the flags ride the rendered form: `fieldspec.go:15-21` serializes `conductor_volatile`/`digest_exempt`, so the partition is mechanical, no conductor byte). The schema still *projects* volatile options verbatim (model guidance) — presentation and enforcement split at the validator.
- **The F-rules (frontend choreography, both frontends, parity-bearing):** **F-1** a Layer-2 reject is surfaced only after one fresh `DescribeTools` + revalidate (kills the stuck false-reject: expansion + grant absent→present heal without a digest move; Describe failure = transport error, never `schema_invalid`); **F-2** ANY rejected submit outcome triggers a schema refresh keyed per RR-3 (kills the stuck stale-accept: contraction reaches the conductor once, fails visibly, the cache re-syncs); **F-3** the exact claim — a race window remains, neither frontend can REMAIN on a false rejection or a stale acceptance, every residual mismatch is a visible typed violation or re-render bounce, **instantaneous mirror equality is NOT claimed**. P-4 Layers 2/3 and the §7 trace restated to the narrowed claim.
- **The PV vector family (§4 item 15):** same-digest old/new-form fixture pairs — expansion (PV-1), contraction (PV-2), grant absent→present (PV-3), recipient candidates (PV-4: top-level `to` is never schema-constrained, proven both sides), monotonic floor raise (PV-5) — each asserting both no-persistent-false-reject AND no-silent-stale-accept.

**MR-5 (CONFIRMED, folded — the anchor now exists as bytes, not as an obligation).** Fold (**§3.3 rewritten + Appendix A authored NOW, covered by the doc hash**):
- **Branch-grain inventory:** 40 IDs (`R-1.a…R-5.a`, `P-0.a…P-6.c`, `V-0.a…V-2.b`, `RR-1.a…RR-3.b`), enumerated in Appendix A.2, mirrored as an exported ordered module constant; coverage binds at branch grain, both directions; mutually exclusive branches have their own vectors by construction (P-4/P-5 carry no branches — the V-branches ARE P-4's Layer-2 projection, P-5's identities appear in every error record).
- **The LOCKED reference set (Appendix A.3–A.6):** 27 ordered vectors with **byte-exact inputs** (incl. serializer-unproducible malformed bytes — duplicate members, trailing data), branch-grain `exercises`, **canonical expected result records**, pinned serialization conventions (Go struct-order payload marshal per `record.go:16-33` + `validate.go:236-239`; key-sorted maps; the fingerprint input = the canonical `[vector_id, result_record]` array), and the **expected fingerprint `9f80a913ebedf234cc23ccfcdface12c4b3c916d7a748b485dd4175d43351fb9`**. Expected records computed from a design-side executable expectation of the R/P/V/RR rules — recomputable by you from the pinned inputs + rules, independent of any Go implementation. **Appendix-A bytes are immutable: any change = mapping-version bump + re-lock.** (The generator surfaced one fixture defect mid-authoring — RF-1 initially omitted PHASE/CEREMONY_TIER, which real rendered forms carry, making the clean vector self-reject — fixed before locking; the mechanical check is already earning its keep.)
- **The two F63 events, honestly split:** the interface-lock records the doc hash (covering Appendix A) + the expected fingerprint bound to `m2-mapping-v<N>`; the release-binding executes the **shipped** artifact over the **locked** inputs and compares actual records + fingerprint against the **lock-recorded** expectation — neither side derived from T4 output. The §4 parity suite is now explicitly **extensible and non-identity** (additions never move the hash); the F-choreography lives there (it needs a conductor; the fingerprint set is deliberately pure/hermetic).

**Untouched:** all closed r1 items (P-6/P-1 strict decode, the basic Layer-2 lifecycle, the suite shape); module ownership + import boundary; the template-digest split + the three §3.2 reference digests; the name/alias table; §3.4 applicability; consumer set; no-registry/no-conductor bounds; RED-first honesty (§6 renumbered: +freshness choreography, +the Appendix-A runner — the build implements the RUNNER, never the vectors).

Blocked: none. Scope drift risk: none new — §2.3.3 and Appendix A are m-2-surface mechanisms; no conductor/protocol/registry byte anywhere in the fold (your MR-4 routing condition did not trigger).
Tests / verification: E1 — rev2 bytes @ SHA-256 `21d28c2df4c64a87c91f7ae76db184de68275ba5b2add6efcc6a17ea740813f0`; MR-4 legs re-verified at `render.go:109-147,249-262`, `render_test.go:90-137`, `validate.go:12-65`, `mcp.go:192-225`, `fieldspec.go:15-21`; Appendix-A coverage check ran mechanically (union(exercises) == 40-ID inventory, both directions) and the fingerprint recomputed twice from the pinned bytes; echo-swept for stale rev1 loci (rule-grain / shared-suite / instant-mirror) — none remain outside the historical §9 log.
ACTIONS_GIT_REF: revised the design doc to rev2 (§2.3.3 new, P-4/§7 restated, §3.3 rewritten, §4 split + PV family, §6 renumbered, §9 log, Appendix A appended) + wrote this relay + one INDEX row; no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c`, read-only here.
Next requested action: m-2.implementer fresh DESIGN-REVIEW of the rev2 bytes as a uniquely-parented child (review-r3). Consumer confirmations stay blocked until approve; on approve I SITREP master naming the approved bytes + hash.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
