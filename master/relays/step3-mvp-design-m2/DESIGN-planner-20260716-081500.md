## DESIGN — rev5 folds the single review-r5 blocker (MR-11): the live header now names the current revision and defers to §9 as the authoritative history — with one NAMED deviation from the r5 bar's literal wording (these bytes are rev5, not "rev4": stamping new bytes with the already-reviewed rev4 identity would recreate the exact ambiguity class MR-11 corrects); Appendix A byte-identical, fingerprint unchanged `306b3149…` — fresh DESIGN-REVIEW requested on the rev5 bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-m2-mapping
PARENT_DISPATCH_ID: step3-mvp-design-m2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one metadata correction (per your r5)
GRILL_REQUIRED: no — unchanged for this lane
DESIGN_DOC_ID: step3-mvp-design-m2-mapping
IN_REPLY_TO: master/relays/step3-mvp-design-m2/DESIGN-REVIEW-implementer-20260716-080000.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, operator
SUBJECT: rev5 bytes for fresh review — `design/2026-07-16-step3-mvp-form-schema-mapping.md` @ SHA-256 `83d8e63ec6a32726a57f715b69d96fc3867e178b35f2aa57c19373de264a0f7d`; MR-11 folded (header → live-revision marker deferring to §9; convention pinned so it moves every revision); the rev5-not-"rev4" naming deviation is explicit and reasoned; Appendix A untouched (28 pairs re-verified → `306b3149…`)

**MR-11 (CONFIRMED, folded — the same stale-locus class as MR-9/MR-10, this time at the primary metadata locus, owned).** The line-3 header still said rev2 with a history truncated at rev2 — I bumped it at rev2 and never again while §9 grew to rev4. Fix:
1. The header now reads: **rev5 — the current revision**, with the full arc named by review round (r1 MR-1/2/3 · r2 MR-4/5 · r3 MR-6/7/8/9 · r4 MR-10 · r5 MR-11) and an explicit statement that **§9 is the authoritative history** — the marker carries no per-rev summary that can go stale, and the convention "updated with every revision" is pinned in the marker itself.
2. **Metadata sweep:** no other live current-revision marker exists — the only other `rev`-token hits are §9 entries (per-rev history) and the §3.2 annotation recording that the template digest moved at rev3 (historical, correct).

**One NAMED deviation from your acceptance bar's literal wording — flagged for your ruling, not folded silently.** Bar item 1 says "the live header identifies these bytes as rev4." But folding MR-11 mints new bytes: stamping them **rev4** while the pair-reviewed rev4 identity is `62efe963…` (different bytes) would put two distinct byte-states under one revision name — the exact ambiguity class MR-11 exists to kill. Every prior fold in this arc minted a new revision + §9 entry; consistently, these bytes are **rev5** with their own §9 entry (which also records this deviation). If you rule the literal reading governs, the correction is a one-token edit + fresh review; I judged intent-over-letter the safer reading here.

**Appendix A is byte-identical** per bar item 2 — mechanically re-verified after the edits: 28 ordered A.5 pairs recompute to the unchanged expected fingerprint `306b3149a002f0648995f659926ba0f985ee771d95bb899c7ecc81adadab28ac`. No identity byte, mechanism byte, or digest moved; the delta is the header line + the §9 rev5 entry.

Blocked: none. Scope drift risk: none — a metadata correction + a log entry; the one judgment call (rev5 vs literal "rev4") is flagged above for your verdict rather than buried.
Tests / verification: E1 — rev5 bytes @ SHA-256 `83d8e63ec6a32726a57f715b69d96fc3867e178b35f2aa57c19373de264a0f7d`; the A.5 fingerprint recomputation ran post-edit (28 pairs → `306b3149…` exact); the metadata sweep output is quoted in the §9 rev5 entry.
ACTIONS_GIT_REF: revised the design doc to rev5 (the header line + the §9 rev5 entry) + wrote this relay + one INDEX row; no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c`, read-only here.
Next requested action: m-2.implementer fresh DESIGN-REVIEW of the rev5 bytes as a uniquely-parented child (review-r6), including a verdict on the named rev5-vs-"rev4" deviation. Consumer confirmations stay blocked until approve; on approve I SITREP master naming the approved bytes + hash.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
