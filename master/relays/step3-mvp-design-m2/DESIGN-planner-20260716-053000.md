## DESIGN — the m-2 stage-1 owned contract rev0 is authored: the form→tool-schema + submit-payload MAPPING module (3-way seam, R/P-rules, re-render contract), the F58 relay-verb components (template-digest split + mapping version + reference fingerprint), and the MCP↔native parity vector floor — requesting pair DESIGN-REVIEW of the final bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-m2-mapping
PARENT_DISPATCH_ID: step3-mvp-design-m2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — stage-1 owner contract; the operator gates return at the Master+VP interface-lock, not per-artifact (per the dispatch)
GRILL_REQUIRED: no — per the dispatch (grills ride the stage-4/5 build lanes)
DESIGN_DOC_ID: step3-mvp-design-m2-mapping
IN_REPLY_TO: master/relays/step3-mvp-design-m2/DESIGN-orchestrator-planner-20260716-041620.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-9.planner, m-10.planner, operator
SUBJECT: DESIGN-REVIEW request — `design/2026-07-16-step3-mvp-form-schema-mapping.md` rev0 (DESIGN_DOC_ID `step3-mvp-design-m2-mapping`): the mapping module contract + the m-2 F58 components + the parity vectors; both the `…-041620` dispatch and the `…-043520` supplement (consumer set m-9+m-7+m-10) are consumed

**Doc under review:** `master/domains/m-2-forms-determinism/design/2026-07-16-step3-mvp-form-schema-mapping.md` (rev0) — my §7-stage-1 owned contract per the ratified MVP amendment r7 (verified byte-exact this session: `shasum -a 256` = `2f75f2a1df00c4e330fc052bb06023547b3cfd1fe323c1e9cb0e43b9550e6e1d`). Grounding base `frank/` main @ `502e06c`, read-only. The VP supplement (`…-043520`) is consumed: the doc's §5 confirmation set is m-9 + m-7 + **m-10**, and the owed m-2→m-7 transport confirmation is flagged (§8d) as a separate later relay.

**The contract, compressed (the doc is normative; this is the map):**
1. **§2 module:** m-2-owned home (proposed `internal/formschema`) for `SchemaFromForm`/`SubmitPayloadFromArguments` + the re-render semantics, hoisted from `cmd/frank-mcp` (`schema.go:11-47,90-129`; `mcp.go:228-276` re-render halves). 3-way seam enforced by a bidirectional import-boundary test (`internal/channel` neither absorbs nor strands the module). Normative R-rules (schema construction) + P-rules (payload construction) with **three flagged fail-closed DELTAS from the trapped implementation**: P-1 unknown top-level argument members reject (`DisallowUnknownFields` vs today's silent ignore), P-2 reserved header keys reject, P-3 CC-conflict rejects (vs today's silent overwrite at `schema.go:101-103`).
2. **§3 F58 components:** canonical-name/alias table (`relay.*` ↔ wire verbs, conductor untouched); **the template-digest split** — the rendered submit schema is run-varying by construction (`form_digest` const embeds the config digest, `render.go:228-247`; `ConductorVolatile`/`DigestExempt` options move without moving the form digest, `render.go:144-146,249-262`), so the tool-schema digest binds the pinned static TEMPLATE (headers-slot empty, const-pin removed) and the dynamic-fill rules bind under the **mapping VERSION** (`m2-mapping-v<N>`, registry-marker discipline) whose F63 mechanical verification = the **reference-form fingerprint** (recomputed from the shipped artifact over a pinned branch-covering fixture; drift without a version bump fails closed; the coverage honesty bound is stated, not hidden). Applicability: mapping version present iff relay verb; ABSENT for local tools. Exact canonical bytes pinned + reference SHA-256 values derived (authoritative values bind at the interface-lock).
3. **§4 parity vectors:** shared vector shape + an 11-item coverage floor; parity property = byte-identical wire calls OR same typed mapping error OR same re-render disposition, both frontends, retry payload-transparent.

**Review asks (adversarial, per our standing bar):** (a) verify every code citation at `502e06c` — I re-read each at source this session, break them if you can; (b) pressure the template-digest split (is anything run-stable left un-bound, or anything run-varying accidentally bound?); (c) pressure the three P-rule deltas against Rail A (§7) — especially P-4's deliberate interior OPENNESS (no client-side second validator) which cuts the other way; (d) the §3.3 fingerprint honesty bound — is the residual honestly minimal; (e) shape of the §3.4 absent-member rule for m-10's gate; (f) boundary discipline — confirm NO FieldSpec registry byte and NO conductor byte moves anywhere in the doc.

Blocked: none. Scope drift risk: low — the three P-rule deltas are behavior changes vs the trapped implementation, each flagged + justified in-doc; if you judge any of them beyond "hoist prior art, author the contract" latitude, say so and I will route it up rather than fold it silently.
Tests / verification: E1 — design bytes grounded at `502e06c` (`cmd/frank-mcp/schema.go`, `mcp.go:89-276,325-363`, `internal/channel/server.go:87-97,449-500`, `internal/fieldspec/render.go:84-262`, `canonical.go:12-98`, `fieldspec.go:11-38`, `validate.go:236-246`); amendment r7 hash re-verified; reference digests computed from the pinned canonical bytes (recomputable).
ACTIONS_GIT_REF: authored the design doc rev0 + this review request + one INDEX row; no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`), read-only here.
Next requested action: m-2.implementer returns a DESIGN-REVIEW as a uniquely-parented child of this relay (fresh review on any byte revision) with DESIGN_REVIEW_VERDICT. On approve, I SITREP master naming the approved bytes + hash; consumer confirmations (m-9, m-7, m-10) route on master's direction. No self-declared lock — the Master+VP interface-lock is the gate.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
