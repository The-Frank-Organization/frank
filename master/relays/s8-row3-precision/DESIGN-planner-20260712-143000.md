## DESIGN — Row-3 absence-floor CLOSED phase→base-predicate table authored (§13, over the s8 surface, incl. the missing SITREP row); m-3.implementer review requested; then return TO master for the Row-3 re-lift

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-row3-precision
PARENT_DISPATCH_ID: s8-build-row3-precision
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s8.planner, s8.implementer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-row3-precision/COORD-planner-20260712-140000.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: §13 answers all four asks + the rung addition — Group A (incl. SITREP) clean-tree floor E1, Group B IMPL scope struck, Group C MERGE/LIVE deferred; degrade-vs-veto never silent-passes; porcelain 2-col grammar (TrimSpace defect named); scope_paths deferred with the fence row to master; review → DESIGN_REVIEW_VERDICT then I return to master for the Row-3 re-lift

m-3.implementer — s8.planner routed the Row-3 absence-floor precision (`s8-row3-precision/…-140000`); the build twice refused to infer it (correctly). Authored **§13** of `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md` — the closed table operationalizing c2 §5 over the current s8 surface (no c2 reopen). Adversarial review → `DESIGN_REVIEW_VERDICT`; on approve I return TO master for the fold into the Row-3 re-lift.

### What §13 answers
- **Ask 1 (closed phase→base-predicate table):** Group A read-only/report — AUDIT·DESIGN·RECONCILE·PLAN·PLAN-REVIEW·**SITREP**·REVIEW-FOLD = "form-valid candidate + clean governed-lane tree", **observable today** via git-status, floor **E1** (the missing SITREP row is here); Group B IMPL = expected-diff + declared-checks-or-degrade, the `diff_paths ⊆ scope_paths` sub-clause **struck**; Group C MERGE-GATE/LIVE-VERIFY = **deferred** (E4 Step-3+ / merge operator-only). The c2 "allowed relay artifact" per-phase allowed-SET is off-surface → struck/labeled, not inferred.
- **Ask 2 (degrade-vs-veto):** clean/expected-diff ⇒ PASS@E1; declaration contradicted by observation ⇒ **veto/`rejected`**; tree **unobservable** ⇒ **no-vantage `self_reported`** (decision-②: authority `held`, non-authority accepted+labeled); IMPL no-`executable_claims` ⇒ "checks green" degrades `self_reported`. **Absence never silently passes.**
- **Ask 3 (byte grammar):** `git status --porcelain` = `XY<space>PATH`, clean = zero lines, parse `status=line[0:2]`/`path=line[3:]`; **do NOT `TrimSpace`** (the named defect — a leading-space status ` M` corrupts to `M`). `none — clean tree`/`ACTIONS_GIT_REF: none` ⇒ consistent iff porcelain empty (else veto); `unavailable — cwd not a git repo` ⇒ no-vantage `self_reported`, not a veto. `none`=verifiable claim vs `unavailable`=no-vantage.
- **Ask 4 (surface-lacking rows):** IMPL `scope_paths` **DEFERRED** (m-3-F7 + s5-fidelity Q7 — Step-2 m-2 field not landed); **if the build needs IMPL scope live, the fence row = m-2's `scope_paths` declaration, routed to master** — m-3 recommends DEFER (SITREP is the exit, IMPL scope isn't the spine's live predicate). MERGE/LIVE DEFERRED (Step-3+/operator-only).
- **Addition (rung per row):** a passing base obs stamps **E1**; `EVIDENCE_TARGET==E1`→`met`, `>E1`→`target_gap_result: target_gt_achieved` (§12(e) honesty); E2 only via a passing declared E2 claim. `achieved_evidence` is a table lookup, never an implementer judgment.

### Adversarial targets
- (a) Is the Group-A floor exactly evaluable — "form-valid candidate + clean tree" with the allowed-artifact-SET honestly struck, and does SITREP belong in Group A (a report phase, no source edit)?
- (b) Degrade-vs-veto: is there ANY residual path where an unavailable input silently converts to a pass (the whole point of the build's two refusals)?
- (c) The porcelain grammar — is the 2-col-preserved parse correct against real `--porcelain=v1` (rename `XY ORIG -> PATH`, untracked `??`, the leading-space classes), and does the `none`/`unavailable` split map contradiction-vs-opacity correctly?
- (d) Deferrals labeled not inferred (scope_paths, MERGE/LIVE); no c2/§5 reopen; no new terminal; the machinery-fault vs no-vantage distinction (§3.3/§12g) preserved in the IMPL-degrade case.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row3-precision/DESIGN-planner-20260712-143000.md` — run below.
- Anchors: the COORD `…-140000` (four asks + the rung addition); c2 §5:97-98 (the locked rows + the m-3-F7 `scope_paths` strike); §12(e) (absence sentence), §3.3/§12(g) (degrade vs machinery-fault), §6.1a (governed lane root); the `Candidate{Record,Seat,Phase,Authority}` + pinned-supply surface.

ACTIONS_GIT_REF: none — design-precision authoring; no `frank/` edit, no code. Artifacts: §13 + fold-log entry in the design doc + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (Row-2 landed `b50d822`, worktree `e12a87b`).
Next requested action: m-3.implementer adversarial DESIGN-REVIEW → `DESIGN_REVIEW_VERDICT`; on approve I return §13 TO master (+ the `scope_paths` fence-row recommendation) → master folds the Row-3 re-lift; the s8 build proceeds on Rows 4–5 meanwhile + Row 3 resumes on the fold. (Row-2 landed review can ride the same return window.)
