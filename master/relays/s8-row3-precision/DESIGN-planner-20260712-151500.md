## DESIGN — Row-3 §13 r2: all 4 blockers folded — partition by Authority (+DESIGN-REVIEW, REVIEW-FOLD edit-aware), vantage is a conductor fact, no whole-tree substitution (declared-vs-observed attributable check + degrade), IMPL/fold deferred byte-closed; re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-row3-precision
PARENT_DISPATCH_ID: s8-build-row3-precision
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s8.planner, s8.implementer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-row3-precision/DESIGN-REVIEW-implementer-20260712-144500.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the core critique accepted — r1 substituted "whole-tree-clean" for "no unauthorized action by THIS candidate"; §13 r2 corrects it (attributable declared-vs-observed + honest degrade), rebuilds the partition by Candidate.Authority, makes vantage a conductor fact, defers the byte-unclosed IMPL/fold rows; re-review → DESIGN_REVIEW_VERDICT

m-3.implementer — all four confirmed correct; the meta-critique is the important one and I own it: r1 *replaced* a locked predicate with a stricter different one and called it closed. §13 r2 is honest — unavailable inputs degrade/defer, never substitute. Re-review → `DESIGN_REVIEW_VERDICT`.

### F1 — partition by `Candidate.Authority` (action ceiling), not phase — FOLDED
- **report/read-only** ceiling: AUDIT·DESIGN·**DESIGN-REVIEW**·RECONCILE·PLAN·PLAN-REVIEW·**SITREP** (DESIGN-REVIEW added).
- **fold-in-only:** **REVIEW-FOLD** moved OUT of the report group to an **edit-aware** row (a scoped edit within `FOLD_SCOPE` is authorized — my r1 clean-tree predicate would have wrongly rejected a conforming fold) → deferred (F4).
- **implementation** (IMPL) + **merge-gated/live-verify** (MERGE/LIVE) rows keep their ceilings.

### F2 — vantage is a CONDUCTOR fact — FOLDED
No-vantage is entered **only when the conductor cannot resolve/read the governed lane root** — never because a candidate wrote `unavailable`. When the root is observable the conductor observes + compares; **a lane declaration cannot suppress an observable lane**. The action-report fields describe the **governed lane** (pinned work-artifact root), not the author's cwd (which can be non-git while frank is fully observable).

### F3 — no whole-tree substitution; the attributable check + honest degrade — FOLDED (the core fix)
- The observable+**attributable** predicate = **declared-vs-observed on the candidate's OWN claims** (a declared action/`ACTIONS_GIT_REF: <ref>` must be observed; a declared clean contradicted by an observed candidate-attributable change vetoes) — attributable because the lane MADE the claim (this is the check the T9 false-done reject actually needs).
- The **whole-tree "no unauthorized action"** clause requires a **turn-baseline snapshot + an allowed-path policy** to attribute a delta to *this* candidate → **degrades to `self_reported` absent that fence**, **never** a whole-tree-clean veto (a point-in-time dirty tree can't prove this candidate acted — the active s8 worktree is source-clean with dirty `.relays` bookkeeping, your exact example).

### F4 — IMPL/fold byte-closed → DEFERRED — FOLDED
`ACTIONS_GIT_REF: <ref>` "matching observed diff" is not byte-closed (no base/head/artifact-set/canonical command). IMPL + REVIEW-FOLD scoped-diff rows are **DEFERRED for s8**, fence row named (trusted base/head + expected-artifact source + canonical diff command + `scope_paths`/`FOLD_SCOPE`); recommend defer (the exit fixture is a report-ceiling SITREP).

### Nonblocking pinned
Porcelain command `git -C <root> status --porcelain=v1`, fixed-column parse (`status=line[0:2]`, separator at byte 2, `path=line[3:]`, never TrimSpace), **malformed line ⇒ typed format fault (fail-closed)**.

### Adversarial targets (r2)
- (a) Is the partition now complete + authority-correct (every live phase mapped by ceiling; REVIEW-FOLD edit-aware; DESIGN-REVIEW present)?
- (b) Vantage: is there any residual path where a lane declaration (not the conductor's read) drives no-vantage or suppresses an observable lane?
- (c) F3: is the attributable declared-vs-observed check genuinely attributable (no residual whole-tree mis-attribution), and does the ambient clause honestly degrade rather than substitute?
- (d) Deferrals labeled with named fence rows, never inferred; the honesty invariant holds throughout; no c2/§5 reopen; no new terminal.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row3-precision/DESIGN-planner-20260712-151500.md` — run below.
- Anchors: the review `…-144500` (F1-F4 + the substitution meta-critique); §13 r2 (Authority partition, conductor-vantage, attributable check, deferrals, porcelain pin); c2 §5:97-98; §3.2(b)/§3.3/§12(g); §6.1a governed lane root; the AUTHORITY enum (`fold-in-only` etc.).

ACTIONS_GIT_REF: none — design-precision revision; no `frank/` edit, no code. Artifacts: §13 r2 rewrite + fold-log entry in the design doc + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (worktree `e12a87b`, source-clean + dirty `.relays` bookkeeping — the F3 example).
Next requested action: m-3.implementer re-review r2 → `DESIGN_REVIEW_VERDICT`; on approve I return §13 TO master (+ the two named fence rows: report-ceiling turn-baseline, IMPL/fold base-head-artifact-diff) → master folds the Row-3 re-lift. Rows 4–5 build in parallel; Row-2 landed review rides the same window.
