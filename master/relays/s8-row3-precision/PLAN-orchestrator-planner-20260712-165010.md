## PLAN — ROW-3 RULING: OPTION 2 ADOPTED (m-3's recommendation, concurred with reasons on the record) — the claimless-report floor is the honest E0 degrade, the T9 false-done exit proof rides a DECLARED `executable_claim` observed-false, and the turn-baseline fence is REGISTERED as named kickoff design item 10 (s9, m-7-inclusive, its own design round) — the exact Row-3 re-lift issues here consuming §13 verbatim; both adopt conditions binding

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-row3-precision-ruling
PARENT_DISPATCH_ID: s8-row3-precision
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a scope ruling + re-lift under the standing conditions (operator CC'd); live adoption and the slice merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: Row 3 (of the five-row re-lift)
FENCE_EXPANSION_AUTHORIZED: granted — ROW 3 RE-LIFT at the named-seam grain: `internal/observe/registry.go` ABSENCE-FLOOR SEAM ONLY (distinct from Row 2's breaker seam in the same file — the fence table maps each row to its seam) + the exact production evaluator seam + `test/fixtures/s8_exit_gate_test.go` + the §13 table/porcelain implementation landing in `checks_base.go`'s git-status machinery OR at most one new `internal/observe/` file, named in the T-report either way; NO other surface licensed
IN_REPLY_TO: master/relays/s8-row3-precision/SITREP-planner-20260712-165000.md
FROM: master.orchestrator-planner
TO: s8.planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-3.implementer, m-7.planner, m-2.planner
SUBJECT: §13 consumed at the doc source (r4-approved, chain r1→r4/F1–F8): the closed Authority-partitioned phase→predicate table · IMPL/REVIEW-FOLD/MERGE-GATE/LIVE-VERIFY = NAMED deferred rows, never inferred · vantage a conductor fact (a lane's "unavailable" never suppresses a readable root) · the pinned porcelain-v1 grammar (fixed columns, byte-2 separator, never TrimSpace, malformed ⇒ typed format fault) · ONE PredicateResult with no substitution — the ruling and its five binding terms below

**THE RULING — Option 2, for three reasons on the record:** (1) it is the honest floor — without a turn-entry baseline the conductor genuinely cannot attribute tree state or prove phase-done-ness for a claimless report, and stamping E1 there would be the label-without-mechanism class this slice has now met seven times, built INTO the floor; (2) the confusion-firewall amendment cuts exactly this way — no new commit-loop mechanism mid-slice when the honest degrade is available and labeled (Option 1 is a real upgrade, not s8 scope); (3) the exit's false-done is BETTER proven by a declared check the conductor ran and watched fail (§12's byte-closed strongest case) than by tree attribution. **Rail-A coherence noted:** the claimless E0 floor extends the §12(e) absence-open posture — absence loses RUNG, never acceptance meaning; authority classes still fail closed via decision-② (`self_reported` ⇒ `held`). **And the dodge is visible, not silent:** a lane avoiding scrutiny by declaring nothing shows an honest `target_gap_result` against its own `EVIDENCE_TARGET`, carries the `self_reported` label, and holds if authority-class — the ratified posture, no new hole.

**BINDING TERMS (the five):**
1. **The floor RUNS — the original defect stays dead:** a claimless record triggers the base observation (the §13 phase-table predicate + the git-provenance read at the governed root); the zero-selection `observe-unavailable`/skip path does not return. What changes vs my r7 implication: the floor's honest yield absent a baseline is **`Degraded`/`self_reported`/E0** — §13 is the precision my r7 row-3 explicitly reserved to m-3 ("the rung a passing base observation stamps"), not a supersession.
2. **The string-veto is live (the class-1 contract at its honest grain):** declared `FINAL_GIT_STATUS_SHORT` vs observed porcelain verifies A STRING — a match yields no rung above E0; a MISMATCH is an observed-false declaration taking the locked §12(g) precedence ⇒ **`rejected` typed**. Fixture leg required: a claimless report declaring a clean tree over observed dirt ⇒ rejected naming the mismatch. (The confident-hallucination catch, verbatim the positioning line — the conductor vetoes what it can SEE without fabricating what it cannot.)
3. **m-3's two adopt conditions, verbatim:** the re-lift + exit fixture **explicitly adopt the E0 degraded claimless floor** (fixture legs: claimless → floor runs → `Degraded`/`self_reported`/E0 + honest target-gap + BOTH decision-② authority legs) · the **observed-false exit proof rides a declared `executable_claim`** (never `ACTIONS_GIT_REF` attribution) — the T9 exit-gate fixture's false-done leg re-anchors accordingly.
4. **The porcelain grammar and the closed table land as pinned:** fixed-column parse, typed format fault on malformed (fail-closed), the four deferred authority classes as NAMED fence rows in the implementation — a deferred phase reached at runtime refuses typed, never infers.
5. **The turn-baseline fence = kickoff DESIGN ITEM 10 (registered this relay):** owner m-3+m-7 (a conductor turn-entry snapshot is commit-loop machinery), lands s9 (the evidence-thicken — its natural home beside item 9), spec basis = §13's table + the Option-1 sketch, and the reviewer's downstream gate carried verbatim: **its own m-7-inclusive design + review before any build — this pair approval pre-approves nothing.** On landing, the claimless floor upgrades E0→E1 through the standing amendment path.

**Process unchanged:** RED-first per the fixture legs above · m-3's row-3 fidelity reviews the LANDED row against §13 (riding the same window as their row-2 fidelity) · Rows 2/4/5 continue in parallel · the five-row gates (exact-head file-captured battery · whole-branch re-review · the refreshed fence table mapping same-file rows to distinct seams) bind after all five land · then the T10/T11 close and the slice-exit package.

ACTIONS_GIT_REF: none — a ruling + re-lift (disk refs: this relay + one INDEX.md row + the kickoff item-10 line, timestamped 20260712-165010).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; the s8 worktree per the pair's last report (source-clean + `.relays` bookkeeping; Rows 2/4/5 in flight).
Next requested action: operator carries this to the s8 pair; the build implements Row 3 per §13 + these terms; master next expects the five-row RED→GREEN report with the m-3/m-7 fidelity returns → the T10/T11 close → the slice-exit package with both tables → the merge-decision relay TO the operator.
