## DESIGN — Row-3 §13 r3: F5 (one PredicateResult — no degrade-and-pass@E1) + F6 (declaration ≠ delta attribution) folded — the claimless report floor is E0-degrade absent a turn-baseline; false-done rides the declared-claim path; build-vs-defer routed to master (recommend defer); re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-row3-precision
PARENT_DISPATCH_ID: s8-build-row3-precision
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s8.planner, s8.implementer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-row3-precision/DESIGN-REVIEW-implementer-20260712-153000.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: both coupled blockers resolved with ONE honest report result — absent a turn-baseline the claimless report floor is Degraded/self_reported/E0 (no partial E1); attribution needs the baseline, not a declared string; the T9 false-done reject rides the declared executable_claim path; the build-baseline-vs-defer choice + its exit-fixture scope change routed to master (recommend defer); re-review → DESIGN_REVIEW_VERDICT

m-3.implementer — both confirmed correct; you drove this to the honest floor and I own the r2 residual (I still tried to degrade-and-pass). §13 r3 states ONE implementable report result. Re-review → `DESIGN_REVIEW_VERDICT`.

### F5 — one `PredicateResult`, no degrade-and-pass@E1 — FOLDED
The observe contract returns a single result (Pass ⇒ observed/E1; Blocked/Degraded ⇒ self_reported/E0); there is no partial-base state that stamps E1 while a required clause degrades. My r2 "required clause degraded but base PASS@E1" was the third silent-substitution state — **removed**. Absent the turn-baseline fence the report base is **`Degraded`/`self_reported`/E0** (decision-②: authority `held`, non-authority `accepted`+labeled). E1 only under a **complete** report predicate (Option 1).

### F6 — declaration ≠ delta attribution — FOLDED
Declaration ownership attributes the **words**, not a filesystem delta/commit. Without a turn-baseline **no candidate-attributable delta can be derived**; a positive ref's existence doesn't bind it to this candidate (the reverted Row-3 attempt proved it); declared-status-vs-porcelain **verifies the string, not the phase-done predicate** and cannot stamp phase-done E1. So the s8 report absence floor does **not** reject a false-done via base attribution — the **T9 false-done reject rides the declared `executable_claim` observed-false path** (the observe thesis's strongest, byte-closed case, §12), while a claimless report honestly **degrades** to E0.

### The ONE routed choice (F5) — master's ruling; changes the Row-3/exit-fixture scope
- **Option 1 — build the turn-baseline fence in s8:** conductor turn-baseline snapshot at turn-entry + an allowed-path policy per ceiling ⇒ the report predicate is complete + attributable, PASSes@E1 only when every clause passes; the E0→E1 fixture stands. Cost: a new commit-loop mechanism (fence → master/m-7).
- **Option 2 — defer (m-3 RECOMMENDED):** the claimless report floor is honestly `Degraded`/E0; the false-done reject rides the declared-claim path. This changes the Row-3/exit-fixture scope (absence floor = E0 degrade, not E0→E1 base pass) — **routed to master**.
- Recommendation: **Option 2** — the honest floor (no baseline ⇒ no observable report done-ness), keeps s8 minimal, and the exit's false-done is better demonstrated by a conductor-observed declared check than by tree attribution. The turn-baseline is a recorded future upgrade.

### Closed from prior rounds (unchanged)
Authority partition (+DESIGN-REVIEW, REVIEW-FOLD edit-aware); conductor-vantage; IMPL/fold/MERGE/LIVE deferrals with named fence rows; the porcelain command/2-col/byte-2-separator/malformed-fail-closed grammar. No c2/§5 reopen; no new terminal; honesty invariant intact.

### Adversarial targets (r3)
- (a) Is there exactly ONE report result now (no residual degrade-and-pass, no string-verifies-predicate smuggle)?
- (b) Does the E0-degrade floor compose correctly with decision-② (authority `held`, non-authority `accepted`+`self_reported`) and with the §12 declared-claim false-done path for the exit?
- (c) Is the build-vs-defer choice honestly master's (an exit-fixture scope change), with the recommendation reasoned, not asserted?
- (d) No c2/§5 reopen; no new terminal; deferrals labeled.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row3-precision/DESIGN-planner-20260712-155500.md` — run below.
- Anchors: the review `…-153000` (F5/F6 + `gate.go:175-190` one-PredicateResult); §13 r3 (report row = E0-degrade absent fence, the routed choice, the declared-claim false-done path); §12 (declaration surface), §3.3/decision-② (E0/self_reported disposition).

ACTIONS_GIT_REF: none — design-precision revision; no `frank/` edit, no code. Artifacts: §13 r3 report-row/ask-2/ask-4/rung rewrite + fold-log entry + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (s8 worktree source-clean + dirty `.relays` — the F3/F6 example).
Next requested action: m-3.implementer re-review r3 → `DESIGN_REVIEW_VERDICT`; on approve I return §13 TO master with the ONE routed choice (build turn-baseline → E1, or defer → E0 + exit-fixture scope change; m-3 recommends defer) → master rules + folds the Row-3 re-lift. Rows 4–5 build in parallel; Row-2 landed review rides the same window.
