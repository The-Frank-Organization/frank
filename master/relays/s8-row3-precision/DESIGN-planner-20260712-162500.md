## DESIGN — Row-3 §13 r4 (narrow): F7 (delete the stale Ask-2 action-ref PASS@E1/veto bullet) + F8 (Ask-3 mapping conditional/field-specific) folded — one rule for the implementer; re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-row3-precision
PARENT_DISPATCH_ID: s8-build-row3-precision
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s8.planner, s8.implementer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-row3-precision/DESIGN-REVIEW-implementer-20260712-161500.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: narrow r4 — the two stale r2 attribution bullets removed/Option-1-scoped so there is ONE rule; F1–F6 stay closed; the master-owned Option-1/2 scope ruling stays separate; re-review → DESIGN_REVIEW_VERDICT

m-3.implementer — the r3 E0 semantic is approvable; the two surviving r2 bullets (F7 Ask-2, F8 Ask-3) that still authorized PASS@E1 from an un-attributable action-ref/`none` are removed/scoped. Narrow byte-consistency fold, no semantic change. Re-review → `DESIGN_REVIEW_VERDICT`.

### F7 — Ask-2 action-ref PASS@E1/veto bullet — FOLDED
Deleted the stale bullet; replaced with the one rule: a declared `ACTIONS_GIT_REF: <ref>` **cannot be bound to this candidate without the turn-baseline** (a resolvable ref proves nothing about who/when — the reverted Row-3 attempt is the demonstration) ⇒ under **Option 2** it is **turn-attribution-unavailable/`self_reported`, cannot pass or veto** the base predicate. The **Option-2 false-done reject rides ONLY a declared `executable_claim` via §12** — never `ACTIONS_GIT_REF` reused as a claim or base-pass proof. Attributable pass@E1 / contradiction-reject is **Option-1-only** (after the complete baseline+allowed-path fence).

### F8 — Ask-3 porcelain mapping conditional + field-specific — FOLDED
- **`ACTIONS_GIT_REF` (turn attribution):** Option 2 ⇒ no baseline ⇒ no attributable delta ⇒ unavailable/`self_reported`, cannot pass/veto; Option 1 only ⇒ baseline derives the delta.
- **`FINAL_GIT_STATUS_SHORT` vs porcelain:** verifies **only the point-in-time status string** — cannot prove phase-done, cannot raise `achieved_evidence` above **E0** (a well-formedness check, not a done-observation).
- **`unavailable`:** never self-certifying — no-vantage only if the **conductor** cannot read the governed root; conductor vantage authoritative.

### Unchanged
F1–F6 closed (authority partition, conductor vantage, IMPL/fold/MERGE/LIVE deferrals, the porcelain fixed-column grammar, the single-result E0 degradation composing with decision-②). The master-owned Option-1-vs-Option-2 scope ruling stays routed + separate from this pair review. No mechanism expansion, no c2/§5 reopen, no new terminal, no new fence.

### Adversarial target (r4)
- One rule only: confirm no residual bullet anywhere in §13 lets an `ACTIONS_GIT_REF`/`none` declaration pass@E1 or veto under Option 2 (I swept §13:275-320 — the only remaining "attributable/pass@E1" mentions are the Option-1-scoped or explicitly-negated ones). No semantic drift from r3.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row3-precision/DESIGN-planner-20260712-162500.md` — run below.
- Anchors: the review `…-161500` (F7 §13:293, F8 §13:301); the corrected §13 Ask-2/Ask-3 bullets; the r3 E0 semantic (report row + routed choice + rung) it now matches.

ACTIONS_GIT_REF: none — narrow design-text revision; no `frank/` edit, no code. Artifacts: §13 Ask-2/Ask-3 bullet rewrite + r4 fold-log entry + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: m-3.implementer re-review r4 → `DESIGN_REVIEW_VERDICT`; on approve I return §13 TO master with the ONE routed Option-1/2 choice (m-3 recommends defer) → master rules + folds the Row-3 re-lift. Rows 4–5 build in parallel; Row-2 landed review rides the same window.
