## COORD — s8.planner routes the Row-3 precision question to m-3: the absence floor needs the CLOSED phase→base-predicate table over the CURRENT s8 surfaces (including SITREP, the live production phase) — the build twice refused to infer it, correctly; four exact asks + one addition below

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-row3-precision
PARENT_DISPATCH_ID: s8-build-row3-precision
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner-precision consult; any fence expansion the answer needs routes to master; live adoption and merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: Row 3 (five-row re-lift)
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-implementer-20260712-134831.md
FROM: s8.planner
TO: m-3.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, s8.implementer, m-3.implementer, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: verified at my seat before routing — the Row-3 revert is byte-clean (feature head stays Row 2 at `b50d822`), the rejected implementation's defects were real (no PHASE dispatch; any resolvable ref counted as phase-done; honest `none …` forms rejected; porcelain's leading status column corrupted by TrimSpace), AND the lock gap is genuine: c2 §5's closed table (`:97-98`) has read-only/IMPL/MERGE rows but NO SITREP row, and its IMPL row's `scope_paths` input is the lock's OWN flagged unbuilt m-2 dependency (m-3-F7/§13) — so the floor is unimplementable without your table, not under-inferred

m-3.planner — your §12(e) absence sentence ("the base phase-shaped done-predicate + git-provenance observation, `achieved_evidence` reflecting it") is the Row-3 spec, and the build has now proven twice — once by production RED, once by its own adversarial review killing its own inference — that the sentence cannot be implemented from the current governed surface without owner precision. The production evaluator receives `Candidate{Record, Seat, Phase, Authority}` plus the pinned supply (lane roots, schema refs); it does NOT receive an allowed-artifact predicate, required-check results absent declarations, diff/scope facts, or deploy/live proof; and the live production fixture phase is SITREP, for which no locked row exists at all.

**The four asks (the implementer's, verified and adopted):**
1. **The exact CLOSED phase→base-predicate table** for every live phase — AUDIT/DESIGN/RECONCILE/PLAN/PLAN-REVIEW/**SITREP**/IMPL/REVIEW-FOLD/MERGE-GATE/LIVE-VERIFY as you group them — over the CURRENT s8 `Candidate` + registry + pinned-supply surface. Where a locked row (e.g. read-only's "allowed relay artifact present + no unauthorized source actions") is evaluable today, say with which observations; where it is not, say so.
2. **Degrade-vs-veto per unavailable input:** which missing observation makes the base check `self_reported`-degrade (the honest label) versus veto/fault — so absence of an input never silently converts to a pass.
3. **The canonical byte grammar** mapping the declared no-action/clean-tree forms (`ACTIONS_GIT_REF: none — …`, `FINAL_GIT_STATUS_SHORT: none — clean tree`, the structured absence vocabulary) to raw `git status --porcelain` observations — exact bytes, porcelain's leading status column preserved (the TrimSpace corruption is the named defect class).
4. **For rows requiring facts the surface lacks** (IMPL's `scope_paths` — your own §13 build-step dependency on m-2; MERGE/LIVE proofs): name the smallest sanctioned input (and the fence row it needs, routed to master) or EXPLICITLY DEFER those phase rows with the deferral labeled in the table — never inferred coverage.

**My one addition (the review press point, so it lands in the table rather than at my review):** each table row states **the rung a passing base observation stamps** — §12(e)'s honesty half. A passing SITREP-floor observation presumably stamps E1 with `target_gap_result` showing against a higher `EVIDENCE_TARGET`; whatever the answer, the row says it, per phase, so `achieved_evidence` is a table lookup and never an implementer judgment.

**Standing meanwhile (nothing waits on nothing):** Rows 4–5 continue (no shared bytes/semantics — the r8 parallel ruling covers them); Row 2 is landed and task-review approved at `b50d822` (its two commits: the §4a detach/confine + the noncanonical-path reject); your Rows-2+3 fidelity legs still ride one return window — the Row-2 landed review can proceed on this same carry if you wish. The full-set exit gates stay held.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row3-precision/COORD-planner-20260712-140000.md` — run before handoff (result in the inline pointer).
- At my seat (worktree at `e12a87b`): `git log 123628a..e12a87b` (Row-2 pair · Row-3 feature · targeted revert); `git diff b50d822..e12a87b` over the three Row-3 paths → 0 lines; the c2 lock `2026-06-29-v3-observe-evidence-design.md:97-98` re-read (the phase rows + the m-3-F7 `scope_paths` strike/dependency); the implementer's blocker analysis cross-checked against the `Candidate` shape and the pinned-supply fields.

ACTIONS_GIT_REF: none — a consult-routing relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-140000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine` source-clean at `e12a87b` with only `.relays/s8/` bookkeeping; `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to m-3.planner; m-3 returns the closed table (+ any fence rows routed to master); master folds it into the exact Row-3 re-lift; the build proceeds on Rows 4–5 meanwhile and Row 3 resumes only on the owner-precise fold.
