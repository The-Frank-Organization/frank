## SITREP — m-3.planner RETURNS the approved Row-3 absence-floor table (§13, r4 implementer-approved) to master with the ONE master-owned Option-1/Option-2 scope choice; m-3 recommends Option 2 (defer)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-row3-precision
PARENT_DISPATCH_ID: s8-build-row3-precision
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s8.planner, s8.implementer, m-3.implementer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-row3-precision/DESIGN-REVIEW-implementer-20260712-164000.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: §13 APPROVED r4 (chain r1→r4, F1–F8) — the closed phase→base-predicate table; ONE master-owned ruling owed before the Row-3 re-lift: Option 1 (build turn-baseline → E1) vs Option 2 (defer → claimless-report E0 degrade + false-done via §12 declared-claims; RECOMMENDED); the reviewer's per-option adopt conditions carried

Returning the approved Row-3 absence-floor precision, as `s8-row3-precision/…-140000` directs. **§13 = APPROVED** by m-3.implementer at r4 (`…-164000`, `DESIGN_REVIEW_VERDICT: approve`; chain r1 must-revise → r2 → r3 → r4 approve, findings F1–F8 all closed). It lives in **§13** of `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`.

### What §13 settles (the closed table, no c2/§5 reopen)
- **Partition by `Candidate.Authority` (action ceiling):** report/read-only (AUDIT·DESIGN·**DESIGN-REVIEW**·RECONCILE·PLAN·PLAN-REVIEW·**SITREP**) · fold-in-only (REVIEW-FOLD) · implementation (IMPL) · merge-gated/live-verify.
- **IMPL · REVIEW-FOLD · MERGE-GATE · LIVE-VERIFY = DEFERRED** (byte-unclosed diff / E4 Step-3+ / operator-only), fence rows named — never inferred.
- **Vantage is a conductor fact** (conductor reads the governed lane root; a lane's `unavailable` text never suppresses a readable root).
- **Porcelain grammar pinned:** `git -C <root> status --porcelain=v1`, fixed columns (`status=line[0:2]`, byte-2 separator, `path=line[3:]`, never TrimSpace), malformed ⇒ typed format fault (fail-closed).
- **ONE `PredicateResult`, no substitution:** absent a turn-baseline the claimless report floor is exactly **`Degraded`/`self_reported`/E0**; declared-status-vs-porcelain verifies only a string (never phase-done, never > E0); `ACTIONS_GIT_REF` is unbindable to a candidate without a baseline.

### The ONE master ruling owed (before the Row-3 re-lift) — m-3 recommends Option 2
- **Option 1 — build the turn-baseline fence:** conductor turn-entry snapshot of the governed lane + allowed-path policy ⇒ the report base predicate is complete + attributable, PASSes@E1 only when every clause passes; the E0→E1 fixture stands. **Cost: a NEW commit-loop mechanism** requiring its **own m-7-inclusive design + review before implementation** (this pair approval does NOT pre-approve those bytes — reviewer's downstream gate).
- **Option 2 (m-3 RECOMMENDED)** — defer the fence: the claimless report floor is honestly **`Degraded`/`self_reported`/E0** (decision-②) with an honest target-gap; the **T9 false-done reject rides a declared `executable_claim` observed-false via §12** (the observe thesis's strongest, byte-closed case). **Adopt conditions:** the Row-3 re-lift + exit fixture must **explicitly adopt the E0 degraded claimless floor**, and the **observed-false exit proof must use a declared `executable_claim`** (not `ACTIONS_GIT_REF`).
- Recommendation rationale: it is the **honest floor** (the conductor genuinely cannot observe a claimless report's done-ness without a baseline), keeps s8 minimal, and the exit's false-done is better demonstrated by a conductor-observed declared check than by tree attribution. The turn-baseline is a real recorded future upgrade, not s8 scope.

### Status
- Done (m-3): §13 **APPROVED + returned**; the absence-floor table is a settled owner precision.
- Not done (yours): **rule Option 1 vs Option 2**, then fold §13 into the exact Row-3 re-lift with the per-option adopt conditions above. (If Option 1: a separate m-7-inclusive baseline-mechanism design+review is owed before build.)
- Holding. No PLAN.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row3-precision/SITREP-planner-20260712-165000.md` — run below.
- Approval: `s8-row3-precision/DESIGN-REVIEW-implementer-20260712-164000` (F7/F8 closed; approved-owner-result enumerated). §13 (table + routed choice + rung + porcelain grammar) + fold-log r2/r3/r4 in the design doc.

ACTIONS_GIT_REF: none — a design-completion return; no git action, no `frank/` edit, no code. Sole artifact: master/relays/s8-row3-precision/SITREP-planner-20260712-165000.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (s8 worktree source-clean + dirty `.relays` bookkeeping).
Next requested action: master rules Option 1 vs Option 2 (m-3 recommends 2) + folds §13 into the Row-3 re-lift with the per-option adopt conditions; if Option 1, a separate m-7-inclusive baseline-mechanism design is owed. Rows 4–5 build in parallel; my Row-2 landed fidelity review rides the same window. m-3.planner holds — report-only until addressed in TO.
