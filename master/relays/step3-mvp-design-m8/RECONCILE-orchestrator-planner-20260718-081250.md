## RECONCILE — DIRECTLY ADDRESSED to m-8.implementer (the F76-at-r32 bounded rebind the VP's F73 rule requires): one review over UNCHANGED m-8 r12 @ `4b670a79…` × m-10 r32 @ `521bc554…` — your own six-locus scan at the r32 bytes (the r28→r32 delta = the §D.2 check-1 `authorize_reject` family + `void_reason` + the turn budget counter; expected disjoint — §D ticket-issue machinery is not an m-8 surface, but the "denials charge"/one-counter rule brushes attempt-row counting, so verify locus 2/3 explicitly); verdict names both hashes

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the bounded basis rebind/review under the VP's F73 rule; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m10/SITREP-planner-20260718-082000.md
FROM: master.orchestrator-planner
TO: m-8.implementer
CC: m-8.planner, m-10.planner, master.orchestrator-reviewer, operator
SUBJECT: your `070249` verdict closed F76 at r28; m-10's hash then moved for the VP-required F80 amendment (r32 `521bc5548d3c8764273ee50a771f507932da7893f64f866f5986212481d37031`, approve `081600`), which hash-reopens the stage-2 basis binding exactly as the VP's disposition anticipated ("it must receive a bounded rebind/review only because F80 requires the m-10 hash to move") — same bounded scope, same discipline: your own scan, not anyone's summary

m-8.implementer — repeat your `070249` six-locus check at the r32 bytes (connector bootstrap · attempt-open handshake · attempt-result vocabulary · terminal rows · cancellation consumer discipline · epoch authority), with one named attention point: r32's accounting section makes the turn tool-call ceiling guard EVERY `tool_calls` insertion and rules denials charge — that is the TICKET/tool-call counter (m-10 §D/§F), not the `provider_attempts` machinery your loci 2/3 consume; verify at the bytes that the attempt-open/attempt-result surfaces are untouched by it rather than accepting my classification. Return the verdict naming r12 `4b670a79…` × r32 `521bc554…`. No m-8 byte moves on approve; a defect routes through master.

ACTIONS_GIT_REF: docs-workspace disk action — this routing relay + one INDEX.md row; no design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-8.implementer returns the bounded verdict; master carries it into the corrected close supplement.
