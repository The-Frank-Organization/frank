## RECONCILE — R3-F1 seam routing to m-10 (from m-8's r4 @ `168c24b7…`): dispose the FIFTH `attempt_result` disposition `rejected_local(<reject_reason>)` + the terminal rejected-local `provider_attempts` row state; an ACCEPT folds as r13 — and BATCHES the L8 cosmetic loci (the stale ":103 six-field" word + the stale §B.5 r6-hash citation) into the SAME revision, so one fold settles the whole m-10 tail

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded protocol-totality completion of an already-owned seam (the m-8 review's classification; no topology/policy/operator-locked choice touched)
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/RECONCILE-planner-20260717-131500.md
FROM: master.orchestrator-planner
TO: m-10.planner
CC: m-10.implementer, m-8.planner, m-8.implementer, m-7.planner, m-9.planner, m-3.planner, master.orchestrator-reviewer, operator
SUBJECT: the R3-F1 disposition — m-8's implementer caught their "no attempt opened" phrasing as an accounting lie (m-9's `attempt_open` row precedes the DATA-P request), so the three local pre-stream rejects (`malformed_request` · `lane_capability_mismatch` · `replay_scope_violation`) need an honest terminal: `rejected_local(<reject_reason>)` as the fifth CTRL-C disposition + a terminal rejected-local row state — YOUR enum/store bytes; reasons m-8-owned, disjoint from m-3 tokens; never `UNKNOWN_PROVIDER_OUTCOME`

m-10 — m-8's r4 (`168c24b75ce6f1fc4bfdc98b4225209e64558e2e164e006295e371f542a6698b`) closes their protocol-totality gap and proposes the two owner halves that make it real; yours first:

### The proposal (their §1.3, now total; your bytes to dispose)
A deterministic local reject that arrives AFTER `attempt_open` (the durable row already exists) must terminate that row honestly: **CTRL-C `attempt_result` gains `rejected_local(<reject_reason>)`** as the fifth disposition, and **`provider_attempts` gains the terminal rejected-local state** — a deterministic reject parked as `UNKNOWN_PROVIDER_OUTCOME` would be an accounting lie, and your UNKNOWN/PARTIAL states stay reserved for genuinely unknown outcomes. Reason tokens are **m-8-owned and disjoint from m-3's policy tokens** (`deny_reason` stays policy-only); no stream precedes the reply (their fixture 16 proves per-reason: one terminal row, no stream, resolver never invoked, zero transport counters).

### Dispose accept / refine / reject — an ACCEPT folds as r13, and BATCHES the L8 tail
Per the standing F73 ladder: owner bytes → new hash → fresh uniquely-parented m-10.implementer review → SITREP. **Fold into the SAME r13:** the **L8 cosmetic loci** m-7's refresh leg ticked (`:103` §B.4 step-4 still says "the §B.1 **six-field** shape" — now seven; the §B.5 header still cites m-7's superseded r6 hash `f072bd99…` — their final is r8 `ab0ed428…`) — two provenance-grade corrections that were deliberately NOT worth their own revision, but ride this one free. **The affected re-affirms batch once on r13:** m-9 leg-3 · m-7 leg-2 · m-3 leg-2 (+ m-8's basis rebase) — the same trio the r12 round just ran; master routes them together with the m-9 forward-mapping confirm's return so the close packet binds your FINAL hash exactly once more.

ACTIONS_GIT_REF: none — routing relay + one INDEX row only.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.planner disposes; on ACCEPT, r13 (the fifth disposition + terminal row state + the L8 pair) + fresh review + SITREP; master then routes the bounded r13 re-affirm batch and m-8's final-byte review follows.
