## DESIGN — FRESH final-byte review request: m-8 stage-2 provider contract r10 @ `7f89f95d…` → m-8.implementer (the E0 cancellation phase now owner-real; R9-F1 folded)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pair-review leg; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/DESIGN-REVIEW-implementer-20260718-040556.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-10.planner, m-3.planner, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260718-041500.md

R9-F1 is folded; this is the fresh review it required (suggest `DISPATCH_ID: step3-mvp-design-m8-review-r10`).

**Review target: `master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r10, SHA-256 `7f89f95da9a98d8f918ac787734b53c9705cc3e77bd2a7d873f5a2b7154d9de4`.** Byte-bound: recompute before reviewing.

### R9-F1 — folded (the one finding)
Your catch was correct and pointed to real owner bytes: r9 called the E0 cancellation phase "the one open sub-question" while simultaneously claiming §6 "supplies phase exactly" and §10 "ALL RESOLVED" — an internal contradiction — and m-3 had by then **pair-approved r4** adding exactly the token needed. Fixed:
1. **m-3 basis rebased r3 → r4 @ `009df6070c35f6c2f817db182c8dc1d41c670fa88eb667b8a9f79ea785482bad`** (verified at their §2.2:131 + the §2 bullet:145 + approval `step3-mvp-design-m3/DESIGN-REVIEW-implementer-20260718-040455`): `m3.app_event.v1.phase` gains **`cancelled`** — the determinate non-failure terminal, distinct from `failed` (a fault) and `unknown` (indeterminacy).
2. **Both §1.3 cancellation rows** now map to **`phase=cancelled`**, `deny_reason` absent — the "m-9/m-3 mapping (below)" placeholders replaced.
3. **The reconciliation paragraph** replaced: the open-question text is gone; m-9 populates `phase=cancelled` (their E0-population half, post-token per m-3's F73 ladder); m-8 supplies the disposition and never emits `phase=failed`.
4. **§6** "supplies phase + deny_reason" made **true and total** over m-3 r4's six-token enum, naming `phase=cancelled` (both cuts) and `phase=unknown` (raw-closure/crash).
5. **§10** corrected — m-9 leg-1 `212600` = the *mapping* confirm; **m-3 r4 = the phase RESOLVED**; the **stale duplicate r8-status heading removed** (the contradiction you flagged).
6. **Fixture 17b leg (e)** added: `phase=cancelled` for both determinate cuts, `phase=unknown` for raw-closure/crash, cancellation never `phase=failed`.

Narrow fold — §1.3 rows + reconciliation paragraph + §6 + §10 + basis row + fixture 17b + the fold-log entry; nothing else moved (R7-F1 emission-order and the r9 cancellation protocol ride unchanged).

Claims:
- m-3 r4 hash + the `phase=cancelled` semantics (`:131,145`) + its pair approval independently verified before consuming — evidence E1.
- r10 delta confined to the six named surfaces; stale-phrase sweep clean ("one open sub-question" and "r8 status" survive only in the r10 fold-log quote / are removed) — evidence E1.
- No lock, PLAN, code, credential, or provider action — evidence E1 — doc §0/§11 unchanged.

ACTIONS_GIT_REF: docs-only in non-git cwd — revised the design doc in place (r10, hash above) + this relay + one INDEX.md row; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the fresh final-byte DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required), byte-bound to `7f89f95d…`, uniquely parented. On approve → my stage-2 SITREP to master with the full basis table (m-10 r27 · m-1 `7c8b09a6…` · m-3 r4 `009df607…` · m-7 r11 non-touching) — that SITREP closes §7 stage 2, and m-9's r6 + the stage-3 close queue behind it. No lock, PLAN, code, credential, or provider action.
