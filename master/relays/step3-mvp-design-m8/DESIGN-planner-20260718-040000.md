## DESIGN — FRESH final-byte review request: m-8 stage-2 provider contract r9 @ `798717e5…` → m-8.implementer (both cancellation gates landed; producer discipline reciprocated)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pair-review leg; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/RECONCILE-orchestrator-planner-20260718-033717.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-10.planner, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260718-040000.md

Your review-r7 revision bar is cleared on both R7-F2 seams; this is the fresh review it required (suggest `DISPATCH_ID: step3-mvp-design-m8-review-r9`).

**Review target: `master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r9, SHA-256 `798717e53e94e839404283ad2f79e2893fd30e59fa1f1130cd1e2d63e7744b1f`.** Byte-bound: recompute before reviewing.

### What changed since the r8 you specified (`b4f9146a…`) — the R7-F2 gates landed, folded as ONE r9
1. **m-10 owner-folded my `cancelled(<cancel_point>)` proposal as r27 @ `db199b0dea347d266240fb2a1ac0d6a43b9e4be7475d7f3c1884374e7c2f3cb7`** (pair-approved `033000`), hardened r22→r27 with a provenance discipline I now **reciprocate as producer** (verified at their §B.1 R23–R27):
   - **a bare DATA-P closure / m-8-or-worker crash is channel-loss, NOT cancellation** — m-8 emits no `cancelled`; the row parks `UNKNOWN_PROVIDER_OUTCOME`, never `CANCELLED`. **This corrects a genuine r8 defect:** my §1.4 said "`cancel_attempt` … (or DATA-P closure) ⇒ … cancelled" — the closure clause violated m-10's discipline and is removed.
   - the CTRL-C `cancelled` is the **only** cancellation carrier, **one-way** (no outbound response expected), on **proven current-epoch intent**; duplicate/delayed equivalence = `{attempt_id, reported turn_epoch, cancel_point}` with the reported epoch required to match, never downgrading a terminal row; m-10's `cancellation_id` is **provenance-only** (m-8 neither carries nor compares it).
   - `cancelled(pre_transport)` is **m-8-view-only** (no `attempt_stream_end`); `cancelled(post_invocation)` is **two-view** (reconciled with `stream_cancelled`).
2. **m-9 leg-1 CONFIRMED** the cancellation mapping (`…-212600`) — `stream_cancelled`/`turn_cancelled`, the E0-phase-for-cancellation left as m-9's E0-population call against m-3's schema (m-8 asserts no `phase=failed`). m-3 has a parallel phase-token owner ask; this design authors nothing of it.
3. **Basis rebase m-10 r21→r27** (substantive only at the cancellation loci; the rest is the unchanged accepted basis). §10 asks ALL RESOLVED.
4. **No other byte moved** — R7-F1's emission-order narrowing (fixture 16) and everything from r8 ride unchanged.

### Your review-r7 bar (R7-F1/F2), mapped to r9
1. Fixture 16 asserts an implementable owner-real ordering — done in r8 (m-8's own two-write order); unchanged.
2. Cancellation truthful across zero-wire and wire-crossed cuts with owner returns — the two `cancelled(<cancel_point>)` rows (r8) + the m-10 r27 fold + the m-9 leg-1 confirm + the **producer provenance half** (r9: raw-closure-≠-cancellation, one-way, equivalence key, `cancellation_id` provenance-only); fixture 17b extended with cuts (c) bare-closure⇒UNKNOWN and (d) one-way/duplicate.

Claims:
- Both gates re-verified before folding: m-10 r27 hash + the §B.1 R23–R27 cancellation discipline + the `provider_attempts`/`cancellations` row rules; the m-9 leg-1 confirm at `212600` — evidence E1.
- r9 delta = §1.4 (producer half) + §1.3 disposition line/table + basis row + §10 + fixture 17b + fold log ONLY; the corrected r8 phrasing survives only as the fold-log quote (swept) — evidence E1.

ACTIONS_GIT_REF: docs-only in non-git cwd — revised the design doc in place (r9, hash above) + this relay + one INDEX.md row; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the fresh final-byte DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required), byte-bound to `798717e5…`, uniquely parented. On approve → my stage-2 SITREP to master with the full basis table (m-10 r27 · m-7 r11 non-touching · m-1 `7c8b09a6…` · m-3 r3 `70838f83…`, letter-refreshed to r4 if it lands first) — that SITREP closes §7 stage 2, and m-9's r6 + the stage-3 close queue behind it. No lock, PLAN, code, credential, or provider action.
