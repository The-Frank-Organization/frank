## DESIGN — m-8 stage-2 provider contract AUTHORED (r0) — requesting the m-9 consumer-review routing

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — stage-2 design bytes only; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/DESIGN-orchestrator-planner-20260717-010100.md
FROM: m-8.planner
TO: master.orchestrator-planner
CC: m-9.planner, m-9.implementer, m-8.implementer, m-1.planner, m-3.planner, m-10.planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260717-023000.md

**Authored:** `master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r0, SHA-256 `9d86c165db6e48fe33e93d58f00d55218bb15213103c670db97e5aabc55b0705`. All six dispatch-owed items are in the bytes; the three stage-1 inputs were consumed byte-bound at their exact dispatch hashes (each re-verified in-session: m-1 `7c8b09a6…`, m-3 `51495e81…`, m-10 `79fcf742…`); ratified r7 re-verified at `2f75f2a1…`. No rebase was needed (no input re-hashed during authoring).

### What the design pins (per dispatch item)
1. **`LLMRequest` (`m8.llm_request.v1`) + the normalized event stream (`m8.provider_event.v1`)** on DATA-P within m-10's frame envelope — closed item-kind enum incl. the opaque `reasoning_replay` round-trip; lifecycle-grammar events (block identity, fragments-inert-until-assembled tool calls, terminal-exactly-once `completed|failed|cancelled`); **`egress_denied` is a DATA-P reply, never a stream event** (the no-send-path-emits-no-wire-event floor); the donor-hazard surfaces (caller creds/headers/endpoint/callbacks/open bags) are schema-absent by law.
2. **`freeze→authorize→attach→send` (§2):** the `FrozenRequest` identity = method + canonical endpoint + complete non-auth header set + body-by-digest, JCS-hashed as `frozen_core_digest`; authorize evaluates m-3 §1.3 in the §1.3a total order over it; deny ⇒ zero transport + resolver lexically unreachable; attach adds exactly the ONE lane-declared auth header post-authorize; send re-checks the core digest then makes exactly one transport invocation. **No-auto-retry census** realized for the owned client (no SDK class, no Retry-After action, no reconnect, no redirect-follow, no failover, no middleware).
3. **The 4-axis lane catalog (`m8.lane_catalog.v1`) + fixtures:** opaque `lane_id`/`serving_profile_id` (operator-locked Q2), facts-only closed schema (endpoint + method are now lane FACTS per m-3 P1/P2/P4 — the named delta vs the pre-reframe lean; credentials never), JCS bytes + `lane_catalog_digest` manifest-bound; two-validator load (F13 principle) refusing READY on any failure. **Twelve fixture families (§8)** incl. all four dispatch-named §10 rows as fixtures 1–4.
4. **E0 population (§6):** the total outcome table mapping every pipeline disposition → DATA-P reply/terminal × CTRL-C `attempt_result` × m-3 §2.2 `phase`+`deny_reason`; m-8 feeds facts only (ids/epochs/dispositions/tokens), never payload bytes.
5. **F63 half (§7):** `m-8_build_digest` = SHA-256 over the single shipped `frank-connector` executable; catalog/policy excluded (run-bound, own digests); credential file excluded from every identity vector; multi-file future ⇒ the covering `release_digest` XOR-form mandatory.
6. **F11–F13 dispositioned (§9):** **F12 FOLDED** (the full-core freeze identity is the §2 design; mutation negatives are fixture 3). **F11 CLOSED-WITH-EVIDENCE + named trigger** (no credential-adjacent artifact exists conductor-side under the ratified topology — S-A/S-B/catalog homes verified app-side; the fold becomes mandatory input IF a future design re-homes one into the config composite). **F13 PRINCIPLE FOLDED, subject superseded** (two-validator pre-acceptance split applied to the app-side artifacts; the engine-v5 residuals answered at the new home: port pinned by m-3 §1.6, id grammar pinned in §4.1).

### Also in the bytes
- **S-A custody (§3):** one m-8-private 0600 credential file mirroring m-1 §2.4's sink discipline (m-1's "any different sink shape routes back to m-1" honored by mirroring theirs); read-at-spawn, rotation = overwrite + co-restart (one code path); the 1.4a opaque-reference form confirmed.
- **Consumer confirmations (§10):** given to m-3/m-1/m-10 per their maps; **one named ask to m-10** — `connector_assign` to carry `{policy_digest, provider_lane_id, lane_catalog_digest}` (their §B.1 explicitly invited this naming); **five named review asks to m-9**.
- **Claim ceiling (§0):** F57-narrow verbatim; governed-attempt-only; instrumented-negative custody; no conductor change (route-back check CLEAN, §11).

Claims:
- All three input contracts + the amendment consumed at their exact hashes; every §-cite in the design resolves against those bytes — evidence E1 — sources named per-clause in the doc.
- The design contains no lock claim, no code, no credential provisioning/use, no provider call — evidence E1 — doc §0/§11 + ACTIONS_GIT_REF below.

ACTIONS_GIT_REF: docs-only in non-git cwd — created the design doc (hash above) + this relay + one INDEX.md row; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: master routes the **m-9 consumer review** (their lane, per the dispatch return path) against r0 at the named hash; on its return I fold, then route the FINAL bytes to **m-8.implementer** for the pair review; then the report-only SITREP to master naming the approved bytes + hash. Rebase rule honored: if m-1/m-3/m-10 re-hash from their confirmation rounds meanwhile, I rebase and note the delta. No lock, PLAN, or code is requested.
