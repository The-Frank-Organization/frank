## DESIGN — final-byte review request: m-8 stage-2 provider contract r1 @ `c5eb7b69…` → m-8.implementer (the released §7 stage-2 pair-review leg)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pair-review leg; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/RECONCILE-planner-20260717-024600.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-10.planner, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260717-031500.md

Your final-byte review is **RELEASED** (m-9's scoped RE-REVIEW-CLEAN `RECONCILE-planner-20260717-024600` — release rule: no further master hop). Review target:

**`master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r1, SHA-256 `c5eb7b69644b7991be6c368768041555bc982e9f66225aa6ed457e60f4c462bc`.** Byte-bound: recompute the hash before reviewing; any mismatch stops the review.

### Review context (the full trail, in order)
1. r0 (`9d86c165…`) authored against the three stage-1 inputs at their dispatch hashes + ratified r7 `2f75f2a1…` — relay `DESIGN-planner-20260717-023000`.
2. m-9 consumer review: **CLEAN**, five asks answered, `reasoning_replay` accepted as m-9's obligation, four non-blocking clarifications (`RECONCILE-planner-20260717-012600`).
3. VP F72 (via master `…-022914`): pin `tool_result.content` in owner bytes; fold→rehash→m-9 re-review→then you.
4. r1: F72 folded (`content: string` + encoding + owner rationale, §1.1) + the m-10 rebase (`79fcf742…`→`9aa9f43f…`, their L7 six-field `connector_assign` fold; §5.3 contract-real) + m-9's C-2/C-3 pinned (§1.2 authoritative usage; §2.2 effort-mismatch typed reject) — one revision (`RECONCILE-planner-20260717-030000`).
5. m-9 scoped RE-REVIEW on the changed surface (§1.1/§1.2/§2.2/§5.3): **CLEAN**; r0 verdict stands for the unchanged surface; your review released.

### What your boot's adversarial lens should hunt (my own suggested attack surface — not a limit)
(a) **Inherit-not-own:** any place the contract leans on pi/opencode shapes as spec rather than fixture source (decision B). (b) **Seam crossings:** any m-8 byte that authors m-3 policy, m-1 secret semantics, m-10 lifecycle, or m-9 turn state rather than consuming/confirming. (c) **The freeze identity (F12 fold):** §2.1/§2.2 — is the full-core binding airtight (header sort, body-by-digest, the one-auth-header attach, the §8 fixture-3 mutation vectors)? (d) **Claim discipline:** §0 ceiling vs every later claim — especially that no clause exceeds F57-narrow, and the E0/instrumented-negative custody is never upgraded by restatement. (e) **Consumed-hash fidelity:** the m-1/m-3 citations against `7c8b09a6…`/`51495e81…` and the m-10 citations against `9aa9f43f…` (the two remaining `79fcf742` strings are rebase-provenance notes, §12/basis — verify that reading). (f) **F11/F13 dispositions:** whether closed-with-evidence + named-trigger and principle-folded genuinely discharge the packet `:94-98` carries. (g) **Fixture completeness:** §8 vs the amendment §10 annex rows assigned to m-8.

Basis caveat you inherit (m-9 flagged it too, not held for): m-10 r11 @ `9aa9f43f…` is still in its own fresh m-10.implementer review; if their bytes move again, my §5.3/basis rows rebase per the standing rule and your review notes the delta — the rest of the surface is unaffected.

Claims:
- r1 hash recomputed immediately before this request — evidence E1 — value above.
- No byte of the design changed since m-9's re-review target — evidence E1 — same hash in their relay §subject and here.

ACTIONS_GIT_REF: docs-only in non-git cwd — this relay + one INDEX.md row; no design-doc edit since r1; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the final-byte DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required) parented to this request, carrying the same DESIGN_DOC_ID and the exact reviewed hash. On approve → my report-only SITREP to master naming the approved bytes + hash (the §7 stage-2 close); on must-revise → I fold and the sequence re-runs per the routing rules. No lock, PLAN, code, credential, or provider action.
