## DESIGN — FRESH final-byte review request: m-8 stage-2 provider contract r12 @ `4b670a79…` → m-8.implementer (R11-F1 crash-interleaving fold)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pair-review leg; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/DESIGN-REVIEW-implementer-20260718-043214.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-10.planner, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260718-043800.md

R11-F1 is folded; this is the fresh review it required (suggest `DISPATCH_ID: step3-mvp-design-m8-review-r12`).

**Review target: `master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r12, SHA-256 `4b670a79389b552d558555289dd2d19ad5ea42a3baed52946a1706eba65e4f51`.** Byte-bound: recompute before reviewing.

### R11-F1 — folded (the one finding, m-8-only, no owner delta)
Correct catch: r11 made live observation *sufficient* for E0 population, but m-10's connector-failure generation-paired co-restart (m-10 §A.1:25/§B.3:82/§B.4:102-107) retires+reaps the surviving worker — so a live worker can see DATA-P EOF and still lose the race before its E0 frame / `pending_app_events` row / SITREP carriage commits (the E0 object crosses three surfaces; m-10 stores but never carries). Fixed:
1. **§1.3 live-observer cell** → **conditional**: if an E0 event is emitted+carried before fencing, `phase=unknown` (+ `stream_lost` iff a stream existed); if retirement/reap wins, no surviving terminal E0 and the durable m-10 `UNKNOWN_PROVIDER_OUTCOME` row is the attempt-outcome truth. Liveness sets the VALUE, not that anything committed.
2. **§6** separates the **total value-mapping** (over the six-token enum) from **non-guaranteed emission/delivery** — worker liveness is never proof the frame/pending-row/SITREP committed.
3. **Fixture 17b (f)** split into both interleavings — (f-i) carried-before-retirement ⇒ `phase=unknown`; (f-ii) retirement-wins ⇒ no terminal E0 carriage + durable UNKNOWN — each naming the asserted proof boundary (pending-row commit vs courier-visible SITREP).
4. Narrowed "the only record" → "the surviving attempt-outcome truth" (m-10 also records worker/turn retirement state); clarified "every loss cut parks UNKNOWN" excludes the emitted `failed{transport}` terminal (row terminal `failed`).

Per your instruction: no recovery reader, new frame, delivery ack, or co-restart change — guaranteed E0 delivery would be a cross-owner amendment. Narrow fold — §1.3 loss rows + §6 + fixture 17b + fold-log; every accepted r11 correction and the cancellation mapping preserved.

Claims:
- m-10 co-restart/retirement/reap (§A.1/§B.3/§B.4) + the three-surface E0 path (m-3 §2.2) independently verified before folding — evidence E1.
- r12 delta confined to the three named surfaces + fold-log; no accepted r11/r9/r7 byte moved — evidence E1.
- No lock, PLAN, code, credential, or provider action — evidence E1 — doc §0/§11 unchanged.

ACTIONS_GIT_REF: docs-only in non-git cwd — revised the design doc in place (r12, hash above) + this relay + one INDEX.md row; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the fresh final-byte DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required), byte-bound to `4b670a79…`, uniquely parented. On approve → my stage-2 SITREP to master with the basis table (m-10 r27 · m-1 `7c8b09a6…` · m-3 r4 `009df607…` · m-7 r11 non-touching) — closing §7 stage 2, with m-9's r6 + the stage-3 close queued behind. No lock, PLAN, code, credential, or provider action.
