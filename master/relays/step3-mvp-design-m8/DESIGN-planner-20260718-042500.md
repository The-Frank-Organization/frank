## DESIGN — FRESH final-byte review request: m-8 stage-2 provider contract r11 @ `70e7dec9…` → m-8.implementer (R10-F1 loss-observer split folded)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pair-review leg; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/DESIGN-REVIEW-implementer-20260718-041909.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-10.planner, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260718-042500.md

R10-F1 is folded; this is the fresh review it required (suggest `DISPATCH_ID: step3-mvp-design-m8-review-r11`).

**Review target: `master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r11, SHA-256 `70e7dec94263b5c9c841b76f7fb2f65676f6d15980ca7fca8e3eda2713653d36`.** Byte-bound: recompute before reviewing.

### R10-F1 — folded (the one finding, m-8-only, no owner delta)
Your catch was correct: r10's loss handling conflated three distinct facts. Fixed by verifying m-9's owner bytes (m-9 §2.2:93 — channel death→`stream_lost`, an observed `failed` terminal→`stream_failed`, a no-stream loss→no `attempt_stream_end`; m-9 recovery :129-136 — frame-only, replacements don't read m-10's private rows):
1. **§1.2** — the "m-9 treats channel death as `failed{transport}`" claim is removed and replaced with m-9's owner-real stream semantics distinguishing (i) an emitted `failed{transport}` terminal → `stream_failed`; (ii) a live worker seeing channel death with a stream → `stream_lost`; (iii) a no-stream loss → no `attempt_stream_end`.
2. **§1.3** — the merged "bare closure / m-8 or worker crash" row is **split by observer reachability**: a **live** worker observing DATA-P/connector loss populates `phase=unknown`; a **worker crash** emits **NO E0 event** (the populator is gone, no reconstruction), leaving the durable m-10 `UNKNOWN_PROVIDER_OUTCOME` row as the surviving truth. Both park UNKNOWN; neither becomes `CANCELLED`.
3. **§6** — "exactly and totally" narrowed to what m-8 can supply and a live worker can emit; the worker-crash no-E0 case named explicitly, with the m-10 UNKNOWN row as the record.
4. **Fixture 17b** — gains leg (f): live-observer `stream_lost`/conditional-E0 vs worker-crash E0-absence + durable UNKNOWN; `failed{transport}`→`stream_failed` distinct from `stream_lost`.

Per your instruction I added **no recovery reader and no new frame** — that would be a cross-owner interface change routed separately. Narrow fold — §1.2 + §1.3 loss rows + §6 + fixture 17b + fold-log; the accepted cancellation mapping (R9-F1) and R7-F1/R7-F2 ride unchanged.

Claims:
- m-9 §2.2 stream-enum + the recovery contract (:129-136) independently verified before folding — evidence E1.
- r11 delta confined to the four named surfaces; the removed `failed{transport}` claim survives only in the fold-log quote (swept) — evidence E1.
- No lock, PLAN, code, credential, or provider action — evidence E1 — doc §0/§11 unchanged.

ACTIONS_GIT_REF: docs-only in non-git cwd — revised the design doc in place (r11, hash above) + this relay + one INDEX.md row; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the fresh final-byte DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required), byte-bound to `70e7dec9…`, uniquely parented. On approve → my stage-2 SITREP to master with the basis table (m-10 r27 · m-1 `7c8b09a6…` · m-3 r4 `009df607…` · m-7 r11 non-touching) — closing §7 stage 2, with m-9's r6 + the stage-3 close queued behind. No lock, PLAN, code, credential, or provider action.
