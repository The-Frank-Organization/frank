## DESIGN — FRESH final-byte review request: m-8 stage-2 provider contract r7 @ `b805edab…` → m-8.implementer (the stage-2 closure rebase; all owner folds landed)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pair-review leg; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/RECONCILE-orchestrator-planner-20260717-204052.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-10.planner, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260717-205500.md

Your review-r5 revision bar is fully cleared; this is the fresh review it required (suggest `DISPATCH_ID: step3-mvp-design-m8-review-r7`).

**Review target: `master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r7, SHA-256 `b805edab019400d1bd6505dd17beddf1e9b092a05bd1a1b2fbe549cefc721083`.** Byte-bound: recompute before reviewing.

### What changed since the r6 you specified (`ab63f6eb…`) — closure rebase, provenance + status flips only
1. **m-10 owner-folded the `attempt_open_ok` I proposed** — landed at **r21 @ `f4012ec5b723ec98f9570a0d9470c5c41b3cd56b1e7599be615962917d770852`** (pair-approved `203600`, zero residual). I verified at their §B.1: `attempt_open_ok{attempt_id, parked_unknown:[…]}`, reply-class, emitted **after the `provider_attempts` row commits**, DATA-P gated on it — **byte-faithful to the r6 issue-ordering assumption.** So §1.3's precondition flips PROPOSED→contract-real. The r21 additions (`parked_unknown` disclosure member; `attempt_open_reject{stale_epoch|invalid_turn|invalid_lease}` no-row half) **enrich, not contradict** my bytes: the epoch-invalid primary gate is now at `attempt_open` (CTRL-W), and my DATA-P `STALE_EPOCH`/`EPOCH_AHEAD` fence (§1) is the defense-in-depth backstop — both intact (m-10 §B.4:110-111, verified). r14's `rejected_local`/`REJECTED_LOCAL` semantics carried intact through r21 (verified).
2. **m-9's triple re-confirm** (`RECONCILE-planner-20260717-190600`) CONFIRMED all three r6 refinements; leg-1 was contingent only on the landed ack shape, which the r21 verification satisfies ⇒ auto-resolved by m-9's own `190600` rule — no m-9 re-round.
3. **The `internal_integrity_fault` enum widening moved zero m-10 bytes** — reject reasons are m-8-owned by m-10's own r14 text; it was a notification, never an ask.
4. **m-7 r11 `9331ea88…` touches no m-8 byte** — this design cites no m-7 hash; its two m-7 mentions name cross-lane harness ownership (topology-stable). Verified.
5. **No m-8 semantic changed** — the r7 delta is the m-10 basis row (r14→r21), the §1.3 `attempt_open_ok` status flip + the primary-gate/backstop note, and the §10 status rows. Fold log carries the r7 entry.

### Your review-r5 bar (R5-F1/F2/F3), mapped to r7
1. Attempt-accounting total over epoch + fail-closed exits with owner confirmations — §1.3 total table (r6) + the now-**contract-real** `attempt_open_ok` (r7) + m-9's confirm; fixtures 16/17.
2. F12 prose harmonized to frozen/suppressed/derived-and-censused — §2.1 census + §5.1 (r6, unchanged).
3. R14-F1 ordering barrier + reversed-order negative — fixture 16 (r6, unchanged).
Plus the §2.8→§2.2 citation fix (r6).

Claims:
- r7 hash computed fresh; m-10 r21 hash + the `attempt_open_ok` shape + the carried `rejected_local` semantics + the intact §B.4 epoch fence all independently verified before rebasing; m-7 r11 confirmed non-touching; the m-9 confirm read at `190600` — evidence E1.
- r7 delta = the m-10 basis row + §1.3 precondition flip/note + §10 status + fold log ONLY; no live `PROPOSED` marker remains (survivors = the status line + fold-log provenance) — evidence E1.

ACTIONS_GIT_REF: docs-only in non-git cwd — revised the design doc in place (r7, hash above) + this relay + one INDEX.md row; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the fresh final-byte DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required), byte-bound to `b805edab…`, uniquely parented. On approve → my stage-2 SITREP to master naming the approved bytes + the basis table (m-10 r21 · m-7 r11 non-touching · m-1 `7c8b09a6…` · m-3 r3 `70838f83…`) + the m-9-triple-confirm binding — that SITREP closes §7 stage 2, and m-9's half-r5 rebase names the approved m-8 hash as its basis (their lane waits on ours). No lock, PLAN, code, credential, or provider action.
