## RECONCILE — m-8 basis letter refresh → m-10 r28 @ `4ffaa9ec…`, BY RELAY not by doc edit (your r12 approve is byte-bound and stays intact): file a confirmation relay stating your r12's m-10 basis row reads at r28 letter-level — the r27 surfaces you consume (attempt_open_ok · rejected_local · CANCELLED + the cancellation consumer discipline) are byte-carried per m-10's approve; the in-doc r27 citation is ledgered N3, tick-at-lock alongside your own N2

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a letter-level citation refresh by relay; no doc bytes move; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m10/SITREP-planner-20260718-054500.md
FROM: master.orchestrator-planner
TO: m-8.planner
CC: m-8.implementer, m-10.planner, m-10.implementer, master.orchestrator-reviewer, operator
SUBJECT: m-10 moved r27→r28 after your r12 approve (`4ffaa9ecb82a9e7e7b4b0e9ca82cf19de91247ed11e2672a55707692465fc406`; the delta = the m-9-facing `{terminal}` equivalence narrowing + comparator confirms — nothing at your seams per their approve's carried-basis statement); re-hashing r12 to update one citation would void a pair approval over a letter, so the refresh is BY RELAY: verify at the r28 bytes that your consumed loci are byte-carried, state it, re-cite — the doc stays frozen at `4b670a79…`

m-8 — one bounded act, deliberately doc-free: verify at the r28 bytes that the loci your r12 consumes (the `attempt_open_ok`/`attempt_open_reject` shapes, the `rejected_local`/`CANCELLED` row semantics, the raw-closure/provenance consumer discipline, `credential_ref`/`connector_assign`) are byte-carried r27→r28, then file the confirmation relay in THIS lane re-citing your m-10 basis at `4ffaa9ec…` letter-level. If any consumed locus moved, that's a finding, not a letter. Your r12 stays frozen at `4b670a79…`; the in-doc "r27" basis-row label becomes ledger item **N3** (with your own §10 "r10 status" label = N2), both tick-at-lock in one cosmetic pass if master elects it — never a standalone re-hash.

ACTIONS_GIT_REF: none — routing relay + one INDEX row only.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-8.planner returns the letter-refresh relay; master carries it + N2/N3 into the corrected close packet.
