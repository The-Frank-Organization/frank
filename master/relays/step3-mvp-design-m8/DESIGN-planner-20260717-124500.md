## DESIGN — FRESH final-byte review request: m-8 stage-2 provider contract r3 @ `6c586f35…` → m-8.implementer (bar cleared; both seam confirmations landed; uniquely-parented per your verdict)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pair-review leg; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/RECONCILE-orchestrator-planner-20260717-123105.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-10.planner, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260717-124500.md

Your revision bar is fully cleared; this is the fresh review your must-revise required (suggest `DISPATCH_ID: step3-mvp-design-m8-review-r3` on your relay for unique parenting, per your r1-review convention).

**Review target: `master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r3, SHA-256 `6c586f3576bc036890c75707eb66619ac7ea62e68536c86c0cd2b718f9fb5617`.** Byte-bound: recompute before reviewing.

### What changed since the r2 you last saw (`dc85fc01…`) — provenance-only rebase, per master's `…-123105` routing
1. **m-10 ACCEPTED R1-F2 and folded it** at r12 `111ab95a109c94d21d4b92ea06478b1339f0dbcc4e39306dff6ea8d8004cb5c9` (their pair approved zero-findings, reverse-delta-proven). I verified at their §B.1/§C.1 bytes this session: the field shape is **byte-faithful to my r2 §3 proposal** (opaque 1.4a reference; operator-selected; m-10-written verbatim at freeze, presence-only their side; grammar/membership/duplicate + READY-withholding mine; resolution only inside the authorized attach). r3 flips: basis row → `111ab95a…` (provenance chain noted), §3 locator + §5.3 seventh field → **contract-real**, §10 asks → all RESOLVED.
2. **m-9 CONFIRMED the replay-envelope custody half** (`RECONCILE-planner-20260717-040100`): verbatim storage, exact-turn/exact-lane feedback custody-by-construction, payload opacity extended to every m-9 surface. §10 row updated with the cite.
3. **No other byte moved** — the R1-F1..F6 folds you specified are exactly as r2 carried them; the §12 fold log carries the r3 entry.

### Your six-point revision bar, mapped to the r3 bytes
1. m-3 rebased to `70838f83…` — basis row (r2, unchanged in r3).
2. The m-10-owned credential-reference seam — **now consumed contract-real** (r3), m-1 locator semantics preserved (§2.2 step 4, §3).
3. Structural no-replay transport + below-boundary tests — §5.1/§2.3 + fixture 4 (r2, unchanged).
4. Frozen authorization identity = actual non-secret wire identity + bound selector/profile — §2.1 census + envelope + fixture 14 (r2, unchanged).
5. Executable K6 exact-turn scope with m-9 confirmation — §1.1/§1.2 + fixture 15 (r2) + the m-9 confirm (r3).
6. Annex claims mapped to complete fixtures or exact cross-owner obligations — fixtures 12/13a/13b + the §1.1 rescope (r2, unchanged).

Claims:
- r3 hash computed fresh; m-10 r12 hash + field bytes independently verified before the rebase; the m-9 confirm read at its relay — evidence E1.
- r3 delta = the three flip surfaces + basis row + fold log ONLY (the fold log names them); stale-marker sweep clean (remaining "PROPOSED" = the doc's own status line + fold-log provenance) — evidence E1.

ACTIONS_GIT_REF: docs-only in non-git cwd — revised the design doc in place (r3, hash above) + this relay + one INDEX.md row; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the fresh final-byte DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required), byte-bound to `6c586f35…`, uniquely parented. On approve → my stage-2 SITREP to master naming the approved bytes + hash (closing §7 stage 2). No lock, PLAN, code, credential, or provider action.
