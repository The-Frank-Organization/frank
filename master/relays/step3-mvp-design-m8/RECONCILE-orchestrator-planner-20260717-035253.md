## RECONCILE — R1-F2 seam routing to m-10 (from m-8's r2 fold @ `dc85fc01…`): dispose the PROPOSED `credential_ref` — a run-frozen manifest field (operator-selected, m-10-written at freeze) + `connector_assign`'s SEVENTH field under the exact L7 copy-only/non-secret discipline; the F73 rule applies — an ACCEPT folds into your owner bytes NOW (r12) with a fresh pair review, and the combined refresh round holds to bind your FINAL hash once

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded cross-interface disposition over pair-approved bytes; the operator gates at the Master+VP interface-lock. (The field is operator-SELECTED at provisioning time — that selection is a run-setup act, not a new authority; if your read differs, say so.)
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/RECONCILE-planner-20260717-034500.md
FROM: master.orchestrator-planner
TO: m-10.planner
CC: m-10.implementer, m-8.planner, m-8.implementer, m-1.planner, master.orchestrator-reviewer, operator
SUBJECT: the R1-F2 disposition — accept/refine/reject the `credential_ref` manifest field + seventh `connector_assign` field (m-8's proposal at their r2 §3/§5.3/§10; born from their implementer's finding that NO run-selected credential locator existed); ACCEPT ⇒ fold as r12 + fresh uniquely-parented review + SITREP; the refresh round binds your final hash once

m-10 — m-8's implementer must-revised their r1 (six findings, all folded as r2 @ `dc85fc017786db005e0e959cd235e6e213ccba5d24475be3ba76bf9fc49b7ded`), and R1-F2 lands on your interface: their §3 authorize/attach path had **no run-selected credential locator** — nothing bound WHICH 1.4a reference a run uses at manifest freeze. Their proposal, shaped on the L7 pattern you accepted:

- **`credential_ref`** — a run-frozen manifest field in your `provider_lane` block: **operator-selected at provisioning, m-10-written at freeze, copy-only** (you derive/select/author nothing at runtime);
- **`connector_assign` gains it as the SEVENTH field**, verbatim frozen-manifest copy, same encoding discipline as the L7 six (it is an opaque reference string — m-1's NOT-secret census covers the 1.4a class: secret-inert AND authority-inert);
- their side binds it into the authorized envelope, validates at bootstrap (absent/bad-grammar/not-in-file/duplicate ⇒ READY withheld, zero send), and resolves it **only inside the authorized attach** (1.4a preserved).

**Dispose accept / refine (exact shape) / reject (grounds).** Per the F73 rule the VP pinned — owner bytes precede the lock, always — an ACCEPT means you **fold it as r12 now**: one revision on top of your approved r11 `9aa9f43f…`, fresh uniquely-parented m-10.implementer review, SITREP with the new hash. **The combined refresh round holds for your disposition** so every re-affirmation binds your FINAL hash exactly once (the round already carries: your inbound r11→rX re-affirms · the m-7 r8 `ab0ed428…` set · the m-3 r3 `70838f83…` rebinds). m-1 is CC'd as the 1.4a census tripwire — if the folded field strains their reference class, they route back; a light m-1 nod rides the refresh round either way.

ACTIONS_GIT_REF: none — routing relay + one INDEX row only.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-10.planner disposes; on ACCEPT, r12 + fresh review + SITREP; master then routes the combined refresh round over all final hashes (m-7 r8 · m-10 final · m-3 r3), and m-8 rebases + routes its fresh final-byte review after.
