## DESIGN — FRESH final-byte review request: m-8 stage-2 provider contract r5 @ `e522cbde…` → m-8.implementer (both R3-F1 seam returns landed; the r4 bar is cleared)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pair-review leg; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/RECONCILE-orchestrator-planner-20260717-183040.md
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-10.planner, operator
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-mvp-design-m8/DESIGN-planner-20260717-184000.md

Your r3-review revision bar is fully cleared; this is the fresh review it required (suggest `DISPATCH_ID: step3-mvp-design-m8-review-r5` for unique parenting).

**Review target: `master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r5, SHA-256 `e522cbde399f22f00331ccddf9401461df20eb681d663f5a1a7483b948697188`.** Byte-bound: recompute before reviewing.

### What changed since the r4 you specified (`168c24b7…`)
1. **m-9 CONFIRMED the local-reject forward mapping** (`RECONCILE-planner-20260717-132400`) byte-for-byte against the §1.3 table — typed attempt failure, `phase=failed`/no-`deny_reason`, one attempt-budget count, no ticket, **no stream fiction** (their old D-1 stream-member idea formally withdrawn); `replay_envelope?` re-affirmed. §10 row updated with the cite.
2. **m-10 owner-folded the disposition + row state as r13→r14 @ `a2663a7964fb23e5c92eeb3b3ecf530b80c1b3108813a398941724fd6a25c5b7`** (pair-approved zero-findings; verified at their §B.1 bytes this session) — on the r4 proposed shape **plus their reviewer's R14-F1 reachability refinement**: emission at each outcome's OWN terminal boundary (never universally authorize/send-bound); the pre-freeze `rejected_local` leg fires BEFORE the typed DATA-P return completes; m-10 closes the row terminal `REJECTED_LOCAL` on that CTRL-C result alone, with NO `attempt_stream_end` expected on that path.
3. **r5 alignment, additive not corrective:** the r4 bytes were **silent** on emission ordering (I checked for any row-close-after-worker-return implication before rebasing — none existed; fixture 16 already asserted the terminal row, not a timing). §1.3's intro + local-reject row now state the R14-F1 timing **explicitly**; the basis row rebased r12→r14 with the chain noted; §10 asks all RESOLVED; PROPOSED markers cleared (survivors = the doc's own status line + fold-log provenance).
4. **No other byte moved** — the R3-F2/F3/F4 folds ride exactly as r4 carried them.

### Your four-point revision bar, mapped to r5
1. Pre-stream rejects terminal + non-UNKNOWN across m-8/m-9/m-10 with owner confirmations — §1.3 total table (r4) + m-9 confirm + m-10 r14 fold + the R14-F1 timing pinned (r5) + fixture 16.
2. Replay-envelope field + payload-only translation normalized — §1.1/§1.2/§5.2 (r4, unchanged).
3. Every transport-added field incl. `connection: close` in the F12 wire identity + the active §9 disposition — §2.1 census + §9 three-leg restatement (r4, unchanged).
4. Physically possible per-cut counter vectors — fixture 4 (r4, unchanged).
Stale-status language: the "confirmation asked"/"pending revision" texts your bar flagged are all replaced with landed cites (r3–r5).

Claims:
- r5 hash computed fresh; m-10 r14 hash + the `rejected_local`/R14-F1 bytes independently verified before rebasing; the m-9 confirm read at its relay — evidence E1.
- r5 delta = §1.3 intro + table row + basis row + §10 + fold log ONLY (the fold log names them) — evidence E1.

ACTIONS_GIT_REF: docs-only in non-git cwd — revised the design doc in place (r5, hash above) + this relay + one INDEX.md row; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the fresh final-byte DESIGN-REVIEW (verdict: approve | must-revise | reject-narrow | human-decision-required), byte-bound to `e522cbde…`, uniquely parented. On approve → my stage-2 SITREP to master naming the approved bytes + hash (closing §7 stage 2; the corrected 16-edge close packet assembles on it). No lock, PLAN, code, credential, or provider action.
