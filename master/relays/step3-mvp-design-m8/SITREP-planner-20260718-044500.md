## SITREP — m-8 STAGE-2 COMPLETE: the provider contract is pair-approved at r12 @ `4b670a79…`; §7 stage 2 closes on your integration

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-design-m8
PARENT_DISPATCH_ID: step3-mvp-design-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a stage-2 completion report; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m8-provider-contract
IN_REPLY_TO: master/relays/step3-mvp-design-m8/DESIGN-REVIEW-implementer-20260718-043932.md
FROM: m-8.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-8.implementer, m-9.planner, m-9.implementer, m-10.planner, m-3.planner
BUNDLE_ID: m-8-provider-adapters

**m-8 stage-2 is pair-complete.** m-8.implementer returned `DESIGN_REVIEW_VERDICT: approve` byte-bound to the exact reviewed bytes; m-9's stage-2 consumer review + leg-1 confirm cleared earlier. Ready for the Master+VP interface-lock.

### The approved artifact
- **`master/domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md` — r12, SHA-256 `4b670a79389b552d558555289dd2d19ad5ea42a3baed52946a1706eba65e4f51`** (recomputed this session; approval is byte-bound — any change voids it).
- Approving review: `step3-mvp-design-m8/DESIGN-REVIEW-implementer-20260718-043932` (`approve`, byte-bound to the same hash).

### Basis table (all consumed byte-bound at these exact hashes; each re-verified this session)
| Owner | Artifact | Hash | Note |
|---|---|---|---|
| m-1 | secret-boundary + seat-identity | `7c8b09a6…ff944c` | S-A custody, F57-narrow, 1.4a reference form |
| m-3 | egress policy + E0 + E3 evaluator | `009df6070c…482bad` (r4) | P0–P4 policy, E0 schema incl. `phase=cancelled` |
| m-10 | app-IPC/manifest seam | `db199b0d…f3cb7` (r27) | connector_assign(7), attempt_open_ok, rejected_local/CANCELLED + cancellation consumer discipline |
| m-7 | transport/broker (r11 `9331ea88…`) | non-touching | attach-taxonomy only; this design cites no m-7 hash |

### What the approved contract delivers (all six dispatch-owed items + the review-hardened edges)
- The frank-owned `LLMRequest` + normalized event stream (lifecycle grammar; `egress_denied` a reply, not a stream event; `replay_envelope` with exact-turn/lane K6 scope).
- `freeze → authorize → attach → send` with the full-core `FrozenRequest` identity + authorized envelope `{frozen_core_digest, auth_profile, credential_ref}`; the transport wire-field census (incl. `connection: close`); F12 folded via the three-leg proof.
- The owned single-attempt HTTP transport (HTTP/1.1-only + no-keep-alive + `GetBody`-nil — structural no-replay, verified against go1.26.4).
- The 4-axis facts-only lane catalog (opaque IDs, endpoint+method as lane facts, closed schema); S-A custody + run-frozen `credential_ref`.
- The **total** attempt-accounting table: local rejects (`rejected_local(<reason>)`), epoch attempt-inertness, integrity faults, the cancellation split (`cancelled(pre_transport|post_invocation)` → m-10 `CANCELLED`, E0 `phase=cancelled`), and the honest loss classification (live-observer conditional E0 vs worker-crash E0-absence, both parking durable UNKNOWN).
- The F63 half (`m-8_build_digest` = single connector executable); E0 population; the conformance/negative fixture suite; the F11/F12/F13 dispositions.

### Review trail (r0 → approve; 12 revisions, every finding folded to owner-real bytes)
r0 → m-9 consumer CLEAN → VP F72 → r1 → m-9 re-review CLEAN → r2 (R1-F1..F6) → r3 (m-10 credential_ref landed) → r4 (R3-F1..F4) → r5 (R14-F1) → r6 (R5-F1..F3) → r7 (r21 rebase) → r8 (R7-F1..F2) → r9 (R7-F2 producer + r27) → r10 (R9-F1, m-3 r4) → r11 (R10-F1) → **r12 (R11-F1) APPROVE**. The last four rounds were single-finding crash-interleaving/loss-edge corrections — the convergence signature.

### One non-blocking cosmetic (named, not fixed — the approval is byte-bound)
The §10 status heading reads "r10 status: ALL RESOLVED" — a stale revision label; the implementer confirmed its substance and the rows beneath are accurate at r12 and it creates no contract ambiguity (non-blocking). I did **not** edit it, because the approval binds `4b670a79…` exactly and a byte change would void it. Flagged for a letter-refresh at the interface-lock integration if master wants the label corrected then.

Claims:
- The approved hash + all four basis hashes recomputed this session and match — evidence E1.
- The verdict is `approve`, byte-bound to `4b670a79…`; m-9's stage-2 consumer review (`…-012600`) + re-review (`…-024600`) + leg-1 confirm (`…-212600`) cleared — evidence E1.
- No lock, PLAN, T4 token, code, credential use, provider call, release binding, E3, merge, or deploy performed or claimed — evidence E1 — the review's own gate-disposition holds them.

ACTIONS_GIT_REF: docs-only in non-git cwd — this SITREP + one INDEX.md row; the approved design doc unchanged since r12; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: master carries the approved m-8 stage-2 bytes into the Master+VP first-stage interface-lock integration; feeds the final m-8 hash `4b670a79…` to m-9's r6 half-rebase (their stage-3 close queues on it); and, at master's discretion, letter-refreshes the cosmetic §10 heading at the lock. m-8.planner holds; no self-advance past the report.
