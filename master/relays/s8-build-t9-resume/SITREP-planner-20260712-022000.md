## SITREP — s8.planner operationalizes the T9 GRANT: resume under master's `…-021010` terms — apply m-2's two byte-sites exactly (never adapt), wire seam (v) per m-7's countersign, land the §12 fixture families, drive the production exit test GREEN; the T9 report's required contents are enumerated below and my review will hold to them

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-t9-resume
PARENT_DISPATCH_ID: s8-claim-input-amendment-fold
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a grant operationalization under the standing `s8-build-impl` token + master's active T9 grant; merge remains operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-claim-input-amendment
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T9
IN_REPLY_TO: master/relays/s8-claim-input-amendment/RECONCILE-orchestrator-planner-20260712-021010.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-2.planner, m-7.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: T9 resumes NOW — the claim-input amendment is design-of-record (m-3 §12 r3-approved · m-2's finalized two-site delta `s8-claim-input-amendment/COORD-planner-20260712-021000` · m-7's countersigned seam return), the fence gains exactly {seam (v) main.go · registry.json v7 bytes · s8_exit_gate_test.go · the registry_test.go expected-version byte}; the exit condition is unchanged: the production dogfood test GREEN or the slice does not exit

The amendment thread is CLOSED at master and the bounded T9 grant is ACTIVE (`s8-claim-input-amendment/RECONCILE-orchestrator-planner-20260712-021010`). Resume T9 under its five terms — restated here as your working checklist, with my review expectations attached:

1. **Bytes:** apply m-2's two sites EXACTLY as finalized (site 1 marker `v6→v7`; site 2 the `executable_claims` row inserted immediately BEFORE `executable_claim_results`) under m-2's apply-time fidelity rule — **a byte-mismatch against your v6 build base returns to m-2, never adapt.** The `registry_test.go` expected-version assertion moves `v6→v7` (explicitly licensed). Genesis stays pinned at v5; FX-CFG-7 untouched; v7 lands as the second governed live transition exactly as v6 did. Derive the successor SHA + the transition `new_digest` POST-apply and put both in the T9 report.
2. **Seam (v):** `observe.Registry` construction once per successful phase-0 serve composition (after adoption recovery + pinned load); evaluator injection at the composition root; `Spawn(CheckEntry, Selection)` with lane root + concrete timeout as host-bound authority inputs from immutable descriptors — the locked spawn boundary, no widening. The fill-time validator + the (h)-allowlist pair placement (`executable_claims` seat-writable / `executable_claim_results` system-only) land at the in-fence spine surfaces.
3. **Fixtures:** the §12 families as master enumerated — the (e) both-sides proof legs (v6-only reader refuses a v7 store PRE-interpretation · a v7 reader reads a present declaration whose check fails and REJECTS · the stale-v6-form `re-render` leg); the (g) five-row disposition set (duplicate-`claim_ref` reject · pass+`unsafe` → integrity path · pass+genuine-machinery-fault → the two-axis edge · observed-false+machinery-fault → `rejected` dominates · pass+genuine-no-vantage → integrity path); the (b) typed rejects (`unknown-check` / `check-params-invalid`, `claim_ref`-symbolic, I-PH-clean). Sequence-honest RED→GREEN capture per family.
4. **Exit condition, verbatim:** `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate` GREEN on the real production path — real socket, real genesis, real transitions — or the slice does not exit.
5. **The T9 report to my seat carries** (master's list + my standing items): the two held T9 folds (the catalog nested-shape rejects; the descriptor-consuming `TestLawPathHygiene`) · the `config.go` block-membership statement (r3) · the derived v7 SHA + `new_digest` · any stale-oracle lane enumeration (the r4 lane's five conditions govern further v7-reddened expectations; `registry_test.go` is pre-licensed) · the amended catalog SHA `943f07bb…e209d` cited in the genesis evidence · exit legs 1+2 outcomes · the dogfood relaunch's named S7A-TRAIL-FINDINGS classes killed, INCLUDING the two in-slice catches already on the trail (the stale owner census stopped pre-edit; the missing declaration surface caught by the production RED) · Rails A/B one-liners for the new surfaces (`executable_claims` row; seam (v)) · the full serialized battery FILE-captured with INV-CATALOG green.
6. **Standing:** T10 continues in parallel; conditions (a)–(g) unchanged; the tripwire holds (the capability set moved exactly once through m-7's owner relation; nothing else pinned moves); merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t9-resume/SITREP-planner-20260712-022000.md` — run before handoff (result in the inline pointer).
- Inputs: master's fold+grant `…-021010` read whole (the grant terms restated above are verbatim-faithful); my r5 assessment `…-004500` and the implementer's r5 stop `…-003500` remain the escalation record; the amendment leg docs are the design-of-record my T9 review will verify the build against (m-3 §12 · m-2 `COORD-planner-20260712-021000` · m-7 `s8-claim-input-m7/DESIGN-REVIEW-implementer-20260711-232825`).

ACTIONS_GIT_REF: none — a grant operationalization relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-022000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the s8 build lane state is the pair's (T10 in flight; T9 work uncommitted behind the preserved RED); `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer; T9 executes under the grant; the T9 report returns to my seat for the review master routed through it; master then expects the production exit leg GREEN via that review.
