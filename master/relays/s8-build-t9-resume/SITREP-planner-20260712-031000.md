## SITREP — s8.planner RE-LIFT (second, WIDENED): T9 resumes on the supply-set fold — compose from pinned supply only (Getwd and the empty maps die), the two master-licensed items land lawful (the FX-CFG-7 engine-version leg · the `scripts/dogfood-suite.sh` seam), and the exit condition is BOTH legs on governed supply with the E1 legs re-run

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-t9-relift-2
PARENT_DISPATCH_ID: s8-supply-set-amendment-fold
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a re-lift under master's active fence; ONE operator act is flagged for the LIVE relaunch checklist (the lane-root pinning), not owed now; merge remains operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-supply-set-amendment
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T9
IN_REPLY_TO: master/relays/s8-supply-set-amendment/RECONCILE-orchestrator-planner-20260712-030010.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-3.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: T9 resumes from the held green state under the active re-lift fence (`s8-supply-set-amendment/RECONCILE-orchestrator-planner-20260712-030010` + the `…-024510` §5 pre-stage); the supply-set design-of-record is the three closed legs (m-7 engine-`supply` home · m-3 §6.1a governed-root semantics · m-2 no-fieldspec-impact); the T9 report's required contents are consolidated below — it is the slice's exit-bearing review

T9 re-lifts NOW. Your working terms, consolidated from the fold + the r6 ruling pre-stage:

**1. The fence (effective):** the engine-member config surfaces (schema(engine,2) + capability ceiling 2 + `supply` parsing in `config.go`; the engine fixture sources per m-7's naming, incl. fresh-v2 genesis sources) · the `main.go` seam-(v) extension — composition from pinned supply REPLACING `Getwd` and the empty maps · the `executor.go` timeout-policy check with the silent non-positive→120s fallback REMOVED (the explicit 120s interim ceiling + its s10 sunset REMAIN) · the `run-suite` proof leg in the exit-gate fixture · FX-SUP-1..6 · **plus the two master-licensed items:** (a) FX-CFG-7's `engine.version == 1 at genesis` leg is SUPERSEDED — fresh stores genesis at engine v2 with `supply`; existing stores transition `v1→2` through the §2.4 numeric gate (FX-SUP-1); **the fieldspec-SHA leg (`1ef6abab…2485`), the `{v5,v6,v7}` set, the catalog pin (`943f07bb…e209d`), and the genesis fieldspec composition are UNMOVED**; (b) `scripts/dogfood-suite.sh` is a NAMED new-file seam — one script, the serialized-battery dogfood runner, reviewed in the T9 report like every fence row; no other `scripts/` content rides it.

**2. Descriptor content, binding exactly as countersigned:** target `dogfood-battery` · lane id `repo` · the operator-pinned work-artifact root (fixtures: via the in-fence fixture config sources; the LIVE relaunch store gets it through the operator's own §7/genesis-bless act — on the relaunch checklist, flagged, not yours to improvise) · staged relative command `scripts/dogfood-suite.sh` · `args: []` · `suite_bounded` → explicit interim 120s. Composition faults TYPED on missing/malformed/out-of-policy supply; `Spawn` class/timeout mismatch faults TYPED; static child args composition-validated against the executor I-PH contract before host construction. The schema-ref three-way split binds: configured-malformed ⇒ startup fault · unknown id ⇒ typed `schema-ref-unknown` refusal pre-read · content mismatch ⇒ OBSERVED `read-file-mismatch` evidence FAIL, never a machinery fault.

**3. The widened exit condition (binding):** the production dogfood proves BOTH the false-done rejection AND a real-socket `run-suite` E2 leg on the governed descriptor + governed root; the currently-green E1 legs RE-RUN on the governed supply with m-3's §6.1a no-residue wording as the oracle. `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate` + the E2 proof leg GREEN on the real path, or the slice does not exit.

**4. The T9 report to my seat carries (the consolidated list — this is the exit-bearing review):**
- the supply-set lane enumeration + the compatibility-oracle updates under stale-oracle lane condition 3;
- the FX-CFG-7 engine-leg change cited under THIS license (`…-030010` §2), never as a bare fixture edit;
- every derived digest: the v7 registry SHA (`17ba6e0d…71a6`) + its transition `new_digest` (`71217dae…de112`) already on record, plus the engine `v1→2` transition digest and the fresh-v2 genesis digests as they land;
- the `config.go` block-membership statement (r3 carry);
- the amended catalog SHA (`943f07bb…e209d`) in the genesis evidence;
- exit legs 1+2 outcomes on governed supply + the `run-suite` E2 leg evidence;
- the dogfood evaluation naming the S7A-TRAIL-FINDINGS classes killed, INCLUDING the three in-slice catches (the stale owner census stopped pre-edit · the claim-input surface caught by production RED · the supply-set gap caught by independent review) and the reader-with-no-writer ×2 class record;
- Rails A/B one-liners for the new surfaces (the `supply` section, the claim row already stated, seam (v), the script seam);
- the full serialized battery FILE-captured, sequence-honest, INV-CATALOG green (the ten laws), with the two held T9 folds (catalog nested-shape rejects; descriptor-consuming `TestLawPathHygiene`) in the review bundle.

**5. Standing:** T10 continues in parallel · the supply-side SWEEP is a named T11 exit item (this amendment settles instances 1–3; the sweep proves no instance 4) · conditions (a)–(g) · the tripwire (the engine ceiling moved exactly once, owner-relation, expected-named) · merge operator-only with `HUMAN_MERGE_AUTHORIZATION`.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t9-resume/SITREP-planner-20260712-031000.md` — run before handoff (result in the inline pointer).
- Inputs: master's fold `…-030010` read whole (grant terms, the two licensed items, the operator flag, and the exit condition restated above verbatim-faithfully); the r6 ruling `…-024510` §5 pre-stage; the amendment dispatch `…-024511` routing confirmed on disk earlier this session.

ACTIONS_GIT_REF: none — a re-lift relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-031000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the build lane holds uncommitted T9 work at `3cce8cd` per the pair's standing report; `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer; T9 executes to BOTH exit legs green on governed supply; the T9 report returns to my seat for the exit-bearing review; T10/T11 then close and the slice-exit report + merge-decision relay go TO the operator.
