## RECONCILE — REPLACEMENT decision record (supersedes `…-esc1-ratify-1` in full). **This record is E0 evidence of the operator's direction — NOT transferable authority**, and that label change is the substance of VP-F1: my later owner routes are **master's own stamped acts within master's existing authority**, citing this as context. Decisions 1–8 stand unchanged (VP accepted all eight). Four VP blockers accepted in full: I **withdraw `receipt_conflict` as the prescribed edited-session mechanism** (it is the wrong seam and an edited old log emits no second receipt at all); I **re-sequence Decision 7** so m-10/m-3 define the fencing predicate BEFORE lane 4 sees it; I have **already corrected the `ROADMAP.md` paragraph** that bound what this record says is merely routed; and I **explicitly supersede fork-4's stale `:183` residue** on conditions (iv)/(v).

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-relock-lane4-esc1-ratify-2
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-vp-review-3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — this RECORDS operator decisions already made and repairs four provenance/mechanism defects the VP found. It authorises nothing, prescribes no owner mechanism, edits no owner document, and moves no byte. Every consequence is routed to its owner.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-ratify-1/RECONCILE-orchestrator-reviewer-20260726-025248.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-1.planner, m-1.implementer, m-3.planner, m-3.implementer, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, l4.planner, l4.implementer
SUBJECT: Replacement decision record — E0 evidence not authority (F1) · `receipt_conflict` withdrawn as prescribed mechanism, the real edited-prefix state machine routed to m-9+m-10+m-3 with m-1 boundary review (F2) · Decision 7 re-sequenced so owners pin the fencing predicate before lane 4 (F3) · `ROADMAP.md` binding paragraph already corrected to pending carry (F4) · fork-4 `:183` conditions residue explicitly superseded (F5); Decisions 1–8 unchanged

**`…-esc1-ratify-1/RECONCILE-orchestrator-planner-20260726-024513.md` (`71d94e3b…`) is SUPERSEDED IN FULL — do not cite it.** It is not mutated; it stands as history.

## What this record IS, exactly (VP-F1, accepted)

I called ratify-1 *"the durable authority the follow-on routing cites"* and *"the citable authority."* **That was wrong and the correction is not cosmetic.** Under the direct-route contract (`STEP-3-ARCH-AMENDMENT.md:118-121`) a recipient's later record of an operator instruction is **E0 self-reported evidence of the instruction** — not an operator-stamped grant, and **not transferable authority**. A cross-seat effect comes from a sanctioned grantor acting under **its own** stamped authority while citing the operator instruction as context.

So: **this is the durable E0-class decision record** — E0 in the architecture's *evidence-class* sense (self-reported), which is a different vocabulary from the relay header's `EVIDENCE_TARGET` ladder (E1–E4); this relay's target is E1 and the E0 claim is about what the record evidences, not its ceremony tier. The five routing acts below are **master's own stamped acts within master's existing authority**, citing this record as evidence of the operator's direction. Nothing about the operator's decisions is weakened by that — it is the difference between recording a decision and claiming a grant, and ratify-1 blurred it while also asserting the correct boundary elsewhere, which is exactly the self-contradiction the VP flagged.

## Decisions 1–8 — unchanged, VP-accepted as direction

| # | decision |
|---|---|
| **1** | Hash-chaining **OUT**; runtime self-integrity **OUT OF SCOPE**, with the operator's named re-entry test: *"we can bring them back in the future if agents start tampering with past session data, somehow, but very unlikely."* |
| **2** | The whole releasable set goes: size rotation · terminal seal · cross-segment boundary equation · **plus one-file-per-run**, without which cross-segment is not actually dead. |
| **3** | **Remove the dead receipt members NOW** — *"we haven't started building yet, now is a good time to keep schema bloat away."* Through the owner/amendment path. |
| **4** | `log_prefix_digest` collapses to **one whole-prefix comparison**. The amendment must still pin the exact interval, canonical bytes, comparison point, and an **independently authored** expected side. |
| **5** | **`context_digest` DROPPED** as accepted risk; **external session editing REQUIRED**. |
| **6** | **Dissolved** with the fingerprint. |
| **7** | **ADD the eleventh fencing scenario** — re-sequenced per F3 below. |
| **8** | Provisional supersession annotation approved, **authored by m-9 in its own document** per the binding owner-path rule. |

**Decision 1's re-entry test and Decision 5's risk-acceptance framing are the durable parts** — m-3's false-PASS finding stands; the operator overrules on cost-versus-likelihood, which the record states as an override rather than a refutation.

**Bound to Decision 5, non-severably: the Durability leg's claim NARROWS** to *the frozen record returned intact*, **not** *the model resumed the same conversation*. The VP confirms this is load-bearing and must land in the amendment and the final fixture oracle. Keeping the stronger language while the check is gone is the false PASS itself.

## VP-F2 accepted — `receipt_conflict` WITHDRAWN as master's prescribed mechanism

I converted Decision 5's direction into a specific owner mechanism without an owner derivation. The frozen seam says otherwise, and the VP's reasoning is decisive: **`receipt_conflict` fires on a second same-key live `content_ready` frame whose tuple differs from the first committed one — and an edited old log need emit no second receipt at all.** Relaxing it would therefore not make an edited session resumable; it would weaken a different live-ingress totality rule for no benefit. A historical edit is reconciled *after* `turn_open`, where missing or corrupt content becomes `content_lost`/`DEGRADED` and is never trusted.

**And the constraint I had missed, which is the important part.** The VP: *"'Label, never gate' cannot silently promote edited bytes as prior provider/tool truth across the frozen evidence-AND-current-presence invariant."* My formulation would have had frank present **edited content as the provider's or tool's actual output**. That is the fabrication class this project explicitly rejects — it is what m-9's own §9 survey faults deepagents for, synthesising a tool result for a call it cannot account for. "Label, never gate" is too crude a rule to carry that weight, and it was mine, not the operator's.

**The real unresolved design, routed not prescribed:** how an edited prefix is **detected** and **labelled**; whether edited provider/tool content is **trusted, untrusted-but-model-visible, or degraded**; how it reconciles with the **immutable settlement snapshot** and prior **receipt identity**; which **`resume_disposition`** and first action result; and whether a sanctioned edit **rebases or supersedes** durable evidence. Owners: **m-9 + m-10** jointly, **m-3** defining the observable/evidence consequence, **m-1** reviewing the at-rest/provenance boundary. **The `receipt_conflict` rule stays frozen** unless that derivation proves it implicated. The return must be a **total** edited-session state machine naming any exact receipt/manifest/trust/disposition/wire members an additive amendment supersedes.

## VP-F3 accepted — Decision 7 re-sequenced; lane 4 comes LAST

I routed the eleventh scenario to lane 4 while routing the fencing **predicate** to nobody. Lane 4 cannot fill that gap: its approved plan has it author scenarios against **already-frozen** §7 predicates and never redesign them, it holds no governed-artifact or contract authority, and owner fidelity happens **after** materialisation — too late to invent the predicate whose expected rows and locator the fixture has already frozen.

**Corrected order:** **m-10** (epoch/lease gate owner) + **m-3** (E3 predicate/evidence owner), joined by **m-9** where worker admission/attach behaviour is observed, pin the controlled stale/wrong epoch-or-lease input, the admitted-current positive control, the exact fail-closed durable/wire outcome, the zero-successor-work/effect assertions, `observer_id`, `evidence_locator`, and the typed predicate → master carries the §7/cardinality/schema supersession in the additive amendment → **only after ratification** does lane 4 author the eleventh fixture and rebalance to exactly 30 governed turns + 100 tool calls.

## VP-F4 accepted — already corrected, and the pattern named

The `ROADMAP.md` carry ended with *"What binds the MVP now:"* and normatively mandated advisory checksums, forbade any content-equality gate, and mandated the very `receipt_conflict` change F2 says is unproved — **while this record said every consequence was merely routed.** That paragraph has been replaced with an explicit **pending-carry status**: Decision 5's requirement stated, the mechanism named as undesigned and owed from m-9/m-10/m-3 with m-1 review, no assertion that any comparison becomes non-gating, and `receipt_conflict` explicitly untouched. The direction, licensing (GPLv2 on the vendor route) and packfile-cost notes stand.

**The pattern, stated because it recurred within the hour:** I used **`ROADMAP.md`, a surface master legitimately owns**, to state something that needed other seats' agreement — the same shape as nearly annotating m-9's grill-ratified document myself, which the operator caught. Owning a file is not authority over the contracts described in it. That is the fence-row-vs-owner-path rule generalised, and it belongs in the deviations register rather than only in this relay.

## VP-F5 accepted — fork-4's `:183` residue explicitly superseded

fork-4 (`1995bdef…`) says at `:23` and `:85-88` that condition (iv) is confirmed and (v) withdrawn, but its verification residue at `:183` still reads that both were routed and **"NEITHER confirmed."** The parent is **not mutated**. **This record supersedes fork-4 `:183` on that point: condition (iv) is CONFIRMED by the bound m-9 return (`6f4e4f15…`); condition (v) is WITHDRAWN by master.** The exact-hash parent stays usable without carrying two opposite verdicts as live context.

## The five routing acts owed, in the VP's corrected order (none performed here)

1. **m-9** — the Decision-8 annotation (m-9 authors, in its own document) · advisory-not-gating checksums as a *question* not an instruction · whether last-record completeness covers torn writes alone.
2. **m-9 + m-10** joint, **m-3** joined, **m-1** boundary review — the **edited-session state machine** (F2). No mechanism preselected.
3. **m-10 + m-3**, **m-9** joined if the worker boundary is observed — the **fencing predicate contract** (F3), before lane 4.
4. **m-9 + m-10** — the member-set removal amendment (Decision 3), m-3-joined; `segment_id` is dead under one-file-per-run, **`seq_hwm` probably is not** — a high-water mark still means something in a single file, and m-3 draws that line.
5. **lane 4** — LAST, after ratification: the eleventh fixture and the sample re-balance.

Then: fan the exact owner returns into **one additive supersession record** covering the D1 floor + member removals + the narrowed durability claim + external-edit semantics + the new §7 fencing predicate and cardinality → VP exact-byte review → operator ratification → only then lane-4 resume.

## Boundaries
Records decisions and repairs provenance. Performs no trim, authors no amendment, prescribes no owner mechanism, edits or annotates no owner document, changes no fixture or manifest, moves no locked byte, resumes no held member, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. `receipt_conflict` stays frozen. Interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, §D-settlement amendment `1fa71cb8…`, m-9 delta `01b885fe…`, m-9 stage-4 doc, m-10 rev16 `3e3c5192…`, m-3 r24 `651c9aec…`, m-8 contract `4b670a79…` all UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- VP review read at its bytes: `…-esc1-ratify-1/RECONCILE-orchestrator-reviewer-20260726-025248.md`; all five findings accepted, none contested.
- §8b direct-route contract re-read at `master/STEP-3-ARCH-AMENDMENT.md:118-121` (recipient record = E0 evidence, not transferable authority).
- `ROADMAP.md` correction verified on disk this turn: `"What binds the MVP now"` → 0 occurrences; `receipt_conflict`-must-not-hard-fail mandate → 0 occurrences; pending-carry paragraph present and rewrapped to the file's 108-char convention.
- fork-4 `1995bdef419f0d54ef6efeaa02b558bedf91384fbe601c901517dbc3338bf2d7` and the ten owner-return hashes stand as bound there; ratify-1 `71d94e3b2391781e9bc543647fb66a4351c48101a14f08e036c937fdd68494e5` superseded, unmutated.
- Binding owner-path rule `master/CYCLE-PLAYBOOK.md:222`; lane-4 authority limits `master/STEP-3-LANE4-PLAN.md:14-18,29-35,73-101` as the VP cites them.
- Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this replacement record + one INDEX.md row + the `ROADMAP.md` binding-paragraph correction (master-owned maintenance surface, corrected to pending status per VP-F4). No trim, no amendment, no owner document edited or annotated, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: the **VP** confirms this replacement record and the `ROADMAP.md` correction close F1/F4/F5 and that F2/F3 are withdrawn-and-routed rather than re-prescribed. Master then issues routes 1–4, holds lane 4 until the fencing contract and the amendment are ratified, and returns the exact additive supersession bytes for VP exact-byte review. Decisions 1–8 stand; amendment ratification, lane-4 resume, fixture freeze, re-lock, T4, and external use remain held.
