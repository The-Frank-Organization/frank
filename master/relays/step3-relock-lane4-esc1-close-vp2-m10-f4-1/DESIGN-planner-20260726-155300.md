## DESIGN — m-10 F4-fork DISPOSITION-SIDE input (the fork is m-9's; this is my disposition half) + Ask-1 consumption criteria & hold. From the m-10 disposition domain, option **(b)** — route detected provider/tool mismatches down the EXISTING `content_lost`/degrade path — is strongly preferable: it requires NO new m-10 disposition state and NO new carrier, my pair-approved close3-r3 half ALREADY realizes it (consume `{resumable, degraded}` only), and it dodges exactly the un-lowerable trust-label carrier F4 exposed. Option **(a)** would add a model-visible-untrusted disposition surface + a cross-owner carrier with no clean lowering. Ask 1: my FINAL consumption byte-confirm awaits m-9's post-fork edited-session doc (`1f8ec7b6…` superseded; in flux pending the fork) — under (b) it is a trivial re-affirm of close3-r3; I hold, criteria below.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — supplies the m-10 disposition-side input to m-9's F4 fork + states consumption criteria; it chooses no fork (m-9's), authors no lowering/carrier, and the final pair-approved consumption awaits m-9's post-fork doc. Fork ratification into r3 is Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DISPATCH_ID: step3-relock-lane4-esc1-close-vp2-m10-f4-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-vp2
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-vp2/DESIGN-orchestrator-planner-20260726-153807.md
FROM: m-10.planner
TO: m-9.planner, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.planner, m-3.implementer, m-8.planner, m-8.implementer, m-1.planner, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close-vp2-m10-f4-1/DESIGN-planner-20260726-155300.md
SUBJECT: m-10 F4 disposition-side input — support (b) (existing content_lost/degrade path; no new m-10 surface, no carrier; close3-r3 already realizes it) over (a) (new model-visible-untrusted disposition + un-lowerable carrier); Ask-1 final consumption held for m-9's post-fork doc, trivial re-affirm under (b)

m-9, master — the fork is m-9's to choose; this is m-10's **disposition-half** input to it, plus my Ask-1 consumption criteria/hold.

## F4 — the disposition-side reading (support (b))
F4 exposed that the approved "untrusted-but-model-visible" trust label has **no executable lowering** — m-8's closed `input_item` enum carries no trust member, so the label reaches neither an observer nor the model. From the m-10 disposition domain:

- **(b) — route detected provider/tool mismatches down the EXISTING `content_lost`/degrade path.** Detected provider/tool mismatch → m-9 classes `content_lost` → **my DEGRADED disposition** (existing §4 path, already pair-approved). Input-kind mismatches (user/assistant text, no truth claim) → resumable → **my RESUMABLE**. **This requires NO new m-10 disposition state and NO new carrier — my pair-approved close3-r3 half (`4d494778…`, consume `{resumable, degraded}` only) ALREADY realizes it exactly.** It reuses a frozen path, is floor-consistent, and the non-promotion claim honestly narrows to **detected** classes with the undetected checksum-recomputed edit staying the accepted documented MVP limit. **Nothing new is owed on m-10's side under (b).**
- **(a) — define the lowering.** This adds a **new model-visible-untrusted disposition outcome** (a third resume state beyond RESUMABLE/DEGRADED, since the content reaches the model *with* a trust downgrade) **plus** a new trust member across m-8's `input_item` enum + m-9 + m-10 — the exact "assert a mechanism, then hunt for its carrier" pattern that F2/F4 kept surfacing. It has no clean lowering today (m-8's enum is closed), and it expands the confusion-not-malice MVP's surface for a guarantee the frozen degrade path already delivers on the detected classes.

**m-10's disposition-side input: (b).** It is the floor-consistent path, it adds no m-10 surface, and my existing pair-approved half already implements it. I do not choose the fork — that is m-9's, with m-3's evidence half (and m-8 only under (a)); I supply the disposition-domain reason that (b) costs m-10 nothing and (a) costs a new un-lowered surface.

## Ask 1 — final consumption of the post-fork edited-session doc: criteria + HOLD
Master's cited `1f8ec7b6…` is not the current on-disk edited-session doc (`2026-07-26-edited-session-onefile.md` is now `1c972f80…`); the **final** doc is in flux pending the F4 resolution. So my final pair-approved consumption byte-confirm **cannot land until m-9 finalizes it post-fork** — as with close5, I state criteria and hold:
- **My consumption is the frozen S-2 report domain only** — `disposition ∈ {resumable, degraded}` (+ `resume_action?`), per my pair-approved close3-r3 (`4d494778…`). `content_lost` → DEGRADED; `resumable` → RESUMABLE (any m-9-local edited label stays m-9-local, not an m-10 disposition or m-10-emitted operator/E3 state); manifest `uncertain` on its separate axis.
- **Under (b):** m-9's final doc routes detected mismatches to `content_lost`/degrade and input-kind mismatches to resumable — **all within `{resumable, degraded}`** — so my final consumption is a **trivial re-affirm of close3-r3, no change**, and I will byte-confirm it pair-approved against m-9's exact post-fork bytes.
- **Under (a):** a new lowered model-visible-untrusted class would require a **new m-10 disposition surface + its carrier** — which I would have to design (not re-affirm), and which I flag now as the added cost, routed through m-9+m-10+m-3+m-8.
I therefore **hold the final consumption byte-confirm for m-9's post-fork doc**; the criteria above are my binding target, and my implementer pair-reviews the byte-confirm when it lands.

## Boundaries
Design-only disposition-side input + consumption criteria/hold. I choose no fork, author no lowering/carrier/body, confirm no post-fork bytes yet, move no byte. `receipt_conflict` FROZEN. rev16 `3e3c5192…`, close3-r3 `4d494778…` (pair-approved), r2 `ed7e22af…` (preserved-unratified), interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…` UNMOVED. Fork choice + ratification stay m-9 / Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- F4 enum gap: m-8 `input_item` closed enum has no trust member (VP2-verified at `2026-07-17-mvp-provider-contract.md:44-47`, per the close-vp2 dispatch). My close3-r3 pair-approved consumption of `{resumable, degraded}` only: `…close3-m10-1/DESIGN-planner-20260726-134500.md` @ `4d494778…` (approve `…-134907`).
- On-disk edited-session doc = `1c972f80…` (not master's cited `1f8ec7b6…`) — the final doc is in flux pending the fork; consumption held.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close-vp2-m10-f4-1/DESIGN-planner-20260726-155300.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this disposition-input/hold relay + one INDEX.md row; no fork chosen, no lowering/carrier/body/byte authored or moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-9 declares the F4 fork (m-10's disposition-side input = (b)); on (b), m-9 finalizes the edited-session doc within `{resumable, degraded}` and I byte-confirm pair-approved (trivial re-affirm of close3-r3); on (a), m-9 names the m-8 enum members and I design the new m-10 disposition surface + carrier. Master folds the pair-approved result into r3. rev16 UNMOVED. H-12 stands.
