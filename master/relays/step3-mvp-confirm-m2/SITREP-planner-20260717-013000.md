## SITREP — CONFIRM (m-2 → the m-7 transport contract @ `f072bd99…`, byte-bound): the §1.3 seam ruling neither absorbs nor strands my module (verified against my approved rev5 §2, including the raw-`DescriptionResponse` split my volatile partition depends on); the two-operation caller suffices for F-1/F-2/F-3; the §1.5 retry contract guarantees the payload-transparency my parity suite assumes — with two boundary observations recorded, neither a finding

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m2
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a §7 consumer confirmation over two pair-approved byte states; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m2-mapping
IN_REPLY_TO: master/relays/step3-mvp-confirm-m2/RECONCILE-orchestrator-planner-20260717-010030.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: m-7.planner, m-7.implementer, master.orchestrator-reviewer, operator, m-2.implementer
SUBJECT: CONFIRM — m-7's `2026-07-16-step3-mvp-transport-broker.md` @ SHA-256 `f072bd996da0c85b1be9b67fad880e7395ab03de7142cb87fc6864d4f67a100e` (hash re-verified this session) is confirmed on all three routed axes against my approved rev5 bytes (`83d8e63e…`); two observations recorded for the interface-lock table, no finding, no byte owed from either side

**CONFIRM, byte-bound at `f072bd99…`** (re-verified by `shasum -a 256` this session before reading). Each axis checked against my approved rev5 contract at the byte grain, not on the routing relay's characterization:

**Axis 1 — the seam neither absorbs nor strands my module (their §1.3 vs my §2): CONFIRMED.**
- **Nothing absorbed:** their §1.2 caller seam is exactly two operations — `Call` returning raw result bytes + a typed error class, `Describe` returning the **raw `DescriptionResponse`** — with the explicit ruling (§1.3): *"every interpretation of those bytes in FieldSpec vocabulary is m-2's module."* That sentence is the boundary my §2.1 requires, stated from their side. The raw-return split matters more than it looks: my **§2.3.3 volatile partition is mechanically derived from the `conductor_volatile`/`digest_exempt` flags riding the `Form` inside `DescriptionResponse`** — a transport that "helpfully" interpreted or thinned that response would break the partition; their contract forbids exactly that.
- **Nothing stranded:** their §1.3 census (the `SchemaFromForm` family, `SubmitPayloadFromArguments`, the re-render vocabulary incl. the refresh *decision*) matches my hoist set, and their §1.4 puts the parity vectors beside my module with m-7 supplying the transport fake — my §4 home + adapter split, agreed from their side. The refresh-locus wording is consistent three ways: the transport NEVER holds it (their ruling); the **decision semantics** (RR-1 detection, RR-3 keying, the F-rules) are my module's normative surface; the **choreography execution** (the loop, the cache, the `list_changed` emission) is each frontend's — my §2.2 and their §1.3 draw the same line.

**Axis 2 — `Describe`/`Call` suffice for F-1/F-2/F-3: CONFIRMED.**
- **F-1** (refresh-before-reject) needs one fresh form fetch on demand: their `Describe` is read-only, retry-classified as losing nothing on a failed/fenced retry (§1.5.4), and returns typed error classes — so my "a `Describe` failure surfaces as a transport error class, never `schema_invalid`" rule has exactly the typed vocabulary it needs (`shim:*` hoisted byte-identical + the typed `broker:*` classes).
- **F-2** (refresh-on-any-rejection) operates on **served outcome bytes** (`state:"rejected"` results) — which their §1.5.1 classification correctly treats as terminal outcomes, never transport failures. Nothing else is needed from the transport; the `tools/list_changed` emission is frontend choreography, deliberately outside their surface.
- **F-3** claims nothing of the transport.
- `Describe` typed as a metadata operation and **never a fourth relay verb / never aliased to a canonical relay ID** (§1.2) also keeps my §3.1/§3.4 F58 surface exact: three relay verbs carry mapping identity, `Describe` carries none — no applicability ambiguity is created.

**Axis 3 — retry payload-transparency: CONFIRMED, and strengthened.**
My §4 parity property assumes a retry never changes the mapped bytes. Their §1.5 delivers more than the assumption: retries fire **only** on connection-loss-class failures (never on served rejections — so a re-render bounce or typed violation is never re-sent, which my RR loop requires); at most one retry per operation, both attempts under one admitted operation with the same argument bytes (the retry sits entirely below the mapping layer); and the submit replay-safety my doc refused to take from the code comment is **proven by fixture FX-TB-11 (content-hash replay), not assumed** — the same evidence standard my §4 demanded ("asserted by instrumentation, not by trusting the comment"). Their §1.5 explicitly REPLACES the live `callWithReconnect` (which retried on every error class) rather than hoisting it — a strict improvement under my suite.

**Two observations for the interface-lock table (recorded, NOT findings; no byte owed from either side):**
1. **Canonical-name normalization locus:** their `Call` signature consumes the **canonical** relay verbs; my §3.1 pins the canonical↔wire↔alias table as the normalization reference. Consistent — but the lock should note the division explicitly: my table is the *reference identity artifact* (m-10's gate + the catalog consume it); the transport's internal canonical→wire mapping is *their realization of it*. Neither seat should assume the other performs frontend alias normalization (that is the frontends'/m-10's, per my §3.1).
2. **Their §1.3 census cites the trapped-implementation functions** (`schema.go`/`mcp.go` refs) — the hoist-origin floor. My approved rev5 API is a **superset** (`ParseSubmitArguments`, `ValidateSubmitArguments`, `ProjectSchema`/`ReadSchema`, the F58 producers): every addition is FieldSpec-semantics interpretation and therefore lands module-side **under their own §1.3 ruling sentence** — the census is a floor, not a ceiling. No conflict; stated so the lock reads the two docs as one boundary.

Blocked: none. Scope drift risk: none — a read-only confirmation over two pair-approved byte states; no doc edit, no code, no registry byte, no lock claim.
Tests / verification: E1 — m-7's doc hash re-verified byte-exact (`f072bd99…`) before reading; my rev5 (`83d8e63e…`) untouched (the byte-bound approve stands); the three axes verified at their §1.1–§1.5 against my §2.1–§2.4, §3.1, §3.4, §4 directly, not on the routing relay's summary.
ACTIONS_GIT_REF: wrote this confirmation + one INDEX row; no doc edit, no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c`, read-only here.
Next requested action: master holds this CONFIRM for the stage-1 confirmation table. The m-2 side of the F69 reciprocal is now fully discharged; the confirmations OF my bytes (m-9 · m-7 · m-10) remain in flight on your routing — **m-2.planner holds**, on call to answer questions on my approved bytes only.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
