## SITREP — OPERATOR-DIRECTED CADENCE CHANGE on the standing s10 token: per-task review gating is DROPPED (unsustainable under hand-relay); build T1→T11 STRAIGHT THROUGH with ONE end-of-slice adversarial review; the Step-5/fidelity confirms batch to the end (stop-on-contradiction); the non-review stops all survive unchanged

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-impl
PARENT_DISPATCH_ID: s10-build-plan-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — an operator-directed execution-cadence change on the already-issued token; merge remains operator-only, unchanged
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-impl/IMPL-planner-20260712-232010.md
FROM: s10.planner
TO: s10.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-2.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: the operator ruled (2026-07-13, direct to s10.planner, quoted verbatim below) that per-task review relays are unsustainable with hand-relaying — the s10 build now runs once all the way through with review churn accepted at the end; this relay amends ONLY the review cadence of my `…-232010` token and `…-002732` ruling; every non-review discipline, gate-of-substance, and stop survives verbatim

**The operator directive (quoted, the license for this change):** *"we went just fine with going once all the way through and then review at the end, this is unsustainable with hand relaying, mayhaps ok with relay automation in the future when this is all built, we can take a bit more review churn at the end."*

**What changes:**
1. **No per-task review relays.** After the T1 commit (finish it exactly per my `…-002732` ruling — T1-g + T1-f, tripwire and semantic-invariance rule binding), proceed T2 → T3 → … → T11 WITHOUT stopping for `s10-build-t<N>-review` legs. Task order, one-commit-per-task, and the §3 seam map stay.
2. **ONE end-of-slice adversarial review (mine), at T11.** I review the WHOLE slice against the plan-of-record + every ruling: all RED-first negatives, the FILE-captured batteries, the diff→license fence-table over every commit, the two mechanical tables, label→mechanism + verify-the-summary-line across all tasks, the I-PH sweep, both sunset demonstrations, the T10 claim-text rider, EXIT LEG 3. The operator has priced more churn here — findings fold as ordinary REVIEW-FOLD legs then.
3. **Owner confirms batch to the end (stop-on-contradiction).** The m-2/m-7 Step-5 confirms of the landed T1 bytes + the genesis derivation arm, m-7's T8 scheduler/A-2 look, and m-3's T6/T9 J1/hook fidelity all ride the end review instead of blocking mid-slice. Operator-priced residual, stated plainly: the T1/T2-before-T4 owner gate converts from blocking to ASYNC — its original target (build-seat-invented schema bytes) is already discharged (the bytes are owner-authored, landed byte-verbatim, OLD-matched), so what batches is the owners' verification of the landing; **a late owner contradiction stops the build wherever it stands and unwinds through master** — that risk is accepted by the directive. (Owners are CC'd here; any of them may object early from CC via ordinary traffic.)

**What does NOT change (the stops that are not review ceremony):**
- **Fence discipline:** a file/seam outside the 16-row block + named-seam licenses → STOP, escalate to master. Never self-expand.
- **Owner-byte fidelity:** an OLD-site mismatch → bounce to the owner, never adapt. `Spawn:83-95` never touched. Schema beyond the ONE T1 transition → master. Lock changes → amendment path.
- **The `…-002732` red lines:** the pinned-v5 tripwire (no hash update, ever) and the fixture semantic-invariance rule — a collision is a STOP-and-return even mid-flight.
- **Condition (f):** a genuinely blocked lane (a red you cannot clear within your named seams) reports and holds — blocker relays are not review churn.
- **Self-executed disciplines, every task:** TDD/RED-first negatives, green-at-every-commit (full battery, FILE-captured under `frank/.relays/s10/`), the seam/license table maintained as you go, byte-exact terminals, R2, Rails A/B per surface, the T10 gate-lift rider verbatim, INV-CATALOG green at every commit point. These cost no relay and are what makes the end review checkable.
- **T11's exit shape:** the exit battery + tables + I-PH sweep + the live EXIT LEG 3 (the dogfood ODB verdicts are the operator's own) + my end review → the merge-decision relay TO the operator. **Merge stays operator-only via HUMAN_MERGE_AUTHORIZATION** — cadence does not touch condition (e).
- **Task-state map:** T9 open (amended semantics), T10 open (gate-lift + rider), T3's owner cells all clear (m-6 `…-002109`), the emit-vs-submit boundary binding on T3/T5/T8.

**For master (CC'd):** this is an operator cadence ruling on the s10 execution layer; recommend recording it in `PROTOCOL-DEVIATIONS.md` + the CYCLE-PLAYBOOK friction log (per-task review gates priced out by hand-relay latency; revisit when relay automation exists — the product being built is, not incidentally, the automation in question). The catch-layer record so far (fence catch #1, F-b catch #2, the T1 battery hold) all predate this change and stand.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-impl/SITREP-planner-20260713-004230.md` — run before handoff.
- The directive quoted verbatim from the operator's 2026-07-13 instruction to this seat; no other input changed — the token `…-232010`, the seam ruling `…-002732`, and all owner returns stand as issued except the review-cadence clauses this relay amends.

ACTIONS_GIT_REF: none — a cadence-amendment relay; no `frank/` edit. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260713-004230.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `8941889`; `s10-comms-spine` carries the in-progress T1 diff per the build seat's last E2 report.
Next requested action: build seat finishes T1 per `…-002732`, then runs T2→T11 straight through under the unchanged disciplines and stops; ONE implementation report at T11 completion (the whole-slice evidence package); I run the end-of-slice adversarial review, batch the owner confirms, and author the merge-decision relay TO the operator.
