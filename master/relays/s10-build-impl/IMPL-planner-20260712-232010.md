## IMPL DISPATCH — s10 comms-spine build authorized: delegated same-owner token on the r2-approved reissued PLAN; all-in 16-row fence at named-seam grain; T1/T2 owner gate before T4; T9 and T10 recorded CLOSED pending the two master items routed beside this token; T1–T8 + T11-prep proceed

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s10-build-impl
PARENT_DISPATCH_ID: s10-build-plan-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the T11 live dogfood ODB verdicts are the operator's own (operator-FROM by locked design); the slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e)
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-plan/PLAN-REVIEW-implementer-20260712-224042.md
FROM: s10.planner
TO: s10.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: delegated implementation dispatch for the s10 comms spine — parented to your approving PLAN-REVIEW r2 (`s10-build-plan-review-r2`, approve, all-in), issued under its five token conditions verbatim; the fence is the 16-row block below at the master-granted named-seam grain; T9/T10 are dispatched-but-CLOSED with their opening conditions recorded; build T1→T8 vertical-slice-first now

All delegated-dispatch preconditions are met: conditional dispatch authority runs through the orchestrator delegation (`s10-dispatch/PLAN-orchestrator-planner-20260712-205011`) → the m-6 gate-clearance token (`s10-plan-m6-impl/IMPL-planner-20260712-194423`) → my boot (`master-boot-s10-planner`, the same-owner loop authorization); your PLAN-REVIEW returned **approve** (`s10-build-plan/PLAN-REVIEW-implementer-20260712-224042`) on the reissued PLAN; the mechanical SCOPE_DIFF below is **all-in** with no hard trigger against the dispatched scope; and the PARENT chain points to that approving review, whose parent is my pair-Planner PLAN (`s10-build-plan/PLAN-planner-20260712-223505`) addressed to you. I therefore issue the delegated token.

DISPATCH IMPL

**The fence (your token condition 2 — the 16-row all-in block; file-wide rows do NOT erase the named-seam licenses):**

SCOPE_DIFF:
- frank/internal/fieldspec/registry.json -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/predicate.go -> in
- frank/internal/fieldspec/render.go -> in
- frank/internal/fieldspec/registry_test.go -> in
- frank/internal/config/config.go -> in
- frank/internal/store/genesis.go -> in
- frank/internal/obligation/obligation.go -> in
- frank/internal/engine/submit.go -> in
- frank/internal/engine/loop.go -> in
- frank/internal/engine/ -> in
- frank/internal/observe/ -> in
- frank/internal/executor/executor.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/ -> in
- frank/.relays/s10/ -> in
SCOPE_DIFF_RESULT: all-in
HARD_TRIGGER_SCAN: none against the dispatched T1–T8/T11-prep scope — the two owner flags (m-7's T9 trigger-sentence amendment; m-3's T10 F-b gate-only disposition) are routed to master in `s10-build-escalate-amendments/SITREP-planner-20260712-232000` with their tasks CLOSED in this dispatch; no schema beyond the ONE T1 transition; no locked-contract edit is licensed to you; merge operator-only.

**Named-seam licenses (master's grant `s10-build-escalate-fence/PLAN-orchestrator-planner-20260712-222627` §1, binding — unrelated edits inside these four files are out-of-fence deviations):** `config.go` — ONLY the capability exact-set (`:307`, m-7's owner move carried on the branch) + the successor relation gains `v7→v8` (`:406-418`, m-2's owner bytes); `registry_test.go` — ONLY the `:18` embedded-version assertion; `executor.go` — ONLY the T9 kill-extend seam at `:170-190` (AND T9 is closed, so NO executor edit until it opens; `Spawn:83-95` is never licensed — an edit there stops and returns through master); `main.go` — ONLY the two composition blocks (`:146-158`, `:268-294`).

**Task state at dispatch (your token condition 4, the live owner state):**
- **OPEN: T1→T8 + T11-prep**, vertical-slice-first, in order, one commit boundary per task, each task's diff reconciling to its §3 seam in the reissued PLAN (acceptance item 14).
- **The T1/T2 owner-fidelity gate before T4 (binding):** m-2 executes/confirms the T1 registry transition bytes + successor relation; m-7 executes/confirms the capability-marker move. Owner confirms in hand before any park fixture opens; a contradiction escalates to master BEFORE T4.
- **T9 CLOSED** until master accepts m-7's two-sentence §2.7/ceiling amendment (routed, `s10-build-escalate-amendments`). m-7 Confirm 1 confirmed the kill machinery composes untouched; only the locked trigger sentence awaits truing.
- **T10 CLOSED** until master disposes the F-b question (routed, same relay). The m-3 fold is IN HAND and composes with m-7's Confirm 2 under the gate-only reading: representative `run-suite-unbounded` entry (additive, `observe/registry.go`), pre-spawn per-entry allow consumed from the live T5-validated operator-FROM verdict, typed `unsafe`/`side-effecting-unapproved` pre-spawn refusal, nothing spawns. Build NOTHING for T10 until master's disposition lands and I relay the opening.
- Any further amendment flag, owner mismatch, or discovered need to touch `Spawn:83-95` ⇒ STOP, report, hold (condition f) — return through master, never adapt owner bytes.

**Conditions (a)–(g)** verbatim from the m-6 token, binding: (a) unique sub-DISPATCH_IDs (`s10-build-t<N>-review`/`-fold`, batteries FILE-captured under `frank/.relays/s10/`); (b) schema beyond the ONE T1 transition → master BEFORE work; (c) locked-contract changes → owning pair + master, never silent; (d) cross-domain beyond the authorized hooks → master; (e) merge operator-only via `HUMAN_MERGE_AUTHORIZATION` at grant time; (f) blocked lanes report and hold; (g) the dogfood evaluation extends the s8 ledger as ONE series.

**Standing disciplines (day-one):** TDD with RED-first negatives per task; byte-exact `{accepted, rejected, held}`; R2 (the ⑤ model-name field payload/render only); Rails A/B as stated per task in the plan-of-record; I-PH on every new seat-visible surface; egress fixture-scoped; INV-CATALOG's ten laws green at every commit point; label→mechanism + verify-the-summary-line in every review request; the two mechanical tables maintained as you go. Branch `s10-comms-spine` off `main@8941889`; execution under `superpowers:executing-plans` task-by-task; per-task review requests TO me under `s10-build-t<N>-review`.

**Not granted:** merge (condition e — your token condition 5), lock changes, T9/T10 work before their openings, any file/seam outside the block above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-impl/IMPL-planner-20260712-232010.md` — run before handoff (result in the inline pointer).
- Lineage: PARENT `s10-build-plan-review-r2` → your PLAN-REVIEW **approve** (FROM s10.implementer, all-in mechanical check) → its parent `s10-build-plan` → my pair-Planner PLAN r2 (FROM s10.planner, TO s10.implementer). The five token conditions of your approval are each satisfied above (parenting/addressing/token shape; the exact block + seam limits; conditions and gates preserved; live owner state recorded; no merge authority).

ACTIONS_GIT_REF: a dispatch/authorization relay; no `frank/` edit, no code, no commit by this file. The build begins in the s10.implementer session on `s10-comms-spine` off `main@8941889`. Artifacts this turn: this relay + the `s10-build-escalate-amendments` relay + two `master/relays/INDEX.md` rows timestamped 20260712-232000/232010.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `8941889`; the `s10-comms-spine` worktree clean at `8941889`.
Next requested action: operator carries this to s10.implementer; the build runs T1→T8 under the fence + conditions with the T1/T2 owner gate before T4; I gate per-task reviews, stand by for blocker relays, relay the T9/T10 openings when master's dispositions land, and author the merge-decision relay TO the operator at slice exit. No merge without HUMAN_MERGE_AUTHORIZATION.
