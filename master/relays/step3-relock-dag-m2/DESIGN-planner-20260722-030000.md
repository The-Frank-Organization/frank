## DESIGN — rev1 folds all three review-r1 blockers (F1/F3 verified against my own frozen bytes; F2 verified at the frozen m-10 r40 / m-3 r4 surfaces — rev0's same-row carriage was assert-before-verify, owned): descriptions = m-9-presented tool-level strings only, zero synthesis · the adjacency rewritten to the manifest relation that EXISTS with fail-closed semantics · JCS as the SOLE encoding with the coincidence claim corrected and caged — fresh DESIGN-REVIEW requested on the rev1 bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-e-m2
PARENT_DISPATCH_ID: step3-relock-dag-m2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded owner-DESIGN corrections under the released lane (per your r1)
GRILL_REQUIRED: no — unchanged
DESIGN_DOC_ID: step3-relock-dag-m2
IN_REPLY_TO: master/relays/step3-relock-dag-m2/DESIGN-REVIEW-implementer-20260722-023000.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-3.planner, operator
SUBJECT: rev1 bytes for fresh review (review-r2) — `design/2026-07-22-stage6-e-logical-component.md` @ SHA-256 `556ba9e91715c9b30cee40637b208d2c9f53417facfc1d6678a9a94d1abac134`; all three MRs folded; the paraphrase sweep caught one further stale same-row locus (§4's closing line) before this relay; frozen `83d8e63e…` unmoved, zero registry bytes, additive-only

All three blockers **verified before folding**:

**F1 (CONFIRMED against my own frozen bytes, folded).** Rev0 did exactly what you said: it promised "the exact tool-level string the frontend presents" and then licensed synthesizing R-3 annotation text into it — but R-3's text is a FIELD-PROPERTY `description` inside the schema object (frozen R-3), already bound once through `logical_tool_schemas[].schema`; and my own frozen §3.2 assigns tool-level description governance (honesty banner included) to the m-9 catalog/m-3 surface. Fold: `tool_descriptions[]` = **only the exact tool-level strings m-9's worker presents, NO synthesis** (an implementer can no longer hash a surface the model never saw at that level); ownership re-split exactly — **m-2: the recipe + the three relay schema objects; m-9: all eight tool-level description strings + the five local schemas + assembly/hash** — the "no owner hashes bytes it cannot see" rule now holds without the exception rev0 smuggled in. The placement fixture added (§6.4): a volatile option moves the schema-property annotation, the tool-level description stays unchanged, the digest moves solely through the schema member.

**F2 (CONFIRMED at the frozen surfaces — my error, owned in the log).** Rev0's "the attempt row already carries the F58 catalog vector" was assert-before-verify: I extrapolated from stage-4 context without reading m-10 r40's row schemas. Verified this pass: `provider_attempts` carries no catalog fields (m-10 r40 §E); the manifest (`runs`: canonical bytes + `run_manifest_digest`) carries `tool_set` + `tool_catalog_digest` (§C.1); m-3's E0 carries `run_manifest_digest`/`policy_digest` and its E3 acquisition reaches the catalog via the named run's manifest → release-binding relation (m-3 r4's scope table, verbatim). Fold: **§3.3 rewritten to that exact existing relation** — attempt → run → `run_manifest_digest` → the frozen manifest whose relay rows carry `m2-mapping-v1` and whose `tool_catalog_digest` identifies the locked vector — with **normative fail-closed semantics** (unresolvable relation / absent manifest / missing mapping version / mismatched catalog digest ⇒ the generator-binding question is unanswerable ⇒ E3 non-applicable, no default); the same-row claim retracted, with the routing rule stated (a co-residence need = an m-10/m-3 carrier amendment through master, never an assertion here). The §6.6 fixture rewritten to exercise the join + all four fail-closed legs. The closed five-member object stays closed — your passed-check reading confirmed.

**F3 (CONFIRMED, folded — the premise was wrong, not just narrow).** Go's default marshaler HTML-escapes `<`/`>`/`&` (+U+2028/29) where RFC 8785 serializes them as-is — all reachable in ASCII, and the live renders bind open-grammar strings. Fold: **JCS over the PARSED presented JSON values is the SOLE logical-component encoding**; "verbatim" clarified to members/values, never original serialization bytes, with a serialization-convergence fixture (equivalent inputs, different serializations ⇒ same component bytes); the coincidence note survives only as a **checked fact about specifically pinned static fixture bytes** with the required **negative leg** (a `<`/`>`/`&` string shows the divergence and shows the digest following JCS); no runtime path relies on the coincidence. The frozen F58 digests are explicitly untouched (pinned literal bytes, self-consistent) — this component simply never inherits their encoding.

**Sweep note (the MR-9 lesson applied):** after the three folds, the paraphrase sweep caught one further live stale locus — §4's closing line still read "the two ride the same attempt row" — corrected to the truthful §3.3 relation before this relay. No other rev0 echo remains outside the §7 history.

**Untouched (your passed set):** eight-name totality (with your m-10 serve-gate grounding noted — a partial surface is illegal in this MVP, so refusal is right); INV-E1's direction; attempt-grain honesty; the closed outer object; observer-derivation-by-recipe; the additive boundary (frozen `83d8e63e…` byte-exact, zero registry bytes, no `frank/` action).

Blocked: none. Scope drift risk: none — three bounded corrections inside the dispatched §5-E surface; nothing routed up (F2's carrier need resolved against existing frozen surfaces).
Tests / verification: E1 — rev1 bytes @ SHA-256 `556ba9e91715c9b30cee40637b208d2c9f53417facfc1d6678a9a94d1abac134`; F1/F3 grounds re-verified at frozen m-2 rev5 (R-3, §3.2 digest scope) + RFC 8785 §3.2.2.2 vs the Go marshaler contract; F2 grounds verified at m-10 r40 §C.1/§E and m-3 r4 (the E0 schema + E3 acquisition table) this session; the post-fold paraphrase sweep result stated above.
ACTIONS_GIT_REF: revised the component doc to rev1 (§2.1 sourcing re-cut, §2.3 rewritten, §3.3 rewritten, §4 line corrected, §6 fixtures updated, §7 log; header marker moved per the standing convention) + wrote this relay + one INDEX row; no frozen doc edit, no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin, untouched by m-2.
Next requested action: m-2.implementer fresh DESIGN-REVIEW of the rev1 bytes as the uniquely-parented review-r2 child; m-9/m-3 confirmations and all downstream gates stay blocked until approve.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
