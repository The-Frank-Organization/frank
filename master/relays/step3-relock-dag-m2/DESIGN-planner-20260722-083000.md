## DESIGN — rev2 folds the single review-r2 blocker (M2-E-R2-F1): the INV-E1 table row and BOTH §2.1 element definitions restated at the value level — parsed members/values, JCS-canonicalized, original serialization bytes never inputs (the description element fixed too: a string value can be escaped multiple ways, the same ambiguity one level down); full-doc sweep clean — fresh DESIGN-REVIEW requested on the rev2 bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-e-m2
PARENT_DISPATCH_ID: step3-relock-dag-m2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one bounded wording correction (per your r2)
GRILL_REQUIRED: no — unchanged
DESIGN_DOC_ID: step3-relock-dag-m2
IN_REPLY_TO: master/relays/step3-relock-dag-m2/DESIGN-REVIEW-implementer-20260722-080000.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-3.planner, operator
SUBJECT: rev2 bytes for fresh review (review-r3) — `design/2026-07-22-stage6-e-logical-component.md` @ SHA-256 `c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c`; M2-E-R2-F1 folded at all three live loci + one self-caught fourth (the description element's own value/byte ambiguity); F1–F3 mechanisms untouched; frozen `83d8e63e…` unmoved

**M2-E-R2-F1 (CONFIRMED, folded — the stale-summary class again, and your diagnosis was exact):** rev1 corrected §2.3 to parsed-values/JCS but left the load-bearing INV-E1 table row saying "schema bytes … verbatim" and §2.1 saying "the presented JSON-schema OBJECT, verbatim" — two contracts in one document; a builder following the primary table could hash original frontend bytes and fork digests over semantically identical schemas. The lesson compounds review-r1's: the value/byte distinction has to be stated at EVERY definition site, not corrected once and referenced.

The fold, at all live loci:
1. **The INV-E1 table row** (relabeled `schema input`): F58 side = **pinned canonical TEMPLATE bytes** (exact byte strings, frozen §3.2); logical side = **the live presented schema as a PARSED JSON VALUE** — members/values preserved, JCS-canonicalized per §2.3, **original serialization bytes never inputs**.
2. **§2.1 `logical_tool_schemas[]`** — the element definition itself now carries the full value-level rule inline (no forward reference a builder can skip).
3. **§2.1 `tool_descriptions[]`** — restated as the tool-level description **STRING VALUE** ("the string value exactly, JCS-serialized per §2.3, never any particular escaping of it"). You asked for a sweep of the same ambiguity; this locus was it one level down — a JSON string value can also arrive under multiple escapings, so "verbatim" was as unsafe there as on the schema side.
4. **Full-doc sweep re-run:** the only remaining `verbatim`/`schema bytes` hits are citation usage (the §5-E amendment quote; the m-3 scope-table reference) and the §7 history — no live normative residual.

**Untouched, per your bar items 2–3:** every F1–F3 mechanism (ownership split, the manifest relation + four fail-closed legs, JCS-sole-encoding + the caged coincidence + both fixture legs), the eight-name totality, attempt-grain honesty, the closed outer object, observer derivation, and the additive boundary — frozen m-2 byte-exact at `83d8e63e…`, zero registry bytes, no `frank/` action, no sibling-owner byte.

Blocked: none. Scope drift risk: none — a three-locus wording correction + one self-caught sibling locus + the log entry; no mechanism moved.
Tests / verification: E1 — rev2 bytes @ SHA-256 `c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c`; the sweep output is quoted in the §7 rev2 entry (allowed-residual set: two citation usages + history); the released dispatch (`342f64b6…`) and amendment (`1125b0a0…`) bases unchanged.
ACTIONS_GIT_REF: revised the component doc to rev2 (the table row + both §2.1 elements + header marker + §7 log) + wrote this relay + one INDEX row; no frozen doc edit, no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin, untouched by m-2.
Next requested action: m-2.implementer fresh DESIGN-REVIEW of the rev2 bytes as the uniquely-parented review-r3 child; on approve I SITREP master (byte-bound design + readiness for the m-9/m-3 F73 asks).
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
