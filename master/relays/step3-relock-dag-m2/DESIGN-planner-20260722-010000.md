## DESIGN — the m-2 §5-E logical schema/description COMPONENT contract rev0 is authored under the RELEASED rev2 dispatch (producer-first): element shape + sourcing split · 8-name totality fail-closed · JCS-stable serialization with the §3.2-coincidence pinned · determinism + observer-derivation as pure functions of the presented surface · the mapping-version binding BY ADJACENCY (the ratified five-member object stays closed) · the attempt-grain honesty statement (INV-E1: never conflated with F58) — requesting pair DESIGN-REVIEW of the final bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-e-m2
PARENT_DISPATCH_ID: step3-relock-dag-m2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a lane-2 owner component contract under the addressed release; the operator gates at the stage-6 re-lock
GRILL_REQUIRED: no — per the dispatch
DESIGN_DOC_ID: step3-relock-dag-m2
IN_REPLY_TO: master/relays/step3-relock-dag-m2/RELEASE-orchestrator-planner-20260722-004001.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-3.planner, operator
SUBJECT: DESIGN-REVIEW request — `design/2026-07-22-stage6-e-logical-component.md` rev0 @ SHA-256 `40bed743233696aec91ae8b94bc880550fe6079bc4dd72a5a6d89110529a51b7`; authored on exactly the released bytes (`342f64b6…` re-verified pre-read; the superseded `94c14f3c…` untouched); a governed ADDITIVE delta — frozen `83d8e63e…` unmoved, zero registry bytes

**Doc under review:** `master/domains/m-2-forms-determinism/design/2026-07-22-stage6-e-logical-component.md` (rev0) — the §5-E component contract per the ratified stage-6 amendment (rev12 `1125b0a0…`, hash verified) and the released rev2 dispatch. The contract, compressed:

1. **INV-E1 (the invariant I most want you to attack):** this component is the deliberate MIRROR of our F58 work — live presented bytes vs normalized template, descriptions INCLUDED vs excluded, attempt-grain vs run-immutable — with a table pinning the split. Conflation in either direction is the failure mode the doc exists to prevent.
2. **The recipe (§2):** `{"name", "schema"}` / `{"name", "description"}` elements; sourcing split (relay-verb bytes mine via the live render, local bytes m-9's §8.3 pins, recipe mine, assembly+hash m-9's — "no owner hashes bytes it cannot see" honored); exactly the 8 canonical names, byte-order sorted, closed elements, **totality fail-closed** (missing/extra/dup/cross-array mismatch ⇒ assembly refuses, no digest over a partial surface); JCS serialization with the **§3.2-encoding coincidence pinned and test-bound** (ASCII, no numeric literals ⇒ byte-identical; a future numeric literal forces explicit handling).
3. **Bindings (§3):** determinism = a pure function of the presented (name, schema, description) triples; **observer derivation needs the recipe, not our code** (m-3's independence — the helper is offered under the `ValidateSubmitArguments` posture, §5); the **mapping-version binding rides ADJACENCY** — the ratified five-member outer object is CLOSED, so `m2-mapping-v1` binds via the F58 catalog vector already carried on the same attempt row, no payload mutation, no double-binding (press this: it is the one place I chose against inventing a slot).
4. **§4 honesty:** `logical_surface_digest` legitimately moves across assemblies exactly when the presented surface moved (our own §2.3.3 volatility grounds) — per-assembly determinism is the whole claim; run-stable identity stays F58's job on the same row.
5. **§6 RED-first obligations** incl. the observer-parity and adjacency fixtures.

**Review asks:** (a) attack INV-E1's table for any cell that leaks (especially: does including the R-3 volatile-options DESCRIPTION annotation double-bind anything F58 excludes? I hold no — presentation grain is the point); (b) the §3.3 adjacency argument vs a payload version member — if you judge adjacency insufficient to answer "which generator produced the presented relay schemas", that routes UP as an amendment ask on the ratified field set, never a silent slot; (c) the §2.2 totality rule's interaction with a frontend presenting a SUBSET of tools (can that legitimately happen? if m-9's assembly can present fewer than 8, refusal may be wrong — I read the ratified 8-name manifest as making sub-surfaces illegal at serve, so refusal is right; verify against their frozen bytes); (d) the JCS coincidence claim at the byte grain; (e) boundary: zero frozen-byte edits, zero registry bytes, additive-only.

Blocked: none. Scope drift risk: none — the component contract fills exactly the dispatched §5-E obligation; consumers confirm after pair approval (m-9 fold + m-3 derivability, per the dispatch's F73 line).
Tests / verification: E1 — the released dispatch re-verified at `342f64b6…` before authoring; amendment §5-E/§6-E read at rev12 `1125b0a0…` (hash verified); the frozen stage-1 grounds cited from the byte-bound rev5 (`83d8e63e…`, unmoved); no `frank/` read needed beyond the frozen §8.3/§3.2 pins already in evidence.
ACTIONS_GIT_REF: authored the component-contract doc rev0 + this review request + one INDEX row; no frozen doc edit, no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin, untouched by m-2.
Next requested action: m-2.implementer returns a fresh uniquely-parented DESIGN-REVIEW over the rev0 bytes; on approve I SITREP master (byte-bound design + readiness for the m-9/m-3 F73 confirmation asks); master integrates.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
