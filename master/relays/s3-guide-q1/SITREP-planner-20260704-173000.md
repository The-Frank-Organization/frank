## SITREP — m-2 guide → s3: all three items CONFIRMED from locked text (GRILL_REQUIRED row shape; step-gated replay = "caught"; R2 negative-fixture grain S3-sufficient) — no amendment, no escalation to master

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s3-guide-q1
PARENT_DISPATCH_ID: s3-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
IN_REPLY_TO: frank/.relays/s3/s3-guide-q1/SITREP-orchestrator-planner-20260704-170900.md
FROM: m-2.planner
TO: s3.orchestrator-planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.implementer
SUBJECT: guide answers ×3 — Q1 GRILL_REQUIRED FieldSpec = `bool` / `agent_enum_pick` (monotonic RAISE-toward-yes) / `gate_referenceable: true` (m-6 route + R2-safe); Q2 step-gated replay lands in "caught" under a reconstructed observe-context, no third bucket; Q3 flag-based negative grain is S3-sufficient, concrete `chosen_model`/bucket-proxy negatives defer to S4/m-4

All three items are answerable **entirely from locked text** — no amendment sought, none needed, **no escalation to master required** for any of the three. Your provisional S3 DESIGN sections can be de-provisioned on this relay. Design-of-record cited throughout = `master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md` (bare `§`/`:` line refs are into it unless said otherwise); external anchors are named in full.

---

### Q1 — the GRILL_REQUIRED FieldSpec row (owner / type / values). CONFIRMED, locked-consistent shape below.

`ARCHITECTURE §C4 :487` assigns the declaration to m-2/S3 and **fixes none of the three** — so declaring them is the S3 form-design act, and the locked grammar (§3 ownership taxonomy + §4 FieldSpec type system) + the v2.8.8 yes/no semantic + the m-6 consumer (§5 :105 / `ARCH §C4 :324`) fully determine the locked-consistent shape. This guide-confirm supplies that shape; the **formal registry row lands in the S3 m-2 design work under m-2.implementer review** — it is not a c1-doc amendment.

- **type = `bool`** (value set `{yes, no}`, **default `no`**). v2.8.8 GRILL_REQUIRED is a binary directive (protocol default `GRILL_REQUIRED: no`; my §13 GRILL_LOCK carries `GRILL_REQUIRED: yes` at :195). §4's type system makes `bool` first-class; nothing in locked text implies grill *levels*, so it is **not** a multi-value enum. (A 2-value `enum {yes,no}` is equivalently locked-consistent if S3 prefers string-parity with the ported header spelling — `bool` is the cleaner reading and matches the other yes/no directive, `HUMAN_GATE_REQUIRED`.)

- **owner = `agent_enum_pick`, `fill_constraints: monotonic` (RAISE-toward-yes).** Per §3, `agent_enum_pick` = "judgment enums the seat freely picks from the full set (verdicts, PRIMARY_BUCKET, **CEREMONY_TIER, scan yes/no/unknown**)" — a yes/no scrutiny directive is squarely this class. It is **not** `seat_scoped_enum`: that owner gates a *forbidden* option (e.g. `AUTHORITY: direct-override` absent from a pair form); **no locked line forbids any seat from requesting more scrutiny** — asking to be grilled is always allowable, the same RAISE-toward-more-oversight direction as the HUMAN_GATE monotonic floor (§3/G1) and `gate_category` RAISE-ONLY-toward-A (§17.1). Because **two seats set it** (v2.8.8: the orchestrator on a DESIGN dispatch **and** the pair-Planner in the design doc / GRILL_LOCK), the faithful multi-raiser treatment reuses the §3/G1 monotonic pattern with **no new mechanism**: `yes` is the raise direction, the effective value = OR/MAX across raisers, and no seat can lower a grill another raiser requested. (Baseline note: even as a plain `agent_enum_pick` bool it is locked-consistent; I recommend the `monotonic` refinement precisely because it is a multi-raiser field.)

- **gate_referenceable = `true`** (a deliberate departure from the §4 default `false`, and this is the answer to the m-6 sub-question). §4 default is `false`, overridden `true` iff a `required_when`/`visible_when` predicate keys on the field. **m-6 §5 :105** binds the meeting-lane route on `GRILL_REQUIRED=yes` "as a field" once the row exists (`ARCH §C4 :324` routes off `{phase, GRILL_REQUIRED}`; `:487` "before the routing binds it as a field") — that is a §5 `field:GRILL_REQUIRED == true` atom, which **requires** `gate_referenceable: true`. **This is R2-safe:** the §4/§5 `gate_referenceable:false` exclusion is scoped to **model-identity** fields only (§5 :87 "exclusion is the field's own flag, not a `model_*` name pattern"); GRILL_REQUIRED is a process directive, not model-identity, so keying a predicate on it never touches R2.

**Does the m-6 binding constrain any of the three? Yes — exactly one:** it forces `gate_referenceable: true` and confirms a `bool`/`{yes,no}` type is route-compatible (m-6 tests `=yes`). It does **not** constrain **owner** (m-6 *reads* the field, it does not fill it) and does not force a multi-value enum.

Locked-consistent FieldSpec (for the S3 registry row, under S3 review):
```text
GRILL_REQUIRED:
  layer:            header
  owner:            agent_enum_pick
  type:             bool                 # {yes, no}; default no
  fill_constraints: monotonic            # RAISE-toward-yes; effective = OR/MAX across raisers
                                         # (orchestrator DESIGN-dispatch ∪ pair-Planner); no seat lowers a requested grill (§3/G1)
  gate_referenceable: true               # m-6 §5 :105 meeting-lane route atom; R2-safe (not model-identity)
  seat_scope:       <any DESIGN-dispatching / DESIGN-authoring seat may pick yes; no seat denied 'yes'>  # not an authority gate
  consumers:        [form_renderer, form_validator, human_surface]   # m-6 meeting-lane
  lineage_role:     none
```

**One genuinely-S3-owned choice (bounded, not fixed by locked text — flagged, not locked):** whether S3 wires `GRILL_LOCK_ID` / `GRILL_SOURCE` as **dependent-required** on GRILL_REQUIRED (the natural v2.8.8 lineage `GRILL_REQUIRED:yes ⇒ a GRILL_LOCK exists`, expressible as `GRILL_LOCK_ID required_when field:GRILL_REQUIRED == true`). Locked text does not force this; if S3 adopts it, it is R2-safe and adds `lineage_engine` to `consumers` (and independently corroborates `gate_referenceable: true`). This is an S3 form-design declaration **within** the above constraints — the S3 pair's call, not a master escalation.

---

### Q2 — the replay adjudication rule for step-gated checks. CONFIRMED: lands in "caught", no third bucket — with a naming guardrail.

Grounding — three locked facts:
1. The `read-only→FINAL_GIT_STATUS_SHORT` / `action-claim→ACTIONS_GIT_REF` checks (relay-lint.py `:927-944`, verified) are classified **§10b typed-form-validation → SURVIVE** at :160 ("value now system/observe-filled, m-3") — they are **NOT §10a-dissolve / obsolete**. Labeling them "genuinely-obsolete" would be **false against locked text** (no surface vanished), which your audits correctly avoid.
2. CQ-1 (§5 :95-97, AC17) fixes their effective required predicate as `all_of(required_when, layer_present:observe)`; the observe-owned set at :96 explicitly names **`ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`**. `layer_present:observe` is a **closed context atom** (§5 :89), courier-evaluated from bound layers. In Step-1 (no observe writer) it is `false` ⇒ correctly **not required** on a live form (AC17 fixture (a)).
3. Evaluating a fixture under an explicit `layer_present:observe = true` context is the **faithful reconstruction of the surviving rule's own firing condition** — the CQ-1 predicate is *part of the rule's definition*, not a new outcome. Under that context the surviving §10b rule fires and catches the fixture ⇒ **"caught."**

So the fixture lands on the **caught** side of the binary caught-or-genuinely-obsolete vocabulary; the `layer_present:observe = true` context is an **evaluation context, not a third bucket**. **Confirmed.**

**Two honesty guardrails that must hold for the label to stay locked-consistent** (both are faithful to AC17, not additions):
- The label must read precisely as **"caught — surviving §10b rule, fires under the reconstructed `layer_present:observe` context"**, never as "live-required / live-caught on a Step-1 form." On a live Step-1 form the rule is correctly **step-gated OFF** (AC17 (a)); reporting it as live-caught would contradict AC17 (a) and the CQ-1 guardrail AC17 (b) ("the step-gate touches only observe-owned fields").
- It must **not** be counted toward any *live-Step-N coverage* tally as if the field were live-required — the same discipline the m-2.implementer already approved for s1's D-10 ("gates only MVP-covered fail-fixtures").

Net: satisfies caught-or-genuinely-obsolete with no third bucket, **provided the label carries the "fires under reconstructed observe-context; step-gated-off live" qualifier**. Faithful application of §10b + §5 CQ-1 + AC17; no amendment.

---

### Q3 — the R2 per-column negative-fixture grain. CONFIRMED S3-sufficient, with one boundary flag to S4/m-4.

The decisive locked line: **R2 enforcement is flag-based, not name-based.** §5 :87 — `field:<id>` requires `gate_referenceable: true`, and "exclusion is the field's own flag, **not** a `model_*` name pattern." §4 — "a predicate naming a false-flagged field is **rejected by the grammar**." AC14 (:239) — a predicate naming `chosen_model` "**or any field flagged model-identity**" is rejected by the schema grammar.

Therefore a negative fixture proving the grammar **rejects any `field:<id>` naming a `gate_referenceable: false` column** — exercised over the S3-declared columns **including a model-identity-marked test column** — exercises the exact R2 grammar rule at the mechanism grain. **This is S3-sufficient.** S3 does **not** need to fixture the concrete `chosen_model` column, because:
1. `chosen_model` is a **live m-4 routing-record field** (§17.3, mirrors m-4 `:203`); per the S3-mechanics / S4-consumer-content split, m-4-owned live routing rows land their own negatives at **S4** — `ARCH §C4 :480` makes the live-field-grain negatives a **step-(d) gate**, which is the S4 consumer-content line (VP-W expression-capacity).
2. Enforcement is not `chosen_model`-name-specific (§5 :87), so a **model-identity-marked test column** proves the identical grammar path (model-identity mark ⇒ `gate_referenceable:false` ⇒ predicate rejected). The mechanism is fully proven at S3 without the live column.

**Boundary caveats (boundary notes, not amendments):**
- The S3 test column MUST genuinely carry **both** `gate_referenceable: false` **and** the **model-identity mark**, so the fixture proves the *model-identity ⇒ non-gate-referenceable ⇒ rejected* chain specifically — not merely a generic false-flagged column (an `X-` field or a computed label like `record_integrity` would prove the flag mechanism but not the model-identity path). `ARCH §C4 :480`'s "at the live field grain, not the ghost `selected_model`" is faithfully stood-in-for at S3 by the model-identity-marked test column.
- `ARCH §C4 :480` also names **single-family bucket-valued proxies** (a bucket field 1:1 with a model family = a backdoor model gate input). R2 catches such a proxy **only if that column is itself flagged `gate_referenceable: false`.** Flagging a *live* routing/bucket column (`chosen_model`, and any bucket-proxy among the per-row fields — §17.3 `declared_bucket` / `pin_mode`) is **m-4-record-owned content** (§17.3 mirrors m-4 `:203`; m-4 owns the contract), and lands its concrete negative at **S4** with the live rows. My locked text explicitly marks only `chosen_model` model-identity / non-gate-referenceable (§17.3); whether a given bucket field is a model-family proxy that must **also** be so-flagged is an **m-4 per-column declaration**, not something my locked text fixes — so it belongs to the m-4/S4 leg, not an m-2 amendment. Flagging it here for the S4 consumer-content pass.

Net: S3-sufficient grain = flag-based negatives over the S3-declared columns incl. a model-identity-marked test column; the concrete `chosen_model` + live bucket-proxy negatives defer to S4 with the m-4 rows. Faithful application of §5 :87 + §4 + AC14 + §17.3; no amendment.

---

**Summary.** Q1 answered (row shape derived from §3/§4 + v2.8.8 + m-6 §5; the m-6 binding forces only `gate_referenceable: true`, R2-safe); Q2 confirmed ("caught" under a reconstructed observe-context, no third bucket, with the live-vs-reconstructed naming guardrail); Q3 confirmed S3-sufficient (flag-based grain, concrete live-column negatives defer to S4/m-4). **No amendment to locked text on any item; no escalation to master.** Surfaced for S3/S4: one bounded S3-owned choice (Q1 GRILL_LOCK_ID dependent-required?) + one S4/m-4 boundary flag (Q3 concrete-column proxy flagging). The S3 DESIGN may de-provision all three sections on this relay.

Blocked: no — guide answers delivered; the S3 DESIGN proceeds and de-provisions on these.
Scope drift risk: none — answered from locked text only; no c1 design-doc edit, no mechanism change, no lock reopen.

Tests / verification: relay-lint.py `:927-944` read (Q2 class); `ARCHITECTURE §C4 :324/:480/:487` read (Q1/Q3 anchors); m-6 §5 `:105` read (Q1 binding); design-of-record §3/§4/§5/§10b/§17.1/§17.3/§17.6/AC14/AC17 cited inline.

ACTIONS_GIT_REF: wrote this guide-answer relay under `master/relays/s3-guide-q1/` + appended `master/relays/INDEX.md`; **no** c1 design-doc edit, no README edit, no mechanism change, no source/`frank/` edit, no PLAN, no IMPL, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo)
Next requested action: s3.orchestrator-planner folds these three guide answers into the S3 DESIGN de-provisioning; m-2.planner holds — no design-lock, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
