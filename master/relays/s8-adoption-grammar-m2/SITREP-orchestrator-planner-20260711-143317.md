## SITREP — master consumes the r2 RE-CONFIRM (`…-142347` REPLACES `…-135623` as the reconcile-A input) + routes the catalog-mutation-path flag to m-7 WITH the master recommendation: (a) the singular `config_change` gains `member: catalog` — the same MAJOR-but-safe switch-widening class as `adoption`, landing in the SAME s8 registry changeset; the answer rides the completion return as check item SIX

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-adoption-grammar-m2-ruling-r2
PARENT_DISPATCH_ID: s8-adoption-grammar-m2
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — consumption + a routed-question recommendation under the standing design dispatch; the mechanism call stays m-7's; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
IN_REPLY_TO: master/relays/s8-adoption-grammar-m2/SITREP-planner-20260711-142347.md
FROM: master.orchestrator-planner
TO: m-7.planner
CC: operator, master.orchestrator-reviewer, m-7.implementer, m-2.planner
SUBJECT: reconcile-A now consumes m-2's `…-142347` (against r11) in place of `…-135623` — supersession chain noted (ask `…-134654` → r10-F6 caught the digest-in-body error convergently with m-2 → corrected ask `…-141455` → re-confirm); the closed body `{catalog, engine}` = the canonical name-sort, m-2's own list-name correction consumed; the ONE routed flag (post-adoption catalog mutation path) answered with a recommendation, not a ruling — your mechanism call, stated in the doc before the completion return

**Consumed for reconcile-A (in place of `…-135623`):** the header-digest correction is CONVERGENT (your r10-F6 = m-2's independent catch; `ExpectedConfigDigest` stays ONE interpreter, `genesis.go:125-140`) · the closed body — exactly `{catalog, engine}`, that order, cardinality 2 — CONFIRMED, with the order verified as the canonical name-sort (`config.go:75`), so the fixed order is digest-consistent, not arbitrary · m-2's own correction consumed: the valid list-names are `{catalog, engine}` exactly (fieldspec is already pinned and never crosses the set boundary; `fieldspec`, `adoption`, or any other name in the list = typed pre-pivot reject) · the three prior rulings carry unchanged (discriminator overload + reserved token · MAJOR-fail-CLOSED-no-migrator · the two `adoption` registry byte-sites).

**The routed flag + the master recommendation — (a), with the reasoning on the record:** post-adoption the pinned set is `{fieldspec, engine, catalog}` but `submit.go:341` accepts only `fieldspec|engine`, so the catalog would have NO mutation path — yet the catalog's day-2 lifecycle is load-bearing across the standing design: your own §4 drift oracle presumes catalog updates happen; m-2's §2.2 `change_convention` governs law changes; and the operator's fresh F5-(a) ratification pins the exact posture "catalog changes arrive as reviewed changesets + an operator §7 apply." A **separate governance mechanism (b) would be a second change path outside §7** — it must either bypass the composite-digest rotation that IS config integrity or reimplement it: the duplicate-axis smell m-2's GL-4 rejects, twice over. So the master recommendation is **(a): the singular `config_change` gains `member: catalog`** — `catalog` joins the enum_set (`registry.json:85`) + seat_scope.operator (`:136`) + the `submit.go:341` switch arm. Two composition notes ride the recommendation: **(i)** this is the SAME MAJOR-but-safe exhaustive-switch widening class as `adoption` and lands in the SAME s8 registry changeset — one MAJOR moment for the record schema, not two; **(ii)** the singular catalog arm must be STATE-aware, not merely code-aware: valid only where the catalog is already in the pinned set, i.e. post-adoption — a singular `member: catalog` on a pre-adoption store is a typed reject via your §5.1 adoption-only absent→initial rule (state the composition explicitly). If you rule (b) instead, the doc must state how catalog bytes rotate the composite digest without a `config_change` — the bar the alternative has to clear.

**Effect on m-2's amendment sizing (informational, per their `…-142347`):** under (a) the s8 registry changeset carries BOTH tokens (`adoption` + `catalog`, each at `:85` + `:136`) with the bounded §2/§9 amendment — still one changeset, one review moment, per my `…-140516` ruling.

**Check item SIX (added to the five in `…-140516`):** the completion return's doc states the catalog-mutation-path decision + its state-awareness composition. Item ONE (the header correction) is observed already closed by r10-F6/r11 — verified at the byte grain when the return lands. The board is otherwise unchanged: the completion SITREP (A1 version carrier · A2 reader-capability gate · the swept §5.1 adoption arm · this flag's answer, full r6→r11+ lineage) is the SOLE remaining s8-PLAN gate; on it, reconcile-A completes → the reconciled DESIGN locks + PROCEED → m-3.planner emits the s8 PLAN.

ACTIONS_GIT_REF: none — consumption + routed recommendation only (disk refs: this relay + one INDEX.md row timestamped 20260711-143317).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; cwd is not a git repo (docs workspace).
Next requested action: operator carries this to m-7.planner (the flag answer + sweep + completion return) and to m-2.planner (consumption + sizing note; they hold).
