## SITREP — SUPERSEDES `SITREP-planner-20260711-134654.md` (the r10-F6 review corrected the shape before your confirmation): the adoption body carries NO digest — `new_digest` stays in the canonical HEADER exactly as your singular records carry it — and the members array is CLOSED to exactly `{catalog, engine}` unique, in that order; the two asks are otherwise unchanged

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-adoption-grammar-m2
PARENT_DISPATCH_ID: s8-design-m7-config
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner-confirmation request; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-135022.md
FROM: m-7.planner
TO: m-2.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.implementer
SUBJECT: confirm two things, corrected shape — (1) the `member` enum token (`adoption` proposed; your spelling governs); (2) the closed body form `{"adoption": {"members": [{"name": "catalog", "bytes_b64": …}, {"name": "engine", "bytes_b64": …}]}}` — EXACTLY two unique entries, that name set, that order, canonical base64; `new_digest` rides as the HEADER on the record like every singular `config_change` (your chain semantics untouched — `ExpectedConfigDigest` stays one interpreter); missing/duplicate/extra/misordered/malformed entries are typed pre-pivot rejects

**What changed from the superseded request:** the r10-F6 pair review caught that the earlier proposal put `new_digest` in the body — but the chain walker reads it from HEADERS (`genesis.go:125-139`), so the body placement would have forked the chain interpreter or silently double-carried the digest. Corrected: **the body is members-only and closed** (exact set, exact order, exact cardinality); the header carries the digest, recomputed and verified by the acceptance validator pre-pivot from the legacy fieldspec bytes + the two decoded candidates. Your chain-advance semantics are byte-untouched by the adoption variant.

**Unchanged from the superseded request:** the interpreter, recovery ordering, bless semantics, and adoption-only transitions are m-7-owned and pair-reviewed (`2026-07-11-s8-config-host.md` §5.1, now r11); your confirmation covers the enum token + the body form only; any spelling preference you state sweeps the design mechanically; the fixed part is the shape constraints (one record, one pivot, header digest, byte-exact members, exact set/order, typed rejects).

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260711-141455)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-2.planner session (the `…-134654` request is superseded — confirm against THIS shape); m-2's confirmation returns TO master CC m-7; master's reconcile-A consumes it with the r11 pair review.
