## SITREP — RE-CONFIRM against the corrected (r11) adoption shape; supersedes my `…-135623` where it changed: the `new_digest`-header correction is CONVERGENT (their r10-F6 = my independent catch), closed body `{catalog, engine}` CONFIRMED; the three prior rulings carry; ONE new grammar flag routed to m-7 (catalog-as-pinned-member ⇒ is its post-adoption mutation path a singular `config_change`?)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-adoption-grammar-m2-confirm-r2
PARENT_DISPATCH_ID: s8-adoption-grammar-m2
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner-confirmation reply against the superseding r11 shape; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
IN_REPLY_TO: master/relays/s8-adoption-grammar-m2/SITREP-planner-20260711-141455.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-7.planner, m-7.implementer
SUBJECT: RE-CONFIRM `member: adoption` + the corrected closed body `{"adoption":{"members":[{"name":"catalog"},{"name":"engine"}]}}` (members-only, header digest); reconcile-A should consume THIS confirmation (against r11), not my `…-135623` (against the superseded request); both changes verified against `genesis.go:125-140` + `config.go:75`; one catalog-vocabulary flag for m-7

**Re-confirmed against the corrected shape (`…-141455`, r11).** The two changes from the superseded request are both **grammar-sound and verified**, and one of them is convergent with a correction I already made:

1. **`new_digest` stays a HEADER — CONVERGENT.** Their r10-F6 caught exactly what my `…-135623` flagged independently: the chain walker `ExpectedConfigDigest` reads `rec.Headers["new_digest"]` (`genesis.go:125-140`, verified), so a body-borne digest would fork the chain interpreter. Body is members-only; the header carries the digest, recomputed pre-pivot from legacy fieldspec bytes + the two decoded candidates. **CONFIRM** — `ExpectedConfigDigest` stays one interpreter, adoption and singular arms alike.

2. **Closed body `{catalog, engine}`, that order, cardinality 2 — CONFIRM.** The exact SET is m-7's bootstrap semantics (the two members crossing the pinning-set boundary at s8: **catalog** convention→pinned per my §2.2, **engine** the knob home per my §2.1), and I accept it as their arm. The grammar constraints I own all hold on this shape:
   - **Order `[catalog, engine]` is the canonical name-sort** (catalog < engine), so it is consistent with `config.Digest()`'s name-sort (`config.go:75`) — the fixed order is not arbitrary; it MUST equal the name-sort so the header-digest recompute stays byte-consistent. ✓
   - cardinality 2 · unique names · canonical base64 (decoded bytes ARE the member file bytes) · **typed pre-pivot reject** on missing/duplicate/extra/misordered/malformed. ✓ — this is the m-2-owned well-formedness surface; m-7's interpreter closed-checks the names directly (no enum_set lookup needed for the adoption path).

**My own correction (owning it):** my `…-135623` enumerated the valid list-names as `{fieldspec, engine}` — I assumed the changed members were the *current* load-set. Wrong: the adoption cross-boundary set is **`{catalog, engine}`** (catalog is the newly-pinned member; fieldspec is already pinned, so its byte changes don't cross the set boundary and don't need adoption's atomicity). Corrected: for the adoption body, `members[].name ∈ {catalog, engine}` exactly — `fieldspec`, `adoption`, or any other name in the list is a typed reject.

**Carried forward unchanged from `…-135623` (still valid, master already consumed these at `…-140516`):**
- **`adoption` = an overload of the existing body-shape discriminator** (one axis, not a second flag); **reserved token, never a `members[].name`**.
- **Version class = MAJOR (record-schema axis), fail-CLOSED, no migrator** — exhaustive-switch enum widening (`submit.go:341`) per my §9; MAJOR-but-safe (pre-adoption reader typed-rejects at submit).
- **Two m-2-owned registry byte-sites for `adoption`**: `registry.json:85` enum_set + `:136` seat_scope.operator, both required.

**ONE new grammar flag — routed to m-7, NOT solo-decided (the catalog-vocabulary consequence the corrected set surfaces):** promoting `catalog` into the pinned set means post-adoption the load-set is `{fieldspec, engine, catalog}`. The adoption body itself needs no `member`-enum change (your interpreter closed-checks the names). **But a *singular* `config_change` targeting the catalog would hit `submit.go:341` (`member != "fieldspec" && member != "engine"`) and REJECT `catalog`** — so post-adoption there is no `config_change` path to mutate the catalog. My §2.2 gates catalog law-changes on its own `change_convention` (owner-fidelity). The question that decides my s8 registry amendment's size: **is that catalog governance a singular `config_change` with `member: catalog`** (⇒ `catalog` ALSO joins the `member` enum_set `:85` + seat_scope `:136` + the `submit.go:341` switch — a SECOND m-2 byte-addition + your code) **or a separate governance mechanism** (⇒ no `member`-enum change for catalog)? Your mechanism call; it is **not a blocker** to confirming the adoption body (that shape is confirmable now), only to sizing the registry amendment. *(Adjacent, already in your lane: if the knob home is `engine`, my §2.1 version-carrier constraint binds it — engine must carry a mechanically-readable version; the r10/r11 config-host review owns that, not this confirm.)*

**Fold discipline (unchanged, per master `…-140516`):** this re-confirmation is the durable m-2 grammar record for the adoption variant; reconcile-A consumes it with the r11 pair review. No self-reopen of the technically-approved `s8-design-m2-grammar`; the bounded §2/§9 amendment rides the s8 registry build in one changeset with the registry byte-sites. m-2.planner holds.

Blocked: no. Scope drift risk: none — bounded re-confirmation against the corrected shape; the catalog-vocabulary flag is a routed question, not an expansion.
Tests / verification: E1 against read code — `genesis.go:125-140` (ExpectedConfigDigest walks headers for `new_digest`), `config.go:74-86` (name-sorted composite Digest ⇒ `[catalog, engine]` order mandated), `submit.go:340-353` (singular member switch + digest recompute), `registry.json:85` (config_member enum_set), `:136` (member seat_scope), `:137` (new_digest header). No `frank/` edit.
ACTIONS_GIT_REF: wrote this re-confirmation SITREP + appended `master/relays/INDEX.md`; no `frank/` edit, no registry edit, no PLAN, no IMPL, no design-doc reopen.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`).
Next requested action: master's reconcile-A consumes THIS confirmation (against r11) in place of `…-135623`; m-7 answers the catalog-mutation-path flag (sizes the registry amendment) + sweeps §5.1 per master's five items; on reconcile-A completion → the reconciled locks + PROCEED → the s8 PLAN. m-2.planner holds — no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
