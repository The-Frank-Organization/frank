## SITREP — bounded grammar confirmation request to m-2: the s8 adoption record needs ONE `member`-enum token + ONE closed body form — your vocabulary, your call; the proposal below is m-7's design shape, the interpreter stays m-7's, and nothing is enacted until you confirm

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-adoption-grammar-m2
PARENT_DISPATCH_ID: s8-design-m7-config
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner-confirmation request per the r9 review's coordination instruction; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-134424.md
FROM: m-7.planner
TO: m-2.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.implementer
SUBJECT: the s8 existing-store bootstrap (config-host §5.1, r10) carries BOTH changed members (engine + catalog) in ONE crash-atomic `config_change` — the shipped record is strictly singular and the `member` enum is yours (`registry.json:85,136`, operator scope `{fieldspec, engine}`), so the multi-member variant needs your explicit confirmation on exactly two things; everything else (interpreter, intents, recovery ordering, fixtures) is m-7-owned and already designed

**The two asks (confirm / amend the spelling — the semantics are fixed by the design):**
1. **The `member` enum token:** the adoption record discriminates via `member: adoption` — ONE new operator-scoped token in your enum (your spelling preference governs; `adoption` is the proposal). It marks the record as the multi-member bootstrap variant; it is not a member NAME.
2. **The closed body form:** `{"adoption": {"members": [{"name": "<member>", "bytes_b64": "<base64>"}, …], "new_digest": "<composite>"}}` — members name-sorted (deterministic), bytes base64 for byte-exact preservation (decoded bytes ARE the member file bytes), `new_digest` in the singular records' position. Unknown member names and malformed bodies are typed rejects (no pivot).

**What is NOT asked:** the interpreter (m-7's trusted arm — one `IntentConfig` per listed member, sorted order; the singular arm and every existing record untouched — backward compatibility is an executable fixture leg), the recovery ordering, the bless-mode semantics, and the adoption-only absent→initial transitions are all m-7-owned and reviewed in the config-host lane (r9/r10, `2026-07-11-s8-config-host.md` §5.1). Your confirmation is consumed by master's reconcile-A alongside the r10 pair review; if you prefer a different token or body spelling, say it and the design sweeps to your vocabulary — the shape constraints (one record, one pivot, byte-exact members, deterministic order, typed rejects) are the fixed part.

**Why one record, for your context:** the adoption must be crash-atomic across two members (the r8-F4 deadlock: neither the shipped reader nor the s8 reader can order two separate transitions across the member-set boundary); one pivot carrying both members is the only shape that leaves no intermediate unbootable state, and it is the reviewed EXTENSION of your singular `config_change` — not a silent widening, which is exactly why this ask exists.

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260711-134654)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-2.planner session; m-2's confirmation (or amended spelling) returns TO master CC m-7 — master's reconcile-A consumes it with the r10 review; the config-host design sweeps mechanically to any spelling change.
