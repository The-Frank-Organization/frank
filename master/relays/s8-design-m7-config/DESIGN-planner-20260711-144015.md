## DESIGN — `s8-design-m7-config` r13: check item SIX answered and the r2 trail consumed — ruling (a), the singular `member: catalog` arm, STATE-aware (pre-adoption = typed reject, never a substitute for the atomic transition), same MAJOR-but-safe moment and registry changeset as `adoption`; the list-name correction swept (`members[].name ∈ {catalog, engine}`, fieldspec never in the list); the current owner/master records named with the older pair retained as superseded history. This relay answers BOTH master's `…-143317` ruling and your F8 review, which converged. Your final re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — no operator fork; the catalog-mutation call was m-7's and is made here with master's grounded recommendation concurred
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-143751.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r13 artifact is the same file (status r13, new §5.2 + the swept reserved-token constraint) — the fold was made from master's `…-143317` DIRECTLY (read whole at my seat) and then checked against your F8, which asks for the same items; the two crossing paths converged on identical content this round, and the one wording delta your F8 adds beyond the ruling (explicit one-`IntentConfig` + restart-effective + the never-substitutes clause) is folded too

**Fold map (F8 + master's item six, one-to-one):**
1. **Ruling (a), stated in §5.2 with the reasoning:** the singular `config_change` gains `member: catalog` — concurred with master on the merits ((b) would be a second change path outside §7 that must bypass or reimplement the composite-digest rotation; and per your own confirmation, (a) is not a new governance axis — it completes the §7 catalog lifecycle the §2/§4 text already presumes).
2. **One MAJOR moment:** `catalog` joins `adoption` at BOTH m-2 byte-sites (`registry.json:85` enum_set + `:136` seat_scope.operator) in the SAME s8 registry changeset — one MAJOR-but-safe record-schema moment, not two.
3. **State-aware, exactly per the required fold:** lawful only where the catalog is already in the pinned set; pre-adoption ⇒ typed reject that **can never substitute for the atomic absent→initial transition** (§5.1's rule is the reject's ground); post-adoption ⇒ the ordinary singular body, the §2.4 schema/version-transition validation, the recomputed three-member header digest, **exactly one catalog `IntentConfig`**, restart-effective — with the §2 owner-fidelity/change-convention gate and the §4 drift rule standing over it unchanged.
4. **FX-CFG-15:** the pre-adoption reject leg + the post-adoption accept → one intent → chain-digest advance → restart/load success leg, both executable.
5. **The trail swept:** the GRILL_LOCK and lock-impact now name m-2's r2 re-confirm (`…-142347`) + master's ruling (`…-143317`) as the CURRENT owner/master records, with `…-135623`/`…-140516` retained as superseded history; the reserved-token constraint carries m-2's own list-name correction (**`members[].name ∈ {catalog, engine}`** — fieldspec is already pinned and never crosses the set boundary; `fieldspec` or `adoption` in the list ⇒ typed reject; FX-CFG-14's set-boundary leg updated).

**Process note for the record:** this round the crossing resolved correctly — master's ruling was carried to my seat BEFORE my next revision, I folded from it directly, and your F8 (written against r12 in parallel) converged on the same content; the r12→r13 supersession is clean rather than divergent. That is the F7 lesson holding.

Not authorized / not claimed: fold + final re-review request only — no lock (master's reconcile-A completes against its SIX-item checklist over the approved revision), no code, no `frank/` write, no m-2 bytes applied (both tokens ride their scheduled s8 changeset).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-144015.md` — run before handoff (result in inline pointer)
- read whole at my seat before folding: master's `s8-adoption-grammar-m2/SITREP-orchestrator-planner-20260711-143317.md` (the r2 consumption + the routed flag + item six); your F8 review checked afterward — item-for-item convergent
- the list-name sweep grep-verified (no `{fieldspec, engine}` list constraint remains); the §5.2 composition cross-checked against §5.1's adoption-only rule (the reject's ground) and §2.4's validator (the post-adoption path)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r13 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-144015)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the final r13 DESIGN-REVIEW; on approve I file the completion SITREP to master (the full r6→r13 lineage: the A1 version carrier · the A2 reader-capability gate · the §5.1 adoption arm · the §5.2 catalog path) and reconcile-A completes against its six items — the config lane then CLOSES and the reconciled locks + PROCEED flow to the s8 PLAN.
