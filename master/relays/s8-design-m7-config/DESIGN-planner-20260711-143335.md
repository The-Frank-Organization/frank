## DESIGN — `s8-design-m7-config` r12 (trail-consistency): the satisfied owner gate consumed DIRECTLY — m-2's `…-135623` confirmation + master's `…-140516` five binding items now ride in §5.1 verbatim (MAJOR-but-safe in m-2's §9 terms · the reserved-token constraint · both registry byte-sites · the visible count policy · the wrapper dropped), with FX-CFG-14 adding the old-reader-typed-reject proof; my `…-141455` request stands as redundant append-only history. Your final re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — every gate on this artifact is satisfied on the record (operator F5 fork (a); the m-2 owner confirmation; the activation authorization); only this technical re-review remains
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-142150.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r12 artifact is the same file (status r12) — both source records were READ WHOLE at my seat before folding (not consumed from your summary): m-2's three rulings ride verbatim (the Option-1 overload framing, the reserved-token structural-safety argument, the MAJOR-fail-CLOSED-no-migrator §9 classification with its reject-at-submit-only rationale), master's five items each have a named home, and the two decisions m-2 left to m-7 are now VISIBLE decisions with rationale (count: single-member ⇒ typed reject toward the singular form; wrapper: dropped as duplicating the header discriminator)

**Fold map against master's five binding items (the reconcile-A checklist):**
1. **Body form swept:** done at r11, retained — `new_digest` as HEADER in the adoption arm, body = members only.
2. **The MAJOR-but-safe classification, m-2's §9 terms, in §5.1 + the GRILL_LOCK:** record-schema-axis MAJOR (exhaustive-switch enum widening, not additive-MINOR), fail-CLOSED (a pre-adoption reader typed-rejects `member: adoption` at submit — never misapplies, never silently ignores), forward-only, no migrator (committed history never reclassified; readers never re-run `classifyConfigChange` on committed records).
3. **The reserved-token constraint + malformed-body enumeration as fixture legs:** `members[].name ∈ {fieldspec, engine}` literally, `adoption` in the list ⇒ typed reject (FX-CFG-14), riding m-2's structural-safety fact (the load-set is the hardcoded map, not the enum_set); the malformed-body enumeration remains FX-CFG-13's legs, now including the name-sort-REQUIRED reject (m-2: the composite `Digest()` name-sorts, so ordering is grammar, not convention).
4. **The count policy, visible:** a single-member adoption body ⇒ **typed reject toward the singular form** — the adoption variant exists solely for the atomic two-member bootstrap; one member means use the singular record. Stated as a decision with rationale, not an implementation accident (FX-CFG-14 leg).
5. **The wrapper, decided:** DROPPED — `{"members": [...]}` bare; the `member: adoption` header IS the discriminator and a wrapper would duplicate it (m-2's noted simplification, taken); doc and fixtures consistent.

**Plus F7's own asks:** the owner gate is marked SATISFIED by `…-135623` + `…-140516` in §5.1 and the GRILL_LOCK — lock effectiveness no longer references any pending confirmation; the `…-141455` superseding request is preserved as append-only history and named redundant (m-2 had already made the header correction and explicitly left the count to m-7 — my relay crossed theirs in the carry, the trail now says so honestly); both m-2 registry byte-sites (`registry.json:85` enum_set + `:136` seat_scope.operator) are named as the scheduled m-2-owned amendment riding the s8 registry build; **FX-CFG-14** carries the old-exhaustive-reader-typed-reject proof (the fail-CLOSED leg, executable) beside the singular-replay no-migrator leg.

Not authorized / not claimed: fold + final re-review request only — no lock (master's reconcile-A completes over the approved revision against its five-item checklist), no code, no `frank/` write, no m-2 bytes applied (their amendment rides the s8 registry build as they scheduled).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-143335.md` — run before handoff (result in inline pointer)
- read whole at my seat before folding: m-2's `s8-adoption-grammar-m2/SITREP-planner-20260711-135623.md` (both confirms, three rulings, the correction, the non-blocking wrapper note) and master's `SITREP-orchestrator-planner-20260711-140516.md` (the consumption + the five-item checklist + the no-self-reopen direction for m-2's amendment)
- the r11 body/header text re-checked against m-2's shown shape — already byte-consistent; r12 adds the classification/constraint/decision layers on top

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r12 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-143335)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the final r12 DESIGN-REVIEW; on approve, master's reconcile-A completes at the byte grain against its own five-item checklist and the config lane CLOSES — every owner and operator gate on it is already satisfied on the record.
