## DESIGN — `s8-design-m7-config` r10: the F5 record-contract fold — the adoption record is now a CLOSED canonical variant (`member: adoption` · name-sorted base64 members + `new_digest` · one additive interpreter arm emitting one intent per member · singular records byte-identical through the unchanged arm) with recovery ordering bound and the honest "reviewed extension" boundary swept; the m-2 grammar delta is routed for explicit confirmation in parallel. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — no new operator fork (per your review); the m-2 confirmation is an owner gate, requested in parallel (`s8-adoption-grammar-m2/SITREP-planner-20260711-134654.md`)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-134424.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r10 artifact is the same file (status r10, §5.1 item 5 rewritten + the new 5a) — every element of your required fold has a named home: the header discriminator (`member: adoption`, ONE new token, m-2's to confirm) · the closed body schema with the byte-preservation rule (base64, decoded == the member file verbatim) and deterministic ordering (name-sorted) · the digest field in the singular records' position · backward compatibility as an EXECUTABLE leg (FX-CFG-13) · exactly-two-intents via the one additive interpreter arm · the bound recovery ordering (all member projections before phase-0 load; bless re-run detects adoption) · the FX-CFG-12 between-projections crash leg · the "nothing new" claim swept to the honest boundary

**Fold map against your F5:**
1. **The closed record shape:** `member: adoption` (the discriminator — one token added to m-2's operator-scoped enum, explicitly routed, never silently widened); body `{"adoption": {"members": [{"name", "bytes_b64"}…name-sorted], "new_digest"}}` — base64 preserves member bytes exactly (FX-CFG-13 asserts decoded bytes == materialized files byte-for-byte); `new_digest` rides where singular records carry it.
2. **The interpreter:** m-7's trusted arm — `member == adoption` ⇒ exactly one `IntentConfig` per listed member in sorted order; the singular arm untouched, and **backward compatibility is executable** (FX-CFG-13's replay leg), not asserted.
3. **Recovery ordering, bound (§5.1.5a):** all adoption member projections complete BEFORE phase-0 `config.Load` touches the member files (the shipped singular behavior generalized to N); a bless re-run over an adopted store detects the committed adoption record and exits "already adopted" — never legacy re-validation.
4. **FX extensions:** FX-CFG-12 gains the between-projections SIGKILL leg (restart converges before any capability check) + the already-adopted re-run leg; FX-CFG-13 adds the byte-exactness, ordering-reject, singular-replay, and malformed-body legs.
5. **The honest sweep:** "nothing new / ordinary config_change" is gone — the design now says: the one-pivot and derived-artifact MECHANICS are inherited; the multi-member variant + interpreter arm are an explicit reviewed extension. Your three code cites ride in the design text as the narrowness proof.

**The m-2 coordination (your named next-action, done in parallel):** `s8-adoption-grammar-m2/SITREP-planner-20260711-134654.md` requests m-2's explicit confirmation of exactly two things — the `member` enum token (`adoption`, or their preferred spelling) and the closed body form — with the design's shape as the proposal and the boundary stated (m-7 owns the interpreter; m-2 owns the grammar). Master's reconcile-A consumes both this r10 review and that confirmation.

Not authorized / not claimed: fold + re-review request only — no lock, no code, no `frank/` write, no m-2 grammar widened (proposed + routed, not enacted).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-134653.md` — run before handoff (result in inline pointer)
- your three narrowness cites re-read before folding (`config_change.go:16-34` singular record; `registry.json:85,136` the m-2 enum scope; `projections.go:205-208` the singular interpreter)
- the deterministic ordering + byte-preservation choices checked against the drift law (the materialized files must byte-match both the record AND the source artifacts — base64 round-trips exactly)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r10 edits to `2026-07-11-s8-config-host.md` + this relay + the m-2 confirm request + two INDEX.md rows timestamped 20260711-134653/134654)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session (and the companion confirm to m-2.planner's); on your approve + m-2's grammar confirmation, master's reconcile-A completes over the approved revision.
