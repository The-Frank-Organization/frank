## DESIGN — `s8-design-m7-config` r1 is authored (grill run, one durable operator fork carried with its default): your DESIGN-REVIEW requested on the config-host artifact — the knob's engine-member home, the catalog member with F5-scoped load claims, the FULL discovery descriptor, the runtime home + drift law, the genesis composition, and the §3 step-4.5 amendment riding this doc

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the one operator-owned fork travels as a durable question inside the GRILL_LOCK with a designed default; it ratifies at review/lock, blocking nothing now
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-orchestrator-planner-20260710-214559.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the artifact is `master/domains/m-7-conductor-core/design/2026-07-11-s8-config-host.md` (r1, GRILL_LOCK §9 folded) — review whole against the dispatch's five items + the VP's F5; the sharpest edges to grill me on: the engine-member home for the knob (vs registry-member), the load-claim boundary wording (shape-not-review, everywhere), the descriptor's one-source-of-truth flip (s7 in-test literals become genesis bytes), genesis `observe:false`, and the §3 step-4.5 amendment entering the lock through THIS doc

**The artifact:** `master/domains/m-7-conductor-core/design/2026-07-11-s8-config-host.md` — sole-authored per the dispatch, my FROM. The grill ran per the skill: codebase-answerable questions answered from the locked record (sources + decisions + rejected alternatives all in the §9 GRILL_LOCK); the one genuinely operator-owned fork (the F5 (a)/(b) owner-attestation question) was posed to the operator this session, got no in-session answer, and travels as a **durable operator question with fork (a) — the lean shape-not-review load claim — as the designed default**, exactly the dispatch's sanctioned path.

**What the design binds (the one-line map):** §1 the knob = `present_layers` in the ENGINE member, activation = §7 `config_change` restart-effective, layer presence digest-visible by design with the A-1 negative preserved; §2 the catalog = its own composite member, load enforces digest + member/provenance SHAPE and explicitly never review-happened; §3 the discovery descriptor = the FULL shipped s7 mechanism as governed catalog data (roots · boundary files · recognizers + portable idioms · all three censuses); §4 runtime home = `<root>/config/catalog/catalog.json`, source artifact blessed by byte-copy, drift = a red law; §5 genesis = {`s7a-fieldspec-v5` (SHA recorded) · engine with `observe:false` · the catalog member}, composite digest recorded, post-genesis adoption only via `config_change`; §6 the §3 step-4.5 amendment with the four confirmed constraints (the lock-discipline obligation from `s8-grammar-confirm-m7/SITREP-…-234643`, discharged here); §7 claim ceilings restated; §8 seven named fixture obligations FX-CFG-1..7.

**Boundary contract, named not authored:** `s8-design-m2-grammar` owns the member grammar/atom semantics, the profile partition, the completeness reason vocabulary, and what predicates may say about layer presence; this design guarantees only that `PresentLayers` reaches evaluation as an engine-computed environment fact (the `RenderEnv`/`PreActive` idiom), identical at render and validate within a config generation.

**Your review asks (the dispatch's process, your call on verdict):** faithfulness to the locked §7/§3 + the s6 amendment set; the F5 conditions carried whole (the full-descriptor requirement especially — item 3 of the dispatch was "not a summary"); the honesty of every claim boundary; the genesis-default reasoning (`observe:false` so the flip EXISTS as a record); whether any m-2 content got authored here by accident. Verdict per the standard enum; must-revise folds run the pair loop as always.

Not authorized / not claimed: design authoring only — no lock yet (that follows your approve + master's reconcile with m-2's artifact), no code, no `frank/` write, no m-2 half authored, no operator fork pre-decided (the default is reversible at ratification).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-015554.md` — run before handoff (result in inline pointer)
- grill grounding re-verified this session: locked §7 `:109-111` + §3 `:69-74`; `internal/config/config.go` (the engine member shape); `internal/store/genesis.go:52-90` (`StoreRootConfigPaths`, the digest check); the shipped s7 discovery mechanism at `s7-close`; the s4 `config_change` build record
- the operator fork posed in-session (no response — recorded durable per the dispatch's sanctioned path)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the design doc `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-015554; `frank/` read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session; the implementer returns `PHASE: DESIGN-REVIEW` on the artifact (same DESIGN_DOC_ID, parenting here); on approve the reviewed doc relays to master for reconciliation with `s8-design-m2-grammar` — the s8 PLAN consumes only the reconciled locks.
