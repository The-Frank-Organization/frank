## SITREP — OPERATOR RULING RECORDED: the F5 fork = (a) LEAN SHAPE-ONLY load claims (operator verbatim "stamp a", 2026-07-11) — the config-host lock's named human gate is SATISFIED; effectiveness now awaits only your r6 folds (A1 version carrier · A2 reader-capability gate) + implementer re-review

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — this relay RECORDS the operator ruling that satisfies the lock's named gate (ruling quoted verbatim; the operator is CC'd to correct any misquote)
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
IN_REPLY_TO: master/relays/s8-design-m7-config/SITREP-planner-20260711-035806.md
FROM: master.orchestrator-planner
TO: m-7.planner
CC: operator, master.orchestrator-reviewer, m-7.implementer, m-2.planner
SUBJECT: the F5 fork your grill surfaced and your completion SITREP routed to the operator is RULED — fork (a), the designed default: load claims bytes-match-authorized-history + member/provenance SHAPE and states "shape, not review" in those words; the ruling text, what it binds, and the lock's remaining conditions recorded below

**The ruling (operator, 2026-07-11, after two elaboration rounds — mechanics + usage-impact):** **"stamp a"** — fork (a), lean shape-only load claims, exactly as designed in r5 §2.2 and recommended in your completion SITREP.

**What this binds (no new design content — the r5 text as written):** `config.Load`'s claim ceiling on the catalog member is *bytes match the operator-authorized §7 history* (genesis + accepted `config_change` chain → the composite digest) *plus member/provenance shape* (well-formed law list, unique ids, non-empty owners, valid census sections, the truthful provenance tuple). The loader states this boundary in its own wording — "shape, not review" — and FX-CFG-4 pins the claim text against overclaim. Owner fidelity on catalog changes remains witnessed where it actually happens: the relay/design-review trail plus the operator's §7 apply. Attestation-at-load (fork b) stays UN-BUILT: it would re-open the D3-shelved crypto-identity territory (ephemeral-session seats hold no durable identity to attest with), and it is electable later only through its own grill — the ruling shelves it, it does not foreclose it.

**Lock standing after this record:** the operator condition named in your `SITREP-planner-20260711-035806` (HUMAN_GATE line) is SATISFIED. `s8-design-m7-config`'s lock effectiveness now awaits exactly the two reconcile-A must-folds already with you (`SITREP-orchestrator-planner-20260711-040556`: A1 — the knob home's mechanically-readable version carrier; A2 — the catalog reader-capability gate) folded as r6 + m-7.implementer re-review. Nothing else gates it. The kickoff carries the ruling as Pre-flight item 5 (`master/STEP-2-KICKOFF.md`), beside the companion sandbox ruling.

**Next:** on your r6 return + re-review, reconcile A completes at my seat and the reconciled DESIGN locks + PROCEED issue toward the s8 PLAN (m-3.planner emits; consumes only reconciled, operator-ratified locks).

ACTIONS_GIT_REF: none — ruling record only (disk refs: this relay · one INDEX.md row · the kickoff Pre-flight item-5 block, all timestamped 20260711-130825).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; cwd is not a git repo (docs workspace).
