## DESIGN — `s8-design-m7-config` r5: the r4 finding folded — every normative reference now points at `canonical_path_families.rows` (the removed `root_homes`/`config_paths` names survive only in the status line naming their removal), and row ordering is DEFINED: set-like, sorted lexicographically by `id`, with the pinned twelve re-sorted to obey their own rule. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the lock stays conditional on the operator F5 record (unchanged)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-035012.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r5 artifact is the same file (status r5) — one fold in two parts, nothing else moved: (i) all four stale field references repaired (the provenance rule → "the `catalog-config` row of `canonical_path_families.rows`"; the ordering rules → "`canonical_path_families.rows` (by `id`)"; the §4.1 runtime-home text → "the `catalog-config` row"; FX-CFG-5's canonical leg → "the three `directory: false` config-file rows"); (ii) row ordering defined as SET-LIKE sorted by `id` — honestly, per the r3-F2 principle: the law's oracle comparisons sort, so row order is NOT semantic and is not marked as such — and the pinned twelve are re-sorted (binding · catalog-config · conductor-lock · engine-config · fieldspec-registry · journal · mailboxes · outbox · projections · quarantine · records · staging) so the pinned values obey the rule they sit under

**The ordering decision, reasoned on the record:** marking `rows` an ordered normative sequence (like `output_families`/`sink_patterns`) was considered and rejected — those two are order-semantic because the live law consumes census order; the canonical-family oracle comparisons sort both sides, so pretending row order carries meaning would recreate the r3-F2 defect in mirror image. Set-like sorted-by-`id` is the truthful contract, and the schema comment states it inline.

**Sweep evidence:** a post-fold grep for `root_homes`/`config_paths` returns only the status line that names their removal; `canonical_path_families.rows` appears at every consuming site (the schema, the pinned-values bullet, the ordering rules, §4.1, FX-CFG-5). The lock ID advanced to `s8-design-m7-config-r5` under the names-the-approved-revision rule.

Not authorized / not claimed: fold + re-review request only — no lock (conditional on the operator F5 record), no code, no `frank/` write.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-035207.md` — run before handoff (result in inline pointer)
- the post-fold greps above, run before this relay; the re-sorted twelve rows checked as a correct lexicographic sort by `id`

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r5 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-035207)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r5 DESIGN-REVIEW; on approve, BOTH reviewed m-7 artifacts (this + the r4-approved executor host) go to master for reconciliation with `s8-design-m2-grammar`/`s8-design-m3-registry` — with the two operator records (the F5 fork; the sandbox election) as the named ratification items before either lock is effective.
