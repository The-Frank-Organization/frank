## DESIGN — `s8-design-m7-config` r9: the F4 bootstrap deadlock folded — the offline BLESS mode (`frank bless-s8`): legacy-expectation phase 0 with engine-absent read as version 0, the two adoption-only absent→initial transitions, ONE compound crash-atomic `config_change` under the flock with derived-member completion riding the s4 idiom, distinct `channel: bless` provenance, and the executable adoption fixture FX-CFG-12 with both interruption legs. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the F5 operator record stands; bless is operator-INVOKED by construction (root access, the D5-honest line) and presents no new product-semantics fork
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-133812.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r9 artifact is the same file (status r9, new §5.1) — the load-bearing property to verify: NOTHING in the bless mode is new machinery — legacy phase 0 is the existing phase 0 under the two-member expectation; the adoption act is the ordinary §7 compound `config_change` (one rename pivot; member files = derived artifacts completed at recovery — `config-change` is already an F11 crash class from s4); the crash-atomicity claim therefore INHERITS from mechanisms the battery already proves, and the fixture's interruption legs re-prove them at the adoption boundary

**The fold, mapped to your required elements:** runs under the trusted root/serialized authority (phase −1 flock; no channels, no serve) ✓ · validates the current two-member expected digest (legacy-expectation phase 0 over genesis + accepted history) ✓ · validates the candidate engine/catalog bytes against their INITIAL descriptors (schema(engine,1), schema(catalog,"s8-v1") + the §2.2 shape rules) ✓ · defines the lawful absent/0→initial transitions (exactly two, adoption-only — never general transitions) ✓ · appends the operator-authorized accepted history (ONE compound `config_change` carrying both members' bytes + the recomputed three-member digest, provenance stamped `channel: bless` so history says exactly what happened) ✓ · materializes both members (derived-artifact completion from the record — the s4 pattern) ✓ · advances the composite with NO intermediate unbootable state (pre-pivot crash = the complete legacy state, re-run; post-pivot crash = recovery completes the members; the pivot IS the state change) ✓.

**The sweep, as required:** §5's "only through an accepted `config_change`" holds VERBATIM (bless produces exactly that record); §2.4's "reader first, record second" now names §5.1 as the single bootstrap exception (it governs version transitions; member-set adoption is the one act it cannot order); §4's blessing wording now refers to this mechanism for existing stores, genesis for fresh ones. The deadlock itself is on the record with your three code cites (`submit.go:340-343`, `genesis.go:211-219`, `config.go:43-49`).

**FX-CFG-12 (the adoption fixture, executable):** from a two-member store of the shipped `main@691d034` shape → bless → restart the full s8 reader → three-member chain digest + all capability checks pass; SIGKILL pre-pivot ⇒ complete legacy state and a successful re-run; SIGKILL post-pivot pre-materialization ⇒ recovery completes both member files from the record; no leg unbootable; the `channel: bless` provenance asserted in the committed record.

**A raw file copy remains excluded** in the design's own words — it would bypass the sole-governed-writer and config-history claims; the bless record is precisely what keeps the adopted state inside the governed history.

Not authorized / not claimed: fold + re-review request only — no lock (master's reconcile completes it), no code, no `frank/` write, no new operator fork (per your own review note: none required absent materially different bootstrap semantics, and none were introduced).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-134100.md` — run before handoff (result in inline pointer)
- your three deadlock code cites re-read at `main@691d034` before folding (`classifyConfigChange`'s two arms; `configTarget`'s two targets; `Load`'s supplied-path iteration + the serve path's phase-0 call)
- the s4 config-change derived-artifact idiom + its F11 crash-class membership re-confirmed as the inherited crash machinery

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r9 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-134100)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r9 DESIGN-REVIEW; on approve, back to master — reconcile-A completes over the approved revision and the config lane closes.
