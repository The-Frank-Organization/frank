## DESIGN — `s8-design-m7-config` r3: both r2 findings folded — the descriptor pins the POST-design census (three `config_paths` including the catalog home this design adds, with its own forbidden token; a provenance rule states the one deliberate departure from the `691d034` baseline) and ordering is now PER FIELD (set-like lists sorted; `output_families`/`sink_patterns` are ordered normative sequences whose re-ordering is a census amendment). Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the lock stays conditional on the operator F5 record (unchanged from r2); the activation-authorization leg is satisfied on the master record per your r2 note (`step2-plan/SITREP-orchestrator-planner-20260711-025643.md`)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-033001.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r3 artifact is the same file (status r3) — two narrow folds, nothing else moved: (F1) `config_paths` = three with the count/fixture/consumption updates (§3 schema + pinned values + §4.1 + FX-CFG-5's new canonical leg) and the provenance rule naming the post-design pin as the ONE deliberate departure from the shipped `691d034` census; (F2) the global sort rule is DELETED — per-field ordering with the family/pattern sequences marked order-semantic

**Fold map:**
1. **F1 → §3 (schema + pinned values + provenance rule) · §4.1 · FX-CFG-5:** `canonical_path_families.config_paths` now pins `["config/catalog/catalog.json", "config/engine.json", "config/fieldspec/registry.json"]` — the POST-design expected census, so the live `StoreRootConfigPaths` oracle (three paths on a conforming implementation) matches instead of redding every correct build; the catalog path contributes its own forbidden token to the leak corpus; FX-CFG-5 gains the canonical leg (live census == the three descriptor paths); and a **provenance rule** above the pinned values states explicitly that everything is the `main@691d034` shipped census EXCEPT this one post-design entry — no silent mixing of baselines.
2. **F2 → the §3 ordering rules:** "every list sorted lexicographically" is gone. Per-field: `boundary_files`/`scan.*`/`root_homes`/`config_paths`/recognizer lists sorted (set-like); `site_census` a sorted multiset; **`output_families` and `sink_patterns` are ORDERED NORMATIVE SEQUENCES** — the live law consumes census order, so the pinned order is semantic and re-ordering is a census amendment, not formatting. No universal rule, no example-inferable exceptions.

Not authorized / not claimed: fold + re-review request only — no lock (conditional on the operator F5 record), no code, no `frank/` write, no operator choice inferred.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-033341.md` — run before handoff (result in inline pointer)
- both r2 findings re-checked against the artifact text before folding; the three-path census cross-checked against §4.1's runtime-home addition and the consumption map's canonical row

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r3 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-033341)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r3 DESIGN-REVIEW; on approve, the reviewed artifact goes to master for reconciliation with `s8-design-m2-grammar` (the F5 fork riding as the named operator item).
