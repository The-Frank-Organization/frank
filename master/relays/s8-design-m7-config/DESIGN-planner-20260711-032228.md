## DESIGN — `s8-design-m7-config` r2: all four r1 findings folded — the descriptor is now a CLOSED schema with every current value pinned at `main@691d034` + a consumption map (F1); the drift oracle is real (bytes + `ExpectedConfigDigest()`, no phantom member digest — F2); the fieldspec SHA is the full 64-hex (F3); technical approval and operator ratification are separated, the lock conditional (F4). Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — per your F4: the reconciled DESIGN_LOCK is now CONDITIONAL on an operator record selecting fork (a) or (b); your approve covers technical soundness only
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-022341.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r2 artifact is the same file (`master/domains/m-7-conductor-core/design/2026-07-11-s8-config-host.md`, status r2) — the fold map below is one-to-one with your findings; nothing else moved (the engine-member home, activation mechanism, A-1 semantics, runtime home, genesis `observe:false`, step-4.5, and the load-claim boundary you confirmed are byte-unchanged)

**Fold map (one per finding):**
1. **F1 → the new §3:** the `discovery` section now has a **closed schema** (unknown field = a load-shape fault via FX-CFG-3) and **pins every current value normatively at `main@691d034`**: the 17-entry `site_census` verbatim (multiset — the `run->write` duplicate preserved), the six output families with the two carve-outs, the six sink patterns with their exact counts (16/9/13/8/10/11), the recognizer sets (context call-selectors, ident calls, `conn`-receiver calls, the `.Method` switch tag, the two tree-wide idioms), the scan roots/exclusions, and the canonical path families (ten homes + two config paths, with the live derivation named as the ORACLE and the descriptor as the EXPECTED census). Binding normalization rules (ToSlash-relative, sorted, multiset semantics, the exact `->`/`=>` site forms) + a **consumption map table**: which field each law leg reads and which mismatch class turns red (outside-boundary · census · family · sink · canonical · leak). The PLAN invents nothing — I verified the census values against the shipped code at `main@691d034` before pinning.
2. **F2 → §4.3 + FX-CFG-5/7:** ONE oracle, no new record machinery — (i) source bytes == runtime member bytes (direct comparison) AND (ii) the recomputed full composite == `ExpectedConfigDigest()` (genesis `config_digest` advanced through the accepted `config_change` chain, `genesis.go:64-79`/`:125-140`). The phantom "genesis-recorded member digest" is gone; a per-member digest manifest was considered and rejected (new machinery duplicating what bytes + the existing chain already prove). FX-CFG-7 asserts the fieldspec member bytes hash against the full-hex literal and the genesis composite against `config.Load(...).Digest`.
3. **F3 → §5.1 + FX-CFG-7:** the full `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485`, sourced to the s7a merge-gate record; the ellipsis form removed from the normative text and the GRILL_LOCK's "lock input" role.
4. **F4 → the GRILL_LOCK's lock-impact section:** your approve = technical soundness ONLY; the reconciled DESIGN_LOCK becomes effective only when an **operator record** selects (a) or (b) (fork (a) stays the recommendation; (b) requires the separately-grilled attestation design first); and master reconciliation carries the m-2 artifact's activation-authorization ratification **as its own operator item, never inferred from this review** — stated in the doc in those words.

Not authorized / not claimed: fold + re-review request only — no lock (conditional per F4), no code, no `frank/` write, no operator choice inferred, no m-2 content authored.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-032228.md` — run before handoff (result in inline pointer)
- the pinned census re-verified against `frank@main 691d034` this session: the 17-site `wantBoundary` read from the shipped test verbatim; families/patterns/counts from `catalog.v1.json`; the canonical homes from the shipped live-census test; the full SHA from `s7a-merge-gate/MERGE-GATE-orchestrator-planner-20260710-155633.md`
- `genesis.go:64-79`/`:125-140` re-read for the F2 oracle choice

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r2 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-032228; `frank/` read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r2 DESIGN-REVIEW; on approve, the reviewed artifact goes to master for reconciliation with `s8-design-m2-grammar` — with the F5 fork and the m-2 activation-authorization ratification traveling as named OPERATOR items in the reconcile package.
