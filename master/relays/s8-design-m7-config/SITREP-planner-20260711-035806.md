## SITREP — DESIGN COMPLETION to master: `s8-design-m7-config` r5 is pair-APPROVED (technical) — the artifact, the approving review, and the ONE outstanding operator item (the F5 fork) handed over for the m-2 reconciliation; the pair holds

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the reconciled lock is effective only after the operator's F5 record (fork (a) lean shape-only load claims = the designed default and my recommendation; fork (b) = the separately-grilled attestation round first)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-035512.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-7.implementer, m-2.planner
SUBJECT: the config-host design lane is complete at the pair — artifact `master/domains/m-7-conductor-core/design/2026-07-11-s8-config-host.md` (r5), approving review `s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-035512.md` (approve, technical), grill `s8-config-grill-r1` folded; five review rounds, all findings resolved; ready for your reconciliation with the approved `s8-design-m2-grammar`

**Handover facts:** the artifact carries — the knob's engine-member home + §7 restart-effective activation + the A-1 semantics · the catalog §7 member with F5-scoped load claims (shape-not-review, in those words) · the FULL closed discovery descriptor with every census pinned (the 17-site multiset, six order-semantic families + carve-outs, six sink patterns + counts, recognizers, scan rules, the twelve closed canonical-family rows incl. the post-design `catalog-config`) · the runtime home + the two-leg drift oracle (`bytes` + `ExpectedConfigDigest()`) · the genesis composition with the full 64-hex fieldspec SHA and `observe:false` · the §3 step-4.5 amendment (the four confirmed constraints) riding this doc per the amendment path. Review lineage: r1→r5, findings 4+2+2+1, all confirmed resolved in the approving relay.

**Operator decisions/defaults for the reconcile package (per the lifecycle):** (1) **the F5 fork — OPEN, operator-owned**, designed default (a); the lock is conditional on the operator record. (2) The m-2 activation-authorization item is ALREADY satisfied (`step2-plan/SITREP-orchestrator-planner-20260711-025643.md`) — consumed, not inferred. No other default was taken beyond the GRILL_LOCK's recorded decisions.

**The pair HOLDS** per the lifecycle: no PLAN, no lock consumption, no further revision unless your reconciliation changes an m-7 contract (in which case the amendment path re-engages the pair, not a silent edit).

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260711-035806)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: master reconciles config-host r5 with the approved `s8-design-m2-grammar`, routes the F5 fork to the operator as the named ratification item, and the s8 PLAN consumes only the reconciled, operator-ratified locks.
