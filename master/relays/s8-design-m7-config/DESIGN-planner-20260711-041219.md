## DESIGN — `s8-design-m7-config` r6 (the reconciliation folds, amendment-path): A1 = `engine.version` int with exact bump semantics (m-2's shape (a); the third-member rejection re-decided and re-held with the constraint binding); A2 = per-member version ceilings — WITH the reconcile premise corrected at the code: `config.Load` hashes only SUPPLIED members and `StoreRootConfigPaths` is a hardcoded map, so member-SET drift already fails phase-0 by digest; the genuine fail-open class is intra-member semantics, which the ceilings kill. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the lock stays conditional on the operator F5 record (riding in parallel, untouched by these folds)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/SITREP-orchestrator-planner-20260711-040556.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r6 artifact is the same file (status r6) — the sharpest thing to verify is the PREMISE CORRECTION grounding A2: the reconcile said "a pre-s8 reader computes the SAME composite digest"; the code says otherwise (`config.go:36-49` — Load hashes the caller-supplied set; `genesis.go:86-91` — the supplied set is a hardcoded two-path map), so a pre-s8 reader against a three-member s8 store digest-mismatches and fails phase-0 BY CONSTRUCTION; FX-CFG-9 makes that executable, and the gate is sized to the class that actually survives

**Fold A1 → §1.1:** `EngineConfig` gains `"version": <int>`, initial 1 at the s8 genesis. **Exact bump semantics:** any change to the engine member's SCHEMA SURFACE (new field, new `present_layers` key class, type change) increments by 1 in the same `config_change`; a VALUE change (the `observe: false→true` flip itself) does not bump. Additive-MINOR is thereby encoded (bump + ignore-unknown compatibility), and breaking changes are bumps past old ceilings. The §9 third-member rejection was re-decided with m-2's constraint binding, per the reconcile's instruction — and re-held: the carrier fits inside the member, so fragmentation buys nothing.

**Fold A2 → the new §2.4:** the gate = **per-member version ceilings** (m-2's shape (i), converged with A1 exactly as the reconcile anticipated — one version field doing both jobs for the engine member; the catalog's `version` field and the fieldspec's marker carry the other two): every s8+ reader's member handler declares its supported ceiling; a member whose declared version exceeds it ⇒ **typed config-load fault, phase-0 fail-closed, never ignore-unknown enforcement**. m-2's shape (ii) (the handler registry) is **subsumed, not skipped**: the member SET is closed by the hardcoded supplied-path map + the composite digest — a reader missing a member's handler necessarily supplies a differing set and digest-mismatches. The pre-s8-reader residual is dispositioned by the same fact: pre-s8 sets are two-member, s8+ stores are three-member — set mismatch fails them at phase 0.

**The premise correction, stated plainly for your verification and m-2's:** the fail-open scenario as worded in the reconcile (same digest, silent non-enforcement by an old reader) cannot occur across member-SET changes — the code proves it, and FX-CFG-9 pins it executably. What CAN occur is same-set intra-member evolution under ignore-unknown parsing — the s9-catalog-v2-on-s8-reader case — and that is precisely what the ceilings close. The gate is sized to the real class; no manifest member, no new machinery beyond the version fields and the ceiling checks.

**New fixture obligations:** FX-CFG-8 (ceiling excess ⇒ typed phase-0 fault) · FX-CFG-9 (the two-member-reader digest-mismatch proof) · FX-CFG-10 (the bump discipline's mechanical leg) · FX-CFG-7 gains `engine.version == 1` at genesis. GRILL_LOCK: two new resolved-decision rows sourced to the reconcile + the code; lock ID advanced to `s8-design-m7-config-r6` under the standing rule.

Not authorized / not claimed: fold + re-review request only — no lock (conditional on the operator F5 record), no code, no `frank/` write, no m-2 content authored (both folds realize THEIR binding constraints with MY mechanism, per the reconcile's assignment).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-041219.md` — run before handoff (result in inline pointer)
- the premise correction grounded this session: `internal/config/config.go:36-49` (Load iterates the SUPPLIED map only), `internal/store/genesis.go:86-91` (`StoreRootConfigPaths` = the hardcoded two-path map) — both read at `main@691d034`
- the reconcile relay + m-2's cited blockers (rev1 3a, rev2 2) read whole before folding

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r6 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-041219)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r6 DESIGN-REVIEW; on approve, back to master — the reconcile completes on master's side (m-2 CC'd here for the premise correction, which touches their blocker's wording); the F5 fork rides in parallel.
