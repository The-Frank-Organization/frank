## DESIGN — `s8-design-m7-config` r4: both r3 lock-text defects folded — canonical families are now twelve CLOSED `{id, relative, forbidden, directory}` rows (the s7 struct shape exactly) with `catalog-config` pinned + its named planted-token fixture leg and the path→ID/token derivation rule stated; the lock ID names the revision that receives the approve, staleness swept. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the lock stays conditional on the operator F5 record (unchanged)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-034210.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r4 artifact is the same file (status r4) — two folds, nothing else moved: (F1) `canonical_path_families` is now `rows: [{id, relative, forbidden, directory} ×12]` — the eight directory homes, `conductor-lock`, `engine-config`, `fieldspec-registry`, and the new **`catalog-config`** — with the derivation rule for the three config-file rows stated in the schema and FX-CFG-5 gaining the named `catalog-config` planted-token leg; (F2) the lock ID is `s8-design-m7-config-r4` with the swept-each-round rule stated, and a stale-identifier grep confirms zero superseded revision names remain

**Fold map:** F1 → the §3 schema (the closed twelve-row table replaces the two path lists; ids match the live law's existing family ids — `engine-config`, `fieldspec-registry`, the s7 forms — plus `catalog-config` as recommended; forbidden tokens per the s7 rules: separator-wrapped for directories, verbatim relative for files) · the pinned-values bullet (twelve rows, oracle = ten homes + three config paths) · FX-CFG-5 (the named `catalog-config` planted-token leg, per-family like the other eleven). F2 → the GRILL_LOCK lock-impact line (`s8-design-m7-config-r4`, with the explicit rule that the lock ID names the revision receiving the approve and is swept each round) — the sweep verified by grep before this relay.

Not authorized / not claimed: fold + re-review request only — no lock (conditional on the operator F5 record), no code, no `frank/` write.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-034700.md` — run before handoff (result in inline pointer)
- stale-identifier + obsolete-wording grep over the artifact after the fold → zero hits
- the twelve family ids cross-checked against the live law's `canonicalPathFamily` rows at `main@691d034` (ids/relatives/tokens/directory flags byte-consistent; `catalog-config` is the one post-design addition, consistent with the §3 provenance rule)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r4 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-034700)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r4 DESIGN-REVIEW; on approve, the reviewed artifact goes to master for reconciliation with `s8-design-m2-grammar` (the F5 fork riding as the named operator item).
