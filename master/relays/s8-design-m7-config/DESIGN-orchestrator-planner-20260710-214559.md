## DESIGN — s8 pre-design dispatch to m-7.planner (sole author): the CONFIG HOST design — the layer-activation knob's member home, the catalog's §7-pinned member/home with load-enforcement claims scoped honestly, the FULL governed discovery descriptor, the catalog's canonical runtime home, and the s8 genesis composition; grill required; your Implementer design-reviews before the lock feeds the s8 PLAN

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design dispatch; operator decisions the grill surfaces route to the operator as durable questions
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step2-prep/RECONCILE-orchestrator-reviewer-20260710-201733.md
FROM: master.orchestrator-planner
TO: m-7.planner
CC: operator, master.orchestrator-reviewer, m-7.implementer, m-2.planner
SUBJECT: supersedes the withdrawn `step2-prep/SITREP-…-193204` (report-only relays cannot direct design; treat it as consultation context only) — you are the SOLE AUTHOR of `s8-design-m7-config`; the m-2 grammar face is the SEPARATE artifact `s8-design-m2-grammar` and your shared content is a named boundary contract, never a shared writer; the VP's F5 requirements bind

**Your artifact (the m-7-owned config-host design, one author, your FROM):**
1. **The layer-activation knob's host:** the member under the pinned engine-config composite digest; activation = an operator-authored §7 `config_change`, restart-effective; A-1 re-render semantics stated (per your own intake shape, `step2-prep/SITREP-planner-…-010806`).
2. **The catalog §7 member/home — with the load claim scoped per the VP's F5:** digest/genesis verification proves the loaded bytes match the operator-authorized config history and can enforce member/provenance SHAPE at load; **it cannot prove the named owners reviewed a change.** Your design either (a) keeps owner fidelity as a relay/design-review gate and claims only digest/member/provenance-shape enforcement at load — the lean default — or (b) designs an owner-attestation mechanism (a larger, separately-grilled addition). **The (a)/(b) fork is a grill question; if it cannot be settled from the locked record, it is operator-owned.**
3. **The governed discovery descriptor — the FULL final s7 mechanism, not a summary (F5):** scan roots/exclusions · the registered boundary files · the tree-wide portable idioms + the transport-context-scoped recognizers · the exact current site/family census — every element that affects whether `TestLawPathHygiene` bites becomes a governed, amendable census member.
4. **The catalog's canonical runtime home:** `test/invariants/catalog.v1.json` is a source artifact, not a guaranteed installed runtime-config home — lock the canonical deployment home + the byte-copy path between them.
5. **The s8 genesis composition:** how the knob member + the catalog member + the existing registry member compose into the dogfood store's genesis (your recorded condition: the exact `s7a-fieldspec-v5` bytes, member SHA `1ef6abab…2485`, composite digest recorded).

**Process (the VP's replacement shape, binding):** run the **grill** per the design-grill skill (one question at a time; codebase-answerable questions answered from the locked record; operator-owned forks surfaced as durable questions) → fold the `GRILL_LOCK_ID` into your design doc → **m-7.implementer returns `PHASE: DESIGN-REVIEW`** on your artifact → the reviewed doc relays to master. **Boundary contract:** name `s8-design-m2-grammar` (m-2's atom-semantics/member-grammar face) where your design consumes it; do not author m-2's content. Master reconciles the two reviewed artifacts; **the s8 PLAN consumes only the reconciled locks.**

Next requested action: your grill → the design doc → your Implementer's DESIGN-REVIEW → the reviewed artifact to master.

ACTIONS_GIT_REF: none — design dispatch only.
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `2e1b4f0` (tag `s7-close`); cwd is not a git repo (docs workspace).
