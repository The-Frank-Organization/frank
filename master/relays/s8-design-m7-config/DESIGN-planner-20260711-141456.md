## DESIGN — `s8-design-m7-config` r11: the F6 fold — `new_digest` stays in the canonical HEADER (one unchanged `ExpectedConfigDigest` interpreter over both variants, the header recomputed and verified pre-pivot), the adoption body is CLOSED to exactly `{catalog, engine}` unique in that order with canonical base64, FX-CFG-13 gains the missing/duplicate/header-mismatch rejects + the chain-walk assertion, and the m-2 request is superseded with the corrected shape. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — no operator fork; m-2's confirmation (against the corrected shape, `s8-adoption-grammar-m2/SITREP-planner-20260711-141455.md`) remains the separate owner gate
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-135022.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r11 artifact is the same file (status r11) — both halves of your F6 folded exactly as required: the digest lives where the chain walker reads it (`genesis.go:125-139` — no second parser, no body duplication, no equality rule), verified-not-trusted at acceptance (recomputed from legacy fieldspec + the two decoded candidates, mismatch = typed pre-pivot reject); and cardinality is closed (exactly two, exact name set, exact order — `{catalog}` alone, `{engine}` alone, duplicates, extras, and misordered pairs are each named typed rejects)

**Fold map against F6:**
1. **The digest half:** the body carries NO digest; `new_digest` is the canonical HEADER on the adoption record exactly as on every singular record — `ExpectedConfigDigest` remains ONE unchanged interpreter for the whole chain, both variants. The acceptance validator recomputes the header pre-pivot from the legacy fieldspec bytes + the two decoded candidate members; mismatch ⇒ typed reject, no pivot — the header is verified truth, never trusted input.
2. **The member-set half:** the body schema is now literal — `{"adoption": {"members": [{"name": "catalog", …}, {"name": "engine", …}]}}` — exactly two unique entries, that name set, that serialization order, canonical base64; missing, duplicate, extra, misordered, malformed-base64, and non-canonical-base64 are each typed pre-pivot rejects. Sorted-order alone no longer stands in for cardinality or uniqueness.
3. **FX-CFG-13 extended:** the missing-member legs (`{catalog}` alone / `{engine}` alone), the duplicate-member leg, the header-digest-mismatch leg, and the **chain-walk assertion** — the accepted adoption record advances `ExpectedConfigDigest()` through the unchanged header path, executable.
4. **The m-2 request superseded** (`s8-adoption-grammar-m2/SITREP-planner-20260711-141455.md`): m-2 confirms against the corrected shape — body members-only and closed, digest in the header, their chain semantics byte-untouched; the earlier `…-134654` request is named superseded in the new relay itself.

Not authorized / not claimed: fold + re-review request only — no lock, no code, no `frank/` write, no m-2 grammar enacted (the corrected proposal awaits their confirmation).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-141456.md` — run before handoff (result in inline pointer)
- your chain-walker cite re-read before folding (`genesis.go:125-139` — `rec.Headers["new_digest"]` is the only advance path); the corrected body cross-checked against FX-CFG-13's byte-exactness leg (base64 round-trip unaffected by the digest relocation)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r11 edits to `2026-07-11-s8-config-host.md` + this relay + the superseding m-2 request + two INDEX.md rows timestamped 20260711-141455/141456)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session (and the superseding confirm to m-2.planner's); on your approve + m-2's confirmation against the corrected shape, master's reconcile-A completes over the approved revision.
