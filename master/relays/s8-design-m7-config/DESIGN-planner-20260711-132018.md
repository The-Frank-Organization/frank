## DESIGN — `s8-design-m7-config` r7: both r6 mechanism gaps folded — (F1) the version gate now has ACCEPTANCE-TIME teeth via the key-path-invariance rule (same version ⟺ same key-path set; typed path-free `config-version-transition` rejects; recovery replays accepted transitions only; the one non-machine-detectable direction stated honestly); (F2) the closed per-member capability table (engine numeric ceiling · catalog/fieldspec exact supported sets · marker-checked-FIRST · s8 initial values pinned). The F5 fork is RESOLVED — operator selected (a). Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the F5 operator record exists (`SITREP-orchestrator-planner-20260711-130825.md`, fork (a)) and the activation authorization was already ratified; only technical review remains
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-131649.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r7 artifact is the same file (status r7) — the sharpest thing to verify is F1's enforcement grain: the KEY-PATH-INVARIANCE rule closes exactly the direction your finding named (schema drift hiding under a same version ⇒ typed reject at the trusted acceptance path, before the pivot), while the direction shape cannot detect (a semantics-only bump under an unchanged key-path set) is admitted lawful and left to the review convention IN THOSE WORDS — no re-created fail-open, no overclaim

**Fold F1 → the new §2.4 (the acceptance-time transition gate):** the `config_change` acceptance validator — in the serialized loop, before the pivot, the same locus as every other gate — compares current vs candidate per member on the **deep JSON key-path set** (`present_layers.observe` vs `present_layers.<new>` are distinct paths, so your §1.1 schema-vs-value distinction is mechanically realized): **version unchanged is lawful ⟺ the key-path set is unchanged**; same-version drift ⇒ typed path-free reject (`config-version-transition`, D-2 parity); engine successor = exactly current+1, rollback and skip rejected; marker members (catalog/fieldspec) require a CHANGED marker on shape change, the successor VALUE staying the owning domain's grammar; **recovery replays accepted transitions without re-validation** (store-is-truth — no historical record re-judged). FX-CFG-10 is rewritten as six executable legs of exactly this gate — the review-convention leg is GONE from the fixture, surviving only as the honestly-named residual direction.

**Fold F2 → the closed capability table in §2.5:** per member — marker field, form, comparison rule, s8 initial, s8 supported, and the fault column: engine `version` integer with an ordered ceiling (max 1); catalog `version` exact set (`{"s8-v1"}` — the s8 PLAN re-stamps the source artifact's `"s7-v1"` as part of the §7 pinning, now stated); fieldspec's registry marker exact set (`{"s7a-fieldspec-v5"}`, the successor grammar m-2's). The common contract is named honestly as **"handler-declared supported versions"** — no pretend total order over string markers. **The marker is read and validated FIRST**: missing/malformed/unknown/rollback/above-support faults phase-0 with zero partial interpretation, and FX-CFG-11 asserts the zero-partial-interpretation property itself.

**Also on this record:** the GRILL_LOCK's operator question is closed — **fork (a) selected by the operator record**, so the lock-impact text now reads: operator conditions SATISFIED; effectiveness waits only on master's reconcile completion over the approved revision. Lock ID advanced to `s8-design-m7-config-r7`.

Not authorized / not claimed: fold + re-review request only — no lock (master's reconcile completes it), no code, no `frank/` write, no m-2 content authored (the fieldspec successor grammar and marker vocabulary stay theirs; my gate consumes "changed shape ⇒ changed marker" and the reader's pinned set only).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-132018.md` — run before handoff (result in inline pointer)
- both r6 findings re-read against the r7 text after folding; the key-path rule cross-checked against §1.1's bump semantics (the `observe` flip = value-only = no bump = lawful same-version, consistent end-to-end)
- the F5 operator record and the activation-authorization record cited as consumed, not inferred

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r7 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-132018)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r7 DESIGN-REVIEW; on approve, back to master — reconcile-A completes over the approved revision and the config lane is fully closed (all operator items already satisfied).
