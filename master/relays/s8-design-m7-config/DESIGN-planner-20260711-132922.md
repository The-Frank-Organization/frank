## DESIGN — `s8-design-m7-config` r8: the transition predicate rebuilt — per-version SCHEMA DESCRIPTORS replace the key-path diff (validation decides schema surface: node kinds, closed key sets, element shapes, index/value-insensitive); the biconditional is gone (one-way implications); string markers get owner-supplied forward-transition relations with rollback/skip-between-supported as typed rejects and the simulated multi-marker fixture leg. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-config
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the F5 operator record stands; only technical review remains
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-config-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-config/DESIGN-REVIEW-implementer-20260711-132429.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: the r8 artifact is the same file (status r8) — your three findings converge on ONE restructure: each handler carries a canonical schema descriptor PER SUPPORTED VERSION, and the acceptance validator becomes (a) declared version within support (with the adopt-new-version sequencing stated: reader first, record second) → (b) candidate validates against schema(V) → (c) V==current ⇒ accept (value-only BY CONSTRUCTION, killing F2's biconditional); V≠current ⇒ the transition must be in the member's forward relation (engine: exactly +1; markers: the owner-supplied successor map — consumed, never invented), rollback/skip between supported versions typed rejects (F3)

**Why one restructure answers all three:** your F1's counterexamples (`{"x":1}`→`{"x":"one"}`, scalar→container, element-shape changes) are invisible to any path-set diff but FAIL schema validation trivially — so the predicate becomes "does the candidate validate against the schema for its declared version," which is mechanically complete, canonical by construction (the descriptor IS the normalization: node kinds at every position, closed key sets where closed — `present_layers`' key set per version — array element shapes with indices never participating, no value constraints beyond kind), and immune to false drift from ordinary catalog row additions (the row shape is declared; the count is unconstrained). Your F2's contradiction dissolves because there is no shape-diff left to bicondition on: unchanged version ⇒ must validate against the CURRENT schema (surface unchanged, proven); changed surface ⇒ needs a lawful forward transition; a semantics-only bump (new version, schema unchanged) passes and is the explicitly named residual. Your F3's gap closes with the **forward-transition relation** each string-marker handler declares alongside its supported set — m-2 supplies fieldspec's sequence, the catalog convention the catalog's; rollback and skip between otherwise-supported markers are typed `config-version-transition` rejects.

**The executable additions (FX-CFG-10 rewritten, nine legs):** the three same-version rejects your fold demanded (added key · scalar TYPE change · container/element-shape change) · the genuine value-only accept · engine rollback/skip rejects · the lawful-transition accept · the recovery replay leg · the semantics-only-bump accept · **the simulated multi-marker handler leg** ({vA,vB} rollback reject; {vA,vB,vC} skip reject).

**Composition dividend, noted:** the descriptors double as §2.2's load-SHAPE validators — one artifact per version serves the acceptance gate and the load gate, so F2's capability table and this predicate share their machinery rather than duplicating it.

Not authorized / not claimed: fold + re-review request only — no lock (master's reconcile completes it), no code, no `frank/` write, no owner vocabulary invented (the fieldspec/catalog successor maps are consumed as boundary contracts).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-config/DESIGN-planner-20260711-132922.md` — run before handoff (result in inline pointer)
- all three r7 findings re-read against the r8 text after folding; the key-path predicate survives only in the status line naming its replacement (grep-verified); the `observe`-flip value-only path re-traced end-to-end under the descriptor model (declares version 1 · validates against schema(1) · V==current ⇒ accept)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r8 edits to `2026-07-11-s8-config-host.md` + this relay + one INDEX.md row timestamped 20260711-132922)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r8 DESIGN-REVIEW; on approve, back to master — reconcile-A completes over the approved revision and the config lane closes (all operator items satisfied).
