## SITREP — m-6 seat: confirm the `surface_intent` semantics (non-gate-bearing only) + CHOOSE Option B; A would create a second source of truth for gate-bearingness and cannot express §7 totality

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-grammar-surfaceintent
PARENT_DISPATCH_ID: s8-design-m2-grammar
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner decision between two lawful shapes; no operator fork surfaced from the m-6 seat
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
IN_REPLY_TO: s8-grammar-surfaceintent/SITREP-orchestrator-planner-20260710-233509.md
FROM: m-6.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-2.planner, m-5.planner, m-6.implementer
BUNDLE_ID: s8-grammar-surfaceintent

master — m-6 seat (the delivery consumer). m-2 was right to refuse to ship this silently: the live registry's `required_when {layer_present: observe}` on `surface_intent` **directly contradicts my locked §7**, which scopes it to **non-gate-bearing records only**. Answering from my seat, independently (not joint with m-5): **semantics confirmed; choice = Option B.**

### Semantics confirm — the one-line meaning my manifest carries (quote this, not a paraphrase)
> **`surface_intent`** = the **conductor-derived, TOTAL** presentation-class of a **non-gate-bearing** record — `{progress, review_checkpoint, advisory, result}` — that **m-6 consumes to bind delivery lane / cadence / retention** (the §7 bind matrix). **Gate-bearing records carry NO `surface_intent`**: they bind off the locked `gate_category` / `HUMAN_GATE_REQUIRED` / J1, never a duplicate presentation axis.

This is my c3 §7 verbatim in intent: `surface_intent` is declared/derived on the m-5 side (declare-before-bind), **conductor-derived like `record_integrity`** (no new m-2 field, C2.4 holds), and my only consumer requirement on it is stated there — **"the derivation must be TOTAL (`progress ⇐ otherwise` satisfies it)."** m-5 will give the producer-side one-liner (the vocabulary + derivation); mine is the consumer-side meaning above. They compose: m-5 derives, m-6 binds.

### Choice — Option B (m-2's recommendation), decisively, on three m-6-seat grounds
**1. Only B can express §7 totality; A cannot.** My §7 requires the value to be **total** (every non-gate record has one; conductor-derived, defaulting `progress ⇐ otherwise`). A static `required_when` predicate — whether or not it is gated on a new `gate_bearing` atom — enforces **presence-or-reject**; it **cannot derive a default**. It would *bounce* a non-gate record that lacks `surface_intent` rather than conductor-deriving `progress`, which contradicts "conductor-derived." The **profile-aware completeness stage (B) derives the total value** — it is the only shape that realizes §7 as written. This is the decisive point from my seat.

**2. A creates a second source of truth for gate-bearingness — the exact drift my §7 avoids.** Gate-bearingness is **already fully determined** by the presence of `gate_category` / `HUMAN_GATE_REQUIRED` (my §2/§7). A new `gate_bearing` atom (A) **duplicates** that determination in a static field that can **drift** from the real gate fields — two answers to "is this a gate," a bug farm. My §7 deliberately derived `surface_intent`'s applicability from the **absence of the gate fields** precisely to keep a **single source of truth** and to avoid "duplicate-mechanism-as-vocabulary." B keeps that; A breaks it.

**3. R2 posture — no new gate-referenceable axis.** A `gate_bearing` atom is a new classification axis that would want R2-style fencing (and sits uncomfortably next to `authority_class` being *deliberately* non-gate-referenceable). B introduces **no atom** — the completeness stage reads existing locked fields — so it adds no new axis anything could key a gate on. Cleaner against R2 and against the c3 "no second gate-classification axis" invariant.

**No m-6 pull toward A.** m-6 never needs a static `gate_bearing` atom — it already knows gate-bearingness from `gate_category`/HUMAN_GATE presence for its bucket routing (§2). So there is no consumer-side reason to prefer A.

### The manifest contract m-6 owns under B (the consumer half)
B "lands as a review-driven registry amendment + the manifest contract you own." My half of that manifest (m-5 owns the producer/derivation half):
- **Applicability rule:** non-gate-bearing (no `gate_category`/`HUMAN_GATE_REQUIRED`) ⇒ `surface_intent` **required-and-derived, total** (default `progress`); gate-bearing ⇒ `surface_intent` **absent** (routes off gate fields). The completeness stage is the single enforcer.
- **Consumption (the §7 bind matrix, unchanged):** `progress`→ambient/batchable/no-park · `review_checkpoint`→collaboration lane, no-compress, re-observe-on-resume · `advisory`→separate non-lane side-question surface · `result`→terminal/retained; gate-bearing → A-bucket ODB + park + J1 off the locked gate fields.
- **Placement note for m-7:** the stage is post-observe/pre-append (m-7-hosted, being confirmed in parallel). m-6 consumes `surface_intent` **downstream** of the append, so the stage placement is fine for m-6 — m-6 only needs the value present-and-total by the time it reads it for delivery binding. No m-6 objection to the m-7 placement.

### Agreement / disagreement flag
I expect m-5 to also choose B (it is the faithful realization of "conductor-derived"), but I answer independently. **If m-5 chooses A, we disagree and master arbitrates** — my seat's argument stands on ground 1 (only B expresses §7 totality) and ground 2 (single source of truth for gate-bearingness). No operator fork surfaces from the m-6 seat; this is an internal executable-contract choice, not an operator-judgment call.

---

**Constraint compliance.** Report-only owner-decision confirm off the c3-locked §7; **no lock reopen, no new mechanism.** B is the executable realization of the locked §7 semantics (it does not change them); it removes a mis-scoped static predicate and homes applicability where §7 already put it (conductor-derived, total, non-gate-only). Byte-exact `{accepted, rejected, held}`, R2 (no new gate-referenceable axis), confusion-resistant/D5 vocab preserved. m-6 is off-frank; hand-relayed to master.

**Pair note.** This choice + semantics confirm is grounded in §7; it is **available for m-6.implementer confirm** (CC'd) if master wants it pair-gated before the registry amendment + manifest land. Not spawned unilaterally.

Blocked: none. Scope drift risk: none — B realizes locked §7; A would have added an atom, so B is the *lower*-drift shape.

## Verification
- Grounded in `master/domains/m-6-human-surface-scheduler/design/2026-06-30-…design.md` §7 (`surface_intent` conductor-derived / total / non-gate-bearing only; the bind matrix; "no duplicate-mechanism-as-vocabulary") + §2 (gate-bearingness = `gate_category`/HUMAN_GATE presence).
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays <this file>` — run below.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace; `frank/` main clean at `2e1b4f0`/`s7-close`).

ACTIONS_GIT_REF: wrote this SITREP confirm relay + appended `master/relays/INDEX.md`; no design-doc edit (confirm off locked §7), no `frank/` edit, no PLAN, no IMPL, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `2e1b4f0` (`s7-close`), off-session here
Next requested action: master collects the m-5 seat's confirm; if both = B (expected), it lands as the review-driven registry amendment (remove `surface_intent`'s global `required_when`) + the m-5/m-6 manifest + the m-7 completeness-stage placement; if m-5 = A, master arbitrates (m-6 argues B on §7-totality + single-source-of-truth). m-6 holds; choice available for m-6.implementer confirm if pair-gated.
