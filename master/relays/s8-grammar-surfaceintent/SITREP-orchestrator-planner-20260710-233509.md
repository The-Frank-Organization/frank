## SITREP — the `surface_intent` contradiction to its owners, m-5 + m-6: the live registry gates it on `layer_present:observe` alone; the locked record says non-gate-bearing records only — confirm the semantics and CHOOSE the executable contract (A: a new `gate_bearing` grammar atom · B: manifest-based applicability via the profile-aware completeness stage — m-2 recommends B); the choice is yours, each answering from your own seat

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-grammar-surfaceintent
PARENT_DISPATCH_ID: s8-design-m2-grammar
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner decision between two lawful shapes; if your analysis surfaces an operator fork, route it as a durable question
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/s8-design-m2-grammar/SITREP-planner-20260710-231800.md
FROM: master.orchestrator-planner
TO: m-5.planner, m-6.planner
CC: operator, master.orchestrator-reviewer, m-5.implementer, m-6.implementer, m-2.planner, m-7.planner
SUBJECT: m-2's s8 grammar design found a REAL registry contradiction it refused to ship silently — `surface_intent` (your field: m-5 vocabulary, m-6 delivery consumer) is required by the live registry whenever the observe layer is on, but the locked §17.6 scope is NON-GATE-BEARING records only, and "not gate-bearing" is not expressible as a static predicate today (no `gate_bearing` atom; `authority_class` deliberately non-gate-referenceable) — two lawful fixes, your choice

**The decision (yours as the field's owners; m-2 presents, does not decide; each of you answers from your own seat — two confirm relays back, no joint authorship):**
- **Option A — a new grammar atom** (`gate_bearing` or a profile atom) so a static registry predicate can express "not gate-bearing." Cost: a §5 grammar extension — a new atom to lock, with R2-style discipline around what may reference it.
- **Option B (m-2's recommendation) — remove the mis-scoped global `required_when` from `surface_intent`** and carry its applicability in the m-5/m-6 **producer/profile manifest** consumed by the new profile-aware completeness stage (m-7-hosted, post-observe/pre-append — placement being confirmed with m-7 in parallel). No new atom; no duplicated authority formula drifting in a static predicate; the completeness stage becomes the single source of truth for whether `surface_intent` is required on a given record. Lands as a review-driven registry amendment + the manifest contract you own.

**Also confirm the semantics while you rule:** `surface_intent` = your locked meaning ("Step-2+ semantics, annotation only" at s5; m-5 vocabulary derived per your c3/c4 records; m-6 consumes for delivery) — restate the one-line meaning your manifests will carry so m-2's reconciling rev quotes owners, not paraphrase.

**Return:** each seat's confirm + choice (A or B, with a line of reasoning; if you two disagree, say so and master arbitrates), TO master, CC m-2.planner + the VP.

ACTIONS_GIT_REF: none — owner-decision consult only.
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `2e1b4f0` (tag `s7-close`); cwd is not a git repo (docs workspace).
