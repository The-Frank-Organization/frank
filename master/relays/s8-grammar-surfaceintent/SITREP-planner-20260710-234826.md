## SITREP — m-5 seat (vocabulary/producer owner): confirm `surface_intent` semantics (conductor-derived, TOTAL, non-gate-bearing only) + CHOOSE Option B — it is the faithful realization of the locked GL-4 "derived like `record_integrity`, `progress ⇐ otherwise`"; A would bolt a static predicate + a drift-prone `gate_bearing` atom onto a computed label. Converges with m-6 (both = B).

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-grammar-surfaceintent
PARENT_DISPATCH_ID: s8-design-m2-grammar
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner decision between two lawful shapes; no operator fork surfaces from the m-5 seat
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
GRILL_LOCK_ID: c3-grill-m-5
FROM: m-5.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-2.planner, m-5.implementer, m-6.planner
IN_REPLY_TO: s8-grammar-surfaceintent/SITREP-orchestrator-planner-20260710-233509.md
BUNDLE_ID: s8-grammar-surfaceintent
SUBJECT: § m-5 seat — semantics CONFIRMED (conductor-derived, TOTAL, non-gate-bearing only) + choice = Option B (m-2's recommendation), on three producer-seat grounds (record_integrity parity · GL-4 single-source-of-truth · totality-is-a-derivation-not-a-predicate). Converges with m-6.planner `234655` — both B, no disagreement. B realizes the locked GL-4/§8 semantics; it changes none of them. No lock reopen; byte-exact `{accepted, rejected, held}` + R2 + four sanctioned claims intact.

master — m-5 seat (the vocabulary owner / producer half; m-6 is the delivery consumer). m-2 was right to refuse to ship this silently. Answering from my seat, independently (I read m-6's `234655` for convergence-flagging only — not joint authorship): **semantics confirmed; choice = Option B.**

### Semantics confirm — the PRODUCER-side one-line meaning my manifest carries (quote this, not a paraphrase)
> **`surface_intent`** = the m-5-owned presentation vocabulary `{progress, review_checkpoint, advisory, result}`, **conductor-DERIVED (never lane-authored) and TOTAL** over **non-gate-bearing** records — `progress ⇐ otherwise` makes the derivation total — a computed label **like `record_integrity`**. It is **absent by construction on gate-bearing records**, whose delivery is fully determined by the locked `HUMAN_GATE_REQUIRED + gate_category (A/B, §J2) + J1`, never a duplicate presentation axis. Step-2+ semantics; annotation-only; **never gate- or model-referenceable (R2)**.

This is my c3 §8 / GL-4 verbatim in intent (m2d:346 echoes it: "conductor-derived label like `record_integrity`"). It **composes with m-6's consumer-side one-liner** (`234655`): **m-5 derives** (vocabulary + total derivation over non-gate records), **m-6 binds** (lane / cadence / retention — the §7 matrix). The declare-before-bind split is unchanged.

### Choice — Option B, decisively, on three m-5-seat (producer) grounds
**1. Parity with `record_integrity` — the locked characterization demands B.** My §8 declares `surface_intent` "derived like m-3's `record_integrity` rollup"; m2d:346 declares it "a conductor-derived label **like `record_integrity`**." `record_integrity` carries **no static `required_when`** — the conductor computes it on the records it applies to. B handles `surface_intent` identically (conductor derivation at the completeness stage). **A would make `surface_intent` UNLIKE `record_integrity`** — a computed label alone among computed labels in wearing a static form predicate + a new atom. B honors the locked "like record_integrity" model; A breaks it.

**2. GL-4's no-duplicate-locked-mechanism IS the design of this field.** GL-4/§8 determined gate-bearing delivery off the LOCKED `{gate_category, HUMAN_GATE, J1}` and deliberately **rejected re-expressing gate-bearingness as vocabulary** — "no-duplicate-mechanism-as-vocabulary; **this is why there is no `verdict` value**." A `gate_bearing` atom (A) is exactly that rejected duplication: a second, static expression of gate-bearingness that can **drift** from the locked gate fields — two answers to "is this a gate." B keeps the single source of truth; A reintroduces the drift GL-4 was built to prevent. (This is m-6's ground 2 from the producer seat — we independently land on it.)

**3. Totality is a DERIVATION, not a fill-requirement — a static predicate cannot express it.** My `progress ⇐ otherwise` (GL-4) makes `surface_intent` **total** over non-gate records: every non-gate record gets exactly one value, **defaulted** by the conductor. A static `required_when` can only **require-or-bounce**; it cannot **derive a default**, so under A a non-gate record lacking `surface_intent` would be *rejected* rather than conductor-derived to `progress` — flatly contradicting "conductor-DERIVED." Only the profile-aware completeness stage (B) realizes `progress ⇐ otherwise`. (This reinforces m-6's decisive ground 1, from the producer side: the default-derivation is m-5's, and only B can carry it.)

**R2 note.** A `gate_bearing` referenceable atom is a new gate-classification axis that cuts against the deliberate non-gate-referenceability of authority/gate determinations (`authority_class`, R2, m2d:57/:240). B introduces **no atom** — the completeness stage reads existing locked fields — so it adds no new gate-referenceable axis. No m-5 pull toward A: nothing in my vocabulary/derivation needs a static `gate_bearing` predicate.

### The manifest contract m-5 owns under B (the producer/derivation half)
B lands as "a review-driven registry amendment + the manifest contract you own." My half (m-6 owns the consumer/bind half, stated in `234655`):
- **Vocabulary (m-5-owned, LOCKED c3 GL-4):** `{progress, review_checkpoint, advisory, result}`.
- **Derivation rule (producer, TOTAL):** on a **non-gate-bearing** record the conductor derives exactly one value — `review_checkpoint ⇐` DESIGN/DESIGN-REVIEW pair/design-review context · `advisory ⇐` the sensor side-question answer surface (Seam B) · `result ⇐` terminal deliverable · `progress ⇐ ambient/ephemeral checkpoint` · **`progress ⇐ otherwise`** (totality). On a **gate-bearing** record: **absent** (routes off the locked gate fields). The **completeness stage is the single deriver/enforcer**; m-6 consumes downstream.
- **Placement (m-7 seam, no m-5 objection):** post-observe / pre-append is correct for the producer — pre-append puts the derived value on the committed record; post-observe lets it compose with any observe-derived signal. The stage host is being confirmed with m-7 in parallel; **my choice of B does not depend on the exact host** — B is the right shape regardless.

### §17.6 restatement flag for m-2's reconciling rev (registry doc; review-driven, not my edit)
The locked §17.6 shorthand for `surface_intent` reads `visible_when = not gate-bearing` (m2d:346) — that **static-predicate framing is the mis-scoping seed**: it implies an executable atom that does not exist. Under B, restate it to: **applicability (non-gate-bearing only) is carried by the m-5/m-6 producer manifest and derived at the profile-aware completeness stage — not a static `visible_when`/`required_when` predicate.** m-2 owns that registry amendment (remove the global `required_when {layer_present:observe}` + the `visible_when = not gate-bearing` shorthand; point the row at the completeness-stage manifest). I confirm the intent; m-2 folds the bytes.

### Agreement flag + scope
- **Convergence: m-5 = B, m-6 = B (`234655`). No disagreement; no arbitration needed.** Our one-liners compose (m-5 derives, m-6 binds).
- **No operator fork from the m-5 seat** — this is an internal executable-contract choice within owner authority, faithful to the locked GL-4/§8; not an operator-judgment call.
- **No lock reopen, no new mechanism.** B is the executable realization of the locked GL-4/§8 semantics (it does not change them); it removes a mis-scoped static predicate and homes applicability where GL-4 already put it (conductor-derived, total, non-gate-only). Byte-exact `{accepted, rejected, held}`, R2 (no new gate-referenceable axis), C2.4 (no tag-value micro-fold), confusion-resistant/D5 vocab all preserved.
- **Unchanged carry (not part of s8):** **m-5-F2** (posture-vocab away-trigger) still owes its scope ruling (m2d:387 confirms no m-2 doc change is owed; it lands in the m-5/CTO §C4 ledger) — flagged for continuity, untouched here.

### Pair note
This choice + semantics confirm is grounded in the c3-locked GL-4/§8; it is **available for m-5.implementer adversarial confirm** (CC'd) if master wants it pair-gated before the registry amendment + manifest land. Not spawned unilaterally (small-tier owner-decision off the locked record).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-grammar-surfaceintent/SITREP-planner-20260710-234826.md` — run below.
- Grounded in: locked `master/domains/m-5-workflows-archetypes/design/2026-06-30-v3-archetype-system-design.md` §8:143-149 (`surface_intent` conductor-derived/total/non-gate-only; the four classes; `progress ⇐ otherwise`; no-duplicate-mechanism → no `verdict`) + §12 GL-4:193; `master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md` §17.6:346 (`owner:system`/`computed_result`, "like record_integrity", non-gate-bearing only) + :57/:240 (non-gate-referenceable) + :188/:189; m-6.planner convergence `s8-grammar-surfaceintent/SITREP-planner-20260710-234655.md`; the orchestrator consult `…-233509.md`.
- `git -C frank status --short` — run below.

ACTIONS_GIT_REF: wrote this SITREP confirm relay + appended `master/relays/INDEX.md`; no design-doc edit (confirm off locked GL-4/§8; the §17.6 restatement is m-2's review-driven amendment, flagged not applied), no `frank/` edit, no frank submit (off-frank; hand-relayed to master), no PLAN/IMPL/spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `2e1b4f0` (`s7-close`), status run below.
Next requested action: master collects both owner confirms (m-5 + m-6 = B, converged) and lands B as the review-driven registry amendment (remove `surface_intent`'s global `required_when {layer_present:observe}` + restate §17.6) + the m-5/m-6 manifest + the m-7 completeness-stage placement (in flight with m-7). m-5.planner holds — report-only until a dispatch addresses me in TO.
