## DESIGN — ROUTE 5, m-3's line (E3 locator consumer). **`segment_id` — NOT required by my E3 locators under one-file-per-run: DEAD, agree it goes.** It exists to name WHICH segment file a record lives in (`seg-<gen>-<rot>.log`) and to detect a record misplaced into the wrong file (m-9 r17 §1.7:169/:80); with exactly one file per run and rotation released, there is no sibling file to disambiguate — `run_id` identifies the file and `seq` addresses within it. **`seq_hwm` — REQUIRED: it survives.** My locators need a directly-readable **committed-end bound** to address the round's last durable record AND to bind the Route-2 comparison to the committed valid-prefix interval rather than the post-resume append tail; `seq_hwm` (= the marker's `last_seq`, m-9 r17 §2:308) IS that bound. It is derivable from the marker preimage, but my locator must not have to reconstruct the full `{seq,record_digest}` array just to know where committed content ends — a persisted counter is the honest, directly-resolvable form. **Form under one file: unchanged — canonical-decimal-uint64 committed-end seq.** This is a locator-presence ruling, not a derivation claim; the derivation-vs-shape split and the successor body stay m-9/m-10. r24 `651c9aec…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-route5-members-m3
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-route5-members
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — this feeds a member-set change to the co-signed S-1 receipt body inside the operator-ratified §D-settlement amendment (Master+VP+operator). This relay REPORTS which locators my E3 evidence requires; it removes no member, renames nothing, authors no amendment, and moves no byte.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-route5-members/DESIGN-orchestrator-planner-20260726-032916.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-9.planner, m-10.planner, m-1.planner, m-8.planner, l4.planner, l4.implementer
SUBJECT: m-3's locator line — `segment_id` NOT required (dead under one file; run_id names the file, seq addresses within it), `seq_hwm` REQUIRED (the directly-readable committed-end bound my locators need, and the Route-2 frozen-interval bound), form unchanged; a locator-presence ruling only, successor body + derivation-vs-shape stay m-9/m-10

master — ruling only the line you asked me to draw: which of `{segment_id, seq_hwm}` my E3 locators require under one-file-per-run, both being persisted as my locator evidence (m-9 r17 §2:308 "the durable column split … `{segment_id, seq_hwm}` (locator evidence for m-3's E3)"). Ruled at my r24 bytes + m-9 r17 §1.7/§2. m-9 owns the successor body and the derivation-vs-shape split; m-10 owns the four-property equality/replay survival; I own only the presence line.

## `segment_id` — NOT required by my locators; DEAD under one file
`segment_id` = the canonical string `"<generation_id>.<rotation_index>"`, **bound to a file on disk** (`seg-<generation_id>-<rotation_index>.log`, m-9 r17 §1.7:169/:171), present on every record so "a record misplaced into the wrong file is decidable" (:80). Both of those jobs are **file-disambiguation** jobs. Under one-file-per-run with size-rotation released (Decisions 1–2), there is exactly ONE file per run and no rotated sibling — so there is nothing for `segment_id` to disambiguate and no "wrong file" to detect. My locator addresses a durable record by **`run_id` (which names the single file) + `seq` (position within it)**; it never needs `segment_id` to resolve. **So `segment_id` is dead as locator evidence — I do not require it, remove it.** One flag, not a retention: if `generation_id` is retained (m-9/m-10's call), any *generation*-level distinction `segment_id`'s prefix carried is subsumed by `generation_id`; I consume `generation_id` if present but I do not require `segment_id` to carry it.

## `seq_hwm` — REQUIRED; it survives
`seq_hwm` = "that marker's highest admitted `seq`" (m-9 r17 §2:308), i.e. `last_seq` of the admitting round. In a single file `seq` remains monotonic, so `seq_hwm` still denotes the **committed-end of the valid prefix** — a meaningful, non-degenerate value. My E3 locators require exactly this, on two counts:
1. **To address the round's last durable record** — the durability/settlement legs resolve to `[first_seq … seq_hwm]`; the end bound must be directly readable.
2. **To bind the Route-2 direct-prefix comparison to the frozen interval, NOT the append tail** (the VP's LANE4-ESC1-VP4-F1 concern). Since frank keeps appending after it resumes, my actual-side locator must stop at a bound that is not "wherever the file happens to end at read time." `seq_hwm` from the durable content-ready receipt is the crash-boundary committed-end — the frozen-derived stop bound my Route-2 half relies on (see my Route-2 return). Without a directly-readable committed-end, the bounded-interval locator has no honest terminus.

`seq_hwm` is **derivable** from the marker preimage (m-9 r17 §2:308 proves an equal marker cannot carry a different `seq_hwm`), so a *derivation-only* trim of how it is produced costs my side nothing — but its **presence** is load-bearing: my locator must not have to reconstruct the whole `{seq, record_digest}` array to learn where committed content ends. So I require `seq_hwm` **present**, in its current **canonical-decimal-uint64 committed-end** form. That is the derivation-vs-shape split applied to my consumer need: release the derivation freely; keep the member.

## Scope / what this is not
A **locator-presence** ruling only. I do not author the successor receipt body (m-9), do not rule the four-property equality/`receipt_conflict` survival (m-10), do not remove or rename any member (amendment-shaped, Master+VP+operator), and move no byte. No finding that a to-be-removed member is secretly load-bearing beyond this: `segment_id`'s only consumers on my side are file-disambiguation, which one-file-per-run retires. Interaction noted for master's sequencing: if Route 3 (edited-session) makes a sanctioned edit rebase/supersede durable evidence, the identity my locator resolves (`seq_hwm` within the single file) must survive or be re-anchored by that operation — I flag the coupling per your Route-5 note; I do not design it here.

## Boundaries
Ratifies nothing, removes/renames no member, authors no amendment or receipt body, re-opens no join, changes no fixture or manifest, moves no owner or locked byte, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. `receipt_conflict` stays frozen. Interface lock `cbd1893c…`, §D-settlement amendment `1fa71cb8…`, stage-6 amendment `1125b0a0…`, m-9 delta `01b885fe…`, m-10 rev16 `3e3c5192…`, m-3 r24 `651c9aec…`, m-8 contract `4b670a79…` all UNMOVED. **H-12 hard-blocks external use.**

## Verification
- S-1 receipt body + the durable column split ("`{segment_id, seq_hwm}` (locator evidence for m-3's E3, ids/counters only)") + `seq_hwm`/`segment_id` encodings read at m-9 r17 bytes `01b885fe…` §2:308.
- `segment_id` = `"<generation_id>.<rotation_index>"` bound to `seg-<…>.log`, on every record for wrong-file detection: m-9 r17 §1.7:169/:171/:80.
- My evidence_locator common shape ("reference into the observer's recorder boundary"): r24 `651c9aec…` §2:102; the Route-2 frozen-interval binding stated in my companion `…-route2-oracle-m3` return this turn.
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this answer relay + one INDEX.md row + one README pointer line. No member removed/renamed, no receipt body/amendment authored, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master folds my locator line (`segment_id` removed, `seq_hwm` retained present in current form) into the additive supersession record alongside m-9's successor body and m-10's four-property confirmation. Amendment ratification, fresh lane-4 plan, lane-4 resume, fixture freeze, re-lock, T4 remain held. m-3.planner holds. r24 `651c9aec…` UNMOVED. H-12 stands.
