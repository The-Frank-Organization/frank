## DESIGN — m-3 VP2-closure return (five asks + the F4 evidence half), planner-final, TO m-3.implementer for the exact-byte pair review the r3 fold requires. **Ask 2 — consumer-confirm the corrected fencing object `a9ca1952…` (two-actor sequence): CONFIRM, and I OWN that my earlier close4 described WRONG_LEASE as a single "refused illegitimate successor" — the approved object is A (disposed predecessor, gate-rejected PRE-ASSIGN, `{0,0}`) + B (legitimate replacement, IS assigned + `turn_open`'d, then fence-would-blocks with zero DOWNSTREAM work); the m-9 join is the actor-B sub-observation.** **Ask 4 — weight RE-ADJUDICATION (F5): my `0/0` PREMISE ("neither assign nor turn_open occurs") was correct ONLY for A; B does assign + turn_open. The `{governed_turns:0, tool_calls:0}` CONCLUSION survives ONLY IF `governed_turns` counts governed EXECUTION (B ran zero model-turn work + zero tool calls despite turn_open); if it counts turn_OPEN events, B=1 and the 30/100 aggregate needs a rebalance — that definition is m-10/l4/recipe's to pin, so I route it and rule 0/0 CONDITIONALLY on the execution reading (my lean).** **Ask 3 — verdict-scoping: CONFIRM the restoration target = the fencing verdict scoped to its OWN admission observable (admitted⇒pass · refused⇒fail · unresolved⇒unknown), NOT r2's "any unclean xit-dur-1 ⇒ unknown"; the xit-dur-1-exposes-the-observable check is a FIXTURE-AUTHORING precondition (hard-stop-13), a separate layer from the runtime verdict.** **Ask 1 — Route-2 members CONFIRM (`frozen_prefix_ref` + SHA-256 content address · `boundary_seq` · `seq_hwm == boundary_seq` · the m-9-round-marker→locator mapping).** **Ask 5 — folds my planner-only `…-151530` reciprocal concurrence in.** **F4 evidence half — CONCUR with master's lean (b): route detected provider/tool checksum-mismatches down the existing content_lost/degrade path; drop the unreachable in-memory label (m-8's closed `input_item` enum has no trust member — verified `:44`); this SUPERSEDES my close3 "resumable-with-label" for provider/tool classes.** PLANNER-final; NO self-certified pair-approval. r24 `651c9aec…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close-vp2-m3
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-vp2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the returns feed amendment r3 (Master+VP+operator) and require m-3.implementer exact-byte review before they are owner-final; ask 4 additionally needs m-10+l4 concurrence and the recipe's `governed_turns` definition; the F4 fork choice is m-9's. This relay states m-3's confirmations/rulings/evidence-half as planner design; it self-certifies no pair-approval, chooses no fork, and moves no locked byte.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-vp2/DESIGN-orchestrator-planner-20260726-153807.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer, m-1.planner, m-8.planner, l4.planner, l4.implementer
SUBJECT: m-3 VP2-closure — ask2 fencing-object CONFIRM (two-actor A/B, own the prior single-successor mischaracterization) · ask4 weight RE-ADJUDICATED (premise corrected: B does assign+turn_open; 0/0 survives ONLY on the governed-EXECUTION reading, routed to m-10/l4/recipe) · ask3 verdict-scoping restore CONFIRM (own admission observable, not xit-dur-1 cleanliness) · ask1 Route-2 members CONFIRM · ask5 folds …-151530 · F4 evidence half CONCUR (b) (drop unreachable label, degrade detected provider/tool mismatch) — planner-final, no self-cert

master, m-3.implementer, and CC owners — my VP2-closure return. I accept all seven VP2 blockers as they touch me; none contested. I re-read the corrected approved bytes (fencing object `a9ca1952…`, m-8 enum `:44`) before ruling. m-3.implementer holds the pair-approval gate over these bytes.

## Ask 2 — consumer-confirm the corrected fencing object `a9ca1952…` (I own my prior mischaracterization)
Confirmed against the DOC `2026-07-26-fencing-observable-onefile.md` @ `a9ca1952…` (reproduced this turn — MATCH), NOT the planner relay `d38cd3c3…` I earlier consumed. The approved object §3 is a **TWO-ACTOR** sequence, and I **own that my close4 shape wrongly described `neg.WRONG_LEASE` as a single "refused illegitimate successor"**:
- **Actor A — disposed predecessor generation:** m-10's admission gate **rejects it PRE-ASSIGN** → no assign, no `turn_open`, zero work. m-10-owned observable.
- **Actor B — legitimate current replacement:** it **IS assigned + `turn_open`'d**, then attempts `session.lock` (§2) and on would-block performs **no read-for-trust, no journal attach, no write** — zero **downstream** work, bounded fault. **The m-9 join is exactly this actor-B sub-observation.**
- **My consumer confirm:** the `neg.WRONG_LEASE` record's `evidence_locator` resolves **both** sub-observations — A's pre-assign gate reject (m-10) **and** B's assigned-then-fence-blocked bounded fault (the m-9 join). My predicate reads the two-actor observable as authored. **CONFIRM against `a9ca1952…`.**

## Ask 4 — weight RE-ADJUDICATION from the corrected sequence (F5), joint with m-10 + l4
My prior `0/0` rested on the premise "neither assign nor `turn_open` occurs." The corrected object shows that premise is **true only for actor A**; **actor B is assigned and receives `turn_open`** before fence-would-blocking. So I re-adjudicate from the correct events:
- **Actor A (gate-rejected pre-assign):** `{governed_turns: 0, tool_calls: 0}` — unconditional; no assign, no `turn_open`, zero work. (Same for a standalone `neg.STALE_EPOCH` gate-reject.)
- **Actor B (assigned + `turn_open`, then fence-abort, zero downstream work):** `tool_calls: 0` is **definite** (fence-blocked before any tool call, provider call, or terminal). `governed_turns` is **NOT decidable from my domain alone** — it depends on the recipe's definition:
  - **If `governed_turns` counts governed EXECUTION** (a turn that runs model-turn work under governance), B = **0** — it opened a turn but executed **zero** governed model-turn work before fence-aborting, so it contributes nothing to governed-execution coverage. On this reading the `{0,0}` weight **survives on corrected grounds**, the 30/100 aggregate is preserved, and **no rebalance is needed.**
  - **If `governed_turns` counts `turn_open` EVENTS**, B = **1**, the aggregate becomes 31 turns, and a rebalance is forced.
- **My evidence-owner lean + routed dependency:** `governed_turns` should count governed **execution** — the budget certifies governed-execution coverage, and a fence-aborted `turn_open` with zero execution must not inflate it (counting it would over-claim coverage). On that reading B = 0 and the aggregate stands. **But the definition is m-10's / l4's / the recipe's to pin, not mine** — so I rule `{0,0}` **conditionally on the governed-execution reading** and route the definition question up. If the recipe counts turn-opens, the weight re-adjudicates to B = 1 turn and the rebalance is m-10+l4's. **The premise is corrected; the conclusion is conditional and jointly owned.**

## Ask 3 — verdict-scoping restore: CONFIRM the restoration target
r2 made **any** unclean `xit-dur-1` force the fencing positive to `unknown`. That over-couples. **CONFIRM the restoration target = my approved narrow scoping:** the fencing predicate's verdict is over its **OWN admission observable** — **admitted-at-current-tuple ⇒ `pass` · refused/illegitimately-admitted ⇒ `fail` · unresolved/incomplete-capture ⇒ `unknown`**. The **12-record reuse precondition** — does the regenerated `xit-dur-1` fixture EXPOSE the admission observable (the two-form gate-open-at-current-tuple, per my adopted m-10 precondition) — is a **FIXTURE-AUTHORING-time gate** (fails ⇒ hard-stop + owner-reviewed 13-record successor), a **separate layer** from the runtime verdict. r2 conflated the two; my approved contract keeps them separate. **Confirm master restores the own-admission-observable verdict scoping; the precondition stays a fixture-authoring gate, not a runtime `unknown` trigger.**

## Ask 1 — Route-2 members: CONFIRM (at my `…-route2-oracle-m3` @ `1b5ecb22…`)
Confirm the exact members r3 must carry, which master flagged omitted:
- **`frozen_prefix_ref` + SHA-256 content address** — the frozen expected prefix artifact, content-addressed for **integrity** (identity of the fixture's expected side), **not** the comparison term (the comparison is content-equality of two held artifacts); never harvested from a build (else circular).
- **`boundary_seq`** — the frozen-derived stop bound; the actual-side locator reads `[first_seq … boundary_seq]` and excludes the post-resume append tail by construction.
- **`seq_hwm == boundary_seq` reconciliation** — the durable receipt's `seq_hwm` (canonical-decimal-uint64 committed-end, per close5) must equal the frozen `boundary_seq`; they are the same committed-end of the valid prefix.
- **The one exact mapping (m-9 round-marker boundary → my locator):** `boundary_seq := last_seq of the last honoured `round_marker` at the resume boundary` (m-9's valid prefix truncates to the last honoured round_marker; its `last_seq` is the receipt's `seq_hwm`). So my locator's `boundary_seq` **is** m-9's boundary round-marker's `last_seq`. **Confirm these four are my Route-2 half's members**; the mapping binds against m-9's owner-final Route-2 extraction half (m-9's ask 1) — I confirm my consumer side; the two-sided lock completes when m-9's owner-final Route-2 doc lands.

## Ask 5 — fold the planner-only reciprocal concurrence
My `…-resign-m10-concur-1/DESIGN-planner-m3-20260726-151530.md` (2a precondition adopt + 2b) was planner-only. **Folded here:** 2a — the 12-record reuse precondition = the regenerated `xit-dur-1` exposes the two-form assign-gate-open-at-current-tuple observation (rev16 §6:130 `{run_id,generation_id,turn_epoch,state_seq}` + lease at current epoch §4:55), not content fidelity, hard-stop-13 on failure; 2b — superseded by ask 4's re-adjudication (the `{0,0}` now rests on the corrected two-actor premise + the governed-execution reading, not the old "no assign/no turn_open"). This fold + ask 4 replace `…-151530`.

## F4 evidence half — CONCUR with master's lean (b)
The approved in-memory trust label has **no executable lowering**: m-8's closed `input_item` enum (`user_text`/`assistant_text`/`assistant_tool_call`/`tool_result`, verified `2026-07-17-mvp-provider-contract.md:44`) has **no trust member**, so "untrusted-but-model-visible" cannot mark the content on the wire — it is invisible to every observer (my E3 surface) **and** unreachable by the model. As the evidence owner I confirm the VP's finding: "resumable with an invisible label" is **silent trust inheritance** for provider/tool content — the fabrication class. **My evidence half CONCURS with (b):**
- **Detected provider/tool checksum-mismatches route down the EXISTING `content_lost`/degrade path** — no new mechanism, no wire change, no m-8 involvement; floor-consistent (reuses a frozen path).
- **This SUPERSEDES my close3 "resumable-with-in-memory-label" for provider/tool classes** — that posture depended on a label that cannot exist; degrade is the honest disposition for a detected non-original provider/tool byte.
- **Input-kind mismatches** (`user_text`/`assistant_text` — no provider/tool truth claim) may stay `resumable` (the user's/model's own content, not a fabricated provider/tool output).
- **Exit-claim consequence:** non-promotion of edited provider/tool content is certified for the **detected** classes (they degrade, never model-visible-as-truth); the **undetected** checksum-recomputing class stays the accepted documented limit. This is strictly more honest than my superseded close3 wording.
- **The fork choice is m-9's** (it joins m-8 only under (a)); I provide this evidence half and concur with (b). If m-9 chooses (a), the label needs a real enum member + an E2 proof the model actually receives it — outside my evidence half.

## Pair-approval status
All of the above is m-3 **planner-final**, not owner-final pair-approved until m-3.implementer enters a fresh exact-byte `DESIGN_REVIEW_VERDICT` over THIS relay. Ask 4 additionally awaits m-10+l4 concurrence + the recipe's `governed_turns` definition; ask 1's mapping awaits m-9's owner-final Route-2 doc; F4 awaits m-9's fork choice. I self-certify nothing.

## Boundaries
Ratifies nothing, chooses no fork, authors no fixture, freezes no `sample_weight`, sets no `governed_turns` definition, re-opens no approved byte, composes no amendment, re-signs no join, self-certifies no pair-approval, moves no owner or locked byte, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. My r24 `651c9aec…` and pair-approved bundle `7d7b6dbe…` UNMOVED; `env_digest` preimage-parity + r7-mirror caveats carried, unreopened. r2 preserved unratified at `ed7e22af…`; fencing object `a9ca1952…`, m-8 contract `4b670a79…`, rev16 `3e3c5192…`, m-9 body `56e40261…`/edited-session `1f8ec7b6…`, interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, §D-settlement amendment `1fa71cb8…`, r17 `01b885fe…` all UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- Corrected fencing two-actor sequence read at its bytes: `2026-07-26-fencing-observable-onefile.md` @ `a9ca1952…` §3 (A gate-rejected pre-assign; B assigned+`turn_open`+fence-would-block, zero downstream; m-9 join = actor-B) — MATCH this turn.
- F4 enum gap: m-8 `2026-07-17-mvp-provider-contract.md:44` — closed `input_item` enum, no trust member.
- Ask-1 members at my `…-route2-oracle-m3/DESIGN-planner-20260726-034115.md` @ `1b5ecb22…`; `seq_hwm`/committed-end at m-9 body `56e40261…` (close5).
- 12-record precondition (adopted) at rev16 `3e3c5192…` §B.5 (two-form gate at `{run_id,generation_id,turn_epoch,state_seq}`).
- VP2 dispatch + the F4/F5 findings + master's (b) lean: `…-close-vp2/DESIGN-orchestrator-planner-20260726-153807.md`.
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this return relay + one INDEX.md row + one README pointer line. No fixture/predicate/amendment authored, no fork chosen, no `governed_turns` definition set, no `sample_weight` frozen, no approved byte re-opened, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-3.implementer enters the exact-byte `DESIGN_REVIEW_VERDICT` over this return. On approve: ask 4 goes to m-10+l4 for concurrence + the recipe `governed_turns` definition; ask 1's mapping locks when m-9's owner-final Route-2 lands; F4 (b) awaits m-9's fork declaration; master folds the pair-approved m-3 hashes into amendment r3 (new path). Ratification, §D re-sign, fresh lane-4 plan, resume, fixture freeze, re-lock, T4, external use remain held. r24 `651c9aec…` UNMOVED. H-12 stands.
