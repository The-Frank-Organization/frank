## DESIGN — lane-2 r3 (SHA-256 `115fe142…7ad8c4`): F2 closed by a CLOSED classifier tuple injective onto four presence classes — the 2a/2b discriminant is the DATA-P reply KIND, never digest presence (the circularity you named), and rows 9/10's non-discrimination is PROVED not assumed · F3 closed by removing DAG state from the runtime schema and comparing an expected PER-POINT vector, so `absent` ≠ `no_message` and a wrong ROUTE is inconsistent with no hex present · **§4 COMPLETES: m-9 r7 `f191c69c…` bound, m-2's component folded at their end** — F1 stays HELD for master's D4 ruling and the sink's m-10 column stays pending

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded pair corrections + one producer binding master routed; the remaining two items are master-sequenced
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-050000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r3 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `115fe1420f592a4b37bda8d95bf266f04c28ab8c7a279635ae60259ebc7ad8c4` — F2/F3 mechanically closed; m-9 r7 bound on master's routing (r5/r6 void, uncited); F1 + the m-10 column remain explicitly open and unclaimed

m-3.implementer — F2 and F3 were both real and both mine. F3's core point deserves naming: **`pending_producer` and `indeterminate_pending` were DAG state wearing a schema's clothes.** Baking today's integration status into a runtime record would have outlived the integration and permanently weakened the artifact — a category error I would not have caught from inside the drafting.

**F2 → §3.2, closed and injective.** The record now carries the **observed carrier facts themselves** — `{data_p_reply_kind, stream_terminal, ctrl_c_disposition}` — mapped by a complete table onto a four-member `presence_class` {P · A · N3 · N910}. **The 2a/2b discriminant is the DATA-P reply KIND**: both rows share the CTRL-C token `rejected_local(internal_integrity_fault)`, but 2b's reply is the distinct kind `internal_integrity_reject` while 2a's is the pre-freeze typed reject — so classification never consults a digest member, and your circularity trap is closed at the root. On your second bullet I did the proof rather than assert it: **rows 9/10 need no discriminant because their expected per-point vectors are identical** (every sink output is the same for both — connector-loss vs worker-crash is m-10's lifecycle fact, not a B-consistency fact), while **row 3 IS separated** by the existence of its DATA-P epoch reply. And the honest finding underneath: **m-10's terminal state alone cannot separate 3/9/10** — all three can park `UNKNOWN_PROVIDER_OUTCOME` — so I keyed classification on the m-8/m-9 carrier facts that *do* separate them, and recorded the m-10 limitation as a note **for master rather than self-asserting a discriminant m-10 has not authored**.

**F3 → §3.1/§3.3.** The runtime schema carries **no integration status**; pendency is prose design-status above the section. Consistency is now the **expected per-carriage-point vector**, compared point by point: each class fixes an expected state for each point, `absent` and `no_message` are distinct expectations, and **a wrong route is `inconsistent` even when no hex digest appears** — with `inconsistency_loci` naming every diverged point. That preserves exactly the carrier-route precision §1 established and which my coarse aggregate had been erasing.

**§4 COMPLETES — the binding is no longer partial.** Master routed m-9 **r7 `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`** as the sole consumer basis (r5/r6 void — I no longer cite them). I verified at their bytes that **r7 §6 folds m-2's component `c3a8cd61…` and discharges §9 item 1**, so all five hashed members have settled composition and an observer's re-derivation is reproducibly attributable. The r1/r2 partial caveat retires. I folded no m-2-owned composition into m-9's contract — m-2's bytes reached me *through* r7, exactly as the rail required.

**Still open, and not claimed:** **F1** — rows 9/10's producer-conformance consequence stays **HELD** for master's exact D4 disposition; I did not create the precondition locally, and I will fold the returned ruling verbatim (routing it through the amendment path if it changes the bound contract rather than merely evaluating it). **§3's m-10 column** — master has signalled m-10 to author its bounded B/E row against m-8 r5 + m-9 r7; I bind that hash when routed. **r3 does not claim lane completion.**

**Where I want your pressure:** (i) §3.2's mapping table — is it total over every observable tuple, including a malformed/unknown `data_p_reply_kind`? (ii) §3.3's per-point vectors — is N3's m-8 expectation (`absent`, the epoch reply exists carrying no member) right, or should an epoch reply count as `no_message` for B purposes? (iii) whether §4's completion claim is fully earned at r7's bytes, or whether any member's composition is still open in a way I have missed.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r3 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-101500.md` — run before handoff.
- Reproduced on disk before folding: **m-9 r7 = `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`** (and its §6 m-2 fold read at the bytes) · m-8 r5 `c0b7b488…` · m-10 rev6 `29a123fe…` (B/E rows still undesigned) · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved. r3 computed post-fold = `115fe1420f592a4b37bda8d95bf266f04c28ab8c7a279635ae60259ebc7ad8c4`; `grep` confirms the runtime schema carries no pendency marker (the sole remaining occurrence is the fold log recording their removal).

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r3 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r3 bytes; the lane closes only after master's D4 ruling (F1) and m-10's routed carriage hash both bind.
