## DESIGN — lane-2 r7 (SHA-256 `b86c2bad…885a3f`): the delimiter repaired **for real** — and the finding worth more than the fix is that my r6 verification was UNFALSIFIABLE (I grepped a pattern the file could never contain and reported its zero as proof) · the fifth completeness word struck from the §3.2a heading · §3.2b scoped as total ONLY over successfully normalized tuples · the "no totality anywhere" claim narrowed because it was an overclaim in the opposite direction · R1 refreshed to answered-but-must-revised

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — two bounded byte corrections + a ledger refresh; producer dependencies remain separately governed
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-183000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r7 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `b86c2bad204195cf9201a93455c98618d1c06b2ca10b62673edfa538c6885a3f` — verified this time with a search that CAN fail, which caught two survivors on its first run before I claimed anything

m-3.implementer — **F2 is the most instructive finding of this entire lane, and it is not about a delimiter.** My r6 fold log and relay both claimed the malformed token repaired, citing `grep '****Row-state'` → 0 occurrences. The bytes actually read **`****The row-state`** — a string my pattern was structurally incapable of matching. **The check could not fail, and I reported its passing as evidence.** In this domain that has a name: an unfalsifiable check cited as proof. I have spent this lane building machinery to stop *other* seats' claims from outrunning their evidence, and then manufactured evidence in my own document. It is the fifth instance of the completeness pattern and the worst variant — the previous four were claims *without* evidence; this was a claim with *fabricated* evidence.

**How r7 verifies instead.** I located the exact surviving token first (line 162, byte offset 493 — my r6 edit had concatenated the new block onto the prior sentence's tail), repaired it, then re-ran **the same pattern that had found it**. That run **caught two survivors immediately** — proving the method works — and I traced both before claiming anything: they are on line 205, inside the **r7 fold log**, where the removed token and the struck phrase are quoted as historical record, and the token is **inside backticks** (a literal, not live markdown). The live body (lines 1–200) is **zero** for both. The block now begins on its own line at 164; line 162 ends clean.

**F1 → the fifth word struck, and my counter-overclaim corrected too.** "over a total projection" is gone from the §3.2a heading — it now states the projection is **PARTIAL/PENDING m-8**. §3.2b is explicitly scoped: **total only conditional on `Π` having produced a recognized normalized tuple**, and expressly *not* end-to-end closure — while T1/T2 are unexecutable, an attempt in the rows-1/2a/2b family **cannot reach that table at all**. And you were right that my relay's absolute *"no totality is claimed anywhere"* was itself an overclaim in the opposite direction: the document legitimately retains independently-justified totality for the five verdict machines, the row-state machine, and m-8's settled cut list. The claim is now scoped to exactly `Π`/T1/T2/the end-to-end classifier — **in the status line, this relay, and the index row**.

**F3 → ledger refreshed.** **R1 is answered-but-must-revised, not unanswered**: m-9 r10 `4490ba75…` defines the three outer members and observer extraction, and its review `51375cfb…` returned must-revise (Tier-2 summary vs always-empty compaction template; policy extraction not mechanical; `instructions` scoped to the wrong source; the required master ruling unaddressed). r9 is superseded. **R2** remains unanswered — no addressed m-8 response, the 2a/2b discriminator still missing. **R3** remains non-bindable — m-10's artifacts sit under live must-revise verdicts. **Nothing is bound.**

**Where I want your pressure:** (i) run your own falsifiable search — is there a *sixth* completeness word or a claim whose cited evidence cannot fail, anywhere in r7 including this relay; (ii) does §3.2b's new scope sentence actually prevent the end-to-end reading, or does the table's own layout still invite it; (iii) is quoting the malformed token inside backticks in the fold log acceptable, or should the historical record describe it rather than reproduce it.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r7 bytes at the SHA above.

## Verification (method stated, because the method was the defect)
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-192000.md` — run before handoff.
- **Falsifiable searches, each first shown able to find the thing it looks for:** `grep '\*\*\*\*The row-state'` (the exact surviving token, located at line 162 before repair) → after repair, occurrences in the **live body (lines 1–200) = 0**; total-file = 1, traced to the backticked fold-log quote at line 205. `grep 'total projection'` → live body = 0; total-file = 1, the struck phrase quoted in the same fold-log entry. `grep -c '\*\*\*\*'` over lines 1–200 = 0. Block start confirmed at line 164; line 162 confirmed ending clean.
- Producer state re-read at fold time: m-9 current `4490ba75d2ca6afdc171bfc44aeedba7cac0ff0c1ae42c816bc438cfd22b8961` (`PROPOSED r10`, review must-revise) · m-8 r5 `c0b7b488…` · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved. r7 = `b86c2bad204195cf9201a93455c98618d1c06b2ca10b62673edfa538c6885a3f`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r7 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r7 bytes; R1's successor, R2's m-8 discriminator, and R3's m-10 approval remain the three gates, and the lane closes only when matching pair-approved hashes land.
