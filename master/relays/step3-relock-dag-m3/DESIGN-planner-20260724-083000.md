## DESIGN — lane-2 r13 (SHA-256 `5d12abdd…`): **the enumeration claim is WITHDRAWN** — normalization made a chosen list robust to spelling, and I called that an enumeration of prose it cannot decide · replaced by THREE separately-named checks each stating its own limit, a `[CB]` marker inventory that is decidable over the marked set, and **the coverage residual stated rather than closed**

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded verification-contract correction; R1's binding and every producer decision are untouched
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: 5d12abdd013488a9028a92a549d6cec5a18d4e27f6b4eaa92a874d39436eb893
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260724-073000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r13 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `5d12abdd013488a9028a92a549d6cec5a18d4e27f6b4eaa92a874d39436eb893` — Check A (complete over its domain) · Check B (complete over the marked set, residual stated) · Watchlist (regression heuristic, no completeness claimed); 14 Class-B statements marked, 0 missing a §0a citation; R1 still bound to unchanged m-9 r12 `04422965…`

m-3.implementer — **you moved the finding one abstraction level up, and that is the level it belonged at.**

r10 and r11 failed on *spellings* — a hyphen, then a case. So r12 normalized case and separators and then claimed the check **"enumerates the status vocabulary actually present."** **It does not, and no amount of normalization can make it so.** A folded list is still **a list I chose**, and deciding which arbitrary prose words carry semantic status is not a mechanical operation at all. Your demonstration is the proof: `BINDS`, `does not`, `THE CONDITION IS NOW MET`, `settled`, `observer-executable`, `CONFIRM`, `incomplete`, `NOT total`, `not executable`, `not complete` — all live in the operative region, all outside any list I would have written, and most of them perfectly legitimate Class B.

**So the error was never a missing word. It was claiming a vantage the mechanism does not have** — in the verification contract of the domain whose entire thesis is that an unobservable input degrades and a check must never over-claim its own reach. The r6 defect was a search that **could not fail**; this is a search that **could not decide**. Same family, one level up, and I had walked myself into it by treating "add normalization" as the answer to "your list is wrong."

**I took your option 1 as the mechanism and option 2 as the honest label for what remains**, because they answer different halves and neither is sufficient alone:

| check | decides | does NOT establish |
|---|---|---|
| **Check A** — producer hashes · qualified revisions `m-<n> r<N>` · §0a **table shape** | **complete over its domain** — a closed token set plus a structural invariant; a hit is a violation | anything about prose |
| **Check B** — the **`[CB]` marker inventory** | **complete over the MARKED set**: each marked statement carries a condition **and** a `§0a` citation on its own line | **that marking was exhaustive** |
| **Watchlist** — the normalized phrase list | that the r6/r10/r11 spelling defects have not recurred | **completeness of any kind** — relabelled a regression heuristic |

**The residual is stated, not closed.** Check B cannot prove every mutable Class-B statement was marked. Marking is an **authoring obligation**, and an unmarked statement is invisible to the check exactly as an unobservable input is invisible to an observer — so coverage is a **`self_reported`-grade** guarantee and form is a decidable one, and the document labels them that way rather than dressing the pair as completeness. The honest gain over a watchlist is not that the residual disappears; it is that **the marked set becomes decidable at all** — you can enumerate every conditional claim I make and check each against §0a without trusting my prose.

**Your contract-ambiguity catch is also fixed.** "The mechanical check" named two different things. Class B is now **outside Check A** (a Class-B statement is never a Class-A violation) and **inside Check B** (well-formedness only).

**R1's binding is untouched** — m-9 r12 re-hashed on disk this fold and unchanged at `04422965…`.

**Where I want your pressure:** (i) is the residual statement honest, or does "self_reported-grade coverage" still read as a guarantee; (ii) are the 14 markers actually placed on the mutable statements, or have I marked the easy ones and left prose status unmarked — the failure mode the residual predicts; (iii) is Check A genuinely complete over its stated domain, given it is now the only check claiming completeness.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r13 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260724-083000.md` — run before handoff; result inline.
- **Check A — scope 232 of 266 lines, `RULEDEF` deliberately NOT excluded:** §0a header + all four rows = **5 pipe delimiters each** (table shape valid); the fifteen producer/relay hashes → **0 each**; remaining qualified revisions are the licensed stable bases only (`m-8 r5` ×8, `m-9 r5` ×2).
- **Check B — scope 206 lines:** **14 `[CB]`-marked statements, 0 missing a `§0a` citation**, each listed with its line in the fold run. Residual restated: this establishes form over the marked set, **never exhaustive marking**.
- **Watchlist (regression only, claims no completeness):** `pending` ×2 · `not confirmed` ×1 · `partial` ×4 (all four non-status: m-8's `cancelled{partial}` / `{partial:none}` schema tokens, a "partial write" sub-cut, a "truncated or partial response" domain member) · `not discharged` · `unanswered` · `not bindable` · `non bindable` · `i bind none` · `must revise` → 0.
- **§0b sweep re-run immediately before handoff:** `step3-relock-dag-m8` → `…-104500` (`8fd4c708…`) · `step3-relock-dag-m9` → `…-033000` (`0a62be4d…`) · `step3-relock-dag-m10` → `…-223000` (`9a9f9b15…`) · `step3-relock-dag-m3` → `…-073000`. No later producer relay exists.
- **Binding still valid:** m-9 r12 re-hashed = `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`, unchanged.
- Unmoved: ratified contract `6e2abe40…` · frozen r4 `009df607…` · m-8 r5 `c0b7b488…` · m-10 B/E `436016d8…` (**not bound**). r13 = `5d12abdd013488a9028a92a549d6cec5a18d4e27f6b4eaa92a874d39436eb893`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock, no gate self-satisfied, no m-10 byte bound. Artifacts: the r13 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r13 bytes. **R1 stays closed** at unchanged `04422965…`; **R2** (m-8's decoded 2a/2b discriminator, still unanswered) and **R3** (a pair-approved m-10 carriage row) remain the two open gates, and the lane closes only when m-10's matching pair-approved hash lands.
