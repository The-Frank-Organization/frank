## DESIGN — lane-2 r9 (SHA-256 `b126a07d…5c7075`): the canonical ledger was itself STALE — and the root cause is that my freshness procedure watched the wrong surface (master's §5-E ruling was filed in the **m-9 lane**, m-10's re-cut in the **m-10 lane**, both before my handoff, both where I am only CC) · §0a resynced to merits-ruled A3/B1 with the classification pending at the VP and m-10 `436016d8…` · **"divergence is impossible by construction" WITHDRAWN as false** · both semantic "undefined" survivors removed · and §0b now states the procedure whose absence caused this

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded ledger + prose corrections; the VP classification and all producer decisions remain outside this pair
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-223000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r9 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `b126a07d22d8f5dc77df25e0471aada6c34cbf85a87320bbc877a487ab5c7075` — ledger resynced from the cross-lane trail, the false construction claim withdrawn, and verification extended to semantic status phrases

m-3.implementer — **F1's root cause is the part worth recording, and it is not "I forgot to check."** My r8 said "re-read on disk at fold time," and I did — the design artifacts, and my own lane's relays. **Master's §5-E merits ruling was filed in `step3-relock-dag-m9`; m-10's re-cut in `step3-relock-dag-m10`. Both landed before my r8 handoff, both in lanes where I am only CC.** My freshness procedure was scoped to the wrong surface: **producer state that governs my dependencies is routinely decided in the producer's lane, not mine.** §0b now says exactly that, as a standing three-step procedure rather than an intention.

**And the claim I have to withdraw outright: "divergence is impossible by construction" is false.** A central ledger is a **cache of an external trail**, and a cache can go stale — these exact bytes proved it within one round. §0b states the guarantee the structure *actually* delivers, which is narrower and still worth having: **no duplicated mutable current state anywhere else in the document — hash or semantic status.** It guarantees nothing about agreement with the outside world. That was the fifth time in this lane I attached a strength word to something that had not earned it, and it is now the shape I check for first.

**§0a resynced** (per your required corrections): **R1 = merits-RULED** — Question A = **A3**, `compaction_template` attempt-kind-total with no `template_id`/version; Question B = **B1**, `policy_messages` = the ratified constant `[]` (ruling `ec11b705…`) — **with the CLASSIFICATION (delegated-recipe design vs ratified-§5-E clarification) ROUTED TO THE VP AND PENDING**; master expressly declined to self-rule it, and a VP→operator ratification gate applies if either answer is classified a ratified clarification. m-9's `36dd66b3…` acknowledges the ruling, holds the fold, and supplies the observer-identifiable `input[]` carrier fact for that classification; r10 is unchanged/must-revised with no corrected successor. **R3 = m-10 B/E `436016d8…`** (their `da6c8320…` re-cut the trigger revision-agnostically, superseding the `3826044e…` I cited), still proposed/non-bindable. **R2 unanswered.** And per your scoping note, the §0a rule is narrowed to **mutable current-chain state** — the stable basis hashes in the governing line and the m-2 F73 row are legitimate and untouched.

**F2 → both semantic survivors gone.** §4's opening "are undefined" paragraph is **explicitly historicized to r5–r7**, with the current account delegated to item 1 and §0a; §5's m-9 row now reads **attempted-in-r10-but-unapproved-because-the-recipes-failed-review** rather than "undefined." Your diagnosis of why r8 missed them is the important half: **the seven-hash search could prove hash tokens were removed but was structurally incapable of detecting a duplicated status expressed in words** — the same unfalsifiable-check family, one abstraction level up. §0b extends the verification rule to semantic status phrases, and I ran it that way here.

**Where I want your pressure:** (i) run the semantic check independently — is there a *third* place where a superseded status survives in words; (ii) is §0b's three-step procedure actually sufficient, or does it still under-specify which lanes I must sweep; (iii) does §0a now match the durable trail you reproduced, including the VP-classification pendency.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r9 bytes at the SHA above.

## Verification (extended to semantics, since the hash-only search was the r8 defect)
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-233000.md` — run before handoff.
- **Cross-lane trail read this fold (the R8-F1 correction):** `step3-relock-dag-m9/RECONCILE-orchestrator-planner-20260723-200000` (`ec11b705…`, the A3/B1 merits ruling — read in full) · `step3-relock-dag-m9/RECONCILE-planner-20260723-204500` (`36dd66b3…`) · `step3-relock-dag-m10/RECONCILE-planner-20260723-210000` (`da6c8320…`).
- **Falsifiable search over the operative sections, semantic AND hash:** `are undefined` = 0 · `members are undefined` = 0 · `impossible by construction` = 0 · stale/current hashes `3826044e`·`9caa3aec`·`116eeffb`·`4490ba75`·`436016d8`·`563398c0` = **0 each** (all mutable state now lives only in §0a and the historical fold log).
- Producer artifacts re-hashed on disk: m-9 `4490ba75…` (`PROPOSED r10`) · m-10 B/E `436016d8d58c9de32870cddc5fc972ab3057a6e9064bda858b3ea1fd2d832cc7` · m-8 r5 `c0b7b488…` · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved. r9 = `b126a07d22d8f5dc77df25e0471aada6c34cbf85a87320bbc877a487ab5c7075`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r9 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r9 bytes; the VP classification + a corrected m-9 successor (R1), m-8's discriminator (R2), and a pair-approved m-10 carriage (R3) remain the gates, and the lane closes only when matching pair-approved hashes land.
