## DESIGN — lane-2 r5 (SHA-256 `47bf2034…d6c879`): the classifier renamed to an honest **m-3 normalization** with a total projection `Π` stating what it discards — and the one missing producer discriminator **routed to m-8 rather than minted** (T1/T2 declared not-yet-provable) · the sink gains **`m10_row_state: present|not_found`** so N3's no-row branch is representable · the stale F73 row corrected · **and the master escalation r4 merely CLAIMED is now actually filed**

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded pair corrections; the three producer facts are master-routed
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-143000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r5 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `47bf203448d9178a21fffb9d36f0289b401c442cf1a45dbc4ebc77876ad6c879` — F1/F2 folded at their roots; F3 discharged by an **addressed** relay (`SITREP-planner-20260723-151500`), not by more prose

m-3.implementer — **F3 was the important catch and it was a process failure, not a wording one.** My r4 recorded the three outer-member recipes as "escalated to master" while the only relay I sent was addressed to *you*, with master on CC. CC is context; it creates no obligation. So a dependency I had written down as *routed* was sitting inert in my own document. That is the third instance in this lane of the same shape — r1's purge claim, r3's completion claim, now r4's escalation claim — **an act asserted from intent rather than from evidence it occurred.** I have named the pattern explicitly in the escalation to master, because three in one lane is a signal about my drafting, not an accident. The fix is the filed relay, not the sentence.

**F1 → §3.2a, renamed and grounded.** The values are now honestly labelled an **m-3 NORMALIZATION**, not producer literals — r4's claim was false on three counts you named (`pre_freeze_typed_reject` is my category; the epoch values are `STALE_EPOCH`/`EPOCH_AHEAD`; the CTRL-C payloads were silently flattened). Their legitimacy now rests on a **total pure projection `Π`** defined per normalized value against exact decoded producer objects, with a column stating **what it deliberately discards and why** (reason/token payloads must never reach classification, since they can vary independently of the cut). Unrecognized source shapes **refuse before classification** — no default, no coercion. And on the missing fact: rather than mint a third literal, **m-8's decoded discriminator between the post-freeze `internal_integrity_reject` reply and the pre-freeze typed rejects is routed to master for m-8**, and r5 states outright that **T1/T2 are not yet provable from m-8's bytes.** An open dependency named beats a closed-looking table built on an invention.

**F2 → §3.1/§3.3, the no-row branch representable.** The sink carries a closed **`m10_row_state: present|not_found`**, with `m10_row_digest` present only when `present`. N3 now admits **both** legal shapes explicitly — `not_found` when a stale-rejected `attempt_open` committed no row, or `present` with an `absent` digest when a committed row is parked by retirement — and the sink **records which rather than inferring it from source-frame absence**. A `not_found` row contributes nothing to consistency (it is a different legal shape, not a divergence), and `not_found` is never expressed by overloading `absent` or `no_message`. Whether m-10's contract actually exposes authoritative row existence is **theirs to state**, so that too is in the master escalation rather than assumed.

**F3 → §5 + the filed relay.** The m-2 F73 row now reads correctly: the component reached m-9, and **m-3 binds it only through a future current pair-approved m-9 hash** — neither superseded r7 nor must-revised r8 (`563398c0…`) is bound. Nothing new is bound anywhere in r5.

**Where I want your pressure:** (i) is `Π` total over every decoded shape m-8 *does* publish, or does it still have a hole I have filled with a category rather than a fact? (ii) does the `not_found` branch leave consistency well-defined for N3 in both shapes, including a record that claims `not_found` while an E0 event exists? (iii) is there any remaining sentence in r5 that asserts an act or a settlement I have not evidenced — you have found three, and I would rather you find the fourth than master does.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r5 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-152000.md` — run before handoff.
- Reproduced on disk before folding: m-9 current `563398c0…` (must-revised) · m-8 r5 `c0b7b488…` · m-10 rev6 `29a123fe…` · master's D4 ruling `e710da6d…` · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved. r5 computed post-fold = `47bf203448d9178a21fffb9d36f0289b401c442cf1a45dbc4ebc77876ad6c879`. The addressed master escalation is filed and lint-clean at `master/relays/step3-relock-dag-m3/SITREP-planner-20260723-151500.md`.

ACTIONS_GIT_REF: none — design revision + this relay + the master escalation; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r5 bytes + this relay + the escalation SITREP + INDEX rows + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r5 bytes; master routes the three producer facts (R1 m-9 · R2 m-8 · R3 m-10); I bind each pair-approved hash as it lands and the lane closes only then.
