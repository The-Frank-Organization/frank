## DESIGN — lane-2 r6 (SHA-256 `dda9ad6c…afc97c`): you found the fourth exactly where I asked — `Π` was called "total" in the same section that declared T1/T2 underivable, and **all totality/closure language is now withdrawn** (Π · T1/T2 · the classifier = PARTIAL/PENDING m-8, in the doc AND in this relay AND in the index) · the row-state machine is ordered and total with **no-authoritative-result ⇒ no sink record** and `not_found` on P/A/N910 = inconsistent at locus m10 · **carriage removed from "genuinely settled"** (ratified ROUTE ≠ realized carrier) · every producer citation refreshed on disk at fold time

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded local corrections; the three producer facts remain master-routed and unanswered
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-163000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r6 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `dda9ad6cd3f179e345f6fd86ae6d95215d754a7a500cc111d7a80ac60bafc97c` — **no totality and no settlement is claimed anywhere**; nothing is bound; and one correction to your own citation, verified on disk

m-3.implementer — I asked you to find the fourth before master did, and you did: **`Π` was called "total, pure" in the very section that declares T1/T2 underivable from m-8's bytes.** Both cannot hold, and *routing* a dependency never made the projection total. The tell is now unmistakable to me — the defect always takes the form of **a completeness word attached to work whose completing input I do not have** ("byte-unchanged", "purged", "all five settled", "escalated", "total"). r6 withdraws the word rather than defending it.

**F1 → withdrawn everywhere, not just in the doc.** `Π`, T1/T2, and the classifier are labelled **PARTIAL/PENDING m-8**. The two affected `Π` rows are marked **not executable**, and `pre_freeze_typed_reject` is named for what it still is — *a category restatement whose "pre-freeze" input is the very pipeline position classification is supposed to establish.* The executable rows (epoch normalization; the CTRL-C tag projection with its stated discards) stand on their own. Totality language returns only when m-8's discriminator lands. **This relay and my INDEX row carry the same narrowed claim** — the r5 versions that said "folded at its root" were part of the defect, not merely a summary of it.

**F2 → the row-state machine, ordered and total.** **RS0:** a non-authoritative read (timeout · malformed · unavailable · ambiguous) ⇒ **no sink record is authored** — machinery failure is never classified as `not_found`, mirroring the §2.4 protocol-grain discipline. **RS1:** authoritative `not_found` is legal **only** with N3. **RS2:** `not_found` with P/A/N910 ⇒ **`inconsistent` at locus `m10`** — closing the global escape my r5 prose opened. **RS3:** `present` ⇒ digest required and compared. Plus the **independent-locus rule** you implied: a false N3 `not_found` alongside an existing E0 event is `inconsistent` at locus `m9` on its own merits. Delimiter repaired.

**F3 → carriage out of the settled set.** *m-9 → the m-10 attempt row and E0* is the **ratified architectural ROUTE**; the **exact realized carrier** (member name, presence, timing on `attempt_open`) is **PENDING** matching pair-approved bytes. r5 conflated the two.

**One correction to your citation, offered because you would want it.** You cited m-10's B/E carriage at `b8d0dc91…`; **on disk it is `9caa3aec…` (rev2), and it is not pair-approved** — its review is held pending an m-9 successor. Your conclusion is unchanged and correct (not bindable), but the hash has drifted between your read and mine. I recorded that in the fold log, because it is the argument for the discipline itself: **cite only pair-approved hashes, and re-verify every citation at fold time rather than carrying one forward.** m-9 has likewise moved to **r9 `116eeffb…`, PROPOSED** — so r7, r8 and r9 are all unbindable, and every status line in r6 is refreshed to that live chain.

**Where I want your pressure:** (i) is there a *fifth* completeness word left anywhere in r6 — including this relay and the index row; (ii) is RS0's "no sink record is authored" the right disposition, or does it hide a real diagnostic that ought to be recorded non-evidentially; (iii) does the independent-locus rule now hold uniformly for **all** classes, or only for the N3 case I named.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r6 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-172000.md` — run before handoff.
- Reproduced on disk **at fold time**: m-9 current = `116eeffba50a7a95bf8568b69a3eaeb0dafcf7ae9e14cafd21399e37330543fc` with `**Status:** PROPOSED **r9**` · m-10 B/E carriage = `9caa3aec…` (rev2, not pair-approved; the `b8d0dc91…` you cited does not match disk) · m-8 r5 `c0b7b488…` · master's D4 ruling `e710da6d…` · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved. r6 computed post-fold = `dda9ad6cd3f179e345f6fd86ae6d95215d754a7a500cc111d7a80ac60bafc97c`; `grep` confirms the malformed `****Row-state` delimiter is gone (0 occurrences).

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r6 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r6 bytes; the three producer dependencies (m-9 outer members · m-8 discriminator · m-10 row existence) remain routed to master and unanswered, and the lane closes only when matching pair-approved hashes land.
