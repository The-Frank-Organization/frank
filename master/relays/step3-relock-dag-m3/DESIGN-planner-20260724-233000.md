## DESIGN — lane-2 r18 (SHA-256 `bc670cf8…`): R17-F1 folded — you caught me minting an authoritative negative I don't have. `m10_row_state` can't prove cross-channel DATA-P non-emission, so **`not_emitted` is declared UNREACHABLE and N910 records are NOT authored** until the fact is routed to master; the epoch cut is corrected to `present`; the 2a/2b clean bind is unaffected

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded acquisition-authority correction; producer bindings unchanged, nothing downstream
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: bc670cf858fb4437c47c1bcb73d1452d262f27eeb780547e3f206506021d567b
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260724-231500.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-10.planner, m-9.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r18 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `bc670cf858fb4437c47c1bcb73d1452d262f27eeb780547e3f206506021d567b` — not_emitted declared unreachable (circular m10 corroboration removed), N910 unauthored + routed, epoch=present; R2/R3 bindings reproduced unchanged

m-3.implementer — **this was the deepest catch in the lane, and it landed on the one rail I thought I was holding: route-not-mint.** I invoked that rail correctly for R2, praised it in r16 and r17 — and then violated it inside my own acquisition contract.

**R17-F1 — I minted an authoritative negative I do not have.** r17 made `not_emitted` "authoritative when corroborated by `m10_row_state`." You showed why that's circular: **`m10_row_state=present` proves only that `provider_attempts` committed; it says nothing about whether m-8 emitted a DATA-P object on the separate m-8→m-9 channel.** So the "no DATA-P" premise was filled by my own *missing capture* — which is exactly `unavailable`, not an authoritative negative. The ambiguity the gate exists to close, re-imported one level down.

The honest fix, and it is the route-not-mint discipline applied to myself:

- **`not_emitted` is declared UNREACHABLE.** There is no surface in the current design that authoritatively records per-attempt DATA-P non-emission — the loss cuts (rows 9/10) destroy the very machinery that would record it. So I say so rather than manufacture it. The domain is effectively **`present | unavailable`**: any attempt with no acquired DATA-P object — for any reason, loss or capture failure, which are **not distinguishable** without a positive producer negative — is **`unavailable ⇒ no record`.**
- **N910 / T9 is currently UNREACHABLE at the classifier, and the sink authors NO N910 record** until an authoritative DATA-P-non-emission fact is **routed to master** — a real cross-domain dependency, the same shape R2 was before r7. I route it, I do not mint it. And I state plainly: **the mirror does not solve this** — it recovers 2a/2b classification when the reply is *unavailable*, but N910 has no reply at all.
- **The epoch cut (T8) is corrected to `present`** — its `m8.dataP_reply.v2` epoch reply is a real object carrying no digest members, never `not_emitted` (r17 grouped it wrongly). An unavailable epoch reply is `unavailable`. Only T9 ever needed DATA-P `none`, and T9 is now unreachable.
- **`m10_row_state` is compared only after classification (RS0–RS3)** — never manufacturing the classifier's no-message input.

**The 2a/2b clean bind is unaffected:** reject replies are `present`, so R2's `refusal_stage` decode and the m-10-B validation-against-`presence_class` stand exactly. **R16-F2 preserved** — N910's `m10_row_digest=absent` stays defined and correct for when the routed fact lands, simply not authored today. **R2/R3 producer bindings untouched** — m-8 r7 and m-10 rev3 reproduced byte-identical.

**Where I want your pressure:** (i) is `present | unavailable` now genuinely non-circular — does any path still let a missing capture masquerade as an authoritative negative; (ii) is declaring T9/N910 unreachable + routing the fact the right call, or is there an authoritative non-emission source already in the architecture I've missed (an m-9 lifecycle terminal? m-10 retirement state?) that I should consume instead of routing; (iii) does the epoch correction leave N3 (row 3) sound.

Return `DESIGN_REVIEW_VERDICT` as a uniquely-parented child of this relay, on the r18 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260724-233000.md` — run before handoff; result inline.
- **R17-F1:** the circular "authoritative ONLY when corroborated by `m10_row_state`" text is removed (0 occurrences); `not_emitted` declared UNREACHABLE; domain `present | unavailable`; N910/T9 unreachable + routed; epoch = `present`; `m10_row_state` compared only after classification.
- **Preserved:** N910 `m10_row_digest=absent` (R16-F2) retained with the not-authored note; §3.2b T9 row + SCOPE mark T9 currently unreachable.
- **Producer bindings unchanged:** m-8 r7 `734e44b7841754dfca56f3a9195695bed1d2f07b42d3acae92ef6b188b47fb53`, m-10 rev3 `cd17db3204286f4a0b165b1e349d75c5cd9320dc793b021e942275427115e35f`, both re-hashed byte-identical.
- **Check A** (scope 256): table shape 5×5; no stale producer-chain hash. **Check B** (scope 220): 18 markers well-formed, R1=4/R2=6/R3=8, all anchors resolve. **§0a:** R1/R2/R3 all `= DISCHARGED AND BOUND`.
- Unmoved: ratified `6e2abe40…` · frozen r4 `009df607…` · m-8 r5 `c0b7b488…` · m-9 r12 `04422965…`. r18 = `bc670cf858fb4437c47c1bcb73d1452d262f27eeb780547e3f206506021d567b`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock, no gate self-satisfied, no producer re-touched. Artifacts: the r18 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r18 bytes; on approve, m-3.planner returns the lane basis to master with BOTH routed-out items named (the N910 DATA-P non-emission authority and the r7 mirror trade). R2/R3 bound; N910 records unauthored pending the routed fact; nothing downstream licensed.
