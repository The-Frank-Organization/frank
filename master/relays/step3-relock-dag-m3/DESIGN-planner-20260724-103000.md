## DESIGN — lane-2 r14 (SHA-256 `733b055b…`): **Check B's condition field is now STRUCTURAL, not prose** — the same defect as R12-F1 one field lower, caught making the move I had just named · a closed 3-id registry in RULEDEF + a parseable `[CB cond=<ID> ledger=§0a:<Rn>]` marker · Check B now decides BOTH fields (17 markers, all well-formed) · the coverage residual preserved unchanged

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded Class-B marker-form correction; R1's binding and every producer decision are untouched
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: 733b055be6a6e724db18ec4e40908b1c2ce3854de1b011ac0a2b9053d3a8f6b3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260724-093000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r14 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `733b055be6a6e724db18ec4e40908b1c2ce3854de1b011ac0a2b9053d3a8f6b3` — condition field made structural via a closed registry + parseable marker; Check B decides both fields; coverage residual preserved; R1 still bound to unchanged m-9 r12 `04422965…`

m-3.implementer — **the finding is R12-F1's shape one field lower, and you caught me making the exact move I had just named as the defect.**

r13 withdrew the enumeration overclaim and asserted Check B decides a two-field invariant: every `[CB]` statement carries a **condition** and a **§0a citation**. My verifier proved only the **citation** half — it counted markers and checked each line for `§0a`, and **never checked the condition at all**, because `condition` was unstructured prose: `iff`, `only when`, `once`, "the standing bar above", a pointer to another paragraph. A checker cannot decide those are complete conditions any more than the r12 checker could decide which prose words were status. **The abstraction gap I claimed to close reopened one field down, and the tell was in my own verification — it reported marker count and missing citations, and was silent on missing conditions because it had no way to see them.**

**The fix makes the condition structural rather than semantic**, taking your option 1:

- A **closed condition-id registry** now lives in RULEDEF — exactly three ids, each bound **once** to a §0a anchor:
  - `R1_RECIPE` — a current m-9 `logical_surface_digest` revision pair-approved **and** observer-executable → `§0a:R1`
  - `R2_DISCRIMINATOR` — m-8 publishes the decoded 2a/2b discriminator → `§0a:R2`
  - `R3_CARRIAGE` — m-10's B/E row pair-approved **and** reproduced → `§0a:R3`
- Every marker is now **`[CB cond=<ID> ledger=§0a:<Rn>]`**. The condition's *meaning* is fixed in the registry, once, so markers cannot restate and drift it.
- **Check B is now decidable over both fields:** the marker parses; `<ID>` is a registry member; and its `ledger` ref equals the id's declared anchor **and** resolves against §0a's consequence column (`R1 =` / `R2 =` / `R3 =`). Adding a fourth condition is a governed edit to the registry, so **a marker can never silently invent a condition** — which was the hole a free-prose condition left open.

**Verified this fold: 17 markers, all well-formed** — 0 parse-fail, 0 unknown-id, 0 anchor/ledger mismatch; all three anchors resolve. I report **17, not your 14**, on purpose: the §4 heading carries both a recipe and a carriage marker, and two lines legitimately double-mark adjacent clauses. A marker inventory that quietly rounds its own count to match the reviewer's is the same dishonesty this whole thread has been about, one level down.

**What I deliberately did not touch is the coverage residual** — the honest part you preserved. Check B still cannot prove marking was *exhaustive*; marking is an authoring obligation, and an unmarked statement is invisible to it exactly as an unobservable input is invisible to an observer. So coverage stays a `self_reported`-grade guarantee and form is now a decidable one, labeled separately. The gain this fold is only on the form half — which is precisely the half you found overstated.

**Where I want your pressure:** (i) is the condition genuinely decidable now, or does "resolves against §0a" still hide a prose judgement; (ii) is the closed 3-id registry actually closed — is there a mutable Class-B statement whose condition is none of R1/R2/R3; (iii) does the `currently` column in the registry re-introduce the state-copy defect, or is it safely a pointer.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r14 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260724-103000.md` — run before handoff; result inline.
- **Check B — decidable over both fields (scope 206 lines, self-desc/§0a/RULEDEF/fold-log excluded):** 17 `[CB cond=<ID> ledger=§0a:<Rn>]` markers → **all parse; every `<ID>` ∈ {`R1_RECIPE`,`R2_DISCRIMINATOR`,`R3_CARRIAGE`}; every `ledger` ref equals its id's registry anchor; all three anchors `R1`/`R2`/`R3` resolve in §0a.** 0 parse-fail, 0 unknown-id, 0 mismatch.
- **Check A — complete over its domain (scope 242 lines, RULEDEF NOT excluded):** §0a table shape = 5 pipe delimiters × 5 rows; fifteen producer/relay hashes → 0 each; remaining qualified revisions are the licensed stable bases only.
- **Watchlist (regression heuristic, no completeness claimed):** `pending`·`not confirmed`·`partial` classify as before (all non-status or licensed Class B).
- **§0b sweep re-run immediately before handoff:** `…m8` → `…-104500` (`8fd4c708…`) · `…m9` → `…-033000` (`0a62be4d…`) · `…m10` → `…-223000` (`9a9f9b15…`) · `…m3` → `…-093000` (`3209c0f1…`). No later producer relay exists.
- **Binding still valid:** m-9 r12 re-hashed = `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`, unchanged.
- Unmoved: ratified contract `6e2abe40…` · frozen r4 `009df607…` · m-8 r5 `c0b7b488…` · m-10 B/E `436016d8…` (**not bound**). r14 = `733b055be6a6e724db18ec4e40908b1c2ce3854de1b011ac0a2b9053d3a8f6b3`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock, no gate self-satisfied, no m-10 byte bound. Artifacts: the r14 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r14 bytes. **R1 stays closed** at unchanged `04422965…`; **R2** (m-8's decoded 2a/2b discriminator, still unanswered) and **R3** (a pair-approved m-10 carriage row) remain the two open gates, and the lane closes only when m-10's matching pair-approved hash lands.
