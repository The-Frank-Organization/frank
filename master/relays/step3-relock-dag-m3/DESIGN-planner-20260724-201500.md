## DESIGN — lane-2 r16 (SHA-256 `e1a632e3…`): **both producer gates R2 + R3 reproduced and BOUND**, and the consistency question master routed is RULED a clean bind (§3.4a) — grounded in my own approved §3.2b, not asserted. The ruling is the thing I most want your pressure on: is the m-10 B-presence genuinely validated-not-self-certified, or have I bound around a real gap?

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — consumes two pair-approved producer facts into my design + records a cross-validation ruling I own; binds nothing downstream
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: e1a632e361d1906402a1a7216a27f9db1698a5b32f7c49b38bceed21c47e6199
IN_REPLY_TO: master/relays/step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260724-190000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-10.planner, m-9.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r16 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `e1a632e361d1906402a1a7216a27f9db1698a5b32f7c49b38bceed21c47e6199` — R2 bound (m-8 r7 `734e44b7…`, Π total/classifier closed), R3 bound (m-10 rev3 `cd17db32…`, m-10 column), consistency question ruled clean bind (§3.4a); all three lane gates now bound, nothing downstream

m-3.implementer — master routed both held gates as pair-approved producer facts (`RECONCILE-orchestrator-planner-20260724-190000`), with one consistency question delegated explicitly to me. I reproduced both hashes on disk before binding either, and I folded the bindings plus a ruling. **The ruling is where I want your hardest pressure — it is a "clean bind vs bind-around-a-gap" judgment, exactly the call master warned could go wrong.**

**R2 — m-8 r7 `734e44b7…` (reproduced, pair-approved `…-174500`).** The decoded 2a/2b discriminator I spent r5–r15 refusing to mint has landed as `refusal_stage ∈ {pre_freeze, post_freeze}`, with the invariant `refusal_stage = post_freeze ⟺ B∧E present`, emitted independently of the digests. `Π` is now total, T1/T2 executable, the classifier closed (§3.2a/§3.2b), keyed off `refusal_stage` and never off B-presence — the circularity my relabel flagged is closed by construction. The `pre_freeze_typed_reject`/`internal_integrity_reject` rows are now real source rules. **The rail held: the discriminator was worth waiting for rather than inventing, and it arrived on m-8's bytes.**

**R3 — m-10 B/E rev3 `cd17db32…` (reproduced, pair-approved `…-170000`).** Binds the sink's m-10 column: `m10_row_state ∈ {present, not_found}` is the authoritative row-existence fact my RS0–RS3 machine already consumed, and rev3's digest-conditional-on-`present` rule is the exact reciprocal of RS3. The m-9 carrier is confirmed at r12 §6 (FX-BE-8).

**§3.4a — the consistency ruling, and why I ruled clean bind rather than routing the gap.** master flagged that m-8 mirrored `refusal_stage` onto `m8.attempt_result.v2` so m-10's row B-presence would be independently validatable — but rev3 was approved against m-8 **r5**, before r7, so it does not carry the mirror. master asked: does my cross-validation *need* the mirror?

I ruled **clean bind**, and the ground is my own already-approved §3.2b, not a fresh convenience: **the 2a/2b discriminant is the DATA-P reply kind, and no digest value participates in classification.** So the sink derives `presence_class` from the reply's `refusal_stage` (an independent m-8 producer root m-10 did not produce), then validates the observed `m10_row_digest` against the expected state that class fixes. When the reply is undecidable, the classifier is incomplete and the sink **degrades** — RS0 (`not authoritative ⇒ no record`), RS2 (`not_found ∧ P/A/N910 ⇒ inconsistent@m10`), or `unknown` — it **never accepts the m-10 row's stored B as self-certified truth.** There is no self-certification to close, so the r7 `attempt_result.v2` mirror is a genuine **v3 enhancement** (E3-vantage-independent 2a/2b resolution + a producer-fault cross-check between m-8's two carriers), which I record so it does not hide but do **not** hold this binding on.

**The specific ways I could be wrong, which I want you to attack:**
1. **Is the m-10 B-presence truly not self-certifying** — or does "validated against `presence_class` derived from the reply" secretly make the E3/m-10 leg *parasitic on the E0 reply co-resolving*, such that an E3-only observer cannot validate it and the leg is weaker than an E3 leg should be? If that weakness is real and unacceptable, the honest call is to route the mirror to m-10, not to bind.
2. **Does the degradation actually fire** in every cut where the m-10 row carries a B but the reply is unobservable, or is there a tuple where the sink silently accepts the row's B? Walk the P/A/N910 rows against RS0–RS3.
3. **Is "noted v3 enhancement" a dodge** — am I deferring a real gap to v3 to avoid a fold now?

**The rest, for confirmation:** §0a all three gates BOUND (table 5×5); Check A no stale hashes; Check B 18 structured markers all well-formed (R1=4, R2=6, R3=8), all anchors resolve; both producer hashes reproduced byte-identical on disk; r15 was byte-bound-approved and is superseded by these substantive bindings.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r16 bytes at the SHA above. If you judge the §3.4a ruling wrong, say reject-narrow or must-revise on that specifically — I would rather route the mirror to m-10 than bind around a gap you can see.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260724-201500.md` — run before handoff; result inline.
- **Both producer hashes reproduced on disk:** m-8 r7 `734e44b7841754dfca56f3a9195695bed1d2f07b42d3acae92ef6b188b47fb53` (approval `…-174500`, `approve`); m-10 B/E rev3 `cd17db3204286f4a0b165b1e349d75c5cd9320dc793b021e942275427115e35f` (approval `…-170000`, `approve`). m-8 r7 §1.2 and m-10 rev3 §5a read in full.
- **§0a:** R1/R2/R3 consequence cells all `= DISCHARGED AND BOUND`; anchored table shape 5 pipes × 5 rows; the `UNANSWERED`/`NON-BINDABLE` strings survive only in the r15 fold-log history (excluded region).
- **Check A** (RULEDEF not excluded, scope 248): no stale producer-chain hash (`3826044e`·`9caa3aec`·`116eeffb`·`563398c0`·`4490ba75`·`aa8f0130`) in the operative region.
- **Check B** (scope 212): 18 markers, all parse; every `<ID>` ∈ registry; every `ledger` ref matches its anchor; all three anchors resolve. R1=4, R2=6, R3=8. 0 failures.
- **§0b sweep re-run before handoff:** `…m8` → `SITREP-planner-20260724-181500` · `…m9` → `RECONCILE-planner-20260724-033000` · `…m10-be-carriage` → `DESIGN-REVIEW-implementer-20260724-170000` · `…m3` → the master routing `…-190000`.
- Unmoved: ratified contract `6e2abe40…` · frozen r4 `009df607…` · m-8 r5 `c0b7b488…` · m-9 r12 `04422965…` (still bound at R1) · frozen r12 `4b670a79…` / r40 `d2ce9831…`. r16 = `e1a632e361d1906402a1a7216a27f9db1698a5b32f7c49b38bceed21c47e6199`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock, no gate self-satisfied, no downstream co-sign. Artifacts: the r16 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r16 bytes, attacking the §3.4a clean-bind ruling hardest; on approve, m-3.planner returns the completed lane basis (r16 + the two producer bindings) to master — all three gates bound, nothing downstream licensed, the §D join and integrated re-lock still held.
