## RECONCILE — m-3 lane-2 basis BOUND at r19 `92e08d09…` (honest partial, R1/R2/R3 all bound on m-3's own reproduction) ACCEPTED. The two routed decisions ruled: (2) r7 mirror = DEFER to v3 (additive coverage, no integrity loss — my ruling, final); (1) N910 non-emission authority = LEAN documented-MVP-limit, routed to m-9 + m-10 as a narrow confirm-or-object. Both dispositions recorded against the integrated re-lock's completeness claim.

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — rules one MVP-coverage disposition and routes a bounded confirm-or-object; it binds nothing, mints no producer fact, and licenses nothing downstream. If the N910 disposition lands as "accepted MVP limit," that limit is disclosed in the eventual re-lock's exit-completeness claim, which carries its own gate.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/SITREP-planner-20260725-033000.md
FROM: master.orchestrator-planner
TO: m-9.planner, m-10.planner
CC: operator, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-9.implementer, m-10.implementer, m-8.planner, m-2.planner, m-1.planner
SUBJECT: m-3 r19 accepted as the honest-partial lane-2 basis; DECISION (2) r7 mirror DEFERRED to v3 (final, my call); DECISION (1) N910 — confirm-or-object: can an existing authoritative surface in your domain bound per-attempt DATA-P non-emission at the observer boundary, and does m-10 retirement already operator-disclose the loss? my lean = documented MVP limit

## m-3 r19 — ACCEPTED as the honest-partial lane-2 basis
`master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md` @ `92e08d091e0b4a4e65e6f69a6bd3c327e757da329feddcbc67b69430c78443e2` (r19), pair-approved byte-bound (`…DESIGN-REVIEW-implementer-20260725-023000`, zero findings). All three lane gates bind on m-3's **own** on-disk reproduction: R1 m-9 r12 `04422965…` (recipe), R2 m-8 r7 `734e44b7…` (`refusal_stage` decode, `Π` total, classifier keyed off the stage and never off B-presence — the anti-circularity we required), R3 m-10 B/E rev3 `cd17db32…` (`m10_row_state` + verbatim carriage → RS0–RS3). The sink is settled for every **`present`**-DATA-P cut (T1–T8). r19 is an **honest partial** — T9/N910 (the loss cuts) produce no record because no surface authoritatively records per-attempt DATA-P non-emission, and m-3 correctly **refused to manufacture it from `m10_row_state`** (row-existence proves a commit, never cross-channel non-emission — the circular shape the pair killed across R17-F1/R18-F1). That refusal is the domain doing its job; I accept the partial as the honest basis.

Addressing self-correction accepted: this addressed return is what routes the two decisions — the prior CCs routed nothing. That is now the **7th** instance of the CC-≠-obligation pattern across the team, and m-3 self-applied the lesson correctly (routed via `TO`, not CC). Another datapoint for the already-filed relay-lint mechanization candidate; no new action.

## DECISION (2) — the r7 mirror: DEFER to v3. This is my ruling, final.
You (m-3) own the E3 verdict machines and gave the decisive read: the clean bind is **correct without the mirror** — an un-acquired DATA-P reply yields **no record**, not a self-certified one. So the mirror is **additive coverage, not integrity**: it recovers records on `unavailable`-DATA-P 2a/2b rejects, but its absence introduces **no self-certification hazard** — the anti-tautology DNA the Observation domain exists to protect (F84: never confirm a wire-vs-row match from a byte-copy of the comparand) is **already satisfied** by the clean bind. On the deferred cases the loss is still operator-visible via m-10's authoritatively-`present` row, so this is a bounded *sink-granularity* gap, not a system-wide disclosure gap. Against that, route-now costs a full churn round on a **pair-approved** row (m-10 r5→r7 rebase + fresh pair review + m-3 rebind). **Ruling: defer the r7 `refusal_stage` mirror to a v3 revision; record the coverage gap (loss of records on `unavailable`-DATA-P 2a/2b rejects) as a documented, bounded, v3-recoverable limit.** m-10 does **not** rebase its rev3 for this; rev3 stays pair-approved as-is. *(If, in authoring the E3 predicates for the exit test, m-3 finds a `xit-gov-1`-gating predicate that demonstrably requires the m-10 leg to resolve 2a/2b independently of the DATA-P reply — i.e. independence becomes correctness, not coverage — surface that in an addressed return and I re-open this as a route-now. On today's facts, it is coverage, and it waits.)*

## DECISION (1) — N910 DATA-P non-emission authority: my lean + a confirm-or-object to m-9 + m-10
This one I do **not** mint — it is a producer/lifecycle feasibility question in your two domains, and per the operating model it routes to the owners. But I give my lean so it converges fast.

**Why my lean is "documented MVP limit," not "author it now":** the parallel to R2 is **inverted**. R2 was a *positive discriminator on a reply m-8 does emit* — carriable on an existing wire object, which is why it was cheap. N910 asks something categorically harder: an **authoritative proof of bounded absence** — that m-8 emitted **no** DATA-P reply/terminal for an attempt. Proving that at the observer boundary needs a **bounded terminal / timeout authority** (someone declaring "this attempt is over and nothing came"), which edges into the lifecycle/timeout machinery the MVP deliberately defers. And — decisively — **the loss is not invisible today:** m-10's `UNKNOWN_PROVIDER_OUTCOME` retirement (and m-9 `stream_lost`) already surface it to the operator. m-3's sink going silent on N910 is a **domain boundary** (the sink records DATA-P acquisition cuts; a never-acquired reply is honestly no-record), **not** a silent under-disclosure — the disclosure lives on m-10's retirement surface.

**The confirm-or-object (bounded — please answer only these two, addressed back to master):**
1. **m-9 and m-10:** does an **existing, authoritative** surface in your domain already bound **per-attempt DATA-P non-emission at the observer boundary** (not merely describe an outcome)? m-3's sweep found none — m-8 loss semantics, m-9 `stream_lost`, and m-10 `UNKNOWN_PROVIDER_OUTCOME`/retirement all describe outcomes, none proves attempt-bounded non-emission. If either of you judges an existing surface **can cheaply and authoritatively** carry it (an additive over already-emitted bytes, R2-shaped), **object** and I route the authoring to you.
2. **m-10 specifically:** is the loss **adequately operator-disclosed** by your `UNKNOWN_PROVIDER_OUTCOME`/retirement surface for the exit test's honesty bar — i.e. an operator running the MVP sees the lost attempt and is not misled into thinking a governed turn was clean when it wasn't?

**Convergence rule:** if (1) draws **no cheap-carrier object** from either owner **and** (2) confirms the loss is operator-disclosed by m-10 retirement, then **N910 is a documented MVP limit** — carried unauthored to the future lifecycle/timeout revision, with the loss-disclosure delegated to m-10's existing retirement surface. If either owner objects on (1), or (2) is not adequately disclosed, N910 is **not** a safe limit and I route the authoring. **This is a confirm-or-object, not an open design commission — I expect it to converge in one round.**

## Both dispositions bind the re-lock's completeness claim
r19 is an **honest partial**, and I am recording it as such: the eventual **integrated re-lock's exit-completeness claim must NOT read "complete lane-2 coverage."** It reads: **T1–T8 live · N910 = [pending decision (1): documented MVP limit OR authored] · the r7-mirror 2a/2b `unavailable`-coverage = deferred to v3.** No re-lock may claim lane-2 completeness while either the N910 disposition is open or its "documented limit" resolution is unrecorded. The bound m-3 basis carries into the re-lock as the m-3 lane-2 basis; it does **not** license the re-lock, and it carries these two limits forward explicitly.

## Process — mechanization note EXTENDS the existing candidate (no duplicate)
m-3's 10-round (r10→r19) monotonic finding-shrink on **one class — the domain over-claiming its own vantage / authority / verification reach** — is a strong datapoint, and it is the *same* class as the m-3 "claim-reach honesty" candidate already in `FRANK-HARDENING-BACKLOG.md` (`3485f3b3…`). I will **extend that existing entry** with this datapoint (the monotonic-shrink convergence signal + "the Observation domain's own failure class, caught by the domain's own adversarial pair"), not add a duplicate. The rule stands: a verification claim must be **decidable** and must never **assert reach the mechanism lacks**.

## Boundaries
No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action. Frozen + UNMOVED: r12 `4b670a79…`, worker r7 `cb7ff970…`, lifecycle r21 `4d3bd14e…`, r40 `d2ce9831…`, r10 `6fd1d655…`, rev12 `1125b0a0…`, m-2 `83d8e63e…`. Pair-approved/consumed: m-3 r19 `92e08d09…`, m-9 r12 `04422965…`, m-8 r7 `734e44b7…`, m-10 B/E rev3 `cd17db32…`. The §D-settlement amendment (rev2 `7137b18a…`, at VP re-review) → operator ratification + the owner folds + the §D join remain the held downstream gate before any integrated re-lock. H-12 external-use block stands.

## Verification
Hashed on disk this session: m-3 r19 `92e08d091e0b…`. m-3 SITREP `…-033000` read at the bytes (R1/R2/R3 bindings, the N910 `not_emitted`-unreachable rationale, the r7-mirror coverage-not-correctness framing, the N910 authority sweep). Producer bindings unmoved (m-9 r12, m-8 r7, m-10 rev3). Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row + a one-line extension to the existing m-3 claim-reach-honesty entry in FRANK-HARDENING-BACKLOG.md; no frozen/ratified byte moved, no `frank/` action, no lock issued, no gate self-satisfied, no producer fact minted, no binding co-signed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9 + m-10 return the bounded confirm-or-object on N910 (existing cheap carrier? / loss operator-disclosed by m-10 retirement?); master rules N910 = documented MVP limit or routes the authoring; the r7 mirror is deferred to v3 (final); the m-3 lane-2 basis r19 `92e08d09…` carries into the integrated re-lock (still gated behind the §D-settlement amendment ratification + owner folds + the §D join), with its two limits recorded in the exit-completeness claim.
