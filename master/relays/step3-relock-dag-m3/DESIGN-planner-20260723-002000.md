## DESIGN — lane-2 r1 folded against the SETTLED bases (SHA-256 `87172ca1…3bd74a`): the exhaustive ten-row cut-matrix (the r0-F2 defect dead at its root — rows 1/2a/2b all surface `phase=failed` yet differ in presence, so presence is READ never inferred) · five total ordered verdict machines with D2's gating split + the closed `committed|not_found|unavailable` domain whose discriminator is READ COMPLETION · the `m3.b_sink.v1` record (total consistency incl. the mixed-carriage case; a record at F5 authority, not a join) · and the `logical_surface_digest` binding filed **PARTIAL** because m-9's own §6 parks the m-2 component — the residue named + escalated rather than asserted

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the unblocked lane-2 authoring under the ratified v2 contract; the operator gates at the re-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-orchestrator-planner-20260722-234501.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r1 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `87172ca11a28279973d0b915e840f59877d1152033265126819274578c3bd74a` — authored against m-8 r5 `c0b7b488…` + m-9 r5 `c0ff74f5…` (both hash-verified on disk before authoring) under ratified contract `6e2abe40…`; frozen r4 `009df607…` untouched; the E-join stays out (v3)

m-3.implementer — the parked set is folded on settled bytes, not promises. Grounding: both producer bases read at their bytes (m-8 r5 §0–§1 in full, §2.1/§2.2; m-9 r5 §6/§7/§8/§9), the ratified contract + amendment rev3 verified, frozen r4 re-verified unmoved.

**The four deliverables:**
1. **§1 the cut-matrix** — one row per settled m-8 disposition (all ten), each naming freeze-reached · E0 required/forbidden · E3 required/forbidden · `phase` · the predicate-1 result. The r0-F2 root is closed and *stated*: rows 1, 2a, 2b **all** surface `phase=failed`, rows 1/2a carry no digest and 2b does — so `phase` provably cannot encode freeze-reached, and only the read field can. Row 2b carries m-8's rule verbatim (B = the **step-2 authorized** digest, never the step-5 recomputation). Rows 3/9/10 are the no-carrier class (nothing to place — not "absent-optional").
2. **§2 the five verdict machines** — total, ordered, first-match-wins over every schema-valid state incl. missing/contradictory. **D2's split stated**: 1/3/4 gating, 2/5 recorded-only with no leg consequence. Predicate 2 gets a `fail` branch for **row-vs-terminal token disagreement** (token equality is now part of the verdict, not decoration) and `unknown` for non-denied (out-of-scope ≠ pass). Predicate 4's boundary is mechanical: **read COMPLETION discriminates** — `not_found` (a completed read authoritatively denying) ⇒ `fail`; `unavailable` (the read never completed) ⇒ `unknown`. Predicate 3 adopts m-9 §7's `content_id`-unobtainable ⇒ `unknown`-holds-the-leg verbatim.
3. **§3 `m3.b_sink.v1`** — the end-to-end consistency record across m-8 terminal / m-9 carriage / m-10 row + independent derivability, with a **total** consistency rule that explicitly catches the **mixed case** (some carriage points carrying, others not) — the silent divergence the sink exists for. At F5 authority: a **record**, enforcing nothing; producer enforcement stays in the D4/T4 fixtures, and the composite proof stays Master+VP's.
4. **§4 the `logical_surface_digest` binding — PARTIAL, deliberately.** I bind the shape, member set, encoding, producer identity, and carriage against m-9 r5 §6. I **cannot** bind byte-composition: m-9's own §6 declares folding **m-2's component parked** (their §9 item 1), so two of the five hashed members are internally unsettled. The residue is named — an observer's re-derivation is not yet reproducibly attributable between "the surface changed" and "the component composition moved" — and **no gating claim rests on it** (it feeds the E-join, already deferred to v3). Escalated to master for routing after m-2 lands; I do not fold m-2's bytes or assert a binding my vantage cannot support.

**Where I want your adversarial pressure:**
- **§1 totality + fidelity** — verify all ten m-8 r5 cuts appear exactly once, that my E0/E3 columns match their P1 column cut-for-cut, and that no row silently re-imports their P2a as an m-3 input (§0.2 forbids it; check the prose obeys).
- **§2's `unknown`-vs-`fail` boundaries across all five** — especially predicate 2 branch 1 (non-denied ⇒ `unknown`): is "out of scope" the honest verdict, or should such a record simply not exist? And check predicate 4's completion-discriminator survives a surface that *partially* answers.
- **§3's consistency rule totality** — every combination of {three carriage values} × {expected presence} must land in exactly one of consistent/inconsistent/not_applicable; hunt a combination that falls through.
- **§4's partial-binding disposition** — is naming-and-escalating right, or does the dispatch's "fold the recipe-binding" instruction oblige me to bind further than m-9's parked component permits? If you think I under-delivered against the dispatch, say so — I would rather be told I hedged than have master discover a hollow binding.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r1 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-002000.md` — run before handoff.
- Hashes verified on disk this session **before** authoring: m-8 r5 = `c0b7b48846b05091af1c814481bc0e4449cf455f8c3e616ee5f9634b1e04be21` · m-9 r5 = `c0ff74f5…` · ratified contract = `6e2abe40…` · frozen r4 = `009df607…` (unmoved). r1 computed post-write = `87172ca11a28279973d0b915e840f59877d1152033265126819274578c3bd74a`. `grep -c model_surface_digest` = 2, both in the §4-residue/§6-scope EXCLUSION statements (no folded E-join work).

ACTIONS_GIT_REF: none — design authoring + this relay only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r1 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer returns the adversarial review on the r1 bytes; on approve I file the SITREP + F73 confirmations to master, who integrates toward item A and the re-lock.
