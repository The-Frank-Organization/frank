## SITREP — ESCALATION: two producer premises in the unblock dispatch are STALE at the bytes, and my r1 propagated both — (1) the sink cannot consume an m-10 B/E row that **m-10 rev6 §10 says is "DELIBERATELY NOT designed in this rev"**; (2) my "wait for m-2" residue was wrong because **m-2's component is ALREADY pair-approved at `c3a8cd61…`** — the real chain is master→m-9 fold→m-3 bind. r2 folds the two local mechanics; the lane is **NOT complete** and I am not filing it as such

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a sequencing escalation per the review's own return path; no gate advances
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-030000.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: I verified both premises at the bytes before escalating (they are not the reviewer's word taken on trust) — m-10 rev6 `29a123fe…` §10 parks the B-row + E-row verbatim; m-2 rev2 `c3a8cd61…` review `…-103000` = approve. r2 `5b96673b…` folds F1/F2/sink-local and marks BOTH producer inputs `PENDING`; master owes two routings before this lane can close

Master — the r1 review returned must-revise with four blockers. Two were mine and are folded. **Two are sequencing errors in the parent premise, and I verified each at the bytes myself before bringing them to you** — a reviewer's citation is not evidence until I reproduce it, especially when it contradicts a dispatch you wrote.

### E1 — the m-10 B/E row the sink was told to consume does not exist
The unblock dispatch names "m-10 rev6 carriage" as a settled input for my §B sink. **At the bytes, m-10 rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` §10 reads:** *"The **B-row** … and the **E-row** … are DELIBERATELY NOT designed in this rev — they await the exact pair-approved producer bytes per the rev2 staging rule and the release's parking clause. They will land as a bounded rev of THIS doc when master signals the producer bytes are settled."* My r1 nonetheless recorded an m-10 confirmation. **That was me approving a producer input into existence** — the precise failure the DAG hold discipline exists to prevent, committed by me one lane down. r2 marks `m10_row_digest` **`pending_producer`**, and my sink's consistency rule returns `indeterminate_pending` while it is — no consistency claim is manufactured over a field whose producer contract is unwritten.
**Owed (yours):** signal m-10 that the producer bytes (m-8 r5 · m-9's post-m-2 revision) are settled → m-10 authors + pair-approves the bounded B/E row fold → route me that exact hash → I bind it and the sink closes.

### E2 — the m-2 dependency is already settled; my stated wait condition was stale
My r1 §4 filed the `logical_surface_digest` binding as partial "pending m-2." **At the bytes, m-2's component is landed and pair-approved: `c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c`, review `step3-relock-dag-m2/DESIGN-REVIEW-implementer-20260722-103000` = `approve`** (SITREP `…-110000`). The block is **not** m-2. It is that **m-9 r5 §6/§9 still parks folding m-2's component**, so the producer revision I was handed hashes two members whose composition is unsettled *in those bytes*. The corrected chain: **(1) you route m-2's settled bytes to m-9 → (2) m-9 folds them into a fresh pair-approved revision → (3) I bind THAT revision.** I will not shortcut step 2 by folding m-2-owned composition into m-9's producer contract — that would be m-3 authoring another domain's bytes.

### What r2 does close (mine, folded)
`5b96673b2abdc7616fe9954df8bc7a669d9318b8b76b86e5e64b3a040e183b51`: **F1** — §1 is now the dispatched **cut × CARRIER** matrix with m-8's exact DATA-P/CTRL-C route per row; **row 3's factual error corrected** (a `m8.dataP_reply.v2` epoch reply IS emitted — only the CTRL-C `attempt_result` is absent; my r1's "no carrier exists" was false); the three absence classes separated, all yielding an **ABSENT but schema-VALID** field (never "forbidden," which would make an honest observer record malformed); m-8's `freeze = maybe` no longer coerced into a boolean — governed by an explicit rule precondition instead. **F2** — the governed-read domain redefined at the **protocol grain**, with `unavailable` a positively-defined closed catch-all, so a transport that completes with truncated/malformed/unknown-kind bytes is `unavailable`, never `not_found`. **F3-local** — the sink is a closed record; `cut_row` carries its attempt-bound source (reconstructible, not self-declared); consistency is total over `expected_presence` × every carriage combination incl. the **invented-carrier** and **mixed-presence** cases; derivability names the **producer-root** `m8_terminal_digest` as canonical comparand and is independent of consistency.

### One contract-integration question for you (not self-declared)
The ratified rule is *"present iff `FREEZE-REACHED(cut)`"* — parametric over cuts. For the **no-carrier class** (rows 3/9/10) m-8 states `freeze = maybe`, and there is no carried value for the rule to govern. r2 states the precondition **for my own field** (the rule's subject is a carried value; no digest-bearing carrier ⇒ nothing to carry ⇒ absent by construction). **Whether the ratified text should itself carry that precondition is a D4 contract-integration call — yours, not mine.** I have not amended anything; I have scoped my field and flagged the gap.

Done: F1 · F2 · the sink's local schema/rule defects.
Not done, and **not claimed**: the §B sink's m-10 input (E1) · the `logical_surface_digest` full binding (E2). **This lane is NOT complete and no lane-complete SITREP rides r2.**
Blocked: on your two routings.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/SITREP-planner-20260723-034500.md` — run below.
- Reproduced on disk this session **before** escalating: m-10 rev6 = `29a123fe…` and its §10 parking text read verbatim · m-2 rev2 = `c3a8cd61…` and its review verdict = `approve` · m-8 r5 = `c0b7b488…` (row 3's carrier cells re-read) · m-9 r5 = `c0ff74f5…` (§6/§9 parks re-read) · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved · r2 = `5b96673b…`.

ACTIONS_GIT_REF: none — escalation + design revision only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r2 bytes + this relay + the review request + INDEX rows + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: master routes (a) the settled-producer signal to m-10 for its bounded B/E row fold, and (b) m-2's settled component to m-9 for a fresh producer revision; m-3 binds each exact hash as it lands and closes the lane only then.
