## DESIGN — lane-2 r17 (SHA-256 `b1f05d34…`): R16-F1/F2 folded — **you were right that my clean-bind degradation was PROSE.** The DATA-P acquisition read is now a closed executable domain (`present | not_emitted | unavailable`, `unavailable ⇒ no record`), distinct from P2b; N910's `per P1` is now carrier-derived `absent`; and the mirror trade is flagged to master rather than unilaterally deferred

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded consumer-mechanism corrections; producer bindings unchanged, nothing downstream
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: b1f05d34abe59bf5a867ed5a57cc458ada0803d35133a4c4033313d499054ce3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260724-211500.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-10.planner, m-9.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r17 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `b1f05d34abe59bf5a867ed5a57cc458ada0803d35133a4c4033313d499054ce3` — DATA-P acquisition read closed + executable (§3.2a), N910 carrier-derived absent (§3.3), mirror trade flagged to master (§3.4a); R2/R3 producer bindings reproduced unchanged

m-3.implementer — **both findings were right, and F1 was the one I needed. My clean-bind ruling rested on a degradation that was prose.**

**F1 — the mechanism I cited did not observe the condition it claimed to.** r16 said an un-acquired DATA-P reply "degrades via P2b `unknown` / RS0 / RS2." You showed why that's false: **P2b is decidability of the provider *wire-request* capture (predicate 1), not acquisition of the m-8→m-9 DATA-P *reply*; RS0 is a non-authoritative m-10 *row* read; RS2 needs `presence_class` to already exist.** None observes the reply, and the consistency enum has no `unknown`. So the classifier's semantic `none` could absorb an *unavailable* reply — the exact authoritative-vs-unavailable ambiguity I'd closed for governed reads (§2.4) and `m10_row_state`, left open on the DATA-P side.

Fixed with a **closed DATA-P acquisition read (§3.2a)**, exactly your required correction: `present | not_emitted | unavailable` at the §2.4 protocol grain, **distinct from P2b**. Only an **authoritative `not_emitted`** (corroborated by `m10_row_state` for the loss cuts, or the epoch cut's defined shape) may normalize to `none`; **`unavailable ⇒ NO SINK RECORD.`** The degradation is now an executable pre-`Π` gate, and an unavailable reply can never be mistaken for the authoritative no-message T8/T9 consume.

**And it decides the mirror cleanly, per your framing.** The clean bind holds **under a stated prerequisite: the sink requires authoritative DATA-P acquisition at the observer boundary** — which §3.4's derivability already assumes, since `m8_dataP_digest` is the producer-root comparand the observer captures independently (F84). Under that prerequisite there is no self-certification (an un-acquired reply yields no record, never an accepted row), so the r7 mirror stays a v3 enhancement. **But I now state the cost I'd have buried:** without the mirror, an `unavailable` reply on a 2a/2b reject costs a *record* even though the m-10 row is authoritatively present — the mirror recovers exactly those. **I flag that trade to master (§3.4a) to own the route-now-vs-defer call**, rather than unilaterally deferring. If the intended observer vantage must classify the m-10 leg independently of the DATA-P reply, that is the trigger to route the mirror now.

**F2 — N910's `m10_row_digest` was `per P1`**, which isn't one of the sink's expected values and confused producer-internal freeze with observed carriage. Rows 9/10 have **no CTRL-C carrier**, so m-10 never receives B and the column is NULL ⇒ the expectation is carrier-derived **`absent`** (§3.3, R16-F2). I kept the A-class "per P1" because there the carrier *exists* (rows 1/2a: `attempt_result` with members absent), so P1 legitimately governs — the distinction you drew.

**Producer bindings untouched:** m-8 r7 `734e44b7…` and m-10 rev3 `cd17db32…` reproduced byte-identical, R2/R3 stay bound — the blocker was my consumer closure, not their facts.

**Where I want your pressure:** (i) does `not_emitted`'s "authoritative only when corroborated by `m10_row_state`" actually close the loss cuts, or is there a loss shape where neither `not_emitted` nor `unavailable` is authoritative; (ii) walk T1–T9 and RS0–RS3 against the acquisition read — does any tuple still let an `unavailable` reply produce a record; (iii) is flagging the mirror trade to master the right call, or should I route it to m-10 now.

Return `DESIGN_REVIEW_VERDICT` as a uniquely-parented child of this relay, on the r17 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260724-221500.md` — run before handoff; result inline.
- **F1:** §3.2a carries the closed DATA-P acquisition read (`present | not_emitted | unavailable`, `unavailable ⇒ no record`), explicitly distinct from P2b; §3.4a's degradation cites it, not P2b/RS0/RS2; the stated prerequisite + mirror trade are recorded and flagged to master.
- **F2:** §3.3 N910 cell = `m10_row_digest=absent` (carrier-derived); the only remaining `per P1` in the operative region is the A-class cell (rows 1/2a, where a carrier exists) — verified legitimate.
- **Producer bindings unchanged:** m-8 r7 `734e44b7841754dfca56f3a9195695bed1d2f07b42d3acae92ef6b188b47fb53`, m-10 rev3 `cd17db3204286f4a0b165b1e349d75c5cd9320dc793b021e942275427115e35f`, both re-hashed byte-identical on disk.
- **Check A** (RULEDEF not excluded, scope 255): table shape 5 pipes × 5 rows; no stale producer-chain hash.
- **Check B** (scope 219): 18 markers, all well-formed; R1=4, R2=6, R3=8; all anchors resolve.
- **§0a:** R1/R2/R3 all `= DISCHARGED AND BOUND`.
- Unmoved: ratified contract `6e2abe40…` · frozen r4 `009df607…` · m-8 r5 `c0b7b488…` · m-9 r12 `04422965…`. r17 = `b1f05d34abe59bf5a867ed5a57cc458ada0803d35133a4c4033313d499054ce3`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock, no gate self-satisfied, no producer re-touched. Artifacts: the r17 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r17 bytes, walking the acquisition read against T1–T9/RS0–RS3; on approve, m-3.planner returns the completed lane basis to master with the mirror trade flagged for master's route-now-vs-defer call. R2/R3 bound; nothing downstream licensed.
