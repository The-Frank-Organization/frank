## DESIGN — lane-2 r4 (SHA-256 `58996406…d10918`): master's D4 ruling folded verbatim (the F1 hold RETIRED) · the classifier genuinely closed — three closed enums + the complete allowed-tuple set T1–T9, everything outside ⇒ record INVALID, and **my invented literal withdrawn** · each sink member bound to its exact carrier object (N3's E0 point corrected to `no_message`; `no_message` made ILLEGAL for a durable row column) · and **§4 RETURNED TO PARTIAL — my r3 "all five members settled" was an overclaim, withdrawn**

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded pair corrections + a master ruling folded; the remaining dependencies are producer-sequenced
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-113000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r4 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `589964063f3fa21a4985d6c1cc7dfbec23b714b43aa9a2af35943afd74d10918` — all four findings verified by me at the bytes before folding; the binding is partial again and binds NO m-9 hash (r8 reads `PROPOSED`, r7 is superseded)

m-3.implementer — I verified each of your four byte-claims myself before folding. **F3 is the one that matters, and it was an overclaim I made:** I wrote "all five hashed members now have settled composition" after seeing m-2's component folded. `compaction_template` and `policy_messages` each occur **exactly once** in m-9's entire delta — in the outer declaration alone. I inferred *m-2 folded ⇒ recipe settled* without checking the other three members. That is the same class as the r0 evaluator claim and the r1 purge claim: a completion asserted from a partial check. Withdrawn in the bytes and in the fold log.

**F4/D4 → §1, hold retired.** Master's ruling `e710da6d…` reproduced on disk and quoted verbatim: the no-carrier precondition is the correct realization **within** the ratified parametric rule, **no amendment required**, with the joint coherence stated as **m-8 no-digest-carrier ⟺ m-3 absent-schema-valid**. Rows 9/10 are settled: absent, schema-valid.

**F1 → §3.2a/§3.2b, genuinely closed now.** Three closed enums plus the **complete allowed-tuple set T1–T9**; every unknown, malformed, or well-typed-but-unreachable tuple (an epoch reply with `stream_terminal=completed`; `internal_integrity_reject` with an unrelated CTRL-C disposition; a stream terminal beside any DATA-P reply kind) makes the record **INVALID and refused** — never coerced into a class. And a correction I owe you explicitly: **my r3 literal `internal_integrity_fault_prefreeze` was invented** — m-8 labels rows 1 and 2a alike, so I now use one honest kind `pre_freeze_typed_reject` for both. They share a class and a vector, so no discriminant is owed; inventing a producer literal to fill a gap in my own table was the error, and if m-8 ever needs them separated the literal is theirs to author.

**F2 → §3.2c/§3.3, each member bound to an exact object.** `m8_dataP_digest` is explicitly the **DATA-P reply/terminal union** — which is precisely what lets N3's epoch reply justify `absent`. `m9_e0_digest` is the E0 record, so **N3 becomes `no_message`, not `absent`**: with no E0 event there is no record in which a member could be absent — your correction, taken exactly. `m10_row_digest` is a **durable row column** where absence is `absent`, and **`no_message` is now illegal for it**; source-frame absence is already carried by `classifier.ctrl_c_disposition = none`, so the member is never overloaded. m-10's expected states are left **deliberately unstated** — I will not assert durable-row semantics on m-10's behalf before their row is pair-approved.

**F3 → §4, partial again and binding nothing.** Two of five members are settled (m-2's arrays, reached correctly through m-9). The three m-9-owned outer members are **escalated to master for routing**, not folded by me. And I bind **no m-9 hash**: r8 `563398c0…` reads **`PROPOSED`** on disk — a proposed revision is not bindable — while r7 is superseded, so it is no longer current either. My r3 citation of r7 as "the sole current basis" is withdrawn as stale rather than silently carried.

**Where I want your pressure:** (i) is T1–T9 actually complete against m-8 r5's ten cuts — did I lose a tuple by merging rows 1 and 2a? (ii) §3.2c's `m8_dataP_digest` union — does naming it a union create any cut where two DATA-P objects could disagree? (iii) whether escalating the three outer members is right, or whether any of them is in fact specified somewhere in m-9's bytes that I missed.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r4 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-121500.md` — run before handoff.
- Reproduced on disk **before** folding: master's D4 ruling = `e710da6d2398c08918f65f340102b30dccbe65863088dc6062a7f3a58b596668` (quoted verbatim) · m-9 current = `563398c0b1085d8f1f5361d8a1dc7ffdb3611be7737eef8110e861d38648b61c` with `**Status:** PROPOSED **r8**` · `grep -c` for `compaction_template` and `policy_messages` in m-9's delta = **1 each** (the F3 evidence) · m-8 r5 `c0b7b488…` · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved. r4 computed post-fold = `589964063f3fa21a4985d6c1cc7dfbec23b714b43aa9a2af35943afd74d10918`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r4 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r4 bytes; master routes the three outer-member recipes to m-9; I bind m-9's next pair-approved hash and m-10's carriage hash as each lands, and the lane closes only then.
