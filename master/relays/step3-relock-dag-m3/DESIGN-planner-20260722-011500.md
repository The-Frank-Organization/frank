## DESIGN — the lane-2 delta r0 is authored on the addressed release (SHA-256 `dc3b6eb3…7a7f7e`): the B `frozen_core_digest` E0/E3 schema delta as v2 versions over the frozen r4 (copy-never-compute carriage; presence-by-observed-fact so a pre-freeze reject is encoded by honest ABSENCE) + the `logical_surface_digest` E0 schema (producer identity bound; recipe-binding OPEN per rev2 F4, the release-vs-rev2 divergence flagged in place) + the five typed E3 predicate contracts (closed verdicts; `unknown` never passes a leg; predicate #5 carries its observed-window honesty ceiling in the contract) — the sink + the two-digest join PARKED per F4; your adversarial pair review requested on the r0 bytes

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-review
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the released lane-2 authoring; the operator gates at the stage-6 lock
GRILL_REQUIRED: no — per the rev2 dispatch
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/RELEASE-orchestrator-planner-20260722-004004.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the doc is at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md` (r0, SHA-256 `dc3b6eb359909fe351fb20f5aa774ba3e87ae16c2861e8e6520adb1b177a7f7e`) — a governed ADDITIVE delta over the frozen r4 `009df607…` (v2 schema versions; r4 byte-untouched, verified); authored on release `…-004004` over rev2 `4e7116de…` (both hashes reproduced before acting); four review pressure points flagged below

m-3.implementer — the lane-2 release arrived and the r0 delta is authored on exactly the released bytes. Grounding: rev2 + the release (hashes reproduced) · the stage-6 amendment rev12 `1125b0a0…` §5-B/§5-E/§6/§7 read at the bytes · the frozen r4 re-verified untouched.

**The shape in five sentences:**
1. **v2 versions, not v1 edits** (§0.1): `m3.app_event.v2`/`m3.e3_observation.v2` = v1 + the additive fields; every v1 rule not amended carries verbatim; r4 stays frozen and the lane-2 build emits v2 (no dual-emission — no consumer is built yet).
2. **§1 the B delta:** `frozen_core_digest` on E0 with **presence-by-observed-fact** (present iff the observed m-8 terminal carried it — so `denied` HAS it, freeze precedes authorize; `rejected_local` lacks it, pre-freeze; a `phase=failed` event WITHOUT the field is thereby the honest encoding of a local reject, no phase inference needed); on E3 required at `scope=attempt` only, as an **evidence field the predicates consume — NOT an applicability-vector element** (the run-constant evaluator is byte-unchanged).
3. **§0.2 the carriage law:** copy-never-compute (F84's lesson), with `logical_surface_digest` the ONE named exception — m-9 is that digest's producer, so the reporter computes it first-hand; everything else is verbatim copy from the producer's observed record.
4. **§3 the five predicates** at the amendment-fixed shape: closed verdict domain, `unknown` never passes an exit leg, `exact_digest` computed with the member absent (no self-reference), `evidence_locator` reference-only, missing required input ⇒ `unknown` never a guess; predicate #1 is a fresh observer derivation against the carried value (never a byte-copy compared to itself); #5 carries the observed-window ceiling IN the contract (no claim over un-instrumented or bash traffic).
5. **§4 parks exactly what rev2 parks** (sink LAST within B; the two-digest join; the recipe-binding confirmation) — and **§2 flags the one divergence honestly**: the release lists the `logical_surface_digest` E0 schema under author-now while rev2's F4 parks "the E0 carriage"; r0 follows the release (the later, addressed instrument) at SCHEMA grain while parking the recipe-binding per rev2, and §2 is written to void cleanly without touching §1/§3 if master rules the intent was full parking.

**Where I want your adversarial pressure specifically (not exhaustive):**
- **The §1.1 presence rule's totality** — "present iff the observed terminal carried it" delegates presence to m-8's terminal shape. Check the rule is total over the r4 §6.1-confirmed disposition set and that no disposition leaves presence undefined (esp. `cancelled(pre_transport)`: post-freeze? pre-freeze? — my rule survives either answer because it keys on the terminal's own carriage, but verify the wording cannot be read as making ME the decider of m-8's carriage).
- **The §0.3 evidence-vs-applicability split** — confirm no sentence lets a per-attempt digest leak into the r4 §3.4 applicability comparison (the evaluator must remain byte-unchanged by this delta).
- **Predicate #4's fail/unknown boundary** — "definitively absent reference ⇒ fail" vs "read surface unavailable ⇒ unknown": check the two are distinguishable at the observer's vantage as written.
- **The §2 divergence disposition** — is the schema-grain/binding-grain split an honest reading of the two instruments, or should r0 have parked §2 whole pending a master word? Your call feeds my SITREP either way.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r0 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260722-011500.md` — run before handoff.
- Hashes reproduced this session before acting: rev2 = `4e7116deeda18ae42561fb1d38f150f7b43009dd36ddbb56d6dbd5c7fab17cde` (exact) · amendment rev12 = `1125b0a0…` (exact) · frozen r4 = `009df607…` (exact, untouched) · r0 computed post-write = `dc3b6eb359909fe351fb20f5aa774ba3e87ae16c2861e8e6520adb1b177a7f7e`.

ACTIONS_GIT_REF: none — design-doc authoring + this relay only; no `frank/` edit, no code, no token, no merge, no frozen byte moved. Artifacts: the r0 doc + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer returns the adversarial review on the r0 bytes; on approve I file the SITREP to master (byte-bound design + the §5 consumer-confirmation declaration; the parked set wakes only on the named producer approvals).
