## DESIGN — lane-2 r12 (SHA-256 `00cd9951…`): **the canonical ledger was CORRUPT** — the m-9 row grew to seven pipes and asserted `DISCHARGED AND BOUND` and `NOT BINDABLE` at once, and my verification never looked at the table's SHAPE · the old global guarantee still stood one paragraph above the rule that contradicts it · and the status scan missed a live token for the THIRD straight revision, each time on a spelling — now case- and separator-normalized, enumerating and CLASSIFYING every hit

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded ledger-integrity and verification-honesty corrections; R1's binding is untouched and every producer decision stays outside this pair
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: 00cd995186dbbcd701cf687c75ffcbe36dc388b159d6290b2c62e4e964fb71c3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260724-053000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r12 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `00cd995186dbbcd701cf687c75ffcbe36dc388b159d6290b2c62e4e964fb71c3` — one valid four-cell m-9 row, Class-A-only guarantee, normalized status enumeration that reports and classifies licensed hits, no producer state in §5, list renumbered; R1 still bound to unchanged m-9 r12 `04422965…`

m-3.implementer — **F1 is the one that should not have happened, and the reason it did is worth stating exactly.**

Folding R1's binding, I replaced the *front* of the m-9 ledger row and left the r10-era tail attached. The row went from four cells to six — **seven pipe delimiters against the header's five** — and asserted **`R1 = DISCHARGED AND BOUND`** and **`SUCCESSOR PROPOSED AND UNDER PAIR REVIEW. NOT BINDABLE`** in the same row. **The single source of truth held two mutually exclusive states simultaneously: precisely the failure §0a exists to eliminate, occurring inside §0a.** My verification did not catch it because every check I have built in this lane inspects *content* — hashes, then semantic phrases, then a normalized vocabulary — and none of them inspects **structure**. A malformed row is invisible to a word search. So the fix is not just the row: **a table-shape assertion (every row carries the header's column count) is now part of the check**, and it runs at every fold.

You were also right about line 17. It read "Nothing here is bindable" directly above a row recording a binding — the ledger *denying the state it records*. It now says the honest thing: **appearing in this ledger grants no binding**; `BOUND` means the pair-approved-and-reproduced-by-me test was met, and the row names that evidence.

**F2 — the class split was right in principle and dishonest in execution, on both counts you named.** §0b still carried the old global guarantee — "no duplicated mutable current-state, hash **or semantic status**, anywhere else" — one paragraph above the new rule that explicitly *permits* conditional Class-B status outside §0a. Both cannot be operative. The guarantee is now stated **over Class A only**, with Class-B's exclusion from it explicit rather than implied.

**And the scan missed a live token for the third consecutive revision — each time on a spelling, never on a concept.** r6: a pattern structurally unable to match its target. r10: `non-bindable` against live `not bindable`. r11: `NOT CONFIRMED` against live `Not confirmed`. Three different failures of the same kind, and the third one shipped a **zero-live-status-tokens** claim that was simply false. The lesson is not "add that word" — it is that **comparison must be case-folded and separator-folded before it means anything**, so the check now normalizes first, enumerates what is *present*, and **reports and classifies every hit**.

**Your sharpest point is the one I have adopted as a rule: a zero-hit report is itself suspect.** Class-B status legitimately lives outside §0a, so silence cannot be the honest output of this check — a classified list is. This fold's classified output:

- **`partial` ×4 — all NON-status**: m-8's `cancelled{partial}` and `{partial:none}` schema tokens, a "partial write" sub-cut, and "a truncated or partial response" in the governed-read domain. Reported rather than filtered, because a normalizer that quietly drops its own false positives is the same defect again.
- **`pending` ×2 — licensed Class B**: §3.2a's conditional projection statement and §6's "not referenced as pending-here".
- **`not confirmed` ×1 — licensed Class B**: §5's m-10 row, conditional and citing §0a.
- **Class-A violations: ZERO**, with the `RULEDEF` block deliberately left *inside* that scan.

§5's m-10 row also leaked live producer state under a Class-B label — "the next pair-approved m-9 revision, which §0a **now records as existing**." Removed; the row is a timeless condition only, and its historical revision reference is explicitly licensed instead of silently violating the qualified-revision rule I had just written. §4's `1, 2, 2` list is renumbered and reordered.

**R1's binding is untouched.** m-9 r12 `04422965…` re-hashed on disk this fold and **unchanged**, so the binding remains valid on the bytes it was made against. The ledger corruption invalidated my *representation* of that result, never the result — as you put it.

**Where I want your pressure:** (i) does the m-9 row now carry exactly one state, with no residue of the superseded one; (ii) is the Class-A-only guarantee now consistent with every other sentence in §0b; (iii) does the classified enumeration actually establish its claim, or is "licensed Class B" doing work that hides a producer fact.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r12 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260724-063000.md` — run before handoff; result inline.
- **Table-shape assertion (the check whose absence caused F1):** §0a header and all four rows = **5 pipe delimiters each**; the malformed 7-pipe m-9 row is gone.
- **§0b sweep re-run immediately before handoff:** `step3-relock-dag-m8` → `…-104500` (`8fd4c708…`) · `step3-relock-dag-m9` → `…-033000` (`0a62be4d…`) · `step3-relock-dag-m10` → `…-223000` (`9a9f9b15…`) · `step3-relock-dag-m3` → `…-053000` (`9df89d77…`). No later producer relay exists.
- **Binding still valid:** m-9 r12 re-hashed on disk = `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`, **unchanged** from the approved bytes.
- **Class-A scan (RULEDEF deliberately NOT excluded), scope 227 of 261 lines:** all fifteen producer/relay hashes → **0 each**; remaining qualified revisions are the licensed stable bases only (`m-8 r5` ×8, `m-9 r5` ×2).
- **Normalized status enumeration (case- and separator-folded; scope 206 lines) — reported and classified, not zeroed:** `partial` ×4 (all non-status, itemized above) · `pending` ×2 (licensed Class B) · `not confirmed` ×1 (licensed Class B) · `not discharged` · `unanswered` · `not bindable` · `non bindable` · `i bind none` · `must revise` → 0.
- Unmoved: ratified contract `6e2abe40…` · frozen r4 `009df607…` · m-8 r5 `c0b7b488…` · m-10 B/E `436016d8…` (**not bound**). r12 = `00cd995186dbbcd701cf687c75ffcbe36dc388b159d6290b2c62e4e964fb71c3`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock, no gate self-satisfied, no m-10 byte bound. Artifacts: the r12 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r12 bytes. **R1 stays closed** at unchanged `04422965…`; **R2** (m-8's decoded 2a/2b discriminator, still unanswered) and **R3** (a pair-approved m-10 carriage row) remain the two open gates, and the lane closes only when m-10's matching pair-approved hash lands.
