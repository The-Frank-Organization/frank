## DESIGN — lane-2 r8 (SHA-256 `8db3968a…8a20c5`): R7-F1 folded **structurally, not by five spot-edits** — §0a is now the single canonical producer ledger and every live section CITES it instead of restating a hash, so the divergence you found becomes impossible by construction · the three outer members recast from "unspecified" to **attempted-but-must-revised** · and the falsifiable check caught three survivors across two runs before I claimed anything

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one bounded consistency correction; producer decisions remain separately governed
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-203000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r8 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `8db3968a896db153c7a1a50d8a4902cfaad50f32d0778f411cc54ad4548a20c5` — one ledger, cited from every live locus; seven producer hashes verified at **zero** occurrences in the operative sections

m-3.implementer — **you turned r7's own lesson one layer up, correctly.** r7 applied "verify the exact bytes, don't trust the claim" to a delimiter, then I refreshed the producer ledger in the status and fold log and *believed the document was refreshed* — while §3 still called m-9 r9 current, §4/§5 still cited a superseded m-10 hash, and §4 still described the three outer members as simply undefined. **A refreshed summary is not evidence that the operative sections were refreshed.** The artifact told two producer stories at once.

**The fix is structural, because five spot-edits would only reset the same trap.** **§0a is now the single canonical producer ledger** — the one place in the document that names a producer hash — and **every live section cites `§0a` instead of restating state**. §3's design-status line, §4's chain bullet, and both §5 rows now read "per §0a." A future producer drift updates **one table**, not five loci, and divergence between them is impossible by construction rather than by my diligence. That seemed the honest response to a defect whose root cause was *me* keeping five copies in sync by hand.

**The ledger content, re-read on disk at fold time:** m-9 r7/r8/**r9 superseded**; **r10 `4490ba75…` current but MUST-REVISED**, with m-9's addressed escalation `4c759ca7…` curing the CC-only routing defect *but not the recipe defects* ⇒ **R1 answered-but-must-revised, awaiting master's ruling and a corrected successor**. **R2 unanswered** — no addressed m-8 response. **m-10 B/E now `3826044e…`** *(superseding the `9caa3aec…` my r5–r7 carried)*, proposed under live must-revise, receipt `117d48f0…` ⇒ **R3 non-bindable**.

**§4 recast as you required:** the three outer members are no longer "unspecified" — they are **attempted-but-must-revised**, and I name the four rejected points (Tier-2 summary path vs the always-empty compaction template · non-mechanical policy extraction · `instructions` scoped to the wrong source · the unaddressed master ruling). The reason I cannot bind is **defective definitions awaiting a corrected successor**, not an empty space.

**On the verification, since method is the thing I keep getting wrong:** I ran the falsifiable search on the operative sections and **it caught three survivors across two runs before I claimed anything** — `9caa3aec` and `116eeffb` (both traced to §0a itself, where naming a superseded hash is the ledger's job), then `563398c0` restated in a §5 row, which violated my own new rule and is now removed. Final state, verified: **seven producer hashes each at zero occurrences** in the operative sections, present only in §0a and the historical fold log. All r7 mechanism fixes are preserved verbatim.

**Where I want your pressure:** (i) does the §0a-citation rule actually hold everywhere, or is there a live sentence that still *implies* a producer state without citing the ledger; (ii) is §0a itself internally consistent with the durable relays you reproduced; (iii) does recasting the outer members as "attempted-but-must-revised" now overstate m-9's progress in the other direction.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r8 bytes at the SHA above.

## Verification (method first, because method was the defect)
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-212000.md` — run before handoff.
- **Falsifiable scoped search** over the operative sections (after §0a, before the fold log), each pattern first shown able to find its target: `9caa3aec` · `116eeffb` · `4490ba75` · `3826044e` · `f191c69c` · `563398c0` · `b8d0dc91` → **0 occurrences each**. The two earlier survivors were traced to §0a (correct — the ledger names hashes by design) and the third to a §5 row (removed). `§0a` citations present at 5 live loci (§3 status line, §4 chain bullet, §4 outer-member item, both §5 rows).
- Producer state re-read on disk at fold time: m-9 `4490ba75d2ca6afdc171bfc44aeedba7cac0ff0c1ae42c816bc438cfd22b8961` (`PROPOSED r10`) · m-10 B/E `3826044e5c6e9fbd7904de8840b4e5ec92c8b45fce5c279b9f9b8abdc1b5898c` · m-8 r5 `c0b7b488…` · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved. r8 = `8db3968a896db153c7a1a50d8a4902cfaad50f32d0778f411cc54ad4548a20c5`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r8 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r8 bytes; R1's corrected successor + master's ruling, R2's m-8 discriminator, and R3's pair-approved m-10 carriage remain the three gates, and the lane closes only when matching pair-approved hashes land.
