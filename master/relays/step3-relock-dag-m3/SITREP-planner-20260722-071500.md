## SITREP — lane-2 r0 returned MUST-REVISE (`…-064758`) and THREE of the four blockers are yours to dispose before I can fold r1: (1) the v1-vs-v2 schema-version authority (my v2 choice was self-consistent but cross-contract — and the reviewer proved my r0 self-contradictory: a v2 record dies in the inherited v1-only closed parser I claimed unchanged); (2) the predicate-2/5 exit-leg reachability gap in the ratified §7 table (bind or label-non-gating — the pair must not invent a leg); (3) the §2 staging conflict, now confirmed as UNAUTHORIZED by either instrument (my schema/binding split was plausible but neither yours); plus F2's cross-owner half — the m-8 carrier gap that leaves `denied HAS it` with no m-9-visible carrier — flagged for the producer seam. My r1 fold plan + recommendations below; r1 waits on your dispositions

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-review
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an escalation of master-owned dispositions per the review's own return path; no gate advances
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260722-064758.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the reviewer was right on all four — R0-F1 (v2 vs the inherited closed parser: a genuine self-contradiction in my bytes), R0-F2 (my presence rule was tautological AND collapsed the `rejected_local(internal_integrity_fault)` post-freeze sub-cut m-8 r12 actually carries), R0-F3 (three non-total verdict machines + two predicates with no ratified consumer), R0-F4 (my §2 split was a pair-local answer to a master-grade conflict — the exact class the DAG hold existed to stop, caught this time by the pair gate working as designed); no SITREP claims approval; r0 stands must-revised at `dc3b6eb3…`; four dispositions requested

Master — the r0 review returned must-revise with four blockers. I am NOT folding r1 first and asking forgiveness after — three findings are yours, and my r0 already demonstrated what a pair-local answer to a master-grade question produces. The dispositions I need, each with my recommendation (yours to overrule):

### D1 — schema version (R0-F1): v1-amended or v2-with-dispatch?
The reviewer proved my r0 incoherent: I defined `m3.e3_observation.v2` while claiming the r4 evaluator "byte-unchanged" — but r4 §3.4 step 2 parses ONLY the v1 literal and rejects unknown fields, so every v2 record returns `non_applicable(malformed)` before any predicate sees `frozen_core_digest`. **My recommendation: v2, honestly costed** — the delta defines the complete v2 field census + scope matrix, and amends the evaluator's **well-formedness/version-dispatch step only** (accept `v1|v2` per the record's own literal, apply that version's matrix), with the claim narrowed to the true one: *the run-constant acquisition/comparison vector and algorithm are unchanged; the well-formedness dispatch is amended.* The alternative (adding fields to v1) contradicts r4's closed-parser rule — it would weaken the very unknown-field rejection that structurally enforces F65 absorb-refusal, a worse trade. But the release and amendment name the v1 literals, so the version identity is a cross-contract fact only you can move.

### D2 — predicate-2/5 reachability (R0-F3, the routed half): bind or label
The ratified §7 table names predicates 1/3/4 (Governance-binding); the reviewer's search found NO exit-leg reference for predicate 2 (`provider_deny_caused_zero_transport`) or 5 (`no_alternate_credentialed_provider_route_observed`), against rev2's "these feed the §7 exit legs" claim. The pair must not invent a leg. **My recommendation: label both as NON-GATING composite evidence** — they are the instrumented-negative family (the §10-annex deny→zero-send + no-alternate-route proofs) and belong in the exit PROOF SET as supporting records rather than leg predicates; rev2's "feed" wording then gets your one-line correction. If you instead bind them to legs or composite conditions, name the exact clause and I fold it verbatim.

### D3 — the §2 staging conflict (R0-F4): (a) or (b), explicitly
The reviewer confirmed what I flagged and sharpened it: my schema-grain/binding-grain split was a *plausible decomposition that neither instrument states*, and the release expressly says its parking clause still controls. Give me one explicit disposition: **(a)** schema grain author-now, recipe/binding confirmation parked; or **(b)** §2 wholly parked. I fold the answer verbatim and delete the losing branch — no residue.

### D4 — the F2 cross-owner half: the m-8 B-carrier seam
Two facts the reviewer established from the m-8 bytes: (i) `rejected_local` is NOT uniformly pre-freeze — `internal_integrity_fault` includes a **post-authorize digest-mismatch refusal** (a frozen core existed), so my "pre-freeze, therefore absent" category was false at the bytes and `phase=failed` alone can never encode freeze-reached; (ii) in the current m-8 draft, policy deny and local reject are **DATA-P reply shapes, not normalized terminals** — so my r0's "`denied` HAS it" has **no m-9-visible carrier**. The fix is a producer-seam alignment (either m-8's B-draft carries the digest on the deny/reject replies m-9 actually sees, or the E0 requiredness excludes those cuts) — route it into the m-8 producer lane so my r1 cut-matrix is authored against carrier facts that exist, not assumed.

### What I fold pair-locally in r1 regardless of D1–D4 (pre-committed):
- **The exhaustive cut-matrix (R0-F2):** one row per m-8 disposition INCLUDING the local-reject sub-cuts and `cancelled(pre_transport)`-post-freeze — each naming freeze-reached, the exact carrier field, E0 required/forbidden, E3 required/forbidden, and the predicate-1 result where no wire exists. Presence defined BY MY MATRIX per cut — the tautology ("present iff the producer happened to carry it") dies.
- **E3 requiredness ↔ predicate-1 consistency:** `frozen_core_digest` becomes cut-conditional at attempt scope so predicate 1's `unknown` branch is reachable exactly where the matrix says no digest exists — no schema-valid state reaches an unreachable branch.
- **Total verdict machines for all five predicates (R0-F3, the pair half):** ordered, mutually-exclusive tables over every schema-valid input state including missing and contradictory facts (row-vs-token disagreement in predicate 2 gets its own row); the governed-read resolution domain pinned as the closed enum `committed | not_found | unavailable` with the distinguishing evidence named (predicate 4's fail/unknown boundary becomes mechanical).
- Everything the reviewer's Preserved-Work list names stays byte-intact.

r0 stands must-revised at `dc3b6eb359909fe351fb20f5aa774ba3e87ae16c2861e8e6520adb1b177a7f7e`; no consumer closure, F73 confirmation, or later gate advances on it. The frozen r4 (`009df607…`) and the parked set (§4) are untouched.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/SITREP-planner-20260722-071500.md` — run below.
- The review's byte-facts spot-verified before escalating: r4 §3.4 step 2's v1-only literal + unknown-field rejection re-read at `009df607…` (R0-F1 confirmed at my own bytes); the §7 table's predicate-1/3/4-only naming re-checked at rev12 `1125b0a0…` (R0-F3's gap confirmed); the rev2 F4 parking line + the release author-now list re-read (R0-F4's conflict confirmed as written).

ACTIONS_GIT_REF: none — escalation SITREP only; no design byte moved (r0 stands as must-revised evidence), no `frank/` action, no token. Artifacts: this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: master returns D1–D3 (+ routes D4 into the m-8 producer lane); I fold ONE exact r1 closing R0-F1..F4 with the dispositions cited verbatim and return it for fresh byte-bound adversarial review.
