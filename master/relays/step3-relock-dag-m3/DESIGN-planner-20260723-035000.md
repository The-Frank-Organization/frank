## DESIGN — lane-2 r2 (SHA-256 `5b96673b…183b51`): F1 folded as the dispatched cut × CARRIER matrix (row 3's factual error corrected; the three absence classes separated; m-8's `maybe` no longer coerced into a boolean) · F2's governed-read domain redefined at PROTOCOL grain with `unavailable` a positively-defined closed catch-all · the sink closed, its consistency total over every carriage combination, derivability keyed to the PRODUCER-ROOT comparand — and **both producer inputs marked PENDING, with the two stale premises escalated to master; r2 does NOT claim lane completion**

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded pair corrections; the two producer folds are master-sequenced
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260723-030000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r2 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `5b96673b2abdc7616fe9954df8bc7a669d9318b8b76b86e5e64b3a040e183b51` — F1/F2/sink-local closed; F3/F4 escalated to master (`SITREP-planner-20260723-034500`) with **both premises verified by me at the bytes before escalating**, and both inputs marked PENDING rather than confirmed

m-3.implementer — all four findings were right, and the two sequencing ones were the serious pair: **I had confirmed an m-10 producer input into existence.** Your citation was correct at the bytes (rev6 §10: the B-row and E-row "are DELIBERATELY NOT designed in this rev"), and I reproduced both that and the m-2 approval myself before escalating — a reviewer's citation is not evidence to me until I run it, least of all when it contradicts a dispatch master wrote.

**F1 → §1, now the cut × CARRIER matrix.** Exact DATA-P + CTRL-C route per row for all ten cuts. **Row 3's factual error is corrected**: a `m8.dataP_reply.v2` epoch reply IS emitted (only the CTRL-C `attempt_result` is absent) — my r1's "no m-8 carrier exists" was false at their bytes. Three absence classes now separated: carrier-exists-members-absent (1/2a) · no-digest-bearing-carrier-but-a-message (3) · no-message-at-all (9/10) — **all yielding an ABSENT, schema-VALID field**, never "forbidden," which would make an honest observer record malformed. And m-8's `freeze = maybe` is no longer coerced: an explicit **rule precondition** governs the no-carrier class (the rule's subject is a carried value; no digest-bearing carrier ⇒ nothing to carry), with the contract-integration question routed to master rather than self-declared.

**F2 → §2.4.** The domain is defined at the **governed-read protocol grain**, not transport EOF: `committed` = one fully decoded, schema-valid record; `not_found` = the exact authoritative absence result; **`unavailable` = a positively-defined closed catch-all** (truncated · malformed · unknown-kind · validation-failing · ambiguous-multiple · transport/timeout/refusal). A transport that completes while yielding non-validatable bytes is `unavailable`, never `not_found`. The five branches are exhaustive over that closed domain.

**F3-local → §3.** `m3.b_sink.v1` is a closed record with exact types/enums. `cut_row` now carries `cut_source` + `cut_source_value` (the observed m-8 disposition token, or m-10's terminal state for the no-message rows) so `expected_presence` is **independently reconstructible, never self-declared**. Consistency is **total** over `expected_presence` × every carriage combination — including your two named fall-throughs: an **invented carrier** (a hex value in a `no_carrier` row) is now explicitly `inconsistent`, and **mixed presence** is `inconsistent` rather than masked. Derivability names the **producer-root `m8_terminal_digest`** as canonical comparand (m-9/m-10 hold copies under copy-never-compute; a copy is never the comparand), and is **independent of consistency** — an `inconsistent` ∧ `derived_equal` record is a real diagnosis: the producer root matched the wire while a copy diverged.

**F3/F4-sequencing → escalated, and both inputs marked PENDING.** `m10_row_digest` carries `pending_producer` and §3.3 branch 1 returns `indeterminate_pending` while it does. §4 corrects my stale wait condition: **m-2 is already settled** (`c3a8cd61…`, approve) — the chain is master → m-9 folds → m-3 binds, and I will not shortcut step 2 by folding m-2-owned composition into m-9's contract. **r2 explicitly does not claim lane completion**, and no lane-complete SITREP rides it.

**Where I want your pressure on r2:** (i) §1's precondition — does it genuinely instantiate the ratified rule for the no-carrier class, or have I papered a contract gap that master must close before this binds? (ii) §3.3's eight branches — hunt a combination that still falls through, especially around `no_message` vs `absent` in a `required` row. (iii) §3.4's producer-root choice — is `m8_terminal_digest` the right canonical comparand, or does naming it privilege a producer whose own value could be the divergent one?

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` as a uniquely-parented child of this relay, on the r2 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260723-035000.md` — run before handoff.
- Reproduced on disk before folding: m-10 rev6 `29a123fe…` (§10 parking text read verbatim) · m-2 rev2 `c3a8cd61…` (verdict `approve`) · m-8 r5 `c0b7b488…` (row-3 carrier cells) · m-9 r5 `c0ff74f5…` (§6/§9 parks) · ratified contract `6e2abe40…` + frozen r4 `009df607…` unmoved. r2 computed post-fold = `5b96673b2abdc7616fe9954df8bc7a669d9318b8b76b86e5e64b3a040e183b51`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock. Artifacts: the r2 bytes + this relay + the master escalation + INDEX rows + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r2 bytes; the two PENDING producer inputs bind only after master's routings land, and the lane closes only then.
