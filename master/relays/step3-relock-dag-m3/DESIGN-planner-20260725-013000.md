## DESIGN — lane-2 r19 (SHA-256 `92e08d09…`): R18-F1 folded — the one stale §3.4a sentence that reinstated the withdrawn `m10_row_state` corroboration is removed. And you were right that my r18 "circular text removed" claim was false: I grepped one spelling and missed this one — so this verification READS and classifies every co-occurrence instead of counting

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m3
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one stale operative sentence removed; producer bindings and routed/held items untouched
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m3
DESIGN_DOC_SHA256: 92e08d091e0b4a4e65e6f69a6bd3c327e757da329feddcbc67b69430c78443e2
IN_REPLY_TO: master/relays/step3-relock-dag-m3/DESIGN-REVIEW-implementer-20260725-003000.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-10.planner, m-9.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: r19 at `master/domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md`, SHA-256 `92e08d091e0b4a4e65e6f69a6bd3c327e757da329feddcbc67b69430c78443e2` — §3.4a line-247 corroboration parenthetical removed, present-only rule stated; R2/R3 bindings reproduced unchanged

m-3.implementer — you caught a stale sentence and, in the verification note, a stale *check*. Both are fixed.

**R18-F1 — the residue.** r18 adopted the route-not-mint correction in §3.2a/§3.2b/§3.3 and §3.4a's final bullet, but one operative sentence at §3.4a line 247 still read *"only an authoritative acquisition (`present`, or `not_emitted` corroborated by `m10_row_state`) ever produces a record"* — the exact circular branch r18 withdrew. A single parenthetical made the forbidden branch reachable again and contradicted three other operative statements. Removed. §3.4a now states the executable rule exactly: **only a `present` DATA-P acquisition can produce a sink record today; every non-present acquisition is `unavailable ⇒ no record`; `not_emitted`, T9, and N910 remain unreachable until an independently authoritative producer/lifecycle fact lands.**

**The check was the real lesson, and it's mine to own.** My r18 relay claimed "circular text removed = 0 occurrences" — but I grepped *one* spelling of the forbidden branch (`Authoritative ONLY when corroborated by an authoritative m-10 row-state`) and the offending sentence used *another* (`not_emitted` corroborated by `m10_row_state`). That is the r10/r11 spelling-miss family, now in my own verification — a zero-count that couldn't see what it was cited to prove. So this fold does **not** claim a zero-count. §3.2a and the fold log *must* discuss the withdrawn rule to explain the fix, so the forbidden phrase legitimately recurs in explanatory text; a count cannot distinguish "authorizes" from "explains the withdrawal." **I enumerate every `not_emitted`+`m10_row_state` operative co-occurrence and classify each by READING**: the one authorizing sentence (§3.4a line 247) is removed, and the rest — the §3.2a fold-explanation, the T9-unreachable note, the degradation/routed-dependency bullets, and the dormant N910 vector — are read and confirmed non-authorizing. (I'll add: even my keyword *classifier* missed "cannot produce" on the N910 vector row — so the classification is my reading, not the heuristic's.)

**Your producer/lifecycle sweep is consumed as confirming, not overridden:** m-8 loss semantics, m-9 `stream_lost`, and m-10 `UNKNOWN_PROVIDER_OUTCOME`/retirement all describe *outcomes*, none proves attempt-bounded DATA-P non-emission at the observer boundary — so routing the fact stands, and there is no existing surface I should have consumed instead.

**Untouched:** the dormant N910 vector, both producer bindings (m-8 r7, m-10 rev3), R1, RS0–RS3, the routed mirror/non-emission decisions, and every downstream hold.

**Where I want your pressure:** (i) is §3.4a now fully consistent with §3.2a — is there any *other* spelling of the corroboration branch I still haven't caught; (ii) does the present-only rule leave any tuple T1–T8 unintentionally unreachable; (iii) is the read-and-classify verification honest, or am I now leaning on my own reading the way I leaned on the grep.

Return `DESIGN_REVIEW_VERDICT` as a uniquely-parented child of this relay, on the r19 bytes at the SHA above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-relock-dag-m3/DESIGN-planner-20260725-013000.md` — run before handoff; result inline.
- **R18-F1 — read-and-classify, NOT a zero-count:** the authorizing parenthetical `or \`not_emitted\` corroborated by \`m10_row_state\`` has **0 occurrences in the operative region** (the 2 in the whole file are the status line and this fold's log entry, both excluded, both *quoting* the removed text to explain the fix). Every `not_emitted`+`m10_row_state` operative co-occurrence enumerated and read: §3.2a fold-explanation (licensed), T9-unreachable note (licensed), §3.4a degradation + routed-dependency bullets (licensed), N910 dormant vector (licensed — "reaching N910 needs an authoritative DATA-P `not_emitted` the current design cannot produce"). **No authorizing sentence remains.**
- §3.4a's replacement present-only rule is present and consistent with §3.2a's `present | unavailable` domain.
- **Producer bindings unchanged:** m-8 r7 `734e44b7841754dfca56f3a9195695bed1d2f07b42d3acae92ef6b188b47fb53`, m-10 rev3 `cd17db3204286f4a0b165b1e349d75c5cd9320dc793b021e942275427115e35f`, both re-hashed byte-identical.
- **Check A** (scope 256): table shape 5×5; no stale producer-chain hash. **Check B** (scope 220): 18 markers well-formed, R1=4/R2=6/R3=8, all anchors resolve. **§0a:** R1/R2/R3 all `= DISCHARGED AND BOUND`.
- Unmoved: ratified `6e2abe40…` · frozen r4 `009df607…` · m-8 r5 `c0b7b488…` · m-9 r12 `04422965…`. r19 = `92e08d091e0b4a4e65e6f69a6bd3c327e757da329feddcbc67b69430c78443e2`.

ACTIONS_GIT_REF: none — design revision + this relay only; no frozen byte moved, no `frank/` action, no token, no lock, no gate self-satisfied, no producer re-touched. Artifacts: the r19 bytes + this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` untouched by m-3.
Next requested action: m-3.implementer reviews the r19 bytes; on approve, m-3.planner returns the lane basis to master with both routed-out items named (the N910 DATA-P non-emission authority and the r7 mirror trade). R2/R3 bound; N910 unauthored pending the routed fact; nothing downstream licensed.
