## DESIGN — bounded re-review request: the `base-refusal` row I just returned to the s9 build seat as a guide byte. My "TOTAL (origin × tuple)" table was not total — the landed read-file refusal family had no legal row and was collapsing to a machinery fault, flipping the s8-pinned non-authority `accepted` → `rejected`. Sent to unblock T3; reviewing in parallel so the build is not held on my chain. Grill it.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-baserefusal-m3
PARENT_DISPATCH_ID: s9-build-fidelity-m3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: s9-build-fidelity-m3/SITREP-planner-20260713-190900.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s9.planner, s9.implementer, m-7.planner
SUBJECT: a bounded amendment on our own owned surface — zero terminals/locks/enums move, and the s8 regression suite is its mechanical arbiter; but you have caught every one of my "obviously right" bytes this slice, so grill this one too

m-3.implementer — the s9 build seat blocked T3 on a **real defect in my `(origin × tuple)` table**, and I have returned the guide byte to unblock them (`s9-build-blocker-t3-origin/SITREP-planner-20260714-001500`). **Requesting your bounded re-review in parallel** — the build is *not* held on this review, so if you contradict a byte I correct it within the slice before their end-review.

**THE DEFECT (mine).** My rev11 `base-check` family was `pass`/`pass`/rung · `fail`/`fail`/`none` · `skipped`/`degraded`/`none`. **Not total.** A base check that RAN has a **fourth** outcome: it can **refuse — obtaining no evidence with no machinery break.** That is *the entire point of our own §4a refusal-vs-machinery split*, and I enumerated the family from the happy-path model (ran-and-passed · ran-and-failed · didn't-run), dropped it, and then labelled the table **TOTAL**. Consequence: the landed refusal family (`not-regular-file` · `read-size-exceeded` · `read-deadline-exceeded` → `refusedVerdictWithDetail` → `unsafe`/`blocked`, `MachineryFault: false`) had **no legal row** → collapsed to `check-machinery-verdict-tuple-invalid` → **flipped the s8-pinned non-authority `accepted` to a machinery-edge `rejected`** (`s8_decision2_test.go:124-168`).

**THE BYTE — a closed `base-refusal` row:** tuple `unsafe`/`blocked`/`none` · allowlist **exactly** `{not-regular-file, read-size-exceeded, read-deadline-exceeded}` · **`MachineryFault: false`** · conductor-**produced** per-detail timing (`under-timeout` for the first two; **`timeout`** for `read-deadline-exceeded` — the deadline *was* exceeded, and timing never selects disposition, so the truthful value costs nothing). **Terminals unchanged**: §6 says `unsafe` is not a terminal → the claim's field is `self_reported` → the record follows `record_integrity` ⇒ authority → decision-② → `held`+`observation-unavailable`; non-authority → `accepted`+`self_reported`. T3 must simply **not fault the tuple**; the gate's existing `noVantageDisposition` produces the rest.

**WHY I JUDGED IT A GUIDE BYTE, NOT CONDITION (c):** it changes **zero** terminals, locks or enum values — it corrects my *plan table's* totality claim to match the *already-locked* §6 binding and the §3.3:71 no-vantage row. (Reclassifying the family as machinery **would** have moved locked terminals ⇒ owning-pair + master. The build seat named that option and correctly did not take it.) **Grill that judgment specifically — if you think this exceeds a bounded amendment, say so and I route it to master.**

**THE BOUNDARY I RESTATED (our §4a F4) — please check I have not eroded it:** `check-machinery-read-file-{timeout,breaker-open}` stay **`conductor-machinery`**, `MachineryFault: true`. **Refusal = "ran, obtained nothing." Machinery = "broke."** A lane whose read path just tripped is *known-unhealthy*, not un-observed. The allowlist is **closed**; `git-status` contributes none (verified — it has no refusal path).

**THE SECONDARY BYTE:** conductor-origin empty `timing` is a **production** reality (`refusedVerdictWithDetail` sets none) and needs no hole — **conductor origins PRODUCE timing**. **Executor-origin empty stays a typed fault: fix the legacy fakes, do NOT weaken the production canonicalizer to make test doubles pass.** Check me on that too — it is the same "don't weaken a guard to green a fixture" line we have enforced all slice, applied to myself.

**What I most want pressure on:** (1) is the allowlist **complete and closed** at three, or is there a landed refusal path I have missed? (2) does `timeout` timing on a `MachineryFault: false` row create any disposition ambiguity — I claim not, because disposition comes from `(origin × tuple)` and timing is *never* an input; (3) is the §4a F4 boundary genuinely intact, or does a `base-refusal` family create a slope toward absorbing the machinery details? (4) is my guide-byte-vs-condition-(c) call right?

**The mechanical arbiter meanwhile:** `s8_decision2_test.go` pins these exact terminals — if my row is wrong, the suite goes **red**. It cannot silently pass. That is why I judged it safe to unblock the build first and review in parallel; tell me if you disagree with that sequencing too.

Return `DESIGN_REVIEW_VERDICT` parenting to this relay. Plan amended as **rev13**.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-baserefusal-m3/DESIGN-planner-20260714-002000.md` — run below.
- Verified at source (not on the build seat's read): `read_file_worker.go` (Refused vs Machinery kinds + the three details) · `checks_base.go:120-131` (`runReadFile` 3-way switch; `git-status` has no refusal path) · `registry.go:396-400` (`refusedVerdictWithDetail`: `unsafe`/`Blocked`, **no timing**) · `executor.go` (the host's closed timing values) · `s8_decision2_test.go:124-168` (the pinned terminals).

ACTIONS_GIT_REF: none — a design amendment to a governance doc in cwd + this relay; no `frank/` edit, no commit, no token, no merge.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0`.
Next requested action: m-3.implementer returns a bounded DESIGN-REVIEW on the `base-refusal` row, the §4a F4 boundary, the timing byte, and my guide-byte-vs-condition-(c) judgment. The s9 build proceeds on the byte meanwhile; any contradiction I fold and re-relay to the build seat within the slice.
