## SITREP — master assessment of the confusion-firewall directive: ADOPTION RECOMMENDED with two interpretation rails that keep it from being misapplied; the four effective s8 locks SURVIVE on the directive's own terms (functions are confusion-class; claims already swept to confusion grade); what actually changes = an s9 evidence re-center (the adjudication rung, under ONE new named constraint: judge-model calls × the egress fence), the SEQ-1 steer to fresh-store-first, a build-time review criterion for s8, and the positioning line — the DRAFTED AMENDMENT TEXT below awaits the operator's ratification word

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: frank-threat-model-scope-assess
PARENT_DISPATCH_ID: frank-threat-model-scope
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the directive binds only on the operator's ratification of the amendment text drafted below; master adoption executes on that word; nothing is folded into the architecture-of-record, the kickoff, or the in-flight s8 lane until then
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/frank-threat-model-scope/SITREP-planner-20260711-162331.md
FROM: master.orchestrator-planner
TO: operator
CC: master.orchestrator-reviewer, m-2.planner, m-3.planner, m-1.planner, m-7.planner, m-7.implementer, m-3.implementer
SUBJECT: the scribe report is consumed with its provenance respected (m-2 surfaced, never enacted); the directive is CONSISTENT with every ruling you have made this week (F5(a) shape-not-review · the v1 ceiling with the sandbox shelved · D3-shelf · D5 · the c5 confusion-resistant relabel) — it converts this week's case law into a standing rail; my assessment, the two rails, the one real new constraint, and the ratification-ready amendment text follow

**1. Impact on the four EFFECTIVE locks — they survive, each on the directive's own terms (mechanism-function analysis, not wishful reading):** `s8-design-m2-grammar` authored the very open-vs-closed distinction the directive cites as its model — trivially aligned. `s8-design-m7-executor` r4 already states its claim as TCB hygiene, never adversarial containment, and the OS sandbox is already your shelved future feature — the directive's logic applied before the directive existed. `s8-design-m3-registry` r1b is deterministic checks + honest labeling — the directive's own part-2 floor. `s8-design-m7-config` r13: the three flagged ceremony items are, at the FUNCTION grain, confusion-class machinery — the composite digest's function is drift-detection + one-source-of-truth (a store disagreeing with its own config history IS a confusion failure, caught whether the cause is accident or anything else); the adoption record's crash-atomicity is reliability (no intermediate unbootable state) + history-truth (a raw copy would make the chain LIE about what happened); the A2 capability gate kills "an old binary confidently enforcing half the semantics" — the purest confidently-wrong-machinery case in the tree. The CLAIM texts over all of it are already confusion-graded (your F5(a) wording, FX-CFG-4's pinned "shape, not review", the §7 claim ceilings). No lock re-opens; no rip-out pass runs.

**2. The two interpretation rails the amendment must carry (they prevent the two foreseeable misapplications):**
- **Rail A — per-surface open-vs-closed, not blanket-open:** additive/open where an old reader's ignoring loses only DETAIL (a new evidence rung, a new label — the directive's own example); closed/fail-closed where an old reader's ignore-unknown CHANGES THE MEANING of acceptance (governance semantics — the A2 class, m-2's knob-vs-catalog line exactly). Without this rail, part 3 reads as license to tear out fail-closed gates — the opposite of a confusion firewall.
- **Rail B — cut by FUNCTION, not by flavor:** a mechanism is cut only when its function is adversarial-ONLY; machinery whose function is drift-detection, crash-safety, history-truth, or provenance STAYS, with its claims worded to confusion grade (the standing c5 discipline). The cut targets ceremony and claims that re-import the adversary — not reliability machinery that happens to also inconvenience one.

**3. What actually CHANGES (the real content, routed on your word):**
- **m-3 — the adjudication rung (lands as a NAMED s9 design item, not in the s8 spine):** the fuzzy-claim majority gets a new evidence class — an independent cheap model reads the claim against courier-captured artifacts and judges consistency; medium-strength, labeled as exactly that, never proof. It fits the locked registry shape (a vetted parameterized check → CheckVerdict; the executor's isolation/timeout/verdict machinery serves as-is) and does NOT touch the R2 law (R2 bars the producing model's identity as a gate input; a judge's VERDICT is an evidence artifact like a test result — the distinction stated so nobody conflates). **The ONE new named constraint: a cloud-model judge call is an EXTERNAL SEND — it sits behind the egress fence you ratified (fixture-scoped through Step-2).** m-3 designs the rung under that fence: a local judge, or design-now-activate-at-the-away-bridge. s8 stays deterministic-only (E1+E2 as dispatched — thin, proven, fence-safe); s9 IS the evidence-thicken slice, the rung's natural home.
- **s8 — an addendum rides the standing dispatch, no re-cut:** (i) the SEQ-1 steer — the directive's "thin slice on a FRESH store" answers the dispatched question's default: fresh genesis first; the bless/adoption machinery stays designed+locked and lands when an existing store actually needs upgrading (the shipped store; the v3 public-release migration posture), the PLAN names the moment; (ii) the confusion-not-malice lens as a BUILD-TIME review criterion (bounce any new adversarial-shaped addition; leave locked confusion-functional machinery alone per Rail B); (iii) no new closed surface without the Rail-A test stated.
- **m-1 — the identity-seam note (additive, no work now):** the channel-stamped FROM stays structured so it COULD later carry a signature (the D3-shelf unchanged); labeled confusion-resistant, never tamper-proof — already true, now a standing rail.
- **Positioning (the publication track, when the public flip comes):** lead "frank catches the confident hallucination honor-system tools pass through silently"; the impersonation-catch is a demo byproduct, never a designed-for robustness claim.

**4. THE AMENDMENT TEXT (ratification-ready — your word binds exactly this):**
> **Cross-domain scope amendment — the confusion-firewall directive (operator, 2026-07-11).** (1) The threat model is CONFUSION AND HALLUCINATION, not malicious adversarial agents: frank is a confusion/hallucination firewall for fallible agents, not a security product; design ceremony that re-imports the adversary is cut on sight. (2) The evidence mechanism is deterministic checks trusted-side + independent cheap-model adjudication for fuzzy claims + honest labeling; adjudication is medium-strength by design and labeled so. (3) The architecture builds extensible SEAMS (observe hook, evidence ladder, identity stamp) filled today with honest minimal mechanisms; no home-grown security primitive ships, ever; seams stay open by the per-surface rule — additive/open where ignoring loses only detail, closed/fail-closed where ignoring changes the meaning of acceptance (Rail A); mechanisms are cut by FUNCTION, not flavor — drift-detection, crash-safety, history-truth, and provenance machinery stays, claims worded to confusion grade (Rail B). (4) This REDUCES scope: no speculative frameworks, no doorways for doors nobody built. (5) Positioning leads with the confident-hallucination catch; robustness claims are never designed or pitched for.

**On your ratification word:** the amendment folds into `ARCHITECTURE.md` (the amendment path) + a `RECONCILE.md` entry + a kickoff scope-fence annotation; the three routings above issue (m-3 s9 item w/ the egress constraint · the s8 addendum · the m-1 note); the positioning line rides the publication file. Until then NOTHING moves — the s8 dispatch stands exactly as issued either way (the addendum only sharpens its named SEQ-1 question and adds the review criterion).

ACTIONS_GIT_REF: none — assessment only; no fold, no routing, no doc edit until the ratification word (disk refs: this relay + one INDEX.md row timestamped 20260711-162826).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; cwd is not a git repo (docs workspace).
Next requested action: the operator ratifies the amendment text (or edits it — the text is the artifact his word binds); master then executes the folds + the three routings in one pass.
