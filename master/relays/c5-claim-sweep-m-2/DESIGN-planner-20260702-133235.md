## DESIGN (m-2 planner-lead → implementer semantic review) — c5 claim-sweep: folded relabels + complete classified survivor list

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-claim-sweep-m-2
PARENT_DISPATCH_ID: c5-claim-sweep-m-2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — claim-text hygiene against the ratified checklist; no mechanism change; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: m-2.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-1.planner, m-7.planner
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: c5-claim-sweep-m-2
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: c5-claim-sweep-m-2/DESIGN-orchestrator-planner-20260702-132110.md

**c5 re-baseline step (c) — m-2 claim-sweep folded. Claim-text only; no mechanism change, no c1 reopen.** Applied the ratified checklist (`c5-claim-sweep-architecture/…-131320`, VP-approved): RELABEL malicious-seat-*containment* claims → confusion-resistant + **D5 residual**; KEEP trusted-engine grammar/control-flow invariants + observer-selected control properties, each **locally classified** in the mechanism text (not just a header note). **m-2.implementer: semantic review-only approve requested.**

**Test applied per hit:** *claims a malicious seat is stopped?* → RELABEL + D5 residual. *Holds by the trusted engine's grammar/control-flow, or by an observer-side selection the lane can't reach?* → KEEP (locally classified). Unsure → relabel (NO-GO safe default).

### Complete classified survivor list (full-net grep, every hit classified — matches the file)

**RELABELED → confusion-resistant + D5 residual (13):**
- **:29** "One-FROM = DMARC-by-construction" → "One-FROM rule (no lane-writable display-FROM — closes the DMARC two-FROM confusion for a *confused* agent; D5 residual)" *(CTO-given text; mirrors ARCHITECTURE §3)*.
- **:203** audit-summary echo of the same → matched.
- **:355** "sole-writer append-only store (I1)" → "governed-write append-only store (m-1 I1 — consumed ref; confusion-resistant, mirrors m-1's sweep; D5 residual)".
- **:262** fold-log "sole-writer stamps FROM/ROLE/PARENT" → "governed-write stamps … [c5: confusion-resistant, consumed m-1 ref]".
- **:111** SPF "forgery-robust by construction" → "confusion-resistant channel-stamp (a confused agent has no tool to stamp a FROM it doesn't hold; D5 residual — m-1 TCB, not the form layer)".
- **:175** lineage "forgery-robust by construction" (system-filled PARENT) → "more strongly confusion-resistant (no tool to supply a false parent; D5 residual)".
- **:184** "strengthens the lineage engine to forgery-robust" **and** "they cannot mismatch" → both relabeled confusion-resistant + D5 (no seat-fillable ROLE / no tool to free-type a false parent).
- **:227** "system-filled PARENT making … lineage forgery-robust" → "confusion-resistant (no tool to supply a false parent; D5 residual)".
- **:317** routing_ref "so unspoofable" → "so a *confused* agent has no tool to supply a false ref (confusion-resistant; D5 residual)".
- **:27** `role_from_consistency_error` "dissolve by construction … cannot disagree" → "dissolve — a confused agent has no seat-fillable ROLE to make disagree (confusion-resistant; D5 residual)".
- **:138** §10a table "dissolves by construction" → "dissolves — confusion-resistant, no seat-fillable ROLE to mismatch (D5 residual)".
- **:256** fold-log "dissolves by construction" → "dissolves — confusion-resistant".
- **:41** fill-time-authority "cannot be submitted — authority is structural" → "a *confused* agent has no tool to submit it (confusion-resistant) **and** the courier validates the pick on submit (set-membership control-flow); D5 residual: an out-of-courier shell bypass is m-1 TCB scope".

**KEEP → justified, locally classified in the text (5):**
- **:98 / :360 — R2 gate-grammar invariant** ("structurally impossible to violate"). KEEP: no predicate atom references the model field, so the property holds **regardless of seat behavior** — the licensed trusted-engine grammar-invariant class; also covers ":98 `any_row` aggregate — a single deviating row cannot *escape* the required-set" (a structural required-set property). `[c5 KEEP …]` added inline at both.
- **:185 — DI-5 observed-value fill** ("evidence-by-construction" → "observer-captured"). KEEP: **observer-selected control property** — the conductor reads out-of-lane; the observed lane cannot reach the selection. The honest-fallback `evidence_integrity = self_reported` **is** the D5 acknowledgment. `[c5 KEEP …]` added inline.
- **:43 — A-floor monotonic-MAX** ("a below-floor value … rejected"). KEEP: structural monotonic-MAX **control-flow** property; a confused below-floor pick is rejected through the courier; D5 residual for an out-of-courier bypass noted inline. `[c5 KEEP …]` added.
- **:311 — `deviation_reason_code` required-set** ("can never be omitted where policy makes it load-bearing"). KEEP: a structural `required_when` guarantee (a confused omission is caught by the required-set), **not** a malicious-containment claim. Listed; no edit (structural required-field property, not overclaim vocab).

**NOTE → non-overclaim (no action):**
- **:47** "structural **sublayer** (type/enum/required-when)" — describes the JSON-Schema vocabulary sublayer, not a containment claim.

### Confirmation of the CTO candidate rulings
Every CTO-named candidate is honored: :29 RELABEL (given text) ✓; :355 RELABEL→governed-write (mirrors m-1) ✓; :98/:360 KEEP (structural gate-grammar) ✓; :111/:184 classified confusion-resistant channel-stamp (mechanism kept, overclaim adjective relabeled) ✓; :175/:227/:256-262 handled (design/refactor properties KEEP where structural, forgery-robust adjective relabeled) ✓.

**No mechanism changed; no c1 reopen; claim-text only.** No new field, verb, gate, or on-disk change. R2 / c1-seam / CQ mechanics untouched.

Blocked: no — holding for m-2.implementer semantic review-only approve.
Scope drift risk: none — claim-text hygiene only; the mechanism sections (§4/§5/§17) are semantically unchanged.

Tests / verification: read-only claim-text sweep; all classifications E1 (each cited to its line + the ratified checklist `131320`).

ACTIONS_GIT_REF: swept `master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md` — 13 relabels (:27/:29/:41/:111/:138/:175/:184/:203/:227/:256/:262/:317/:355) + 4 inline `[c5 KEEP …]` classifications (:43/:98/:185/:360); wrote this relay + appended `master/relays/INDEX.md`; no mechanism change, no code/`pcode/`, no PLAN, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-2.implementer files the review-only `DESIGN-REVIEW` semantic approve; the orchestrator folds into the c5 status ledger. m-2.planner holds — no mechanism change, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
