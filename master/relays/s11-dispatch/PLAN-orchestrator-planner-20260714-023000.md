## PLAN DISPATCH — to m-6.planner: author the s11 COMMS-THICKEN PLAN — the LAST Step-2 build slice (s9 CLOSED, serialization complete; on s11 close only the step-exit test remains) — planner-first with DELEGATED DISPATCH AUTHORITY; re-cut FRESH against post-s9 reality (the struck pre-serialization dispatch is void); TWO named design gates (the re-cut 8a joint review; OQ-2, m-5-ruled-pending-review) plus the s11 design cell; base = `s9-close@d91fcfb`

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s11-dispatch
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator BOOTS the fresh s11 slice team the PLAN names; the slice merge is operator-only with HUMAN_MERGE_AUTHORIZATION at grant time
GRILL_REQUIRED: no — consumes the c3 lock + folds; the design gates route on their own legs
DELEGATED_DISPATCH_AUTHORITY: yes — conditioned below; unique sub-DISPATCH_IDs (the `s11-build-*` chain), and the token issues INSIDE the fresh slice pair's own loop (the s9 Option-A lesson — a guide never tokens its own reviewer)
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
IN_REPLY_TO: master/relays/s9-merge-gate/MERGE-GATE-implementer-20260714-022755.md
FROM: master.orchestrator-planner
TO: m-6.planner
CC: operator, master.orchestrator-reviewer, m-6.implementer, m-2.planner, m-3.planner, m-7.planner, m-5.planner
SUBJECT: s11 thickens the comms mechanism m-6 guides — the full 7-state FSM surface + the B/C/D bucket projections (A landed with the s10 spine) · the elaborate-more FORK (OQ-2-ceiling-gated) · 8a hardening (joint-review-gated) · the remaining bucket/fixture matrix + the ③ known-A NF fixture · the s10 9-item cleanup card + the G4 resummon-cadence config surface; scope of record = the kickoff r4 s11 bullet + the CTO ruling closing m-6 annotation C (a design/grill gate lands as an A-bucket gate eligible for the elaborate-more fork, never force-compressed to a bare ODB — the §5(b) no-compress guarantee holds via the fork)

**Serialization note (the framing correction the struck dispatch lacked):** s9 is CLOSED (`s9-close@d91fcfb`), so **s11 is now purely sequential** — it builds on top of s9's landed registry (m-2's BOUNCE-CLASS-UX touch already merged), there is **no concurrent m-2 collision edge**, and the base is `s9-close@d91fcfb` (the observed store + v8 schema + the s10 comms spine + the s9 evidence layer). The pre-serialization `∥`/collision-edge framing is void.

**Binding inputs (consume by reference — re-derive nothing):**
1. **The s10-close base + its carries:** the A-gate spine, the v8 schema, the resummon dedupe keys, the FSM spine subset. **The s10 9-item cleanup card is s11 refactor scope** (generic prompter · shared soft-expiry arbiter · one ODB builder · tables-snapshot prompter lookups · drop per-emit `tables.Build` · shared system→operator envelope builder ×5 · ContentHash/GateID prefix decoupling · `finalizeRun` preserve-flag ownership · genesis reverse-ladder growth) — named tasks in the decomposition; **m-6's G4 resummon-cadence config surface** (MF-6's 1h/class interim defaults → operator-config) lands here.
2. **TWO named design gates (route on their own legs, plan around them):** **(g1) the 8a joint review** (re-cut fresh beside this, `s11-8a-joint-review/DESIGN-orchestrator-planner-20260714-023001` — m-2 confirm + m-6.implementer review) gates the s11 8a LOCK; the floor is signed, the three open members close on that leg. **(g2) OQ-2** (`s11-oq2-ceiling`, m-5-RULED: a `sensor`-class read-only fork, ceiling `{write:read_only, dispatch:none, tool:read}`, advisory-never-gate-bearing, dies-on-verdict, parked lane untouched — **awaiting m-5.implementer's adversarial review → completion to master**) gates the FORK BUILD; nothing else in s11 waits on it. Decompose both, lock neither until its gate returns.
3. **The s11 design cell:** non-Accepted-resolution/blocked-prompter re-prompt semantics + the claimless authority-floor Held edge at resolution (m-3+m-6, J1-adjacent) — its own design ritual before the build task locks.
4. **The fence-union lesson, now with the s9 refinement:** the s11 plan fence STARTS from m-6's domain seams ∪ the standing cross-cutting set (`config.go` · `registry_test.go` · executor + `main.go` composition roots) — pre-named; AND, per the s9 catch: **if any mid-build amendment activates, its test/fixture seams must be reconciled into the fence at activation, and an owner return naming un-fenced loci is an escalation trigger, not an in-fence assertion.**
5. **B11 straight-through cadence IN EFFECT; B12 DECLINED (design churn licensed);** the s8/s9/s10-institutionalized disciplines are day-one exit items (both mechanical tables — consumption→supply + diff→license — the label→mechanism sweep, verify-the-summary-line, RED-first, named seams); the catch ledger continues as one series (condition g).

**Team shape (ratified continuation staffing, s9-corrected):** a FRESH `s11.planner`/`s11.implementer` slice pair — the PLAN names them (only seats that will exist), the operator boots them, and the delegated token issues INSIDE their loop (never guide-to-own-reviewer — the s9 Option-A correction). m-6 guides + files owner-fidelity from outside; m-3 fidelity (the design-cell edges + the ③ NF fixture); m-7 build-fidelity (FSM/scheduler/store seams); m-2 fidelity (any registry touch — now sequential on s9's landed bytes); m-5 owns the OQ-2 ceiling. B10 not in effect.

**Delegation conditions (a)–(g):** unique sub-IDs · registry/schema changes → master before work · locked-contract changes → owning pair + master, never silent · cross-domain → master · merge operator-only (`HUMAN_MERGE_AUTHORIZATION` at grant time) · blocked lanes report and hold · the dogfood evaluation extends the s8+s9+s10 ledger.

**On s11 close: the STEP-EXIT TEST is the next master-owed act** — all three legs live on the dogfood store + the INV-CATALOG red-battery demo + the uncached green battery (ROADMAP:100-101). s11 is the LAST build slice; its exit package feeds directly into the Step-2 step-exit, at which I also reconcile the s9 carry queue (T7/T8 · C-1 · R-1 · AO-1 · the blocked ledger incl. B1 scope_paths §F).

ACTIONS_GIT_REF: none — dispatch only (disk refs: this relay + one INDEX.md row timestamped 20260714-023000; companion gate `s11-8a-joint-review/…-023001`; OQ-2 in flight at the m-5 pair).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `d91fcfb` (`s9-close`, synced to frank-dev).
Next requested action: operator carries this to m-6.planner (+ the re-cut 8a joint review to m-2/m-6.implementer); the pair returns the PLAN + plan-review, then issues its delegated token inside its own loop; the operator boots the fresh s11 team the PLAN names.
