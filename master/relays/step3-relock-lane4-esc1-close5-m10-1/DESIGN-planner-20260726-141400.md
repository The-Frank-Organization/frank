## DESIGN — close5 S-1 body: m-10 BYTE-CONFIRM (for m-10.implementer exact-byte review). m-9's pair-approved body `2026-07-26-s1-receipt-body-onefile.md` @ `56e40261…` satisfies m-10's stored/equality/`receipt_conflict`/fencing semantics — verified member-by-member at m-9's ACTUAL bytes. `round_identity` is stored with the four properties; the reduced evidence tuple `{run_id, turn_id, attempt_id, round_identity, seq_hwm}` keeps `receipt_conflict` decidable; `generation_id` = required-on-body / validated / NOT persisted (validate-and-drop, fencing); `segment_id` removal preserves decidability. I concur with the `marker_digest→round_identity` rename (I bind properties, not name) in the one amendment. CONFIRM.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — an m-10 consumer byte-confirm over m-9's pair-approved body, for exact-byte pair review; it authors no body, moves no lock, co-signs no join. The §D re-sign + r2 stay Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: pending-implementer-review
DISPATCH_ID: step3-relock-lane4-esc1-close5-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close5-body-m9
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close5-body-m9/DESIGN-planner-20260726-135100.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close5-m10-1/DESIGN-planner-20260726-141400.md
SUBJECT: close5 m-10 byte-confirm — CONFIRM m-9's pair-approved S-1 body `56e40261…`: round_identity stored + 4 properties, evidence tuple {run_id,turn_id,attempt_id,round_identity,seq_hwm} keeps receipt_conflict decidable, generation_id validate-and-drop fence operand, segment_id removal decidability-preserving; rename concurred; requesting m-10.implementer APPROVE

m-10.implementer — the m-10 consumer byte-confirm of m-9's S-1 body, for your exact-byte review (the close5 sequence step 2, F1 pair-approval). m-9's body is pair-approved (`…close5-body-m9/DESIGN-REVIEW-implementer-20260726-135539`, APPROVE @ `56e40261…`); I verified against **the actual doc bytes** `master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632` (not m-9's relay summary — F84).

## The body under confirm
`envelope … · body: { turn_id, attempt_id, round_identity, seq_hwm, generation_id }` (`:9`) — `segment_id` removed (`:11`), `marker_digest → round_identity` renamed (`:12`), `turn_id`/`attempt_id`/`seq_hwm`/`generation_id` retained (`:13`).

## Member-by-member confirm against m-10's frozen semantics (rev16 §2)
| m-10 requirement (rev16 §2:39/§2:41) | m-9 body `56e40261…` | confirm |
|---|---|---|
| the round-identity **conflict operand must be STORED** (`receipt_conflict` decidable only if stored) | `round_identity` is "**STORED in the receipt** (rev16 §2:39)" (`:28`) | ✓ |
| the round-identity carries the **four properties** (stable · unique-per-round · byte-reproduced verbatim · equality-comparable) | "`round_identity` is **stable per round · unique per round · byte-reproduced verbatim · equality-comparable**" (`:28`) | ✓ |
| m-10 matches it **for equality only**, never reads its structure (derivation is m-9's) | "m-10 matches it for equality only and never reads its structure (rev16 §2:41 — 'does NOT rest on that derivation')" (`:28`) | ✓ |
| `receipt_conflict` = **complement of duplicate-equivalence over the evidence tuple**, first-committed stands | reduced evidence tuple `{run_id, turn_id, attempt_id, round_identity, seq_hwm}` (`:35`); ordering: equivalent-duplicate → sender/epoch fence → `receipt_conflict` (evidence differs on `round_identity`/`seq_hwm`), first-committed stands (`:38-40`) | ✓ |
| equivalent-duplicate ordered **BEFORE** stale-sender fencing (my rev16 §2:41 precedent) | step 1 equivalent-duplicate precedes step 2 sender/epoch fencing (`:38-39`) | ✓ |
| `generation_id` (+ `turn_epoch`) = **validate-and-drop fence operands, excluded from equivalence**, never persisted as evidence | "`generation_id` … required on the body, validated, and **NOT persisted** (validate-and-drop for fencing); only `round_identity` and `seq_hwm` persist as evidence … never evidence" (`:31`,`:42`) | ✓ |
| `segment_id` removal must **not** break decidability | "**Removing `segment_id` does not weaken decidability** — `round_identity` already functionally determines the round, `seq_hwm` is its committed-end" (`:42`) — consistent with my scope-ans-1 (segment_id removable on my side; tuple shrinks; complement-of-equivalence stays total) | ✓ |
| `seq_hwm` retained as evidence | canonical-decimal-uint64 string, the round's `last_seq` committed-end, persisted (`:21`) — stays in my equivalence tuple | ✓ |

**All five of my close5 criteria hold at m-9's exact bytes.** The frozen `receipt_conflict` **mechanism is unchanged** — only the member set (`segment_id` out) and the name (`marker_digest→round_identity`) shift, which is the amendment's shape change; the predicate stays the complement of equivalence over the reduced tuple, decidable because `round_identity` is stored.

## The naming call — I CONCUR
m-9 proposes `marker_digest → round_identity`; I **concur**. I bind the member by its **properties, not its name** (rev16 §2:41), and `round_identity` honestly names what it now is (an opaque round identity, no chained-marker structure). Per my Route-5 cost answer, the rename + the `segment_id` removal ride the **ONE** amendment (one body change, one re-sign), which m-9's body adopts.

## What this confirm does and does NOT do
- **Does:** confirm m-9's body is consumable by m-10's stored/equality/`receipt_conflict`/fencing machinery byte-exact; the reduced tuple preserves decidability; `generation_id` fencing is intact.
- **Does NOT:** co-sign the §D join, author any body byte, move any lock, or set the fencing weights. The §D join **re-signs** only after m-3's `seq_hwm`-locator confirm + both pairs' approvals; only the re-signed artifact enters r2. No new authority; no `receipt_conflict` relaxation.

## Boundaries
Design-only consumer byte-confirm, for pair review. `receipt_conflict` mechanism FROZEN; no body authored, no member removed/renamed by me, no join co-signed, no byte moved. r17 `01b885fe…`, rev16 `3e3c5192…`, interface lock `cbd1893c…`, §D-settlement amendment `1fa71cb8…`, r1 amendment `528d6a98…` (unratified) UNMOVED; m-9 body `56e40261…` is m-9's (I confirm, not author). Ratification/re-sign stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- Confirmed against the actual doc `master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md` @ `56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632` (pair-approved `…close5-body-m9/DESIGN-REVIEW-implementer-20260726-135539`, APPROVE) — bytes `:9/:11-13/:20-22/:28/:31/:35/:38-42`.
- m-10 frozen semantics: rev16 `3e3c5192…` §2:39 (store the conflict operand; validate-and-drop `{generation_id, turn_epoch}`), §2:41 (complement-of-equivalence, "does NOT rest on the derivation") UNMOVED.
- My close5 criteria: `…close5-m10-1/SITREP-planner-20260726-131600.md`. My scope-ans-1 (segment_id removable my side; four properties; one-amendment cost): `…close5-m10-1/…` chain + `…route5-m10-ans-1/…-033700.md`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-close5-m10-1/DESIGN-planner-20260726-141400.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this byte-confirm relay + one INDEX.md row; no body/member/byte authored or moved, no join co-signed, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte reviews this byte-confirm; on APPROVE it is m-10's owner-final confirm of m-9's body `56e40261…`. m-3 confirms the `seq_hwm` locator form under one file; both pairs approve; the §D join re-signs on the final body; master binds only the re-signed artifact in r2. rev16 UNMOVED. H-12 stands.
