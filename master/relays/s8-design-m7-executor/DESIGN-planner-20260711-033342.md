## DESIGN — `s8-design-m7-executor` r3: the three r2 residual-wording findings folded — the surviving-cache sentence is GONE (all v1 cache content run-scoped, §2.7-cleaned, survivor-preserved); re-execution safety is scoped to sanctioned/provided-surface effects with ambient misbehavior named the accepted operator risk; network is UNSANCTIONED, never "absent". Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-executor
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the lock stays conditional on the operator sandbox record (unchanged from r2)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-executor
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-executor-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-executor/DESIGN-REVIEW-implementer-20260711-033002.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner
SUBJECT: the r3 artifact is the same file (status r3) — three wording folds, no mechanism moved: (F1) §2.9 now says all v1 cache content is run-scoped under the workdir and follows the full §2.7 state machine, with the WHOLE workdir (cache included) preserved on a survivor fault; (F2) §2.11 + the GRILL_LOCK say re-execution introduces no sanctioned/provided-surface side effect for a CONFORMING check, ambient same-uid misbehavior under repetition = the explicitly accepted operator risk until the sandbox, the conductor's guarantee = one committed verdict/one pivot; (F3) §2.6 says network use is UNSANCTIONED by the suite-class declaration/policy — no handle or credential provided, ambient access possible and untested without the sandbox

**Fold map (one per finding; each is the exact required-fold wording realized):**
1. **F1 → §2.9:** the contradictory "cache dir survives runs" sentence is deleted; replacement: all v1 cache content is run-scoped under the workdir and follows the full §2.7 cleanup state machine — nothing survives a run; on a survivor fault the whole workdir, cache content included, is preserved for bounded diagnosis. §2.2's no-shared-cache rule now has no surviving contradiction anywhere in the doc.
2. **F2 → §2.11 + the GRILL_LOCK resolved-decisions row:** "safe because read-only" is replaced by the scoped claim — re-execution introduces **no sanctioned/provided-surface side effect for a conforming suite check** (read-only is the check contract + the provided surface, not OS enforcement); **ambient same-uid misbehavior under repetition remains the explicitly accepted operator risk until the sandbox election**; the conductor's actual guarantee stays one committed verdict / one pivot.
3. **F3 → §2.6:** network use is **unsanctioned by the suite-class declaration/policy — never claimed absent**; the executor provides no network handle or credential; ambient network access by same-uid code remains possible and untested without the operator-elected sandbox.

No mechanism, fixture obligation, boundary shape, or scope line moved in r3 — this round is purely the document holding itself to its own §1 wording rule, which is exactly what your F4-discipline review is for.

Not authorized / not claimed: fold + re-review request only — no lock (conditional on the operator sandbox record), no code, no `frank/` write, no m-3 content authored, no operator choice inferred.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-executor/DESIGN-planner-20260711-033342.md` — run before handoff (result in inline pointer)
- all three r2 findings re-checked against the artifact text before and after the folds; a residual-contradiction grep run over the doc (`survives`, `absent`, `safe because`) — zero remaining instances of the flagged wordings outside the rejected-alternatives records

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r3 edits to `2026-07-11-s8-executor-host.md` + this relay + one INDEX.md row timestamped 20260711-033342)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r3 DESIGN-REVIEW; on approve, the reviewed artifact goes to master for reconciliation with `s8-design-m3-registry` (the sandbox election + the m-3 "confined there" scoping flag riding as named items).
