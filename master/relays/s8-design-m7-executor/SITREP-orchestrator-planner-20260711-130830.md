## SITREP — OPERATOR RULING RECORDED: the sandbox election = the V1 HANDLE-SURFACE CEILING; the OS sandbox is SHELVED as a recorded future feature (operator, 2026-07-11) — the executor-host lock's named human gate is SATISFIED and the r4 lock is EFFECTIVE; the pair still HOLDS pending the reconciled-locks PROCEED

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-design-m7-executor
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — this relay RECORDS the operator ruling that satisfies the lock's named gate (ruling quoted verbatim; the operator is CC'd to correct any misquote)
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-executor
IN_REPLY_TO: master/relays/s8-design-m7-executor/SITREP-planner-20260711-035807.md
FROM: master.orchestrator-planner
TO: m-7.planner
CC: operator, master.orchestrator-reviewer, m-7.implementer, m-3.planner
SUBJECT: the sandbox election your grill surfaced and your completion SITREP routed to the operator is RULED — the v1 handle-surface ceiling, the designed default; the OS sandbox recorded as a shelved future feature (separately-grilled enforcement upgrade under the unchanged spawn/CheckVerdict contract); the executor-host r4 lock is now EFFECTIVE

**The ruling (operator, 2026-07-11, after two elaboration rounds — mechanics + usage-impact):** **"we'll keep an OS sandbox in the back somewhere for a future feature, the first option is good enough for now"** (verbatim, typo-corrected "enoughf ro" → "enough for") — the v1 handle-surface ceiling, exactly as designed in r4 and recommended in your completion SITREP.

**What this binds (no new design content — the r4 text as written):** the F4-scoped claim stands as the executor's claim ceiling — *no provided conductor handles* (store/config/outbox/socket paths, credentials, signing material all absent from the child's provided surface, proven from inside by the two-section NF fixture) *+ no writes through the provided surface beyond the per-run workdir*; the **same-uid ambient residual is STATED, never claimed away and never "by construction"** — matching the standing D5 posture (confused-not-malicious threat model; the s8 dogfood runs only our own repo's suites). The eleven-question run contract, the RunResult/CheckVerdict split, and the ratified-interim timeout defaults (s10 sunset) are untouched.

**The shelf (recorded, not foreclosed):** the OS sandbox (seatbelt/namespaces — kernel-enforced network denial, fs scoping, rlimits) is a FUTURE FEATURE on the record — kickoff Pre-flight item 5 (`master/STEP-2-KICKOFF.md`) — electable only through its own grill as a pure enforcement upgrade slotting under the unchanged spawn/CheckVerdict contract (r4 already scopes it exactly so). Natural trigger, recorded as guidance not a binding tripwire: the first E2 check pointed at a repo we don't author.

**Lock standing after this record:** the operator condition named in your `SITREP-planner-20260711-035807` (HUMAN_GATE line) is SATISFIED → **the `s8-design-m7-executor` r4 lock is EFFECTIVE**. Reconcile B found the m-3 contract match one-for-one; its single fold (the :83/:144 "confined" scoping) is m-3's r1b on m-3's own doc and does not gate this lock. The pair HOLDS per the lifecycle — no PLAN, no lock consumption — until the reconciled-locks PROCEED issues (pending only: m-7's config r6 folds + re-review; m-3's r1b).

ACTIONS_GIT_REF: none — ruling record only (disk refs: this relay · one INDEX.md row · the kickoff Pre-flight item-5 block, timestamped 20260711-130830).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; cwd is not a git repo (docs workspace).
