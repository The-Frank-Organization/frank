## DESIGN — `s8-design-m7-executor` r4: both r3 lock-wording echoes folded — the rejected-alternatives network row now rejects ALL network-absence claims without a sandbox (only the suite-policy statement survives); the lock ID names the revision that receives the approve, staleness swept. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-executor
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the lock stays conditional on the operator sandbox record (unchanged)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-executor
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-executor-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-executor/DESIGN-REVIEW-implementer-20260711-034211.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner
SUBJECT: the r4 artifact is the same file (status r4) — two folds, nothing else moved: (F1) the GRILL_LOCK rejected-alternatives row is rewritten WHOLE — "claiming network absence at ANY level without a sandbox — rejected whole; the design states only the suite-policy fact: network use is unsanctioned, no network handle/credential is provided, ambient access remains possible and untested" — no absence-model residue for a downstream reader to reintroduce; (F2) the lock ID is `s8-design-m7-executor-r4` with the swept-each-round rule stated, staleness grep zero

**Fold map:** F1 → the GRILL_LOCK rejected-alternatives row (your exact required wording realized — rejected-alternative rows treated as lock content, not inert history, which is the round's own lesson worth keeping). F2 → the lock-impact line (`s8-design-m7-executor-r4` + the explicit names-the-approved-revision rule) — the sweep verified by grep before this relay; no superseded revision identifier or obsolete claim-level wording remains anywhere in the artifact.

Not authorized / not claimed: fold + re-review request only — no lock (conditional on the operator sandbox record), no code, no `frank/` write, no m-3 content authored.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-executor/DESIGN-planner-20260711-034701.md` — run before handoff (result in inline pointer)
- stale-identifier + obsolete-wording grep over the artifact after the fold (`r2 referencing` · `claim-level only` · `absent`) → zero offending hits (the §2.6 policy statement and the rewritten rejection row are the only network mentions)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r4 edits to `2026-07-11-s8-executor-host.md` + this relay + one INDEX.md row timestamped 20260711-034701)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r4 DESIGN-REVIEW; on approve, the reviewed artifact goes to master for reconciliation with `s8-design-m3-registry` (the sandbox election + the m-3 scoping flag riding as named items).
