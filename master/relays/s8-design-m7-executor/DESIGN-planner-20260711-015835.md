## DESIGN — `s8-design-m7-executor` r1 is authored (grill run; the eleven VP questions resolved in §2; the OS-sandbox election durable with the v1 default): your DESIGN-REVIEW requested on the executor-host artifact — F4-scoped claims throughout, no c4 amendment needed (the executor composes inside the locked step-4/§2.3 contract)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-executor
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the operator-owned sandbox election travels as a durable GRILL_LOCK question with a designed default; ratifies at review/lock
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-executor
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-executor-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-executor/DESIGN-orchestrator-planner-20260710-214607.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner
SUBJECT: the artifact is `master/domains/m-7-conductor-core/design/2026-07-11-s8-executor-host.md` (r1, GRILL_LOCK §5 folded) — review whole against the dispatch's four items + the F4 discipline; the sharpest edges to grill me on: the manifest-as-identity shape (Q1/Q11 and its A-2 composition), the allowlist-not-denylist env, the exit-confirmed-reap rule (the s7b lesson applied at the host), the read-only-checks scope fence (Q10), and whether the claim wording anywhere slips past F4

**The artifact:** `master/domains/m-7-conductor-core/design/2026-07-11-s8-executor-host.md` — sole-authored per the dispatch, my FROM. The grill ran per the skill; every one of the VP's eleven enumerated questions is resolved in §2 with its source named in the GRILL_LOCK; the one genuinely operator-owned fork (the OS-sandbox election) travels as a **durable question with the v1 handle-surface claim ceiling as the designed default** — the dispatch's sanctioned path for an unanswered operator fork.

**What the design binds (the one-line map):** §1 the F4 claim verbatim — no-provided-handles + no-writes-through-the-provided-surface, ambient same-uid residual STATED separately, never "by construction", OS containment not claimed; §2 the eleven-question run contract — run manifest identity (pinned code version + input digests), per-run workdir as sole writable surface, deny-by-default env allowlist, group-kill with **exit-confirmed reap before disposal** (the s7b CRASHPOINT-KILL-RETURN lesson applied at the host side), capture-side ceilings with marked truncation, read-only checks with no operator-gate plumbing, no-verdict faults dispositioned by NF-S6's two-axis rule at the OUTER gate (the executor adds zero disposition authority), and verdict binding via deterministic content-hash keys composing with A-2 replay/coalesce; §3 the NF fixture with **two separately-labeled sections** (handle-surface absence asserted; the ambient residual stated — the fixture fails on a leak OR on overclaiming wording) + five companions FX-EXE-2..6; §4 the named out-of-scope lines. **No c4 amendment**: the executor lives inside the locked §3 step-4 hook + §2.3 timeout contract.

**Boundary contract, named not authored:** `s8-design-m3-registry` owns which checks are suite-class, each check's probe intent + declared inputs, and the timeout/operator-gate policy inputs; this design owns only what the executor provides, withholds, binds, and reports.

**Your review asks:** F4 fidelity everywhere (hunt for any wording that claims more than §1 allows — that hunt is the review's sharpest value); the eleven answers against the dispatch's verbatim list; the Q10 fence (no side-effecting class, new round if m-3 ever wants one); the disposition path (nothing new — NF-S6 consumed, not extended); whether any m-3 content got authored here. Verdict per the standard enum.

Not authorized / not claimed: design authoring only — no lock (your approve + master's reconcile with `s8-design-m3-registry` follow), no code, no `frank/` write, no m-3 half authored, no sandbox election pre-decided (the default is reversible at ratification).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-executor/DESIGN-planner-20260711-015835.md` — run before handoff (result in inline pointer)
- grill grounding this session: NF-S6 (`:156`) + §3 step 4 (`:72`) + the §8.4 absence-set idiom + §2.3; the A-2 replay machinery (the s6 set); the s4 frame-ceiling disclosure pattern; the s7b CRASHPOINT-KILL-RETURN mechanism record (the kill-vs-exit rule this design imports); the D5 residual wording as locked
- the operator fork posed in-session (no response — recorded durable per the dispatch's sanctioned path)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the design doc `2026-07-11-s8-executor-host.md` + this relay + one INDEX.md row timestamped 20260711-015835; `frank/` read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session; the implementer returns `PHASE: DESIGN-REVIEW` on the artifact (same DESIGN_DOC_ID, parenting here); on approve the reviewed doc relays to master for reconciliation with `s8-design-m3-registry` — the s8 PLAN consumes only the reconciled locks.
