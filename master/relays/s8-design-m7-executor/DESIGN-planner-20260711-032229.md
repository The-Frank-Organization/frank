## DESIGN — `s8-design-m7-executor` r2: all five r1 findings folded — v1 accepts suite-class only with m-3's taxonomy intact (F1); provided-surface wording with the shared cache REMOVED (F2); exactly-once narrowed to one-committed-verdict with at-least-once execution admitted (F3); the post-kill state machine with bounded group-death verification (F4); the RunResult/CheckVerdict split returning only m-3's closed shape (F5). Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-design-m7-executor
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — per your Operator Gate: the reconciled DESIGN_LOCK is CONDITIONAL on an operator record ratifying the v1 residual (or electing the sandbox round); your approve covers technical soundness only
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-executor
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-executor-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-executor/DESIGN-REVIEW-implementer-20260711-022342.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner
SUBJECT: the r2 artifact is the same file (`master/domains/m-7-conductor-core/design/2026-07-11-s8-executor-host.md`, status r2) — the fold map is one-to-one with your five findings; I read the approved m-3 design's cited sections whole before folding (`:32-46`, `:81-83`, `:107-144`), and one reconcile flag rides to master rather than any silent authoring of m-3's text

**Fold map (one per finding):**
1. **F1 → §2.10 + §4:** the claim "no side-effecting class exists" is WITHDRAWN — m-3's approved taxonomy (`base | suite | side_effecting`, default-deny + static engine-config allowlist) stands as theirs, unrestated and unaltered. The v1 host **accepts `spawn()` only for `class: suite` (read-only, `executor_required`)**; a `side_effecting` entry is refused upstream by m-3's selection validation (their `unsafe` path), and one reaching `spawn()` anyway gets a typed host refusal — defense-in-depth, not policy. No operator-gate plumbing here.
2. **F2 → §2.1/§2.2 + the GRILL_LOCK:** identity separated from enforcement — the manifest digests immutably identify **pre-spawn bytes**; staged copies are admitted ambiently mutable (§1.3 residual); **the shared per-executor cache is REMOVED from v1** (rejected-alternatives row: a second provided writable surface + a cross-run contamination channel; a future one requires content-addressing + manifest declaration as a design change); binding wording rule: "the provided writable surface", never "only writable"/"writes confined" simpliciter. **Reconcile flag carried to master (not authored into m-3's doc):** m-3's §4 "writes are confined there" sentence inherits this provided-surface + stated-residual scoping at reconciliation.
3. **F3 → §2.11 + FX-EXE-3:** the honest property — **at-most-one COMMITTED verdict per manifest** (A-2 in-flight coalescing + completed-outcome replay; one pivot ever) with **at-least-once EXECUTION admitted under crash** (spawn-then-crash-before-commit re-executes after recovery), safe because v1 runs are read-only — your named lean fit. The reservation protocol is a rejected-alternatives row (machinery whose only beneficiary is a class v1 refuses). FX-EXE-3 now asserts the committed-record count across all three legs (concurrent duplicates · completed re-dispatch · crash-before-commit), never "run count 1".
4. **F4 → §2.7 + FX-EXE-2:** the explicit post-kill state machine — group SIGKILL → reap the direct child → **bounded `kill(-pgid, 0)`-until-`ESRCH` verification (re-signaling each round)** → only-then cleanup/verdict; a bound-expiring survivor ⇒ typed no-verdict machinery fault with the **workdir PRESERVED** and the pgid reported; platform assumptions stated (POSIX process groups, darwin/linux; reap-before-verify bounds the pgid-reuse window, the post-reap residual noted). "No orphan" is never claimed from child reap alone.
5. **F5 → §2.11 + FX-EXE-6:** two objects, one boundary — the host-internal **`RunResult`** (manifest, exit class, ceiling-marked captures, duration, kill path) never crosses; the return is **byte-shape-exactly m-3's closed `CheckVerdict`** (`check_id, claim_ref, outcome, rung_reached, predicate, timing, failing_detail`), no `output ref`, no path-bearing field; m-3's conductor-side §6.1 redaction pass stands between the verdict and anything seat-deliverable (consumed, not re-authored). FX-EXE-6 gains the planted-path-in-`failing_detail` leg co-owned with m-3's fixture.

**Your Operator Gate — folded the same way as config F4:** the GRILL_LOCK's lock-impact now states your approve = technical soundness only; the reconciled DESIGN_LOCK becomes effective only on an operator record ratifying the v1 handle-surface ceiling (or electing the sandbox design round first).

Not authorized / not claimed: fold + re-review request only — no lock (conditional), no code, no `frank/` write, no m-3 content authored (the one interaction is a named reconcile FLAG), no operator choice inferred.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-design-m7-executor/DESIGN-planner-20260711-032229.md` — run before handoff (result in inline pointer)
- read whole for the fold: your five findings; the approved m-3 design's cited sections (`2026-07-10-s8-check-registry-probe-design.md:32-46` taxonomy, `:81-83` side-effecting gate, `:107-144` CheckVerdict + §6.1 hygiene + the §7 boundary contract)
- the s7b CRASHPOINT-KILL-RETURN mechanism record re-consulted for the F4 state machine's reap-before-verify ordering

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the r2 edits to `2026-07-11-s8-executor-host.md` + this relay + one INDEX.md row timestamped 20260711-032229; `frank/` read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the r2 DESIGN-REVIEW; on approve, the reviewed artifact goes to master for reconciliation with `s8-design-m3-registry` — with the sandbox election and the m-3 "confined there" scoping flag traveling as named items in the reconcile package.
