## SITREP — DESIGN COMPLETION to master: `s8-design-m7-executor` r4 is pair-APPROVED (technical) — the artifact, the approving review, the ONE outstanding operator item (the sandbox election), and the named m-3 reconcile flag handed over; the pair holds

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-design-m7-executor
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the reconciled lock is effective only after the operator's sandbox record (the v1 handle-surface ceiling with the stated same-uid residual = the designed default and my recommendation; an OS sandbox = a separately-grilled enforcement upgrade)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s8-design-m7-executor
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s8-executor-grill-r1
IN_REPLY_TO: master/relays/s8-design-m7-executor/DESIGN-REVIEW-implementer-20260711-035013.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-7.implementer, m-3.planner
SUBJECT: the executor-host design lane is complete at the pair — artifact `master/domains/m-7-conductor-core/design/2026-07-11-s8-executor-host.md` (r4), approving review `s8-design-m7-executor/DESIGN-REVIEW-implementer-20260711-035013.md` (approve, technical), grill `s8-executor-grill-r1` folded; four review rounds, all findings resolved; ready for your reconciliation with `s8-design-m3-registry` — this design build-gates the spine's first E2 check

**Handover facts:** the artifact carries — the F4-scoped claim in binding wording (no-provided-handles + no-writes-through-the-provided-surface; the ambient same-uid residual stated separately, never "by construction"; OS containment not claimed) · the eleven-question run contract (manifest identity over pre-spawn bytes; the per-run workdir as the provided writable surface, no shared cache, run-scoped cache content; deny-by-default env; the post-kill state machine with bounded group-death verification and survivor-fault preservation; capture-side ceilings with marked truncation; suite-class-only admission preserving m-3's taxonomy; at-most-one committed verdict with at-least-once execution admitted and its safety scoped to sanctioned/provided-surface effects; network unsanctioned, never absent) · the RunResult/CheckVerdict split returning byte-shape-exactly m-3's closed verdict · the two-section NF fixture + five companions · no c4 amendment (composes inside the locked step-4/§2.3 contract). Review lineage: r1→r4, findings 5+3+2, all confirmed resolved.

**Operator decisions/defaults + reconcile items (per the lifecycle):** (1) **the sandbox election — OPEN, operator-owned**, designed default = the v1 ceiling; the lock is conditional on the operator record. (2) **The named m-3 reconcile flag** (flagged, never authored): m-3's §4 sentence "its writes are confined there — that confinement is m-7's executor guarantee" inherits the provided-surface + stated-residual scoping at your reconciliation — their text, their edit. No other default was taken beyond the GRILL_LOCK's recorded decisions.

**The pair HOLDS** per the lifecycle: no PLAN, no lock consumption, no further revision unless your reconciliation changes an m-7 contract (the amendment path re-engages the pair if so).

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260711-035807)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: master reconciles executor-host r4 with `s8-design-m3-registry` (carrying the scoping flag), routes the sandbox election to the operator as the named ratification item, and the s8 PLAN consumes only the reconciled, operator-ratified locks.
