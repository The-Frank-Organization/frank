## DESIGN — m-6 c5 claim-sweep: 3 relabels + 1 DI-1 mirror + 1 tighten folded; full classified survivor list → m-6.implementer semantic approve

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-claim-sweep-light
PARENT_DISPATCH_ID: c5-claim-sweep-light
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — claim-text hygiene against the ratified checklist; no mechanism change, no locked-contract reopen
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-6.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.planner, m-4.planner, m-5.planner, m-7.planner
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: c5-claim-sweep-light
OWNER: m-6 (Human Surface & Scheduler) — owns its own claim text + classified survivor list
IN_REPLY_TO: c5-claim-sweep-light/DESIGN-orchestrator-planner-20260702-132139.md

m-6's own claim-text fold + **complete classified survivor list** against the ratified checklist (`c5-claim-sweep-architecture/DESIGN-orchestrator-planner-20260702-131320`, VP-approved) and D4/D5 (`GRILL-LOCK-deployment-fork-2026-07-01.md:43-49`). **Claim-text only — no mechanism change, no locked-contract reopen** (§2B). I ran my **own** full-net grep (the census's 2-hit starting point undercounted; the real net over the current doc, post-c4 edits, is below). **m-6.implementer — review-only semantic `approve` requested.**

The census predicted "likely a clean KEEP." It was close but not clean: **3 genuine relabels** surfaced where my text borrowed system-level "sole-writer / sole-external-send" claims from m-1/m-3 (exactly the class the r2 checklist-hardening added). Folded, claim-text only.

### RELABELED → confusion-resistant + D5 residual (3)
1. **§2 :48** — "`failing_edge = egress` at the **sole-external-send chokepoint**" → "**conductor-governed egress chokepoint** (governance-surface, not system-level sole-egress — D5 residual)." *(Mirrors m-3 :118 / m-7 §9 / ARCHITECTURE :154.)*
2. **§4 :75** — "the conductor's **sole-writer append-only store** IS the durable checkpointer" → "**governed-write append-only store** … (governed-write per m-1's c5 relabel — confusion-resistant + D5 residual)." *(Consumed m-1 ref → mirror m-1's governed-write relabel, exactly as the census directed for m-3 :20.)*
3. **§11 :161** — "The **sole external send** is the §4 away bridge" → "The **only conductor-governed external send** is the §4 away bridge (governance-surface claim, not system-level sole-egress — D5 residual: same-uid shell/curl bypass out of scope)." *(Same scoping ARCHITECTURE :154 / m-7 §9 use.)*

### MIRROR-SCOPED (1) — consumed m-1 DI-1 ref
4. **§4 :89** — "**DI-1** (nonce-burn = an atomic conductor-owned store append; **lanes have no write path**)" → "…**confused lanes have no store-write *tool*** — D5 residual: same-uid direct store write out of scope, per m-1's c5 relabel." *(DI-1 is m-1-owned; I state it in m-1's confusion-resistant + D5 sense rather than as malicious-containment.)* The DI-2 "signing key OS-isolated from lanes" (:90/:174) is m-1's owned PLAN-carry property — cited, not re-relabeled here; it rides m-1's DI-2 framing.

### TIGHTENED (1) — bare "structural" → named grammar basis
5. **§2 :42** — "the alert-vs-notification gate is **structural**" → "a property of the **bucket grammar** (it keys on the locked writer + TO/CC direction, not on seat assertion)." Not a malicious-containment claim; naming the grammar basis removes the by-construction flavor. *(KEEP-class, worded honestly.)*

### KEEP (justified — never blind-relabel; each holds by grammar / policy / no-tool, not malicious containment)
- **§2 :44/:98, §4 :80 — "non-suppressible / cannot be silenced" (A-gate / `held` A-lane).** KEEP — the **monotonic-floor** structural property (config may raise, never lower; MAX wins). Exactly the class the checklist KEEPs at ARCHITECTURE :125 ("a confused floor-lowering attempt is rejected"). Not malicious containment.
- **§4 :62/:66/:82, §9 :146 — "never auto-approve / never escalate to a verdict / never auto-resolve" (J1).** KEEP — J1 authority policy (`hold_and_resummon`); a conductor-policy property, not a seat-containment claim.
- **§3 :57 `completed_proof`, §5 :98 "proof requirement".** KEEP — a **field name** and a **design-proof obligation**, not security "by-construction" vocab.
- **§4 :87/:89/:90, §11 :161, §12 :177 — "fail-closed" (egress / verify disposition).** KEEP — a trusted-engine **control-flow disposition** (block-on-uncertainty), rides m-3 §7 / m-1 `verify`; honest as-is.
- **§3 :66/:67, §6 :109/:111, §9 :144, §10 :155 — fork/side-question "read-only / tool-blocked / must not mutate / never injects / write-capable declined".** KEEP — **confusion-resistant no-tool** design properties (the fork/side-question has no mutate/inject tool) + an operator design decision; not malicious containment.
- **§4 :88 — "hard per-archetype never-bridge ceiling" (reserved m-5 hook).** KEEP — a reserved-future design constraint, not a claim.
- **§7 :124, §6 :111 — "never gate-bearing" (advisory/side-question).** KEEP — a **gate-grammar** property (advisory records carry no gate), R2-class.
- **§4 :88 — "away_bridge_eligible … A-bucket ∧ opt-in ∧ egress-clear".** KEEP — a policy predicate, not a claim.

**Full-net grep result: every hit above classified; zero unaccounted raw overclaims remain** (post-fold re-grep of `sole[- ]writer|sole external|sole-external-send|by construction|structural|tamper|non-lane-writable|no write path|forgery-robust|unbypassable` → 0 unclassified).

### CQ-status / requirement mapping
1. Owner planner folds/confirms relabels — **done** (3 relabel + 1 mirror + 1 tighten; claim-text only, no mechanism change). ✓
2. Owner classified survivor list — **done** (above; full-net, every hit RELABEL / MIRROR / TIGHTEN / KEEP-with-reason). ✓
3. Owner implementer review-only semantic approve — **requested** (m-6.implementer).

**Invariants preserved:** no mechanism changed (buckets/ODB/park-wake/away-bridge/interjection all unchanged); no locked-contract reopen; the KEEPs are the licensed classes (monotonic-floor grammar, J1 policy, R2 gate-grammar, confusion-resistant no-tool); the relabels only scope borrowed system-level claims to governance-surface + D5 residual. c3 design-lock-readiness intact (this is claim-text hygiene, not a design change).

Blocked: no — holding for m-6.implementer semantic approve.
Scope drift risk: none — claim-text only; the A-floor table / CQ token folds from c4 are untouched.

Tests / verification: read-only claim-sweep; all claims E1 (ratified checklist `131320`; D4/D5 `GRILL-LOCK…:43-49`; post-fold re-grep clean).

ACTIONS_GIT_REF: folded `master/domains/m-6-human-surface-scheduler/design/2026-06-30-v3-human-surface-scheduler-design.md` (§2 :42/:48 relabel+tighten; §4 :75/:89 relabel+mirror; §11 :161 relabel — claim-text only); wrote this survivor-list relay + appended `master/relays/INDEX.md`; no mechanism change, no other-domain-doc edit, no code/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-6.implementer files a review-only `DESIGN-REVIEW` semantic approve; the orchestrator folds the m-6 closure into the c5 status ledger. m-6 holds.
