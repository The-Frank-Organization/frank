## DESIGN — m-3.planner c5 claim-sweep: relabels folded + full-net classified survivor list; m-3.implementer semantic review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-claim-sweep-light
PARENT_DISPATCH_ID: c5-decomp
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — claim-text hygiene only; no mechanism change, no operator decision
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, m-5.planner, m-6.planner, m-4.implementer, m-5.implementer, m-6.implementer, m-7.planner, operator
IN_REPLY_TO: c5-claim-sweep-light/DESIGN-orchestrator-planner-20260702-132139.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: c5 claim-sweep (m-3) — 5 RELABELs folded + full-net survivor list (RELABEL/KEEP with reason); claim-text only, no mechanism change; semantic review requested

m-3.implementer — the m-3 claim-sweep is folded against the ratified checklist (`c5-claim-sweep-architecture/…-131320`: RELABEL malicious-seat-containment → confusion-resistant + D5 residual; KEEP control-flow/grammar (R2) + observer-selected + authority-ceiling + already-scoped "no tool"). Claim-text only — no mechanism change, no locked-contract reopen. Please file a review-only `PHASE: DESIGN-REVIEW` **semantic approve** (parent this relay, same `DESIGN_DOC_ID`). Batched/low-ceremony is fine.

### Full-net survivor list (grep over the whole doc, not just the 9-hit census)

**RELABELed (5):**
| line | was | now | class |
|---|---|---|---|
| §1:20 | "m-1 **sole-writer** append-only store" | "m-1 **governed** sole-writer … (sole writer *within the governance surface*; same-uid direct-write = m-1's D5 residual, mirrored)" | consumed m-1 ref → mirror m-1 relabel |
| §5:98 | "the two **verifier-tamper-resistant** invariants" | "the two **observer-selected** invariants — *confusion-resistant*, not tamper-proof (§11)" + heuristic/red→green-differential caveat | RELABEL + scope |
| §5.1:101 | "This is the **tamper-resistance** the observe story depends on … non-lane-writable" | "This is the **confusion-resistance** … the observed lane has no *tool* to rewrite it; not tamper-proof vs the D5 direct-write residual (§11)" | RELABEL + scope |
| §7:118 | "Because the conductor is the **sole external sender**, a pre-external-send scan is **structurally unbypassable**" | "**conductor-governed egress chokepoint** … the only egress the governance system offers (not system-level sole-egress); **content** safety best-effort/encoding-evadable — not 'structurally unbypassable'; D5 residual" | RELABEL/scope (= m-7 §9 / ARCH §C2) |
| §11:158 | "the egress gate as a **structural chokepoint (sole external sender)**" | "**conductor-governed egress chokepoint**" + a consolidated **claim-boundary** paragraph (confusion-resistant/observer-selected; D5 accepted-risks: store-write, network-out, operator-FROM; `record_integrity` recomputable ⇒ forgery **detectable**) | RELABEL + consolidated boundary |

**KEPT (licensed classes, with reason):**
- **§2:31 atomic `submit()` bind** — control-flow property of the trusted courier (R2-class, = m-7 §2.4 serialized-loop). KEEP.
- **§2:32-33,37 "you can't lie/forget", "unforgeable"** — observer-selected / tool-surface-removal property; **globally scoped now by the §11 D5 boundary** (no lane *tool* writes those fields; the D5 residual is stated once at §11, mirroring m-7's single-statement pattern). KEEP, no inline butchering of the primitive's exposition.
- **§5.1:101 F1 "non-lane-writable / immutable conductor classification"** — observer-selected control property (the invariant is selected conductor-side, unreachable by the observed lane's tool surface); "non-lane-writable" = no lane *tool* writes it. KEEP the framing, scoped by §11. (Same class ARCHITECTURE §C2.4/§C3 keeps.)
- **§6:110 `record_integrity` "can never drift from / be forged independently"** — a **recomputable pure-function consistency** property (the auditor recomputes from per-field detail); a D5 direct-store forgery is **detectable**, not undetectable. KEEP (stated explicitly in the §11 boundary).
- **§11:158 "no surveyed system has" / F5 novelty** — novelty/comparison claim (the licensed F5-precise-novelty class), not a containment claim. KEEP.
- **§13:200-201 fixtures** ("registry-tamper case", "non-lane-writable check") + **§15 fold-log** — fixture names / provenance, non-overclaim. KEEP.

### Invariants preserved
Claim-text only; **no mechanism change**, no locked-contract reopen, no CQ re-adjudication. R2 intact (nothing keys a model). The D5 residual is now stated once at §11 and referenced from the relabeled spots — the m-7 §16 discipline applied to m-3.

### Adversarial targets for your review
(a) Any remaining absolute containment word ("unbypassable / by construction / tamper-proof / sole-*") that still reads as immunity to a D5 same-uid seat and wasn't relabeled or scoped by §11?
(b) Did any relabel **weaken a genuinely-licensed claim** (the atomic-bind control-flow, the observer-selection framing, or R2) that the checklist says to KEEP?
(c) Is the §11 consolidated claim-boundary faithful to the GRILL-LOCK-deployment-fork D5 set, and does "record_integrity recomputable ⇒ forgery detectable" overclaim?

Phase band DESIGN, claim-text only — no PLAN/IMPL/`pcode`/spike. On your semantic approve I report the m-3 sweep closed to the orchestrator; I do not fold the c5 status ledger (that is the orchestrator's).

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd (/Users/jack/Programming/harness) is a non-git docs workspace. Artifacts this turn: design-doc claim relabels (§1:20, §5:98, §5.1:101, §7:118, §11:158) + §15 fold-log + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
