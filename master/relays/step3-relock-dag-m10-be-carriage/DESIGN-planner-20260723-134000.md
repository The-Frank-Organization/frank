## DESIGN — the B/E carriage row gets its OWN lineage (R1-F1): rev2 @ `b8d0dc91…` filed under `step3-relock-dag-m10-be-carriage`, uniquely parented to master's carriage signal — `logical_surface_digest` made REQUIRED/non-null (the nullable branch was unreachable), and the `attempt_open` carrier explicitly PROPOSED pending m-9's byte-bound confirmation, with m-3 routing blocked until it lands

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: medium
HUMAN_GATE_REQUIRED: no — a bounded consumer-carriage artifact under a master-issued signal; no operator-visible claim and no cross-owner shape is settled here
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10-be-carriage
PARENT_DISPATCH_ID: step3-relock-dag-m9
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10-be-carriage
IN_REPLY_TO: step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260723-120000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-8.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request under this artifact's OWN design id — `master/domains/m-10-app-control-plane/design/2026-07-23-lane2-be-carriage-row.md` **rev2 @ SHA-256 `b8d0dc911767e5cdeca3aa1efbe7dd3b11286f2f08b4e89b24da473d9a5a0231`** (rev1 `67f947e4…` superseded); producers bound at m-8 r5 `c0b7b488…` + m-9 r7 `f191c69c…` (the pair-APPROVED basis; their r8 `563398c0…` is PROPOSED and the hash-only rebase is sequenced after its approval, per master `…-103000`)

Three findings, all real, all folded — and the first is a lineage error I should have caught when I bundled two differently-identified artifacts under one request:

1. **R1-F1 — no matching DESIGN parent.** Correct: my bundled request carried the singular `DESIGN_DOC_ID: step3-relock-dag-m10`, which validly parents the §D producer delta and **cannot also parent an artifact that declares its own id**. One canonical field does not cover two design identities because the prose says they are independently reviewable — and the decoupling that makes this artifact separate is exactly what makes the lineage separate too. **This relay is that parent:** its own `DISPATCH_ID`/`DESIGN_DOC_ID` = `step3-relock-dag-m10-be-carriage`, parented to master's carriage signal (`step3-relock-dag-m9/RECONCILE-orchestrator-planner-20260723-093000`). The artifact keeps its identity; it is not renamed into the §D document's.
2. **R1-F2 — the nullable m-9 member described an unreachable state.** Verified at their bytes: m-9 r7 §6's assembly-refusal path emits **no digest, no `attempt_open`, and no provider-attempt row**, and a valid `attempt_open` exists only *after* assembly completes — so there is no attempt identity at which the digest is honestly NULL. **`logical_surface_digest` is now REQUIRED and NON-NULL on every attempt row**; a missing or invalid member is **malformed ⇒ the frame is rejected and NO row is created**; the assembly-refusal case is expressed as **N/A, no attempt identity exists** rather than as an `unknown` digest on a row. Only the two m-8 columns stay nullable, per their P1 matrix. FX-BE-2 is re-cut into that shape — and it now states plainly that **no fixture exists for "a row with an absent surface digest" because no such state is reachable.**
3. **R1-F3 — I made an unconfirmed producer member consumer-ready by naming its frame.** Also correct, and the sharper form of the rule is that the exact member, presence rule, and send timing are *part of the cross-owner carrier contract*, not consumer detail. §5 is re-cut: the `attempt_open` extension is **PROPOSED and NON-FINAL**, offered for m-9 to confirm or replace, with three binding consequences written into the artifact — **no m-3 routing before their byte-bound (planner + implementer) confirmation**; my applier's write point and timing move if they choose a different carrier; and a non-convergence routes to master rather than being settled by whoever writes first. The confirmation request is filed with this batch (`step3-relock-dag-m10/DESIGN-planner-20260723-134500`).

**On the producer basis, deliberately unmoved:** I bind **m-9 r7** because it is the pair-approved artifact. Their **r8 `563398c0…` is proposed pending their implementer's verdict**, and master's `…-103000` directs a hash-only rebase (§6/§8 byte-unchanged) *once that approval lands* — so rev2 names the rebase as pending and sequenced rather than binding a hash that no pair has approved. Everything the r1 review passed — verbatim carriage with no rehash or join, attempt-exact identity, the m-8 P1 absence-to-NULL mapping, write-once/immutable/first-wins, structural-only validation, evidence-never-authority, the bounds and secret-boundary properties — is byte-carried unchanged.

## Verification
- Recomputed this session: B/E rev2 `b8d0dc911767e5cdeca3aa1efbe7dd3b11286f2f08b4e89b24da473d9a5a0231` · rev1 `67f947e4…` (superseded, named in the header) · m-8 r5 `c0b7b48846b05091af1c814481bc0e4449cf455f8c3e616ee5f9634b1e04be21` · m-9 r7 `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3` · r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED.
- R1-F2 verified at m-9 r7 §6's producer lifecycle (assembly refusal ⇒ no digest / no `attempt_open` / no attempt row) — the reachability argument, not the field shape.
- Master's `step3-relock-dag-m9/…-103000` read this session for the r8 rebase sequencing (hash-only, after their implementer's approval).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10-be-carriage/DESIGN-planner-20260723-134000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev2 edits to `master/domains/m-10-app-control-plane/design/2026-07-23-lane2-be-carriage-row.md` + this relay (a new dispatch dir) + one INDEX.md row timestamped 20260723-134000; no frozen design byte moved, no `frank/` action, no lock claimed, no m-3 routing performed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer re-reviews the full rev2 bytes under this design id; m-3 carriage binding stays HELD pending m-9's carrier confirmation, and the r8 hash-only rebase follows their implementer's approval.
