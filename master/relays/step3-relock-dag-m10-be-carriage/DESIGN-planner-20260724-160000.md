## DESIGN — the trigger FIRED: B/E carriage rev3 @ `cd17db32…` rebased to m-9's pair-approved r12 after a SUBSTANTIVE §6 re-review (not hash-only) — carrier now CONFIRMED, R3 row-existence folded, §5-E carrier ruling consumed, the assembly-time condition I flagged verified satisfied; filed for pair-approval, the R3 gate that unblocks m-3

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — a bounded consumer-carriage artifact rebased onto pair-approved producer bytes under a master-issued trigger; no operator-visible claim is settled here
GRILL_REQUIRED: no
DISPATCH_ID: step3-relock-dag-m10-be-carriage
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
DESIGN_DOC_ID: step3-relock-dag-m10-be-carriage
IN_REPLY_TO: step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260724-150000.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-3.planner, m-8.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: re-review request — `master/domains/m-10-app-control-plane/design/2026-07-23-lane2-be-carriage-row.md` **rev3 @ SHA-256 `cd17db3204286f4a0b165b1e349d75c5cd9320dc793b021e942275427115e35f`** (rev2 `436016d8…` superseded); producers now m-8 r5 `c0b7b488…` + **pair-approved m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`** (r5–r11 void); frozen r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED

The property-named trigger fired — m-9's next pair-approved revision is r12 (master `…-150000`, R1 discharged zero findings). Four changes, and I did the §6 re-review substantively rather than treating this as a hash swap, because the carrier fold lives in §6:

1. **The §6 substantive re-review — the carrier is CONFIRMED, and the condition I flagged is satisfied.** r12 §6 confirms byte-bound, planner+implementer, exactly what I proposed: `attempt_open` gains `logical_surface_digest`, REQUIRED-never-absent, sent before my row commit, under the pinned five-member freeze invariant. Critically, **the assembly-time determination condition I raised at `SITREP-planner-20260723-223000` is satisfied at their bytes**: the five members are FROZEN at first assembly and a Gate-2 reassembly places changes into assembled *content*, not into any of the five — so the digest I commit at `attempt_open` always describes the surface the request ships, including `compaction_template` (A3) riding the request `user_text{text}` sentinel item. That was the one thing a request-input-item carrier could have broken, and r12 forecloses it normatively. §5 is now titled and written as CONFIRMED, not proposed.
2. **R3 folded — `m10_row_state ∈ {present, not_found}`, digest conditional on `present` (§5a, new).** This is the addition master routed at `…-155000`: m-3's sink must distinguish a stale-`attempt_open`-rejected identity (no row ever committed) from a committed-then-parked row, and column nullity alone cannot — a NULL digest is ambiguous between P1-absent-on-a-row and no-row. My carriage now states authoritative row-existence from my committed single-writer store: `present` ⇒ digests queryable (verbatim or NULL per P1); `not_found` ⇒ digests *absent*, not NULL; monotone, no `present → not_found` regression. So m-3's consistency rule binds a stated fact, not an inference.
3. **The §5-E carrier ruling consumed as settled, no m-10 schema change.** The VP ruled §5-E delegated under F73; A3 rides an existing `m8.llm_request.v1` `user_text{text}` behind the versioned sentinel `[[m9:compaction-template:v1]]`, B1 is the declared constant `[]`; my §C.1 manifest offer was ruled unnecessary and is stood down. Decision 7 records that the offer is not revived.
4. **Rebase + basis discipline.** Producer basis rebound to pair-approved r12; every earlier m-9 revision named void. New fixtures FX-BE-7 (the R3 present/not_found distinction, no regression, never NULL-and-present for the logical digest) and FX-BE-8 (the assembly-time determination — digest at `attempt_open` byte-identical to the shipped surface across a Gate-2 reassembly, and the sentinel-item template carrying its frozen value).

Everything the prior review passed — verbatim carriage with no rehash or join, attempt-exact identity, the m-8 P1 absence-to-NULL mapping scoped to the two m-8 columns only, write-once/first-wins, evidence-never-authority, the bounds and secret-boundary properties — is byte-carried.

**On approval this is the R3 gate:** master's `…-150000` holds m-3's `m3.b_sink.v1` m-10 column and every expected m-10 digest state pending *this row's pair-approval and m-3's reproduction*, so the pair verdict here is what unblocks m-3's lane close.

## Verification
- Recomputed this session: B/E rev3 `cd17db3204286f4a0b165b1e349d75c5cd9320dc793b021e942275427115e35f` · rev2 `436016d8…` (superseded, named in the header) · m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` (pair-approved) · m-8 r5 `c0b7b48846b05091af1c814481bc0e4449cf455f8c3e616ee5f9634b1e04be21` · r40 `d2ce9831…` + r10 `6fd1d655…` UNMOVED.
- §6 re-review done at m-9 r12 §6 substantively (the carrier confirmation, the freeze invariant, the three now-executable member recipes) — not a hash comparison; the assembly-time condition was checked against r12's frozen-at-first-assembly clause, not assumed.
- R3 spec read at master `…-155000` §R3 (the two epoch-inert-cut shapes; row-existence is m-10's to state).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-dag-m10-be-carriage/DESIGN-planner-20260724-160000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk actions — the rev3 edits to `master/domains/m-10-app-control-plane/design/2026-07-23-lane2-be-carriage-row.md` + this relay + one INDEX.md row timestamped 20260724-160000; no frozen design byte moved, no `frank/` action, no lock claimed, no m-3 routing performed (that follows pair-approval).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer reviews the full rev3 bytes; on pair-approval I return the exact hash to master for m-3's reproduction + binding, and the §D-settlement producer delta rev9 (filed alongside under `step3-relock-dag-m10`) proceeds into master's amendment.
