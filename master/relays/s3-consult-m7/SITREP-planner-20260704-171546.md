## SITREP — m-7 consult answers ×3 for s3: Q1 = (a) one larger member; Q2 = version INSIDE the member (the CQ-4b axis), and the phase-0 wall is a feature to fixture, not a limitation; Q3 = yes at the current wire grain, render sits handler-side, the loop stays the authority

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s3-consult-m7
PARENT_DISPATCH_ID: s1-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: frank/.relays/s3/s3-consult-m7/SITREP-orchestrator-planner-20260704-170901.md
FROM: m-7.planner
TO: s3.orchestrator-planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.implementer
SUBJECT: consult answers from the locked design + the engine as built (all three cited sites read this session); all three are independent of the s3-scope-q1 mandate outcome; de-provision on this relay

Answers from the locked design-of-record (`master/domains/m-7-conductor-core/design/2026-07-01-v3-conductor-core-design.md`, §2.1/§7/§8) plus the engine as built (I read `genesis.go:118-127`, `channel/server.go:200-220`, and the `Validate(cand, meta, "")` dead-path this session). None requires a lock amendment. All three answers hold regardless of how master rules `s3-scope-q1` — where the §7 record is mandated in S3, only Q2's simulation note relaxes (stated inline).

### Q1 — CONFIRMED: (a). One larger `fieldspec` member under the existing single top-level digest.

The S2 ruling's grain carries verbatim: genesis pins what exists; the CQ-4b composed shape (per-domain sections + per-section stamps) is a property of the artifact *once multiple domain-authored sections exist*, and arrives via the §7 config-change record — which is exactly the s3-scope-q1 question, so (b) would additionally front-run a mandate master hasn't issued. One attribution sharpening, same as S2's: the S3 registry is s3-built to the m-2 **locked shape** and m-2-**guide-reviewed** (the fidelity edge) — that is real sanction but it is not a CQ-4b *m-2-stamped section*; do not present the member as one until the composition machinery exists. Practical latitude that costs nothing: the member's internal JSON may carry its own metadata block (see Q2) — self-description inside one member claims nothing about section composition.

### Q2 — the registry's version lives INSIDE the member content. And the phase-0 wall is the integrity property working — fixture it as a positive.

The locked design already places this axis: CQ-4b puts **m-4's per-section version-stamp INSIDE the artifact** precisely so change attribution never becomes a second integrity root (§7 `:109` — "a section stamp aids per-domain change attribution without adding a second integrity root"). The S3-concrete composition of the three axes, each with its home:
- **top-level config digest** — integrity identity; lives in genesis; the ONLY integrity root;
- **registry version** — change attribution; lives **inside the member content** (self-describing: a `version` + minimal provenance block in `registry.json`); never in genesis (that would couple the integrity root to an attribution counter — the exact thing CQ-4b's ruling avoided); not derived-only (digest-as-identity is true for integrity but mute for attribution — the locked design wanted the stamp *in addition to* the digest);
- **record `schema_version`** — record-schema evolution; unrelated axis, already per-record (m-2 §18's layering note stands).

**Your constraint is confirmed and is by design:** a changed member ⇒ changed digest ⇒ phase-0 `ErrDigestMismatch` on an existing store (`genesis.go:112-116`). Absent the §7 config-change record there is NO sanctioned in-place registry mutation — that is the F7/NF-S15 property doing its job, not a gap to engineer around. So: (1) add/keep a **positive fixture** asserting old-store + changed-registry ⇒ phase-0 fail-closed *serving reads, summoning operator* — the disposition, not just the error; (2) the re-render/drift fixtures simulate registry change as **restart-with-new-store** (a seat holding a render from the prior store generation ⇒ stale form digest ⇒ `re-render` bounce). That is the sanctioned S3 simulation. *(If master mandates the §7 record in S3 via s3-scope-q1, the simulation may additionally exercise the record path — the axes answer above is unchanged either way.)*

### Q3 — YES: serve the rendered form + form digest over the existing socket protocol. Render sits handler-side; the loop remains the only authority.

§8.2's property is about the **seat-visible tool surface**, not about MCP framing: "submit's input schema IS the rendered form" must hold at whatever wire grain exists. Extending the current protocol (a `tools/describe`-grade response carrying the per-seat rendered form + form digest, replacing the static `toolDescriptions()`) realizes the locked property now; the MCP-proper shape inherits it at the wire-up slice unchanged. Closing the `Validate(…, "")` dead path with the real digest is required for the drift fixtures to bite — do it.

**Where the render call sits — locked §2.1 answers this directly:** "Form renders are concurrent reads too: the render is advisory, the loop's in-critical-section validation is authoritative." So the render runs **channel-handler side** ([A]/[E], the concurrent read path) — never a loop turn. Its inputs are all safely readable there: the registry (immutable after load-once), the seat binding (fixed per authenticated connection — render only after `authed`, from `SeatMeta`, never from payload), and phase. Bind the **form digest** to (config digest, seat, phase) so the loop's re-render check is exact across store generations.

**The conditional-grant render (the S1-narrowing's S3 landing):** its lineage-state read fits the same split, and this is the constraint to hold: the render's lineage read may touch **committed records only** (concurrent reads of immutable state are lock-free-safe per §2.1) — never the FIFO, never in-flight jobs. TOCTOU is then closed by construction in both directions: a stale-*positive* render (lineage changed after render) is rejected by the loop's in-critical-section authoritative lineage check at submit; a stale-*negative* render just means the seat re-renders. No consistency machinery, no loop involvement, no new claim. One guardrail note for the schema you serve: the rendered form carries conductor-supplied candidate sets (relay ids, addresses) — fine; keep it inside the absence set (no store/config paths, no config values — G(iii)/I-PH applies to the schema payload like any seat-deliverable surface).

Not authorized / not claimed: consult only — no scope ruling (s3-scope-q1 is master's), no lock amendment, no `DISPATCH IMPL`, no code, no `frank/` write by this relay.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s3-consult-m7/SITREP-planner-20260704-171546.md` — run before handoff (result in inline pointer)
- locked lines re-read this session: §2.1 render-advisory/loop-authoritative, §7 `:109` (single digest + in-artifact section stamps + config-change record), §8.2, §8.4 absence set
- engine sites read this session: `frank/internal/store/genesis.go:110-129` (digest check + configTarget), `frank/internal/channel/server.go:200-220` (tools/list + static descriptions), the `reg.Validate(cand, meta, "")` empty-digest call in the submit handler
- `git -C frank status --short` — untouched by this relay

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260704-171546; `frank/` read-only; no doc/design/code edits
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: operator carries this to the s3 session; s3 de-provisions the three consult-dependent design sections; the s3-scope-q1 mandate stays with master (my answers are stable under either outcome).
