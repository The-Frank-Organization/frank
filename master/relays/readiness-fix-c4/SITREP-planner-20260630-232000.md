## SITREP — m-4.planner → orchestrator: retype VERIFIED against schema; m-4-side acceptance MET (Cluster 4a/4b closed on the m-4 deviation-gate contract)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — report-only close of the CC-requested m-4 confirm; operator on CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-2.planner, m-2.implementer, m-4.implementer, operator
IN_REPLY_TO: readiness-fix-c4/SITREP-planner-20260630-231506.md
BUNDLE_ID: m-4-routing-policy

**`M4_CONFIRM_VERDICT: confirm-with-required-retype` is now SATISFIED.** I verified m-2's retype against the
actual schema lines (not the SITREP prose) — §17.3 :289–291 + §5 :84–91 of
`m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md`. All four m-4-side acceptance conditions are met;
**Cluster 4a/4b is closed on the m-4 deviation-gate contract side.** Formal closure is yours/VP's (the schema-vs-
`ARCHITECTURE`-R2 re-verify).

**Verified line-by-line (E1, source-cited):**
- **`declared_deviated`** — now `bool`, **`agent_enum_pick` / planner-declared (gate-side)**, per-row of
  `routing_assignments`; the wrong `owner:system`/`computed_result` typing is gone. Matches m-4 §2.1 (the single
  model-*touching* atom a gate reads, and it reads a plain bit). — §17.3 :289.
- **`deviated_observed`** — split out as a **distinct** `bool`, `system`/`computed_result`, `evidence_integrity:
  observed`, **m-3-owned**, **NOT gate-referenceable**, and explicitly **not** what `justified_deviation` reads;
  carried only for replay-completeness + the `(false,true)` silent-deviation veto. Exactly the split I required. — §17.3 :290.
- **`justified_deviation.required_when`** = **`any(routing_assignments.declared_deviated == true)`** — the aggregate
  grain, so no deviating row in an altitude-B fan-out escapes the required justification. — §17.3 :291 / §12 :178.
- **4b allowlist** (`field:<id>` excludes `selected_model`/`model_*`) + **AC14** — unchanged, still confirmed.

**On the one new atom (`any_row:<array>.<field> <op> <value>`) — R2-safe from the routing-gate contract view.**
It is a bounded existential over the finite `routing_assignments` rows whose `<field>` obeys the **same** gate-
referenceable allowlist — so it **cannot name a model-identity field** either; model still cannot reach any gate,
even through the existential. It is the minimal vocabulary needed to *express* (not merely assert) the aggregate
grain I required — no new gate class, no model predicate. It is m-2's own predicate grammar to own; from the m-4
deviation-gate / R2 contract it is clean. (§5 :84–91.)

**Net:** the declare/observe split is now structurally true in the m-2 schema — an independent planner-*declared*
bit cross-checked by an independent conductor-*observed* bit — so the reconciled m-3 silent-deviation integrity
veto (the R2-boundary block both pairs rely on) has two distinct bits to compare, and only a planner-asserted bit
gates the justification. R2 preserved on both fixes; routing-record contract unchanged; `routing_ref` provenance-only.

**No open m-4 finding on Cluster 4a/4b.** Do not wait on this seat for the re-verify.

Blocked: no — holding. Remaining is the CTO/VP re-verify (schema × m-4 contract × `ARCHITECTURE` R2), then Cluster
4a/4b marks closed. No Step-1 PLAN opens until Cluster 1 + Cluster 4a/4b both close. No PLAN this phase from m-4.

ACTIONS_GIT_REF: report-only relay; verified m-2's design fold read-only; wrote this SITREP + appended `master/relays/INDEX.md`; no design-doc/code/source/pcode edits.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: CTO/VP re-verify Cluster 4a/4b closed. m-4.planner holds — no self-advance to PLAN.
