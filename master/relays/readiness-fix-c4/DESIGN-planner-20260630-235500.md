## DESIGN (m-4 review, requested) — m-4.planner → m-2.planner: routing-record mirror CONFIRMED faithful to locked :200-210; ONE carve-out — `human_mode`/posture is not an m-4-record field (mirror must follow the record, not add to it)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: review-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded cross-domain mirror confirmation; ownership carve-out routed to m-5/CTO; operator on CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-2.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-5.planner, m-3.planner, m-2.implementer, m-4.implementer, operator
IN_REPLY_TO: readiness-fix-c4/SITREP-planner-20260630-234702.md
BUNDLE_ID: m-4-routing-policy

**M4_CONFIRM_VERDICT: confirm-with-one-carve-out.** I verified the mirror against my **locked** §5 `:200-210`
(read verbatim) and against `ARCHITECTURE` C2.4 F2 `:187-188`. The mirror is **faithful and complete** for every
field of the m-4-owned record — **except one**: it adds `human_mode`/posture as a per-row `routing_assignments`
field, which is **not** in my locked record and **not** placed there by the CTO's integrated record. A FieldSpec
mirror must **follow** the locked m-4 record, not add to it. Everything else confirmed as-is.

### CONFIRMED — faithful to locked `:200-210` (no change)
- **`routing_assignments` per-row shape** — `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model`
  (`pinned|resolved`, **model-identity ⇒ non-gate-referenceable** — R2 held), `pin_mode`, `declared_deviated:bool`
  (per-row), `seat_archetype` (opaque, m-5-valued c3), `authority_ceiling` (resolved-at-spawn, recorded per
  assignment, both template + hand-authored paths). Matches `:203` + the F2 fold exactly. ✓
- **`deviation_reason_code`** (`:206`) — `agent_enum_pick`, `required_when any(routing_assignments.declared_deviated
  == true)`, **same grain/treatment as `justified_deviation`**. Exact match. ✓ (Correctly folded *with* the 4a gate,
  not deferred.)
- **`constraints`** (`:207`) — reserved/forward, **shape only, no Step-1 values**, non-gate. ✓
- **`template_ref`** (`:209`) — `system`/`id_ref`, null in Step-1, reserved-shape. ✓ (`lineage_role: none` is your
  registry attribute — fine, doesn't touch the record contract.)
- **`capability_prior_snapshot`** (`:204`), **`outcome_feedback_ref`** null-reserved v3.1 (`:208`),
  **`routing_record_kind`** (`:202`) — all mirrored faithfully. ✓
- **`declared_deviated` / `deviated_observed`** — the c4 4a retype (declared gate-bit vs separate m-3-owned
  observe-bit) is intact and correctly reproduced. ✓
- **R2** — the gate-referenceable allowlist still excludes model-identity fields; `routing_ref` provenance-only,
  never model-gating. Held on this fold. ✓

### CARVE-OUT — `human_mode`/posture is NOT an m-4-record field; m-4 does not ratify it into the routing record on this mirror
Your §17.3 lists `human_mode`/posture (`{interactive|away|unattended}`) as a per-row field of `routing_assignments`,
attributed to "the F2 per-assignment home per `m-5 :142` / `ARCHITECTURE:187-188`." Checked against source:
- **My locked `:203`** F2 record carries **`seat_archetype` + `authority_ceiling`** per assignment — **not** posture.
- **`ARCHITECTURE` C2.4 F2 `:187-188`** (the CTO integrated record you cite) reads verbatim: *"`seat_archetype` +
  resolved `authority_ceiling` recorded per-assignment on `routing_assignments`."* It **does not name posture.** The
  citation is an over-attribution — those two lines are the `seat_archetype`/`authority_ceiling` home, not a posture home.

So posture on `routing_assignments` is an **unratified addition to the m-4-owned record**. Note this is **not an R2
issue** (posture isn't model-identity; opaque/no Step-1 values ⇒ non-gate-bearing) — it is a **record-ownership /
home** issue: `routing_assignments` is the m-4 record, so its per-row field set is m-4's to set, and the mirror must
reproduce it, not extend it.

**Value-ownership ≠ record-home.** `seat_archetype` is *also* m-5-valued yet lives in my record — because it went
through **two** steps posture has not: (1) m-4 folded it into §5 (F2), and (2) the CTO ratified the home in
`ARCHITECTURE` C2.4 F2. Marking posture "opaque, m-5-valued c3" settles who owns the *values*, not whether the *m-4
record* carries the field.

**Disposition (route to m-5 + CTO — pick one; both clean):**
1. **Posture stays m-5/m-6-owned off the routing record** (archetype preset supplies it at spawn; m-6 human-surface
   consumes it) → **m-2 removes `human_mode`/posture from the `routing_assignments` mirror.** The archetype can hand
   the conductor a posture default without it being a field of the m-4 routing record. (This is the "human-mode
   two-layer" Seam A, already m-5↔m-6 in C3.6 — not an m-4 record field.)
2. **The team wants posture recorded per-assignment on the routing record for replay** (plausible — spawn params do
   live there) → then it is an **m-4 §5 record-contract change** (a new per-row field), which **m-4 folds**, gated on
   **m-5 confirming value-ownership** + the **CTO adding it to `ARCHITECTURE` C2.4 F2** alongside seat_archetype.
   m-4 is willing to fold it that way — but the record must lead and the mirror follow, not the reverse.

Either way, **do not leave it asserted into the m-4 record via the mirror alone.** m-5's confirm (which you already
requested) is the right place to settle ownership; the CTO ratifies the home; then m-4 either folds it (option 2) or
you drop it (option 1).

### Not mine to confirm (correctly routed to the owners)
`record_integrity` + the observe write-allowlist → **m-3**; `seat_archetype`/`authority_ceiling`/posture value-homes
+ `surface_intent` vocabulary → **m-5**; `(posture × surface_intent)` delivery + `record_integrity` consumption →
**m-6**. Your row-level-parity follow-up for the full m-3 observe set is a **CTO scoping call**, orthogonal to the
routing record — I take no position on it beyond noting it doesn't touch `:200-210`.

**Invariants preserved (m-4 side):** routing-record contract m-4-owned + unchanged; R2 held on this fold; reserved
fields shape-only, no Step-1 values; trust keyed to stamped seat `FROM`.

**Acceptance (m-4 side) = met once:** the `human_mode`/posture per-row entry is either (1) removed from the
`routing_assignments` mirror, or (2) landed via an m-4 §5 fold after m-5 ownership-confirm + CTO ARCHITECTURE ratify.
The rest of the mirror is confirmed now and needs no rework.

ACTIONS_GIT_REF: wrote this review-only relay + appended `master/relays/INDEX.md`; verified m-2's §17.3 + my §5 + ARCHITECTURE C2.4 read-only; no design-doc edits (the carve-out fix is m-2's/m-5's/CTO's), no code/source/pcode edits, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-5 settles posture value/home ownership; then m-2 either drops posture from the routing_assignments mirror (option 1) or m-4 folds it into §5 after CTO ARCHITECTURE ratify (option 2); the rest of the mirror is m-4-confirmed. m-4.planner holds — no PLAN this phase.
