## SITREP — m-2 retype folded: `declared_deviated` → planner-declared (`agent_enum_pick`); `deviated_observed` split out (Cluster 4a closed on m-2 side)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded one-field retype closing the m-4 confirm; operator on CC
GRILL_REQUIRED: no
FROM: m-2.planner
TO: master.orchestrator-planner
CC: m-4.planner, master.orchestrator-reviewer, m-2.implementer, m-4.implementer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)
IN_REPLY_TO: readiness-fix-c4/DESIGN-planner-20260630-231200.md

**m-4's required retype is folded — the `M4_CONFIRM_VERDICT: confirm-with-required-retype` is satisfied.** m-4 was right: I had conflated a *declared* bit with a *computed* one. Read-only DESIGN fold; no source/`pcode/` edits, no PLAN, no closure claim.

**What m-4 confirmed as-is (no change):** 4b grammar-enforced R2 (`:84`/`:90` model-identity exclusion) + AC14; and the 4a trigger **direction** (`selected_model …` → `declared_deviated`). Held unchanged.

**The retype (exactly what m-4 specified):**
- **`declared_deviated` → `agent_enum_pick`, planner-owned (declared, gate-side).** Dropped the wrong `owner:system`/`computed_result` typing. It is now a plain **declared** boolean the routing planner/orch-planner seat asserts — the single model-*touching* atom a gate may read, and it reads a plain bit (no model comparison enters the gate). §17.3 :289.
- **`deviated_observed` split out as a distinct field** — `system`/`computed_result`, the conductor observe-side bucket-vs-bucket computation (`declared_bucket ≠ rank-1(recommended)` vs `capability_prior_snapshot`), **m-3-owned observed evidence** (`evidence_integrity: observed`), **NOT gate-referenceable** and **not** what `justified_deviation` reads. m-2 only *carries* it (replay-completeness); m-4/m-3 own/compute it. §17.3 :290.
- **Trigger grain = the `any(...)` aggregate you specified.** `justified_deviation.required_when` = **`any(routing_assignments.declared_deviated == true)`** — so in an altitude-B fan-out a single deviating row cannot escape the required justification. §17.3 :291, §12 :178.
- **Made the aggregate grammar-expressible (not just asserted).** Added a bounded `any_row:<array>.<field> <op> <value>` existential atom to the §5 vocabulary (decidable over the finite `routing_assignments` rows; same gate-referenceable allowlist, so still no model-identity field). This keeps canonical-iff-consumed honest — the predicate the tool reads can actually express the trigger. §5 :84–91.
- Propagated to AC14 (added the **declare/observe split fixture**: two independent bits so the `(declared=false, observed=true)` silent-deviation veto has something to compare), the §5 R2 rule, and the §18 c4 fold-log.

**Why this holds the seam (agreeing with your two reasons):** (1) the `(false,true)` silent-deviation integrity veto needs an independent *declared* bit cross-checked against an independent *observed* bit — collapsing them removes the thing the veto catches; (2) a `computed_result` field can't be what a planner is *held to justify* — only a planner-*asserted* bit can. Both now structurally true in the m-2 schema.

**Acceptance (m-4 side) — met:** `declared_deviated` is `agent_enum_pick`/planner-declared (not `system`/`computed_result`); the system-computed bucket-vs-bucket boolean is named `deviated_observed` and is **not** gate-referenced; the trigger is the `any(routing_assignments…)` aggregate. 4b + AC14 unchanged. — E1, §17.3 :289–291 + §5 :84–91 + §18.

**Invariants preserved:** model = payload/bookkeeping, never a gate input (R2, on both fixes); trust keyed to stamped seat `FROM`; routing stays a separate seat-stamped evidenced record; `routing_ref` provenance-only; the declare/observe split (and thus the m-3 integrity seam) intact. Routing-record *contract* unchanged — only m-2's schema typing/wording + one bounded predicate atom.

**Not done (by design):** Cluster 4a/4b closes when the **CTO/VP re-verify** the retyped schema against the m-4 contract + `ARCHITECTURE` R2 lock. No Step-1 PLAN opens until Cluster 1 + Cluster 4a/4b both close.

Blocked: no — holding for CTO/VP re-verify.
Scope drift risk: none — the one field retype m-4 required + its dependent echoes/atom; no other c2 lock touched; phase stays AUDIT+DESIGN. The `any_row:` atom is the minimal vocabulary needed to *express* the aggregate grain m-4 required (not a new gate class).

Tests / verification: read-only DESIGN fold; all claims E1 (source-cited to design §5/§12/§17.3/§18 + the m-4 confirm `231200`).

ACTIONS_GIT_REF: folded `master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md` (§17.3 `declared_deviated` retype + new `deviated_observed` row + aggregate `justified_deviation` trigger; §5 `any_row:` atom + R2-rule update; §12 echo; AC14 split fixture; §18 c4 fold-log); wrote this SITREP + appended `master/relays/INDEX.md`; no code/source/pcode edits; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: CTO/VP re-verify Cluster 4a/4b closed across m-2's schema, the m-4 deviation-gate contract, and `ARCHITECTURE` R2. m-2.planner holds — no self-advance to PLAN.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
