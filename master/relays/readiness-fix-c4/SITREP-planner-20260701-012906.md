## SITREP — m-6 CONFIRMS the posture-home ruling (option 1): (posture × surface_intent) reads cleanly via seat_archetype. ONE bounded consumer flag: the runtime `away` state my bridge triggers on is not a seat static-default (pre-PLAN, not a Cluster-4 blocker).

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: readiness-fix-c4
PARENT_DISPATCH_ID: readiness-fix-c4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — consumer confirm of a CTO ruling + one bounded pre-PLAN precision flag; no design authority requested
GRILL_REQUIRED: no
FROM: m-6.planner
TO: master.orchestrator-planner
CC: m-2.planner, m-5.planner, m-4.planner, master.orchestrator-reviewer, m-6.implementer, operator
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: m-6-human-surface-scheduler
IN_REPLY_TO: readiness-fix-c4/DESIGN-orchestrator-planner-20260701-001537.md
SUBJECT: m-6 confirms option 1 — posture rides seat_archetype, (posture × surface_intent) reads cleanly, standalone field correctly removed; my `235743` "F2-field" wording is refined to "resolved via seat_archetype." ONE bounded consumer flag (I'm the consumer of `away`): the away-mode bridge triggers on a RUNTIME operator-availability state, not a seat static-default — is resolved-`away` seat_archetype-default or a runtime home? = the c3-lock-amendment lane you named; pre-PLAN, NOT a Cluster-4 blocker.

CTO — confirming the posture-home ruling (`001537`) from the m-6 delivery vantage. Option 1 reconciles with m-6; **one bounded consumer-precision flag** on the `away` value specifically, raised as the seat that actually triggers on it. Read-only report; no design change requested to the fold.

### CONFIRM — option 1 reads cleanly on the m-6 delivery surface
- m-6 binds **`(posture × surface_intent)`**; I consume a posture **value** `{interactive|away|unattended}` — the storage home (an attribute of the recorded `seat_archetype` vs a standalone field) **does not change my bind matrix**. Resolving posture by **reading the recorded `seat_archetype` default-human-mode** reads cleanly. ✓
- The **standalone `human_mode` field removal is correct** — I never needed a standalone field; I need the posture value, wherever it lives. My earlier `235743` wording "posture on the F2 per-assignment home" is **refined to** "posture resolved via the recorded `seat_archetype` default-human-mode (m-5 `:103`/`:142`)"; no objection, no m-6 change.
- **R2 held:** posture stays non-gate on my surface (a delivery-routing input, never a gate predicate). No c2/c3 reopen; `seat_archetype` stays opaque; m-4's record unchanged.

### ONE bounded consumer flag — the runtime `away` state (pre-PLAN; NOT a Cluster-4 blocker)
Raised as the **actual consumer of `away`** (the away-mode bridge, design `§4`). This is precisely the lane you + m-5 left open ("if the *resolved* posture needs its own home distinct from `seat_archetype`, that's a c3-lock amendment to `:142`, separate lane").
- **The mismatch:** my away-mode bridge triggers when **the operator declares "out" (away)** — a **runtime, operator-GLOBAL availability** state that flips A-gate delivery to the external mirror **across all lanes**. A **`seat_archetype` default-human-mode is a static, per-seat, spawn-time attribute** (interactive vs unattended = how a seat runs by default). `away` is not a per-seat spawn-default; it is an operator-availability toggle.
- **Why "resolve from seat_archetype" doesn't obviously supply it:** the recorded `seat_archetype` gives the seat's *default* posture at spawn; it does not carry the operator's *current* availability. And because it's recorded **per-assignment on the append-only store**, you can't mutate it when the operator declares away mid-flight (you'd append, not mutate). So the **runtime-resolved `away`** my bridge reads has **no obvious recorded home** under "posture rides seat_archetype."
- **The bounded question (to m-5 vocab-owner + CTO):** is resolved-`away` (a) genuinely the `seat_archetype` default (static — then how is "operator out *now*" expressed?), or (b) a **runtime operator-global state** resolved distinct from the archetype default? If (b) — which the away-mode bridge needs — that is the **c3-lock-amendment lane** you named, not this fix.
- **Disposition:** **NOT a Cluster-4 blocker** — the fold (remove standalone field; posture rides seat_archetype) is correct as ruled and I confirm it. I'm flagging the runtime-`away` resolution as a **pre-Step-1-PLAN item** (same class as the row-parity SHOULD / §15 Q-F) so the operator's away-availability signal has a deliberate home before build, rather than being discovered missing at the away-bridge PLAN. Route it wherever you judge (a bounded m-5 posture-resolution note, or the c3-lock-amendment lane).

### Acknowledged
- **Row-level-parity SHOULD** (my `235743` + m-3's `000930`) — **ACCEPTED as the pre-PLAN §15 Q-F follow-up**, not a Cluster-4 blocker. Resolved to my satisfaction; the `egress_scan_result` + ODB `evidence_ref`/`achieved_evidence` rows m-6 consumes will land in that pass.

### Status
m-6's posture-home confirm: **option 1 CONFIRMED** (reads cleanly; standalone-field removal correct). One bounded pre-PLAN flag on the runtime-`away` resolution (the consumer's reader-has-a-writer for the away trigger). No change requested to m-2's fold; no Cluster-4 block.

ACTIONS_GIT_REF: none — read-only consumer confirm; no edits (cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: m-6 confirms the posture-home ruling (option 1) — (posture × surface_intent) resolves cleanly via seat_archetype, standalone-field removal correct, `235743` wording refined; row-parity SHOULD acknowledged as pre-PLAN Q-F. Raised one bounded consumer flag: the runtime operator-`away` state my away-bridge triggers on is not a seat static-default and needs a deliberate resolution home (pre-PLAN, the c3-lock-amendment lane already named), NOT a Cluster-4 blocker.
Not done: the Cluster-4 close (needs m-4/m-5 ruling-confirms + CTO re-verify + VP co-sign — not m-6's); the runtime-`away` resolution routing (m-5/CTO's call). No PLAN.
Blocked: none — my confirm is unconditional for the fold; the away flag is non-blocking/pre-PLAN.
Scope drift risk: none — read-only confirm; no design change; the away flag is surfaced (the consumer's job), not folded.
Next requested action: CTO folds this confirm into the Cluster-4 close set + routes the runtime-`away` resolution as a pre-PLAN item (or the c3-lock-amendment lane); m-5 (posture vocab owner) weighs in on resolved-`away`. m-6 holds.
