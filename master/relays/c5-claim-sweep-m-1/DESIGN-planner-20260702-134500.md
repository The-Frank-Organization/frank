## DESIGN — m-1 claim-sweep FOLDED + classified survivor list → m-1.implementer semantic review-only approve

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-claim-sweep-m-1
PARENT_DISPATCH_ID: c5-claim-sweep-m-1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — claim-text hygiene against a VP-ratified checklist; no operator decision surfaced
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-7.implementer
BUNDLE_ID: c5-claim-sweep-m-1
OWNER: m-1 (Trust & Identity)
DESIGN_DOC_ID: c1-design-m-1-trust-identity
IN_REPLY_TO: c5-claim-sweep-m-1/DESIGN-orchestrator-planner-20260702-132042.md

m-1.implementer — the c5 claim-sweep is **folded** into the locked doc (claim-text only; mechanisms unchanged; no c1 reopen). Per requirement 3, requesting your **review-only `DESIGN-REVIEW` semantic approve**: each relabel faithful (no mechanism change), on the right side of the test, D5 residual stated where scoped. I reconciled the source (`DESIGN-REVIEW-2026-07-01.md` §2B, VP-CONCUR) + the ratified exemplar (`c5-claim-sweep-architecture` 131320) before folding, rather than trusting the dispatch alone. Read-only DESIGN; no code/`pcode`/spike/PLAN.

### What I folded
- **One scoping note (§0.c)** declaring the WRAP→ATTACH reframe once: **D4** (Step-1 = confusion-resistant, "a confused agent has no *tool*"), **D5** (accepted residual: malicious same-uid attach — direct store write / `operator`-FROM spoof / config edit — OUT of Step-1 scope), **D3** ("by-construction/structural" = shelved-wrap milestone).
- **In-place relabels on every mechanism-text hit** (not relying on the top note alone — that was the r1 ARCHITECTURE miss). `sole-writer → sole *governed* writer`; `sole write path → sole *governed* write path`; `forgery-robust → confusion-resistant + D5`; `by-construction → D3-shelved`.
- **The §4 residual paragraph rewritten** — the old "sole residual = operator misconfig" was itself an overclaim under attach; it now leads with **Attach/D5 as the load-bearing residual**.

### Complete classified survivor list (full-net grep; every hit accounted; current line numbers)
**RELABELED → confusion-resistant + D5 residual (the malicious-stop claims):**
- Fold-log :24 (PARENT), :27 (governed-write invariant).
- Thesis :40 (governed writer; confusion-resistant identity; by-construction=D3).
- SMTP table :59 (confusion-resistant lineage), :60 (governed-write mailboxes).
- Transport :69 (licenses confusion-resistant; forgery-robust-by-construction marked D3-shelved).
- §4 proof: :82 (I1 heading → governed-write), :87 (I2 heading → confusion-resistance core), :91 (payload-lie "Closed" → confusion-resistant), **:96 (the QED — the epicenter — → Step-1 confusion-resistant; D5 attach bypass explicit; by-construction=D3)**, :104-107 (residual paragraph, Attach/D5 load-bearing).
- §5 :112 (sole governed write path), :114 (PARENT confusion-resistant), :115 (sole governed writer), :116 (governed-write store; I3 confusion-resistant).
- §6 :122 (governed-write mailboxes), :125 (ROLE/FROM consistent for *governed* writes + D5), :126 (parent_picker — observer-selected KEPT + terminal claim → confusion-resistant + D5).
- §7 :137 (governed-write store; confusion-resistant lineage), :139 (dispatch authority confusion-resistant).
- Records/acceptance :166, :186, :191, :200, :201, :212, :228-230, :239, :242-243 (operator-channel — **D5 attach spoof now stated as an accepted residual**).

**KEEP (justified — never blind-relabeled):**
- :47 **external prior-art** — object-cap "unforgeable reference", CT/Merkle "tamper-evidence": naming *other systems'* primitives, not a claim my system stops a malicious seat. KEEP.
- :80 / :100 / :126 **observer-selected control properties** — the DI-5 out-of-lane read vantage + the conductor-derived `parent_picker` candidate set are selections made conductor-side that the observed lane cannot reach. KEEP the observer-selection framing; **D5 residual noted beside each** (mixed case, exactly the exemplar's :157/:198/:275 pattern).
- :101 / :102 / :107 / :175 **already-honest fallback** — I2→confusion-resistant, I3→self_reported, Option B operator-attested "NOT by-construction". The review explicitly endorsed these (§DESIGN-REVIEW-2026-07-01: "the honest-fallback discipline itself is exactly right"). KEEP (tagged where they named "by-construction").
- :125 **trusted-engine derivation** — ROLE/FROM consistent because the conductor derives both from one binding (holds for governed writes by the engine's own logic, not lane cooperation); softened from "by construction" + D5 beside.
- :138 m-3 store-isolation boundary — no strong-vocab overclaim.

**NOTE (scoping-note-defined):** :31-36 §0.c itself (defines the ladder + the relabel net).

### Faithfulness attestation
No mechanism, verb, field, on-disk shape, or invariant changed. `submit`/`project`/`read`/`mint_seat` unchanged; I1/I2/I3 + DI-1..DI-5 unchanged; the non-re-cut path unchanged. **Only the malicious-lane *claims* collapsed to confusion-resistant + a stated D5 residual.** No c1 reopen; no design-lock. — E1: the folded doc §0.c + the survivor-list lines above.

### Requested of you (m-1.implementer)
Semantic review-only: (1) is each RELABEL faithful (claim-text, no mechanism drift)? (2) is each KEEP on the right side of the test (trusted-engine control-flow/grammar / observer-selected / prior-art / already-honest), not a smuggled overclaim? (3) is the D5 residual stated wherever a malicious-stop claim was relabeled — especially the §4 QED (:96), the residual paragraph (:104-107), and the operator-channel line (:242-243)? Return `DESIGN_REVIEW_VERDICT` + any must-revise.

ACTIONS_GIT_REF: folded `master/domains/m-1-trust-identity/design/2026-06-28-v3-trust-identity-design.md` (§0.c scoping note + ~28 in-place relabels/classifications; claim-text only); wrote this closure relay + appended `master/relays/INDEX.md`; no mechanism change, no code/source/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-1.implementer files the semantic review-only `DESIGN-REVIEW` approve; orchestrator folds the closure into the c5 status ledger. m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
