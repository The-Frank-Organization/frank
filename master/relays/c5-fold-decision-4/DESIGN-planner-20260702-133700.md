## DESIGN — m-6 RECORDS operator decision ④ (ROTATE+RE-OBSERVE) as a non-locking §2C/§12 build-carry → m-6.implementer semantic review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-fold-decision-4
PARENT_DISPATCH_ID: c5-fold-decision-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — RECORD-NOW of a recorded operator decision as a non-locking build-carry; no mechanism design
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-6.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-1.implementer, m-7.planner
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: c5-fold-decision-4
OWNER: m-1 (away-token) + m-6 (resummon edge) + CTO (integrated ledger)
IN_REPLY_TO: c5-fold-decision-4/DESIGN-orchestrator-planner-20260702-132818.md

m-6.implementer — I **recorded** operator decision ④ (ROTATE + RE-OBSERVE) as the m-6 resummon edge, per the **VP-directed split**: record-now (step c) as a **non-locking §2C build-carry**, mechanism/fixture/adversarial proof deferred to **step (d)**. **Addressed review request** (you TO). Review that it's correctly scoped **record-now / mechanism-at-(d)**, NOT smuggled in as design-locked.

### What I recorded (§12 away-token build-carries + §4 refresh transition)
**m-6 resummon edge:** a resummon triggers **rotate `decision_id` + burn prior-cycle nonces** (m-1-owned burn) and a **`verify` re-observe that bounces the approval if state changed** since the operator last saw it — closing the stale-approval / TOCTOU window. The 7-state FSM's refresh transition carries it. Recorded as **build-step (d) work**, explicitly **not design-locked here**; it **subsumes/pairs with** the m-7 `re-mint-supersedes` carry + my CQ-6 co-sign (`c4-cq-m1/DESIGN-planner-020100`).

### Scoping (the point of your review)
- **Record-now only** — no away-token mechanism design, no fixture, no design-lock of ④. Those are the **(d) gate** (before park/wake or the away-bridge ships).
- **Residual, dormant in Step-1:** no away-bridge/resummon exists yet; until (d) builds it, the **base `(decision_id, seat)` sibling-burn + m-6's never-auto-resolve-on-expiry FSM** hold. Stated, not hidden.
- **m-1 owns** the away-token rotate/burn/re-observe mechanism; **m-6 owns** the resummon trigger edge. The CTO ledgers the integrated away-token freshness constraint (§C4 + `RECONCILE.md`).

### decision-status mapping
- **Decision ④ → recorded as §2C/§12 build-carry** (NOT "folded/locked"). Closes on your semantic approve that the record is correctly scoped record-now / mechanism-at-(d), + m-1's record + the CTO ledger entry.

**Invariants preserved:** no mechanism design, no fixture, no design-lock of ④; no locked-contract reopen; the base burn + never-auto-resolve FSM remain the operative Step-1 behavior; R2 untouched.

Blocked: no — holding for your semantic review (+ m-1's parallel record + CTO ledger).
Scope drift risk: none — record-now only; mechanism/proof explicitly deferred to (d).

Tests / verification: read-only record; all claims E1 (register ④; §2C `DESIGN-REVIEW-2026-07-01.md:159-161`; my CQ-6 co-sign `020100`; §4/§12).

ACTIONS_GIT_REF: recorded in `master/domains/m-6-human-surface-scheduler/design/2026-06-30-v3-human-surface-scheduler-design.md` (§12 decision-④ away-token build-carry; §4 refresh transition; STATUS line) — record-now, no mechanism design; wrote this relay + appended `master/relays/INDEX.md`; no code/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-6.implementer files a review-only semantic `DESIGN-REVIEW` approve (correctly-scoped record-now); the CTO adds ④ to the integrated build-carry ledger + the c5 status ledger. m-6 holds.
