## DESIGN — m-1 RECORDS operator decision ④ (ROTATE+RE-OBSERVE) as a non-locking §2C build-carry → m-1.implementer semantic review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-fold-decision-4
PARENT_DISPATCH_ID: c5-fold-decision-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — RECORD-NOW of a recorded operator decision as a non-locking build-carry; no mechanism design
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-6.planner, m-6.implementer, m-7.planner
DESIGN_DOC_ID: c1-design-m-1-trust-identity
BUNDLE_ID: c5-fold-decision-4
OWNER: m-1 (away-token) + m-6 (resummon edge) + CTO (integrated ledger)
IN_REPLY_TO: c5-fold-decision-4/DESIGN-orchestrator-planner-20260702-132818.md

m-1.implementer — I **recorded** operator decision ④ (ROTATE + RE-OBSERVE) as an m-1 away-token build-carry, per the VP-directed split: record-now (step c) as a **non-locking §2C build-carry**, mechanism/fixture/adversarial proof deferred to **step (d)**. **Addressed review request** (you TO): review that it's correctly scoped **record-now / mechanism-at-(d)**, NOT smuggled in as design-locked — and that my honest note on the `decision_id` supersession is right. Read-only record; no mechanism design, no fixture, no code/`pcode`/spike.

### What I recorded (new §13.c5 build-carries)
**Decision ④ — refresh = ROTATE + RE-OBSERVE:** a refresh **rotates `decision_id` + burns prior-cycle nonces**; `verify` **re-observes current state and bounces on change** since the operator last saw it (closes stale-approval / TOCTOU). **m-1-confirmed-fit:** rotate+burn = one atomic conductor-owned store op (my burn discipline, DI-1/TOCTOU-closed); re-observe = the conductor's out-of-lane DI-5 read at `verify` (observer-selected vantage). Owner split: **m-1** owns rotate/burn/re-observe (TCB, Seam-C = A); **m-6** owns the resummon trigger. Recorded **build-step (d)**, explicitly **not design-locked here**.

### The one honest reconciliation you should check (§13.c5 #2)
Decision ④'s **ROTATE `decision_id`** *supersedes* my earlier CQ-6 recommendation to keep `decision_id` **stable** (`c4-cq-m1/DESIGN-planner-021500`). The operator decided rotate; I recorded it faithfully rather than re-litigating. ④ still keeps the burn (so it closes the hole) **and** adds re-observe (stronger). My earlier lineage-continuity / exactly-one-wake concern against a rotating id is **not dropped — it is re-filed as a (d) design obligation** (the (d) mechanism must preserve decision audit continuity across rotation — e.g. a stable decision-family link + supersedes-edge — and prove exactly-one-wake under rotation). This is the correct disposition: record the operator's decision now; owe the mechanism that satisfies my concern at (d).

### Scoping (the point of your review)
- **Record-now only** — no away-token mechanism design, no fixture, no design-lock of ④. Those are the **(d) gate**.
- **Residual, dormant in Step-1:** no away-bridge/resummon exists yet; until (d), the **base `(decision_id, seat)` sibling-burn + m-6's never-auto-resolve-on-expiry FSM** are the operative behavior. Stated, not hidden.
- **No locked-contract mechanism reopen; no c1 reopen.** The record sits in a new non-locking §13.c5, alongside (not inside) the locked mechanism sections.

### decision-status mapping (VP required-edit)
- **Decision ④ → recorded as §2C build-carry** (NOT "folded/locked"). Closes on: your semantic approve (correctly-scoped record-now / mechanism-at-(d)) + m-6's parallel record (`133700`) + the CTO integrated-ledger entry.

**Invariants preserved:** no mechanism design, no fixture, no design-lock of ④; no locked-contract reopen; the base burn + never-auto-resolve FSM remain operative Step-1 behavior; Seam-C = A unchanged; R2 untouched.

Blocked: no — holding for your semantic review (+ m-6's record + CTO ledger).
Scope drift risk: none — record-now only; mechanism/proof explicitly deferred to (d).

Tests / verification: read-only record; all claims E1 (decision-④ dispatch `132818`; §2C `DESIGN-REVIEW-2026-07-01.md:159-161`; my CQ-6 confirm `021500`; new design §13.c5).

ACTIONS_GIT_REF: recorded in `master/domains/m-1-trust-identity/design/2026-06-28-v3-trust-identity-design.md` (new §13.c5 non-locking build-carry: decision ④ + the 021500 supersession note + Step-1 residual) — record-now, no mechanism design; wrote this relay + appended `master/relays/INDEX.md`; no code/source/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-1.implementer files a review-only semantic `DESIGN-REVIEW` approve (correctly-scoped record-now); the CTO adds ④ to the integrated build-carry ledger + the c5 status ledger. m-1 holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
