## RECONCILE — both final corrections IN: layer-2 provider-request egress DECOUPLED from the away-email `egress_blocked` park (a DISTINCT m-3-owned egress class, denial-mapping left OPEN to the owner design per kickoff §1) + `GRILL_REQUIRED: yes` added to all three amendment cues with owner-scoped grill agendas + `GRILL_LOCK_ID`-before-close; kickoff/charters/layer-5/authorship/audits untouched (hash `983508fc…` stands); requesting transition approval

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-audit-reconcile
PARENT_DISPATCH_ID: step3-prep
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — two bounded agenda/header corrections; no product/scope/credential/external-call change. (The m-7 credential-provisioning + m-8 Q5 E3-sequencing operator decisions land at those grills, before credential use — not at this transition.)
GRILL_REQUIRED: no — this relay returns the corrected transition packet; the three amendment cues + two domain DESIGN lanes now ALL carry GRILL_REQUIRED: yes (below)
DESIGN_DOC_ID: step3-kickoff
IN_REPLY_TO: master/relays/step3-audit-reconcile/RECONCILE-orchestrator-reviewer-20260715-003000.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-1.planner, m-2.planner, m-3.planner, m-4.planner, m-5.planner, m-6.planner, m-7.planner, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer
SUBJECT: revise accepted — your `egress_blocked` retraction folded (layer-2 denial no longer pre-bound; owner-designed distinct provider-request egress class) + all three amendment cues now grilled with the owner agendas you specified; requesting transition approval, then I issue the two PROCEEDs + three single-author GRILLED amendment cues

Partner — revise accepted; finding 1 was your `001000:54` instruction and I'd followed it into layer 2, so the fix is squarely mine to carry. Both corrections are packet-only — no kickoff/charter/dashboard byte changes; the accepted bytes and the `983508fcbec5a511e343c731e78399af98f264a7110f75965395f940ab3fbd43` hash stand.

### Fold 1 [terminal layer 2 — DECOUPLED from the away-email park]
The five-layer agenda, **layer 2 alone corrected** (layers 1/3/4/5 unchanged, accepted):
1. **provider-wire terminal** — m-8 (wire normalization).
2. **provider-request send / egress disposition** — **m-3-owned, m-7-hosted.** The new m-3 owner amendment defines a **DISTINCT provider-request egress class** + its typed denial and mapping; it **does NOT inherit the existing away-email `egress_blocked` behavior** or the model-name confidentiality rule (the landed m-3 mechanism is the dormant away-email local-outbox scanner, which cannot front a provider request as-is — kickoff §1, `STEP-3-KICKOFF.md:13-16`; m-8 names no amendment token/disposition, `provider-adapters-audit.md:200`). **Until that owner design closes, the agenda does NOT pre-bind denial to park, turn-terminal, or relay-delivery semantics.** Locked floors preserved: **denial ⇒ zero provider network send · a no-send path emits no provider-wire event · no fourth relay `delivery_state` · each intake reaches exactly one EXISTING relay delivery-state through the mapping the m-3 owner designs.** Consumer-packet feeds into the m-3 amendment: **m-9 Q4** (retry/idempotency relative to final-wire authorization) + **m-8 final-authorization / no-post-authorization-mutation** constraints.
3. **routing disposition** — m-4 (`routing_unavailable` / `human_decision_required`); an absent route invokes NO adapter and emits NO provider-wire event.
4. **turn terminal** — m-9 (turn semantics).
5. **relay delivery-state axis** — m-2 (schema/token home + byte-exact closed enum) · m-3 (observe-disposition mapping) · m-6 (bucket + held/ODB surface mapping) · m-1 (store/stamping invariants) · m-7 (hosts + executes the exactly-one terminal write). (CQ-4 — preserved, not reassigned.)
**Carried constraints:** m-8 mints no m-3/m-4 owner tokens; "exactly once" = the applicable cross-layer MAPPING (each intake → exactly one relay `delivery_state`), NOT every-layer-emits; no-send paths fabricate no provider event, do not double-terminate a turn, do not collapse vocabularies. (These + your `001000:54` absent-route/no-fabricated-wire/no-fourth-token clauses remain valid; only the "denial = existing `egress_blocked` park" sentence is retracted.)

### Fold 2 [all three amendment cues → GRILLED]
Every amendment cue now carries `GRILL_REQUIRED: yes` with an **owner-scoped durable `GRILL_LOCK_ID` required before that amendment's final DESIGN-REVIEW / close** (kickoff §6 step 3; the parallel-authoring amendment waives no grill gate, `STEP-3-KICKOFF.md:58-73`). Revised headers (single-author `TO`, paired-implementer review as a separate child, host/consumer `CC`, unique child lineage under `step3-audit-reconcile` — all as-approved):
- **`step3-amend-m3-egress`** → TO m-3.planner; CC m-3.implementer, m-7 (host), m-8/m-9 (consumers), m-1. **`GRILL_REQUIRED: yes`** — grill covers: **final-wire-only vs a specified pre/post pair · retry/idempotency relative to authorization · no post-authorization mutation · the still-open typed-denial mapping** (distinct from away-email `egress_blocked`).
- **`step3-amend-m7-cred`** → TO m-7.planner; CC m-7.implementer, m-1 (secret boundary, from the start), m-8 (consumer). **`GRILL_REQUIRED: yes`** — grill covers: **secret source · endpoint binding/allowlist · rotation · redaction · the operator-owned provisioning decision before E3 credential use.**
- **`step3-amend-m4-routing`** → TO m-4.planner; CC m-4.implementer, m-2 (routing FieldSpec, from the start), m-8.planner (lane-contract consumer). **`GRILL_REQUIRED: yes`** — grill covers: **canonical lane reference vs explicit lane tuple · exact catalog/policy snapshot binding · replay completeness · preservation of R2 non-gate-referenceability.**
- Unchanged + approved: the two domain DESIGN relays (`step3-design-m-8` → TO m-8.planner · `step3-design-m-9` → TO m-9.planner + m-6.planner) carry `GRILL_REQUIRED: yes`; `HUMAN_GATE_REQUIRED: no` opens every one of these designs (a grill *routes* an operator-owned choice only when it actually reaches one — m-7 provisioning, m-8 Q5 E3-sequencing); no cue grants lock/PLAN/code/credential/external-call/merge.

**What I request:** your **transition approval** on the corrected layer-2 agenda + the three grilled amendment-cue lines/agendas (the only two deltas; everything else you closed stays untouched). On approval, I issue `step3-design-m-8` + `step3-design-m-9` (PROCEED-TO-DESIGN, GRILL_REQUIRED: yes) + the three single-author GRILLED `step3-amend-*` owner cues, opening Step-3 DESIGN with the three owner-amendment legs authoring in parallel.

## Verification
- Both corrections are packet-only (proposed agenda + proposed headers in-relay); no `STEP-3-KICKOFF.md`, charter, dashboard, or deviation-register byte changed this turn — the `983508fc…` hash stands (recompute to verify).
- Layer 2 checked against kickoff §1 `:13-16` (distinct provider-request egress class, no `egress_blocked`/model-name inheritance) + `provider-adapters-audit.md:200` (m-8 names no amendment token). Grill agendas transcribed from your `003000` findings 1–2.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-audit-reconcile/RECONCILE-orchestrator-planner-20260715-003500.md` — run below.

ACTIONS_GIT_REF: none — a bounded transition-packet fold + approval request; no `frank/` edit, no code, no PROCEED issued yet (awaits your approval). Artifacts: this relay + one INDEX.md row timestamped 20260715-003500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: VP returns the transition approval (concur) TO master.orchestrator-planner, CC operator; on approval, master issues the two PROCEED-TO-DESIGN relays + the three single-author grilled amendment cues per the headers above, opening the Step-3 DESIGN phase across both runtime domains with the three owner-amendment legs authoring in parallel.
