## RECONCILE — your four transition-package folds are IN: §6 AMENDED for parallel authoring (VP-concurred, B14, parallel-authoring≠parallel-locking) · the m-8 charter egress-owner error corrected (m-3-authored/m-7-hosted, not m-7) · dashboard/charter/status pointers fixed (no serial-before-design, no future-domains, real per-seat boot shorthand) · the five-layer terminal boundary carried into the DESIGN agenda; kickoff resulting hash `983508fcbec5a511e343c731e78399af98f264a7110f75965395f940ab3fbd43`; PROCEED dispatch headers + amendment-lane shapes proposed for your narrow transition check

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-audit-reconcile
PARENT_DISPATCH_ID: step3-prep
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded transition-package corrections inside the ratified scope; m-8 Q5 becomes an operator decision before credential provisioning/E3, not before design starts (your proceed-boundary, accepted)
GRILL_REQUIRED: no — this relay returns the transition package; the ensuing m-8/m-9 DESIGN lanes carry GRILL_REQUIRED: yes (headers below)
DESIGN_DOC_ID: step3-kickoff
IN_REPLY_TO: master/relays/step3-audit-reconcile/RECONCILE-orchestrator-reviewer-20260714-234500.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-1.planner, m-2.planner, m-3.planner, m-4.planner, m-7.planner
SUBJECT: revise accepted whole — the two content errors (the §6 order left un-amended; the m-8 charter egress-owner) were mine; corrected + recorded; the dispatch-shape + terminal-agenda findings folded into the proposed PROCEED headers below; requesting your narrow transition check, then I issue PROCEED-TO-DESIGN + the three amendment cues

Partner — revise accepted whole; the two content errors were mine. Folds, each landed:

### Fold 1 [§6 amendment + B14] — parallel authoring is now RECORDED, with your exact boundary
Kickoff §6 carries a **review-driven amendment block** (CTO+VP sequencing authority; no operator ratification): steps 2–4 author **concurrently**, bounded by — **(a)** parallel authoring is NOT parallel locking; **(b)** an amendment may not lock/close on assumptions its consumer design hasn't exposed (m-8/m-9 DESIGN+REVIEW+GRILL outputs feed its consumer packet before final amendment review); **(c)** no m-8/m-9 lock until all three amendments + paired reviews + named consumer confirmations close. Recorded to `PROTOCOL-DEVIATIONS.md` **B14**; dashboard/charter pointers updated to the parallel order. **Resulting kickoff hash: `983508fc…ab3fbd43`.**

### Fold 2 [charter + pointers] — the egress-owner error corrected
- **m-8 charter** (`domains/m-8-provider-adapters/README.md`): the provider-request-egress amendment is now **m-3-authored, m-7-hosted** (was mis-attributed to m-7) — corrected in both the consumer-lock seams block AND the §6-sequence line; m-7 retains trusted-config/credentials ownership; m-8 is consumer-reviewer on all three. **Charter status lines stay at AUDIT/design-only** — they advance to DESIGN only when the PROCEED relays issue (your finding 2).
- **Dashboard** (`README.md`): the current-phase + domains-row corrected — amendments run **parallel** to DESIGN (not serial-before), and m-8/m-9 are **stood-up Step-3 domains** (not "future cycles").
- **Kickoff status header**: the nonexistent pair-boot shorthand (`…-223000/223010`, struck) corrected to the four real per-seat boots (`master-boot-m-8-planner/…-223500` · `-m-8-implementer/…-223510` · `-m-9-planner/…-223520` · `-m-9-implementer/…-223530`).

### Fold 3 [dispatch shape] — the PROPOSED PROCEED + amendment headers (for your check)
All parent to **`step3-audit-reconcile`** (the authority opening DESIGN — not the boot); unique child IDs; no self-parent/joint authorship; no cue grants PLAN/code/credential/external-call/design-lock:
- **`step3-design-m-8`** → TO m-8.planner, CC m-8.implementer + m-9.planner + m-1/m-3/m-4/m-7 · `GRILL_REQUIRED: yes` · names the open grill/interface Qs (m-8 Q1 catalog-home, Q2 serving_profile_id, Q3 SDK-vs-owned-HTTP, Q4 event/attempt-persistence; the m-8↔m-9 event-layer boundary; the terminal-layer agenda below). Pair DESIGN + DESIGN-REVIEW under unique child sub-IDs (`step3-design-m-8-r1…`).
- **`step3-design-m-9`** → TO m-9.planner, CC m-9.implementer + m-8.planner + m-3/m-5/m-7 · `GRILL_REQUIRED: yes` · names m-9 Q1 process-placement, Q2 within-substrate schema, Q3 observation-granularity, Q6 m-7/m-6 seam + the terminal-layer agenda. Unique child sub-IDs.
- **`step3-amend-m3-egress`** → TO m-3.planner (owner-author) + m-3.implementer (paired adversarial review), CC m-7 (host), m-8/m-9 (consumers), m-1.
- **`step3-amend-m7-cred`** → TO m-7.planner + m-7.implementer, CC **m-1 (secret boundary — from the start)**, m-8 (consumer).
- **`step3-amend-m4-routing`** → TO m-4.planner + m-4.implementer, CC **m-2 (routing FieldSpec — from the start)**.

### Fold 4 [terminal-layer DESIGN agenda] — carried explicitly into both DESIGN dispatches
The audits are compatible only because the terminal boundary is still open. The DESIGN agenda distinguishes **FIVE** layers, each with a **named emitter/owner + exactly-once mapping**, no silent collapse:
1. **provider-wire terminal** — m-8 (wire normalization).
2. **provider-send / egress disposition** — m-3 (owns the egress-disposition contract; the typed denial = zero network send).
3. **routing disposition** — m-4 (emits `routing_unavailable` / `human_decision_required`; **an absent route occurs BEFORE the adapter is invoked** — routing disposition, not a wire terminal).
4. **turn terminal** — m-9 (turn semantics).
5. **relay-store `{accepted,rejected,held}`** — m-1/m-7 (the store outcome axis — separate from the above).
**Constraints:** m-8 may not mint m-4 or m-3 owner tokens; no-send paths must not fabricate a provider event, double-terminate a turn, or collapse these vocabularies. m-4 emits routing outcomes · m-3 owns egress disposition · m-8 owns wire normalization · m-9 owns turn semantics.

**What I request:** your **narrow transition check** on the amended kickoff/deviation/charter bytes (hash `983508fc…`) + the proposed dispatch headers + the terminal agenda. On your check, I issue the two PROCEED-TO-DESIGN relays + the three amendment cues per the headers above. (Your proceed-boundary accepted: no re-audit; m-8 Q5 → operator decision before credential provisioning/E3, not before design starts.)

## Verification
- Folds on disk: `STEP-3-KICKOFF.md` §6 amendment + status header · `PROTOCOL-DEVIATIONS.md` B14 · `domains/m-8-provider-adapters/README.md` (seams + §6 line) · `README.md` (current-phase + domains row). Kickoff resulting hash `983508fcbec5a511e343c731e78399af98f264a7110f75965395f940ab3fbd43` (recompute to verify).
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-audit-reconcile/RECONCILE-orchestrator-planner-20260714-235500.md` — run below.

ACTIONS_GIT_REF: none — a transition-package fold + narrow-check request; no `frank/` edit, no code, no PROCEED issued yet (awaits your narrow check). Artifacts: this relay + the amended kickoff/deviation/charter/dashboard bytes + one INDEX.md row timestamped 20260714-235500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: VP returns the narrow transition check (concur / revise) on the amended bytes + proposed headers + terminal agenda TO master.orchestrator-planner, CC operator; on concur, master issues `step3-design-m-8` + `step3-design-m-9` PROCEED-TO-DESIGN (GRILL_REQUIRED: yes) + the three `step3-amend-*` owner cues (m-1/m-2 routed from the start).
