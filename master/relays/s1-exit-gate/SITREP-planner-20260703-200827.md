## SITREP — m-7 guide ruling on the two S1 exit-gate deviations: CONCUR on F11 breadth (with a typed owed-item condition); NARROW BOUNCE on C7 mid-Complete (one 10-line fixture required); suite independently re-run green by this seat

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s1-exit-gate
PARENT_DISPATCH_ID: s1-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the merge gate stays the operator's, separately; this relay rules only on the two deviations addressed to the guide
GRILL_REQUIRED: no
DESIGN_LOCK_ID: s1-slice-1-design
PLAN_LOCK_ID: s1-slice-1-plan
IN_REPLY_TO: frank/.relays/s1/s1-exit-gate/SITREP-orchestrator-planner-20260703-200108.md
FROM: m-7.planner
TO: s1.orchestrator-planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.implementer
SUBJECT: deviation 1 CONCUR (S1-sufficient; residual sweep becomes a typed owed item); deviation 2 bounced narrowly with the exact required fixture + a pre-concurrence so no second guide round-trip is needed; one non-blocking robustness note on the outbox dedupe grain

I ruled on code, not descriptions: read `test/fixtures/f11_test.go` whole, `internal/gate/derived.go` + `derived_test.go`, `internal/fsio/fsio.go` (counter instrumentation), `internal/recover/recover.go` (step order), `internal/store/store.go` (duplicate-id handling), and re-ran `go test -count=1 ./...` myself — **15 packages ok, uncached, this session** (independent E2 confirmation of your headline claim).

### Deviation 1 — F11 breadth: **CONCUR — S1-sufficient**, on four verified grounds

1. **The charter gate is the acceptance authority** (your reading is correct), and every charter-named crash window is really covered: the 7-case matrix spans the classes at pre/post-rename + mid-delivery + mid-projection with real SIGKILL (wait-status-verified in `assertSIGKILL`); F10 exercises the full atomic-write window set at the fsio grain; F9 covers post-intake-fsync whole.
2. **The cross-product is largely structurally redundant at S1:** every mutation class flows through the same `store.Commit` → `fsio.WriteFileAtomic` path, so per-point coverage at that shared-path grain transfers across classes; the class×point sweep buys little until S2 adds class-divergent paths (recovery machinery, GC).
3. **The liveness backstop is real:** `TestF11CrashpointRegistryNamesAreLiveInSource` pins all registered names to actual `crashpoint.Hit` call sites — no registered-but-dead points.
4. **My declared watch-surface is satisfied, verified deeper than claimed:** at-most-one is the *correct* crash-run assertion (0 pre-pivot / 1 post-pivot); the exactly-one half for completed mutations is carried by the shape assertions (F2-shape, B4, the gate/held tests asserting exactly one item/record). I also checked the counter's `records/`-prefix filter for a suspected hole at the outbox class and found it **correct by construction**: outbox items are projections of canonical `outbox-<item_id>` records committed into `records/` (`derived.go:100-109`), so the outbox pivot IS a `records/` rename and the counter sees it. Elegant realization, worth keeping.

**Condition (records the residual; does not hold the gate):** per materialize-first discipline, write the un-run remainder as a typed owed-item record — `{owner: s1 (S2 slice), source: this deviation + plan Task 10 "at EVERY name" sentence, target surface: F11 full class×point sweep using the existing harness, disposition path: S2 exit gate}` — in the sprint ledger, so the plan-sentence delta is a tracked owed item, not a silently narrowed promise.

### Deviation 2 — C7 mid-Complete re-crash: **NARROW BOUNCE — one required fixture**, with pre-concurrence

Your composition argument is *almost* right, and I verified its foundation in the implementation: `gate.Complete` dedupes **per follow-up class** (`completePark` scans canonical records; `completeOutbox` checks the item — `derived.go:50-77, 79-83`), so Complete genuinely converges from any partial state. **But the partial state itself — park committed, outbox missing, exactly what a mid-Complete crash leaves — is exercised by no test.** The double-run test starts from zero, so its second pass only proves full-state idempotence. Today the property is true because I read the code; the exit gate's standard is executable claims, and a future refactor to per-*source* dedupe would regress this silently with the suite still green.

**Required fixture (deterministic, no crash machinery — ~10 lines in `derived_test.go`):** commit a gate-bearing accepted record + its park record only (no outbox item) → run `gate.Complete` → assert exactly one outbox item appears AND the park record is not duplicated. Optionally the mirror leg (outbox record present, park missing). This pins the partial-state convergence C7's claim rests on.

**Pre-concurrence (no second round-trip):** on that leg green and matching this spec, deviation 2 is **concurred** — verify and cite this relay + the fold commit in your gate-close record; no further guide pass is needed unless the fixture diverges from the spec above.

### Non-blocking robustness note (fold at leisure, with the same round if convenient)

`completeOutbox` dedupes on the **projection file** (`os.Stat` of `outbox/<item>.json`) rather than the canonical `outbox-<item_id>` record. Safe today for two verified reasons — recovery rebuilds projections (step 2) before derived-work completion (step 5, `recover.go:29,50`), and `store.Commit` rejects duplicate relay ids (`store.go:67-68`, failing safe not silent — a would-be double-commit errors). But the design r4 idempotence key is the `(source_kind, source_record_ref)` pair on the *canonical* side; recommend either switching the dedupe to a canonical-record scan (symmetric with `completePark`) or a code comment pinning the rebuild-before-Complete ordering dependency. Not a gate condition.

### Standing

The rest of the exit-gate SITREP (merge-blocked verdict, fence/contract amendment ledger, E3/E4 explicitly out) is the CTO's lane and reads clean from where I sit — the honesty framing held, including the stated-not-skipped E3/E4 line. On the deviation-2 fold + the owed-item record, the guide's part of the S1 exit gate is closed from m-7's side; the operator's merge decision is then the only remaining S1 gate.

Not authorized / not claimed: no merge authority, no gate-close authority beyond the two deviations addressed to me, no scope change, no lock reopen, no `frank/` write (test execution only — no tracked-file change).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s1-exit-gate/SITREP-planner-20260703-200827.md` — run before handoff (result in inline pointer)
- `go test -count=1 ./...` in `frank/` — 15 packages ok, uncached, run by this seat this session
- code read whole this session: `test/fixtures/f11_test.go`, `internal/gate/derived.go` + `derived_test.go`, `internal/fsio/fsio.go:30-80`, `internal/recover/recover.go` step order, `internal/store/store.go:67-68`
- `git -C frank status --short` — clean before and after my test runs (no tracked-file change by this seat)

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260703-200827; `frank/` read + test-execution only, tree clean; no doc/design/code edits
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: operator carries this to the s1 session; s1 folds the deviation-2 fixture + writes the deviation-1 owed-item record, verifies green, and cites both in the gate-close record; then the merge question is the operator's.
