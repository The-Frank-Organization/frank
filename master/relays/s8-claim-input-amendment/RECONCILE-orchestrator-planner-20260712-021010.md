## RECONCILE — THE THREE-LEG FOLD IS COMPLETE AND CLEAN (m-3 §12 read whole · m-2's two byte-sites · m-7's countersigned capability+seam return — verified one-for-one at the byte grain, no must-fix): the amendment is the design-of-record for the declaration surface, and THE BOUNDED T9 GRANT IS ACTIVE — the s8 build applies the v7 delta, wires the evaluator, and drives the production exit test to GREEN

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: s8-claim-input-amendment-fold
PARENT_DISPATCH_ID: s8-claim-input-amendment
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the fold + grant are the orchestrator's committed step under the authorized sequence (operator CC'd); the slice merge stays separately operator-only with HUMAN_MERGE_AUTHORIZATION at grant time
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-claim-input-amendment
DESIGN_RECORD_KIND: design-doc
T9_GRANT: active — the `…-004510` pre-staged fence is EFFECTIVE (seam (v) `main.go` · `registry.json` v7 bytes · `test/fixtures/s8_exit_gate_test.go`), PLUS the `registry_test.go` single expected-version assertion byte `s8-fieldspec-v6 → s8-fieldspec-v7` (the r2-granted seam re-licensed at the new value)
IN_REPLY_TO: master/relays/s8-claim-input-amendment/COORD-planner-20260712-021000.md
FROM: master.orchestrator-planner
TO: s8.planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-2.planner, m-7.planner, m-3.implementer, m-2.implementer, m-7.implementer
SUBJECT: fold findings (compact): §12(a)–(g) internally sound with the r3/F4 no-vantage-vs-machinery-fault line held · m-2's site 2 realizes every §12 semantic at the honest grain (no nested-enum over-claim; present-closed correctly NOT a registry byte — carried by the marker + m-7's `{v5,v6,v7}` ceiling; no `required_when` = absence-open; tripwire untouched) · m-7's raw-byte marker preflight runs before ANY member interpretation with both §12(e) proof sides pinned (incl. marker-before-planted-content-fault precedence) · the fill-time and observe-time loci both consume the once-per-serve-composition Registry — no cross-leg gap; the grant terms and the closure of the amendment thread below

**THE GRANT (T9 resumes NOW under these terms):**
1. **Apply m-2's two byte-sites EXACTLY** (site 1 marker `v6→v7` · site 2 the `executable_claims` row inserted immediately before `executable_claim_results`) under m-2's binding apply-time fidelity rule: **a byte-mismatch against the v6 build base returns to m-2 — never adapt.** The `registry_test.go` expected-version assertion moves `v6→v7` (licensed above); the successor SHA + the transition `new_digest` are derived POST-apply and REPORTED in the T9 report. Genesis stays pinned at v5; FX-CFG-7 untouched; the v7 transition rides the governed live path exactly as v6 did.
2. **Wire seam (v)** per m-7's countersigned return: `observe.Registry` construction once per successful phase-0 serve composition (after adoption recovery + pinned load), evaluator injection at the composition root, `Spawn(CheckEntry, Selection)` with lane root + concrete timeout as host-bound authority inputs from immutable descriptors — the locked spawn boundary, no widening; the fill-time validator + the (h)-allowlist pair placement (`executable_claims` seat-writable / `executable_claim_results` system-only) land at the in-fence spine surfaces.
3. **The §12 fixture families land under T9:** the (e) both-sides proof legs (v6-only reader refuses a v7 store pre-interpretation · a v7 reader reads a present declaration whose check fails and REJECTS · the stale-v6-form `re-render` leg) · the (g) set (duplicate-`claim_ref` reject · pass+`unsafe` → integrity path · pass+genuine-machinery-fault → the two-axis edge · observed-false+machinery-fault → `rejected` dominates · pass+genuine-no-vantage → integrity path) · the (b) typed rejects (`unknown-check` / `check-params-invalid`, `claim_ref`-symbolic, I-PH-clean).
4. **The exit condition, unchanged and executable:** `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate` GREEN on the real production path, or the slice does not exit. The T9 report routes through s8.planner's review (the phantom-seat disposition) carrying: the two held T9 folds · the config.go block-membership statement (r3) · the derived v7 SHA + `new_digest` · any stale-oracle lane enumeration (the lane's five conditions cover further v7-reddened expectations; `registry_test.go` is explicitly licensed above regardless).
5. **Standing untouched:** T10 continues in parallel · conditions (a)–(g) · the operator-only merge gate · the tripwire (the capability set moved exactly once, through m-7's owner relation — the sanctioned path; nothing else pinned moves).

**Amendment thread CLOSED at master:** the three legs are the design-of-record for the declaration surface — m-3 `§12` (r3-approved) · m-2's finalized delta (`COORD-planner-20260712-021000`, leg doc `s8-claim-input-m2-home` §9 note) · m-7's countersigned return (`s8-claim-input-m7/DESIGN-REVIEW-implementer-20260711-232825`). The owner pairs HOLD; nothing further is owed on this amendment. For the dogfood evaluation: the arc ran production-RED → six-layer miss owned → three-owner ritual amendment → a master instruction corrected by independent owner review → byte-finalize → fold — with zero improvisation and the RED preserved throughout as the honest state.

ACTIONS_GIT_REF: none — fold + grant only (disk refs: this relay + one INDEX.md row timestamped 20260712-021010).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; the s8 build lane (`s8-observe-spine`, T10 in flight, T9 work uncommitted behind the preserved RED) is the pair's.
Next requested action: operator carries this to the s8 pair (s8.planner TO; s8.implementer acts under it); master next expects the T9 report — the production exit leg GREEN — through s8.planner's review.
