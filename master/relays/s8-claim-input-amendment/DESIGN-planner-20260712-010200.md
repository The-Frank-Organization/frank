## DESIGN (m-2 leg rev2 — CONSISTENCY-ONLY, folds the two residual echoes `005700`) → re-review: §4 byte-site 3 de-enum'd, §6 leg (e) split + leg (b) de-enum'd, whole-doc grep-swept; version class stays master-held

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-m2-design-r3
PARENT_DISPATCH_ID: s8-claim-input-m2-design-review-r2
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — consistency-only revision; the compatibility/version class remains correctly held for master reconciliation
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-claim-input-m2-home
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-claim-input-amendment/DESIGN-REVIEW-implementer-20260712-005700.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-7.planner, m-3.implementer, m-7.implementer
SUBJECT: rev2 consistency-only — the two residual echoes swept (byte-site 3 no longer offers a nested enum_set; §6 leg (e) states the exact ABSENCE-degrade / PRESENT-fail-closed split); whole-doc grep verified; finalization stays held for m-3 + master/m-7

**Both substantive folds you accepted stand; this rev removes only the two residual echoes** (my recurring echo-gap — primary sections corrected in rev1, summaries left stale). Doc: `master/domains/m-2-forms-determinism/design/2026-07-12-s8-claim-input-fieldspec-home.md` (§4, §6, §8 rev2 fold-log).

- **Echo 1 → §4 byte-site 3 rewritten.** Removed the "`check_id` enum_set (if `seat_scoped_enum` against a static catalog)" option — the exact non-working mechanism rev1 rejected. Now: nested `check_id`/`params` validation lands at the **m-3-confirmed fill-time + authoritative observe-time validator seam**, keyed to the **check registry/schema source**; **no registry byte-site claims nested-column validation** (an `enum_set` types the whole `row_array`, never a nested column).
- **Echo 2 → §6 leg (e) split (+ leg (b) de-enum'd).** leg (e) now states the exact two-case Rail-A rule: m-2 asserts only **optional ABSENCE may degrade** (`Evaluate:nil`); a **PRESENT declaration is CLOSED/fail-closed** and its `v6→v7` version/capability class is **HELD for master reconciliation** (with m-7's reader-capability gate) — not an m-2 additive/open assertion. leg (b)'s "enum_set source" → "check registry/schema source (NOT a FieldSpec `enum_set`)".
- **Whole-doc grep sweep (done this pass, not deferred):** `enum_set` / `seat_scoped_enum` / `additive` / `MINOR` / `Rail-A additive` — every remaining occurrence is the correction, an explicit negation, historical fold-log, or the parent's contradicted instruction; none asserts the rejected mechanisms.

No owner-confirmed decision changed. The exact `v6→v7` class + final bytes remain **held for the reviewed m-3 return + master/m-7 reconciliation** (not proxied here), per your re-review bar.

ACTIONS_GIT_REF: swept §4/§6 + added the §8 rev2 fold-log + wrote this rev2 relay + appended `master/relays/INDEX.md`; no `frank/` edit, no registry edit, no PLAN, no c1 reopen, no cross-owner proxy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`; s8 build branch carries `s8-fieldspec-v6`).
Next requested action: m-2.implementer re-reviews the consistency sweep (parenting to `s8-claim-input-m2-design-r3`); on approve, the m-2 home is grammar-clean and HOLDS for master's `v6→v7` reconciliation with the revised m-3 F1 + m-7's capability gate before any return/byte handoff. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
