## DESIGN — three-owner AMENDMENT DISPATCH: the executable-claim DECLARATION surface (the reader-with-no-writer gap T9's production RED exposed) — m-3 owns the semantics (first-among-equals), m-2 owns the FieldSpec home landing as the second governed transition v6→v7, m-7 confirms the one new composition-root seam; bounded amendment via the ritual, NOT a re-design; the s8 build's T9 leg holds on this thread's returns

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-amendment
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded owner amendment under the standing design dispatches (the amendment path per every affected lock's own text); no operator fork is open; the slice merge stays operator-only
GRILL_REQUIRED: no — the gap is fully characterized by two independent verifications (`s8-build-escalate-fence/SITREP-implementer-20260712-003500.md` + `SITREP-planner-20260712-004500.md`) and the forcing evidence is an executable production RED; the decision record is this thread
DESIGN_DOC_ID: s8-claim-input-amendment
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-build-escalate-fence/PLAN-orchestrator-planner-20260712-004510.md
FROM: master.orchestrator-planner
TO: m-3.planner, m-2.planner, m-7.planner
CC: operator, master.orchestrator-reviewer, m-3.implementer, m-2.implementer, m-7.implementer, s8.planner, s8.implementer
SUBJECT: the locked record defines the agent-declaration semantic precisely (c2 §4:88; s8-m3 §2 id+params selection; §6.1 closed param schemas) and assigns it NO schema home — production `observe.Env.Evaluate` is nil with zero non-test call sites, and the m-2 activation matrix's seat-rendered observe set carries no claim row; each owner's leg, the binding constraints, and the return path are below; pair-internal review per your standing practice; returns TO master under unique sub-IDs

**LEG m-3 (SEMANTICS — first-among-equals; m-2's bytes finalize against your return):** the declaration triple's design — (a) shape: `(claim_ref, check_id, params)` as the locked selection semantic, its cardinality (may one record declare several claims?), and its relation to the live `EVIDENCE_TARGET` field (the declared checks are HOW the target is met — state the coupling or its absence); (b) the §6.1 relation: params validate against the check's closed param schema — name WHERE (fill-time against the registry's schemas, observe-time, or both) and the typed reject for an unknown `check_id` or non-conforming params; (c) **R2 posture, explicit:** the row is non-gate-referenceable — selection is lane INTENT; results stay conductor-computed in the system-owned output; (d) **the (h)-guard pairing, stated in your text:** the INPUT row is seat-owned/declared, the OUTPUT `executable_claim_results` is system-owned — opposite sides of the channel-keyed suppliability guard, by design; (e) **Rail A line:** the row is ADDITIVE — its absence is exactly the honest no-vantage degrade (`Evaluate: nil` semantics preserved verbatim per the binding r5 point 3); (f) I-PH over every rendered/bounce surface the row grows.

**LEG m-2 (the FieldSpec HOME — the bytes):** one seat-fillable row (shape yours — the assessment sketched an `executable_claims` row_array, `layer_present:observe`-gated, seat-owned/declared; your grammar governs), landing as the **SECOND governed fieldspec transition v6→v7 over YOUR successor map** — the T2-built §2.4 acceptance machinery and the FX-CFG-10 legs run a second live lap (spine-proving, not overhead). The row joins your already-scheduled bounded §2/§9 amendment set (one review moment stays one review moment); registry byte-sites enumerated in your return; the A-1 stale-form story rides the standing machinery (a v6-rendered form bounces `re-render` post-transition — correctly).

**LEG m-7 (the SEAM CONFIRM):** seam (v) at the composition root — `observe.Registry` construction + evaluator injection into the production submit handler + the RegistryEnv wiring construction needs (lanes, schema refs, the executor handle per your locked r4 spawn contract). Confirm: (i) it composes with the locked §3 step order (observe consumes the selection at step 4; no new step); (ii) no boot-order interaction with the §5.1.5a recovery ordering (none expected — say so); (iii) the evaluator consumes the executor exactly at the locked `spawn(check_id, params, lane_ref, timeout) → CheckVerdict` boundary — no widening.

**CONSTRAINTS BINDING ALL THREE LEGS:** the TRIPWIRE (no lock-pinned value moves — the genesis v5 SHA / FX-CFG-7 hash / capability exact-sets move only through the owner relations; the catalog byte-pin `943f07bb…` is the s8 pair's pending-review artifact, not this thread's) · the confusion-firewall rails (Rail A stated per surface; Rail B — this is drift-prevention and honest-labeling machinery, keep the claims confusion-graded) · the ratified interim defaults + s10 sunset untouched · scope is the DECLARATION surface only — no adjudication-rung content (that is design item 9, s9, separately).

**RETURN PATH:** pair-internal review per your standing practice (author + implementer review, small tier); returns TO master under unique sub-IDs (`s8-claim-input-m3` / `-m2` / `-m7`); m-2 and m-7 may draft in parallel but m-2's bytes FINALIZE against m-3's returned semantics; master folds the reconciled result into the exact T9 seam grant to the s8 pair (`s8-build-escalate-fence-r5-ruling` pre-staged it: seam (v) + `registry.json` + `s8_exit_gate_test.go`); the s8 build runs T10 in parallel meanwhile and T9 resumes on the fold. The exit condition is executable and already written: `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate` turns GREEN on the real production path, or the slice does not exit.

ACTIONS_GIT_REF: none — a design-amendment dispatch; no `frank/` edit (disk refs: this relay + one INDEX.md row timestamped 20260712-004511).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; cwd is not a git repo (docs workspace).
Next requested action: operator carries this to m-3.planner, m-2.planner, m-7.planner; m-3 returns first or alongside; master folds and the T9 grant issues.
