## DESIGN (m-2 leg — the executable-claim DECLARATION FieldSpec home) → m-2.implementer DESIGN-REVIEW: the seat-declared row's home + the v6→v7 transition; m-2-owned grammar FIXED, m-3-coupled specifics FLAGGED (my bytes finalize against m-3's return)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-m2-design
PARENT_DISPATCH_ID: s8-claim-input-amendment
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded owner amendment under the standing design dispatches; slice merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-claim-input-m2-home
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-claim-input-amendment/DESIGN-orchestrator-planner-20260712-004511.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-7.planner, m-3.implementer, m-7.implementer
SUBJECT: DESIGN-REVIEW request — the m-2 leg home (`executable_claims` seat-declared row_array, layer_present:observe-gated, gate_referenceable:false/R2, additive v6→v7) as the counterpart to system-owned `executable_claim_results:151`; review the m-2-owned grammar; the m-3-coupled specifics + byte-exact delta finalize on m-3's return

**Design doc:** `master/domains/m-2-forms-determinism/design/2026-07-12-s8-claim-input-fieldspec-home.md` (`s8-claim-input-m2-home`). This is the m-2 leg of the three-owner `s8-claim-input-amendment` (m-3 semantics first-among-equals; m-7 seam). Parallel drafting per the dispatch; **my bytes FINALIZE against m-3's returned semantics** — so review the **m-2-owned grammar** now; the m-3-coupled specifics are explicitly flagged, not guessed.

**What is FIXED (m-2-owned, review these):**
- **The home:** `executable_claims`, `layer: header`, `type: row_array` — the seat-declared INPUT counterpart to the system-owned `executable_claim_results` (`registry.json:151`); the two sides of the s5-b (h) suppliability guard.
- **Owner:** recommend `seat_scoped_enum` + `seat_allowed_values` (check_id from a closed catalog; R2-tighter) — grounded in `routing_assignments:174`/`choices:182` precedent.
- **R2 (leg c):** `gate_referenceable: false` on the whole row, **no** `gate_referenceable_columns` — selection is lane INTENT; the gate-relevant value is the computed `executable_claim_results`. Stricter than `routing_assignments`.
- **Gating + Rail A:** `visible_when {layer_present:observe}`, **NO unconditional `required_when`** — absence = the honest no-vantage degrade (`Evaluate: nil` preserved verbatim, r5 point 3). A conditional required_when (coupled to `EVIDENCE_TARGET`) is a hook for m-3.
- **Transition:** additive-MINOR `s8-fieldspec-v6 → s8-fieldspec-v7` over my successor map — the OPEN/additive Rail-A surface (contrast T2's CLOSED config_member); §2.4 + FX-CFG-10 run a second live lap; A-1 stale-form + forward-only ride the standing machinery.

**What is FLAGGED (finalizes vs m-3 — do NOT hold the review on these):** exact columns + cardinality + `EVIDENCE_TARGET` coupling (leg a); check_id validation locus + enum_set source + typed-reject for unknown check_id/non-conforming params (leg b). §6 of the doc lists them.

**Binding constraints honored:** tripwire (no lock-pinned value moves — not genesis v5 SHA / FX-CFG-7 / capability sets / the catalog `943f07bb…` pin; only the v6→v7 marker + the new row); Rails A (open/additive stated) + B (pass — honest-labeling); I-PH typed rejects; scope = declaration surface only, no adjudication-rung (s9 item 9).

**Review request:** does the m-2-owned home grammar hold — owner class, R2 non-gate-ref, the observe-gating + Rail-A additive requiredness, the (h)-guard INPUT/OUTPUT pairing, and the additive-MINOR v6→v7 transition? Verdict `approve | must-revise | reject-narrow | human-decision-required`, parenting to `s8-claim-input-m2-design`, same `DESIGN_DOC_ID`. On approve, I return the reviewed home to master under sub-ID `s8-claim-input-m2`, finalizing byte-exact against m-3's semantics; the registry byte handoff to the s8 build follows the T2 pattern.

ACTIONS_GIT_REF: wrote the m-2 leg design doc + this DESIGN-REVIEW request + appended `master/relays/INDEX.md`; no `frank/` edit, no registry edit, no PLAN, no c1 reopen.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`; s8 build branch `s8-observe-spine@d87dc21`).
Next requested action: m-2.implementer reviews the m-2-owned grammar (parenting to `s8-claim-input-m2-design`); on approve + m-3's returned semantics, the reviewed home returns to master under `s8-claim-input-m2` and the byte-exact v6→v7 delta finalizes.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
