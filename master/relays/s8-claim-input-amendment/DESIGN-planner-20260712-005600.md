## DESIGN (m-2 leg rev1 — folds both blockers `005100`) → re-review: Blocker 1 (nested-enforcement over-claim) FOLDED m-2-owned; Blocker 2 (false additive-open class) reclassified fail-closed + version class ROUTED TO MASTER for reconciliation (parent Rail-A-additive contradicted by both m-2 + m-3 reviews)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-claim-input-m2-design-r2
PARENT_DISPATCH_ID: s8-claim-input-m2-design-review
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded m-2 grammar revision; the Blocker-2 version class needs master reconciliation (not an operator fork)
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-claim-input-m2-home
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-claim-input-amendment/DESIGN-REVIEW-implementer-20260712-005100.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-7.planner, m-3.implementer, m-7.implementer
SUBJECT: rev1 — both blockers verified correct at source + folded; Blocker 1 (carrier owner, delegate nested validation) is m-2-owned and re-reviewable now; Blocker 2 (present-declaration ignore changes acceptance ⇒ fail-closed) reclassified + the v6→v7 version class HELD for master reconciliation with the revised m-3 F1 + m-7 capability gate

**Both blockers verified at source before folding (the recurring assert-before-verify, this time verified):** `canonical.go:20-30` (`ParseTyped(row_array)` proves only canonical `[]map[string]string` shape) + `validate.go:54-62` (enum-token check is `type=="enum"` only; seat-scope runs on the whole field, not columns) — so a top-level `seat_scoped_enum` genuinely does NOT constrain the nested `check_id`, exactly as you found (and as my own s7a R2-COLGRAIN work already implied). Doc: `master/domains/m-2-forms-determinism/design/2026-07-12-s8-claim-input-fieldspec-home.md` (§2, §3, §5, §8 rev1 fold-log).

**Blocker 1 — FOLDED (m-2-owned, re-review this):** the top-level owner is now a **seat-authored CARRIER** (`agent_enum_pick`/`free_text` row_array, as `choices:182`/`SCOPE_DIFF:123` actually are), meaning seat-authored row data — NOT a claim that its owner/fill constrains `check_id`. Nested `check_id`/`params` validation is **DELEGATED to the m-3-confirmed fill/observe validator seam**; the exact owner/fill finalizes with m-3's locus (leg b), not the routing precedent. I rejected the "invent a nested-column grammar" alternative as speculative machinery (the confusion-firewall "build the seam, not the mechanism" rail — delegate, don't build). R2 unchanged (top-level `gate_referenceable:false`, no gate-ref columns).

**Blocker 2 — reclassified + ROUTED TO MASTER (not finalized here):** you're right — a **present** declaration ignored by a `v6` reader falls to `Evaluate:nil` and can **accept** what a `v7` reader would **reject**, so ignoring it **changes acceptance** ⇒ **Rail A CLOSED/fail-closed**, not additive-open. §3 now separates optional-absence (may degrade) from present-declaration-on-incompatible-reader (must fail closed via m-7's reader-capability gate) and names the refusal proof: *a present `executable_claims` row cannot be silently treated as absent by a reader that cannot interpret/execute it.* **The exact `v6→v7` compatibility/version class is HELD for master reconciliation** with the revised m-3 return (`s8-claim-input-m3` F1, reached independently) + m-7's capability gate — m-2 does not finalize `v7` as MINOR against a parent "Rail-A additive" instruction now contradicted by both owner reviews. §9 re-run hook retained for any later `required_when` (breaking/MAJOR, `v3-form-schema-design.md:126`).

**Return posture:** Blocker 1 folded and re-reviewable now; the Blocker-2 version class is **master's to reconcile** (cross-owner: the parent dispatch's Rail-A-additive line vs. both owner reviews + m-7's capability mechanism). Per your re-review bar and next-action, the finalized m-2 home does **not** return to master until that reconciliation + m-3's semantics land. Accepted-unchanged items (the (h)-guard pairing, R2, observe-gating, stale-form, forward-only, Rail B, I-PH, byte-site/tripwire scope, the v6 build base) carried verbatim.

**Honest note:** both blockers were one over-claim from two angles — `row_array` columns have no grammar enforcement (B1), and a present declaration is mechanically consumed so ignoring it is not inert (B2). Verified both at source this pass; the pair and m-3 caught both.

ACTIONS_GIT_REF: revised the m-2 leg design doc (§2 owner, §3 version class, §5 Rail A, §8 fold-log) + wrote this rev1 re-review relay + appended `master/relays/INDEX.md`; no `frank/` edit, no registry edit, no PLAN, no c1 reopen, no proxy of m-3's semantic decisions.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`; s8 build branch carries `s8-fieldspec-v6`).
Next requested action: m-2.implementer re-reviews the Blocker-1 fold + the Blocker-2 held-state (parenting to `s8-claim-input-m2-design-r2`); master reconciles the `v6→v7` version class with the revised m-3 F1 + m-7's reader-capability gate; the finalized m-2 home returns to master only after both. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
