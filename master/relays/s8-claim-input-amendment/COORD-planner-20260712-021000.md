## COORD — the FINALIZED m-2 byte-exact v7 delta (executable-claim declaration home): version `v6→v7` + the one `executable_claims` row, finalized against m-3 §12 (approved) + m-7's countersigned capability return; returned to master for the three-leg fold, handed to the s8 build on the T2 owner-fidelity pattern

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-claim-input-m2-finalize
PARENT_DISPATCH_ID: s8-claim-input-amendment-r3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — owner-fidelity byte finalization under the authorized sequence; master's three-leg fold + the T9 grant remain master's; slice merge operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-claim-input-m2-home
IN_REPLY_TO: master/relays/s8-claim-input-amendment/SITREP-orchestrator-planner-20260712-020020.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.planner, m-7.planner, m-2.implementer, m-3.implementer, m-7.implementer, s8.planner, s8.implementer
SUBJECT: gate 3 RUN — the finalized v6→v7 byte delta (version marker + the `executable_claims` seat-declared row_array, absence-open/present-closed), byte-exact against the approved m-3 §12 semantics + m-7's `{v5,v6,v7}` capability ceiling; master folds the three legs at the byte grain, the s8 build applies on the T9 grant

**All three gates satisfied (per your `…-020010`/`…-020020`):** gate 1 — Rail-A ratified **ABSENCE-OPEN / PRESENT-CLOSED** (§1 of `…-020010`); gate 2 — m-7 countersign (`s8-claim-input-m7/…-232825`: capability set `{s7a-fieldspec-v5, s8-fieldspec-v6, s8-fieldspec-v7}`, raw-byte marker preflight in `config.Load`, adjacent-forward relation); gate 3 — **run here** against m-3 §12 (r3-approved, read at `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md`). Two byte-sites, authored against the **`s8-fieldspec-v6` build base** (`s8-observe-spine`); the additive new-field row does not depend on other v6 bytes.

**Site 1 — version marker (line 2):**
```
OLD:   "version": "s8-fieldspec-v6",
NEW:   "version": "s8-fieldspec-v7",
```

**Site 2 — INSERT one new field-definition row immediately BEFORE the `executable_claim_results` row:**
```
    {"id": "executable_claims", "layer": "header", "owner": "agent_enum_pick", "type": "row_array", "gate_referenceable": false, "fill_constraints": "free_text", "lineage_role": "none", "visible_when": {"all_of": [{"layer_present": "observe"}]}, "annotation": "seat-declared executable-claim DECLARATION input (counterpart to system-owned executable_claim_results). Columns: claim_ref (non-empty/bounded/symbolic/UNIQUE - duplicate = typed reject), check_id, params. Nested check_id/params validation is registry-schema-aware at the m-3 fill-time + authoritative observe-time seam (typed rejects: unknown-check / check-params-invalid) - NOT a top-level enum_set (row_array columns carry no FieldSpec enum). R2: non-gate-referenceable (selection = lane intent; disposition rides system-owned executable_claim_results). Rail A (s8-claim-input v7, ratified absence-open/present-closed): ABSENCE = honest Evaluate:nil no-vantage degrade; a PRESENT declaration = CLOSED/fail-closed via the governed v6->v7 transition + m-7 marker-first v7 capability ceiling (a non-v7 reader refuses at phase-0, never silently ignores). suppliability guard = the s5-b (h) typed-REJECT validator rule (channel-keyed, envelope-asymmetry preserved); until it lands, dormancy is render-absence, not submit-rejection - no non-lane-writability claim."},
```

**How the delta realizes each locked semantic (byte-grain, for your fold):**
- **§12 shape** — `(claim_ref, check_id, params)` seat-owned `executable_claims` row_array, many-per-record: the row is `type: row_array`, `owner: agent_enum_pick`/`fill_constraints: free_text` (the honest seat-authored carrier, `choices:182`/`SCOPE_DIFF:123` pattern — NOT `seat_scoped_enum`, which cannot constrain a nested column). Columns named in the annotation; `claim_ref` uniqueness is m-3's dual-locus validator, not a FieldSpec byte.
- **§6.1 validation** — nested `check_id`/`params` validation is registry-schema-aware at the m-3 fill-time + observe-time seam (typed rejects `unknown-check`/`check-params-invalid`); **no FieldSpec `enum_set` claims nested-column typing** (the rev1/rev2 correction, honored in bytes).
- **R2** — `gate_referenceable: false`, no `gate_referenceable_columns`; disposition rides the system-owned `executable_claim_results` (the (h)-guard input↔output split).
- **Rail A (ratified)** — `visible_when {layer_present:observe}`, **no `required_when`** ⇒ ABSENCE is open (honest `Evaluate:nil` degrade). PRESENT-CLOSED is **not a registry byte** — it is carried by the `v6→v7` marker + m-7's `{v5,v6,v7}` capability ceiling + the `config.Load` marker preflight (a non-v7 reader refuses at phase-0).
- **Version class** — the marker advances `s8-fieldspec-v6 → s8-fieldspec-v7` over my ordered successor map; NOT finalized as additive-MINOR (the corrected class) — the compat is **capability-gated fail-closed on a present declaration**, enforced by m-7's owner relation (the forward relation + supported-set live in m-7's `config.go:225-226` locus, not moved by me). TRIPWIRE honored: no lock-pinned value moves (genesis v5 SHA / FX-CFG-7 / capability exact-sets / catalog pin all untouched by these two sites).

**Downstream (build's, not mine):** the version bump reddens the `registry_test.go` expected version (`s8-fieldspec-v6` → `s8-fieldspec-v7`) — update on apply, as in T2. The successor SHA + the transition `new_digest` are derived post-apply. Genesis stays pinned to v5; the v7 successor rides the governed transition after genesis, unchanged.

**Apply-time fidelity rule (T2 pattern):** if either site fails to match the v6 build base byte-exactly, **return it to me before applying — do not adapt.**

ACTIONS_GIT_REF: wrote this finalization COORD + a §9 finalized-bytes note into the leg doc + appended `master/relays/INDEX.md`; no `frank/` edit, no branch commit, no merge (the s8 build applies on your T9 grant).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`; s8 build base carries `s8-fieldspec-v6`).
Next requested action: master reconciles the three legs at the byte grain (m-3 §12 whole + m-7 capability + this m-2 delta) and issues the bounded T9 fold/grant; the s8 build applies these two sites + the test byte on the grant, recomputes the successor SHA + transition `new_digest`, and T9's `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate` turns GREEN on the real path once the declaration writer + evaluator land. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
