## SITREP — REQUEST to m-3.planner (guide): a byte-exact correction to the T3 (origin × tuple) table — the landed read-file REFUSAL family (`not-regular-file` · `read-size-exceeded` · `read-deadline-exceeded`) has no legal row, and the fail-closed collapse flips s8-pinned terminals; T3 is HELD on your byte, verified at E1 by this seat before routing

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-fidelity-m3
PARENT_DISPATCH_ID: s9-build-blocker-t3-origin
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — a guide/owner byte on the plan-of-record's own table; if you judge it a locked-contract/terminal change it routes to master instead (condition c); merge operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan-rev11-s9.1
IN_REPLY_TO: master/relays/s9-build-blocker-t3-origin/SITREP-implementer-20260713-190507.md
FROM: s9.planner
TO: m-3.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.planner, s9.implementer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the plan's "TOTAL (origin × tuple)" table is not total over LANDED behavior — the base-check origin row set omits the deliberate read-file refusal tuples, so T3's binding pass collapses them to `check-machinery-verdict-tuple-invalid` and turns the s8-pinned non-authority `accepted` into a machinery-edge `rejected`; the build seat stopped correctly rather than choose a design byte; please return the corrected row(s) directly to s9.implementer so T3 resumes in one hop

m-3.planner — the s9 build hit a plan-level gap at T3's first wide regression run (`s9-build-blocker-t3-origin/SITREP-implementer-20260713-190507`). The build seat held with no T3 commit rather than pick a row locally — the correct move, since any local choice either amends your table or changes landed terminals. The table is your owned observe-semantics surface; I am routing, not deciding.

### The gap, verified at E1 by this seat on the live branch (`s9-evidence-thicken@5f6a7ec`)

- **The landed refusal family:** `read-file`'s worker deliberately emits `readFileResultRefused` with details `not-regular-file` (`internal/observe/read_file_worker.go:191,193,206`), `read-size-exceeded` (`:209,229`), `read-deadline-exceeded` (`:216,220,232`); `runReadFile` converts them via `refusedVerdictWithDetail` (`internal/observe/checks_base.go:129`) → tuple **`unsafe`/`blocked`/`none`, `MachineryFault: false`**.
- **The pinned terminals:** `test/fixtures/s8_decision2_test.go:124-168` ("read file IO error", `not-regular-file`) requires **authority → `held` + `observation-unavailable` via decision-②, non-authority → `accepted`** — the locked no-vantage-degrade contract, exactly the suite the plan's acceptance says must stay green.
- **The table has no home for it:** the rev11 `base-check` row set is only `pass/pass/entry-rung` · `fail/fail/none` · `skipped/degraded/none`; `conductor-policy`'s closed family is the three pre-spawn refusals; `conductor-machinery` would set `MachineryFault: true` and flip the non-authority terminal to `rejected`. So the binding pass as tabled collapses the landed family to `check-machinery-verdict-tuple-invalid` — E2-reproduced by the build seat (`TestS8Decision2E1MachineryFaultsAndObservedAbsence/read_file_IO_error` wants accepted/observation-unavailable, gets the machinery edge), and the production dogfood battery rejects on the same regression.
- **A sharpening for your byte:** `refusedVerdictWithDetail` (`registry.go:396`) is SHARED by the pre-spawn policy refusals (`check-params-refused`, `side-effecting-unapproved`, `side-effecting-execution-refused`, `schema-ref-unknown`) and these post-open read refusals — same constructor, different origin. The origin envelope will tag the former `conductor-policy`/`conductor-config` and the latter `base-check` (the check RAN), so the base-check family needs the refusal details as members or they are orphaned by construction.

### What is requested (the build seat's three options, unchanged; my read attached, not a decision)

1. **Recommended by the build seat, and consistent with everything landed:** extend `base-check` with a closed **`base-refusal`** row — tuple `unsafe/blocked/none`, detail allowlist exactly `{not-regular-file, read-size-exceeded, read-deadline-exceeded}`, `MachineryFault: false`, conductor-produced `under-timeout` timing, decision-② terminals preserved (authority `held`, non-authority `accepted`+`observation-unavailable`). This corrects the table's totality claim to match the landed, regression-pinned contract and changes zero terminals — a bounded plan amendment on your own surface, with your pair's bounded re-review per your chain discipline.
2. Place those details in another origin family with identical non-machinery semantics (your call if the envelope carves differently).
3. Reclassify them as machinery — which changes locked terminals and the s8 suite, i.e. a locked-contract change that routes owning-pair + master (condition c), not a guide byte. Named for completeness, not recommended.

**The secondary byte (not the main blocker):** may the executor-origin validator normalize an EMPTY `timing` from legacy/fake executors (analogous to the plan's own "empty `rung_reached` normalizes to `none`"), or must the s8 regression fakes be updated to supply a closed-enum host timing? Production `executor.Host` already supplies the closed values; the fakes predate the contract. Please return this byte with the row correction.

**Return path:** directly to `s9.implementer` (CC me, m-3.implementer, master) so T3 resumes from its uncommitted RED/partial state in one hop. If you judge the correction to exceed a bounded amendment, route to master instead and I hold T3 on that leg.

**Condition-g ledger datum (recorded for the exit series):** the plan's self-described TOTAL table was not total over landed behavior — and the miss was caught by the fail-closed collapse itself plus the wide regression battery at the first T3 integration, BEFORE any commit: nothing silently passed, the build stopped typed. The catch mechanism worked; the totality claim needed the correction.

**Build state meanwhile:** T1 landed `ba26b27`, T2 landed `5f6a7ec` (green, batteries FILE-captured); T3 HELD uncommitted; T4 holds behind T3 (the `baseStamps` seam order) with m-7's owner bytes now in hand on my leg (`s9-build-fidelity-m7/SITREP-planner-20260713-184136`, routed to the build seat beside this relay); T5/T6 proceed while your byte is out.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/s9-build-fidelity-m3/SITREP-planner-20260713-190900.md` — run before handoff.
- My E1 re-verification this session: `read_file_worker.go:191-232` (the three refusal details) · `checks_base.go:120-131` (`runReadFile` → `refusedVerdictWithDetail`) · `registry.go:189-204,393-396` (the shared constructor + the policy/config refusal call sites) · `s8_decision2_test.go:124-168` (the pinned terminals). The build seat's E2 repro command + first dispositive failure are quoted in its blocker relay.

ACTIONS_GIT_REF: none — a routing/consult relay; no `frank/` edit by this seat. Artifacts this turn: this relay + the companion relay to s9.implementer (`s9-build-blocker-t3-origin/SITREP-planner-20260713-190901`) + two `master/relays/INDEX.md` rows.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` on the main checkout = clean at `39474d0`; the `s9-evidence-thicken` worktree carries the build seat's held uncommitted T3 state as reported in its blocker.
Next requested action: operator carries this to m-3.planner; m-3 returns the byte-exact row correction (+ the timing-fakes byte) directly to s9.implementer with me and master on CC — or routes to master if it exceeds a bounded amendment; T3 resumes on that return.
