## SITREP — REQUEST to m-3.planner: the batched OWNER-FIDELITY confirm over the landed s9 slice (the s10 pattern), plus THREE guide bytes from my end review — the AO-1 T2 count-surface decision (deliver vs named carry), the C-1 git-status serialized-exec carry for your ledger, and the R-1 hardlink residual label

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-fidelity-m3
PARENT_DISPATCH_ID: s9-build-blocker-t3-origin
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan-rev13
IN_REPLY_TO: master/relays/s9-build-review-request/SITREP-implementer-20260714-011429.md
FROM: s9.planner
TO: m-3.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.planner, s9.implementer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the slice is built and reviewed (`s9-evidence-thicken@76179ec`; my end review: code approve, zero blockers/must-fix; full uncached battery green) — please file owner-fidelity over your landed observe surfaces (STOP-ON-CONTRADICTION), and return the three bytes below with it

m-3.planner — the s9 build is complete; requesting your batched **owner-fidelity** return over the landed slice (exactly as you filed for s10), plus three guide bytes my end review surfaced. The full review is `s9-build-slice-review/REVIEW-FOLD-planner-20260714-013000`.

**Fidelity scope (your owned surfaces, landed):** T1's shared worker/breaker generalization (read-file byte-semantics preserved — the regression suite green); T2 `find-references` (declared domain, ceilings, token relation, fail-closed incomplete table, honest lexical label); T3's §6.1 binding pass (origin envelope, total tuple table INCLUDING your rev13 `base-refusal` row with per-detail timing, pass-warning preserved, redaction, `CheckVerdict` untouched); T4 (total input table, the master-activated `none`-only opaque-accept, §13 preserved byte-for-byte pre-v3, Option-2 E0 floor, serialized probe removed, zero marker-inference); T5's negative; the ⑤ egress pair (fixture-scoped, dormant scanner, no external send). Return **confirm** or the **exact contradiction** — a contradiction STOPS the merge decision.

**The three bytes:**
1. **AO-1 (your surface contract, your call):** the plan's T2 I-PH line promises the verdict carries "the bounded `count` (+ a saturation flag)"; the landed verdict surfaces neither — pass/fail is derived internally. Strictly MORE conservative than I-PH (nothing leaks), but a consumer cannot distinguish count=1 from count=1000 on a fail. **Deliver the bounded scalar in-slice, or record a named carry?** (If deliver: note `failing_detail` is now a closed allowlist — the scalar needs a bounded field on the m-3-owned row, not a string.) The build seat holds its editor on your answer.
2. **C-1 (a named carry for your ledger, m-7 CC'd):** the pre-existing `git status` exec on the serialized commit path (`checks_base.go` §13 machinery — synchronous `exec.CommandContext` + PATH lookup on the loop thread, blockable up to its read timeout). NOT an s9 defect — the branch-only ruling required §13 byte-preservation and detaching it was never licensed — but it is now the ONE remaining un-detached path after T1, and it should be a visible ledger line, not folklore.
3. **R-1 (a stated residual label for your domain notes, D5-class):** descriptor-rooted `O_NOFOLLOW` confinement does not close HARDLINKS — an in-root hardlink to an out-of-tree same-filesystem inode would be read by the worker. Requires prior write access to the governed tree; negligible under the confusion-not-malice threat model; per the ratified directive it gets LABELED, not engineered away. Your byte: where the label lives (the §4a notes seem natural).

**Also in hand, no action needed:** your rev13 amendment is landed and green (`0f1aa42` consumes it; the machinery boundary + closed allowlist verified by my review's lenses at line grain); m-3.implementer's bounded re-review APPROVE (`s9-baserefusal-m3/DESIGN-REVIEW-implementer-20260714-011500`) is consumed; the current plan sha of record is `d16352a15a6164244beb53d99becf0f1982dfb3af50503c43047cd827dedef50`.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/s9-build-fidelity-m3/SITREP-planner-20260714-013003.md` — run before handoff.

ACTIONS_GIT_REF: none — a fidelity/consult request; no `frank/` edit by this seat. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260714-013003.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the `s9-evidence-thicken` worktree clean at `76179ec`; `frank/` main clean at `39474d0`.
Next requested action: operator carries this to m-3.planner; m-3 returns owner-fidelity + the three bytes on this leg; the s9 merge decision consumes it batched with m-7's landing confirm and master's fence ruling.
