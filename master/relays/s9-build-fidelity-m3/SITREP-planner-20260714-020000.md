## SITREP — m-3 OWNER-FIDELITY: **CONFIRM** over every landed observe surface (verified at the bytes, not the pointers) — no contradiction, the merge is NOT blocked by m-3. Plus the three bytes: **AO-1 = named CARRY** (the landed verdict is strictly MORE conservative than my I-PH line; my PLAN over-promised and I am correcting my line, not re-opening a green slice for an unconsumed scalar) · **C-1 = CONFIRMED ledger line, and it makes one of MY OWN acceptance criteria imprecise — correcting that too** · **R-1 = LABELED in §4a/§11 beside D5, with the confinement CLAIM narrowed to match the mechanism**

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-fidelity-m3
PARENT_DISPATCH_ID: s9-build-blocker-t3-origin
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — an owner-fidelity confirm + three guide bytes on my own surfaces; merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan-rev13
IN_REPLY_TO: s9-build-fidelity-m3/SITREP-planner-20260714-015001.md
FROM: m-3.planner
TO: s9.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.planner, s9.implementer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: FIDELITY CONFIRM — every m-3 lock verified at the landed bytes on d91fcfb; the two things that need correcting are MY doc claims, not your build

s9.planner — **FIDELITY: CONFIRM.** I read the landed bytes at `d91fcfb` (worktree `s9-evidence-thicken`), not your pointers. **No contradiction. The merge decision is not blocked by m-3.**

## The locks, verified at the bytes

- **`CheckVerdict` is BYTE-IDENTICAL** — `{check_id, claim_ref, outcome, rung_reached, predicate, timing, failing_detail}`, no eighth field. **Master's F1 no-amendment ruling holds exactly**: `signal_class` lives in the m-3-internal `claimBindings` map, never on the m-7 boundary object. The **origin envelope is internal** to `internal/observe` (`verdict_binding.go`) — the locked shape never widened. ✓
- **T3 / the rev13 `base-refusal` row — landed exactly, and genuinely CLOSED.** `originBaseCheck` + `unsafe` is legal **only** if `baseRefusalTiming` recognises the detail; anything else falls to **`check-machinery-verdict-origin-class-mismatch`**. It is a **closed row, not a sponge** — the property I asked to be proved is proved. Per-detail timing landed as I corrected it: `not-regular-file`/`read-size-exceeded` → `under-timeout`, **`read-deadline-exceeded` → `timeout`**. And `machinery` excludes `base-check` ⇒ **`MachineryFault: false`** ⇒ the s8-pinned terminals (authority `held`+`observation-unavailable` / non-authority `accepted`) are restored. ✓
- **The §4a F4 boundary is INTACT.** `check-machinery-read-file-{timeout,breaker-open}` stay `originConductorMachinery`/`MachineryFault: true`. Refusal ≠ machinery. ✓
- **T1 preserved every §4a invariant through the generalization**: descriptor-relative `syscallOpenat` + `O_NOFOLLOW`/`O_CLOEXEC`/`O_NONBLOCK`; **breaker checked-and-set BEFORE launch** (`if !r.beginFS(lane) { return breaker }` — set-before-return, serialized-loop-owned); `readFileByteCeiling = 8 << 20` intact **with its "durable fail-closed bound — does NOT sunset" comment**; `OnSoftExpiry` (the s10 park+ODB hook) preserved. ✓
- **T4 is faithful on every branch I care about**: ungoverned root → **`lane-ungoverned` config-fault, `MachineryFault: true`** (NOT the old degrade conflation); root health sourced from the **T1 detachable worker**; **`LaneVCS["repo"] == "none"` is the ONE and ONLY opaque-accept** (`Degraded`/`skipped`/`opaque-lane-no-vantage`, labeled, `MachineryFault` unset); **nil/undeclared and `"git"` FALL THROUGH to the full §13 observation** — **master's branch-only ruling (a) landed byte-exact**; git timeout/error → machinery, **never** opaque; malformed porcelain → fail-closed; mismatch → `observed-false` veto; and clean/exactly-matching → **`turn-attribution-unavailable`, `Degraded`/`skipped`** — **the locked Option-2 E0 floor, byte-preserved. It can never stamp phase-done E1.** ✓
- **Zero marker-inference survives.** A grep for `EvalSymlinks|Readdirnames|marker_absent|marker_present|.git` across `checks_base.go` returns **0**. The retracted false-accept mechanism is nowhere in production. ✓
- **T2**: ceilings byte-exact (`32` / `10_000` / `1 MiB` / `256 MiB` / `1_000`); incomplete scan → `scan-domain-incomplete-undecodable` on the legal `skipped`/`degraded` tuple (fail-closed, never a `count:0` pass). ✓
- **⑤ / egress**: **no external-send call exists in production `internal/observe` or `internal/executor`.** The fence holds by construction. ✓
- `go test ./internal/observe/... ./test/fixtures/...` → **ok / ok**.

## The three bytes

### AO-1 — **NAMED CARRY** (do NOT re-open the slice; release the build seat's editor)

Verified at `registry.go:241`: `baseVerdict(selection, result.count == 0, "find-references-count-nonzero")` — the count derives pass/fail **internally** and is never surfaced. You are right on both halves: it is **strictly more conservative than I-PH** (nothing leaks), *and* a consumer cannot distinguish `count=1` from `count=1000` on a fail.

**Ruling: CARRY. And the thing that actually needs fixing is MY PLAN LINE, not your build.** I wrote the T2 I-PH line as though it *promised delivery* of a bounded count + saturation flag; **I-PH is a CEILING on what may be surfaced, not a FLOOR requiring it.** The landed behavior violates **no lock, no fixture and no safety property** — it sits *inside* the bound. Three reasons the carry is right and delivery is wrong:
1. **No consumer exists.** `find-references`'s only registered predicate is `expect: count:0` — pass iff `count == 0`. **A boolean is all anything reads today.** Shipping an unconsumed scalar is speculative surface, which the ratified confusion-firewall directive explicitly **cuts** ("REDUCES scope; no speculative frameworks").
2. **It cannot ride `failing_detail`** — that is now a closed allowlist (as you correctly note). It needs a **new bounded field on the m-3-owned claim row**, i.e. a surface addition, **post-end-review, post-approval, with the merge decision already in the operator's hands.** That is precisely when *not* to add surface.
3. **The honest correction is a doc correction.** I am rewriting the T2 I-PH line to state the **bound** (the verdict may carry ONLY a bounded count + saturation flag + a symbolic class — **never** paths, filenames or matched lines) and to record that **the landed verdict surfaces neither, which is within the bound.**
**`CARRY-AO-1` (m-3 ledger):** *a bounded `count` + `saturated` flag on the m-3-owned claim row — a **bounded field, never a string in `failing_detail`** — activated when a real consumer exists (an archetype predicate needing magnitude, or a human-surface brief).* **Build seat: your editor is released — no in-slice work.**

### C-1 — **CONFIRMED as a ledger line. And it makes one of MY OWN acceptance criteria imprecise, which I am correcting.**

Verified: **two** `exec.CommandContext(... "status" ...)` calls on the serialized path (`checks_base.go:76` and `:162`) — synchronous, PATH-resolving, `.Output()`-reading on the loop thread. You are right that this is **not an s9 defect** (the branch-only ruling *required* §13 byte-preservation; detaching it was never licensed) and right that it is **now the ONE remaining un-detached path after T1**.

**But it also means my own acceptance criterion — *"No filesystem syscall on the serialized commit path"* — is now an over-claim, in exactly the class this slice has been correcting all week.** The honest statement, which I am folding into the plan and my domain notes:
> **The serialized commit loop performs no filesystem syscall *in the observe worker paths* (T1/T2/T4, proven by the injected per-stage blocking fixtures). The `git status` SUBPROCESS EXEC remains on the serialized path**, bounded by `context` (killable, so it is *not* the D-state class of the §4a Part-B residual), **and is the named `CARRY-C-1`.** The blanket claim is false; the scoped claim is true. **label == mechanism, including in my own acceptance criteria.**
**`CARRY-C-1` (m-3 ledger, m-7 CC'd):** *detach the `git status` exec behind the T1 worker seam. Natural home: the **item-10 / turn-baseline** work, which reopens this same absence-floor machinery — or its own bounded task. Until then it is a **visible ledger line, not folklore**.*

### R-1 — **LABELED, and the CONFINEMENT CLAIM is narrowed to match the mechanism**

You are right, and this is the D5-class handling the ratified directive prescribes: **label, do not engineer away.** Descriptor-rooted `O_NOFOLLOW` traversal proves the opened fd is **beneath the governed root *by path***; it does **not** close **hardlinks** — an in-root hardlink to an out-of-tree inode on the same filesystem **is** a regular file beneath the root and would be read. Closing it needs inode-provenance checks (`st_dev`/`st_ino` allowlisting) or an fs-level sandbox — **home-grown security primitives the directive cuts** — and it requires **prior write access to the governed tree**, which is outside the confusion-not-malice threat model.

**The label lives in the §4a mechanism notes + the §11 residual list**, beside the **D5 same-uid** residual and the **operator-topology (D-state)** residual. **And the important half is the claim correction, not the label:**
> **`R-1` — Hardlink residual (D5-class, STATED, never engineered away).** The confinement claim is **"the opened descriptor is beneath the governed root *by path*"** — it is **NOT** "the bytes originate beneath the governed root." An in-root hardlink to an out-of-tree same-filesystem inode is read. Requires prior write access to the governed tree (outside the threat model). Stated, not closed.

That narrowing is the whole point: my §4a said confinement is "proven by the opened descriptor," and that is true — **of the path, not the inode.** Saying so is the difference between a residual and a lie.

## What I am amending on my own surfaces (no build work, no re-review of your slice)

The T2 I-PH line (bound-not-floor + the landed reality) · the "no filesystem syscall on the serialized path" acceptance criterion (scoped to the observe worker paths; the git exec named) · `CARRY-AO-1` / `CARRY-C-1` / `R-1` into the m-3 ledger + §4a/§11. **All three are corrections to MY claims to match YOUR landed mechanism** — which is the fidelity review doing its job in the direction it less often runs.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-build-fidelity-m3/SITREP-planner-20260714-020000.md` — run below.
- Read at the bytes on `s9-evidence-thicken@d91fcfb`: `internal/observe/registry.go` (`CheckVerdict` struct; `:241` the count-derived verdict; the origin tagging at `:223-254`) · `internal/observe/verdict_binding.go` (the origin envelope, `validOriginClass`'s closed base-check arm, `baseRefusalTiming`, the `machinery` derivation) · `internal/observe/checks_base.go` (`evaluateAbsenceFloor` in full; the two `exec.CommandContext` sites) · `internal/observe/fs_worker.go` (`beginFS` breaker-before-launch, `openat`+`O_NOFOLLOW`, the ceilings, `OnSoftExpiry`) · `internal/observe/read_file_worker.go` (`readFileByteCeiling = 8 << 20`) · greps proving zero marker-inference and zero external-send. `go test ./internal/observe/... ./test/fixtures/...` = ok/ok.

ACTIONS_GIT_REF: none — a read-only owner-fidelity review + three guide bytes; no `frank/` edit, no commit, no token, no merge by me. My amendments land in cwd governance docs only.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0`; the `s9-evidence-thicken` worktree clean at `d91fcfb` per your report and my read.
Next requested action: s9.planner consumes this **CONFIRM** into the merge decision batched with m-7's landing confirm — **m-3 raises no contradiction and does not block the merge.** The build seat's editor is **released** (AO-1 = carry, no in-slice work). Merge remains operator-only (`HUMAN_MERGE_AUTHORIZATION` at grant time). m-3 holds report-only.
