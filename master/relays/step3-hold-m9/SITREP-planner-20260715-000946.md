## SITREP — m-9 bounded status handoff under STOP-WORK (`step3-hold-m9`); all bytes preserved, no further design

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-hold-m9
PARENT_DISPATCH_ID: step3-hold-m9
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the bounded status handoff the hold requests; nothing else
FROM: m-9.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.implementer
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: step3-hold-m9/RECONCILE-orchestrator-planner-20260715-013510
DESIGN_DOC_ID: step3-design-m-9-model-runtime
SUBJECT: work STOPPED on receipt; artifacts + verdict/lock state + reframe-affected findings + the dependencies the reframe must disposition, per the hold's item 3 — nothing re-designed, nothing deleted

## SITREP — m-9 pair / m-9-model-runtime
Phase: DESIGN lane HELD (stop-work acknowledged; no semantic edit, grill, review loop, consumer confirmation, or lock move since receipt)
Current artifact:
- `master/domains/m-9-model-runtime/audit/2026-07-14-model-runtime-promotion-matrix.md` — c7 AUDIT rev1, pair-CONFIRMED (…-215642), accepted at `step3-audit-reconcile`. Untouched.
- `master/domains/m-9-model-runtime/design/2026-07-15-model-runtime-design.md` — DESIGN **DRAFT r0** under the now-invalidated dispatch framing. **Bytes preserved exactly as of the hold**; provisional audit input to the reframe.
Claims:
- Verdict/lock state: NO GRILL run, NO GRILL_LOCK_ID, NO DESIGN-REVIEW requested, NO lock, NO PLAN. Brainstorm partially run with the operator (three directional answers, all recorded AS PROVISIONAL in draft §9 — the draft self-quarantined topology before this hold, on the operator's in-session "no solid decisions yet") — evidence E1 — source: draft §9 + header status line.
- The draft's reframe-AFFECTED content (flagged, deliberately NOT fixed under the hold): §2 binds canonical session state to m-7-substrate commits; §3 places tool-request authorization at "the m-5/m-7 door" + the m-7-hosted executor; §6.4 rides the m-7 expiry timer; §7 names final-wire authorization "m-7-hosted". All four bindings assume conductor transit the reframe removes — evidence E1 — source: draft §§2/3/6.4/7 vs hold `:20`.
- The draft's likely-SURVIVING content (my claim; for the refreshed consumer audit to verify, not assert): §1 turn state machine shape + §1.1 typed turn terminals + the no-fourth-delivery-state separation law (relay plane untouched by the reframe); the C7/C8/C9 invariants (complete-only tool calls, stable request identity + no-replay, observe-before-action floor); §4 two-point observation; §5 compaction-as-governed-event; §6.1-6.3 typed dispositions; §7.1 Q4 packet items 1-6 (content host-neutral; the enforcement-host NAME must re-point); §8 m-8 contract requirements — evidence E1 — source: the sections as written.
- Charter-byte consequence for the reframe packet: the invalidated sentence lives in the m-9 boot relay, the m-9 domain README ("runs ON m-7"), kickoff §2, and my audit rows that echo it (§1 table authority row, C2, E5 timer hosting) — the amendment should sweep those surfaces, not only the design dispatch — evidence E1 — source: `master/domains/m-9-model-runtime/README.md:13`, kickoff `:24`, audit rows named.
- Cross-lane note acknowledged: m-7 credential r3 + its paired reviews = provisional audit input only; the draft took no dependency on r3 specifics (§7 referenced the §1b amendment generically) — E1 — source: draft §7.
Dependencies the reframe must disposition (hold item 3 + two of mine):
1. **m-9 ↔ control-plane/supervisor boundary** (VP's provisional m-10): worker lifecycle, crash/restart contract, and WHERE the exactly-one-active-turn invariant (draft §1/A2) is enforced — m-9 must not become its own supervisor.
2. **Tool-execution authorization path vs the conductor's `{submit,project,read}` guardrail**: where the m-5 ceiling check physically runs app-side, who mints/verifies the authorization, and what governed record of authorization+disposition lands in the store when the traffic itself no longer transits the conductor.
3. **Q4 retry/idempotency seam vs the app-side connector's final-wire authorization**: draft §7.1's six requirements re-pointed at the connector actuator (m-8 per `step3-hold-m8:29`) once ratified; the digest/idempotency binding + typed zero-send denial content stands ready for m-3's amendment.
4. *(mine)* **The canonical locus + audit trail of turn records**: if model-turn traffic is app-side, WHICH turn artifacts are governed store records (evidence for gates/step-exits, relayed summaries) vs app-local state — this dispositions draft §2's derived-transcript/digest split.
5. *(mine)* **Observation vantage under the reframe**: m-3's observe-as-send was designed at the conductor door; the reframe must name where m-3's vantage sits over app-side model output (draft §4's floor + two-point proposal is re-usable if a vantage exists).
Actions:
- Stop-work applied on receipt; zero design-doc edits after the hold; this relay + one INDEX row are the only writes.
ACTIONS_GIT_REF: wrote only master/relays/step3-hold-m9/SITREP-planner-20260715-000946.md + its append-only INDEX.md row; no frank/ edit; no source/credential/provider-call/external-send/merge/live-store action at any point in this lane
FINAL_GIT_STATUS_SHORT:
unavailable — cwd is not a git repo (docs workspace); `frank/` untouched at `502e06c` (s11-close)
Done: hold acknowledged; bytes preserved; this bounded handoff.
Not done (frozen): grill, DESIGN-REVIEW, consumer packets, any §9 annex binding.
Blocked: on the operator-ratified architecture-amendment packet + refreshed consumer audit + replacement single-author design dispatch (per the hold).
Scope drift risk: none — the draft never bound topology (its §9 was already OPEN on the operator's pre-hold instruction).
Tests / verification: relay-lint on this file before handoff (E2).
Next requested action: none owed by m-9 until the reframe re-dispatches this lane; m-9.planner stands by.
