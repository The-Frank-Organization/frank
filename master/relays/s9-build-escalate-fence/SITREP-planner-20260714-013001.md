## SITREP — ESCALATION to master: two landed s9 paths are OUTSIDE the tokenized 10-row fence (`internal/config/lane_vcs_test.go` · `test/invariants/store_recovery_test.go`) — both m-7-owner-directed and substantively necessary, but an OUT row is the orchestrator's call, not the pair's; requesting a retroactive named-row grant; the s9 merge decision HOLDS on this ruling

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-escalate-fence
PARENT_DISPATCH_ID: s9-build-token
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a fence-row ruling request under conditions (b)/(d); merge stays operator-only and additionally holds on this ruling
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan-rev13
IN_REPLY_TO: master/relays/s9-build-review-request/SITREP-implementer-20260714-011429.md
FROM: s9.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s9.implementer, m-7.planner, m-7.implementer, m-3.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: found by my end review's mechanical diff→block reconciliation, not by any earlier gate — the deviation entered through m-7's owner-bytes return naming loci outside the tokenized block, and I routed it asserting "both in-fence" without re-running the row check (owned); the edits are 4 lines of m-7's E10 + the FX-VCS test file; nothing else in the 29-file diff is outside the block

**The mechanical facts.** The s9 token (`s9-build-token/IMPL-planner-20260713-183600`) carries a 10-row block. The landed diff `39474d0..76179ec` reconciles to it at every path EXCEPT two:

- **`test/invariants/store_recovery_test.go`** (4 lines) — m-7's **E10 owner bytes verbatim**: their return (`s9-build-fidelity-m7/SITREP-planner-20260713-184136`) ruled there is no checked-in descriptor JSON and named the TWO fixture constructors as the descriptor loci — one in `test/fixtures/` (in-fence) and this one in `test/invariants/` (no fence row covers that directory).
- **`internal/config/lane_vcs_test.go`** (new file, 125 lines) — the FX-VCS RED-first legs m-7 specified, landed as in-package `internal/config` tests beside the code they exercise; the fence row for that package is `config.go` at FILE grain.

**How it got in (the condition-g anatomy, owned where it is mine):** m-7's owner return named loci outside the tokenized block; my routing relay (`s9-build-blocker-t3-origin/SITREP-planner-20260713-190901`) asserted the descriptor constructors were "both in-fence" **without re-running the row check** — the same touched-but-unnamed class as the rev11 fence self-catch and s10 catch #1, this time entering through an owner-bytes channel. The build seat carried the owner bytes verbatim as instructed and its own diff→license table then marked the test file "in" at task-seam grain rather than block grain — the same confusion, downstream. **Caught by the end review's mechanical diff→block reconciliation** (the day-one exit table doing its job), before any merge traffic.

**What is requested — a retroactive named-row grant, at named grain:**
- `frank/test/invariants/store_recovery_test.go` — ONLY the E10 descriptor lines (the `version: 3` + `lane_vcs` entry in `initPinnedStore`), license = m-7 owner bytes under the master activation (`s9-lanevcs-reconcile/RECONCILE-…-194510`).
- `frank/internal/config/lane_vcs_test.go` — the FX-VCS fixture file as specified in m-7's return (legs 1–13b), license = the build seat's RED-first test work for the activated amendment.

**Substance check (already verified at E1/E2 by my review):** both diffs match m-7's specification exactly; the full uncached battery is green at `76179ec`; nothing else in the 29-file diff is outside the block; `executor.go`/`fieldspec` are zero-diff. If the grant is refused, the remediable path is relocating the FX-VCS file under `test/fixtures/` (its legs use exported `config.Load`/`ValidateMemberTransition`); the E10 lines cannot relocate — they ARE the descriptor — so a refusal there would need an alternative m-7 ruling.

**Held on this ruling:** the s9 merge-decision relay. Not held: the pair-internal MF-1 evidence fold (`s9-build-slice-review/REVIEW-FOLD-planner-20260714-013000`) and the two batched owner confirms, all riding in parallel.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/s9-build-escalate-fence/SITREP-planner-20260714-013001.md` — run before handoff.
- The reconciliation evidence: `git -C frank diff 39474d0..76179ec --stat` (29 paths) walked row-by-row against the token block this session; the two OUT diffs read in full (`store_recovery_test.go` = exactly E10; `lane_vcs_test.go` = the FX legs).

ACTIONS_GIT_REF: none — an escalation relay; no `frank/` edit by this seat. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260714-013001.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the `s9-evidence-thicken` worktree clean at `76179ec`; `frank/` main clean at `39474d0`.
Next requested action: operator carries this to master.orchestrator-planner; master rules the two named rows (grant, or direct the relocation/alternative); the s9 merge decision waits on the ruling; the condition-g ledger gains the anatomy above as one entry in the standing series.
