## PLAN — s9 BUILD STAFFING RULED: OPTION A — a FRESH `s9.planner`/`s9.implementer` slice pair consumes the approved rev11 plan-of-record; m-3 reverts to its dispatched role (GUIDE + owner-fidelity); the suspended token `IMPL-planner-20260713-214500` is VOID; the two boot relays are cut beside this (master's function per the s10 precedent) — you named the wrong real seat, caught it before harm, and routed it perfectly; the plan-of-record itself is untouched-and-sound

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-build-impl
PARENT_DISPATCH_ID: s9-plan-m3-review-r12
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the operator BOOTS the fresh `s9.planner`/`s9.implementer` sessions on the two boot relays cut beside this; the slice merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: master/relays/s9-build-impl/SITREP-planner-20260713-220000.md
FROM: master.orchestrator-planner
TO: m-3.planner
CC: operator, master.orchestrator-reviewer, m-3.implementer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: RULED (A), your recommendation and the only defensible reading — the fresh slice pair restores the three-role independence (adversarial reviewer / builder / owner-fidelity) that produced every save in this slice; (B) is rejected on the record as the s8 hold inverted; the recovery you ran (suspend + refuse-to-self-correct + route with precedents) is the model

**RULING: OPTION A.** A fresh `s9.planner` / `s9.implementer` slice pair (the s10 pattern) consumes your **approved rev11 plan-of-record** (`master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md`; PLAN-REVIEW `approve` r12; SCOPE_DIFF all-in; every master ruling consumed — B-opaque ratification, the `lane_vcs` branch-only reconciliation + activation, the scope_paths co-sign with the §F leg still pending m-1). **m-3 reverts to its dispatched role: GUIDE + owner-fidelity** (as m-6 guided s10 and as m-3 itself filed owner-fidelity *for* s10, catching two real things). The delegated `DISPATCH IMPL` token issues **inside the fresh pair's own loop** (`s9.planner → s9.implementer`), never from the guide to its own reviewer.

**(B) REJECTED, on the record as you asked:** the m-3-pair-builds reading collapses the adversarial-reviewer, builder, and owner-fidelity roles into one seat — precisely what the s8 same-owner token was held to prevent, and precisely the independence that caught the false-accept, the §13 regression, the executor self-selected disposition, the claimless-SITREP rejection, and the unfenced seams across r1–r12. It is not traded away by a drafting slip.

**TOKEN `s9-build-impl/IMPL-planner-20260713-214500` — VOID.** Confirmed void on arrival (you already suspended it; I ratify): it was a same-owner delegated token to your own adversarial reviewer, never hand-relayed, no `frank/` byte moved. It does not execute.

**A ledger note worth its line (condition g, and a real dogfood datum):** that token would have **linted CLEAN** — it was a mechanically valid same-owner delegated token with correct lineage. No lint/trail check catches a *semantically mis-seated* build assignment; the shape is valid, only the staffing is wrong. It was caught by the **operator's read** ("that's the wrong seat"), not by any gate. The trail validates lineage and authority *shape*, not staffing *semantics* — a standing limit, and exactly the class of human judgment the conductor does not (yet) mechanize. Recorded for the s9 dogfood evaluation and the eventual "what the gates don't catch" honesty line.

**The split for execution (master boots, guide hands off — the s10 division):**
- **Master (done in this pass):** the two boot relays `boot/master-boot-s9-planner/…-220011` + `boot/master-boot-s9-implementer/…-220012` — constituting the fresh seats with their conditions, fence, and disciplines (booting a fresh team is the orchestrator's function, per m-6's own s10 note).
- **You (m-3.planner):** amend your PLAN's **team-shape section** to name `s9.planner`/`s9.implementer` + m-3-as-guide (a drafting correction to align with this ruling — no re-review; the mechanics r12 approved are untouched), and stand ready as GUIDE + owner-fidelity. The boots point the fresh seats at your rev11 plan-of-record.

**Standing carried:** B11 straight-through build cadence · B12 declined (design churn licensed) · the s8/s10 institutionalized disciplines as day-one exit items · conditions (a)–(g) · the open s9 gates that do NOT block the build's non-scope_paths/non-B-opaque tasks (scope_paths §F pending m-1; B-opaque now buildable on the ruled bytes) · merge operator-only.

ACTIONS_GIT_REF: none — a staffing ruling (disk refs: this relay + the two boot relays cited + INDEX rows timestamped 20260713-220010/220011/220012).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `39474d0` (`s10-close`) — no build has touched it.
Next requested action: operator boots `s9.planner` + `s9.implementer` on the two boot relays; m-3 amends its PLAN team-shape section + stands to as guide/fidelity; the fresh pair runs its own PLAN-adopt → plan-review → token loop consuming rev11, then builds straight-through under B11. `s9-build-impl`'s suspended token stays void.
