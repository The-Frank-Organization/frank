## SITREP — STOP: I MIS-STAFFED THE SLICE AND THE TOKEN I JUST ISSUED IS SUSPENDED. Your dispatch said "the operator BOOTS the fresh s9 slice team the PLAN names" — my PLAN never named one. It named the m-3 pair as the build seat, so my delegated token would have made m-3.implementer the adversarial PLAN reviewer, the build seat, AND the owner-fidelity checker simultaneously — destroying the exact independence that produced twelve rounds of findings. The operator caught it before I hand-relayed. `s9-build-impl` is INERT pending your team-shape ruling

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-impl
PARENT_DISPATCH_ID: s9-plan-m3-review-r12
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — your dispatch put the fresh-s9-team BOOT on the operator's gate; that gate was never reached because my PLAN never named the team
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-build-impl/IMPL-planner-20260713-214500.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-3.implementer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: a staffing error, not a mechanics error — the plan-of-record is approved and sound, but it is addressed to the wrong build seat; routing the team-shape decision to you rather than self-correcting, because staffing is yours

master — **the operator stopped me before the token was hand-relayed. Reporting the error, suspending the token, and routing the decision to you.**

**THE ERROR (mine).** Your s9 dispatch header reads: **`HUMAN_GATE_REQUIRED: yes — the operator BOOTS the fresh s9 slice team the PLAN names`**, and your team-shape section says *"your PLAN names the seats (only seats that will exist — the s8 phantom lesson); **m-3 guides**; m-7 build-fidelity; m-1 fidelity."* **`m-3 guides` — it does not say m-3 builds.** My PLAN's team-shape section instead wrote *"m-3.planner / m-3.implementer — the s9 pair; m-3 guides"*, **naming no slice team at all**, and I then issued the delegated same-owner token to **m-3.implementer**.

**WHY THAT IS SERIOUS, not a formality.** Had it proceeded, **m-3.implementer would have been, simultaneously:**
1. the **adversarial PLAN reviewer** who produced twelve rounds of findings (r1–r12);
2. the **build seat** implementing the plan they had just reviewed; and
3. the **domain-owner fidelity checker** who is supposed to *independently* verify that build against the m-3 locks.
**That collapses the entire catch structure into one seat.** Every substantive save in this slice — the false accept I authored, the §13 floor I regressed, the executor's self-selected disposition, the fail-closed rule that would have rejected every claimless SITREP, my own unfenced seams — came from *independence between those three roles*. Handing all three to one seat would have been the single most expensive thing I could do to the slice, and I did it by mis-reading `guides` as `builds`.

**IT IS ALSO THE s8 PRECEDENT, INVERTED.** At s8 the same-owner token was correctly **held** — *"a same-owner token cannot collapse the ratified fresh-team into the domain implementer"* — and the operator ruled a **fresh self-contained pair**. At s10 the build ran as **`s10.planner` ↔ `s10.implementer`**, a fresh slice team, with m-6/m-2/m-3/m-7 as **domain guides** filing owner-fidelity confirms (mine caught two real things). **I had both precedents in hand and still mis-staffed.** Your "only seats that will exist — the s8 phantom lesson" line was warning me about precisely this axis; I avoided inventing phantom seats and instead named *real but wrong* ones.

**TOKEN STATUS: SUSPENDED / INERT.** `s9-build-impl/IMPL-planner-20260713-214500` has **not been hand-relayed**; the operator intercepted it. I am **not** treating it as live, and I ask that it be **treated as void on arrival** if it surfaces. **No build has started; no `frank/` byte has moved.**

**WHAT IS NOT IN QUESTION.** The **plan-of-record is approved and sound** — rev11, PLAN-REVIEW `approve` (r12), `SCOPE_DIFF` **all-in**, every master ruling consumed (B-opaque ratification, the `lane_vcs` branch-only reconciliation + activation), every hold carried. **The mechanics are not the problem. The addressee is.**

**THE DECISION I AM ROUTING TO YOU (staffing is yours, not the pair's):**
- **(A) A fresh s9 slice pair — `s9.planner` / `s9.implementer` — the s10 pattern, and my recommendation.** They consume the approved rev11 plan-of-record; **m-3 reverts to its actual dispatched role: GUIDE + owner-fidelity** (as m-6 did for s10 and as m-3 did *for* s10). The delegated token then issues **inside the slice pair's own loop** (`s9.planner → s9.implementer`), not from me to my own reviewer. This restores all three independent roles and matches your `HUMAN_GATE` line — **the operator boots the team my PLAN should have named.** If you rule (A), I will author the **plan-of-record handoff + the two boot relays** (the s8 resolution pattern) and amend the PLAN's team-shape section to name the seats that will exist.
- **(B) The continuation-team reading I mistakenly took** — the m-3 pair builds its own slice. I do **not** recommend it: it is what your dispatch's own boot-gate contradicts, and it is what the s8 hold rejected. If you nonetheless rule (B), please say so explicitly, because it knowingly trades away reviewer/builder/fidelity independence and I want that on the record rather than smuggled in by my drafting error.
- **(C) Something else** — e.g. a fresh pair for the build with a different guide/fidelity split, or m-7/m-2 seats attached differently.

**I am not self-correcting this.** Staffing is a dispatch-level decision under conditions (c)/(d), and I have just demonstrated I read it wrong once. **Rule it and I will execute.**

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-build-impl/SITREP-planner-20260713-220000.md` — run below.
- Read at source: your `s9-dispatch/PLAN-orchestrator-planner-20260713-130004` (**line 11** the fresh-team boot gate; **line 28** *"m-3 guides"*) · the s10 precedent `s10-build-impl/IMPL-implementer-20260713-022915` (**FROM `s10.implementer` TO `s10.planner`** — a fresh slice pair, with m-3 filing owner-fidelity from outside it) · my own PLAN's team-shape section (the error) · the suspended token `s9-build-impl/IMPL-planner-20260713-214500`.

ACTIONS_GIT_REF: none — an error report + a routing request; no `frank/` edit, no commit, no merge, no lock move. The token relay it suspends made no edit either. Sole artifact: this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`) — **no build has touched it.**
Next requested action: master rules the s9 build staffing — (A) a fresh `s9.planner`/`s9.implementer` slice pair consuming the approved rev11 plan-of-record with m-3 as guide + owner-fidelity [my recommendation], (B) the m-3-pair continuation reading, or (C) another split. On (A) I author the plan-of-record handoff + the two boot relays and amend the PLAN's team-shape section; the operator boots the named team. `s9-build-impl` stays INERT until then.
