## IMPL — the delegated s9 build token. SCOPE_DIFF re-run against the rev11 corrected fence: `all-in`, no hard trigger. Build the s9 evidence-thicken slice straight-through (B11) per the approved plan: T1 shared detachable FS worker → T2 find-references → T3 the §6.1 conductor verdict pass → T4 the master-activated `lane_vcs:none` opaque branch → T5/T6 → T7/T8 (HELD on the m-2/m-1 co-signs) → T9 exit fixtures. m-7's owner bytes are REQUESTED and carried VERBATIM (s10-T1), never authored by m-3. No merge authority.

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s9-build-impl
PARENT_DISPATCH_ID: s9-plan-m3-review-r12
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the slice MERGE is operator-only (`HUMAN_MERGE_AUTHORIZATION` at grant time); this token authorizes IMPLEMENTATION only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-213000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: your r12 approval permitted a fresh SCOPE_DIFF and, on `all-in` with no hard trigger, a separately valid delegated dispatch parenting through it — the rerun is all-in, so here is the token, with every hold and owner boundary carried explicitly

m-3.implementer — your r12 boundary: *"The planner may issue delegated implementation authority only in a later relay if the rerun is `all-in`, no hard trigger is present, the relay contains the required live token, and its lineage parents through this approving review."* All four conditions are met. This is that relay.

## SCOPE_DIFF (re-run against the rev11-corrected fence, at `frank@39474d0`)

SCOPE_DIFF:
- internal/observe/fs_worker.go -> in
- internal/observe/read_file_worker.go -> in
- internal/observe/registry.go -> in
- internal/observe/gate.go -> in
- internal/observe/checks_base.go -> in
- internal/observe/fs_worker_test.go -> in
- internal/observe/registry_test.go -> in
- internal/observe/gate_test.go -> in
- internal/observe/checks_base_test.go -> in
- internal/config/config.go -> in
- cmd/frank/main.go -> in
- internal/fieldspec/registry.json -> in
- internal/fieldspec/registry_test.go -> in
- internal/fieldspec/validate.go -> in
- internal/fieldspec/validate_test.go -> in
- test/fixtures -> in

SCOPE_DIFF_RESULT: all-in

**Row annotations (0 OUT rows, no hard trigger; the rev11 fence correction closed the two seams the first run surfaced):**
- **m-3 domain, m-3-authored:** `internal/observe/*` — `fs_worker.go` is NEW (T1 creates, T2 extends); `read_file_worker.go`'s §4a semantics stay **byte-unchanged**; `registry.go` carries T2's entry, T3's verdict pass and T4's `RegistryEnv.LaneVCS`; `gate.go` the T3 `baseStamps` seam; `checks_base.go` T2's scan op + T4's absence floor.
- **m-7-OWNED (master-activated `lane_vcs`):** `internal/config/config.go` (schema/transition/reader-ceiling/`composeSupply` + the cloned `Supply.LaneVCS`) and `cmd/frank/main.go` (the runtime handoff) — **requested from m-7 and carried VERBATIM under the s10-T1 pattern; m-3 authors NEITHER**, and m-7 confirms end-of-slice.
- **m-2-owned, condition-(f) HELD:** `internal/fieldspec/registry.json`/`registry_test.go` (T7) and `validate.go`/`validate_test.go` (T8) — **do not build before the m-2 / m-1 co-signs land.**
- **`test/fixtures`:** T9's ⑤ ODB egress pair lands here; the s8/s10 regression baselines (`s8_decision2_test.go`, `s8_adversarial_test.go`, `iph_test.go`) **must stay GREEN — no unrelated fixture churn.**

## Build order (B11 straight-through; RED-first every task)

**T1** the shared detachable descriptor-rooted FS worker + per-lane breaker (**the serialized commit loop performs NO filesystem syscall**; `read-file`'s §4a semantics byte-unchanged, its full regression suite green) → **T2** `find-references` on it (declared textual domain · deterministic binary rule · the numeric ceilings · exact token-boundary relation · **fail-closed incomplete-scan table** · lexical-not-semantic label) → **T3** the **locked §6.1 conductor verdict pass** (identity bound from `Selection` · the total `(origin × tuple)` table · **`failing_detail` discarded-and-re-derived**, `MachineryFault` from the **conductor's** origin — never the executor's prefix · timing: conductor origins **produce**, executor origin **canonicalized**, never selects disposition · `output-truncated` preserved as a **pass-warning** · `signal_class` derived from the **conductor-selected** entry id) → **T4** the **master-activated `lane_vcs: none` opaque-accept branch** (Step **3a** request m-7's owner bytes → carry **VERBATIM**; Step **3b** m-3 authors only the consumer + terminal semantics; **nil/`git` can NEVER opaque-accept**; **pre-v3 nil still runs the full §13 observation**; the **restored Option-2 E0 floor**; the serialized FS probe **removed**; Step **6** m-7's end-of-slice confirm) → **T5** the attestation negative → **T6** the `mixed` **verification-only** pass (no commit) → **T7/T8 HELD** → **T9** the runnable exit set + the ⑤ ODB egress pair.

## Standing holds — carried into the build, none of them liftable by this token

- **T7 / T8 are condition-(f) HELD** on the m-2 / m-1 co-signs. **Report-and-hold; do not build them early.**
- **m-7's owner bytes are REQUESTED and carried VERBATIM (the s10-T1 pattern). m-3 authors NONE of them** — not `config.go`, not `main.go`, not the dogfood descriptor, not the FX-VCS negatives. m-7 confirms the landing end-of-slice.
- **The blocked ledger stays blocked: B1** (scope_paths — the co-sign's m-1 §F leg is still open) · **B-diff** (m-7 two-run capability) · **B3** (item-10) · **B2** (item-9) · **B4** (operator-attestation positive). **No `t.Skip` stubs in `frank/`** — blocked items live in the governance ledger.
- **Locks are read-only:** the `{accepted, rejected, held}` terminal enum · the `CheckVerdict` byte shape · c2 §5 · s8 §13. **A locked-contract change stops and routes to the owning pair + master.**
- **No external send** (egress fence). **No side-effecting execution** (s10 T10 shelf).
- **MERGE IS OPERATOR-ONLY** (`HUMAN_MERGE_AUTHORIZATION` at grant time). This token grants implementation, nothing else.
- **Any owner flag, locked-contract pressure, or scope deviation STOPS and returns here** (condition (f)).

DISPATCH IMPL

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-build-impl/IMPL-planner-20260713-214500.md` — run below.
- `SCOPE_DIFF` re-run against the real tree at `39474d0`: every fenced path verified present (or, for `fs_worker.go`, verified absent and created by T1); `test/fixtures/` holds 47 `_test.go` files including the three regression baselines this build must keep green.
- Lineage: this dispatch parents through the approving PLAN-REVIEW `s9-plan-m3-review-r12` (`…-213000`), whose parent is my pair-Planner PLAN addressed to you.

ACTIONS_GIT_REF: none — this relay is a dispatch token only; no `frank/` edit, no commit, no branch, no merge by me. The build's commits land on the `s9-*` branch off `s10-close@39474d0` under this token, authored by the build seat.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer builds the slice straight-through (B11) per the approved rev11 plan, reporting at the end with ONE review; T7/T8 hold on the co-signs; m-7's bytes are requested and carried verbatim; the blocked ledger stays blocked. Merge remains operator-only.
