## SITREP — s8.planner T1+T2 review VERDICT: APPROVE both (T1 retroactive per master's r2 phantom-seat ruling); T3 proceeds; the catalog-marker question is ruled REAL-but-routed — m-7-owned s8-v1 bytes requested in parallel, in hand before T9/T10; one non-blocking forward note on the genesis predecessor derivation

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-t2-review-verdict
PARENT_DISPATCH_ID: s8-build-t2-review
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — task-review verdict under the standing `s8-build-impl` token; no new authority granted; merge remains operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T1, T2
IN_REPLY_TO: frank .relays/s8/s8-build-t2-review/SITREP-implementer-20260711-211500.md (worktree s8-observe-spine)
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: T1 (d87dc21 + d94dfd4) and T2 (82ebe84) APPROVED on my own verification, not your report; continue to T3 under the standing token; fire the m-7 catalog-bytes request on `s8-build-fidelity-m2`-pattern now so the s8-v1 member is in hand before T9 genesis; the phantom `s8.reviewer` is dropped from routing per master's r2 ruling — per-task reviews land on this seat

REVIEW_VERDICT: approve — T1 and T2; implementation continues at T3. No merge authority rides this relay.

### What I verified at my own seat (every claim below is my E1/E2, not your report relayed)

1. **Authority trail is sound.** The r2/r3/r4 fence grants are real master rulings (`s8-build-escalate-fence/PLAN-orchestrator-planner-20260711-202010`, `…-203510`, `…-205010`) — each stop escalated BEFORE edit, each grant named-seam, the r4 stale-oracle class lane conditioned on pre-commit enumeration + planner review (this verdict is that review). Master's r2 ruling also disposed the phantom `s8.reviewer` and landed T1's review on this seat — your retroactive routing is correct.
2. **Scope held.** `git diff --name-only main...82ebe84`: every changed file is inside the 29-entry fence (including the `test/invariants/` directory row) or is one of the four granted stale-oracle files, each edited at its granted seam. Your enumeration is complete — no unenumerated file, no production file through the class lane.
3. **T2 bytes are owner-authored and verbatim.** m-2's byte relay (`s8-build-fidelity-m2/COORD-planner-20260711-201500`) authored the four sites against the verified v5 base; I checked sites 2/3/4 in the applied `registry.json` (enum + seat_scope carry `catalog`+`adoption`; `surface_intent` statics removed with the exact Option-B annotation) and the file hashes to the successor SHA `ff7feb0cd42da4f4a0079af3be8f026f66f60ba01be1c7a29737ef6bbaa0e556` you and master's r3 ruling both record.
4. **The genesis-v5 tripwire is real fail-closed code, not a narrative.** `genesis.go:25` pins the full-hex v5 SHA; `genesisMemberBytes` derives the predecessor by reversing exactly the four m-2 sites with a count-must-be-1 check per replacement and refuses on any mismatch OR on a predecessor hash ≠ `1ef6abab…`. FX-CFG-7's grain is intact; v6 arrives only through the live accepted transition. No pinned value moved.
5. **m-7's T1 correction folded to the required observable shape.** `d94dfd4`: serve fails closed on catalog-absence with the typed actionable fault (`ErrStoreNotAdopted`, "store-not-adopted: run frank -bless"), the legacy two-member enumeration is the EXPLICIT `LegacyStoreRootConfigPaths` constructor reserved for bless/T10 fixtures, and the fixture leg asserts the fault text (`s8_config_activation_test.go:150`). The stat-conditional map survives only behind the serve guard — the option m-7's correction explicitly allowed ("the observable contract is what binds"). On my read the no-second-round condition is satisfied; m-7.planner is CC'd here and owns the objection if my read is wrong.
6. **The battery evidence is sequence-honest and FILE-captured.** Both transcripts exist in `.relays/s8/s8-build-t2-battery/`; my `shasum -a 256` reproduces your RED (`3e1c228e…`) and GREEN (`9109356b…`) hashes exactly. The RED transcript shows the rejected shared-fixture auto-transition breaking mint eligibility and the applicability hit-trace — the honest capture of a workaround you then removed. The GREEN transcript: zero FAILs, every package ok including `test/invariants` (the ten laws) and the replay/dogfood/zeroloss suites.

### The catalog-marker question — ruled: REAL, not closed by the green battery, routed, not a T3 blocker

You are right on all three parts: `catalog.v1.json` still reads `"version": "s7-v1"` / `"status": "convention-only"`; r13 §2.5 pins the s8 initial marker `"s8-v1"` ("the s8 PLAN re-stamps the source artifact's s7-v1 as part of the §7 pinning") and §5 genesis item 3 composes "the blessed catalog member, discovery descriptor included" (§3's pinned normative bytes); and T1/T2 correctly refused to invent those owner bytes.

Ruling:
- **This is m-7-owned content** (r13 §2.5 re-stamp + §3's fully-pinned descriptor + §4's byte-copy path), to be delivered exactly as m-2 delivered T2: a reviewed byte relay, applied verbatim. **Action: fire the request to m-7.planner on the `s8-build-fidelity-m7` chain NOW, in parallel with T3.** If m-7 rules any part lands differently (e.g. descriptor staging vs T10), the owner's ruling governs and this verdict carries it.
- **Deadline grain: the blessed s8-v1 member bytes must be in hand BEFORE T9** — the fresh-genesis dogfood store must compose the lock's true catalog member, and T10's FX-CFG-12/13 validate candidates against `schema(catalog,"s8-v1")`. T9/T10 do not start on the s7-v1 placeholder.
- **T3/T4 proceed now** — the observe-gate surface is disjoint from the catalog member; holding the spine on a parallel byte handoff buys nothing.
- Your instinct not to treat the green battery as closure of the unimplemented capability-marker surface is correct and is now on the trail.

### Non-blocking forward note (trail record, no action this task)

`genesisMemberBytes` hardcodes the v6→v5 four-site reversal. It is fail-closed against drift (count≠1 or hash mismatch refuses genesis), which is right for s8 — but any future fieldspec successor (v7) makes the shipped source underivable to v5 by this function. When the next changeset is ever scheduled, this function is a named revisit; recording it now so it is a decision then, not a surprise.

### Seat-list correction (master r2 ruling, recorded at this plan touch)
The phantom `s8.reviewer` is dropped from all s8 routing/CC lists. The per-task adversarial gate = this seat (s8.planner) + review panels/subagents at the slice-exit review. The execution map of `s8-build-plan/PLAN-planner-20260711-193000` §3 is amended to that effect by this relay — no other PLAN content changes; the fence, conditions, and acceptance stand.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t2-review/SITREP-planner-20260711-213000.md` — run before handoff (result in the inline pointer).
- Run/read at my seat this session: `git log/diff main...82ebe84` (worktree `s8-observe-spine`); `registry.json` sites + `shasum` (ff7feb0c…); `genesis.go:20-118` (pin, derivation, refusal, `StoreRootConfigPaths`/`LegacyStoreRootConfigPaths`); `d94dfd4` stat + the fault-text fixture (`s8_config_activation_test.go:150`); both battery transcripts (SHA-reproduced; RED failure classes + GREEN zero-FAIL all-packages read); master rulings `…-202010/-203510/-205010`; m-2 `COORD-planner-20260711-201500`; m-7 `SITREP-planner-20260711-182245`; `catalog.v1.json:2-3`.

ACTIONS_GIT_REF: none — a review-verdict relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260711-213000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `691d034`; worktree `s8-observe-spine@bfa9202` carries only the untracked/modified `.relays/s8/` bookkeeping.
Next requested action: operator carries this to s8.implementer; T3 begins under the standing `s8-build-impl` token; the m-7 catalog-bytes request goes out on `s8-build-fidelity-m7` in parallel; next checkpoint to this seat at the T3–T6 spine review (or earlier on any blocker/fence stop).
