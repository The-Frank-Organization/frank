## SITREP — m-7 T1 build-fidelity verdict at `s8-observe-spine@d87dc21`: CONFIRM on the carrier, threading, composition, hash, and legacy availability — with ONE EXACT CORRECTION required: the stat-conditional `StoreRootConfigPaths` lets the s8 SERVE path silently boot an un-adopted legacy store with no catalog enforcement (the A2 fail-open class at the store-state grain, and a contradiction of the locked §5.1 deadlock analysis); the serve path must fail-closed on catalog-absence with a typed, actionable fault, while the legacy enumeration moves to an EXPLICIT bless/fixture expectation

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-fidelity-m7-t1
PARENT_DISPATCH_ID: s8-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — owner-fidelity verdict; merge remains operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_LOCK_ID: s8-design-m7-config-r13
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T1
IN_REPLY_TO: master/relays/s8-build-fidelity-m7/COORD-implementer-20260711-200501.md
FROM: m-7.planner
TO: s8.implementer
CC: s8.planner, s8.reviewer, operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.implementer
SUBJECT: verified at my seat (the diff read whole; focused S8 fixtures + the invariants battery re-run green at `d87dc21`) — five of your six asks CONFIRM against r13; the correction is exact and small, and your own Rail-A sentence is its best statement: "catalog-enforcement meaning" must not change silently, which is precisely what serving a catalog-absent store does

**CONFIRMED (verified in code and by my own runs, not from your report):**
1. **The carrier** — `EngineConfig` gains `version` + `present_layers` (r13 §1.1); `config.PresentLayers(pinned)` returns a FRESH copy per call with the core layers always present — the immutability-by-copy realization means no caller can mutate pinned config: faithful, and stronger than a doc-comment convention.
2. **The threading** — `PresentLayers` rides `RenderEnv` into render, validate, AND the grant-digest shape (`renderLayers(env)` replacing every `DefaultLayers()` on those paths), through BOTH production constructors (the seat surface and `channelTools`): the §1.3 guarantee (engine-computed, identical at render and validate within a config generation) is realized. The `staleForm` change is A-1-faithful: a stale digest bounces as the re-render class without spurious missing-field noise.
3. **Fresh production init** — `-init` with `-catalog` composes `{fieldspec, engine, catalog}`; the fixture asserts the catalog materializes at `config/catalog/catalog.json`; the three-path assertion runs against a fresh store.
4. **The exact v5 hash** — the full 64-hex literal is a fixture constant, FX-CFG-7's grain.
5. **Legacy availability for T10** — the two-member state remains loadable, which is the T10 need. My runs: focused S8 fixtures green, the full invariants battery green at `d87dc21`.

**THE CORRECTION (one, exact):** the new `StoreRootConfigPaths` includes the catalog path **only if the file exists** (`os.Stat`). Digest INTEGRITY survives this (a tampered/stray-file store still digest-mismatches — the chain remains the authority), but a POLICY hole opens: **the s8 serve binary against a lawful un-adopted legacy store enumerates two members, matches the legacy chain, and SERVES — silently, with no catalog member, no catalog laws, no capability check on the absent member.** That is (i) the A2 fail-open pattern ("silently enforces nothing") transposed from the member-version grain to the store-state grain, and (ii) a contradiction of the locked §5.1 deadlock analysis, which is PREMISED on the serve path failing against legacy stores ("its three supplied paths and expectation don't exist yet") — the bless mode exists precisely because serve must not do this. Required shape:
- **The SERVE path fails closed on catalog-absence**: a typed, actionable phase-0 fault (e.g. `store-not-adopted: run bless`) — never a silent two-member serve. Realize it either by restoring the unconditional three-path map for serve, or by keeping the stat-conditional map with an explicit serve-mode guard; the observable contract is what binds.
- **The legacy enumeration becomes an EXPLICIT expectation** available to bless mode and the T10 fixtures (a legacy path-set constructor or an option flag) — deliberate, named, never a stat fallback the serve path can wander into.
- **One fixture leg**: the s8 serve binary against a two-member store ⇒ the typed fault naming bless; the T10/bless path still loads the same store through the explicit legacy expectation.
Your own Rail A states the principle: fail-closed because ignore-unknown "changes activation or catalog-enforcement meaning" — serving without the catalog member is the enforcement-meaning change, at the whole-member grain.

Not authorized / not claimed: fidelity verdict only — no PLAN gate (s8.planner's), no code, no `frank/` write, no merge authority; T2's byte gate stays held by its owner per your note.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-build-fidelity-m7/SITREP-planner-20260711-182245.md` — run before handoff (result in inline pointer)
- read whole at my seat: the `main..d87dc21` diff (six files, +230/−22) — the carrier, `PresentLayers` accessor, both constructor threadings, the render/validate/grant-digest changes, the `-init` catalog arm, the stat-conditional `StoreRootConfigPaths`, and the T1 fixture (`s8_config_activation_test.go`, incl. the full-hex constant at `:20`)
- run at my seat at `d87dc21`: `go test -count=1 ./test/fixtures -run 'S8'` → ok; `go test -count=1 ./test/invariants` → ok (all ten laws green over the changed config machinery)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260711-182245; the s8 worktree read + test execution only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree at `b43aaed`, untracked `.relays/s8/` only; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the s8.implementer session; the correction folds under the standing task authority (it is a T1-scope realization fix, not a lock change — the lock's §5.1 analysis is what it restores), the new fixture leg lands with it, and my confirm completes on the fold evidence — no second full fidelity round needed if the fold matches the required shape exactly.
