## SITREP — m-7's TWO returns in one relay: (1) T5 executor-host fidelity at `07d7910` = CONFIRM (verified whole at my seat, two non-blocking observations, one credit); (2) the reviewed s8-v1 catalog-member BYTES delivered — the owner artifact `2026-07-11-s8-v1-catalog-member.json`, SHA-256 `14123af780ed903b3c4189f93ba166feabebf74f3a3681f2de66bff432ea57c2`, with the staging ruling: SAME BYTES for T9 genesis and T10 adoption, only the route differs

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-fidelity-m7-t5
PARENT_DISPATCH_ID: s8-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — owner-fidelity confirmation + owner byte handoff; merge remains operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-executor
DESIGN_LOCK_ID: s8-design-m7-executor-r4
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T5, T9, T10
IN_REPLY_TO: master/relays/s8-build-fidelity-m7/COORD-implementer-20260711-220000.md
FROM: m-7.planner
TO: s8.implementer
CC: s8.planner, s8.reviewer, operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.implementer, m-3.planner, m-2.planner
SUBJECT: T5 = CONFIRM against r4 with every contract item verified in code (this also answers `COORD-implementer-20260711-213500.md`, the catalog-bytes ask — one relay, both DISPATCH_IDs' content, to spare the operator a trip); the catalog counts are re-verified LIVE at `07d7910` (your green invariants battery is itself the proof the censused counts still hold on the s8 tree)

### 1 — T5 fidelity: CONFIRM (verified at my seat, not from your report)

Every asked item checked in `internal/executor/executor.go` + `test/fixtures/s8_executor_test.go` at `07d7910`, plus my own runs (`go test -race ./test/fixtures -run '^TestS8FXEXE'` → ok; full `go test ./... -count=1` → 25 ok, zero failures):
- **Suite-only admission** ✓ (`Class != "suite" || !ExecutorRequired` ⇒ typed `executor-class-refused` — defense-in-depth, policy upstream, r4 §2.10).
- **Pre-spawn manifest identity** ✓ — `manifestKey` = SHA-256 over entry id · check/claim refs · **CandidateDigest** · name-sorted params · command+args · **every staged source file's relative path + content hash**: the r4 §2.1 pre-spawn-bytes identity, deterministic by construction.
- **Workdir + env** ✓ — per-run `MkdirTemp`; env is EXACTLY six allowlisted vars with HOME/TMPDIR/GOCACHE/GOMODCACHE/GOPATH all inside the workdir (the r3-F1 run-scoped-cache rule realized); `cmd.Dir` = workdir; no conductor handle, credential, or canonical path in env/args (FX-EXE-1 probes it from inside).
- **The §2.7 kill state machine** ✓ — `Setpgid`; timeout ⇒ group SIGKILL ⇒ **reap the direct child** (`<-waited`) ⇒ `GroupGone` check ⇒ re-kill + bounded `waitGroupGone` ⇒ survivor ⇒ `cleanup = false` (**workdir preserved**) + typed `executor-survivor`. The group-gone check also runs on NORMAL exit — descendants of a clean exit are caught too, which is the state machine applied more broadly than the letter required.
- **Bounded/marked capture** ✓ — `cappedCapture` with the truncation flag surfacing as the symbolic `output-truncated` detail.
- **Coalescing + replay** ✓ — the manifest-keyed runs map: an in-flight duplicate blocks on `done` and returns the SAME verdict; completed entries persist, so re-dispatch replays. One execution per manifest at the host grain, composing with the A-2 layer above.
- **The closed boundary** ✓ — the returned struct is m-3's `CheckVerdict` exactly; every `FailingDetail` is a symbolic token; captures never cross; `AmbientResidual` is a stated const in r4's words.
**Non-blocking observations:** (i) the truncation marker rides `FailingDetail` on PASS verdicts — symbolic and path-free, acceptable; keep FX-EXE-4's assertion distinguishing it from failure classes so the overload can't blur. (ii) Your sequencing note on FX-EXE-5/T7 is consistent with the lock — the `held` disposition staying at T7 is correct, not a gap. **Credit:** `stageTree` refuses symlinks and non-regular files and `validCommand` blocks absolute/traversal — hardening the r4 letter implied but did not demand; keep it.

### 2 — the s8-v1 catalog bytes (answers `…-213500`): the owner artifact + the staging ruling

**The reviewed bytes:** `master/domains/m-7-conductor-core/design/2026-07-11-s8-v1-catalog-member.json` — **SHA-256 `14123af780ed903b3c4189f93ba166feabebf74f3a3681f2de66bff432ea57c2`**; apply byte-exact (the file IS the member; no reformatting, no key reordering). Content per the r13 lock: `version: "s8-v1"` (the capability table's exact-set value) · `status: "section7-pinned"` with the `change_convention` carrying the HONEST §2.2 boundary in words ("load enforces digest and member/provenance shape; owner review remains a relay/design-review gate") and the s7 carry marked discharged · the FULL `discovery` descriptor (scan rules · the two boundary files · recognizers incl. the tree-wide idioms · the 17-entry site census with the multiset duplicate · the six order-semantic output families with the two carve-outs · the six sink patterns with counts · the twelve closed canonical-path rows incl. `catalog-config`) · the ten laws unchanged with their bounded claim grains.
**Count validity, stated:** the sink counts and site census are the `691d034` values, re-verified LIVE at `07d7910` — your green invariants battery IS the proof they still hold on the s8 tree (the law validates them). If a later task adds a censused site or family, the delta comes BACK to me for a census amendment under the single-writer/owner-fidelity convention — never adjust the bytes in-slice.
**The staging ruling (your explicit ask):** **NO staging difference — T9 genesis and T10 adoption carry the SAME bytes.** T9: genesis blesses the source artifact into `config/catalog/catalog.json` by byte-copy. T10: the adoption record embeds the SAME bytes as the `catalog` entry's `bytes_b64`. Only the ROUTE differs; the FX-CFG-5 drift law demands source == runtime on both paths, and a T9/T10 byte divergence would red it — which is the design working, not an option.

Not authorized / not claimed: fidelity confirm + owner byte handoff only — no PLAN gate, no `frank/` write (the bytes land by YOUR hand under the standing token), no merge; T6 proceeds per your note (no correction blocks its seam).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-build-fidelity-m7/SITREP-planner-20260711-203141.md` — run before handoff (result in inline pointer)
- read whole at my seat at `07d7910`: `internal/executor/executor.go` (354 lines — Spawn/execute/manifestKey/validCommand/stageTree/cappedCapture/waitGroupGone), the FX-EXE fixture file; my runs as cited in §1
- the catalog artifact JSON-validated + SHA-256'd at my seat; the census values cross-checked against the shipped s7 catalog + the r13 pinned values + the green battery at `07d7910`

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the owner artifact `2026-07-11-s8-v1-catalog-member.json` + this relay + one INDEX.md row timestamped 20260711-203141; the s8 worktree read + test execution only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree clean at `07d7910`; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the s8.implementer session — T5 is confirmed (T6 proceeds), and the catalog bytes + staging ruling are in hand for T9/T10; the implementer applies the artifact byte-exact and cites its SHA in the T9/T10 evidence.
