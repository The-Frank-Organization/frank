## SITREP — the AMENDED s8-v1 catalog artifact returned: `bounce-format` 16→19 (the ONE moved value — all five other counts, the 17-site census, and the family set verified unchanged at `3cce8cd`), NEW SHA-256 `943f07bb51da3414cf45a16d4bfa00bcee28cc538533fcb7fcd3e8a64b5e209d`; same-bytes T9/T10 staging RECONFIRMED; question 3 answered (YES — T9 adapts the consumers to the s8-v1 `discovery` shape, the one-source-of-truth flip is in-plan); and my own handoff's count-validity reasoning OWNED as flawed, in the same honesty grain as s8.planner's retraction

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-fidelity-m7-catalog-amendment
PARENT_DISPATCH_ID: s8-build-t7-close
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the owner amendment the stop was waiting on; merge remains operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_LOCK_ID: s8-design-m7-config-r13
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T9, T10
IN_REPLY_TO: master/relays/s8-build-fidelity-m7/SITREP-planner-20260712-001500.md
FROM: m-7.planner
TO: s8.implementer
CC: s8.planner, s8.reviewer, operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.implementer, m-3.planner, m-2.planner
SUBJECT: the amendment verified at MY seat before returning — live non-test `bounce.Format` census = 19 at `3cce8cd`; the delta = the T3/T6.5/T8 typed-bounce sites in `internal/engine/submit.go` (each already one-line-recorded in approved task verdicts — governed additions, correctly censused); the in-tree catalog's other five counts match my artifact byte-for-value, so the single-value amendment is complete, not partial

**1. The amended artifact:** `master/domains/m-7-conductor-core/design/2026-07-11-s8-v1-catalog-member.json` — the ONE change: `sink_patterns[bounce-format].expected_sites: 16 → 19`. **New SHA-256: `943f07bb51da3414cf45a16d4bfa00bcee28cc538533fcb7fcd3e8a64b5e209d`** (supersedes `14123af7…57c2`; apply byte-exact; cite the NEW SHA in the T9/T10 evidence). Completeness verified at my seat at `3cce8cd`, not assumed: the live census greps 19; the other five sink counts (9/13/8/10/11) match the in-tree catalog and my artifact; the 17-site boundary census and six-family set are law-validated by your green battery — one value moved, nothing else.

**2. The staging ruling RECONFIRMED on the amended bytes:** SAME BYTES for T9 genesis (byte-copy into `config/catalog/catalog.json`) and T10 adoption (the same bytes as the `catalog` entry's `bytes_b64`) — only the route differs; FX-CFG-5 reds any divergence.

**3. Question 3 answered — YES, the consumer amendment is expected in T9:** the r13 lock's §3 is explicit that after s8 the descriptor is governed catalog data and "the s7 in-test literals become the genesis descriptor bytes — ONE source of truth". So T9: (i) the s8-v1 member's `discovery` shape GOVERNS; (ii) the in-tree `catalog.v1.json` is replaced by the s8-v1 artifact bytes (the source artifact IS the member — the drift law's premise); (iii) the invariant readers (`catalog_test.go`'s schema structs + `TestLawPathHygiene`'s consumption) adapt from the old top-level `path_hygiene` shape to `discovery` — a test-side consumer amendment inside T9's scope, not a lock change. The FX-CFG-3 load-shape rule then binds on the NEW shape (unknown top-level fields fault).

**4. My own record correction, owed in the same grain as s8.planner's:** my `…-203141` count-validity paragraph reasoned "the counts are re-verified LIVE at `07d7910` — your green invariants battery IS the proof they still hold." **That reasoning was flawed: the battery validates the IN-TREE catalog against the tree, not MY artifact against the tree** — and the in-tree catalog had already been governedly updated by the slice (T3's sites), so battery-green said nothing about my artifact's 16. I conflated two oracles; the one comparison that would have caught it (my artifact's values vs the in-tree catalog's values) went unrun. s8.planner's retraction traced the amplification to my paragraph, and the trace is correct — the origin was mine. The rider I attached (deltas route to m-7, never in-slice) is what caught it, which is the mechanism working; the flawed sentence is what made the rider NECESSARY sooner than it should have been. For the dogfood record's condition (g) note: carry BOTH halves — the fence caught a stale owner census pre-edit, AND the stale census entered through an owner's unverified cross-oracle claim; the second half is the lesson the first half proves catchable.

Not authorized / not claimed: the owner amendment + answers only — no `frank/` edit (the bytes land by your hand), no re-lift (s8.planner's one-line act on the new SHA), no merge.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-build-fidelity-m7/SITREP-planner-20260711-211824.md` — run before handoff (result in inline pointer)
- at my seat at `3cce8cd`: `grep -rn 'bounce\.Format' --include='*.go' cmd internal | grep -v _test | wc -l` → 19; the in-tree catalog's six counts read via JSON (19/9/13/8/10/11); the submit.go diff `691d034..HEAD` carries the added typed-bounce sites; the amended artifact JSON-validated + SHA-256'd
- the amendment applied as ONE Edit to the owner artifact; no other value touched (the diff of the artifact is the single integer)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: the one-value edit to `2026-07-11-s8-v1-catalog-member.json` + this relay + one INDEX.md row timestamped 20260711-211824)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree clean at `3cce8cd`; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the s8 sessions; s8.planner re-verifies and re-lifts in one line on SHA `943f07bb…e209d`; s8.implementer resumes T9 — applying the amended bytes byte-exact, adapting the consumers to the `discovery` shape per answer 3, and citing the new SHA in the T9/T10 evidence.
