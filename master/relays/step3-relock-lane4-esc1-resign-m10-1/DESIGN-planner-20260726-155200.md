## DESIGN — §D re-sign m-10 half: RE-REVIEW REQUEST to m-10.implementer (VP2 requires re-signs pair-approved, not planner-only). Requesting exact-byte review of my existing §D re-sign half `9f8c290f…` (`…resign-m10-1/DESIGN-planner-20260726-150300`) — UNCHANGED for r3: it re-signs the S-1 receipt leg over the final body `56e40261…` (which the F4/F5 corrections do NOT touch), superseding the old `{…, marker_digest, segment_id, …}` (r17 §2:308), on the strength of my pair-approved byte-confirm `92c9b3a8…`. Pair-approve so it can enter r3.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — routes an existing planner-only co-signature for the pair review VP2 now requires; authors no new byte. The join re-sign completes on all three legs; r2/r3 composition is Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 9f8c290f3bf37af7
DISPATCH_ID: step3-relock-lane4-esc1-resign-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-vp2
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-vp2/DESIGN-orchestrator-planner-20260726-153807.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-1.planner, m-3.planner, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-resign-m10-1/DESIGN-planner-20260726-155200.md
SUBJECT: re-review request — exact-byte review of my §D re-sign half `9f8c290f3bf37af7…` (`…resign-m10-1/DESIGN-planner-20260726-150300`), unchanged for r3 (re-signs S-1 over `56e40261…`, F4/F5 do not touch the receipt body); pair-approve for r3

m-10.implementer — VP2 requires the §D re-sign co-signature to be **pair-approved, not planner-only** (the join is an owner act and pairs like one). Requesting your exact-byte review of my existing re-sign half.

## The target (unchanged for r3)
`master/relays/step3-relock-lane4-esc1-resign-m10-1/DESIGN-planner-20260726-150300.md` @ SHA-256 `9f8c290f3bf37af7…`. It **re-signs the §D join's S-1 content-ready receipt leg** over the final body `56e40261…` (`{turn_id, attempt_id, round_identity, seq_hwm, generation_id}`), superseding the old co-signed body `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}` (r17 §2:308), on the strength of my **pair-approved byte-confirm `92c9b3a8…`**; S-2/S-4/S-5 legs unchanged; m-1 redaction leg stated not body-shape-sensitive.

## Why it is unchanged by the r2 rejection
The seven VP2 blockers are about the **edited-session lowering (F4)** and the **fencing weight (F5)** — **neither touches the S-1 receipt body `56e40261…`**, which is pair-approved and frozen. So my re-sign half stands exactly as filed; it needs only the pair review VP2 requires. m-9's re-sign half (`ef72c732…`) is under the same requirement; both re-signs + m-1's redaction leg compose the re-signed §D join for r3.

## Review ask
Confirm the re-sign half accurately co-signs the S-1 leg over `56e40261…`, names the correct superseded fragment (r17 §2:308), rests on the pair-approved byte-confirm `92c9b3a8…`, scopes to S-1 only, and asserts no m-9/m-1 byte. On your APPROVE it is m-10's owner-final re-sign half for r3.

## Boundaries
Design-only, routing an existing co-signature for pair review. No new co-signature authored, no body/byte moved, no S-2/S-4/S-5 re-open. `receipt_conflict` FROZEN. rev16 `3e3c5192…`, r17 `01b885fe…`, m-9 body `56e40261…`, §D-settlement amendment `1fa71cb8…`, interface lock `cbd1893c…`, r2 `ed7e22af…` (preserved-unratified) UNMOVED. The join re-sign + r3 composition stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- Re-sign half re-hashed on disk: `…resign-m10-1/DESIGN-planner-20260726-150300.md` = `9f8c290f3bf37af7…` (matches master's VP2 citation). Body `56e40261…` + byte-confirm `92c9b3a8…` pair-approved, UNMOVED.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-resign-m10-1/DESIGN-planner-20260726-155200.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this re-review-request relay + one INDEX.md row; no co-signature/body/byte authored or moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-10.implementer exact-byte reviews the re-sign half `9f8c290f…`; on APPROVE it is m-10's pair-approved re-sign half. m-9's re-sign half + m-1's redaction leg (both under the same pair-approval requirement) compose the re-signed §D join for r3. rev16 UNMOVED. H-12 stands.
