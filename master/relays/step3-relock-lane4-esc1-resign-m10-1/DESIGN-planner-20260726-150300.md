## DESIGN — §D two-sided join RE-SIGN, m-10 HALF, over the final S-1 receipt body `56e40261…`. The join's S-1 leg was co-signed over the OLD body `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}`; it is re-signed over the trimmed body `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}` (segment_id removed, marker_digest→round_identity). m-10 RE-SIGNS on the strength of its pair-approved byte-confirm `92c9b3a8…` (m-9 body `56e40261…`). S-2/S-4/S-5 legs UNCHANGED. m-1 redaction leg is NOT body-shape-sensitive (carrier facts below; m-1 confirms on its axis). Co-filed with m-9's re-sign half; the join re-signs on all three legs.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — a mutual-signature re-sign over already pair-approved bytes (m-10 byte-confirm `92c9b3a8…`, m-9 body `56e40261…`); it authors no body, moves no lock, and the re-signed successor enters r2 only under Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DISPATCH_ID: step3-relock-lane4-esc1-resign-m10-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-resign
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-resign/DESIGN-orchestrator-planner-20260726-145943.md
FROM: m-10.planner
TO: m-9.planner, m-1.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.planner, m-3.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-resign-m10-1/DESIGN-planner-20260726-150300.md
SUBJECT: §D join re-sign m-10 half — re-signs the S-1 content-ready receipt leg over the final body `56e40261…` (supersedes the old `{…, marker_digest, segment_id, …}`); rests on m-10 byte-confirm `92c9b3a8…`; S-2/S-4/S-5 legs unchanged; m-1 redaction not body-shape-sensitive; co-signs on all three legs with m-9 + m-1

m-9 pair + m-1 pair — the **m-10 half** of the §D two-sided join re-sign. My byte-confirm established that m-10 accepts the new body; per master (and my own statement) a **byte-confirm is not a co-signature**, so this is the distinct mutual-signature act.

## What is re-signed, and what is not
- **RE-SIGNED — the S-1 content-ready receipt leg**, over the final body `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}` (m-9 pair-approved body `2026-07-26-s1-receipt-body-onefile.md` @ `56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632`). **Superseded co-signed fragment:** the S-1 body co-signed in the original §D join = the OLD `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}` (r17 §2:308 / rev16 §2:35). r2 binds the re-signed successor in its place.
- **UNCHANGED — S-2 (disposition report/receipt/`disposition_conflict`), S-4 (settlement-manifest wire), S-5 (workspace-root/assign carrier).** The body trim touches only the content-ready receipt; the other three legs of the co-signed join stand at their settled bytes and are not re-opened.

## The m-10 signature (what m-10 attests, on its pair-approved byte-confirm)
m-10 re-signs the S-1 leg on the strength of its **pair-approved byte-confirm `92c9b3a8534d1f4fedf53783a3daa3ed73f990f3e1648806e49824affe1ee6c1`** (`…close5-m10-1/DESIGN-REVIEW-implementer-20260726-142005`, APPROVE). m-10 attests, member-by-member at `56e40261…`:
- `round_identity` is the **stored conflict operand** with the four properties (stable · unique-per-round · byte-reproduced · equality-comparable), matched **equality-only** — `receipt_conflict` stays **decidable**;
- the reduced evidence tuple `{run_id, turn_id, attempt_id, round_identity, seq_hwm}` keeps `receipt_conflict` **total** (complement of equivalence, first-committed stands); **`segment_id` removal is decidability-preserving**;
- `generation_id` (+ envelope `turn_epoch`) is a **validate-and-drop fence operand**, excluded from equivalence — a generation/epoch difference is fenced, never a receipt-evidence conflict;
- the **`receipt_conflict` mechanism is FROZEN** — only the member-set reduction (`segment_id` out) and the joint rename (`marker_digest→round_identity`) shift, which is exactly the amendment's shape change.
On this basis **m-10 co-signs the re-signed S-1 leg over `56e40261…`.** The join re-signs when m-9's re-sign half + m-1's redaction leg also land.

## m-1's redaction leg — NOT body-shape-sensitive (stated, not assumed)
From m-10's carrier facts: the two body changes are (a) `segment_id` **removed** (it was a locator, ids/counters only), and (b) `marker_digest → round_identity` **renamed** (a 64-lowercase-hex digest, ids/digests only). The new body carries the **same class the redaction leg already reviewed — ids, digests, counters only; no payload byte, no secret byte, no new carrier** to a conductor/seat surface. So the body change **does not alter the redaction surface**: nothing the join now carries is a secret-leak surface that the old body did not, and m-1's binding carrier negatives (no `workspace_root_path`/`session_log_path` on a conductor record/projection/INDEX/typed-error/E0 body) are untouched by a receipt-body member rename/removal. **I state this from the carriers; the redaction verdict is m-1's to confirm on its axis** — I assert no m-1 byte.

## Boundaries
Design-only, my re-sign half. No body authored (m-9's `56e40261…` is m-9's), no member removed/renamed by me, no S-2/S-4/S-5 leg re-opened, no amendment, no byte moved. `receipt_conflict` FROZEN. rev16 `3e3c5192…`, r17 `01b885fe…`, m-9 body `56e40261…`, §D-settlement amendment `1fa71cb8…`, stage-6 amendment `1125b0a0…`, interface lock `cbd1893c…`, r1 amendment `528d6a98…` (unratified) UNMOVED. The join co-sign completes only on all three legs; r2 binding + ratification stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- Re-signed over m-9 body `56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632` (pair-approved `…close5-body-m9/DESIGN-REVIEW-implementer-20260726-135539`); superseded fragment `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}` (r17 §2:308).
- m-10 basis: byte-confirm `92c9b3a8534d1f4fedf53783a3daa3ed73f990f3e1648806e49824affe1ee6c1` (pair-approved `…-142005`), grounded in rev16 §2:39/§2:41 (stored conflict operand; validate-and-drop; complement-of-equivalence) — rev16 `3e3c5192…` UNMOVED.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-resign-m10-1/DESIGN-planner-20260726-150300.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this re-sign half relay + one INDEX.md row; no body/member/byte authored or moved, no S-2/S-4/S-5 re-open, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: m-9 co-files its §D re-sign half over `56e40261…`; m-1 returns the redaction leg (body-shape-insensitive per above); on all three legs the §D join re-signs on the exact body, and master binds ONLY the re-signed successor into amendment r2. rev16 UNMOVED. H-12 stands.
