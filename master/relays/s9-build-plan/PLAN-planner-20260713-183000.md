## PLAN — s9 pair PLAN: the approved rev11 plan-of-record ADOPTED byte-verbatim (sha-pinned, incl. the rev12 team-shape correction that names this pair); execution map T1→T9 under B11 straight-through; the 10-row all-in mechanical fence at named-seam grain; T7/T8 held on condition (f); the blocked ledger carried as governance relays; adversarial plan-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-build-plan
PARENT_DISPATCH_ID: master-boot-s9-planner
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the slice merge is operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e); no other human leg in this plan
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan-rev11-s9.1
IN_REPLY_TO: master/relays/boot/master-boot-s9-planner/SITREP-orchestrator-planner-20260713-220011.md
FROM: s9.planner
TO: s9.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-2.planner, m-7.planner, m-1.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: adopt-not-re-litigate — m-3's r12-approved plan-of-record consumed as the spec-of-record with an execution delta only (order, seams, holds, evidence capture, the fence block of record); review it adversarially and return PLAN_REVIEW_VERDICT under `s9-build-plan-review-r1`; on approve + all-in + no hard trigger I issue the delegated token under `s9-build-token`

s9.implementer — this is the directly addressed PLAN your boot holds for. Per our boots and master's staffing ruling (`s9-build-impl/PLAN-orchestrator-planner-20260713-220010`, Option A), we are the fresh slice pair; m-3 guides and files owner-fidelity from outside; the mechanics below are r12-approved and are NOT re-litigated here. `PLAN_LOCK_ID: s9-evidence-thicken-plan-rev11-s9.1` names this instrument: the plan-of-record byte-verbatim + this execution delta + the fence block of record in §4.

### 1. Adoption + lineage

- **Plan-of-record, adopted byte-verbatim:** `master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md` — rev11 as amended by the rev12 team-shape drafting correction (master-granted without re-review; it names this pair as the build seats). Pinned: `sha256 f2a3cb3a5dc63a0814352f654d30de162b8af43816c6b7688d05960ab7b8853b`. Verify the hash yourself before your verdict; a mismatch is a must-revise, not a judgment call.
- **Approval lineage (lives in the m-3 chain, consumed by reference):** PLAN-REVIEW `approve` r12 (`s9-plan-m3/PLAN-REVIEW-implementer-20260713-213000`, `DISPATCH_ID: s9-plan-m3-review-r12`) on rev11 with the SCOPE_DIFF returned all-in. This relay carries `DESIGN_DOC_ID` as context only and no `DESIGN_LOCK_ID`/`DESIGN_RECORD_KIND`, per the s10 §1 lineage reading: the owner pair locks no design; the s8 design lineage lives in the m-3 chain.
- **Master rulings consumed (do not re-open):** the B-opaque ratification (`s9-plan-m3/PLAN-orchestrator-planner-20260713-191510`) · the `lane_vcs` branch-only reconciliation + ACTIVATION (`s9-lanevcs-reconcile/RECONCILE-orchestrator-planner-20260713-194510` — B-opaque is in the buildable set; `none` is the opaque-accept discriminator; the undeclared token gates ONLY the opaque-accept branch; RED-first both ways) · the scope_paths co-sign (`s9-scopepaths-cosign/RECONCILE-orchestrator-planner-20260713-160510` — B1's ruled shape; its §F m-1 leg is STILL PENDING, so B1 stays ledgered and `diff_paths ⊆ scope_paths` stays STRUCK until item-10 regardless).
- **Delegated-authority chain for the coming token:** the orchestrator delegation (`s9-dispatch/PLAN-orchestrator-planner-20260713-130004`, `DELEGATED_DISPATCH_AUTHORITY: yes` under conditions (a)–(g)) → the Option-A staffing ruling (the token issues inside THIS pair's loop) → my boot. The prior token `s9-build-impl/IMPL-planner-20260713-214500` is VOID on master's ratification, is not in this chain, and stays void.

### 2. Execution map (the delta this relay adds — nothing in the plan-of-record moves)

- **Branch:** `s9-evidence-thicken` off `frank/ main@39474d0` (`s10-close`). You are sole code writer.
- **Order (SEQ per the plan-of-record):** T1 (shared FS-worker seam, FIRST) → T2 (`find-references`) → T3 (§6.1 verdict-binding pass + rows) → T4 (opaque-lane total table + probe removal + the master-activated `lane_vcs: none` branch) → T5 (attestation negative) → T6 (verification-only, no commit) → T9 (runnable exit-fixture set + ⑤ ODB egress pair). **T7/T8 open ONLY on their co-signs** (condition f): T7 on the m-2 co-sign; T8 on the m-1 + m-2 confirms. If a co-sign lands mid-slice the task slots in after T4; if not in hand by T9, both exit as report-and-hold — never proceed-on-assumption, never a `t.Skip` stub.
- **Same-file seams (ordering load-bearing, arm-per-task):** T1 lands `internal/observe/fs_worker.go` + the extraction from `read_file_worker.go` (read-file byte-unchanged) → T2 and T4 consume it. `registry.go` is touched by T2 (the `find-references` entry + its `validParams` branch), T3 (the `Evaluator` binding pass), and T4 (`RegistryEnv.LaneVCS` + its non-aliasing clone in `NewRegistry`) — each its own named unit, in that order. `gate.go` is touched by T3 (the `baseStamps` claim-row seam) then T4 (extends it). A diff outside a task's named seam at review is a deviation, not a judgment call.
- **T4 owner-byte sequence (the s10 T1 pattern, binding):** Step 3a — the m-7 owner-bytes request goes out at token time under `s9-build-fidelity-m7` (FROM me), so the bytes are in hand by the time you reach T4; you carry them **VERBATIM as their own commit** (config schema/transition/reader-ceiling/`composeSupply` + `Supply.LaneVCS` cloned map · the `main.go` → `observe.RegistryEnv.LaneVCS` runtime handoff · the dogfood descriptor bytes at the locus m-7 names — store-side pinned config; its fixture pins live under `test/fixtures/` · the FX-VCS RED-first negatives). You author only the m-3 consumer + terminal semantics (Step 3b). Step 6 — m-7's landing confirm is batched with the end-of-slice owner confirms. **No marker-derived logic anywhere** (the text-sweep guard is part of T4's Step 4).
- **B11 cadence:** straight-through, task-by-task under `superpowers:executing-plans`, NO per-task review relays. ONE end-of-slice adversarial review by me at completion; owner Step-5/fidelity confirms batched at exit with STOP-ON-CONTRADICTION (m-3 owner-fidelity as guide · m-7 lane_vcs bytes + executor-untouched · m-2/m-1 only if T7/T8 landed). Mid-build blockers: report and hold TO me (condition f); cross-domain → master through me; locked-contract changes → owning pair + master, never silent.
- **Evidence capture:** per-task RED/GREEN batteries FILE-captured under `frank/.relays/s9/` (sequence-honest — the capture precedes the claim); the ten INV-CATALOG laws green at every commit point; full `go test ./...` at exit. Day-one exit items: the two mechanical tables (consumption→supply · diff→license fence-row reconciliation) maintained as you go; label→mechanism sweep + verify-the-summary-line ride my end review; RED-first negatives per task; byte-exact `{accepted, rejected, held}`; R2; Rails A/B per new surface; I-PH over every new seat-visible surface.
- **Blocked ledger (governance relays, NOT code):** B1 (scope_paths — ruled shape co-signed; §F m-1 leg pending; evaluation STRUCK until item-10) · B-diff (the two-run executor capability + m-7 capability-confirm) · B3 (item-10 + attribution-gated entries) · B2 (item-9, egress-fenced) · B4 (the operator-attestation positive). Each stays behind its named gate; the s9 dogfood evaluation extends the s8+s10 ledger as ONE series (condition g), including the mis-seated-token datum (it linted clean; the operator's read caught it — no gate checks staffing semantics).

### 3. The mechanical fence (the block of record for the token)

SCOPE_DIFF:
- frank/internal/observe/ -> in
- frank/internal/config/config.go -> in
- frank/internal/fieldspec/registry.json -> in
- frank/internal/fieldspec/registry_test.go -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/validate_test.go -> in
- frank/internal/executor/executor.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/ -> in
- frank/.relays/s9/ -> in
SCOPE_DIFF_RESULT: all-in

**Named-seam licenses (file-wide rows do NOT erase them):** `config.go` — ONLY the master-activated `lane_vcs` v3 loci, m-7-authored bytes carried verbatim (`validateEngineSchema` v3 arm + ceiling · `ValidateMemberTransition` v2→v3 adjacent hop · the reader-marker call site · `composeSupply`/`validSupplyShape` + the cloned non-aliasing `Supply.LaneVCS`). `main.go` — ONLY the runtime handoff (the pinned map → `observe.RegistryEnv.LaneVCS`). `executor.go` — **NO named seam in s9**: the row is standing fence-union membership only; no task touches it; `CheckVerdict` shape + `Spawn` refusal byte-untouched (master F1); any diff there STOPS and returns through me. `registry.json`/`registry_test.go` — m-2-owned, T7 only, held on the co-sign; the s11 collision edge is dormant (serialized after s9) and coordinated through the m-2 guide. `validate.go`/`validate_test.go` — m-2-owned, T8 only, held on the co-signs. `test/fixtures/` — T9's ⑤ ODB egress pair + the s8/s10 regression suites kept green (`s8_decision2_test.go` · `s8_adversarial_test.go` [the truncated-pass baseline T3 must preserve byte-intact] · `iph_test.go`) + m-7's FX-VCS fixture pins carried verbatim. `.relays/s9/` — evidence captures only, not governance relays.

### 4. Acceptance criteria

The plan-of-record's acceptance criteria (rev3 block, as revised through rev11) stand **verbatim** — including: no filesystem syscall on the serialized commit path (injected-blocking-proven); no incomplete observation passes; no executor-claimed identity trusted; the locked Option-2 E0 floor holds (claimless clean/matching stays `Degraded`/`self_reported`/E0 — item 10 alone lifts it); no contradictory verdict tuple survives; no executor-authored string or self-selected disposition reaches a seat surface; the opaque-accept branch fires ONLY on a governed `lane_vcs: none` (nil/`git` never accepts; the pre-v3 nil path still runs the full §13 observation; the locked §13 suite stays green); no marker-derived logic survives (text-sweep guard); every label states exactly what it proves; no blocked item as a code skip. **Plus the binding grains of this instrument:** (i) each task's diff reconciles to its §2 named seam in the diff→license table; (ii) T4's m-7 bytes land verbatim with Step-6 confirm at exit; (iii) T7/T8 land only with their co-signs in hand, else report-and-hold; (iv) the truncated-pass regression survives T3 byte-intact; (v) batteries are FILE-captured sequence-honest under `frank/.relays/s9/`.

### 5. Conditions (a)–(g), verbatim binding from the dispatch

(a) unique sub-DISPATCH_IDs (the `s9-build-*` chain; batteries FILE-captured under `frank/.relays/s9/`); (b) schema/registry changes beyond the ruled ones → master BEFORE work, coordinated with s11 through the m-2 guide; (c) locked-contract changes → owning pair + master, never silent; (d) cross-domain → master; (e) merge operator-only via `HUMAN_MERGE_AUTHORIZATION` at grant time; (f) blocked lanes report and hold; (g) the dogfood evaluation extends the s8+s10 ledger as ONE series.

### 6. What I request from you

1. **Verify the adoption is faithful:** re-hash the plan-of-record against the pinned sha; check this delta adds no task, mechanism, acceptance criterion, owner relation, or authority beyond the r12-approved bytes; check the fence rows against the plan's fence-union and the live tree at `39474d0`; check the holds (T7/T8, the ledger) and the ordering against the plan's SEQ.
2. Return `PLAN_REVIEW_VERDICT` in a lint-clean relay, `FROM: s9.implementer`, `TO: s9.planner`, `DISPATCH_ID: s9-build-plan-review-r1`, `PARENT_DISPATCH_ID: s9-build-plan`, with your own mechanical scope check over the §3 block.
3. On **approve** with an all-in result and no hard trigger, I issue the delegated same-owner token under `s9-build-token`, parented to your approving review, restating the block, the seam licenses, the holds, and conditions (a)–(g) — and I cut the m-7 owner-bytes request (`s9-build-fidelity-m7`) beside it. The token remains the only thing that starts code; T7/T8 additionally wait on their co-signs even after dispatch.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-build-plan/PLAN-planner-20260713-183000.md` — run before handoff (result in the inline pointer).
- Inputs consumed this turn: the charter · my boot (`master-boot-s9-planner/SITREP-…-220011`) · the plan-of-record (sha-pinned above) · the r12 approving review (`s9-plan-m3-review-r12`) · the staffing ruling (`s9-build-impl/PLAN-…-220010`) · the three master rulings (`…-191510`, `…-194510`, `…-160510`) · the dispatch conditions (`s9-dispatch/PLAN-…-130004`) · the live tree (`frank/` main clean at `39474d0`; `internal/observe/{registry,gate,checks_base,read_file_worker}.go` and `internal/fieldspec/{validate,registry}*.{go,json}` confirmed present; no `fs_worker.go` yet).

ACTIONS_GIT_REF: none — plan authoring only; no `frank/` edit, no branch, no code, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260713-183000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `39474d0` (`s10-close`).
Next requested action: operator carries this to s9.implementer; adversarial review → `PLAN_REVIEW_VERDICT` under `s9-build-plan-review-r1`; on approve + all-in I issue the delegated token under `s9-build-token` + the m-7 owner-bytes request; a must-revise returns the exact finding to me.
