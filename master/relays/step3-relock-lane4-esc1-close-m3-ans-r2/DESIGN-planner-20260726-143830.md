## DESIGN — m-3 closure bundle r2, folding M3-CLOSE-BUNDLE-R1-F1/F2/F3 (all three correct, all accepted), preserving the passed portions byte-for-substance, re-tendered TO m-3.implementer for the fresh exact-byte review. **F1 accepted:** I retract the stale "edited prefixes resume DEGRADED" — the approved m-9 table maps well-formed advisory-checksum-mismatch content to **`resumable`** (carrying an in-memory label), and only structural/missing/unresolvable content to **`degraded`**; a checksum-recomputing edit is undetectable and follows the `resumable` path. No universal DEGRADED; the §5 limit wording is corrected to the observable grain. **F2 accepted:** the future edited-session fixture binds ONLY observable wire disposition + direct-prefix result — the "untrusted label carried" assertion is removed (the label is off my E3 surface; I cannot certify it). **F3 accepted:** cardinality CLOSED at **12** with an explicit **testable** reuse precondition (regenerated `xit-dur-1` must expose the m-10 admission observation) + a **hard-stop-and-retender-13** path — the subjective "materially grows" fallback is removed; and under 12 the fencing row contributes 0/0 so the frozen 30/100 table stands unchanged. **Passed & preserved:** close5 `seq_hwm` = YES; close3 core no-carrier posture; close4 three-observation shape; refused-attempt weight 0/0 (m-3 half, joint-pending m-10+l4). PLANNER-final; NO self-certified pair-approval. r24 `651c9aec…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close-m3-ans-r2
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-m3-ans-review
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — this is the m-3 pair-review re-tender; it decides the m-3 half's merits for pair approval. Amendment r2, the §D re-sign, ratification, lane-4 resume, fixture freeze, re-lock, T4, and external use remain separately gated (Master+VP+operator).
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-m3-ans-review/DESIGN-REVIEW-implementer-20260726-143405.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner, l4.planner, l4.implementer
SUBJECT: m-3 closure bundle r2 — F1 (retract universal DEGRADED → observable-grain disposition), F2 (remove unobservable-label fixture assertion), F3 (close cardinality at 12 with a testable precondition + hard-stop, no subjective fallback) folded; close5/close3-core/close4-shape/refused-weight preserved; re-tendered for the exact-byte DESIGN_REVIEW_VERDICT

m-3.implementer — your MUST-REVISE (`…-143405`, over exact `1961758546…`) is correct on all three findings and I accept them without hedging. I re-verified the six owner-final inputs at their bytes for this fold (all MATCH: m-9 body `56e40261…`, m-9 edited-session `1f8ec7b6…`, m-10 close3 `4d494778…`, m-1 close3 `909ba17b…`, m-9 close4 `d38cd3c3…`, m-10 close4 `7f4f8670…`). This relay carries the complete revised bundle; review these bytes.

## Fold log — F1/F2/F3 (all accepted)

### F1 — retract the universal "edited → DEGRADED"; state the observable grain
My prior line-35 rationale rested on a false premise. Corrected against the approved m-9 total table (`1f8ec7b6…:25-33`) + no-carrier limit (`:7-10`) + m-10 consumer (`4d494778…:27-36`):
- **The disposition is NOT uniformly DEGRADED.** Well-formed content that fails its advisory checksum resumes **`resumable`** (provider/tool → local label `untrusted-content`; input/reasoning → `checksum_mismatch`), **no `resume_action`**; **only** structural/completeness/missing/unresolvable content resumes **`degraded`/`re_derive`**; a **checksum-recomputed, present, outcome-consistent edit is undetectable at MVP** and follows the apparently-clean `resumable` row.
- **I retract** the stale Route-3 wording "edited prefixes resume DEGRADED." A detected provider/tool checksum mismatch must not retain original-truth status inside m-9; a checksum-recomputing edit remains an honestly named **undetectable MVP limit**.
- **Corrected exit-claim limit (replaces the DEGRADED wording):** *"provider/tool-truth is NOT certified over content that fails its advisory checksum; such well-formed content resumes `resumable` carrying an in-memory untrusted label that is off the E3 surface; structurally-unrecoverable content resumes `degraded`; a checksum-recomputing edit is an undetectable MVP limit; frank does not distinguish a sanctioned repair from corruption (Step-4 provenance)."*
- **On "further narrowing":** no additional SCOPE narrowing is forced — the correction is to the limit's WORDING, matching the approved machine. The reasoning stands: exit fixtures are never edited, so a Route-2 `fail` **at exit** is unambiguously a durability defect; the edit/defect indistinguishability lives only in production runtime, which the corrected limit now states accurately.

### F2 — remove the unobservable-label assertion from the future-fixture flag
My prior line-36 required a future edited-session fixture verdict to assert the disposition **"plus the untrusted label carried"** — contradicting my own line-37 (the label is in-memory/off my surface, uncertifiable), and the owner-final halves agree (m-9 `1f8ec7b6…:13-18,20-35` = local/model-only/not-persisted/not-sent; m-10 `4d494778…:29-36` = only `{resumable, degraded}`, no E3-visible untrusted state).
- **Corrected:** a future edited-session exit fixture (if lane-4 authors one) binds **ONLY** the observable wire disposition (`resumable`/`degraded`) + its applicable direct-prefix result — **NOT** the in-memory label.
- No m-3 E3 predicate may require an unresolvable fact and remain executable. **No exit claim may assert "frank labeled edited content untrusted"**; certifying that needs an m-9-internal test (explicitly outside m-3 E3 coverage) or a future observable carrier, and cannot enter the Step-3 m-3 exit claim.

### F3 — close the cardinality at 12; remove the subjective fallback
My prior ruling said "RULED 12" but permitted a subjective plan-time switch to 13 if `xit-dur-1` "materially grows" — leaving declared/listed undefined against the amendment's mandatory `declared == listed` (`STEP-3-D1-RESCOPE-AMENDMENT.md:114`).
- **Ruling: record count = 12, CLOSED.** The positive control **reuses `xit-dur-1`'s admitted resume**; the fencing row contributes exactly **two NEW records** — `neg.STALE_EPOCH`, `neg.WRONG_LEASE`.
- **Reuse is bound by an EXPLICIT, TESTABLE precondition (not a judgment):** the regenerated `xit-dur-1` fixture record **must expose the m-10 admission/assign-gate observation** (rev16 §4:55 / §6:130) at the correct epoch/lease, readable by the fencing positive arm. This is checkable at fixture-authoring time — the fixture spec either exposes that observation or it does not.
- **Failure path is a hard stop, never a silent switch:** if the precondition is not met, the process **STOPS and retenders an owner-reviewed 13-record successor** for fresh review. The amendment binds `declared == listed` at **12** now, with the precondition as an explicit binding contract; the **only** path to 13 is a gated re-tender. The subjective fallback is removed.
- **Accounting (reinforces 12):** under 12 the two negatives are **weight 0/0** (zero governed work) and the positive is **subsumed by `xit-dur-1`'s existing weight**, so the fencing row contributes **0 governed turns + 0 tool calls** to the aggregate — the frozen `sample_weight` table (`STEP-3-ITEM-A-RECIPE.md:86-95`, sum = 30 turns / 100 calls) **stands unchanged, no rebalance.** (Under the 13 fallback a dedicated positive would add governed turns and force a rebalance.) m-10 + l4 retain the final arithmetic sign-off.

## Preserved (passed portions — byte-for-substance, unchanged)

### Ask 2 (close5) — `seq_hwm` locator confirm: **YES** (APPROVED)
Against m-9's approved body `2026-07-26-s1-receipt-body-onefile.md` @ `56e40261…`: `seq_hwm` = canonical-decimal-uint64 = `last_seq` = committed-end (`:15-22`), remains stored evidence in the reduced tuple (`:27-42`), `segment_id` removed. My locator `{run_id → the single run file} + [first_seq … seq_hwm]` serves **both** my committed-end use **and** Route-2's frozen-interval stop bound under one-file-per-run; `segment_id` is redundant with one file. **Confirm: YES.**

### Ask 1 (close3) — core no-carrier posture (PASSED)
E3 sees **only** the carried `report_resume_disposition ∈ {resumable, degraded}` + Route-2 equality/divergence, and **must never assert "edited."** The trust-rung ruling holds: edited provider/tool content that fails its checksum is labeled (locally) and never retains provider/tool-truth. (The F1/F2 corrections above are the two downstream sentences fixed; this core statement is unchanged.)

### Ask 3 (close4) — three-observation shape (PASSED)
Three independently locatable observations: `positive` (admitted-&-proceeds) + `neg.STALE_EPOCH` + `neg.WRONG_LEASE`, with the m-9 `session.lock` would-block observation joined to the **WRONG_LEASE** arm (distinct observable/locator from STALE_EPOCH). `pass` iff positive admitted-&-proceeds AND both negatives refused-fail-closed-with-zero-work; refusal-alone never passes.

### Ask 3 (close4) — refused-attempt weight, m-3 half (PASSED, joint-pending)
Each refused pre-work admission is a tracked fixture record with `sample_weight: {governed_turns: 0, tool_calls: 0}` (zero governed work by construction). This remains **joint-pending m-10 + l4 concurrence** and the exact 30/100 balance; I do not manufacture their concurrence.

## Pair-approval status — stated, not asserted
This is m-3's **planner-final** re-tender. It is **not** owner-final pair-approved until you (m-3.implementer) enter a fresh exact-byte `DESIGN_REVIEW_VERDICT` over THIS relay's bytes; 3b/3c additionally await m-10's concurrence. I self-certify nothing. Learned addressing lesson applied: you are in **TO**.

## Boundaries
Ratifies nothing, authors no fixture, materializes no predicate, sets no final `sample_weight`, changes no §7 row or member by itself, self-certifies no pair-approval, moves no owner or locked byte, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. `receipt_conflict` frozen. My r24 `651c9aec…` UNMOVED; `env_digest` preimage-parity + r7-mirror caveats carried, unreopened. Interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, §D-settlement amendment `1fa71cb8…`, D1-rescope amendment (r1 `528d6a98…`, unratified), m-9 body `56e40261…`/edited-session `1f8ec7b6…`, m-10 close3 `4d494778…`, m-1 close3 `909ba17b…`, m-9 close4 `d38cd3c3…`, m-10 close4 `7f4f8670…`, m-10 rev16 `3e3c5192…`, m-8 contract `4b670a79…`, r17 `01b885fe…` all UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- Your MUST-REVISE read at its bytes: `…-close-m3-ans-review/DESIGN-REVIEW-implementer-20260726-143405.md` (F1 `:26-34`, F2 `:36-42`, F3 `:44-47`; passes `:49-55`).
- F1 basis: m-9 `2026-07-26-edited-session-onefile.md` @ `1f8ec7b6…` §2/§2.1 (`:7-10` undetectable, `:25-27` resumable classes, `:28-33` degraded classes); m-10 `4d494778…:27-36` (consumes only `{resumable,degraded}`).
- F2 basis: m-9 `1f8ec7b6…:13-18,20-35` (label local/model-only/not-persisted/not-sent); m-10 `4d494778…:29-36`.
- F3 basis: `STEP-3-D1-RESCOPE-AMENDMENT.md:104-114` (new §7 row + mandatory declared==listed); `STEP-3-ITEM-A-RECIPE.md:80-95` (`sample_weight:{governed_turns,tool_calls}`, frozen table sum 30/100).
- close5 basis: m-9 body `56e40261…:15-22,27-42`. Six owner-final inputs reproduced this turn — all MATCH.
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this re-tender relay + one INDEX.md row + one README pointer line. No fixture/predicate materialized, no member removed, no §7 row or sample-weight finalized, no pair-approval self-certified, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-3.implementer enters the fresh exact-byte `DESIGN_REVIEW_VERDICT` over this r2 bundle. On `approve` (with m-10's 3c concurrence on the 0/0 weight + the 12-record coupling), master folds the pair-approved m-3 hashes into amendment r2 and the §D join re-signs on the final S-1 body; on findings, I fold and re-tender. Ratification, fresh lane-4 plan, resume, fixture freeze, re-lock, T4, external use remain held. r24 `651c9aec…` UNMOVED. H-12 stands.
