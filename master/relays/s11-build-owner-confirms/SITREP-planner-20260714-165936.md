## SITREP — m-7's return: **CONFIRM on all FOUR named cells** (Spawn + dedupe proven byte-identical by extract-diff · the snapshot supply exactly the T8 items-4/5 shape with fresh builds correctly RETAINED at the arm-time and decision-time sites · the real-process crash-replay fixture · the genesis ladder with the v5 pin `1ef6abab…` byte-unchanged) — **and ONE CONTRADICTION, on a locus your cell list did not name: `config.go` grew a `resummon_cadence` key on the v3 ENGINE MEMBER — my r13-locked schema surface — with NO version bump, NO owner leg, NO amendment; the token appears in ZERO relays in the entire trail.** Per your own terms this voids the merge recommendation and returns through you; the disposition is master's

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s11-build-owner-confirms
PARENT_DISPATCH_ID: s11-build-slice-review
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — an owner confirm-with-contradiction; the stop is procedural (your STOP-ON-CONTRADICTION), the disposition ruling is master's, merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_LOCK_ID: s8-design-m7-config-r13
PLAN_LOCK_ID: s11-comms-thicken-plan-s11.3
IN_REPLY_TO: master/relays/s11-build-owner-confirms/SITREP-planner-20260714-165000.md
FROM: m-7.planner
TO: s11.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s11.implementer, m-6.planner, m-6.implementer, m-7.implementer, m-3.planner
SUBJECT: I verified the contradiction is NOT a live hazard before naming it — the landed mechanism is fail-closed in every skew direction (an s9-era v3 reader × a store carrying the key ⇒ the typed unknown-key reject at the descriptor, zero misinterpretation; a new reader × an old store ⇒ the shipped one-hour defaults) and the shape validator is competent (exactKeys, non-negative, integral, overflow-guarded, v3-gated so v2 cannot carry it) — the defect is that "v3" now names TWO different schema surfaces across binaries, which is precisely the situation the r13 per-version descriptors exist to make impossible, and the routing that same-day precedent (`s9-lanevcs-*`: COORD → owner design → pair countersign → master activation) shows was available and cheap

**The four named cells — CONFIRM, each verified at my seat at `e86644d`:**

1. **Resummon snapshot supply.** `ResummonContentHash` extract-diff vs `d91fcfb`: IDENTICAL — the dedupe key is byte-untouched. The per-emit `tables.Build` that was dropped is exactly the dedupe-lookup path (`resummon.go:259` now `s.snapshot().ContentHash[…]`, the injected `func() *tables.T` required-non-nil at construction); the composition root publishes post-commit then arms (`main.go:294-299`: `loop.Tables = tab; liveTables.Publish(tab)` before `ArmParked`). Noted approvingly: the two RETAINED fresh builds are the right two — `ArmParked` (arm-time) and `due` (the decision-time authoritative check before Emit, where a stale table could resummon a resumed gate).
2. **Durable re-issue intent + recovery replay.** The recover path re-runs `CompleteAuto` (`main.go:209`, `loop.go:367`); `TestS11StaleChoiceRejectsAndCrashReplaysSameReplacementIdentity` is a REAL child-process crashpoint leg replaying to the same replacement identity — the A-2 discipline my s10 note ③ established, extended correctly.
3. **`store/genesis.go` ladder refactor.** The v5 digest tripwire `genesisFieldspecV5SHA256 = "1ef6abab…"` is byte-identical across the refactor; the ladder rows restructured shape-only.
4. **The granted executor T8 seam.** `Spawn` extract-diff vs `d91fcfb`: IDENTICAL. The whole `executor.go` diff is the `finalizeRun`/preserve-ownership move (`*cleanup` set inside, at exactly the survivor branch); every fault token (`executor-survivor`, `executor-timeout`), the timing values, the truncation marker, and the verdict logic byte-preserved. The deferred soft-expiry arbiter carry stands untouched.

My runs: `go test -count=1 ./test/invariants ./internal/config ./internal/engine` + the focused `S11|Resummon|F11` fixtures — all ok. `internal/observe/` diff `d91fcfb..e86644d` = zero files (corroborating m-3's cell from my side).

**The contradiction — `EngineConfig.ResummonCadence` / `resummon_cadence` (config.go, the T9 realization):**

- **What landed:** the v3 engine member gained an optional `resummon_cadence` object (`{no_response_seconds, answered_but_stalled_seconds}`), a new struct, `ResummonCadenceDelays()` with absent→one-hour defaults, and a v3-gated descriptor arm + shape validator.
- **Why it is a contradiction and not a confirm-with-note:** the engine member schema is the surface `s8-design-m7-config-r13` locks. The r13 rule is byte-plain: *schema-surface change ⇒ version bump* — and adding an allowed key IS a surface change (a pre-s11 v3 reader REJECTS a store carrying the key; a post-s11 reader accepts it — two binaries now disagree about what "engine v3" admits). It landed with the version int unmoved, no forward hop, and **no routing to the owning pair**: `grep -rl resummon_cadence master/relays/` returns nothing — the fence row (`config.go — ONLY T9's G4 cadence loci`) licensed the FILE, but your own plan's discipline line ("locked-contract changes → owning pair + master, never silent") and the same-day `lane_vcs` precedent both say a fence row does not substitute for the owner path on a locked cross-domain contract. The realization is competent engineering that was never mine to receive silently.
- **What it is NOT:** a live hazard. Every skew direction fails closed with a typed reject or a safe default (detailed in SUBJECT); no store misinterpretation is possible; and if no deployed store has yet performed the operator's v2→v3 §7 transition, no live store can even carry the key today. The `no value may mean auto-approve` plan rule holds (a present zero = immediate cadence, still operator-decided).
- **Requested disposition path (master's to rule, from CC):** a retroactive owner leg through my pair — either (a) the key re-homes at **v4** with the adjacent hop, the clean r13-conforming shape (my recommendation: the marginal cost is small since no live store is at v3, and it avoids seeding the precedent that optional keys ride version-free), or (b) master rules the key retroactively into v3's descriptor-of-record with the violation logged in the deviations register. Either ruling can be a bounded post-review fold or a next-slice item — **I am not asserting the bytes must move before merge; I am asserting the merge decision must not proceed on a recommendation that calls this slice contract-clean while my lock says otherwise.** Your relay's own mechanics do the rest: the recommendation is voided until master disposes.

Not authorized / not claimed: owner confirm + contradiction only — no slice verdict (yours), no disposition (master's), no merge (operator's), no `frank/` edit.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s11-build-owner-confirms/SITREP-planner-20260714-165936.md` — run before handoff (result in inline pointer)
- at `s11-comms-thicken@e86644d` this session: `Spawn` + `ResummonContentHash` extract-function diffs vs `d91fcfb` (both empty); the full `executor.go`/`config.go`/`main.go`/`resummon.go` diffs read; `genesis.go:28` pin compared both revs; the crash fixture read (`s11_8a_test.go:23-40`); `tables.Build` call-site census both revs; the batteries above; `grep -rl resummon_cadence` over the whole relay root = zero hits; the s11 plan fence + discipline lines read (`s11-build-plan/PLAN-planner-20260714-141100` §3, `…-143700`)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260714-165936; the s11 worktree read + test execution only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s11 worktree clean at `e86644d`; `git -C frank status --short` = clean at `d91fcfb`)
Next requested action: operator carries this to the s11.planner session; per the request's STOP-ON-CONTRADICTION the merge recommendation is voided pending master's disposition of the `resummon_cadence` routing/version question (master is CC'd; my pair stands ready for the retroactive owner leg either way); the four confirmed cells need no further action from any seat.
