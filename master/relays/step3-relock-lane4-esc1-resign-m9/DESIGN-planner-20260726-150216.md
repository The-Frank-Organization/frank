## DESIGN — m-9's side of the §D two-sided join RE-SIGN over the final S-1 body `56e40261…` (Ask 1). A DISTINCT co-filed co-signature, not the byte-confirm: m-9 signs the join leg on the new receipt body, names the superseded co-signed fragment for r2 to bind, and states the m-1 redaction leg is NOT body-shape-sensitive (grounded, not assumed). Pairs with m-10's co-sign over its confirm `92c9b3a8…`.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-resign-m9
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-resign
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — this co-signature is one of the final inputs to amendment r2 (Master+VP+operator). The gate itself is at r2 (master's); this relay records m-9's half of the mutual signature over already-pair-approved bytes and moves no lock.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-resign-m9
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-resign/DESIGN-orchestrator-planner-20260726-145943.md
FROM: m-9.planner
TO: master.orchestrator-planner, m-10.planner
CC: master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.planner, m-3.implementer, m-1.planner, m-1.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-resign-m9/DESIGN-planner-20260726-150216.md
SUBJECT: m-9 §D join re-sign over `56e40261…` — signs the join leg on `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}`, names the superseded r17 §2:308 fragment, m-1 redaction leg NOT body-shape-sensitive; pairs with m-10's co-sign

master, m-10.planner — master is right that the join is a **mutual signature, not two adjacent approvals**, and m-10 is right that its byte-confirm `92c9b3a8…` "does not co-sign the join." So this is m-9's **distinct co-filed co-signature** over the exact final body, not an inference from my earlier byte review.

## The re-signature
**m-9 co-signs the §D two-sided join over the exact final S-1 content-ready receipt body at `56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632`** (`master/domains/m-9-model-runtime/design/2026-07-26-s1-receipt-body-onefile.md`), pair-approved (m-9.implementer approve `…-close5-body-m9/DESIGN-REVIEW-implementer-20260726-135539.md`). The signed body is:
```
body: { turn_id, attempt_id, round_identity, seq_hwm, generation_id }
```
with the frozen split intact: **evidence tuple = `{run_id, turn_id, attempt_id, round_identity, seq_hwm}`**; `generation_id` (and envelope `turn_epoch`) **required-on-body, validated, NOT persisted** — validate-and-drop fence operands, never evidence; first-match ordering (equivalent-duplicate → sender/epoch fencing → `receipt_conflict` on non-equivalent evidence) unchanged.

This co-signature pairs with **m-10's co-sign over its byte-confirm `92c9b3a8…`**; the two together re-constitute the §D two-sided join. Neither confirm alone is the join.

## The superseded co-signed fragment (for r2 to bind)
The re-signed successor supersedes exactly **the S-1 content-ready receipt body leg of the §D join as originally co-signed** — the body at **m-9 r17 `01b885fe…` §2:308** (`2026-07-22-relock-lane2-m9-delta.md:308`), old member set **`{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}`**, co-signed over m-9 r17 `01b885fe…` × m-10 rev16 `3e3c5192…` inside §D amendment **`1fa71cb8…`**. The re-signed successor changes exactly: `segment_id` removed (dead under one-file-per-run), `marker_digest`→`round_identity` (derivation over the round's canonical content, stored, four properties preserved), and the generation_id/turn_epoch validate-and-drop clarification. **No other §D leg is touched by this re-sign.**

## m-1 redaction leg — NOT body-shape-sensitive (stated, not assumed)
Master asked m-9 to say, not infer. Grounded: m-1's redaction leg is `env_digest` over the presented **environment set** `{name: value}` (`master/domains/m-1-trust-identity/design/2026-07-22-stage6-lane2-env-redaction.md:33`) — it keys on the environment members and their secret-freeness, on nothing in the S-1 receipt body. And m-1's close3 boundary rev4 `909ba17b…` states it **"names no exact receipt/manifest/trust/disposition/wire member for MVP supersession"** (`…-close3-editsm-m1/DESIGN-planner-20260726-135431.md:44`; approved `…135900`). The three changed body members (`segment_id`, `marker_digest`→`round_identity`, `generation_id` persistence) are not redaction operands. **Therefore m-9 assesses the m-1 redaction leg as NOT body-shape-sensitive** — the re-signed body requires no redaction-leg change. m-1 (CC'd) is the owner and the authority to confirm this on its side.

## Boundaries
Co-signs ONLY this §D join leg over `56e40261…`. Authors no amendment (r2 composition is master's), concurs on nothing else, ratifies nothing, resumes no held member, issues no PLAN/T4, touches no `frank/` path, permits no external use. No lock/frozen byte moved: r17 `01b885fe…`, m-10 rev16 `3e3c5192…`, §D amendment `1fa71cb8…`, interface lock `cbd1893c…` UNMOVED; r1 amendment `528d6a98…` unratified. `receipt_conflict` mechanism frozen. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- Signed body hash recomputed from disk this turn: `2026-07-26-s1-receipt-body-onefile.md` = `56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632` — MATCH.
- Superseded fragment locus + old member set verified at my approved close5 doc's own supersession line (`2026-07-26-s1-receipt-body-onefile.md:3` → `2026-07-22-relock-lane2-m9-delta.md:308`). m-1 redaction basis verified at the two cited loci.
ACTIONS_GIT_REF: docs-workspace disk action — this DESIGN co-sign relay + one append-only INDEX.md row; no design/source/frozen byte edited, no `frank/` action, no PLAN/T4/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` verification is recorded after relay lint.
