## DESIGN — m-3.planner CQ-5 CO-SIGN (observe-pipeline half): the post-form/lineage · PRE-observe-hook · atomic-bind `slot_in` ordering matches — and is REQUIRED by — locked m-3 §5.1/§2. No correction.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-slotin
PARENT_DISPATCH_ID: c4-cq-coord
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded single-CQ observe-pipeline co-sign against a locked contract; addressed to my pair implementer for the operator-directed adversarial review
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-5.planner, m-5.implementer, m-7.planner, m-7.implementer, operator
IN_REPLY_TO: c4-cq-slotin/SITREP-orchestrator-planner-20260702-024102.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: CQ-5 co-sign (observe-pipeline half) — done-predicate reads the just-classified in-courier `slot_in`, both binding atomically, matches locked m-3 §5.1/§2; pins my open §13 PLAN-detail; m-3.implementer adversarial review requested (closure-gate leg per c4-cq-coord/…-013323)

m-3.implementer — this is the CQ-5 **observe-pipeline-ordering co-sign** the orchestrator flagged as the one missing CQ-5 leg (`c4-cq-slotin/SITREP-orchestrator-planner-20260702-024102`). m-5.planner answered the classification half (`c4-cq-slotin/DESIGN-planner-20260702-014506`, m-5.implementer approved `…-020448`); this is the m-3-owned half. Per the closure gate (`c4-cq-coord/…-013323:32-38`): m-3 co-signs → you adversarially review → the orchestrator folds. Respond read-only `PHASE: DESIGN-REVIEW`, parent to this relay, same `DESIGN_DOC_ID`, return `DESIGN_REVIEW_VERDICT`.

## The ordering as posed (m-7 §3 / m-5 CQ-5 answer)
Classify `slot_in` at work-record acceptance, inside the single-threaded commit loop, **POST-form/lineage-gate, PRE-observe-hook**, so the Step-2 observe done-predicate — keyed on `slot_in` (m-3 §5.1) — reads the just-classified in-courier value, and **classification + observation bind into one atomic commit**.

## Verified against locked m-3 contract (E1) — CONFIRM, not just consistent but REQUIRED
1. **PRE-observe-hook is REQUIRED by m-3 §5/§5.1.** The Step-2 done-predicate hook **keys on `slot_in`** (§5.1: "the done-predicate reads the conductor-classified `slot_in`"). A predicate keyed on `slot_in` cannot evaluate until `slot_in` exists ⇒ classification **must** precede the observe hook. The inverse (classify after observe) is impossible for my gate — it would have nothing to key on. ✓✓
2. **The observe hook READS `slot_in` but does NOT WRITE it — so a distinct pre-observe conductor step must set it.** My §3.1 write-allowlist is closed and **excludes `slot_in`** (the hook writes only the observed/computed set + veto). Since the observe hook is not the writer of `slot_in`, `slot_in` must be classified by a separate conductor step upstream of the hook — exactly m-7's post-form/lineage, pre-observe placement. ✓✓
3. **Atomic classify+observe bind = my §2 TOCTOU closure.** §2: "running observe *inside* the atomic `submit()` closes the check→edit→send-stale TOCTOU — the observation that passed is the observation stamped." An atomic classify-then-observe commit means the lane can never interpose between classification and observation, so it cannot re-tag to dodge the `slot_in`-selected invariant. The atomic bind is the mechanism that makes "the observer selects the invariant" tamper-proof from the m-3 side (mirrors m-5 §4 from the classification side). ✓
4. **POST-form/lineage-gate** — consistent with my §2 fixed pre-flight order (m-2 form-validation first, then m-3 observe); classifying an already-admitted (m-2-valid) record before the observe pre-flight raises no m-3 objection. ✓
5. **per-record, immutable-at-acceptance, non-lane-writable** — the done-predicate reads a conductor-owned, non-lane-writable tag; preserved. ✓

## What this pins (a realization, NOT a reopen)
My §5.1/§13 previously left the exact pipeline point as "a PLAN detail (submit pre-flight vs terminal-outcome commit)." CQ-5 **pins** it: post-form/lineage, pre-observe, atomic bind. I folded the pin into **§5.1** (line ~101) and **§13** (line ~200) + a **§15 fold-log** entry, citing `c4-cq-slotin`. This **applies** my locked §5.1/§2/§3.1 — no m-3 contract change beyond recording the pin; the immutability-after-acceptance and non-lane-writability *enforcement* stay PLAN details. R2/C2.4 preserved (`slot_in` selects an observe-invariant, never keys a model or an m-2 `required_when`/`visible_when` predicate).

## Scope of my co-sign (honest boundary)
I co-sign the **m-3-owned observe-pipeline half** only. The `slot_in` **classification** half (conductor-classified-at-acceptance, immutable, non-lane-writable) is m-5's — already answered + m-5.implementer-approved; I do not re-adjudicate it. The two halves are complementary and clean.

## Adversarial targets for your review
(a) Did I overclaim "REQUIRED"? Is there any ordering where a `slot_in`-keyed done-predicate evaluates correctly **without** pre-observe classification (e.g. classify-within-the-observe-step)? My position: any such variant still sets `slot_in` before the predicate portion and collapses to the same atomic commit, but the **separated** step is cleaner because §3.1 excludes `slot_in` from the hook's writes — confirm that reasoning holds.
(b) Does the atomic classify+observe bind conflict with any per-record re-classification path (long-lived seat, successive records)? My read: no — each record is classified once at its own acceptance in its own commit.
(c) Is `closed`/co-signed (no correction) the right mapping, or does the §5.1/§13 pin wording need a tighten?

## CQ-status
**CQ-5 (m-3 observe-pipeline half): co-signed — no correction.** Composite CQ-5 closure requires (per `…-013323:32-38`): m-5 lead answer ✓ + m-5.implementer review ✓ + **this m-3 co-sign** + m-3.implementer review (pending) → orchestrator fold. I claim only the m-3 co-sign; I do not infer the fold.

Bounded co-sign only — no c2/c3 reopen, no new archetype/tag-space design, no m-7 design-LOCK by implication, no PLAN/IMPL/`pcode`/spike, no m-5 half proxied.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd (/Users/jack/Programming/harness) is a non-git docs workspace. Artifacts this turn: design-doc pin fold (§5.1, §13, §15) + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
