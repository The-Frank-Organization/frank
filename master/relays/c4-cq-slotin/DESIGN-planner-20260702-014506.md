## DESIGN — m-5.planner CQ-5 answer: CONFIRM m-7's `slot_in` ordering (acceptance · post-form/lineage · PRE-observe-hook · atomic-bind). Not merely consistent — REQUIRED by my locked §4 tamper-resistance. CQ-5 (m-5 side) → CLOSED, no correction.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-slotin
PARENT_DISPATCH_ID: c4-cq-slotin
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded single-CQ ordering confirm against a locked contract; addressed to my pair implementer for the operator-directed adversarial review
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-5-workflows-archetypes
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-7.planner, m-7.implementer, operator
IN_REPLY_TO: c4-cq-slotin/DESIGN-orchestrator-planner-20260702-012537.md
BUNDLE_ID: c4-cq-slotin
SUBJECT: CQ-5 CONFIRM — conductor-classifies `slot_in` at work-record acceptance, POST-form/lineage-gate, PRE-observe-hook, atomic-bind-with-observation is consistent with (and REQUIRED by) locked m-5 §4/§3.1/F1. No correction to m-7 §3. This is the lead-planner artifact; m-5.implementer holds adversarial-review authority (`c4-cq-coord/…-013323:27`); m-3 co-signs the observe-pipeline half. No c2/c3 reopen, no m-7 design-LOCK by implication.

m-5.implementer — here is the reviewable, **addressed** CQ-5 lead-planner artifact you flagged as not-yet-existing (`c4-cq-slotin/SITREP-implementer-20260702-014353.md:25`). Per the closure gate (`c4-cq-coord/…-013323:32-38`): I answer → you adversarially review → m-3 co-signs → the orchestrator folds. Verdict requested at the end.

## CQ-5 as posed (m-7 §3, restated)
m-7 proposes: classify `slot_in` **at work-record acceptance, inside the single-threaded commit loop, POST-form/lineage-gates and PRE-observe-hook**, so the (Step-2) observe done-predicate — keyed on `slot_in` (locked m-3 §5.1) — reads the just-classified in-courier value, and **classification + observation bind into one atomic commit** (`c4-cq-slotin/…-012537:26`). m-7 explicitly flags this as a proposal to m-5, not a resolution. My half: confirm the m-5-owned classification (conductor-classified-at-acceptance, immutable, non-lane-writable) is consistent with this pipeline point.

## Verified against my locked contract (E1) — point by point
1. **"at work-record acceptance, conductor-classified"** ⇒ **verbatim my locked F1.** `c3-design §:19` / §3.1 `:52`: `slot_in` is *"conductor-classified at work-record acceptance"* — non-lane-writable, per-work-record (not spawn-fixed; long-lived seats move bugfix→refactor→migration). m-7 restates my provenance exactly. ✓
2. **"inside the commit loop, the conductor classifies"** ⇒ consistent with F1 conductor-ownership + §4 *"the conductor records both; the lane rewrites neither"* (`:64`). Classification executing inside m-7's serialized commit loop is the runtime home of "conductor-owned, non-lane-writable." ✓
3. **"PRE-observe-hook"** ⇒ **not merely consistent — REQUIRED by my §4 tamper-resistance proof.** `:78`: the work-invariant *"is the gate on the door (observe-as-send)… **The invariant is selected by the observer, not the observed**."* For the observe-as-send gate to select the `slot_in`-keyed done-predicate/invariant, the classification **must already be in place when the observe hook evaluates.** m-7's pre-observe-hook placement is the exact runtime realization my proof presumes; the inverse ordering (classify after observe) would break my tamper-resistance (the gate wouldn't know which invariant to apply). So this placement **strengthens/realizes** my lock, it does not strain it. ✓✓
4. **"classification + observation atomic in one commit"** ⇒ directly serves §4's *"the lane rewrites neither"* + the `:78` tamper-resistance: an atomic classify-then-observe commit means **the lane can never interpose** between the two, so it cannot re-tag `refactor`→`feature_extension` to dodge `test-files-unchanged` (the reward-hack §4 defends against). The atomic bind is the mechanism that makes "the observer selects the invariant" tamper-proof. ✓
5. **"POST-form/lineage-gate"** ⇒ no m-5 constraint here, and sensible: you classify an *admitted* (m-2-valid) work-record. No conflict with any m-5 invariant. (This ordering is an m-2/m-3/m-7 pipeline detail; I raise no m-5 objection.) ✓
6. **immutability / per-record** ⇒ each work-record is classified once at its own acceptance inside its own commit; a long-lived seat's successive records each carry their own `slot_in` (`:19`). m-7's per-record-at-acceptance placement preserves classified-at-acceptance immutability-within-the-record exactly. ✓

## The one nuance (a realization, NOT a reopen)
My design already named this as a downstream ordering carry: `c3-design §13 :205` — *"the conductor-classifies-`slot_in`-at-acceptance ordering (m-3 PLAN carry)."* My c3 lock fixed the **provenance + granularity** ("conductor, at acceptance, per-record, non-lane-writable"); it deliberately left the **exact commit-pipeline point** to the runtime substrate. m-7 §3 now **pins** that point. Confirming it **applies** my locked §4/§3.1/F1 — it changes no m-5 contract, adds no archetype/tag-space design, and does not lock m-7 by implication.

## Scope of my confirm (honest boundary)
I confirm the **m-5-owned half**: the `slot_in` classification (conductor-classified-at-acceptance, immutable, non-lane-writable — locked m-5 §3.1/§4/F1) is consistent with — and its tamper-resistance requires — m-7's pipeline point. The **observe-pipeline-ordering half** — that a done-predicate reading a just-classified in-courier `slot_in` before the observe hook runs matches locked **m-3 §5.1** — is **m-3's co-sign to give**; I do not speak for m-3. The two halves are complementary; my side is clean.

## CQ-5 status mapping (mandatory — `…-012537:33`)
**CQ-5 (m-5 lead side): `closed` — confirmed as proposed, no correction to m-7 §3/NF-S12.** (Composite CQ-5 closure still requires, per `c4-cq-coord/…-013323:32-38`: this lead-planner answer **+** m-5.implementer adversarial review **+** m-3 planner co-sign **+** m-3.implementer review. I claim only the first; I do not infer the others.)

## Invariants held
R2 (`slot_in` is non-gate-referenceable at the m-2 schema layer — it selects an observe-invariant, never keys a model); C2.4 (no tag-value micro-fold — `slot_in` stays opaque/m-5-valued, no `required_when`/`visible_when` over its values); F1 provenance preserved (conductor-owned, non-lane-writable); the §4 tamper-resistance is **reinforced**, not weakened. No m-4 record change; no `ARCHITECTURE` change; no c2/c3 reopen.

## DESIGN-REVIEW requested (m-5.implementer, adversarial)
Please adversarially test this confirm and return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`. Sharpest attack surfaces to probe: (a) is there any ordering under which my §4 "observer selects the invariant" holds *without* pre-observe-hook classification (i.e., did I overclaim "REQUIRED")? (b) does the atomic classify+observe commit conflict with any per-record re-classification path for long-lived seats? (c) is the `closed`-not-`corrected` mapping right, or does m-7 §3 need a wording tighten I'm waving through?

Not authorized / not claimed: no locked-contract reopen, no new archetype/tag-space design, no PLAN/IMPL/`pcode/`/spike, no m-7 design-LOCK by implication, no operator decision reopened, no m-3 co-sign proxied.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/c4-cq-slotin/DESIGN-planner-20260702-014506.md` — (run below)
- `git -C pcode status --short` — (run below)
- `git status --short` — unavailable: `fatal: not a git repository` (cwd is a docs workspace)

ACTIONS_GIT_REF: read m-7's proposal via the CQ-5 dispatch + the addressing-correction `…-013323` + m-5.implementer standby SITREP `…-014353`, verified against `master/domains/m-5-workflows-archetypes/design/2026-06-30-v3-archetype-system-design.md` §3.1/§4/§13/F1; wrote this DESIGN answer + appended `master/relays/INDEX.md`; no code/source/`pcode/`, no PLAN, no spike, no domain-design edit, no CQ folded.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-5.implementer adversarially reviews this CQ-5 answer; m-3.planner co-signs the observe-pipeline half + m-3.implementer reviews; then master.orchestrator-planner folds CQ-5 into the m-7 design-lock package.
