## RECONCILE — master's scoping of the operator-raised loud-failure question: your split is RATIFIED as proposed — the app-local loud alert is the MVP answer (yours, folded, correct); the governed error-relay-to-operator is a NAMED STEP-4 CARRY (recorded as T11 in the hardening backlog, m-6-composed, with the carriage-when-dead question preserved as its design heart); ONE narrow MVP addition: the failure fact must already land in the DURABLE E0 record under the EXISTING schema — verify `phase=failed` coverage at your terminal classes, no m-3 delta

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-stage5-m10
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a scoping return on an operator-raised question the operator already holds; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — severable from the running stage-5 grill, as you filed it
DESIGN_DOC_ID: step3-mvp-design-m10-control-plane
IN_REPLY_TO: master/relays/step3-mvp-stage5-m10/RECONCILE-planner-20260720-051500.md
FROM: master.orchestrator-planner
TO: m-10.planner
CC: m-10.implementer, m-3.planner, m-9.planner, m-6.planner, master.orchestrator-reviewer, operator
SUBJECT: the disposition, all three parts — (1) MVP: your app-local persistent-alert + non-zero-exit fold stands; the operator IS the app user in the MVP, the loud surface reaches them; (2) Step-4: the courier error relay = T11, composing with m-6's gate→bucket/ODB machinery (a run-terminal failure is exactly an Owner-Decision-Brief-class event) — and the hard question you named is preserved verbatim as T11's design heart: the failure that kills the worker kills the CARRIER, so the relay half needs either a control-plane-held reporter credential (an m-1/F60 question, NOT settled now) or an accepted may-not-arrive semantics + polling fallback; (3) the one MVP check: your census rows + E0 flows must show a run-terminal failure is already VISIBLE IN THE DURABLE RECORD under m-3 r4's existing enum — `phase=failed` on the last attempt + your FAILED run state — so the Step-4 relay upgrades DELIVERY of a recorded fact, never records a new one; if any terminal class produces NO durable E0-visible trace today, say so in your design (a residual row, not a schema delta)

m-10 — proceed exactly on your split; the one addition is the §3 verification above, which is a statement in your working draft, not new machinery. G-4 onward.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row + the T11 append to `master/FRANK-HARDENING-BACKLOG.md`; no closed design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10 continues the grill (G-4+) with the §3 verification folded; T11 rides to Step-4 kickoff; no m-3/m-9 act owed now.
