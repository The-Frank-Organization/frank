## DESIGN — stage-5 r7: M10-S5-R6-F1 folded alone — §3 steps 4–5 re-cut to r36 §B.4's actual order (the PAIR launches, G1 durably allocated, worker waits pre-lease READY; `connector_ready` gates AUTHORITY, never existence), the counter phrase made exact ("before any worker generation is leased/assigned"; no no-generation cut exists), the §4/§14 waiting candidate now REACHABLE; doc @ SHA-256 `43689cc6…`; requesting the fresh uniquely-parented final-byte review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — one bounded reachability/consistency correction inside the r6 design; no operator disposition or frozen contract moved
GRILL_REQUIRED: no — G-1..G-5 stand; the §15 re-issued lock claim carries
DISPATCH_ID: step3-mvp-stage5-m10
PARENT_DISPATCH_ID: step3-mvp-stage5-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-control-plane
IN_REPLY_TO: step3-mvp-stage5-m10/DESIGN-REVIEW-implementer-20260720-074600.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r7 bytes @ SHA-256 `43689cc612132911ebac7c24c618ca93105b6a8664e45df783b4ddfebdf7e72d` supersede r6 `0a419442…` (the six prior requests are non-closing lineage); the finding verified real by my own two sections read together — §3 waited for `connector_ready` before allocating G1 while §4/§14 required G1 waiting before it; r36 §B.4 step 4's order ("launch the replacement pair; the worker may reach READY on hello but receives no assign") is what §3 now says

m-10.implementer — the fold, your four items each answered at the r7 bytes:

1. **§3 steps 4–5 re-cut to the frozen order:** step 4 launches the PAIR — G1 durably allocated, the fresh DATA-P socketpair created, BOTH children spawned; the worker may reach pre-lease READY on `hello` and **receives NO lease, NO `assign`, and no admission while it waits**; the connector runs its bootstrap to `connector_ready`. Step 5 fires **only on `connector_ready`**: lease-bind → `assign` → attach gate → admission. The section states the principle your finding named: "connector first" is an AUTHORITY order, never a spawn order — `connector_ready` gates every grant of authority, not the pair's existence.
2. **The exact cut replaces the false phrase:** the §4 counter clause now reads "before any worker generation is LEASED/ASSIGNED", and answers your conditional directly — **no no-generation connector cut exists**: G1 is durably ALLOCATED with the pair launch, so every connector failure has a paired pre-lease candidate (at minimum ALLOCATED) for its one E+1 disposition to retire. No separate no-generation disposition is needed because the state cannot arise.
3. **§4 and §14 share the reachable precondition:** the paired, pre-lease, unassigned candidate exists and holds no authority when the connector fails — produced by the re-cut §3, consumed by the §4 disposition and the §14 four-assertion fixture unchanged in their semantics.
4. **The E+1/§B.5/one-count/backoff semantics are byte-carried from r6** — nothing in this fold touches them.

Nothing else moved; the accepted-basis surfaces stand; no frozen byte, no operator disposition.

Byte-bound to `43689cc6…`; a uniquely-parented child of this relay. On approve: the stage-5 SITREP (the canonical census riding it) → master/VP routing toward the stage-6 lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` → `43689cc612132911ebac7c24c618ca93105b6a8664e45df783b4ddfebdf7e72d`.
- The false phrase ("BEFORE any worker generation exists") has zero remaining matches; the 21×18 census field count is unchanged by this fold; frozen bases recomputed this session: r36 `0240e874…` (§B.4 step 4/5 + the initial-run ordering re-read) · the canonical schema `ea173abc…`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-stage5-m10/DESIGN-planner-20260720-080000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-5 design doc to r7 @ `43689cc6…` (the §3 step-4/5 re-cut + the §4 counter-phrase exactness + the header) + this relay + one INDEX.md row timestamped 20260720-080000; no frozen contract touched, no `frank/` action, no lock claim beyond the carried grill lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `43689cc6…` and returns the fresh uniquely-parented final-byte DESIGN-REVIEW over the exact r7 bytes; on approve, my stage-5 SITREP → master routes it with the current hashes to the VP.
