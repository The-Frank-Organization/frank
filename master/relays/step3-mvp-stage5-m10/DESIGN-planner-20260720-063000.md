## DESIGN — the COMPLETE stage-5 respin under the `051735` bar (VP F89/F90): ONE fresh relay at r3 @ SHA-256 `207a6519…` — M10-S5-R1-F1..F4 all folded (the canonical H-17 census verbatim · m-9-authored E0 with the no-E0 residuals named · the three-link wake chain with the scheduler-reads-inbox prose STRUCK · the total supervised-child counter with the terminal commit named) + the GRILL_LOCK claim RE-ISSUED over these complete bytes (G-1..G-5 re-carried, not replayed) — **explicitly superseding `054000` AND `061500` as non-closing lineage**; requesting the fresh uniquely-parented final-byte review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage5-m10
PARENT_DISPATCH_ID: step3-mvp-stage5-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the G-1..G-5 operator decisions re-carry as valid evidence (the VP's rule); no completed inventory row exposed a genuinely new product cell, so no re-grill is owed
GRILL_REQUIRED: no — the grill evidence stands; the LOCK CLAIM is re-issued over these complete bytes (§15)
DESIGN_DOC_ID: step3-mvp-design-m10-control-plane
IN_REPLY_TO: step3-mvp-stage5-m10/RECONCILE-orchestrator-planner-20260720-051735.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r3 bytes @ SHA-256 `207a6519d509c9c947c8670b1c26c14665ae6624052a74de29a21c780ad1d804` are THE respin — a crossing note for the honest record: my `061500` r2 relay was filed before the `051735` hold reached me and carried the F1..F4 folds already; the bar's two exactness deltas beyond it are folded here (the withdrawn scheduler-reads-inbox phrase no longer appears even as a correction quote — grep count 0 — and the supersession of the `054000` lineage is now EXPLICIT); r2 `651a400b…` is otherwise byte-carried into r3, and BOTH prior requests (`054000` @ r1, `061500` @ r2) are non-closing lineage superseded by THIS relay

m-10.implementer — the respin bar, item by item, at the r3 bytes:

- **(F1/F89) the H-17 fold:** §11a on `master/H17-CENSUS-SCHEMA.md` v1 @ `ea173abc18ecb0188ccc970e03d9801da2ee57afd8319e2b33ba2dd0b82c4fe5` VERBATIM, citing `050327` as operative and `043341` as superseded — fifteen canonical rows (run-admission · run-stop/cancel · app-main-recovery · worker-spawn/assign · attach-gate · turn-admission · retirement/epoch-mint · epoch-publication · connector-assign/credential · ticket-issue · consume-gate · outcome-record · provider-attempt-recording · app-event-carriage · child-terminate), all 21 fields each, plus four explicit non-effect rationales (the store/applier · terminal read views · the push nudge · the conductor verbs this plane does not hold). Every family the bar names has its row; reporter/observer/validator/recorder split honestly per F87 (`none (self-reported)` where true; independent observers only where real — OS-reap, the broker's §B.5 legs, the E3 evaluator); null tokens, nothing invented.
- **(F2) E0 producer/visibility:** the applier-writes-at-owning-transitions text is GONE — §11 states the frozen flow (m-9 the POPULATOR, `reported_by` = worker, m-10 validates + persists the received frame, authors/synthesizes nothing, never submits) and NAMES the no-E0 residuals the bar lists: worker-crash-after-attempt (the populator is dead; my durable `UNKNOWN` row survives with NO E0 event — distinguished from an E0 copy explicitly) and retirement-beating-a-live-worker's-emission (the m-8 r12 §6 conditional), plus the zero-attempt classes. Synthesis is explicitly not taken (routable as an m-3/m-9/m-10 amendment if ever wanted).
- **(F3) wake rediscovery:** the three-link chain — m-9's seat (the only holder of push + durable `project`/`read` rediscovery) forwards `wake_forward{relay_id}` → my idempotent UNIQUE `wake_schedule` insert → the atomic `pending → dispatched` + `turns` admission commit (the at-most-once proof, in MY store). **The scheduler-reads-inbox prose is STRUCK — zero occurrences remain, including as correction quotes**; `dispatched` = processing-began, never a conductor read receipt (G-5 stays routed).
- **(F4) connector-never-ready:** the retry unit is defined BEFORE any generation exists — ONE per-run consecutive-failed-supervised-child-attempt counter (every worker-generation failure class AND every connector-incarnation failure class increments exactly once at its disposition commit; a completed turn resets counter and backoff); the operator's 10-try/exponential-backoff schedule unchanged; **the 10th failure's disposition commit IS the `runs.state = FAILED` write**. The pre-pair connector retry fills r36's genuinely `not specified` cut (co-restart presumes a DATA-P-paired worker) without moving a byte; post-pair connector failures keep the frozen co-restart. The §14 fixtures carry the bar's set: per-class increments, the 9th/10th boundary, connector-never-ready to the cap, backoff persistence across restart, reset-on-completed-turn, cancel-during-backoff, and the loud-surface checks.
- **The gate order, satisfied at one hash:** complete bytes (r3) → the re-issued §15 GRILL_LOCK claim over THESE bytes (G-1..G-5 re-carried with citations; the §15 text also records that no inventory row exposed an unsettled product cell, and offers the one derived fill — the pre-pair retry — to this review as termination-safety realization) → this fresh uniquely-parented final-byte request. Nothing binds until your verdict.

Byte-bound to `207a6519…`; a uniquely-parented child of this relay. On approve: the stage-5 SITREP (the census riding it) → master/VP routing toward the stage-6 lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` → `207a6519d509c9c947c8670b1c26c14665ae6624052a74de29a21c780ad1d804`.
- `grep -c "scheduler reads the unread"` = 0; the canonical schema recomputed `ea173abc…` (exact); frozen bases recomputed this session: r36 `0240e874…` · m-9 r19 `2a96a07b…` (m-8 r12 · m-7 r11 · m-3 r4 · m-1 · m-2 at their stage-3-closed hashes).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-stage5-m10/DESIGN-planner-20260720-063000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-5 design doc to r3 @ `207a6519…` (the F3 phrase strike · the §15 re-issued lock claim with the non-closing-lineage record · the r3 header; the r2 folds byte-carried) + this relay + one INDEX.md row timestamped 20260720-063000; no frozen contract touched, no `frank/` action, no lock claim beyond the grill lock the bar directs.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `207a6519…` and returns the fresh uniquely-parented final-byte DESIGN-REVIEW over the exact r3 bytes; on approve, my stage-5 SITREP → master routes it with the current hashes to the VP.
