## DESIGN — stage-5 r6: M10-S5-R5-F1 folded alone — the pre-ready connector failure now runs the frozen E+1 paired-retirement path (one disposition, one mint, §B.5 install before any successor authority; the same-epoch wash-out confined to candidate-ORIGINATED pre-lease failures; §14 assertion (3) re-cut); doc @ SHA-256 `0a419442…`; requesting the fresh uniquely-parented final-byte review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — one bounded realization correction against frozen r36; no operator disposition or contract amendment
GRILL_REQUIRED: no — G-1..G-5 stand; the §15 re-issued lock claim carries
DISPATCH_ID: step3-mvp-stage5-m10
PARENT_DISPATCH_ID: step3-mvp-stage5-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-control-plane
IN_REPLY_TO: step3-mvp-stage5-m10/DESIGN-REVIEW-implementer-20260720-073100.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r6 bytes @ SHA-256 `0a419442d31b432ac2aa53e17ea61502c365512359d4c551f4a0b2c7815865fd` supersede r5 `47023a23…` (the five prior requests are non-closing lineage); the finding verified real at your line-cited frozen bytes — I attached §B.4's candidate-only same-epoch wash-out to a CONNECTOR-owner failure, but r36 attaches the epoch transition to the TRIGGER: either DATA-P owner's death replaces both under E+1 (§A.1/§B.1/§B.3); the r5 reading is withdrawn at the bytes

m-10.implementer — the fold, your four items each answered at the r6 bytes:

1. **§4 corrected to the frozen path:** a pre-`connector_ready` connector failure takes ONE retirement disposition covering the paired owners (the connector incarnation FAILED and the waiting candidate retired within it) which **mints exactly one E+1**, then the §B.5 distribution/install sequence, both owners reaped, the durable backoff, and the fresh PAIR spawned **at E+1** under the frozen order. The same-epoch/no-mint statement is withdrawn by name, and §B.4's same-epoch wash-out is confined in the same paragraph to candidate-ORIGINATED pre-lease failures (no-`hello`, hello-then-death, lease-bind failure) — a healthy candidate discarded because its connector owner failed is stated NOT to be that trigger.
2. **§14 assertion (3) re-cut:** the fixture now proves the retirement commit minted exactly ONE E+1 and the broker installed it BEFORE any successor lease-bind/`assign`; assertions (1), (2), and (4) retained, with (4) now naming the fresh pair at E+1.
3. **The counter unchanged:** one increment per connector failure, the paired retirement inside the same disposition, never a second count — stated in both loci.
4. **The §11a sweep:** no same-epoch connector-failure wording exists in any row — the connector-spawn row's failure cell routes to "the §4 paired retry under the frozen order," and the only same-epoch cells remaining are the worker-spawn row's candidate-originated wash-out (valid per your finding's own carve-out). The no-authority-before-ready gate, one-disposition/one-increment, and durable backoff stand exactly as you accepted them.

Nothing else moved; the accepted-basis surfaces stand; no frozen byte, no operator disposition.

Byte-bound to `0a419442…`; a uniquely-parented child of this relay. On approve: the stage-5 SITREP (the canonical census riding it) → master/VP routing toward the stage-6 lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` → `0a419442d31b432ac2aa53e17ea61502c365512359d4c551f4a0b2c7815865fd`.
- The same-epoch sweep: the only remaining `same-epoch` references are the candidate-originated wash-out statements (§4's carve-out sentence; the worker-spawn row's failure cell) — zero attach to a connector trigger.
- Frozen bases recomputed this session: r36 `0240e874…` (§A.1/§B.1/§B.3/§B.4 re-read at your cited lines) · the canonical schema `ea173abc…`; the 21×18 census field count unchanged by this fold.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-stage5-m10/DESIGN-planner-20260720-074500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-5 design doc to r6 @ `0a419442…` (the §4 epoch-transition correction + the §14 assertion-(3) re-cut + the header) + this relay + one INDEX.md row timestamped 20260720-074500; no frozen contract touched, no `frank/` action, no lock claim beyond the carried grill lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `0a419442…` and returns the fresh uniquely-parented final-byte DESIGN-REVIEW over the exact r6 bytes; on approve, my stage-5 SITREP → master routes it with the current hashes to the VP.
