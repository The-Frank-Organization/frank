## DESIGN — stage-5 r8: M10-S5-R7-F1 folded alone — the two live "pre-pair" clauses (§12 G-2 · the §14 fixture family) replaced with the reachable **paired pre-`connector_ready`/pre-lease** cut; whole-document sweep leaves only the two historical withdrawal records, named as history; every counter/lifecycle semantic byte-carried; doc @ SHA-256 `78876829…`; requesting the fresh uniquely-parented final-byte review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — one bounded vocabulary/fixture consistency correction; no operator disposition or frozen contract moved
GRILL_REQUIRED: no — G-1..G-5 stand; the §15 re-issued lock claim carries
DISPATCH_ID: step3-mvp-stage5-m10
PARENT_DISPATCH_ID: step3-mvp-stage5-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-control-plane
IN_REPLY_TO: step3-mvp-stage5-m10/DESIGN-REVIEW-implementer-20260720-080100.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r8 bytes @ SHA-256 `7887682998b12a3d1a9ea6cfb682ad47c70b577eef4660e2d36f30e579552baa` supersede r7 `43689cc6…` (the seven prior requests are non-closing lineage); the finding verified real — the §12/§14 clauses named a state §3/§4 abolish, leaving an implementer unable to tell whether to build an unreachable fixture; both now say the reachable cut in your suggested vocabulary

m-10.implementer — the fold, your four items each answered at the r8 bytes:

1. **Both live clauses replaced:** §12 G-2 now reads "incl. **paired pre-`connector_ready`/pre-lease connector failures** — the reachable cut (M10-S5-R7-F1)"; the §14 counter-family line likewise names "**paired pre-`connector_ready`/pre-lease** connector-never-ready". No implementer ambiguity remains: the fixture family is the reachable state §3 produces.
2. **Counter totality preserved:** every connector-failure class still increments exactly once and connector-never-ready still reaches the 10-try terminal — the clauses changed vocabulary, not semantics.
3. **§3/§4/§14 byte-carried:** the pair launch, no-authority-before-ready, one-disposition, exactly-one-E+1, broker-install-before-successor-authority, and durable-backoff semantics are untouched by this fold.
4. **The whole-document sweep:** `pre-pair` now greps at exactly two loci — the status header's revision history and the §15 lock's record of the r3 withdrawal — both historical records of a withdrawn reading, and the header now NAMES them as history; `no-generation` appears only inside the §4 sentence stating that no such cut exists. No equivalent wording survives anywhere else.

Nothing else moved; the accepted-basis surfaces stand; no frozen byte, no operator disposition.

Byte-bound to `78876829…`; a uniquely-parented child of this relay. On approve: the stage-5 SITREP (the canonical census riding it) → master/VP routing toward the stage-6 lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` → `7887682998b12a3d1a9ea6cfb682ad47c70b577eef4660e2d36f30e579552baa`.
- `grep -n "pre-pair"` → lines 3 and 172 only (the historical records); the 21×18 census field count unchanged; frozen bases recomputed this session: r36 `0240e874…` · the canonical schema `ea173abc…`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-stage5-m10/DESIGN-planner-20260720-081500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-5 design doc to r8 @ `78876829…` (the two clause replacements + the header) + this relay + one INDEX.md row timestamped 20260720-081500; no frozen contract touched, no `frank/` action, no lock claim beyond the carried grill lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `78876829…` and returns the fresh uniquely-parented final-byte DESIGN-REVIEW over the exact r8 bytes; on approve, my stage-5 SITREP → master routes it with the current hashes to the VP.
