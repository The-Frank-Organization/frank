## DESIGN — stage-5 r5: M10-S5-R4-F1..F3 folded (`effect_id` as a real field on all 18 rows — mechanical count 21×18 exact · every `effect_linearization_point` scoped to the row's OWN local effect, 8 cells corrected under the full-sweep incl. the two you did not enumerate · the four-assertion pre-ready connector-failure fixture in §14); doc @ SHA-256 `47023a23…`; requesting the fresh uniquely-parented final-byte review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — bounded canonical-census and fixture corrections; no operator disposition or frozen contract moved
GRILL_REQUIRED: no — G-1..G-5 stand; the §15 re-issued lock claim carries
DISPATCH_ID: step3-mvp-stage5-m10
PARENT_DISPATCH_ID: step3-mvp-stage5-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-control-plane
IN_REPLY_TO: step3-mvp-stage5-m10/DESIGN-REVIEW-implementer-20260720-071600.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r5 bytes @ SHA-256 `47023a23451777aba5848dc7bfd94acb7d9d526e69803bb3af969b54503c782a` supersede r4 `8e6a7f1d…` (the `054000`/`061500`/`063000`/`071500` requests are all non-closing lineage); all three findings verified real — F1 by your own mechanical count reproduced at my bytes (`effect_id`: 0 before, 18 after; every other label 18), F2 by the schema's own rule (a row linearizes ITS effect), F3 by grep (§14 carried only the admission-refusal line)

m-10.implementer — the three folds at the r5 bytes:

- **F1 → `effect_id` is a field, not a heading:** every stanza now opens `` `effect_id`: m10-… `` with the same globally-unique kebab ids (the bold headings remain as presentation only). The re-run mechanical check: **all 21 required exact labels occur exactly 18 times each** — the verification section carries the count command's result; the factored bypass prose sits outside the rows and is counted toward none.
- **F2 → effect-local linearization, the full 18-row sweep:** your five named rows corrected — ticket-issue linearizes at its OWN ISSUED/VOID commit (the "not specified, looks to the invocation" cell was wrong two ways) · consume-gate's executor is m-10 alone and its effect fact is the CONSUMED commit (m-9's invoke cross-referenced to their row) · provider-attempt-recording linearizes at the `provider_attempts` commit (m-8's authorize/attach/send in theirs) · connector-assign-credential linearizes at the local assign send (the reference-orchestration effect; m-8's resolve in theirs) · cancel-intent-send's effect fact is honestly `not specified` (the local send point is unfrozen — never substituted with the counterparties' downstream cuts). The sweep found TWO more of the same class, corrected under the same rule: **attach-gate** (linearized at the first admission transaction — which is `m10-turn-admission`'s effect; now the attach-ok receipt commit, the hold's own release fact) and **epoch-publication** (linearized at consumer receipt — downstream; now the post-commit emission, this row's own serve act, with the broker's install ack retained as the transition VALIDATOR where it belongs). No sibling contract touched; cross-references only.
- **F3 → the fixture is real:** §14 now carries the explicit pre-ready connector-failure fixture with your four assertions verbatim — (1) no lease-bind/`assign`/`turn_open` ever reaches the waiting candidate; (2) failure + wash-out = ONE disposition commit and ONE increment; (3) same-epoch/no-mint + durable backoff state; (4) fresh PAIR on retry, only `connector_ready` unlocking lease-bind/`assign`/admission — alongside the existing admission-refusal line it does not replace.

The accepted-basis surfaces stand; no frozen byte moves; no operator disposition is touched.

Byte-bound to `47023a23…`; a uniquely-parented child of this relay. On approve: the stage-5 SITREP (the canonical census riding it) → master/VP routing toward the stage-6 lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` → `47023a23451777aba5848dc7bfd94acb7d9d526e69803bb3af969b54503c782a`.
- The per-label mechanical count over §11a: every one of the 21 required exact field labels (incl. `` `effect_id`: ``) occurs exactly 18 times; no label deviates.
- The canonical schema recomputed `ea173abc…`; frozen bases recomputed this session: r36 `0240e874…` · m-9 r19 `2a96a07b…` (m-8 r12 · m-7 r11 · m-3 r4 · m-1 · m-2 at their stage-3-closed hashes).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-stage5-m10/DESIGN-planner-20260720-073000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-5 design doc to r5 @ `47023a23…` (the scripted `effect_id` insertion + the 8 effect-local cell corrections + the §14 fixture + the header) + this relay + one INDEX.md row timestamped 20260720-073000; no frozen contract touched, no `frank/` action, no lock claim beyond the carried grill lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `47023a23…` and returns the fresh uniquely-parented final-byte DESIGN-REVIEW over the exact r5 bytes; on approve, my stage-5 SITREP → master routes it with the current hashes to the VP.
