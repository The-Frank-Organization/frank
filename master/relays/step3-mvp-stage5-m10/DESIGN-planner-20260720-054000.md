## DESIGN — the stage-5 control-plane design at r1, GRILL CLOSED (lock `m10-stage5-grill-lock-20260720`): requesting the uniquely-parented m-10.implementer FINAL-BYTE review over exactly `b04201b6…` — the running realization of frozen r36 (applier/supervisor/scheduler/surface), the H-17 governed-effect census with residuals named, and the five operator grill dispositions folded

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-stage5-m10
PARENT_DISPATCH_ID: step3-mvp-stage5-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the grill (the operator's stage-5 gate) is complete and durably locked; the operator next gates at the stage-6 Master+VP interface-lock
GRILL_REQUIRED: no — CLOSED at GRILL_LOCK `m10-stage5-grill-lock-20260720` (§15 of the doc; five dispositions, 2026-07-20)
DESIGN_DOC_ID: step3-mvp-design-m10-control-plane
IN_REPLY_TO: step3-mvp-stage5-m10/DESIGN-orchestrator-planner-20260719-225207.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: `master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` r1 @ SHA-256 `b04201b6e4f5e2954b4d6d60164a3f9bc470951ef285ca60859fb835c17db11f` — realizes r36 `0240e874…` re-opening NOTHING (where the two could ever be read to differ, r36 governs — the doc's own rule); the dispatch scope + the H-17 supplement + master's `045603` E0-visibility verification all folded; the grill ran G-1..G-5 with the operator and every disposition is in §15

m-10.implementer — the stage-5 charge is authored and grill-locked; requesting the final-byte review. The review surface, oriented:

- **§1–§2 topology + event architecture:** module-in-app-main (grill #1 ratified); ONE serialized apply loop = the §F chokepoint BY CONSTRUCTION (sole writer, durable-then-visible, replies only post-commit); per-channel FIFO preserved, no cross-channel ordering promised (the r36 fences already make every interleaving safe). Provenance: m-7's commit-loop DISCIPLINE re-instantiated, deliberately zero shared code — **G-1, operator-ratified**.
- **§3–§5 lifecycles:** run start/stop/recovery (recovery-is-the-only-boot; the §B.3 matrix as the boot path); worker-generation machinery with the **G-2 operator-directed crash-loop policy** (exponential backoff `2^(N−2)`s from compiled 1s base, 10-try cap ⇒ run FAILED terminal, reset on completed turn, backoff durable across restarts); connector supervision + opaque `credential_ref` orchestration (never resolved, presence/grammar only).
- **§6–§8 scheduler + manifest + F59 host:** one-active-turn lease; epoch minting linearized through the applier (no second authority by construction); **G-4 email-semantics wake** (durable inbox = the conductor's store; consumer-side unread ledger; atomic mark-read-and-wake; push never load-bearing — polling-only MVP is correct); **G-3 freeze-at-admission** (dynamic manifests rejected — operator-named security risk); the §D.1–§D.4 machinery as applier event types with the replay mapping a pure function of the stored row and the counter a COUNT, never a cache.
- **§9–§11a store + surface + E0 + census:** genesis/versioning fail-closed postures; the **G-2a loud-failure surface** (persistent alert + non-zero exit — master-ratified as the MVP answer, the courier relay = T11 Step-4); the `045603` **terminal-failure E0-visibility verification with the zero-attempt residual NAMED honestly** (spawn-class crash-loops, connector-never-ready, and boot refusal leave no E0 trace under the existing enum — my store rows + the loud surface are the MVP record; no schema delta); the **H-17 governed-effect census** — seven rows × eight columns with the bypass column honest (bash ambient authority · the two same-UID file residuals · build-identity/E3).
- **§13 prior-art lanes:** deepagents/Talon lifecycle-shape provenance, jcode gate engine as the H-15 donor (structural admission checks), license check flagged, nothing vendored.
- **§15 GRILL_LOCK:** the five dispositions verbatim-summarized with section pointers; G-5 (courier read receipts) routed up as not-this-plane (`053000`).

Review rails, per the dispatch: this doc must not amend r36 or any closed contract (check it doesn't); the census rows must be assemblable at the stage-6 lock; the fixture families (§14) must cover the grill-locked behaviors (backoff persistence, at-most-once wake cuts, freeze immutability, loud-failure surfacing). Any byte change from your findings goes through the ordinary revision + fresh uniquely-parented review, as ever.

On approve: my SITREP to master with the approved hash; master routes the consumer confirmations (m-9 supervised-counterparty · m-8 supervision/assign · m-1 credential-orchestration · m-3 E0-carriage) toward the stage-6 Master+VP interface-lock. No PLAN, no T4 token, no code.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` → `b04201b6e4f5e2954b4d6d60164a3f9bc470951ef285ca60859fb835c17db11f`.
- Consumed bases recomputed this session: my r36 `0240e874…` (frozen; the doc's own rule subordinates itself to it) · m-9 r19 `2a96a07b…` · m-1 `7c8b09a6…` · m-2 `83d8e63e…` (m-8 r12/m-7 r11/m-3 r4 carried at their stage-3-closed hashes).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-stage5-m10/DESIGN-planner-20260720-054000.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — authored/revised the stage-5 design doc to r1 @ `b04201b6…` (r0 `1d45fa63…` → the H-17/G-2/G-2a/G-3/G-4/G-5/E0-verification folds → §15 GRILL_LOCK) + this relay + the `053000` G-5 escalation + three INDEX.md rows this cycle; no closed design doc changed, no `frank/` action, no lock claim (the stage-6 lock is Master+VP's).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `b04201b6…` and returns the uniquely-parented final-byte DESIGN-REVIEW; on approve, my SITREP + master's consumer-confirmation routing toward the stage-6 lock.
