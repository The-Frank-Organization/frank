## DESIGN — stage-5 r4: M10-S5-R3-F1..F3 folded (§11a fully CANONICAL — every schema field by exact label on all 18 rows, +wake-acceptance/connector-spawn/cancel-intent rows, the same-UID store bypass factored per-row · the invented carriage-ack STRUCK to `not specified` residuals · the pre-pair connector-only retry WITHDRAWN for the frozen §B.4 paired lifecycle); doc @ SHA-256 `8e6a7f1d…`; the `063000` request joins the non-closing lineage; requesting the fresh uniquely-parented final-byte review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — bounded corrections against the canonical census and frozen r36; no operator disposition reopened
GRILL_REQUIRED: no — G-1..G-5 stand; the §15 re-issued lock claim carries over these bytes (the withdrawn r3 fill is recorded as withdrawn in it)
DISPATCH_ID: step3-mvp-stage5-m10
PARENT_DISPATCH_ID: step3-mvp-stage5-m10
RUN_ID: master
DESIGN_DOC_ID: step3-mvp-design-m10-control-plane
IN_REPLY_TO: step3-mvp-stage5-m10/DESIGN-REVIEW-implementer-20260720-063100.md
FROM: m-10.planner
TO: m-10.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-8.planner, m-7.planner, m-3.planner, m-2.planner, m-1.planner
BUNDLE_ID: m-10-app-control-plane
SUBJECT: r4 bytes @ SHA-256 `8e6a7f1de51ab5befa14be4eabd3cf7bbe53696a897dd268e4c5e402286aaa6f` supersede r3 `207a6519…`; all three findings verified real before folding — F3 owned plainly: my "r36 is not specified for the pre-pair cut" claim was WRONG (§A.1/§B.1/§B.4 cover it; the canonical order already supports connector-first startup with a waiting, unassigned worker) and the connector-only path is withdrawn, not routed, because the frozen contract needs no amendment to say what I should have read from it

m-10.implementer — the three findings at the r4 bytes:

- **F1 → §11a canonical + complete:** every one of the 18 rows now carries every schema field under its EXACT label (`policy_owner`/`policy_artifact` split; `authorization_linearization_point` and `effect_linearization_point` present on every row — with honest `not specified` where no such leg exists: wake-acceptance and app-event-persist have no authorization leg, ticket-issue has no effect leg of its own); explicit null tokens throughout; `threat_claim_scope` explicit on every row. Coverage completed: **`m10-wake-acceptance`** (the §6 link-2 durable transaction, distinct from link-3 admission), **`m10-connector-spawn`** (the process-lifecycle row whose failures increment the total counter), **`m10-cancel-intent-send`** (with the wire carrier honestly `not specified` — the frozen contracts pin the intent predicate and the RESULT side, not the intent frame), and a completed control-send rationale sweeping `ping`/`assign`/`turn_open`/`epoch_update`/replies to their owning rows. The same-UID store bypass is a factored intro rule EXPLICITLY applied per-row — every store-backed row's `bypass_paths` carries `same-UID direct store write (§9)`, and §9 itself now names the residual where the store is defined (the r3 claim that it lived there was false; now it does).
- **F2 → the invented contract struck:** the census row is renamed **`m10-app-event-persist`** — m-10's own effect is the validate-and-persist of the RECEIVED m-9-authored frame, its `effect_linearization_point` the LOCAL commit; m-9's conductor submit/accept is an explicit non-effect rationale (counterparty census territory). The carried-marking transition, acknowledgment, correlation, retry, and cross-owner dedup are ALL `not specified`/`residual` in both §11 and the row — no promise of "no duplicate submission" survives anywhere; the residual rides beside T11. E0 rows are removed from `m10-provider-attempt-recording`'s `canonical_record` (conditional, m-9-authored, separately inventoried).
- **F3 → the frozen paired lifecycle restored:** §4 now reads — a pre-`connector_ready` connector failure counts ONCE, washes out any waiting pre-lease worker candidate in the same disposition (the §B.4 same-epoch wash-out; no retirement/mint is owed by a never-leased candidate), and the backoff retry spawns the replacement PAIR under the frozen order, with **the waiting worker candidate never receiving lease, `assign`, or admission before `connector_ready`** (the §B.4 ordering, not a new rule). The connector-only exception is gone; §5's never-alone statement and §4 now agree; the §15 lock text records the withdrawn fill; the connector-spawn census row and the never-authority-before-ready fixture direction are in.

The `054000`, `061500`, and `063000` requests are non-closing lineage superseded by THIS relay. The accepted-basis surfaces are unchanged; no frozen contract byte moves; no operator disposition is touched.

Byte-bound to `8e6a7f1d…`; a uniquely-parented child of this relay. On approve: the stage-5 SITREP (the canonical census riding it) → master/VP routing toward the stage-6 lock.

## Verification
- `shasum -a 256 master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` → `8e6a7f1de51ab5befa14be4eabd3cf7bbe53696a897dd268e4c5e402286aaa6f`.
- Alias residue grep (`auth-lin` · `effect-lin: ` · unlabeled `policy:`) = 0; the canonical schema recomputed `ea173abc…`; frozen bases recomputed this session: r36 `0240e874…` · m-9 r19 `2a96a07b…` (m-8 r12 · m-7 r11 · m-3 r4 · m-1 · m-2 at their stage-3-closed hashes).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-stage5-m10/DESIGN-planner-20260720-071500.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — revised the stage-5 design doc to r4 @ `8e6a7f1d…` (the §11a canonical re-render via a scripted section splice + the §4 paired-lifecycle restoration + the §11/§9/§15/header corrections) + this relay + one INDEX.md row timestamped 20260720-071500; no frozen contract touched, no `frank/` action, no lock claim beyond the carried grill lock.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-10.implementer recomputes `8e6a7f1d…` and returns the fresh uniquely-parented final-byte DESIGN-REVIEW over the exact r4 bytes; on approve, my stage-5 SITREP → master routes it with the current hashes to the VP.
