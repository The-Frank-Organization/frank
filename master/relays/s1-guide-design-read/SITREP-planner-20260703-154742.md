## SITREP — m-7 guide advisory read of `s1-slice-1-design` r2: faithful to the locked engine; NO must-fix-before-plan blockers; two small should-fix items + advisory notes

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s1-guide-design-read
PARENT_DISPATCH_ID: s1-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s1-slice-1-design
IN_REPLY_TO: frank/.relays/s1/s1-guide-design-read/SITREP-orchestrator-planner-20260703-154101.md
FROM: m-7.planner
TO: s1.orchestrator-planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.implementer
SUBJECT: advisory verdict on the six asks — C7 faithful (and the right idiom); constraints landed; B1 consistent with §16 modulo one held-visibility qualifier; §J2 set pre-verified byte-exact; grant narrowing forecloses nothing m-7-side; Go fine but the D-3 fallback invariants must add wake-push

Advisory read as requested — no gate verdict owed or given; the formal m-7+VP co-gate stays on the PLAN. I read the full r2 doc against the locked m-7 design-of-record and re-verified the cited locked lines this session. **Summary: the design is faithful to the locked engine. Zero must-fix-before-plan findings. Two should-fix-before-plan items (both one-paragraph doc edits, no re-architecture) + advisory notes below.** Your six asks in order:

**1. C7 / crash-window fold — CONFIRMED faithful to §4/§6/F11, and the right shape.** Park and outbox staying distinct single-pivot mutation classes is exactly what F11 requires (`...conductor-core-design.md:172` lists them as separate classes); the rejected compound-pivot alternative was rejected for the correct reason. The healing mechanism — committed gate record = durable intent, recovery derives + idempotently completes missing follow-ups keyed by `gate_record_ref` before channels open — instantiates the locked design's own presence-based-derivation idiom (the Phase-3 burn set is "derived from committed operator-verdict records, whose presence implies the burn", `:93`; the owed-item pattern "open = record with no disposition-record" is the same shape). Two sharpenings, advisory: (a) state that derived-work completion executes on the single-writer commit path (the loop goroutine, or recovery running single-threaded before it starts) so the sole-store-writer discipline holds during recovery too; (b) have the PLAN note that S2's owed-item projection **generalizes this same mechanism** — when S2 lands, C7's scan should become an instance of it, not a second parallel mechanism.

**2. R-1/R-2/R-3 + O-1..O-3 + F9-whole — all landed as issued.** Spot-checked D-4 (checksum a first-commit format field, quarantine disposition explicitly S2; outcome→`intake_id`), D-9 (rebuild-before-open, F9 run whole as C3), D-7 (loop-mutation outbox with D5 qualifier; produce-only claim-honest; open envelope, no `model_name`, ⑤ cited as the S4-bound carry). No drift.

**3. B1 narrowing — consistent with the §16 claim boundary**, and the honest S1 shape: tool-surface-scoped, direct-file detection explicitly deferred to S2, D5 stated in the row. **One qualifier needed (should-fix ①, folds into the held item below):** B1's "only committed `accepted` records are served/delivered" is byte-true for *downstream work* but needs the locked m-2 carve-out beside it — a `held` record IS consumed by the gate/escalation/operator-resolution machinery and is **operator-visible by locked text** (m-7 §6 `:100` "parked operator-visible"; m-2 `:76`/`:376`), never by downstream work authority. As written, B1 could be read as forbidding the operator surface m-7 §6 requires. **The finding:** your D-6 commits `held` records (fixture H creates one) but the doc never states how the operator learns one exists or what resolves it in S1. Two compliant shapes — pick one and state it: **(i) recommended, cheapest:** a `held` record also derives an ODB/outbox item via the exact derived-work mechanism you built for gates (one more derived-intent class, keyed by the held record; the fault path still passes through no gate that could hold *it*, so the §6 self-exclusion rule survives); or **(ii)** an explicit stated deferral — held is terminal for the intake (m-2 `:376`), the author gets the typed outcome, the operator sees it only via `project()`/INDEX on the operator channel, and the re-run/verdict *resolution* flow is S2 — stated in the doc and swept by SWEEP so S1 never implies live held-resolution. Either passes my gate; silence does not.

**4. §J2 full-set carry — no m-7 concern, and I pre-verified it byte-exact** against `master/ARCHITECTURE.md:110-115` this session: all 8 A-tokens, all 4 B-tokens, `other`→A hardcoded, `routing_escalation` correctly a cited non-member (`:481` — and mind the c6.1a history: that exact token has already been transcription-slipped once; your byte-exact posture is the right defense). Carrying the full frozen set as registry data is config, not scope creep — it *prevents* an S3 enum re-cut. Formal byte-custody stays with the m-2 fidelity review per your charter edge; expect no drift.

**5. Blocker-2 grant narrowing — forecloses nothing m-7-side.** The render is per-(seat, phase) trusted-side registry construction from registry data (m-7 §8.2); adding the S3 conditional pair-Planner render later is additive render logic, zero engine re-cut. What m-7 cares about is that the *authoritative in-loop check* — not the render — enforces seat-scope, and your A2 (hand-crafted call supplying `grant` ⇒ rejected in-loop, introspection both ways) covers it. The product-behavior consequence (no delegated dispatch through frank in S1) is real but is orchestrator/operator territory, and you routed it for ratification correctly.

**6. Go + MCP Go SDK — no conductor-core concern with the stack;** D-2 maps §1/§2 faithfully including both single-writer tiers and handlers-never-touch-files, and the "discipline, not an OS guarantee" honesty matches my own doc's posture. **Should-fix ②: the D-3 fallback invariant list is one invariant short.** It preserves per-seat channels + stamped identity + the 3-verb registry, but not **delivery/wake** — m-7 §8.3 requires delivery = a server-initiated nudge `write()` onto the seat's held connection. If the SDK's socket transport cannot push a server→client frame outside a request/response exchange, a framing fallback that drops push breaks L1/W1 (a parked lane would wake only by polling `project()` — exactly what the locked design rejects as the primary mechanism). Add "server-initiated nudge push on the held per-seat connection" to the fallback invariants so the capability check tests for it explicitly. (Scripted-test-seat scope for S1 is fine; real-runtime lane qualification stays the operator-gated spike per m-7 `:122`.)

**Beyond the six — positive notes, no action:** the crash-point name registry doubling as S2's F11 syscall enumeration is a genuinely good reuse; D-11 is a legitimate, correctly-scoped instance of the one licensed by-construction claim (racing verdicts on the serialized loop, cited to §2.4); the outbox item committing via the standard pivot into `outbox/` satisfies F11's one-pivot property with presence=enqueued; the binding-table stage→fsync→rename fold and the D-10 typed-equivalent replay posture (importer test-only, m-2 §8 preserved) are both right.

**Net:** fold the two should-fix items (held-visibility posture ①; wake-push fallback invariant ②) as r3 one-paragraph edits or as PLAN-carried lines — either satisfies me at the gate. Nothing here re-architects, and PLAN drafting need not pause.

Not authorized / not claimed: advisory only — no gate verdict, no lock reopen, no scope change, no edit to any s1 or governance artifact, no `DISPATCH IMPL`, no code.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s1-guide-design-read/SITREP-planner-20260703-154742.md` — run before handoff (result in inline pointer)
- read whole: `frank/docs/sprints/2026-07-03-s1-slice-1/designs/s1-slice-1-design.md` (r2, 202 lines)
- re-verified locked cites this session: m-7 design `:93, :100, :122, :132, :172` + §2.4/§4/§6/§8; `master/ARCHITECTURE.md:105-118` (§J2 byte-check), `:481`; m-2 design `:76, :167, :177, :376`
- `git -C frank status --short` — untouched by this relay

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260703-154742; read-only on all `frank/` artifacts; no doc/design/code edits
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` read-only this relay)
Next requested action: operator carries this to the s1 session; s1 folds ①/② (r3 or PLAN-carried) and proceeds to the S1 PLAN for the formal m-7+VP gate.
