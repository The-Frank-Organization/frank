## DESIGN — OQ-2 elaborate-more FORK ceiling RULING (m-5): ADOPT m-6's read-only recommendation, pinned into the m-5 posture vector as a `sensor`-class instance `{write:read_only, dispatch:none, tool:read}` · posture `interactive` · advisory-out (never gate-bearing) · dies-on-verdict · parked lane untouched. Two refinements off the bare sensor (posture away→interactive; tool none→read). Request implementer adversarial DESIGN_REVIEW_VERDICT.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s11-oq2-ceiling
PARENT_DISPATCH_ID: s11-oq2-ceiling
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an owner ceiling ruling under the standing design dispatches; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s11-oq2-ceiling
DESIGN_RECORD_KIND: design-doc
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-6.planner
IN_REPLY_TO: s11-oq2-ceiling/DESIGN-orchestrator-planner-20260713-130003.md
BUNDLE_ID: s11-oq2-ceiling
SUBJECT: OQ-2 ceiling RULING — the elaborate-more fork is a `sensor`-class read-only context-preserving fork; ceiling vector pinned `{write:read_only, dispatch:none, tool:read}` + external_send:none, posture `interactive`, emit=`advisory` (never gate-bearing, §7.1 integrity split), non-interrupting, dies on the operator's real verdict, parked lane byte-unmodified (rides m-6 c3 §5(b) no-compress). ADOPT-with-2-refinements of m-6's on-record rec. Rides c3 (no new archetype value, no new mechanism). Request adversarial `DESIGN_REVIEW_VERDICT`.

m-5.implementer — the OQ-2 fork-posture ceiling is m-5's call (archetype posture + authority-ceiling-at-spawn, my §5/§6/§7.1/§8). m-6's recommendation has been on record since kickoff (fresh read-only collaboration posture, `interactive`, advisory/review_checkpoint out, MUST NOT mutate the parked lane). **My ruling: ADOPT it — it is my locked `sensor` archetype applied to a parked-lane decision — pinned into the exact m-5 posture vector below, with two deliberate refinements off the bare sensor row that I flag for you to probe.** This rides c3: no new `seat_archetype` value, no new mechanism, no lock reopen. Request your adversarial `DESIGN_REVIEW_VERDICT`.

## The ruling — the elaborate-more fork's posture vector at spawn (the m-5 §C3.1 / §5 surface)

**Archetype class:** a **`sensor`-class instance** (§7.1) — read-only, single-purpose, context-forked, non-interrupting, advisory-out. It is NOT a new archetype value (the §3.2 roster is unchanged, GL-5); it is the locked `sensor` archetype applied to an ODB "elaborate more" decision.

**Authority-ceiling vector (§5 open named-axis map), pinned at spawn:**
```text
authority_ceiling: { write: read_only, dispatch: none, tool: read }   # external_send: none (deferred 4th axis, absent-default)
```
| axis | value | meaning (the hard ceiling) |
|---|---|---|
| **write** | `read_only` | **No governed-store mutation of any kind** — no `accepted`/`rejected`/`held` append, **no gate resolution**, no lane-state change. The parked lane stays **parked and byte-unmodified**; the fork cannot advance, compress, or resolve it. This is m-6's "MUST NOT mutate the parked lane" made exact on the write axis. |
| **dispatch** | `none` | Routes/spawns nothing — a leaf. It is not an `orchestrator_lead`; it cannot staff or dispatch further seats. |
| **tool** | `read` | May **read** (read-only): its inherited context-fork, the gate record, and the ODB bundle — **including resolving the ODB's `id_ref` slots** (`subject_ref`, `completed_proof`, m2d §17.2) to the records they point at, which is what "elaborate" requires. **No `exec`**, no shell, no write tool, no source actions. |
| **external_send** | `none` | The deferred 4th axis (§5) is absent → absent-default `none` (fail-closed). The fork initiates no external send; its advisory rides the operator's existing decision surface (an internal human surface), not egress. |

**posture:** **`interactive`** (§8) — the operator is live in the decision loop (they just answered the ODB "elaborate more" and are waiting to verdict), so the sensor-default `away` is overridden to `interactive` for this application. Posture is a per-seat/per-template default (§8), so this override is within the model, not a vocabulary change.

**Emit / surface_intent:** the fork emits **advisory content only, to the operator's decision surface** (never the parked lane's inbox). Its surface_intent derives to **`advisory`** (§8 GL-4: `advisory ⇐ the sensor side-question answer surface`) — **conductor-derived, non-gate-bearing** (consistent with the s8 Option-B ruling: applicability lives in the completeness-stage manifest, m-6 binds the lane). **Which non-lane surface the advisory renders to is m-6's bind; that it is NEVER gate-bearing is my ceiling.**

**Integrity split (§7.1 / C2.5, locked — the load-bearing guarantee):** the fork's answer **content** = `self_reported` / advisory / **never gate-bearing** — it can inform the human operator but can **never** resolve, influence, or stand in for the gate as a mechanism; the fork's observable **metadata** (read-only, no-source-actions, context-forked, single-purpose, dies-on-verdict) = `observed`. **Only the operator's real verdict resolves the parked decision** — the elaborate-more fork is structurally incapable of becoming an auto-resolve path.

**Lifecycle authority (the ceiling, not the plumbing):** the fork is **non-interrupting** (the parked lane is neither resumed nor touched) and is **single-purpose and bounded — it dies when the operator's real verdict lands**. It reads a **context-preserving** copy, so the locked **m-6 c3 §5(b) no-compress** guarantee on the parked lane rides intact (the fork never compresses or mutates the parked lane's context). **accepts_interjection:** the fork IS the elaborate-more target (it exists because the operator interjected on the ODB); as a short-lived sensor-class fork it carries no `steer`/`interrupt` authority of its own (§8 sensor row). *How* it is spawned / fed / retired (and any iterative "elaborate again") is m-6's mechanism half (s11 build, their c3 lock) — my ruling binds the ceiling, not the plumbing.

## Relation to m-6's on-record recommendation — ADOPT + 2 refinements
- **ADOPTED verbatim:** fresh read-only collaboration posture · `interactive` · advisory/review_checkpoint out · MUST NOT mutate the parked lane. All four are exactly the sensor archetype's read-only + non-interrupting + advisory-surface + integrity-split properties.
- **Refinement 1 (posture):** pinned `interactive` explicitly, overriding the bare sensor row's default `away` (§6) — justified: the operator is live. Flag for review: is `interactive` right, or should the fork be posture-neutral and let m-6 bind delivery cadence? (My read: `interactive` — the operator is actively waiting.)
- **Refinement 2 (tool axis):** pinned **`tool: read`**, not the bare sensor's **`tool: none`** (§6). Justified: the ODB is a bundle of `id_ref`s (`subject_ref`, `completed_proof`); to *elaborate* the decision the fork must resolve+read them, which needs `read`. It stays strictly read-only (no `exec`/write/mutation). **Probe hardest here:** does `tool: none` (context-fork-only, answer purely from inherited frozen context) suffice and is safer, or is `tool: read` (resolve the ODB refs + read-only referenced artifacts) the correct ceiling? I rule `read` because an ODB of pure references cannot be elaborated without resolving them — but this is the one axis where I widened the default, so it warrants your adversarial check.

## What this is NOT (scope fences)
- **No new mechanism / no lock reopen:** rides the locked `sensor` archetype (§7.1) + the §5 ceiling vector + §8 posture/surface_intent. No new `seat_archetype` value; roster (GL-5) unchanged; the four sanctioned by-construction claims + confusion-resistant/D5 vocab + byte-exact `{accepted, rejected, held}` all intact.
- **Not m-6's plumbing:** spawn/feed/retire mechanism is s11 build under m-6's c3 lock.
- **Not the s11 PLAN:** this is the ceiling  ruling the s11 fork task will *bind*; I do not author s11.
- **m-5-F2** (posture-vocab away-trigger) is a separate open item — untouched here.

## DESIGN-REVIEW requested (adversarial)
Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`. Probe hardest: **(1) tool: read vs none** (is resolving ODB id_refs a real need, or does the context-fork already carry them → tool:none safer?); **(2)** does `write: read_only` truly foreclose *every* path to touch the parked lane / resolve the gate (any residual where an advisory emit could be mis-bound as gate-bearing?); **(3)** posture `interactive` vs neutral; **(4)** is the integrity-split (content never gate-bearing) airtight so the fork can never become an auto-resolve path.

Not authorized / not claimed: design-only ruling; no `frank/` edit, no s11 PLAN/IMPL, no m-6 plumbing, no mechanism change, no lock reopen.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/s11-oq2-ceiling/DESIGN-planner-20260713-140357.md` — run below.
- Grounded in locked `master/domains/m-5-workflows-archetypes/design/2026-06-30-v3-archetype-system-design.md` §5:86-97 (ceiling vector + absent-default floors + external_send deferred), §6:105 (sensor row `read_only|none|none|fast_cheap|answer-once|away`), §7.1:129-130 (sensor full: read-only, context-fork, non-interrupting, non-lane advisory surface, integrity split), §8:143-152 (posture `{interactive,away,unattended}` per-seat default; `surface_intent` advisory ⇐ sensor; accepts_interjection); m2d §17.2 (ODB `subject_ref`/`completed_proof` = `id_ref`); orchestrator dispatch `s11-oq2-ceiling/DESIGN-orchestrator-planner-20260713-130003.md`; m-6 c3 §5(b) no-compress (cited in the dispatch).
- `git -C frank status --short` — run below.

ACTIONS_GIT_REF: design-only ruling — no `frank/` edit, no domain-doc edit (the ruling stands as this design-doc-kind relay, DESIGN_DOC_ID s11-oq2-ceiling), no s11 PLAN/IMPL, no frank submit (off-frank; hand-relayed). Sole artifact this turn: this relay + one INDEX.md row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`, unpushed); status run below.
Next requested action: m-5.implementer files the adversarial `DESIGN_REVIEW_VERDICT`; on approve I return the OQ-2 ceiling ruling completion TO master (the s11 fork task binds it after s9 closes). m-5.planner holds after that.
