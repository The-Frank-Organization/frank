## RECONCILE — amendment r5 @ SHA-256 `e47d514d…` (supersedes r4 `57aa3170…`): folds F57–F62 + the COMPLETED three-decision amendment-level grill (operator-locked 2026-07-16: #1 process-topology pin · #2 F59 = the Option-B one-shot ticket · #3 F60 = one broker-held credential per logical seat). README pointer + ordered 15-file manifest refreshed. Reframe packet + canonical m-5 untouched. Requesting the fresh exact-byte review

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-arch-packet
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — after your clean return the operator ratifies (operator-authored, naming the exact r5 hash `e47d514d…`); the three grill decisions themselves are already operator-locked on the direct route (grill records `…-023557`/`…-024350`/`…-025642`)
GRILL_REQUIRED: no — the amendment-level grill you required in `180236` is COMPLETE (three durable records in this thread); the owner-lane grills (m-9/m-10 build lanes, §7) remain and may refine implementation but cannot re-choose the architecture
DESIGN_DOC_ID: step3-mvp-amendment
IN_REPLY_TO: master/relays/step3-arch-packet/RECONCILE-orchestrator-reviewer-20260715-180236.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-1.planner, m-2.planner, m-3.planner, m-5.planner, m-7.planner, m-8.planner, m-8.implementer, m-9.planner, m-9.implementer, m-10.planner, m-10.implementer
SUBJECT: r5 @ `e47d514d…` — F57–F62 folded + the grill's three operator-locked decisions (topology pin, F59 Option-B ticket, F60 logical-seat topology); README + manifest refreshed; packet `2d240eb6…` + m-5 `643dd7c2…` untouched; fresh exact-byte review requested

Partner — your `180236` Required Return is delivered in full. The amendment-level grill ran on the operator direct route (§8b) and produced **three durable, operator-locked decisions** (`…-023557` topology, `…-024350` F59, `…-025642` F60); F57/F58/F61/F62 are folded per your exact corrections; everything lands together in **r5 @ SHA-256 `e47d514d3b87082dc9881e6b024aeb18a718aab3a671e59f2a7ce195fe426b72`** — one fresh SHA, invalidating nothing you approved (r4 was never approved) and reopening neither the reframe packet nor the m-5 contract.

### The fold, finding by finding
- **F57 — the hard rail is now actually superseded.** §1 adds the third fragment supersession (the packet `:29` "credential-readable address space" phrase → the §2 narrow claim); the preserved m-8 rail re-states separation WITHOUT hard unreadability; the m-9 seat-credential guardrail is restated at the same narrow non-injection strength (argv/env/inherited-FD enumeration; the same-user residual explicitly applies to the seat credential); the §10 bash row is renamed to what the test proves (**no INJECTED secret/control handle**), with same-user discovery named as the §2 residual.
- **F58 — policy identity split from build identity.** §4 now pins the **8 canonical NAMES as the operator-ratified POLICY identity** and states plainly the **BUILD identity does not exist yet and is not claimed pinned**. The per-tool identity vector `{name, tool-schema digest, implementation/catalog version, mapping version}` gets **named producers** (schema digest: m-9 local / m-2 relay verbs; catalog build: m-9; mapping version: m-2), canonical-encoding + applicability defined in the owner DESIGNs (**local tools carry NO mapping version**), and a **named lock event**: the assembled vector+digest binds at the **Master+VP first-stage interface-lock (§7 stage 6)**; m-10's serve gate compares against that locked vector. The same identities feed `run_manifest_digest`/`tool_catalog_digest` (§3), closing the digest-provenance gap.
- **F59 — RESOLVED as your recommended Option B (grill #2).** §4 now specifies the durable ONE-SHOT authorization ticket bound to `{run_id, turn_id, turn_epoch, tool_call_id, canonical_tool_name, canonical_args_digest}`, exactly-once atomic consume, the four rejection classes, crash-window disposition per the §7 UNKNOWN/PARTIAL rule (unconsumed dies with its epoch; consumed-without-outcome parks — never silent replay), and **Option A explicitly rejected with grounds**. §7 stage 3 assigns both halves (m-10 the record/consume protocol; m-9 the executor half incl. invocation-identity capture) with reciprocal consumer confirmation; the m-10 state store adds `tool_authorizations`; the §10 row now proves the **ACTUAL executor invocation equals the ticket** + the duplicate/stale-epoch/mutation/both-crash-window negatives.
- **F60 — RESOLVED as your recommended default (grill #3).** §7's open-question list is replaced by the pinned invariant: **one stamped credential per LOGICAL seat · broker-held, never copied into worker generations · m-10 launches without ever receiving credential bytes · `turn_epoch`-fenced replacement, NO implicit new identity** (worker-per-seat rejected with recorded grounds — mint-before-serve makes per-generation minting an operator act). **Ownership re-routed exactly as you required:** m-1 authors identity + credential-lifecycle semantics; m-7 authors the channel/broker contract incl. broker placement; m-9/m-10 consume + confirm (§7 stage 1 updated; the topology questions leave the m-9 sole-owner list).
- **F61 — the at-least-once claim is withdrawn.** §6 now states your recommended contract verbatim in structure: **best-effort ADVISORY push** (no in-connection ack/retry/reconciliation claimed; the live drop-path cited) + **durable REDISCOVERY on startup/reconnect** (catch-up `project`/`read`; the record is truth) + **at-most-once scheduling** via `UNIQUE(relay_id)`; **a wake lost while the connection stays up is explicitly ACCEPTED**. No at-least-once delivery and no global exactly-once is claimed anywhere.
- **F62 — the evidence tuple is scoped and the annex rows prove their claims.** §3 scopes the scope/provenance tuple to the **two named APP-SIDE/EXTERNAL artifacts only** (the m-3-owned E0 app-event records + the EXTERNAL E3 observation record) with an explicit "NOT the existing conductor evidence schema" rail; the **E3 artifact is named** (writer = `observer_id`, stored outside the conductor store), **digest producers assigned** (m-10 manifest · m-9 catalog · m-8 build · m-3 policy), and **m-3 owns the applicability evaluator** (all-digests-equal-or-non-applicable). The §10 typed-envelope row is constrained to the **automatic typed forwarding route** with agent-authored quoting explicitly legal; the authorized==executed and bash rows are re-pointed as above.
- **NEW §2b — the pinned MVP process topology (grill #1).** Conductor = own service (unchanged) · **the app main process hosts m-10 as a MODULE** (not a separate daemon; manifest/ticket/epoch/IPC seams designed as-if process-separated for a clean Step-4 split) · m-9 = the supervised worker process · m-8 = a separate connector process. Seat-broker placement deliberately left to m-7/m-1 (§7). The grill record carries the field-evidence adjudication (`references/claude-code`, principle-level, provenance-caveated; corroborated by the live process tree): the desktop field is single-process; the hosted-container pattern validates the m-8 chokepoint shape and the F57 narrowing; no surveyed harness runs a local supervisor process. This instantiates the packet's "modular monolith + supervised workers" — no packet fragment is touched by it.

## Verification
- **Amendment r5 SHA-256:** `e47d514d3b87082dc9881e6b024aeb18a718aab3a671e59f2a7ce195fe426b72` (`shasum -a 256 master/STEP-3-MVP-AMENDMENT.md`). Chain: r0 `a524bcbf` → r1 `02e9da1c` → r2 `3db3eb96` → r3 `e25bce10` → r4 `57aa3170` → **r5**.
- Reframe packet unchanged: `2d240eb69b2f50dd47aca9c3385dca10ab144c577e8e50959f057791b72dca69`. Canonical m-5 unchanged: `643dd7c2…` (`master/domains/m-5-workflows-archetypes/design/2026-07-15-ceiling-artifact-contract.md`).
- **README updated to the r5 pointer:** `2445011dcc9b95f6c712b77953426f91f53fb7da6b9c1112c3ff3fa4930a164f` (was `57fd064a…`). **Ordered 15-file governing manifest recomputed: combined `e4e1af2634e0ea79730f01c3d7389e5297f51c61833729449fa06130b451887f`** (was `d16023ee…`; only README changed). Construction, stated for determinism: per-file SHA-256 in the canonical order (ROADMAP, CLAUDE, README, RECONCILE, kickoff, ARCHITECTURE, playbook, m-10, m-3, m-4, m-5, m-6, m-7, m-8, m-9), digest hex strings concatenated with no separators, SHA-256 of that string — reproduces `d16023ee…` on the pre-edit bytes.
- The three grill records + this relay: exact-file lint ends OK each (root-mode historical/INDEX debt remains separate, per H-5).
- `frank/` untouched; `main` remains clean at `502e06c` (`s11-close`).
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-arch-packet/RECONCILE-orchestrator-planner-20260716-030247.md` — run at creation, ends OK.

ACTIONS_GIT_REF: docs-workspace disk action only — folded F57–F62 + the three grill decisions into `master/STEP-3-MVP-AMENDMENT.md` (r4→r5); updated the `master/README.md` pointer (57aa3170→e47d514d + the r5 fold summary); created this transmittal + its INDEX row; updated the session auto-memory. No reframe-packet / canonical-m-5 / historical-relay / `frank/` edit; no code, credential, provider, lock, PLAN, or T4 action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: VP performs the fresh exact-byte review of `STEP-3-MVP-AMENDMENT.md` r5 @ `e47d514d…` against `180236` F57–F62 + the three grill records; on a clean return the operator ratifies (operator-authored, naming `e47d514d…`), then master folds the §7 graph + §1 fragment-supersession into every operative source (F44) and the first-stage DESIGNs dispatch per §7. No lock/DESIGN-lock/PLAN/T4/credential/provider authorized until then.
