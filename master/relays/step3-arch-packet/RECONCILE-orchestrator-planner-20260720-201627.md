## RECONCILE — THE STAGE-6 INTERFACE-LOCK PACKET (master → VP for the adversarial pass; the joint Master+VP lock issues on your approve, operator-gated): the complete §7-graph evidence set at final hashes — nine design artifacts · six grill locks · the F58 build-identity vector · the assembled H-17 census · the errata + semantic pins · every confirmation/reciprocal at the finals — binding the identity contract + expected catalog vector per the ratified amendment; NO PLAN/T4/credential/provider/release/E3/merge/deploy authority issues from the lock itself

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-arch-packet
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the stage-6 lock is the operator-gated Master+VP join per the ratified amendment §7; this packet is master's half; the lock record issues only after your approve + the operator's gate
GRILL_REQUIRED: no — all six grill locks stand and bind below
DESIGN_DOC_ID: step3-mvp-amendment
IN_REPLY_TO: master/relays/step3-arch-packet/RECONCILE-orchestrator-reviewer-20260720-051057.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-1.planner, m-2.planner, m-3.planner, m-7.planner, m-8.planner, m-9.planner, m-10.planner
SUBJECT: the lock set, complete per your F79/`070757`-§5 statement + the two amendment cycles since — every hash below recomputed from disk immediately before filing

## 1. The design artifacts the lock binds (nine, at exact hashes)

| artifact | hash | final approval |
|---|---|---|
| m-1 secret-boundary/seat-identity | `7c8b09a6289f117b86133bb391e02c50e26476b252c56c17cfa52a4794ff944c` | stage-1 pair approve |
| m-2 form-schema mapping (rev5) | `83d8e63ec6a32726a57f715b69d96fc3867e178b35f2aa57c19373de264a0f7d` | stage-1 pair approve |
| m-3 egress/E0/E3 r4 | `009df6070c35f6c2f817db182c8dc1d41c670fa88eb667b8a9f79ea785482bad` | `040455` |
| m-7 transport/broker r11 | `9331ea889375b7e36c1c32b472d09c5162bc7624140ee61a8de3ac8a6d742572` | `195335` |
| m-10 IPC/manifest contract r40 | `d2ce98310afff4a8aeb998a0e530beedbde44cb723c6510ebe351841d70a9146` | `155100` (supersedes r36/r28 per the two F73 amendment cycles) |
| m-8 provider contract r12 | `4b670a79389b552d558555289dd2d19ad5ea42a3baed52946a1706eba65e4f51` | `043932` + basis verdicts through r40 (`200710`) |
| m-9 lifecycle half r21 | `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` | `203100` |
| m-9 full worker r7 (stage-4) | `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` | `211500` |
| m-10 control-plane realization r10 (stage-5) | `6fd1d655146d447194e5181bd81108936fa4560a4f8b4e9de843cf2daac11faf` | `160100` |

## 2. The grill locks (six) + operator decisions
The three amendment grills (`023557` topology · `024350` F59 · `025642` F60) · the m-7 broker-placement grill · the stage-5 lock `m10-stage5-grill-lock-20260720` (G-1..G-5, re-issued over the approved bytes) · the stage-4 worker grill lock (in the r7 bytes). The T11 loud-failure split + the `admission_ref` arbitration are operator-concurred records inside these.

## 3. The F58 build-identity vector (binds at THIS lock per the amendment)
The 8-name policy identity (the ratified dispatch constant) × the build vector: the 8-row expected table in worker r7 (five local `tool_schema_digest`s + m-2's three produced relay digests + `m2-mapping-v1` + `m9-catalog-v1`) ⇒ **expected `tool_catalog_digest` = `7fae5fc1dd8f91c48828beaf0cfba45a1da4c297bf82f790ec2912b0a168c9d4`** (m-2-recomputed independently at their leg-3). Post-build: recompute vs the shipped registry (surface drift) + F63 `m9_worker_build_digest` (impl drift); the release-binding event stays post-build, pre-live-E3.

## 4. The assembled H-17 census
`master/H17-CENSUS.md` v1 @ `13d1fd16989192ea25e889ea5663a5c501845df7bb1507acdc2711a7cf2c5d73` — the merge index over the two approved row sets (m-10 18×21 · m-9 16+2) + the five master-owned conductor-plane rows (cited to the closed Step-2 record; the H-16 post-commit fate named as in-flight) + the coverage statement making "governed" a bounded claim.

## 5. The pins + errata (permanent lock-record items; no byte edits)
N1–N4 as previously disposed (N4's referent re-verified carried to r40 at m-7's `200656`) · **P1 the `sent` semantic pin** (= observed `attempt_started`, never wire-crossing; the m-3 ruling `194500` + m-8 concurrence `200000`; the wire-crossing reading ruled the dishonest option) · **P2 the narrowed E0 terminal-visibility invariant** (attempt-linked, only-when-live-worker-emitted; the m-3 ruling at the stage-5 leg) · **P3 the S-1 strength** (Governance Decay: authorization-outside-context; claim-discipline-scoped) · the L-ledger (L2–L4, L6 ride this record; L5 resolved =B in worker r7; L1/L7/L8 folded historically).

## 6. The confirmation/reciprocal evidence (all at the finals)
The stage-1 16-edge census (close-confirmed `224500`) as amended by the two F73 cycles: the r40 rebinds (m-7 `200656` · m-3 `203000` · m-8 `200710` · m-9 via the r21 fold) + the admission_ref reciprocal delta (m-10 `194500`: the three F84 identities byte-identical survivors, the new seam fresh both directions) · the stage-5 consumer legs 4/4 (`140000`/`090000`/`071330`/`090000`) + the stage-5-leg-1 delta over r10 × r21 (m-9 `214500`, option-(b) rebind certification) · the stage-4 consumer legs 4/4 (`194500-m10`/`200000-m8`/`201500-m2`/`194500-m3`) · the three-identity reciprocal lineage (`223500` + the delta). Counted per the F81 rule: current carriers only, lineage named in the underlying relays.

## 7. What the lock DOES and DOES NOT do
DOES: freeze the nine artifacts as the build's contract-of-record; bind the F58 vector + expected digest; adopt the census as the claim boundary; convert any post-lock byte change into a full-F73 re-open. DOES NOT: issue PLAN, the T4 code token, credentials, provider calls, release binding, E3, merge, or deploy — the PM-to-build-team dispatch follows the lock separately (per §7), the H-16 fix lane completes its master/VP pass before T4 wires the API, and the release-binding + live E3 + exit test (the corrected three-leg F88 bar) close the step.

## 8. Requested return
Your adversarial pass over this packet. On your approve: the joint Master+VP lock record issues, operator-gated; then the T4 PM dispatch (carrying the corrected exit bar + the PRIOR-ART §4 reference lanes + H-25's raw-ID instrumentation) and the parallel H-16 completion.

## Verification
All nine artifact hashes + the census + schema hashes recomputed from disk immediately before filing (exact); the F58 expected digest cross-checked against worker r7 + m-2's independent recompute; each cited approval/confirmation relay verified present on disk; exact-file lint of this relay OK (root-mode historical noise disclosed per your erratum rule).

ACTIONS_GIT_REF: docs-workspace disk action — this packet + one INDEX.md row + `master/H17-CENSUS.md` (new, hashed above); no design doc, no `frank/` action, no lock issued by this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: the VP returns the adversarial pass; on approve master drafts the joint lock record for the operator's gate.
