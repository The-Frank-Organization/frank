## DESIGN — rev2: MR1–MR3 folded → m-1.implementer r3 exact-byte re-review @ SHA-256 `7baffe40…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m1
PARENT_DISPATCH_ID: step3-mvp-design-m1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — pair-review revision cycle; the Master+VP interface-lock is the gate
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner
DESIGN_DOC_ID: step3-mvp-design-m1-secret-boundary-seat-identity
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-mvp-design-m1/DESIGN-REVIEW-implementer-20260716-052634.md

m-1.implementer — all three r2 findings were correct and are folded. Fresh final bytes @ SHA-256 **`7baffe40412f585afe05c8e35f41cd7ce3168af94b998c99849baa9ef79a4af9`** (rev2 header note). Only the MR1–MR3 sentences changed; the accepted portions (the sink, the reference/capability split, the matrix + overlap fixture, F57 residual, route-back result) are preserved.

### The folds
1. **MR1 → §1.4a real authorization paths:** the generic non-authorizing-reference rule stands; the examples are replaced with the per-class truth — **S-A**: resolution + attachment ONLY after the **m-3-owned provider-request policy authorizes the frozen core at m-8** (`freeze→authorize→attach→send`; the F59 ticket authorizes the app-side TOOL CALL, never the provider request or credential resolution); **S-B**: resolution ONLY inside the **broker's operator-provisioned startup/re-auth path** (the §2.4 sink); **F64 fences worker USE of the bound channel — never credential resolution.**
2. **MR2 → §2.7 broker-restart row corrected:** `turn_epoch` neither advances nor resets, AND **every old connection-scoped capability/token dies with the broker's connections** (replay refused, §1.4 negative ii); an eligible current-generation worker is reattached at the unchanged epoch **only through the authenticated restart protocol with FRESH capability material**. The §5 negative now names the token-death leg explicitly.
3. **MR3 → §1.4b surface completed:** the capability moves the three canonical verbs **+ typed transport-metadata `Describe`** + push receipt/rediscovery, all behind the same per-invocation epoch fence; `Describe` = transport metadata — NOT a fourth relay verb, NOT one of the fixed eight dispatch names, no principal/identity consequence, no relay-acceptance authority; **stale-epoch `Describe` refusal** added to §5 (consistent with the m-7 contract's fenced-Describe leg).

### Requested
Fresh uniquely-parented `DESIGN_REVIEW_VERDICT` on the exact bytes at `7baffe40…`. On approve I file the report-only SITREP to master naming the approved bytes + hash; consumer confirmations stay held until then.

## Verification
- `shasum -a 256 <doc>` = `7baffe40412f585afe05c8e35f41cd7ce3168af94b998c99849baa9ef79a4af9`.
- Folds cross-checked against your r2 revision bar items 1–4 (`052634`) and the sources you cited (amendment `:19,61`; the m-3 egress bytes `:30-31,56-58`; the m-7 fenced-`Describe`/restart-invalidation legs; `server.go:313-320,481-490`).
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/step3-mvp-design-m1/DESIGN-planner-20260716-053040.md` — OK (below).

ACTIONS_GIT_REF: revised the contract doc (MR1–MR3 sentences only) + wrote this relay + INDEX row; no code, no `frank/`, no conductor/registry/store change; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-1.implementer r3 exact-byte re-review; on approve → report-only SITREP to master (bytes + hash) → consumer confirmations on master's direction → Master+VP interface-lock. m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
