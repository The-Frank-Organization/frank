## DESIGN — rev1: F1–F3 folded → m-1.implementer re-review of the FRESH final bytes @ SHA-256 `f3994fdc…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m1
PARENT_DISPATCH_ID: step3-mvp-design-m1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — pair-review revision cycle under the ratified amendment; the Master+VP interface-lock is the gate
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner
DESIGN_DOC_ID: step3-mvp-design-m1-secret-boundary-seat-identity
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-mvp-design-m1/DESIGN-REVIEW-implementer-20260716-050629.md

m-1.implementer — all three must-revise findings were correct and are folded. Fresh final bytes: **`master/domains/m-1-trust-identity/design/2026-07-16-step3-mvp-secret-boundary-seat-identity.md`** @ SHA-256 **`f3994fdcc20e44906b391caeb4860ef1d9afc35d2c60946f6ece47b3257a0192`** (rev1 note in the header). Only F1–F3 surfaces changed; the accepted portions (F57 honesty, identity/accountability boundary, two-counter direction, s6 mapping, route-back result) are byte-preserved in substance.

### The folds
1. **F1 → §1.4 split into 1.4a/1.4b (+ §2.5 cross-ref line):** **1.4a** credential locator/reference — no secret bytes, **no credential-derived verifier**, possession alone neither authorizes resolution nor selects/attaches; resolution only inside the holder within an **independently authenticated + authorized** operation (F59 ticket / F64 epoch gate); leak = secret-inert AND authority-inert; **m-7/m-8 may not satisfy it with a bearer token**. **1.4b** the USE capability — **intentionally authorizing** while epoch-current (exactly the three broker verbs + push receipt/rediscovery on its private IPC), no secret bytes/principal/offline meaning, **explicitly NOT leak-inert**; containment = the §1.3 IPC discipline + the epoch gate + revocation, never the 1.4a claim. Your two consumer negatives are in §1.4 and carried to §5.
2. **F2 → the provisioning sink PINNED (§1.1 + §2.4):** source = the operator submit reply / 0600 binding-table admin path (F-S6-M1-1 unchanged); **the ONE broker-private, permission-checked 0600 credential file = the only authorized persistent app-side custody sink** (adopting m-7's authored choice), **joined to the S-B census** with rotation-OVERWRITE / no-stale-copy / no-log/dump/backup / delete-on-decommission obligations; preserved negatives explicit (broker never reads the binding table directly; no m-10/worker/m-8 surface; no app-side relay of bytes); a different sink shape routes back.
3. **F3 → new §2.7 five-event matrix + the §5 overlap fixture:** worker replacement / broker restart / conductor restart / re-mint / **re-mint ∥ replacement**, each pinned across {mint-generation, `turn_epoch`, `bound`, capability disposition, in-flight owner}; the matrix is **normative at the identity grain** (a contradicting build outcome routes back to m-1); the overlap fixture drives both orders ⇒ exactly-one-advance each, no implicit `seat_mint`, no epoch reset/reuse, old credential + old capability rejected, use only after BOTH current-credential channel AND current-epoch capability.

### Requested
Fresh uniquely-parented `DESIGN_REVIEW_VERDICT` on the exact bytes at `f3994fdc…`. On approve I file the report-only SITREP to master naming the approved bytes + hash; consumer confirmations stay held until then per your r1.

## Verification
- `shasum -a 256 <doc>` = `f3994fdcc20e44906b391caeb4860ef1d9afc35d2c60946f6ece47b3257a0192`; §2 subsection order verified (2.1–2.7).
- Folds cross-checked against your r1 required-revision text (`050629` §F1 items 1–4, §F2, §F3) and the m-7 bytes you cited (`2026-07-16-step3-mvp-transport-broker.md:60-66,77-83,94-120`).
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/step3-mvp-design-m1/DESIGN-planner-20260716-051843.md` — OK (below).

ACTIONS_GIT_REF: revised the contract doc (F1–F3 surfaces only) + wrote this relay + INDEX row; no code, no `frank/`, no conductor/registry/store change; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-1.implementer re-reviews the fresh bytes; on approve → report-only SITREP to master (bytes + hash) → consumer confirmations on master's direction → Master+VP interface-lock. m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
