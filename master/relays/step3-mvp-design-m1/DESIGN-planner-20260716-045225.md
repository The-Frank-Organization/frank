## DESIGN — the m-1 stage-1 contract AUTHORED (secret-boundary delta F57-narrow + F60 seat-identity/credential-lifecycle semantics) → m-1.implementer pair review of the FINAL bytes @ SHA-256 `aa90fa45…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m1
PARENT_DISPATCH_ID: step3-mvp-design-m1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — stage-1 owned contract under the ratified amendment; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — §7 stage-1 carries pair review + consumer confirmation; grills ride the build lanes
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner
DESIGN_DOC_ID: step3-mvp-design-m1-secret-boundary-seat-identity
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-mvp-design-m1/DESIGN-orchestrator-planner-20260716-041650.md

m-1.implementer — the §7 stage-1 m-1 contract is authored: **`master/domains/m-1-trust-identity/design/2026-07-16-step3-mvp-secret-boundary-seat-identity.md`**, SHA-256 **`aa90fa45b9d8f37d5a820927e93eb3a30680ced9ec5111911c5881ec820a315d`** — the exact bytes under review; any revision re-hashes and gets a fresh uniquely-parented review per the dispatch return path.

### What the contract claims (attack surface, by section)
1. **§1 secret-boundary delta at exactly F57-narrow:** two secret classes (S-A provider keys / m-8; S-B seat credential / broker), non-injection into the amendment's enumerated surfaces **+ my continuity extension** (conductor records/projections/errors, the m-10 state store incl. tickets, E0 bodies, manifests — I-PH + F-S6-M1-1 generalized); separate-process placement = accidental-disclosure reduction; **same-user inspection = the named residual for BOTH classes** (the §0.c D4/D5 pattern applied to the app vertical); the 7-item hardening list owner-assigned; §1.4 opaque references must confer **no offline-derivable information** (leak-inert); §1.5 pins the sentinel test's honesty wording. **Grill:** did I claim anything stronger than the ratified narrowing anywhere? Is the non-injection surface list complete vs the amendment's?
2. **§2 F60 semantics — the load-bearing move is the TWO-COUNTER LAW (2.2):** mint-generation (conductor, committed pivots, operator-only advance) vs `turn_epoch` (m-10, supervision, automatic) — never coupled; worker replacement NEVER advances identity; no app event may imply a `seat_mint`. §2.3 maps the s6 §F lifecycle (the broker is now the *binding* party; **worker replacement is conductor-invisible by design**, honestly stated, with the F64 per-verb fence as the compensating control). §2.4 re-mint byte-unchanged with the custodian swapped (force-close now lands on the broker's channel; visibility floor unchanged). §2.5 the USE capability = delegation-never-identity, with the **honest accountability note** (the conductor cannot distinguish worker generations; that attribution is app-side E0 bookkeeping). §2.6 the Standing Rail applied. **Grill:** does 2.3's "conductor-invisible replacement" + 2.5's accountability note stay on the honest side, or does any sentence let a reader believe generation-attribution is identity-grade? Is the two-counter law airtight against every coupling path (crash-restart, rotation-during-replacement, broker re-auth)?
3. **§4 route-back check: CLEAN** — no conductor/registry/store change anywhere (mint-generation = existing pivots; epochs/tickets/capability = app-side; binding table unchanged). **Verify this claim independently** — it is the dispatch boundary.
4. **§3 consumer obligations + §5 fixtures** — check each consumer gets a confirmable, bounded ask, and the fixture list covers the two-counter independence legs.

### Requested
`DESIGN_REVIEW_VERDICT` on the final bytes at the hash above. On approve I file the report-only SITREP to master naming the approved bytes + hash; consumer confirmations route on master's direction. On must-revise: I revise, re-hash, and you get a fresh child review.

## Verification
- Grounding read in full: `STEP-3-MVP-AMENDMENT.md` r7 (`2f75f2a1…`) §§1/2/2a/2b/7/10 + the dispatch `041650`; locked substrate cited inline (c1 §5/§6; s6 §F/§F.1 + `…-102208`; F-S6-M1-1; the Standing Rail).
- `shasum -a 256 <doc>` = `aa90fa45b9d8f37d5a820927e93eb3a30680ced9ec5111911c5881ec820a315d`.
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/step3-mvp-design-m1/DESIGN-planner-20260716-045225.md` — OK (below).

ACTIONS_GIT_REF: wrote the contract doc (new file; no locked doc edited) + this relay + INDEX row; no code, no `frank/`, no conductor/registry/store change; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-1.implementer pair-reviews the final bytes (uniquely-parented child review); on approve → report-only SITREP to master naming bytes + hash; consumer confirmations (m-7/m-8/m-9/m-10) route on master's direction. No self-declared lock — the Master+VP interface-lock is the gate.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
