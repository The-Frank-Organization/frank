## DESIGN — rev3: R3-F1 folded (the three F64-surface summaries normalized to the complete §1.4b closed surface) → m-1.implementer r4 exact-byte re-review @ SHA-256 `7c8b09a6…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m1
PARENT_DISPATCH_ID: step3-mvp-design-m1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — closed-surface consistency correction under the existing MR3 ruling; the Master+VP interface-lock is the gate
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner
DESIGN_DOC_ID: step3-mvp-design-m1-secret-boundary-seat-identity
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-mvp-design-m1/DESIGN-REVIEW-implementer-20260716-053603.md

m-1.implementer — R3-F1 folded, exactly at your revision bar and nothing beyond it. Fresh final bytes @ SHA-256 **`7c8b09a6289f117b86133bb391e02c50e26476b252c56c17cfa52a4794ff944c`** (rev3 header note).

### The fold (three sentences, no other text changed)
- **§2.3 (the F64 fence clause):** "EVERY verb invocation and push delivery" → "EVERY request operation — **each of the three canonical relay verbs AND typed transport-metadata `Describe`** — and on **every push delivery/forwarding** (the complete §1.4b surface)".
- **§2.5 (the authority summary):** "the three verbs + push" → "**exactly the complete §1.4b surface** — the three canonical relay verbs + typed `Describe` (request operations) + push receipt/rediscovery (fenced at forward time)"; "per-verb epoch gate" → "per-operation epoch gate".
- **§3 (the m-7 obligation):** "the F64 per-verb/push epoch gate" → "the F64 **per-operation epoch fence over the three canonical relay verbs AND typed `Describe`, plus the per-push fence at forward time** (the complete §1.4b surface; `Describe` stays metadata — never a relay verb, dispatch-tool name, or relay-acceptance authority)".
- Post-fold grep: no `three verbs` / `per-verb` phrasing survives anywhere in the doc; §1.4b's non-verb/non-dispatch classification of `Describe` is preserved verbatim; all accepted r1/r2 portions untouched.

### Requested
Fresh uniquely-parented `DESIGN_REVIEW_VERDICT` on the exact bytes at `7c8b09a6…`. On approve I file the report-only SITREP to master naming the approved bytes + hash; consumer confirmations stay held until then.

## Verification
- `shasum -a 256 <doc>` = `7c8b09a6289f117b86133bb391e02c50e26476b252c56c17cfa52a4794ff944c`.
- Fold cross-checked against your R3-F1 required revision + the m-7 closed-surface target (`2026-07-16-step3-mvp-transport-broker.md:99-114,148-158`).
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/step3-mvp-design-m1/DESIGN-planner-20260716-060705.md` — OK (below).

ACTIONS_GIT_REF: revised the contract doc (the three R3-F1 sentences + the rev3 header note only) + wrote this relay + INDEX row; no code, no `frank/`, no conductor/registry/store change; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-1.implementer r4 exact-byte re-review; on approve → report-only SITREP to master (bytes + hash) → consumer confirmations on master's direction → Master+VP interface-lock. m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
