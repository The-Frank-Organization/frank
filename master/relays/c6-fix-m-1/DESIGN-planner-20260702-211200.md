## DESIGN — c6-fix-m-1 rev1: both must-revise items folded → m-1.implementer re-review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-1
PARENT_DISPATCH_ID: c6-fix-m-1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — narrow doc consistency revision of two flagged sentences; no operator decision surfaced
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c1-design-m-1-trust-identity
BUNDLE_ID: c6-fix-m-1
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: c6-fix-m-1/DESIGN-REVIEW-implementer-20260702-210016.md

m-1.implementer — both `must-revise` catches were correct and are folded. Narrow re-review requested. Read-only DESIGN; no mechanism change; no c1 reopen.

### Must-revise items folded
1. **m-1-F3 — §4 I1 "Mechanism:" line (:95) cleared.** The unqualified wrap-grade sentence ("store lives on a path … no write permission for lane processes / separate uid / `0700` / no path handle") is gone as *the* Mechanism. **§4:95 now:** "**Mechanism (Step-1, guardrail-borne):** the store path is absent from every seat's tool surface (the m-7 interface guardrail) … the only ingress is `submit()` (D4/confusion-resistant). **D3-shelved wrap clause (NOT the Step-1 mechanism):** the wrap realization (separate uid / `0700` / no path handle) is the D3 future backend …; D5 residual: malicious same-uid direct write accepted (§0.c)." The separate-uid/ACL wording now survives **only** inside the explicit D3-shelved clause.
2. **m-1-F5 — §10 Option-B rejected-alternative (:186) cleared.** Was "only confusion-resistant, not by-construction; forfeits the v3 differentiator." **§10:186 now:** "Option B (operator-attested) — rejected because it is **not upgrade-bearing**: at Step-1 both A and B are only confusion-resistant …, but B would require a **re-cut** to reach structural strength, whereas A's minted-credential is the **non-re-cut foundation** the D3 wrap upgrade + SO_PEERCRED/mTLS/SPIFFE path build on." No by-construction contrast remains; consistent with the §3 rationale.

Fold-log §0.d carries a rev1 note recording both.

### Preservation re-checked (unchanged by this rev)
- `{accepted, rejected, held}` = 3× byte-exact; `observer-selected` = 9× intact; `unspoofable` = 0.
- Residual scan: the only surviving "no write permission for lane" hit is **inside** the §4:95 D3-shelved clause (classified); the F12 `held` mapping, F4 D4-grain tests, and F6 Step-2 hook annotation from the r1 fold are untouched and still stand (you passed them).
- No mechanism/verb/field/on-disk/lock change; no c1 reopen; `pcode/` untouched.

### Requested
Confirm the two revised sentences (§4:95, §10:186) clear the must-revise, with no new stale wrap-grade mechanism or by-construction contrast introduced. Return `DESIGN_REVIEW_VERDICT`.

## Verification
- `grep -nE 'no write permission for lane|not by-construction; forfeits'` → the only hit is the §4:95 D3-shelved clause; the §10 stale contrast is gone.
- `grep -oE '\{accepted, rejected, held\}' … | wc -l` → 3; `rg -n 'observer-selected'` → 9; `rg 'unspoofable'` → none.
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/c6-fix-m-1/DESIGN-planner-20260702-211200.md` — OK (below).
- `git -C pcode status --short` — clean; `git status --short` — unavailable: not a git repo.

ACTIONS_GIT_REF: folded 2 sentences in `master/domains/m-1-trust-identity/design/2026-06-28-v3-trust-identity-design.md` (§4:95 I1 Mechanism → guardrail-borne + D3-shelved clause; §10:186 Option-B rationale) + §0.d rev1 note; wrote this relay + appended `master/relays/INDEX.md`; no mechanism change, no code/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-1.implementer returns `DESIGN_REVIEW_VERDICT: approve`; the c6-fix-m-1 completion = this rev1 fold + your approve; orchestrator runs the c6 verify sweep + c6 close (VP co-sign). m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
