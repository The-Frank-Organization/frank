## DESIGN — c6-fix-m-1 planner fold: 5 re-review cleanup findings applied → m-1.implementer adversarial review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-1
PARENT_DISPATCH_ID: c6-fix-m-1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — doc-only consistency folds into a locked doc; no operator decision surfaced
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c1-design-m-1-trust-identity
BUNDLE_ID: c6-fix-m-1
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: c6-fix-m-1/DESIGN-orchestrator-planner-20260702-204500.md

m-1.implementer — applied the **5** c6 re-review findings routed to us (`DESIGN-REREVIEW-2026-07-02.md` §5, ◆VP where marked) as **doc-only** consistency folds. Requesting your adversarial `DESIGN_REVIEW_VERDICT` per the dispatch. I reconciled the source-of-record (§5/§68 + the appendix + the four-sanctioned-claims list §82) before editing, not the dispatch table alone. **No mechanism change; lock invariants unchanged; no c1 reopen.** Read-only DESIGN; no code/`pcode`/spike/PLAN.

### Per-finding changelog
| id | tag | fix landed |
|---|---|---|
| **m-1-F3** | B ◆ | **DI-1 dispositioned.** §4 DI-1 line rewritten: wrap-grade (no-lane-write / separate-uid / store-ACL) = **D3-shelved wrap invariant**; **Step-1 I1 rides the m-7 interface guardrail** (store path absent from every seat tool surface, D4/confusion-resistant). §4 proof re-cut ("QED for the D4 claim, conditional on the m-7 guardrail"). §9 Step-1 row → "guardrail-borne, not a lane-excluding store-dir ACL." §12 AC#1 → **D4 guardrail fixture** (no seat tool/verb/path reaches the store) **+ a D3-scoped wrap-spike criterion** (direct-write-fails only if/when the wrap backend is built). |
| **m-1-F12** | C | **Third persisted outcome `held`.** §5 `submit()` atomic-commit: on an engine-internal fault *during* commit (machinery ran-and-broke / trusted check could-not-run, ≠ form/lineage fail) → **m-7 C4.1 / NF-S6 two-axis**: authority-bearing → **`held`** (escalated, not `rejected`); non-authority → `rejected`/author-returned with the fault edge. Outcome set now byte-exact **`{accepted, rejected, held}`**. Marked "m-7's disposition, surfaced for `submit()` completeness — not an m-1 mechanism change." |
| **m-1-F4** | M | **DI-2 tests re-cut to the D4 grain.** §12 #3, §13 #1, §13.rev2 #1: the wrap-grade "sibling process *must fail* to read" isolation probe → **tool-surface negative fixture** (no verb/path/credential-read in the seat's surface; a confused sibling has no tool). Isolation-probe explicitly labeled the D3-shelved form. |
| **m-1-F5** | M | **Transport rationale rebuilt post-sweep.** §3: added the A ≻ B basis — both A and B are only confusion-resistant at Step-1, so A is chosen because its minted-credential is the **non-re-cut foundation** the D3 wrap upgrade + SO_PEERCRED/mTLS/SPIFFE path build on; B is a re-cut dead-end. |
| **m-1-F6** | M | **Ordered strike of stale `m-3 observe`.** §13.2 (atomic-commit carry) + §7 (m-3 consumer line): annotated `[Step-2+, reserved hook 2c]` with the §5(2c)/CQ-1(a) cross-ref; §7 "filled at submit" → "filled at the Step-2 observe hook (2c), not in Step-1." |

### Verified CTO-already-applied (per dispatch — verified, NOT redone)
- **F7** — §4 header now reads "The **three** invariants" (I1/I2/I3). ✓
- **F9** — no surviving `unspoofable` overclaim anywhere in the doc/README/ARCHITECTURE (the c5 sweep already relabeled that line to confusion-resistant). ✓
- **F10** — ARCHITECTURE DI-1..DI-5 anchors present and **consistent** with my guardrail-borne DI-1 disposition (CTO-owned doc; not my edit). ✓
- **F1/F2** — README §Owns relabeled to "sole *governed* writer … confusion-resistant; D5 accepted" + seat-stamper "confusion-resistant, GRILL-LOCK D4." ✓

### Preservation attestation (dispatch constraints)
- **Four sanctioned by-construction claims preserved** — m-1's is **observer-selected control** (DI-5 read-vantage / `parent_picker`): untouched, still framed observer-selected (9 occurrences intact). The other three (serialized-loop kill, R2 gate-grammar, authority-ceilings) are other domains'.
- **Swept confusion-resistant / D5-residual vocabulary preserved** — every wrap-grade phrase I touched is now D-classified (quoted as D3-shelved or carrying a D5 residual); no new raw overclaim introduced.
- **Byte-exact `{accepted, rejected, held}` enum** — verified 3× byte-exact; no drift.
- **No mechanism / verb / field / ownership / on-disk / lock-invariant change; no c1 reopen; `pcode/` untouched.** All 5 are consistency folds recorded in fold-log **§0.d**.

### Requested of you (m-1.implementer)
Adversarial review: (1) is the **DI-1 disposition (F3)** faithful to ◆ — Step-1 I1 guardrail-borne, wrap = D3-shelved, AC#1 split into a D4 fixture + a D3 spike criterion, no lingering wrap-grade demand? (2) does **`held` (F12)** correctly defer to m-7 C4.1/NF-S6 (authority→held) without inventing an m-1 mechanism, enum byte-exact? (3) are the **F4** DI-2 tests genuinely D4-grain (tool-surface negative), not smuggled wrap-probes? (4) F5/F6 accurate + no sanctioned-claim or enum regression? Return `DESIGN_REVIEW_VERDICT` + any must-revise.

## Verification
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/c6-fix-m-1/DESIGN-planner-20260702-210000.md` — OK (below).
- Enum byte-exact: `grep -oE '\{accepted, rejected, held\}'` → 3 hits (fold-log §0.d ×2, §5 submit ×1); no variant spelling/order.
- Overclaim scan: every residual `no write access`/`store-dir ACL`/`must *fail*`/`verified E2` hit is D3-shelved-quoted or D5-residual-tagged; `unspoofable` = 0.
- Sanctioned claim: `observer-selected` = 9 occurrences, DI-5/`parent_picker` framing intact.
- Findings source of record: `master/DESIGN-REREVIEW-2026-07-02.md` §5/§68/§82 + appendix.
- `git -C pcode status --short` — clean; `git status --short` — unavailable: not a git repo.

ACTIONS_GIT_REF: folded `master/domains/m-1-trust-identity/design/2026-06-28-v3-trust-identity-design.md` (5 findings: §3/§4/§5/§7/§9/§12/§13/§13.2/§13.rev2 + fold-log §0.d); wrote this relay + appended `master/relays/INDEX.md`; no mechanism change, no code/source/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-1.implementer returns `DESIGN_REVIEW_VERDICT: approve` (or must-revise); on approve the c6-fix-m-1 completion is the pair (this fold + your approve); orchestrator runs the c6 verify sweep + c6 close (VP co-sign). m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
