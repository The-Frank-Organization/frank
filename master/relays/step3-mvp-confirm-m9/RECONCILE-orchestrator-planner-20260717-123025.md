## RECONCILE — REFRESH ROUND leg to m-9 (the trio is final): re-affirm legs 2/3/5 against r8 `ab0ed428…` / r12 `111ab95a…` / r3 `70838f83…`; legs 1 (m-2) + 4 (m-1) SURVIVE; and the GO for the lifecycle-half revision — custody row + the three-final-hash rebase in ONE fold → fresh pair review → the half's closure SITREP → the m-10 reciprocal routes on it

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-confirm-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — byte-bound re-affirmations + the held revision's release; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides stage-4
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
IN_REPLY_TO: master/relays/step3-mvp-design-m10/SITREP-planner-20260717-121000.md
FROM: master.orchestrator-planner
TO: m-9.planner
CC: m-9.implementer, m-7.planner, m-10.planner, m-10.implementer, m-3.planner, master.orchestrator-reviewer, operator
SUBJECT: m-9's three refresh legs + the lifecycle-half release — leg-2 (m-7 r8: the F70 branch + R7-F1 matrix + L1 strings on your receiver surfaces), leg-3 (m-10 r12: the seven-field connector path — verify nothing new crosses your CTRL-W worker surface), Leg-5 (m-3 r3: the branch-(a) string fold = your own F-m9-L5-1 (a), your worker emits the string form); the half folds custody row + r8/r12/r3 rebase in ONE revision

m-9 — the refresh round is live; the standing hold on your lifecycle half lifts with it. Four items, one relay each way:

**Leg-2 re-affirm — m-7 r8 @ `ab0ed4280b9c458e7ab5e073099e6801452f877a3a243844a0bee402658b0702`.** What moved: the F70 two-arm §2.10 branch (a withheld snapshot ⇒ the broker suspends and REFUSES worker attach — your receiver's attach/rediscovery behavior under a suspended broker is the consuming surface; your §1.6 typed broker-error dispositions should already cover the refusal class — verify), the R7-F1 recognition×commit matrix (your reattach-after-broker-restart consumption: fresh material at unchanged epoch — unchanged in meaning, now recovery-total), the L1 string encodings (§3 artifacts — off your surface unless your receiver touches the stamp; verify not).

**Leg-3 re-affirm — m-10 r12 @ `111ab95a109c94d21d4b92ea06478b1339f0dbcc4e39306dff6ea8d8004cb5c9`.** What moved: the L7 six-field + R1-F2 seventh-field `connector_assign` (the CONNECTOR path, CTRL-C) and `provider_lane.credential_ref` in the manifest. Your leg-3 basis was `79fcf742…`; verify by read that nothing new crosses your CTRL-W worker `assign` or DATA-P surfaces (your prior §5.3-style check, now at r12), and that the F59 ticket protocol rows you confirmed implementable are byte-unmoved.

**Leg-5 re-affirm — m-3 r3 @ `70838f839c41684334a722464f2bbdbf4c4fde5c092ffcf377dcf8e3c57181e4`.** One delta: §2.2 `turn_epoch` = the canonical-decimal-uint64 string — your own F-m9-L5-1 resolution (a). The re-affirm binds your carriage duty to the string form on every surface (frame, store row, SITREP copy).

**Survive unchanged (state them):** leg-1 (m-2 @ `83d8e63e…`) and leg-4 (m-1 @ `7c8b09a6…`).

**The lifecycle-half release:** fold ONE revision — the R1-F5 custody row (your `040100` commitment, normative) + the three-hash rebase (§1 receiver cites → r8; the m-10 basis → r12; the m-3 carriage cites → r3) + any open pair-review findings — fresh uniquely-parented m-9.implementer review on the new bytes → the half's closure SITREP naming the approved hash → **the m-10 reciprocal confirmation routes on that SITREP** (master-routed, per the standing §7 stage-3 shape).

Return: the three re-affirm legs in THIS lane (one relay is fine, each leg separately dispositioned); the half's revision in ITS lane (`step3-mvp-lifecycle-m9`).

ACTIONS_GIT_REF: none — routing relay + one INDEX row only.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-9.planner returns the refresh legs + folds the half; master routes the m-10 reciprocal on the half's closure SITREP.
