## DESIGN — LEG m-7 (PRIMARY) return: the governed home = an ENGINE member section `supply` (engine `v1→2` through my §2.4 numeric successor gate — the ordered-ceiling half's first live lap, exactly as the dispatch named); the closed schema(engine,2), the exact production dogfood descriptor, the composition/runtime typed-fault shape with the silent 120s fallback KILLED, the I-PH line, and five fixture obligations FX-SUP-1..5. Pair review requested (the claim-input pattern — your countersign to master).

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-supply-set-m7
PARENT_DISPATCH_ID: s8-supply-set-amendment
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded owner amendment leg; the slice merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-supply-set-amendment
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/s8-supply-set-amendment/DESIGN-orchestrator-planner-20260712-024511.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-2.planner, s8.planner, s8.implementer
SUBJECT: the gap re-verified live at my seat before ruling (`main.go:118` ambient `os.Getwd()` as the "repo" lane · `:122` empty `Suites{}` · `:124` empty `NamedSuites{}` — three readers, no writer, exactly as characterized); the home ruling re-affirms the §9 fragmentation rejection a THIRD time (a fourth member buys nothing a section does not) and the catalog is REJECTED as the home too (it is the LAW census member — execution/host policy in it would mix two owner grains under one marker)

### 1 — The home: the ENGINE member gains the closed `supply` section; engine `v1→2`

**Ruling: the engine member** (host/execution policy beside `present_layers` — one-writer-per-fact: the engine owns what the engine executes). The section, closed under schema(engine,2):

```text
"version": 2,
"supply": {
  "lane_roots":  { "<lane-id>": "<absolute root path>" },                      # map<string,string>, ids closed by use
  "schema_refs": { "<schema-id>": "<registry-anchored ref>" },                 # map<string,string>
  "suites": {
    "<target-id>": {
      "lane":            "<lane-id>",            # MUST resolve in lane_roots — dangling = composition fault
      "command":         "<relative path>",      # repo-resident, staged, validCommand-conformant (no abs, no traversal)
      "args":            ["<string>", …],
      "timeout_class":   "suite_bounded",        # MUST equal the CheckEntry class at composition
      "timeout_seconds": <int >= 1>              # EXPLICIT; must be <= the class ceiling (120s ratified-interim, s10 sunset)
    }
  }
}
```

All kinds closed (schema(engine,2) validates node kinds, the closed suite-object key set, and the constraints above as SHAPE where expressible — the ≤-ceiling and dangling-lane checks are composition-time policy, §3 below). **Pinned · digest-covered · restart-effective · marker-gated** — automatic under §7 + the r13 capability table, with the engine ceiling advancing `max 1 → max 2` **through my owner relation exactly as the dispatch named**: the numeric successor gate's first live lap; a v2 store × a v1-capability reader ⇒ the typed marker-first phase-0 refusal (the preflight, per the countersigned claim-input leg); a fresh store may GENESIS at v2 directly (genesis writes members; the v2-capable reader loads it), while a pre-supply store transitions `v1→2` through §2.4 (schema-surface change ⇒ the bump is REQUIRED by my own §1.1 rule — the supply section adds key paths). **Rail A stated as instructed: descriptors are acceptance-meaning-bearing, so PRESENT-CLOSED is the right side** — a reader that cannot interpret the supply section refuses the store at the marker; it never executes a suite on half-understood authority inputs.

### 2 — The production dogfood descriptor (exact values)

```text
target id:        "dogfood-battery"
lane:             "repo"
lane_roots.repo:  <the operator-pinned ABSOLUTE path of the work-artifact checkout the dogfood governs — store-specific operator data; the SHAPE (absolute, existing directory) is what this leg pins>
command:          "scripts/dogfood-suite.sh"     # repo-resident lane code (m-3's "repo code the conductor runs"), staged, relative
args:             []
timeout_class:    "suite_bounded"
timeout_seconds:  120                             # the ratified-interim class value, EXPLICIT — the silent fallback dies (§3)
```

The one-line `scripts/dogfood-suite.sh` (`exec go test ./... -count=1`) is **lane content landing in the T9 fold** — named here so master's fold authorizes it explicitly (it is work-artifact-root content, not conductor production code). The T9 real-socket E2 fixture may alternatively genesis its own store whose `supply` carries a fixture-owned lane root + script **through the same governed shape** — governed-by-config either way; ambient cwd nowhere.

### 3 — Composition + runtime (the countersigned shape, now implementable)

1. **Composition (startup, typed, fail-closed):** a missing, malformed, dangling-lane, or out-of-policy (timeout > class ceiling; timeout < 1; unknown timeout_class) descriptor ⇒ a **typed composition fault** (the phase-0/composition class) — the conductor refuses to serve; no partial supply.
2. **Runtime (Spawn, defense-in-depth):** `Spawn` asserts the entry's `TimeoutClass` == the descriptor's class and the concrete timeout is positive-and-ceiling-bounded; a mismatch is the **typed machinery fault**, never a fallback. **The silent `timeout <= 0 → 120s` default (`executor.go:153-156`) is KILLED** — the 120s value survives only as the explicit ratified-interim class ceiling with its s10 sunset.
3. **The lane root** resolves once at composition from `lane_roots` (replacing `os.Getwd()` at `main.go:118`); the registry env's `Lanes`/`NamedSuites`/`SchemaRefs` populate from the `supply` section and nothing else.

### 4 — I-PH

The engine member carries paths and command text — **operator-side config, never seat-delivered** (the locked F7/G(iii) posture; config values appear in no seat surface). Seats see **lane IDs and target ids only**; the executor's child env and `CheckVerdict` remain path-free (the shipped shape, restated as this amendment's hygiene line).

### 5 — Fixture obligations

- **FX-SUP-1:** missing/malformed/dangling-lane/out-of-policy descriptor ⇒ the typed composition fault; the conductor does not serve.
- **FX-SUP-2:** the timeout discipline — `timeout_seconds` absent-or-nonpositive ⇒ fault (the silent default provably dead); class mismatch at Spawn ⇒ the typed machinery fault.
- **FX-SUP-3:** the engine ceiling's first live lap — value-only change at v2 lawful; a v1 store + v2 reader loads clean and the governed `v1→2` transition accepts through §2.4; a v2 store × a v1-capability reader ⇒ the typed marker-first refusal with zero content interpreted (the FX-CFG-8/10 legs instantiated on the numeric half).
- **FX-SUP-4:** I-PH — no lane root or command text in any seat-deliverable surface; lane/target ids only.
- **FX-SUP-5:** the supply provenance — the serve composition's lanes/suites/schema-refs derive from pinned config ONLY (`os.Getwd` absent from the serve path, grep-level assertable).

**Constraints honored, stated:** the TRIPWIRE untouched (genesis fieldspec v5 SHA · FX-CFG-7 · the fieldspec `{v5,v6,v7}` set · the catalog pin `943f07bb…`); the engine ceiling moves ONLY through the relation above; the ratified interim defaults + s10 sunset untouched (120s survives as the explicit ceiling); scope = the supply set only — no adjudication content, no new terminal, no c4/§3 reopen. **Boundary contracts:** m-3 owns what makes a lane root APPROVED and the enum/value-set split (their leg — my `lane_roots` map is the HOME their semantics govern); m-2's no-fieldspec-impact confirm is expected (nothing here touches a registry byte; condition (b) stands if that changes).

Not authorized / not claimed: the PRIMARY leg return only — no code (the machinery lands in the T9 fold under master's pre-staged fence), no m-3 approval semantics authored, no m-2 bytes, no T9 lift.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-supply-set-m7/DESIGN-planner-20260712-005213.md` — run before handoff (result in inline pointer)
- the gap re-verified at the s8 worktree before ruling: `cmd/frank/main.go:118` (`os.Getwd()`), `:122` (empty `Suites{}`), `:124` (empty `NamedSuites{}`, the "repo" lane id in use — kept); the exit-gate test's `check_id: run-suite` usage; `executor.go:153-156` (the silent default this leg kills)
- the home ruling checked against the r13 §1.1 bump semantics (supply adds key paths ⇒ schema change ⇒ the bump is mandatory, not optional) and the §9 fragmentation rejections (re-affirmed)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260712-005213; the s8 worktree read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree carries the pair's in-flight T9/T10 work, untouched by me; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session for the pair review (countersign TO master per the claim-input pattern); m-3/m-2 finalize their confirms against THIS returned home in parallel; master folds the three legs, the pre-staged fence becomes effective, and s8.planner re-lifts T9 with the widened exit condition.
