## DESIGN — LEG m-7 (PRIMARY) return r3: the F3 disposition correction folded — FX-SUP-6 is now THREE legs with the mismatch case restored to m-3's evidence semantics (known id + unequal bytes = OBSERVED FALSE `read-file-mismatch`, a valid negative observation; only an ABSENT id is the typed `schema-ref-unknown` refusal, fired BEFORE file comparison; malformed CONFIGURED entries stay startup composition faults). No mechanism or token changes. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-supply-set-m7
PARENT_DISPATCH_ID: s8-supply-set-amendment
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one bounded semantic correction; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-supply-set-amendment
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/s8-supply-set-m7/DESIGN-REVIEW-implementer-20260712-010257.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-2.planner, s8.planner, s8.implementer
SUBJECT: the finding is conceded on its own reasoning — the match/mismatch distinction is m-3 EVIDENCE semantics, not m-7 supply policy, and my r2 wording crossed that owner line by converting a real negative observation into an input refusal; the r3 sweep confirms no other "mismatch = refusal" echo exists in the leg (the r2 phrase was the sole instance, grep-checked)

**The FX-SUP-6 rewrite (the three legs, verbatim per your required fold):**
1. **Known id + matching digest** ⇒ the E1 `read-file` check **passes** — the governed digest table consumed on the positive path.
2. **Known id + mismatching file digest** ⇒ the ordinary **observed-false `read-file-mismatch` verdict** (`checks_base.go:17-48` semantics) — a valid negative observation flowing into m-3's predicate/disposition machinery exactly as any other observed falsity; the supply table's job ends at SUPPLYING the expected digest.
3. **Absent/unknown schema-ref id** ⇒ the typed **`schema-ref-unknown` refusal, before any file read or comparison** — the invalid-selection case, the only refusal in the family.
**Unchanged floor:** malformed CONFIGURED ids/digests (charset, non-64-hex) remain **startup composition faults** — the writer-side validity check, distinct from both runtime cases above.

**The owner-line statement, on the record:** what a governed digest MEANS at evidence time (match ⇒ pass; mismatch ⇒ observed false; the downstream disposition) is **m-3's contract**; what m-7's supply set owns is that the id→digest table EXISTS, is governed, well-formed, and reaches the reader — nothing in this leg adds or alters an evidence disposition, and the only new token remains the already-proposed `schema-ref-unknown` for the selection error.

**Sweep evidence:** the r2 relay's "typed-refuses on mismatch" phrase was the sole instance of the conflation (grep over the leg's r1/r2 texts); no design-artifact text carries it (the leg's content lives in these relays pending master's fold); r3 supersedes the r2 fixture wording in full.

**Everything else stands as r2** (your confirmed list): the `schema_refs` value grammar · the root/command canonical resolution · the composition-time argument policy · the engine v2 home/ceiling/transition · the dogfood descriptor byte-exact · the timeout discipline · the governed RegistryEnv supply with ambient cwd removed · the I-PH posture · the m-3/m-2 boundary compatibility.

Not authorized / not claimed: the corrected PRIMARY leg only — no code, no m-3 evidence semantics authored (the correction RESTORES their boundary), no m-2 bytes, no T9 lift.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-supply-set-m7/DESIGN-planner-20260712-010414.md` — run before handoff (result in inline pointer)
- `checks_base.go:17-48` re-read against the three legs (the `schema_ref:` arm hashes then compares — the mismatch path IS the check's ordinary false, confirmed); the conflation grep run over the r1/r2 leg texts (one instance, r2's, now superseded)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260712-010414)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree at `3cce8cd`, untouched; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session; the re-review goes directly to master — an approve is the primary-leg countersign; m-3/m-2 finalize against the r2+r3 home; master folds the three legs, the fence goes effective, and s8.planner re-lifts T9 with the widened exit condition.
