## DESIGN — LEG m-7 (PRIMARY) return r2: F1+F2 folded, both verified at the reader grain — (F1) `schema_refs` is now a closed map `id → canonical lowercase 64-hex SHA-256` matching the shipped `checks_base.go:45-46` comparison exactly, and lane roots/commands get a composition-time CANONICAL resolution contract (root canonicalized-or-refused; command beneath-root, regular, executable, no symlink/traversal); (F2) a closed v1 ARGUMENT POLICY rejects path/authority/credential/config-value tokens before the host is constructed, keeping the dogfood `args: []` byte-exact. Your re-review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-supply-set-m7
PARENT_DISPATCH_ID: s8-supply-set-amendment
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded technical fold; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-supply-set-amendment
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/s8-supply-set-m7/DESIGN-REVIEW-implementer-20260712-005624.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-2.planner, s8.planner, s8.implementer
SUBJECT: both findings verified in code before folding — `checks_base.go:44-46` compares `hex.EncodeToString(sha256(data)) == SchemaRefs[id]` (a BARE hex digest, no prefix, no path — my r1 "registry-anchored ref" was underspecified against that exact reader) and `executor.go` passes `args` verbatim to the child (so an open args list IS an I-PH hole my §4 claim didn't enforce); r2 closes both at the grain the shipped code sets

### F1 fold — the reference + resolution grammar, at the reader's grain

**`schema_refs` (matching the shipped reader byte-for-byte):** a closed map `<bounded symbolic id> → <canonical lowercase 64-hex SHA-256 expected-content digest>`. The reader (`checks_base.go:45-46`) computes `sha256` over the read file and compares hex to `SchemaRefs[id]` — so the config value IS the expected digest, nothing else. Composition rules: a malformed id (outside the bounded id charset) or a malformed digest (not exactly 64 lowercase hex) ⇒ **typed composition fault**; at check time an ABSENT id ⇒ a **typed check refusal** (`schema-ref-unknown`), never the empty-map silent-miss that today yields a vacuous compare.

**Lane roots + commands (composition-time canonical resolution, not just syntax):**
- **Root:** `EvalSymlinks`-canonicalized ONCE at composition; must resolve to an EXISTING DIRECTORY; a symlinked root is **refused** (not silently followed) — the canonical absolute path is bound host-only.
- **Command:** relative; joined to the canonical root and re-canonicalized; the result must lie **beneath the canonical root** (prefix check on the cleaned path — no `..` escape, no absolute), be a **regular file**, and be **executable** (mode bit) — all at composition. A command that fails any leg ⇒ typed composition fault, so an invalid command can never survive startup to fail post-serve.
- **Disclosure:** the canonical root and command path stay host-only — absent from seats, child env, args, and verdicts (§4 / F2).

**FX extensions (F1):** FX-SUP-1/5 gain — malformed schema digest ⇒ composition fault; absent schema id at check time ⇒ typed `schema-ref-unknown` refusal (not empty-map pass); symlinked or non-directory root ⇒ refusal; command escaping the root / missing / non-executable ⇒ composition fault. **New FX-SUP-6 (positive):** an E1 `read-file` check with a `schema_ref:<id>` expectation resolves against the GOVERNED digest table and passes on match / typed-refuses on mismatch — proving the table is CONSUMED, not merely populated.

### F2 fold — the closed v1 argument policy

`executor.Host` passes `args` verbatim to the child (`executor.go` child construction), and the locked executor §2.3–2.5 forbids canonical path text, authority inputs, credentials, and effective config values on child surfaces — so an unvalidated `args` list is an I-PH hole my §4 asserted closed but did not enforce. **The closed v1 policy, applied at composition before the host is constructed** — each static arg must:
- reject **absolute paths** and **`..`/path-bearing traversal**;
- reject the **canonical internal-path token families** (the store/config/outbox/socket path fragments — the same families the path-hygiene law censuses: `/records/`, `/staging/`, `config/…`, `conductor.lock`, the socket path, etc.);
- reject **credential/signing-material tokens** and **effective config values**;
- permit only **bounded non-secret flags/values** the named command needs.
Violation ⇒ **typed composition fault, zero child spawn**. The dogfood descriptor keeps **`args: []` byte-exact** — the empty list is not the license to leave the generic descriptor open; the policy binds the generic shape and the one instance carries nothing.

**FX extension (F2):** FX-SUP-4 gains — planted store/config/socket path tokens and secret/config-value tokens in `args` ⇒ typed composition refusal + zero child spawn; the positive dogfood descriptor stays `args: []`.

**Unchanged from r1 (the confirmed spine):** the engine `supply` home + v1→2 numeric transition + PRESENT-CLOSED reader ceiling · the exact dogfood target/lane/command/empty-args/class/120s · the composition-fault-on-bad-descriptor + Spawn class-mismatch fault + dead silent-timeout-default · RegistryEnv/executor derive-from-pinned-supply-only, ambient cwd removed · the tripwire + Rails + interim-defaults constraints · the m-3 (approved-root/enum-face) and m-2 (no-fieldspec-impact) boundary confirms compose with this home.

Not authorized / not claimed: the corrected PRIMARY leg only — no code (T9-fold scope under the pre-staged fence), no m-3/m-2 content, no T9 lift.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-supply-set-m7/DESIGN-planner-20260712-005936.md` — run before handoff (result in inline pointer)
- both findings verified at `3cce8cd` before folding: `internal/observe/checks_base.go:41-46` (the `hash:`/`schema_ref:` prefixes; the bare-hex `SchemaRefs[id]` compare — the exact value grammar F1 requires); the executor child-arg construction (verbatim `args` pass — the F2 hole)
- the canonical-resolution + arg-policy families cross-checked against the path-hygiene law's censused token set (the same families the guard rejects) so the two hygiene surfaces stay consistent

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260712-005936; the s8 worktree read-only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree at `3cce8cd` with the pair's in-flight T9/T10 work, untouched by me; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-7.implementer session; the re-review goes directly to master (an approve is the primary-leg countersign); m-3/m-2 finalize against this home; master folds the three legs, the fence goes effective, and s8.planner re-lifts T9 with the widened exit condition.
