## REVIEW-FOLD — the corrected v4-fold scope (r2): `internal/config/lane_vcs_test.go` reconciled INTO the fold scope under the fence-union rule (an activated amendment's test seams reconcile at activation), licensed for EXACTLY the v4→v5 reader-ceiling re-pin — which realizes m-7's own leg F, so the countersign extends over it without a new owner round; complete the fold on this scope

ROLE: Planner
PHASE: REVIEW-FOLD
AUTHORITY: fold-in-only
DISPATCH_ID: s11-build-fold-v4
PARENT_DISPATCH_ID: s11-build-escalate-config-lock
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator's merge grant stays HELD; grant only on the reissued decision at the post-fold head
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
DESIGN_LOCK_ID: s8-design-m7-config-r13
PLAN_LOCK_ID: s11-comms-thicken-plan-s11.3
IN_REPLY_TO: master/relays/s11-build-fold-v4/SITREP-implementer-20260714-171918.md
FROM: s11.planner
TO: s11.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-7.implementer, m-6.planner, m-3.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: your deviation hold was correct (OUT row untouched, no commit, report before edit — the discipline as written); the addition is granted under the standing fence-union provision, not ad-hoc: the v4 re-home is the activated amendment and the stale reader-ceiling fixture is its test seam; the licensed edit is the ONE re-pin that m-7's leg-F spec already describes (probe marker 4→5, rename to RefusesV5, expected phase-0 `engine-marker` unchanged) — preserving the stale assertion by making production reject v4 at preflight would violate the master ruling and is expressly forbidden

s11.implementer — scope corrected. The authority for the addition is the standing fence-union / mid-amendment rule carried by the boot, the dispatch conditions, and the plan (`if any mid-build amendment activates, its test/fixture seams are reconciled into the fence at activation`): master's (a) ruling IS the activated amendment; `lane_vcs_test.go`'s reader-ceiling fixture is its test seam. The re-pin is not new design — m-7's spec (`s11-build-escalate-config-lock/SITREP-planner-20260714-171302`, leg F) already names "a v5 doc vs ceiling 4 ⇒ the phase-0 `engine-marker` fault" as the required beyond-ceiling behavior; the stale fixture asserted the same probe one version lower. m-7 and master are CC'd here and were TO'd on your deviation report; an objection from either stops the fold — absent one, m-7's countersign of the realized bytes extends over this file.

### The corrected fold scope (binding)

FOLD_SCOPE:
- frank/internal/config/config.go -> in
- frank/test/fixtures/s11_cadence_test.go -> in
- frank/test/fixtures/ -> in
- frank/internal/config/lane_vcs_test.go -> in
- frank/.relays/s11/fold-v4-red-green.md -> in
- frank/.relays/s11/mechanical-tables.md -> in
FOLD_SCOPE_RESULT: all-in

**The added row's license is exactly one edit:** `TestLaneVCSReaderCeilingRefusesV4BeforeSupply` → `TestLaneVCSReaderCeilingRefusesV5BeforeSupply` with its document marker 4→5; the expected phase-0 `engine-marker` rejection stays asserted unchanged. Any other diff in `lane_vcs_test.go` is a deviation. All prior licenses stand (F1–F6 verbatim; legs A–G; the byte-preserved set untouched).

### Completion (unchanged from the r1 dispatch)

Finish the evidence + mechanical-table files, run the targeted set (`./internal/config` now expected fully green + the cadence fixtures + the ten INV-CATALOG laws), commit the ONE bounded fold commit, push, and report under this DISPATCH_ID with the RED/GREEN capture (your A–F RED is already sequence-honest of record in the deviation report — carry it into `fold-v4-red-green.md` with the leg-F/G results and the lane_vcs re-pin noted as fence-union-reconciled). Then m-7 countersigns against their spec; then I re-verify and reissue the merge decision at the new head. Merge stays held.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s11-build-fold-v4/REVIEW-FOLD-planner-20260714-172200.md` — run before handoff (result in the inline pointer).
- The deviation verified consistent with the ruled bytes: under F1/F4 the v4 marker is supported, so an invalid-supply v4 doc correctly reaches member validation (`config-load: engine`) instead of the phase-0 reject — the fixture, not production, is what must move; m-7's leg F pins the same probe at v5. The fence-union provision consumed from the boot (`master-boot-s11-planner` §fence-union), the dispatch (condition set item 4), and the plan-of-record (Global Constraints).

ACTIONS_GIT_REF: none — a scope-correction dispatch; no `frank/` edit by this relay. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260714-172200.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the s11 worktree carries the implementer's authorized uncommitted fold diff (their report of record: `config.go` + `s11_cadence_test.go` modified, nothing else).
Next requested action: operator carries this to s11.implementer; the fold completes on the corrected scope (the lane_vcs re-pin + evidence files + targeted checks + one commit + push) and reports back; m-7 countersigns; I re-verify and reissue the merge decision. An m-7 or master objection to the fence-union reconciliation stops the fold instead.
