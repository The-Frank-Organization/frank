## DESIGN (m-2 planner, rev1 — folds m-2.implementer must-revise `184218`) → re-review: three blockers folded (F13 three-layer, Envelope.To full-projection, F12→F17 record)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-amend-m-2
PARENT_DISPATCH_ID: s6-amend-m-2-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — pair DESIGN-REVIEW re-review request; master/VP/operator gates remain upstream
GRILL_REQUIRED: no — consumes the m-1 fork, does not decide it (§3)
DESIGN_DOC_ID: s6-amend-m-2-transport-codec
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s6-amend-m-2/DESIGN-REVIEW-implementer-20260706-184218.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: rev1 re-review — all three blockers folded (each verified correct against source + my own s5 rulings); codec thesis unchanged; no c1 reopen

**All three blockers verified correct and folded** — not performative: two of them caught me contradicting my *own* prior rulings. Revised doc: `master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md` (§10 rev1 fold-log). The central codec thesis (one canonical `address_list` decoder for validate/lineage/render/delivery) is unchanged and was confirmed sound.

**Blocker 1 — F13 conflated membership with authorization → §2.6/§5/§7.3 rewritten.** Correct: rev0 would have accepted `record_kind=genesis` from any seat, which **contradicts my own s5-escalations M-3(e) ruling** (`genesis` removed from every `seat_scope`, `registry.json:84` vs `:125`; kept in the enum for compat only). Folded the **three-layer split** you prescribed:
1. **membership** — `enum_set` (`validate.go:54-61`, already single-sourced);
2. **authorization** — `seat_scope`/form-offer (`reg.Validate`, `submit.go:47-64`) — **`genesis` in no seat_scope ⇒ a public submit is rejected here** (the fill-time-authority layer);
3. **per-kind required-fields** — the only thing `validateRecordKind` (`submit.go:143`) retains, running *after* `reg.Validate`; its `default→"unknown"` (`:177`) no longer rejects authorized-and-offered kinds (`gate_resolution`/`disposition`/`diagnostics`/`config_change`/owed).
Fixture now carries the **negative leg**: public `record_kind=genesis` rejected at seat-scope; authorized kinds accepted subject to required fields. Also resolves `gate_category: other` without widening `record_kind`.

**Blocker 2 — `Envelope.To` = primary-projection was insufficient → §2.4/§7.1 rewritten.** Correct: rev0 left rendered markdown + `INDEX.md` printing `Envelope.To` (`projections.go:110-113`) while delivery used the full list — the same "one path sees less than another" divergence the codec exists to kill. Folded: the **canonical decoded Header `TO`/`CC` is the recipient truth for EVERY projection** — markdown header, index row, lineage gate, and mailbox intents; none reads `Envelope.To` as the recipient set (it may remain an *optional* m-7 compat/routing projection only). Fixture proves the identical full multi-TO+CC set across all four surfaces.

**Blocker 3 — F12 header fallback under-specified/inexpressible → §4/§5/§7.5 rewritten.** Correct: my rev0 "operator-scoped `free_text` header" is **exactly the free-text-cannot-be-seat-scoped limit I myself established** (s4-wire: `seat_scope` is enum-only; s5-escalations M-1). Folded to the **single branch**: F12 **binds to m-1's F17 operator-only waiver record** (operator-only via record-authorship / channel-stamped FROM, not field seat-scoping — sidesteps the limit); m-2 defines the exact `rationale` (`free_text`)/`waiver_scope`/`retracts` (`id_ref`) rows on that record; the bare `"*"` header is **retired**, and the reviewer-visibility gate reads the waiver *record* (scope + not-retracted), not a presence-only flag.

**Non-blocking confirmations accepted as-is** (no change needed): the F6/F7 codec thesis directional soundness; the parent seam's composability with either fork branch (`render.go:55-63` hides `owner:system`/`system_only` — the conductor-computed dissolution; the widened branch still needs one render==accept list); no c1 reopen under this DESIGN_DOC_ID.

**Unchanged / scope held:** interpretation-only; byte-exact `{accepted,rejected,held}`, the `submit`/`project`/`read` surface, channel-stamped FROM, and I-PH all untouched (no by-construction creep); §3 still consumes the m-1 fork (does not decide it); no c1 fold until the integrated VP co-sign.

Blocked: no — holding for m-2.implementer re-review of the three folded sections (§2.4, §2.6, §4, §7).
Scope drift risk: none — the three folds tighten acceptance/projection/typing toward the locked contract; the codec thesis and all boundaries are unchanged.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/s6-amend-m-2/DESIGN-planner-20260706-190000.md` — OK (below).
- Blockers re-verified against source: `registry.json:84/:125` (`genesis` enum-vs-seat_scope), `submit.go:47-64/:143/:177` + `validate.go:54-61` (three layers), `projections.go:110-113` (index/markdown print `Envelope.To`), `fieldspec.go:15-20`/`registry.json:134` (no operator-scoped-free-text header), m-1 `2026-07-06-s6-transport-amendments.md:43-51` (F17 record class); my own s4-wire + s5-escalations rulings.
- `git status --short` — unavailable: not a git repo (docs workspace; `frank/` probed read-only @ 7e5c527).

ACTIONS_GIT_REF: edited the amendment doc §2.4/§2.6/§4/§5/§7 + added §10 rev1 fold-log (`master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md`); wrote this rev1 relay + appended `master/relays/INDEX.md`; read-only `frank/` probes; no `frank/` edit, no c1 fold, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, probed read-only @ 7e5c527)
Next requested action: m-2.implementer re-reviews the three folded sections; on `approve`, m-2.planner SITREPs master (design complete, held for integration + VP co-sign). No build, no c1 fold, no fork lock in this leg.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
