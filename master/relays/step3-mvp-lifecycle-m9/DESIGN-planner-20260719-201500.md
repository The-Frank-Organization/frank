## DESIGN — m-9 lifecycle half r15: the VP-F82/F83 F59-seam fold (four-field `consume_ticket` wire-vs-row match + derivation-point contract + total consume first-match order + F83 ceiling token; m-10 basis r32→r34) — the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `4c387559…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded consumer/emitter fold on a pair-approved owner amendment; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 4c3875596365e1bef734b7acab805614a0c4e8b7b4994fe47a22c58954808be4
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/RECONCILE-orchestrator-planner-20260719-195939
SUBJECT: r15 folds the VP-F82/F83 F59-seam amendment (m-10 r34 `c6542042…`, pair-approved `195600`) — the widened four-field `consume_ticket`, the derivation-point contract, the total consume first-match order, the F83 ceiling token; `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `4c3875596365e1bef734b7acab805614a0c4e8b7b4994fe47a22c58954808be4`; full-byte verdict requested; on approve → SITREP → the fresh complete reciprocal over m-9 r15 × m-10 r34 (the stage-3 close)

m-9.implementer — r14 was APPROVED; the VP's r4 review then re-opened the F59 seam (F82/F83). r15 folds master's release `…-195939`, bound to m-10 r34's exact bytes (verified §D.3:222–237 + §D.2 F83). m-10 r34 `c6542042…` (pair-approved `195600`, supersedes r32), m-8 r12 `4b670a79…`, m-3 r4 `009df607…`, m-7 r11 `9331ea88…` re-verified byte-exact.

**What r15 folded (bound to m-10 r34):**
- **F82 — the wire shape.** `consume_ticket{ticket_id}` → **`consume_ticket{ticket_id, turn_epoch, canonical_tool_name, canonical_args_digest}`** (§3.2), with the three non-substitutable operand authorities pinned (wire request · `assign`-bound sender association · m-10 durable epoch) so m-10's match is **wire-vs-row, not row-vs-itself** — both ratified negatives (mutated-args, stale-worker-consume) decide at consume. The success predicate (state=ISSUED ∧ epoch stored=presented=durable-current ∧ current-lease-holder ∧ name+digest match) is stated.
- **The derivation-point contract sentence** (the VP's explicit ask) — §3.2: the immutable invocation identity is derived ONCE at §3.1 request construction and held byte-immutable across authorize→consume→execute; the same identity is sent at authorize, presented at consume, and re-derived at the executor guard.
- **Preserved untouched** — the executor-boundary re-check + the actual-invocation capture into `record_tool_outcome` (§3.2; the VP names both correct + required; the split post-consume mutation guard — m-10's half authorize→consume, my half consume→invocation — stands).
- **The total consume first-match order** (§3.3, re-keyed) — unknown-ticket / presented-epoch-above-current = **NO-reply channel faults** (§B.3/§1.6, generation FAILED path) → **stale-sender `STALE_EPOCH`** (now naming EXACTLY the ratified stale-worker negative) → **spent `DUPLICATE_CONSUME`** (incl. the stale-TICKET case via VOID-at-mint — `STALE_EPOCH` is no longer the stale-ticket token) → **`IDENTITY_MISMATCH`** (row stays ISSUED). Overlap winners + §6 fixtures (the all-three overlap ⇒ `STALE_EPOCH`).
- **F83 — the ceiling token** (§3.3): `DENIED_ABOVE_SET` never survives at the §2a ceiling — check (6) `turn_budget_exhausted` (the lawful `turn_exhausted` end) precedes check (7); verified §2.4 carries no contrary claim.
- **Basis rebase m-10 r32→r34** throughout; the §B D-2/D-4/D-5 + cancellation loci carry forward unchanged.

Everything else is byte-identical to the r14-approved content (the D.2 check-1 `authorize_reject` family, the total replay mapping, the H-14 census-completion, the cancellation cuts, and every r9-approved invariant).

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r15 `4c387559…`. Hunt priority: (a) does the four-field `consume_ticket` + the three operand authorities make m-10's match wire-vs-row, and is the derivation-point contract sentence unambiguous? (b) are the executor re-check + `record_tool_outcome` capture preserved intact? (c) is the §3.3 consume first-match order total and correctly keyed (channel faults → stale-sender `STALE_EPOCH` → spent `DUPLICATE_CONSUME` incl. VOID-at-mint stale-ticket → `IDENTITY_MISMATCH` row-stays-ISSUED), with the all-three overlap ⇒ `STALE_EPOCH`? (d) does no locus claim `DENIED_ABOVE_SET` survives at ceiling (F83)? (e) is the m-10 basis fully rebased to r34 with no stale current-basis r32, and all live self-references m-9 r15 × m-10 r34? (f) did the fold disturb any r14-accepted or r9-approved locus? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the r15 SITREP naming the approved hash; master triggers the fresh complete reciprocal over the final pair m-9 r15 × m-10 r34 (field-grain) — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r15 (@ `4c387559…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r15 `4c387559…`; on approve, m-9.planner files the SITREP and master triggers the fresh complete reciprocal over m-9 r15 × m-10 r34 (stage-3 close).
