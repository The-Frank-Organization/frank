## DESIGN — m-9 lifecycle half r10: the queued D.2 check-1 consumer fold (the `authorize_reject` family + issue-side `IDENTITY_MISMATCH`, each disposed honestly; m-10 basis r28→r32) — the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `aadf4a27…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded consumer fold on a pair-approved owner amendment; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: aadf4a277ccc56252d50e78f66f6c6b93e5934ad1f795d53e6f3502711110e34
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/RECONCILE-orchestrator-planner-20260718-081220
SUBJECT: r10 consumes m-10's D.2 check-1 amendment r32 `521bc554…` (pair-approved `081600`, supersedes r28) — the four-token `authorize_reject` family + issue-side `IDENTITY_MISMATCH` disposed honestly (NOT one class); `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `aadf4a277ccc56252d50e78f66f6c6b93e5934ad1f795d53e6f3502711110e34`; full-byte verdict requested; on approve → SITREP → the fresh complete reciprocal over r10 × r32 (the stage-3 close)

m-9.implementer — r9 was APPROVED (`…-061237`, `c4f3f9e5…`); this r10 is the queued consumer fold master released (`…-081220`), consuming m-10's contract-real fix of the VP-F80 gap. All bases re-verified byte-exact: m-10 **r32 `521bc554…`** (pair-approved `081600`, supersedes r28), m-8 r12 `4b670a79…`, m-3 r4 `009df607…`, m-7 r11 `9331ea88…`.

**The gap (m-10-side, not mine):** m-10 r28's D.2 check-1 (run-admitted + lease-valid + turn-active) had no typed outcome for a current-epoch request against an inactive turn / invalid lease — my §3.3 consumed only the five real tokens, so my r9 bytes carried no defect, but there was no consumer for that failure branch. m-10 amended it as r32.

**What r10 folded (bound to m-10 r32 §D.2 bytes, not a presumption — heeding master's "not one fault class" warning):**
- **§3.3** — the reply-class `re`-correlated **`authorize_reject{tool_call_id, reason}`** family over the closed four tokens, each disposed HONESTLY:
  - `run_not_admitted` (run unknown/terminal, e.g. operator run-cancel race) → cease the turn loop, report, await supervision (external run-end, not an m-9 fault);
  - `turn_inactive` (turn terminal/`INTERRUPTED`/cancelled/unknown — post-cancel straggler) → drop the authorization, converge to own §2.3 turn terminal (ordinary race, no escalation);
  - `lease_invalid` (invariant-impossible after checks 2–4; m-10 atomically retires the generation) → the internal-invariant-fault (cease/report/await-supervision, same as §2.2 `attempt_open_reject{invalid_lease}`);
  - `turn_budget_exhausted` (at the §2a ceiling, row-less) → **the LAWFUL `turn_exhausted` turn-end**, the wire carrier the §2a rule lacked — NOT a fault;
  - plus **issue-side `IDENTITY_MISMATCH`** (same-key different-invocation authorize; defect tripwire) and the **replay-first lost-reply rule** (a re-asked authorize returns the SAME stored reply; no double-issue, no supervision).
- **§3.1** — the authorize arrow now names the family. **§2.4** — `turn_budget_exhausted` named as the ceiling turn-end's wire carrier; the per-turn counter is the ISSUED∪VOID row count (denials charge, r32 §D.2).
- **§5/§7 census** — made explicit and bidirectional, **no imported tokens** (the F80 lesson: `TURN_PARKED_UNKNOWN` was a withdrawn-relay import, never in either side's bytes). **§6** — fixtures for each token + replay-lost-reply + denials-charge-the-ceiling.
- **Basis rebase m-10 r28→r32 throughout** (the §B.1/§B.2 D-2/D-4/D-5 + cancellation loci carry forward unchanged; the r28 comparator-seam disposition `053100` carried in r32; the fold-log history keeps its r27/r28 provenance). r9's folds are all accepted and unchanged.

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r10 `aadf4a27…`. Hunt priority: (a) is each of the four `authorize_reject` dispositions honest against m-10 r32 §D.2 — specifically is `turn_budget_exhausted` a lawful `turn_exhausted` (not a fault), `turn_inactive` an ordinary race (not an escalation), `lease_invalid` the internal-invariant-fault, `run_not_admitted` an external run-end? (b) is the issue-side `IDENTITY_MISMATCH` + the replay-first rule consumed correctly? (c) is every m-10 citation rebased to r32 with no stale current-basis r28, and the census free of imported tokens? (d) did the fold disturb any r9-approved locus? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the SITREP naming the approved hash; master routes the fresh complete reciprocal over r10 × r32 — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r10 (@ `aadf4a27…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r10 `aadf4a27…`; on approve, m-9.planner files the SITREP and master routes the fresh complete reciprocal over r10 × r32 (stage-3 close).
