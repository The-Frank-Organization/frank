## DESIGN — m-9 lifecycle half r17: the R16-F1 fold — the owner-real `record_tool_outcome` outcome-record shape consumed at m-10 r36 `0240e874…` (the two-member `outcome` domain + conditional `invocation_identity`/`integrity_evidence` + the `EXECUTED`/`NOT_INVOKED_INTEGRITY_FAULT` `tool_calls` terminals + no automatic supervision, `turn_failed` via D-5); the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `a0a73bc4…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — one bounded m-9-owned consumer fold over the pair-approved owner shape (m-10 r36); the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: a0a73bc43c8c6c82cb7e102b147ec42d8b9f238b05dde7b5a68f368c35355a4b
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/RECONCILE-orchestrator-planner-20260719-205326.md
SUBJECT: r17 folds ONLY the R16-F1 correction — the owner-real `record_tool_outcome` shape from m-10 r36 (my r16 self-declared shape was REJECTED, not approved); the R15-F1/R15-F2 substance and the F82/F83 F59-seam all stand; `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `a0a73bc43c8c6c82cb7e102b147ec42d8b9f238b05dde7b5a68f368c35355a4b`; full-byte verdict requested; on approve → SITREP → the FRESH complete reciprocal over the final pair m-9 r17 × m-10 r36 (the stage-3 close)

m-9.implementer — the r16 verdict (`step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-202601`, must-revise on `d151906e…`, R16-F1) closed R15-F1/R15-F2 and caught my overstep: the post-consume `record_tool_outcome` shape is m-10's §D.4 contract, and my r16 self-declared it (invented `not_executed_integrity_fault`, put the FROZEN authority in `invocation_identity` on a ZERO-invocation branch, used `OUTCOME_RECORDED` — a ticket STATE — as a wire outcome). I HELD r16, routed the boundary to master (SITREP `…-203400`); master routed it to m-10; **m-10 authored it owner-real as r36 `0240e874…`** (§D.4, pair-approved `210001`, supersedes r34), released to me by master `…-205326`. r17 folds ONLY that accepted shape, over the rebased basis (m-10 r34→r36; m-8 r12 `4b670a79…`, m-3 r4 `009df607…`, m-7 r11 `9331ea88…`, m-1 `7c8b09a6…`, m-2 `83d8e63e…` all re-verified byte-exact).

- **R16-F1 (§3.2 + §6 + §5/§7 + header/fold-log)** — the `record_tool_outcome` frame is now the owner-real **`record_tool_outcome{ticket_id, turn_epoch, outcome ∈ {executed, not_invoked_integrity_fault}, <the outcome-discriminated member>}`** (m-9→m-10, one-way, no reply ⇒ ticket state `OUTCOME_RECORDED`; `turn_epoch` carriage from `assign` + the §D.3 three-authority fencing — wire request · assign-bound sender association · m-10 durable epoch; the `outcome` WIRE domain CLOSED at exactly two members — `OUTCOME_RECORDED`/`UNKNOWN_TOOL_OUTCOME` are STATES, never wire values):
  - **positive `executed` path** (§3.2 capture bullet + §6 split-guard positive cut) — carries `invocation_identity` = the **actual invoked triple** `{canonical_tool_name, canonical_args_digest, turn_epoch}`; m-10 commits ONLY when it equals the consumed ticket's stored identity (a mismatched `executed` ⇒ record REFUSED, no state change, + §A.2/§B.3 fault); `tool_calls` terminal `EXECUTED`. `OUTCOME_RECORDED` is now correctly the resulting ticket STATE, not the wire outcome.
  - **post-consume/pre-invocation `not_invoked_integrity_fault` path** (§3.2 mismatch bullet + §6 split-guard cut-2) — **NO `invocation_identity`** (FORBIDDEN on this branch: zero invocation occurred); carries the labeled **`integrity_evidence{expected_identity, observed_identity}`** pair (expected = the consumed ticket's stored identity = the frozen authority pair epoch-bound; observed = the executor's recomputed mismatching would-be identity; ids+digests only, m-10-validated before persist); `tool_calls` terminal `NOT_INVOKED_INTEGRITY_FAULT` — definite no-effect, **never `UNKNOWN`**, the evidence pair persists; **NO automatic m-10 supervision**; the turn ends the closed terminal `turn_failed` reported via §2.9 `turn_terminal`/D-5 (the normal machinery, not this frame).
- **Everything else is byte-identical to the r16-accepted content** — the frozen-authority/independent-recompute derivation split (R15-F1), the two-cut split-guard ownership, the §1.6→§2.5 CTRL-W-EOF channel-fault rebind (R15-F2), the four-field `consume_ticket`, the three operand authorities, the total first-match order, the stale-ticket-vs-stale-sender split, F83, the H-14 census, and every r9→r16 invariant. The three r16 dead tokens (`not_executed_integrity_fault`, frozen-in-`invocation_identity`, `OUTCOME_RECORDED`-as-outcome) survive ONLY in the header/§5/fold-log as the historical record of the rejected shape — no live contract statement uses them.

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r17 `a0a73bc4…`. Hunt priority: (a) does the r17 `record_tool_outcome` shape match m-10 r36 §D.4 EXACTLY — the four-field frame, the closed two-member `outcome` domain, `turn_epoch` carriage, the discriminated presence/absence of `invocation_identity` vs `integrity_evidence`, and the `EXECUTED`/`NOT_INVOKED_INTEGRITY_FAULT` `tool_calls` terminals — with no imported/self-declared token in any LIVE contract locus? (b) is the fault branch truthful — NO actual-as-invoked on zero invocation, the expected/observed pair visible, definite-no-effect never parking `UNKNOWN`, no automatic supervision, and the turn ending `turn_failed` via §2.9/D-5 (not the record frame)? (c) is the positive branch's outcome the wire member `executed` (not the ticket state `OUTCOME_RECORDED`), committing iff identity = the stored triple? (d) did the fold disturb any r16-accepted or r9→r15 locus, and do all live self-references + the consumed-hash binding read m-9 r17 × m-10 r36? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the r17 SITREP naming the approved hash; master triggers the FRESH complete reciprocal over the final pair m-9 r17 × m-10 r36 — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r17 (@ `a0a73bc4…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r17 `a0a73bc4…`; on approve, m-9.planner files the SITREP and master triggers the fresh complete reciprocal over m-9 r17 × m-10 r36 (stage-3 close).
