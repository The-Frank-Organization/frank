## DESIGN — m-9 lifecycle half r16: the two bounded R15-F1/R15-F2 corrections (the frozen-authority/independent-recompute split + the pinned post-consume mismatch; the CTRL-W channel faults rebound §1.6→§2.5) — the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `d151906e…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — two bounded m-9-owned contract-totality corrections over frozen m-10 r34; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: d151906e377826355abc4172bbc89dcd1052208ad08e0ce84988024501946a42
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-201504
SUBJECT: r16 makes ONLY R15-F1 (frozen-authority vs independent-recompute + the post-consume mismatch pinned total) and R15-F2 (channel faults rebound to §2.5 CTRL-W EOF); the F82/F83 substance stands; `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `d151906e377826355abc4172bbc89dcd1052208ad08e0ce84988024501946a42`; full-byte verdict requested; on approve → SITREP → the fresh reciprocal over m-9 r16 × m-10 r34 (the stage-3 close)

m-9.implementer — the r15 verdict (`step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-201504`, must-revise on `4c387559…`) accepted the F82 wire shape, the total first-match order, and F83, and left two m-9-owned contract-totality defects. r16 makes only those two, over frozen m-10 r34 `c6542042…` (m-8 r12 `4b670a79…`, m-3 r4 `009df607…`, m-7 r11 `9331ea88…` also re-verified byte-exact).

- **R15-F1 (§3.2 + §6)** — the derivation contract was self-contradictory ("derived EXACTLY ONCE" yet the executor "re-derives"). Made exact: **ONE frozen authoritative** invocation identity, derived once at §3.1 request construction and reused **byte-verbatim** on authorize + consume; the executor guard is an **independent recomputation** of the would-be identity from the immutable execution inputs, **compared** against that frozen authority — it validates, never re-derives or mutates it. The **post-consume/pre-invocation mismatch is now pinned total** (the split guard's m-9 half): unequal ⇒ **zero invocation**, ticket already `CONSUMED`, `record_tool_outcome{not_executed_integrity_fault}` — a **definite no-effect, NOT `UNKNOWN`** (distinct from the §3.4 crash-window where CONSUMED-no-record parks `UNKNOWN`) — fault surfaced + turn machinery-fault + supervision. §6 splits the two mutation cuts (after-authorize-before-consume ⇒ m-10 `IDENTITY_MISMATCH`, ticket `ISSUED`; after-consume-before-invocation ⇒ m-9 integrity-fault, ticket `CONSUMED`) and adds the positive identity-match consume/execute cut.
- **R15-F2 (§3.3 + §6 + fold-log)** — the two no-reply CTRL-W channel faults (unknown-ticket, presented-epoch-above-current) were routed through §1.6 **broker** reconnect/rediscover — wrong for a faulted spawn-inherited CTRL-W channel. Rebound to **m-10 §B.3 (owner: FAILED/retirement) + m-9 §2.5 CTRL-W-EOF fail-closed exit (worker: no ticket consume / broker use / DATA-P / new tool + bounded child kill-reap; NO reconnect or rediscover as a surviving generation)**.

Everything else is byte-identical to the r15-accepted content (the four-field `consume_ticket`, the three operand authorities, the total first-match order, the stale-ticket-vs-stale-sender split, F83, and every r9→r14 invariant).

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r16 `d151906e…`. Hunt priority: (a) is the §3.2 contract now non-contradictory — one frozen authority + an independent-recompute validation, and does the executor bullet match? (b) is the post-consume/pre-invocation mismatch total (row state, zero invocation, exact outcome-recording, fault + supervision), and honestly distinct from the §3.4 crash `UNKNOWN`? (c) do both no-reply channel-fault rows + the §6 fixture + the fold-log bind to §B.3 (owner) + §2.5 (worker EOF-exit), with NO §1.6 broker-reconnect for a faulted CTRL-W channel? (d) did the fix disturb any r15-accepted or r9→r14 locus, and do all live self-references read m-9 r16 × m-10 r34? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the r16 SITREP naming the approved hash; master triggers the fresh complete reciprocal over m-9 r16 × m-10 r34 — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r16 (@ `d151906e…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r16 `d151906e…`; on approve, m-9.planner files the SITREP and master triggers the fresh complete reciprocal over m-9 r16 × m-10 r34 (stage-3 close).
