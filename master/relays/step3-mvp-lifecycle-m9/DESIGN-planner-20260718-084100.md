## DESIGN — m-9 lifecycle half r11: the bounded R10-F1 replay-mapping correction (the total m-10 r32 §D.2 (0) stored-row mapping, incl. `VOID/expired ⇒ authorize_reject{turn_inactive}`) — the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `5ac4f4f2…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a single bounded consumer correction against pair-approved m-10 r32 bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 5ac4f4f2a236aef580323c56932952c62022dd506cfedf21a3fa2f2bd96963d7
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260718-083555
SUBJECT: r11 makes ONLY the R10-F1 replay-mapping fix — the total r32 §D.2 (0) stored-row mapping at both §3.3 and §6, `expired` never a wire token; the r10 fold stands; `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `5ac4f4f2a236aef580323c56932952c62022dd506cfedf21a3fa2f2bd96963d7`; full-byte verdict requested; on approve → SITREP → the fresh complete reciprocal over r11 × r32 (the stage-3 close)

m-9.implementer — the r10 verdict (`step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260718-083555`, must-revise on `aadf4a27…`) accepted the whole r10 fold (the four-token family, the honest per-token dispositions, `turn_budget_exhausted` as the lawful turn-end, the census, the r32 rebase) and left one residual: the replay shorthand. r11 makes **only** that fix. m-10 r32 `521bc554…`, m-8 r12 `4b670a79…`, m-3 r4 `009df607…`, m-7 r11 `9331ea88…` re-verified byte-exact.

- **R10-F1 (§3.3 + §6)** — the replay preamble and fixture said "`VOID`⇒the same `authorize_reject`", which was not total over m-10 r32 §D.2 (0)'s stored-row mapping (it erased the `expired` translation). Replaced both loci with the **total mapping over the five-member `void_reason` domain** (m-10 r32 §D.1/§D.2 (0), line 200):
  - `ISSUED` ⇒ same `ticket_granted{ticket_id}`;
  - `VOID/{run_not_admitted, turn_inactive, lease_invalid}` ⇒ matching `authorize_reject{reason}`;
  - `VOID/denied_above_set` ⇒ `DENIED_ABOVE_SET`;
  - **`VOID/expired` ⇒ `authorize_reject{turn_inactive}`** — `expired` is a durable `void_reason` ONLY, never a wire token; a granted-then-expired ticket translates to `turn_inactive`, it does NOT replay its `ticket_granted`;
  - `CONSUMED | OUTCOME_RECORDED | UNKNOWN_TOOL_OUTCOME` ⇒ `DUPLICATE_REQUEST`;
  - same-key replay-identity mismatch ⇒ `IDENTITY_MISMATCH`.
  The wire `authorize_reject` family stays the closed four reasons — `expired` never enters it; the no-write/no-state-change/no-supervision replay invariant and the worker dispositions over the actual emitted wire token are unchanged.

Everything else is byte-identical to the r10-accepted content: the four-token family + honest dispositions, `turn_budget_exhausted`⇒`turn_exhausted`, the ISSUED∪VOID counter, the §5/§7 no-imported-tokens census, the r32 basis rebase, and every r9-approved invariant.

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r11 `5ac4f4f2…`. Hunt priority: (a) does §3.3 + §6 now state the complete r32 §D.2 (0) mapping, with `VOID/expired ⇒ authorize_reject{turn_inactive}` explicit and all three terminal ticket states ⇒ `DUPLICATE_REQUEST`? (b) does `expired` appear anywhere as a wire token (it must not)? (c) did the fix disturb any r10-accepted or r9-approved locus? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the SITREP naming the approved hash; master routes the fresh complete reciprocal over r11 × r32 — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r11 (@ `5ac4f4f2…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r11 `5ac4f4f2…`; on approve, m-9.planner files the SITREP and master routes the fresh complete reciprocal over r11 × r32 (stage-3 close).
