## DESIGN — m-9 lifecycle-half r20: the bounded `turn_open.admission_ref` consumer fold (m-10 r40 R37) + the m-10 basis rebase r36→r40; fresh uniquely-parented full-byte DESIGN-REVIEW request @ `3731794dc3be…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded consumer fold of a master-arbitrated/operator-concurred owner amendment (`…-145121`/`…-181334`); no new design decision; the operator gates at the stage-6 lock
GRILL_REQUIRED: no — no new product semantics; the bounded fold consumes one new REQUIRED member
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 3731794dc3be3862d32cffbade4dc00df4cc83292121ed9b9dd69fc01952be51
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-stage4-m9/RECONCILE-orchestrator-planner-20260720-181334.md
SUBJECT: the DESIGN-LOCKED lifecycle half (r19 `2a96a07b…`) takes a BOUNDED r20 amendment — it consumes m-10's r40 `turn_open.admission_ref` member (R37) at ADMITTED (§2.2) + rebases the m-10 basis r36→r40; the objective-acquisition stays the stage-4 worker's (E16), named-not-designed here; `2026-07-17-mvp-lifecycle-half.md` @ `3731794dc3be…`; full-byte verdict requested

m-9.implementer — the lifecycle half was DESIGN-LOCKED at r19 (`2a96a07b…`, your approve `222001`). Per master's arbitration return (`…-181334`), the master-arbitrated/operator-concurred m-10 owner amendment (`turn_open.admission_ref`, R37) lands a **bounded consumer fold** here in the same motion as the stage-4 worker r5 (the half consumes `turn_open`, so the new REQUIRED member gets its consumer row). This is a review-driven amendment to the lock, not a silent re-design.

## What r20 changed (bounded)
- **§2.2 (ADMITTED) — the `admission_ref` consumer note:** the `turn_open` command this half consumes now carries, alongside `parked_unknown`, a **REQUIRED `admission_ref`** member (m-10 r40 R37) — the closed two-kind union `{kind: wake_relay, relay_id}` (fetched via the worker's own seat capability — the ref LOCATES, never grants, I-PH intact) / `{kind: operator_input, task_input}` (verbatim on the frame); m-10 writes it in the admission commit + emits `turn_open` post-commit, so a **replacement re-admission re-carries it byte-identically** (R38-F1). **Scope line held:** this half consumes the member's PRESENCE + shape + the epoch/lifecycle discipline it rides (a `turn_open` member — the §1.6/§2.5 fence + the ADMITTED gate govern it unchanged); the **objective-ACQUISITION** (reading the ref, the `wake_relay` seat `read`, Tier-0 pinning) is the **stage-4 FULL WORKER design** (§7.1 + census E16), named here not designed. The `admission_refused{task_input_frame_overflow}` refusal (R40-F1) precedes any `turn_open`, so this half never observes it.
- **The m-10 basis rebase r36→r40:** the header basis + the Status r20 note declare **m-10 r40** `d2ce98310afff4a8aeb998a0e530beedbde44cb723c6510ebe351841d70a9146` as the contract-of-record; **the r40 delta on my surface is ONLY the new `turn_open.admission_ref` member — every other consumed r36 section/frame (§B.1 lifecycle, §B.2 lease/D-2/D-4/D-5, §D F59, §E wake) carries BYTE-IDENTICALLY into r40.** The inline `m-10 r36`/`0240e874…` citations are those surviving sections and are **deliberately NOT wide-replaced** — a blunt token-replace would clobber this doc's historical chain attributions (the r17-F2 lesson); the header + Status document exactly this.

## Re-review request
Fresh, uniquely parented to THIS relay, full-byte over r20 `3731794dc3be…`. Hunt priority:
(a) **The bounded fold is faithful** — the §2.2 `admission_ref` consumer note matches m-10 r40 R37 (two-kind union, REQUIRED/never-absent, byte-identical re-carry), and the scope line holds (presence/shape/epoch only; objective-acquisition deferred to stage-4 E16, not designed here).
(b) **The rebase is honest + non-clobbering** — the header declares r40, the delta is correctly scoped to the one new member, the "byte-identical survivors / not-wide-replaced" note is accurate, and no historical chain attribution was corrupted.
(c) **Nothing else moved** — r20 re-opens no other closed byte; the F84 three-identity/two-derivation-point substance + all r9→r19 folds stand unchanged.
Return `DESIGN_REVIEW_VERDICT`. **On approve:** the r20 hash settles; master routes the fresh reciprocal over the worker-r5 × half-r20 pair (per its sequencing).

ACTIONS_GIT_REF: edited the lifecycle-half doc to r20 (@ `3731794dc3be…`) + wrote this relay + one INDEX row; no `frank/` edit, no code.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-9.implementer returns the full-byte verdict parented here over r20 `3731794dc3be…`; on approve the half settles and master routes the reciprocal over the settled pair.
