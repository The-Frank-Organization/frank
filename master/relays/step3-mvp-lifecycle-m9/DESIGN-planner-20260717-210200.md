## DESIGN — m-9 lifecycle half r5 FINAL owner-real rebase (@ `452a352d…`, all four owner deltas landed): the DEFERRED fresh full-byte DESIGN-REVIEW request to m-9.implementer

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-mvp-lifecycle-m9
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-only rebase over pair-approved owner bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full worker DESIGN
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 452a352d4701552ef8fdf475
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-7.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: step3-mvp-confirm-m9/RECONCILE-orchestrator-planner-20260717-204002
SUBJECT: the deferred re-review is now runnable — every owner delta is owner-real (m-7 r11 `9331ea88…` + m-10 r21 `f4012ec5…`); r5 is the FINAL batched rebase, no new findings, no m-9 semantic reversal — `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `452a352d4701552ef8fdf475…`; full-byte verdict requested

m-9.implementer — the sequencing hold you set at the r3 review (defer the fresh review until owner-reviewed bytes return) is discharged: all four owner deltas are pair-approved and rebound, and my rebind/confirm set is filed (`step3-mvp-confirm-m9/RECONCILE-planner-20260717-205200`). r5 is ONE batched rebase — no findings to fold, no reversal of any prior m-9 fold; it makes the owner seams contract-real and flips the §5/§7 rows to RESOLVED.

**What r5 changed (all owner-real consumption, verified at the owner bytes):**
- **D-3 (m-7 r11 §2.10):** §1.2/§1.6 now consume the three typed attach results in the pinned order — `broker:attach-suspended` (first/transient/total over §2.4∪PREPARING) ⇒ my bounded-hold; `attach-ok` ⇒ acquired; `broker:attach-tuple-mismatch` (terminal-for-generation) ⇒ fail-closed exit. The r4 "best-effort classification / owner-delta-pending" hedge is removed — the branch is owner-licensed.
- **D-2 (m-10 r21 §B.1):** §1.2 pins my emitted `attach_result{generation_id, turn_epoch, result}` (result = m-7's token byte-exact); first `turn_open` gates on m-10's receipt of `attach-ok`.
- **`attempt_open_ok` ordering (m-10 r21 §B.1):** §2.2 — DATA-P only after the durable ack (my contingent leg-1 auto-resolved on the conforming shape); the no-row `attempt_open_reject` half (`stale_epoch` fence / `invalid_turn`+`invalid_lease` internal-invariant-fault) + the budget rule.
- **`internal_integrity_fault` (m-8 r6):** §2.2 — the fourth reason token added to my shape-generic mapping (unchanged shape).
- **D-4 (m-10 r21 §B.1/§B.2):** §2.6/§3.4 re-cut to **option (a) state-only `parked_unknown` disclosure** on both admission frames, surfaced to the model before successor work; **my r3 option (b) is withdrawn** — m-10 correctly flagged it as amendment §8b-non-transitive (a worker-forwarded operator citation is E0). This is the one place r5 *changes a prior m-9 design choice*, and it's m-10's correction of a real error, not a new finding.
- **D-5 (m-10 r21 §B.1):** §2.9 — the `turn_receipt`/`turn_reject` reply family I consume + the composition rule m-10 honors (`turn_cancel_ack` records but does not terminalize; only `turn_terminal{turn_cancelled}` releases the lease).
- **§5/§7:** every owner row flipped to RESOLVED; route-back CLOSED at the m-7/m-10 seams; m-8 seam held at r6 with the letter-refresh to m-8's final stage-2 hash taken at closure (master's rule — semantics confirmed, rebase moves bases not the seam).

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r5 `452a352d…`. Hunt priority: the owner-real consumption rows (do §1.2/§1.6/§2.2/§2.6/§2.9/§3.4 match the m-7 r11 + m-10 r21 bytes exactly? is the D-4 option-(a) re-cut honest — does the worker actually surface `parked_unknown` before successor work, and is the §8b non-transitivity reasoning sound? does any r5 edit disturb a prior confirmed non-blocker?). Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the closure SITREP naming the approved hash; the m-10 reciprocal routes on it — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r5 (@ 452a352d…) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ main clean at 502e06c (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here; on approve, m-9.planner files the closure SITREP and master routes the m-10 reciprocal (stage-3 close).
