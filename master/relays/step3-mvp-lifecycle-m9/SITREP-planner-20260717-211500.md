## SITREP — m-9 lifecycle half r5 took a must-revise (all four findings correct, none rejected); r5 HELD; the single r6 fold is BLOCKED on m-8's pair-approved cancellation/epoch bytes (m-8 r7 = must-revise, verified) — dependency chain surfaced to master

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-mvp-lifecycle-m9
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report + a dependency surfacing; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
IN_REPLY_TO: step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260717-210652
FROM: m-9.planner
TO: master.orchestrator-planner
CC: m-9.implementer, m-10.planner, m-8.planner, m-7.planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-9-model-runtime
SUBJECT: r5 @ `452a352d…` must-revise (R5-F1..R5-F4, all verified correct); the stage-3 close does NOT land on r5 — two findings block on m-8 (r7 `b805edab…` is itself must-revise, cancellation seam rejected); r5 HELD, ONE r6 folds all four after m-8's pair-approved cancellation/epoch bytes; the m-9-local resolutions previewed; the D-5 comparator + cancellation consumer seam will route to m-10 at the r6 fold

## SITREP — m-9 pair / m-9-model-runtime
Phase: DESIGN (r5 must-revise; HELD pending an m-8 dependency; the stage-3 close is deferred, NOT reached)
Current artifact: `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ `452a352d…` (r5, HELD — not edited)
Claims:
- The r5 re-review returned **must-revise** with four findings; I verified each at the bytes; **all four are correct, none rejected** — E1 — source: `step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260717-210652`.
- **R5-F1 (BLOCKED on m-8) — the cancellation seam.** My r5 maps provider `cancelled`→`stream_cancelled`→`turn_cancelled` as a single path, but m-8's own cancellation disposition is **in flux**: m-8 r7 `b805edab019400d1…` is **must-revise** (verified — `step3-mvp-design-m8/DESIGN-REVIEW-implementer-20260717-210409`, its reviewer rejects r7 mapping every cancellation to `transport_failed`/`phase=failed` as false for the zero-wire branch + mislabeling a post-invocation operator cancel). I cannot pin my cancellation mapping until m-8 returns pair-approved bytes, and I must then distinguish **two cancellation cuts** — (i) cancel BEFORE transport invocation (zero wire) and (ii) cancel AFTER invocation (wire crossed, runs its server course) — which my r5 conflated. E1.
- **R5-F2 (BLOCKED on m-8) — incomplete m-6/r6 fold + stale bases.** Correct on both legs: (a) the doc carries mutually stale m-8 bases (header still cites r4 `168c24b7…`; §7/fold-log cite r6 `ab63f6eb…`; §2.2 still says "PROPOSED/to-confirm/three tokens" while adding the fourth) — not one byte-bound owner basis; (b) I under-folded my OWN triple-confirm leg-3 — r5 has **no m-8 DATA-P `EPOCH_AHEAD` consumer branch** and uses `STALE_EPOCH` only for the m-10 attempt-open/executor paths, not the m-8 DATA-P epoch path. Both fold onto m-8's final hash. E1.
- **R5-F3 (m-9-LOCAL, resolvable now) — the second `parked_unknown` gate isn't sequenced.** Correct: r5 says the worker surfaces `parked_unknown` from BOTH `turn_open` and `attempt_open_ok`, but ASSEMBLING happens before attempt-open, so a list that GREW between the two frames isn't guaranteed to reach the model before DATA-P. **Committed resolution (ready to fold):** the `turn_open` list enters context BEFORE ASSEMBLING; on `attempt_open_ok`, compare its closed list against what was surfaced — any new item **BLOCKS DATA-P, is surfaced, and the request is REASSEMBLED** (fail-closed to no-DATA-P if reassembly can't complete); + a fixture where the durable list changes between frames proving no DATA-P/tool work precedes the new-item surface. E1.
- **R5-F4 (SPLIT) — D-5 idempotency field domains open + §B.1/§B.2 miscitations.** Correct: m-10 r21's equivalence predicate keys over `{terminal, attempts_summary_ref?}` and `{partial_disposition}`, but r5 leaves `attempts_summary_ref?` (type/absence/equality) and `partial_disposition` (prose, not a closed domain) undefined — so m-10 can't deterministically distinguish an equivalent resend from a conflict. Also my D-2/D-4/D-5 citations say "§B.1" but the attach gate / D-5 consumption / D-4 disclosure live under m-10's later §B subsections. **m-8-independent (ready to fold):** DROP `attempts_summary_ref?` for the MVP (YAGNI — the attempts live in m-10's `provider_attempts` rows; the D-5 equivalence key over `turn_terminal` becomes just the closed `{terminal}` enum), and correct the citations. **Coupled to m-8 (waits):** the exact `partial_disposition` closed domain — its cancellation identity depends on the m-8 zero-wire-vs-wire-crossed cancellation cut (R5-F1), so it is designed to match m-8's final shape, then routed to m-10 for the comparator confirmation. E1.
Actions:
- Reviewed + verified the four findings + the m-8 r7 must-revise dependency + the m-10 §B loci; wrote this SITREP. No doc edit (r5 HELD — a single r6 avoids the churn the reviewer's Next-action calls for).
ACTIONS_GIT_REF: wrote only this relay + its INDEX.md row; no design-doc edit (r5 held), no frank/ edit, no code
FINAL_GIT_STATUS_SHORT:
unavailable — cwd is not a git repo (docs workspace); `frank/` untouched at `502e06c` (s11-close)
Done: the r5 verdict verification; the dependency diagnosis; the R5-F3 + R5-F4-local resolutions designed (ready to fold).
Not done (blocked): the r6 fold — held for m-8's pair-approved cancellation/epoch bytes; the closure SITREP / m-10 reciprocal / stage-3 close do NOT land on r5.
Blocked: on **m-8 returning pair-approved cancellation + DATA-P-epoch + final-hash bytes** (m-8 r7 is must-revise). The dependency chain: **m-8 cancellation disposition → my r6 (`partial_disposition` domain + the two cancellation cuts designed to match) → m-10 D-5 comparator confirmation** (routed at the r6 fold, through master).
Scope drift risk: none — r5 pins the m-9 side of every landed delta; the open items are genuine cross-owner dependencies, not m-9 drift.
Tests / verification: relay-lint on this file (E2); m-8 r7 `b805edab…` + its must-revise verdict recomputed; m-10 r21 D-2/D-5/D-4 loci read.
Next requested action: master tracks the dependency chain; when m-8's pair-approved cancellation/epoch bytes land, m-9 folds **ONE r6** (R5-F1..F4: the m-8 cancellation + DATA-P-epoch consumption, the four-token/hash rebase, the R5-F3 second-gate sequencing, the R5-F4 `attempts_summary_ref?` drop + `partial_disposition` domain + citations), routes the D-5-comparator + cancellation consumer seam to m-10 through master, and returns a fresh uniquely-parented DESIGN request. No closure SITREP / reciprocal / lock consumes `452a352d…` or r5.
