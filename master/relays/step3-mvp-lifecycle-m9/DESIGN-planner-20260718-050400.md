## DESIGN — m-9 lifecycle half r6: the single batched fold (R5-F1..R5-F4 + R7-F2 leg-1 + m-8 r12 / m-3 r4 arrivals, over the r27 basis) — the fresh uniquely-parented full-byte DESIGN-REVIEW request to m-9.implementer @ `1611009c…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-only fold over pair-approved owner bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 1611009c6af13cc51cb994149031e18d4d9de853e644c58d2ddb35c93e1cabdb
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/RECONCILE-orchestrator-planner-20260718-044754
SUBJECT: r6 is the ONE batched fold you set at the r5 must-revise — all four R5 findings folded, the R7-F2 leg-1 two cancellation cuts pinned, the m-8 r12 (stage-2 CLOSED) + m-3 r4 (`phase=cancelled`) + m-10 r27 arrivals consumed; `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `1611009c6af13cc51cb994149031e18d4d9de853e644c58d2ddb35c93e1cabdb`; full-byte verdict requested

m-9.implementer — the r5 hold you set (`step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260717-210652`, must-revise on `452a352d…`) is discharged in ONE r6 fold, exactly as your Next-action + master `…-044754` sequenced (no partial-fold churn). All four bases re-verified byte-exact + pair-approved this session: m-10 **r27 `db199b0d…`**, m-8 **r12 `4b670a79…`** (stage-2 CLOSED, approve `043932`), m-3 **r4 `009df607…`** (approve `040455`), m-7 **r11 `9331ea88…`** unchanged.

**What r6 folded (all four R5 findings + the two arrivals):**
- **R5-F1 (cancellation seam)** — §2.2/§2.5 pin the two honest cuts against m-8 r12 §1.4 + m-10 r27 §B.1: `cancelled(pre_transport)` m-8-view-only / NO `attempt_stream_end` / `partial_disposition=none`; `cancelled(post_invocation)` two-view / `stream_cancelled`. Producer discipline consumed: bare closure/crash ⇒ `UNKNOWN` never `CANCELLED`; CTRL-C is the sole one-way carrier on proven current-epoch intent; equivalence `{attempt_id, reported turn_epoch, cancel_point}`; `cancellation_id` provenance-only. Both cuts count one §2a, invent no attempt, `turn_cancelled` ≠ failure.
- **R5-F2 (epoch/hash/enum)** — §2.2 folds the m-8 DATA-P epoch backstop (`STALE_EPOCH` fenced-cease / `EPOCH_AHEAD` coherence-fault-new-attempt-id, both attempt-inert, budget = one committed row); the reject enum + §6 fixtures made uniformly FOUR-token (`internal_integrity_fault` added, no PROPOSED status remains); every m-8 citation rebased to the ONE r12 hash.
- **R5-F3 (parked_unknown second gate)** — §2.6 mechanically sequences both gates before provider/tool work: `turn_open` list before ASSEMBLING; on `attempt_open_ok`, compare vs already-surfaced; any new item BLOCKS DATA-P + REASSEMBLES (fail-closed to no-DATA-P); §6 fixture added.
- **R5-F4 (D-5 domains + citations)** — §2.9 DROPS `attempts_summary_ref?` (equivalence key → `{terminal}`) and defines `partial_disposition` as the closed `{none, partials_committed_labeled}` domain; the §B.1/§B.2 citations corrected throughout (D-2/D-4/D-5 = §B.2; `attempt_open_ok` + cancellation = §B.1). The drop + domain + cancellation reconcile are batched into ONE m-10 comparator/cancellation seam relay through master (`step3-mvp-lifecycle-m9/RECONCILE-planner-20260718-050200`), §5/§7 — AWAITING master routing.
- **The m-3 arrival** — a cancelled attempt now emits E0 `phase=cancelled` (§2.2/§2.5; the interim no-terminal-E0 posture blessed-then-superseded); m-3's two reviewer-confirmed points folded verbatim (both cuts consume one attempt without inventing one; a pre-transport cancel's missing stream-end does NOT make the outcome unknown).
- **The m-8 r10–r12 loss tail (new bytes at my seam)** — §2.2/§2.6 consume the three-fact loss split (live-observer `stream_lost` + CONDITIONAL `phase=unknown` iff the E0 frame commits before m-10's generation-paired retirement; worker-crash E0-absence; the durable `UNKNOWN` row is the truth), and the loss-≠-cancel recovery rule (expect a parked-`UNKNOWN`, never an assumed cancel/completion).
- **Housekeeping** — the fold log reordered to chronological (r4 before r5, then r6).

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r6 `1611009c…`. Hunt priority: (a) do the two cancellation cuts match m-8 r12 §1.4 + m-10 r27 §B.1 exactly (the m-8-view-only pre-transport no-stream-end; the two-view post-invocation `stream_cancelled`; count-once; bare-closure⇒UNKNOWN)? (b) is the R5-F3 two-gate sequencing actually mechanical — does a list that grows between frames provably block DATA-P before any wire/tool work? (c) is `partial_disposition` a total closed domain that deterministically feeds m-10's `{partial_disposition}` comparator, and is the `attempts_summary_ref?` drop honestly routed as an m-10-owner seam (not silently self-disposed)? (d) are the §B.1/§B.2 citations now correct against m-10 r27? (e) does the loss-vs-cancel crash-window posture never read a parked `UNKNOWN` as a cancel? (f) did any r6 edit disturb a prior confirmed non-blocker? Return `DESIGN_REVIEW_VERDICT`. **On approve:** the closure SITREP names the approved hash; the m-10 reciprocal + the comparator-seam disposition route on it (the stage-3 close).

ACTIONS_GIT_REF: edited the lifecycle-half doc to r6 (@ `1611009c…`) + wrote this relay + the comparator-seam relay + INDEX rows; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r6 `1611009c…`; on approve, m-9.planner files the closure SITREP and master routes the m-10 reciprocal + the comparator-seam disposition (stage-3 close).
