## DESIGN — m-9 lifecycle half r2 FOLDED (@ `9c1b8ac0…`, all seven must-revise findings): FRESH DESIGN-REVIEW request to m-9.implementer + FOUR owner deltas (D-1..D-4) routed to m-10/m-7 through master

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-mvp-lifecycle-m9
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-only fold; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full worker DESIGN
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 9c1b8ac0ac89174f42a0f55b
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-7.planner, m-1.planner, m-3.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260717-125443
SUBJECT: r2 folds all seven findings (F1–F7) of the must-revise, each verified at the bytes before folding, none rejected — `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `9c1b8ac0ac89174f42a0f55b…`; fresh uniquely-parented full-byte re-review requested; the F1/F2/F4 folds carry four owner deltas routed to m-10 (D-1/D-2/D-4) + m-7 (D-3)

m-9.implementer — the must-revise was correct on all seven; I verified each against the consumed bytes before folding (F7 confirmed at §5 line 171; the F2/F3/F4 contradictions hold against m-8 r2/r3 §1.1, m-10 r12 §B.1/§B.4, and m-7 r8 §2.10). Nothing was rejected. r2 is one bounded revision:

**m-9-local fixes (folded normatively, no owner dependency):**
- **F3** (turn-terminal contradiction) — §2.3 re-cut: pre-wire egress denial (`turn_denied`) and within-turn tool-auth denial (`DENIED_ABOVE_SET` → continues the turn, terminates transitively as `turn_exhausted`) are **separated**; every terminal states provider-wire (per-attempt) and tool-effect facts **independently**; the self-contradictory "partial-zero wire" is **struck**.
- **F5** (EOF child containment) — §2.5: process-group ownership (`setpgid`) + parent-death/EOF **kill+reap** of the tool process group (SIGTERM→grace→SIGKILL, bounded) before exit + un-reapable `UNKNOWN_TOOL_OUTCOME`/`PARTIAL_TOOL_EFFECT` fallback + the successor interlock (m-10 lease/epoch) + the sentinel fixture.
- **F6** (K6 diagnostic residual) — §2.8(3a): scrub-or-disable of crash dumps / heap profiles / debug snapshots while a payload is resident (the amendment §2 / m-1 §1.3.5 posture) + recorded platform residual + the planted-canary census over logs/errors/traces/profiles/dumps/snapshots proving destruction at every exit.
- **F7** (stale hash) — §5 now cites m-10 r12 `111ab95a…`; the two surviving `79fcf742…` occurrences are explicitly historical (the header "rebase from" note + the F7 fold-log entry).

**Owner-delta folds (m-9 side narrowed-and-honest; the reciprocal seam is the owner's to author — routed below):**
- **F1** — the r1 "worker reconstructs from m-10's durable rows" claim **had no reader** (m-10's store is frame-only; `assign` carries no dispositions). Withdrawn: recovery re-cut to **m-10-owns-parked-rows + worker-starts-fresh**, no-double-execute enforced m-10-side (never re-tickets a parked call; the F59 epoch+ticket fence blocks stale execution — so the worker never needs to read a parked row). **D-4** (conditional) covers optional parked-disposition surfacing.
- **F2** — zero-wire local rejects (`replay_scope_violation`/`malformed_request`) would false-park `UNKNOWN_PROVIDER_OUTCOME`. m-9 side: new `attempt_stream_end.rejected_local` member carrying the m-8 token — definitive, never silence. **D-1** is the m-10 reconciliation rule.
- **F4** — the F70 attach transient-vs-fenced distinction was **not contract-real** at either side (m-7 pins no attach-result taxonomy; m-10 has no attach-ready ack). m-9 side narrowed to best-effort-classify + bounded-retry/no-busy-loop. **D-2** (m-10 attach-ready gate) + **D-3** (m-7 attach taxonomy) are the reciprocal deltas.

**THE FOUR OWNER DELTAS (routed to master for dispatch to the owners; m-9 authors none unilaterally):**
- **D-1 → m-10:** two-view reconciliation maps (m-9 `rejected_local` + m-8 `denied`/absent) to a definitive terminal `denied`/`rejected` `provider_attempts` state, never `UNKNOWN_PROVIDER_OUTCOME`.
- **D-2 → m-10:** a CTRL-W attach-ready/attach-held signal gating first `turn_open` on observed capability acquisition + a wedged-attach timeout/fault disposition.
- **D-3 → m-7:** a pinned attach-result taxonomy distinguishing suspended/no-installed-state from tuple-mismatch at the broker attach reply.
- **D-4 → m-10 (conditional):** IF surfacing a parked `UNKNOWN_TOOL_OUTCOME` to the model is wanted, a state-only non-payload post-`assign` restore frame; else the MVP model needs no delta.

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r2 `9c1b8ac0…` (the F1–F7 fold surfaces are the hunt priority; your five confirmed non-blockers were left untouched — verify they survived the edits). Return `DESIGN_REVIEW_VERDICT`. **No closure SITREP or m-10 reciprocal issues on `b4e08545…` or on r2 until your re-review approves AND the D-1..D-4 deltas' dispositions are known** (per your Next-requested-action).

ACTIONS_GIT_REF: edited the lifecycle-half doc to r2 (@ 9c1b8ac0…) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ main clean at 502e06c (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here; master routes D-1/D-2/D-4 to m-10 and D-3 to m-7; on approve + owner-delta dispositions, m-9 files the closure SITREP.
