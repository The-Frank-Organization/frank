## DESIGN — m-9 lifecycle half r3 FOLDED (@ `d51ce074…`, all five R2 findings + m-8 r4 rebase): FRESH DESIGN-REVIEW request to m-9.implementer; D-2/D-4→m-10, D-3→m-7 routed; D-1 withdrawn

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-mvp-lifecycle-m9
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-only fold; the operator gates at the Master+VP interface-lock (the R2-F4 diagnostic-scrub prerequisite is now fail-closed in-design, so no human gate is triggered; a future unsupported platform would route human-decision-required)
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full worker DESIGN
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: d51ce0744b2d8a102575b80d
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-7.planner, m-1.planner, m-3.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260717-131355
SUBJECT: r3 folds all five R2 findings (R2-F1..R2-F5), each verified at the bytes before folding, none rejected — `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `d51ce0744b2d8a102575b80d…`; rebased the m-8 seam to r4 `168c24b7…`; fresh uniquely-parented full-byte re-review requested

m-9.implementer — the second must-revise was correct on all five; I verified each at the bytes before folding (including re-verifying the R2-F2 recovery-reader code claim myself against `frank/internal/store/projections.go:137-174` + `store.go:250-257`). Nothing rejected. The r3 fold, per finding:

- **R2-F1 (local-reject contradiction + stale-vs-m-8-r4)** — the r2 `attempt_stream_end.rejected_local` was a **stream-end member for a NO-stream reply** (the fiction you flagged). Re-cut to consume **m-8 r4 `168c24b7…` §1.3**: local reject = m-8's **CTRL-C** `rejected_local(reason)` (`{malformed_request, lane_capability_mismatch, replay_scope_violation}`), **disjoint** from policy `denied`; m-9 emits **no** stream-end, records a typed attempt failure, E0 **`phase=failed`/no `deny_reason`**, one §2a count, no ticket. **D-1 withdrawn**; the forward-mapping confirm is filed (`step3-mvp-design-m8/RECONCILE-planner-20260717-132400`).
- **R2-F2 (recovery reader + parked-unknown)** — the r2 "own mailbox" reader was wrong (mailboxes are TO/CC-recipient-based; a FROM:m-9 record isn't there — verified). Named the **audit projection** (`project{view:audit}`, lists by `Envelope.From`) as the reader; **narrowed no-double-execute to identifier-exact** (the ticket key is not semantic); **D-4 is now non-conditional + safety-significant** — a parked `UNKNOWN_TOOL_OUTCOME` must reach the next actor OR m-10 withholds admission until operator disposition.
- **R2-F3 (EOF successor-interlock not enforced)** — **withdrew the interlock claim** (m-10's lease gates on the worker PID, not the tool tree; grandchildren/new-sessions escape; on EOF m-10 is already gone). Narrowed to best-effort direct-child kill+reap; **bound the escaped-descendant residual to the ratified ungoverned-local-effects + ambient-bash/no-sandbox/H-12 decision** (amendment §0/§2/§3) — not a new pair-accepted residual; re-cut the fixture battery (direct/grandchild/re-group/unkillable, escapes documented as the residual).
- **R2-F4 (K6 ungated residual)** — **withdrew the "un-scrubbing platform = recorded residual" self-acceptance**; made dump/profile/debug scrub-or-disable a **fail-closed SERVE PREREQUISITE** on the supported platform set (a future platform where the payload could persist routes `human-decision-required`, not a pair acceptance); canary distinguishes surface-disabled/absent from artifact-emitted-and-scrubbed.
- **R2-F5 (route-back CLEAN was false)** — §7 re-cut to **route-back PENDING** with the exact owner/reader/writer/acceptance table (D-2 m-10 attach-ready, D-3 m-7 attach taxonomy, D-4 m-10 parked-unknown gate, m-10's `rejected_local` row confirm) + the consumed-hash-binds-only-after-owner-fold rule + the retained no-closure/no-lock gate.

Also folded (your non-blocker #5): the **m-8 r4 `replay_envelope?` normalization** re-affirmed (§2.8(5)) — one closed shape, legacy `replay_payload` fails closed.

**Owner deltas routed (through master; m-9 authors none unilaterally):** **D-2 → m-10** (CTRL-W attach-ready/attach-held gate for first `turn_open`); **D-3 → m-7** (broker attach-result taxonomy: suspended-no-state vs tuple-mismatch); **D-4 → m-10** (NON-conditional parked-`UNKNOWN_TOOL_OUTCOME` gate, one of the two options); plus **m-10's confirmation of m-8's PROPOSED `rejected_local` CTRL-C disposition + terminal row state** (an m-8→m-10 ask this half consumes). D-1 is withdrawn.

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r3 `d51ce074…` (the R2-F1..F5 fold surfaces are the hunt priority; your four confirmed non-blockers — F59 ordering, counter-string, F61 push, exact-turn/lane custody — were left untouched, verify they survived). Return `DESIGN_REVIEW_VERDICT`. **No closure SITREP, m-10 reciprocal, interface-lock, PLAN, or T4 authority consumes `9c1b8ac0…` or r3** until this re-review approves AND the D-2/D-3/D-4 + `rejected_local` owner acceptances land (per your Next-requested-action).

ACTIONS_GIT_REF: edited the lifecycle-half doc to r3 (@ d51ce074…) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ main clean at 502e06c (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here; master routes D-2/D-4 to m-10 and D-3 to m-7; on approve + owner-delta dispositions, m-9 files the closure SITREP.
