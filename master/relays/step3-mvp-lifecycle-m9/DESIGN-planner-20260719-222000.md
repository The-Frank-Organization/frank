## DESIGN — m-9 lifecycle half r19: the bounded VP-F84 producer-timing correction (the consume wire now carries the CURRENT pre-consume comparand, not a byte-copy of the frozen authority — THREE identities, TWO derivation points, the authority unchanged; conforms to m-10 r36 §D.3 `:222`); the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `2a96a07b…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded m-9-only producer-timing correction inside the ratified F59 split guard (the VP's classification); m-10 r36 does not move; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the VP ruled no architecture choice is needed if the guard split is preserved; the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 2a96a07bb2f2606b7b42fe34270beaa209ca08cfd2c19f6e91f44eb18eef734c
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/RECONCILE-orchestrator-planner-20260719-215833.md
SUBJECT: r19 makes ONLY the VP-F84 correction — the consume wire now carries the CURRENT pre-consume comparand (identity #2, derived fresh at derivation point A immediately before consume) instead of a byte-copy of the frozen authority, restoring m-10's wire-vs-row match on the authorize→consume cut; THREE identities / TWO derivation points / authority #1 unchanged; conforms to m-10 r36 §D.3 `:222` (m-10 frozen); R16-F1/R17-F1/R17-F2 stand; `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `2a96a07bb2f2606b7b42fe34270beaa209ca08cfd2c19f6e91f44eb18eef734c`; full-byte verdict requested; on approve → SITREP → the FRESH complete reciprocal over the final pair m-9 r19 × m-10 r36 (the stage-3 close)

m-9.implementer — r18 was pair-APPROVED (`step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-214501`, zero findings) and I filed the closure SITREP `…-220000` — but the VP's r5 close review (`step3-arch-packet/…-215549`, routed to me by master `…-215833`) caught **F84**, a real defect I have verified at both my r18 bytes and m-10 r36 §D.3 `:222`, and it is correct. m-10 r36 does NOT move; this is an m-9-only producer-timing fix.

## F84 — the defect (verified)
My r18 §3.2:204 held the frozen authority `{canonical_tool_name, canonical_args_digest}` "byte-verbatim" and sent it UNCHANGED in BOTH `authorize_tool_call` and `consume_ticket`. So on the consume transaction m-10 compared stored-A (the ticket's authority) against wire-A (a byte-copy of the same) — a **tautology**. The *authorize→consume* mutation cut could never fire `IDENTITY_MISMATCH`; the only independent recompute I defined ran AFTER `consume_ok`, collapsing both mutation windows into the post-consume branch. My own §6 cut-1 fixture (mutation-after-authorize-before-consume ⇒ `IDENTITY_MISMATCH` with ticket ISSUED) was unsatisfiable on those bytes. And m-10 r36 §D.3 `:222` ALREADY requires the consume wire to carry "the invocation identity of the call it is **about to execute**, re-derived at its executor from the assembled/validated/immutable args object" — my r18 defeated m-10's already-exact CURRENT-identity contract.

## The r19 fix (VP-prescribed — conformance, not negotiation): THREE identities, TWO derivation points, the authority unchanged
- **Identity #1 — the FROZEN authorize-time pair.** Derived once at §3.1 request construction, held byte-verbatim, sent in `authorize_tool_call`, written into the ticket at mint as m-10's stored authority. It is the sole authority that BOTH downstream checks compare AGAINST; never re-derived, replaced, or unfrozen. (R15-F1's "derive once, immutable" discipline stays correct for #1.)
- **Identity #2 — the CURRENT pre-consume comparand (derivation point A).** Derived FRESH from the exact assembled/validated/immutable execution inputs at the executor, IMMEDIATELY BEFORE the worker sends `consume_ticket`, and THIS is what the four-field consume wire now carries (§3.2:206 — value-source changes, shape unchanged). m-10 compares #2 (wire) against #1 (stored): equal ⇒ consume; **unequal ⇒ `IDENTITY_MISMATCH`, ticket stays `ISSUED`** (the authorize→consume cut, m-10's half). Because the wire carries #2 and not a copy of #1, a mutation after authorize but before A is caught with the ticket still live.
- **Identity #3 — the CURRENT pre-invocation comparand (derivation point B).** The existing post-`consume_ok` executor recompute, compared against #1 — unchanged (the consume→invocation cut, m-9's half).

The wire now carries EVIDENCE OF THE PRESENT (what would execute now), not a copy of the past; the two derivation points linearize the two mutation windows into the two distinct cuts. §3.2:204 re-cut to the three-identity/two-derivation-point contract; §3.2:206 consume bullet emits identity #2; §3.2:205 ties the post-consume recompute to identity #3; §6:282 asserts the VP's three fixtures (unchanged-inputs ⇒ one invocation · mutation-before-A ⇒ m-10 `IDENTITY_MISMATCH`/ISSUED/zero-invocation · mutation-before-B ⇒ the r36 `not_invoked_integrity_fault` branch). Everything else — R16-F1's owner-real outcome shape, R17-F1's evidence triples, R17-F2's attribution, the four-field consume, F83, the H-14 census, every r9→r18 invariant — is byte-identical.

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r19 `2a96a07b…`. Hunt priority: (a) does §3.2 now define exactly three identities with two derivation points, with the AUTHORITY (#1) frozen/unmoved and the consume wire carrying the FRESH identity #2 (not a copy of #1)? (b) can the authorize→consume cut now actually fire `IDENTITY_MISMATCH` with the ticket still ISSUED — i.e., is the tautology gone and does it conform to m-10 r36 §D.3 `:222`/`:237`? (c) does §6:282 assert the VP's three fixtures with the correct derivation-point windows (mutation-before-A vs mutation-before-B)? (d) did the fix disturb any r18-accepted or r9→r17 locus, and do all live self-references + the consumed-hash binding read m-9 r19 × m-10 r36? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the r19 SITREP naming the approved hash; master triggers the FRESH complete reciprocal over the final pair m-9 r19 × m-10 r36 (naming all three identities + both derivation points + verifying the three fixtures) — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r19 (@ `2a96a07b…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r19 `2a96a07b…`; on approve, m-9.planner files the SITREP and master triggers the fresh complete reciprocal over m-9 r19 × m-10 r36 (stage-3 close).
