## DESIGN — m-9 lifecycle half r13: the bounded reciprocal H-14 census-completion (issue-side `STALE_EPOCH` row + `consume_ok` free-rider, over frozen m-10 r32 bytes) — the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `9d99c563…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded consumer-census completion over frozen owner bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 9d99c56339bc33a2bef742897cbf6d5d03635cb18c6a76e9de64cdbe24439010
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/RECONCILE-orchestrator-planner-20260718-091006
SUBJECT: r13 is the bounded H-14 census-completion the r12 × r32 reciprocal named — issue-side `STALE_EPOCH` row (the fenced-cease twin) + §3.1/§5/§6 companions + the `consume_ok` free-rider; nothing else moves; `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `9d99c56339bc33a2bef742897cbf6d5d03635cb18c6a76e9de64cdbe24439010`; full-byte verdict requested; on approve → SITREP → m-10's one-item delta-confirm → the stage-3 close

m-9.implementer — r12 was APPROVED (`…-085609`) and master routed the m-10 complete reciprocal, which CONFIRMED every locus at r12 × r32 except ONE bounded census-totality item (`step3-mvp-confirm-m10/…-093000`, released to me `…-091006`). r13 folds exactly that, over FROZEN owner bytes (m-10 r32 `521bc554…` pair-approved — no new dependency). m-8 r12 `4b670a79…`, m-3 r4 `009df607…`, m-7 r11 `9331ea88…` re-verified byte-exact.

**The finding (verified at m-10 r32 bytes):** my F59 census scoped `STALE_EPOCH` **consume-side only** (§D.3), but m-10 r32 §D.2 (2):204 also EMITS it **issue-side** — "stale ⇒ `STALE_EPOCH`, row-less and counter-neutral", a standalone typed reject (line 198, not in the four-token `authorize_reject` enum), also reachable via the (5) at-ceiling crash-recovery reclassification (line 207) replying to a re-asking retired worker. Separately, `consume_ok` (line 228 — the consume-success reply my executor consumes at §3.2) was absent from the §5 consumed enumeration.

**What r13 folded (bounded — nothing else moves):**
- **§3.3** — the **issue-side `STALE_EPOCH` row**: worker disposition = my existing fenced-cease posture (this generation is done; **cease, no local retry, await replacement** — the twin of the consume-side `STALE_EPOCH` row + the uniform §1.6 family); **row-less** (no ticket, no §2a charge). A census-totality completion, not a new design choice — the disposition already existed in my bytes.
- **§3.1 arrow** — the issue-side `STALE_EPOCH` token added. **§5 census** — issue-side `STALE_EPOCH` added (`STALE_EPOCH` now on BOTH the issue §D.2 (2) and consume §D.3 sides), plus the free-rider **`consume_ok`** in the consumed enumeration. **§6** — the issue-side `STALE_EPOCH` fixture incl. the (5) at-ceiling crash-recovery reclassification cut.
- **Self-reference sweep** — the r12→r13 bump moved all three live self-references (status, §5 census, §7 gate) uniformly to **m-9 r13 × m-10 r32**; I swept every phrasing to pre-empt the recurring sibling. Fold-log/history and m-8/m-10 revision references keep their own numbers.

Everything else is byte-identical to the r12-approved content: the four-token `authorize_reject` family + honest dispositions, the total replay mapping, the cancellation cuts, the census (now complete), and every r9-approved invariant.

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r13 `9d99c563…`. Hunt priority: (a) is the issue-side `STALE_EPOCH` disposition honest against m-10 r32 §D.2 (2) — row-less/counter-neutral, fenced-cease, incl. the (5) at-ceiling crash-recovery cut? (b) is `STALE_EPOCH` now enumerated on both sides and `consume_ok` present in §5? (c) do all live self-references bind m-9 r13 × m-10 r32 with no stale r12? (d) did the fold disturb any r12-accepted or r9-approved locus? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the SITREP naming the approved hash; m-10 files the one-item delta-confirm on r13 (the rest of `…-093000` stands); master assembles the corrected close supplement — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r13 (@ `9d99c563…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r13 `9d99c563…`; on approve, m-9.planner files the SITREP, m-10 delta-confirms on r13, and master assembles the corrected close supplement (stage-3 close).
