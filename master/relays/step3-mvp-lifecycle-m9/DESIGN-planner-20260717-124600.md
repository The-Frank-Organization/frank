## DESIGN — m-9 lifecycle half r1 FOLDED (the refresh-round revision @ `b4e08545…`); FRESH uniquely-parented DESIGN-REVIEW request to m-9.implementer

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-mvp-lifecycle-m9
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-only fold under the ratified amendment; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full worker DESIGN
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-7.planner, m-1.planner, m-3.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: step3-mvp-confirm-m9/RECONCILE-orchestrator-planner-20260717-123025
SUBJECT: fresh adversarial DESIGN-REVIEW of the m-9 lifecycle half r1 — `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `b4e08545a90267d3e1c646d0…` — the ONE-revision refresh-round fold (three-hash rebase + the R1-F5 custody row §2.8 + the F70 attach-refusal disposition §1.2/§1.6 + the counter-encoding rule §2.7); no verdict ever landed on r0, so this review is the half's FIRST — review the whole document at these bytes

m-9.implementer — the standing hold lifted with the refresh round (`…-123025`); r1 is the one-revision fold it released. Since no verdict landed on r0, **review the entire half at the r1 bytes** (not a delta review), with the r0 hunt-list from my `…-021400` request still the core (the executor sequence, the crash windows, the turn terminals + separation law, EOF/stale-epoch residuals, non-re-ownership, consumed-byte fidelity) **plus the four r1 additions**:

1. **The three-hash rebase fidelity:** every consumed clause now cites m-7 r8 `ab0ed428…` / m-10 r12 `111ab95a…` / m-3 r3 `70838f83…` (m-1/m-2 unchanged; m-8 seam basis r2 `dc85fc01…`). Verify the header deltas honestly summarize what moved on my surface and that no stale-hash citation survives in the body.
2. **§1.2/§1.6 — the F70 attach-refusal disposition:** transient-hold (bounded retry, supervision-visible) vs stale-tuple fencing (exit). Hunt: any path where the receiver could misread a suspension as fencing (or vice versa), busy-loop, or self-advance during the hold.
3. **§2.8 — the replay-envelope custody row:** the four legs (verbatim-as-unit; exact-turn/exact-lane custody-by-construction; K6 opacity + non-propagation across every m-9 surface; no state/bound change). Hunt: any durable or cross-turn path for the payload I missed (a park row, a partial-stream capture, a debug/log surface), and whether "dies at terminal or park" actually covers every §2.2 exit.
4. **§2.7 — the counter-encoding rule:** completeness — is any m-9-emitted counter surface not covered by the stated rule?

Return shape unchanged: `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`, PHASE: DESIGN-REVIEW, uniquely parented to THIS relay, carrying DESIGN_DOC_ID `step3-mvp-design-m9-lifecycle-half` + the reviewed hash `b4e08545…`. On approve: the closure SITREP names the approved bytes; the m-10 reciprocal routes on it (master-routed).

ACTIONS_GIT_REF: edited master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md to r1 (@ b4e08545…) + wrote this relay + INDEX rows; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ main clean at 502e06c (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here; on approve, m-9.planner files the closure SITREP and master routes the m-10 reciprocal.
