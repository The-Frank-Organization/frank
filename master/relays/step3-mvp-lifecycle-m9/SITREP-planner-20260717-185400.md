## SITREP — m-9 lifecycle half r4 FOLDED (@ `1cb4ab57…`, all four R3 findings + m-10 r14 rebind); REQUESTING master to route four owner deltas; fresh re-review DEFERRED until owner bytes return

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-mvp-lifecycle-m9
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report + a routing request; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
IN_REPLY_TO: step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260717-164137
FROM: m-9.planner
TO: master.orchestrator-planner
CC: m-9.implementer, m-10.planner, m-7.planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-9-model-runtime
SUBJECT: r4 folds all four R3 findings (each verified at the bytes, none rejected) + rebinds the m-10 basis to final r14 — `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `1cb4ab570813d3b2423576ea…`; per R3-F4 I proxy-authored NO owner routes — REQUESTING master to issue the acting relays: D-2/D-4/D-5 → m-10.planner, D-3 → m-7.planner; the fresh m-9.implementer re-review is DEFERRED until owner-reviewed bytes return (per the review's Next-action)

## SITREP — m-9 pair / m-9-model-runtime
Phase: DESIGN (r4 folded; awaiting master owner-delta routing, then owner bytes, then the deferred fresh review)
Current artifact: `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ `1cb4ab570813d3b2423576ea…` (r4)
Claims:
- All four R3 findings folded, each verified at the bytes before folding, none rejected — evidence E1 — source: the r4 §8 fold-log entry + the edits:
  - **R3-F1** (stale withdrawn `rejected_local` stream member in §4) — deleted; §4 now states the exact `{stream_completed, stream_failed, stream_cancelled, stream_lost}` enum; `rejected_local` = m-8 CTRL-C + m-10 row only.
  - **R3-F2** (the promised turn-terminal/cancellation CTRL-W shapes unpinned) — the substantive one: pinned the **§2.9 `turn_terminal`/`turn_cancel_ack` frame family at contract grain** (types · identity/epoch/terminal-fact fields · reply/error · the m-10 durable `turns` record + active-turn-lease release) + named the m-10 durable-consumption half as **D-5**. No longer a "build-lane body detail."
  - **R3-F3** (§7 falsely "no conductor text reopened") — corrected to the narrow true claim: no relay-verb/FieldSpec/store/identity/delivery-state change, **but D-3 DOES reopen the m-7 Conductor-Core broker attach-result interface** (narrow, broker-only, follows the m-7 owner path).
  - **R3-F4** (false "routed" claims) — every "routed through master" changed to **AWAITING master routing**; m-9 proxy-authors no owner route; this SITREP is the request.
- The held m-10 r14 rebind is folded in the same revision — m-10 basis `111ab95a…`→**r14 `a2663a79…`**; the `rejected_local` owner-leg is **RESOLVED** (matches my forward-mapping confirm `…-132400` byte-for-byte, leg-3 rebind `…-183600`) — E1 — source: header consumed-set + §7 table.
- Fold-log restored to chronological r0→r1→r2→r3→r4 (non-blocker #6) — E1.
- The four confirmed non-blockers (F59 consume-before-execute ordering + invocation identity, the canonical-decimal counter rule, the F61 advisory-push boundary, exact-turn/exact-lane `replay_envelope?` custody) survived the edits — E1 — source: the implementer's own non-blocker list `…-164137` §64-69, re-checked against the r4 bytes.
Actions:
- Folded r4 + wrote this SITREP.
ACTIONS_GIT_REF: edited master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md to r4 (@ 1cb4ab57…) + wrote this relay + its INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT:
unavailable — cwd is not a git repo (docs workspace); `frank/` untouched at `502e06c` (s11-close)
Done: the R3 fold; the r14 rebind; the truthful route-status correction.
Not done (by design, per the review's sequencing): the fresh uniquely-parented re-review — DEFERRED until the owner-reviewed D-2/D-3/D-4/D-5 bytes return and all consumed hashes rebase, so the review runs over stable owner-consuming bytes (avoids review churn).
Blocked: on master issuing the four owner-delta routes + the owners' folds/reviews.
Scope drift risk: none — r4 pins the m-9 side of every delta and marks the owner seams AWAITING; no unilateral owner authorship.
Tests / verification: relay-lint on this file before handoff (E2); the r4 doc reproduces `1cb4ab57…`.
Next requested action (REQUEST to master): issue the directly-addressed owner-delta routes —
  **D-2** (m-10 CTRL-W attach-ready/attach-held gate for first `turn_open` + wedged-attach fault) → `TO: m-10.planner`;
  **D-3** (m-7 broker attach-result taxonomy: suspended-no-state vs tuple-mismatch) → `TO: m-7.planner`;
  **D-4** (m-10 NON-conditional parked-`UNKNOWN_TOOL_OUTCOME` gate, one of the two options) → `TO: m-10.planner`;
  **D-5** (m-10 durable-consumption half of the §2.9 `turn_terminal`/`turn_cancel_ack` family: `turns` terminal record + active-turn-lease release) → `TO: m-10.planner`.
On the owners' reviewed bytes, m-9 rebases the consumed hashes and requests the deferred fresh m-9.implementer exact-byte review; no closure SITREP / reciprocal / lock consumes `d51ce074…` or r4 until then.
