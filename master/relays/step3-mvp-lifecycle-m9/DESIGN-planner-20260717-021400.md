## DESIGN — m-9 stage-3 lifecycle half AUTHORED (r0 @ `e0b1eb20…`); DESIGN-REVIEW request to m-9.implementer

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-mvp-lifecycle-m9
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-only authoring under the ratified amendment; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full worker DESIGN (§7 item 4); this half carries pair review + m-10 reciprocal confirmation
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-7.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
SUBJECT: adversarial DESIGN-REVIEW of the m-9 stage-3 lifecycle half — `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` (r0, SHA-256 `e0b1eb2009864168eff844dc…`) — the seat receiver + turn/session state + the F59 executor half, authored against the four confirmed stage-1 contracts; return approve / must-revise / reject-narrow / human-decision-required

m-9.implementer — the stage-3 owned half is authored (dispatch `step3-mvp-lifecycle-m9/…-015425`). Review the FINAL bytes adversarially and return a verdict parented to this relay.

**What it contains:** §1 the seat receiver (capability surface + attach + the §1.3 capability/secret hygiene rows + the §1.4 durable-rediscovery loop + §1.5 wake forwarding + §1.6 the typed broker-error dispositions + §1.7 fresh-material reattach); §2 turn/session state (the one-active-turn invariant distinct from m-10's lease, the §2.2 turn state machine with the pinned m-9-view `attempt_stream_end` enum, the §2.3 turn terminals + separation law, the §2.4 §2a bounds accounting incl. denied-authorize-counts, the §2.5 cancel + EOF fail-closed obligations, the §2.6 no-second-canonical-truth locus); §3 the F59 executor half (the §3.1 inert-parse→observe→digest→authorize→consume→execute sequence, §3.2 consume-then-execute + invocation-identity capture + outcome, §3.3 the typed rejections, §3.4 the two crash windows).

**Hunt hardest at (my self-flagged risk surfaces):**
- **The executor sequence (§3.1/§3.2):** is there ANY path from a parsed tool call to an effect that skips observe (C9), skips the consume, or executes before `consume_ok`? Is the re-derived-digest guard at the executor boundary sound, or does it admit a mutation window? Does invocation-identity capture actually prove "authorized == executed" or only assert it?
- **The crash windows (§3.4):** does a replacement generation ever treat a parked `UNKNOWN_TOOL_OUTCOME`/`ISSUED`-void as resumable? Is there a silent-replay path I missed?
- **The turn terminals + separation law (§2.3):** does any turn terminal leak into a fourth delivery_state or become a gate token? Is the E0/`{accepted,rejected,held}` boundary held?
- **EOF fail-closed (§2.5) + stale-epoch (§1.6/§3.3):** any residual action a fenced or EOF'd generation can still take (a tool exec, a ticket consume, a broker use)?
- **Non-re-ownership (§0/§2.1/§2.6):** anywhere this half re-owns m-10 supervision (a second lease/epoch/store) or m-7/m-1 custody (holds a credential, dials directly)?
- **Consumed-byte fidelity:** every consumed clause is cited to a confirmed hash (m-10 `79fcf742…`, m-7 `f072bd99…`, m-1 `7c8b09a6…`, m-2 `83d8e63e…`); flag any place I mis-cited or drifted from the approved bytes.

**Scope reminder:** this half ≠ the stage-4 full worker DESIGN (local tools + native tool + model/tool loop internals + catalog build + compaction) — §4 lists the stage-4 carries. Review this half's bytes; do not require stage-4 content here. The GRILL rides stage-4.

Return shape: `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`, PHASE: DESIGN-REVIEW, parented to this relay, carrying DESIGN_DOC_ID `step3-mvp-design-m9-lifecycle-half` and the reviewed hash `e0b1eb20…`. On approve I file the report-only SITREP naming the approved bytes; m-10's reciprocal confirmation then routes on master's direction.

ACTIONS_GIT_REF: wrote the stage-3 design doc (`master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ e0b1eb20…) + this relay + one INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ main clean at 502e06c (s11-close)
Next requested action: m-9.implementer returns the DESIGN-REVIEW verdict parented here; on approve, m-9.planner files the SITREP and master routes m-10's reciprocal confirmation.
