## SITREP — m-9 lifecycle half r6 took a must-revise (R6-F1/F2/F3, all verified correct, none rejected); r6 HELD; R6-F1+R6-F2 are m-9-LOCAL (resolutions designed, ready to fold), R6-F3 depends on m-10's disposition of the comparator seam (master routed it `step3-mvp-design-m10/…-051544`, m-10 not yet returned) — ONE batched r7 after m-10 disposes, per the reviewer's anti-churn Next-action

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report + a dependency surfacing; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
IN_REPLY_TO: step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260718-051753
FROM: m-9.planner
TO: master.orchestrator-planner
CC: m-9.implementer, m-10.planner, m-10.implementer, m-8.planner, m-3.planner, m-7.planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-9-model-runtime
SUBJECT: r6 @ `1611009c…` must-revise (R6-F1..R6-F3, all verified correct); the stage-3 close does NOT land on r6; R6-F1 (epoch-inert vs terminal no-stream) + R6-F2 (Gate-2 comparator totality) are m-9-local with resolutions designed below; R6-F3 (status/§2.9 overclaim + attempts_summary_ref? shape) blocks on m-10's disposition of the r6 comparator seam (master routed `…-051544`); r6 HELD, ONE r7 folds all three after m-10 returns

master — the r6 re-review returned **must-revise** with three findings. I verified each at the bytes; **all three are correct, none rejected**.

## The findings (verified)
- **R6-F1 (m-9-LOCAL) — the generic no-stream rule terminalizes the attempt-inert epoch branch.** Correct: §2.2 puts the DATA-P epoch backstop in the "closed by m-8's CTRL-C view alone" set (`:93`) and in the "typed attempt terminal / the typed reply IS the terminal answer" generic (`:99`) — but m-8 r12 §1.3 makes `STALE_EPOCH`/`EPOCH_AHEAD` **attempt-INERT**: no `attempt_result`, no m-8 CTRL-C close, no E0 view from this path, the committed row's fate belongs to m-10's retirement/epoch machinery. My dedicated epoch paragraph (`:94`) states this correctly; the generic aggregation contradicts it. **Resolution (designed, ready):** split the no-stream set into (a) **no-stream TERMINAL outcomes** (`denied` · `rejected_local` · `cancelled(pre_transport)`) — closed by m-8's CTRL-C view, a typed attempt terminal, honest E0 phase, one §2a count; and (b) **no-stream ATTEMPT-INERT epoch replies** (`STALE_EPOCH`/`EPOCH_AHEAD`) — NO `attempt_stream_end`, **NO m-8-view terminal close, NO `attempt_result`, NO E0 terminal from this path**, row disposition left to m-10, the already-committed row charged **once**, the worker's response = the fence (`STALE_EPOCH`) / coherence-fault-new-attempt-id (`EPOCH_AHEAD`) posture, **never a "terminal answer."** Remove the epoch branch from the `:93`/`:99` terminal-close rows; align the §6 fixture.
- **R6-F2 (m-9-LOCAL) — Gate 2 is not total over the closed `parked_unknown` lists.** Correct: §2.6 handles equal + growth (a new identity blocks DATA-P + reassembles) but not (i) **removal-only** (Gate 2 a strict subset — unequal, no new item) nor (ii) **same-identity changed member** (identity equal but `state`/`canonical_tool_name`/`canonical_args_digest` differ); my membership test keyed on identity only. **Resolution (designed, ready):** make Gate 2 a **total set comparator over the FULL closed member** `{turn_id, tool_call_id, ticket_id, state, canonical_tool_name, canonical_args_digest}` — pre-reject a malformed member or a duplicate identity within a list (fail-closed to no-DATA-P); then, keyed on `{turn_id, tool_call_id, ticket_id}` and comparing the full member: **equal** ⇒ proceed; **added-or-changed** (new identity, OR same identity with any differing closed field) ⇒ BLOCK DATA-P, surface, REASSEMBLE (fail-closed if reassembly can't complete); **removed-only** ⇒ **proceed with the already-surfaced conservative superset** — the honest fail-safe direction, since over-disclosing a since-resolved parked item never lets the model UNDER-know (the D-4 safety purpose), and it avoids an unnecessary reassembly. Every valid pair of lists then has a deterministic branch. Extend the §6 fixture to removal-only and changed-member cuts alongside growth.
- **R6-F3 (m-10-DEPENDENT) — the status/§2.9 text overclaims while m-10's consumer shape is unmatched.** Correct: `:6` says "Every owner delta RESOLVED; route-back closed at the m-7/m-10 seams" and §2.9 `:168` says "contract-real on both sides (D-5 landed)" — but the `attempts_summary_ref?` drop means m-10 r27 §B.2's shape/predicate still include the member; §5/§7 correctly say the comparator seam is routed/pending. Internal contradiction. **Master has now routed the three-item comparator seam to m-10** (`step3-mvp-design-m10/RECONCILE-orchestrator-planner-20260718-051544` — verified on disk), which discharges "awaiting master routing" but NOT m-10's disposition/bytes/comparator confirm/pair review. **Resolution (after m-10 returns):** rebase r6→r7 to m-10's final disposed hash; correct the status + §2.9 to the true state (now "routed, AWAITING m-10 owner disposition"; after, "m-10-disposed at rXX"); withdraw the "contract-real on both sides / route-back closed" claim for the moved shape; if m-10 rejects either offered domain (`attempts_summary_ref?` narrowing or the `partial_disposition` comparator), fold m-10's owner-real alternative rather than preserving my claim.

## Disposition
Per the reviewer's explicit anti-churn Next-action ("holds r6… folds R6-F1/R6-F2 after m-10 returns its owner disposition and any pair-approved bytes; then rebases the status/contract hashes and returns one fresh uniquely-parented DESIGN request"), I **HOLD r6** and fold **ONE r7** = R6-F1 + R6-F2 (both m-9-local, resolutions above) **+** R6-F3 (the m-10 rebase) after m-10 disposes the comparator seam. Folding R6-F1/F2 now then re-folding at m-10's return is exactly the churn to avoid; batching them is the reviewer's endorsed sequence.

Done: the r6 verdict verification (all three correct); the R6-F1 + R6-F2 resolutions designed (m-9-local, ready to fold).
Not done (held): the r7 fold — batched for m-10's comparator disposition; the closure SITREP / m-10 reciprocal / stage-3 close do NOT land on r6.
Blocked: R6-F3 on **m-10 disposing the r6 comparator/cancellation seam** (`attempts_summary_ref?` drop → possible r28; `partial_disposition` comparator confirm; cancellation reconcile) — master routed it to m-10 at `…-051544`; m-10 not yet returned. R6-F1/F2 are NOT blocked (m-9-local), held only for the single-r7 batch.
Dependency chain: **m-10 comparator disposition → my r7 (R6-F1+R6-F2 folded now-ready, R6-F3 rebased to m-10's hash) → fresh uniquely-parented review → closure SITREP → m-10 reciprocal (stage-3 close).**
Scope drift risk: none — R6-F1/F2 are internal-consistency/totality corrections; R6-F3 is honest-status alignment to a genuine cross-owner pending.
Tests / verification: relay-lint this file (E2); r6 `1611009c…` + the four bases + the master→m-10 route `…-051544` re-verified.
ACTIONS_GIT_REF: wrote only this SITREP relay + its INDEX.md row; no design-doc edit (r6 HELD, unchanged at `1611009c…`), no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: master tracks the dependency; when m-10 returns its comparator disposition (+ any pair-approved bytes), m-9 folds the single r7 (R6-F1 epoch-inert split · R6-F2 total Gate-2 comparator · R6-F3 rebase + honest status), and returns a fresh uniquely-parented DESIGN request. No closure SITREP / reciprocal / lock consumes `1611009c…` or r6.
