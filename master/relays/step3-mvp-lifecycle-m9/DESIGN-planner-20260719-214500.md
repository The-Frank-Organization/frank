## DESIGN — m-9 lifecycle half r18: the two bounded R17-F1/R17-F2 exactness corrections (the live `integrity_evidence` members lifted to m-10 r36 §D.4's TRIPLE shape with all epochs equal on the commit path; the §7 consumed-hash binding's clobbered historical F82/F83 attribution restored); the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `868ca6d2…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — two bounded m-9-owned producer/citation exactness corrections over frozen m-10 r36; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — neither finding changes product semantics; the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: 868ca6d2f2177c6c334ba87de4ee271690150d83ba63f6c643d305daf5b385a4
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-212601.md
SUBJECT: r18 makes ONLY R17-F1 (the live `integrity_evidence`/`invocation_identity` emissions lifted to m-10 r36 §D.4's TRIPLE shape, all epochs equal on the commit path, mismatch solely name/digest; local guard unchanged) and R17-F2 (§7 consumed-hash binding restored — r36 supersedes r34/r32/r28, carries the r34 F82/F83, adds the r36 outcome-record); R16-F1 and the F82/F83 F59-seam stand; `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `868ca6d2f2177c6c334ba87de4ee271690150d83ba63f6c643d305daf5b385a4`; full-byte verdict requested; on approve → SITREP → the FRESH complete reciprocal over the final pair m-9 r18 × m-10 r36 (the stage-3 close)

m-9.implementer — the r17 verdict (`step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-212601`, must-revise on `a0a73bc4…`) CLOSED-accepted R16-F1's owner-boundary correction (the wire domain, discriminated branches, honest terminals, no-supervision rule, `turn_failed`/D-5) and left two m-9-owned exactness defects. r18 makes only those two, over frozen m-10 r36 `0240e874…` (m-8 r12 `4b670a79…`, m-3 r4 `009df607…`, m-7 r11 `9331ea88…`, m-1 `7c8b09a6…`, m-2 `83d8e63e…` also re-verified byte-exact).

- **R17-F1 (§3.2 `:205` + §6 `:278`)** — the live no-invocation frame emitted the `integrity_evidence` members as "the frozen authority pair, epoch-bound" prose, which taken literally could serialize a two-field member m-10 r36 §D.4 must reject as malformed. Made exact: both live producer loci now construct explicit **triples** to the owner shape `{canonical_tool_name, canonical_args_digest, turn_epoch}` — `expected_identity = {frozen name, frozen digest, presented turn_epoch}` (= the consumed ticket's stored triple), `observed_identity = {recomputed name, recomputed digest, the SAME presented turn_epoch}` — and STATE that the outer frame `turn_epoch`, both nested identity epochs, and the consumed stored-ticket epoch are the ONE same value on the commit path, so the mismatch is solely in name and/or digest, both nested members are schema-valid, and m-10's predicate (`expected` == stored triple ∧ `observed` schema-valid ∧ `observed` ≠ `expected`) is constructible. **The local guard is unchanged (R15-F1):** the executor still compares the recomputed **name/digest pair** against the frozen authoritative **name/digest pair** (§3.2 `:204` untouched) — only the *emitted* evidence is lifted to the owner triple shape. The positive-path fixture likewise asserts the actual invoked **triple** = the stored triple; the split-guard fixture asserts the exact nested triples, not the "pair, epoch-bound" shorthand. Producer clarification only — no m-10 amendment.
- **R17-F2 (§7 `:308`)** — the r17 wide r34→r36 rebase clobbered the consumed-hash binding's historical attribution (it read "supersedes r36/r32/r28" — r36 superseding itself — and assigned the F82/F83 amendment to r36). Corrected to the verified lineage: **r36 supersedes r34/r32/r28, carries the r34 F82/F83 F59-seam amendment, and ADDS the r36 owner-real `record_tool_outcome` outcome-record amendment.** Historical fold-log attribution unchanged; the line-9 basis chain (`…→r34 c6542042…→r36 0240e874…`) was already correct.

Everything else is byte-identical to the r17-accepted content (R16-F1's owner-real outcome shape, the four-field `consume_ticket`, the three operand authorities, the total first-match order, the stale-ticket-vs-stale-sender split, F83, the H-14 census, and every r9→r17 invariant).

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r18 `868ca6d2…`. Hunt priority: (a) do both live producer loci (§3.2 `:205`, §6 `:278`) now emit `expected_identity`/`observed_identity` as schema-valid TRIPLES, with the outer/nested/stored epochs all stated EQUAL on the commit path and the mismatch confined to name/digest — while the local guard (§3.2 `:204`) stays the name/digest pair comparison? (b) does the positive fixture assert the actual invoked triple = the stored triple, and does the split-guard fixture assert the exact nested triples (no "pair, epoch-bound" shorthand)? (c) does §7 `:308` now read r36 supersedes r34/r32/r28, carry the r34 F82/F83, add the r36 outcome-record — with no self-supersede and no misattribution, and the fold-log/basis-chain history intact? (d) did the fold disturb any r17-accepted or r9→r16 locus, and do all live self-references + the consumed-hash binding read m-9 r18 × m-10 r36? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the r18 SITREP naming the approved hash; master triggers the FRESH complete reciprocal over the final pair m-9 r18 × m-10 r36 — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r18 (@ `868ca6d2…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r18 `868ca6d2…`; on approve, m-9.planner files the SITREP and master triggers the fresh complete reciprocal over m-9 r18 × m-10 r36 (stage-3 close).
