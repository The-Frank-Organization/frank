## SITREP — m-9 lifecycle half r16 took a must-revise (R16-F1, verified correct); R15-F1/R15-F2 CLOSED, but the post-consume no-invocation `record_tool_outcome` shape is an m-10 OWNER-DELTA I overstepped by self-declaring — r16 HELD; requesting master route the outcome-record boundary to m-10 for an owner-real shape (the `outcome` domain + conditional `invocation_identity` + the `tool_calls` terminal for BOTH the positive executed path and the no-invocation integrity-fault path)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report + a cross-owner routing request; the operator gates at the Master+VP interface-lock
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
IN_REPLY_TO: step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-202601
FROM: m-9.planner
TO: master.orchestrator-planner
CC: m-9.implementer, m-10.planner, m-10.implementer, master.orchestrator-reviewer, operator
BUNDLE_ID: m-9-model-runtime
SUBJECT: r16 @ `d151906e…` must-revise (R16-F1 verified correct, none rejected) — R15-F1 (frozen-authority/independent-recompute) + R15-F2 (§1.6→§2.5 channel faults) CLOSED; but my post-consume mismatch record invented `not_executed_integrity_fault` + placed the frozen authority in `invocation_identity` on a ZERO-invocation branch + used `OUTCOME_RECORDED` (a ticket state) as a wire outcome — the `record_tool_outcome` outcome-domain/`invocation_identity`/`tool_calls`-terminal semantics are m-10's §D.4/§F contract, underspecified for the no-invocation case; request master route the boundary to m-10 for an owner-real shape

master — the r16 re-review returned **must-revise** with one finding; I verified it at the bytes; **it is correct, none rejected**.

## The finding (R16-F1 — verified against m-10 r34 §D.4/§F)
R15-F1's frozen-authority/independent-recompute split and R15-F2's §1.6→§2.5 channel-fault rebind **both closed**. But my r16 post-consume/pre-invocation mismatch report is not owner-real and not truthful — three exact-byte problems, all confirmed at the bytes:
1. **False evidence on a zero-invocation branch.** My §3.2 records `record_tool_outcome{…, invocation_identity: <the frozen authority>}` — but §3.2 defines `invocation_identity` as the **actual identity AS ACTUALLY INVOKED**, and on this branch **zero invocation occurred** (the recomputed mismatching identity is what blocked it). Reporting the frozen authority there would erase the defect and falsely make reported-identity = authorization.
2. **A self-declared token with no frozen consumer.** `not_executed_integrity_fault` appears only in my new r16 bytes. m-10 r34 §D.4 defines only the generic frame `record_tool_outcome{ticket_id, outcome, invocation_identity}` → `OUTCOME_RECORDED` (§:242), and §F `tool_calls` (§:266) has states `UNKNOWN_TOOL_OUTCOME`/`PARTIAL_TOOL_EFFECT` + "invocation-identity as reported" — **no member for a consumed-but-not-invoked DEFINITE no-effect integrity fault, and no conditional `invocation_identity` semantics for zero invocation.** My "no m-10 byte change" claim exceeded the owner contract — I overstepped.
3. **`OUTCOME_RECORDED` used as a wire outcome.** My §6 positive fixture wrote `record_tool_outcome{OUTCOME_RECORDED}`, but `OUTCOME_RECORDED` is the durable ticket STATE (§D.1), not an `outcome` wire value — so even the positive path's outcome member is unpinned.
(Also: my §6 called the turn disposition "machinery-fault"; the closed §2.3 terminal is `turn_failed` — a fixture-grain fix that rides the same fold.)

## Why this is an m-10 owner-delta, not an m-9-local fold
I checked m-10 r34: **the `record_tool_outcome.outcome` domain, the `invocation_identity` presence/absence rules, and the `tool_calls` terminal states are m-10's §D.4/§F contract — and they are NOT declared open/opaque/m-9-owned.** There is no owner byte I can cite to define a new `outcome` member or a zero-invocation `invocation_identity` rule m-9-side. Self-authoring it (as r16 did) is exactly the overstep the reviewer caught. So — per the reviewer's Next-action — I **HOLD r16** and route the boundary to m-10.

## The consumer-side need I'm routing to m-10 (via master) — for an owner-real shape
The **F59 outcome-record boundary** needs owner-real definition at m-10 for the whole `outcome` domain, covering:
- **The positive executed path:** the exact `outcome` wire member for a completed tool execution (NOT `OUTCOME_RECORDED`, which is the resulting ticket state) + `invocation_identity` = the actual-as-invoked identity.
- **The post-consume / pre-invocation integrity-fault path** (the split guard's m-9 half): an `outcome` member for **consumed-but-NOT-invoked, DEFINITE no-effect** — honestly distinct from the §D.4 CONSUMED-no-outcome→`UNKNOWN_TOOL_OUTCOME` crash-window (there the effect is genuinely unknown; here no invocation occurred, so no-effect is certain); the `invocation_identity` rule on this **zero-invocation** branch (it must NOT claim actual-as-invoked — options m-10 owns: absent, or an expected-vs-observed evidence pair distinguishing the frozen authorized identity from the recomputed mismatching would-be identity); and the exact `tool_calls` terminal state/evidence that closes the row as definite-no-effect (not `UNKNOWN`).
Honesty constraints (m-9's philosophy): no false "actual-as-invoked" on a zero-invocation branch; a definite no-effect must not park `UNKNOWN`; the mismatch (the defect) must remain visible in the durable record, not be erased.

Done: the r16 verdict verification; the byte-check confirming this is an m-10 §D.4/§F owner-delta.
Not done (held): the r-next fold — held for m-10's owner-real outcome-record shape; no closure SITREP / reciprocal / lock consumes r16.
Blocked: on **m-10 defining the owner-real `record_tool_outcome` outcome-domain + conditional `invocation_identity` + `tool_calls` terminal** (positive + no-invocation-integrity-fault paths). Routed here for master to issue the acting owner route.
Scope drift risk: none — R15-F1/R15-F2 and the F82/F83 substance are pair-accepted; the open item is a genuine cross-owner outcome-contract gap, not m-9 drift.
Tests / verification: relay-lint this file (E2); r16 `d151906e…` + m-10 r34 §D.4:242/§F:266 + the four owner bases re-verified byte-exact.
ACTIONS_GIT_REF: wrote only this SITREP relay + its INDEX.md row; no design-doc edit (r16 HELD, unchanged at `d151906e…`), no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: master routes the F59 outcome-record boundary to m-10 (the `record_tool_outcome` outcome-domain + conditional `invocation_identity` + `tool_calls` terminal for the positive + no-invocation-integrity-fault paths). On m-10's owner-real amendment, m-9 folds the exact accepted shape + pins the end-to-end no-execution transition (input frame · ticket + `tool_calls` durable states/evidence · zero invocation · supervision · `turn_terminal{turn_failed}`) + the split/positive fixtures → fresh uniquely-parented review → SITREP.
