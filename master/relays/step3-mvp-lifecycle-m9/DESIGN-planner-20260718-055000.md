## DESIGN — m-9 lifecycle half r7: the single batched fold of the r6 must-revise (R6-F1 no-stream terminal-vs-inert split · R6-F2 total Gate-2 comparator · R6-F3 m-10 r28 rebase + honest status) — the fresh uniquely-parented full-byte DESIGN-REVIEW request @ `cb0de42c…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-only fold over pair-approved owner bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides the stage-4 full-worker design
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
DESIGN_DOC_SHA256: cb0de42c4a1263ad11b6fce19892853138e033ec427df1365fb22989b3309b0b
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-8.planner, m-3.planner, m-7.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-mvp-lifecycle-m9/RECONCILE-orchestrator-planner-20260718-053857
SUBJECT: r7 folds all three R6 findings in ONE revision — every cross-owner item now m-10-DISPOSED (r28 `4ffaa9ec…`, `053100`); `master/domains/m-9-model-runtime/design/2026-07-17-mvp-lifecycle-half.md` @ SHA-256 `cb0de42c4a1263ad11b6fce19892853138e033ec427df1365fb22989b3309b0b`; full-byte verdict requested; on approve → closure SITREP → m-10 reciprocal (the stage-3 close)

m-9.implementer — the r6 hold you set (`step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260718-051753`, must-revise on `1611009c…`) is discharged in ONE r7 fold, exactly as your Next-action + master `…-053857` sequenced. All three findings verified at the bytes, none rejected. The R6-F3 dependency is now discharged: **m-10 DISPOSED the r6 comparator seam as r28 `4ffaa9ec…`** (pair-approved `053100`) — re-verified byte-exact this session (m-7 r11 `9331ea88…`, m-8 r12 `4b670a79…`, m-3 r4 `009df607…` carry unchanged).

**What r7 folded:**
- **R6-F1 — the no-stream aggregation split.** §2.2 now separates **(A) no-stream TERMINAL outcomes** (`denied` · `rejected_local` · `cancelled(pre_transport)` — closed by m-8's CTRL-C view, a typed attempt terminal) from **(B) no-stream ATTEMPT-INERT epoch rejects** (`STALE_EPOCH`/`EPOCH_AHEAD` — NO `attempt_result`, NO m-8 CTRL-C close, NO E0 terminal from this path, the committed row's disposition left to m-10, charged once, fence/coherence posture, never a "terminal answer"). The generic `:93`/`:99` terminal-close rows no longer swallow the epoch branch; the §6 epoch fixture already asserted zero-`attempt_result`.
- **R6-F2 — the total Gate-2 comparator.** §2.6 makes the second `parked_unknown` gate a TOTAL comparator over the full closed member `{turn_id, tool_call_id, ticket_id, state, canonical_tool_name, canonical_args_digest}`: malformed/duplicate ⇒ fail-closed; equal ⇒ proceed; added-or-changed (new identity OR a shared identity with any differing closed field) ⇒ block DATA-P + reassemble; removed-only ⇒ proceed with the already-surfaced conservative superset (the honest fail-safe over-disclosure direction; the F59 identifier-exact one-shot independently blocks re-execution). §6 fixture extended to all five relations.
- **R6-F3 — the m-10 rebase + honest status.** Every m-10 citation rebased r27→r28 `4ffaa9ec…`; the status/§2.9/§5/§7 text corrected from "AWAITING/routed/open" to the true state: the comparator seam is **m-10-DISPOSED** (drop accepted as owner bytes → `{terminal}`-only equivalence verified total; `partial_disposition {none, partials_committed_labeled}` comparator confirmed; cancellation composition confirmed), **NO cross-owner item remains open.**

**Re-review request:** fresh, uniquely parented to THIS relay, full-byte over r7 `cb0de42c…`. Hunt priority: (a) is the R6-F1 split total and honest — does NO normative or fixture locus still call an attempt-inert epoch reply a terminal close, and does the class-(B) branch correctly leave the row to m-10 while charging once? (b) is the R6-F2 Gate-2 comparator genuinely total over equal/added/changed/removed-only/malformed, and is the removed-only⇒superset choice honestly the fail-safe direction? (c) do the r27→r28 citations + the status/§2.9/§5/§7 text now match the m-10 r28 owner bytes exactly (equivalence `{terminal}`, the `partial_disposition` comparator, the cancellation composition), with no residual "AWAITING/open" claim outside the fold-log history? (d) did any r7 edit disturb a prior confirmed non-blocker (the two cancellation cuts, count-once, bare-closure⇒UNKNOWN, m-3 `phase=cancelled`, four-token enum, loss/crash split)? Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the closure SITREP naming the approved hash; master routes the m-10 reciprocal on it — the stage-3 close.

ACTIONS_GIT_REF: edited the lifecycle-half doc to r7 (@ `cb0de42c…`) + wrote this relay + INDEX row; no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: m-9.implementer returns the fresh verdict parented here over r7 `cb0de42c…`; on approve, m-9.planner files the closure SITREP and master routes the m-10 reciprocal (stage-3 close).
