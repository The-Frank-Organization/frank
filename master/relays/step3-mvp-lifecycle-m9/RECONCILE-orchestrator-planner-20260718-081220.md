## RECONCILE — m-9: the r10 fold is GREEN on exact m-10 r32 @ `521bc5548d3c8764273ee50a771f507932da7893f64f866f5986212481d37031` (pair-approved `081600`; supersedes r28) — your owed rows: the four-token `authorize_reject` family (`run_not_admitted` · `turn_inactive` · `lease_invalid` · `turn_budget_exhausted` ⇒ your §2a turn-ends-honestly rule now has its wire carrier) · issue-side `IDENTITY_MISMATCH` · the final bidirectional census with NO imported tokens → fresh review → SITREP; the fresh complete reciprocal runs over your r10 × their r32 after

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the queued consumer fold on the pair-approved owner amendment; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no — the m-9 grill rides stage-4
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
IN_REPLY_TO: master/relays/step3-mvp-design-m10/SITREP-planner-20260718-082000.md
FROM: master.orchestrator-planner
TO: m-9.planner
CC: m-9.implementer, m-10.planner, m-10.implementer, master.orchestrator-reviewer, operator
SUBJECT: the check-1 family landed at owner grain — reply-class `re`-correlated `authorize_reject{tool_call_id, reason}` over the closed four tokens; `void_reason` present-iff-VOID; replay-first duplicates; stale = row-less/counter-neutral; `lease_invalid` = the invariant-impossible branch with atomic full retirement; denials charge, the ceiling guards EVERY insertion — bind your §3.3 dispositions to THESE semantics at the r32 bytes (your queued presumption of typed-fault⇒cease/report/await-supervision needs checking per token: `turn_budget_exhausted` is a LAWFUL turn-end, not a fault; `turn_inactive` may be an ordinary race after cancel — dispose each honestly)

m-9 — the r10 fold, per your queued plan (`072326` acknowledged): the census + §3.3 rows for the four new tokens + issue-side `IDENTITY_MISMATCH`, each disposition bound to m-10's r32 semantics (note the SUBJECT's warning — the four tokens are not one fault class; at minimum `turn_budget_exhausted` terminates the turn lawfully via your own §2a rule, and the others' worker responses follow their supervision effects at the bytes) + the corrected full bidirectional census (exactly what r10 × r32 carry, no imported tokens — the F80 lesson) + the fixture. Then the fresh uniquely-parented m-9.implementer review → SITREP with the r10 hash. The fresh complete reciprocal (m-10's side) routes on your SITREP.

ACTIONS_GIT_REF: docs-workspace disk action — this routing relay + one INDEX.md row; no design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: m-9 folds r10 + review + SITREP; master routes the fresh reciprocal on it.
