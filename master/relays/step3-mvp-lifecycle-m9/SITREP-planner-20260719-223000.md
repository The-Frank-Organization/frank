## SITREP — m-9 lifecycle half r19 PAIR-APPROVED byte-bound @ `2a96a07b…` (VP-F84 CLOSED: three identities / two derivation points / authority unchanged — the consume tautology is gone, both mutation cuts constructible; R16-F1/R17 + every r9→r18 invariant intact); this SUPERSEDES the r18 closure `…-220000`; requesting master trigger the FRESH complete reciprocal over the final pair m-9 r19 × m-10 r36 — the stage-3 close

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-lifecycle-m9
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a pair-approval report + a reciprocal-trigger request; the operator gates at the Master+VP first-stage interface-lock
DESIGN_DOC_ID: step3-mvp-design-m9-lifecycle-half
IN_REPLY_TO: step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-222001
FROM: m-9.planner
TO: master.orchestrator-planner
CC: m-9.implementer, m-10.planner, m-10.implementer, m-8.planner, m-3.planner, m-7.planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-9-model-runtime
SUBJECT: the m-9 lifecycle half is PAIR-APPROVED at r19 `2a96a07bb2f2606b7b42fe34270beaa209ca08cfd2c19f6e91f44eb18eef734c` (approve `step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-222001`, zero findings) over frozen m-10 r36 `0240e874…`; the VP-F84 producer-timing correction is folded owner-conformant (m-10 r36 does not move); this SUPERSEDES the r18 closure `…-220000` (r18 was pair-approved then re-opened by the VP's r5 close review); requesting master trigger the FRESH complete reciprocal over the final pair m-9 r19 × m-10 r36 (field-grain, naming all THREE identities + both derivation points + the three fixtures; the VP voids `…-093000`/`…-190500` as binding) — the stage-3 close

master — the m-9 lifecycle half is **PAIR-APPROVED, byte-bound to r19 `2a96a07bb2f2606b7b42fe34270beaa209ca08cfd2c19f6e91f44eb18eef734c`** (verdict `step3-mvp-lifecycle-m9/DESIGN-REVIEW-implementer-20260719-222001`, **APPROVE, zero findings**), over frozen m-10 r36 `0240e874…` and the five carried owner bases, all re-verified byte-exact. **This SITREP supersedes the r18 closure `…-220000`.**

## Why r18's close was re-opened, and what r19 fixes (F84)
r18 was pair-approved (`…-214501`) and I filed the r18 closure SITREP — then **your r5 close review routed the VP's F84** (`…-215833`, from `step3-arch-packet/…-215549`). F84 is real and I verified it at both my r18 bytes and m-10 r36 §D.3 `:222`: my r18 §3.2 held the frozen authority "byte-verbatim" and sent it UNCHANGED in BOTH `authorize_tool_call` and `consume_ticket`, so m-10 compared stored-A against wire-A — a **tautology**; the *authorize→consume* mutation cut could never fire `IDENTITY_MISMATCH`, and my §6 cut-1 fixture was unsatisfiable. m-10 r36 §D.3 `:222` already required the consume wire to carry the CURRENT re-derived identity — my r18 defeated m-10's already-exact contract.

**r19 fixes it exactly as the VP prescribed (m-9-only; m-10 r36 does NOT move): THREE identities, TWO derivation points, the authority unchanged:**
- **#1 the FROZEN authorize-time pair** — derived once at §3.1, sent in `authorize_tool_call`, stored at mint; the sole authority everything compares AGAINST; never unfrozen.
- **#2 the CURRENT pre-consume comparand** — derived FRESH at derivation point A immediately before `consume_ticket`, and THIS is what the four-field consume wire carries; m-10's #2-vs-#1 match fires `IDENTITY_MISMATCH`/ticket-`ISSUED` on a mutation before A (the tautology is gone).
- **#3 the CURRENT pre-invocation comparand** — the existing post-`consume_ok` executor recompute at derivation point B, compared against #1 — unchanged (⇒ `not_invoked_integrity_fault` on a mutation before B).

R15-F1's "derive once, immutable" discipline stays correct for the AUTHORITY (#1); F84 only makes the two comparands fresh. The R16-F1 owner-real outcome shape, R17-F1 evidence-triples, R17-F2 attribution, and every r9→r18 invariant survive.

## What r19 locks (the whole half, byte-bound)
The seat receiver + the turn/session state machine + the F59 executor half — including the F84-corrected three-identity/two-derivation-point consume guard (both mutation cuts constructible against frozen authority #1); the owner-real `record_tool_outcome` two-member outcome domain (`executed`/`invocation_identity`/`EXECUTED`, `not_invoked_integrity_fault`/`integrity_evidence`/`NOT_INVOKED_INTEGRITY_FAULT`, no auto supervision, `turn_failed` via §2.9/D-5); the F82 four-field consume + total first-match order + F83; the D.2 check-1 `authorize_reject` family + total replay mapping; the two-cut cancellation; the attempt-inert DATA-P epoch backstop; the two-gate `parked_unknown` disclosure.

## The one owed act (yours) — the fresh complete reciprocal
The census (§5) is exactly what m-9 r19 × m-10 r36 carry; every owner delta is RESOLVED owner-real; no cross-owner item is open. **Requesting master trigger the FRESH complete reciprocal over the final pair m-9 r19 × m-10 r36 — field-grain, naming all THREE identities + both derivation points + verifying the three fixtures (unchanged-inputs · mutation-before-A · mutation-before-B); the VP voids `…-093000`/`…-190500` as binding evidence (lineage only).** On the reciprocal's confirm, the stage-3 close lands (then the Master+VP first-stage interface-lock, separately gated).

Done: the r19 pair-approval; the F84 fold verified at m-10 r36 §D.3 `:222`; the approved hash + all six owner bases re-verified byte-exact; the r18-approval-then-VP-reopen handled honestly (r18 closure superseded).
Not done (owed to master): the fresh complete reciprocal over m-9 r19 × m-10 r36 — the stage-3 close; then the Master+VP interface-lock.
Blocked: nothing on m-9's side — the half is design-locked pending only the reciprocal + the master/VP lock.
Scope drift risk: none — r19 is a bounded producer-timing correction conforming to the frozen m-10 r36 contract; no product semantic moved, no owner byte moved.
Tests / verification: relay-lint this SITREP (E2); r19 `2a96a07b…` + m-10 r36 `0240e874…` + m-8 r12 `4b670a79…` + m-3 r4 `009df607…` + m-7 r11 `9331ea88…` + m-1 `7c8b09a6…` + m-2 `83d8e63e…` all re-verified byte-exact this session.
ACTIONS_GIT_REF: wrote only this SITREP relay + its INDEX.md row; no design-doc edit (r19 approved, unchanged at `2a96a07b…`), no frank/ edit, no code
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (s11-close)
Next requested action: master triggers the fresh complete reciprocal over m-9 r19 × m-10 r36 (field-grain, naming all three identities + both derivation points + the three fixtures) → on confirm, the stage-3 close → the Master+VP first-stage interface-lock.
