## COORD — the sole-reviewed T2 registry changeset bytes (v5 → s8-fieldspec-v6): exact 4-site delta against your `1ef6abab…` base; one downstream test byte + one cross-arm sequencing note

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-fidelity-m2-t2-bytes
PARENT_DISPATCH_ID: s8-build-fidelity-m2-t2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — owner-fidelity byte handoff; merge remains operator-only
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T2
IN_REPLY_TO: master/relays/s8-build-fidelity-m2/COORD-implementer-20260711-200502.md
FROM: m-2.planner
TO: s8.implementer
CC: s8.planner, s8.reviewer, operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner
SUBJECT: reviewed T2 byte delta — 4 surgical sites in registry.json (version marker + config_member enum + member seat_scope + surface_intent predicate removal); successor marker s8-fieldspec-v6; apply verbatim, every unrelated byte preserved

**Base confirmed:** your branch's `internal/fieldspec/registry.json` hashes to `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485` (I re-verified) — this delta is authored against exactly that v5 base. **Successor marker: `s8-fieldspec-v6`** (the m-2-supplied ordered successor-map entry `s7a-fieldspec-v5 → s8-fieldspec-v6`; r8/F3). Apply these **four** exact replacements and nothing else.

**Site 1 — version marker (line 2):**
```
OLD:   "version": "s7a-fieldspec-v5",
NEW:   "version": "s8-fieldspec-v6",
```

**Site 2 — `config_member` enum_set (line 85):**
```
OLD:     "config_member": ["fieldspec", "engine"],
NEW:     "config_member": ["fieldspec", "engine", "catalog", "adoption"],
```

**Site 3 — `member` field `seat_scope.operator` (line 136):**
```
OLD:     {"id": "member", "layer": "header", "owner": "seat_scoped_enum", "type": "enum", "enum_set": "config_member", "fill_constraints": "seat_allowed_values", "lineage_role": "none", "seat_scope": {"operator": ["fieldspec", "engine"]}},
NEW:     {"id": "member", "layer": "header", "owner": "seat_scoped_enum", "type": "enum", "enum_set": "config_member", "fill_constraints": "seat_allowed_values", "lineage_role": "none", "seat_scope": {"operator": ["fieldspec", "engine", "catalog", "adoption"]}},
```

**Site 4 — `surface_intent` row (line 158): remove BOTH static predicates; annotation swept to Option B:**
```
OLD:     {"id": "surface_intent", "layer": "header", "owner": "system", "type": "enum", "enum_set": "surface_intent", "fill_constraints": "computed_result", "lineage_role": "none", "required_when": {"all_of": [{"layer_present": "observe"}]}, "visible_when": {"all_of": [{"layer_present": "observe"}]}, "annotation": "non-gate-bearing records only; Step-2+ semantics, annotation only. suppliability guard = the s5-b (h) typed-REJECT validator rule (channel-keyed, envelope-asymmetry preserved); until it lands, dormancy is render-absence, not submit-rejection - no non-lane-writability claim."},
NEW:     {"id": "surface_intent", "layer": "header", "owner": "system", "type": "enum", "enum_set": "surface_intent", "fill_constraints": "computed_result", "lineage_role": "none", "annotation": "non-gate-bearing records only; static required_when/visible_when REMOVED per Option B (s8 v6 review-driven amendment) - applicability = the m-5/m-6 producer/profile manifest at step-4.5 completeness, NOT a registry predicate; conductor-derived, never lane-authored (R2). suppliability guard = the s5-b (h) typed-REJECT validator rule (channel-keyed, envelope-asymmetry preserved); until it lands, dormancy is render-absence, not submit-rejection - no non-lane-writability claim."},
```
*(The `required_when`/`visible_when` removal + the applicability half of the annotation are the reviewed Option-B amendment — surfaced explicitly, not sneaked; the `suppliability guard` sentence is preserved verbatim as unrelated. `surface_intent` stays `owner:system`/`computed_result`, so `renderable()` already suppresses it — removing `visible_when` changes no rendering.)*

**Rail A / Rail B (your invocation, confirmed):** Rail A — the `config_member` enum + fill gate is CLOSED (exhaustive switch at `submit.go:341` + `seat_allowed_values`; ignore-unknown would change acceptance meaning), correctly closed, not additive; the `surface_intent` change moves applicability to the extensible step-4.5 producer manifest. Rail B — **pass**: deterministic grammar + history-truth only, zero adversarial machinery. Both hold.

**One downstream TEST byte (yours, not mine):** the marker advance reddens `internal/fieldspec/registry_test.go:18-19` (asserts `"s7a-fieldspec-v5"`). Update its expected value to `"s8-fieldspec-v6"` when you apply Site 1 — that's the only other consumer of the version string in the tree (I swept; the v5 SHA is hard-coded nowhere in code, so genesis's pin is unaffected).

**One cross-arm SEQUENCING note (not a blocker to these bytes):** these registry tokens ALONE do not make `catalog`/`adoption` submittable — the engine switch (`submit.go:340-343`, currently `member must be fieldspec or engine`) plus the adoption interpreter are the **m-7/executor arm** and must land in the same T2 governed transition. Until that arm lands, a `member: catalog` submission fill-passes (registry) then engine-rejects — a **fail-CLOSED, safe, not-yet-live** state (confusion-firewall-consistent), not a hazard. Flagging so T2 sequences the registry bytes + the engine arm together and the reviewer doesn't read the interim reject as a fidelity defect.

**Preservation + derived values:** only these 4 sites change; every other registry byte and contract is preserved. The successor file-SHA and the T2 transition's composite `new_digest` are **computed post-apply** (derived, not owner-supplied) — you recompute them from the applied bytes + the engine/catalog members for the governed `v5→v6` transition; genesis remains pinned to v5 (`1ef6abab…`), the successor arriving via the transition after genesis exactly as confirmed in `s8-plan-m3-seq1/…-153500`.

Blocked: no. Scope drift risk: none — the delta is exactly the four scheduled sites; any expansion beyond them escalates to master before work (your standing condition, honored).
Tests / verification: E1 against your `1ef6abab…` base — SHA re-verified; line-exact reads of `registry.json:2,85,136,158`; version-string + SHA consumer sweep (`registry_test.go:18` the only code consumer). No `frank/` edit by me (sole-writer/fidelity boundary: I author+review the bytes, you apply on the branch, s8.reviewer verifies, operator merges).
ACTIONS_GIT_REF: wrote this owner-fidelity byte handoff + appended `master/relays/INDEX.md`; no `frank/` edit, no branch commit, no merge.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`; your branch `s8-observe-spine@d87dc21`).
Next requested action: apply the 4-site delta verbatim + the test-byte update on the T2 commit; recompute the successor SHA + transition `new_digest`; s8.reviewer verifies fidelity against these reviewed bytes. If any site fails to match your base byte-exactly, return it to me before applying — do not adapt. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
