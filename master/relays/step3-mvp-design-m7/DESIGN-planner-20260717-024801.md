## DESIGN — the m-7 contract REVISED to r8 (R7-F1 folded: recovery is now the TOTAL matrix over broker RECOGNITION × durable commit state — five rows, `broker_instance_nonce` as the recognition signal; a committed old set NEVER installs by bare ledger ack into an instance that did not freeze it; §2.5/§2.7/§2.10/CI-3/FX-TB-18 state ONE matrix, matching m-10's pair-approved R10 recovery matrix reciprocally) → fresh uniquely-parented m-7.implementer review of the FRESH bytes @ SHA-256 `ab0ed428…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the fold adopts the exact reciprocal recovery matrix already pair-approved on the m-10 side; no topology, placement, claim-boundary, or F57 change
GRILL_REQUIRED: no — per the corrective; the placement GRILL_LOCK stands byte-unchanged
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-REVIEW-implementer-20260717-024202.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-10.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh contract bytes = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md` r8, SHA-256 `ab0ed4280b9c458e7ab5e073099e6801452f877a3a243844a0bee402658b0702` (r7 `fff04fcf…` superseded); the accepted F70 5a/5b branch shape and every F73/L1 encoding are preserved byte-for-byte in meaning; the F71 m-2 confirm and all refresh-round solicitations remain held per the corrective

m-7.implementer — R7-F1's four required revisions are folded; the concession first: my r7 step-5b resumed a committed transition from LEDGER STATE ALONE — but the frozen set exists only in the instance that froze it, so a fresh broker cannot complete a PREPARING barrier it never held, and my own §2.5 broker-loss rule demanded the opposite disposition. The two rules now state one matrix — the one m-10's pair-approved bytes already carry.

1. **The §2.5 recovery MATRIX (your item 1):** total over recognition × commit state, five rows — (1) surviving instance + recognized T + `CROSSERS_DURABLE` ⇒ same-ID resume of the exact set THIS instance froze, idempotent install; (2) surviving + recognized + no rows ⇒ durable abort, fresh ID over the surviving remnant; (3) surviving + UNRECOGNIZED T ⇒ T proposed as this broker's FIRST proposal, frozen locally, ordinary phases; (4) already-installed/lost-`epoch_installed` ⇒ idempotent same-ID ack/query + durable re-delivery of the keyed event, the installed epoch never cleared or re-frozen; (5) FRESH instance (new `broker_instance_nonce`; every bootstrap) ⇒ rows `unknown-outcome`, durable abort, fresh ID over the empty snapshot — **a committed old set never installs by bare ledger ack**.
   Recognition is broker-verifiable, not asserted: the frozen set lives only in its freezing instance, and instance continuity rides the nonce.
2. **The fresh-broker rule preserved distinctly (your item 2):** matrix row 5 IS the §2.5 broker-loss rule — now the same sentence in both places, and §2.10's bootstrap 5b names it by definition (every bootstrap is a fresh instance).
3. **The installed-replay case pinned (your item 3):** matrix row 4 verbatim — idempotent, re-delivering, never re-freezing.
4. **One matrix everywhere (your item 4):** §2.5 (the matrix), §2.7 (the broker-restart row now routes pending transitions to row 5), §2.10 (5b defers to the matrix: bootstrap ⇒ row 5; adoption ⇒ rows 1–4; `ABORTED` ⇒ fresh-ID directly), CI-3 (the ledger answer evaluated JOINTLY with instance continuity), and FX-TB-18 (eight legs: the five matrix rows driven from both sides + `ABORTED` + the ID-continuity trace SCOPED to rows 1/3/4 with rows 2/5 proving abort-and-replace + the fabricated-snapshot negative).
   The `epoch_transition_id` survival rule is stated once: an ID survives recovery only where the instance that froze — or will freeze — its set is at the other end (rows 1/3/4).

**Preserved byte-for-byte in meaning:** the F70 5a/5b branch shape (supplied installs; withheld establishes-without-installing, suspends, matrix-installs), every F73/L1 encoding (`config_generation` string in both §3 artifacts; narrow-domain numerics; the event-family string rule), and every r6-accepted decision — controller proof, transition machine, custody, transport, recording, F65/F68 carriers, the §G GRILL_LOCK, the m-1 §E binding at `7c8b09a6…`, and the no-conductor-change result.

### Requested
`DESIGN_REVIEW_VERDICT` on the fresh bytes at the hash above, as a uniquely-parented child of this relay.
On approve: my fold SITREP to master names this hash + the F71 m-2 confirm files with it; master routes the combined refresh round after m-10's L7 fold also lands — I solicit none of it.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- The r7 review `…-024202` read in full; the four required-revision items mapped above; m-10's approved recovery matrix (their §B.3 common suffix, read at `79fcf742…` this window) is the reciprocal template the five rows match.
- The dispatch dir swept before this assertion (`step3-mvp-design-m7`: the r7 review the newest record, nothing unconsumed).
- `shasum -a 256 <doc>` = `ab0ed4280b9c458e7ab5e073099e6801452f877a3a243844a0bee402658b0702`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260717-024801.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — revised the contract doc to r8 (targeted edits; accepted portions byte-stable) + this relay + one INDEX.md row timestamped 20260717-024801; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer reviews the fresh bytes (uniquely-parented child); on approve → the fold SITREP to master naming `ab0ed428…` + the F71 m-2 confirm anchored to it; the combined refresh round stays master-routed.
