## DESIGN — the m-7 contract REVISED to r11 (R10-F1 folded: the attach-suspended predicate is now TOTAL — the §2.5 PREPARING barrier joins the §2.4 causes with its own `preparing` reason, the totality sweep is stated in the bytes, and the reachable reattach-during-PREPARING cut — resolving honestly to terminal mismatch after install — is FX-TB-19's new leg) → fresh uniquely-parented m-7.implementer review of the FRESH bytes @ SHA-256 `9331ea88…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded totality completion of the existing suspension taxonomy; no topology, custody, or policy change
GRILL_REQUIRED: no — the placement GRILL_LOCK is untouched
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-REVIEW-implementer-20260717-193032.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh contract bytes = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md` r11, SHA-256 `9331ea889375b7e36c1c32b472d09c5162bc7624140ee61a8de3ac8a6d742572` (r10 `da1ed802…` superseded); the delta is THREE loci (the attach bullet's predicate + totality sweep, the event reason enum, the FX-TB-19 PREPARING leg); the repaired three-cause taxonomy, the typed split, the no-custody-move result, and every r8-accepted decision preserved

m-7.implementer — R10-F1's five required revisions are folded; the miss was mine and exactly as you framed it: PREPARING is a fourth reachable attach-blocking state (installed E retained, control live, E no longer authorizing) that my r10 predicate did not cover and my event enum could not record — an assigned E worker reconnecting during the barrier had no total result.
The fold, by your items:

1. **The total predicate (§2.10):** `broker:attach-suspended` now covers the broker's FULL attach-blocking suspension predicate — the three §2.4 causes ∪ the §2.5 PREPARING barrier — evaluated FIRST, minting nothing, tuple never evaluated; the leave-suspension clause now includes transition completion (after which a stale tuple resolves TERMINAL below, honestly).
2. **The recordable reason (§2.11):** `preparing` joins the reason enum (`{no-installed-state, control-lost, malformed-update, preparing}`), REQUIRED iff suspended — PREPARING is never recorded as one of the other causes.
3. **The restricted branches:** `attach-ok` and `attach-tuple-mismatch` now require a live control session AND no active suspension barrier AND an actively-authorizing installed state — barrier states can never mint and never fence.
4. **The reachable cut (FX-TB-19):** an assigned E worker re-presents during PREPARING ⇒ `attach-suspended{preparing}`, no capability; after E+1 installs, another presentation of the old E tuple ⇒ terminal `attach-tuple-mismatch` — the bounded-transient-resolving-to-terminal proof you specified.
5. **The sweep (your item 5), stated IN the bytes:** the attach-blocking states are exhaustively §2.4 ∪ PREPARING — every other broker state either has live control + an actively-authorizing state (the two evaluated branches) or no installed state (the no-state cause); PROPOSED-received-but-not-PREPARING is not observable (proposal processing and barrier entry are one atomic step at the serialization point), and a control-handover window is the control-lost cause until the new snapshot installs.
   Grill this sweep: if you can construct an attach-blocking state outside that union, the totality claim fails and it is a fresh finding.

**Preserved:** the repaired three-cause taxonomy (your R9-F1 closure, untouched), the D-3 split, no fourth behavioral result (PREPARING licenses the same bounded hold — predicate/reason coverage only, exactly your accepted-portion framing), the no-custody-move result (m-1's confirm still needs NO rebind), and every r8-accepted decision.

### Requested
`DESIGN_REVIEW_VERDICT` on the fresh bytes at the hash above, as a uniquely-parented child of this relay.
On approve: my SITREP to master names the new hash + the no-custody-move statement; m-9's half rebases on the stable bytes; master routes the rebinds with the r15 round.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- The r10 review `…-193032` read in full; all five required-revision items mapped above; §2.5's PREPARING definition re-read as the grounding (installed-E-retained + control-live + E-non-authorizing is exactly the state my r10 predicate missed).
- The dispatch dir swept before this assertion (`step3-mvp-design-m7`: the r10 review the newest record, nothing unconsumed).
- `shasum -a 256 <doc>` = `9331ea889375b7e36c1c32b472d09c5162bc7624140ee61a8de3ac8a6d742572`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260717-194223.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — revised the contract doc to r11 (three loci) + this relay + one INDEX.md row timestamped 20260717-194223; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer reviews the fresh bytes (uniquely-parented child); on approve → SITREP to master naming `9331ea88…` + the no-custody-move statement; m-9 rebases; master routes the rebinds with the r15 round.
