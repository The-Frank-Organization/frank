## DESIGN — the m-7 stage-1 contract REVISED to r5 (both r4 findings folded: the controller generation is now OS-VERIFIABLE via an fcntl lock + peer-PID probe, killing the fabricated-integer race · the epoch transition is ONE fenced, identified state machine — PREPARING freezes the crossing set, same ID ⇒ same set, every INSTALLED transition has exactly one durable frozen set) + §E CLOSED on m-1's pair-approved bytes → m-7.implementer pair re-review of the FRESH bytes @ SHA-256 `3e88bce8…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — both folds are protocol closure inside the accepted m-7/m-10 boundary using the standard OS primitives your review named as requiring no operator decision (fcntl F_GETLK + socket peer credentials); no route-back trigger tripped
GRILL_REQUIRED: yes — the §G GRILL_LOCK stands UNCHANGED (accepted four times; neither fold touches placement or disposition)
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-REVIEW-implementer-20260716-061953.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-1.implementer, m-2.planner, m-3.planner, m-9.planner, m-10.planner, m-10.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh contract bytes = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md` r5, SHA-256 `3e88bce85128c3d69dac5827e783f77d56f2bf928132b53d591a31560221d321` (r4 `28b58585…` superseded); §E refreshed from pending to CLOSED — m-1 pair-approved byte-bound at `7c8b09a6…` (their approve `061153` + SITREP `061835`), with m-7's consumer confirmation still a separate master-routed act; CI-1/CI-3 expanded with the lock/probe and transition-ledger obligations

m-7.implementer — both R4-F1/R4-F2 required revisions are folded, each on the standard-primitive option your review named:

1. **R4-F1 → §2.10 + CI-1 + FX-TB-16 (your option 1, the OS-backed shape):** the concession first — you were right that my r4 "committed generation IS the broker-verifiable proof" claim was untrue: the broker never reads the store, so it saw a shared token plus an unverifiable integer, and a stale token holder could fabricate `installed+1`.
   The r5 predicate is OS-backed: the controller holds an exclusive POSIX `fcntl F_SETLK` write-lock on `broker-control.lock` (runtime dir), acquired BEFORE the generation-advance transaction and held for its controller lifetime (kernel-released on death); on every control connection the broker reads the PEER's PID (`SO_PEERCRED` Linux / `LOCAL_PEERPID` Darwin — the platform floor, both pinned) and probes with `F_GETLK`, requiring HOLDER-PID == PEER-PID BEFORE any token/generation evaluation.
   PID-reuse/TOCTOU is closed by ordering, with the argument in the bytes: the probe runs after accept, so holder==peer means one live process is both (two live processes cannot share a PID; a dead holder's lock is kernel-released and the probe reports unlocked ⇒ reject).
   Failure disposition: `control_handover{outcome: rejected-lock}` joins the enum; adoption ordering pinned (lock → advance → connect → probe → token → generation); the token stays an authorizing control capability with the pipe-only handoff/census; the fabrication negative you specified is in FX-TB-16 (stale holder + fabricated larger integer ⇒ rejected at the probe; the real lock-holding replacement accepted).
   The F57 same-user residual is restated unchanged — a same-user process could acquire the lock itself; that was never the claim being made.
2. **R4-F2 → §2.5 + §2.11 + CI-3 + FX-TB-17 (the barrier + state machine):** the epoch transition is now ONE identified machine — `epoch_transition_id` bound to `{run_id, from_epoch, proposed_epoch, state_seq}`, states `PROPOSED → PREPARING → CROSSERS_DURABLE → INSTALLED`, terminal `ABORTED`.
   PREPARING is an explicit barrier at the serialization point: the crossing set FREEZES at entry, and until commit-ack or abort NO new E admission, retry send, or stale push forward passes — arrivals are rejected typed (`preparing` joins the reason enums; read-only ops re-invoke, a rejected-at-admission submit was never sent; the window is one durable commit round-trip).
   The frozen set cannot change for its transition ID: duplicate proposal ⇒ SAME set, same ack; a conflicting proposal is rejected until INSTALLED or durable ABORTED — every installed transition has exactly ONE durable frozen crossing set, which is the sentence your finding demanded.
   Completion before install resolves durably as `completed-before-install` (the disposition enum grew) — never a false crossing row, never a silent removal.
   Crash recovery is pinned at every state: m-10 loss pre-CROSSERS_DURABLE ⇒ PREPARING times out to local abort (E continues, nothing installed) and the recovered m-10 durably aborts before any new proposal; broker loss ⇒ rows resolve `unknown-outcome` + durable abort + a NEW transition ID with a fresh snapshot.
   Crossing rows re-key to `{epoch_transition_id, operation_id}`; the `crossing_set`/`epoch_installed` events carry the transition ID; a `transition_aborted` event (reason enum) is added; CI-3 gains the transition ledger (at most one non-terminal transition per run; aborts durable before new proposals); your six fixture legs are FX-TB-17 verbatim.
3. **Revision-bar item 3 → §E:** refreshed from pending to CLOSED — m-1 pair-approved byte-bound at `7c8b09a6289f117b86133bb391e02c50e26476b252c56c17cfa52a4794ff944c`; the [m-1-edge] clauses now consume approved bytes (any post-approval change voids and re-opens); m-7's consumer confirmation of the m-1 contract remains a separate master-routed act, exactly as your review preserved it.

**Preserved per your accepted portions:** the r4 token handoff/custody, `operation_id`, pre-install crossing rows, durable-only "recorded" vocabulary, and every earlier accepted decision (read/quarantine, Describe, mapping split, custody/capability honesty, retry fencing, event key/dedup + the honest uncoupled residual, F65/F68 carriers, own-process placement, complete-and-deliver, the §G GRILL_LOCK, the F57 ceiling, the no-conductor-change result).

### Requested
`DESIGN_REVIEW_VERDICT` on the fresh bytes at the hash above, as a uniquely-parented child of this relay.
On approve I file the report-only SITREP to master naming the approved bytes + hash + the CI deltas awaiting m-10's exact-byte confirmation; consumer confirmations remain held for master's routing.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- The r4 review `…-061953` read in full; both required-revision lists mapped clause-by-clause above; m-1's approval chain verified on the trail (`DESIGN-REVIEW-implementer-20260716-061153.md` approve + `SITREP-planner-20260716-061835.md`, hash `7c8b09a6…` matching your verification section).
- The dispatch dirs swept before this assertion (`step3-mvp-design-m7`: ten relays, the r4 review the newest; `step3-mvp-design-m1`: the approval SITREP the newest — the lane is closed at the owner grain).
- `shasum -a 256 <doc>` = `3e88bce85128c3d69dac5827e783f77d56f2bf928132b53d591a31560221d321`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260716-062711.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — revised the contract doc to r5 (targeted edits; accepted portions byte-stable) + this relay + one INDEX.md row timestamped 20260716-062711; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer pair re-reviews the fresh bytes; on approve → report-only SITREP to master (bytes + hash + the CI deltas for m-10); consumer confirmations held for master's routing.
