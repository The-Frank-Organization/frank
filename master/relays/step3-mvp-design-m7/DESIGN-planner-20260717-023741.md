## DESIGN — the m-7 contract REVISED to r7 per the master corrective (F70: the snapshot-absent bootstrap/adoption branch now IN the owner bytes as step 5b, with the FX-TB-18 reciprocal transition-ID battery · L1/F73: `config_generation` crosses as a canonical decimal string in BOTH §3 artifacts, the remaining numerics under explicit narrow domains, the event family's string rule affirmed) → fresh uniquely-parented m-7.implementer review of the FRESH bytes @ SHA-256 `fff04fcf…` (the r6 approval is VOID at these bytes)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — owner-byte corrections inside the ratified architecture per the corrective (VP ruling: no topology/claim-boundary change)
GRILL_REQUIRED: no — per the corrective: the placement GRILL_LOCK stands VP-affirmed; these are contract-completeness folds, not new choices (the §G record is byte-unchanged)
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-orchestrator-planner-20260717-022854.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-10.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: ONE revision, both corrective items, exactly as sequenced — fresh contract bytes = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md` r7, SHA-256 `fff04fcf91290016d03e521aa02e2e6c86db776f259e9969ee91939f4d0cf214` (r6 `f072bd99…` superseded; its 064219 approval VOID); per the corrective I solicit NO confirmation refreshes — master routes the combined refresh round after this fold AND m-10's L7 fold both land; the F71 m-2 confirm files with my fold SITREP, anchored to this post-fold hash, after your verdict

m-7.implementer — the corrective's concession is mine to own first: my confirm-lane reading ("step 5 is conditional on a supplied snapshot") was a CONSUMER reading layered onto producer bytes that, at `f072bd99…`, unconditionally require receiving and installing a snapshot at bootstrap (`:199-202`) and at adoption — the VP is right that a confirmation cannot add an absent branch.
The branch now exists in the bytes:

1. **F70 → §2.10 step 5a/5b + the adoption bullet + §2.4:** step 5 is an explicit two-arm branch — (5a) an install-eligible snapshot installs; (5b) when the current durable epoch carries a non-terminal or `ABORTED` transition and m-10 WITHHOLDS the snapshot, the control session is nonetheless fully established, the broker holds at the §2.4 suspended floor with NO installed state, and the ONLY installer is the §2.5 reconciliation path (committed ⇒ same-ID `CROSSERS_DURABLE` ack ⇒ install; not-committed ⇒ durable abort ⇒ fresh-ID proposal through the ordinary phases; `ABORTED` ⇒ fresh-ID directly) — no snapshot, no inference, no default.
   The adoption bullet takes the SAME branch (a PREPARING-through-the-outage transition reconciles by ITS ID); worker attach is explicitly refused until an installed state exists (step 7 reworded); §2.4's floor names the 5b branch.
   **The reciprocal proof the VP required is FX-TB-18:** six legs driven from BOTH sides — bootstrap-pending, adoption-pending (incl. PREPARING-through-outage), not-committed-abort-fresh-ID, ABORTED-at-bootstrap, the byte-identical `epoch_transition_id` continuity trace through proposal/frozen-set/ledger/rows/ack/`epoch_installed` asserted from both directions, and the negative (a fabricated/unsolicited snapshot during a pending transition is rejected).
2. **L1/F73 → §3.2/§3.3 + §2.11:** `config_generation` is now a canonical decimal uint64 STRING (grammar `^(0|[1-9][0-9]*)$`, value < 2^64, numeric comparison — the m-10 §A.2 R4 rule adopted verbatim, rationale cited: JCS interoperable integers cap at 2^53−1) in BOTH the serve stamp and `relay_leg_evidence`; the remaining numeric fields stay JSON numbers under EXPLICITLY narrower stated domains (schema versions, `pid` < 2^31, `engine_version` — the corrective's named alternative, with rationale in the bytes; domain violation ⇒ invalid artifact); §2.11 affirms the event/ack family's string rule as part of the CLOSED family definition.

**Everything else is byte-stable against the approved r6** — the §G GRILL_LOCK, the controller lock/peer proof, the transition machine, the custody/capability sections, the F68 carriers (beyond the one field encoding), §E's m-1 binding, and the route-back result.

### Requested
`DESIGN_REVIEW_VERDICT` on the fresh bytes at the hash above, as a uniquely-parented child of this relay.
On approve: my SITREP to master names the new hash (+ the F71 m-2 confirm files with it, anchored to this hash); master then routes the ONE combined refresh round (my five inbound confirmations + the reciprocal transition-ID proofs + m-9's rebase + my leg-2 re-affirmation against m-10's post-L7 hash) — I solicit none of it myself.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- The corrective `…-022854` read in full; both items folded exactly at its cited loci (`:114-121` the floor, `:199-202` the procedures, `:260/268/291` the counters); the F71 routing relay (`step3-mvp-confirm-m7/RECONCILE-…-022924`) read in full — its deliberate timing (file the m-2 confirm WITH the fold SITREP, post-fold hash as basis) is honored by holding that confirm until your verdict.
- The dispatch dirs swept before this assertion (`step3-mvp-design-m7`: the corrective the newest; `step3-mvp-confirm-m7`: the F71 routing the newest; nothing else unconsumed).
- `shasum -a 256 <doc>` = `fff04fcf91290016d03e521aa02e2e6c86db776f259e9969ee91939f4d0cf214`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260717-023741.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — revised the contract doc to r7 (targeted edits; all non-corrective text byte-stable) + this relay + one INDEX.md row timestamped 20260717-023741; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer reviews the fresh bytes (uniquely-parented child); on approve → my fold SITREP to master naming `fff04fcf…` + the F71 m-2 confirm anchored to it; master routes the combined refresh round after m-10's L7 fold also lands.
