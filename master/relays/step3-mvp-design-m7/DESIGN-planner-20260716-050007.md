## DESIGN — the m-7 stage-1 contract AUTHORED (shared transport/client boundary + channel/BROKER with the F64 fence + the F68 conductor-identity producer; GRILL_LOCK folded per F67) → m-7.implementer pair review of the FINAL bytes @ SHA-256 `bddf868b…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — stage-1 owned contract under the ratified amendment; the grill's selected answers alter no ratified topology or claim boundary (the F67 operator-routing trigger did not fire); the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: yes — per the supplement `…-043459` (F67); the durable GRILL_LOCK is folded into the DESIGN record at §G, BEFORE this final-byte review, as required
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-orchestrator-planner-20260716-043459.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-2.planner, m-3.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: both dispatch relays consumed (`…-041630` + the `…-043459` supplement: F67 grill flip, F68 identity-producer ownership, F69 m-2 in the consumer set); the contract doc = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md`, SHA-256 `bddf868ba050e201302ece0c63e9544b4f6402b0ad5302b49769d98f33818b2e` — the exact bytes under review; any revision re-hashes and gets a fresh uniquely-parented child review

m-7.implementer — the §7 stage-1 m-7 contract is authored.
Attack surface, by section (grill where it is weakest):

1. **§1 transport hoist:** the managed client owns exactly {lifecycle, reconnect-once with the content-hash-replay justification, byte-identical `shim:*` error classes, 0600 credential sourcing, best-effort push, frame codec}; the seam ruling in §1.3 assigns the ENTIRE re-render vocabulary (`mcp.go:209-276`) to m-2's module, not transport. **Grill:** is that seam ruling complete — does anything m-2-owned still strand in the skins, or anything m-7-owned leak into the mapping module? Is the verb-caller interface (§1.2) sufficient for BOTH the credential-holding direct path and the §2.3 capability path without widening either?
2. **§2 broker:** placement = own supervised process (GRILL_LOCK §G); the F64 fence gates every verb AND every push forward against m-10's epoch feed with a fail-closed floor + monotonic re-seed; §2.5 linearizes through one serialization point with complete-and-deliver-recorded in-flight disposition and a synchronous in-flight count as m-10's drain hook. **Grill hard:** (a) does complete-and-deliver survive an adversarial read of the §10 acceptance row ("an old-epoch worker can invoke NONE of the three verbs… nor receive/forward a new push") — I read an admitted-in-flight RESPONSE as neither an invocation nor a new push, verify that reading; (b) is the broker-restart story (§2.3 capability death + §2.7 re-bind/re-attach) free of a window where a stale generation could re-attach; (c) does §2.8's interface-absence rail actually close every m-10→verb path, including describe.
3. **§3 F68 producer:** ONE serve-start stamp (the `…-060542` mechanism extended by `conductor_build_digest`); the two-source binding (self-digest + independent external digest) stated at the honest D5 ceiling; the census/name-bytes routed to the build-lane round, fields pinned here. **Grill:** does any sentence overclaim attestation, and is the F65 scope edge (§3.4) stated so m-3 can confirm it without absorbing the leg?
4. **§5 route-back check: CLEAN claimed** — no conductor protocol/store/seat-surface change anywhere; §3's stamp is an OUTPUT artifact only. **Verify this claim independently — it is the dispatch boundary.**

### Requested
`DESIGN_REVIEW_VERDICT` on the final bytes at the hash above, as a uniquely-parented child of this relay.
On approve I file the report-only SITREP to master naming the approved bytes + hash; consumer confirmations (m-9, m-10, m-1, m-2 + m-3 on the F65 edge) route on master's direction.
On must-revise: I revise, re-hash, and you get a fresh child review.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- Grounding read in full this session: both dispatch relays (`041630`, `043459`); `STEP-3-MVP-AMENDMENT.md` r7 (`2f75f2a1…`) in full; m-1's authored contract (`aa90fa45…`) in full; `frank@502e06c` `internal/channel/server.go` in full + `cmd/frank-mcp/{main,mcp,schema,errors}.go` in full; my `step3-amend-m5-ceiling/SITREP-planner-20260715-060542.md` (the stamp §3 composes with).
- The dispatch dir swept before this state assertion (`ls master/relays/step3-mvp-design-m7/` — the two dispatch relays only, nothing unconsumed; the standing sweep-before-assert discipline).
- `shasum -a 256 <doc>` = `bddf868ba050e201302ece0c63e9544b4f6402b0ad5302b49769d98f33818b2e`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260716-050007.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — wrote the contract doc (new file; no locked doc edited) + this relay + one INDEX.md row timestamped 20260716-050007; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer pair-reviews the final bytes (uniquely-parented child review; the GRILL_LOCK is in the record per F67); on approve → report-only SITREP to master naming bytes + hash; consumer confirmations route on master's direction.
