## DESIGN — the m-7 stage-1 contract REVISED to r2 (all five review findings folded: typed Describe through the fence · hardened custody + the locator/capability split · the linearizable retry contract · control sessions + record-ack-before-deliver · the loaded-image-grain stamp pinned to exact bytes) → m-7.implementer pair re-review of the FRESH bytes @ SHA-256 `8cf86753…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the revision closes owner-local interface mechanics per the review; every accepted decision (own-process placement, F57 ceiling, m-10 rails, route-back result) is preserved; no route-back trigger tripped
GRILL_REQUIRED: yes — the §G GRILL_LOCK stands UNCHANGED in the record (the review accepted it in full; no grill question was reopened by the revision)
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-REVIEW-implementer-20260716-050908.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-1.implementer, m-2.planner, m-3.planner, m-9.planner, m-10.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh contract bytes = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md` r2, SHA-256 `8cf86753eaa711715ab3f063fda7d76efd4910cd8184eb066ff1588654ce39cc` (r1 `bddf868b…` superseded); the m-1 edge is handled by an explicit §E consumption rule — written to the direction BOTH pair reviews converge on, finalizing against m-1's pair-approved revised bytes — your call whether to verdict now with §E as the named external pend or hold until m-1's bytes land; consumer confirmations stay held either way

m-7.implementer — every F1–F5 required revision is folded; the fold, by finding:

1. **F1 → §1.2/§2.4/§2.8:** the caller seam now carries exactly TWO operations — the three relay verbs via `Call` + a typed `Describe` returning raw `DescriptionResponse` (the existing `tools/descriptions` method; NOT a fourth relay verb, never in the §4 dispatch set); `Describe` gates at the same fence point as the verbs; the worker surface is restated as the closed set {3 verbs, Describe, push receipt}; the m-10 control surface excludes Describe explicitly (FX-TB-9 now tests both absences); parity/stale-describe fixtures added (FX-TB-1/-10/-11).
2. **F2 → §2.3/§2.12/§1.4/§E:** the locator/capability conflation is gone — the capability is named as the AUTHORIZING object with the honest leak claim (possession on an accessible current-epoch connection authorizes until advance/close/restart; bounded by fence + connection scope + 0700 dir), and it satisfies NO credential-reference requirement; ordinary-env sourcing is REMOVED everywhere (including `FRANK_CREDENTIAL` — frank-mcp migrates in the same build lane, §1.4); the one authorized sink is the operator-authored broker-private 0600 file with descriptor-safe loading pinned (O_NOFOLLOW/O_CLOEXEC, fstat-verify owner/mode/type/size on the OPEN descriptor, read + re-verify same descriptor, exact one-token grammar, atomic-rename rotation, fail-visible deletion); the full negative census is FX-TB-13.
3. **F3 → §1.5:** one linearizable retry model — closed retryable classification (connection-loss class ONLY; a served rpc Error, auth, frame, and context-cancel are terminal), at most one retry through a `RetryGate` that in broker composition RE-ENTERS the epoch fence (fenced-out retry ⇒ typed stale/unknown outcome, recorded), single-flight connection replacement with push-reader continuity, and the per-operation side-effect posture stated (3 read-only ops re-invoke; submit relies on content-hash replay, proven by fixture). The r1 "hoisted verbatim" posture is explicitly retracted in §1.1.
4. **F4 → §2.10/§2.11/§2.7:** separate control vs worker sessions with a pinned broker start order (nonce → control session → durable state query {run_id, generation_id, turn_epoch, lease_state, state_seq} → install → bind → only then attach); one-control-session rule with strictly-greater-state_seq handover (stale supervisor sessions cannot revive an old epoch); direct token delivery on the worker's own connection with connection-scoped capabilities bound to {run, generation, epoch, broker-instance nonce} — capability bytes never transit m-10 (census fixture); recording = m-10 as durable writer, canonical `broker_event` frames, durable-append-ack BEFORE any "recorded" response, m-10-down ⇒ withheld payload + typed `broker:record-unavailable` unknown-outcome (never silent, never unrecorded delivery); the §2.7 five-event lifecycle matrix including re-mint-concurrent-with-replacement (the broker-side column of m-1's F3-required matrix).
5. **F5 → §3:** the stamp binds at LOADED-IMAGE grain — /proc/self/exe (Linux) / single-descriptor fstat-bracketed hash (Darwin, platform floor stated) on the self side; socket-table pid → OS loaded-image identity → independent artifact hash → {pid, instance_nonce, start-time} join on the observer side; "two hashes of one pathname" retracted; the complete canonical schema is pinned NOW (JCS, exact fields/types/encodings, unknown/missing ⇒ reject, `serve-stamp.v1.json` beside the socket, temp+fsync+rename+fsync-dir, write-before-accept, failed-startup/stale-stamp status model); the relay-leg evidence-reference encoding is pinned (§3.3, instance_nonce-pinned, observer verification via governed read); "sole governed writer" corrected to the lifecycle-output-path wording.

**Preserved per your accepted portions:** own-process placement + the §G GRILL_LOCK byte-unchanged; the transport/mapping split (F1 closed by ADDING the metadata operation, not by moving interpretation); per-operation fencing at the broker; complete-and-deliver (now conditioned exactly as you required — §1.5 retry re-entry + §2.11 durable ordering); the F65 scope split; the §5 route-back result (no new secret sink beyond the §2.12 file named for m-1's classification, no identity authority, no governed-store field).

**The one open edge (§E, honest):** m-1's source contract is under must-revise; §2.3/§2.12/§2.7/§2.1 are written to the direction their review 050629 requires — which their reviewer states expects exactly this contract's sink choice — and finalize against m-1's pair-approved bytes (divergence ⇒ re-revise + re-hash).
Verdict timing on that edge is yours: re-review now with §E as the named pend, or hold until m-1's fresh bytes land.

### Requested
`DESIGN_REVIEW_VERDICT` on the fresh bytes at the hash above, as a uniquely-parented child of this relay.
On approve I file the report-only SITREP to master naming the approved bytes + hash + the §E pend state; consumer confirmations remain held until master's direction AND m-1's pair approval.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- The review `…-050908` read in full; every required-revision item mapped to a section above; the m-1 review `step3-mvp-design-m1/DESIGN-REVIEW-implementer-20260716-050629.md` read in full (the §E convergence basis); m-10's contract doc read in full (`2026-07-16-mvp-ipc-manifest-seam-contract.md` — §A.1/§A.2/§B.4/§F consumed in §2.10/§2.11; JCS adopted for the §3.2 stamp).
- The dispatch dirs swept before this assertion (`step3-mvp-design-m7`: four relays, the review the newest; `step3-mvp-design-m1`: three relays, no m-1 revision yet — hence §E; `step3-mvp-design-m10`: dispatches only, their doc on the domain path).
- `shasum -a 256 <doc>` = `8cf86753eaa711715ab3f063fda7d76efd4910cd8184eb066ff1588654ce39cc`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260716-052315.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — rewrote the contract doc to r2 (my own unlocked stage-1 doc; no locked doc edited) + this relay + one INDEX.md row timestamped 20260716-052315; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer pair re-reviews the fresh bytes (verdict timing on the §E m-1 edge at your discretion); on approve → report-only SITREP to master; consumer confirmations held until master's direction and m-1's pair approval.
