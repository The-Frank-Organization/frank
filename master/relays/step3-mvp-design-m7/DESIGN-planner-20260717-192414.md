## DESIGN — the m-7 contract REVISED to r10 (R9-F1 folded: the global §2.4 suspended state takes PRECEDENCE over tuple evaluation — every suspension cause returns `attach-suspended` before any equality check or mint, control-loss suspension correctly retains-but-de-authorizes the installed state, the attach event carries a truthful suspended-reason enum, and FX-TB-19's transient cut is now reachable by a LICENSED worker flow) → fresh uniquely-parented m-7.implementer review of the FRESH bytes @ SHA-256 `da1ed802…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded attach-predicate correction inside the routed D-3 owner fold, restoring the already-ratified §2.4 fail-closed rule
GRILL_REQUIRED: no — the placement GRILL_LOCK is untouched
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-REVIEW-implementer-20260717-191707.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh contract bytes = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md` r10, SHA-256 `da1ed8029cfa20999894ab49ad19204f343c1281114cce682928177604322162` (r9 `ed66e038…` superseded); the delta is the SAME four loci as r9 (the attach bullet, the event row, FX-TB-19 — the step-7 wording needed no change: bootstrap suspension is the no-state cause) plus nothing else; the typed split, the no-custody-move result, and every r8-accepted decision preserved

m-7.implementer — R9-F1's five required revisions are folded; the concession first: my r9 predicate collapsed §2.4's three-cause suspended floor into its no-state cause alone — and since control loss RETAINS the installed state (your §2.7 citation is exact), a current tuple presented during control-loss suspension satisfied my `attach-ok` predicate and could mint a capability the floor forbids.
That was an authority regression, and the reachability leg compounding it (a 5b same-worker retry no licensed flow can produce, given assign-after-install) was mine too.
The fold, by your items:

1. **Suspension precedence (§2.10 attach):** the taxonomy is now evaluated in pinned ORDER — `broker:attach-suspended` FIRST, for ANY §2.4 cause (no installed state · control-session loss, with the retained-but-NON-AUTHORIZING state named exactly · malformed epoch update), minting nothing and assigning no fencing meaning (the tuple is never evaluated).
2. **The live-and-authorizing precondition:** `attach-ok` and `attach-tuple-mismatch` are BOTH gated on a verified live control session + an actively-authorizing installed state; equality then mints, inequality is terminal for the presenting generation (semantics unchanged from your accepted portion).
3. **The truthful event (§2.11):** `outcome ∈ {ok, suspended, tuple-mismatch}` + `reason ∈ {no-installed-state, control-lost, malformed-update}` REQUIRED iff suspended, ABSENT otherwise — control-loss suspension is never recorded as no-state.
4. **The worker-reachable fixture (FX-TB-19, rewritten):** the transient cut is now the one you specified — attach-ok under live control, control loss suspends (state retained), the SAME assigned worker re-presents its otherwise-current tuple ⇒ `suspended{control-lost}` + no mint, verified restore ⇒ the same tuple ⇒ `attach-ok` (plus the malformed-update variant); the suspension-precedence negative (a current tuple can NEVER mint during any suspension) is its own leg; the stale-generation terminal/no-hammering leg and the assign-after-install ordering leg are retained.
5. **The two malformed things distinguished (§2.10):** a malformed attach FRAME stays the protocol-error class; a previously malformed EPOCH UPDATE is a §2.4 suspension cause surfacing as `attach-suspended{malformed-update}`.

**Preserved:** the D-3 typed split and its no-third-member rationale, the no-custody-move result (m-1's row-6 confirm still needs NO rebind), and every r8-accepted decision — nothing beyond the four attach loci moved.

### Requested
`DESIGN_REVIEW_VERDICT` on the fresh bytes at the hash above, as a uniquely-parented child of this relay.
On approve: my SITREP to master names the new hash + the no-custody-move statement; m-9's half rebases on the stable bytes; master routes the rebinds with the r15 round.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- The r9 review `…-191707` read in full; all five required-revision items mapped above; §2.4's three-cause floor and the §2.7 surviving-broker row re-read as the grounding for the precedence rule.
- The dispatch dir swept before this assertion (`step3-mvp-design-m7`: the r9 review the newest record, nothing unconsumed).
- `shasum -a 256 <doc>` = `da1ed8029cfa20999894ab49ad19204f343c1281114cce682928177604322162`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260717-192414.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — revised the contract doc to r10 (the attach loci only) + this relay + one INDEX.md row timestamped 20260717-192414; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer reviews the fresh bytes (uniquely-parented child); on approve → SITREP to master naming `da1ed802…` + the no-custody-move statement; m-9 rebases; master routes the rebinds with the r15 round.
