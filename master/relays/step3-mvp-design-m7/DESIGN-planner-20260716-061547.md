## DESIGN — the m-7 stage-1 contract REVISED to r4 (both r3 findings folded: the durable `control_generation` adoption transition kills the equal-snapshot deadlock, with ONE pinned pipe-FD token handoff · cross-epoch operations are durably identified BEFORE the epoch installs via a two-phase crossing-set handshake, so the F64 record survives every m-10-down window) → m-7.implementer pair re-review of the FRESH bytes @ SHA-256 `28b58585…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — both folds land inside the accepted own-process topology and m-10's existing store ownership, exactly the two solutions your review named as requiring no operator decision; no route-back trigger tripped
GRILL_REQUIRED: yes — the §G GRILL_LOCK stands UNCHANGED (accepted three times; neither fold touches placement or disposition)
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-REVIEW-implementer-20260716-060815.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-1.implementer, m-2.planner, m-3.planner, m-9.planner, m-10.planner, m-10.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh contract bytes = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md` r4, SHA-256 `28b585856f67a4ce47397a07318d4bd0e71ac54274fa96324eb275916e3b692f` (r3 `8862780a…` superseded); §E refreshed to m-1 rev3 `7c8b09a6…` (their rev2 rejected on the Describe-surface summaries; rev3 normalizes them to the closed §1.4b surface matching my §2.8; in r4 re-review, approval still absent, edge held); the CI-1/CI-3 deltas are EXPANDED to carry the new durable obligations for m-10's exact-byte confirmation

m-7.implementer — both R3-F1/R3-F2 required revisions are folded, each on the option your review preferred:

1. **R3-F1 → §2.10 + CI-1 + FX-TB-16 (your option 1):** a third counter — the durable **`control_generation`** in a `broker_control` row `{control_token, control_generation, minted_at}` under m-10's exclusive store lock — is the HANDOVER authority, coupled to neither `turn_epoch` nor `state_seq`.
   Every controller start (spawn AND adoption) atomically advances it in one durable transaction BEFORE connecting; the broker accepts exactly token-match + strictly-greater COMMITTED generation; the equal-`state_seq` crash case now progresses because the replacement's advance transaction manufactures the strictly-newer value the comparison needs, with `turn_epoch` untouched — and the committed generation IS the broker-verifiable proof (a stale process holding the old token in memory cannot commit a fresher generation without the lock, presents stale, loses, and the rejection is recorded `control_handover{outcome: rejected-token|rejected-generation}`).
   The token handoff is ONE shape: 32 CSPRNG bytes as 64 lowercase hex, stored in the row, handed at spawn via ONE inherited pipe descriptor (read-one-line-close; no persistent copy outside the store, no file lifecycle, no argv/env; census-covered), rotated ONLY on spawn (adoption presents the existing token), classified honestly as an authorizing CONTROL capability — never a conductor credential, never a principal.
   The equality fixture you specified is FX-TB-16: crash immediately after an acknowledged install ⇒ equal snapshot ⇒ generation advance ⇒ successful adoption with no epoch change, raced against the stale holder.
2. **R3-F2 → §2.5 + §2.11 + CI-3 + FX-TB-17 (your pre-recording option):** every broker operation now has a stable **`operation_id` = `{broker_instance_nonce, op_seq}`** from admission, carried through attempts, retry fencing, crossing, completion, response, and every event row (concurrent crossings individually correlatable).
   The epoch install is a TWO-PHASE handshake: m-10 proposes E+1 → the broker snapshots the bounded in-flight E set at the serialization point and returns the `crossing_set` → m-10 durably commits one `crossing-pending` row per operation ID and acks → ONLY THEN does E+1 install.
   The mandatory F64 record therefore pre-exists every m-10-down window by construction: completion updates the keyed row (disposition enum) before payload delivery; an unresolved row recovers as `unknown-outcome` under m-10's §B.3 park discipline — durable, operation-correlated, never silent, never replayed; duplicate proposals are idempotent (re-snapshot, stable keys).
   Vocabulary reserved as you required: "recorded" = durable commit ONLY; uncoupled queue entries are **pending-resend** until committed; the overflow counter is named honest telemetry, not a per-event record; nothing in the mandatory set rides the pending queue.
   The open enums are completed (`forward_suppressed.reason ∈ {stale-epoch, suspended}`; the shared `op` domain defined once); `epoch_installed` now carries `crossing_count`; `control_handover` carries `{control_generation, outcome, state_seq, dropped_event_count}`.
   The crash triad you specified is FX-TB-17: persist-E+1-before-install / after-crossing-ack-before-install / complete-before-completion-ack — each leaving exactly one durable operation-correlated disposition.
   No broker-local durable spool — everything lands in m-10's existing store (CI-3 expanded with the crossing-row states/recovery/bounds for their exact-byte confirmation).

**Preserved per your accepted portions:** the r3 read/quarantine classification, the dial-in listener direction, the event-key/dedup protocol, the F68 stamp + evidence bytes, all earlier F1–F5 folds, the §G GRILL_LOCK, the F57 ceiling, and the no-conductor-change result (§5 restates both untaken route-back triggers).
The §2.7 app-main-crash matrix row is refined to match: same-epoch operations deliver normally; only response-coupled records withhold, and their crossing rows pre-exist.

**External holds (unchanged in kind, refreshed in pointer):** §E now names m-1 rev3 `7c8b09a6…` (r4 re-review; approval absent; edge held); CI-1/CI-2/CI-3 remain proposed deltas until m-10 confirms exact bytes — pair approval here does not approve those consumer bytes.

### Requested
`DESIGN_REVIEW_VERDICT` on the fresh bytes at the hash above, as a uniquely-parented child of this relay.
On approve I file the report-only SITREP to master naming the approved bytes + hash + the §E pend + the three CI deltas; consumer confirmations remain held until master's direction AND m-1's pair approval.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- The r3 review `…-060815` read in full; both required-revision item lists mapped clause-by-clause above; the ratified F64 wording re-read (`STEP-3-MVP-AMENDMENT.md:92`) against the §2.5 two-phase protocol; m-1's trail read through `DESIGN-planner-20260716-060705.md` (rev3 hash + the three normalized summaries).
- The dispatch dirs swept before this assertion (`step3-mvp-design-m7`: eight relays, the r3 review the newest; `step3-mvp-design-m1`: eight relays, newest = their r4 re-review request — no approval yet; `step3-mvp-design-m10`: dispatches only).
- `shasum -a 256 <doc>` = `28b585856f67a4ce47397a07318d4bd0e71ac54274fa96324eb275916e3b692f`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260716-061547.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — revised the contract doc to r4 (targeted edits; accepted portions byte-stable) + this relay + one INDEX.md row timestamped 20260716-061547; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer pair re-reviews the fresh bytes; on approve → report-only SITREP to master (bytes + hash + the §E pend + the CI deltas for m-10); consumer confirmations held until master's direction and m-1's pair approval.
