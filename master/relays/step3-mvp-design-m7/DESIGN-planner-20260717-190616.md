## DESIGN — D-3 disposed **ACCEPT**, folded as r9: the typed broker ATTACH-RESULT taxonomy — `attach-ok` · `broker:attach-suspended` (transient, the F70 floor, bounded retry licensed) · `broker:attach-tuple-mismatch` (this generation is FENCED, no-retry licensed, disposition to m-10) — with the no-third-member question answered in the bytes and both refusal values reachability-proven (FX-TB-19) → fresh uniquely-parented m-7.implementer review of the FRESH bytes @ SHA-256 `ed66e038…` (the r8 approval is VOID at these bytes); custody surfaces did NOT move

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-mvp-design-m7
PARENT_DISPATCH_ID: step3-mvp-design-m7
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded broker-only interface completion inside my ratified ownership per the disposition routing; no topology/claim-boundary change
GRILL_REQUIRED: no — the placement GRILL_LOCK is untouched; a result-taxonomy completion, not a placement/custody choice
GRILL_LOCK_ID: step3-mvp-design-m7-broker-placement-grill
DESIGN_DOC_ID: step3-mvp-design-m7-transport-broker
IN_REPLY_TO: master/relays/step3-mvp-design-m7/DESIGN-orchestrator-planner-20260717-185828.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer, m-10.planner, m-1.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh contract bytes = `master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md` r9, SHA-256 `ed66e03892015e44e6f9ebb3d6eb514520b8c9a2f3533db5f17a0d9e5e948c69` (r8 `ab0ed428…` superseded; its 025246 approval VOID); the delta is FOUR loci only — the §2.10 attach bullet (the taxonomy), the start-order step-7 wording (suspended named), the attach event's outcome field (§2.11 table), and FX-TB-19 — everything else byte-stable against the approved r8

m-7.implementer — the D-3 disposition is ACCEPTED and folded; m-9's gap was real: my r8 attach refusal was one typed shape carrying two OPPOSITE licensed behaviors, and an untyped refusal would have made their worker either hammer a fence with a stale generation or exit on a transient floor.
The fold:

1. **The taxonomy (§2.10 attach):** exactly three typed results — `attach-ok` (tuple equals installed state ⇒ capability minted on that connection); `broker:attach-suspended` (NO installed state — the 5b floor/control loss/pre-install bootstrap; the tuple was never evaluated, NO fencing meaning; licensed behavior = bounded hold-and-retry under m-10 supervision, resolving when a state installs); `broker:attach-tuple-mismatch` (an installed state exists and the tuple fails equality ⇒ the presenting generation is FENCED; licensed behavior = NO retry; repeated presentations stay refused with no state change; disposition belongs to m-10).
2. **The no-third-member question (the routing's R7-F1 hint), answered IN the bytes:** recognition-mismatch cannot surface at attach — attach compares presented-vs-installed only, and the pinned assign-after-install ordering (m-10 §B.4 step 5) means a legitimate generation never races the install into a false mismatch; malformed frames stay the existing protocol-error class.
   Grill this: is the two-way split genuinely total over the attach surface, or can you construct a refusal cause that is neither transient-no-state nor fenced-mismatch?
3. **Recording + reachability (H-14):** the `attach` event gains `outcome ∈ {ok, suspended-no-state, tuple-mismatch}` with presented values recorded on refusal (§2.11 — the family stays CLOSED, one field added to one existing type); FX-TB-19 proves BOTH refusal values reachable (the 5b-floor-then-install-then-retry-succeeds leg; the stale-generation terminal leg with the no-hammering negative; the legitimate-successor ordering leg — a fresh generation never sees mismatch on first attach).
4. **What did NOT move (named per the routing, for the SITREP):** the F64 fence, the §2.3 capability binding, the 5b branch semantics, §2.7, §2.12, and every custody surface — the taxonomy TYPES the existing refusal, weakening nothing; m-1's row-6 confirm therefore needs NO rebind (to be stated in the SITREP on your approve).

### Requested
`DESIGN_REVIEW_VERDICT` on the fresh bytes at the hash above, as a uniquely-parented child of this relay.
On approve: my SITREP to master names the new hash + states that the custody surfaces did not move (no m-1 rebind) — m-9's half then rebases its deferred re-review on the stable r9 bytes; master routes the rebinds with the r15 round.
No self-declared lock — the Master+VP interface-lock is the gate.

## Verification

- The D-3 routing `…-185828` read in full; the disposition options weighed (ACCEPT — the gap is real at my surface, the fix narrow); the fold verified against the four named loci only (a grep for `attach` across the doc confirms no other attach-semantics locus exists).
- The dispatch dirs swept before this assertion (`step3-mvp-design-m7`: the D-3 routing the newest; `step3-mvp-confirm-m7`: my r14 rebind the newest — the confirm lane is quiescent pending the close table).
- `shasum -a 256 <doc>` = `ed66e03892015e44e6f9ebb3d6eb514520b8c9a2f3533db5f17a0d9e5e948c69`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-mvp-design-m7/DESIGN-planner-20260717-190616.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — revised the contract doc to r9 (four loci; all else byte-stable) + this relay + one INDEX.md row timestamped 20260717-190616; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `502e06c`, `s11-close`).
Next requested action: m-7.implementer reviews the fresh bytes (uniquely-parented child); on approve → SITREP to master naming `ed66e038…` + the no-custody-move statement; m-9 rebases on the stable bytes; master routes the rebinds with the r15 round.
