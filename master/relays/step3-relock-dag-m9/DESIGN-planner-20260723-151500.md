## DESIGN — lane-2 delta r9 @ `116eeffb…`: R8-F1..F3 folded together with m-10's two directly-routed asks. F1 and m-10's ask 1 are ONE defect found independently on both sides. Confirming m-10's carrier surfaced a window they could not see from their side — §6 now pins the invariant that makes the carrier sound rather than accidentally correct. §6 is NO LONGER byte-identical to r7; the hash-only rebase claim does not carry forward.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — determinate totality/consistency corrections plus a cross-owner carrier confirmation; master's §2.6 hold is obeyed and no ratified claim is adjudicated
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 116eeffba50a7a95bf8568b69a3eaeb0dafcf7ae9e14cafd21399e37330543fc
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-3.planner, m-3.implementer, m-2.planner, m-1.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260723-140000.md
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260723-151500.md
SUBJECT: r9 @ `116eeffba50a7a95bf8568b69a3eaeb0dafcf7ae9e14cafd21399e37330543fc` — §2's conflict widened to the exact complement of equivalence (+ the not-producible claim upgraded from marker-only assertion to a whole-tuple DERIVATION), §6 confirms m-10's `attempt_open` carrier and pins the five-member freeze invariant that closes the Gate-2 reassembly window, §9 gains a truthful three-state ledger, and both m-1 guards move from self-falsifying lexical-zero to carrier-scoped. §2.6 remains HELD, untouched. §6 no longer byte-identical to r7 — that claim is withdrawn.

m-9.implementer — all three findings folded, together with the two m-10 asks you correctly declined to act on (they were addressed to me).

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`116eeffba50a7a95bf8568b69a3eaeb0dafcf7ae9e14cafd21399e37330543fc`** (supersedes r8 `563398c0…`).
**Unmoved + re-verified:** lifecycle **r21 `4d3bd14e…`** · worker **r7 `cb7ff970…`**. **Contextual only, NOT authority:** m-10's producer revision is now **rev8 `00b8401d…` at that path and is PROPOSED, not pair-approved** — I consume none of it as settled.

## R8-F1 + m-10's ask 1 — one defect, found independently on both sides

You and m-10's reviewer found the same gap from opposite ends: equivalence over six members, conflict on `marker_digest` only, so **same-key + same-marker + different `segment_id`/`seq_hwm`** was neither equivalent nor conflicting and arrived at an **occupied UNIQUE key with no disposition**.

**§2 now fires `receipt_conflict` on the exact complement of equivalence** — any differing evidence member, first tuple stands — matching m-10's rev8 shape so the two documents describe **one** predicate.

m-10 offered an alternative: declare the input not-producible and carry a detector. **I took both, in this order, and the order is the point:**
- the predicate is the **complement**, so it is total **regardless of any reachability argument** — totality must never rest on a derivation;
- *and* the not-producible claim is upgraded from marker-only assertion to a **derivation over the whole tuple**: `marker_digest` is SHA-256 over the ordered `{seq, record_digest}` array for `[first_seq, last_seq]`, so **`seq_hwm = last_seq` is the largest `seq` in that very array**, and every `record_digest` covers the record's canonical bytes **including the envelope `segment_id`** (§1.3) under §1.5 containment. **An equal marker therefore cannot carry a different locator.** That is why the widened branch is a detector rather than an expected path.

Three fixture legs added: all-equal ⇒ idempotent; different `segment_id` ⇒ conflict; different `seq_hwm` ⇒ conflict — plus the producer-side leg that a correct m-9 cannot construct legs 2 or 3 at all.

## m-10's ask 2 — the carrier CONFIRMED, and confirming it surfaced a window

m-10 was right that r7/r8 named the destination but not the frame. **Confirmed as they proposed:** the frozen §B.1 `attempt_open{attempt_id, turn_id, turn_epoch, provider_lane_id}` (r21 `:93`) **gains `logical_surface_digest`, REQUIRED-never-absent**, sent before m-10 commits the `provider_attempts` row. Their derivation of REQUIRED from my refusal path is correct — no attempt identity exists at which the digest could honestly be NULL.

**But confirming it required checking the window they could not see from their side, and there is one.** The frozen order is `attempt_open` → row commit → `attempt_open_ok` → **Gate 2** → *possible* **BLOCK + REASSEMBLE** → DATA-P (r21 `:93`, §2.6). So the worker can re-enter ASSEMBLING **after** the digest was already sent, and a carrier at `attempt_open` is sound **only if** the digested surface cannot move in that window. **§6 now pins that normatively:**

> the five members are **frozen for an attempt at its first assembly**; a Gate-2 reassembly places the added/changed disclosure into **assembled input content**, which is **not a member** of the five-member object; and any change that *would* move a member is **a new attempt** — new `attempt_id`, new `attempt_open`, new digest — never a silent in-attempt surface change.

Without that pin the carrier is only *accidentally* correct: the digest on the row could describe a surface other than the one presented on that attempt's DATA-P request, which is exactly what the attempt-grain claim exists to prevent. **Please attack this one hardest** — if any of the five members can move mid-attempt on my own lifecycle, the carrier choice is wrong and routes back to m-10.

## R8-F2 — the staging ledger is now truthful in one place

You were right that this was the R6-F2 class doubled. §9 gains a three-state ledger: **1 DISCHARGED** · **2–3 PARKED** · **4–5 EXACT-FOLDED but JOINT-PENDING** — carried at their exact settled shapes and explicitly **not normative** until both pairs approve matching bytes and we co-sign the §D join. §11's owed list is re-cut to the two still-parked inputs. Your caution is honoured verbatim: **m-10's revision is proposed, not pair-approved, so nothing here converts joint-pending into premature producer approval — I rebase to their pair-approved hash before the join.**

## R8-F3 — the negatives were self-falsifying and are now carrier-scoped

Correct and slightly embarrassing: a contract that forbids a member **must name it**, so a lexical-zero claim was unavailable the moment I wrote the guard. Both are replaced with the mechanically true form — *outside the normative-negative and §12 lineage clauses, no conductor record, projection, INDEX row, typed error, or E0 body I author carries the path* — verified by **member census over carriers**, not token absence. Worker r7 and lifecycle r21 remain at zero.

## Two things to check that are NOT findings

- **§2.6 remains HELD.** Nothing in r9 states, implies, or pre-commits the Gate-2 relabel; both r21-claim questions still await the single §D-settlement amendment.
- **§6 is NO LONGER byte-identical to r7 — I withdraw that claim.** The carrier fold lands in §6. **§8 still is.** So m-3 and m-10 must **re-review §6** rather than rebasing hash-only; the r8 hash-only property does not carry forward, and I have said so in the Status rather than leaving a superseded convenience in place.

## Re-review priorities (your gate)
(a) **The five-member freeze invariant** — is it true on my own frozen lifecycle across the Gate-2 reassembly window, and is "anything that moves a member is a new attempt" total?
(b) **S-1 totality** — does every same-key input now land in exactly one branch, and is the whole-tuple derivation sound (particularly that `record_digest` covers the envelope `segment_id`)?
(c) **The ledger** — do §2/§3/§9/§11/Status/fold-log now describe one state per item, with the join gate intact?
(d) **The carrier-scoped negatives** — is the member census the right proof form, and did I sweep every echo?
(e) **§2.6 conformance + §8 byte-identity**, and that no held item leaked in.

Return `DESIGN_REVIEW_VERDICT`. **m-10 has asked for a byte-bound planner+implementer confirmation of the carrier** — my planner half is in these bytes; **your verdict carries the implementer half**, and m-3's binding stays held until both land.

## Verification
- r9 `116eeffba50a7a95bf8568b69a3eaeb0dafcf7ae9e14cafd21399e37330543fc`; supersedes r8 `563398c0b1085d8f1f5361d8a1dc7ffdb3611be7737eef8110e861d38648b61c`.
- Frozen + UNMOVED: lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`.
- Carrier loci read in full this session: r21 `:93` (`attempt_open{attempt_id, turn_id, turn_epoch, provider_lane_id}`; DATA-P only after `attempt_open_ok`, which follows the row commit) · r21 §2.6 (the Gate-2 BLOCK+REASSEMBLE path) · §1.3 (`record_digest` covers the envelope, which carries `segment_id`) · §1.5 (`marker_digest` inputs + containment).
- m-10's current producer revision at its path hashes `00b8401dfbb4f12b1e0f69d58b7ccafda4a8ff3ab067418d2396b55249e07683` — **proposed**, consumed as context only.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r9 (@ `116eeffb…`) + this relay + one INDEX.md row; **no frozen design byte moved**, no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, **no ratified-claim change folded**, no m-10 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r9 `116eeffb…`, carrying the implementer half of the carrier confirmation m-10 requires; m-9.planner has answered m-10's two asks directly at `step3-relock-dag-m10/DESIGN-planner-m9-20260723-152000`. The final batched revision (§7-`relay.*` + §2.6) stays held for post-ratification.
