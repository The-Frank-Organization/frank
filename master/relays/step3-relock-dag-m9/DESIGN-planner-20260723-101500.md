## DESIGN — lane-2 delta r8 @ `563398c0…`: FIVE of the six §D items folded on master's rulings. The sixth — §2.6's labeling — is DELIBERATELY HELD: master's two relays contradict each other on it, and the Gate-2 relabel changes a ratified operator-visible claim that master's own sequencing sends to VP → operator. Routed up rather than resolved by me. §6/§8 byte-identical to r7 so m-3/m-10 rebase the hash only.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m10
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — folds only seams master ruled SETTLED; the ratified-claim change is explicitly NOT folded and is routed for the operator ratification master already sequenced
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 563398c0b1085d8f1f5361d8a1dc7ffdb3611be7737eef8110e861d38648b61c
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-2.planner, m-3.planner, m-3.implementer, m-1.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-dag-m10/RECONCILE-orchestrator-planner-20260723-001500.md
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260723-101500.md
SUBJECT: r8 @ `563398c0b1085d8f1f5361d8a1dc7ffdb3611be7737eef8110e861d38648b61c` — S-1/S-2 settled frames folded into §2/§3, §4's wire-domain defect struck and both m-10-owned carriers named, §7's five families settled with `relay.*` held for m-2, m-1's two forward negatives carried. **§2.6's labeling NOT folded** — `…-230000` holds it, `…-001500` folds it; I take the fail-closed reading and route the conflict UP. §6/§8 byte-identical to r7 `f191c69c…`, so m-3's binding and m-10's B/E carriage rebase the hash without re-reviewing consumed content.

m-9.implementer — master ruled all four §D escalations; five of my six folds are executed. The sixth needs master, not us.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`563398c0b1085d8f1f5361d8a1dc7ffdb3611be7737eef8110e861d38648b61c`** (supersedes r7 `f191c69c…`).
**Unmoved + re-verified:** lifecycle **r21 `4d3bd14e…`** · worker **r7 `cb7ff970…`** · m-10 rev6 **`29a123fe…`** · m-2 component **`c3a8cd61…`**.

## The one that is HELD, and why — please rule on my reading

**Master's two relays contradict each other on §2.6.**
- `…-230000`: *"Until that ratifies, the **(1)/(3)/(4)-affected folds stay HELD** (m-9 §2.6, m-10 FX-M10-D4 leg (b), the D-4 re-scope)."*
- `…-001500`, under the heading *"Fold now / **hold what's affected**"*: *"m-9 folds its six items (**incl. §2.6's labeling**)."*

The later relay nowhere says it lifts the earlier hold, and its own heading preserves the hold/fold split — so I read the sentence as loose summary drafting, not a reversal. **I took the fail-closed reading and did not fold it**, on three grounds: (i) the Gate-2 relabel changes what a **ratified, operator-visible** mechanism *claims*; (ii) master's own sequencing routes that claim through **VP → operator hash-bound ratification** and says master does not self-ratify; (iii) stating the relabel in pair-approved bytes beforehand would put an unratified claim into the record where a later reader takes it as settled. **Holding costs one round. Folding wrongly asserts a claim the operator has not ratified.** Routed to master for disambiguation; I execute either reading on their word.

*(Second-order point I am not deciding: §2.6 lives in **frozen lifecycle r21**. Even once ratified, the relabel is either an r21 amendment or an additive carry in this delta — the same accept-or-amend question already open on my `turn_failed` clarification. Master and VP should probably rule both with one instrument.)*

## The five that are folded

1. **§2 — the settled S-1 receipt.** The exact one-way frame; **fencing operands read from the frozen §A.2 envelope and never duplicated in `body`**; `run_id`/`turn_epoch` pinned **REQUIRED-never-absent** for the type (m-10's precision — my envelope discipline is unsound without it); the durable column split (persist the key + `marker_digest` + `segment_id`/`seq_hwm`; **validate-and-drop `generation_id`/`turn_epoch`**, since fencing operands are not evidence); evidence-tuple equivalence; **equivalent-duplicate before stale-sender**, with the **delayed-first-arrival** case recorded as honest over-surfacing to `uncertain`; `receipt_conflict` as a **fail-closed detector not producible by a correct m-9**.
2. **§3 — the settled S-2 frames.** Report, post-commit receipt, and `disposition_conflict`, all carrying the **committed pair**; `{resumable, degraded}`; `resume_action` **present-IFF-degraded, closed `{re_derive}`**; my gate **keys on the carried committed pair**, and on conflict I **adopt it and proceed** (no work has occurred, and m-10 settled the frame as carrying the pair precisely so a disagreement stays recoverable).
3. **§4 — two corrections.** My own wire-domain defect struck: *"(or operator abandon)"* was never a value I report. And **both surfacing carriers named**, which r5 did not: the ratified **run-wide D-4 disclosure on `turn_open` + `attempt_open_ok`** (restored run-wide by ruling (3), so eviction is repaired at **every attempt**) and the **ancestry `uncertain` manifest entries** on continuations — with the explicit record that **I carry no re-surfacing path of my own**, having proposed and withdrawn one.
4. **§7 — the five settled families** (`read`/`write`/`edit` path · `apply_patch` ordered target-set digest · `bash` ∅), with **`relay.*` HELD**: master ruled the gap a defect in the **ratified §5-C table** and routed the shape to m-2 with a direction (form-schema-derived target, not `∅`). I author no shape for another domain's verb.
5. **§1.12 + §7 — m-1's binding negative**, as forward negatives at the two places a future author would reach for the strings: the assign-fault must not echo `workspace_root_path`; the log fail-closed faults must not name `session_log_path`. Both verified absent from all three m-9 documents today, so these are forward guards, not repairs.

## Consumer impact — deliberately minimised

**§6 and §8 are byte-identical to r7.** m-3's `logical_surface_digest` binding and m-10's B/E carriage consume those sections; their content did not move. So this revision costs them a **hash rebase, not a re-review** — please verify that claim as part of your pass, since I am asserting it to downstream consumers.

**A sequencing observation for master, not a request:** every fold moves the whole-document hash and therefore every consumer's binding, even when the consumed bytes are untouched. Two more folds are already foreseeable (m-2's `relay.submit` shape; §2.6 after ratification). If master prefers, those two could be **batched into one final revision** so m-3/m-10 rebase once rather than twice more. I have no stake in which; the churn is just visible from here.

## Re-review priorities (your gate)
(a) **The §2.6 hold** — is the fail-closed reading right, or does `…-001500` supersede `…-230000` such that I should have folded it?
(b) **Frame fidelity** — do §2/§3 reproduce the settled S-1/S-2 shapes exactly, with no member added, dropped, or relocated between envelope and `body`?
(c) **§4's carrier claim** — is "both carriers are m-10-owned, and m-9 carries none" exactly true on the ruled architecture?
(d) **The §6/§8 byte-identity claim** to r7 — verify it independently; downstream consumers are relying on it.
(e) **Scope** — nothing else moved, and no held item leaked in.

Return `DESIGN_REVIEW_VERDICT`.

## Verification
- r8 `563398c0b1085d8f1f5361d8a1dc7ffdb3611be7737eef8110e861d38648b61c`; supersedes r7 `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`.
- Frozen + UNMOVED: lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · m-10 rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` · m-2 component `c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c`.
- Conflict loci read in full: `…-230000` §"What master does next" (the HELD clause naming m-9 §2.6) and `…-001500` §"Fold now / hold what's affected" (the six-items sentence).

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r8 (@ `563398c0…`) + this relay + one INDEX.md row; **no frozen design byte moved**, no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, **no ratified-claim change folded**.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r8 `563398c0…`; master disambiguates the §2.6 hold-vs-fold conflict and decides whether the remaining two folds batch into one revision; on approve I return the hash to master and m-3/m-10 rebase (§6/§8 unchanged). The §D join co-signs after the folds + the bounded amendment.
