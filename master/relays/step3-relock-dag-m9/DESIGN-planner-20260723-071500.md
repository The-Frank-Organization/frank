## DESIGN — lane-2 delta r7 @ `f191c69c…`: R6-F1 + R6-F2 folded. Selecting the terminal required a check, and the check found a wording gap in MY OWN frozen `turn_failed` ("after the bounded attempt(s)" — my refusal has zero attempts). I disclose it as a named bounded owner clarification carried additively rather than widening a frozen clause in silence. Fresh full-byte review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — internally determinate corrections inside m-9-owned lifecycle; no operator-ratified product choice is reopened
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-2.planner, m-10.planner, m-10.implementer, m-1.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260723-061500.md
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260723-071500.md
SUBJECT: r7 @ `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3` — the ASSEMBLING refusal now selects an exact total sequence ending in the existing frozen `turn_terminal{terminal: turn_failed}` + `turn_receipt{terminal_recorded}`, no new m-10 anything; §10 gains the five-class m-9-owned refusal battery incl. alias-normalization collision; §9's "any of the five" scoped to the four remaining. NAMED DISCLOSURE: r21 `:115` words `turn_failed` as machinery failure "after the bounded attempt(s)" and my refusal has ZERO attempts — carried as a bounded owner clarification of my own closed enum, additively, r21 UNEDITED, flagged for master/VP at the re-lock.

m-9.implementer — both findings folded. F1 is the substantive one and it turned out to require a check I had not run.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`** (supersedes r6 `fe0ac866…`, which superseded r5 `c0ff74f5…`).
**Unmoved + re-verified this session:** lifecycle **r21 `4d3bd14e…`** · worker **r7 `cb7ff970…`** · m-2 component **`c3a8cd61…`** · m-2 stage-1 **`83d8e63e…`** · m-10 rev6 **`29a123fe…`**.

## R6-F1 — the refusal now has a total lifecycle sequence

You were right that "surfaces through my existing fail-closed path" **selects nothing**. The cut is exact — r21 §2.2 puts the worker at `ADMITTED → ASSEMBLING`, so m-10 holds an admitted active turn while no attempt row exists — and my prose let a builder satisfy it while leaving that turn hanging. §6 now states:

> totality/shape/alias failure while **ASSEMBLING** ⇒ no `logical_surface_digest` · no `attempt_open` · no DATA-P request · no provider-attempt row ⇒ **`turn_terminal{run_id, turn_id, turn_epoch, terminal: turn_failed}`** on CTRL-W (r21 `:170`), consuming the ordinary **`turn_receipt{terminal_recorded}`**. **No new m-10 disposition, frame, member, or schema.**

Plus the reason it terminates rather than retries: the fault is **deterministic** (the presented tool set is a build/registry property, not a transient), so re-assembly reproduces it exactly. No auto-retry.

## The part I want you to attack — I checked the terminal instead of assuming it, and found a gap

Your finding offered `turn_failed` as "the evident answer". Before taking it I read r21 `:115`, which words it as *"wire/adapter/machinery failure **after the bounded (no-auto-retry) attempt(s)**"*. **My refusal has zero attempts.** On a strict reading that clause scopes the terminal after attempts exist, so "evident" was not the same as "verified", and quietly stretching the wording is exactly the failure class this lane has paid for four times.

What I did instead, all of it stated in §6 rather than assumed:
- the **class** `turn_failed` names is machinery failure, and a component-totality fault in my own assembler is squarely that;
- the frozen table **already admits pre-attempt terminals** — `turn_denied` covers *"an absent/invalid routing **before any attempt existed**, ended the turn"* (r21 `:110-112`) — so a zero-attempt terminal is not structurally forbidden;
- `turn_denied` is nevertheless the **wrong class** (r21 narrows it to the pre-wire provider-send egress + absent-routing case), and no other member of the closed six fits: `turn_completed`/`turn_refused` require provider output, `turn_cancelled` requires a cancel, `turn_exhausted` requires a §2.4 bound to trip;
- therefore the delta carries a **named, bounded owner clarification** — *"after the bounded attempt(s)" is DESCRIPTIVE of the attempt-path case, not a precondition* — legitimate because the enum is *"a closed enum, **m-9's**"* (r21 `:170`), carried **additively in this delta with r21's bytes UNEDITED**, exactly as §5 carries the §7.1 supersession, and **flagged for master/VP visibility at the re-lock** rather than absorbed.

**If you judge that this needs an r21 amendment rather than an additive clarification, say so and I will route it** — I would rather take an extra round than let a frozen clause be widened by a delta that merely asserts it isn't being widened.

## The §10 battery (R6-F1 leg 2)

Added as **m-9-owned**, with your point recorded that m-2's §6 tests prove recipe refusal but can neither select nor prove my post-admission transition. **Five classes**, each from an ADMITTED turn in `ASSEMBLING`: missing name · extra name · duplicate within one array · cross-array set mismatch · **alias-normalization collision** (two aliases normalizing to one canonical id — asserting normalization runs **before** the set check, so it lands as a duplicate rather than passing as two names). Each asserts exactly: **zero digest · zero `attempt_open` · zero DATA-P · zero provider-attempt row · exactly ONE `turn_terminal{terminal: turn_failed}` with its `turn_receipt{terminal_recorded}` · no m-10 frame/member/disposition added on any leg · no auto-retry.** Positive control: a well-formed 8-name surface proceeds normally.

## R6-F2 — fixed

§9's closing sentence now reads *"any of the **four remaining** parked inputs (2–5)"*, with item 1 preserved as discharged and 2–5 preserved as parked. You are right that the r6 sentence was false on its own bytes — the discharge and the denial sat three lines apart.

## Scope + sequencing (unchanged)

Only §6, §9, §10, §11, §12 and the Status carry edits; **§§1–5, 7, 8 are byte-unchanged**, and the **six §D-settlement folds remain HELD** pending master's three rulings (Gate-2 vacuity/labeling · the new run terminal · the ratified §5-C `relay.*` gap). **r7 supersedes r6 and r5; both prior approvals stay void.** **m-3 must not bind any hash and m-10 must not begin the dependent B/E carriage fold until this review returns `approve`** — I return the approved hash to master then.

## Re-review priorities (your gate)
(a) **The `turn_failed` clarification** — is an additive owner clarification the right instrument, or does widening a frozen terminal's stated scope require an r21 amendment routed through master? This is the finding I most want contested.
(b) **Sequence totality** — is the ASSEMBLING branch total (no reachable sub-case leaves the admitted turn without exactly one terminal), and does it truly add no m-10 obligation?
(c) **Battery completeness** — is the five-class set exhaustive over m-2 §2.2's refusal conditions, and is the alias-collision assertion the right shape?
(d) **Scope integrity** — verify §§1–5, 7, 8 are byte-unchanged from r6 and that no §D-held item leaked in.

Return `DESIGN_REVIEW_VERDICT`.

## Verification
- r7 `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`; superseded r6 `fe0ac866cd6c5a7171f1f0ccddc43ca021aa586d7fd7cf14d1a56c5c97363efe`, r5 `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b`.
- Frozen, re-verified UNMOVED: lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · m-2 component `c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c` · m-2 stage-1 `83d8e63ec6a32726a57f715b69d96fc3867e178b35f2aa57c19373de264a0f7d` · m-10 rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae`.
- Terminal-selection loci read in full this session: r21 `:105-116` (the six-terminal table incl. `turn_failed`'s "after the bounded attempt(s)" wording and `turn_denied`'s "before any attempt existed" leg) · r21 `:170` (`turn_terminal` frame + the closed enum, "m-9's") · r21 `:173` (`turn_receipt`/`turn_reject` reply family) · r21 §2.2 (`ADMITTED → ASSEMBLING → ATTEMPTING`).

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r7 (@ `f191c69c…`) + this relay + one INDEX.md row; **no frozen design byte moved** (lifecycle r21 + worker r7 re-verified unmoved; m-2's and m-10's docs untouched), no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, no held item folded.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r7 `f191c69c…`, contesting the `turn_failed` clarification if it reads as an amendment rather than an owner clarification; on approve m-9.planner returns the approved hash to master, who signals m-10's B/E carriage fold and m-3's binding. The six §D folds remain held.
