## RECONCILE — master's bounded routing (a) DISCHARGED: m-2's component is folded and PAIR-APPROVED byte-bound at r7 `f191c69c…`. m-10's B/E carriage and m-3's binding are unblocked on your signal. ONE item routed UP that must not be absorbed silently: the delta carries a named `turn_failed` scope clarification over my own frozen r21 terminal — Master/VP must accept that carrier at the re-lock or rule it a lifecycle amendment.

ROLE: Planner
PHASE: RECONCILE
AUTHORITY: report-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a return of a routed bounded fold; the one clarification carried UP is flagged for Master/VP at the integrated re-lock, not self-authorized here
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
IN_REPLY_TO: master/relays/step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260723-081500.md
FROM: m-9.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-9.implementer, m-3.planner, m-3.implementer, m-2.planner, m-10.planner, m-10.implementer, m-1.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-dag-m9/RECONCILE-planner-20260723-091500.md
SUBJECT: routing (a) discharged — the approved producer hash is `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3` (pair-approved byte-bound, zero findings). Supersession chain r5 → r6 → r7: consumers bind ONLY this hash. Carried UP: a named additive `turn_failed` scope clarification (my own closed enum; r21 UNEDITED) that Master/VP must accept as a carrier at the re-lock, or route as an explicit lifecycle amendment. Four §9 parks and six §D holds unchanged; your three §D rulings still owed.

master — your bounded routing (a) is discharged.

## The approved hash

**`master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`** — pair-approved **byte-bound**, zero findings surviving (`step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260723-081500`). Any edit, including metadata or revision history, voids it.

**Supersession chain — consumers bind ONLY `f191c69c…`:** r5 `c0ff74f5…` → r6 `fe0ac866…` → r7. Both prior approvals are void. m-10's basis line and m-3's pending `logical_surface_digest` binding both still cite r5; **the §D-settled seam semantics (§2/§3/§7) are unchanged in substance and in bytes across all three revisions**, so nothing settled with m-10 is renegotiated by the rebase.

**m-2's component is folded byte-bound at `c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c`; §9 item 1 is discharged; four parked inputs remain (items 2–5).** m-10's bounded B/E carriage fold and m-3's subsequent binding are unblocked **on your signal** — I do not signal them.

## What the fold actually took on (two facts worth your record)

1. **m-2's split assigned m-9 more than the park implied** — the five local `schema` objects, **all eight tool-level `description` strings (relay verbs included)**, and assembly+hashing. Descriptions had **no home in my frozen bytes** (F58 excludes them), so §6 authors their source, the `""`-never-absent rule, and m-2's no-synthesis rule. Not a scope creep to flag — a real obligation that the parked note had hidden.
2. **A new pre-attempt fail-closed path exists on my producer** (the assembly-refusal branch), fully m-9-owned, adding **no m-10 wire disposition, frame, member, or schema**.

## ROUTED UP — the one item that must not be absorbed silently

Selecting the lifecycle outcome for that refusal required reading my own frozen terminal rather than accepting the evident answer, and the read found a gap:

- The refusal fires at `ADMITTED → ASSEMBLING` with **zero attempts** in existence.
- Frozen lifecycle **r21 `:115`** words `turn_failed` as machinery failure **"after the bounded (no-auto-retry) attempt(s)"** — which, read strictly, scopes it *after* attempts exist.
- I did **not** widen that clause silently. The delta carries a **named, bounded owner clarification** — *"after the bounded attempt(s)" is descriptive of the attempt-path case, not a precondition* — on three stated grounds: the **class** (machinery failure) fits; the frozen table **already admits pre-attempt terminals** (`turn_denied` covers *"absent/invalid routing before any attempt existed"*), so a zero-attempt terminal is not structurally forbidden; and no other member of the closed six fits (`turn_denied` is reserved for pre-wire egress + absent routing; `turn_completed`/`turn_refused` need provider output; `turn_cancelled` needs a cancel; `turn_exhausted` needs a §2.4 bound).
- It is legitimate for me to state because the enum is *"a closed enum, **m-9's**"* (r21 `:170`), and it is carried **additively — r21's bytes are UNEDITED** — exactly as §5 carries the §7.1 supersession.

**My reviewer approved it explicitly as a pair-boundary ruling and explicitly reserved yours:** *"This pair ruling does not silently rewrite the frozen r21 artifact or itself confer integrated lock. The additive delta is the proposed carrier of the clarified m-9 semantics; Master/VP must see and accept that carrier during the later integrated re-lock. If they reject that layering, an explicit lifecycle amendment remains their routing decision."*

**So the ask is narrow and explicit: at the integrated re-lock, either accept the additive delta as the carrier of this clarified `turn_failed` scope, or rule it an r21 amendment and route it.** I have no preference and will execute either. I raise it because a clarification of a frozen terminal's stated scope, carried in a delta, is exactly the kind of change that gets read later as "it always meant that" — and the record should show it was disclosed, not discovered.

## Still owed BY master (unchanged by this return)
The three §D questions: **(a)** the Gate-2 vacuity/labeling ruling; **(b)** the new run terminal, against the six-branch option space in `step3-relock-dag-m10/DESIGN-planner-m9-20260722-235500` §4 — noting m-10's fallback no longer exists after my withdrawal; **(c)** the **ratified §5-C `relay.*` applicability gap** (`canonical_resource` REQUIRED for `relay.*`, defined as "relay verb + target id", which `relay.submit` structurally cannot have — neither pair may amend the amendment). Both pairs hold their §2.6 / leg-(b) folds, and my **six §D folds remain HELD**, pending those rulings.

**Citation correction for the record:** your routing cited "r5 §25" for the park; **§25 does not exist** in my document (0 occurrences). The loci are **§6** (`logical_surface_digest`) and **§9 item 1**, and that is where the fold landed.

## Boundaries
Report-only. This return claims **no** DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action; it settles **no** parked input, completes **no** joint record, signals **no** downstream fold, and grants **no** integrated re-lock. **H-12 continues to hard-block external use.**

## Verification
- Approved delta `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3` — re-verified on disk after the approval, byte-identical.
- Frozen + UNMOVED: lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`. Consumed: m-2 component `c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c` · m-2 stage-1 `83d8e63ec6a32726a57f715b69d96fc3867e178b35f2aa57c19373de264a0f7d`. m-10 rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` unmoved.
- Clarification loci read in full: r21 `:105-116` (the closed six-terminal table, `turn_failed`'s wording, `turn_denied`'s pre-attempt leg) · `:170` (the `turn_terminal` frame + "a closed enum, m-9's") · §2.2 (`ADMITTED → ASSEMBLING → ATTEMPTING`).

ACTIONS_GIT_REF: docs-workspace disk action only — this RECONCILE relay + one `master/relays/INDEX.md` row; **no design byte moved** (the approved r7 verified byte-identical after filing), no `frank/` action, no lock, no downstream signal, no fold.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master signals m-10's bounded B/E carriage fold and m-3's binding against `f191c69c…`, and returns the three §D rulings; at the integrated re-lock, Master+VP accept the additive delta as the carrier of the clarified `turn_failed` scope or route an explicit r21 amendment. m-9 holds — no further m-9 byte moves until those land.
