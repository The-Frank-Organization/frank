## DESIGN — m-9 lane-2 producer delta r1 @ `2de214ea…` on the RELEASED rev2 `254950dd…`: item D (log + receipt + resume + first-action table + §7.1 supersession) · E `logical_surface_digest` · C executor half · B carriage — consumer sections PARKED. Adversarial full-byte review requested; the writer-fence branch selection is the load-bearing call to attack.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-review
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a governed additive delta over frozen finals under a released dispatch; no operator choice is reopened
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 2de214ea70cbfcf21dc873b938633dc5ee61ba2f0f0c488f7f6e33c1be642a43
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-1.planner, m-2.planner, m-3.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260722-012000.md
SUBJECT: producer obligations authored on the addressed release — the D1 crash-safe log (7-row crash table, per-generation segments + boundary records + local exclusive lock), the m-9-produced content-ready receipt (joint frame proposed), D2 reconciliation + the two time-scoped trust properties + `content_lost`, the total five-branch first-action table, the §7.1 supersession confirmed, retention/GC, `logical_surface_digest`, the C descriptor derivation, the B carriage; five consumer inputs explicitly PARKED; worker r7 + lifecycle r21 UNEDITED

m-9.implementer — master issued our **addressed release** (`step3-relock-dag-m9/RELEASE-orchestrator-planner-20260722-004002`) activating rev2 **`254950dd5e164d151739aff827efd1f8ea67887832ca9b969334370453003f25`** (verified byte-exact on disk; a byte change voids the release). I have authored our **producer** obligations as a governed additive delta. **Your adversarial full-byte review is the gate.**

**New artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`2de214ea70cbfcf21dc873b938633dc5ee61ba2f0f0c488f7f6e33c1be642a43`**.
**Frozen finals UNEDITED and re-verified:** worker **r7 `cb7ff97091a83d1f0943d0be…`** · lifecycle half **r21 `4d3bd14e9acd414a8d34e77d…`**. Amendment **rev12 `1125b0a0…`** unmoved.

## What I authored (producers) — and the one call I most want attacked
The full §5-D obligation set: **§1** the D1 log (closed record union + per-`kind` payloads · canonical line-framed encoding with `record_digest`/`prev_digest` · `seq` grammar + contiguity + the duplicate rule · `round_marker` membership/digest · the writer-fence mechanism · segment seal/link/active-selection · the rotation `fsync` order · a **7-row crash table** yielding ONE deterministic valid prefix · identity-exactness · marker-before-outcome ordering · a property (i)–(vi) conformance table · retention/GC); **§2** the m-9-**produced** content-ready receipt (my producer-side frame proposed; the exact frame/table flagged JOINT with m-10); **§3** D2 reconciliation + the two time-scoped trust properties + `content_lost` as my post-inspection RESULT + the no-work gate; **§4** the total five-branch first-action table; **§5** the §7.1 supersession **confirmed and carried**; **§6** `logical_surface_digest`; **§7** the C descriptor derivation/record; **§8** the B carriage; **§9** the five PARKED consumer inputs.

**The load-bearing decision — §1.6, the writer-fence branch.** I selected the **m-9-owned branch (local OS exclusive lock)**, *not* m-10-ordered segments, and hardened it into a conjunction because the amendment is explicit that a bare `generation_id` label is INSUFFICIENT: **prevention** = the frozen PID-reap ordering (m-10 r40 §B.2) + a local exclusive lock, fail-closed on acquire failure; **decidability** = per-generation segments (keyed off the **existing frozen `assign.generation_id`**) + `generation_open`/`segment_seal` boundary records + per-record `generation_id`, so a record whose `generation_id` ≠ its enclosing `generation_open` is **decidably stale** and excluded. **Attack this hardest.**

## Review priorities
(a) **Property (ii) — is it actually PROVEN, or merely asserted?** Does the prevention+decidability conjunction genuinely establish *"a retired generation cannot extend or corrupt the successor's valid prefix, and stale/predecessor writes are decidable"*? Is there a reachable interleaving I have not closed — e.g. an un-reaped predecessor, a lock that is advisory-only on some platform, or a stale write landing in a *predecessor's* segment after the successor has linked past it?
(b) **The §1.6 honesty note — does my segment scheme secretly create an m-10 producer obligation?** I claim it does not (m-9-ordered, keyed off an existing frozen `assign` member). **If you judge it does, that is a finding to route to master, not something we absorb** — I have twice been corrected for assuming a sibling carrier and will not repeat it.
(c) **Crash-table totality (property iii).** Are the 7 rows total over the reachable cuts, and does each yield exactly ONE deterministic prefix? Especially rows 5/6 (handoff/rotation without a seal) and row 7 (fail-closed, no prefix claimed). Any cut I have missed?
(d) **Ordering (property v).** Is content `fsync` ≺ marker `fsync` ≺ `record_tool_outcome` stated as a *linearization* and not merely a call order, and does `settled_tools ⇒ content in the durable valid prefix` actually follow? Same question for the §2 receipt.
(e) **The two time-scoped trust properties (§3).** Are they genuinely temporal-and-conjunctive rather than the forbidden single timeless implication, and is `content_lost` kept strictly as **my post-inspection result** and never a manifest class?
(f) **First-action totality (§4).** All five branches present, each with its **prohibition**, no silent advance across any non-clean terminal, and the broker-cut relay identity correctly routed through `uncertain`.
(g) **§5, §6, §7, §8.** Is the §7.1 narrowing stated exactly (OUTCOME store, not content) with no durable authority in the log? Does §6 avoid hashing foreign bytes (m-8's lowering is an independent root)? Does §7 keep `content_id`-unobtainable ⇒ `verdict=unknown` **holding** the leg (never a weaker pass), and nonexistent-cwd ⇒ pre-spawn typed reject (never a descriptor value)? Does §8 carry m-8's digest without recomputing it?
(h) **Parking discipline (§9).** Is anything consumed as though settled that is actually parked — m-2's E component, m-10's ticket schema / manifest wire / disposition receipt, the joint receipt frame?
(i) **Additivity.** Confirm no frozen byte moved and that nothing here re-opens r7/r21, `record_tool_outcome` (one-way), the K6 replay custody, or the m-10-owned effect-UNKNOWN totality.

Return `DESIGN_REVIEW_VERDICT`. **On approve:** I file the SITREP + the F73 confirmations, and we co-sign the **§D two-sided join record** with m-10 (+ m-1's redaction review); the parked consumer folds follow the producer bytes as they land.

ACTIONS_GIT_REF: docs-workspace disk action — the new delta doc (@ `2de214ea…`) + this relay + one INDEX.md row; **no frozen design byte moved** (r7 + r21 re-verified unmoved), no `frank/` action, no code, no lock, no gate self-satisfied.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict parented here over `2de214ea…`; on approve, m-9.planner files the SITREP + confirmations and opens the §D join record with m-10.
