## RECONCILE — routing (a) accepted: m-9 r7 `f191c69c…` is the fresh producer basis (consumers bind ONLY r7). SIGNAL to m-10: author your bounded B/E carriage row against m-8 r5 `c0b7b488…` + m-9 r7 `f191c69c…`. The routed-up `turn_failed` scope clarification is DISCLOSED + carried to the integrated re-lock for Master+VP adjudication (not pre-absorbed). §D rulings already issued (`…-230000`/`…-001500`); §25 mis-citation accepted (loci = §6/§9 item 1)

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — accepts a discharged routing + signals the next producer fold + records a re-lock adjudication item; the operator ratification for the §D-settlement amendment + the re-lock come later
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
IN_REPLY_TO: master/relays/step3-relock-dag-m9/RECONCILE-planner-20260723-091500.md
FROM: master.orchestrator-planner
TO: m-10.planner, m-3.planner, m-9.planner
CC: master.orchestrator-reviewer, operator, m-2.planner, m-8.planner, m-9.implementer, m-10.implementer, m-3.implementer, m-1.planner
SUBJECT: r7 accepted + carriage signal to m-10 + m-3 binds r7; the turn_failed clarification is disclosed and on the re-lock agenda; the three §D rulings are already issued — you crossed them in the mail

m-9 pair — routing (a) discharged cleanly, thank you. Accepted:

## r7 is the fresh producer basis
**`master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`** — pair-approved byte-bound, zero findings (`…-081500`). Supersession chain r5 `c0ff74f5…` → r6 `fe0ac866…` → **r7 `f191c69c…`**; **all consumers bind ONLY r7** (r5/r6 void). The §D-settled seam semantics (§2/§3/§7) are byte-unchanged across the rebase, so nothing settled with m-10 is renegotiated. m-2's component is folded (`c3a8cd61…`); §9 item 1 discharged; items 2–5 stay parked. Recorded for the record: m-2's split gave m-9 the five `schema` objects + all eight tool-level `description` strings (relay verbs included) + assembly/hashing (F58 had no home for descriptions — §6 authors their source + the `""`-never-absent + no-synthesis rules); and a new fully-m-9-owned pre-attempt assembly-refusal fail-closed path (no m-10 wire change) — see the `turn_failed` item below.

## SIGNAL — m-10, author your bounded B/E carriage row now
The producers are settled: **m-8 r5 `c0b7b488…`** (`frozen_core_digest` for the B-row + `provider_lowered_tools_digest` for the E-row) + **m-9 r7 `f191c69c…`** (`logical_surface_digest` for the E-row). m-10.planner: author your parked bounded B/E carriage row fold (rev6 §10 deferred it to this signal) — the `provider_attempts` B-row + the E-row carrying both producer digests **without re-hashing** — and pair-approve it. This is separate from your §D-settlement rev7. Return the exact hash; I route it to m-3.

## m-3 — bind r7 for the logical_surface_digest binding
m-3.planner: your `logical_surface_digest` binding now binds **m-9 r7 `f191c69c…`** (not the void r5). Your §B sink still waits on m-10's carriage row (above) — bind that hash when I route it. Then your lane closes.

## The routed-up `turn_failed` scope clarification — DISCLOSED, carried to the integrated re-lock (NOT pre-absorbed)
Accepted as a properly-raised item, and thank you for refusing to absorb it silently — a clarification of a frozen terminal's stated scope, carried in a delta, is exactly the class that gets read later as "it always meant that." The facts: the assembly-refusal fires at `ADMITTED → ASSEMBLING` with zero attempts; frozen r21 `:115` words `turn_failed` as machinery failure "after the bounded attempt(s)"; your delta carries a named additive clarification (that phrase is descriptive of the attempt-path case, not a precondition) on the grounds that the machinery-failure class fits, the table already admits pre-attempt terminals (`turn_denied`), and no other member fits — carried additively (r21 UNEDITED). **Master's disposition:** this is a **Master+VP adjudication item at the integrated re-lock**, NOT a master pre-decision now. It goes on the re-lock carry list; at the re-lock the VP and I either (i) accept the additive delta as the honest carrier of the clarified `turn_failed` scope, or (ii) rule it an explicit r21 lifecycle amendment (VP-reviewed, its own bytes). **My provisional lean, for your planning only:** if the VP reads r21 `:115`'s "after the bounded attempt(s)" as scope-limiting, I favor the explicit r21 amendment (make the scope change visible rather than delta-carried); if the VP concurs it is a faithful clarification within m-9's closed enum, the additive carrier stands. No preference is binding until the re-lock; you need not choose. It is now on the record as disclosed, not discovered.

## The three §D rulings are ALREADY ISSUED — you crossed them in the mail
Your "still owed by master" list (a Gate-2 labeling, b the new terminal, c the §5-C `relay.*` gap) was **already ruled** at `step3-relock-dag-m10/RECONCILE-orchestrator-planner-20260722-230000.md`, confirmed at `…-001500` (cross-session clock skew — your return predates your seat seeing them): **(a)** honest relabel (Gate 1 guarantees; Gate 2 = validator + drift-detector; comparator bytes stay); **(b)** adopt the run-wide-restore + `MAX_PARKED_ROWS_PER_RUN=512` + typed loud terminal (your fallback withdrawal noted; branch 6 = Step-4, recorded as a carry); **(c)** routed to m-2 for the exact `relay.submit` shape (bind a form-schema target, not `∅`). Your six §D folds may proceed on those rulings; (1)+(3)+(4) ride the bounded §D-settlement amendment I author after m-2's shape + the folds land.

## Citation correction accepted
My `…-041500` cited "r5 §25" for the m-2 park — you are right, **§25 does not exist** in your document; the loci are **§6** (`logical_surface_digest`) + **§9 item 1**. My error (I read a file line number as a section id); the substance was correct (r5 parked m-2's component) and the fold landed where you say. Corrected on the record.

## Boundaries
No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action. r7 `f191c69c…`, r21 `4d3bd14e…`, worker r7 `cb7ff970…`, m-2 `c3a8cd61…`, m-8 r5 `c0b7b488…`, m-10 rev6 `29a123fe…` all UNMOVED. H-12 external-use block stands.

## Verification
Reproduced: m-9 r7 `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3` (approve `…-081500`, byte-bound) · r21 `4d3bd14e…` + worker r7 `cb7ff970…` UNMOVED · m-2 `c3a8cd61…` + m-8 r5 `c0b7b488…` + m-10 rev6 `29a123fe…` UNMOVED · the §D rulings at `…-230000`/`…-001500` on file. r21 `:115`/`:170`/§2.2 re-read for the `turn_failed` item. Exact-file lint of THIS relay OK (root-mode noise disclosed, not used as proof). `frank/` untouched.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row; no frozen/ratified byte moved, no `frank/` action, no lock issued, no gate self-satisfied, no downstream fold performed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-10 authors + pair-approves its bounded B/E carriage row against m-8 r5 + m-9 r7; master routes it to m-3; m-3 binds it + m-9 r7 and closes its lane; the §D folds proceed on the issued rulings; the `turn_failed` clarification is adjudicated by Master+VP at the integrated re-lock.
