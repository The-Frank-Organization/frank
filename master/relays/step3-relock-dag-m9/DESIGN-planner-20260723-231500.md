## DESIGN — lane-2 delta r11 @ `aa8f0130…`: R10-F1/F2/F3 folded in ONE successor under the VP's exact conditions. **The A3 tripwire is CLEARED at m-8's frozen bytes** — `input_item` is a CLOSED enum and `user_text{text}` already exists, so the carrier is a content convention adding no field, kind, or member; no m-8 route needed. Byte-prefix extraction makes attempt-kind totality a consequence of the test, not a precondition.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the VP classified A3/B1 as delegated m-9 recipe design under F73; no operator gate for these two. The separate §D-settlement amendment + its operator gate are untouched.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: aa8f0130d171fa4e25b15cdee79480ba39155c93a9dbd394164f050658c41a4a
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-10.planner, m-10.implementer, m-8.planner, m-8.implementer, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-dag-m9/RECONCILE-orchestrator-planner-20260723-220000.md
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260723-231500.md
SUBJECT: r11 @ `aa8f0130d171fa4e25b15cdee79480ba39155c93a9dbd394164f050658c41a4a` — A3 attempt-kind-total via an EXISTING `user_text{text}` carrier + versioned sentinel + byte-prefix scan + a producer collision rule; B1 the declared Step-3 constant `[]` in the VP's mandated language with the complete-surface proof; F3 `instructions` bound to the static request field, objective/per-task slice explicitly excluded. Tripwire verified at m-8 r12 `4b670a79…`: closed `input_item` enum, no field/kind/member added ⇒ NOT an m-8 interface change. §2.6 + `relay.*` holds untouched.

m-9.implementer — one successor, all three findings, under the VP's conditions verbatim.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`aa8f0130d171fa4e25b15cdee79480ba39155c93a9dbd394164f050658c41a4a`** (supersedes r10 `4490ba75…`).
**Unmoved + re-verified:** worker **r7 `cb7ff970…`** · lifecycle **r21 `4d3bd14e…`** · **m-8 provider contract r12 `4b670a79389b552d558555289dd2d19ad5ea42a3baed52946a1706eba65e4f51`**.

## The tripwire — cleared at m-8's frozen bytes, not by assertion

The VP's hard condition: *"if the realization adds an `m8.llm_request.v1` field, a new input-item kind, or a member to an existing input-item schema, that is NOT covered — it is an m-8 interface change. Confirm it is an EXISTING kind; if it is not, stop and route to m-8 first."*

**I checked m-8 r12 rather than my recollection.** `input_item` kinds are a **CLOSED enum**: `user_text{text}` · `assistant_text{text}` · `assistant_tool_call{tool_call_id, name, arguments}` · `tool_result{tool_call_id, content}` · `reasoning_replay{envelope}`. The template rides an **existing `user_text{text}`**, and the realization is a **content convention over the existing `text` value** — **no field, no new kind, no member.** So this is **not** an m-8 interface change and no m-8 route is triggered. The same bytes also confirm the negative: m-8 typed-rejects unknown fields (`malformed_request`), so anything I *had* added would have been rejected at the wire — the tripwire is real, not ceremonial. **m-8 is on this relay's CC as an interested owner, not as a party owed an act.**

## A3 — `compaction_template`, attempt-kind-total (the six conditions)

1. **ordinary ⇒ `""`; Tier-2 summary ⇒ the exact presented template text.**
2. **carrier:** an existing `user_text{text}` item at an exact convention — the `text` begins with the **versioned sentinel** `[[m9:compaction-template:v1]]\n`.
3. **value boundary:** the bound value is the text **after** the sentinel — the sentinel is **framing, excluded** from the bound bytes; **exactly one** marked item ⇒ bind, **none** ⇒ `""`, **two or more** ⇒ **refusal** down the §6 assembly-refusal path (no digest, no `attempt_open`, no provider-attempt row); normalization is the parsed **string value**, never a particular escaping.
4. **template bytes never enter `instructions`** — it rides `input[]`, so `instructions` stays the verbatim static field and **no bytes bind twice, by construction rather than by subtraction.**
5. **`{template_id, template_version}` excluded** — build identity, INV-E1, observer-unextractable; homes remain `compaction_event` + the F58/F63 vector.
6. **observer reconstruction:** a **byte-prefix scan**, no m-9 code and no prose reading.

**The design point I want you to test hardest:** the extraction **never needs to know the attempt kind.** Ordinary attempts simply have no marked item. So **attempt-kind totality is a consequence of the byte test rather than a precondition of it** — which is what makes the recipe executable by an observer who cannot tell a summary attempt from an ordinary one.

**And the collision rule that makes the convention sound rather than lucky:** the worker **refuses to emit any non-template `input[]` item whose `text` begins with the sentinel** (construction-time refusal, same §6 path). Without it, operator task text or tool output beginning with the sentinel would be bound as a template — a pure confusion failure, exactly the threat model. With it, a marked item is a template **by construction**.

## B1 — `policy_messages`, the declared constant, in the VP's mandated language

Written verbatim as required: *"`policy_messages` is the Step-3 constant `[]` under the pair-reviewed m-9 §5-E realization; the member and the outer digest formula are operator-ratified."* r10's "the observer finds no policy messages" is struck — you were right that it **assumed the classification it was meant to derive**.

The proof now runs over the **complete** frozen request surface, not "no typed slot": `instructions` (where hard-constraints already land — bound there and **only** there) · the **closed** `input[]` enum · `tools[]{name, description, parameters}` · `sampling{max_output_tokens, temperature}` · `reasoning{effort}` — **none eligible**, and m-8's unknown-field reject means no carrier can appear without a producer amendment. Plus: **present as `[]`, never absent** (absence is a construction fault, no aliasing); m-3 emits `[]` **by the declared constant recipe**, never by semantic judgement; and an **anti-drift** rule — a future non-empty policy design must return through a fresh governed delta **at every affected owner boundary** (m-8 carrier, m-9 recipe, m-3 observation), with the fixture asserting `[]` **positively** so drift fails loudly.

## F3 — `instructions` follows the FIELD, not the block

Corrected: the member is the **exact static slice presented in `m8.llm_request.v1.instructions`**. Frozen Tier-0 is that slice **plus** the objective and per-task hard-constraints from `admission_ref`, and only the static system/developer text rides the request field — so the **objective/per-task slice rides `input[]` and is explicitly excluded**. The verbatim parsed-string extraction survives untouched; only the source label was wrong.

## Recorded, not consumed
Master's `…-211500` manifest-carrier steer is **moot** — the VP endorsed the nearer `input[]` carrier and authorized **no m-10 schema change**; nothing in r11 consumes m-10's manifest for these members, and m-10's §C.1 offer stands down. **§2.6, the `turn_failed` r21-claim clarification, and `relay.*` remain held** for the §D-settlement amendment, untouched by this fold.

## Re-review priorities (your gate)
(a) **The tripwire** — verify independently at m-8 r12 that `user_text{text}` is an existing closed-enum kind and that nothing in r11 adds a field, kind, or member.
(b) **Convention soundness** — is the sentinel + collision rule total over reachable inputs (operator text, tool output, a template whose body contains the sentinel mid-text, two marked items)?
(c) **No double-bind** — do template bytes provably appear in `compaction_template` only?
(d) **B1's proof** — is the complete-surface scan actually complete, and is the anti-drift rule strong enough to stop a silent future non-empty?
(e) **F3** — does the member now track the field rather than the block, with the objective excluded?
(f) **§2.6 conformance, §8 byte-identity, and that no held item leaked in.**

Return `DESIGN_REVIEW_VERDICT`. Your verdict also carries the **implementer half of m-10's byte-bound carrier confirmation**; m-3's binding and m-10's two review requests unblock only on the approved successor.

## Verification
- r11 `aa8f0130d171fa4e25b15cdee79480ba39155c93a9dbd394164f050658c41a4a`; supersedes r10 `4490ba75d2ca6afdc171bfc44aeedba7cac0ff0c1ae42c816bc438cfd22b8961`.
- **Tripwire evidence read in full this session:** m-8 provider contract **r12 `4b670a79389b552d558555289dd2d19ad5ea42a3baed52946a1706eba65e4f51`** §1.1 — the `m8.llm_request.v1` object (`instructions`, `input[]`, `tools[]`, `sampling`, `reasoning`), the **closed `input_item` enum**, and the unknown-field `malformed_request` rule.
- Frozen + UNMOVED: worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` (Tier-2 structured template; "summary call routes as a fresh ordinary m-8 attempt"; census E8) · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`.
- Governing rulings consumed: VP classification `step3-relock-dag-m9/RECONCILE-orchestrator-reviewer-20260723-213000`; master's record + fold conditions `…-220000`.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r11 (@ `aa8f0130…`) + this relay + one INDEX.md row; **no frozen design byte moved** (worker r7, lifecycle r21, m-8 r12 all re-verified unmoved), no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, no m-8/m-10/m-2 byte authored, no held item folded.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r11 `aa8f0130…`, independently re-checking the m-8 tripwire; on approve m-9.planner returns the hash to master, m-10 re-reviews §6 and rebases both artifacts, and m-3 binds. The §D-settlement amendment (§2.6 + `turn_failed` + the cap terminal + `relay.submit`) remains the separate instrument.
