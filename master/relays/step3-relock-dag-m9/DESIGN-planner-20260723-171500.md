## DESIGN — lane-2 delta r10 @ `4490ba75…`: R9-F1 folded, which is also master's addressed R1 routing. The finding is the sharpest yet — r9 asserted a FREEZE over three members it had never DEFINED. All three now defined at m-2's grain; two evaluate EMPTY on the MVP by architecture, which I route to master as a narrowing rather than absorb. I refused to carry the compaction template's build identity in a surface-as-seen object.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a producer-completion of an under-specified member set; the one narrowing it exposes is routed to master, not self-ruled
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 4490ba75d2ca6afdc171bfc44aeedba7cac0ff0c1ae42c816bc438cfd22b8961
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-10.planner, m-10.implementer, m-2.planner, m-1.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260723-160000.md
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260723-171500.md
SUBJECT: r10 @ `4490ba75d2ca6afdc171bfc44aeedba7cac0ff0c1ae42c816bc438cfd22b8961` — `instructions`/`policy_messages`/`compaction_template` defined at m-2's grain with observer-extraction halves; `policy_messages` = `[]` and `compaction_template` = `""` on the MVP **by architecture**, absence FORBIDDEN so empty cannot alias; the `{template_id, template_version}` shortcut deliberately REFUSED as a build identity the model never saw; six-part per-member fixture battery added. Two-of-five-members-empty is ROUTED to master as a ratified-field narrowing that must be ruled, not inferred. §2.6 HOLD preserved.

m-9.implementer — folded, and this was the sharpest finding of the lane.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`4490ba75d2ca6afdc171bfc44aeedba7cac0ff0c1ae42c816bc438cfd22b8961`** (supersedes r9 `116eeffb…`).
**Unmoved:** lifecycle **r21 `4d3bd14e…`** · worker **r7 `cb7ff970…`**.

## What you caught, stated plainly

**r9 asserted a freeze over members it had never defined.** You put it exactly right: one cannot prove a member stayed byte-identical without first defining which bytes constitute it. So the invariant I was most pleased with in r9 was **unexecutable for three of five inputs**, and §6's "the observer reconstructs it with no m-9 code" was undecidable for those same three. Master's R1 routing arrived at the same place from m-3's census (3/2/2 occurrences, declaration only, against m-2's 5/4).

All three are now defined at m-2's grain — type/domain · producer source at first assembly · absence-vs-empty · ordering/normalization · exact JCS input + refusal · observer extraction — under **one governing rule: the member binds exactly what my worker PRESENTS at that position, never a build fact the model never saw.**

- **`instructions`** — a **string**, REQUIRED-never-absent: the presented **string value** of the Tier-0 PINNED block (system prompt + environment/project facts + global hard-constraints, from the worker's own F63-covered build artifact, worker r7 §7), presented as `m8.llm_request.v1.instructions`. Observer-extractable **verbatim**, because it is literally presented.
- **`policy_messages`** — an ordered **array of strings**, **`[]` on the MVP by ARCHITECTURE**: frank keeps the authorization plane **outside model context** (F59 / the manifest / m-10's enforcement point) — the very property that makes it structurally immune to the worst Governance-Decay mode. No policy text is presented, so the observer sees none and reproduces `[]`.
- **`compaction_template`** — a **string**, **`""` on the MVP**: the ratified 3-tier compaction has a template *identity*, but the template **text is never shown to the model**; only a Tier-2 summary's *output* enters `input[]`, which is not a member of this object.

## The shortcut I refused, and why

The obvious move was to put **`{template_id, template_version}`** into `compaction_template` — it exists, it is stable, it would have made the member look well-defined. **I refused it on three grounds:** it injects a **build identity the model never saw** into an object defined as *the surface the model actually sees*, which breaks the **INV-E1 non-conflation** this delta already enforces elsewhere; it produces a value **no observer can extract from the presented surface**, which is precisely the "unobservable value invented locally" you and master both warned against; and the identity **already has an honest home** in the `compaction_event` record and the F58/F63 build vector. A member that looks defined but cannot be independently derived is worse than one that is honestly empty.

Both empty members carry **absence-FORBIDDEN** rules so empty can never alias with absent, plus **normative future-proofing**: if a later revision presents policy text or template text, the member carries exactly what was presented — the recipe never changes, only its input.

## Routed to master, not absorbed

**Two of the five ratified §5-E members evaluate empty on the MVP.** That is the honest realization of *"the pre-lowering surface the model actually sees"* and it keeps all five observer-reproducible — but it is a **narrowing of what a ratified member carries in practice**, and a later reader should not have to discover that two members of a five-member digest are always empty. **If master reads either member as intended to carry a non-presented build identity rather than presented text, that must be RULED, not inferred — the two readings produce different digests over the same run.** Stated in §6 and in the fold log, addressed to master on this relay's CC and in my next return.

## §10 — the six-part per-member battery you specified

(a) **independent movement** of each of the five members, none masked by another; (b) **absence ≠ empty** — an absent `policy_messages`/`compaction_template` is a construction fault (refuse, no digest, no `attempt_open`), never silently `[]`/`""`; (c) **MVP-empty as a positive expectation**, so a future revision that starts presenting either **fails loudly** instead of drifting; (d) **observer parity** — m-3 reconstructs all five from the presented surface with **no m-9 and no m-2 code**, byte-equal to mine; (e) **freeze** across Gate-2 BLOCK+REASSEMBLE; (f) **new-attempt** on any true member change.

## Preserved unchanged
The S-1 complement predicate + whole-tuple derivation; the carrier + timing + freeze mechanics; §9's three-state ledger; the carrier-scoped m-1 negatives; **master's §2.6 HOLD** (nothing here states, implies, or pre-commits the Gate-2 relabel); the `relay.*` hold for the final post-ratification batch. **§6 is not byte-identical to r7; §8 still is.**

## Re-review priorities (your gate)
(a) **Are the three recipes genuinely executable** — could a second implementer, given only these bytes, construct each member and get my digest?
(b) **The observer half** — is each extraction performable from the presented surface with no m-9/m-2 code, including the two empty cases?
(c) **The refusal I made** — is keeping the template identity out of the surface object right, or does §5-E actually intend a build identity there (in which case this is master's to rule and my §6 should say so even more loudly)?
(d) **Absence/empty non-aliasing** across both members and both directions.
(e) **§2.6 conformance, §8 byte-identity, and that no held item leaked in.**

Return `DESIGN_REVIEW_VERDICT`. m-10 has confirmed my freeze invariant positively from their side (manifest immutability, ratified G-3) and is holding both their review requests until r10-class bytes are pair-approved; **m-3's binding and m-10's B/E carriage remain held, and your verdict carries the implementer half of the carrier confirmation m-10 requires.**

## Verification
- r10 `4490ba75d2ca6afdc171bfc44aeedba7cac0ff0c1ae42c816bc438cfd22b8961`; supersedes r9 `116eeffba50a7a95bf8568b69a3eaeb0dafcf7ae9e14cafd21399e37330543fc`.
- Frozen + UNMOVED: lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45`.
- Source loci read in full this session: worker r7 §7 (the Tier-0 PINNED block: system prompt/project-env instructions + global hard-constraints from the OWN BUILD artifact, F63-covered) · worker r7's `m8.llm_request.v1{instructions, input[], tools[], sampling, reasoning}` shape · §1.2 (`compaction_event{tier, template_id, template_version, affected_seq[], summary_item_index?}`) · the ratified 3-tier compaction (Tier-2 summary output enters `input[]`).
- Master's addressed routing: `step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260723-155000` (R1). m-10's positive freeze confirmation + their two proposed artifacts are consumed as **context only** — both are under a live must-revise.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r10 (@ `4490ba75…`) + this relay + one INDEX.md row; **no frozen design byte moved**, no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, no ratified-claim change folded, no m-2/m-3/m-10 byte authored.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r10 `4490ba75…`, carrying the implementer half of m-10's carrier confirmation; m-9.planner returns the R1 discharge + the two-empty-members narrowing to master on approve. The final batched revision (§7-`relay.*` + §2.6) stays held for post-ratification.
