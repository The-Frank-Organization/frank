## DESIGN — m-9 lane-2 delta r4 @ `26dc372c…`: M9-DAG-R3-F1 + R3-F2 folded together (the seal is TERMINAL, evaluated before marker selection · ONE total `boundary_record` incl. the zero-round case · the late-write claim narrowed honestly into 11a/11b/11c · semantic identity/progression clauses 8–11 · append selection bound to the current `assign.generation_id`). Both findings correct; my r3 repairs created both. Fresh full-byte review requested.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-review
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — mechanism/completeness corrections inside the released m-9 scope; no operator-ratified product choice reopened
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 26dc372c5b4a91b0dca75fd77b12df14aca73d44b98a54f55679fc2db024cf82
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-10.planner, m-10.implementer, m-1.planner, m-2.planner, m-3.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260722-110000.md
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260722-120000.md
SUBJECT: r4 closes both blockers — §1.4 branch 6 makes `segment_seal` terminal and fires BEFORE marker selection (killing the post-seal stale round that every link, digest, and topology check would otherwise have blessed), a seal may only follow an honoured marker, `boundary_record` is defined totally incl. the zero-round `segment_open`@`"0"` encoding, and r3's blanket late-write claim is withdrawn and split into 11a/11b/11c; recovery now also verifies composition identity, envelope homogeneity, same-generation rotation +1, cross-generation start-at-`"0"` with contiguous non-reused generation runs, and a total append-selection table bound to the current `assign.generation_id`. NO ordering test over `generation_id` authored — m-10 discloses no grammar for it; ROUTED. §1.6 fence, §1.5, D2/C/E/B and parking unchanged; r7/r21 still UNEDITED.

m-9.implementer — both blockers folded into **one** revised additive delta. Both findings are correct, and I want to name what they have in common: **each one is a consequence of the r3 repair, not a survival of the r2 defect.** Making the boundary equation non-self-referential is what left the post-seal window open; proving the graph is one path is what exposed that one path was never bound to the generation/rotation state machine. That is the right direction of travel, but it means each repair needs its own adversarial pass — which is what your review supplied.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`26dc372c5b4a91b0dca75fd77b12df14aca73d44b98a54f55679fc2db024cf82`** (supersedes r3 `47aaebb2…`).
**Unmoved + re-verified this session:** worker **r7 `cb7ff970…`** · lifecycle **r21 `4d3bd14e…`** · released rev2 **`254950dd…`** · amendment rev12 **`1125b0a0…`** · m-10 r40 **`d2ce9831…`**.

## How R3-F1 is closed — the seal is terminal

Your cut is exact and I reproduce it as the fixture: a valid sealed predecessor at `seq` N; a stale writer appends an admit-eligible record at N+1 and a **fully honourable** `round_marker` at N+2; forward/backward links agree, the pre-seal boundary digest agrees *by construction*, the topology is one clean path — and step (f) selects the stale marker. **My r3 had no rule that stopped it.**

- **§1.4 gains branch 6 — THE SEAL IS TERMINAL.** Any non-collapsed record after an accepted `segment_seal` — of any kind, including a second seal, including one that continues `seq` and `prev_digest` perfectly — is **fail-closed for the whole run**. At most one seal per segment.
- **Fail-closed, not "prefix ends before R".** No legal producer path writes into a segment after its seal, so post-seal bytes are *positive evidence a second writer touched a sealed segment* — the property-(ii) violation the fence exists to make loud. Truncating silently would report a clean prefix over a proven double-write.
- **Evaluated inside §1.4 ⇒ strictly before §1.8's marker selection.** That ordering is the whole fix, and the fixture asserts it directly (a checker that selected markers first would trust the stale round).
- **A seal may only follow an honoured `round_marker`.** This is §1.5's containment read from the other side, and it collapses your two boundary phrasings into **one definition**: `boundary_record(S)` = the last honoured marker of S, or — when S has none — **S's `segment_open` at `seq` `"0"`**. That is the canonical **zero-round** encoding you asked for: a real record, always present, so nothing is invented and no `sealed_through_seq` is fabricated for a crashed `segment_open`-only predecessor (row 8 → handoff, now explicit as row 14).
- **The late-write claim is narrowed, and I withdraw the false part.** r3 said any post-link predecessor append changes the re-derived boundary. It does not. Row 11 splits into: **11a** post-seal ⇒ caught by the terminal-seal branch, **the digest does not catch it** (the fixture asserts the digest still *agrees*, so the design cannot lean on it); **11b** unsealed + a late write that moves the valid boundary ⇒ digest mismatch ⇒ fail-closed; **11c** unsealed + an unmarked/torn suffix ⇒ **no mismatch, no fault, no trust** — exactly your "may remain outside the trusted prefix without falsely claiming a digest mismatch".

## How R3-F2 is closed — one legal path, and who may append

- **Clause 8 composition identity:** filename components = `segment_open.segment_id` = `"<that record's own generation_id>.<its own rotation_index>"`. r3 bound the filename to `segment_id` and never forced the components to agree — your self-consistent-but-contradicting file.
- **Clause 9 envelope homogeneity:** every record's envelope `segment_id` **and** `generation_id` equal the opening identity.
- **Clause 10:** same-generation edge ⇒ `rotation_index` + 1 **exactly** (numeric over the decoded canonical-decimal strings). **Clause 11:** cross-generation edge ⇒ successor `rotation_index == "0"`, with **contiguous, non-reused generation runs**.
- **Clause 12 — append selection bound to the current `assign.generation_id`,** as a total table over the tail's two axes (unsealed+same ⇒ the **only** appendable case; unsealed+older ⇒ frozen, never adopted, create `<current gen>.0`; sealed+same ⇒ create the seal-named successor; sealed+different ⇒ create `<current gen>.0`; no segments ⇒ genesis). This resolves the contradiction you found between §1.7's "the tail is the ACTIVE segment for append" and row 10's "never adopted for append" — **the recovered tail is the boundary, never automatically the append target** — and it is what makes clause 9 an invariant rather than a hope.

**One requested item I deliberately did NOT author, and why.** You asked that a cross-generation successor "advance according to the frozen generation rule". **There is no such frozen rule to consume.** m-10 r40 §A.2's trust-bearing-counter list is exactly `turn_epoch`, `seq`, `re`, `state_seq`, `control_generation`, `event_seq`, `op_seq` + transition/operation identity components — **`generation_id` is not among them**, and §B.4's "G+1" is allocation prose, not a disclosed grammar or comparison rule. Inventing a monotone-decimal test over another domain's identifier is precisely the class of error I have now made three times in this lane. So I verify what my own bytes can prove — **contiguity and non-reuse**, which rejects the reversed shape `A.0 → B.0 → A.1` with no ordering assumption — and bind everything else to **equality against the current assignment**, which *is* owner-real (`assign` is the worker-facing disclosure, r40 §B.2:60). **Routed to master/m-10:** if a true ordering test over `generation_id` is wanted, that is an m-10 grammar disclosure to author.

**One scoping change to a rule you accepted at r2-F2 — please attack it.** Forward-link equality now applies **only to same-generation sealed edges**. Reason: after a rotation seal whose successor file never appeared (row 6), a *replacement generation* legally opens `"<new gen>.0"` — comparing the seal's unrealized `next_segment_id` there would **fail-close a legal history** (now row 13). The load-bearing checks — backward id and boundary digest — bind on **both** kinds of edge, and `next_segment_id` must still be internally consistent (`"<own gen>.<rot+1>"`). Correspondingly `next_segment_id` is REQUIRED on a rotation seal and ABSENT on a terminal/drain seal.

**Also re-derived:** §1.8's pass is now eight steps (a)–(h); the crash table is **15 rows** (11a/11b/11c + 13 replacement-over-old-generation + 14 zero-round); §1.11 (ii) now names **three distinct decidability mechanisms, each honestly scoped** (terminal seal / boundary equation / identity+progression) instead of one over-reaching digest claim; §10 gains the post-seal battery, the seal/boundary well-formedness set, the three-late-write-cases battery incl. the **11c false-alarm negative**, the identity+progression negatives, and all five clause-12 append branches plus your replacement-recovery case.

## Re-review priorities (your gate)
(a) **Terminal-seal sufficiency:** is fail-closed the right disposition (vs. truncate-at-seal), and is there any **legal** producer path — graceful drain, retry after a partial rotation, a re-opened file — that writes into a segment after its seal? If yes, my justification is wrong, not just my disposition.
(b) **Boundary totality:** is `boundary_record` total and unambiguous over every segment state (no marker / marker then unmarked tail / sealed / `segment_open`-only), and does the sealed and unsealed coincidence actually hold under the seal-follows-marker rule?
(c) **11c honesty:** is "present, untrusted, unfaulted" the correct disposition for an unmarked late suffix, or does it leave a state where a *later* legal writer could extend it into trust?
(d) **Clause 11 vs. the missing ordering:** does contiguity/non-reuse plus current-assignment equality actually cover every reversed/duplicated-generation shape reachable **without** an ordering test — and is routing the ordering question to m-10 the right call rather than deriving one locally?
(e) **The forward-link scoping:** does restricting (i) to same-generation edges reopen any shape that r2-F2 closed?
(f) **Unchanged-surface integrity:** confirm §1.6, §1.5, D2, the first-action table, §5–§8, and §9 parking are untouched in substance, and that r7/r21/`record_tool_outcome`/K6/the m-10-owned effect-UNKNOWN totality are still not re-opened.

Return `DESIGN_REVIEW_VERDICT`. **On approve:** the lane SITREP + F73 confirmations + the §D join record with m-10 (+ m-1 redaction) — none of which I file from r3 or r4 before your approve, per your gate.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r4 (@ `26dc372c…`) + this relay + one INDEX.md row; **no frozen design byte moved** (r7 + r21 re-verified unmoved), no `frank/` action, no code, no lock, no PLAN, no T4 token, no SITREP/confirmation/join filed.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict parented here over r4 `26dc372c…`, re-running the post-seal complete-round battery, the second seal, the `segment_open`-only segment, every identity/progression mismatch, and recovery by a replacement whose current assignment differs from the recovered tail; on approve, m-9.planner files the SITREP + confirmations and opens the §D join record with m-10.
