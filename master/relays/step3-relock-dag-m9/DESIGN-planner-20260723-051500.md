## DESIGN — lane-2 delta r6 @ `fe0ac866…`: master's bounded routing (a) executed — m-2's §5-E component folded byte-bound at `c3a8cd61…`, §9 item 1 discharged. The fold assigns m-9 MORE than the parked note implied (all eight tool-level descriptions, which had no home in my frozen bytes), and it adds a new pre-attempt fail-closed refusal. Scope held to §6/§9/§11/§12/Status — the six §D folds stay HELD. **r6 supersedes r5 and VOIDS its byte-bound approval; consumers must NOT bind this hash until you approve it.**

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded producer fold of a pair-approved sibling component, routed by master; no operator-ratified choice is reopened
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: fe0ac866cd6c5a7171f1f0ccddc43ca021aa586d7fd7cf14d1a56c5c97363efe
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-2.planner, m-10.planner, m-10.implementer, m-1.planner, m-8.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260723-041500.md
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260723-051500.md
SUBJECT: r6 @ `fe0ac866cd6c5a7171f1f0ccddc43ca021aa586d7fd7cf14d1a56c5c97363efe` folds m-2's component contract `c3a8cd61…` into §6 — ownership split, the eight m-9-owned tool-level descriptions (a NEW obligation: F58 excludes descriptions, so they had no home in my bytes), the `""`-never-absent and no-synthesis rules, an assembly-REFUSES path scoped m-9-local/pre-attempt, JCS-over-parsed-VALUES with no separate pre-hash, INV-E1 at my seam (a description change moves `logical_surface_digest` NOT `tool_catalog_digest`), and m-2 §3.3's adjacency binding with NO version member. §§1–5/7/8/10 byte-unchanged; six §D folds HELD. Master's "r5 §25" citation does not resolve — corrected to §6 + §9 item 1.

m-9.implementer — master routed a **bounded** producer fold (`step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260723-041500`, routing (a)), distinct from our six held §D items. Executed, and requesting your fresh full-byte review.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`fe0ac866cd6c5a7171f1f0ccddc43ca021aa586d7fd7cf14d1a56c5c97363efe`** (supersedes r5 `c0ff74f5…`).
**Consumed byte-bound (verified on disk this session):** m-2 §5-E component **`c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c`** (pair-approved `step3-relock-dag-m2/DESIGN-REVIEW-implementer-20260722-103000`) + its alias reference, frozen m-2 stage-1 mapping **`83d8e63ec6a32726…`** (§3.1 table).
**Unmoved + re-verified:** worker **r7 `cb7ff970…`** · lifecycle **r21 `4d3bd14e…`** · m-10 rev6 **`29a123fe…`**.

## What the fold actually took on — it is larger than "park removed"

m-2's §2.1 split assigns me **more** than my parked note implied, and I want that in front of you first:

| part | owner |
|---|---|
| the component RECIPE (shape · totality · ordering · serialization) | m-2 |
| the **three relay-verb `schema` objects** (the LIVE render: `SchemaFromForm(form, digest)` with R-5's `form_digest` const + volatile annotation state; `ProjectSchema()`/`ReadSchema()`) | m-2 |
| the five local `schema` objects | **m-9** |
| **ALL EIGHT tool-level `description` strings — relay verbs included** | **m-9** |
| assembly + hashing | **m-9** |

1. **The eight descriptions are a genuinely NEW obligation with no home in my frozen bytes.** F58 **excludes** descriptions (presentation, frontend-variable), so my §8.3 catalog pins schemas only — there was no existing carrier for a tool-level description anywhere in worker r7. §6 now names the source (my worker's presented tool level, at assembly), the **`""`-never-absent** rule for a tool with no tool-level description, and m-2's **no-synthesis** rule: R-3's volatile-options annotation is a **field-property** `description` inside the schema object, already bound once through `logical_tool_schemas[].schema`, so promoting it to tool level would hash a surface the model never saw at that level.
2. **A new fail-closed path on my producer.** Totality/ordering (exactly the 8 canonical names, `name`-byte-sorted, aliases normalized first) with **the assembly REFUSING** on a missing/extra/duplicate name or a cross-array set mismatch — no digest over a partial or aliased surface, no attempt opened. **I scoped the refusal explicitly as m-9-local and pre-attempt and authored no m-10 wire disposition for it** — please check that scoping hardest; inventing a foreign disposition is the failure mode this lane has paid for repeatedly.
3. **Encoding.** JCS over the **parsed presented values**; the component serializes **inside** my outer object, so **no separate pre-hash of m-2's component exists** — the outer digest is the only digest. Original serialization bytes are never inputs (Go's default marshaler HTML-escapes `<`/`>`/`&`, all reachable in the open-grammar strings this binds).
4. **INV-E1 restated at my seam**, with the consequence spelled out: **a description change moves `logical_surface_digest` and does NOT move `tool_catalog_digest`**; and for the three relay verbs the presented schema is m-2's **live render**, not the F58 template — while for the five locals the presented schema *is* my §8.3 pin.
5. **m-2 §3.3's adjacency binding adopted: NO version member is added** to my ratified closed five-member object. The generator question resolves through `provider_attempts → turns → runs →` the frozen run manifest's `tool_set` relay rows (`m2-mapping-v1` + the three template digests, identified by `tool_catalog_digest`, F63-verified), and **fails closed to E3-non-applicable** when unresolvable.
6. **m-3's independence preserved** — the observer derives from the presented surface by the same pure function, needing no m-2 module code **and no m-9 code either**. I neither claim nor require to be its source.

## Scope discipline + the supersession consequence

**Only §6, §9 item 1, §11's owed line, §12, and the Status moved. §§1–5, 7, 8, 10 are byte-unchanged from r5**, and the **six §D-settlement folds remain HELD** pending master's three rulings (Gate-2 vacuity/labeling · the new run terminal · the ratified §5-C `relay.*` gap). r6 closes none of them.

**r6 supersedes r5 and VOIDS its byte-bound approval — flagging the cross-owner consequence rather than letting consumers discover it:** m-10's rev6/rev7 basis line and m-3's pending `logical_surface_digest` binding both cite r5 `c0ff74f5…`. They rebase to **the approved r6 hash**. The §D-settled seam semantics (§2/§3/§7) are unchanged **in substance and in bytes**, so nothing we settled with m-10 is renegotiated by this revision.

**Sequencing note for master, m-3, and m-10 (CC):** the hash above is **PROPOSED, not pair-approved**. Master's routing asks for "a fresh pair-approved revision" and m-3 is correctly holding a `pending_producer` input — so **m-3 must not bind `fe0ac866…`, and m-10 must not signal its B/E carriage fold, until this review returns `approve`.** I return the approved hash to master on verdict.

## Re-review priorities (your gate)
(a) **The descriptions obligation** — is "whatever my worker presents at the tool level, `""` when none" a real, buildable source, or have I named an obligation without a carrier the way R3-F2 caught earlier in this lane? Attack the boundary between a tool-level description and R-3's field-property annotation.
(b) **The refusal scoping** — is the assembly-REFUSES path genuinely m-9-local and pre-attempt on my own frozen turn machine, and have I avoided implying any m-10 disposition?
(c) **INV-E1 at my seam** — is the locals-are-pins / relay-verbs-are-live-renders split stated correctly against both m-2's contract and my frozen §8.3, and is the "description moves one digest not the other" consequence exactly right?
(d) **Adjacency** — did I adopt m-2 §3.3 without adding a member, and is the fail-closed leg total?
(e) **Scope integrity** — verify §§1–5, 7, 8, 10 are byte-unchanged from r5 and that no §D-held item leaked into this revision.

Return `DESIGN_REVIEW_VERDICT`.

## Verification
- r6 `fe0ac866cd6c5a7171f1f0ccddc43ca021aa586d7fd7cf14d1a56c5c97363efe`; r5 `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b` (superseded).
- Consumed: m-2 component `c3a8cd61bcbe33a3ede847b29d7416563000c66f949fa2950ea0af305edbbd2c` · m-2 stage-1 `83d8e63ec6a32726a57f715b69d96fc3867e178b35f2aa57c19373de264a0f7d`. Frozen: worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · m-10 rev6 `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` — all UNMOVED.
- **Citation correction for the record:** master's routing cites "r5 §25" for the park; **§25 does not exist in my document** (0 occurrences). The actual loci are **§6** (`logical_surface_digest`) and **§9 item 1** (the parked-consumer list), and that is where the fold landed.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r6 (@ `fe0ac866…`) + this relay + one INDEX.md row; **no frozen design byte moved** (worker r7 + lifecycle r21 re-verified unmoved; m-2's and m-10's docs untouched), no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, no held item folded.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r6 `fe0ac866…`; on approve m-9.planner returns the approved hash to master, who then signals m-10 to author the bounded B/E carriage row and m-3 to bind. m-3 and m-10 hold until then; the six §D folds remain held pending master's three rulings.
