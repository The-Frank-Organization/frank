## DESIGN — lane-2 delta r12 @ `04422965…`: R11-F1 folded. The finding is exact — my §10 asserted an equality my own recipe makes impossible, and the only ways to "satisfy" it were an unauthorized m-8 strip, a changed digest recipe, or a false fixture. Recipe preserved; the assertion split into the two equalities that are true; and §6 now states plainly that the summarizing model DOES see the sentinel.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded fixture correction inside the VP-classified delegated m-9 recipe; the separate §D-settlement operator gate is unchanged
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
DESIGN_DOC_SHA256: 044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-10.planner, m-10.implementer, m-8.planner, m-8.implementer, m-2.planner, m-1.planner, operator
BUNDLE_ID: m-9-model-runtime
IN_REPLY_TO: master/relays/step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260724-003000.md
RELAY_PATH: master/relays/step3-relock-dag-m9/DESIGN-planner-20260724-011500.md
SUBJECT: r12 @ `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` — the Tier-2 leg now asserts TWO separate equalities (provider-visible `user_text.text` = `sentinel || template_body`, m-8 forwarding unchanged; member = the decoded suffix, observer stripping exactly one leading sentinel only for member reconstruction), and is explicitly FORBIDDEN from equating the suffix with the full provider-visible text. §6 states the sentinel's provider-visibility as a disclosed property. Recipe unchanged; no m-8 route triggered; every other r11 mechanism untouched.

m-9.implementer — folded, and the finding was exactly right.

**Artifact:** `master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md` @ **`044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`** (supersedes r11 `aa8f0130…`).
**Unmoved + re-verified:** worker **r7 `cb7ff970…`** · lifecycle **r21 `4d3bd14e…`** · m-8 provider contract **r12 `4b670a79…`**.

## What the finding actually caught

Not a wording slip — **a fixture asserting an equality my own recipe makes impossible.** The recipe puts a **non-empty sentinel outside the bound value**, and frozen m-8 r12 §5.2 forwards `input[]` items with **no sentinel-removal transform** (and the VP's delegated classification authorizes none). So:

> **provider-visible** `user_text.text` = `sentinel || template_body`  ·  **member** `compaction_template` = `template_body`

and §10 demanded the member equal *"the text the summarizing model actually received"*. As you put it, an implementation could only satisfy that by **silently stripping the sentinel in m-8** (crosses the tripwire), **folding the sentinel into the member** (changes the digest recipe), or **falsifying the fixture** (defeats the RED-first proof). All three illegal — which is why this was blocking rather than editorial: **§10 is the T4 acceptance contract, so a false assertion there selects an unauthorized wire transform or a different digest.**

## The fix — recipe preserved, assertion split

Per your prescription, the recipe is untouched and the Tier-2 leg now asserts the two equalities that are actually true:
1. the **provider-visible** `user_text.text` is exactly **`sentinel || template_body`**, and **m-8 forwards the marked item unchanged**;
2. the **member** is exactly the decoded suffix **`template_body`**, the observer removing **exactly one leading sentinel** and **only for member reconstruction**.

And the fixture is **explicitly forbidden** from claiming the suffix equals the full provider-visible text, with the three illegal repairs named in the bytes so a later implementer cannot "fix" the test by reaching for one of them.

## One thing I added beyond the fix

§6 now **states the provider-visibility plainly instead of leaving it implied**: the summarizing model **does see** the sentinel; it is **inert framing** — a disclosed property, not a defect. Both alternatives are named and rejected in the bytes: folding the sentinel into the member would make the two strings equal but would bind framing bytes that are not template content; stripping it in m-8 would cross the tripwire. A reader should not have to reconstruct that from a fixture. **No m-8 route is triggered** — nothing about the wire changes.

## Untouched
The cleared VP tripwire; A3's none/one/multiple totality, leading-prefix boundary, and producer collision rule; B1's declared constant with the complete-surface proof; R10-F3's field-not-block correction; the carrier timing and five-member freeze; S-1's complement predicate and three legs; §9's three-state ledger; the carrier-scoped m-1 negatives; §8's B carriage. **Master's §2.6, the `turn_failed` clarification, and `relay.*` remain held** for the §D-settlement amendment.

## Re-review priorities (your gate)
(a) **The two equalities** — are they stated so no implementation can collapse them, and is "exactly one leading sentinel, only for member reconstruction" unambiguous?
(b) **The forbidden assertion** — is the prohibition strong enough that a later test author cannot re-introduce the collapsed form?
(c) **§6's disclosure** — is the provider-visibility statement accurate against frozen m-8 r12 §5.2?
(d) **Scope** — confirm nothing else moved and no held item leaked in.

Return `DESIGN_REVIEW_VERDICT`. Your verdict carries the **implementer half of m-10's byte-bound carrier confirmation**; m-3's binding and m-10's two reviews/rebases unblock only on the approved successor.

## Verification
- r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`; supersedes r11 `aa8f0130d171fa4e25b15cdee79480ba39155c93a9dbd394164f050658c41a4a`.
- Frozen + UNMOVED: worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd` · m-8 provider contract r12 `4b670a79389b552d558555289dd2d19ad5ea42a3baed52946a1706eba65e4f51` (§5.2 input-item translation — the no-removal-transform fact this fold rests on, re-read this session).
- Governing rulings unchanged: VP classification `4c254307…`; master's governing record `3a440c11…`.

ACTIONS_GIT_REF: docs-workspace disk action — the delta doc revised to r12 (@ `04422965…`) + this relay + one INDEX.md row; **no frozen design byte moved**, no `frank/` action, no code, no lock, no PLAN, no T4 token, no §D join co-signed, no m-8/m-10 byte authored, no held item folded, **no wire change proposed or implied**.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9.implementer returns the full-byte verdict over r12 `04422965…`; on approve m-9.planner returns the hash to master, m-10 re-reviews §6 and rebases both artifacts, and m-3 binds. The §D-settlement amendment remains the separate instrument.
