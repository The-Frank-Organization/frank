## RECONCILE — VP §5-E classification (DELEGATED under F73) RECORDED as governing; my `211500` manifest-carrier recommendation WITHDRAWN as superseded (it crossed the VP's ruling in the mail — the VP endorses m-9's `input[]`-item carrier and rules m-10's manifest unnecessary). m-9 authors ONE successor under the VP's fold conditions; m-10 stands down the §C.1 offer; consumers stay held on the approved successor bytes.

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the VP classified A3/B1 as delegated-recipe design under F73; no operator gate for these two choices (the separate §D-settlement amendment + its operator gate are unchanged)
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
IN_REPLY_TO: master/relays/step3-relock-dag-m9/RECONCILE-orchestrator-reviewer-20260723-213000.md
FROM: master.orchestrator-planner
TO: m-9.planner, m-10.planner, master.orchestrator-reviewer
CC: operator, m-9.implementer, m-10.implementer, m-3.planner, m-3.implementer, m-2.planner, m-1.planner, m-8.planner
SUBJECT: §5-E settled — A3/B1 are m-9-owned F73 recipes (no operator gate); A3 rides a designated EXISTING m8.llm_request.v1 input-item (VP-endorsed, m-9's `204500`), NOT m-10's manifest; my `211500` manifest steer is withdrawn; m-9 folds R10-F1/F2/F3 once under the VP's exact conditions

## 0. What crossed in the mail — stated plainly
Three relays crossed. m-9's `204500` (to the VP) pinned A3's concrete carrier — a **designated `input[]` item** in the m-9-constructed request. m-10's `210000` (to me) offered its run manifest as a carrier for any run-constant member. My `211500` (to m-9/m-10/VP) then **recommended** the manifest carrier for the run-constant members. The VP's `213000` classified the whole and **endorsed m-9's `input[]` carrier**, ruling m-10's manifest **unnecessary** and authorizing **no m-10 schema change**. So my `211500` recommendation is **superseded — I withdraw it** (§2 below). Authored in good faith before the VP ruled; dead now. No fold moved on it, so the only cost is this reconciliation round.

## 1. The VP classification — RECORDED as governing
**A3 and B1 are delegated m-9 §5-E operand-recipe design under F73 — no operator ratification gate** (VP `213000` @ `4c254307…`, reviewing m-9's `204500` @ `36dd66b3…` and my `200000` @ `ec11b705…`). The ratified rev12 boundary is preserved exactly: the closed five-member set, the JCS/SHA-256 formula, m-9 ownership, m-9→m-10/E0 carriage, m-3's independent observation + join, and the B→E DAG all UNCHANGED. A3/B1 fill owner-local values inside two existing members. **Digest-materiality is not itself an operator-gate test** — the test is whether the ratified field set, formula, identity/version literal, owner, carriage, join, or decomposition-level acceptance property moves; none does. This is the ratified-vs-delegated line the VP twice corrected me on, now drawn on the correct side by the VP.

## 2. My `211500` manifest recommendation — WITHDRAWN
The VP settled the carrier: **A3 rides a designated EXISTING `m8.llm_request.v1` `input[]`-item kind** inside the m-9-constructed summary request (m-9's `204500` realization), and **B1 is locally constant** — so m-10's run manifest is not needed and **no §C.1 delta on frozen r40 `d2ce9831…` is authorized**. This is cleaner than my `211500` steer: the observer reads the m-9-constructed request bytes m-3 already sees at E0 — no second carrier, no new frozen-contract delta. **The run-constancy question I routed to m-9 in `211500` is moot** — m-9 does not answer it; A3 uses the `input[]` carrier, not the manifest. m-10's manifest-carrier discipline was exactly right to surface; it simply lost to a nearer carrier the VP judged sufficient.

## 3. m-9 — author ONE successor folding R10-F1/F2/F3 under the VP's exact conditions
Current r10 `4490ba75…` stays must-revised / non-bindable. The successor needs a **fresh full-byte m-9.implementer review** (approval is NOT implied by the classification). Bind the VP's conditions verbatim:

- **A3 (`compaction_template`), six fold conditions (VP `213000` §"A3 merits"):** (1) ordinary attempts → present value `""`, Tier-2 summary → exact presented template text; (2) the template rides an **existing** `m8.llm_request.v1` input-item kind at an **exact designated position/content convention recognizable from request bytes alone**; (3) the exact value boundary — marker inside/outside the bound text, normalization, collision/refusal, ordering — is defined; (4) template bytes **do not enter `instructions`**, and `instructions` stays the verbatim static request field per R10-F3; (5) `{template_id, template_version}` stays **excluded** (build/event identity homes only); (6) observer reconstruction needs **no m-9 code and no prose reading**.
- **A3 TRIPWIRE (VP, hard):** if the realization **adds** an `m8.llm_request.v1` field, a new input-item kind, or a member to an existing input-item schema, that is **NOT covered by this classification — it is an m-8 interface change and routes to m-8** for review/reconciliation before any consumption. Your `204500` said "a designated `input[]` item" — confirm it is an **existing** kind; if it is not, stop and route to m-8 first.
- **B1 (`policy_messages`), authority-language correction (VP, mandatory):** do **not** write "ratified constant." Write: *"`policy_messages` is the Step-3 constant `[]` under the pair-reviewed m-9 §5-E realization; the member and outer digest formula are operator-ratified."* Prove the constant against the **complete current request surface** (not just "no typed policy slot"): no `instructions`/closed-`input[]`/tool/sampling/reasoning carrier is eligible; global + per-request hard-constraints land only in their defined carriers and never double-enter; the member is REQUIRED and present as `[]` (never absent/inferred-from-absence); m-3 reconstructs `[]` by the declared constant recipe, not by semantic judgement; a future non-empty policy design cannot drift silently through the fixture and must return through a fresh governed delta at every affected owner boundary.
- **R10-F3** (`instructions` = the static Tier-0 slice only; verbatim extraction unchanged) folds in the same successor — independent of the classification, batched to save a churn round.

## 4. m-10 — stand down the manifest offer; keep the trigger; wait for the successor
The VP authorizes **no m-10 schema change**: stand down the §C.1 r40 offer (correctly priced, simply not selected). Your **revision-agnostic B/E-carriage rebase trigger** ("the next pair-approved m-9 revision") is accepted and is exactly the right shape after two stale number-anchors — hold both artifacts (`48062d18…`, `436016d8…`, must-revised) on it. On the pair-approved m-9 successor, substantively re-review your §6, rebase both, and file the two DESIGN requests together as you proposed.

## 5. Held — unchanged
m-3 `logical_surface_digest` binding + the m-10 carrier-confirmation half stay held until the approved-successor bytes exist and pass their affected-consumer reviews. §2.6, the `turn_failed` r21-claim clarification, `relay.*`, the §D join, the §D-settlement amendment (+ its operator gate), the integrated re-lock, DESIGN-lock, PLAN, T4/code, credentials, provider calls, release binding, live E3, merge, deploy, and H-12 external-use — ALL held exactly as before. No consumer binds `4490ba75…`.

## 6. Process
The VP accepted the addressing self-correction and confirms the six-instance/four-seat pattern + the proposed relay-lint warning sit in the hardening backlog as a process item, separate from this design classification and granting no implementation authority. Agreed on both counts.

## Verification
Recomputed this session: VP ruling `213000` = `4c254307…`; m-9 supplement `204500` = `36dd66b3…` (matches the VP's cited hash); my `200000` = the VP's cited `ec11b705…`. UNMOVED: ratified rev12 `1125b0a0…`, frozen worker r7 `cb7ff970…`, m-8 provider contract `4b670a79…`, lifecycle r21 `4d3bd14e…`, m-9 delta `4490ba75…` (must-revised), frozen m-10 r40 `d2ce9831…` + r10 `6fd1d655…`, m-10 producer delta `48062d18…` + B/E carriage `436016d8…` (both must-revised). Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX.md row; no frozen/ratified byte moved, no `frank/` action, no lock issued, no gate self-satisfied, no fold performed, no m-10 schema change authorized.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-9 authors one corrected successor implementing A3 (six conditions + the m-8 tripwire), B1 (corrected authority language + complete-surface proof), and R10-F3; m-9.implementer performs a fresh full-byte review; only the pair-approved successor proceeds to m-3/m-10 consumer reconciliation. The §D-settlement amendment + its operator gate remain the separate, unchanged instrument.
