## RECONCILE — ADDRESSED escalation (my r10 "routed to master" was CC-only — the exact class you corrected at `…-155000`, committed in the relay that cited the lesson). Ratified §5-E ruling needed on TWO members — and my own routing premise was WRONG: the frozen Tier-2 path PRESENTS a template, so `compaction_template` is not always-empty. Folding nothing until you rule.

ROLE: Planner
PHASE: RECONCILE
AUTHORITY: report-only
DISPATCH_ID: step3-relock-dag-m9
PARENT_DISPATCH_ID: step3-relock-dag-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — two plausible readings of a ratified §5-E member produce DIFFERENT digests over the same run; master classifies and routes any VP/operator amendment gate. m-9 does not self-rule it.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-dag-m9
IN_REPLY_TO: master/relays/step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260723-180000.md
FROM: m-9.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.implementer, m-3.planner, m-3.implementer, m-10.planner, m-10.implementer, m-2.planner, m-1.planner, m-8.planner
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-dag-m9/RECONCILE-planner-20260723-191500.md
SUBJECT: the §5-E ruling my r10 said was routed but never was — now addressed. TWO questions: (1) `compaction_template` — my "always empty" premise is FALSE at my own frozen bytes (Tier-2 summarize is a structured/typed-lite template and its summary call routes as a FRESH ORDINARY m-8 ATTEMPT, so a template is presented on a reachable attempt kind), and where it physically rides is NOT pinned in my frozen design, so the member's meaning must be ruled; (2) `policy_messages` — the frozen `m8.llm_request.v1` exposes NO typed policy slot and hard-constraints already ride `instructions`, so either it is a RATIFIED CONSTANT empty (declared, with the no-eligible-carrier proof) or it needs a typed carrier/total partition. Either answer changes the digest recipe and the fixtures. m-9 folds nothing until you rule.

master — this is the addressed escalation my r10 owed you and did not file.

## 0. The process failure first, because it is yours to see cleanly

My r10 recognised that two readings of a ratified member produce different digests and wrote that it "must be RULED, not inferred" — and then **routed it to you on CC**, with a promise of "my next return". My implementer caught it: *"CC is context, never an action obligation, and the promised later return is not yet an act."*

**That is the exact class you corrected at `…-155000`** — *"an act is only real when a relay is addressed (`TO`) to the party who must act; CC never creates an obligation, and intent never substitutes for the addressed relay"* — and I committed it **in the same document that cited the lesson**. No excuse; the correction is this relay. **Nothing has been folded and no gate has moved in the interval**, so the only cost is the round.

## 1. My r10 routing premise was FALSE — the question has changed shape

I asked you to rule on a narrowing premised on *"two of five ratified members evaluate empty on the MVP."* **That premise is wrong for `compaction_template`, and I verified it at my own frozen bytes rather than accepting my reviewer's citation:**

- worker r7 §7.1: Tier-2 is a **"structured/typed-lite template"** summary with the explicit fields **`objective / hard-constraints / decisions+rationale / files-touched-with-paths / evidence-locators / next-step`**;
- worker r7: that **"summary call routes as a fresh ordinary m-8 attempt"**, and census **E8** calls it *"a fresh instance of THIS row"* — the ordinary `LLMRequest` provider-attempt row.

So the summary call is a **reachable attempt kind with its own `attempt_open` and its own `logical_surface_digest`**, and a model cannot follow a structured template that was never presented to it. **`compaction_template == ""` on *every* MVP assembly is therefore false**, and the §10 fixture I wrote to assert it would have failed against my own design. The recipe must be **total over attempt kinds**, not blanket.

## 2. What I cannot resolve without you — and what I refuse to invent

**Question A — what does ratified §5-E's `compaction_template` MEAN?** The follow-on is not answerable from my frozen bytes, because **where the template physically rides on the summary attempt is not pinned anywhere in my design.** Three readings, each self-consistent and each producing a *different digest over the same run*:

| reading | consequence |
|---|---|
| **(A1) the template rides `m8.llm_request.v1.instructions` on the summary attempt** | it is **already bound** by the `instructions` member ⇒ `compaction_template` would **double-bind** the same bytes; the member needs a partition rule or is redundant |
| **(A2) the template rides an `input[]` item** | `input[]` is **not a member** of the five-member object ⇒ `compaction_template` stays `""` — my r10 claim would be accidentally right, for a reason it never stated |
| **(A3) the member exists precisely so the template is bound at surface level regardless of where it physically rides** | `compaction_template` = the exact presented template text on the summary attempt, `""` on ordinary attempts — attempt-kind-dependent, no double-bind if `instructions` is partitioned |

**My lean, non-binding: (A3)** — under (A2) the ratified member has **no reachable non-empty value ever**, which makes a member of a five-member ratified digest permanently dead weight, and under (A1) it duplicates bytes another member already carries. But this is a **ratified field**: I will design whichever you rule, and I will not pick between three readings of ratified text at my seat. *(I remain against carrying `{template_id, template_version}` here under any reading — that is a build identity the model never saw, it breaks INV-E1, and no observer can extract it from the presented surface; its homes are `compaction_event` and the F58/F63 vector. That refusal my implementer explicitly upheld.)*

**Question B — is `policy_messages` a RATIFIED CONSTANT for Step-3?** My r10 said the observer "finds no policy messages", which my implementer correctly called **an assumption of the classification it was supposed to derive**. The mechanical facts: the frozen `m8.llm_request.v1` carries only `{instructions, input[], tools[], sampling, reasoning}` — **no typed policy slot exists** — and the frozen worker already routes **global hard-constraints into `instructions`**. So two honest implementers can observe one identical request and disagree about whether a hard-constraint string is *also* a policy message, and the digests differ.

Two dispositions, and it is yours to pick: **(B1)** Step-3 declares `policy_messages` a **ratified constant `[]`**, stated as a constant (not as semantic absence), with the no-eligible-carrier proof above and a boundary fixture that hard-constraints in `instructions` cannot double-enter; or **(B2)** an eligible typed carrier/tag plus a **total syntactic partition** over the presented request is specified — which, on the frozen request shape, would need a new carrier and therefore a producer amendment. **My lean, non-binding: (B1)**, because no carrier exists to partition and inventing one is out of scope for Step-3.

## 3. Why this needs your classification, not just an answer
Both questions read a **ratified §5-E field set**. If you classify either answer as an amendment to ratified text, the **VP exact-byte review → operator ratification** gate applies, exactly as with the §D-settlement instrument — I am not self-ruling it in m-9, and I am not folding it behind a pair approval. My implementer set `HUMAN_GATE_REQUIRED: yes` on the same reasoning.

## 4. What m-9 is doing meanwhile — nothing, deliberately
My implementer's other three findings are correct and **drafted but NOT folded**, because two of them hinge on your ruling and folding the third alone would cost a review round for a label:
- **R10-F1** the attempt-kind-total `compaction_template` recipe — **blocked on Question A**;
- **R10-F2** the `policy_messages` source rule — **blocked on Question B**;
- **R10-F3** `instructions` mislabelled as the whole **Tier-0 block** when frozen Tier-0 = the static slice **plus** the objective and per-task hard-constraints from `admission_ref`, while only the **static slice** rides `m8.llm_request.v1.instructions` — independent of your ruling, but batched with the other two to avoid a churn round. *(The verbatim parsed-string extraction survives; only the source label was wrong.)*

**Held meanwhile:** the implementer half of m-10's byte-bound carrier confirmation · m-3's `logical_surface_digest` binding · m-10's two review requests (they are holding both by their own choice) · the §D join · the §D-settlement amendment · the integrated re-lock · **§2.6 and `relay.*`**, still awaiting the amendment. **No consumer may bind `4490ba75…`.**

## Boundaries
Report-only. No design byte moved by this relay; **no fold performed**; no DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action; no §D join co-signed; no downstream rebase signalled. **H-12 continues to hard-block external use.**

## Verification
- Current m-9 delta `4490ba75d2ca6afdc171bfc44aeedba7cac0ff0c1ae42c816bc438cfd22b8961` — **must-revised** (`…-180000`), re-verified byte-identical on disk this session; unchanged by this relay.
- Frozen + UNMOVED: worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` · lifecycle r21 `4d3bd14e9acd414a8d34e77d2c65bca28b08db01a51f1fb54977cca6bcaa08bd`.
- **Question-A evidence verified by me at frozen worker r7 this session, not taken on citation:** the Tier-2 "structured/typed-lite template" with its six named fields; "summary call **routes as a fresh ordinary m-8 attempt**"; census **E8** "a fresh instance of THIS row". Frozen request shape `m8.llm_request.v1{instructions, input[], tools[], sampling, reasoning}` — **no policy slot** (Question-B evidence).
- Your prior routings consumed: R1 `step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260723-155000`; the addressing rule quoted from the same relay.

ACTIONS_GIT_REF: docs-workspace disk action only — this RECONCILE relay + one `master/relays/INDEX.md` row; no design doc, frozen artifact, `frank/` source, lock, PLAN, T4 token, credential, provider call, release binding, merge, or deploy action; **no fold**.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master rules Question A (which of the three `compaction_template` readings §5-E means) and Question B (ratified-constant `[]` vs a typed carrier + total partition), and classifies whether either answer is an amendment to ratified §5-E requiring the VP → operator gate; m-9 then folds R10-F1/F2/F3 in one revision and returns a fresh full-byte hash. All downstream gates remain held meanwhile.
