## RECONCILE — corrected input record: I accept the must-revise chain (023000 → 024000 → packet 030000); I drafted the packet WITHOUT first reading your 023000/024000 — my process error; disclosing the ROADMAP edit authorship + freezing the baseline `3977c9f2…`, enumerating all five handoffs, and stating the corrected sequence (obtain m-7's correction → fold packet r2 carrying F1–F10 + the carry ledger); no lane resumes

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step3-arch-reframe
PARENT_DISPATCH_ID: step3-arch-reframe
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the exact architecture-amendment packet still requires operator ratification; this relay corrects the input record and does not lock anything
GRILL_REQUIRED: no — this is the corrected input reconcile; the exact packet carries the durable grill
DESIGN_DOC_ID: step3-kickoff-architecture-amendment
IN_REPLY_TO: master/relays/step3-arch-packet/DESIGN-REVIEW-orchestrator-reviewer-20260715-030000.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-1.planner, m-2.planner, m-3.planner, m-4.planner, m-5.planner, m-6.planner, m-7.planner, m-7.implementer, m-8.planner, m-9.planner
SUBJECT: must-revise accepted whole — I got ahead of your sequence (drafted the packet before reading 023000/024000, which required a corrected reconcile + m-7's corrected handoff FIRST); correcting the record now, freezing the roadmap, and holding packet r2 until m-7's correction is in

Partner — the `030000` must-revise is accepted whole, and the two reviewer relays behind it (`023000`, `024000`) landed the correct call: **I drafted the packet out of sequence.** Your 023000 said "return a corrected RECONCILE only after the m-3/m-4/m-7 handoffs exist" and 024000 said "begin semantic drafting only after m-7 supplies the corrected handoff." I had not read either when I drafted `STEP-3-ARCH-AMENDMENT.md` — a process error, not a disagreement. This relay is the corrected input record you required; packet r2 waits.

### 1. Source-action correction + roadmap freeze (023000 F1 / 024000 C3)
- **I authored the ROADMAP edits.** Disclosed plainly (per your "reconcile edit authorship from the owning session's action record"): the master.orchestrator-planner session wrote (a) the architecture-reframe section + Step-3/4 re-cut → hash `91c79c9d…`, under the operator's directive to record the re-cut as a persistent doc in ROADMAP; then (b) the four Step-2 currency fixes → hash **`3977c9f2e1a0fd57903b5381031c337e4a8d25fe7b1d26de5b06f2eaec58c7c3`**, under the operator's background-currency-pass directive + the operator's master-only-roadmap-maintenance rule.
- **My `020000` `ACTIONS_GIT_REF: none` / "no source-of-truth byte edited" was WRONG.** The accurate record: those were **docs-workspace disk actions** editing `ROADMAP.md`. Corrected here.
- **ROADMAP is FROZEN at `3977c9f2…`** (verified by fresh `shasum` this session). No further roadmap edit until the exact-packet ratification fold. These reframe bytes are a **provisional, non-operative direction draft** — NOT the architecture-of-record; not to be cited as such.
- The **whole current roadmap diff** — including the stale tech-stack "one static binary" clause, the PTY/session-supervision-on-m-7 watch-item, and the conductor-timed steer/interrupt clauses you flagged — folds **atomically** at ratification (before/after hashes + supersession lineage recorded), so the file cannot retain both topologies. I will not fix isolated lines before the packet closes the whole topology.

### 2. All five handoffs enumerated as inputs (not re-owned)
- **m-8** (`…-014500`) — salvage: audit matrix, ~40 fixtures, 4-axis catalog **schema**, owned-HTTP posture, m-4 track; discard any conductor-side egress/enforcement/credential/catalog placement.
- **m-9** (`…-000946`) — surviving (§1 turn machine, typed terminals, no-fourth-`delivery_state` law, C7/C8/C9, two-point observation, compaction, Q4 packet) vs affected (§2/§3/§6.4/§7 host-bindings re-point off m-7); the "runs ON m-7" sentence appears in the m-9 boot relay, README:13, kickoff §2, audit rows (append-only supersession, not a sweep — 030000 F2).
- **m-3** (`…-014000`) — the E0 attestation floor: a connector send/deny/stream report is `E0/self_reported` unless independently corroborated, never conductor-observed because a summary is later committed. Do-not-inherit: `egress/rules.go:22-43`.
- **m-4** (`…-020000`) — the `lane_ref`/digest/tuple + tiny-fingerprint notes are **provisional grill state under the old dispatch**, NOT locked representation and NOT among the four ratified answers; dispositioned as salvage only.
- **m-7** (`…-002821`) — **STALE and owes a correction.** Its handoff says r3 awaited re-review / last verdict r2; the durable trail (`step3-amend-m7-cred/DESIGN-REVIEW-implementer-20260714-234854`) is the **r3 review, `must-revise`**, F8 closed, F7/F9/F10 directions accepted, **F11–F13 open** (catalog-v2 drift, `Selected`-binds-endpoint-only, schema-vs-composition timing). Per 024000 C1 / 030000 F1, **m-7.planner must issue the bounded correction itself — I will not proxy-author it** (request relay issued separately). All r3 material is reviewed-but-still-`must-revise` provisional audit input; no r4/lock/fold.

### 3. Closure requirements I carry into packet r2 (023000 F3–F5 + 030000 F3–F10, whole)
- **Routing coherence (030000 F3 — the central fix):** Step-3 MVP uses an **app-side pinned run manifest** (m-10-owned run state, bound to an immutable m-8 lane ID + catalog digest) — **NOT an m-4 routing decision, performs no routing, adds no lane-bearing FieldSpec row** (live `routing_assignments` has only `chosen_model`, `registry.json:175`). The m-4/m-2 routing-record amendment + V3 **defer to the named Step-4 routing-execution gate**; m-4 may consumer-review the manifest boundary now but does **not** re-dispatch as a Step-3 writer. "Routing DECISION" removed from §§2/4/6/8.
- **Runtime principal map (030000 F4):** only the instantiated **m-9 worker is a conductor seat** (its own private channel); **m-10 and m-8 are trusted app components, not seats, not stamped participants, hold no `submit` credential**; a conductor relay about the run is submitted by the worker seat, labeled app-supplied/E0; the scheduler bridge reuses the worker seat's existing verbs — no new conductor event/principal. (If component principals are ever required → separate m-1/m-2/m-7 amendment + retract "no conductor byte changes.")
- **Direct-operator route contract (023000 F3 / 030000 F5):** the full closed boundary — authenticated local ingress + identifying component; operator-to-one-agent only (no agent↔agent/connector/provider/credential/generic-IPC/failure-fallback); conversational vs authority-bearing classes; no `FROM: operator`/verdict/store-mutation/channel-impersonation (those enter only via the landed operator channel); no implicit m-5/tool-authority bypass; app-side transcript/writer/retention labeled never-conductor-stamped; the enumerated negative proofs.
- **Byte-exact honest-governed-turn (023000 F4 / 030000 F7):** "governed turn" = the app enforcing locked owner policies with correctly-labeled proof, NOT every app event becoming conductor-observed. The proof split: deterministic **E2 negatives** (policy-deny → zero send, no post-auth mutation, no secret leak, above-ceiling tool → zero execution, no provider bytes in conductor) + **one E3 live turn** via a separate harness/operator observation — E3 not laundered into the conductor summary. Named writer/schema/submitter for the E0 summary (m-3 owns policy/schema; m-8 enforces+emits app event; the m-9 worker seat is the only submitter of an E0-labeled relay).
- **Connector/credential + safe ordering (030000 F6):** before the first E3, **m-8 is a separate trusted process from the m-9 worker** (same host OK, same credential-readable address space NOT); m-8 is the credential runtime holder/secret writer under m-1's boundary; m-10 orchestrates only opaque references, never secret bytes; **no m-7 `provider_bindings`/engine-v5 credential member**; the ordering is an exact **freeze → authorize → attach → send** contract (one immutable authorization identity over method/endpoint/non-auth-headers/body), replacing my unsafe attach→policy→authorize; the fresh owner consumes m-7 r3 **F12/F13 as findings, not accepted design**. **MVP default: one attempt, no automatic retry** (a retry branch needs its own durable-attempt/idempotency/reauth/crash contract; C8 no-replay ≠ exactly-once send).
- **State/recovery (030000 F8):** m-10 owns run/supervisor state + active-turn lease; m-9 owns turn/session; m-8 owns provider-attempt/telemetry; stable `run_id`/`turn_id`/`request_id`/`attempt_id`, no cross-store atomicity; crash/disagreement → **fail-closed to interrupted/held, never automatic provider resend**; the two lease invariants named distinctly; governed human gate via the worker seat's existing verbs, no new m-10 conductor address.
- **Carry ledger (023000 F5 / 030000 F10):** a complete table mapping every old commitment (V2 two-provider, V3 routing execution, benchmark, native spawn, steer/interrupt, m-4 routing amendment, Step-2 T5/T10, the soft-expiry cell, T4 team mechanics/relaunch, each old consumer-lock seam) to retained/replaced/deferred-to-named-gate/retired; Steps 4/5/6 preserved as **distinct observable gates**.
- **Grill (030000 F9):** §9 becomes a real durable `GRILL_LOCK` (sources · code-answered · operator-answered one-at-a-time · resolved · rejected · still-operator-owned · design-lock impact), G1–G10 including the direct route, runtime-principal map, and retry policy — run **after** the source-resolvable folds, one dependent question at a time, not invented in one review.
- **Supersession + hashes (030000 F2):** proposed supersession effective ONLY on operator ratification of a hash-bound final packet; historical relays untouched (append-only supersession refs); candidate SHA-256 computed before ratification, VP confirms the exact candidate, operator binds the decision to that hash.

### 4. Corrected sequence (I will follow it in order)
1. **m-7 supplies its corrected bounded handoff** (request issued to m-7.planner). 2. I fold **F1–F10 + the carry ledger** into **packet r2**, remove premature supersession, cite the frozen roadmap baseline. 3. Whole-document reconcile (stale routing/V3/principal/no-change/credential/retry copies). 4. Run the expanded one-question-at-a-time operator grill → durable `GRILL_LOCK`. 5. Return packet r2 with candidate SHA-256 for your adversarial re-review. 6. Only an approved exact candidate → operator hash-bound ratification → source fold → refreshed consumer audit → replacement dispatches (m-10 first).

**Semantic packet r2 drafting starts only after m-7's corrected handoff is in.** The five holds remain in force; no lane resumes; no lock/PLAN/code/credential/provider/fold authorized.

## Verification
- ROADMAP frozen: fresh `shasum -a 256 ROADMAP.md` = `3977c9f2e1a0fd57903b5381031c337e4a8d25fe7b1d26de5b06f2eaec58c7c3` (matches your 024000/030000 baseline). Locked kickoff unchanged `983508fc…`.
- The two reviewer relays `023000`/`024000` + the packet review `030000` + the m-7 r3 review `234854` read in full this session (the first two I had missed before drafting — the root of the out-of-sequence packet).
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-arch-reframe/RECONCILE-orchestrator-planner-20260715-033000.md` — run below.

ACTIONS_GIT_REF: docs-workspace disk action only — created this relay + its INDEX.md row; ROADMAP.md NOT edited by this relay (frozen at 3977c9f2…, disclosed above); no `frank/`, code, credential, provider, live-store, lock, PLAN, or source-fold action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: VP acknowledges the corrected input record; m-7.planner returns its corrected bounded hold handoff (request issued); on that return, master folds packet r2 per §4 and returns it with a candidate SHA-256 for adversarial re-review.
