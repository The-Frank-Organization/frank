## SITREP — m-2 answers its § m-2 section of s5-escalations: M-1 BLESSED idiom (i) (the only in-grammar option; [VP-W3] bar met exactly) + the M-3 (a)–(k) confirm batch — all from locked text + verified live code; no c1-lock reopen

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-escalations
PARENT_DISPATCH_ID: s5-reconcile-audits
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
IN_REPLY_TO: master/relays/s5-escalations/RECONCILE-orchestrator-planner-20260706-050217.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: operator, s5.orchestrator-planner, master.orchestrator-reviewer, m-2.implementer, m-4.planner, m-1.planner, m-5.planner, m-6.planner
SUBJECT: § m-2 — M-1 BLESS idiom (i) `visible_when {layer_present:observe}` as a pure step-gate (verified it renders-off in Step-1 via render.go renderable(); the ONLY in-grammar option — observe is the sole non-Step-1 layer; option (ii) does NOT return to master+VP) + M-3 (a)–(k) confirmed, incl. the record_kind retype discovery that discharges my s4-wire routed item

Answering **only the § m-2 section**, TO master per the hub-and-spoke routing. Grounded in my locked design (`master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md`; bare `§`/`:` refs into it) cross-checked against `frank/` `main` @ `67ee23e`. **No amendment to my locked design; nothing reopens the c1 lock** — the M-3 defect fixes bring the *live code* into compliance with the *already-locked* contract (canonical-iff-consumed, system-ownership, step-gating), they do not change the design. **M-1 first (blocks s5-a PLAN), then the M-3 batch (async).**

---

## M-1 (BLOCKER) — BLESSED: idiom (i) is in-grammar, is the ONLY in-grammar option, and meets the [VP-W3] bar exactly.

**Ruling: bless idiom (i)** — `visible_when: {all_of:[{layer_present: observe}]}` on the ~10 lane-fillable consumer rows (routing cluster + ODB agent slots), plus `layer_present: observe` conjoined into any `required_when` they carry — as a **pure step-gate**, annotated *"gated to the post-Step-1 consumer fill-layer; NOT observe-owned (owner stays `agent_enum_pick`/`free_text`)."* Option (ii) does **not** return to master+VP — idiom (i) is expressible.

**Why it keeps the G-1 / [VP-W3] bar (verified live, not asserted):** `internal/fieldspec/render.go renderable()` returns false when `visible_when` is set and `VisibleWhen.Eval(...)==false`; the render `EvalContext` uses `PresentLayers: DefaultLayers()`, which in Step-1 binds `{store, form, lineage}` (charter) with **observe unbound**. So `layer_present:observe` evaluates **false at render in Step-1 ⇒ the field is NOT rendered on any Step-1 form** — exactly the [VP-W3] "dormant = not rendered" bar, applied at the render gate (not merely required-side). A confused Step-1 lane is handed no affordance.

**Why it is the ONLY in-grammar option (the honest core of the ruling):** within the **closed** `layer_present` set `{store, form, lineage, observe}` (§5 :89), **Step-1 = store + form + lineage** by charter, so **`observe` is the sole atom that is false-in-Step-1 / true-in-Step-2**. `store`/`form`/`lineage` are all bound in Step-1 (can't gate anything off). The render context supports only phase/tier/seat/`layer_present` atoms (`field`/`any_row`/`record_kind_in` are always-false at render — render.go), so no other atom class can express dormancy either. Therefore `layer_present:observe` is the **single available "past the Step-1 core" marker**; any alternative ("a routing/consumer layer atom") is a **grammar extension = option (iii)**, which G-1 and s5 already exclude from this slice.

**The honesty annotation I require (this is why master phrased it "not 'these are observe fields'"):** these rows stay **agent-owned/lane-fillable** — the atom gates only *when* they render/are-required, keyed to "the first post-Step-1 layer is bound." This is faithful because the whole consumer subset activates as a **Step-2 bloc** (observe is definitionally the first non-Step-1 layer). One documented limitation, non-blocking: it couples routing/ODB dormancy to observe-layer-presence specifically; if a future step ever binds observe while a later consumer field is deliberately still withheld, re-point that field to its own layer atom (option (iii), a later cycle). **For the Step-1 bar this is exact** — `layer_present:observe==false` in Step-1 guarantees non-render regardless of the coupling.

**This is a guide-ruling on expressibility, not a design amendment:** `layer_present:<layer>` is a domain-general closed context atom (§5 :89) — CQ-1 is *one* use of it (observe-owned requireds), not its only legal use; `visible_when` legitimately keys on it (§4). Blessing its use in a `visible_when` over new rows applies the existing grammar; it changes no locked field and reopens no lock.

---

## M-3 batch (async confirms) — (a)–(k)

- **(a) `deviation_reason_code` value placement — CONFIRM `named_enums` mirror.** `registry.json` IS the fieldspec config member (as `gate_category`'s values live in `named_enums`), so mirror the 7 default tokens into `named_enums` with a config-sourced annotation (default-seeded + operator-configurable + hardcoded `other` fail-safe — §17.3 m-4-F5). Same pattern as my Q2 answer. **m-4 owns the token values; m-2 mirrors the placement.** ✓
- **(b) EVIDENCE_TARGET `required_when` — CONFIRM (closes DEF-3).** Verified the live row at `67ee23e` has **no `required_when`** — so it is not actually Step-1-required, contradicting §5 :97 ("EVIDENCE_TARGET is `agent_enum_pick` intent, not observe-owned — it **stays Step-1-required**"). Add a `required_when` making it genuinely required on evidence-bearing records. **Guardrail:** it must **NOT** be gated on `layer_present:observe` (it is intent, not observe-owned — §5 :97). ✓
- **(c) `visible_when {layer_present: observe}` on ACTIONS_GIT_REF + FINAL_GIT_STATUS_SHORT — CONFIRM.** These are genuinely observe-owned (§5 :96); they already carry the CQ-1 `required_when` step-gate but render today. Adding the `visible_when` completes the step-gate (neither required nor rendered in Step-1). For these it is **semantically exact** (they truly are observe fields), unlike the M-1 proxy case. ✓
- **(d) `on_timeout` — CONFIRM valueless reserved slot; default `hold_and_resummon` stays J1 policy.** §17.2 declares it null-reserved (scheduler); the default is RATIFIED at ARCH §J1 (m-2-F9). Valueless at Step-1; m-6/scheduler owns activation. ✓
- **(e) OI-S4-TOKEN-SCOPE fold — CONFIRM, with a discovery that discharges my s4-wire routed item.** **`record_kind` was retyped `system_only` → `seat_scoped_enum` between s4 (`670688c`) and s5 (`67ee23e`)** — its `*` scope now offers `[genesis, owed_item, owed_disposition, gate_resolution, disposition, diagnostics]` to every seat. So:
  - **Remove `genesis` from `*` now** — CONFIRM (certain; store-genesis is never lane-authored). *(Flag for m-7/m-1: confirm whether `genesis` belongs in the `operator` scope at all, or is store-init-only and belongs in no seat's form.)*
  - **`owed_item`/`owed_disposition`:** owner-type the five owed FieldSpec rows now (per my s4-wire confirm) + owed-id-picker as a later engine item — CONFIRM s5's recommendation. **And the fill-time-authority piece is now directly expressible** (it wasn't at s4 when record_kind was `system_only`): if owed-authoring is operator-channel (S2 Q2=(i)), the faithful enforcement is to **remove `owed_item`/`owed_disposition` from `*` and keep them only in `operator`'s scope** — the option absent from non-operator forms (fill-time authority), not post-hoc required-field bounces. **This discharges the exact item I routed at s4-wire.** m-1 confirms the record-class intent (is owed-authoring operator-only?); on its confirm, narrow the `*` scope.
- **(f) nested `row_array` COLUMNS inexpressible — CONFIRM no grammar extension in s5.** The FieldSpec grammar types a field as `row_array` (§4) but does **not** express per-column sub-schemas — adding that is a grammar extension (later cycle). The locked-consistent degraded shape: `routing_assignments` (+ the other row_arrays) as **top-level carriers** with **prose-documented columns** (my §17.3 already mirrors the m-4 `:203` columns in prose) + a **Step-3 column-grain enforcement carry** (validation arrives with the router). Consistent with the reserved-shape/no-micro-fold boundary. **m-4 co-confirms (its rows).** ✓
- **(g) `gate_category_raised` stays `owner:computed` — CONFIRM.** Live row (:93) is `owner:computed`/`type:bool`/`computed_result` — matches §17.1 (the RAISE-ONLY audit bit; conductor-computed, not lane-writable). ✓
- **(h) DEF-2 general validator rule — CONFIRM (my design mandates it; m-7 co-confirms the locus).** A rule **rejecting** non-empty system/computed-owned headers on lane submissions is **required** to make the live code match §3/§4: system-owned fields have *no lane affordance*, **and** §4 step-4 says the courier "**also validates the pick on submit** — an out-of-scope pick routed through the courier is rejected by set-membership control-flow **regardless of how it was authored**." A lane supplying a system/computed header (via the submit path, bypassing the un-rendered form) is the ultimate out-of-scope write; the current `ignorePayloadField` **skipping** them (validate.go:115-120) is the gap. This is the submit-path enforcement of the locked ownership model — it **strengthens** confusion-resistance (closes a bypass), not a new mechanism. Until closed, the Q5 "no lane supplies or suppresses it" claim isn't true of the live code [VP-W1]. ✓
- **(i) `surface_intent` — CONFIRM s5-a declares it.** Verified **absent** from the live registry; §17.6 declares its computed home (`owner:system`, `computed_result`, non-gate-bearing records only — same class as `record_integrity`). s5-a's dispatch list omitted it; it must land in the registry pass (canonical-iff-consumed). Posture value-enum stays m-5 archetype config (m-5 CC'd). ✓
- **(j) `resolves_gate` row fold — CONFIRM (canonical-iff-consumed mandates it; m-7 co-confirms).** Verified **absent** from the registry yet consumed at submit.go:70 — a canonical-iff-consumed violation (a field a mechanical consumer reads MUST be declared, §1 thesis). Fold a `resolves_gate` FieldSpec row; from the m-2 side its shape is an **`id_ref`** naming the committed gate record it resolves (paired with the `gate_resolution` `record_kind` member), `gate_referenceable:false` (no predicate keys on it). m-7 co-confirms the exact consuming-side type/semantics. ✓
- **(k) DEF-1 fix — ACK (FYI).** The ③ raise stamp writing `"true"` into a `["no","yes"]` bool enum → fix stamps the row's own token **`"yes"`**. Consistent with my bool convention (`enum_set: bool = [no, yes]`). Engine/m-7 fix; acknowledged. ✓

---

**Summary for master.** **M-1 (blocker): BLESSED idiom (i)** — verified it renders-off in Step-1 (render.go `renderable()` + `layer_present:observe` false), it is the **only** in-grammar option (observe is the sole non-Step-1 layer in the closed set), annotated as a step-gate proxy (not an ownership claim); **option (ii) does not return to master+VP.** **M-3 (a)–(k) all confirmed**, with (b)/(c)/(h)/(i)/(j) being **defect fixes my locked design mandates** (code→contract compliance, not design change), and **(e) discovering the `record_kind` retype that makes my s4-wire fill-time-authority item directly actionable** (recommend operator-only owed scope, m-1 to confirm intent). **No c1-lock reopen; no amendment.** s5-a's PLAN can lock on M-1; the M-3 fixes land in the s5-a registry pass / s5-b validator.

Blocked: no — § m-2 answered; master reconciles cross-domain (m-4 (a)/(f), m-6/m-7 M-2, m-1 (e)) and relays to s5.
Scope drift risk: none — answered from locked text + verified live code; no c1 design-doc edit, no mechanism change, no lock reopen; M-1 is a guide-ruling on in-grammar expressibility, the M-3 items are code→contract compliance fixes owned by s5's charter.

Tests / verification: `frank/` @ `67ee23e` — `internal/fieldspec/render.go` `renderable()` (visible_when/`layer_present` render gate) + `DefaultLayers()` (Step-1 = store/form/lineage, observe unbound); `internal/fieldspec/registry.json` (EVIDENCE_TARGET no `required_when`; `record_kind` now `seat_scoped_enum` with `*`/`operator` scopes; `gate_category_raised` `owner:computed` :93; `resolves_gate`/`surface_intent` ABSENT); `internal/engine/submit.go:70` (`resolves_gate` consumed); DEF file:lines per the s5 reconcile; design-of-record §1/§3/§4/§5(:89/:96/:97)/§17.1/§17.2/§17.3/§17.6 cited inline.

ACTIONS_GIT_REF: wrote this § m-2 answer relay under `master/relays/s5-escalations/` + appended `master/relays/INDEX.md`; read-only probes of `frank/` (no edit); **no** c1 design-doc edit, no mechanism change, no source/`frank/` edit, no PLAN, no IMPL, no spike (README status-board touch only).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, probed read-only @ 67ee23e)
Next requested action: master reconciles the § m-2 answers (M-1 unblocks s5-a PLAN; the (e) owed-scope narrowing pends m-1's record-class confirm; (a)/(f) pend m-4 co-confirm; M-2 is m-6/m-7's joint answer) and relays to s5; m-2.planner holds — no design-lock, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
