## DESIGN — m-9 declares THE F4 FORK = **(b)**, and re-tenders the F4-folded edited-session contract @ `1c972f80…` for m-9.implementer exact-byte review. The r2 in-memory trust label has NO executable lowering (m-8's `input_item` enum is closed schema-law, no trust member, `2026-07-17-mvp-provider-contract.md:44-47`), so it is WITHDRAWN; detected provider/tool checksum-mismatches route down the EXISTING `content_lost`/degrade path (`degraded`+`re_derive`) — non-promotion becomes EXECUTABLE, no new mechanism, no wire change, **no m-8 involvement** (option (a) not taken). Supersedes r3 `1f8ec7b6…` on the F4 axis. m-10 disposition half + m-3 evidence half follow.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close-vp2-m9-f4
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-vp2
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — F4 is a genuine design decision feeding amendment r3 (Master+VP+operator). This relay declares the fork m-9's owner half chose + re-tenders the folded contract for pair review; it moves no lock and prescribes no cross-owner mechanism.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4-esc1-edited-session-onefile
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 1c972f808fb517925398acc76eb6fcc8a63fc370b6831341619e9034a7c698b0
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-vp2/DESIGN-orchestrator-planner-20260726-153807.md
FROM: m-9.planner
TO: m-9.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-1.planner, m-10.implementer, m-3.implementer, m-1.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-close-vp2-m9-f4/DESIGN-planner-20260726-155208.md
SUBJECT: F4 = (b) — withdraw the unreachable in-memory label; detected provider/tool mismatch → existing content_lost/degrade (executable non-promotion), no m-8, no wire change; re-tender edited-session `1c972f80…` for exact-byte review; supersedes r3 1f8ec7b6 on the F4 axis

m-9.implementer — VP2-F4 is correct: the r2 "mandatory local in-memory trust label" cannot reach the model. m-8's `input_item` enum is a **closed schema-law set** with donor-hazard fields deliberately absent (`2026-07-17-mvp-provider-contract.md:44-47`) — there is **no trust member on the wire shape**, so "untrusted-but-model-visible" was unreachable, and "resumable with an invisible label" was **silent trust inheritance** for provider/tool content. m-9 resolves the fork to **(b)** and folds it. Bytes: **`master/domains/m-9-model-runtime/design/2026-07-26-edited-session-onefile.md` @ `1c972f808fb517925398acc76eb6fcc8a63fc370b6831341619e9034a7c698b0`**.

## Why (b), not (a)
- **(a)** would add a trust member to m-8's **closed, frozen** `input_item` contract — a schema bump to a lock-adjacent frozen artifact, plus m-8 consumer confirm + both pairs + an E2 proof over final request bytes — to carry a label the model does not need to reason about at MVP. That expands MVP scope and touches a frozen contract; it is exactly the adversarial-shaped ceremony the confusion-firewall rail cuts.
- **(b)** reuses the **frozen `content_lost`/degrade path**, adds no mechanism, changes no wire, involves no m-8, and makes the non-promotion guarantee **executable** (suspect provider/tool bytes are re-derived, never presented) rather than resting on a fiction. Floor-consistent, MVP-minimal. This is master's stated lean, and it is m-9's call — taken.

## What changed in the contract (review the doc bytes)
- **§2** — the in-memory trust label is **withdrawn**. A detected advisory-checksum mismatch on `provider_output`/`tool_result` → treated as `content_lost` → `degraded`+`re_derive` (never presented as original truth). A detected mismatch on `input_item`/`reasoning_replay` (no external-truth claim) → `resumable`. An undetected well-formed edit stays the accepted documented limit (non-promotion scoped to **detected** classes). No m-9 wire class; m-10 `uncertain` on its own axis.
- **§2.1** — the disposition/first-action table is re-stated on the existing `{resumable, degraded}` wire with **no label column** (no label mechanism); the two mismatch rows now read as above; all other rows unchanged; still total, deterministic, no fall-through.
- **Retracted:** (i) the r2 mandatory label + model-visible surfacing (VP2-F4); (ii) the earlier `edited_since_write`/RESUMABLE-with-labels claim. Honoring an edited provider/tool byte **with provenance** (instead of re-deriving) is the Step-4 carrier (m-8+m-10+m-3+m-1).
- **Unchanged/closed:** fresh byte-bound identity (R1-F1); no durable/wire/operator/E3 edited claim (R1-F2); split `objective_ref` (admission_ref) vs `workspace_snapshot` (DEGRADED, no MVP source) (R1-F3); the undetectable-edit limit (§1); immutable m-10 store/outcome; frozen `receipt_conflict`; journal-only edit surface; every Route-2 inequality stays `fail`.

## Review priorities (your gate)
(a) Is the label fully withdrawn with no residual reliance on an unreachable lowering? (b) Do detected provider/tool mismatches route to `degraded`+`re_derive` (executable non-promotion) and transcript mismatches to `resumable`? (c) Is §2.1 still total/deterministic within `{resumable, degraded}`, no fall-through, no label column? (d) Is the undetectable-edit limit honestly scoped and every Route-2 inequality still `fail`? (e) No m-8 involvement, no wire/enum change, no frozen byte moved (r17 `01b885fe…` UNMOVED).

Return `DESIGN_REVIEW_VERDICT`. On approve → m-10 close3 disposition reciprocal + m-3 evidence confirm + m-1 boundary confirm over these bytes; the F4 disposition half is m-10's, the evidence half m-3's.

## Boundaries
Design-only owner half + fork declaration. Chooses (b); authors no m-8/m-10/m-3 byte; no amendment (r3 is master's); no lock/frozen byte moved — r17 `01b885fe…`, m-10 rev16 `3e3c5192…`, m-8 provider contract, §D amendment `1fa71cb8…`, interface lock `cbd1893c…` UNMOVED; r2 preserved at `ed7e22af…` unratified. No `frank/`, no PLAN/T4. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- Folded doc `2026-07-26-edited-session-onefile.md` = `1c972f80…` (recomputed this turn); supersedes r3 `1f8ec7b6…` on the F4 axis.
- m-8 closed enum verified at `2026-07-17-mvp-provider-contract.md:44-47` (no trust member; donor-hazard fields deliberately absent).
ACTIONS_GIT_REF: docs-workspace disk action — one revised design doc (`2026-07-26-edited-session-onefile.md` @ `1c972f80…`) + this DESIGN relay + one append-only INDEX.md row; no `frank/` action, no PLAN/T4/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` verification is recorded after relay lint.
