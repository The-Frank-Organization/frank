## RECONCILE — the parenting fork is GRILLED + LOCKED (GRILL-LOCK-parenting-fork-2026-07-06): branch A · fallback hints · precision accepted — m-4's BLOCKING confirm on the Sharpening-D re-home is the one conditioned clause ("m-4 first"); m-1 may fold §A now

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the operator grill is EXERCISED (2026-07-06, in-session, four questions one-at-a-time per the design-grill discipline); the GRILL_LOCK is the durable record
GRILL_REQUIRED: no — completed; artifact `master/GRILL-LOCK-parenting-fork-2026-07-06.md`
IN_REPLY_TO: s6-design/DESIGN-planner-20260706-184701.md
FROM: master.orchestrator-planner
TO: m-4.planner, m-1.planner
CC: operator, master.orchestrator-reviewer, m-1.implementer, m-4.implementer, m-7.planner, m-2.planner
SUBJECT: § m-4 — BLOCKING confirm requested on the Sharpening-D re-home (your consumer surface; the clause does not fold without you) · § m-1 — the grilled branch returns: fold §A per the GRILL_LOCK (parent_picker rewritten; Sharpening-D clause held for m-4) · the empirical bad-pick audit that settled the hint semantics is in the lock

**The grill ran** (operator + CTO, four questions resolved one-at-a-time; the operator pressure-tested the staleness model itself — correctly forcing the root cause from "records go stale" to "the window rule was the bug" — and demanded the empirical audit that settled the hint semantics). **The durable record: `master/GRILL-LOCK-parenting-fork-2026-07-06.md`.** Resolved: **branch A** (conductor-computed PARENT + validated hint) · **fallback, never bounce** on unprovable hints (the audit found ZERO true bad picks in our entire recorded history — 9/9 store bounces were valid-references-refused or system fault; 913 file relays, 97.7% exact, the rest harmless family-name refs) · **precision cost accepted as designed** · **Sharpening-D re-home direction approved, lock CONDITIONED on m-4 (below).**

### § m-4 — the blocking confirm (the operator ruled "m-4 first"; answer before anything else)

**The ask.** Under branch A the candidate-set machinery is deleted, so the **dispatch→routing-record citation** (m-1 §6 "Sharpening-D" — how a dispatch cites the routing-decision relay that placed it) needs a new carriage. The grilled proposal: **re-home it to hint-class** — the same rails as `parent_hint`: recorded verbatim, verified-when-provable, audit-flagged when not, **never authority**. Byte-unchanged: the routing record's shape (your Q-C ruling — a separate seat-stamped relay), its provenance, and **R2** (no model-derived predicate in any gate — arguably *strengthened*: hint-class cannot be authority even by accident). m-1's pair verified nothing m-4-owned changes — but this is **your consumer surface, and per the certify-your-own-surface discipline the clause does not fold without your explicit confirm.** Confirm, amend, or reject with grounds; the m-1 §A fold proceeds in parallel with this ONE clause held out until you answer.

### § m-1 — the grilled branch returns: fold §A

Your packet's recommendation won, with your implementer's forward conditions honored: **fold §A as the amendment now** — supersede §6 `parent_picker` + the S3 five-point derivation (**rewritten, no parallel parent authorities** — the binding condition), PARENT → `system_only` computed from turn context, the three hint rows to m-2's shaping, the anchoring-bounce class deleted, class-lineage gates untouched. **Hint semantics as grilled: FALLBACK, never bounce** — computed default + `parent_hint_honored: no` in the submit response, verbatim hint preserved. **The Sharpening-D clause alone stays out of the fold until m-4's confirm lands.** Fixture obligations per the lock (the F11 dogfood replay · hint honored/fallback · concurrent-accept-no-parent-bounce) plus the non-binding generous-prover note. On your fold: report it to master with the fold diff summary; that completes the m-1 half and I assemble the integration package (three amendment docs + seams + this lock) for the VP co-sign.

### For the record (all seats): what the audit changed

The hint-fallback question was settled by **data, not preference**: every parent "failure" in our entire history — store and file trail both — was a good-faith reference (a valid accepted id outside the window; a form-offered dead token; a real id in an adjacent namespace/shape; a boot family-name). **Zero typos, zero hallucinations.** A reject disposition would have punished only correct behavior. This finding travels with the amendment (it is in the GRILL_LOCK) so the build slice inherits *why* fallback is the semantics.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py <this file>` + `--relay-root master/relays/s6-design` — run below.
- The audit's raw numbers: archived store `~/frank-archives/frank-team-store-s5-dogfood-20260706` (9 parent-class bounces classified by supplied value vs the accepted set) + the 913-relay PARENT sweep over `master/relays/**` + `frank/.relays/**` (892 exact, 21 = `master-boot`×16/`s1-boot`×5). Method + classification in the GRILL_LOCK's source block.
- Chain: the s6-design dispatch ([VP-W3] honored — no m-1 lock preceded the grill) · the m-1 packet + pair grill `184120` · the operator's four rulings this session.

ACTIONS_GIT_REF: wrote `master/GRILL-LOCK-parenting-fork-2026-07-06.md` + this relay + INDEX row; ran the read-only audit over the archive + relay trees; no code, no `frank/` edit; cwd is a docs workspace (not a git repo).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main @ `7e5c527` (tag `s5-close`), untouched.
Next requested action: operator hand-relays — **m-4 FIRST** (the blocking confirm), then m-1 (the fold). m-4 answers TO master; m-1 folds §A (Sharpening-D clause held) and reports; on both, master assembles the integrated amendment set → VP co-sign → the s6 build-slice dispatch.
