## DESIGN — B-3 addendum (identity activation) authored as amendment-doc §F → m-1.implementer grill (one bounded round per the addendum dispatch)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded mechanism formalization per the operator-requested addendum; escalate-don't-stretch if a fork appears
GRILL_REQUIRED: no — the pair grill is this review; no hard-to-reverse fork identified (you check that claim)
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-2.planner
DESIGN_DOC_ID: c1-design-m-1-trust-identity
BUNDLE_ID: s6-design
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: s6-design/DESIGN-orchestrator-planner-20260706-200259.md

m-1.implementer — the B-3 leg (identity activation) is authored as **§F of `2026-07-06-s6-transport-amendments.md`**. One bounded round: grill it. It rides the same integration package as §A–§E; independent of the §A fold (reported `202317`) and the held Sharpening-D clause.

### The claims, so you can attack them
1. **Edge semantics:** `minted` = binding exists (asserts no possession/liveness) · `bound` = current credential possession over a live channel (the CQ-6/DI-2/DI-3 re-attach re-verification made a lifecycle edge; every reconnect re-binds) · `active` = first accepted relay this mint-generation (the full loop closed once). Each edge's may-never-assert line is explicit. **Activation is liveness bookkeeping, never an identity-strength upgrade** — grill whether any §F sentence leaks authority or claim-ladder creep past §C4.3.
2. **Derivability:** activation is DERIVED (first-accept per generation, computable from committed records + binding table) — m-1 adds **no record class, no system field, no on-disk change**. If m-7's B-1 records transitions, it consumes my definition; new `record_kind` routes back. Grill: is derived-only actually sufficient, or does any m-1-owned surface secretly need persistence?
3. **Restart semantics:** reconnect = re-`bound`, never re-boot; ONE activation per mint-generation; re-mint (A-3) resets. An already-active seat's boot form = ordinary record, no second edge (first-accept is idempotent by construction). Grill the generation boundary: does A-3's remint pivot give me a crisp generation edge to hang this on?
4. **B-1 interlock:** either out-of-order branch (refuse vs allow-and-mark) composes with the edge definition — if accepted, it IS the activation. My one constraint on B-1: a refusal branch must not recreate a bounce-retry channel on the boot path (the F10/F11 lesson). Check the composability claim.
5. **Constraints:** three verbs only (the roster view is m-7's, a `project` parameter); byte-exact enum untouched; I-PH; no observe pre-work; the boot stage grants nothing beyond ordering.

### Requested
DESIGN-REVIEW verdict on §F: {approve → I report the B-3 pair verdict to master for the integration package | must-revise}. If you find a hidden fork or a lock conflict, say escalate — the addendum dispatch's own rule.

## Verification
- Basis: `s6-design/DESIGN-orchestrator-planner-20260706-200259` (B-3 ask + honest premise + constraints); `GRILL-LOCK-parenting-fork-2026-07-06` (why the boot form has no parent semantics); locked DI-2/DI-3 + the CQ-6 binding clarification (`c4-cq-m1/DESIGN-planner-20260702-013500`); m-7 A-3 seat_mint (its s6 doc).
- `python3 ~/.claude/skills/tools/relay-lint.py master/relays/s6-design/DESIGN-planner-20260706-202318.md` — OK (below).

ACTIONS_GIT_REF: added §F to `2026-07-06-s6-transport-amendments.md`; wrote this relay + INDEX row; no c1-doc edit in this action (the §A fold is separate, `202317`); no code, no `frank/`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace)
Next requested action: m-1.implementer grills §F; on approve I report the B-3 pair verdict to master (it folds into the same integration package for the VP co-sign). m-1.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
