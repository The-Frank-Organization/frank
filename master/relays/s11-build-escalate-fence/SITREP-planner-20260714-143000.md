## SITREP — s11.planner ESCALATES F1 (fence contradiction, cross-domain): T8's cleanup card requires the executor/read-file soft-expiry loci (m-7 substrate + m-3 observe worker) that the r1 fence excluded or g2-mislicensed; requesting master's exact seam disposition (or explicit rescope) before the r2 PLAN reissues — no token issued

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s11-build-escalate-fence
PARENT_DISPATCH_ID: s11-build-plan-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a fence/seam disposition request to master; merge remains operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s11-comms-thicken-plan-s11.1
IN_REPLY_TO: master/relays/s11-build-plan/PLAN-REVIEW-implementer-20260714-142646.md
FROM: s11.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-7.planner, m-3.planner, m-6.planner, s11.implementer
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: my r1 PLAN's fence made un-gated T8 impossible — the locked nine-item cleanup card's "shared soft-expiry arbiter (executor/read-file)" + "finalizeRun preserve-flag ownership" live in `executor.go` (m-7) and `observe/fs_worker.go` (m-3), while my fence licensed `executor.go` only for gated T5 and put `observe/` OUT; per conditions (d)/(f) and the review's required revision I route the exact loci to you and HOLD — the r2 PLAN folds only what you rule

## SITREP — s11.planner / m-6-human-surface-scheduler
Phase: PLAN loop paused on must-revise r1; this escalation is the unblock path.
Current artifact: `s11-build-plan/PLAN-REVIEW-implementer-20260714-142646` — `PLAN_REVIEW_VERDICT: must-revise`, single blocker F1, mechanical result `deviation-present`; no token issued (review condition honored).

Claims:
- **F1 verified at the bytes, not accepted on report** — evidence E1 — source: read this session at `d91fcfb`: `frank/internal/executor/executor.go` ~`:190-292` owns the executor soft-expiry arbitration (the `OnSoftExpiry` select + timer path) and `:295-323` owns `finalizeRun` + its preserve return; `frank/internal/observe/fs_worker.go:107-160` owns the parallel read-file soft-expiry path (`resolveFSExpiry`, `ExpiryExtend`). The s10 carry names the boundary explicitly: "shared soft-expiry arbiter (executor/read-file)" (`s10-build-exit/SITREP-planner-20260713-125905`).
- **The contradiction is mine, not the plan-of-record's** — evidence E1 — my r1 fence (`s11-build-plan/PLAN-planner-20260714-141100` §3) licensed `executor.go` only after g2 and only for T5's fork seam, and excluded `internal/observe/` entirely; T8 is un-gated and its two items are inside the locked nine-item card (`2026-07-14-s11-comms-thicken-plan.md` T8). Nothing except T5 may wait on g2.
- **Cross-domain, so not mine to fold in-fence** — evidence E1 — the loci sit on m-7's executor substrate (the s9/s10 trail treats `executor.go` as master-ruled at named seams; s9 carried "NO licensed seam, any diff STOPS") and m-3's observe worker (s9's T1 landed `fs_worker.go`). Condition (d) routes this to you; the review's required-revision step 1 says route, do not infer.

Actions:
- wrote this escalation relay + one INDEX.md row — ACTIONS_GIT_REF below; no `frank/` edit, no branch, no code, no token.

FINAL_GIT_STATUS_SHORT:
unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `d91fcfb` (`s9-close`).

Done: r1 PLAN authored + adversarially reviewed; adoption/hash/lineage/gate handling passed (g1→T6 §B consumption confirmed by the reviewer); F1 verified independently at the source.
Not done: the r2 PLAN reissue (waits on your disposition); the token (waits on an approve + all-in r2 review).
Blocked: T8's fence rows — on your ruling. Everything else in the r1 fence stands as reviewed.
Scope drift risk: none beyond F1 — the request below adds no task and no mechanism; it seeks seam licenses for work already inside the locked cleanup card.

**The disposition I request (rule any of these; I fold exactly what returns):**
1. **A T8-named `executor.go` seam independent of g2** — the soft-expiry arbitration loci (~`:190-292`) + `finalizeRun`/preserve (`:295-323`) for the behavior-preserving refactor only, with `Spawn` refusal and every other executor surface byte-untouched, and T8's seam kept distinct from T5's fork seam if g2 later opens (the same-file two-task ordering recorded in the diff→license table).
2. **A T8-named read-file seam** — `observe/fs_worker.go` `resolveFSExpiry`/soft-expiry loci only (m-3's domain; their fidelity confirm at exit), OR an m-3-ruled alternative locus if they place the shared arbiter elsewhere.
3. **Alternatively, an explicit rescope** deferring cleanup items 2 and/or 8 out of T8's nine with the acceptance criterion amended by you — the pair will not silently ship fewer than nine while claiming T8 complete.

Tests / verification: `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s11-build-escalate-fence/SITREP-planner-20260714-143000.md` run before handoff; source proof read live at `d91fcfb` this session (`executor.go:190-323`; `fs_worker.go:107-160`).
Next requested action: master rules the T8 executor/read-file seam disposition (coordinating m-7/m-3 as you see fit); on the return I fold the exact ruled loci into §3 at task grain, re-run the mechanical scope diff, and reissue the bounded r2 PLAN to s11.implementer for re-review. No token before an all-in r2 approve.

ACTIONS_GIT_REF: none — report/escalation only; artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260714-143000.
