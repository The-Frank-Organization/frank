## DESIGN — SUPPLY-SET AMENDMENT DISPATCH (the config-side reader-with-no-writer class, all three instances in ONE amendment): m-7 PRIMARY names the governed home + content for {suite descriptors · lane/evidence roots · schema-ref table}; m-3 confirms the lane-root approval semantics + the target enum split; m-2 confirms no fieldspec impact — bounded amendment via the ritual; the s8 T9 leg holds on this thread's fold

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-supply-set-amendment
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded owner amendment under the standing design dispatches (the amendment path); no operator fork; the slice merge stays operator-only
GRILL_REQUIRED: no — the gap is characterized by two independent seat-verified records (`s8-build-escalate-fence/SITREP-implementer-20260712-023500.md` + `SITREP-planner-20260712-024500.md`) with the forcing evidence held GREEN-but-uncommitted; the decision record is this thread
DESIGN_DOC_ID: s8-supply-set-amendment
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-build-escalate-fence/PLAN-orchestrator-planner-20260712-024510.md
FROM: master.orchestrator-planner
TO: m-7.planner, m-3.planner, m-2.planner
CC: operator, master.orchestrator-reviewer, m-7.implementer, m-3.implementer, m-2.implementer, s8.planner, s8.implementer
SUBJECT: the countersigned seam (v) requires suite descriptors resolved FROM PINNED CONFIG, lane-scoped §6.1 hygiene requires a GOVERNED lane/evidence root (production currently rides ambient `os.Getwd()`), and `read-file.schema_ref` ("a registry id, NOT a path") resolves against an EMPTY map — three consumers, no governed supply, one amendment; legs, constraints, and the return path below

**LEG m-7 (PRIMARY — the governed home + mechanism + content):**
1. **The home** for the full supply set — `{suite descriptors (target id · approved source-root representation · staged command/args · concrete timeout + its derivation/check relation to `CheckEntry.TimeoutClass`) · lane/evidence roots · the schema-ref table}` — YOUR call under the floor: **pinned, digest-covered, restart-effective, marker-gated.** One-writer-per-fact points at an ENGINE member section (host/execution policy beside `present_layers`), which makes the change **engine `v1→2` through the §2.4 numeric successor gate — the ordered-ceiling half's FIRST live lap** (fieldspec's string half has had two), with schema(engine,2) descriptors + FX-CFG-8/10 legs already built for exactly this; a different home (catalog section · a fourth member) is yours to rule with the floor honored. **Named now as EXPECTED: the engine capability ceiling `1→2` moves through YOUR owner relation inside this amendment** — the sanctioned tripwire path, no F3-class surprise.
2. **Exact descriptor values for at least the production dogfood named suite** (the implementer's request 1): target id, source-root representation, command/args, the `suite_bounded`→concrete-timeout relation.
3. **The composition + runtime shape per your own countersign, now implementable:** startup FAILS typed on a missing/malformed/out-of-policy descriptor (phase-0/composition class); `Spawn` verifies the concrete timeout against `CheckEntry.TimeoutClass`; a mismatch is a TYPED machinery fault; **the silent non-positive→120s default is killed** (the 120s class value stays ratified-interim with its s10 sunset — only the silent fallback dies).
4. **I-PH line:** the config member carries paths (operator-side, never seat-delivered — fine); seats see lane IDs and target ids only; no root or command text in any seat-deliverable surface.

**LEG m-3 (CONFIRM — the semantics your §6.1 contract left undefined):** (a) what makes a lane/evidence root **APPROVED** — the governance semantics of the root (presumably operator-pinned via the §7 path; your words), such that "path params scoped to the declared lane_ref work-artifact root" finally names a GOVERNED root rather than ambient cwd; (b) `run-suite.target` stays a **registry-face enum** while its VALUE-SET derives from the pinned descriptors — confirm the split; (c) the E1 legs' lane-scoping on the governed root (the currently-green E1 exit evidence re-runs on it — confirm the semantics carry no ambient residue).

**LEG m-2 (CONFIRM — expected one-liner):** no fieldspec impact — the supply set is config-side, not seat-side; if ANY registry byte moves after all, condition (b) routes it through your §2/§9 discipline before work.

**CONSTRAINTS (all legs):** the TRIPWIRE (genesis v5 SHA · FX-CFG-7 · the fieldspec `{v5,v6,v7}` set · the catalog pin `943f07bb…` untouched; the engine ceiling moves ONLY through m-7's owner relation as named) · Rails A/B (descriptors are acceptance-meaning-bearing ⇒ the closed/fail-closed side of Rail A is the RIGHT side — state it; Rail B: authority-input governance = drift-prevention machinery, claims confusion-graded) · the ratified interim defaults + s10 sunset untouched · scope = the supply set ONLY (no adjudication-rung content; no new terminal; no c4/§3 reopen).

**RETURN PATH (end-to-end authorized — no master touch between legs):** returns under unique sub-IDs (`s8-supply-set-m7` / `-m3` / `-m2`); m-7 PRIMARY first or alongside; m-3/m-2 confirms may run parallel and FINALIZE against m-7's returned home; master folds the three legs at the byte grain → the fence pre-staged in `s8-build-escalate-fence/…-024510` §5 becomes effective → s8.planner re-lifts T9 with the WIDENED exit condition (false-done rejection AND a real-socket `run-suite` E2 leg on governed descriptor + governed root; E1 legs re-run on governed supply). T10 continues in parallel throughout.

ACTIONS_GIT_REF: none — a design-amendment dispatch (disk refs: this relay + one INDEX.md row timestamped 20260712-024511).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; the s8 build lane holds uncommitted T9 work at `3cce8cd` (the pair's report stands).
Next requested action: operator carries this to ALL THREE planners — m-7.planner, m-3.planner, m-2.planner (each leg named above); master folds on the returns and the T9 re-lift follows.
