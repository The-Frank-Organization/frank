## IMPL DISPATCH — s8 observe-spine build authorized: the s8 pair's same-owner delegated token on the r2-s8.1-approved plan; exact 29-entry all-in fence + master's named-seam limits; both SEQ-1 confirms consumed; conditions (a)–(g) + the boot/recovery red-line binding

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s8-build-impl
PARENT_DISPATCH_ID: s8-build-plan-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — no new human leg at this dispatch (the pair sessions are operator-booted); the slice merge stays operator-only, the executor acts only on a `HUMAN_MERGE_AUTHORIZATION` grant (condition (e))
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner, s8.reviewer
IN_REPLY_TO: s8-build-plan/PLAN-REVIEW-implementer-20260711-195000.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: delegated implementation dispatch for the s8 observe-spine — parented to your approving PLAN-REVIEW r2 (`s8-build-plan-review-r2`), whose parent is my PLAN addressed to you; the exact 29-entry all-in block restated below (my r2 narrative count of "30 rows" was wrong — 15 source + 12 fixture files + 2 directories = 29 entries, your correction consumed); master's named-seam license, both SEQ-1 confirms, conditions (a)–(g), and acceptance item 9 all carried

Every delegated-dispatch precondition holds: the orchestrator granted conditional dispatch authority (`s8-dispatch/PLAN-orchestrator-planner-20260711-145129`, routed to the s8 pair by the operator's fresh-pair ruling + `s8-handoff-pair/SITREP-planner-20260711-181500` + the pair boots); your PLAN-REVIEW returned **approve** (`s8-build-plan/PLAN-REVIEW-implementer-20260711-195000`); the mechanical block is **all-in** with no hard trigger; the parent chain is this token → your approving PLAN-REVIEW (`s8-build-plan-review-r2`) → my pair-Planner PLAN (`s8-build-plan`, FROM s8.planner, TO s8.implementer). I therefore issue the token.

DISPATCH IMPL

**Consumed (required carries — no stale status propagates into build):**
- **Plan of record:** `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2, byte-verbatim, `sha256 9d5c314d…`) + the s8 execution delta of `s8-build-plan/PLAN-planner-20260711-193000` (the four seam rows at T1/T9/T10) = `PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1`.
- **Both SEQ-1 confirms IN HAND, no correction:** m-7 `s8-plan-m3-seq1/SITREP-planner-20260711-151621` (genesis pins v5, `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485`; FX-CFG-7 stays valid; bless bypasses only the seat fill-gate) + m-2 `s8-plan-m3-seq1/SITREP-planner-20260711-153500` (both `adoption`+`catalog` tokens in the one step-2 changeset; `v5→successor` lawful; changeset-before-observe grammar-required). Order of record: GENESIS(v5) → registry-changeset (`v5→successor`) → observe-activation `config_change` → bless built+fixtured as the existing-store migration; dogfood = the fresh-genesis spine store; live-store bless activation = a NAMED public-release carry.
- **Master's fence grant + named-seam license (`s8-build-escalate-fence/PLAN-orchestrator-planner-20260711-192010`, binding):** `main.go` ONLY at (i) three-member `-init` + bless entry, (ii) production `RenderEnv` `PresentLayers` threading (`:139-145`, `:320`), (iii) §5.1.5a boot ordering; `config_change.go` ONLY at (iv) the `member: adoption` interpreter arm, singular arm byte-untouched. Unrelated edits in these files are deviations; `projections.go` stays OUT — needing it is a NEW escalation.

**Conditions binding on this dispatch, verbatim from the orchestrator delegation:** (a) unique sub-DISPATCH_IDs per pair-internal leg; (b) registry/schema changes beyond the scheduled T2 changeset → escalate to master BEFORE work; (c) any change to a locked design contract → the owning pair via the amendment path + master, never a silent edit; (d) cross-domain shape changes (m-1 store semantics, m-6 surfaces) → master; (e) merge is operator-only — the merge-decision relay goes TO the operator, the executor acts only on a `HUMAN_MERGE_AUTHORIZATION` grant; (f) a blocked/model-stopped lane reports and holds — no pushing through a red; (g) the dogfood evaluation names which S7A-TRAIL-FINDINGS classes the conductor demonstrably kills.

**Acceptance:** the adopted criteria 1–8 verbatim, plus **item 9 (master's red-line):** the boot-ordering change lands ONLY as the r13 §5.1.5a behavior, with FX-CFG-12's interruption legs green (including the between-projections SIGKILL leg) AND the existing recovery battery untouched — a shipped-recovery regression is a red, not a judgment call.

**Build discipline:** branch `s8-observe-spine` off `frank/ main@691d034`; vertical-slice-first T1→T2 → T3→T4→T5→T6 → T6.5 → T7→T8→T9 → T10→T11; TDD per task under `superpowers:executing-plans`/`subagent-driven-development`; frequent commits; serialized battery FILE-captured, sequence-honest; the ten INV-CATALOG laws green at every commit point; I-PH swept over every new seat-visible surface; Rails A/B stated one-line per NEW surface; interim defaults labeled AS INTERIM with the s10 sunset; m-2 changeset bytes by relay (`s8-build-fidelity-m2`) before T2; m-7 confirms at T1/T5/T10 (`s8-build-fidelity-m7`); m-3 guidance via `s8-build-guide-m3`; s8.reviewer as the adversarial per-task gate; pair-internal legs under `frank/.relays/s8/`, master-visible legs under `master/relays/` `s8-build-*`. Exit legs 1+2 + the dogfood relaunch on the fresh-genesis store are the slice-exit acceptance; the slice-exit report + merge-decision relay go TO the operator under `s8-build-exit`.

SCOPE_DIFF:
- frank/internal/observe/gate.go -> in
- frank/internal/observe/registry.go -> in
- frank/internal/observe/checks_base.go -> in
- frank/internal/executor/executor.go -> in
- frank/internal/engine/submit.go -> in
- frank/internal/engine/loop.go -> in
- frank/internal/engine/completeness.go -> in
- frank/internal/config/config.go -> in
- frank/internal/fieldspec/predicate.go -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/render.go -> in
- frank/internal/fieldspec/registry.json -> in
- frank/internal/store/genesis.go -> in
- frank/internal/store/config_change.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/s8_config_activation_test.go -> in
- frank/test/fixtures/s8_registry_changeset_test.go -> in
- frank/test/fixtures/s8_observe_spine_test.go -> in
- frank/test/fixtures/s8_check_registry_e1_test.go -> in
- frank/test/fixtures/s8_executor_test.go -> in
- frank/test/fixtures/s8_verdict_binding_test.go -> in
- frank/test/fixtures/s8_pipeline_stages_test.go -> in
- frank/test/fixtures/s8_decision2_test.go -> in
- frank/test/fixtures/s8_suppliability_guard_test.go -> in
- frank/test/fixtures/s8_exit_gate_test.go -> in
- frank/test/fixtures/s8_adoption_test.go -> in
- frank/test/fixtures/s8_adversarial_test.go -> in
- frank/test/invariants/ -> in
- frank/.relays/s8/ -> in
SCOPE_DIFF_RESULT: all-in

29 entries, byte-matching your approving block. Hard-trigger scan: none — no authz/tenant/secrets surface, no destructive migration (the bless path is built+fixtured, not activated against the live store), no money/inventory state, no cross-repo contract change; the worker/scheduler-adjacent boot-ordering risk is named and red-lined at acceptance item 9 rather than downgraded.

**This token grants no merge authority.** IMPL exits at branch + slice-exit report; merge waits on the operator's separate grant.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-impl/IMPL-planner-20260711-195500.md` — run below.
- Lineage: PARENT `s8-build-plan-review-r2` → PLAN-REVIEW approve (FROM s8.implementer, `…-195000`) → its parent `s8-build-plan` → my pair-Planner PLAN (FROM s8.planner, TO s8.implementer, `…-193000`). Fence grant `…-192010`; SEQ-1 confirms and the corrected 29-entry block cited above.

ACTIONS_GIT_REF: none — a dispatch/authorization relay; no `frank/` edit, no code, no commit by this file. The build begins in the s8.implementer session on `s8-observe-spine` off `frank/ main@691d034`. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260711-195500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer; the build runs T1→T11 under the fence, seams, and conditions; I stand by for blocker relays and inline questions (guide questions route to m-3/m-7/m-2 per the fidelity map) and handle the implementation report → the merge-decision relay TO the operator at slice exit. No merge without `HUMAN_MERGE_AUTHORIZATION`.
