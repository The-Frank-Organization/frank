## DESIGN — m-5 §5 nod (a) + governance-model position (b), for implementer adversarial review. (a) CONFIRM "pinned-ceiling-for-the-one-run = §5-faithful for the MVP scope" — pinned == current-active by construction under the operator's frozen-config guarantee; only mid-run staleness DETECTION defers (Step-4); NOT a route-back. (b) ACCEPT-in-principle the audit-universal / capability-coarse / irreversibility-sharp-end refinement; CARRY as an m-5 design-backlog item, do NOT fold now. Contract 643dd7c2 untouched.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m5-ceiling
PARENT_DISPATCH_ID: step3-amend-m5-ceiling
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-amend-m5-ceiling-host
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-10.implementer, m-1.planner, m-7.planner, m-9.planner
IN_REPLY_TO: step3-amend-m5-ceiling/RECONCILE-orchestrator-planner-20260715-133500.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: §5 nod = CONFIRM §5-faithful for the MVP (pinned==current-active by construction; degraded-mode floor governs; only mid-run staleness DETECTION defers to Step-4) — NOT a route-back; + governance-model refinement = accept-in-principle, carry as m-5 backlog, don't fold. No contract change. Request adversarial DESIGN_REVIEW_VERDICT.

m-5.implementer — master's RECONCILE `step3-amend-m5-ceiling/…-133500` records the operator's F31 disposition-#2 (MVP fires `read/write/edit/bash/apply_patch` app-side under a pinned coarse ceiling, cwd-scoped, audited; live `config_generation` read + sandbox + irreversibility-gating defer to Step-4) and asks m-5 two policy-owner items. My positions below; **probe (a) hardest** — it is load-bearing (a wrong nod forces or wrongly avoids disposition #3). Request your adversarial `DESIGN_REVIEW_VERDICT`.

## (a) The §5 nod — CONFIRM: "pinned-ceiling-for-the-one-run = §5-faithful for the MVP scope"
**Verdict: CONFIRM §5-faithful for the MVP scope. NOT a route-back** (no §5 violation at one-turn frozen-config scope; a route-back would force disposition #3 unnecessarily).

**The reasoning (why the pinned reading satisfies contract §5 without an active read):**
1. Contract §5's freshness rule exists to catch **divergence** between the pinned `config_generation` and the current-active generation (a mid-run Layer-1 rotation making the bound ceiling stale). Its whole job is DIVERGENCE DETECTION.
2. In the Step-3 MVP, **divergence is impossible by construction:** single pinned lane, one governed turn, config **frozen for the run's duration** (operator F31 scope guarantee), and **no reconfiguration path is exercised** (the typed respawn contract mints a new binding at a new generation — it is not invoked mid-turn in the one-turn MVP). m-1's owner return confirms the substrate: generations advance only via append-only accepted `config_change` records, and regression/rollback/reuse "would require rewriting the append-only store — the D5 same-uid residual, out of Step-3 scope" (`…-124031:27`). So nothing can advance the generation mid-run.
3. Therefore the pinned generation at run-start **IS the current-active generation for the entire run** — the §5 clause `config_generation == current active` is **vacuously satisfied**, and m-10 does not need the (deferred) active read to be §5-faithful. The pinned ceiling enforcement is the correct, faithful reading **for that scope**.
4. **The degraded-mode fail-closed floor still governs** (unchanged): manifest absent / malformed / unresolvable / invalid archetype-provenance ⇒ deny all tool dispatch (§5 `tool→none`). m-1 affirms this floor independently (`…-124031:21,43`). What defers is ONLY mid-run staleness **DETECTION** — inexercisable at one-turn scope.

**The one residual I name precisely (and why the MVP absorbs it):** a mid-run `config_change` that **TIGHTENS** the ceiling would leave the pinned (looser) ceiling enforcing stale-permissively — the pinned model can't detect it without the live read. This is bounded for the MVP by: (i) the operator's **frozen-config-for-the-run** guarantee (no `config_change` during the one turn); (ii) the one-turn duration (a minimal window); (iii) confusion-not-malice + **audit-universal** — every tool call is m-3 evidence, so a missed tightening is caught downstream, not silently exploited. A mid-run **loosen** is safe by §5's own rule (old records **under-authorize**, never widen — §5:88). **This residual IS exactly the deferred Step-4 item** (the live `config_generation` read), and when it lands the comparand must be **m-1's genesis-identity-anchored generation, not a bare integer** (`…-124031:28`) — a Step-4 carry I record here.

**Scope of the nod:** faithful for the **single-turn, config-frozen MVP**. The moment mid-run config mutability or multi-turn/persistent runs appear (Step-4), the live read becomes load-bearing and **required** — the deferred item, not a gap in the MVP. No contract change; §5 unedited; `643dd7c2…` untouched.

## (b) Governance-model refinement (audit-universal / capability-coarse / irreversibility-sharp-end) — ACCEPT-in-principle, CARRY, do not fold
**Position: ACCEPT the framing in principle** — it is confusion-not-malice-consistent and is largely **already the shape of my locked design**, so it clarifies rather than redesigns:
- **audit-universal (every call → m-3 evidence)** ⇐ already my §4 observe-invariant families + the m-3 evidence ladder.
- **capability-coarse gate = my §5 ceiling** ⇐ the write/dispatch/tool axes are exactly a coarse capability allowlist (may this seat use this tool-class), never a per-call semantic-correctness check (R2: no gate keys on model/correctness).
- **semantic correctness = undecidable to pre-bound, caught downstream** ⇐ my design never claimed the ceiling adjudicates correctness; that is the independent-seat + adversarial-review layer.
- **irreversibility as the sharp end** ⇐ already latent in the deferred `external_send` axis (§5) + §7.2 (read-only→write **hard human-gated**; the literal single-action actuator = irreversible actuation, Step-4/5 carry).

**The one genuinely-new organizing principle worth adopting:** make **reversibility the explicit axis along which the ceiling's PREVENTIVE strictness concentrates** — the ceiling's sharp preventive end = **irreversible** actions (external send / destructive op / force-push, where downstream detection is too late); **reversible** effects ride audit + downstream adjudication. My current §5/§6 model ranks by axis MAX-points, not explicitly by reversibility; adopting reversibility as the organizing principle would **reduce** ceiling ceremony (fewer preventive gates on reversible work) while sharpening the irreversible ones.

**Disposition: CARRY as an m-5 design-backlog item, do NOT fold now.** It is out of this phase (report-only, feeds the m-10 DESIGN), does not gate the MVP or the first-stage lock, and does not touch the canonical contract (`643dd7c2…`) — the contract is the mechanical interface; this refines the policy CONTENT the interface enforces, orthogonally. I will formalize it via a proper m-5 design-amendment (with grill + your adversarial review) when a design phase for it opens; it is already logged to `FRANK-HARDENING-BACKLOG.md` (BR-adjacent). Not rejected, not folded — accepted-in-principle and queued.

## Scope fence
Report-only-feeding (the nod feeds the m-10 DESIGN, does not bypass it — VP F28); no contract change (`643dd7c2…` untouched, no re-review owed); no lock/fold/PLAN/code/`frank/`; VP F20's locked §9:158-174 remains operative. **m-5-F2** posture item untouched.

## DESIGN-REVIEW requested (adversarial)
Return `DESIGN_REVIEW_VERDICT`. Probe hardest: **(a)** is "pinned == current-active by construction" airtight for the one-turn MVP — is there ANY mid-run path (a `config_change` accepted at the conductor while the app-side turn executes) that advances the generation such that the pinned ceiling enforces stale-permissively, and if so is my frozen-config + audit-downstream bounding sufficient to keep the nod §5-faithful rather than forcing a route-back? Does the nod correctly scope what defers (mid-run tightening detection) vs what holds (the floor)? **(b)** is accept-in-principle-and-carry the right disposition, or does adopting reversibility-as-organizing-principle have a hidden interaction with the locked §5 lattices/§7.2 I'm missing?

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/step3-amend-m5-ceiling/DESIGN-planner-20260715-144500.md` — run below.
- Basis: master RECONCILE `…-133500` (operator F31 disposition #2 + the two asks); m-1 owner return `…-124031` (genesis-anchored generation; disposition #2; fail-closed floor); the canonical contract §5 (`2026-07-15-ceiling-artifact-contract.md`); my locked §4/§5/§6/§7.2; `FRANK-HARDENING-BACKLOG.md` (governance-model finding logged by master).
- `git -C frank status --short` — run below.

ACTIONS_GIT_REF: design-only — no `frank/` edit, no contract byte changed (`643dd7c2…`), no locked-doc edit (VP F20), no lock/PLAN/code, no frank submit. Artifact: this DESIGN + one INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`); status run below.
Next requested action: m-5.implementer returns the adversarial DESIGN_REVIEW_VERDICT on (a) the §5 nod + (b) the governance-model disposition; on approve I return the confirmed §5 nod to master (report-only) to feed the m-10 DESIGN, and queue (b) as an m-5 design-backlog item. m-5.planner holds otherwise.
