## DESIGN — m-5 Step-3 PERMISSIVE-TOOLS amendment (authoritative, folds master RECONCILE `145500` / VP F33 / operator Branch B). Supersedes my `145500` (unreviewed). A scoped Step-3 ADDENDUM overlaying `643dd7c2…`: authorizes positive MVP tool dispatch under an operator-accepted permissive/audit-only rule; current-active freshness proof + sharp-capability sandbox DEFER to Step-4; universal per-call m-3 audit MANDATORY; unsandboxed `bash` = operator-accepted residual, stated honestly; structural fail-closed floor preserved. Durable GRILL_LOCK folded. New hash on approve+grill+operator-ratify. Request adversarial DESIGN_REVIEW_VERDICT.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m5-ceiling
PARENT_DISPATCH_ID: step3-amend-m5-ceiling
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — records the operator's already-made Branch-B + ambient-bash acceptance as policy; authors no new operator decision (final bytes ratified at the amendment lock)
GRILL_REQUIRED: yes
DESIGN_DOC_ID: step3-amend-m5-ceiling-host
GRILL_LOCK_ID: step3-permissive-tools-grill
DESIGN_RECORD_KIND: design-doc
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-10.implementer, m-3.planner, m-1.planner, m-7.planner, m-9.planner
IN_REPLY_TO: step3-amend-m5-ceiling/RECONCILE-orchestrator-planner-20260715-145500.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: Step-3 permissive-tools amendment — a scoped ADDENDUM overlaying `643dd7c2…`: positive MVP tools under operator-accepted permissive/audit-only; freshness + sandbox → Step-4; universal audit mandatory (m-3); unsandboxed bash honestly stated; fail-closed floor preserved; GRILL_LOCK step3-permissive-tools-grill. Supersedes my `145500`. Request adversarial DESIGN_REVIEW_VERDICT.

m-5.implementer — master's RECONCILE `…-145500` (folding VP F33 + the operator's Branch-B election) is the authoritative task, and it **converges with my `145500` revision** (Branch B / changed contract / deny-all-until-amended). **This DESIGN SUPERSEDES my `…-145500`** (which you have not yet reviewed — do not review it; review THIS) and folds the RECONCILE's authoritative constraints, which refine my earlier framing in three ways: (1) the operator has **already accepted** the unsandboxed-`bash` ambient authority — so it is **recorded honestly here, not "routed up as open"**; (2) the honest framing is a **permissive/audit-only** rule with freshness + sharp-capability **deferred to Step-4**, replacing my "provisioning-establishes-current-active" story; (3) **GRILL_REQUIRED: yes** + **universal audit mandatory**.

## 1. The honest baseline (carried from the accepted must-revise)
Under the **unchanged** canonical contract `643dd7c2…`, the packet-preserving reading is **deny-all `tool→none`**: §5 requires `config_generation == current-active` at bind time, and m-1 (`124031`) proved **no packet-compliant app-side read establishes that equality** (disposition #2). So firing positive MVP tools **requires a changed rule** — this amendment. `643dd7c2…` stays canonical and **mandates deny-all until this addendum is approved + grilled + re-locked**; no silent rewrite (VP F20 §9:158-174 operative meanwhile).

## 2. Delivery form (grill GA) — a scoped Step-3 ADDENDUM referencing `643dd7c2…`, NOT amend-in-place
My owner call: a **Step-3-boxed addendum** that **overlays** the canonical contract for the MVP and **sunsets at Step-4**, restoring the base `643dd7c2…` rule. Rationale: it keeps `643dd7c2…`'s full freshness + capability semantics as the **durable Step-4 target**, makes the permission **visibly boxed + sunset** (not a permanent loosening of the base rule), and is a clean supersession model (overlay-for-Step-3 → expire-at-Step-4). The addendum is a new m-5-owned file with its **own new SHA-256**, referencing `643dd7c2…` as the base it overlays.

## 3. The Step-3 permissive-tools rule (proposed addendum semantics — exact language for your review; NOT yet a file/hash)
Scope: the **Step-3 MVP only** — one governed turn, single pinned app-side run manifest, the Codex-first tool set `read/write/edit/bash/apply_patch` (app-side in the m-9 worker under m-10, never the conductor).

**HOLDS in the MVP (the preventive gates that remain real):**
- **The coarse tool-CLASS ceiling** — the pinned archetype's tool-axis grant still gates **which tool-classes** the worker may dispatch (a `sensor` archetype is still denied `exec`; an above-class tool still → **zero execution**). The MVP's pinned worker archetype grants the Codex-first set; the ceiling STRUCTURE (archetype→tool-class) is unchanged.
- **The structural fail-closed floor** — manifest absent / malformed / invalid or absent `spawn_authority_ref` / unresolvable archetype ⇒ **deny all tool dispatch** (§5 floors). The permissive rule relaxes ONLY the freshness proof, never the structural floor on a missing/corrupt artifact.
- **Archetype-provenance (F1) unchanged** — copy-only `spawn_authority_ref` (operator/master-provisioned MVP lane); app-local/absent ⇒ fail-closed.
- **Universal audit MANDATORY (grill GC)** — **every** tool call is an m-3 evidence record. The operator's acceptance is of **ambient authority, not un-audited action**; the MVP is strictly better than `--dangerously-skip` **by carrying the trail**. *(m-3 dependency: the per-tool-call app-side evidence record — CC m-3.planner; confirm the mechanism at the reconcile.)* Audit is the **accountability trail, explicitly NOT a preventive substitute** for freshness.

**DEFERS to Step-4 (honestly, not disguised):**
- **The current-active `config_generation` freshness proof** — unestablishable app-side in the MVP (m-1). Step-4 restores it via the live read with m-1's **genesis-identity-anchored** comparand (not a bare integer) + active mid-run staleness detection.
- **Sharp capability confinement / sandbox** — **`bash` runs with ambient host/external/destructive authority; NO cwd-confinement, sandbox, or irreversibility gate is claimed for the MVP** (Step-4 hardening **H-12**, trusted executor). This is an **operator-accepted residual risk** (Branch B). The addendum **states this honestly** — it does **not** imply a bounded/cwd-scoped surface that does not exist.

**So the MVP's honest posture:** positive tools dispatch within the pinned coarse tool-class ceiling, **every call audited**, **freshness unproven + effects unsandboxed** as the operator-accepted Step-3 residual, with the structural fail-closed floor still catching a missing/corrupt artifact. Deny-all remains the floor for degraded modes.

## 4. Step-4 sunset (grill GE)
The addendum is **Step-3-only** and **expires at Step-4**, at which point `643dd7c2…`'s full rule restores: current-active freshness (genesis-anchored) + sharp capability enforcement (sandbox/irreversibility, H-12) + active staleness detection. The permissive rule is a **scoped, sunsetting overlay**, not a permanent change to the base contract.

## 5. GRILL_LOCK — step3-permissive-tools-grill (folded)
```
GRILL_LOCK_ID: step3-permissive-tools-grill
GRILL_REQUIRED: yes
GRILL_SOURCE: master RECONCILE …-145500 (Branch B + constraints); VP F33/F34 step3-arch-packet/…-144149; operator Branch-B election (in-session, recorded step3-arch-packet/…-150000); m-1 …-124031 (no app-side read; genesis-anchoring); the canonical contract §5; my locked §4/§5/§6/§7.2; FRANK-HARDENING-BACKLOG H-11/H-12.
Resolved decisions:
- GA delivery form — a scoped Step-3 ADDENDUM overlaying 643dd7c2 (Step-3 overlay + Step-4 sunset), not amend-in-place. Keeps the base rule as the durable Step-4 target; permission visibly boxed. Source: owner design judgment.
- GB relaxed-vs-preserved — RELAXED (MVP): the current-active config_generation freshness proof + sharp capability sandbox. PRESERVED: the coarse tool-class ceiling (above-class denies), the structural fail-closed floor (degraded artifact denies), archetype-provenance F1, mandatory universal audit. Source: RECONCILE 145500 + m-1 124031.
- GC audit — universal per-tool-call m-3 evidence is MANDATORY (accountability trail; strictly better than --dangerously-skip); evidence not prevention. m-3 mechanism dependency. Source: RECONCILE 145500.
- GD bash honesty — bash = ambient host/external/destructive authority, UNSANDBOXED, operator-accepted residual; stated honestly, no bounded surface claimed; sandbox = Step-4 H-12. Source: operator Branch B + VP F34.
- GE Step-4 sunset — the addendum expires at Step-4; 643dd7c2's full freshness (genesis-anchored) + sharp capability enforcement restore. Source: RECONCILE 145500 + m-1 124031.
- GF operator authority — the ambient-bash acceptance + permissive/audit-only disposition are the OPERATOR's (Branch B; F31/F33); this amendment RECORDS the operator-accepted policy, it does not independently approve shell authority; operator ratifies the final bytes at the amendment lock. Source: RECONCILE 145500.
Operator forks: NONE new — the operator already elected Branch B + accepted ambient bash; the grill resolves only the amendment's policy shape faithful to that election. Final-byte operator ratification lands at the amendment lock (a scope/authority change).
Design-lock impact (STAGED): the addendum overlays 643dd7c2 for Step-3 and sunsets at Step-4; the locked §9:158-174 stays operative until the master-authored staged fold; no fold/lock this leg.
```

## 6. Item (b) / H-11 governance-model refinement — non-operative backlog companion (unchanged)
Accept-in-principle, carry (`FRANK-HARDENING-BACKLOG.md` H-11), **do not fold**, and — per your prior F3 — **not** a basis for the present `bash`/ambient boundary (which is the operator's Branch-B acceptance, stated honestly above, not a claimed bounded surface). It is the natural companion that says WHAT the **Step-4** capability gate should preventively bound (the irreversible) — a Step-4 concern, not an MVP gate.

## 7. Path + scope fence
design (this) → **your adversarial DESIGN-REVIEW** → durable GRILL_LOCK (folded) → on approve I **finalize the addendum file + its NEW SHA-256** → **m-10 consumes the new hash** → **Master+VP review the changed interface + operator ratifies the final bytes** at the amendment lock. Until that lands: `643dd7c2…` canonical, **deny-all is the floor**, no positive tool authorized; VP F20 §9 operative; feeds the m-10 DESIGN, does not bypass it (VP F28). No new hash minted this leg. No lock/fold/PLAN/code/`frank/`. **m-5-F2** untouched.

## 8. DESIGN-REVIEW requested (adversarial)
Return `DESIGN_REVIEW_VERDICT`. Probe: (1) is the relaxed-vs-preserved split honest — does the addendum relax ONLY the freshness proof + sandbox (operator-accepted) while genuinely preserving the coarse tool-class ceiling + the structural fail-closed floor, with no hidden further loosening? (2) is `bash` ambient authority stated honestly (no implied cwd/bounded surface)? (3) is audit correctly positioned as mandatory-accountability, NOT preventive-substitute? (4) is the Step-3 box + Step-4 sunset airtight (does 643dd7c2 fully restore, nothing permanently loosened)? (5) is the m-3 universal-audit dependency correctly scoped as consumed, and the operator-authority (GF) correctly kept as the operator's not mine?

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/step3-amend-m5-ceiling/DESIGN-planner-20260715-152000.md` — run below.
- Basis: master RECONCILE `…-145500` (authoritative task + constraints); VP `step3-arch-packet/…-144149` (F33/F34); m-1 `…-124031`; canonical contract §5 (`…:40-43`); locked §5:82-95, §7.2; `FRANK-HARDENING-BACKLOG.md` H-11/H-12; my superseded `…-145500`.
- `git -C frank status --short` — run below.

ACTIONS_GIT_REF: design-only authoring — no new contract file/hash this leg (proposed addendum language only), no `frank/` edit, no locked-doc edit (VP F20), no lock/PLAN/code, no frank submit. Artifact: this DESIGN + one INDEX row. (Supersedes my `…-145500`.)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`); status run below.
Next requested action: m-5.implementer adversarially reviews this permissive-tools amendment; on approve I finalize the Step-3 addendum file + its new SHA-256 and return to master (report-only) for m-10 re-consume + Master/VP review + operator final-byte ratification. Under `643dd7c2…` the MVP stays `tool→none` until then. m-5.planner holds.
