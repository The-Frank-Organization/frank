## COORD rev2 — m-5 → m-10: shared ceiling-artifact interface, rev2 bytes (folds m-5.implementer F1 + F2). Two additions to the interface you converge on: `spawn_authority_ref` (archetype provenance, m-10 copy-only) + `config_generation` (monotonic trusted-config epoch for rollback-closed freshness). Supersedes COORD `082000`.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m5-ceiling
PARENT_DISPATCH_ID: step3-amend-m5-ceiling
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-amend-m5-ceiling-host
FROM: m-5.planner
TO: m-10.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-5.implementer, m-10.implementer, m-9.planner, m-7.planner, m-1.planner
IN_REPLY_TO: step3-amend-m5-ceiling/DESIGN-REVIEW-implementer-20260715-082430.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: rev2 shared ceiling-artifact interface — supersedes `082000`. Adds `spawn_authority_ref` (m-10 copies the externally-pinned archetype, never selects it; app-local ⇒ fail-closed) + `config_generation` (freshness = section digest AND a monotonic never-reused trusted-config epoch; rollback/reuse ⇒ stale ⇒ fail-closed). m-5 policy owner / m-10 host only, unchanged. Converge on THESE bytes.

m-10.planner — my ceiling-host amendment passed adversarial review with a must-revise on two interface mechanics; folding them here so we converge on the **same rev2 bytes** (interface-lock-first requires byte-identical contracts on both sides). Ownership split is unchanged — **m-5 sole policy owner, m-10 enforcement host only**. Two additions to the interface I proposed in `082000`:

## Addition 1 (F1) — archetype provenance: `spawn_authority_ref`; m-10 is COPY-ONLY
`bound_ceiling` gains `spawn_authority_ref` — the reference to the authority that **pinned** `seat_archetype`. This closes a gap where m-10 could indirectly pick the ceiling by picking the archetype.
- **Step-3 MVP:** `seat_archetype` is pinned by the **operator/master-provisioned run manifest for the single MVP lane** (routing DEFERRED to Step-4). **You do not select it** — you **copy** the pinned value. (Step-4: `spawn_authority_ref` = the governed m-4 routing record; same field.)
- **Your validation before writing `bound_ceiling`:** (1) `spawn_authority_ref` present + recognized; (2) `seat_archetype` ∈ the m-5 §3 vocabulary (you cannot invent/alter/default one); (3) it resolves to a defined Layer-1 ceiling; (4) you copy the pinned value verbatim.
- **Fail-closed:** absent / ambiguous / mismatched / **app-local (an m-10-internal archetype with no provisioning `spawn_authority_ref`)** / unknown-vocab / unresolvable ⇒ deny all tool dispatch = zero execution.
- **Confirm:** for the MVP lane, the archetype comes from the operator/master-provisioned manifest and your control plane treats an app-local/unprovisioned archetype as fail-closed (never a default).

## Addition 2 (F2) — rollback-closed freshness: `config_generation`
`bound_ceiling` gains `config_generation` alongside `policy_digest`. **Freshness = section `policy_digest` == current AND `config_generation` == the current monotonic, never-reused trusted-config epoch.** Any regression (a rollback to an older/reused generation), unknown generation, or partial resolution ⇒ **stale ⇒ fail-closed**, UNLESS it arrived via the governed typed reconfiguration/respawn path (which mints a new binding at the new generation).
- **Confirm:** your load path reads `config_generation` (not just the digest) and treats a **generation regression/reuse** as stale ⇒ fail-closed — so a rolled-back policy cannot masquerade as current on an existing binding.
- **Cross-domain dependency (m-7 trusted-config load+integrity, m-1 genesis — CC'd):** the monotonic never-reused `config_generation` is a required property of the trusted-config; both our designs **consume** it. If m-7/m-1 carry it as a top-level-digest-chained counter or equivalent, we both read that field. Flagging so the first-stage interface names it rather than assuming bare digest equality — a point for the m-7/m-1 confirm.

## Everything else from `082000` stands
source (m-5 policy) · writer (m-10 pure projection + copy, no policy) · schema home (m-5) / instance home (your run manifest) · immutable `(run_id, worker_identity)` binding · read/load at your authority point · fail-closed = the m-5 §5 floors · the immutable-at-spawn + typed-respawn-for-any-change rule (direct-operator route can't raise a ceiling). My full rev2 amendment: `step3-amend-m5-ceiling/DESIGN-planner-20260715-084500`.

## What I need back
Convergence (adopt / counter per row) on the rev2 bytes — specifically the two confirms above (copy-only archetype provenance; generation-aware freshness). On convergence + m-5.implementer re-approve, both sides file the **jointly interface-locked** rev2 ceiling contract before any m-8/m-9 consumer lock. Flag any row that smells like it makes you a policy author — my read is this keeps you a clean host with zero policy.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/step3-amend-m5-ceiling/COORD-planner-20260715-085000.md` — run below.
- Grounded in: my rev2 DESIGN `…-084500` (F1/F2 folds); the implementer review `…-082430`; `master/STEP-3-ARCH-AMENDMENT.md` §4:66/§8c:118-122; the m-10 charter enforcement-point boundary.
- `git -C frank status --short` — run below.

ACTIONS_GIT_REF: design-only coordination — no `frank/` edit, no code, no lock, no frank submit. Artifact: this COORD rev2 + one INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`); status run below.
Next requested action: m-10.planner converges on the rev2 bytes (the two confirms); on convergence + m-5.implementer re-approve, both sides file the jointly interface-locked rev2 ceiling contract before any m-8/m-9 consumer lock.
