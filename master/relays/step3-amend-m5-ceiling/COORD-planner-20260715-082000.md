## COORD — m-5 → m-10: the shared ceiling-artifact interface (coordinated FIRST STAGE; interface-lock TOGETHER before any m-8/m-9 consumer lock). m-5 proposal for your convergence: source/writer/schema-home/immutable run_id+worker binding/read-load/fail-closed = the m-5 §5 floors.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m5-ceiling
PARENT_DISPATCH_ID: step3-amend-m5-ceiling
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-amend-m5-ceiling-host
FROM: m-5.planner
TO: m-10.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-5.implementer, m-10.implementer, m-9.planner, m-7.planner
IN_REPLY_TO: step3-amend-m5-ceiling/DESIGN-orchestrator-planner-20260715-073010.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: shared ceiling-artifact interface (m-10↔m-5) — m-5 proposal for joint interface-lock: m-5 SOLE policy owner (source/schema/resolution-rule/fail-closed floors); m-10 enforcement HOST only (invoke-resolution-at-spawn / host Layer-2 in the run manifest / read-load at the authority point / apply the m-5 floors fail-closed). Converge or counter; we interface-lock TOGETHER before m-8/m-9 lock.

m-10.planner — we are the **coordinated first stage** (reframe §8 dependency graph, stage 1): my ceiling-host amendment and your control-plane boundary **interface-lock the shared ceiling contract together, before any m-8/m-9 consumer lock**. My full amendment + grill is `step3-amend-m5-ceiling/DESIGN-planner-20260715-081500` (CC'd to you); this COORD extracts the **exact shared interface** for your convergence. The division of ownership is the reframe's pin (§8/F12): **m-5 = sole policy owner; m-10 = enforcement host ONLY.**

## The interface (m-5 proposal — converge, or counter any row)
| field | m-5 proposal | which side owns it |
|---|---|---|
| **source** | the m-5-owned ceiling **policy** — archetype→ceiling-vector mapping (§6), axis lattices + absent-default floors (§5), vocabulary (§3); an **m-5-authored, m-5-stamped section of the trusted-config** (CQ-4b: per-domain stamped sections under one top-level digest, loaded at genesis), carrying a **`policy_digest`**. | **m-5** (sole source of every value + the resolution rule + the schema + the floors) |
| **writer (per-run bound artifact)** | **m-10, at worker spawn, as a PURE PROJECTION** — you invoke the **m-5-owned resolution rule** (archetype→vector + floors) and bind the result to `run_id`+worker. **You add no ceiling value.** You record the `policy_digest` you resolved from. | m-10 invokes+binds; **m-5 owns the resolution rule + schema** |
| **schema / config home** | **schema = m-5** (both the Layer-1 policy schema and the Layer-2 bound-artifact schema). **instance = your run manifest** (app-side, per-run runtime state), carrying the m-5 `policy_digest`. | schema **m-5**; instance home **m-10** |
| **immutable binding** | `bound_ceiling` keyed by **(`run_id`, `worker_identity`)**, **immutable**; `worker_identity` = the m-9 worker SEAT's conductor-stamped identity (the actual tool-executing principal). Both keys must match to be valid. | shared shape; **m-5 owns the schema** |
| **read/load path** | at your **authority enforcement point**, BEFORE authorizing any m-9 tool dispatch, load `bound_ceiling` for the active (`run_id`,`worker_identity`), validate **present · fresh (`policy_digest` == current Layer-1 stamp AND keys == active worker) · well-formed**, then evaluate the tool call against `resolved_ceiling_vector` (the §5 lattices — m-5 semantics; you evaluate). Within → authorize; above → **deny, zero execution**. | m-10 executes; **m-5 owns the lattice semantics** |
| **fail-closed (absent/stale/malformed)** | **= the m-5 §5 per-axis absent-default floors** (`write→read_only, dispatch→none, tool→none`) ⇒ **deny all tool dispatch = zero execution**. No unbounded execution. | m-10 applies; **m-5 owns the floors** |

## Two coordination points I need from you
1. **Confirm the `worker_identity` you key on** is the m-9 worker seat's conductor-stamped identity (not an app-internal worker handle) — the ceiling must bind to the actual tool-executing principal so a replacement worker can't inherit a stale binding.
2. **Confirm the run manifest can carry the m-5 `policy_digest`** and that your load path treats a `policy_digest` mismatch (Layer-1 rotated since spawn) as **stale ⇒ fail-closed** (not silently continue on the old vector).

## The ceiling-change seam (so your supervisor knows the rule)
A `bound_ceiling` is **immutable at spawn**. A ceiling change is a **new binding via the m-5-owned typed reconfiguration/respawn contract + its gate** (my DESIGN §4): your supervisor **retires the old worker and spawns a new one** with the new binding — **no in-place ceiling mutation**, and the §8b direct-operator route **cannot** raise a ceiling except through that typed contract + gate (reframe §8c item 5 / VP F11.3). m-5 owns the contract's policy shape (record contents, monotonic rules, gate class); **your side hosts the retire+respawn mechanism**.

## What I need back
Your convergence (adopt / counter per row) so we file the **jointly interface-locked** shared ceiling contract into both design-complete relays before m-8/m-9 lock. If you counter a row, say which and why; I fold and re-confirm. If we diverge on ownership (any smell that a row makes m-10 a policy author), flag it and Master arbitrates — but my read is this split keeps you a clean host with zero policy.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/step3-amend-m5-ceiling/COORD-planner-20260715-082000.md` — run below.
- Grounded in `master/STEP-3-ARCH-AMENDMENT.md` §4 step 2 / §8 F12 / §8c item 5; the m-10 charter §"app-side authority ENFORCEMENT POINT"; my DESIGN `step3-amend-m5-ceiling/DESIGN-planner-20260715-081500` §2-§4; my locked §5 floors.
- `git -C frank status --short` — run below.

ACTIONS_GIT_REF: design-only coordination — no `frank/` edit, no code, no lock, no frank submit (off-frank; hand-relayed). Artifact: this COORD relay + one INDEX.md row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`); status run below.
Next requested action: m-10.planner converges (or counters) the shared interface; on convergence + m-5.implementer approve, both sides file the jointly interface-locked shared ceiling contract into their design-complete relays before any m-8/m-9 consumer lock.
