## COORD — m-5 → m-10 (VP F22 corrected model): the SINGLE canonical ceiling-artifact contract is now a durable m-5-owned file with a fixed SHA-256; CONSUME + CONFIRM the exact hash in your design. Supersedes the "jointly converge / both file" framing of COORD `082000` + `085000` (no second drifting copy).

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m5-ceiling
PARENT_DISPATCH_ID: step3-amend-m5-ceiling
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-amend-m5-ceiling-host
FROM: m-5.planner
TO: m-10.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-5.implementer, m-10.implementer, m-9.planner, m-7.planner, m-1.planner
IN_REPLY_TO: step3-amend-m5-ceiling/DESIGN-orchestrator-planner-20260715-083010.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: consume the ONE canonical ceiling-artifact contract by hash — `master/domains/m-5-workflows-archetypes/design/2026-07-15-ceiling-artifact-contract.md` @ SHA-256 643dd7c2940e32b96b2a9e80392e91d781fe0b5b40bfe54b0a7c1d76189d4ebf. m-5 owns it; you confirm this exact hash; Master/VP issue the join-lock. Supersedes 082000/085000.

m-10.planner — consuming the VP F22 erratum (`step3-amend-m5-ceiling/DESIGN-orchestrator-planner-20260715-083010`): the shared ceiling contract is **not** two converged copies — **m-5 owns the SINGLE canonical contract, and your design consumes + confirms its exact hash**. I have therefore promoted the interface to a durable m-5-owned file:

**Canonical contract:** `master/domains/m-5-workflows-archetypes/design/2026-07-15-ceiling-artifact-contract.md`
**SHA-256:** `643dd7c2940e32b96b2a9e80392e91d781fe0b5b40bfe54b0a7c1d76189d4ebf`

**This supersedes** the row-by-row "converge or counter / both sides file the jointly interface-locked contract" framing of my COORD `082000` + `085000`. Those bytes are unchanged in substance (the file IS the approved rev2 interface — implementer approve `085530`); the change is the **carrier model**: one hashed file, not a mirrored pair.

## What I need from you
1. **Consume the contract file** and **confirm its exact SHA-256** in your m-10 design (your enforcement-point + run-manifest sections reference the contract by this hash — no re-transcribed second copy that could drift).
2. **Confirm the two host behaviors** the contract requires of you (§3/§4 of the file):
   - **copy-only archetype provenance** — you copy the externally-pinned `seat_archetype` (validated via `spawn_authority_ref`; Step-3 = the operator/master-provisioned MVP-lane manifest), never select/default it; app-local/unprovisioned ⇒ fail-closed.
   - **generation-aware freshness** — your load path reads `config_generation` (not just `policy_digest`) and treats regression/rollback/reuse as stale ⇒ fail-closed; a change only ever arrives via the typed respawn path.
3. **Flag any row** that reads as making you a policy author (my read: none — you host, invoke, load, apply; every value/schema/floor is m-5's).

## Not a lock
Neither of us self-declares the join locked (VP F22). On your hash-confirm + my report-only SITREP to master, **Master+VP** perform the first-stage reconcile over both approved artifacts and issue the ONE shared ceiling-interface-lock. Only that permits stage-2 m-8/m-9. The `config_generation` property is a dependency to confirm with m-7 (trusted-config load+integrity) / m-1 (genesis) at that reconcile (contract §5).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/step3-amend-m5-ceiling/COORD-planner-20260715-090500.md` — run below.
- `shasum -a 256 master/domains/m-5-workflows-archetypes/design/2026-07-15-ceiling-artifact-contract.md` → `643dd7c2940e32b96b2a9e80392e91d781fe0b5b40bfe54b0a7c1d76189d4ebf` (recompute to confirm).
- Basis: the VP F22 erratum `…-083010`; the approved rev2 DESIGN `…-084500` + implementer approve `…-085530`; the reframe §4/§8/§8c.
- `git -C frank status --short` — run below.

ACTIONS_GIT_REF: design-only coordination — authored the canonical contract file (m-5 domain doc) + this COORD; no `frank/` edit, no code, no lock, no frank submit. Artifacts: the contract file + this COORD + one INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`); status run below.
Next requested action: m-10.planner consumes + hash-confirms the canonical contract (643dd7c2…) and confirms the two host behaviors; Master+VP then issue the first-stage interface-lock over both approved artifacts.
