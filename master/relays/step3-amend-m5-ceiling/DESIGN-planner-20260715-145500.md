## DESIGN rev — m-5 §5 nod: ACCEPT the must-revise (F1/F2/F3), WITHDRAW "vacuously satisfied". Choose BRANCH 2 (the only F31-consistent path): author a changed one-turn freshness/provenance contract (bind-time provenance = the operator/master PROVISIONING, not an app-side read; the trust-the-provisioning residual named EXPLICITLY, not audit-as-prevention; degraded modes still fail-closed; full active-read rule → Step-4). Two pieces routed UP (NOT mine): operator confirmation of the residual + VP-F34 shell-authority. Item (b) stays non-operative backlog. New hash finalized on approve + master/operator confirm. Request re-review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-amend-m5-ceiling
PARENT_DISPATCH_ID: step3-amend-m5-ceiling-review-r3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — surfaces two items for master/operator routing (residual acceptance + F34 shell authority); authors no operator decision
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-amend-m5-ceiling-host
DESIGN_RECORD_KIND: design-doc
FROM: m-5.planner
TO: m-5.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-10.implementer, m-1.planner, m-7.planner, m-9.planner
IN_REPLY_TO: step3-amend-m5-ceiling/DESIGN-REVIEW-implementer-20260715-144650.md
BUNDLE_ID: m-5-workflows-archetypes
SUBJECT: rev — must-revise accepted; WITHDRAW the vacuous nod; BRANCH 2 chosen (F31 mandates positive tools, so the unchanged contract's fail-closed forbids the MVP ⇒ a changed contract is required). Proposed amended freshness/provenance semantics inside; new hash on approve+confirm. Shell-authority (F34) + residual-acceptance routed UP. Request adversarial DESIGN_REVIEW_VERDICT.

m-5.implementer — your must-revise (`…-144650`) is correct on all three findings; accepting in full and revising decisively.

## Accept the review
- **F1 accepted — I WITHDRAW "pinned == current-active by construction / vacuously satisfied."** You are right: the frozen-run premise proves only that no *later* generation change occurs; it does **not** prove the run-start bound value equals the authoritative genesis-anchored committed-chain head **at bind time**, and m-1's return establishes m-10 has **no already-landed, packet-compliant app-side path** to read the current-active stamp (`124031:36-46`). Under the **unchanged** contract §5, absence of current-active proof ⇒ **fail-closed `tool→none`** (contract `:40-43`). My nod tried to satisfy `== current active` without a way to read `current active` — invalid.
- **F2 accepted — audit is EVIDENCE, not prevention.** I remove audit-universal from the *preventive* freshness argument entirely. m-3 evidence is downstream evidence; it does not license dispatch under a stale/unproven ceiling. Any accepted stale-permissive window must be an **explicit semantic residual-risk branch**, not "dispatch-and-audit-later."
- **F3 accepted — item (b) is non-operative backlog and does NOT justify the `bash`/cwd boundary.** The governance-model refinement is a *proposed* m-5 refinement only; it is not a safety proof for a present unsandboxed tool surface. Per VP F34, an allowed `bash` can leave cwd / follow symlinks / spawn programs / do destructive-external effects, and audit-after ≠ prevention — that **shell authority is Master/VP/operator's**, not something my ceiling nod may carry.

## The branch decision — BRANCH 2 is forced by F31
The three branches you named, against the operator's F31 decision (the MVP **fires** `read/write/edit/bash/apply_patch` app-side under a pinned coarse ceiling):
- **Branch 1 (strict `tool→none` under unchanged `643dd7c2…`):** the MVP fires **no** positive tools. This **contradicts** the operator's F31 (which fires positive tools). Not the operator's chosen path — Branch 1 would silently override an operator decision.
- **Branch 3 (existing bind-time mechanism):** **dead** — m-1 explicitly says no already-landed, packet-compliant app-side read exists (`124031:39,42,48-50`). No E1 proof to supply.
- **Branch 2 (author a changed one-turn freshness/provenance contract, new hash):** the **only** path that realizes F31 (positive tools) without pretending the unchanged contract permits them. **Chosen.**

So the honest statement is: *the unchanged `643dd7c2…` contract, given disposition #2's no-read-path, mandates `tool→none`; firing positive MVP tools therefore REQUIRES a changed contract.* I author that change below.

## The BRANCH 2 amendment — proposed one-turn freshness/provenance semantics (exact language for your review; NOT yet a new file/hash)
A Step-3-MVP qualification to the contract's freshness rule, scoped to the **one-turn, config-frozen, operator/master-provisioned MVP lane**:
1. **Bind-time provenance = the PROVISIONING authority, not an app-side read.** The run-start `config_generation` + `policy_digest` are **established by the operator/master provisioning act** (the same `spawn_authority_ref` = operator/master-provisioned MVP-lane manifest that pins `seat_archetype`). The provisioner reads the then-current config out-of-band and pins the ceiling+generation into the manifest at run-start. This is the bind-time authority (§8b: the live operator channel is a trusted authenticator **by construction** — confusion-not-malice). m-10 does **not** independently re-establish current-active (it cannot — disposition #2).
2. **m-10 validation (what it CAN check):** valid `spawn_authority_ref` (operator/master-provisioned) present · pinned `config_generation` + `policy_digest` present + well-formed · archetype-provenance valid (the existing F1 copy-only rule). m-10 pins + enforces on pass.
3. **Named residual-risk — EXPLICIT, operator-accepted, NOT audit-as-prevention:** m-10 cannot verify the provisioned generation against the live committed-chain head (no app-side read). The MVP **trusts the operator/master provisioning** to have pinned the current generation — a **deliberate, operator-scoped residual** (F31 "packet-preserving fail-closed defer"), sound under confusion-not-malice (the trusted-provisioner-by-construction; a *confused* provisioner is not the threat model, and operator impersonation is malice/out-of-scope). Audit remains **downstream evidence only** — it is named here as detection-after, explicitly **not** the preventive mechanism.
4. **Degraded modes still fail-closed (unchanged floors):** manifest absent / malformed / invalid or absent `spawn_authority_ref` / unresolvable archetype / internal provenance mismatch ⇒ **deny all tool dispatch** (§5 `tool→none`). The floor governs everything m-10 CAN detect; only the live *current-active cross-check* is what the provisioning-trust replaces for the MVP.
5. **Step-4 restores the full rule:** when the app-side read path lands (Step-4), freshness reverts to `config_generation == current-active` via the live read, with m-1's **genesis-identity-anchored** comparand (not a bare integer, `124031:28`) + active mid-run staleness detection + hard sandbox + irreversibility gating. The residual closes there.
6. **New canonical hash:** this changes the contract, so it produces a **new SHA-256** (a v2 of `2026-07-15-ceiling-artifact-contract.md`), which m-10 must re-consume and Master/VP re-lock — **finalized only on your approve + master/operator confirmation** of the two routed-up items below. I do not mint the new hash inside this review.

## Routed UP — NOT decided by me (per F3 + VP F34, and the residual-acceptance)
- **(i) Operator confirmation of the trust-the-provisioning residual** as the intended MVP contract semantics. F31 implies it, but baking an operator-accepted residual into a hashed contract warrants an explicit operator confirmation — master's to route.
- **(ii) VP F34 shell authority.** My ceiling governs **tool-CLASS capability** (may this seat invoke `bash` at all — the coarse allowlist). The **sandbox / cwd-scoping / irreversibility-gating of what `bash` may DO** is **not my ceiling's domain and is NOT approved by this amendment**; per VP F34 it is Master/VP/operator's, deferred to Step-4. This amendment must not be read as approving an unsandboxed `bash`. I state it loudly rather than bury it.

## Item (b) — unchanged disposition: non-operative backlog only
Accept-in-principle, carry as an m-5 design-backlog item (logged, `FRANK-HARDENING-BACKLOG.md`), **do not fold**, and — per your F3 — it is **not** a basis for approving the present `bash`/ambient-tool boundary. Reversibility-as-organizing-principle is a future amendment, not a current safety proof.

## Scope fence
No new hash minted this leg (proposed language only); `643dd7c2…` remains the current canonical contract, and it **mandates `tool→none`** until the Branch-2 amendment is approved+confirmed+re-locked; VP F20's locked §9:158-174 operative; no lock/fold/PLAN/code/`frank/`. Feeds the m-10 DESIGN, does not bypass it (VP F28). **m-5-F2** untouched.

## DESIGN-REVIEW requested (adversarial re-review)
Return `DESIGN_REVIEW_VERDICT`. Probe: (1) is the provisioning-established bind-time provenance a *real* authority (does `spawn_authority_ref` = operator/master-provisioned genuinely establish the generation at run-start, or does it just relocate the same unread-current-active gap?) — i.e., is the residual honestly named as "trust the provisioner," not smuggled proof? (2) are the degraded-mode floors complete (everything m-10 CAN detect still fails closed)? (3) is the F34 shell-authority carve-out clean (does my amendment truly not imply bash-sandbox approval)? (4) is Branch 2 correctly scoped so Step-4 fully restores the active-read rule?

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/step3-amend-m5-ceiling/DESIGN-planner-20260715-145500.md` — run below.
- Basis: your must-revise `…-144650` (F1/F2/F3 + the 3 branches); m-1 return `…-124031`; VP `step3-arch-packet/…-144149` (F34); master RECONCILE `…-133500` (operator F31); canonical contract `…:40-43`; locked §5:82-95; §8b (trusted operator by construction).
- `git -C frank status --short` — run below.

ACTIONS_GIT_REF: design-only revision — no new contract file/hash this leg (proposed language only), no `frank/` edit, no locked-doc edit (VP F20), no lock/PLAN/code, no frank submit. Artifact: this DESIGN + one INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`); status run below.
Next requested action: m-5.implementer re-reviews the Branch-2 revision; on approve I return to master (report-only) recommending Branch 2, requesting (i) operator confirmation of the residual + (ii) Master/VP handling of the F34 shell authority, and — on those — I finalize the v2 canonical contract + new hash for m-10 re-consume + Master/VP re-lock. Under `643dd7c2…` the MVP remains `tool→none` until then. m-5.planner holds.
