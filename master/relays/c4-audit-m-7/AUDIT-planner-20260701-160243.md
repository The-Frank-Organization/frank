## AUDIT — m-7.planner independent substrate audit (c4, conductor-core)

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: c4-audit-m-7
PARENT_DISPATCH_ID: c4-audit-m-7
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — read-only audit; operator-judgment items: none blocking (see Questions)
IN_REPLY_TO: c4-audit-m-7/AUDIT-orchestrator-planner-20260701-153627.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: m-7.implementer, master.orchestrator-reviewer, operator
SUBJECT: AUDIT — runtime-substrate sweep complete; PRIMARY_BUCKET still-open; seam inventory + 6 contract questions surfaced

**Artifact of record:** `master/domains/m-7-conductor-core/audit/2026-07-01-substrate-audit.md` — full findings, per-source corpus reports, §2A mechanism sweep, concurrency-invariant analysis, seam inventory (16 rows), design questions. This relay summarizes; the artifact carries the detail. CF-1 honored (full `master/...` paths). CF-2 honored: five read-only corpus lenses (v2.8.8, jcode, claude-code, codex [operator-directed addition], web prior-art) served the planner's OWN sweep only; the m-7.implementer audit is untouched and independent (F4 stands).

4-bucket verdict:
PRIMARY_BUCKET: still-open
- still-open: the ENTIRE §2A substrate is net-new — serialized commit loop, crash-atomic multi-file commit + recovery + corrupt-record quarantine, internal-fault disposition, trusted config load + integrity, interface-guardrail enforcement, local-outbox-only send, store genesis/GC, restart seat-binding recovery. No internal system has ANY of it; new design work is needed and is exactly what c4 DESIGN produces.
- already-closed: none — no existing conductor exists to promote, enable, or wire. (Promote-PARTS consumed inside the net-new engine, not rebuilt: v2.8.8 semantic contract layer via the locked m-2 dissolve/survive refactor; jcode crash-atomic single-file write discipline + idempotency-by-replay + restart disposition; claude-code per-recipient mailbox sharding + mark-read-after-deliver + priority dequeue; codex single-owner serialized writer + ToolExposure visible/executable split + untrusted-config denylist + config-lock replay validation.)
- product-overlapped: none material — wrap/adversarial isolation is SHELVED (GRILL-LOCK D3), not overlap; the six policy domains are HOSTED per the one-line boundary, not overlapped; Claude Code/codex app-server are the Step-1 RIDE surface, not competing conductors.
- recommended-next: proceed to c4 DESIGN of the minimal engine per the dispatch design question, carrying (a) the mechanism shapes incl. the named-commit-pivot refinement, (b) seam inventory → full seam matrix with biting negative fixtures, (c) CQ-1..CQ-6 as targeted COORDs / tracked dependencies, (d) GRILL before design-lock.

Duplicate/already-built gate: checked v2.8.8 (whole-tree grep: zero runtime — "a coordination protocol, not an orchestration runtime", its README:7), the LIVE master/ instance (planner-run E2 probes), jcode, claude-code, codex source, and the external field (MCP gateways allowlist tools but none runs a governed serialized store). Nothing existing to promote-in-place; no rebuild risk.

Key claims (evidence per claim):
- The live team store has no substrate: 261 INDEX rows with out-of-order + duplicate timestamps from unserialized independent seat appends; no lock/tmp files; no daemon; seats hold raw store paths — evidence E2 — planner-run probes this session (awk monotonicity, uniq -d, find, ps, stat), detailed in artifact §2.
- v2.8.8 executable substrate = relay-lint.py + a 35-line PostToolUse hook that "never hard-blocks" and is bypassed by Bash/heredoc writes; no config read; no delivery mechanism; lineage confusion-robust only (agent-authored FROM/PARENT) — evidence E1 (lens-collected; hook header + protocol cites planner-verified) — artifact §3.1.
- jcode: trusted connection binding exists but the message path trusts wire `from_session` verbatim (planner-verified at `crates/jcode-protocol/src/wire.rs:378-390`); no single-writer loop; message log in-memory only. Its `crates/jcode-storage/src/lib.rs:261-384` write discipline (tmp→fsync→.bak hardlink→rename→dir-fsync + backup recovery, planner-verified) is the best internal single-file primitive to promote — evidence E1 — artifact §3.2.
- claude-code: in-place writeFile + corrupt-read-swallowed-to-empty = silent total-inbox loss on mid-write crash; no multi-file transaction; `from` self-reported and privilege-checked by string-compare — evidence E1 (lens-collected) — artifact §3.3.
- codex: per-file single-owner serialized writer with acked barriers + append-only JSONL replay + 4-level ToolExposure (incl. Hidden) + untrusted-config denylist + config-lock replay check = the strongest internal donor; but flush-without-fsync, eventual-consistency record/index tiers, silent skip of torn lines, no burn-once — evidence E1 (lens-collected) — artifact §3.4.
- Every §2A mechanism has strong citable external prior art (LMAX/Redis/SQLite/Kafka single-writer; maildir/SQLite-super-journal/git multi-file atomicity; Kafka CRC+truncate; K8s admission failurePolicy: Fail; arXiv 2605.18414 remove-the-tool with 48-68% forbidden-tool-selection data) — evidence E1 (lens-collected with source URLs) — artifact §3.6. One refinement surfaced: atomicity comes from ONE named atomic FS op as the commit pivot; the journal + idempotent replay is the recovery half — DESIGN must name the pivot.
- Concurrency invariant (dispatch AC2): one single-threaded commit loop running submit()'s read-validate-append and verify()'s check-and-burn to completion per request structurally removes the two-honest-seats double-accept interleaving — a control-flow property of the trusted engine, licensed "by construction"; the loop does not stop same-uid direct store writes (D5 accepted-risk, adversarial-lane, claim-swept) — evidence E1 (analysis over locked contracts + prior art) — artifact §4.1.
- Interface-guardrail realization (dispatch AC4): conductor-as-MCP-server presents a per-seat trusted-side tool registry of exactly {submit, project, read}; raw store/config paths appear in no tool schema/description/resource; negative fixture = (i) registry enumeration, (ii) scripted confused-seat raw-write attempt finds no tool, (iii) config path absent from every seat-deliverable projection. Claim: CONFUSION-RESISTANT only — evidence E1 — artifact §4.4.
- SEAM INVENTORY (dispatch AC6): 16 rows {contract owner · doc/section · m-7 execution obligation} over m-1 store/stamp + Seam-C verify/nonce-burn, m-2 render/validation/lineage, phase-split required-set, m-3 observe hook + egress, decision-② fail-closed, pure-judgment A-floor, m-4 route_dispatch/templates, m-5 spawn/ceiling/slot_in, m-6 scheduler/buckets, trusted config, terminal-state tokens — evidence E1 (all six locked docs read in full by the planner) — artifact §5.
- Six contract under-specifications flagged as targeted-COORD candidates, each with a policy owner, none reopening a lock: CQ-1 phase-split step-gating not yet visible in locked m-1/m-2 text (§2A.5); CQ-2 decision-② fail-closed fold into m-3 §3.2/§8/§12 outstanding (fail-open text stands at m-3 :63); CQ-3 pure-judgment A-floor table has no locked author; CQ-4 terminal-state token set (Q-E bounced→rejected + the new HELD fault state) — engine-blocking at design-lock; CQ-4b config artifact composition owner; CQ-5 slot_in classification ordering; CQ-6 persisted seat-binding table + sibling-burn restart semantics (§2C) — evidence E1 — artifact §5.
- away/parked-lane + restart touch-points (dispatch AC7) surfaced, not designed: persisted binding table; parked-lane recovery from the store alone; journal-resolved in-flight submits; nonce-burn inside the commit loop; re-observe-on-wake ordering; timer substrate — artifact §4.7/§7.

Boundary contract (this audit): Writes: this relay + the audit artifact + one INDEX row (docs only). Reads: governing corpus, six locked design docs, live store probes, references/ + extracted/ corpora (read-only), web. Target entity: the c4 DESIGN phase's input set. Downstream consumer: m-7.implementer (pair reconcile), then orchestrator/VP at audit-reconcile. Contract: artifact §5 seam inventory + §7 design questions. Proof: E1/E2 as tagged. No-consumer action: not applicable.

Risks / reject-or-narrow gates: over-reach guarded (every CQ assigned a policy owner — m-7 consumes, never authors policy); under-reach claimed (timer substrate, held-record surface, pane-spawn = m-7 engine detail); claim-boundary regression template in artifact §4.1/§4.4; CQ-2/CQ-4 are dependencies owned elsewhere that block byte-consistent design-LOCK (not design start) — flagged for the reconcile. Narrow gate: if DESIGN finds INDEX.md cannot be both human-append-readable and crash-atomic, the derived-INDEX fallback touches m-1's locked on-disk layout → targeted m-1 COORD, never a silent change.

Questions:
- To m-7.implementer (reconcile inputs): confirm/refute the absence claims; concur/dissent on the §4.1 by-construction scope; re-bucket any seam row; over-reach check on artifact §7.
- To operator: none blocking at AUDIT.

Not authorized / not claimed: no DESIGN lock, no PLAN, no IMPL, no code/source/`pcode/` writes, no spike, no locked-contract reopen, no reinterpretation of any policy contract (questions surfaced as CQs instead).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/c4-audit-m-7/AUDIT-planner-20260701-160243.md` — run before handoff (result in inline pointer)
- `git -C pcode status --short` — clean (planner-run this session)
- five lens agents read-only; three load-bearing cites re-verified verbatim against source by the planner

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created `master/domains/m-7-conductor-core/audit/2026-07-01-substrate-audit.md` + this relay `master/relays/c4-audit-m-7/AUDIT-planner-20260701-160243.md` + one appended INDEX.md row timestamped 20260701-160243; no code/source/`pcode/` edits (pcode verified clean)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C pcode status --short` = clean, pcode untouched)
Next requested action: operator relays this audit to m-7.implementer for the independent-audit reconcile once their audit files; pair-reconcile follows; audit-reconcile re-engages the VP.
