## DESIGN — the H-16 design REVISED to rev9 (R8-F1..F3 folded: the system anchor now requires a MECHANICAL COMPLETENESS PROOF over the raw redo stream — exactly-once membership of the canonical legacy pivot set + intact segment sequence; presence/parseability is NEVER authority and every deficiency fails closed to the operator anchor + quarantine, with snapshot-once idempotent pass mechanics and GC gating · the quarantine gains its EXECUTABLE contract — derived-state locus, the widened typed `Resolve` refusal match-gated against leakage, atomic clearing, and a reachable recovery for all THREE conflict shapes incl. the sole-operator LOCAL ADMINISTRATIVE ANCHOR on the genesis trust basis · the three stale rev7/rev8 clauses removed and the FINAL m-1/m-2 confirmation scopes enumerated in the design header) → fresh uniquely-parented m-7.implementer review @ SHA-256 `921eb058…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the master/VP pass and BOTH owner confirmations precede lock/IMPL
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-212838.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev9, SHA-256 `921eb0587c9f0d1d35695a104cfd75e17c74b7e3e9942bd0f88a97b5731b9166` (rev8 `405d28df…` superseded); the header now carries the FINAL six-part m-1 scope (incl. the completeness-verified-redo ruling as a DISTINCT question and the sole-operator recovery authority basis) and the final m-2 scope (exact rejection bytes + the three operator forms)

m-7.implementer — R8-F1..F3 folded; the concessions first: rev8 treated redo presence as selection authority when `readRedo` provably tolerates vanished segments and torn lines with no canonical cross-check (a valid-looking partial redo omitting the true tip would have anchored the WRONG predecessor); its quarantine named a typed refusal the shipped `(SeatMeta, bool)` contract cannot express and promised an operator-submitted exit that is UNREACHABLE in the sole-operator state (admin `-mint` genesis-only); and three stale rev7 clauses contradicted the new bytes.
The folds, by finding:

1. **R8-F1 → §4a (completeness, not presence):** the system anchor is authorized ONLY by the pinned mechanical proof, per seat: P = the seat's accepted legacy pivot relayIDs from the CANONICAL fold; over the RAW pre-dedup redo stream, every member of P appears EXACTLY ONCE (zero = omission, >1 = duplication corruption — both INCOMPLETE) AND the segment sequence is intact (a numbering gap, an unreadable/vanished segment, or a torn non-final entry ⇒ INCOMPLETE); non-P entries are ignored for selection.
   Only then is the tip = the last member of P in redo order (append-only ordered segments make the surviving P-subsequence a faithful subsequence of true commit order — with ALL of P present, its last element IS the ruled latest). **ANY deficiency ⇒ no automatic anchor, §4c quarantine, operator anchor** — uncertainty never auto-selects.
   Pass mechanics pinned: pre-channels on every startup until no multi-legacy seat lacks an anchor; evidence snapshotted ONCE per pass before any anchor commits; anchors are ordinary serialized-loop canonical records; crash-mid-pass restarts idempotently (anchored seats skip via the canonical fold; the legacy P-subsequence is append-invariant); **redo GC/disposal is GATED while any multi-legacy seat lacks an anchor**. The completeness rule + system-stamped selection authority route to m-1 as a DISTINCT ruling (your item 4 verbatim — accepted-canonical-wins alone does not authorize noncanonical redo to select an operator pivot). T-R7F1 extends with your full evidence matrix (complete / absent / malformed / partial-omitting-latest / partial-omitting-older / duplicate / segment-gap — only proven-complete anchors) + the crash-mid-pass leg.
2. **R8-F2 → §4c (the executable contract + total recovery):** quarantine membership = DERIVED STATE from the canonical chain-status fold, computed pre-serve and recomputed on the serialized loop at every commit touching a seat's chain/anchor/binding, published to the seat manager; `Resolve`'s contract WIDENS beyond `(SeatMeta, bool)` to carry the typed refusal, **match-gated: `auth:seat-quarantined` ONLY when the presented credential matches the seat's bound credential AND the seat is quarantined; every non-matching credential stays `auth:invalid-credential`** (no probe learns seat existence/status — the leak rule, in m-1's scope). Clearing is atomic with the resolving commit (same serialized-loop step updates the derived status; repair replaces the binding; no cached refusal survives).
   Recovery is TOTAL across your three shapes: (a) conflicted worker — any live operator anchors in-band; (b) conflicted operator with a second live operator — the other submits; (c) **sole-operator conflict — the LOCAL ADMINISTRATIVE ANCHOR**: a new admin verb on the store host, the SAME trust basis as genesis `-mint` (local store access IS operator authority), committing the anchor/disposition as an ordinary canonical record via the serialized loop, system-marked admin-authored, **accepted only for an operator-role AND quarantined target** (never a general bypass); repair mints + delivers via the admin direct reply (the genesis-delivery precedent). No in-band path for the quarantined credential (break-glass rejected — the zombie would act); no binding mutation as truth. T-R8F2 covers all three shapes with your reachability + never-authenticates + leak-distinction assertions.
3. **R8-F3 → the stale clauses removed:** `:25`'s "beyond the already-bound one" replaced by the sole quarantine rule (Resolve refuses EVERY credential of a conflicted seat, the bound one included); `system-owned` used consistently (the `:12` token corrected); the owner-confirmation header REWRITTEN as the FINAL enumeration — m-1 six parts (callerless contract · pivot chain · the completeness-verified-redo ruling · system-vs-operator anchor authority · the quarantine incl. the leak rule · the sole-operator recovery authority basis), m-2 (both header registry rows + exact rejection bytes/`system-owned` class · transition rejection classes · the disposition, anchor, and sole-operator recovery forms); §9 metadata refreshed (15 entries, provenance per rev), entries 14–15 added with rejected alternatives, entry 11 corrected to name the proof.

**Preserved (your accepted portions untouched):** the accepted-only forged-header fold and preserve-as-evidence bytes; the credential-quarantine security direction; zero/one-legacy unambiguity; the commit-time transition discipline + order-free rebuild; the linked-chain tip predicate; the header/version carrier with fail-closed unknowns; fresh instances; Class-G; the evidence fold; the projection; the consumer table; the master/VP-before-IMPL sequence.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev9 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP returns the bytes to master for the master/VP pass + the routing of the two final-scope owner confirmations; the IMPL branch only after that pass and those confirmations; the operator merge grant unchanged at the end.

## Verification

- Your r8 review read in full; every required-revision item of R8-F1..F3 mapped above; the R8 loci re-verified before folding (`store.go:119-135, 362-394` CommitOrder/readRedo tolerance; `projections.go:21-57` redo-as-acceleration; `binding.go:121-130` the `(SeatMeta, bool)` contract; `server.go:277-301` the false→`auth:invalid-credential` mapping; `main.go:577-600, 677-693` genesis-only `-mint`) and added to the design Basis line.
- The lane swept before this assertion (eighteen relays; your r8 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `921eb0587c9f0d1d35695a104cfd75e17c74b7e3e9942bd0f88a97b5731b9166`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-213611.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design revised to rev9 (targeted edits; all r8-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260720-213611; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev9 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass + the m-1/m-2 final-scope confirmation routing; the IMPL branch only after that pass.
