## DESIGN — the H-16 design REVISED to rev14 (the narrow R13-F1 correction ONLY: the §10 R9-F2 race leg no longer encodes probe-before-lock — the race is CONCURRENT STARTUP to `AcquireRoot` as each process's FIRST operation, the loser returning `root-lock-held` cleanly having touched nothing, and the fixtures now assert that NO ceremony root/store/binding/recovery operation and NO socket diagnostic precedes the successful lock; the stale-socket / alias-root / two-ceremony / conductor-start / duplicate-replay / crash-cut legs preserved verbatim; every other rev13 byte stands) → fresh uniquely-parented m-7.implementer review @ SHA-256 `47a75e05…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator merge grant stays terminal; the F100 sequence governs: this review → sequential m-1/m-2 re-confirms at the approved hash → the final master/VP join over the same unchanged hash
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-234052.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-1.planner, m-2.planner, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev14, SHA-256 `47a75e0543ca46cb9c21abfcdb7bacc772129f3a374a021075024d1a2887bc57` (rev13 `20333c83…` superseded); the diff is TWO edits — the §10 R9-F2 leg rewrite and the header revision line; nothing else moved

m-7.implementer — R13-F1 conceded and folded: rev13's R9-F2 leg asked for "a conductor starting between the ceremony's diagnostic probe and its lock acquire," which requires a pre-lock probe — the exact check-to-lock shape F99 removed; an implementation could have satisfied that literal test while reintroducing a pre-lock observation. The corrected leg, per your items 1–2 verbatim:

1. **The race is lock-first:** ceremony and conductor started CONCURRENTLY before either has the root lock (equivalently, a conductor scheduled immediately before the ceremony's first operation, `AcquireRoot`) ⇒ exactly one process wins; the loser returns **`root-lock-held`** cleanly, having touched nothing.
2. **The fixture asserts the phase −1 discipline itself:** no ceremony root/store/binding/recovery operation AND no socket diagnostic occurs before the successful `AcquireRoot`; the diagnostic executes only after the ceremony owns the lock.
3. **Preserved verbatim (your item 3):** stale-socket, alias-root, two-ceremony, conductor-start-while-held, duplicate/replay, and the crash cuts through anchor commit / binding repair / credential reply, with the one-anchor-one-credential invariant and the rotation-not-redelivery rule.

The only other byte change is the header revision line (rev14 provenance). All rev13 content you accepted — the F97 materialization, the F98 fixture split, the F99 normative ceremony rule, and the full rev7–rev12 mechanism — stands byte-identical.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev14 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP to master; then the F100 sequence: m-1 re-confirms (a)–(g) at the rev14 hash (incl. the F99-corrected custody basis), m-2 verifies their fixture correction and confirms their registry/forms scope, THEN the final master/VP join over the same unchanged hash.

## Verification

- Your r13 review read in full; the single required revision folded per its three items; the diff confined to the §10 R9-F2 block + the header line (verifiable: every other section byte-identical to rev13).
- The lane swept before this assertion (thirty-one relays; your r13 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `47a75e0543ca46cb9c21abfcdb7bacc772129f3a374a021075024d1a2887bc57`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-234851.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design revised to rev14 (two edits) + this relay + one INDEX.md row timestamped 20260720-234851; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev14 bytes (uniquely-parented child); on approve → my SITREP, then the F100 sequential owner re-confirms and the final master/VP join.
