## RECONCILE — VP F97–F99 to m-7 (from the VP's rev12 pass `224710`; the rev7–rev12 mechanism direction + both owners' substantive rulings are PRESERVED as input — the corrections are completeness, one fixture, and one lock-linearization): publish rev13 as ONE SELF-CONTAINED normative document (F97 — no bare `unchanged` pointer may carry required semantics; the superseded-hash bases have NO retrievable bytes, so the delta-shell form binds nothing) + fold H16-M2-F1 (F98 — the forged-header fixtures must run through the raw shared `channel.Client`, asserting the conductor's committed `system-owned` rejection; conforming native/MCP fixtures assert typed client-side NO-CALL) + the offline-ceremony lock correction (F99 — `store.AcquireRoot` BEFORE any root/store/binding/recovery touch, held through exit; socket liveness = post-lock diagnostic ONLY; and the `-mint` precedent is FALSE at the live code — dispose it explicitly)

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator merge grant stays terminal; all H-16/stage-6/T4 gates held per the VP's disposition
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/RECONCILE-orchestrator-reviewer-20260720-224710.md
FROM: master.orchestrator-planner
TO: m-7.planner
CC: m-7.implementer, m-1.planner, m-2.planner, master.orchestrator-reviewer, operator
SUBJECT: the three items + the corrected sequence — (F97) rev13 materializes the FULL contract: sections 1–10 complete, all 21 decision entries, the five-site census, the durable work identity/cursor/recovery machinery, the complete migration + per-hook idempotency tables, the full T1–T18+R matrix — the operative `050307` bar verbatim, no pointer shells · (F98) m-2's own catch: the generated-schema gate rejects system-owned fields CLIENT-side (`schema_invalid`, no conductor call), so rev12's both-frontends-produce-committed-rejections test is IMPOSSIBLE as written — the raw-client fixture split is the fix · (F99) the live `-mint` path (`frank/cmd/frank/main.go:112-122` + `:577-600`) probes/mints BEFORE `store.AcquireRoot` — it is NOT a lock precedent; your ceremony contract must acquire the root lock as phase −1 per the locked s6 law, add the check-to-lock race / stale-socket / alias-root / two-ceremony / conductor-start race cuts, and EITHER converge `-mint` onto the same offline-admin lock helper in this design's scope OR name `-mint`'s unlocked writer as an explicit prerequisite defect for master's backlog — never cite it as locked trust

m-7 — sequence per the VP's F100 (the parallel fan-out is DEAD; my election of it was half-right and the reciprocal void was missing — m-2's finding proved it): rev13 (self-contained, F97+F98+F99 folded) → the fresh uniquely-parented pair review → THEN m-1/m-2 re-confirm the exact rev13 hash (m-1 re-rules the corrected custody basis per F99; m-2 verifies their fixture correction landed) → THEN the final master/VP join over the same unchanged hash. Only that joined record closes F93 and feeds the census/lock packet. If you elect the name-the-defect branch on `-mint`, say so in the SITREP and master records it as a bounded backlog item (the H-16 IMPL may then fix both under one lane if the operator wills it).

ACTIONS_GIT_REF: docs-workspace disk action — this routing relay + one INDEX.md row; no design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: m-7 publishes rev13 + fresh pair review + SITREP; master routes the sequential owner re-confirmations, then the final join.
