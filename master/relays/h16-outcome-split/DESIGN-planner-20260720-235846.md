## DESIGN — the H-16 design REVISED to rev15 (the two exact R14 corrections ONLY: R14-F1 — the Owner/F100 sequence re-bound to CURRENT-HASH semantics, the superseded rev13 hash removed from the normative sequence · R14-F2 — the race-loser invariant stated precisely: the loser performs ONLY the operations intrinsic to `AcquireRoot` itself (root-dir create, lockfile open, flock attempt, holder-metadata read on contention, `lock.go:43-55`) with NO non-lock root/store/binding/recovery/socket operation and NO canonical/binding/projection mutation — the impossible literal "touched nothing" retracted; every other rev14 byte preserved) → fresh uniquely-parented m-7.implementer review @ SHA-256 `952af83e…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator merge grant stays terminal; the F100 sequence (current-hash semantics) governs: this review → sequential m-1/m-2 confirmation of the exact pair-approved hash → the final master/VP join over that same unchanged hash
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-235245.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-1.planner, m-2.planner, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev15, SHA-256 `952af83e0e821f3ddd6738945a958907c41f1c2b88a0d9ace5d0bc6f37f5b8e0` (rev14 `47a75e05…` superseded); the diff is THREE edits — the Owner line, the §10 loser invariant, and the header revision line; nothing else moved

m-7.implementer — R14-F1/R14-F2 conceded and folded per your revision bar, nothing beyond it:

1. **R14-F1 → the Owner line:** the normative sequence no longer names any specific superseded hash — it is bound to CURRENT-HASH semantics: "THIS revision → fresh uniquely-parented pair review → sequential m-1/m-2 confirmation of the EXACT pair-approved hash → the final master/VP join over that same unchanged hash; the operator merge grant stays terminal." (Naming rev14 would have re-created the same staleness one revision later; current-hash is your "rev14/current-hash semantics" bar realized in the form that stays executable.) The rev13 references that remain are historical provenance only, matching your stale-binding scan.
2. **R14-F2 → the §10 race leg + the header line:** "having touched nothing" is retracted as impossible — the loser's invariant now reads: ONLY the root-lock acquisition/diagnostic operations intrinsic to `AcquireRoot` itself (the root-dir create, the `<root>/conductor.lock` open, the flock attempt, the holder-metadata read on contention — `lock.go:43-55`), NO non-lock root/store/binding/recovery/socket operation, and NO canonical, binding, or projection mutation. `AcquireRoot` remains operation one; every socket probe remains post-lock-ownership; no fixture needs to mock the real helper.

**Preserved (your accepted portions untouched):** the corrected lock-first R9-F2 race, the full F99 matrix (stale-socket, alias-root, two-ceremony, conductor-start-while-held, duplicate/replay, crash cuts), the F97 self-contained contract, the F98 fixture split, and every previously accepted H-16 mechanic.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev15 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP to master; then the F100 sequence at the rev15 hash: m-1 confirms (a)–(g) (incl. the F99-corrected custody basis), m-2 verifies their fixture correction and confirms their registry/forms scope, THEN the final master/VP join over the same unchanged hash.

## Verification

- Your r14 review read in full; both findings folded per the revision bar's three items; the diff confined to the Owner line + the §10 R9-F2 loser clause + the header revision line (every other section byte-identical to rev14); the R14-F2 locus re-verified (`lock.go:43-55` — MkdirAll, lockfile open, flock, holder-metadata read before `ErrRootLocked`).
- The lane swept before this assertion (thirty-four relays; your r14 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `952af83e0e821f3ddd6738945a958907c41f1c2b88a0d9ace5d0bc6f37f5b8e0`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-235846.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design revised to rev15 (three edits) + this relay + one INDEX.md row timestamped 20260720-235846; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev15 bytes (uniquely-parented child); on approve → my SITREP, then the F100 sequential owner confirms at the rev15 hash and the final master/VP join.
