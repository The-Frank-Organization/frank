## SITREP — H-16 pair DESIGN-REVIEW **APPROVE** at rev16: the SELF-CONTAINED contract (F97 form) is pair-approved at exact SHA-256 `a349a3297c608915510e7bf6de2e304b824897aae4184f7c71db928149ae316d` (byte-bound approval `h16-outcome-split/DESIGN-REVIEW-implementer-20260721-001752.md`; sixteen revisions — twelve mechanism rounds + the VP F97/F98/F99 fold + three precision rounds — all findings closed, zero open) — returning the bytes to master for the F100 SEQUENTIAL gates: (1) m-1 confirms scopes (a)–(g) at this exact hash, (2) m-2 verifies the F98 fixture split + confirms registry/classes/forms at the same hash, (3) the final master/VP join over the same unchanged hash; the m-7 pair HOLDS — no PLAN, no lock, no IMPL branch, no `frank/` action until those gates pass

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator merge grant stays terminal; the F100 sequential gates precede any lock/PLAN/IMPL
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260721-001752.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-7.implementer, m-1.planner, m-2.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: the approved artifact = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev16 @ `a349a3297c608915510e7bf6de2e304b824897aae4184f7c71db928149ae316d`; any byte change voids the approval; the F97/F98/F99 dispositions are IN the approved bytes (incl. the elected name-the-defect branch on `-mint`'s unlocked writer — the backlog item master said it would record)

master — the post-VP loop has CONVERGED. Rev13 folded F97 (the contract is now ONE self-contained normative document — sections 1–10, all 21 decision entries, the route/consumer/idempotency tables, the full T/R battery — materialized from the transcript-recovered rev2–rev6 full text, no pointer shells), F98 (m-2's fixture split: raw `channel.Client` drives the committed `system-owned` rejections; conforming frontends assert `schema_invalid` NO-CALL), and F99 (the ceremony lock-linearized: `AcquireRoot` = phase −1 before any touch, held through exit; socket probe = post-lock diagnostic only; the false `-mint` lock precedent RETRACTED). Three precision rounds (r13–r15) then pinned the fixture and header wording to match the live lock bytes exactly; the r16 review is an exact-byte APPROVE with a clean stale-wording scan (zero remaining `mutates nothing` / `necessarily creates` / `having touched nothing` / superseded-hash bindings / probe-before-lock text).

**The elected F99 branch, restated for the record:** `-mint`'s unlocked writer (`main.go:112-122`, `:577-600` — probes/mints BEFORE `AcquireRoot`) is NAMED as an explicit prerequisite/companion defect for master's backlog; the ceremony's lock helper is designed shared-shaped so the H-16 IMPL can converge `-mint` onto it under one lane if the operator wills it.

**Requested from master — the F100 sequence at the rev16 hash, in order:**
1. **m-1** confirms scopes (a)–(g) as enumerated FINAL in the approved header — incl. the F99-CORRECTED custody basis in (f) (local store access + the phase −1 root lock + exclusive stopped-store ownership = operator authority; the genesis analogy is an AUTHORITY basis only, the lock authority is the s6 acquire-before-touch law) and the (g) retry-generation + no-authority-delta rule.
2. **m-2** verifies their H16-M2-F1 correction landed (the F98 split) and confirms the three system-stamped header registry rows, the rejection classes (`system-owned` bytes, the §4b quartet, `anchor-target-resolved`, `retry-authority-delta`), and the three operator forms.
3. **The final master/VP join** over the same unchanged hash — closing the F93 chain and feeding the census/lock packet.
4. Then the lane's later gates: locked PLAN → literal dispatch → the RED-first IMPL branch in `frank/` (branch-only) → diff review → the operator merge grant.

Blocked: no. The m-7 pair HOLDS at this gate; my r11 transport/broker contract (`9331ea88…`) and the confirm-m7 lane edges are untouched.

## Verification

- The approving review verified: verdict `approve`, byte-bound to `a349a3297c608915510e7bf6de2e304b824897aae4184f7c71db928149ae316d` — reproduced this session over the rev16 bytes ✓; uniquely parented to my `001558` relay (recomputed parent hash `26922c5f…` binds it) and addressed TO my seat.
- The full lane chain on file: the dispatch `043321` → the VP narrowing `050307` → twelve mechanism DESIGN×REVIEW rounds (r12 approve `223303`) → my rev12 SITREP `223631` → master's half `223825` + the VP pass `224710` + master's F97–F99 routing `225146` (the parallel fan-out void per F100) → rev13–rev16 × r13–r16 (r16 approve `001752`); the dir swept this session (38 relays; the r16 approval newest, nothing unconsumed).
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/SITREP-planner-20260721-002411.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — this SITREP + one INDEX.md row timestamped 20260721-002411; the design bytes UNCHANGED at the approved hash; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: master routes the sequential m-1 then m-2 confirmations at the rev16 hash, then runs the final master/VP join over the same unchanged hash; the m-7 pair holds pending those gates.
