## DESIGN — the H-16 fix design note AUTHORED: the monotonic split as an ADDITIVE shape (`State` = the decision dimension, byte-exact enum unchanged; new `post_commit_state ∈ {complete, pending, failed}`), a minimal single-slot retry/park machinery — with the ONE load-bearing discovery that `AfterAccepted` must PARK, never retry (its extras are the unpersisted mint delivery; a retried mint issues a credential nobody receives) → m-7.implementer concurrence @ SHA-256 `eb7a4bd3…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the MERGE grant per the lane dispatch; nothing below authorizes a merge
GRILL_REQUIRED: no — per the dispatch; a bounded defect fix inside the existing outcome contract
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-orchestrator-planner-20260720-043321.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: the note = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md`, SHA-256 `eb7a4bd3973991666360b701429c94a13cfb34f0ded98ada4abb8a3552014969`; grounded at the CURRENT `frank@6e4d657` (all five defect sites re-verified at these bytes, incl. the `supersededCredentialOutcome` label-consistent-by-luck variant); per the dispatch I will CHECK THIS LANE for the VP's parallel narrowing before any IMPL begins

m-7.implementer — the fix design note is authored; concurrence requested.
The shape decisions, with the reasoning you should attack:

1. **Additive, not versioned (§2):** the old `State` field IS `decision_state` — fixed at commit, never relabeled; the wire enum `{accepted, rejected, held}` byte-unchanged; new `post_commit_state` (omitempty) with empty ⇒ semantically complete for pre-split emitters.
   Rationale: the five sites were LYING — fixing a lie is not a compat break, and a versioned Outcome would fork every consumer for five call sites; no in-tree caller takes authority-bearing action on the old `obligation-error`/`derived-work-error` rejections (verified).
2. **The fault machinery (§3) is deliberately minimal:** one pending-derived slot on the serialized loop (no locking, no new goroutine); the retry set = {completeTurn, AfterGateResolution, AfterApprovalResolution} (idempotent by contract, asserted by test); retry at the top of each `process` + on replay; slot turnover parks the older entry as ONE typed system record in the existing `faultOutcome` family shape — no new record kind, no store schema change.
3. **The discovery — `AfterAccepted` parks IMMEDIATELY, never retries:** its extras (credential/endpoint) are the mint DELIVERY, exist only in the direct return, and are never persisted (F-S6-M1-1) — a later re-run mints a credential no caller receives.
   The committed-mint-without-credential window is the already-ruled s6 crash window (`realized_mint_ref` completion provenance + operator re-mint); the fix makes it VISIBLE (`accepted` + `failed` + no extras), never re-executes delivery.
   Grill this hardest: is the park-vs-retry partition of the four hooks exactly right, and is there any hook-failure interleaving the single slot mishandles beyond the turnover rule?
4. **Replay coherence (§4):** `outcomeFromRecord` derives the dimension — retry-first (a healing replay reports `complete`), `pending` from the live slot, `failed` from the parked record via the tables (store-rebuilt ⇒ the crash/restart cut is durable).
5. **Fail-closed compat (§5):** old callers now read the TRUTH in `state`; the one genuinely fail-closed case (accepted-with-missing-extras) is closed by the immediate `failed` on the same outcome + the existing s6 provenance; the T4 consumption rule is stated for the m-9/m-10/m-8 lanes.
6. **The test battery (§6):** T1–T8 RED-first, incl. the dispatch's crash/retry cut (T2), the AfterAccepted no-re-run negative (T4), and the additive-decode proof (T7); INV-CATALOG untouched (T8).

### Requested
Concurrence (or findings) on the note at the hash above.
On concurrence: I check this lane for the VP's parallel narrowing, then IMPL proceeds on a `frank/` branch per the dispatch sequence (branch + tests → your uniquely-parented review of the diff → SITREP with branch/commit + test evidence → the operator merge grant).
No merge, no main-branch write, no lock is authorized by this relay.

## Verification

- The lane dispatch read in full; all five defect loci re-verified at `frank@6e4d657` this session (`loop.go:168-187` the four `process` sites, `:279-281` the superseded path, `:24-32` the Outcome shape, `:136/:153/:299-314/:346-364` the replay truth path); the extras-never-persisted fact verified at `outcomeFromRecord` (no Credential/Endpoint on any replay).
- The dispatch dir swept before this assertion (`h16-outcome-split`: the dispatch the newest record; the standing confirm/design lanes quiescent).
- `shasum -a 256 <note>` = `eb7a4bd3973991666360b701429c94a13cfb34f0ded98ada4abb8a3552014969`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-044605.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design note (new file, my domain) + this relay + one INDEX.md row timestamped 20260720-044605; no code, no `frank/` action (the lane authorizes branch work only after concurrence + the VP-narrowing check).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer returns concurrence (or findings) on the note; on concurrence I check the lane for the VP narrowing, then open the IMPL branch per the dispatch sequence.
