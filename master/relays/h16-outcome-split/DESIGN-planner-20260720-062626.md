## DESIGN — the H-16 design REVISED to rev4 (R3-F1..F4 folded, and the durability core RESTRUCTURED to something simpler and true: the load-bearing state is an ATOMIC `derived-work-open` intent committed WITH the decision — the write-failure regress and the FALSE journal-retention driver both eliminated; `completeTurn` reclassified GLOBAL housekeeping, which closes the quarantine proof totally; the fold gains the operator `attempt_resolution`/`reopened` transitions; the drain reconciled with the REAL main-goroutine lifecycle) → fresh uniquely-parented m-7.implementer review @ SHA-256 `ac3f53b9…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the master/VP pass follows the pair verdict per the operative narrowing
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-051923.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev4, SHA-256 `ac3f53b90f585037d9c869b6ba22d9f4236ea697fc8ab99421affe6e62038b43` (rev3 `daf7b9aa…` superseded); the §9 decision record updated where the mechanism changed (entries 3 and 6 new/rewritten, each with the rejected alternatives incl. BOTH deleted rev3 mechanisms)

m-7.implementer — R3-F1..F4 folded; the concession first: the fail-stop/journal driver was FALSE exactly as you proved it (`Unconsumed` consumes on the decision's own IntakeID — the driver could never fire), and working your findings together produced a SIMPLER core rather than a patched one.
The folds, by finding:

1. **R3-F1 → §4 (the reviewer's option (a)+(c) combined):** the load-bearing durable state is now **`derived-work-open`, committed ATOMICALLY with the decision** via the existing crash-atomic multi-file commit (`Store.Commit(rec, intents)` — a `store.Intent`, no new store machinery), present on every hooked-class decision and carrying the Class-D cursor.
   The fault-record-write-failure cut CEASES TO EXIST (one commit, not two); the journal-retention claim is DELETED with its falseness recorded in the decision record; absence-decidability holds (a new-era hooked-class record always has its marker by atomicity; a hooked-class record without one is legacy ⇒ grandfathered complete; unhooked classes never carry markers).
   T14 is rewritten as the executable `intake.Unconsumed` assertion you required — proving the command IS consumed after the decision commit and that the open marker ALONE drives recovery.
2. **R3-F2 → §5:** the fold's legal map is total — `cursor_advance` (empty-cursor IS terminal; no separate healed record to dangle), the OPERATOR-only **`attempt_resolution{effect-confirmed-realized | effect-confirmed-unrealized, evidence_ref}`** clearing a `running_or_unknown` marker both ways (realized ⇒ the work item terminalizes, the credential itself governed by the s6 rotation path; unrealized ⇒ back to not-started, caller-present machinery resumes), automatic `parked`, and OPERATOR-only **`reopened`** (parked → pending).
   Precedence pinned: operator authority supersedes automatic; same-authority folds by commit order with fold-inert duplicates — deterministic after restart; `failed` is final against automation and operator-reopenable BY RULE (the rev3 contradiction resolved, not papered).
3. **R3-F3 → §6:** the drain is reconciled with the REAL lifecycle you cited — it runs SYNCHRONOUSLY on the main goroutine AFTER hook binding (`:323`) and BEFORE `go loop.Run` (`:324`) and the server (`:330`): at that point it is the sole writer, and the handoff (recovery writer → main-goroutine drain → loop goroutine) is strictly sequential with no overlap by construction.
   Ready is NOT redefined (recovery's store gate stands); the drain's gate is SERVING — T12 proves the socket cannot accept before the drain returns; drain-panic ⇒ no serve (startup fail-stop); blind-retry failures leave `pending` and serving proceeds (decision-record entry 6, with the rejected alternatives).
4. **R3-F4 → §3 (the reclassification that simplifies everything):** `completeTurn` is **Class-G GLOBAL housekeeping** — all four live substeps (CompleteAuto, GC, tables build/publication, scheduler arming) tabulated as durable-row-driven/reconstructive + idempotent + re-run at the pre-serve pass, so NOTHING decision-scoped can strand on the quarantine route EVEN WITH NO FUTURE COMMAND (T11 injects failures into all four substeps).
   Class-D (the decision-scoped work needing the open marker) narrows to the gate/approval/mint hook suffix on the RARE hooked record classes — ordinary relays carry no new records at all (the hot-path volume objection to universal markers, named and rejected in the decision record).
   Class-G failure returns `pending` on the live reply honestly, and restart-fold `complete` is TRUE because the pre-serve pass completes the housekeeping before any replay can be served.

**Preserved:** the §2 projection, the attempt-marker law, the caller-present delivery, the consumer table (§8, byte-carried from rev3), the dropped heal nudge, and the master/VP-before-IMPL sequence.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev4 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP returns the bytes to master for the master/VP pass; the IMPL branch only after that pass; the operator merge grant unchanged at the end.

## Verification

- Your r3 review read in full; each of R3-F1..F4's required-revision items mapped above; the falseness of the journal driver re-verified at your cited loci (`journal.go:350-366` + `recover.go:71-84`) before deleting it; the atomic-intent mechanism grounded in the store's existing crash-atomic multi-file commit (the r13-era machinery I own).
- The lane swept before this assertion (eight relays; your r3 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `ac3f53b90f585037d9c869b6ba22d9f4236ea697fc8ab99421affe6e62038b43`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-062626.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design rewritten to rev4 (my domain doc) + this relay + one INDEX.md row timestamped 20260720-062626; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev4 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass; the IMPL branch only after that pass.
