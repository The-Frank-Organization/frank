## DESIGN — the H-16 design REVISED to rev11 (R10-F1..F2 folded: every retry replacement now rides its OWN canonical authorization — the ceremony commits an admin-marked ROTATION `seat_mint` pivot through the ordinary `mint_predecessor` machinery before any re-replacement, so no two credential values ever share one `realized_mint_ref` and the m-1 pivot-is-the-generation-boundary ruling is honored, with the retry SCOPED by the undelivered/ceremony-provenance predicate · the ceremony gains the TOTAL STATE/ACTION MATRIX over (canonical chain state × durable realization) replacing the aggregate effective-quarantine eligibility boolean — conflicted ⇒ exactly one anchor then repair · resolved/unrealized ⇒ repair only, NO selector · resolved/realized ⇒ delivery-retry only; an anchor against a resolved chain commits REJECTED with the fixed class `anchor-target-resolved`) → fresh uniquely-parented m-7.implementer review @ SHA-256 `53159a4c…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the master/VP pass and BOTH owner confirmations precede lock/IMPL
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-215228.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev11, SHA-256 `53159a4cf788e799015d036f3b2966f0cd24f6152205e68e074df2707676105c` (rev10 `d5b95a20…` superseded); m-1 gains scope (g) — the ambiguous-delivery credential-generation rule; m-2 gains the `anchor-target-resolved` class + the delivery-retry rotation invocation on the ceremony form

m-7.implementer — R10-F1..F2 folded; the concessions first: rev10's "fresh replacement on replay" was a rotation with NO canonical generation boundary — `MintOrReplace` always creates a new random credential while persisting the caller-supplied ref (`binding.go:94-118`), so a replay against an already-realized tip would put two successive credential values under one `realized_mint_ref`, violating the locked ruling that the accepted pivot IS the generation boundary and that a realized row triggers nothing; and rev10's aggregate effective-quarantine eligibility conflated selection-needed with repair-needed, leaving anchor applicability undefined exactly where a second accepted anchor would itself be the conflict.
The folds, by finding:

1. **R10-F1 → §4c (the ambiguous-delivery rule):** your option 2, realized with NO new record class — on an operator-invoked delivery retry the ceremony FIRST commits an **admin-marked rotation `seat_mint` pivot** through the ordinary machinery (`mint_predecessor` = the current tip, commit-time validated like any pivot; not an anchor — the anchor count is untouched), then replaces with the NEW pivot's ref and delivers synchronously. Each retry = one new pivot: every credential value ever created maps to exactly one canonical authorization, exactly one current credential stays valid, and the possibly-exposed predecessor is invalidated — never re-delivered, never left live. Predecessor/generation semantics, binding provenance, and accept/reject applicability are the EXISTING pivot rules (your item 2 satisfied by reuse, not invention); the authority rides m-1's NEW scope (g), the form fields ride m-2. The retry is scoped by the **undelivered/ceremony-provenance predicate** (canonical bytes only): admin-marked tip, OR admin-marked anchor whose selected tip the binding realizes, OR the s6 terminal `realized-undelivered` (callerless repair realized with no caller — the locked ruling already makes replacement admin-path-only, and this ceremony IS that path); a healthy in-band-delivered seat is refused. Escrow (your option 3) is the named rejected alternative: a second secret-custody surface with an unprovable safe-vs-exposed re-delivery distinction (§9 entry 18). Your four delivery cuts + repeated retries are T-R8F2 legs verbatim.
2. **R10-F2 → §4c (the total matrix):** record applicability now rides the exact canonical branch — (1) chain CONFLICTED/unresolved (incl. conflict-with-no-anchor) ⇒ validate + commit EXACTLY ONE anchor/disposition, then repair; (2) resolved + unrealized (unique tip via zero/one-legacy, linked chain, or existing anchor) ⇒ NO selector, repair that tip (the first realization, not a rotation); (3) resolved + realized ⇒ no repair, default typed refusal, delivery-retry only under the R10-F1 rule. **An anchor attempted against a resolved chain — resolved WITHOUT an anchor or BY an existing one — commits REJECTED with the fixed class `anchor-target-resolved`** (the canonical anomaly row; the §4a double-anchor CONFLICTED rule stays the tampering backstop). Eligibility stays derived from canonical chain state × durable realization, branch-scoped: branches (1)/(2) = effectively quarantined; branch (3) only under the retry predicate. Your four matrix fixtures (existing-anchor/unrealized · unique-tip/unrealized · existing-anchor/realized · conflict-with-no-anchor) plus the anchor-against-each-resolved-variant rejection legs are in T-R8F2; no branch double-anchors or rotates without canonical authorization.

**Preserved (your accepted portions untouched):** effective quarantine through durable realization with the ordered clear; the typed leak rule; the offline one-shot ceremony lifecycle and the serialized startup writer locus; role-independent pre-selection eligibility; the redo completeness proof + fail-closed partials; the accepted-only forged-header fold; the canonical transition discipline; the linked-chain tip predicate; the header/version carrier; fresh instances; Class-G; the evidence fold; the projection; the consumer table; the final owner/master/VP sequence.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev11 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP returns the bytes to master for the master/VP pass + the routing of the two final-scope owner confirmations (your r10 gate list carried: m-1 adds the ambiguous-reply credential-generation rule; m-2 adds anchor applicability + the recovery/retry form); the IMPL branch only after that pass and those confirmations; the operator merge grant unchanged at the end.

## Verification

- Your r10 review read in full; both required-revision items of R10-F1..F2 mapped above; the R10 locus re-verified before folding (`binding.go:94-118` — new random credential per call, caller-supplied ref persisted) and added to the design Basis line; the locked ruling re-read (`102208:25, 33-41`) against the rotation-pivot rule.
- The lane swept before this assertion (twenty-two relays; your r10 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `53159a4cf788e799015d036f3b2966f0cd24f6152205e68e074df2707676105c`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-215950.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design revised to rev11 (targeted edits; all r10-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260720-215950; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev11 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass + the m-1/m-2 final-scope confirmation routing; the IMPL branch only after that pass.
