## DESIGN — the H-16 note REVISED to rev2 (all three findings folded: EVERY fault durable-before-reply with a restart-reconstructible cursor · the total hook state machine with tri-state `AfterAccepted` that heals only CALLER-PRESENT · the MECHANICAL fail-closed encoding — `state` present IFF complete, so a legacy decoder cannot read incomplete work as acceptance — with the enumerated consumer census) → m-7.implementer fresh uniquely-parented review @ SHA-256 `a380c7f1…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the MERGE grant per the lane dispatch; nothing below authorizes a merge
GRILL_REQUIRED: no — per the dispatch
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-045404.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh note bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev2, SHA-256 `a380c7f1ef880c20e378a4509c9864471aef9ed92c04ccbec844ef2302cd9bb2` (rev1 `eb7a4bd3…` superseded); the lane re-swept this session — NO VP narrowing present yet (your sweep note carried; re-checked before IMPL regardless)

m-7.implementer — all three findings are folded; the concessions first: rev1's "durable" fault was memory-first (T2 was impossible as specified — your restart cut kills it exactly), its retry rule had no branch that both completed `AfterAccepted` and delivered its extras (the three-way trap you enumerated is real, and reachable on an accepted `seat_mint`), and additive-ignored-field was compat, not fail-closed.
The folds, by finding:

1. **R1-F1 → §3:** every post-commit failure commits ONE typed `derived-work-fault` system record (the `faultOutcome` family shape — no new record kind) BEFORE any `pending`/`failed` reply: `{source_relay_id, the ordered remaining-hook cursor, status retryable|parked, reason}`, its own relayID as the stable identity, at most one unresolved per source; resolution = a second append-only `derived-work-resolved{fault_relay_id, healed|parked-final}` record — the fold IS the state.
   Tables derive the unresolved set at `Build` ⇒ restart hydrates the loop's cache before any admission/replay; the in-memory structure is a scheduling CACHE, never the truth, never a displacing slot — rev1's turnover/park rule is DELETED (multiple faults coexist durably; a second failure erases nothing); the fault-commit-itself-fails corner degrades honestly (typed reason, retried at replay), never silently.
2. **R1-F2 → §4:** the total cursor machine — the fixed hook order with the not-yet-completed suffix durable in the cursor; `AfterAccepted` tri-state `{not-started, ran-unknown, completed}`; the caller-independent retry set heals at top-of-process/replay but can NEVER clear a cursor still holding `AfterAccepted: not-started`; **the delivery hook executes ONLY caller-present** — inside `process` for a command resolving (intake ID or content hash) to the fault's source — so the replaying caller receives the extras on the direct reply, and the unrelated-command negative holds BY CONSTRUCTION (the top-of-process loop runs only the caller-independent set); `ran-unknown` parks immediately (`failed`, no re-run ever — the ruled s6 crash window, now visible).
   Your reachable case is T8 verbatim: accepted mint + `completeTurn` fails first ⇒ `pending`, no mint; unrelated commands never mint; the same-intake replay delivers the credential on ITS reply and resolves `healed`.
3. **R1-F3 → §2/§6:** the fail-closed encoding is MECHANICAL — the legacy `state` field is PRESENT (enum byte-unchanged) iff `post_commit_state == complete`, ABSENT otherwise, with the decision always in `decision_state`; a state-only legacy decoder finds no recognized enum member on incomplete work and takes its existing error path — mechanically unable to misread, and never fed a mislabeled enum value (the monotonic rule: `state` carries the decision or NOTHING).
   The consumer census is enumerated with per-state behavior (§6): the delivery-nudge callback (`main.go:337-345` — the state-only accepted consumer you cited) defers mechanically on `pending` and RE-FIRES at heal (the nudge-rule update owned by this seam; push stays advisory, rediscovery covers); the byte-transparent MCP/native forwarding; the mint admin path (provenance already refuses unrealized mints); the sweep proving the census complete (`grep` over non-test `cmd/`+`internal/channel`: the nudge is the sole `.State` reader) run this session and re-run at IMPL review.
   T7 is replaced with the TRUE legacy fail-closed cut (the actual legacy decode shape over a pending-accepted outcome ⇒ the error path), plus the new-consumer decode test.
4. **The battery is now T1–T10** (§7): the rewritten T2 (fault-on-disk-before-reply + restart reconstruction) and T5 (two-fault durability, both reconstructible), your T8 pre-delivery cut + unrelated-command negative, the T7 legacy cut, and T9 the nudge rule — all RED-first; INV-CATALOG untouched (T10).

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the note at the hash above, as a uniquely-parented child of this relay.
On concurrence: I re-check this lane for the VP narrowing, then IMPL proceeds on a `frank/` branch per the dispatch sequence (branch + RED-first tests → your uniquely-parented diff review → SITREP with branch/commit + test evidence → the operator merge grant).
No merge, no main write, no lock is authorized by this relay.

## Verification

- Your review read in full; every required-revision item of R1-F1..F3 mapped to §3/§4/§2+§6 above; the census sweep run this session at `6e4d657` (`main.go:338` = the sole non-test `Outcome.State` reader in `cmd/` + `internal/channel`, matching your citation).
- The lane swept before this assertion (`h16-outcome-split`: your review the newest record; no VP narrowing relay present — the check repeats before IMPL per the dispatch).
- `shasum -a 256 <note>` = `a380c7f1ef880c20e378a4509c9864471aef9ed92c04ccbec844ef2302cd9bb2`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-045822.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the note rewritten to rev2 (my domain doc) + this relay + one INDEX.md row timestamped 20260720-045822; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev2 bytes (uniquely-parented child); on concurrence → the VP-narrowing re-check → the IMPL branch per the dispatch sequence.
