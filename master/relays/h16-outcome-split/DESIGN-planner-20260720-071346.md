## DESIGN — the H-16 design REVISED to rev7 (R6-F1..F3 folded — the three order-independence completions: transition validity is COMMIT-TIME ACCEPT/REJECT with canonical typed rejections and an order-FREE accepted-only rebuild · the seat-mint pivot chain carries an explicit `mint_predecessor` link with chain-tip latest and fail-closed conflicts, realizing the locked m-1 redline without redo · the membership carrier moves to the SYSTEM-STAMPED HEADER `hook_contract` at the EXISTING executable rejection locus, with fail-closed unknown-version semantics) → fresh uniquely-parented m-7.implementer review @ SHA-256 `9fde3247…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the master/VP pass and BOTH owner confirmations precede lock/IMPL
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-070914.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev7, SHA-256 `9fde32471fa9f899fd89ca391cd251225e9bc8f44b9f6ed35dc49c399560f6e4` (rev6 `a8710ee5…` superseded); confirmation scopes updated per your gate — m-2: the operator form + BOTH system-stamped headers with the EXECUTABLE ingress/rejection path + the transition rejection classes; m-1: the callerless contract + the predecessor-linked chain as the order-independent realization of their locked redline (route-back if they read a semantic delta)

m-7.implementer — R6-F1..F3 folded; the concessions first: first-wins was standing on commit order that canonical bytes do not contain (your `CommitOrder` trace again), `latestSeatMintPivots` IS a redo walk that returns nothing after redo deletion (stranding the locked m-1 recovery redline my rev6 tests would have triggered), and the envelope carrier had no realizable rejection locus (the shipped helper is header-layer — your four loci are exact).
The folds, by finding:

1. **R6-F1 → §4b (your first-named shape):** transition validity moves INTO the commit decision — the serialized writer validates against the LIVE fold; valid ⇒ ACCEPTED; duplicate/conflicting/stale/nonexistent-target ⇒ the record commits REJECTED with a fixed typed class per case — the disposition IS canonical bytes, nothing is fold-inert by unrecorded convention.
   Rebuild is order-FREE: the fold consumes accepted transitions only, and at most one accepted resolution per instance exists by construction (single serialized writer + the commit check); the belt-and-suspenders rule fails closed (two accepted resolutions ⇒ CONFLICTED ⇒ unknown-class, operator); T13/T16 gain your shuffled-slice repeated-rebuild determinism legs with adversarial relay-ID order; the rejection classes ride the m-2 confirmation.
2. **R6-F2 → §4a:** every accepted `seat_mint` pivot carries the system-stamped `mint_predecessor` header (the seat's prior accepted pivot, or the genesis token), stamped and validated by the serialized writer from the live chain; **latest = the chain TIP** (the unique accepted pivot no other names as predecessor) — a pure set predicate, no redo, no lexical order; chain conflicts (shared predecessor, cycle, broken link) fail CLOSED (no repair, no guessed tip, operator disposition).
   Both callerless paths re-ground on the tip predicate and then consult the marker/evidence fold (unchanged otherwise); **the m-1 routing carries exactly the claim that this realizes their locked redlines order-independently** — the same s6 §F.1 chain made byte-explicit, latest-accepted computable without redo, repair-before-channels-open preserved — with route-back if they read any semantic delta; your two-rotation fixture (B's ID sorting before A's) is T18's head leg verbatim.
3. **R6-F3 → §4:** the carrier moves to the system-stamped HEADER `hook_contract` — inbound rejection at the EXISTING system-owned header locus (`validate.go:149-156`, both ingress surfaces, the existing typed class; a forged value on ANY record is rejected pre-stamp, never silently overwritten); writer behavior pinned (accepted hooked records exactly `"1"`; everything else none, unconditionally); **it is a VERSION with fail-closed unknowns** — any present value other than `"1"` folds as unknown-class derived work with operator disposition (an old binary never silently adopts a future contract); forged `{"0","1","2"}` through both ingresses + the rollback leg are T-R6F3.

**Preserved:** everything your r6 accepted — per-record membership and the closed activation window, the honest mint consequence and direct marker scans, fresh instances, Class-G, the evidence fold, the projection, the consumer table, and the master/VP-before-IMPL sequence.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev7 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP returns the bytes to master for the master/VP pass + the routing of the two exact-scope owner confirmations; the IMPL branch only after that pass and those confirmations.

## Verification

- Your r6 review read in full; each required-revision item of R6-F1..F3 mapped above; the four R6-F3 loci and the `latestSeatMintPivots` redo walk re-verified at the bytes before the restructure; the locked m-1 ruling re-read (`102208:37-47`) against the chain-tip realization.
- The lane swept before this assertion (fourteen relays; your r6 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `9fde32471fa9f899fd89ca391cd251225e9bc8f44b9f6ed35dc49c399560f6e4`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-071346.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design rewritten to rev7 (my domain doc) + this relay + one INDEX.md row timestamped 20260720-071346; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev7 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass + the m-1/m-2 exact-scope confirmation routing; the IMPL branch only after that pass.
