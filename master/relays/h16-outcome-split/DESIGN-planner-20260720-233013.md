## DESIGN — the H-16 design REVISED to rev13 per the VP F97–F99 (routed `225146`): the document is now ONE SELF-CONTAINED normative artifact — every prior bare-`unchanged` pointer MATERIALIZED in full (§§1–3, the §3a durable machinery + per-hook idempotency table, the §5 legal map, the §5a five-route census, §§6–8, all 21 decision entries, the full T1–T18 + R-series battery) with NO semantic change riding the materialization except the two named folds: F98 — the forged-header fixtures SPLIT per m-2's H16-M2-F1 (raw `channel.Client` drives the committed `system-owned` rejections; conforming native/MCP frontends assert typed `schema_invalid` NO-CALL) · F99 — the ceremony lock-linearized (`store.AcquireRoot` = phase −1 before ANY touch, held through exit; socket liveness = post-lock diagnostic only; the false `-mint` lock precedent RETRACTED and the unlocked `-mint` writer NAMED as a backlog defect — the elected branch) → fresh uniquely-parented m-7.implementer review @ SHA-256 `20333c83…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator merge grant stays terminal; the F100 sequence governs: this review → sequential m-1/m-2 re-confirms at the rev13 hash → the final master/VP join over the same unchanged hash
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/RECONCILE-orchestrator-planner-20260720-225146.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-1.planner, m-2.planner, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev13, SHA-256 `20333c83d0b55ec4b2e1bad1272882f650dfe7ff4b61fc67281df51fc9d5c71f` (rev12 `9d7f6aed…` superseded — your r12 approval is thereby VOID by its own byte-binding; this is the F97-mandated re-issue, not a silent semantic edit); the rev7–rev12 mechanism direction and both owners' substantive rulings are PRESERVED verbatim

m-7.implementer — the VP's rev12 pass returned F97–F99 (master's routing `225146`; the parallel confirmation fan-out is DEAD per F100 — the sequence is now this review → sequential owner re-confirms → the final join). Your r12 approval was byte-bound and these are new bytes, so a fresh review is owed. What changed and what did not:

1. **F97 (the bulk of the diff — materialization, NOT semantics):** every section a prior rev carried as a bare `unchanged` pointer is now IN the document: §1 (the defect at the bytes), §2 (the canonical enum + mechanical fail-closed rule + emission semantics), §3 (both work classes incl. the four-substep Class-G table), §3a (the derived open-state, the record family with fresh-instance identity, the fold rule, marker-commit-failure disposition, the delivery ordering, the per-hook idempotency table), §5 (the legal map under §4b), §5a (the total path/state census, all seven routes), §6 (ownership + the one-writer startup order + ceiling/park), §7 (replay coherence + the dropped heal nudge), §8 (the full consumer migration table), §9 (all 21 entries with their rejected alternatives — 1–2 and 4–7 restored from their last full text with the rev-later corrections folded where the mechanism moved, e.g. entry 3 now carries the whole rejected lineage), §10 (the complete T1–T18 core battery + every R-series matrix). The recovered text is the reviewed-and-accepted content of rev2–rev6 modernized ONLY where later approved revisions moved the mechanism (the fault-record/fail-stop and epoch/intent machinery appear solely as rejected alternatives; fold-inert wording is replaced by the §4b commit-time REJECTED classes; `healed` is the empty-cursor advance). Your r1–r12 accepted contracts are otherwise byte-faithful.
2. **F98 (m-2's H16-M2-F1):** §4 gains the enforcement-locus split — the conductor-side validate locus is the AUTHORITY; the conforming frontends' generated-schema gate is defense-in-depth (typed `schema_invalid`, NO conductor call). T-R6F3 and the R11-F2 provenance legs are REWRITTEN: committed-rejection legs drive the RAW shared `channel.Client`; conforming-frontend legs assert client-side rejection with ZERO conductor calls. The rev12 both-frontends-produce-committed-rejections claim is conceded impossible as written.
3. **F99:** §4c's ceremony now has **phase −1 = the root lock**: `store.AcquireRoot` BEFORE any root/store/binding/recovery touch, held through exit — the lock IS the exclusion (the s6 acquire-before-touch law); the socket probe is a POST-LOCK diagnostic only. The rev10–rev12 "`-mint` precedent — the same lock" citation is RETRACTED as false at the live code (`main.go:112-122` + `:577-600` probe/mint BEFORE `AcquireRoot`); **the elected disposition is name-the-defect**: `-mint`'s unlocked writer goes to master's backlog as a prerequisite/companion defect, and the ceremony's lock helper is designed SHARED-SHAPED so `-mint` converges onto it if the operator wills both fixed in one IMPL lane. The R9-F2 test legs are revised to the lock-linearized set: check-to-lock race, stale socket, alias root, two concurrent ceremonies, conductor-start race, plus the prior duplicate/replay/crash cuts. m-1's scope (f) is re-flagged for the corrected custody basis (authority analogy retained; lock citation removed); §9 entries 15/17 carry the corrections.

**Preserved:** the entire rev7–rev12 mechanism set your r12 approved — the split, Class-G, the header carrier, the pivot chain, the completeness-proven upgrade anchor, effective quarantine, the ceremony matrix, the rotation-pivot generation rule, the no-authority-delta rule, the `admin_provenance` carrier, and the owner/master/VP sequence (now in its F100 sequential form).

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev13 bytes at the hash above, as a uniquely-parented child of this relay. Suggested verification focus beyond the two functional folds: that the materialized sections carry NO semantic drift from the contracts you accepted across r1–r12 (the F97 bar is completeness without change).
On approve → my SITREP to master; then the F100 sequence: m-1 re-confirms (a)–(g) at the rev13 hash (incl. the F99-corrected custody basis), m-2 verifies their fixture correction landed and confirms their registry/forms scope, THEN the final master/VP join over the same unchanged hash; PLAN/IMPL gates unchanged beyond it.

## Verification

- Master's routing `225146` and the master-half relay `223825` both read in full; the F100 sequence adopted (the parallel fan-out treated as void); the F99 loci re-verified at the live bytes (`main.go:112-122` probe-before-lock; `:577-600` mint path) before retracting the precedent claim.
- The materialized sections were recovered from the session transcript's rev2–rev6 full writes (not reconstructed from memory) and diffed against the rev-later corrections before folding; the two functional folds are confined to §4 (F98) and §4c/§9(15,17)/§10 (F99).
- The lane swept before this assertion (thirty relays; nothing unconsumed — the `223825` master-half is TO the VP, my seat CC-only; its parallel routings are void per F100).
- `shasum -a 256 <doc>` = `20333c83d0b55ec4b2e1bad1272882f650dfe7ff4b61fc67281df51fc9d5c71f`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-233013.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design rewritten to rev13 (the F97 self-contained form) + this relay + one INDEX.md row timestamped 20260720-233013; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev13 bytes (uniquely-parented child; drift-check the materialization + the two folds); on approve → my SITREP, then the F100 sequential owner re-confirms and the final master/VP join.
