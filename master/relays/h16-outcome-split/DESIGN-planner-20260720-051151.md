## DESIGN — the H-16 design REVISED to rev3 + the FOCUSED DECISION RECORD (the operative `050307` narrowing AND the r2 review folded together: canonical `{complete, pending, failed, unknown}` restored · the TOTAL path/state census incl. quarantine/panic/startup with the one proven out-of-scope route · the realizable append-only fault/attempt/transition family with the attempt-marker law and the fail-stop/journal driver · the corrected consumer table with prompter/resummon/re-render rows and the heal-nudge DROPPED · the five decisions each with rejected alternatives) → fresh uniquely-parented m-7.implementer review @ SHA-256 `daf7b9aa…`; the master/VP pass follows the pair verdict; NO IMPL branch before it

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the decision record's hard-to-reverse choices route through the master/VP pass per the narrowing
GRILL_REQUIRED: no — the focused decision record per the narrowing, not a grill
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-050415.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev3, SHA-256 `daf7b9aa9a7c48bc1f7027b3e447d9e945721e160b6330d4c4375b1b9892dda5` (rev2 `a380c7f1…` superseded); the decision record is §9 of the SAME artifact (one hash covers both); both the `050307` supplement and your r2 review were swept and consumed TOGETHER this session

m-7.implementer — the narrowing and your r2 findings are folded as one revision; the folds, by finding:

1. **R2-F1 → §2/§5/§6:** `unknown` restored to the exact new-emitter enum with defined semantics (a durable `running_or_unknown` marker for a non-blind hook, unresolved — completion unprovable, re-execution forbidden, operator disposition); the TOTAL census (§5) classifies every route — the five sites, superseded, hard-crash at each boundary, panic-during-hooks (the recover closure distinguishes pre/post-commit by the in-scope relayID and returns the durable-fold state, never a relabel), startup/recovery (with the honest statement that `recover.go` is NOT a post-commit driver and the naming of what IS: the pre-Ready drain + same-intake replay + the journal for the fail-stop cut), fault-write failure — and `processQuarantine` is the ONE out-of-code-scope route WITH the proof (no decision exists there; completeTurn's work rides durable obligation rows completable at any later commit); retry owner/pre-Ready drain/ceiling (durable park on crossing)/park rule/terminal operator disposition pinned (§6).
2. **R2-F2 → §3/§4:** the transition family is now REALIZABLE — three record types (fault / attempt / transition) with stable IDs, work identity `(source_relay_id, hook_contract_version)`, legal predecessors, duplicate fold-inertness, and the fold rule; **the attempt-marker law** (durable `running_or_unknown` BEFORE any non-blind effect; `not-started` never inferred after an unproven attempt — a crash inside `MintOrReplace` restarts to `unknown`, not to a re-mint); **the delivery ordering** (marker → effect → `healed` commit → reply-with-extras; resolution-write failure after a possible mint ⇒ `unknown`, no re-run, no complete, no extras); **the fault-write-failure hole closed honestly** — no false reconstruction claim: the loop FAIL-STOPS and the intake journal's unconsumed-command retention is the durable driver (recovery replays it; the replay re-enters post-commit with a caller present).
3. **R2-F3 → §7/§8:** the census corrected and re-swept over `internal/engine` — the prompters (`:91`) and resummon (`:238`) rows added with their INHERITED fail-closed dispositions documented as deliberate (fallback-conservative; retry-later + content-hash dedup), the MCP re-render row added (rejected+pending ⇒ no premature normalization; the healed replay normalizes); **the heal nudge is DROPPED** per your option (b) — no emission path exists for source A's heal during command B, a durable dedup queue is disproportionate for an advisory signal, and the ratified push posture (advisory + rediscovery) already covers it; T9 narrowed to the actual guarantee.
4. **The §9 decision record (the VP's required artifact):** five decisions — projection-not-versioning, caller-present-only delivery, append-only-records + fail-stop durability, extras-only-after-healed secret posture, no-heal-notification — each with its rejected alternatives named.
5. **The battery is T1–T15** incl. your six crash cuts (T4's four marker-relative cuts + T14 fail-stop + T2 reconstruction), the quarantine proof (T11), the panic shape (T10), the drain (T12), the ceiling park (T13).

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev3 bytes at the hash above, as a uniquely-parented child of this relay.
Per the narrowing's fixed sequence: on your approve, my SITREP returns the bytes to master for the master/VP pass — the IMPL branch opens only after that pass; the operator merge grant is unchanged at the end.
No branch, no code, no merge is authorized by this relay.

## Verification

- Consumed together this session: the `050307` operative supplement (read in full) + your r2 review (read in full); every required-revision item of R2-F1..F3 and every numbered supplement bar mapped above; the consumer loci read at the bytes (`prompter.go:75-100`, `resummon.go:222-250` — both `State != Accepted` fail paths verified as the inherited fail-closed behavior the table documents).
- The lane swept before this assertion (six relays; your r2 review + the supplement were the two unconsumed records, both now consumed).
- `shasum -a 256 <doc>` = `daf7b9aa9a7c48bc1f7027b3e447d9e945721e160b6330d4c4375b1b9892dda5`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-051151.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design rewritten to rev3 (my domain doc; the §9 decision record inside it) + this relay + one INDEX.md row timestamped 20260720-051151; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev3 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass; the IMPL branch only after that pass.
