## DESIGN — the H-16 design REVISED to rev10 (R9-F1..F3 folded: quarantine becomes the EFFECTIVE-QUARANTINE predicate — chain conflict OR unrealized resolved tip; canonical resolution alone never clears, the clear publishes only after DURABLE `MintOrReplace`, and repair errors/crashes retain it until pre-serve recovery proves realization · the local recovery is pinned as the OFFLINE ONE-SHOT CEREMONY — exclusive stopped-store ownership, the same binary's serialized startup writer phase, synchronous credential reply, fresh-replacement on undelivered, never a second live writer · operator-role bits REMOVED from admin eligibility (they live on exactly the disputed pivots) — eligibility = effectively-quarantined target + the anchor/disposition record class only, the selected tip's own canonical bytes governing authority after selection · the §4a migration locus pinned to the serialized startup writer phase) → fresh uniquely-parented m-7.implementer review @ SHA-256 `d5b95a20…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the master/VP pass and BOTH owner confirmations precede lock/IMPL
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-213949.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev10, SHA-256 `d5b95a20ff9578aeb203fc672543aed9f49e78c18f1d125f3e7250d334e9d011` (rev9 `921eb058…` superseded); the m-1 scopes (e)/(f) updated to the effective-quarantine predicate and the extended custody basis incl. the role-disagreement rule; the m-2 forms updated (anchor schema with admin-authored marking; the offline ceremony form)

m-7.implementer — R9-F1..F3 folded; the concessions first: rev9's chain-status-only quarantine would have cleared at the anchor commit and reopened the old bound credential in the commit→`AfterCommit`→`AfterAccepted` gap — the exact ruled zombie window; its "admin verb through the serialized loop" named no realizable write path (recovery and binding completion finish before `engine.Loop` exists; an online injection or a second-process commit each break the one governed writer); and its "operator-role AND quarantined" gate read `role`/`is_operator` from exactly the disputed pivots the anchor exists to resolve — trusting non-authority or stranding the disagreement case.
The folds, by finding:

1. **R9-F1 → §4c (effective quarantine):** the predicate is now **(chain CONFLICTED/unresolved) OR (a resolved canonical tip the binding row has not durably realized)** — derived over canonical fold × durable binding row, computed pre-serve, republished by the serialized writer. The clear publishes ONLY after `MintOrReplace` has durably replaced the credential + `realized_mint_ref` — one atomic swap of the published auth state after the repair callback completes; no auth attempt can interleave between replacement and clear. A repair ERROR keeps the seat quarantined and the anchor outcome reports per the §2 split (`accepted × failed` — never "usable recovery"); a crash after the anchor commit retains quarantine across restart because the pre-serve recomputation finds an unrealized resolved tip — clearing requires PROOF of realization. Pre-realization the old credential draws `auth:seat-quarantined`; post-realization `auth:invalid-credential`. Your concurrent-auth leg + the three crash/error cuts are in T-R8F2 verbatim.
2. **R9-F2 → §4c (the elected transport) + §4a (the writer locus):** the recovery is the **OFFLINE ONE-SHOT CEREMONY** — a mode of the SAME binary that refuses while the conductor socket is live, takes EXCLUSIVE store ownership via the same lock (mutual exclusion both ways), runs the serialized startup writer phase itself (recovery → validation → the admin-marked anchor commit → repair to durable realization), returns the fresh credential SYNCHRONOUSLY in its own direct reply (the genesis-delivery precedent), and exits. Custody = OS permissions on the store directory; no socket; structurally absent from the seat `submit/project/read` surfaces. Idempotency pinned: an existing accepted anchor ⇒ skip to repair (the double-anchor rule holds); a post-replacement pre-reply crash ⇒ a FRESH replacement is minted and delivered — possibly-exposed bytes never re-delivered; exactly one anchor + one realized credential under every interleaving. The online local-admin socket and the second-process live commit are the named rejected alternatives (§9 entry 17). Per your item 3, §4a now pins the migration locus: the SERIALIZED STARTUP WRITER PHASE — the same process applying the same sole-writer commit discipline sequentially in the order recovery → upgrade anchors → binding completion → loop start → channels; startup phase and service loop are the one governed writer at different times, never concurrent.
3. **R9-F3 → §4c (eligibility from canonical truth):** the role gate is REMOVED. Eligibility = the target is EFFECTIVELY QUARANTINED, and the ceremony writes ONLY the anchor/disposition record class — the authority is the custody basis alone (local + exclusive + stopped = operator, per genesis), never the target's role bits; AFTER selection the selected tip's own canonical bytes govern the seat's authority, so role disagreement is resolved BY the anchor, not read before it. "No other usable operator" is operational guidance for preferring paths (a)/(b), not a machine precondition — nothing unprovable is checked. Eligibility is a pure function of canonical bytes × durable realization: for a conflicted history the conflict alone quarantines, so the same canonical history yields the same eligibility whatever candidate the binding row reflects, and no authority-disagreement case is unrecoverable. Your role-flip fixture matrix is the T-R8F2 R9-F3 legs verbatim; the extended custody basis + disagreement rule ride m-1 scope (f) explicitly.

**Preserved (your accepted portions untouched):** the completeness proof + partial-redo fail-closed rule and its m-1 authority reservation; snapshot-once/skip-on-restart/GC gating; the typed leak rule; the three-shape recovery partition; the accepted-only forged-header fold; the canonical transition discipline; the linked-chain tip predicate; the header/version carrier; fresh instances; Class-G; the evidence fold; the projection; the consumer table; the final owner/master/VP sequence.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev10 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP returns the bytes to master for the master/VP pass + the routing of the two final-scope owner confirmations (your gate-disposition scope list carried); the IMPL branch only after that pass and those confirmations; the operator merge grant unchanged at the end.

## Verification

- Your r9 review read in full; every required-revision item of R9-F1..F3 mapped above; the R9 loci re-verified before folding and added to the design Basis line (`loop.go:156-186` + `main.go:286-303, 603-621` the commit→tables→later-`AfterAccepted` ordering; `main.go:259-330` recovery/binding-completion before `engine.Loop`; `submit.go:391-421` the pivot authority members).
- The lane swept before this assertion (twenty relays; your r9 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `d5b95a20ff9578aeb203fc672543aed9f49e78c18f1d125f3e7250d334e9d011`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-215002.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design revised to rev10 (targeted edits; all r9-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260720-215002; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev10 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass + the m-1/m-2 final-scope confirmation routing; the IMPL branch only after that pass.
