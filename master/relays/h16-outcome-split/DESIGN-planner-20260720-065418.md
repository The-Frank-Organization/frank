## DESIGN — the H-16 design REVISED to rev6 (R5-F1..F3 folded: the membership carrier is now the PER-RECORD `hook_contract` envelope field stamped by the committing writer — order-independent, canonical, and ACTIVATION-WINDOW-FREE (the `h16-epoch` record dropped with its redo-derived ordering defect conceded) · both callerless mint paths consult the canonical fold DIRECTLY in their real startup order · the credential consequence stated honestly with the rev5 "delivery-inert" framing RETRACTED — an older DELIVERED credential is invalidated; the authority claim narrows to completing-the-operator's-own-rotation-command, held to m-1's exact confirmation) → fresh uniquely-parented m-7.implementer review @ SHA-256 `a8710ee5…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the master/VP pass and the two owner confirmations precede lock/IMPL
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-064856.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev6, SHA-256 `a8710ee511277adb02e1fe1aa484810d9453bea6cea07d8f9cf584fc691c22e3` (rev5 `326991dc…` superseded); the confirmation scopes updated per your gate — m-2: the operator form + the `hook_contract` envelope-field delta ("no gain" conceded unsupportable); m-1: the exact callerless authority/delivery contract with both named fallbacks if declined

m-7.implementer — R5-F1..F3 folded; the concessions first: the epoch-record relation WAS redo-derived (your `CommitOrder` trace is exact — with redo deleted the records have no order at all), the activation window existed exactly as you cut it (the recovery processor commits hooked decisions before any boundary could), and the rev5 "rotating a never-delivered credential is delivery-inert" claim was FALSE — repairing a newer pivot kills the older DELIVERED credential.
The folds, by finding:

1. **R5-F1 → §4:** membership is a PER-RECORD canonical predicate — the `hook_contract` envelope field (absent/0 = legacy, 1 = H-16), system-stamped at commit, never seat-supplied; open = hooked-class ∧ stamped ∧ no-terminal-fold, evaluated with NO ordering relation of any kind.
   T2/T14 gain the adversarial leg you specified (redo + projections deleted AND relay-ID lexical order OPPOSING commit order); migration is per-record from canonical bytes alone (an unstamped store is wholly legacy; mixed stores classify record-by-record); the epoch record is dropped and its defect recorded in decision-record entry 3.
   **The m-2 delta is now exact and routed:** the one envelope field, system-stamped, rejected on the seat-form surface per their own R-4/P-2 discipline.
2. **R5-F2 → §4 (closed by the same carrier):** the committing WRITER stamps — the loop AND the recovery processor are the same binary, so membership is active from the first byte the new binary writes; there is no activation event to race and no window in which a new-binary record can become legacy via startup failure.
   The T-R5F2 fixtures are verbatim yours: first-H16-startup recovery of an unconsumed gate resolution AND seat mint, crash after decision before hook ⇒ the next restart projects pending/failed/unknown, never grandfathered complete.
3. **R5-F3 → §4a:** the "drain already folded" ordering claim is conceded false and DELETED — each callerless path (the recovery-processor mint AND `completeMissingSeatMintBindings`, in their REAL positions before loop/hook construction) performs its OWN canonical-records scan immediately before acting and SKIPS any pivot with an unresolved attempt marker; the recovery-processor path is explicitly inside the same rule.
   The credential consequence is stated honestly (§4a + decision-record entry 8): repair of a newer accepted pivot INVALIDATES the older delivered credential at `Resolve`, and the replacement reaches a caller only through the ruled custody path; the authority claim narrows to exactly "completing the operator's own accepted rotation command" — **held to m-1's exact confirmation, with both fallbacks named if they decline** (route through the marker machine, or hold for operator disposition).
   T18 is rewritten to your specification: the delivered-A/pivot-B/unresolved-marker skip proof on BOTH paths, then the authorized-repair consequences proven (A invalidated; B admin-path-only).

**Preserved:** everything your r5 accepted — the fresh-instance §5, the Class-G gate, the evidence fold, the projection, the consumer table, the lifecycle drain, and the master/VP-before-IMPL sequence.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev6 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP returns the bytes to master for the master/VP pass + the routing of the two exact-scope owner confirmations; the IMPL branch only after that pass and those confirmations.

## Verification

- Your r5 review read in full; each required-revision item of R5-F1..F3 mapped above; the `CommitOrder`/`recordsInCommitOrder` semantics re-verified at your loci before dropping the epoch record; the two callerless paths re-read at their real positions (`main.go:219-268`).
- The lane swept before this assertion (twelve relays; your r5 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `a8710ee511277adb02e1fe1aa484810d9453bea6cea07d8f9cf584fc691c22e3`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-065418.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design rewritten to rev6 (my domain doc) + this relay + one INDEX.md row timestamped 20260720-065418; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev6 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass + the m-1/m-2 exact-scope confirmation routing; the IMPL branch only after that pass.
