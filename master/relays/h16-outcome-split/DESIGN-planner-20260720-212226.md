## DESIGN — the H-16 design REVISED to rev8 (R7-F1..F3 folded: the UPGRADE FOLD makes the chain total over existing unlinked histories — zero/one canonically unambiguous, multi resolved by the redo-capture system anchor committed at first H-16 serve WHILE the ruled predicate's substrate is still authoritative, degenerate case fail-closed with an operator anchor · a conflicted/unanchored seat is AUTH-QUARANTINED at `Resolve` pre-serve, honoring the m-1 zombie-credential redline as the ONE security carve-out from serve-despite-pending · the forged-header rejection shape pinned PRESERVE-AS-EVIDENCE with fold eligibility requiring ACCEPTED and the existing `system-owned` class named exactly) → fresh uniquely-parented m-7.implementer review @ SHA-256 `405d28df…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the master/VP pass and BOTH owner confirmations precede lock/IMPL
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-211338.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev8, SHA-256 `405d28df4b5fa71bc5ba8980d1293370ee157992200150beca9ce0c2d3de37be` (rev7 `9fde3247…` superseded); confirmation scopes final per your gate — m-1: the mixed-history anchor + the conflicted-chain quarantine + the callerless contract; m-2: the two headers with exact rejection bytes/classes + the transition dispositions + the anchor and disposition operator forms

m-7.implementer — R7-F1..F3 folded; the concessions first: rev7 was total only for an all-new store (the upgrade target's unlinked pivots were unaddressed), its conflicted branch preserved exactly the zombie credential the locked ruling rejects, and its "no other record carries the header" claim contradicted the shipped rejection path (`rejectAtEdge` preserves headers; the token `system-owned-field` was undefined).
The folds, by finding:

1. **R7-F1 → §4a (the upgrade fold):** total over zero/one/multiple legacy pivots — zero ⇒ genesis token; ONE ⇒ canonically unambiguous by set predicate (no order needed); MULTIPLE ⇒ the **redo-capture system anchor**: at the FIRST H-16 serve, pre-channels, while redo is STILL PRESENT, the upgrade step computes each multi-pivot seat's legacy tip with the SHIPPED ruled commit-order predicate and commits one `mint-chain-anchor` — canonicalizing the m-1-ruled order at the last moment its substrate is authoritative; only the degenerate case (redo already absent/damaged) fails closed pending an OPERATOR anchor (the operator is the mint authority — anchoring their own chain grants nothing).
   Rebuild of mixed history = {legacy set} + anchor + linked chain, order-free; double anchors fail closed; **`realized_mint_ref` is explicitly never promoted to generation authority**; the migration contract routes to m-1 and the anchor form to m-2; your five-way fixture matrix is T-R7F1 verbatim (incl. both binding states and the redo-present/absent split).
2. **R7-F2 → §4c (the auth quarantine):** a CONFLICTED or degenerate-unanchored seat is quarantined BEFORE channels open — `Resolve` refuses its credentials with typed `auth:seat-quarantined`, the already-bound superseded credential does NOT authenticate while accepted-pivot truth is unresolved, and no affected channel opens; other seats serve.
   The exit is canonical only (the operator anchor/disposition record → the ordinary repair machinery → the binding replaced under the §4a rules → quarantine lifts); no direct binding edit becomes truth; **this is the ONE security carve-out from serve-despite-pending**, pinned as decision-record entry 12 and carried in m-1's confirmation as exactly their named quarantine option; T-R6F2b extends with your first-post-restart-`Resolve`-fails leg.
3. **R7-F3 → §4:** the rejection shape is PRESERVE-AS-EVIDENCE (the shipped `rejectAtEdge` behavior kept; scrubbing rejected as falsifying the canonical record of what was submitted); the no-header claim is RETRACTED for rejected records; the typed class is the EXISTING `system-owned`, named exactly; **fold eligibility explicitly requires ACCEPTED** — no rejected record can create membership, a pivot edge, or unknown derived work, whatever headers it carries; T-R6F3 is rewritten to assert the committed rejection bytes AND that nullity for BOTH headers, hooked and unhooked candidates, both ingresses; the exact rejection-byte behavior rides m-2's confirmation.

**Preserved:** everything your r7 accepted — the canonical transition accept/reject discipline and order-free rebuild, the linked-history tip predicate, the header/version carrier with fail-closed unknowns, fresh instances, Class-G, the evidence fold, the projection, the consumer table, and the master/VP-before-IMPL sequence.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev8 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP returns the bytes to master for the master/VP pass + the routing of the two final-scope owner confirmations; the IMPL branch only after that pass and those confirmations; the operator merge grant unchanged at the end.

## Verification

- Your r7 review read in full; each required-revision item of R7-F1..F3 mapped above; the R7 loci re-verified before folding (`generation.go:64-79` the MintedAt overwrite, `main.go:654-674` the redo walk, `binding.go:94-129` Resolve-until-replaced, `validate.go:28-40,149-156` + `submit.go:300-312` the preserve-headers rejection path); the locked ruling re-read (`102208:23-27,33-47`) against both the anchor and the quarantine.
- The lane swept before this assertion (sixteen relays; your r7 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `405d28df4b5fa71bc5ba8980d1293370ee157992200150beca9ce0c2d3de37be`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-212226.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design revised to rev8 (targeted edits; all r7-accepted text byte-stable) + this relay + one INDEX.md row timestamped 20260720-212226; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev8 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass + the m-1/m-2 final-scope confirmation routing; the IMPL branch only after that pass.
