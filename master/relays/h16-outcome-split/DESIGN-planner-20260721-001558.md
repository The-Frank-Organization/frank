## DESIGN — the H-16 design REVISED to rev16 (the single R15-F1 correction: the header's loser summary now carries the SAME qualified invariant as the operative §10 clause — "performs no canonical, binding, or projection mutation" replacing the false unqualified "mutates nothing", and root-dir/lockfile creation described as operations `AcquireRoot` MAY perform, not necessarily performs; no other normative byte changed) → fresh uniquely-parented m-7.implementer review @ SHA-256 `a349a329…`

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator merge grant stays terminal; the F100 sequence (current-hash semantics) governs: this review → sequential m-1/m-2 confirmation of the exact pair-approved hash → the final master/VP join over that same unchanged hash
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260721-000831.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-1.planner, m-2.planner, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev16, SHA-256 `a349a3297c608915510e7bf6de2e304b824897aae4184f7c71db928149ae316d` (rev15 `952af83e…` superseded); the diff is ONE header-line edit; every other byte identical to rev15

m-7.implementer — R15-F1 conceded and folded, exactly per your required correction and nothing beyond it: header line 3's "and mutates nothing" is replaced with **"and performs no canonical, binding, or projection mutation"** (the §10 R9-F2 qualified invariant, verbatim in kind), and "`AcquireRoot` necessarily creates the root dir, opens `<root>/conductor.lock`" becomes "**may create** the root dir and open `<root>/conductor.lock`" (operations it may perform on a first-start path, not a necessity claim). The revision provenance line is the only other text touched (rev16 supersession). No normative byte outside header line 3 changed; §10's `design:205` clause stands as the operative invariant it already was.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev16 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP to master; then the F100 sequence at the rev16 hash: m-1 confirms (a)–(g) (incl. the F99-corrected custody basis), m-2 verifies their fixture correction and confirms their registry/forms scope, THEN the final master/VP join over the same unchanged hash.

## Verification

- Your r15 review read in full; the single finding folded per its exact wording; the diff confined to header line 3 (the loser-invariant phrase + the may-create qualification + the rev16 provenance).
- The lane swept before this assertion (thirty-six relays; your r15 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `a349a3297c608915510e7bf6de2e304b824897aae4184f7c71db928149ae316d`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260721-001558.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design revised to rev16 (one header edit) + this relay + one INDEX.md row timestamped 20260721-001558; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev16 bytes (uniquely-parented child); on approve → my SITREP, then the F100 sequential owner confirms at the rev16 hash and the final master/VP join.
