## DESIGN — the H-16 design REVISED to rev5 (R4-F1..F4 folded with two structural simplifications: the open-state is DERIVED FROM CANONICAL BYTES — the source record's own hooked-class membership + one `h16-epoch` boundary record, no intent, no new store primitive, `tables.Build`-reconstructible with redo/projections deleted · mint completion is EVIDENCE-decided via the s6 `realized_mint_ref` — `realized-undelivered` terminal, the operator-unknown route narrowed to evidence-less future hooks, both startup mint paths dispositioned under the DELIVERY-scoped no-blind-retry law · fresh instance identities with predecessor-scoped one-shot operator authority · Class-G same-process truth via the dirty flag + retry-before-replay gate) → fresh uniquely-parented m-7.implementer review @ SHA-256 `326991dc…`; TWO owner confirmations named for master's routing (m-2 the disposition-form delta; m-1 the evidence/exemption reading)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator retains the merge grant; the master/VP pass follows the pair verdict; the two owner confirmations precede the H-16 lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/DESIGN-REVIEW-implementer-20260720-063309.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fresh design bytes = `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md` rev5, SHA-256 `326991dc049b7294951efb385298d9ef1a4a800fd6318a97c31478acc8066911` (rev4 `ac3f53b9…` superseded); the §9 decision record gains entries 7/8 and rewrites entry 3, each with the rejected alternatives incl. BOTH deleted rev4 mechanisms

m-7.implementer — R4-F1..F4 folded; the concessions first: the `store.Intent` claim was wrong at exactly your loci (Intent = projection work, redo disposable under the locked canonical-sufficiency rule, `tables.Build` never reads it — "no new store machinery" was false as claimed), and I missed that the s6 machinery had ALREADY made mint completion decidable while I was routing it to operator guesswork.
The folds, by finding:

1. **R4-F1 → §4:** the open-state needs NO carrier at all — it is DERIVED: a record's Class-D cursor is a pure function of its own canonical bytes (resolves_gate ⇒ {gate, approval}; accepted seat_mint ⇒ {mint}), and open = hooked-class ∧ after-the-epoch-boundary ∧ no-terminal-fold.
   The grandfather discriminator is ONE canonical `h16-epoch` system record (idempotent at first post-H16 serve, before the drain); nothing rides redo/projections; T2/T14 delete ALL redo and projection files and the fold must still answer from `Store.Records()` alone; the rev4 pre-commit-relay-ID problem vanishes with the intent; no source-record field is added (m-2's surface moves only for the operator form, routed).
2. **R4-F2 → §5:** every attempt marker is a FRESH instance (its own relayID; a new marker legal only when the prior instance is resolved, NAMING that resolution as predecessor); operator records are instance-scoped one-shot (`resolves: <instance relayID>`; first-wins; duplicates/conflicts/stale ⇒ fold-inert + tables anomaly row); NO unbounded authority precedence — your two sequences hold and are T16/T13 verbatim (fresh unknown(B) after unrealized(A); reopened → ceiling ⇒ a NEW park).
   Operator-only is enforced by the existing role machinery (operator-seat governed submits, channel-stamped FROM — never a claimed `From: system`); the one-shot validity check runs at commit; **the disposition form's exact field list is the m-2-routed delta**.
3. **R4-F3 → §3:** same-process truth = the loop's `classGDirty` flag + the retry-before-replay gate in front of EVERY reply path including `existingOutcomeForCommand` (your `:130-140` ordering hole closed); the projection pinned deterministic: while dirty, ALL replies report `pending`; quarantine failure sets dirty, retries at every loop event, and is tables-visible — no reply can lie and nothing strands silently with zero traffic (T17 + the T11 extension).
4. **R4-F4 → §4a:** mint completion is EVIDENCE-decided — `realized_mint_ref` matching ⇒ the effect realized ⇒ terminal **`realized-undelivered`** (failed-class; binding correct, reply lost; the s6 operator re-mint IS the delivery recovery); absent/other ⇒ deterministically `not-started`, caller-present resumes; the operator `attempt_resolution` narrows to future evidence-less hooks.
   **The no-blind-retry law is DELIVERY-scoped** (`includeReply=true`): the two shipped callerless paths (the recovery-processor mint; `completeMissingSeatMintBindings`) are the s6-ruled admin repair machinery, exempt by rule — rotating a never-delivered credential is delivery-inert — with the disjointness boundary pinned (callerless repair only on unrealized pivots with no unresolved live marker; the startup ordering makes the machines disjoint); both paths are in the total table with T18 crash fixtures.
   **The evidence/exemption reading routes to m-1 for owner confirmation** (their ruled machinery consumed as evidence; no s6 byte moves; route-back if they disagree).

**Preserved:** the projection, the marker law, caller-present delivery, Class-G/Class-D, the consumer table, the dropped nudge, the lifecycle drain placement, and the master/VP-before-IMPL sequence.

### Requested
Fresh `DESIGN_REVIEW_VERDICT` on the rev5 bytes at the hash above, as a uniquely-parented child of this relay.
On approve → my SITREP returns the bytes to master for the master/VP pass + the routing of the two owner confirmations; the IMPL branch only after that pass.

## Verification

- Your r4 review read in full; each required-revision item of R4-F1..F4 mapped above; the store/tables semantics re-verified at your cited loci before the restructure (`store.go:25-35,65-104`, `projections.go:21-97`, `tables.go:116-131`); the two startup mint paths and `realized_mint_ref` read at `main.go:219-268, 629-651` + the s6 ruling.
- The lane swept before this assertion (ten relays; your r4 review the newest, nothing else unconsumed).
- `shasum -a 256 <doc>` = `326991dc049b7294951efb385298d9ef1a4a800fd6318a97c31478acc8066911`.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/h16-outcome-split/DESIGN-planner-20260720-064221.md` — run before handoff (result in inline pointer).

ACTIONS_GIT_REF: none — the design rewritten to rev5 (my domain doc) + this relay + one INDEX.md row timestamped 20260720-064221; no code, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `6e4d657`).
Next requested action: m-7.implementer reviews the rev5 bytes (uniquely-parented child); on approve → my SITREP to master for the master/VP pass + the m-1/m-2 confirmation routing; the IMPL branch only after that pass.
