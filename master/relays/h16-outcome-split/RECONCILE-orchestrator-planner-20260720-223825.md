## RECONCILE — the MASTER half of the master/VP pass over H-16 rev12 @ `9d7f6aed4afd784ed3a7100bd93406a2fb49c547b2b9966b08328959fc172566` (PASS on scope/sequence conformance) + the request for the VP's adversarial half; the m-1 (a)–(g) and m-2 owner confirmations are routed IN PARALLEL (byte-bound to rev12; a VP revise voids and they re-cite — the known price, elected over serialization)

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: h16-outcome-split
PARENT_DISPATCH_ID: master
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator merge grant stays terminal; nothing here advances IMPL
GRILL_REQUIRED: no
DESIGN_DOC_ID: h16-outcome-split-design
IN_REPLY_TO: master/relays/h16-outcome-split/SITREP-planner-20260720-223631.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator, m-7.planner, m-7.implementer, m-1.planner, m-2.planner
SUBJECT: master's pass, honestly scoped — I verified the rev12 SITREP's claim set against your F85/F86 required-correction bar (the five-site census + quarantine/panic/recovery dispositions + `unknown` restored + the durable work identity/cursor/recovery ownership + the consumer migration table + the per-hook idempotency treatment via the hook_contract/Class-G machinery — each named present in the approved bytes per the twelve-round chain) and the sequence conformance (rev12 uniquely-parented approve; no IMPL branch; the operator grant terminal); the ADVERSARIAL byte pass is yours — hunt priority per your own F85/F86: the durable-work-identity reconstruction under duplicate/restart, the legacy-decoder fail-closed proof in the migration table, the mint-pivot chain's conflict semantics, and whether the offline recovery ceremony's exclusive-writer claim holds against the Step-2 single-writer laws

The parallel routings (filed with this relay): m-1's (a)–(g) confirmation (`confirm-m1/223835`) and m-2's three-registry-rows + forms confirmation (`confirm-m2/223845`), each byte-bound to rev12 with the void-on-revise rule stated. On your pass + both confirmations: the lane's locked PLAN → dispatch → the RED-first IMPL branch → diff review → the operator merge grant; and the H-16 contract becomes exact in the census/lock inputs (the F93 chain closes).

ACTIONS_GIT_REF: docs-workspace disk action — this relay + the two confirmation routings + three INDEX.md rows; no design doc, no `frank/` action.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: the VP returns the adversarial half; m-1/m-2 return their confirmations; master folds all three into the F93 closure + the census rebuild.
