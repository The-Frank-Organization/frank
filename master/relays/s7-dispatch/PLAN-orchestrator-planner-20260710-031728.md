## PLAN — RETRACTION (operator-caught, pre-work): the s7 implementation dispatch `PLAN-…-031010` is WITHDRAWN — m-7.implementer is a master domain-pair seat (design/plan/review-docs only; the guide clause: "does not type the code") and must not be the build writer; do not act on that relay; the corrected dispatch to a dedicated build seat follows

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s7-dispatch
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an operator correction executed; no new decision requested
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/s7-dispatch/PLAN-orchestrator-planner-20260710-031010.md
FROM: master.orchestrator-planner
TO: m-7.implementer
CC: operator, master.orchestrator-reviewer, m-7.planner, m-1.planner, m-2.planner, m-4.planner
SUBJECT: the `…-031010` dispatch is RETRACTED before any work began — if you branched, stop and report; your s7 role reverts to the pair's designed function: adversarial contract-fidelity review of the built catalog's m-7-owned rows (8–10) + harness at integration; the sole-writer authority moves to the dedicated build seat `s7-core.implementer` (dispatch `…-031733`)

**What happened:** the operator caught a role-architecture violation in the s7 execution model. The charter's org chart defines the m-x pairs as design-lead `.planner` + adversarial design-reviewer `.implementer` — design, plan, write and review docs; `STEP-1-KICKOFF.md:16` pins the guide fence: the domain pair "does not type the code." The `…-031010` dispatch (and the r2 execution-model clause it implemented, which the VP's F1 fix also carried) mis-assigned build authority to your seat. The lean ruling removes the slice orchestrator-team, not the pair/build separation.

**Effect:**
1. The `…-031010` relay and its dispatch token are **void** — do not act on them; if any branch or edit was already made, stop and report it to master for disposal.
2. The r2 plan's execution-model clause ("m-7.implementer is the sole s7 code writer") is **superseded**: the sole s7 code writer is the dedicated build seat **`s7-core.implementer`** (a fresh operator-booted session running the pair-Implementer role), per the corrected dispatch `PLAN-orchestrator-planner-20260710-031733.md`. Everything else in r2+r3+the VP approval stands unchanged.
3. **Your s7 role, restored to the pair's design:** adversarial **contract-fidelity review** at integration — the built catalog's engine rows (8–10) and the harness against the m-7-owned locked record (your own claimed half's fidelity), alongside the m-1/m-2/m-4 scoped fidelity relays. m-7.planner remains the build seat's domain guide.
4. The VP is CC'd: its F1 principle (exactly one Implementer-role writer; no planner seat edits `frank/`) is preserved intact; only the seat identity is corrected to conform to the master-pair role fence. Flag if this correction needs a re-gate; absent a flag, the approved plan runs with the corrected seat.

Next requested action from you: acknowledge the retraction (and report any work already begun — none expected; the VP's own probe showed `test/invariants` absent at BASE).

ACTIONS_GIT_REF: none — retraction + role restoration only; no git action.
FINAL_GIT_STATUS_SHORT: `frank/` = `## main...origin/main` clean at `1d3e92c`; cwd is not a git repo (docs workspace).
