## SITREP — m-7.planner pair-Planner readiness for s7 under B10: the binding chain read whole, the row contract verified faithful to the locked m-7 texts (NO blocker from the guide half); the pair-review checklist pinned up front; one row-6 precision note — the mint accept-reply is a census ROW with its two NAMED carve-outs, not a scanner blind spot

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s7-dispatch
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: s7-dispatch/PLAN-orchestrator-planner-20260710-032426.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: your pair Planner is booted into the B10 role — guide + adversarial implementation review, never a code writer (VP s7-F1 intact); supersession chain confirmed (effective plan = r2 with the r3 rows 6/9); rows 4/5/8/9/10 pre-verified against my locked texts; awaiting your intake-ack for the co-sign, then your IMPL report for the pair review

**Role accepted per the operator's B10 ruling** (registered, `PROTOCOL-DEVIATIONS.md`): for s7 the m-7 pair IS the build agent-pair — **you are the sole s7 code writer; I am the pair Planner** (domain guide for the engine rows + adversarial reviewer of your implementation before it reaches master). I do not edit `frank/` — the planner-seats-never-code fence (VP s7-F1) holds inside the pair exactly as outside it.

**Binding chain read whole this session:** the v3 dispatch (`PLAN-…-032426`), r2 (`PLAN-…-023635`), the r3 replacements (`PLAN-…-030148` — rows 6 and 9 use the r3 text), the VP approval (`RECONCILE-…-030737`), and the B10 register entry. Supersession understanding confirmed: effective plan = r2 + the four r3 folds; `…-031733` withdrawn; the `…-031728` stand-down clause superseded by B10. BASE re-verified at my seat just now: `frank/main@1d3e92c` clean, `test/invariants` absent — no s7 work has begun.

**Guide-half pre-check of the row contract against the locked record — NO blocker.** The five m-7-grounded rows are faithful to the locked texts as written:
- **Row 4** (`TestLawDerivedOnlyActivation`) = B-1 r5 exactly: `minted→bound→active` derived solely from committed records per m-1's ORDER rule; the check must assert the **absence** of any persisted activation marker (the r4→r5 correction is the law here, not a footnote).
- **Row 5** (`TestLawSoleGovernedWriter`) = A-4 (flock loser full-exit, `root-lock-held`) + I1-P at the c5/c6-narrowed grain — the claim-grain bound rides **verbatim**: sole *governed* write path, D5 same-uid direct-store residual stated. Short law names must not re-expand this.
- **Row 8** (`TestLawOnePivotPerMutation`) = the §4 grain: crash-before-pivot ⇒ absent, crash-after ⇒ present, exactly one rename per governance mutation.
- **Row 9** (`TestLawIntakeOutcomeOneToOne`) = my A-2 grain **verbatim** (the r3 text cites the s6 transport amendments `:35-39` correctly): all five clauses, and **pending-with-zero-outcomes is legitimate state, never a violation**.
- **Row 10** (`TestLawRebuildBeforeOpen`) = the R-3 discipline: refuse `submit` until the recovery phases complete; Ready only post-rebuild.

**One precision note for the build (row 6 — not a blocker, a census requirement):** the r3 census families (bounce/reason text · process/tool errors · tool descriptions/results · rendered projections · delivery payloads) must include the **`seat_mint` accept-reply as its own named census row** — it is the fifth seat-delivered I-PH family (the R2 fold in `s6-guide-m7/DESIGN-REVIEW-planner-20260707-012324.md`), and it is the one family carrying **two NAMED carve-outs** (the fresh credential + the new seat's endpoint, operator-channel-only). Enter it in the census with the exemption stated, so the scanner neither misses the family nor false-positives on the sanctioned carve-outs. VP finding 4's acceptance behavior applies to it like any family: unregistered-but-seat-visible ⇒ red.

**The pair-review checklist, pinned now so the review is not improvised** (what my adversarial pass will check when your IMPL report arrives):
1. ten named tests present, each **executing its mechanism** from `test/invariants` (pointer metadata = fail);
2. rows 6 and 9 implement the **r3** text; rows 4/5 carry the claim-grain bounds verbatim;
3. row 6: census named-and-counted, complete-corpus scan, **planted-leak negative red**, and the mechanical unregistered-family-⇒-red behavior demonstrable (VP finding 4) — including the mint-reply row per the note above;
4. the red-battery transcript command-pinned to `go test -count=1 ./test/invariants` (fail names the law → scratch discarded → green);
5. full uncached `go test ./...` + `go vet` at tip (baseline shape 24 ok + 2 no-test-files);
6. diffstat **test-only** — zero production paths, no registry edit, no new record kinds;
7. the catalog artifact carries the law list, owners, and the single-writer/owner-fidelity convention — **no §7/governance claim** (that is the s8 carry);
8. findings-not-fixes fence respected: any defect a law exposes is relayed to master, never fixed in-slice.

**Relay-plumbing reading, stated so it cannot drift:** per dispatch step 2, the pair's s7 relays (your intake-ack, my pair review, your IMPL report) live in `frank/.relays/s7/` (gitignored — my never-write-tracked-`frank/`-files fence holds); my master-lane relays, like this one, stay in `master/relays/`.

Not authorized / not claimed: readiness + pre-check only — no code, no `frank/` write, no intake co-sign yet (that waits on your intake-ack), no review verdict (that waits on your IMPL report), no row contract amended.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s7-dispatch/SITREP-planner-20260710-033057.md` — run before handoff (result in inline pointer)
- read whole this session: the v3 dispatch, r2, r3, the VP approval, the B10 register entry (`PROTOCOL-DEVIATIONS.md:101-115`)
- `git -C frank status --short --branch` = `## main...origin/main` clean at `1d3e92c`; `frank/test/` contains fixtures/replay/seatproc only — `invariants` absent (matches the VP's pre-dispatch probe)
- locked texts re-checked for the row pre-verify: B-1 r5 (derived-only), A-4 + I1-P grain, §4 pivot, A-2 `:35-39`, R-3, the R2 fifth-family fold

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260710-033057; `frank/` read-only; no doc/design/code edits
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: operator carries this to the m-7.implementer session alongside the v3 dispatch; the implementer files the intake-ack in `frank/.relays/s7/` (blockers/ambiguities to master), the operator carries it here, and I co-sign per dispatch step 3 — then the build proceeds and my next act is the pair review of the IMPL report.
