## SITREP — routing the VP's F1-R2 to the s7 pair (the last open finding): the boundary FILE set must be guarded by a full-tree scan — seat-egress capability outside the registered boundary files fails the named law; proof = a genuine-new-FILE scratch red; everything else in the slice is closed or pre-ruled standing

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
EVIDENCE_TARGET: E2
DISPATCH_ID: s7-dispatch
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
HUMAN_GATE_REQUIRED: no — one narrow test-only fold under the standing s7 authority (the VP's own routing); the merge relay stays unissued until VP approval
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/s7-dispatch/RECONCILE-orchestrator-reviewer-20260710-180750.md
FROM: master.orchestrator-planner
TO: m-7.implementer
CC: operator, master.orchestrator-reviewer, m-7.planner, m-1.implementer, m-2.implementer, m-4.implementer, m-2.planner, m-4.planner
SUBJECT: fold the VP's F1-R2 (`RECONCILE-…-180750`) under your standing s7 authority — derive-or-guard the boundary file set across the full production tree (a full-tree non-test scan failing when seat-egress write/encode/protocol-dispatch capability appears outside the registered centralized boundary, with the exact 17-site census retained inside it); scratch red = a NEW production egress file + an unknown helper call from the current boundary → the named law fails BEFORE any census edit → discard → green; all protected blocks byte-identical ⇒ no m-1/m-2/m-4 round (VP pre-ruled)

**What the VP closed (do not touch):** F2 whole (the recovery clause, executed with per-`intake_id` counts) · F1 at the symbol grain inside the current files · the current 17-site census as coherent (the channel socket writer + the MCP stdout encoder are today's whole egress topology — its own production-wide scan concurs) · rows 1–5, the m-1/m-2 row-6 halves, row 3 + catalog bytes, F3 reporting, and all standing disclosures.

**The one open finding, F1-R2 (the VP's exact reasoning, carried whole):** your discovery finds unknown *symbols* only inside a trusted, hard-coded two-file slice — the unguarded assumption moved up one level. A new production file (e.g. a helper in a new `internal/channel/*.go` calling `serverConn.write`, invoked from an existing boundary method through an unrecognized call) emits a seat-visible frame while the 17 strings and protocol-case counts stay unchanged — the named law stays green. And the s8 §7-pinning your review note (and my package) leaned on **cannot mechanically close this**: pinning governs an intentional census edit; it cannot detect a file the battery never parses. The acceptance behavior from r3 ("a future seat-visible family cannot ride outside the census") therefore requires the file set itself to be discovered, not trusted.

**The fold (the VP's required shape, verbatim bar):**
1. **Derive or guard the boundary file set independently across the production tree** — a valid shape: a full-tree non-test AST/type-aware scan that FAILS the named law when seat-egress write/encode/protocol-dispatch capability appears **outside** the registered centralized boundary files, while the exact site census is retained **inside** them. (The scan defines "capability" tightly enough to exclude canonical persistence — the VP's own scan classifies store/fsio writes as persistence, not seat egress; that distinction is yours to encode with your planner-guide.)
2. **The command-pinned scratch red:** under scratch, add a NEW production egress file plus an unknown helper call from the current boundary → `go test -count=1 ./test/invariants` FAILS on the named row **before any expected census is edited** → discard → the real branch green. Cleanup proven (no scratch worktree/branch remains).
3. **Fences:** test-only; preserve the six families, all captures/negatives, the canonical-path census, F2, row 3, and the catalog **byte-identical** (then no m-1/m-2/m-4 round is owed — the VP pre-ruled it; the only changing mechanism is your discovery root).
4. **Return path:** your fold report → the m-7 pair review over the full-tree guard + the new-file scratch proof → master reruns → the corrected package to the VP with its enumerated evidence set (focused row-6 verbose · the invariants package · the serialized full battery · vet · diff/path/protected-byte proofs · scratch cleanup proof · exact final status, tracked-vs-untracked distinguished).

Next requested action — m-7.implementer: the fold + evidence, then the pair review leg. The merge relay stays unissued until the VP approves the corrected package.

ACTIONS_GIT_REF: none — routing only.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main = `## main...origin/main` clean at `54420db`; the s7 worktree at `bbf3147` — `.relays/s7/INDEX.md` tracked-and-modified + untracked `.relays/s7/s7-inv-catalog-impl/`, implementation paths clean.
