## Team m-1 — Trust & Identity: DESIGN-REVIEW REQUEST

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c1-design-m-1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — read-only DESIGN-REVIEW; operator decisions already grilled (folded into GRILL_LOCK)
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, m-2.planner, master.orchestrator-reviewer, operator
RUN_ID: master
PARENT_DISPATCH_ID: c1-design-m-1
DESIGN_DOC_ID: c1-design-m-1-trust-identity
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
REPO: master/ (docs workspace; cwd is not a git repo; pcode/ is the future code repo)

Current scope for the `TO` addressee: read-only DESIGN-REVIEW. No source/test edits, no implementation branches, no commits, no PRs. This cycle is AUDIT+DESIGN only; the terminal is design-lock.

Design doc to review:
- DESIGN_DOC_ID: c1-design-m-1-trust-identity
- Location: master/domains/m-1-trust-identity/design/2026-06-28-v3-trust-identity-design.md
- Selected option: a sole-writer SMTP-inspired stamping conductor; **Option A transport** (minted per-seat credential over an isolated per-seat connection) licensing "forgery-robust by construction"; four verbs `submit`/`project`/`read`/`mint_seat`; on-disk = v2.8.8 layout as a projection of a conductor-stamped typed-envelope record; non-re-cut Step-1→standalone (swap only the attestation backend).
- Rejected alternatives: Option B (operator-attested — only confusion-resistant; kept only as the honest fallback label if a deployment cannot meet DI-2); Option C (OS-peer/mTLS/SPIFFE now — premature; it is the standalone backend); public `attest()` (folded into `mint_seat`; `submit` fails on unbound); payload display-FROM (DMARC trap); Merkle/DKIM in v3.0 (deferred, courier-as-TCB); agent-writable store/FROM + lane-writable mailboxes (the gaps being closed).
- Boundary contract: §7 — m-2 (system-field set + sole-writer store; system-filled PARENT = forgery-robust lineage; joint lock); m-3 (store-isolation boundary reused for probe-from-outside + `submit()` pre-send hook); m-4 (stamped store as routing-record target); m-6 (addressing graph / inbox projection). identity≠authority routed to m-4/orchestrator.
- Acceptance criteria draft: §12 (8 criteria; DI-1/DI-2 verifiable E2).
- Operator decisions/defaults: GRILL_LOCK_ID c1-design-m-1-grill (§10) — Option A; defer Merkle; SMTP idiom+security-model over a typed envelope; certification null-reserved; attest folded into mint_seat; identity≠authority. m-1↔m-2 envelope contract converged (B1/B2/B3 acked, `c1-design-m2-m1-coord`).
- Open questions: §11 — DI-2 concrete realization (the "fork-2 infra call", → orchestrator at PLAN); identity≠authority confirm (→ m-4/orchestrator); mailbox realization (→ PLAN).

Specific challenge targets (adversarial review — try to break these):
1. **The I1/I2 proof (§4).** Is the strength claim "forgery-robust by construction" actually earned by DI-1..DI-4, or have I smuggled an assumption? Attack the enumeration (a)-(d): is there a fifth attack vector (e.g. TOCTOU between resolve and append, credential reuse across re-mint, a confused-deputy via the inline gate, mailbox-write as a side channel)?
2. **DI-2 load-bearing-ness.** The whole "by construction" claim rests on per-lane credential isolation. Is it honest to license the claim at DESIGN when DI-2's Step-1 realization is deferred to PLAN? Should the claim be conditional-stated more strongly?
3. **recipient_picker / parent_picker.** Does sender-selected TO/CC (B2) open any forgery or mis-delivery surface I missed? Is the conductor-derived parent candidate set genuinely unspoofable?
4. **Non-re-cut (§9).** Is the API truly invariant under the token→SO_PEERCRED/mTLS swap, or does an attestation upgrade leak into the verbs/envelope?
5. **Reuse vs rebuild.** Anything in §6 on-disk/mailbox I am rebuilding that should be promoted from v2.8.8/claude-code instead (or vice versa)?

Requested response:
Use Template C (`PHASE: DESIGN-REVIEW`, `FROM: m-1.implementer`, `TO: m-1.planner`, `PARENT_DISPATCH_ID: c1-design-m-1`, same `DESIGN_DOC_ID: c1-design-m-1-trust-identity`) and return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`.

ACTIONS_GIT_REF: no edits claimed beyond authoring this relay + the design doc/README under master/domains/ (design artifacts, not source); cwd is not a git repo so a git ref is unavailable.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
