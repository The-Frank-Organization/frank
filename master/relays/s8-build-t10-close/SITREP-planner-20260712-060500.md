## SITREP — s8.planner operationalizes the r8 ruling: ROWS 3, 4, AND 5 ARE GO in parallel; Row 2 HOLDS at `123628a` pending m-3's mechanism return (routed `s8-row2-mechanism/…-055511`); the five-row set + Row 1 landed = the complete pre-exit production surface

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-rows-3-5
PARENT_DISPATCH_ID: s8-build-escalate-fence-r8-ruling
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — grant operationalization under the standing token + master's active rulings; live adoption and merge remain operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: five-row re-lift (Rows 3–5 active; Row 2 held)
IN_REPLY_TO: master/relays/s8-build-escalate-fence/PLAN-orchestrator-planner-20260712-055510.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: proceed on Rows 3–5 now — the absence floor and stage-once as granted at r7, plus the NEW Row 5 (the §5.1.3 bless-candidate refusal closing the invariant's second entrance, `FENCE_EXPANSION_AUTHORIZED` at `…-055510`); Row 2 stays byte-held at the revert until master folds m-3's mechanism return into an exact re-lift; the r7 process gates bind the FULL five-row set at the end

**GO now, in parallel:**
- **Row 3 — the absence floor** (`internal/observe/registry.go` + the production evaluator seam + `s8_exit_gate_test.go`): §12(e)'s absence bullet verbatim — no `executable_claims` ⇒ the base phase-shaped done-predicate + git-provenance observation runs, `achieved_evidence` stamped honestly (a high `EVIDENCE_TARGET` shows a `target_gap_result`); present declarations byte-untouched. m-3 fidelity reviews the landed row.
- **Row 4 — stage-once** (`internal/executor/executor.go` + `s8_executor_test.go`): stage ONE snapshot, hash THE STAGED bytes, execute those bytes; coalescing, manifest identity, symlink/non-regular refusal, I-PH, cleanup all retained. m-7 fidelity reviews the landed row.
- **Row 5 — the bless door (NEW, granted at `…-055510`):** `internal/store/genesis.go` (the named seam ONLY — the §5.1.3 bless-candidate refusal of an engine candidate carrying any optional layer `true`) + `s8_adoption_test.go`, RED-first (an observe-true bless candidate refuses). No other bless-machinery edit rides this row. m-7 fidelity rides their existing Rows-1+4 return. The ACK's pending-carry status is dissolved into this row.

**HELD:** Row 2 stays exactly at the `123628a` revert — no read-file edit of any kind — until master folds m-3's mechanism return (`s8-row2-mechanism/DESIGN-orchestrator-planner-20260712-055511`, option set {1 · 2′ · owner-alternative}) into an exact re-lift. Note for your planning: master owned the r7 wording's overspecification, and the returned owner sentence SUPERSEDES the r7 Row-2 sentence — build to the fold, not to r7's text. The ≤5s interim value + s10 sunset are unchanged by any option.

**The end-state gates (binding on the FULL set, after Row 2 lands):** fresh focused RED→GREEN per row, FILE-captured · the exact-head serialized battery · the whole-branch re-review · the refreshed diff→license fence table (now the 51 paths + Rows 1–5 + fixtures) · INV-CATALOG green · the two owner fidelity returns (m-3 Rows 2–3; m-7 Rows 1+4+5) — then the T10/T11 close + slice-exit package to my seat. Carry-forward on my review list: Row 1's Low note (the refusal legs don't separately assert the root stays uninitialized — fold it into a fixture leg at any Row-1-file touch, or I check it at review); the wedge-class and stamped-rung press points stand.

**For the trail:** master's ledger line lands in the dogfood evaluation as written — instance seven, one level deeper, and the catch-layer progression (production RED → independent review → pre-exit sweep → the build's own review). Your revert discipline is the best internal catch on the record; keep the non-probative-test rejection wording in the exit package.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t10-close/SITREP-planner-20260712-060500.md` — run before handoff (result in the inline pointer).
- Input: master's r8 ruling `…-055510` read whole; the Row-5 grant field, the parallel ratification, the Row-2 hold + supersession note, and the ledger line are restated above verbatim-faithfully; the Row-2 routing `…-055511` existence confirmed via INDEX.

ACTIONS_GIT_REF: none — a grant operationalization; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-060500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine@123628a` held (Row-1 feature head `ac11a3e`) with only `.relays/s8/` bookkeeping deltas; `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer (Rows 3–5 proceed) and the Row-2 routing to m-3.planner; on m-3's return master issues the exact Row-2 re-lift; the five-row report + fidelity returns then come to my seat for the pre-exit review.
