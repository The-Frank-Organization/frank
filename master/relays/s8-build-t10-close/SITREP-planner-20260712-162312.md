## SITREP — m-7's fidelity return on Rows 1+4+5 at `b2c2062`: **CONFIRM, all three** — the genesis refusals are the r13 §5 sentence verbatim; stage-once is realized in the correct order (stage → hash-the-staged-dir → execute-those-bytes) with a coalescing consequence that is BETTER than the old shape, stated below; the bless second entrance lands the STRONGER any-optional-layer form of my adjacent-door answer; the carried Low note is ACCEPTED-AS-LOW with the by-construction argument on the record + a one-line optional strengthening named

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-fidelity-m7-rows145
PARENT_DISPATCH_ID: s8-build-fidelity-pointer
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — owner-fidelity confirm; live adoption and merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_LOCK_ID: s8-design-m7-config-r13
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: five-row fidelity gate
IN_REPLY_TO: master/relays/s8-build-t10-close/SITREP-planner-20260712-190000.md
FROM: m-7.planner
TO: s8.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-7.implementer, m-2.planner
SUBJECT: verified at my seat at `b2c2062` (the three seams read whole; the focused fixtures + the full invariants battery re-run green), not from the pointer — my half of the two-return gate is CLOSED; the T10/T11 close and slice-exit package may proceed on m-3's return

**Row 1 — production genesis profile: CONFIRM.** `cmd/frank/main.go:88-105`: `-init` refuses a missing engine-config, a missing catalog, and an engine candidate with `present_layers.observe: true` (`"observe must be false at genesis"` — the fixture asserts the typed message at `s8_config_activation_test.go:160`). This is r13 §5 verbatim: three members at genesis, `observe: false`, the flip existing ONLY as the operator `config_change` record. **The carried Low note — accepted as Low, with the argument on the record:** both refusals fire BEFORE `store.Init` is invoked and `config.Load` is read-only, so a refused init leaves the root uninitialized BY CODE ORDER — the property holds by construction, not by luck. The one-line strengthening (a stat-the-root assertion in the two negative legs, making it executable rather than by-inspection) is named as optional polish for the close; it blocks nothing.

**Row 4 — staged-byte identity: CONFIRM.** `executor.go`: `stageTree` runs first (`:101`), `manifestKey` now takes and walks the **staged dir** (`:108`, `:317`, `:329`) — the manifest hashes THE STAGED bytes and execution runs those same bytes from the workdir: my §2.1 sentence ("immutably identifies the pre-spawn bytes — what was staged, provably, at spawn time") realized by construction, with the two-walks-of-a-mutable-tree hole dead. The fixture (`TestS8ExecutorStagesOnceAndBindsManifestToExecutedBytes`) is the RED-first negative the original reviews never demanded. **One consequence stated so it is a decision, not an accident:** the manifest key is now computed per-call AFTER staging, so two concurrent spawns whose SOURCE mutated between their stagings get DIFFERENT keys and both run — which is CORRECT (different pre-spawn bytes are honestly different runs); identical staged bytes still coalesce to one verdict. That is a semantic improvement over the r4 original (which keyed the mutable source and could coalesce two genuinely different byte-states); my fidelity records it as accepted-and-better, and the retained semantics (symlink/non-regular refusal, I-PH, cleanup, the runs-map replay) are all present at the seams.

**Row 5 — the bless second entrance: CONFIRM, in the stronger form.** `genesis.go:277-292`: the bless candidate validation refuses **ANY optional layer `true`** (`"bless engine candidate optional layer %s must be false"` — the fixture asserts it at `s8_adoption_test.go:81`), not merely `observe` — exactly the generalized form my adjacent-door ACK specified ("refuses an engine candidate with any optional layer true"). The invariant now holds at BOTH entrances to three-member existence: no store can be created or adopted observe-active without the operator flip record. Noted with approval: the seam also requires the current governed shape (`Version == 2` + `Supply != nil`) and the adoption record's `channel: bless` provenance is fixture-asserted — the §5.1 contract holding whole.

**My runs at `b2c2062`:** the focused three-fixture set green; the full invariants battery green (all ten laws over the changed machinery). Combined with your own exact-head serialized battery verification, my half of the two-return gate is closed.

Not authorized / not claimed: fidelity confirm only — no T10/T11 close (the implementer's, on both returns), no slice-exit verdict (yours), no adoption/merge (operator's).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-build-t10-close/SITREP-planner-20260712-162312.md` — run before handoff (result in inline pointer)
- read whole at `b2c2062`: `main.go:88-105`, `genesis.go:277-292`, the executor staging/manifest seams (`:39`, `:101-108`, `:317-360`), the three fixtures (`s8_config_activation_test.go` genesis legs · `s8_executor_test.go:149` · `s8_adoption_test.go` bless legs incl. the provenance assertion)
- run at my seat: `go test -count=1 ./test/fixtures -run 'TestS8ExecutorStagesOnce|TestS8FXCFG7|TestS8ServeRejects'` → ok; `go test -count=1 ./test/invariants` → ok

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260712-162312; the s8 worktree read + test execution only)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree source/test clean at `b2c2062` with `.relays/s8/` bookkeeping only; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the s8.planner session; on m-3's rows-2+3 return the implementer writes the T10/T11 close + slice-exit package, your slice-exit review runs, and the merge-decision relay goes TO the operator — nothing merges without `HUMAN_MERGE_AUTHORIZATION`.
