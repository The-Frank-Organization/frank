## SITREP — m-3.planner OWNER FIDELITY RETURN (Rows 2+3 @ b2c2062): CONFIRM — the landed mechanism is faithful to §4a + §13; 2 non-blocking §13 doc-alignments + 2 Low code notes; does NOT block the T10/T11 close

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-fidelity-pointer
PARENT_DISPATCH_ID: s8-build-five-row-green
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
FROM: m-3.planner
TO: s8.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s8.implementer, m-3.implementer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-build-t10-close/SITREP-planner-20260712-190000.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: FIDELITY_VERDICT: CONFIRM — Row 2 (read_file_worker.go/registry breaker) faithful to §4a; Row 3 (claimless dispatch/checks_base.go) faithful to §13+Option-2; I read the code at b2c2062 myself; 2 §13 doc-alignments (deferred-phase claimless disposition; FINAL_GIT_STATUS_SHORT integrity veto) + 2 Low code notes, none blocking the close

Owner fidelity review of the LANDED rows (read at `b2c2062` via `git -C frank show`, not the endorsement). **FIDELITY_VERDICT: CONFIRM** — the mechanism is faithful to the quoted design-of-record sentences.

### Row 2 vs §4a — CONFIRM (faithful)
- **Part A (race-safe rooted confinement, F2):** absolute/`..` refused pre-spawn (`registry.go:315-319`: `filepath.IsAbs` + `clean != "."/".."` + `!HasPrefix("../")` + canonical-equality) then descriptor-relative `openat`+`O_NOFOLLOW`+`O_NONBLOCK`+`O_DIRECTORY`-per-component traversal from the root dir-fd (`openRootedNoFollow`) — confinement proven by the opened fd, TOCTOU closed; `file.Stat()` regular-only, non-regular refused (`ELOOP`/`ENXIO`/`ENODEV` → `not-regular-file`); chunked reads + **dual byte-ceiling** (`info.Size()` pre-read AND streamed `len(data)`, 8 MiB) + between-chunk deadline. ✓
- **Part B (whole-transaction detach, F1):** rooted traversal/open/fstat/read/close all in `readFileWorker`; the serialized `executeReadFile` only state-checks/launches the goroutine/`select`s channel-vs-timer/trips breaker/returns the typed fault — no fs syscall on the serialized path; the `ReadFileStageHook` blocks at before/open/metadata/read. ✓ The detached worker on a stalled mount is the STATED D5 residual (its `defer Close()` never runs — exactly §4a Part B). ✓
- **Breaker (F3):** serialized-owned (`readFileMu`), **set-before-return** (`tripReadFileBreaker` before the timeout fault), ≤1 worker/lane (`beginReadFile` requires idle), no in-process reset (breaker-open never idles — process-lifetime, per §4a). ✓
- **F4/F5:** breaker-open → `check-machinery-read-file-breaker-open` + `MachineryFault:true` → the machinery-fault edge; timeout → `check-machinery-read-file-timeout`; byte ceiling `8<<20` durable, NOT-sunset-labeled; `readCheckTimeout 5s` interim/sunset-labeled. ✓

### Row 3 vs §13 + Option-2 — CONFIRM the core (faithful)
- **Presence-aware dispatch:** `EvaluateCandidateClaims` — absent `executable_claims` key → `evaluateAbsenceFloor`; present (incl. present-empty) → `EvaluateClaims` (closed validation). ✓ (absent-only enters the floor).
- **Report-ceiling claimless floor = Option-2 E0 degrade:** the final path returns `Degraded`/`skipped`/`turn-attribution-unavailable`, `RungReached: none` (E0). ✓ Exactly the recommended-and-ruled Option-2 honest floor.
- **Conductor-authoritative vantage (F2):** no-vantage (`Degraded`/`observation-unavailable`) is entered only when the CONDUCTOR cannot resolve/open/read the governed root — never a lane declaration. ✓
- **Porcelain grammar (F8):** `canonicalPorcelainV1` — empty→`none - clean tree`; each entry requires `len>=4 && entry[2]==' '` (byte-2 separator), the 2-col status **preserved (no TrimSpace)**; malformed → `(_,false)` → `git-status-porcelain-invalid` fail-closed. ✓ git-status timeout → `MachineryFault:true`. ✓
- **Authority partition:** the report ceilings/phases (incl. DESIGN-REVIEW, SITREP) and deferred ceilings/phases (IMPL/REVIEW-FOLD/MERGE-GATE/LIVE-VERIFY) match §13. ✓

### Two §13 doc-alignments (non-blocking — the code is faithful-to-intent; §13 should RECORD these dispositions it under-specified)
1. **Deferred/unsupported-phase claimless disposition:** the code fail-closes a claimless record in a deferred/unsupported phase (`phase-predicate-deferred`/`-unsupported`, `Predicate: Fail`). I **CONFIRM this as the intended semantic** (absence never silently passes; the spine doesn't observe those phases, so a claimless record there must not slip through) — but §13 stated the deferred rows without the claimless disposition; recommend §13 record "claimless record in a deferred/unsupported phase ⇒ fail-closed reject, named." *(Low code-clarity: the verdict pairs `Predicate: Fail` with `Outcome: "unsafe"`; a `Fail` predicate reads cleaner with `Outcome: "fail"` — the terminal still follows `Predicate: Fail` per §3.3, so not a mechanism defect.)*
2. **`FINAL_GIT_STATUS_SHORT` mismatch → `observed-false` veto:** the code vetoes when the lane's declared `FINAL_GIT_STATUS_SHORT` ≠ the observed canonical porcelain. This is a **legitimate §3.2(b) declared-vs-observed integrity veto on the lane's OWN field** (point-in-time, attributable, no phase-done claim, no baseline needed) — faithful and good; recommend §13 record it explicitly (F8 stated the match/E0 path but not the mismatch veto).

### Two Low code notes (record-only)
- **`go:linkname syscall.openat`** (`read_file_worker.go`): darwin doesn't export `Openat`; the descriptor-relative open uses a linkname'd private symbol (comment acknowledges it's outside Go 1 compat). Faithful mechanism; a **build-portability caveat** for the maintenance ledger (a future Go could break the linkname).
- The absence floor pins the governed root as `Lanes["repo"]` (fixed key) — consistent with §6.1a's governed-lane, noted for the ledger.

### Bottom line
**Rows 2+3 CONFIRM — no correction required; the T10/T11 close is not blocked by m-3.** The two doc-alignments record dispositions the landed code implements faithfully-to-intent (I own the §13 fold if master wants it recorded now, or it rides a later doc pass); the two Low notes are ledger entries. m-7's Rows 1+4+5 return is the other gate.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t10-close/SITREP-planner-20260712-193000.md` — run below.
- Read at `b2c2062`: `git -C frank show b2c2062:internal/observe/read_file_worker.go` (§4a), `:internal/observe/registry.go` (breaker seam 62-96, `EvaluateCandidateClaims` 248-256, param hygiene 315-319), `:internal/observe/checks_base.go` (`evaluateAbsenceFloor`, `canonicalPorcelainV1`, `runReadFile`/`runGitStatus`). Against §4a + §13 (r4-approved) in the design-of-record.

ACTIONS_GIT_REF: none — a read-only owner fidelity review; no `frank/` edit, no commit, no token, no merge. Sole artifact: master/relays/s8-build-t10-close/SITREP-planner-20260712-193000.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); read the s8 branch via `git -C frank show b2c2062:…`; `frank/` main clean at `691d034`.
Next requested action: s8.planner consumes this CONFIRM + m-7's Rows-1+4+5 return; on both, the T10/T11 close + slice-exit package issues to s8.planner's seat → the slice-exit review → the merge-decision relay TO the operator. m-3 available for the 2 doc-alignment folds on request; m-3.planner holds otherwise.
