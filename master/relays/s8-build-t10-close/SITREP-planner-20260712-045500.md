## SITREP — s8.planner operationalizes the r7 grant: the four rows are GO at `d9fb80d` — each implements its QUOTED locked sentence, RED-first negatives per row, the last production edits before slice exit; the four-row report returns to my seat with the exact-head battery, the whole-branch re-review, and the refreshed fence table; fidelity reviews the LANDED rows

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-four-rows
PARENT_DISPATCH_ID: s8-build-escalate-fence-r7-ruling
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — grant operationalization under the standing token + master's active four-row fence; live adoption and merge remain operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T10, T11, slice-exit review
IN_REPLY_TO: master/relays/s8-build-escalate-fence/PLAN-orchestrator-planner-20260712-044510.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: resume on the four granted rows (`…-044510`, FENCE_EXPANSION_AUTHORIZED verbatim — row 3 is `internal/observe/registry.go`, NOT fieldspec; the T9 Parse grant's non-precedential terms are untouched); the quoted owner sentences ARE the specs; fidelity confirms come AFTER landing (m-3 rows 2–3, m-7 rows 1+4), and the slice-exit package consumes them; T10/T11 stay held un-reported until then

The four rows are GO. The specs are master's quoted sentences — build to them verbatim, nothing beyond them:

1. **Genesis profile** (`cmd/frank/main.go` + `s8_config_activation_test.go`; m-7 reviews landed): production `-init` REQUIRES the catalog member and REFUSES an engine candidate with `present_layers.observe: true`; the legacy two-member constructor stays fixture/bless-only. Spec: r13 §5 — three members, `observe: false` at genesis, the flip exists ONLY as an operator-authored restart-effective `config_change`. RED-first: the missing-catalog refusal leg + the observe-true refusal leg. **Carry m-7's named adjacent door in your report:** whether the same refusal binds the §5.1.3 BLESS candidate validation — m-7's row-1 fidelity states it either way; if their answer requires a bless-side edit, that is a NEW named row, not an inference from this grant.
2. **`read-file` mechanism** (`internal/observe/checks_base.go` + `s8_check_registry_e1_test.go`; m-3 reviews landed): regular-file-only + bounded read + an ACTUAL ≤5s wall deadline + byte ceiling; faults symbolic/path-free. Master's severity framing binds the test shape: the unbounded read is a LIVENESS hole inside the serialized commit loop — the RED-first negative should prove the wedge class dies (a non-regular file refuses; a deadline actually fires), not merely that the label exists.
3. **The absence floor** (`internal/observe/registry.go` + the production evaluator seam + `s8_exit_gate_test.go`; m-3 reviews landed): a record with NO `executable_claims` runs the locked base observation — §12(e)'s absence bullet verbatim: the base phase-shaped done-predicate + git-provenance observation, `achieved_evidence` reflecting it honestly (a high `EVIDENCE_TARGET` shows a `target_gap_result`). Present declarations keep the closed validation/run-all semantics byte-untouched. m-3's fidelity return may refine the per-phase predicate set / the stamped rung / the class-1 veto wiring — a refinement adjusts the fix in THIS lane, no new thread.
4. **Stage-once** (`internal/executor/executor.go` + `s8_executor_test.go`; m-7 reviews landed): stage ONE run snapshot, hash THE STAGED bytes, execute those same bytes — coalescing, manifest identity, symlink/non-regular refusal, I-PH, and cleanup semantics all retained. Spec: m-7 §2.1's "what was staged, provably, at spawn time."

**Process, binding (master's terms + my review expectations):** fresh focused RED→GREEN per row, FILE-captured, sequence-honest — the negatives the original reviews never demanded get written RED FIRST · the exact-head serialized battery on the final commit (the `cf67432` capture stays labeled non-final) · the whole-branch re-review · the refreshed diff→license fence table (the 51 paths + these rows + their fixtures) · INV-CATALOG green · no other production surface touched — these four rows are the LAST production edits before slice exit. T10/T11 stay held un-reported exactly as you have them; the eventual T10/T11 close + slice-exit package to my seat consumes the two fidelity returns.

**My review posture at the four-row report (stated now so the bar is visible):** I will re-run the RED-first negatives and the battery, re-derive the fence table from `git diff --name-only 691d034..<head>` myself, sweep each row label→mechanism (the r7 lesson), and check counts/selectors only from re-executed output (the verify-the-summary-line rule). The wedge-class negative (row 2) and the absence-floor stamped-rung leg (row 3) are where I will press hardest.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t10-close/SITREP-planner-20260712-045500.md` — run before handoff (result in the inline pointer).
- Input: master's r7 ruling `…-044510` read whole; the grant field, the four quoted sentences, the fidelity-as-review model, the adjacent door, and the process terms are restated above verbatim-faithfully.

ACTIONS_GIT_REF: none — a grant operationalization; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-045500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine@d9fb80d` held with only `.relays/s8/` bookkeeping deltas; `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer (the build proceeds on the four rows) and flags the two post-landing fidelity legs to m-3.planner and m-7.planner; the four-row report + fidelity returns come to my seat; then the T10/T11 close, the slice-exit review, and the merge-decision relay TO the operator.
