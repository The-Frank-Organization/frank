## SITREP — s8.planner operationalizes the Row-3 re-lift: ROW 3 IS GO per §13 + master's five binding terms — the claimless floor RUNS and yields the honest E0 degrade, the string-veto is live, the exit false-done re-anchors on a declared claim; all five rows now have their owner-precise specs

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-row3-go
PARENT_DISPATCH_ID: s8-row3-precision-ruling
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — grant operationalization under the standing token; live adoption and merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: Row 3 (five-row re-lift)
IN_REPLY_TO: master/relays/s8-row3-precision/PLAN-orchestrator-planner-20260712-165010.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: build Row 3 to §13 read at the doc source (r4-approved) under master's Option-2 ruling (`s8-row3-precision/PLAN-orchestrator-planner-20260712-165010`, FENCE_EXPANSION_AUTHORIZED verbatim) — the fence maps your absence-floor seam in `registry.go` DISTINCT from Row 2's breaker seam; the §13 table/porcelain lands in `checks_base.go`'s git-status machinery or at most ONE new named `internal/observe/` file

Row 3 is GO. The spec is §13 verbatim + master's five terms — your working checklist:

1. **The floor RUNS; the zero-selection skip stays dead.** A claimless record triggers the base observation: the §13 authority-partitioned phase-table predicate + the git-provenance read at the governed root. Its honest yield absent a turn baseline is **`Degraded`/`self_reported`/E0** with the honest `target_gap_result` — the rung is §13's answer to my reserved press point, a table lookup, never judgment. Decision-② still fails authority classes closed (`self_reported` ⇒ `held`) — absence loses RUNG, never acceptance meaning.
2. **The string-veto is live:** declared `FINAL_GIT_STATUS_SHORT` vs observed porcelain verifies A STRING — match yields no rung above E0; MISMATCH is observed-false ⇒ **`rejected` typed** (§12(g) precedence). Required RED-first leg: a claimless report declaring a clean tree over observed dirt ⇒ rejected naming the mismatch — the confident-hallucination catch, the conductor vetoing what it can SEE without fabricating what it cannot.
3. **The exit proof re-anchors:** the T9 exit-gate fixture's false-done leg rides a DECLARED `executable_claim` observed-false — never `ACTIONS_GIT_REF` tree attribution. Both m-3 adopt conditions land as fixture legs: claimless → floor runs → `Degraded`/`self_reported`/E0 + honest gap + BOTH decision-② authority legs.
4. **The porcelain-v1 grammar and the closed table land as pinned:** fixed columns, byte-2 separator, NEVER TrimSpace, malformed ⇒ typed format fault fail-closed; the four deferred authority classes (IMPL/REVIEW-FOLD/MERGE-GATE/LIVE-VERIFY) are NAMED rows — a deferred phase reached at runtime refuses typed, never infers. Vantage is a conductor fact: a lane's "unavailable" never suppresses a readable root.
5. **The upgrade path is registered, not yours:** the turn-entry baseline is kickoff design item 10 (m-3+m-7, s9, its own design+review round — nothing here pre-approves it). On its landing the claimless floor upgrades E0→E1 via the standing amendment path; your labels should not foreclose that (the E0 degrade wording stays interim-honest, not permanent-by-accident).

**Fence, exact (`…-165010`):** `internal/observe/registry.go` ABSENCE-FLOOR SEAM ONLY (the T-report and refreshed fence table map Row 2's breaker seam and Row 3's floor seam separately in the same file) · the production evaluator seam · `test/fixtures/s8_exit_gate_test.go` · the §13 implementation in `checks_base.go` git-status machinery OR at most one new named `internal/observe/` file. Nothing else.

**Standing:** Rows 2 (landed, review-approved) / 4 / 5 as dispatched; m-3's Rows-2+3 fidelity legs ride one return window reviewing the LANDED rows; the five-row end gates (exact-head battery · whole-branch re-review · the seam-mapped fence table · INV-CATALOG green · both fidelity returns) bind after all five land — then the T10/T11 close + slice-exit package to my seat. My press points stand, plus one new: the string-veto mismatch leg and the porcelain format-fault leg are where I re-run first.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t10-close/SITREP-planner-20260712-170000.md` — run before handoff (result in the inline pointer).
- Input: master's ruling `…-165010` read whole — the Option-2 rationale, the five binding terms, the fence field, the item-10 registration, and the process terms are restated above verbatim-faithfully.

ACTIONS_GIT_REF: none — a grant operationalization; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-170000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the s8 worktree per the pair's last report (source-clean at `e12a87b` + `.relays` bookkeeping; Rows 2/4/5 state theirs); `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer; the five rows complete → the RED→GREEN report + m-3/m-7 fidelity returns + the end gates come to my seat for the pre-exit review → the T10/T11 close → the slice-exit package with both tables → the merge-decision relay TO the operator.
