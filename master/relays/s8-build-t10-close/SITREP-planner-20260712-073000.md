## SITREP — s8.planner operationalizes the Row-2 re-lift: ROW 2 IS GO per §4a verbatim — the full five-row set is now in flight; after Row 2 lands the end-state gates fire and the slice runs to its exit package

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-row2-go
PARENT_DISPATCH_ID: s8-build-escalate-fence-row2-relift
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — grant operationalization under the standing token; live adoption and merge remain operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: Row 2 (five-row re-lift)
IN_REPLY_TO: master/relays/s8-build-escalate-fence/PLAN-orchestrator-planner-20260712-072010.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: build Row 2 to §4a read at the source (`2026-07-10-s8-check-registry-probe-design.md:83-88`, r3-approved) — Part A race-safe rooted confinement proven by the opened descriptor + chunked bounded reads + the durable 8 MiB ceiling; Part B the entire fs transaction behind the detachable worker with the per-lane breaker; the r7 "ACTUAL wall deadline" sentence is formally superseded and appears nowhere in the landed row's claims

Row 2 is GO under `s8-build-escalate-fence/PLAN-orchestrator-planner-20260712-072010` (`FENCE_EXPANSION_AUTHORIZED` verbatim). Working terms:

**1. The fence, exact:** `internal/observe/checks_base.go` (the whole §4a mechanism — the descriptor-relative race-safe rooted open REPLACING the `EvalSymlinks` path; Part-A chunked/bounded reads + the 8 MiB ceiling; the Part-B worker) · `test/fixtures/s8_check_registry_e1_test.go` (the §4a fixture set) · `internal/observe/registry.go` for the BREAKER/LAUNCH SEAM ONLY — explicitly distinct from Row 3's absence-floor seam in the same file; your report and the refreshed fence table map each row to its own seam · at most ONE new `internal/observe/` file if the worker/breaker splits out, named in the report.

**2. The spec is §4a verbatim (read the doc lines, not any relay summary):** Part A — confinement proven by the OPENED DESCRIPTOR, never a pre-open pathname; `fstat` regular-file-only; lane-creatable FIFO/socket/symlink-to-nonregular refused typed PRE-read; chunked reads with between-chunk ≤5s checks; the ceiling enforced against BOTH the `fstat` size and the streamed bytes. Part B — the serialized loop touches NO filesystem syscall: launch → buffered-completion-vs-deadline → trip-breaker SET-BEFORE-RETURN → typed fault; breaker-open = the explicit `check-machinery-read-file-breaker-open` machinery fault taking the two-axis edge; the D-state residual STATED beside D5, never claimed away.

**3. RED-first, probative-or-honestly-scoped exactly as master enumerated:** the FIFO/socket refusals + the three ceiling boundaries (exactly-ceiling / ceiling+1 / growth-beyond) are mechanically probative · the component-swap RACE leg proves no outside bytes cross · the DETACH leg proves through the injected blocking seam at all four block points (before/open/metadata/read), its claim text scoped to the seam grain · the BREAKER legs prove second-same-lane-refused-without-a-second-worker, different-lane-usable, BOTH authority-class terminals, and the exact fault class. The 1ns-polling pretense class is dead — a fixture that observes an expired timer instead of a blocked operation is a finding, not a proof.

**4. Two values, two lifetimes (state both in-code + in-trail so s10 cannot conflate them):** the ≤5s interim deadline SUNSETS at s10 per the ratified ruling; the 8 MiB `readFileByteCeiling` is a DURABLE fail-closed bound — NOT sunset (a promote-to-governed-config candidate later, per §4a/F5).

**5. Carry into the report:** the Part-A TOCTOU note (the rooted open also closes the shipped resolve-then-open race — a confinement hardening landing under the same owner sentence, name it in the dogfood record) · the two m-3 fidelity legs (Rows 2 + 3) ride one return window · Rows 3–5 continue exactly as dispatched · after Row 2 lands, the end-state gates fire on the FULL set: exact-head file-captured battery, whole-branch re-review, the refreshed fence table with same-file rows mapped to distinct seams, INV-CATALOG green, both fidelity returns — then the T10/T11 close + slice-exit package to my seat.

**My review press points for the five-row report (stated now):** the DETACH leg's four block points and the breaker's set-before-return ordering (Row 2) · the stamped-rung leg of the absence floor (Row 3) · the staged-bytes hash identity (Row 4) · the observe-true bless refusal RED capture (Row 5) · Row 1's carried Low note · the fence table re-derived by me from the raw diff.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t10-close/SITREP-planner-20260712-073000.md` — run before handoff (result in the inline pointer).
- Input: master's re-lift `…-072010` read whole — the fence field, the §4a two-part spec, the supersession, the fixture enumeration, the two-lifetimes note, and the process terms are restated above verbatim-faithfully.

ACTIONS_GIT_REF: none — a grant operationalization; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-073000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the s8 worktree holds at `123628a` (Row-1 feature head `ac11a3e`), Rows 3–5 in flight; `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer; the five rows complete → the RED→GREEN report + fidelity returns + gates come to my seat → the T10/T11 close → the slice-exit review → the merge-decision relay TO the operator under HUMAN_MERGE_AUTHORIZATION.
