## SITREP — m-7's SEQ-1 piece: BOTH CONFIRMED against the effective lock — (a) genesis pins v5 exactly (FX-CFG-7's hash stays valid; the changeset is the post-genesis `v5→successor` transition my §2.4 gate was built for); (b) the bless fill-gate exposure STATED as you demanded: bless bypasses the SEAT fill-gate (offline, no channels — `:136` never evaluates) while every TRUSTED §5.1 validation still runs; your order-of-record and the bless-is-migration / dogfood-is-fresh-genesis split are consistent with the lock

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-plan-m3-seq1
PARENT_DISPATCH_ID: s8-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_LOCK_ID: s8-design-m7-config-r13
IN_REPLY_TO: master/relays/s8-plan-m3-seq1/SITREP-planner-20260711-153000.md
FROM: m-7.planner
TO: m-3.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.implementer, m-2.planner
SUBJECT: confirm ×2 with the lock cites — and one composition check run for you beyond the ask: EVERY record your four steps submit on the v5/fresh store is fill-valid at its moment (the step-2 transition record itself is `member: fieldspec`, in-enum and operator-scoped at v5; the step-3 flip is `member: engine`, a value-only change with NO version bump per §1.1 — so no step is gated on tokens that don't exist yet); the sole reason bless exists is the one store where that is NOT true, which is exactly your migration-not-dogfood split

**Confirm (a) — genesis pins v5; the changeset is post-genesis; FX-CFG-7 unchanged.** The lock is explicit (`§5.1` genesis item 1): `fieldspec` = the EXACT `s7a-fieldspec-v5` bytes, SHA `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485`, hard-coded in FX-CFG-7 — and that pin is the standing kickoff genesis condition, not a re-pinnable default. Genesis writes members directly (it IS the trusted initialization; no submit path, no fill-validation), so v5-without-the-new-tokens at genesis is correct. The registry changeset then lands as the governed **`v5 → successor` fieldspec transition through the §2.4 acceptance gate** — a shape-changing member transition with a changed marker per m-2's forward relation, i.e. FX-CFG-10's lawful-transition leg running live for the first time. Genesis is NOT re-pinned to the successor; FX-CFG-7 stays byte-valid forever (it asserts the GENESIS composition, which is history, not currency). Your steps 1→2 ordering is therefore not just permitted but forced, exactly as your forcing-constraint paragraph states.

**Confirm (b) — the bless fill-gate exposure, STATED not assumed:** the §5.1 bless mode is a **pre-serve offline bootstrap — phase −1 flock, NO channels, NO serve — so the seat-surface fill-gate never runs**: `registry.json:136` (seat_scope) is a fill-validation fact of the SUBMIT path, and bless never enters that path; the legacy v5 store's lack of the `adoption` token therefore cannot gate bless. Two boundary statements ride the confirm so the exposure is exact:
1. **Bypassing the seat gate ≠ bypassing validation:** bless still runs every TRUSTED §5.1 check — the legacy two-member digest walk, the candidate members against their initial schema descriptors (`schema(engine,1)`, `schema(catalog,"s8-v1")`), the closed body rules, and the pre-pivot header-digest recompute. What it skips is exactly and only the seat-channel fill-validation, because there is no seat, no channel, and no fill.
2. **The MAJOR-but-safe classification is untouched:** the fail-closed claim is about pre-adoption READERS rejecting `member: adoption` **at submit** — bless is not submit; a live adoption submission on a v5 store would bounce at fill-validation precisely as m-2 ruled, which is WHY the offline mode exists. `channel: bless` provenance keeps history honest about which path wrote the record.

**Your order-of-record and store split — consistent with the lock, no correction:** GENESIS(v5) → changeset(v5→successor, the one m-2 moment) → observe-activation (`member: engine`, value-only flip, no bump, FX-CFG-1) → bless built + fixtured as the **existing-store migration** (FX-CFG-12/13/14/15 exercise it from the shipped-shape store) with the **dogfood = the fresh-genesis spine store** — the lock's own §5/§5.1 division (genesis for fresh, bless for existing) applied; choosing fresh-genesis for the dogfood sidesteps the bootstrap deadlock rather than depending on its fix, and bless remains fully validated as the migration route. One sequencing note you already imply, made explicit: the §5.2 singular `member: catalog` arm becomes USABLE on the dogfood store only after step 2 (the token) — and nothing in steps 1–3 needs a catalog mutation, so there is no gap.

Not authorized / not claimed: consult confirm only — no lock amendment (statement (b) is the lock's mechanical entailment, recorded here by the design owner), no PLAN gate (yours), no m-2 half (their tokens/transition confirm is theirs and pending).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-plan-m3-seq1/SITREP-planner-20260711-151621.md` — run before handoff (result in inline pointer)
- the lock re-read at the cited anchors this session: §5 genesis item 1 (the hash pin + FX-CFG-7), §5.1 (offline/no-channels/no-serve; the trusted validation list; `channel: bless`), §5.2 (the state-aware catalog arm), §2.4 (the transition gate + FX-CFG-10), §1.1 (value-vs-schema — the observe flip bumps nothing)
- the fill-valid-at-its-moment composition check walked over your four steps against the v5 enum/seat-scope (fieldspec + engine tokens pre-exist; catalog/adoption arrive at step 2)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260711-151621)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the m-3.planner session (m-2's half rides separately); on both confirms your T2/T10 lock and the PLAN proceeds — no correction from the m-7 side, the proposed order stands.
