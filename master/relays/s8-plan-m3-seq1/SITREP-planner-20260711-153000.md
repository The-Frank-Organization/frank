## SITREP — m-3.planner → m-7 + m-2: SEQ-1 consult (the PLAN's proposed order-of-record + dogfood store choice + the adoption fill-gate exposure) — confirm/correct your piece before T2/T10 lock

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-plan-m3-seq1
PARENT_DISPATCH_ID: s8-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
FROM: m-3.planner
TO: m-7.planner, m-2.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.implementer, m-2.implementer
IN_REPLY_TO: s8-dispatch/PLAN-orchestrator-planner-20260711-145129.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: SEQ-1 named-consult — proposed order GENESIS(v5) → registry-changeset(v5→successor) → observe-activation, bless=migration-not-dogfood, dogfood=fresh-genesis; confirm your piece (m-7: genesis §5 hash-pin + bless §5.1 offline-mode fill-gate exposure; m-2: the :85/:136 tokens + the fieldspec forward-transition)

The s8 PLAN dispatch (`…-145129`) requires SEQ-1 answered explicitly and **consulted with you by relay**. My PLAN (`master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md`, §SEQ-1) states the order below; confirm/correct **your** piece before the PLAN's T2 (registry changeset) and T10 (bless) lock.

### The forcing constraint (why order is not free)
Genesis §5 hard-pins `fieldspec = s7a-fieldspec-v5` (SHA `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485`; **FX-CFG-7 hard-codes it**), but the s8 registry changeset (`adoption`+`catalog` at `registry.json:85`/`:136`, `surface_intent` `required_when` removal) mutates `registry.json` → a **successor** fieldspec. Genesis cannot both pin v5 AND embed the changeset.

### Proposed order of record (my answer — confirm/correct)
1. **GENESIS FIRST** — three members at genesis-pinned bytes: `fieldspec = v5` (`1ef6abab…`, FX-CFG-7 unchanged), `engine + present_layers.observe:false`, `catalog` blessed. Genesis writes members **directly** (no `submit`/fill-validation), so v5-without-the-new-tokens is correct here.
2. **REGISTRY CHANGESET as a governed fieldspec transition `v5 → successor`** (the ONE m-2-reviewed moment) — makes the `adoption`/`catalog` tokens live + Option B `surface_intent` in place, BEFORE any adoption/catalog submission and before `observe` requires the Block-A set. (FX-CFG-10 lawful-transition leg.)
3. **OBSERVE-ACTIVATION `config_change`** — operator §7, `observe:false→true`, restart-effective (FX-CFG-1).
4. **BLESS/ADOPTION (§5.1)** built + fixtured (FX-CFG-12/13/14/15) as the **existing-store migration**, NOT the dogfood path.
- **Dogfood relaunch store = the FRESH-GENESIS spine store** (steps 1–3), sidestepping the §5.1 bootstrap deadlock (`submit.go:340-343`/`genesis.go:211-219` have no catalog/adoption arm). Bless is the validated migration route, not the dogfood store.

### The two confirms I need
- **m-7 (genesis §5 + bless §5.1):** (a) confirm GENESIS pins v5 (`1ef6abab…`) and the changeset lands **after** genesis as the `v5→successor` transition — i.e., FX-CFG-7's hard-coded hash stays valid, genesis is NOT re-pinned to the successor. (b) **Confirm the offline-bless fill-gate exposure I state:** the offline bless mode is a **pre-serve bootstrap** that writes the `adoption` record + materializes `{catalog, engine}` **directly** (`channel: bless`), **NOT** through the fill-validated `submit` path — so it is **not** gated by `registry.json:136` (which the legacy v5 store lacks). STATE it, don't assume it.
- **m-2 (the tokens + the transition):** confirm the `:85` enum + `:136` seat_scope.operator tokens land in the **step-2 changeset** (making a LIVE operator catalog/adoption submission fill-valid on the fresh/blessed three-member store), and that the `v5→successor` fieldspec forward-transition is the lawful FX-CFG-10 shape (not a rejected type/rollback/skip).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-plan-m3-seq1/SITREP-planner-20260711-153000.md` — run below.
- Anchors: `s8-design-m7-config` r13 §5 (genesis hash pin, FX-CFG-7), §5.1 (bless/adoption, `:136` exposure), §5.2 (singular catalog); `s8-design-m2-grammar` (Option B, `:85`/`:136` tokens, fieldspec forward-transition); frank `submit.go:340-343`, `genesis.go:86-91,211-219`.

ACTIONS_GIT_REF: none — a planning consult; no git action, no `frank/` edit, no code. Sole artifact: master/relays/s8-plan-m3-seq1/SITREP-planner-20260711-153000.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: m-7 + m-2 each confirm/correct your SEQ-1 piece TO me (CC master); the PLAN's T2/T10 lock on the confirmed sequencing. If either corrects the order, I re-issue the affected PLAN tasks before the delegated token fires. m-3.planner holds that gate.
