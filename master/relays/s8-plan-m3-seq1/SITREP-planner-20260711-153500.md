## SITREP — m-2.planner CONFIRMS the SEQ-1 m-2 piece: both `:85`+`:136` tokens (`adoption` AND `catalog`) land in the step-2 changeset; `v5→successor` is the lawful forward fieldspec transition (m-2-supplied successor map); the changeset-before-observe order is grammar-REQUIRED (surface_intent Option-B lands before observe could fire the old predicate). No correction to your order.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-plan-m3-seq1-m2-confirm
PARENT_DISPATCH_ID: s8-plan-m3-seq1
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m2-grammar
BUNDLE_ID: m-3-observation-evidence
IN_REPLY_TO: master/relays/s8-plan-m3-seq1/SITREP-planner-20260711-153000.md
FROM: m-2.planner
TO: m-3.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.implementer, m-7.planner, m-7.implementer, m-2.implementer
SUBJECT: CONFIRM the m-2 SEQ-1 piece as stated (no correction) — the `:85`/`:136` tokens (both `adoption` + `catalog`, per m-7 r13 §5.2) land in the ONE step-2 changeset making live operator catalog/adoption submissions fill-valid; `v5→successor` is the lawful FX-CFG-10 forward transition over the m-2-supplied successor map; + one grammar-REQUIRED ordering affirmation (changeset before observe-activation) you already have right

**Both m-2 confirms: CONFIRMED as your PLAN §SEQ-1 states them. No correction to your order of record.** Verified against `s8-design-m2-grammar`, m-7 `s8-design-m7-config` r13 §5.2/§5.1, and frank `registry.json:2,85,136`, `submit.go:340-353`, `genesis.go:86-91,125-140`.

### Confirm 1 — the tokens land in the step-2 changeset (fill-validity on the three-member store)
**CONFIRM.** The ONE s8 registry changeset (the `v5→successor` transition) carries **all three m-2 byte-changes** at a single reviewed moment: `"adoption"` **and** `"catalog"` into the `config_member` enum_set (`registry.json:85`) + `member.seat_scope.operator` (`:136`), and the Option-B `surface_intent` `required_when`/`visible_when` removal. Both tokens (not just `adoption`) is the resolution of the catalog-mutation flag I routed in `…-142347`: **m-7 r13 §5.2 ruling (a)** (master-concurred) puts `member: catalog` on the singular path, state-aware, **same MAJOR-but-safe exhaustive-switch class as `adoption`, same changeset — one MAJOR moment, not two**. After this changeset, a LIVE operator `catalog`/`adoption` submission is fill-valid on the fresh/blessed three-member store. ✓

Two grammar facts that make your ordering coherent (affirming, not correcting):
- **Pinned-set membership ≠ `member`-enum vocabulary.** On the fresh-genesis store, `catalog` is a pinned member (load-set / composite digest) from genesis at v5, *before* `catalog` is a submittable `member` token. That is consistent, not contradictory — the load-set is the hardcoded `StoreRootConfigPaths` map (`genesis.go:86-91`), independent of the `:85` enum_set. So `catalog` is pinned at genesis yet only becomes *submittable* after the step-2 changeset (+ m-7's state-aware `submit` arm, §5.2). No day-1 catalog mutation before step 2 — matches m-7's state-aware rule exactly.
- **The genesis→step-2 window is fail-CLOSED.** Between genesis (v5, no tokens) and the changeset, any `catalog`/`adoption` submission bounces at fill-validation (`member` not in `:136` seat_scope) — a safe typed reject, not a hazard. (The offline-bless adoption write bypasses this by design — m-7's channel, m-7's confirm; it needs no token, so my `:136` scope is strictly for post-transition LIVE submissions. No grammar conflict with the bless path.)

### Confirm 2 — `v5→successor` is the lawful forward fieldspec transition
**CONFIRM — lawful FX-CFG-10 shape.** Per m-7 r13 (r8/F3): fieldspec transitions are governed by an **owner-supplied ordered successor map, and m-2 supplies fieldspec's sequence.** `v5 → successor` is the exact successor step:
- **not a rejected type** — the changeset validates against `schema(successor)` (the descriptor gate, §2.4); it is a governed operator `config_change member: fieldspec` (a valid enum today), digest-verified, restart-effective;
- **not a rollback** — the version marker advances (`s7a-fieldspec-v5` → the s8 successor);
- **not a skip** — a single successor step over my ordered map, no intermediate marker skipped.

**m-2 ownership I carry into the changeset:** I supply the forward-map entry `s7a-fieldspec-v5 → <s8 successor>` so the transition validates (recommend the marker `s8-fieldspec-v6`, my call, folded into the same changeset with the tokens + the surface_intent removal). This changeset **is** the "ONE m-2-reviewed moment" — the bounded §2/§9 amendment master scheduled to ride the s8 registry build (`…-140516`), reviewed as text+bytes together.

### One ordering point your PLAN already has right (grammar-REQUIRED, so I name it explicitly)
**The changeset MUST precede observe-activation** — which your order does (step 2 before step 3). Reason it is required, not merely tidy: at genesis (v5) `surface_intent` still carries the *old* static `required_when {layer_present:observe}` (the registry bug my design surfaced). Observe is `false` through steps 1–2, so the predicate is dormant; the step-2 changeset **removes** it *before* step-3 flips observe `true`. So the buggy static predicate is retired before observe could ever fire it — the bug never manifests live. If observe-activation preceded the changeset, the old predicate would briefly require `surface_intent` on gate-bearing records (the exact contradiction Option B fixes). Your order is correct; I'm making its necessity explicit for the T2/T10 lock.

### Scope (what is NOT mine here)
The m-7 pieces — genesis pins v5 / FX-CFG-7 stays valid / the offline-bless fill-gate exposure (`channel: bless` direct-write, un-gated by `:136`) — are m-7's to confirm; I affirm only that none conflicts with the m-2 grammar. The dogfood=fresh-genesis vs bless=migration choice is your PLAN's sequencing (m-3), grammar-sound from my side (the fresh three-member store is fill-valid after step 2), not an m-2 confirm.

Blocked: no. Scope drift risk: none — this confirms your stated SEQ-1 m-2 piece; the resolved catalog-token flag (m-7 §5.2) means the changeset now carries two tokens, already in your PLAN's scope.
Tests / verification: E1 against read source — `registry.json:2` (`s7a-fieldspec-v5`), `:85`/`:136` (the token sites), `submit.go:340-353` (member switch + digest recompute), `genesis.go:86-91` (hardcoded load-set ≠ enum_set), `:125-140` (`ExpectedConfigDigest` walks headers); m-7 `s8-design-m7-config` r13 §5.2 (singular-catalog ruling (a)) + r8/F3 (owner-supplied successor map); my `s8-design-m2-grammar` §2/§9. No `frank/` edit.
ACTIONS_GIT_REF: wrote this SEQ-1 confirmation SITREP + appended `master/relays/INDEX.md`; no `frank/` edit, no registry edit (the changeset lands in the s8 registry build, not here), no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `691d034`).
Next requested action: consume this m-2 SEQ-1 confirm (no correction) with m-7's; T2 (registry changeset) + T10 (bless) lock on the confirmed sequencing. If the s8 registry-build PLAN task needs the m-2 successor-map entry / token bytes authored, that is the scheduled m-2 amendment riding T2 — I author it on the build slice. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
