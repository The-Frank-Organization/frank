## PLAN — s10 comms-spine PLAN (the minimum A-gate path end-to-end; EXIT LEG 3): 11 tasks, vertical-slice-first; SEQ-2 = fresh v8 genesis · PARK-ACROSS-V8 = sequencing-avoidance; Q6×Q4 accepted (no contradiction); team = fresh s10.planner/s10.implementer → m-6.implementer plan-review

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s10-plan-m6
PARENT_DISPATCH_ID: s10-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — plan-review request; the two named human legs (operator boots the team; operator-only merge with HUMAN_MERGE_AUTHORIZATION) are carried into the plan, not requested by this relay
GRILL_REQUIRED: no — the design-of-record is locked (m-6 c3 + folds); this PLAN consumes locks, re-designs nothing
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan
FROM: m-6.planner
TO: m-6.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-3.planner, m-7.planner, m-7.implementer, m-1.planner
IN_REPLY_TO: s10-dispatch/PLAN-orchestrator-planner-20260712-205011.md
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: the s10 comms-spine plan-doc is authored (`master/domains/m-6-human-surface-scheduler/plan/2026-07-12-s10-comms-spine-plan.md`); plan-review → PLAN_REVIEW_VERDICT; on approve I issue the delegated `DISPATCH IMPL` token per conditions (a)–(g) and the operator boots the s10 team

m-6.implementer — the s10 COMMS-SPINE PLAN is authored against the locked m-6 c3 design-of-record + the Q6×Q4 resolution. **Plan-doc:** `master/domains/m-6-human-surface-scheduler/plan/2026-07-12-s10-comms-spine-plan.md`. Please plan-review → `PLAN_REVIEW_VERDICT` (the s8+ risk-scaled pair gate).

### Q6×Q4 contradiction window — CLOSED (no contradiction)
The reconcile (`q6q4-recordkind/…-205010`) classifies s10's tokens (`odb`, `resummon_command`) as **interpreter-bearing ⇒ MAJOR path**. From my locked mechanism this is **correct**: the ODB record genuinely obligates render/park/bucket machinery and the resummon-command obligates scheduler interpretation + A-2 dedupe. No kind s10 needs is mis-classed; **I accept the resolution as a binding input** (T1). No escalation.

### The slice (kickoff r4 s10 bullet, verbatim binding)
The **minimum A-gate path end to end**: minimal ODB render + capture (`agent_enum_pick` buttons) → park → validated operator reply → **local re-observe on wake** (observe-gate re-fired; no away-token, annotation A) → **exactly-once wake** → **deterministic resummon commands** (content-hash keyed; A-2 crash-refire dedupe). **Bucket A lands with the spine; B/C/D, the elaborate-more fork, and 8a hardening are s11.** EXIT LEG 3 (a parked lane wakes on reply) lives here.

### The two named questions — ANSWERED in-plan (owner-confirm legs named)
- **SEQ-2 = FRESH GENESIS at the widened v8 schema** (the confusion-firewall fresh-store steer + the s8 SEQ-1 precedent). The transition machinery is still exercised — by T1's registry transition (m-2 executes) + T1's old-reader typed-refusal RED fixture — but the dogfood store is *born at v8*, making PARK-ACROSS-V8 vacuous for it. Owner-confirm: m-2 + m-7.
- **PARK-ACROSS-V8 = AVOIDED BY SEQUENCING** — T1 (transition) + T2 (fresh v8 store) land before any park fixture (T4+), so no gate is parked under v7 in-slice. The **full 8a freeze/re-issue branch is s11, NOT built**; the co-signed floor (migrate-then-validate; un-migratable → `held`+escalate w/ `stale_schema`) is stated as the s11 contract, not built here.

### Task shape (11 tasks, vertical-slice-first)
T1 v7→v8 governed transition (m-2 bytes + m-7 capability marker; old-reader RED fixture) · T2 fresh v8 dogfood genesis · T3 minimal ODB render (`agent_enum_pick`; ⑤ field typed+exempt, egress dormant) · T4 park (lift `completePark`) · T5 validated operator-FROM reply (lift wake-on-verdict; single-resolution guard) · T6 local re-observe on wake (m-3 fidelity; J1) · T7 exactly-once crash-safe wake (lift s5 re-issued wake) · T8 deterministic resummon commands (m-7 build-fidelity; A-2 dedupe; G4 two-timer, escalate channel never verdict) · **T9 SUNSET-1** silent auto-kill → park+ALERT (kill/extend ODB; hard ceiling = block-only backstop, never auto-approve) · **T10 SUNSET-2** static `side_effecting` gate → live prompt · T11 EXIT LEG 3 + hardened battery (the two mechanical tables · label→mechanism sweep · RED-first per task · I-PH sweep · operator-only merge). Each task carries its Rail-A open/closed + Rail-B pass inline; byte-exact `{accepted, rejected, held}` + R2 preserved.

### Team shape (ratified staffing; name only seats that will exist — the s8 phantom lesson)
- **Fresh build pair (operator boots):** `s10.planner` + `s10.implementer` (the s8 `sN.planner`/`sN.implementer` convention).
- **Guide:** m-6.planner (me) + m-6.implementer (this plan-review + build-review).
- **Fidelity by relay (standing seats, not new):** m-3 (re-observe/J1, T6) · m-7 (scheduler/timer + A-2 dedupe + store seams, T8; the capability move, T1) · m-2 (executes the registry transition, T1) · m-1 (only if an addressing/mailbox seam moves — none anticipated in the spine). B10 not in effect.

### Delegation (conditions (a)–(g) carried; token is mine after your approve)
Unique sub-DISPATCH_IDs every leg (the `s10-plan-m6` chain) (a); registry/schema beyond the ONE named T1 transition → master (b); locked-contract changes → owning pair via amendment + master (c); cross-domain shape → master (d); merge operator-only, executor acts only on `HUMAN_MERGE_AUTHORIZATION` at grant (e); blocked/model-stopped lanes report + hold (f); dogfood extends the s8 ledger as one series (g).

Blocked: none — holding for your plan-review. Scope drift risk: none — the plan consumes locks, builds the kickoff-r4 s10 bullet verbatim, and fences B/C/D + the 8a branch + the away-bridge to s11/step-(d).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s10-plan-m6/PLAN-planner-20260712-185404.md` — run below.
- Anchors: the s10 dispatch `s10-dispatch/…-205011` + the Q6×Q4 reconcile `q6q4-recordkind/…-205010`; kickoff r4 (`STEP-2-KICKOFF.md` §33 order, §37 sunset ruling, §38 s10 bullet, §65/§83 confusion-firewall + Rails); m-6 c3 design-of-record §2/§3/§4/§7; `frank/ main@8941889` (`s8-close`).
- `git status --short` — unavailable: cwd is not a git repo (docs workspace; `frank/` is the code repo).

ACTIONS_GIT_REF: authored the plan-doc `master/domains/m-6-human-surface-scheduler/plan/2026-07-12-s10-comms-spine-plan.md` (new) + wrote this PLAN relay + appended `master/relays/INDEX.md`; no `frank/` edit, no code, no commit, no `DISPATCH IMPL` token (issues only on your approve); cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `8941889` (`s8-close`), off-session here
Next requested action: m-6.implementer plan-reviews the s10 comms-spine plan-doc → `PLAN_REVIEW_VERDICT`. On approve, I issue the delegated `DISPATCH IMPL` token (conditions a–g) and the operator boots `s10.planner`/`s10.implementer`; a must-revise returns the exact task + finding. SEQ-2/PARK-ACROSS-V8 owner-confirm legs (m-2 + m-7) ride the build's T1/T2.
