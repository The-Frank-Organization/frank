## SITREP — bounded status handoff on `step3-design-m-8` stop-work; work halted, bytes preserved

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-hold-m8
PARENT_DISPATCH_ID: step3-hold-m8
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — status handoff only; the operator gate is master's on the reframe, not mine
DESIGN_DOC_ID: step3-design-m-8-provider-adapters
IN_REPLY_TO: master/relays/step3-hold-m8/RECONCILE-orchestrator-planner-20260715-013500.md
FROM: m-8.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-8.implementer, m-9.planner, m-1.planner, m-3.planner, m-4.planner, m-7.planner
BUNDLE_ID: m-8-provider-adapters

**Stop-work ACKNOWLEDGED and complied.** All semantic design activity on `step3-design-m-8` halted; no lock/PLAN/impl move was ever reached; every artifact byte preserved as-is (no rewrite under the new framing); no source/credential/provider-call/external-send/merge/deploy/live-store action taken. The reframe (conductor = relay plane for stamped participants, NOT app hub; connector + provider egress = app-side) is accepted as invalidating the dispatched framing; I do not resume on the prose. Bounded handoff follows, in the four requested parts.

### 1. Current artifacts + paths (preserved; provisional audit input to the reframe)

- **`master/domains/m-8-provider-adapters/audit/2026-07-14-provider-adapters-audit.md`** — rev3, SHA-256 `09a1fb094f6fe7618962b8965dbb64e5679c16aa648f10c75bd87c559450792c`. The **pair-reconciled AUDIT** (kickoff §6 step 1), complete BEFORE the design dispatch. Mostly topology-independent (the promote/reject/fresh matrix, the fixture inventory, the locked-contract constraint list). The parts the reframe touches are flagged in part 3.
- **Six-relay AUDIT trail** under `master/relays/step3-audit-m-8/` (rev1 → adversarial → rev2 → residual → rev3 → confirm) + the `SITREP-planner-20260714-231500` AUDIT-complete report. Unaffected as a record.
- **`scratchpad/m8-design-progress.md`** (session scratch, NOT a committed domain artifact, NOT under the design dispatch) — the in-conversation brainstorm state. Named here for completeness; it holds the provisional Q2/Q3 leans below. No DESIGN doc was written; the design dir has **no** `2026-07-15-*` file.

**No artifact was produced under `step3-design-m-8` itself:** no DESIGN doc, no DESIGN relay to m-8.implementer, no GRILL_LOCK, no PLAN. The design phase halted mid-brainstorm.

### 2. Current verdict / lock state

- **Lock state: NONE.** No design-lock, no PLAN, no DESIGN-REVIEW issued or received. Design-lock was never approached.
- **Brainstorm state (provisional, in-conversation only — not a filed verdict):**
  - **Q3 wire substrate — provisionally leaned:** frank-owned HTTP client (not vendor SDKs). Precedent grounded at the bytes (codex owns wire via reqwest+eventsource_stream, no SDK crate; opencode migrating off the AI-SDK to a native path). **Reframe interaction: likely SURVIVES and is reinforced** — an app-side connector owning its own wire is consistent with the correction; but its process home is now a reframe question (part 4).
  - **Q2 `serving_profile_id` — provisionally leaned:** opaque, m-8-assigned handle for a distinct serving path; facts-only spec-sheet grounded against models.dev + pi rows (cost/limits at the profile level — proven by Kimi K2 moonshot-ai vs moonshot-cn vs kimi-coding; reasoning-replay + tool-use facts at the wire-dialect level). **Reframe interaction: catalog SCHEMA is topology-independent and should carry forward; catalog HOME does not (see Q1).**
  - **Q1 catalog-home — PARKED by the operator** pending exactly this architecture ruling; never decided. Correct call in hindsight.
  - **Q4 event/attempt persistence — NOT STARTED.**

### 3. Unresolved findings (open before the hold; the reframe must re-scope, not inherit)

- **The provider-egress seam my audit already flagged as the sharp open edge is the one the reframe resolves directionally.** Audit §6 (m-7 §9) recorded the tension: frank's locked claim is "the local outbox is the only egress the governance system offers," yet a provider request is a new outbound path with no analog. The reframe's ruling — **provider request bytes/network send BYPASS the conductor** — resolves that in a specific direction: provider egress is NOT the conductor outbox chokepoint. This retires the audit's open "third egress path?" question and replaces it with the app-side-connector question.
- **The three mandatory owner amendments remain open and are now reframe-dependent, not just consumer-review-dependent:**
  - m-3 provider-request-egress class — its *host* moves (was "m-7-hosted at the conductor outbox"; now app-side last-pre-wire). m-3 keeps policy/evidence ownership per the VP fork; the enforcement *locus* is what changed.
  - m-7 credential/trusted-config amendment — the **r3 lane + its three paired reviews are provisional audit input only (no r4, no lock)**, per the hold. Credential *attach* now has to reach an app-side connector, not a conductor-internal secret load — a materially different secret-boundary shape for m-1 to govern.
  - m-4/m-2 exact-lane routing-record amendment — the **record-shape tension is topology-independent** (m-4 §12 "record does not change between steps" vs binding an exact lane) and survives the reframe unchanged; it can proceed on its own track.
- **The five-layer terminal agenda** (provider-wire / provider-egress / routing / turn / relay-delivery) is unchanged as a vocabulary-non-collapse requirement, but layer 2's *home* (provider-request egress disposition) moves app-side.
- **Catalog home (Q1) + event persistence (Q4)** are both unresolved and both were correctly identified in-brainstorm as architecture-sensitive; they are now explicitly blocked on the reframe.

### 4. Dependencies the reframe MUST disposition (for m-8 to re-cut)

The VP fork already names m-8 as the **likely connector actuator / last pre-wire enforcement host**. To resume, the ratified architecture-amendment packet needs to pin, specifically:

1. **Connector process boundary** — is the LLM connector/provider-adapter a **separate app-side process** (its own address space, attached to the conductor as a stamped participant over the same per-seat channel), or an in-app library the app-shell calls? This decides everything downstream: where adapters run, where the owned HTTP client lives, and what "seat surface" even means for the connector.
2. **Credential attach path** — how m-1-governed secrets reach the app-side connector without entering the conductor's trusted-config, the catalog, records, or any stamped surface. (Under the old framing this was an m-7 conductor-internal load; app-side changes the secret-delivery topology and the m-7 credential amendment with it.)
3. **Last-pre-wire enforcement locus** — if m-8 is the enforcement host, where exactly does the "final authorization, no post-authorization mutation" point sit relative to (a) the conductor's governance stamp and (b) the actual socket write? The Q3 owned-HTTP lean was chosen precisely to make this by-construction; the reframe must confirm the enforcement point is inside m-8's owned client and say what evidence of it flows back to m-3.
4. **Observe/evidence reachback** — how m-3 observation + qualification evidence is produced for a send that happens app-side, outside the conductor's serialized commit loop. (Audit §6: observations must come from the conductor's DI-5 vantage; an app-side send changes the vantage.)
5. **Catalog + event-record home** — where the facts-only lane catalog and any store-visible event/attempt records physically live and how they're integrity-referenced, given the store/config is conductor-side but the connector is app-side. (This is the Q1/Q4 blocker, now explicitly a topology question.)

**What carries forward regardless of the reframe** (offered to reduce rework): the AUDIT promote/reject/fresh matrix, the ~40-fixture conformance inventory, the grounded 4-axis catalog *schema*, the Q3 owned-HTTP-client posture, and the m-4 record-shape amendment track. What must NOT carry forward: any placement of egress/enforcement/credential-load/catalog *at the conductor*.

### Escalation scan

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — credential attach path moves app-side; a real secret-boundary re-cut for m-1/m-7; this handoff changes nothing, only names it
- migration/backfill/destructive-write/canonical-data-repair: no — report-only handoff, no data or code mutation
- money/inventory/orders/planning/accounting/trust-critical-state: yes — final-wire authorization locus is trust-critical and is exactly what the reframe relocates
- AI-or-automation-acts-downstream: yes — provider send + downstream tool calls are the automation boundary being re-homed
- worker/scheduler/queue/retry/async-side-effect: yes — app-side send changes where retry/timeout/backpressure live relative to the commit loop
- cross-repo/service-contract/generated-schema/shared-API-event: yes — m-8/m-9/m-3/m-4/m-7 seams + the connector process boundary are shared contracts under re-cut
- user-visible-control-with-materializer/downstream-consumer: no — no product mutation in this handoff
- test-runtime-role-mismatch: unknown — greenfield connector runtime does not exist yet
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the reframe is a topology re-cut; scope of the connector boundary is the open operator/architecture question
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no ceremony downgrade or risk acceptance requested by this report-only handoff
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

Operator-judgment items: the architecture re-cut itself (conductor/connector process boundary, credential attach, last-pre-wire enforcement locus) — master + operator's to ratify; not mine to design in this hold.

ACTIONS_GIT_REF: docs-only in non-git cwd — wrote this handoff relay + one INDEX.md row; preserved (did NOT edit) the rev3 audit + scratch progress note; frank/ untouched, fresh `git -C frank status --short` = empty (clean) at 502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8 HOLDS. No design, grill, review, amendment r4, lock, PLAN, code, credential, provider-call, or live-store action until the operator-ratified architecture-amendment packet + refreshed consumer audit + a replacement single-author design dispatch re-open this lane. On re-dispatch I will re-cut the connector/provider-adapter design against the app-shell topology, reusing the carry-forward set named in part 4.
