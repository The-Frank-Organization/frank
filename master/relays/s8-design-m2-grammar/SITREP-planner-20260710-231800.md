## SITREP — s8 grammar has hit its solo ceiling: rev4 echo swept, but the design is BLOCKED-by-dependency (m-3/m-5/m-6/m-7 confirms + surface_intent contract choice + operator ratification); requesting master orchestrate the multi-party close instead of a 6th solo rev

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-m2-grammar-coord
PARENT_DISPATCH_ID: s8-m2-grammar-design-review-r5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — proposes a coordination step + carries the pending operator activation-authorization ratification
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m2-grammar
IN_REPLY_TO: master/relays/s8-design-m2-grammar/DESIGN-REVIEW-implementer-20260710-231100.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-2.implementer, m-3.planner, m-5.planner, m-6.planner, m-7.planner
SUBJECT: the s8 m-2 grammar has iterated 5 rounds and the implementer's r5 Blocker-3 is explicit — it CANNOT approve until the sibling contracts return; I've swept the r5 echo (my solo part) and I'm routing the cross-domain close to you rather than churning another solo rev that hits the same wall

**Honest status.** `s8-design-m2-grammar` has taken **five must-revise rounds**. The m-2-solo parts are now correct (implementer-accepted directionally): the census, the locked-pipeline order, the reader-capability gate, the seat-predicate form-validation coverage, the producer/profile partition *direction*, and the independent-manifest fixture *shape*. But the last review's **Blocker 3 is a hard dependency: the design cannot approve until m-3/m-5/m-6/m-7 confirm their contracts** — and Blocker 2's `surface_intent` fix must be **chosen after** those confirms. **A 6th solo revision would hit the same wall.** So I did the one solo thing that was genuinely mine and I'm routing the rest to you.

**Done this turn (solo, my doc):** swept the r5 Blocker-1 echo — the stale global `observe_gate`-computes-everything claims that survived in the matrix (`:55`), pipeline step (`:62`), §4 seam (`:86`), and GRILL decision (`:112`) are now the **per-producer sequence** (m-2 `authority_class` pre-disposition · m-3 observe outputs step 4 · m-5/m-6 `surface_intent` non-gate-only · per-profile completeness), consistent with §3. Grep-verified clean.

**The cross-domain confirms this design needs (please route):**
1. **m-3** — confirm the `observe_gate` output manifest = `{achieved_evidence, evidence_integrity, executable_claim_results, egress_scan_result, degradation_notes, attestation_source, target_gap_result, record_integrity, deviated_observed, bucket_binding_observed}` (owns these, NOT `authority_class`/`surface_intent`).
2. **m-7** — confirm the pipeline placement: where `authority_class` is computed (pre-m-3-disposition) and where the **profile-aware completeness** stage runs (post-observe, pre-append), in the locked form/lineage/slot/observe order.
3. **m-5 + m-6** — confirm `surface_intent` semantics + **choose the executable profile contract** (below).

**The `surface_intent` decision to put to m-5/m-6/m-7 (Blocker 2 — I present options, I do NOT decide):** the live registry unconditionally gates `surface_intent` on `layer_present:observe` alone, but locked §17.6:346 says **non-gate-bearing records only** — a real contradiction. "Add a not-gate-bearing gate" is **not executable as-is** (there is no `gate_bearing` atom; `authority_class` is deliberately non-gate-referenceable; the gate profile derives from the authority record-kind set OR config-sourced `gate_category ∈ A`). Two lawful shapes:
- **Option A — a new grammar atom** (`gate_bearing`/profile atom) so a static registry predicate can express "not gate-bearing." Cost: a §5 grammar extension (a bigger change; new atom to lock).
- **Option B (my recommendation) — remove the global `required_when: layer_present:observe` from `surface_intent` and move its profile-applicability to the m-5/m-6 producer/profile MANIFEST consumed by the profile-aware completeness check.** No new atom; no duplication of the authority formula into a drifting static predicate; the completeness stage (already profile-aware, Blocker 2) is the single source of truth for whether `surface_intent` is required on a given record. This is a **review-driven registry amendment** (remove the mis-scoped predicate) + the manifest contract — owned by m-5/m-6 with m-7's completeness stage.

**Requested of you (master):** orchestrate the four confirms + the `surface_intent` A-vs-B choice; the operator's ratification of the activation-authorization (`operator-authored config_change`, still pending) can run in parallel. On the reconciled returns, I fold **one** reconciling rev (the confirmed partition + the chosen `surface_intent` contract + byte-consistency) and route it for the final DESIGN-REVIEW — the next rev being the *reconciling* one, not another solo guess.

**On the churn, plainly:** five rounds is too many, and the root was mine — I repeatedly asserted structural/cross-domain claims before verifying them (the CQ-5 pipeline, `config.Load`, the registry gating, my own §17.6). The design genuinely grew into a cross-domain activation-partition + a registry bug (`surface_intent`) that a solo grammar doc can't close — the value it produced is surfacing that bug rather than shipping it silently. Routing to you now is the corrective: coordinate the contracts, don't keep guessing them.

Blocked: yes — on the four sibling confirms + the `surface_intent` choice + operator ratification. Not blocked on any m-2-solo item.
Scope drift risk: none — the m-2 grammar face is fixed; the open items are explicitly cross-domain, routed not decided.

ACTIONS_GIT_REF: swept §3 matrix/pipeline/§4/GRILL to the per-producer sequence (grep-verified) in `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md`; wrote this coordination SITREP + appended `master/relays/INDEX.md`; no `frank/` edit, no PLAN, no IMPL, no silent registry/CQ-5 change, no proxied sibling/operator decision.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: master routes the m-3/m-5/m-6/m-7 confirms + the `surface_intent` A/B choice + the operator activation-authorization ratification; on the reconciled returns, m-2.planner folds one reconciling rev → final DESIGN-REVIEW → master reconcile with `s8-design-m7-config` → the s8 PLAN. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
