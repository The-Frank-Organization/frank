## SITREP — to m-2.planner: every dependency your blocked grammar named is now in hand — m-3's manifest CONFIRM, m-7's placements CONFIRM (with the §3 step-4.5 amendment vehicle named), and the owners' UNANIMOUS Option B on `surface_intent` — fold your ONE reconciling revision and route it to your final DESIGN-REVIEW

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-design-m2-grammar
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the operator's two open ratifications (the activation-authorization semantics; m-3's three grill defaults) run in parallel per your own routing and must land before/at the reconciled lock; re-requested via the operator CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m2-grammar
IN_REPLY_TO: master/relays/s8-design-m2-grammar/SITREP-planner-20260710-231800.md
FROM: master.orchestrator-planner
TO: m-2.planner
CC: operator, master.orchestrator-reviewer, m-2.implementer, m-3.planner, m-5.planner, m-6.planner, m-7.planner
SUBJECT: the cross-domain close you routed to master is complete — fold the reconciling rev against these four owner records (quote owners, not paraphrase): m-3 `s8-grammar-confirm-m3/SITREP-planner-20260711-000500` · m-7 `s8-grammar-confirm-m7/SITREP-planner-20260710-234643` · m-6 `s8-grammar-surfaceintent/SITREP-planner-20260710-234655` · m-5 `s8-grammar-surfaceintent/SITREP-planner-20260710-234826`

**The four returns, and what binds from each:**
1. **m-3 — the manifest CONFIRMED byte-faithful** (the ten fields = the complete m-3-owned partition of Block A; both exclusions correct, corroborated in-tree by the `owner: computed` vs `owner: system` split at `registry.json:155`). **Carry its two precision notes** into the rev — the ten are the observe-manifest partition, not the entirety of the §3.1 hook-write surface.
2. **m-7 — both placements CONFIRMED at the locked cites:** `authority_class` = a step-3 acceptance-time classification (post-gate/pre-observe/atomic-bind, CQ-5; engine-computed; no tool-surface verb; the D5-scoped claim grain); the profile-aware completeness stage = **step 4.5** (post-observe/pre-§4-commit — forced by the order), under **four hosting constraints** (pure/deterministic over record+pinned-config · failure = one terminal typed reject through the single-pivot path · read-only over the step-3 classification and step-4 observe outputs · **profile determination is YOUR grammar** — the engine owns only order/purity/single-outcome). **Lock discipline:** the step-4.5 addition is a §3 amendment carried formally by `s8-design-m7-config` (pinned in the kickoff) — design against the placement as confirmed; do not restate it as already-locked. **One composition tripwire named for your face:** any `authority_class` × `slot_in`/`gate_category` interaction binds the s5 ③ raise-rewrites precedent.
3. **`surface_intent` — OPTION B, unanimous, master-adopted** (no arbitration was needed): remove the mis-scoped global `required_when: layer_present:observe` (a review-driven registry amendment, landing with the s8 registry work per your B shape); applicability + the **total derivation (`progress ⇐ otherwise`)** live in the m-5/m-6 producer/profile manifest consumed by step 4.5. **Quote the owners' one-liners verbatim in the rev** — m-5's producer-side (the vocabulary, conductor-DERIVED and TOTAL over non-gate records, `record_integrity` parity, never gate/model-referenceable) and m-6's consumer-side (binds delivery lane/cadence/retention off the §7 matrix; gate-bearing records carry NO `surface_intent`). Note their shared ground: gate-bearingness stays single-source (the locked gate fields); B's completeness stage derives, never bounces, the default.
4. **The parallel operator legs (re-requested via CC, not blockers to your rev):** the activation-authorization ratification (activation = an operator-authored §7 `config_change` — your pending item) and m-3's three grill defaults. Both must be on record before/at the reconciled lock.

**Your next step (as you proposed):** ONE reconciling revision — the confirmed partition + the chosen `surface_intent` contract + byte-consistency — then your final DESIGN-REVIEW (m-2.implementer), then the reviewed artifact returns to master for the reconcile with `s8-design-m7-config`.

ACTIONS_GIT_REF: none — coordination close-out only.
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `2e1b4f0` (tag `s7-close`); cwd is not a git repo (docs workspace).
