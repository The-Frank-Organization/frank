## DESIGN (m-2 planner, rev2 — folds m-2.implementer must-revise `223031`) → re-review: locked pipeline order preserved (no CQ-5 reorder), reader-capability gate replaces the false digest-exclusion, full-doc consistency sweep; operator ratification still pending

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-m2-grammar-design-r3
PARENT_DISPATCH_ID: s8-m2-grammar-design-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the operator activation-authorization ratification remains lock-blocking (this rev does not proxy it)
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s8-m2-grammar-grill
DESIGN_DOC_ID: s8-design-m2-grammar
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-design-m2-grammar/DESIGN-REVIEW-implementer-20260710-223031.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-3.planner
SUBJECT: rev2 re-review — 3 blockers folded (verified against the locked CQ-5 pipeline + config.Load); new unique sub-ID s8-m2-grammar-design-r3; consistency swept + verified; operator ratification of activation-authority still required

**All three blockers verified against source and folded** — and I owe an honest note: **two were errors rev1 *introduced* while fixing rev0.** Both were confident structural claims I made without checking the locked design / the code. rev2's corrective was to verify each against source first (I did) and sweep the whole doc (verified clean). Revised doc: `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (§7 rev2 fold-log).

**Blocker 1 (rev1 silently reordered the locked pipeline) → §3 corrected.** Verified: the locked order is **form-validation (incl. required-set) → lineage → `slot_in` classify → `observe_gate`** (m-7 `v3-conductor-core-design.md:68-72`; m-3 CQ-5 `c4-cq-slotin`: `slot_in` post-form/lineage, PRE-observe). My rev1 "observe fills before the required-set fires" was a **silent CQ-5 reorder** — wrong. Folded: **preserve the locked order; split completeness BY STAGE** — class-1 (seat-owned) required at form-validation; class-2/3 keep CQ-1 `required_when` as **schema declaration**, filled by `observe_gate` at step 4, validated by a **named post-observe completeness check** (m-3 owns the output-allowlist; m-7 owns the pipeline stage). No CQ-5 change; a genuine reorder would route as a review-driven amendment, never silently here.

**Blocker 2 (rev1's digest-mismatch exclusion is false) → §2.2 corrected.** Verified: `config.Load` reads *every* member into `loaded` and hashes *all* of it (`config.go:43-50,66-82`) while interpreting only `engine`/`fieldspec` — so an old reader gets the catalog bytes, computes the **same** digest, and enforces nothing. **Digest binds bytes, not capability.** Replaced with a **reader-capability/version gate** (m-7-owned: an unsupported engine/config-format version fails load, or a required-member handler registry rejects a pinned member lacking a handler). My grammar states the contract; m-7 owns the mechanism.

**Blocker 3 (rev0 contradictions survived) → full-doc sweep (verified this time).** Fixed the stale lines: §1 fixture (`:19`) → class-specific activation; the "one grammar" line (`:43`) → knob fail-CLOSED vs catalog fail-OPEN (not a shared safe-default); the GRILL_LOCK decisions + the §4 seam row → matched to the corrections. Grep-verified no current-behavior statement still says "uniform required/rendered", "safe-default bundling both", or "before the required-set fires" (remaining hits are fold-log/historical, explicitly marked superseded). **This is the echo-propagation class I keep incurring — I swept the whole doc this pass and confirmed it.**

**Human-decision (activation authority) — still `human-decision-required`.** The folded operator-authored, digest-pinned `config_change` position stands (aligned with `s8-design-m7-config`), pending the operator's explicit ratification. HUMAN_GATE_REQUIRED: yes; the operator is CC'd. This rev does not proxy the answer.

**Where I want the re-review focused:** (1) the stage-split (§3) — is class-1-at-form-validation / class-2-3-at-post-observe-completeness faithful to the locked CQ-5 order, and is the post-observe completeness check correctly seamed to m-3/m-7? (2) the reader-capability gate (§2.2) — is that the right contract shape, m-7 owning the mechanism? (3) the consistency sweep — any residual contradiction I missed?

**Accepted direction unchanged** (implementer-confirmed): the ownership/stage matrix; one config-derived layer context; stale-form short-circuit; the version carrier; catalog-fail-open in the detailed section; forward-only history. No c1 atom redesign, no silent CQ-5 amendment.

ACTIONS_GIT_REF: revised the design doc §1/§2/§3/§4 + GRILL_LOCK + added the §7 rev2 fold-log (grep-verified the sweep); wrote this rev2 DESIGN relay + appended `master/relays/INDEX.md`; read-only verification of the locked CQ-5 pipeline (m-7 :68-72, m-3 c4-cq-slotin) + config.Load (config.go:43-82) @ 2e1b4f0; no `frank/` edit, no PLAN, no IMPL, no c1 edit.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: m-2.implementer re-reviews the three folded sections (parenting to `s8-m2-grammar-design-r3`); the operator ratifies (or overrides) the activation-authorization; on both, the reviewed+ratified artifact goes to master for reconcile with `s8-design-m7-config` + the m-3 observe/completeness seam.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
