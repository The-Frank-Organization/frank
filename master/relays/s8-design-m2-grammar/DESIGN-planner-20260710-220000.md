## DESIGN — s8 config/atom grammar (m-2-owned face) → m-2.implementer DESIGN-REVIEW: layer_present:observe activation semantics + member grammar + required-set consequences; grill folded; consumes s8-design-m7-config

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-m2-grammar-design
PARENT_DISPATCH_ID: s8-design-m2-grammar
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — pair DESIGN-REVIEW request; the one operator fork (activation-authorization) is surfaced durable in the GRILL_LOCK
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s8-m2-grammar-grill
DESIGN_DOC_ID: s8-design-m2-grammar
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-design-m2-grammar/DESIGN-orchestrator-planner-20260710-214603.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner
SUBJECT: DESIGN-REVIEW request — s8 config/atom grammar `s8-design-m2-grammar` (doc `design/2026-07-10-s8-config-atom-grammar.md`); grill run + GRILL_LOCK `s8-m2-grammar-grill` folded; unique pair sub-ID `s8-m2-grammar-design` (s7a convention); grill the invariant, the §9 member grammar, and the forward-only migration

**Request.** Grill the m-2-owned s8 grammar face: `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (DESIGN_DOC_ID `s8-design-m2-grammar`; GRILL_LOCK `s8-m2-grammar-grill` folded, §5). Sole-author m-2; consumes m-7's `s8-design-m7-config` at the §4 seams (not authored here). Baseline `frank@2e1b4f0`. No mechanism change to the atom/CQ-1; no c1 reopen.

**The four items (grounded in code + §5/§9):**
1. **Activation semantics (§1):** the atom `layer_present:observe` + CQ-1 are **unchanged**; only its **input moves** — `PresentLayers` goes from the hardcoded `DefaultLayers()` code-constant (predicate.go:17, `{store,form,lineage}`, observe absent) to **config-derived** (compose `observe` from the governed knob). Invariant: **"activation is a governed, visible act — never a silent code default,"** proven by the s5-dormancy→activation test mirror (same Block-A fields dormant knob-off / required knob-on).
2. **Member grammar (§2, §9-compatible):** the knob + the catalog are named config members under the composite digest. **Add-member = additive-MINOR + ignore-unknown safe-default** (old readers ⇒ observe-off / no-catalog-enforcement). The **knob value flip = a config-DIGEST act, NOT a record schema_version bump** (CQ-4b's distinct-axis point). **Catalog:** add-law = MINOR; **weaken/remove-law = owner-fidelity-gated governance-MAJOR** (its `change_convention`); the s8 `section_7` carry promotes it convention-only → digest-pinned.
3. **Required-set consequences (§3):** what flips when observe goes live = **Block A's CQ-1 `required_when` firing** (fields become required/rendered at the validator grain; `EVIDENCE_TARGET` unaffected — the CQ-1 guardrail). The **A-1 re-render is ONE governed boundary**, not F5 churn. **Forward-only: pre-activation records stay valid, never re-validated** (CQ-1 epoch + §9 immutability).
4. **Boundary contract (§4):** names `s8-design-m7-config` (the knob home, load-time flip, catalog digest-pinning, genesis composition — m-7's) at each seam my grammar consumes; both halves cited, neither locks in isolation.

**Where I want the hardest grill:**
1. **The invariant + fixture (§1):** is "config-derived `PresentLayers`, never `DefaultLayers()`" the complete statement, and does the s5-dormancy→activation mirror actually pin *both* states executably?
2. **The §9 axis split (§2.1):** is "knob-value-flip = config-digest act, not a record schema_version bump" faithful to CQ-4b, and is add-member-MINOR/ignore-unknown-safe-default correct against §9?
3. **Forward-only migration (§3):** is "pre-activation records never re-validated, immutability held" airtight against CQ-1 + §9 — any code path that would re-validate history at activation?
4. **The operator fork (GRILL_LOCK "Still operator-owned"):** the **activation-authorization policy** (who flips the knob live) — I recommend an operator-authorized governed config-change record, surfaced durable. Is that the right thing to leave to the operator, or is more inferable?

**Boundary (grill too):** m-7's digest home / load-flip / genesis / catalog-pinning are named seams, not claimed. On approve → the reviewed artifact to master → reconcile with `s8-design-m7-config` → the s8 PLAN consumes only the reconciled locks.

ACTIONS_GIT_REF: wrote the design doc `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (grill + GRILL_LOCK folded) + this DESIGN relay + appended `master/relays/INDEX.md`; read-only `frank/` probes @ 2e1b4f0; no `frank/` edit, no PLAN, no IMPL, no c1 edit.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: m-2.implementer returns `PHASE: DESIGN-REVIEW` (verdict) on `s8-design-m2-grammar`, parenting to this DESIGN's `DISPATCH_ID: s8-m2-grammar-design`; on approve, the reviewed artifact goes to master for reconcile with `s8-design-m7-config`.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
