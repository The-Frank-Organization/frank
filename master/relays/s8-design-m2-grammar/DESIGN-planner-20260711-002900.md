## DESIGN (m-2 planner, rev7 — MINIMAL; folds m-2.implementer must-revise `002300`) → re-review: the last lock-summary echo + the pre-commit fixture noun corrected; primary sequence + fixture already passed; operator legs still pending

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-m2-grammar-design-r8
PARENT_DISPATCH_ID: s8-m2-grammar-design-review-r7
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the two operator legs (activation-authorization ratification; m-3's three grill defaults) remain required at the reconciled lock; not proxied
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s8-m2-grammar-grill
DESIGN_DOC_ID: s8-design-m2-grammar
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-design-m2-grammar/DESIGN-REVIEW-implementer-20260711-002300.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-5.planner, m-6.planner, m-7.planner
SUBJECT: minimal final fix — the `Design-lock impact` bullet + the fixture pre-commit noun; the primary sequence, fixture, matrix, seam, §4, GRILL, §8 all passed; unique sub-ID s8-m2-grammar-design-r8

**Two minimal corrections, both as required (`002300`); the implementer confirmed everything else passes.** Doc: `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (§7 rev7 fold-log).

1. **`Design-lock impact` bullet (§7)** — was still the superseded pre-reconciliation contract ("observe-fill enrichment ordering + §4 seams **m-7/m-3** must bind"). Replaced with the **confirmed four-owner step-3/4/4.5 contract**: m-2 `authority_class` step-3 (+ s5 ③ tripwire); m-3's step-4 ten-field manifest + precision notes; m-5/m-6 Option B + **m-2's registry-row amendment removing `surface_intent`'s static predicates**; m-7's step-4.5 profile-aware completeness carried as a §3 amendment by `s8-design-m7-config`; + both operator legs on record at the lock.
2. **Fixture `Actual` (§3)** — "assembled **committed** record … at step 4.5" → "assembled **in-courier candidate** … at step 4.5" (step 4.5 is **pre-commit** per m-7's confirmed placement; the committed record is tested only by the behavioral oracle).

**Grep-verified whole-doc:** no residual "observe-fill enrichment ordering", "§4 seams m-7/m-3", or "assembled committed record" in current sections. No owner-confirmed decision changed — minimal terminology/lock-summary fix only.

**Operator legs — still explicitly pending** (activation-authorization ratification; m-3's three grill defaults). HUMAN_GATE_REQUIRED: yes; operator CC'd; not proxied.

ACTIONS_GIT_REF: corrected the `Design-lock impact` bullet + the fixture `Actual` noun + §7 rev7 fold-log; grep-verified whole-doc; wrote this rev7 DESIGN relay + appended `master/relays/INDEX.md`; no `frank/` edit, no PLAN, no IMPL, no c1 edit, no registry edit, no owner-decision change.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: m-2.implementer's re-review (parenting to `s8-m2-grammar-design-r8`); on approve + the two operator legs on record, the reviewed artifact returns to master for the reconcile with `s8-design-m7-config` → the s8 PLAN.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
