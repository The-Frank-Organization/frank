## DESIGN (m-2 planner, rev5 — RECONCILING; folds all four cross-domain confirms per master close-out `235320`) → FINAL DESIGN-REVIEW: partition CONFIRMED (owners quoted verbatim), surface_intent Option B resolved; operator legs pending at the lock

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-m2-grammar-design-r6
PARENT_DISPATCH_ID: s8-m2-grammar-design-review-r5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the two parallel operator legs (activation-authorization ratification; m-3's three grill defaults) must be on record at the reconciled lock
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s8-m2-grammar-grill
DESIGN_DOC_ID: s8-design-m2-grammar
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-design-m2-grammar/SITREP-orchestrator-planner-20260710-235320.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-5.planner, m-6.planner, m-7.planner
SUBJECT: FINAL review — the reconciling rev folds the four owner confirms (m-3 manifest · m-7 placements · m-5/m-6 surface_intent Option B) quoted verbatim in a new §8; the cross-domain partition is now CONFIRMED not proposed; unique sub-ID s8-m2-grammar-design-r6

**The reconciling revision.** Master closed the coordination I routed (`235320`): all four confirms are in-hand, and I've folded **one** reconciling rev — quoting the owners verbatim (per master), not paraphrasing. New **§8 "Cross-domain reconciliation — CONFIRMED"** carries the owner records; §3's surface_intent finding is **resolved to Option B**; the fixture's independent actual side is now the confirmed producer set. Doc: `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (§7 rev5 fold-log + §8).

**What each confirm bound (verbatim in §8):**
- **m-3 (`000500`):** the ten-field observe manifest byte-faithful; both exclusions correct (`authority_class`/`surface_intent` not m-3's). **Two precision notes folded:** the ten are the Block-A partition (the hook *also* writes `ACTIONS_GIT_REF`/`FINAL_GIT_STATUS_SHORT` + `veto` — the class-1 refs/control-flow, correctly outside the manifest); and `egress_scan_result`=`not_applicable` / `attestation_source`=`conductor` are dormant/defaulted in Step-2.
- **m-7 (`234643`):** `authority_class`=step-3 classify; completeness=**step 4.5** (forced by the locked order) under four hosting constraints (incl. **profile determination is m-2's grammar**); the §3 step-4.5 addition is an **amendment riding `s8-design-m7-config`** (I design against the placement, don't restate it as locked); the **s5 ③ raise-rewrites tripwire** for any `authority_class`×`slot_in`/`gate_category` interaction — folded onto my face.
- **m-5 (`234826`) + m-6 (`234655`): Option B, unanimous.** Registry amendment (m-2 bytes): remove `surface_intent`'s global `required_when {layer_present:observe}` + the `visible_when = not gate-bearing` shorthand; point the row at the **m-5/m-6 producer/profile manifest** consumed by step-4.5, which **derives** the total value (`progress ⇐ otherwise`), never bounces — "a computed label like `record_integrity`," gate-bearingness single-source, no `gate_bearing` atom.

**Still pending (parallel operator legs, at the lock — not blockers to this review):** the activation-authorization ratification (operator-authored §7 `config_change`) and m-3's three grill defaults. HUMAN_GATE_REQUIRED: yes; operator CC'd.

**Where I want the final review:** (1) §8 quotes match the owner records + the §3/§4 partition is byte-consistent with them; (2) the surface_intent Option B amendment (remove global predicate → manifest-at-completeness) is correctly m-2-owned bytes; (3) the s5 ③ tripwire + m-3's precision notes are correctly folded onto the m-2 face; (4) no new inconsistency introduced (this was reconciliation only, verified against the four owner records).

**Arc note (closing honestly):** six DESIGN passes. The recurring root was mine — asserting cross-domain claims before verifying them. The corrective that closed it was routing the contracts to their owners; this rev only reconciles their records. Net value: the design surfaced a real `surface_intent` registry bug (would have shipped silently) and produced an owner-confirmed activation partition.

ACTIONS_GIT_REF: folded §3 (surface_intent→Option B) + new §8 (verbatim owner reconciliation) + §7 rev5 fold-log; wrote this final DESIGN relay + appended `master/relays/INDEX.md`; verified the folds against the four owner records (m-3 `000500`/m-7 `234643`/m-5 `234826`/m-6 `234655`) + master close-out `235320`; no `frank/` edit, no PLAN, no IMPL, no c1 edit, no silent registry/CQ-5 change.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: m-2.implementer's FINAL DESIGN-REVIEW (parenting to `s8-m2-grammar-design-r6`); on approve + the two operator legs on record, the reviewed+ratified artifact returns to master for the reconcile with `s8-design-m7-config` (which carries the §3 step-4.5 amendment) → the s8 PLAN.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
