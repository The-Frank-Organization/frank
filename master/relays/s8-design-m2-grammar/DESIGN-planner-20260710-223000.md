## DESIGN (m-2 planner, rev1 — folds m-2.implementer must-revise `220736`) → re-review: ownership/stage matrix, single layer context + stale-form short-circuit, version carrier + fail-open catalog, operator activation-authority folded

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-m2-grammar-design-r2
PARENT_DISPATCH_ID: s8-m2-grammar-design-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the activation-authorization decision is folded (operator-authored config_change) but requires the operator's ratification before the lock feeds PLAN
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s8-m2-grammar-grill
DESIGN_DOC_ID: s8-design-m2-grammar
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-design-m2-grammar/DESIGN-REVIEW-implementer-20260710-220736.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-3.planner
SUBJECT: rev1 re-review — all four findings folded (verified against source); new unique sub-ID s8-m2-grammar-design-r2; the operator activation-authority ratification is the one remaining human gate

**All four findings verified against source and folded** — this was a strong review; two (the required/rendered conflation, the fail-open-labeled-safe) were substance errors I should have caught. Revised doc: `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (§7 rev1 fold-log). The accepted direction (atom/CQ-1 unchanged; knob=config-digest axis; catalog add/weaken; forward-only sound) is unchanged.

**Blocker 1 (required/rendered conflation) → §3 ownership/stage matrix.** Verified the Block-A owners: only `ACTIONS_GIT_REF`/`FINAL_GIT_STATUS_SHORT` are `agent_enum_pick`/`observed_value` (seat-rendered); the rest are `system`/`observed_value` (conductor-observed) or `system`|`computed`/`computed_result` (conductor-computed), all suppressed by `renderable` (`render.go:88-98`). Folded the **three-class matrix** — each fires at its grain; classes 2/3 satisfied by the **m-3 `observe_gate` enrichment stage BEFORE** the observe-owned required-set fires (new m-3 seam). Fixtures assert per-class, not "every row on a seat form."

**Blocker 2 (single context + stale-form) → §1 + §3.1.** Folded: **one immutable config-derived `PresentLayers`** threaded through all three `DefaultLayers()` sites (`render.go:54`/`:254-260`, `validate.go:29`); and the decisive rule — a **stale form digest short-circuits** the required-set (only the `re-render` disposition; fresh digest then meets the activated set), with the two-step no-retry fixture (`validate.go:22-24` continues today → the double-bounce closed).

**Blocker 3 (version carrier + fail-open) → §2.** Folded: the knob home **MUST carry a mechanically-readable version** (`EngineConfig` has none, `config.go:24-28`; a MINOR you can't read is not a MINOR); and the catalog's old-reader behavior labeled **fail-OPEN governance** (not a safe default) — old readers that cannot enforce **must not participate** in the governed runtime (pinned-digest mismatch is the stop) — split from the knob's fail-CLOSED observe-off.

**Human-decision (activation authority) → GRILL_LOCK folded, pending ratification.** Moved from an open hole to the **folded position** aligned with `s8-design-m7-config`: **operator-authored, digest-pinned `config_change`, restart-effective; no lane activation surface.** This is **lock-blocking** — HUMAN_GATE_REQUIRED: yes; the operator (CC'd) ratifies or overrides before the design lock feeds PLAN.

**Where I want the re-review focused:**
1. The ownership/stage matrix (§3) + the m-3 `observe_gate` ordering — is the enrichment-before-required-set boundary correctly placed, and is each class asserted at its real grain?
2. The stale-form short-circuit (§3.1) — is "only re-render, then fresh digest → activated set" the airtight rule, no residual double-bounce path?
3. The version-carrier constraint + fail-open catalog labeling (§2) — honest and mechanically grounded?
4. The activation-authority fold — correctly lock-blocking + correctly routed to operator ratification?

**New seams introduced by the fold:** m-3 (`observe_gate` fill/compute enrichment stage, §3/§4) now joins m-7 (config home/load/genesis) as a named boundary — both cited, neither authored here (m-3.planner CC'd).

ACTIONS_GIT_REF: revised the design doc §1/§2/§3/§4 + GRILL_LOCK + added §7 rev1 fold-log; wrote this rev1 DESIGN relay + appended `master/relays/INDEX.md`; read-only `frank/` verification of the four findings (render.go:88-98, validate.go:22-45, config.go:24-55, Block-A owners @ 2e1b4f0); no `frank/` edit, no PLAN, no IMPL, no c1 edit.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: m-2.implementer re-reviews the four folded sections (parenting to `s8-m2-grammar-design-r2`); the operator ratifies (or overrides) the activation-authorization; on both, the reviewed+ratified artifact goes to master for reconcile with `s8-design-m7-config` (+ the m-3 observe-fill seam).
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
