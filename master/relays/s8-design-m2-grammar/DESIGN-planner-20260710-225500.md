## DESIGN (m-2 planner, rev3 — folds m-2.implementer must-revise `224600`) → re-review: CQ-1 census corrected (registry-verified: -completed_proof, +bucket_binding_observed) + mechanical exact-set fixture; three rev2 structural folds accepted; operator ratification still pending

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-m2-grammar-design-r4
PARENT_DISPATCH_ID: s8-m2-grammar-design-review-r3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the operator activation-authorization ratification remains lock-blocking (not proxied)
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s8-m2-grammar-grill
DESIGN_DOC_ID: s8-design-m2-grammar
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-design-m2-grammar/DESIGN-REVIEW-implementer-20260710-224600.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-3.planner
SUBJECT: rev3 re-review — narrow CQ-1 census fix (registry-verified field-by-field) + registry-derived exact-set fixture; new unique sub-ID s8-m2-grammar-design-r4; the three rev2 structural blockers stay closed; operator ratification of activation-authority still required

**The narrow blocker is folded, verified field-by-field against the registry** — the same root cause as my earlier errors (I hand-listed the Block-A fields instead of querying the registry's actual `layer_present:observe` gating; this time I queried it). Revised doc: `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (§7 rev3 fold-log).

**Blocker (CQ-1 matrix census) → §3 corrected.** Verified at `2e1b4f0`:
- **`completed_proof` REMOVED** from class 2 — it is `system`/`observed_value` but carries **no** `required_when`/`visible_when`, so it is **not CQ-1-activated**; stated as outside the activation matrix.
- **`bucket_binding_observed` ADDED** to class 3 — `system`/`computed_result` with **both** predicates gated on `layer_present:observe`; a real activated computed output.
- **Fixture made mechanical:** the two-state fixture now **derives** the class-2/3 expected ID set **from the registry** (`layer_present:observe` gating ∧ `owner`∈{system,computed}/`fill`∈{observed_value,computed_result}) and asserts against it — so an inclusion (stray `completed_proof`) or omission (missing `bucket_binding_observed`) **fails mechanically**, not surviving a prose census.
- **Honest census note added:** the routing/ODB consumer rows (`routing_assignments`, `justified_deviation`, `constraints`, `plain_language_change`, `why_now`, `tradeoffs_risks`, `recommendation`, `choices`, `routing_record_kind`, `deviation_reason_code`) are **also** `layer_present:observe`-gated — but as the **s5-M-1 dormancy proxy**, seat-fillable, satisfied at form-validation, **outside** the `observe_gate` post-observe-completeness contract. Distinguished so the observe-evidence census isn't conflated with the dormancy-gated consumer set.

**Human-decision (activation authority) — still `human-decision-required`.** The folded operator-authored `config_change` position stands, pending the operator's explicit ratification. HUMAN_GATE_REQUIRED: yes; operator CC'd; not proxied.

**Accepted rev2 folds unchanged (implementer-confirmed):** the locked form/lineage/slot/observe order + stage-split + post-observe completeness seam; the reader-capability gate replacing the false digest-exclusion; the knob-fail-closed/catalog-fail-open consistency; the single layer context, stale-digest short-circuit, version carrier, forward-only history.

**Where I want the re-review focused:** the corrected class-2/3 census (registry-verified) + the mechanical exact-set fixture — is the derived-from-registry assertion the right shape, and is the observe-evidence vs dormancy-proxy split honest?

ACTIONS_GIT_REF: corrected §3 matrix (−completed_proof / +bucket_binding_observed) + the mechanical fixture + census note + §7 rev3 fold-log; grep-verified no other stale census reference; wrote this rev3 DESIGN relay + appended `master/relays/INDEX.md`; read-only registry query of the actual `layer_present:observe`-gated set @ 2e1b4f0; no `frank/` edit, no PLAN, no IMPL, no c1 edit.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: m-2.implementer re-reviews the corrected census + fixture (parenting to `s8-m2-grammar-design-r4`); the operator ratifies (or overrides) the activation-authorization; on both, the reviewed+ratified artifact goes to master for reconcile with `s8-design-m7-config` + the m-3 observe/completeness seam.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
