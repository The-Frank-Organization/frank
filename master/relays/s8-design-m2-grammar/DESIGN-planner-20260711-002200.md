## DESIGN (m-2 planner, rev6 — CONSISTENCY-ONLY; folds m-2.implementer must-revise `001600`) → re-review: primary sequence + fixture swept to the confirmed step-3/4/4.5 Option-B contract; no owner decision changed; operator legs still pending

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-m2-grammar-design-r7
PARENT_DISPATCH_ID: s8-m2-grammar-design-review-r6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the two operator legs (activation-authorization ratification; m-3's three grill defaults) remain required at the reconciled lock; not proxied
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s8-m2-grammar-grill
DESIGN_DOC_ID: s8-design-m2-grammar
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-design-m2-grammar/DESIGN-REVIEW-implementer-20260711-001600.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-5.planner, m-6.planner, m-7.planner
SUBJECT: consistency-only sweep — the reconciliation (§8) was accepted; I've rewritten the primary sequence + fixture that still encoded the superseded global-predicate model; grep-verified clean; unique sub-ID s8-m2-grammar-design-r7

**Both blockers were the same recurring failure — my echo-gap.** §8's reconciliation was accepted, but I'd left the **primary** stage rule + fixture + matrix + seam + §4 still encoding the superseded global-predicate model. Both correct; swept (consistency-only, no owner-confirmed decision changed). Doc: `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (§7 rev6 fold-log).

**Blocker 1 → the numbered stage rule (§3) rewritten byte-to-the-owner-records:**
1. form-validation — every seat-fillable activated predicate;
2. lineage;
3. **step 3** — `slot_in` classify + **`authority_class` compute** (m-7-confirmed; s5 ③ raise-rewrites tripwire);
4. **step 4** — m-3's confirmed **ten-field manifest** + the git-ref/`veto` control outputs (never `authority_class`/`surface_intent`);
5. **step 4.5** — producer/profile completeness: validate m-3's manifest + m-2's `authority_class`, **derive Option-B `surface_intent` for non-gate records only (`progress ⇐ otherwise`), absent on gate-bearing**, then validate all applicable outputs pre-commit.
Removed the false "observe hook produced the class-2/3 outputs" + "all system/computed retain CQ-1 `required_when`" (`surface_intent` carries **no** static predicate under Option B). Matrix (`surface_intent` "pending"→CONFIRMED), seam ("predicate fix"→**"registry-row amendment removing the static predicates"**), §4 (→confirmed, same wording), all four confirms marked complete.

**Blocker 2 → the fixture (§3) rewritten to three INDEPENDENT expectations:** the "registry = producer union" equality was impossible under Option B (the manifest derives `surface_intent` on every non-gate record; the registry carries no `surface_intent` predicate). Now: **registry expectation** (activated FieldSpec requirements, by seat/system ownership — no `surface_intent`) · **producer/profile expectation** (m-3 manifest + m-2 step-3 `authority_class` + m-5/m-6 Option-B derivation) · **actual** (record + producer-write provenance at step 4.5) — each asserted independently, then per-profile behavioral: gate-bearing has no `surface_intent`; non-gate gets exactly one, defaulting `progress`; m-3 can't write `authority_class`/`surface_intent`; no producer writes outside its manifest.

**Grep-verified clean:** no residual "pending"/"predicate fix"/"pre-disposition point"/impossible-equality phrase remains in current sections (fold-log historical entries excluded).

**Operator legs — still explicitly pending** (activation-authorization ratification; m-3's three grill defaults). HUMAN_GATE_REQUIRED: yes; operator CC'd; not proxied.

**Arc note:** this was the same echo-propagation class I've incurred repeatedly — corrected content in §8, superseded content left in the primary sections. The durable corrective I should have applied earlier and did here: **grep-verify the whole doc after any fold.** Consistency-only; owner-confirmed decisions unchanged.

ACTIONS_GIT_REF: swept §3 numbered rule + fixture + matrix + seam + §4 + §7 rev6 fold-log to the confirmed step-3/4/4.5 Option-B contract; grep-verified; wrote this rev6 DESIGN relay + appended `master/relays/INDEX.md`; no `frank/` edit, no PLAN, no IMPL, no c1 edit, no registry edit, no owner-decision change.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: m-2.implementer's re-review of the swept sequence + fixture (parenting to `s8-m2-grammar-design-r7`); on approve + the two operator legs on record, the reviewed artifact returns to master for the reconcile with `s8-design-m7-config` → the s8 PLAN.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
