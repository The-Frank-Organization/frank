## DESIGN (m-2 planner, rev4 — folds m-2.implementer must-revise `225600`) → re-review + m-3/m-5/m-6/m-7 confirms: activation-stage/producer partition corrected; surface_intent registry contradiction SURFACED (proposed amendment, not silently applied); operator ratification still pending

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-m2-grammar-design-r5
PARENT_DISPATCH_ID: s8-m2-grammar-design-review-r4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the operator activation-authorization ratification remains lock-blocking (not proxied)
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s8-m2-grammar-grill
DESIGN_DOC_ID: s8-design-m2-grammar
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s8-design-m2-grammar/DESIGN-REVIEW-implementer-20260710-225600.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-3.planner, m-5.planner, m-6.planner
SUBJECT: rev4 — 3 activation-partition blockers folded (verified vs my §17.6 + registry); the design now needs m-3/m-5/m-6/m-7 confirms + a review-driven surface_intent registry amendment; unique sub-ID s8-m2-grammar-design-r5; operator ratification still required

**All three blockers verified against my own §17.6 + the registry and folded** — the census correction (rev3) is accepted; these are the deeper activation-stage/producer partition. Revised doc: `master/domains/m-2-forms-determinism/design/2026-07-10-s8-config-atom-grammar.md` (§7 rev4 fold-log). **This fold now legitimately requires sibling confirms — I fold the m-2-clear parts and PROPOSE (not solo-decide) the cross-domain contracts, the corrective for my recurring unverified-cross-domain-claim error.**

**Blocker 1 (form-validation excluded activated seat requirements) → §3.** Verified: `justified_deviation`/`deviation_reason_code` have `required_when = layer_present:observe ∧ any(routing_assignments.declared_deviated == "yes")` (`2e1b4f0`). Folded: form-validation evaluates **every seat-fillable activated predicate** — the two evidence refs (required+rendered) + the ten s5-M-1 proxy rows (render-eligible; those two **conditionally required**). Replaced the false "CQ-1 touches only observe-owned rows" echo with the exact CQ-1-proper vs dormancy-proxy distinction.

**Blocker 2 (computed class ≠ one observe_gate set; surface_intent contradiction) → §3 producer/profile partition.** Verified against **my §17.6**: `authority_class` is **m-2-computed** (`:345`, m-3 consumes it) — not an observe_gate output; `surface_intent` is **m-5/m-6-owned, non-gate-bearing-only** (`:346`). Folded the per-producer partition. **SURFACED (not silently resolved):** the registry gates `surface_intent` on `layer_present:observe` **alone** — missing the locked non-gate-bearing gate — so a global completeness rule would wrongly require it on gate-bearing records. **I propose a review-driven registry amendment** (add the not-gate-bearing gate to match §17.6) **routed for m-6 + m-5 confirm (+ m-7 pipeline)** — not applied here.

**Blocker 3 (circular fixture) → §3.** Folded: the exact-set fixture now compares the **registry-derived EXPECTED** set against **INDEPENDENT producer manifests** (m-3 allowlist / m-2 `authority_class` / m-5-m-6 `surface_intent` profile) + a **behavioral-record oracle**, per profile — asserting activated requirements = union of authorized producer outputs, no missing/extra/wrong-owner write.

**Human-decision (activation authority) — still `human-decision-required`** (operator-authored `config_change`; pending ratification; not proxied).

**Cross-domain confirms this fold needs (routed via CC + master):**
- **m-3:** the observe_gate producer allowlist (which computed rows it owns) matches the §3 partition.
- **m-5 + m-6:** the `surface_intent` non-gate-only profile + the proposed registry-predicate amendment.
- **m-7:** the profile-aware post-observe completeness stage in the commit pipeline.

**Accepted (rev2/rev3) unchanged:** locked pipeline order + stage-split; reader-capability gate; consistency sweep; knob-fail-closed/catalog-fail-open; the evidence census (−completed_proof/+bucket_binding_observed); layer-context, stale-digest, version-carrier, forward-only.

**Honest note (the arc):** four revisions, each finding real issues — the recurring root is my asserting structural/cross-domain claims before checking the source of truth (the locked CQ-5 pipeline, `config.Load`, the registry gating, now my own §17.6). rev4's discipline: verify each against §17.6 + the registry (done), and **route the cross-domain contracts for confirm instead of solo-guessing them**. The design has genuinely grown into a cross-domain activation-partition + a registry amendment — I'm surfacing that rather than papering it into a solo grammar tweak.

ACTIONS_GIT_REF: revised §3 (stage rule + producer/profile partition + surface_intent finding + fixture) + §7 rev4 fold-log; wrote this rev4 DESIGN relay + appended `master/relays/INDEX.md`; read-only verification vs §17.6 (`:345`/`:346`) + registry predicates (`surface_intent`/`authority_class`/`justified_deviation`/`deviation_reason_code`) @ 2e1b4f0; no `frank/` edit, no PLAN, no IMPL, no c1 edit, no silent registry/CQ-5 change.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 2e1b4f0).
Next requested action: m-2.implementer re-reviews the partition + fixture (parenting to `s8-m2-grammar-design-r5`); m-3/m-5/m-6/m-7 confirm their halves (esp. the surface_intent registry amendment); the operator ratifies the activation-authorization; master reconciles the confirmed partition + `s8-design-m7-config` before the s8 PLAN.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
