## DESIGN-REVIEW request (fold re-review) — m-3.planner → m-3.implementer: c2-fold-m-3 F1+F3 folded

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c2-fold-m-3
PARENT_DISPATCH_ID: c2-fold-m-3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded additive fold; no new operator gate
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, m-5.planner, operator
IN_REPLY_TO: c2-fold-m-3/DESIGN-orchestrator-planner-20260630-035412.md
SUBJECT: Re-review request — bounded additive fold of m-5 findings F1 (slot_in conductor-owned/at-acceptance/immutable/non-lane-writable) + F3 (slot_in = work-archetype axis only); return approve | must-revise | reject-narrow

m-3.implementer — bounded additive fold per `c2-fold-m-3`. Your r1 approve (`DESIGN-REVIEW-implementer-20260629-211003.md`) stands on everything else; this re-review is scoped to the fold delta only. Respond read-only `PHASE: DESIGN-REVIEW`, parent to this relay, carry `DESIGN_DOC_ID: c2-design-m-3-observation-evidence`, return `DESIGN_REVIEW_VERDICT`. Re-approval is the c2-lock prerequisite per the dispatch.

**Delta (additive only):**
- **§5.1 new** — **F1:** `slot_in` (work-archetype) is **conductor-filled** (the already-reserved m-2 opaque atom; filler = existing `system` category), **classified at work-record acceptance**, immutable thereafter, **never lane-writable** — the tamper-resistance defense (a lane can't re-tag `refactor`→`extension` to escape no-test-edits, or `bugfix`→other to escape red→green). VP-pinned wording used: *at work-record acceptance, NOT spawn-time binding* (spawn-derivation would wrongly forbid a long-lived seat doing bugfix→refactor→migration). **F3:** the hook keys on `slot_in` = work-archetype axis **only**; `seat_archetype` (seat-archetype, per-seat-at-spawn) is a separate axis owned outside m-3 (m-4's routing vector); m-3 does not absorb seat/ceiling/authority semantics.
- **§13** — added the slot_in classification ordering (submit pre-flight vs `submitted→accepted`) as a PLAN carry-forward.
- **§15 fold-log** — the c2-fold-m-3 provenance.

**Bounded-shape compliance (please confirm I stayed within the VP guardrail):** `slot_in` kept **opaque** — no concrete Step-1 values, no `required_when`/`visible_when` on slot values, no m-2 ownership-category or bounded-predicate-vocabulary change; concrete tag-space + invariant selection remain m-5-owned (c3). Filler-as-`system` selects an existing m-2 category (pre-authorized by the dispatch), not an m-2 redesign.

**Please adversarially test:** (1) does §5.1 stay strictly additive + opaque (no silent stronger move that should have been an m-2 micro-fold flag)? (2) is the F1 tamper-resistance argument airtight — does conductor-classified-at-acceptance + immutable + non-lane-writable actually close the re-tag-to-escape-invariant hole? (3) is the F3 axis line clean (no seat/ceiling semantics leaking into `slot_in`)? (4) does the "at acceptance, not spawn" framing correctly preserve a long-lived seat changing work-archetype over its life?

Phase band DESIGN only — no PLAN/IMPL. On your approve I report fold-complete; I have already reported fold-complete to the orchestrator in parallel and will relay your verdict onward.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd (/Users/jack/Programming/harness) is a non-git docs workspace. Artifacts this turn: design-doc fold edits (§5.1, §13, §15, header) + this relay + the parallel fold-complete SITREP.

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
