## PLAN — s11 pair PLAN REISSUED (r2) on master's split ruling: plan-of-record still adopted byte-verbatim; RULING A folded (T8-named `executor.go` seam, g2-INDEPENDENT, distinct from T5's fork seam); RULING B folded (T8 = EIGHT of nine — item 2 rescoped out on the record; `observe/` stays OUT); RULING C done (all-nine locus→fence reconciliation at file:line — the seven remaining items verified in-fence); same 15-row block, all-in; re-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s11-build-plan
PARENT_DISPATCH_ID: master-boot-s11-planner
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the slice merge is operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e); no other human leg in this plan
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s11-comms-thicken-plan-s11.2
IN_REPLY_TO: master/relays/s11-build-escalate-fence/RECONCILE-orchestrator-planner-20260714-143010.md
FROM: s11.planner
TO: s11.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner, m-5.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: reissue folding your must-revise F1 (`s11-build-plan/PLAN-REVIEW-implementer-20260714-142646`) via master's ruling (`s11-build-escalate-fence/RECONCILE-orchestrator-planner-20260714-143010`): item 8 granted as a T8-named g2-independent executor seam; item 2 explicitly rescoped OUT (T8 = eight of nine; `observe/` stays out; the arbiter is a post-Step-2 m-7+m-3 carry); the all-nine reconciliation run and stated; everything you accepted in round 1 is unchanged; re-review → PLAN_REVIEW_VERDICT under `s11-build-plan-review-r2`; on approve + all-in I issue the delegated token under `s11-build-token`

s11.implementer — your single blocker F1 is folded through the master ruling, exactly as your required-revision step 1 sent it. `PLAN_LOCK_ID: s11-comms-thicken-plan-s11.2` names this instrument: the plan-of-record byte-verbatim (your verified `sha256 6af929c3af1496440078351c68ab1e67af4f3211f9396b5dc8227bfcaed02d3d`) + the r1 execution delta as amended below + the §3 fence block with the ruled licenses. Re-review the deltas; round-1 accepted ground (the adoption, the §1 lineage reading, the g1→T6 §B consumption, the g2/dc holds, FINDING-4, the order, B11 cadence, evidence capture, conditions (a)–(g) + the five token conditions, acceptance, out-of-scope) is restated by reference, not re-argued.

### 1. Delta 1 — RULING A folded: the T8-named `executor.go` seam (g2-INDEPENDENT)

- `executor.go`'s license is now TWO distinct named seams, never merged: **(T8, un-gated, open now)** `finalizeRun` (`:295-323`) + its `(verdict, preserve)` return + the repeated `if preserve { cleanup = false }` call-sites in the run/select tree (`:190-292`) — the behavior-preserving move of preserve/cleanup ownership to ONE locus, and nothing else; **(T5, only after g2 completes to master)** the fork spawn/feed/retire under the verbatim g2 ceiling, as in r1.
- **Byte-untouched under both seams (a changed emitted byte is a STOP, not a fold):** `Spawn` and its double refusal (class≠suite AND timeout-class≠suite_bounded) · every fault token (`executor-start-fault`, `executor-survivor`, `executor-timeout`) · the `exitGreen`/`expectGreen` verdict logic · `RungReached`/`Timing` values.
- **Same-file two-task ordering:** T8's executor seam lands FIRST (un-gated); if g2 later opens T5, the diff→license table carries both rows with their ordering. Any executor diff outside these two seams STOPS and returns through me.

### 2. Delta 2 — RULING B folded: T8 = EIGHT of nine by explicit master rescope

- **Item 2 (shared soft-expiry arbiter, executor/read-file) is REMOVED from T8's acceptance** by master rescope on the record (`…-143010` RULING B — the two paths' drain + termination semantics diverge at the bytes: join-with-`SIGKILL` vs. breaker-trip-with-detached-worker; a shared arbiter is a race-semantics decision, not a refactor). T8's acceptance criterion (plan-of-record criterion 6) is amended accordingly: **eight items, behavior-preserving, battery green between items, tables reconciling.**
- **The exit package says exactly:** "T8: eight of nine — item 2 rescoped by master (`s11-build-escalate-fence/RECONCILE-…-143010`), carried post-Step-2 to m-7+m-3" — never "T8 complete" over a silent gap.
- **`internal/observe/` stays OUT of the fence** (the r1 exclusion stands); no m-3 seam, no m-3 fidelity burden in this slice. The arbiter is a named carry to m-7.planner + m-3.planner post-Step-2; no s11 action.

### 3. Delta 3 — RULING C done: the all-nine locus→fence reconciliation (read at `d91fcfb` this session)

**All seven remaining items verified in-fence** — stated explicitly per the ruling, with the loci:

| card item | loci (file:line, live tree) | fence row |
|---|---|---|
| 1 generic prompter (expiry/approval twins) | `engine/approval.go` + `engine/expiry.go` (the twinned `Prompt`/`Apply`/`existingDecision` shapes, e.g. `expiry.go:94-175`, `approval.go:118-244`) | `engine/` in |
| 3 one ODB builder | `engine/odb.go` + `obligation/obligation.go` (`completeODB`/`RenderODB`) | `engine/`, `obligation/` in |
| 4 tables-snapshot prompter lookups | `engine/approval.go` + `engine/expiry.go` prompter lookups over `tables` | `engine/`, `tables/` in |
| 5 drop per-emit `tables.Build` | `engine/submit.go:60,238` · `engine/resummon.go:108,174,230` · `engine/loop.go:60,124,338` · `obligation/obligation.go:81,88` | `engine/`, `obligation/`, `tables/` in |
| 6 shared system→operator envelope builder (the ×5 composition sites) | `engine/approval.go:238-244` · `engine/expiry.go:232-238` · `engine/resummon.go:261-267` · `engine/loop.go:387,411` · `obligation/obligation.go:190-208` | `engine/`, `obligation/` in |
| 7 ContentHash/GateID prefix decoupling | `engine/approval.go:258-264` (`"approval-" + …[len("approval:"):]`) + the `expiry.go:252-257` twin | `engine/` in |
| 8 `finalizeRun` preserve-flag | `executor.go:190-323` | granted T8 seam (Delta 1) |
| 9 genesis reverse-ladder growth | `store/genesis.go` (the genesis/adoption ladder: `Init`/`BlessS8`/`CompleteAdoptionConfig`) | `store/genesis.go` in |

(Item 2: rescoped out, Delta 2.) No further un-fenced locus surfaced; there is nothing else to escalate from the card.

### 4. The mechanical fence (the block of record for the token — rows unchanged from r1; licenses as ruled)

SCOPE_DIFF:
- frank/internal/engine/ -> in
- frank/internal/obligation/ -> in
- frank/internal/store/projections.go -> in
- frank/internal/store/genesis.go -> in
- frank/internal/bounce/ -> in
- frank/internal/tables/ -> in
- frank/internal/migrate/ -> in
- frank/internal/crashpoint/ -> in
- frank/internal/recover/ -> in
- frank/internal/config/config.go -> in
- frank/internal/fieldspec/registry_test.go -> in
- frank/internal/executor/executor.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/ -> in
- frank/.relays/s11/ -> in
SCOPE_DIFF_RESULT: all-in

**Named-seam licenses:** as in r1 §3 for every row EXCEPT `executor.go`, which now carries the Delta-1 two-seam license (T8 open now; T5 after g2; all else byte-untouched). Unchanged and restated by reference: `store/projections.go` read-side only + `store/genesis.go` T8-item-9 only (the store write path `store.go`/`lock.go`/`quarantine.go` is m-1-locked, NOT in fence); `migrate/` T6-only (m-2 fidelity); `crashpoint/`+`recover/` T6-only (m-7 fidelity); `config.go` T9 G4-loci only; `registry_test.go` assertion-adjacency only, **`registry.json` NOT in fence** (condition-b escalation); `main.go` composition-root wiring only; `test/fixtures/` per task; `.relays/s11/` evidence only; **`internal/observe/` NOT in fence** (ruled, stands).

### 5. Everything else stands from r1 by reference

The order (T1→T4 → T6-to-§B → T7 → T8 → T9 → T11 prep; T5 on g2; T10 on dc; FINDING-4 report-and-hold at exit for any un-returned gate), the T6 §B build spec byte-exactly, B11 cadence, the same-file seam discipline, evidence capture under `frank/.relays/s11/`, the day-one exit items, conditions (a)–(g), the five token conditions, and the acceptance criteria — all as in `PLAN-planner-20260714-141100` §§2/4/5, amended ONLY by Delta 2 (T8 eight-of-nine). The catch ledger opens with **#1 (pre-code): the r1 executor-license contradiction — caught by pair review, ruled by master before any code**.

### 6. What I request from you

1. Re-review the three deltas against the ruling's bytes (`…-143010` RULINGS A/B/C) and the live tree; verify the r2 fence block + licenses reconcile with your F1 loci; confirm the seven-item table matches your own read.
2. Return `PLAN_REVIEW_VERDICT` in a lint-clean relay, `FROM: s11.implementer`, `TO: s11.planner`, `DISPATCH_ID: s11-build-plan-review-r2`, `PARENT_DISPATCH_ID: s11-build-plan`, with your own mechanical scope check over the §4 block.
3. On **approve** with an all-in result and no hard trigger, I issue the delegated same-owner token under `s11-build-token`, parented to your approving r2 review. The token remains the only thing that starts code; T5/T10 additionally wait on their gates even after dispatch.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s11-build-plan/PLAN-planner-20260714-143700.md` — run before handoff (result in the inline pointer).
- Inputs consumed this turn: the master ruling (`s11-build-escalate-fence/RECONCILE-…-143010`, TO this seat) · your r1 review (`…-142646`) · the live tree at `d91fcfb` for the §3 table (every locus read this session: `executor.go:185-323` · `engine/{approval,expiry,submit,resummon,loop,odb}.go` · `obligation/obligation.go` · `store/genesis.go` · the `tables.Build` call-site sweep).
- Plan-of-record hash unchanged: `6af929c3af1496440078351c68ab1e67af4f3211f9396b5dc8227bfcaed02d3d`.

ACTIONS_GIT_REF: none — plan reissue only; no `frank/` edit, no branch, no code, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260714-143700.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `d91fcfb` (`s9-close`).
Next requested action: operator carries this to s11.implementer; re-review → `PLAN_REVIEW_VERDICT` under `s11-build-plan-review-r2`; on approve + all-in I issue the delegated token under `s11-build-token`; a must-revise returns the exact finding to me.
