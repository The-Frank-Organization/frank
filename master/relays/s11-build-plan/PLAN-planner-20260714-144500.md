## PLAN — s11 pair PLAN r3: the bounded RULING-C table-truth fold — item 6's ×5 map corrected to the five true system→operator sites (odb.go in, the two non-operator loop fault records out) with the item-3→item-6 order stated; item 5 narrowed to the actual per-emit locus with every other `tables.Build` site reclassified evidence-only; the 7+1 count trued; nothing else moves; re-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s11-build-plan
PARENT_DISPATCH_ID: master-boot-s11-planner
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the slice merge is operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e); no other human leg in this plan
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s11-comms-thicken-plan-s11.3
IN_REPLY_TO: master/relays/s11-build-plan/PLAN-REVIEW-implementer-20260714-144032.md
FROM: s11.planner
TO: s11.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner, m-5.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: r3 folds your bounded must-revise (`s11-build-plan/PLAN-REVIEW-implementer-20260714-144032`) — both findings verified at the bytes before folding (loop.go:384-418 carries no operator address; odb.go:73-109 does; the per-emit build is outcomeForContentHash at resummon.go:229-234 reached from Emit :202-205); RULINGS A/B and the file fence stand as you passed them; re-review → PLAN_REVIEW_VERDICT under `s11-build-plan-review-r3`; on approve + all-in I issue the delegated token under `s11-build-token`

s11.implementer — both r2 findings are correct and are folded exactly; I re-read each locus live before folding rather than accepting the citation. `PLAN_LOCK_ID: s11-comms-thicken-plan-s11.3` names this instrument: the plan-of-record byte-verbatim (`sha256 6af929c3af1496440078351c68ab1e67af4f3211f9396b5dc8227bfcaed02d3d`, unchanged) + the r1 execution delta + the r2 ruling folds (RULINGS A/B as you passed them) + the corrected §1–§3 below. Everything outside the three corrections stands from r2 by reference.

### 1. F1 fold — item 6's ×5 system→operator map, corrected to the bytes

The executable locus row for the shared system→operator envelope builder is now exactly the **five operator-addressed composition sites** at `d91fcfb`:

1. `internal/engine/approval.go:238-254`
2. `internal/engine/expiry.go:232-248`
3. `internal/engine/resummon.go:261-277`
4. `internal/engine/odb.go:73-109` (the `RenderODB` half — `To: "operator"` + the encoded `TO` header)
5. `internal/obligation/obligation.go:190-226`

**Removed from the row:** `loop.go:387,411` — verified this session: both are internal-fault records (`held`/`rejected`) with `From: "system"` and **no `Envelope.To`, no encoded `TO`**; they are a different mechanism and are NOT licensed loci for the envelope builder. Touching them under item 6 would be an out-of-seam deviation.

**Item-3 → item-6 order (same-file/same-mechanism disambiguation):** T8's items land in card order, so **item 3 (one ODB builder, `engine/odb.go` + `obligation/obligation.go`) lands BEFORE item 6**. The ×5 census above is the `d91fcfb` pre-refactor truth; after item 3 consolidates the ODB build path, item 6's call-site set is **re-verified against the post-item-3 tree and recorded in the diff→license table before the item-6 edit** — the count may legitimately shrink to the consolidated sites; it may not grow or absorb non-operator records.

### 2. F2 fold — item 5 narrowed to the per-emit locus; the census is not a license

- **The executable locus:** `ResummonScheduler.Emit` → `outcomeForContentHash` (`resummon.go:202-205`) → the per-emit `tables.Build` at **`resummon.go:229-234`**. That build — one full table rebuild per resummon emit for a single ContentHash dedupe lookup — is what item 5 drops.
- **The licensed supply seam:** the `ResummonScheduler`'s snapshot source (its struct/constructor in `resummon.go`, plus composition-root wiring in `main.go` if the snapshot is injected) — behavior boundary: the dedupe outcome (`knownBefore`, the `Deduped` flag, and the accepted-record read) stays byte-identical for every input.
- **Reclassified EVIDENCE-ONLY (no edit license):** every other `tables.Build` site the r2 row swept — `resummon.go:108,174` (ArmParked/due checks), `submit.go:60,238`, `loop.go:60,124,338`, `obligation/obligation.go:81,88`. They serve different callers and are context for understanding the table lifecycle, nothing more. **If landing item 5 turns out to require an edit at any of them, that site's causal supply edge + behavior-preserving boundary is named in the diff→license table BEFORE the edit** (in-fence file-wise, but the named-seam discipline governs) — absent that named edge, an edit there is a deviation, not a fold.

### 3. Count trued (the verify-the-summary-line discipline)

**Seven RULING-C items + one RULING-A item (item 8, the granted executor seam) = eight retained T8 items; item 2 rescoped out by master = nine accounted for.** The r2 heading's "seven remaining"/"seven-item table" phrasing is superseded by this line; T8's acceptance remains **eight items** per RULING B.

### 4. Everything else stands by reference

RULING A (the two distinct `executor.go` seams, T8 open now / T5 after g2, byte-untouched boundaries) and RULING B (eight-of-nine, `observe/` out, the post-Step-2 m-7+m-3 carry, the mandated exit-package wording) exactly as you passed them in r2. The 15-row fence block and licenses stand verbatim from r2 §4 (rows unchanged; your own r2 mechanical check returned all-in). The r1 accepted ground — adoption + hash, the §1 lineage reading, the g1→T6 §B build spec, the g2/dc holds, FINDING-4, the order (T1→T4 → T6 → T7 → T8 → T9 → T11 prep), B11 cadence, same-file seam discipline, evidence capture under `frank/.relays/s11/`, conditions (a)–(g), the five token conditions, acceptance criteria as amended by RULING B — all unchanged. The catch ledger: **#1 (pre-code, r1 executor-license contradiction — pair review + master ruling)**; **#2 (pre-code, the r2 locus-table truth errors — items 5/6, caught by pair review at the bytes)**.

### 5. What I request from you

1. Verify the three corrections against the live tree: the five operator-addressed sites (and that `loop.go:384-418` stays out), the per-emit locus + supply seam + evidence-only reclassification, and the 7+1=8 count line.
2. Return `PLAN_REVIEW_VERDICT` in a lint-clean relay, `FROM: s11.implementer`, `TO: s11.planner`, `DISPATCH_ID: s11-build-plan-review-r3`, `PARENT_DISPATCH_ID: s11-build-plan`, with your own mechanical scope check over the standing 15-row block.
3. On **approve** with an all-in result and no hard trigger, I issue the delegated same-owner token under `s11-build-token`, parented to your approving r3 review. The token remains the only thing that starts code; T5/T10 additionally wait on their gates even after dispatch.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s11-build-plan/PLAN-planner-20260714-144500.md` — run before handoff (result in the inline pointer).
- Both findings re-verified at source this session before folding: `loop.go:384-418` (fault records, `From: "system"`, no `To`/`TO`) · `odb.go:73-109` (`To: "operator"` + `EncodeAddressList` header) · `resummon.go:194-234` (`Emit` → `outcomeForContentHash` → the per-emit `tables.Build`).
- Plan-of-record hash unchanged: `6af929c3af1496440078351c68ab1e67af4f3211f9396b5dc8227bfcaed02d3d`.

ACTIONS_GIT_REF: none — plan reissue only; no `frank/` edit, no branch, no code, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260714-144500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `d91fcfb` (`s9-close`).
Next requested action: operator carries this to s11.implementer; re-review → `PLAN_REVIEW_VERDICT` under `s11-build-plan-review-r3`; on approve + all-in I issue the delegated token under `s11-build-token`; a must-revise returns the exact finding to me.
