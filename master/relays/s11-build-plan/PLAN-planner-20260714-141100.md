## PLAN — s11 pair PLAN: m-6's approved plan-of-record ADOPTED byte-verbatim (sha-pinned); execution map T1→T11 under B11 straight-through; g1 CLOSED → T6 LOCKED to the integrated §B contract; g2/dc still OPEN → T5/T10 held (FINDING-4 binding); the 15-row all-in mechanical fence at named-seam grain; adversarial plan-review requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s11-build-plan
PARENT_DISPATCH_ID: master-boot-s11-planner
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the slice merge is operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e); no other human leg in this plan
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s11-comms-thicken-plan-s11.1
IN_REPLY_TO: master/relays/boot/master-boot-s11-planner/SITREP-orchestrator-planner-20260714-024919.md
FROM: s11.planner
TO: s11.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-6.implementer, m-2.planner, m-3.planner, m-7.planner, m-5.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: adopt-not-re-litigate — m-6's pair-approved plan-of-record consumed as the spec-of-record with an execution delta only (order, gate state incl. the g1 CLOSE, seams, evidence capture, the fence block of record); review it adversarially and return PLAN_REVIEW_VERDICT under `s11-build-plan-review-r1`; on approve + all-in + no hard trigger I issue the delegated token under `s11-build-token`

s11.implementer — this is the directly addressed PLAN your boot holds for. Per our boots and the s11 dispatch's ratified staffing (`s11-dispatch/PLAN-orchestrator-planner-20260714-023000`, the s9 Option-A model), we are the fresh slice pair; m-6 guides and files owner-fidelity from outside; the mechanics below are pair-approved and are NOT re-litigated here. `PLAN_LOCK_ID: s11-comms-thicken-plan-s11.1` names this instrument: the plan-of-record byte-verbatim + this execution delta + the fence block of record in §3.

### 1. Adoption + lineage

- **Plan-of-record, adopted byte-verbatim:** `master/domains/m-6-human-surface-scheduler/plan/2026-07-14-s11-comms-thicken-plan.md` (11 tasks, T1–T11, base `s9-close@d91fcfb`). Pinned: `sha256 6af929c3af1496440078351c68ab1e67af4f3211f9396b5dc8227bfcaed02d3d`. Verify the hash yourself before your verdict; a mismatch is a must-revise, not a judgment call.
- **Approval lineage (lives in the m-6 chain, consumed by reference):** PLAN-REVIEW `approve` (`s11-plan-m6/PLAN-REVIEW-implementer-20260714-024712`, `DISPATCH_ID: s11-plan-m6-review-r1`, no blockers). This relay carries `DESIGN_DOC_ID` as context only and no `DESIGN_LOCK_ID`/`DESIGN_RECORD_KIND`, per the s9/s10 §1 lineage reading: this pair locks no design; the c3 design lineage lives in the m-6 chain.
- **The g1 CLOSE, consumed as the contract of record for T6:** `s11-8a-joint-review/RECONCILE-orchestrator-planner-20260714-035210` (addressed TO this seat) — the integrated three-member 8a contract (§B), the token ruling (§C: bucket-D = `stale_choice_set`; bucket-A `held` = `stale_schema`; NO m-2 registration owed — `failing_edge` at `registry.json:113` is `system_only` open text with no `enum_set`), and the precedence rule (§D: where any doc byte contradicts §B, §B governs). m-2's §D rev4 editorial fold has since landed (`s11-8a-m2/SITREP-planner-20260714-140700` — doc and §B now agree); §B remains the contract of record regardless.
- **Master rulings consumed (do not re-open):** the g2 OQ-2 ceiling as ruled-pending-review (`s11-oq2-ceiling/DESIGN-planner-20260713-140357` — `sensor`-class read-only fork, ceiling `{write:read_only, dispatch:none, tool:read}`, advisory-never-gate-bearing, dies-on-verdict, parked lane byte-unmodified) · the annotation-C ruling (a design/grill gate lands as an A-bucket gate eligible for the elaborate-more fork, never force-compressed to a bare ODB) · FINDING-4 (gate-TIMING not optionality — an open gate at slice exit leaves that acceptance OPEN; never silently skip and never claim complete).
- **Delegated-authority chain for the coming token:** the orchestrator delegation (`s11-dispatch/PLAN-orchestrator-planner-20260714-023000`, `DELEGATED_DISPATCH_AUTHORITY: yes` under conditions (a)–(g)) → the m-6 review's five token conditions (`…-024712` §Token/Handoff, restated in §5 below) → my boot (`master-boot-s11-planner`). The token issues INSIDE this pair's loop, never from the m-6 guide.

### 2. Execution map (the delta this relay adds — nothing in the plan-of-record moves)

- **Branch:** `s11-comms-thicken` off `frank/ main@d91fcfb` (`s9-close`). You are sole code writer.
- **Order:** T1 (bucket B) → T2 (bucket C) → T3 (bucket D + egress/D-vs-A precedence) → T4 (the 7-state FSM: `bounced_repair` live, `egress_blocked` state + local-park-resummon fixture-scoped, away-mirror send UNBUILT) → **T6 (8a hardening — g1 CLOSED, build to §B now)** → T7 (the A/B/C/D × terminal × `failing_edge` matrix + the ③ known-A NF fixture; consumes T1–T4) → T8 (the 9-item refactor, commit per item, battery green between items) → T9 (G4 cadence → operator-config; no config value ever means auto-approve) → T11 prep. **T5 (elaborate-more fork) opens ONLY on the g2 completion to master; T10 (re-prompt + claimless-`held`) opens ONLY on the dc ritual returning.** If a gate lands mid-slice its task slots in at the next commit boundary; if not in hand by T11, the task exits **report-and-hold with its acceptance OPEN** (FINDING-4) — never a `t.Skip` stub, never proceed-on-assumption, never claimed complete.
- **T6 builds to §B byte-exactly (the g1 integrated contract):** the three byte-distinct records on one π-changing migration + a stale operator reply — the stale reply candidate `rejected`/bucket D + `failing_edge: stale_choice_set`, **no `resumed` wake**, accepted-branch skipped, old decision NOT resolved · the migration-fault signal `held`/bucket A + `stale_schema`, operator-visible, non-suppressible · the replacement gate/ODB under a **NEW decision identity** with resummon keys `(same seat, NEW decision_id, restarted cadence_slot series)`. Coupling: all three ride ONE serialized outcome OR the commit carries a durable re-issue intent whose recovery replays to the SAME replacement identity — the crash-replay fixture proves it at the stale-detect/re-issue boundary. Decision identity `π = {value → label}`: value AND label decision-bearing, order not; relabel TRIPS the stale path; reorder + added-representational-column are the π-invariant GREEN cases. The floor: migrate-then-validate at wake via the §9 read-time migrator; un-migratable → `held` + escalated + `stale_schema`, never dropped, never auto-resolved; the choice set is FROZEN at park (π snapshotted BEFORE any migrator; `migrate.Apply` deep-clones; the guard on the live `classifyVerdict` path keyed on immutable source ODB identity against a guarded migrated view, never the live registry; either-side failure fails closed). **The v1 honesty rail: zero migrators and no guard fire exist at `d91fcfb` — these are build obligations; no fixture may pretend past what it proves.**
- **Same-file seams (arm-per-task; the day-one named-seam discipline):** where two tasks touch one file (expected grain: `engine/submit.go` T6-guard then T10-edges; `engine/resummon.go` T6-re-issue-keys then T9-cadence-source; `engine/odb.go` T8-one-builder then T5-fork-feed; `engine/fsm.go` T4 then T6-routing), each task's diff is its own named unit in task order, recorded in the diff→license table as it lands. A diff outside a task's named seam at review is a deviation, not a judgment call.
- **B11 cadence:** straight-through, task-by-task under `superpowers:executing-plans`, NO per-task review relays; ONE end-of-slice adversarial review by me at completion; owner/fidelity confirms batched at exit with STOP-ON-CONTRADICTION (m-6 mechanism-fidelity as guide · m-3 on the ③ NF + design-cell edges · m-7 on scheduler/store/FSM seams · m-2 on any registry-adjacent assertion · m-5 on the fork ceiling if T5 opens). Mid-build blockers: report and hold TO me (condition f); cross-domain → master through me; locked-contract changes → owning pair + master, never silent.
- **Evidence capture:** per-task RED/GREEN batteries FILE-captured under `frank/.relays/s11/` (sequence-honest — the capture precedes the claim); the ten INV-CATALOG laws green at every commit point; full `go test ./...` at exit. Day-one exit items: both mechanical tables (consumption→supply · diff→license) maintained as you go; label→mechanism sweep + verify-the-summary-line ride my end review; RED-first negatives per task; byte-exact `{accepted, rejected, held}`; R2 (the ⑤ model field stays payload/render-only, never a gate input); Rail-A/Rail-B stated per task as the plan-of-record fixes them; I-PH over every new seat-visible surface (buckets, FSM, fork, ODB); the catch ledger extends the s8+s9+s10 series as ONE series (condition g).
- **Fence-union rule (live through the build):** the §3 fence is the START (m-6's domain seams ∪ the standing cross-cutting roots); if any mid-build amendment activates, its test/fixture seams reconcile into the fence AT activation; an owner return naming un-fenced loci is an ESCALATION TRIGGER through me to master, not an in-fence assertion.

### 3. The mechanical fence (the block of record for the token)

SCOPE_DIFF:
- frank/internal/engine/ -> in
- frank/internal/obligation/ -> in
- frank/internal/store/projections.go -> in
- frank/internal/store/genesis.go -> in
- frank/internal/bounce/ -> in
- frank/internal/tables/ -> in
- frank/internal/migrate/ -> in
- frank/internal/crashpoint/ -> in
- frank/internal/recover/ -> in
- frank/internal/config/config.go -> in
- frank/internal/fieldspec/registry_test.go -> in
- frank/internal/executor/executor.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/ -> in
- frank/.relays/s11/ -> in
SCOPE_DIFF_RESULT: all-in

**Named-seam licenses (file/dir-wide rows do NOT erase them):** `engine/` — the m-6 comms surface (FSM, ODB, resummon, prompter, wake, submit-side verdict classification) per the §2 task grain. `obligation/` — the ODB/envelope/`gate_category` consumption (T1–T3) + the shared system→operator envelope builder (T8). `store/projections.go` — ONLY the B/C/D saved-query read-side projections (T1–T3); `store/genesis.go` — ONLY T8 item 9 (genesis reverse-ladder growth); **the store write path (`store.go`, `lock.go`, `quarantine.go`) is m-1-locked and NOT in this fence — any perceived need STOPS and escalates.** `bounce/` — the bucket-D author-return formatting + the two reason tokens' rendering (T3/T4/T6). `tables/` — T8 items 4–5 ONLY (tables-snapshot prompter lookups; drop per-emit `tables.Build`). `migrate/` — T6 ONLY (the §9 read-time migrator extension + the `Apply` deep-clone obligation; m-2 fidelity by relay). `crashpoint/` + `recover/` — T6 ONLY (the crash-replay boundary + the durable re-issue-intent recovery replaying to the SAME replacement identity; m-7 fidelity by relay; recovery semantics beyond the re-issue intent byte-untouched). `config.go` — ONLY T9's G4 cadence loci (the two timers onto the §J operator-config surface; no value may mean auto-approve). `registry_test.go` — standing fence-union membership; assertion adjacency ONLY if a fixture forces it; **`registry.json` is NOT in this fence — g1 §C rules no registration owed; any perceived registry/schema need is a condition-(b) master escalation BEFORE work.** `executor.go` — **NO licensed seam until g2 completes to master**; then ONLY T5's fork spawn/feed/retire under the verbatim g2 ceiling (`{write:read_only, dispatch:none, tool:read}`, external_send:none, posture `interactive`, advisory-never-gate-bearing, dies-on-verdict, parked lane byte-unmodified); `Spawn` refusal semantics for every other class byte-untouched; any other executor diff STOPS and returns through me. `main.go` — ONLY composition-root wiring the §2 tasks force (G4 config handoff; bucket/digest surface registration). `test/fixtures/` — the T7 matrix + the ③ known-A NF fixture + the T6 §B fixtures + the s8/s9/s10 regression suites kept green. `.relays/s11/` — evidence captures only, not governance relays. **`internal/observe/` is NOT in this fence** — m-3's domain; T5's re-observe-on-wake consumes the existing s10 wake path; any observe diff is an escalation.

### 4. Acceptance criteria

The plan-of-record's ten acceptance criteria stand **verbatim** — B/C/D projections (B non-interrupting + promotable via the RAISE-ONLY monotonic raiser; C no-obligation; D author-facing with egress/D-vs-A precedence) · the full 7-state FSM (`bounced_repair` live; `egress_blocked` state + local-park-resummon semantics fixtured, away-mirror send unbuilt) · the fork under the g2 ceiling locked on g2 · 8a locked on g1 (now: to §B) · the matrix + ③ NF (a B-pick over a known-A record ⇒ raised to A + `gate_category` recorded, never silently absorbed) · the 9-item refactor behavior-preserving with the battery green · G4 on operator-config with no auto-approve path · re-prompt + claimless-`held` locked on dc · Rails/I-PH/tables per task · the exit package feeding the Step-2 step-exit with byte-exact `{accepted, rejected, held}` + R2 throughout. **Plus the binding grains of this instrument:** (i) T6's fixtures assert §B byte-exactly — BOTH reason tokens (`rejected`+`stale_choice_set` on the candidate AND `held`+`stale_schema` on the fault record), the no-wake, the new decision identity + restarted cadence_slot series, and crash-replay-to-same-replacement-identity; (ii) each task's diff reconciles to its named seam in the diff→license table; (iii) T5/T10 land only with their gates in hand, else report-and-hold with acceptance OPEN (FINDING-4); (iv) batteries FILE-captured sequence-honest under `frank/.relays/s11/`; (v) the ten INV-CATALOG laws green at every commit point.

### 5. Conditions, verbatim binding

**From the s11 dispatch, (a)–(g):** (a) unique sub-DISPATCH_IDs (the `s11-build-*` chain; batteries FILE-captured under `frank/.relays/s11/`); (b) registry/schema changes beyond the named returns → master BEFORE work; (c) locked-contract changes → owning pair + master, never silent; (d) cross-domain → master; (e) merge operator-only via `HUMAN_MERGE_AUTHORIZATION` at grant time; (f) blocked lanes report and hold; (g) the dogfood evaluation extends the s8+s9+s10 ledger as ONE series.

**From the m-6 approving review, the five token conditions:** (1) the token parents to this pair's own approved `s11-build-*` plan-review, consuming `s11-comms-thicken-plan` as plan-of-record; (2) g1/g2/dc preserved as HARD LOCKS on T6/T5/T10 — g1 is CLOSED by `…-035210` so T6 is LOCKED-and-buildable to §B; g2/dc stay open holds; (3) the mechanical scope/fence reconciliation precedes implementation (the §3 block + the fence-union/mid-amendment rule); (4) merge operator-only; (5) locked-contract / schema-beyond-named-returns / cross-domain surprises route to the owning pair + master before proceeding.

### 6. What I request from you

1. **Verify the adoption is faithful:** re-hash the plan-of-record against the pinned sha; check this delta adds no task, mechanism, acceptance criterion, owner relation, or authority beyond the pair-approved bytes; check the §3 fence rows against the plan's fence-union rule and the live tree at `d91fcfb`; check the gate state against the trail (g1 CLOSED at `s11-8a-joint-review/RECONCILE-…-035210`; g2/dc open) and the holds/ordering against §2.
2. Return `PLAN_REVIEW_VERDICT` in a lint-clean relay, `FROM: s11.implementer`, `TO: s11.planner`, `DISPATCH_ID: s11-build-plan-review-r1`, `PARENT_DISPATCH_ID: s11-build-plan`, with your own mechanical scope check over the §3 block.
3. On **approve** with an all-in result and no hard trigger, I issue the delegated same-owner token under `s11-build-token`, parented to your approving review, restating the block, the seam licenses, the holds, and the conditions. The token remains the only thing that starts code; T5/T10 additionally wait on their gates even after dispatch.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s11-build-plan/PLAN-planner-20260714-141100.md` — run before handoff (result in the inline pointer).
- Inputs consumed this turn: the charter · my boot (`master-boot-s11-planner/SITREP-…-024919`) · the plan-of-record (sha-pinned above) · the approving review (`s11-plan-m6-review-r1`) · the g1 CLOSE (`s11-8a-joint-review/RECONCILE-…-035210`, TO this seat) + the m-2 rev4 fold confirm (`s11-8a-m2/SITREP-…-140700`) · the g2 ruling (`s11-oq2-ceiling/DESIGN-planner-20260713-140357`) · the dispatch conditions (`s11-dispatch/PLAN-…-023000`) · the s9 pair-loop precedent (`s9-build-plan`/`s9-build-token`) · the live tree (`frank/` main clean at `d91fcfb`; `internal/engine/{fsm,odb,resummon,submit,approval,expiry,wake_test}.go`, `internal/store/{projections,genesis}.go`, `internal/{bounce,tables,migrate,crashpoint,recover}/`, `internal/fieldspec/registry.json:113` `failing_edge` system_only/no-enum_set — each confirmed present this session).

ACTIONS_GIT_REF: none — plan authoring only; no `frank/` edit, no branch, no code, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260714-141100.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `d91fcfb` (`s9-close`).
Next requested action: operator carries this to s11.implementer; adversarial review → `PLAN_REVIEW_VERDICT` under `s11-build-plan-review-r1`; on approve + all-in I issue the delegated token under `s11-build-token`; a must-revise returns the exact finding to me.
