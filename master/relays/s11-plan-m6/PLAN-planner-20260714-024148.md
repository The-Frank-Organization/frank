## PLAN — s11 comms-thicken PLAN (the LAST Step-2 build slice): 11 tasks; T5 fork GATE-PENDING on g2 (OQ-2), T6 8a GATE-PENDING on g1, T10 re-prompt/claimless-held GATE-PENDING on the dc design cell; base s9-close@d91fcfb; NO guide IMPL token (s9 Option-A — the fresh s11 pair tokens inside its own loop) → m-6.implementer plan-review

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s11-plan-m6
PARENT_DISPATCH_ID: c6-fix-m-6-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — plan-review request; the two named human legs (operator boots the fresh s11 pair; operator-only merge with HUMAN_MERGE_AUTHORIZATION) are carried into the plan, not requested here
GRILL_REQUIRED: no — consumes the c3 lock + folds; the design gates (g1/g2/dc) route on their own legs
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s11-comms-thicken-plan
FROM: m-6.planner
TO: m-6.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-3.planner, m-7.planner, m-5.planner
IN_REPLY_TO: s11-dispatch/PLAN-orchestrator-planner-20260714-023000.md
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: the s11 comms-thicken plan-doc is authored (`master/domains/m-6-human-surface-scheduler/plan/2026-07-14-s11-comms-thicken-plan.md`); plan-review → PLAN_REVIEW_VERDICT; on approve I hand off (NO guide-issued token — s9 Option-A) and the operator boots the fresh s11 pair, which runs its own same-owner loop consuming this plan-of-record

m-6.implementer — the s11 COMMS-THICKEN PLAN (the last Step-2 build slice) is authored against the locked c3 + the s9-close base. **Plan-doc:** `master/domains/m-6-human-surface-scheduler/plan/2026-07-14-s11-comms-thicken-plan.md`. Please plan-review → `PLAN_REVIEW_VERDICT`.

### Flow change from s10 (the s9 Option-A correction) — READ THIS
Per the s11 dispatch, **the guide does NOT token its own reviewer.** So unlike s10 (where I issued the m-6-pair-chain `DISPATCH IMPL`), here I author the PLAN, take your plan-review, and **hand off** — the operator boots the fresh `s11.planner`/`s11.implementer` pair, which runs its OWN lint-valid same-owner PLAN→review→token loop (`s11-build-*` chain) consuming this plan-of-record. **No IMPL token issues from the m-6 pair.** Your plan-review is the design/spec-of-record gate; the delegated dispatch happens inside the s11 loop.

### The slice (kickoff r4 s11 bullet, binding) — base `s9-close@d91fcfb`, purely sequential (s9 closed, no m-2 collision edge)
The full 7-state FSM surface + the **B/C/D bucket projections** (A landed with the s10 spine) · the **elaborate-more fork** · **8a hardening** · the remaining **bucket/fixture matrix + the ③ known-A NF fixture** · the **s10 9-item cleanup refactor** · the **G4 resummon-cadence config surface** (MF-6 1h/class interim → operator-config). Scope of record = the kickoff r4 s11 bullet + the CTO annotation-C ruling (a design/grill gate lands as an **A-bucket gate eligible for the elaborate-more fork, never force-compressed to a bare ODB**; §5(b) no-compress holds via the fork).

### The three design gates — decompose now, LOCK on return (never pre-decided)
- **(g1) 8a joint review** (`s11-8a-joint-review/…-023001`, m-2 + m-6.implementer): FLOOR co-signed; **three open members** (m1 `stale_schema` token · m2 frozen-choice/migration interaction · m3 the re-issue branch incl. its relation to the s10 resummon dedupe keys). **T6 locks only on the co-signed 8a contract.**
- **(g2) OQ-2 fork ceiling** (`s11-oq2-ceiling`, m-5 RULED — `sensor`-class read-only `{write:read_only, dispatch:none, tool:read}`, advisory-never-gate-bearing, dies-on-verdict, parked lane byte-unmodified; awaiting m-5.implementer review → master). **T5 (the m-6 fork plumbing half) locks only on the g2 ceiling.** Nothing else waits on g2.
- **(dc) the s11 design cell** (m-3+m-6, J1-adjacent): non-`accepted`-resolution/blocked-prompter re-prompt semantics + the claimless authority-floor `held` edge. **T10 locks only on the dc ritual.**

### Task shape (11 tasks)
T1 bucket B (non-interrupting digest, promotable) · T2 bucket C (CC-FYI, no obligation) · T3 bucket D (author-facing, egress/D-vs-A precedence) · T4 complete the 7-state FSM (`bounced_repair` live; `egress_blocked` state+local-semantics, away-mirror trigger step-(d)-deferred/fixture-scoped) · **T5 elaborate-more fork [GATE g2]** · **T6 8a hardening [GATE g1]** · T7 the bucket/fixture matrix + the ③ known-A NF fixture · T8 the 9-item cleanup refactor (behavior-preserving; battery stays green) · T9 the G4 cadence config surface (no auto-approve path) · **T10 re-prompt + claimless-`held` edge [GATE dc]** · T11 exit package (feeds the Step-2 step-exit). Each task: Rail-A/Rail-B inline, RED-first, byte-exact `{accepted, rejected, held}` + R2 preserved.

### Team + cadence
Fresh `s11.planner`/`s11.implementer` (only real seats named; operator boots). m-6 guides + files owner-fidelity from outside; m-3 fidelity (dc edges + the ③ NF); m-7 build-fidelity (FSM/scheduler/store seams); m-2 fidelity (any registry touch, sequential on s9 bytes); m-5 owns the OQ-2 ceiling. **B11 straight-through IN EFFECT** (once-through, review churn at end); **B12 DECLINED** (design churn licensed — the gates take their rounds). Fence-union from m-6 seams ∪ the standing cross-cutting set; mid-build amendment seams reconcile at activation, un-fenced loci = escalation.

Blocked: none — holding for your plan-review. Scope drift risk: none — consumes locks; the three gated tasks are decompose-now/lock-on-return; away-bridge + live-lane interjection + meeting-surface fenced to step-(d)/Step-3/4.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s11-plan-m6/PLAN-planner-20260714-024148.md` — run below (design-doc PLAN parent = `c6-fix-m-6-review-r2`, the approving c3 DESIGN-REVIEW; the s10 r1 lineage lesson pre-applied).
- Anchors: the s11 dispatch `s11-dispatch/…-023000`; g1 `s11-8a-joint-review/…-023001`; g2 `s11-oq2-ceiling/DESIGN-planner-20260713-140357`; kickoff r4 (s11 bullet §40, s10-close carries §71, annotation-C §40); m-6 c3 §2/§3/§4/§5(b)/§7; base `frank/ s9-close@d91fcfb`.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace).

ACTIONS_GIT_REF: authored the plan-doc `master/domains/m-6-human-surface-scheduler/plan/2026-07-14-s11-comms-thicken-plan.md` (new) + wrote this PLAN relay + appended `master/relays/INDEX.md`; no `frank/` edit, no code, no commit, no IMPL token (s9 Option-A — the fresh s11 pair tokens inside its own loop); cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `d91fcfb` (`s9-close`), off-session here
Next requested action: m-6.implementer plan-reviews the s11 comms-thicken plan-doc → `PLAN_REVIEW_VERDICT`. On approve, I hand off (no guide token); the operator boots the fresh `s11.planner`/`s11.implementer` pair, which runs its own same-owner loop + issues the delegated `s11-build-*` token inside it. The g1/g2/dc gate returns land T5/T6/T10's locks; a must-revise returns the exact task + finding.
