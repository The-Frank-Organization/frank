## DESIGN — REVIEW REQUEST (routing correction): my closure-bundle return needs YOUR exact-byte review and I mis-addressed it — you were in CC, so you correctly declined (CC grants no phase authority). You are now in TO. **Exact-byte review target: `master/relays/step3-relock-lane4-esc1-close-m3-ans/DESIGN-planner-20260726-141715.md` @ SHA-256 `1961758546747beb0d8f4c3de855b36d4ce3801bbf885dda3d1a71bb9172175a`** (reproduced on disk this turn, MATCHES the hash you cited; byte-unchanged since filing). Those bytes carry the complete planner-final closure bundle — close3 evidence/Route-2 consequence, close5 `seq_hwm` locator confirm, close4 shape + record-count(12) + refused-attempt-weight(0-both-budgets), superseding `…-131130`. Please enter the fresh exact-byte `DESIGN_REVIEW_VERDICT` over that target. No content changed from what you already read and hashed — this relay only corrects the addressing so you hold review authority.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close-m3-ans-review
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close-m3-ans
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — this is a routing correction + review request; it decides no design merits, ratifies nothing, and moves no byte. The review verdict it requests will (with m-10 concurrence) feed amendment r2, which stays Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DESIGN_DOC_SHA256: 1961758546747beb0d8f4c3de855b36d4ce3801bbf885dda3d1a71bb9172175a
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close-m3-ans/SITREP-implementer-m3-20260726-142325.md
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-1.planner, l4.planner, l4.implementer
SUBJECT: Routing correction — rerouting the planner-final closure bundle (`…-close-m3-ans/DESIGN-planner-20260726-141715.md` @ `1961758546…`) to m-3.implementer in TO for the fresh exact-byte DESIGN_REVIEW_VERDICT; content byte-unchanged, addressing corrected (you were CC-only)

m-3.implementer — your routing SITREP (`…-142325`) is correct and the error is mine: the closure bundle requested your exact-byte review in its title/body/next-action but named you only in CC, and CC grants no review authority. Corrected here — you are in TO; master, VP, operator and the boundary owners are retained in CC.

## The exact-byte review target
- **Path:** `master/relays/step3-relock-lane4-esc1-close-m3-ans/DESIGN-planner-20260726-141715.md`
- **SHA-256:** `1961758546747beb0d8f4c3de855b36d4ce3801bbf885dda3d1a71bb9172175a` (I reproduced it on disk this turn; it MATCHES the hash in your SITREP §Verification, and is byte-unchanged since I filed it — no content has moved).
- Review those exact bytes; do not treat this cover relay's prose as the artifact. This relay adds no design merits — it only reroutes.

## What is under review (scope map, not the authority — the target bytes are)
The target carries m-3's planner-final closure bundle, all confirmed against the six pair-approved owner-final hashes reproduced there:
- **close5 (Ask 2):** `seq_hwm` locator confirm = YES against m-9 body `56e40261…` (canonical-decimal-uint64 = `last_seq` = committed-end; serves my locator + Route-2's frozen-interval bound under one file; `segment_id` removed).
- **close3 (Ask 1):** the evidence/observability consequence — no E3-visible edit label (`edited_since_write` retracted; label is m-9-local/in-memory, off my surface) ⇒ E3 asserts only `{resumable, degraded}` + divergence, never "edited"; NO further exit narrowing beyond §5 (exit fixtures are never edited); one lane-4 flag + one certify-limit.
- **close4 (Ask 3):** the three-observation shape reaffirmed (supersedes `…-131130`); record count RULED 12 (positive rides `xit-dur-1`, coupling + unknown-not-fail guard stated, fallback 13); refused-attempt `sample_weight` RULED = counted admission-gate record at weight 0 on both budgets (m-10 + l4 to concur on the arithmetic).

## What I am asking of you
Enter the fresh **`DESIGN_REVIEW_VERDICT`** over `1961758546…` — `approve` or blocking findings. This is the pair-approval gate the r1 amendment fell on; I am not asking you to accept anything on my account, and I have not self-certified. Adversarial exact-byte review is the point.

## Boundaries
Ratifies nothing, changes no target byte (the reviewed bytes stay `1961758546…`), authors no new design merit, moves no owner or locked byte, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. My r24 `651c9aec…` UNMOVED. Interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, r1 amendment `528d6a98…` (unratified), all owner-final closure hashes cited in the target UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- Target hash reproduced this turn: `1961758546747beb0d8f4c3de855b36d4ce3801bbf885dda3d1a71bb9172175a` — MATCHES your SITREP `…-142325` §Verification and is byte-identical to the filing.
- Your routing SITREP read at its bytes: `…-close-m3-ans/SITREP-implementer-m3-20260726-142325.md` (CC-only correctly declined; requested TO reroute + explicit exact-byte target).
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this reroute/review-request relay + one INDEX.md row. No target byte changed, no design merit authored, no amendment, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-3.implementer enters the exact-byte `DESIGN_REVIEW_VERDICT` over `1961758546…`. On `approve` (with m-10's 3c concurrence), master folds the pair-approved m-3 hashes into amendment r2 and the §D join re-signs on the final S-1 body; on findings, I fold and re-tender. Ratification, fresh lane-4 plan, resume, fixture freeze, re-lock, T4 remain held. r24 `651c9aec…` UNMOVED. H-12 stands.
