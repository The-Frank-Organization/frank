## PLAN (m-2 planner, rev1 reissue — LINEAGE-ONLY: PARENT edge corrected per m-2.implementer `132500`; body byte-identical to `132400`) — s7a F-S7-R2-COLGRAIN

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s7a-dispatch
PARENT_DISPATCH_ID: s7a-dispatch-review-r2
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_RECORD_KIND: audit-record
IN_REPLY_TO: master/relays/s7a-dispatch/PLAN-REVIEW-implementer-20260710-132500.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-4.planner, m-4.implementer, m-7.planner, m-7.implementer
SUBJECT: rev1 reissue (lineage-only) — PARENT_DISPATCH_ID corrected to s7a-dispatch-review-r2 (my prior `s7a-dispatch-review-r1` was an invented predecessor — orphan edge, my error); acceptance/scope/mechanism/sequencing text UNCHANGED from `PLAN-planner-20260710-132400.md`

**Lineage-only correction.** Per m-2.implementer `132500`: the technical plan body is approved (all three r1 findings folded) — the sole must-revise was the orphan `PARENT_DISPATCH_ID: s7a-dispatch-review-r1` (no relay carries that DISPATCH_ID). This reissue points `PARENT_DISPATCH_ID` at **s7a-dispatch-review-r2** (the actual immediate predecessor, `PLAN-REVIEW-implementer-20260710-132500.md`). **No acceptance, scope, mechanism, or sequencing text changes** from `132400`. The body follows verbatim for the approving-review's single-object convenience.

### Mechanism (unchanged, approved-in-principle r1/r2)
Per-column validation at `any_row` resolution + a MINIMAL per-column gate-referenceable allowlist (**default-deny**); NOT the Step-3 nested-column schema (s5-(f)); NOT a `model_identity` blocklist (default-allow, weaker for R2). Restores fidelity to c1 §5 `:88` ("`any_row:<array>.<field>` — `<field>` obeys the same gate-referenceable allowlist"); the code (`predicate.go` any_row validates the **array only**) never enforced it. R2 enforcement, not redesign.

### Data contract (unchanged)
Add to `FieldSpec` (`registry.go`): `GateReferenceableColumns []string \`json:"gate_referenceable_columns,omitempty"\`` — allowlist only, **empty ⇒ default-deny**. The `any_row` resolver validates `rowField` against the array's `GateReferenceableColumns`, rejecting a non-allowlisted column at registry load.

### Acceptance criteria (unchanged from 132400)
- **AC1 (i):** the two synthetic `any_row:routing_assignments.chosen_model` fixtures (required_when + visible_when) accepted before / rejected after — the reject a **registry-load Go error** with pinned substrings (`owner` + `non gate-referenceable row field` + `routing_assignments.chosen_model`), **no path text**; not a runtime bounce.
- **AC2 (ii):** `any_row:routing_assignments.declared_deviated == "yes"` stays accepted + compiling (regression green; `registry.json:175-176`).
- **AC3:** `any_row:routing_assignments.seat` (non-model-identity, non-allowlisted) is **also rejected** — proves an allowlist, not a `chosen_model` special-case.
- **AC4 (iii):** full **uncached** battery + `go vet` green at tip (incl. the retargeted generic positive).
- **AC5 (iv):** diff confined to `internal/fieldspec/{predicate.go, registry.go, registry.json, predicate_test.go, registry_test.go}`; no engine/store/seat-surface; no s7-branch touch.
- **AC6 (v):** the only shipped-`registry.json` data change is `routing_assignments.gate_referenceable_columns = ["declared_deviated"]` — **flagged to master, cleared before the token issues**.
- **AC7:** a `registry_test.go` assertion that the decoded `routing_assignments.GateReferenceableColumns` is byte-exactly `["declared_deviated"]`.

### Build sequence (unchanged): red-first
1. **Red:** land the two `chosen_model` fixtures asserting registry-load rejection; run against current code ⇒ FAIL (currently accepted) — defect demonstrated.
2. **Green:** implement the column-grain check (`predicate.go`) + the `GateReferenceableColumns` member (`registry.go`) + the singleton allowlist (`registry.json`).
3. **Retarget (Fold 2):** the existing `any_row:SCOPE_DIFF.path` positive (`predicate_test.go:19,47`) → `routing_assignments.declared_deviated` (or a test-local registry allowlisting `SCOPE_DIFF.path`); shipped delta stays the singleton.
4. **Regression:** `declared_deviated` legal atom + `seat` default-deny negative + AC7 singleton assertion.

### Boundary contract (unchanged)
R2 made executable at column grain; byte-exact `{accepted,rejected,held}` + the 3-verb surface untouched; the load-error carries no path. **Out of scope:** the full nested-column FieldSpec schema (Step-3, s5-(f)); a `model_identity` per-column blocklist; any change to the routing_assignments column set or the justification-atom semantics.

### Branch (unchanged)
`s7a-colgrain` off BASE `frank/main@1d3e92c`.

### Delegated-dispatch (four conditions, unchanged)
This reissued PLAN (`DISPATCH_ID: s7a-dispatch`, `PARENT: s7a-dispatch-review-r2`) → m-2.implementer PLAN-REVIEW → **approve** (parenting to this PLAN) → `SCOPE_DIFF` **all-in** (incl. `registry.go`) with no hard trigger → I issue the bare `DISPATCH IMPL` token, `PARENT_DISPATCH_ID` chaining to that approving PLAN-REVIEW's unique DISPATCH_ID. The **AC6 registry-data flag must be cleared by master** before the token issues. Any deviation re-engages master.

ACTIONS_GIT_REF: wrote this lineage-only PLAN reissue + appended `master/relays/INDEX.md`; no `frank/` edit, no branch, no IMPL, no token; body byte-identical to `132400` save the corrected PARENT edge.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 1d3e92c, read-only).
Next requested action: m-2.implementer final PLAN-REVIEW (parenting to this PLAN's `DISPATCH_ID: s7a-dispatch`); on approve + `SCOPE_DIFF` all-in + no hard trigger + master's AC6 clearance, I issue the delegated dispatch token parenting to that approving review.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
