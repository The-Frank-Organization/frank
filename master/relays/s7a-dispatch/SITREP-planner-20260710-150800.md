## SITREP — s7a VP-F1 provenance fold pair-review: APPROVE (diff-grain verified + re-ran green); AC6 amendment ratified; returning to master for m-7 final-bytes re-confirm

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s7a-plan-m2-fold-f1-pairreview
PARENT_DISPATCH_ID: s7a-plan-m2-fold-f1
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — pair-review verdict; m-7 final-bytes re-confirm, VP re-approval, and operator merge remain downstream
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/s7a-dispatch/REVIEW-FOLD-implementer-20260710-150027.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-2.implementer, m-7.planner, m-7.implementer, m-4.planner, m-4.implementer
SUBJECT: VP F1 fold (s7a-colgrain@2bc0763) pair-review = APPROVE — provenance now truthfully attributes v5 to m-2/F-S7-R2-COLGRAIN/s7a-plan-m2; exact 4-value delta, zero semantic drift, re-ran green; AC6 wording amendment ratified

**Pair-review verdict: APPROVE.** VP F1 is a legitimate provenance-honesty finding — the v5 registry still attributed itself to the superseded s6 pass. The fold corrects it; I verified the exact `d76c3ad..2bc0763` delta at diff-grain and re-ran green. No blockers, no m-2 finding.

**Verified against the diff (`d76c3ad..2bc0763`):**
- **Exactly two files** — `internal/fieldspec/registry.json` + `registry_test.go`; nothing else.
- **registry.json fold = ONLY the four provenance values:** `owner` `s6-core-registry`→`m-2`; `design_doc_id` `s6-slice-6-design`→`F-S7-R2-COLGRAIN`; `plan_lock_id` `s6-slice-6-plan`→`s7a-plan-m2`; `note`→the truthful s7a note. **`version: s7a-fieldspec-v5`, `named_enums`, every field row, the guard, the `GateReferenceableColumns` member, and the singleton are byte-unchanged.** Zero semantic drift.
- **Test strengthened (good):** the weak provenance-owner nonempty check is replaced by an **exact-tuple `reflect.DeepEqual`** over all four values — so a future stale/wrong attribution fails the suite (`registry_test.go`).
- **Red-first:** red `37ac1dc` (`test: pin s7a registry provenance` — failed against the observed s6 tuple) precedes green `2bc0763` (`fix: attribute fieldspec v5 to s7a`). ✓
- **Full fence intact:** `git diff --name-only 1d3e92c..2bc0763` remains exactly the original five `internal/fieldspec/` files.

**Independently re-ran (worktree, not inherited):** `go test -count=1 ./internal/fieldspec` → ok · the provenance test `TestRegistryV2MemberParsesAndExposesLockedEnums` → ok · `go vet ./internal/fieldspec` → exit 0.

**AC6 wording amendment — RATIFIED (planner-side).** As amended: *"one semantic row delta (the singleton `routing_assignments.gate_referenceable_columns = ["declared_deviated"]`) plus the required provenance-attribution metadata update."* Consistent with my plan + §9: the provenance change is **VP-required attribution metadata, not a semantic field row**, and carries **no further version implication** (the additive-MINOR `s6-v4→s7a-v5` bump already covered the one semantic delta; correcting attribution to match the already-bumped v5 is not a new schema change). The singleton-only invariant for *semantic* rows still holds.

Blocked: no — pair-approved; returning for the m-7 final-bytes leg.
Scope drift risk: none — five-file fence held; the only additions since the last pair-approve are the four VP-required provenance values + their exact-tuple test.

Tests / verification: diff-grain review of `d76c3ad..2bc0763` (provenance-only registry change + exact-tuple assertion) + independent `go test`/`go vet` green; REVIEW-FOLD `150027`; full fence re-confirmed via `1d3e92c..2bc0763`.

ACTIONS_GIT_REF: read-only review of `s7a-colgrain@2bc0763` (worktree `/Users/jack/.config/superpowers/worktrees/frank/s7a-colgrain`) + re-ran tests (no edit); wrote this pair-review SITREP + appended `master/relays/INDEX.md`; no `frank/` edit, no merge, no PR.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `s7a-colgrain@2bc0763` clean tree (implementer-reported + review-confirmed).
Next requested action: master routes to **m-7 final-bytes** (composite-digest / s8-genesis re-confirm on the corrected registry) → VP re-approval of s7a → operator merge into `main`. m-2.planner holds for any m-7-surfaced finding.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
