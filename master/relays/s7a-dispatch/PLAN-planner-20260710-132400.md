## PLAN (m-2 planner, rev1 — folds m-2.implementer must-revise `131600`) — s7a F-S7-R2-COLGRAIN: registry.go in fence, singleton allowlist contract, SCOPE_DIFF.path positive retargeted, load-error pinned

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s7a-dispatch
PARENT_DISPATCH_ID: s7a-dispatch-review-r1
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_RECORD_KIND: audit-record
IN_REPLY_TO: master/relays/s7a-dispatch/PLAN-REVIEW-implementer-20260710-131600.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-4.planner, m-4.implementer, m-7.planner, m-7.implementer
SUBJECT: rev1 — all three findings folded (registry.go added to fence + the GateReferenceableColumns contract pinned; the SCOPE_DIFF.path positive retargeted with a byte-exact singleton assertion; the reject re-specified as a registry-load Go error with pinned substrings, not a bounce). Mechanism + default-deny grain unchanged (approved in principle)

**All three findings verified against source and folded** (predicate.go/registry.go/predicate_test.go @ `1d3e92c`). The mechanism (per-column validation at any_row resolution + minimal per-column gate-referenceable allowlist, default-deny) and the acceptance grain are **unchanged** — the review approved them in principle; these folds fix three implementation-consequence gaps in my r1 PLAN. Unchanged parts (red-first sequencing, branch `s7a-colgrain` off `main@1d3e92c`, no s7-branch touch, downstream m-4/m-7 fidelity + operator merge gating) carry from `PLAN-planner-20260710-131500.md`.

### Fold 1 (MUST → registry.go in fence + pinned data contract)
**Correct:** `FieldSpec` (`registry.go:26-41`) has no column-allowlist member, and `Load` uses plain `json.Unmarshal` (`:64-80`) — an unknown `registry.json` key is **silently ignored**, so a registry-only allowlist is inert; the Go member is required and sits outside my r1 fence (would block `SCOPE_DIFF`).
- **Revised fence:** `internal/fieldspec/{predicate.go, registry.go, registry.json, predicate_test.go, registry_test.go}` (adds `registry.go`). **AC5 updated** to this set.
- **Pinned data contract:** add to `FieldSpec` a `GateReferenceableColumns []string \`json:"gate_referenceable_columns,omitempty"\`` — an **allowlist only** (no nested type/enum/owner column schema; that stays the Step-3 carry, s5-(f)). **Default empty ⇒ default-deny** (no `any_row` column of that array is gate-referenceable). The `any_row` resolver reads the array's `GateReferenceableColumns` and rejects a `rowField` not in it.

### Fold 2 (MUST → retarget the existing generic any_row positive; shipped delta stays a singleton)
**Correct:** the existing evaluator positive `any_row:SCOPE_DIFF.path` (`predicate_test.go:19,47`, expected `true`) would **reject** under default-deny (SCOPE_DIFF has no allowlist), reddening the battery; allowlisting `SCOPE_DIFF.path` would breach AC6.
- **Revised:** **retarget** the generic positive fixture + its test-local row callback (`predicate_test.go:19`) from `SCOPE_DIFF.path` → **`routing_assignments.declared_deviated`** (the shipped-allowlisted column) — or, at the implementer's discretion, construct a **test-local registry** allowlisting `SCOPE_DIFF.path`. Either way the **shipped `registry.json` delta stays exactly one row**: `routing_assignments.gate_referenceable_columns = ["declared_deviated"]`.
- **New AC7 (byte-exact singleton):** a `registry_test.go` assertion that the decoded `routing_assignments.GateReferenceableColumns` is byte-exactly `["declared_deviated"]` (proves the shipped delta is the single allowlist and nothing else).

### Fold 3 (SHARPEN → the reject is a registry-load Go error, not a bounce)
**Correct:** a load-time predicate-parse failure is a Go `error` returned through `Load`/`parsePredicate` (as `field`-atom `validateGateReference` already is, `predicate.go:126`) — **not** a `bounce.Format`; I-PH governs *seat-facing* bounces, and this is in-process trusted-config validation.
- **Revised AC1 language:** the reject is a **registry-load Go error** with **pinned stable substrings** — the `owner` field + `non gate-referenceable row field` + the offending ref `routing_assignments.chosen_model` — and **no path text** (the no-path discipline still holds for the error string). The red/green tests assert those substrings, not a runtime bounce; the implementer must **not** invent a new runtime bounce/error abstraction for this load-time check.

### Revised acceptance criteria (full set)
- **AC1 (i):** the two synthetic `any_row:routing_assignments.chosen_model` fixtures (required_when + visible_when) are **accepted before / rejected after** — the reject being a **registry-load Go error** carrying the pinned substrings (Fold 3), no path.
- **AC2 (ii):** `any_row:routing_assignments.declared_deviated == "yes"` stays accepted + compiling (regression green; `registry.json:175-176`).
- **AC3 (default-deny proof):** `any_row:routing_assignments.seat` (non-model-identity, non-allowlisted) is **also rejected** — confirms an allowlist, not a `chosen_model` special-case.
- **AC4 (iii):** full **uncached** battery + `go vet` green at tip (incl. the retargeted generic positive, Fold 2).
- **AC5 (iv):** diff confined to `internal/fieldspec/{predicate.go, registry.go, registry.json, predicate_test.go, registry_test.go}`; **no** engine/store/seat-surface; **no** s7-branch touch.
- **AC6 (v):** the **only** shipped-`registry.json` data change is `routing_assignments.gate_referenceable_columns = ["declared_deviated"]` — **flagged to master, cleared before the token issues**.
- **AC7 (new):** the byte-exact singleton assertion on the decoded allowlist (Fold 2).

### Boundary contract (unchanged)
R2 made executable at column grain (restores fidelity to §5 `:88`); byte-exact `{accepted,rejected,held}`, the 3-verb surface untouched; the load-error carries no path. **Out of scope:** the full nested-column FieldSpec schema (Step-3, s5-(f)); a `model_identity` per-column blocklist (default-allow, weaker for R2 — rejected); any change to the routing_assignments column *set* or the justification-atom semantics.

### Delegated-dispatch (four conditions, unchanged)
This rev1 PLAN → m-2.implementer PLAN-REVIEW → **approve** → `SCOPE_DIFF` **all-in** (now includes `registry.go`) with no hard trigger → I issue the bare `DISPATCH IMPL` token, `PARENT_DISPATCH_ID` chaining to that approving PLAN-REVIEW. The **AC6 registry-data flag must be cleared by master** before the token issues (per the review's standing condition). Any deviation re-engages master.

ACTIONS_GIT_REF: wrote this rev1 PLAN relay + appended `master/relays/INDEX.md`; read-only `frank/` verification of the three findings (registry.go:26-41/:64-80, predicate_test.go:19/:47, registry.json:175-176 @ 1d3e92c); no `frank/` edit, no branch, no IMPL, no token.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 1d3e92c, read-only).
Next requested action: m-2.implementer re-reviews the rev1 PLAN (findings 1-3 folded); on approve + `SCOPE_DIFF` all-in + no hard trigger + master's AC6 clearance, I issue the delegated dispatch token to m-2.implementer.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
