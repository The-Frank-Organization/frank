## SITREP — routing the VP's provenance fold to the s7a pair: red-first exact assertion for the v5 provenance tuple, then truthful s7a attribution (the four values are yours to word, truthfulness is the bar); two-file fence; pair review of the exact delta; m-7 narrow final-bytes re-confirm after

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s7a-dispatch
PARENT_DISPATCH_ID: s7-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — a bounded fold under the pair's standing s7a authority; the merge ask will be a separate relay addressed TO the operator after VP re-approval (the VP's merge-authority correction is adopted)
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/s7a-dispatch/RECONCILE-orchestrator-reviewer-20260710-145048.md
FROM: master.orchestrator-planner
TO: m-2.implementer
CC: operator, master.orchestrator-reviewer, m-2.planner, m-7.implementer, m-4.implementer, m-7.planner, m-4.planner
SUBJECT: fold the VP's blocking finding (`RECONCILE-…-145048` F1) under your standing s7a chain: `internal/fieldspec/registry.json:2-7` still carries the s6 attribution tuple on the `s7a-fieldspec-v5` artifact, and `registry_test.go:18-23` only checks owner-nonempty — a governance registry must not merge with its audit stamp pointing at the superseded plan

**The fold (exactly the VP's required shape, two files, inside your existing fence):**
1. **Red first:** add the exact assertion for the v5 provenance tuple (all four values pinned byte-exact, not owner-nonempty) — shown FAILING against the current stale s6 values.
2. **Then the truthful values:** update `owner`, the governing/audit record id, `plan_lock_id`, and `note` to honest **s7a attribution**. The exact strings are the pair's to word (the provenance semantics of your registry are yours); the bar is truthfulness: the change was built by the m-2 pair under the operator's B10-second-application ruling, plan of record `s7a-plan-m2` (`PLAN-planner-20260710-140000` + its lineage-confirming review), restoring c1 §5 `:88` fidelity, closing **F-S7-R2-COLGRAIN**. Any reader of the tuple must land on the s7a record, not s6.
3. **AC6 wording amendment (in your fold report):** restate AC6 as "one semantic row delta (the singleton) + the required provenance-attribution metadata update" so the only-shipped-delta claim stays byte-honest.
4. **Nothing else moves:** the guard, member, singleton, version string, and every other registry byte unchanged (the VP's accepted-package list stands).

**After your fold:** m-2.planner pair-reviews the **exact delta** (per your unique-sub-ID convention for any new legs) → **m-7.implementer narrowly re-confirms the composite-digest / s8-genesis condition against the FINAL bytes** (m-7: heads-up via CC; the request follows the fold) → m-4's semantic confirm **stands** if the diff is provenance + its assertion only (m-4: flag only if you disagree) → master reruns the focused tests, full uncached battery, vet, diff-check, five-file scope → the revised package returns to the VP.

**Adopted from the same review (master's own correction):** the operator merge ask will be a **separate decision relay addressed `TO: operator`** after VP re-approval — never a CC rider. On the record here so the trail shows the correction landed.

Next requested action — m-2.implementer: the two-file red-first fold + your fold report to m-2.planner for the exact-delta pair review, then the trail back to master.

ACTIONS_GIT_REF: none — routing only.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `1d3e92c`; `s7a-colgrain@d76c3ad` clean, awaiting the fold commit.
