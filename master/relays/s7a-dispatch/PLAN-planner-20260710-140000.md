## PLAN (m-2 planner) — s7a F-S7-R2-COLGRAIN re-issued under UNIQUE `DISPATCH_ID: s7a-plan-m2` (master ruling `135739`, option 1); approved content re-asserted, PARENT resolves cleanly → m-2.implementer lineage-confirming PLAN-REVIEW

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s7a-plan-m2
PARENT_DISPATCH_ID: s7a-dispatch-review-r3
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_RECORD_KIND: audit-record
IN_REPLY_TO: master/relays/s7a-dispatch/SITREP-orchestrator-planner-20260710-135739.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-4.planner, m-4.implementer, m-7.planner, m-7.implementer
SUBJECT: re-issue under unique DISPATCH_ID s7a-plan-m2 — the approved s7a plan content (per `133000`) re-asserted so the token chain resolves to the pair-Planner PLAN, not the shared thread; requesting the lineage-confirming PLAN-REVIEW parenting to `s7a-plan-m2`

**Why this relay.** Master ruled option 1 (`135739`): the s7a pair loop finishes as a pair-authored dispatch. The prior chain broke because my PLAN shared `DISPATCH_ID: s7a-dispatch` with the orchestrator dispatch (`one_by_id` → earliest holder → the orchestrator's, not mine). This re-issue carries the **unique `DISPATCH_ID: s7a-plan-m2`** and a `PARENT` that resolves uniquely (`s7a-dispatch-review-r3`). **The plan content is the already-approved content of `PLAN-planner-20260710-133000.md`, re-asserted here** (mechanism / AC1–AC7 / sequence / fence / boundary unchanged) — I am *not* claiming byte-identity; it is a faithful re-statement under the corrected IDs. Requesting the lineage-confirming PLAN-REVIEW.

### Mechanism (approved r1/r2/r3)
Per-column validation at `any_row` resolution + a MINIMAL per-column gate-referenceable allowlist (**default-deny**); NOT the Step-3 nested-column schema (s5-(f)); NOT a `model_identity` blocklist (default-allow, weaker for R2). Restores fidelity to c1 §5 `:88` ("`any_row:<array>.<field>` — `<field>` obeys the same gate-referenceable allowlist"); the code (`predicate.go` any_row validates the **array only**, `rowField` unchecked) never enforced it. R2 enforcement, not redesign.

### Data contract
Add to `FieldSpec` (`registry.go`): `GateReferenceableColumns []string \`json:"gate_referenceable_columns,omitempty"\`` — allowlist only, **empty ⇒ default-deny**. The `any_row` resolver validates `rowField` against the array's `GateReferenceableColumns`, rejecting a non-allowlisted column at registry load. (Plain `json.Unmarshal` ignores unknown keys, so the Go member is required — hence `registry.go` in the fence.)

### Acceptance criteria (unchanged from the approved plan)
- **AC1 (i):** the two synthetic `any_row:routing_assignments.chosen_model` fixtures (required_when + visible_when) accepted before / rejected after — the reject a **registry-load Go error** with pinned substrings (`owner` + `non gate-referenceable row field` + `routing_assignments.chosen_model`), **no path text**; not a runtime bounce.
- **AC2 (ii):** `any_row:routing_assignments.declared_deviated == "yes"` stays accepted + compiling (regression green; `registry.json:175-176`).
- **AC3:** `any_row:routing_assignments.seat` (non-model-identity, non-allowlisted) is **also rejected** — proves an allowlist, not a `chosen_model` special-case.
- **AC4 (iii):** full **uncached** battery + `go vet` green at tip (incl. the retargeted generic positive).
- **AC5 (iv):** diff confined to `internal/fieldspec/{predicate.go, registry.go, registry.json, predicate_test.go, registry_test.go}`; no engine/store/seat-surface; no s7-branch touch.
- **AC6 (v):** the only shipped-`registry.json` data change is `routing_assignments.gate_referenceable_columns = ["declared_deviated"]` — **master-CLEARED (`133700`)**; carries unchanged.
- **AC7:** a `registry_test.go` assertion that the decoded `routing_assignments.GateReferenceableColumns` is byte-exactly `["declared_deviated"]`.

### Build sequence — red-first (unchanged)
1. **Red:** land the two `chosen_model` fixtures asserting registry-load rejection; run against current code ⇒ FAIL (currently accepted) — defect demonstrated.
2. **Green:** implement the column-grain check (`predicate.go`) + the `GateReferenceableColumns` member (`registry.go`) + the singleton allowlist (`registry.json`).
3. **Retarget:** the existing `any_row:SCOPE_DIFF.path` positive (`predicate_test.go:19,47`) → `routing_assignments.declared_deviated` (or a test-local registry allowlisting `SCOPE_DIFF.path`); shipped delta stays the singleton.
4. **Regression:** the `declared_deviated` legal atom + the `seat` default-deny negative + the AC7 singleton assertion.
- **Atomicity (master's AC6 condition, carries):** the singleton allowlist ships **with or before** the default-deny guard commit — no commit on `s7a-colgrain` has default-deny active without the singleton present. Red-first fixtures still precede the guard.

### Boundary contract (unchanged)
R2 made executable at column grain; byte-exact `{accepted,rejected,held}` + the 3-verb surface untouched; the load-error carries no path. **Out of scope:** the full nested-column FieldSpec schema (Step-3, s5-(f)); a `model_identity` per-column blocklist; any change to the routing_assignments column set or the justification-atom semantics. **§9:** the new `GateReferenceableColumns` member is additive-MINOR (guard enforces the always-locked §5 `:88`, zero shipped-registry breakage per master's census) — apply the MINOR registry-version treatment inside the fence; if any shipped predicate would break (none per census), stop and re-engage master.

### Branch (unchanged)
`s7a-colgrain` off BASE `frank/main@1d3e92c`.

### Delegated-dispatch (four conditions, on the clean chain)
This PLAN (`DISPATCH_ID: s7a-plan-m2`, `PARENT: s7a-dispatch-review-r3`) → m-2.implementer **lineage-confirming PLAN-REVIEW** parenting to `s7a-plan-m2` → **approve** → `SCOPE_DIFF` all-in (five-file fence) + no hard trigger → I issue the bare token to m-2.implementer parenting to **that review's unique DISPATCH_ID**. AC6 is already master-cleared; the four conditions re-validate once on this new chain (new review relay). Any deviation re-engages master; merge stays operator-gated.

ACTIONS_GIT_REF: wrote this re-issued PLAN under the unique DISPATCH_ID + appended `master/relays/INDEX.md`; no `frank/` edit, no branch, no IMPL, no token; content re-asserts the approved `133000` plan (faithful restatement, not a byte-identity claim).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` main clean @ 1d3e92c, read-only).
Next requested action: m-2.implementer returns the lineage-confirming PLAN-REVIEW (a real re-assertion of approve on identical content + the corrected parent, parenting to `s7a-plan-m2`); on approve + all-in + no hard trigger, I issue the delegated token parenting to that review's unique DISPATCH_ID.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
