## SITREP — m-7 guide → s1: both readings CONFIRMED from the locked design-of-record (Q-A recovery line; Q-2 ⑤/outbox), with the shape-right-from-S1 constraints that keep S2/S4 from re-cutting

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s1-guide-q1
PARENT_DISPATCH_ID: s1-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: frank/.relays/s1/s1-guide-q1/SITREP-orchestrator-planner-20260703-140843.md
FROM: m-7.planner
TO: s1.orchestrator-planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.implementer
SUBJECT: guide answers — Q-A: your recovery line is the locked line ("dumb replay" is the GRILL-LOCK's own words; F9 is already S1-sized in full); Q-2: confirmed, ⑤ stays a Step-1-wide S4 carry; three outbox shape constraints; no lock amendment needed either way

Both questions are answerable entirely from locked text — no amendment sought, none needed, no escalation to master required. Your provisional design sections can be de-provisioned on this relay. Design-of-record cited throughout = `master/domains/m-7-conductor-core/design/2026-07-01-v3-conductor-core-design.md` (line refs below are into it unless said otherwise).

### Q-A — CONFIRMED. Your line is the locked line; here is exactly where it falls and why the IN/OUT tension is only apparent.

**S1 builds (confirmed):** the §2.2 intake journal with its full semantics — single intake-writer, append+fsync **before** the in-memory FIFO, ack only on typed outcome, `intake_id` on every entry, **clear-on-pop atomic with the outcome commit** (every outcome record references the `intake_id` it consumes; no separate mark-consumed write exists) (`:57-58`) — plus the §4 Package-A rename pivot with redo-journal projections (`:80-84`), and a minimal recovery pass. "Dumb replay" is not your paraphrase, it is the GRILL_LOCK's resolved Q1 verbatim: "recovery = dumb replay" (`:186`). Its S1 contents, each anchored: staging cleanup (torn staging = delete, never committed, routine — `:91`), projection rebuild from canonical via redo replay, canonical wins, INDEX append-only (`:92`), binding-table restore (`:93`, §11 — required by NF-S1's restart bullet and your re-attach liveness line), re-enqueue `intake − outcomes` in arrival order (`:94`, phase 3.5 semantics), wake re-issue for committed-but-undelivered deliveries (liveness: inbox durable, pipe = nudge).

**S1 does NOT build (confirmed):** phase-0 genesis validation (genesis itself is charter-OUT), the phase-1 **canonical**-checksum quarantine disposition (`store/quarantine/` + HELD-class incident record, `:91`), GC / journal segment rotation (`:137`), and the reified phase-0→4 state machine with the formal no-authority-consumption-until-phase-4 rule (`:95`) — S1 has no nonce/burn set to protect; its only authority-adjacent restores are the binding table and parked-lane wake re-issue, which your dumb replay covers.

**The IN/OUT tension resolved:** the charter's OUT item "full recovery phases 0–4 + FIFO durability (S2)" does **not** exclude the intake journal — the S1 crash matrix (crash-after-intake-fsync, replayed-intake-id) is unpassable without §2.2's semantics, and those semantics are an operator-grilled rider (`:188-189`), not optional machinery. What S2 adds around them: segment rotation, drained-segment GC (only segments "whose entries all have outcomes", `:137`), quarantine + genesis validation, and the structured phase machine with its authority gate. Journal *semantics* in S1; journal *lifecycle machinery* in S2.

**F9, precisely:** you asked what F9 minimally requires in S1 vs asserts in full at S2 — the clean answer is that **F9 as written (§13 `:172`) is already S1-sized and S1 runs it whole**: kill -9 with N intake entries of which K have committed outcomes ⇒ recovery re-enqueues exactly the N−K without outcomes, in arrival order, zero re-emission of the K (the doc's instance: 5 enqueued, 2 outcomes ⇒ exactly the 3 re-enqueued). Pair it with the replayed/duplicate-`intake_id` no-double-emission check (content-hash dedupe, `:57`). S2 does not strengthen F9's property — it **re-runs F9 under the added machinery** (across a segment rotation; after a drained-segment GC; inside the full phase machine). Name your S1 fixture as F9 itself, not an F9-minimal variant.

**Three shape-right-from-S1 constraints** (same class as the rename pivot — deferring the machinery must not defer the format, or S2 re-architects):
1. The canonical record is a **self-contained checksummed record file from the very first commit** (`:81`) — the checksum field is S1 format even though the quarantine *disposition* that consumes a mismatch is S2.
2. Outcome records **reference `intake_id` from the very first record** — clear-on-pop is a record-shape property, not a recovery feature.
3. Recovery discipline even in dumb replay: **rebuild-before-open** — accept no `submit` until staging cleanup + projection rebuild + re-enqueue are complete. That is the phase-ordering *discipline* without the reified state machine; S2 formalizes it, S1 must not violate it.

### Q-2 — CONFIRMED. The S1 outbox line is typed ODB-item production only; ⑤ stays outside S1.

- **Dormant is the locked posture, not an S1 simplification:** the m-3 egress/content gate at the outbox chokepoint is "fail-closed, **dormant until the m-6 away bridge activates it**" (§9 `:132`) — the away bridge is a step-(d) mechanism, so no slice of Step-1 activates the scan. Your exit-gate B4 line covers production of the typed local outbox/ODB item through the loop, nothing more.
- **⑤'s classification, confirmed:** your charter (`master/relays/s1-dispatch/PLAN-orchestrator-planner-20260703-130634.md:72`) assigns S1 exactly three carries (code-layer guardrail, I-PH fixture, ③ guardrail-adjacent portion). ⑤ (`master/ARCHITECTURE.md:479`) is bound to the **Step-1 build as a whole**, receiving on m-7, and lands with the consumer-schema slice per the kickoff decomposition (`master/STEP-1-KICKOFF.md` §4 — "the §C4 owed fixtures") — i.e. **S4, not S1**. It needs the m-3/m-6 ODB field set (including the decision-⑤ exempt `model_name` slot), which is itself charter-OUT for S1.
- **The minimal ⑤-shaped constraint set your S1 outbox fixture must carry** (so S4 extends instead of re-cuts):
  1. **Chokepoint shape:** outbox enqueue is a **loop mutation** committing a **store-visible queue item** through the standard one-pivot discipline — F11 already lists "outbox enqueue" as a mutation class (`:172`). No side path may produce an outbox item; this queue is the surface ⑤'s scan later attaches to.
  2. **Produce-only, claim-honest:** S1 builds no drain/external send, and no S1 code, error text, or doc claims the egress scan is live; any "only egress" wording carries §9's governance-surface qualifier + D5 residual verbatim.
  3. **Open envelope:** the ODB item's field set is m-6/m-3-owned consumer schema — S1 types the minimal envelope it needs and must **not close or finalize the item schema**. Do **not** pre-build a `model_name` slot in S1; the S4 landing adds fields (the ⑤ carve-out included) without re-cutting the envelope.

**Ledger note (for your typed-owed-item hygiene):** ⑤ is already recorded on the master §C4 ledger with owner/source/target — it is not an s1 obligation to record, only to *not foreclose* via constraints 1–3 above. If your plan mentions it, cite it as the S4-bound §C4 carry, not as S1 scope.

Not authorized / not claimed: answers restate locked lines with citations; no lock amendment, no scope change to your charter (the IN/OUT lists stand as dispatched), no `DISPATCH IMPL`, no code, no `frank/` write by this relay.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s1-guide-q1/SITREP-planner-20260703-141628.md` — run before handoff (result in inline pointer)
- every cited line re-read this session: design doc `:57-58, :80-95, :132, :137, :172, :186-189`; ARCHITECTURE `:479`; STEP-1-KICKOFF §4; dispatch `:72`
- `git -C frank status --short` — untouched by this relay

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260703-141628; no doc/design edits; no code/`frank/` writes
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` not written by this relay)
Next requested action: operator carries this to the s1 session; s1 de-provisions the two dependent design sections against these answers and proceeds to its S1 PLAN for the guide+VP gate.
