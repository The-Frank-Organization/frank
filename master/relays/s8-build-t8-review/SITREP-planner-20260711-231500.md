## SITREP — s8.planner T7–T8 review VERDICT: T8 APPROVE · T7 MUST-REVISE — the E1 checks' own machinery faults dodge the fault edge: a timed-out or broken `git-status` on a non-authority record is ACCEPTED+self_reported (the exact §3.3:71/:72 conflation the m-3 lock's r1/F1 forbids); three findings, one seam, fold in-fence

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-t8-review-verdict
PARENT_DISPATCH_ID: s8-build-t8-review
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — task-review verdict under the standing `s8-build-impl` token; no new authority; merge remains operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T7, T8
IN_REPLY_TO: frank .relays/s8/s8-build-t8-review/SITREP-implementer-20260711-230000.md (worktree s8-observe-spine)
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: T8 approved on my independent verification; T7's executor-routed fault edge is correct but its E1 leg is not — the machinery-fault flag keys on the `executor-` detail prefix, so E1 check breakage (including E1's OWN 5s timeout) routes to no-vantage acceptance; fold F1–F3 below (all in-fence, no lock change, no escalation needed) and return the fold evidence; T9 stays held on the catalog bytes regardless

REVIEW_VERDICT: T8 approve · T7 must-revise (findings F1–F3, one seam: E1 fault classification). The fold is within the standing token and the 29-entry fence; it is NOT a lock question — the lock is unambiguous, the code deviates. m-3.planner is CC'd as observe-semantics guide if you want the row vocabulary confirmed before folding.

### What I verified and confirm (both tasks)

1. **Scope held:** `git diff --name-only e7199b5..59f01df` — gate.go, registry.go, submit.go, the two named fixtures, catalog.v1.json, `.relays/s8/`; all in-fence. Catalog diff is exactly one line (`bounce-format.expected_sites: 18→19`, matching T8's new typed bounce site); pending m-7 bytes untouched.
2. **T8 is correct and approved:** `LaneSuppliedSystemField` is placement-keyed in the authenticated lane submit handler (`submit.go:53-59`), observe-active, BEFORE form validation and conductor producers; ownership derives from the pinned registry (`gate.go:65-68`), not a hand list; the reject is the typed `lane-supplied-system-field` class with zero delivery intents; the four-field forge fixture + conductor-fill pass leg run green at my seat.
3. **T7's executor leg is correct:** `registry.go:128` flags `executor-*` details; `machineryFaultDisposition` gives authority `held`+escalate / non-authority `rejected` naming `observe-machinery-fault`; the decision-② rows (authority+self_reported/mixed → held; non-authority no-vantage → accepted+labeled; observed-fail → rejected both) match the lock, and `AuthorityClass` derives from the lineage engine (`completeness.go:25-30`) so the gate reads a computed value.
4. **Batteries:** full uncached battery green at my seat (25 packages ok, zero FAILs); focused Decision2 + SuppliabilityGuard green. The battery is green because the F1–F3 legs do not exist as fixtures — green is not closure here.

### T7 findings (one seam: E1 fault classification vs the lock's two-row split)

**F1 — BLOCKER: E1 `git-status` machinery failure — including its OWN 5s timeout — takes the no-vantage acceptance row.** `checks_base.go:48-59`: on `ctx.Err() != nil || err != nil` (the E1 hard-timeout expiring, git absent, or command failure) the check returns `Outcome: "unsafe", Predicate: Blocked, FailingDetail: "git-status-unavailable"`. The prefix rule at `registry.go:128` leaves `MachineryFault=false`, and `gate.go`'s `Blocked` arm routes to `noVantageDisposition` → **a non-authority record whose git-status check was killed or broke is ACCEPTED + `self_reported`**. The m-3 lock is explicit (§4 r1/F1; §8 fixture family): a check the conductor STARTED that could not run to completion is the §3.3:72 machinery-fault row — authority `held`, non-authority `rejected` + fault named — **never** `accepted`+label, which is reserved for the :71 no-vantage row (your own "opaque" fixture leg). Your boundary claim "timeout cannot become accepted+self-reported" is currently true only for executor-routed timeouts. Secondary: `Outcome: "unsafe"` mislabels machinery breakage — the lock reserves `unsafe` for the refused `side_effecting` policy path.

**F2 — must-fix, same seam: `read-file` IO error is classed as observed-false.** `checks_base.go:23-26`: `os.ReadFile` error → `baseVerdict(false, "read-unavailable")` = `Predicate: Fail` → `rejected` BOTH classes as `observed-false`. A machinery/IO failure (e.g. permission error) is not "ran and said no": on an AUTHORITY record the lock requires `held`+escalate, and the committed history mislabels the failure class either way.

**F3 — correction, same seam: a citation of a NONEXISTENT file is refused instead of observed-false.** `scopedPath` (`checks_base.go:74-77`) runs `EvalSymlinks` on the full path, which errors when the cited file does not exist → `refusedVerdict` → the claim marks `self_reported` → a non-authority lane citing any nonexistent file is accepted+labeled. Observed ABSENCE is the E1 vantage working: a valid-shaped citation of a missing file is `Fail` (observed-false → rejected) — the exit-leg-1-adjacent behavior for read-file claims. Param refusal belongs to the I-PH violations (absolute, `..`) only. Keep the symlink-escape guard by resolving the PARENT for containment and letting final-component absence surface as the observed Fail.

### Required fold shape (stays in-fence; the locked `CheckVerdict` shape is untouched)

1. Introduce a symbolic in-process machinery marker (e.g. `check-machinery-*` failing details) and extend the `registry.go:128` flag to the prefix set {`executor-`, `check-machinery-`}. `PredicateResult.MachineryFault` is m-3-internal registry-face semantics — this changes no locked m-7 boundary byte.
2. Reclass the E1 machinery paths: git-status timeout/command-failure and read-file non-absence IO errors → the machinery marker (outcome per the lock's claim-row vocabulary, not `unsafe`); missing-cited-file → observed `Fail`.
3. New fixture legs in `s8_decision2_test.go`/`s8_check_registry_e1_test.go` (the lock's §8 "timeout/machinery-fault ≠ no-vantage" family, now at BOTH rungs): E1 git-status timeout → authority `held` / non-authority `rejected`+fault named; read-file machinery error → same pair; missing-file citation → `rejected` observed-false; each PAIRED with the existing no-vantage acceptance leg so the rows cannot re-conflate silently.
4. Sequence-honest RED→GREEN capture for the new legs; full battery + INV-CATALOG green; report the fold on this chain.

### Gates unchanged
- T7 close = this fold + m-7's pending T5 executor-fidelity verdict (`s8-build-fidelity-m7/COORD-implementer-20260711-220000`) — both conditions, either order.
- T9/T10 stay hard-gated on the m-7 s8-v1 catalog bytes (`…-213500`). Operator: both m-7 requests remain the critical path — re-flagging the carry.
- T8 needs no further action; its approval is not conditioned on the T7 fold.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-t8-review/SITREP-planner-20260711-231500.md` — run before handoff (result in the inline pointer).
- Read/run at my seat (worktree `s8-observe-spine@59f01df`): the full `e7199b5..59f01df` diff; `checks_base.go` whole (the three fault paths at `:23-26`, `:48-59`, `:65-83`); `registry.go:100-133` (the prefix flag + executor-required fallthrough); `gate.go:100-170` (the disposition arms); `completeness.go:25-30`; `submit.go:53-59`; the decision2 fixture's no-vantage + executor-timeout legs (confirming the E1-timeout leg is absent); focused Decision2/SuppliabilityGuard tests + the full uncached battery (25 ok, zero FAIL).

ACTIONS_GIT_REF: none — a review-verdict relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260711-231500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine` clean at the reviewed checkpoint; `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer; the T7 fold lands per the required shape and returns on this chain with RED→GREEN evidence; T8 stands approved; T9 waits on the catalog bytes; the two pending m-7 requests still need carry to m-7.planner.
