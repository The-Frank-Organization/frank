## SITREP — the pre-s8 design round to m-7 + m-2 (joint): produce the co-signed config-family design note — the observe-layer activation knob as a §7 member, the INV-CATALOG artifact §7-pinned (member/home), and the discovery censuses (walk roots + boundary files) governed beside it — due before the s8 PLAN

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step2-prep
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded design round on shapes already intake-agreed; the deltas land as design-doc amendments through the ritual, co-signed by both pairs
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/s7-merge-gate/MERGE-GATE-implementer-20260710-190100.md
FROM: master.orchestrator-planner
TO: m-7.planner, m-2.planner
CC: operator, master.orchestrator-reviewer, m-7.implementer, m-2.implementer
SUBJECT: the step2-prep items 1 + the s7 additions, now due — s7 closed at `main@2e1b4f0` (tag `s7-close`), so the s8 PLAN is next and these gate it: (a) the layer-activation knob (your intake-agreed shape: a member under the pinned engine-config composite digest, activation = an operator-authored §7 `config_change`, restart-effective, A-1 re-render correct); (b) the catalog §7-pinning (member/home for `test/invariants/catalog.v1.json` — the staged "governed like registry.json" property completes here); (c) NEW from the s7 reviews: the path-hygiene discovery's OWN censuses — the AST walk roots + the two-file seat-egress boundary set — governed beside the others

**The ask:** one short **joint design note** (file-first, in the m-7 domain dir with m-2 co-sign or split per your §-ownership — your call), locking: the knob's member name + placement under the composite digest (m-7) and its atom semantics + the never-a-code-default rule (m-2) · the catalog's §7 member/home + its single-writer/owner-fidelity enforcement at load (m-7 leads, m-2 confirms the member grammar) · the census-governance shape for the discovery's walk roots + boundary-file set (from the pair-review note at `RECONCILE-planner-…-173423` and the S-1 fold — a new egress FILE must be a governed census amendment, not an invisible test literal) · the genesis/§7 semantics for the s8 dogfood store (your recorded condition: genesis from the exact `s7a-fieldspec-v5` bytes, member SHA `1ef6abab…2485`, composite digest recorded; state how the knob + catalog members compose into that genesis). Pair-implementer review per your normal discipline; the note is the s8 PLAN's input, so it lands **before** that PLAN is drafted.

Next requested action: the joint note + both pairs' co-signs, relayed to master; I fold it into the s8 PLAN inputs.

ACTIONS_GIT_REF: none — design-round request only.
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `2e1b4f0` (tag `s7-close`); cwd is not a git repo (docs workspace).
