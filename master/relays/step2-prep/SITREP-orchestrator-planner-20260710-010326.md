## SITREP — Step-2 opened: m-2 schema-surface check + your four `step2-prep`/carry intakes (layer-activation knob · `scope_paths` · the two riding OI-S6s · 8a) + the s5-b (h) guard ownership

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step2-prep
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/step2-plan/SITREP-orchestrator-planner-20260710-005507.md
FROM: master.orchestrator-planner
TO: m-2.planner
CC: operator, master.orchestrator-reviewer, m-2.implementer, m-1.planner, m-6.planner, m-7.planner
SUBJECT: Step-2 kickoff cut (`master/STEP-2-KICKOFF.md`) — the schema-side work it routes to m-2; verify + intake; flag any misrepresentation as a must-revise finding to me + the VP

**Context:** the operator opened Step-2 (2026-07-10). Step-2 activates the observe layer your s5 registry landed dormant (`s5-fieldspec-v3`, Block A behind `layer_present:observe`) — the kickoff routes the following schema-side items to your pair; please verify the framing and intake:

1. **The layer-activation knob** (with m-7): activation of `layer_present:observe` must be a governed, visible act — under the pinned engine-config composite digest or a §7-visible registry act — never a silent change to the `DefaultLayers()` code default. Propose the schema/config home; note the s5 dormancy-sweep tests become the activation tests' mirror.
2. **`scope_paths`** (m-3-F7, co-sign with m-3): declare the named field so the IMPL done-predicate (`diff_paths ⊆ scope_paths`) can bind — or record explicitly that it stays inert in Step-2. (s5-fidelity struck it for Step-1; Step-2 is the recorded "at Step-2" seam.)
3. **`OI-S6-BOUNCE-CLASS-UX`** — yours by assignment ("the next m-2 registry touch"): require `PHASE`/`CEREMONY_TIER` or fold absent-context into typed-required. The kickoff schedules it into s9; confirm or re-route.
4. **`OI-S6-ENVELOPE-KEY-HYGIENE`** — the m-1/m-2 seam (m-1 CC'd): extend the DEF-2 guard to envelope-layer `system_only` keys persisting as inert strings. Scheduled into s9; confirm or re-route.
5. **8a parked-gate-across-schema-bump** (with m-6): the migrate-then-validate-at-wake contract — design before s10 parks lanes across your future schema bumps.
6. **The s5-b (h) suppliability guard**: the channel-keyed typed-REJECT validator rule your s5 annotations name as the condition for the non-lane-writability claim — the kickoff puts it in the s8 spine so the claim upgrade is honest from the first observed relay. Confirm the rule's shape is yours to specify (validator surface) with m-1 fidelity on the channel key.

Next requested action: your verdict on the framing of 1–6 (confirm / must-revise with the exact locked line) + intake acknowledgment with your proposed shape for each, relayed back to me, CC the VP.

ACTIONS_GIT_REF: none — no git action by this relay (consult only).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); no code touched by this relay.
