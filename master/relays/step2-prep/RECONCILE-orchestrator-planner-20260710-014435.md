## RECONCILE — r4 returned for co-sign: all three r3-review fixes applied exactly (the 8a evidence boundary · the four pre-split s10 references · the dashboard disambiguation); no substantive change beyond your named corrections

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: step2-prep
PARENT_DISPATCH_ID: step2-prep
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — unchanged: the operator ratification set (now targeting kickoff r4 + the s7–s11 proposal) and the pre-s7 baseline precondition; this relay adds no new operator decision
GRILL_REQUIRED: no
IN_REPLY_TO: master/relays/step2-prep/RECONCILE-orchestrator-reviewer-20260710-014110.md
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator
SUBJECT: kickoff r4 — (1) the 8a co-signed floor now reads exactly what the evidence carries: migrate-then-validate + un-migratable → `held`/escalated, never silently dropped; `stale_schema` joins frozen-choices + bounce/re-issue in the m-6-proposed set, one joint 8a review may co-sign all three; (2) guide scope pluralized, OQ-2 + fixture ③ re-routed to s11, the operator ratification target is the s7–s11 bundle; (3) the dashboard row leads with a current-state label (queue s7–s11 · precise baseline · exact 8a status) and marks the r1 segment superseded

**Fix 1 (8a attribution):** kickoff design-item 5 now defines the **co-signed floor** as migrate-then-validate (§9 registry, then current-form validation) + un-migratable → `held`/escalated — never silently dropped, never auto-resolved — and lists `stale_schema` · frozen-choice-set · bounce-as-stale/RE-ISSUE as **the m-6-proposed set, not yet co-signed**, awaiting m-2's frozen-choice/migration confirmation (+ the reason token) and the m-6.implementer design review; per your note, no extra prose-only relay is being solicited — the joint 8a review co-signs all three together.

**Fix 2 (pre-split references):** the build-execution model now reads "m-6 guides the comms slices"; OQ-2 resolves **before the s11 fork build**; fixture ③ lands in **s11**; pre-flight item 3 asks the operator to ratify **the s7–s11 proposal** (r4). The gate-bearing line no longer references the superseded bundle.

**Fix 3 (dashboard):** `master/README.md`'s Step-2 row now opens with an explicit current-state label — queue s7→s11 with step-exit after s11 · baseline = pre-s7 operator precondition at `main@a1bc6d45ac5c` (1105 expanded entries) · 8a = partial co-sign with the floor/proposed split — and marks the r1 append-order segment superseded where it conflicts; the r1 queue text itself is tagged *(r1 — superseded by the r3 recut)*, and the pair-verified segment's 8a line is corrected to the floor/proposed split. One unambiguous operator-facing state; the append-order history remains legible.

Nothing else moved: no mechanism, routing, or scope change beyond your three named corrections; the closed findings (F1/F3/F4, the s7/s8 watchpoints) are untouched.

Next requested action: your co-sign on r4. On co-sign: the operator ratification set (kickoff r4 · naming · egress ruling · the s7–s11 proposal) + the baseline precondition, then I formalize the `step2-plan` decomposition and cut the s7 INV-CATALOG dispatch.

ACTIONS_GIT_REF: none — no git action by this relay (docs-workspace edits: `STEP-2-KICKOFF.md` r4; `master/README.md` row disambiguation; this relay + its INDEX row).
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` untouched at `main@a1bc6d45ac5c`, the baseline precondition pending with the operator.
