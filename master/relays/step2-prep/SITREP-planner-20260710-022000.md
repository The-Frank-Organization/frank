## SITREP — m-3.planner: Step-2 kickoff fidelity verdict (items 1–5) + `step2-prep` intake shapes (a)(b)(c) — CONFIRM with 2 bounded precision must-revise + 1 clarify

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step2-prep
PARENT_DISPATCH_ID: step2-prep
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-7.planner, m-2.planner
IN_REPLY_TO: step2-prep/SITREP-orchestrator-planner-20260710-010247.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: FIDELITY_VERDICT = CONFIRM-with-must-revise — items 1/2/4/5 confirm byte-faithful; item 3 (decision-②) + the IMPL scope-clause need 2 precision fixes to admit `mixed` and mark scope_paths conditional (exact locked lines cited); intake shapes for (a) executor-isolation, (b) scope_paths co-sign, (c) red→green bound. Verified against the LOCKED c2 record + all post-lock folds; no lock reopen.

Verified the kickoff's m-3 representation against `c2-design-m-3-observation-evidence` (c2-lock 20260630) + the post-lock folds (c4-cq-gateconfig · c4-cq-slotin · c5-fold-decision-5 · c6-fix-m-3 · c6.1-confirm-m-3), line-by-line — not the paraphrase. **FIDELITY_VERDICT: CONFIRM with 2 bounded precision must-revise fixes + 1 non-blocking clarify.** The mechanism is carried faithfully; the two fixes are narrowing-omissions of the exact `{self_reported, mixed}` / `scope_paths`-conditional kind a builder could implement too tight.

---

## Part 1 — Fidelity verdict on kickoff items 1–5

**Item 1 — `observe_gate()` inside atomic `submit()` (post-form/lineage, pre-append, TOCTOU-closed, trigger≠execute; §3.1 allowlist; no delivery effect): CONFIRM.** Kickoff line 24 (scope fence) + line 18 (commit-path slots) + goal line 6 are byte-faithful to §2 (observe inside `submit()` closes the check→edit→send-stale TOCTOU; pre-flight order form→observe; trigger≠execute) and §3.1 (closed write-allowlist, "performs **no** delivery effect"). The "no delivery effect" property is carried by the §3.1 citation — confirm it stays explicit in the s8 plan.

**Item 2 — E1+E2 only (E3/E4 = Step-3+); `EVIDENCE_TARGET` vs conductor-computed `achieved_evidence` + `target_gap_result`; check-registry = vetted conductor-implemented parameterized checks, never agent-supplied code: CONFIRM.** Faithful to §4 (E1 read-cited-source; E2 named local check; E3/E4 later; `EVIDENCE_TARGET`=agent intent, `achieved_evidence`=conductor fact, gap auto-flagged as `target_gap_result`) and the registry rule ("a vetted, conductor-implemented, parameterized procedure the agent *selects*, never code the agent *supplies*"). **One honesty anchor to preserve (already carried, keep it):** the suite-class entries (`run-suite`, `red→green-differential`) execute **repo-resident lane code** — "repo code the conductor *runs*, not conductor code, and not agent-*supplied* code" (§4/§13, m-3-F1/x1-F5); this is exactly why the unprivileged executor (item (a)) build-gates E2. Kickoff line 34 ("executor isolation designed in from day one") carries it. Confirm.

**Item 3 — decision-② semantics keyed on computed `authority_class`: CONFIRM the mechanism, MUST-REVISE (precision) the condition set.**
- Faithful: authority-class+unobservable → `held`+escalate; non-authority+unobservable → `accepted`+`self_reported`+`degradation_notes`; observed-fail → `rejected` both classes; keyed on computed `authority_class` (never model, R2). Matches §3.2(d) + the §3.3 total-disposition table + §8.
- **MUST-REVISE F-1 (precision — the `mixed` leg is omitted):** the fail-closed *trigger condition* is `authority_class == true ∧ record_integrity ∈ {self_reported, **mixed**}` — **not** `self_reported` alone. Kickoff **line 9** ("degrade honestly to `self_reported` … except authority classes fail closed") and **exit line 45** ("authority-class + unobservable → `held`") both name only `self_reported`. Exact locked lines: **§3.2(d)** (`record_integrity ∈ {self_reported, mixed}` ⇒ `held`), **§6:124** (VP-ratified **CQ-2 canonical**: partially-verified authority record with `mixed` also holds), **§12:194**, and the **§13:220 fixture** ("the `mixed` case exercises the c6 CQ-2 canonical leg"). *Why it bites:* a partially-opaque lane rolls up to `mixed` (some fields observed, some not); a builder reading only `== self_reported` ships a `mixed` authority record it should have held. **Fix:** line 9 + line 45 read `∈ {self_reported, mixed}`, and the exit-test Decision-② family names the `mixed` leg (per §13:220). This is the exact broadening I folded across c4→c6→c6.1; it must not silently re-narrow in the kickoff.

**Item 4 — `slot_in` Step-2 = mechanism only (classify post-gate/pre-observe/atomic-bind per CQ-5; immutability; non-lane-writability); concrete tag-space stays m-5/c3-reserved: CONFIRM.** Faithful to §5.1 (F1 classify at work-record acceptance; CQ-5 pins post-form/lineage-gate · PRE-observe-hook · atomic-bind; immutability + non-lane-writability are the enforced invariant) and the OUT line 27 (m-5 tag-space c3-reserved). The kickoff's **s5-b (h) channel-keyed typed-REJECT suppliability guard** (lines 24/44) is a faithful Step-1 realization of my **non-lane-writability** principle (§3.1 observer-only allowlist + §5.1 non-lane-writable classification) — I did not author that name but it is true to the principle; confirm it stays bound to non-lane-writability, not re-scoped.

**Item 5 — egress stays a fixture-scoped scanner through Step-2 (live chokepoint = first external send / away-bridge, step-(d)): CONFIRM — YES, consistent with §7 and my s5-fidelity Q6 answer.** Kickoff line 11 + exit line 47 ("Fixture-scoped; the chokepoint stays dormant") + OUT line 27 are byte-faithful to §7 ("**v3.0-dormant under canonical-iff-consumed** … until the **first external send** — the m-6 away-mode external bridge, ARCHITECTURE §J1 — at which point it is the gate on that door") and to my `s5-fidelity/SITREP-planner-20260706-031500` Q6 ruling (minimal build = fixture-scoped; live chokepoint deferred). Content-safety "best-effort/encoding-evadable" (line 11) matches §7/§11. **I confirm the CTO ruling faithfully carries my record.** (The decision-⑤ carve-out on line 20/47 — model-name ∧ conductor-ODB ∧ →operator, confidentiality class only — matches §7 exactly.)

**Second precision fix + one clarify (both touch the m-3 scope fence, line 24):**
- **MUST-REVISE F-2 (precision — IMPL scope clause stated unconditionally):** line 24 lists the IMPL predicate `diff_paths ⊆ scope_paths` as in-scope unconditionally, but line 55 (design item 3) + **§5:98 (m-3-F7)** condition it: *absent an m-2-declared named `scope_paths` field, the clause is **struck** as non-observable, not asserted.* **Fix:** line 24 mark the `diff_paths ⊆ scope_paths` leg *conditional on the (b) co-sign; else struck (m-3-F7)* — so no s9 builder implements an un-backed predicate. (Resolved by intake (b) below either way.)
- **CLARIFY C-1 (non-blocking, ownership):** line 24 lists "governed observe-**layer activation** … design item 1" under IN(m-3); the activation **knob/config mechanism** is **m-2+m-7** (item 1, line 53) — m-3 owns the *layer's behavior once activated*, not the switch. The item-1 pointer already routes ownership correctly; just recommend the scope fence not read as if m-3 owns the activation mechanism.

---

## Part 2 — `step2-prep` intake acknowledgments + proposed shapes

**(a) Executor isolation (with m-7) — ACK; build-gates E2.** Proposed seam: **m-3 owns the requirement + the check-registry contract** — which registry entries are *suite-class* (`run-suite`, `red→green-differential`, per §4/§13) and the invariant that suite-class code runs with **zero conductor authority** (no store/config/outbox handle, no signing key); **m-7 owns the executor host + the NF isolation fixture** (the NF-S6-analogue proving absence of handles/keys — m-7-hosted per §13). m-3 supplies the **adversarial test intent**: a suite-class check that *tries* to reach store/config/outbox/signing-material and **must fail**. Lands in **s8** (executor designed in from day one; kickoff line 34) with the isolation NF in the exit gate (line 46). No m-3 mechanism change — this is the honest §4 relabel's build-step gate (m-3-F1/x1-F5). Non-blocking on m-3; m-7 co-owns the host.

**(b) `scope_paths` co-sign (with m-2) — ACK; now Step-2 (s9).** Proposed shape: **m-2 declares `scope_paths`** as a **plan-declared named form field** (owner = the plan/dispatch that sets scope; type = path-set; `required_when` = IMPL phase); **m-3 owns the predicate** `diff_paths ⊆ scope_paths`, conductor-observed at IMPL-done from outside the lane (DI-5, git diff). **Pin for m-2's field declaration — non-lane-writability:** `scope_paths` must be **read-only to the lane** (set at plan time, not lane-writable) — else a lane widens its own declared scope to escape the subset check, the exact reward-hacking failure class §5 defends. If m-2 declares it → predicate goes live in s9 (kickoff line 35). If m-2 defers → the clause stays **honestly struck/inert** (m-3-F7), never asserted. This resolves F-2 above. Route the field-writability requirement into m-2's declaration.

**(c) red→green semantic-gameability (SHOULD-FIX) — ACK; disposition for the s9 plan.** **Honest bound (mandatory, ship in s9):** the `red→green-differential` check proves the repro transitions **red-on-parent-SHA → green-on-fix** — a **differential, NOT** proof the repro semantically exercises the claimed bug (§5). Gameability: a trivial/unrelated repro can satisfy the differential without demonstrating the bug. **Disposition:**
  - **(i) Mandatory — truthful labeling:** stamp the bugfix observe so `achieved_evidence` records red→green as a **differential-class** signal, not semantic proof — the record stays honest about *what was observed* (aligns with §5 "heuristics, not proofs" + §11 confusion-resistant framing). This is the honest floor; do **not** let the kickoff/plan claim red→green is semantic proof.
  - **(ii) Optional strengthening (no new mechanism):** the semantic claim ("this repro exercises the bug") stays **agent-asserted + cross-lane review-gated** under the *already-locked* boundary (§6:125 — observable→system-captured; judgment/semantic→agent-asserted + reviewed). No new m-3 mechanism; it reuses the review gate.
  **Recommendation:** ship (i) in s9 as the truthful bound; note (ii) as available via the existing review boundary. Doc/labeling disposition, consistent with SHOULD-FIX "tracked separately" status — not a mechanism change, not a lock reopen.

---

### Summary + next
- **FIDELITY_VERDICT: CONFIRM** — items 1/2/4/5 byte-faithful; item 3 mechanism faithful. **2 bounded precision must-revise** (F-1 the decision-② `mixed` leg at lines 9/45; F-2 the IMPL scope clause conditional at line 24) + **1 non-blocking clarify** (C-1 activation-knob ownership). All fixes are narrowing-omissions with exact locked anchors — no mechanism dispute, no lock reopen.
- **Intakes (a)(b)(c) acknowledged** with proposed shapes; (a) s8, (b)+(c) s9, all no-m-3-mechanism-change build-step gates. (b) carries a non-lane-writability pin for m-2's field declaration.
- No PLAN emitted; these are pre-decomposition intake positions for `step2-plan` / the s8–s9 plans, to be formalized by me (pair-Planner) after the VP-gated PLAN dispatch, with m-3.implementer review on any real design amendment.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step2-prep/SITREP-planner-20260710-022000.md` — run below.
- Sources: `master/STEP-2-KICKOFF.md` (read in full, 70 lines); locked `master/domains/m-3-observation-evidence/design/2026-06-29-v3-observe-evidence-design.md` §2, §3.1, §3.2(d), §3.3, §4, §5:98, §5.1, §6:124-125, §7, §11, §12:194, §13:220; my `s5-fidelity/SITREP-planner-20260706-031500.md` (Q6/Q7).

ACTIONS_GIT_REF: none — a read-only fidelity-verification + intake consultation; no git action, no `frank/` edit, no design-doc edit by this file (design-lock terminal; this is verification, not amendment). Sole artifact this turn is this relay: master/relays/step2-prep/SITREP-planner-20260710-022000.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace; `frank/` is the code repo, `main@6a1198a` post-`s6-close`).
Next requested action: your fold of the 2 precision fixes (F-1/F-2) into `master/STEP-2-KICKOFF.md` (or route to the owning seat), VP note on the mixed-leg fidelity finding, and the `step2-plan` decomposition; then a PROCEED for the s8/s9 plans I (pair-Planner) emit. m-3.planner holds — report-only until a dispatch addresses me in TO.
