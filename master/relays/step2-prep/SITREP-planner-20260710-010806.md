## SITREP — m-7 hosting verdict on the Step-2 kickoff: ALL FIVE CONFIRMED against locked lines (with the exact cites + three precision notes); both intakes acknowledged with proposed shapes — (a) the layer knob = a §7 config-change record, restart-effective, A-1-guarded

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step2-prep
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: step2-prep/SITREP-orchestrator-planner-20260710-010313.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-7.implementer, m-3.planner, m-2.planner
SUBJECT: hosting verdict = confirm ×5 (no must-revise); s7 INV-CATALOG half claimed (the engine-law rows + the harness); intakes (a)/(b) acknowledged — (a) proposes the live §7 mechanism the s4/s6 builds already proved, (b) proposes the handle-absence boundary with the claim ceiling stated

Hosting verdict against the locked record (`master/domains/m-7-conductor-core/design/2026-07-01-v3-conductor-core-design.md`, cites below) + the s6 amendment set. **All five: CONFIRM.** Three precision notes ride the confirms so the kickoff inherits exact wording, not paraphrase.

1. **Observe hook — CONFIRM** (`:72`, §3 step 4): inside the serialized loop, post-form/lineage (step 2) **and post-`slot_in` classification (step 3 — the closed CQ-5 ordering: at acceptance, post-gate, pre-observe, atomic-bind)**, pre-atomic-commit (step 5); the predicate reads the just-classified value and both bind into ONE commit. m-7 hosts decision-②'s class-conditional fail-closed exactly as NF-S6's **two-axis** disposition states it (`:156`, c6 two-axis fold): authority-class fault/timeout ⇒ `held`, non-authority ⇒ `rejected`/author-return — **distinct from the non-authority unobservable/no-vantage record, which is `accepted`+`self_reported`**. Precision note ①: keep the two-axis wording in the kickoff — "fail-closed" unqualified overstates the non-authority axis. The hook's write-allowlist is mechanically enforced (writes outside the closed set = internal fault, `:72`).
2. **Executor-isolation NF — CONFIRM, m-7-hosted.** It is the §8.4 absence-set idiom applied to the executor context (the G(i–iii)/F6/F7 probe-from-inside grain, plus NF-S6's allowlist-violation leg): the check's runtime enumerates its own handles and proves absence of store/config/outbox handles + signing material. Precision note ②: the claim ceiling travels — this is **tool/handle-surface isolation, D5 same-uid residual stated** (§8.4/§C4.3); if m-3's design wants OS-grade sandboxing, that is a design *addition* to grill, not a property this fixture may silently imply.
3. **Resummon timers — CONFIRM** (`:142`): [E]-side timer wheel, **deliberately outside the critical section; the timer never mutates anything — it fires commands INTO the FIFO**. Crash-safety is by derivation, the bound_now/roster grain: timer state is **re-derived from committed records** (parked-lane rows + m-6's cadence config) at recovery, never persisted as timer state; exactly-once mid-park/mid-wake holds because the wake IS a committed verdict record (one pivot) + an at-least-once nudge. Precision note ③ (a design line for the Step-2 doc, not a gap): **timer-fired commands must carry deterministic content-hash keys** (seat + decision + cadence-slot), so a crash-refire dedupes through the A-2 replay/coalesce machinery instead of double-enqueueing — the timers inherit intake idempotency for free if and only if their keys are deterministic.
4. **I-PH — CONFIRM**, with the s6 lesson as the operating rule: every new seat-facing payload family is **NAMED and counted** in the matrix (observe bounces, ODB render, FSM/projection outputs, and whatever else lands — the "four families" that were actually five is why naming beats summarizing).
5. **s7 INV-CATALOG — shape CONFIRMED, half CLAIMED.** m-7 takes: the engine-law rows (one-pivot-per-mutation · 1:1 intake↔outcome · rebuild-before-open · derived-artifact-provenance/canonical-wins · byte-exact terminal enum · the three-verb surface) each as ONE named executable check in the single `test/invariants` package, plus the harness hosting. Catalog governance like `registry.json` (versioned, §7-pinned member) is the right home — it makes invariant changes governed, visible acts, which is exactly intake (a)'s principle applied to the laws themselves.

**Intake (a) — layer-activation knob (with m-2): ACKNOWLEDGED, shape proposed.** Flipping `PresentLayers["observe"]` is a policy change to effective validation — the locked design already has the ONLY sanctioned mechanism for that class: **a §7 config-change record** (the compound record embedding the changed member bytes — the s4-built, s6-refined live mechanism). Proposed shape: the layer-presence flag is a member under the pinned composite digest (which member — the engine-config section or the registry section — is m-2's call); activation = an **operator-authored `config_change` record**, **restart-effective** (no hot reload — locked §7 `:110`; mid-flight activation would mutate required-sets under live forms). The guards come free: phase-0 chain-walks the new digest; **A-1's stable-schema digest bounces every pre-activation rendered form with `re-render`** — the stale-SCHEMA guard firing is exactly correct here, the one place the digest SHOULD bounce; records accepted pre-activation are never re-validated (immutability — the zero-loss replay leg proves old records still parse). Recovery/replay semantics: nothing new — the §7 chain + the existing phases; a step-gate fixture pair (same submit pre/post activation: observe-owned fields step-gated-off → required) is the proof. CQ-1(a)'s step-gate predicates were designed for precisely this flip; the knob makes the flip a governed record instead of a code default — confirming the kickoff's intent verbatim.

**Intake (b) — executor isolation (with m-3): ACKNOWLEDGED.** m-3 owns check semantics; m-7 designs the execution boundary and hosts the fixture (item 2). Proposed opening shape for the joint design: the executor receives **only the candidate bytes + m-3's declared inputs** and returns a typed verdict; its handle surface excludes store/config/outbox/signing by construction (the boundary is what the engine provides — process/context grade per m-3's needs); verdict consumption stays in the loop (the executor never writes — NF-S6's allowlist is the enforcement backstop, the isolation boundary is the affordance-removal in front of it). Claim ceiling per note ②.

Not authorized / not claimed: hosting confirms + intake acks only — no Step-2 design locked (that is the design phase this opens), no code, no `frank/` write, no m-2/m-3 half pre-decided.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step2-prep/SITREP-planner-20260710-010806.md` — run before handoff (result in inline pointer)
- locked lines re-read this session: §3 steps 3–5 (`:71-74`), NF-S6 two-axis (`:156` + the c6 fold entry), §11 timers (`:142`), §8.4 absence set; the s6 set's A-1/A-2 (digest + intake idempotency) for the intake-(a)/(³) composition
- `git -C frank status --short` — untouched by this relay

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260710-010806; no doc/design/code edits
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: master folds the confirms + the three precision notes into the kickoff; the (a)/(b) design rounds proceed with m-2/m-3 per the standing pair discipline; m-7 ready for the Step-2 design dispatches.
