## SITREP — m-2 step2-prep verdict: all six framings CONFIRMED (faithful to my locked lines + s5/s6 rulings); intake ack with a proposed shape per item

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step2-prep
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
IN_REPLY_TO: master/relays/step2-prep/SITREP-orchestrator-planner-20260710-010326.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-2.implementer, m-1.planner, m-6.planner, m-7.planner
SUBJECT: step2-prep — CONFIRM all six framings (no must-revise; each traced to a locked m-2 line or a prior m-2 ruling) + intake with proposed shapes; items 4/6 are one guard family, item 2 executes my s5-Q7 seam, item 5 is my §9 territory

**Verdict: CONFIRM all six** — no misrepresentation of a locked m-2 line; each framing is faithful to my c1 design, my s6 amendment, or a prior m-2 ruling. Intake acknowledged with a proposed shape per item (the declarations themselves land in the named slices). Design-of-record = `master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md` + the s6 amendment; `frank@a1bc6d4`.

### 1. Layer-activation knob (with m-7) — CONFIRM
Faithful to §5 `:89` (`layer_present:<layer>` is "courier-evaluated from which fill-layers/writers are bound at runtime") + CQ-1/AC17 (observe-owned requireds gate on `layer_present:observe`). The hazard is exact: today `DefaultLayers()` hardcodes observe OFF (a **code** default) — flipping it silently would activate the whole dormant Block A with no governance record.
**Proposed shape:** activation reads a **governed flag in m-7's pinned engine-config composite digest** (CQ-4b — append-only, restart-relevant, under the single top-level digest), NOT the `DefaultLayers()` code default. `layer_present:observe` stays the §5 step-gate atom (my half, unchanged); the **activation home is the config digest** (m-7's half). The s5 dormancy-sweep (`s5_registry_dormancy_test.go`) becomes the activation test's mirror: same fields, asserted *required/rendered* once the config flips, *absent* while it hasn't. m-2 owns the atom semantics + "activation is a governed config act, never a code default"; m-7 owns the digest home + the load-time flip. Lands with the s8 spine.

### 2. `scope_paths` (co-sign m-3) — CONFIRM; DECLARE (not inert)
Faithful — this executes my **s5-fidelity Q7 ruling** verbatim (struck-until-Step-2; Step-2 is the recorded "at Step-2" seam) + m-3 §5:98 (`diff_paths ⊆ scope_paths`, the struck clause). Now that Step-2 activates the observe layer and builds the IMPL done-predicate (s9), `scope_paths` gains a live consumer ⇒ **canonical-iff-consumed flips it from struck to declared**.
**Proposed shape:** a **plan-declared path-scope field** — `owner: agent_enum_pick` (the planner declares the allowed paths), a **list type** (path-pattern list; exact type co-signed with m-3, who owns the `⊆` check semantics), `gate_referenceable: false` (no §5 predicate keys on it — it is an **observe-layer** input to the m-3 IMPL done-predicate, not a form gate), consumers `[observe_gate]`. Declared in **s9** alongside the IMPL predicate. m-2 owns the slot; m-3 owns the `diff_paths ⊆ scope_paths` check + value semantics.

### 3. `OI-S6-BOUNCE-CLASS-UX` (mine) — CONFIRM; s9
Faithful, and mine by assignment. Verified live: `PHASE`/`CEREMONY_TIER` have **no explicit `required_when`** (they ride MIN_HEADER_FIELDS) — so a bounce can lack the context that makes its class legible.
**Proposed shape:** make `PHASE` + `CEREMONY_TIER` **typed-required via the established always-required idiom** `required_when: {not: {phase_in: []}}` (the same pattern `EVIDENCE_TARGET` already uses, `:90`), so every record — and thus every bounce — carries its phase/tier context by construction (canonical-iff-consumed: the bounce-UX consumer reads them ⇒ they are required). Additive-MINOR (tightening a required-when; no new field/enum). Confirmed for **s9**; I prefer the require-both option over "fold absent-context into typed-required" (requiring them is simpler and legible from the first record). No re-route.

### 4. `OI-S6-ENVELOPE-KEY-HYGIENE` (m-1/m-2 seam) — CONFIRM; s9
Faithful — this **extends the DEF-2 / s5-M-3(h) guard** (reject lane-supplied non-empty system/computed-owned *header* fields) to the **envelope layer** (`system_only` keys: FROM/ROLE/relay_id/dispatch_id/timestamp/schema_version). Grounded in §3 (system fields have **no lane affordance**) + §4 step-4 ("the courier **also validates the pick on submit** — an out-of-scope pick is rejected **regardless of how it was authored**") + my s6 §2.3.
**Proposed shape:** the DEF-2 typed-REJECT rule applies to **envelope-layer `system_only` fields** too — a lane supplying a non-empty envelope system key is **rejected at submit**, never persisted as an inert string. **Same guard family as item 6** (header vs envelope layer). m-2 owns the validator rule + the `system_only` typing; **m-1 fidelity on the channel key** (which channel/FROM authored the submit) + the envelope-stamping. s9 (the next m-1/m-2 seam touch). No re-route.

### 5. `8a` parked-gate-across-schema-bump (with m-6) — CONFIRM; design before s10
Faithful — squarely my **§9** (records immutable; migration applied at **read/projection time** by the version→version migrator registry; "**Parked lanes wake to the current form**") + m-6 park/wake. Sharpest inherited carry because Step-2 parks lanes for real.
**Proposed shape — the migrate-then-validate-at-wake contract:** when a parked lane wakes across a `schema_version` bump, the parked record is **migrated to current** (my §9 read-time migrator registry — the seam ships with zero migrators; the first breaking bump registers the first) **then re-validated against the current form** at wake (the §9 "wake to the current form" line + m-6 re-observe-on-wake). If no migrator spans the version gap, the gate is **explicitly dispositioned** (`held`/escalated), **never silently dropped** (matches the kickoff exit-gate 8a leg). m-2 owns the migration/`schema_version`-comparison contract; m-6 owns the wake FSM. Design before **s10**.

### 6. s5-b `(h)` suppliability guard — CONFIRM; the rule's shape is mine; s8
Faithful — this is **my DEF-2 / s5-M-3(h)** guard, the condition that makes the **non-lane-writability** claim honest. Its **shape is mine (the validator surface):** a **channel-keyed typed-REJECT** rule rejecting any lane-supplied non-empty `system`/`computed`-owned field, so the observed-evidence fields (Block A) **cannot be lane-supplied** — the "observed, not supplied" claim is honest from the **first** observed relay (correctly in **s8**, the observe spine). **m-1 fidelity on the channel key** (the channel-stamped FROM that distinguishes lane vs conductor authorship). This + item 4 are the header-layer and envelope-layer faces of one guard; I'll design them as one rule over both layers.

---

**Net:** 6/6 confirmed, 0 must-revise; intake acknowledged. Cross-cuts I'm flagging for your decomposition: **items 4 + 6 are one guard family** (the DEF-2 typed-REJECT over header + envelope `system_only`/`computed` fields — I'll spec it once, applied at both layers, s8 for the observe fields / s9 for the envelope-hygiene extension); **item 2 co-signs m-3** (I own the slot, m-3 the `⊆` check); **items 1 + 5 seam to m-7 (config digest) and m-6 (wake FSM)** respectively. None reopens the c1 lock — items 1/2/3/4/6 are additive-MINOR registry/validator work; item 5 is the §9 migration seam already shipped (zero migrators) being exercised. The `Q6×Q4 record_kind ODB-widening MAJOR/MINOR` reconcile (kickoff design-item 4 / s10) is **master's** — my `:126` line stands as I gave it at s5 (system-enum exhaustive-switch widening = MAJOR); I'll confirm on the reconcile when routed.

Blocked: no — verdict + intake delivered; the declarations land in s8/s9/s10 per the schedule.
Scope drift risk: none — confirmed framings against locked lines; proposed shapes are additive/seam work, no c1 reopen, no mechanism change.

Tests / verification: verified `PHASE`/`CEREMONY_TIER` unrequired + the `EVIDENCE_TARGET` always-required idiom (`frank@a1bc6d4` `registry.json`); framings cross-checked against §3/§4-step-4/§5(:89/:95-97)/§9/AC17, my s5-fidelity Q7 + s5-escalations M-3(h), my s6 amendment §2.3, and m-3 §5:98.

ACTIONS_GIT_REF: wrote this verdict/intake relay + appended `master/relays/INDEX.md`; read-only `frank/` probe; no `frank/` edit, no c1 edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, read-only @ a1bc6d4)
Next requested action: master folds the m-2 intake into the step2-plan decomposition (items 4+6 as one guard family; item 2 m-3 co-sign; items 1/5 seams); the declarations land in s8 (activation knob + (h) guard) / s9 (scope_paths + bounce-UX + envelope-hygiene) / s10 (8a). m-2.planner holds for the slice dispatches.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
