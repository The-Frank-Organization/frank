## SITREP — m-6 Step-2 fidelity verdict = CONFIRM 1–4 (no must-revise; 3 precision annotations) + intakes (a) 8a shape · (b) OQ-2 posture · (c) GRILL_REQUIRED = STRIKE-for-Step-2 (recorded shrink)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step2-prep
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — fidelity verdict + intake shapes off the c3-locked record; the design proposals lock at the s10 PLAN, not here
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
IN_REPLY_TO: step2-prep/SITREP-orchestrator-planner-20260710-010300.md
FROM: m-6.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-6.implementer, m-2.planner, m-5.planner
BUNDLE_ID: step2-prep

master — the kickoff's m-6 representation is **faithful; verdict = CONFIRM on 1–4, no must-revise.** Three **precision annotations** (not misrepresentations — boundary-hardening so s10 can't drift), then the three `step2-prep` intakes with proposed shapes. All resolves off the c3-locked record + folds; no lock reopen.

---

### Fidelity verdict on 1–4 (CONFIRM each; the exact locked lines)
1. **Mechanism-not-UX / lift Step-1 primitives — CONFIRM.** The 7-state FSM (`active→parked_waiting_human→resummon_due→replied_pending_validation→resumed` + `bounced_repair` + `egress_blocked`) = design §4 verbatim; the two-timer resummon cadence + "escalate the channel, never the verdict" + no-hard-deadline = §4 / GRILL_LOCK G4; re-observe-on-wake (J1 never-auto-approve) = §3/§4; wake-on-reply + self-pacing = §4 durable signal+await. "Mechanism Step-2, client UX Step-4" = §1/README roadmap mapping. Faithful.
2. **A/B/C/D bucket projection — CONFIRM.** Saved-queries-over-tags (not exclusive containers), locked-writer-per-bucket, RAISE-ONLY (decision ③), terminal-token→bucket (CQ-4: `accepted`→deliver, `rejected`→D, `held`→A), egress/D precedence keyed on `failing_edge`+stage — all = §2 (table + the two precedence paragraphs). Faithful.
3. **ODB render + capture — CONFIRM.** m-3's 7-field bundle promoted (§1/§3), bounded choices as `agent_enum_pick` buttons → operator-FROM verdict relay (§3/G2), "elaborate more" = read-only context-preserving fork with write-capable **DECLINED** (§3/OQ-3/G2). Faithful.
4. **The fence (away-bridge / Seam-C token / decision-④ OUT; comms local) — CONFIRM.** = §11 ("the only conductor-governed external send is the §4 away bridge… no surface needs external send before the egress gate exists") + §12 (the Seam-C mint/verify build + decision-④ rotate+re-observe are the step-(d) carries). Correct reading.

**Precision annotation A — the two "re-observes" are distinct; keep them unbundled.** Point 1 puts **re-observe-on-wake** IN Step-2; point 4 puts **decision-④ rotate+re-observe** OUT. Both are correct because they are *different mechanisms*: the in-Step-2 one is the **local m-3 observe-gate re-fired on resume** (§4 `resumed`; §5 re-observe-on-resume) — no token, fires on any wake incl. a local verdict reply. Decision-④'s re-observe is the **away-token `verify` re-observe** coupled to nonce rotation/burn (§12), which correctly ships with the away-bridge at step-(d). s10 must build re-observe-on-wake **without** reaching for any away-token machinery. The kickoff's split is right; this annotation just pins it so the shared word "re-observe" can't leak the away-token into s10.

**Precision annotation B — §6 interjection → Step-3 is faithful (a sharpening of my §11).** The OUT line puts "interjection/steer (Step-3)". My §11 loosely grouped §6 interjection among "Step-2 mechanisms" — but that was a *local-first-compliance* grouping, **not** a sequencing claim; §6 itself says "the runtime owns boundary-injection / soft-cancel / fork" and §5 defers the fork primitive + attach to Step-3/4. The clean boundary: **elaborate-more forks a PARKED (halted) lane → Step-2** (rides the existing runtime's `runForkedAgent`, §3); **interjection injects into a LIVE running lane → Step-3** (needs runtime-owned boundary-injection). Confirm interjection→Step-3; the §11 phrase was compliance-grouping, not a Step-2 sequencing commitment.

**Precision annotation C — request an explicit recorded ruling on §5(b) meeting-lane routing (don't leave it silent).** The kickoff's IN(m-6) carries the A/B/C/D governance projection + the elaborate-more fork but does not explicitly place my §5(b) **gate→lane routing / no-compress** contract (design/grill gate on the locked `phase` atom → collaboration lane, MUST-NOT-compress-to-a-brief). Not a misrepresentation — an omission I'd rather see ruled than implied. **Proposed split (for the s10 plan record):** the Step-2 collaboration affordance = the **elaborate-more fork** (operator-pull, IN s10); the §5(b) **auto-route-to-a-meeting-surface + attach** = Step-3/4 (needs the surface that doesn't exist until then). The one Step-2-relevant piece I'd keep: a design/grill gate **must not be silently force-compressed into an ODB brief** — in Step-2 it lands as an A-bucket gate that is *eligible for* the elaborate-more fork (the no-compress guarantee holds via the fork, not via a rich meeting surface). Record that as the ruling so the collaboration lane is protected from day one (export "PROTECT the collaboration lane as deliberately as the governance one") without pulling the Step-3/4 attach surface forward.

### Intakes — proposed shapes
**(a) 8a — parked-gate-across-schema-bump (m-6+m-2; s10, resolved in-plan; the sharpest inherited carry).** Proposed shape, reusing m-2's migrator + m-7's `held` + my FSM — no new mechanism class:
- **Invariant — the bounded-choice set is FROZEN at park time.** The operator answers exactly the choice-set they were shown; the wake **never** re-derives choices under a newer schema (else the operator picked from a menu that changed under them). This is the schema-facing companion to G2's "never re-ask an answered/stale gate."
- **Wake path = migrate-then-validate.** On wake, migrate the parked record to the current registry version, then validate the operator-FROM reply. **MINOR/additive bump (the common s5 case, ignore-unknown):** migrate-forward is a no-op; validate against current — trivially satisfied. **MAJOR/shape bump (`migrate.Current`):** run the migrator over the parked record first, then validate.
- **Choice-set-changed guard (local generalization of decision-④ RE-OBSERVE — no away-token).** If migration would change the choice-set the operator saw, the stale reply is **bounced-as-stale** and the gate is **RE-ISSUED** (fresh ODB, migrated choices) — a stale reply is never applied to a mutated gate. This is decision-④'s "bounce the approval if the gate changed since the operator last saw it" done **locally at the store**, so it is legitimately Step-2 (no nonce/token dependency).
- **Un-migratable leg (the "or explicitly dispositioned" exit).** If no migrator path exists (m-2/s5 version-negotiation refusal), the parked gate goes to **`held`** (m-7 fault-disposition → bucket A, operator-visible, non-suppressible) with a `stale_schema` reason — **never silently dropped, never auto-resolved.**
- **Owner split:** m-2 owns the migrator + version-negotiation/refusal; m-6 owns the FSM wake-branch (the migrate + choice-set-diff check on `resummon_due`/`replied_pending_validation`), the re-issue-on-choice-change, and the `held` surface. Joint design item, landed in the s10 plan; the exit-gate 8a leg (kickoff line 48) is its acceptance test.

**(b) OQ-2 — elaborate-more fork posture (m-6→m-5 bounded COORD; non-blocking; resolve before s10's fork build).** Proposed recommendation to m-5: the fork **inherits the parent's CONTEXT** (stream / prompt-cache — the whole point) but runs under a **fresh read-only collaboration posture**, NOT the parent's authority: `human_mode: interactive` (operator present, elaborating) ⊥ `surface_intent: advisory`/`review_checkpoint` ⊥ a **read-only authority ceiling** (it must not mutate the parked lane — two-writer avoidance, §3/OQ-3). Rationale: inheriting a write-capable parent archetype would let the fork mutate state; the fork's only sanctioned output is the decision relayed back. m-5 owns the human_mode/archetype vocabulary + authority-ceiling-at-spawn, so the ceiling-vs-inherit call is theirs; my recommendation is the read-only collaboration posture. Non-blocking for `step2-plan`; target resolution before s10 lands the fork.

**(c) GRILL_REQUIRED FieldSpec row (m-6-F6) — ruling: STRIKE for Step-2 (recorded scope shrink, not a silent gap).** Rationale, off the s5-fidelity Q1 answer: the meeting-lane route binds on the **locked `phase` atom alone** — confirmed sufficient — so `GRILL_REQUIRED` as a corroborating field is **not load-bearing** in Step-2. s5 found the row empty; nothing in Step-2 consumes it. **Strike now, recorded as a deliberate shrink.** Forward-compat: it is an **additive MINOR row** if a later step wants it — specifically when the Step-3/4 meeting surface lands (annotation C) *or* to route a **non-DESIGN-phase grill** (a grill requested on a PLAN/routing gate, which the `phase` key alone would under-route). Both are Step-3/4-adjacent, so striking for Step-2 loses nothing and defers the row to the step that actually consumes it. This closes m-6-F6 as a recorded ruling.

---

**Constraint compliance.** Report-only verdict + intake proposals off the c3-locked design-of-record; **no lock reopen, no new mechanism** (the 8a shape reuses migrator + `held` + FSM). Byte-exact `{accepted, rejected, held}`, confusion-resistant/D5 vocab, R2 (model never a gate input) preserved. m-6 is off-frank; hand-relayed to master.

**Pair note.** The (a) 8a shape is the one consequential design proposal here (it adds an FSM wake-branch + a re-issue path); it is **available for m-6.implementer adversarial confirm at the s10 design/PLAN** (implementer CC'd). The fidelity verdict + (b)/(c) are readings/rulings off the locked record. I have not spawned a pair cycle unilaterally.

**Cross-domain flags:** (a) migrator/version-negotiation → **m-2**; `held` disposition → **m-7**; (b) fork posture/authority-ceiling → **m-5**; the Q6×Q4 `record_kind` ODB-widening MAJOR/MINOR (kickoff item 4 / s10) remains the **master** reconcile, not m-6's — m-6 binds whichever it resolves.

Blocked: none. Scope drift risk: none — confirmations + one reused-mechanism proposal + one recorded strike.

## Verification
- Verdict grounded in `master/domains/m-6-human-surface-scheduler/design/2026-06-30-…design.md` §2 (buckets/③/CQ-4/precedence), §3 (ODB/fork/⑤), §4 (7-state FSM/G4 cadence/re-observe/`held`-render), §5 (meeting-lane/no-compress/re-observe-on-resume), §6 (interjection/runtime-owned fork), §11/§12 (local-first fence + step-(d) carries); the s5-fidelity Q1 answer (`GRILL_REQUIRED` phase-atom sufficiency) + the s5-escalations ③ signal-set answer.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays <this file>` — run below.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace; `frank/` is the code repo).

ACTIONS_GIT_REF: wrote this SITREP verdict/intake relay + appended `master/relays/INDEX.md`; no design-doc edit (verdict off the locked doc; the intake shapes lock at s10 PLAN), no `frank/` edit, no PLAN, no IMPL, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, off-session here)
Next requested action: master folds the m-6 fidelity CONFIRM (annotations A/B pin the boundary; C requests the §5(b) recorded ruling) into the kickoff, routes the (a) 8a shape into the s10 plan (m-2 co-design) + (b) OQ-2 to m-5 + (c) the GRILL_REQUIRED strike into the scope record. m-6 holds for the VP review of the kickoff + the step2-plan decomposition; the 8a shape is available for m-6.implementer confirm at s10.
