## DESIGN — RE-FILE (lineage repair, Ask 1) + evidence-owner answers 2a/2b/2c. **§A re-states my `xit-dur-1` `context_digest` finding verbatim-in-substance** (original `step3-relock-lane4-esc1-m3/DESIGN-planner-20260725-195200.md` left in place as history, per Ask 1, exactly as m-9 did). **2a — I separate the PRODUCER boundary (what m-9 hashes) from the OBSERVER boundary (what MY gate captures), because the thread has been blurring them. Verified at my r24 bytes: my v2 gate actively re-derives from the wire exactly ONE digest (`frozen_core_digest`, predicate-1); `logical_surface_digest` is CARRIED on `attempt_open`, its independent re-derivation being the `model_surface_digest` E-join ratified OUT to v3. m-9's now-live spec rides `context_digest` on `attempt_open` "exactly as `logical_surface_digest`", so the durability comparison — m-9-carried `actual` vs harness-frozen `expected` — is EXECUTABLE by my gate under EITHER producer boundary, needing NO direct capture of (A) or (B). The boundary choice governs ONLY the OPTIONAL observer-parity cross-check, which my gate can attempt solely at the wire (B) and solely if m-8's lowering is invertible for the full ordered `input[]` content — gated on `…-m8-boundary-1`. At (A) my gate has NO direct capture; but m-9's carriage means it doesn't need one.** **2b — expected IS freezable without a build, via an authored preimage pinned in m-9's PRODUCER (A) canonical form (matching the carried actual); harvest-from-build is circular — packet blocker.** **2c — the durability leg does NOT need an epoch/lease term: prefix-identity doesn't witness successor-legitimacy, but that property need not ride this leg — m-10 names a FROZEN fail-closed fencing gate (rev16 §4:55 + §6:130) that covers it independently; the only residual is m-10's own flag that a distinct exit-test term must EXERCISE that gate (lane-4's).** Nothing authored: no recipe, no boundary selected, no amendment, no fixture/manifest touched, no byte moved. r24 `651c9aec…` UNMOVED.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-m3-answer-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-m3
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — 2a reports the witness is executable by my gate as a carried comparison but that the OPTIONAL observer-parity independence inherits an unresolved m-8 lowering-invertibility dependency; 2b reports an authored-vs-harvested fixture fork with a circularity blocker + new fixture members to pin; 2c reports the property is covered by an m-10 frozen gate that is not yet an exercised exit term. Each resolution touches a ratified §7 field, the exit fixture, or the exit-test suite ⇒ Master+VP+operator, never a lane-4 or in-thread fix. This relay REPORTS; it authors nothing.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-m3-refile-2/DESIGN-orchestrator-planner-20260725-204829.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-9.planner, m-10.planner, m-8.planner, l4.planner, l4.implementer
SUBJECT: (§A) `context_digest` finding re-filed under a fresh unique id, parented to `step3-relock-lane4-esc1-m3`; (2a) PRODUCER vs OBSERVER boundary separated — my v2 gate actively re-derives only `frozen_core_digest` from the wire, carries `logical_surface_digest` (E-join is v3); m-9 rides `context_digest` on `attempt_open` too, so the durability comparison (carried actual vs frozen expected) is executable under EITHER boundary with NO direct capture of (A)/(B); the boundary choice governs only the OPTIONAL observer-parity check, realizable solely at wire (B) and solely on m-8's full-content lowering-invertibility answer; at (A) my gate has no direct capture and m-9's carriage means it needs none; (2b) expected freezable WITHOUT a build via an authored preimage pinned in m-9's producer (A) canonical form (matching the carried actual) — harvest-from-build is circular (packet blocker); new fixture members to pin named; (2c) durability leg needs NO epoch/lease term — successor-legitimacy is covered by m-10's frozen fail-closed gate (rev16 §4:55 + §6:130), independent of the leg; residual = a distinct exit-test term must EXERCISE that gate (lane-4's), per m-10's own flag

master — answered against my own frozen r24 bytes (`domains/m-3-observation-evidence/design/2026-07-22-stage6-lane2-e0-e3-delta.md` @ `651c9aecd1dd27b61d1a5fd3e802b05ee350e1acb848b827ee57b27751eccb97`, the lock constituent, UNMOVED — I read it, changed nothing). I verified every external anchor at its bytes (m-8 §1.1; m-9 answer-1 + the now-live m-9-spec-1, which I am CC'd on; m-10 answer-1) and inherited no claim I did not re-check. Your Q1/Q2 answer stands and is not reopened; nothing below re-argues necessity.

## §A — the `context_digest` finding, re-filed (Ask 1)

Re-stated in substance from `step3-relock-lane4-esc1-m3/DESIGN-planner-20260725-195200.md` (left in place as history), now uniquely addressable:

- **Q2 — what the Durability POSITIVE leg stops proving if `context_digest` is struck:** with only `{predecessor_turn_id, resumed_round_index, log_prefix_digest}`, the leg proves the resume reproduced the same durable **prefix identity** at the same round. It does **not** prove the model was presented the **same assembled context**. `logical_surface_digest` cannot cover the gap: it binds the five-member pre-lowering **surface** and **deliberately EXCLUDES the assembled `input[]` items** (my r24 §4:256 — "a Gate-2 reassembly touches assembled `input[]` content, which is **not** a member"; m-9 §6:389). So a resume that reconstructs a correct-looking prefix while assembling a **different** model-visible `input[]` would score **PASS** — the exact case §7's Durability row forbids ("a degraded re-derivation never satisfies `xit-dur-1`", `STEP-3-STAGE6-AMENDMENT.md:371`). `context_digest` is therefore **LOAD-BEARING**, not vestigial: its absence admits a false PASS on the leg's headline property. This is what settled the requirement, and I do not reopen it.

## 2a — which observation boundary my E3 evaluator can capture + compare

**The distinction the thread keeps collapsing, and the one that answers your question: PRODUCER boundary ≠ OBSERVER boundary.** The producer boundary is *what m-9 hashes* to produce `context_digest`; that is m-9's choice, and m-9 has now chosen (A) (`…-m9-spec-1`, which I am CC'd on and verified). The observer boundary is *what MY gate can capture and compare*. Your question is about the second, and it has a different answer depending on whether the witness is **carried** or **observer-re-derived** — so I answer both, grounded in how my v2 gate actually works.

**How my v2 gate actually handles carried digests — verified at r24:** exactly **one** digest is actively re-derived by the observer from the captured wire and compared in v2 — `frozen_core_digest` (predicate-1, §2.1:105 "the observer's **independent** derivation over its captured wire request"; F84 rail §2:113 "a fresh derivation from captured bytes"). By contrast `logical_surface_digest` is **carried** on `attempt_open` in v2; its independent observer re-derivation + comparison is the `model_surface_digest` **E-join, ratified OUT of v2 to a future v3 delta** (§6:274 "Not designed, not referenced as pending-here"). So my v2 gate today *carries* the surface digest — it does not yet independently re-derive it.

**m-9's now-live spec rides `context_digest` the same way** — "`context_digest` should **ride `attempt_open`** exactly as `logical_surface_digest` does … the observer reads it from the **same carrier** — no need to capture the app-internal object directly" (`…-m9-spec-1`:34), and it explicitly assigns "which surface the E3 observer can capture" to me (:68). Given that, my evidence-owner answer:

1. **The durability comparison — m-9-carried `actual` vs harness-frozen `expected` — is EXECUTABLE by the gate I bound at r24, under EITHER producer boundary, needing NO direct capture of (A) or (B).** It needs only the value carried on `attempt_open` (present-never-absent, like `logical_surface_digest`) plus the frozen expected preimage (2b). Its independence comes from the **frozen expected**, not from re-derivation: a runtime assembly/replay divergence makes m-9's carried digest differ from the frozen expected ⇒ **mismatch, caught** (m-9 condition (iii)/G4). This is the identical shape as the frozen-expected legs my gate already runs. **So there is no blocker to the witness itself from my side, and my earlier "digest the boundary directly" framing was over-strong.**

2. **My gate's OBSERVER boundary is the WIRE (B) only — it has NO direct capture of the app-internal (A).** `evidence_locator` (§2:102) is "a reference into the observer's recorder boundary"; that recorder sits at **egress** (the wire). Per m-8 §1.1 (`2026-07-17-mvp-provider-contract.md:29`) `m8.llm_request.v1` is "the app-internal request (**NEVER a wire object**)" — outside my recorder. So m-9's producer boundary (A) is one my observer **cannot see directly**; m-9's carriage design is precisely what makes that not matter for the leg.

3. **The boundary choice ((A) vs (B)) governs ONLY the OPTIONAL observer-parity cross-check** — the analogue of predicate-1's F84 re-derivation / the deferred v3 E-join: would the observer *additionally* reconstruct the digest from what it captured and confirm m-9's carried value is honest (catching a producer that hashes X but assembles Y)? That is the **only** part that needs an observation boundary, and:
   - It is realizable **solely at the wire (B)** — my gate captures nothing else — by reconstructing the assembled `input[]` from the wire, i.e. **inverting m-8's lowering**.
   - It is a **strictly heavier demand** than anything r24 exercises: `logical_surface_digest` touches `input[]` only for a **versioned-sentinel byte-prefix scan** (§4:256), reading a prefix marker, never the content; the parity check needs the **full ordered content** to lower deterministically **and be invertible/canonical** from the wire. That is exactly the question you routed to m-8 (`…-m8-boundary-1`), and m-8's answer-1 already flags the direction is lowering-dependent (it notes (B) expected "can't be derived from frozen app-internal bytes without reproducing my lowering"). So **observer-parity independence is available iff m-8 confirms full-content lowering-invertibility; if not, the leg still stands on carried-actual-vs-frozen-expected, minus that bonus cross-check.**

**Decisive summary for the packet:** the context witness is executable by the gate I bound at r24 **as a carried-value-vs-frozen-expected comparison, under either producer boundary** — it does **not** require my gate to observe (A) or (B). What the boundary choice decides is only whether the **optional** observer-parity independence is available, and that rides entirely on m-8's `…-m8-boundary-1` full-content lowering-invertibility answer (available only at wire (B); impossible at (A), which my gate cannot capture). No blocker from my side; the open dependency is m-8's answer **iff** parity independence is wanted, plus the frozen expected preimage (2b).

## 2b — can the EXPECTED side be frozen without running the build?

**Yes — via an authored preimage; the harvest-from-build path is circular and I flag it as a packet blocker.**

- **Realizable path (authored preimage):** the fixture pins the **exact expected ordered assembled `input[]` vector** for the resumed turn as an **authored fixture input** (the scripted crash-resume scenario's known continuation), plus the frozen canonicalization recipe; the harness derives `expected` by running that recipe over the frozen vector; `actual` is the **carried** `context_digest` m-9 computed over what it assembled at runtime. Both are closed functions of frozen bytes (harness side) / of the run (producer side) — non-circular. This is the same shape as every frozen-expected leg I already rely on, so it is realizable in my evaluator.
- **Canonical-form note, corrected for the carriage design:** because `actual` is m-9's **producer-computed** digest (boundary (A)), the expected preimage must be pinned in **m-9's producer canonical form** — the closed-union ordered item vector and JCS recipe m-9 specifies at `…-m9-spec-1` G2 — **not** a wire-lowered form. (The wire-lowered/canonical-form question only re-enters if the *optional* observer-parity check of 2a is pursued, since that one derives from the wire.) I name this so the fixture author pins the right domain.
- **What the fixture must pin that it does NOT pin today:** (i) the **expected ordered assembled `input[]` vector** for the resumed turn — a NEW fixture member; my r24 has **no context member at all**, deliberately; (ii) the **JCS/canonicalization recipe** over m-9's closed-union item schema; (iii) the **attempt selector** (m-9 G3: first resumed-turn attempt with empty compaction_template) so `actual` is drawn from a well-defined attempt. None is in `STEP-3-EXIT-FIXTURES.json` today.
- **The blocker, stated for the operator packet:** sound ONLY if `expected` is **authored by construction**. If instead `expected` is obtained by **running the build and recording the assembled `input[]` it emits**, the leg is **circular** — expected becomes "whatever this build produces," so any build passes against its own output. State it at packet time, not at T4. Note this **sidesteps** the Q1 assembly-determinism fork: on the authored path the harness never re-runs assembly, so the leg does not depend on assembly being harness-replicable.

## 2c — does the Durability positive leg need an epoch/lease evidence term?

**No — the leg needs no such term, provided a distinct exit-test term exercises m-10's frozen fencing gate.** Applying the exact standard I applied to `context_digest` (does absence admit a false PASS on the leg's headline property):

- **Prefix-identity and successor-legitimacy are DIFFERENT properties, on DIFFERENT axes.** `{predecessor_turn_id, resumed_round_index, log_prefix_digest}` proves **what prefix was reproduced**; it does not prove the resumer was the **single legitimate successor at the correct `turn_epoch` under the correct lease**. (`context_digest` is a third, independent axis — *what the model sees*; none of the three substitutes for another.)
- **I can name NO predicate of MINE that covers successor-legitimacy** (predicate-1 frozen-core; the five typed E3 machines; §2.4 stamped-`FROM`; §2.5 non-gating egress — none witnesses `turn_epoch`/lease admission; the cut-matrix row-3 `STALE_EPOCH`/`EPOCH_AHEAD` fencing, §3:88, fences a stale *attempt* at DATA-P, a different object from admitting the successor *turn* on resume).
- **But it need not ride the durability leg at all.** m-10's answer-1 names a **frozen, fail-closed** proof of successor-legitimacy independent of the resumed context: rev16 §4:55 (atomic lease-at-current-epoch admission) + §6:130 (the two-form assign gate keyed by `{run_id, generation_id, turn_epoch, state_seq}`, exactly-once, stale-reject via `state_seq`) — verified in m-10's citation. So the property **is** covered by a frozen predicate; just **m-10's, not mine, and off the durability leg.** From the evidence side that is the right factoring: successor-legitimacy is a fencing invariant, not a resumed-context member, and folding it into `xit-dur-1` would conflate two axes.
- **The one residual, which is exactly m-10's own honest flag:** a frozen invariant in a design is not automatically an **exercised exit-test term** (m-10 answer-1 :51). So the successor-legitimacy property is *designed-covered* but only *exit-covered* if a **distinct fencing `xit-*` term** actually exercises rev16 §4:55/§6:130 in the suite. That confirmation is **lane-4's**, not the durability leg's and not mine to author. My decisive evidence answer: **`xit-dur-1` does not need an epoch/lease term; do not add one to the durability leg. Ensure instead that a distinct exit-test term exercises m-10's frozen fencing gate — else successor-legitimacy is designed but unwitnessed at exit.**

## What I did NOT do
No recipe authored (`context_digest`, the input-item digest, the parity check, or otherwise); no producer or observer boundary selected; no amendment; no fixture or manifest touched; no predicate rewritten; no digest computed; no byte moved. My r24 `651c9aec…` stays UNMOVED. My `env_digest` preimage-parity and r7-mirror caveats stay carried and unreopened. Boundary selection, recipe authoring, and amendment remain Master+VP+operator; the fencing-term exercise is lane-4's to confirm.

## Boundaries
This relay ratifies nothing, selects no boundary, decides no scope, authors no recipe or amendment, changes no fixture or manifest, moves no locked byte, resumes no held member, issues no PLAN/T4 token, touches no `frank/` path, and permits no external use. Interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, m-3 r24 `651c9aec…`, m-9 delta `01b885fe…`, m-8 contract `4b670a79…`, m-10 rev16 `3e3c5192…` all UNMOVED. **H-12 hard-blocks external use.** Lane 4 stays held on `xit-dur-1`.

## Verification
- My capture boundary + carriage-vs-re-derivation behaviour re-read at my r24 bytes `651c9aec…`: predicate-1 independent wire re-derivation §2.1:105; F84 rail §2:113; `evidence_locator` = "reference into the observer's recorder boundary" §2:102; `logical_surface_digest` observer-executable-from-request-bytes R1_RECIPE :65 and §4:256 (sentinel byte-prefix scan; assembled `input[]` content "is **not** a member"); the `model_surface_digest` E-join "ratified OUT of v2 … Not designed" §6:274.
- m-8 §1.1 "the app-internal request (NEVER a wire object, NEVER a conductor payload)" verbatim at `domains/m-8-provider-adapters/design/2026-07-17-mvp-provider-contract.md:29`.
- m-9's carriage design + parity dependency read at `step3-relock-lane4-esc1-m9-spec-1/DESIGN-planner-20260725-211230.md:34` (rides `attempt_open`, "no need to capture the app-internal object directly") and :68 (parity = m-8/m-3 domain; direct capture not required); boundary (A) chosen at :26.
- m-10's frozen fencing gate + exercise-flag read at `step3-relock-lane4-esc1-m10-answer-1/SITREP-planner-20260725-211200.md:46-49` (rev16 §4:55 + §6:130) and :51 (a frozen invariant is not automatically an exercised exit term — lane-4's).
- §7 Durability row + "degraded re-derivation never satisfies `xit-dur-1`" at `STEP-3-STAGE6-AMENDMENT.md:371`; `resume_prefix_expectation` members at :383.
- Original finding preserved at `step3-relock-lane4-esc1-m3/DESIGN-planner-20260725-195200.md` (unmoved, now history). Lineage rule `CYCLE-PLAYBOOK.md:139-164`.
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this answer relay + one INDEX.md row + one README pointer line. No design byte moved, no recipe authored, no boundary selected, no fixture/manifest/lock/frozen byte moved, no history mutated, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master folds §A/2a/2b/2c into the operator packet, exact-hash-binding all three unique owner returns (m-9 answer-1/spec-1, m-10 answer-1, this m-3 answer-1). Decision-relevant summary from my side: the witness is executable by my gate as a carried-actual-vs-frozen-expected comparison under either producer boundary (no blocker from me); the boundary choice governs only optional observer-parity, which rides on m-8's full-content lowering-invertibility answer; the fixture needs a new authored `input[]` preimage in m-9's producer canonical form (harvest-from-build is circular); and `xit-dur-1` needs no epoch/lease term provided a distinct exit term exercises m-10's frozen fencing gate. Boundary selection, the m-8 answer, the fixture decision, and the fencing-term exercise remain Master+VP+operator / lane-4. m-3.planner holds. r24 `651c9aec…` UNMOVED. H-12 stands.
