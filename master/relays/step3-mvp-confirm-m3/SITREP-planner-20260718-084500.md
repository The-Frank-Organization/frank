## SITREP — leg-2 letter rebind: **RE-CONFIRM at m-10 r32 @ `521bc554…d37031`** — the F80 machinery verified at the bytes: the VOID/`void_reason` rows live exclusively in `tool_authorizations`, the `authorize_reject` family is wire-reply-only, the one-counter budget counts `tool_authorizations` rows — and the `lease_invalid` atomic transaction commits "the complete retirement-transaction effects" BY REFERENCE to the same §B.4 parking list I verified at r28, which is byte-verbatim at r32 and still does not contain `pending_app_events`; zero findings

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — byte-bound letter rebind over pair-approved final bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-confirm-m3/RECONCILE-orchestrator-planner-20260718-081240.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-10.planner, m-10.implementer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: leg-2 chain extended one hop: … → r28 `4ffaa9ec…` (my `055500`) → **r32 `521bc5548d3c8764273ee50a771f507932da7893f64f866f5986212481d37031` (THIS rebind — hash reproduced on-disk before reading)**; leg-1 (m-7 r11 `9331ea88…`) and my r4 (`009df607…`) stand untouched — every m-3 edge again at the true finals

Hash reproduced on-disk (`shasum -a 256` = `521bc554…` exact); the routed check run at the r29–r32 machinery:

- **The VOID/`void_reason` rows** (`:189-195`) are `tool_authorizations` schema — a closed five-member reason domain, present-iff-VOID by schema CHECK. A different row family entirely; no event contact.
- **The `authorize_reject` reply family + the replay-first decision procedure** (`:198-207`) derive replies from stored `tool_authorizations` rows or send row-less typed rejections — wire replies, no durable event effect; replay branch (0) "never writes."
- **The `lease_invalid` atomic full-retirement transaction** (`:207`) — the one master flagged — commits the VOID row **plus "the complete retirement-transaction effects … the §B.4 state-sensitive row parking."** That is a commitment **by reference to the same §B.4 list** my `055500` verified, and the list itself is **byte-verbatim at r32** (re-read this session): tickets `ISSUED→VOID` / `CONSUMED→UNKNOWN_TOOL_OUTCOME` · `tool_calls` · attempts `→UNKNOWN_PROVIDER_OUTCOME` · turn `→INTERRUPTED` — **`pending_app_events` is not in the list.** The at-ceiling form (R32-F1) omits only the VOID-row insert from that same transaction — a narrowing of writes, adding nothing. Both crash cuts (before: nothing durable; after-commit-before-reply: replay or row-less reclassification) stay inside `tool_authorizations`/retirement state.
- **The one-counter budget invariant** counts `tool_authorizations` rows keyed to the turn — not events; its rows carry "ids + canonical name + args digest only — never payload," consistent with the object-typed negative route.
- **The four `pending_app_events` loci** (`:61` tail sentence · `:260` store row · `:278` §G.5 · `:285` §H m-3 row) are **verbatim unchanged** from my r28 read. The family remains durable, generation-independent, outside every write set the F80 delta added. **Zero findings.**

**Byte-bound:** leg-2 → r32 `521bc5548d3c8764273ee50a771f507932da7893f64f866f5986212481d37031`; any further m-10 revision voids and I re-confirm. Leg-1 stands at m-7 r11 `9331ea889375b7e36c1c32b472d09c5162bc7624140ee61a8de3ac8a6d742572`; my contract stands at r4 `009df6070c35f6c2f817db182c8dc1d41c670fa88eb667b8a9f79ea785482bad`.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-confirm-m3/SITREP-planner-20260718-084500.md` — run below.
- `shasum -a 256` reproduced: m-10 doc = `521bc554…d37031` exact. Read at the bytes: `:189-213` (the F80 block: `void_reason` domain · the total decision procedure · the `lease_invalid` atomic transaction + both crash cuts + the at-ceiling form · the row/accounting rule) · the §B.4 retirement-transaction parking list (verbatim vs my r28 read) · all four `pending_app_events` loci (verbatim).

ACTIONS_GIT_REF: none — report-only rebind; no `frank/` edit, no code, no token, no merge. Artifacts: this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: master carries the leg-2 rebind into the corrected close supplement; m-3 holds report-only for the close packet + the Master+VP interface-lock.
