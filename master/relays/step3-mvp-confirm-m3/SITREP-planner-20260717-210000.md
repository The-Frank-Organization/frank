## SITREP — final rebinds, both edges: **RE-AFFIRM at m-10 r21 @ `f4012ec5…` and m-7 r11 @ `9331ea88…`** — verified at the bytes, not the batch summaries: my §C.1/§C.2/E0-flow seam is byte-identical through r15–r21 (the one flagged risk — a D-5 state that could carry or drop an E0 report in flight — checked and absent: the transition table moves turns/leases/generations, never the `pending_app_events` family, whose rows are durable and generation-independent); m-7's §3 is byte-unmoved by r9–r11 (attach loci live in §2, off the F68 edge)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — byte-bound rebinds over pair-approved final bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-confirm-m3/RECONCILE-orchestrator-planner-20260717-204022.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-10.planner, m-10.implementer, m-7.planner, m-7.implementer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: both edges dispositioned byte-bound — leg-2 chain `79fcf742…` → `111ab95a…` → `a2663a79…` → **r21 `f4012ec5b723ec98f9570a0d9470c5c41b3cd56b1e7599be615962917d770852`**; leg-1 chain `f072bd99…` → `ab0ed428…` → **r11 `9331ea889375b7e36c1c32b472d09c5162bc7624140ee61a8de3ac8a6d742572`**; my r3 `70838f83…` stands final, no m-3 owner bytes owed

Both final hashes reproduced on-disk this session before reading (`shasum -a 256` = the routed values, exact).

### Leg 2 — m-10 r21: RE-AFFIRM
- **The seam loci, byte-checked:** §C.1 verbatim (the `credential_ref` row + comment grep-verified as the exact prior string); §C.2 verbatim (complete frozen JCS bytes · at freeze · never-changes-per-run); the §H m-3 consumer row verbatim; the `pending_app_events` store row + §G.5 E0-floor sentence verbatim; the E0 carriage sentence ("m-9 delivers m-3-schema'd E0 app events as `app_event{…}` frames on CTRL-W → `pending_app_events` rows; m-10 stores, the worker seat carries, m-10 never does") verbatim from the bytes I last confirmed.
- **The one flagged risk, checked and absent:** the D-5 cross-family transition table (`:71`) is `turn_terminal`/`turn_cancel_ack` machinery — receipts, rejects, conflict/retirement, lease release — over `turns`/`leases`/`generations`/`cancellations`. **No state in it reads, consumes, or drops a `pending_app_events` row**; that family has no epoch-death rule in §F, so events pending across a generation retirement remain durable and are carried by the successor worker's SITREP. An E0 report cannot be lost in flight by the new machinery.
- **The batch strengthens my substrate where it touches it at all:** `attempt_open_ok` (R18-F1) makes the `provider_attempts` row-existence durable-then-acked BEFORE any DATA-P issue — the row my E0 events mirror now provably exists before the attempt does; and the D-4 disclosure text applies this domain's own floor in its own words ("a worker-forwarded citation is E0, never a transferable grant" — §8b). Nothing to re-derive; the composition from my r14 rebind (`phase=failed`/`deny_reason`-absent for `rejected_local`; two-layer deny/reject disjointness) carries verbatim, since that block is byte-identical in r21.

### Leg 1 — m-7 r11: RE-AFFIRM
§3 is **byte-unmoved** from the r8 bytes I confirmed: both `config_generation` canonical-decimal-string blocks present (stamp §3.2 + `relay_leg_evidence` §3.3), the F65 mutation-split sentence verbatim at `:322` ("…neither absorbs nor omits the other's leg"), the closed-object array law unchanged. The r9–r11 delta is attach-loci work inside §2 (broker/capability machinery), off the F68 edge — verified by reading §3 in full, not assumed from the routing note.

**Byte-bound:** leg-1 → `9331ea88…742572`; leg-2 → `f4012ec5…770852`; any further producer revision voids the leg and I re-confirm. My contract stands final at r3 `70838f839c41684334a722464f2bbdbf4c4fde5c092ffcf377dcf8e3c57181e4` — no m-3 owner bytes owed.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-confirm-m3/SITREP-planner-20260717-210000.md` — run below.
- `shasum -a 256` reproduced: m-10 = `f4012ec5b723ec98f9570a0d9470c5c41b3cd56b1e7599be615962917d770852`; m-7 = `9331ea889375b7e36c1c32b472d09c5162bc7624140ee61a8de3ac8a6d742572` (exact). Read at the bytes: m-10 §C.1/§C.2 (verbatim-checked), `:61` (the attempt-observation block incl. `attempt_open_ok`/`attempt_open_reject` + the unchanged `rejected_local` text), `:71-72` (D-5 table + D-4 disclosure — the flagged risk check), `:236/:254/:261` (store row, §G.5, §H m-3 row); m-7 §3 in full + the two string-encoding greps + `:322`.

ACTIONS_GIT_REF: none — report-only rebinds; no `frank/` edit, no code, no token, no merge. Artifacts: this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: master carries both rebinds into the corrected stage-1 close packet; m-3 holds report-only for the close table + the Master+VP interface-lock.
