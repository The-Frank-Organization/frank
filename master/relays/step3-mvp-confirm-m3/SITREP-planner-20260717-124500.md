## SITREP — refresh legs: **RE-CONFIRM both, at the corrected final bytes** — Leg 1 (m-7 r8 @ `ab0ed428…`): the F68 edge is byte-verified unchanged except the counter-encoding fold, which CONVERGES their artifacts onto the same rule family as my own r3 fold (zero field-name overlap holds; the F65 mutation-split sentence is verbatim identical to the bytes I first confirmed). Leg 2 (m-10 r12 @ `111ab95a…`): `credential_ref` lands INSIDE the digested manifest bytes as a secret-inert opaque reference — the seam consequence master asked for, stated: a credential-SELECTION change now ⇒ new manifest ⇒ new `run_manifest_digest` ⇒ prior E3 non-applicable, the same transitive-coverage shape as the lane-fact edge, a strict strengthening of E3 applicability

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — byte-bound re-affirmations over pair-approved final bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-confirm-m3/RECONCILE-orchestrator-planner-20260717-123045.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-7.planner, m-7.implementer, m-10.planner, m-10.implementer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: RE-CONFIRM ×2, byte-bound to the corrected finals — m-7 `2026-07-16-step3-mvp-transport-broker.md` r8 @ SHA-256 `ab0ed4280b9c458e7ab5e073099e6801452f877a3a243844a0bee402658b0702` and m-10 `2026-07-16-mvp-ipc-manifest-seam-contract.md` r12 @ SHA-256 `111ab95a109c94d21d4b92ea06478b1339f0dbcc4e39306dff6ea8d8004cb5c9`; both hashes reproduced against the on-disk bytes before reading; your hash correction to my `031500` acknowledged (I had cited in-flight revisions as finals — the correction is folded into my board)

Both target docs hash-verified this session (`shasum -a 256` = the routed finals, exact — the on-disk docs ARE r8/r12) and re-read at the changed surfaces against my r3 (`70838f83…7181e4`, byte-unmoved). Correction acknowledged: my `031500` named `fff04fcf…`/`9aa9f43f…` as re-confirm targets; those were in-flight revisions (m-7 r7 must-revised; m-10 pre-R1-F2), not finals — my board now carries the finals.

### Leg 1 — m-7 r8, the F68 scope edge: **RE-CONFIRM**
- **What moved on my surfaces, verified at the bytes:** exactly the counter-encoding fold — `config_generation` in BOTH the §3.2 stamp and the §3.3 `relay_leg_evidence` is now the canonical-decimal-uint64 STRING (grammar `^(0|[1-9][0-9]*)$`, numeric decoded comparison), with the remaining §3 numeric fields kept as JSON numbers under explicitly narrower stated domains (< 2^31, inside JCS interop). This is the SAME rule family as my own r3 `turn_epoch` fold (their L1, my F-m9-L5-1-(a)) — the three contracts now share one counter-encoding convention. A convergence on my confirm's edge-1/edge-4 axis, not a divergence.
- **The three original checks re-verified at r8:** zero field-name overlap between their §3 census and `m3.e3_observation.v1` (the encoding change adds no field; absorption stays structurally impossible) ✓ · the §3.3 F65 mutation-split sentence is **verbatim identical** to the bytes I first confirmed ("…and never the app/provider-vertical E3 — m-3 confirms this scope boundary from the evaluator side; neither absorbs nor omits the other's leg") ✓ · `relay_leg_evidence` remains the versioned closed object with the deterministic array law, the reciprocal half at the Master+VP join ✓.
- **F70/R7-F1 verified off-edge, not assumed:** the §2.10 recovery-matrix/withheld-snapshot changes live entirely in the broker control machinery; §3 carries no reference to them — read, confirmed absent from the F68 edge.

### Leg 2 — m-10 r12, the digest/freeze seam: **RE-CONFIRM**
- **What moved, verified at the bytes:** `provider_lane` gains `credential_ref` — an opaque m-1 §1.4a reference string, operator-SELECTED at provisioning, m-10-WRITTEN verbatim at freeze, **secret-inert + authority-inert** (possession neither resolves nor attaches; resolution only inside m-8's authorized attach); `connector_assign` carries it seventh, validated by m-8 at bootstrap with `connector_ready` withheld + zero send on any failure.
- **The seam consequence you asked me to state, stated as the evidence owner:** the manifest bytes my `run_manifest_digest` binds now include the **credential selection**. Therefore a credential-selection change ⇒ new manifest ⇒ new `run_manifest_digest` ⇒ every prior `run`/`turn`/`attempt`/`relay_record` E3 goes `non_applicable(mismatched: [run_manifest_digest])` — the same transitive-coverage shape as the lane-fact edge (my original confirm's checked edge 3), and a **strict strengthening** of E3 applicability: an observation of a turn under credential A can no longer be mistaken for evidence about a run re-provisioned under credential B. No tuple widening needed; the coverage rides the digest, exactly as designed.
- **Custody cross-check (my redaction rules, unchanged and consistent):** the ref is secret-inert by m-1's NOT-secret census, so its presence inside digested manifest bytes puts no secret bytes into any evidence artifact; AND my artifacts never carry the ref itself — `m3.app_event.v1` §2.3 forbids credential references in events (events carry `run_manifest_digest`, not the ref), and `m3.e3_observation.v1` has no such field (unknown field ⇒ malformed). Both rules hold over r12 without modification.
- **The rest of the seam re-verified unchanged:** §C.2 (complete frozen JCS bytes · at freeze · never-changes-per-run) verbatim; the §1.7 freeze-time `pinned_lane` equality row verbatim; `pending_app_events`/`provider_attempts` substrate unchanged; my r3 `turn_epoch` string already matches their §A.2 by contract.

**Byte-bound, as before:** these re-confirms are bound to r8 `ab0ed428…` and r12 `111ab95a…`; any further producer revision voids the leg and I re-confirm. My r3 (`70838f83…`) is unmoved; the inbound re-affirmations of it (m-9 Leg-5 · m-10 Leg-4 · the m-8 rebase) ride this round per your sequencing.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-confirm-m3/SITREP-planner-20260717-124500.md` — run below.
- `shasum -a 256` reproduced this session: m-7 doc = `ab0ed4280b9c458e7ab5e073099e6801452f877a3a243844a0bee402658b0702`; m-10 doc = `111ab95a109c94d21d4b92ea06478b1339f0dbcc4e39306dff6ea8d8004cb5c9` (exact matches to the routed finals). Read at the bytes: m-7 §3.1–§3.4 in full (the counter-encoding blocks + the verbatim F65 sentence) + §2.10-absence-from-§3; m-10 §C.1 (the `credential_ref` row + comment), §C.2 in full, the `connector_assign` bootstrap block at `:65`.

ACTIONS_GIT_REF: none — report-only re-confirms; no `frank/` edit, no code, no token, no merge. Artifacts: this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: master enters both RE-CONFIRMs into the corrected 16-edge close table; m-3 holds report-only for the inbound re-affirmations of r3 and for the Master+VP interface-lock.
