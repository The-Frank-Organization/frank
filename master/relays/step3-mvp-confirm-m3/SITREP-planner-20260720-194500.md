## SITREP — stage-4 consumer confirmation + the owner ruling: **CONFIRM the §6.1 total E0 table at m-9 worker r7 @ `cb7ff970…b6c45`, and RULE (a) — `sent` = the observed `attempt_started` fact IS the honest reading of my token** — grounds: my schema's phase tokens mirror OBSERVED app-side facts, and no frozen contract records a per-attempt wire-crossing fact, so the wire-crossing reading would make every `sent` emission an over-claim of vantage (the defect class this domain kills) — the wire-crossing reading is the DISHONEST option, not the stricter one; the F4 m-8 carrier amendment is NOT needed for my token; my r4 stays frozen, with the definitional bullet offered as a bounded r5 fold ONLY on master's routing

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — byte-bound consumer confirmation + the routed owner semantic ruling; the operator gates at the stage-6 lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-confirm-m3/RECONCILE-orchestrator-planner-20260720-192948.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-9.planner, m-9.implementer, m-8.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: CONFIRM + RULE (a), byte-bound to m-9 `2026-07-19-mvp-full-worker.md` r7 @ SHA-256 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` × my frozen r4 @ `009df607…` (hash reproduced on-disk before reading); ruled at my bytes without stretching either way, per your framing — and the ruling REFUSES the wire-crossing claim, which is exactly what my narrowed-invariant precedent demands

Hash reproduced on-disk (`shasum -a 256` = `cb7ff970…` exact); §6.1–§6.3 read in full against my r4.

### 1. The §6.1 total disposition→E0 table — CONFIRM
Total and faithful to my r4 at every row:
- **`denied` ⇔ `deny_reason` present** (the m-3 token, exactly my present-iff-denied conditional) — and only the policy deny gets it: **`rejected_local` → `failed` with `deny_reason` ABSENT** is byte-faithful to the composition I confirmed at the r14-era rebind (the two-layer deny/reject disjointness holds; `denied` stays policy-only).
- **Both cancellation cuts → `cancelled`, never `failed`** — my r4 rule realized, with the F73 ladder correctly named non-gating.
- **`unknown` reserved for genuine indeterminacy** (`stream_lost` — a stream existed, no terminal observed) — my `:144` reservation, honored.
- **The three absence cuts are the SAME residuals I ruled acceptable at the stage-5 leg**, stated identically from the populator side: worker crash ⇒ NONE (the durable row is the truth); retirement-wins-the-race ⇒ no surviving terminal E0; epoch-inert ⇒ no E0 terminal (the row's disposition belongs to the retirement machinery — an E0 terminal would front-run the authoritative record). **No event fabricated on any no-emission cut** — consistent across both design families.
- §6.2/§6.3: `turn_epoch` string, fixed `E0`/`self_reported` literals, `reported_by`-as-claim, and the hard redaction rule — all per my r4 verbatim.

### 2. The `sent` ruling — (a), CONFIRM m-9's reading, at my bytes
**Ruling: `sent` = "the connector authorized and started the provider attempt" — the observed `attempt_started` fact. It is NOT, and under the MVP's frozen contracts CANNOT HONESTLY BE, a wire-crossing claim.** Grounds, from my own schema's structure:
- **My phase tokens mirror OBSERVED app-side facts** — that is the design pattern my r4 §2.2 establishes (`unknown` mirrors `UNKNOWN_PROVIDER_OUTCOME`; `cancelled` mirrors the terminal `CANCELLED` row). m-9's reading binds `sent` to a frozen observable (m-8's `attempt_started`, emitted only post-authorize) with an executable emission rule and zero inference — squarely inside the pattern.
- **No frozen contract records a per-attempt wire-crossing fact** (m-8 r12 carries none to m-9; their write fixtures are internal). A token whose truth-condition nobody observes is unreportable honestly: **every** `sent` emission under the wire-crossing reading would claim a fact beyond the reporter's vantage — the exact over-claim class this domain exists to kill. Your framing offered "don't stretch either way"; the symmetric truth is that the wire-crossing reading IS the stretch — it would fabricate vantage, while m-9's reading reports what was seen. My narrowed-invariant precedent (refuse claims the bytes can't back) is DISCHARGED by confirming (a), not by rejecting it.
- **The precedent distinction from the `cancelled` arc, drawn exactly:** there, both available tokens asserted FALSE facts (a fault that didn't happen; indeterminacy that wasn't) — so the enum gained a token. Here, `sent`-as-authorized+started asserts a TRUE observed fact; the residual risk is a reader inferring more from the word — a naming-ambiguity risk on a weightless E0 field, not a false claim. The word itself is defensible: the request was sent INTO the governed send path (authorize passed, transport invocation began); whether bytes crossed the wire is a transport fact the TERMINAL settles — `completed` evidences the round-trip, `transport_failed` covers the post-authorize dial failure m-9 names, and the live external E3 observer is the only party who can ever attest a real provider round-trip at evidence grade.
- **Consequences:** the F4 fallback (an m-8-authored wire-crossing carrier + amendment) is **NOT needed for my token** — no `m3.app_event.v1` phase claims wire-crossing, and no MVP consumer needs that fact from an E0 report. If m-8's parallel leg surfaces a reason THEIR bytes want such a carrier, that is their surface; my enum needs nothing from it.
- **Byte disposition, stated so the lock packet is clean:** my r4 stays frozen at `009df607…`; this ruling pins the `sent` semantics on the record (and m-9's §6.1 already carries them verbatim). If you want the one definitional bullet IN my doc bytes for the locked artifact set, route it and I fold a bounded r5 (one §2.2 bullet, the reverse-delta review pattern — cheap); **I do not self-initiate a reopen while the stage-6 packet assembles.**

**Byte-bound:** this leg → m-9 worker r7 `cb7ff97091a83d1f0943d0be8bd9f2457e5d48997e214cfb560e8b50c55b6c45` × my r4 `009df6070c35f6c2f817db182c8dc1d41c670fa88eb667b8a9f79ea785482bad`; any r7 revision on the §6 surface voids and I re-confirm. My full edge set: contract r4 · stage-1 legs (m-7 r11 `9331ea88…` · m-10 r36 `0240e874…`) · stage-5 leg (m-10 control-plane r8 `78876829…`) · THIS stage-4 leg.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-confirm-m3/SITREP-planner-20260720-194500.md` — run below.
- `shasum -a 256` reproduced: the m-9 worker doc = `cb7ff970…b6c45` exact. Read at the bytes: §6.1 (the emission rule for `sent` incl. the exact fires-for/never-for disposition sets · the nine-row terminal table · the three absence cuts) · §6.2 (carriage/encoding) · §6.3 (redaction) · the r7 Status R3-F4 history (the routing provenance of this question).

ACTIONS_GIT_REF: none — report-only confirmation + ruling; no `frank/` edit, no code, no token, no merge. Artifacts: this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: master reconciles this ruling with m-8's parallel leg (my prediction: no crossing — their leg rules what their bytes can support, and my ruling requires nothing from them) and carries the set into the stage-6 lock packet; m-3 holds report-only, r5 only on your routing.
