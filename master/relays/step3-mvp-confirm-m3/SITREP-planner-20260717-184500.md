## SITREP — leg-2 REBIND: **RE-CONFIRM at m-10's FINAL r14 @ `a2663a79…`** — verified at the bytes, not off the summary: the manifest/digest/freeze seam is byte-identical r12→r14 (§C.1 incl. `credential_ref`, §C.2 what/when/never, the §1.7 equality row); the R3-F1 `rejected_local` terminal composes EXACTLY with my schema — `phase=failed` with `deny_reason` ABSENT satisfies my present-iff-denied rule, the disjointness is both token-level AND stage-level (a pre-freeze reject never reaches my policy, so `denied` stays policy-only by construction), and UNKNOWN stays reserved for the genuinely unknown — the honest-terminal rule this domain wrote, applied by m-10

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a byte-bound rebind over pair-approved final bytes; the operator gates at the Master+VP interface-lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-confirm-m3/RECONCILE-orchestrator-planner-20260717-183030.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-10.planner, m-10.implementer, m-8.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: leg-2 hash chain stated as routed: `79fcf742…` (first confirm, voided by the L7 fold) → `111ab95a…` (refresh re-confirm, voided by r13/r14) → **`a2663a7964fb23e5c92eeb3b3ecf530b80c1b3108813a398941724fd6a25c5b7` (r14, THIS rebind — pair-approved zero-findings)**; leg-1 (m-7 r8 @ `ab0ed428…`) stands untouched; both legs now byte-bound to finals

Hash reproduced on-disk this session (`shasum -a 256` = `a2663a79…` exact) and the two routed verification points checked at the bytes:

### 1. The manifest/digest/freeze seam — UNTOUCHED r12→r14, verified not assumed
§C.1 is byte-identical to my r12 read: the full `run_manifest` schema including the `provider_lane` block (`lane_id` · `lane_catalog_digest` · `credential_ref` with the secret-inert/authority-inert comment verbatim), the closed `release_binding` XOR form, and the F65 vector comment. §C.2 is verbatim (complete frozen JCS bytes · digested at run freeze · never changes for a run). The §1.7 freeze-time `pinned_lane` equality row is verbatim. The r13/r14 delta lives where you said it does — the CTRL-C `attempt_result` disposition set (`:61`) and the `provider_attempts` store row (`:229`) — none of it inside the bytes my `run_manifest_digest`/`policy_digest`/F62 tuple bind. **Leg-2 seam re-confirm carries over to r14 unchanged.**

### 2. The `rejected_local` composition — CONFIRM, and it is this domain's own rule applied
- **Schema composition, exact:** a deterministic pre-stream local reject closes `phase=failed` with **`deny_reason` ABSENT** — my §2.2 pins `deny_reason` present **iff** `phase=denied`, so the composition is not merely compatible, it is the only encoding my schema admits for this outcome. No schema change needed on my side.
- **Disjointness is two-layer, and I state the stronger half:** token-level — m-8's `reject_reason` vocabulary is m-8-owned and disjoint from my §1.3a deny enum, and `denied(<m-3 token>)` stays policy-only in the disposition set; **stage-level — `rejected_local` is emitted on pre-FREEZE validation/translation failure, upstream of authorize, so a rejected attempt never reaches my policy at all.** A deny is a policy verdict over a frozen core; a reject is m-8 declining to produce a frozen core. The two cannot collide because they cannot co-occur on one attempt, and the r14 bytes make the resolver-never-invoked + zero-transport-counters floors explicit on the reject path too.
- **No UNKNOWN absorption — the honest-terminal rule:** the r14 bytes say it in this domain's own voice ("parking a deterministic reject as UNKNOWN would be an accounting lie — UNKNOWN/PARTIAL stay reserved for genuinely unknown outcomes"), the `provider_attempts` row gains terminal `REJECTED_LOCAL` never parked UNKNOWN, and only a genuinely missing/conflicting result or crash window parks `UNKNOWN_PROVIDER_OUTCOME`. That is exactly the §13/Option-2 honesty shape this domain has enforced since s9 — a mechanism that refuses to fabricate certainty in either direction. CONFIRM, gladly.
- **E0 substrate:** the `app_event` CTRL-W carriage → `pending_app_events` flow is unchanged; an E0 event reporting a rejected attempt carries `phase=failed`, no deny token, ids/epochs/dispositions only — inside my §2.3 redaction rule as written.

**Byte-bound:** leg 2 now binds r14 `a2663a7964fb23e5c92eeb3b3ecf530b80c1b3108813a398941724fd6a25c5b7`; any further m-10 revision voids and I re-confirm. Leg 1 stands at m-7 r8 `ab0ed4280b9c458e7ab5e073099e6801452f877a3a243844a0bee402658b0702`. My r3 (`70838f83…`) is unmoved.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-confirm-m3/SITREP-planner-20260717-184500.md` — run below.
- `shasum -a 256` reproduced: m-10 doc = `a2663a79…25c5b7` exact. Read at the bytes: §C.1 (`:129-145`, byte-identical to the r12 read incl. the `credential_ref` comment), §C.2 in full, the `:61` attempt-observation block (the disposition set + emission boundaries + the UNKNOWN reservation), the `:229` `provider_attempts` row.

ACTIONS_GIT_REF: none — report-only rebind; no `frank/` edit, no code, no token, no merge. Artifacts: this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: master enters the leg-2 rebind into the corrected close table; m-3 holds report-only for the remaining inbound re-affirmations of r3 and the Master+VP interface-lock.
