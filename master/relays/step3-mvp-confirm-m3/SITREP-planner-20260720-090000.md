## SITREP — stage-5 consumer confirmation (leg 4 of 4): **CONFIRM at m-10 stage-5 r8 @ `78876829…552baa`** — the E0-carriage realization honors my r4 exactly (validate-against-my-schema + persist-received; the applier authors, synthesizes, and submits NOTHING — provenance preserved by refusal, which is the correct refusal); ALL THREE named no-E0 residuals ruled **ACCEPTABLE AS NAMED** under my evidence model — none swallows a run-terminal fact, because in every cut the durable m-10 row + the G-2a unmissable surface record the fact and only the E0 COPY (a report, weightless when absent) is missing; ONE precision returned on the invariant's phrasing: the r8 bytes carry the NARROWED honest form, which is the right one — the routing's "every run-terminal class durably E0-visible" overstates what the bytes (correctly) promise

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m3
PARENT_DISPATCH_ID: step3-arch-packet
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a byte-bound consumer confirmation over pair-approved bytes; the operator gates at the stage-6 lock
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-mvp-design-m3-egress-e0-e3
IN_REPLY_TO: master/relays/step3-mvp-confirm-m3/RECONCILE-orchestrator-planner-20260720-070757.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-10.planner, m-10.implementer, m-9.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: CONFIRM, byte-bound to `master/domains/m-10-app-control-plane/design/2026-07-19-mvp-control-plane.md` r8 @ SHA-256 `7887682998b12a3d1a9ea6cfb682ad47c70b577eef4660e2d36f30e579552baa` (hash reproduced on-disk before reading); my r4 stays frozen at `009df607…` — the census's `m10-app-event-persist` row binds it by exact hash; m-3's edge set is now: stage-1 contract legs (m-7 r11 · m-10 r36) + THIS stage-5 realization leg

Hash reproduced on-disk (`shasum -a 256` = `78876829…` exact); §11, §11a (the census's E0 rows), and the §12 G-2a/G-4 blocks read at the bytes.

### 1. The carriage realization — CONFIRM
§11's frozen producer flow is my r4 §2 realized without drift: **m-9 authors** the `m3.app_event.v1` events (`reported_by` = the worker) and carries them; **m-10 validates against my schema at receipt and durably persists** — "authors nothing, synthesizes nothing, never submits." Rows persist durably with no silent drop of the plane's own rows; the generation-independence my prior legs verified at the contract (no epoch-death rule; retirement never touches the family) is what this design runs. The census row `m10-app-event-persist` binds `policy_owner: m-3` and `policy_artifact: m-3 r4 @ 009df607…` — my exact final hash — with `outcome_reporter: m-9`, `outcome_validator: m-10 (schema conformance only)`, `canonical_record: pending_app_events`. `phase=cancelled` is present in the terminal-visibility mapping (`cancellations → phase=cancelled`). **Provenance is preserved by the strongest available mechanism — refusal:** the design explicitly declines to have the applier synthesize a worker-attributed event for a dead worker, because that would fabricate `reported_by` — an m-10-authored byte claiming worker authorship is exactly the false-provenance my schema exists to prevent. Their naming that refusal (and routing any future synthesis want to the m-3/m-9/m-10 owners) is the correct disposition, not a gap.

### 2. The three no-E0 residuals — ALL ACCEPTABLE AS NAMED (ruled under your stated rule)
Your rule: a residual that swallows a run-terminal fact = finding; a residual on non-terminal authorship/marking = the accepted class. Ruling each:
- **Worker-crash non-emission (attempt-linked):** ACCEPTABLE. The populator died before authoring; the E0 COPY never existed — an absent claim, weightless by construction (my `055500` rule, now realized). The FACT is not swallowed: the canonical `provider_attempts` row (parked `UNKNOWN_PROVIDER_OUTCOME` or terminal) is the durable, payload-free record, and the G-2a surface makes terminal classes unmissable. Nothing my E3-side machinery needs is hidden — **my F62 evaluator consumes digests and claim contexts, never E0 events**; no E3 applicability question ever turns on an E0 copy's presence.
- **Retirement-beats-emission:** ACCEPTABLE, same structure — the durable UNKNOWN row is the honest record; the missing item is only the report.
- **Zero-attempt terminal classes (crash-loop FAILED · connector-never-ready · store-integrity boot refusal):** ACCEPTABLE, with the reason stated precisely: these have **no attempt to report** — `m3.app_event.v1` is an attempt-scoped provider report (`event_kind: provider_attempt`, `scope: attempt`), so "no E0 trace" is not a swallowed fact but a **category fact**: there is no event my schema could even express for a run that never reached an attempt. The durable `runs`/`workers` rows + the §10 loud surface are the record; T11 (Step-4) is where their governed-relay delivery lives.
- **The carriage-marking `not specified` cell:** ACCEPTABLE — your accepted class exactly (non-terminal marking/ack/dedup on the carriage leg, honestly declared `not specified` rather than invented). One consumer nuance stated for the record: with no frozen dedup rule at that leg, E0 consumers must key on event identity, never on count — which my schema already supports (ids + `event_ts`) and my evidence model already requires (a duplicated E0 report is still one E0 claim; E0 weight is zero either way).

### 3. The invariant statement — present in the r8 bytes IN ITS NARROWED FORM, and the narrowed form is the correct one
Your routing described the `045603` fold as "every run-terminal class is durably E0-visible under your existing enum." **The r8 bytes deliberately do not say that** — §11's heading says "Terminal-failure E0 visibility … NARROWED per M10-S5-R1-F2," and the narrowed statement is: attempt-linked terminal classes are E0-visible **only when the live worker emitted before retirement fencing** (`failed`/`unknown`/`cancelled` under my existing enum — no schema delta), with the no-E0 cuts named; every run-terminal class is durably **RECORDED** (the m-10 rows) and **LOUD** (G-2a's unmissable alert + non-zero exit), with the courier error relay = T11 at Step-4. The named residuals are consistent with the narrowed invariant — they ARE its named conditionals. They would contradict the strong form your routing phrased; that form was never in the bytes and **should not be** — promising E0-visibility from a dead populator is exactly the fabricated-provenance claim §1 refuses. The honest narrowed invariant + durable rows + loud surface is the right composition; I confirm it as written.

**Byte-bound:** this leg → stage-5 r8 `7887682998b12a3d1a9ea6cfb682ad47c70b577eef4660e2d36f30e579552baa`; any further stage-5 revision voids and I re-confirm. My r4 stays frozen at `009df607…`; the stage-1 legs stand (m-7 r11 `9331ea88…` · m-10 r36 `0240e874…`).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-mvp-confirm-m3/SITREP-planner-20260720-090000.md` — run below.
- `shasum -a 256` reproduced: the stage-5 doc = `78876829…552baa` exact. Read at the bytes: §11 in full (producer flow · terminal-visibility · the three residual classes) · §11a `m10-app-event-persist` + `m10-run-admission` rows · §12 G-2a (the loud split + T11 routing) + G-4 (the mailbox-checker correction — the scheduler never touches the conductor, consistent with my carrier assumptions).

ACTIONS_GIT_REF: none — report-only confirmation; no `frank/` edit, no code, no token, no merge. Artifacts: this relay + one INDEX.md row + the m-3 README pointer.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: master carries this leg-4 confirmation into the stage-6 lock packet; m-3 holds report-only.
