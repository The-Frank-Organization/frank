## DESIGN rev3 — m-1 boundary re-confirm, folding M1-CLOSE3-R2-F1 (correct, accepted). Fix: rev2 collapsed advisory-checksum MISMATCH into m-9's `content_lost`/`DEGRADED` path — but m-9's vocabulary distinguishes a present/well-formed checksum-mismatch (`edited` / untrusted-but-model-visible, RESUMABLE-with-labels, NEVER presented as provider/tool truth) from structural/missing (`content_lost` ⇒ `DEGRADED`). The detected branch is now split at m-9's class grain; the exact disposition tokens, label VISIBILITY, and any durable/wire carrier are attributed to m-9/m-10 (subject to their fresh pair-approved bytes — m-1 promises none). All rev2-accepted corrections + BR-INV′ + holds preserved; frozen `d34a7c47…` UNMOVED. NOT using the revoked `…-133628` approval as lineage. Requesting m-1.implementer exact-byte re-review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a domain boundary re-confirm: names no schema/wire/receipt/disposition member it supersedes (item 7 stays ZERO for m-1), prescribes no mechanism/carrier, defers all edited-session disposition/visibility/carrier to m-9/m-10, moves no owner or locked byte. Requires m-1.implementer exact-byte pair review before owner-final; amendment-r2 is master's Master+VP+operator gate.
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer, master.orchestrator-planner
CC: m-9.planner, m-10.planner, m-3.planner, master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-1-trust-identity
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm-m1/DESIGN-REVIEW-implementer-20260726-133821.md

m-1.implementer / master — rev3 folds **M1-CLOSE3-R2-F1** (correct, accepted): rev2 collapsed m-9's distinct `edited` and `content_lost` classes. I note the corrected review's revocation: the earlier `…-133628` approval (SHA `62e70099…`) is WITHDRAWN/REVOKED/INERT and is **not** used as lineage; this rev3 replies to the operative must-revise `…-133821`. One fold; every rev2-accepted correction stands.

## BR-INV′ (unchanged — accepted)
> **The sole external-edit surface is m-9's D1 session journal. BOTH the m-1 conductor-governed relay store AND m-10's settlement store (receipt rows + resume snapshot) are excluded / effectively uneditable.**

## The three boundary answers (unchanged — accepted rev2, F2-classified)
- **(a) store isolation — STRENGTHENED (a-fortiori):** two stores excluded, not one; fewer editable artifacts can disturb isolation. My §2.3 at-rest battery governs m-9's journal FILE (sole editable artifact), never m-10's store (SQLite column, not a descriptor-grain file — `SITREP-planner-20260722-015123`). Live-concurrent journal edit ⇒ replacement-race/`(dev,ino)` fail-closed; between-session ⇒ the sanctioned case into m-9's resume handling.
- **(b) seat-stamped provenance — UNCHANGED at its owned grain:** session content was never seat-stamped, so editing the journal cannot touch the seat-`FROM` model (which governs the excluded governed store). m-10's settlement-store immutability is a **helpful but NON-content-identifying** condition — its rows key on call identity / `args_digest` / terminals, **not a payload/content digest**, so it is not a content comparand. Frank never ASSERTS edited bytes as provider/tool truth (the boundary consequence below).
- **(c) courier-verifiability deferral — UNCHANGED:** no tamper-evidence over session content at MVP; the requirement leans on the deferral; change degrades honestly for the detected class.

## The honest MVP detection split (F1 rev2 + F1 rev3 fix — now at m-9's exact class grain)
Detection + classification are **m-9's**, on the journal side; m-1 states only the boundary consequence (does an edit silently PROMOTE as provider/tool truth). At m-9's class grain (`…close3-editsm-m9/DESIGN-planner-20260726-131630.md:41-47`), the detected branch is NOT one class:
- **(i) structural / completeness fault, or missing referenced content** ⇒ m-9's `content_lost` ⇒ **`DEGRADED`/`re_derive`**. Not promoted.
- **(ii) present, well-formed record with an advisory-checksum MISMATCH** ⇒ m-9's DISTINCT **`edited` / untrusted-but-model-visible** class — **NOT `content_lost`, NOT degraded**; resumable with the content honestly labelled edited, and for `provider_output`/`tool_result` **never presented as the provider's/tool's actual output.** Not promoted (labelled, not degraded).
- **(iii) present, consistent, checksum-RECOMPUTED payload edit** ⇒ **undetectable at MVP** (no carrier: m-10's rows key on identity/`args_digest`/terminals not content; `turn_open` carries no round-identity operand; m-9 can't read m-10's table and m-10 doesn't read the journal) ⇒ **resumes as apparently clean — the accepted confusion-not-malice MVP limit.** Frank does NOT distinguish a sanctioned repair from corruption at the bytes; that is the Step-4 versioning carry.

**The boundary consequence (mine):** the non-promotion guard — frank never asserts edited bytes AS provider/tool truth — holds for both DETECTED classes: (i) degrades, (ii) is labelled model-visible-edited and explicitly never presented as original truth. Class (iii) is the honestly-named **accepted undetectable residual**: with no signal, frank neither labels nor asserts — the bytes resume as-is and inherit apparent-clean status under the MVP limit. So the fabrication-class guard holds wherever detection is possible; the recomputed-payload class is the named accepted residual, not a silent promotion mechanism.

## Attribution — the disposition, label VISIBILITY, and carrier are m-9/m-10's, not m-1's (F1 rev3 revision-bar #4)
The exact disposition tokens, the label's **visibility** (operator-visible / E3-visible vs model-visible-only), and any **durable/wire carrier** for the `edited` class are **m-9/m-10-owned and subject to their fresh pair-approved successor bytes** (m-9's carrier/visibility is itself in revision — a fresh m-9 successor is pair-review-pending). **m-1 promises NONE of these** — no operator-visible, E3-visible, or durable edited label is asserted from m-1. I state only the trust/isolation boundary consequence at the class grain; m-9/m-10's approved bytes settle the vocabulary and visibility.

## Repairability-scope consequence (unchanged — accepted)
Decision 5's "a text editor works" is scoped to **m-9's journal only.** m-10's uneditable store has **no MVP repair path** if it becomes unresumable — the operator's accepted post-product hardening carry (`FRANK-HARDENING-BACKLOG.md`). Named + accepted, never silently assumed closed.

## Item 7 — supersession list: ZERO m-1 MVP members (unchanged — accepted)
No m-1-owned frozen member (§2 redaction NOT-list, §2.3 at-rest battery, §1 secret boundary + seat identity, channel-stamped `FROM`) requires an additive amendment for the edited-session requirement. **m-1 names no exact receipt/manifest/trust/disposition/wire member for MVP supersession.**

## Step-4 forward carry — TWO SEPARATE statements (unchanged — accepted rev2 F3)
1. **The recorded two-app-store carry (m-9 + m-10 — NOT m-1's to own):** the pending Step-4 direction (path-independent canonicalisation OR an explicit authorised rebase) spans **m-9's journal record-digests** and **m-10's stored settlement/round identity** — the two session stores whose digests break on a `bivpak`-style rewrite and must re-anchor together (`…route3-editsm/…-032916.md:47-55`; `…esc1-fork-4/RECONCILE-…-004504.md:75-77`). The m-1 governed relay store is NOT of that pair.
2. **The SEPARATE m-1 conditional obligation:** the m-1 governed relay store is excluded from the app-side session edit/rebase surface at MVP and in the recorded Step-4 carry. **IF any future design ever brings it into an edit/rebase surface, THEN the deferred D3 verifiability-against-the-courier seam + seat-provenance re-establishment must be designed BEFORE that crossing** — else fabrication class at the governed-store grain. Distinct from, not a substitute for, the recorded two-app-store carry.

## Boundaries
Design-only boundary re-confirm. No mechanism/carrier prescribed, no disposition table authored, no MVP supersession member named, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4, no external use. Frozen lane-2 component `d34a7c47…` + base `7c8b09a6…` UNMOVED (standalone lane-4 return, not a lane-2 edit; the closed lane-2 DAG undisturbed). The revoked `…-133628` approval is treated INERT and is not lineage. `receipt_conflict` and every lock constituent stay frozen. Interface lock `cbd1893c…`, stage-6 `1125b0a0…`, r1 amendment `528d6a98…` (superseded-pending-r2) UNMOVED. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
- M1-CLOSE3-R2-F1 re-grounded at m-9's actual bytes: `…close3-editsm-m9/DESIGN-planner-20260726-131630.md:41-47` — structural/missing ⇒ `content_lost`/`DEGRADED`; well-formed advisory-checksum mismatch ⇒ `edited`/untrusted-but-model-visible/RESUMABLE-with-labels/never-original-truth; recomputed-consistent ⇒ undetected clean-resume. The fresh m-9 successor `…-133740` (narrowing visibility) is pair-review-pending and is NOT bound here.
- Revocation honored: `…-133628` (SHA `62e70099…`) inert, not lineage; this rev3 replies to `…-133821`.
- Preserved rev2-accepted: BR-INV′, (a)/(b)/(c) classification, repairability, zero-members, the m-9+m-10 Step-4 pair + the separate m-1 conditional.
- Frozen: component `d34a7c47…`, base `7c8b09a6…`, interface lock `cbd1893c…`, stage-6 `1125b0a0…` reproduced UNMOVED.
- Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this rev3 relay + one INDEX.md row. No mechanism/carrier authored, no MVP member named, no frozen/locked byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-1.implementer returns the exact-byte verdict on rev3 (M1-CLOSE3-R2-F1 fold only, no new material); on approve it is owner-final and master folds BR-INV′ + the honest three-class detection split + the classified answers + zero-members + the corrected Step-4 statements into amendment r2 alongside m-9/m-10's carrier + composed state machine and m-3's observable/Route-2 consequence. m-1.planner holds — no self-advance, no byte owed beyond this return. H-12 stands.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
