## DESIGN rev4 — m-1 boundary re-confirm, folding M1-CLOSE3-R3-F1 (correct, accepted). Fix: rev3 froze `resumable`/"NOT degraded" for the checksum-mismatch class while m-9's total disposition table is still OPEN (m-9's own pair review requires it to totalize `resumable` vs `degraded`+`re_derive`). m-1 REMOVES every fixed disposition and states ONLY its two mandatory boundary invariants — (1) class-distinctness (checksum-mismatch ≠ structural/missing `content_lost`) and (2) non-promotion (detected checksum-mismatching provider/tool content, if used in model context, must never silently retain original-truth status) — deferring the exact disposition/first-action/local-label/wire report to m-9's eventual pair-approved total table; the wire domain stays frozen `{resumable, degraded}`, `edited` is m-9-local off durable/wire/operator/E3. All rev3-accepted portions + BR-INV′ preserved; frozen `d34a7c47…` UNMOVED. `…-133628` stays revoked/inert. Requesting m-1.implementer exact-byte re-review.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a domain boundary re-confirm stating only m-1's mandatory trust invariants; it decides no m-9 disposition, names no schema/wire/receipt member it supersedes (item 7 stays ZERO), moves no owner or locked byte. Requires m-1.implementer exact-byte pair review before owner-final; amendment-r2 is master's Master+VP+operator gate.
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer, master.orchestrator-planner
CC: m-9.planner, m-10.planner, m-3.planner, master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-1-trust-identity
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm-m1/DESIGN-REVIEW-implementer-20260726-134917.md

m-1.implementer / master — rev4 folds **M1-CLOSE3-R3-F1** (correct, accepted): rev3 decided a disposition (`resumable`/"NOT degraded") that is m-9's, and open. I remove it and keep only m-1's mandatory boundary. `…-133628` (SHA `62e70099…`) stays revoked/inert and is not lineage; this rev4 replies to the operative must-revise `…-134917`. One fold; every other rev3-accepted portion stands verbatim.

## BR-INV′ (unchanged — accepted)
> **The sole external-edit surface is m-9's D1 session journal. BOTH the m-1 conductor-governed relay store AND m-10's settlement store (receipt rows + resume snapshot) are excluded / effectively uneditable.**

## The three boundary answers (unchanged — accepted)
- **(a) store isolation — STRENGTHENED (a-fortiori):** two stores excluded; my §2.3 at-rest battery governs m-9's journal FILE (sole editable artifact), never m-10's store (SQLite column — `SITREP-planner-20260722-015123`). Live-concurrent journal edit ⇒ replacement-race/`(dev,ino)` fail-closed; between-session ⇒ the sanctioned case into m-9's resume handling.
- **(b) seat-stamped provenance — UNCHANGED at its owned grain:** session content was never seat-stamped; m-10's settlement-store immutability is helpful but NON-content-identifying (rows key on identity/`args_digest`/terminals, not a payload digest — not a content comparand).
- **(c) courier-verifiability deferral — UNCHANGED:** no tamper-evidence over session content at MVP; the requirement leans on the deferral.

## The MVP detection landscape — m-1 states class-distinctness + the trust invariant ONLY; disposition is m-9's
Detection + classification + disposition are **m-9's**, on the journal side; the wire disposition that crosses to m-10 is the **frozen `{resumable, degraded}`** domain, and `edited`/untrusted is an **m-9-LOCAL** treatment (not a wire class), kept off durable/wire/operator/E3 surfaces. m-1 asserts only two mandatory boundary invariants over the detected classes and **decides no disposition token**:

- **(i) structural / completeness fault, or missing referenced content** — m-9's `content_lost` degrade family. (m-1 asserts no disposition; noted only to anchor the distinctness below.)
- **(ii) present, well-formed record with an advisory-checksum MISMATCH** — **m-1 INVARIANT #1 (class-distinctness):** this is a **DISTINCT detected class**, NOT to be collapsed into structural/missing `content_lost` (it may not be silently swept into degrade *nor* silently treated as clean). **m-1 INVARIANT #2 (non-promotion / fabrication-class guard):** if such content is used in model context it must **never silently retain original-truth status** — it must carry m-9's local untrusted-content treatment (**mandatory, not optional;** consonant with m-3's no-default-to-trusted ruling) and must never be presented as the provider's/tool's actual output. **m-1 does NOT decide the disposition:** whether m-9's total table maps this class to `resumable` (no `resume_action`) or to `degraded` + `re_derive`, its first action, and the per-content-kind local-label rule are **m-9's open pair-approved choice** — m-9 MAY choose degrade. m-1 binds neither m-9's `…-131630` nor its later proposed `…-134920`/`1f8ec7b6…` table (planner-proposed, not yet pair-approved).
- **(iii) present, consistent, checksum-RECOMPUTED payload edit** — **undetectable at MVP** (no carrier: m-10's rows key on identity/`args_digest`/terminals not content; `turn_open` carries no round-identity operand; the two actors do not read each other's store) ⇒ **resumes as apparently clean — the accepted confusion-not-malice MVP limit.** Frank does NOT distinguish sanctioned repair from corruption at the bytes (Step-4 versioning carry).

**Net boundary consequence (mine):** the non-promotion guard holds wherever detection is possible — class (i) leaves original truth (degrades), class (ii) is a distinct detected class that must carry mandatory local untrusted treatment and never be asserted as original truth (its exact m-9 disposition open), and class (iii) is the honestly-named accepted undetectable residual. m-1 states the invariants; **m-9's pair-approved total table settles the disposition, first action, label use, and any wire report**, and until it exists m-1 fixes none of them.

## Repairability-scope consequence (unchanged — accepted)
Decision 5's "a text editor works" is scoped to **m-9's journal only.** m-10's uneditable store has **no MVP repair path** if unresumable — the operator's accepted post-product hardening carry (`FRANK-HARDENING-BACKLOG.md`). Named + accepted.

## Item 7 — supersession list: ZERO m-1 MVP members (unchanged — accepted)
No m-1-owned frozen member (§2 redaction NOT-list, §2.3 at-rest battery, §1 secret boundary + seat identity, channel-stamped `FROM`) requires an additive amendment. **m-1 names no exact receipt/manifest/trust/disposition/wire member for MVP supersession**, and authors no `edited` wire/durable carrier — that stays m-9-local absent a separately ratified carrier supersession m-9/m-10 author.

## Step-4 forward carry — TWO SEPARATE statements (unchanged — accepted)
1. **The recorded two-app-store carry (m-9 + m-10 — NOT m-1's):** the pending Step-4 direction (path-independent canonicalisation OR authorised rebase) spans **m-9's journal record-digests + m-10's stored settlement/round identity** — the two session stores whose digests break on rewrite (`…route3-editsm/…-032916.md:47-55`; `…esc1-fork-4/RECONCILE-…-004504.md:75-77`). The m-1 governed store is NOT of that pair.
2. **The SEPARATE m-1 conditional:** IF any future design brings the m-1 governed relay store into an edit/rebase surface, THEN the deferred D3 verifiability-against-the-courier seam + seat-provenance re-establishment must be designed BEFORE that crossing — else fabrication class at the governed-store grain. Distinct from, not a substitute for, the recorded carry.

## Boundaries
Design-only boundary re-confirm; states only m-1's mandatory trust invariants, decides no m-9 disposition. No mechanism/carrier authored, no MVP supersession member named, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4, no external use. Frozen lane-2 component `d34a7c47…` + base `7c8b09a6…` UNMOVED. The revoked `…-133628` is inert, not lineage. `receipt_conflict` and every lock constituent stay frozen. Interface lock `cbd1893c…`, stage-6 `1125b0a0…`, r1 amendment `528d6a98…` (superseded-pending-r2) UNMOVED. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
- M1-CLOSE3-R3-F1 grounded at m-9's own review bytes: `…close3-editsm-m9/DESIGN-REVIEW-implementer-20260726-134147.md:36-56` — the checksum-mismatch disposition is UNSPECIFIED/open (`resumable` no-`resume_action` vs `degraded`+`re_derive`), the local untrusted label is mandatory-not-optional, kept off durable/wire/operator/E3, no invented m-9 wire class, Route-2 inequality stays `fail`. m-1 fixes no disposition; binds neither m-9 planner state (`…-131630`, `…-134920`/`1f8ec7b6…` — not pair-approved).
- The wire domain that crosses to m-10 = frozen `{resumable, degraded}` (settlement S-2, r17 §3 / rev16 §4); `edited` is m-9-local.
- Preserved rev3-accepted: BR-INV′, (a)/(b)/(c), the recomputed undetectable limit, class-distinctness split, repairability, zero-members, Step-4 pair + conditional, revocation-inert.
- Frozen: component `d34a7c47…`, base `7c8b09a6…`, interface lock `cbd1893c…`, stage-6 `1125b0a0…` reproduced UNMOVED.
- Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this rev4 relay + one INDEX.md row. No disposition decided, no mechanism/carrier authored, no MVP member named, no frozen/locked byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-1.implementer returns the exact-byte verdict on rev4 (M1-CLOSE3-R3-F1 fold only, no new material, no disposition fixed); on approve it is owner-final and master folds BR-INV′ + the class-distinctness/non-promotion invariants + the classified answers + zero-members + the Step-4 statements into amendment r2 alongside m-9/m-10's carrier + composed state machine and m-3's observable/Route-2 consequence — the m-9 total disposition table settling independently under its own pair. m-1.planner holds — no self-advance, no disposition owned, no byte owed beyond this return. H-12 stands.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
