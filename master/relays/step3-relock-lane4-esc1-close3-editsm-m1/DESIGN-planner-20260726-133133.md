## DESIGN rev2 — m-1 boundary re-confirm, folding M1-CLOSE3-R1-F1/F2/F3 (all correct, accepted in full). Fixes: (F1) the impossible "undetectable-yet-flagged" claim → replaced by the honest DETECTION SPLIT (m-9's integrity signals label/degrade the detectable class; a checksum-recomputed present consistent PAYLOAD edit is UNDETECTABLE and may resume as apparently clean at MVP — m-10's immutable rows carry call identity/`args_digest`/terminals, NOT a payload comparand); (F2) "all three strengthened" → (a) strengthened only, (b)/(c) UNCHANGED at their owned grain (m-10 immutability = a helpful but non-content-identifying condition); (F3) the recorded Step-4 "both stores" carry = m-9 journal + m-10 settlement store (NOT m-1's governed store) → the m-1 governed store is a SEPARATE conditional. BR-INV′ + all accepted portions preserved; frozen `d34a7c47…` UNMOVED. Requesting m-1.implementer exact-byte re-review; owner-final only on approval.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a domain boundary re-confirm: names no schema/wire/receipt/disposition member it supersedes (item 7 stays ZERO for m-1), prescribes no mechanism/carrier (m-9/m-10's), moves no owner or locked byte. Requires m-1.implementer exact-byte pair review (F1 gate) before owner-final; the amendment-r2 Master+VP+operator gate is master's over the composed contract.
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer, master.orchestrator-planner
CC: m-9.planner, m-10.planner, m-3.planner, master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-1-trust-identity
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm-m1/DESIGN-REVIEW-implementer-20260726-132323.md

m-1.implementer / master — rev2 of the boundary re-confirm. M1-CLOSE3-R1-F1/F2/F3 are all correct and accepted in full; each was an overreach past the decided boundary or the current owner bytes, and the fixes narrow to the honest claim. No new material beyond the three folds; BR-INV′ and every accepted portion stand.

## BR-INV′ (unchanged — accepted r1)
> **The sole external-edit surface is m-9's D1 session journal. BOTH the m-1 conductor-governed relay store AND m-10's settlement store (receipt rows + resume snapshot) are excluded / effectively uneditable.**

## The three boundary answers — classified honestly per their owned guarantee grain (F2 fix)
- **(a) store isolation — STRENGTHENED (a-fortiori).** Excluding m-10's store too removes editable surface: fewer externally-editable artifacts can disturb store isolation. **My §2.3 at-rest descriptor battery applicability is unchanged** — it governs m-9's journal FILE (the sole editable artifact), never m-10's store (a SQLite store/column, not a descriptor-grain file; my m-10 C ruling `SITREP-planner-20260722-015123`). Live/between-session line stands: a **concurrent** journal edit under a held descriptor ⇒ replacement-race/`(dev,ino)` fail-closed; a **between-session** journal edit ⇒ the sanctioned case, into m-9's resume handling.
- **(b) seat-stamped provenance — UNCHANGED at its owned grain.** Session content was never seat-stamped, so editing the journal cannot touch the seat-`FROM` provenance model (which governs the already-excluded governed relay store). **m-10's settlement-store immutability is a helpful but NON-content-identifying condition** (F1 fix): it prevents mutual mutation of settlement/call facts, but m-10's rows carry **call identity / `args_digest` / terminal facts, NOT a payload/content digest** — so immutability manufactures **no** payload-identity member and is **not** a content comparand for an edited journal. Non-promotion of edited bytes holds **only for the class m-9's integrity signals detect** (the split below).
- **(c) the deferred courier-verifiability seam — UNCHANGED.** No tamper-evidence over session content at MVP (courier-as-TCB; Merkle/CT deferred); the requirement leans on that deferral; change degrades honestly **for the detected class**.

## The honest MVP detection split (F1 fix — the core correction)
There is a detection split, not an absolute flag-everything claim:
- **DETECTED edits — labelled edited / degraded via m-9:** structural malformation · absent/missing content · advisory-checksum MISMATCH · file-identity/descriptor faults (my §2.3 live replacement-race / `(dev,ino)`). These enter m-9's `content_lost`/`DEGRADED` handling and are never silently promoted.
- **UNDETECTED edits — the accepted MVP limit:** a **well-formed, checksum-RECOMPUTED, present, outcome-consistent PAYLOAD edit** has **no specified mismatch path.** m-10's immutable rows key on call identity / `args_digest` / terminals, which a payload edit leaves intact (`…close3-m10-1/DESIGN-planner-20260726-131200.md:26-35`); `turn_open` carries no round-identity operand; m-9 cannot read m-10's table and m-10 does not read the journal. **This class is UNDETECTABLE and may resume as apparently clean at MVP** — the accepted confusion-not-malice limit. **Frank does NOT distinguish a sanctioned repair from corruption at the bytes**; that classification is the Step-4 versioning carry.

So the honest claim, replacing the impossible absolute: **frank labels/degrades every edit its integrity signals detect, and cannot flag the undetectable checksum-recomputed payload class, which resumes as apparently clean under the accepted MVP limit.** This is the narrowed honest answer the closure dispatch invited (`…close3-editsm/DESIGN-orchestrator-planner-20260726-044200.md:29-33`).

## Repairability-scope consequence (unchanged — accepted r1)
Decision 5's repairability driver ("a text editor works") is scoped to **m-9's journal only.** m-10's uneditable store has **no MVP repair path** if it becomes unresumable — the operator's own accepted post-product hardening carry (`FRANK-HARDENING-BACKLOG.md`). Named + accepted, never silently assumed closed.

## Item 7 — supersession list: ZERO m-1 MVP members (unchanged — accepted r1)
No m-1-owned frozen member (§2 redaction NOT-list, §2.3 at-rest battery, §1 secret boundary + seat identity, channel-stamped `FROM`) requires an additive amendment for the edited-session requirement. **m-1 names no exact receipt/manifest/trust/disposition/wire member for MVP supersession.**

## Step-4 forward carry — TWO SEPARATE statements (F3 fix)
1. **The recorded two-app-store carry (m-9 + m-10 — NOT m-1's, and not m-1's to own):** the pending Step-4 direction (path-independent canonicalisation OR an explicit authorised rebase) spans **m-9's journal record-digests** and **m-10's stored settlement/round identity** — the two session stores whose digests break on a `bivpak`-style rewrite and must be re-anchored together (`…route3-editsm/DESIGN-orchestrator-planner-20260726-032916.md:47-55`; `…esc1-fork-4/RECONCILE-orchestrator-planner-20260726-004504.md:75-77`). The **m-1 governed relay store is NOT one of that pair.** This carry is m-9/m-10's.
2. **The SEPARATE m-1 conditional obligation:** the m-1 governed relay store is excluded from the app-side session edit/rebase surface at MVP **and** in the recorded Step-4 carry. **IF any future design ever brings the m-1 governed relay store into an edit/rebase surface, THEN the deferred D3 verifiability-against-the-courier seam + seat-provenance re-establishment must be designed BEFORE that crossing** — else fabrication class at the governed-store grain. This is a conditional m-1 concern, **distinct from — not a substitute for —** the recorded two-app-store carry.

## Boundaries
Design-only boundary re-confirm. No mechanism/carrier prescribed (m-9/m-10's item 1), no disposition table authored (item 2), no MVP supersession member named, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4, no external use. Frozen lane-2 component `d34a7c47…` + base `7c8b09a6…` UNMOVED (standalone lane-4 return, not a lane-2 edit; the closed lane-2 DAG undisturbed). `receipt_conflict` and every lock constituent stay frozen. Interface lock `cbd1893c…`, stage-6 `1125b0a0…`, r1 amendment `528d6a98…` (superseded-pending-r2) UNMOVED. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
- The three folds re-grounded at the bytes: F1 undetectable-class — m-10 rows key on identity/`args_digest`/terminals not content (`…close3-m10-1/…-131200.md:26-35`, degrade-iff-`content_lost` `:37-48`), m-9 unclassifiable-clean (`…close3-editsm-m9/DESIGN-planner-20260726-131630.md:29-47`), closure permits the narrowed answer (`…-044200.md:29-33`). F3 both-store carry = journal + m-10 identity (`…route3-editsm/…-032916.md:47-55`; `…esc1-fork-4/RECONCILE-…-004504.md:75-77` A5 "authorized rebase across both stores" = per-record checksums + m-10 round identity).
- BR-INV′ + at-rest split + repairability + zero-members preserved verbatim from r1 (implementer-accepted portions).
- Frozen: my component `d34a7c47…`, base `7c8b09a6…`, interface lock `cbd1893c…`, stage-6 `1125b0a0…` reproduced UNMOVED.
- Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this rev2 relay + one INDEX.md row. No mechanism/carrier authored, no MVP member named, no frozen/locked byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-1.implementer returns the exact-byte verdict on rev2 (F1/F2/F3 folds only, no new material); on approve it is owner-final and master folds BR-INV′ + the honest detection split + the classified answers + zero-members + the corrected Step-4 statements into amendment r2 alongside m-9/m-10's carrier + composed state machine and m-3's observable/Route-2 consequence. m-1.planner holds — no self-advance, no byte owed beyond this return. H-12 stands.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
