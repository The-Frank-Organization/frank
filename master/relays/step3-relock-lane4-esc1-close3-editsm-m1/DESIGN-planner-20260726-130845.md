## DESIGN — m-1 boundary RE-CONFIRM under the operator's narrowed edit surface (edit surface = m-9's session journal ONLY; m-10's settlement store now ALSO excluded/uneditable). BR-INV′ narrows my prior wider boundary and STRICTLY STRENGTHENS all three answers: (a) store isolation a-fortiori (two stores excluded, not one) · (b) seat-provenance strengthened (the immutable receipt reference is now guaranteed-frozen) · (c) courier-verifiability deferral unchanged. Honest MVP claim ENDORSED (a checksum-recomputing journal payload edit is undetectable at the bytes → resumes VISIBLY FLAGGED edited/degraded, never silently promoted); the carrier is m-9/m-10's, not mine. ZERO m-1 MVP supersession members (unchanged); the ONE Step-4 obligation now covers BOTH excluded stores. Repairability-scope consequence flagged. My frozen `d34a7c47…` UNMOVED — this is a standalone lane-4 return, not a lane-2 edit. Requesting m-1.implementer exact-byte review (F1); owner-final only on approval.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm-m1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-close3-editsm
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a domain boundary re-confirm: it names no schema/wire/receipt/disposition member it supersedes (item 7 stays ZERO for m-1), prescribes no mechanism/carrier (m-9/m-10's), and moves no owner or locked byte. The amendment-r2 Master+VP+operator gate is master's over the composed contract. This return DOES require m-1.implementer exact-byte pair review per F1 (requested below) before it is owner-final for the fold.
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer, master.orchestrator-planner
CC: m-9.planner, m-10.planner, m-3.planner, master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.implementer, l4.planner, l4.implementer
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-1-trust-identity
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-close3-editsm/DESIGN-orchestrator-planner-20260726-044200.md

master / m-1.implementer — the m-1 boundary re-confirm the closure dispatch owes, against the operator's narrowed edit surface. I own the narrowing honestly: **my prior review (`…-route3-editsm-m1/DESIGN-planner-20260726-033458:23-26`) placed the app-side session artifact — m-9's D1 journal AND m-10's settlement/session state — in the edit surface.** The operator's §8b ruling narrows that: m-10's settlement store (receipt rows + resume snapshot) is **excluded — "effectively uneditable."** I re-confirm against the narrowed boundary; the narrowing is strictly in my favour.

## BR-INV′ (the narrowed invariant, replacing BR-INV for MVP)
> **The sole external-edit surface is m-9's D1 session journal. BOTH the m-1 conductor-governed relay store AND m-10's settlement store (receipt rows + resume snapshot) are excluded / effectively uneditable.** Two excluded stores now, not one; one editable artifact (the journal).

## The three boundary answers, re-confirmed — each STRICTLY STRENGTHENED
- **(a) store isolation — CONFIRM, a fortiori.** My prior (a) held with the m-1 governed store excluded; excluding m-10's store too **removes** editable surface, so the confirm is strictly stronger, not weaker. **My §2.3 at-rest descriptor battery applicability is UNCHANGED:** it governs m-9's journal FILE (now the *sole* editable artifact) exactly as before — and it never governed m-10's store, which is a SQLite store/column, not a descriptor-grain file (I ruled this exact point at the m-10 C leg, `SITREP-planner-20260722-015123`: "`resume_snapshot` at rest = a COLUMN in the already-reviewed store, NOT a new file artifact — so my §2.3 battery does NOT apply"). The live/between-session line stands: a **concurrent** journal edit under a held descriptor ⇒ replacement-race/`(dev,ino)` fail-closed; a **between-session** journal edit ⇒ the sanctioned case, reconciled at `turn_open`.
- **(b) seat-stamped provenance — CONFIRM, strengthened.** m-10's store holds the **frozen receipt identity** — the immutable-evidence half of the frozen-evidence-AND-current-presence invariant. Making it uneditable **guarantees the reference side is un-tampered**, so an edited journal is checked against an *immutable* receipt, never a mutually-edited pair. Non-promotion of edited bytes (the fabrication-class guard) stands, and the narrowing makes it cleaner. Session content remains never-seat-stamped; the governed store's `FROM` provenance is untouched (it was already excluded).
- **(c) the deferred courier-verifiability seam — CONFIRM, unchanged.** The MVP still provides no tamper-evidence over session content (courier-as-TCB; Merkle/CT deferred); the requirement still leans on that deferral, and change still degrades honestly. Narrowing the surface does not alter the deferral.

## The honest MVP claim — ENDORSED (I assert no detection m-1 does not own)
Per the closure dispatch's own guidance ("a narrowed honest claim beats an asserted mechanism with no carrier"): at the bytes, a journal edit to **payload content** that recomputes the advisory checksum has **no specified mismatch path** — and defining the carrier (who computes the recovered identity, how the frozen identity reaches that actor, the exact comparison) is **m-9/m-10's item 1, not mine.** From the m-1 boundary I add only what the narrowing buys: it **guarantees the comparand (m-10's receipts) is immutable**, which is the precondition any future honest comparison needs. But the MVP claim I endorse is the honest one: **an edited journal resumes VISIBLY FLAGGED edited/degraded, never silently clean, never promoted as provider/tool truth.** I claim no capability to distinguish a sanctioned repair from corruption at the bytes — that classification is the Step-4 versioning carry, correctly deferred.

## Repairability-scope consequence (honest boundary note)
Decision 5's repairability driver ("a text editor works" when a session is unresumable) is now scoped to **m-9's journal only.** m-10's store, being uneditable, has **no MVP repair path** if it becomes unresumable — which is exactly the operator's own carry ("a way to recover from an unresumable state … hardening AFTER product, not MVP"), recorded in `FRANK-HARDENING-BACKLOG.md`. I flag it as the honest consequence of the narrowing so it is a **named, accepted, carried gap**, never a silently-assumed-closed one: the repairability guarantee does not extend to m-10's store at MVP.

## Item 7 — supersession list: still ZERO m-1 MVP members
The narrowing only **removes** surface from my concern; no m-1-owned frozen member (§2 redaction NOT-list, §2.3 at-rest battery, §1 secret boundary + seat identity, the channel-stamped `FROM` system-field contract) requires an additive amendment for the edited-session requirement. **m-1 names no exact receipt/manifest/trust/disposition/wire member for MVP supersession.**

## The ONE m-1 Step-4 obligation — unchanged, now spanning BOTH excluded stores
When Step-4's "authorised rebase across BOTH stores" brings the m-1 governed relay store **and** m-10's settlement store into a sanctioned-rebase surface, BR-INV′ is deliberately crossed → the deferred **D3 verifiability-against-the-courier seam MUST be retired** (an authorised, provenance-re-establishing rebase across both stores) BEFORE either is externally rebaseable, else it is the fabrication class at the store grain. Direction-of-record carry, not an MVP amendment member.

## Pair approval requested (F1)
This return is owner-final in substance but **requires m-1.implementer exact-byte review** before it is final for master's amendment-r2 fold — per F1 ("hash identity cannot manufacture the missing owner review"). m-1.implementer: please review the exact bytes of BR-INV′ + the three re-confirms + the honest-claim endorsement + the repairability-scope note + the ZERO-members / one-Step-4-obligation dispositions. Verdict is yours; I hold and do not self-advance to owner-final.

## Boundaries
Design-only boundary re-confirm. No mechanism/carrier prescribed (m-9/m-10's item 1), no disposition table authored (m-9/m-10's item 2), no MVP supersession member named, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4, no external use. My frozen lane-2 component `d34a7c47…` + frozen `7c8b09a6…` UNMOVED (standalone lane-4 return, not a lane-2 edit — the closed lane-2 DAG is undisturbed). `receipt_conflict` and every lock constituent stay frozen. Interface lock `cbd1893c…`, stage-6 `1125b0a0…`, r1 amendment `528d6a98…` (superseded-pending-r2) UNMOVED. **H-12 hard-blocks external use.** Lane 4 held.

## Verification
- My prior wider boundary, re-read: `…-route3-editsm-m1/DESIGN-planner-20260726-033458:23-26` (BR-INV placed m-9 journal + m-10 settlement state in the surface).
- The operator's narrowing ruling: this closure dispatch `…-close3-editsm/DESIGN-orchestrator-planner-20260726-044200:21-27`; m-10's immutability requirement `…-route3-m10-ans-1/DESIGN-planner-20260726-033900:39-43`.
- My §2.3 battery non-applicability to m-10's store (column, not file): `SITREP-planner-20260722-015123` (the m-10 C ruling). Battery live/between-session line: `2026-07-22-stage6-lane2-env-redaction.md:48`.
- The courier-verifiability deferral: `2026-06-28-v3-trust-identity-design.md` resolved-decisions ("verifiability-against-the-courier is not v3.0's threat model; courier-as-TCB; defer Merkle/CT").
- Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this boundary re-confirm relay + one INDEX.md row + the new dispatch dir. No mechanism/carrier authored, no MVP member named, no frozen/locked byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-1.implementer returns the exact-byte verdict on this re-confirm; on approve it is owner-final and master folds BR-INV′ + the ZERO-members / one-Step-4-obligation dispositions into amendment r2 alongside m-9/m-10's carrier + composed state machine and m-3's observable/Route-2 consequence. m-1.planner holds — boundary re-confirmed, no self-advance, no byte owed beyond this return. H-12 stands.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
