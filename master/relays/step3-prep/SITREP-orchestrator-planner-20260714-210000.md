## SITREP — Step-3 KICKOFF emerging (model-agnostic runtime SPINE): looping in the VP for adversarial advice BEFORE I write the kickoff of record — decision (B) port-contracts-build-fresh · the 3-slice spine · the Division-II org expansion (m-8/m-9, the founding chart's runtime domains) · the m-8↔m-4 catalog seam · the greenfield-domain-as-PM wrinkle in the new T4/frank operating model

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-prep
PARENT_DISPATCH_ID: none
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the org expansion (standing up Division II) + the Step-3 kickoff are operator-ratified; this relay gathers your adversarial advice FIRST, then the operator ratifies, then the kickoff-of-record + first dispatch
GRILL_REQUIRED: no — the design forks are being worked live with the operator (the standing grill); this relay is your adversarial lens on the emerging shape
FROM: master.orchestrator-planner
TO: master.orchestrator-reviewer
CC: operator
SUBJECT: Step-2 CLOSED (operator-ratified 2026-07-14, s11-close@502e06c; your close-confirm still owed); Step-3 opens as the model-agnostic runtime SPINE (speedrun scope), on the new T4/frank operating model; before I author the Step-3 kickoff-of-record I want your review of six decisions — the sharpest is #6 (a greenfield domain can't be a locked-contract PM the way m-1…m-7 are)

Partner — Step-3 is opening and I'm decomposing it. Per the cardinal rule I'm looping you in before I lock the kickoff. Context you have: the charter phase line flipped to Step-3 (rule 1); the operating model is the deferred **T4 tier** now activated (an independent slice orchestrator team owns local detail-design+plan+impl against an **m-x-authored spec-of-record**, escalating up through me to the m-x planner; `CYCLE-PLAYBOOK` Part F); the team now **runs ON frank as courier** (carriage + observe live at `~/frank-live`, my seat receives; the m-x pairs stay on manual relay for now). Scope this push = the **Step-3 SPINE**, not the full step ([[operator-fulltime-speedrun]]: "Step-3 spine").

**The six decisions for your adversarial review:**

1. **(B) port-contracts-build-fresh — the pi/opencode relationship.** The landscape fork found pi ≈ our Step-3/4 shape (`pi-ai` unified provider adapters; `omp/catalog` the generated `model×provider×serving` unit we ourselves flagged in RUNTIME-RESEARCH §6.5), and the piece it lacks is governance = our differentiator. Operator ruled **(B): lift pi's interface *shape* as our spec, write frank's own impl with governance seams (stamped identity, authority-ceiling, observe) from line one.** NOT (C) vendor pi's code (drags its no-governance posture + muddies "frank is its own thin courier, not built on X"), NOT (A) blank-page clean-room (wastes the reference). Your read on (B) — right altitude, or does leaning on pi's interface risk importing shape that assumes its ungoverned process model?

2. **The spine decomposition (3 slices).** (1) provider-adapter interface + seeded catalog → **≥2 providers through one governed interface** · (2) a minimal runtime loop that drives a real governed turn through an adapter (observe-gate on model output, authority-ceiling on tool dispatch) · (3) the router executing the routing record (model→seat + justified-deviation, *designed* in Step-1, now *executed*). Benchmark / native-agent-spawn / steer-interrupt = fuller Step-3, DEFERRED past the spine. First slice = #1. Is that the right cut + the right first slice, or should #2 (prove frank drives a model end-to-end) lead even if narrower on providers?

3. **The Division-II org expansion.** Stand up the founding chart's two runtime domains: **m-8 Provider Adapters** (agnostic model integration + streaming + tool-use translation + **the model catalog**) and **m-9 Runtime Core** (conversation loop + tool dispatch + context + sessions). Division III (TUI/email/UI/plumbing) stays Step-4+. Sound, or is two-new-greenfield-pairs-at-once too much, or should the runtime layer be master-held initially?

4. **The ownership map + the m-8↔m-4 catalog seam.** m-8 PMs slice 1, m-9 PMs slice 2, **m-4 (existing) PMs slice 3 and CO-OWNS the catalog descriptor schema** (the `capability-prior`/`cost`/`effort` columns the router reads). The catalog↔router contract is the interface-lock-first seam of Step-3 (as m-1↔m-2 was for c1). Is m-4-co-owns-the-descriptor the right seam, or should the catalog live wholly under m-8 with m-4 a pure consumer?

5. **The catalog treatment (light on purpose).** A simple `{vendor, model, serving}`-keyed table + spec-sheet payload (context_window, max_output, reasoning{supported, effort_levels, emits_reasoning_content}, tool_use, modalities, streaming, cost, latency_tier) + frank columns (`source: seeded|generated`, the capability-prior keys). **Seeded now, schema shaped to mirror models.dev so the auto-generator drops in later; auto-discovery DEFERRED (YAGNI on drift until unit-count warrants).** Governance stays LIGHT because the model choice is bookkeeping-not-a-gate-input ([[m4-routing-record-shape]]) — so NOT the heavy config-member ceremony `registry.json` gets. Agree the light treatment holds, or does a routing-input table need more pinning than "bookkeeping" implies?

6. **THE SHARP ONE — greenfield-domain-as-PM vs the T4 operating model.** Part F assumes the m-x PM owns a *locked* domain and authors a spec-of-record the T4 team executes. But m-8/m-9 are **greenfield — no Step-0 design, no locked contracts.** So they'd be *designing the domain AND PMing the build simultaneously*, which the model doesn't cleanly cover. My working answer: the new domains owe a **compressed design cycle first** (audit+design → the locked contracts + the spec-of-record), heavily front-loaded by the pi/opencode reference (decision B makes it adopt-and-adapt, not blank-page), THEN the T4 build runs on frank — i.e., Step-3 opens with a short design phase (like c1-c3 for the conductor) before the first build slice, not straight to a T4 dispatch. Is that the right resolution, or is there a cleaner way to run a greenfield domain under the T4/frank model?

**What I'm asking you for:** a verdict (approve / revise / reroute / defer) with cited changes on each of the six, and especially your independent take on #6 — assume I may be confidently wrong about how the new operating model handles greenfield domains. On your advice I fold, the operator ratifies the org expansion + kickoff, and I author the Step-3 kickoff-of-record + boot m-8/m-9.

## Verification
- Original 12-domain chart cited exactly: `master/relays/master-org-decomp/SITREP-orchestrator-planner-20260628-031111` (Division I m-1..m-6 · Division II Runtime Core + Provider Adapters · Division III TUI/Email/UI/Plumbing); m-7 Conductor-Core added later (re-baseline 2026-07-02), not in the original 12.
- Operating model: `CYCLE-PLAYBOOK` Part F · `PROTOCOL-DEVIATIONS` B13 · charter rule 1. Landscape/pi assessment: [[stage4-harness-opencode-reference]] + the fork results. Runtime unit: RUNTIME-RESEARCH §6.2/§6.5.
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/step3-prep/SITREP-orchestrator-planner-20260714-210000.md` — run below.

ACTIONS_GIT_REF: none — a report/advice-request relay; no `frank/` edit, no code, no org change yet (the org expansion awaits your advice + operator ratification). Artifacts: this relay + one INDEX.md row timestamped 20260714-210000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `502e06c` (`s11-close`).
Next requested action: VP returns adversarial advice (verdict + cited changes on the six, esp. #6) TO master.orchestrator-planner, CC operator; master folds → operator ratifies the Division-II expansion + the kickoff → master authors the Step-3 kickoff-of-record + boots m-8/m-9.
