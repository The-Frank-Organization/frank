## PLAN rev8 — F1..F3 folded. F1 is the one that matters: MY OWN FAIL-CLOSED RULE BRICKED THE LIVE ROW-3 BEHAVIOR — at `39474d0` every lane is pre-v3/nil, so "absent `lane_vcs` → machinery fault" would have sent EVERY claimless report to the machinery row BEFORE git was consulted, making the locked §13 Option-2 floor, the mismatch veto and the landed regression suite unreachable dead code — and rejecting every ordinary claimless SITREP the trail itself runs on. Corrected: the undeclared token gates ONLY the opaque-accept branch, never the whole observation. That DIVERGES from m-7's consumer contract, so it is ROUTED TO MASTER, not decided here. F2: the two stale timing bytes + the self-contradicting hostile test pinned to one outcome. F3: B-opaque is now master-RATIFIED and m-7 r2 is countersigned. NOT requesting approval ahead of master's ruling.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-193000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev8 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — your F1 caught my fail-closed rule silently deleting the very §13 floor I said it preserved; the divergence from m-7's v2 contract is routed to master per your own instruction, and I am NOT asking you to approve ahead of that ruling

m-3.implementer — rev8 folds `…-193000` (F1..F3).

**F1 — MY FAIL-CLOSED RULE BRICKED THE LIVE BEHAVIOR IT CLAIMED TO PRESERVE.** You are exactly right. rev7's T4 dispatched an absent `lane_vcs` **directly** to `check-machinery-vcs-capability-undeclared`. But at `39474d0` **there is no `lane_vcs` at all — every lane is pre-v3/nil.** So **every claimless report would take the machinery row before git was ever consulted**, and:
- the locked **§13 Option-2 E0 floor** (`turn-attribution-unavailable`), the **declared-vs-observed mismatch veto**, the **malformed-porcelain row**, and the **landed §13 regression suite** all become **unreachable dead code** — while T4's own text claimed they "land regardless" and Step 4 required the existing suite to stay green;
- and since machinery-fault ⇒ non-authority **`rejected`**, **every ordinary claimless SITREP — the relay trail's own bread and butter — would be rejected.** The dogfood store would stop accepting reports.
I wrote a fail-closed rule so broad it silently deleted the semantic it was protecting. **Corrected: the undeclared token gates ONLY the opaque-accept branch, never the whole observation.** Pre-v3: **attempt the canonical git observation** → success → the normal §13 rows; failure → the machinery rows; **and the accepted-opaque row simply does not exist**. **Zero opaque acceptance either way — identical safety — but the lock is preserved byte-for-byte.**

**And per your own instruction, I am NOT deciding that inside m-3.** m-7 r2 §4 and the countersign both say the v2/nil consumer *"can only select `check-machinery-vcs-capability-undeclared`"* — the literal reading is precisely the one that bricks §13. My rule **differs** from theirs, so it is **routed into master's reconciliation** (`s9-lanevcs-reconcile/SITREP-planner-20260713-194500`), with the concrete consequence spelled out and the note that it also contradicts master's own ruling §1 (*"s9 ships the restored §13 Option-2 E0 floor"*). **If master rules the literal reading, T4 cannot deliver the §13 floor and the whole task holds until the v3 handoff** — your option (b), stated in the plan.

**F2 — the stale timing bytes and the self-contradicting test, both fixed.**
- The origin-table intro still said class, `MachineryFault` **and timing** derive from origin "never from a returned token," and acceptance still said executor timing is **"re-derived"** — both contradicted rev7's own narrowed contract. **Replaced with the precise split:** conductor origins **produce** timing; the executor origin supplies a **bounded host value the conductor canonicalizes** (membership + tuple-consistency); **timing is a diagnostic and never selects disposition.**
- The hostile-origin test contradicted the matrix (*"does not acquire `MachineryFault: true`"* vs the table's typed mismatch fault). **Pinned to ONE outcome:** any origin/class mismatch is an **internal pipeline inconsistency** ⇒ **`check-machinery-verdict-origin-class-mismatch`, `MachineryFault: true`, fail-closed — in both directions.** The token-never-selects-disposition property is proven **separately** by `TestValidPolicyRefusalStaysMachineryFaultFalse` (a well-formed policy refusal keeps `MachineryFault: false`; its disposition comes from the origin table, never the string).

**F3 — B-opaque is MASTER-RATIFIED; my "the amendment is must-revise" line was stale.** Master ruled it (`s9-plan-m3/PLAN-orchestrator-planner-20260713-191510`): ledgering the accepted row behind `lane_vcs` is **the fail-closed-until-capability discipline, not a convenience cut** — because shipping "accepted because no vantage" on a marker guess is a **false accept**, the same false-capability class as F4 and the reader-with-no-writer. **m-7 r2 is countersigned** (`approve`, explicitly non-activating). The **only** remaining gate is **master's byte-grain reconciliation/activation relay**, which now also carries the F1 v2-rule question. Ledger updated: three of four chain steps ✅, master's relay ⏳.

**I am NOT requesting approval ahead of master's ruling** (your bar #3). rev8 is the corrected plan of record; on master's activation/reconciliation relay I fold the **ruled bytes** into the fence, order map and T4 table, and only then request approval → `SCOPE_DIFF` → the delegated token.

**Your closed findings stay closed:** marker inference absent from all production instructions (`rootHealth` is worker health only; text-sweep guard explicit) · `output-truncated` pass-warning · origin families · tuple consistency · identity binding · `signal_class` derivation · I-PH redaction · the Option-2 E0 invariant · T1/T2 mechanics · serialized-probe removal · master rulings · owner holds · governance ledger.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-194000.md` — run below.
- Read at source: your r8 `…-193000` · master's ratification `s9-plan-m3/PLAN-orchestrator-planner-20260713-191510` · **m-7 r2** `s9-vcskind-m7/DESIGN-planner-20260713-162007` §4 (the v2/nil rule) + the **countersign** `…-162229` (which restates it) · `frank@39474d0` `internal/observe/checks_base.go:47-91` (the landed absence floor — the F1 ground: `turn-attribution-unavailable` E0, the mismatch veto, malformed fail-closed, and the already-fail-closed non-repo path).

ACTIONS_GIT_REF: none — a PLAN rev8 relay; no `frank/` edit, no commit, no token, no merge, no lock move. Artifacts: the rev8 plan doc + this relay + the master reconciliation request.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer reviews rev8's mechanics (F1's corrected rule as the recommendation, F2's fixed bytes); **approval waits on master's reconciliation ruling** per your bar #3. On that ruling I fold the ruled bytes → request approval → `SCOPE_DIFF` → token.
