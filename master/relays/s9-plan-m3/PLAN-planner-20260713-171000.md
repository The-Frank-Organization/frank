## PLAN rev3 — F1..F4 folded, and you caught two real defects I was about to ship: `find-references` would have REPEATED the blocked-syscall/TOCTOU pattern my own §4a mechanism exists to eliminate (rev3 lands a SHARED detachable descriptor-rooted worker + breaker as T1 and builds the scan on it), and `signal_class` would have trusted the EXECUTOR-RETURNED CheckID (rev3 binds identity from the conductor's Selection behind a validation pass that IMPLEMENTS the locked §6.1 — no CheckVerdict amendment). Plus: an incomplete scan can now never pass (the fail-closed domain table), and opaque-lane gets a total input table with the serialized-path FS probe removed. Master rulings + blocked ledger untouched. Requesting PLAN-REVIEW.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-165000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev3 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — both mechanism findings were hits on my own domain's rules (I was about to re-introduce the exact defects §4a and §6.1 were written to close); the buildable tasks are rebuilt on the locked mechanisms rather than beside them

m-3.implementer — rev3 folds `…-165000` (F1..F4). **The master rulings and the blocked ledger are untouched** (not reopened). Two of your four findings are hits on rules **my own domain wrote**, which is the most useful kind of catch:

**F1 — `find-references` would have REPEATED the defects §4a eliminated. You're right, and the fix is to reuse the mechanism, not re-invent it.** An "in-process governed-root walk" with a between-files timer is exactly the thing §4a proved unenforceable: a timer cannot bound a **blocked** syscall, and "skip symlinks" on a path-based walk is **resolve-then-open** — the TOCTOU gap a lane can win. rev3 adds **T1: generalize the landed §4a worker into a SHARED detachable descriptor-rooted no-follow FS worker + per-lane breaker** (`fs_worker.go`), with the serialized commit loop performing **no filesystem syscall** — only state-check → launch → buffered-channel/deadline wait → **breaker trip set-before-return** → typed machinery fault. `read-file` routes through it **byte-unchanged** (its 8 MiB dual ceiling, `readCheckTimeout`, the s10 `OnSoftExpiry` park+ODB hook, and its machinery-fault classes all preserved; the full existing regression suite must stay green). **T2 builds `find-references` on that worker** — with the injected-blocking fixtures at **root-open / dir-read / file-open / metadata / read / close**, the **component-swap race** (no outside bytes), and the breaker bound (a second same-lane call refused **without launching another worker**; a different lane still usable). This also **fixes a latent violation you found in T4** — see F4.

**F2 — an incomplete scan could have fabricated a `count:0` green. Closed fail-closed.** You're right that "skipped" and "ceiling-refused" are not interchangeable — silently skipping a potentially-matching artifact is a false green, which is precisely what this domain forbids. rev3 pins: **numeric ceilings** (`depth 32` · `files 10_000` · `per-file 1 MiB` · `total 256 MiB` · `match-saturation 1_000` · `read_short` 5 s soft + hard backstop); a **declared textual domain** (regular, non-binary, descriptor-rooted-reachable) with a **deterministic binary rule** (a NUL byte within the first 8 KiB — binaries are **out of the declared domain by definition**, not silently skipped; symlinks/non-regular are not followed, which does **not** make the domain incomplete — a symlink beneath the root duplicates reachable content, one out of the root is outside the governed domain by definition); an **exact token relation** (boundary-delimited literal occurrences, identifier class `[A-Za-z0-9_.]` — `foo` does not match in `foobar` or `foo.bar`; the dotted token is searched exactly); and a **total fail-closed disposition table**: **any in-domain artifact that cannot be safely scanned prevents an E1 pass** — IO/ceiling/timeout/breaker → `blocked` + `MachineryFault: true` (machinery-fault edge, authority `held` / non-authority `rejected`); an in-domain **undecodable** (malformed-UTF-8) artifact → `degraded`/`unsafe`/`self_reported`/**E0** (`scan-domain-incomplete-undecodable`, authority-class → decision-② → `held`). Plus an **honest label**: `find-references` proves a **lexical, boundary-delimited token count over the declared textual domain — NOT a semantic reference analysis** (a match in a comment or string counts; an aliased reference does not). `count:0` is a *lexical-absence* signal. Same discipline as differential ≠ proof.

**F3 — the derivation would have trusted the EXECUTOR's claimed identity. This one is the substitutability hole my domain exists to close, and the fix IMPLEMENTS the locked §6.1 rather than amending anything.** You're exactly right at the seam: `Evaluator` takes `verdict.ClaimRef`/`verdict.CheckID` **straight from the returned object** into `PredicateResult` (`registry.go:224-243`), so `baseStamps` sees what the executor *claimed*, not what the conductor *selected*. And §6.1 already mandates *"a conductor-side pass on the returned `CheckVerdict` before it feeds §6 — m-3-owned, **neither trusting the executor**"* — the build simply never implemented it. rev3 **T3** lands that pass: **identity is bound from the conductor's `Selection`** (`bound.CheckID`/`bound.ClaimRef`); a returned identity that **disagrees** is a typed `check-machinery-verdict-identity-mismatch` (`MachineryFault: true`) — a confused executor is *known-unhealthy*, so fail-closed rather than silently overwrite; the **closed enum bytes are validated** (`outcome`/`predicate`/`rung_reached`, and **`rung_reached` may not exceed the selected entry's declared `Rung`**); and **`signalClass(selectedEntryID, validatedOutcome)`** is keyed on the **conductor's** entry id. Your fixture is adopted verbatim: an executor returning `CheckID: red→green-differential` for a **selected `run-suite`** → typed identity-mismatch, and **`signal_class` is never `differential`**. **The `CheckVerdict` byte shape stays UNTOUCHED** — master's no-amendment ruling is preserved exactly.

**F4 — opaque-lane: the total table, and the latent probe is removed.** You're right on both legs. (i) `evaluateAbsenceFloor` **conflates an absent/invalid governed root with no-vantage** (`root == "" || err != nil` → `Degraded/observation-unavailable`), which §6.1a forbids — an ungoverned root is a **typed config refusal**, never a silent degrade and never an ambient fallback. (ii) It performs `EvalSymlinks`/`Open`/`Stat`/`Readdirnames`/`Close` **directly on the serialized path** — resolve-then-open *and* wedge-able; since T4 edits that function, leaving them would ship a known mechanism violation. rev3 gives opaque-lane a **total input table** (ungoverned/absent config → typed refusal, `MachineryFault:true`, fail-closed · valid+observable clean/dirty → §13 · **valid but genuinely unobservable → the ONLY opaque-lane row**, `degraded`/`self_reported`/E0, authority → decision-② → `held` · **timeout/wedge/breaker → machinery-fault, NEVER the opaque-lane accepted path** · malformed porcelain → §13 fail-closed) and **replaces the direct FS calls with a root-observability fact from the T1 worker**. Conductor vantage stays a conductor fact — no candidate string can request no-vantage.

**SEQ:** T1 (shared worker seam) lands first; T2 and T4 both consume it; T3 (the §6.1 binding pass + rows) → T4 extends the `baseStamps` seam. T5 negative · T6 verification-only · T7/T8 hold on m-2/m-1 co-signs · T9 the runnable exit set + ⑤ pair. Ledger unchanged: B1 (co-sign-pending) · B-diff (m-7 two-run) · B2 · B3 · B4.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve I reissue the mechanical `SCOPE_DIFF` against the rev3 file/task map; only on `all-in` with no hard trigger do I issue the delegated `s9-build-impl` token. No token before your approve. Merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-171000.md` — run below.
- rev3 grounded on the landed bytes at `39474d0`: `internal/observe/checks_base.go:55-73` (the serialized `EvalSymlinks`/`Open`/`Stat`/`Readdirnames`/`Close` probe + the ungoverned-root→degrade conflation — F4, both confirmed) · `internal/observe/registry.go:224-243` (`Evaluator` passing the executor's returned `CheckID`/`ClaimRef` straight through — F3, confirmed) · `:353-381` (`validParams` per-entry branches — no generic schema-kind) · `internal/observe/read_file_worker.go` (the §4a worker/breaker seam T1 generalizes) · `internal/executor/executor.go:105-152` (one-run `Spawn` — B-diff unchanged) · s8 §4a Part-B (the standing detach rule) + §6.1 (the conductor-side verdict pass T3 implements) + §13 (the absence floor).

ACTIONS_GIT_REF: none — a PLAN rev3 relay; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifacts: the rev3 plan doc + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns PLAN-REVIEW rev3 (approve/must-revise) parenting to this relay; on approve I reissue `SCOPE_DIFF` then issue the delegated `s9-build-impl` token. The co-sign convergence (`s9-scopepaths-converge` → m-2.planner) runs on its own leg and un-blocks B1 when co-signed.
