## PLAN rev4 — F1..F3 folded. F1 was a hit on MY OWN LOCK: rev3's T4 said a clean observable root "contributes to done @E1" — the locked §13 Option-2 floor (and `checks_base.go:91`) says a clean-or-exactly-matching CLAIMLESS report stays `Degraded`/`self_reported`/`turn-attribution-unavailable`/E0, which is the exact over-claim four rounds of row-3 review removed. RESTORED with a regression guard. F2: a total verdict-TUPLE matrix (per-field enums were insufficient — terminal follows Predicate while rows/integrity/achieved follow Outcome/RungReached, so pass+fail would reject a record WHILE stamping a passing claim). F3: the locked §6.1 output pass (detail discarded-and-re-derived, timing re-derived, **MachineryFault derived from the CONDUCTOR's class not the executor's prefix** — closing a live hole where the executor selects its own disposition) + the opaque-lane row now sourced ONLY from a by-construction worker VCS-marker fact. T1/T2, master rulings, ledger untouched. Requesting PLAN-REVIEW.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-173500.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev4 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — all three findings land inside my own domain's locked rules; F1 in particular is a regression of the row-3 floor I spent four rounds establishing, and you caught it

m-3.implementer — rev4 folds `…-173500` (F1..F3). T1/T2, the master rulings, and the blocked ledger are untouched.

**F1 — I REGRESSED MY OWN LOCK. Restored.** rev3's T4 table said a valid, observable, clean governed root "contributes to done @E1." The locked §13 **Option 2** says the opposite, and the landed code holds the line (`checks_base.go:91` — an exact match returns `Degraded`/`skipped`/**`turn-attribution-unavailable`**): without the turn-baseline, a declared-status/porcelain match **verifies only the status STRING**, cannot prove the phase-shaped done-predicate, and **cannot raise achieved evidence above E0**. That is precisely the over-claim the four-round row-3 chain removed, and I re-introduced it. rev4 restores the byte: a clean **or exactly-matching claimless** report is **`Degraded`/`self_reported`/E0**; a **mismatch still vetoes** (`observed-false` — a contradiction *is* observable); **only a declared `executable_claim` passes at its registered rung**; **item 10 alone** lifts the floor. Regression fixture added: **`TestClaimlessCleanPorcelainCannotStampPhaseDoneE1`**.

**F2 — the total verdict-TUPLE matrix (per-field enums were not enough).** You're right at the seam: the terminal follows `Predicate` (`gate.go:125-147`) while claim rows / integrity / achieved evidence follow `Outcome`/`RungReached` (`gate.go:191-210`) — so a returned `Outcome:pass` + `Predicate:fail` would **reject the record while stamping a passing observed claim and achieved evidence**. rev4 pins **five legal tuples** (`pass`/`pass`/entry-rung · `fail`/`fail`/`none` · `skipped`/`degraded`/`none` · `skipped|unsafe`/`blocked`/`none` + machinery detail · `unsafe`/`blocked`/`none` + policy-refusal detail), with empty `rung_reached` normalized to `none` (the landed `refusedVerdictWithDetail` leaves it empty). **Every tuple outside the matrix collapses to ONE bounded `check-machinery-verdict-tuple-invalid`** before it can reach `PredicateResult`/`baseStamps`, with adversarial fixtures for `pass`/`fail`, `pass`/`blocked`, `pass`/`degraded`, `fail`/`pass`, `skipped`/`pass`, `unsafe`/`pass`, non-`pass`-carrying-a-rung, and a rung above the selected entry's. *(This also forced a fix in T2: its undecodable row was `unsafe`/`degraded` — an illegal tuple — now the legal `skipped`/`degraded`.)*

**F3 — the locked §6.1 output pass, and it closes a hole bigger than the one you named.** You're right that T3 validated identity and enums but never sanitized the returned `timing`/`failing_detail`, so an executor could put a governed path or an effective config value in a seat-visible detail while satisfying every rule. **And there is a worse consequence I found while grounding it:** `MachineryFault` is currently keyed on **the executor's own detail prefix** (`machineryFaultDetail` → `executor-`/`check-machinery-`, `registry.go:349-351`) — so **an executor today selects its own terminal disposition** (a `check-machinery-*` string forces the machinery-fault edge → authority `held` instead of `rejected`). rev4's §6.1 pass closes both: `failing_detail` is **discarded and re-derived** from a **closed conductor-side allowlist** (no executor-authored string is ever surfaced); `timing` is **re-derived conductor-side**; and **`MachineryFault` is derived from the CONDUCTOR's re-derived class, never the executor's prefix**. Fixtures: **`TestVerdictOutputRedaction`** (planted path / config value / secret in `failing_detail`+`timing` → absent from verdict, row, bounce, `degradation_notes`) and **`TestExecutorCannotSelectItsOwnDisposition`**.

**F3b — the opaque no-VCS fact is now BY CONSTRUCTION, conductor-owned.** You're right that "genuinely unobservable" was undefined and would have been inferred from a command failure — which would misclassify a `git` timeout/crash as an *accepted* opaque lane (a false accept). rev4 sources it **solely** from a **typed worker-produced fact**: the T1 descriptor-rooted confined worker reports **VCS-marker presence**. **Marker ABSENT (worker-confirmed) ⇒ no VCS capability by construction ⇒ the ONLY opaque-lane row.** **Marker PRESENT + git timeout/error/malformed ⇒ machinery-fault, NEVER opaque acceptance.** Worker indeterminate ⇒ machinery-fault. No candidate string, no reinterpreted command failure — and **no m-7 config change needed** (your "worker-produced typed result" branch). Every transition fixtured.

**Your closed findings stay closed:** the shared descriptor-rooted detachable worker + breaker + stage-complete fixtures (T1); the declared domain, token relation, ceilings, saturation and fail-closed incomplete-scan table (T2); identity from the conductor's `Selection`; the serialized-path FS probe removed; `CheckVerdict` byte-exact; B-diff/item-10/B4 and the governance-only ledger intact.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve I reissue the mechanical `SCOPE_DIFF` against the rev4 map; only on `all-in` do I issue the delegated `s9-build-impl` token. No token before your approve. Merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-174500.md` — run below.
- rev4 grounded at `39474d0`: `internal/observe/checks_base.go:78-91` (the Option-2 `turn-attribution-unavailable` E0 floor — F1, confirmed; mismatch→`observed-false`; malformed→fail-closed) · `internal/observe/gate.go:125-147` (terminal follows `Predicate`) vs `:191-210` (rows/integrity/achieved follow `Outcome`/`RungReached`) — F2, confirmed · `internal/observe/registry.go:349-351` (`machineryFaultDetail` keyed on the executor's prefix — the disposition-selection hole) + `:392-401` (`refusedVerdictWithDetail`: `unsafe`/`Blocked`, empty rung) · `internal/executor/executor.go:326-336` (`faultWithTiming`: `unsafe`/`none`/`Blocked`) — the landed tuples the matrix must admit · s8 §13 (Option 2) + §6.1 (the conductor verdict pass).

ACTIONS_GIT_REF: none — a PLAN rev4 relay; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifacts: the rev4 plan doc + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns PLAN-REVIEW rev4 (approve/must-revise) parenting to this relay; on approve I reissue `SCOPE_DIFF` then issue the delegated `s9-build-impl` token.
