## PLAN rev6 — SUPERSEDES rev5 in your queue: m-7 answered the `vcs_kind` route with (1) — it lands additively in s9 as governed `lane_vcs` (engine v2→v3) — and REJECTED one clause of my proposal, correctly: I had asked them to fail composition on a declared-`git`-but-not-a-repo lane, which needs either a subprocess in the TCB load path or the exact ancestor-blind marker inference my own F3 had just retracted. Clause WITHDRAWN. T4 now binds to `supply.lane_vcs[lane]` (no inference anywhere); the operator-trust residual is STATED as D5-class with the distinction from my F3 bug made load-bearing; the fence licenses m-7's five loci under the s10 T1 pattern. Requesting PLAN-REVIEW on rev6.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-180000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev6 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — review THIS instead of rev5; your F3 route landed the governed fact, and the owner's refusal of my composition-probe clause caught me making the same inference error one finding later

m-3.implementer — **rev6 supersedes rev5**; review this one. rev5's F1/F2/F3 folds are unchanged and intact (the pass-warning `output-truncated`, the conductor origin envelope + the `(origin × tuple)` table, the retraction of the marker inference). What changed is **T4's source of truth**, because **m-7 answered** (`s9-vcskind-m7/SITREP-planner-20260713-160743`).

**m-7 answered (1): the governed fact lands ADDITIVELY in s9.** Engine **v2→v3** adds **`lane_vcs: map[lane-id]("git"|"none")`** — a **fourth top-level supply key**, *not* a lane-descriptor field (there is no descriptor object at `39474d0`; `lane_roots` is a flat id→root map and every landed consumer stays byte-untouched). **T4 now binds to `supply.lane_vcs[lane]` — a declared fact, with no inference anywhere in the path.** `none` ⇒ the **only** opaque-lane accepted row; `git` ⇒ **any** observation failure (timeout, exec error, malformed output, worker indeterminacy) is machinery-fault, never opaque acceptance.

**m-7 REJECTED one clause of my proposal, and they were right — this is the part worth your scrutiny.** My COORD asked that composition **fail** when a lane declares `git` but is not a valid repository. m-7 refused: satisfying it needs either **(a)** executing git inside the trusted config-load path (a new subprocess surface in the TCB) or **(b)** a filesystem-marker heuristic — **and (b) is the exact ancestor-blind inference I had retracted one finding earlier.** I had just learned that capability cannot be inferred from the filesystem, and then asked the config owner to infer it. **Clause withdrawn. Composition validates SHAPE, not TRUTH.**

**The residual is now STATED, not engineered away — and I have made the distinction from my F3 bug load-bearing, because the two produce similar-looking outcomes and must not be collapsed:**
- **A false `git`** (declared git, no real vantage) **degrades fail-closed** under T4's own rule — machinery-fault, never acceptance. New fixture: `TestFalseGitDeclarationDegradesFailClosedNeverOpaque`.
- **A false `none`** (an observable lane declared opaque) can only **suppress observation to a labeled `self_reported`/E0 degrade — it can NEVER fabricate a verified pass**, only an honestly-unverified one; and it is an operator-authored, digest-covered, §7-transitioned **trusted-config statement**, the same trust class as `lane_roots` pointing at the right directory at all.
- **The distinction:** **F3 was the CONDUCTOR silently mis-inferring capability on an ordinary, correct config** — machine-authored, invisible, unbounded, firing on a perfectly normal subdirectory-rooted lane. **`lane_vcs` is the OPERATOR declaring their own topology in the TCB's trusted input** — visible, deliberate, owned. Config is the TCB's input; the operator vouching for it is the system's floor, not a hole this field introduces. **Recorded as a D5-class residual.**

**Totality-at-v3 re-scopes my fail-closed token (a better token than I wrote).** A v3 store **cannot represent an undeclared lane** (declared for every lane or composition fails), so `check-machinery-vcs-capability-undeclared` now covers **exactly the pre-v3 interim** — a v2 store still loading — *undeclared-because-the-schema-predates-the-fact*, **never** undeclared-by-omission.

**The fence licenses m-7's five loci, under the s10 T1 pattern.** Rather than serialize T4 behind a separate m-7 leg, the engine v2→v3 amendment rides **inside this slice**: `validateEngineSchema` (v3 arm + ceiling) · `ValidateMemberTransition` (the v2→v3 hop) · the reader-marker call site · `composeSupply`/`validSupplyShape` (+ the `Supply` field) · the dogfood descriptor — **plus m-7's RED-first negatives** (absent `lane_vcs` at v3 · unknown enum · keyset mismatch both directions · v2 store still loads · v2→v3 accepted · v3→v2 and v1→v3 rejected). **Discipline: m-7 authors the exact owner bytes on the build seat's request; the build seat carries them VERBATIM; m-7 confirms end-of-slice. m-3 authors none of them and proposes no bytes in m-7's schema.**

**Effective condition + the interim, unweakened:** the schema is license on **m-7.implementer's approve** of m-7's sibling amendment leg. **Until then — and through any gap — s9 performs NO opaque acceptance at all** (fail-closed). T4's total input table, the restored **§13 Option-2 E0 floor**, the labeling machinery and the serialized-probe removal land regardless.

**Everything else from rev5 is unchanged:** the pass-warning (`output-truncated` preserved, never faulted) · the conductor origin envelope + the total `(origin × tuple) → class / MachineryFault / timing` table (cross-origin disposition-selection faulted both directions; `extended` only from the conductor's own expiry router) · the restored Option-2 floor · T1's shared detachable descriptor-rooted worker · T2's declared domain, ceilings and fail-closed incomplete-scan table · identity bound from `Selection` · the m-3-owned bounded, path-redacted output pass · `CheckVerdict` byte-exact · the master rulings, owner holds and governance-only ledger.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve I reissue the mechanical `SCOPE_DIFF` against the rev6 map (which now includes the m-7-owned config loci); only on `all-in` do I issue the delegated `s9-build-impl` token. No token before your approve. Merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-185000.md` — run below.
- rev6 grounded on: **m-7's owner answer** `s9-vcskind-m7/SITREP-planner-20260713-160743` (all six points; my acceptance filed at `…-184500`) · `frank@39474d0` `config.go:483-500` (`composeSupply` — any clean abs dir; the ground for "capability is not inferable from m-7's surface") + `:595-640`/`:255-266`/`:313` (the version machinery m-7's v3 arm rides) · rev5's grounding, unchanged (`executor.go:308-322` the truncated-pass · `registry.go:349-351` the prefix-keyed disposition hole · `gate.go:125-147`/`:191-210` the two axes · `checks_base.go:78-91` the Option-2 floor).

ACTIONS_GIT_REF: none — a PLAN rev6 relay; no `frank/` edit, no commit, no token, no merge, no lock move. Artifacts: the rev6 plan doc + this relay + my m-7 acceptance relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns PLAN-REVIEW on **rev6** (rev5 is superseded — do not review it); on approve I reissue `SCOPE_DIFF` then issue the delegated `s9-build-impl` token. m-7's amendment leg runs in parallel; my fail-closed interim holds until their schema is license.
