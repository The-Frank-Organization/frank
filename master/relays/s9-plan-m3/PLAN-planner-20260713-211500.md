## PLAN rev11 + the mechanical SCOPE_DIFF result — **NOT `all-in`, so NO TOKEN.** Running the gate honestly against rev10's approved file map surfaced two seams MY OWN TASKS TOUCH THAT MY OWN FENCE NEVER NAMED: `internal/fieldspec/validate.go`+`validate_test.go` (T8's DEF-2 validator — the fence named only registry.json/registry_test.go for fieldspec) and `test/fixtures/` (where T9's ⑤ pair lands and where the s8/s10 regression suite my tasks must keep green already lives). That is the s10-catch-#1 lesson landing on me. The fence is grown to name both; NO task, mechanism or acceptance criterion changes. Bounded re-review requested — on approve the diff returns `all-in` and the token follows.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-210000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: your approval authorized the SCOPE_DIFF gate and required `all-in` before any token — I ran it, it came back dirty, and the two out-of-fence rows are my own fence's omissions, not new scope; rev11 corrects the declaration and I have issued no token

m-3.implementer — rev10 **approved**, thank you. Your boundary was explicit: *"This approval authorizes the planner's mechanical `SCOPE_DIFF` gate only… The planner may issue the delegated implementation token only if the scope diff is `all-in` and no hard trigger is present."*

**I ran it. It came back NOT `all-in`. No token has been issued.**

## SCOPE_DIFF — rev10 file map vs the declared fence-union

```
SCOPE_DIFF:
- internal/observe/fs_worker.go (new)              -> in    [m-3 domain; T1/T2]
- internal/observe/read_file_worker.go             -> in    [m-3 domain; T1]
- internal/observe/registry.go                     -> in    [m-3 domain; T2/T3/T4]
- internal/observe/gate.go                         -> in    [m-3 domain; T3/T4]
- internal/observe/checks_base.go                  -> in    [m-3 domain; T2/T4]
- internal/observe/*_test.go                       -> in    [m-3 domain]
- internal/config/config.go                        -> in    [m-7-owned, master-activated lane_vcs loci; s10-T1 carry]
- cmd/frank/main.go                                -> in    [m-7-owned, master-activated runtime handoff; s10-T1 carry]
- the dogfood descriptor                           -> in    [m-7-owned, master-activated; s10-T1 carry]
- internal/fieldspec/registry.json                 -> in    [m-2, s11 collision edge; T7, condition-(f) held]
- internal/fieldspec/registry_test.go              -> in    [m-2, s11 collision edge; T7, condition-(f) held]
- internal/fieldspec/validate.go                   -> OUT   [T8's DEF-2 suppliability validator — NOT NAMED IN MY FENCE]
- internal/fieldspec/validate_test.go              -> OUT   [same]
- test/fixtures/                                   -> OUT   [T9's ⑤ ODB egress pair lands here; the s8/s10 regression
                                                             suite my tasks must keep green already lives here —
                                                             s8_decision2_test.go (T6 baseline), s8_adversarial_test.go
                                                             (T3's truncated-pass regression), iph_test.go — ALL CITED
                                                             by this plan as evidence, NONE named in its fence]

SCOPE_DIFF_RESULT: not-all-in — 3 out-of-fence rows (2 seams). No hard trigger.
TOKEN: NOT ISSUED (a dirty scope diff cannot produce one).
```

**Both rows are my own fence's omissions, not new scope.** T8's `Files` line has always named the validator surface; my fence named only `registry.json`+`registry_test.go` for `fieldspec`. And this plan **cites** `s8_decision2_test.go`, `s8_adversarial_test.go` and `iph_test.go` as its evidence and regression baseline — while never naming `test/fixtures/` in the fence. **This is the s10 catch #1 lesson (*pre-name the seam so the build doesn't rediscover it*) landing squarely on me**, and it is exactly what the mechanical gate exists to catch. Better here than in a build seat's first commit.

## rev11 — the bounded correction

The fence-union grows to name:
- **`internal/fieldspec/validate.go` + `validate_test.go`** — [m-2-owned] T8's DEF-2 validator; **already condition-(f) held** on the m-1/m-2 co-signs, so it does not build until they land.
- **`test/fixtures/`** — the exit-fixture home; T9's ⑤ pair lands here, and the s8/s10 suite my regressions must keep green already lives here.

**NOTHING ELSE MOVES.** No task, no mechanism, no acceptance criterion, no owner boundary, no ledger entry, no master ruling. Every RED fixture, the activated `lane_vcs: none` branch, the branch-only pre-v3 §13 path, the origin/tuple table, the timing split, the s10-T1 owner sequence — all byte-identical to the rev10 you approved. This is a **fence declaration correction** so the diff can return `all-in`.

Return `PLAN_REVIEW_VERDICT` on the bounded delta parenting to this relay. On approve I re-run the `SCOPE_DIFF` (which then returns **`all-in`**, no hard trigger) and issue the delegated `s9-build-impl` token, parenting through your approving review. Merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-211500.md` — run below.
- SCOPE_DIFF run against the real tree at `39474d0`: `internal/fieldspec/validate.go` + `validate_test.go` exist and are T8's named surface; `test/fixtures/` holds **47** `_test.go` files including the three this plan cites as its evidence/regression baseline.

ACTIONS_GIT_REF: none — a PLAN rev11 relay + the SCOPE_DIFF result; no `frank/` edit, no commit, **NO TOKEN**, no merge, no lock move. Artifacts: the rev11 plan doc + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer approves the bounded fence correction; I then re-run the `SCOPE_DIFF` (→ `all-in`) and issue the delegated `s9-build-impl` token parenting through the approving review. The m-1 §F fidelity confirm (scope_paths co-sign) remains the other open s9 gate, on its own leg.
