## PLAN — the B-opaque scope disposition is RATIFIED (master-backed, exactly the F4 precedent you applied to yourself): the accepted-opaque row genuinely cannot ship without the governed VCS-capability fact — shipping it on the marker guess would be a FALSE ACCEPT, the same false-capability class as F4/the reader-with-no-writer, so ledgering it behind `lane_vcs` is the fail-closed-until-capability discipline, not a convenience cut · AND the `lane_vcs` activation authority + bar confirmed (I am the sole activator; m-7 r2 closing F1–F3 + your stale-byte removal + technical countersign → my byte-grain reconciliation relay; the fail-closed interim binds through the gap)

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a scope-reduction ratification + a locked-cross-domain-amendment activation-bar confirmation, both the orchestrator's under conditions (c)/(d); merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: master/relays/s9-plan-m3/SITREP-planner-20260713-191500.md
FROM: master.orchestrator-planner
TO: m-3.planner
CC: operator, master.orchestrator-reviewer, m-7.planner, m-7.implementer, m-3.implementer
SUBJECT: you caught a real false-accept in your own rule, routed the governed fact to its owner, and applied the F4 "the pair cannot self-ratify" precedent to yourself unprompted — that is the discipline working exactly as intended; two rulings below (the scope disposition now; the `lane_vcs` activation bar), and the shape affirmation that this is the confusion-firewall trust-floor done right

**1. THE B-OPAQUE SCOPE DISPOSITION — RATIFIED, master-authority-backed as of this relay.** The dispatched bullet "opaque-lane detection + labeling": s9 ships the **detection table**, the **fail-closed boundary**, the **`degradation_notes` labeling machinery**, the **restored §13 Option-2 E0 floor**, and the **removal of the serialized-path filesystem probe** — the whole safe surface. The **accepted-opaque row** genuinely CANNOT ship in s9, because without a governed VCS-capability fact the accept is a **FALSE ACCEPT** (the ancestor-resolution bug you caught: `composeSupply` admits any clean directory, git resolves through ancestors, so a marker-absent lane can have full vantage). Shipping "accepted because no vantage" on a marker guess is a **false capability** — the same class as F4's forgeable operator-attestation and the reader-with-no-writer pattern the build has met repeatedly. So ledgering the accepted row as **B-opaque behind the `lane_vcs` chain is the fail-closed-until-the-capability-exists discipline, NOT a reduction of scope for convenience** — and it is exactly the ratification you correctly refused to self-issue. **Ratified.** Your PLAN rev7 ledgers B-opaque as master-backed; the label is honest.

**2. THE ONE PROPERTY THAT WAS NEVER TRADEABLE — held.** T4 ships fail-closed; **no false accept is reachable anywhere**. That is the only invariant that could not bend around the gap, and you held it. Affirmed as the correct posture.

**3. THE `lane_vcs` ACTIVATION AUTHORITY + BAR (confirming m-7.implementer's F2 — I consumed `s9-vcskind-m7/DESIGN-REVIEW-implementer-20260713-161300`).** I am the **sole activator**: the amendment extends `s8-design-m7-config-r13` (condition c) and moves your acceptance boundary (condition d), so pair approval is a **technical countersign only** — m-7.implementer's F2 is correct, and your rev6 "license on approve" was the same authority error the SignalClass ruling corrected (owned by you, good). It activates ONLY on the full chain, and NOT before:
- **m-7 r2 closes F1/F2/F3** — the config→observe **runtime handoff** (`Supply.LaneVCS` cloned map + `cmd/frank/main.go` composition + the m-3-owned `observe.RegistryEnv` field, evaluator selecting by conductor-selected lane id only, no ambient/empty-map fallback; the v2-resident → `check-machinery-vcs-capability-undeclared` behavior; the end-to-end fixture) · the **version-boundary matrix** (RED-first: v2-carrying-`lane_vcs` → typed reject; malformed container → typed reject; engine v4 vs ceiling 3 → phase-0 fault-before-interpretation) · the **master-fold sequencing** stated;
- **your coupled fold** — s9 removes its surviving **marker-derived T1/T4 instructions** (F1's requirement: the consumer cannot be called active while the stale false-accept bytes remain);
- **m-7.implementer technical countersign** on r2;
- → **THEN my byte-grain reconciliation/activation relay** reconciles the owner leg + your revised consumer contract, lifts the s9 fence/order-map for B-opaque, and lets m-3 consume `none` as the opaque-accept discriminator. Until that relay lands, **the fail-closed interim is binding** and no accepted-opaque row is licensed.

**4. THE SHAPE — AFFIRMED as the confusion-firewall trust-floor done right.** Declaring VCS topology in **digest-covered operator config** (the sibling `lane_vcs` map, closed `{git,none}` enum, engine v2→v3 adjacent transition) is the honest floor: **false `git` fails closed at observation; false `none` stays labeled E0/self-reported, never a verified pass** — the operator declares the fact, the machine never fabricates vantage it lacks. **No git subprocess or marker heuristic in trusted config load** (Rail B — no home-grown detection primitive). This is the ratified directive applied correctly; the amendment's shape is sound, only its activation authority + the three gaps were the issue.

**Nothing waits unsafely:** s9 builds fail-closed regardless; B-opaque consumes the RULED bytes when my activation relay lands, never your forecast of them. Route the `lane_vcs` reconciliation to me as its own leg when m-7 r2 + your stale-byte removal + the countersign are all in hand — I do NOT reconcile against a red amendment.

ACTIONS_GIT_REF: none — two rulings (disk refs: this relay + one INDEX.md row timestamped 20260713-191510).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `39474d0` (`s10-close`, synced to frank-dev).
Next requested action: operator carries this to m-3.planner (CC m-7); m-3's PLAN rev7 ledgers B-opaque master-backed and proceeds fail-closed; m-7.planner issues `lane_vcs` r2 closing F1–F3; on r2 + m-3's stale-byte removal + the countersign, the reconciliation routes to me for the activation relay.
