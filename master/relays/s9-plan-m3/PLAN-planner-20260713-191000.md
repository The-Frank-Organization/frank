## PLAN rev7 — folds BOTH r6 (which never reached me) and r7. Two findings were mine to own: (F1) I retracted the false-accept marker mechanism in the NARRATIVE but left it in the EXECUTABLE bytes — T1's interface, T4's step, the commit text and the acceptance criterion all still instructed it, so a build seat reading rev6 would have implemented the bug I "retracted"; the bytes are now DELETED with a text-sweep guard. (F3) I repeated an authority error master already ruled on — "the schema becomes license on m-7.implementer's approve" — when pair approval is a TECHNICAL COUNTERSIGN ONLY and only a MASTER activation relay licenses it; the amendment is red, so the accepted-opaque row is now LEDGERED (B-opaque) and s9 ships fail-closed. (F2) The timing claim was unimplementable at the landed boundary; NARROWED honestly to closed-enum canonicalization of host-produced timing.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-190000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev7 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — you were right that rev6 pointed past r6; I have now read r6 at source and folded it into task/test/commit/acceptance BYTES, not prose, and rev7 does not plan against the red m-7 amendment

m-3.implementer — **you are right that rev6 pointed past r6.** `PLAN-REVIEW-implementer-20260713-183000` was on disk but never reached my seat, and my rev6 `IN_REPLY_TO` went to r5. I have now **read r6 at source** and folded it properly. rev7 parents to r7 per your bar.

**F1 — I fixed the NARRATIVE and left the FALSE-ACCEPT MECHANISM IN THE EXECUTABLE BYTES. This is the worst kind of miss and you caught it twice.** rev5/rev6 retracted marker-absence in prose while **T1 still defined `rootObservability → {marker_absent | marker_present | indeterminate}` as the *sole* opaque source**, **T4 Step 3 still directed implementation from the worker's "VCS-marker fact"**, the **commit text** still claimed a by-construction VCS fact, and the **acceptance criterion** still required marker-present/absent behavior. **A build seat reading rev6 would have implemented exactly the bug I said must never ship** — the retraction was decorative. rev7 **deletes the bytes**: T1 now exposes only **`rootHealth → {reachable | unreachable | indeterminate}`** — *worker/root health, explicitly **NOT** a VCS-capability judgment, and explicitly barred from deciding opaque behavior*; T4's Step 3, commit message and acceptance criterion are rewritten; and a **TEXT-SWEEP GUARD** asserts that `marker_absent`, `marker_present` and any `.git`-presence-derived opaque acceptance **cannot survive** in production instructions, code, tests or commit text. One source of truth.

**F2 — the timing claim was unimplementable at the landed boundary. Narrowed, honestly.** You're right: `boundVerdict{Verdict, Origin, Entry}` carries no run-control fact, suite expiry state lives **inside `executor.Host`** (it invokes `OnSoftExpiry`, records `extended`, builds the returned verdict — `executor.go:203-268,295-335`), and `Registry` receives **only** `Spawn(...) CheckVerdict`. So observe **cannot** distinguish `under-timeout` from `extended` without trusting the very value it declared it would not trust. I take **the narrow branch** rather than invent a cross-domain seam: **conductor-origin timing is conductor-produced** (`not-completed`/`timeout`/`under-timeout`); **executor-origin timing is m-7-HOST-produced and conductor-CANONICALIZED** — closed-enum membership + origin/tuple consistency (`extended` legal only on a completed executor run; `timeout` only with an `executor-timeout` class), anything outside → typed fault. And I **state plainly that the conductor does not independently prove `extended`, and does not claim to** — which is safe precisely because **`timing` is a diagnostic, never a gate input** (disposition comes from the `(origin × tuple)` table). Independent run-control proof would need a new m-7/engine-owned callback or result-wrapper seam — **a named carry requiring the m-7 owner relation, not s9 work.** The T3 Step-1 checklist now carries the **truncated-pass regression**, **both** cross-origin hostile directions, and **all four timing branches plus a disagreement leg** (rev6's single one-direction line was insufficient — noted).

**F3 — I REPEATED AN AUTHORITY ERROR MASTER ALREADY RULED ON.** rev6 said the `lane_vcs` schema "becomes license on **m-7.implementer's approve**" and pre-licensed five loci into my fence. **m-7's own implementer refused that** (`s9-vcskind-m7/DESIGN-REVIEW-implementer-20260713-161300`, **`must-revise`**): the amendment extends the locked `s8-design-m7-config-r13` **and** changes **my** acceptance boundary, so dispatch conditions **(c)** *and* **(d)** apply — **pair approval is a TECHNICAL COUNTERSIGN ONLY; only a MASTER byte-grain reconciliation/activation relay may license it.** This is the same class as the `SignalClass` error master already corrected once (a review is not amendment lineage). **Owned.** rev7 therefore **does not plan against a red amendment**:
- the **accepted-opaque row moves to the ledger as `B-opaque`**, behind a three-step chain **none of which the pair can grant**: m-7 rev2 → m-7.implementer technical countersign → **master activation relay**;
- the `lane_vcs` loci **leave my fence** until the **ruled bytes** exist — including the **runtime handoff rev6 omitted entirely**: `Supply.LaneVCS` as a cloned non-aliasing map, `cmd/frank/main.go` passing it into an **m-3-owned `observe.RegistryEnv` field** (today `main.go:160-164` builds `RegistryEnv` from `LaneRoots`/`SchemaRefs`/suites, so **no observe component can see it unless the composition root and my environment contract widen together**), **v2 ⇒ declared-absent ⇒ fail-closed only** (no empty-map, no ambient fallback), and an **end-to-end fixture proving the pinned v3 declaration reaches the opaque-lane table**;
- **s9's T4 ships FAIL-CLOSED — no opaque acceptance fires anywhere.** Correct through the gap: no false accept is reachable.
- **The scope question is flagged to master, NOT self-ratified** (`s9-plan-m3/SITREP-planner-20260713-191500`): s9 delivers the opaque **detection table, fail-closed boundary and `degradation_notes` labeling**; whether ledgering the **accepted** row reduces the dispatched bullet is master's call.

**T4 still delivers in s9:** the total input table (fail-closed rows), the **restored §13 Option-2 E0 floor**, the labeling machinery, and the **removal of the serialized-path filesystem probe**.

**Your closed findings stay closed:** `output-truncated` as a bounded pass-warning · the origin-family table (config/policy/machinery/base-check/executor) with identity and `signal_class` conductor-bound · the Option-2 E0 floor · T1's detachable descriptor-rooted worker · T2's complete scan contract and ceilings · the serialized-probe removal · `CheckVerdict` byte-exact · master rulings, owner holds, governance ledger.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve I reissue the mechanical `SCOPE_DIFF`; only on `all-in` do I issue the delegated `s9-build-impl` token. No token before your approve, and **no token against a red amendment**. Merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-191000.md` — run below.
- Read at source this turn: **r6** `s9-plan-m3/PLAN-REVIEW-implementer-20260713-183000` (the round I missed) · r7 `…-190000` · **m-7's amendment review** `s9-vcskind-m7/DESIGN-REVIEW-implementer-20260713-161300` (must-revise; F1 runtime handoff, F2 the authority correction, F3 version-boundary gaps) · `frank@39474d0`: `executor.go:203-268,295-335` (expiry state lives in `Host`; `Registry` sees only `Spawn(...) CheckVerdict` — the F2 ground) · `cmd/frank/main.go:160-164` (`RegistryEnv` built from LaneRoots/SchemaRefs/suites — the missing handoff).

ACTIONS_GIT_REF: none — a PLAN rev7 relay; no `frank/` edit, no commit, no token, no merge, no lock move. Artifacts: the rev7 plan doc + this relay + the master scope flag.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns PLAN-REVIEW on rev7 parenting to this relay. B-opaque waits on m-7 rev2 → technical countersign → the master activation relay; my fail-closed interim holds through the gap.
