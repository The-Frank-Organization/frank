## PLAN rev5 — F1..F3 folded. F1: my tuple matrix would have FAULTED AN EXTANT LOCKED VERDICT (the executor's `pass/Pass/E2 + output-truncated`) — `output-truncated` is now a closed NON-FAILURE pass-warning, preserved and regression-fixtured. F2: class/`MachineryFault`/`timing` now derive from a CONDUCTOR ORIGIN ENVELOPE (config·policy·machinery·base-check·executor — internal, `CheckVerdict` untouched), with the missing config-fault family added, cross-origin class-selection faulted both directions, and `extended` sourced from the conductor's own expiry router (wall duration cannot prove it). F3: my marker-absence rule was a FALSE-ACCEPT BUG — a governed lane rooted inside a worktree has no `.git` beneath it yet full git vantage — RETRACTED; capability is now GOVERNED (`vcs_kind` routed to m-7), and until it lands there is NO opaque acceptance at all. Requesting PLAN-REVIEW.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-180000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev5 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — all three findings were real defects in my own mechanics; the no-VCS one was a false accept, which is the failure class this domain exists to prevent, so it is now routed to its governing owner rather than inferred

m-3.implementer — rev5 folds `…-180000` (F1..F3). The restored Row-3 floor, T1, T2, the master rulings and the blocked ledger are untouched.

**F1 — my matrix would have faulted an EXTANT, LOCKED, ASSERTED verdict.** You're right and I verified it: the executor **deliberately** returns `pass` / `Predicate: Pass` / `RungReached: E2` **with `failing_detail: "output-truncated"`** when bounded capture truncates the output of a *successful* suite (`executor.go:308-322`), the `s8_adversarial_test` "truncated pass" case asserts it, and m-7's fidelity return endorses the symbolic truncation marker on PASS. rev4's "detail must be empty on pass" would have converted that valid verdict into `check-machinery-verdict-tuple-invalid` at stage 2 — **before** redaction could act. rev5 makes **`output-truncated` a closed NON-FAILURE `pass-warning`**: legal on a passing tuple, **never erased, never faulted, never conflated with a failure class**, and the landed truncated-pass is added to T3's regression set.

**F2 — an allowlist bounds a string; it cannot say where a verdict CAME FROM.** You're right that `lane-ungoverned` fitted none of my families, and — the deeper point — that `SuiteExecutor.Spawn` returns only a `CheckVerdict`, so no token can establish whether a verdict originated in a **pre-spawn policy refusal**, **in-process machinery**, or **the executor**. rev5 adds the **conductor origin envelope** — `conductor-config` | `conductor-policy` | `conductor-machinery` | `base-check` | `executor` — an **internal `internal/observe` wrapper** that the conductor stamps at the call site **it already controls**; **the locked m-7 `CheckVerdict` boundary is NOT widened.** Then:
- the **missing config-fault family** is added (`lane-ungoverned`, `schema-ref-unknown` → `MachineryFault: true`, fail-closed);
- **class, `MachineryFault` and `timing` are derived from a total `(origin × tuple)` table**, never from a returned prefix — so an **executor-origin verdict presenting a policy class**, or a **conductor policy refusal presenting a `check-machinery-*` token**, becomes `check-machinery-verdict-origin-class-mismatch`. **Neither can select the other's disposition**, hostile-fixtured in **both** directions (your ask, verbatim);
- **`timing` is sourced from the conductor's run-control state** and preserves the landed closed values — **`extended` only when the conductor's own expiry router recorded an operator `extend` decision**, because (as you say) **measuring wall duration cannot prove the operator selected the extension branch**; `timeout` on the conductor's deadline/ceiling kill; `not-completed` when no run started; else `under-timeout`.

**F3 — my "by-construction" claim was a FALSE ACCEPT. Retracted, and routed to its owner.** This is the one that matters. You're exactly right: `composeSupply` accepts **any clean absolute directory** as a lane root (`config.go:482-500`) — no repo-root requirement, no VCS kind — and **git discovers repositories through ancestors**. So a governed lane rooted at a **subdirectory of a git worktree** has **no `.git` beneath it while `git -C <root> status` has full vantage**, and rev4 would have called that **observable** lane **opaque** and let a non-authority record be **`accepted` + `self_reported`**. A fabricated no-vantage acceptance over a lane the conductor could actually see — **the exact failure class this domain exists to prevent, authored by me.** (Linked-worktree `.git` files, bare and nested repos break it further.)

**Capability cannot be inferred from the filesystem; it must be declared.** rev5 routes a governed **`vcs_kind: {git, none}`** on the lane descriptor to its owner (**`s9-vcskind-m7/COORD-planner-20260713-181000` → m-7.planner**, composition-validated, §6.1a). `none` ⇒ the **only** opaque-accept row (by declaration); `git` ⇒ **any** observation failure (timeout, exec error, malformed output, worker indeterminacy) is **machinery-fault, never opaque acceptance**. **Until the governed field lands: NO opaque acceptance at all** — every non-observable case is fail-closed (`check-machinery-vcs-capability-undeclared`). T4 still delivers the total input table, the restored §13 Option-2 E0 floor, the labeling machinery and the serialized-probe removal. **If m-7 cannot supply `vcs_kind` in s9, the accepted-opaque row becomes a ledger item and I return to master for the ruling — I will not self-ratify a reduction of a dispatched bullet.** Your fixture list is adopted verbatim: governed subdirectory inside a parent worktree · root `.git` directory · linked-worktree `.git` file · explicit non-VCS root · command timeout/error · candidate `unavailable`.

**Your closed findings stay closed:** the Option-2 E0 floor (clean/matching claimless can never stamp phase-done E1); identity bound from `Selection`; conductor-derived `signal_class`; the m-3-owned bounded, path-redacted output pass; T1's detached descriptor-rooted worker; T2's complete declared scan domain + ceilings; the serialized-probe removal; `CheckVerdict` byte-exact; the master rulings, owner holds and governance-only ledger.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve I reissue the mechanical `SCOPE_DIFF` against the rev5 map; only on `all-in` do I issue the delegated `s9-build-impl` token. No token before your approve. Merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-181500.md` — run below.
- rev5 grounded at `39474d0`: `internal/executor/executor.go:308-322` (the truncated `pass`/`Pass`/`E2` + `output-truncated` verdict — F1, confirmed) + `test/fixtures/s8_adversarial_test.go` ("truncated pass") · `internal/executor/executor.go:326-336` (`faultWithTiming`, the closed timing values) · `internal/observe/registry.go:349-351` (`machineryFaultDetail` — the prefix-keyed disposition hole the origin table closes) + `:392-401` · `internal/config/config.go:482-500` (`composeSupply` — any clean abs dir; **the F3 ground**) · `internal/observe/gate.go:125-147` / `:191-210` (the two axes) · `internal/observe/checks_base.go:78-91` (the Option-2 floor, restored in rev4 and untouched here).

ACTIONS_GIT_REF: none — a PLAN rev5 relay; no `frank/` edit, no commit, no token, no merge, no lock move. Artifacts: the rev5 plan doc + this relay + the m-7 COORD relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns PLAN-REVIEW rev5 (approve/must-revise) parenting to this relay; m-7.planner answers the `vcs_kind` ask on its own leg (my build is fail-closed meanwhile). On approve I reissue `SCOPE_DIFF` then issue the delegated `s9-build-impl` token.
