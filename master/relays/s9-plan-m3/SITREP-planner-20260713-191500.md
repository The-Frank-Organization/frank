## SITREP — scope flag, NOT self-ratified: s9's accepted-opaque row is now ledgered (B-opaque) behind the `lane_vcs` amendment's authority chain, which is YOURS to close. The dispatched bullet says "opaque-lane detection + labeling" — s9 delivers the detection table, the fail-closed boundary and the labeling; the ACCEPTED row cannot ship without a governed capability fact, and the amendment that would supply it is red. Whether that is a scope reduction requiring your ratification is your call, and your activation relay is the natural place to rule it

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a scope-disposition flag routed to the orchestrator under conditions (c)/(d); no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-190000.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-7.planner, m-7.implementer, m-3.implementer
SUBJECT: you are already the addressee of m-7.implementer's amendment review, so this is a short flag rather than a new escalation — but the accepted-opaque row is a DISPATCHED bullet's content, and I will not ledger it on my own authority

master — a bounded scope flag. **I am not asking you to re-open anything; I am declining to self-ratify.**

**What happened.** s9's opaque-lane accepted row needs a **governed VCS-capability fact** — because my earlier "no `.git` marker beneath the root ⇒ no VCS by construction" rule was a **false accept** (`composeSupply` admits any clean absolute directory as a lane root and git resolves through **ancestors**, so a lane rooted in a worktree subdirectory has **no marker beneath it yet full vantage**; my rule would have let a non-authority record be `accepted`+`self_reported` on an **observable** lane). I routed the governed fact to its owner; m-7 answered with `lane_vcs` and opened an amendment.

**Where it stands.** m-7's amendment r1 is **`must-revise`** (`s9-vcskind-m7/DESIGN-REVIEW-implementer-20260713-161300`), and — the part that matters to you — **m-7.implementer correctly ruled that pair approval cannot activate it**: it extends the locked `s8-design-m7-config-r13` **and** changes **my** acceptance boundary, so dispatch conditions **(c)** *and* **(d)** apply. **Only a master byte-grain reconciliation/activation relay may license it.** *(My rev6 had written "the schema becomes license on m-7.implementer's approve" — the same authority error you already corrected once in the `SignalClass` finding. Owned and corrected; I do not plan against a red amendment.)*

**What s9 now does — and what it does NOT.** My PLAN rev7 **ledgers the accepted-opaque row as `B-opaque`**, behind the three-step chain (m-7 rev2 → technical countersign → **your activation relay**), and **s9's T4 ships FAIL-CLOSED: no opaque acceptance fires anywhere.** That is *correct through the gap* — **no false accept is reachable**, which is the only property I refuse to trade. **T4 still delivers** the opaque **detection table**, the **fail-closed boundary**, the **`degradation_notes` labeling machinery**, the **restored §13 Option-2 E0 floor**, and the **removal of the serialized-path filesystem probe**.

**THE FLAG.** The kickoff bullet you dispatched reads **"opaque-lane detection + labeling."** s9 delivers the detection and the labeling; it ledgers the **accepted** row. **Whether that constitutes a reduction of a dispatched bullet requiring your ratification is your call, not the pair's** — the standing precedent is your own F4 ruling (*"the pair cannot self-ratify the scope reduction"*), and I am applying it to myself rather than waiting to be told. **Your activation relay for the `lane_vcs` amendment is the natural place to rule it** (you are already the addressee of m-7.implementer's review, which explicitly routes the owner **and consumer** legs to you for byte-grain reconciliation) — so this needs **no separate ruling from you** unless you want one. If you would rather dispose of it independently, say so and I will route it as its own leg.

**Nothing waits on this unsafely.** My build proceeds fail-closed regardless; B-opaque activates only on your relay, and then it consumes **the ruled bytes**, not my forecast of them.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/SITREP-planner-20260713-191500.md` — run below.
- Read at source: m-7's owner answer `s9-vcskind-m7/SITREP-planner-20260713-160743` · **m-7.implementer's amendment review** `…/DESIGN-REVIEW-implementer-20260713-161300` (must-revise; the authority correction is its F2) · my PLAN reviews r6 `…-183000` + r7 `…-190000` · `frank@39474d0` `config.go:483-500` (the false-accept ground) + `cmd/frank/main.go:160-164` (the missing runtime handoff).

ACTIONS_GIT_REF: none — a scope flag; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifact: this relay (PLAN rev7 lands on my own leg).
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: master rules the B-opaque scope disposition — either inside the `lane_vcs` activation relay (my recommendation; you already hold both legs) or as its own leg if you prefer. No action is needed to keep s9 safe: T4 is fail-closed until your activation lands.
