## PLAN rev1 — s9 evidence-thicken: F1..F5 folded. Task 1 = the RESERVED `red→green-differential` entry with `{repro_target,parent_sha,fix_ref}` + a real two-run + a NAMED m-7 two-revision executor dependency + an explicit bounded `SignalClass` verdict field (additive §6 fold, NOT `FailingDetail`); Task 2 = the reserved family at byte grain (find-references buildable · diff-shape/test-files-unchanged item-10-gated); Task 5 = operator-attestation DEFERRED to B4 (no trusted writer), the lane-cannot-forge negative kept; Task 6 = VERIFICATION-ONLY (mixed already green at 39474d0); Task 9 = runnable exit set split from a master-authority-backed blocked ledger (no skip counted green); SCOPE_DIFF reissued, no token before approve. Re-requesting PLAN-REVIEW.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-142500.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev1 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — all five must-revise findings folded; the plan no longer claims RED→GREEN evidence it can't produce, pins the exact registry + verdict interface, gives operator-attestation a real gate, drops the already-green edit, and stops counting skips as coverage

m-3.implementer — rev1 folds your `…-142500` must-revise (F1..F5). You were right on all five — every one was a task claiming evidence the locked interfaces can't yet produce. The fixes:

**F1 (Task 1 relabels a green run-suite; it doesn't observe red→green).** rev1 replaces Task 1 with the **RESERVED locked entry `red→green-differential`** (`class:suite`, E2, `suite_bounded`) and its **exact closed params `{repro_target, parent_sha, fix_ref}`** (s8 §5 / registry table:66). The RED fixture proves **both runs** (RED-on-`parent_sha` → GREEN-on-`fix_ref`) and **fails** when the parent is green, the fix is red, either revision is unresolved, or the executor can't establish the transition. Ordinary `run-suite` can **never** acquire `signal_class:differential` — the label is conductor-stamped from an observed transition, **never a lane assertion**. **NAMED DEPENDENCY:** the two-revision execution is a NEW **m-7 executor** capability (checkout both revisions, run twice, return the transition) — coordinated; the m-3 side (entry + label + refusal + I-PH) lands first, the execution leg holds until m-7's capability lands.

**F2 (Tasks 2/3 defer the registry + verdict contract; `FailingDetail` is not a success enum; `CheckVerdict` has no `SignalClass`).** rev1 Task 2 lists the reserved family **at byte grain** (id/class/rung/timeout/closed-params/produces/host): `find-references` (E1 base — **buildable**, a tree read), `diff-shape` + `test-files-unchanged` (E1 base — **item-10-gated** for candidate-attribution), `red→green-differential` (Task 1). The m-5 archetype-tag→invariant mapping stays a **RESERVED seam** (not built s9). And a **new explicit bounded `SignalClass` field** (closed enum `{none, differential}`) is pinned on `CheckVerdict` + the claim row as an **additive §6 amendment** (small-tier fold, your review before locking) with closed-enum unknown-label refusal + I-PH tests — **`FailingDetail` is not overloaded**.

**F3 (Task 5 is a reader with no trusted writer).** Right — `baseStamps` hardcodes `"conductor"` and no input carries an operator-attestation signal; a lane-selected `operator` would be forgeable. rev1 **defers the positive operator branch to a new blocked item B4** (a trusted operator-attestation producer — likely folding into item-9's m-6 park+ODB operator-in-the-loop seam), and keeps only the honest buildable part: the **RED fixture that a lane CANNOT manufacture `attestation_source:operator`** (it's `owner:system` → `LaneSuppliedSystemField` reject) + the conductor default. Marker semantics alone are insufficient — the negative is the provenance guarantee.

**F4 (Task 6's RED is already green).** Confirmed at `39474d0`: `gate.go:191-219` computes `mixed`, `gate.go:126-130` routes `self_reported`+`mixed` through decision-②, `s8_decision2_test.go:13-46` proves authority-mixed→`held`. rev1 **reclassifies Task 6 as VERIFICATION-ONLY (no production commit)** — confirm coverage + that Task 3's row thicken didn't regress the rollup; a commit only if a genuine gap surfaces. No edit to reconfirm landed bytes.

**F5 (gated skips counted as exit coverage).** rev1 **splits runnable exit fixtures** (each EXECUTES a locked mechanism — Task 9 acceptance) **from a master-authority-backed blocked ledger** (the scope NFs, diff-shape/test-files-unchanged attributable, the differential execution, items 9/10, the operator positive — each an explicit named `t.Skip("blocked: …")`, **NOT counted green**). The slice-exit statement now says s9 reaches end-review on the runnable m-3 sides; the blocked ledger carries forward per master's sequencing.

**F6/point-6:** the mechanical `SCOPE_DIFF` is **reissued** against the rev1 file/task map; **no delegated token issues before your approve.**

**Your accepted ground preserved:** base/B11/B12/terminal-immutability/executor-isolation/I-PH/egress-fence carried; the `baseStamps` + `NewRegistry` same-file fence-union ordering; opaque-lane keeps the no-vantage-vs-machinery-fault distinction; Tasks 7/8 hold on the m-2/m-1 co-signs.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve + the reissued `SCOPE_DIFF` returning `all-in` with no hard trigger, I issue the delegated `s9-build-impl` `DISPATCH IMPL` token (condition (a): unique sub-ID); Tasks 7/8 additionally hold on the m-2/m-1 co-signs.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-152500.md` — run below.
- rev1 grounded on the landed bytes: `internal/observe/registry.go:76-84` (`CheckVerdict` — no `SignalClass`, F2) · `internal/observe/gate.go:191-219,126-130,245` (mixed/decision-②/`attestation_source` hardcode, F3/F4) · `test/fixtures/s8_decision2_test.go:13-46` (F4) · `internal/executor/executor.go:84-95` (`Spawn` — one run, F1's two-revision gap) · s8 §5 registry table:66 (the reserved differential + params) · c2 §5:103-107 (the reserved family + archetype seam).

ACTIONS_GIT_REF: none — a PLAN rev1 relay; no `frank/` edit, no commit, no token, no merge. Sole artifacts: the rev1 plan doc + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`, unpushed) per the s9 dispatch.
Next requested action: m-3.implementer returns PLAN-REVIEW rev1 (approve/must-revise) parenting to this relay; on approve + SCOPE_DIFF all-in I issue the delegated `s9-build-impl` token; the operator boots the s9 team this PLAN names. The scope_paths co-sign (`s9-scopepaths-m3` rev1) proceeds on its own leg.
