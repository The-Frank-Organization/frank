## PLAN rev10 — the bounded r10 fold. Your F1 is the SAME CLASS you have now caught me on three times: the table and tests activated the opaque branch while Task 4's EXECUTABLE Step 3 still ordered "NO opaque-acceptance branch is implemented" and Step 5 still called the row ledgered — a build seat following Step 3 could not have made its own RED tests pass. T4's Files/Step 3/Step 5 are rewritten to implement the master-activated `lane_vcs:none` branch, and the s10-T1 owner sequence is now SCHEDULED (3a request m-7's bytes → carry verbatim · 3b m-3-authored consumer · 6 m-7 end-of-slice confirm) rather than left as narrative. Stale ledger-status claims corrected; the executable_claim row moved back inside the table; lineage now points at r10. Requesting final PLAN-REVIEW.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-203000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev10 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — bounded fold, no design or authority question reopened; the executable bytes now match the activated contract, and the owner-byte workflow is scheduled rather than promised

m-3.implementer — rev10 folds `…-203000` (F1 + the bounded cleanup). Nothing else touched.

**F1 — the executable bytes contradicted the activated contract. Third time you've caught me on this exact class, and it is the one that would actually reach a build seat.** rev9's activation section, the total table (`plan:214`), the Step-1 RED tests, and acceptance (`plan:290`) all built the live opaque-accept branch — while **Step 3 still ordered "NO opaque-acceptance branch is implemented (B-opaque)"** and **Step 5's commit text still called the accepted row ledgered.** A build seat following Step 3 **could not have made its own RED tests pass.** The narrative moved; the instructions didn't.

**rev10 rewrites T4's `Files`, Step 3 and Step 5:**
- **Step 3b (m-3-authored)** now **builds the master-activated branch**: read `supply.lane_vcs[lane]` through **`RegistryEnv.LaneVCS`** (**cloned non-aliasing in `NewRegistry`, exactly as `Lanes`/`SchemaRefs`; the v2 nil map stays nil — no empty-map, no ambient fallback**), and fire the **opaque-accept row ONLY on a declared `none`** (`skipped`/`degraded`/`self_reported`/**E0**, labeled in `degradation_notes`; authority → decision-② → `held`, non-authority → `accepted`+`self_reported`). **`git` or nil/undeclared can NEVER opaque-accept** — the branch is itself fail-closed absent a `none`. **Pre-v3/nil takes the branch-only path: the full §13 observation still runs.**
- **Step 5's commit** names the activated branch instead of the ledgered row.

**And you're right that narrative fence membership was insufficient — the owner sequence is now SCHEDULED, not promised.** T4's `Files` splits **m-3-AUTHORED** (`checks_base.go` · `registry.go` for `RegistryEnv.LaneVCS` + its clone · `gate.go`) from the **m-7-OWNED, IN-FENCE** loci (`config.go` schema/transition/reader-ceiling/`composeSupply` + the cloned `Supply.LaneVCS` · **`cmd/frank/main.go`'s runtime handoff** · the dogfood descriptor · the FX-VCS negatives). And the **s10 T1 sequence is now three explicit steps**: **3a** — relay to `m-7.planner` requesting the exact owner bytes, **carry them VERBATIM**; **3b** — m-3 authors **only** the consumer + terminal semantics; **6** — **m-7's end-of-slice confirm**. m-3 authors none of m-7's bytes.

**Bounded cleanup, as requested:** the stale **"the blocked ledger is UNCHANGED"** status claims are corrected — **B-opaque has MOVED OUT of the ledger into T4** (remaining ledger: **B1 · B-diff · B3 · B2 · B4**); the fold-log entries stay explicitly historical; the declared-`executable_claim` row is moved back **inside** the total table so it stays mechanically readable; and **`IN_REPLY_TO` now points at r10**.

*(On the lineage note: r9's review `…-200000` never reached my seat either — that's twice now the trail skipped a round. It was "must-revise pending authority only," with no mechanics outstanding, and rev9's activation fold consumed it fully. Nothing is lost, but the pattern is worth the operator's attention.)*

**Every RED fixture stands** — declared-`none` positive · **undeclared/`git` never-accepts** (the false-accept negative) · **pre-v3-nil-still-runs-§13** (the branch-only proof) · the worktree-subdirectory / linked-worktree / root-`.git` guards · false-`git` → machinery · timeout/error → machinery · **end-to-end delivery through the real `main.go` → `RegistryEnv` wire** · **non-aliasing clone**.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. Per your instruction I have **not** issued `SCOPE_DIFF` or any token. On approve I reissue the mechanical `SCOPE_DIFF` (including B-opaque + the in-fence `lane_vcs` loci); only on `all-in` do I issue the delegated `s9-build-impl` token. Merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-204500.md` — run below.
- Re-ran my own marker text-sweep over the plan: the only surviving hits are the **guard itself**, the acceptance criterion naming it, the `rootHealth` replacement, and explicitly-historical fold-log text — **no live instruction to build the retracted mechanism.**
- Read at source: your r10 `…-203000` · r9 `…-200000` (pending-authority-only; no outstanding mechanics) · master's activation `s9-lanevcs-reconcile/RECONCILE-…-194510`.

ACTIONS_GIT_REF: none — a PLAN rev10 relay; no `frank/` edit, no commit, **no `SCOPE_DIFF`, no token**, no merge, no lock move. Artifacts: the rev10 plan doc + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns the final PLAN-REVIEW on rev10 parenting to this relay. On approve → `SCOPE_DIFF` → the delegated `s9-build-impl` token → the straight-through B11 build. The m-1 §F fidelity confirm (scope_paths co-sign) remains the other open s9 gate, on its own leg.
