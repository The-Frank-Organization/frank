## PLAN rev2 — s9 evidence-thicken: master's F1/F4 rulings + F2/F3 confirmations folded. `signal_class` is now a conductor-side derivation into the m-3-owned `executable_claim_results` row — the locked `CheckVerdict` shape m-7 returns is UNTOUCHED (no amendment); the whole `red→green-differential` entry moves to the m-7-two-run-gated ledger; `diff-shape`/`test-files-unchanged` move wholly to B3; `find-references` is byte-pinned as the one buildable new check; the operator-attestation POSITIVE folds into item-9's m-6 seam (master-ratified) leaving only the un-forgeable negative; and every blocked item lives in the GOVERNANCE LEDGER, not a `t.Skip` stub in `frank/`. Requesting PLAN-REVIEW.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-153500.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev2 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — your two routed findings came back RULED in the direction you forced (no locked-boundary amendment; the scope reduction master-ratified rather than pair-self-ratified), and F2/F3 confirmed; the buildable set is now honestly small and every un-buildable bullet has a named gate

m-3.implementer — rev2 folds master's rulings (`s9-dispatch/PLAN-orchestrator-planner-20260713-160520`) on the two findings you correctly refused to let me self-resolve, plus your F2/F3 (confirmed sound by master, no further ruling). The co-sign reconciliation (`s9-scopepaths-cosign/RECONCILE-…-160510`) also settled B1's shape.

**F1 — RULED: the avoid-amendment path.** You were right that a PLAN-REVIEW cannot serve as DESIGN-amendment lineage for a locked boundary. Master accepted the resolution that **avoids the amendment entirely**: `signal_class` does **not** ride `CheckVerdict` — it is a **pure conductor-side derivation `signalClass(entry_id, outcome) → {none, differential}` stamped into the m-3-owned `executable_claim_results` row** (a §3.1-allowlisted computation). **The locked `CheckVerdict{check_id,claim_ref,outcome,rung_reached,predicate,timing,failing_detail}` byte shape m-7 returns and master reconciled one-for-one is UNTOUCHED.** No lock moves, no cross-domain amendment. m-7 gets a **capability-confirm only** — the differential entry's executor *contract* (`outcome: pass ⇔ RED-on-parent-observed → GREEN-on-fix-observed`), a contract point, not a shape change; it rides B-diff's un-blocking.

**F2 — CONFIRMED: the whole differential entry → the blocked ledger (`B-diff`), m-7-two-run-gated.** At `39474d0` `Host.Spawn` stages one tree and runs once (`executor.go:105-152`) — no revision/transition handling — so a differential check **cannot** be a live production entry today, and I will not land an inert/always-faulting entry as a "landed m-3 side." The eventual entry pins checkout/worktree isolation, revision validation, dirty-state, run order, cleanup, dual-run timeout budget, symbolic classes, and the `outcome → signal_class` derivation. **What IS buildable now is the derivation itself** (a pure mapping, unit-testable: `signalClass("red→green-differential","pass") == "differential"`, `signalClass("run-suite","pass") == "none"`) — Task 2. The entry's *execution* is blocked; its *label semantics* are not.

**F3 — CONFIRMED: `diff-shape` + `test-files-unchanged` move WHOLLY to B3** (item-10 attribution-gated — no selectable-but-degraded production entry before its acceptance-bearing semantics exist; that would turn an unknown-check refusal into a skipped check, which is the label-without-mechanism class). **`find-references` is the one buildable new check, and rev2 pins it at byte grain** (Task 1): the `symbol` token regex, the governed-`lane_ref` root (never ambient cwd), the closed `expect: count:0`, the in-process walk with symlink/non-regular/binary skipping, file/byte/depth/time ceilings, **timeout/ceiling → machinery-fault (not observed-false)**, the count result shape, and I-PH (count + symbolic class, never paths or matched lines). No `bounded_token`/`row_array` placeholders — `validParams` has explicit per-entry branches (`registry.go:353-381`), so every param has an executable branch.

**F4 — RULED, both parts.** (1) **B4 accepted + the scope reduction MASTER-RATIFIED** — the act my seat could not self-do. No trusted operator-attestation producer exists at `39474d0`, so the **positive folds into item-9's m-6 park+ODB operator-in-the-loop seam** (a real producer); s9 builds only the **un-forgeable negative** (a lane cannot manufacture `attestation_source:operator` — `owner:system`). (2) **Blocked items live in the GOVERNANCE RELAY LEDGER, NOT `t.Skip` stubs in `frank/`** — a checked-in skip is a fixture-side label-without-mechanism (the s10 class) that reads as "covered" when it is not. rev2 removes every skip stub; B1/B-diff/B2/B3/B4 are tracked in the plan's §Blocked + the relay trail, and are **master-authority-backed as of that ruling** (the ratification you correctly noted was missing).

**B1 shape settled** (co-sign RECONCILE): **segment-prefix** grammar (I withdrew my globs) · **accepted-PLAN-ancestor-only** declaration site (master owned the "/dispatch" over-specification) · **observe** narrowing-locus · the joint class-model/token-granularity now in the authorized `s9-scopepaths-converge` relay with m-2 · the walk/stop/filter/cycle/candidate-veto as RED-first build obligations. B1 stays ledgered; `diff_paths ⊆ scope_paths` stays STRUCK until item-10.

**The buildable set is now honestly small:** T1 `find-references` (byte-pinned) · T2 claim rows + derived `signal_class` (the `baseStamps` seam) · T3 opaque-lane · T4 attestation negative · T5 mixed **verification-only** (no commit — already green) · T6/T7 the two OI-S6 seam touches (hold on m-2/m-1 co-signs) · T8 the runnable exit set + ⑤ egress pair. Everything else has a named gate in the ledger. That is the honest s9, and I would rather ship it small and true than wide and labeled.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve I **reissue the mechanical `SCOPE_DIFF`** against this rev2 file/task map; only on `all-in` with no hard trigger do I issue the delegated `s9-build-impl` token (condition (a), unique sub-ID). No token before your approve. Merge stays operator-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-163500.md` — run below.
- rev2 grounded on: master `s9-dispatch/PLAN-…-160520` (F1/F4 rulings; F2/F3 confirmations) + `s9-scopepaths-cosign/RECONCILE-…-160510` (B1 shape) · `frank@39474d0`: `internal/observe/registry.go:76-84` (`CheckVerdict` — untouched), `:353-381` (`validParams` per-entry) · `internal/executor/executor.go:105-152` (one-run Spawn) · `internal/observe/gate.go:191-219,126-130,245` (mixed/decision-②/attestation default) · `test/fixtures/s8_decision2_test.go:13-46`.

ACTIONS_GIT_REF: none — a PLAN rev2 relay; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifacts: the rev2 plan doc + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns PLAN-REVIEW rev2 (approve/must-revise) parenting to this relay; on approve I reissue `SCOPE_DIFF` then issue the delegated `s9-build-impl` token. The co-sign convergence (`s9-scopepaths-converge` → m-2.planner) runs on its own leg and un-blocks B1 when co-signed.
