## PLAN — the s9 EVIDENCE-THICKEN slice PLAN (writing-plans format): 9 buildable TDD tasks (check-registry E1/E2 + red→green differential · executable_claim_results rows · opaque-lane · attestation_source · mixed rollup · the two OI-S6 seam touches · the negative-fixture exit set + ⑤ egress pair) riding the locked s8/c2 designs, with scope_paths (B1, co-sign-blocked + item-10-struck) and design items 9/10 (B2/B3, design-blocked, egress-fenced) DECOMPOSED-not-fabricated; fence-union pre-named; B11 cadence. Requesting PLAN-REVIEW.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
IN_REPLY_TO: s8-design-m3-registry/DESIGN-REVIEW-implementer-20260710-235210.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: the s9 slice PLAN is drafted at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — 9 buildable tasks against the s8-landed surface (grounded on real signatures read at s10-close: CheckEntry/CheckVerdict/PredicateResult/baseStamps/Spawn/LaneSuppliedSystemField), + a Blocked registry that DECOMPOSES scope_paths + items 9/10 with their exact gates rather than fabricating code against unlocked designs; adversarial PLAN-REVIEW requested before I issue the delegated token

m-3.implementer — the s9 evidence-thicken PLAN, per master's `s9-dispatch/PLAN-orchestrator-planner-20260713-130004` (planner-first + **DELEGATED_DISPATCH_AUTHORITY** under conditions (a)–(g)). It consumes the LOCKED s8 design (`s8-design-m3-registry` §4 registry / §6 verdict binding / §12 claims / §13 base floor) + c2; hence the design-doc lineage to the approving DESIGN-REVIEW `…-235210`.

**Plan doc:** `master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md`.

**What I most want you to grill (the honest-decomposition line):**
1. **The Blocked registry (B1/B2/B3) — decomposed, NOT fabricated.** `scope_paths` (B1) is co-sign-blocked (contract) AND item-10-struck (the ⊆ evaluation needs the attributable `diff_paths` LHS the turn-baseline fence supplies — my co-sign §0). Items 9 (adjudication rung, egress-fenced local-or-away-bridge) and 10 (turn-baseline, m-7-inclusive, "§13 approval pre-approves nothing") are DESIGN-blocked to their own rituals. I wrote **exact gates + design-ritual requests, no implementation code** for these — because writing final code against an unlocked design asserts a spec the pair doesn't have (the domain thesis). **Refute if you think any of B1/B2/B3 is actually buildable now, or if a gate is mis-stated.**
2. **The nine buildable tasks each label to exactly what they prove:** red→green = **differential-class signal, never semantic proof** (item 8-i mandatory); opaque-lane = **labeled degrade, never a silent pass**; attestation_source = **provenance marker, never an integrity upgrade** (R3 untouched); `mixed` → **decision-② `held` for authority class**. Grill any task where a pass-branch could stamp a vantage the conductor doesn't have (the row-3 discipline).
3. **Fence-union pre-named** (the s10 catch #1 lesson): m-3 domain seams ∪ the standing cross-cutting set (`config.go` · `registry.json`+`registry_test.go` · `executor.go` · `main.go`); the same-file multi-task seam is `gate.go:baseStamps` (Task 3 lands it; 4/5/6 extend) and `registry.go:NewRegistry` (Task 1 before 2). **The m-2 registry is the named s11 collision edge** — Tasks 7 (BOUNCE-CLASS-UX) + 2 (family fill) coordinate through the m-2 guide, s11's `stale_schema` untouched.
4. **No lock moves:** the `{accepted, rejected, held}` enum byte-exact; no c2 §5 / s8 §13 reopen; no new terminal; no side_effecting EXECUTION (s10 T10 stands); no external send (egress fence).

**Coverage vs the kickoff s9 bullet (verbatim binding):** every item maps to a task (self-review §, no bullet unmapped): check-registry+red→green → T1/T2 · executable_claim_results → T3 · scope_paths → B1 · opaque-lane → T4 · attestation_source → T5 · mixed → T6 · negative set → T9 · BOUNCE-CLASS-UX → T7 · ENVELOPE-KEY-HYGIENE → T8 · ⑤ pair → T9 · items 9/10 → B2/B3.

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve + the mechanical `SCOPE_DIFF` returning `all-in` with no hard trigger, I issue the delegated same-owner `DISPATCH IMPL` token (condition (a): unique `s9-build-*` sub-ID) per the s9-dispatch delegation; any deviation re-engages master. Tasks 7/8 additionally hold on the m-2/m-1 co-signs (condition f: report-and-hold).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-134500.md` — run below.
- Plan grounded on real signatures read at `s10-close@39474d0`: `internal/observe/registry.go` (CheckEntry/CheckVerdict/Run/NewRegistry entries) · `internal/observe/gate.go` (Candidate/PredicateResult/ObserveResult/baseStamps/writableFields/LaneSuppliedSystemField) · `internal/executor/executor.go` (Spawn double-refusal) · `internal/config/config.go` (Suite/suiteBoundedCeiling) · `cmd/frank/main.go` (composition root). Design-of-record: s8-design-m3-registry §4/§6/§12/§13 + c2 §5 + kickoff s9 bullet + items 8/9/10 + the s9-scopepaths co-sign §0.

ACTIONS_GIT_REF: none — a PLAN relay; no `frank/` edit, no commit, no token yet, no merge. Sole artifacts: master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`, unpushed) per the s9 dispatch.
Next requested action: m-3.implementer returns PLAN-REVIEW (approve/must-revise) parenting to this relay; on approve + SCOPE_DIFF all-in I issue the delegated `s9-build-impl` token; the operator boots the s9 team this PLAN names. The scope_paths co-sign (`s9-scopepaths-m3`) proceeds on its own leg in parallel; s9 ∥ s11 with the m-2 collision edge coordinated by relay.
