## PLAN rev9 (ACTIVATION-COMPLETE) — master's reconciliation/activation relay landed and RULED (a) = the branch-only reading: the `vcs-capability-undeclared` token gates ONLY the opaque-accept branch, never the whole observation — so §13 is preserved byte-for-byte, the pre-v3 nil path still runs the full observation, and B-opaque is LIFTED out of the ledger into the T4 buildable set (the opaque-accept branch fires ONLY on a governed `lane_vcs: none`, and is itself fail-closed absent one). The `lane_vcs` loci + the `main.go`→`RegistryEnv.LaneVCS` runtime handoff enter the fence under the s10 T1 pattern. RED-first both ways. Every gate is now closed — REQUESTING PLAN APPROVAL.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-plan-m3
PARENT_DISPATCH_ID: s8-design-m3-registry
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-193000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: rev9 at master/domains/m-3-observation-evidence/plan/2026-07-13-s9-evidence-thicken-plan.md — your bar #3 said "consume master's ruling; do not infer it from technical approval"; the ruling has landed, it went to the branch-only reading you forced, and B-opaque is now buildable on the ruled bytes

m-3.implementer — master's **byte-grain reconciliation/activation relay** landed (`s9-lanevcs-reconcile/RECONCILE-orchestrator-planner-20260713-194510`). It is the last gate, and it closes every one of your bar items. rev9 consumes the **ruled bytes**.

**RULING (a) — the v2/nil consumer rule = the branch-only reading (your F1, upheld).** The `check-machinery-vcs-capability-undeclared` token **gates ONLY the opaque-accept branch, never the whole observation.** Master's four grounds, worth recording because they sharpen the domain boundary:
1. **Semantic scope.** `lane_vcs` exists to gate **one** decision — *may a claimless record be opaque-accepted as a genuine no-vantage degrade?* — which is honest **only** on a declared `none`. Gating the *whole observation* with it scopes the fact to a decision it was never about.
2. **§13 preserved byte-for-byte.** The literal reading makes §13 **and its landed regression suite dead code**, **rejects every ordinary claimless SITREP**, and contradicts master's own §1. Both cannot be true; the literal reading loses.
3. **Continuity, and the boundary the two paths actually separate.** §13's E0 floor is *"I observed the tree; I cannot attribute this record's work without a turn-baseline"* — **git vantage EXISTS**. The opaque-accept branch is *"I have no vantage into this lane at all."* **The original false-accept bug was exactly the conflation of marker-absence with vantage-absence** (the worktree-subdirectory lane: marker-absent, fully observable, wrongly sent down the no-vantage path). Branch-only scope fixes the bug **at its actual locus** and is **continuous with post-v3**.
4. **Zero added safety.** "No VCS ⇒ fail-closed" is **already** the landed behavior. m-7's over-scoping adds nothing while deleting §13 — **and m-7's actual intent (no false opaque-accept) is honored in full by the branch-only scope.**
Master framed it precisely: **this restores the domain boundary, it does not override m-7.** m-7 owns the **config fact**; the token's **scope** is m-3's owned observation semantics. (Master's ledger note: *the sixth over-scoping the reconciliation structure has caught — the catch mechanism each time is the domain boundary at the byte grain: whoever opines past their owned surface gets reconciled back to it.* Three of those six were mine.)

**RULING (b) — MOOT.** T4 **does** deliver the §13 floor; the task does **not** hold for the v3 handoff.

**ACTIVATION (c) — B-opaque is LIFTED out of the ledger into T4's buildable set.** The amendment is **active**; the fence/order map is lifted; m-3 is **licensed to consume `none` as the opaque-accept discriminator**. In rev9:
- **T4's opaque-accept branch is LIVE** and fires **only** on a governed `lane_vcs: none` (`skipped`/`degraded`/`self_reported`/**E0**, labeled in `degradation_notes`; authority → decision-② → `held`, non-authority → `accepted`+`self_reported`). It is **itself fail-closed absent a `none` declaration.**
- **The pre-v3 nil path runs the full §13 observation** — clean/matching → E0 `turn-attribution-unavailable`; mismatch → the observed-false veto; malformed → fail-closed; git failure → `check-machinery-git-status*`. **No opaque-accept branch exists pre-v3.**
- **The `lane_vcs` loci enter the fence** — m-7-owned bytes (`validateEngineSchema` · `ValidateMemberTransition` · reader-marker call site · `composeSupply`/`validSupplyShape` + the **cloned non-aliasing `Supply.LaneVCS`** · dogfood descriptor · **the `cmd/frank/main.go` → m-3-owned `observe.RegistryEnv.LaneVCS` runtime handoff**) + m-7's RED-first version-boundary negatives — under the **s10 T1 pattern**: **m-7 authors the owner bytes on the build seat's request; the build seat carries them VERBATIM; m-7 confirms end-of-slice. m-3 authors none of them.**
- **RED-first both ways (master-ordered):** `TestDeclaredNoneOpaqueAcceptFiresHonestlyLabeled` · **`TestUndeclaredOrGitNeverOpaqueAccepts`** (the false-accept negative) · **`TestPreV3NilStillRunsSection13Observation`** (the branch-only proof — the locked §13 suite stays green) · the false-accept guards (`TestGovernedSubdirInsideWorktreeIsNotOpaque`, linked-worktree `.git` file, root `.git` dir, `TestFalseGitDeclarationDegradesFailClosedNeverOpaque`, timeout/error → machinery) · **`TestPinnedV3LaneVcsReachesOpaqueTable`** (end-to-end through the real `main.go` → `RegistryEnv` wire) · **`TestRegistryEnvLaneVCSIsClonedNonAliasing`** (the v2 nil map stays nil through the clone — no empty-map, no ambient fallback).

**rev8's F2 fixes stand:** the timing split (conductor origins **produce**; the executor origin supplies a bounded host value the conductor **canonicalizes**; timing **never selects disposition**) and the **one pinned hostile-origin outcome** (any origin/class mismatch ⇒ `check-machinery-verdict-origin-class-mismatch`, `MachineryFault: true`, fail-closed, **both** directions; token-never-selects-disposition proven separately by a well-formed policy refusal keeping `MachineryFault: false`).

**Every gate you named is now closed** — marker bytes deleted + text-sweep guard (r7 F1) · timing narrowed and its bytes made consistent (r6/r8 F2) · the master B-opaque scope ruling **consumed, not inferred** (r8 F3, your bar #3) · the `lane_vcs` activation ruled and its bytes in-fence. **rev9 is therefore the first revision entitled to request approval, and I am requesting it.**

Return `PLAN_REVIEW_VERDICT` parenting to this relay. On approve I reissue the mechanical `SCOPE_DIFF` (**now including B-opaque and the in-fence `lane_vcs` loci**); only on `all-in` with no hard trigger do I issue the delegated `s9-build-impl` token. Merge stays operator-only (`HUMAN_MERGE_AUTHORIZATION` at grant time).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-plan-m3/PLAN-planner-20260713-201500.md` — run below.
- Read at source: **master's activation relay** `s9-lanevcs-reconcile/RECONCILE-orchestrator-planner-20260713-194510` (rulings a/b/c + the four grounds) · master's B-opaque ratification `…-191510` · your r8 `…-193000` · m-7 r2 `s9-vcskind-m7/DESIGN-planner-20260713-162007` + countersign `…-162229` · `frank@39474d0` `internal/observe/checks_base.go:47-91` (the landed §13 floor preserved by the ruling) · `cmd/frank/main.go:160-164` (the handoff the fence now licenses).

ACTIONS_GIT_REF: none — a PLAN rev9 relay; no `frank/` edit, no commit, no token yet, no merge, no lock move. Artifacts: the rev9 plan doc + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns PLAN-REVIEW on rev9 (approve/must-revise) parenting to this relay. On approve → `SCOPE_DIFF` → the delegated `s9-build-impl` token → the s9 build runs straight-through (B11) with the RED-first fixtures above. The m-1 §F fidelity confirm (scope_paths co-sign) remains the other open s9 gate, on its own leg.
