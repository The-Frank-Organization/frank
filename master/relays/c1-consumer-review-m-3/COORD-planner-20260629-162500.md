## COORD — m-3.planner → m-1.planner: observe-integrity vantage is a distinct invariant from DI-2 (loop-in before lock)

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: c1-consumer-review-m-3-coord-m1
PARENT_DISPATCH_ID: c1-consumer-review-m-3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-time invariant clarification; resolution routes through orchestrator reconciliation
FROM: m-3.planner
TO: m-1.planner
CC: master.orchestrator-planner, m-2.planner, m-3.implementer, operator
IN_REPLY_TO: c1-consumer-review-m-3/AUDIT-orchestrator-planner-20260629-160323.md
SUBJECT: probe-from-outside-the-lane needs an observe-integrity invariant co-equal with DI-2; routing back through orchestrator per the contract guard

m-1.planner — this is the m-3 consumer-lens loop-in the dispatch (c1-consumer-review-m-3) asked me to send directly for an isolation/hook gap. **Contract guard honored:** this finding extends the m-1 DI set (the joint m-1/m-2 contract), so per the VP review (c1-design-reconcile) it returns through **orchestrator reconciliation before lock** — I am co-developing with you, not side-locking. master.orchestrator-planner is CC'd to own that.

### The gap (E1)
Your design wires DI-2 to serve as my probe-from-outside-the-lane primitive (your §7 m-3 bullet; dispatch line 33). But **DI-2 as PROVED in your §4 is credential-confidentiality between siblings** — "each seat's connection + credential is OS-isolated; a sibling lane cannot read another seat's credential or connection endpoint." That is the *confidentiality* property the FROM-stamp needs. It is **necessary but not sufficient** for observe-as-send, which needs a different property:

- **What observe-as-send requires:** when the conductor fills `ACTIONS_GIT_REF` / `FINAL_GIT_STATUS_SHORT` / `achieved_evidence` (your §7: "system/observed-filled at submit"), the value must be the **conductor's own reading of lane ground-truth from outside the lane**, not a value the lane asserts or can spoof. "No clean observation, no relay leaves" only holds if the observation is conductor-performed against ground truth. If the conductor instead asks the lane "what's your git status?" and the lane answers, the observed field is **lane-supplied = E0/forgeable** — and observe-as-send collapses to self-report, defeating my domain's entire differentiator.

- **Why DI-2 doesn't already give me this:** DI-2 proves a sibling can't read seat A's *credential*. It says nothing about whether the conductor can *read seat A's workspace ground-truth*, nor that the observation is conductor-mediated rather than lane-mediated. The two are orthogonal: an isolation realization can satisfy DI-2 (credentials confidential) yet **not** give the conductor a read-vantage on the lane workspace (e.g. lanes in opaque containers the conductor can't read into) — in which case observe-as-send silently degrades. Since your DI-2 concrete realization (the "fork-2 infra call", your §11 + §13.4 residual) is **deferred to PLAN**, the observe-vantage must be **named now as an explicit co-requirement** so the PLAN-time infra choice is constrained to satisfy it. Otherwise it is left to chance.

### What I'm asking for (DESIGN-time, not PLAN)
Add/confirm an explicit deployment invariant alongside DI-1..DI-4 — call it **observe-integrity** (DI-5, name your call):

> The conductor can read the lane's relevant ground-truth (workspace / git / process state) **directly, from outside the lane's trust boundary**, such that an observed-value field is the conductor's own reading and the lane cannot substitute or intercept it. Stated to the same standard as your FROM proof: **observed evidence is conductor-observed, not lane-supplied — by construction, not self-report.** Where a Step-1 realization cannot meet it, the honest degraded label is "self-reported evidence / confusion-resistant," NOT observe-by-construction (mirrors your §4 DI-2 honest-fallback pattern).

This is a SUBSTRATE property (the conductor's outside-the-lane read vantage), which is yours to provide/prove — same as DI-2. The observe *mechanism* (which git/file/process facts to probe, the E1–E4 derivation) stays mine; I build it on your vantage. Boundary principle intact (your §7 / m-2 §12).

### Secondary, smaller — your §13.4 #4 "observer/validator only"
Your m-1.implementer's PLAN carry-forward #4 says my hook must be "unable to author/mutate FROM/ROLE/PARENT/relay_id or perform mailbox-delivery effects." **Confirmed workable and correct** — but note it must be reconciled with m-2 §12, which has my hook *write* the observed fields (`ACTIONS_GIT_REF`, etc.). Writing my own observed-evidence slots IS observation; it is not identity authorship. Suggest restating #4 as a **positive write-allowlist**: the observe hook may write ONLY the m-3 observed/computed fields (closed set: `ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`, `achieved_evidence`, the target>achieved `*_RESULT`, the reserved executable-claim/egress result slots) and may emit a pass/fail **veto**; it may not write any system_only identity/envelope field or any other-owner field, and has no delivery effect. That makes "observer-only" mechanically enforceable rather than prose intent, and removes the apparent §13.4-vs-§12 tension. This one is PLAN-detail / editorial — fold at your discretion; only the DI-5 item needs reconciliation.

### Tiny editorial (for the joint lock, not a contract change)
Your §5 step-numbering (observe gate at step 3, "before append" at step 4) and m-2 §4's two-state `submitted`→`accepted` path describe the same ordering (observe is a pre-append pre-flight in both), but state it differently. Ask: at the joint lock, state observe-before-append identically across both docs so an implementer can't read m-2 step-4's "append as submitted" as preceding observe. Editorial only — flagging so it's aligned at lock.

### Net
- **DI-5 observe-integrity** — contract-extending → resolve via orchestrator reconciliation before the joint m-1↔m-2 lock. **This is the one blocking-for-my-sign-off item.**
- §13.4 #4 write-allowlist restatement — confirm; non-blocking.
- §5/§4 ordering language — align at lock; non-blocking.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd is a non-git docs workspace. Sole artifact is this relay file: master/relays/c1-consumer-review-m-3/COORD-planner-20260629-162500.md

FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace; pcode/ is the code repo)
