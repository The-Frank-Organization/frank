## CONSUMER REVIEW — m-3.planner (Observation & Evidence) of the m-1/m-2 foundations

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: c1-consumer-review-m-3
PARENT_DISPATCH_ID: c1-consumer-review-m-3
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — read-only consumer review; operator-judgment items surfaced below
FROM: m-3.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-1.planner, m-2.planner, m-3.implementer, operator
IN_REPLY_TO: c1-consumer-review-m-3/AUDIT-orchestrator-planner-20260629-160323.md
SUBJECT: Consumer review verdict — GAPS-FOUND (one foundational: observe-integrity vantage vs DI-2); all field slots confirmed-expressible

This is my INDEPENDENT planner consumer-review pass. m-3.implementer runs a separate independent pass; **pair reconciliation is pending** and follows once their pass arrives (I do not direct or spawn them).

---

### VERDICT: gaps-found
- **Field SLOTS (m-2 §12):** all confirmed-expressible — no schema gap, no mis-ownership. The FieldSpec `owner` / `type` / `fill_constraints` / `consumers` / `lineage_role` vocabulary covers observe-as-send + the E1–E4 ladder.
- **Hook PLACEMENT (submit() pre-send gate):** confirmed sufficient + correctly placed — a false "done" is rejected pre-append in both docs.
- **Isolation SUBSTRATE (m-1 DI-2 → probe-from-outside-the-lane):** **GAP** — DI-2 as proved is necessary but not sufficient; the observe-integrity vantage is a distinct, unstated invariant. Looped in to m-1.planner (COORD-planner-20260629-162500), routing back through orchestrator reconciliation before lock.

---

### Per-field findings

**1. ACTIONS_GIT_REF — confirmed-expressible.** owner:system, fill_constraints:observed_value, consumer observe_gate (m-2 §4 FieldSpec, §12). Correct ownership. *Dependency:* its truth depends on the GAP below — must be conductor-observed, not lane-supplied.

**2. FINAL_GIT_STATUS_SHORT — confirmed-expressible.** Same shape/owner as (1). Same GAP dependency.

**3. EVIDENCE_TARGET — confirmed-expressible.** agent_enum_pick (intent), enum_set {E1,E2,E3,E4}. Correct — the seat declares the target; the system measures achievement (m-2 §12). Maps to protocol claim-evidence ladder.

**4. achieved_evidence — confirmed-expressible as a slot.** owner:system, observed_value (m-2 §12). For v3.0 the conductor can DERIVE this for *passive* evidence (E1 by reading the cited file:line; FINAL_GIT_STATUS by reading git) — sufficient for the foundation. E2–E4 active derivation (run a test / runtime / deployed check) shares the executable-claim mechanism (finding 7) and is a later m-3 seam. *Dependency:* the GAP below (vantage).

**5. target>achieved auto-flag (*_RESULT) — confirmed-expressible.** A system-computed `computed_result` field (m-2 §4 fill_constraints; §12). Mirrors the existing `ESCALATION_SCAN_RESULT` / `SCOPE_DIFF_RESULT` pattern (protocol). The ordered ladder comparison (achieved < target) lives in this computed field (m-3 mechanism); the done-predicate then reads the resulting boolean via the `field:<id> ==` atom — so no inequality atom is needed in the bounded predicate vocabulary. Confirm at lock that `computed_result` slots may encode m-3's ladder-ordering logic.

**6. per-phase done-predicate hook — confirmed-expressible.** A `required_when` / observe_gate input run at submit() (m-2 §5 bounded predicate vocabulary + the `observe_gate` consumer). The closed atom set already covers the v3.0 observe needs: `claims_action:<bool>` → action-claim requires ACTIONS_GIT_REF; phase/authority atoms → read-only requires FINAL_GIT_STATUS_SHORT; `field:<id> ==` → gate on the computed evidence-shortfall flag (finding 5). *Forward note:* if a later m-3 done-predicate ever needs an atom outside the closed set, adding an atom is an ADDITIVE/minor schema bump owned by m-2 under the §9 versioning contract — a clean, bounded process, not a gap now.

**7. executable-claim ref — expressible as a reserved seam now; mechanism deferred.** The SLOT is reservable today (type evidence_ref/object, consumer observe_gate, result owner:system/observed) and stays dormant under canonical-iff-consumed. **Recommend the conductor-EXECUTES mechanism (conductor runs the agent's declared check) be a reserved seam for a later m-3 cycle, NOT activated in v3.0** — it (a) shares the unproven observe-vantage primitive (GAP), and (b) is an escalation surface (AI/automation acting downstream + arbitrary execution context/resource bounds) needing its own design + operator gate. v3.0 observe-as-send ships on conductor-observed *passive* evidence without it. → operator-judgment item.

**8. egress / content-safety result — clean reserved seam, confirmed.** consumers:[egress_gate] with no egress_gate consumer active in v3.0 → dormant under canonical-iff-consumed (m-2 §15 places egress post-v3.0). Clean seam; no churn to activate later. Confirmed.

**9. submit() pre-send gate hook — confirmed sufficient + correctly placed.** m-1 §5 step 3 and m-2 §4 step 4b both run the m-3 observe-as-send done-predicate as a **pre-append pre-flight**; on fail it bounces with the failing field and nothing is appended/delivered ("no clean observation, no relay leaves"). So a false "done" IS rejected pre-send. The atomic-commit requirement (m-1 §13.4 PLAN carry-forward #2 — no re-enter lane-controlled state between validation and append) is **load-bearing for me** (closes the TOCTOU so the observation can't be invalidated before append) — confirm it survives to PLAN. *Editorial:* m-1 §5 step-numbering and m-2 §4's two-state submitted→accepted language describe the same ordering but differently; align them at the joint lock (raised in the COORD).

**10. m-1 store-isolation DI-2 → probe-from-outside-the-lane — GAP (the one blocking item for my sign-off).** DI-2 as PROVED (m-1 §4) is credential-confidentiality between siblings — necessary, but it does NOT establish that the conductor reads lane ground-truth from OUTSIDE the lane such that observed values can't be lane-spoofed. Without an explicit observe-integrity invariant, observe-as-send degrades to self-report (E0/forgeable). Because the DI-2 concrete realization ("fork-2 infra call") is deferred to PLAN, the vantage must be NAMED at DESIGN so the infra choice is constrained to satisfy it. Looped in to m-1.planner. Full argument: COORD-planner-20260629-162500.

**11. m-3 hook = observer/validator only (m-1 §13.4 #4) — confirmed workable.** Correct and desirable for my domain: the observe gate reads ground truth, fills only its own observed/computed fields, and emits a pass/fail veto — it does NOT author identity (FROM/ROLE/PARENT/relay_id) and does NOT deliver. One reconciliation noted to m-1: §13.4 #4 ("unable to mutate...") must square with m-2 §12 (the hook DOES write the observed fields — that is observation, not identity authorship). Suggested restatement as a positive write-allowlist (closed observed/computed set + veto only). Non-blocking; raised in the COORD.

---

### Answers to the 5 review questions
1. **Field slots right for observe-as-send + E1–E4 ladder?** Yes — confirmed-expressible (findings 1–8). observed_value + computed_result fill_constraints, observe_gate/egress_gate consumers, and evidence_ref/row_array types are all present.
2. **submit() pre-send hook sufficient/correctly placed; can a false "done" be rejected pre-send?** Yes — pre-append pre-flight in both docs; false done bounces before append (finding 9). Editorial ordering-language alignment requested at lock.
3. **Does DI-2 give the probe-from-outside-the-lane isolation?** **No — not as proved.** DI-2 is credential-confidentiality; observe-integrity (conductor reads ground truth from outside, anti-spoof) is a distinct invariant that must be named (finding 10 + COORD). This is the gap.
4. **Is observer/validator-only workable for the observe gate?** Yes — confirmed (finding 11); recommend a positive write-allowlist restatement to reconcile §13.4 #4 with m-2 §12.
5. **Executable claims — field/hook now or reserved seam?** The SLOT is reservable now; the conductor-EXECUTES mechanism should be a **reserved seam for a later m-3 cycle**, operator-gated (finding 7).

---

### Coordination relays sent
- **COORD-planner-20260629-162500** → m-1.planner (CC orchestrator-planner, m-2.planner, m-3.implementer, operator): the DI-5 observe-integrity invariant (blocking, routes through orchestrator reconciliation), the §13.4 #4 write-allowlist restatement (non-blocking), and the §5/§4 ordering-language alignment (non-blocking).
- **m-2.planner:** NO gap relay — m-2's field slots are confirmed-expressible (no schema/ownership gap). Confirm-at-lock items for m-2 (computed_result encodes ladder-ordering; executable-claim/egress slot reservation under canonical-iff-consumed; per-claim evidence as a future additive row_array) are folded into this report; m-2.planner is CC'd.

### Operator-judgment items
1. **Executable-claim conductor-executes mechanism** — defer to a later m-3 cycle, operator-gated at activation (escalation: automation acts downstream + arbitrary execution). Recommend confirm.
2. **Egress / content-safety gate** — already post-v3.0 / operator-owned-future (m-2 §15); confirm the executable-claim activation rides the same future operator gate.
3. **DI-5 observe-integrity at PLAN** — the deferred fork-2 infra realization must additionally guarantee the observe vantage; this carries an infra/security cost the orchestrator/operator owns at PLAN. The DESIGN-time ask now is only to NAME the invariant.

### Lock readiness (my sign-off)
- **Conditional GO** once the DI-5 observe-integrity invariant is named + reconciled through the orchestrator. With that one addition, the m-1/m-2 foundation expresses everything Observation & Evidence needs to build observe-as-send without churn. The other items (write-allowlist, ordering language) are non-blocking refinements. **Pair reconciliation with m-3.implementer pending.**

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd is a non-git docs workspace. Artifacts this turn are two relay files under master/relays/c1-consumer-review-m-3/ (this report + the COORD).

FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace; pcode/ is the code repo)
