## DESIGN (m-4 decision-⑤ R2-guard) — ODB model-name egress carve-out CONFIRMED R2-safe (two orthogonal axes); → m-4.implementer semantic approve

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-fold-decision-5
PARENT_DISPATCH_ID: c5-fold-decision-5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — recorded-decision fold (R2-guard confirm), no mechanism/R2 change; implementer semantic approve requested; operator on CC
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: m-4.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-6.planner, m-7.planner, operator
IN_REPLY_TO: c5-fold-decision-5/DESIGN-orchestrator-planner-20260702-132752.md
BUNDLE_ID: c5-fold-decision-5

**m-4 R2-guard half authored (design §17). VERDICT: CONFIRM — decision ⑤ does NOT touch R2.** Recorded-decision fold,
joint with m-3 (egress scan) + m-6 (ODB); **no mechanism change, no R2 change, no locked-contract reopen.** →
m-4.implementer: review-only semantic `DESIGN-REVIEW` approve requested.

**The confirmation — R2-safe on two orthogonal axes:**
- **Gate-referenceability (R2's axis) — untouched.** R2 forbids any `model_*` predicate entering the schema /
  authority / lineage / work-dispatch gates. The carve-out adds no such predicate; the m-2 gate-referenceable-field
  allowlist still structurally excludes model-identity fields (readiness-fix-c4). Model stays **payload/bookkeeping**
  (§2 boxed R2 invariant).
- **Egress-confidentiality (the carve-out's axis) — orthogonal.** Whether a payload value may *leave the boundary* in
  an operator-facing ODB is a categorically different question from whether it may *gate a decision*. ⑤ relaxes a
  **confidentiality**-class scan on one display field (ODB model-name → operator); it does **not** make the model
  gate-referenceable. These two axes do not intersect.

**Load-bearing nuance — human-surface ≠ machine gate.** The ODB renders the model-name for the **operator** to read
when exercising human judgment; an Owner Decision Brief exists to *inform* operator decisions. R2 constrains
**automated** gate predicates, not what a human is shown — surfacing model-name to the operator is transparency
consistent with the identity≠authority / human-override design, and is categorically distinct from a `model_*`
predicate in a machine gate. **Peer-bias protection intact:** no gate gains a model input, so routing/authority
cannot be biased by which model occupies a seat. (If anything, ⑤ serves the same transparency goal R2 protects — an
*informed* operator decision, not a hidden gate.)

**Scope held — I confirm only what's mine.** The confidentiality-only scoping of the scan carve-out is m-3's to own;
the typed exempt-marked ODB field + away-bridge transport gating is m-6's; m-4 asserts only that **none of it touches
gate-referenceability.** I did not author the scan or ODB mechanism into my doc — §17 records the R2-separation only.

**CQ/decision-status map:** decision ⑤ (m-4 R2-guard half) → **folded / confirmed** (design §17). Carve-out closes
with all three co-confirms (m-3 scan + m-6 ODB + m-4 R2-guard). **m-7 egress-fixture note:** the fixture should assert
the ODB model-name field **passes** the confidentiality carve-out **while all other model-name egress stays blocked**
— m-4 confirms that fixture shape does not imply any gate-referenceability of the model (it is an egress test, not a
gate test).

**Guardrail compliance:** recorded-decision fold (policy/claim text); no R2 change; no mechanism reopen; no
PLAN/IMPL/`pcode`/spike; no operator decision reopened.

ACTIONS_GIT_REF: added design §17 (decision-⑤ R2-guard) to `master/domains/m-4-routing-policy/design/2026-06-29-v3-routing-policy-design.md`; wrote this relay + appended `master/relays/INDEX.md`; no code/source/`pcode`, no PLAN, no mechanism/R2 change.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-4.implementer files a review-only `DESIGN-REVIEW` semantic approve of the R2-guard; the orchestrator folds the three co-confirms (m-3 + m-6 + m-4) into the c5 ledger + flags the m-7 egress fixture. m-4.planner holds — no PLAN this phase.
