## DESIGN — m-6 folds operator decision ⑤ (ODB model-name egress carve-out, m-6 half) into §3 → m-6.implementer semantic review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-fold-decision-5
PARENT_DISPATCH_ID: c5-fold-decision-5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — folding a recorded operator decision (⑤); joint carve-out; no R2 change
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-6.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-3.implementer, m-4.planner, m-4.implementer, m-7.planner
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: c5-fold-decision-5
OWNER: m-3 (egress scan) + m-6 (ODB) + m-4 (model field / R2) — joint
IN_REPLY_TO: c5-fold-decision-5/DESIGN-orchestrator-planner-20260702-132752.md

m-6.implementer — I authored the **m-6 half** of operator decision ⑤ (narrow ODB model-name egress carve-out) into §3. **Addressed review request** (you TO). Joint fold — closes only with **all three co-confirms** (m-3 scan + m-6 ODB + m-4 R2-guard).

### The m-6 half (§3 ODB render)
When an ODB legitimately surfaces a model-name to the operator (e.g. a routing/deviation brief), m-6 renders it in a **typed, exempt-marked field** (`record_kind = ODB`, operator-facing, field = model-name) so m-3's egress scan applies the **confidentiality-class carve-out only here**. Transport gating unchanged:
- **Non-away:** the ODB renders **locally and never egresses**.
- **Away-mode:** the away-bridge **opt-in + egress gate still govern the send**; the carve-out relaxes only the *confidentiality* scan on this one field — **not the opt-in, not the safety/content scan**.

### Boundary discipline (for your review)
- **R2 UNTOUCHED** — the model-name stays **bookkeeping/payload** in the render, **never a gate input**. This is an *egress-confidentiality* relaxation, **not** a *gate-referenceability* change (m-4 co-confirms; the load-bearing separation).
- **Narrow** — scoped to `record_kind=ODB` + field=model-name + destination=operator + confidentiality-class-only. Every other external send still blocks model-names; fail-closed default preserved elsewhere.
- **Flagged to m-7:** egress-fixture — the ODB model-name field passes the confidentiality carve-out; all other model-name egress still blocked.

### decision-status mapping
- **Decision ⑤ (m-6 half) → folded** (§3 typed exempt-marked ODB model-name field; transport gating; R2-untouched note). Global closure = **m-3 scan carve-out + m-4 R2-guard co-confirms** + the three implementer approves.

**Invariants preserved:** R2 intact (model never a gate input); the carve-out is confidentiality-scoped only (safety/content still blocks); away-bridge opt-in + egress gate unchanged; non-away ODB never egresses; no locked-contract mechanism reopen.

Blocked: no — holding for your semantic review + m-3/m-4 co-confirms.
Scope drift risk: none — the m-6 half only (ODB render field typing); no R2 change, no egress-mechanism authorship (m-3's), no gate-referenceability change.

Tests / verification: read-only fold; all claims E1 (register ⑤; §3 ODB render; m-3 egress `:116-122`; m-4 R2 §2.1).

ACTIONS_GIT_REF: folded `master/domains/m-6-human-surface-scheduler/design/2026-06-30-v3-human-surface-scheduler-design.md` (§3 ODB model-name egress carve-out — typed exempt-marked field + transport gating + R2-untouched note; STATUS line); wrote this relay + appended `master/relays/INDEX.md`; no R2 change, no code/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-6.implementer files a review-only semantic `DESIGN-REVIEW` approve; m-3 (scan carve-out) + m-4 (R2-guard) co-confirm; the CTO folds the three halves into the c5 ledger. m-6 holds.
