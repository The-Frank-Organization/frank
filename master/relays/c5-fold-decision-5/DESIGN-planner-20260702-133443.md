## DESIGN — m-3.planner decision-⑤ egress carve-out FOLDED (§7); m-3.implementer semantic review requested; m-6/m-4 co-confirm seam stated

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c5-fold-decision-5
PARENT_DISPATCH_ID: c5-decomp
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — fold of a recorded operator decision (policy/claim text); no R2 change, no mechanism reopen
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-4.planner, m-6.implementer, m-4.implementer, m-7.planner, operator
IN_REPLY_TO: c5-fold-decision-5/DESIGN-orchestrator-planner-20260702-132752.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: decision-⑤ folded (m-3 egress half) — typed confidentiality-only ODB model-name carve-out; R2 untouched; non-forgeable; away-bridge-gated; semantic review requested; needs m-6+m-4 co-confirms

m-3.implementer — my half of operator decision ⑤ is folded into §7 (+ §12 decision, §13 fixture, §15 fold-log). Fold of a recorded decision, claim/policy text — **no R2 change, no mechanism reopen.** Please file a review-only `PHASE: DESIGN-REVIEW` **semantic approve** (parent this relay, same `DESIGN_DOC_ID`).

### m-3 half folded (§7)
A **typed, narrow confidentiality-class exemption**:
- **Scope (all four must hold):** `record_kind == ODB` (operator-facing, **conductor-generated**) ∧ `field == model_name` (m-6's typed **exempt-marked** ODB field) ∧ `destination == operator` ∧ scan-class `== confidentiality`.
- **Confidentiality class ONLY** — the scan is split into **confidentiality** (undisclosed model-name → peer-bias) and **safety/content** (secrets / PII / auth-URLs). ⑤ exempts the one field from confidentiality only; **safety/content still fully scans the ODB** (a secret / PII / auth-URL in any ODB field still blocks).
- **Everything else unchanged** — model-names still block on every other record / field / destination; fail-closed `other→block` default preserved on all other paths.
- **Non-forgeable** — reaches only a **conductor-generated** ODB's exempt-marked field, never lane-supplied content (a lane cannot mark arbitrary bytes "exempt model-name" — that would be a confidentiality-scan bypass; the ODB is conductor-generated, the field non-lane-writable per §3.1).
- **Transport still gated** — away-mode: leaves only via the m-6 away-bridge opt-in; non-away: renders locally, never egresses (carve-out moot).
- **R2 untouched** — relaxes an *egress-confidentiality* scan on one operator-facing field; does **not** make the model a *gate input*; model stays payload / bookkeeping, never gate-referenceable.

### Cross-owner seam (co-confirms needed — VP c5 shape: closes only with all three)
- **m-6 (ODB):** my carve-out **keys on** m-6's typed exempt-marked model-name ODB field + the away-bridge opt-in gating transport. m-6 authors that field/render; I read its typing.
- **m-4 (R2-guard):** confirm the carve-out is confidentiality-scoped only and does not touch R2 (I assert it from the egress side; m-4 owns the R2 seal).
- **m-7:** flagged for the egress-fixture update — §13 now carries the fixture pair (ODB model-name **passes** confidentiality; non-ODB model-name, any-ODB secret/PII, and lane-supplied "exempt-marked" content all **still block**).

### Adversarial targets for your review
(a) Is the carve-out **airtight against a confidentiality-scan bypass** — can any lane-reachable path get arbitrary content treated as the exempt field? (My guard: conductor-generated ODB + non-lane-writable field + all-four-conditions AND.)
(b) Is the **confidentiality/safety split** correctly scoped — does anything a model-name field could carry (an embedded secret?) escape the still-active safety/content class? (My position: a model-name is a model name; safety/content still scans the whole ODB regardless.)
(c) Does the fold stay **claim/policy only** — no R2 change, no new gate-referenceable field, no mechanism reopen?
(d) Any interaction with the c5 claim-sweep §11 boundary or the always-block/J1 posture I missed?

### CQ/decision-status
**Decision ⑤ (m-3 egress half): folded — pending your semantic approve + m-6/m-4 co-confirms** for the joint close (per the dispatch: all three halves). I do not fold the c5 ledger (orchestrator's).

Claim/policy fold only — no R2 change, no locked-contract mechanism reopen, no PLAN/IMPL/`pcode`/spike, no operator decision reopened.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd (/Users/jack/Programming/harness) is a non-git docs workspace. Artifacts this turn: design-doc fold (§7 carve-out, §12 decision, §13 fixture, §15 fold-log) + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
