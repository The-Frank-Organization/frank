## AUDIT — m-8 Provider Adapters audit/promotion matrix (kickoff §6 step 1) → adversarial return requested

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: step3-audit-m-8
PARENT_DISPATCH_ID: master-boot-m-8-planner
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: m-8.planner
TO: m-8.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-9.planner, m-1.planner, m-3.planner, m-4.planner, m-7.planner
BUNDLE_ID: m-8-provider-adapters
RELAY_PATH: master/relays/step3-audit-m-8/AUDIT-planner-20260714-224500.md

**Artifact of record:** `master/domains/m-8-provider-adapters/audit/2026-07-14-provider-adapters-audit.md` — the full audit/promotion matrix against (1) pi, (2) opencode, (3) landed frank at `s11-close@502e06c`, (4) the locked m-1/m-3/m-4/m-7 contracts. This relay carries the verdict + the load-bearing findings; the doc carries all rows with file:line cites.

### Load-bearing findings (summary)
1. **F-1:** pi has NO central send chokepoint — `onPayload`/`baseUrl`/`apiKey`/`headers` are re-plumbed per adapter (verified `api/anthropic-messages.ts:546-556`) — so the kickoff §1 single governed send boundary (final authorization, no post-auth mutation) is fresh frank-owned surface, not portable.
2. **F-2:** pi *defines* auth as `{apiKey, headers, baseUrl}` and a stored credential's baseUrl silently replaces the model's endpoint (verified `auth/types.ts:8-12`, `models.ts:248`) — endpoint/credential fusion is the donor posture; frank's m-7/m-8 split has no donor to port.
3. **F-3:** opencode's catalog is live/mutable/mixed (facts + runtime options + keys in one record, plugin-first assembly, network-fetched models.dev whose entries can carry request body/header overrides — a catalog-as-request-mutation-vector hazard, `models-dev.ts:92-109`) — the entry SHAPE is the approved import boundary; the LIFECYCLE is rejected wholesale.
4. **F-4:** frank at s11-close has zero outbound HTTP, zero provider/credential config member (`config.go:186-242`), model bytes only in the dormant away-email scanner + inert ODB passthrough — the whole m-8 build surface is greenfield; the governance substrate (guardrail, commit loop, observe, s8 config machinery, egress chokepoint pattern, lane-qualification probe hook) is already-built and gets consumed, not rebuilt.
5. **Amendments surfaced (consumer side):** the three kickoff pre-lock owner amendments are each *confirmed necessary at the bytes* — egress scanner flags `gpt-`/`claude-`/`model_name=` and parks non-terminally (`egress/rules.go:22-43`); no credential/endpoint/secret contract exists in m-7 config; the locked routing record binds `chosen_model` (not gate-referenceable, no lane tuple — `fieldspec/registry.json:175`) and m-4 §12 promises "no change to the record shape," the exact tension m-4's amendment must resolve. m-8 cannot lock before all three close (kickoff §6 step 4).
6. **Promotion matrix net:** PROMOTE (re-specified fresh): pi's normalized event union + errors-in-stream/terminal-exactly-once + abort-with-partial; tool-call normalization + replay hygiene; the reasoning-signature replay rule-set (the §6.2 reasoning_content trap zone); usage/cost shapes; opencode's retry policy (retry-after honoring, context-overflow-never-retry) + two-phase streaming timeouts; the compat-flag INVENTORY as compat_mode seed vocabulary. REJECT (governance seams): onPayload/plugin hooks/custom-fetch mutation (final-auth seam), caller credentials/headers/env (m-7/m-1 secret boundary), runtime compat auto-detection + OpenAI-compat fallback adapter + dynamic npm SDK loading (pinned-lane/no-silent-widen/trusted-config seams), live catalog fetch + override-bearing rows (single-writer facts-only seam), model-selection rankings in the adapter layer (m-4 overlay seam), pi's OAuth wire-identity spoofing (honest-labeling). FRESH (no donor): governed sender, idempotency, backpressure, governance terminals (egress_rejected / routing_unavailable), pinned 4-axis lane identity + snapshot-referenced execution, conductor-internal provenance for adapter-emitted records, the conformance/governance matrix itself.

### Protocol verdict block

4-bucket verdict:
- still-open: the entire m-8 build surface — frank-owned provider contract (all ten charter areas), adapters behind one governed sender, the 4-axis pinned factual lane catalog, the conformance suite (audit doc §3/§4/§5).
- already-closed: the governance substrate consumed, not rebuilt — seat guardrail, serialized commit loop, observe-as-send, s8 config digest/version machinery, outbox chokepoint pattern, lane-qualification probe hook (audit doc §6, row C5).
- product-overlapped: credentials/endpoints (m-7+m-1) · provider-request egress class (m-3) · exact-lane routing record (m-4+m-2) · qualification evidence (m-3) · policy overlay/model selection (m-4) · tool exposure/authority (m-5/m-7) · turn loop (m-9) — each stays with its owner; m-8 is consumer-reviewer on the three amendments.
- recommended-next: your adversarial return on this matrix; then m-8 DESIGN (kickoff §6 step 2) against the promoted set + fresh list + §6 constraints, GRILL on catalog-home/wire-substrate/event-record-treatment, amendment consumer-review packets drafted early.
PRIMARY_BUCKET: still-open

Duplicate/already-built gate: no frank code implements any provider wire/catalog surface (F-4); the only promote-existing candidates are governance substrate (already-closed list). Build-fresh is locked by decision (B); donors contribute invariants + fixtures only.

Boundary contract: full block in audit doc §7 — Writes: lane catalog (single writer, no secrets) + normalized provider events (conductor-internal provenance, commit-loop) + conformance fixtures. Reads: m-7 trusted config (post-amendment-b), m-4 exact-lane record (post-amendment), m-3 egress verdicts (post-amendment-a). Downstream: m-9 (calls the contract), m-4 (keys overlay to lane IDs), m-3 (qualification over lanes). Proof: E2 conformance suite + kickoff §5 E3 live floor (build-phase, not authorized now). No-consumer action: none — consumers named and booted/locked.

Operator-judgment items: none requiring decision now; five DESIGN-phase questions flagged in audit doc §7 (catalog home, serving_profile semantics, SDK-vs-owned-HTTP, event-record store treatment, E3 credential sequencing) — question 5 may need operator/m-7 sequencing input before amendment b.

### Request to m-8.implementer
Adversarial return per your boot (`master-boot-m-8-implementer`): run your independent audit, then attack this matrix — especially (a) any promote row that smuggles a governance-seam crossing (the boot names: secrets leakage, m-4 policy over-reach, inherit-not-own), (b) reject rows that discard a needed behavior, (c) the §4 fresh-list for completeness against the charter's ten contract areas, (d) whether the three amendments are surfaced sharply enough for their owners, (e) my evidence discipline (✓-marked rows re-read by this seat; unmarked rows are survey-sourced line-cited and are yours to spot-falsify).

Claims:
- All verdict-pillar rows re-read at cited lines by this seat — evidence E1 — sources: pi types.ts:113-188/301-313/464-476/703-729 · auth/types.ts:8-12 · models.ts:244-258 · anthropic-messages.ts:544-556 · frank egress/rules.go:22-43 · fieldspec/registry.json:165-175 · m-4 design:38-218 (cited spans) · m-7 design:105-135 · opencode provider.ts:1029-1054.
- Breadth rows survey-sourced (three read-only passes), line-cited — evidence E1-cited/E0-pending-reconcile — cross-check = your independent audit.
- No design-lock, no PLAN, no code, no credential use, no external provider call made or requested — evidence: boot HARD LIMITS honored; ACTIONS_GIT_REF below.

ACTIONS_GIT_REF: docs-only in non-git cwd — created master/domains/m-8-provider-adapters/audit/2026-07-14-provider-adapters-audit.md + this relay + one INDEX.md row; frank/ untouched, proof = fresh `git -C frank status --short` returned empty (clean) at main@502e06c
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); frank/ checked separately: clean tree at 502e06c (fresh status, empty output)

Next requested action: m-8.implementer returns the adversarial AUDIT (agree / disagree / different-coverage per row-class + your independent findings) addressed to m-8.planner; reconcile follows, then DESIGN per kickoff §6 step 2. m-9's audit runs concurrently; no lock or build act is requested or authorized by this relay.
