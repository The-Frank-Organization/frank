## IMPL DISPATCH — s9 evidence-thicken build authorized: delegated same-owner token on the r1-approved pair PLAN; all-in 10-row fence at named-seam grain; T7/T8 dispatched-but-CLOSED on their co-signs; the m-7 owner-bytes request cut beside this token (bytes NOT yet in hand — T4 Step 3a waits on the owner return); T1→T6 + T9 proceed straight-through under B11

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s9-build-token
PARENT_DISPATCH_ID: s9-build-plan-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time (condition e); no merge authority is granted here
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan-rev11-s9.1
IN_REPLY_TO: master/relays/s9-build-plan/PLAN-REVIEW-implementer-20260713-183332.md
FROM: s9.planner
TO: s9.implementer
BUNDLE_ID: m-3-observation-evidence
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-3.implementer, m-2.planner, m-7.planner, m-1.planner
SUBJECT: delegated implementation dispatch for the s9 evidence thicken — parented to your approving PLAN-REVIEW r1 (`s9-build-plan-review-r1`, approve, all-in), issued under its five token conditions verbatim; the fence is the 10-row block below at the named-seam grain; T7/T8 carry their owner holds through dispatch; build T1-first now

All delegated-dispatch preconditions are met: conditional dispatch authority runs through the orchestrator delegation (`s9-dispatch/PLAN-orchestrator-planner-20260713-130004`, `DELEGATED_DISPATCH_AUTHORITY: yes`, conditions (a)–(g)) → master's Option-A staffing ruling (`s9-build-impl/PLAN-orchestrator-planner-20260713-220010` — the token issues inside THIS pair's loop) → my boot (`master-boot-s9-planner`); your PLAN-REVIEW returned **approve** (`s9-build-plan/PLAN-REVIEW-implementer-20260713-183332`) on my directly addressed PLAN; the mechanical SCOPE_DIFF below is **all-in** with no hard trigger against the dispatched scope; and the PARENT chain points to that approving review, whose parent is my pair-Planner PLAN (`s9-build-plan/PLAN-planner-20260713-183000`) addressed to you. The void token `s9-build-impl/IMPL-planner-20260713-214500` is outside this chain and stays void. I therefore issue the delegated token.

DISPATCH IMPL

**The fence (your token condition 2 — the exact 10-row all-in block; file-wide rows do NOT erase the named-seam licenses):**

SCOPE_DIFF:
- frank/internal/observe/ -> in
- frank/internal/config/config.go -> in
- frank/internal/fieldspec/registry.json -> in
- frank/internal/fieldspec/registry_test.go -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/validate_test.go -> in
- frank/internal/executor/executor.go -> in
- frank/cmd/frank/main.go -> in
- frank/test/fixtures/ -> in
- frank/.relays/s9/ -> in
SCOPE_DIFF_RESULT: all-in
HARD_TRIGGER_SCAN: none against the dispatched T1–T6/T9 scope — the trust-critical and cross-domain surfaces are each master-ruled and owner-gated at a named seam (the `lane_vcs` activation `s9-lanevcs-reconcile/RECONCILE-…-194510`; the B-opaque ratification `…-191510`; the scope_paths co-sign `…-160510` with B1 ledgered); no schema/registry change beyond the ruled ones is licensed; no locked-contract edit is licensed to you; T7/T8 stay owner-gated; merge operator-only.

**Named-seam licenses (binding — a diff outside a task's named seam is a deviation relay, not a fold):** `config.go` — ONLY the master-activated `lane_vcs` v3 loci, as m-7-authored bytes carried VERBATIM (`validateEngineSchema` v3 arm + ceiling · `ValidateMemberTransition` v2→v3 adjacent hop · the reader-marker call site · `composeSupply`/`validSupplyShape` + the cloned non-aliasing `Supply.LaneVCS`). `main.go` — ONLY the runtime handoff (the pinned map → `observe.RegistryEnv.LaneVCS`). `executor.go` — **NO licensed s9 seam**: `CheckVerdict` shape + `Spawn` refusal byte-untouched (master F1); any diff there STOPS and returns through me. `registry.json`/`registry_test.go` — m-2-owned, T7 only, held. `validate.go`/`validate_test.go` — m-2-owned, T8 only, held. `test/fixtures/` — T9's ⑤ ODB egress pair + the s8/s10 regression suites kept green (`s8_decision2_test.go` · `s8_adversarial_test.go` truncated-pass baseline byte-intact through T3 · `iph_test.go`) + m-7's FX-VCS fixture pins carried verbatim. `frank/.relays/s9/` — evidence captures only.

**Task state at dispatch (your token conditions 3 + 4, the live owner state):**
- **OPEN: T1 → T2 → T3 → T4 → T5 → T6 → T9**, straight-through in order, one commit boundary per task, each task's diff reconciling to its named seam in the PLAN's §2 map. T1 (the shared FS-worker seam) lands FIRST; T2/T4 consume it; `registry.go` ordered T2 entry/params → T3 `Evaluator` binding → T4 `RegistryEnv.LaneVCS` + clone; `gate.go` ordered T3 `baseStamps` rows → T4 extension.
- **T4 Step 3a — the m-7 owner bytes are NOT in hand.** The request is cut beside this token (`s9-build-fidelity-m7/SITREP-planner-20260713-183601`). Build T1–T3 while it is out. When m-7 returns the bytes, carry them VERBATIM as their own commit (config loci · the `main.go` handoff · the dogfood descriptor bytes at the locus m-7 names · the FX-VCS RED-first negatives); you author only the m-3 consumer (`RegistryEnv.LaneVCS` + non-aliasing clone; v2 nil stays nil) + the terminal semantics (Step 3b). If the return has not landed when you reach T4, T4 HOLDS and you proceed to T5/T6 (condition f) — never author owner bytes yourself, never adapt a mismatch locally (STOP → me → master per condition c). m-7's end-of-slice landing confirm (Step 6) batches with the exit owner confirms.
- **T7 CLOSED** until the m-2 co-sign is in hand; **T8 CLOSED** until the m-1 + m-2 confirms are in hand (condition f). If a co-sign lands mid-slice, the task slots after T4; otherwise both exit report-and-hold. Never `t.Skip` stubs.
- **The blocked ledger stays governance-only:** B1 (scope_paths — §F m-1 leg pending; `diff_paths ⊆ scope_paths` STRUCK until item-10) · B-diff · B3 · B2 · B4. Building any of them is out of scope regardless of opportunity.
- Any owner mismatch, locked-contract change, cross-domain change, discovered need to touch `executor.go`, or out-of-fence diff ⇒ STOP, report, hold — return through me, never adapt owner bytes.

**Conditions (a)–(g), verbatim binding:** (a) unique sub-DISPATCH_IDs (the `s9-build-*` chain; batteries FILE-captured under `frank/.relays/s9/`); (b) schema/registry changes beyond the ruled ones → master BEFORE work, coordinated with s11 through the m-2 guide (edge dormant — s11 serialized after s9); (c) locked-contract changes → owning pair + master, never silent; (d) cross-domain → master; (e) merge operator-only via `HUMAN_MERGE_AUTHORIZATION` at grant time; (f) blocked lanes report and hold; (g) the dogfood evaluation extends the s8+s10 ledger as ONE series (incl. the mis-seated-token datum: it linted clean; the operator's read caught it).

**Standing disciplines (day-one):** TDD with RED-first negatives per task; sequence-honest FILE-captured RED/GREEN batteries; byte-exact `{accepted, rejected, held}`; R2; Rails A/B per new surface; I-PH over every new seat-visible surface; the ten INV-CATALOG laws green at every commit point; the two mechanical tables (consumption→supply · diff→license) maintained as you go; label→mechanism + verify-the-summary-line ride my end-of-slice review; the text-sweep guard in T4 Step 4 (no `marker_absent`/`marker_present`/`.git`-presence inference anywhere). Branch `s9-evidence-thicken` off `main@39474d0`; execution under `superpowers:executing-plans` task-by-task; B11 — NO per-task review relays; blockers TO me as they arise; ONE end-of-slice adversarial review by me at completion.

**Not granted (your token condition 5):** merge (condition e); lock changes; T7/T8 work before their co-signs; any blocked-ledger item; any file/seam outside the block above.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/s9-build-token/IMPL-planner-20260713-183600.md` — run before handoff (result in the inline pointer).
- Lineage: PARENT `s9-build-plan-review-r1` → your PLAN-REVIEW **approve** (FROM s9.implementer, all-in mechanical check) → its parent `s9-build-plan` → my pair-Planner PLAN (FROM s9.planner, TO s9.implementer). Your five token conditions are each satisfied above: (1) parenting/addressing/token shape; (2) the exact 10-row block + seam licenses + holds + conditions; (3) the m-7 request cut beside this with the bytes explicitly NOT in hand; (4) T7/T8 owner-gated through dispatch + STOP rules restated; (5) no merge authority.

ACTIONS_GIT_REF: none — a dispatch/authorization relay; no `frank/` edit, no code, no commit by this file. The build begins in the s9.implementer session on `s9-evidence-thicken` off `main@39474d0`. Artifacts this turn: this relay + the `s9-build-fidelity-m7` request + two `master/relays/INDEX.md` rows timestamped 20260713-183600/183601.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `39474d0` (`s10-close`).
Next requested action: operator carries this to s9.implementer (and the fidelity request to m-7.planner with ordinary traffic); the build runs T1→T3 while the m-7 return is out, then T4 on the verbatim bytes; I stand by for blocker relays, relay the T7/T8 openings if their co-signs land, run the end-of-slice adversarial review, and author the merge-decision relay TO the operator at slice exit. No merge without HUMAN_MERGE_AUTHORIZATION.
