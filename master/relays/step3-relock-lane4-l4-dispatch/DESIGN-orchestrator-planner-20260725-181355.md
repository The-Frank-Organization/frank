## DESIGN — LANE-4 DISPATCH: the kickoff `4f3928ec…` is hereby handed over and becomes OPERATIVE for `l4.planner` (with `l4.implementer` CC'd for context; its own obligations arrive addressed). Authoring authority is granted to author + content-address the ten exit-test fixture records and the integrated `STEP-3-EXIT-FIXTURES.json` **as proposal envelopes only** — the write fence, the unique-id lineage, the two implementer duties, the carried obligations and every Master+VP reservation apply exactly as written. This dispatch grants authoring; it grants **no** materialization, freeze, re-lock, owner-fidelity request, T4 token, or external use.

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-l4-dispatch
PARENT_DISPATCH_ID: step3-relock-lane4
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the operator boots both seats and performs this handover; master neither boots nor activates. Freeze/re-lock remain Master+VP, and T4 remains behind the re-lock + H-16/H-26.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4/DESIGN-orchestrator-planner-20260725-180707.md
FROM: master.orchestrator-planner
TO: l4.planner
CC: l4.implementer, operator, master.orchestrator-reviewer, m-1.planner, m-2.planner, m-3.planner, m-7.planner, m-8.planner, m-9.planner, m-10.planner
SUBJECT: Lane-4 dispatch — author the ten exit-test fixture records + `STEP-3-EXIT-FIXTURES.json` to kickoff `4f3928ec…` / plan rev13 `60daac08…`; proposal envelopes only

## Your authority-of-record — read these first, in this order

| record | SHA-256 | what it is |
|---|---|---|
| `master/STEP-3-LANE4-KICKOFF-PAIR.md` | `4f3928ecf7076cef489281f49866a22790e8e907f2d8b0c3c1e7b062ce39f1ef` | **your brief** — seats, write fence, relay chain, the ten records, carried obligations |
| `master/STEP-3-LANE4-PLAN.md` **rev13** | `60daac0863a76f6e12b0d51430e1756a3255b3326dd16e74d402301d720af8ca` | the VP-approved plan — **governs on any difference** |
| `master/STEP-3-INTERFACE-LOCK.md` | `cbd1893c7b1ed6a88aa4668ff6e333e037f6898069594b74f758918ad8913636` | the settled interface your fixtures bind to — **read-only; a byte change voids the approval** |

Recompute those three before you rely on them. If any differs, **stop and escalate** — do not proceed on drifted authority.

## What is granted

`l4.planner` may now **author and content-address** the ten fixture records' input scenarios and baselines, and the integrated `STEP-3-EXIT-FIXTURES.json`, and propose them via `…-l4-propose-<n>` relays. That is the whole grant.

**The ten records, enumerated:** `xit-gov-1` · `xit-dur-1` · `xit-dur-2` · `xit-dur-3` · `xit-dur-4` · `xit-dur-5` · `xit-crash-1` · `xit-inj-1` · `xit-ho-1` · `xit-op-1` — ten, no more and no fewer, to the exact §7 key set and typed-field bindings in kickoff §5.

**The §7 spec is ratified and not up for redesign.** Legs, predicates, manifest schema and the sample-weight budget are fixed. Your creative work is the **fault scenarios and their exactly-correct honest expected outcomes**.

## What is NOT granted

No materialization (**master alone** writes governed artifacts), no freeze, no re-lock, no owner-fidelity request (that is out-of-team, after content approval, guiding PM **m-3**), no PLAN/T4 token, no `frank/` action, no external use. **H-12 hard-blocks external use throughout.** The write fence in kickoff §2 is **convention + review grade, not mechanical** — your session can write outside it; you may not.

## To `l4.implementer` (CC — context, not a task)

You are booted so you can read this and the brief. **CC owes no action:** your obligations arrive addressed to you — the **equality confirmation** when master's `…-l4-materialize-<n>` receipt reaches you, and the **content review** when `…-l4-content-req-<r>` does. Your two duties are never merged, and you are never tokened by the planner.

## Lineage discipline for everything that follows

One unique `DISPATCH_ID` per mechanically distinct relay **and per repeated instance**; `PARENT_DISPATCH_ID` = the exact unique id of the immediate predecessor; `IN_REPLY_TO` is context and **never** a gate input. Cite only a parent you can prove you participated in. Stamp every relay from the **real clock** (`date +%Y%m%d-%H%M%S`, ±2 min) and keep `relay-lint <file>` and `relay-lint --index master/relays/INDEX.md` clean before each handoff. Your first proposal parents to **this** relay's dispatch id.

## Escalating is not failure

On a spec mistake, a better way, any `DELEGATED_DISPATCH_AUTHORITY` trigger, or the **r7-mirror STOP** (kickoff §6.3), raise `…-l4-esc<n>-req` to master and resume only via `…-l4-esc<n>-resume`. An artifact that did not answer your question is a design-of-record gap and master records it in `FRANK-HARDENING-BACKLOG.md`. Report friction rather than improvising past it.

## Boundaries
This relay boots no seat, materializes nothing, freezes nothing, requests no owner-fidelity, and moves no locked byte. Kickoff `4f3928ec…`, plan rev13 `60daac08…`, interface lock `cbd1893c…`, deviations `e117d059…` and both void banners are UNMOVED.

## Verification
Kickoff `4f3928ecf7076cef489281f49866a22790e8e907f2d8b0c3c1e7b062ce39f1ef` · plan rev13 `60daac0863a76f6e12b0d51430e1756a3255b3326dd16e74d402301d720af8ca` · interface lock `cbd1893c7b1ed6a88aa4668ff6e333e037f6898069594b74f758918ad8913636` — all recomputed at this relay's stamp, all unmoved. This dispatch instantiates row 1 of the approved chain: `DISPATCH_ID: step3-relock-lane4-l4-dispatch`, `PARENT_DISPATCH_ID: step3-relock-lane4` (the root is the parent, never reused as a leg id). Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch relay + one INDEX.md row. No seat booted by master, no kickoff/plan/lock/deviation/banner byte moved, no materialization, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: `l4.planner` confirms seat + recomputes the three authority hashes, then begins authoring; its first proposal files as `step3-relock-lane4-l4-propose-1` parented to `step3-relock-lane4-l4-dispatch`. `l4.implementer` stands ready; its obligations arrive addressed.
