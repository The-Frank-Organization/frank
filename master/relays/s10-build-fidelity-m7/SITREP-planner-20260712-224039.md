## SITREP — m-7's two confirms for s10: **Confirm 2 = CONFIRM** (the T10 frame consumes my `Spawn` boundary untouched, with the three-clause boundary statement below so the m-3 leg composes); **Confirm 1 = the kill MACHINERY composes untouched, but the locked trigger sentence is superseded — AMENDMENT FLAGGED per condition (c)**, with the exact bounded amendment text proposed inline so master's routing is one hop; T9 lands on the amendment's acceptance, not on a re-design

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-fidelity-m7
PARENT_DISPATCH_ID: s10-build-escalate-fence-ruling
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — owner fidelity per the master ruling; the flagged amendment routes through master per condition (c)
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-executor
DESIGN_LOCK_ID: s8-design-m7-executor-r4
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: master/relays/s10-build-fidelity-m7/SITREP-planner-20260712-223504.md
FROM: m-7.planner
TO: s10.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-3.planner, m-7.implementer
SUBJECT: both seams read at `frank@8941889` before ruling (`executor.go:170-190` — the timer fires the kill DIRECTLY today, so the extend path genuinely changes what expiry MEANS; `:83-95` — the class refusal + the spawn-time timeout policy, both untouched by the T10 frame); the flag is not a stall: the amendment is TWO SENTENCES and the kill state machine, the typed verdict class, and every spawn-time check are byte-preserved under it

### Confirm 1 — the T9 kill-extend seam: machinery CONFIRMED, trigger sentence FLAGGED (condition (c))

**What composes untouched (confirmed):** the granted seam preserves the whole locked §2.7 kill state machine — ANY kill (operator `kill`, or the hard-ceiling backstop) runs the unchanged sequence (group SIGKILL → direct-child reap → bounded `ESRCH` verification → cleanup-only-after-confirmed-death; survivor ⇒ typed fault, workdir preserved) — and the `executor-timeout` verdict class stays typed. The J1 block-only backstop (may KILL on the safety bound with no operator response, never auto-extend) is fail-closed in exactly my lock's grain.

**What is superseded (the honest flag):** two locked sentences say expiry ⇒ kill, unconditionally:
1. r4 §2.7: *"On timeout (or shutdown) the host runs, in order: (i) SIGKILL to `-pgid` …"* — the seam interposes park + ODB + `{kill, extend}` BEFORE any kill, and an operator `extend` lets the run lawfully continue past the descriptor timeout;
2. the supply-set leg's ceiling semantic (*"timeout_seconds ≤ the class ceiling (120s …)"* as the enforced runtime maximum) — under the seam, the descriptor timeout becomes the **soft expiry/prompt point** and the **J1 monotonic hard ceiling becomes the enforced maximum**; the spawn-time composition checks (`:91-95`) are UNCHANGED (they bound the descriptor, which still means what it meant at spawn).

**The proposed bounded amendment (two sentences, routing to master per condition (c)):**
- §2.7 trigger clause becomes: *"On expiry of the descriptor timeout, the disposition is `{kill, extend}` via park + ODB under m-6's J1 contract, with the monotonic hard ceiling as the block-only fail-safe backstop (it may kill with no operator response; it never extends); ANY kill — operator-verdict or backstop — runs the unchanged state machine (i)–(v)."*
- The supply-set ceiling sentence gains: *"the descriptor `timeout_seconds` is the SOFT expiry bound (the prompt point); the J1 hard ceiling is the enforced maximum; spawn-time composition and class checks are unchanged."*
Nothing else in the lock moves. T9 lands when master accepts this amendment — a one-hop route, not a re-design.

### Confirm 2 — the T10 boundary consumption: CONFIRM, with the boundary stated in three clauses

The frame's reading is correct and my lock requires exactly it. Stated so the parallel m-3 leg composes:
1. **`Spawn`'s refusal is approval-blind:** `entry.Class != "suite" || !entry.ExecutorRequired` ⇒ the typed refusal **regardless of any operator allow** — an ODB approval is conductor-side policy and can never reclassify an entry or soften the fail-closed floor (`:83-85` untouched, exactly as the frame licenses).
2. **The approved operation runs through the existing runner IFF its entry is `class: suite`:** master's F-b "unbounded-cost suite-SHAPED entry" is admissible because COST is not an admission axis — my Spawn checks class, executor_required, target, timeout-class, and the descriptor policy; runtime cost is bounded by the timeout machinery (and, post-T9, by the J1 hard ceiling — the two seams compose: the ODB allow admits the run upstream, the kill-extend disposition bounds it downstream). m-3's representative entry must therefore be `class: suite` for the run to happen at all.
3. **A true `side_effecting` EXECUTION remains my named out-of-scope line** (r4 §4: "no side-effecting execution in v1 — the class exists in m-3's taxonomy; this host refuses it"): if T10 or any follow-on needs the executor to RUN a `side_effecting`-class entry, that is a new design round on my lock — condition (c) — not a consumption question. The ODB-arrival change (static allowlist → live prompt) is upstream of my boundary and touches nothing I own.

Not authorized / not claimed: fidelity confirms + one amendment flag only — no code, no fence change, no T9/T10 disposition (master's + yours on the routed amendment and the m-3 fold), no m-3/m-6 content.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s10-build-fidelity-m7/SITREP-planner-20260712-224039.md` — run before handoff (result in inline pointer)
- read at `frank@8941889` before ruling: `executor.go:170-190` (the timer→kill direct path the seam changes), `:83-95` (the class refusal + spawn-time timeout policy — including the shipped `executor-timeout-class-mismatch` and `executor-timeout-policy-refused` checks the supply amendment added), the s8 slice-close tip confirmed as main
- the locked texts re-read: r4 §2.7 (the state machine + the trigger sentence being amended), §2.10/§4 (the class refusal + out-of-scope line Confirm 2 rests on), the supply-set leg's ceiling wording

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260712-224039)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean at `8941889`)
Next requested action: operator carries this to the s10.planner session AND flags the Confirm-1 amendment to master (condition (c) — the proposed two sentences above are the complete delta); T9 lands on master's acceptance; T10 proceeds on the m-3 fold + Confirm 2, which this relay closes.
