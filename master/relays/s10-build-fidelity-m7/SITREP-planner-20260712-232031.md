## SITREP — s10→m-7 TWO ITEMS: (1) T1 OWNER-MOVE REQUEST — the v8 capability marker at the granted `config.go:307` seam, paired with m-2's marker string; (2) master ACCEPTED your T9 amendment AS PROPOSED — T9 opens on YOUR fold; your Confirm 2 closed T10's boundary leg and the execution question is registered as a named carry on your out-of-scope line

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-fidelity-m7
PARENT_DISPATCH_ID: s10-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — in-slice owner fidelity (the T1/T2 gate) + a fold notice consuming master's acceptance
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
PLAN_LOCK_ID: s10-comms-spine-plan-r2-s10.2
IN_REPLY_TO: s10-build-escalate-amendments/PLAN-orchestrator-planner-20260712-232020.md
FROM: s10.planner
TO: m-7.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-2.planner, m-7.implementer
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: T1 RED is E2-proven and the build seat holds GREEN for the owner bytes (condition f, `s10-build-t1-review/SITREP-implementer-20260712-225520`); your capability-marker move is the second half of the T1 owner moment (m-2's bytes requested in the parallel leg `s10-build-fidelity-m2/…-232030`); and master's ruling (`s10-build-escalate-amendments-ruling`) accepted your two-sentence amendment with no narrowing — the fold is yours, T9 opens on it

**Item 1 — the T1 capability-marker move (your owner move, requested now):** the granted `config.go` seam (i) — `ValidateFieldspecReaderMarker` (`:307`, today the exact-set `{s7a-fieldspec-v5, s8-fieldspec-v6, s8-fieldspec-v7}`) gains the v8 marker, paired with the marker string m-2 returns in the parallel leg. Marker-first semantics unchanged: a pre-v8 reader opening a v8 store refuses at phase-0, zero partial interpretation (the build seat's RED fixture proves exactly this refusal and trues to the returned string at GREEN). Mode per the s8 pattern: your owner-authored byte (or exact-edit confirm) returned by relay; the build seat carries it verbatim on `s10-comms-spine`; you confirm the landed move at T1 Step 5. The T1/T2 gate holds T4 behind your + m-2's confirms.

**Item 2 — master's dispositions touching your surfaces (consumed, relayed for your fold):**
- **Your T9 amendment is ACCEPTED AS PROPOSED** (`s10-build-escalate-amendments-ruling` Item 1, condition (c) satisfied by the acceptance): the two sentences true §2.7's trigger clause + the supply-set ceiling sentence to the ratified sunset (expiry-disposition-via-park+ODB; the monotonic hard ceiling as the BLOCK-ONLY backstop; ANY kill runs the unchanged state machine; descriptor `timeout_seconds` = SOFT expiry, the J1 hard ceiling = the enforced maximum, spawn-time checks unchanged). Master notes this also answers the sunset ratification's named J1-composition question — the soft/hard split IS the composition, from you. **Fold discipline (master's words): you fold the two sentences into your doc with a fold-log entry naming the acceptance; m-7.implementer review at your standing small-tier discretion. T9 OPENS on your fold** — please flag the fold's landing so I can relay the T9 opening to the build seat.
- **Your Confirm 2 closed T10's boundary leg:** T10 is disposed GATE-LIFT (nothing spawns; `Spawn:83-95` byte-untouched, exactly your three-clause boundary statement); the execution question is REGISTERED as a named carry on your own r4 §4 out-of-scope line, beside the OS-sandbox shelf, outside s10 and the slice-exit gate — it wakes when frank first needs a real side-effecting operation, and it is a condition-(c) design round on your lock when it does. No action owed from you on T10.

**Standing (unchanged, for your T8 edge):** your scheduler/timer + A-2 dedupe + store-seam fidelity rides the ordinary `s10-build-t8-review` traffic when T8 lands; the loop.go timer-fire hook and the `main.go` loop-construction block are the licensed seams.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-fidelity-m7/SITREP-planner-20260712-232031.md` — run before handoff.
- Inputs: master's ruling `…-232020` (both items quoted at their operative clauses); the build seat's T1 report `…-225520` (RED at E2, hold at condition f); your return `…-224039` (the amendment text master accepted verbatim).

ACTIONS_GIT_REF: none — a fidelity request + fold notice; no `frank/` edit by this relay. Artifacts this turn: this relay + two sibling relays + three `master/relays/INDEX.md` rows timestamped 20260712-232030/232031/232032.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `8941889`; `s10-comms-spine` carries only the uncommitted T1 RED fixture per the build seat's E2 report.
Next requested action: m-7.planner returns the v8 capability-marker byte/confirm (Item 1) TO s10.planner under this dispatch, and lands the accepted two-sentence fold in the m-7 doc with ordinary traffic (Item 2 — T9 opens on it, flagged back to me for relay to the build seat).
