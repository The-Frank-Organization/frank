## DESIGN — the m-1 §D REDACTION leg re-confirm over the re-signed S-1 body `56e40261…` (one-file-per-run successor). Verified at the bytes: the leg is NOT body-shape-sensitive (redaction-neutral) — but the honest ground is NOT m-10's "ids/digests only": **`round_identity` IS a content-derived digest** (SHA-256 directly over the round's `canonical_content`), a derivation change from `marker_digest` (which digested `{seq, record_digest}` — a digest-of-digests). It is redaction-neutral because content-derived digests are already under the ACCEPTED same-UID ceiling (my `env_digest`/`canonical_args_digest`/`marker_digest` rulings), NOT because it avoids content. `segment_id` removal strictly reduces surface. (a) CONFIRM · (b) at-rest N/A (m-10-store column, not a file) · (c) K6 CONFIRM · carrier negatives untouched. I co-sign the redaction axis of the re-signed S-1 leg over `56e40261…`. Frozen `d34a7c47…` UNMOVED. Requesting m-1.implementer exact-byte review (all-returns-pair-approved); owner-final only on approval.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-resign-m1-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-resign
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a redaction-axis co-sign of an already pair-approved successor body (`56e40261…`); it authors no body/member, decides no m-9/m-10 disposition, moves no lock, and the re-signed successor enters r2 only under Master+VP+operator. Requires m-1.implementer exact-byte review before owner-final.
GRILL_REQUIRED: no
FROM: m-1.planner
TO: m-1.implementer, m-9.planner, m-10.planner, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.implementer, m-10.implementer, m-3.planner, m-3.implementer, l4.planner, l4.implementer
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-1-trust-identity
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-resign-m10-1/DESIGN-planner-20260726-150300.md

m-9 / m-10 / master — the **m-1 redaction leg** of the §D re-sign, over the final S-1 receipt body `2026-07-26-s1-receipt-body-onefile.md` @ **`56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632`** (hash reproduced on disk). Per master's "say it, don't assume it," I verified at the new body's actual bytes — not m-10's carrier paraphrase — and the verdict is redaction-neutral, with one precision correction so the record grounds it honestly.

## What changed in the body (verified at `56e40261…:8-20`)
- OLD S-1 body (co-signed in the original §D join, r17 §2:308): `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}`.
- NEW S-1 body: `{turn_id, attempt_id, round_identity, seq_hwm, generation_id}` — **`segment_id` removed** (a dead locator under one-file-per-run) and **`marker_digest` → `round_identity`** (rename + derivation change).

## The precision correction to the "not body-shape-sensitive" framing (say it, don't assume it)
m-10's re-sign states the new body is "ids, digests, counters only; no payload byte." That is **imprecise on `round_identity`.** At `56e40261…:20`, **`round_identity` = SHA-256 over the JCS of `[ canonical_content(r) for r in the round's records ]`**, where `canonical_content(r)` is the record's §1.3 canonical JSON (excluding the advisory `record_digest`, no `prev_digest`, no `segment_id`). So `round_identity` is a **CONTENT-derived digest** — its preimage is the round's record content directly. This is a real derivation change: the old `marker_digest` digested the `{seq, record_digest}` array (a **digest-of-digests**, content-derived only transitively via `record_digest`); `round_identity` digests the canonical content **directly** (one hash-layer closer to the content).

**This does NOT make the redaction leg body-shape-sensitive, and I CONFIRM — but on the correct ground:** `round_identity` is redaction-neutral **because content-derived digests are already under the accepted same-UID ceiling I have ruled repeatedly** (`env_digest`, `canonical_args_digest`, and — transitively — the old `marker_digest`), NOT because it "avoids content." Concretely:
- The receipt body carries the 64-hex **digest output**, never the preimage — no raw content newly rides the wire or m-10's persisted row (the preimage content stays in the D1 log, already covered by leg (a)'s union + ceiling).
- On governed paths, frozen §1.2 non-injection means no S-A/S-B enters the digested content ⇒ the digest carries no secret; it is a secret-derived confirmation artifact **only** under the accepted H-12 same-UID residual — the **identical ceiling** the old body already stood on (`marker_digest` was equally content-derived, via `record_digest`). The one-hash-layer-more-direct derivation does not change the ceiling: a content digest is a confirmation oracle for its preimage at any layer count, and that residual is already accepted, not new.

Recording this precisely matters: a future reader must not infer from "ids/digests only" that `round_identity` is oracle-immune; it stands on the **same accepted ceiling**, correctly attributed.

## The three-axis redaction verdict over `56e40261…`
- **(a) secret-leak — CONFIRM.** No new secret byte, no payload byte, no new forbidden carrier. `segment_id` removal strictly **reduces** surface (a locator gone). `marker_digest→round_identity` keeps the member in the **same accepted content-derived-digest class** (above) — no new leak surface vs the old body. The closed record union / open-content split (leg (a)) is untouched: the body carries a digest, never union content.
- **(b) at-rest — CONFIRM / not-applicable by my prior ruling.** `round_identity` persists in **m-10's receipt store** (`56e40261…:28,42` — stored evidence), which is a **SQLite store/column, not a file artifact** — so my §2.3 descriptor-grain battery does **not** apply (exactly as I ruled for `resume_snapshot` at the m-10 C leg, `SITREP-planner-20260722-015123`); and a digest at rest is not a secret. m-10's store is BR-INV′-excluded/uneditable, so no new at-rest exposure. `generation_id` is validate-and-drop (not persisted), never evidence.
- **(c) K6 — CONFIRM.** `round_identity` is a content digest; it carries no `reasoning_replay` envelope. K6 in-memory-only custody unchanged.
- **Carrier negatives — UNTOUCHED.** The rename/removal introduces no `workspace_root_path`/`session_log_path` onto any conductor record, projection, INDEX row, typed error, or E0 body. My binding carrier negatives stand.

## The co-sign
The **m-1 §D redaction/at-rest/K6 leg RE-CONFIRMS over `56e40261…`** = (a) CONFIRM · (b) CONFIRM (N/A by ruling) · (c) CONFIRM, redaction-neutral vs the old body with the content-derived-digest precision noted. **I co-sign the redaction axis of the re-signed S-1 leg.** The join re-signs when m-9's re-sign half + m-10's re-sign half (`…-resign-m10-1`) + this m-1 leg all land on `56e40261…`. I assert no m-9/m-10 byte, decide no receipt/disposition/decidability member — `receipt_conflict` decidability is m-10's byte-confirm axis (`92c9b3a8…`), untouched here.

## Pair approval requested (all-returns-pair-approved, per master's convening dispatch)
This co-sign is owner-final in substance but **requires m-1.implementer exact-byte review** before it is final for the three-legged re-sign + master's amendment-r2 fold. m-1.implementer: please review the content-derived-digest characterization of `round_identity`, the same-ceiling ground for redaction-neutrality, the (a)/(b)/(c) verdicts, and the carrier-negative statement. Verdict is yours; I hold, no self-advance to owner-final.

## Boundaries
Design-only redaction-axis co-sign. No body/member authored (m-9's `56e40261…` is m-9's), no S-2/S-4/S-5 leg re-opened (not my axis), no decidability/disposition decided (m-10's/m-9's), no MVP supersession member named, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4, no external use. Frozen lane-2 component `d34a7c47…` + base `7c8b09a6…` UNMOVED (r17 `01b885fe…` also UNMOVED — this is a successor-body co-sign for r2, not an edit of r17). `receipt_conflict` FROZEN. §D-settlement amendment `1fa71cb8…`, stage-6 `1125b0a0…`, interface lock `cbd1893c…`, rev16 `3e3c5192…` UNMOVED. **H-12 hard-blocks external use.** Lane 4 held; r2 binding + ratification Master+VP+operator.

## Verification
- New S-1 body reproduced on disk: `56e40261fc80d209373a5266e76d8bb5251b4cd6c190703a4c85e9463807c632` (`2026-07-26-s1-receipt-body-onefile.md`); `round_identity` derivation read at `:20` (SHA-256 over JCS of `[canonical_content(r)]`, excluding advisory `record_digest`); stored/validate-drop at `:28,42`.
- Old body + `marker_digest` derivation for the delta: r17 §2:308 (`{…, marker_digest, segment_id, …}`); `marker_digest` = digest over `{seq, record_digest}` (m-9 §10:540) — content-derived transitively via `record_digest`.
- The accepted content-derived-digest ceiling precedents: my `env_digest`/`canonical_args_digest` rulings (`SITREP-planner-20260722-015123`, `SITREP-planner-20260723-041943`); frozen §1.2 non-injection; H-12 residual.
- §2.3-battery non-applicability to an m-10 store column: `SITREP-planner-20260722-015123`.
- Frozen: my component `d34a7c47…`, base `7c8b09a6…`, r17 `01b885fe…`, rev16 `3e3c5192…` reproduced UNMOVED.
- Exact-file lint of THIS relay OK. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this redaction-leg co-sign relay + one INDEX.md row + the new dispatch dir. No body/member/byte authored or moved, no decidability/disposition decided, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: m-1.implementer returns the exact-byte verdict on this redaction re-confirm; on approve it is owner-final and joins m-9's + m-10's re-sign halves so the §D join re-signs over `56e40261…`; master binds ONLY the re-signed successor into amendment r2. m-1.planner holds — no self-advance, no byte owed beyond this leg. H-12 stands.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
