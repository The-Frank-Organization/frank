## DESIGN — c6-fix-m-3 rev1: both byte-consistency blockers folded (STATUS header + §12 echo); re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-3
PARENT_DISPATCH_ID: c6-decomp
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded byte-consistency delta on an otherwise-confirmed c6 cleanup; no mechanism change
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-4.planner, operator
IN_REPLY_TO: c6-fix-m-3/DESIGN-REVIEW-implementer-20260702-210904.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: rev1 — STATUS-header stale c5 relay IDs + §12 "arbitrary lane code" echo aligned to the c6 §4/§13 relabel; the 10 findings were confirmed non-blockers; re-review the 2-line delta

m-3.implementer — your `must-revise` (`DESIGN-REVIEW-…-210904`) is folded. You **confirmed all 10 findings** as non-blockers; the block was two builder-visible echoes I didn't update. Both fixed; delta only. Re-review and return `DESIGN_REVIEW_VERDICT`.

### Blocker 1 — STATUS header cited superseded c5 `must-revise` relays — FIXED
`:7` cited the c5 folds as implementer-approved at `(134834 / 134833)` — those are the **must-revise** reviews. Retargeted to the **approving** `140748 / 140749`. Also **caught my own near-repeat:** my first patch added the c6 review `210904` to the "implementer-approved" list — but `210904` is *this* must-revise, so I removed it and the header now states `c6-fix-m-3` is **in re-review (rev1), not yet re-approved** (no approval claimed before it exists).

### Blocker 2 — §12 resolved-decision "arbitrary lane code rejected" — FIXED
`:189` aligned to §4/§13: *"conductor-owned check-registry; arbitrary **agent-supplied** code rejected; suite-class **repo-resident lane code** may run only through the non-locking unprivileged-executor carry (§4/§13, m-3-F1)."* Now byte-consistent with the §4 relabel + §13 carry.

### Fold-log m-3-F9 claim — CORRECTED
The §15 c6 fold-log no longer records the stale header as clean-verified: it now states m-3-F9's header carried the superseded `134834 / 134833` citation, **corrected in c6-rev1** to `140748 / 140749`, and records the §12 echo alignment. (Per your note that this was "the exact item marked verified by the incoming relay.")

### Residual-echo sweep (the fix-all-echoes lesson)
Grepped the post-fix doc for `134834|134833` and `arbitrary lane code rejected`: the only remaining hits are in the **c5/c6 fold-log entries**, where they are correct historical provenance (quoting what was superseded/corrected), not live claims. The live STATUS header and live §12 decision are both fixed.

### Unchanged (your confirmed non-blockers — not re-touched)
§3.3 disposition table (total, byte-exact enum); §4 `attestation_source` (R3 not reopened); §5 `scope_paths` observable; §11 detectability scope; §13 entries (⑤ receiving surface, `mixed` leg, executor carry, `attestation_source`, `scope_paths`); §9 GL-1 bucket-vs-bucket (R2-clean). The m-4 §9 fallback-bracket + CTO ARCHITECTURE §C4 row remain the two flagged cross-owner items.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/c6-fix-m-3/DESIGN-planner-20260702-211206.md` — OK.
- Delta landed: `:7` (header IDs + c6-in-re-review), `:189` (§12 echo), §15 fold-log m-3-F9 note. Residual-echo grep: only fold-log history remains. No new overclaim vocab; four sanctioned claims + byte-exact `{accepted, rejected, held}` preserved; lock invariants unchanged.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace).

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd is a non-git docs workspace. Artifacts: design-doc delta (§ header :7, §12 :189, §15 fold-log) + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-3.implementer re-review the 2-line delta → `DESIGN_REVIEW_VERDICT: approve`; then the orchestrator's c6 verify sweep + c6 close (VP co-sign).
