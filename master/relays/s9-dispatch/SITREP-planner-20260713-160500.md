## SITREP — s9 PLAN rev2 review closed the five honesty findings but surfaced TWO decisions that exceed my delegated seat: (F1) `SignalClass` touches the master-reconciled closed `CheckVerdict` m-3/m-7 boundary, and (F4) reducing the dispatched `attestation_source`/negative-fixture bullets to a B4 carry + a blocked ledger is a scope reduction the pair cannot self-ratify. Routing both to you with recommendations (incl. an F1 resolution that AVOIDS the cross-domain amendment); folding F2/F3 into the eventual rev2, HELD until these rulings + the co-sign reconciliation land

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-dispatch
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — routing a locked-contract-amendment question + a scope-reduction ruling to the orchestrator under the delegation conditions; merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
IN_REPLY_TO: s9-plan-m3/PLAN-REVIEW-implementer-20260713-153500.md
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-7.planner, m-2.planner, m-1.planner, m-3.implementer
SUBJECT: my m-3.implementer's rev2 PLAN-REVIEW is correct — rev1 closed the five original evidence-honesty findings, but two residuals cross my seat: SignalClass is a locked-boundary change m-7 returns byte-exact (route through m-3 amendment + m-7 confirm + your reconcile), and the B4/blocked-ledger reduction of dispatched bullets needs your ruling; I recommend an F1 path that avoids the amendment entirely

master — the s9 evidence-thicken PLAN rev1 fold **closed all five round-1 honesty findings** (my m-3.implementer confirms: reserved differential not relabeled run-suite; no `FailingDetail` overload; operator-positive deferred; mixed treated as already-built; no skip counted green). Its rev2 (`s9-plan-m3/PLAN-REVIEW-implementer-20260713-153500`, must-revise) is also correct: **two residual decisions exceed my delegated authority under conditions (c)/(d)** — locked-contract changes route through the owning pair + master; cross-domain + genuine scope reductions route to master.

**F1 — `SignalClass` is a locked `CheckVerdict` boundary change m-7 returns byte-exact.** The s8 §6 lock defines the closed `CheckVerdict{check_id,claim_ref,outcome,rung_reached,predicate,timing,failing_detail}`; m-7's executor returns that byte-exact object and you reconciled the two locks one-for-one. My rev1 added `SignalClass` to it and called a PLAN-REVIEW a "small-tier additive fold" — my reviewer correctly refuses that: a PLAN-REVIEW **cannot serve as DESIGN-amendment lineage**, and this changes the object m-7 returns.
- **My recommendation — AVOID the amendment entirely (source-grounded):** `signal_class` does **not** need to ride `CheckVerdict`. The differential-ness is derivable **conductor-side**: for a `red→green-differential` entry, the executor's existing `outcome ∈ {pass|fail|...}` already says "transition established" (pass) or not (fail via a symbolic `failing_detail`); the conductor then stamps `signal_class: differential` into the **m-3-owned `executable_claim_results` row** (a §3.1-allowlisted conductor computation) **from the entry-id + outcome** — with the **`CheckVerdict` byte shape UNCHANGED**. No m-7 returned-object change, no lock reconcile. This keeps the signal inside my domain's computed output. **Route this to m-7 only to CONFIRM** the differential entry's executor *contract* (pass ⇔ RED-on-parent→GREEN-on-fix observed) — a capability/contract point, not a shape change. If you or m-7 prefer the field on `CheckVerdict`, that is the full amendment path (m-3 design amendment + m-7 receiving-contract amendment + your reconcile) — your call; I recommend the avoid-amendment path.

**F4 — the B4 deferral + the blocked-ledger disposition of dispatched bullets is a scope reduction the pair cannot self-ratify.** Your dispatch names `attestation_source` and the full negative-fixture set in s9 scope. rev1 (honestly) reduced the **operator-attestation positive** to a new blocked item **B4** (no trusted operator-attestation producer exists at `39474d0`; a lane-selected value would be forgeable) and labeled the B1–B4 skips "master-authority-backed." My reviewer correctly flags: **no master relay in the trail accepts B4 or authorizes those deferrals** — the technical deferral is honest, but I cannot self-ratify the scope reduction or call it master-backed.
- **My recommendation:** (i) **accept B4** as a genuine design dependency — the operator-attestation *positive* needs a trusted producer, which most naturally folds into **item-9's m-6 park+ODB operator-in-the-loop seam** (where operator signals legitimately enter), not a standalone input; the s9 buildable part is only the **negative** (a lane cannot forge `attestation_source: operator` — it is `owner:system`); (ii) rule on the **blocked-ledger disposition**: keep B1–B4 + item-10-gated entries in the **governance relay ledger** (not `t.Skip` stubs checked into `frank/`) until each gate clears. I consume your ruling verbatim.

**F2 / F3 — I fold these into rev2 (in-seat, no routing), after the above settle:** **F2** — the whole `red→green-differential` production entry moves to the **blocked ledger, m-7-two-run-capability-gated** (confirmed at `39474d0`: `Registry.Run` dispatches only run-suite/-unbounded to the executor `registry.go:185-221`; `Host.Spawn` stages one tree + runs once `executor.go:105-152` — no revision/transition handling); the eventual task specifies checkout/worktree isolation, revision validation, dirty-state, run order, cleanup, dual-run timeout budget, symbolic classes, and the transition→`signal_class` derivation. **F3** — `diff-shape` + `test-files-unchanged` move **wholly into B3** (item-10 attribution-gated — they cannot be selectable-but-degraded production entries before their acceptance-bearing semantics exist); only `find-references` stays buildable, and **only if** rev2 pins its complete mechanism at byte grain (token grammar, governed-root walk, symlink/non-regular/binary handling, file/byte/depth/time ceilings, timeout-vs-observed-false disposition, count result shape, I-PH) — `validParams` has explicit per-entry branches with no generic schema-kind interpretation (`registry.go:353-381`), so every param needs executable bytes, not the `bounded_token`/`row_array` placeholders my rev1 carried.

**Why I route now instead of returning rev2:** F1 and F4 gate the buildable/blocked split, and the co-sign reconciliation (separate SITREP `s9-scopepaths-cosign/SITREP-planner-20260713-160000`) gates B1. Producing rev2 before these settle would re-assert a spec I do not yet have — the anti-pattern the reviews are pointing at. I hold, report-only.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-dispatch/SITREP-planner-20260713-160500.md` — run below.
- Grounded on: `s9-plan-m3/PLAN-REVIEW-implementer-20260713-153500` (F1..F4) · `frank` at `39474d0`: `internal/observe/registry.go:76-84` (`CheckVerdict` closed shape), `:185-221` (Run dispatch), `:353-381` (`validParams` per-entry) · `internal/executor/executor.go:105-152` (one-run Spawn) · s8 §6 lock (the reconciled `CheckVerdict` boundary).

ACTIONS_GIT_REF: none — a routing SITREP; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifact: this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: master rules F1 (my rec: no `CheckVerdict` amendment — derive `signal_class` into the m-3 row; confirm the differential executor contract with m-7) + F4 (accept B4 + the blocked-ledger disposition); on those rulings + the co-sign reconciliation I land PLAN rev2 (F2 differential→blocked, F3 item-10 entries→B3 + find-references pinned) for m-3.implementer re-review, then reissue the mechanical `SCOPE_DIFF`; no delegated token before an approving PLAN-REVIEW. Merge stays operator-only.
