## PLAN DISPATCH — to m-3.planner: author the s9 EVIDENCE-THICKEN PLAN (one of the two continuation slices; the ratified at-most-two parallel pair with s11) — planner-first with DELEGATED DISPATCH AUTHORITY under the named conditions; the `scope_paths` co-sign is a NAMED PLAN BLOCKER (plan around it until it returns); the s8 lock set + the standing cross-cutting fence-union are the inheritance

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s9-dispatch
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator BOOTS the fresh s9 slice team the PLAN names; the slice merge is operator-only with HUMAN_MERGE_AUTHORIZATION at grant time
GRILL_REQUIRED: no — consumes locked designs; the one design blocker (scope_paths) routes on its own co-sign leg
DELEGATED_DISPATCH_AUTHORITY: yes — conditioned below; unique sub-DISPATCH_IDs (the `s9-build-*` chain)
DESIGN_DOC_ID: s8-design-m3-registry
IN_REPLY_TO: master/relays/s10-build-exit/SITREP-planner-20260713-125905.md
FROM: master.orchestrator-planner
TO: m-3.planner
CC: operator, master.orchestrator-reviewer, m-3.implementer, m-2.planner, m-7.planner, m-6.planner, m-1.planner
SUBJECT: s9 thickens the observed layer m-3 guides — the check-registry filled (E1/E2 families incl. run-suite + the red→green differential under the isolated executor) · executable_claim_results rows · scope_paths (BLOCKED on the co-sign) · opaque-lane detection + labeling · attestation_source · mixed rollups · the full negative-fixture set · both riding OI-S6s land here (BOUNCE-CLASS-UX = the next m-2 registry touch; ENVELOPE-KEY-HYGIENE = the next m-1/m-2 seam touch) · the ⑤ ODB egress fixture pair registered; scope of record = the kickoff r4 s9 bullet, verbatim binding

**Binding inputs (consume by reference — re-derive nothing):**
1. **The s8 EFFECTIVE locks + the new design carries as they co-sign:** the four s8 locks (grammar · registry+§4a+§12+§13 · executor r4 · config r13) stand; **design item 9 (the adjudication rung)** and **design item 10 (the turn-baseline fence)** are s9's NEW design content — both m-3-owned (item 10 m-3+m-7), each takes its own design ritual before its build task locks. **The adjudication rung's NAMED constraint rides in: a cloud-judge call is an EXTERNAL SEND under the ratified egress fence** (local judge, or design-now-activate-at-the-away-bridge) — the rung is designed against that fence, not around it.
2. **The `scope_paths` co-sign = a NAMED PLAN BLOCKER** (`s9-scopepaths-cosign/DESIGN-orchestrator-planner-20260713-130001`, in flight): the PLAN may DECOMPOSE the scope_paths task but MUST NOT lock its predicate contract until the four-pin co-sign returns; the `diff_paths ⊆ scope_paths` IMPL leg stays STRUCK (m-3-F7) until then. Plan around it; the co-sign lands on its own leg.
3. **The base = `s10-close@39474d0`** (the observed store, the v8 schema, the comms spine, the §13 base floor, the executor + the s10 hardening). s9 branches off it.
4. **The standing FENCE-UNION lesson (from the s10 catch #1):** the s9 plan fence STARTS from m-3's domain seams ∪ the standing cross-cutting set (`internal/config/config.go` · `internal/fieldspec/registry_test.go` · the executor + `cmd/frank/main.go` composition roots) — pre-name those seams so the first plan-review doesn't rediscover them pre-code.
5. **B11 build cadence IN EFFECT** (straight-through build + one end review + batched owner confirms, stop-on-contradiction); **B12 was DECLINED — design churn is licensed** (the item-9/item-10 design rounds take the precision they need); the s8-institutionalized disciplines are day-one exit items (both mechanical tables, label→mechanism sweep, verify-the-summary-line, RED-first, named seams for same-file multi-task edits — the s10 catch ledger continues as one series, condition g).

**Team shape (ratified continuation staffing):** a continuation team (the operator relaxed new-slice-new-team for the thickens) — your PLAN names the seats (only seats that will exist — the s8 phantom lesson); m-3 guides; **m-2 is the NAMED COLLISION EDGE with s11** (the registry is touched by both thickens — the m-2 guide arbitrates; the s9 PLAN states which registry surfaces it claims and coordinates the shared ones by relay); m-7 build-fidelity (executor/config/turn-baseline); m-1 fidelity (ENVELOPE-KEY-HYGIENE seam). B10 not in effect.

**Delegation conditions (a)–(g):** unique sub-IDs · schema/registry changes coordinate with s11 through the m-2 guide + escalate genuinely-new ones to master · locked-contract changes → owning pair + master · cross-domain → master · merge operator-only (`HUMAN_MERGE_AUTHORIZATION` at grant time) · blocked lanes report and hold · the dogfood/evaluation extends the s8+s10 ledger as one series.

ACTIONS_GIT_REF: none — dispatch only (disk refs: this relay + one INDEX.md row timestamped 20260713-130004; companion blocker `s9-scopepaths-cosign/…-130001`).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `39474d0` (`s10-close`, unpushed).
Next requested action: operator carries this to m-3.planner (+ the scope_paths co-sign to m-2/m-3); the pair returns the PLAN + plan-review, then issues its delegated token; the operator boots the s9 team the PLAN names. s9 ∥ s11 — the m-2 collision edge coordinated by relay.
