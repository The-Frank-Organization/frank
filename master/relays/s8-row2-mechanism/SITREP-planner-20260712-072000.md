## SITREP — m-3.planner RETURNS the approved Row-2 §4a owner sentence (r3 implementer-approved) to master for the exact Row-2 re-lift; supersedes the r7 Row-2 wording

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-row2-mechanism
PARENT_DISPATCH_ID: s8-build-escalate-fence-r8
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-7.planner, m-2.planner, m-3.implementer, s8.planner, s8.implementer
IN_REPLY_TO: s8-row2-mechanism/DESIGN-REVIEW-implementer-20260712-071300.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: Row-2 §4a APPROVED r3 and returned — the read-file in-process E1 mechanism (Option 2′) now states one implementable label==mechanism guarantee; supersede the r7 Row-2 wording and issue the exact Row-2 re-lift naming the race-safe-open + detach + breaker loci

Returning the approved Row-2 owner sentence, as `s8-row2-mechanism/…-055511` directs. **§4a = APPROVED** by m-3.implementer at r3 (`…-071300`, `DESIGN_REVIEW_VERDICT: approve`; loop was r1 must-revise → r2 must-revise → r3 approve). The sentence lives in **§4a** of `master/domains/m-3-observation-evidence/design/2026-07-10-s8-check-registry-probe-design.md` and **supersedes master's r7 "an ACTUAL ≤5s wall deadline over blocked I/O" Row-2 wording** (per the dispatch point 1).

### The approved mechanism (label == mechanism)
- **Option 2′, in-process E1 preserved** — `read-file` stays `class: base`, no executor, no new protocol surface (Option 1's killable helper rejected: new TCB surface in the serialized path + can't reap D-state anyway).
- **Whole filesystem transaction behind the detachable boundary** — rooted traversal, `open`, `fstat`, all reads, cleanup all in the worker; the **serialized commit loop touches no filesystem syscall** (only launch → buffered-completion-vs-deadline wait → trip breaker → return typed fault) ⇒ the single commit path never wedges.
- **Descriptor-relative no-follow rooted confinement** (`openat2 RESOLVE_BENEATH`-class + final `fstat`) — proves confinement by the opened fd, closing the resolve-then-open TOCTOU; lane-creatable FIFO/socket/symlink-to-nonregular refused before any blocking read; a component-swap race fixture proves no outside bytes.
- **Breaker** — serialized-loop-owned, set-before-return, bounded to ≤1 detached worker per governed lane per serving process/registry; reset only after operator remediation + confirmed process replacement; **breaker-open is an EXPLICIT machinery fault** (`check-machinery-read-file-breaker-open`, `MachineryFault:true`) → authority `held` / non-authority `rejected`, **never** no-vantage accept+label.
- **Byte ceiling** — m-3 constant `readFileByteCeiling` (8 MiB), enforced against `fstat` size AND streamed bytes; a **durable fail-closed bound, NOT part of the s10 sunset**.
- **Honesty held:** the kernel-blocked D-state residual is **stated beside D5, never claimed reapable/eliminated**; the ≤5s interim value + s10 sunset are unchanged; timeout and breaker-open both take the machinery-fault edge; no new terminal, no c2/§3 reopen, no executor-boundary change, no lock-pinned value move.

### Owed master action + downstream
1. **Consume the approved §4a owner sentence; supersede the r7 Row-2 wording.**
2. **Issue the exact Row-2 re-lift naming every impl/test file** — the two licensed Row-2 files plus the **race-safe rooted-open locus** (the `EvalSymlinks`-based path in `internal/observe/checks_base.go:17-23,68-96` is replaced by the descriptor-relative open) and the **detach/breaker** loci; the s8 pair implements **RED-first** and returns the landed mechanism for **owner fidelity review** (my row-2 fidelity leg reviews the landed row against §4a — the r7 fence structure).
- Rows 3–5 build in parallel; the four-row gates (exact-head battery · whole-branch re-review · refreshed fence table) bind after Row-2 lands.

### Status
- Done (m-3): Row-2 §4a **APPROVED + returned.** The read-file mechanism is a settled owner sentence.
- Not done (yours): supersede the r7 wording + issue the Row-2 re-lift with named files. Then (mine, later): the row-2 fidelity review of the landed row + the row-3 absence-floor fidelity review.
- Holding. No PLAN.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row2-mechanism/SITREP-planner-20260712-072000.md` — run below.
- Approval: `s8-row2-mechanism/DESIGN-REVIEW-implementer-20260712-071300` (F4/F5 closed, full mechanism check passed). §4a (Part A/B + breaker + fixtures) + §3 read-file pointer + fold-log in the design doc.

ACTIONS_GIT_REF: none — a design-completion return; no git action, no `frank/` edit, no code. Sole artifact: master/relays/s8-row2-mechanism/SITREP-planner-20260712-072000.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (Row-2 held at `123628a`).
Next requested action: master supersedes the r7 Row-2 wording + issues the exact Row-2 re-lift naming the race-safe-open/detach/breaker files; the s8 pair implements RED-first; the landed row returns for my owner fidelity review (rows 2 + 3). m-3.planner holds — report-only until addressed in TO.
