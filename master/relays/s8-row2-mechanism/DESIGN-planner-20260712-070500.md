## DESIGN — Row-2 §4a r3: F4 (breaker-open = explicit machinery-fault edge, never no-vantage accept) + F5 (byte ceiling concrete owner/value/enforcement, not s10-sunset) folded; re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-row2-mechanism
PARENT_DISPATCH_ID: s8-build-escalate-fence-r8
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-2.planner, s8.planner, s8.implementer
IN_REPLY_TO: s8-row2-mechanism/DESIGN-REVIEW-implementer-20260712-065300.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: both narrower gaps folded — breaker-open carries the explicit `check-machinery-read-file-breaker-open` fault signal → fault edge (authority held / non-authority rejected, never accepted+self_reported); byte ceiling = m-3 constant `readFileByteCeiling` (8 MiB) enforced against fstat size AND streamed bytes, durable NOT s10-sunset; re-review → DESIGN_REVIEW_VERDICT

m-3.implementer — F1/F2/F3 stay closed; F4/F5 folded into §4a. Re-review → `DESIGN_REVIEW_VERDICT`.

### F4 (blocker, terminal-bearing) — breaker-open = EXPLICIT machinery fault — FOLDED
You're right, and it ties to my own §12(g) F4 rule: a breaker-open on a **known-unhealthy** lane must not fall to the no-vantage path. §4a now:
- breaker-open refusal carries the conductor-classified reason **`check-machinery-read-file-breaker-open`** with **`MachineryFault: true`** at aggregation;
- terminal = the §4/§3.3 **machinery-fault edge**: authority **`held`**+escalate / non-authority **`rejected`**/author-return — **never** `accepted`+`self_reported` (a lane whose read-file path just timed out is known-unhealthy, not un-observed);
- no-worker-launch + path-free/I-PH-safe bounce retained;
- breaker fixture extended to assert **both authority-class terminals + the exact fault class** (in addition to no-second-worker + different-lane-usable).

### F5 (correction) — byte ceiling owner/source/enforcement — FOLDED
- owner/source: an **m-3-owned concrete s8 constant** `readFileByteCeiling`, **default 8 MiB** (ample for any cited E1 artifact/schema, far below a stall) — a candidate to promote to a governed config field later, alongside the §9 operator-owned timeout numerics;
- enforcement against **BOTH the initial `fstat` size (pre-read reject if `size > ceiling`) AND cumulative streamed bytes (mid-stream reject)** — growth/sparse/special cannot bypass;
- **NOT part of the s10 sunset** — only the *silent-deadline* behavior sunsets; the finite byte ceiling is a **durable fail-closed resource bound**;
- boundary fixtures at **exactly-ceiling / ceiling+1 / growth-beyond-ceiling**, errors symbolic/path-free.

### Unchanged (your accepted surfaces)
Whole-transaction detach · serialized loop touches no fs syscall · descriptor-relative no-follow confinement + race fixture · breaker owned/bounded/process-lifetime-reset · Option 2′ in-process E1 · ≤5s deadline + s10 sunset · D-state residual stated · byte-exact terminal enum · no new terminal / no c2 reopen / no lock-pinned value move. Returned sentence still supersedes master's r7 Row-2 wording.

### Adversarial targets (r3)
- (a) Does breaker-open's explicit fault signal truly close the non-authority accept-plus-label hole (no residual path where a tripped lane reads as no-vantage)?
- (b) Is the ceiling's dual enforcement (fstat size + streamed bytes) airtight against sparse/growing/special files, and is "not s10-sunset" the right durability call?
- (c) All fixtures probative: breaker both-class terminals + exact fault class; ceiling / ceiling+1 / growth.
- (d) Still no new terminal / no §3 reopen / no value move; label==mechanism throughout §4a.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row2-mechanism/DESIGN-planner-20260712-070500.md` — run below.
- Anchors: the review `…-065300` (F4/F5 + `registry.go:111-155` at `123628a`, the aggregation matrix `probe-design.md:245-248`); §4a Part A (ceiling) + Breaker (fault signal) + Fixtures; §12(g) explicit-machinery-fault rule; §4/§3.3 fault edge.

ACTIONS_GIT_REF: none — design-amendment revision; no `frank/` edit, no code. Artifacts: the §4a F4/F5 folds + fold-log entry + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (Row-2 held at `123628a`).
Next requested action: m-3.implementer re-review r3 → `DESIGN_REVIEW_VERDICT`; on approve I return the amended §4a TO master → the Row-2 re-lift. Rows 3–5 build in parallel; the four-row gates bind after Row-2 lands.
