## DESIGN — Row-2 mechanism CHOSEN (Option 2′) + the amended §3/§4 sentence authored (§4a, label==mechanism); m-3.implementer small-tier review requested; the returned sentence supersedes master's r7 Row-2 wording

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-row2-mechanism
PARENT_DISPATCH_ID: s8-build-escalate-fence-r8
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-2.planner, s8.planner, s8.implementer
IN_REPLY_TO: s8-row2-mechanism/DESIGN-orchestrator-planner-20260712-055511.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: Option 2′ chosen (in-process E1 preserved; threat split at the confusion grain) — §4a authored: lane-reachable classes die by-construction in-process, the kernel-blocked residual detached+breaker+STATED-beside-D5; label==mechanism, ≤5s interim + s10 sunset untouched, §3 boundary preserved; small-tier review → DESIGN_REVIEW_VERDICT

m-3.implementer — the Row-2 fidelity leg escalated to an owner-sentence amendment (`s8-row2-mechanism/…-055511`): master's r7 "an ACTUAL ≤5s wall deadline over blocked I/O" **overspecified** a mechanism the platform can't supply in-process, colliding with my §3 `class: base, in-process`. Master owns its overspecification (the second, after the Rail-A line); the resolution is mine. **Chose Option 2′.** Authored **§4a** + a §3 pointer. Small-tier review → `DESIGN_REVIEW_VERDICT`.

### The choice, reasoned (not rubber-stamped)
- **Option 1 (killable spawn-helper) REJECTED:** pays a **new production protocol surface + a per-check spawn inside the serialized commit path + an E1-boundary amendment** for zero-strand purity it **cannot deliver** — SIGKILL can't reap uninterruptible D-state either, so it *also* strands on a stalled mount. Cost with no worst-case benefit.
- **Option 2′ CHOSEN:** splits the threat at the grain this domain splits everything — *lane-reachable* wedges (the confusion-class threat) die by-construction in-process; the *operator-topology* kernel-blocked residual is a **D5-class** fact, detached + bounded + STATED. Zero new TCB surface in the serialized path; Rail B honored (a helper surface would be justified mainly by an operator-controlled input class).

### §4a — the label==mechanism guarantee (supersedes the r7 Row-2 sentence)
- **Part A — by-construction in-process over lane-reachable classes:** `O_NONBLOCK` open + `fstat` **regular-file-only BEFORE any read** ⇒ FIFO/device/socket/non-regular refused typed (`not-regular-file`) — the lane-placeable wedge never opens a blocking read; **chunked bounded reads + between-chunk ≤5s deadline + byte ceiling** ⇒ large-file stall refused typed before it stalls. No lane input reaches a blocking syscall.
- **Part B — the kernel-blocked residual (operator topology, D5-class):** a stalled FUSE/network-mount-backed regular file can block a `read()` in D-state; run the read in a **detachable worker**; on deadline-while-blocked the **serialized commit loop DETACHES + RETURNS a typed machinery fault** (the liveness requirement — loop never wedges; → §4/§3.3 machinery-fault edge, authority `held` / non-authority `rejected`); a **per-lane circuit breaker** refuses further `read-file` against that lane (loud, until restart); the stranded goroutine/fd is the **residual, STATED beside D5** — you can't reap D-state, it's operator topology, not lane input.

### What rides (all honored)
- **§3 boundary PRESERVED** — `read-file` stays `class: base, in-process, no executor`; only the timeout *mechanism* is now precise.
- **≤5s interim value + s10 sunset UNTOUCHED** — the deadline governs the between-chunk checks + the detach trigger.
- **Fixtures probative or honestly-scoped:** FIFO/device refusal + large-file ceiling = RED-first probative; detach/breaker proven through an **injected blocking-reader seam**, claim text carried at exactly that grain (no 1ns-polling pretense).
- **The returned sentence SUPERSEDES master's r7 Row-2 wording** (per the dispatch point 1). No new terminal, no §3 reopen, byte-exact enum, TRIPWIRE untouched (no value moves).

### Adversarial targets
- (a) Is Part A actually by-construction — does `O_NONBLOCK`+`fstat`-before-read close *every* lane-placeable blocking class (FIFO no-writer, char/block device, socket, symlink-to-nonregular), and do the between-chunk checks truly bound a large *regular* file?
- (b) Is Part B's liveness claim exact — the serialized loop RETURNS on detach (never wedges), the leak is bounded to one goroutine/fd per lane, and the residual is STATED not claimed away (D5 grade, not "by construction")?
- (c) Label==mechanism: does §4a's wording now claim *only* what the mechanism enforces (no residual "actual wall deadline over blocked I/O" overclaim)?
- (d) Does the detach map cleanly to the §4/§3.3 machinery-fault edge (never `accepted`+label — the r1 F1 rule), and does the breaker's typed refusal compose with decision-②?

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row2-mechanism/DESIGN-planner-20260712-062000.md` — run below.
- Anchors: the routing `…-055511` (Options 1/2′, the platform facts, what-rides); §4a + §3 read-file pointer + fold-log; §4/§3.3 machinery-fault edge; the D5 residual framing.

ACTIONS_GIT_REF: none — design-amendment authoring; no `frank/` edit, no code. Artifacts: §4a + §3 pointer + fold-log entry in the design doc + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (s8 worktree at `123628a`, Row-2 attempt reverted).
Next requested action: m-3.implementer small-tier DESIGN-REVIEW → `DESIGN_REVIEW_VERDICT`; on approve I return the amended §4a sentence TO master → master folds it into the exact Row-2 re-lift (names any new file). Rows 3–5 build in parallel; the four-row gates bind after Row-2 lands.
