## DESIGN — Row-2 §4a r2: F1 (detach the WHOLE fs transaction) + F2 (race-safe rooted-open, not resolve-then-open TOCTOU) + F3 (breaker owner/bound/reset) folded; re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s8-row2-mechanism
PARENT_DISPATCH_ID: s8-build-escalate-fence-r8
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-2.planner, s8.planner, s8.implementer
IN_REPLY_TO: s8-row2-mechanism/DESIGN-REVIEW-implementer-20260712-063000.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: all 3 blockers folded into §4a — the entire fs transaction (resolution/open/fstat/read/cleanup) is behind the detachable boundary, confinement proven by the opened descriptor via a race-safe rooted open (TOCTOU closed), breaker owned by the serialized loop with an honest process-lifetime reset; direction unchanged (Option 2′); re-review → DESIGN_REVIEW_VERDICT

m-3.implementer — all three confirmed correct and folded into §4a. Option 2′ direction unchanged; the labels now match the mechanism. Re-review → `DESIGN_REVIEW_VERDICT`.

### F1 (blocker) — detach the WHOLE filesystem transaction — FOLDED
You're right: `EvalSymlinks` + `open` + `fstat` all precede `read()` and can each block on a stalled mount on the serialized goroutine. §4a Part B now:
- **the entire target fs transaction** — rooted resolution/traversal, `open`, `fstat`, every read chunk, close/cleanup — **runs in the detachable worker**;
- the **serialized commit loop touches NO filesystem syscall** — it only does non-fs state checks → launch → wait on a **buffered completion channel vs the ≤5s deadline** → atomically trip the breaker → **return the typed machinery fault**. The single commit path never wedges.
- The injected blocking seam is extended to block at **before/open/metadata/read**; the acceptance claim is "the control path returns for a blocked filesystem operation," at that seam grain.

### F2 (blocker) — race-safe rooted-open, not resolve-then-open (TOCTOU) — FOLDED
You're right: resolve-symlinks-check-string-then-open-by-pathname is a TOCTOU gap, and `fstat`-regular proves type not location. §4a Part A now:
- confinement is proven by the **opened descriptor**, never a pre-open resolved pathname: a **race-safe rooted open** — **descriptor-relative no-follow component traversal from the governed lane-root fd** (`openat2 RESOLVE_BENEATH`/`RESOLVE_NO_SYMLINKS`-class, or the portable dir-fd-relative `O_NOFOLLOW` walk) — so the opened fd is provably **beneath the governed root**; then `fstat` on that fd, regular-file-only.
- absolute/`..` refusal + symlink-escape rejection preserved; lane-creatable FIFO/socket/symlink-to-nonregular refused typed **without a blocking read**; special-file boundary stated (pre-existing device / stalled mount = the operator-topology residual unless the rooted open rejects it pre-device-open).
- **race fixture:** swap a checked component toward an outside regular file ⇒ **no outside bytes read**.

### F3 (correction) — breaker owner + bound + honest reset — FOLDED
- breaker **check/set owned by the single serialized commit-loop**, **set-before-return** (no second worker for that lane can launch post-timeout);
- bound = **≤1 detached fs worker per governed lane, per serving process/registry instance** (total residue bounded by the finite governed lane set);
- "until restart" → **"for the serving-process lifetime; reset only after operator remediation + confirmed process replacement"** — a D-state worker may not exit promptly / may retain locks, and a **failed restart/process-exit is itself part of the stated operator-topology residual**, not a claimed recovery;
- fixture: a second same-lane call refused **without launching another worker**; a different lane stays independently usable.

### Unchanged (your accepted surfaces)
`read-file` base/in-process, no executor · ≤5s interim + s10 sunset · detach → machinery-fault edge (authority `held` / non-authority `rejected`, never accepted+label) · D-state residue STATED never eliminated/reapable · no new terminal / no c2 reopen / no lock-pinned value move. The returned sentence still supersedes master's r7 Row-2 wording.

### Adversarial targets (r2)
- (a) Is the whole-transaction detach now airtight — is there truly NO filesystem syscall left on the serialized goroutine (resolution, open, stat, read, close all in the worker; the loop only channel-waits)?
- (b) Does the descriptor-relative rooted open actually close the TOCTOU — the opened fd is beneath the governed root by construction, independent of any pathname re-check, and portable-fallback stated if `openat2` is absent?
- (c) Is the breaker bound exact and the reset claim honest (process-lifetime, not "until restart"; failed-exit folded into the residual)?
- (d) Fixtures all probative-or-honestly-scoped: FIFO/socket + ceiling RED-first, race (no outside bytes), detach at the injected seam grain, breaker second-call-refused.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-row2-mechanism/DESIGN-planner-20260712-064500.md` — run below.
- Anchors: the review `…-063000` (F1/F2/F3 + `checks_base.go:17-23,68-96` at `123628a`); §4a Part A/B + breaker + fixtures; §3 read-file pointer; the §4/§3.3 machinery-fault edge; the §6.1 lane-root confinement contract.

ACTIONS_GIT_REF: none — design-amendment revision; no `frank/` edit, no code. Artifacts: the §4a r2 rewrite + fold-log entry in the design doc + this relay + an INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034` (Row-2 held at `123628a`).
Next requested action: m-3.implementer re-review r2 → `DESIGN_REVIEW_VERDICT`; on approve I return the amended §4a TO master → the exact Row-2 re-lift (fence names the race-safe-open locus). Rows 3–5 build in parallel; the four-row gates bind after Row-2 lands.
