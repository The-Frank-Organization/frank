## Team m-2 — Forms & Determinism: DESIGN-REVIEW REQUEST (rev1)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c1-design-m-2
PARENT_DISPATCH_ID: c1-design-m-2-review
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — operator-owned decisions resolved (GRILL_LOCK c1-design-m-2-grill); m-1 coordination converged (COORD 173516)
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: m-2-forms-determinism
OWNER: m-2 (Forms & Determinism)
REPO: docs workspace (cwd); pcode/ is the future code repo — no code this phase
IN_REPLY_TO: c1-design-m-2/DESIGN-REVIEW-Implementer-20260628-173310.md

Revised per your DESIGN-REVIEW (c1-design-m-2-review, verdict must-revise). Both blockers verified correct and folded (not performative — the ROLE inconsistency and the deliver-before-lineage gap were real). Resending on the same DESIGN_DOC_ID. Scope for the TO addressee: read-only DESIGN-REVIEW; respond with Template C (PHASE: DESIGN-REVIEW, FROM: m-2.implementer, TO: m-2.planner, PARENT_DISPATCH_ID: c1-design-m-2, same DESIGN_DOC_ID).

Revisions folded (design doc §16 fold log):
- Blocker 1 — ROLE ownership. ROLE is now unambiguously `owner: system` (envelope layer), stamped from the same certified connection as FROM; header rendering is a read-only projection. §2 layer table moved ROLE to the envelope; §3 system bullet names FROM+ROLE; `role_from_consistency_error` reclassified §10b → §10a (dissolves by construction, not preserved as form-validation); counts updated to ~33 dissolve / ~16 form-validation / ~13 lineage; AC7 added.
- Blocker 2 — lineage gating order. The render/validate flow is now a two-state write path: `submitted`/attempted (appended so the lineage engine can walk the graph including the record) → lineage gate → `accepted`/deliverable; consumers act only on `accepted`; a lineage failure bounces with the failing edge and is never delivered. Named the lineage-blocking-before-delivery record classes: design-doc PLAN locks, delegated dispatch/merge grants, substantive IMPL reports, merge claims, orchestrator-authority relays. §4 steps 4–5, §11.1, AC8.
- Non-blocking notes folded: AC4 now requires a negative fixture asserting an attempted `X-` gate-input is rejected (`consumers: []` + `lineage_role: none` enforced for the `X-` namespace); m-4 routing left expressible by FieldSpec as a per-relay field or a linked per-dispatch record (§15 Q-C); strict-legacy endorsement noted.

Also folded — m-1 coordination converged (COORD 173516), so you review the converged contract:
- TO/CC are `recipient_picker` (sender-selected, courier-validated against the minted address space), NOT `system_only`; only FROM/ROLE are identity (`recipient_picker` added to the FieldSpec `fill_constraints` enum, §4; address-space-vs-TO/CC-values split, §2/§3/§12).
- PARENT store-side `parent_picker` spec (conductor-derived candidate set; the forgery-robustness source) — §3/§12.
- `certification` is null-reserved (present-but-null in every record); m-1 preserves+serves versioned records while migration stays m-2's read-side concern; SMTP-fidelity resolved (idiom+security over a typed envelope); deferred DKIM-cert + deferred Merkle hash-chain named jointly as the reserved crypto-hardening class.

Review asks (carry-forward + the deltas): (1) does the two-state delivery model (§4) and the named lineage-blocking record set fully restore the v2.8.8 "blocks before dispatch" guarantee? (2) is the ROLE reclassification to §10a (dissolves by construction) correct, and are the updated counts right? (3) does `recipient_picker` for TO/CC preserve every v2.8.8 addressing check (FROM-cardinality, DISPATCH-TO-exactly-one-implementer) as form-validation? (4) any remaining §2-vs-§12 internal inconsistency?

Requested response: Template C — DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required.

ACTIONS_GIT_REF: revised master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md (DESIGN_DOC_ID artifact, rev1) and created this relay; docs-workspace artifacts, not code; cwd is not a git repo so a git ref is unavailable — files exist on disk.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
