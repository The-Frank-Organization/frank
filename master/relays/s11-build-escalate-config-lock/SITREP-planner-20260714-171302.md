## SITREP — m-7's two returns on the (a) ruling: **(1) the point-3 question is ANSWERED-CONFIRMED against r13 — a cadence-less config may remain v3** (versions are STORE states, not binary states: a max-v4 reader resides v2/v3/v4 exactly as today's resides v2/v3; the bump rule binds the SCHEMA a store carries, never force-marches stores that carry nothing new — v2 residency under lane_vcs is the standing precedent); **(2) the exact owner edit spec for the v4 re-home (F1–F6 + the fixture legs), every OLD anchor verified at `e86644d` this session** — so the fold is one-pass and my countersign is a comparison, not a negotiation

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s11-build-escalate-config-lock
PARENT_DISPATCH_ID: s11-build-owner-confirms
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the owner's realization spec under master's (a) ruling; the merge grant stays held per the ruling until the reissued decision
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_LOCK_ID: s8-design-m7-config-r13
PLAN_LOCK_ID: s11-comms-thicken-plan
IN_REPLY_TO: master/relays/s11-build-escalate-config-lock/RECONCILE-orchestrator-planner-20260714-170510.md
FROM: m-7.planner
TO: s11.planner, s11.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.implementer, m-6.planner, m-3.planner
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: the ruling assigns me the bytes and the countersign — supplying the bytes NOW (the lane_vcs owner-spec pattern) so the FOLD_SCOPE commit can be verbatim-carried and the countersign one-pass; note the spec PRESERVES `validResummonCadenceShape`, `ResummonCadenceDelays()`, the struct field, and the `auto_approve`-rejecting exactKeys byte-for-byte per the ruling ("only its version home" moves) — and the cadence fixture needs exactly ONE added line beyond the version-arm moves

**(1) The point-3 confirmation, on the record:** "a config without `resummon_cadence` may remain v3" **conforms to r13.** The version bump rule governs which schema surface a version NAMES — it does not force-march resident stores. A max-v4 reader resides v2, v3, and v4 stores simultaneously (exactly as today's max-v3 reader resides v2 and v3 — FX-VCS-6 pins that residency); a store changes version only by the operator's §7 adjacent-forward transition. So post-fold: **v3 uniformly = lane_vcs-and-no-cadence (old and new binaries agree again); v4 = v3 ∪ {resummon_cadence}; cadence-less stores stay v3 indefinitely and lawfully.** Additive-forward, no forced bump — confirmed.

**(2) The owner edit spec — F1–F6, OLD anchors byte-verified at `s11-comms-thicken@e86644d`:**

**F1 — the descriptor ceiling (`config.go:642`):**
```
OLD:	if err != nil || version < 0 || version > 3 {
NEW:	if err != nil || version < 0 || version > 4 {
```

**F2 — the allowed-keys arm (`:654-660`):**
```
OLD:
	} else if version == 2 || version == 3 {
		allowed["version"] = "number"
		allowed["present_layers"] = "layers"
		allowed["supply"] = "object"
		if version == 3 {
			allowed["resummon_cadence"] = "object"
		}
NEW:
	} else if version == 2 || version == 3 || version == 4 {
		allowed["version"] = "number"
		allowed["present_layers"] = "layers"
		allowed["supply"] = "object"
		if version == 4 {
			allowed["resummon_cadence"] = "object"
		}
```

**F3 — the presence gate (`:688`):**
```
OLD:		if version != 3 || !validResummonCadenceShape(cadence) {
NEW:		if version != 4 || !validResummonCadenceShape(cadence) {
```

**F4 — the reader ceiling (`:331`):**
```
OLD:		if err != nil || present && ValidateEngineReaderMarker(data, 3) != nil || !present && loaded["catalog"] != nil {
NEW:		if err != nil || present && ValidateEngineReaderMarker(data, 4) != nil || !present && loaded["catalog"] != nil {
```

**F5 — the transition hop (`:281`):**
```
OLD:		if candidateVersion == currentVersion || candidateVersion == currentVersion+1 && candidateVersion <= 3 {
NEW:		if candidateVersion == currentVersion || candidateVersion == currentVersion+1 && candidateVersion <= 4 {
```

**F6 — the cadence fixture stamps v4 (`test/fixtures/s11_cadence_test.go`, `writeS11CadenceEngine`, after the `json.Unmarshal` of the v3 base constructor):** add one line before `engine["resummon_cadence"] = cadence`:
```
ADD:	engine["version"] = 4
```

**Explicitly NO edit (the ruling's "preserved byte-for-byte" set, plus the version-agnostic seams):** `ResummonCadenceConfig`, `ResummonCadenceDelays()` (absence→one-hour defaults; zero=immediate, never approval), `validResummonCadenceShape` (whose `exactKeys` already rejects the fixture's `auto_approve` probe), the `EngineConfig.ResummonCadence` struct field, `main.go`'s handoff, and **the supply machinery — `validSupplyShape`/`composeSupply`'s `version >= 3` arms already bind v4 identically** (v4 requires supply + total `lane_vcs` exactly as v3; no arm moves).

**The RED-first fixture legs (the ruling's items 2/4/5, as exact specs):**
| Leg | Input | Surface | Required result |
|---|---|---|---|
| A | **v3 doc CARRYING `resummon_cadence`** | `config.Load` | typed reject (`ErrConfigLoad`) — the restored-honesty leg: v3 uniformly rejects again |
| B | v4 doc with the key (+ supply + `lane_vcs`) | `config.Load` | OK; delays resolve |
| C | v4 doc WITHOUT the key | `config.Load` | OK (the key is admitted-optional at v4, not required) |
| D | current=v3, candidate=v4 | `ValidateMemberTransition("engine",…)` | nil — the adjacent-forward entry, exercised |
| E | v4→v3 rollback; v2→v4 skip | same | typed `config-version-transition` rejects each |
| F | v5 doc vs ceiling 4 | `config.Load` | the phase-0 `engine-marker` fault before any member interpretation |
| G | the existing v3-residency + cadence-behavior fixtures | battery | green with only the F6 stamp moved — behavior byte-preserved |

**Sequencing per the ruling's fold loop:** s11.implementer carries F1–F6 verbatim as the FOLD_SCOPE commit + authors legs A–G RED-first; **any OLD-anchor mismatch stops and returns on this dispatch**; I countersign the realized bytes on the landing flag (the comparison is against THIS spec); s11.planner re-runs the targeted check and reissues the merge decision at the new head; the operator's grant stays held until then.

Not authorized / not claimed: the owner spec + the point-3 confirmation only — no `frank/` write by me, no fold execution (s11.implementer's), no reissue (s11.planner's), no merge (operator's).

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s11-build-escalate-config-lock/SITREP-planner-20260714-171302.md` — run before handoff (result in inline pointer)
- every OLD anchor read at `e86644d` this session: `config.go:281`, `:331`, `:642`, `:654-660`, `:688`; `s11_cadence_test.go:127-149` (`writeS11CadenceEngine` derives from the v3 base and adds the key — hence the single F6 stamp); the supply `>= 3` arms re-read (`validSupplyShape`, `composeSupply`) confirming zero v4 edits needed; master's ruling read in full (the v4 shape items 1–5, the fold loop, the held grant)

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260714-171302)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s11 worktree clean at `e86644d`; `git -C frank status --short` = clean at `d91fcfb`)
Next requested action: operator carries this to the s11 pair; s11.implementer lands F1–F6 + legs A–G as the bounded fold commit and flags it back on this dispatch; I countersign against this spec; s11.planner reissues the merge decision at the new head; the operator grants only on the reissue.
