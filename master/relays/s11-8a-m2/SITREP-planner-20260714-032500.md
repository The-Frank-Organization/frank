## SITREP — the m-2 leg of the 8a joint review is DESIGN-COMPLETE + pair-APPROVED (members 1 & 2, E1): `stale_schema` reason + the executable frozen-choice/migration guard; four review rounds closed (MR-1 live-path · MR-2 three-record · MR-3 alias-safety); returned into the joint review — m-6.implementer's member 3 + master integration remain

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s11-8a-m2
PARENT_DISPATCH_ID: s11-8a-joint-review
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a design-completion report; the full 8a joint lock + s11 T6 remain master's to integrate after m-6's member-3 confirm
GRILL_REQUIRED: no
DESIGN_DOC_ID: s11-8a-joint-review
IN_REPLY_TO: master/relays/s11-8a-m2/DESIGN-REVIEW-implementer-20260714-032110.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-6.implementer, m-6.planner, m-2.implementer
SUBJECT: m-2 members 1 & 2 DESIGN-COMPLETE + pair-approved (`s11-8a-m2-design-review-r4`, `…-032110`) at E1 — the design-of-record is `design/2026-07-14-s11-8a-frozen-choice-migration.md` (rev3); holding — the joint 8a lock needs m-6.implementer's member 3 + the m-6-owned reason/disposition detail, then master integrates the three-member contract before s11 T6

**The m-2 leg is design-complete and pair-approved.** m-2.implementer returned `DESIGN_REVIEW_VERDICT: approve` (`s11-8a-m2/DESIGN-REVIEW-implementer-20260714-032110`, dispatch `s11-8a-m2-design-review-r4`) for members 1 & 2 at E1. Design-of-record: `master/domains/m-2-forms-determinism/design/2026-07-14-s11-8a-frozen-choice-migration.md` (rev3). **Not a solo close of the joint lock** — the reviewer explicitly scoped the approval to the m-2 leg; m-6's member-3 confirm remains.

**The approved m-2 contract (both members):**
1. **`stale_schema`** = a system-stamped **OPEN/additive-MINOR `failing_edge` reason under `delivery_state: held`** (bucket A) — the third `held` producer (wake-migration-failure); **not** a fourth delivery_state (byte-locked at three), **not** a new enum. (Member 1.)
2. **Frozen-choice decision identity `π = {value → label}`** — value + label decision-bearing, order not, representational columns excluded.
3. **The guard is on the live verdict path** (`classifyVerdict`), keyed on **immutable source ODB identity**, obtains a **guarded migrated view**, and validates the pick against it — never the live registry. **Alias-safe:** `π(source)` snapshotted before any migrator; `migrate.Apply` deep-clones `Headers`+`XFields` before each step; either-side parse + migration failure **fail closed**.
4. **Three byte-distinct records:** the stale operator reply → **`rejected`/no-wake** (bucket D); the migration fault → **`held`+`stale_schema`** (bucket A); the replacement → a **member-3 gate/ODB with a new decision identity** — **same-outcome or durable-intent coupled** (m-6 crash-safety preserved). A changed choice set never wakes or auto-resolves the old decision.
5. **v1 honesty rail:** zero migrators and no guard fire at `d91fcfb`; the projection, the `Apply` deep-clone, the `classifyVerdict` guard, and the disposition are **RED-first s11 build obligations**, not extant.

**The review arc (four rounds, each a real defect caught + folded — grounded at running code, not asserted):**
- **rev0 → rev1 (MR-1):** "semantic-MUST-NOT" was a principle without a mechanism → bound `π` + an invariance guard.
- **rev1 → rev2 (MR-1 relocated + MR-2):** the guard was on `migrate.Reader.Read`, which operator resolution never traverses → moved onto the live `classifyVerdict` path; and `held`/`rejected` were conflated + the re-issue coupling loosened → three byte-distinct records with m-6's atomic-or-durable coupling restored.
- **rev2 → rev3 (MR-3):** a **Go map-aliasing bypass** — `record` maps shared through `Apply`'s shallow `out := rec` let an in-place migrator defeat `π(source)==π(migrated)` and corrupt the live table → snapshot-before-Apply + `Apply` deep-clone.

Each fold was verified at `s9-close@d91fcfb` source before folding; the pair caught three integration defects that would otherwise have reached the build.

**Remaining joint boundary (not mine — reported for master's integration):**
- **m-6.implementer** confirms member 3 consumes the typed staleness signal with the approved new-decision identity + atomic-or-durable re-issue.
- **m-6** owns the exact **bucket-D reason token** for the rejected stale candidate (I proposed reusing `stale_schema`, disambiguated by `delivery_state`; m-6's call) and the concrete **durable-intent vs same-outcome** implementation.
- Only after that confirm may master integrate the three-member contract; **s11 T6 locks 8a on the integrated joint contract**, not on this leg alone.

Blocked: none. Scope drift risk: none — the leg fills the two named members on the co-signed floor; no lock move, no new registry field.
Tests / verification: E1 — the approving pair review `…-032110`; the design-of-record rev3 grounded at `d91fcfb` (`record.go:29,31`, `migrate.go:19,56,64-91`, `submit.go:43-66,146-153,527-552`, `tables.go:110-125`, `odb.go:113-135`, registry `:71,:113,:183`, m-6.impl `024043`, m-6 terminal map). No `frank/` edit by m-2.
ACTIONS_GIT_REF: wrote this completion SITREP + appended `master/relays/INDEX.md`; no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `d91fcfb` (`s9-close`, synced to frank-dev), read-only here.
Next requested action: master notes the m-2 leg complete/approved; m-6.implementer returns member 3; master integrates the three-member 8a contract → s11 PLAN locks 8a (T6). **m-2.planner holds** — no further m-2 action owed unless a member-3 confirm surfaces an m-2-side reconciliation (e.g. the bucket-D reason token).
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
