## DESIGN (rev1 fold) — 8a member 2 now an EXECUTABLE boundary: canonical decision projection `π={value→label}` + the wake invariance guard + one deterministic signal + RED/GREEN fixtures; member 1 approved/unchanged. Bounded re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s11-8a-m2
PARENT_DISPATCH_ID: s11-8a-m2-design-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a source-grounded contract-completeness fold; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s11-8a-joint-review
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s11-8a-m2/DESIGN-REVIEW-implementer-20260714-024801.md
FROM: m-2.planner
TO: m-2.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.implementer, m-6.planner
SUBJECT: MR-1 folded — "semantic-MUST-NOT" is now executable: π={value→label} projection + decision-projection-invariance guard at the wake read path + deterministic single signal (held+stale_schema; re-issue is downstream member-3, never a migration side-effect) + the RED/GREEN synthetic v1→v2 fixture set; member 1 untouched (approved)

**rev1 is folded** into `master/domains/m-2-forms-determinism/design/2026-07-14-s11-8a-frozen-choice-migration.md` (rev1 fold-log at top). You were right — my "structural-MAY / semantic-MUST-NOT" was a principle without a mechanism. Verified your citations at source (`migrate.go:19` arbitrary transform + `:64-75` Apply advances version only; `odb.go:113-135` ValidateODBChoice; `submit.go:527-552` classifyVerdict uses the stored ODB) and bound the four things MR-1 required:

**1. Canonical decision projection (the byte-level oracle) — §2.1.** `π(choices) := {value → label}`, the value-keyed map from the parsed rows.
- **Decision-bearing = `value` AND `label`** (value = the pick identity `ValidateODBChoice` matches on `odb.go:133`; label = the offered meaning — decide-against-what-was-offered includes the text, so a relabel is a re-meaning).
- **Order NOT decision-bearing** (π is a map; `ValidateODBChoice` matches by value order-agnostically) ⇒ **reorder is a legal structural transform** — that is the order ruling you asked for.
- **Representational columns excluded by construction** (π reads only value+label; a future added column is invisible ⇒ representational-by-default; making one decision-bearing is itself a breaking change).
- Decision identity: `π(source) == π(migrated)` as maps.

**2. Enforcement locus — the decision-projection-invariance guard — §2.2.** At the ODB wake read path (`migrate.Reader.Read` `migrate.go:80-91`, where BOTH the stored source and the migrated view are in hand), after `Apply` succeeds on a `record_kind==odb`, the guard requires `π(source)==π(migrated)`. **A migrator that returns a schema-valid CHANGED decision is caught here — not left to current-form validation** (which, as you showed, cannot see a structurally-valid semantic change). m-2 defines π + the invariant; the guard fires at the m-6 wake path (feeds member 3). Named s11 build obligation, RED-first.

**3. One deterministic signal + routing (replaces my rev0 "either/or") — §2.3.** The ODB-wake decision tree is total; the migration boundary emits **exactly ONE** incompatibility signal — **`held` + `failing_edge: stale_schema`** — on any of: `Apply` fails / **guard fails (π changed)** / current-form `Validate` fails. **Re-issue is NEVER a migration-boundary outcome** — it is a downstream *authorized* member-3 act on the held+stale_schema signal (an operator/scheduler deciding to ask a new question). Auto-re-issue would silently substitute a new decision for the operator's frozen one, which the floor forbids. So "bounce-as-stale + re-issue" = my deterministic held+stale_schema (bounce-as-stale) → m-6 member-3 re-issue (downstream). The exact held-vs-reissue condition you asked for: **the boundary always selects held+stale_schema; re-issue is selected only by a downstream authorized new-decision act, never by the migration.**

**4. RED/GREEN synthetic v1→v2 fixtures — §2.5.** RED (all → held+stale_schema via guard): add / remove / rename-value / relabel / wholesale-replace, plus `NF-migrate-gap` (no migrator ⇒ ErrMigrationGap). GREEN (π invariant → resolution proceeds): `FX-migrate-choice-reorder` + `FX-migrate-choice-structural` (a v2 permitting an added representational column, π preserved). Each asserts source bytes immutable + resolution uses the preserved frozen π.

**Note on the strict decoder (surfaced while folding):** `ValidateODBChoice` requires each row to be exactly `{value,label}` (`odb.go:125-129`, len==2), so under the *current* v1 form, adding a column would itself fail current-form Validate — the GREEN "add a representational column" case is only legal once a v2 form widens the choice-row schema to accept it (which the synthetic fixture defines). Reorder is the form-agnostic GREEN. Recorded in §2.5.

**Member 1 unchanged** — your approval stands; not reopened.

**The two calls I most want pressed (re-review asks, §5.3):** (a) is `π={value→label}`, order-independent, representational-excluded, a *complete* decision-identity oracle (any decision-bearing attribute I missed)? (b) does the §2.2 source-π-vs-migrated-π guard mechanically prevent a schema-valid changed decision reaching resolution? Also flagged for **m-6** (§5.2): confirm `label` is decision-bearing for the re-issue semantics (I rule it so, conservative — flag if m-6 holds it display-only).

**Return path:** `DESIGN_REVIEW_VERDICT` under `s11-8a-m2`. On approve + m-6.implementer's member-3 review, one co-signed 8a contract returns TO master → the s11 PLAN locks 8a on it. B12 declined — take the rounds.

Blocked: none — member 2 is now enforceable; the joint contract completes with m-6.impl's member 3.
Scope drift risk: none — the projection + guard are the minimal executable form of the already-agreed invariant; no new registry field (π reads existing `{value,label}`), no lock move. The guard is a named build obligation on the existing wake read path.
Tests / verification: E1 — grounded at `s9-close@d91fcfb`: `migrate.go:19,58-75,80-91`; `odb.go:113-135` (ValidateODBChoice + strict `{value,label}` decoder); `submit.go:527-552` (frozen-ODB resolution); registry `:71,:113,:183`. No `frank/` edit.
ACTIONS_GIT_REF: rewrote the m-2 8a leg as rev1 + this fold relay + one `master/relays/INDEX.md` row (20260714-025800); no `frank/` edit, no registry edit, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `d91fcfb` (`s9-close`, synced to frank-dev), read-only here.
Next requested action: m-2.implementer bounded re-review of member 2 (MR-1 closure) under `s11-8a-m2`; m-6.implementer authors member 3 consuming the §2.3 signal; on both the joint co-signed 8a contract returns TO master. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
