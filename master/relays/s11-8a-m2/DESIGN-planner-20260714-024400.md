## DESIGN (m-2 leg) — 8a joint review, members 1 & 2: `stale_schema` = system-stamped `failing_edge` reason on a `held` record (additive-MINOR / Rail-A OPEN, NOT a 4th delivery_state) + the frozen-choice/migration interaction (migrator upgrades STRUCTURE, never mutates decision-bearing CONTENT; resolution validates against the frozen offered set); DESIGN-REVIEW request to m-2.implementer; member 3 grounded for m-6.implementer

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s11-8a-m2
PARENT_DISPATCH_ID: s11-8a-joint-review
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a bounded co-sign completion of two named 8a members on the co-signed floor; no operator fork
GRILL_REQUIRED: no — the floor is co-signed and the members are named; this completes the contract, it does not re-open it
DESIGN_DOC_ID: s11-8a-joint-review
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s11-8a-joint-review/DESIGN-orchestrator-planner-20260714-023001.md
FROM: m-2.planner
TO: m-2.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.implementer, m-6.planner
SUBJECT: DESIGN-REVIEW the m-2 8a leg (`design/2026-07-14-s11-8a-frozen-choice-migration.md`) — members 1 (`stale_schema` reason home + version class) & 2 (frozen-choice/migration contract); the v1 honesty rail (zero migrators registered at d91fcfb — both members are the contract the first bump activates, RED-first-testable, not extant); member 3 grounded for m-6.implementer's joint leg

**My two 8a legs are authored:** `master/domains/m-2-forms-determinism/design/2026-07-14-s11-8a-frozen-choice-migration.md` (DESIGN_DOC_ID `s11-8a-joint-review`, m-2 members 1 & 2). Both grounded at `s9-close@d91fcfb` (cites inline), on the co-signed floor (migrate-then-validate; un-migratable → `held`/escalated, never silently dropped).

**Member 1 — `stale_schema` (CONFIRM):** home = a **system-stamped `failing_edge` reason value on a `held` record** — the **third `held` producer** (wake-migration-failure: `migrate.Apply` `ErrMigrationGap`/`ErrUnversioned`/`ErrFutureVersion` `migrate.go:58-68`, OR post-migration current-form `Validate` failure). **NOT a 4th `delivery_state`** (that set stays byte-locked at three `{accepted,rejected,held}`, registry `:71`); **NOT a new `enum_set`** (`failing_edge` `:113` is `owner:system`/`type:text`/`system_only` free-form — the forge vector is closed by `system_only`; a registry enum here = machinery the grammar doesn't need). **Version class = additive-MINOR / Rail-A OPEN:** no exhaustive consumer on the reason (the held/escalate disposition is driven by `delivery_state==held`, the reason is operator-display), so an unrecognized `stale_schema` still routes to generic held-escalate — ignore-unknown never becomes a silent accept ⇒ OPEN. Deliberately on the *open reason axis*, not the *closed outcome axis*.

**Member 2 — frozen-choice/migration (CONFIRM):** the migrator **MAY** upgrade the frozen `choices` (`:183`, `agent_enum_pick`/`row_array`) **STRUCTURE** (row-shape, identity-preserving) but **MUST NOT** alter decision-bearing **CONTENT** (add/remove/rename/re-mean a frozen choice) — the frozen set is the immutable record of *what was offered*. **The operator's resolution validates against the FROZEN offered set, not the post-bump registry options** (decide against what was offered). A bump that *would* change the offered set is **surfaced, never silently applied** → member 1's `held`+`stale_schema` (validation fails) or member 3's bounce-as-stale + re-issue. **Additive widen ≠ retroactive widen:** an additive/MINOR open-enum widen does NOT add the new option to an already-frozen set — new options are for new gates only, so the common bump leaves parked gates untouched.

**Member 3 grounded for m-6.implementer (not decided here):** a changed offered set = a **new decision identity** = a **new gate**, not a mutation of the frozen one — so a re-issued gate's resummon key (s10-built `seat+decision+cadence-slot`) reflects a new decision, distinct from the original's. m-6.impl owns the exactly-once/crash story + whether "decision" in the key is the gate's decision-identity; my confirm only fixes the upstream fact (content-change ⇒ new decision ⇒ new gate, never an in-place edit — consistent with the immutable store + never-dropped floor).

**v1 honesty rail (flagged prominently, §0):** at `d91fcfb` zero migrators are registered (`migrate.go:11` `Current=1`, empty `steps`), so **neither member fires at v1** — both are the contract the first breaking bump activates, RED-first-testable against a synthetic v1→v2 step. No claim `stale_schema` fires today. Same rail as §9 "ship the seam, zero migrators."

**The two calls I most want pressed (my pair-review asks):**
1. **Member 1 version class.** Is "no exhaustive consumer on the `failing_edge` reason" tight? If any m-6 bucket/escalation path switches exhaustively on `failing_edge` reason values, `stale_schema` would be MAJOR, not additive-MINOR — press this against the m-6 bucket-split (I claim the disposition keys on `delivery_state==held`, not the reason).
2. **Member 2 migrator boundary.** Is "structural-MAY / semantic-MUST-NOT" the right migrator boundary, and is "resolution validates against the frozen set, not the live registry" byte-consistent with how the ODB pick is actually validated at resolution (does the resolution path re-derive options from the current registry, which would contradict my confirm)?

**Return path:** `DESIGN_REVIEW_VERDICT` under `s11-8a-m2`. The joint co-sign completes on my two confirms (pair-approved) + m-6.implementer's member-3 review — one co-signed 8a contract returns TO master → the s11 PLAN locks 8a on it. B12 declined — take the rounds member 3's branch needs.

Blocked: none — my two members sit on the co-signed floor; the joint contract completes with m-6.impl's member 3.
Scope drift risk: none — fills two named members; no new mechanism (stale_schema rides the existing system reason slot; the migrator boundary is the existing §9 read-time seam).
Tests / verification: E1 — grounded at `s9-close@d91fcfb`: `migrate.go:11,58-68` (Current=1, zero steps, the failure modes), registry `:71` (delivery_state byte-lock), `:113` (failing_edge system/text), `:183` (choices agent_enum_pick/row_array); §9 + §17.1/§17.2 of the locked design. No `frank/` edit.
ACTIONS_GIT_REF: wrote the m-2 8a design leg + this DESIGN-REVIEW request + one `master/relays/INDEX.md` row (20260714-024400); no `frank/` edit, no registry edit, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `d91fcfb` (`s9-close`, synced to frank-dev), read-only here.
Next requested action: m-2.implementer returns DESIGN_REVIEW_VERDICT under `s11-8a-m2`; m-6.implementer authors member 3; on both the joint co-signed 8a contract returns TO master. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
