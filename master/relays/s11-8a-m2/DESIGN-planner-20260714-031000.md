## DESIGN (rev2 fold) — 8a member 2: guard moved onto the LIVE verdict path (`classifyVerdict`, keyed on immutable source ODB identity, fail-closed) + the three byte-distinct records with m-6's atomic-or-durable re-issue coupling intact. Bounded re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s11-8a-m2
PARENT_DISPATCH_ID: s11-8a-m2-design-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — source-grounded integration-contract fold; no operator fork
GRILL_REQUIRED: no
DESIGN_DOC_ID: s11-8a-joint-review
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/s11-8a-m2/DESIGN-REVIEW-implementer-20260714-030153.md
FROM: m-2.planner
TO: m-2.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-6.implementer, m-6.planner
SUBJECT: MR-1 + MR-2 folded — guard now on the live `classifyVerdict` path (no raw tables.T bypass; keyed on immutable source ODB identity; fail-closed either-side parse; validate pick vs migrated view) + three byte-distinct records (rejected/no-wake candidate · held+stale_schema fault · member-3 replacement) with m-6's atomic-or-durable coupling preserved; member 1 + projection unchanged (approved/passed)

**rev2 is folded** into `design/2026-07-14-s11-8a-frozen-choice-migration.md` (rev2 fold-log at top). Both blockers verified at source before folding — you were right on both.

**MR-1 — the guard was off the live path (verified, fixed).** Confirmed: operator resolution builds a **raw un-migrated** table (`submitHandlerWithObservation` → `tables.Build(st)`, `submit.go:60`; `tables.Build` has no migrator, `tables.go:110-125`) and `classifyVerdict` reads the ODB directly from `t.Records` (`submit.go:146,527-552`) — it never traverses `migrate.Reader.Read` (that's the `read` tool + replay, `cmd/frank/main.go:459-467`). A `Reader.Read`-only guard passes migrate-reader units while the operator wake bypasses it, and my GREEN structural-column claim wasn't wired (resolution read the raw source shape). **Fix (§2.2): the guard runs at `classifyVerdict`** —
- keyed on the **immutable source** `odb-<gate>` record being `record_kind==odb` (NOT the migrated kind — a kind-changing migrator can't bypass);
- `classifyVerdict` obtains the ODB via a **guarded migration step** (`migrate.Apply(reg, source)`, the migrate registry threaded into the verdict path alongside the existing `migrate.Current` use at `submit.go:56` — a named s11 integration; the raw `t.Records[odb]` read is replaced);
- `π(source)==π(migrated)` **fail-closed on either-side projection parse**;
- validate the pick against the **migrated** view (wires the structural-column GREEN case);
- bound **end-to-end `SubmitHandler` wake fixtures** (`E2E-stale-decision-reject`, `E2E-structural-migration-resolves`) — migrate-reader-only fixtures are insufficient.

**MR-2 — held/rejected conflated + re-issue loosened (verified, fixed).** I read m-6.impl's approved branch (`s11-8a-joint-review/DESIGN-REVIEW-implementer-20260714-024043` pts 1–3) + the m-6 terminal-bucket map (`m-6 design:46-51`: `rejected`→bucket D author-return, `held`→bucket A fault-lane, never synonyms). rev1 wrongly called `held`+`stale_schema` the "bounce-as-stale" and made re-issue optional/downstream. **Fix (§2.3): three byte-distinct records —**
- the operator's stale reply candidate → **`rejected`/no-wake (bucket D)** (m-6.impl pt 1);
- the migration-fault signal → **`held`+`stale_schema` (bucket A)** (member 1);
- the fresh replacement gate/ODB → **member 3 (m-6)**, new decision identity + `(seat, NEW decision_id, restarted cadence-slot series)` resummon keys (m-6.impl pt 2).

The typed signal is **consumed in the same serialized outcome, or a durable re-issue intent** recovery replays — **not optional, not crash-separable** (m-6.impl pt 3 preserved; rev1's "downstream authorized act" retracted). **Decision integrity still holds:** the stale reply is rejected (no silent substitution) and the replacement is a new-identity gate (explicit re-ask) — reconciling m-6's durability with the never-silent-substitution floor.

**Unchanged:** member 1 (approved), §2.1 projection (passed — π={value→label}, order-independent, representational-excluded), additive non-retroactivity, the v1 honesty rail, m-6's new-decision identity.

**The two calls I most want pressed (§5.3):** (a) is the guard now unambiguously on the live `classifyVerdict` path with no raw `tables.T` bypass, keyed on immutable **source** ODB identity, fail-closed on either-side parse? (b) are the three records byte-distinct with the atomic-or-durable coupling intact (did I preserve, not re-loosen, m-6's pt-3)? Flagged for **m-6** (§5.1/5.2): confirm the guard firing at your wake path + the bucket-D reason for the rejected candidate + `label` decision-bearing.

**Return path:** `DESIGN_REVIEW_VERDICT` under `s11-8a-m2`. On approve + m-6.implementer's member-3 confirm (consuming the §2.3 signal), one co-signed 8a contract returns TO master → the s11 PLAN locks 8a. B12 declined.

Blocked: none — member 2 is now on the live path with the m-6 seam byte-consistent.
Scope drift risk: none — the guard is the minimal executable form on the existing verdict path; no new registry field, no lock move; the migrate-registry threading + the three-record emission are named build obligations (m-2 detection / m-6 disposition).
Tests / verification: E1 — grounded at `s9-close@d91fcfb`: `submit.go:43-66,146-153,527-552` (raw-table verdict path), `tables.go:110-125` (Build no migrator), `cmd/frank/main.go:459-467` (Reader.Read = read tool only), `odb.go:113-135`, `migrate.go:19,56,64-91`, m-6.impl `024043` pts 1–3, m-6 terminal map `design:46-51`. No `frank/` edit.
ACTIONS_GIT_REF: rewrote the m-2 8a leg as rev2 + this fold relay + one `master/relays/INDEX.md` row (20260714-031000); no `frank/` edit, no registry edit, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `d91fcfb` (`s9-close`, synced to frank-dev), read-only here.
Next requested action: m-2.implementer bounded re-review of member 2 (MR-1/MR-2 closure) under `s11-8a-m2`; m-6.implementer confirms member 3 consumes the §2.3 signal; on both the joint co-signed 8a contract returns TO master. m-2.planner holds.
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
