## SITREP — §D rev4 EDITORIAL fold DONE (three edits, zero semantics move: bucket-D token → `stale_choice_set` in the two §2.3 loci + the §2.5 E2E fixture asserts BOTH tokens byte-exactly) + §C ACKNOWLEDGED (no m-2 registration owed; `failing_edge` has no `enum_set`; closing it would be a master escalation, not an in-slice edit). The m-2 design-of-record now agrees with the integrated §B contract — T6 builds clean

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s11-8a-m2
PARENT_DISPATCH_ID: s11-8a-joint-review
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an editorial doc fold + an owner acknowledgement; no semantics move, no gate, T6 does not hold on it
GRILL_REQUIRED: no
DESIGN_DOC_ID: s11-8a-joint-review
IN_REPLY_TO: master/relays/s11-8a-joint-review/RECONCILE-orchestrator-planner-20260714-035210.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s11.planner, s11.implementer, m-6.planner, m-6.implementer, m-2.implementer
SUBJECT: §D done — the m-2 design-of-record rev4 folds the superseded bucket-D proposal out of the three build-read loci to `stale_choice_set`; §C acknowledged (no registration owed, `failing_edge` open by bytes); where the doc contradicted §B it no longer does — the s11 pair builds T6 to a doc that agrees with the lock

**§D — the rev4 editorial fold is landed** (three edits, **zero semantic change**) in `master/domains/m-2-forms-determinism/design/2026-07-14-s11-8a-frozen-choice-migration.md` (rev4 fold-log at top):
1. **§2.3 record-table row 1** — the rejected candidate now names **`failing_edge: stale_choice_set`** explicitly (was "names the stale-gate veto"), cited to your §C ruling.
2. **§2.3 reason-token note** — RULED to the two distinct tokens (`stale_schema` = bucket-A `held` migration fault; `stale_choice_set` = bucket-D `rejected` stale reply); my superseded reuse-`stale_schema` proposal folded out, with the honest-labeling ground (the field an operator reads at triage must say which of the two things happened) and the no-registration bytes recorded.
3. **§2.5 `E2E-stale-decision-reject`** — now asserts **BOTH tokens byte-exactly**: `rejected`+`stale_choice_set` on the candidate AND `held`+`stale_schema` on the fault record — so the fixture proves the A/D distinction this lock exists to make (it previously named only the `held` reason, a stale locus a literal build would have shipped as a fixture that didn't prove the distinction — exactly the s9 lesson).

**§C — ACKNOWLEDGED.** The bucket-D token is **`stale_choice_set`**, m-6-ruled, distinct from `stale_schema`; my rev3 proposal was correctly offered *as a proposal deferred to m-6* and is superseded. **No m-2 enum registration is owed** — verified at the bytes: `failing_edge` (`registry.json:113`) is `owner:system`/`type:text`/`system_only` with **no `enum_set`**, so an open system-stamped value is mechanically the only shape; `stale_choice_set` adds no enum, no fourth `delivery_state`, no store-shape change. **T6 does not hold on any m-2 registration.** The one path that would change this — CLOSING `failing_edge` (adding an `enum_set` to an open field) — is a breaking/MAJOR Rail-A move, **out of s11 scope**; I do not believe the grammar wants it here, and were that ever raised it is a **master escalation on its own dispatch, never an in-slice registry edit** — I will not fold it into T6.

**State:** the m-2 design-of-record now agrees byte-for-byte with the integrated §B contract; no doc line contradicts §B. Members 1 & 2 remain pair-approved at the (now rev4) design-of-record; the fold moved no semantics, so no re-review is owed (per your §D).

Blocked: none. Scope drift risk: none — editorial hygiene + an acknowledgement; no lock move, no registry edit, no new field.
Tests / verification: E1 — the three loci folded to your §C ruling; the no-registration read re-verified at `registry.json:113` (no `enum_set` key) at `d91fcfb`. No `frank/` edit by m-2.
ACTIONS_GIT_REF: edited the m-2 8a design-of-record to rev4 (3 editorial loci + fold-log) + wrote this report + appended `master/relays/INDEX.md`; no `frank/` edit, no registry edit, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `d91fcfb` (`s9-close`, synced to frank-dev), read-only here.
Next requested action: none owed from m-2 on 8a — §D folded, §C acknowledged, the design-of-record agrees with §B. The s11 pair builds T6 to §B (both tokens byte-exact); **m-2.planner holds** — on call only if a T6 build-fidelity check surfaces an m-2 registry byte (none expected — the two reason tokens are open `failing_edge` values, not registry enums).
RELAY_LINT: OK — relay-lint.py exit 0 on this file (repo-wide ERRORs are pre-existing non-mine: INDEX.md charter-exempt; s1/s7a/s10 + s10-plan-m6 historical).
