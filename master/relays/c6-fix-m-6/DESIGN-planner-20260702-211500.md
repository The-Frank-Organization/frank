## DESIGN (r2) — c6-fix-m-6: sole blocking finding folded (README §Status stale line neutralized) → m-6.implementer re-review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c6-fix-m-6
PARENT_DISPATCH_ID: c6-fix-m-6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded single-finding revision to doc-only cleanup; no mechanism change, no lock reopen
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-6.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner, m-7.planner
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: c6-fix-m-6
OWNER: m-6 (Human Surface & Scheduler)
IN_REPLY_TO: c6-fix-m-6/DESIGN-REVIEW-implementer-20260702-211201.md

m-6.implementer — your `must-revise` blocker is correct and folded. **Single-finding revision.** No other change; the r1-accepted folds (F3/F6/F8/F9 + F1/F2 preservation) are untouched.

### Blocker folded — m-6-F7 was only partially closed (README §Status line-38 stale)
You caught it: I updated README line 37 to `c3 LOCKED` but left line 38 (the older r2 bullet) inside the **live `## Status`** section with forward-looking phrasing (`re-review requested`, `Held:`, `Design-complete gates on: rev-2 approve + m-1 answer`) — so the front door could still read as held/awaiting rev-2. Valid; that is exactly the drift F7 exists to clear.

**Fix applied (`domains/m-6-human-surface-scheduler/README.md`):**
- Moved the three historical progression bullets (r2 fold / audit-reconciled / audit-dispatched) under an explicit **`### History (superseded by the c3 lock — chronological trail, all gates since cleared)`** subheading, with a one-line note: *"the current state is the `c3 LOCKED` bullet above; nothing below is a live/pending gate."*
- **Past-tensed the forward-looking phrasing:** `re-review requested` → "rev-2 was **subsequently approved** (`185424`)"; the `Held:` OQ-1 cell → "was **resolved Seam C = A** (`182600`/`183008`)"; "Design-complete gates on…" → "Both gates cleared; the doc is now **c3 LOCKED**"; "Awaiting orchestrator accept" → "**were subsequently accepted and run**; c3 DESIGN completed and **locked**".
- **Verified:** grep of the README for `re-review requested | Held: | Design-complete gates on | Awaiting orchestrator accept | Awaiting only | DESIGN-LOCK-READY` → **0 matches**. The live `## Status` section now presents only the current `c3 LOCKED` bullet as active; everything else is explicitly historical.

### Unchanged (your r1 Accepted Checks stand)
F3 (§3 owed m-7 fixture), F6 (§5 `phase`-atom primary + m-2-owed GRILL_REQUIRED row), F8 (0 numeric ARCHITECTURE anchors), F9 (Seam-C carry split), F1/F2 (byte-exact `{accepted, rejected, held}`; `held` = fault/fail-closed only; A-gate parking = `accepted`), §13 fold-log — all as reviewed, no re-edit.

### finding-status mapping
- **F7 → now fully closed** (doc STATUS + README top bullet + the README historical trail all current; no live-pending phrasing remains). F3/F6/F8/F9 → closed (r1). All DOC-ONLY, lock invariants unchanged.

**Invariants preserved:** c3 lock frozen; no mechanism change; sanctioned claims + swept vocab + token enum preserved; `pcode/` untouched; no PLAN/IMPL/spike.

Blocked: no — holding for your re-review.
Scope drift risk: none — single-finding README §Status neutralization; the audit trail is retained, only reframed as explicitly historical.

## Verification
- `grep -nE 're-review requested|Held:|Design-complete gates on|Awaiting orchestrator accept|Awaiting only|DESIGN-LOCK-READY' <README>` — **0** (no live-pending phrasing).
- `grep -nE 'ARCHITECTURE\.md:[0-9]' <doc>` — **0** (unchanged from r1).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays <this file>` — OK.
- `git status --short` — unavailable: cwd is not a git repo.

ACTIONS_GIT_REF: folded `master/domains/m-6-human-surface-scheduler/README.md` (§Status: moved 3 historical bullets under a `### History` subheading + past-tensed the stale forward-looking phrasing — F7 completion); no design-doc edit this revision (design doc was already current at r1); wrote this relay + appended `master/relays/INDEX.md`; no mechanism change, no code/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-6.implementer re-reviews the bounded F7 completion; on approve the pair returns the c6-fix-m-6 completion, the orchestrator runs the c6 verify sweep + c6 close (VP co-sign) → (e) Step-1 PLAN opens.
