## SITREP — the m-1 (a)–(g) re-confirmation at H-16 rev16 @ `a349a329…`: **ALL SEVEN CONFIRM** — (a)–(e)+(g) re-bound with the carriage VERIFIED at the self-contained bytes; (f) FRESH-RULED per F99: the corrected custody basis is confirmed and is STRONGER than what rev12 asserted — the lock authority now genuinely rests on my s6 acquire-before-touch law; H-26 endorsed from the invariant owner's seat — F100-conditional accepted

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-mvp-confirm-m1
PARENT_DISPATCH_ID: step3-mvp-confirm-m1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — byte-bound owner confirmation; the operator merge grant stays terminal on the H-16 lane
GRILL_REQUIRED: no
FROM: m-1.planner
TO: master.orchestrator-planner
CC: m-1.implementer, m-7.planner, m-7.implementer, master.orchestrator-reviewer, operator
DESIGN_DOC_ID: h16-outcome-split-design
BUNDLE_ID: m-1-trust-identity
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: step3-mvp-confirm-m1/RECONCILE-orchestrator-planner-20260721-002720.md

master — the seven rulings at rev16, on my own read (hash reproduced: `a349a3297c608915510e7bf6de2e304b824897aae4184f7c71db928149ae316d`). **The F100 conditional form is accepted:** this confirmation binds only if m-2's parallel leg and the final master/VP join return over the SAME unchanged rev16 hash; any byte movement voids all legs and I re-cite.

### (a)–(e) + (g) — CONFIRM, re-bound at rev16; the carriage VERIFIED, not assumed
Per the routing's instruction I verified that the self-contained bytes carry the same normative content my rev12 rulings covered (the loci moved with the F97 materialization; the content did not): the §4a callerless contract and the `mint_predecessor`/chain-TIP predicate text (a/b); the §4a completeness proof with its exactly-once membership + intact-segment clauses and the full T-R7F1 evidence matrix including partial-omitting-the-true-latest ⇒ NO anchor (c); the operator-anchor fallback totality (d); the two-clause effective-quarantine predicate and the match-gated `auth:seat-quarantined` leak rule with its non-matching-probe negative (e); the one-pivot-per-credential rule, the retry scope predicate, NO-AUTHORITY-DELTA, and `retry-authority-delta` (g). **My rev12 grounds carry verbatim for these six scopes and I re-bind them at `a349a329…`** — including the (c) ruling's scope conditions (selection-only / one-time / never-presence-grade, no ongoing-authority precedent). **The F98 fixture split is noted and consistent with my discipline:** the conductor's committed `system-owned` rejection remains the enforcement authority (the raw-client legs still assert the committed rejection bytes + nullity); the conforming frontends' client-side `schema_invalid` gate is defense-in-depth, not a substitute locus — no weakening of the §0.e never-from-ingress rule.

### (f) — the FRESH ruling per F99: **CONFIRM the corrected custody basis** — and it is stronger than what I confirmed at rev12
The question as routed: is operator authority correctly grounded in **local store access + the phase −1 root lock + exclusive stopped-store ownership**, with the genesis analogy as an AUTHORITY basis only and the lock authority resting on the s6 acquire-before-touch law? **Yes, on all three parts:**
1. **The mechanism now genuinely rests on my law.** My s6 §D I1-P ruling: the claim is acquired BEFORE phase-0 recovery begins — because recovery mutates journal/projections and must be under the claim — held for the writer's lifetime, root = canonical identity not path string, and a bare liveness heuristic never satisfies the invariant. The F99 shape realizes each clause for the ceremony writer: `AcquireRoot` as **phase −1, before ANY root/store/binding/recovery touch, held through exit**; mutual exclusion both ways through the SAME lock (the conductor blocks/refuses while the ceremony holds it, and vice versa — one live writer per root, the ceremony now correctly counted as a writer); **socket-probe-as-exclusion correctly REJECTED** (a stale socket blocks nothing; a check-to-lock race admits a second writer — the same heuristic class as the pidfile my §D banned; the probe survives only as post-lock operator UX); the **alias-root fixture** carries my canonical-identity clause; the rev15 race-loser invariant (the loser performs ONLY `AcquireRoot`-intrinsic operations, no canonical/binding/projection mutation) is precise and honest.
2. **The `-mint` retraction is the honest move, and I endorse H-26 from the invariant owner's seat:** the live `-mint` path probes and mints BEFORE `AcquireRoot` (`main.go:112-122`, `:577-600`) — that is an **unlocked writer, an I1-P violation window at the live code**, and citing it as a lock precedent was false. Retracting the citation, naming the defect for the backlog, and shaping the ceremony's lock helper SHARED so `-mint` converges onto it is exactly the right disposition — the defect is real, its fix is correctly scoped out of H-16, and the convergence path is the right shape. My rev12 (f) confirmation had credited the mechanism to "the I1-P discipline" on the strength of that precedent; **the F99 correction makes the mechanism actually be what I credited it as** — hence stronger, not merely repaired.
3. **The authority/mechanism split is now clean:** the genesis analogy carries ONLY the authority claim (local custody + exclusive stopped-store = operator — the F-S6-M1-1 admin-custody floor extended), never the exclusion mechanism; the exclusion is the lock, under the law. The role-disagreement rule and the custody-alone eligibility carry unchanged from my rev12 (f) grounds (accepted-canonical-wins honestly applied; no pre-selection read of disputed bits; the selected tip's canonical bytes govern after).

**The R9-F2 fixture legs are the right proof set** for this ruling: lock-first asserted structurally (no ceremony operation NOR socket diagnostic before the successful `AcquireRoot`), the concurrent-startup race with the clean `root-lock-held` loser, the stale-socket cut, the alias-root cut, two concurrent ceremonies, and the conductor-start race while held.

Blocked: no. Byte-bound to rev16 `a349a329…`; F100-conditional on m-2's leg + the master/VP join returning over the same hash; any movement voids and I re-cite.

Tests / verification: rev16 hash reproduced by `shasum -a 256`; the revision trail read (rev13 F97/F98/F99 → rev14 R13-F1 → rev15 R14-F1/F2 → rev16 R15-F1); the F99 loci read in full (`:115` ceremony phase −1, `:126` eligibility, entries 15/17, the §10 R9-F2 legs); carriage greps for the six re-bound scopes (chain-TIP ×3, completeness ×5, operator-anchor ×4, predicate + `auth:seat-quarantined` ×4, `retry-authority-delta` ×4, one-pivot ×3); my s6 §D I1-P text compared clause-by-clause against the F99 mechanism.

ACTIONS_GIT_REF: wrote this confirmation relay + INDEX row; no doc/code/`frank/` change; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `6e4d657`.
Next requested action: master takes this leg + m-2's parallel leg into the final master/VP join over the same rev16 hash (the F100 condition); H-26 rides the backlog with my invariant-owner endorsement on record. m-1.planner holds — no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
