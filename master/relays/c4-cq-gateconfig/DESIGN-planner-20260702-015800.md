## DESIGN — m-6 CQ closure: CQ-3 pure-judgment A-floor table PRODUCED · CQ-4 bucket-D/`held` co-sign · CQ-4b config-composition CONFIRM → m-6.implementer review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-gateconfig
PARENT_DISPATCH_ID: c4-cq-gateconfig
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded contract-question closure against locked contracts; full-pair + co-sign; operator on CC
GRILL_REQUIRED: no
FROM: m-6.planner
TO: m-6.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-2.planner, m-2.implementer, m-3.planner, m-3.implementer, m-4.planner, m-4.implementer, m-7.planner, m-7.implementer
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: c4-cq-gateconfig
OWNER: m-6 (Human Surface & Scheduler) — lead on CQ-3 (A-floor table); co-owner on CQ-4 (bucket-D) + CQ-4b (config)
IN_REPLY_TO: c4-cq-gateconfig/DESIGN-orchestrator-planner-20260702-012336.md

**m-6's lead-planner answer for CQ-3 + the CQ-4/CQ-4b co-signs.** Bounded contract-question closure for m-7's design-lock — **no c1/c2/c3 reopen, no design-lock, no PLAN/IMPL/`pcode`/spike.** Full-pair: **m-6.implementer — independent adversarial review requested** before these close. I did **not** edit the c3 lock-ready design doc; the A-floor table's canonical home is this closure artifact (m-7 NF-S8 binds to the COORD table), and the bounded CQ-4 terminology alignment in my §2/§4 is flagged below as a fold-point carry (not a mid-CQ reopen of the lock-ready doc). No genuine contradiction with a locked m-6 contract surfaced.

---

### CQ-3 — pure-judgment A-floor table by (phase × record_kind) · m-6: **PRODUCED**

**What this is.** The *mandatory* HUMAN_GATE floor (`master/DESIGN-REVIEW-2026-07-01.md` §2A.6): for A-capable work, certain (phase × record_kind) crossings carry a **structural** A-gate — the conductor raises the floor **regardless of the seat's self-assessment**. This is the confused-agent guardrail: a confused seat that *fails to self-raise* an irreversible/human-reserved decision is still caught, because the floor fires from the record's class, not the seat's judgment. Distinct from a *situational* A-gate a seat raises on its own (`HUMAN_GATE_REQUIRED` RAISE); the floor makes the human-reserved class **non-downgradeable**.

**How it composes (m-2-confirmed `014626`).** The A-floor is **one MAX-input raiser** into m-2's §3 monotonic model: `floor = MAX(system baseline, agent RAISE, m-5 archetype-ceiling, m-4 routing-raise, m-6 A-floor)`. The mandatory floor always wins; a below-floor value is **never rendered/submittable** (structural, `m-2 …:39`). A **below-baseline pick** is reachable only via an explicit **`gate_category = ceremony_downgrade`** waiver — itself A-bucket, operator-visible. m-7 enforces at fill/submit (NF-S8), no new m-2 mechanism.

**Record_kind boundary (reader-has-a-writer).** The canonical `record_kind` enum is **m-2/m-4-owned** (m-4 `record_kind=routing …:194`; m-2 `authority_class` derives from `record_kind ∈` the authority-bearing set, `014626` CQ-2). m-6 does **not** author the enum — the A-floor **binds by the decision-class** each (phase, record_kind) carries, keyed to the locked `ARCHITECTURE §J2` A-set. The table's `record_kind` column names the committed-action class; the byte-exact enum spelling rides m-2/m-4.

**The table (shipped default; operator-configurable per §J2 except the hardcoded `other`→A fail-safe, `ARCHITECTURE:105` / m-6 §2:49):**

| # | Phase(s) | record_kind (committed-action class) | A-floor | Anchoring `gate_category` (§J2 A-set) | Why pure-judgment (not seat-discretion / not B) |
|---|---|---|---|---|---|
| 1 | MERGE-GATE | merge → **protected branch** (operator-configured set) | **A — mandatory** | `merge_to_protected` | affects shared dev/prod; the J2 merge-split reserves it to the operator (`ARCHITECTURE:106-109`) |
| 1b | MERGE-GATE | merge → feature/feature (incl. stacked) | — (no floor; **B**) | `merge_feature_to_feature` (B, orch-absorbed) | autonomy without affecting others/prod |
| 2 | IMPL · MERGE-GATE · LIVE-VERIFY | **irreversible / destructive / external / non-rollbackable write** | **A — mandatory** | `irreversible_write` | cannot be undone; a seat cannot self-authorize (read-only phases: N/A — no such record commits) |
| 3 | MERGE-GATE · LIVE-VERIFY | **residual-risk acceptance** (ship with known risk) | **A — mandatory** | `residual_risk_acceptance` | accepting known residual risk is an operator prerogative |
| 4 | LIVE-VERIFY | **live-verify skip / waiver** | **A — mandatory** | `live_verify_skip` | waiving live verification is operator-reserved |
| 5 | **ANY** | **ceremony downgrade** (a below-floor pick) | **A — mandatory (self-referential)** | `ceremony_downgrade` | the floor cannot be lowered without an operator-visible waiver; this is the monotonic self-reference m-2 confirmed |
| 6 | **ANY** | **authz / security / secret-boundary / credential change** | **A — mandatory** | `authz_security` | security-boundary changes are operator-reserved |
| 7 | DESIGN · DESIGN-REVIEW · PLAN | **product-semantics decision** (what the product does for users) | **A — mandatory** | `product_semantics` | "what should the product do" is not delegable to routing/orchestration; routes to the **meeting/collaboration lane, no-compress** (m-6 §5) |
| 8 | PLAN · PLAN-REVIEW · IMPL · REVIEW-FOLD | **scope expansion** (beyond locked scope) | **A — mandatory** | `scope_expansion` | expanding locked scope is operator-reserved; `scope_within_bounds` stays **B** |
| 9 | **ANY** | **`other` / unclassified** | **A — fail-safe (hardcoded, non-overridable)** | `other` → `human_decision_required` | J2 fail-safe: unclassified ⇒ human-only, never auto-absorbed (`ARCHITECTURE:105`) |
| 10 | **ANY** | **operator verdict** (operator-FROM) | — (N/A — **it IS the human**) | — | the record is the operator's own decision; no floor to raise |
| 11 | AUDIT · SITREP · RECONCILE | report-only records | — (no A-floor) | — | read-only phases carry no A-committing decision; a decision *surfaced* here is emitted as an A-gate on its own decision record_kind (rows 1–9), not on the report |

**Two roads to bucket A, both non-suppressible (coheres with CQ-4):** (i) the **structural A-floor above** — at fill/submit; (ii) the **internal-fault `held`** — at runtime (m-7 §6: a check that *failed to run* on an authority-bearing record → `held` → the m-6 A-surface, `m-7 …:100`). The A-floor governs *mandatory-gate placement*; `held` governs *disposition when a gate check faults*. m-6's bucket-A absorbs both.

**Operator-judgment surfaced (non-blocking, per m-2's "novel-category placement is operator-ratifiable" `m-2 …:242`):** the *membership* of rows 1–9 is the shipped §J2 default and is **operator-tunable config** (the map is m-6's surface, the values the operator's — GRILL_LOCK G5); only the row-9 `other`→A fail-safe is hardcoded. No operator decision is *required* to close CQ-3 — flagging that the table is a configurable default, not a fixed law.

### CQ-4 — terminal-state tokens · m-6 half: **CO-SIGN (bucket-D + `held` lane, byte-exact)**

m-2 settled `delivery_state = {accepted, rejected, held}` (`014626`; `rejected` canonical, `bounced` retired; `held` = post-commit gate-pending). **m-6 co-signs from the delivery vantage** — the mapping onto my §2 buckets:

| terminal token | m-6 bucket / lane | operator-facing? |
|---|---|---|
| **`accepted`** | normal delivery into the addressed mailbox; if the record is an A-gate → ODB **park** (§3/§4) | per record (A-gate → yes; else per bucket) |
| **`rejected`** | **bucket D — author-return**; `failing_edge` names the **acceptance-stage** veto (form-validation / lineage / observe-predicate / declared-vs-observed integrity-veto) | **No — author-facing** (unless the failing edge is *itself* an A-gate → §2 precedence) |
| **`held`** | **bucket A** — the ODB/park A-lane (operator-visible, **non-suppressible**); includes the m-7 §6 internal-fault-on-authority disposition | **Yes** |

- **Bucket-D naming = "author-return bounce", driven by `delivery_state = rejected` + `failing_edge`** (was `bounced` in my §2 table / §4 `bounced_repair` state — the token aligns to `rejected`; my park/wake FSM state name `bounced_repair` stays m-6-local but is now "the `rejected`-terminal author-return state"). ✓ matches m-2 "bucket-D aligns to `rejected`."
- **`held` drives the A-lane, NOT bucket-D** ✓ matches m-2 "`held` drives the ODB/park lane, not bucket-D" and m-7 §6.
- **No token conflict with the egress-A/D precedence (my §2, locked).** An **egress-block on an already-`accepted` A-gate's away-mirror** is an **outbox event** (`egress_scan_result = blocked` → my `egress_blocked` FSM → **bucket A local resummon**), **not** a terminal token on the gate record (the record stayed `accepted`/parked). So the three-token set governs *record* outcomes; mirror-egress-block stays the distinct A-lane event my §2 precedence already handles. No double-assignment.
- **Bounded fold-point carry (flagged, not hidden):** my c3 lock-ready doc §2 (two `delivery_state=bounced` spots, lines 40/45) + §4 (`bounced_repair`, line 76) carry the retired `bounced` token; the byte-exact alignment to `rejected` + the `held`→A row land when the orchestrator folds these closures / locks c3 — a terminology alignment to an already-settled token, **no contract change** (bucket D was always the acceptance-stage reject). Exact line pointers given so the fold is mechanical. **Co-sign: m-3 (observe-bounce token) + m-6 (this) + m-2 (settled).**

### CQ-4b — trusted-config composition · m-6: **CONFIRM (no correction)**

The CTO ruling (per-domain-authored sections → conductor-composed into one artifact under a single top-level digest; restart-only load; change = operator-authorized new committed store record) **preserves m-6's config assumptions.** My section = the gate_category A/B map + protected-branch set (§2 / §J2), the park/wake config (resummon cadence, away opt-in, digest interval — §4), the ODB config (§3), the egress whitelist (§4 / G5). All are **read-once-at-trusted-startup policy values** — the composed-section / single-digest / restart-only model fits exactly; nothing in my surface needs hot-reload.
- **The change-contract matches GRILL_LOCK G5** verbatim: "the §J map / protected-branch / away-opt-in / egress-whitelist config surface is m-6's, the **values** are the operator's." An operator config change = an **operator-authorized committed store record** carrying the recomputed digest — exactly the ruling's clause 3. ✓
- **Endorse m-2's layering note + m-4's per-section stamp** (`014626`/`013000`): a per-section version stamp under the one top-level digest lets my section version independently while the single digest still covers integrity — compatible with my config, no conflict.
- **One boundary note (reinforces, does NOT correct the ruling):** the operator's **runtime `away` availability toggle** (the away-bridge *trigger* — "operator out *now*") is a **runtime state on a separate channel** (a committed store record / runtime signal), **NOT part of the digest-pinned config artifact**. The artifact carries the away-bridge *policy* (opt-in, destination map, digest cadence — static, digest-pinned); flipping "out now" must not require a config re-digest + restart. The CQ-4b ruling is about **config composition** and does not contradict this — I **confirm** the ruling and note the runtime-away channel is out of its scope. This is the same pre-PLAN runtime-`away` resolution already routed (readiness-fix-c4 `012906`); **not a Cluster / CQ blocker.**

---

### CQ-status mapping (VP required-edit 3)
| CQ | m-6 disposition | status | closes when (addressed relays) |
|---|---|---|---|
| CQ-3 | A-floor table **PRODUCED** (this artifact; NF-S8 binds) | **m-6 lead complete — non-locking until review** | m-2 mechanics-confirm (**done, `014626`**) + m-6.implementer review |
| CQ-4 | bucket-D→`rejected` + `held`→A **co-signed** (m-6 half) | **m-6 half complete — joint** | m-2 (done) + m-3 observe-bounce token + all-three co-sign + m-6.implementer review |
| CQ-4b | **CONFIRM** + endorse per-section stamp; runtime-away boundary note | **m-6 confirm complete — joint** | m-2 (done) + m-3 + m-4 confirms + m-6.implementer review + CTO fold |

None is CLOSED by this relay alone (co-sign discipline); each maps to its exact remaining addressed relays. No CQ left implicit.

### Separately — CQ-6 co-sign (c4-cq-m1) — being filed alongside
m-1 produced its CQ-6 answer (`c4-cq-m1/DESIGN-planner-20260702-013500`); my away-token **park/wake co-sign** is filed as a sibling relay in that thread (window = m-6 policy vs one-shot nonce/burn = m-1). Flagging so it isn't assumed folded here.

**Invariants preserved:** R2 (the A-floor keys on decision-class/record_kind, never model; `held`/tokens carry no model); J1 (the A-floor never auto-approves — `ceremony_downgrade` is itself A-gated; `held` parks operator-visible, never silent-accept); monotonic-MAX (floor never lowered); no c1/c2/c3 reopen (the A-floor *binds* §J2 + m-2 §3, it does not change them).

Blocked: no — holding for m-6.implementer review + the m-3/m-4 co-signs (m-2's halves already landed `014626`).
Scope drift risk: none — the three named CQ rows only; the A-floor table binds locked §J2/§3, no new gate mechanism, no record_kind-enum authorship (m-2/m-4-owned), no locked-doc reopen (the §2/§4 token alignment is a flagged fold-point carry).

Tests / verification: read-only DESIGN closure; all claims E1 (`ARCHITECTURE §J2:101-109`; `master/DESIGN-REVIEW-2026-07-01.md` §2A.6:140; m-2 §3:39 + `014626`; m-4 `record_kind:194`; m-7 §6:100 / NF-S8:157 / NF-S16:165; m-6 design §2/§3/§4/§5).

ACTIONS_GIT_REF: none — wrote this closure relay + appended `master/relays/INDEX.md`; no design-doc edit (the A-floor table's canonical home is this artifact; the §2/§4 token alignment is a flagged fold-point carry), no code/source/`pcode/`, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
Next requested action: m-6.implementer adversarially reviews the A-floor table + the CQ-4/CQ-4b co-signs; m-3 co-signs CQ-4 (observe-bounce token) + CQ-4b; the CTO folds the closures into the m-7 design-lock package. I hold — no design-lock, no self-advance.
