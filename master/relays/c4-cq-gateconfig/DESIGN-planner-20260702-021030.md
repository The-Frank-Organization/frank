## DESIGN — m-3.planner revision: byte-consistency sweep folded per must-revise; re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-cq-gateconfig
PARENT_DISPATCH_ID: c4-cq-coord
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded byte-consistency patch of already-approved CQ shape; no new operator decision
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-3-observation-evidence
GRILL_LOCK_ID: c2-grill-m-3
FROM: m-3.planner
TO: m-3.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-4.planner, m-6.planner, m-2.implementer, m-4.implementer, m-6.implementer, operator
IN_REPLY_TO: c4-cq-gateconfig/DESIGN-REVIEW-implementer-20260702-020509.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: must-revise folded — 6 stale lines patched (fail-open + submitted-limbo); intended CQ-2/CQ-4/CQ-4b shape unchanged; re-review requested

m-3.implementer — your `must-revise` (`DESIGN-REVIEW-…-020509`) was correct and is folded. You **approved the intended shape** (CQ-2 mechanism, CQ-4 token cut, CQ-4b confirm) and blocked only on stale contradictory text a faithful builder could read as the old behavior. No mechanism changed; this is a byte-consistency sweep. Re-review the delta only; respond read-only `PHASE: DESIGN-REVIEW`, parent to this relay, same `DESIGN_DOC_ID`, return `DESIGN_REVIEW_VERDICT`.

### Finding 1 (decision-② byte-consistency) — FOLDED
- **§1:15** — "Self-reported completion … is delivered, stamped `self_reported`" → **non-authority-only**: authority/merge class unobservable→`self_reported` is **held/escalated, not delivered** (decision ②, §3.2(d)/§8).
- **§6:111** — "No mechanical *delivery* gate branches on either label" → qualified **"for non-authority records"** + the **one** class-conditional exception (`authority_class ∧ record_integrity==self_reported ⇒ held`).
- **§12:175** — opaque-lane floor "deliver-and-label" → **class-conditional** (deliver-and-label non-authority; held/escalate authority).
- All three now match §3.2(d)/§8, `READINESS-REGISTER.md:340-344`, `DESIGN-REVIEW-2026-07-01.md:142-143`, and key on m-2's declared `authority_class` (§17.6:328-335). R2 intact.

### Finding 2 (no-`submitted`-limbo byte-consistency) — FOLDED
- **§1:19** — "Two-state `submitted → accepted` write path" → **the readiness-fix-c1 no-limbo seam**: candidate held in-courier through pre-append form/lineage/observe, then **exactly one terminal outcome `{accepted | rejected | held}`**; no persisted `submitted` limbo (m-7 §6/NF-S16; m-2 §17.6:269-272).
- **§5.1:101** and **§13:200** — both stale "submit pre-flight vs the `submitted→accepted` transition" phrasings → "submit pre-flight vs the point of **terminal-outcome commit** (the readiness-fix-c1 no-limbo seam, not a persisted `submitted` state)". *(You cited :200; I also caught the identical phrase at :101 — same defect, both fixed so the sweep is complete.)*

### Surfaced, not silently resolved (guardrail)
The `authority_class == true ∧ record_integrity == mixed` edge: the §6 safety rule ("`mixed` = not-fully-verified anywhere a trust/gate decision reduces to yes/no") arguably implies a `mixed` authority record should also `held`, but m-2's declared key (§17.6:334) and operator decision ② both say `== self_reported`. I did **not** broaden the folded key — it stays `== self_reported` — and flagged the edge in §6:111 as an **open co-sign question for m-2/CQ-4**. Please confirm leaving it flagged (not resolved in this bounded fold) is the right call; it is a genuine substantive question, not a byte-consistency slip.

### CQ-status (post-revision)
- **CQ-2** → **corrected-by-artifact** (fold byte-consistent; keyed on m-2 `authority_class`). Foldable to closed on your re-approve.
- **CQ-4** → **closed (m-3 half)**, byte-consistent (no-limbo stale text removed). Needs m-2 + m-6 co-sign.
- **CQ-4b** → **confirmed, no correction** (you concurred). Needs m-2/m-4/m-6 confirms + CTO fold.

Delta only — §1:15, §1:19, §5.1:101, §6:111, §12:175, §13:200, plus the §15 fold-log sweep note. No mechanism change, no c1/c2/c3 reopen, no design-lock, no PLAN/IMPL/`pcode`/spike.

ACTIONS_GIT_REF: none — no git edit/commit/PR/migration; cwd (/Users/jack/Programming/harness) is a non-git docs workspace. Artifacts this turn: design-doc byte-consistency patch (§1, §5.1, §6, §12, §13, §15) + this relay + INDEX row.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)
