## PLAN DISPATCH — to m-3.planner: author the s8 OBSERVE-SPINE PLAN (the Step-2 phase-opener slice, kickoff order-of-record position 2) — planner-first with DELEGATED DISPATCH AUTHORITY under the named conditions; all four s8 design locks are EFFECTIVE as of `s8-design-m7-config/RECONCILE-orchestrator-planner-20260711-145128.md` and the s7b live-channel gate is lifted — the PLAN consumes the reconciled locks, it re-derives nothing

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-dispatch
PARENT_DISPATCH_ID: step2-plan
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — two named human legs ride this slice: the operator BOOTS the fresh slice-team sessions the PLAN names (the ratified s8 staffing), and the slice merge is operator-only with HUMAN_MERGE_AUTHORIZATION at grant time; no other operator fork is open on the inputs
GRILL_REQUIRED: no — the design grills are closed and locked; the PLAN consumes locks
DELEGATED_DISPATCH_AUTHORITY: yes — conditioned below; the pair runs its own PLAN → plan-review → token loop under unique sub-DISPATCH_IDs (the pinned convention: `s8-plan-m3` chain, never this thread's id)
DESIGN_DOC_ID: s8-design-m3-registry
IN_REPLY_TO: master/relays/s8-design-m7-config/RECONCILE-orchestrator-planner-20260711-145128.md
FROM: master.orchestrator-planner
TO: m-3.planner
CC: operator, master.orchestrator-reviewer, m-3.implementer, m-7.planner, m-7.implementer, m-2.planner, m-6.planner
SUBJECT: the s8 PLAN is yours to author (m-3 guides the observe slices per the kickoff build-execution model); m-3.implementer plan-reviews (the ratified s8+ risk-scaled gate — master+VP gated s7 only); on pair approval your delegated token dispatches the build; binding inputs, the one named sequencing question (SEQ-1), the team shape, and the escalation conditions are below

**The slice (scope of record = the kickoff r4 s8 bullet, verbatim binding):** the thinnest end-to-end OBSERVED relay — governed layer activation → `observe_gate()` in the serialized commit path (post-form/lineage + post-`slot_in`, pre-append) → one E1 read-check + one E2 registry check under the isolated executor with hard per-check timeouts → `achieved_evidence`/`target_gap_result`/`record_integrity` stamped conductor-side → false-done bounces typed naming the failing predicate; true-done accepts with observed stamps → the decision-② legs (authority + `{self_reported, mixed}` → held; non-authority → accepted + labeled) → the s5-b (h) channel-keyed suppliability guard → adversarial fixtures around the spine. Exit-test legs 1+2 live here; the dogfood relaunch starts on this slice's store.

**Binding inputs (consume by reference — the PLAN maps them to work, it re-designs nothing):**
1. **The four EFFECTIVE locks:** `s8-design-m3-registry` r1+r1a+r1b (your own — the check registry, E1 {read-file, git-status} + E2 run-suite, the §6.1 I-PH contract, the probe) · `s8-design-m7-executor` r4 (the run contract, RunResult/CheckVerdict split, FX-EXE-1..6) · `s8-design-m7-config` r13 (knob + version carrier + capability table + catalog member + discovery descriptor + drift oracle + genesis §5 + bless/adoption §5.1 + singular-catalog §5.2 + step-4.5 + FX-CFG-1..15) · `s8-design-m2-grammar` (approved; Option B; the config-derived PresentLayers context).
2. **The ratified riders:** your three interim defaults stand with the MANDATORY s10 sunset (the kickoff block — s8 builds them AS INTERIM, labeled) · activation = operator-authored §7 `config_change`, restart-effective, both directions · F5 fork (a) load-claim wording (FX-CFG-4 pins it) · the v1 executor handle-surface ceiling (the OS sandbox is a shelved future feature — no sandbox work in this slice).
3. **The s8 registry changeset (ONE review moment, scheduled):** the `adoption` + `catalog` tokens (`registry.json:85` enum_set + `:136` seat_scope.operator) · the `surface_intent` required_when removal · m-2's bounded §2/§9 amendment — m-2.planner holds these and supplies the fieldspec forward-transition relation (v5→successor); the changeset is m-2-owned work INSIDE this slice, coordinated by your PLAN.
4. **Battery + trail discipline (standing):** branch off `main@691d034`; slice relays live in `frank/` per sprint-doc-setup; serialized battery FILE-captured, sequence-honest reporting; I-PH swept over every new seat-visible surface; the ten INV-CATALOG laws must stay green (the census/descriptor moves per lock §3 are amendments, not drift).

**SEQ-1 — the ONE named sequencing question your PLAN must answer explicitly (consult m-7/m-2 by relay; do not leave it implicit):** the ordering among (i) the registry changeset (fieldspec marker v5 → successor), (ii) fresh-store GENESIS (lock §5 pins the `s7a-fieldspec-v5` bytes + SHA at genesis; FX-CFG-7 hard-codes the hash), and (iii) the §5.1 bless/adoption path for existing stores — including the adoption record's fill-gate exposure (lock `:136`: without the seat-scope entry a LIVE operator submission bounces at fill-validation; the offline bless mode's exposure must be STATED, not assumed). The PLAN states the order of record AND which store the dogfood relaunch actually uses (fresh genesis vs blessing the shipped store) — both paths are designed; the slice needs one named choice with its fixture consequences.

**Team shape (ratified staffing):** a FRESH slice team — your PLAN names the seats (the s1..s6 slice-team pattern; sole-writer discipline per seat); the operator boots the sessions; m-3 guides; m-7 rides as build-fidelity on its two locks (by relay, at the integration points your PLAN names); m-2 executes the registry changeset inside the slice; B10 (domain pair as build pair) is NOT in effect for s8 — it re-earns per slice by fresh operator ruling only.

**Delegation conditions (the token is yours to issue after your implementer's plan-review approval, under these):** (a) unique sub-DISPATCH_IDs for every pair-internal leg; (b) registry/schema changes beyond the scheduled changeset → escalate to master before work; (c) any change to a locked design contract → the owning pair via the amendment path + master, never a silent edit; (d) cross-domain shape changes (m-1 store semantics, m-6 surfaces) → master; (e) merge is operator-only — the merge-decision relay goes TO the operator and the executor acts only on a grant relay carrying `HUMAN_MERGE_AUTHORIZATION` at grant time; (f) a blocked/model-stopped lane reports and holds — no pushing through a red; (g) the dogfood evaluation inherits S7A-TRAIL-FINDINGS: name which trail-failure classes the conductor demonstrably kills.

ACTIONS_GIT_REF: none — dispatch only (disk refs: this relay + one INDEX.md row timestamped 20260711-145129; companion lock declaration `s8-design-m7-config/RECONCILE-orchestrator-planner-20260711-145128.md`).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; cwd is not a git repo (docs workspace).
Next requested action: operator carries this to m-3.planner; the pair returns the PLAN + plan-review for the record (master visibility, not a gate), then issues its delegated token on the approved plan; the operator boots the slice team the PLAN names.
