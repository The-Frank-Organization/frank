## SITREP — cross-branch handoff from the prior-art fork: the 2026-07-26 landscape sweep is DONE and filed as **PRIOR-ART.md §2d** (register at `e8189c767dbd13c1dacdc08ff4da7a091a4386f034926c0116de7e3d0c091664`). This relay carries ONLY what bears on work in flight in the main branch — the register holds the content (drift rule: no second copy). **Headline for the branch: four of frank's five pillars now have shipping analogs (AMQ typed evidence · jinn server-stamped provenance · gajae-code crash-honesty fragments · codex git-like session lineage), the fifth (routing-as-governance) acquired a published ANTI-position, and codex shipped the operator's session-versioning direction 72 hours after the operator chose it.** The compound still has zero occupants; the JOIN (typed evidence × stamped identity) is now the differentiator, not any single pillar.

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: step3-prior-art-sweep-0726
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-vp-review-5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a report-only cross-branch handoff; the register update is a master-owned maintenance act already performed. Nothing here ratifies, trims, or changes any in-flight contract.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
IN_REPLY_TO: master/PRIOR-ART.md
FROM: master.orchestrator-planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator
SUBJECT: Prior-art fork handoff — §2d filed (4 lanes, ~1,800 commits, 15 repos + web); five items bear on in-flight branch work: (1) codex `rollout_lineage` + OpenRath = mandatory pre-reads for the Step-4 session-versioning carry; (2) gajae-code `committed-degraded` + typed outcome domains = Route-3 edited-session prior art; (3) the hash-chain claim is RETIRED from frank's competitive core — amendment r2 must not claim tamper-evidence; (4) deepagents T13 in-tree verbatim + omp asymmetric allow/deny = dispatch-constant citations; (5) cadence pressure sharpened

To the main branch (same seat, forked session — this file is the durable handoff; the operator hand-relays the path):

## What bears on YOUR in-flight work, item by item

1. **Step-4 session-versioning carry (ROADMAP, pending):** codex shipped **`rollout_lineage`** (`05f000263b` — forks over a frozen source-history prefix, child-owned records, fork-boundary selectors) + per-thread single-writer filesystem locks (`5c94796dc9`), **within 72 hours of the operator choosing the same direction.** Academic formalization: **OpenRath, arXiv 2606.19409** (session as a first-class branchable/replayable value). **Both are now §2d adopt-list items with read-BEFORE-design-locks status.** The direction is validated and the port source exists; the cadence signal is the sharpest yet.
2. **Route 3 (edited-session state machine, awaiting owner returns):** gajae-code shipped the closest external crash-honesty implementation — an atomic prepare/ready/commit session-identity rotation whose post-commit fault surfaces as **`committed-degraded`** (`0c25d133`), and a typed rename-outcome domain where unrecognized codes are **refused, not guessed** (`de7ae1a3`). Worth citing in the Route-3 fold as independent arrival of the explicit third outcome. Its `504bbf11` producer/checker authority-split bug (two sides computing the same comparand with different authorities) is a cautionary tale for the Route-2 oracle's two-sided comparison.
3. **Claims discipline — binds amendment r2:** §2d formally **retires "tamper-evident (hash-chained) evidence" from frank's defensible core** (consequence of the operator's D1 re-scope + the m-1 deferral). r2 and all external copy must not claim it. The honest core as now registered: the compound/JOIN · byte-bound locks · typed-unknown-as-evidence · fill-time authority/forms · routing-as-governance.
4. **Dispatch-constant citations (T4-adjacent):** deepagents landed T13 in-tree (`f7922795d`) — "textual command classification cannot robustly constrain execution" — the field writing frank's fixed-constant argument verbatim; omp's `85110c366` asymmetric matching (deny matches any `&&`/`;` segment; allow requires whole-command, never compound) is a rule the constant's bash lane should state explicitly.
5. **Competitive/positioning:** AMQ now ships typed `evidence`/`no_evidence`/`error` with refuse-inference doc language (§2.7's "no evidence concept" superseded); jinn server-stamps provenance and structurally refuses caller-supplied source as "spoofable" (§2.9's "trust plane untouched" retired); paperclip verdicts hold (no chain, no unknown); opencode `specs/v2` unchanged twice running (port plan de-risked); jcode still zero trust on its DAG. New entrant to watch: **Galileo Agent Control** (Apache-2.0 runtime-governance control plane; launch date unresolved). Routing-as-governance remains ours alone — amq-squad's advisory-only routing policy doc is the quotable anti-position.

## Boundaries
Report-only. The register edit (§2d) is the only disk action of the fork besides its survey reads; no amendment, contract, fixture, manifest, lock, owner byte, or `frank/` path touched; no PLAN/T4. PRIOR-ART.md stays out of any public cut; no frank-dev refresh performed (on-request only). **H-12 hard-blocks external use.** Lane 4 stays held in the main branch on the three closure returns.

## Verification
- §2d landed and measured: section present once, §3 still follows, supersede markers present, UNVERIFIED discipline carried, hash-chain retirement present; register SHA-256 `e8189c767dbd13c1dacdc08ff4da7a091a4386f034926c0116de7e3d0c091664`.
- Four lanes completed and delivered (pi-lineage · control-plane · harness-majors · web); all repo claims carry commit hashes in §2d; web claims are primary-source-verified with three items explicitly excluded as UNVERIFIED.
- `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this handoff relay + one INDEX.md row + the PRIOR-ART.md §2d append (master-owned register). Nothing else.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: the main branch folds items 1–4 into its in-flight work at the appropriate moments (r2 drafting for item 3; Route-3 fold for item 2; the Step-4 carry pre-reads for item 1; T4 for item 4). No action is owed to this fork; it closes with this relay.
