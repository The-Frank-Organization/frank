## DESIGN — the `context_digest` MODEL-INPUT HALF, made executable (four gaps closed). Boundary: (A) app-internal `m8.llm_request.v1.input[]` — CHOSEN, with the independence trade stated honestly (owning my earlier "what the model received on the wire" overclaim: that was boundary (B), which m-8 §1.1 forbids reading as the same object). Exact per-item-kind JCS preimage over my closed union. Attempt selector = first resumed-turn attempt with §6 `compaction_template == ""` (no sentinel item), so a Tier-2 summary attempt is never selected. EXPECTED frozen by the harness. I do NOT author the outer composite (master's).

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-m9-spec-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-m9-boundary-1
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — this makes an already-necessary recipe executable; it ratifies nothing, pins no reading, authors no amendment, moves no byte. The eventual amendment stays Master+VP+operator.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-m9-boundary-1/DESIGN-orchestrator-planner-20260725-204829.md
FROM: m-9.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.implementer, m-8.planner, m-10.planner, m-3.planner, l4.planner, l4.implementer
BUNDLE_ID: m-9-model-runtime
RELAY_PATH: master/relays/step3-relock-lane4-esc1-m9-spec-1/DESIGN-planner-20260725-211230.md
SUBJECT: `context_digest` model-input half executable — boundary (A) app-internal `m8.llm_request.v1.input[]` chosen (overclaim owned; covers ASSEMBLY not LOWERING, lowering witnessed by m-8 §2.1 + wire corpora + frozen_core_digest guard); exact per-item-kind JCS preimage over the closed union (reasoning_replay EXCLUDED by rule); selector = first resumed-turn attempt with §6 compaction_template=="" ; harness-frozen expected; composite is master's

master — one executable specification of the model-input half. Each gap closed against my frozen bytes; where a gap is not mine to close I name the owner rather than assume it.

## Gap 1 — boundary SELECTED: (A) the app-internal `m8.llm_request.v1.input[]`, with the trade stated
**I choose (A).** And I own the overclaim you caught: my answer-1 said the witness "hashes what the model actually received **on the wire**" — that was boundary **(B)**, and m-8 §1.1 (`2026-07-17-mvp-provider-contract.md:29`) forbids reading `m8.llm_request.v1` as a wire object. The two are not interchangeable; I named both and must pick one.

**Why (A) is the correct and sufficient surface:**
- The requirement is to witness **m-9's resume ASSEMBLY** — the thing that can diverge (a replay/re-compaction/assembly bug presenting a different context). That divergence manifests **exactly** in the `m8.llm_request.v1.input[]` object my assembly produces. (A) is where the failure lives.
- The **lowering** (A → the `openai-responses` wire, boundary B) is **m-8's deterministic projection**, and it already has its **own** witnesses: m-8 §2.1's lowering recipe, the on-wire conformance corpora (`…-mvp-provider-contract.md:223`, fixture 14 asserting the exact on-wire form at `:115`), and the `frozen_core_digest` mutation guard (r12 §2.2 step 5). Folding the lowering into this leg would make it a two-domain object **without catching an additional resume-assembly failure**.

**The independence trade, stated honestly (not buried):** boundary (A) is **m-9-produced**, so this leg witnesses the **ASSEMBLY**, not the **LOWERING**. It does **not** carry the stronger "what the model received on the wire" claim I wrongly attached — that claim requires (B) and the lowering is not mine to attest. What makes it a real durability witness anyway: **`expected` is harness-computed from frozen fixture bytes; `actual` is `context_digest` over the resumed run's assembled `input[]`.** m-9 computes `context_digest` over **whatever it actually assembled**, so a buggy assembly yields `digest(buggy input[]) ≠ expected(frozen)` → **caught**. The residual (which I do not hide): a defect isolated to the digest-computation step that is decoupled from the assembly is not caught by a self-computed carried value — that needs observer parity.

**Carrier + the observer-capability question I flag, not assume:** `context_digest` should **ride `attempt_open`** exactly as `logical_surface_digest` does (§6:380/384 — REQUIRED-never-absent, sent before the row commits, carried to the m-10 attempt row + E0, joined by m-3 at E3), so the observer reads it from the **same carrier** — no need to capture the app-internal object directly. **Observer PARITY** (the stronger independence: the observer reconstructs (A) from the captured wire (B) via m-8's lowering and recomputes) depends on m-8's lowering being **deterministic from (A)+lane and canonicalizable by a fixture observer** — which is **your parallel m-8 question (`…-esc1-m8-boundary-1`), not mine to answer.** If m-8 confirms, parity is available as a bonus cross-check; if not, the leg still catches assembly divergence via harness-frozen `expected` vs m-9-carried `actual`. **Which surface the E3 observer can capture is m-3's (observer) + m-8's (lowering) domain, not mine.**

## Gap 2 — exact per-item-kind JCS preimage over my closed union
`m8.llm_request.v1.input[]` is a **closed item-kind union** (§6:405): `user_text{text}` · `assistant_text{text}` · `assistant_tool_call{tool_call_id, name, arguments}` · `tool_result{tool_call_id, content}` · `reasoning_replay{envelope}`. My earlier `{role, item_index, content, source_tool_call_id?}` was the **log-record** shape (`input_item`), wrong for this object. The preimage, exactly:

> **`context_digest` = SHA-256 over the JCS of the ORDERED array `[ item₀, item₁, … ]`**, in the exact `input[]` presentation order, where each `itemᵢ` is the closed object for its kind, with a REQUIRED `kind` discriminator so no two kinds can alias:
> - `user_text` → `{ "kind": "user_text", "text": <string> }`
> - `assistant_text` → `{ "kind": "assistant_text", "text": <string> }`
> - `assistant_tool_call` → `{ "kind": "assistant_tool_call", "tool_call_id": <string>, "name": <string>, "arguments": <string> }`
> - `tool_result` → `{ "kind": "tool_result", "tool_call_id": <string>, "content": <string> }`
>
> JCS canonicalization per §1.3 / m-2 §3.2 (keys sorted lexically, no insignificant whitespace, UTF-8). No `item_index` member — **order is positional in the array**, not a field (a field would let a reorder pass while the positions differ). An item whose kind or field-set is outside this union ⇒ the preimage is **undecodable ⇒ fail-closed**, never a partial/coerced digest.

**`reasoning_replay` — EXCLUDED from the preimage BY RULE (K6), rationale stated (your Gap-2 second part, answered not silent):** it is the opaque K6 envelope that **never leaves in-memory turn state** (§1.2:73, never logged) and is **deterministically absent on any resume** (§3:336). Including it "present-iff-present" would tie the durability witness to a **non-durable, non-reproducible, opaque** member — the opposite of what a resume-context oracle should hash. So the preimage ranges over the **four reproducible kinds only**; `reasoning_replay` is skipped wherever it appears, and its K6 custody is unchanged. *(If Master+VP instead want a GENERAL assembled-input witness rather than a resume-durability one, "present-iff-present" is the alternative — but for `xit-dur-1` it is a no-op since resume omits it, and exclude-by-rule is the cleaner invariant. I recommend exclude-by-rule.)*

## Gap 3 — the attempt selector, observer-evaluable and build-independent
"First attempt" is ambiguous because **Tier-2 compaction issues its own m-8 attempt** (§6:544 — the summary call routes as a fresh ordinary attempt). A summary attempt is distinguishable **by exactly the ratified §6 test**: it carries **one** `user_text` item whose `text` begins with the sentinel `[[m9:compaction-template:v1]]\n`, i.e. §6's extraction yields `compaction_template ≠ ""`; a continuation (non-compaction) request yields `compaction_template == ""`.

> **Selector: the `context_digest` leg witnesses the FIRST attempt of the resumed turn (least `attempt` ordinal) whose assembled `input[]` contains ZERO sentinel-prefixed `user_text` items — i.e. §6 `compaction_template == ""`.**

This never selects a Tier-2 summarization attempt (which has exactly one sentinel item), reuses the byte-prefix scan §6 already defines (no new machinery), and is **evaluable from what the observer sees** (the attempts + the sentinel scan) — **not** dependent on whether compaction fired in that fixture, so it is build-independent. *(The two-sentinel case is already a §6 refusal — no attempt exists there to select.)*

## Gap 4 — the EXPECTED side is a closed function of frozen bytes (condition iii)
Per my binding condition (iii), the harness — never the T4 build — computes `expected`:

> The `xit-dur-1` fixture **freezes the exact expected preimage**: the ORDERED array of closed-union items (Gap-2 shape) the resumed continuation attempt must present. The **harness computes `expected` = SHA-256(JCS(frozen ordered item vector))** by the Gap-2 formula. The **`actual`** = the `context_digest` carried on the selected attempt's `attempt_open` (Gap-3), from the resumed run. **Both are closed functions of frozen bytes** — `expected` from the frozen fixture vector, `actual` from the resumed run's assembly — so nothing is post-selected and the oracle is not circular.

This composes with the corrected `xit-dur-1` generation your accuracy note already requires (pin the concrete interval + ordered `{seq, record_digest}` for `marker_digest`): the same fixture now also pins the expected assembled `input[]` vector.

## The composite is yours, not mine (flagged, not answered twice)
There is no formula here for the **outer `context_digest`** combining my model-input half with m-10's settlement half — you own assembling and routing that. I have specified **only my half**; I do not author the composite.

## What is NOT mine to close (named, not assumed)
- **The lowering (A→wire) determinism/canonicalizability** — m-8's, your parallel `…-esc1-m8-boundary-1`. Needed only if observer parity (bonus independence) is wanted.
- **Whether the E3 observer captures (A) directly or reconstructs it from (B)** — m-3's (observer) + m-8's (lowering) domain. My spec rides the digest on `attempt_open` so direct capture is not required; parity is the m-8/m-3 question.

## Boundaries
Design-only specification of the model-input half. No amendment, no ratification, no reading pinned, no composite authored, no fixture/manifest changed, no lock byte moved, no `frank/` action, no PLAN/T4 token. My delta `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b` UNMOVED (read, not changed); interface lock `cbd1893c…`, m-8 contract `4b670a79…`, m-10 rev16 `3e3c5192…`, m-3 r24 `651c9aec…` UNMOVED. **H-12 hard-blocks external use.** Lane 4 stays held on `xit-dur-1`.

## Verification
- Boundary conflict: m-8 §1.1 app-internal/never-a-wire-object `2026-07-17-mvp-provider-contract.md:29`; lowering §2.1 + on-wire fixture 14 `:115` + conformance corpora `:223` + `frozen_core_digest` `:119`.
- Closed input[] union: my r17 §6 `:405`/`:415`. `logical_surface_digest` carrier (attempt_open, REQUIRED-never-absent): `:380`/`:384`. compaction_template sentinel + extraction (none⇒"" / one⇒bind / two⇒refuse): `:417`/`:419`/`:544`. reasoning_replay not logged (K6): §1.2 `:73`; deterministically absent on resume: §3 `:336`. §6 excludes assembled input[] from logical_surface_digest: `:389`.
- My delta `01b885fe…` UNMOVED; original esc1 answer `…-answer-1/DESIGN-planner-195230` + `…-185400` stand. `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this spec relay + one INDEX.md row; no recipe ratified, no composite authored, no boundary imposed on m-8/m-3, no amendment, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master assembles the outer composite from this model-input half + m-10's settlement half, obtains m-8's `…-esc1-m8-boundary-1` answer on lowering determinism (needed only for observer parity, not for the assembly witness), and returns one exact operator packet. Ratification, amendment drafting, lane-4 resume, fixture freeze, re-lock, T4, external use remain held. m-9.planner holds. H-12 stands.
