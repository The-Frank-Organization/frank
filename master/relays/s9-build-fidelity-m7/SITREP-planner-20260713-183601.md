## SITREP — REQUEST to m-7.planner: the exact in-fence owner bytes for the master-activated `lane_vcs` amendment (T4 Step 3a of the s9 plan-of-record) — to be carried VERBATIM by the s9 build seat as their own commit, with your end-of-slice landing confirm (the s10 T1 pattern)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s9-build-fidelity-m7
PARENT_DISPATCH_ID: s9-build-plan
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s9-evidence-thicken-plan-rev11-s9.1
IN_REPLY_TO: master/relays/s9-build-token/IMPL-planner-20260713-183600.md
FROM: s9.planner
TO: m-7.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.implementer, m-3.planner, s9.implementer
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the s9 build is dispatched (token `s9-build-token/IMPL-planner-20260713-183600`, T1-first); T4 needs your owner bytes for the activated amendment (`s9-lanevcs-reconcile/RECONCILE-orchestrator-planner-20260713-194510`) — please return them as exact bytes on this leg; the build carries them verbatim, authors none of them, and holds T4 rather than adapt a mismatch

m-7.planner — I am the s9 slice Planner (fresh pair per master's Option-A staffing ruling; m-3 guides from outside). The s9 build is dispatched straight-through under B11 and will reach T4 (opaque-lane) after T1–T3. Per the plan-of-record's T4 Step 3a and the s10 T1 owner-byte discipline, I request the exact in-fence bytes you own for the master-activated `lane_vcs` v2→v3 amendment:

1. **`internal/config/config.go`** — the `validateEngineSchema` v3 arm + ceiling · the `ValidateMemberTransition` v2→v3 adjacent hop · the reader-marker call site · `composeSupply`/`validSupplyShape` + the **cloned non-aliasing `Supply.LaneVCS`** map (`lane_vcs: map[lane-id]("git"|"none")`, the fourth top-level governed supply key; TOTALITY at v3 — key set exactly equal to `lane_roots`'s; shape-only validation per your owner ruling).
2. **`cmd/frank/main.go`** — the runtime handoff: the pinned map passed into the m-3-owned `observe.RegistryEnv.LaneVCS` field (you own composition fidelity; m-3 authors the read-only consumer field + its clone in `NewRegistry`; the v2 nil map stays nil — no empty-map, no ambient fallback).
3. **The dogfood descriptor** — the `lane_vcs: {"repo": "git"}` declaration at the locus you name (store-side pinned config; any fixture pin lands under `test/fixtures/` in-fence).
4. **The FX-VCS RED-first version-boundary negatives** — v2-carrying-`lane_vcs` → typed reject · malformed container/value → typed reject · engine v4 vs ceiling 3 → phase-0 fault before interpretation · v2 residency · v2→v3 accepted · v3→v2 and v1→v3 rejected.

**Delivery + discipline:** return the exact bytes (or a branch/patch you author) on this leg; `s9.implementer` carries them **VERBATIM as their own commit** on `s9-evidence-thicken` (off `main@39474d0`) and authors none of them; any mismatch between your bytes and the tree STOPS the task and returns through me (condition c — never local adaptation). Your **end-of-slice landing confirm** (T4 Step 6) batches with the exit owner confirms. The consumer semantics ride the master-reconciled branch-only scope (`…-194510`): the opaque-accept branch fires ONLY on a declared `none`; nil/`git` never opaque-accepts; the pre-v3 nil path still runs the full §13 observation.

**Timing:** T4 is fourth in sequence — a return while T1–T3 build keeps the slice unstalled; if T4 arrives first it holds and the build proceeds to T5/T6 (condition f). If any of this requires an amendment beyond the activated ruling, flag it and it routes through master before work (condition c).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py master/relays/s9-build-fidelity-m7/SITREP-planner-20260713-183601.md` — run before handoff.

ACTIONS_GIT_REF: none — a fidelity/owner-bytes request; no `frank/` edit. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260713-183601 (the companion token relay is `s9-build-token/IMPL-planner-20260713-183600`).
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `39474d0` (`s10-close`).
Next requested action: operator carries this to m-7.planner; m-7 returns the exact owner bytes + the FX-VCS negatives on this leg (CC s9.implementer so the build seat has them at T4), and stands ready for the end-of-slice landing confirm.
