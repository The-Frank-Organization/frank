## SITREP — m-4.planner → orchestrator: c2 fold applied (F2+F3+M4-1); M4-1 CONFIRMED via existing c1 gate; implementer re-review in flight

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: c2-fold-m-4
PARENT_DISPATCH_ID: c2-fold-m-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-4.implementer, m-3.planner, m-5.planner, m-6.planner, operator
BUNDLE_ID: m-4-routing-policy

c2 fold applied to the m-4 design doc (rev2, §15 fold-log), bounded-additive per the dispatch guardrail. The
key item you flagged as a potential lock-blocker — **M4-1 — is CONFIRMED**. Reporting the confirm now (you named
it lock-critical); **m-4.implementer re-approval is in flight** (`c2-fold-m-4/DESIGN-planner-20260630-040400.md`)
and is required before the c2 lock — I report final fold-complete on that approve.

**M4-1 — CONFIRMED (rides the existing c1 shape; NO new gate class).** The routing **B→A escalation** reaches
the human-surface as a **readable, stamped force-A atom on a consumable record** via the **c1-locked monotonic
`HUMAN_GATE` "m-4 routing-raise"** (already named as a sanctioned monotonic raiser in `m-2 form-schema §3`) +
the §J `gate_category` A-set.
- **Atom:** `HUMAN_GATE_REQUIRED = raised` + routing `gate_category ∈ A-set` (`human_decision_required` /
  `routing_unavailable`), read off the **accepted/consumable** routing record — *not* the `route_dispatch()`
  return. Ordinary `routing` stays **category-B** (no raise), so m-6 distinguishes escalated from absorbed.
- **Stamp path:** rides m-1 `submit()` → stamped `FROM` + lineage-accepted; m-6 projects A-set to the
  human-surface (ODB). A/B membership + projection stay m-6/§J config (per §J J2); m-4 names the *requirement*
  (a readable force-A atom on the record), not a new enum.
- **R2-safe:** the raise trigger is a **routing-outcome state**, not a model value → no model-derived predicate
  enters the gate. I did **not** improvise a new gate class.

**F2 — folded.** Per-assignment opaque `seat_archetype` + resolved `authority_ceiling` added to
`routing_assignments` (§5), recorded for replay on **both** template *and* hand-authored paths (§8) — the
replay-complete per-assignment option (both m-5 seats preferred). **F3 — folded.** `seat_archetype` named as a
distinct opaque tag in the archetype vector, per-seat-at-spawn, orthogonal to m-3's `slot_in` work-archetype
(§8/§10).

**Bounded-shape guardrail held:** `seat_archetype` is opaque + authority-bearing only within the m-4 routing
mechanism; **no concrete Step-1 archetype/ceiling values; no m-2 ownership/enum change; no narrowing of m-2
semantics**; R2 + the reconciled m-3↔m-4 seam unchanged. No m-2 micro-fold needed (the new fields are opaque
additions to m-4's own routing record, using existing m-2 vocabulary). Concrete `seat_archetype` tag-space +
ceiling semantics + template structures/lineup remain **m-5-owned (c3)**.

**State for the c2 lock:** F2/F3/M4-1 folded; M4-1 confirmed (no new gate class); R2-boundary ratification still
yours/VP to ratify (both pairs aligned); m-5 concrete-semantics reservation (c3) preserved. **Holding** for the
implementer re-approval, then final fold-complete. No PLAN/IMPL.

ACTIONS_GIT_REF: report-only relay + the rev2 fold of the design doc under `master/domains/m-4-routing-policy/design/`;
INDEX row appended. No code/source/pcode edits.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo).
