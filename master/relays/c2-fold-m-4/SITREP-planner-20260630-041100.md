## SITREP — m-4.planner → orchestrator: c2 fold-COMPLETE (implementer re-approved); m-4 design ready for the c2 lock

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: c2-fold-m-4
PARENT_DISPATCH_ID: c2-fold-m-4
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
FROM: m-4.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-4.implementer, m-3.planner, m-5.planner, m-6.planner, operator
BUNDLE_ID: m-4-routing-policy

c2 fold **complete and pair-approved**. m-4.implementer re-approved the rev2 fold
(`c2-fold-m-4/DESIGN-REVIEW-implementer-20260630-040641.md`, `DESIGN_REVIEW_VERDICT: approve`) — the re-approval
the dispatch required before the c2 lock is now in hand. The m-4 routing/policy design is ready for the c2 lock.

**Fold outcome (all three, bounded-additive; verified by the implementer):**
- **F2** — per-assignment opaque `seat_archetype` + resolved `authority_ceiling` on `routing_assignments`,
  replay-complete on **both** template and hand-authored paths (§5/§8).
- **F3** — `seat_archetype` = distinct opaque tag in the archetype vector (per-seat-at-spawn), orthogonal to
  m-3's `slot_in` (§8/§10); model-free, no `required_when`/`visible_when` branch on tag values, no ownership
  change.
- **M4-1 — CONFIRMED** (the named lock-blocker-if-unconfirmable, now closed): routing B→A escalation rides the
  **existing c1 monotonic `HUMAN_GATE` "m-4 routing-raise"** (named in `m-2 form-schema §3`) + the §J A-set;
  readable/stamped atom (`HUMAN_GATE_REQUIRED=raised` + routing `gate_category∈A-set`) on the accepted/consumable
  routing record; ordinary routing stays category-B; **no new gate class**; R2-safe (raise trigger = routing
  outcome state, not a model value). The implementer confirmed it matches the m-2 monotonic floor model + the §J
  A/B shape.

**R2 + the reconciled m-3↔m-4 seam unchanged; no m-2 micro-fold needed** (implementer-verified). Bounded-shape
guardrail held: `seat_archetype` opaque + authority-bearing only within the m-4 routing mechanism; concrete
tag-space/ceiling semantics/template structures + lineup stay **m-5-owned (c3)**.

**Remaining for the c2 m-4 lock (both yours/VP — not m-4's to close):**
1. **R2-boundary ratification** — silent-deviation block via m-3's generic integrity-veto (no model-derived
   schema predicate); both pairs aligned.
2. **m-5 reservation (c3)** — concrete `seat_archetype` tag-space / ceiling semantics / template structures +
   lineup reserved to m-5.

**Carry-forward (implementer watch-note, recorded):** if later work makes `seat_archetype` authority-bearing
*outside* the m-4 routing-record mechanism, defines concrete tag values, or adds predicates over those values,
that routes as a new m-2-adjacent fold — not silently absorbed.

**m-4 is design-locked-ready and holding** for the c2 lock. No PLAN/IMPL this phase.

ACTIONS_GIT_REF: report-only relay; the approved rev2 design doc under `master/domains/m-4-routing-policy/design/`;
INDEX row appended. No code/source/pcode edits.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo).
