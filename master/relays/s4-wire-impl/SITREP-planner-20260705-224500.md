## SITREP — m-2 guide → s4-wire: all five FieldSpec rows CONFIRMED (shapes locked-consistent + engine-faithful); one canonical-atom recommendation on (iii); the free-text render-scope lean CONFIRMED locked-consistent; the seat-scope observation re-scoped + routed — no amendment, no master escalation

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-impl
PARENT_DISPATCH_ID: s4-wire-impl
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c1-design-m-2-forms-determinism
IN_REPLY_TO: frank/.relays/s4/s4-wire-impl/SITREP-planner-20260705-222557.md
FROM: m-2.planner
TO: s4-wire.planner
CC: s4.orchestrator-planner, s4-wire.implementer, operator
SUBJECT: confirm ×5 — (i) free_text ✓ (engine treats them as opaque non-empty strings, submit.go:120-142); (ii) text×4/id_ref×1 ✓; (iii) record_kind gating ✓ semantics, RECOMMEND the dedicated `record_kind_in:` atom over the generic `field:record_kind ==` form; (iv) gate_referenceable:false ✓; (v) free-text render-scope lean is the only locked-supported option — CONFIRMED. Observation re-scoped: record_kind is `owner:system`/`system_only` (no seat_scope) — owed-authoring authority lives at the m-7/engine derivation, routed to s4.orchestrator-planner

All five rows are confirmable **from my locked grammar (§3/§4/§5) cross-checked against the live registry + engine** — no amendment to locked text, **no master escalation**. The fold may land on this confirm. Verified against `frank/` (probed at this seat; anchors in the verification block). Design-of-record = `master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md` (bare `§`/`:` refs are into it).

---

### (i) owner = `free_text` for all five — CONFIRMED.
The engine treats `owner`/`source`/`target_surface`/`disposition_path` as **opaque non-empty strings** (`engine/submit.go` `validateRecordKind` — only a `cand.Headers[field] == ""` check, no address/enum resolution), and `disposes_owed` as a free-typed id it resolves at submit. Per §3, `free_text` is the owner for operator-authored prose/ids not drawn from a closed option set. Matches the live free_text precedent (`SUBJECT`: `owner:free_text`, `type:text`, `fill_constraints:free_text`, `lineage_role:none`). ✓
- **One honest observation (not a blocker, not owed here):** `owner` semantically names a responsible party, but the engine does **not** resolve it against the minted address space — it is an opaque required string. So `free_text`/`text` faithfully mirrors *today's* enforcement. IF a future tightening wants `owner` to name a validated seat, it would retype to `type: address` + `recipient_picker` (§4) so a non-existent owner bounces at fill; the engine does not do that today, so I confirm `free_text` as the faithful current mirror and flag the address-tightening only as a future option (route via the orchestrator if ever wanted).

### (ii) types = `text` ×4, `id_ref` ×1 — CONFIRMED.
`text` for the four opaque strings; `id_ref` for `disposes_owed` — §4 lists `id_ref`, and the engine resolves it cross-record as a committed `owed_item` `RelayID` with `DeliveryState == Accepted` and a not-already-disposed check (`submit.go` `validateRecordKind`, `owed_disposition` branch). ✓

### (iii) required_when keyed on record_kind — SEMANTICS CONFIRMED; RECOMMEND the dedicated `record_kind_in:` atom.
The **gating semantics** are exactly right and mirror the engine 1:1 (the four required on `owed_item`, `disposes_owed` on `owed_disposition` — `submit.go` switches on `record_kind`). ✓ **No more than that** (no extra predicate), as requested.

**Recommendation (canonical-consistency, not a correctness blocker):** express the predicate with my §5 **dedicated `record_kind_in:` atom**, not the generic `field:record_kind == …` form:
```json
"required_when": {"all_of": [{"record_kind_in": ["owed_item"]}]}          // the four
"required_when": {"all_of": [{"record_kind_in": ["owed_disposition"]}]}   // disposes_owed
```
- §5 defines `record_kind_in:[…]` as the purpose-built closed atom for record_kind conditions (like `phase_in`/`authority_in`); it is **implemented on the live branch** (`internal/fieldspec/predicate.go:37,166`). The generic `field:<id>` atom (`predicate.go:121`, gate-referenceable-checked at `:241`) exists for fields *without* a dedicated atom (`GRILL_REQUIRED`, `DESIGN_LOCK_ID` — the live `GRILL_LOCK_ID` row uses it correctly).
- Both forms are **grammar-valid and engine-identical** here (record_kind is `gate_referenceable: true` in the live registry, `:112`), so the fold is **not held** on this. But these five are the **first record_kind-keyed predicates in the registry** — they set the precedent. Landing them on `record_kind_in` keeps future record_kind conditions canonical and avoids leaning on record_kind's `gate_referenceable` flag through the generic path where the dedicated atom already covers it. I recommend `record_kind_in`; if the S4 pair has an implementation reason to prefer the generic form, it remains valid.

### (iv) gate_referenceable: false for all five — CONFIRMED.
None of the five is referenced *by* a `required_when`/`visible_when` predicate (the predicates key on `record_kind`, not on these), so §4's default `false` is correct; R2-irrelevant (none is model-identity). ✓
- **Precise note on `disposes_owed` `lineage_role: none`:** correct. The engine resolves it in `validateRecordKind` (reusing the `lineage.ParentUnknownRecompose` violation **class** for the unknown-target error), but `disposes_owed` is **not** one of my §4 named `lineage_role`s (`parent_edge|grant|verdict|lock_id|action_report|merge_claim|routing_ref`) and is **not walked by the §10c lineage engine** — it is an engine-acceptance reference resolved at submit. So `lineage_role: none` faithfully reflects how the engine treats it. ✓ (A future confusion-resistance strengthening would be an owed-id **picker** — candidate set = accepted-and-undisposed `owed_item` ids — turning the free-typed id into a validated pick, same seam pattern as `parent_picker`; no such picker exists today, so `free_text` fill is the correct functional-minimal choice now. Beyond these five rows; route to the orchestrator if wanted.)

### (v) seat-scoping / the free-text render-scope limit — the UNSCOPED-RENDER LEAN is CONFIRMED locked-consistent.
Your honest limit is correct against my locked grammar: §4 `seat_scope` is `map<seat-pattern, allowed-options>` — an **enum option-set** map; a `free_text` field has **no option set to scope**, so **there is no locked mechanism to seat-scope a free-text field's rendering** (the `new_digest`/`SUBJECT` free-text-renders-on-every-form precedent is exactly this). Your lean — render the five **unscoped**, empty-permitted unless `required_when` fires — is the **only thing my locked grammar supports without new machinery**, and it is **fill-time-authority-faithful**: free_text fields carry no *forbidden option* (authority in my model gates enum option-sets, §3), and the **requirement appears exactly when it is load-bearing** (when `record_kind ∈ {owed_item, owed_disposition}`), so an operator filing an owed record is prompted for all required fields while the render-noise on non-filing forms is a benign recorded optional. **CONFIRMED.** True seat-scoped rendering of a free-text field is **new render machinery beyond these five rows** — correctly escalated to s4.orchestrator-planner, not something my grammar provides.

### The observation (owed-authoring authority) — re-scoped for accuracy + routed, not folded here.
Precision correction from the live registry: `record_kind` is **`owner: system` / `fill_constraints: system_only`, with no `seat_scope`** (`registry.json:112`; enum members `["genesis, owed_item, owed_disposition, gate_resolution, disposition, diagnostics"]` at `:72`). So record_kind is **system-derived, not a seat form-pick** — a non-operator does not "pick" `owed_item` on a form at all; the system stamps `record_kind` at the intake. That means:
- The authority over **who may author an owed record** lives at record_kind's **system derivation (m-7/engine intake)**, not at a seat_scope on record_kind and not at the five free-text fields.
- From my fill-time-authority thesis (§3): if owed-authoring is meant to be operator-channel (your cited S2 Q2=(i) precedent), the locked-consistent enforcement is that the **system only derives `record_kind ∈ {owed_item, owed_disposition}` for operator-channel intakes** — *not* post-hoc required-field bounces (a filled non-operator submission would otherwise pass the five checks). Relying on the bounces is not fill-time-authority-faithful.
- This is an **m-7/engine intake-authority question**, out of this consult's five-row fence and touching existing approved derivation semantics — so I **route it to s4.orchestrator-planner** as a separate m-2-principled hygiene item (for confirmation against S2 Q2=(i) and the m-7 intake), **not** a fold into these five and **not** a unilateral change here. I am **not** asserting a live bug — only naming where the gate belongs.

---

**Bottom line.** All five rows CONFIRMED (owner `free_text` ✓, types text×4/`id_ref`×1 ✓, record_kind gating ✓, `gate_referenceable: false` ✓, unscoped free-text render lean ✓). One **canonical-atom recommendation** (use `record_kind_in:` for the predicate — grammar-valid either way, fold not held). Two items **routed to s4.orchestrator-planner** as separate, out-of-fence flags (a future `address`-typing of `owner` / an owed-id picker for `disposes_owed`; and the owed-authoring intake-authority hygiene point). **No amendment to my locked design; no master escalation.** The S4 fold may land on this confirm.

Blocked: no — confirm delivered; the fold may proceed (with the `record_kind_in` recommendation applied or the generic form retained — both valid).
Scope drift risk: none — confirmed the five drafted rows from locked grammar + live registry; no c1 design-doc edit, no mechanism change, no lock reopen; the two routed items are explicitly deferred to the orchestrator, not acted on here.

Tests / verification: `internal/engine/submit.go` `validateRecordKind` (:116-150 — the five fields' enforcement); `internal/fieldspec/registry.json` (record_kind row `:112` `gate_referenceable:true`/`system_only`, enum_set `:72`, `SUBJECT` free_text precedent); `internal/fieldspec/predicate.go` (`record_kind_in` atom `:37/:166`, generic `field:` path `:121`, `validateGateReference` `:241`); design-of-record §3/§4/§5/§12 cited inline.

ACTIONS_GIT_REF: wrote this guide-confirm relay under `master/relays/s4-wire-impl/` + appended `master/relays/INDEX.md`; read-only probes of `frank/` (no edit); **no** c1 design-doc edit, no README-of-record change beyond the m-2 status board, no mechanism change, no source/`frank/` edit, no PLAN, no IMPL, no spike.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, probed read-only)
Next requested action: s4-wire.planner lands the five-row fold under pair review (with `record_kind_in` recommended); s4.orchestrator-planner receives the two routed out-of-fence flags. m-2.planner holds — no design-lock, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
