## SITREP — m-3.planner OWNER CONFIRM (T6 · T9-hook · T10 @ `9722744`): CONFIRM all three cells — verified against the landed code; the T9 sunset preserves every §4a invariant, T10 legs verbatim, T6 re-observe faithful; 1 Low note; no contradiction, merge decision not blocked by m-3

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s10-build-owner-confirms
PARENT_DISPATCH_ID: s10-build-impl
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
GRILL_LOCK_ID: s8-grill-m3-registry
FROM: m-3.planner
TO: s10.planner
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, s10.implementer, m-6.planner, m-3.implementer
IN_REPLY_TO: s10-build-owner-confirms/SITREP-planner-20260713-024153.md
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: CONFIRM T6/T9-hook/T10 — read the landed bytes at 9722744 myself; the T9 soft-expiry sunset into read_file_worker.go keeps the detach/breaker/machinery-fault/byte-ceiling invariants + adds the block-only hard-ceiling backstop; T10 run-suite-unbounded + side-effecting-unapproved verbatim, nothing spawns; T6 re-observe re-fires the gate and rejects a stale done; 1 Low note (completed-then-killed edge); merge not blocked by m-3

s10.planner — owner confirm of my three fidelity edges, verified against the landed code at `9722744` (`git -C frank show`/`diff`, not the pointers alone). **FIDELITY: CONFIRM all three.**

### T6 — re-observe / J1 — CONFIRM
`submit.go:510-527`: on wake, `if PresentLayers["observe"]` re-fires **`observe.Gate(...)`** (newest-authoritative re-observe); a non-`Accepted` terminal sets `DeliveryState = terminal` + the violation body and returns — the wake is **rejected, not delivered** (a stale done rejects the wake). Faithful to §3 (observe-as-send re-runs on wake) + the J1 refresh-before-resummon / never-auto-approve seam; the park-retained + no-delivery legs are covered by your cited stale-leg fixture.

### T9 hook — the s10 sunset of my §4a silent deadline — CONFIRM (invariants preserved)
Verified the `b2c2062 → 9722744` diff of `read_file_worker.go`:
- **Sunset framing correct:** `readCheckTimeout` is now "the E1 soft-expiry prompt point; NOT a silent auto-disposition boundary"; the byte ceiling comment notes it does NOT change with the prompt behavior. Only the *silent* deadline sunsets — exactly the ratified ruling.
- **`resolveReadFileExpiry` preserves my §4a:** the worker still runs the whole fs transaction detached to the **hard ceiling**; the serialized caller waits on channels/timers (no fs syscall — detach intact); on the SOFT timer it prompts `OnSoftExpiry(ctx, ExpiryRequest)` (park+ODB `{kill, extend}`) with a **hard-deadline `ctx` = the block-only backstop**. **Every kill path** — operator `kill`, OR `ctx.Done()` (hard ceiling), OR an `extend` that reaches the ceiling — calls **`tripReadFileBreaker` (set-before-return)** and returns **`check-machinery-read-file-timeout`** (the §4/§3.3 machinery-fault edge). Breaker, confinement, byte ceiling all unchanged.
- **Backward-safe:** `OnSoftExpiry == nil` (s8/s9) keeps the original silent trip-breaker path — the b2c2062-confirmed behavior is untouched where the park+ODB hook isn't wired.
This is a clean sunset: silent-deadline → soft-expiry-prompt + hard-ceiling backstop, with my Row-2 invariants intact. ✓

### T10 — my three legs — CONFIRM (verbatim)
`registry.go`: `run-suite-unbounded` (`ID/Rung E2/Class side_effecting`) landed **additive** (`:165-166`; the three prior entries unchanged); the unapproved selection returns **`refusedVerdictWithDetail(…, "side-effecting-unapproved")` PRE-SPAWN** (`:197/:201`) — before the executor-dispatch case (`:210`), so **nothing spawns** (gate-lift, F-a intact); `allow-once`/`allow-for-entry` (`ApprovalOnce`/`ApprovalForEntry`, `:55-56`) as specified; the live operator-FROM verdict lifts the gate; the claim-text rider is the fixture constant. Legs 1–3 verbatim per master's gate-lift disposition. ✓

### One Low note (ledger, no action)
In `resolveReadFileExpiry`, if the worker **completes during the prompt window** and the operator then decides **`kill`**, the completed read result is **discarded** and a `check-machinery-read-file-timeout` fault is returned instead of the successful data. Defensible (the operator's kill is honored over a just-finished read), but the completed-then-killed result is dropped — noted for the maintenance ledger, not a §4a fidelity issue.

### Bottom line
**CONFIRM T6 + T9-hook + T10 — no contradiction; the merge decision is not blocked by m-3.** The T9 sunset trues my §4a to the ratified operator-in-the-loop behavior without weakening the detach/breaker/machinery-fault/byte-ceiling floor; T10 is my legs verbatim under the ruled gate-lift; T6 re-observe is faithful.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s10-build-owner-confirms/SITREP-planner-20260713-030000.md` — run below.
- Read at `9722744`: `git -C frank diff b2c2062 9722744 -- internal/observe/read_file_worker.go` (the T9 hook); `git -C frank show 9722744:internal/observe/read_file_worker.go` (`resolveReadFileExpiry`); `:internal/engine/submit.go` (510-527, T6 wake); `:internal/observe/registry.go` (`run-suite-unbounded` :165, `side-effecting-unapproved` :197/:201, scopes :55-56). Against my locked `s8-design-m3-registry` §3/§4/§4a/§6/§12g + the ratified s10 sunset.

ACTIONS_GIT_REF: none — a read-only owner confirm; no `frank/` edit, no commit, no token, no merge. Sole artifact: master/relays/s10-build-owner-confirms/SITREP-planner-20260713-030000.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); read the s10 branch via `git -C frank show 9722744:…`; s10 worktree clean at `9722744` per your report.
Next requested action: with m-3 CONFIRMED (+ m-2, m-7), you author the merge-decision relay TO the operator on all three confirms + the MF-1 `engine/expiry.go` fold; merge only via HUMAN_MERGE_AUTHORIZATION. m-3.planner holds — report-only.
