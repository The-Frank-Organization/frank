## PLAN — r8 RULING: both sequencing recommendations RATIFIED (Rows 3–4 proceed IN PARALLEL now · the bless door becomes NAMED ROW 5, granted, closing the second entrance in-slice) — Row 2 HOLDS pending the m-3 mechanism return (routed as `s8-row2-mechanism/DESIGN-orchestrator-planner-20260712-055511.md`, with master's share of the sentence conflict OWNED there) — Row 1 consumed landed-and-spec-true; the revert discipline is the ledger's best internal catch yet

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-build-escalate-fence-r8-ruling
PARENT_DISPATCH_ID: s8-build-escalate-fence-r8
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — sequencing + fence ruling under the standing conditions (operator CC'd); live adoption and the slice merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: four-row re-lift (now five), Row 2 held
FENCE_EXPANSION_AUTHORIZED: granted — ROW 5: `internal/store/genesis.go` (already in-block; the row NAMES the seam — the §5.1.3 bless-candidate refusal of an engine candidate carrying any optional layer `true`) + the adoption fixture (`test/fixtures/s8_adoption_test.go`), RED-first (an observe-true bless candidate refuses), m-7 fidelity riding their existing Rows-1+4 return; NO other bless-machinery edit is licensed by this row
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-planner-20260712-055500.md
FROM: master.orchestrator-planner
TO: s8.planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-3.implementer, m-7.planner, m-7.implementer, m-2.planner
SUBJECT: both r8 records consumed (the implementer's `…-054500` — Row 1 landed at `ac11a3e` with both negatives RED-first, the polling attempt rejected BY THE BUILD'S OWN REVIEW as non-probative and byte-clean reverted at `123628a`, no silent bless edit, condition (f) honored — and your `…-055500` platform verification + Option 2′); dispositions below

**1. Rows 3–4 — PROCEED IN PARALLEL, ratified:** the absence floor and stage-once share no bytes and no semantics with the read-file mechanism; the in-order rule was convenience, and the T10-parallel precedent applies exactly. The original gates stay binding (RED-first per row · exact-head battery · whole-branch re-review · refreshed fence table — all AFTER Row 2 lands too).

**2. ROW 5 — GRANTED NOW (your recommendation adopted whole):** m-7's ACK made the invariant entrance-agnostic and their fidelity already rejects a silent single-door reading; the bless mechanism is in-slice, built, and fixtured — leaving door two as a carry would ship a bless that can mint an observe-active adopted store, the exact class Row 1 just killed at door one. The pending-carry status from the ACK DISSOLVES into this row. Row 1's Low note (the refusal legs don't separately assert the root stays uninitialized) rides your four-row review list as filed.

**3. ROW 2 — HOLDS at `123628a` pending the owner return**, routed to m-3 as `s8-row2-mechanism/DESIGN-orchestrator-planner-20260712-055511.md` with the option set {1 · 2′ · owner-alternative}, bare-Option-2 rejected on the r7 grounds, Option 3 verified nonexistent — and with master's share of the conflict OWNED: the "ACTUAL wall deadline" wording was the r7 ruling's, and it overspecified against the platform inside m-3's locked boundary. The returned owner sentence SUPERSEDES my r7 Row-2 sentence; the exact Row-2 re-lift issues on the fold of that return. The ≤5s interim VALUE and its s10 sunset are unchanged by any option.

**4. The ledger:** instance SEVEN, one level deeper — a mechanism REQUIREMENT whose platform writer does not exist; caught this round INSIDE the build (their own task review rejected their own fix as label-without-mechanism, then reverted byte-clean). The catch-layer progression now reads: production RED → independent review → pre-exit sweep → the build's own review. That sentence goes in the dogfood evaluation.

ACTIONS_GIT_REF: none — ruling + routing (disk refs: this relay · the Row-2 routing `…-055511` · two INDEX.md rows).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; worktree `s8-observe-spine@123628a` held with only `.relays/s8/` bookkeeping deltas (Row-1 feature head `ac11a3e`; the pair's report stands).
Next requested action: operator carries this to the s8 pair (Rows 3–5 proceed; Row 2 holds) and the routing to m-3.planner; master folds m-3's return into the exact Row-2 re-lift.
