## PLAN — r5 RULING: classification CONFIRMED (lock-grain reader-with-no-writer — and the miss count is SIX layers including BOTH master reconciles, owned here) · route 1 is DEAD by the design record (no input home was ever locked — nothing to "name") · ROUTE 2 GRANTED as framed and the three-owner amendment is DISPATCHED (`s8-claim-input-amendment/DESIGN-orchestrator-planner-20260712-004511.md`) · T10-PARALLEL AUTHORIZED · T9 holds on its preserved RED until the folded amendment returns with the exact seam grant

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-build-escalate-fence-r5-ruling
PARENT_DISPATCH_ID: s8-build-escalate-fence-r5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the schema-amendment routing and fence staging are the orchestrator's per conditions (b)/(c) (operator CC'd); the slice merge stays separately operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T9
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-planner-20260712-004500.md
FROM: master.orchestrator-planner
TO: s8.planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-2.planner, m-7.planner
SUBJECT: both r5 records are consumed (the implementer's `…-003500` primary + your `…-004500` assessment — independently verified, convergent, and correctly routed); the production-path exit test is endorsed as the STRONG form and its RED as the catch the directive built T9 to produce (the fixture-injection seam masked what the real path exposed); dispositions below

**1. Classification — CONFIRMED, with the miss count corrected upward:** a lock-grain boundary-contract gap of the reader-with-no-writer class. The gate, the registry, the §6.1 param contract, the R2 flag on results, and the executor spawn contract were all designed; the DECLARATION surface for `(claim_ref, check_id, params)` was assigned a schema home by no lock. Your four-review miss count is honest but short: **both master reconciles missed it too** — reconcile B matched the m-3×m-7 spawn contract one-for-one and never asked *where the selection comes from*. Owned here for the trail. **Process lesson, registered now for the cycle playbook at close:** the reconcile checklist gains a **Writes/Reads-per-seam boundary sweep** — every consumed value names its producing surface, or the reconcile does not complete. (This gap would have failed that sweep in one line.)

**2. Route 1 — answered and DEAD:** I hold the design record; no existing mapping exists to name. Any implicit mapping from `ACTIONS_GIT_REF`, body text, or cwd would contradict the locked explicit-selection triple and its closed param schemas — correctly refused.

**3. Route 2 — GRANTED as framed; the amendment is DISPATCHED** as `s8-claim-input-amendment/DESIGN-orchestrator-planner-20260712-004511.md` TO the three owning planners (m-3 semantics first-among-equals · m-2 FieldSpec home as the SECOND governed transition v6→v7 over their successor map · m-7 seam-(v) confirm). The T9 leg HOLDS until the folded result returns through me with the exact fence grant — pre-staged now, effective on that fold: **seam (v)** (`main.go`: `observe.Registry` construction + evaluator injection + the RegistryEnv wiring), **`registry.json`** (already in-fence; the v7 bytes ride the m-2 leg), **`test/fixtures/s8_exit_gate_test.go`** (nominally in the fixture family, named explicitly as the exit-bearing artifact). The implementer's point 3 is BINDING on every leg: `Evaluate: nil` remains the honest no-vantage row — the fix ADDS a selected evaluator; absence is never reinterpreted as failure.

**4. T10-PARALLEL — AUTHORIZED** per your sequencing option: bless/adoption is fixture-driven from the shipped-shape store with no dependency on the selection surface; same-file/different-seams is safe under the single writer; T9 resumes on the granted seam + the v7 bytes; T11 stays last. The two uncommitted T9 folds (the catalog nested-shape rejects · `TestLawPathHygiene` consuming the governed descriptor — the latter is r13 §3 working as designed) ride to your T9 review as you proposed; the production RED stays preserved and uncommitted as the honest state; the corrected catalog stays byte-pinned at `943f07bb…5b209d` pending that review.

**5. For the dogfood record (condition (g)):** count this beside the four fence stops — the strongest entry yet: a schema-level design gap that five human review layers inherited was caught by the REAL production path on the first honest run, held at a preserved RED, and routed to its owners without one line of improvisation. That is the product's thesis executing itself.

ACTIONS_GIT_REF: none — a ruling; no `frank/` edit (disk refs: this relay · the amendment dispatch `…-004511` · two INDEX.md rows; stamped after the replied-to filename per the skew convention).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; the build branch carries the implementer's uncommitted T9 work behind the preserved RED (their report's status stands).
Next requested action: operator carries this to the s8 pair and the amendment dispatch to m-3.planner, m-2.planner, m-7.planner; T10 proceeds meanwhile; master folds the amendment returns into the exact T9 seam grant.
