## SITREP — s8.planner passes the corrections GO: both test-body edits are licensed (Decision2 under my lane review · double-init under master's exact grant `…-180010`) — land both, recapture the exact-head battery green, refresh the fence table; the consequence-clause binds verbatim

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-corrections-go
PARENT_DISPATCH_ID: s8-build-five-row-battery-grant
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — grant operationalization; live adoption and merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: five-row exact-head battery correction
IN_REPLY_TO: master/relays/s8-build-escalate-fence/PLAN-orchestrator-planner-20260712-180010.md
FROM: s8.planner
TO: s8.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: land the two corrections — (1) Decision2 `read file IO error` subcase retargets to symbolic `not-regular-file` + the no-vantage authority partition with the git-status-timeout machinery leg PRESERVED (my `…-180000` lane review is the license); (2) `TestFrankInitTwiceRejectsExistingGenesis` gains `"-catalog", sources["catalog"]` on BOTH init invocations, every other byte preserved (master's grant field `…-180010` is the license — no other edit in that file rides it); then the recapture

Both corrections are GO. The closing sequence, binding:

1. Land the two test-body edits exactly at their licensed seams; nothing else moves.
2. Recapture the exact-head serialized battery (`go test -p=1 ./... -count=1`), FILE-captured, sequence-honest. **The consequence-clause verbatim: the dogfood red MUST clear as consequence or it becomes a NEW concrete finding — no silent absorption.** All legs green including the dogfood, INV-CATALOG included.
3. Refresh the diff→license fence table with the two correction seams (Decision2 → the lane + my review; double-init → master's grant field).
4. The two owner fidelity returns remain the pacing item (m-3 Rows 2+3 vs §4a+§13; m-7 Rows 1+4+the Row-5 entrance) — the operator carries the landed-state pointers; your requests are already in their hands.
5. Then the bounded T10/T11 close + the slice-exit package to my seat: the verdict chain, exit evidence on governed supply, the dogfood evaluation (killed classes · the in-slice catches · the reader-with-no-writer ledger · the scar · the catch-layer progression), both tables, all digests, the interim/s10 labels, the operator relaunch checklist (the live lane-root pin). My slice-exit review then produces the merge-decision relay TO the operator; nothing merges without `HUMAN_MERGE_AUTHORIZATION`.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-escalate-fence/SITREP-planner-20260712-181000.md` — run before handoff (result in the inline pointer).
- Input: master's grant `…-180010` read whole; the grant field, the Decision2 consumption, the consequence-clause, and the checklist echo are restated above faithfully.

ACTIONS_GIT_REF: none — a grant pass-through; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-181000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine@fb6e51d` source/test clean per the pair's report; `git -C frank status --short` = clean at `691d034`.
Next requested action: operator carries this to s8.implementer AND the landed-state fidelity pointers to m-3.planner + m-7.planner; the corrections + green recapture + refreshed table + fidelity returns come back; then the T10/T11 close and slice-exit package to my seat.
