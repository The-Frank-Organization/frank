## SITREP — s8.planner dispositions on the exact-head battery reds: the Decision2 seam is APPROVED under the class lane (the planner review its conditions require — the implementer may land it now); the double-init test-body grant is ROUTED to master with a GRANT recommendation; both root causes verified first-hand; the fidelity legs still need carry

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-five-row-battery-dispositions
PARENT_DISPATCH_ID: s8-build-five-row-battery-red
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — one exact master test-body grant is requested; live adoption and merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: five-row exact-head battery correction
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-implementer-20260712-174500.md
FROM: s8.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: verified at my seat — the capture SHA reproduces (`8937be95…3f9d`, exit 1, 24 ok, invariants/replay green, exactly the three named FAILs); both init invocations omit `-catalog` (`main_assembly_test.go:440,444` — Row 1's locked refusal firing on a pre-Row-1 fixture); the Decision2 subcase mkdirs `not-a-file` and expects machinery where §4a refuses typed non-regular pre-read; the whole-branch review's 52-path map re-checked — no unmapped path, the same-file seams distinctly licensed; one exact grant closes the set

### 1. Decision2 — APPROVED under the r4 stale-oracle class lane (this relay is the lane's required planner review)
The enumerated seam is exactly lane-compliant: expectation-only retarget of the ONE `read file IO error` subcase (`s8_decision2_test.go:95-149`) to assert the symbolic `not-regular-file` refusal + the correct no-vantage authority partition; the independent `git status timeout` machinery-fault leg is PRESERVED (Decision2's E1 machinery coverage survives — the lane condition I checked hardest); no lock pin, no composition change, no source/history edit; enumerated before commit per condition 3. The whole-branch review's classification concurs, and the landed §4a behavior it retargets to is the granted Row-2 semantics — reverting production to call a directory a machinery fault would regress the grant. **s8.implementer may land this correction now under the standing lane.**

### 2. Double-init — the exact test-body grant is REQUESTED, recommendation GRANT
The fix: add `"-catalog", sources["catalog"]` to BOTH `exec.CommandContext` invocations of `TestFrankInitTwiceRejectsExistingGenesis` (`main_assembly_test.go:440,444`) — `writeFixtureConfigSources` already supplies the catalog — so the first init constructs the now-mandatory lawful three-member genesis and the second reaches the UNCHANGED existing-genesis rejection oracle. Every assertion and every other byte preserved. The implementer's refusal to self-license this is correct twice over: r4 lane condition 4 excludes genesis-composition changes even in fixtures, and the older `main_assembly_test.go` grants name different seams — file presence is not a wildcard (the registry.go scar's lesson, applied by the build unprompted). Post-scar conservatism at exactly the right grain; the grant itself is mechanically obvious.

### 3. The rest of the pre-exit state, verified
- The whole-branch review (`.superpowers/sdd/five-row-whole-branch-review.md`): NO production blocker across I-PH, Rails, recovery/adoption, and all five row semantics; the 52-path fence map has NO unmapped path, with `registry.go`'s two seams (Row-2 breaker `:62-96` / Row-3 floor `:248-256`) and the Row-2 worker file (`read_file_worker.go`, the at-most-one-new-file clause) each pointing at their license. I re-checked the map's exception list against the grant trail — it is faithful, including the scar entry.
- The battery red is HONEST evidence handling: file-captured, exit-1 stated, the dogfood red correctly classified as downstream ("the suite is not a cosmetic green stub" — the review's line worth keeping). The framing that binds: after both corrections, the dogfood red MUST clear as consequence or it becomes a NEW concrete finding — no silent absorption.
- Row 3 landed spec-true at `fb6e51d` (presence-aware dispatch: only a genuinely ABSENT key enters the floor; present-empty rejects typed on the closed path — the fix-wave finding closed correctly).

### 4. What closes the slice from here (the checklist, so nothing arrives by surprise)
(1) master's double-init grant → both corrections land → (2) the recaptured exact-head serialized battery, file-captured, ALL legs green incl. the dogfood → (3) the refreshed diff→license fence table adding the two correction seams → (4) the two owner fidelity returns (m-3 on Rows 2+3 vs §4a+§13; m-7 on Rows 1+4+the Row-5 entrance — **operator: these two carries are now the pacing item alongside the grant**) → (5) the T10/T11 close + slice-exit package to my seat → (6) my slice-exit review → (7) the merge-decision relay TO the operator. Nothing merges without `HUMAN_MERGE_AUTHORIZATION`.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-escalate-fence/SITREP-planner-20260712-180000.md` — run before handoff (result in the inline pointer).
- At my seat (worktree `s8-observe-spine@fb6e51d`): the battery capture SHA reproduced + its FAIL set read (exactly three, two direct); `main_assembly_test.go:433-451` (both invocations lack `-catalog`; the sources map carries it); `s8_decision2_test.go:95-100` (the mkdir); the whole-branch review read whole + its fence-exception list cross-checked against the grant relays; the class-lane conditions re-read at `PLAN-orchestrator-planner-20260711-205010.md`.

ACTIONS_GIT_REF: none — a disposition/routing relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-180000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree source/test clean at `fb6e51d` with `.relays/s8/` battery + bookkeeping artifacts only; `git -C frank status --short` = clean at `691d034`.
Next requested action: master returns the exact double-init grant; the implementer lands both corrections (Decision2 already licensed by this review), recaptures the exact-head battery green, and refreshes the fence table; the operator carries the two fidelity legs to m-3.planner and m-7.planner; then the T10/T11 close and slice-exit package come to my seat.
