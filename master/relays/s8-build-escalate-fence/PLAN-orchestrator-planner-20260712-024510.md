## PLAN — r6 RULING: the WIDENED SUPPLY-SET SCOPE IS ADOPTED (the implementer's descriptor ask is real and is one of THREE instances of the config-side reader-with-no-writer class — govern the SET or meet instance three at r7) — the amendment is DISPATCHED (`s8-supply-set-amendment/DESIGN-orchestrator-planner-20260712-024511.md`), the supply-side sweep becomes a NAMED T11 exit item, the re-lift carries the WIDENED exit condition, and the silent 120s fallback dies with the amendment

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-build-escalate-fence-r6-ruling
PARENT_DISPATCH_ID: s8-build-escalate-fence-r6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — scope ruling + amendment routing under the standing conditions (operator CC'd); the slice merge stays operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T9
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-planner-20260712-024500.md
FROM: master.orchestrator-planner
TO: s8.planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-2.planner, m-7.planner
SUBJECT: both r6 records consumed (the implementer's `…-023500` primary — the held GREEN-but-uncommitted state is the correct honest state, second time running, with the v7 SHAs on the record (`17ba6e0d…71a6` registry · `71217dae…de112` transition digest · catalog pin intact) — and your `…-024500` assessment, seat-verified at `main.go:118-124`); dispositions below; T10 unaffected

**1. Scope — WIDENED, your recommendation adopted with reasons on the record:** the implementer's suite-descriptor blocker is subsumed into the full `RegistryEnv` supply set — {suite descriptors · lane/evidence roots · schema-ref table} — because all three are the SAME class (a countersigned consumer with no governed supply), instance three would otherwise arrive as r7, and the currently-green E1 exit legs ride the ambient `os.Getwd()` root, which means slice-exit evidence would be weaker than it looks if left unruled. Class record for the dogfood: reader-with-no-writer instance TWO (r5 = the lane-side declaration input; r6 = the config-side authority inputs), one level down, same shape.

**2. The amendment — DISPATCHED** as `s8-supply-set-amendment/DESIGN-orchestrator-planner-20260712-024511.md`: m-7 PRIMARY (the governed home + descriptor content + composition/runtime shape; the home is THEIR call under the floor — pinned, digest-covered, restart-effective, marker-gated; if they rule the engine member, that is the numeric ceiling's FIRST live lap — engine `v1→2` through the §2.4 successor gate — and **the engine capability ceiling `1→2` moves through m-7's owner relation as an EXPECTED part of the amendment**, named now so it is not an F3-class discovery) · m-3 CONFIRMS the lane-root approval semantics (§6.1's missing definition) + the `run-suite.target` registry-face/value-set split · m-2 CONFIRMS no fieldspec impact (condition (b) routes any registry byte that moves). End-to-end authorized; carry instruction names ALL THREE planners (the r5 transport lesson, applied).

**3. BINDING on the landed shape — no silent defaulting:** `Spawn`'s non-positive→120s fallback dies with the amendment: a missing/malformed/out-of-policy descriptor is a TYPED phase-0/composition failure; a runtime `CheckEntry.TimeoutClass` mismatch is a TYPED machinery fault — per the countersign, now implementable because the policy source will exist. (The 120s CLASS value itself stays the ratified interim with its s10 sunset — only the silent fallback is killed.)

**4. The supply-side sweep — ADOPTED as a NAMED T11 exit item:** before T11 closes, one table enumerating EVERY consumption surface the slice added (`RegistryEnv` fields · `executor.Config` · the claim row) → its governed supply. This is the r5 Writes/Reads-per-seam lesson at the build grain — one lesson, two grains; both feed the playbook at cycle close. Instance three gets found by sweep, not by RED.

**5. Re-lift — yours to execute, with the WIDENED exit condition adopted:** T9 re-lifts on the landed owner bytes/shape; the production dogfood must prove BOTH the false-done rejection AND a real-socket `run-suite` E2 leg on a governed descriptor + governed lane root; the currently-green E1 legs RE-RUN on the governed supply so no exit evidence carries an ambient dependency. Fence pre-staged, effective on the amendment fold: the ruled-home config surfaces + member fixture sources per m-7's naming · the `main.go` seam-(v) extension (descriptor consumption + governed lane/schema-ref supply REPLACING `Getwd`) · the `executor.go` timeout-policy check · the `run-suite` proof leg in the exit-gate fixture.

**6. Trail notes:** the compatibility-oracle updates in the implementer's disposition list are stale-oracle CLASS-LANE usage — enumerate per lane condition 3 in the T9 report for your review; the held state, the conceded-findings discipline, and the six-for-six clean escalation record ride the dogfood evaluation.

ACTIONS_GIT_REF: none — ruling + routing (disk refs: this relay · the amendment dispatch `…-024511` · two INDEX.md rows).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; the build lane holds uncommitted T9 work at `3cce8cd` behind the seam-(v) blocker (the pair's report stands).
Next requested action: operator carries this to the s8 pair and the amendment to ALL THREE owner planners; on the fold you re-lift T9; T10 continues meanwhile.
