## SITREP — s8.planner assessment on r8: the Row-2 conflict is REAL (platform facts verified; no portable in-process hard wall over blocked file syscalls exists) and the decision is m-3's — but the option list is missing the choice that fits this slice's own residual pattern, named here as Option 2′; Rows 3–4 should proceed in parallel; the bless door should become a named row NOW

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-escalate-fence-r8-assessment
PARENT_DISPATCH_ID: s8-build-escalate-fence-r8
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an assessment supporting master's routing to m-3; live adoption and merge remain operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: four-row re-lift, Row 2
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-implementer-20260712-054500.md
FROM: s8.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: verified at my seat — `SetReadDeadline`/`SetDeadline` docs confirm ordinary files generally lack deadline support; the revert is byte-clean (empty `ac11a3e..123628a` production diff); Row 1 landed spec-true; the implementer's rejection of their OWN polling fix is the label→mechanism discipline running at full strength; the mechanism choice belongs to m-3 (both conflicting sentences are theirs) with my ranked recommendation below

### 1. Confirmations
- **The conflict is real, not an implementation excuse.** Verified: Go contexts do not cancel blocked file syscalls; `os.File` deadlines are unsupported on ordinary files (`ErrNoDeadline` path confirmed in the local toolchain docs); a goroutine+timer strands the blocked syscall; a kill-capable helper reopens the locked in-process E1 boundary. The r7 Row-2 sentence ("an ACTUAL ≤5s wall deadline" over blocked I/O) and the m-3 lock's `class: base, in-process` sentence cannot both hold as written. This is the ledger's class running one level deeper: a mechanism REQUIREMENT whose platform writer does not exist.
- **The hold discipline is again exact:** the non-probative 1ns polling test was rejected by the build's own review (it proved an expired context was observed, not that a blocked syscall was interrupted — the precise r7 error class, caught internally this time), the attempt was fully reverted (`123628a`; production diff vs `ac11a3e` is empty), Rows 3–4 untouched, no unlicensed helper, no bless edit.
- **Row 1 is landed and spec-true** at `ac11a3e` (both refusal negatives RED-first). The task review's Low note (the refusal legs do not separately assert the root stays uninitialized) joins my four-row review list.

### 2. The mechanism decision — m-3's, with a fourth option the r8 list is missing
The implementer's three options are honest but incomplete. **Option 2′ (recommended for m-3's consideration):** keep in-process E1 AND keep a hard wall over everything a LANE can reach, by splitting the threat at the same grain this slice splits every other claim:
- **Lane-reachable wedge classes die mechanically, in-process:** `O_NONBLOCK` open + `fstat` regular-file-only BEFORE any read (kills FIFO/device/socket wedges — the attack a lane can actually stage inside a governed root) · chunked bounded reads with deadline checks between chunks + the byte ceiling (kills the large-file stall) · symbolic path-free faults throughout.
- **The kernel-blocked residual (a stalled FUSE/network mount backing a regular file) is OPERATOR TOPOLOGY, not lane input** — a lane cannot conjure a stalled mount inside the operator-pinned root. Handle it as this slice handles D5: detach the blocked worker with a TYPED machinery fault (the conductor's serialized loop returns — the actual liveness requirement), a **per-lane circuit breaker** (after one strand, further `read-file` checks against that lane refuse typed until restart — bounding the leak to one goroutine/fd per lane, loud not silent), and the residual STATED at the deployment grain beside D5, never claimed away.
- **Why 2′ over Option 1 (the killable helper):** even SIGKILL cannot reap a process in uninterruptible sleep — against the stalled-mount case Option 1's helper ALSO strands; what it buys is the conductor's return, which 2′'s detach buys without a new production protocol surface, an E1 boundary amendment, or a per-check process spawn inside the serialized path. Option 1 dominates only if m-3 values zero-strand purity above surface economy. Bare Option 2 (best-effort, silent) should be rejected on the r7 grounds; Option 3's portable primitive does not exist (verified).
The choice and its exact wording are m-3's — both sentences in tension are theirs; 2′ is submitted as input, not a ruling.

### 3. Two sequencing recommendations (master's to ratify)
1. **Rows 3–4 proceed IN PARALLEL with the Row-2 ruling:** the absence floor (`internal/observe/registry.go` + evaluator seam) and stage-once (`executor.go`) share no bytes and no semantics with the read-file mechanism; the in-order rule was convenience, not dependency. Same pattern as the T10-parallel ruling — the build should not idle on a design round it does not consume.
2. **The bless door becomes a NAMED row NOW:** m-7's ACK (`…-042245`) binds the observe-active refusal to §5.1.3 bless candidate validation. The bless MECHANISM is in-slice (T10, built + fixtured); leaving the second entrance for a carry ships a bless that can mint an observe-active adopted store — the exact class Row 1 just killed at the first entrance. Recommend: one named row, `internal/store/genesis.go` + the adoption fixture, RED-first (an observe-true bless candidate refuses), m-7 fidelity riding their existing Row-1 return. Small, same invariant, closes the pair of doors together.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-escalate-fence/SITREP-planner-20260712-055500.md` — run before handoff (result in the inline pointer).
- At my seat (worktree at `123628a`): `go doc os.File.SetReadDeadline` (the support caveat chain); `git log d9fb80d..123628a` (Row 1 + attempt + revert); `git diff --stat ac11a3e..123628a` → empty production diff; the m-3 lock §3/§4 base-class sentences and the r7 Row-2 sentence re-read side by side; m-7's adjacent-door ACK read.

ACTIONS_GIT_REF: none — an assessment relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-055500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine@123628a` carries only `.relays/s8/` bookkeeping deltas; `git -C frank status --short` = clean at `691d034`.
Next requested action: master routes the Row-2 mechanism decision to m-3 (Options 1 / 2′ / owner-alternative — bare best-effort rejected), ratifies or declines the two sequencing recommendations, and returns the exact re-lift; the pair holds at `123628a` on Row 2 and (if ratified) proceeds on Rows 3–4 meanwhile.
