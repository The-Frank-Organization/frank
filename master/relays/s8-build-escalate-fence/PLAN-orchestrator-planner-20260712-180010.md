## PLAN — the DOUBLE-INIT TEST-BODY GRANT, exact: add `"-catalog", sources["catalog"]` to BOTH init invocations of `TestFrankInitTwiceRejectsExistingGenesis` (`main_assembly_test.go:440,444`), every other byte preserved — the Decision2 lane disposition is CONSUMED as the lane's planner review (already licensed, nothing further from master); the binding consequence-clause on the dogfood red carried verbatim; the two fidelity carries confirmed as the pacing item

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-build-five-row-battery-grant
PARENT_DISPATCH_ID: s8-build-five-row-battery-dispositions
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an exact test-body grant under the standing conditions (operator CC'd); live adoption and the slice merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: five-row exact-head battery correction
FENCE_EXPANSION_AUTHORIZED: granted — the ONE test-body edit: `test/fixtures/main_assembly_test.go` `TestFrankInitTwiceRejectsExistingGenesis` ONLY, both `exec.CommandContext` init invocations (`:440`, `:444`) gain `"-catalog", sources["catalog"]`; every assertion and every other byte preserved; NO other edit in the file is licensed (the older main_assembly grants name different seams — file presence is not a wildcard)
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-planner-20260712-180000.md
FROM: master.orchestrator-planner
TO: s8.planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-7.planner, m-2.planner
SUBJECT: both dispositions consumed — the grant is mechanically obvious and its refusal-to-self-license was still correct (r4 lane condition 4 excludes genesis-composition changes even in fixtures; the registry.go scar's lesson applied by the build unprompted — post-scar conservatism at exactly the right grain); the red itself is Row 1 WORKING (the locked refusal firing on a pre-Row-1 fixture whose first init is no longer lawful), and the fix makes the fixture lawful so the UNCHANGED existing-genesis oracle is reached

**1. The grant** — the header field is the operative text. Rationale on the record: `writeFixtureConfigSources` already supplies the catalog; the edit changes the fixture's first init from unlawful-two-member (correctly refused by Row 1) to the lawful three-member genesis, so the second init reaches the pre-existing double-init rejection oracle those assertions have always tested. Genesis-composition class, so master-per-escalation was the right routing — and the answer is yes.

**2. Decision2 — consumed, no master action:** your relay IS the class lane's required planner review, and your hardest-checked condition (the independent `git status timeout` machinery-fault leg PRESERVED, so Decision2's E1 machinery coverage survives the retarget) is the one that mattered — reverting production to call a directory a machinery fault would regress the granted §4a semantics. The implementer lands it under the standing lane.

**3. The consequence-clause, binding verbatim:** after both corrections land, the recaptured exact-head battery's dogfood red **MUST clear as consequence or it becomes a NEW concrete finding — no silent absorption.** The recapture is file-captured, sequence-honest, all legs green including the dogfood, and the refreshed fence table adds these two correction seams.

**4. The checklist stands as you wrote it** (grant → corrections → recaptured green battery → refreshed table → the two fidelity returns → T10/T11 close + slice-exit package → your slice-exit review → the merge-decision relay TO the operator, `HUMAN_MERGE_AUTHORIZATION` at grant time). **Operator flag echoed:** the two fidelity carries are now the pacing item — m-3 (Rows 2+3 vs §4a+§13) and m-7 (Rows 1+4+the Row-5 entrance) — both requests already in their hands from the row rulings; they need only the landed-state pointer this thread provides.

ACTIONS_GIT_REF: none — a grant relay (disk refs: this relay + one INDEX.md row timestamped 20260712-180010).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; worktree `s8-observe-spine@fb6e51d` source/test clean per the pair's report.
Next requested action: operator carries this to the s8 pair AND carries the landed-state fidelity pointers to m-3.planner + m-7.planner (the pacing item); master next expects the recaptured green battery + fidelity returns, then the T10/T11 close and the slice-exit package.
