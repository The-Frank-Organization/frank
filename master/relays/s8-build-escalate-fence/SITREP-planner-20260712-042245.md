## SITREP — m-7's rows-1+4 fidelity ACK + the adjacent-door question ANSWERED NOW from the lock (so row 1 can be built right the first time): YES — the genesis observe-refusal binds the §5.1.3 BLESS candidate validation IDENTICALLY; the invariant is entrance-agnostic (no store may ENTER three-member existence observe-active without the operator flip record; genesis and adoption are the two entrances); the row-4 bar pre-stated verbatim

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-escalate-fence-m7-ack
PARENT_DISPATCH_ID: s8-build-escalate-fence-r7-ruling
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a fidelity-leg acknowledgment + a lock-derived pre-statement; live adoption and the slice merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m7-config
DESIGN_LOCK_ID: s8-design-m7-config-r13
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T10, T11
IN_REPLY_TO: master/relays/s8-build-escalate-fence/PLAN-orchestrator-planner-20260712-044510.md
FROM: m-7.planner
TO: s8.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, s8.implementer, m-7.implementer, m-3.planner
SUBJECT: my fidelity runs as REVIEW of the landed rows per the ruling (small tier, the quoted sentences the bar) — this relay adds nothing to the fence and changes no row scope; it pre-states the two things my return will check so neither surprises the build: the row-1 adjacent-door answer (below, with the lock cites) and the row-4 stage-once bar (my own §2.1 sentence, restated so the fix and the fidelity read the same words)

**The adjacent-door answer (the ruling asked my row-1 fidelity to state it either way — stated now instead, so the build can bind it): YES, the same refusal binds the §5.1.3 bless candidate validation.** The invariant the row defends is not a `-init` property — it is a **store-state-creation invariant**: *no store may enter three-member existence with `observe: true` unless the flip exists as an operator-authored `config_change` record*. The lock says it twice: r13 §5 item 2 (genesis carries `observe: false`; "the flip to `true` lands later as an operator `config_change`, making activation the governed, visible act — a genesis-ON default would mean the flip never exists as a record") and §5.1.3 (the bless candidates validate against their initial descriptors — the engine candidate is the SAME engine member, entering existence by the second door). An adoption record whose engine bytes carried `observe: true` would create exactly the flip-less observe-active store the genesis refusal exists to prevent — same violation, second entrance. So: **the §5.1.3 candidate validation refuses an engine candidate with any optional layer `true`, identically to `-init`**; post-adoption activation happens only via the ordinary §7 flip record (which the adopted store can accept the moment it serves). If row 1's fix lands only the `-init` half (bless machinery being T10-scoped), the bless-side refusal must be NAMED in the row's record as bound-and-pending for the adoption path — my fidelity will accept either realization (both entrances landed, or one landed + one named-bound), and will reject a silent single-door reading.

**The row-4 bar, pre-stated (my §2.1 sentence is the spec, so both seats hold the same words):** the manifest "immutably identifies the **pre-spawn bytes** — what was staged, provably, at spawn time." One snapshot is staged; the manifest hashes **THE STAGED bytes**; execution runs **those same bytes**. Two walks of a mutable source tree cannot prove that identity — the fix is stage-once-hash-staged-execute-staged, with coalescing, manifest identity, symlink/non-regular refusal, I-PH, and cleanup semantics retained byte-for-byte. My fidelity will check the hash input is the staged tree (not the source), the RED-first negative demonstrates the mutable-source hole (a source mutation between walks under the OLD shape ⇒ the manifest lies; under the NEW shape ⇒ impossible by construction), and the retained semantics list against the r4 lock.

**Fidelity logistics per the ruling:** small-tier confirms against the LANDED rows (1 and 4), returned after the four-row RED→GREEN report; the quoted sentences are the bar; no pre-fix gate from my seat — the build moves now.

Not authorized / not claimed: acknowledgment + lock-derived pre-statement only — no fence change, no row-scope change, no code, no fidelity verdict yet (that follows the landed rows), no T10/T11 or merge authority.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/s8-build-escalate-fence/SITREP-planner-20260712-042245.md` — run before handoff (result in inline pointer)
- the lock re-read for the answer: r13 §5 item 2 (the genesis-OFF rationale, verbatim above), §5.1.3 (the bless candidate validation), §5.1.4 (the adoption-only transitions — the flip is NOT one of them, confirming activation is never an adoption side-effect); the r4 §2.1 manifest sentence re-read for the row-4 bar

ACTIONS_GIT_REF: none — no git action by this relay (disk refs: this relay + one INDEX.md row timestamped 20260712-042245)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; the s8 worktree at `d9fb80d` per the ruling, untouched by me; `git -C frank status --short` = clean at `691d034`)
Next requested action: operator carries this to the s8 pair alongside the r7 ruling (row 1 can now bind both doors or name the second as bound-and-pending, first pass); my rows-1+4 fidelity confirm follows the landed RED→GREEN report.
