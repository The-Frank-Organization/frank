## SITREP — s8.planner assessment on r7: ALL FOUR findings CONFIRMED at my seat with the exact code sites; recommend the bounded four-row re-lift EXACTLY as requested (m-3 fidelity on 2–3, m-7 on 1+4); the class ledger now reads six label/mechanism–reader/writer splits, and this set was caught by the REQUIRED whole-branch review rather than a production RED — the process maturing exactly as designed; my own T1/T4/T9 review-layer share of the miss is owned on the record

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-escalate-fence-r7-assessment
PARENT_DISPATCH_ID: s8-build-escalate-fence-r7
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an assessment supporting master's requested ruling; live adoption and merge remain operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T10, T11, slice-exit review
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-implementer-20260712-043000.md
FROM: s8.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-2.planner, m-7.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: verified first-hand at `d9fb80d` — (1) `main.go` init makes the catalog OPTIONAL (`if cfg.Catalog != ""`) with no observe:true refusal; (2) `runReadFile` is unbounded `os.ReadFile` with `ReadTimeout` consumed ONLY by git-status — `read_short` is a label with no mechanism; (3) `EvaluateClaims` returns `observe-unavailable/Blocked` on zero selections so absent-claim records skip the locked base phase/git observation entirely; (4) `manifestKey` and `stageTree` are two separate walks of the LIVE tree — the manifest does not identify the staged bytes; the four-row re-lift should issue as asked, and the hold discipline was again exactly right

### 1. Verification (each finding re-derived at my seat, exact sites)
1. **Genesis profile unenforced — CONFIRMED.** `main.go` `cfg.Init` branch: `sources = {fieldspec, engine}` with catalog added only `if cfg.Catalog != ""`; `store.Init` accepts the two-member set; no engine-candidate `observe:true` refusal exists on the path. The r13 §5 locked fresh-store composition (three members, `observe:false`, activation ONLY by the operator's restart-effective `config_change`) is bypassable in production. The T1/T9 fixtures always supplied correct values — the refusal negatives were never written, and my reviews did not demand them.
2. **`read-file` deadline label without mechanism — CONFIRMED.** `checks_base.go:23` unbounded `os.ReadFile`; `grep ReadTimeout` hits only the git-status `CommandContext` (`:53`). No regular-file-only check, no byte ceiling, no wall deadline: a lane-scoped FIFO blocks the SERIALIZED submit loop indefinitely — worse than a slow check, it wedges the conductor's single commit path. The ratified E1 ≤5s interim exists as `timeout_class: read_short` metadata only.
3. **Absence-open skips base observation — CONFIRMED.** `registry.go:202-204`: zero selections ⇒ `{ID: "observe-unavailable", Predicate: Blocked}` ⇒ the no-vantage row ⇒ non-authority accepted+`self_reported` at E0. The locked contract (the m-2 matrix's class-1 rows: the conductor OBSERVES `ACTIONS_GIT_REF`/`FINAL_GIT_STATUS_SHORT` ground truth DI-5 and may veto; the T3 spine's false-action-claim leg) requires the base phase-shaped done-predicate + git-provenance observation to run REGARDLESS of claim selection — declaration adds rungs, its absence must not subtract the floor. Production wires `EvaluateClaims` as the only evaluator (`submit.go:37`), so the floor is gone on the absent-claim path.
4. **Manifest/stage TOCTOU — CONFIRMED.** `executor.go:94` computes `manifestKey` (hash walk of live `suite.SourceDir`), then `:128` `stageTree` copies the live tree in a second walk. The m-7 lock §2.1 is explicit: the manifest "immutably identifies the pre-spawn bytes — what was staged, provably, at spawn time." Two reads of mutable source cannot prove that; a mutation between walks caches a verdict under bytes that never ran.

### 2. Classification + the ledger
All four are the same family the slice has now met six times (r5 lane-input, r6 config-supply ×3-in-1, r7 ×4): a contract with a reader and no writer, or a label with no mechanism, at an integration boundary. The maturation worth recording: r5 was caught by a production RED, r6 by independent review mid-task, r7 by the REQUIRED whole-branch review + sweep discipline BEFORE the exit package was assembled — each catch moved one layer earlier. **Review-layer ownership (mine):** findings 1–3 traverse seams my T1/T4/T9 verdicts approved; my reviews verified fixtured behavior and lock-text conformance of what was BUILT, but did not sweep label→mechanism (finding 2) or demand the profile-refusal negatives (finding 1) — that sweep dimension joins my slice-exit review checklist alongside the verify-the-summary-line rule.

### 3. Recommendation
**Grant the bounded four-row re-lift exactly as the implementer scoped it** — the rows are minimal, each names its files + fixtures, and the discipline requests are already right: m-3 fidelity on findings 2–3 (the E1 deadline mechanism; the absence-open base-observation semantics — their §3/§6.1 contracts), m-7 fidelity on findings 1 and 4 (the genesis profile; the stage-once/hash-staged snapshot under their §2.1), a fresh focused RED→GREEN cycle per row, the exact-head serialized battery, a whole-branch re-review, and the refreshed 51-path fence table before any slice-exit package. One addition I request: the re-lift's finding-3 row should carry m-3's explicit wording of the absence floor (what exactly runs on zero declarations — the base predicate set and its stamped rung) so the fix implements an owner sentence, not an inference. T10/T11 task-level closure holds meanwhile — their reviews were clean and nothing in the four rows disturbs the bless machinery or the tables beyond the refresh.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-escalate-fence/SITREP-planner-20260712-044500.md` — run before handoff (result in the inline pointer).
- At my seat (worktree `s8-observe-spine@d9fb80d`): `main.go` init branch read whole; `checks_base.go:17-30` + the `ReadTimeout` grep; `registry.go:197-230` (`EvaluateClaims` incl. the zero-selection branch); `executor.go:94,117-128,261,308` (the two walks); `submit.go:37`; the locked anchors re-read (r13 §5; m-3 §3/§4 + the ratified E1 interim; the m-2 matrix class-1 rows; m-7 executor §2.1).

ACTIONS_GIT_REF: none — an assessment relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-044500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine@d9fb80d` carries only `.relays/s8/` bookkeeping deltas per the pair's report; `git -C frank status --short` = clean at `691d034`.
Next requested action: master rules the four-row re-lift (with the m-3 absence-floor sentence added); the owner fidelity legs run; the pair executes the fixes with fresh RED→GREEN; the whole-branch re-review + exact-head battery + refreshed tables land; THEN the T10/T11 report closes to my seat and the slice-exit review + merge-decision relay TO the operator proceed.
