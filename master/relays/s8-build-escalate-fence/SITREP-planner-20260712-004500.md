## SITREP — s8.planner assessment on the r5 escalation: the stop is CONFIRMED and correctly routed; root cause = a LOCK-GRAIN gap (the executable-claim declaration has a defined semantic but NO schema home — a reader with no writer surface), inherited by the plan and missed by four reviews; recommendation = the implementer's route 2 as a three-owner amendment + one new main.go seam, with T10 authorized in parallel

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-escalate-fence-r5-assessment
PARENT_DISPATCH_ID: s8-build-escalate-fence-r5
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an assessment supporting master's requested ruling; merge remains operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T9
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-implementer-20260712-003500.md
FROM: s8.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-2.planner, m-7.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: the plan-owner's half of the r5 record: I verified the root cause independently (registry.json carries ONLY the system-owned OUTPUT row `executable_claim_results` — no input row for `(claim_ref, check_id, params)` exists in v6; production `observe.Env.Evaluate` is nil with zero non-test engine call sites; c2 §4:88 and s8-m3 §2 define the agent-declaration semantic but no lock ever named its FieldSpec home); the ruling is yours — this relay classifies the gap, recommends the route, and flags the sequencing option

### 1. Verification (mine, at my seat)
- `registry.json` (v6, post-T2): `executable_claim_results` (`:151`, owner system, observed_value) is the OUTPUT; grep for any `claim_ref`/`check_id`/claim-input row → none. The T2 changeset was correct as scheduled — the input home was never in it because no lock ever scheduled one.
- `grep -rn 'Evaluate\b' internal/engine/*.go` excluding tests → empty; `SubmitHandlerWithRender` builds `observe.Env` without an evaluator, so production observe runs every submit through the no-vantage row — the T3–T8 fixtures injected `Evaluate` directly, which is why the pair-grain batteries were honestly green while the production leg is honestly RED.
- The locked record defines the semantic precisely (c2 §4:88 "the agent declaring 'claim X, verify by registry-check C(params)'"; s8-m3 §2 "the agent selects by id+params" with the §6.1 closed param schemas) and never assigns the declaration a schema home. The m-2 activation matrix's seat-rendered observe set is exactly {`ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`} — no claim-declaration row.

### 2. Classification (for the record master's ruling will set)
This is a **lock-grain boundary-contract gap** — the protocol's own "reader-with-no-writer" class: the gate (reader) was fully designed, the declaration surface (writer) never was. It is NOT a build failure (the implementer's spine is lock-faithful and their production exit test is the honest strong form — real socket, real genesis, real transitions), and NOT a fence dodge (they stopped pre-edit with the RED preserved and uncommitted). The plan I adopted inherited the gap; my adoption review, both m-3-pair plan reviews, and both s8-pair plan reviews missed it — the reconcile step that should have caught it is the boundary-contract sweep (Writes/Reads per seam), which none of us ran over the CLAIM-INPUT seam. On the trail for the dogfood record's condition (g): the fixture-injection seam masked it; the production-path dogfood test caught it — the exact class of catch the directive built T9 to produce.

### 3. Recommendation (the ruling is yours)
**Route 2, as the implementer framed it — no existing mapping to name (I looked), no improvisation:**
1. **m-3** owns the declaration semantics: the triple's shape, its relation to the §6.1 closed param schemas, R2 posture (the row must stay non-gate-referenceable; selection is lane intent, results stay conductor-computed).
2. **m-2** owns the FieldSpec home bytes: one seat-fillable row (shape theirs — e.g. an `executable_claims` row_array, `layer_present:observe`-gated, seat-owned/declared), landing as a **second governed fieldspec transition v6→v7 over the m-2 successor map** — the T2-built §2.4 machinery and FX-CFG-10 legs run a second live lap, which is spine-proving in itself, not overhead.
3. **m-7** confirms the ONE new production seam: `observe.Registry` construction + evaluator injection at the `main.go` composition root (an extension of the r1 grant's seam list — seam (v)), plus the RegistryEnv wiring (lanes/schema-refs/executor) that construction needs.
4. **Fence grant, exact:** the m-2 amendment rides `registry.json` (in-fence) + the new `main.go` seam (v) + `test/fixtures/s8_exit_gate_test.go` (the implementer's untracked production test — inside the fence's fixture family by name, but state it explicitly since it is the exit-bearing artifact).
5. **Their point 3 stands verbatim:** `Evaluate: nil` remains the honest no-vantage row; the fix ADDS a selected evaluator, it does not reinterpret absence as failure.

### 4. Sequencing option (my call to propose, yours to ratify)
Authorize **T10 (bless/adoption) to proceed in parallel** while the amendment routes through m-3/m-2/m-7: the bless machinery is fixture-driven from the shipped-shape store and has no dependency on the selection surface; T9 resumes on the granted seam + the v7 bytes; T11 stays last. This keeps the build moving without touching the held leg. The implementer's uncommitted T9 folds (the catalog nested-shape rejects + the descriptor-consuming law) are review-worthy work I will take up in the T9 review once the leg is unblocked — nothing about them needs to move now.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-escalate-fence/SITREP-planner-20260712-004500.md` — run before handoff (result in the inline pointer).
- At my seat (worktree `s8-observe-spine`): `registry.json:76,151` (output enum + row; no input row by grep); non-test `Evaluate` grep over `internal/engine` → empty; c2 lock `2026-06-29-v3-observe-evidence-design.md:88` + s8-m3 lock §2 read at the declaration-semantic anchors; the m-2 grammar matrix's seat-rendered set re-read (`2026-07-10-s8-config-atom-grammar.md` §3).

ACTIONS_GIT_REF: none — an assessment relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-004500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); worktree `s8-observe-spine` carries the implementer's uncommitted T9 work behind the preserved RED (their report's status stands); `git -C frank status --short` = clean at `691d034`.
Next requested action: master rules on the r5 escalation (the amendment route, the exact fence/seam grant, and the T10-parallel option); on the ruling the owners run their legs (m-3 semantics → m-2 v6→v7 bytes → m-7 seam confirm), I review the folded result at the T9 checkpoint, and the exit legs turn GREEN on the real production path or the slice does not exit.
