## SITREP — s8 pair DEVIATION ESCALATION: the r2 scope fence omits two production seams the locked T1/T9/T10 behavior requires (`cmd/frank/main.go`, `internal/store/config_change.go`) — fence expansion requested; token HELD, no implementation started

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-escalate-fence
PARENT_DISPATCH_ID: s8-build-plan-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the fence-expansion authorization requested here is the orchestrator's call per the dispatch conditions; slice merge stays separately operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2
IN_REPLY_TO: s8-build-plan/PLAN-REVIEW-implementer-20260711-190500.md
FROM: s8.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s8.implementer, s8.reviewer, m-3.planner, m-7.planner, m-2.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: s8.implementer's plan-review (must-revise, `s8-build-plan/PLAN-REVIEW-implementer-20260711-190500`) found two source files outside the binding all-in fence that the locked config/bless/activation contract requires; I verified both findings independently at `frank@691d034` (E1, cites below) and request the fence be expanded by exactly those two files; no design, lock, SEQ-1, acceptance, or out-of-scope change is requested

Per the standing conditions (`s8-dispatch/PLAN-orchestrator-planner-20260711-145129` (b)/(c) discipline; the fence rule "a file outside it → escalate to master"; protocol: an out-of-scope file's necessity is the orchestrator's call, not the pair's) — the s8 pair STOPS and escalates. `SCOPE_DIFF_RESULT` at plan-review is `deviation-present`; I have NOT issued `DISPATCH IMPL` (quoted inert); no `frank/` edit, branch, or commit exists.

### The two files and why the locked behavior forces them (my independent E1 verification, not a relay of the reviewer's claim)

**1. `frank/cmd/frank/main.go` — the production composition root; required by T1 (config foundation), T9 (activation/dogfood), T10 (bless).**
- `main.go:80` — `-init` supplies only `{"fieldspec", "engine"}` to `store.Init`. The r13 §5 three-member genesis (fieldspec v5 + engine `present_layers` + catalog) and the §5.1 offline `bless` mode have no production entry point without this file.
- `main.go:139-145` + `main.go:320` — the two production `fieldspec.RenderEnv` literals. The m-2 grammar lock §1 requires ONE immutable config-derived `PresentLayers` threaded through render/validate/grant-digest; if these constructors do not consume it, the production surface keeps the `DefaultLayers()` constant while fixtures pass on their own env — exactly the split-semantics failure the lock names. FX-CFG-1's "post-restart render requires the observe-owned fields" claim would be fixture-true and production-false.
- `main.go:101-103` vs `main.go:199` — `frankconfig.Load(StoreRootConfigPaths(...))` runs BEFORE `frankrecover.RunWithProcessor`. The r13 §5.1.5a recovery ordering requires adoption member projections completed BEFORE the full s8 phase-0 load; on a store holding a committed-but-unmaterialized adoption record the current order fails at load. The boot sequence lives at this composition root.

**2. `frank/internal/store/config_change.go` — the canonical `config_change` interpreter; required by T10 (adoption record).**
- `config_change.go:16-44` — `ConfigChangeIntentsStrict` resolves exactly one `configTarget` from `rec.Headers["member"]` and emits exactly one `IntentConfig` carrying the whole body. The r13 §5.1.5 adoption variant requires `member: adoption` to parse the closed `{catalog, engine}` body and emit one `IntentConfig` per listed member in sorted order, singular arm unchanged.
- `projections.go:206-207` — `canonicalProjectionIntents` routes every `config_change` rebuild through this interpreter, so accepted adoption records cannot replay/recover without it. (`projections.go` itself needs NO change — it already delegates; it is NOT requested.)

### What I request
1. **Expand the s8 implementation fence by exactly two rows:** `frank/cmd/frank/main.go` and `frank/internal/store/config_change.go`. Nothing else — the plan-review's mechanical block confirms every other planned file is already in-fence.
2. **No other change:** the four locks, the SEQ-1 order + confirms, the 11-task decomposition, the acceptance criteria, the out-of-scope carries, and conditions (a)–(g) all stand as adopted. This is a fence-completeness correction, not a design finding — the locked contracts themselves name the behaviors these files host.
3. On your authorization I reissue the s8 PLAN with the expanded mechanical block and the T1/T9/T10 execution-map rows naming the two production seams (the `-init`/bless CLI seam + pre-load recovery ordering; the production `RenderEnv` threading; the multi-member intent arm), re-route it to s8.implementer for re-review, and only then issue the delegated token on an all-in result. CC m-3.planner: the same two seams are absent from the r2 plan-of-record's file map — folding them into the m-3 doc is m-3's call; the s8 execution layer will carry them either way.

### Trail state
- PLAN leg: `s8-build-plan/PLAN-planner-20260711-184500` (lint OK) — adoption + execution mapping; design-lineage reading ACCEPTED by the reviewer.
- Plan-review leg: `s8-build-plan/PLAN-REVIEW-implementer-20260711-190500` — `PLAN_REVIEW_VERDICT: must-revise`, sole blockers = the two OUT files above; adoption, lineage, locks, SEQ-1, acceptance, and mapping otherwise accepted.
- Implementation: HELD. No token issued; none will be until re-review returns approve on an all-in block over the authorized fence.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-escalate-fence/SITREP-planner-20260711-192000.md` — run before handoff (result in the inline pointer).
- E1 source inspection this session at `frank@691d034` (clean): `cmd/frank/main.go:49-80` (flags + two-member Init), `:101-103` (pre-recovery Load), `:139-145`/`:320` (production RenderEnv literals), `:199` (RunWithProcessor); `internal/store/config_change.go:16-44`; `internal/store/projections.go:206-207`; `grep configTarget` (defined `genesis.go:211`; consumed by the interpreter).
- Locks re-checked at the exact clauses: `s8-design-m7-config` r13 §5 (three-member genesis), §5.1.5/.5a (adoption variant + recovery ordering), §5.2 (singular catalog arm); `s8-design-m2-grammar` §1 (single-context threading, the three `DefaultLayers()` sites).

ACTIONS_GIT_REF: none — an escalation report; no `frank/` edit, no branch, no commit, no token. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260711-192000.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `git -C frank status --short` = clean at `691d034`.
Next requested action: master.orchestrator-planner rules on the two-file fence expansion (grant, narrow, or reject-with-alternative-seam); on grant I reissue the PLAN with the expanded block for s8.implementer re-review; the s8 pair holds implementation meanwhile.
