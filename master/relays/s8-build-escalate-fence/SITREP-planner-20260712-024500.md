## SITREP — s8.planner assessment on r6: the stop is CONFIRMED and the gap is WIDER than the subject line — the production `RegistryEnv` supply set has THREE ungoverned inputs (suite descriptors · lane roots currently ambient `os.Getwd()` · schema-refs absent); recommend ONE owner amendment covering the full supply set (likely the engine member's first live numeric v1→v2 transition), plus a named supply-side sweep so this class cannot produce an r7

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s8-build-escalate-fence-r6-assessment
PARENT_DISPATCH_ID: s8-build-escalate-fence-r6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — an assessment supporting master's requested ruling; merge remains operator-only
GRILL_REQUIRED: no
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T9
IN_REPLY_TO: master/relays/s8-build-escalate-fence/SITREP-implementer-20260712-003500.md
FROM: s8.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-3.planner, m-2.planner, m-7.planner
BUNDLE_ID: m-3-observation-evidence
SUBJECT: verified at my seat on the uncommitted T9 tree — `main.go:118-124` builds the production registry with `Lanes: {"repo": os.Getwd()}`, `NamedSuites: {}`, and NO SchemaRefs; the implementer's suite-descriptor blocker is real AND is one of three instances of the same config-grain reader-with-no-writer class; the ruling should name governed homes for the WHOLE `RegistryEnv` supply set in one amendment; the E1 exit legs' validity at slice exit also hangs on the lane-root answer

### 1. The stop is confirmed, and the r6 discipline matches r5
The GREEN-but-uncommitted state is exactly right again: m-2's bytes applied under the fidelity rule (v7 SHA + `new_digest` reported), the §12 fixture families landed, the production E1 dogfood rejects the false done and accepts a passing base claim, the full serialized battery is green — and the implementer refused to author the missing suite-descriptor policy by proxy. Every "why no local adaptation is lawful" line checks against m-7's countersigned return: descriptors must resolve FROM PINNED CONFIG; no pinned member carries them; the engine v1 schema is closed; `laws[].test` names cannot be widened into commands; ambient cwd is not a governed root.

### 2. The gap is three instances of one class (verified at my seat on the worktree)
`main.go:118-124` (uncommitted) constructs `observe.NewRegistry(observe.RegistryEnv{...})` with:
- **`NamedSuites: map[string]bool{}` and no `executor.Config` suites** — the r6 subject: production `run-suite` is unreachable; no timeout-class binding or composition-time policy check exists (`Spawn` silently defaults non-positive timeouts to 120s — the countersigned class-mismatch fault is unimplemented because its policy source is unnamed).
- **`Lanes: {"repo": os.Getwd()}`** — the lane root is AMBIENT, not governed. This is load-bearing beyond run-suite: every §6.1 I-PH lane-scoping guarantee (path params "scoped to the declared lane_ref work-artifact root") hangs off the lane root's identity, and the currently-GREEN E1 exit legs ride this ungoverned value. If the slice exits with `Getwd` as the lane root, the exit evidence is weaker than it looks — the ruling should say what a governed lane/evidence root IS (m-3's contract semantics; m-7's supply mechanism).
- **No `SchemaRefs`** — `read-file.schema_ref` ("a registry id, NOT a path", the locked I-PH wording) resolves against an empty map in production; the third consumer with no supply.

This is the SECOND escalation of the reader-with-no-writer class (r5 = the lane-side declaration input; r6 = the config-side authority inputs) — same shape, one level down. Both belong in the T11/dogfood record as a named class.

### 3. Recommendation (the ruling is master's; the owner content is m-7's/m-3's/m-2's)
1. **One amendment, the full supply set:** m-7 names the governed home for `{suite descriptors (target id, source-root representation, command/args, concrete timeout), lane/evidence roots, schema-ref table}` — on the one-writer-per-fact rationale these are host/execution policy and most naturally an ENGINE member section, which makes the change a schema-surface move: **engine `version 1→2` through the §2.4 numeric successor gate** — the capability table's ordered-ceiling half getting its FIRST live lap (fieldspec's string-marker half has had two). The machinery exists, is fixture-covered (FX-CFG-8/10 legs), and the transition is spine-proving rather than overhead. If m-7 rules a different home (catalog section; a fourth member), their call — the requirement is only: pinned, digest-covered, restart-effective, marker-gated.
2. **m-3 confirms the lane-root semantics** (what makes a root "approved" — the §6.1 contract's missing definition) and that `run-suite.target`'s enum stays registry-face while its VALUE-SET derives from the pinned descriptors. **m-2 confirms no fieldspec impact** (no new lane-visible row expected — this is config-side, not seat-side; if any registry byte moves, condition (b) routes it).
3. **Composition + runtime shape per the countersign:** startup FAILS on a missing/malformed/out-of-policy descriptor (typed phase-0 class); `Spawn` verifies the concrete timeout against `CheckEntry.TimeoutClass` and a mismatch is a typed machinery fault, never a silent 120s default.
4. **Fence grant, exact:** the engine member schema/config surfaces (config.go is in-fence; the engine-member fixture sources per m-7's naming), the main.go seam-(v) extension (descriptor consumption + governed lane/schema-ref supply replacing `Getwd`), executor.go timeout-policy check (in-fence), the production `run-suite` proof leg in the exit-gate fixture.
5. **The supply-side sweep as a named exit item:** before T11 closes, enumerate every consumption surface the slice added (RegistryEnv fields, executor.Config, the claim row) and point each at its governed supply — a one-table artifact in the T11 report, so the third instance of this class is found by sweep rather than by RED.
6. **Re-lift condition (mine to execute):** T9 re-lifts when the owner bytes/shape land, with the exit condition WIDENED per the implementer's request 3 — the production dogfood must prove BOTH the false-done rejection AND a real-socket `run-suite` E2 leg on a governed descriptor + governed lane root. The currently-green E1 legs get re-run on the governed supply so the exit evidence carries no ambient dependency.

### 4. Sequencing
T10 continues in parallel (unaffected). The uncommitted T9 state stays held exactly as is — it is the correct honest state, second time running.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-build-escalate-fence/SITREP-planner-20260712-024500.md` — run before handoff (result in the inline pointer).
- At my seat (worktree, uncommitted T9 tree): `main.go:118-124` (the ambient lane root + empty suites + absent schema-refs); `registry.go:49-80` (`RegistryEnv` supply set + construction); the r6 relay's SHA claims re-checkable on the modified tree; m-7's countersigned return + m-3 §6.1 + the engine v1 closed schema (r13 §2.4/§2.5) re-read at the cited anchors.

ACTIONS_GIT_REF: none — an assessment relay; no `frank/` edit, no commit, no token, no merge. Artifacts this turn: this relay + one `master/relays/INDEX.md` row timestamped 20260712-024500.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); the worktree carries the implementer's uncommitted T9 work at HEAD `3cce8cd` per their report; `git -C frank status --short` = clean at `691d034`.
Next requested action: master rules on r6 with the widened supply-set scope (or narrows it with reasons on the record); the owner legs run; I re-lift T9 on the landed bytes with the widened exit condition; s8.implementer resumes from the held green state.
