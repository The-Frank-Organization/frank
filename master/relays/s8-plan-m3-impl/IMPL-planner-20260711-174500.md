## IMPL DISPATCH — s8 observe-spine build authorized: delegated token issued on the r2-approved plan, both SEQ-1 confirms consumed, all-in scope fence preserved, conditions (a)–(g) binding; operator boots the ratified fresh slice team

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s8-plan-m3-impl
PARENT_DISPATCH_ID: s8-plan-m3-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the operator BOOTS the fresh slice-team sessions this dispatch names (ratified s8 staffing); slice merge stays operator-only with HUMAN_MERGE_AUTHORIZATION at grant time
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
DESIGN_RECORD_KIND: design-doc
DESIGN_LOCK_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-7.planner, m-2.planner
IN_REPLY_TO: s8-plan-m3/PLAN-REVIEW-implementer-20260711-172400.md
BUNDLE_ID: m-3-observation-evidence
SUBJECT: delegated implementation dispatch for the s8 observe-spine — parented to the approving PLAN-REVIEW r2 (approve), both SEQ-1 confirms (m-7 …-151621 + m-2 …-153500) consumed, all-in SCOPE_DIFF preserved, conditions (a)–(g) binding; the operator boots the fresh slice team (s8.lead/s8.config/s8.registry/s8.reviewer) that executes under m-3 guidance

All delegated-dispatch preconditions from `s8-dispatch/PLAN-orchestrator-planner-20260711-145129` are met: orchestrator conditional dispatch authority granted, the pair PLAN-REVIEW returned **approve** (r2, `s8-plan-m3/PLAN-REVIEW-implementer-20260711-172400`), the mechanical SCOPE_DIFF is **all-in** with no hard trigger, and the PARENT chain points to that approving PLAN-REVIEW, whose parent is my pair-Planner PLAN addressed to the Implementer. I therefore issue the delegated token.

DISPATCH IMPL

**Consumed (required carry — no stale status propagates into build):**
- **Plan of record:** `master/domains/m-3-observation-evidence/plan/2026-07-11-s8-observe-spine-plan.md` (r2; 11 tasks + Task 6.5).
- **Both SEQ-1 confirms IN HAND (the pre-token gate is SATISFIED):** m-7 `s8-plan-m3-seq1/SITREP-planner-20260711-151621` (genesis pins v5 / FX-CFG-7 valid / post-genesis `v5→successor` / bless bypasses only the seat fill-gate) + m-2 `s8-plan-m3-seq1/SITREP-planner-20260711-153500` (both `adoption`+`catalog` tokens land in the one step-2 changeset / `v5→successor` lawful / changeset-before-observe grammar-required). **No correction to the order** — GENESIS(v5) → registry-changeset(`v5→successor`) → observe-activation; dogfood = fresh-genesis spine store; bless = existing-store migration built+fixtured in-slice, live-store activation a named public-release carry.

**Binding scope fence (the reviewer's all-in SCOPE_DIFF, `…-172400`) — do not exceed; the structured block is below.** Hard-trigger scan: none.

**Conditions binding on this dispatch (a)–(g), verbatim from the orchestrator delegation:**
(a) unique sub-DISPATCH_IDs per pair-internal leg; (b) registry/schema changes beyond the scheduled T2 changeset → escalate to master BEFORE work; (c) any change to a locked design contract → the owning pair via the amendment path + master, never a silent edit; (d) cross-domain shape changes (m-1 store semantics, m-6 surfaces) → master; (e) **merge is operator-only** — the merge-decision relay goes TO the operator, the executor acts only on a `HUMAN_MERGE_AUTHORIZATION` grant; (f) a blocked/model-stopped lane reports and holds — no pushing through a red; (g) the dogfood evaluation names which S7A-TRAIL-FINDINGS classes the conductor demonstrably kills.

**Staffing + the human boot leg (reconciling the pair-chain token with the fresh-team ratification).** This token is the lint-valid pair-chain authorization (the pinned `s8-plan-m3` loop — FROM m-3.planner TO the pair Implementer of record). The **actual build is carried by the operator-booted FRESH slice team** the plan names — **`s8.lead`** (m-3-guided, the observe-spine seats), **`s8.config`** (m-7 build-fidelity — config/genesis/executor/bless), **`s8.registry`** (m-2 — the T2 changeset + tokens/transition), **`s8.reviewer`** (adversarial per-task build gate) — sole-writer discipline per seat; **B10 is not in effect (fresh team, not the domain pair auto-building)**. **Operator action (the HUMAN_GATE leg): boot these sessions**; execution then runs task-by-task under `executing-plans`/`subagent-driven-development` with m-3 guiding by relay. If the ratified staffing routes build-seat authority differently than this pair-chain token implies, redirect at boot — the lint constrains the token to the same-owner pair, so the operational routing is yours.

**Build discipline:** vertical-slice-first (T1–T2 foundation → T3–T6 the thinnest end-to-end observed relay → T6.5 the staged pipeline → T7–T9 dispositions+guard+activation+exit-legs → T10–T11 migration+hardening); TDD per task, frequent commits, serialized battery FILE-captured + sequence-honest; the ten INV-CATALOG laws stay green; I-PH swept over every new seat-visible surface; Rails A/B stated per new surface; the interim defaults labeled interim with the s10 sunset. Exit-test legs 1+2 + the dogfood relaunch on the fresh-genesis store are the slice-exit acceptance.

SCOPE_DIFF:
- frank/internal/observe/gate.go -> in
- frank/internal/observe/registry.go -> in
- frank/internal/observe/checks_base.go -> in
- frank/internal/executor/executor.go -> in
- frank/internal/engine/submit.go -> in
- frank/internal/engine/loop.go -> in
- frank/internal/engine/completeness.go -> in
- frank/internal/config/config.go -> in
- frank/internal/fieldspec/predicate.go -> in
- frank/internal/fieldspec/validate.go -> in
- frank/internal/fieldspec/render.go -> in
- frank/internal/fieldspec/registry.json -> in
- frank/internal/store/genesis.go -> in
- frank/test/fixtures/s8_config_activation_test.go -> in
- frank/test/fixtures/s8_registry_changeset_test.go -> in
- frank/test/fixtures/s8_observe_spine_test.go -> in
- frank/test/fixtures/s8_check_registry_e1_test.go -> in
- frank/test/fixtures/s8_executor_test.go -> in
- frank/test/fixtures/s8_verdict_binding_test.go -> in
- frank/test/fixtures/s8_pipeline_stages_test.go -> in
- frank/test/fixtures/s8_decision2_test.go -> in
- frank/test/fixtures/s8_suppliability_guard_test.go -> in
- frank/test/fixtures/s8_exit_gate_test.go -> in
- frank/test/fixtures/s8_adoption_test.go -> in
- frank/test/fixtures/s8_adversarial_test.go -> in
- frank/test/invariants/ -> in
- frank/.relays/s8/ -> in
SCOPE_DIFF_RESULT: all-in

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s8-plan-m3-impl/IMPL-planner-20260711-174500.md` — run below.
- Lineage: PARENT `s8-plan-m3-review-r2` → PLAN-REVIEW approve (FROM m-3.implementer) → its parent `s8-plan-m3` → my pair-Planner PLAN (FROM m-3.planner, TO m-3.implementer). SEQ-1 confirms + SCOPE_DIFF fence cited above.

ACTIONS_GIT_REF: none — a dispatch/authorization relay; no `frank/` edit, no code, no commit by this file. The build begins in the operator-booted slice sessions on a branch off `frank/ main@691d034`. Sole artifact this turn: master/relays/s8-plan-m3-impl/IMPL-planner-20260711-174500.md
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `691d034`.
Next requested action: operator boots the s8 slice team; the build runs T1→T11 under the fence + conditions; m-3 guides by relay, stands by for blocker relays, and handles the PR/implementation report → the merge-decision relay TO the operator at slice exit. No merge without `HUMAN_MERGE_AUTHORIZATION`.
