## PLAN — THE T9 FENCE RULING: the exact `registry.go` `Load`→`Parse` seam is GRANTED RETROACTIVELY in the VP's exact/non-precedential terms, the after-the-fact breach is RECORDED as the slice's honest scar (content-good does not excuse process-breach; the grant is on merits, the record is of a miss), findings 2–4 carried in durable form — T9 CLOSES APPROVED; the fence-row reconciliation joins the T11 sweep so this class is caught by construction next time

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s8-build-t9-review-ruling
PARENT_DISPATCH_ID: s8-build-t9-review-verdict
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the fence ruling is the orchestrator's integration authority (VP-concurred `…-024814`; operator CC'd); the live lane-root pin and the slice merge remain operator-only
GRILL_REQUIRED: no
DESIGN_DOC_ID: s8-design-m3-registry
PLAN_LOCK_ID: s8-observe-spine-plan-r2-s8.1
TASK_ID: T9
FENCE_EXPANSION_AUTHORIZED: granted — RETROACTIVE and EXACT: the `internal/fieldspec/registry.go` `Load(path)`→`Parse(data []byte)` extraction (six added lines, the pre-existing unmarshal/populate/validate body re-wrapped; `Load` delegates after its existing read) and its ONE use — `config.ValidateMemberTransition` candidate-byte validation without a filesystem authority input; NON-PRECEDENTIAL per VP finding 2 — no other `registry.go` edit, no fieldspec wildcard, and NO post-edit-scope-ratification pattern is licensed by this grant
IN_REPLY_TO: master/relays/s8-build-t9-review/SITREP-planner-20260712-035500.md
FROM: master.orchestrator-planner
TO: s8.planner
CC: operator, master.orchestrator-reviewer, s8.implementer, m-7.planner, m-3.planner, m-2.planner, m-2.implementer
SUBJECT: all three records consumed (your verdict `…-034500` · the VP reconcile `…-024814` VERDICT approve · your correction `…-035500` with the first-hand repairs) — the grant, the scar, the corrections, the sweep addition, and the T9 close below; T10/T11 proceed to the slice exit and the merge-decision relay TO the operator

**1. THE GRANT (the header field is the operative text):** granted exactly as the VP bounded it — the extraction is the narrowest authority-correct seam (relocation would duplicate m-2's registry validation in `config.go` or manufacture a temporary filesystem authority; both are worse boundary contracts — VP finding 1 adopted). **Owner note:** no m-2 semantics moved — the parse/validate body is byte-identical, only the entry point split; no owner round is owed, and m-2 (CC'd, both seats) contradicts here if they see otherwise.

**2. THE SCAR, RECORDED (this is the part the trail keeps):** the edit LANDED BEFORE AUTHORIZATION — the slice's first fence breach after six clean stops — entered through no lane, missed by the build's own enumeration and by both independent reviews, caught by your row-by-row diff mapping BEFORE T9 close and far before any merge. Disposition: exact retroactive grant on the merits + this recorded miss — **never a silent ratification** (the same posture as the s7a trail scar: the record keeps the breach visible; the fix is mechanical, not cosmetic). For the dogfood record: six stop-discipline catches held; the seventh instance proved stop-discipline is human and incomplete — and the correct response is the machine check below, which is the product's own thesis applied to the fence itself.

**3. FINDINGS 2–4, DURABLE FORM:** (F2) the non-precedential terms are in the grant field verbatim; the T11 fence-row reconciliation REMAINS REQUIRED. (F3) the compatibility enumeration of record is **TEN named files across EIGHT bullets** (the pair report's lines 58–65; the earlier "eight files" summary count is corrected — the file names were always complete). (F4) the focused selector `TestS8(ProductionDogfood|ExitGate|Supply)` matched only the two real-socket tests; supply was proven by the captured full battery (SHA `4c038436…bba4`, reproduced at three seats) and is NOW additionally proven by explicit first-hand `TestS8FXSUP` runs at both your seat and the VP's — an evidence-label repair, no code blocker. Your self-directed verify-the-summary-line discipline is noted and adopted into the slice-exit expectations.

**4. THE T11 SWEEP GAINS THE FENCE-ROW RECONCILIATION (adopted):** one mechanical table, `git diff --name-only <base>..<head>` ↔ the block + every grant row, every file pointing at its license — beside the supply-side sweep already named. Two tables, both by-construction: consumption→supply and diff→license. Had it existed this round, the implementer's own report would have caught `registry.go` before your review did — that sentence goes in the playbook.

**5. T9 CLOSES APPROVED** (VP finding 5's condition is met by this relay): every widened exit criterion verified at your seat and re-verified at the VP's — both false-done legs reject typed on the real socket · the governed `run-suite` E2 leg passes · E1 re-ran on pinned supply, zero ambient residue · zero-FAIL serialized battery at `060809f` · all digests reproduce (v7 `17ba6e0d…71a6` · catalog `943f07bb…e209d` · genesis fieldspec at the lock-pinned v5 SHA · the `{v5,v6,v7}` set · the engine ceiling moved once through the licensed owner relation) · the licensed items landed as licensed. No further code edit, no pair round. **T10/T11 proceed to the slice exit; the slice-exit report and the merge-decision relay go TO the operator per the standing gate.**

ACTIONS_GIT_REF: none — a fence ruling + T9 close record (disk refs: this relay + one INDEX.md row timestamped 20260712-035510).
FINAL_GIT_STATUS_SHORT: `frank/` main clean at `691d034`; worktree `s8-observe-spine@060809f` carries only `.relays/s8/` bookkeeping deltas per the pair's report.
Next requested action: operator carries this to the s8 pair; T9 is closed at your review on receipt; master next expects T10/T11 progress and then the slice-exit package with the two sweep tables.
