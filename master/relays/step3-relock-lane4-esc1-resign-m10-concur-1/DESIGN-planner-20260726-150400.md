## DESIGN — m-10 CONCURRENCE on m-3's two rulings (resign Ask 2). (2a) 12-record coupling — CONCUR: the fencing positive arm may ride the regenerated `xit-dur-1` fixture, because a positive resume admits a successor at the current epoch under a valid lease, which IS my assign-gate observation (rev16 §4:55 / §6:130); the binary precondition = the regenerated fixture must EXPOSE the assign-gate-open-at-the-current-tuple observation (not merely "a resume happened"), with the hard-stop-and-retender-13 as the correct fallback on failure. (2b) refused-attempt accounting — CONCUR: a refused admission is a tracked fixture record at `sample_weight {governed_turns: 0, tool_calls: 0}` (an admission-gate event, zero successor work), closing my previously-joint half.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
EVIDENCE_TARGET: E1
CEREMONY_TIER: large
HUMAN_GATE_REQUIRED: no — design-level concurrence on m-3's two rulings within the amendment-r2 inputs; authors no fixture, freezes no weight, moves no byte. The §7/sample-weight supersession + ratification stay Master+VP+operator via the fresh lane-4 plan.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
DISPATCH_ID: step3-relock-lane4-esc1-resign-m10-concur-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-resign
RUN_ID: master
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-resign/DESIGN-orchestrator-planner-20260726-145943.md
FROM: m-10.planner
TO: master.orchestrator-planner, m-3.planner
CC: master.orchestrator-reviewer, operator, m-10.implementer, m-3.implementer, m-9.planner, m-9.implementer, l4.planner, l4.implementer
BUNDLE_ID: m-10-app-control-plane
RELAY_PATH: master/relays/step3-relock-lane4-esc1-resign-m10-concur-1/DESIGN-planner-20260726-150400.md
SUBJECT: m-10 concurs on m-3's two rulings — (2a) 12-record `xit-dur-1` coupling CONCUR with the exact binary precondition (fixture must expose the assign-gate-open-at-current-tuple observation) + hard-stop-to-13 fallback; (2b) refused-attempt `sample_weight {governed_turns:0, tool_calls:0}` CONCUR, closing my joint half

master, m-3 — m-10's concurrence on the two m-3 rulings.

## Ask 2a — the 12-record coupling: CONCUR (with the precondition named against my observable)
**CONCUR.** The fencing **positive control** proves the assign gate **discriminates** — it must observe a legitimate successor **admitted at the current `{run_id, generation_id, turn_epoch, state_seq}` under a valid lease**, the assign gate opening exactly once (rev16 §4:55 atomic lease-at-current-epoch admission + §6:130 two-form assign gate). A **positive `xit-dur-1` resume is exactly that event** — it admits a successor turn at the current epoch/lease via my admission/assign-gate path. So the fencing positive arm can **ride the regenerated `xit-dur-1` fixture** rather than duplicate a fresh scenario, and the 12-record count (10 + the two mandatory negatives, positive riding `xit-dur-1`) is sound **on my machinery** — my admission/assign-gate observation is genuinely present in a positive resume.

**The binary precondition, named precisely against my observable** (so it is testable, not assumed): the regenerated `xit-dur-1` fixture must **EXPOSE the assign-gate observation itself** — that the two-form gate **opened at the current trusted tuple** (a valid tuple-matching `epoch_installed`/`state_proposal_result{installed}`, exactly once, rev16 §6:130) and the lease bound at the current epoch (§4:55) — **not merely that "a resume happened" or that resumed-content fidelity held.** If the regenerated fixture surfaces that gate-open-at-correct-tuple record, the reuse is valid (12 records). **If it does not** (e.g. it observes only content fidelity, not the gate), the precondition **fails ⇒ hard stop + separately-reviewed 13-record successor** giving the positive its own record — which I **concur is the correct fallback** (never a silent cardinality change). Lane 4 verifies this precondition when it regenerates the fixture; I name the exact observation it must carry.

## Ask 2b — refused-attempt accounting: CONCUR (closes my joint half)
**CONCUR.** A refused admission (`neg.STALE_EPOCH`, `neg.WRONG_LEASE`) is a **tracked fixture record** at **`sample_weight {governed_turns: 0, tool_calls: 0}`** — an **admission-gate event, not a governed turn**. This is exactly right on my machinery: the gate precedes all work, so a fenced successor performs **zero successor work** (no `provider_attempts`/`tool_calls`/`tool_authorizations` rows, no `assign`/`turn_open`/DATA-P frames), and it commits **no governed turn and no tool call** — hence `{0, 0}`. It is nonetheless a **tracked record** (the refusal is the observable, and it weighs on the record/turn-**attempt** count), which is what my close4 rev3 left **expressly joint** for this rebalance. m-3's ruling resolves it, and **I close my half: refused admission = tracked record, `{governed_turns: 0, tool_calls: 0}`.** This reconciles cleanly with the EXACTLY-30-turns/100-calls invariant (the two refused negatives add 0 to both budgets; only the positive — riding `xit-dur-1` — is a governed turn, already counted in the durability leg it rides).

## Boundaries
Design-level concurrence only. No fixture authored, no §7 row, no sample-weight frozen, no `xit-dur-1` regenerated, no amendment, no byte moved, lane 4 not resumed. rev16 `3e3c5192…`, m-3 consolidated bundle `7d7b6dbe…`, stage-6 amendment `1125b0a0…`, interface lock `cbd1893c…`, r1 amendment `528d6a98…` (unratified) UNMOVED. The §7/sample-weight supersession + the fresh lane-4 plan + ratification stay Master+VP+operator; lane 4 held. H-12 stands.

## Verification
- 2a grounded in rev16 `3e3c5192…` §4:55 (atomic lease-at-current-epoch admission) + §6:130 (two-form assign gate opens exactly once at the current tuple) — UNMOVED; the positive-resume-admits-at-current-epoch equivalence is a fact of my admission path.
- 2b grounded in my Route-4/close4 zero-work-observed-not-reachable contract (`…close4-m10-1/DESIGN-planner-20260726-134700.md`) — a fenced successor commits zero governed turn + zero tool call.
- m-3's two rulings: the consolidated bundle `7d7b6dbe…` (approval `b73085e6…`), per master's resign dispatch `…-145943`.
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/step3-relock-lane4-esc1-resign-m10-concur-1/DESIGN-planner-20260726-150400.md` — run at filing; result inline.

ACTIONS_GIT_REF: docs-workspace disk action — this concurrence relay + one INDEX.md row; no fixture/§7 row/weight/amendment authored, no byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo; `frank/` main clean at `c78da38`.
Next requested action: master folds m-10's two concurrences (2a with the named precondition + hard-stop-to-13; 2b `{0,0}`) alongside l4's design-level concurrence into the amendment-r2 inputs; the actual rebalance + `xit-dur-1`-reuse precondition verification land in the fresh (post-ratification, VP-reviewed) lane-4 plan. rev16 UNMOVED. H-12 stands.
