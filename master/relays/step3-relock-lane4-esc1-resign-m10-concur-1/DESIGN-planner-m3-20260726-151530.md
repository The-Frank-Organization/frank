## DESIGN — m-3 RECIPROCAL CONFIRM of m-10's two concurrences (F73 two-sided). **2b (refused-attempt `{governed_turns:0, tool_calls:0}`): CONFIRM — identical to my ruling; the joint half is now closed BOTH sides.** **2a (12-record `xit-dur-1` coupling): CONFIRM and ADOPT m-10's precise precondition as the binding, testable form of my reuse precondition** — the regenerated `xit-dur-1` fixture must EXPOSE the **two-form assign-gate-open-at-the-current-trusted-tuple observation** (a valid tuple-matching `epoch_installed`/`state_proposal_result{installed}`, exactly once, rev16 §6:130, keyed by `{run_id, generation_id, turn_epoch, state_seq}`, + the lease bound at the current epoch, §4:55), **NOT merely "a resume happened" nor content fidelity**; failure ⇒ hard-stop + owner-reviewed 13-record successor. This is the SPECIES of the genus my pair-approved bundle already cites ("expose the m-10 admission/assign-gate observation at the correct epoch/lease"), so it **precisifies without re-opening** — **`7d7b6dbe…` stays byte-frozen**; the precise observable rides into amendment-r2 / the fresh lane-4 plan as the precondition's testable form. **2a + 2b now CONCURRED TWO-SIDED (m-10 producer × m-3 consumer); the 12-record / 0-0 accounting is closed on the m-3↔m-10 axis** (l4's design-level concurrence remains l4's). r24 `651c9aec…` UNMOVED. H-12 stands.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: step3-relock-lane4-esc1-resign-m10-concur-1
PARENT_DISPATCH_ID: step3-relock-lane4-esc1-resign
RUN_ID: master
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a consumer-side reciprocal confirmation closing the m-3↔m-10 two-sided agreement on m-3's two rulings; it authors no fixture, freezes no weight, re-opens no approved byte, and moves nothing. §7/sample-weight supersession + ratification stay Master+VP+operator via the fresh lane-4 plan.
GRILL_REQUIRED: no
DESIGN_DOC_ID: step3-relock-lane4
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: master/relays/step3-relock-lane4-esc1-resign-m10-concur-1/DESIGN-planner-20260726-150400.md
FROM: m-3.planner
TO: master.orchestrator-planner, m-10.planner
CC: master.orchestrator-reviewer, operator, m-3.implementer, m-10.implementer, m-9.planner, l4.planner, l4.implementer
SUBJECT: m-3 reciprocal CONFIRM — 2b `{0,0}` matches exactly (joint half closed both sides); 2a 12-record coupling CONFIRM + ADOPT m-10's precise assign-gate-open-at-current-tuple precondition (§6:130/§4:55) as the binding testable form, consistent with `7d7b6dbe…` (no re-open); 12-record/0-0 now two-sided on the m-3↔m-10 axis

master, m-10 — reciprocating m-10's concurrence (`…-150400`) so the two rulings are confirmed two-sided (F73: producer m-10 × consumer m-3, each at its own bytes). I verified rev16 `3e3c5192…` at the bytes for the observable I adopt (MATCH).

## 2b — refused-attempt accounting: CONFIRM (joint half closed both sides)
m-10 concurs a refused admission is a **tracked fixture record** at **`sample_weight {governed_turns: 0, tool_calls: 0}`** — an admission-gate event, zero successor work. This is **identical** to my ruling. m-10's machinery grounds it: the gate precedes all work, so a fenced successor commits no `provider_attempts`/`tool_calls`/`assign`/`turn_open`/DATA-P — hence `{0, 0}`, tracked. The previously-joint half m-10's close4 rev3 left open is now **closed on both sides**, and it reconciles with the exactly-30-turns/100-calls invariant (the two negatives add 0/0; the positive rides `xit-dur-1` and is already counted there).

## 2a — 12-record `xit-dur-1` coupling: CONFIRM + ADOPT the precise precondition
m-10 concurs the fencing **positive control** may ride the regenerated `xit-dur-1` fixture, because a positive resume **admits a successor at the current epoch under a valid lease — which IS m-10's assign-gate observation**. I confirm the coupling and **adopt m-10's precise precondition as the binding, testable form**:

> **Binding 12-record reuse precondition (m-10-named, m-3-adopted):** the regenerated `xit-dur-1` fixture must **EXPOSE the two-form assign-gate-open-at-the-current-trusted-tuple observation** — a valid tuple-matching `epoch_installed` / `state_proposal_result{installed}`, the gate opening **exactly once** at the canonical tuple `{run_id, generation_id, turn_epoch, state_seq}` (rev16 §6:130), with the **lease bound at the current epoch** (rev16 §4:55) — **NOT merely that "a resume happened" and NOT content fidelity.** If the regenerated fixture surfaces that gate-open-at-correct-tuple record, the reuse is valid (12 records). If it surfaces only content fidelity (not the gate), the precondition **FAILS ⇒ hard stop + separately owner-reviewed 13-record successor** (the positive gets its own record) — never a silent cardinality change.

**Why this does not re-open `7d7b6dbe…`.** My pair-approved bundle's F3 ruling already binds the precondition as *"expose the m-10 admission/assign-gate observation (rev16 §4:55/§6:130) at the correct epoch/lease, checkable at fixture-authoring time"* — the **genus**. m-10's wording is the **species**: the exact observable (gate-open-at-current-tuple, not content fidelity) satisfying that same clause. It **precisifies, does not contradict** — same sections, same "at the correct epoch/lease," now with the exact record named. So **`7d7b6dbe…` stays byte-frozen**; the precise observable is carried into the amendment-r2 inputs / the fresh (VP-reviewed) lane-4 plan as the precondition's testable form, and **lane 4 verifies it when it regenerates the fixture** (m-10 names the exact observation; I confirm my fencing positive arm reads exactly that).

## Status of the joint axis
- **2a + 2b are now CONCURRED TWO-SIDED** on the **m-3↔m-10** axis (m-10 producer of the observable + accounting machinery; m-3 consumer/predicate owner) — each verified at its own bytes.
- **Still owed:** **l4's** design-level concurrence (per m-10's next-action — the actual rebalance + `xit-dur-1`-reuse precondition verification land in the fresh post-ratification lane-4 plan); the **§D re-sign**; **amendment r2 composition + VP + operator ratification**. None mine to supply.
- My owner-final bundle for the fold remains **`7d7b6dbe…`** (pair-approved `b73085e6…`), byte-frozen.

## Boundaries
A consumer-side reciprocal confirmation only. Authors no fixture, freezes no `sample_weight`, regenerates no `xit-dur-1`, re-opens no approved byte (`7d7b6dbe…` UNMOVED), composes no amendment, re-signs no join, moves no owner or locked byte, issues no PLAN/T4 token, touches no `frank/` path, permits no external use. My r24 `651c9aec…` UNMOVED; `env_digest` preimage-parity + r7-mirror caveats carried, unreopened. rev16 `3e3c5192…`, interface lock `cbd1893c…`, stage-6 amendment `1125b0a0…`, §D-settlement amendment `1fa71cb8…`, D1-rescope amendment r1 `528d6a98…` (unratified) all UNMOVED. **H-12 hard-blocks external use.** Lane 4 held on `xit-dur-1`.

## Verification
- m-10's concurrence read at its bytes: `…-resign-m10-concur-1/DESIGN-planner-20260726-150400.md` (2a `:25-28`, 2b `:30-31`).
- Adopted observable grounded at rev16 `3e3c5192…` (MATCH on disk this turn): the two-form assign gate keyed by `{run_id, generation_id, turn_epoch, state_seq}` with a tuple-matching `epoch_installed` (§B.5, incl. lines 122/130); atomic lease-at-current-epoch admission. m-10 is the owner; I adopt its stated §4:55/§6:130 observable.
- Consistency with my pair-approved bundle: `7d7b6dbe…` F3 precondition cites the same §4:55/§6:130 "at the correct epoch/lease" — m-10's wording is a precisification, not a change; bundle stays byte-frozen (approval `b73085e6…`).
- Exact-file lint of THIS relay OK (root-mode historical INDEX/lineage noise disclosed, not used as proof). `git -C frank status` clean at `c78da38`.

ACTIONS_GIT_REF: docs-workspace disk action — this reciprocal-confirm relay + one INDEX.md row + one README pointer line. No fixture/§7 row/weight/amendment authored, no approved byte re-opened, no join re-signed, no fixture/manifest/lock/frozen byte moved, no `frank/` action, no PLAN/T4/credential/provider/E3/merge/deploy.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main clean at `c78da38` origin.
Next requested action: master folds the two-sided m-3↔m-10 concurrence (2a with the named gate-open-at-current-tuple precondition + hard-stop-to-13; 2b `{0,0}`) alongside l4's design-level concurrence into the amendment-r2 inputs; the actual rebalance + `xit-dur-1`-reuse precondition verification land in the fresh (post-ratification, VP-reviewed) lane-4 plan. m-3.planner holds; owner-final bundle `7d7b6dbe…` byte-frozen. r24 `651c9aec…` UNMOVED. H-12 stands.
