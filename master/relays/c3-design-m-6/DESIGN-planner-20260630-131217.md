## m-6 DESIGN-REVIEW REQUEST → m-6.implementer — c3 Human Surface & Scheduler design doc (r1)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m-6
PARENT_DISPATCH_ID: c3-design-m-6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-review request; the operator-judgment items are already grilled + folded (GRILL_LOCK §9)
GRILL_REQUIRED: yes — folded as GRILL_LOCK §9 (operator decisions, 2026-06-30)
FROM: m-6.planner
TO: m-6.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-5.planner, operator
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: m-6-human-surface-scheduler
SUBJECT: DESIGN-REVIEW request — the m-6 c3 design-of-record (five surfaces, promote-and-bind). Adversarially review; return DESIGN_REVIEW_VERDICT. Held: the away-mode token-bridge cell (OQ-1, m-1 confirm-or-gap) is recorded-NOT-locked by design. ALSO (CC orchestrator): the away-mode binding is reached → OQ-1 ready for orchestrator routing to m-1.planner.

m-6.implementer — adversarial design-review request per the pair contract. The design doc is written + operator-approved (Superpowers brainstorming + the operator grill); the seam with m-5 is converged in the COORD thread. Review for evidence, challenge the proofs, flag any product-semantics or boundary issue.

**Design doc (the artifact to review):**
`master/domains/m-6-human-surface-scheduler/design/2026-06-30-v3-human-surface-scheduler-design.md` (DESIGN_DOC_ID `c3-design-m-6-human-surface-scheduler`).

**Lineage:** basis = VP-approved reconciled audit `c3-audit-m-6` (`RECONCILE-planner-20260630-054107`); dispatch `c3-design-m-6/DESIGN-orchestrator-planner-20260630-121325`; seam `c3-design-m5-m6-coord` (declare `122628` + bind `123022`).

**What to adversarially scrutinize (the proof obligations the dispatch set):**
1. **promote-and-bind — PROVE no rebuild.** §1/§8: does every surface bind a LOCKED writer (m-1 addressing/stamp, m-2 `gate_category`/HUMAN_GATE/`delivery_state`, m-3 egress/observe/`record_integrity`/ODB-schema, m-4 routing-raise) or design the away-mode bridge? Challenge any place I re-built a schema (the ODB must be promoted from m-3, §3) or assumed a TUI (Step-4 contract only).
2. **no-bucket-without-a-writer + direction-explicit (§2).** Each of A/B/C/D has a cited locked writer; only A+C reach the operator; D is author-facing. Is the cut right — or does D belong outside the operator-surface taxonomy entirely (DQ-3)?
3. **ODB capture + the "elaborate more" fork (§3).** Bounded `agent_enum_pick` (buttons) → operator-FROM verdict; J1 hold_and_resummon, refresh-before-resummon, never auto-approve. The **elaborate-more = read-only context-preserving fork** of the parked lane (gate stays parked; the fork's ONLY output is the decision relayed back — write-capable fork operator-declined; re-observe-on-resume). Challenge: is the fork truly read-only-safe + non-interrupting? Two-writers risk?
4. **7-state park/wake on the durable store + escalate-channel-never-resolve (§4).** Confirm none auto-resolves (J1); the two-timer model; `egress_blocked → local resummon, never auto-redact/send`.
5. **away-mode bridge + the HELD blocker (§4/§10 OQ-1).** A-live + B-frequent-digest, opt-in, egress-gated (m-3 §7). The inbound verdict-token (signed, one-time, per-`(decision,seat,choice)`; POST-not-GET; nonce≠window) is **recorded-NOT-locked** pending the m-1 confirm-or-gap (A m-1-owns-mint / B m-6-owns-bridge). **Verify I did not silently lock the away-mode cell** — it must read as a blocker/human-decision, not a gap.
6. **governance-vs-collaboration split + gate→lane routing (§5).** Design/grill gates route to the meeting lane (not compressed); routing off LOCKED `{gate_category, phase, GRILL_REQUIRED}` (no new field); re-observe-on-resume; attach mechanism deferred Step-3/4.
7. **interjection host (§6) + the m-5 bind matrix (§7).** Claude-Code three-mechanism (not jcode side-panel); the (posture × surface_intent) matrix binds only on m-5's DECLARED vocabulary (declare-before-bind, F2 — no pre-bind; the actuator-interrupt cell is a named placeholder). `surface_intent` derivation must be TOTAL.
8. **local-first / egress fail-closed (§11).** Every Step-2 mechanism local-only; sole external send (away bridge) egress-gated.

**Verdict requested:** return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` (read-only PHASE: DESIGN-REVIEW; parent this relay; carry DESIGN_DOC_ID). Treat the away-mode token-bridge cell (OQ-1) as a **recorded blocker, not a gap** — its resolution is the orchestrator-routed m-1 confirm-or-gap, not an m-6 design defect.

**→ orchestrator (CC), separate action:** the m-6 design has **reached the away-mode binding** — per the COORD (`122616`/`123022`), the bounded **m-1 confirm-or-gap (OQ-1)** is drafted and **ready for you to route to m-1.planner** as the first conditional-upstream-contract-check. The m-6 design-complete report holds until: (a) this DESIGN-REVIEW returns approve, (b) the c3 seam reconcile, and (c) the m-1 confirm-or-gap resolves (or is recorded as a blocker/human-decision). I will NOT self-advance to PLAN.

ACTIONS_GIT_REF: none — design-phase relay + design doc authored under master/domains/ (docs workspace; cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: m-6 c3 design-of-record written (`2026-06-30-v3-human-surface-scheduler-design.md`, DESIGN_DOC_ID `c3-design-m-6-human-surface-scheduler`) — five proven surfaces + GRILL_LOCK + seam resolution + the held m-1 blocker; DESIGN-REVIEW requested from m-6.implementer; the away-mode binding flagged to the orchestrator for m-1 routing.
Not done: the implementer's DESIGN-REVIEW verdict; the c3 seam reconcile; the m-1 confirm-or-gap resolution; the design-complete report (all gating the c3 lock). No PLAN/IMPL (terminal at design-lock this phase).
Blocked: the away-mode token-bridge cell on OQ-1 (by design — recorded). Everything else locks independently.
Scope drift risk: none — design-only; locked m-1..m-4 not reopened (only the bounded m-1 confirm-or-gap); declare-before-bind honored; the held blocker recorded not silently locked.
Next requested action: m-6.implementer returns DESIGN_REVIEW_VERDICT; orchestrator routes the OQ-1 m-1 confirm-or-gap to m-1.planner. On implementer-approve + seam-reconcile + m-1-answer, I report design-complete to the orchestrator for the c3 lock.
