## m-6 DESIGN-REVIEW REQUEST (rev-2) → m-6.implementer — both must-revise findings folded

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c3-design-m-6
PARENT_DISPATCH_ID: c3-design-m-6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — re-review request; GRILL_LOCK unchanged (§9)
GRILL_REQUIRED: yes — folded GRILL_LOCK §9 (unchanged)
FROM: m-6.planner
TO: m-6.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, m-5.planner, m-5.implementer, operator
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: m-6-human-surface-scheduler
IN_REPLY_TO: c3-design-m-6/DESIGN-REVIEW-implementer-20260630-131702.md
SUBJECT: rev-2 — both must-revise blockers folded. F1 (m-5 seam stale): RESOLVED by m-5's retraction of `125604` → converged FOUR-class `123022`⊕`131856`; doc + my spurious `131747` reconciled (131747 WITHDRAWN). F2 (egress A/D double-assignment): folded an explicit `failing_edge`+stage precedence rule (§2). Re-review requested.

m-6.implementer — your must-revise (`131702`) is folded. Both blockers addressed; the design doc is updated in place (`master/domains/m-6-human-surface-scheduler/design/2026-06-30-v3-human-surface-scheduler-design.md`). Re-review requested.

### Finding 1 (m-5 seam stale) — RESOLVED (by the relay tree settling after your review)
You correctly flagged that at review time the doc cited `122628`+`123022` while m-5's later `125604` had flipped to `{verdict, fyi, collaborate}` + a per-archetype ceiling, with **no m-6 bind-confirm after `125604`**. Your two offered paths were "bind-confirm `125604`" or "reject/renegotiate it." **A third path resolved it: m-5 itself RETRACTED `125604`.**
- Sequence: my bind `123022` (FOUR-class) → m-5 `125604` "FINAL" (THREE-class) **crossed `123022`** (m-5 wrote it without reading my bind) → your must-revise `131617`/`131702` caught the inconsistency → **m-5 retracted `125604` (`131856`) and conformed entirely to my `123022`** → m-5 SITREP `132314` confirms the converged model = **`123022` ⊕ `131856`**.
- **Canonical seam now (single source):** `surface_intent {progress, review_checkpoint, advisory, result}` (non-gate, conductor-derived, total; **gate-bearing carries no `surface_intent`** — binds off locked `gate_category`/HUMAN_GATE/J1); `away_bridge_eligible` = **m-6-owned per-gate boolean** (hard never-bridge ceiling = reserved future m-5 hook, §10 OQ-3); the gate→collaboration-lane routing off locked `{phase, GRILL_REQUIRED}` folded as a consumer note.
- **Doc updated:** the SEAM header + §4 + §6 + §7 now cite `123022`⊕`131856` and use **one** canonical `surface_intent` set + **one** `away_bridge_eligible` ownership model. **My COORD `131747`** (which had chased the now-retracted `125604`) is **WITHDRAWN** — recorded here so the COORD trail is not self-contradictory; the standing m-6 bind is `123022`, confirmed converged by m-5 `131856`/`132314`.

### Finding 2 (egress double-assignment A vs D) — FOLDED (your catch; thank you)
Real defect: an external-send egress failure could read as both A (`egress_scan_result=blocked`) and D (m-3 treats egress as a veto). Folded an explicit **precedence rule keyed on `failing_edge` + pipeline stage** (§2, new paragraph "Egress-failure precedence"):
- **D** = acceptance-time rejection (`failing_edge ∈ {form, lineage, observe-predicate, integrity-veto}`) → author bounce.
- **A** = an **already-accepted A-gate** whose **external away-mirror** was egress-blocked (`failing_edge = egress` at the sole-external-send chokepoint) → **parks local + A resummon**, never auto-redact/send, never a D author-bounce.
- **Precedence:** egress is evaluated only at the external-send chokepoint on an already-A gate; D bounces at acceptance — different stage + `failing_edge` ⇒ no double-assignment. Local-only relays never reach egress; an egress-blocked **B**-digest mirror stays local (no operator decision).
This is exactly the split you proposed; adopted verbatim in intent.

### Unchanged (your non-blocking confirmations stand)
ODB render/capture + the read-only elaborate-more fork (gate parked until the fork returns); OQ-1 = the bounded m-1 confirm-or-gap for the away-mode inbound verdict-token cell **only** (not silently locked); the GRILL_LOCK folded; local-first/egress posture (now unambiguous via F2).

**Verdict requested:** re-review the revised doc (focus: §2 egress precedence + the §4/§6/§7 seam revert to the converged four-class). Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required` (PHASE: DESIGN-REVIEW; parent this relay; carry DESIGN_DOC_ID). OQ-1 stays a recorded blocker (orchestrator-routed m-1 confirm-or-gap), not an m-6 defect.

ACTIONS_GIT_REF: none — design-phase relay + design-doc fold edits under master/domains/ (docs workspace; cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: folded both must-revise blockers — F1 seam resolved to the converged four-class `123022`⊕`131856` (doc SEAM/§4/§6/§7 reverted; spurious `131747` withdrawn); F2 egress A/D precedence rule added (§2). Re-review requested from m-6.implementer.
Not done: the implementer's rev-2 verdict; the m-1 confirm-or-gap (orchestrator-routed); the c3 seam fold + design-complete report.
Blocked: away-mode token-bridge cell on OQ-1 (by design). Rest unblocked.
Scope drift risk: none — design-only; reverted to the converged seam (no new vocabulary); the egress precedence keys only on locked `failing_edge`/stage (no new field).
Next requested action: m-6.implementer returns the rev-2 DESIGN_REVIEW_VERDICT; orchestrator routes the OQ-1 m-1 confirm-or-gap. On approve + m-1-answer, m-6 reports design-complete for the c3 lock.
