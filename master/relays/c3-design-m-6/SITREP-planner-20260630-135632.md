## m-6 DESIGN-COMPLETE (report + hold) → orchestrator — pair-approved + seam-converged; OQ-1 the one held cell; route the m-1 confirm-or-gap

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: c3-design-m-6
PARENT_DISPATCH_ID: c3-design-m-6
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-completion report + hold; the operator GRILL_LOCK is folded (§9)
GRILL_REQUIRED: yes — folded as GRILL_LOCK §9 (operator decisions, 2026-06-30)
FROM: m-6.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-6.implementer, m-5.planner, operator
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
BUNDLE_ID: m-6-human-surface-scheduler
IN_REPLY_TO: c3-design-m-6/DESIGN-REVIEW-implementer-20260630-133839.md
SUBJECT: m-6 DESIGN-COMPLETE (report + HOLD; no self-advance to PLAN). Pair-approved (`133839`) + seam converged (`123022`⊕`131856`). One held cell: OQ-1 away-mode inbound verdict-token — REQUEST: route the bounded m-1 confirm-or-gap to m-1.planner (away-mode binding reached + reviewed). Awaiting m-1 answer + your c3 seam-fold / lock.

Orchestrator — m-6 Human Surface & Scheduler is **design-complete and holding** per the c3 design dispatch (`c3-design-m-6/DESIGN-orchestrator-planner-20260630-121325`). I do **not** self-advance (c3 terminal = design-lock; no PLAN/IMPL).

### Design-complete status
- **DESIGN_DOC_ID:** `c3-design-m-6-human-surface-scheduler` — `master/domains/m-6-human-surface-scheduler/design/2026-06-30-v3-human-surface-scheduler-design.md` (r2).
- **Pair-approved:** `DESIGN_REVIEW_VERDICT: approve` — m-6.implementer `c3-design-m-6/DESIGN-REVIEW-implementer-20260630-133839.md` (rev-2; both must-revise blockers folded: F1 seam, F2 egress A/D precedence). Approval covers the whole doc **except** the held OQ-1 cell.
- **Seam converged (both sides):** `c3-design-m5-m6-coord` — **m-6 bind `123022` ⊕ m-5 confirm `131856`** (the interim `125604`/`131747` three-class excursion crossed `123022` and was retracted both sides; m-5 SITREP `132314`). Canonical seam: `surface_intent {progress, review_checkpoint, advisory, result}` (non-gate, conductor-derived, total; gate-bearing carries none — binds off locked `gate_category`/HUMAN_GATE/J1); `away_bridge_eligible` = m-6-owned boolean (hard never-bridge = reserved future m-5 hook); gate→collaboration-lane routing off locked `{phase, GRILL_REQUIRED}`. m-5's own implementer approved its doc (`c3-design-m-5/DESIGN-REVIEW-implementer-20260630-133831`); m-6.implementer cross-verified the two docs match. **The reconciled seam statement is fold-ready** (COORD `131747`/`132314`).
- **GRILL_LOCK folded (§9):** operator decisions (2026-06-30) — away-mode A-live + B-frequent-digest (default sub-daily); buttons + read-only context-preserving elaborate-more fork (write-capable declined); Bucket-B live non-interrupting digest; escalate-channel-never-resolve cadence; delegation deferred; §J config surface m-6-owned / values operator-owned.
- **The five surfaces locked** (independent of the blocker): A/B/C/D bucket taxonomy + the egress A/D precedence rule (§2); ODB render + capture + elaborate-more fork (§3); the 7-state park/wake machine + resummon cadence (§4 minus the away-token cell); email+meeting surface + gate→lane routing + re-observe-on-resume (§5); interjection host (§6); the m-5 bind matrix (§7). Consumer boundary contract named (§8). Local-first / egress fail-closed: PASS (§11).

### The ONE held cell + the action I need from you
- **OQ-1 [LOCK-BLOCKING — VP F4] — the away-mode inbound verdict-token bridge.** Recorded, **not silently locked** (m-6.implementer confirmed). The bounded question is drafted (doc §4/§10; COORD `122616`/`123022`/`133146`): **(A) m-1 owns the mint** vs **(B) m-6 owns the bridge over m-1-crypto** (token signature = the operator-channel stamp brought inbound; POST-not-GET; replay-nonce ≠ validity-window; fail-closed).
- **REQUEST → orchestrator:** the m-6 away-mode binding is now **reached AND pair-reviewed** — please **route the bounded m-1 confirm-or-gap to m-1.planner** as the first conditional-upstream-contract-check (VP F3) and re-engage m-1. **The away-mode token cell stays unlocked** until m-1 answers (A/B) or you record it as a blocker/human-decision. Nothing else in m-6 waits on it.

### Hold
m-6 holds at design-complete. Awaiting: (1) the m-1 confirm-or-gap answer (orchestrator-routed), and (2) your **c3 seam-fold + design-lock** integration (the orchestrator owns the cross-pair fold). No PLAN/IMPL this cycle. On the m-1 answer I will fold it into the away-token cell (a bounded, additive close) and confirm design-lock-ready.

ACTIONS_GIT_REF: none — report-only SITREP; no edits (cwd is not a git repo)

FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; pcode/ is the code repo)

Done: m-6 design-complete report — pair-approved (`133839`), seam converged (`123022`⊕`131856`, both pairs), GRILL_LOCK folded, five surfaces locked, consumer contract named; the held OQ-1 cell flagged + the m-1 confirm-or-gap requested for orchestrator routing.
Not done: the m-1 confirm-or-gap answer; the away-token cell lock; the orchestrator's c3 seam-fold/design-lock. No PLAN (c3 terminal = design-lock).
Blocked: the away-mode token-bridge cell on OQ-1 (by design — recorded, not silently locked).
Scope drift risk: none — report-only; holding; c1/c2 not reopened; the one upstream touch is the bounded orchestrator-routed m-1 confirm-or-gap.
Next requested action: orchestrator routes the OQ-1 m-1 confirm-or-gap to m-1.planner + folds the converged c3 seam at the lock. On the m-1 answer, m-6 folds the away-token cell and confirms design-lock-ready.
