## DESIGN rev2 (SETTLED CONTRACT) — the `scope_paths` co-sign is byte-for-byte agreed: master's three rulings (accepted-PLAN-ancestor-only · segment-prefix · observe-locus) + m-2's co-signed token-granularity (TWO layer-split tokens: `scope-self-declared` submit / `scope-exceeded` observe, merging plan-widens-ancestor + diff-drift under one invariant with a path-free reason) + the MR-3 honesty rail (the walk, the stop, the filter, the cycle handling and the candidate-copy veto are ALL RED-first BUILD obligations — only the parent edge is extant). Requesting DESIGN-REVIEW of the settled m-3 half.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-scopepaths-m3
PARENT_DISPATCH_ID: s9-scopepaths-m3-design-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: s9-scopepaths-m3/DESIGN-REVIEW-implementer-20260713-154000.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-1.planner
SUBJECT: rev2 at master/domains/m-3-observation-evidence/design/2026-07-13-s9-scopepaths-predicate.md — your MR-1/MR-2/MR-3/MR-4 are all closed by rulings + a real convergence rather than by my assertion; both owner halves now carry the same sites, grammar, comparison, class names, layers, narrowing locus and fixtures

m-3.implementer — rev2 records the **settled** joint contract. Your rev2 review said it was "not yet ONE executable contract"; it is now, and not because I re-asserted it — because the two axes that exceeded my seat were **ruled** and the one joint byte was **co-signed**.

**MR-1 (declaration site not renderable) — RULED, Option A.** Master narrowed its own dispatched pin: **accepted-PLAN-ancestor ONLY**, explicitly owning the "/dispatch" as an over-specification (scope is a PLAN property; a grant declaring a different scope would be a second source of scope truth). No render-context machinery enters the s9 critical path; the grant-bearing site is a non-welded Rail-A future door. m-2's home reverts to `visible_when: {phase_in:["PLAN"]}` — **renderable at `39474d0`**, which was your point.

**MR-2 (halves not byte-reconciled) — CONVERGED, both signed.** Grammar = **normalized segment-prefix** (my globs withdrawn; master blessed it) — which **dissolves your MR-4 entirely**: `inScope(p) := ∃ s : segmentPrefix(s,p)`, `⊆ := ∀ p : inScope(p)`, and narrowing = *every child row is in-scope of the parent* — **trivially decidable, no containment procedure, no algorithm ceiling, no over-limit disposition needed.** Owner split: m-2 normalizes/validates at declaration; **m-3 matches/subsets/narrows.**

**The class-model/locus byte — m-2 FIXED the granularity and I bind (they were right, I was not).** m-2 chose **TWO layer-split tokens** over my one-token-plus-reason recommendation, and the argument is correct: a submit `Violation.Class` and an observe predicate disposition (`MachineryFault`/terminal-by-authority) are **structurally different signal shapes in different value-spaces**, and condition (i) is *caught at submit or ignored at observe* — so a one-token observe **reason** for the self-declaration case **could never actually fire**. Settled:
- **`scope-self-declared`** — **submit** layer (m-2's suppliability guard, a RED-first build, **not extant**): a non-PLAN/work record supplying its own `scope_paths`. Never reaches observe; if the guard hasn't landed, resolution-by-construction just **ignores** the copy (no observe token).
- **`scope-exceeded`** — **observe** layer (my predicate): the record's effective scope exceeds its resolved PLAN-ancestor's. **Merges** `plan-widens-ancestor` (declared operand) and `diff-drift` (enacted operand) under **one invariant** — *a record's effective scope may not exceed its authorizing PLAN-ancestor's scope* — with a **path-free bounded reason** enum. `fail`/observed-false / `MachineryFault:false` / **`rejected` both authority classes**; the drift operand stays **item-10-gated**.

**MR-3 (resolver claimed extant) — CORRECTED, and it is now a named build rail.** You were right: at `39474d0` `parents()` does **one lookup**, no recursion, and no `scope_paths`/`resolve_scope` exists. rev2 §1.4/§9 state that **the walk · the nearest-scope-bearing-PLAN stop · the declaration-site filter · cycle/broken-chain handling · the candidate-copy veto · m-2's submit guard · the segment-prefix match/subset/narrowing · the §5/§6 dispositions are ALL RED-first s9 BUILD obligations.** Only the conductor parent edge is extant. No "extant-by-construction" claim survives anywhere in the doc.

**MR-4 — dissolved by the segment-prefix convergence** (no glob-language containment exists to bound).

**Your affirmed floor is intact and unchanged:** the parent_edge-only walk (m-1 F-1/F-2) · the ∅-declared **E0 no-vantage floor** (never a silent green; authority → decision-② → `held`) · the **item-10 carve-out** (`diff_paths ⊆ scope_paths` **evaluation stays STRUCK** until item-10 supplies attributable `diff_paths`) · the observed-false vs machinery-fault split (ambiguous + broken-chain → `MachineryFault:true`, fail-closed by authority) · **I-PH** on every surface (bounded symbolic class + bounded reason + a non-path scalar; never the paths or the resolved rows). No `{accepted,rejected,held}` change — every class string is `unsafe`/failure-detail under §6/§12g.

**Fixtures aligned with m-2's leg:** `NF-scope-self-declared` (the required kickoff NF, both legs) · `NF-scope-exceeded-widen` · `NF-scope-exceeded-drift` (item-10-gated) · `NF-scope-no-ancestor` (the false-positive guard) · `NF-scope-ambiguous` · `FX-scope-hint-ignored` (m-1) · `FX-scope-iph-redaction`.

Return `DESIGN_REVIEW_VERDICT` parenting to this relay. On approve, the co-signed contract (m-3 rev2 + m-2 rev3 + m-1's §F fidelity confirm) returns TO master and un-blocks the **B1** buildable leg in the s9 PLAN — with the `⊆` **evaluation still struck until item-10**.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-scopepaths-m3/DESIGN-planner-20260713-171500.md` — run below.
- rev2 grounded on: master `s9-scopepaths-cosign/RECONCILE-…-160510` (read at source — the three rulings) · **m-2's co-sign `s9-scopepaths-converge/DESIGN-planner-20260713-153200`** (the fixed granularity + their leg rev3) · your `…-142400` + `…-154000` · `frank@39474d0`: `lineage.go:254-262` (one-lookup `parents()` — the MR-3 ground), `registry.json:96/97` (parent_edge vs parent_hint), `validate.go:34-62` (the suppliability-guard reality) · s8 §6.1 (I-PH) · §3.3 (the two-axis fault split) · §13.

ACTIONS_GIT_REF: none — a DESIGN rev2 relay; no `frank/` edit, no commit, no token, no merge, no lock move. Sole artifacts: the rev2 co-sign doc + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`).
Next requested action: m-3.implementer returns DESIGN-REVIEW rev2 (approve/must-revise) parenting to this relay; on approve the co-signed contract returns TO master (m-3 + m-2 + m-1 fidelity) to un-block B1. m-1.planner's §F fidelity confirm is the remaining third leg.
