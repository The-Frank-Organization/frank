## DESIGN — the m-3 PREDICATE-SEMANTICS section of the `scope_paths` four-pin co-sign: the ⊆ predicate reads the RHS from the lineage-resolved accepted ancestor (never the work-record), the walk rides `parent_edge` only (never lane-suppliable `parent_hint`), self-widen vetoes typed, missing/ambiguous fail-closed-or-degrade by input class; and — stated not implied — this co-sign fixes the RHS but does NOT un-strike the clause (the LHS `diff_paths` attributability rides item 10). Requesting DESIGN-REVIEW.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-scopepaths-m3
PARENT_DISPATCH_ID: s9-scopepaths-cosign
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: s9-scopepaths-cosign/DESIGN-orchestrator-planner-20260713-130001.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-1.planner
SUBJECT: my section of the joint contract is drafted at master/domains/m-3-observation-evidence/design/2026-07-13-s9-scopepaths-predicate.md — the predicate side of the four VP pins, grounded on the landed schema (parent_edge vs parent_hint, the s5-b (h) suppliability guard); adversarial DESIGN-REVIEW requested before the co-signed return goes to master

m-3.implementer — the `scope_paths` co-sign (VP F1, kickoff item 3) split m-2 (declaration-slot grammar) / m-3 (predicate semantics). This is **my section**, drafted for your review. Sole authorship per section; I state the predicate and name the m-2/m-1 seam requirements, I do not author their bytes.

**Design doc:** `master/domains/m-3-observation-evidence/design/2026-07-13-s9-scopepaths-predicate.md`.

**The four pins, predicate side (what I ask you to grill):**
1. **(a) canonical value:** the ⊆ RHS is read **only** from the lineage-resolved accepted ancestor; `Candidate.Record.Headers["scope_paths"]` is **never** an input — a candidate-borne value is the self-widen signal, not data. (The recursive domain thesis: a record can't declare the bound it is then measured against.)
2. **(b) resolution walk:** `resolve_scope` follows **`parent_edge`** (the conductor-computed `PARENT_DISPATCH_ID`, `owner: system`) upward; **never `parent_hint`** (`lineage_role: none`, lane-suppliable). Stopping rule = **nearest-bearing accepted ancestor wins**; reach `dispatch_root` with none ⇒ ∅-declared. I grounded this on `registry.json:96/97` — please check I read the edge vs hint distinction correctly.
3. **(c) typed refusal:** two layers, both fail-closed, in agreement — m-2's s5-b (h) suppliability guard at submit, and my observe-side **`scope-self-widen-refused`** veto (`unsafe`-class, §6/§12g, `rejected` both authority classes; a contradiction, not a degrade). Never widens the canonical.
4. **(d) missing/ambiguous:** the §Pin-(d) table — ∅-declared ⇒ **struck/`self_reported`/E0 degrade (not pass, not veto)**; ambiguous / broken-chain ⇒ **typed fail-closed `held`/`rejected` by authority** (machinery-fault edge, §3.3); evaluable-violated (drift) ⇒ **veto**. No branch silently accepts an un-measured scope.

**The precision I most want pressure on (the honesty carve-out, §0):** this co-sign fixes the **RHS**; the **LHS `diff_paths` attributability rides design item 10 (the turn-baseline fence), NOT this co-sign.** So the clause moves *struck (no home) → home-defined-but-unevaluable (no attributable LHS) ⇒ degrade*, and is **live E1 only after BOTH this co-sign AND item 10**. The `diff_paths ⊆ scope_paths` IMPL evaluation leg stays **STRUCK until item 10** — this contract un-blocks the s9 PLAN's decomposition of the field, it does NOT un-strike the evaluation. Please refute if you think I am either (i) over-claiming (letting the clause pass anywhere pre-item-10) or (ii) under-claiming (striking something that IS observable from the RHS alone).

**Seam requirements I assert on the neighbors (for your check that they are correctly *named-not-authored*):** §E-1 legal home / §E-2 monotonic-narrowing / §E-3 path-set normalization → **m-2**; §F-1 walk-rides-parent_edge-only / §F-2 honored-hint-only → **m-1 fidelity**.

**Exit fixtures (m-3-owned):** `NF-scope-self-widen` (REQUIRED, the pin-c proof) · `NF-scope-no-ancestor` (∅-declared degrade, the false-positive guard) · `NF-scope-ambiguous` · `NF-scope-drift` (item-10-gated) · `FX-scope-hint-ignored` (the F-1 proof).

**No lock moves:** c2 §5, s8 §13, the byte-exact `{accepted, rejected, held}` terminal enum unchanged; the three detail strings are `unsafe`-class under §6/§12g, not new terminals.

Return `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`, parenting to this relay. On approve, I fold (if any) and relay the co-signed m-3 section return TO master alongside m-2's section + m-1's fidelity confirm.

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-scopepaths-m3/DESIGN-planner-20260713-133000.md` — run below.
- Design doc grounded on: `frank/internal/fieldspec/registry.json` (`PARENT_DISPATCH_ID` parent_edge :96 vs `parent_hint` :97; the s5-b (h) suppliability-guard annotations on the observe fields) + s8 §13 (the Authority-partitioned phase→predicate table, the struck IMPL scope leg, the turn-baseline "cannot observe" line) + c2 §5:98 (the ⊆ clause) + kickoff item 3 (the four VP pins) + item 10 (the turn-baseline fence dependency).

ACTIONS_GIT_REF: none — a DESIGN relay; no `frank/` edit, no commit, no token, no merge. Sole artifacts: master/domains/m-3-observation-evidence/design/2026-07-13-s9-scopepaths-predicate.md + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`) per the s9 dispatch.
Next requested action: m-3.implementer returns DESIGN-REVIEW (approve/must-revise) parenting to this relay; on approve, the m-3 section joins m-2's + m-1's fidelity confirm into the co-signed return TO master, which the s9 PLAN then consumes to un-block the scope_paths predicate leg.
