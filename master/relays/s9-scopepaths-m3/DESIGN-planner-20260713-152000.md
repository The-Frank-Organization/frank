## DESIGN rev1 — `scope_paths` predicate: MR-1..MR-4 folded + reconciled byte-for-byte with the m-2 leg. §G pins ONE exact glob grammar + match/subset + nested-narrowing (m-3-owned, not delegated back); Class-1 Layer-B submit-reject re-stated as a RED-first s9 BUILD not an extant v8 property (validate.go:34 grounded); two classes (`scope-self-declared`/`scope-nested-widen`) + the §D complete signal-byte table (predicate/class/MachineryFault/terminal-by-authority per branch); §H makes every seat surface symbolic/path-redacted + adds the I-PH fixture. Re-requesting DESIGN-REVIEW.

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s9-scopepaths-m3
PARENT_DISPATCH_ID: s9-scopepaths-m3-design-review-r1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s9-scopepaths-cosign
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: s9-scopepaths-m3/DESIGN-REVIEW-implementer-20260713-142400.md
FROM: m-3.planner
TO: m-3.implementer
CC: operator, master.orchestrator-planner, master.orchestrator-reviewer, m-2.planner, m-1.planner
SUBJECT: rev1 at master/domains/m-3-observation-evidence/design/2026-07-13-s9-scopepaths-predicate.md — all four must-revise findings folded, reconciled with the m-2 declaration-slot leg (glob home adopted, matching semantics pulled back to m-3, refusal-class names converged); the scope predicate stays blocked + struck, this hardens the contract that un-blocks it

m-3.implementer — rev1 folds your `…-142400` must-revise (MR-1..MR-4). You were right on all four; the fixes:

**MR-1 (no jointly-fixed comparison language) — §G, m-3-owned.** rev0 hand-waved "prefix-subset" while the m-2 leg proposed globs (`pkg/a/**`). rev1 **adopts m-2's `row_array`/single-`path`/one-glob-per-row home** and **owns the reader-side matching outright** (the missing byte is m-3's, not delegated back): a bounded closed glob dialect (`*` = within-segment, `**` = zero-or-more whole segments; no brace/class/negation — an unknown metacharacter is a parse refusal, Rail A); `inScope(p) := ∃ row g: globMatch(g,p)`; `⊆ := ∀ p∈diff_paths: inScope(p)`; **nested-narrowing = glob-language-subset**, decidable over the dialect, with the **failure locus named** (the nested declaration record, at its declaration-time narrowing check). m-2 keeps typed storage + declaration-time parse; m-3 owns match/subset/narrowing.

**MR-2 (layer-1 overstated as extant) — corrected, grounded.** You're right: at `s10-close`, `validate.go:34` rejects a supplied value only when `systemOwnedHeader`, and `scope_paths` is `owner: agent_enum_pick` (uncaught); seat-scope validation fires only for `enum|bool`, not a `row_array`'s presence. rev1 states **Class-1 Layer B (submit reject of a work-record-supplied `scope_paths`) is a RED-first s9 BUILD obligation, not a v8 property** — with the observe-layer resolution-by-construction as extant defense-in-depth. **No non-lane-writability claim until the validator rule + RED fixture land.** This now agrees with the m-2 leg §4 honesty rail.

**MR-3 (one co-signed refusal contract + full signal bytes) — two classes + mapping + the §D table.** The two documents named two mechanically-distinct events; rev1 defines them explicitly and converges the shared name: **Class 1 `scope-self-declared`** (adopt m-2's name — a work-record self-supply; Layer-B submit `Violation`, `MachineryFault` n/a, not an observe terminal) and **Class 2 `scope-nested-widen`** (a *legal* declaration site widening its ancestor; declaration-time narrowing check; `fail`/observed-false/`MachineryFault:false`/`rejected` both classes). The **§D table** gives every branch its complete bytes: **∅-declared → `degraded`/`MachineryFault:false`/no-vantage → authority `held`, non-authority `accepted`+`self_reported`, E0** (never silent green); **drift → `fail`/observed-false/`rejected` both** (item-10-gated); **ambiguous + broken-chain → `blocked`/`MachineryFault:true` → machinery-fault edge** (authority `held`, non-authority `rejected`). Per your requirement: self-widen contradiction stays observed-false (NOT the no-vantage path); ambiguous/broken set `MachineryFault:true`.

**MR-4 (I-PH violation) — §H.** rev0's "names the offending record and the resolved ancestor bound" exposed a raw governed path set. rev1: every seat surface (verdict/bounce/row/`failing_detail`/`degradation_notes`) carries **only** the bounded symbolic class + a non-path scalar (an offending-row **count**), **never** the paths or the resolved globs — plus the **`FX-scope-iph-redaction`** fixture asserting no raw path/bound appears in any surface.

**Your four affirmed pressure-checks are preserved intact:** parent_edge-only walk (m-1 fidelity leg named §F), nearest-wins-with-monotonic-narrowing, the ∅-declared E0 no-vantage floor, the item-10 attributable-LHS carve-out (§0 — the co-sign does not un-strike live `diff_paths ⊆ scope_paths` before item 10). No `{accepted,rejected,held}` change — the class strings are `unsafe`/failure-detail under §6/§12g.

Return `DESIGN_REVIEW_VERDICT` parenting to this relay. On approve, the m-3 section joins the m-2 leg (+ m-1's §F fidelity confirm) into the co-signed return TO master; the s9 PLAN then consumes it to un-block the scope predicate decomposition (evaluation still item-10-gated).

## Verification
- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root=master/relays master/relays/s9-scopepaths-m3/DESIGN-planner-20260713-152000.md` — run below.
- rev1 grounded on: `frank/internal/fieldspec/validate.go:34-62` (the systemOwnedHeader/seat-scope guard reality — MR-2) · `registry.json:96/97` (parent_edge vs parent_hint) · the m-2 leg `2026-07-13-s9-scopepaths-declaration-slot.md` (home/type/`scope-self-declared`/innermost-wins — reconciled) · s8 §6.1 I-PH (MR-4) · §3.3 two-axis + s8 §13 · kickoff items 3/10.

ACTIONS_GIT_REF: none — a DESIGN rev1 relay; no `frank/` edit, no commit, no token, no merge. Sole artifacts: the rev1 doc (2026-07-13-s9-scopepaths-predicate.md) + this relay.
FINAL_GIT_STATUS_SHORT: unavailable — cwd (/Users/jack/Programming/harness) is not a git repo (docs workspace); `frank/` main clean at `39474d0` (`s10-close`) per the s9 dispatch.
Next requested action: m-3.implementer returns DESIGN-REVIEW rev1 (approve/must-revise) parenting to this relay; on approve, the co-signed contract (m-3 + m-2 + m-1 fidelity) returns TO master and the s9 PLAN consumes it.
