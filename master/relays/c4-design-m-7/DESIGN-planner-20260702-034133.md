## DESIGN — r4 lock package assembled (CQ gate satisfied); final adversarial pass requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: c4-design-m-7
PARENT_DISPATCH_ID: c4-design-m-7
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: c4-cq-coord/RECONCILE-orchestrator-planner-20260702-032227.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: r4 — CQ closures folded, three CTO integration items applied, DESIGN_LOCK block at §22; your final lock-package pass closes the pair side

Partner — the CQ gate closed (certified `master/relays/c4-cq-coord/RECONCILE-orchestrator-planner-20260702-031533.md`, CQ-6 re-scoped to BASE by `...-032227.md`, VP approve `RECONCILE-orchestrator-reviewer-20260702-032843.md`). I assembled the lock package on that basis. **r4 changes are CQ-closure folds only — no engine mechanism changed from the r3 you approved.** Doc: `master/domains/m-7-conductor-core/design/2026-07-01-v3-conductor-core-design.md`; full r4 fold-log in §21.

What r4 folded:
1. **§15 ledger → CLOSED** with resolutions + closing legs; gate line updated (gate satisfied).
2. **NF-Sx bound to landed outcomes:** NF-S5→CQ-1(a) step-gate; NF-S7→CQ-2 fail-closed `held`; NF-S8→CQ-3 table + `other`→A fail-safe; NF-S16→CQ-4 byte-exact tokens; NF-S15→CQ-4b single-digest composition + per-section stamp (one integrity root); NF-S12→CQ-5 confirmed post-gate/pre-observe/atomic-bind; NF-S1/NF-S2→**CQ-6 BASE only**; §4→CQ-8 (INDEX layout unchanged, m-1-confirmed).
3. **CTO integration items:** (i) m-4 per-section version-stamp inside the single-digest artifact (§7/NF-S15); (ii) byte-exact `{accepted, rejected, held}` — the `bounced` value token retired, swept ("bounce" survives only as the author-return verb; rg-verified no `bounced` token remains); (iii) m-3's exactly-one-outcome framing checked: "candidate-not-delivered + terminal audit record" = the effect description of `rejected` (the audit record IS the rejected record, referencing `intake_id`) — no fourth outcome, no outcome-free exit (§6).
4. **CQ-6 discipline per the VP carry-forward:** NF-S1/S2 bind to the base only; `re-mint-supersedes` is listed in §20 as a §2C away-bridge build-carry with its dormancy residual stated, and is nowhere presented as pair-approved or design-lock-bearing.
5. **§22 DESIGN_LOCK block** assembled: DESIGN_LOCK_ID `c4-design-m-7-lock`, referencing GRILL_LOCK_ID `c4-grill-m-7`, your r3 approve, the CQ certifications, the locked content, the claim boundary, and the build-carries. Effective only on your lock-package approve + the VP co-sign.
6. Status line → DESIGN-LOCK-READY r4 (your r3 metadata nit is thereby also current).

**Your final pass hunt-list:** (a) do the r4 folds faithfully transcribe the certified CQ resolutions (especially CQ-6 base-only and the byte-exact token set)? (b) did any fold smuggle `re-mint-supersedes` back into lock-bearing text? (c) does §22 cite everything the lock needs and grant nothing it shouldn't? (d) any regression against your r1–r3 findings?

DESIGN_REVIEW_VERDICT requested on the r4 lock package: approve | must-revise | reject-narrow | human-decision-required.

Not authorized / not claimed: the §22 lock is ASSEMBLED, not effective — no design-LOCK until your approve + VP co-sign; no PLAN, no IMPL, no code/source/`pcode/`, no spike, no locked-contract reopen.

## Verification

- `python3 /Users/jack/.claude/skills/tools/relay-lint.py --relay-root master/relays master/relays/c4-design-m-7/DESIGN-planner-20260702-034133.md` — run before handoff (result in inline pointer)
- `rg -n "bounced" master/domains/m-7-conductor-core/design/2026-07-01-v3-conductor-core-design.md` — post-fold: zero `bounced` value-token uses (only the retirement notes naming it as retired)
- `git -C pcode status --short` — clean (planner-run this session)

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: edited `master/domains/m-7-conductor-core/design/2026-07-01-v3-conductor-core-design.md` (r4: header, §4, §6, §7, §12 S1/S2/S5/S7/S8/S12/S15/S16, §15, §20, §21, new §22) + created this relay + one appended INDEX.md row timestamped 20260702-034133; no code/source/`pcode/` edits (pcode verified clean)
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C pcode status --short` = clean)
Next requested action: operator relays to m-7.implementer for the final lock-package pass; on approve the package goes to the VP for the design-lock co-sign (clearing re-baseline step (e): Step-1 PLAN becomes operator-openable).
