# s8 CONFIG HOST — the layer-activation knob's member home · the catalog's §7-pinned member with honestly-scoped load claims · the full governed discovery descriptor · the catalog's canonical runtime home · the s8 genesis composition · the §3 step-4.5 amendment

**DESIGN_DOC_ID:** `s8-design-m7-config` · **Status:** r13 — r12 consumed the owner trail; r13 consumes master's `s8-adoption-grammar-m2-ruling-r2` (`SITREP-orchestrator-planner-20260711-143317.md`): m-2's r2 RE-CONFIRM (`…-142347`) replaces `…-135623` as the reconcile input — the LIST-NAME CORRECTION swept (valid adoption list names = `{catalog, engine}` exactly; fieldspec never crosses the set boundary) — and the routed catalog-mutation-path flag is ANSWERED: **(a)**, the singular `config_change` gains `member: catalog`, state-aware, same MAJOR-but-safe class, same s8 registry changeset (check item SIX). For m-7.implementer re-review (supersedes the r12 review target).
**Amends:** `2026-07-01-v3-conductor-core-design.md` (the c4 lock) via the review-driven amendment path — ONE amendment carried: §3 gains step 4.5 (§6 below, pre-confirmed in `s8-grammar-confirm-m7/SITREP-planner-20260710-234643.md`). Everything else composes with the locked record unchanged.
**Boundary contracts consumed, never authored:** `s8-design-m2-grammar` (member grammar / atom semantics / the profile partition / the completeness reason vocabulary; layer presence consumed as an environment fact per §1.3) · the m-1 store/genesis chain semantics as locked.
**GRILL_REQUIRED: yes** — the GRILL_LOCK is §9; one fork is operator-owned and travels as a durable question with a recommended default.

## §1 The layer-activation knob's host

1. **Member home: the ENGINE member** (`engine.json`, beside `gc_enabled`/`segment_rotate_bytes`): `"present_layers": {"observe": false, ...}` — a closed map of layer name → presence. Rationale (one-writer-per-fact): layer presence gates **engine pipeline execution** (whether §3 step 4 runs at all), which is engine-hosted truth; the registry's step-gate predicates *consume* it, they do not own it. A third member was rejected AND re-decided with m-2's version constraint binding (r6/A1): still rejected — the version carrier fits inside the member (below), so fragmentation buys nothing the field does not.
   **(r6/A1) The version carrier — m-2's shape (a):** `EngineConfig` gains `"version": <int>` (initial value 1 at the s8 genesis; the pre-s8 implicit state is version-absent = 0). **Bump semantics, exact:** ANY change to the engine member's schema surface — a new field, a new `present_layers` key class, a type change — increments `version` by exactly 1 in the same `config_change` that carries the change; additive-MINOR is thereby ENCODED (the bump + ignore-unknown compatibility), and a semantic/breaking change is a bump whose new value exceeds old readers' ceilings (§2.4). The knob flip itself (toggling an EXISTING `observe` key `false→true`) is a VALUE change, not a schema change — no bump; adding a NEW layer key IS a schema change — bump. One writer (m-7), one member, one mechanically-readable version.
2. **Activation = an operator-authored §7 `config_change` record, restart-effective** — the locked mechanism exactly (`:109-110`): the compound record embeds the changed member bytes and the recomputed composite digest; no hot reload; phase-0 chain-walks the digest at the next start; direct file replacement stays phase-0 fail-closed. Nothing new is built — this is the s4-built, s6-refined machine carrying one more member field.
3. **The grammar face (boundary contract):** the engine exposes `PresentLayers` to predicate evaluation as a **render/validation environment fact** (the `RenderEnv` idiom `PreActive` already uses) — `s8-design-m2-grammar` owns what predicates may say about it (the CQ-1(a) step-gate atoms); this design guarantees only: the fact is engine-computed from the pinned config, never lane-supplied, identical at render and validate within one config generation.
4. **A-1 semantics, stated:** layer presence changes the effective schema (required sets flip), so it is **digest-visible by design** — a form rendered pre-activation bounces `re-render` post-restart, the one place the stable-schema digest SHOULD bounce (`step2-prep/SITREP-planner-…-010806` intake shape, confirmed). Records accepted pre-activation are never re-validated (immutability; the zero-loss replay leg proves old records still parse). N-foreign-accepts still cause zero digest bounces (the A-1 negative holds — presence is config-generation-scoped, not traffic-scoped).

## §2 The catalog as a §7-pinned member — load claims scoped per F5

1. **The catalog joins the composite as its own member** (`catalog`), authored per the s7 convention (single writer, owner fidelity on change), pinned under the same single top-level digest, changed only by operator `config_change`.
2. **What LOAD enforces (the honest claim, fork (a) default):** (i) the member's bytes match the operator-authorized config history (digest/genesis chain); (ii) **member SHAPE**: well-formed law list — unique law ids, unique test names, non-empty owners/claims, the census sections structurally valid; (iii) **provenance SHAPE**: the version/convention fields present and coherent. **What load does NOT claim: that the named owners reviewed a change.** Owner fidelity remains a relay/design-review gate — the trail witnesses review; the loader witnesses bytes and shape. Every artifact and fixture states this boundary in these words; an unqualified "load-enforced governance" claim is a design defect.
3. **Fork (b)** (owner-attestation verified at load) is NOT designed here — recorded as rejected-by-default with the operator owning the reversal (§9); if ever elected it is a separately-grilled addition (new attestation machinery, its own claim analysis).
4. **(r8/F1–F3) The acceptance-time version-transition gate — per-version SCHEMA DESCRIPTORS; validation, not diffing.** Each member handler carries, for every version it supports, a **canonical schema descriptor**: the member's closed shape at that version — node KINDS at every position (object/array/string/number/bool), closed object key sets where the shape is closed (e.g. `present_layers`' key set is closed per version), array ELEMENT shapes (indices never participate; element count is unconstrained), and no value constraints beyond kind. So a scalar type change (`{"x":1}`→`{"x":"one"}`), a scalar→container change, and an element-shape change all FAIL the descriptor; ordinary value changes (the `observe` flip; adding a catalog row of the declared row shape) all PASS it — the §1.1 schema-vs-value line, mechanically complete.
   **The acceptance validator** (in the serialized loop, before the pivot — the same locus every other gate uses), per member of the candidate `config_change`:
   (a) read the candidate's declared version V — V must be in the handler's supported set/ceiling (a candidate this binary cannot enforce is rejected: **adopt-new-version sequencing = upgrade the reader first, then accept the record**);
   (b) validate the candidate bytes against **schema(V)** — failure ⇒ the typed reject;
   (c) **one-way implications (r8/F2 — no biconditional):** if V == the current pinned version, (b) already proves the schema surface is unchanged — accept (value-only by construction). If V ≠ current, the transition current→V must be in the member's **forward-transition relation (r8/F3)**: engine = the successor function `V == current + 1` exactly; catalog/fieldspec = an **owner-supplied ordered successor map** (m-2 supplies fieldspec's sequence, the catalog convention supplies the catalog's — this design consumes the relation, never invents it). Rollback and skip **between otherwise-supported markers** ⇒ the typed reject (class `config-version-transition`, path-free, D-2 parity).
   - **The lawful residual, named:** a version bump whose new schema equals the old (a semantics-only bump) passes (a)–(c) and is lawful — the explicitly named semantics-only residual; review convention governs its meaning, the machine governs everything else.
   - **Recovery:** replays already-ACCEPTED transitions without re-validation (store-is-truth; the gate runs at acceptance only — no historical record is ever re-judged).
   - **Composition note:** the descriptors double as §2.2's load-SHAPE validators — one artifact per version serves both gates.

5. **(r6/A2, r7/F2-closed) The reader-capability gate — killing the fail-OPEN class mechanically.** First, the reconciliation premise CORRECTED at the code (`config.go:36-49`, `genesis.go:86-91`): `config.Load` hashes only the members its caller SUPPLIES, and `StoreRootConfigPaths` is a hardcoded path map — so a pre-s8 reader (two supplied members) computes a TWO-member composite against an s8 store's THREE-member genesis digest and **fails phase-0 by construction**; the same holds for any future member-SET change in either direction. The digest mechanism therefore already kills the new-member and removed-member fail-open cases. **The genuine residual fail-open class is: same member set, new INTRA-member semantics under ignore-unknown parsing** (an s9 `config_change` writes catalog-v2 or engine-v2 bytes; the chain legitimizes them; an s8 reader parses, silently drops the new fields, enforces old semantics). **The gate (m-2's shape (i), converged with A1): handler-declared supported versions — the CLOSED capability table (r7/F2):**

| member | marker field | form | comparison rule | s8 initial | s8 reader supports | missing / malformed / unknown / rollback / above-support |
|---|---|---|---|---|---|---|
| `engine` | `version` | integer ≥ 1 | ordered numeric ceiling: declared ≤ the handler's max | `1` | max = `1` | typed phase-0 config-load fault |
| `catalog` | `version` | exact string marker | handler-declared EXACT SUPPORTED SET | `"s8-v1"` (the s8 PLAN re-stamps the source artifact's `"s7-v1"` as part of the §7 pinning) | `{"s8-v1"}` | typed phase-0 config-load fault |
| `fieldspec` | the registry version marker | exact string marker | handler-declared EXACT SUPPORTED SET (the successor grammar is m-2's; the reader pins only its set) | `"s7a-fieldspec-v5"` | `{"s7a-fieldspec-v5"}` | typed phase-0 config-load fault |

Named honestly per the common contract **"handler-declared supported versions"**: engine uses an ordered ceiling; catalog/fieldspec use exact sets — no pretend total order over string markers. **Order of checks: the marker is read and validated FIRST, before any other member content is interpreted** — a missing, malformed, unknown, rolled-back, or above-support marker faults phase-0 with zero partial interpretation. Shape (ii)'s handler registry is subsumed: the member set is closed by the hardcoded path map + digest (above), so "a member without a handler" cannot load undetected. **Stated residual:** a PRE-s8 binary carries no capability check; its exposure is the member-set mismatch (which fails it, proven above) — a pre-s8 binary against a hypothetical same-set future store cannot arise (pre-s8 sets are two-member; s8+ sets are three). §2.2's load-SHAPE enforcement stays necessary and orthogonal, exactly as the reconcile noted.

## §3 The governed discovery descriptor — the FULL final s7 mechanism as census content (F5; r2: the CLOSED schema + every current value pinned)

The catalog member carries a `discovery` section. **The closed schema (no field beyond these; unknown fields = a load-shape fault per FX-CFG-3):**

```text
discovery {
  scan {
    root:                  "."                          # the repo root (relative; the law resolves it)
    exclude_dir_prefixes:  ["."]                        # hidden trees (.git, .relays, …)
    exclude_file_suffixes: ["_test.go"]
    include_only_suffixes: [".go"]
  }
  boundary_files: ["cmd/frank-mcp/mcp.go", "internal/channel/server.go"]   # sorted; ToSlash repo-relative
  recognizers {
    channel_context { path_prefix: "internal/channel/",
                      call_selectors: ["write", "writeLocked", "writePush", "writePushes"],
                      ident_calls:    ["writePushes"],
                      conn_receiver_calls: ["Write"] }
    mcp_context     { path_prefix: "cmd/frank-mcp/", call_selectors: ["Encode"] }
    protocol_switch_tag_selector: "Method"
    tree_wide_idioms: ["conn_receiver_write", "method_switch"]             # the s7 S-1 fold: recognized in EVERY scanned file
  }
  site_census: [ …the 17 strings below, verbatim… ]     # sorted lexicographic, LIST-with-duplicates (multiset)
  output_families:        [ …the six families, carve_outs included… ]      # the existing catalog section, relocated under discovery
  sink_patterns:          [ …the six patterns, symbol + expected_sites… ]  # ditto
  canonical_path_families {          # CLOSED rows — {id, relative, forbidden, directory}, the s7 canonicalPathFamily shape exactly
    rows: [                            # SET-LIKE, sorted lexicographically by id (r5 — order is NOT semantic; the law's oracle comparisons sort)
      {id: "binding",            relative: "binding",                        forbidden: "/binding/",                       directory: true},
      {id: "catalog-config",     relative: "config/catalog/catalog.json",    forbidden: "config/catalog/catalog.json",     directory: false},
      {id: "conductor-lock",     relative: "conductor.lock",                 forbidden: "conductor.lock",                  directory: false},
      {id: "engine-config",      relative: "config/engine.json",             forbidden: "config/engine.json",              directory: false},
      {id: "fieldspec-registry", relative: "config/fieldspec/registry.json", forbidden: "config/fieldspec/registry.json",  directory: false},
      {id: "journal",            relative: "journal",                        forbidden: "/journal/",                       directory: true},
      {id: "mailboxes",          relative: "mailboxes",                      forbidden: "/mailboxes/",                     directory: true},
      {id: "outbox",             relative: "outbox",                         forbidden: "/outbox/",                        directory: true},
      {id: "projections",        relative: "projections",                    forbidden: "/projections/",                   directory: true},
      {id: "quarantine",         relative: "quarantine",                     forbidden: "/quarantine/",                    directory: true},
      {id: "records",            relative: "records",                        forbidden: "/records/",                       directory: true},
      {id: "staging",            relative: "staging",                        forbidden: "/staging/",                       directory: true}
    ]
    # derivation rule for the three store-config rows: id = the path's member name kebab-cased with a -config/-registry suffix;
    # forbidden = the relative path verbatim for file rows, the separator-wrapped home for directory rows (the s7 forms exactly).
  }
}
```

**The pinned values (normative; these bytes ARE the genesis descriptor). Provenance rule (r3/F1): every value below is the `main@691d034` shipped census EXCEPT the `catalog-config` row of `canonical_path_families.rows`, which pins the POST-design expected census — the PLAN adds the catalog home to `StoreRootConfigPaths`, so the live oracle returns three config-file rows; pinning only the pre-implementation two would red every conforming implementation:**
- `site_census` (17 entries, sorted, duplicates preserved): `cmd/frank-mcp/mcp.go:MCPServer.Serve->Encode` · `cmd/frank-mcp/mcp.go:MCPServer.handle=>initialize` · `cmd/frank-mcp/mcp.go:MCPServer.handle=>notifications/initialized` · `cmd/frank-mcp/mcp.go:MCPServer.handle=>ping` · `cmd/frank-mcp/mcp.go:MCPServer.handle=>tools/call` · `cmd/frank-mcp/mcp.go:MCPServer.handle=>tools/list` · `internal/channel/server.go:Server.PushTo->writePushes` · `internal/channel/server.go:serverConn.handle->write` · `internal/channel/server.go:serverConn.handle=>tools/call` · `internal/channel/server.go:serverConn.handle=>tools/descriptions` · `internal/channel/server.go:serverConn.handle=>tools/list` · `internal/channel/server.go:serverConn.run->write` (×2 — the multiset carries both) · `internal/channel/server.go:serverConn.write->writeLocked` · `internal/channel/server.go:serverConn.writeLocked->Write` · `internal/channel/server.go:serverConn.writePush->writeLocked` · `internal/channel/server.go:writePushes->writePush`.
- `output_families` (six, in census order): `bounce-reason` · `process-tool-errors` · `tool-descriptions-results` · `rendered-projections` · `delivery-payloads` · `seat-mint-accept-reply` — the last carrying exactly two carve-outs (`fresh credential on operator channel only`, `new seat endpoint on operator channel only`).
- `sink_patterns` (six, symbol → pinned sites): `bounce-format`/`bounce.Format`→16 · `safe-error-valves`/`safeError|scrubError|frameTooLargeError`→9 · `tool-description-and-call`/`toolDescriptions|mcpTools|ToolDescriptions|DescribeTools|Call`→13 · `projection-render`/`DefaultProjectionIntentsStrict|RebuildProjections`→8 · `delivery-intent-and-push`/`IntentMailbox|IntentOutbox|PushTo`→10 · `seat-mint-outcome-extras`/`OutcomeExtras`→11.
- `canonical_path_families`: TWELVE closed rows (eight directory homes + `conductor-lock` + three config files, `catalog-config` included) as in the schema above — ids, relatives, forbidden tokens, and directory flags all pinned; the live derivation via `store.Open`/`ScanQuarantine`/`StoreRootConfigPaths` remains the ORACLE (ten root homes + three config paths on a conforming implementation); the descriptor pins the EXPECTED census — post-design for `catalog-config`, per the provenance rule.

**Normalization + ordering rules (binding; r3/F2 — ordering is PER FIELD, no global rule):** all paths ToSlash, repo-relative; site strings use the s7 forms `file:owner->callee` and `file:owner=>method` exactly. Ordering:
- **sorted lexicographically (set-like):** `boundary_files` · `scan.*` lists · `canonical_path_families.rows` (by `id`) · `recognizers.*` lists; `site_census` is a **sorted multiset** (duplicates preserved after sorting);
- **ORDERED NORMATIVE SEQUENCES (order is semantically pinned — the live law consumes census order):** `output_families` and `sink_patterns` keep exactly the pinned order above; a re-ordering is a census AMENDMENT, not a formatting change.

**The consumption map — which field each law leg reads, and which mismatch class turns red:**
| descriptor field | law leg that consumes it | red class |
|---|---|---|
| `scan.*` | the full-tree walk's roots/filters | a scanned-set change ⇒ site/outside divergence reds below |
| `boundary_files` | the registered-vs-outside partition | recognized capability in an unregistered file ⇒ **outside-boundary red** (before census comparison) |
| `recognizers.*` | site discovery (context-scoped) + the tree-wide idiom pass | an unrecognized new-idiom transport is the stated §7 residual (design-gate class) |
| `site_census` | exact multiset equality vs discovered sites in registered files | any delta ⇒ **census red** |
| `output_families` (+carve-outs) | the capture census + the carve-out strip-then-scan | unregistered family / count≠1 / carve-out misuse ⇒ **family red** |
| `sink_patterns` | the AST sink census | unregistered sink / count mismatch ⇒ **sink red** |
| `canonical_path_families` | the live-root census equality + the forbidden-token corpus | live census ≠ pinned ⇒ **canonical red**; planted family token in any capture ⇒ **leak red** |

The law reads the descriptor from the loaded catalog member; the s7 in-test literals become the genesis descriptor bytes — ONE source of truth after s8. The PLAN maps fields to code mechanically; it invents no name, normalization, or membership.

## §4 The catalog's canonical runtime home + the byte-copy path

1. **Runtime home:** `<root>/config/catalog/catalog.json` — the `StoreRootConfigPaths` family (beside `config/engine.json`, `config/fieldspec/registry.json`); created at genesis, replaced only by §7 `config_change`, phase-0-verified. **This home is the `catalog-config` row of the descriptor's `canonical_path_families.rows` census and contributes its own forbidden token to the leak scan** (the descriptor pins the post-design census; the live `StoreRootConfigPaths` oracle returns all three config-file rows on a conforming implementation).
2. **`test/invariants/catalog.v1.json` is the SOURCE artifact** — the developer-tree original the genesis blesses. **Byte-copy path:** genesis (or the blessing step) copies the source bytes into the runtime home and records the composite digest; thereafter the runtime member is truth for the running store, and the source artifact is truth for the repo battery.
3. **The drift law — ONE oracle, no new record machinery (r2, F2 fold):** the invariant leg asserts BOTH (i) **source-artifact bytes == runtime member bytes** (direct byte comparison, no digest indirection) and (ii) **the full composite digest recomputed over the runtime members == `ExpectedConfigDigest()`** — the genesis `config_digest` advanced through the accepted `config_change` chain (`genesis.go:64-79` + `:125-140`), which is the live mechanism's own expectation and stays correct after any member changes. No per-member digest is recorded in genesis or `config_change` records (rejected: new record machinery for what byte-comparison + the existing composite chain already prove). Repo/runtime divergence is a red check, not a silent fork. On a §7 catalog change, the same change lands in the source artifact in the same reviewed changeset (single-writer convention; owner fidelity by relay per §2).

## §5 The s8 genesis composition (the dogfood spine store)

Genesis initializes the composite from exactly three members and records the composite digest in the genesis record (the recorded m-7 condition, `STEP-2-KICKOFF.md` item 2):
1. **`fieldspec`** = the exact `s7a-fieldspec-v5` bytes — member SHA-256 `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485` (full 64-hex, normative — r2/F3; source: `s7a-merge-gate/MERGE-GATE-orchestrator-planner-20260710-155633.md`);
2. **`engine`** = the current engine member + `present_layers` with **`observe: false` at genesis** — the flip to `true` lands later as an operator `config_change`, making activation the governed, visible act the kickoff demands (and the mechanism's own live proof); a genesis-ON default would mean the flip never exists as a record;
3. **`catalog`** = the blessed catalog member (§4's byte-copy from the source artifact), discovery descriptor included.
An already-initialized store adopts any of these only through an accepted operator `config_change` carrying the recomputed composite digest before restart (the standing genesis condition, verbatim) — **and for the shipped two-member store the FIRST such transition runs through the §5.1 bless mode below (r9/F4), because neither the old reader (no catalog target in `classifyConfigChange`/`configTarget`) nor the full s8 serve path (phase-0 loads three paths against a two-member store) can perform it.**

### §5.1 (r9/F4) The existing-store adoption path — the offline BLESS mode

**The deadlock, on the record:** "reader first, record second" (§2.4) governs VERSION transitions; the two-member→three-member MEMBER-SET adoption is the one bootstrap step where it cannot: the shipped reader cannot accept a catalog `config_change` (`submit.go:340-343`, `genesis.go:211-219` have no catalog arm), and the s8 serve path cannot start against the legacy store (its three supplied paths and expectation don't exist yet). A raw file copy is excluded — it would bypass the sole-governed-writer and config-history claims.

**The mechanism — `frank bless-s8 -root <root>` (an offline mode of the s8 binary; no channels, no serve):**
1. **Authority:** phase −1 (the `conductor.lock` flock — trusted root/serialized authority by construction; the invoker holds root access = the operator, the D5-honest line); the adoption record's provenance is stamped distinctly (`channel: bless`) so history says exactly what happened.
2. **Validate the legacy state:** run phase 0 under the LEGACY expectation — the two-member supplied set, the chain-expected two-member digest (genesis + any accepted `config_change` history), engine version ABSENT read as version 0 (the defined pre-s8 state, §1.1).
3. **Validate the candidates:** the engine member bytes (version 1 + `present_layers`) against schema(engine,1); the catalog member bytes (`"s8-v1"`, the descriptor included) against schema(catalog,"s8-v1"); the load-shape rules of §2.2.
4. **The lawful initial transitions (the validator gains exactly two):** engine `absent(0) → 1` and catalog `absent → "s8-v1"` — lawful ONLY as this adoption act (absent→initial is never a general transition; post-adoption the ordinary forward relations govern).
5. **One governed crash-atomic act — the ADOPTION RECORD (r10/F5: a closed, reviewed EXTENSION of the singular mechanism, not "the ordinary record"):** the shipped `config_change` is strictly singular (one `member` header ∈ m-2's `{fieldspec, engine}`, body = that member's raw bytes, ONE `IntentConfig` — `config_change.go:16-34`, `projections.go:205-208`); the adoption act therefore lands as ONE record of a **new closed variant**:
   - **Header discriminator:** `member: adoption` — **CONFIRMED by m-2** (`s8-adoption-grammar-m2/SITREP-planner-20260711-135623.md`, consumed by master `…-140516`) as an Option-1 OVERLOAD of the existing body-shape discriminator (one axis, no second flag). The owner gate is **SATISFIED**; the follow-up request `…-141455` is append-only history (redundant — m-2 had already made the header correction and left member count to m-7). Three m-2 rulings ride with the token, carried here verbatim (r12/F7):
     - **Version class = MAJOR (record-schema axis), fail-CLOSED, no migrator — MAJOR-but-safe** (m-2 §9 terms): `member` is consumed by an exhaustive hardcoded switch + the fill-gate, so widening it is MAJOR, not additive-MINOR; it is safe because a **pre-adoption reader typed-rejects the adoption record at submit** (`member/enum`) — it can never misapply or silently ignore it; committed history is never reclassified (readers never re-run `classifyConfigChange` on committed records); the transition is **forward-only** and needs **no migrator** (the singular arm + every committed record untouched).
     - **Reserved token (r13: m-2's own list-name correction consumed):** `adoption` marks the record SHAPE and is never a loadable member name — structurally safe (the load-set is the hardcoded `StoreRootConfigPaths` map, not the enum_set) AND interpreter-enforced: **`members[].name ∈ {catalog, engine}` literally** (the adoption carries the two members the bootstrap ADDS/CHANGES; `fieldspec` is already pinned and never crosses the set boundary — m-2's r2 re-confirm correcting their own first ruling); `fieldspec`, `adoption`, or any other name in the list ⇒ typed reject.
     - **Two m-2-owned registry byte-sites**, a review-driven registry amendment riding the s8 registry build (named, not applied here): `"adoption"` into the `config_member` enum_set (`registry.json:85`) AND into `member.seat_scope.operator` (`registry.json:136`) — both required, else the operator's own submission bounces at fill-validation.
   - **Body schema (closed, r11/F6 + r12 item-5):** `{"members": [{"name": "catalog", "bytes_b64": "<base64>"}, {"name": "engine", "bytes_b64": "<base64>"}]}` — **the outer `"adoption"` wrapper is DROPPED** (m-2's noted simplification, my call made visible: the `member: adoption` header IS the discriminator; a wrapper would duplicate it). **Exactly TWO unique entries with the exact name set `{catalog, engine}`, serialized in that order** — and the count policy is a VISIBLE DECISION (r12 item-4, m-2 left it to m-7): a single-member adoption body is a **typed reject toward the singular form** (one member = use the singular record; the adoption variant exists solely for the atomic two-member bootstrap). Member bytes **canonical base64** (byte-exact: the decoded bytes ARE the member file bytes). Missing, duplicate, extra, misordered, malformed-base64, non-canonical-base64, name-sorted-violations (m-2: REQUIRED not conventional — the composite `Digest()` name-sorts) ⇒ typed reject, **before pivot**.
   - **The chain digest stays in the HEADER (r11/F6):** the adoption record carries `new_digest` as the canonical HEADER exactly as every singular `config_change` does — `ExpectedConfigDigest` (`genesis.go:125-139`) remains ONE unchanged interpreter over both variants; the body carries NO digest (no duplication, no equality rule needed). **The acceptance validator recomputes the header pre-pivot** from the legacy fieldspec bytes + the two decoded candidate members and rejects a mismatch — the header is verified truth at acceptance, not trusted input.
   - **The interpreter (m-7's, one additive arm):** `ConfigChangeIntentsStrict` on `member == adoption` parses the closed body and emits **exactly one `IntentConfig` per listed member, in the sorted order**; every existing singular record continues to emit exactly one intent through the unchanged arm — **old records replay byte-identically (backward compatibility is a fixture leg, not a hope)**.
   - **One rename pivot** as before; the member FILES are derived artifacts completed from the record (the s4-established pattern; `config-change` is already an F11 crash class — the adoption variant adds intents to the existing class, not a new class).
   Then exit.
   **5a. Recovery ordering, bound (r10/F5):** after a post-pivot interruption, derived-work recovery **interprets and completes ALL member projections of the adoption record BEFORE the full s8 phase-0 `config.Load` reads the member files** — matching the shipped singular behavior generalized to N intents; and a **bless re-run detects the committed adoption record in the chain and recovers/validates the THREE-member state** (complete projections → validate → exit "already adopted"), never re-entering legacy validation over an adopted store.
6. **Crash-atomicity — the one-pivot/derived-artifact MECHANICS are inherited; the multi-member record variant + interpreter arm are the explicit reviewed extension above (the honest boundary, r10):** crash PRE-pivot ⇒ the store remains the complete legacy state (bootable under the legacy expectation; re-run bless); crash POST-pivot pre-materialization ⇒ the record is canonical and recovery's existing derived-work completion materializes both member files (bless re-run or the s8 serve path's normal recovery — the machinery is shared); no intermediate unbootable state exists because the pivot IS the state change.
7. **After bless:** the full s8 serve path starts normally — three members, the three-member chain-expected digest, the capability table, all §2 gates live.
**Sequencing sweep (binding):** §2.4's "reader first, record second" + this §5.1 as the named single bootstrap exception; §4's "blessing" wording refers to THIS mechanism for existing stores (genesis handles fresh stores); §5's "only through an accepted config_change" holds verbatim — bless produces exactly that record.

### §5.2 (r13, check item SIX) The post-adoption catalog mutation path — ruling (a)

Post-adoption the pinned set is `{fieldspec, engine, catalog}`, but the shipped singular switch accepts only `fieldspec|engine` — the catalog would have NO day-2 mutation path, and its lifecycle is load-bearing (the §4 drift oracle presumes updates; m-2's `change_convention` governs law changes; the operator's F5-(a) record pins "reviewed changesets + an operator §7 apply"). **Ruling: (a) — the singular `config_change` gains `member: catalog`** (master's recommendation, concurred on the merits: any separate mechanism (b) would be a second change path outside §7 that must either bypass or reimplement the composite-digest rotation — the duplicate-axis smell, twice). Composition, explicit:
1. **Same class, same moment:** the `catalog` token is the SAME MAJOR-but-safe exhaustive-switch widening as `adoption` and lands in the SAME s8 registry changeset (both tokens at `registry.json:85` + `:136`) — one MAJOR moment for the record schema, not two.
2. **State-aware, not merely code-aware:** the singular `member: catalog` arm is valid ONLY where the catalog is already in the pinned set (post-adoption). On a pre-adoption store it is a **typed reject** via the §5.1 rule (absent→initial transitions exist ONLY as the adoption act) — the arm checks the pinned state, not just the enum.
3. **The ordinary gates apply whole:** a singular catalog change uses the ordinary singular body (raw member bytes), runs the §2.4 acceptance validator (schema(V), the forward relation over the catalog's marker set), recomputes the THREE-member header digest, **emits exactly ONE catalog `IntentConfig`**, and is **restart-effective** like every §7 change — nothing bespoke; the §2 owner-fidelity/change-convention review gate and the §4 source-artifact drift rule stand over it unchanged.
FX-CFG-15: a pre-adoption singular `member: catalog` submission ⇒ the typed reject (it can never substitute for the atomic absent→initial transition); a post-adoption singular catalog change accepts ⇒ exactly ONE catalog `IntentConfig` ⇒ the derived member file materializes ⇒ `ExpectedConfigDigest()` advances through the unchanged header path ⇒ restart/load passes every capability check.

## §6 The §3 amendment: step 4.5 — profile-aware completeness (pre-confirmed placement)

**Amendment text (to the locked §3 enumeration):** after step 4 (observe) and before step 5 (atomic commit), the loop runs **step 4.5 — profile-aware completeness**: the single validation stage over the fully-assembled in-courier record (form + lineage outputs + step-3 classification + step-4 observe outputs) that checks the record's producer profile carries its complete field set (the m-2 r5 per-producer partition). Binding constraints (from the placement confirm, `s8-grammar-confirm-m7/SITREP-planner-20260710-234643.md`):
1. pure + deterministic over the assembled record + pinned config (no I/O, no vantage, no clock) — recovery re-execution byte-identical (the §4 locus principle);
2. failure = ONE terminal typed reject through the same single-pivot path (§3 step 6 mechanics extended to "gate failure at 2, 4, or 4.5"); bounce names the failing field, path-free per I-PH; the 1:1 intake↔outcome law unchanged;
3. read-only over the step-3 classification and step-4 outputs — the CQ-5 atomic-bind untouched;
4. profile determination is m-2 grammar; the engine owns only order, purity, and disposition.
`authority_class` placement rides the same confirm: computed at the step-3 classify point, engine-owned, non-lane-writable at the tool surface (D5 residual stated), consumed by the step-4/6 disposition.

## §7 Claim ceilings (standing, restated where they bind)

Tool-mediated confusion-resistance with the D5 same-uid residual stated beside every exclusivity-shaped claim (§1.3's "never lane-supplied", §2.2's load claims, the I-PH legs). I-PH: no canonical store/config/outbox/operator-channel path in any seat-delivered surface — the catalog runtime home is config-family censused; the descriptor itself is config content and never seat-delivered.

## §8 Fixture obligations (named; the s8 PLAN maps them to tests)

- FX-CFG-1: knob flip end-to-end — genesis `observe:false` → operator `config_change` (new member bytes + recomputed digest) → restart → phase-0 passes → post-restart render requires observe-owned fields; pre-activation rendered form bounces `re-render`; pre-activation accepted records replay untouched.
- FX-CFG-2: the A-1 negative preserved — N foreign accepts across the flip's config generation cause zero digest bounces within a generation.
- FX-CFG-3: catalog load-shape enforcement — a malformed member (duplicate law id / empty owners / broken census section) refuses load at phase 0, typed config-load fault, no seat-visible surface.
- FX-CFG-4: the load-claim boundary stated-not-overclaimed — the fixture's own assertion text carries the "shape, not review" wording (the executable-claims discipline).
- FX-CFG-5: the drift law per the §4.3 oracle — (i) source-artifact bytes == runtime member bytes AND (ii) recomputed composite == `ExpectedConfigDigest()` (genesis + accepted config-change chain); either divergence red; a post-`config_change` state passes (ii) by construction (the chain advances the expectation). Plus (r3/F1) the canonical leg: the live `StoreRootConfigPaths` census == the three `directory: false` config-file rows of `canonical_path_families.rows` exactly; and (r4/F1) the **named `catalog-config` planted-token leg** — a planted `config/catalog/catalog.json` token in any captured family turns the leak scan red, per-family like the other eleven rows.
- FX-CFG-6: step 4.5 — complete-profile accept · incomplete-profile ONE terminal typed reject (no partial append; intake↔outcome 1:1 held) · recovery replay byte-identical through the stage · the reject bounce path-free.
- FX-CFG-7: genesis composition — the three members present; the genesis-recorded composite digest == `config.Load(...).Digest` over them; the `fieldspec` member bytes hash to exactly `1ef6abab4d496b11017f57ca400e8296d63824994ffce8311e4533f70cc92485` (the full-hex literal in the fixture); `engine.version == 1` at genesis.
- FX-CFG-8 (r6/A2): the version-ceiling gate — a member declaring a version above the reader's ceiling (engine.version bumped past support; a catalog version marker beyond the handler's set) ⇒ typed config-load fault at phase 0, no partial enforcement, no seat-visible surface.
- FX-CFG-9 (r6/A2, the corrected-premise proof): a two-member reader expectation against a three-member store (the pre-s8-reader shape, simulated by supplying only engine+fieldspec paths to `config.Load` against the s8 genesis digest) ⇒ composite mismatch ⇒ phase-0 fail-closed — the member-SET fail-open case is dead by the existing mechanism, executable.
- FX-CFG-10 (r8, rewritten — the ACCEPTANCE gate over schema descriptors, executable): (a) same-version engine member with an ADDED key ⇒ typed `config-version-transition` reject, no pivot; (b) same-version **scalar TYPE change** at an existing path ⇒ the same reject (fails schema(V)); (c) same-version **container/element-shape change** ⇒ the same reject; (d) a genuine value-only change at the same version (the `observe` flip; a new catalog row of the declared row shape) ⇒ accepts; (e) engine rollback and skip (current−1 / current+2) ⇒ the reject; (f) a lawful transition (new schema surface + the exact successor + candidate validates against schema(V)) accepts and commits; (g) recovery replays the accepted transition without re-validation; (h) a semantics-only bump (new version, schema unchanged) accepts — the named residual; (i) **the simulated multi-marker handler (r8/F3)**: a handler supporting {vA, vB} with the forward relation vA→vB — a rollback vB→vA ⇒ the typed reject, and with {vA, vB, vC} a skip vA→vC ⇒ the typed reject.
- FX-CFG-11 (r7/F2): the capability-table faults — a missing marker, a malformed marker, an unknown catalog/fieldspec marker, and an above-ceiling engine version each ⇒ typed phase-0 config-load fault BEFORE partial interpretation (asserted by proving no other member content was consumed), no seat-visible surface.
- FX-CFG-12 (r9/F4 + r10/F5, the adoption fixture): start from a two-member store of the shipped `main@691d034` shape → run the bless mode → restart the full s8 reader → the three-member chain digest and every capability check pass; the interruption legs: SIGKILL pre-pivot ⇒ the complete legacy state (re-run bless succeeds); SIGKILL post-pivot pre-materialization ⇒ recovery completes both member files from the record; **(r10) SIGKILL BETWEEN the first and second member projection ⇒ restart converges (both files complete) BEFORE any capability check runs**; a bless re-run over the adopted store detects the committed adoption record and exits "already adopted"; no leg leaves an unbootable state; the `channel: bless` provenance asserted in the committed record.
- FX-CFG-14 (r12/F7 + r13, the version-class legs): a PRE-adoption reader (the shipped exhaustive switch) receiving a `member: adoption` submission ⇒ the typed `member/enum` reject at submit — it can NEVER silently accept or misapply (the fail-CLOSED proof, executable); the singular committed-record replay leg doubles as the no-migrator/backward-history proof; a `members[].name` of literal `adoption` OR `fieldspec` (the r13 list-name correction) ⇒ typed reject (the reserved-token/set-boundary legs); a single-member adoption body ⇒ typed reject toward the singular form (the count-policy leg).
- FX-CFG-13 (r10/F5 + r11/F6, the record-contract legs): the adoption record's decoded `bytes_b64` == the materialized member files byte-exactly; the exact-set/order/cardinality rejects — missing member (`{catalog}` or `{engine}` alone), duplicate member, extra member, misordered pair, unknown name, malformed base64, non-canonical base64 — each a typed reject, no pivot; the **header-digest-mismatch reject** (a `new_digest` header not matching the recomputation over legacy fieldspec + decoded candidates); a pre-adoption SINGULAR `config_change` replays byte-identically through the unchanged interpreter arm; and the **chain-walk assertion**: the accepted adoption record advances `ExpectedConfigDigest()` through the UNCHANGED `new_digest` header path (one interpreter, both variants, executable).

## §9 GRILL_LOCK

```text
GRILL_LOCK_ID: s8-config-grill-r1
GRILL_REQUIRED: yes
GRILL_SOURCE:
- plan/design/audit relay read: the s8-design-m7-config dispatch; the VP F5 conditions (step2-prep/RECONCILE-…-201733 chain); STEP-2-KICKOFF items 1/2/2b; my step2-prep intake (SITREP-…-010806); the s8-grammar-confirm (SITREP-…-234643)
- code/docs inspected: locked §7 (:109-111), §3 (:69-74); frank internal/config/config.go (engine member shape), internal/store/genesis.go:52-90 (StoreRootConfigPaths, digest check); the s7 catalog + discovery mechanism at s7-close; the s4 config_change build
- questions answered from codebase: member home (engine member — one-writer-per-fact vs the pipeline gate); restart-effective + A-1 semantics (locked §7 + A-1 verbatim); catalog runtime home (the StoreRootConfigPaths family); descriptor content (the final s7 mechanism enumerated from the shipped tests); drift law shape; genesis composition (the standing recorded condition + the s7a member SHA)
- questions asked operator: the F5 (a)/(b) fork — posed 2026-07-11, no response in-session

Resolved decisions:
- knob member home = engine member `present_layers` — engine owns pipeline presence; grammar consumes an env fact — source: code+locked record
- (r6/A1) engine member gains `version` int, bump-on-schema-surface-change, initial 1 — m-2's shape (a); the third-member alternative re-decided and re-rejected with the constraint binding — source: the reconcile A1 + m-2 rev1 blocker 3a
- (r6/A2) reader-capability gate = handler-declared supported versions (typed phase-0 fault on excess); member-SET drift already fail-closed by hardcoded supplied-paths + composite digest (premise corrected at config.go:36-49/genesis.go:86-91); residual = none for pre-s8 binaries (set mismatch fails them) — source: the reconcile A2 + m-2 rev2 blocker 2 + the code
- (r10/F5) the adoption record = the `member: adoption` closed variant (one additive interpreter arm emitting one intent per member; singular records untouched); recovery ordering bound (all member projections complete before phase-0 load; bless re-run detects adoption); the m-2 grammar delta routed for explicit confirmation as `s8-adoption-grammar-m2`; the honest boundary swept — mechanics inherited, the variant an explicit reviewed extension — source: review r9-F5
- (r11/F6) `new_digest` = the canonical HEADER on both variants (ExpectedConfigDigest unchanged, one chain interpreter); the header recomputed and verified at acceptance pre-pivot; the body closed to exactly `{catalog, engine}` unique in that order, canonical base64 — source: review r10-F6
- (r13) m-2's r2 RE-CONFIRM (`…-142347`, via master `…-143317`) replaces `…-135623` as the reconcile input: the list-name correction swept (adoption list names = {catalog, engine} exactly; fieldspec never in the list); the routed catalog-mutation flag answered = (a) singular `member: catalog`, state-aware (pre-adoption ⇒ typed reject), same MAJOR moment, same registry changeset; FX-CFG-15 added — source: master's ruling-r2 relay, recommendation concurred on the merits
- (r12/F7) the owner gate SATISFIED and consumed directly (m-2 `…-135623` + master `…-140516`; my `…-141455` = redundant append-only history): the MAJOR-but-safe classification (record-schema MAJOR, fail-CLOSED, forward-only, no migrator — pre-adoption readers typed-reject at submit, history never reclassified); the reserved-token constraint; both m-2 registry byte-sites (`registry.json:85` enum_set + `:136` seat_scope.operator) as the scheduled m-2 amendment; the count policy visible (single-member ⇒ typed reject toward singular); the wrapper dropped — source: review r11-F7 + the consumed owner trail
- (r9/F4) existing-store adoption = the offline bless mode: legacy-expectation phase 0 (engine-absent = version 0) · candidate validation against the initial descriptors · the two lawful absent→initial transitions (adoption-only) · ONE compound config_change under the flock with derived-member completion (the s4 idiom; crash-atomic by the pivot) · distinct bless provenance; the deadlock cited at submit.go:340-343 / genesis.go:211-219 / config.go:43-49 — source: review r8-F4
- (r7/F1, r8-replaced) the acceptance-time transition gate: per-version SCHEMA DESCRIPTORS (node kinds, closed key sets, element shapes, index/value-insensitive) — validation not diffing; one-way implications (unchanged version ⇒ validates against current schema; changed surface ⇒ lawful forward transition); engine successor = exactly +1; string markers = owner-supplied forward-transition relations with rollback/skip-between-supported typed rejects; adopt-new-version sequencing = reader first, record second; semantics-only bumps = the named lawful residual; recovery replays accepted transitions only; the descriptors double as the §2.2 load-shape validators — source: reviews r6-F1 + r7-F1/F2/F3
- (r7/F2) the closed capability table: engine numeric ceiling · catalog/fieldspec exact supported sets · marker-first check order · fault-before-partial-interpretation · the s8 initial values pinned (1 / "s8-v1" / "s7a-fieldspec-v5") — source: review r6-F2
- activation mechanism = §7 config_change, restart-effective, phase-0 fail-closed — source: locked §7 verbatim
- A-1: layer presence is digest-visible BY DESIGN; pre-activation forms bounce re-render; the F11-negative holds within a generation — source: locked A-1 + the intake shape
- catalog = its own composite member; load enforces digest + member/provenance SHAPE only (fork (a) default) — source: VP F5 + the staged-truth discipline
- discovery descriptor = a catalog section carrying scan roots/exclusions · boundary files · recognizers + portable idioms · the three censuses — source: the shipped s7 mechanism (F5's "full, not summary")
- runtime home = <root>/config/catalog/catalog.json; source artifact blessed by byte-copy at genesis; drift = a red law — source: StoreRootConfigPaths pattern
- genesis = {s7a-fieldspec-v5 (SHA recorded), engine+present_layers{observe:false}, catalog}; composite digest recorded; adoption post-genesis only via config_change — source: the recorded m-7 genesis condition
- §3 step 4.5 amendment carried here with the four confirmed constraints — source: my placement confirm (lock-discipline note discharged)

Rejected alternatives:
- a third member for the knob — fragments the composite for no isolation gain
- hot/live activation — locked out (§7 no-hot-reload; mid-flight required-set mutation under live forms)
- genesis observe:true — erases the governed flip the knob exists to create
- owner-attestation at load (fork b) — rejected AS DEFAULT: duplicates what the relay trail witnesses; F5 concedes load cannot prove review; operator may reverse (below)
- descriptor outside the catalog member — splits the law from its census; two digests for one mechanism

Still operator-owned:
- the F5 (a)/(b) fork — **RESOLVED (r7): the operator selected fork (a)** (`step2-plan`-lane record `SITREP-orchestrator-planner-20260711-130825.md`); the lean shape-only load claim is now the ratified design, not a default. No open operator question remains on this artifact.

Design-lock impact (r2/F4 — approval and ratification SEPARATED):
- m-7.implementer approve = TECHNICAL soundness only. The reconciled DESIGN_LOCK (s8-design-m7-config-r13 — the lock ID names the revision that RECEIVES the approve, swept each round — referencing GRILL_LOCK_ID s8-config-grill-r1) has its operator conditions SATISFIED (fork (a) selected, `SITREP-…-130825`; the m-2 activation authorization ratified, `SITREP-…-025643`) and its CURRENT owner/master records = m-2's r2 re-confirm `s8-adoption-grammar-m2/SITREP-planner-20260711-142347.md` + master's ruling-r2 `SITREP-orchestrator-planner-20260711-143317.md` (the `…-135623`/`…-140516` pair retained as superseded history) — effectiveness now waits only on master's reconcile completion (the six-item checklist) over the approved revision.
- the §3 step-4.5 amendment enters the locked record through this doc's approved+ratified lock (the c6/s6 amendment path); the s8 PLAN consumes only the master-reconciled, operator-ratified locks (with s8-design-m2-grammar)
```
