# m-3 stage-6 lane-2 delta — the exhaustive cut-matrix · the five typed E3 verdict machines · the §B sink record · the `logical_surface_digest` recipe-binding (r24)

**DESIGN_DOC_ID:** `step3-relock-dag-m3`
**Owner:** m-3 — sole author; m-3.implementer pair-reviews the final bytes.
**Dispatch:** `step3-relock-dag-m3/DESIGN-orchestrator-planner-20260722-234501` (the unblock: schema v2 RATIFIED, settled producer bases handed over).
**Governing bytes (verified on disk this session):** ratified schema-version amendment rev3 `9e874df8…` binding my contract **`6e2abe40be7a6031163348d47e6b9c13990c5599eb877d30ef196b59efbf3e1f`** · frozen m-3 r4 **`009df6070c35f6c2f817db182c8dc1d41c670fa88eb667b8a9f79ea785482bad`** (v1 BYTE-FROZEN) · settled **m-8 r5 `c0b7b48846b05091af1c814481bc0e4449cf455f8c3e616ee5f9634b1e04be21`** · settled **m-9 r5 `c0ff74f5…`** (both pair-approved).
**Status:** r24 — M3-SETTLE-R23-F1 folded from DESIGN-REVIEW `…-093000`. **The r22/r23 over-claim's last residue: I STATED the backstop as an OR predicate and EXECUTED a narrower AND, then reported no watermark — stated-broad, checked-narrow, unbounded.** §0b step 1b said select iff `m-3-addressed OR subject-cites-hash`, but my r23 execution scanned for relays "touching m-3 **+** a bound hash" (an AND that misses either OR class), and named no lower bound, so the zero-result was unreproducible (the relay root holds historical non-derived m-3-addressed relays only the watermark excludes). Fixed: **one exact predicate** — `non-derived AND newer-than-<watermark> AND (m-3-addressed OR subject-cites-any-bound-hash)` — used identically in the rule and the evidence; the **watermark stated as `20260724-170000`** (earliest current-binding approval); and the proof records predicate + watermark + candidate **count (0)** + dispositions. Re-executed with the true OR + watermark: **0 candidates.** **R1 (r17)/R2/R3/N910/mirror/nine-input reconciliation all unchanged.** **Not the complete lane deliverable; m-3 remains the sole §D-join leg.**

*(prior)* r10 — M3-L2-R9-F1/F2 folded from DESIGN-REVIEW `…-001500`. **F1: I wrote the §0b freshness procedure in r9 and did not execute it on the very fold that introduced it** — four relays (the VP's F73 classification, master's governing record, m-10's stand-down, and m-9's proposed successor) all landed *before* my r9 handoff, and §0a therefore stated a classification, an operator-gate possibility, a current revision, and a successor-existence claim that the durable trail had already settled. A stated principle is not a check: §0b is now an **enumerated, recorded, re-run-before-handoff sweep**, and B1's authority wording is corrected to the VP's mandated form. **F2:** mutable current status is removed from §4/§5 entirely — those sections now state **timeless dependency semantics** and cite §0a — and verification is **delimiter-scoped over the whole mutable vocabulary** instead of phrases I had already repaired. **Nothing bound. Not the complete lane deliverable.**
**Supersedes:** r0 `dc3b6eb3…` (its R0-F1..F4 are closed here or by the ratified contract).
<!-- SELFDESC-END — above this line is THIS document's own revision identity (licensed self-description, not producer state); §0a below is the sole home of mutable PRODUCER state. Verification excludes both by delimiter. -->

---

## §0a — THE PRODUCER LEDGER (the single source of truth; every live section cites this, none restates it)
<!-- LEDGER-START — the ONLY licensed home of mutable current-chain state. Verification excludes this block mechanically, not by hand-picked phrases. -->
Synchronized by the §0b sweep at fold time. **Appearing in this ledger grants no binding.** A row records what the trail says; m-3 binds a producer's bytes **only** when they are pair-approved **and** I have reproduced them on disk myself — the `BOUND` marker on a row means that test was met and names the evidence, never that the ledger authorized it.

**Table-shape assertion (R11-F1):** every row of this table carries exactly the header's column count. A row that silently grew extra cells is how this ledger came to assert `BOUND` and `NOT BINDABLE` at once; the shape is now checked mechanically at every fold, not eyeballed.
| producer | current bytes | state | consequence for m-3 |
|---|---|---|---|
| **m-9** (`logical_surface_digest` + the three outer members) | **r17 `01b885feff23005d611b10a1b243bf3c0b878493d7274da75d7c7093d05f221b`** — **BOUND** *(REBASED from r12 `04422965…` through the §D-settlement folds r13–r16; chain r5–r16 superseded)* | **PAIR-APPROVED BYTE-BOUND, zero findings surviving** — approval `step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260726-013000` (`f31b4099…`, `approve` over `DESIGN_DOC_SHA256: 01b885fe…`), artifact re-hashed byte-identical on disk by me; m-9's §D-join re-tender `…-015500` requests this rebase and asserts the §6 recipe locus is byte-unchanged across the chain. **The binding rests on my DIRECT semantic re-check, not on that assertion and not on any byte-stability claim I cannot prove (r12's bytes are superseded/off-disk, R20-F3): I re-read r17's §6 in full and re-ran my consumer check on r17's own bytes** — the five-member recipe is observer-executable exactly as my binding requires, and the r17 pair review preserves the §5-E surface. I make **no** claim that any specific line proves §2.6/`relay.*`/S-4 left §6's bytes untouched; I bind on the current r17 semantics being correct, which they are. **Any edit, including metadata, voids the approval and this binding with it.** **Authority context (governing, unchanged):** the VP classified the §5-E operand recipes A3/B1 as **delegated-recipe design under F73** (`…-213000`, `4c254307…`) — the ratified five-member set, formula, ownership, carriage, join and acceptance property all unchanged, so **no new operator gate**; master recorded it as governing (`…-220000`, `3a440c11…`). B1's authority wording is the VP's mandated form: **"`policy_messages` is the Step-3 constant `[]` under the pair-reviewed m-9 §5-E realization; the member and the outer digest formula are operator-ratified"** — expressly **not** "ratified constant". **m-3's INDEPENDENT consumer check, RE-PERFORMED on the r17 bytes themselves** (§6 read in full at the rebase, not inherited from m-9's byte-unchanged claim): `instructions` = the verbatim static `m8.llm_request.v1.instructions` string, objective/per-task slice excluded · `policy_messages` = the declared constant `[]`, emitted by recipe and never by semantic judgement · `compaction_template` = a versioned-sentinel **byte-prefix scan** of `input[]` (exactly one ⇒ suffix · none ⇒ `""` · two or more ⇒ assembly refusal, so no attempt identity exists to observe), which **never needs to know the attempt kind** · m-2's two arrays folded byte-bound at `c3a8cd61…` · the five members **frozen at first assembly**. All three are executable from request bytes with no m-9 and no m-2 code — the standing bar in §4 **[prov (R21-F1/R22-F1 explicit inputs): artifact=`domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md`@`01b885fe…` (immutable hash-verified basis, outside the sweep) · approval=`step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260726-013000` · source=`step3-relock-settlement-amend/DESIGN-planner-m9-20260726-024000`]** | **R1 = DISCHARGED AND BOUND at `01b885fe…` (rebased from r12).** The `logical_surface_digest` **recipe** binds (§4). **This binds NOTHING else:** it does not approve m-10, does not settle the §D join, does not close R2, and licenses no downstream gate |
| **m-8** (the decoded 2a/2b discriminator) | **r7 addendum `734e44b7841754dfca56f3a9195695bed1d2f07b42d3acae92ef6b188b47fb53`** — **BOUND** *(bounded additive over integrated r5 `c0b7b488…`; frozen r12 `4b670a79…` unmoved)* | **PAIR-APPROVED BYTE-BOUND** (`step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260724-174500`, `approve` over `734e44b7…`), reproduced byte-identical on disk by me. Publishes the required positive field **`refusal_stage ∈ {pre_freeze, post_freeze}`** on every `m8.dataP_reply.v2` reject, with the checkable invariant **`refusal_stage = post_freeze ⟺ B∧E present`**, emitted **independently of the digests** — so the 2a/2b cut keys off `refusal_stage`, never off digest presence (the circularity is closed, F84 discipline). Decode: `internal_integrity_fault + pre_freeze` = 2a (no B) · `internal_integrity_fault + post_freeze` = 2b (step-2 authorized B) · row 1 by its distinct reason tokens **[prov: artifact=`domains/m-8-provider-adapters/design/2026-07-22-stage6-BE-digests-addendum.md`@`734e44b7…` (immutable hash-verified basis) · approval=`step3-relock-dag-m8/DESIGN-REVIEW-implementer-20260724-174500` · source=`step3-relock-dag-m8/SITREP-planner-20260724-181500`]** | **R2 = DISCHARGED AND BOUND at `734e44b7…`.** `Π`'s T1/T2 rows are now executable, the `internal_integrity_reject`/`pre_freeze_typed_reject` normalization is retired to a source rule, and the classifier is CLOSED (§3.2a/§3.2b) |
| **m-10** (the B/E carriage row) | **B/E carriage rev3 `cd17db3204286f4a0b165b1e349d75c5cd9320dc793b021e942275427115e35f`** — **BOUND** *(supersedes `436016d8…`; approved against m-8 r5 `c0b7b488…` + m-9 r12 `04422965…`, frozen r40/r10 unmoved)* | **PAIR-APPROVED BYTE-BOUND** (`step3-relock-dag-m10-be-carriage/DESIGN-REVIEW-implementer-20260724-170000`, `approve` over `cd17db32…`), reproduced byte-identical on disk by me. Carries the three producer digests on `provider_attempts` at exact attempt identity (verbatim, never re-derived), the authoritative **`m10_row_state ∈ {present, not_found}`** with digest members conditional on `present` (§5a) — so the sink distinguishes a stale-`attempt_open`-rejected identity from a committed-then-parked row by a STATED fact, not an ambiguous NULL — and confirms the m-9 carrier at r12 §6 with the assembly-time condition closed (FX-BE-8) **[prov: artifact=`domains/m-10-app-control-plane/design/2026-07-23-lane2-be-carriage-row.md`@`cd17db32…` (immutable hash-verified basis) · approval=`step3-relock-dag-m10-be-carriage/DESIGN-REVIEW-implementer-20260724-170000` · source=`step3-relock-dag-m10-be-carriage/SITREP-planner-20260724-173000`]** | **R3 = DISCHARGED AND BOUND at `cd17db32…`.** The sink's `m10_row_state` row-state machine (§3.3 RS0–RS3) and the m-10 column of §3.3 bind. **Consistency ruling (§3.4a):** the m-10 B-presence is validated against `presence_class` (from the reply's `refusal_stage`), NOT self-certified; the r7 `attempt_result.v2` mirror is a NOTED ENHANCEMENT, not a required gap |
<!-- LEDGER-END -->

## §0b — The cross-lane freshness sweep (executable, and re-run immediately before handoff)
**§0a is a cache of an external trail, and a cache can go stale — it does not make divergence "impossible."** What the §0a structure actually guarantees is narrower and worth stating exactly, and **only over Class A** (defined below): **no duplicated current PRODUCER state — hash, revision, verdict, or gate status — anywhere else in this document.** **Class-B consumer status is deliberately NOT covered by that guarantee** — it lives outside §0a by design, in conditional form. The guarantee also says nothing about agreement with the outside world. **R9-F1 proved a stated principle is not a check:** r9 named "re-read the cross-lane relay trail" but enumerated no dispatches and no watermark, and its first execution missed four relays that already existed. The procedure is therefore an **enumerated sweep with a recorded result**, run at every fold **and re-run immediately before handoff** (four of the four missed relays landed inside that window):

1. **DERIVE the sweep set from §0a's EXPLICIT per-row provenance inputs — and state honestly what that derivation does NOT reach (R22-F1).** Each bound §0a row now carries an explicit `[prov: artifact=<file>@<hash> · approval=<relay> · source=<relay>]` triple. The derivation extracts **all three classes from every bound row** and reconciles each: **artifact** resolves to a hash-verified design-doc **file** (an immutable basis, deliberately OUTSIDE the mutable sweep — it is verified by reproducing its hash, not by sweeping a lane); **approval** and **source** must each resolve to a **swept dispatch lane**. A bound row for which any of the three does not resolve is a completeness FAILURE caught before handoff. **What this GUARANTEES (bounded, and stated exactly rather than over-claimed):** completeness over **§0a-RECORDED provenance** — every lane §0a knows a binding depends on is swept. **What it does NOT guarantee — the discovery residual (R22-F1's sharpest point):** §0a is itself a *potentially-stale cache*, so a producer that has MOVED its current-source to a lane §0a has **not yet recorded** is **undiscoverable by a §0a-derived sweep** — the derivation can *validate* recorded paths, it cannot *discover* an omitted one. **I retract the r22 "cannot omit a bound producer's lane by construction" claim: it is false; the mechanism validates supplied paths, it does not detect an unrecorded move.** **The named backstop for that residual (step 1b) — ONE exact predicate, used identically in the rule and the evidence (R23-F1):** at every fold, scan every **dispatch NOT in the derived set** and select a relay iff **`newer-than-<watermark>` AND (`m-3.planner`/`m-3.implementer` in `TO`/`CC` **OR** its `SUBJECT` cites a bound producer hash)`** — a genuine **OR** over the two admission classes (a new lane addressed to m-3 before its subject carries the final hash; a producer-source relay citing the hash without addressing m-3), never an AND that would miss either. **The `<watermark>` is the earliest current-binding approval timestamp** — the point after which any relay could pertain to a live binding — **currently `20260724-170000`** (R3's approval, the earliest of R1 `20260726-013000` · R2 `20260724-174500` · R3 `20260724-170000`); it is stated explicitly so the zero-result is reproducible (the relay root contains historical non-derived m-3-addressed relays that only the watermark excludes). **The proof records the exact predicate, the watermark, the candidate COUNT, and every candidate path + disposition (zero as an explicit count).** A candidate, if found, is added to the derived set and §0a corrected. This is discovery beyond §0a, run precisely because the derivation cannot provide it. **Current derived set (OUTPUT, re-derived each fold; the three §0a source/approval lanes + authoring + producer Master/VP-children):** **`step3-relock-dag-m8` · `step3-relock-dag-m9` · `step3-relock-dag-m10` · `step3-relock-dag-m10-be-carriage` · `step3-relock-dag-m3` · `step3-relock-settlement-amend`**. **Order every lane by the timestamp embedded in the filename, never by filename sort** — `DESIGN-…-231500` sorts *before* `SITREP-…-153000` lexically, so a filename sort silently hides the newest relay.
2. **Record, per dispatch, the latest relevant `INDEX.md` row / relay path actually read**, in the fold's verification block. An unrecorded sweep is not a performed sweep.
3. **Re-hash each producer artifact on disk** *and* confirm its live verdict and gate state from those durable relays — an artifact hash without its verdict is half the state.
4. **Run the three checks defined below, each over its own domain — NOT a single whole-vocabulary scan (R14-F2, correcting R8/R9's framing).** **Check A** over its closed token/structure domain (producer hashes · qualified revisions · §0a table shape), with `<!-- LEDGER-START/END -->`, `<!-- SELFDESC-END -->`, and the fold log excluded **by delimiter**. **Check B** over the marked set (parse · id∈registry · anchor-match · §0a resolution). **The watchlist** as a regression heuristic only. **Class-B marker coverage is `self_reported` and non-exhaustive** — no step of this procedure claims to enumerate all status-bearing prose, because that is not mechanically decidable (R12-F1).

<!-- RULEDEF-START — §0b's normative rule DEFINES the forbidden vocabulary and therefore must quote it; excluded from the vocabulary scan by delimiter, never from the Class-A hash/revision scan. -->
**Scope of the §0a rule — TWO CLASSES, stated honestly because r10's stronger claim was false (R10-F2).** r10's relay said §4/§5 "carry no mutable status at all." They did: live `PENDING`, `NOT CONFIRMED`, `not bindable`, `PARTIAL`, and `I bind none` all survived, and the check missed them because it searched `non-bindable` while the bytes read **`not bindable`** — a hand-picked vocabulary that did not contain the words actually present. The guarantee is therefore split into what is centralized and what is merely *derived*:

- **Class A — PRODUCER state** (a producer's revision number, current hash, live verdict, gate/classification status): lives **ONLY in §0a**. This is the centralized invariant, and it is the one the mechanical check enforces.
- **Class B — m-3's OWN derived consumer status** (which of my sections bind, what I have confirmed, what I refuse to bind): **NOT claimed absent, and not centralizable** — a design record must be able to state its own completeness. The rule on Class B is weaker and exact: each such statement must be **conditional** — it names the condition under which it changes and cites §0a for the current evaluation — so it is a *consequence* of the ledger rather than a second copy of it. **Class B is outside Check A** (a Class-B statement is never a Class-A violation) **and inside Check B** (the marker inventory). The two were previously conflated as "the mechanical check" — a real ambiguity, since one decides violations and the other only well-formedness.

Two further categories are licensed by delimiter: stable non-mutating basis hashes (the governing-basis line and the m-2 F73 row), and **this document's own revision identity and supersession history** (the header block above `SELFDESC-END`, and the fold log). **Licensing self-description does not license *stale* self-description (R10-F3)** — the title said `(r9)` over r10 bytes; an excluded header must be internally exact before the exclusion is honest.

**THREE checks, each claiming only what it can decide (R12-F1).** My previous wording called all of this "the mechanical check" and asserted it *enumerates the status vocabulary actually present*. **It cannot, and no normalization fixes that.** Case- and separator-folding cured the r10/r11 spelling defects, but a folded list is still a **list I chose**; deciding which arbitrary prose words carry semantic status is not a mechanical operation at all. Live status language sits outside any list I would write — `BINDS` / `does not` / `THE CONDITION IS NOW MET` / `settled` / `observer-executable` / `CONFIRM` / `incomplete` / `NOT total` / `not executable` / `not complete` — most of it legitimate Class B. **Claiming enumeration over free prose was claiming a vantage the mechanism does not have**, which is the exact failure this domain exists to catch, committed in my own verification contract. So the checks are now named separately, and each states its own limit:

| check | domain | decides | does NOT establish |
|---|---|---|---|
| **Check A — Class-A violation scan** | producer hashes · qualified producer revisions `m-<n> r<N>` · §0a **table shape** (every row = the header's column count) | **complete over its domain**: a hit is a violation, and the domain is a closed set of exact tokens plus a structural invariant | anything about prose |
| **Check B — Class-B inventory** | every statement carrying a marker of the exact form **`[CB cond=<ID> ledger=§0a:<Rn>]`** | **complete over the MARKED set**: every marker **parses**, its `<ID>` is a member of the closed condition-id registry (RULEDEF), and its `ledger` reference equals that id's declared §0a anchor — a **decidable** check on both fields, not a prose reading | **that marking was exhaustive** — see the residual below |
| **Watchlist — regression only** | the normalized phrase list (`pending` · `not confirmed` · `partial` · `not bindable` · …) | that the three historical spelling defects (r6 unmatchable pattern · r10 hyphen · r11 case) have not recurred | **completeness of any kind** — it is a heuristic guard, explicitly **not** an enumeration |

**THE RESIDUAL, STATED RATHER THAN CLOSED.** Check B verifies that marked Class-B statements are well-formed; **it cannot prove every mutable Class-B statement was marked.** Marking is an **authoring obligation**, and an unmarked one is invisible to it — exactly the way an unobservable input is invisible to an observer. This is a **self_reported-grade** guarantee on coverage and a decidable one on form, and it is labeled that way rather than dressed as completeness. The honest gain over a watchlist is that the *marked* set is decidable at all: a reader can enumerate every conditional claim I make and check each against §0a, without trusting my prose.

**A zero-hit report remains suspect.** Class-B status legitimately exists outside §0a, so silence from Check B means nothing was marked — not that nothing is there.

**One caution this check proved on itself:** §0b names the ledger delimiters in prose, and an *unanchored* exclusion pattern matched that prose, re-opened the excluded region, and left only **19 of 229 lines** under inspection — a check that would have reported clean because it had inspected almost nothing. **Anchor delimiter patterns to line start (`^<!--`)**, and always confirm the surviving operative line count against the file's, since a check that excludes its own subject cannot fail. This is the r6 unfalsifiable-search defect reappearing in the tooling built to prevent it.

**Rule:** any statement of **Class-A producer** state lives **only** in §0a; every other section states **timeless dependency semantics** or a **conditional Class-B consequence**, citing §0a for the live values. A refreshed summary is not evidence that the operative sections were refreshed (the r7 defect); a centralized ledger is not evidence that it matches the external trail (the r8 defect); a stated procedure is not evidence that it was executed (the r9 defect); and **a search over words I chose is not evidence about the words that are there** (the r6 defect, recurring at r10 as `non-bindable` vs `not bindable`).

**Producer revisions are always written QUALIFIED — `m-<n> r<N>` — never bare.** A bare `r10` is *this document's* revision. The two were genuinely ambiguous while both chains sat at r10, which is how a producer revision could hide from a scan of my own revision tokens; qualification makes the Class-A scan exact.

**Condition-id registry (closed) — the source of truth for Check B's `cond` field (R13-F1).** A `[CB cond=<ID> ledger=§0a:<Rn>]` marker is well-formed iff `<ID>` is one of exactly these three and `<Rn>` equals the anchor declared here. The point of the id is that condition presence becomes **decidable**: a parser checks membership and anchor-match, it does not read prose. What the condition *means* is fixed here, once, rather than restated (and drifting) at each marker:

| `cond` id | anchor | the condition (fixed here; markers do not restate it) |
|---|---|---|
| `R1_RECIPE` | `§0a:R1` | a current m-9 `logical_surface_digest` revision is pair-approved **and** its five-member extraction is observer-executable from request bytes alone |
| `R2_DISCRIMINATOR` | `§0a:R2` | m-8 has published the decoded field that discriminates its post-freeze integrity refusal from its pre-freeze typed rejects |
| `R3_CARRIAGE` | `§0a:R3` | m-10's B/E carriage row is pair-approved **and** reproduced by me |

**The registry declares each condition's FIXED meaning and its §0a anchor — and deliberately records NO current MET/UNMET evaluation (R14-F1).** A `currently` column would be exactly the mutable, producer-derived state the registry exists to keep out of every place but §0a; the authoritative evaluation of each anchor lives ONLY in §0a's consequence column (`R1 = …`, `R2 = …`, `R3 = …`). When a fold needs to report current evaluations, it **derives them from §0a at check time and records that in the relay**, never as a second durable table here. The only pointer this registry carries is the anchor itself. Adding a fourth condition is a governed edit to this registry, so a marker can never silently invent a condition.
<!-- RULEDEF-END -->

## §0 — What this delta is, and the three rules it obeys
A governed additive delta realizing the **ratified v2 contract** over frozen r4. Three rules govern every section:
1. **Presence is READ, never INFERRED.** Per m-8 r5 §0-P1, a consumer reads `frozen_core_digest` presence **directly off the field** — never from `phase`, never from the `rejected_local` token (that token spans a freeze-failed no-digest cut AND a post-freeze has-digest cut; §1 rows 2a/2b). My r0's "`rejected_local` ⇒ pre-freeze" was false at the bytes; this delta never re-derives presence from a disposition.
2. **P2b is mine and keys on MY vantage alone.** m-8 r5 §0 splits producer eligibility (P2a, their internal `request_write_completed`) from evaluator decidability (P2b, mine). **m-3 does not read, trust, or record P2a** — my predicate-1 decidability keys **only** on whether the `evidence_locator` resolves to a complete capture. Their eligibility column is context, never an input.
3. **Every verdict machine is TOTAL and ordered.** First-match-wins over an exhaustive, mutually-exclusive branch list covering every schema-valid input state including missing and contradictory facts. `unknown` is the honest cannot-decide and **never passes a gating leg**.

## §1 — The exhaustive cut × CARRIER matrix (the parametric rule instantiated over m-8 r5's settled ten cuts)
The ratified rule — `frozen_core_digest` present iff `FREEZE-REACHED(cut)` — evaluated against the settled cut-LIST, **with m-8's exact carrier route on every row** (F1). Layer-1 byte parsing is unchanged: **absence is schema-VALID** at E0 and E3-attempt; this table is the layer-2 producer-conformance obligation the D4/T4 fixtures enforce, never an acceptance-time check.

**Rule precondition — MASTER-RULED, hold retired (F4; ruling `step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260723-041500` @ `e710da6d2398c08918f65f340102b30dccbe65863088dc6062a7f3a58b596668`, reproduced on disk).** Master's exact disposition, folded: *"your r2 local precondition is the correct realization **within** the ratified parametric rule — the rule's subject is a carried value; a cut with **no digest-bearing carrier has no carried value, so the field is ABSENT by construction (schema-VALID, never forbidden)**, regardless of `freeze = maybe`. This does not contradict the ratified contract `6e2abe40…`; it realizes it totally over the cut classes. **No amendment to the ratified text is required** — the joint coherence holds: **m-8 no-digest-carrier ⟺ m-3 absent-schema-valid**."* The precondition is therefore **ratified-rule-conformant, not an m-3-created rule**, and rows 9/10's producer-conformance consequence is settled: **absent, schema-valid**, never forbidden.

| # | m-8 r5 cut | freeze (m-8 P1) | DATA-P carrier (m-8→m-9) | CTRL-C `m8.attempt_result.v2` (m-8→m-10) | digest-bearing carrier? | m-3 E0 | m-3 E3 (attempt) | predicate-1 |
|---|---|---|---|---|---|---|---|---|
| 1 | pre-freeze reject: `malformed_request` · `lane_capability_mismatch` · `replay_scope_violation` | **NO** | `m8.dataP_reply.v2` typed reject — **no digest members** | `rejected_local(<reason>)` — **members absent** | carriers exist, **no members** | EVENT EXISTS, `phase=failed`, field **ABSENT** | record exists, field **ABSENT** | `unknown` |
| 2a | `rejected_local(internal_integrity_fault)` — duplicate frozen headers, **freeze interrupted** | **NO** | `m8.dataP_reply.v2` typed reject — **no digest members** | `rejected_local(internal_integrity_fault)` — **members absent** | carriers exist, **no members** | EVENT EXISTS, `phase=failed`, field **ABSENT** | record exists, field **ABSENT** | `unknown` |
| 2b | `rejected_local(internal_integrity_fault)` — send-digest mismatch, **post-freeze pre-wire** | **YES** | `m8.dataP_reply.v2` `internal_integrity_reject` **(+B+E)** | `rejected_local(internal_integrity_fault)` **(+B+E)** | **YES** | `phase=failed`, field **PRESENT** = the **step-2 authorized** digest (never the step-5 recomputation) | field **PRESENT** (same value) | `unknown` — no wire exists |
| 3 | epoch-inert `STALE_EPOCH` / `EPOCH_AHEAD` | NO (fenced at DATA-P **before** the pipeline) | **`m8.dataP_reply.v2` epoch reply EXISTS — no digest members** (F1 correction: a DATA-P message IS emitted; my r1's "no m-8 carrier exists" was false) | **NONE — no `attempt_result` message** | **no digest-bearing carrier** | **NO E0 EVENT** — no terminal to report; the disposition is m-10's epoch/retirement machinery (r4 §6.1 class (B), unchanged) | **if an observer authors a record, the field is ABSENT** (nothing was carried) — the record is schema-valid; NOT "forbidden" | `unknown` |
| 4 | **policy deny** | **YES** (freeze precedes authorize) | `m8.dataP_reply.v2` `egress_denied` **(+B+E)** — no stream | `denied(<token>)` **(+B+E)** | **YES** | `phase=denied` + `deny_reason`, field **PRESENT** | field **PRESENT** | `unknown` — no wire written |
| 5 | terminal `completed` | **YES** | `m8.provider_event.v2` stream → `completed` **(+B+E)** | `sent_completed` **(+B+E)** | **YES** | `phase=completed`, field **PRESENT** | field **PRESENT** | **`pass`/`fail` iff the `evidence_locator` resolves a complete capture; else `unknown`** |
| 6 | terminal `failed{transport}` | **YES** (freeze ran on every sub-cut) | `m8.provider_event.v2` stream → `failed` **(+B+E)** | `transport_failed` **(+B+E)** | **YES** | `phase=failed`, field **PRESENT** | field **PRESENT** | **capture-only**: resolves ⇒ `pass`/`fail`; else `unknown` (the fresh-dial / nothing-written / partial-write sub-cuts simply never yield one) |
| 7 | cancel **pre-transport** | **YES** | `m8.provider_event.v2` `cancelled{partial:none}` (m-8-view-only) **(+B+E)** | `cancelled(pre_transport)` **(+B+E)** | **YES** | `phase=cancelled`, field **PRESENT** | field **PRESENT** | `unknown` — no wire written |
| 8 | cancel **post-invocation** | **YES** | `m8.provider_event.v2` stream → `cancelled{partial}` **(+B+E)** | `cancelled(post_invocation)` **(+B+E)** | **YES** | `phase=cancelled`, field **PRESENT** | field **PRESENT** | **`pass`/`fail` iff capture resolves; else `unknown`** |
| 9 | DATA-P / connector loss, live worker | **maybe** (not evaluated — see the precondition) | **none — channel gone** (no message) | **NONE** (no message) | **no carrier at all** | **NO E0 EVENT** — nothing observed to report | **if an observer authors a record, the field is ABSENT** (nothing was carried); m-10's durable row is the truth | `unknown` |
| 10 | m-9 worker crash | **maybe** (not evaluated) | **none** (no message) | **NONE** (no message) | **no carrier at all** | **NO E0 EVENT** — the populator is gone (r4 §6.1, unchanged) | **if an observer authors a record, the field is ABSENT**; m-10's row is the truth | `unknown` |

**Three distinct absence classes, never conflated (F1):** *(i)* **carrier exists, members absent** (rows 1/2a) — a message was emitted carrying no digest; *(ii)* **no digest-bearing carrier, some message** (row 3 — the DATA-P epoch reply exists); *(iii)* **no message at all** (rows 9/10). In every class the m-3 field is **ABSENT and schema-valid** — never "forbidden," which would wrongly make an honest observer record malformed.
**Totality:** the ten cuts are m-8 r5's complete set; each appears exactly once. **The stated consequence** (the r0-F2 root): rows 1, 2a, 2b **all** surface `phase=failed`, and 1/2a carry no digest while 2b does — so `phase` provably cannot encode freeze-reached, and only the read field can. Rows 5/6/8 are the only wire-comparable cuts; on 2b/4/7 predicate 1 is honestly `unknown` because no wire exists to compare — **not a defect of those cuts, a truthful statement about them.**
**P2a is never an m-3 input** (§0.2): m-8's eligibility column is context; a cut they mark ineligible simply never resolves a capture and lands on predicate 1's no-capture branch.

## §2 — The five typed E3 verdict machines (total, ordered, first-match-wins)
Common shape (amendment-fixed): `{predicate_id, version, required_inputs[], observed_facts[], evidence_locator, verdict ∈ pass|fail|unknown, observer_id, exact_digest}`. Common rules: `observer_id` per the r4 §3.6 contract-pinned registry; `exact_digest` = SHA-256 over the record's canonical JCS bytes **with the `exact_digest` member absent**; `evidence_locator` is a **reference** into the observer's recorder boundary, never payload toward any conductor surface. **Gating status (master's ratified D2 fold):** predicates **1, 3, 4 are GATING** (§7 legs); predicates **2 and 5 are RECORDED-ONLY / NON-GATING** — they carry no §7 leg or exit consequence, and no verdict of theirs passes or fails a leg.

### 2.1 `provider_request_matches_frozen_core` v1 — GATING
Inputs: the carried `frozen_core_digest` (E3 attempt record) · the observer's **independent** derivation over its captured wire request per m-8 r5 §2.1's recipe.
| # | condition (first match wins) | verdict |
|---|---|---|
| 1 | the record is not attempt-scoped, or is not `applicable` under the r4 evaluator | *(not evaluated — applicability precedes predicates)* |
| 2 | the carried digest is **absent** (a §1 freeze-not-reached or no-carrier cut) | `unknown` |
| 3 | the `evidence_locator` does not resolve, or resolves to an **incomplete** capture (any §2.1 derivation input missing) | `unknown` |
| 4 | both derived; the observer's derivation **byte-equals** the carried digest | `pass` |
| 5 | both derived; **unequal** | `fail` |
**F84 rail, restated:** the comparand is a **fresh derivation from captured bytes** — never a byte-copy of the carried value compared to itself. **P2a is not an input** (§0.2): a cut m-8 marks ineligible simply never resolves a capture and lands at branch 3.

### 2.2 `provider_deny_caused_zero_transport` v1 — NON-GATING (recorded-only, D2)
Inputs: the attempt's disposition + m-3 deny token (the E3/attempt record) · the observer's transport-invocation count for that attempt · (where both exist) the m-10 row's deny token.
| # | condition (first match wins) | verdict |
|---|---|---|
| 1 | the disposition is **not** `denied` | `unknown` — **out of scope, not a pass**: a non-denied attempt makes no claim about deny-caused zero-transport |
| 2 | the deny row/terminal is **absent or malformed** (no decidable token) | `unknown` |
| 3 | the row token and the terminal token are **both present and disagree** | `fail` — a contradictory deny record is a governance defect, not an indeterminacy |
| 4 | the transport-invocation count is **not observable** for the attempt | `unknown` |
| 5 | denied ∧ count **= 0** | `pass` |
| 6 | denied ∧ count **> 0** | `fail` |
Branch 3 makes token equality **part of the verdict**, not decoration in `observed_facts[]`.

### 2.3 `local_invocation_matches_effect_descriptor` v1 — GATING
Inputs: the consumed ticket's effect descriptor (the ratified §5-C per-action applicability table, as m-9 r5 §7 derives and records it) · the executor's captured invocation identity · the observed effect locus.
| # | condition (first match wins) | verdict |
|---|---|---|
| 1 | no descriptor record resolves for the ticket | `unknown` |
| 2 | the descriptor is **incomplete against its action's `R` set** (any required field missing) | `unknown` |
| 3 | a required identity component is **unobtainable** — explicitly including `shell_interpreter_ref.content_id` for `bash` (m-9 r5 §7's own rule: unobtainable `content_id` ⇒ `unknown`, **HOLDS the leg, never a weaker pass**) | `unknown` |
| 4 | no captured invocation resolves for the ticket | `unknown` |
| 5 | every `R`-applicable descriptor field **matches** the captured invocation/effect locus | `pass` |
| 6 | any `R`-applicable field **mismatches** | `fail` |
`∅`-inapplicable fields are excluded by the ratified table — never judged, never counted as a mismatch. Branch 3 is the m-9-aligned honest hold: an unverifiable identity is not a pass.

### 2.4 `relay_record_committed_with_stamped_sender` v1 — GATING
Inputs: the named `relay_id` · a governed-read resolution through an observer/operator seat.
**The governed-read result domain — CLOSED at three members, defined at the GOVERNED-READ PROTOCOL grain, not transport EOF (F2):**
- **`committed`** — the read **completed at the protocol grain** AND yielded **exactly one fully decoded, schema-valid committed record** for the id.
- **`not_found`** — the read **completed at the protocol grain** AND yielded **the exact authoritative absence result** the read surface defines (a positive negative from a working surface).
- **`unavailable`** — **everything else, positively defined** (this member is the closed catch-all, so the domain is total by construction): transport failure · timeout · capability refusal · a **truncated or partial** response · **malformed/undecodable** bytes · an **unknown or unrecognized result kind** · a response that **fails schema validation** · **multiple or ambiguous** results for one id. **A transport that completes while yielding non-validatable bytes is `unavailable`, never `not_found`** — the discriminator is *"did the surface deliver one of its two authoritative results, fully decoded and validated,"* not *"did the socket close cleanly."*

| # | condition (first match wins; exhaustive over the closed domain) | verdict |
|---|---|---|
| 1 | resolution = `unavailable` (any member of the catch-all above) | `unknown` — machinery, not evidence (the r4 fail-closed distinction) |
| 2 | resolution = `not_found` | `fail` — a completed, validated, authoritative denial is evidence of absence |
| 3 | `committed`, but the record's kind does not match the claim | `fail` |
| 4 | `committed` ∧ kind matches ∧ the **channel-stamped `FROM` equals the expected sender** | `pass` |
| 5 | `committed` ∧ kind matches ∧ stamped `FROM` **differs** | `fail` |
Branches 1–5 are exhaustive: every resolution is exactly one of the three members, and `committed` is fully split by branches 3/4/5.

### 2.5 `no_alternate_credentialed_provider_route_observed` v1 — NON-GATING (recorded-only, D2)
Inputs: the observation window's egress capture · the governed-route identity (m-8's send path).
| # | condition (first match wins) | verdict |
|---|---|---|
| 1 | the window has **no egress capture**, or the capture is incomplete/gapped | `unknown` |
| 2 | the capture is complete ∧ **zero** credentialed provider sends outside the governed path | `pass` |
| 3 | the capture is complete ∧ **≥1** such send observed | `fail` |
**Honesty ceiling, in the contract (r4 §0.2 carried):** decidable only where egress is observable (instrumented/namespaced runs); a `pass` claims **the observed window only** and is never a claim about un-instrumented production or `bash`-originated traffic. Its non-gating status (D2) means this ceiling never silently props a gating leg.

## §3 — The §B sink record `m3.b_sink.v1` (m-3-authored; F73 confirmations, NOT a join) — **m-10 column BOUND at rev3 `cd17db32…`**
> **Design status `[CB cond=R3_CARRIAGE ledger=§0a:R3]` (not a schema value):** the m-10 column of this section binds **iff** m-10's B/E carriage row is pair-approved and reproduced by me. **That condition is MET** — m-10 B/E rev3 is pair-approved and reproduced (§0a:R3, `cd17db32…`), master having routed it — so the m-10 column, its `m10_row_state` machine (§3.3), and the expected-state cells bind. Per M3-L2-R2-F3 the *runtime schema below carries no pendency marker* — DAG state is design status in prose, never modelled as an attempt value.

### 3.1 The closed runtime record (exact types, closed enums; no integration status)
```
{ "schema":            "m3.b_sink.v1",                       // literal, REQUIRED
  "run_id","turn_id","attempt_id":  string,                  // REQUIRED
  "classifier":        { "data_p_reply_kind":  <CLOSED enum §3.2a>,
                         "stream_terminal":    "completed|failed|cancelled|none",
                         "ctrl_c_disposition": <CLOSED enum §3.2a> },   // REQUIRED
  "presence_class":    "P|A|N3|N910",                        // REQUIRED — derived per §3.2b
  "m8_dataP_digest":        "<64-hex>|absent|no_message",    // REQUIRED — see §3.2c for the EXACT object
  "m9_e0_digest":           "<64-hex>|absent|no_message",    // REQUIRED — renamed from m9_carriage_digest
  "m10_row_state":          "present|not_found",             // REQUIRED (F2) — authoritative ROW existence
  "m10_row_digest":         "<64-hex>|absent",               // present IFF m10_row_state=present; a DURABLE ROW COLUMN, never `no_message` (§3.2c)
  "observer_derived_digest":"<64-hex>|not_derived",          // REQUIRED
  "consistency":  "consistent|inconsistent",                 // REQUIRED
  "inconsistency_loci": [ "m8"|"m9"|"m10" ],                 // REQUIRED (empty iff consistent)
  "derivability": "derived_equal|derived_unequal|not_derivable",
  "observer_id","observation_ts" }
```
### 3.2a The classifier is an **m-3 NORMALIZATION** — NOT producer literals, and its projection is **now TOTAL: m-8 r7's `refusal_stage` is the decoded discriminator (F1 CLOSED)** `[CB cond=R2_DISCRIMINATOR ledger=§0a:R2]`
**The DATA-P ACQUISITION READ — a CLOSED result BEFORE normalization (R16-F1).** The classifier's inputs (`data_p_reply_kind`, `refusal_stage`, `m8_dataP_digest`) come from the **m-8-authored DATA-P reply/terminal union** — an object the observer must *acquire*, and acquiring it is **NOT** predicate-1's wire-request capture: **P2b keys on the provider *request* m-8 sent, never on this m-8→m-9 *reply*** (r16 wrongly leaned on P2b/RS0/RS2 for this, and none of them observes this object — R16-F1, owned). Acquisition is read at the **governed protocol grain** — the same §2.4 discipline, a *distinct* read — yielding a **CLOSED domain**:
- **`present`** — the read completed and yielded **exactly one decoded, schema-valid** DATA-P object — the reply/terminal **union**, covering every rows-1–8 shape (a typed reply, an epoch reply, or a `m8.provider_event.v2` stream terminal) ⇒ decode ⇒ `Π` ⇒ tuples **T1–T8**. **The epoch cut (row 3 / T8) is `present`** — its `m8.dataP_reply.v2` epoch reply is a real object carrying no digest members, NOT an absence (r17 wrongly grouped it under `not_emitted` — R17-F1, owned).
- **`unavailable`** — the read did not complete, or the capture is absent / incomplete / gapped / undecodable / ambiguous. **`unavailable` ⇒ NO SINK RECORD IS AUTHORED** — the honest degradation.
- **`not_emitted` is UNREACHABLE in the current design, and I say so rather than manufacture it (R17-F1, the sharp one).** r17 made `not_emitted` "authoritative when corroborated by `m10_row_state`" — **but `m10_row_state=present` proves only that `provider_attempts` committed; it says NOTHING about whether m-8 emitted a DATA-P object on the m-8→m-9 channel.** Filling the "no DATA-P" premise from my own missing capture IS the unavailable-vs-not_emitted circularity this gate exists to remove — I minted an authoritative negative I do not have, the exact opposite of the route-not-mint rail I invoked for R2. **There is no surface in the current design that authoritatively records per-attempt DATA-P non-emission** — the loss cuts (rows 9/10) kill the very machinery that would record it. So `not_emitted` **has no authoritative producer**, and the domain is effectively **`present | unavailable`**: any attempt with no acquired DATA-P object — for ANY reason, loss or capture failure, which are **not distinguishable** without a positive producer/lifecycle negative — is **`unavailable` ⇒ no record.**
**Consequence, stated honestly: T9 / N910 is currently UNREACHABLE at the classifier, and the sink authors NO N910 record until an authoritative DATA-P-non-emission fact is ROUTED (not minted).** This is a real routed-out cross-domain dependency, the same shape R2 was before m-8 r7 landed — flagged to master in the addressed lane return, never papered with the m-10 row. `m10_row_state` is compared **after** classification (RS0–RS3), and **may not manufacture the classifier's no-message input.** N910's expected vector (§3.3, `m10_row_digest=absent`) stays defined and correct for when that fact is routed; it is simply not authored today.

**Correction owned:** r4 called these "producer literals." **They are not** — `pre_freeze_typed_reject` is an m-3 category m-8 never authored, the epoch values are m-8's `STALE_EPOCH`/`EPOCH_AHEAD` (not lowercase forms), and the CTRL-C dispositions carry payloads (`rejected_local(<reason>)`, `denied(<token>)`, `cancelled(pre_transport)`) that r4 silently flattened. Renamed and made honest: these are **m-3 normalized categories**, and their legitimacy rests entirely on the projection below.
**The projection `Π` — pure, explicit about what it discards, and NOW TOTAL (F1 closed).** The condition r5–r15 held on — *`Π` is total and the classifier closed IFF m-8 publishes the decoded field discriminating its post-freeze integrity refusal from its pre-freeze typed rejects* — **is MET: m-8 r7 publishes `refusal_stage ∈ {pre_freeze, post_freeze}` on every reject reply** (§0a:R2, bound at `734e44b7…`). `[CB cond=R2_DISCRIMINATOR ledger=§0a:R2]` Rows T1/T2 are therefore executable and the classifier is closed; **§0a records the bound bytes** (R2). `[CB cond=R2_DISCRIMINATOR ledger=§0a:R2]` — R2's discriminator, now bound; §0a evaluates it. The r5 withdrawal of the totality claim stood exactly until this producer fact landed, and it now returns on r7's bytes, not on my confidence. `Π` maps an exact decoded producer object to one normalized value; it is defined only over the named decoded facts, and **any source shape it does not recognize REFUSES the record before classification** (never a default, never a coercion):
| normalized value | exact decoded source fact | discarded (deliberately) |
|---|---|---|
| `internal_integrity_reject` (**executable** — R2 bound, §0a) | the `m8.dataP_reply.v2` reject object with `reject_reason=internal_integrity_fault` **and `refusal_stage=post_freeze`** (r7 §1.2) — the decoded field that distinguishes 2b from 2a, read directly, never inferred from B-presence | — |
| `pre_freeze_typed_reject` (**executable** — R2 bound, §0a) | a `m8.dataP_reply.v2` reject object with **`refusal_stage=pre_freeze`** (r7 §1.2) — covering both the row-1 translate rejects and the row-2a interrupted-freeze refusal. **Now a real source rule, not a category restatement:** `refusal_stage` is an independent pipeline-position fact m-8 emits, so the classifier reads it rather than establishing it | *which* pre-freeze reason — rows 1 and 2a share a class and vector, so the distinction is not needed here (m-8 keeps the reason token on the CTRL-C side) |
| `egress_denied` | the `m8.dataP_reply.v2` `egress_denied` object | — |
| `stale_epoch` / `epoch_ahead` | m-8's epoch reply values **`STALE_EPOCH` / `EPOCH_AHEAD`** — normalized case only, one-to-one and reversible | nothing |
| `rejected_local` · `denied` · `sent_completed` · `transport_failed` · `cancelled_pre_transport` · `cancelled_post_invocation` | the `m8.attempt_result.v2` disposition **tag** | the **payload** — `<reason>`, `<token>`, and the cancel-point-as-payload (the cancel point survives as its own tag) — because classification must never consult a reason/token that could vary independently of the cut |
| `none` (any member) | the named object/frame is **absent** for this attempt | — |
**The ONE missing producer fact — ROUTED not minted, and now LANDED (F1's rail, discharged):** `Π`'s first two rows needed m-8's **decoded discriminator** separating the post-freeze `internal_integrity_reject` reply from the **pre-freeze** typed rejects. I refused to mint a third m-3 literal to paper it and escalated it (`SITREP-planner-20260723-151500`); master routed it to m-8, and **m-8 r7 §1.2 published exactly that field — `refusal_stage`, an independent pipeline-position fact with the invariant `refusal_stage = post_freeze ⟺ B∧E present`, emitted independently of the digests** (§0a:R2, `734e44b7…`, reproduced by me). So T1/T2 are now provable, `Π` is total, and the classifier is closed — **on m-8's bytes, exactly as the rail required, never on a minted literal.** The rail held: the discriminator was worth waiting for rather than inventing.

### 3.2b The allowed-tuple mapping → presence class — **TOTAL ONLY OVER SUCCESSFULLY NORMALIZED TUPLES** (F1 scope; anything outside ⇒ record INVALID)
| # | `data_p_reply_kind` | `stream_terminal` | `ctrl_c_disposition` | class | §1 rows |
|---|---|---|---|---|---|
| T1 | `pre_freeze_typed_reject` | `none` | `rejected_local` | **A** | 1 · 2a |
| T2 | `internal_integrity_reject` | `none` | `rejected_local` | **P** | 2b |
| T3 | `egress_denied` | `none` | `denied` | **P** | 4 |
| T4 | `none` | `completed` | `sent_completed` | **P** | 5 |
| T5 | `none` | `failed` | `transport_failed` | **P** | 6 |
| T6 | `none` | `cancelled` | `cancelled_pre_transport` | **P** | 7 |
| T7 | `none` | `cancelled` | `cancelled_post_invocation` | **P** | 8 |
| T8 | `stale_epoch` \| `epoch_ahead` | `none` | `none` | **N3** | 3 |
| T9 | `none` | `none` | `none` | **N910** | 9 · 10 — **currently UNREACHABLE at the classifier (R17-F1): reaching T9 needs an authoritative DATA-P `not_emitted`, which no current surface provides; the loss cuts are `unavailable ⇒ no record` until the fact is routed** |
**SCOPE (F1, now CLOSED):** this mapping is total over recognized normalized tuples, and with `Π` now total (§3.2a, R2 bound), **the classifier is closed end-to-end** — every rows-1/2a/2b attempt reaches this table via `Π`'s `refusal_stage` decode. `[CB cond=R2_DISCRIMINATOR ledger=§0a:R2]` The end-to-end closure carried the **same R2 condition** as `Π`, and §0a records it bound. `[CB cond=R2_DISCRIMINATOR ledger=§0a:R2]` Within that scope: **T1–T8 are reachable via a `present` DATA-P acquisition; T9 is currently unreachable (§3.2a, R17-F1) and its cut degrades to no-record. T1–T9 are the complete set of tuples the settled cuts emit.** Every other combination — unknown/malformed member, or a well-typed but unreachable pairing (an epoch reply with `stream_terminal=completed`; `internal_integrity_reject` with an unrelated CTRL-C disposition; a stream terminal alongside any DATA-P reply kind) — is **not classified**: the record is **INVALID and refused**, never coerced into P/A/N3/N910. The 2a/2b discriminant is the **DATA-P reply kind as decoded by `refusal_stage`** (T1 `pre_freeze` vs T2 `post_freeze`, r7 §1.2), and **no digest value participates in classification** — the circularity the relabel flagged is closed by construction: the cut is keyed off `refusal_stage`, then B-presence is *validated* against it, never the reverse.
### 3.2c Each member's EXACT carrier object (F2)
- **`m8_dataP_digest`** — the digest member on **the m-8-authored DATA-P object for this attempt**: the `m8.dataP_reply.v2` typed reply **or** the `m8.provider_event.v2` stream terminal (the reply/terminal **union**, named explicitly so N3's epoch reply can justify `absent`). `no_message` iff m-8 emitted no DATA-P object at all.
- **`m9_e0_digest`** — the field on **the m-9-authored `m3.app_event.v2` E0 record** for this attempt. **`no_message` iff no E0 record exists** (there is then no record in which a member could be absent); `absent` iff the record exists carrying no value.
- **`m10_row_digest`** — the column on **m-10's durable `provider_attempts` row**. A row that exists with an unset column is **`absent`**; **`no_message` is not a legal value for a durable row column** (source-frame absence is a different fact — and it is already carried by `classifier.ctrl_c_disposition = none`, so this member is never overloaded to express it).
### 3.3 Consistency — the expected PER-POINT vector, compared point by point (F3/F2)
| class | expected `m8_dataP_digest` | expected `m9_e0_digest` | expected `m10_row_digest` |
|---|---|---|---|
| **P** | `<64-hex>` | `<64-hex>` | `m10_row_state=present`, `m10_row_digest=<64-hex>` expected (BOUND, rev3 §5a) `[CB cond=R3_CARRIAGE ledger=§0a:R3]` |
| **A** | `absent` (object exists, member absent) | `absent` (E0 record exists — §1 rows 1/2a) | `m10_row_state=present`, `m10_row_digest=absent` expected (BOUND, rev3 §5a: a `present` row may carry a NULL m-8 digest per P1) `[CB cond=R3_CARRIAGE ledger=§0a:R3]` |
| **N3** | `absent` (the epoch reply exists, carries no member) | **`no_message`** (§1: NO E0 EVENT) | **EITHER row-state is legal (F2):** `not_found` when `attempt_open` was stale-rejected so **no row was ever committed**, or `present` with the digest `absent` when a committed row is parked by m-10's retirement machinery — **the sink records which; it never infers one from source-frame absence** |
| **N910** | `no_message` (no DATA-P object at all) | `no_message` (no E0 event) | `m10_row_state=present` (row committed at `attempt_open`), **`m10_row_digest=absent`** — CARRIER-DERIVED (R16-F2): §1 rows 9/10 have **no CTRL-C `attempt_result` carrier**, so m-10 receives no B and the column is NULL (rev3 §2); the expectation is the exact `absent` state, never the deliberately-unevaluated P1 fact. **This vector stays DEFINED but is NOT AUTHORED today** — reaching N910 needs an authoritative DATA-P `not_emitted` the current design cannot produce (§3.2a, R17-F1); it is the correct expected state for when that routed fact lands `[CB cond=R3_CARRIAGE ledger=§0a:R3]` |
Each observed member is compared to **its own** expected state; **`absent` and `no_message` are distinct expectations**, so a wrong ROUTE is `inconsistent` even when no hex digest appears. `consistency = consistent` iff every bound point equals its expected state **and** (class P) all bound hex values are byte-equal; otherwise `inconsistent`, with `inconsistency_loci` naming each diverged point — covering the **invented-carrier** (hex in A/N3/N910) and **mixed-presence** (P) cases.

**The row-state machine (F2) — ordered, total, evaluated BEFORE the per-point comparison:**
| # | condition (first match wins) | disposition |
|---|---|---|
| RS0 | the governed read for the row is **not authoritative** — timeout · malformed/undecodable · store unavailable · ambiguous/multiple results | **NO SINK RECORD IS AUTHORED.** Machinery failure is never classified as `not_found`; the sink is authored only from an authoritative row result (this mirrors the r4 §2.4 protocol-grain discipline) |
| RS1 | authoritative `not_found` ∧ `presence_class = N3` | **LEGAL** — the stale-rejected `attempt_open` branch; `m10_row_digest` is **absent from the record**, and the m-10 point contributes nothing to consistency (a different legal shape, not a divergence) |
| RS2 | authoritative `not_found` ∧ `presence_class ∈ {P, A, N910}` | **`inconsistent`, locus `m10`** — those classes commit a row at `attempt_open`, so an authoritative absence contradicts the class |
| RS3 | `present` | `m10_row_digest` is **REQUIRED** and compared to the class's expected state (§3.3) like any other point |
**The independent-locus rule (F2, stated):** the m-10 branch never excuses another point. An N3 record claiming `not_found` while an E0 event exists is **`inconsistent` at locus `m9`** on its own merits — N3 expects `m9_e0_digest = no_message`, and the per-point rule judges each point against its own expectation regardless of the row-state branch.
The m-10 column's expected DIGEST states are now BOUND against rev3 §5a `[CB cond=R3_CARRIAGE ledger=§0a:R3]`: `m10_row_state` is the authoritative `present|not_found` fact and the digest members are conditional on `present`, matching my RS0–RS3 machine exactly (rev3 §5a's digest-conditional-on-`present` rule is the reciprocal of RS3)

### 3.4 Derivability — the PRODUCER-ROOT comparand, independent of consistency (preserved)
The canonical comparand is **`m8_dataP_digest`** (§3.2c) — m-8 computes where the bytes exist; m-9/m-10 hold copies under copy-never-compute, and a copy is never the comparand. `not_derivable` iff `observer_derived_digest = not_derived` **or** `m8_dataP_digest ∉ <64-hex>`; else `derived_equal`/`derived_unequal` by byte-comparison against it. **Independent of consistency by construction:** an `inconsistent` ∧ `derived_equal` record is a precise diagnosis — the producer root matched the wire while a *copy* diverged — and `inconsistency_loci` names which copy.
**Authority boundary (F5, carried):** an m-3 **RECORD** over three producers' confirmations — not a join; it enforces nothing (producer enforcement is the D4/T4 fixtures) and the composite exit proof stays Master+VP's.

### 3.4a Consistency ruling — the m-10 B-presence is validated, NOT self-certified (master's routed question, ruled)
Master routed one consistency question with R2/R3: m-8 published `refusal_stage` on the reply **and** offered to mirror it onto `m8.attempt_result.v2` so m-10's row B-presence would be independently validatable — but m-10's rev3 was pair-approved against m-8 **r5**, before r7, so its bound row does **not** carry that mirror. The question: does my cross-validation of the m-10 row's B-presence **need** the mirror, or does it bind cleanly? **I own the sink's cross-validation completeness, and I rule it a CLEAN BIND**, grounded in my own approved §3.2b, not asserted:
- **The m-10 B-presence is never self-certifying in this sink.** My classifier derives `presence_class` from the **DATA-P reply kind** — the `refusal_stage` decode (§3.2b: *the 2a/2b discriminant is the DATA-P reply kind; no digest value participates in classification*) — and §3.3 compares the observed `m10_row_digest` against the expected state that `presence_class` fixes. So the m-10 row's B-presence is checked against an **independent producer root** (the reply's `refusal_stage`, an m-8 fact m-10 did not produce), exactly the F84 shape.
- **When the DATA-P reply is not authoritatively acquired, the sink DEGRADES — via the EXECUTABLE acquisition gate (§3.2a), not the hand-wave r16 gave.** R16-F1 was right that r16 leaned on P2b/RS0/RS2, none of which observes this reply. The correct mechanism is the **DATA-P acquisition read (§3.2a): `unavailable ⇒ NO SINK RECORD.`** So when the reply is not acquired, the sink authors nothing and never reaches the m-10 point — at **no** point does it treat the m-10 row's stored B as self-certified truth. **Only a `present` DATA-P acquisition can produce a sink record today** (R18-F1): every non-present acquisition is `unavailable ⇒ no sink record`, and `not_emitted` / T9 / N910 remain **unreachable** until an independently authoritative producer/lifecycle fact lands (§3.2a — the `m10_row_state` corroboration is withdrawn everywhere, including here). On a `present` reply the 2a/2b split is decided by `refusal_stage` and the m-10 B is validated *afterward*.
- **The clean bind therefore has a STATED PREREQUISITE, and I name it rather than hide it: the sink REQUIRES authoritative DATA-P reply acquisition at the observer boundary.** My §3.4 derivability already assumes this — `m8_dataP_digest` is the producer-root comparand the observer captures independently (F84), so authoritative acquisition is the intended boundary, and `unavailable ⇒ no record` keeps every un-acquired case honest. Under that prerequisite the mirror is **not required for correctness** (reviewer pressure-point 3, first branch): there is no self-certification, because an un-acquired reply yields no record rather than an accepted row.
- **The honest cost, stated (not buried): without the mirror, an `unavailable` DATA-P reply on a 2a/2b reject costs a RECORD even though the m-10 row IS authoritatively present.** The r7 `attempt_result.v2` mirror would recover exactly those cases — resolving the 2a/2b split from the CTRL-C/E3 side independently of the reply, and adding a producer-fault cross-check between m-8's two carriers. That is a **coverage + independence enhancement**, not a correctness gap: it converts some `unavailable ⇒ no record` outcomes into validated records. I record it for a future **v3** revision, and I do **not** hold this binding on it — binding rev3 as-is is honest (self-certification-free), not a bind-around. **If master judges the E3 m-10 leg must be classifiable independently of the DATA-P reply, that is the trigger to route the mirror to m-10 now rather than defer it; I flag the trade so master owns that call.**
- **A SECOND routed-out dependency, distinct from the mirror (R17-F1):** the loss cuts (N910 / rows 9/10) need an **authoritative per-attempt DATA-P non-emission fact** that no current surface provides — `m10_row_state` proves row commit, not cross-channel non-emission, so corroborating `not_emitted` with it was circular. Until a producer/lifecycle fact is routed, **N910 records are not authored** (the cut degrades to `unavailable ⇒ no record`); I route this to master rather than mint it, the same rail that held for R2. **Note the mirror does NOT solve this** — it recovers 2a/2b classification when the *reply* is unavailable, but N910 has no reply at all. **Both facts (the mirror trade and the N910 non-emission authority) go to master in the addressed lane return; CC here is context only and routes nothing.**

## §4 — The `logical_surface_digest` recipe-binding — **the RECIPE binds AND the CARRIAGE ROW now binds (rev3 `cd17db32…`)** `[CB cond=R1_RECIPE ledger=§0a:R1]` *(carriage half: `[CB cond=R3_CARRIAGE ledger=§0a:R3]`, item 1 below)*
**The overclaim, owned:** r3 declared *"all five hashed members now have settled composition."* **That was false at the bytes.** m-2's component settles **two** members (`logical_tool_schemas[]`, `tool_descriptions[]`) — mechanically, with element shape, the exact eight-name set, ordering, encoding, ownership, and refusal behavior. The other **three — `instructions`, `compaction_template`, `policy_messages`** — were **undefined at r5–r7** *(historical: they then appeared only inside the one outer-recipe declaration)*. **That state is SUPERSEDED (R8-F2)** — the live account is item 1 below and §0a; this paragraph states only the historical origin of the finding. Folding m-2's two-array component therefore does **not** establish that an observer can reproducibly derive the same outer object. I inferred "m-2 folded ⇒ recipe settled" without checking the other three members — the same over-claim class as the r0 evaluator claim and the r1 purge claim, and it is mine.
**The standing bar, unchanged across every revision:** the recipe binds **iff** a current m-9 revision is pair-approved **and** its extraction is independently observer-executable — reconstructible from request bytes alone, with no m-9 implementation code and no semantic reading of prose. §0a records whether that condition currently holds and against which bytes.

**THE CONDITION IS MET on the bytes §0a records, and I checked it myself rather than inheriting it.** `[CB cond=R1_RECIPE ledger=§0a:R1]` — §0a names the bytes that satisfy it. All five members have a settled, observer-executable composition on the bound m-9 bytes (§0a): m-2's two arrays (`logical_tool_schemas[]`, `tool_descriptions[]`) at element shape, the exact eight-name set, ordering, encoding, ownership, and refusal behavior; and the three m-9-owned members — `instructions` (the verbatim static request field, objective/per-task slice excluded), `policy_messages` (the declared constant `[]`, by recipe not by semantic judgement), `compaction_template` (a versioned-sentinel byte-prefix scan over `input[]`, total across none/one/many without knowing the attempt kind). **So an observer's re-derivation of `logical_surface_digest` is reproducible**, and the pre-lowering surface is fixed at first assembly — a Gate-2 reassembly touches assembled `input[]` content, which is not a member.

**I state this deliberately against my own worst precedent.** r3 declared *"all five hashed members now have settled composition"* when m-2's component settled **two** of five and the other three were bare names in the outer declaration — I had inferred *m-2 folded ⇒ recipe settled* without checking the remaining members. The claim is made again here only because the missing three are now defined at m-2's grain on pair-approved bytes I read in full and checked for executability at my own boundary. **The difference between the two claims is the checking, not the confidence.**

**What still does NOT bind — the distinction r5 collapsed and I keep separate:**
1. **The exact CARRIAGE ROW — now BOUND** `[CB cond=R3_CARRIAGE ledger=§0a:R3]`**.** *m-9 → the m-10 attempt row and E0* is the **ratified ARCHITECTURAL ROUTE** (§5-E); m-9's half was settled at r12 §6 (`attempt_open` gains the digest, required-never-absent, sent before m-10's row commit), and **m-10's separate row is now pair-approved (rev3 `cd17db32…`, §0a R3)** — the carrier confirmed at r12 §6 with the assembly-time condition closed (FX-BE-8). The two-halves distinction r5 collapsed is preserved and both halves have now landed on their own bytes: a settled producer half was never a settled carriage, and I bound each only on its own pair-approval.
2. **The producer HASHES — the binding rule, which has not moved:** I bind a producer hash **only** when it is pair-approved **and** I have reproduced it on disk myself, and each producer is bound **separately** — one producer's approval never carries another's. Which producers currently satisfy that rule, and at which bytes, is in **§0a**; this section deliberately restates no hash and no verdict.
3. **This fold closes all three lane gates (R1/R2/R3) but nothing downstream.** R1 (recipe), R2 (`refusal_stage`), and R3 (carriage) are now all bound; the recipe leg feeds the E-join **ratified out to v3**. It settles **no §D join**, licenses **no** DESIGN-lock/PLAN/T4/code/credential/provider/E3/merge/deploy, and the composite exit proof stays Master+VP's.

## §5 — F73 consumer confirmations (m-3 as consumer, byte-bound)
| producer | bytes | m-3 confirms |
|---|---|---|
| **m-8 r5** `c0b7b488…` + **r7 addendum** `734e44b7…` (§0a:R2) | the ten-cut matrix; the B recipe (§2.1)+placement (§2.2); the E recipe (§3.1); **r7's `refusal_stage` 2a/2b decode + the `post_freeze ⟺ B∧E present` invariant** | `[CB cond=R2_DISCRIMINATOR ledger=§0a:R2]` **CONFIRM both — consumable as specified, r7 reproduced by me.** My §1 instantiates their P1 column exactly; P2b decidability keys on my own `evidence_locator` only (their P2a is context); the row-2b step-2-authorized-B rule is carried verbatim into §1; and **r7's `refusal_stage` binds as `Π`'s 2a/2b source rule (§3.2a), keyed independently of B-presence** — the mirror on `attempt_result.v2` is noted as a future-v3 enhancement (§3.4a), not consumed here |
| **m-9** — live state + bound hash per **§0a** | §6 `logical_surface_digest`; §8 B carriage (verbatim copy, never recomputed); §7 the C descriptor derivation | `[CB cond=R1_RECIPE ledger=§0a:R1]` **CONFIRM — the seams and, on the bound bytes, the recipe** (§4). The carriage rule matches my copy-never-compute law; §7's `content_id`-unobtainable ⇒ `unknown`-holds-the-leg is adopted verbatim as my §2.3 branch 3; and the five-member composition is observer-executable at my boundary, checked by me on the approved bytes rather than accepted on m-9's account. Confirming a seam was never confirming a recipe — this row now does both, and says which is which |
| **m-10** B/E rev3 `cd17db32…` (§0a:R3) | the B-row + E-row + `m10_row_state` | `[CB cond=R3_CARRIAGE ledger=§0a:R3]` **CONFIRM — pair-approved and reproduced by me.** Verbatim carriage matches my copy-never-compute law; **`m10_row_state ∈ {present, not_found}` is the authoritative row-existence fact my RS0–RS3 machine consumes (§3.3)**, and digest-members-conditional-on-`present` is the exact reciprocal of RS3; the m-9 carrier confirmed at r12 §6 (FX-BE-8). The m-10 B-presence is validated against `presence_class`, not self-certified (§3.4a). *(Historical, licensed: an earlier m-10 revision recorded both rows as "DELIBERATELY NOT designed in this rev".)* |
| **m-2** rev2 `c3a8cd61…` | the E logical component | `[CB cond=R1_RECIPE ledger=§0a:R1]` **REACHED m-9 CORRECTLY — through their §6**, exactly as the chain required (never around it). m-3 folded no m-2-owned composition. **m-3 binds it ONLY through a current pair-approved m-9 hash, never directly** — the chain and the hash that currently satisfies that condition are in **§0a** |

## §6 — Scope: what is NOT in this fold
- **`model_surface_digest` / the E two-digest join** — ratified OUT of v2; a future governed **v3 delta**, its own cycle. Not designed, not referenced as pending-here.
- The frozen r4 (v1) — byte-untouched. The ratified v2 contract — realized, not re-authored.
- No DESIGN-lock, PLAN, T4/code token, credential, provider call, release binding, live E3, merge, deploy, or `frank/` action.

## §7 — Fold log
- r24 (2026-07-26): M3-SETTLE-R23-F1 folded from `DESIGN-REVIEW-implementer-m3-20260726-093000`. **The last residue of the r22/r23 over-claim, and the same shape once more: I stated a broad check and executed a narrow one, then claimed the narrow satisfied the broad — this time in the discovery backstop I had just added.** §0b step 1b's normative predicate admits a relay on an **OR** — `m-3 in TO/CC` **OR** `SUBJECT cites a bound hash` — so it deliberately catches a new lane addressed to m-3 *before* its subject carries the final hash, and a producer-source relay citing the hash that does *not* address m-3. My r23 execution instead reported "no non-derived relay touching m-3 **+** a bound hash," an **AND** that would miss either OR class, and it named **no watermark** — leaving the zero-result unreproducible, since the relay root contains historical non-derived m-3-addressed relays excluded only by the "since last sweep" bound. Fixed exactly as required: **one predicate stated once and used unchanged in rule and proof** — `non-derived AND newer-than-<watermark> AND (m-3-addressed OR subject-cites-any-bound-hash)`; **the watermark made explicit** — `20260724-170000`, the earliest current-binding approval (R3), the point after which any relay could pertain to a live binding, chosen so the bound is reproducible; and the proof now records **predicate + watermark + candidate count + every candidate path/disposition**. Re-executed with the true OR predicate over the stated watermark: **candidate count = 0** (no non-derived dispatch carries a post-watermark relay that either addresses m-3 or cites `01b885fe`/`734e44b7`/`cd17db32` in its subject) — so no undiscovered source lane exists, now provably and reproducibly. **R1 stays at m-9 r17 `01b885fe…`; R2/R3 unchanged; the nine-input three-class reconciliation, the six-lane sweep, N910's documented limit, mirror-v3 + re-open caveat, and every prior R20/R21/R22 correction all stand; m-9's B-consumability confirmation still owed.** No binding moves — the backstop's predicate + watermark + evidence were corrected. r23 SHA-256 `66e1d54af872a692d4d3e079725d35173b49b484b3c015ef9264dc020ab8d392`.
- r23 (2026-07-26): M3-SETTLE-R22-F1 folded from `DESIGN-REVIEW-implementer-m3-20260726-073000`. **The over-claim moved up one level into my own fix, and this is the cleanest statement of the whole lane's recurring defect.** r22 replaced the hand-list with a "derivation from §0a" and claimed it "cannot omit a bound producer's lane by construction." False at the bytes on two counts the reviewer named: (1) §0a's rows carried explicit dispatch paths only for **approvals** — no structured artifact-path or current-source field existed to extract — so my reconciliation verified only the three approval lanes yet reported "every provenance path resolves"; (2) the deeper one — **§0a is itself the potentially-stale cache this very §0b warns about, so a derivation FROM §0a can validate the paths §0a already records but can never DISCOVER a source §0a failed to record.** A validate-only mechanism dressed as discovery-by-construction is exactly the over-claim class this lane keeps surfacing, now committed in the completeness check itself. **The honest fix does both things the reviewer offered:** (a) **make the inputs real** — each bound row now carries an explicit `[prov: artifact=<file>@<hash> · approval=<relay> · source=<relay>]` triple, and the derivation extracts + reconciles **all three classes** (artifact resolves to a hash-verified immutable file *outside* the sweep; approval + source each to a swept lane), so the guarantee over §0a-recorded provenance is now actually checkable per class, not approval-only; (b) **narrow the claim + name the residual** — I retract "cannot omit by construction" (the mechanism validates supplied paths, it does not detect an unrecorded move), state that completeness holds only over §0a-recorded provenance, and add a **relay-root discovery backstop (step 1b)**: scan every dispatch NOT in the derived set for any relay whose TO/CC names m-3 or whose subject cites a bound producer hash, since the last watermark — a moved source surfaces there before §0a records it. Executed this fold: the three-class reconciliation passes for R1/R2/R3 (artifact/approval/source all resolve), and the relay-root backstop finds **no** non-derived dispatch touching m-3 + a bound producer — so no undiscovered source lane exists today. **R1 stays at m-9 r17 `01b885fe…`; R2 (m-8 r7)/R3 (m-10 rev3) unchanged; N910 documented-limit + mirror-v3 + re-open caveat unchanged; the six-lane result and prior R20/R21 corrections stand.** No binding moves — procedure + proof-honesty corrected. r22 SHA-256 `c43bd0032ed5f182e20cb64455a27acb3de34952d690bc6ddd46c55e27abde9a`.
- r22 (2026-07-26): M3-SETTLE-R21-F1 folded from `DESIGN-REVIEW-implementer-m3-20260726-053000`. **The wrong-surface sweep defect for the THIRD time (R9-F1 → R20-F1 → R21-F1), and the third occurrence is the one that forced the real fix.** r21 added `step3-relock-settlement-amend` and wrote a "general rule" (every lane carrying provenance for a bound hash is a sweep source) — and that rule was **already violated by the same list**: R3's bound producer, m-10 B/E rev3 `cd17db32…`, was approved in `step3-relock-dag-m10-be-carriage`, a lineage DISTINCT from the `step3-relock-dag-m10` I listed, and §0a's own R3 row names that path. A stated general rule beside a hand-maintained list does not enforce itself. **The fix is to stop hand-maintaining the list and DERIVE it from §0a:** §0b step 1 now extracts the dispatch path of every artifact/approval/current-source in §0a's bound producer rows, and the sweep set IS that closure (plus the m-3 authoring lanes and the producers' Master/VP-children lanes). A bound row whose provenance path does not resolve to a swept lane is a completeness failure caught before handoff — which a hand-list can never guarantee, because the derived set *is* the §0a provenance closure. **This is the generality I should have reached at r21 instead of adding one lane;** point-fixing a wrong-surface defect one lane at a time simply relocates it. Executed this fold: the reconciliation passes (§0a's three approval lanes `step3-relock-dag-m8`/`step3-relock-settlement-amend`/`step3-relock-dag-m10-be-carriage` all resolve to swept lanes), the current 6-lane derivation ran before handoff, and the omitted lane's latest source `step3-relock-dag-m10-be-carriage/SITREP-planner-20260724-173000` was read and confirms **no R3 drift** (rev3 pair-approved, R3 cleared). **R1 stays at m-9 r17 `01b885fe…`; R2 (m-8 r7)/R3 (m-10 rev3) unchanged; N910 documented-limit + mirror-v3 + re-open caveat unchanged.** The R3 binding does not move — this finding corrected the procedure and its proof record, not the binding. r21 SHA-256 `c5d387efadefafab1734892d458251b0ef4e005d283879041c7b94ec92fcaca7`.
- r21 (2026-07-26): M3-SETTLE-R20-F1/F2/F3 folded from `DESIGN-REVIEW-implementer-m3-20260726-033000` — three evidence-honesty defects around a sound rebase (the reviewer independently confirmed r17 §6 observer-executable and preserved the binding). **All three are this lane's signature — verification over-claim — and two are RECURRENCES I should have caught.** **F1 (R9-F1 recurring):** I moved current producer/approval/join provenance into a NEW dispatch (`step3-relock-settlement-amend`) and did not extend the §0b sweep to cover it — the identical wrong-surface defect from r9, one dispatch later. The trail proved it, not hypothesised it: m-9's `…-024000` crossed after my handoff recording m-10 + m-1 legs clean and **m-3 as the sole remaining §D-join leg**, current join state my enumerated sweep could not see. Fixed: added the lane + the general rule that every lane carrying provenance for a bound hash is a sweep source (a fixed list is valid only while it names them all), and recorded `…-024000` as crossed mail (no producer-byte drift; m-3 sole remaining). **F2 (the r18→r19 defect recurring):** I claimed r12 `04422965` was "0 operative / fold-log only" — but §0a, which **Check A deliberately excludes**, carries r12 twice as legitimate provenance (the m-9 rebase-from; the m-10 rev3 approval basis). A scan that excludes a region cannot prove that region clean — the exact shape of the r18→r19 spelling/region miss, committed again one fold later. Reclassified honestly: r12 is not a stale current binding, it is licensed §0a provenance + fold-log history; every "0 operative" claim removed. **F3:** I cited r17 §6 line 429 as proving the §D-settlement folds left §6's bytes untouched — but line 429 speaks only to the settlement amendment/operator gate, not §6. The binding never needed it: I bind on the direct semantic re-check of r17 §6 (which passes), and I drop the byte-stability-across-r12→r17 claim I cannot prove (r12 is off-disk). Also removed a duplicated `Any edit…voids the approval` sentence (a fold artifact) from the m-9 row. **R1 stays bound to r17 `01b885fe…`; R2 (m-8 r7)/R3 (m-10 rev3) unchanged; N910 documented-limit + mirror-v3 + re-open caveat unchanged.** The m-10 row's r12 reference is its own approval basis and stands (no m-10 rebase established by this finding); m-9's separate B-consumability confirmation to my sink stays owed, not collapsed into this leg. r20 SHA-256 `fed25029259fea8660771f07d4ee6a6ef89ccf3caaf85cff48f3d50aaa9a8f36`.
- r20 (2026-07-26): **R1 rebased from m-9 r12 `04422965…` onto the pair-approved m-9 r17 `01b885fe…`**, per m-9's §D-join re-tender `step3-relock-settlement-amend/DESIGN-planner-m9-20260726-015500` (leg 3, addressed to m-3.planner in TO). m-9's lane-2 delta advanced r12→r17 through the §D-settlement folds (§2.6 Gate-2 relabel, `turn_failed` clarification, `relay.*`, the S-4 exact-wire binding, §1.6a at-rest battery); m-9 states the §6 `logical_surface_digest` recipe locus is byte-unchanged across the chain and asks me to advance the bound hash, **verify not assume** (F84). **My binding discipline requires binding the CURRENT pair-approved hash** — r12 is now superseded (the on-disk delta is r17), so continuing to cite r12 would bind a superseded hash. **I verified rather than rubber-stamped:** (a) reproduced r17 `01b885fe…` on disk and confirmed its pair-approval (`step3-relock-settlement-amend/DESIGN-REVIEW-implementer-20260726-013000`, `approve` over `01b885fe…`, zero findings, review hash `f31b4099…`); (b) **re-ran my consumer check on r17's OWN §6 bytes** — not a byte-copy of m-9's claim: `instructions` = verbatim static `m8.llm_request.v1.instructions` field (objective/per-task excluded); `policy_messages` = declared constant `[]` by recipe; `compaction_template` = versioned-sentinel byte-prefix scan over `input[]`, total over none/one/many, never needing the attempt kind; m-2's two arrays folded byte-bound at `c3a8cd61…`; five members frozen at first assembly — all observer-executable from request bytes with no m-9/m-2 code, exactly as at r12; (c) bound on that direct semantic re-check alone (NOT on any byte-stability-across-r12→r17 claim, which I cannot prove — r12 is superseded — and NOT on r17 §6 line 429, which speaks only to the settlement amendment/operator gate, not §6's bytes; corrected at r21 per R20-F3). So the binding survives on r17's own bytes, and I advance §0a's m-9 row only. **Nothing else moves:** R2 (m-8 r7 `734e44b7…`) and R3 (m-10 rev3 `cd17db32…`) stay bound; N910 stays the ruled documented MVP limit; the r7 mirror stays v3-deferred; the mirror re-open caveat stands. The superseded r12 hash `04422965` is not a stale current binding; it remains legitimately in §0a as provenance (the m-9 rebase-from + the m-10 rev3 approval basis) plus fold-log history (the "0 operative" claim was corrected at r21 per R20-F2). r19 SHA-256 `92e08d091e0b4a4e65e6f69a6bd3c327e757da329feddcbc67b69430c78443e2` was master-accepted as the honest-partial basis and is superseded here **only** by the R1 hash advance — the design is otherwise byte-stable.
- r19 (2026-07-24): M3-L2-R18-F1 folded from `DESIGN-REVIEW-implementer-20260725-003000`. **The narrowest finding of the lane, and it is a residue, not a new hole — but it carried a real verification lesson.** r18 adopted the route-not-mint correction everywhere *except one operative sentence*: §3.4a still read *"only an authoritative acquisition (`present`, or `not_emitted` corroborated by `m10_row_state`) ever produces a record"* — the exact circular corroboration r18 withdrew in §3.2a/§3.2b/§3.3 and in §3.4a's own final bullet. A single parenthetical made the forbidden branch reachable again and contradicted three other operative statements. Removed; §3.4a now states the executable rule exactly: **only a `present` DATA-P acquisition produces a sink record today; every non-present acquisition is `unavailable ⇒ no record`; `not_emitted`, T9, and N910 remain unreachable until an independently authoritative producer/lifecycle fact lands.** **The lesson is about my verification, not just the sentence:** r18's relay claimed "circular text removed" on a grep of ONE spelling of the forbidden branch (`Authoritative ONLY when corroborated by an authoritative m-10 row-state`) and missed the §3.4a parenthetical (`not_emitted` corroborated by `m10_row_state`) — the r10/r11 spelling-miss family, now in my own check. The honest verification cannot be a zero-count, because §3.2a and the fold log MUST discuss the withdrawn rule to explain the fix: it **enumerates every `not_emitted`+`m10_row_state` co-occurrence in the operative region and classifies each by reading** — authorizes (a violation) vs explains-the-withdrawal (licensed). This fold: the one authorizing sentence removed; the remaining co-occurrences are the §3.2a fold-explanation, the T9-unreachable note, the degradation/routed-dependency bullets, and the dormant N910 vector (`m10_row_state=present`, `m10_row_digest=absent`) — all read and confirmed non-authorizing. The reviewer's producer/lifecycle sweep independently confirmed **no existing surface** supplies an authoritative per-attempt DATA-P non-emission result (m-8 loss semantics · m-9 `stream_lost` · m-10 `UNKNOWN_PROVIDER_OUTCOME`/retirement all describe outcomes, none proves non-emission at the observer boundary), so routing the fact stands. **Producer bindings (m-8 r7 `734e44b7…`, m-10 rev3 `cd17db32…`) untouched; dormant N910 vector, both routed-out items, and every downstream hold preserved.** r18 SHA-256 `bc670cf858fb4437c47c1bcb73d1452d262f27eeb780547e3f206506021d567b`.
- r18 (2026-07-24): M3-L2-R17-F1 folded from `DESIGN-REVIEW-implementer-20260724-231500`. **The deepest catch in this lane, and it is the one rail I thought I was holding: I minted an authoritative negative I do not have.** r17 fixed r16's prose-degradation by defining a DATA-P acquisition read, but made `not_emitted` "authoritative ONLY when corroborated by `m10_row_state`". The reviewer showed why that is circular: **`m10_row_state=present` proves only that `provider_attempts` committed — it says nothing about whether m-8 emitted a DATA-P object on the separate m-8→m-9 channel.** So the "no DATA-P" premise was being filled by my own *missing capture*, which is precisely `unavailable`, not an authoritative negative — the same unavailable-vs-not_emitted ambiguity the gate was built to close, re-imported one level down. **This is the route-not-mint rail (the one that held for R2, that I praised in r16/r17) violated in my own acquisition contract:** there is no surface in the current design that authoritatively records per-attempt DATA-P non-emission — the loss cuts (rows 9/10) destroy the very machinery that would record it — so `not_emitted` **has no authoritative producer**, and I say so rather than manufacture it. The domain is now effectively **`present | unavailable`**: any attempt with no acquired DATA-P object (for ANY reason — loss or capture failure, not distinguishable without a positive producer negative) is **`unavailable ⇒ no record`**. **Consequence, stated: N910 / T9 is currently UNREACHABLE at the classifier, and the sink authors NO N910 record** until an authoritative DATA-P-non-emission fact is **routed to master** (a real cross-domain dependency, the same shape R2 was before r7 — and the mirror does NOT solve it, since N910 has no reply at all). The **epoch cut (T8) is corrected**: its `m8.dataP_reply.v2` epoch reply is a real `present` object, never `not_emitted` (r17 wrongly grouped it), and an unavailable epoch reply is `unavailable`, not authoritative absence; only T9 ever needed DATA-P `none`. `m10_row_state` is compared **after** classification (RS0–RS3), never manufacturing the classifier's no-message input. **The 2a/2b clean bind is unaffected** — reject replies are `present`, so R2's `refusal_stage` decode and the m-10-B validation-against-`presence_class` stand. **R16-F2 preserved** (N910's `m10_row_digest=absent` stays defined and correct for when the fact lands, simply not authored today); **R2/R3 producer bindings untouched** — m-8 r7 `734e44b7…` and m-10 rev3 `cd17db32…` reproduced byte-identical. **All three producer gates stay bound; N910 non-emission + the mirror trade are the two routed-out items for the addressed master return; nothing downstream moves.** r17 SHA-256 `b1f05d34abe59bf5a867ed5a57cc458ada0803d35133a4c4033313d499054ce3`.
- r17 (2026-07-24): M3-L2-R16-F1/F2 folded from `DESIGN-REVIEW-implementer-20260724-211500`. **I asked the reviewer to attack the §3.4a clean-bind ruling, and it found the real hole: my degradation was prose.** **F1 — the mechanism I cited did not observe the condition it claimed to.** r16 said an un-acquired DATA-P reply "degrades via P2b `unknown` / RS0 / RS2". But **P2b is decidability of the provider *wire-request* capture (predicate 1), not acquisition of the m-8→m-9 DATA-P *reply*; RS0 is a non-authoritative m-10 *row* read; RS2 needs `presence_class` to already exist** — none of the three observes the reply, and the consistency enum has no `unknown`. So the classifier's semantic `none` could absorb an *unavailable* reply and be classified as the authoritative no-message T8/T9 consume — the exact ambiguity I'd closed for governed reads (§2.4) and `m10_row_state`, left open on the DATA-P side. Fixed by defining a **closed DATA-P acquisition read (§3.2a)** — `present | not_emitted | unavailable` at the §2.4 protocol grain, explicitly *distinct* from P2b — where only an **authoritative** `not_emitted` (corroborated by `m10_row_state` for the loss cuts) may normalize to `none`, and **`unavailable ⇒ NO SINK RECORD`**. The degradation is now an executable pre-`Π` gate, not a claim. **This also decides the mirror question rigorously (reviewer's framing):** the clean bind is correct **under a STATED PREREQUISITE** — the sink requires authoritative DATA-P acquisition at the observer boundary, which §3.4's comparand already assumes (F84: the observer captures `m8_dataP_digest` independently) — so there is no self-certification (an un-acquired reply yields no record, never an accepted row), and the r7 `attempt_result.v2` mirror stays a **v3 coverage+independence enhancement**. But I now name the **honest cost I'd have buried**: without the mirror, an `unavailable` reply on a 2a/2b reject costs a record even though the m-10 row is authoritatively present; the mirror recovers exactly those. I flag that trade to master to own the route-now-vs-defer call, rather than unilaterally deferring. **F2 — N910's `m10_row_digest` was `per P1`**, which is not one of the sink's expected values (`<64-hex>|absent`) and confused producer-internal freeze with observed carriage: rows 9/10 have **no CTRL-C carrier**, so m-10 never receives B and the column is NULL ⇒ the expectation is carrier-derived **`absent`**. **The producer bindings are untouched** — m-8 r7 `734e44b7…` and m-10 rev3 `cd17db32…` reproduced byte-identical, R2/R3 stay bound; the blocker was my consumer closure, not their facts. **R1/R2/R3 all bound; nothing downstream moves.** r16 SHA-256 `e1a632e361d1906402a1a7216a27f9db1698a5b32f7c49b38bceed21c47e6199`.
- r16 (2026-07-24): the two producer gates **R2 + R3 discharged and bound**, per master routing `RECONCILE-orchestrator-planner-20260724-190000`. I reproduced both hashes on disk myself before binding either. **R2 — m-8 r7 `734e44b7…`** (pair-approved `…-174500`): the decoded 2a/2b discriminator I **routed rather than minted** (r5–r15 held `Π`'s T1/T2 unexecutable and refused to invent a third literal) landed as `refusal_stage ∈ {pre_freeze, post_freeze}`, with the invariant `refusal_stage = post_freeze ⟺ B∧E present`, emitted **independently of the digests**. `Π` is now total, T1/T2 executable, the classifier closed — **on m-8's bytes, exactly as F1's rail required.** The `internal_integrity_reject`/`pre_freeze_typed_reject` rows become real source rules keyed off `refusal_stage`; §3.2b's 2a/2b discriminant names it; the circularity the relabel flagged is closed by construction (key off `refusal_stage`, then *validate* B-presence against it). **R3 — m-10 B/E rev3 `cd17db32…`** (pair-approved `…-170000`): binds the sink's m-10 column — `m10_row_state ∈ {present, not_found}` is the authoritative row-existence fact my RS0–RS3 machine already consumed, and digest-members-conditional-on-`present` is the exact reciprocal of RS3; the m-9 carrier confirmed at r12 §6 (FX-BE-8). **THE CONSISTENCY QUESTION master routed, RULED (§3.4a — a clean bind, grounded not asserted):** m-8 mirrored `refusal_stage` onto `attempt_result.v2` so m-10's row B-presence would be independently validatable, but rev3 was approved against m-8 r5 and does not carry the mirror. I ruled it a clean bind **because my own approved §3.2b already fixes the 2a/2b discriminant as the DATA-P reply kind with no digest participating** — so the sink validates the m-10 B-presence against `presence_class` (from the reply's `refusal_stage`, an independent producer root) and **degrades via RS0/refusal/`unknown` rather than accepting the row's stored B** when the reply is undecidable. There is no self-certification to close, so the mirror is a genuine **v3 enhancement** (E3-vantage-independent 2a/2b resolution + a producer-fault cross-check between m-8's two carriers), recorded so it does not hide but **not held against this binding** — I refused both errors master named: binding around a real gap, and demanding a needless fold. **All three lane gates R1/R2/R3 are now bound; nothing downstream moves** (no §D join, no re-lock, no lock/PLAN/T4/code/credential/provider/E3/merge/deploy). §4's two-halves distinction is preserved with both halves now landed on their own bytes; §5 confirms m-8 r7 + m-10 rev3. r15 SHA-256 `d004dbc77e70477175a78a7aa649ad8cdb5bf33224ea4abff38ab3d818e54300` was byte-bound-approved and is superseded here by the substantive producer bindings.
- r15 (2026-07-24): M3-L2-R14-F1/F2 folded from `DESIGN-REVIEW-implementer-20260724-113000`. **Both findings are the same class — an OLD CONTRACT that survived the refactor that should have retired it** — and F1 is one I saw coming and shipped anyway. **F1:** re-encoding the markers (r14), I gave the condition-id registry a `currently` column reading MET / UNMET / UNMET beside R1/R2/R3. That is a **mutable, producer-derived evaluation living outside §0a** — precisely the drift path the registry was built to eliminate: §0a can move while the `currently` cell goes stale. Labeling it "a convenience pointer" did not change its semantics, and I had literally asked you in the r14 relay whether it "re-introduces the state-copy defect" — then left it in. **The lesson is the sharp one: naming a risk is not mitigating it, and a question to the reviewer is not a substitute for the fix.** The column is removed; the registry now holds only the closed id, its §0a anchor, and the condition's fixed meaning. When a fold needs current evaluations it **derives them from §0a at check time** (this fold: R1 = DISCHARGED AND BOUND · R2 = UNANSWERED · R3 = NON-BINDABLE, read straight from §0a's consequence column) and records them in the relay, never in a second durable table. **F2:** §0b step 4 still read "Verify the §0a rule mechanically, over the whole mutable-status vocabulary" — the exact whole-vocabulary proof capability r13 withdrew and r14 replaced with three bounded checks. Two contracts cannot both stand: the numbered executable procedure was instructing a future author to claim the vantage the prose three paragraphs below says the mechanism does not have. Step 4 now **runs the three checks actually defined** (Check A over its closed token/structure domain · Check B over the marked set with parse/membership/anchor/resolution · the watchlist as a regression heuristic) and states **in the step itself** that Class-B marker coverage is `self_reported` and non-exhaustive. **Verified unchanged from r14:** 17 structured markers all well-formed (R1=4, R2=5, R3=8 — matching the reviewer's reproduction); Check A table shape 5×5 and producer hashes zero; the coverage residual preserved. Editorial: `item 1 beloon` → `item 1 below`. **R1's binding untouched** — m-9 r12 re-hashed unchanged at `04422965…`. **R2 unanswered; R3 held; no m-10 byte bound; no downstream gate closed.** r14 SHA-256 `733b055be6a6e724db18ec4e40908b1c2ce3854de1b011ac0a2b9053d3a8f6b3`.
- r14 (2026-07-24): M3-L2-R13-F1 folded from `DESIGN-REVIEW-implementer-20260724-093000`. **The finding is R12-F1's shape one field lower, and it caught me making the same move I had just named.** r13 replaced the enumeration overclaim with three checks, and asserted Check B decides a two-field invariant: every `[CB]` statement carries a **condition** and a **§0a citation**. My verifier proved only the **citation** half — it counted markers and checked each line for `§0a`, and never checked the **condition** at all, because `condition` was unstructured prose (`iff` · `only when` · `once` · "the standing bar above" · a pointer to another paragraph). **A checker cannot decide those are complete conditions any more than the r12 checker could decide which prose words were status — the very abstraction gap I had claimed to close, reopened one field down, and my own verification's silence on missing conditions was the tell.** **Fix — make the condition STRUCTURAL, not semantic.** A **closed condition-id registry** now lives in RULEDEF: exactly three ids — `R1_RECIPE` (a current m-9 revision pair-approved **and** observer-executable) · `R2_DISCRIMINATOR` (m-8 publishes the decoded 2a/2b discriminator) · `R3_CARRIAGE` (m-10's B/E row pair-approved **and** reproduced) — each bound **once** to a §0a anchor, so the condition's *meaning* is fixed in one place and markers cannot restate (and drift) it. Every marker is now the parseable form **`[CB cond=<ID> ledger=§0a:<Rn>]`**, and Check B is **decidable over both fields**: the marker parses, `<ID>` is a registry member, and its `ledger` ref equals the id's declared anchor **and** resolves against §0a's consequence column (`R1 =`/`R2 =`/`R3 =`). Verified this fold: **17 markers, all well-formed — 0 parse-fail, 0 unknown-id, 0 anchor/ledger mismatch; all three anchors resolve.** (17, not the reviewer's 14, because the §4 heading carries both a recipe and a carriage marker and two lines legitimately double-mark adjacent clauses — reported, since a marker inventory that hides its own count is the same dishonesty one level down.) **What did NOT change, deliberately:** the coverage residual — Check B still cannot prove marking was exhaustive; marking is an authoring obligation and an unmarked statement is invisible to it, a `self_reported`-grade guarantee on coverage and a decidable one on form. Adding a fourth condition is now a governed edit to the registry, so **a marker can never silently invent a condition.** **R1's binding untouched:** m-9 r12 re-hashed unchanged at `04422965…`. **R2 unanswered; R3 held; no m-10 byte bound; no downstream gate closed.** r13 SHA-256 `5d12abdd013488a9028a92a549d6cec5a18d4e27f6b4eaa92a874d39436eb893`.
- r13 (2026-07-24): M3-L2-R12-F1 folded from `DESIGN-REVIEW-implementer-20260724-073000`. **The defect is one abstraction level above the last three, and naming it correctly matters more than the fix.** r10 and r11 failed on *spellings* — a hyphen, then a case — so r12 normalized case and separators and then claimed the check **"enumerates the status vocabulary actually present."** **It does not, and normalization cannot make it so.** A folded list is still **a list I chose**; deciding which arbitrary prose words carry semantic status is not a mechanical operation at all. The reviewer demonstrated it by naming live status language no list of mine contained — `BINDS` · `does not` · `THE CONDITION IS NOW MET` · `settled` · `observer-executable` · `CONFIRM` · `incomplete` · `NOT total` · `not executable` · `not complete` — most of it perfectly legitimate Class B. **So the error was not a missing word; it was claiming a vantage the mechanism does not have, in the verification contract of the domain whose entire thesis is that an unobservable input degrades and a check must never over-claim its own reach.** The r6 lesson was a search that could not fail; this is a search that could not *decide*. **Fix: three separately-named checks, each carrying its own limit rather than borrowing the others' credibility.** **Check A** — producer hashes, qualified producer revisions `m-<n> r<N>`, and the §0a **table shape** — a closed token set plus a structural invariant, so it is **complete over its domain** and a hit is a violation. **Check B** — the **`[CB]` marker inventory**: every statement carrying the literal marker must also carry a **condition** and a **`§0a`** citation on its own line; this is **complete over the MARKED set** and nothing more. **Watchlist** — the normalized phrase list, honestly relabelled a **regression heuristic** guarding the three historical spelling defects, **explicitly not an enumeration**. **THE RESIDUAL IS STATED, NOT CLOSED:** Check B cannot prove every mutable Class-B statement was marked — marking is an **authoring obligation**, and an unmarked statement is invisible to it exactly as an unobservable input is invisible to an observer. That makes coverage a **`self_reported`-grade** guarantee and form a decidable one, and it is labeled that way instead of dressed as completeness. The real gain over a watchlist is that the marked set is **decidable at all**: a reader can enumerate every conditional claim I make and check each against §0a without trusting my prose. **14 Class-B statements marked, 0 missing a §0a citation.** The r12 contract ambiguity is also resolved — "the mechanical check" named two different things; Class B is **outside Check A** (never a violation) and **inside Check B** (well-formedness only). **R1's binding untouched:** m-9 r12 re-hashed on disk and **unchanged** at `04422965…`. **R2 unanswered; R3 held; no m-10 byte bound; no downstream gate closed.** r12 SHA-256 `00cd995186dbbcd701cf687c75ffcbe36dc388b159d6290b2c62e4e964fb71c3`.
- r12 (2026-07-24): M3-L2-R11-F1/F2 folded from `DESIGN-REVIEW-implementer-20260724-053000`. **F1 — THE CANONICAL LEDGER WAS CORRUPT, and in the exact way it exists to prevent.** Folding R1's binding, I replaced the *front* of the m-9 row and left the r10-era tail attached: the row grew from four cells to six (**seven pipe delimiters against the header's five**) and simultaneously asserted **`R1 = DISCHARGED AND BOUND`** and **`SUCCESSOR PROPOSED AND UNDER PAIR REVIEW. NOT BINDABLE`**. A reader could take either. **The single source of truth held two mutually exclusive states at once — the precise failure §0a was built to eliminate — and my verification never noticed, because I checked hashes and vocabulary but never the SHAPE of the table carrying them.** Rebuilt as exactly one four-cell row, and **a table-shape assertion (every row matches the header's column count) is now part of the check** rather than something eyeballed. Line 17 also *denied the state it recorded* — "Nothing here is bindable" standing directly above a row recording a binding; it now says the honest thing: **appearing in the ledger grants no binding**, and `BOUND` means the pair-approved-plus-reproduced test was met and names its evidence. **F2 — the class split was right in principle and dishonest in execution.** §0b still carried the **old global guarantee** ("no duplicated mutable current-state — hash **or semantic status** — anywhere else") one paragraph above the new rule that *permits* conditional Class-B status outside §0a; both cannot be operative, and the guarantee is now stated **over Class A only**, with Class-B exclusion explicit. **And the status scan missed a live token for the third consecutive revision — each time on a spelling, never on a concept:** r6 a pattern structurally unable to match its target · r10 `non-bindable` against live `not bindable` (hyphen) · r11 `NOT CONFIRMED` against live `Not confirmed` (**case**) — while reporting **zero live status tokens**, which was false. The check now **normalizes case and separators before comparing**, **enumerates what is present**, and **reports and classifies every hit**; a zero-hit report is itself treated as suspect, since licensed Class-B status legitimately exists outside §0a and silence therefore means the spelling missed again. Classified output this fold: `partial` ×4 — **all non-status** (m-8's `cancelled{partial}` / `{partial:none}` schema tokens, a "partial write" sub-cut, a "truncated or partial response" domain member) · `pending` ×2 — §3.2a's conditional projection statement and §6's "not referenced as pending-here", both licensed · `not confirmed` ×1 — §5's m-10 row, conditional and citing §0a. **Class-A violations: zero**, with the rule-definition block deliberately left *inside* that scan. §5's m-10 row also leaked live producer state under a Class-B label ("the next pair-approved m-9 revision, which §0a **now records as existing**") — removed, its historical revision reference explicitly licensed rather than left violating the qualified-revision rule; and §4's `1, 2, 2` list is renumbered. **R1's binding is untouched and remains bound only to unchanged m-9 r12 `04422965…`; R2 unanswered; R3 held; no m-10 byte bound; no downstream gate closed.** r11 SHA-256 `3356f4213b31a1353dae8765d20c97fad4ed1f9c35c3ddeb227a357dcd97ebdb`.
- r11 (2026-07-24): M3-L2-R10-F1/F2/F3 folded from `DESIGN-REVIEW-implementer-20260724-033000`. **F1 — R1 IS DISCHARGED AND BOUND, the first binding in this lane.** m-9 r12 `04422965…` was pair-approved byte-bound with zero findings (`…-023000`, `6e3dd705…`) and m-9's return `…-033000` (`0a62be4d…`) discharged the routing, both crossing after my r10 handoff — **crossed mail, not a failure of the pre-handoff sweep**, and the reviewer was right to say so. **I bound it on my own consumer check, not on their account:** I read r12 §6 in full and confirmed each of the three m-9-owned members is executable at my observer boundary from request bytes alone — `instructions` verbatim from the static request field with the objective slice excluded; `policy_messages` the declared constant `[]` emitted by recipe, never by semantic judgement; `compaction_template` a versioned-sentinel byte-prefix scan that is total over none/one/many and **never needs to know the attempt kind**, with two-or-more ⇒ assembly refusal so no attempt identity exists to observe. With m-2's two arrays already folded, **all five members now have settled, observer-executable composition** — the same sentence r3 asserted falsely, made here only after reading the bytes that were missing then. **The difference between the two claims is the checking, not the confidence.** What did NOT move: m-10's carriage row (a settled producer half is not a settled carriage), R2, the §D join, and every downstream gate. **F2 — my r10 relay's claim that §4/§5 "carry no mutable status at all" was FALSE**, and the way it was false is the r6 defect again: live `PENDING`/`NOT CONFIRMED`/`PARTIAL`/`not bindable` survived across §3/§3.2a/§3.3/§4/§5, and my 31-pattern search missed them because **I searched `non-bindable` while the bytes read `not bindable`** — a hand-picked vocabulary that did not contain the words actually present. §0b now states **two honest classes**: Class A (producer revision/hash/verdict/gate state) is centralized in §0a and mechanically enforced; **Class B (m-3's own derived status) is explicitly NOT claimed absent** — a design record must be able to state its own completeness — but must be **conditional**, naming its condition and citing §0a for the evaluation. The check **enumerates the vocabulary present in the bytes** instead of testing a list I chose, and it immediately earned that change by catching a **factual error the chosen list could not**: §4 still read *"I bind none of them"*, which the binding above had just made false. Producer revisions are now written **qualified** (`m-<n> r<N>`), closing a real ambiguity — while both chains sat at r10, a producer revision could hide inside a scan for my own revision tokens. **F3** — the title said `(r9)` over r10 bytes: licensing self-description does not license *stale* self-description, and an excluded header must be internally exact before the exclusion is honest. Final state: **Class-A survivors ZERO** (producer hashes and qualified producer revisions, scanned with the rule-definition block deliberately **not** excluded), **live status vocabulary ZERO** outside §0a, scope asserted at 222/206 of 252 lines. **R2 unanswered; R3 held; no m-10 byte bound; no downstream gate closed.** r10 SHA-256 `2653cab4f08115b9b617796559136a145d7d4ba9ee4062ebddd5f564ee9bb542`.
- r10 (2026-07-24): M3-L2-R9-F1/F2 folded from `DESIGN-REVIEW-implementer-20260724-001500`. **F1 — the defect is the sharpest form of my recurring one: I authored the freshness procedure in r9 and did not run it on the fold that introduced it.** Four relays predated my `233000` handoff — VP `…-213000` (`4c254307…`) classifying A3/B1 **delegated-recipe design under F73 with no new operator gate**; master `…-220000` (`3a440c11…`) recording that as governing, withdrawing its crossed m-10 manifest steer, and directing ONE successor; m-10 `…-223000` (`9a9f9b15…`) standing down the §C.1 offer with no consumption finding and one assembly-time condition preserved; and m-9 `…-231500` authoring **proposed r11 `aa8f0130…`** (superseding r10, no implementer verdict). So §0a was wrong on four counts at once — classification, operator-gate possibility, current revision, and successor existence — and it retained master's earlier phrase *"ratified constant `[]`"*, which the VP expressly corrected to **"the Step-3 constant `[]` under the pair-reviewed m-9 §5-E realization; the member and the outer digest formula are operator-ratified."** That is authority language, not editorial drift: the first phrase claims operator ratification for a value the operator never ratified. **§0b is rebuilt from a principle into an executable sweep** — an enumerated dispatch set *including the Master/VP children filed in producer lanes*, ordered by the **timestamp embedded in the filename** (a filename sort places `DESIGN-…-231500` before `SITREP-…-153000` and hides the newest relay — a real defect caught in this fold's own first sweep), with the latest row/path **recorded per dispatch**, and **re-run immediately before handoff**, since all four missed relays landed inside exactly that window. **F2 — mutable current status removed from §4/§5 entirely.** §4 item 1, §4's chain bullet, and both §5 producer rows now carry **timeless dependency semantics** (the recipes do not bind until a current m-9 revision is pair-approved *and* independently observer-executable) with every revision number, live verdict, and gate state delegated to §0a. **The verification is now delimiter-scoped rather than phrase-scoped**, over the full mutable vocabulary — and it immediately caught what phrase-picking could not: an *unanchored* exclusion pattern matched §0b's own prose mention of the delimiter, re-opened the excluded region, and left **19 of 229 lines** under inspection while reporting clean. Anchoring to `^<!--` and asserting the operative line count fixed it, and the corrected run then surfaced two genuine survivors in the header block — now licensed explicitly as self-description behind `SELFDESC-END` rather than left as an unexplained exception. **This is the r6 unfalsifiable-search lesson recurring inside the very tooling built to enforce it, which is why the delimiters and the line-count assertion are written into §0b rather than remembered.** **And the re-run-before-handoff step earned itself on its first outing.** After §0a had been fully resynchronized to the four missed relays, the mandatory pre-handoff re-run surfaced **three further facts that landed while I was folding**: an m-9 implementer verdict over r11 (`…-003000`, **must-revise** on M9-DAG-R11-F1 — the Tier-2 fixture asserted an equality the recipe itself makes impossible), a further m-9 DESIGN (`…-011500`), and a changed artifact hash on disk. Had I handed off on the sweep I ran at the *start* of this fold, §0a would have been stale again — third consecutive revision — in exactly the way R9-F1 describes. §0a therefore now carries **r12 `04422965…`**, with the absence of an r12 verdict established by enumerating **every** `DESIGN_REVIEW_VERDICT` in the m-9 lane and confirming the newest binds r11's bytes, not r12's — a negative I can fail rather than one I assumed. **Nothing bound; R1/R2/R3 all still open.** r9 SHA-256 `b126a07d22d8f5dc77df25e0471aada6c34cbf85a87320bbc877a487ab5c7075`.
- r9 (2026-07-23): M3-L2-R8-F1/F2 folded from `DESIGN-REVIEW-implementer-20260723-223000`. **F1 — my canonical ledger was itself STALE, and the root cause is worth naming: I re-read the design artifacts and my OWN lane's relays, but master's §5-E merits ruling was filed in `step3-relock-dag-m9` and m-10's re-cut in `step3-relock-dag-m10` — both BEFORE my r8 handoff, both in lanes where I am CC.** Producer state that governs my dependencies is routinely decided in the *producer's* lane, so "re-read on disk at fold time" was a procedure scoped to the wrong surface. §0a resynchronized: **R1 = merits-RULED (A3 attempt-kind-total `compaction_template`, no `template_id`/version; B1 `policy_messages` = ratified constant `[]`; ruling `ec11b705…`), CLASSIFICATION pending at the VP** (master expressly declined to self-rule it; a VP→operator gate applies if either answer is a ratified clarification), m-9 hold obeyed via `36dd66b3…`, r10 unchanged/must-revised, no corrected successor; **R3 = m-10 B/E now `436016d8…`** (their `da6c8320…` re-cut the trigger revision-agnostically, superseding the `3826044e…` my r8 cited); R2 unanswered. **The "divergence is impossible by construction" claim is WITHDRAWN as false** — a central ledger is a cache of an external trail and can go stale, as these exact bytes proved; **§0b** now states the narrowed guarantee (no duplicated *mutable* current state inside this document — hash **or** semantic status) plus the three-step freshness procedure whose absence caused the defect, and **scopes the §0a rule to mutable current-chain state** so the legitimately-stable basis/F73 hashes elsewhere are not violations. **F2** — the two **semantic** duplications removed: §4's opening "are undefined" paragraph is explicitly **historicized to r5–r7** with the current account delegated to item 1/§0a, and §5's m-9 row now reads **attempted-in-r10-but-unapproved-because-the-recipes-failed-review**, never "undefined." **The r8 seven-hash search was structurally incapable of catching either** — it could prove hash tokens were removed but not a duplicated status expressed in words; §0b extends verification to semantic status phrases. **Nothing bound.** r8 SHA-256 `8db3968a896db153c7a1a50d8a4902cfaad50f32d0778f411cc54ad4548a20c5`.
- r8 (2026-07-23): M3-L2-R7-F1 folded from `DESIGN-REVIEW-implementer-20260723-203000`. **The defect, and it is the r7 lesson turned on my own fold:** r7's ledger refresh reached the top status and this fold log **but not the operative §3/§4/§5**, so the live artifact told **two different producer stories** — §3 still called m-9 r9 current, §4/§5 still cited the superseded m-10 `9caa3aec…`, and §4 still described the three outer members as simply *undefined*. **A refreshed status is not evidence that every operative statement was refreshed** — the same discipline r7 correctly applied to the delimiter, missed one layer up. **Structural fix rather than five spot-edits: §0a is now the single canonical producer ledger, and every live section CITES it instead of restating a hash** — so a future drift updates one table, not five loci, and divergence between them becomes impossible by construction. Ledger content re-read on disk at fold time: m-9 r7/r8/**r9 superseded**, **r10 `4490ba75…` current-but-MUST-REVISED** with m-9's addressed escalation `4c759ca7…` curing the CC-only routing defect (not the recipe defects) ⇒ **R1 answered-but-must-revised, awaiting master's ruling + a corrected successor**; **R2 unanswered** (no addressed m-8 response); **m-10 B/E now `3826044e…`** *(superseding the `9caa3aec…` my r5–r7 cited)*, proposed under live must-revise, receipt `117d48f0…` ⇒ **R3 non-bindable**. **§4's three-outer-member text recast from "unspecified" to ATTEMPTED-BUT-MUST-REVISED**, naming the four rejected points (Tier-2 summary vs always-empty compaction template · non-mechanical policy extraction · wrong `instructions` source scope · the unaddressed master ruling). All r7 mechanism fixes preserved verbatim. **Nothing bound.** r7 SHA-256 `b86c2bad204195cf9201a93455c98618d1c06b2ca10b62673edfa538c6885a3f`.
- r7 (2026-07-23): M3-L2-R6-F1/F2 folded + F3's ledger refresh, from `DESIGN-REVIEW-implementer-20260723-183000`. **F2 — the instructive one: my r6 verification was UNFALSIFIABLE.** The fold log and relay claimed the malformed delimiter repaired, citing `grep '****Row-state'` = 0 occurrences. **The actual bytes read `****The row-state` — a string my pattern could never match**, so the search was incapable of failing and I reported its passing as proof. The token survived at line 162 byte 493 (my r6 edit concatenated the new block onto the prior sentence). **This is the fifth instance of the completeness-word pattern and the worst kind: the previous four were claims without evidence; this one was a claim with FABRICATED evidence** — precisely the unfalsifiable-check defect this domain exists to catch, committed in my own document. Repaired for real (the block now begins on its own line), and re-verified with a search that *can* fail: the exact surviving token was located first, then removed, then its absence confirmed by the same pattern that had found it. **F1** — the fifth completeness word removed from the §3.2a heading ("over a total projection" struck; the heading now says the projection is PARTIAL/PENDING m-8); **§3.2b explicitly scoped** as total *conditional on `Π` having produced a recognized normalized tuple* — not end-to-end closure, since an attempt in the rows-1/2a/2b family cannot reach the table at all while T1/T2 are unexecutable; and the **status/relay/index claim narrowed from the absolute "no totality anywhere"** (itself an overclaim) to exactly `Π`/T1/T2/the end-to-end classifier, preserving the independently-justified totality of the verdict machines, the row-state machine, and m-8's settled cut list. **F3** — the producer ledger refreshed at fold time: **R1 answered-but-must-revised** (m-9 r10 `4490ba75…`, review `51375cfb…`), R2 still unanswered, R3 still non-bindable; r9 superseded. **Nothing bound.** r6 SHA-256 `dda9ad6cd3f179e345f6fd86ae6d95215d754a7a500cc111d7a80ac60bafc97c`.
- r6 (2026-07-23): M3-L2-R5-F1/F2/F3 folded from `DESIGN-REVIEW-implementer-20260723-163000` — **the fourth instance of my one recurring defect, found exactly where I asked the reviewer to look.** **F1** — r5 called `Π` "total, pure" **in the same section** that declared T1/T2 underivable from m-8's bytes; both could not hold. **All totality/closure language is withdrawn**: `Π`, T1/T2 and the classifier are **PARTIAL/PENDING m-8**, the two affected `Π` rows are marked *not executable* (and the `pre_freeze_typed_reject` row is named for what it still is — a category restatement whose "pre-freeze" input is the very pipeline position classification must establish), while the executable rows (epoch normalization, the CTRL-C tag projection + discards) stand. Routing a dependency never made the projection total. **F2** — the row-state machine is now **ordered and total**: RS0 non-authoritative acquisition (timeout · malformed · unavailable · ambiguous) ⇒ **no sink record is authored** (machinery failure is never classified as `not_found`); RS1 authoritative `not_found` is legal **only** with N3; RS2 `not_found` with P/A/N910 ⇒ **`inconsistent` at locus `m10`** (closing r5's global escape); RS3 `present` ⇒ digest required and compared. The **independent-locus rule** is stated: a false N3 `not_found` paired with an existing E0 event is `inconsistent` at locus `m9` on its own merits. The malformed `****Row-state` delimiter is repaired. **F3** — **carriage is removed from the "genuinely settled" set**: *m-9 → the m-10 attempt row and E0* is the **ratified architectural ROUTE**, not an exact realized carrier, and the exact member/presence/timing stays **PENDING**. Every status line is refreshed to the live chain, re-verified on disk at fold time: r7 superseded · r8 superseded/must-revised · **r9 `116eeffb…` proposed** · **m-10 B/E rev2 on disk at `9caa3aec…`, not pair-approved** — and I record that my reviewer's cited `b8d0dc91…` no longer matches disk, which is precisely why only pair-approved hashes get cited and every citation is re-verified at fold time. **F4** — the addressed escalation is real and unanswered; the three producer dependencies stand. **Nothing is bound.** r5 SHA-256 `47bf203448d9178a21fffb9d36f0289b401c442cf1a45dbc4ebc77876ad6c879`.
- r5 (2026-07-23): M3-L2-R4-F1/F2/F3 folded from `DESIGN-REVIEW-implementer-20260723-143000`. **F1** — the classifier is renamed to an explicit **m-3 NORMALIZATION** (r4's "producer literals" was false: `pre_freeze_typed_reject` is my category, the epoch values are `STALE_EPOCH`/`EPOCH_AHEAD`, and the CTRL-C payloads were silently flattened) and given a **total pure projection `Π`** from exact decoded producer objects, stating **what it discards and why** (reason/token payloads must never reach classification); unrecognized source shapes **refuse before classification**. **The one missing producer fact — m-8's decoded discriminator between the post-freeze `internal_integrity_reject` reply and the pre-freeze typed rejects — is ROUTED to master for m-8, not minted**, and T1/T2 are stated as **not yet provable** from m-8's bytes. **F2** — the sink gains a closed **`m10_row_state: present|not_found`** with `m10_row_digest` present only when `present`, so **N3's legitimate no-row branch is representable** (a stale-rejected `attempt_open` commits no row); a `not_found` row contributes nothing to consistency and is never expressed by overloading `absent`/`no_message`. **F3** — the m-2 F73 row corrected (m-3 binds only through a future current pair-approved m-9 hash; neither superseded r7 nor must-revised r8), and **the master escalation r4 merely CLAIMED is now actually filed** as an addressed relay (`SITREP-planner-20260723-151500`) — CC is context and creates no obligation; asserting an escalation that did not exist was the error. r4 SHA-256 `589964063f3fa21a4985d6c1cc7dfbec23b714b43aa9a2af35943afd74d10918`.
- r4 (2026-07-23): M3-L2-R3-F1/F2 folded + **master's D4 ruling folded verbatim** from `DESIGN-REVIEW-implementer-20260723-113000`. **F4/D4** — ruling `e710da6d…` reproduced on disk and quoted into §1: the no-carrier precondition is the correct realization **within** the ratified parametric rule (no amendment required; **m-8 no-digest-carrier ⟺ m-3 absent-schema-valid**) — the F1 hold is RETIRED and rows 9/10 are settled as absent-schema-valid. **F1** — the classifier is now genuinely closed: three **closed enums** plus the **complete allowed-tuple set T1–T9**; every unknown, malformed, or well-typed-but-unreachable tuple makes the record **INVALID/refused** rather than being coerced into a class. **My invented literal `internal_integrity_fault_prefreeze` is WITHDRAWN** — m-8 labels rows 1 and 2a alike, so one honest kind `pre_freeze_typed_reject` covers both (they share a class and a vector; no discriminant is owed, and inventing a producer literal was the error). **F2** — each member is bound to its **exact carrier object** and renamed accordingly: `m8_dataP_digest` = the DATA-P reply/terminal **union** (which is what lets N3's epoch reply justify `absent`); `m9_e0_digest` = the E0 record, so **N3 is `no_message` not `absent`** (no E0 event exists in which a member could be absent — r3's error); `m10_row_digest` = a **durable row column** whose absence is `absent`, with **`no_message` removed as illegal** for it (source-frame absence is already carried by `classifier.ctrl_c_disposition = none`, so the member is never overloaded); m-10's expected states are left **deliberately unstated** pending its pair-approved row. **F3 → §4 RETURNED TO PARTIAL: the r3 completion claim is withdrawn as an overclaim** — m-2 settles two of five members; `instructions`/`compaction_template`/`policy_messages` are m-9-owned and undefined (each occurs once, in the outer declaration alone), so observer re-derivation is not reproducible; and **no hash is bound** because r8 `563398c0…` reads `PROPOSED` and r7 is superseded. r3 SHA-256 `115fe1420f592a4b37bda8d95bf266f04c28ab8c7a279635ae60259ebc7ad8c4`.
- r3 (2026-07-23): M3-L2-R2-**F2/F3** folded from `DESIGN-REVIEW-implementer-20260723-050000`, and **m-9 r7 `f191c69c…` BOUND** per master routing `step3-relock-dag-m9/RECONCILE-…-093000`. **F2** — classification is now a **closed tuple** (`data_p_reply_kind` · `stream_terminal` · `ctrl_c_disposition`) mapped injectively onto a four-member `presence_class` {P·A·N3·N910}; the **2a/2b discriminant is the DATA-P reply KIND**, never digest presence (closing the circularity — both rows share the CTRL-C token, only the reply kind differs); **rows 9/10 need no discriminant, and that is PROVED** (identical expected per-point vectors ⇒ identical sink output), while **row 3 IS separated** by its epoch reply's existence; the note that m-10's terminal state alone cannot separate 3/9/10 is recorded for master rather than self-asserted. **F3** — `pending_producer`/`indeterminate_pending` are **removed from the runtime schema** (DAG state is design status in prose, never an attempt value that would permanently weaken the record); consistency is now the **expected PER-POINT vector compared point by point**, so `absent` and `no_message` are no longer interchangeable and **a wrong route is `inconsistent` even with no hex digest**, with `inconsistency_loci` naming every diverged point. **§4 COMPLETES** — r7 §6 folds m-2's component (`c3a8cd61…`, pair-approved) discharging §9 item 1, so all five hashed members have settled composition and the r1/r2 partial caveat retires; r5/r6 are void and uncited. **STILL OPEN: F1** (rows 9/10's producer-conformance consequence — HELD for master's exact D4 disposition, to be folded verbatim; m-3 does not create the precondition locally) and **§3's m-10 carriage column** (m-10 signalled; binds on master's routing). r2 SHA-256 `5b96673b2abdc7616fe9954df8bc7a669d9318b8b76b86e5e64b3a040e183b51`.
- r2 (2026-07-23): M3-L2-R1-F1/F2 + the sink's local defects folded from `DESIGN-REVIEW-implementer-20260723-030000`. **F1** — §1 is now the dispatched **cut × CARRIER** matrix (exact DATA-P + CTRL-C route per row); **row 3's factual error corrected** (a `m8.dataP_reply.v2` epoch reply IS emitted — only the CTRL-C `attempt_result` is absent; my r1's "no m-8 carrier exists" was false); the **three absence classes** separated (carrier-with-absent-members · no-digest-bearing-carrier-but-a-message · no-message-at-all), all yielding an **ABSENT, schema-VALID** field — never "forbidden," which would make an honest observer record malformed; m-8's `freeze = maybe` (rows 9/10) is **no longer converted** into a boolean or a forbidden claim, governed instead by an explicit **rule precondition** (the rule's subject is a carried value; no digest-bearing carrier ⇒ nothing to carry) with the contract-integration question escalated to master rather than self-declared. **F2** — the governed-read domain is redefined at the **protocol grain**: `unavailable` is a positively-defined closed catch-all (truncated · malformed · unknown-kind · validation-failing · ambiguous-multiple · transport/timeout/refusal), so a transport that completes with non-validatable bytes is `unavailable`, never `not_found`; the five branches are exhaustive over the closed domain. **F3 (local half)** — `m3.b_sink.v1` is now a closed record with exact types/enums; `cut_row` carries its **attempt-bound source + value** (independently reconstructible, never self-declared); consistency is **total over `expected_presence` × every carriage combination** including the invented-carrier case (a digest where none may exist) and mixed-presence; derivability names the **producer-root `m8_terminal_digest`** as canonical comparand and is **independent of consistency** (an `inconsistent` ∧ `derived_equal` record is a real, useful diagnosis). **F3/F4 (sequencing)** — both producer inputs are marked **PENDING, not confirmed**, and the two stale premises are escalated: m-10 rev6 §10 parks the B/E rows the dispatch assumed; m-2 is **already settled** (`c3a8cd61…`, pair-approved) so the real chain is master → m-9 fold → m-3 bind, not "wait for m-2." **This artifact does not claim lane completion.** r1 SHA-256 `87172ca11a28279973d0b915e840f59877d1152033265126819274578c3bd74a`.
- r1 (2026-07-22): the parked set folded on the unblock dispatch `…-234501` against **settled** bases (m-8 r5 `c0b7b488…`, m-9 r5 `c0ff74f5…`, both hash-verified) under the **ratified** v2 contract `6e2abe40…` (amendment rev3 `9e874df8…`). **§1** the exhaustive ten-row cut-matrix — the parametric FREEZE-REACHED rule instantiated over settled cuts; the r0-F2 defect closed at its root (rows 1/2a/2b all surface `phase=failed` yet differ in presence — presence is read, never inferred). **§2** five total ordered verdict machines, with D2's gating split stated (1/3/4 gating; 2/5 recorded-only), the closed governed-read domain `committed|not_found|unavailable` and its completion-is-the-discriminator rule, and m-9's `content_id` hold adopted verbatim. **§3** the `m3.b_sink.v1` record with total consistency/derivability rules incl. the mixed-carriage case, at F5 authority (a record, not a join, enforcing nothing). **§4** the recipe-binding filed as **PARTIAL with the residue named + escalated** — m-9's own §6 parks the m-2 component, so two of five hashed members are unsettled and no gating claim rests on re-derivation. **§5** F73 confirmations, m-2 explicitly NOT confirmed. r0 SHA-256 `dc3b6eb359909fe351fb20f5aa774ba3e87ae16c2861e8e6520adb1b177a7f7e`.
