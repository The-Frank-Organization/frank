# m-10 stage-6 lane-2 producer delta — item-D manifest/lifecycle · C ticket descriptor · M10-C0/C1/C2 (the governed delta over r40 + r10)

**DESIGN_DOC_ID:** step3-relock-dag-m10 · **rev16 — folds M10-DAG-R15-F1: the cap terminal's ratified no-revival acceptance predicate.** rev15 committed the `parked_unknown_capacity_exceeded` terminal's `runs.state = FAILED` + `stop_reason` transition and no-successor-generation, but OMITTED ratified Correction 2 §2.2 rule 3's complete lifecycle negatives (no same-run continuation, no lease, no snapshot, no revival) and rule 4's acceptance predicate; the only complete no-successor/no-lease/no-snapshot/no-revival language in rev15 was attached to the DISTINCT `resume_frame_overflow` continuation-sizing terminal, which cannot prove the cap terminal's own ratified lifecycle. **This is exactly the amendment's timeless-fold rule: a fresh successor folds the ratified packet's COMPLETE terminal semantics + acceptance predicate, not only the pre-ratification mechanism's status flip.** rev16 folds the ratified predicate at its two operative sites: **§4's cap-terminal clause** (`:72`) gains the no-revival rule verbatim (no successor generation, no same-run continuation, no lease, no snapshot, no revival — any next work is an ordinary NEW run) + the enforcement-threshold-NOT-512-row-storage-cap clarification (the full retirement batch commits and the complete parked set stays queryable past 512, §2.3) + an explicit statement that this is DISTINCT from `resume_frame_overflow` (shared no-revival/new-run-only lifecycle, different loci, neither's fixture proves the other's); **`FX-M10-CAP`** extends leg (ii) to observe the FULL ratified acceptance predicate in the ONE transaction (complete retirement batch · SINGLE `runs.state=FAILED` + `stop_reason=parked_unknown_capacity_exceeded` terminal · no successor/continuation/lease/snapshot/revival · durable operator projection rendering the terminal + the COMPLETE queryable parked set) and NOT by reusing the `resume_frame_overflow` fixture, and extends leg (iii) (the multi-row overshoot) to assert no successor/lease/snapshot/revival. The packet-bound status, the two compile-time assertions, the §D join gate, and every other byte are UNCHANGED from rev15 — only §4's cap-terminal clause + the two `FX-M10-CAP` legs moved. **Verified real at the ratified packet `1fa71cb8…` before folding** (§2.2 rule 3 lines 36–37 "no successor generation, same-run continuation, lease, snapshot, or revival"; rule 4's acceptance predicate; "MAX_PARKED_ROWS_PER_RUN cannot be implemented as a 512-row storage cap"); the stronger rev15 language on `resume_frame_overflow` (`:59`) is left intact and distinct. Sweep discipline applied (the recurring lesson): case-insensitive concept sweep for the no-revival predicate + a `**`-balance check; the resume_frame_overflow terminal keeps its own no-successor language, the cap terminal now carries its own. rev15 = `4cf52859087aed8f00a7c25585c1fb102c503d2ebb176d1d2123427e56605094` (ancestry, superseded). *(Ancestry fold-log, retained for lineage:)* rev15 — THE FRESH POST-RATIFICATION FOLD (the propagation-matrix successor; NOT a relabel of rev14). The §D-settlement amendment is **operator-ratified 2026-07-25** (packet `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b`, recorded §8b at `step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000`), which OPENED the owner fold matrix. Per master's rule, **no pre-ratification working revision (rev1–rev14, incl. rev14 `b96a1511…`) is a durable fold — they are ANCESTRY ONLY**; this revision is m-10's single fresh, pair-reviewed successor folding the ratified corrections **as IN FORCE**: **Correction 1 (producer-side D-4 Gate-2 claim relabel)** and **Correction 2 (run-wide producer restore + `MAX_PARKED_ROWS_PER_RUN=512` + the `parked_unknown_capacity_exceeded` terminal + the two compile-time frame assertions + the fixtures — §2.3 boundary/multi-row/no-prefix predicate, §2.4 production max-witness `≤ FRAME_CONTENT_BOUND` + reduced-table exact-fit)** are now **RATIFIED architecture-of-record, no longer "amendment-borne / pending operator ratification."** The mechanism, schema/presence rules, and every fixture are byte-carried from the rev8→rev14 lineage that authored them (they FED the amendment); only their status flips pending→ratified, the basis binds the ratified packet, and the header records the fold. **The §D-seam FRAME normativity (S-1/S-2/S-4/S-5) is UNCHANGED — still JOINT-PENDING until this successor's pair approval AND the §D two-sided join co-sign, which follows both owners' folds; ratification of the amendment did NOT retire the join gate.** rev14 = `b96a1511d01545d58adabb52a80ee5d6f62aba6defc7f7572e7a5e30025a7ddc` (ancestry, superseded). *(Ancestry fold-log, retained for lineage:)* rev14 folded the r13 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-201500` (M10-DAG-R13-F1, verified at rev13 line 152): my rev13 added the correct per-seam provenance but **left a trailing lowercase restatement of the very blanket claim it removed** — "so S-1/S-2/S-4/S-5 are exact-folded, not normative, until then" — sitting one clause before the correct per-seam text, an internal contradiction; and my "grep 0" verification missed it because I checked the UPPERCASE string and the survivor was lowercase. rev14 replaces that trailing clause with the per-seam statement (S-1/S-2 exact-folded in r12; S-4 pair-settled/m-10-folded but PARKED on m-9 pending my hash + their rebase, `:472`/`:474`; S-5 pair-settled/m-10-folded under the master/pair records; all four non-normative until their folds/approvals AND the join), fixes the `****each` quad-asterisk delimiter, and this time the sweep was **case-insensitive + a `**`-balance check** (32 runs, 0 quad; the only live "exact-folded" survivors are the correctly-scoped S-1 (`:32`) and S-2 (`:84`) receipts). **LESSON (this is the THIRD consecutive round my grep verification under-matched — R11 "normative now", R13 lowercase "exact-folded"): the verifying grep must be case-insensitive AND synonym-inclusive, and a delimiter-balance check belongs in the same sweep; "grep 0" on one casing of one phrasing is not a clean-sweep proof.** Labels/mechanism/detector/local bytes/every fixture byte-for-byte from rev13; only the trailing §9 clause + the delimiter moved. rev13 = `c2b8819c3d8f901fbc3aebad3bdb7725813afc67aed79fc7c3ce99e3b81022fd` (superseded). *(Prior fold, retained for lineage:)* rev13 folded the r12 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-194500` (M10-DAG-R12-F1, verified at m-9 r12 `:472`/`:474`): the S-1..S-5 status LABELS are all correct (no premature normativity), but the **PROVENANCE was over-broad** — I cited the m-9 r12 receipt ledger `:299`/`:473`/`:511` as supporting the S-4/S-5 exact-fold state, when those loci fold only the S-1/S-2 receipts (ledger items 4-5). r12 `:472` expressly PARKS item 3 (my D2 manifest wire schema = S-4) and `:474` says m-9 rebases to my pair-approved hash before the join. rev13 scopes the citations exactly: **S-1/S-2** cite the receipt ledger; **S-4** is stated as pair-settled + folded on m-10's side but **PARKED in m-9 r12** pending this pair-approved hash + m-9's rebase/fold; **S-5** binds to the master settlement records + the pair-axis settlement (m-9 confirmed the recompute-verify buildable at `…231500`), not the receipt ledger; §4's S-2 "every §D-settled frame" tightened to the disposition-receipt-item-4 scope + the general gate rule; S-3 stays separate. The status LABELS, mechanism, detector, local recipe/carrier bytes, and every fixture are byte-for-byte from rev12 (all passed r11/r12); only the provenance/scope of the S-4/S-5 fold citations moved. rev12 = `b82cf76edd0c0641fe0c9ca971b1b87ed0c00fc18c85d01a82815824c3be889f` (superseded). *(Prior fold, retained for lineage:)* rev12 folded the r11 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-191500` (M10-DAG-R11-F1, verified at rev11 line 110): **the incomplete-sweep recurred one layer down — my rev11 grep pattern ("NORMATIVE at this revision" / "all SETTLED and folded") did not cover "normative now", so a THIRD S-5 site survived.** §5's S-5 `assign` carrier said "the producer contract (this recipe, this carrier) is **normative now**" — advancing the S-5 writer-to-reader contract before the same §D join §9 holds pending. rev12: §5's S-5 status is now EXACT-FOLDED / JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D co-sign, with the **m-10-LOCAL recipe** (run-start input · realpath/NFC/SHA-256 · immutable §C.1 manifest pair) named as the one independently-normative part and the **joint `assign` carrier** (member encoding + worker recompute-verify) explicitly EXCLUDED from that local claim and riding the join. Also fixed the stray `**.**` markdown emphasis delimiter at §9 (mechanical, not semantic). **LESSON, sharpened: a status sweep is only as complete as its PATTERN — I grepped the two phrasings I had used and missed a synonym; the fix is to sweep the CONCEPT (every way "normative/settled" is asserted), then grep-prove the residue with a synonym-inclusive pattern, which this revision did (`normative now|is normative|are normative|folded normative`).** The r12 basis, the widened mechanism, the detector label, the local recipe/carrier BYTES, and every fixture are byte-for-byte from rev11 (all passed r10/r11); only the §5 S-5 status wording + the §9 delimiter moved. rev11 = `8a26fd014da6598fea3a5c0407fe7c85b723b9befc6677a7bbe096cf60795d47` (superseded). *(Prior fold, retained for lineage:)* rev11 folded the r10 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-182000` (M10-DAG-R10-F1): my rev10 fold of R9-F1 was INCOMPLETE — I corrected the S-1 sites (header/§2/§9 seam-list) but **the same join gate governs S-2/S-4/S-5, and two operative assertions still collapsed it**: §4 called the S-2 report/receipt/conflict frames "SETTLED … NORMATIVE at this revision", and §9's seam-list sentence swept "S-1 through S-5 are all SETTLED and folded normative above" (both premature, and the §9 blanket also wrongly folded S-3 into the join gate). rev11 corrects both: §4 S-2 is now EXACT-FOLDED / JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D co-sign; §9 states seam-specific truth — S-1/S-2/S-4/S-5 exact-folded/joint-pending until the join, S-3 stated on its OWN status (five families folded, `relay.*` held for m-2's ratified shape, not under the join gate). **LESSON (the recurring incomplete-sweep class at this seat): a status correction is a claim about EVERY site the status appears, not the first one — I swept the S-1 label and stopped, when the join gate is one fact governing four seams.** The r12 basis, the widened complement-of-equivalence mechanism, the detector label, and every fixture are byte-for-byte from rev10 (all passed r9/r10); only the two remaining status assertions moved. rev10 = `56124f42c11402c915fa71e8f35c44650161f1dcb79deb7f1977b1a9a58c2540` (superseded). *(Prior fold, retained for lineage:)* rev10 folded the r9 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260724-171500` (M10-DAG-R9-F1, verified at m-9 r12's own bytes): rev9 advanced the S-1 status ONE GATE too early. It rebased the basis r7 → **pair-approved r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`** and correctly established that m-9 r12 §2's predicate **MATCHES** mine (both documents describe one complement-of-equivalence predicate) — but "the predicates match at pair-approved bytes" is NOT "the seam is normative". **m-9 r12 itself preserves the fuller gate:** its §2 joint-status (`:299`) makes the frame normative only when **both pairs approve the same exact bytes AND we co-sign the §D join record**; its ledger (`:473`) keeps the settled frames **EXACT-FOLDED, JOINT-PENDING, NOT NORMATIVE** until those two conditions hold; its owed-list (`:511`) still awaits m-10's pair-approved hash **plus** the §D co-sign. At rev9's handoff m-10 pair approval did not yet exist and the §D join is absent (sequenced after the VP/operator-gated §D-settlement amendment). **So this revision RESTORES the honest status: S-1 is PAIR-MATCHED / EXACT-FOLDED at pair-approved r12 bytes, and JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D join co-sign — the join, not this document, retires the status.** The r12 basis, the widened complement-of-equivalence mechanism, the whole-tuple not-producible detector label, and every fixture are preserved byte-for-byte from rev9 (all passed the r9 review); only the settlement-status wording is corrected. rev9 = `bc39cc3ad5a3e979b9ed7559b7475252882b6cfc32f8f4bff2f241e7838c76d1` (superseded). Carried from rev9 — the r7→r12 rebase over: folds the r7 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260723-114500.md` (M10-DAG-R7-F1..F4, all four verified at my own bytes before folding): **F1** the new terminal was absent from the closed `runs.stop_reason` declaration — the store delta is now **TOTAL over `{resume_frame_overflow, parked_unknown_capacity_exceeded}`** with exact present-iff rules and `resume_action = operator_new_run` kept exclusive to the overflow terminal; **F2** FX-M10-CAP enforced an effective cap of **511** (MAX−1 plus one = 512, which is not *greater than* 512) — both boundary legs are now exact (511+1 continues; 512+1 terminalizes) plus the load-bearing **multi-row overshoot** leg (a batch from 510 commits in full, the count legitimately exceeds the cap, no truncation/prefix path exists); **F3** the live fixture still asserted the superseded **3,764,736 B** while the normative bound is **3,704,832 B**, and my rev7 header had retroactively rewritten the r4 history to the new number — the fixture is corrected, **r4's history is restored to the value its bytes actually carried**, and the rev7 recomputation is recorded as its own supersession (below) rather than as an edit of r4; **F4** the S-1 conflict branch fired only on a differing `marker_digest` while equivalence compared all six evidence members, leaving a same-key/same-marker/different-locator receipt matching **no** branch — the conflict branch is now the exact complement of equivalence (**any** differing member), with the correction **routed to m-9** since their producer prose carries the same asymmetry. **The rev7 frame-bound supersession, stated as its own record:** the run-wide restore moved `PARKED_MAX` 81,920 → **327,680**, hence `ADMISSION_REF_ENC_MAX` 2,809,856 → **2,564,096** and `FRAME_CONTENT_BOUND` 3,764,736 → **3,704,832**; the earlier values remain correct as the history of the bytes that carried them. rev7 = `8ce78381f901861322f28deb80ac2c7b88f4c6213c49dc6dd01783a525d696bb` (superseded). Carried from rev7 — the §D-settlement outcome: master's four rulings (`step3-relock-dag-m10/RECONCILE-orchestrator-planner-20260722-230000`, confirmed with the branch-6 correction accepted at `…-20260723-001500`) + the closed m-9/m-10 pair settlement + m-1's CONFIRM. **(1) D-4 Gate-2 HONESTLY RELABELED** (Gate 1 delivers the disclosure guarantee; Gate 2 = fail-closed validator + drift-detector over MVP-unreachable states; the comparator bytes STAY) — a ratified-claim change, so it takes effect on master's bounded §D-settlement amendment reaching **operator ratification**; carried here as the ruled disposition, NOT self-ratified. **(3) The rev2–rev6 D-4 SCOPING IS WITHDRAWN — run-wide carriage RESTORED on both gates** (back-to-ratified, worker-independent per r40 `:72`), bounded by the ruled **`MAX_PARKED_ROWS_PER_RUN = 512`** enforced at the single growth site with the typed loud run terminal **`parked_unknown_capacity_exceeded`** (also amendment-borne); `turn_disclosures` KEPT for byte-identical `turn_open` replay; a **SECOND compile-time assertion** now covers `attempt_open_ok`, which had no sizing gate at all (m-9's check (i)). **(4)** the five settled action families stand; the `relay.*` `canonical_resource` cell is HELD pending m-2's master-routed shape (bind a form-schema target, not `∅`). **Settled seams folded NORMATIVE:** S-1 receipt frame · S-2 report + `disposition_conflict` carrying the committed pair · S-4/S-5 confirmed. **FX-M10-D4 leg (b) CORRECTED** — the `changed` fixture was unbuildable (both pairs' independent traces agree the branch is MVP-unreachable). **m-1 ruled CONFIRM** on all three surfaces with one binding carrier condition, folded as explicit negatives. **m-9's basis REBOUND r5 → r7 `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`** (r5/r6 void; the §D-settled seam semantics are byte-unchanged across their rebase — verified at their bytes, not assumed). The **B/E carriage** is authored SEPARATELY per master's signal (`2026-07-23-lane2-be-carriage-row.md`), so §10's parking discharges through that artifact, not here. rev6 = `29a123fe8f153127ccfe03559850c8e5ce4c488671402cebf8325e3cafbcd7ae` (superseded). Carried from rev6 — the r5 fold: the r5 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-170000.md` (M10-DAG-R5-F1/F2, both verified): F1 the terminalization fixture demanded an ILLEGAL emission (a terminal state is outside the closed wire domain) — the leg is split into the two real branches: terminalization ⇒ ABSENT from live Gate 2, the m-9 `removed-only` path + conservative-superset proceed rule; `UNKNOWN_TOOL_OUTCOME → PARTIAL_TOOL_EFFECT` ⇒ the `changed` path (block/surface/reassembly); byte-identity claimed for the snapshot-backed `turn_open` ONLY, `attempt_open_ok` asserted as live re-derivation + comparison outcome; F2 the live confirmation route re-based — the D-4 m-9 confirmation runs over THIS rev's `turn_disclosures` + split source rules (the rev4 `disclosed_by_turn_id` relation is labeled history, no live obligation names it), and the m-9 status citations updated to their PAIR-APPROVED producer delta r5 @ `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b` (hash reproduced on disk this revision; a pair-approved producer PROPOSAL, expressly NOT joint settlement — the seams stay parked). rev5 = `7feef4035a93a77c4e191ed0d87e312477e952c66ddf214d89a0e0399704f1ce` (superseded). Carried from rev5 — the r4 fold: the r4 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-153000.md` (M10-DAG-R4-F1..F3, each verified): F1 the relation froze MEMBERSHIP but not the mutable six-field BYTES — the admission transaction now persists the exact six-field **disclosure snapshot** per stamped row (`turn_disclosures`); `turn_open` replays from the SNAPSHOT (byte-identical though the live row terminalizes); `attempt_open_ok` is LIVE-derived at each emission (the frozen Gate-2 equal/added/changed/removed totality PRESERVED — the rev4 "byte-stable within the turn" claim withdrawn; m-9 r21 treats the two frames as distinct snapshots and this delta keeps that true); F2 the `MAX_LEGAL_FRAME` equality pin was a sum of `≤`-ceilings with no attaining witness — DEMOTED **at that revision** to the conservative upper bound **`FRAME_CONTENT_BOUND = 3,764,736 B`** (the value the r4/rev5 bytes actually carried, under the then-current scoped `PARKED_MAX`; **rev7's run-wide restore recomputes it to 3,704,832 B — a NEW supersession recorded in this header's rev7 entry, not a retroactive edit of this one**) with NO equality requirement; the production leg builds the defined max-witness vector, asserts commit + measured size ≤ the bound; exact-fit/one-over stay under the injected test table; F3 the root classification is now an ORDERED first-match table — absolute-form check BEFORE any filesystem call (ambient cwd unreachable), one realpath attempt (every failure incl. OS length errors ⇒ `unresolvable`), resolved-`/` ⇒ `filesystem_root` (covers symlink-to-root), then grammar BEFORE length (`out_of_grammar` wins the overlap; `too_long` = the post-resolution output check ONLY). rev4 = `25f3eefbdca1dd0f625109a9d80943bdcb91cec4ed0e2f110bb2754aa1ec775e` (superseded). Carried from rev4 — the r3 fold: the r3 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-120000.md` (M10-DAG-R3-F1..F3, each verified): F1 the same-transaction `disclosed` marker erased its own carrier — replaced by the durable **`disclosed_by_turn_id` relation**: the admission transaction stamps each selected row with the successor `turn_id`; the frame for turn T reconstructs from `disclosed_by_turn_id = T` (its OWN rows, byte-identical across send/replay/recovery), later frames select the un-stamped set; `attempt_open_ok` carries the current turn's stamped set (byte-stable within the turn); the changed D-4 producer semantics NAMED for m-9 confirmation (their r21 consumes the two-gate contract); F2 the production `FRAME_MAX` exact-fit was arithmetically unreachable (actual maxima sum to 3,764,736 B) — the boundary rule is now: **`MAX_LEGAL_FRAME = 3,764,736 B`** derived from actual (not ceiling) maxima, the production leg builds and commits THAT frame and regression-pins its size; exact-fit-at-`FRAME_MAX` + one-byte-over run ONLY under the build-tagged reduced limits table with real closed-shape bytes; the compile-time assertion stays over the conservative enforced ceilings; F3 the root recipe made TOTAL — `/` EXCLUDED with a typed refusal, and the closed pre-admission refusal family `workspace_root_invalid{reason ∈ {not_absolute, filesystem_root, unresolvable, out_of_grammar, too_long}}` covers realpath failure and post-normalization violations with zero run/manifest effects. rev3 = `1c25e82fa5e8df3c08895e0ce73da8b7ccb24714143ac1edf052627d3c33f213` (superseded). Carried from rev3 — the r2 fold: the r2 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-100000.md` (M10-DAG-R2-F1..F3, each verified at the cited bytes): F1 the orphan-`VOID` mapping reconciled with the byte-bound M10-C1 seam — the decidable split `void_reason × canonical tool identity`: the four denial reasons ⇒ `determinate_no_resume` (any tool); **`expired` × the three relay verbs ⇒ `uncertain`** (the confirmed cut-relay disclosure preserved; ordinary expired relay tickets over-surface, accepted-safe); `expired` × local tools ⇒ `determinate_no_resume`; §1/§7/fixtures now agree and no broker event is a producer input; F2 the frame proof redone over exact JCS-encoded shapes — the D-4 row's real six-member shape + `ticket_id`/`canonical_tool_name` bounds, the run-wide cardinality closed by a NEW durable disclosed-marker scoping supersession of §B.2 D-4 (undisclosed-only arrays, ≤ one turn's cap), encoded-bytes gating of `admission_ref` (raw-length gating withdrawn), and the test-only limits-injection seam making the overflow fixture constructible; F3 `workspace_root_id` given a complete producer/carrier/consumer identity — the run-start input surface, the exact resolve/NFC/hex-SHA-256 recipe, the immutable manifest pair, the `assign` carrier extension with worker-side recipe verification, and the structural-validation claim narrowed (no symlink-truth from strings). rev2 = `ceb9ae3113ee7edb9d3022f17c5d674d554aff9c35b1e79156eae338020714e3` (superseded). Carried from rev2 — the r1 fold: the r1 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260722-073000.md` (M10-DAG-R1-F1..F8, every finding verified against the frozen source bytes before folding): F1 the manifest input domain closed + the orphan cells qualified by exact `void_reason` + the discriminated entry union; F2 the §2/§4 frame shapes demoted to PROPOSED/NON-NORMATIVE pending the joint m-9 settlement (m-9's differing rev1 shapes named); F3 `resume_action` persisted, closed, and in the replay-equivalence identity; F4 the exact frame-bound arithmetic + the compile-time constraint + the tightened initial reserve; F5 the C descriptor operand source split pinned (m-9-supplied vs m-10-derived; the NEW `workspace_root_id` manifest member; replay identity extended, consume wire unwidened); F6 the total `state_proposal_result` receiver fold over all five dispositions + correlation lifetime + crash cuts; F7 the two missed r10 loci (:44, :61) superseded + the vocabulary re-sweep recorded; F8 both confirmation halves cited. rev1 = `1c69931703986049bcba62e229b6b7ffc38e08d7e7cb1038e65415841e9d3792` (superseded). (planner-authored; for m-10.implementer adversarial re-review of the full bytes)
**Basis:** the ratified `master/STEP-3-STAGE6-AMENDMENT.md` rev12 `1125b0a06697826751786bd82968354f0789f3bc98e6e9a5e8dbdb298f40e183` (§5-C/§5-D/§6 verbatim) · **the operator-ratified `master/STEP-3-STAGE6-SETTLEMENT-AMENDMENT.md` (packet `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b`, operator-ratified 2026-07-25 §8b at `step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000`) — Corrections 1 (D-4 Gate-2 relabel) + 2 (run-wide restore + `MAX_PARKED_ROWS_PER_RUN=512` + the `parked_unknown_capacity_exceeded` terminal + the two compile-time frame assertions) are what THIS revision folds as IN FORCE; additive to rev12/r40, r21 byte-frozen** · the released rev2 dispatch `step3-relock-dag-m10/DESIGN-orchestrator-planner-20260721-235601.md` @ `6df5367ff294424e06e9f09e6e078330d85d16c47452018f12baf5e64e72a10d` (release `RELEASE-orchestrator-planner-20260722-004003`) · the pair-approved broker study rev8 `64f9136e0b851e31f129372ec50b5667c2b6dcda197ef0dcf95c4f0eca5ff4ce` (the m-10 PAIR F73 confirmation = the planner half `step3-relock-broker-confirm/RECONCILE-planner-20260721-214500` + the completing implementer half `RECONCILE-implementer-20260721-221000.md` @ `375da939fc1cfb1689ffd9ced9c892a166c94273afcabb3ab48d57e16e4f478c`) · **the frozen bases: my contract r40 `d2ce98310afff4a8aeb998a0e530beedbde44cb723c6510ebe351841d70a9146` + my stage-5 r10 `6fd1d655146d447194e5181bd81108936fa4560a4f8b4e9de843cf2daac11faf` — BOTH UNMOVED; this doc is a GOVERNED ADDITIVE DELTA under F73: every superseded clause is named here byte-exactly and replaced here, never edited in place** · consumed counterparty finals: m-9 worker r7 `cb7ff970…` + half r21 `4d3bd14e…` · m-7 r11 `9331ea88…` (as amended by the study) · m-8 r12 `4b670a79…` · m-3 r4 `009df607…` · m-1 `7c8b09a6…` · m-2 `83d8e63e…`.
**Bounds:** DESIGN-only. No DESIGN-lock, no PLAN, no T4/code token, no credential, no provider call, no live E3, no merge/deploy, no `frank/` action. The **consumer carriage sections stay PARKED** (§10) per the rev2 staging rule. Hard constraints inherited unweakened: F67 `{m-8, broker}` · F64 · F60/F66 · item-D park-not-replay/no-auto-resend · the no-payload-column store rule (r40 §F) · m-10 owns NO policy and is NOT a seat.

---

## §1 — D2: the settlement-manifest PRODUCER (producer-total, 3-class, identity-exact)

**Production point:** the manifest is produced INSIDE the continuation-admission transaction (§4), from m-10's canonical rows + the §2 receipt-presence predicate, BEFORE `turn_open` — never from any m-9 artifact, never from any broker event (§7).

**Scope = the full continuation ancestry within the run:** walk `turns.predecessor_turn_id` from the immediate predecessor to the chain root. **The EXACT producer input domain (F1 — closed, stated once):** for every turn in that chain, (i) every `tool_calls` row, (ii) every `provider_attempts` row, and (iii) every **authorization-orphan** `tool_authorizations` row — a row with NO matching `tool_calls` row at its `(run_id, turn_id, tool_call_id)` key. Nothing else is an input; in particular no `broker_events` row, no receipt row (receipts enter only through the §2 predicate), no wake/lease/epoch row. **Producer-totality:** every domain row maps to EXACTLY ONE class below — no omission, no producer-time "or". A domain row in a state the tables below do not name (e.g. an orphan still `ISSUED` at production time — invariant-impossible, since retirement parking and turn-end expiry each VOID it) is an **invariant fault: production fails closed, no manifest, no admission** — never a guessed class.

**The closed 3-class union (amendment §5-D verbatim, realized TOTAL over my row states):**
- **`settled_with_content`** — tool row `EXECUTED` (the marker-before-outcome ordering, §3, guarantees content+marker were durable before my terminal committed) · provider row `completed` AND a committed §2 receipt for that exact `{run_id, turn_id, attempt_id}`.
- **`determinate_no_resume`** — tool row `NOT_INVOKED_INTEGRITY_FAULT` (definite zero-effect; first action `turn_failed` per the amendment table) · **authorization-orphan `VOID` by the DECIDABLE split `void_reason × canonical_tool_name` (R2-F1 — both operands stored on the canonical row; NO broker event is ever an input):** the four denial reasons `{run_not_admitted, turn_inactive, lease_invalid, denied_above_set}` ⇒ determinate for ANY tool (typed issue-refusals — authority never issued, nothing could dispatch); **`expired` × a LOCAL tool** (`canonical_tool_name` ∉ the three relay verbs) ⇒ determinate — for local tools the consuming wrapper IS the effect path (m-9's uniform F59 executor: consume-then-invoke in one process), so unconsumed = uninvoked at the enforcement point itself. Each orphan entry carries its `void_reason`; first action: surfaced, never auto-retried — an informed re-issue is a fresh ticket · provider row `REJECTED_LOCAL` · `denied` · `CANCELLED` with `cancel_point = pre_transport` (genuinely zero-content) · `transport_failed` · `CANCELLED` with `cancel_point = post_invocation` (determinate terminal; partial pre-terminal bytes discarded/untrusted for resume, NOT claimed never-to-have-existed; entry carries terminal + `cancel_point`).
- **`uncertain`** — tool row `UNKNOWN_TOOL_OUTCOME` · `PARTIAL_TOOL_EFFECT` · **authorization-orphan `VOID/expired` × a RELAY verb (`canonical_tool_name` ∈ {relay.submit, relay.project, relay.read}) — the confirmed M10-C1 seam preserved (R2-F1):** for relay verbs the F59 consume gate (m-10, CTRL-W) and the effect path (the broker) are SEPARATE processes, so an unconsumed ticket is NOT provably uninvoked from my rows alone — the honest class is `uncertain`, the successor rediscovers against conductor truth (exactly the byte-bound study §Q3.4 / pair-confirmation disclosure; this is the class the seam requires and rev2's blanket-determinate cell wrongly overturned — WITHDRAWN). The mapping cannot and does not distinguish a cut-expired from an ordinarily-expired relay ticket (`boundary_cut` is telemetry-never-input): ordinary expired relay tickets therefore ALSO surface `uncertain` — over-surfacing, the accepted-safe direction (under-surfacing is what the seam forbids) · provider row `UNKNOWN_PROVIDER_OUTCOME` · `PARTIAL_STREAM` · **`completed` WITHOUT a committed receipt** (producer-decidable: no receipt ⇒ uncertain, never settled). Surfaced; no auto-resend, ever. *(rev1's blanket VOID⇒uncertain exceeded the input domain and misread the denials; rev2's blanket expired⇒determinate contradicted the confirmed seam — the split above is the correction that makes §1, §7, and the M10-C1 confirmation one consistent reading.)*

**`content_lost` is NOT a manifest class** — it is m-9's post-`turn_open` reconciliation result (→ `DEGRADED` via the §4 report). The producer never emits it.

**Per-entry schema — a CLOSED DISCRIMINATED union (F1):** every entry carries the two discriminants **`kind ∈ {tool, provider}`** and **`class ∈ {settled_with_content, determinate_no_resume, uncertain}`** explicitly, plus the kind-exact identity members: `kind: tool` ⇒ `{run_id, turn_id, tool_call_id, args_digest, terminal, void_reason?}` (`args_digest` = the frozen canonical args digest from the authorization row — identity #1, never recomputed; `void_reason` present-IFF the entry is an authorization-orphan); `kind: provider` ⇒ `{run_id, turn_id, attempt_id, terminal, cancel_point?}` — NO `args_digest` (m-10 has no canonical provider args-digest producer). Presence rules closed both ways: a member outside its kind/class shape, a missing discriminant, or an unknown discriminant value makes the manifest UNDECODABLE — the consumer rejects fail-closed (fixture-backed); the producer can never emit such bytes (the union is constructed, not filtered). `turn_id` = the SOURCE turn (frozen tool identity is `UNIQUE(run, turn, tool_call_id)`, so a bare id is not identity-exact across the chain). `last_settled_round_index` does not exist (removed by the amendment; the worker derives the resumable round from its own `round_marker`s ∩ the settled set).

**Canonical encoding, deterministic:** JCS canonical JSON; top-level `{settlement_manifest_v: 1, run_id, produced_for_turn_id, entries[]}`; `entries[]` ordered by (ancestry position oldest→newest, then `kind` — `tool` before `provider`, then the kind's id member lexicographic) — so equal durable state ⇒ byte-equal manifest, which is what makes the §4 byte-identical re-emission and the `resume_snapshot` digest check meaningful. **The manifest is payload-free by construction** — ids, digests, terminals, classes only; no content byte, no argument byte; the r40 no-payload-column rule extends to the snapshot unbroken.

**Retention/GC:** manifest bytes (`resume_snapshot`) + receipts (§2) are per-run rows, GC'd on run-terminal — the r40 store GC rule applied, nothing new; m-1's at-rest review hook is §9.

## §2 — The composite provider-settlement gate: my consuming half + the JOINT receipt seam

**The gate (evidence property #1, amendment F105-D2-R11 — NORMATIVE regardless of the frame shape below):** a provider entry is `settled_with_content` IFF at manifest-production time BOTH (a) my canonical `provider_attempts` terminal `completed` AND (b) the matching durable receipt row are committed. The class records that content WAS made durable at that earlier point — nothing more; presence-at-resume is m-9's stage-2 check (their `content_lost` branch), which my machinery neither performs nor claims. A worker that dies before sending its receipt simply leaves `uncertain` — the honest default. The durable receipt table exists (`content_ready_receipts`, keyed `UNIQUE(run_id, turn_id, attempt_id)`, ids + digests only, no payload column); its exact digest/segment columns finalize with the joint frame.

**The receipt frame — the SHAPE settled with m-9 (S-1) and the widened predicate PAIR-MATCHED / EXACT-FOLDED at pair-approved r12 bytes; JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D join co-sign (R9-F1 — matching bytes prove both documents describe ONE predicate, they do not perform the join):** the frame shape settled `…m9-20260722-235500` × `…-20260722-234500`; the widened conflict predicate (the exact complement of equivalence) is folded on m-9's side and **pair-approved at r12 `04422965…` §2** ("§2 widens `receipt_conflict` to the exact complement of equivalence, matching m-10's rev8 so both documents describe one predicate"). **But m-9 r12 itself keeps the fuller gate** (`:299`/`:473`/`:511`): the frame is normative only when both pairs approve matching bytes AND the §D join is co-signed — so the seam is EXACT-FOLDED and pair-matched, NOT yet normative; the §D join, not this document, retires that status. Consumed at m-9 **r12 §2** (the seam semantics are byte-carried from the r7 settlement through the r8→r12 arc — verified at their bytes):

> **envelope** (frozen r40 §A.2, unchanged): `{v: 1, chan: "ctrl-w", type: "content_ready", seq, run_id, turn_epoch}` — **no `re` member** (one-way; no reply-class frame exists on this seam)
> **`body`:** `{turn_id, attempt_id, marker_digest, segment_id, seq_hwm, generation_id}`

**The envelope discipline (m-9's proposal, accepted):** the fencing operands `run_id` + `turn_epoch` are read from the **envelope**; `generation_id` rides `body` (the frozen envelope has no generation member); **no `body` duplicate of an envelope operand exists** — an operand in two places invites "which one fences?", closed here at authoring time. **My precision (1), accepted by m-9 and load-bearing:** the frozen envelope declares `run_id`/`turn_epoch` as `<string|absent>`, so for **`content_ready` and `report_resume_disposition` both members are pinned REQUIRED-never-absent** — otherwise "read the operands from the envelope" is unenforceable and an absent operand becomes an undefined case in a fencing path. Counters are canonical decimal strings (§A.2); `marker_digest` 64 lowercase hex; `segment_id` = m-9's `"<generation_id>.<rotation_index>"`, grammar-legal under §4.

**The durable row (`content_ready_receipts`, `UNIQUE(run_id, turn_id, attempt_id)`):** PERSIST `{run_id, turn_id, attempt_id, marker_digest}` (the key + the conflict operand — `receipt_conflict` is only decidable if the digest is stored) and `{segment_id, seq_hwm}` (locator evidence for m-3's later E3 comparison; two bounded members, no payload). **VALIDATE-AND-DROP `{generation_id, turn_epoch}`** — fencing operands, not evidence: I already hold the canonical current epoch and the channel-assign sender association, and persisting a copy would create a second record of **my own authority**, the pattern both pairs have been deleting.

**The ordered TOTAL first-match disposition** (three non-substitutable authorities: wire epoch carriage · channel-assign sender association · durable current epoch): (0) malformed ⇒ fault, no row · (1) unknown `{run_id, turn_id, attempt_id}` ⇒ fault, no row · (2) presented epoch above current ⇒ fault (m-10 sole mint) · (3) **equivalent duplicate** (key exists, equal **evidence tuple** `{run_id, turn_id, attempt_id, marker_digest, segment_id, seq_hwm}`) ⇒ idempotent, same result — **ordered BEFORE stale-sender, per my own frozen §D.4 record-table precedent (my rev6 §2 had these reversed; corrected here)**, so a delayed byte-identical re-send crossing an epoch bump is idempotent rather than rejected · (4) **stale SENDER** (association or below-current epoch) ⇒ typed reject, recorded, no row — a retired generation can never author NEW or DIFFERING settlement evidence · (5) **same-key, NON-EQUIVALENT on ANY member of the evidence tuple** — a differing `marker_digest`, `segment_id`, or `seq_hwm` alike (R7-F4: scoping the conflict branch to `marker_digest` alone left a same-key/same-marker/different-locator receipt matching no branch — the "TOTAL" fold had an uncovered input, and the UNIQUE key would have made the fall-through an unhandled insert failure) ⇒ typed **`receipt_conflict`**, fail-closed, recorded, the **FIRST-committed row STANDS** · (6) valid ⇒ durable commit (durable-then-visible; nothing observes it before the commit). **The branch predicate is now the exact complement of (3)'s:** equivalence is all-six-members-equal, conflict is any-member-differing, so every same-key input lands in exactly one of (3)/(5) and no gap exists by construction. **m-9's answer, consumed (their `…m9-20260723-152000`, planner half; r9 PROPOSED so nothing here is bound to it):** they **matched the widening** on their producer — and separately **derived** what I had only offered as an alternative reading: on their producer `marker_digest` **functionally determines both locators** — `seq_hwm` IS `last_seq`, the largest `seq` in the very array the marker digests (§1.5), and each `record_digest` covers the record's canonical bytes **including the envelope `segment_id`**, with §1.5 containment putting a whole round inside one segment (§1.3). So a same-key/same-marker/different-locator input is **not merely unemitted by a correct producer — it is unconstructible under the digest recipe itself.** **Therefore this branch is carried as a FAIL-CLOSED DETECTOR over a not-producible input** (the same honest label we both adopted for `receipt_conflict` as a whole) — and, per their own framing and mine, **the totality of the fold does NOT rest on that derivation**: the predicate is the exact complement of equivalence and stays total whether or not the input is constructible. The label is the claim; the complement is the mechanism. `generation_id`/`turn_epoch` are **excluded from the equivalence tuple** — they are fenced at (4), and a same-generation re-send carries them identically anyway.

**`receipt_conflict` is a FAIL-CLOSED DETECTOR, not an expected path** (m-9's honesty note, adopted): a correct m-9 cannot produce one — a lost-frame re-send within a generation is byte-identical, a successor never authors an ancestor attempt's receipt, and their fence + terminal seal make a moved prefix fail closed before a receipt could derive from it. **The delayed-FIRST-arrival case, recorded as honest over-surfacing:** a receipt whose only send crosses an epoch bump finds no committed row, falls to (4), and is rejected — so the attempt reads `uncertain` though its content genuinely was durable. That is the accepted-safe direction and matches "a worker that dies before sending its receipt simply leaves `uncertain`"; it is not a defect.

## §3 — The marker-before-outcome ordering + the conditional writer-fence PRODUCER branch

**Marker-before-outcome (consumed cross-owner rule, named):** m-9's discharged obligation is that the content record AND its admitting durable `round_marker` fsync-linearize BEFORE `record_tool_outcome` is sent; my §D.4 `executed` commit is the settlement-producing step and RELIES on that order (my evidence property #1 is only as true as it). m-10 cannot verify a foreign fsync — this is trust-by-frozen-contract, honestly named, checked by m-9's own crash table + the E3/§14 fixtures, not by my gate.

**The conditional segment-producer branch (F2, carried explicitly):** IF m-9 selects the **m-10-ordered per-generation segments** writer-fence branch, m-10 becomes the segment PRODUCER — per-generation segment identity issued from my durable rows (allocation at generation lease-bind, retirement at the retirement transaction, so the exclusive-writer boundary is decidable from my canonical state) — designed JOINTLY with m-9 (joint pair review + join record) when and only when selected. IF m-9 selects the local OS-exclusive-lock branch, this obligation is INERT and nothing here activates. No internals are authored now (authoring them would pre-empt m-9's branch selection); the scope + the decidability source are the carried commitments.

## §4 — D3: the continuation-turn lifecycle (durably snapshotted, receipt-gated)

**Store delta (`turns`):** + `predecessor_turn_id` (NULL ⇔ initial turn) · + **immutable `resume_snapshot`** = the canonical §1 manifest BYTES + the session-log path (written ONCE in the continuation-admission transaction; no UPDATE path exists at the applier — the single-chokepoint rule makes immutability structural) · + mutable **`resume_disposition`** ∈ `{PENDING, RESUMABLE, DEGRADED}` (a SEPARATE column, never conflated with the snapshot) · constraint **`UNIQUE(run_id, predecessor_turn_id)`** (one successor, ever). **Store delta (`runs`):** + `stop_reason`, a **CLOSED domain TOTAL over both terminals this delta adds — `{resume_frame_overflow, parked_unknown_capacity_exceeded}`** (R7-F1; schema CHECK, extensible only by amendment). **Presence, exact:** `stop_reason` is **present (NOT NULL) IFF `runs.state = FAILED` by one of these two paths**, and NULL in every other state and on every other failure path; the two members are mutually exclusive by construction (they are committed by different transactions at different loci — the continuation-admission sizing branch (§4) and the retirement parking branch (§4's cap)). **`resume_action` on the `runs` record is present IFF `stop_reason = resume_frame_overflow`, with the single value `operator_new_run`** — it is NOT carried on the cap terminal (whose remedy is not a re-run of the same objective; the operator surface renders the terminal and the queryable parked set, and any next act is an ordinary new run). A `stop_reason` present without `FAILED`, absent with either terminal, or paired with the wrong `resume_action` is a schema violation, not a runtime case.

**Trigger + the one transaction:** on replacement of a turn parked `INTERRUPTED`, the continuation-admission transaction commits: the new `turns` row + `predecessor_turn_id` + `resume_snapshot` + `resume_disposition = PENDING` + the active-turn lease at the CURRENT epoch + **`admission_ref` inherited BYTE-IDENTICALLY from the predecessor row** (task identity stable across the chain; no wake row is consumed — the ref is inherited, not re-acquired). Durable linearization = this commit; emission is POST-COMMIT (the r38 split, lesson 3); crash-after-commit-before-send ⇒ re-emit byte-identically from the committed snapshot bytes (the digest verifies the bytes, never replaces them). **Chain bound = the ratified G-2 counter:** every continuation follows a failure disposition that incremented the consecutive-failure counter, so the chain is bounded by the same 10-try/backoff/run-FAILED machinery — no separate bound, no unbounded chain.

**The `turn_open` frame delta (extends the frozen r40 §B.2 closed frame — a named F73 supersession, §6.5):** + `session_log_path` on EVERY `turn_open` (D1 property (vi): one carrier; the path is derived from the per-run runtime dir m-10 allocated at run admission; the D1 (iv) file-identity binding to `{run_id, run_manifest_digest}` is m-9's check) · + `settlement_manifest` (the §1 bytes) and `predecessor_turn_id`, **present-IFF-continuation** (closed presence rule; an initial turn carries neither). Replayed emission is byte-identical from the committed row — the R38-F1 rule extended to the new members.

**Frame-totality (F105-D3-R9/R10/R11):** the COMPLETE candidate `turn_open` (all members + framing overhead) is sized BEFORE the admission transaction — ALWAYS, both kinds. ≤ `FRAME_MAX` (4 MiB = 4,194,304 B, §A.2) ⇒ commit + emit. Over the bound, two distinct closed outcomes by kind: **initial turn** ⇒ the r40 operator-boundary `admission_refused{reason: task_input_frame_overflow}` (pre-frame, zero durable effects; the gate's BOUND is tightened below — the member and boundary are unchanged); **continuation** ⇒ **NO successor turn, NO lease, NO snapshot committed; instead ONE durable transaction commits `runs.state = FAILED` + `stop_reason = resume_frame_overflow` + `resume_action = operator_new_run`** (F3 — the manual-action source is a durable member of the terminal record, not prose) — a run TERMINAL (never parked/nonterminal), no same-run successor or revival, the §10 loud surface renders the committed triple.

**The EXACT bound proof (F4 as corrected by R2-F2 — exact JCS-encoded shapes, real member sets, a closed cardinality, and a constructible boundary fixture; every value a compiled constant, names unremovable, values T4-tunable under the constraint):**
- **The encoding grammar (what makes raw = encoded):** every identifier, digest, token, tool name, and path member is schema-CHECKed to the JSON-escape-free grammar `[A-Za-z0-9._:/-]` — for these members the canonical JCS encoding adds NO escape expansion (factor 1), so their raw bounds ARE encoded bounds. **`task_input` is the ONE member with no grammar** (verbatim operator text) — it is therefore bounded and gated on its ENCODED bytes, never its raw length (below); rev2's raw-length `TASK_INPUT_MAX` gating is WITHDRAWN.
- **Identifier/token bounds (NEW pinned schema rule, additive):** `run_id`, `turn_id`, `attempt_id`, `tool_call_id`, **`ticket_id`**, `relay_id`, `generation_id` ≤ `ID_MAX = 64` B; digests = 64 hex; **`canonical_tool_name` ≤ 64 B** (the 8-name constant's longest is far shorter); `terminal` ≤ 40, `class` ≤ 24, `kind` ≤ 8, `cancel_point` ≤ 16, `void_reason` ≤ 24, `state` ≤ 24.
- **Per-entry bound `E_MAX = 768 B`:** the widest manifest entry (`kind: tool`, 8 members) = member names + JCS punctuation ≤ 160 B + values (32 + 3×64 + 64 + 40 + 24 = 352 B) ⇒ ≤ 512 B actual; 768 is the enforced ceiling.
- **The `parked_unknown` row, at its REAL frozen shape (R2-F2.1):** `{turn_id, tool_call_id, ticket_id, state, canonical_tool_name, canonical_args_digest}` = names/punctuation ≤ 140 B + values (3×64 + 24 + 64 + 64 = 344 B) ⇒ ≤ 484 B actual; **`PARKED_ROW_MAX = 640 B`** enforced.
- **RUN-WIDE CARRIAGE RESTORED — the rev2–rev6 D-4 scoping is WITHDRAWN (master ruling (3)).** Both `turn_open` and `attempt_open_ok` carry the **FULL run-wide parked set** from my canonical rows, exactly as ratified at r40 `:72`, whose load-bearing property is **worker-INDEPENDENCE** ("m-10 attaches it unconditionally from durable state (worker-independent by construction)") — a property any consumer-side re-derivation would invert precisely where it matters most, the `content_lost`/`DEGRADED` path where the consumer's own durable state is what failed. **Why the scoping was withdrawn (M9-D4-SCOPE-F2, m-9's finding, verified at my own bytes):** my `settlement_manifest` is **present-IFF-continuation**, so on an ORDINARY turn no manifest exists and the scoping left **no carrier at all**; and a `parked_unknown` disclosure lands in m-9's assembled content, not their Tier-0 pinned set, so their ratified compaction can evict it — the run-wide mechanism repaired that automatically every frame, the scoping repaired nothing. That is the D-4 safety purpose defeated on a reachable path, repaired here **at the architecture level** (the frame re-delivers at every turn AND before every attempt) rather than delegated to a compaction-aware consumer — the general lesson master recorded in the hardening backlog for the Governance-Decay class. **The D-4 CLAIM, honestly relabeled (master ruling (1), RATIFIED Correction 1, operator 2026-07-25):** *Gate 1 (`turn_open`) delivers the disclosure guarantee; Gate 2 (`attempt_open_ok`) is a **fail-closed validator + drift-detector** over states unreachable on the MVP's bytes.* The comparator bytes STAY (they cost nothing and catch exactly the producer drift a future m-10 revision could introduce) and Gate 2's **validation** — malformed member, duplicate identity within one list ⇒ fail-closed, no DATA-P — stays fully reachable; it is only the **comparison** that is vacuous on reachable states. This changed what a ratified operator-visible mechanism claims — and it is now **RATIFIED and IN FORCE**: the §D-settlement amendment (packet `1fa71cb8def2d52690cc513faa7860e0d8c2c2224b33b38ff97808990adc879b`, Correction 1) was **operator-ratified 2026-07-25** (recorded §8b, `step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000`), so the D-4 Gate-2 relabel is architecture-of-record, not a pending disposition.
- **The `turn_disclosures` snapshot (R4-F1, unchanged in kind):** the admission transaction for turn T **writes one row per carried identity with the EXACT SIX-FIELD DISCLOSURE SNAPSHOT** — `{disclosing_turn_id: T, source_turn_id, tool_call_id, ticket_id, state_at_disclosure, canonical_tool_name, canonical_args_digest}` — the frozen values as-of-admission, IMMUTABLE thereafter (the applier's chokepoint; no separate write; no UPDATE path). **Two SEPARATE source rules (R4-F1):**
  · **`turn_open` for T** — derived at first emission, replay, and crash-recovery re-emission alike from **T's `turn_disclosures` SNAPSHOT rows** under the §1 deterministic ordering: byte-identical by construction EVEN IF a disclosed row later terminalizes through the ordinary owner machinery (r40 :72 — the live row evolves; the snapshot does not; the two are different records with different jobs).
  · **`attempt_open_ok`** — **LIVE-derived at each emission from durable state** (the full run-wide parked set at its CURRENT field values, incl. rows parked after T's admission): the frozen Gate-2 producer stays TOTAL over equal/added/changed/removed — m-9 r21 treats the two frames as distinct snapshots precisely so its pre-work comparison can see additions and changes, and this delta PRESERVES that (the rev4 "byte-stable within the turn" claim is WITHDRAWN — it would have silently deleted the added/changed detection). Its §B.1 lost-reply replay re-derives from durable state at re-send time — the array may honestly differ across replays; m-9's comparison, not byte-equality, is the contract there (exactly the frozen semantics).
  Every parked identity remains permanently in the store and queryable; ancestry parked rows ALSO ride the manifest as `uncertain` entries (carried twice-over for a continuation chain, never lost).
  **`turn_disclosures` retention: the ACTIVE turn only** — GC'd at the next admission commit, since replay of T's frame is only ever needed while T is live (one-active-turn makes T terminal once T+1 admits). The snapshot is kept even though both pairs' traces say the live set cannot drift while a worker lives, because **a durability mechanism must hold by construction, not by an argument about reachability** — the lesson of R4-F1.
- **THE BOUND — `MAX_PARKED_ROWS_PER_RUN = 512` (master-ruled), enforced at the SINGLE growth site.** The run-wide parked set has **no static bound**: the G-2 counter resets on a completed turn, the §2a bounds are all per-turn, and no frozen constant limits turns-per-run (both pairs' independent negative searches, accepted by master). The three options reduce to one honest choice — **truncation is FORBIDDEN** (silent under-disclosure is the exact harm D-4 exists to prevent), unbounded growth makes the frame-overflow terminal silently reachable for a long-lived run, so **fail loudly at a cap**. Parked rows are created at exactly one locus — the §B.4 step-1 parking inside the retirement transaction (r40 `:81`) — so the check has one enforcement point: **the retirement transaction still commits IN FULL (fence, park, mint — fencing is never traded for a bound); if the post-commit parked count would exceed the cap, that SAME transaction also commits `runs.state = FAILED` with `stop_reason = parked_unknown_capacity_exceeded`** — a typed, loud, operator-visible terminal rendered by the §10 surface, with every parked identity still queryable afterward. **The cap terminal admits NO revival (ratified Correction 2 §2.2 rule 3, packet `1fa71cb8…`): no successor generation, no same-run continuation, no lease, no snapshot, no revival — any next work is an ordinary NEW run; and `MAX_PARKED_ROWS_PER_RUN` is an ENFORCEMENT threshold at the parking write, NOT a 512-row storage cap — the full retirement batch commits and the complete parked set stays queryable even past 512 (§2.3).** This is DISTINCT from the `resume_frame_overflow` continuation-sizing terminal (§4 above): that one prevents an un-emittable continuation `turn_open`; this one bounds run-wide parked-set accumulation — they share the no-revival/new-run-only lifecycle but arise at different loci and neither's fixture proves the other's. **RATIFIED and IN FORCE:** the new operator-visible terminal was **operator-ratified 2026-07-25** as Correction 2 of the §D-settlement amendment (packet `1fa71cb8…`, recorded §8b at `step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000`) — architecture-of-record, no longer amendment-borne. *(Recorded as a bounded-MVP choice, not a permanent design — master filed a Step-4 carry: once the deferred permission/authority system lands, an operator-surface clearing path for a parked identity could relax or remove this terminal. The clearing path is Step-4 because an operator disposition over governed state is an authority ingress the MVP defers wholesale; R18-F1's withdrawal of the r16 path was an authority-ROUTING defect — a worker-forwarded citation treated as transferable — never a merits-bar on a direct operator surface.)*
- **`PARKED_MAX = 512 × 640 = 327,680 B`.** Per-frame wire cost tracks the **ACTUAL** parked count, never the cap — the empty-array-never-absent floor means a healthy run pays zero rows on every frame, so the cap bounds only the worst case and does not tax the common one.
- **Manifest bound:** entries ≤ `CHAIN_DEPTH_MAX 10` × (128 + 32) = 1,600 × 768 + 4,096 envelope = **`M_MAX = 1,232,896 B`**.
- **Fixed members:** `session_log_path` ≤ `PATH_MAX_M10 = 4,096 B` (grammar-bounded); wake `relay_id` ≤ 64 B; frame member names + framing `OVERHEAD_MAX = 65,536 B`.
- **Constraint 1 — the `turn_open` compile-time assertion over the PRODUCTION limits table:** `ADMISSION_REF_ENC_MAX + M_MAX + PARKED_MAX + PATH_MAX_M10 + OVERHEAD_MAX ≤ FRAME_MAX` ⇒ with the restored `PARKED_MAX = 327,680`: 1,232,896 + 327,680 + 4,096 + 65,536 = 1,630,208, so **`ADMISSION_REF_ENC_MAX = 4,194,304 − 1,630,208 = 2,564,096 B`** — the bound on the JCS-ENCODED `admission_ref` contribution (escape expansion included by construction, since the gate MEASURES encoded bytes).
- **Constraint 2 — a SECOND compile-time assertion covering `attempt_open_ok` (m-9's check (i); NEW at this revision):** the frame-totality gate below sizes **`turn_open` only** — there is exactly one such gate, and a reply frame is otherwise governed solely by §A.2's generic oversized-frame rule, whose disposition is a channel fault ⇒ generation death. Under the restored run-wide carriage the reply frame's parked term is 4× its scoped size, so the safety must be structural rather than arithmetic-in-prose: **`ATTEMPT_ACK_MEMBERS_MAX + (MAX_PARKED_ROWS_PER_RUN × PARKED_ROW_MAX) ≤ FRAME_MAX`** — at ≈1,024 + 327,680 = **328,704 B against 4,194,304 B (≈7.8%, ~12× headroom)**. A cap raised for disclosure headroom that would push a reply frame toward a channel fault is therefore a **build error**, not a runtime surprise. *(The class of defect here was not a wrong number but an UNASSERTED one — the fix is to give the invariant a checker rather than a sentence.)*
- **The initial gate delta (a named §B.2 supersession, restated over encoded bytes):** the initial sizing gate constructs the canonical initial frame and enforces BOTH total ≤ `FRAME_MAX` AND the encoded `admission_ref` contribution ≤ `ADMISSION_REF_ENC_MAX` (same member, same boundary, `task_input_frame_overflow`) — refusing at the operator boundary an input whose ENCODED form would doom any continuation (a raw-small/escape-heavy string is caught exactly here, which the rev2 raw gate missed).
- **The constructible boundary fixtures (R2-F2.4 → R3-F2 → honest per R4-F2):** the compiled limits live in ONE table; **a build-tagged TEST-ONLY injection seam substitutes a reduced table — no production injection point exists**. The chain of corrections, stated once: rev3 claimed production exact-fit at `FRAME_MAX` (unreachable); rev4 claimed equality at a derived 3,764,736 B — but that number sums `≤`-bounds (name/punctuation estimates, generic token caps, the expressly-conservative `OVERHEAD_MAX`), and JCS has no padding to turn inequalities into an attained size, so NO equality pin is legitimate without an attaining witness. **The honest rule:** **`FRAME_CONTENT_BOUND = 3,704,832 B` is a PROVEN CONSERVATIVE UPPER BOUND on any legal production frame — nothing more; no fixture asserts equality to it.** (i) The PRODUCTION leg builds the defined **max-witness vector** — max cardinalities (1,600 manifest entries, **512** parked rows at the restored run-wide cap), maximum-length grammar-legal identifiers, the LONGEST legal literal of every enum, the maximum encoded `admission_ref`, the maximum path — asserts it COMMITS, and asserts its measured size **≤ `FRAME_CONTENT_BOUND`** (a violated inequality means the proof or the shapes drifted — loud either way); (ii) exact-fit-at-the-limit AND one-byte-over run ONLY under the build-tagged reduced table, where real closed-shape bytes reach the reduced limit legally; (iii) the compile-time assertion binds the PRODUCTION table over the conservative enforced ceilings. Under the production assertion `resume_frame_overflow` is statically unreachable and remains the fail-closed runtime backstop — the no-un-emittable-commit invariant is never conditional on the arithmetic being right.

**The disposition + the post-commit receipt no-work gate (my producing half):**
- **The durable record is the PAIR (F3):** the mutable column set is **`resume_disposition ∈ {PENDING, RESUMABLE, DEGRADED}` + `resume_action`**, committed together in the one idempotent transition. **Closed presence/domain rule:** `resume_action` is **present IFF `resume_disposition = DEGRADED`, closed domain `{re_derive}`** (the worker-reportable member; the amendment's "operator abandon" alternative is NOT a report value — it is the operator surface acting on the committed pair via the existing `run cancel` verb; the overflow terminal's `resume_action = operator_new_run` lives on the `runs` record, §above). The committed-snapshot projection reads the committed PAIR — the operator surface renders it from durable state, never from wire memory.
- **Replay equivalence is over the COMPLETE identity (F3):** equivalent duplicate = same `{disposition, resume_action}` pair; a report matching the committed disposition but carrying a DIFFERENT action is a CONFLICT, not a replay.
- **The report/receipt/conflict frames — the SHAPE settled with m-9 (S-2), EXACT-FOLDED / JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D join co-sign (R10-F1 — the same join gate as S-1; S-2 is the disposition receipt, ledger item 4, so m-9 r12 `:299`/`:473`/`:511` fold it directly; the gate rule at `:473` governs normativity for the joint frames generally)**, on the same envelope discipline as §2 (fencing operands on the frozen §A.2 envelope, pinned **REQUIRED-never-absent** for these types; no `body` duplicate):
  > **report** (m-9 → m-10): envelope `{…, type: "report_resume_disposition", run_id, turn_epoch}` · `body: {turn_id, disposition, resume_action?}`
  > **receipt** (m-10 → m-9, POST-COMMIT): envelope `{…, run_id, turn_epoch}` · `body: {turn_id, disposition, resume_action?}` — **carrying the COMMITTED pair**
  > **`disposition_conflict`** (m-10 → m-9): envelope `{…, run_id, turn_epoch}` · `body: {turn_id, disposition, resume_action?}` — **also carrying the COMMITTED pair** (m-9's ask, accepted: disclosing it turns a recoverable disagreement into a surfaced one, where withholding it would convert the same disagreement into a dead run; it is ids plus two closed tokens, no payload — so m-9's gate may key on it exactly as on the success receipt)
  `disposition` on the report is `{resumable, degraded}` — `PENDING` is my initial column value and is never reported. **The ordered TOTAL disposition:** (0) malformed ⇒ drop/fault · (1) unknown turn ⇒ fault · (2) epoch-above ⇒ fault · (3) stale sender ⇒ typed reject, recorded · (4) equivalent duplicate (the COMPLETE `{disposition, resume_action}` pair) ⇒ idempotent, re-emit the SAME receipt · (5) committed-but-different (disposition OR action) ⇒ `disposition_conflict` carrying the committed pair, fail-closed, recorded, the committed pair STANDS — `PENDING → RESUMABLE|DEGRADED` transitions EXACTLY ONCE · (6) commit the pair → POST-COMMIT emit the receipt carrying it. The worker's no-work gate keys on the **carried committed pair**, never on what it sent, so the action can never drift (F3).
- **The no-work gate is receipt-existence-by-construction:** the receipt is emitted ONLY post-commit, so m-9's frozen wait-for-receipt rule (no provider attempt, no tool effect, no conductor verb before it) can never observe an uncommitted pair. Crash cuts converge without permitting work: before the report ⇒ still `PENDING`, worker re-derives + re-sends; after the report before my commit ⇒ same; after commit before the receipt send ⇒ the worker's re-send lands in branch (4) ⇒ the same receipt (same pair) re-emits. `degraded` (m-9's `content_lost` result) commits identically — my rows never change class retroactively (evidence property #1 is about the earlier commit, not about presence-now). Inability to establish even the degraded path is m-9's fail-closed run-terminal report, consumed through the ordinary failure machinery.
- **First-action discipline (my producer-side share of the amendment's total table):** nothing in my machinery auto-resends, auto-retries, or auto-clears a parked row; determinate terminals reach the worker ONLY through the manifest (surfaced, never advanced as clean-positive); an uncertain re-issue is a FRESH F59 ticket through the full §D.2 procedure. The table's worker-side actions are m-9's realization.

## §5 — C: the ticket schema + dispatch gate (the effect descriptor bound at mint)

**The authorization row (r40 §D tickets) gains the EFFECT DESCRIPTOR — persisted at ISSUE, frozen with the ticket (the identity-#1 discipline extends to it):** `{action, canonical_resource, workspace_root_id, cwd, backend_id, network_policy_id, tool_impl_ref, canonical_args_digest, one_shot: true}` — presence per the ratified §5-C per-action R/∅ table EXACTLY (an ∅ cell = the member is ABSENT; a present-∅ or absent-R frame/row fails closed).

**The operand SOURCE SPLIT (F5 — m-10 cannot construct what it never receives; frozen r40 gives the issue wire only the `{canonical_tool_name, canonical_args_digest, turn_epoch}` triple, one-way, args never interpreted):**
- **m-9-SUPPLIED, on the wire (a named `authorize_tool_call` frame EXTENSION — the encodings are **SETTLED with m-9 for FIVE action families** — `read`/`write`/`edit`/`apply_patch`/`bash`, byte-for-byte against their §7; the **`relay.*` cell shape is now RATIFIED as Correction 3** (m-2's bound cell `5ec7a3d254da555f4536446827298c001ccc8b2a01a8d8926a0924c68cbe836f`, operator-ratified 2026-07-25: `canonical_resource(relay.submit) = "relay.submit:" || SHA-256(JCS{form_digest, dispatch_id?, to?, cc?})`, total via a REQUIRED `form_digest`) — the earlier "held pending m-2's shape" is resolved; **the fold of that cell into a consumer is m-9's §7-`relay.*` per the propagation matrix, not this rev's scope (which is Corrections 1+2), so my C-descriptor cites the ratified shape and does not re-author it** (ruling (4) bound a form-schema-derived target identity, **not `∅`** — `relay.submit` structurally has no pre-existing target id since it creates the record it names, and neither m-9 nor m-10 may invent another domain's verb shape; the five settled families are explicitly NOT held behind it); presence per the ratified R/∅ table):** `canonical_resource` (the workspace-root-resolved canonical path for read/write/edit; the ordered target-set digest — SHA-256 over the JCS-sorted resolved paths — for apply_patch; relay verb + target id for relay.*; ABSENT for bash) and `cwd` (the single canonical form: workspace-root-relative POSIX, symlink-resolved, NFC, no trailing slash, root `"."`; ∅ for relay.*) — derived by m-9 FROM THE PARSED CALL it alone holds.
- **m-10-DERIVED, from named immutable local sources:** `workspace_root_id` — the COMPLETE producer/carrier/consumer contract below (R2-F3); `backend_id`/`network_policy_id` — the ratified per-action constants (`"ambient"`/`"conductor-client"`, `"none"`); `tool_impl_ref` — from the manifest's frozen tool vector; `one_shot` — constant `true`; `canonical_args_digest` — the existing wire member, stored as today.
- **The `workspace_root_id` identity contract (R2-F3 — producer, recipe, carrier, consumer, each pinned):**
  1. **Input surface:** the operator's `run start` (the existing r10 §5 surface verb — a named input extension) gains the REQUIRED `workspace_root` argument: an absolute path to the run's workspace root. No other source exists.
  2. **The value recipe — an ORDERED FIRST-MATCH decision procedure, total over every reachable input (R3-F3 as ordered by R4-F3); the refusal family `workspace_root_invalid{reason}` is typed, operator-rendered, ZERO run/manifest/store effects on every row:**
     (0) **Input-form check, BEFORE ANY FILESYSTEM CALL:** input absent, empty, or not beginning with `/` ⇒ **`not_absolute`** — the ambient process cwd is structurally unreachable (no resolution has happened yet, so a relative path can never be resolved against it);
     (1) **ONE `realpath` attempt** on the absolute input: failure of ANY kind — nonexistent component, permission, unresolvable/looping links, or ANY OS error INCLUDING OS path-length errors (`ENAMETOOLONG`-class) ⇒ **`unresolvable`** (`too_long` is NOT an OS-failure class — it is solely the post-resolution output check at row (4));
     (2) resolved path = `/` ⇒ **`filesystem_root`** — evaluated on the RESOLVED path, so a symlink resolving to the root lands here, and the literal-strip-to-empty-string defect is unreachable;
     (3) NFC-normalize the resolved path (no trailing slash exists post-(2) — realpath output carries none except the root itself); bytes outside the §4 grammar ⇒ **`out_of_grammar`** — evaluated BEFORE length, so a path that is both out-of-grammar and over-length yields THIS one token;
     (4) normalized length > `PATH_MAX_M10` ⇒ **`too_long`**;
     (5) success ⇒ `workspace_root_path` = the normalized resolved path; **`workspace_root_id` = lowercase-hex SHA-256 of its UTF-8 bytes**. Domain: 64 hex. Equality = byte equality.
     The recipe's success domain is exactly what survives rows (0)–(4); the convergence property (one id per real root) is claimed over that admitted domain only.
  3. **The immutable manifest members (a named §C.1 schema delta):** the frozen run manifest gains the PAIR `workspace_root_path` + `workspace_root_id` at admission freeze, bound by the recipe (the E3-checkable relation: id = SHA-256(path)); immutable per `run_id` like every manifest member.
  4. **The writer-to-reader carrier (a named §B.1 frame supersession):** the worker `assign` frame (m-10 → worker at lease-bind) gains the same PAIR — the ONE legal disclosure path (m-10-authored frame, no store read-back, no new channel); the exact member encoding SETTLES JOINTLY with m-9 like the other parked wires. **Status (R11-F1 join gate, provenance corrected per R12-F1):** the `assign` writer-to-reader carrier contract is pair-settled and folded on m-10's side but **JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D join co-sign**. Its settled-pending-join status binds to the **master settlement records** (`step3-relock-dag-m10/RECONCILE-orchestrator-planner-20260722-230000` + `…-20260723-001500`: S-4/S-5 pair-settled, fold behind fresh reviews, join afterward) and the **pair-axis settlement** (m-9 `…DESIGN-planner-m9-20260722-231500` confirmed the recompute-verify buildable; m-10 `…234500`), **NOT** the m-9 r12 receipt ledger `:299`/`:473`/`:511`, which covers only the S-1/S-2 receipts and does not fold the S-5 `assign` carrier. The one part independently normative on m-10's side is the **m-10-LOCAL recipe** — the run-start `workspace_root` input, the realpath/NFC/SHA-256 derivation, and the immutable §C.1 manifest pair — which is m-10's own producer property and does not depend on the joint `assign` wire; the **joint `assign` carrier** (its exact member encoding + the worker recompute-verify seam) is explicitly EXCLUDED from that local-normative claim and rides the join. **The consumer check (deterministic shared derivation):** the worker recomputes SHA-256 over the received `workspace_root_path` and fails closed on mismatch with the received `workspace_root_id` (assign-fault, no attach) — so the ticket value m-10 freezes and the value m-9 records are byte-equal by construction, and E3 can compare all three (manifest, ticket, executor record) as one identity.
- **The validation split (no authority widening; the claim NARROWED per R2-F3):** m-10 validates the supplied members (`canonical_resource`, `cwd`) as canonical-FORM strings ONLY — the grammar, NFC form, no `..` segment, no trailing slash, root `"."`, `length ≤` the §4 bounds. **m-10 does NOT and cannot verify symlink-resolution truth from a declared string** — whether the declared form actually names the resolved target is executor-side reality, checked by E3 (`local_invocation_matches_effect_descriptor` over m-9's record), never by my gate; m-10 persists what the executor declared, frozen at mint (confusion-not-malice: a false declaration is E3-detectable evidence, not an m-10-preventable act). m-10 NEVER interprets args. **The dispatch gate extends §D.2's serve gate:** a request whose descriptor members are absent-where-R, present-where-∅, or form-invalid is refused at issue (typed, row-less where the existing gate is row-less) — never defaulted, never partially bound.
- **Replay identity + consume comparison (pinned):** the §D.2 (0) replay-identity predicate EXTENDS to compare the stored supplied-member set (`canonical_resource`, `cwd`) alongside the existing triple — a same-key re-ask with equal triple but different declared operands ⇒ `IDENTITY_MISMATCH` (the issue-side twin; a stable executor derivation never trips it). **The §D.3 consume wire is UNWIDENED** — the frozen four-field frame stands; the descriptor is row-frozen at mint and needs no re-presentation (invocation-time drift is E3's to catch, not consume's).

**The invocation-resolved members are NOT mine to store:** `env_digest` (SHA-256 over the JCS-sorted COMPLETE env set actually presented — the m-1-sanitized set) and `shell_interpreter_ref{path, version, content_id}` are m-9 executor-derived at invocation, recorded m-9-side, and checked by m-3's `local_invocation_matches_effect_descriptor` (verdict `unknown` HOLDS the leg when `content_id` is unobtainable — never a weaker pass). My schema DECLARES them (per-action R/∅ per the table) as invocation-stage members; the frozen §D.4 record frame gains NO member (the four-member closed frame is untouched — C is the EVIDENCE half; the evidence rides m-9's record + E3, not my store). A nonexistent cwd at spawn is m-9's pre-spawn typed reject (`rejected_local`-class), never a descriptor value. m-1's env/no-leak review is the §9 hook. **No authority claim:** the descriptor authorizes nothing beyond what the frozen 8-name constant + F59 already authorize (H-21 is the Step-4 authorization successor); no containment/per-effect-hold claim is made (amendment D2 caveat).

## §6 — M10-C0: the broker-protocol fold into contract r40 (the mechanism fold; superseded clauses named byte-exactly)

The following r40 text is SUPERSEDED by this delta (r40 stays the frozen historical lock; nothing else in r40 moves). The replacement realizes the study rev8 mechanism I confirmed at `214500`.

1. **§B.5 items 2–8 + 7a ENTIRE** (propose-with-`epoch_transition_id` · `crossing_set`/durable-`PREPARING` · `CROSSERS_DURABLE` one-transaction crossing rows + ack-gated install · `cross_epoch_completion` application · recovery-by-transition-ID with resume/abort · CI-3's crossing/ledger event handling · `INSTALLED`-classification-from-`epoch_installed` · the replacement sequencing hook) — **REPLACED by my half of the simplified protocol:** (1) §B.5.1 durable-then-visible stands UNCHANGED (E+1 committed before anything carries it — the proposal is authority); (2) send **`state_proposal{proposal_correlation, run_id, generation_id, turn_epoch, lease_state, state_seq}`** on CI-1 (`proposal_correlation` attempt-scoped, correlation-ONLY — no durability semantics; counters as canonical decimal strings, §A.2); (3) consume **`state_proposal_result`** — the closed response schema carried from the study verbatim: `{proposal_correlation (echoed), disposition ∈ {installed, transition-started, rejected-stale, rejected-transition-active, rejected-malformed}, installed_state}` with `installed_state` = the canonical trusted tuple, REQUIRED-iff-`installed`, ABSENT otherwise, unknown fields reject; **under the TOTAL m-10 receiver fold (F6 — every disposition + absence has one bounded control action):**
   - **Correlation lifetime:** the serialized applier holds AT MOST ONE outstanding proposal per run; the correlation is live from send until (result consumed | `PROPOSAL_RESULT_DEADLINE` expiry = `EPOCH_DRAIN_DEADLINE` + one conductor-round-trip margin, compiled); a result bearing a dead/unknown correlation is DISCARDED fail-closed (never consumed, never proof); a duplicate equivalent result is idempotent (consumed once, no second gate event).
   - **`installed`, tuple == my CURRENT durable tuple** ⇒ install proof recorded — the two-form gate condition is satisfiable; proceed per (4).
   - **`installed`, tuple MISMATCH** ⇒ fail-closed: assign NEVER opens on it, recorded, and because my proposal was derived from my current durable state a mismatched install answer is store/controller divergence ⇒ the §10 loud surface + operator disposition — no retry loop (retrying cannot converge a divergent authority).
   - **`transition-started`** ⇒ the gate STAYS CLOSED; m-10 awaits EITHER a tuple-matching `epoch_installed` event OR `PROPOSAL_RESULT_DEADLINE` expiry ⇒ re-propose (the broker's row 3/row 5 make the re-proposal join-or-ack — idempotent, no second cut, no deadline reset; the drain's own local deadline bounds how long `transition-started` can recur, so the re-proposal cycle is bounded, never busy — each wait is a full `PROPOSAL_RESULT_DEADLINE`).
   - **`rejected-transition-active`** ⇒ the gate STAYS CLOSED; the active transition's target/deadline are untouched by rule; m-10 re-proposes after `PROPOSAL_RESULT_DEADLINE` (by which the pending install has completed at its local deadline — the next answer is `installed`/`transition-started`, converging); never a tight retry.
   - **`rejected-stale`** ⇒ INVARIANT FAULT: my proposal carries my current durable tuple, so a stale verdict means the broker holds NEWER trusted state than my durable rows — possible only if another controller advanced control (adoption superseded me) or my store regressed; this incarnation MUST NOT proceed — fail-closed, loud, NO re-proposal, operator disposition (the control-lock/generation machinery is the arbiter, not a retry).
   - **`rejected-malformed`** ⇒ INVARIANT FAULT in my own frame construction ⇒ fail-closed, loud, no auto-retry (a malformed constructor loops if retried).
   - **Absent result** (frame-fault path or loss) ⇒ `PROPOSAL_RESULT_DEADLINE` expiry ⇒ re-propose (bounded as above).
   - **Crash with a proposal outstanding** ⇒ the correlation dies with me; recovery re-proposes fresh (5) — the replacement never depends on the old correlation.
(4) **the TWO-FORM assign gate keyed by the canonical trusted tuple `{run_id, generation_id, turn_epoch, state_seq}`** — a valid tuple-matching `epoch_installed` event OR a valid tuple-matching `state_proposal_result{installed}`, either opens the same logical gate, EXACTLY ONCE per tuple by my durable single-writer rows (proof is idempotent evidence; assignment uniqueness stays lifecycle-owned — the keyed-transaction machinery that makes retirement exactly-once); tuple-mismatched evidence rejects fail-closed — **and because the tuple includes `state_seq`, an OLD `epoch_installed` event can never match the current tuple while a newer transition is pending: the gate cannot open on stale evidence during any active transition (F6's race, closed by the key itself)**; (5) **recovery = re-proposal of the CURRENT durable tuple, always** — no transition identity survives anything because none is durable.
2. **§B.4 step 1's clause** "durably allocate the stable `epoch_transition_id` + its `epoch_transitions` ledger row (state `PROPOSED`) — so no post-retirement crash cut lacks a transition ID (R8-F1)" — **DROPPED** (no ledger exists; a crash cut needs no transition ID — the replacement re-proposes the durable tuple). The retirement transaction is otherwise UNCHANGED: retire + release lease + mint E+1 + pre-allocate G+1 + the state-sensitive F59 park + `state_seq` bump, one atomic commit. **§B.4 step 2's** "the §B.5 broker transition handshake for E+1" reads "the §6.1 `state_proposal` exchange (drain-bounded per the study)"; the interregnum `epoch_state` tuple, the non-attachability proof, and steps 3–5 (install-precedes-lease-bind-precedes-`assign`) are UNCHANGED — the AUTHORITY order survives the mechanism swap byte-for-byte in effect.
3. **§B.3's recovery-matrix transition machinery:** branch (b)'s substate totality over `PROPOSED`/`PREPARING`/`CROSSERS_DURABLE`/`INSTALLED`/`ABORTED`; the common-suffix R9 clause ("when the current durable epoch carries a non-terminal or `ABORTED` transition, m-10 supplies NO install-eligible snapshot … only the exact `CROSSERS_DURABLE` ack may install E+1"); the ENTIRE recognition × commit-state sub-matrix (all five bullets incl. the R10-F1 lost-install replay) — **REPLACED by:** branch (b) = `RETIRED_PENDING_REAP` committed ⇒ continue at reap/spawn (no substates exist); the common suffix = (1) establish the CI-1 control session (adoption or fresh spawn — §2.10 machinery unchanged); (2) **ALWAYS propose the current durable tuple** — it is install-eligible by construction (E+1 was committed before any crash could matter), and the broker's ordered disposition table is total over every cut: fresh instance ⇒ bootstrap install; already installed ⇒ idempotent `installed` ack; mid-drain ⇒ join (`transition-started`, original deadline); same-epoch newer state (wash-out) ⇒ ordinary install; genuinely newer epoch ⇒ the bounded-drain transition; my two-form gate then opens assign exactly once; (3) fresh channel set → connector bootstrap → lease-bind → `assign` → admission, unchanged. **No withholding logic exists** — there is nothing to withhold for, and the suspended broker's fail-closed floor (m-7 §2.4, unchanged) covers the control-absent window.
4. **§F rows `epoch_transitions` and `crossing_ops` — DROPPED ENTIRELY** (no replacement rows; the CI-3 shrink I confirmed). **§F row `broker_events` — AMENDED:** the closed family per study §Q3.3.3 — loses `crossing_set`/`cross_epoch_completion`/`transition_aborted`, gains **`boundary_cut`**`{epoch_transition_id, operation_id, generation_id, admitted_epoch, op, disposition ∈ {unknown-outcome, stale-cut}}` (uncoupled, at-least-once + key-dedup); **`epoch_installed` amended to `{epoch_transition_id (correlation-only), generation_id, turn_epoch, state_seq}`** — `crossing_count` REMOVED; a frame carrying the removed member REJECTS under the existing unknown-field rule; `UNIQUE(broker_instance_nonce, event_seq)` + ack-after-durable-commit + same-ack-on-duplicate UNCHANGED.
5. **§B.2's `turn_open` closed frame** — EXTENDED per §4 (`session_log_path` always; `settlement_manifest` + `predecessor_turn_id` present-iff-continuation); every other member + the D-4 discipline + the sizing gate UNCHANGED (the gate's totality now spans both overflow families per §4).
6. **§H's m-7 row** — the CI-3 clause re-cites on the study's approved bytes `64f9136e…` (broker_events + ack survive; crossing rows + ledger dropped; the R9/R10 delta clauses die with the machinery they qualified); **+ CI-4** joins the consumer map (the broker-spawn discipline, §8). §H's m-9 row gains the §2/§4 joint items (receipt frame · disposition report/receipt · first-action realization · the conditional §3 branch). Everything else in §H stands.

## §7 — M10-C1: the cut relay identity binds into D2/D3 EXACTLY ONCE

A broker epoch-boundary cut of a relay tool call produces **NO new record, class, or carrier anywhere in this design**: the identity already lives in my canonical rows under the state-sensitive map (consumed-no-outcome ⇒ `UNKNOWN_TOOL_OUTCOME` on authorization + `tool_calls`; issued-unconsumed ⇒ `VOID/expired`; terminal stays terminal), and §1's mapping carries EVERY cut relay identity into **`uncertain`** — the parked `UNKNOWN_TOOL_OUTCOME`/`PARTIAL_TOOL_EFFECT` rows by their state cells, and the issued-unconsumed `VOID/expired` relay rows by the §1 `expired × relay` cell (R2-F1 — §1 and this section now state ONE reading, the confirmed seam's) — the disclosed identity reaches the successor through `parked_unknown` (the parked states) + the manifest entry, ONCE. **`boundary_cut` events are telemetry and NEVER a manifest input** — the invariant, fixture-backed (§11): no manifest entry is ever derived from a `broker_events` row; the single derivation source is the canonical rows. Reconciliation = informed rediscovery via the worker's own seat capability; re-invocation = a fresh F59 ticket, never automatic. (The co-signed §D join record: the m-10 PAIR confirmation = my planner half `step3-relock-broker-confirm/RECONCILE-planner-20260721-214500` Part B + the completing implementer half `RECONCILE-implementer-20260721-221000.md` @ `375da939…` (F8), binding against the study bytes — this section is their design realization, same reading, no drift.)

## §8 — M10-C2: the stage-5 r10 sweep + the affirmative CI-4 broker-spawn realization

The following r10 text is SUPERSEDED (r10 stays the frozen lock): **the §3 startup step 3's "`epoch_state` for E=1 published per §B.4/§B.5 rules" reads "published per §B.4 + the §6.1 `state_proposal` exchange" (F7);** the §3 boot line's "pending-transition rules; broker install before lease-bind" reads "re-propose the current durable tuple; the two-form install proof precedes lease-bind"; **the §4 crash-loop paragraph's "then the §B.5 distribution/install sequence runs" reads "then the §6.1 propose/install sequence runs" — the paired-owner ONE-retirement/ONE-E+1 behavior and the install-before-successor-authority order are UNCHANGED, only the retired broker mechanism is replaced (F7);** the §4 retire/replace line's "fence + park + E+1 + transition-ledger row" drops the ledger clause and "§B.5 handshake" reads "the §6.1 `state_proposal` exchange"; the §6 epoch-linearization line's "ledger-row" member drops (mint, park, fence remain one serialized commit); **census row `m10-app-main-recovery`** — `policy_artifact` drops the §B.5 citation for the §6.3 replacement; `canonical_record` drops `epoch_transitions`; `failure_unknown_semantics` reads "re-proposal convergence total over every cut (bootstrap/idempotent-ack/join/ordinary-install/drain); children EOF-fail-closed, never adopted"; `replay_idempotency` reads "one-mint by `(run_id, retiring generation_id)`; equal re-proposal acks idempotent"; **census row `m10-worker-retirement-epoch-mint`** — `enforcement_point` drops "+ ledger row"; `canonical_record` = `workers`/`epochs` + parked rows (drops `epoch_transitions`/`crossing_ops`); `outcome_validator` reads "the committed-once key + the two-form install proof"; `failure_unknown_semantics` drops the recognition×commit matrix for "crash converges on ONE mint; recovery re-proposes"; **census row `m10-epoch-publication`** — `enforcement_point` drops the R9 snapshot-withholding clause (ordering install-precedes-lease-bind stands); `failure_unknown_semantics` reads "lost `epoch_installed` ⇒ the re-proposal's `installed` ack is equivalent proof (two-form)"; **§14's** "each recovery-matrix branch (a)–(d) with the §B.5 substates" reads "branches (a)–(d) with re-proposal recovery; the shared broker battery FX-TB-5′/17′/20/21 (the reciprocal m-10-side rows per study §Q3.3.7)". **The re-sweep record (F7):** the full r10 bytes were re-searched this revision for `epoch_transitions` · `crossing` · `CROSSERS` · `§B.5` · handshake · transition-ledger · pending-transition · lost-install vocabulary — every LIVE occurrence is superseded above (the §3 step 3 + §4 crash-loop clauses were the two the rev1 sweep missed); the remaining occurrences sit in r10's Status/Consumed header lines, which are the historical revision record, named as history exactly as the broker study treats r11's.

**The affirmative CI-4 realization (NEW, the gap my broker confirmation named):** the broker spawn is a distinct spawn class with its OWN discipline — spawned in its OWN session + process group (`setsid`), **no `PDEATHSIG`, no kill-on-parent-exit rule, and membership in NO process group any m-10 exit/crash path signals** — structurally outside the app-main death set, unlike the worker/connector (whose §B.3 EOF-fail-closed teardown + group-kill discipline is exactly what the broker must NOT get). Supervision of a suspended broker stays `CONTROL_REATTACH_DEADLINE` policy (m-7 §2.10; killing/respawning is an explicit m-10 decision, never an exit side-effect).

**NEW census row (schema-canonical, all 21 fields):**
**m10-broker-spawn** — `effect_id`: m10-broker-spawn · `effect_class`: process_lifecycle · `requester`: run/controller bootstrap (no live broker to adopt) · `executor`: m-10 spawn path · `authority_source`: CI-1 (m-7 §2.10) + CI-4 (study rev8 §Q1.1) · `policy_owner`: m-7 (the broker contract) · `policy_artifact`: m-7 r11 @ `9331ea88…` as amended by study rev8 @ `64f9136e…` · `decision_point`: the adopt-or-spawn branch of the boot sequence (probe the control lock; spawn only when no adoptable broker exists) · `enforcement_point`: the spawn call's CI-4 discipline (own session/pgroup; no parent-death kill of any form; spawn-pipe token handoff only) · `exclusive_credential_holder`: the broker (seat credential via the §2.12 operator-authored sink — m-10 passes NO credential bytes; F67 set `{m-8, broker}` unchanged) · `request_freeze_point`: `not specified` (a spawn, not a request pipeline) · `authorization_linearization_point`: the durable `control_generation` advance (the `broker_control` transaction) · `effect_linearization_point`: OS process creation (pid) · `outcome_reporter`: the broker (CI-1 hello/control handshake) · `outcome_observer`: the OS process table via the control-lock probe (independent of the broker's claims; NOT `waitpid` — the broker is deliberately not reaped-on-exit by m-10's child machinery) · `outcome_validator`: the CI-1 token + strictly-greater-generation check · `canonical_record`: `broker_control` · `bypass_paths`: same-UID ambient process creation + same-UID direct store write (§9) — the factored residual, per-row as everywhere · `failure_unknown_semantics`: spawn-fail ⇒ retry under the G-2 counter? NO — the broker is NOT a supervised child of the G-2 counter (that counter is worker/connector-scoped by its frozen definition); broker spawn-fail ⇒ loud §10 surface + operator disposition, run cannot proceed (fail-closed, no silent retry loop) · `replay_idempotency`: the control lock + strictly-greater `control_generation` make a duplicate spawn/adopt race converge on ONE controller · `threat_claim_scope`: confusion-not-malice; same-UID residual accepted.

## §9 — Cross-owner review hooks (named, not silently absent)

**m-1 — RULED CONFIRM on my exact bytes** (`step3-relock-dag-m10/SITREP-planner-20260722-015123`, byte-bound; S1/S2/S3 all confirm, **no finding, no m-10 edit owed**; F60/F67/the no-payload-column rule/K6 each verified rather than accepted; `resume_snapshot` ruled a column in the already-reviewed private store, not a new at-rest artifact). **Their ONE BINDING CARRIER CONDITION, folded here as explicit negatives:** the absolute **`workspace_root_path`** and the **`session_log_path`** must **NEVER** ride a conductor record, projection, INDEX row, typed error, or E0 body — so (a) a `workspace_root_id` recompute-mismatch fault must not echo the offending path into any typed error that could reach a conductor surface, and (b) no run-terminal `stop_reason`, refusal, or fault frame of mine names either path. My live carriers (the frozen manifest, my private store, the app-side `assign`/`turn_open` frames to a worker that already holds both paths) are compliant; any new carrier crossing to a conductor or seat-delivered surface routes back to m-1. **The residual reviews below stay theirs.** — the log-content + at-rest secret-leak review incl. the K6 exclusion (amendment §5-D retention para) — over m-9's log design + my snapshot/receipt rows (which hold ids/digests/terminals only; no payload column exists by schema — the review confirms, my schema enforces); the C-ticket env/no-leak review (§5; the sanitized-env rule is m-1's, digested not re-cleared). **m-9 — the settlement is EXACT-FOLDED and PAIR-MATCHED but NOT yet joint-closed: the widened S-1 conflict predicate is folded and pair-approved at their r12 §2 (matching m-10's rev8/this rev — one predicate on both sides), which discharges the "no matching producer" half of R8-F2; but m-9 r12's join-gate rule (`:473`: both pairs approve matching bytes AND the §D join co-signs) keeps every §D-settled frame JOINT-PENDING / NOT NORMATIVE until then — while its receipt ledger (`:299`/`:473`/`:511`) folds only the S-1/S-2 receipts and `:472` expressly PARKS S-4 — so per seam: **S-1/S-2 are exact-folded in m-9 r12; S-4 is pair-settled and folded on m-10's side but PARKED on m-9 pending this pair-approved hash + m-9's rebase (`:472`/`:474`); S-5 is pair-settled and folded on m-10's side under the master/pair records (§5); all four are NON-NORMATIVE until their required folds/approvals AND the §D join (R9-F1/R13-F1)** — no blanket "all four exact-folded" claim, which `:472` refutes for S-4. The basis is REBOUND to their pair-approved r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35` (r5 through r11 VOID/superseded — the §D-settled §2/§3/§7 seam semantics are byte-carried across the r7→r12 arc, verified). Formerly bound to r7 `f191c69c22f2609fe5a10fa2e6d4262b9e196dbf5e24b1773563c30fb1f3bda3`** (r5 `c0ff74f5…`/r6 `fe0ac866…` VOID; this delta binds ONLY r7 — and the §D-settled seam semantics at their §2/§3/§7 are byte-unchanged across their rebase, verified at their bytes, so nothing settled is renegotiated). **S-1 (receipt — FRAME SHAPE settled, widened predicate pair-matched at r12; EXACT-FOLDED / JOINT-PENDING until the §D co-sign, R9-F1), S-2 (report/receipt/`disposition_conflict`), S-3 (five families; `relay.*` with m-2), S-4 (my manifest schema, confirmed consumable byte-exact — and no m-9 mechanism depends on `entries[]` order: my ordering exists for the `resume_snapshot` digest check and constrains them not at all), S-5 (the `assign` workspace-root pair with their recompute-verify, confirmed buildable) — **each remains JOINT-PENDING / NON-NORMATIVE until m-10 pair approval AND the §D join co-sign (R10-F1), the join retiring that status — but their fold provenance differs and R12-F1 requires it stated exactly: S-1/S-2 (the receipts) are EXACT-FOLDED at pair-approved m-9 r12 bytes (r12 `:299`/`:473`/`:511`, the receipt ledger items 4-5); S-4 (my D2 manifest wire schema) is pair-settled and folded ON M10'S SIDE but is expressly PARKED in m-9 r12 (its ledger `:472` item 3, "nothing here consumes them as settled") pending THIS m-10 pair-approved hash and m-9's ensuing rebase/fold before the join (r12 `:474`); S-5 (the `assign` workspace-root pair) is pair-settled and folded on m-10's side per the master settlement records (`step3-relock-dag-m10/RECONCILE-orchestrator-planner-20260722-230000` + `…-20260723-001500`) and the pair-axis settlement (m-9 `…DESIGN-planner-m9-20260722-231500`/`…235500` × m-10 `…234500`), NOT the r12 receipt ledger.** S-3's five settled families are folded, its `relay.*` cell shape now RATIFIED as Correction 3 (m-2 cell `5ec7a3d2…`, its consumer-fold m-9's §7-`relay.*`, not this rev) — S-3 is not swept into the S-1/S-2/S-4/S-5 join gate.** Their §1.6 per-run `flock` writer fence is m-9-owned, so **my §3 conditional segment-producer branch is permanently INERT**. Their re-surfacing proposal was **withdrawn by its own author** (it would have moved a worker-independent guarantee onto worker-dependent machinery) — the run-wide restore above is the agreed repair. *(Historical, superseded: the §2/§4 frames were parked non-normative while the shapes differed — that record reads:* **the current m-9 basis is their PAIR-APPROVED producer delta r5 @ `c0ff74f5589e0dcbd066fe8a7e04ee7fafd53a9a20befa6453084110feccf70b` (R5-F2 — a pair-approved PRODUCER PROPOSAL, which is NOT joint settlement; the joint settlement consumes their r5 bytes × these bytes)**; the §3 conditional branch rides their writer-fence selection at their current pair-approved bytes (the worker-owned OS-lock branch — my §3 producer branch stays inert unless the joint settlement moves it); **and the §4 D-4 SCOPING SUPERSESSION — the `turn_disclosures` six-field immutable snapshot with the SPLIT source rules (`turn_open` = snapshot-replay; `attempt_open_ok` = live-derived, the Gate-2 equal/added/changed/removed totality preserved) — is a NAMED m-9 F73 confirmation item (R3-F1 as re-based by R5-F2): their frozen r21 consumes D-4 as the two-gate contract over the run-wide array, so the changed producer semantics requires their byte-bound confirmation over THIS rev's mechanism (the superseded rev4 `disclosed_by_turn_id` relation is history, not a live obligation) and is never closed m-10-locally.** **m-3:** the E3 predicate consumption of the §5 descriptor is theirs; nothing here authors an E3 predicate.

## §10 — PARKED consumer sections (rev2 staging; named so absence is never silence)

**DISCHARGED ELSEWHERE at rev7:** master's carriage signal (`step3-relock-dag-m9/RECONCILE-orchestrator-planner-20260723-093000`) landed with the producer bytes settled (m-8 r5 `c0b7b488…`, m-9 r7 `f191c69c…`), and the B/E carriage is authored as a **SEPARATE bounded artifact** — `master/domains/m-10-app-control-plane/design/2026-07-23-lane2-be-carriage-row.md` — deliberately not folded here, so m-3's binding is not coupled to this doc's §D-settlement amendment cycle. This section's parking is therefore discharged by that artifact and nothing B/E-related is owed in this doc. *(Historical record of the parking:)* The **B-row** (carry m-8's `frozen_core_digest` on `provider_attempts`) and the **E-row** (carry m-9's `logical_surface_digest` AND m-8's `provider_lowered_tools_digest` on the exact attempt identity, without re-hashing either producer's bytes) were DELIBERATELY NOT designed in the earlier revs — they await the exact pair-approved producer bytes per the rev2 staging rule and the release's parking clause. They will land as a bounded rev of THIS doc when master signals the producer bytes are settled; the final pair review covers settled bytes only.

## §11 — Fixture obligations (delta rows)

FX-M10-D2-1 producer-totality: a synthetic ancestry covering EVERY row state in §1's input domain — incl. **the full orphan-`VOID` split (R2-F1): each of the four denial reasons × any tool ⇒ `determinate_no_resume`; `expired` × EACH of the three relay verbs ⇒ `uncertain` (the confirmed cut disclosure); `expired` × a local tool ⇒ `determinate_no_resume`; an ordinarily-expired (non-cut) relay ticket ⇒ `uncertain` (the over-surfacing leg, asserted deliberate)** — ⇒ every domain row appears in exactly one class; an out-of-vocabulary state / orphan-`ISSUED` ⇒ fail-closed, no manifest, no admission; **no `broker_events` row is read by the producer on any leg (asserted)**. · FX-M10-D2-1b the discriminated union (F1): a consumer decode of a manifest with a missing `kind`, an unknown `class`, an extra member outside the kind/class shape, and a `void_reason` on a non-orphan entry ⇒ each REJECTS fail-closed. · FX-M10-D2-2 completed-without-receipt ⇒ `uncertain`; receipt-then-manifest ⇒ `settled_with_content`; receipt AFTER production ⇒ the produced manifest is unchanged (immutable snapshot). · FX-M10-D2-3 determinism: equal durable state ⇒ byte-equal manifest (ordering rule proven). · FX-M10-R receipt acceptance (over the m-9-r12-matched frame, R8-F1 — the rev8 clause named only "equivalent duplicate / conflicting digest" and contained no `segment_id` or `seq_hwm` token, so a marker-only implementation still passed it; these legs are written to KILL that implementation): **(a)** all six evidence members equal ⇒ **idempotent**, one row, same result; **(b)** same key + same `marker_digest` + **different `segment_id`** ⇒ **`receipt_conflict`**, the first complete tuple stands unchanged; **(c)** same key + same `marker_digest` + same `segment_id` + **different `seq_hwm`** ⇒ **`receipt_conflict`**, first tuple stands; **(d)** different `marker_digest` ⇒ `receipt_conflict` (the original leg); plus the retained **stale sender** ⇒ rejected, no row · **unknown attempt** ⇒ fault, no row. Legs (b) and (c) are independent locator mutations, each asserted alone. · FX-M10-D3-1 the crash cuts: before admission commit ⇒ predecessor `INTERRUPTED`, idempotent retry; after commit before send ⇒ byte-identical re-emission (manifest + inherited `admission_ref` byte-equality asserted), disposition `PENDING`; the §4 report/receipt cuts each converge (same receipt, same committed PAIR, on replay). · FX-M10-D3-2 `UNIQUE(run_id, predecessor_turn_id)`: a second successor attempt fails structurally. · FX-M10-D3-3 frame-totality (per R2-F2.4 → R3-F2 → R4-F2): the PRODUCTION leg builds the defined max-witness vector (max cardinalities, longest grammar-legal ids, longest legal enum literals, max encoded ref, max path), COMMITS, and **asserts measured size ≤ `FRAME_CONTENT_BOUND` = 3,704,832 B — NO equality pin (no attaining witness exists; the bound is conservative by construction)**; exact-fit-at-the-limit AND one-byte-over (continuation) run **under the build-tagged TEST-ONLY reduced limits table with real closed-shape bytes** ⇒ the over leg commits `runs.FAILED` + `resume_frame_overflow` + `resume_action = operator_new_run` in ONE transaction, NO successor/lease/snapshot row exists, loud surface asserted; one-byte-over (initial) ⇒ `admission_refused{task_input_frame_overflow}`, zero durable effects — **plus the ENCODED-reserve leg (R2-F2.3): a raw-short but escape-heavy `task_input` whose JCS-encoded contribution exceeds `ADMISSION_REF_ENC_MAX` ⇒ refused at the operator boundary (the raw-length gate would have passed it — asserted)**. · FX-M10-CAP the parked-row cap + terminal (rev7, boundary CORRECTED per R7-F2 — the rev7 legs enforced an effective cap of 511): **the rule is post-commit count > 512**, so the two boundary legs are **(i) 511 parked + a 1-row retirement ⇒ post-commit 512, NOT greater than the cap ⇒ the run CONTINUES normally** (no terminal, successor spawned as usual) and **(ii) 512 parked + a 1-row retirement ⇒ post-commit 513 ⇒ the retirement commits IN FULL (fence, park, mint, all asserted) AND the same transaction commits `runs.state = FAILED` + `stop_reason = parked_unknown_capacity_exceeded`**, loud §10 surface rendered, every parked identity queryable, **and the FULL ratified acceptance predicate observed in the ONE transaction (Correction 2 §2.2 rule 4): the complete retirement batch (all parked rows committed) · the SINGLE `runs.state=FAILED` + `stop_reason=parked_unknown_capacity_exceeded` terminal · NO successor generation, NO same-run continuation, NO lease, NO snapshot, NO revival · the durable operator projection rendering the terminal + the COMPLETE queryable parked set** (this leg does NOT reuse the `resume_frame_overflow` fixture — the cap terminal's lifecycle is proven on its own bytes). **(iii) The multi-row overshoot leg (R7-F2's load-bearing case):** a retirement parking N rows at once from 510 ⇒ **all N are durable and queryable and the post-commit count legitimately EXCEEDS 512** (proving `MAX_PARKED_ROWS_PER_RUN` is an enforcement threshold, not a 512-row storage cap) — asserting the batch is never truncated to remaining capacity, no prefix path exists, **no successor/lease/snapshot/revival is created**, and the terminal commits in that same transaction. **(iv) The no-truncation negatives:** no disclosure array is ever shortened and no parked row is ever dropped or overwritten to fit a bound, on any path (schema + frame-content assertions). · FX-M10-ACK-BOUND the second compile-time assertion (rev7, m-9's check (i)): a build configured with a cap that would violate `ATTEMPT_ACK_MEMBERS_MAX + (MAX_PARKED_ROWS_PER_RUN × PARKED_ROW_MAX) ≤ FRAME_MAX` **fails to compile** (asserted as a build-level negative test, not a runtime one); at the shipping cap a maximum-cardinality `attempt_open_ok` is constructed and asserted to serialize ≤ 328,704 B and to be accepted, never a channel fault. · FX-M10-WR-R the root refusal table (R3-F3 as ordered by R4-F3): a RELATIVE input **with a crafted ambient cwd that would resolve it — asserting the cwd is NEVER consulted** ⇒ `not_absolute`; `/` ⇒ `filesystem_root`; **a symlink RESOLVING to `/`** ⇒ `filesystem_root` (row (2) on the resolved path); a nonexistent path ⇒ `unresolvable`; an inaccessible/looping path ⇒ `unresolvable`; **an OS path-length failure inside realpath** ⇒ `unresolvable` (never `too_long`); a resolved path with out-of-grammar bytes ⇒ `out_of_grammar`; over-length ⇒ `too_long`; **a path BOTH out-of-grammar AND over-length ⇒ the ONE token `out_of_grammar`** (row order) — EACH asserting zero run/manifest/store effects; the convergence legs (FX-M10-WR) run over the admitted domain only. · FX-M10-D4 the disclosure scoping (R2-F2.2 + R3-F1 + R4-F1 as corrected by R5-F1): the admission transaction writes T's six-field `turn_disclosures` snapshot rows; **the snapshot cuts — crash BEFORE the commit (no snapshot, no turn; retry re-selects identically) · crash AFTER commit BEFORE first send (recovery re-derives turn T's array from T's SNAPSHOT rows — BYTE-IDENTICAL) · duplicate `turn_open` replay after a lost send (same snapshot, same bytes) — byte-identity is claimed for T's snapshot-backed `turn_open` ONLY.** **The Gate-2 legs, CORRECTED at rev7 (the rev5/rev6 leg (b) demanded an emission the closed wire shape forbids — both pairs' independent traces agree the branch is MVP-UNREACHABLE, so it is WITHDRAWN rather than restated):** on the MVP's reachable states the parked set cannot grow, and a parked row's fields cannot change, while a worker is alive between the gates — `UNKNOWN_TOOL_OUTCOME`/`PARTIAL_TOOL_EFFECT` are written at exactly one locus inside the §B.4 step-1 retirement (which kills the worker awaiting Gate 2), and the §D.4 terminals are unreachable for a parked row's retired generation (§D.4 row (8) rules the current-sender × `UNKNOWN` case invariant-impossible). Therefore: **(a) the REACHABLE comparison leg is `equal`** — Gate 1's snapshot and Gate 2's live derivation agree, asserted end-to-end across a multi-attempt turn; **(b) the REACHABLE validation legs stay and are asserted** — a malformed member and a duplicate identity within one list each ⇒ fail-closed, no DATA-P (Gate 2's *validation* is not vacuous even where its *comparison* is); **(c) the drift-detector legs are asserted with a TEST-ONLY injected mutation** (no production path can produce one): an injected added/changed row fires m-9's branch correctly, proving the comparator still catches producer drift a future revision could introduce. For `attempt_open_ok` the assertions are LIVE RE-DERIVATION + the m-9 comparison outcome — never reply-byte identity. **The run-wide legs (rev7):** every `turn_open` and every `attempt_open_ok` carries the FULL parked set (asserted across an ORDINARY non-continuation turn — the case the withdrawn scoping left with no carrier at all), the identity stays queryable, and a compaction-evicted disclosure is re-delivered by the very next frame. · FX-M10-WR workspace-root identity (R2-F3): trailing-slash / non-NFC / symlinked spellings of one root ⇒ ONE `workspace_root_id` (recipe determinism); the worker's recompute-mismatch ⇒ assign-fault, no attach; manifest pair immutability (post-freeze mutation fails structurally); id = SHA-256(path) checkable from the manifest pair alone. · FX-M10-D3-4 disposition (F3): `PENDING→RESUMABLE` then a conflicting `degraded` report ⇒ `disposition_conflict`, pair stands; **same disposition + DIFFERENT `resume_action` ⇒ `disposition_conflict`, never an equivalent replay**; `resume_action` present on `resumable` / absent on `degraded` ⇒ malformed, fail-closed. · FX-M10-C-1 descriptor census: per-action R/∅ exactly per the ratified table (absent-R and present-∅ both fail closed); apply_patch's ordered target-set digest recomputes stably under path re-ordering; **the F5 replay-identity leg: a same-key re-ask with equal `{name, digest, epoch}` but a different declared `canonical_resource` or `cwd` ⇒ `IDENTITY_MISMATCH`, no row mutation**. · FX-M10-C0 (F6): the receiver fold total — one leg per disposition (`installed`-match ⇒ gate-satisfiable; `installed`-mismatch ⇒ fail-closed loud, no gate, no retry; `transition-started` ⇒ gate closed, event-or-timeout re-propose; `rejected-transition-active` ⇒ gate closed, deadline re-propose, no tight loop; `rejected-stale` ⇒ invariant fault, no re-proposal; `rejected-malformed` ⇒ invariant fault, no auto-retry; absent ⇒ deadline re-propose) + dead-correlation result discarded + duplicate result idempotent + **an OLD tuple-mismatched `epoch_installed` during a pending transition never opens the gate** + crash-with-outstanding-proposal ⇒ recovery re-proposes fresh; the shared broker battery (study §Q3.3.7's FX-TB-5′/17′/20/21) named as SHARED fixtures, not duplicated. · FX-M10-C1: a cut relay call yields exactly ONE manifest entry (`uncertain`, from the canonical row); a `boundary_cut` event with NO matching canonical row yields NO manifest entry (telemetry-never-input asserted). · FX-M10-C2: the broker-spawn census row's discipline (spawned broker survives an app-main SIGKILL; worker/connector do not — the death-set asymmetry asserted in one fixture).

## §12 — Decision record

1. **The manifest is produced inside the continuation-admission transaction from canonical rows only.** *Rejected:* production at parking time (the receipt-presence predicate must be read at admission — a receipt landing between park and admission would be silently missed); production from m-9 artifacts or broker events (a second truth source; §7's invariant). 2. **Receipt conflicts fail closed with first-committed-wins.** *Rejected:* last-wins (evidence replacement — a retired generation's honest receipt could be overwritten by a confused successor claim); reject-both (destroys committed evidence retroactively). 3. **Two overflow families, not one.** *Rejected:* unifying initial + continuation overflow (they differ in everything that matters — boundary, durable effects, terminality; the r40 member is operator-boundary-scoped by the R39/R40 arbitration and must not stretch). 4. **The G-2 counter bounds the chain.** *Rejected:* a separate chain-depth constant (a second counter with no new property; the failure counter already counts exactly the events that create continuations). 5. **Invocation-resolved descriptor members stay out of my store.** *Rejected:* extending the frozen §D.4 record frame (a fifth F73 cycle on a closed frame for members whose consumer is E3, not my gate; the amendment's owner split says m-9 records them). 6. **Always-propose recovery.** *Rejected:* any conditional withholding (the R9 machinery existed to protect crossing commits; nothing left needs protecting, and the broker's ordered table is total — a withheld proposal only delays convergence). 7. **The broker is outside the G-2 counter.** *Rejected:* folding broker spawn-fail into the supervised-child counter (its frozen definition is worker/connector-scoped; the broker is not a supervised child by CI-4's whole point — silent retry of the secret-holding process is exactly what should be loud). 8. **The orphan-`VOID` split: denials determinate; `expired × relay` uncertain; `expired × local` determinate (R2-F1, superseding both prior attempts).** *Rejected:* blanket VOID⇒`uncertain` (rev1 — misread the four denials, which never issued authority); blanket `expired`⇒determinate (rev2 — locally overturned the byte-bound confirmed seam, and the F59-gate argument is only sound where the consuming wrapper IS the effect path: for relay verbs the gate (m-10) and the effect (the broker) are separate processes, so unconsumed is not provably uninvoked); distinguishing cut-expired from ordinary-expired via `boundary_cut` (telemetry-never-input — the invariant §7 fixtures enforce; the over-surfacing of ordinary expired relay tickets is the accepted-safe price); excluding orphans from the domain (an issued-then-expired identity would vanish from the successor's view, violating producer-totality). 9. **Tighten the INITIAL gate rather than accept a doomed continuation (F4) — over ENCODED bytes (R2-F2).** *Rejected:* keeping the r40 initial bound and letting a large-but-legal `task_input` guarantee `resume_frame_overflow` later (a mid-run terminal for a fault decidable at the operator boundary is strictly worse on every axis); raw-length gating (rev2 — JCS escaping expands raw text up to 6×, so a raw bound is not an encoded bound; the gate measures what the frame actually carries); imposing a grammar on operator text (verbatim carriage is the ratified rule — measure, don't restrict). 21. **The D-4 scoping is WITHDRAWN; run-wide carriage restored and bounded by a loud cap (rev7, master ruling (3)).** *Rejected:* keeping the scoping (M9-D4-SCOPE-F2 — no carrier on ordinary turns, and a ratified compactor can evict the only remaining copy: the safety purpose defeated on a reachable path); m-9's consumer-side re-surfacing (it inverts r40 `:72`'s worker-INDEPENDENCE exactly where the guarantee matters most — the `content_lost`/`DEGRADED` path where their own durable state is what failed; withdrawn by its author too); truncation at the cap (silent under-disclosure, the one harm D-4 exists to prevent); unbounded growth (makes the frame-overflow terminal silently reachable for a long-lived run); a non-terminal "blocked run" (r40 `:82` gives a parked TOOL outcome NO clearing path, so a blocked run can never unblock — a dead run wearing a recoverable label); an operator-surface clearing path (Step-4 — an authority ingress the MVP defers wholesale; recorded by master as a Step-4 carry that could later relax this terminal). 22. **Gate 2 is relabeled, not deleted (master ruling (1), RATIFIED as Correction 1, operator 2026-07-25, packet `1fa71cb8…`).** *Rejected:* deleting the comparator (it costs nothing and catches exactly the producer drift a future m-10 revision could introduce); keeping the two-gate *guarantee* claim (both pairs' traces show the comparison is vacuous on every reachable MVP state — calling it a guarantee would be the confusion-firewall philosophy applied to everyone except ourselves); folding the relabel as settled (it changes a ratified operator-visible claim ⇒ operator ratification via master's bounded amendment). 23. **The `attempt_open_ok` bound is a compile-time assertion, not a stated number (rev7, m-9's check (i)).** *Rejected:* stating the bound in prose (the failure mode was not a wrong number but an UNASSERTED one — a gate cannot check what only prose asserts, and the runtime consequence would have been a channel fault killing a generation). 12. **The D-4 disclosure re-scopes to undisclosed-only, via a durable marker set in the carrying admission commit (R2-F2.2).** *(SUPERSEDED by 21 — the scoping is withdrawn; retained as the decision chain's record.)* *Rejected:* the frozen run-wide array (unbounded across chains — no frame-bound proof can exist over it); truncating the array at a cap (silent under-disclosure, the one direction the seam forbids); scoping to ancestry-only without a marker (a fresh chain's first turn_open would silently drop prior-chain identities that were never disclosed — the marker makes "already disclosed" a durable fact, not an inference). 13. **A build-tagged test-only limits injection seam (R2-F2.4).** *Rejected:* production-configurable limits (a silent operator foot-gun and a policy surface m-10 must not grow); violating-constants test builds (rejected at compilation by the assertion itself — unconstructible, the defect the reviewer named); skipping the runtime-branch fixture (the backstop invariant would ship untested). 14. **The workspace root rides `run start` + the manifest pair + the `assign` carrier with consumer recomputation (R2-F3).** *Rejected:* worker-side derivation from its spawn environment (no frozen contract pins the worker cwd to the workspace root — an unstated coupling); carrying only the id without the path (the consumer could verify nothing and E3 could not re-derive); a digest over filesystem identity (`st_dev`/`st_ino` — not stable across remounts/restarts, and equality must survive an app-main restart). 15. **`disclosed_by_turn_id` is a relation, not a boolean (R3-F1).** *Rejected:* the rev3 same-transaction boolean marker (erases its own carrier — the post-commit selector returns empty, breaking byte-identical replay and the durable-then-visible discipline); holding the selected list in memory for the first send (dies at exactly the crash-after-commit-before-send cut the rule exists for); snapshotting the list bytes onto the turn row (workable but redundant — the relation reconstructs the identical bytes from canonical rows under the deterministic ordering, without a second copy to drift). 16. *(SUPERSEDED by 19 — the rev4 equality pin repeated the defect at a lower target; kept as the decision chain's record.)* **The production boundary fixture pins `MAX_LEGAL_FRAME`, not `FRAME_MAX` (R3-F2).** *Rejected:* claiming production exact-fit at `FRAME_MAX` (arithmetically unreachable by legal closed shapes — an unexecutable fixture is a false floor); padding a frame to reach the limit (illegal bytes prove nothing about legal frames); shrinking `FRAME_MAX` to the reachable maximum (needless coupling — the slack is honest conservatism, now regression-pinned instead of hidden). 17. **`/` is refused, not special-cased (R3-F3).** *Rejected:* the `/`-stays-`/` carve-out (a whole-filesystem "workspace" is never the operator's intent under confusion-not-malice, and every downstream relative-path rule would need a root-flavored branch; one typed refusal is total and self-explaining). 18. **The disclosure snapshot is a separate immutable record; the live row keeps evolving (R4-F1).** *Rejected:* reconstructing frames from live canonical rows (mutable `state` breaks byte-identical replay — the r40 :72 terminal path is ordinary, not exceptional); freezing `attempt_open_ok` to the admission set (silently deletes the frozen Gate-2 added/changed detection m-9 r21 consumes — a consumer contract is never narrowed by a producer convenience); blocking parked-row transitions while disclosed (the disclosure is a record OF state, never a lock ON it). 19. **`FRAME_CONTENT_BOUND` is a bound, not a size (R4-F2).** *Rejected:* equality pins to any sum of `≤`-ceilings (rev3 at FRAME_MAX, rev4 at 3,764,736 — the same defect at different targets: no attaining witness exists and JCS has no padding); computing the true attained maximum from exact spellings + longest literals (achievable but brittle — every enum or name edit re-derives it, for no property the ≤-witness leg doesn't already give). 20. **Form-check before filesystem; grammar before length; OS length errors are `unresolvable` (R4-F3).** *Rejected:* realpath-first (an implementation would resolve a relative input against ambient cwd — the exact leak the ordered row (0) forecloses); unordered overlap tokens (two legal answers for one input is nondeterminism in a refusal surface); folding OS `ENAMETOOLONG` into `too_long` (conflates a resolution failure with a post-resolution output check — two different facts about two different strings). 10. **m-9 supplies the parsed-call operands; m-10 never interprets args (F5).** *Rejected:* m-10-side parsing of args to derive resource/cwd (re-owns argument interpretation the frozen contract deliberately keeps one-way, and duplicates m-9's executor derivation — two derivations of one fact is the F84 defect class); omitting the operands from the ticket (C's evidence half needs the declared identity frozen at mint for E3 to compare against). 11. **`rejected-stale`/`rejected-malformed` are no-retry invariant faults (F6).** *Rejected:* retry-with-backoff (a superseded controller or a broken frame constructor cannot converge by repetition; the control-generation machinery and a build fix are the respective arbiters — looping merely delays the loud surface).
