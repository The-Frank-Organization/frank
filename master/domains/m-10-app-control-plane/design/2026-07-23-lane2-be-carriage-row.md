# m-10 lane-2 B/E CARRIAGE ROW — the `provider_attempts` carriage of the three producer-first evidence digests (governed delta over r40 §B.1/§F)

**DESIGN_DOC_ID:** step3-relock-dag-m10-be-carriage · **rev3** — the property-named rebase trigger FIRED: m-9's next pair-approved revision is **r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`** (master `step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260724-150000`; R1 discharged, zero findings). This revision (1) **rebases the producer basis to m-9 r12** after a **substantive §6 re-review** (NOT hash-only — the carrier fold lives in §6); (2) records the carrier **now byte-bound CONFIRMED** at r12 §6 (the planner+implementer confirmation both landed — `attempt_open` gains `logical_surface_digest`, REQUIRED-never-absent, sent before my row commit, under the pinned five-member freeze invariant); (3) **folds R3 — the authoritative row-existence fact `m10_row_state ∈ {present, not_found}`, digest members conditional on `present`** (master `step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260723-155000`), so m-3's sink distinguishes the two epoch-inert-cut shapes (a stale-rejected `attempt_open` where no `provider_attempts` row was ever committed vs. a committed row parked by retirement) from a STATED fact rather than inferring it from column nullity. **The §5-E carrier ruling is consumed as settled: A3 (`compaction_template`) rides an existing `m8.llm_request.v1` `user_text{text}` behind the versioned sentinel `[[m9:compaction-template:v1]]`, B1 (`policy_messages`) is the declared constant `[]` — the VP ruled DELEGATED under F73, my §C.1 manifest offer STOOD DOWN, no m-10 schema change from that ruling.** **The assembly-time determination condition I flagged (`SITREP-planner-20260723-223000`) is SATISFIED at r12 §6:** the five members are FROZEN at first assembly and a Gate-2 reassembly places changes into assembled *content*, not into any of the five — so the digest I carry at `attempt_open` always describes the surface the request ships, even for A3 riding a request input-item. rev2 = `436016d8d58c9de32870cddc5fc972ab3057a6e9064bda858b3ea1fd2d832cc7` (superseded). *(Prior fold history, retained for lineage:)* rev2 folded the r1 pair review `step3-relock-dag-m10/DESIGN-REVIEW-implementer-20260723-120000.md` (M10-BE-R1-F1..F3, all verified): **F1** this artifact had no matching DESIGN lineage (the bundled request carried the singular `DESIGN_DOC_ID: step3-relock-dag-m10`, which parents the §D delta only) — a **separate DESIGN relay under `step3-relock-dag-m10-be-carriage`**, uniquely parented to master's carriage signal, carries this revision; the artifact keeps its own identity rather than being renamed into the §D document's; **F2** `logical_surface_digest` was NULLABLE, which m-9 r7 §6's lifecycle makes impossible — an assembly failure emits **no digest, no `attempt_open`, and no provider-attempt row**, so no valid row exists at which the digest is honestly absent ⇒ the member is now **REQUIRED and NON-NULL on every attempt row**, a missing/invalid member is malformed and **creates no row**, and the assembly-refusal case is expressed as **N/A — no attempt identity exists**, never as an `unknown` digest on a row; **F3** the `attempt_open` carrier was normatively extended while admittedly unconfirmed — the carrier, member spelling, presence rule, and send timing are now **explicitly PROPOSED and NON-FINAL pending byte-bound m-9 planner+implementer confirmation** (requested with this revision), and **no m-3 routing may occur before it lands**. **Producer basis:** m-9 **r7 `f191c69c…`** remains the bound basis because it is the pair-APPROVED one; their r8 `563398c0…` went **must-revise** and their **r9 `116eeffb…` is ALSO must-revise** (byte-bound verdict `step3-relock-dag-m9/DESIGN-REVIEW-implementer-20260723-160000` @ `3768ca1fd65205440a6a5638abe6b417068fadf85c98b14104b4fbe60a7bbb02`: three of the five frozen members — `instructions`, `compaction_template`, `policy_messages` — are not defined at the executable grain the two arrays are, so member byte-identity is unprovable and the carrier confirmation explicitly does NOT land) — and **r10 `4490ba75…` is must-revise likewise** (verdict `51375cfb…`, withholding the implementer carrier confirmation over the same three unresolved recipes; m-9 has routed the required §5-E ruling to master). **The rebase target is stated REVISION-AGNOSTICALLY and deliberately: the NEXT m-9 revision that is PAIR-APPROVED, whatever its number** — this trigger has already gone stale twice by being anchored to a specific revision (r9, then r10), and a target named by property cannot go stale by another round. Its §6 is **re-reviewed substantively**, never as a hash-only move. **The rebase is NOT hash-only (m-9's own correction to master's `…-103000` convenience, retracted by them rather than left for me to discover): r8's §6/§8 were byte-identical to r7, but r9's §6 is NOT — the carrier fold lands there; §8 still is.** So on the corrected successor's approval I **re-review §6** and rebase, rather than accepting a hash swap. rev1 = `67f947e42b85dc22167e0d47675cb7d6ba24d7aaecce910c9ff4e418c5e480d8` (superseded). (planner-authored; for m-10.implementer adversarial re-review)
**Basis:** the ratified `master/STEP-3-STAGE6-AMENDMENT.md` rev12 `1125b0a06697826751786bd82968354f0789f3bc98e6e9a5e8dbdb298f40e183` (§5-B, §5-E, §6-B/§6-E) · master's carriage signal `step3-relock-dag-m9/RECONCILE-orchestrator-planner-20260723-093000` · **the settled producer bytes, both recomputed on disk this session: m-8 r5 `c0b7b48846b05091af1c814481bc0e4449cf455f8c3e616ee5f9634b1e04be21` · m-9 r12 `044229654f256eb3afe102ca8f4254bde5c97aa4de4b104d4e2a2024df858c35`** (this doc binds ONLY the pair-approved m-9 r12; r5 through r11 VOID/superseded) · **the frozen bases: my contract r40 `d2ce98310afff4a8aeb998a0e530beedbde44cb723c6510ebe351841d70a9146` + stage-5 r10 `6fd1d655146d447194e5181bd81108936fa4560a4f8b4e9de843cf2daac11faf`, both UNMOVED** — this is a GOVERNED ADDITIVE delta under F73, never an in-place edit.
**Scope bound:** this doc carries the B-row and E-row ONLY. It is deliberately **separate from the §D-settlement rev7** (master's instruction) so m-3's binding is not coupled to the §D amendment's operator-ratification cycle. It authors no §D byte, no D-4 change, no manifest change.
**Bounds:** DESIGN-only. No DESIGN-lock, PLAN, T4/code token, credential, provider call, live E3, merge, deploy, or `frank/` action. F60/F67 unmoved (a digest is an identity, not secret bytes; the secret-holding set stays `{m-8, broker}`); the no-payload-column rule extended, not relaxed; H-12 hard-blocks external use.

---

## §1 — The three carried members (identity-exact, verbatim, never re-derived)

`provider_attempts` gains three columns, each **lowercase-hex SHA-256, exactly 64 hex bytes**, bound to the **exact attempt identity `{run_id, turn_id, attempt_id}`** that already keys the row:

| member | item | producer (owner-real) | arrives on |
|---|---|---|---|
| `frozen_core_digest` | **B** | **m-8**, computed at freeze (r5 §2.1 — an existing r12 internal value, re-emitted, not newly computed) | `m8.attempt_result.v2` on CTRL-C |
| `provider_lowered_tools_digest` | **E** | **m-8**, the JCS-canonical lowered `tools` value (r5 §3) | the same §1 carriers as B, same presence class |
| `logical_surface_digest` | **E** | **m-9**, the pre-lowering surface they assemble (r12 §6) | `attempt_open` on CTRL-W (§5, confirmed) |

**The one rule that governs all three: m-10 stores what the producer emitted, byte-verbatim, and derives nothing.** I hold neither m-8's frozen request bytes nor m-9's assembled surface, so I *cannot* recompute any of these and do not claim to. No aggregator hashes foreign bytes (§6-E). **m-10 performs no join either** — `model_surface_digest` is m-3's join of the two E component digests at the E3 binding; my row carries components, never a composite.

## §2 — Presence: the honest absent case (m-8's P1 matrix consumed, not re-derived)

The two m-8 members are **present IFF m-8's freeze completed (P1)** — their r5 matrix makes several outcomes carry no digest at all (pre-freeze rejects, the interrupted-freeze cut). My consumption:
- **Present** ⇒ store verbatim.
- **Absent** ⇒ the column is **NULL**. Never an empty string, never a zero/placeholder digest, never a value carried over from another attempt. A fabricated digest would be indistinguishable from a real one at E3 and is exactly the confusion this architecture exists to prevent.
- **Absence is not a failure**: no supervision effect, no attempt-state change, no retry. m-3's evaluator reads an absent carried digest as **`unknown`** (their P2b decidability), never as a mismatch — my row's job is to make the absence *visible and honest*, not to repair it.
- **The m-9 member is DIFFERENT and is REQUIRED, NON-NULL (R1-F2):** `logical_surface_digest` is present on **every** provider-attempt row, because m-9 r7 §6's lifecycle admits no row without it — an assembly failure emits no digest, **no `attempt_open`, and therefore no attempt row at all**. A carrier frame missing or carrying an invalid member is **malformed: the frame is rejected and NO row is created** (there is no "row with an absent surface digest" state to reach). The assembly-refusal case is therefore **N/A — no attempt identity exists**, so there is nothing for E3 to resolve and no `unknown` is produced *on a row*; the honest record of that case lives in m-9's own refusal path, not as a null column of mine. **Only the two m-8 columns are nullable**, per their P1 matrix above.
**m-10 owns none of P1/P2a/P2b** — P1 and P2a are m-8's facts, P2b is m-3's; I carry the record and its absence.

## §3 — Write discipline: once, immutable, conflict fails closed

Each member is written **inside the ordinary applier transaction that records its carrier's effect** (the `attempt_result` terminal commit for the two m-8 members; the attempt-row commit for the m-9 member) — the §F single-transition-chokepoint rule, no ad-hoc write site.
- **Written once, immutable thereafter.** No UPDATE path exists at the applier.
- **A second, DIFFERING value at the same attempt identity is a typed conflict — fail-closed, recorded, and the FIRST committed value STANDS** (the same fail-closed-detector shape settled for `receipt_conflict`; and like it, not producible by a correct producer — m-8 emits one value per attempt at freeze, m-9 one per attempt at assembly). An identical repeat is idempotent.
- **Validation is structural ONLY**: 64 lowercase hex, matching the §4 grammar. m-10 does not compare a carried digest against anything — not against an observer value (that is E3's positive leg), not against another attempt's, not against a recomputation it cannot perform.
- **These are EVIDENCE, never authority**: no member gates admission, issue, consume, send, or any transition. They fence nothing and are fenced by nothing; the frames carrying them are fenced by their own existing operands.

## §4 — Bounds + boundary properties

Three members × 64 hex + names/punctuation ≈ **≤ 320 B** added to the attempt-carrier frames, all within the escape-free grammar `[A-Za-z0-9._:/-]` (so raw = encoded, per the §D-lane encoding rule). **No `turn_open` member is added and no turn-frame bound moves** — this delta is invisible to the frame-totality gate and to the parked-disclosure arithmetic. Storage: three fixed-width columns per attempt row.
**Boundary assertions (build-testable):** no payload byte, no argument byte, no header value, and no prompt/response byte enters my store or any frame by this delta — digests only (m-1's no-payload-column rule, extended). No credential material (F60), no change of secret placement (F67, the set stays `{m-8, broker}`). No conductor verb, no seat capability, no policy.

## §5 — The carrier — CONFIRMED byte-bound at m-9 r12 §6 (planner+implementer both landed)

m-8's carrier is explicit in their approved bytes (`m8.attempt_result.v2` on CTRL-C) and needs nothing from me. **The m-9 carrier is now SETTLED, byte-bound at pair-approved r12 §6** — the carrier, member spelling, presence rule, and send timing were the cross-owner contract m-9 owned to state, and at r12 both pair halves have confirmed it exactly as m-10 proposed:
- **Carrier:** the frozen §B.1 **`attempt_open{attempt_id, turn_id, turn_epoch, provider_lane_id}`** gains `logical_surface_digest` — the only m-9→m-10 frame at attempt identity before the provider request (a named r40 §B.1 frame supersession).
- **Presence:** **REQUIRED-never-absent, non-null.** A missing or invalid member is **malformed ⇒ no `provider_attempts` row is created**; the assembly-refusal case is **N/A — no attempt identity exists** (m-9 r12 §6: refusal emits no digest, no `attempt_open`, no row), never a NULL column.
- **Timing:** sent on CTRL-W **at `attempt_open`**, before I commit the `provider_attempts` row and therefore before `attempt_open_ok`.

**The soundness invariant, now normative on m-9's approved bytes (r12 §6) — and the assembly-time condition I flagged at `SITREP-planner-20260723-223000` is SATISFIED by it:** the frozen order is `attempt_open` → my row commit → `attempt_open_ok` → Gate 2 → *possible* BLOCK + REASSEMBLE → DATA-P, so a worker can re-enter ASSEMBLING after the digest was sent. r12 pins that **the five members `{instructions, logical_tool_schemas[], tool_descriptions[], compaction_template, policy_messages}` are FROZEN for an attempt at its first assembly**; a Gate-2 reassembly places the added/changed disclosure into **assembled input content**, which is **not a member of the five**, and any change that would move one of the five is **a new attempt** with its own `attempt_id`/`attempt_open`/digest. **Consequence for my carriage: the digest I commit at `attempt_open` always describes the surface the request actually ships** — including `compaction_template` (A3), which the §5-E ruling routes onto an existing `m8.llm_request.v1` `user_text{text}` behind the versioned sentinel `[[m9:compaction-template:v1]]`; its presented value is fixed at first assembly like the other four, so the request-input-item carrier does not break the timing.
**My side of the invariant, recorded with its exact scope:** my confirmation covers the TWO array members (`logical_tool_schemas[]`, `tool_descriptions[]`) that reach m-9 through my run manifest — nothing m-10 owns can move them mid-attempt or even mid-run, since the manifest is immutable per `run_id` and frozen at admission under the operator-ratified G-3 decision (dynamic mid-run manifest mutation rejected outright). The other three members are m-9's own, authored to executable grain and pair-approved at r12; I make no claim about them beyond consuming their frozen values. I raise no finding against the invariant.

## §5a — R3: authoritative ROW EXISTENCE (`m10_row_state`), digest members conditional on it

**Why m-3 needs it (master `step3-relock-dag-m3/RECONCILE-orchestrator-planner-20260723-155000` R3):** m-8's epoch backstop admits two shapes for the epoch-inert cut that m-3's sink must distinguish, and **column nullity alone cannot** — a NULL digest column is ambiguous between "a row exists but its P1-absent digest is NULL" and "no row was ever committed". The two shapes are (i) a **stale-rejected `attempt_open`** where **no `provider_attempts` row was ever committed**, and (ii) a **committed row parked by retirement**. Whether my carriage exposes authoritative row-existence is mine to state, not m-3's to infer.

**The fact I state:** the B/E carriage exposes a closed **`m10_row_state ∈ {present, not_found}`** at each queried attempt identity `{run_id, turn_id, attempt_id}`:
- **`present`** — a `provider_attempts` row is durably committed at that identity. The three digest members are **conditional on `present`**: each is either a verbatim value or NULL per its producer's presence class (§1/§2). Digest members are only meaningful, and only queryable, on a `present` row.
- **`not_found`** — no `provider_attempts` row is durably committed at that identity (the stale-`attempt_open`-rejected shape, or any identity that never reached a row commit). **On `not_found`, the digest members do not exist** — not NULL, *absent*: there is no row to carry a column. `not_found` is the authoritative negative, sourced from my durable store's single-writer state, never from a timeout or an inference.
- **Authority + timing:** `m10_row_state` is decided from my committed store at query time; it is monotone in the only direction that matters — a `not_found` identity can later become `present` if its row commits, but a `present` row is never un-committed (retirement parks a row, it does not delete it), so the fact never regresses `present → not_found`. This is the same single-writer durable-state discipline as every other m-10 fact m-3 consumes.

This resolves R3 from a **stated** fact: m-3's `m10_row_state: present|not_found` consistency rule binds my authoritative row-existence, and the digest-conditional-on-present rule matches my §1/§2 presence classes exactly (a `present` row may still carry a NULL m-8 digest per P1; `logical_surface_digest` is required-non-null on every `present` row per §5, so it is never NULL-and-present).

## §6 — What m-3 will receive (the handoff this doc UNBLOCKS — the §5 carrier is now CONFIRMED at m-9 r12; on this row's pair-approval + m-3's reproduction, the binding proceeds)

At each attempt identity: **B** (`frozen_core_digest`) for their §B sink record and the composite exit proof; **both E components** (`logical_surface_digest`, `provider_lowered_tools_digest`) carried verbatim and independently, for their `model_surface_digest` join at the E3 binding — each producer's value reaching them **untouched by me** — plus the **§5a `m10_row_state`** so nullity is never ambiguous with non-existence.
**Absence, scoped exactly (R2-F2 — the rev2 wording generalized this across both E components and let a reader reconstruct the forbidden state):** a NULL carried member, and therefore an E3 `unknown` from absence, arises **ONLY for m-8's two nullable P1-derived digests** (`frozen_core_digest`, `provider_lowered_tools_digest`). **`logical_surface_digest` is never NULL on any row** — m-9 assembly refusal is **N/A: no attempt identity exists**, so there is nothing for m-3's decidability gate to resolve and no `unknown` is produced on a row. There is no "attempt row with an absent logical digest" state anywhere in this design. The join recipe, the E3 predicates, and the evaluator are m-3's; nothing here authors them.

## §7 — Fixture obligations

FX-BE-1 verbatim carriage: a known digest emitted by each producer is stored byte-identical and read back byte-identical at the exact attempt identity; no recomputation occurs on any path (asserted by construction — no hashing call exists on the carriage path). · FX-BE-2 the absent legs, CORRECTED (R1-F2): for each m-8 P1-absent outcome class, the column is NULL, the attempt state is unaffected, no retry/supervision fires, and E3 reads `unknown`. **For the m-9 member the legs are the opposite shape:** a carrier frame with the member **missing or invalid ⇒ malformed, rejected, NO attempt row created** (asserted: zero rows, zero state change); and the **m-9 assembly-refusal path asserts NO attempt identity exists at all** — no row, no NULL column, nothing for E3 to resolve. There is no fixture for "a row whose `logical_surface_digest` is absent" because no such state is reachable. · FX-BE-3 immutability + conflict: an identical repeat is idempotent; a differing second value at the same identity ⇒ typed conflict, recorded, first value stands, row otherwise unchanged. · FX-BE-4 structural validation: a non-hex, wrong-length, or uppercase value is rejected as malformed at receipt (no row mutation). · FX-BE-5 identity-exactness: two attempts in one turn, and the same `attempt_id` across a continuation chain, each carry their own digests with no cross-contamination. · FX-BE-6 boundary: the store contains no payload/argument/header/prompt bytes from this delta (schema assertion) and no frame bound moved. · **FX-BE-7 R3 row-existence (rev3):** a queried identity with a durably committed row ⇒ `m10_row_state = present` and its digest members are queryable (verbatim or NULL per P1); a **stale-`attempt_open`-rejected identity with no committed row** ⇒ `m10_row_state = not_found` and the digest members are **absent, not NULL** (asserted: the two shapes are distinguishable, and column nullity alone never has to carry the distinction); a `present` row that retirement later parks stays `present` (no `present → not_found` regression); a `present` row always carries a non-null `logical_surface_digest` (never NULL-and-present, §5). · **FX-BE-8 assembly-time determination (rev3, the condition from `SITREP-planner-20260723-223000`):** the `logical_surface_digest` committed at `attempt_open` is byte-identical to the digest of the surface the request ships, across a Gate-2 BLOCK+REASSEMBLE (the reassembly changes assembled content, not any of the five members); and `compaction_template` riding the `user_text{text}` sentinel item carries the same frozen-at-first-assembly value into the shipped request.

## §8 — Decision record

1. **Verbatim carriage with no re-derivation.** *Rejected:* recomputing any digest at m-10 (I hold neither producer's bytes; a second derivation of one fact is the F84 defect class, and §6-E forbids an aggregator hashing foreign bytes). 2. **NULL for absence — for m-8's TWO nullable P1-derived columns ONLY (scoped per R2-F2; `logical_surface_digest` is required/non-null and has no absent case on a row)** — and never a placeholder. *Rejected:* an empty-string or zero-digest sentinel (indistinguishable from a real value at the evaluator — a fabricated evidence artifact is worse than an honest gap); treating absence as a fault (m-8's matrix makes absence a legitimate, expected outcome class). 3. **First-committed-wins on conflict.** *Rejected:* last-wins (evidence replacement); reject-both (destroys committed evidence retroactively) — the same reasoning settled for `receipt_conflict`. 4. **Evidence, never authority.** *Rejected:* using a carried digest to gate any transition (it would make an evidence artifact load-bearing for a fence it was never designed to secure, and would give a producer a lever on my lifecycle). 5. **A separate artifact from the §D rev7.** *Rejected:* folding B/E into rev7 (it would couple m-3's binding to the §D amendment's operator-ratification cycle and block their lane close for no reason — master's instruction, and correct). 6. **`m10_row_state` is a stated authoritative fact, not an inferred one (rev3, R3).** *Rejected:* leaving m-3 to infer non-existence from a NULL digest column (nullity is ambiguous between P1-absent-on-a-row and no-row — the exact confusion this fact exists to remove); a timeout/liveness-based "not seen" (a `not_found` sourced from anything but my committed single-writer store could regress or race). 7. **The §5-E carrier ruling consumed as settled, no m-10 schema change (rev3).** *Rejected:* reviving the §C.1 manifest carrier offer (the VP ruled it unnecessary — A3 rides an existing m-8 input-item, B1 is a local constant; my offer was correctly priced and stood down, and reviving it would author a schema delta the ruling declined).
