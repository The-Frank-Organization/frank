# m-1 — Trust & Identity (the TCB)

**Pair:** design-lead `m-1.planner` + adversarial design-reviewer `m-1.implementer`.
**Cycle c1 scope:** AUDIT + DESIGN of the v3 Step-1 trust/identity foundation. Terminal = design-lock. No impl.

## Owns (the trusted core / TCB)
The conductor's trusted core: the **sole *governed* writer** of the append-only relay store (through the
governed `submit()` path; confusion-resistant; D5 accepted), the **seat-stamper** (channel-stamped `FROM` =
conductor-stamped identity — confusion-resistant, GRILL-LOCK D4; forgery-robust-by-construction = the
D3-shelved wrap milestone, not the Step-1 claim), **store isolation**, and the placement of the inline
lint/form gate pre-delivery. The v3 differentiator neither prior-art harness makes: a stamping courier that
is the sole *governed* writer of the relay store (through the governed submit path; D5 direct-store residual
accepted) and stamps `FROM` from the connection a relay arrived on, never a lane-supplied field. See the
design doc §0.c D3/D4/D5 ladder + GRILL-LOCK D4-D5 for the full scoping.

## Standing rails
- **THE IDENTITY-SEAM RAIL** (operator-ratified 2026-07-11; text of record `ARCHITECTURE.md` § CROSS-DOMAIN SCOPE AMENDMENT; routed `frank-threat-model-scope/SITREP-orchestrator-planner-20260711-163145`): the identity stamp is one of the three named SEAMS — filled with the honest minimal mechanism (channel-stamped FROM, seat-trust via connection provenance), kept **STRUCTURED so it COULD later carry a signature/attestation** (never a bare string that assumes it never will), labeled **confusion-resistant, never tamper-proof**, and **never filled with a home-grown security primitive** (forgery-robust wrap stays D3-SHELVED; crypto-identity, if ever elected, arrives as vetted machinery through its own grill). The locked design already satisfies this (verified at adoption, `…-162826`). **Binding on future touches:** any relay/design touching the identity stamp's SHAPE cites the rail; any identity-strengthening proposal routes through the amendment path with the no-home-grown-primitives bar explicit.

## Boundaries
- **Co-foundational with m-2** (Forms & Determinism): m-2 *declares* the system-owned envelope fields; m-1's
  courier *fills + certifies* them. Neither locks in isolation — the orchestrator runs the joint lock after
  m-3/m-4/m-6 consumer review.
- **Consumes:** nothing (foundational). **Consumed by:** m-2 (store + system-field fill), m-3 (store-isolation
  boundary + submit pre-send hook), m-4 (stamped store as routing-record target), m-6 (addressing graph / inbox
  projection).
- **identity ≠ authority:** m-1 owns *who* (identity + store); m-4/m-5 own *what a stamped seat may do*.

## Status
- AUDIT: complete — reconciled `c1-audit-m-1`, VP-approved (RECONCILE.md).
- DESIGN: rev1 pair-approved (`c1-design-m-1-review`, 184021). **rev2 in progress** (`c1-refine-m-1`): folded the
  VP-approved c1 consumer review (m-3 DI-5 observe-integrity; m-6 G3 operator address-space; m-4 Sharpening-D
  routing-relay parent candidate; + 2 non-blockers) into the design doc (same DESIGN_DOC_ID; fold-log §0).
  m-1↔m-2 contract **mutually re-affirmed for rev2**. m-1.implementer rev2 DESIGN-REVIEW = **approve**
  (`c1-design-m-1-review-r2`, 170825; no rev1 regression, no DESIGN reopen; 4 PLAN-only carry-forwards → §13.rev2).
  **design-complete-rev2 reported** to orchestrator (`c1-refine-m-1-complete`, 173400). **Awaiting the joint
  m-1↔m-2 co-foundational lock** (orchestrator integration + VP full review). Holding — no self-advance to PLAN.
- c3 Seam-C bounded upstream-contract check (`c3-design-m5-m6-coord`, 182218): the away-mode inbound
  verdict-token mint/verify surface → **answered A** (`COORD-planner-20260630-182600`): mint/verify is m-1 TCB
  work (forced by DI-1 nonce-burn + DI-2 key-custody), the **first activation of the reserved `certification`/
  DKIM seam (§8)**, scoped inbound-verdict-only; m-6 owns the bridge over it. **c1 contract unchanged / not
  reopened** — additive later-step build carry. Holding for m-6's cell fold + the orchestrator's Seam-C fold at the c3 lock.
- build-readiness Cluster 1 (`readiness-fix-c1`, CTO-arbitrated · VP co-signed 20260630-223751): the m-1/m-2
  `submit()`/`send()` write-path collision → **m-1 side folded** (`SITREP-planner-20260630-224500`): §5 `submit()`
  rewritten to one-atomic-accept + **pre-append validation** [form → **m-2 lineage gate** → reserved Step-2 m-3
  observe], **no persisted `submitted` limbo**, terminal `rejected` on fail; **Step-1 = store + form + lineage**
  (m-3 observe reserved to Step-2). Byte-consistent with `ARCHITECTURE.md:58-65`; all I1/TOCTOU/authority-blocks
  invariants preserved; **no breaking domain constraint**. Cluster 1 closes only on m-2's `send()` fold + the CTO
  re-verify — holding.
- c4 CQ store-provenance cluster (`c4-cq-m1`, m-7 design-lock CQs on the m-1 surface; full-pair, VP-reviewed
  decomposition): **m-1.planner confirm-or-correct filed** (`DESIGN-planner-20260702-013500`, → m-1.implementer for
  independent review). **CQ-1 → closed** (resolution **(a) step-gate**; (b) conductor-filler rejected — would breach
  the Step-1/observe boundary or fabricate evidence). **CQ-6 → closed** (persisted binding table = conductor-owned
  table made durable; **binding clarification**: re-attach re-verifies credential possession per DI-2/DI-3;
  decision-scoped sibling-burn = atomic conductor-only `(decision_id, seat)`). **CQ-8 → closed** (INDEX-derived-authority
  consistent with locked §6; on-disk layout UNCHANGED). No c1 reopen; no m-7 LOCK by implication.
  **m-1.implementer approved** the three answers (`DESIGN-REVIEW-implementer-20260702-020418`, no must-revise).
  m-6 co-signed CQ-6 (`020100`) with a +1 **re-mint-supersedes-on-resummon** rule → **m-1 confirmed** it fits the burn
  model (`DESIGN-planner-20260702-021500`): keep `decision_id` stable, atomic **burn-first** required (crash ⇒ zero live ⇒
  re-summon, never auto-approve), signed **cycle-epoch** recommended as m-1-internal defense-in-depth. Global CQ-6 close
  pends m-6.implementer review of `020100` + a light m-1.implementer in-contract nod. **CQ-1: m-2 co-signed (a)**
  (`024704`, the `required_when` schema half — observe-owned set + `layer_present:observe` step-gate atom); **m-1
  concurred** (`DESIGN-planner-20260702-025200`) — faithful to (a), no m-1 system-field drift (`EVIDENCE_TARGET` stays
  Step-1 intent, only observed achievement step-gated); CQ-1 triad pends m-2.implementer review. CQ-8 closed.
  Then I report the co-signed CQ-1/6/8 closure to the orchestrator for the m-7 design-lock fold. Holding.
- **c5 (build-readiness NO-GO re-baseline, WRAP→ATTACH):**
  - **Claim-sweep** (`c5-claim-sweep-m-1`, VP-ratified §2B): **folded** (`DESIGN-planner-20260702-134500`). Added scoping
    note §0.c (D3/D4/D5 ladder) + ~28 in-place relabels — every "sole-writer/forgery-robust/by-construction/
    unbypassable" malicious-stop claim → **confusion-resistant (D4) + stated D5 attach residual**; the §4 QED epicenter
    and the "sole residual = misconfig" line corrected; observer-selected (DI-5, parent_picker) + prior-art (:47) +
    honest-fallback KEPT with reason. Mechanisms/verbs/invariants unchanged; no c1 reopen. Classified survivor list filed;
    pends m-1.implementer semantic review-only approve.
  - **Decision ④ record** (`c5-fold-decision-4`, RECORD-NOW / mechanism-at-(d)): recorded away-token **ROTATE + RE-OBSERVE**
    as non-locking §13.c5 build-carry (`DESIGN-planner-20260702-135800`); honestly noted it **supersedes** my 021500
    "keep `decision_id` stable" stance (operator chose rotate; lineage-continuity concern re-filed as a (d) obligation).
    **m-1.implementer approved** the scoped-record (`140100`, no must-revise).
  - **Both c5 items pair-approved + closed on the m-1 side** (claim-sweep `140000` + decision-④ `140100`; the sweep
    reviewer ran a wider survivor scan than my census → no unclassified blocker). Consolidated completion reported
    (`SITREP-planner-20260702-141500`). Remaining is the CTO's ledger folds (c5 status ledger + integrated build-carry
    ledger). Holding — no self-advance to PLAN.
  - **c6-fix (re-review cleanup, CONDITIONAL-GO; last gate before Step-1 PLAN):** applied the 5 doc-only findings
    (`DESIGN-planner-20260702-210000`, fold-log §0.d) — **F3** DI-1 dispositioned (D3-shelved wrap; Step-1 I1 =
    m-7-guardrail-borne; AC#1 = D4 fixture + D3 spike); **F12** third outcome `held` (m-7 C4.1/NF-S6; enum byte-exact
    `{accepted, rejected, held}`); **F4** DI-2 tests re-cut to D4 tool-surface grain; **F5** transport A≻B rebuilt
    (non-re-cut foundation); **F6** stale `m-3 observe` struck → `[Step-2+ hook 2c]`. Verified CTO-applied F7/F9/F10/F1-2.
    No mechanism/lock change; four sanctioned by-construction claims + D5 vocab preserved. m-1.implementer r1 returned
    **must-revise** (2 catches: §4 I1 "Mechanism:" line still wrap-grade; §10 Option-B stale by-construction contrast) —
    both **folded rev1** (`DESIGN-planner-20260702-211200`; §4:95 Mechanism → guardrail-borne + D3-shelved clause;
    §10:186 Option-B → not-upgrade-bearing). **m-1.implementer re-review = approve** (`211300`, no must-revise
    remaining). **c6-fix-m-1 COMPLETE + pair-approved**; completion reported (`SITREP-planner-20260702-211800`).
    Remaining is the orchestrator's c6 verify sweep + close (VP co-sign) across the 7 domain returns → then
    (e) Step-1 PLAN opens. Holding — no self-advance.
- **Step-1/2 BUILD — the F3 fidelity edge (m-1 as required reviewer of the s1/s2 consuming surface):**
  - **s1-fidelity-m1** (`frank/.relays/s1/s1-fidelity-m1/`): m-1.implementer verdict = **fidelity-finding-blocks** (F-M1-1: seat-credential lifecycle carry `:228-229` named in design but not landed in the s1 plan). Three other questions clean (I1 guardrail-borne, submit pipeline, operator-address). Fix is **s1-core.planner** → m-1.implementer re-review. m-1.planner (CC) held; planner design-intent read filed *in-reply*: **option (b)** (idempotent `Mint(existing)` + E2 no-stale-survivor) is the S1-faithful minimum; full rotation/revocation is the deferred carry, not S1.
  - **s2-fidelity-m1** (`frank/.relays/s2/s2-fidelity-m1/SITREP-implementer-20260704-034158.md`): m-1.implementer verdict = **must-revise** (F-M1-1 field homes / F-M1-2 two typed read-corruption states / F-M1-3 store-root config). Reviewed against locked §5/§6 (`:122-145`): **faithful, no contract contradiction, no reopen**; byte-exact `{accepted, rejected, held}` preserved (no fourth outcome). s2 fix is **s2-core.planner** → m-1.implementer re-review (not mine). **→ CLOSED: m-1.implementer S2 narrow re-review = `FIDELITY_VERDICT: approve`** (`frank/.relays/s2/s2-fidelity-m1/SITREP-implementer-20260704-043529.md`) — F-M1-1/2/3 folded (design r4 @845a7d1, plan r5 @ca23a44), homes table implemented; the m-1 fidelity gate for the s2 consuming surface is **satisfied**. Remaining s2 sequencing (scope-diff → impl dispatch parented to the r6 plan-review approve) is s2's, not m-1's.
  - **s3-fidelity-m1** (`frank/.relays/s3/s3-fidelity-m1/SITREP-orchestrator-planner-20260704-183833.md`): S3 form-system design §4 — seven store/lineage proposals at frank main@291ab08 (D-7 engine tables over the locked verbs; **D-6 `parent_picker` realization — the canonical VP lineage-movement watchpoint**; D-6 lineage-field homes per the S2 homes precedent; D-9 migrator read-facade wrap; D-2 JSON-in-string headers; no new `record_kind` tokens; fresh-Init registry replacement). TO: **m-1.implementer** (verdict = theirs; s3 `DISPATCH IMPL` blocked until on record). **→ verdict rendered: `approve-conditional` for PLAN** (`SITREP-implementer-20260704-184437`) — four PLAN conditions (F-S3-M1-1 tables = rebuildable store-derived read model, no API/authority change; **F-S3-M1-2 parent_picker candidate set = active-lineage scoped**, S3's broad "delivered/accepted horizon" rejected; F-S3-M1-3 homes split — system fields stay envelope, `DESIGN_*` = m-2 header slots; F-S3-M1-4 migrator = facade above raw verbs) + items 5/6/7 approved. **m-1.planner design-lead check: faithful application of locked §6 `:139`/`:140` + `:131` — precision within the lock, NOT new contract surface; no amendment needed** (contrast s2, where the contract was silent on non-`submit` authorship). No narrow re-review owed if the PLAN carries the conditions verbatim; deviation routes back to m-1. Holding.
  - **s4-fidelity-m1** (`frank/.relays/s4/s4-fidelity-m1/SITREP-orchestrator-planner-20260705-041556.md`): s4 design's five m-1-touching surfaces at frank main@2ef9437 — ① `config_change` record_kind, **operator-channel provenance** (genesis stays the sole system-stamped exception — consistent with the §0.e boundary: machinery-authored vs operator *act*); ② **credential-hash active-channel index** — locked m-1 text SILENT on concurrent connects ([VP-W1] pre-constrains) → **flagged as a potential s4-amend-m-1 candidate post-verdict** (the s2 system/system shape: convention on contract-silent m-1 surface); ③ shim custody (D4/D5 wording, §13.3 no-in-band-rotation carry stated honestly); ④ per-recipient wake (identity consumed, not extended); ⑤ `config/` demoted to derived projection (evolves the implementer's own S2 F-M1-3). TO: **m-1.implementer** (verdict = theirs; s4 store/identity dispatch blocked until on record). **→ verdict rendered: `approve-conditional` for PLAN** (`SITREP-implementer-20260705-042308`) — six conditions (F-S4-M1-1 operator-channel provenance, `system` never a config-mutation shortcut — the §0.e boundary applied; **F-S4-M1-2 the sharp edge**: full-body `config_change` requires a redacted non-operator read view, anchored m-7 I-PH, facade-above-raw-`store.Read` grain; F-S4-M1-3 active index = in-memory admission cache, NOT a second identity authority, no persistence/heartbeat/takeover/rotation; F-S4-M1-4 custody honest per §13.3; F-S4-M1-5 wake = committed-record recipients only; F-S4-M1-6 config demotion via canonical pivot, `store.Init` untouched). No narrow re-review owed on verbatim carry; binding route-back triggers named. **m-1.planner design-lead check: faithful; §0.e clause load-bearing at item ①.** Amendment proposed to operator (pre-positioned pre-verdict): fold the durable "**at most one live channel per credential**" admission rule (contract-silent DI-3/DI-4 surface, source of record currently = [VP-W1] + this verdict) into the design-of-record as a short realization clause — s2-amend pattern, pending operator authorization. Holding.
  - **s5-escalations § m-1 ask** (`RECONCILE-orchestrator-planner-20260706-052214` — TO m-1.planner, first s5 engagement; rides s5-a's registry pass, non-blocking): **ANSWERED** (`DESIGN-planner-20260706-052940`, lint OK, INDEX-appended). **Q1 owed authoring = CONFIRM operator-only, on the two-axis reading:** S2 settled the *provenance* axis (principal-authored via `submit`, never machinery — shape (ii) guide-rejected); the newly-expressible *scope* axis is ruled here — `owed_disposition` is authority-bearing in effect (discharges an exit-gating obligation) → pessimistic-floor operator-only for both tokens, fill-time enforcement; widening later = registry amendment + m-1 route-back, not a contract break. **Q2 `genesis` = CONFIRM NO seat's scope, operator included** (remove from `*` AND `operator`): §0.e/§6 — genesis is `FROM=system`, never submit-path-producible; a form-offered `genesis` = a seat-stamped record wearing a machinery token (provenance lie); `incident`/`gc_marker` already absent from the enum — genesis was the anomaly. Adjacent flag to m-2's pass: `gate_resolution` in `*` vs (j)'s operator-scope; `disposition` class-check. m-1.implementer CC'd for a light nod if master wants pair rigor. Holding.
- **s6 — THE TRANSPORT FIX (design-amendment phase; operator-ruled in-Step-1 2026-07-06):** dispatch `s6-design/DESIGN-orchestrator-planner-20260706-180315` (VP-confirmed, [VP-W1..W3]) assigns m-1: **THE FORK** (parenting/anchor model {F11 livelock, F4} — decision packet → operator GRILL, [VP-W3] non-collapsible) + F10 (anchor/projection hygiene) + F17 (waiver scoping/retraction) + the F14 store-lock INVARIANT (semantics mine; runtime enforcement m-7's, [VP-W1/W2]). **Amendment doc authored:** `design/2026-07-06-s6-transport-amendments.md` — §A fork packet (rec: **Option A conductor-computed PARENT + validated hint, never-bounce anchoring** — completes §6's own stamp-what-the-engine-knows logic; observer-selected→observer-computed; honesty note: my S3 narrowing is part of why F11 binds; costs named incl. §6/S3 supersession + Sharpening-D re-home); §B F10 split (anchor half = code-vs-contract in-slice fix — `WokenOn` drawn from reject-bearing projection violates locked §5(2b); `project()` default-scope = the real amendment); §C F17 (scoped waivers + `waiver_retraction` id_ref class, commit-order gate lookup, operator-only floor, no retroactive invalidation); §D F14 **I1-P** (one live conductor per store root; claim precedes phase-0; takeover only on proof-of-death; D4/D5-honest). **→ m-1.implementer GRILL requested** (`s6-design/DESIGN-planner-20260706-183326`): §A packet-quality → then to master for the operator grill (no m-1 fork lock before it); §B/§C/§D lock on approve. **→ GRILL RETURNED approve** (`DESIGN-REVIEW-implementer-20260706-184120`): §A approve-to-forward (forward conditions incl. "grill decides hint **fallback vs reject vs HOLD** first"; lane-selected-parent branches must prove no-livelock on archived F11 traffic); §B/§C/§D **approve-locks** with carry-forwards (polluted-archived-mailbox fixture; `WokenOn` never raw mailbox line; **waiver validity deterministic/store-derived, never wall-clock TTL**; store root = canonical identity, aliasing closed; loser-refuses-reads-included). **Carry-forwards folded as doc §E (part of the locks); §B–§D reported PAIR-LOCKED to master + §A packet formally forwarded for the operator grill** (`DESIGN-planner-20260706-184701`, lint OK, INDEX-appended). Awaiting: master integration (§B–§D + [VP-W2] joint F14 citation with m-7) + the §A operator grill → GRILL_LOCK → then m-1 folds the winning branch (rewriting `parent_picker` if A). **→ GRILLED + LOCKED: branch A won** (`GRILL-LOCK-parenting-fork-2026-07-06`; fallback-never-bounce settled by the empirical audit — zero author bad picks in 913 relays; precision accepted; Sharpening-D re-home conditioned on m-4's BLOCKING confirm). **→ §A FOLD EXECUTED in the c1 design-of-record** (`SITREP-planner-20260706-202317`): new §0.f + §6 parent bullet/§5/§10/§12 REWRITTEN to branch A (PARENT = system_only conductor-computed at commit; `parent_hint` fallback semantics; anchoring-bounce class deleted; observer-selected → **observer-computed**, stronger claim; S3 five-point superseded); **Sharpening-D sub-bullet HELD in place** (annotated pre-s6 record; folds only on m-4's confirm — one round, mine, when it lands). Post-fold greps clean (no parallel parent authority; enum 5×). **B-3 addendum (identity activation, operator-requested boot stage `200259`):** authored as amendment-doc **§F** — edges minted/bound/active with may-never-assert lines; activation DERIVED (first-accept per mint-generation; no new record class/field/on-disk); reconnect = re-`bound` never re-boot, one activation per generation, re-mint resets; composable with either B-1 branch (constraint: no bounce-retry channel on the boot path); D4/D5-honest. **→ m-1.implementer grill requested** (`DESIGN-planner-20260706-202318`). **→ §F APPROVED** (`DESIGN-REVIEW-implementer-20260706-202929`) with carry-forwards folded as **§F.1** (generation keyed to the COMMITTED `seat_mint` pivot, never raw credential bytes/mutable table row; `active` = first accepted `FROM=<seat>` submit only — never seat_mint/addressed-traffic/read/project/rejected/held; **`bound` starts empty after restart**; authority ceiling = ordering + roster only; no persisted marker — routes back; boot form unbounceable under a B-1 refusal branch). **B-3 pair-approved reported to master** (`SITREP-planner-20260706-203318`). All four m-1 s6 legs dispositioned pair-side (§A folded · §B–§D locked · §F approved · dispositions+fixtures consolidated). Remaining m-1: the **Sharpening-D fold on m-4's blocking confirm** (one round when it lands); VP co-sign items if raised. Package hold (not mine): m-7 B-1 classifier must-revise (`202815`). **→ m-4 confirm LANDED** (`201500`, one binding condition) → **Sharpening-D FOLDED** (`SITREP-planner-20260706-210259`): §6 sub-bullet re-homed to hint-class (citation on the `parent_hint` rails; unprovable citation persists **verbatim + `routing_ref_honored: no`**, audit-only; Q-C byte-unchanged, R2 strengthened); surface mapping confirmed = m-4 §5:216 `lineage_role: routing_ref`; no m-4.implementer dissent (INDEX-checked); §0.f closure appended; zero HELD markers survive. **THE m-1 s6 DESIGN PHASE IS COMPLETE** (§A folded whole · §B–§D locked · §F approved · all findings dispositioned). Awaiting: master's integrated-set assembly → VP co-sign → the s6 build-slice dispatch (next m-1 touch = the build slice's fidelity gate, standing pattern). Holding.
  - **s6 build-slice fidelity gate (`frank/.relays/s6/s6-fidelity-m1/SITREP-orchestrator-planner-20260707-010003`):** the slice-team's design r2 @ `main@a499bc3` — full m-1 surface (branch-A stamp + hint rows + generous prover; §B scope + the flagged `RebuildProjections` fix; §C waivers; §D I1-P via flock/one-inode/phase −1; §F/§F.1 activation verbatim; A-2 1:1). **Headline ask: re-mint = binding replacement** (lands my §13.3 carry; supersedes the S1 option-(b) posture per that option's own no-remint-op condition, now false since A-3 adds the op — supersession logic confirmed sound from the design-lead seat; no persisted counter = §F.1 boundary honored). TO: **m-1.implementer** (verdict = theirs; s6 PLAN dispatch blocked until on record). m-1.planner (CC) holds. **→ verdict: `approve-conditional`** (`SITREP-implementer-20260707-012143`) — five verbatim-carry conditions, no re-review owed: F-S6-M1-1 re-mint/custody (binding table **current-credential-only** — §F.1 boundary held; credential visible ONLY in the operator submit reply); F-S6-M1-2 lock (flock = authority, content diagnostic-only); F-S6-M1-3 both filter halves; **F-S6-M1-4 commit-time intake guard** (the one addition — "the sweep is evidence the guard held, not the guard" = §13.2 writer-boundary discipline applied to A-2; plan r1 lacked it); F-S6-M1-5 activation/roster verbatim. Route-back triggers consolidated. **Design-lead check: pure enforcement of the locked amendment set — no contract-silent surface, no amendment candidate** (contrast s2/s4). Ball: s6-core carries F-S6-M1-1..5 into the PLAN/dispatch gate. Holding.
  - **Re-mint crash-window CONSULT** (`SITREP-orchestrator-planner-20260707-042057`, TO m-1.implementer — the F-S6-M1-1 hard stop fired correctly, pre-fold): interrupted re-mint ⇒ OLD credential still Resolves post-recovery (read-only via the R1 generation tag; `completeMissingSeatMintBindings` only repairs fresh mints — the current-credential-only table can't distinguish rotation-applied from rotation-pending). Shapes: **(A) binding-row `pivot_ref` completion provenance (pair+orchestrator rec)** vs (B) documented residual vs (D) rotate-before-commit. **m-1.planner design-intent read (this session, offered to ride with the consult):** the gap is real (post-crash zombie = read-grade stale-credential survival, §13.3 violated); **(A) is intent-faithful** — completion-keyed-by-committed-record = the S2 idiom + §F.1 pivot-keyed discipline; `pivot_ref` = provenance not history (the AuthGeneration class line), doesn't breach current-credential-only; (B) dilutes the carry; (D) inverts pivot-first/§F.1. Expected redlines if A: relay-id only, never in records/projections/roster/errors (I-PH), 0600 admin read. **Ruling = m-1.implementer's.** **→ RULED** (`SITREP-implementer-20260707-102208`): **approve A with 8 redlines** (`realized_mint_ref` = pivot relay-id only; credential+ref in ONE atomic write; recovery predicate = row-realizes-latest-pivot; repair BEFORE channels open — first post-restart Resolve fails; canonical records win; 0600 custody, never on any seat surface; activation boundary untouched; red-first SIGKILL fixture). **B rejected** (zombie dilutes "dies at Resolve"); **D rejected** (effect-before-record — m-7 concurred in-round). Matches the design-lead intent read point-for-point. **Ruled exception recorded in the amendment doc §F.1** (doc-hygiene, cites the ruling). Ball: the s6 pair folds option A per the redlines + fixture. Holding.
- **s7/s7b (build continues; frank main @ `2e1b4f0`, tag `s7-close`):** s7 fidelity round handled in the m-1.implementer lane (`s7-fidelity-m1/…-174040`). **s7b-fidelity-m1** (`master/relays/s7b-fidelity-m1/SITREP-orchestrator-planner-20260711-010840`): scoped close-ordering seam — does the `sync.Once` close owner (fix `a2a6966`, worktree tip `e155aa6`; kills the double-close panic the VP proved) preserve the m-1 lifecycle semantics (no auth/re-attach window re-opened) vs the B-3 bind + §8.5 re-attach record. TO: **m-1.implementer** (verdict TO master, CC VP; on confirm → VP integration → operator merge gate, lifting only `OI-S7A-CLOSE-ONCE-RACE`). No planner intent question (no contract-silent surface — ordering-preservation vs locked text). m-1.planner (CC) holds. **→ CONFIRMED** (`SITREP-implementer-20260711-011823`): `sync.Once` preserves the lifecycle semantics vs §F/B-3 (`amendments.md:95-103,108`); one-credential-one-channel + proven-dead reconnect station-proven. Closed.
- **s8–s10 (CC-visibility, no m-1 ask):** s8 dispatched with the threat-model "confusion not malice" cut applied pre-hardening (`frank-threat-model-scope` — the identity half = the Standing Rail above, recorded). **s10** (m-6's parked-dispatch/scheduler slice, dispatched `s10-dispatch/PLAN-…-205011`): m-1 = **conditional fidelity seat only** — "if any addressing/mailbox seam moves; none anticipated in the spine"; enum + R2 preserved verbatim in the plan. Watching, not working. Holding. **Design-lead action taken (operator-authorized):** the verdict's `system`/`system` **conductor-internal provenance** convention (first non-`submit` records; already in code `frank/internal/gate/derived.go`) is new m-1 stamping-contract surface → **folded into the design-of-record** as a review-driven amendment: new §6 bullet + §0.e fold-log (`s2-amend-m-1/DESIGN-planner-20260704-035030`, lint OK, INDEX-appended). Scoped to m-1's stamping mechanism only (record_kind = m-2; record catalog/shapes = m-7/S2 — not claimed). Enum 3→5×, observer-selected 9×, no verb/on-disk/lock change. **m-1.implementer `DESIGN_REVIEW_VERDICT: approve`** (`035323`, findings: none) — **s2-amend-m-1 COMPLETE + pair-approved**; completion reported up to the orchestrator (`SITREP-planner-035629`) for the note/ratification into the design-of-record trail. Remaining is the orchestrator's ratification (ARCHITECTURE/RECONCILE note); the **separate** S2 fix track (F-M1-1/2/3 fold + m-1.implementer S2 re-review) proceeds via s2.orchestrator-planner — not mine. Holding — no self-advance.

## Step-3 engagements (m-1 as directly-addressed owner)
- **step3-amend-m5-ceiling — the config-generation seam (`RECONCILE-orchestrator-planner-20260715-111000`, TO m-1.planner — first Step-3 owner action on my seat; the VP corrected a prior CC-only ask):** m-10's app-side ceiling-freshness enforcement wants to read a current-active Layer-1 config stamp + monotonic `config_generation` outside the conductor. **ANSWERED** (`SITREP-planner-20260715-124031`, lint OK, INDEX-appended): **Q1 CONFIRM** — `config_generation` is monotonic/never-reused/genesis-anchored by the append-only accepted-graph, on the precise condition it binds the **genesis IDENTITY not a bare integer** (fresh `store.Init` = different genesis; same shape as re-mint pivot-keying + I1-P canonical-identity; bare-integer = route-back trigger). **Q2 CONFIRM** — the stamp is a config-integrity digest, **not credential bytes** (class of `realized_mint_ref`/AuthGeneration/FROM); exposing the value crosses no secret boundary. **Read path ROUTES BACK** — reconciled against the newer **VP F31** (`123753`): both mechanisms m-7 returned (conductor-published stamp, direct IPC) cross packet-r4's no-conductor-change/no-direct-m10-edge invariant; disposition #1 (already-landed mechanism) fails on the store facts (current-active = committed chain, derived `config/` non-authoritative per F-S4-M1-6, no existing verb returns the chain head); so from the store-owner seat **disposition #2 (fail-closed defer, operator-scoped) is the packet-compliant Step-3 path**, #3 (conductor output contract + architecture amendment) the only route to an active read and a master+operator choice, not mine in a confirm. **The m-1 leg the VP flagged outstanding is discharged.** No lock, no new contract surface. Holding.

- **step3-mvp-design-m1 — the m-1 §7 STAGE-1 OWNED CONTRACT (dispatch `DESIGN-orchestrator-planner-20260716-041650`, approved-as-issued; under the ratified MVP amendment r7 @ `2f75f2a1…`; the prior config-generation leg `124031` is OBE with the ceiling stand-down):** two coupled artifacts, **AUTHORED** → `design/2026-07-16-step3-mvp-secret-boundary-seat-identity.md` @ SHA-256 **`aa90fa45…`**. **§1 secret-boundary delta (F57-narrow):** S-A provider keys (m-8-held) + S-B seat credential (broker-held); non-injection into the enumerated surfaces + the I-PH/F-S6-M1-1 continuity extension (conductor records, m-10 store/tickets, E0, manifests); separate-process = accidental-disclosure reduction, same-user inspection = the named residual for BOTH classes (D4/D5 pattern); 7-item hardening list owner-assigned; opaque refs must be leak-inert (§1.4); sentinel honesty wording pinned. **§2 F60 semantics:** logical seat = `(minted address, mint-generation)`, conductor-side, never a process; **THE TWO-COUNTER LAW** — mint-generation (committed pivots, operator-only) vs `turn_epoch` (m-10, automatic) never coupled, worker replacement NEVER advances identity; s6 §F lifecycle mapped (broker = the binding party; worker replacement conductor-invisible + F64 fence as compensating control); re-mint byte-unchanged, custodian swapped; USE capability = delegation-never-identity with the honest E0-attribution note; Standing Rail applied (broker = custody plumbing). **§4 route-back check CLEAN** (no conductor/registry/store change). **→ m-1.implementer pair review of the final bytes requested** (`DESIGN-planner-20260716-045225`, lint OK, INDEX-appended). **→ r1 = must-revise** (`DESIGN-REVIEW-implementer-20260716-050629`; F57 honesty/identity model/two-counter direction/s6 mapping/route-back all ACCEPTED; 3 interface findings, F1/F2 already causing divergent m-7 readings): **F1** §1.4 conflated the leak-inert credential REFERENCE with the intentionally-authorizing USE capability → **split 1.4a/1.4b** with per-object authority+leak claims + 2 consumer negatives; **F2** the visibility floor didn't name the broker's persistent sink → **pinned: ONE broker-private 0600 credential file** (m-7's authored choice) joined to the S-B census (rotation-overwrite/no-dump/no-backup/delete-on-decommission), preserved negatives explicit; **F3** the two-counter law lacked the event cross-product → **new §2.7 five-event matrix** (replacement/broker-restart/conductor-restart/re-mint/**re-mint∥replacement**), normative at the identity grain, + the both-orders overlap fixture in §5. **All folded (rev1) → fresh bytes @ SHA-256 `f3994fdc…` → re-review requested** (`DESIGN-planner-20260716-051843`, lint OK). Cross-lane note: m-7's parallel r1 must-revise (`step3-mvp-design-m7/…-050908` F2) requires m-7 to **rebase on these revised m-1 bytes** — the rebase target now exists. **→ r2 = must-revise** (`…-052634`; r1-F2 closed, split structurally correct, matrix+overlap accepted; 3 narrow consistency findings vs current consumer bytes): **MR1** §1.4a's authorization examples were the wrong mechanisms → corrected to the REAL paths (S-A: the m-3 provider-policy gate authorizes the frozen core at m-8 — F59 authorizes the app-side tool call only; S-B: the broker's operator-provisioned startup/re-auth — F64 fences channel USE, never resolution); **MR2** the §2.7 broker-restart row let old connection tokens survive → corrected (all old connection-scoped capability material DIES; reattach at unchanged epoch only via the authenticated restart protocol with FRESH material); **MR3** the capability surface omitted typed `Describe` → completed (fenced transport metadata — not a fourth verb, not a dispatch tool, no identity consequence; stale-epoch `Describe` negative added). **All folded (rev2) → fresh bytes @ SHA-256 `7baffe40…` → r3 re-review requested** (`DESIGN-planner-20260716-053040`, lint OK, INDEX-appended). **→ r3 = must-revise, ONE residual** (`…-053603`; MR1/MR2/MR3 all closed): R3-F1 — my MR3 fix made `Describe` explicitly not-a-verb, so the three later "verb + push" normative summaries (§2.3/:56, §2.5/:66, §3/:86) now EXCLUDED it by my own definition → **normalized all three to the complete §1.4b closed surface** (3 canonical relay verbs + typed `Describe` request operations + per-push fence at forward time; no `three verbs`/`per-verb` phrasing survives). **rev3 → fresh bytes @ SHA-256 `7c8b09a6…` → r4 re-review requested** (`DESIGN-planner-20260716-060705`, lint OK, INDEX-appended). Cross-lane: m-3's stage-1 contract is pair-approved (`055500`) — m-1 and m-7 are the remaining stage-1 lanes in revision. **→ r4 = APPROVE, byte-bound @ `7c8b09a6…`** (`DESIGN-REVIEW-implementer-20260716-061153`: R3-F1 + all r1/r2 bars re-verified closed; F57 honesty, identity model, route-back certified). **THE m-1 STAGE-1 CONTRACT IS PAIR-COMPLETE** — four uniquely-parented rounds (r1 F1–F3 structural → r2 MR1–MR3 sentence-grade → r3 R3-F1 consistency → r4 approve), monotonic convergence, every round fresh-hashed. **Pair-approved SITREP filed to master** (`SITREP-planner-20260716-061835`, exact-file + lane-root lint OK, INDEX-appended) naming the frozen bytes + hash. NOT claimed: consumer confirmations (m-7 realizes the broker UNDER these semantics — its lane still in its own must-revise with my bytes as rebase target; m-8/m-9/m-10) route on master's direction; the interface lock is Master+VP's only. m-1 stands ready for confirmation round-trips or lock findings. Holding — no self-advance.
- **step3-mvp-confirm-m1 — the m-1→m-7 semantics-fit confirmation (routed `RECONCILE-orchestrator-planner-20260717-010050`; m-7's contract pair-approved @ `f072bd99…`, its §E consumed my approved bytes):** **CONFIRM returned, byte-bound both ways** (`SITREP-planner-20260717-011145`, lint OK, INDEX-appended). All six surfaces verified from the m-7 bytes directly: ① S-B custody + the ONE 0600 sink classified as my §1.4a resolution path with census-by-reference (their descriptor-safe loading **exceeds** my floor); ② the 1.4a/1.4b split consumed verbatim incl. the not-a-locator rule (their connection-scoped per-generation minting **exceeds** MR2 — fresh material structural); ③ the rev3 closed surface exact (3 verbs + `Describe` + per-push fence; stale-epoch denies all four + push); ④ the two-counter law held (epoch ONLY from m-10's feed, never derived; overlap row cites my §2.2; fail-closed suspension = no stale authority); ⑤ the s6 §F mapping + my §2.7 matrix realizable (MR2 token-death cited by name; FX-TB-7 overlap leg; their additive app-main-crash row couples nothing); ⑥ the Standing Rail held (stamped FROM untouched; no principals; OS machinery with ceiling stated; no attestation claim — seam open). Two non-blocking notes: **N1** stale "rev2" label in their §2.12 (content-exact — §1.4a unchanged rev2→rev3; label nit for their next natural revision); **N2** their `control_generation` = a third counter for handover ordering only, couples to neither identity counter — inside my semantics. Awaiting: the reciprocal m-7→m-1 confirmation + m-8/m-9/m-10 confirmations of my contract + the Master+VP interface-lock. Holding. **→ REFRESH ROUND (`RECONCILE-…-123055`; my `011145` confirm voided by m-7's F70/L1/R7-F1 corrective):** both items returned (`SITREP-planner-20260717-124027`, lint OK, INDEX-appended). **(1) Row-6 RE-AFFIRMED at m-7 r8 `ab0ed428…`** — the F70 5b withheld-snapshot arm **strengthens** my 1.4b (worker attach refused fail-closed until an installed state exists — no capability minted under epoch ambiguity); R7-F1 recognition×commit = **my MR2 principle extended to transition state** (a fresh broker instance inherits nothing it didn't freeze; committed old sets never install by bare ledger ack; E never resumes); L1 canonical-decimal-string counters = serialization fix off my credential surfaces (`…-060542` comparand semantics unchanged); surfaces 1/2/4/6 re-verified unmoved; **N1 stale-label nit SURVIVES r8** (2 loci: §2.12 "rev2 §1.4a" + the Basis line — content-exact, non-blocking, named twice so it doesn't fossilize into the lock package). **(2) m-10 r12 `credential_ref` CENSUS CONCUR: in-class 1.4a** (operator-selected opaque label at provisioning, m-10 copy-only presence-checked, resolution ONLY inside m-8's authorized attach = my MR1 S-A path; the no-credential-derived-verifier clause restated as binding provisioning/grammar guidance). Byte-bound to all three hashes. Master folds into the corrected 16-edge close table. Holding. **→ FINAL citation refresh (`RECONCILE-…-204042`; m-7 closed at r11 `9331ea88…`, reviewer-certified "custody did NOT move" r8→r11):** row-6 **re-cited at r11** (`SITREP-planner-20260717-205136`, lint OK, INDEX-appended) — **TCB-owner option exercised**: spot-verified rather than inherited (hash reproduced; 5/5 custody loci present/singular/byte-consistent; the r9→r11 folds all move fail-closed in the suspension/attach space — attach-during-PREPARING refused further strengthens no-mint-under-ambiguity); certification concurred on my own read. **N1 named a THIRD time** (stale "rev2" labels persist at 2 loci into the FINAL basis) — recommended as a one-line close-packet erratum, no r12. **THE m-1 STAGE-1 DESK IS CLEAR**: owner contract final @ `7c8b09a6…` (pair-approved r4) · row-6 confirm final @ r11 · the credential_ref census concur standing. Awaiting only the Master+VP interface-lock (and any lock findings routed back). Holding.
  - **Stage-5 consumer confirmation, leg 3 of 4 (`RECONCILE-…-20260720-070747`; m-10's RUNNING control-plane design r8 @ `78876829…`, approve `081600`):** **CONFIRM returned, own-read, no findings** (`SITREP-planner-20260720-071330`, lint OK, INDEX-appended; byte-bound r8 × `7c8b09a6…`). ① `credential_ref` lifecycle faithful and **structural beyond my floor** — "no secret-bearing column exists (§F)" (a schema with no place for secret bytes); the census row carries `policy_owner: m-1` + my contract cited by hash; the bypass cell states F57-narrow verbatim ("frank never moves secret bytes, not OS isolation"). ② Non-possession negatives hold through ALL the new lifecycle machinery (spawn holds NOTHING per F60/F66; recovery-is-the-only-boot is credential-path-free; retire/pair-launch/G-2 crash-loop credential-free; F59 "a ticket is never a bearer token"; the terminal surface never-secrets; "m-10 never submits"). ③ The §11a census applies my confusion-not-malice/same-UID-residual boundary **per-row** with honest edges ("'private' is a discipline against confusion, not an OS boundary"; external-kill-presents-as-crash; bash-ambient named). Master carries the leg into the **stage-6 lock packet**. Holding.
  - **H-16 (a)–(g) TCB owner confirmation (`RECONCILE-…-20260720-223835`; m-7's outcome-split design rev12 @ `9d7f6aed…`, pair-approved after 11 must-revise rounds; VP adversarial half runs parallel):** **ALL SEVEN CONFIRM** (`SITREP-planner-20260720-224307`, lint OK, INDEX-appended; byte-bound, VP-revise-voids accepted). Highlights: **(b)** the `mint_predecessor` chain = my commit-order redline REALIZED byte-explicitly (post-redo-deletion, the writer-stamped link IS commit order in canonical bytes; tip-by-set-predicate; fail-closed conflicts); **(c) the DISTINCT ruling GRANTED** — completeness-verified redo (exactly-once P-membership + intact segments ⇒ faithful commit-order subsequence) may serve as **one-time** system-anchor selection evidence, scoped selection-only / upgrade-only / never-presence-grade (no ongoing-authority precedent; every deficiency → operator anchor); **(e)** effective-quarantine = **exactly my named zombie option** — both windows closed incl. the R9-F1 resolution-alone gap (the `AfterCommit`→`AfterAccepted` reopen); the match-gated `auth:seat-quarantined` leak rule confirmed (probes learn nothing); **(f)** the offline ceremony preserves sole-writer (I1-P mutual exclusion) + the credential-visibility floor (synchronous reply = the genesis-delivery custody path); role-disagreement resolved BY selection under custody authority (accepted-canonical-wins honestly applied); **(g)** pivot-is-the-generation-boundary ratified against the R10-F1 replay defect — one canonical pivot per credential ever created; NO-AUTHORITY-DELTA (delivery is never an authority edit; `retry-authority-delta` typed rejection); `admin_provenance` = §0.e discipline verbatim. Notable: three of the design's correction rounds (R9-F1/R10-F1/R11-F1) were their reviewer catching drift against MY locked rulings — the cross-domain contract doing its work. Master folds into the F93 closure; a VP revise routes back for re-cite. Holding. **→ The VP revised (F97 self-contained / F98 fixture split / F99 custody-basis correction) → rev16 re-confirmation routed (`RECONCILE-…-002720`) → ALL SEVEN RE-CONFIRMED at `a349a329…`** (`SITREP-planner-20260721-003036`, lint OK, INDEX-appended; **F100-conditional accepted** — binds only with m-2's leg + the master/VP join over the same hash). (a)–(e)+(g): carriage VERIFIED through the materialization (greps, not assumption); rev12 grounds re-bound verbatim incl. the (c) scope conditions; F98 consistent (wire locus stays the authority; client gate = defense-in-depth). **(f) FRESH-RULED per F99 — CONFIRM, stronger than rev12:** phase −1 `AcquireRoot` realizes my s6 §D acquire-before-touch law clause-by-clause (before-ANY-touch · held-through-exit · both-ways one-writer exclusion · alias-root canonical identity · socket-probe = the banned-heuristic class, demoted to post-lock UX); the false `-mint` lock precedent honestly RETRACTED and **H-26 ENDORSED from the invariant owner's seat** (the live `-mint` unlocked writer = an I1-P violation window; the shared-shaped lock helper is the right convergence path); genesis analogy = authority basis only, mechanism = the law. Awaiting: m-2's parallel leg + the final master/VP join (same-hash condition) → the H-16 locked PLAN → IMPL branch → operator merge grant. Holding.
- **Lane-2 DAG HOLD (`step3-relock-dag-hold/RECONCILE-…-235500`, STOP addressed to all six lane-2 planners; the VP caught a live-dispatch authority leak):** the m-1 DAG dispatch (`step3-relock-dag-m1/…-231505` @ `07fd8974…`) is SUPERSEDED + INERT. **Action-to-date returned: NONE** (`SITREP-planner-20260721-231922`, lint OK, INDEX-appended) — the hold was the FIRST sight of the lane at this seat; disk-verified (zero m-1 files in the dispatch dir; domain design newest = the frozen stage-1 contract). The re-cut already in the dir (`…-235605`) is treated INERT per the hold's own rule ("a VP verdict is not a release; this hold is not a release"). **m-1 is STOPPED on lane-2 until master's explicit addressed release arrives** (after the F2–F5-closed re-cut + fresh VP decomposition review). Note for the eventual release: the VP's F2 names an "m-1 at-rest/K6 review" among the missing Tier-HARD obligations — expect that to appear in the corrected m-1 dispatch. Holding. **→ THE ADDRESSED RELEASE ARRIVED** (`RELEASE-…-20260722-004005`; rev2 `9a4ee380…` ACTIVE, VP r3-approved) → **the author-now scope EXECUTED**: delta doc `design/2026-07-22-stage6-lane2-env-redaction.md` @ **`41385dfc…`** (F73 additive over frozen `7c8b09a6…`, zero frozen bytes moved) — **§1 the C env sanitization rule** (construct-from-empty; 3 allow classes — POSIX baseline / frozen-manifest run vars copy-only / nothing else; deny-lists non-compliant; honest class-(ii) value boundary) **+ the §6-C descriptor rulings** (`env_digest` CONFIRM w/ the input-identity condition — digest the presented set never the parent env, the one real oracle risk; `shell_interpreter_ref` CONFIRM; `canonical_resource`/`cwd` CONFIRM w/ workspace-root separation elevated to a review invariant); **§2 the D redaction boundary** (BY CONSTRUCTION — class gate at the writer, scanning explicitly not a boundary; the closed NOT-list = S-A/S-B + `reasoning_replay` (K6 §2.8, F2) + **USE/control capability tokens** (my 1.4b addition); at-rest: #6 placement/permissions, #5 torn-write class safety, per-run GC, no backup/telemetry copy; §1.5 sentinel grain verbatim). **§4 producer-attaching halves PARKED** (m-9 C/D + m-10 ticket reviews + the §D redaction co-sign — criteria stated, judged only against landed pair-approved bytes; leak risk = revise-request never silent accept). **→ m-1.implementer pair review requested** (`DESIGN-planner-20260721-233847`, lint OK, INDEX-appended). **→ r1 = must-revise, 5 findings, all correct** (`DESIGN-REVIEW-implementer-20260721-234730`): **F1** the by-construction claim overreached (open content strings can't carry a structural guarantee — a same-UID `bash cat` of the credential file IS the accepted residual) → **claim SPLIT**: structural on typed object routes / honest confusion-not-malice ceiling on open content, the spec-mismatch route named-not-taken; **F2** the authority family incomplete → F59 tickets + capabilities + control tokens + **fail-closed type evolution**; credential refs non-loggable; **F3** at-rest needed descriptor grain → the m-7 §2.12 symmetry battery (EXCL/NOFOLLOW/CLOEXEC, on-descriptor verification, containment on the opened object); **F4** class (ii) was an OWNERLESS m-10 surface + `LC_*` wildcard → **class (ii) EMPTIED for the MVP**, exact enumeration, dedicated tool-HOME/TMPDIR (default-credential discovery dead at the env grain, honestly bounded), fixed PATH; **F5** the no-oracle-by-shape claim was wrong → WITHDRAWN, one canonical JCS preimage bound. **All folded → rev2 @ `734df728…` → fresh review requested** (`DESIGN-planner-20260721-235452`, lint OK, INDEX-appended). Producer halves stay parked. **→ r2 = must-revise, 2 exactness findings** (`…-000641`; r1 F1–F5 all accepted-closed): **R2-F1** the baseline permitted an internally FALSE child env — `PWD` bound to the relative descriptor cwd (their executed probe: `$PWD`=`.` vs `/bin/pwd -P`), `TZ` two-valued, "system constants" oversold → **fixed**: `PWD` = absolute resolved EXECUTION cwd (descriptor keeps its relative encoding; truth leg `PWD == /bin/pwd -P`), `TZ=UTC` pinned, source argument = closed reviewed non-secret set (trusted-config/generated/OS provenance), non-UTF-8 a REACHABLE reject; **R2-F2** sentinel expectations depended on unstated ingress routes (the same bytes are expected-absent from a governed source but PERMITTED from open content) → **every leg route-labeled** (governed-source plants ⇒ absent · prohibited-object routes ⇒ writer rejects · **the open-content ceiling leg ⇒ expected-present, never a pass/fail oracle**), labels carried into the parked m-9 criteria. **rev3 @ `2f091751…` → r3 review requested** (`DESIGN-planner-20260722-001938`, lint OK, INDEX-appended). **→ r3 = must-revise, ONE residual** (`…-002830`; R2-F1/F2 mechanically pass): R3-F1 — the stale `no operator-supplied values` parenthetical survived in the digest paragraph, contradicting the accepted provenance correction (and my r3 relay had claimed it gone — the drift class exact-byte review exists for) → **narrowed to its true scope** (class (ii) empty ⇒ no run-manifest class-(ii) values; class-(i) may carry trusted operator/config influence; safety = per-source review, never provenance-absence). **rev4 @ `d34a7c47…` → r4 review requested** (`DESIGN-planner-20260722-003304`, lint OK, INDEX-appended). **→ r4 = APPROVE, byte-bound @ `d34a7c47…`** (`DESIGN-REVIEW-implementer-20260722-003714`; all accumulated bars r1 F1–F5 / r2 F1–F2 / r3 F1 closed on the exact bytes; the verification inverse-transformed the rev4 diff reproducing rev3's hash — single-sentence delta proven). **THE m-1 LANE-2 COMPONENT IS PAIR-COMPLETE** — the author-now scope of the addressed release discharged; **component-complete SITREP filed** (`SITREP-planner-20260722-003913`, lint OK, INDEX-appended) with the four producer-attaching halves NAMED + PARKED (m-9 C · m-9 D · m-10 C · the §D join co-sign — attach only to landed pair-approved bytes; leak risk = revise-request never silent accept). Awaiting: producer bytes routing back for my reviews, the §D co-sign at the settled join, and master's lane-2 integration / any stage-6 re-lock sequence. Holding — no self-advance. **→ PARKED HALF #3 (m-10 C) DISCHARGED** (`step3-relock-dag-m10/DESIGN-planner-20260722-193000` requested the ruling over their pair-approved rev6 `29a123fe…`): **S1/S2/S3 all CONFIRM, no findings, no m-10 edit owed** (`SITREP-planner-20260722-015123`, lint OK, INDEX-appended; byte-bound rev6 × frozen `7c8b09a6…` × component `d34a7c47…`). **S1** the manifest/receipt/disclosure union verified CLOSED from their schemas (constructed not filtered — ids/digests/closed tokens only) + 2 scoped ceiling notes: `canonical_args_digest` is a digest over OPEN CONTENT (safe on governed paths per frozen §1.2 non-injection; secret-derived only under the accepted residual — the SAME ceiling as my `env_digest`, not new exposure), and **`resume_snapshot` at rest = a COLUMN in the already-reviewed store, NOT a new file artifact — so my §2.3 descriptor battery does NOT apply** (it governs m-9's log FILE; importing it here would be ceremony without a boundary). **S2** CONFIRM — `env_digest`/`shell_interpreter_ref` absent from m-10's store ⇒ **my class-(ii)-EMPTY ruling satisfied, no manifest env member exists or is implied** (my §4 criterion verbatim); `canonical_resource`/`cwd` safe by my containment invariant (sink + runtime dir outside every workspace root), and their form-only/E3-truth narrowing matches my opened-object-not-lexical-path rule. **S3** CONFIRM **+ ONE BINDING CARRIER CONDITION** (not a finding — their bytes comply): `workspace_root_path` **and** the `session_log_path` inside `resume_snapshot`/`turn_open` must NEVER ride a conductor record, projection, INDEX row, typed error, or E0 body (I-PH + frozen §1.2) — bound as a negative so a later carrier can't silently breach it. F60/F67/no-payload/K6 confirmed **by schema check, not by claim**; the §1 3-class mapping explicitly NOT ruled (m-10/m-9 seam, outside my lane — silence ≠ endorsement). **Parked ledger: #1 m-9 C · #2 m-9 D · #4 §D join co-sign remain** (the receipt seam is still jointly unsettled, so the join bytes don't exist yet — correctly parked both sides). Holding. **→ THE §D JOIN OPENED** (receipt seam settled: m-9 r14 `514f8855…` + m-10 rev16 `3e3c5192…` pair-approved; `step3-relock-settlement-amend/RECONCILE-…-220000` routes the three legs — m-9 half tendered, m-10 reciprocal + m-1 redaction in PARALLEL). **m-1 redaction/at-rest/K6 leg RETURNED (parked halves #2+#4)** over m-9 r14's D1 log (`SITREP-planner-20260722-211004`, lint OK, INDEX-appended): **(a) secret-leak CONFIRM as the SPLIT** — the closed record union structurally excludes S-A/S-B + `reasoning_replay` + the authority family (F59 tickets/capabilities/control tokens = fail-closed evolution via unknown-kind-faults); open content (`input_item`/`tool_result`/`provider_output`/args) is under the ratified same-UID ceiling, no new surface; co-signed AS the split. **(c) K6 CONFIRM** schema-verified (no union kind carries the replay envelope; in-memory only). **(b) AT-REST = FINDING M1-JOIN-F1** — the D1 log/segment FILES lack the descriptor-grain create/open/verify battery (O_EXCL/O_NOFOLLOW create · no-follow reopen · on-descriptor regular-file/owner/0600/link/dev-inode verify · containment on the opened object not lexical path); §1.7's logical-record checks pass while the descriptor points elsewhere (the R2-F1/F3 lesson); the log is a SEPARATE artifact, NOT m-10's already-reviewed store, so it needs its own battery (symmetric with my §2.3 + m-7 §2.12). **The m-1 leg does NOT co-sign; the §D join is HELD on this leg** pending m-9's F73 fold → fresh pair review → re-tender → my re-review. Positive: my r8 carrier-negative already correctly folded at m-9 §1.12. m-9 C (§7) stays parked (out of this leg's scope). Holding. **→ m-10 reciprocal (leg 1) received** (`DESIGN-planner-20260725-224500`, S-1/S-2 member-by-member confirm + S-4 grain-agreement over m-10 rev16 × m-9 r14) — filed in parallel, so it assumed my leg would clean-confirm. **Leg-reconciliation note filed** (`SITREP-planner-20260722-211417`, lint OK, INDEX-appended) surfacing three cross-leg facts m-10 couldn't yet see: (1) leg 3 is a FINDING not a confirm → the join is HELD; (2) the "within the reviewed private-store discipline" framing (m-10 echoing master's routing) is the imprecise one M1-JOIN-F1 corrects — m-9 §1.1 says the log is "Not m-10-hosted," a separate artifact; (3) **M1-JOIN-F1 moves m-9 r14→r15, voiding m-10's r14 binding under m-10's OWN F84 discipline** — m-10 must rebase its reciprocal to the folded hash before co-sign (master sequences). No new m-1 deliverable owed. Holding. **→ m-9 FOLDED M1-JOIN-F1** (r15 added §1.6a → r16/r17 accounting-only; **pair-approved r17 `01b885fe…`**, zero findings) and **re-tendered** (`DESIGN-planner-m9-20260726-014500`) requesting my (b) §1.6a re-review only ((a)/(c) claimed byte-stable). **→ m-1 §D LEG CO-SIGNED on r17** (`DESIGN-planner-20260723-023020`, lint OK, INDEX-appended): **M1-JOIN-F1 CLOSED** — §1.6a verified **point-by-point against the finding, not the summary** (all 9 elements present: 0700 parent · `O_EXCL|O_NOFOLLOW` create · no-follow reopen · `fstat`-on-descriptor reg/owner/0600/nlink/dev-ino · replacement-race reject · containment-on-opened-object · fail-closed-names-class · 7-RED+1-GREEN · same-UID residual explicit; + precision beyond the ask: `fstat`-not-`stat`, `O_CLOEXEC`+0600 on create, the symlink-redirected RED leg). **(a) split + (c) K6 CARRY** (§1.2:73 + §11:552 byte-verified stable, not trusted). All three m-1 sub-legs clean ⇒ **m-1 leg co-signed, byte-bound to r17. Discharges parked half #2 + the m-1 side of #4.** Note: m-9 self-caught the F84 reopen I flagged in `211417` (its R15-F1). **The JOIN co-signs when m-10's fresh reciprocal on r17 + m-3's r19→r17 rebase also land clean** (neither mine). Still parked: #1 m-9 C (§7). Holding — leg clean. **→ m-10's REBASED reciprocal LANDED clean** (`DESIGN-planner-20260726-020000`, superseding r14-bound `…-224500`): S-1 (§2) + S-2 (§3/§4) re-verified byte-stable at r17's bytes + **S-4 exact-wire consumer CLOSED member-by-member** (PARKED→CONFIRMED, discharging the §11-owed exact consumer confirmation). Its "m-1 owes §1.6a" line is stale parallel-write — my leg co-signed at `…-023020` before it. **Leg-reconciliation SITREP filed** (`SITREP-planner-20260723-025036`, lint OK, INDEX-appended): all three §D co-sign legs now stand clean on r17 × rev16 (m-9 half `…-014500` · m-10 reciprocal `…-020000` · m-1 co-sign `…-023020`); **the sole remaining item before master records the lane-2 DAG close is m-3's r19 `92e08d09…`→r17 consumer rebase** (not mine — I do not re-verify the m-9⇄m-10 seam). No new m-1 deliverable owed; my component `d34a7c47…` unmoved. Holding — leg co-signed, no self-advance. **→ MASTER RECORDED the §D join CO-SIGNED** (`RECONCILE-…-123000`; all three legs byte-verified + m-3's r19→r17 rebase pair-approved at r24 `651c9aec…`) and routed THREE residuals gating the lane-2 DAG close — **residual 1 is mine: the formal C-confirm over m-9 r17 §7** (the context-binding effect descriptor, parked at `…-211004` as "aligned on a glance, formal reserved for when routed"). **→ C-CONFIRM RETURNED** (`SITREP-planner-20260723-041943`, lint OK, INDEX-appended; against r17 §7 `:475-491` × my `d34a7c47…` §1 class table `:16-30` + §6-C `:33-35`): **CONFIRM, no finding, no m-9 edit owed.** `env_digest` digests the m-1-SANITIZED presented set never the parent env (my RED-leg binding condition honored, `:489`⇄`:33`); `shell_interpreter_ref{path,version,content_id}` = non-secret system fact w/ honest `unknown`-HOLD; `cwd`/`canonical_resource` = workspace-root form strings honoring the F103 relative-cwd/absolute-`PWD` split (`:27`) + the separation invariant (`:35`) + my r8 `workspace_root_path` carrier-negative folded at `:487`; the two open-content digests (`canonical_args_digest` + `env_digest` whole-set) ride the SAME accepted same-UID ceiling, no new surface. **ONE carried binding condition** (not a finding): the byte-exact `env_digest` preimage recipe is m-1-owned (JCS over `{name:value}` · duplicate-name reject · non-UTF-8 reachable reject), which m-9's looser §7 prose defers to and both m-9 + the E3 observer realize at the fixture lane. **THIS DISCHARGES PARKED HALF #1 — the LAST m-1 lane-2 obligation.** With the §D leg co-signed + C-confirm returned, **m-1 owes NOTHING further on lane-2**; the DAG close now waits only on the two non-m-1 residuals (m-10 §9-item-2 C ticket schema · m-9 B-consumability to m-3). Holding — lane-2 fully discharged, no self-advance. **→ m-9 ACCEPTED the env_digest binding condition** (`DESIGN-planner-m9-20260726-144500` §3; +confirmed consuming m-10's rev16 §5 C ticket schema = residual #2, m-9↔m-10, not mine; r17 rightly UNMOVED for a non-join F73 confirm). **→ m-1 ACKNOWLEDGED + ONE record-accuracy correction** (`SITREP-planner-20260723-043617`, lint OK, INDEX-appended; verified at the bytes): the acceptance is substantively right, BUT m-9's grounding claim "§10 asserts preimage-parity" is unsupported — **m-9 r17 §10 (`:513-550`) carries NO env_digest preimage/duplicate-name/non-UTF-8 leg** (grep-verified; §10 `:546` C-fixtures = content_id + cwd only; the `:542c` observer-parity is for the E `logical_surface_digest` `:380`, a DIFFERENT digest). The three legs live in **my §5 `:63`** (verified present), which is the correct locus (I own the env rule + preimage recipe; m-9 constructs the env set, so my §5 specifies+exercises its derivation). **NOT a leak finding, NOT an r17-move** — the obligation IS on record via my §5, so residual #1 stays DISCHARGED and the DAG close is not gated; any executor-side §10 env_digest leg is a re-lock nicety. Correction is attribution-only: T4 build binds the env_digest fixture from m-1 §5 `:63`, not m-9 §10. m-1 lane-2 FULLY DISCHARGED (§D co-sign · C-confirm · this ack). Holding — no self-advance. **→ ★ LANE-2 INTERFACE DAG CLOSED** (master `RECONCILE-…-160000`, TO all six lane-2 planners): master records the close over the **nine byte-verified settled bases** (m-1's env/redaction component `d34a7c47…` / frozen `7c8b09a6…` among them) atop the co-signed §D join; all three residuals discharged both sides; the §D join + §B sink + item-E/B/C bindings NORMATIVE. The `env_digest` **preimage-parity fixture obligation** is carried into `STEP-3-EXIT-FIXTURES.json` as **m-1-owned** (no edit owed), alongside the two carried limits (N910 doc-MVP-limit · r7-mirror v3-deferred). **→ MY CORRECTION ACCEPTED** (master `RECONCILE-…-163000`, TO m-1+m-9): the preimage-parity obligation binds **m-1 §5 `:63` ONLY** (not "m-1 §5 + m-9 §10" — m-9 owned its §10 E-parity/C-env conflation at its own bytes); the DAG close STANDS (pointer refinement, not a re-open; r17 UNMOVED). Master recorded a LESSON crediting the F84 catch ("a fixture-locus citation is a claim a build reader executes against — must be checked at the cited bytes"). **m-1 LANE-2 IS CLOSED — nothing owed.** Item A is now RATIFIED + AUTHORED as the plain byte-bound interface-lock record `master/STEP-3-INTERFACE-LOCK.md` (external SHA-256 `cbd1893c7b1ed6a88aa4668ff6e333e037f6898069594b74f758918ad8913636`), per the item-A simplification amendment `master/STEP-3-ITEM-A-SIMPLIFICATION-AMENDMENT.md` rev7 `3443f73d30240c90aa6b1b46cbcd3763444592c422ff8ab655307bc1ebbd7373` (operator-ratified 2026-07-27) — the prior extraction-bundle mechanism is superseded/withdrawn. The record is now in VP + F73 review. Next m-1 engagement: lane 4 (the Master+VP re-lock over the record + the `STEP-3-EXIT-FIXTURES.json` freeze) → lane 5 (T4), whenever master routes it. Holding — no self-advance.
- **LANE-4 esc1 Route 3 — the EDITED-SESSION state machine (JOINT dispatch `step3-relock-lane4-esc1-route3-editsm/DESIGN-orchestrator-planner-20260726-032916`, TO m-9/m-10/m-3/m-1; operator Decision 5 makes external session editing a REQUIREMENT — `bivpak` + emergency hand-repair; governing constraint: an edited byte must NOT silently inherit the original's trust = the fabrication class; `receipt_conflict` stays FROZEN, not m-1's seam):** m-9+m-10 own the state machine (items 1–6), m-3 the evidence consequence, **m-1 the at-rest/provenance boundary review**. **→ BOUNDARY REVIEW RETURNED** (`step3-relock-lane4-esc1-route3-editsm-m1/DESIGN-planner-20260726-033458`, lint OK, INDEX-appended; TO master+m-9+m-10). **Load-bearing distinction: the edit surface is the APP-SIDE session artifact (m-9 D1 journal + m-10 settlement state), NOT the m-1 conductor-governed relay store** — stated as **BR-INV** (the edit surface never reaches the governed store; the store stays sole-governed-writer D4 + same-UID D5/H-12 residual). **(a) store isolation UNDISTURBED** — my §2.3 at-rest battery already draws the needed line: a CONCURRENT edit during a live turn ⇒ replacement-race/`(dev,ino)` leg fail-closes (`:48`); a BETWEEN-session edit ⇒ outside the descriptor window = the sanctioned case, reconciled at next `turn_open`; no m-1 change. **(b) seat-stamped provenance UNDISTURBED + ALIGNED** — session content was never seat-stamped; non-promotion of edited bytes IS the confusion-not-malice/honest-labelling DNA. **(c) CONFIRM master's reading at the bytes** (`2026-06-28…` "verifiability-against-the-courier is not v3.0's threat model; courier-as-TCB; defer Merkle/CT"): the requirement LEANS ON the deferral — change degrades honestly (`content_lost`/`DEGRADED`) precisely because there's no tamper-evidence invariant to violate (mirror of why hash-chaining died). **Item 7: ZERO m-1 MVP supersession members** (edited-session semantics are all m-9/m-10 members). **ONE Step-4 m-1 obligation flagged** (direction-of-record carry, not an MVP amendment): when Step-4's "authorised rebase across BOTH stores" brings the governed store into the edit surface, BR-INV is crossed → the deferred D3 verifiability-against-the-courier seam MUST be retired (an authorised, provenance-re-establishing rebase) BEFORE the governed store is externally rebaseable, else it's the fabrication class at the governed-store grain. m-1.implementer adversarial review offered pre-gate. m-1 byte owed: NONE (`d34a7c47…`/`7c8b09a6…` unmoved). Holding — boundary confirmed, no self-advance. **→ CLOSURE dispatch (`…-close3-editsm/DESIGN-orchestrator-planner-20260726-044200`, VP F1–F4 + operator §8b ruling): the operator NARROWED the edit surface — m-10's settlement store (receipt rows + resume snapshot) is now ALSO EXCLUDED ("effectively uneditable"); the edit surface is m-9's journal ONLY.** My prior BR-INV had placed m-10's settlement state IN the surface, so master owed me a re-confirm — and F1 requires it PAIR-APPROVED (implementer exact-byte review), the gate the whole amendment fell on. **→ BOUNDARY RE-CONFIRM RETURNED** (`…-close3-editsm-m1/DESIGN-planner-20260726-130845`, lint OK, INDEX-appended; TO m-1.implementer + master): **BR-INV′** (sole edit surface = m-9 journal; BOTH the m-1 governed store AND m-10's settlement store excluded). All three answers STRICTLY STRENGTHENED — (a) a fortiori (two stores excluded; my §2.3 battery still governs the journal file, never m-10's SQLite column, per my `…-015123` ruling); (b) strengthened (the immutable receipt reference is now guaranteed-frozen → an edited journal fails presence against an un-tampered comparand, never a mutually-edited pair); (c) courier-verifiability deferral unchanged. **Honest MVP claim ENDORSED** (a checksum-recomputing journal payload edit is undetectable at the bytes → resumes VISIBLY FLAGGED edited/degraded, never silently promoted; the carrier is m-9/m-10's item 1, not mine). **Repairability-scope consequence flagged** (Decision 5's "a text editor works" now scoped to the journal ONLY; m-10's store has no MVP repair path = the operator's accepted post-product carry). **Item 7: still ZERO m-1 MVP supersession members; the ONE Step-4 obligation now spans BOTH excluded stores.** Frozen `d34a7c47…` UNMOVED (standalone lane-4 return, not a lane-2 edit). **PENDING m-1.implementer exact-byte verdict** before owner-final for master's r2 fold — I do NOT self-approve. Holding — awaiting the pair review. **→ r1 = MUST-REVISE, 3 findings, all correct** (`DESIGN-REVIEW-implementer-20260726-132323`): **F1** the impossible "undetectable-yet-flagged" claim — I admitted a checksum-recomputing payload edit is undetectable then claimed it resumes visibly flagged; can't both hold → replaced by the honest DETECTION SPLIT (m-9 labels/degrades the detectable class: structural/absent/checksum-mismatch/file-identity; a well-formed checksum-recomputed present consistent PAYLOAD edit is UNDETECTABLE → resumes apparently clean at MVP; **m-10's immutable rows key on call identity/`args_digest`/terminals, NOT a payload comparand** — my "fails presence against an immutable receipt" overclaimed); **F2** "all three strengthened" contradicts (c) unchanged → (a) strengthened only (a-fortiori), (b)/(c) UNCHANGED at their owned grain (m-10 immutability = helpful but non-content-identifying); **F3** the recorded Step-4 "both stores" carry = **m-9 journal + m-10 settlement store** (the two session stores whose digests break on rewrite), NOT m-1's governed store — I substituted the wrong first store → split into the recorded m-9/m-10 carry + a SEPARATE m-1 conditional (if the governed store is ever brought into an edit/rebase surface, retire D3 first). Accepted portions: BR-INV′, at-rest split, repairability scope, zero m-1 MVP members, frozen/phase boundaries all PASS. **→ rev2 FOLDED all three → fresh bytes `…-133133`** (lint OK, INDEX-appended; TO m-1.implementer + master) — no new material, F1/F2/F3 only; frozen `d34a7c47…` still UNMOVED. **PENDING m-1.implementer re-review** before owner-final. Holding — no self-advance. **→ r2 = MUST-REVISE, 1 finding** (`DESIGN-REVIEW-implementer-20260726-133821`, which REVOKED a premature `…-133628` approval — inert, not lineage; disciplined self-catch): **M1-CLOSE3-R2-F1** — rev2 collapsed advisory-checksum MISMATCH into m-9's `content_lost`/`DEGRADED`, but m-9's vocabulary (verified at `…close3-editsm-m9/…-131630:41-47`) distinguishes a present/well-formed checksum-mismatch = **`edited` / untrusted-but-model-visible / RESUMABLE-with-labels / NEVER original-truth** from structural/missing = `content_lost` ⇒ `DEGRADED`. I over-reached into m-9's disposition classes (mine is the boundary, not the disposition). **→ rev3 FOLDED → fresh bytes `…-134205`** (lint OK, INDEX-appended): the detected branch now split at m-9's exact 3-class grain — (i) structural/missing ⇒ degrade; (ii) well-formed checksum-mismatch ⇒ m-9's distinct `edited`/model-visible class (NOT content_lost); (iii) recomputed-consistent ⇒ undetectable clean-resume MVP limit — with the boundary consequence (non-promotion holds for detected (i)+(ii); (iii) = accepted residual) stated at the class grain, and the exact tokens/label-VISIBILITY/durable-carrier explicitly ATTRIBUTED to m-9/m-10 (subject to their fresh pair-approved bytes; both m-9 relays pending — bound neither; m-1 promises no operator/E3-visible/durable label). All rev2-accepted portions preserved; frozen `d34a7c47…` UNMOVED. **PENDING m-1.implementer re-review** before owner-final. Holding — no self-advance. **→ r3 = MUST-REVISE, 1 finding** (`DESIGN-REVIEW-implementer-20260726-134917`): **M1-CLOSE3-R3-F1** — rev3 FROZE `resumable`/"NOT degraded" for the checksum-mismatch class while m-9's total disposition table is still OPEN (m-9's own review `…-134147:36-56` requires it to totalize `resumable`-no-action vs `degraded`+`re_derive`; the wire domain to m-10 stays frozen `{resumable, degraded}`, `edited` is m-9-LOCAL off durable/wire/operator/E3). I over-committed to m-9's planner-proposed (not pair-approved) disposition — my lane is the boundary, not m-9's disposition selection. **→ rev4 FOLDED → fresh bytes `…-135431`** (lint OK, INDEX-appended): m-1 REMOVES every fixed disposition and states ONLY its two mandatory invariants — **(1) class-distinctness** (checksum-mismatch ≠ structural/missing `content_lost`; may not be silently swept to degrade NOR silently treated clean) and **(2) non-promotion** (detected checksum-mismatching provider/tool content used in model context must never silently retain original-truth status; carries m-9's local untrusted treatment, mandatory-not-optional, per m-3's no-default-to-trusted) — DEFERRING the exact disposition/first-action/label-use/wire-report to m-9's eventual pair-approved total table (binds neither m-9's `…-131630` nor its proposed `…-134920`/`1f8ec7b6…`). Class (iii) recomputed-payload undetectable residual + all rev3-accepted portions preserved; frozen `d34a7c47…` UNMOVED; `…-133628` stays revoked/inert. **PENDING m-1.implementer re-review** before owner-final. Holding — no self-advance. **→ r4 = APPROVE, byte-bound `909ba17b…`** (`DESIGN-REVIEW-implementer-20260726-135900`; M1-CLOSE3-R3-F1 CLOSED; all prior findings preserved-closed; disposition-neutral boundary certified; cross-owner compatibility VERIFIED-not-re-owned — m-9's approved table `1f8ec7b6…` selects the frozen `{resumable, degraded}` wire deterministically + satisfies my invariants, m-10's approved consumer receives only that seam). **THE m-1 EDITED-SESSION BOUNDARY IS OWNER-FINAL + PAIR-APPROVED** — four uniquely-parented rounds (r1 F1–F3 → r2 F1 [+ revoked premature `…-133628`] → r3 F1 → r4 approve), monotonic to a disposition-NEUTRAL boundary (BR-INV′ + class-distinctness + non-promotion; m-1 decides no `resumable`/`degraded` token). **→ OWNER-FINAL SITREP reported upward** (`SITREP-planner-20260726-140302`, lint OK, INDEX-appended; TO master): names the pair-approved rev4 `909ba17b…` + carries the approval's citation-drift correction (master must compose against m-9's CURRENT `…-135100` `b518994d…` + approval `…-135539` `a0810d59…`, NOT the stale `…-134920` my rev4 non-bindingly mentioned). **m-1 owes NOTHING further on Route 3.** Master composes amendment r2 over the current pair-approved bytes → m-3 + VP review → ratification (Master+VP+operator, held). Frozen `d34a7c47…` UNMOVED. Holding — Route-3 boundary owner-final, no self-advance.
- **§D JOIN RE-SIGN (lane-4 esc1, one-file-per-run body trim; `step3-relock-lane4-esc1-resign` + m-10's half `…-resign-m10-1/DESIGN-planner-20260726-150300`, TO m-1):** the Route-5/close5 one-file-per-run re-scope TRIMMED the S-1 content-ready receipt body — `segment_id` removed (dead locator), `marker_digest`→`round_identity` — so the §D join's S-1 leg re-signs over the new body `56e40261…` (m-9 pair-approved). m-10 filed its re-sign half (byte-confirm `92c9b3a8…`) and deferred the redaction verdict to me. **→ m-1 REDACTION LEG RE-CONFIRMED** (`…-resign-m1-1/DESIGN-planner-20260726-151718`, lint OK, INDEX-appended; TO m-1.implementer + m-9/m-10 + master). Verified at `56e40261…` bytes, NOT m-10's paraphrase: **precision correction — `round_identity` IS a content-derived digest** (SHA-256 directly over the round's `canonical_content`, `:20`), a derivation change from `marker_digest` (digest-of-`{seq,record_digest}`, content-derived only transitively); m-10's "ids/digests only" understated this. **Redaction-neutral CONFIRM on the correct ground:** content-derived digests are already under the ACCEPTED same-UID ceiling (my `env_digest`/`canonical_args_digest`/`marker_digest` rulings + frozen §1.2 non-injection + H-12 residual), NOT because it avoids content — the body carries the 64-hex digest OUTPUT, never the preimage; the one-hash-layer-closer derivation doesn't change the ceiling. (a) CONFIRM · (b) at-rest N/A (persists in m-10's SQLite store column, not a file — §2.3 battery N/A per `…-015123`) · (c) K6 CONFIRM · carrier negatives untouched. `segment_id` removal strictly reduces surface. **I co-sign the redaction axis** of the re-signed S-1 leg; the join re-signs when m-9+m-10+m-1 legs land on `56e40261…`. **PENDING m-1.implementer exact-byte review** (all-returns-pair-approved) before owner-final for r2. Frozen `d34a7c47…`/`7c8b09a6…` + r17 `01b885fe…` UNMOVED; `receipt_conflict` FROZEN (m-10's axis, not mine). Holding — no self-advance. **→ AMENDMENT r2 REJECTED by VP2 on 7 blockers** (omnibus closure `step3-relock-lane4-esc1-close-vp2/DESIGN-orchestrator-planner-20260726-153807`, TO m-1; r2 bytes preserved `ed7e22af…`, r3 lands at a NEW path). **NONE of the 7 is m-1's** (F4 = m-9's trust-label-lowering fork [the approved in-memory label has no executable lowering — m-8's closed `input_item` enum has no trust member]; F5 = master reversed the pair-approved WRONG_LEASE actors → m-9/m-10/m-3/l4 weight re-adjudication; rest = pair-approval debt + master's own drafting). **The m-1 ask is EXACTLY ONE: the m-1.implementer exact-byte review of my §D redaction leg `0791d458…`** (hash reproduced on disk = master's binding; the review already requested in `…-151718`'s TO) — no new m-1.planner deliverable; the ball is m-1.implementer's. **Master VALIDATED my precision correction:** "r3 will carry your corrected ground — `round_identity` is content-derived; redaction neutrality rests on the accepted same-UID content-digest ceiling, not on a body-avoids-content claim." So my byte-level catch landed and is now r3's ground. Holding — leg filed + hash-confirmed, awaiting the pair verdict, no self-advance.

## Layout
- `audit/` — AUDIT artifacts.  `design/` — DESIGN docs + grill locks.

## Step-3 MVP amendment delta (RATIFIED 2026-07-16 — `master/STEP-3-MVP-AMENDMENT.md` r7 @ `2f75f2a1…`; the F44 fold)
**m-1's stage-1 authorship (§7 + grill #3/F60):** **(a)** the **connector secret-boundary delta** — the m-8 provider-key custody AND the m-9 seat-credential custody at the ratified F57-narrow strength (secrets **not injected into** the model context / tool args / bash env / argv / inherited FDs / logs; separate-process placement = accidental-disclosure reduction; same-user inspection of peer-process state = an explicit unsandboxed MVP residual; the OS boundary is Step-4 H-12) + the §2 minimum hardening list; **(b)** the **seat identity + credential-lifecycle semantics** — what a LOGICAL seat IS across worker generations: **one stamped credential per LOGICAL seat**, broker-held (the m-7 broker contract realizes custody under these semantics), never copied into worker generations; a replacement worker is `turn_epoch`-fenced and mints **NO implicit new identity** (worker-per-seat rejected: mint-before-serve makes per-generation minting an operator act; identity churn wrecks lineage accountability); m-10 launches/supervises with opaque references only, never credential bytes. Consistent with the Standing Identity-Seam Rail (channel-stamped FROM unchanged; no new primitive — the broker is custody plumbing, not identity machinery). The stage-1 DESIGN dispatch is issued with this fold.
