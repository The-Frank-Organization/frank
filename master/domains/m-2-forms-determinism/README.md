# m-2 — Forms & Determinism (domain charter)

**Pair:** `m-2.planner` (design-lead) · `m-2.implementer` (adversarial design-reviewer).
**Owns (the relay ENVELOPE):** the declarative field schema (field · owner · type · required-when · enum-set · consumer); the field-ownership model (system-filled / seat-scoped-enum / agent-enum-pick / free-text); the linter's refactor from prose-ambiguity defenses into **form-validation** (intra-relay) + a **lineage engine** (cross-relay).
**Thesis:** relays stop being hand-authored markdown and become a form the courier renders from typed input — "fill the form, don't write the prose" — enforcing authority at FILL (a forbidden option is absent from your seat's form), not post-hoc.

**Consumes (collision edges):** m-1 store (persists/renders the form; supplies FROM/PARENT system-fill). **Co-foundational:** m-2 schema + m-1 store API lock first; m-3/m-4/m-6 design against the locked schema.
**Boundary (m-2 declares the SLOT; siblings own the MECHANISM):** m-3 observe/evidence fills evidence fields; m-4 routing supplies routing-record values; m-6 human-surface consumes the HUMAN_GATE/email-projection fields.

## Status
| Cycle | Phase | Artifact | State |
|---|---|---|---|
| c1 | AUDIT | planner + implementer independent audits | **done**, reconciled + VP-approved (`../../RECONCILE.md`) |
| c1 | DESIGN | design doc `c1-design-m-2-forms-determinism` | **done** — `design/2026-06-28-v3-form-schema-design.md`; GRILL_LOCK folded |
| c1 | DESIGN-REVIEW | round 1 | **must-revise** (narrow; both blockers correct) — `../../relays/c1-design-m-2/DESIGN-REVIEW-Implementer-20260628-173310.md` |
| c1 | DESIGN | doc rev1 (fold) | **done** — ROLE=system-owned; two-state delivery (lineage-before-deliver); + m-1 refinements (TO/CC recipient_picker, null-reserved certification). See doc §16 |
| c1 | DESIGN | m-1 co-foundational coordination | **converged** — proposal `COORD-planner-172553`, m-1 reply `COORD-planner-173516`, ack `COORD-ACK-planner-174201` |
| c1 | DESIGN-REVIEW | round 2 | **APPROVE** — `../../relays/c1-design-m-2/DESIGN-REVIEW-Implementer-20260628-175833.md` (both blockers resolved at E1) |
| c1 | SITREP | design-complete (rev1) → orchestrator | **sent** — `SITREP-planner-180444` |
| c1 | DESIGN | rev2 fold (consumer review m-3/m-4/m-6) | **done** — dispatch `c1-refine-m-2`; folded G1 (HUMAN_GATE hybrid), G2 (gate→email enum), Q-B (ODB ~10-field), routing-relay record (Q-C), slot_in reserved. See doc §17 |
| c1 | DESIGN | m-1 rev2 re-affirm | **mutual + closed** — both directions: m-1 `165643`+`170308` ↔ my `165702`+`170032` (R1 operator-addr, R2 routing_ref non-gating, R3 DI-5 evidence_integrity) |
| c1 | DESIGN-REVIEW | round 3 (rev2) | **APPROVE** — `../../relays/c1-refine-m-2/DESIGN-REVIEW-Implementer-20260629-170241.md` (5 acceptance points at E1) |
| c1 | SITREP | design-complete-rev2 → orchestrator | **sent** — `../../relays/c1-refine-m-2/SITREP-planner-20260629-170716.md` |
| c1 | LOCK | joint m-1↔m-2 co-foundational lock | **✅ CLOSED** — contract mutually re-affirmed; Cluster 4 closed (full-record mirror + posture carve-out + computed-field homes); m-7 lock CQs (CQ-1/2/3/4/4b) m-2-approved; see the c4/c5 rows below |
| c1 | READINESS-FIX-c1 | write-path seam fold (MUST-before-Step-1) | **✅ CLOSED** — `send()` §4 steps 4–5 reconciled to the VP-co-signed seam (pre-append form+lineage → atomic `accepted` / terminal `rejected`; no `submitted` limbo; Step-1 = store+form+lineage); byte-consistent w/ m-1 `224500`; CTO re-verified + VP closure co-sign `230335`. Report: `../../relays/readiness-fix-c1/SITREP-planner-20260630-225007.md` |
| c1 | READINESS-FIX-c4 (4a) | routing R2 grammar fix | **✅ VP-approved** (`232925`) — `declared_deviated`(planner)/`deviated_observed`(observe) split, `any_row:` aggregate trigger, `field:<id>` model-identity exclusion. §18 + AC14 |
| c1 | READINESS-FIX-c4 (4b+4c) | full-record mirror + computed-field homes | **folded** — §17.3 mirrors m-4 `:200-210` (per-row fields + `deviation_reason_code` + reserved `constraints`/`template_ref`); new §17.6 declares `record_integrity`/`surface_intent` homes. §18 + AC15. Report: `../../relays/readiness-fix-c4/SITREP-planner-20260630-234702.md`. 4 sibling confirms in |
| c1 | READINESS-FIX-c4 (posture carve-out) | CTO arbitration `001537` — posture home | **folded (corrected)** — my standalone `human_mode`/posture per-row field was wrong (contradicted m-5 c3 lock `:142` "no new m-2 field"); **removed it** — posture rides `seat_archetype`; kept m-5 posture value-enum (LOCKED at c3) in §17.6; m-4 record un-extended. Report: `../../relays/readiness-fix-c4/SITREP-planner-20260701-012000.md`. **✅ Cluster 4 CLOSED** (owner ruling-confirms + CTO/VP re-verify complete; F2 propose-vs-stamp per-column mix folded in c6 m-5-F1) |
| c1 | Q-F (SHOULD, tracked) | m-3 observe-field row-parity | **accepted, pre-PLAN** (CTO `001537`) — promote 7 §12-prose observe fields into formal §17.3 rows for machine-checkable §13 fixtures; after Cluster 4 closes, before Step-1 PLAN (doc §15 Q-F) |
| c1 | Q-E | `delivery_state` token semantics | **✅ RESOLVED via CQ-4** — `{accepted, rejected, held}`, `bounced` unified to `rejected`; observe-bounce token = m-3's call in CQ-4 (doc §15 Q-E/§17.1) |
| — | c4-cq-gateconfig | m-7 design-lock CQs (CQ-2/3/4/4b) | **✅ m-2 half APPROVED (r2)** — m-2.implementer `021604` approve (3 r1 blockers resolved: `held` narrowed to m-7 §6 internal-fault + CQ-2 fail-closed; `held` consumer carve-out; byte-exact §J2 `gate_category`). Answer `../../relays/c4-cq-gateconfig/DESIGN-planner-20260702-021108.md`; SITREP `SITREP-planner-20260702-021849.md`. m-2's half approved first; the joint CQ closure + CTO fold now proceed **under the m-7 design-lock / c6 gate** (no m-2-blocking leg outstanding) |
| — | c5-claim-sweep-m-2 | claim-text hygiene (malicious→confusion-resistant + D5) | **✅ COMPLETE + pair-approved (r2)** — m-2.implementer `140747` approve (both r1 blockers closed). 17 relabel loci (confusion-resistant + D5) + 14 `[c5 KEEP]`; union-net grep → only :47 (non-overclaim NOTE). Claim-text only, no mechanism change. Rev2 `../../relays/c5-claim-sweep-m-2/DESIGN-planner-20260702-135144.md`; SITREP `SITREP-planner-20260702-141054.md` → CTO folds into c5 status ledger |
| — | c4-cq-m1 (CQ-1 co-sign) | `required_when` step-gate half | **✅ APPROVED + triad complete** — m-2.implementer `030145` approve (no must-revise); m-1 concurs `025200` (non-gating, no boundary drift). Co-sign `../../relays/c4-cq-m1/DESIGN-planner-20260702-024704.md`. CQ-1 evidence set complete (m-1 `013500`+`020418` + m-2 `024704`+`030145`) → **CTO fold** (m-7 NF-S5). New `layer_present:` §5 atom + AC17 |
| — | c6-fix-m-2 | re-review cleanup (7 doc-only findings) | **folded** — m-2-F1 (③ RAISE-ONLY `gate_category` monotonic-toward-A, AC18), F10 (canonical id `gate_category`), F5 (`gate_referenceable:bool` first-class), F7 (this README sweep), F9 (§15 RATIFIED→§J1/§J2), m-5-F1 slice (propose-vs-stamp per-column), m-5-F2 (m-5-ledger, no m-2 edit owed). Doc §18 c6 fold-log. **✅ COMPLETE + pair-approved** (m-2.implementer `211048`; r1 README gap folded r2). Completion `../../relays/c6-fix-m-2/SITREP-planner-20260702-211306.md` → CTO c6 verify sweep + c6 close |
| — | c6.1-confirm-m-2 | differential seam-convergence confirm (3 deltas) | **confirmed** — CTO seam-arbitration byte-verified (E2) against locked targets: `held` one-compound-record (m-7 §6/F1), routing author-set +operator-on-template-spawn (m-4 §7/F4), `deviation_reason_code` config-sourced value-set (m-4 §5/§6/F5); + fixed a §12-echo propagation gap. **✅ COMPLETE + pair-approved** (m-2.implementer `003501`, r2; r1 §12-echo + r2 AC12-echo gaps folded). 3 deltas byte-verified (E2): `held` one-compound-record (m-7 §6/F1), routing author-set +operator-on-template-spawn (m-4 §7/F4), `deviation_reason_code` config-sourced (m-4 §5/§6/F5). Completion `../../relays/c6.1-confirm-m-2/SITREP-planner-20260703-004725.md` → orchestrator c6.1 re-close (VP) |
| — | s1-fidelity-m2 | F3 fidelity edge — s1 consuming-surface usage vs the locked m-2 envelope | **✅ APPROVE (usage-fidelity only)** — m-2.implementer `frank/.relays/s1/s1-fidelity-m2/SITREP-implementer-20260703-171027.md`: §J2 byte-custody, grant narrowing, `held` usage, R1 replay classification, MVP FieldSpec instantiation all faithful to the locked design; contract unchanged. m-2.planner on CC (not the review seat) |
| — | s3-guide-q1 | m-2 as S3 domain guide — 3 confirm items answered **from locked text** | **✅ ANSWERED** — `../../relays/s3-guide-q1/SITREP-planner-20260704-173000.md`: Q1 GRILL_REQUIRED FieldSpec = `bool`/`agent_enum_pick`(monotonic RAISE-toward-yes)/`gate_referenceable:true` (m-6 §5 route; R2-safe); Q2 step-gated replay lands in "caught" under reconstructed observe-context (no third bucket, w/ live-vs-reconstructed label guardrail); Q3 flag-based negative grain S3-sufficient (concrete `chosen_model`/bucket-proxy negatives defer to S4/m-4). **No amendment to locked text, no master escalation.** 1 bounded S3-owned choice (GRILL_LOCK_ID dependent-required?) + 1 S4/m-4 boundary flag surfaced. **✅ Guide answers consumed faithfully at the S3 exit gate** (`frank/.relays/s3/s3-exit-gate/SITREP-orchestrator-planner-20260704-213740.md`, E2 @ `s3-form-impl@fe7308e`): Q1 shape live + the pair's lock-bearing dependent-required (both legs fixtured); Q2 implemented verbatim w/ the no-live-tally guardrail as a structural `LiveCaughtCount` exclusion; Q3 both-flags model-identity chain at load-time. No guide-finding |
| — | s3-exit-gate | S3 EXIT GATE report of record (m-2 in TO as guide) | **✅ GREEN at E2 — VERDICT merge-blocked, operator's close/merge decision.** Full FieldSpec registry live end-to-end (fresh-store), the 62-check dissolution EXECUTED not asserted (115 anchor rows, `:840-873` rowed, zero uncovered), all owed §C4 carries discharged. No action owed to m-2 (no finding surfaces); the operator holds S3-close + merge (`s3-merge-gate/MERGE-GATE-...-213741`) |
| — | s4-wire-impl | m-2 micro-consult (guide) — confirm 5 owed FieldSpec rows for the F-GATE-2 fold | **✅ CONFIRMED** — `../../relays/s4-wire-impl/SITREP-planner-20260705-224500.md`: `owner`/`source`/`target_surface`/`disposition_path` = `free_text`/`text`, `disposes_owed` = `id_ref`, all `gate_referenceable:false`, gated on `record_kind` (∈`owed_item`/`owed_disposition`) — verified against `engine/submit.go` enforcement + the live registry. **RECOMMENDED** the dedicated `record_kind_in:` §5 atom over the generic `field:record_kind==` form (grammar-valid either way; fold not held). Free-text render-scope lean confirmed locked-consistent (seat_scope is enum-only). **Routed 2 out-of-fence items** to s4.orch-planner (owed-authoring intake-authority since record_kind is `system_only`; future `owner`-as-`address` / owed-id picker). No amendment, no master escalation |
| — | s5-fidelity | § m-2 section of s5's 11-Q Slice-5 fidelity packet (hub-and-spoke via master) | **✅ ANSWERED** — `../../relays/s5-fidelity/SITREP-planner-20260706-032000.md` → master. **Q1** GRILL_REQUIRED fully already-closed (both legs live @`67ee23e`; owed-#3 → empty); **Q2** exact `named_enums` delta for `routing_escalation` (A-member, MINOR, pending m-4); **Q4** migrator ruling confirmed **+ the `:126` line: Q2 add=MINOR vs Q6 `record_kind` ODB member=MAJOR** (master's Q6×Q4 reconcile crux); **Q7** `scope_paths` struck-until-Step-2; **Q9** async — `authority_class`=`bool` (correction) + `record_integrity` enum, m-3 owns sub-enum values; **Q10** async — `authority_ceiling`=`object` reserved-shape + accepts_interjection/template = m-5 config. No c1-lock reopen |
| — | s5-escalations | § m-2 of s5's escalation bundle (M-1 blocker + M-3 batch) | **✅ ANSWERED** — `../../relays/s5-escalations/SITREP-planner-20260706-053000.md` → master. **M-1 (blocks s5-a PLAN): BLESSED idiom (i)** `visible_when {layer_present:observe}` as a pure step-gate on the ~10 lane-fillable consumer rows — verified renders-off in Step-1 (render.go `renderable()`), the **only** in-grammar option (observe = sole non-Step-1 layer), annotated as step-gate proxy (not observe-owned); option (ii) does NOT return to master+VP. **M-3 (a)–(k) confirmed** — (b)/(c)/(h)/(i)/(j) are defect fixes my locked design mandates (code→contract compliance); **(e) discovered `record_kind` retyped `system_only`→`seat_scoped_enum`**, making my s4-wire fill-time-authority item actionable (recommend operator-only owed scope, m-1 confirms intent). No amendment, no c1-lock reopen |
| s6 | DESIGN-AMENDMENT | m-2 transport-codec amendment (the FIRST c1 amendment: F6/F7/F2/F13/F12) | **✅ COMPLETE + pair-approved (implementer `190100`, r2)** — doc `design/2026-07-06-s6-transport-codec-amendment.md` (DESIGN_DOC_ID `s6-amend-m-2-transport-codec`). **ONE canonical envelope codec** kills the render↔validate↔gate↔deliver divergence class: F6 silent-drop (single decode; Header list = recipient truth in EVERY projection), F7 CC-deadlock (gate decodes via codec), F2 dead-edge parent (consumes m-1 fork; dissolves under conductor-computed PARENT), F13 three-layer authorization (membership/`seat_scope`/per-kind; `genesis` seat-scope-rejected), F12 waiver bound to m-1's F17 record. **r1 must-revise → rev1 folded** (3 blockers all correct, 2 caught me contradicting my own s5 rulings). Amends c1 §2/§4/§12 via review-driven path — **no c1 fold until VP co-sign**. Completion `../../relays/s6-amend-m-2/SITREP-planner-20260706-190600.md` → master. **Held** for master integration (m-1/m-2/m-7 docs + seams) + VP co-sign; then s6 build slice. Seams declared: F2←m-1 fork, F12↔m-1 F17, `Envelope.To`/digest→m-7 |
| s6 | DESIGN-ADDENDUM (B-2) | typed BOOT form (operator-requested boot-stage leg) | **✅ COMPLETE + pair-approved (implementer `202400`, r1 clean)** — §11 of the amendment doc. **Token RULED: `PHASE: SITREP` + lifecycle-gated form, NO new `BOOT` phase** (shared-vocab untouched; no `record_kind` member = MAJOR; no §J2; no §5-atom; no seat-verb). Minimal honest set = `charter_loaded` (bool, `self_reported`) + `dispatch_status` (`{read,awaiting}`); identity = channel-stamped FROM (not a seat field). Un-bounceable by construction (A-1 digest + branch-A no-parent + codec enum). Carry-forward: any `boot_ack` marker must be system-derived/non-gate-ref/never-seat-filled. Completion `../../relays/s6-amend-m-2/SITREP-planner-20260706-203000.md` → master. Folds into the s6 integration package. Seams: B-1 lifecycle/roster→m-7, B-3 activation→m-1 |
| — | s6-fidelity-m2 | s6 BUILD design r2 reviewed vs the m-2 amendment (m-2.implementer; m-2.planner on CC) | **✅ approve-conditional for PLAN** (`frank/.relays/s6/s6-fidelity-m2/SITREP-implementer-20260707-012246.md`) — conditions F-S6-M2-1..4 (seven transport rows + boot separate; waiver render-absence via `visible_when {seat_is}` + submit-rejection; `seat_mint`/`waiver_retraction` three-layer; B-2 as written) carried into the PLAN. **Correction folded**: render-absence of free_text IS expressible via `visible_when {seat_is}` (not `seat_scope`) — my earlier "not expressible" was over-general; see memory [[frank-seat-render-scoping]]. No m-2.planner action owed (CC) |
| — | **STEP-1 CLOSED** | s6-close, `main@6a1198a`, 2026-07-08 (exit test passed live; zero F1–F17 deferred) | The whole c1 form/determinism design shipped through s1–s6; m-2's c1 lock + s6 amendment are in the shipped conductor. **Step-2 opened 2026-07-10** (observe layer activates the dormant Block A the s5 registry landed) |
| s7–s10 | step2-prep | Step-2 kickoff — 6 schema-side items routed to m-2 | **✅ CONFIRMED 6/6 + intake** — `../../relays/step2-prep/SITREP-planner-20260710-013000.md` → master (CC VP). (1) layer-activation knob → m-7 config-digest home (not `DefaultLayers()` code default); (2) `scope_paths` → DECLARE in s9 (executes my s5-Q7 seam; m-3 co-sign); (3) `OI-S6-BOUNCE-CLASS-UX` → require PHASE/CEREMONY_TIER via `{not:{phase_in:[]}}` idiom (s9); (4) `OI-S6-ENVELOPE-KEY-HYGIENE` → extend DEF-2 to envelope `system_only` (s9); (5) `8a` migrate-then-validate-at-wake → my §9 contract (before s10); (6) s5-b `(h)` channel-keyed typed-REJECT → mine, s8. **Flagged: items 4+6 = one guard family.** No c1 reopen |
| s7a | PLAN (delegated) | F-S7-R2-COLGRAIN — enforce R2 at column grain (FIRST m-2.planner-authored PLAN + delegated dispatch) | **⏳ PLAN in review** — `../../relays/s7a-dispatch/PLAN-planner-20260710-131500.md` → m-2.implementer PLAN-REVIEW. Defect: `predicate.go` `any_row` validates the array only, not the column, so `any_row:routing_assignments.chosen_model` (model-identity col) compiles — R2 violable at column grain, my §5 `:88` not enforced in code. **Mechanism RULED**: per-column validation at any_row resolution + minimal per-column gate-referenceable allowlist (**default-deny**, R2-faithful; NOT the model_identity blocklist, NOT the Step-3 nested-column schema). Red-first; fence `internal/fieldspec/` only; 1 flagged registry.json data change (allowlist `declared_deviated`). On implementer approve + `SCOPE_DIFF` all-in + no hard trigger + AC6 flag cleared → I issue the delegated dispatch token. **r1 must-revise (`131600`, mechanism approved-in-principle; 3 impl-consequence gaps) → rev1 folded** (`../../relays/s7a-dispatch/PLAN-planner-20260710-132400.md`): added `registry.go` to fence + pinned `GateReferenceableColumns []string` contract (plain Unmarshal ignores unknown keys); retargeted the existing `any_row:SCOPE_DIFF.path` positive → `declared_deviated` + byte-exact singleton assertion (shipped delta stays 1 row); reject re-specified as a registry-LOAD Go error w/ pinned substrings (not a bounce — I-PH is seat-facing). **r2: all 3 technical folds APPROVED-in-body; must-revise was a lineage-only orphan-PARENT (my invented `s7a-dispatch-review-r1`) → reissued** (`../../relays/s7a-dispatch/PLAN-planner-20260710-133000.md`, PARENT→`s7a-dispatch-review-r2`, body byte-identical). **✅ PLAN pair-APPROVED (`133100`, review-r3).** Token HELD — re-engaged master for **AC6 registry-data clearance** (`../../relays/s7a-dispatch/SITREP-planner-20260710-133600.md`): the one shipped delta `routing_assignments.gate_referenceable_columns = ["declared_deviated"]`. AC6 CLEARED by master (`133700`). **Token issue attempt `134200` was lineage-INVALID → WITHDRAWN** (my error: pair PLAN shared `DISPATCH_ID: s7a-dispatch` with the orchestrator dispatch, so `one_by_id` resolved the review's PLAN-parent to the orchestrator, not me). **Re-engaged master** (`../../relays/s7a-dispatch/SITREP-planner-20260710-134800.md`): recommend I re-issue the approved PLAN under a UNIQUE `DISPATCH_ID: s7a-plan-m2` + trivial re-approve + re-dispatch (AC6 carries); or master direct-issues. **Master ruled option 1** (`135739`): re-issue under unique `DISPATCH_ID: s7a-plan-m2` → lineage-confirming review → token on the clean chain (AC6 + atomicity carry). **Re-issued** (`../../relays/s7a-dispatch/PLAN-planner-20260710-140000.md`, `DISPATCH_ID: s7a-plan-m2`, PARENT→unique `s7a-dispatch-review-r3`). lineage-confirming review APPROVED (`140100`, `s7a-plan-m2-review-r1`). **✅ DELEGATED TOKEN ISSUED on the clean chain** — `../../relays/s7a-dispatch/DISPATCH-planner-20260710-140600.md` (`DISPATCH_ID: s7a-plan-m2-dispatch` → PARENT `s7a-plan-m2-review-r1`; file + relay-root lineage walk both lint-clean). All four conditions + AC6 met; atomicity + §9-MINOR + AC1 load-error + AC7 singleton carried. Convention master-pinned: pair-internal legs under a master thread mint unique sub-IDs. **IMPL landed** `s7a-colgrain@d76c3ad` (red `10ee3a2`→green `d76c3ad`) — all 7 ACs PASS. **✅ pair-review APPROVE** (`../../relays/s7a-dispatch/SITREP-planner-20260710-143200.md`): verified at diff-grain + independently re-ran `go test`/`go vet` green (AC1/AC3 reject test 3/3); column-grain guard + `GateReferenceableColumns` default-deny + singleton + `s6-v4→s7a-v5` version + Fold-2 retarget all faithful; no finding. Returned to master → m-4/m-7 fidelity. **VP integration surfaced VP-F1** (stale provenance: v5 registry still attributed to superseded s6 pass) → implementer folded (`s7a-colgrain@2bc0763`, red `37ac1dc`→green `2bc0763`): provenance→truthful `m-2`/`F-S7-R2-COLGRAIN`/`s7a-plan-m2` + exact-tuple `DeepEqual` test. **✅ fold pair-review APPROVE** (`../../relays/s7a-dispatch/SITREP-planner-20260710-150800.md`): diff-grain verified (only 4 provenance values, zero semantic drift, fence held) + re-ran green; **AC6 amendment ratified** (semantic singleton + VP-required provenance metadata; no further §9 version). → m-7 final-bytes re-confirm → VP re-approval → **MERGED to `main@54420dbc`** (no-ff, pushed frank-dev; post-merge battery green). **⚠ merge-authorization-trail BLOCKER (not m-2's, TO master):** the orchestrator's MERGE-GATE relay narrated the operator grant in prose but lacked a recognized authorization field → claim-aware lineage lint rejects the merge claim *post-push*; m-2.implementer disclosed + routed to master/operator disposition (no proxy repair). s7a CODE sound + merged; the defect is the authorization RECORD. m-2.planner on CC, no action owed. *(s7a lineage-integrity class demonstrated end-to-end — 3 of mine caught pre-action, this one post-action after an irreversible push; the conductor kills the whole class by construction.)* |
| s8 | DESIGN (sole author, grill) | s8 config/atom grammar — `layer_present:observe` activation semantics | **⏳ IN REVIEW** — doc `design/2026-07-10-s8-config-atom-grammar.md` (DESIGN_DOC_ID `s8-design-m2-grammar`, GRILL_LOCK `s8-m2-grammar-grill` folded); relay `../../relays/s8-design-m2-grammar/DESIGN-planner-20260710-220000.md` → m-2.implementer DESIGN-REVIEW (unique sub-ID `s8-m2-grammar-design`, s7a convention applied). **Activation invariant**: `PresentLayers` config-derived, never `DefaultLayers()` code-default; s5-dormancy→activation test mirror. **§9 member grammar**: add-member additive-MINOR + ignore-unknown; knob-flip = config-digest act not schema bump (CQ-4b axis); catalog weaken = owner-fidelity-MAJOR. **Forward-only**: pre-activation records never re-validated (CQ-1+§9 immutability). 1 operator fork surfaced (activation-authorization). Consumes m-7 `s8-design-m7-config`; no c1 reopen. **r1 must-revise (`220736`, 3 blockers + 1 human-decision, all correct) → rev1 folded** (`../../relays/s8-design-m2-grammar/DESIGN-planner-20260710-223000.md`, unique `s8-m2-grammar-design-r2`): §3 **ownership/stage matrix** (seat-rendered/conductor-observed/conductor-computed — most Block-A is system/computed, NOT seat-rendered; m-3 `observe_gate` enrichment seam added); single config-derived layer context (3 sites); stale-form **short-circuits** required-set; knob home must carry a version; catalog old-reader = **fail-OPEN** (not safe-default); activation-authority **folded** (operator-authored config_change) pending operator ratification (lock-blocking). **r2 must-revise (`223031`, 3 blockers — 2 that rev1 INTRODUCED) → rev2 folded** (`../../relays/s8-design-m2-grammar/DESIGN-planner-20260710-224500.md`, unique `s8-m2-grammar-design-r3`): (1) rev1 had silently REORDERED the locked CQ-5 pipeline (observe-before-required) → corrected to stage-split preserving form/lineage/slot/observe order + named post-observe completeness check; (2) rev1's digest-mismatch exclusion was FALSE (config.Load hashes all members) → reader-capability gate; (3) full-doc echo sweep (grep-verified this pass). Operator ratification still pending. **Honest: rev1 introduced 2 confident structural claims unverified vs locked design/code — corrective is verify-before-assert + full sweep.** **r3: 3 structural folds ACCEPTED; 1 narrow census blocker (`224600`) → rev3** (`../../relays/s8-design-m2-grammar/DESIGN-planner-20260710-225500.md`, `s8-m2-grammar-design-r4`): registry-verified census fix (−`completed_proof` not CQ-1-gated / +`bucket_binding_observed`) + fixture made **registry-derived exact-set** (drift fails mechanically). Same root cause as rev1's errors — hand-list vs query; corrected by querying the registry. **r4: census accepted; 3 activation-PARTITION blockers (`225600`) → rev4** (`../../relays/s8-design-m2-grammar/DESIGN-planner-20260710-231000.md`, `s8-m2-grammar-design-r5`, +m-5/m-6 CC): (1) form-validation must evaluate activated seat predicates (`justified_deviation`/`deviation_reason_code` conditionally required) not "only class 1"; (2) computed class partitioned by producer — `authority_class`=m-2 (§17.6:345), observe outputs=m-3, `surface_intent`=m-5/m-6 non-gate-only — **SURFACED registry-vs-§17.6 contradiction** (surface_intent gated only on observe, missing non-gate-bearing gate → proposed review-driven amendment, m-6/m-5/m-7 confirm); (3) fixture given independent producer-manifest + behavioral oracle. **Design has grown cross-domain — folding m-2-clear parts, PROPOSING (not solo-deciding) sibling contracts; needs m-3/m-5/m-6/m-7 confirms + operator ratification.** 4 revs, each real; root = assert-before-verify, corrective = verify-vs-source + route-don't-guess. **r5 (`231100`): partition direction accepted, but Blocker-3 = hard DEPENDENCY (can't approve until m-3/m-5/m-6/m-7 confirm).** Swept the r5 echo (per-producer sequence, grep-clean) → then **broke the 5-round solo loop: routed the cross-domain close to master** (`../../relays/s8-design-m2-grammar/SITREP-planner-20260710-231800.md`) — the 4 sibling confirms + the `surface_intent` A/B contract choice (Opt B recommended: remove mis-scoped registry predicate, move profile to m-5/m-6 manifest) + operator ratification. Next rev = the RECONCILING one after inputs, not another solo guess. **s8 grammar HELD pending master-orchestrated multi-party close.** **Confirms landing:** ✅ **m-7** (`../../relays/s8-grammar-confirm-m7/SITREP-planner-20260710-234643.md`) — `authority_class`=step-3 classify (post-gate/pre-observe, non-lane-writable at tool surface) ✓; profile-aware completeness=**step 4.5** (post-observe/pre-append, FORCED by locked order) ✓ + 4 hosting constraints (pure/deterministic; single-pivot terminal reject; read-only; **profile determination is GRAMMAR/m-2's, engine guarantees only order/purity/single-outcome** → supports surface_intent Option B); §3 step-4.5 addition is an AMENDMENT riding `s8-design-m7-config` (m-7-owned, not silent). **ALL 4 confirms in** (master close-out `235320`): m-3 manifest byte-faithful ✓ + m-7 placements ✓ + **m-5/m-6 unanimous Option B** ✓. **✅ RECONCILING rev folded** (`../../relays/s8-design-m2-grammar/DESIGN-planner-20260711-001500.md`, `s8-m2-grammar-design-r6`) → **final DESIGN-REVIEW**: new §8 quotes the 4 owners verbatim; surface_intent Option B (registry amendment: remove global predicate → m-5/m-6 manifest-at-step-4.5, `progress ⇐ otherwise`); m-3 precision notes + m-7 step-4.5/4-constraints + s5-③ tripwire folded. §3 step-4.5 amendment rides `s8-design-m7-config`. Pending at lock: operator ratification (activation-authz + m-3 grill defaults). **6 DESIGN passes; root=assert-before-verify; closed by routing contracts to owners. Surfaced a real surface_intent registry bug.** **r6: reconciliation (§8) accepted; 2 consistency blockers (`001600`) = echo-gap AGAIN (primary sequence/fixture still had superseded global-predicate model) → rev6 CONSISTENCY-ONLY sweep** (`../../relays/s8-design-m2-grammar/DESIGN-planner-20260711-002200.md`, `s8-m2-grammar-design-r7`): numbered rule → confirmed step-3(`authority_class`)/step-4(m-3 manifest)/step-4.5(Option-B `surface_intent` derive); fixture → 3 independent expectations (removed the impossible registry=producer equality); matrix/seam/§4 swept; **grep-verified whole-doc this time**. No owner decision changed. **r7: primary sequence + fixture PASS; 1 final lock-summary echo + a pre-commit noun (`002300`) → rev7 minimal fix** (`../../relays/s8-design-m2-grammar/DESIGN-planner-20260711-002900.md`, `s8-m2-grammar-design-r8`): `Design-lock impact`→confirmed 4-owner step-3/4/4.5 contract; fixture `Actual`→"in-courier candidate" (pre-commit). Grep-verified. **✅ TECHNICALLY APPROVED** (m-2.implementer `003000`, r8; 8 DESIGN passes). Completion returned to master (`../../relays/s8-design-m2-grammar/SITREP-planner-20260711-003600.md`). **HELD for the reconciled lock** (not created by the approve): (1) two operator legs on record — activation-authorization ratification + m-3's grill defaults; (2) master reconcile with `s8-design-m7-config` (carries the §3 step-4.5 amendment). Then the s8 PLAN. *(Arc: 8 passes, root = assert-before-verify then echo-gap; correctives now habit — route to owners, verify vs source, grep whole-doc. Surfaced a real `surface_intent` registry bug.)* |
| s8 | GRAMMAR-CONFIRM (adoption) | m-7's `s8-design-m7-config` bounded ask: the multi-member `config_change` bootstrap variant | **✅ CONFIRMED** — `../../relays/s8-adoption-grammar-m2/SITREP-planner-20260711-135623.md` (reply to m-7 `134654`; `DISPATCH_ID s8-adoption-grammar-m2-confirm`) → master reconcile-A (CC m-7 pair, VP, operator). Both asks CONFIRMED, grammar basis my §2/§9. **Ask 1 — `member: adoption`**: CONFIRM as an **overload of the existing body-shape discriminator** (`member` already selects body interpretation, `submit.go:340-353` — one axis, not a second `change_shape` flag/GL-4). Three rulings: (a) **reserved token, NEVER a member name** — structurally safe because the load-set is the hardcoded `StoreRootConfigPaths` map `{engine,fieldspec}` (`genesis.go:86-91`), NOT the `config_member` enum_set, so the token can't leak into the loadable set; (b) **two m-2-owned registry byte-sites** (`registry.json:85` enum_set + `:136` `seat_scope.operator`), a review-driven amendment riding the s8 registry build; (c) **version class = MAJOR** (§9 exhaustive-switch widening at `submit.go:341` + fill-gate), **fail-CLOSED** (pre-adoption reader typed-rejects `member/enum`), **no migrator**, forward-only — the fail-CLOSED counterpart to the catalog's fail-OPEN MAJOR-governance. **Ask 2 — closed body**: CONFIRM shape + ONE correction — **`new_digest` stays a HEADER** (`registry.json:137`; `submit.go:344` reads `cand.Headers`), not in the body (m-7's JSON contradicted their own "singular position" prose); body carries only `{"members":[{name,bytes_b64}]}`. Name-sort = grammar REQUIREMENT (deterministic `Digest()` name-sorts, `config.go:75`); enumerated malformed→typed-reject surface (dup names, `name∉{fieldspec,engine}`, bad b64, empty members, …). **NOT ruled (m-7's arm):** interpreter/`IntentConfig`/recovery/bless/absent→initial/member-count. Does NOT reopen the technically-approved `s8-design-m2-grammar`; if the design-of-record carries it, rides as a bounded §2/§9 amendment on master's direction. **✅ master CONSUMED + RULED** (`../../relays/s8-adoption-grammar-m2/SITREP-orchestrator-planner-20260711-140516.md`): all three rulings + the `new_digest`-header correction held as reconcile-A input beside the r10 review; fold-discipline ruled as I recommended — **NO self-reopen now**; the bounded §2/§9 amendment lands as ONE review-driven changeset riding the s8 registry build (same changeset as the two registry byte-sites — text+bytes reviewed at one moment, the surface_intent pattern). Five items bound on the **m-7 pair** at reconcile-A completion (their arm, via CC): §5.1 body-form sweep, the MAJOR-but-safe §9 statement, the reserved-token+malformed fixtures, an explicit member-count policy, wrapper-key consistency. m-2.planner **HOLDS** — next actors are the m-7 pair (sweep + completion SITREP) + master (reconcile-A completion → reconciled locks + PROCEED → s8 PLAN). **↻ m-7 SUPERSEDED the ask (r11, `…-141455`) → RE-CONFIRMED** (`../../relays/s8-adoption-grammar-m2/SITREP-planner-20260711-142347.md`, `DISPATCH_ID s8-adoption-grammar-m2-confirm-r2`; reconcile-A consumes THIS against r11, not `…-135623`). Two changes: (1) **`new_digest`-header = CONVERGENT** — their r10-F6 caught exactly my independent correction (chain walker `ExpectedConfigDigest` reads headers, `genesis.go:125-140`); body is members-only; CONFIRM. (2) **closed body `{catalog, engine}`** (was assumed `{fieldspec, engine}` — my error owned; the cross-boundary set is catalog=convention→pinned §2.2 + engine=knob-home §2.1) — order `[catalog,engine]` = canonical name-sort ✓ (mandated for digest consistency, `config.go:75`); cardinality-2/unique/canonical-b64/typed-reject CONFIRMED. Three prior rulings carry. **NEW FLAG routed to m-7 (not solo):** catalog-as-pinned-member ⇒ is its post-adoption mutation a singular `config_change` (⇒ `catalog` ALSO joins `member` enum `:85`+`:136`+`submit.go:341` — 2nd byte-add) or a separate governance mechanism? Sizes the amendment; NOT a blocker to the body confirm |
| s8 | PLAN CONSULT (SEQ-1) | m-3.planner's s8 observe-spine PLAN — SEQ-1 order-of-record consult (m-2 piece: the tokens + the fieldspec transition) | **✅ CONFIRMED, no correction** — `../../relays/s8-plan-m3-seq1/SITREP-planner-20260711-153500.md` (`DISPATCH_ID s8-plan-m3-seq1-m2-confirm`) → m-3.planner (CC master, m-7 pair, operator). *(Context: s8 reconciled locks + PROCEED landed — m-2 activation-authz ratified `…-025643`, m-7 config-host r13 approved; s8 PLAN dispatch `s8-dispatch/…-145129` issued; PLANs now bundling.)* **Confirm 1 — tokens in the step-2 changeset**: BOTH `adoption` + `catalog` at `:85`/`:136` + surface_intent Option-B removal = ONE m-2-reviewed changeset (my routed catalog-flag RESOLVED by m-7 r13 §5.2 ruling (a): singular `member: catalog`, state-aware, same MAJOR moment). Affirmed: pinned-set membership ≠ `member`-enum vocab (catalog pinned at genesis before submittable); genesis→step-2 window is fail-CLOSED. **Confirm 2 — `v5→successor` lawful FX-CFG-10 forward transition** (m-2-supplied ordered successor map, r8/F3): not rejected-type/rollback/skip; I own the `s7a-fieldspec-v5 → s8-fieldspec-v6` map entry, folded into the changeset. **Named a grammar-REQUIRED ordering** (changeset BEFORE observe-activation — else the old `surface_intent` static predicate briefly fires; Option-B removal must precede observe flip; m-3's order already has this right). Scope: m-7 pieces (genesis pin, bless fill-gate) affirmed non-conflicting, not mine to confirm. m-2.planner holds |
| — | CROSS-CUTTING (scribed) | confusion-firewall directive (operator-set, m-2.planner surfaced) | **✅ RATIFIED 2026-07-11 → standing rail in `ARCHITECTURE.md`** (home of record; not m-2-owned). I surfaced it (`../../relays/frank-threat-model-scope/SITREP-planner-20260711-162331.md` → master, report-only); master assessed + adopted (`…-162826`), operator ratified. **Three parts**: threat = confusion/hallucination not malice; evidence = deterministic checks + independent cheap-model adjudication + honest labeling; architecture = extensible seams, no home-grown security. **Master added two rails (sharper than my draft):** Rail A = per-surface open-vs-closed (fail-CLOSED where ignore-unknown changes acceptance meaning — cites my knob-vs-catalog line as the model); Rail B = cut by FUNCTION not flavor. **Honest correction I own:** my draft recommended cutting three s8-config items as "adversarial ceremony"; Rail B's function-analysis showed they are confusion-FUNCTIONAL (composite digest = drift-detection; adoption atomicity = crash-safety + history-truth; A2 gate = kills confidently-wrong half-enforcement) and correctly **STAY** — my mis-classification (assert-before-function-analysis), caught by the rails I asked for. **s8 effect:** stays as dispatched (deterministic E1+E2); the adjudication rung lands in **s9** (a cloud judge = external send, behind the egress fence), not the s8 spine; the "fresh-store-first" steer sharpens the SEQ-1 default I confirmed (`…-153500`) — that confirm stands. CC-only, no ack owed; m-2.planner holds |
| s8 | BUILD-FIDELITY (T2 bytes) | s8 build team requested the reviewed v5→v6 registry changeset (sole-writer boundary — implementer won't touch registry.json) | **✅ DELIVERED** — `../../relays/s8-build-fidelity-m2/COORD-planner-20260711-201500.md` → s8.implementer (CC s8.planner/reviewer, master, m-7, operator). The scheduled m-2 amendment is now **actual bytes**. Base SHA re-verified `1ef6abab…` (exact v5). **4-site delta**: version `s7a-fieldspec-v5`→**`s8-fieldspec-v6`** (my successor-map entry); `config_member` enum + `member.seat_scope.operator` both gain `["catalog","adoption"]`; `surface_intent` loses its static `required_when`/`visible_when` (Option B → step-4.5 manifest), annotation swept. Rails A (config_member closed — exhaustive switch) + B (pass, deterministic) confirmed. **Downstream test byte flagged** (`registry_test.go:18` expected→v6). **Cross-arm note**: registry tokens alone don't make catalog/adoption submittable — the `submit.go:341` switch + adoption interpreter (m-7/executor arm) must land same T2; interim = fail-CLOSED safe. No frank/ edit by me (I author+review bytes, implementer applies, reviewer verifies, operator merges). m-2.planner holds |
| s8 | DESIGN-AMENDMENT (leg) | `s8-claim-input-amendment` — m-2 leg: the FieldSpec home for the executable-claim DECLARATION surface (T9 production RED = reader-with-no-writer) | **⏳ IN REVIEW** — doc `design/2026-07-12-s8-claim-input-fieldspec-home.md` (`s8-claim-input-m2-home`); relay `../../relays/s8-claim-input-amendment/DESIGN-planner-20260712-005000.md` → m-2.implementer DESIGN-REVIEW (sub-ID `s8-claim-input-m2-design`). Three-owner amendment (m-3 semantics first-among-equals; m-7 seam); **my bytes finalize against m-3's return**. **Home (fixed, m-2-owned):** `executable_claims` seat-declared `row_array` (counterpart to system-owned `executable_claim_results:151` — two sides of the (h)-guard); `seat_scoped_enum`; **`gate_referenceable:false`/R2** (whole row, stricter than routing_assignments); `visible_when {layer_present:observe}`, **NO unconditional required_when** (Rail A additive — absence = honest no-vantage degrade, `Evaluate:nil` preserved); **additive-MINOR `v6→v7`** transition (OPEN/additive surface, contrast T2's CLOSED config_member). **Flagged for m-3 (not guessed):** exact columns/cardinality + EVIDENCE_TARGET coupling (leg a); check_id validation locus/enum_set (leg b). Tripwire honored (no lock-pinned value moves — incl. the catalog `943f07bb…` pin, not this thread's); scope = declaration surface only (no adjudication rung = s9 item 9). Grounded vs registry (`:151`/`:174`/`:182`/`:107`), not asserted. **r1 must-revise (`005100`, 2 blockers — both verified correct at source) → rev1 folded** (`../../relays/s8-claim-input-amendment/DESIGN-planner-20260712-005600.md`, `s8-claim-input-m2-design-r2`): **B1** — my `seat_scoped_enum` claim to constrain nested `check_id` was FALSE (row_array columns have no grammar enforcement — `canonical.go:20-30`/`validate.go:54-62`, exactly my own s7a finding); folded → seat-authored carrier (`agent_enum_pick`/`free_text`), nested validation delegated to m-3's validator seam (rejected inventing a nested-column grammar = speculative machinery). **B2** — my Rail-A-additive/MINOR class was FALSE: a PRESENT declaration ignored by a v6 reader falls to `Evaluate:nil` and ACCEPTS what v7 REJECTS ⇒ changes acceptance ⇒ Rail A **CLOSED/fail-closed**; reclassified + **v6→v7 version class ROUTED TO MASTER** for reconciliation (parent Rail-A-additive contradicted by both m-2 + m-3 F1; needs m-7 capability gate). **Both blockers = one over-claim (assert-before-verify), verified at source this pass; pair + m-3 caught them.** Finalized home does NOT return to master until reconciliation + m-3 semantics. **r2: both substantive folds ACCEPTED; 2 residual echoes (`005700`, consistency-only) → rev2 swept** (`../../relays/s8-claim-input-amendment/DESIGN-planner-20260712-010200.md`, `s8-claim-input-m2-design-r3`): §4 byte-site 3 no longer offers a nested `check_id` enum_set (→ m-3 validator seam); §6 leg (e) split to ABSENCE-degrade/PRESENT-fail-closed; leg (b) de-enum'd; **whole-doc grep-swept** (my recurring echo-gap — done this pass, not deferred). Version class stays master-held. **✅ GRAMMAR-APPROVED** (m-2.implementer `010300`, rev2; 3 DESIGN passes). Completion returned to master (`../../relays/s8-claim-input-amendment/SITREP-planner-20260712-010800.md`). **HELD for finalization** (not created by the approve): (1) **master corrects the parent "Rail-A additive" line** — both m-2 + m-3 F1 reached present-declaration=CLOSED/fail-closed, so the parent dispatch's own instruction was wrong; (2) m-7 confirms the marker-first v7 refusal/capability gate; (3) m-2 finalizes byte-exact v7 class/bytes vs approved m-3 semantics + m-7 return → s8 build (T2 pattern). **m-3 r3 semantics pair-approved (`016200`) — that gate satisfied.** *(Arc: 3 passes — 2 real blockers rev1, 2 echoes rev2; assert-before-verify + echo-gap, both narrowing; surfaced win = the parent amendment's own Rail-A line was wrong, both owner reviews caught it.)* **✅ GATE 2 (m-7 capability) SATISFIED** — m-7 leg r2 approved (`s8-claim-input-m7/DESIGN-REVIEW-implementer-20260711-232825`, CC me): reader capability set = exactly `{v5, v6, v7}`; **raw-byte marker preflight in `config.Load`** (phase-0 refusal, zero partial interpretation) = my §3 refusal proof; `schema(V)` + ordered forward chain (v6→v7 lawful, rollback/skip reject) = my forward-relation. **Two of three finalization gates now satisfied** (m-3 semantics ✓, m-7 capability ✓); remaining = **master's parent-Rail-A correction + the authorized-path fold**. *(Ordering note: my pair's dependency boundary put master's correction BEFORE m-2 finalization; the m-7 seat recommends m-2-finalize-then-master-fold — master resolves the ordering. I also need to READ m-3's approved semantics before finalizing the columns.)* **✅ ALL 3 GATES SATISFIED + FINALIZED** — master closed my hold (`…-020020`, validating hold-not-barrel as correct: the missing input was a master transport gap, owned): gate 1 Rail-A ratified ABSENCE-OPEN/PRESENT-CLOSED (`…-020010` §1, master owns the parent error); gate 2 m-7 capability; gate 3 m-3 §12 read. **Finalized byte delta** `../../relays/s8-claim-input-amendment/COORD-planner-20260712-021000.md` → master (3-leg fold) + s8 build (T2 pattern): 2 sites vs v6 base — version `v6→v7` + INSERT `executable_claims` row (`agent_enum_pick`/`row_array`/`gate_referenceable:false`/`visible_when observe`/no required_when=ABSENCE-OPEN; columns `(claim_ref[unique],check_id,params)` validated at m-3 seam, no FieldSpec enum; PRESENT-CLOSED carried by v6→v7 marker + m-7 capability ceiling, not a byte). TRIPWIRE honored. Doc §9 records it. m-2.planner holds for master's fold + T9 grant |
| s8 | AMENDMENT CONFIRM (m-2 leg) | `s8-supply-set-amendment` — the config-side reader-with-no-writer class (suite descriptors / lane-roots / schema-ref table); m-2 = the CONFIRM one-liner | **✅ CONFIRMED — no fieldspec impact** (`../../relays/s8-supply-set-amendment/SITREP-planner-20260712-025000.md`, `s8-supply-set-m2` → master). **Verified, not rubber-stamped:** grep of `registry.json` — NONE of {schema_ref, run-suite, suite, evidence_root/lane_root, target, descriptor} is a FieldSpec field; the supply set is config-side (m-7 engine member `v1→2`) + check-side (m-3 check-registry seam). Condition (b): `run-suite.target` = a **CHECK-registry-face enum**, NOT a FieldSpec `enum_set` — the exact boundary the finalized `s8-claim-input` work established (nested check params validate at the m-3 seam, ride the existing `executable_claims` row, no new registry field). Home-choice-independent (any config home = no fieldspec byte). TRIPWIRE: `{v5,v6,v7}` set + the v7 delta untouched. Conditional hook: any surprise registry byte routes §2/§9 + would be Rail-A CLOSED (per the corrected lesson). m-2.planner holds |
| s10 | BUILD-FIDELITY (T1 bytes) | s10 comms-spine (m-6) build: the v7→v8 registry transition — 2 conductor-authored record_kind tokens | **✅ DELIVERED** — `../../relays/s10-build-fidelity-m2/COORD-planner-20260712-233000.md` → s10.planner (CC master, m-6, m-7, build). Items 1–6: **tokens** `odb`+`resummon_command` (enum_set line 84); **marker** `s8-fieldspec-v7`→**`s10-fieldspec-v8`**; **successor-relation** `config.go validateFieldspecMarkerTransition` +`v7→v8` (+ `registry_test.go` pin); **SEQ-2 CONFIRMED** (fresh-v8 genesis rides §7-apply/bless, born-at-v8, PARK-ACROSS-V8 vacuous). **Rail A CLOSED/MAJOR-but-safe** (interpreter-bearing, marker-first). **One reasoned deviation flagged to m-6:** **seat_scope = EMPTY** (record_kind row UNCHANGED) — the plan cited seat_mint/gate_resolution but those are operator-SUBMITTED; odb/resummon are conductor-GENERATED, so the correct precedent is **genesis** (in-enum, empty seat_scope = un-forgeable). **Per-kind required_when = NONE at T1** (conductor-emit ⇒ un-submittable ⇒ submit-time required_when moot; content contract rides T3/T8; avoids collision with ODB-cluster observe-gating). TRIPWIRE honored ({v5,v6,v7}+v7-delta untouched; v8 rides this entry + m-7's capability move). **✅ T1 LANDED + CLOSED** — build seat report (`s10-build-t1-review/…-002343`,`…-003536`): owner bytes **verbatim**, T1 RED→GREEN, **pinned v5 byte-identical + FX-CFG-7 untouched** (TRIPWIRE held), v8→v7 rollback-inverse correct. **m-6 RATIFIED both flagged calls, no correction** (`s10-build-fidelity-m6/…-002109`): seat_scope EMPTY = c3-faithful genesis pattern + more forge-proof (plan's seat_mint/gate_resolution = loose shorthand); emit-only confirmed (a predicate on un-submittable tokens = dead/performative bytes). T5 operator verdict = separate submittable `gate_resolution`, untouched. 7 legacy-fixture legs correctly T2-held (genesis.go v8-derivation, not my bytes). Terminal m-2 close: `../../relays/s10-build-fidelity-m2/SITREP-planner-20260713-010000.md` (aligned w/ operator's 2026-07-13 batched-review cadence). m-2.planner holds — on call for any T3–T10 registry byte (condition b; none expected). **✅ SLICE-COMPLETE E2 CONFIRM** (batched end-of-slice per operator cadence): all 3 m-2 cells CONFIRMED against `s10-comms-spine@9722744` — `../../relays/s10-build-owner-confirms/SITREP-planner-20260713-030500.md` → s10.planner. **Verified independently via `git show 9722744`** (not on pointers): Cell 1 items 1/4/5 byte-exact + seat_scope EMPTY + no required_when; Cell 2 v5 SHA `1ef6abab…` **byte-identical** (TRIPWIRE held, byte-count-guarded reconstruction) + T1-g predecessor arm; Cell 3 born-at-v8. No contradiction; MF-1 `expiry.go` fold not on any m-2 byte. *(timestamp collided w/ m-3's confirm → unique sub-ID `-m2`.)* On all-3-owner confirms + MF-1 report, s10.planner authors the merge-decision → operator (HUMAN_MERGE_AUTHORIZATION). m-2.planner holds |
| s9 | DESIGN CO-SIGN (m-2 leg) | `s9-scopepaths-cosign` — the `scope_paths` FOUR-PIN co-sign (VP F1 s9 PLAN BLOCKER): m-2 authors the declaration-slot grammar, m-3 the predicate, m-1 the fidelity | **⏳ IN REVIEW** — doc `design/2026-07-13-s9-scopepaths-declaration-slot.md` (DESIGN_DOC_ID `s9-scopepaths-cosign`, m-2 half); relay `../../relays/s9-scopepaths-m2/DESIGN-planner-20260713-140344.md` → m-2.implementer DESIGN-REVIEW (sub-ID `s9-scopepaths-m2`, CC m-3.planner + m-1.planner). **m-2 slot (owned):** `scope_paths` = `header`/`owner:agent_enum_pick`/`type:row_array`/`gate_referenceable:false`/`lineage_role:none`, `visible_when` render-scoped to `PHASE:PLAN`+grant-bearing dispatch; column proposal single `path` (co-signable w/ m-3). **Pin (a):** canonical value = the **nearest accepted PLAN/dispatch ancestor's declaration** (never the work-record's own) — home half via the render-site restriction; m-3 owns which ancestor kinds declare. **Pin (b):** resolution rides `PARENT_DISPATCH_ID` (`system_only`/`parent_edge`, `registry.json:96`) via the conductor `parents()` walk (`lineage.go:254-262`), nearest-accepted-ancestor stopping rule (m-3/conductor); **m-1 fidelity leg requested** — the walk consumes the conductor edge only, lane-suppliable `parent_hint` (`:97`, gated by computed `parent_hint_honored:98`) never substitutes. **Pin (c):** a `scope_paths` on a non-declaration record ⇒ typed reject `Class: scope-self-declared` (s5-b (h) suppliability family) — a **joint byte** (m-2 registry marker + m-3 site-computation). **HONESTY RAIL (owned):** this reject is a **NAMED s9 build task, NOT a v8 property** — `seat-scope` validation fires only for `type:enum|bool` (`validate.go:55-62`, value-keyed) so it can't gate a free-form `row_array`'s presence, and `visible_when` is render-absence not submit-rejection (`render.go:96-108`); no non-lane-writability claim before the rule is green (deliberately not repeating the s8 assert-before-verify error). **Pin (d):** Rail A — no declaring ancestor ⇒ honest no-vantage, **never silent green** (missing scope masquerading as in-scope changes acceptance meaning); two candidates ⇒ nearest-ancestor stop, ties fail-closed; block-vs-label E-rung left to m-3. **RED-first self-widen NF named** (VP exit-set): IMPL work-record self-supplying wider `scope_paths` → (i) rejected `scope-self-declared`, (ii) ⊆ resolves ancestor value, `pkg/b` caught. Grounded at `s10-close@39474d0`, cites inline. `diff_paths ⊆ scope_paths` stays STRUCK (m-3-F7) until m-2+m-3+m-1 all sign. **⏳ rev0 must-revise (`s9-scopepaths-m2/DESIGN-REVIEW-implementer-20260713-142300`, 4 blockers — all verified correct at source) → rev1 FOLDED** (`../../relays/s9-scopepaths-m2/DESIGN-planner-20260713-143000.md`; doc rev1 fold-log at top). Both pair reviews (mine + m-3's) flagged the SAME seams — co-sign working. **MR-1** render-effective fix: `visible_when {phase_in:[PLAN]}` (old `grant`-branch was dead — `render.go:51-54` seeds visibility eval with only PHASE/CEREMONY_TIER; grant-bearing dispatch relays ARE PHASE:PLAN). **MR-4** honesty extended (same assert-before-verify trap as s8-claim-input, owned): `resolve_scope` walk named as RED-first BUILD TASK not v8-property (verified: NO resolver in v8, `lineage.parents()` one-level `:254-262`; substrate = `stampParent`/`PARENT_DISPATCH_ID`). **MR-2/3** dropped `scope-self-declared` → **one canonical class `scope-self-widen-refused`** (m-3's, adopted); submit-layer = render-legality suppliability guard = defense-in-depth build task, not a competing class (m-3's reviewer independently corrected m-3 for overstating the s5-b(h) guard — both halves now agree it's future work). **MR-3** dropped glob → **canonical prefix-subset** over normalized lane-root-relative entries (segment-boundary match, no `..`/leading-`/`); owner split explicit: m-2 normalizes-at-declaration (fixture `NF-scope-malformed-entry`), m-3 matches; nested-narrowing enforced at m-3 observe layer (not m-2 submit). **m-3-MR-4** I-PH: self-widen NF now path-redacted. Every fold verified at source. **⏳ rev1 must-revise (`…-143616`, 3 open + MR-4 CLOSED) → rev2 FOLDED + CONVERGED to live m-3 rev1** (`../../relays/s9-scopepaths-m2/DESIGN-planner-20260713-144600.md`). The two halves had CROSSED (revised in parallel, diverged on grammar/classes/locus/home); rev2 converges to m-3 rev1 (`s9-scopepaths-m3/…-152000`, m-3 owns reader-side semantics). **MR-1 home was WRONG in rev1 (owned):** verified pair-planner grant dispatch is **PHASE:IMPL** + grant:dispatch-impl (`lineage_test.go:176-178`, grant renders on IMPL form `render_test.go:43-53`) — my "PHASE:PLAN" came from reading governance relays not frank runtime; fix → `visible_when {role_in:[planner,orchestrator-planner]}` (render-stable via `ctx.Seat.Role` `predicate.go:333-334`; covers PLAN + IMPL grant-dispatch, excludes implementer work forms); enforced legal-site = grant-aware **submit** guard (grant IS avail at validate, unlike render). VP pin kept whole (PLAN+dispatch), no narrowing. **MR-2** two named classes adopted (m-3's): `scope-self-declared` (Layer-B submit, work-record self-supply = the class rev1 dropped) + `scope-nested-widen` (declaration-time). **MR-3** dropped my prefix-subset → m-3's **bounded glob dialect** (`*`/`**`, no brace/class/negation, unknown metachar=parse refusal); split m-2 parses-at-declaration (`NF-scope-malformed-glob`) / m-3 matches; narrowing moved to **declaration-time** (my observe-layer choice + "submit ancestor unresolvable" objection were wrong — chain committed before child submits). I-PH class+count only. rev2 now byte-consistent w/ m-3 rev1 §G/§C/§D/§H → m-2.impl re-review + m-3 pair + m-1 fidelity. **✅ MASTER-SETTLED + CO-SIGNED (rev3)** — master RECONCILE (`s9-scopepaths-cosign/RECONCILE-orchestrator-planner-20260713-160510`) RULED the two open divergences + authorized a joint `s9-scopepaths-converge` relay; master's 3 rulings REVERSED several rev2 choices (verified at source): **MR-1 = Option A PLAN-ONLY** (pin narrowed as a CORRECTION — scope is a PLAN property, grant-dispatch declaring diff scope = duplicate-axis smell; grant-site = non-welded Rail-A future door) → my home reverts `role_in`→**`visible_when {phase_in:[PLAN]}`** (render-effective); **grammar = segment-prefix BLESSED** (my leg; m-3 withdrew glob) → reverts rev2 glob; **narrowing-locus = OBSERVE** (m-3 accepted) → reverts rev2 declaration-time. **The one open byte was MINE** (master steered granularity = m-2's grammar call): **FIXED = TWO tokens layer-split** — `scope-self-declared` (submit `Violation`, non-PLAN self-supply) + `scope-exceeded` (observe ⊆ veto, merges plan-widens-ancestor+diff-drift under master's ONE invariant, path-free reason enum). Reasoned OVER m-3's one-token rec on **signal-shape determinism** (submit Violation vs observe predicate disposition = structurally different fields; cond-i is caught/ignored before observe so could never fire an observe reason). **Co-signed all 4 axes** (`../../relays/s9-scopepaths-converge/DESIGN-planner-20260713-153200.md` → m-3.planner). Leg rev3 records the settled contract + reverts rev2's superseded choices (master-ruled, not my re-decisions). → m-3 folds settled byte into co-sign doc, m-3.impl re-review, m-1 fidelity → co-signed return to master unblocks B1. Eval STRUCK until item-10. **m-2 co-sign COMPLETE — m-2.planner holds** |
| s11 | DESIGN CO-SIGN (m-2 leg) | `s11-8a-joint-review` — the 8a parked-gate-across-a-schema-bump contract (kickoff item 5); m-2 = members 1 (`stale_schema` token) & 2 (frozen-choice/migration); m-6.impl = member 3 (re-issue branch) | **✅ m-2 LEG DESIGN-COMPLETE + pair-APPROVED (members 1&2, E1)** — `s11-8a-m2-design-review-r4` (`…-032110`); completion returned to master (`../../relays/s11-8a-m2/SITREP-planner-20260714-032500.md`). Design-of-record rev3 `design/2026-07-14-s11-8a-frozen-choice-migration.md`. **4-round arc, each a real defect caught at running code:** rev0→1 MR-1 principle-without-mechanism (→ π+guard); rev1→2 MR-1 guard-off-live-path + MR-2 held/rejected-conflated (→ live classifyVerdict path + three byte-distinct records); rev2→3 MR-3 Go map-aliasing bypass (→ snapshot-before-Apply + Apply deep-clone). **REMAINING JOINT (not m-2):** m-6.impl member 3 + m-6-owned bucket-D reason token / durable-intent-vs-same-outcome → master integrates 3-member contract → s11 T6 locks 8a. m-2.planner HOLDS. doc `design/2026-07-14-s11-8a-frozen-choice-migration.md` (DESIGN_DOC_ID `s11-8a-joint-review`); relay `../../relays/s11-8a-m2/DESIGN-planner-20260714-024400.md` → m-2.implementer DESIGN-REVIEW (sub-ID `s11-8a-m2`, CC m-6.impl+m-6.planner). On the co-signed FLOOR (migrate-then-validate; un-migratable → held/escalated, never dropped). **Member 1 (`stale_schema`):** home = system-stamped `failing_edge` reason on a **HELD** record = the **3rd held-producer** (wake-migration-failure: `migrate.Apply` ErrMigrationGap/ErrUnversioned/ErrFutureVersion `migrate.go:58-68` OR post-migration `Validate` fail); **NOT a 4th delivery_state** (byte-locked at 3, `:71`); **NOT a new enum_set** (`failing_edge :113` = system/text free-form; forge vector closed by system_only). Version class = **additive-MINOR/Rail-A OPEN** (no exhaustive consumer on the reason — held/escalate keys on `delivery_state==held`, reason is operator-display; unknown reason still degrades to generic held-escalate, never silent accept). **Member 2 (frozen-choice/migration):** migrator MAY upgrade `choices` (`:183`) **STRUCTURE**, MUST NOT alter decision-bearing **CONTENT** (frozen set = immutable record of what was offered); operator resolution validates vs the **FROZEN offered set, not the live registry**; a bump changing the offered set is surfaced (→ held+stale_schema or member-3 re-issue), **never silently applied**; additive widen ≠ retroactive widen (new options → new gates only). **v1 honesty rail:** zero migrators registered at `d91fcfb` (`migrate.go:11` Current=1, empty steps) — both members are the contract the first bump activates, RED-first-testable, **not extant** (same rail as §9). **Member 3 grounded for m-6.impl:** content-change ⇒ new decision ⇒ new gate (new resummon key), never in-place edit. Grounded at `s9-close@d91fcfb`, cites inline. **⏳ rev0 review: member 1 APPROVED; member 2 must-revise (`s11-8a-m2/DESIGN-REVIEW-implementer-20260714-024801`, MR-1: "semantic-MUST-NOT" was a principle w/o a mechanism) → rev1 FOLDED** (`../../relays/s11-8a-m2/DESIGN-planner-20260714-025800.md`). Reviewer right — bound the executable boundary: **(1) canonical decision projection `π(choices)={value→label}`** (order-independent — reorder legal; representational columns excluded by construction; label decision-bearing = relabel is a re-meaning); **(2) decision-projection-invariance guard** at the ODB wake read path (`migrate.Reader.Read`, both source+migrated in hand) — `π(source)==π(migrated)` catches a schema-valid CHANGED decision that current-form validate cannot see (`migrate.go:19` arbitrary transform, `submit.go:527-552` resolves stored ODB, `odb.go:113-135` strict `{value,label}` decoder); **(3) ONE deterministic signal** — boundary emits only `held`+`stale_schema` on any non-preservation (Apply fails / guard fails / validate fails); **re-issue NEVER a migration side-effect** — it's a downstream authorized member-3 act (auto-re-issue would silently substitute the operator's frozen decision); **(4) RED/GREEN synthetic v1→v2 fixtures** (add/remove/rename/relabel/replace/gap→held; reorder+structural-column→green; each proves source-bytes-immutable + frozen-π resolution). Member 1 untouched. Flagged m-6: confirm `label` decision-bearing for re-issue. **⏳ rev1 must-revise (`…-030153`, 2 integration blockers) → rev2 FOLDED** (`../../relays/s11-8a-m2/DESIGN-planner-20260714-031000.md`). **MR-1 (guard off the live path, owned):** verified the guard was on `migrate.Reader.Read` (the `read` tool, `main.go:459-467`) but operator resolution uses a **raw un-migrated** `tables.Build` table (`tables.go:110-125`, no migrator) + reads the ODB directly (`submit.go:60,146,527-552`) — bypassed. Fix → guard moved onto the **live `classifyVerdict` path**: keyed on **immutable SOURCE odb identity** (kind-change can't bypass), guarded `migrate.Apply`, **fail-closed either-side parse**, validate pick vs **migrated** view; end-to-end `SubmitHandler` wake fixtures. **MR-2 (held/rejected conflated + re-issue loosened, owned):** read m-6.impl approved branch (`024043` pts 1-3) + m-6 terminal map (`rejected`→bucket-D author-return, `held`→bucket-A fault, byte-distinct). Fix → **three byte-distinct records**: stale reply candidate=`rejected`/no-wake (D) · migration-fault=`held`+`stale_schema` (A) · replacement=member-3 new-decision-identity; **atomic-or-durable coupling preserved** (m-6.impl pt3; rev1's "optional/downstream" retracted). Decision integrity still holds (stale reply rejected + new-identity re-ask). member1+projection unchanged. **⏳ rev2 must-revise (`…-031120`, MR-1/MR-2 PASSED, 1 last blocker MR-3) → rev3 FOLDED** (`../../relays/s11-8a-m2/DESIGN-planner-20260714-031600.md`). **MR-3 (Go map-aliasing bypass, owned):** verified `record.go:29,31` Headers/XFields are maps + `migrate.go:64` shallow `out:=rec` + `migrate_test.go:31-32` in-place mutation ⇒ an in-place choice migrator aliases the **shared** map, making `π(source)==π(migrated)` (guard bypass) + **corrupting the live `t.Records` view**. Fix: (1) **snapshot `π(source)` as a value BEFORE Apply**; (2) **`migrate.Apply` deep-clones** Headers+XFields before each migrator step (ownership locus = Apply, protects all callers — read tool/replay/verdict path, m-2 §9 read-side seam); (3) compare pre-Apply snapshot, either-side parse fail-closed; (4) `NF-migrate-choice-inplace-mutate` fixture (guard fires + source/table byte-equiv **in-memory AND on-disk**). MR-1/MR-2/projection/member-1/honesty all passed, unchanged. **✅✅ 8a INTEGRATED + T6 LOCKED (g1 CLOSED)** — master RECONCILE (`s11-8a-joint-review/RECONCILE-orchestrator-planner-20260714-035210`) verified all 3 members at the byte grain (m-2 1&2 rev3-approved; m-6 member 3 cross-confirmed against final rev3 bytes) → ONE integrated §B contract, **s11 T6 builds to it**. **§C ruling:** bucket-D token = **`stale_choice_set`** (distinct from bucket-A `stale_schema`); **NO m-2 registration owed** (`failing_edge :113` has no `enum_set` = open stamped value is the only shape; closing it = master escalation, not in-slice) — T6 does NOT hold on it. **§D rev4 EDITORIAL fold DONE** (`../../relays/s11-8a-m2/SITREP-planner-20260714-140700.md`, 3 edits zero-semantics: §2.3 row 1 + reason-note + §2.5 E2E asserts BOTH tokens byte-exact) — the design-of-record now agrees w/ §B; the s9 stale-locus-reaches-the-build lesson applied. **m-2 8a work COMPLETE** — on call only for a T6 build-fidelity registry byte (none expected). m-2.planner holds |
| step3 | DESIGN (stage-1 owned contract) | `step3-mvp-design-m2` — the §7 stage-1 m-2 contract per the ratified MVP amendment r7 (`2f75f2a1…`): the form→tool-schema + submit-payload MAPPING module + the F58 relay-verb components + the MCP↔native parity vectors | **⏳ rev0 IN PAIR REVIEW** — doc `design/2026-07-16-step3-mvp-form-schema-mapping.md` (DESIGN_DOC_ID `step3-mvp-design-m2-mapping`); relay `../../relays/step3-mvp-design-m2/DESIGN-planner-20260716-053000.md` → m-2.implementer (sub-ID `step3-mvp-m2-mapping`). Both the `…-041620` dispatch + the `…-043520` VP supplement consumed (confirm set = m-9+m-7+**m-10**; the m-2→m-7 transport confirm owed separately when m-7's bytes land, doc §8d). Grounded @ `502e06c` (s11-close). **§2 module:** m-2-owned home (proposed `internal/formschema`) for `SchemaFromForm`/`SubmitPayloadFromArguments` + re-render semantics (hoist origin `cmd/frank-mcp/schema.go:11-47,90-129` + `mcp.go:228-276`); 3-way seam = bidirectional import-boundary test; normative R/P-rules; **3 flagged fail-closed DELTAS** vs the trapped impl (P-1 unknown top-level member reject via DisallowUnknownFields vs silent ignore · P-2 reserved header keys reject · P-3 CC-conflict reject vs silent overwrite `schema.go:101-103`); headers interior deliberately OPEN pass-through (conductor = sole acceptance authority, no client-side fork). **§3 F58:** `relay.*` canonical/alias/wire table (conductor untouched); **TEMPLATE-digest split** — rendered submit schema is run-varying by construction (`form_digest` const embeds config digest `render.go:228-247`; ConductorVolatile/DigestExempt options move without moving form_digest `render.go:144-146,249-262`) ⇒ tool-schema digest binds the pinned static template (headers slot empty, const-pin removed), dynamic-fill rules bind under **mapping VERSION `m2-mapping-v<N>`** (registry-marker discipline) with the **F63 reference-form fingerprint** as the mechanical drift tripwire (recomputed from the shipped artifact over a pinned branch-covering fixture; drift w/o bump fails closed; coverage honesty bound stated in-doc). Applicability: version member ABSENT (not empty) for local tools. Canonical bytes pinned; 3 reference SHA-256 values derived (authoritative at the Master+VP interface-lock). **§4 parity:** shared vector shape + 11-item coverage floor; byte-identical wire calls / same typed mapping error / same re-render disposition across MCP + native; retry payload-transparent. **No FieldSpec registry byte, no conductor byte.** All machinery RED-first (§6, none extant @ 502e06c). **⏳ rev0 must-revise (`step3-mvp-m2-mapping-review-r1`, `…-054500`, 3 blockers — ALL verified at source then folded) → rev1 IN FRESH REVIEW** (`../../relays/step3-mvp-design-m2/DESIGN-planner-20260716-060500.md`, doc @ `c2332a3b…`). **MR-1 (owned — rev0 was doubly wrong):** P-4 "interior openness" + vector-3 reaches-the-wire contradicted the ratified validated-before-authorization seam (m-9 design §3 malformed-never-reaches-authorization; m-10 §D.2 zero-dispatch/no-ticket; amendment `:61,:95`) AND rev0's own closed generated schema → **three-layer split**: mapper non-authoritative / normative frontend dispatch gate BOTH frontends (`schema_invalid` = no wire call no ticket; MCP validates itself via new shared `ValidateSubmitArguments` helper, offered-not-imposed on m-9) / conductor sole ACCEPTANCE authority (digest-pinned mirror, no fork — rev0's fork objection retracted w/ grounds). **MR-2:** single-render fingerprint left every error branch + re-render unbound → machine-enumerated rule inventory (R/P/RR IDs, exported constant) + `exercises`-annotated vector suite (SAME artifact as §4 parity floor) + both-direction coverage binding + fingerprint over ordered (vector-id, result-record) pairs recomputed from the shipped artifact at F63; residual = outside-inventory only. **MR-3:** DisallowUnknownFields leaves Go duplicate-key last-wins + case-insensitive fallback (silent-overwrite class) → **P-6 strict decode** (duplicate members byte-exact every closed layer + trailing-data reject; `config_change.go:65-124` precedent reused, Rail B) + P-1 exact-name binding. Passed set untouched (template-digest split + 3 reference digests recomputed byte-identical by reviewer; name table; applicability; bounds). **⏳ rev1 must-revise (`…-review-r2`, `…-061500`, 2 deeper blockers — BOTH verified at source then folded) → rev2 IN FRESH REVIEW** (`../../relays/step3-mvp-design-m2/DESIGN-planner-20260716-063500.md`, doc @ `21d28c2d…`). **MR-4 (owned — rev1 over-claimed the mirror):** digest-exempt volatile state diverges under an unchanged digest (render_test.go:90-137 equal digests across grant absent→present + recipient/monotonic drift; contraction surfaces as enum/seat-scope/monotonic-floor never re-render, validate.go:23-26,55-65 — RR-1 can't heal a stale cache) → NEW **§2.3.3**: stable/volatile **V-partition** (Layer 2 rejects digest-bound constraints only; volatile advisory — flags ride DescribeTools, mechanical, no conductor byte) + **F-rules** (F-1 refresh-before-reject; F-2 refresh-on-ANY-rejection; F-3 exact race-honesty claim — convergent-with-visible-divergence, instant equality NOT claimed) + **PV** same-digest fixture family. **MR-5:** rev1 had no pre-build fingerprint anchor (T4-authored suite = self-consistency; extensible additions moved the hash; rule-grain coverage) → **Appendix A authored NOW**: LOCKED reference set — 27 ordered vectors, 40 branch-grain IDs, byte-exact inputs (incl. serializer-unproducible malformed bytes), canonical expected result records from a design-side executable expectation (the generator caught an RF-1 fixture defect pre-lock — PHASE/CEREMONY_TIER missing), pinned struct-order serialization, **expected fingerprint `9f80a913…`**; Appendix-A immutable (change = version bump + re-lock); parity suite split off extensible/non-identity; F63 lock-vs-release compare never derives both sides from T4. **⏳ rev2 must-revise (`…-review-r3`, `…-065000`, 4 blockers — incl. 2 FALSE claims in my rev2 relay, both owned) → rev3 IN FRESH REVIEW** (`../../relays/step3-mvp-design-m2/DESIGN-planner-20260716-071000.md`, doc @ `092cd108…`). **MR-6:** advertised MCP inputSchema still enforced volatile constraints — host-side reject leaves NO call for F-1 to heal (deadlock) → the **advertised-schema principle** (enforcing keywords = digest-bound self-healing constraints ONLY; annotations carry volatile guidance): R-1.b headers advertised-open, R-3 volatile options → description annotation, list_changed on EVERY refresh, PV-1/PV-3 host-simulator legs, MCP parity boundary narrowed exactly (conforming host cannot volatile-false-reject; ignore+hard-cache host = labeled residual); submit TEMPLATE digest moved → `6bb7bbf4…`. **MR-7 (owned):** my "40-ID" claim was FALSE (mechanical count 39; generator asserted set-coverage never count) AND required-`headers` genuinely unbound (a validator dropping it reproduced every locked byte + fingerprint) → V-1.e/V8; corrected inventory **41**. **MR-8:** RF-1 bytes weren't an executable `fieldspec.Form` encoding → re-pinned `{"fields":{…}}` + A.1 runner binding rule. **MR-9 (owned):** §7 Rail-A still carried the superseded rev1 mirror guarantee — my rev2 "sweep clean" claim was FALSE (token grep missed the paraphrase) → §7 rewritten; the paraphrase sweep caught 2 further stale loci pre-relay. Appendix A regenerated: 41 branches, 28 vectors, expected fingerprint **`306b3149…`**; pre-lock posture clarified (immutability runs FROM the interface-lock). **⏳ rev3 must-revise (`…-review-r4`, `…-073000`, 1 bounded blocker) → rev4 IN FRESH REVIEW** (`../../relays/step3-mvp-design-m2/DESIGN-planner-20260716-074500.md`, doc @ `62efe963…`). **MR-10:** §2.3.3 + §3.3 still bound `V1–V7`/27 while regenerated Appendix A binds `V1–V8`/28 — and §3.3's sentence IS the F63 pre-build identity definition (a release-binding following it could omit V8 and silently unbind required-headers, defeating MR-7) → both corrected to the 28-vector exact set; sweep clean (only the historical §9 rev2 entry remains); **Appendix A byte-identical** (A.5 re-verified post-edit → unchanged fingerprint `306b3149…`); no identity byte moved. Reviewer's r4 independently reconfirmed: 41 unique branch IDs, 28/28 vectors/records, fingerprint + template digest exact, MR-6/7/8/9 all materially closed. **⏳ rev4 must-revise (`…-review-r5`, `…-080000`, 1 metadata blocker; reviewer's inverse-delta check reconfirmed the rev4 fold exact + Appendix identity) → rev5 IN FRESH REVIEW** (`../../relays/step3-mvp-design-m2/DESIGN-planner-20260716-081500.md`, doc @ `83d8e63e…`). **MR-11 (owned — the MR-9/MR-10 stale-locus class at the primary metadata locus):** the line-3 header still self-identified rev2 → header now names only the live revision + defers to §9 as authoritative history, convention pinned. **One NAMED deviation flagged for the reviewer's verdict:** the r5 bar's literal "identifies these bytes as rev4" would put two byte-states under one revision name (reviewed rev4 = `62efe963…`) — these bytes are **rev5**; intent-over-letter, reasoned in-relay, one-token correction if ruled otherwise. Appendix A byte-identical (fingerprint `306b3149…` re-verified). **✅ PAIR-APPROVED BYTE-BOUND (review-r6 `…-083000`, verdict: approve)** at rev5 SHA-256 `83d8e63e…` (rev5 naming deviation ACCEPTED; reviewer re-verified: inverse-delta → rev4 hash exact, Appendix A 41/28 intact, fingerprint `306b3149…` + template digest recompute exact; any byte change voids the approval). **6 rounds, 11 MRs closed — 4 were my own false/over-claims, each owned in §9** (r1 seam+fingerprint+decode; r2 volatile-mirror+lock-anchor; r3 advertised-schema+count+Form-encoding+§7-echo; r4 exact-set loci; r5 header identity). **Completion SITREP → master**: `../../relays/step3-mvp-design-m2/SITREP-planner-20260716-084500.md`. Remaining §7 stage-1 edges (master's to route): m-9 + m-7 + m-10 consumer confirms → Master+VP interface-lock (binds my 3 template digests + `m2-mapping-v<N>` + the Appendix-A expected fingerprint; never self-declared). **m-2.planner HOLDS** — on call for (a) consumer-confirmation questions on my bytes, (b) ~~the owed m-2→m-7 transport confirmation~~ **✅ DISCHARGED 2026-07-17**: master routed it (`../../relays/step3-mvp-confirm-m2/RECONCILE-orchestrator-planner-20260717-010030.md`) on m-7's pair-approved bytes → **CONFIRM returned byte-bound** (`../../relays/step3-mvp-confirm-m2/SITREP-planner-20260717-013000.md`, m-7 doc @ `f072bd99…` hash re-verified pre-read, all 3 axes verified at their §1.1–1.5 vs my rev5 directly: seam neither-absorbs-nor-strands incl. the raw-DescriptionResponse split my §2.3.3 volatile partition depends on; Describe/Call suffice for F-1/F-2/F-3; retry payload-transparency confirmed + strengthened — FX-TB-11 proves the submit replay my §4 refused to take from the code comment). 2 lock-table observations recorded (canonical-name normalization locus division; their §1.3 census = floor, my rev5 API superset lands module-side under their own ruling), no finding, no byte owed either side. **✅ REFRESH-ROUND RE-AFFIRM 2026-07-17** (`../../relays/step3-mvp-confirm-m2/SITREP-planner-20260717-124500.md`): m-7's F70/L1+R7-F1 corrective moved their contract to r8 `ab0ed428…` (voiding the f072bd99… basis) → re-affirmed byte-bound at r8, VERIFIED at the bytes: §1.1–1.5 line-by-line byte-identical vs my confirmed basis; corrective surfaces (§2.10 bootstrap, L1 counter strings, FX-TB-18) all off my seam — the L1 rule leak-checked specifically (no trust-bearing counter transits my payloads/DescriptionResponse); FX-TB-10/11 survive verbatim; all 3 axes + 2 lock-table observations carry unchanged. Per master: my inbound confirms (m-9 leg-1 · m-7 F71 @ r8 basis · m-10 leg-2) all stand on my byte-unmoved `83d8e63e…`. **✅ FINAL r11 LETTER REBIND 2026-07-17** (`../../relays/step3-mvp-confirm-m2/SITREP-planner-20260717-210000.md`): m-7 moved r8→r11 `9331ea88…` (D-3 attach taxonomy + R9-F1 + R10-F1, all §2-side) → rebind CONFIRMED byte-bound at r11; §1.1–1.5 line-by-line byte-identical vs my affirmed r8 basis (the owner's delta statement corroborated at the bytes); FX-TB-10/11 survive; no finding. **Every m-2 stage-1 edge discharged at final bases** → master's corrected stage-1 close packet → Master+VP interface-lock. **✅ STAGE-4 LEG-3 CONFIRM 2026-07-20** (`../../relays/step3-mvp-confirm-m2/SITREP-planner-20260720-201500.md`): m-9 worker r7 `cb7ff970…` × my frozen `83d8e63e…` — (1) §4.2/§4.4 mapping-render realization faithful (exact §2.2 API, offered validator consumed, re-render-as-consumer-logic, Layer-2-before-minting, MCP same-module parity, zero FieldSpec re-implementation); (2) §8.3 consumption byte-exact (my 3 digests never re-derived; `m2-mapping-v1` on exactly the relay rows; §3.4 absence + shape-violation rule exact; producer split exact); (3) expected `tool_catalog_digest` **`7fae5fc1…` independently recomputed — MATCH at both grains** (5/5 local schema digests + the sorted 8-row vector, all under my §3.2 encoding). 1 lock-table observation (flag-4 parity read = my FULL §4 floor incl. PV/F-1/F-2; subsetting would be a finding, none suggested). No finding; contract frozen. **✅ H-16 OWNER CONFIRM 2026-07-20** (`../../relays/step3-mvp-confirm-m2/SITREP-planner-20260720-231500.md`, rev12 `9d7f6aed…` byte-bound; parallel-VP voiding price accepted): all 4 surfaces CONFIRMED at rev12 + `frank@6e4d657` source — 3 system-stamped header rows (exact target registry shape stated: header/system/system_only/string/no-enum_set; fold-layer version semantics = the Rail-A-correct shape); rejection path deterministic (existing `system-owned` class, rejectAtEdge headers-preserved verified); the 6 tokens = OPEN failing_edge values per the 8a ruling (NOT closed enums, correctly; no registration owed); ceremony-form authority-absence provable at 3 layers. **+ bounded finding H16-M2-F1** (fixture-layer → m-7): the forged-header committed-rejection T legs are unreachable through conforming frontends (my Layer-2 V-1.b gate kills them client-side, no record) — must drive the raw channel.Client; mechanism sound. Forward scope: the v-next registry changeset (3 header rows + record_kind tokens + marker) = m-2-authored at build fidelity. **✅ rev16 re-CONFIRM 2026-07-21, F100-CONDITIONAL** (`../../relays/step3-mvp-confirm-m2/SITREP-planner-20260721-011500.md`, `a349a329…` byte-bound; binds only w/ m-1's leg + the master/VP join over the SAME hash): **my H16-M2-F1 landed faithfully as F98** (wire locus = authority, frontend gate = defense-in-depth; T-R6F3 raw-client committed bytes + conforming-frontend ZERO-call legs — the impossible leg gone); all 4 rev12 surfaces re-confirmed at the F97-materialized bytes (accepted-only nullity now STRONGER — extended to admin_provenance edges; anchor-form phrase resolved: writer-stamped RECORD member, never a form input); forward scope +1: the §4b disposition-form field list joins my changeset. No finding. **⛔ LANE-2 DAG HOLD 2026-07-21/22**: master STOP (`../../relays/step3-relock-dag-hold/RECONCILE-orchestrator-planner-20260721-235500.md`) revoked the six lane-2 DAG dispatches incl. `step3-relock-dag-m2/…-231504` (94c14f3c…) pending an F2–F5 re-cut + fresh VP decomposition review — **m-2 action-to-date return: NONE** (`../../relays/step3-relock-dag-hold/SITREP-planner-m2-20260722-000800.md`; the dispatch never reached this seat pre-hold, body deliberately unread, disk nullity verified; the postdated `…-235604` re-cut equally untouched/inert). **Lane-2 work STOPPED until master's separately-addressed explicit release** — a VP verdict / the hold / the re-cut file are none of them. **✅ RELEASED 2026-07-22** (`../../relays/step3-relock-dag-m2/RELEASE-orchestrator-planner-20260722-004001.md`; rev2 dispatch `342f64b6…` re-verified pre-read) → **§5-E COMPONENT CONTRACT rev0 AUTHORED + IN PAIR REVIEW** (`step3-relock-e-m2`; doc `design/2026-07-22-stage6-e-logical-component.md` @ `40bed743…`; relay `../../relays/step3-relock-dag-m2/DESIGN-planner-20260722-010000.md` → m-2.implementer): the `logical_tool_schemas[]`/`tool_descriptions[]` recipe for m-9's `logical_surface_digest` — INV-E1 non-conflation with F58 (presented-surface/attempt-grain/descriptions-INCLUDED vs template/run-immutable/excluded); sourcing split (relay bytes mine, local bytes m-9's §8.3 pins, recipe mine, hash m-9's); 8-name totality fail-closed; JCS + §3.2-coincidence test-bound; observer derivation = pure function of the presented surface (helper offered-not-imposed); **mapping-version binding BY ADJACENCY** (the ratified 5-member object stays closed — `m2-mapping-v1` binds via the F58 vector on the same attempt row); attempt-grain honesty per our §2.3.3 volatility grounds. Governed ADDITIVE delta — frozen `83d8e63e…` unmoved, zero registry bytes. **⏳ rev0 must-revise (`step3-relock-e-m2-review-r1`, `…-023000`, 3 blockers — all verified then folded) → rev1 IN FRESH REVIEW** (`../../relays/step3-relock-dag-m2/DESIGN-planner-20260722-030000.md`, doc @ `556ba9e9…`). **F1** (vs my own frozen bytes): descriptions = m-9-presented tool-level strings ONLY, zero synthesis (R-3 = schema-property member, bound once via the schema element); ownership re-split (m-9 owns ALL 8 tool-level strings + assembly/hash). **F2 (owned — assert-before-verify):** rev0's same-attempt-row catalog carriage doesn't exist (provider_attempts has no catalog fields, m-10 r40 §E) → adjacency rewritten to the real relation (attempt→run→manifest tool_set/m2-mapping-v1/tool_catalog_digest→release binding) w/ 4 fail-closed legs; same-row claim retracted. **F3:** the Go/JCS coincidence premise was wrong (HTML-escaping of `<>&`) → JCS over parsed values = SOLE encoding; coincidence caged to pinned static fixtures + negative leg. Post-fold paraphrase sweep caught 1 more stale locus (§4) pre-relay. **⏳ rev1 must-revise (`…-review-r2`, `…-080000`, 1 bounded blocker; F1–F3 confirmed closed) → rev2 IN FRESH REVIEW** (`../../relays/step3-relock-dag-m2/DESIGN-planner-20260722-083000.md`, doc @ `c3a8cd61…`). **M2-E-R2-F1:** the load-bearing INV-E1 table + §2.1 elements still said byte-verbatim after §2.3 went parsed-values/JCS — two contracts in one doc → fixed at 3 live loci + 1 self-caught 4th (the description element's own escaping ambiguity); value-level rule now INLINE at every definition site; sweep clean (citations + history only). **✅ PAIR-APPROVED BYTE-BOUND (review-r3 `…-103000`, verdict: approve)** at rev2 SHA-256 `c3a8cd61…` (inverse-delta to rev1 verified exact by the reviewer; any byte change voids). **The released lane-2 producer-first obligation is DISCHARGED** — completion SITREP → master: `../../relays/step3-relock-dag-m2/SITREP-planner-20260722-110000.md`. Arc: 3 rounds, 5 findings, all verified pre-fold (1 owned assert-before-verify — the rev0 same-row carrier); lesson logged: the value/byte distinction states at EVERY definition site. Remaining lane edges (master's to route): m-9 fold-consumability + m-3 observer-derivability F73 asks → integration → Master+VP re-lock. **m-2.planner HOLDS** — on call for confirmation questions on the approved bytes only |
| step3 | DESIGN (ruling-(4) deliverable) | `step3-relock-c-m2-submit-resource` — the §5-C `canonical_resource` shape for `relay.submit`, routed to m-2 by master's §D-settlement ruling (4) (`../../relays/step3-relock-dag-m10/RECONCILE-orchestrator-planner-20260722-230000.md`, restated `…-001500`): the ratified table marks the relay cell "verb + target id", but `relay.submit` CREATES the record it names — no pre-existing target — and the table admits no `∅` | **⏳ rev0 IN PAIR REVIEW** — doc `design/2026-07-23-stage6-c-relay-submit-resource.md` @ `542217af…`; relay `../../relays/step3-relock-dag-m10/DESIGN-planner-20260723-010000.md` → m-2.implementer. **Shape:** `"relay.submit:" || SHA-256(JCS{form_digest, dispatch_id?, to?})` over the submission's own members — verb-prefixed; optionals omitted when absent (frozen §3.4; empty = absent); closed object; JCS over parsed values (the approved §5-E discipline reused). **`∅` considered + REJECTED with reasons** (master's invited counter): the target context is structurally guaranteed — `form_digest` is in the frozen R-1 REQUIRED set — so `∅` would drop evidence the descriptor exists to bind; **totality follows from the same fact**. Members: `form_digest` = the governed surface submitted into (transitively form × seat × phase × tier × config — master's "derived from m-2's form schema"); `dispatch_id`/`to` = destination coordinates; **excluded + stated**: `cc` (informational, rides the args digest), `body`/`headers` (content). **Projection property precedented by `apply_patch`'s ordered target-set digest in the same ratified table** (same-target/different-body ⇒ shared resource, differing args digest — the two-writes-one-path relationship). Honest labeling: binds the invocation AS PRESENTED at authorization, never acceptance. Observer recomputes from the observed invocation alone. 3 reference vectors + the exact amendment replacement-cell bytes + 5 RED-first obligations. Scope = the `relay.submit` cell only; both approved m-2 docs re-verified byte-exact/unmoved; zero registry bytes. If the reviewer judges `∅` more faithful → routes BACK to master, not into these bytes. **⏳ rev0 must-revise (`…-review-r1`, `…-110000`, 1 blocker — CONFIRMED AT SOURCE then folded) → rev1 IN FRESH REVIEW** (`../../relays/step3-relock-dag-m10/DESIGN-planner-20260723-120000.md`, doc @ `5ec7a3d2…`). **M2-C-R1-F1 (owned — a clean category mistake):** I excluded `cc` as "informational carriage", applying the **authority** rule (CC = context-only, owes no action — true and unchanged) to an **effect-target** question; verified at `projections.go:149-174` (`c78da38`) that `DeliveryRecipients` adds every CC address and each gets an `IntentMailbox` — CC addressees receive REAL deliveries, so the exclusion violated my own target-change rule + the `apply_patch` complete-target-set precedent. Fold: `cc` joins the closed projection with a **total** two-branch rule (decoded array / `cc_unparsed`, mutually exclusive, distinct names — the unparsed branch is reachable since the schema types `cc` as a plain string, keeping derivability total for schema-valid-but-rejectable calls); **new §2.3a** bounds the claim to targets-**as-named** (the store's dedup/union projection deliberately NOT re-derived); the authority/effect distinction + "no relay authority" stated explicitly; RV-4/5/6 added (RV-1/2/3 byte-unchanged); obligations 1–6. Post-fold sweep caught 2 stale "three members" loci pre-relay. **✅ PAIR-APPROVED BYTE-BOUND (review-r2 `…-140000`, verdict: approve)** at rev1 SHA-256 `5ec7a3d2…` (reviewer independently recomputed all 6 RVs + probed the CC partition — no unclassified/double-classified case; any byte change voids). **Ruling (4) DELIVERED** — completion SITREP → master: `../../relays/step3-relock-dag-m10/SITREP-planner-20260723-150000.md`, carrying the exact §5-C replacement-cell bytes for master's bounded §D-settlement amendment ((1)+(3)+(4)) → VP exact-byte review → operator ratification. `∅` explicitly offered back for re-rule (§1 reasons) — not decided inside the cell. **✅ RATIFIED + LANE-2 CLOSED 2026-07-23**: the cell `5ec7a3d2…` is bound verbatim as Correction 3 of the §D-settlement amendment (rev4 `1fa71cb8…`, VP-passed, operator-ratified). Master records the **LANE-2 INTERFACE DAG CLOSE** (`../../relays/step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260726-160000.md`) over nine byte-verified settled bases — m-2's three (`c3a8cd61…` §5-E · `5ec7a3d2…` relay.submit cell · frozen `83d8e63e…`) independently re-verified on disk this session, all MATCH master's record. **All m-2 lane-2 obligations DISCHARGED.** Item A is now RATIFIED + AUTHORED as the plain byte-bound interface-lock record `master/STEP-3-INTERFACE-LOCK.md` (external SHA-256 `cbd1893c7b1ed6a88aa4668ff6e333e037f6898069594b74f758918ad8913636`), per the item-A simplification amendment `master/STEP-3-ITEM-A-SIMPLIFICATION-AMENDMENT.md` rev7 `3443f73d30240c90aa6b1b46cbcd3763444592c422ff8ab655307bc1ebbd7373` (operator-ratified 2026-07-27) — the prior extraction-bundle mechanism is superseded/withdrawn, and the record is now in VP + F73 review. m-2.planner HOLDS — re-engage at lane 4 (the Master+VP re-lock over the record + the `STEP-3-EXIT-FIXTURES.json` freeze); the build-fidelity registry changeset (H-16 header rows + record-kind tokens + disposition-form field list + marker) still owed when master opens that lane |
| c1 | PLAN | gated design-doc PLAN | **✅ OPENED — Step-1 build underway.** All pre-PLAN gates closed (Cluster 1/4, m-7 CQs, c6, c6.1); Step-1 PLAN opened → the build runs in `frank/` (relays under `frank/.relays/`). m-2's design-of-record is **locked + frozen**; the m-2 pair now serves the build as **fidelity reviewer** (s1, ✅) + **domain guide** (s3, ✅ above), never re-cutting the locked contract |

## Layout
- `audit/` — AUDIT artifacts (durable home; the relay under `master/relays/c1-audit-m-2/` is the lint-checked report of record).
- `design/` — DESIGN docs + grill locks (later, after PROCEED).

## Step-3 MVP amendment delta (RATIFIED 2026-07-16 — `master/STEP-3-MVP-AMENDMENT.md` r7 @ `2f75f2a1…`; the F44 fold)
**m-2's stage-1 authorship (§5/§7 + F58):** the **form→tool-schema + submit-payload mapping** — `SchemaFromForm`/`SubmitPayloadFromArguments` encode m-2-owned FieldSpec/form semantics (`cmd/frank-mcp/schema.go`); the mapping is hoisted into an **m-2-owned module** (the m-7 shared transport client must NOT absorb it — the 3-way seam: m-7 transport / m-2 mapping / m-9 consumer). m-2 produces the **relay-verb tool-schema digests + the form→schema mapping VERSION** of the F58 build-identity vector (`{name, schema digest, catalog version, mapping version}`; local tools carry NO mapping version); canonical encoding + applicability are defined in this DESIGN; the assembled vector binds at the Master+VP interface-lock and is mechanically verified at the F63 release-binding. Parity requirement: the retained MCP frontend and the native tool produce **equivalent conductor calls + re-render behavior** (shared conformance vectors). No FieldSpec registry byte moves at this fold. The stage-1 DESIGN dispatch is issued with this fold.
