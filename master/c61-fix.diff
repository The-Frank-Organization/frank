--- a/master/domains/m-3-observation-evidence/design/2026-06-29-v3-observe-evidence-design.md	2026-07-02 22:53:53
+++ b/master/domains/m-3-observation-evidence/design/2026-06-29-v3-observe-evidence-design.md	2026-07-03 00:59:05
@@ -60,7 +60,7 @@
 
 **§3.1 — the closed write-allowlist (R3 observer-only).** The hook writes ONLY: `ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT`, `achieved_evidence`, the `target>achieved` computed `*_RESULT`, per-field `evidence_integrity`, the computed `record_integrity` rollup, the per-claim `executable_claim_results`, the `egress_scan_result`, `degradation_notes`, the routing-profile observed set (§9), and a pass/fail **veto**. It writes **no** identity/envelope field (FROM/ROLE/PARENT/relay_id) and performs **no** delivery effect. "Observer-only" is enforced as a positive allowlist, not prose intent.
 
-**§3.2 — what may veto.** Delivery gates on *evidence integrity* **only class-conditionally** (operator decision ②, folded `c4-cq-gateconfig`): for a **non-authority** record a `self_reported` field still delivers — the honest fallback, §8 — but for the **authority/merge (A-gate) class** a not-fully-verified record (`record_integrity ∈ {self_reported, mixed}`) is **held/escalated, not delivered** (condition (d)). The vetoing/holding conditions are: (a) a phase-shaped done-predicate the conductor can observe to be **false** (e.g. an action-claim with no matching git ref; a read-only report over a dirty tree); (b) the generic **declared-vs-observed integrity veto** (a lane-declared field contradicted by conductor observation — e.g. clean-tree-claimed/dirty-observed; the routing silent-deviation case, §9); (c) a failed **egress** scan (§7); (d) **class-conditional fail-closed (decision ②):** `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` ⇒ the record is **held/escalated** (terminal `held`, resummon the operator — J1 `hold_and_resummon`), never shipped on a not-fully-verified authority claim. `authority_class` is m-2's declared key (`bool`·`owner:system`·`computed_result`, m-2 §17.6 — `record_kind ∈` the authority-bearing set **or** `gate_category ∈ A`; **never keyed on model**, R2 holds). Token mapping (CQ-4): (a)/(b)/(c) are the check **running and saying no** ⇒ terminal **`rejected`** (§2); (d) is the check **unable to fully verify** an authority-class record ⇒ terminal **`held`**. *Deviation-as-such* and *integrity-labeling-as-such* still never act; only silent-deviation (b) and authority-class not-full-verification (d) do.
+**§3.2 — what may veto.** Delivery gates on *evidence integrity* **only class-conditionally** (operator decision ②, folded `c4-cq-gateconfig`): for a **non-authority** record a `self_reported` field still delivers — the honest fallback, §8 — but for the **authority/merge (A-gate) class** a not-fully-verified record (`record_integrity ∈ {self_reported, mixed}`) is **held/escalated, not delivered** (condition (d)). The vetoing/holding conditions are: (a) a phase-shaped done-predicate the conductor can observe to be **false** (e.g. an action-claim with no matching git ref; a read-only report over a dirty tree); (b) the generic **declared-vs-observed integrity veto** (a lane-declared field contradicted by conductor observation — e.g. clean-tree-claimed/dirty-observed; the routing silent-deviation case, §9); (c) a failed **egress** scan (§7); (d) **class-conditional fail-closed (decision ②):** `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` ⇒ the record is **held/escalated** (terminal `held`, resummon the operator — J1 `hold_and_resummon`), never shipped on a not-fully-verified authority claim. `authority_class` is m-2's declared key (`bool`·`owner:system`·`computed_result`, m-2 §17.6 — `record_kind ∈` the authority-bearing set **or** `gate_category ∈ A`; **never keyed on model**, R2 holds). Token mapping (CQ-4): (a)/(b) are the check **running and saying no** at acceptance ⇒ terminal **`rejected`** (§2); **(c) a failed egress scan is evaluated ONLY at the OUTBOUND external-send chokepoint (§7, m-6 §50/§51) ⇒ the non-terminal `egress_blocked` park + A local resummon — never a terminal token (not `rejected`, not `held`)**; (d) is the check **unable to fully verify** an authority-class record ⇒ terminal **`held`**. *Deviation-as-such* and *integrity-labeling-as-such* still never act; only silent-deviation (b) and authority-class not-full-verification (d) do.
 
 **§3.3 — total observe→terminal disposition (m-3-F6; keyed by authority class).** Every submit reaches exactly one terminal `{accepted | rejected | held}` (m-7 §6 / NF-S16, exactly-one-outcome). The `observe_result` sub-enums are **observe-layer signals, not independent terminals**: `executable_claim_results` and `degradation_notes` feed `record_integrity`/`achieved_evidence`, they do not map to a terminal token directly. The total map (realizing the VP two-axis, `DESIGN-REREVIEW-2026-07-02` §5):
 
@@ -70,10 +70,10 @@
 | `predicate_result=fail` (check **ran, observably false**) or `veto=block_delivery` from a declared-vs-observed contradiction (§9) | `rejected` | `rejected` |
 | `predicate_result=blocked \| degraded` (**no vantage** — opaque lane, §8; ⇒ fields `self_reported`/`mixed`) | `accepted` + `self_reported`/`mixed` label | **`held`** + escalate (§3.2(d): `record_integrity ∈ {self_reported, mixed}`) |
 | **conductor check machinery ran-and-broke** (internal fault — m-7 §6) | `rejected` / author-returned, fault edge named | **`held`** + escalate |
-| `egress_scan_result=blocked` (§7) | **`held`** + resummon (J1) | **`held`** + resummon (J1) |
+| `egress_scan_result=blocked` (§7, outbound away-mirror) | *non-terminal* **`egress_blocked`** park + local resummon (m-6 §4 / m-7 NF-S9) | *(same — non-terminal)* |
 | `executable_claim_results ∈ {skipped, unsafe}` | *(not a terminal — marks that claim's field `self_reported`; the record's terminal follows `record_integrity` per the rows above)* | *(same)* |
 
-The two axes: **check-ran-said-no ⇒ `rejected`** (both classes); **check-could-not-run** (no vantage *or* machinery-broke) **⇒ authority `held` / non-authority `accepted`+label or `rejected`+fault-edge**; **egress-block ⇒ `held`** (both). Consistent with CQ-4 (`c4-cq-gateconfig`), decision ② (§3.2(d)/§8), and m-7 §6 internal-fault-`held`.
+The two axes: **check-ran-said-no ⇒ `rejected`** (both classes); **check-could-not-run** (no vantage *or* machinery-broke) **⇒ authority `held` / non-authority `accepted`+label or `rejected`+fault-edge**; an outbound **egress-block ⇒ the non-terminal `egress_blocked`** park + A local resummon (m-6 §50/§51 / m-7 NF-S9 / ARCHITECTURE :309-310), **not a terminal token** (not `rejected`, not `held`). Egress is evaluated **only** at the outbound external-send chokepoint (§7, dormant until away-mode); the acceptance-stage `→ rejected` conditions are the predicate-false / declared-vs-observed integrity (a)/(b) vetoes, **not** egress. Consistent with CQ-4 (`c4-cq-gateconfig`), decision ② (§3.2(d)/§8), m-6 §2:46/§50/§51, m-7 §6/NF-S9, and ARCHITECTURE :309-310.
 
 ---
 
@@ -130,7 +130,7 @@
 
 **A fail-closed scan at the conductor-governed egress chokepoint.** The conductor's local outbox is the **only egress the governance system offers** (a governance-surface claim, **not** a system-level sole-egress claim): destination control over that governed path is by-construction (post-spike), so a pre-external-send scan on it cannot be skipped *through the governed surface* (vs agent-scripts' own `cmd_force_send` hole, `openclaw_relay.py:676-719`, which sends without a scan). The scan inspects the outbound body + attached/generated proof artifacts for **secrets / PII / undisclosed-model-names / auth-URLs**; that **content** safety (regex/entropy) is **best-effort and encoding-evadable — not "structurally unbypassable."** D5 residual (§11): under same-uid attach a shell-bearing seat can reach the network outside the conductor entirely (host Bash/curl); the egress gate vouches only for the governed outbox.
 
-- **Policy = always-block + hold-and-resummon (operator decision, §12).** Any flag ⇒ **block, never auto-send**; park the gate and resummon the operator (aligns with locked J1 `hold_and_resummon`). **No auto-redact** in the default; auto-redact is available only for content classes the operator *later explicitly whitelists*.
+- **Policy = always-block + hold-and-resummon (operator decision, §12).** Any flag ⇒ **block, never auto-send**; park the gate and resummon the operator (aligns with locked J1 `hold_and_resummon`) — this park is the **non-terminal `egress_blocked` state (§3.3), NOT the terminal `held` token**; egress is never a terminal outcome (m-6 §50/§51). **No auto-redact** in the default; auto-redact is available only for content classes the operator *later explicitly whitelists*.
 - **Rule set is operator-configurable policy** (mirrors locked §J2 `gate_category` config-sourced pattern) with a fail-closed `other`→block default. **Promote, do not rebuild** the detection engines: agent-scripts `agent-transcript:193-235` (redact/unsafe/fail-closed) + claude-code `secretScanner.ts:277-324`; for model-name disclosure, claude-code `sanitizeModelName`/`isInternalModelRepo` (`commitAttribution.ts:114-168`).
 - **Decision-⑤ carve-out — narrow, typed ODB model-name confidentiality exemption (operator decision, §12; folded `c5-fold-decision-5`).** The scan distinguishes two classes: **confidentiality** (undisclosed model-name disclosure → peer-bias — R2's rationale) and **safety/content** (secrets / PII / auth-URLs). Decision ⑤ exempts **exactly one typed field** from the **confidentiality class only**: `record_kind == ODB` (operator-facing, **conductor-generated**) ∧ `field == model_name` (m-6's typed **exempt-marked** ODB field) ∧ `destination == operator`. **Everything else is unchanged** — model-names still block on every other record / field / destination; the **safety/content** class still fully scans the ODB (a secret / PII / auth-URL in *any* ODB field still blocks); the fail-closed `other→block` default is preserved on all other paths. **Confusion-resistant, not forgery-proof (c5 checklist; D5 residual §11):** the exemption is reachable **only** through the governed ODB render/scan surface — the field is conductor-generated and **non-lane-writable via the tool surface (§3.1)**, so a *confused* lane has **no tool** to mark arbitrary bytes as the exempt model-name field (which would otherwise be a confidentiality-scan bypass). The **D5 same-uid direct-store-write residual remains** (§11): a shell-bearing same-uid seat writing the store directly is the documented accepted-risk, out of scope — a governed-surface property, not an absolute unforgeability claim. **Transport still gated:** in away-mode the ODB leaves only via the m-6 away-bridge opt-in; outside away-mode it renders locally and never egresses (carve-out moot). **R2 untouched:** this relaxes an *egress-confidentiality* scan on one operator-facing field — it does **not** make the model a *gate input*; the model stays payload / bookkeeping, never gate-referenceable (m-4 R2-guard co-confirms, `c5-fold-decision-5`). Seam: keys on m-6's typed exempt-marked ODB field (m-6 authors the render; m-3 authors this scan carve-out).
 - **v3.0-dormant under canonical-iff-consumed** (no external send yet) until the **first external send** — the m-6 away-mode external bridge (ARCHITECTURE.md §J1) — at which point it is the gate on that door.
@@ -151,11 +151,11 @@
 
 Reconciled both sides (`c2-design-m3-m4-coord/COORD-planner-20260629-192916.md` confirmed by `COORD-RECONCILE-planner-20260629-193400.md`). Folded verbatim (the c2 record; the `deviated_observed` formula is superseded by the GL-1 canonical, next):
 
-**GL-1 canonical fold (c6, m-4-F2 — SUPERSEDES the `chosen_model ∉ rank-1(capability_prior_snapshot)` formula in the verbatim block below).** `deviated_observed` is computed **bucket-vs-bucket**: `deviated_observed := chosen_bucket ≠ declared_bucket`, where `declared_bucket` is the planner-declared capability bucket and `chosen_bucket` is the conductor-observed bucket the chosen model resolves to — recorded as the auxiliary observe-atom **`bucket_binding_observed`** (the bucket binding the conductor actually observed). This is **strictly more R2-clean** than the original model-vs-rank1 phrasing: the comparison is bucket-vs-bucket, never a model-identity gate (model identity never enters any predicate). m-4 deletes / precisely-conditions its "equivalent fallback" bracket (m-4 §9, CC'd on this dispatch) so the two docs state one formula. Everything else in the seam (the silent-deviation block via the generic declared-vs-observed integrity veto; snapshot-provenance not DI-5; `outcome_feedback_ref` null-reserved v3.0 / benchmark-sampled v3.1) is unchanged.
+**GL-1 canonical fold (c6, m-4-F2 — SUPERSEDES the `chosen_model ∉ rank-1(capability_prior_snapshot)` formula in the verbatim block below).** `deviated_observed` is computed **bucket-vs-bucket**: `deviated_observed := declared_bucket ≠ rank-1(recommended bucket for (role, task_tag))` — the planner-declared capability bucket vs the capability-prior's rank-1 recommendation (**deviation from the floor**), evaluated against `capability_prior_snapshot`. The **separate** auxiliary observe-atom **`bucket_binding_observed := chosen_model ∈ members(declared_bucket)`** (a **boolean** — did the chosen model actually bind to the bucket the planner declared) is a distinct binding check, **not** the deviation. Both are bucket-grain; model identity never enters a predicate. Byte-identical to m-4 §2/§9 (GL-1) and m-2 §17.3. This is **strictly more R2-clean** than the original model-vs-rank1 phrasing: the comparison is bucket-vs-bucket, never a model-identity gate (model identity never enters any predicate). m-4 deletes / precisely-conditions its "equivalent fallback" bracket (m-4 §9, CC'd on this dispatch) so the two docs state one formula. Everything else in the seam (the silent-deviation block via the generic declared-vs-observed integrity veto; snapshot-provenance not DI-5; `outcome_feedback_ref` null-reserved v3.0 / benchmark-sampled v3.1) is unchanged.
 
 > `routing_decision.deviated` is a planner-declared boolean (gate-side; the only model-touching atom the m-2 schema gate reads, via `justified_deviation.required_when: declared_deviated==true`), cross-checked by a conductor-derived `deviated_observed := chosen_model ∉ rank-1(capability_prior_snapshot)` for `(role, task_tag)` (observe-side; an m-3 `record_kind=routing_decision` profile of the single R3 observe-atom). The check is observed on all routing decisions and rides on snapshot-provenance, not DI-5 (conductor-stamped snapshot ⇒ `evidence_integrity: observed`, including for opaque lanes). The four declared×observed cells deliver, EXCEPT silent deviation (declared=false, observed=true), which m-3 blocks via its generic declared-vs-observed integrity veto — an observe-layer bounce that prompts honest re-declaration, which then triggers m-2's existing justification requirement. No model-derived predicate enters the m-2 schema gate (R2 preserved) — escalated to orchestrator/VP for ratification at the c2 lock. Task-outcome evidence (`outcome_feedback_ref`) is null-reserved in v3.0 and benchmark-sampled in v3.1 (sampled independent of `deviated`).
 
-**m-3-side commitments:** the routing observe-atom is a `record_kind=routing_decision` profile of the single R3 atom; the silent-deviation block is the generic integrity veto (clean-tree class), realized as an observe-layer bounce (not a form-mutation, not a model-derived `required_when`); deviation-check stamped on all routing decisions; the layer is **finalized to capability-bucket** by the GL-1 canonical above (`chosen_bucket ≠ declared_bucket` via the `bucket_binding_observed` atom), not model-instance — the mechanism is identical, now R2-clean by grammar (bucket-vs-bucket, never a model-identity predicate).
+**m-3-side commitments:** the routing observe-atom is a `record_kind=routing_decision` profile of the single R3 atom; the silent-deviation block is the generic integrity veto (clean-tree class), realized as an observe-layer bounce (not a form-mutation, not a model-derived `required_when`); deviation-check stamped on all routing decisions; the layer is **finalized to capability-bucket** by the GL-1 canonical above (`declared_bucket ≠ rank-1(recommended)`; `bucket_binding_observed := chosen_model ∈ members(declared_bucket)` is the separate boolean membership atom), not model-instance — the mechanism is identical, now R2-clean by grammar (bucket-vs-bucket, never a model-identity predicate).
 
 ---
 
@@ -234,8 +234,9 @@
 
 ## 15. Fold-log
 - **`c2-fold-m-3` (20260630) — additive.** Folded m-5 consumer-lens findings (basis: `c2-consumer-reconcile`, VP-approved `RECONCILE-orchestrator-reviewer-20260630-034321.md`): **F1** — `slot_in` is conductor-owned, classified at work-record acceptance, immutable, non-lane-writable (§5.1); **F3** — `slot_in` = work-archetype axis only, `seat_archetype` is a separate axis owned outside m-3 (§5.1). Bounded/additive only — no concrete slot values, no `required_when`/`visible_when` on slot values, no m-2 ownership-category or predicate-vocabulary change; concrete tag-space + invariant selection stay m-5-owned (c3). Re-review: `c2-fold-m-3` → m-3.implementer (re-approval required before c2 lock).
-- **`c4-cq-gateconfig` (20260702) — decision-② fold + CQ-4 settle (bounded, directed).** Basis: orchestrator COORD `c4-cq-gateconfig/DESIGN-orchestrator-planner-20260702-012336` + addressing correction `c4-cq-coord/…-013323`; operator decision ② (`READINESS-REGISTER.md` ②) + `DESIGN-REVIEW-2026-07-01.md` §2A.7. **CQ-2 (fold):** §2 / §3.2 / §8 universal fail-OPEN → **class-conditional fail-CLOSED** — `authority_class ∧ record_integrity==self_reported` ⇒ terminal `held`/escalate (not delivered); non-authority still delivers `self_reported`. Keyed on m-2's declared `authority_class` (§17.6, line 334 — m-2 owns the field-home, m-3 owns this disposition fold; co-owner reconciled: m-2 already declared it + mapped to `held`). **CQ-4 (settle, m-3 half):** §2 refined — the Step-2 observe-bounce (predicate observably false / integrity-contradiction / egress-fail) **shares the terminal `rejected` token** (candidate not delivered; terminal evidenced record committed for audit), unifying with m-7 §6 / NF-S16 exactly-one-outcome; the `held` token stays **distinct** for authority-class unobservability (the check *couldn't run* vs *ran and said no* — m-7 §6 held-semantics); non-authority unobservable → `accepted`+`self_reported`. **CQ-4b (confirm):** the CTO section-composed single-top-level-digest config ruling preserves m-3's egress/evidence/registry config assumptions (restart-only trusted load, tamper-evident digest, per-domain authorship, change=committed store record) — the decision-② class→disposition map is m-3 config keyed on m-2's `authority_class`, composes into the artifact; confirmed, no correction. R2 preserved throughout (class-keyed, never model); no c1/c2 contract reopened (additive dispositions consistent with the exactly-one-outcome seam). Re-review: `c4-cq-gateconfig` → m-3.implementer (adversarial review — closure-gate leg per `…-013323`); cross-domain co-sign CQ-4 with m-2 + m-6. **Byte-consistency sweep (rev, per m-3.implementer `DESIGN-REVIEW-…-020509` must-revise — intended shape approved, stale text blocked):** patched the residual pre-fold lines a builder could read as the old behavior — §1:15 (`self_reported` "is delivered" → non-authority-only, authority→held), §1:19 + §5.1:101 + §13:200 (`submitted→accepted` two-state → the readiness-fix-c1 **no-`submitted`-limbo** terminal-outcome seam), §6:111 ("no delivery gate branches on either label" → non-authority-only + the one decision-② exception), §12:175 (opaque-lane "deliver-and-label" → class-conditional). Surfaced (not silently resolved): the `authority_class ∧ record_integrity==mixed` edge — folded key stayed `== self_reported` as m-2 declared until reconciled. *(Resolved c6 per the VP-ratified **CQ-2 canonical** — authority-class `mixed` now also `held` under the §6 pessimistic rule; the closed CQ-4 is superseded as the vehicle. See §6:111 / §3.2(d).)*
+- **`c4-cq-gateconfig` (20260702) — decision-② fold + CQ-4 settle (bounded, directed).** Basis: orchestrator COORD `c4-cq-gateconfig/DESIGN-orchestrator-planner-20260702-012336` + addressing correction `c4-cq-coord/…-013323`; operator decision ② (`READINESS-REGISTER.md` ②) + `DESIGN-REVIEW-2026-07-01.md` §2A.7. **CQ-2 (fold):** §2 / §3.2 / §8 universal fail-OPEN → **class-conditional fail-CLOSED** — `authority_class ∧ record_integrity==self_reported` ⇒ terminal `held`/escalate (not delivered); non-authority still delivers `self_reported`. Keyed on m-2's declared `authority_class` (§17.6, line 334 — m-2 owns the field-home, m-3 owns this disposition fold; co-owner reconciled: m-2 already declared it + mapped to `held`). **CQ-4 (settle, m-3 half):** §2 refined — the Step-2 observe-bounce (predicate observably false / integrity-contradiction) **shares the terminal `rejected` token** (candidate not delivered; terminal evidenced record committed for audit), unifying with m-7 §6 / NF-S16 exactly-one-outcome; *(c6.1: **egress-fail removed from this `rejected` group** — egress is evaluated only at the OUTBOUND external-send chokepoint ⇒ the non-terminal `egress_blocked` park, never a terminal token; §3.2(c)/§3.3, m-6 §50/§51)*; the `held` token stays **distinct** for authority-class unobservability (the check *couldn't run* vs *ran and said no* — m-7 §6 held-semantics); non-authority unobservable → `accepted`+`self_reported`. **CQ-4b (confirm):** the CTO section-composed single-top-level-digest config ruling preserves m-3's egress/evidence/registry config assumptions (restart-only trusted load, tamper-evident digest, per-domain authorship, change=committed store record) — the decision-② class→disposition map is m-3 config keyed on m-2's `authority_class`, composes into the artifact; confirmed, no correction. R2 preserved throughout (class-keyed, never model); no c1/c2 contract reopened (additive dispositions consistent with the exactly-one-outcome seam). Re-review: `c4-cq-gateconfig` → m-3.implementer (adversarial review — closure-gate leg per `…-013323`); cross-domain co-sign CQ-4 with m-2 + m-6. **Byte-consistency sweep (rev, per m-3.implementer `DESIGN-REVIEW-…-020509` must-revise — intended shape approved, stale text blocked):** patched the residual pre-fold lines a builder could read as the old behavior — §1:15 (`self_reported` "is delivered" → non-authority-only, authority→held), §1:19 + §5.1:101 + §13:200 (`submitted→accepted` two-state → the readiness-fix-c1 **no-`submitted`-limbo** terminal-outcome seam), §6:111 ("no delivery gate branches on either label" → non-authority-only + the one decision-② exception), §12:175 (opaque-lane "deliver-and-label" → class-conditional). Surfaced (not silently resolved): the `authority_class ∧ record_integrity==mixed` edge — folded key stayed `== self_reported` as m-2 declared until reconciled. *(Resolved c6 per the VP-ratified **CQ-2 canonical** — authority-class `mixed` now also `held` under the §6 pessimistic rule; the closed CQ-4 is superseded as the vehicle. See §6:111 / §3.2(d).)*
 - **`c4-cq-slotin` (20260702) — CQ-5 observe-pipeline co-sign (bounded, additive).** Basis: orchestrator CQ-5 dispatch + poke (`c4-cq-slotin/…-012537`, `…-024102`); m-5.planner answer `c4-cq-slotin/DESIGN-planner-20260702-014506` (m-5.implementer approved `…-020448`). **Co-signed the observe-pipeline half:** the conductor classifies `slot_in` at work-record acceptance **post-form/lineage-gate, PRE-observe-hook, atomic-bind-with-observation** matches — and is *required* by — locked m-3 §5.1/§2 (the observe done-predicate is keyed on `slot_in`; §3.1 allowlist excludes `slot_in` from the hook's writes ⇒ a distinct conductor step must set it pre-observe; the atomic bind is the §2 TOCTOU closure). **Pins** the previously-open §5.1/§13 pipeline point (was "a PLAN detail"). No m-3 contract change beyond recording the pin; immutability + non-lane-writability enforcement stay PLAN. R2/C2.4 preserved (`slot_in` selects an observe-invariant, never keys a model or an m-2 predicate). Co-sign relay + m-3.implementer review = the m-3 legs of the CQ-5 triad.
 - **`c5-fold-decision-5` (20260702) — decision-⑤ ODB model-name egress carve-out (owner-authored, joint).** Basis: orchestrator COORD `c5-fold-decision-5/DESIGN-orchestrator-planner-20260702-132752`; operator decision ⑤ (`READINESS-REGISTER.md`). **m-3 half (§7):** a typed, narrow confidentiality-class exemption — `record_kind==ODB` (operator-facing, conductor-generated) ∧ `field==model_name` (m-6's exempt-marked field) ∧ `destination==operator`, exempt from the **confidentiality** class only; safety/content class + all other model-name egress unchanged; fail-closed default preserved; governed-surface scoped (conductor-generated, non-lane-writable field — no lane *tool* marks it; confusion-resistant, D5 direct-store residual §11); away-transport still m-6 away-bridge-gated; non-away renders local. **R2 untouched** (egress-confidentiality, not gate-referenceability). §12 decision + §13 egress-fixture pair added; m-7 flagged for the egress-fixture update. Joint close needs all three co-confirms (m-3 scan + m-6 ODB + m-4 R2-guard). Semantic review: `c5-fold-decision-5` → m-3.implementer.
 - **`c5-claim-sweep-light` (20260702) — claim-text hygiene (no mechanism change).** Basis: orchestrator batched COORD `c5-claim-sweep-light/DESIGN-orchestrator-planner-20260702-132139`; ratified checklist `c5-claim-sweep-architecture/…-131320` (VP-approved). **RELABELed** (malicious-seat-containment overclaims → confusion-resistant + D5 residual): §1:20 m-1 "sole-writer" → *governed* sole-writer within the governance surface (mirror m-1); §5:98 "verifier-tamper-resistant invariants" → *observer-selected (confusion-resistant)*; §5.1:101 "the tamper-resistance the observe story depends on" → *confusion-resistance* (no lane *tool* rewrites the tag; D5 residual); §7:118 egress "sole external sender / structurally unbypassable" → *conductor-governed egress chokepoint* + content-safety best-effort/encoding-evadable + D5 residual; §11:158 "structural chokepoint (sole external sender)" → *conductor-governed* + a consolidated **claim-boundary** paragraph (confusion-resistant/observer-selected, D5 accepted-risks, `record_integrity` recomputable⇒forgery-detectable). **KEPT** (licensed classes): the atomic `submit()` bind (control-flow property of the trusted courier, R2-class); the trigger/executor separation + §2:31-37 (trusted-courier control-flow, implementer-confirmed KEEP); the observer-selected done-predicate/invariants (framing, now globally scoped by §11); F5 novelty/comparison claims; the fixture names (§13) + fold-log. **Rev (per m-3.implementer `DESIGN-REVIEW-…-134833` must-revise): the parallel `c5-fold-decision-5` fold introduced fresh RELABEL-net vocab ("Non-forgeable" / "a lane cannot mark") that this sweep must also cover — now RELABELed at §7:122 / §12:178 / §13:204 / §15:221 to *governed-surface scoped: no lane tool marks the field; confusion-resistant; D5 direct-store residual §11*. Full-net grep re-run against the post-⑤ document: clean.** No mechanism change, no locked-contract reopen. Survivor list: `c5-claim-sweep-light/DESIGN-planner-…`; m-3.implementer semantic review = the closure leg.
-- **`c6-fix-m-3` (20260702) — re-review cleanup, DOC-ONLY (no mechanism change; lock invariants unchanged).** Basis: `c6-fix-m-3/DESIGN-orchestrator-planner-20260702-204506`; source of record `master/DESIGN-REREVIEW-2026-07-02.md` §5 + appendix (VP-concurred CONDITIONAL-GO). **Applied (m-3 domain judgment):** **m-3-F1 / x1-F5** — §4 relabeled honestly (agent supplies no arbitrary code, but suite-class entries *execute repo-resident lane code*) + a named non-locking §13 carry (unprivileged executor, no store/config/outbox handle, no signing key; m-7-hosted executor-isolation fixture). **m-4-F2 (seam)** — §9 folds the GL-1 canonical `deviated_observed := chosen_bucket ≠ declared_bucket` via the `bucket_binding_observed` atom (bucket-vs-bucket, supersedes the model-vs-rank1 verbatim formula; more R2-clean; m-4 conditions its 'equivalent fallback' bracket, CC'd). **x3-F3** — verified §6 `{self_reported, mixed}` broaden (CTO/m-3-F2) + §13 fixture exercises the `mixed` leg. **m-3-F10** — §11 detectability scoped (inconsistent forgery recompute-detectable; coherent D5 forgery is not — a residual, not a guarantee). **m-3-F4** — §13 ⑤ egress fixture given a named receiving surface (ARCHITECTURE §C4 + m-7 fixture set, receiving owner CTO/m-7). **m-3-F5** — §4/§13 operator-attested E4 gets a separate `attestation_source {conductor|operator}` provenance marker (NOT a third `evidence_integrity` value; R3 untouched). **m-3-F6** — §3.3 total observe→terminal `{accepted|rejected|held}` disposition table, keyed by authority class (VP two-axis). **m-3-F7** — §5 IMPL "no scope drift" → observable `diff_paths ⊆ scope_paths` (declared m-2 field) or struck. **x2-F4** — §2 "lane-unforgeable" → "lane-**unsubstitutable** through the tool surface" (+ D5 ground-truth residual). **Verified (CTO-applied, not redone):** m-3-F2 (§6 broaden), m-3-F3 (§2 scope), x1-F3 (§6/§15 mixed home), m-3-F8 (README). **m-3-F9 (header) — corrected in c6-rev1** (per m-3.implementer `DESIGN-REVIEW-…-210904`): the STATUS header had cited the superseded c5 `must-revise` relays `134834 / 134833`; retargeted to the approving `140748 / 140749` + the `c6-fix-m-3` entry. **§12 echo aligned:** the resolved-decision "arbitrary lane code rejected" → "arbitrary agent-supplied code rejected; suite-class repo-resident lane code only via the §4/§13 carry" (byte-consistency with the m-3-F1 §4/§13 relabel). Preserved: the four sanctioned by-construction claims, the confusion-resistant/D5-residual vocabulary, and the byte-exact `{accepted, rejected, held}` enum. Review: `c6-fix-m-3` → m-3.implementer (`DESIGN_REVIEW_VERDICT`).
+- **`c6-fix-m-3` (20260702) — re-review cleanup, DOC-ONLY (no mechanism change; lock invariants unchanged).** Basis: `c6-fix-m-3/DESIGN-orchestrator-planner-20260702-204506`; source of record `master/DESIGN-REREVIEW-2026-07-02.md` §5 + appendix (VP-concurred CONDITIONAL-GO). **Applied (m-3 domain judgment):** **m-3-F1 / x1-F5** — §4 relabeled honestly (agent supplies no arbitrary code, but suite-class entries *execute repo-resident lane code*) + a named non-locking §13 carry (unprivileged executor, no store/config/outbox handle, no signing key; m-7-hosted executor-isolation fixture). **m-4-F2 (seam)** — §9 folds the GL-1 canonical `deviated_observed := declared_bucket ≠ rank-1(recommended)` (bucket-vs-bucket, **byte-identical to m-4 §2/§9 + m-2 §17.3**; `bucket_binding_observed := chosen_model ∈ members(declared_bucket)` is the separate boolean atom; supersedes the model-vs-rank1 verbatim formula; **c6.1-corrected from an initial mis-fold that stated `chosen_bucket ≠ declared_bucket`**). **x3-F3** — verified §6 `{self_reported, mixed}` broaden (CTO/m-3-F2) + §13 fixture exercises the `mixed` leg. **m-3-F10** — §11 detectability scoped (inconsistent forgery recompute-detectable; coherent D5 forgery is not — a residual, not a guarantee). **m-3-F4** — §13 ⑤ egress fixture given a named receiving surface (ARCHITECTURE §C4 + m-7 fixture set, receiving owner CTO/m-7). **m-3-F5** — §4/§13 operator-attested E4 gets a separate `attestation_source {conductor|operator}` provenance marker (NOT a third `evidence_integrity` value; R3 untouched). **m-3-F6** — §3.3 total observe→terminal `{accepted|rejected|held}` disposition table, keyed by authority class (VP two-axis). **m-3-F7** — §5 IMPL "no scope drift" → observable `diff_paths ⊆ scope_paths` (declared m-2 field) or struck. **x2-F4** — §2 "lane-unforgeable" → "lane-**unsubstitutable** through the tool surface" (+ D5 ground-truth residual). **Verified (CTO-applied, not redone):** m-3-F2 (§6 broaden), m-3-F3 (§2 scope), x1-F3 (§6/§15 mixed home), m-3-F8 (README). **m-3-F9 (header) — corrected in c6-rev1** (per m-3.implementer `DESIGN-REVIEW-…-210904`): the STATUS header had cited the superseded c5 `must-revise` relays `134834 / 134833`; retargeted to the approving `140748 / 140749` + the `c6-fix-m-3` entry. **§12 echo aligned:** the resolved-decision "arbitrary lane code rejected" → "arbitrary agent-supplied code rejected; suite-class repo-resident lane code only via the §4/§13 carry" (byte-consistency with the m-3-F1 §4/§13 relabel). Preserved: the four sanctioned by-construction claims, the confusion-resistant/D5-residual vocabulary, and the byte-exact `{accepted, rejected, held}` enum. Review: `c6-fix-m-3` → m-3.implementer (`DESIGN_REVIEW_VERDICT`).
+- **`c6.1-confirm-m-3` (20260703) — differential convergence, DOC-ONLY (no mechanism change).** Basis: `c6.1-confirm-m-3/DESIGN-orchestrator-planner-20260702-233004` (CTO seam-arbitration) + owner-confirm `…-235604` + implementer approve `…-000123` + CTO authorization `…-005455`. **Delta-1 (§9) CONFIRMED** (CTO-applied): `deviated_observed := declared_bucket ≠ rank-1(recommended)` + separate `bucket_binding_observed := chosen_model ∈ members(declared_bucket)` — byte-identical to m-4 §9:90/93 + m-2 §17.3:312; corrects the c6 mis-fold (`chosen_bucket ≠ declared_bucket`, which was the binding check, not the deviation). **Delta-2 (§3.2(c)/token-map/§3.3-note/§7) FOLDED by m-3 (CTO-authorized):** egress is **never a terminal token** — the outbound egress-block is the **non-terminal `egress_blocked` park + A local resummon** (evaluated ONLY at the external-send chokepoint; §7 dormant-until-away-mode), **not** terminal `rejected` (removed from the acceptance-stage (a)/(b) group) **nor** `held`. Converges to m-6 §2:46/§50/§51, m-7 NF-S9:159, ARCHITECTURE :309-310. Also corrected the c4 fold-log echo (egress-fail removed from the `rejected` group) + the §7 park/`held` clarifier. Acceptance-stage `→ rejected` = predicate-false / declared-vs-observed integrity (a)/(b) only. No new token, no lock reopen, byte-exact enum preserved. Re-review: `c6.1-confirm-m-3` → m-3.implementer.
--- a/master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md	2026-07-02 22:53:53
+++ b/master/domains/m-2-forms-determinism/design/2026-06-28-v3-form-schema-design.md	2026-07-03 00:32:22
@@ -184,7 +184,7 @@
 
 - **m-1 (co-foundational; confirmed by m-1 coord 173516):** `FROM` and `ROLE` are `owner:system` / `system_only`, stamped from one certified connection (a seat address *is* `owner.role`, so ROLE derives from the same binding as FROM — a confused agent has no seat-fillable ROLE to make them mismatch; confusion-resistant, D5 residual). `relay_id`, `DISPATCH_ID`, `timestamp` courier-assigned. `PARENT_DISPATCH_ID` = `parent_picker` (courier supplies the conductor-derived candidate set + default = the woken-on relay; agent may only select within it; a free-typed parent is rejected) — **this conductor-derived candidate set is what strengthens the lineage engine to more strongly confusion-resistant** (a confused agent has no tool to free-type a false parent; **D5 residual** on a shell-bearing seat, m-1 TCB scope). `schema_version` stamped immutably (m-1 preserves + serves versioned records; **migration is m-2's read-side concern — m-1 does not migrate**). `certification` **null-reserved** in every record. The **address space** (minted seat addresses / graph topology) is `owner:system`; but a relay's **`TO`/`CC` values** are `recipient_picker` (sender-selected, courier-validated ∈ the minted address space, then delivered) — choosing recipients is not identity-forgery, so TO/CC are **not** `system_only` (only `FROM`/`ROLE` are). Lock together.
 - **m-3 (observe/evidence):** `ACTIONS_GIT_REF`, `FINAL_GIT_STATUS_SHORT` (`owner:system`, `fill_constraints: observed_value`), `EVIDENCE_TARGET` (`agent_enum_pick`, intent), `achieved_evidence` (`owner:system`, observed), per-phase **done-predicate** hook (a `required_when`/observe-gate input), executable-claim ref, egress/content-safety result. **target>achieved auto-flag** = a system-computed `*_RESULT`. All `observed_value` fields are **conductor-read from OUTSIDE the lane** (m-1 DI-5; the locked observe-as-send mechanism) — **[c5 KEEP: observer-selected control property — the observed lane cannot reach the conductor-side selection]** observer-captured, not lane-reported — and carry an **integrity label `evidence_integrity ∈ {observed, self_reported}`** (`observed` when DI-5 holds; `self_reported` honest-fallback where a Step-1 deployment cannot read out-of-lane). The m-3 observe hook is a **positive write-allowlist**: it may write ONLY this closed observed/computed set + a pass/fail veto — no `system_only`/identity/envelope write, no delivery effect (m-1 coord R3 + FYI). The ODB `completed_proof` (§17.2) inherits the `evidence_integrity` label. **Record-level rollup `record_integrity {observed|self_reported|mixed}`** (`owner:system`, `computed_result`; conductor rollup, m-3 `:107-112`) is declared in §17.6 — a pure function of the per-field stamps, ODB/m-6/audit-consumed, **not** gate-referenceable **[c5 KEEP: R2 gate-grammar invariant]**, **not lane-writable** (conductor-computed; no lane write tool — confusion-resistant, D5 residual) — so the whole m-3 write-allowlist resolves to an m-2-declared set (m-1 requirement + canonical-iff-consumed).
-- **m-4 (routing-record) — Q-C RESOLVED (rev2): a SEPARATE seat-stamped routing relay, not a dispatch header** (model is payload, never a work-relay gate input — pillar :33). Record fields **mirror m-4's locked record `:200-210`** (full spec §17.3; readiness-fix-c4 4b): `routing_assignments` (`row_array`, `seat_scoped_enum` planner/orch-planner — altitude-B; **per row:** `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned|resolved`, model-identity ⇒ non-gate-referenceable), `pin_mode`, `declared_deviated:bool`, `seat_archetype` (opaque, m-5 c3 — carries the archetype default posture, m-5 `:103`), `authority_ceiling` (resolved-at-spawn, recorded — F2)) — **no standalone posture field** (CTO `001537`; posture rides `seat_archetype`), `capability_prior_snapshot` (`owner:system`, computed/observed, replay-complete), `justified_deviation` (`free_text`, `required_when` `any(routing_assignments.declared_deviated == true)` — keyed on the **planner-declared** deviation bit (`agent_enum_pick`), never `selected_model` and never the computed observe-side `deviated_observed`; readiness-fix-c4), `deviation_reason_code` (`agent_enum_pick`, **same required-when grain** as `justified_deviation`), `routing_record_kind` (own named-enum, DESIGN_RECORD_KIND *shape* not values), `constraints` (**reserved-shape**, no Step-1 values) + `template_ref` (**reserved-shape**; set when spawned from a template, null otherwise — m-4 `:209`), `outcome_feedback_ref` (`owner:system`, `id_ref`, null-reserved v3.1). The work dispatch references the *accepted* routing relay via `parent_picker` (m-1 owns that candidate-set half). m-4 owns the record contract; m-2 mirrors it — m-4 confirms the mirror.
+- **m-4 (routing-record) — Q-C RESOLVED (rev2): a SEPARATE seat-stamped routing relay, not a dispatch header** (model is payload, never a work-relay gate input — pillar :33). Record fields **mirror m-4's locked record `:200-210`** (full spec §17.3; readiness-fix-c4 4b): `routing_assignments` (`row_array`, `seat_scoped_enum` planner/orch-planner **(+ `operator` on `template_ref`-bearing template-spawn records only — m-4 §7/`:208`, m-4-F4; c6.1)** — altitude-B; **per row:** `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned|resolved`, model-identity ⇒ non-gate-referenceable), `pin_mode`, `declared_deviated:bool` (planner/orch-planner; `operator` on template-spawn), `seat_archetype` (opaque, m-5 c3 — carries the archetype default posture, m-5 `:103`), `authority_ceiling` (resolved-at-spawn, recorded — F2)) — **no standalone posture field** (CTO `001537`; posture rides `seat_archetype`), `capability_prior_snapshot` (`owner:system`, computed/observed, replay-complete), `justified_deviation` (`free_text`, `required_when` `any(routing_assignments.declared_deviated == true)` — keyed on the **planner-declared** deviation bit (`agent_enum_pick`), never `selected_model` and never the computed observe-side `deviated_observed`; readiness-fix-c4), `deviation_reason_code` (`agent_enum_pick`, **same required-when grain** as `justified_deviation`; **value-set = config-sourced enum** — default-seeded + operator-configurable + `other` fail-safe, m-4 §5/§6, m-4-F5; c6.1), `routing_record_kind` (own named-enum, DESIGN_RECORD_KIND *shape* not values), `constraints` (**reserved-shape**, no Step-1 values) + `template_ref` (**reserved-shape**; set when spawned from a template, null otherwise — m-4 `:209`), `outcome_feedback_ref` (`owner:system`, `id_ref`, null-reserved v3.1). The work dispatch references the *accepted* routing relay via `parent_picker` (m-1 owns that candidate-set half). m-4 owns the record contract; m-2 mirrors it — m-4 confirms the mirror.
 - **m-6 (human-gate/email) — rev2 refined (G1/G2/Q-B; full specs §17.1–17.2):** `HUMAN_GATE_REQUIRED` (`fill_constraints: monotonic` **hybrid** — system floor, agent RAISE-able within `[floor, MAX]`, floor = MAX across raisers; §3 G1). `gate_category` (canonical id, m-2-F10 — the retired `human_gate_reason` alias folded away) = **config-sourced closed enum** (byte-exact §J2 set, **m-6-owned A/B map**: A e.g. `merge_to_protected`/`irreversible_write`/`residual_risk_acceptance`/`live_verify_skip`/`ceremony_downgrade`/`authz_security`/`product_semantics`/`scope_expansion`; B `merge_feature_to_feature`/`routing`/`sequencing`/`scope_within_bounds`; `other`→A fail-safe — full spec §17.1) — drives the gate→email bucket split mechanically (canonical-iff-consumed). `delivery_state` {accepted|rejected|held} + `failing_edge` = `owner:system` computed slots (so the bucket-D lint-bounce email states why; `rejected` = the readiness-fix-c1 terminal-bounce record; `held` = m-7 §6 internal-fault-on-authority + CQ-2 fail-closed, drives the bucket-A ODB/park lane — §17.1). The **Owner Decision Brief** ~10-field sub-schema (§17.2): agent-scripts 7 + retyped `subject_ref`(id_ref)/`completed_proof`(evidence_ref from m-3, never free-text)/`choices`(row_array) + `gate_category` + null-reserved `decision_deadline`/`on_timeout`. m-2 declares the slots; m-6 owns the bucket-projection + scheduler mechanism. **Conductor-derived `surface_intent {progress|review_checkpoint|advisory|result}`** (`owner:system`, `computed_result`, **non-gate-bearing records only**) is declared in §17.6 — m-5 owns the vocabulary, m-6 binds `(posture × surface_intent)` (`m-6 …:113-116`); gate-bearing records carry **no** `surface_intent` (bind off the locked `gate_category`/HUMAN_GATE, C2.4).
 - **m-5 (workflows/archetypes; parameterizes m-2, review-on-CC):** `slot_in` reserved atom (§5; tag-space m-5-owned, c3); `seat_archetype` + `authority_ceiling` ride the **F2 per-assignment home** in `routing_assignments` (§17.3, opaque/m-5-valued c3); **posture rides `seat_archetype`, not a standalone field** (CTO `001537`; m-5 c3 lock `:142`). `posture` + `surface_intent` value-enums are **m-5-owned delivery vocabulary LOCKED at c3** (§17.6, m-6-bound) — *not* opaque/reserved (their values are fixed, unlike `seat_archetype`/`slot_in`). m-2 declares the shape/vocabulary; m-5 owns the values (no tag-value micro-fold).
 
@@ -234,7 +234,7 @@
 9. `HUMAN_GATE_REQUIRED` is agent-**RAISE-able** (hybrid `monotonic`): a seat may raise the gate to or above the system floor but never lower it; the floor is the MAX across all raisers (rev2 G1). Negative fixture: a lower-than-floor write is rejected.
 10. `gate_category` (canonical id, m-2-F10) is a **closed enum** that mechanically drives the gate→email bucket split (A human-only vs B orchestrator-absorbed); `delivery_state`+`failing_edge` are readable computed slots for the bucket-D bounce (rev2 G2).
 11. The Owner Decision Brief sub-schema (§17.2) is encoded with `completed_proof` as an m-3 `evidence_ref` (never agent free-text) and `choices` as a `row_array` the operator reply selects within (rev2 Q-B).
-12. The routing decision is a **separate seat-stamped routing relay** (§17.3) with `routing_assignments` seat-scoped to planner/orch-planner; model never appears in a work relay's authority header (rev2 Q-C). `slot_in` is a reserved atom with no Step-1 values (rev2 #5).
+12. The routing decision is a **separate seat-stamped routing relay** (§17.3) with `routing_assignments` seat-scoped to planner/orch-planner **(+ `operator` on `template_ref`-bearing template-spawn records only — m-4 §7/`:208`, m-4-F4; c6.1)**; model never appears in a work relay's authority header (rev2 Q-C). `slot_in` is a reserved atom with no Step-1 values (rev2 #5).
 13. **(readiness-fix-c1)** The write path has **no persisted `submitted` state**: form+lineage run **pre-append** over `persisted ∪ {candidate}` with the candidate in-courier; on pass exactly **one atomic `accepted` append** commits; on fail a **terminal `rejected` record** + bounce is produced. **Negative fixture:** no code path can leave a `submitted`-but-non-`accepted`/non-`rejected` record in the store; a lineage-failing authority record is never delivered/consumed and lands as terminal `rejected`. Step-1 requires **no** m-3 observe gate (observe is a reserved Step-2 hook).
 14. **(readiness-fix-c4)** R2 is **grammar-enforced**: the `field:<id>` predicate atom cannot reference a model-identity field, and `justified_deviation` keys on the **planner-declared** `declared_deviated` bit (`agent_enum_pick`, aggregated over `routing_assignments`), not `selected_model` and not the computed observe-side `deviated_observed`. **Negative fixture:** a required-when/visible-when predicate that names `chosen_model` (or any field flagged model-identity) is **rejected by the schema grammar** (not merely unused); model is provably not a gate input on any code path **[c5 KEEP: R2 gate-grammar invariant + negative fixture — a control-flow property of the predicate grammar, holds regardless of seat behavior]**. **Declare/observe split fixture (m-4 seam):** `declared_deviated` (declared, gate-side) and `deviated_observed` (system-computed, non-gate-referenced) are distinct fields, so the `(declared=false, observed=true)` silent-deviation veto has two independent bits to compare.
 15. **(readiness-fix-c4 4b+4c)** m-2's routing FieldSpec (§17.3/§12) **mirrors m-4's locked record `:200-210`** — full per-row `routing_assignments` fields + `deviation_reason_code` (same required-when grain as `justified_deviation`) + reserved-shape `constraints`/`template_ref`; and the conductor-computed fields `record_integrity`/`surface_intent` have **declared homes** (§17.6), so the m-3 write-allowlist resolves to an m-2-declared set and every consumed field has a registry entry (canonical-iff-consumed). **Posture is NOT a standalone field** — it rides the recorded `seat_archetype` (CTO arbitration `001537`; m-5 c3 lock `:142`); m-2 declares only the m-5-owned posture value-enum m-6 binds. **Fixtures:** no consumed field lacks a declaration; the m-2 **registry declares no value-enums** for the reserved/opaque fields (`seat_archetype`, `authority_ceiling`, `constraints`, `template_ref`, `slot_in`) — their values are m-5-config-/template-sourced and **opaque to the schema gate** (so a template-spawned or F2 record MAY carry Step-1 record values for `template_ref`/`seat_archetype`/`authority_ceiling` per GL-4/m-5 §9/m-7 S11, while the schema still enumerates none — the no-micro-fold boundary; `constraints`/`slot_in` remain reserved-shape with no Step-1 values); no computed label (`record_integrity`/`surface_intent`) is gate-referenceable; no standalone posture field exists on `routing_assignments`. Confirmed by m-4 (record mirror) + m-3/m-5/m-6 (their field homes).
@@ -278,7 +278,7 @@
 - `delivery_state` — `type enum {accepted, rejected, held}`, `owner system`, `computed_result` (the §4 write-path **terminal-outcome token**, readable; **exactly one outcome per record, no `submitted` limbo** — mirrors m-7's locked outcome structure). **CQ-4 settled (c4-cq-gateconfig; m-2 spelling + homes, joint with m-3/m-6):**
   - **`accepted`** — committed to the store + delivered to recipient/consumers. **An A-gate record that passes form/lineage/gate is `accepted`** and delivered into the A-gate **ODB/park lane** (m-6) — ordinary `HUMAN_GATE_REQUIRED` parking is `accepted`, **not** `held` (m-6 `015800` mapping).
   - **`rejected`** — form/lineage fail ⇒ terminal bounce record; never delivered/consumed (readiness-fix-c1 seam). **Canonical spelling `rejected`; legacy `bounced` is unified away (resolves Q-E).**
-  - **`held`** (new token, CQ-4) — the **HELD field-home** a new consumed token needs (m-7 never invents schema). **Narrowed to the m-7 §6 / m-6 semantics (per m-2.implementer review `020503`):** `held` is the **internal-fault / check-could-not-run** outcome for an **authority-bearing record** — **not accepted** (a crash/timeout is not a yes; K8s `failurePolicy: Fail`), **not rejected** (the check *failed to run*, it did not *say no*) — parked **operator-visible** (m-6 A-surface), resolved by re-run or operator verdict. **Exactly two producers:** (i) a trusted-side check that **throws / times out / reads corrupt data** on an authority record (m-7 §6, `:97-104`); (ii) the **CQ-2 class-conditional fail-closed** disposition — `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` (§17.6; `mixed` = a partially-unverified authority record, held pessimistically per the CQ-2 canonical). **`held` is a *terminal outcome for the intake command*** — represented append-only as the candidate + a **held-disposition record referencing it + the `intake_id`**; any re-run / operator verdict is a **subsequent record, never a mutation** of the held record. **NOT the removed pre-gate `submitted` limbo** (the record IS committed; only recipient release is withheld pending resolution).
+  - **`held`** (new token, CQ-4) — the **HELD field-home** a new consumed token needs (m-7 never invents schema). **Narrowed to the m-7 §6 / m-6 semantics (per m-2.implementer review `020503`):** `held` is the **internal-fault / check-could-not-run** outcome for an **authority-bearing record** — **not accepted** (a crash/timeout is not a yes; K8s `failurePolicy: Fail`), **not rejected** (the check *failed to run*, it did not *say no*) — parked **operator-visible** (m-6 A-surface), resolved by re-run or operator verdict. **Exactly two producers:** (i) a trusted-side check that **throws / times out / reads corrupt data** on an authority record (m-7 §6, `:97-104`); (ii) the **CQ-2 class-conditional fail-closed** disposition — `authority_class == true ∧ record_integrity ∈ {self_reported, mixed}` (§17.6; `mixed` = a partially-unverified authority record, held pessimistically per the CQ-2 canonical). **`held` is a *terminal outcome for the intake command*** — represented append-only as **one compound canonical record**: the **held-disposition record embeds the candidate** + references the `intake_id` (m-7 §6 / **m-7-F1** one-pivot-per-mutation; supersedes the earlier two-record "candidate + separate disposition record" shape, whose two renames opened a crash window); any re-run / operator verdict is a **subsequent record, never a mutation** of the held record. **NOT the removed pre-gate `submitted` limbo** (the record IS committed; only recipient release is withheld pending resolution).
   - **m-3 edge (CQ-4, SETTLED):** m-3 settled it in the same CQ-4 thread — the Step-2 observe-bounce **shares `rejected`** (the check ran and said no; m-3 §2 `:31`). The `delivery_state` token set is therefore **closed at three** `{accepted, rejected, held}`; a distinct fourth/observe-specific token is **no longer admissible** (it would violate the byte-exact m-7 terminal-enum lock). The observe-side mapping onto `rejected` stays m-3-owned. **m-6 edge (co-signed `015800`):** bucket-D aligns to `rejected`; `held` drives the **bucket-A ODB/park lane**, not bucket-D.
 - `failing_edge` — `type id_ref|string`, `owner system`, `computed_result`, null unless `delivery_state = rejected` (names the failed lineage/form edge so the bucket-D bounce email states why). For `delivery_state = held`, the held-reason is carried by `gate_category` + the escalation/ODB ref, not `failing_edge`.
 
@@ -306,12 +306,12 @@
 
 | field | type | owner | fill_constraints / note |
 |---|---|---|---|
-| `routing_assignments` | row_array | **seat_scoped_enum** (planner / orch-planner only) | altitude-B fill-time authority. **Per-row fields (mirror m-4 `:203`, readiness-fix-c4 4b):** `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned\|resolved` — **model-identity ⇒ non-gate-referenceable**, R2), `pin_mode` (`pinned\|slot`), `declared_deviated:bool` (per-row; the gate trigger, §17.3/§5), `seat_archetype` (**opaque; concrete values m-5-owned, c3** — also **carries the archetype's default human-mode/posture** as a recorded attribute, m-5 `:103`), `authority_ceiling` (**resolved-at-spawn, recorded per assignment** for replay/audit on both template + hand-authored paths — the F2 fold; opaque, m-5-valued c3). **Propose-vs-stamp per-column ownership (m-5-F1 seam; confused-deputy fix):** within a `routing_assignments` row the planner *proposes* `seat`/`role`/`seat_archetype` from a **fill-time-pruned candidate set** (fill-time authority, §3), and the **conductor *stamps* the resolved `seat_archetype` + `authority_ceiling` per-column** (`fill_constraints: system_only` on those two columns — system ownership *inside* the row) — so a lane cannot self-elevate its own ceiling by authoring the row; the ceiling that denied write authority is conductor-stamped and replay-complete. m-4 (routing record) + m-5 (archetype registry) own the propose/candidate-set + stamp mechanism; m-2 declares the per-column ownership split alongside the §2C per-column FieldSpec work. **No standalone posture field** (CTO arbitration `001537`): posture is **not** its own `routing_assignments` field — it **rides `seat_archetype`** (m-5 c3 lock `:142`, "no new m-2 field"); m-6 resolves posture by reading the recorded `seat_archetype`. m-4 owns the record contract (`:203`, unchanged); m-2 mirrors it as FieldSpec |
+| `routing_assignments` | row_array | **seat_scoped_enum** (planner / orch-planner; **+ `operator` on `template_ref`-bearing template-spawn records only** — GL-4, mirrors m-4 §7/`:208`, m-4-F4) | altitude-B fill-time authority. **Per-row fields (mirror m-4 `:203`, readiness-fix-c4 4b):** `seat`, `role`, `task_tag`, `declared_bucket`, `chosen_model` (`pinned\|resolved` — **model-identity ⇒ non-gate-referenceable**, R2), `pin_mode` (`pinned\|slot`), `declared_deviated:bool` (per-row; the gate trigger, §17.3/§5), `seat_archetype` (**opaque; concrete values m-5-owned, c3** — also **carries the archetype's default human-mode/posture** as a recorded attribute, m-5 `:103`), `authority_ceiling` (**resolved-at-spawn, recorded per assignment** for replay/audit on both template + hand-authored paths — the F2 fold; opaque, m-5-valued c3). **Propose-vs-stamp per-column ownership (m-5-F1 seam; confused-deputy fix):** within a `routing_assignments` row the planner *proposes* `seat`/`role`/`seat_archetype` from a **fill-time-pruned candidate set** (fill-time authority, §3), and the **conductor *stamps* the resolved `seat_archetype` + `authority_ceiling` per-column** (`fill_constraints: system_only` on those two columns — system ownership *inside* the row) — so a lane cannot self-elevate its own ceiling by authoring the row; the ceiling that denied write authority is conductor-stamped and replay-complete. m-4 (routing record) + m-5 (archetype registry) own the propose/candidate-set + stamp mechanism; m-2 declares the per-column ownership split alongside the §2C per-column FieldSpec work. **No standalone posture field** (CTO arbitration `001537`): posture is **not** its own `routing_assignments` field — it **rides `seat_archetype`** (m-5 c3 lock `:142`, "no new m-2 field"); m-6 resolves posture by reading the recorded `seat_archetype`. m-4 owns the record contract (`:203`/`:208`, template-spawn author widened per m-4-F4); m-2 mirrors it as FieldSpec |
 | `capability_prior_snapshot` | object | system | `computed_result`/`observed_value` — the prior in effect at decision time, **replay-complete** |
-| `declared_deviated` | bool | **agent_enum_pick** (planner / orch-planner — **declared**, gate-side) | the routing seat's **declared** deviation bit — a plain boolean it asserts when deviating from the capability-prior floor; **per-row of `routing_assignments`** (altitude-B fan-out grain). The single model-*touching* trigger a gate may read, and it reads a **plain bit — no model comparison enters the gate** (m-4 §2.1 boxed R2 invariant). This is the `required_when` trigger (m-4 owns the contract; retyped per m-4 confirm `231200`) |
+| `declared_deviated` | bool | **agent_enum_pick** (planner / orch-planner; **`operator` on template-spawn records** — **declared**, gate-side) | the routing seat's **declared** deviation bit — a plain boolean it asserts when deviating from the capability-prior floor; **per-row of `routing_assignments`** (altitude-B fan-out grain). The single model-*touching* trigger a gate may read, and it reads a **plain bit — no model comparison enters the gate** (m-4 §2.1 boxed R2 invariant). This is the `required_when` trigger (m-4 owns the contract; retyped per m-4 confirm `231200`) |
 | `deviated_observed` | bool | system | `computed_result` — the conductor **observe-side** bucket-vs-bucket computation (`declared_bucket ≠ rank-1(recommended)` vs `capability_prior_snapshot`); **m-3-owned observed evidence** (`evidence_integrity: observed`). **NOT gate-referenceable** and **not** what `justified_deviation` reads — carried only for replay-completeness + the `(declared=false, observed=true)` **silent-deviation integrity veto** (m-4 §2:106–122). Owned/computed by m-4/m-3; m-2 only carries it |
 | `justified_deviation` | free_text | free_text | **required_when** `any(routing_assignments.declared_deviated == true)` (readiness-fix-c4 — keyed on the **planner-declared** bit, aggregated over the fan-out rows so no deviating row escapes justification; **not** `selected_model`, **not** the computed `deviated_observed`; R2: model is never a gate input — c5 KEEP: R2 gate-grammar invariant) |
-| `deviation_reason_code` | enum | agent_enum_pick | machine-readable reason code paired with the `justified_deviation` narrative (mirror m-4 `:206`, readiness-fix-c4 4b). **required_when** `any(routing_assignments.declared_deviated == true)` — **same grain/treatment** as `justified_deviation`, so the reason code can never be omitted where policy makes it load-bearing |
+| `deviation_reason_code` | enum | agent_enum_pick | machine-readable reason code paired with the `justified_deviation` narrative; **value-set = config-sourced enum (default-seeded + operator-configurable + `other` fail-safe — mirror m-4 §5/§6, m-4-F5)** (mirror m-4 `:206`, readiness-fix-c4 4b). **required_when** `any(routing_assignments.declared_deviated == true)` — **same grain/treatment** as `justified_deviation`, so the reason code can never be omitted where policy makes it load-bearing |
 | `routing_record_kind` | enum | agent_enum_pick | own named-enum — **DESIGN_RECORD_KIND *shape*, not its values** |
 | `constraints` | object | agent_enum_pick / free_text | budget/latency/privacy — **reserved/forward** (mirror m-4 `:207`; **shape only, no Step-1 values**, non-gate — same reserved-seam pattern as `slot_in`/`certification`) |
 | `template_ref` | id_ref | system | **set when spawned from a template; null otherwise** (m-4 `:209` verbatim — the source this row mirrors). Templates land with m-5 (**c3**) and are selectable in the **v3.0/Step-1 release** (GL-4, locked c2), so a template-spawned record **carries a `template_ref` value at Step-1**; `lineage_role: none` — reserved-*shape* declaration (the schema enumerates no values; the value is m-5-template-sourced, opaque to the gate) |
--- a/master/domains/m-5-workflows-archetypes/design/2026-06-30-v3-archetype-system-design.md	2026-07-02 22:53:53
+++ b/master/domains/m-5-workflows-archetypes/design/2026-06-30-v3-archetype-system-design.md	2026-07-02 22:57:32
@@ -160,7 +160,7 @@
 The archetype system DESIGN is Step-1-rideable as a **recorded governance contract**; ceiling ENFORCEMENT is **tiered**:
 - **Step 1 (ride existing runtimes):** archetype + ceiling DECLARED + recorded per-assignment (F2) + enforced **best-effort by host config** — real on claude-code's agent-type `tools[]`/`disallowedTools[]` allowlist (`agentToolUtils.ts:122-225`); partial/host-dependent elsewhere. The governance VALUE (recorded, auditable, fail-closed routing-refusal) holds regardless.
 - **Steps 4-5 (standalone runtime):** the conductor enforces ceilings + topology uniformly (ROADMAP Step-5 "a human picks a workflow at spawn that sets the authority ceiling"). The literal single-action actuator (§7.2) + the `external_send` axis (§5) + uniform tool-blocking specifically need this.
-- **Observe-invariant phasing (mirrors the ceiling tiering — resolves the §7-T1-vs-§9 read):** the **observe-as-send gate mechanism is always-on from Step-1** (it is the locked m-3 send-gate; every send observes). The per-`slot_in` observe-invariant **families** (§4/§7 — refactor `test-files-unchanged`, bugfix `red→green`, …) **bite from Step-2**, when m-3's observe pipeline + done-predicates land. Step-1 records the `slot_in` classification (F1) and rides the always-on gate; the invariant families enforce as m-3 ships them — the same **declared-now / enforced-as-the-tier-lands** shape as the ceiling above. So T1's "observe-as-send always-on" (§7) names the *gate mechanism* (Step-1), not a claim that every `slot_in` family predicate is enforced pre-Step-2.
+- **Observe-invariant phasing (mirrors the ceiling tiering — resolves the §7-T1-vs-§9 read):** the observe-as-send **send-gate is the locked m-3 chokepoint *by design***, but in **Step-1 the observe hook is inert** — no observe writer (m-1 §5: "Step-1 records carry **no observe gate**"; m-7 NF-S5 / the CQ-1(a) step-gate), so Step-1 sends pass the chokepoint **without an observe predicate** (the observe pipeline + done-predicates land at Step-2). The per-`slot_in` observe-invariant **families** (§4/§7 — refactor `test-files-unchanged`, bugfix `red→green`, …) **bite from Step-2**, when m-3's observe pipeline + done-predicates land. Step-1 records the `slot_in` classification (F1) and rides the always-on gate; the invariant families enforce as m-3 ships them — the same **declared-now / enforced-as-the-tier-lands** shape as the ceiling above. So T1's "observe-as-send always-on" (§7) names the *gate mechanism* (Step-1), not a claim that every `slot_in` family predicate is enforced pre-Step-2.
 - **No re-cut:** the ceiling is already recorded per-assignment (F2) as an open named-axis map (§5); standalone enforcement reads the same fields. **Dependency routed to the orchestrator** as a later-step build concern.
 
 ---
--- a/master/domains/m-7-conductor-core/design/2026-07-01-v3-conductor-core-design.md	2026-07-02 22:53:53
+++ b/master/domains/m-7-conductor-core/design/2026-07-01-v3-conductor-core-design.md	2026-07-02 22:57:35
@@ -158,7 +158,7 @@
 | S8 | pure-judgment A-floor (m-2/m-6) | CQ-3 triad (m-6 table + m-2 mechanics); §2A.6 | enforce the **closed CQ-3 (phase × record_kind) A-floor table** at §3 step 2a via the existing monotonic MAX (no new m-2 mechanism); CEREMONY_TIER monotonic | **NF-S8 (bound to CQ-3):** a submit picking below the table's floor for its (phase × record_kind) ⇒ floor wins (monotonic MAX), `gate_category=ceremony_downgrade` auto-set; a table-absent (phase × record_kind) pair ⇒ the `other`→A fail-safe | CQ-3 **CLOSED** |
 | S9 | egress gate (m-3) | m-3 §7 | §9 chokepoint | **NF-S9:** an outbox item flagged by the scan ⇒ blocked + `egress_blocked` park + local resummon; **no conductor-owned code path** sends unscanned bytes externally (the outbox drain is **the conductor's only component that writes to non-seat destinations** — seat pipes/MCP channels are seat-facing and out of this claim's grain (c6 m-7-F6 reword); a governance-surface claim; the D5 same-uid seat residual per §9 applies) | — |
 | S10 | routing record + route_dispatch (m-4) | m-4 §3, §5 | §3 acceptance; host fail-closed refusal | **NF-S10:** no acceptable route ⇒ `routing_unavailable`/`human_decision_required`, never a silent default; engine gate evaluation over a routing record never reads `chosen_model` (R2 grammar — m-2 AC14 hosted) | — |
-| S11 | template/pane spawn (m-4/m-5) | m-4 GL-4 §7; m-5 §7 | spawn panes (existing multiplexer), deliver boot relays, record ceiling per assignment | **NF-S11:** an off-floor template slot spawn emits a `routing_decision` with `declared_deviated=true` + snapshotted justification (no-bypass); spawn above any ceiling axis ⇒ fail-closed refusal | — |
+| S11 | template/pane spawn (m-4/m-5) | m-4 GL-4 §7; m-5 §7 | spawn panes (existing multiplexer), deliver boot relays, record ceiling per assignment | **NF-S11:** an off-floor template slot spawn emits a `routing_decision` (**authored `FROM=operator` on the operator-relay channel — m-4 §7/GL-4, m-4-F4; the routing_assignments seat-scope admits `operator` only on `template_ref`-bearing records**) with `declared_deviated=true` + snapshotted justification (no-bypass); spawn above any ceiling axis ⇒ fail-closed refusal | — |
 | S12 | `slot_in` classification (m-3/m-5) | CQ-5 triad (m-5 + m-3 co-sign); m-3 §5.1; m-5 §4 | §3 step 3 — the **closed CQ-5 ordering: classify at acceptance, post-gate / pre-observe / atomic-bind** (m-3/m-5-confirmed; required by locked m-5 §4) | **NF-S12 (bound to CQ-5):** a lane-supplied `slot_in` value in a submit payload ⇒ ignored; the conductor-classified value is on the committed record, bound in the same atomic commit as the observation it selected, and immutable; a re-tag attempt post-acceptance has no verb to express it | CQ-5 **CLOSED** |
 | S13 | scheduler park/wake (m-6) | m-6 §4 | §11 | **NF-S13:** kill -9 with three lanes parked ⇒ Phase-3 restores all three `parked_waiting_human` states from store records alone; wake before the verdict record commits is impossible (wake is pipe-write sequenced after commit) | — |
 | S14 | buckets + ODB delivery (m-6) | m-6 §2, §3 | mailbox projection + ODB render slots | **NF-S14:** a bucket-D-class bounce never appears in the operator mailbox (author-facing only); an A-gate record is non-suppressible (appears regardless of digest/batch config) | CQ-4 (token) |
--- a/master/ARCHITECTURE.md	2026-07-02 22:53:53
+++ b/master/ARCHITECTURE.md	2026-07-03 01:56:21
@@ -109,6 +109,7 @@
 preclude it.*
 - **A (reserved-to-human):** `merge_to_protected` · `irreversible_write` · `residual_risk_acceptance` ·
   `live_verify_skip` · `ceremony_downgrade` · `authz_security` · `product_semantics` · `scope_expansion`.
+  *(Routing-escalation force-A is reached via the `other`→A fail-safe below — m-4 §7:365-367 "no §J2 change required for correctness"; a distinct explicit `routing_escalation` member is an owed clarity carry, §C4. `routing_unavailable` is the route_dispatch **outcome state** (§C2 route_dispatch), **not** a §J2 member.)*
 - **B (orchestrator-absorbed):** `merge_feature_to_feature` · `routing` (model = payload) · `sequencing` ·
   `scope_within_bounds`.
 - **Fail-safe:** `other` → A (`human_decision_required`) — unclassified ⇒ human-only, never auto-absorbed.
@@ -472,3 +473,5 @@
 **Owed Step-1-build fixtures (registration owed — recorded here so a (d)/Step-1 builder inherits them; flagged by the c6 pairs, receiving side CTO/m-7):**
 - **③ known-A / RAISE-ONLY direction-invariant NF fixture** (m-7-side; flagged owed by m-2 `c6-fix-m-2` + m-6 §2, §J-ratified) — a B-pick / B-absorb over a known-A category ⇒ **raised to A + `gate_category` recorded**, never silently orchestrator-absorbed. Bound to Step-1 build; not yet in m-7's locked design-of-record (which locked before the ③ fold).
 - **⑤ ODB model-name egress fixture** (m-3-authored (a)/(b)/(c) set, `c6-fix-m-3` §13; receiving on m-7 / this §C4) — a model-name in the **exempt ODB `model_name` slot** (decision-⑤ carve-out) passes the egress scan; a model-name **outside** the carve-out is scanned/blocked. Bound to Step-1 build.
+- **m-2 `GRILL_REQUIRED` v3 FieldSpec row** (owed, m-6-F6) — m-2 declares the ported v2.8.8 `GRILL_REQUIRED` header's FieldSpec (owner/type/values) before the m-6 meeting-lane route binds it as a field; until then the route keys on the locked `phase` atom alone (m-6 §5). An owed m-2 build item.
+- **§J2 explicit `routing_escalation` A-member** (owed cross-domain carry, m-4-F7 / x3-F1) — a **distinct** `routing_escalation` gate_category member for precise classification/telemetry of routing-escalation force-A. **Correctness already holds via the `other`→A fail-safe** (m-4 §7:365-367 — "no §J2 change required for correctness"), so this is **NOT a Step-1-PLAN blocker**; routed as a pre-wire cross-domain carry = CTO §J2 add + m-2 §J2-mirror + m-6 confirm. The member token is `routing_escalation`, distinct from the `routing_unavailable` route_dispatch outcome state (m-4 §7:363-364). *(c6.1a: an earlier c6.1 §J2 edit mis-named this member `routing_unavailable` — a differential-caught transcription slip, reverted; recorded here as the owed carry m-4 prescribed.)*
--- a/master/CYCLE-PLAYBOOK.md	2026-07-02 22:53:53
+++ b/master/CYCLE-PLAYBOOK.md	2026-07-02 23:01:57
@@ -146,6 +146,20 @@
 (m-6 + m-3); and every Part C friction is a place the engine should remove a human/lint round-trip. When m-5
 designs archetypes, start here.
 
+**Owed-item tracking is a first-class store capability, not a checklist (c6 forward input — build in Step-1).** c6 surfaced a
+failure class the relay/INDEX substrate does not catch: an actionable item **flagged *inside* a delivered relay body** (a
+cross-domain mirror, an owed fixture) can silently drop at integration **even though the relay was delivered AND read** — because
+tracking is at the *relay* grain, not the *item* grain (the dropped m-4 routing-mirror is the motivating case). The fix is **not a
+mutable checklist** (that fights the append-only grain and re-introduces the same drift — someone forgets to tick the box). It is the
+**m-7 `held`/burn pattern generalized:** an owed item is a **typed record appended when raised**, **closed by a *subsequent* record
+referencing it** (never a mutation), and its status is a **projection** — `open = owed-record with no disposition-record`. A
+cycle-close gate then *projects* "open owed items" and refuses to close while any remain. So the checklist is a **query, not a
+document you maintain**. Step-1 should build this as a first-class store capability (an owed-item `record_kind` + a `project()` over
+it), making silent-drop impossible **by construction of the projection** — exactly as the store already does for `held`. Deliberately
+deferred as a Step-1 build, **not** a manual bandaid: the design-phase fan-out that exposed this (7 parallel pairs) does not recur at
+Step-1's serial one-section-at-a-time implementation grain, so the orchestrator's own working context is a sufficient checklist until
+the real capability ships — which is then dogfooded immediately.
+
 ---
 
 ## Part A.2 — Cycle c2, as it ran (Observation & Evidence + Routing & Policy)
@@ -264,6 +278,7 @@
 - **Decline-don't-guess is a load-bearing mechanism.** Telling file-apply agents to DECLINE (not guess) anything needing a design decision turned 8 mis-classified "mechanical" findings into clean pair-dispatches instead of bad auto-edits. **Fix:** bake "decline + report" into any automated apply pass over judgment-bearing artifacts.
 - **Snapshot before a batch apply on a no-VCS workspace.** The docs tree has no git; a `cp` snapshot before the fan-out made every misedit recoverable. **Fix:** always snapshot before an automated multi-file edit pass here.
 - **Owed items get a ledger home, not a prose flag.** The ③/⑤ owed fixtures + m-5-F2 were "flagged owed" in pair docs but only became inherit-able once **enrolled in the §C4 build-carry ledger**. **Fix:** an owed/deferred item is closed only when it sits in a ledger a downstream builder reads — not when a pair doc says "owed."
+- **Seam convergence needs a dedicated adversarial differential — presence-checks and sampling pass while the *formulas* diverge (c6.1).** The c6 close certified "all seams converged" on a presence-check verify sweep (atoms present in both docs) + VP sampling; a scoped post-c6 differential then found **5 seams that had diverged on the actual formula/token** (m-3 folded `chosen_bucket ≠ declared_bucket` where the lock says `declared_bucket ≠ rank-1`; m-3 mapped egress→terminal `held`; m-5 said "every send observes" vs Step-1's no-observe boundary; two m-2 mirrors lagged m-4/m-7) **plus 3 dropped cross-domain flags** buried in delivered+read relays. **Fix:** when parallel pairs edit *toward* a shared seam, run a cheap independent differential before the close — each seam = one adversarial lane trying to find a **byte-level disagreement**, not "is the atom present." It caught what pair-review + orchestrator-sweep + reviewer-sampling all missed. (The item-level owed-ledger in Part D is the standing form of the flag-census half of this.)
 
 ---
 
