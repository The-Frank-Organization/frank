# master — `frank` team dashboard

Living status board for the standing `master` governing team **building `frank`** (the conductor — our thin trusted courier). The stable constitution is the charter
(`CLAUDE.md` / `AGENTS.md`); the sequencing plan is `ROADMAP.md`; the integrated design-of-record is
`ARCHITECTURE.md`; **how the team runs a cycle** is `CYCLE-PLAYBOOK.md` (process record + reusable template);
this file tracks live status.

## Current phase
**✦✦✦ STEP-3 STAGE-6 RE-LOCK — §11 SEQUENCE: LANE 2 CLOSED, ITEM A CLOSED — LANE 4 NEXT (as of 2026-07-27).** Lane 1 (m-7 broker study) is DONE. **Lane 2 (the interface DAG) is CLOSED** (9 settled owner bases + 5 joins) across every domain the re-scope amendment touches. **Item A is CLOSED** (`master/STEP-3-INTERFACE-LOCK.md`, external SHA-256 `cbd1893c…`; VP + F73 final approve `step3-relock-item-a/DESIGN-REVIEW-orchestrator-reviewer-20260727-210000.md`, 38/38 hashes 0-mismatch); Lane 4 (the Master+VP re-lock over the record's external SHA + freezing `STEP-3-EXIT-FIXTURES.json`) is **NEXT**, then Lane 5 (T4). Per-seat lane-2 state (pair-approved unless noted; full detail in Org status): **m-1** env/redaction component `d34a7c47…` · **m-2** §5-E component `c3a8cd61…` + the `relay.submit` cell `5ec7a3d2…` (bound into the amendment packet; m-2 frozen final `83d8e63e…` unmoved) · **m-3** lane-2 E0/E3 delta **r19 `92e08d09…`** — an HONEST-PARTIAL disposition (T1–T8 live; N910 ruled a documented MVP limit, not a gap; the r7-mirror question deferred to v3; frozen r4 `009df607…` unmoved) — plus a ratified schema-version amendment (contract `6e2abe40…`) — holding · **m-8** B/E producer `c0b7b488…` (r5) + the 2a/2b `refusal_stage` discriminator **r7 `734e44b7…`** — m-8's lane-2 producer obligations are now COMPLETE (frozen r12 `4b670a79…` unmoved) · **m-9** lane-2 delta **r12 `04422965…`** (three §5-E recipes realized; A3/B1 delegated under F73; frozen lifecycle r21 `4d3bd14e…` + worker `cb7ff970…` unmoved) — its batched final revision (§2.6 + §7 `relay.*` + §6 `turn_failed`) is HELD pending §D-settlement ratification · **m-10** B/E carriage **rev3 `cd17db32…`** PAIR-APPROVED (this is the R3 gate that unblocked m-3's r19 binding), while the producer delta sits at **rev14 `b96a1511…`, NOT pair-approved** (an ancestry/wording fold; will re-cut fresh post-ratification; frozen contract r40 `d2ce9831…` + control-plane r10 `6fd1d655…` unmoved). **THE §D-SETTLEMENT AMENDMENT — OPERATOR-RATIFIED 2026-07-25.** (`master/STEP-3-STAGE6-SETTLEMENT-AMENDMENT.md` rev4 SHA-256 `1fa71cb8…`, byte-bound as one packet with m-2's pair-approved `relay.submit` cell `5ec7a3d2…`) — VP-approved zero-findings then **operator hash-bound ratified 2026-07-25** (recorded §8b, `step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000`). The four corrections are now in force and the **propagation matrix is OPEN**. **That sequence has COMPLETED — Lane 2 (the interface DAG) is now CLOSED** (9 settled owner bases + 5 joins). **THE ITEM-A SIMPLIFICATION AMENDMENT — OPERATOR-RATIFIED 2026-07-27** (`master/STEP-3-ITEM-A-SIMPLIFICATION-AMENDMENT.md` rev7 SHA-256 `3443f73d…`, VP-approved) **WITHDRAWS the bundle mechanism** (`STEP-3-INTERFACE-BUNDLE.json`/`bundle_sha256`, the extractor tool + `--verify`, HARD markers, the `bundle-soft-stability` fixture) **and the extraction recipe** (`master/STEP-3-ITEM-A-RECIPE.md` r3 `06e6956e…`, WITHDRAWN, not built) — replaced by a plain byte-bound **interface-lock RECORD** (`master/STEP-3-INTERFACE-LOCK.md`, external SHA-256 `cbd1893c…`), the same "named at this hash; any edit voids the lock" discipline used for every other approval in the project. **Item A is now CLOSED** (VP + F73 final approve 2026-07-27, `step3-relock-item-a/DESIGN-REVIEW-orchestrator-reviewer-20260727-210000.md`). **Next: Lane 4** (the Master+VP re-lock over the record's external SHA + freezing `STEP-3-EXIT-FIXTURES.json`) **→ Lane 5** (T4). **H-12 continues to hard-block external/untrusted/security-sensitive/multi-tenant use throughout.** *(The paragraph immediately below records the 2026-07-21 stage-6 re-scope ratification that opened this §11 sequence, and stays accurate as history.)*

**✦✦ STEP-3 STAGE-6 RE-SCOPE — OPERATOR-RATIFIED 2026-07-21.** §7 stages 4–5 designed (m-9 worker `cb7ff970…` + m-10 control plane `6fd1d655…`) and both stage-6 lock halves closed on exact bytes — then a **stage-5.1 third-party review** (`master/STAGE-5.1-EXTERNAL-REVIEW-2026-07-21.md` `b4e79f3b…`) faulted the milestone CLAIM + lock SCOPE (governance kernel real, not yet honestly a coding-agent MVP). The operator kept the "frank harness MVP" label and pulled scope up; master HELD the all-artifact lock and drove a **bounded stage-6 re-scope amendment** through **twelve VP decomposition-review rounds** to **rev12 `1125b0a06697826751786bd82968354f0789f3bc98e6e9a5e8dbdb298f40e183`**, **VP-APPROVED** (r12 `step3-arch-packet/…-163600`) and **operator-RATIFIED in-session 2026-07-21** (`…-165500`, agent-authored + operator-cited per §8b). **The re-scope:** sandbox **FORGONE** for the MVP (ambient bash; **H-12 promoted to a HARD pre-external-use blocker** — no untrusted/external/security-sensitive/multi-tenant use until a real sandbox lands) · bash claim narrowed to invocation-context · a **six governance-property exit gate** (Governance · Durability · Crash-honesty · Injection-**visibility** · Handoff · Operability) + an **objective overhead budget** (F59 p95≤250ms · relay≤1s · journal-commit≤100ms · per-turn wall-clock p50≤20% PASS/20–100% HOLD/>100% FAIL) instead of a benchmark score · **utility DEMONSTRATED not gated** (public dogfood CRM+bivpak + honestly-labeled agent-as-operator SWE-bench) · **durable session-state + resume BUILT** (worker-owned crash-safe session-content log; two-time-scoped trust invariant — content trusted only under settlement evidence AND current-prefix presence, else `content_lost`/degrade, never fabricated; outcomes stay m-10-canonical) · lock re-cut into a **hashable Tier-HARD bundle** (`bundle_sha256`) + the pair order made an **acyclic DAG**. The held joint lock `b7e1f0ef…` is **SUPERSEDED** (replaced by a later shorter re-lock). **D5 decoupling: real-work/dogfood start ⊥ the exit gate.** **NOW OPEN — the ratified §11 sequence (separately-gated):** (1) **m-7 broker study FIRST** (+H-24 if cross-epoch survives) → (2) interface DAG legs under F73 + join records → (3) author the extraction recipe+bundle + `bundle-soft-stability` fixture + freeze `STEP-3-EXIT-FIXTURES.json` → (4) the **shorter stage-6 re-lock** over `bundle_sha256` + whole-file-hard contracts → (5) **T4** (behind the re-lock + H-16/H-26). Ratification issued **NO** lock/PLAN/T4/cred/provider/release/E3/merge/deploy. Record: `master/STEP-3-STAGE6-AMENDMENT.md` + `master/relays/step3-arch-packet/` (r1–r12 + ratify `…-165500`). *(The paragraphs below are the prior Step-3 record, kept as history.)*

**▶▶ STEP-3 = the one-governed-turn MVP — ARCHITECTURE REFRAME RATIFIED (2026-07-15).** Architecture-of-record: `master/STEP-3-ARCH-AMENDMENT.md`, **operator-ratified at SHA-256 `2d240eb6…`** (`step3-arch-packet/070000`), VP-approved after a 6-round review (`…/063000`). **The conductor is ONE isolated governed-relay service, NOT the app's hub**; the app shell around it = **m-10 App Control Plane/Supervisor (NEW domain)** + **m-8** connector (app-side, holds provider creds + does the provider wire) + **m-9** worker (app-side runtime) + human/terminal surfaces; provider traffic / credentials / tool payloads / PTY / run-state **NEVER** transit the conductor. **Step-3 = the MVP** (scope CONVERGED 2026-07-15): a **barely-enough coding agent on the governed courier** — a minimal coding-agent worker (m-9, Codex-first) with local tools + the **conductor as a native relay tool** + a **built-but-EMPTY permission seam** + a **push-based wake-on-relay stretch**. The whole permission/authority system, sandbox, and multi-model carousel + m-4 routing **defer to Step-4**. The five original Step-3 DESIGN/amendment lanes were **HELD (`step3-hold-*`) + re-cut** — all stopped clean (no lock/PLAN/code, bytes preserved). **The permission/authority system is DEFERRED WHOLESALE to Step-4** (operator, 2026-07-15) — no config-derived ceiling, no `config_generation` freshness in the MVP; authorization = a **trivial static run-manifest allow-list** ("these tools are on for this run") exercising a built-but-empty enforcement seam. This **dissolves the entire seam-13 / freshness / m-5-amendment knot** for the MVP. Its history (for the record): owner returns proved no packet-compliant live-freshness read exists (m-7 `060542`, m-1 `124031`) ⇒ under the unchanged contract `643dd7c2…` positive tools would need an m-5 amendment (VP F33–F35 folded `150000`; F36–F38 `150756` were corrections to that amendment) — **all now OBE**: the operator re-cut the MVP to defer the ceiling entirely, so **the m-5 MVP amendment STANDS DOWN** (`step3-amend-m5-ceiling/…-152000` withdraws `145500`) and m-5's ceiling `643dd7c2…` + freshness → Step-4. **MVP tools:** local `read/write/edit/bash/apply_patch` (m-9, app-side) + the **conductor as a NATIVE tool** (`submit/project/read` over `internal/channel`, agent-to-agent relay); the **MCP server is RETAINED** as the foreign-harness adapter; a **shared conductor-client refactor** hoists the reusable client logic out of `cmd/frank-mcp` so both frontends are thin. **`bash` = ambient host authority, operator-accepted** (no sandbox/cwd claim; audit is evidence; trusted executor = Step-4 H-12). Full spec: `ARCHITECTURE.md` "Step-3 MVP" §; reframe → VP `step3-arch-packet/…-152000`. **✦ THE MVP AMENDMENT IS RATIFIED + OPERATIVE (2026-07-16):** `master/STEP-3-MVP-AMENDMENT.md` @ SHA-256 **`2f75f2a1…` (r7)** (branch-**a** real harness: frank owns the provider client, API-key to start; **BUILD** {m-10, m-9, m-8} + **GATE-OWNERS** {m-1 secret boundary — m-8 key + m-9 seat-credential custody, m-3 egress + E0 schema, m-2 form-schema, m-7 transport}; m-5 stood down; local tool effects — incl. bash egress — NOT governed; only relay comms + the designated provider attempt are; live E3 = a separate observer, not m-3; wake = at-most-once, m-10 never polls the conductor) is **OPERATIVE — operator-ratified 2026-07-16 at the exact VP-approved hash** (VP byte-bound approve `step3-arch-packet/…-035505` after seven exact-byte rounds F39–F66; ratification record `…-040405`, §8b convention; grill records `…-023557`/`…-024350`/`…-025642`) (r7 folds VP F65/F66 ONLY — `033048` revise-narrow, all r6 mechanisms + earlier closures preserved: **F65** = the release/E3 vector scoped EXACTLY to the app/provider vertical (the packet-`:75` provider-turn scope) — the CONDUCTOR SERVICE is a separate lifecycle, deliberately NOT an app-release field; its build digest + governing config identity are bound SEPARATELY in the exit-test record for the relay-exchange leg (evidenced by the Step-2 conductor-captured observe-as-send E1/E2 machinery); evidence tuple, release event, exit test, and annex now describe the same artifact set; **F66** = the packet-`:27` worker-as-principal fragment added to the §1 supersession list (the LOGICAL m-9 identity = the sole app seat/principal owning the channel through the m-7 broker; a replaceable worker generation acts only through its epoch-bound capability; sole-app-seat/genuine-relay-only/m-10-no-seat/no-provider-through-conductor rails preserved; §2b aligned; broker constrained OUTSIDE the replaceable worker generation; the F44 fold carries the wording into the m-9 charter). r6 folded VP F63/F64 — **F63** = two binding events split honestly (the pre-build interface-lock binds the identity contract + expected catalog vector; a NEW post-build RELEASE-BINDING event before the live E3/exit gate binds the app-main/m-10 + m-9-worker + m-8-connector artifact digests or a covering `release_digest`; `tool_catalog_digest` mechanically verified against the shipped registry, drift fails closed; E3 tuple + annex extended to every binary executing the governed turn); **F64** = the broker GENERATION FENCE (the LOGICAL m-9 seat vs the m-9 WORKER-PROCESS vocabulary pinned; the m-7 broker checks the current `turn_epoch` on EVERY relay verb + push delivery/forwarding — connect-time channel auth alone is insufficient; epoch-change linearization + in-flight disposition specified; m-10 supplies epoch state, still no credential bytes + no conductor verb; stale-worker acceptance row extended to the three verbs + push). r5 folded VP F57–F62 + the THREE operator-locked amendment-level grill decisions of 2026-07-16 — **#1 the process-topology pin**: m-10 = a MODULE in the app main process (not a separate daemon; seams designed as-if process-separated), m-8 stays a separate process, m-9 the supervised worker, seat-broker placement → the m-7/m-1 DESIGNs (`…-023557`); **#2 F59** = the Option-B durable ONE-SHOT authorization ticket ({run/turn/epoch/call-id/name/args-digest}-bound, exactly-once atomic consume, actual-executor-invocation proof; Option A rejected) (`…-024350`); **#3 F60** = ONE broker-held credential per LOGICAL seat, never copied into worker generations, `turn_epoch`-fenced replacement with NO implicit new identity, m-1/m-7 author the identity/broker contracts (`…-025642`) — plus F57 (the packet `:29` address-space phrase now explicitly superseded; every copy at narrow non-injection strength), F58 (policy identity = the 8 ratified NAMES vs build identity = the per-tool vector with named producers, BOUND at the Master+VP first-stage interface-lock), F61 (wake = best-effort advisory push + durable rediscovery on startup/reconnect + at-most-once scheduling; NO at-least-once claim), F62 (evidence tuple scoped to the app-side/external artifacts only; E3 artifact/writer/digest-producers/applicability-evaluator named); r4 `57aa3170` folded the external-review errata; chain → r3 `e25bce10` → r2 `3db3eb96` → r1 `02e9da1c` → r0 `a524bcbf`; VP APPROVED `035505` → operator-RATIFIED `040405`, 2026-07-16). Its **§7 graph IS the first-stage plan** — reconciled into every operative source at the 2026-07-16 F44 fold (`ARCHITECTURE.md` · `ROADMAP.md` · `CLAUDE.md` rule 1 + m-10 row · the m-1/m-2/m-3/m-5/m-7/m-8/m-9/m-10 charters, m-9 carrying the F66 wording of record; m-4/m-6 verified no-contradiction): **stage-1 DESIGNs DISPATCHED 2026-07-16** (`step3-mvp-design-m2` form→tool-schema mapping · `step3-mvp-design-m7` shared transport + channel/broker + the F64 fence · `step3-mvp-design-m10` app-IPC/manifest/dispatch-seam · `step3-mvp-design-m1` secret-boundary + seat identity/lifecycle · `step3-mvp-design-m3` egress/E0/E3-evaluator — each owner authors, its implementer pair-reviews the final bytes, consumers confirm) → **stage-2 m-8** (dispatches on stage-1 artifacts) → the m-9↔m-10 **lifecycle halves** (reciprocal confirmation) → the **m-9 (stage-4) + m-10 (stage-5) DESIGNs** (adversarial review + grill) → the **Master+VP first-stage interface-lock** (binds the identity contract + expected catalog vector) → PM to a T4 build team → the **post-build RELEASE-BINDING event** → live E3 + the exit test. *(The kickoff **§§1–3 AND §§5–8** framing below is SUPERSEDED by the reframe — see the kickoff banner; old kickoff hash `983508fc…` preserved as the historical lock.)* **STEP-2 CLOSED (operator-ratified in-session; VP adversarial close-confirm filed `step2-step-exit/RECONCILE-orchestrator-reviewer-20260714-211500`).** All five slices closed (s7·s8·s10·s9·s11); the step-exit test PASSED at `main@502e06c`: three roadmap legs green live on the dogfood store (false-done rejected pre-send + predicate named — fresh-genesis AND the real production daemon socket · passing send with conductor-captured E1/E2 evidence · a parked lane waking exactly-once on the operator's validated reply, fresh v8 store) · the INV-CATALOG red-battery fired (a planted fourth `delivery_state` → RED with `TestLawTerminalEnumByteExact` naming itself + three interlocked laws → reverted green, `main` byte-pristine) · uncached battery exit 0 / 26 ok / 0 FAIL / vet clean / ten laws. **The FULL LIVE RELAUNCH (real seats, honest E3/E4) stays a SEPARATE operator act, un-gated by this close.** T5/T10 acceptance-OPEN behind g2/dc — master recommends rescope as named Step-3 carries, not hold-open. Close relay `step2-step-exit/RECONCILE-…-182600` → operator (ratify + rule T5/T10) + VP (confirm); on both, the charter phase line flips to **Step-3**. *(Full per-slice record below in the Step-2 dashboard row; the paragraph immediately following is the Step-2 opening context, kept as history.)*

**✦ §7 STAGES 1–3 CLOSED — VP CLOSE-CONFIRM ISSUED 2026-07-19** (`step3-arch-packet/RECONCILE-orchestrator-reviewer-20260719-224500`, review r6 of the close thread; F70–F84 all closed). The seven closed owner artifacts, byte-bound: m-1 secret-boundary `7c8b09a6…` · m-2 form-schema `83d8e63e…` · m-3 egress/E0/E3 r4 `009df607…` (+ `phase=cancelled`) · m-7 transport/broker r11 `9331ea88…` · m-10 IPC/manifest/dispatch r36 `0240e874…` (the F59 host: check-1 `authorize_reject` family, four-field sender-fenced consume, two-member outcome-record, `CANCELLED`/`REJECTED_LOCAL`/`NOT_INVOKED_INTEGRITY_FAULT` honest terminals) · m-8 provider contract r12 `4b670a79…` (stage 2) · m-9 lifecycle half r19 `2a96a07b…` (stage 3; the three-identity/two-derivation-point F59 guard — both ratified negatives constructible). The 16-edge confirmation census + the field-grain m-9↔m-10 reciprocal close the evidence; N1–N4 = permanent lock-record errata (no byte edits); any pre-lock byte change re-opens via the full F73 sequence. **NOW OPEN: stage-4 (m-9 full-worker DESIGN + grill; carries L5 + the F63 shared-client pin + the PRIOR-ART §4 reference lanes) + stage-5 (m-10 control-plane DESIGN + grill) → the Master+VP stage-6 interface-lock → T4 build → the post-build release-binding → live E3 + the exit test.**

**▶ STEP-2 BUILD OPENED (2026-07-10, operator)** — governance hardening + minimal comms: **observe-as-send + evidence ladder (E1/E2) + executable claims (m-3)** and **the park/wake + buckets + ODB mechanism (m-6)**, hosted by m-7, riding the closed Step-1 conductor. Kickoff: **`master/STEP-2-KICKOFF.md`** — queue (r3, VP-recut): **s7 = INV-CATALOG first** (the 7-law executable tripwire) → s8 the observe spine → s9 evidence thicken → s10 the comms spine (the minimum A-gate park→reply→re-observe→wake path; exit leg 3) → s11 the comms thicken (B/C/D buckets, the elaborate-more fork, 8a) → step-exit after s11 (ROADMAP:100-101 three legs, on the dogfood store). Away-bridge OUT (step-(d) carry); egress stays fixture-scoped (ruling pending operator ratification); `step2-prep` carries 8 bounded design items. **★ STEP-1 CLOSED 2026-07-08** (`s6-close` at `main@6a1198a`; the exit test passed live, both upgraded legs). *The paragraph below is the Step-1 record, kept as history.*

**▶ STEP-1 BUILD OPENED (2026-07-03) — the product is named `frank`; the charter transitioned to the build phase.** Step-0 design is COMPLETE + clean: re-baseline + c6 / c6.1 / c6.1a + the external-review `step1-prep` §C4.3/I-PH amendment — all VP-co-signed; a final 5-lane differential returned the **4 seam lanes CLEAN**. **Slice-1 is CLOSED — COMPLETE at E2 (2026-07-03).** The first `frank` code exists: the thin end-to-end conductor relay, built + closed in `frank/` (tag `s1-close`→`main@f0dcb85`, ledger `674c844`; `go test ./...` 15 packages green + `go vet` clean — CTO re-ran uncached; **operator-ratified + VP confirmatory pass**; full sign-off chain in `master/RECONCILE.md` § S1). The path: decomposed into **4 sequential vertical slices** (team-per-slice, m-x-guided) → VP-gated (`step1-plan`) → operator-ratified → `s1` dispatched (`s1-dispatch`) → guide(m-7)+VP plan-gate → built by the `s1-core` pair through the m-1/m-2 F3 fidelity edge → S1-scoped hardened exit gate → guide deviation ruling (C7 fixture caught a real bug **red-first**) → operator merge gate. **The design→build method is validated on its first slice.** **▶ S2 DISPATCHED (2026-07-03, `s2-dispatch`)** — thicken store/engine (full recovery phases 0–4, durable FIFO, GC/genesis) + the **owed-item-as-typed-record projection** (`OI-S1-F11-SWEEP` = its first customer); guide **m-7** (refines the decomposition's m-1; VP-concurred); F2 conditioned delegation; **a new slice-team** (new sprint = new team — operator; it onboards to the S1 code, m-7 the continuity). Dispatch `…-223913` (r2, supersedes `…-223146`; dispatch-root lint fix per VP F-S2-1). **Deferred (operator — no testbed yet):** the MCP live-adapter + fuller-FieldSpec "wire-it-up" slice — S1 is *built-but-unwired* (real socket daemon, E2-proven, but no live agent session has connected; speaks frank's own socket + a minimal FieldSpec dialect); it returns when there's a live testbed. The design-of-record is the spec; `frank/` is the build target (formerly `pcode/`); slice-team relays live in `frank/`.
The post-re-baseline design-of-record was re-reviewed (10 lanes; **90 findings, 0 FATAL, CONDITIONAL-GO** — the 2026-07-01 NO-GO
**structurally discharged**) and cleaned in **c6** (90 = 44 CTO + 45 pair [7/7 pair-approved] + 1 subsumed; all seams converged;
`c6-apply.diff` 18 files/+433/−177), then **seam-hardened in c6.1** — a scoped differential caught + corrected 5 cross-pair seam divergences + 3 dropped flags, all **owner-confirmed** (`c61-fix.diff` +35/−17). The design-of-record is now **verified-at-the-seams, not asserted**, and the confusion-resistance claim was sharpened to *tool-mediated* + a path-hygiene invariant (`I-PH`, external review). **Step-1 PLAN GATED (revise→approve `step1-plan`); Slice-1 (`s1`) DISPATCHED (`s1-dispatch`);** the owed carries (away-bridge, R2/altitude-B fixtures, ③/⑤/GRILL_REQUIRED/routing_escalation, I-PH, m-5-F2) all land at
their own **build step**, not at PLAN. *History / why:* the 2026-07-01 adversarial
pre-build review (`DESIGN-REVIEW-2026-07-01.md`; 16-lens × 3-verifier fleet, ~48 verified findings / ~12 FATAL)
**retracted the "MUST-gate satisfied" certification as unsound** and returned NO-GO. Root causes (~8): the design assumes a **WRAP**
deployment but the operator locked **ATTACH** (①) — inverting every "structural / by-construction / sole-writer /
unbypassable" claim; **conductor-core** (the running program: serialization / crash-atomicity / config-integrity /
recovery) is **nobody's domain and has no design doc** yet Step-1 IS "build the conductor core"; policy config is
**lane-writable under attach** (one Bash line = total governance bypass); altitude-B grain breaks the routing veto;
the Step-1/Step-2 split dead-ends the write path; R2 under-specified/untestable at schema grain (VP-corrected — not a confirmed `chosen_model` leak); pure-judgment
A-categories silently absorb; and the **five operator decisions were never folded into the docs** (② fail-closed vs
still-locked fail-OPEN m-3 text). **The bones are sound — it's a bounded re-baseline (weeks), not a rewrite.**
**Design-review dispatch CLOSED (2026-07-01, VP-approved `design-review/…-150235`)** — NO-GO concurred; deployment fork
**decided**; review-of-record self-consistent (corrected §2A/§2B/§2C bucketing). Re-baseline is the active phase:
- **(a) ✅ DONE — deployment fork DECIDED** (`GRILL-LOCK-deployment-fork-2026-07-01.md`): **attach + interface-level
  guardrail; adversarial containment / wrap / "by-construction" shelved.** Standing claim = *confusion-resistant;
  malicious code-executing agent explicitly out of scope.*
- **(b) ✅ COMPLETE — m-7 Conductor-Core substrate DESIGNED + LOCKED** (`c4` CLOSED 2026-07-02; VP design-lock co-sign
  `c4-design-m-7/…-040327`; `DESIGN_LOCK_ID c4-design-m-7-lock`; close declaration `…-040839`). The **engine** — one-writer
  durable-FIFO serialized commit loop + atomic clear-on-pop, rename-pivot crash-atomicity, phase-0–4 recovery, HELD
  internal-fault disposition, byte-exact `{accepted,rejected,held}`, MCP `{submit,project,read}` interface guardrail,
  per-domain-section single-digest trusted config, local-outbox-only send, seat-binding base — folded into
  `ARCHITECTURE.md` **§C4** + `RECONCILE.md` **c4**. **8 CQs closed full-pair** (6 pairs re-engaged; the gate/config
  cluster took an r1→r2 revise cycle; closure gate held throughout); grilled (`c4-grill-m-7`); **claim boundary held —
  confusion-resistant; m-7's headline by-construction claim = the §2.4 serialized-loop double-accept kill (one of the four licensed classes — §C4.3).** *The NO-GO's
  core defect — the running substrate was nobody's domain — is corrected.*
- **(c) ✅ COMPLETE — global claim-sweep + decision-folds** (`c5` CLOSED 2026-07-02; VP close-approve `c5-decomp/…-143205`).
  All 7 design-of-record docs (m-1..m-6 + `ARCHITECTURE`) swept to the honest line — adversarial-strength claims
  ("by-construction / sole-writer / forgery-robust / tamper-resistant") → **confusion-resistant + D5 residual**; the genuine
  trusted-engine invariants (R2 gate-grammar, observer-selected controls, authority-ceilings) **kept**; byte-consistent
  (`{accepted,rejected,held}`; `bounced` retired). Decisions **③** raise-only (+ §J), **⑤** ODB carve-out, **④** away-token
  **recorded as §2C build-carry** folded; 6 lanes owner+implementer-approved (3 must-revise→approve cycles). Folded into
  `RECONCILE.md` **c5**. **The docs now tell the honest story end-to-end.**
- **(d)** §2C at its build step (the `re-mint-supersedes` + decision-④ away-token carries + the routing-lane **R2 `gate_referenceable`-per-column** & **altitude-B per-row** carries land there);
  **(e)** THEN Step-1 PLAN.

**Step-1 PLAN remains the operator-opened gate (re-baseline step e). (a)/(b)/(c) done; (d)'s items are deferred
build-step carries — no PLAN / code / spike yet.**

### Superseded (history — the retracted certification)
**~~Step-0 COMPLETE + build-readiness pass COMPLETE — the full MUST-before-Step-1 gate is SATISFIED (2026-07-01)~~.**
[RETRACTED by the adversarial review above; preserved for the audit trail.]
After c3 locked all six domains, the team ran the **measure-twice pass**:
- **Build-readiness review** — 7 red-team lenses → `READINESS-REGISTER.md` (GO-WITH-FIXES; 8 clusters).
- **Runtime + model research** — how the conductor rides existing runtimes, **provider-agnostically**, with **persistent
  seats** → `RUNTIME-RESEARCH.md`; cross-checked vs an external brief, **primary-source-verified** (§14), **VP-reviewed**.
- **Five operator decisions ①–⑤ RECORDED** (`READINESS-REGISTER.md` → *Operator decisions*). **① = attach-first +
  "confusion-resistant"** (conductor-owned identity; "sole external sender **by construction**" is a **6-property
  spike-gated** milestone, never shipped on assumption). ② fail-closed authority-class · ③ raise-only A/B · ④ away-token
  rotate+re-observe · ⑤ narrow ODB model-name exemption.
- **The two MUST-reconcile fixes CLOSED (VP co-signed):** **Cluster 1** (m-1/m-2 `submit()` write-path seam — pre-append
  form+lineage → one atomic `accepted`; **Step-1 = store+form+lineage**, observe reserved Step-2; `readiness-fix-c1`
  VP co-sign `…-230335`) · **Cluster 4** (m-2 routing FieldSpec ← m-4 `:200-210`; R2 grammar-enforced; computed-field
  homes; posture rides `seat_archetype`; `readiness-fix-c4` VP co-sign `…-013613`).

**Tracked carries — route before their relevant PLAN, NOT gate blockers:** row-parity §15 Q-F (pre-Step-1-PLAN) ·
runtime-`away` posture-vocab split (pre-Step-2 away-bridge) · the ①-unblocked m-1 claim/identity fixes + the
decision-implied owner-fixes (5a/6a/6b/6c) · the m-6 `delivery_state` `bounced→rejected` ripple · the **Cluster-3
security spike** (the "by-construction" milestone — a separate operator-opened gate).
**Readiness relay trail:** `readiness-decomp` · `readiness-reconcile` · `runtime-research` · `readiness-fix-dispatch` ·
`readiness-fix-c1` · `readiness-fix-c4`.

### c3 lock (history — all six domains locked, `ARCHITECTURE.md` §1–§C3)
**Cycle c3 — CLOSED / LOCKED** (`c3-lock` 20260630-191315/191525; C3.6 integration capstone; m-5/m-6 stood down).

What c3 locked (detail: `ARCHITECTURE.md` §C3; the two design docs):
- **m-5** — an **archetype = one governed expansion-slot** binding {topology + gate-set + ceiling-at-spawn +
  observe-invariants + routing-prior}; `lower_snake_case` tag-space; observer-selected control-property invariants (F1);
  3-axis open-map ceiling vector + modular `external_send`; T1/T2/T3 + sensor-full; `actuator` = derived class.
- **m-6** — a promote-and-bind human surface: A/B/C/D buckets; ODB render+capture + the read-only elaborate-more fork;
  the 7-state park/wake machine; the opt-in egress-gated away-bridge; meeting-lane routing; the interjection host.
- **The seam-of-record** — human-mode = posture × `surface_intent` (four-class, conductor-derived, non-gate;
  gate-bearing binds off locked fields → **no m-2 micro-fold**).
- **Seam C = A** — m-1 owns the away-mode inbound-token mint/verify via its reserved `certification` seam (**additive**,
  no c1 reopen); m-6 owns the bridge. The first conditional-upstream-contract-check.
- **C3.6 capstone** — the six domains compose (writer-backed + acyclic; three seams closed; locked invariants intact;
  deferrals recorded as build-carries, not gaps).

**Cycle c2 — CLOSED / LOCKED.** m-3 Observation & Evidence + m-4 Routing & Policy locked as the Step-1
runtime-intelligence layer (`c2-lock` 20260630-043859 / -044308). Detail below + `ARCHITECTURE.md` §C2.

What's locked (detail: `ARCHITECTURE.md` §C2; the rev2 domain docs):
- **m-3** observe-as-send gate (conductor observes done from outside the lane, observer-only against R3; evidence
  ladder; fail-closed egress chokepoint).
- **m-4** routing governance-record (routing = a first-class recorded justifiable decision; two-layer bucket prior;
  fail-closed `route_dispatch()`; **model = payload, never a gate input — R2**); GL-4 routing-templates (record
  mechanism; pane-spawn rides existing multiplexer infra).
- **The R2-preserving seam** — silent-deviation block via m-3's generic integrity-veto; no model-derived predicate
  in any gate; bucket-vs-bucket; snapshot-provenance (holds for opaque lanes).
- **Two opaque archetype atoms** — `slot_in` work-archetype (conductor-classified at acceptance) + `seat_archetype`
  per-seat (per-assignment record home); concrete tag-space/semantics **reserved to m-5 (c3)**.
- **M4-1** — routing B→A escalation rides the c1 monotonic HUMAN_GATE routing-raise (no new gate class, R2-safe).

**Cycle c1 — CLOSED / LOCKED.** Step-1 foundations jointly locked: **m-1 Trust & Identity + m-2 Forms &
Determinism**. VP close-confirm `c1-joint-lock` 20260629-180934; operator §J ratified; close 181713.

**Cycle c1 — CLOSED / LOCKED.** Both Step-1 foundations jointly locked as the frank design-of-record:
**m-1 Trust & Identity + m-2 Forms & Determinism**. VP `approve` close-confirm (`c1-joint-lock`,
20260629-180934); operator §J ratified; close declaration `c1-joint-lock` 20260629-181713; pairs stood down.

## Org status
- `master.orchestrator-planner` (CTO) — Claude Opus 4.8; online.
- `master.orchestrator-reviewer` (VP) — GPT-5.5 (high); idle — **co-signed the c4 conductor-core DESIGN-LOCK** (`VP_DESIGN_LOCK_CO_SIGN: approve`, `c4-design-m-7/…-040327`; 5 checks passed, no blocking finding). Caught 4 routing/certification errors across the CQ cycle before they propagated.
- **m-1 Trust & Identity** — c1 **LOCKED**; stood down (+ the additive Seam-C inbound-token mint/verify carry, C3.7 — does not reopen c1).
- **m-2 Forms & Determinism** — c1 **LOCKED**; stood down.
- **m-3 Observation & Evidence** — **c2 LOCKED**; stood down.
- **m-4 Routing & Policy** — **c2 LOCKED**; stood down.
- **m-5 Workflows & Archetypes** — **c3 LOCKED**; stood down. The archetype system (the governed expansion-slot: tag-space,
  ceiling vector, T1/T2/T3, sensor + derived-actuator, the human-mode vocabulary).
- **m-6 Human Surface & Scheduler** — **c3 LOCKED**; stood down. The promote-and-bind human surface (buckets / ODB /
  park-wake / away-bridge [Seam C = A] / meeting / interjection host).
- **m-7 Conductor-Core** — **c4 LOCKED; stood down** (design-of-record locked 2026-07-02, VP co-sign `c4-design-m-7/…-040327`;
  re-baseline step (b) COMPLETE). The runtime substrate the six policy domains ride on — owns the
  ENGINE (serialized commit loop / crash-atomicity + recovery / internal-fault disposition / trusted config load +
  integrity / attach + interface-guardrail enforcement / local-outbox-only send / store genesis+GC), hosts+executes the
  six CONTRACTS, does not re-own policy. Owns `DESIGN-REVIEW-2026-07-01.md` §2A. Audit reconciled (still-open; VP co-sign
  `…-162319`; ledger `RECONCILE.md` c4). Design-of-record `design/2026-07-01-v3-conductor-core-design.md` — grill-locked
  `c4-grill-m-7`, seam matrix biting, F8 semantic claim-sweep clean. **CQ gate SATISFIED** — all 8 CQs
  (CQ-1/2/3/4/4b/5/6/8) closed with approved full-pair triads via 3 COORD clusters (`c4-cq-gateconfig` / `c4-cq-m1` /
  `c4-cq-slotin`, 6 pairs re-engaged; gate/config took an r1 must-revise→fold→r2 approve cycle). CTO fold
  `…-031533` + CQ-6 correction `…-032227`, **certification VP-approved `c4-cq-coord/…-032843`** (CQ-6 base-closed;
  **re-mint-supersedes carried as a §2C away-bridge build-carry**, not lock-bearing; + 3 lock-assembly items: m-4
  per-section stamp · byte-exact `{accepted,rejected,held}` · exactly-one-outcome check). **DESIGN_LOCK `c4-design-m-7-lock`
  EFFECTIVE** (design r3 + lock r5 pair-approved; CTO-certified `…-040011`; **VP co-sign `…-040327`**; close declaration
  `…-040839`). Folded into `ARCHITECTURE.md` §C4 + `RECONCILE.md` c4. **c4 CLOSED; re-baseline step (b) COMPLETE** → (c)
  global claim-sweep + (d) §2C-at-build-step remain → **(e) Step-1 PLAN** operator-openable.
- **s1 Slice-1 team** — **S1 CLOSED / COMPLETE at E2** (tag `s1-close`→`main@f0dcb85`; operator-ratified 2026-07-03 + VP confirmatory pass). The thin end-to-end conductor relay is built in `frank/` (15 pkgs green, `go vet` clean — CTO re-verified uncached). Built by the `s1-core` pair, m-7-guided, through the m-1/m-2 F3 fidelity edge (F-M1-1 block→fold→approve). Slice-1 CLOSED; the team continues into S2.
- **s2 Slice-2 team** — **DISPATCHED 2026-07-03** (`s2-dispatch` r2 `…-223913`); a **new** slice-team (new sprint = new team; onboards to the S1 code, m-7 the continuity), guided by **m-7** (m-1 keeps authority over the owed-item `record_kind`/store-layout/API + fidelity — VP watchpoint). **▸ S2 CLOSED / COMPLETE at E2 (2026-07-04)** — merged `main@b322b6d`, tag **`s2-close`** (operator-authorized, token-lineage clean; battery 18 pkgs green at main — CTO re-ran uncached; `master/RECONCILE.md` § S2). Engine thickened (recovery 0–4 · durable FIFO · GC/genesis · **the owed-item projection**); **`OI-S1-F11-SWEEP` closed THROUGH frank** (operator-stamped owed record → executed sweep → disposition → open set empty on a real store — **frank's first real governance transaction**); 2 latent S1 races found+fixed by the fresh team; **zero owed items ride out**. s2 team stands down. **▸ S3 CLOSED / COMPLETE at E2 (2026-07-04)** — `main@b5a2c95` (`--no-ff`), tag **`s3-close`**; VP pre-integration confirm + operator's four decisions on record; **battery 20 green at main — CTO re-ran uncached** (`master/RECONCILE.md` § S3). **frank speaks the real protocol on fresh stores** (MVP dialect deleted; the 62-check dissolution EXECUTED — 115 rows, zero uncovered, [VP-W] adjudication checked row-by-row); five independent verification chains; mid-slice s3-scope-q1 ruled DEFER cleanly. **Riding out: `OI-S3-CONFIG-CHANGE` alone** (→ the wire-up slice). s3 team stands down. **▶ FORK ELECTED (operator, 2026-07-05): s4 = THE WIRE-UP** (the per-seat MCP shim — live Claude Code/Codex sessions on frank; ends the operator-as-transport; discharges `OI-S3-CONFIG-CHANGE` via the §7 config-change record; **first live E3**, scoped to transport). **Renumber of record: old Section-4 (consumer schemas) → s5**, built *over* the wired conductor (its rows = the first real §7 changes; its relays = the first usage data). **s4 BUILD DONE — E2 floor green at 3 independent stations (`s4-wire-impl@7dc5f92`, 14 commits; 21 test-bearing pkgs, race clean, zero regression; S2 store untouched). ▶ LIVE GATE-DAY PENDS THE OPERATOR** (`s4-exit-gate …-145109`, TO operator): designate seats A/B (procedure assumes Claude Code ↔ Codex) → run `results/e3-gate-procedure.md` (init THE persistent team store → mint → wire → **the first no-hand-relay relay** → live adversarial/crash legs → **the §7 round-trip authored by the operator** → OI-S3-CONFIG-CHANGE disposition) → evidence returns via the pair → s4-orchestrator verifies → master SITREP → merge/`s4-close`. [VP-W1..W4] all folded pre-handoff. Step-1 remaining: s4 → s5 → the step-exit test. — forms/lineage: full FieldSpec registry (frank speaks the REAL protocol envelope) + the 62-check linter dissolution with the **FULL replay** as the gate centerpiece + `schema_version`/migrators + the R2-negatives/GRILL_REQUIRED owed carries. Guide **m-2** (m-7 consulted on config seams; m-1 fidelity on store touches); NEW slice-team; F2 conditions. **The wire-up slice is explicitly next-after-S3** (S3 is its prerequisite — real relays can't validate through frank until the registry lands). Scope: full recovery (phases 0–4) + durable FIFO + GC/genesis + the **owed-item projection** (`OI-S1-F11-SWEEP` = first customer). **F2** conditioned delegation (no bootstrap guide+VP plan-gate; escalate to master only on a trigger). Exit gate re-runs F9/F11 under the new recovery machinery + discharges the `OI-S1-F11-SWEEP` sweep. **Deferred:** the MCP live-adapter/wire-it-up slice (no testbed yet).
- **▸ s4 WIRE-UP — CLOSED / MERGED (2026-07-05): the operator-as-transport is OVER.** Merged `main@fb61fda` (`--no-ff`, parents `a47381a`+`6a23cf0`), tag **`s4-close`**; operator-directed grant (`s4-merge-gate/MERGE-GATE …-233708`) → `s4-wire.implementer` executed → **battery 21 green at the merge commit, CTO re-ran uncached**; VP-confirmed; `master/RECONCILE.md` § s4. **A governed relay flew Claude Code → Codex, conductor-stamped, no human transport** (`relay-4a33925b…`). §7 config-change + owed cycle exercised live; **`OI-S3-CONFIG-CHANGE` discharged → Step-1's mandatory owed set is EMPTY.** 3 gate-day findings dispositioned (F-GATE-1 fixed; **F-GATE-2 owed-fill-time fixed = 5 registry `required_when` rows**; F-GATE-3 ruled out-of-I-PH-scope). Discretionary follow-on `OI-S4-TOKEN-SCOPE` (incl. `genesis`, VP+m-7). **▶ Step-1 remaining: s5 (consumer schemas, over the wired conductor) → step-exit test → Step-1 closes.**
- **▸ s5 DOGFOOD (the first team ON frank) — STOOD DOWN AT CHECKPOINT (2026-07-06, operator-directed stop-the-line).** s5 ran its live governance through frank (dispatch `relay-45e263fa` · boot-ack + full onboarding via `submit`/`project`/`read` · 5 sub-seats + an operator seat minted · hub-and-spoke m-x routing worked · operator-authored waiver record live). The run delivered its real objective early: **`master/TRANSPORT-FINDINGS-2026-07-06.md` (F1–F17)** — headline **F11: lineage livelock under concurrent traffic** (a seat can construct NO acceptable parent; even report-only relays blocked), plus the render↔validate divergence family (F2/F6/F7/F13), the F9 `intake_id`-reuse audit break, and the F17 irrevocable-waiver gap. What held: crash-atomic store (3 restarts, zero corruption), channel-stamped FROM, I-PH clean. **Conductor decommissioned; store archived** (`~/frank-archives/frank-team-store-s5-dogfood-20260706`). **s5 RESUMES as a standard file-relay team to its checkpoint** (`s5-resume/PLAN …-034602`; [VP-W7] §7-live-store leg adapted → rides the transport-fix relaunch; dogfood-record exit item closed early by the ledger). **m-x fidelity packet Q1–Q11 fully reconciled** (`s5-fidelity/RECONCILE …-034602` — all six owners answered, zero rejections): owed-#3 **EMPTY** · **`routing_escalation` §J2 LANDED + the [VP-W5] hold LIFTED** (m-2's exact delta → s5-a's registry pass) · no Step-1 `slot_in` writer · `scope_paths` struck · Q4 artifact set final (no performative migrator) · ⑤ shape ruled (real code, real chokepoint, dormant, fixture-driven) · no live `record_kind` widening · `attestation_source` folded into s5-a (O-2). **▶ Next: s5 checkpoint (file-relay) → the TRANSPORT-FIX cycle (m-7/m-1/m-2, grilled — {F11,F4,F5} liveness first) → step-exit test → Step-1 closes.**
- **▸ s5 CONSUMER SCHEMAS — CLOSED (2026-07-06): STEP-1'S BUILD QUEUE IS EMPTY.** Close tip `main@f31d43a`, close record `7e5c527`, tag **`s5-close`**; three integrations under the WRITTEN grant chain; operator close-gate exercised in-session; **battery 23 green uncached — CTO re-ran at the close tip** + probes (`master/RECONCILE.md` § s5). Registry **47→83 rows @ `s5-fieldspec-v3`**, consumer fields declared + proven dormant (334-subtest sweep [VP-W3]); ③ live-wired (S1+S2 live, S3 inert per the confirmed claim boundary — in the code, `detector.go:25`) + ⑤ dormant at the real drain + `routing_escalation` + I-PH; zero-loss replay **incl. the real-store leg vs the archived dogfood store (three seats)**; NO envelope migrator (`Current=1`); **`OI-S4-TOKEN-SCOPE` closed by the scope narrowings** (`genesis` in NO scope; owed/`gate_resolution`/`disposition` operator-only). Fresh-team bar paid a **6th** time (DEF-1..5 fixed red-first, incl. DEF-2's lane-suppliable-system-headers reject rule). Q1–Q11 + M-1..M-4 settled by all six owners, three adversarial implementer approves, zero unresolved contradictions. s5 team → standby on the master fold. **s5 close RATIFIED (operator, 2026-07-06).** **▸ OPERATOR RULING (2026-07-06): Step-1 does NOT close until the transport is fixed** — the honest reading of the step's goal ("remove the operator-as-transport"; a transport that livelocks under multi-seat load hasn't). **▶ Step-1 remaining: s6 = THE TRANSPORT FIX (in-step; design-amendment phase first — m-1 lineage/parenting THE fork, m-7 engine liveness, m-2 one-codec; F1–F17 the spec seed; GRILL on the parenting fork) → the step-exit test ON THE FIXED CONDUCTOR (the ROADMAP:83-85 legs + §7-applies-s5's-registry as the first live act + the F11 regression leg = the dogfood traffic pattern replayed without livelock) → Step-1 CLOSES.**
- **▸ s6 DESIGN PHASE — CLOSED / VP CO-SIGNED (2026-07-06, `…-220325`).** Three pair-complete domain halves (m-1: the fork+§B/§C/§D+B-3 · m-7: A-1..A-4+D-1/D-2+B-1 · m-2: the one-codec+B-2) integrated as **`S6-AMENDMENT-SET-2026-07-06.md` (r3)**; the parenting fork **GRILLED with the operator** (`GRILL-LOCK-parenting-fork-2026-07-06`: branch A conductor-computed PARENT · fallback-never-bounce settled by the zero-true-bad-picks audit · m-4-gated Sharpening-D, discharged); the boot stage added on operator request (derived-only activation — no persisted marker). **The gates caught real defects at every altitude:** two pair must-revises (the B-1 pre-active classifier hole; m-2's r1 self-contradictions) + two VP must-revises (the F14 mis-home; the activation-marker cross-domain lock conflict + byte-fidelity) — zero silent folds. §C4 pointer LANDED (the amendment docs are the authoritative deltas pending the post-build fold). **▶ the s6 BUILD slice dispatching** (package held for VP pre-handoff; [VP-W1..W3] of the co-sign folded: hold-on-m-7-revert · FX-B1g explicit · seven registry rows, no marker row).
- **▸ s6 TRANSPORT FIX — CLOSED (2026-07-08) ★ STEP-1 CLOSED ★** Merge `main@6a1198a` (`--no-ff`, parents `73116e9`+`58f2233`), tag **`s6-close`**; operator-granted, VP-confirmed, **battery 24-ok uncached at the merge commit — CTO re-ran** (`master/RECONCILE.md` § s6). The co-signed amendment set implemented whole; **THE STEP-EXIT TEST PASSED LIVE**: the operator §7-apply as the first live act · ROADMAP:83-85 + gate→outbox · **the F11 redrive 14/14 with ZERO parent-class and ZERO same-context re-render bounces** · the live boot walk (`minted→bound→active`, derived-only, no restart). Ten verification chains; the fence held (2 typed OIs ride out). **Step-1's goal — remove the operator-as-transport — is DELIVERED on a transport that survives its own governance load.** **▶ NEXT: Step-2 planning (governance hardening + minimal comms — observe-as-send, evidence ladder, park/wake) with the INV-CATALOG follow-on first in the queue and the dogfood relaunch riding Step-2's first live store.**
- **▶ STEP-2 OPENED (2026-07-10, operator).** Kickoff package cut: **`master/STEP-2-KICKOFF.md`** (PLAN-approach). **[The rest of this row is the append-order record (r1→r2→r3); superseded where it conflicts with the r3/r4 segments at the row's end — the CURRENT state: queue = s7→s11 with step-exit after s11 · baseline = a pre-s7 operator precondition at `main@a1bc6d45ac5c` (1105 expanded entries) · 8a = partial co-sign only (floor: migrate-then-validate + `held`/escalated; `stale_schema`/freeze/re-issue = m-6-proposed, open).]** Scope fence (away-bridge OUT = the step-(d) carry; E3/E4 OUT = Step-3+; m-5 tag-space OUT = c3-reserved; egress fixture-scoped through Step-2, ruling proposed); the s7–s10 proposal *(r1 — superseded by the r3 recut)*: s7 = **INV-CATALOG** (7 laws, one named executable check each, catalog governed like the registry) · s8 = the **observe spine** (observe-as-send in the serialized commit path · governed layer activation · E1 + first E2 check · decision-② fail-closed · the s5-b (h) suppliability guard · exit legs 1+2) · s9 = **evidence thicken** (check-registry + isolated executor + `scope_paths` + both riding OI-S6s + the ⑤ fixture pair) · s10 = the **m-6 comms mechanism** (7-state FSM · resummon · re-observe-on-wake · **wake-on-reply** · A/B/C/D buckets · ODB render+capture · exit leg 3) → **step-exit** live on the dogfood store + the INV-CATALOG red-battery demo. **`step2-prep`** = 8 bounded design items (layer-activation knob · executor isolation · `scope_paths` co-sign · Q6×Q4 `record_kind` widening · 8a park-across-bump · OQ-2 fork posture · `GRILL_REQUIRED` row · red→green honest bound). Charter phase line flipped (rule 1). **VP adversarial review requested** (`step2-plan` SITREP `20260710-005507`); **operator pre-flight owed: the `frank/` baseline commit (42 publication-sweep paths on `main`) + the kickoff/naming/egress ratifications.** **▸ PAIR-VERIFIED same day — 4/4 CONFIRM** (`step2-prep` m-7 `010806` · m-6 `011009` · m-2 `013000` · m-3 `022000`; reconciled `011612`): m-3's two precision must-revises FOLDED → **kickoff r2** (the decision-② trigger reads `record_integrity ∈ {self_reported, mixed}` — the CQ-2 canonical leg; the IMPL scope clause conditional on the s9 co-sign); the NF-S6 **two-axis** disposition + deterministic timer keys + the no-away-token re-observe pin folded; **shapes agreed**: layer knob = engine-config-digest member + operator-authored §7 `config_change` restart-effective (m-7+m-2) · 8a = freeze-at-park / migrate-then-validate / re-issue-on-choice-change / `held`+`stale_schema` (m-6+m-2 convergent) · executor-isolation seam split w/ claim ceiling · `scope_paths` DECLAREs s9 (lane-read-only pin) · **`GRILL_REQUIRED` STRUCK for Step-2** (recorded shrink, m-6-F6 closed) · red→green = differential-class labeling (s9) · **CTO ruling: no-compress via the elaborate-more fork** (meeting surface = Step-3/4). m-7 claimed its s7 INV-CATALOG half. Still open: ~~the VP verdict~~ · Q6×Q4 widening (master, **before the s10 PLAN**) · OQ-2 (before the s11 fork build) · the operator pre-flight set. **▸ VP VERDICT: REVISE (`step2-prep/RECONCILE-orchestrator-reviewer-20260710-012951`) — all 4 findings accepted + FOLDED → kickoff r3** (fold relay `…-013501`): F1 `scope_paths` reopened as an s9 PLAN blocker (4-pin producer/reader contract: canonical = the accepted PLAN/dispatch ancestor · lineage resolution · typed self-widen refusal · missing-source disposition; self-widen NF owed) · F2 8a corrected to the **partial** co-sign it is (the floor: migrate-then-validate + `held`/escalated co-signed; the `stale_schema` token + freeze/re-issue branch OPEN pending m-2 confirm + m-6.implementer review) · F3 the comms slice split **s10 spine / s11 thicken**, step-exit after s11, Q6×Q4 before the s10 PLAN, OQ-2 before s11 · F4 the baseline = a **pre-s7 operator precondition** at the true base `main@a1bc6d45ac5c` (38 tracked + 1067 untracked = 1105 expanded entries; battery at the baseline commit; SHA recorded as s7 `BASE`; clean status before dispatch). VP concurrences on record: s8/s9 boundary · egress fence · layer-activation shape · the s7 claim-grain watchpoint (derived-only = seat-lifecycle; I1-P = sole *governed* write path, D5 residual). **▸ VP r3 RE-REVIEW (`…-014110`): substantive recut ACCEPTED (F1/F3/F4 closed); 3 bounded truth/cross-ref fixes → kickoff r4** (`stale_schema` moved to the m-6-proposed set — the co-signed 8a floor is migrate-then-validate + `held`/escalated only; the four pre-split s10 references re-routed to s11, ratification target = s7–s11; this row disambiguated with the current-state label at its head). r4 fold relayed for VP co-sign. **▸ VP CO-SIGN (`…-014732`): APPROVE r4** — both revise rounds discharged; approved for formal `step2-plan` decomposition (s7 INV-CATALOG → s8 observe spine → s9 evidence thicken → s10 comms spine → s11 comms thicken → step-exit), with the sequenced gates on record: `scope_paths` co-sign before the s9 PLAN · Q6×Q4 before the s10 PLAN · OQ-2 before the s11 fork · the 8a joint review before the s11 PLAN. **The step now waits on the operator alone: (1) ratify — kickoff r4 · naming (s7…/step2-prep/step2-plan) · the fixture-scoped egress ruling · the s7–s11 proposal; (2) the baseline precondition — inventory the 1105 expanded entries → one baseline commit → full uncached battery at that commit → record its SHA as s7 `BASE` → clean status. Then: formal `step2-plan` + the s7 dispatch.** **▸ OPERATOR RATIFICATIONS (2026-07-10): egress ruling ✔ · kickoff r4 ✔ · naming ✔ · s7–s11 slicing ✔ (round 2, with order+staffing). NEW RULING: releases live on a separate release tree/branch; `frank/` is developer-only, PRIVATE, never a public remote** (the release directory → `iwnlcern/frank` stays the sole public-facing surface). **▸ FORMAL `step2-plan` CUT (`PLAN …-021150`): order of record s7 (lean, CTO+m-7) → s8 (fresh) → s10 (fresh; comms spine before evidence thicken — all three exit legs live after two build slices) → s9 ∥ s11 (continuation teams; m-2 registry = the named collision edge) → step-exit; the s10/s9 order delta explicitly flagged to the VP; Q6×Q4 resolves during s8. Baseline inventory DONE (38 tracked + 540 `.relays/` + 525 `master-docs/` + LICENSE/NOTICE = 1105 entries, all publication-sweep). REMAINING before s7: the operator's baseline commit → uncached battery at it → SHA = s7 `BASE`.** **▸ BASELINE LANDED (2026-07-10, operator-directed): s7 `BASE` = `frank/main@1d3e92c`** (1105 entries, one commit; battery **24-ok/0-FAIL uncached + vet clean** at it; status clean). **Remote system executed per the release-separation ruling:** `frank/` → private **`iwnlcern/frank-dev`** (main + all close tags pushed; a pre-push guard refuses the public URL); releases ship only from `~/frank-release` → `iwnlcern/frank`. **▶ s7 OPEN: the INV-CATALOG plan relayed to the VP (`s7-dispatch`).** VP r1 = revise (seat authority · executable rows · governance staging) → **plan r2 (`…-023635`) folded all three: m-7.implementer = sole s7 code writer under one master IMPL dispatch; the ten-row law→subtest→mechanism contract is in the plan; catalog §7-pinning = a mandatory s8 carry (kickoff design item 1). VP r2 verdict = revise-narrow (F1/F2 closed; 4 precision folds) → **plan r3 (`…-030148`): kickoff `:34` governance sentence fixed at source; row 6 I-PH exhaustive-with-census + planted-leak negative; row 9 restored to the locked A-2 at-most-one grain (pending-zero legitimate); fidelity scopes bound m-1→2/4/5/6, m-2→1/3/6, m-4→3. **▸ VP APPROVE on r3 (`…-030737`) — the s7 plan gate is PASSED** (effective plan = r2 + the r3 replacements; integration bar: ten checks green in `test/invariants` · command-pinned red-then-green demo · full uncached battery+vet · scoped fidelity relays · test-only diff; no merge authority granted). **▸ s7 IMPL dispatch `…-031010` RETRACTED (operator-caught role violation, pre-work): master domain pairs design/plan/review docs only — they never type code; the guide fence ("does not type the code") holds even in lean mode.** Retraction `PLAN …-031728` (m-7.implementer restored to contract-fidelity review of rows 8–10 + harness); **corrected dispatch `PLAN …-031733` TO the fresh build seat `s7-core.implementer`** (operator boots one pair-Implementer session in `frank/`; scaffolds per sprint-doc-setup; intake-ack before code; sole writer; branch `s7-inv-catalog` off BASE `1d3e92c`; binding = r2 + r3 replacements + VP approval + the retraction). **▸ OPERATOR RULING B10 (2026-07-10, registered in `PROTOCOL-DEVIATIONS.md`): a lean slice may use the owning domain pair as the build agent-pair — the floor is at least one real pair, not a fresh team.** Dispatch v3 `PLAN …-032426` (bare token): **m-7 pair = the s7 build pair — m-7.implementer writes (sole writer), m-7.planner pair-reviews**; `…-031733` withdrawn (seat never booted); the `…-031728` stand-down superseded by the ruling; pairs-don't-code stays the DEFAULT. **▸ s7 BUILT + PAIR-REVIEWED + MASTER-VERIFIED (2026-07-10):** the m-7 pair delivered on branch `s7-inv-catalog` (worktree `~/.config/superpowers/worktrees/frank/s7-inv-catalog`, tip **`35aabb9`** off BASE `1d3e92c`) — ten `TestLaw*` checks exact + `catalog.v1.json`, one pair finding (R-1, three-verb negative leg) folded with red/green proof; **master re-verified uncached: test-only diff (14 files, all slice paths), invariants ok, battery 25 ok / 0 FAIL, vet clean.** Fidelity requests CUT: `s7-fidelity-m1` (rows 2/4/5/6) · `s7-fidelity-m2` (rows 1/3/6) · `s7-fidelity-m4` (row 3), TO the pairs' implementer halves. **▸ FIDELITY RETURNED 3/3 (2026-07-10):** rows 1/2/4/5 **CONFIRM**; row 6 must-revise ×2 (test-only: complete the canonical path-family census + planted-per-family negatives [m-1]; live `Outcome.Detail` bounce-reason capture + family-local negative [m-2]) — **routed to the s7 pair** (`SITREP …-113841`); row 3 must-revise ×2 **HELD** — the required `any_row:routing_assignments.chosen_model` negatives go RED on current production: **F-S7-R2-COLGRAIN registered** (pre-existing enforcement gap; the C1 column-grain trigger is already met; the fidelity gate caught a real production defect on the catalog's first outing — B9 executable-truth paying off). **▸ ROW-6 FOLD CLOSED AT THE PAIR (2026-07-10): fold `81dce49`** (one file; census live-derived incl. the lazy quarantine home, scan corpus built FROM the census — drift structurally impossible; 11 per-family planted negatives; live `Outcome.Detail` byte-parity in the named row; red proofs via disposable worktrees, cleaned) — pair review APPROVE no-must-fix; **master re-verified uncached (25 ok / 0 FAIL, one-file fold diff, zero production paths)**; R-1 pre-concurrence formally discharged on the same record. Narrow re-confirm requests CUT (`s7-fidelity-m1/…-124301`, `s7-fidelity-m2/…-124304`). **▸ ROW-6 NARROW CONFIRMS IN 2/2 (2026-07-10): m-1 CONFIRM (`…-124647` — all four fold requirements discharged; census live-derived + corpus-from-census + 11 family negatives verified at its own station) · m-2 CONFIRM (`…-124542` — live D-2 `Outcome.Detail` byte-parity + family-local negatives verified).** s7 fidelity state: rows 1/2/4/5/6 CONFIRMED · rows 7–10 pair-reviewed (m-7's claimed half, per the gated plan) · **row 3 = the sole open item, held on F-S7-R2-COLGRAIN.** **▸ OPERATOR RULED "A" (2026-07-10): B10 second application — the m-2 pair builds the guard** (registered in `PROTOCOL-DEVIATIONS.md` B10; the trigger refines to *small + the owning domain's own mechanism + requirements already adversarially specified*). **s7a ASSIGNED — corrected routing (operator-caught: work enters a pair through its PLANNER when shape freedom remains): `s7a-dispatch/PLAN …-125439` TO m-2.planner with DELEGATED_DISPATCH_AUTHORITY** (`…-125205`, tokened straight to the implementer, withdrawn before delivery). The pair runs its own loop: m-2.planner PLANs the mechanism (per-column validation vs column metadata) → m-2.implementer plan-reviews → on approve + `SCOPE_DIFF: all-in` + no hard trigger the planner issues the token. Master-pinned bar: red-first (`any_row:routing_assignments.chosen_model` shown accepted → then typed-rejected at load) · `declared_deviated` regression pinned · branch `s7a-colgrain` off `1d3e92c` · `internal/fieldspec/`-only fence · registry-data changes flag to master. **▸ s7a PLAN pair-APPROVED (m-2 internal r1→r3, `PLAN-planner-…-133000` + `PLAN-REVIEW-…-133100`); token correctly HELD on the registry-data rule → master AC6 clearance GRANTED (`…-133700`): default-deny column grain + the positive singleton allowlist `gate_referenceable_columns=["declared_deviated"]` — stronger than the finding demanded (closes the class, not the instance); master independently verified the live census (exactly 2 `any_row` atoms, both `declared_deviated`); atomicity condition pinned (allowlist ships with/before the guard). **▸ s7a BUILT + PAIR-APPROVED + MASTER-VERIFIED (2026-07-10):** after the lineage-ID snag (pair PLAN reused the master thread id → token chain resolved wrong → pair withdrew correctly → master ruled option 1, unique `s7a-plan-m2` chain; **CONVENTION PINNED: pair loops under a master thread mint unique sub-DISPATCH_IDs**), the clean chain ran whole: PLAN → lineage-confirming review → pair-issued token → build. Landed at **`s7a-colgrain@d76c3ad`** (red `10ee3a2` first): default-deny `validateGateRowReference` at `any_row` column resolution + `GateReferenceableColumns` member + the AC6 singleton + marker `s6-fieldspec-v4→s7a-fieldspec-v5` additive-MINOR, atomicity held, five-file fence exact, `declared_deviated` retarget keeps the shipped delta singleton. Pair review = diff-grain APPROVE (`…-143200`); **master full uncached battery at the tip: 24 ok / 0 FAIL, vet clean.** Fidelity requests CUT: `s7a-fidelity-m4/…-143432` (R2 routing face + the C1-residue statement) · `s7a-fidelity-m7/…-143436` (load-fault disposition, I-PH error text, §7/digest story for future live stores, A-1 non-perturbation). **▸ s7a FIDELITY 2/2 CONFIRM (2026-07-10): m-4 (`…-144020` — row-3 blocker discharged; C1 discharged at the current grammar grain, residue representational-only for the Step-3 nested-column schema; claim wording bounded) · m-7 (`…-144100` — fails-before-serve trusted-startup posture; I-PH path-free; §7/composite-digest faithful; A-1 non-perturbation; **s8 GENESIS CONDITION registered**: dogfood genesis from the `s7a-fieldspec-v5` bytes + recorded composite digest). **FLAKE-SOCKET-PAR registered** (2 pre-existing socket fixtures flake under parallel runs; pass isolated + `-p=1`; disposition owed at s8). **VP integration request CUT (`RECONCILE …-144333`).** **▸ VP s7a INTEGRATION = REVISE (`…-145048`):** mechanism/chain/fidelity/red-green/scope all ACCEPTED; **one blocker — the v5 registry still carries the s6 provenance tuple** (`registry.json:2-7`: owner `s6-core-registry`, s6 design/plan ids; the test only checks owner-nonempty) — a governance registry must not merge with its audit stamp pointing at the superseded plan. **Fold routed to the pair (`…-145439`):** red-first exact v5-tuple assertion → truthful s7a attribution (strings theirs; truthfulness the bar) → AC6 wording amended → pair review of the exact delta → m-7 narrow final-bytes re-confirm (m-4's confirm stands if provenance-only) → master reruns → VP re-approve. **VP F2 adopted on the record: CC carries no action authority — the merge ask will be a separate relay addressed TO the operator.** **▸ PROVENANCE FOLD LANDED + PAIR-APPROVED (2026-07-10): `s7a-colgrain@2bc0763`** (red `37ac1dc` exact-tuple assertion → green: owner `m-2` · design_doc_id `F-S7-R2-COLGRAIN` · plan_lock_id `s7a-plan-m2` · the truthful B10 note; version/guard/singleton byte-unchanged; weak owner-nonempty check replaced by exact-tuple DeepEqual; AC6 amended "one semantic row delta + the required attribution update"); pair APPROVE at diff grain (`…-150800`); **master re-verified uncached: 24 ok / 0 FAIL, vet clean, tuple read truthful.** m-7 final-bytes re-confirm requested (`s7a-fidelity-m7/…-151000`). **▸ m-7 FINAL-BYTES CONFIRM (`…-152858`): member SHA pinned (`1ef6abab…2485`); the s8 genesis condition re-bound to `2bc0763`; provenance load-inert repo-wide (A-1 / fail-before-serve / I-PH carry unmodified); serialized full suite green. VP context-check (`…-151213`) had already pre-verified the fold at source. RE-APPROVAL PACKAGE CUT (`RECONCILE …-153158`).** **▸ VP RE-REVIEW (`…-154020`): F1 + F2 DISCHARGED, the `2bc0763` package ACCEPTED — one revise-disclosure finding: its fresh verification proved the third flaky test is a REAL PRE-EXISTING PRODUCTION RACE** (`Client.Close`/`readLoop` unsynchronized double-close of `c.done`, `server.go:519-525/555-562`; `-race -count=20` panics on BOTH `main@1d3e92c` and `2bc0763`; reachable via live MCP reconnect). **Truth fold landed (`…-154310`): OI-S7A-CLOSE-ONCE-RACE registered** (kickoff 2b; owner m-7; disposition gated BEFORE the s8 dogfood opens live MCP channels; acceptance shape verbatim: idempotent close owner · reconnect green under focused `-race` · battery rerun); FLAKE-SOCKET-PAR fenced to its two startup fixtures; the honest-sequence evidence rule written into the ledger. **▸ VP FINAL APPROVE (`…-154754`)** — mechanism, attribution (member SHA independently matched), cross-domain fidelity, and the honest verification record all accepted; the race stays a hard pre-dogfood gate; merge authority correctly separated. **▸ MERGE DECISION relay CUT, addressed TO THE OPERATOR (`PLAN …-155040`): `s7a-colgrain@2bc0763` → private `frank/main`, `--no-ff`; option 1 = operator runs the merge, option 2 = grant-and-execute.** **▸ OPERATOR GRANTED (2026-07-10) — executor = m-2.implementer (operator-named, the build seat; s4/s6 precedent). MERGE-GATE dispatch CUT (`s7a-merge-gate/MERGE-GATE …-155633`):** bounded steps — `--no-ff` merge of `2bc0763` w/ the pinned message · vet + serialized uncached battery at the merge commit (sequence-honest, OI-S7A-CLOSE-ONCE-RACE open) · push to the private `frank-dev` · no tag, no branch deletion · execution report back. **★ s7b CLOSED (2026-07-11): merge `main@691d034` (`--no-ff`, parents `2e1b4f0`+`e155aa6`), NO tag, pushed to `frank-dev`; executor m-7.implementer; serialized battery 25-ok/exit-0 file-captured ×2 stations; trail lint-clean end-to-end.** Register: **`OI-S7A-CLOSE-ONCE-RACE` CLOSED — the s8 live-MCP-channel gate LIFTS** · `FLAKE-SOCKET-PAR` CLOSED (×3 members, by mechanism) · `CRASHPOINT-KILL-RETURN` CLOSED. Record: `RECONCILE.md` §s7b. **The s8-blocking list is now purely design-side: m-2's reconciling rev · m-7's two design docs · master's two reconciles · the operator's two ratifications (activation-authorization; m-3's three defaults).** **▸ s7a MERGED (2026-07-10): `main@54420dbc`** (executor m-2.implementer; parents `1d3e92c`+`2bc0763`; no conflicts; serialized battery green at the merge; pushed to `frank-dev`; no tag; **master re-verified**: parents/clean/ls-remote match/vet/2× serialized suite PASS). **▸ TRAIL INCIDENT, dispositioned same day (kickoff 2c):** the executor's claim-aware lint caught that master's grant relay lacked the recognized authorization field (prose-only conveyance) — merge stands (authority real, shape defective); the corrective record (`MERGE-GATE …-160704`) carries `HUMAN_MERGE_AUTHORIZATION` explicitly post-hoc; **the root ordering failure stays as the honest scar** (backdating refused); **CONVENTION PINNED: merge-grant relays carry the recognized field at grant time.** **▸ OPERATOR COUNTERSIGNED ("ratified", 2026-07-10) — the s7a trail incident is CLOSED. ▸ THE s7 ENDGAME ORDER IS CUT (`s7-dispatch/SITREP …-161846`, TO m-7.implementer under its standing dispatch):** merge `main@54420dbc` INTO `s7-inv-catalog` (merge not rebase — trail SHAs stay valid) → fold the two `chosen_model` `any_row` negatives into `TestLawR2NoModelPredicate` (green against the merged guard; the mechanism red was s7a's `10ee3a2`; claim wording stays m-4-bounded) → serialized battery + vet + diff audits → pair review → report. **▸ ENDGAME FOLD EXECUTED + PAIR-APPROVED (2026-07-10):** merge `908c878` (parents exactly `81dce49`+`54420dbc`; the branch absorbed the guard, trail SHAs intact) + fold **`61cf35e`** (one file, +41: both `chosen_model` `any_row` negatives asserting the guard's typed error — pass-for-the-right-reason verified); pair review APPROVE no-must-fix (`RECONCILE-planner-…-163404`); **master re-verified (parentage · one-file fold diff · invariants ok · serialized battery 25 ok / 0 FAIL).** **FINAL re-confirm requests CUT: `s7-fidelity-m2/…-163846` + `s7-fidelity-m4/…-163850`.** **▸ BOTH FINAL CONFIRMS IN (m-2 `…-165309` · m-4 `…-165351`, each verifying the pass binds to the guard's typed error and the claim stays bounded) — THE s7 FIDELITY MATRIX IS COMPLETE** (rows 1–6 confirmed by every owning seat; 7–10 pair-reviewed per the gated plan). **▸ THE s7 INTEGRATION PACKAGE IS CUT (`RECONCILE …-170133`, TO the VP):** all five r3 acceptance items satisfied at `61cf35e`; test-only branch (main absorbed by merge, parentage exact); s8 obligations + S7A-TRAIL-FINDINGS disclosed; on approve → the TO-operator merge relay (w/ `HUMAN_MERGE_AUTHORIZATION` at grant time, per the pinned convention) + the **`s7-close` tag decision** → **the operator merge closes Step-2's opening slice.** **▸ VP INTEGRATION = REVISE (`…-171215`):** ancestry/scope/row-3/staged-governance/E2 all ACCEPTED — two acceptance behaviors not yet executable: **F1** row 6's new-family tripwire is circular (negatives inject into an already-built list; AST walk seeded from catalog-known symbols — a new symbol rides green) and **F2** row 9 never executes `recover.RunWithProcessor` (manual double-enqueue proves replay-harmlessness, not at-most-once re-enqueue); **F3** report correction adopted (tracked-vs-untracked stated exactly). **Folds routed to the s7 pair (`SITREP …-171701`):** discovery-over-the-real-egress-boundary + genuine-new-symbol scratch red proof; recovery executed w/ per-`intake_id` call counts; row 3 + catalog byte-identical (no m-4 re-review). Chain: folds → pair review → narrow m-1/m-2 row-6 re-confirms → corrected package → VP re-review → THEN the merge relay.** **▸ F1/F2 FOLDS EXECUTED + PAIR-APPROVED (`bbf3147`, +181 test-only):** F1 discovery derives recognition from the boundary code itself (write-chain + dispatch-switch, zero catalog seeding; scratch red verifiable by construction) w/ 17-site census equality; F2 executes `recover.RunWithProcessor` w/ per-`intake_id` counts; protected row-3/catalog bytes `diff --exit-code` identical (m-4 stands); the pair's s8 note REGISTERED (the discovery's own walk roots + boundary-file set get governed at §7-pinning); master verified (serialized 25 ok / 0 FAIL). **Round-2 row-6 re-confirm requests CUT (`s7-fidelity-m1/…-174040` · `s7-fidelity-m2/…-174044`).** **▸ ROUND-2 CONFIRMS IN 2/2 (m-1 `…-174557` — census/corpus/negatives intact at exact cites · m-2 `…-174451` — the D-2 capture stays the sole bounce-reason capture, the discovery layer structurally cannot mutate it) — the fidelity matrix is RE-CLOSED at `bbf3147`. ▸ THE CORRECTED PACKAGE IS CUT (`RECONCILE …-175524`, TO the VP):** every required-return item satisfied (folds + pair review + round-2 confirms + the scratch-red transcript + the recovery callback-count evidence + the serialized battery at five stations + the exact tracked-vs-untracked status). **▸ VP RE-REVIEW (`…-180750`): F2 CLOSED whole · F1 closed at the symbol grain — ONE remaining blocker, F1-R2: the two-file discovery root is itself an unguarded literal** (a new production egress file rides green; §7 pinning governs an intentional census edit but cannot DETECT an unparsed file). **F1-R2 routed (`SITREP …-180956`, TO m-7.implementer):** a full-tree non-test scan guards the boundary file set (egress capability outside the registered boundary fails the law; the 17-site census retained inside; persistence ≠ egress encoded with the guide); scratch red = a genuine NEW egress file failing the law BEFORE any census edit; protected blocks byte-identical ⇒ **no m-1/m-2/m-4 round owed (VP pre-ruled)**. Chain: fold → pair review → master reruns → corrected package → VP → the TO-operator merge relay + the **`s7-close` tag decision**.** **▸ F1-R2 FOLDED (2 commits: `8e0f6f2` full-tree boundary guard + `5e6bf83` the pair's pre-concurred S-1 — portable egress idioms [.Method switch · .conn Write] fail TREE-WIDE outside registered boundaries):** the genuine-new-FILE scratch red proven (`internal/futuretransport/sender.go` → the law failed w/ both outside sites, pre-census-edit; cleanup proven ×3); one file changed across the whole chain; protected bytes identical ⇒ no owner round (pre-ruling). **Round-3 package CUT (`RECONCILE …-183256`)** — incl. the plainly-stated claim boundary (genuinely novel wire idioms aren't scanner-definable a priori; locked §8 routes new transports through amendment+grill) and the sequence-honest battery (master attempt-1 pipe-count anomaly disclosed; captured run exit 0 / 25 ok / 0 FAIL). **▸ VP APPROVE, round 3 (`…-183819`): NO REMAINING INTEGRATION BLOCKER** — F1-R2 closed (the tripwire discovers the boundary tree-wide; the new-file proof "materially stronger"); the claim boundary accepted as honest (novel idioms → the §8 amendment ritual, no overclaim); all fidelity + protected bytes standing; the anomaly-disclosure practice endorsed. **▸ THE s7 MERGE DECISION IS CUT, TO THE OPERATOR (`PLAN …-183959`):** `5e6bf83` → private `main`, `--no-ff`; the **`s7-close` tag choice presented** (rec: A — tag it, continuing the s1..s6-close pattern; private repo only); executor default = m-7.implementer (the s7a "original implementer" precedent); the grant will ride the execution dispatch as `HUMAN_MERGE_AUTHORIZATION` at grant time (the s7a lesson, applied). **▸ OPERATOR GRANTED (2026-07-10): "granted, tag it, and send it to m-7.implementer to merge." MERGE-GATE dispatch CUT (`s7-merge-gate/MERGE-GATE …-185615`) — with `HUMAN_MERGE_AUTHORIZATION` in the header AT GRANT TIME (the s7a convention applied; the dispatch-root lints clean end-to-end this time):** executor m-7.implementer; bounded steps — `--no-ff` merge of `5e6bf83` w/ the pinned close message · **tag `s7-close`** · vet + serialized uncached battery FILE-captured at the merge commit · push main+tag to the private `frank-dev` · no branch deletion · execution report. **★ s7 CLOSED (2026-07-10): merge `main@2e1b4f0` (`--no-ff`, parents `54420dbc`+`5e6bf83`), tag `s7-close`, pushed to the private `frank-dev` (main + tag remote-matched); executor m-7.implementer; battery at the merge commit 25-ok/exit-0 serialized FILE-captured at two stations; the merge-authority trail lints clean end-to-end (the s7a lesson applied).** The constitution is executable: ten named laws in every battery from this merge on — weakening one = a red battery naming it. The slice paid for itself pre-merge: F-S7-R2-COLGRAIN (found→s7a-fixed→guarded) + OI-S7A-CLOSE-ONCE-RACE (found, gated pre-dogfood) + S7A-TRAIL-FINDINGS + three standing conventions. Close record: `RECONCILE.md` §s7. **▶ THE PRE-s8 PACKAGE IS DISPATCHED (2026-07-10):** **(1) operator ruled "A" — B10 third application: the s7b lane (`s7b-dispatch/PLAN …-193159`, TO m-7.planner, planner-first w/ delegated dispatch)** — the m-7 pair dispositions **OI-S7A-CLOSE-ONCE-RACE** (mechanism theirs; VP acceptance pre-pinned; red-first `-race` demo; branch `s7b-close-once` off `2e1b4f0`) **+ FLAKE-SOCKET-PAR** as a second separately-accepted item (unify-or-separate per evidence); m-1 fidelity on the lifecycle seam → VP → operator merge; **on merge, the s8 live-channel gate lifts**. **(2)+(3) the two joint design SITREPs (`…-193204`/`…-193209`) were STRUCK by the VP (`step2-prep/RECONCILE …-201733`) — rightly: report-only relays cannot direct design; shared IDs + joint authorship recreate the s7 collision class; both surfaces trigger the grill rule; plus two substantive bars (F4: executor claim must split handle-absence from ambient process authority — "no conductor-governed mutation handles / no canonical-state writes through the provided surface", OS sandbox = an operator election; F5: load-time digest proves shape, not owner participation — the owner-fidelity fork is a grill/operator question; the discovery descriptor must cover the FULL mechanism + the catalog's canonical runtime home). REPLACED with four unique medium-tier grilled DESIGN lineages, one author each, implementer DESIGN-REVIEW each: `s8-design-m7-config` (`…-214559`) · `s8-design-m2-grammar` (`…-214603`) · `s8-design-m7-executor` (`…-214607`) · `s8-design-m3-registry` (`…-214611`); shared content = named boundary contracts. Master reconciles the reviewed locks; THE s8 PLAN HOLDS until they exist.** **s7b APPROVED by the VP (`s7b-dispatch/RECONCILE …-201732`)** w/ two conditions riding the pair PLAN: FLAKE-SOCKET-PAR needs a mechanical acceptance (focused repeats + 3 consecutive parallel suites, or a checked-in exercised parallelism pin, race-registration status stated), and the merge lifts ONLY the live-channel blocker, not global s8 readiness. → Then: the s8 observe-spine PLAN (fresh team, m-3 guide) + the dogfood relaunch (genesis from the exact `s7a-fieldspec-v5` bytes).** **▸ DESIGN RETURNS (07-10 late): m-3's `s8-design-m3-registry` COMPLETE r1** (registry face locked; 3 operator questions w/ rec defaults; prereq = the master reconcile w/ the m-7 executor doc) · **m-2's grammar = honest blocked-by-dependency after 5 rounds, having found a REAL registry bug** (`surface_intent` gated on `layer_present:observe` alone vs the locked non-gate-bearing-only scope). **Coordination routed:** `s8-grammar-confirm-m3` (manifest) · `s8-grammar-confirm-m7` (placements) · `s8-grammar-surfaceintent` (TO m-5+m-6: choose A new-atom / B manifest-applicability, m-2 recs B). **Operator asks pending: ratify the activation-authorization semantics + m-3's three defaults.** Still in flight: m-7's two design docs · the s7b build.** Chain: s7a build → m-4 (R2 routing face) + m-7 (load surface) fidelity → VP integration of s7a → operator merge of s7a → s7 pair rebases + folds the row-3 negatives → m-2+m-4 re-confirm → full s7 package to VP → operator merge closes the slice.** **▸ s7b CLOSED (2026-07-11): merged `691d034`** (idempotent channel-close via `sync.Once` owners · crashpoint block-after-kill · fixture startup hardening); OI-S7A-CLOSE-ONCE-RACE + FLAKE-SOCKET-PAR ×3 + CRASHPOINT-KILL-RETURN dispositioned — **the s8 live-channel gate LIFTED** (`RECONCILE.md` §s7b). **▸ ALL FOUR s8 DESIGN LANES PAIR-APPROVED (07-11)**: m-2 grammar · m-3 registry r1(+r1a/r1b) · m-7 config-host r5 · m-7 executor-host r4. Master reconciles RUN: **B (m-3×m-7-executor) COMPLETE** (one-for-one contract match; the “confined” wording folded r1b, `s8-design-m3-registry/…-043000`); **A (m-2×m-7-config): two must-folds with m-7 as r6** (A1 knob-home version carrier · A2 catalog reader-capability gate; `…-040556`; pair fold round open `DESIGN-…-041219`). **▸ OPERATOR RULINGS (07-11)**: activation-authorization ✔ · m-3’s three interim defaults ✔ with the MANDATORY s10 sunset · **F5 = fork (a) lean shape-only load claims (“stamp a”) ✔** · **executor containment = the v1 handle-surface ceiling ✔, the OS sandbox SHELVED as a recorded future feature** (kickoff Pre-flight item 5; records `s8-design-m7-config/…-130825` + `s8-design-m7-executor/…-130830`) → **the executor-host r4 lock is EFFECTIVE; the config-host lock awaits ONLY m-7’s r6 return + implementer re-review → then the reconciled DESIGN locks + PROCEED → the s8 observe-spine PLAN (m-3.planner emits; fresh team, m-3 guide) + the dogfood relaunch** (genesis: the `s7a-fieldspec-v5` bytes · engine `observe:false` · the blessed catalog).** **★ ALL FOUR s8 DESIGN LOCKS EFFECTIVE — s8 DISPATCHED (2026-07-11 afternoon).** The config lane closed at r13 after a five-supersession endgame: the r6 folds grew into r6→r13 (A1 = engine `version` int w/ exact bump semantics · A2 = the closed reader-capability table converged w/ A1, member-SET drift proven already-fail-closed FX-CFG-9) + the §5.1 existing-store BLESS/ADOPTION path (r8-F4 deadlock → the `member: adoption` closed variant, m-2-confirmed twice w/ one convergent correction — `new_digest` stays a HEADER — and m-2’s own list-name correction `{catalog, engine}`) + §5.2 the singular `member: catalog` day-2 arm (master rec (a) concurred; state-aware; one MAJOR moment). Master verified the SIX-item checklist + A1/A2 at the byte grain over r13 (read whole) → **`RECONCILE-…-145128` declares `s8-design-m7-config-r13` EFFECTIVE** — the four-lock board complete (grammar · registry r1b · executor r4 · config r13). **→ `s8-dispatch/PLAN-…-145129` TO m-3.planner**: the observe-spine PLAN, planner-first + DELEGATED_DISPATCH_AUTHORITY (7 conditions incl. operator-only merge w/ HUMAN_MERGE_AUTHORIZATION at grant time), fresh slice team (operator boots), m-7 build-fidelity, m-2 executes the ONE s8 registry changeset (adoption+catalog tokens + surface_intent removal + the bounded §2/§9 amendment), **SEQ-1 named** (registry-amendment × genesis-v5-pin × bless ordering + the adoption record’s fill-gate exposure — the PLAN states the order + which store the dogfood uses).** **▸ THE CONFUSION-FIREWALL DIRECTIVE — OPERATOR-RATIFIED (2026-07-11, post-s8-dispatch): the standing scope rail.** Operator-originated (m-2 session, scribed `frank-threat-model-scope/…-162331` at the correct authority grain), master-assessed (`…-162826`: ALL FOUR s8 locks survive by FUNCTION analysis — digest=drift/one-truth · adoption-atomicity=crash-safety/history-truth · A2=anti-confidently-wrong-machinery · executor-ceiling/sandbox-shelf = the directive pre-applied), ratified verbatim. Text of record: **`ARCHITECTURE.md` § CROSS-DOMAIN SCOPE AMENDMENT** (+ RECONCILE entry + kickoff Pre-flight 6): threat = confusion/hallucination NOT malice · evidence = deterministic checks + independent cheap-model adjudication (medium-strength, labeled) · seams-not-security-primitives, governed by **Rail A** (open where ignoring loses detail; closed where it changes acceptance MEANING) + **Rail B** (cut by function, not flavor). Routed: the s8 ADDENDUM (`s8-dispatch/…-163144`: SEQ-1 default = FRESH GENESIS first, adoption lands when actually needed; Rails = build-time criterion on new surfaces; dispatch unchanged) · **design item 9 NEW** (the s9 adjudication rung, m-3, NAMED constraint: a cloud-judge call is an external send under the egress fence) · the m-1 identity-seam rail (`…-163145`, no work). Positioning line banked for the public flip.** **★ s8 CLOSED (2026-07-12) — THE OBSERVE SPINE IS LIVE: merge `main@8941889` (--no-ff, parents 691d034+b2c2062), tag `s8-close`, pushed frank-dev, remote-matched; operator-granted w/ the recognized field at grant time (3rd clean run of the convention); two-seat exit APPROVE + master 3rd-seat verification (merge tree byte-identical to the candidate; own battery 25ok/0FAIL; capture SHA reproduced). The conductor now OBSERVES done-ness in the atomic submit through governed supply only — false-done rejected typed at the real socket (read-file AND suite legs), exit legs 1+2 of the step-exit LIVE. The build arc: 2 ritual amendments (claim-input v6→v7 declaration surface; supply-set engine v1→2 — ambient cwd + silent timeouts KILLED) + 5-row hardening (both genesis doors · §4a read mechanism · §13 absence floor + string-veto · stage-once executor) + 8 failure classes caught in-slice w/ the catch layer moving earlier every round + 1 recorded scar answered by the mechanical diff→license table. Record: `RECONCILE.md` §s8. **▶ NEXT: the Q6×Q4 record_kind reconcile (master, the sole s10-PLAN prereq) → s10 comms spine (fresh team) → s9∥s11. The LIVE RELAUNCH = the operator's separate checklist act (lane-root pin · catalog bytes · honest E3/E4).** **★ s10 CLOSED (2026-07-13) — EXIT LEG 3 LIVE, ALL THREE STEP-EXIT LEGS NOW EXIST: merge `main@39474d0` (ff from 8941889, 13 commits), tag `s10-close`, NOT pushed (operator timing); master verified (lineage/tag exact + own battery 25ok/0FAIL + vet clean). The A-gate path end-to-end on the fresh v8 store: ODB → park → the operator's validated reply → re-observe → EXACTLY-ONCE wake → deterministic resummon (A-2 dedupe); both sunsets GONE behaviorally (soft-expiry park+ODB {kill,extend} w/ block-only ceiling; live-prompt pre-spawn default-DENY gate-lift, zero spawn). B11's first full slice: straight-through build + planner end review (MF-1) + an operator-ordered second review (7 must-fixes incl. a genesis v8 integrity bypass + a commit TOCTOU — folded at 39474d0) + three owner CONFIRMs, stop-on-contradiction never fired. Trail residual disclosed: direct operator authorization w/o the grant-relay field — one-line ratification OFFERED (`s10-merge-decision/…-041930`). Carries filed to s11/m-7/m-6. Record: RECONCILE §s10. ▶ REMAINING IN STEP-2: s9 ∥ s11 → the step-exit test. B12 (design-side cadence) OPEN with the operator before those dispatches cut.** **▶ s9 ∥ s11 DISPATCHED (2026-07-13) — the LAST build phase of Step-2.** Both continuation-slice PLANs cut planner-first w/ delegated authority: **s9 evidence-thicken** (`s9-dispatch/…-130004` TO m-3.planner — check-registry fill + red→green differential + executable_claim_results + opaque-lane/attestation/mixed + OI-S6s + ⑤ fixture; NEW design items 9 [adjudication rung, egress-fenced] + 10 [turn-baseline fence]) ∥ **s11 comms-thicken** (`s11-dispatch/…-130005` TO m-6.planner, the LAST slice — full 7-state FSM + B/C/D buckets + elaborate-more fork + 8a hardening + the 9-item s10 cleanup card + G4 config). THREE design legs routed in parallel: the **scope_paths four-pin co-sign** (`s9-scopepaths-cosign/…-130001`, m-2+m-3+m-1 — the s9 PLAN blocker, VP F1) · the **8a joint review** (`s11-8a-joint-review/…-130002`, m-2+m-6.impl — gates s11's 8a lock) · **OQ-2 the fork ceiling** (`s11-oq2-ceiling/…-130003`, m-5 — gates s11's fork build). **B12 (design-side cadence) DECLINED** (operator: design churn is fine, precision before implementation) — B11 stays build-scoped; design loops keep the full ritual. m-2 = the named registry collision edge between the two thickens (m-2 guide arbitrates). On s11 close → **the STEP-2 STEP-EXIT test** (all 3 legs live + INV-CATALOG red-battery demo + uncached green).** **▸ s10 PUSHED to private frank-dev (2026-07-13): origin main 8941889→39474d0 + tag s10-close, remote-matched; a private dev-remote sync (the pre-push guard held), NOT the public flip.** **★ s9 CLOSED (2026-07-14) — tag `s9-close` at `main@d91fcfb` (--ff-only, 9 commits, PUSHED frank-dev); master-verified (FF tree byte-identical to the battery-cleared candidate 26ok/0FAIL; capture SHA `2ac1161a…` reproduced; tag/parent/ls-remote exact). The evidence layer thickened: find-references E1 on the shared FS worker · §6.1 verdict pass (CheckVerdict untouched) · the master-activated lane_vcs:none opaque-accept branch (branch-only; git/nil never accepts; Option-2 E0 floor restored; probe removed) · m-7 E1–E10 owner bytes verbatim · FX-VCS matrix · ⑤ ODB egress pair. Fresh s9.planner/s9.implementer pair (Option-A staffing correction — the mis-seated token VOIDED before harm; that token LINTED CLEAN → the standing 'gates check authority-shape not staffing-semantics' limit, the honest what-the-gates-don't-catch line). B11 straight-through, end review APPROVE, 2 owner confirms clean, AO-1=CARRY. Carries to master's step-exit queue: T7/T8 · C-1 · R-1 · AO-1 · the blocked ledger (B1 scope_paths §F pending m-1). 4th clean run of the grant-time-field merge convention. Record: RECONCILE §s9. **▶ ONLY s11 LEFT in Step-2** (the comms thicken; struck relays re-cut fresh now) → then the step-exit test closes Step-2.** **★ s11 CLOSED (2026-07-14) — THE LAST STEP-2 BUILD SLICE: tag `s11-close` (annotated `0462c24`) at `main@502e06c` (--ff-only, 18 commits, PUSHED frank-dev, main+tag remote-matched, PR #1 merged); executor s11.implementer on the operator-granted bounded dispatch (recognized field at grant time — the convention's 5th clean run); master-verified (local main + tag exact; own uncached battery 26ok/0FAIL/vet clean/ten laws at 502e06c; v4 fold spec-exact). The comms mechanism thickened: B/C/D bucket projections + the complete 7-state FSM + the full g1 §B 8a hardening (both reason tokens `stale_schema`/`stale_choice_set` byte-exact, frozen-π guard fail-closed, no-wake, new decision identity, real-process crash-replay) + the ③ known-A NF + T8 cleanup (8-of-9) + G4 cadence re-homed at engine v4. Fresh Option-A pair, four-lens end review unanimous + batched STOP-ON-CONTRADICTION owner confirms. TWO mid-build master rulings at the bytes: the T8 fence split (item-8 `finalizeRun` granted g2-independent; item-2 soft-expiry arbiter DEFERRED out — the executor/fs_worker paths' drain+termination semantics diverge, so a shared arbiter is a race-semantics change not a refactor; post-Step-2 m-7+m-3 design cell) + the T9 r13 config-lock contradiction (OWNER-CAUGHT: `resummon_cadence` landed on locked engine-v3 w/o a version bump — ruled (a) re-home at engine v4 folded on-branch + m-7 hunk-by-hunk countersign, NOT (b) retroactive descriptor widening which would make 'engine v3' name two key-sets; ruling (a)=r13 conformance, no deviation). Catch-ledger completed the cycle-datum pair: the end review catches what a green battery cannot (s10); the owner confirm catches what the review panel cannot (s11); binding refinement — a fence row licenses a FILE, never the owner path on a locked contract inside it. g1 CLOSED/T6 built; **T5/T10 HELD OPEN behind g2/dc, acceptance-OPEN per FINDING-4** — master's step-exit disposition. Carries: T5/T10 gate-bound · the item-2 arbiter design cell · the s9 queue (T7/T8·C-1·R-1·AO-1·B1 §F m-1). Record: RECONCILE §s11. **★★ THE STEP-2 BUILD IS COMPLETE — all five slices closed (s7·s8·s10·s9·s11). The ONLY remaining Step-2 act is the master-owed STEP-EXIT TEST** (all three legs live on the dogfood store + the INV-CATALOG red-battery demo + uncached green) → on its pass, Step-2 closes.**
- *(superseded)* s4 exit gate master-accepted + VP-confirmed — `s4-wire-impl@6a23cf0` (16 commits; **21 pkgs green, CTO re-ran uncached**; `s4-exit-gate/RECONCILE …-231116`). **THE OPERATOR-AS-TRANSPORT IS OVER, demonstrated live:** a governed relay flew from a live **Claude Code** seat to a live **Codex** seat, conductor-stamped `from: s4-wire.host-a`, no human transport (`relay-4a33925b…`, verified at the store). §7 config-change round-trip on an existing store + the owed cycle exercised live; **`OI-S3-CONFIG-CHANGE` discharged → Step-1's mandatory owed set is EMPTY.** Three gate-day findings all dispositioned: F-GATE-1 (handshake `serverInfo.version`) fixed+class-pinned; **F-GATE-2 (owed fill-time-authority gap — my finding) fixed as 5 registry `required_when` rows, red-first**; F-GATE-3 (shim-stderr) ruled out-of-I-PH-scope with grounds. One discretionary follow-on proposed: `OI-S4-TOKEN-SCOPE` (the first post-close owed item on the live store). **Remaining: (optional VP pass) → operator merges + tags `s4-close` → master folds S4 → s5** (consumer schemas, built OVER the wired conductor — first usage data) **→ Step-1 exit test.**
- **m-8 Provider Adapters + m-9 Model Runtime + m-10 App Control Plane/Supervisor** — Division II, greenfield/DESIGN-only; boundaries re-cut by the ratified reframe (`STEP-3-ARCH-AMENDMENT.md`). Lane states: m-10 (new) booted + coordinated-first-stage DESIGN issued/held; m-5 ceiling-host amendment issued/held (coordinated first stage); m-8/m-9 HELD, re-dispatch app-side at stage 2; m-3 egress HELD (stage 3); m-4 deferred to Step-4; m-7 credential lane re-owned to m-8 (m-7 conductor-host untouched). Product domains (Division III: TUI · Email UX · UI/UX · Platform Plumbing) — Step-4+ cycles. *(Superseded by stage-4/5/6 progress — see below and the Current phase banner.)*
- **Per-seat lane-2 pointer board (as of 2026-07-25, stage-6 §11 re-lock — supersedes the DESIGN-only line above):**
  - **m-1 Trust & Identity** — env/redaction component pair-approved `d34a7c47…`.
  - **m-2 Forms & Determinism** — §5-E component pair-approved `c3a8cd61…`; `relay.submit` cell pair-approved `5ec7a3d2…` (bound into the §D-settlement amendment packet). Frozen final `83d8e63e…` unmoved.
  - **m-3 Observation & Evidence** — lane-2 E0/E3 delta pair-approved **r19 `92e08d09…`** — HONEST-PARTIAL (T1–T8 live; N910 = documented MVP limit; r7-mirror deferred to v3). Schema-version amendment ratified (contract `6e2abe40…`). Holding. Frozen r4 `009df607…` unmoved.
  - **m-8 Provider Adapters** — B/E producer `c0b7b488…` (r5) + 2a/2b `refusal_stage` discriminator pair-approved **r7 `734e44b7…`** — lane-2 producer obligations COMPLETE. Frozen r12 `4b670a79…` unmoved.
  - **m-9 Model Runtime** — lane-2 delta pair-approved **r12 `04422965…`** (three §5-E recipes; A3/B1 delegated under F73). Final batched revision (§2.6 + §7 `relay.*` + §6 `turn_failed`) HELD for §D-settlement ratification. Frozen lifecycle r21 `4d3bd14e…`, worker `cb7ff970…` unmoved.
  - **m-10 App Control Plane** — B/E carriage pair-approved **rev3 `cd17db32…`** (the R3 gate that unblocked m-3); producer delta live at **rev14 `b96a1511…`** — NOT pair-approved (ancestry/wording fold; will re-cut fresh post-ratification). Frozen contract r40 `d2ce9831…`, control-plane r10 `6fd1d655…` unmoved.
  - **§D-settlement amendment — RATIFIED 2026-07-25** (`master/STEP-3-STAGE6-SETTLEMENT-AMENDMENT.md` rev4 `1fa71cb8…` + bound m-2 cell `5ec7a3d2…`; recorded §8b `step3-relock-settlement-amend/RECONCILE-orchestrator-planner-20260725-170000`). Four corrections in force; propagation matrix CLOSED — **Lane 2 (the interface DAG) is CLOSED** (9 settled owner bases + 5 joins). **Item A** is RATIFIED + AUTHORED (`master/STEP-3-INTERFACE-LOCK.md`, external SHA-256 `cbd1893c…`) and in VP + F73 review, per the item-A simplification amendment (`master/STEP-3-ITEM-A-SIMPLIFICATION-AMENDMENT.md` rev7 SHA-256 `3443f73d…`, VP-approved, operator-ratified 2026-07-27) which WITHDRAWS the prior bundle mechanism (`STEP-3-INTERFACE-BUNDLE.json`/`bundle_sha256`) and the extraction recipe (`master/STEP-3-ITEM-A-RECIPE.md` r3 `06e6956e…`, not built). **Next:** Lane 4 (the Master+VP re-lock over the record's external SHA + freezing `STEP-3-EXIT-FIXTURES.json`) → Lane 5 (T4). H-12 hard-blocks external use throughout.

## Decisions (c1 — LOCKED)
- **Org / Roadmap / both c1 reconciles** — LOCKED (VP-approved); own-the-gate-first; `C1_PRODUCT_SCOPE = B`.
- **Transport** — Option A minted per-seat credential = confusion-resistant (GRILL-LOCK D4; forgery-robust-by-construction = the D3-shelved wrap milestone, not the Step-1 claim).
- **Schema carrier** — bespoke FieldSpec registry; strict form-only submit; `X-` overflow; zero-migrator versioning.
- **Shared seam** — R1 operator/special-address; R2 routing = separate seat-stamped relay (model never a gate input);
  R3 observe-integrity (`evidence_integrity`). PARENT (m-1) strengthens lineage (m-2) → more strongly confusion-resistant (D5 residual).
- **identity ≠ authority** — ratified (`ARCHITECTURE.md` §5).
- **§J operator-judgment** — RATIFIED (`ARCHITECTURE.md` §J): J1 `on_timeout = hold_and_resummon` (never auto-approve);
  J2 `gate_category` default set, **operator-configurable** (forward m-6/config); merge split by target branch +
  protected-branch set.

## Forward requirements (recorded; do not reopen the lock)
- Customizable `gate_category` membership / A·B map / protected-branch set — m-6 + config (`ARCHITECTURE.md` §J).
- Opt-in away-mode external-inbox bridge, egress-gated — m-6 scheduler + m-3.
- DI-2/DI-5 independent-isolation realization (the fork-2 infra call) — build cycle.
- m-3 / m-4 / m-6 full domain designs — later cycles.
- Full PLAN carry-forwards in `ARCHITECTURE.md` §6.

## Relay log
See [`master/relays/INDEX.md`](relays/INDEX.md). c1 terminal: close declaration
`c1-joint-lock/RECONCILE-orchestrator-planner-20260629-181713.md`.
