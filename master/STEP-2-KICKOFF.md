# Step-2 KICKOFF — governance hardening + minimal comms / "unfakeable gates, self-pacing lanes" (PLAN-approach; pre-decomposition)

**Status: PLAN-APPROACH DRAFT (2026-07-10, operator-opened) — PAIR-VERIFIED same day.** All four consulted pairs returned **CONFIRM against their locked lines** (`step2-prep` relays: m-7 ×5 + 3 precision notes · m-6 ×4 + 3 annotations · m-2 ×6 · m-3 confirm-with-2-precision-must-revise — **F-1/F-2 folded below**, so this revision carries the exact locked semantics, not my paraphrase). **r3 (2026-07-10): the VP's revise verdict folded** (`step2-prep/RECONCILE-orchestrator-reviewer-20260710-012951`, VERDICT: revise — 4 findings, all accepted): `scope_paths` REOPENED as an s9 PLAN blocker with the four-pin producer/reader contract (F1); the 8a record corrected to the **partial** co-sign it actually is (F2); the comms work re-cut into **two** slices — s10 spine + s11 thicken, step-exit after s11 (F3); the baseline gate made a **pre-s7 operator precondition** with the exact base + expanded inventory named (F4). **r4 (2026-07-10): the VP r3 re-review's three truth/cross-reference fixes applied** (`…-014110` — substantive recut ACCEPTED, F1/F3/F4 closed): the 8a **co-signed floor** stated exactly (`stale_schema` moved to the m-6-proposed set); the four pre-split s10 references re-routed (guide scope pluralized · OQ-2 + fixture ③ → s11 · the ratification target = s7–s11); the dashboard row disambiguated. The formal decomposition + VP-gated PLAN dispatch (`step2-plan`) follows on this r4, pending the VP co-sign. Design-of-record: **already locked** — m-3 `c2-lock` + m-6 `c3-lock` + their post-lock folds (c4-cq-*, c5 decisions ②③⑤, c6/c6.1 fixes); m-7 `c4-design-m-7-lock` hosts. Sequencing: `ROADMAP.md` Step-2 (:96-101). Step-1 CLOSED 2026-07-08 (`s6-close`, `main@6a1198a`; exit test passed live; zero F1–F17 deferred).

## Goal (one line)
**Make the gates unfakeable; let lanes self-pace.** The conductor **observes** done-ness at send time — the observe-as-send gate runs inside the atomic `submit()`, captures evidence (E1/E2), fail-closes authority classes (decision ②) — and the m-6 mechanism lets a parked lane **wake on reply** without the operator hand-pumping. Rides the Step-1 conductor and existing runtimes (own runtime = Step-3). This is the moat-completing step (ROADMAP Positioning): evidence *observed*, not supplied.

## Framing (honesty rails)
- Step-2 upgrades work claims from `self_reported` to **observed** for **E1/E2 only**; E3 (integration) / E4 (live-verify) are Step-3+. "Observed" = a conductor point-in-time snapshot from outside the lane (DI-5). Opaque lanes degrade honestly to `self_reported` (labeled, `degradation_notes`) — **except authority classes, which fail closed**: an authority-class record with `record_integrity ∈ {self_reported, mixed}` is `held` + escalated (decision ② recorded 2026-06-30; the **`mixed` leg is the CQ-2 canonical** — m-3 §3.2(d)/§6:124 — a partially-opaque lane rolls up `mixed` and must hold too; F-1 fold).
- The identity posture is unchanged: **confusion-resistant, not theft-proof**; malicious code-executing agents remain out of scope (deployment-fork GRILL-LOCK 2026-07-01; D5 residual stands). The suite-executor isolation below is TCB hygiene — lane code must never run holding conductor handles — not an adversarial-containment claim.
- **Egress stays a fixture-scoped scanner through Step-2.** Step-2 comms are local-only (ROADMAP:49-51); the live outbox chokepoint activates with the first external send — the away-bridge, which is the deferred step-(d) carry, **not Step-2**. Content safety remains best-effort / encoding-evadable when it does land; full hardening = Step-6. *(CTO ruling proposed here; operator ratifies.)*
- The s6 ledger **exonerated engine latency** (median project/read 10ms, submit 40ms) — no speculative perf work.

## What Step-1 already gives (verified in-tree 2026-07-10)
The whole layer is **scaffolded-but-dormant behind one switch** — s5 landed the schema precisely so Step-2 needs no foundation surgery:
- **Registry Block A** (dormant behind `layer_present:observe`; owner system/computed): `achieved_evidence` (E0–E4), `target_gap_result`, `evidence_integrity`, `record_integrity`, `executable_claim_results` (row_array), `egress_scan_result`, `degradation_notes`, `attestation_source`, `authority_class`, `deviated_observed`, `bucket_binding_observed`, `surface_intent`; plus `completed_proof` (evidence_ref), `away_bridge_eligible`, `model_name`. **`EVIDENCE_TARGET` is already live + always-required** — the false-"done" leg is precisely "declared target vs conductor-computed `achieved_evidence`."
- **The activation seam**: `fieldspec.DefaultLayers()` hardcodes observe OFF at `validate.go:29` + `render.go:54/:259`; dormancy pinned by `s5_registry_dormancy_test.go`.
- **Commit-path slots mapped**: observe gates after form/lineage, pre-append, inside the serialized loop (`submit.go` pipeline); external chokepoint = `egress.Drain` at `AfterCommit` (`main.go:220`); done-predicates register in `obligation.CompleteAuto` (the per-turn derived-completion pass).
- **park/wake primitives BUILT**: `completePark` + `gate_resolution` wake-on-operator-verdict (operator-FROM only, single-resolution guard) + durable-inbox liveness + crash-safe re-issued wake. Local outbox items derive and write `outbox/*.json`.
- **Egress scanner present-but-dormant** with the decision-⑤ carve-out (model-name ∧ conductor-ODB ∧ →operator, confidentiality class only) **already coded**; a production-import guard test pins dormancy — activation is a deliberate, tested act.
- **Gate/raise path live**: `gate_category` (14 members incl. `routing_escalation`), `HUMAN_GATE_REQUIRED`, RAISE-ONLY + known-A detector (`validate.go:90-108`), `gate_resolution`/`resolves_gate`.

## Scope fence
**IN (m-3):** `observe_gate()` inside the atomic `submit()` (post-form/lineage **and post-`slot_in` classification**, pre-append — the m-7 §3 step order; TOCTOU-closed; trigger≠execute) with the **§3.1 closed write-allowlist** (writes outside the set = internal fault) · governed observe-**layer activation** (a visible config act, never a silent code default — design item 1; the switch mechanism is m-2+m-7's; m-3 owns the layer's behavior once active — C-1) · **E1 + E2** rungs + the conductor-implemented **check-registry** (agent selects a vetted parameterized check, never supplies code) · the **unprivileged suite-executor** (no store/config/outbox handle, no signing key — build-gates E2; m-7-hosted NF fixture) · phase-shaped **done-predicates** (read-only: artifact present + clean tree; IMPL: diff + checks green + `diff_paths ⊆ scope_paths` — **the scope leg conditional on the s9 co-sign, else struck per m-3-F7** (F-2 fold)) · **decision-② class-conditional fail-closed** keyed on computed `authority_class` (trigger: `record_integrity ∈ {self_reported, mixed}`) · `record_integrity`/`evidence_integrity`/`mixed` rollups + opaque-lane detection + `attestation_source` · `slot_in` mechanism (classify post-gate/pre-observe/atomic-bind per CQ-5; immutability; non-lane-writability incl. the **s5-b (h)** channel-keyed typed-REJECT suppliability guard) · `executable_claim_results` · the enumerated negative-fixture set.
**IN (m-6):** lift the Step-1 primitives into the locked mechanism — the **7-state FSM** (`active→parked_waiting_human→resummon_due→replied_pending_validation→resumed` + `bounced_repair` + `egress_blocked`) · resummon cadence (two timers; **escalate the channel, never the verdict**; no hard deadline) · **m-3 re-observe-on-wake** (refresh-before-resummon; J1 never-auto-approve) · **wake-on-reply** generalization + self-pacing · the **A/B/C/D bucket projection** (saved queries over tags; locked writers; RAISE-ONLY; terminal-token→bucket; egress/D precedence) · **ODB render + capture** (m-3's 7-field bundle promoted; bounded choices as `agent_enum_pick` buttons → operator-FROM verdict relay; "elaborate more" = read-only context-preserving fork).
**IN (cross):** **INV-CATALOG first** (see strategy) · the **dogfood relaunch** riding Step-2's first live store · the owed fixture registrations (③ known-A NF m-7-side; ⑤ ODB egress pair).
**OUT:** the away-bridge / Seam-C token / decision-④ rotate+re-observe (step-(d) carry) · E3/E4 · egress live-chokepoint activation + full hardening (first-external-send / Step-6) · m-5 concrete tag-space, invariant selection, ceilings (c3-reserved — Step-2 ships base predicates + the opaque `slot_in` mechanism only) · interjection/steer (Step-3) · TUI / email-client UX (Step-4) · benchmark loop (later release) · engine perf.

## Build-execution model — carried from Step-1 unchanged
Team-per-slice, m-x-guided (m-3 guides the observe slices; m-6 guides the comms slices; m-7 continuity throughout); risk-scaled plan gates — master+VP gate **s7's plan only** (phase-opener), s8+ run on pair-Implementer plan-review + conditioned delegated dispatch with the named escalation triggers. One slice at a time (at most two). Slice relays live in `frank/` per `sprint-doc-setup`; master dispatch acts in `master/relays/`. **Naming:** slices number continuously → Step-2 = **s7…**; cycle-level dispatches **`step2-prep` / `step2-plan`** (mirroring step1-*). *(Naming ruling proposed here; operator ratifies.)*

## Build strategy — INV-CATALOG first, then the vertical observe spine
**Order of record (operator-ratified 2026-07-10; s9∥s11 SERIALIZED to s9→s11 by the operator 2026-07-13): s7 → s8 → s10 → s9 → s11 → step-exit.** s10 depends only on s8 (+ the Q6×Q4 reconcile), so the comms spine runs before the evidence thicken — after two build slices all three ROADMAP exit legs exist; the thickens then run as the at-most-two parallel pair. The slice descriptions below keep numeric order for reference.
- **s7 — INV-CATALOG** (queue-first per the close record; owner CTO/m-7, m-1/m-2/m-4 fidelity). One `test/invariants` package: the seven standing laws — byte-exact `{accepted, rejected, held}` · the three-verb seat surface · R2 no-model-predicate · derived-only activation · I1-P sole-writer · I-PH · canonical-wins — each **named**, one executable check per law; the catalog ships as a **versioned artifact carrying the single-writer / owner-fidelity-on-change convention** — the registered "governed like `registry.json`" property completes at **s8** via the mandatory §7-digest-pinning carry (design item 1), and s7 does not claim it (VP s7-F3 staging). Effect: weakening a global law = a red battery **naming the law** — Cardinal rule 1 as a compile-time tripwire, landed **before** Step-2's observe machinery adds code. **Claim-grain watchpoint (VP):** the check text scopes "derived-only activation" to the **seat-lifecycle invariant**, and states I1-P as the **sole *governed* write path with the D5 direct-store residual** — short law names must not re-expand the claims narrowed in c5/c6. Small slice; **prereq: the operator baseline precondition (below) is complete — the baseline is NOT s7 scope (VP F4).**
- **s8 — the observe spine (vertical slice first, Step-1 discipline):** the thinnest end-to-end *observed* relay — governed layer activation → `observe_gate()` in the serialized commit path → one E1 read-check + one E2 registry check (executor isolation **+ hard per-check timeouts** designed in from day one, even if the first E2 check is minimal; the m-3 operator gate preserved for side-effecting/unbounded-cost checks; both m-7 timeout dispositions — authority → `held`, non-authority → `rejected` — exercised) → `achieved_evidence`/`target_gap_result`/`record_integrity` stamped conductor-side → **false-done bounces typed naming the failing predicate; true-done accepts with observed stamps** → decision-② legs (authority + `∈ {self_reported, mixed}` → `held`; non-authority → accepted + `self_reported`) → the **s5-b (h)** suppliability guard (m-2's DEF-2 channel-keyed typed-REJECT over `system`/`computed`-owned fields, m-1 fidelity on the channel key; one rule family with s9's envelope-hygiene extension) → adversarial fixtures around the spine. Rides the existing wake-on-verdict for its gate leg. **Exit-test legs 1+2 live here.** Dogfood relaunch starts on this store.
- **s9 — evidence thicken:** the check-registry filled (E1/E2 families incl. `run-suite` + the red→green differential under the isolated executor) · `executable_claim_results` rows · **`scope_paths`** (m-2 declaration + the IMPL predicate) · opaque-lane detection + labeling · `attestation_source` · `mixed` rollups · the full negative-fixture set · **both riding OI-S6s land here** (`BOUNCE-CLASS-UX` = the next m-2 registry touch; `ENVELOPE-KEY-HYGIENE` = the next m-1/m-2 seam touch) · ⑤ fixture pair registered.
- **OPERATOR SUNSET RULING (2026-07-11, binding on s10's exit):** m-3's interim check-policy defaults — the silent 120s/5s auto-kill disposition and the static-config `side_effecting` gate — are **testing-phase only ("ok for now for testing purposes but MUST BE REMOVED by s10", operator verbatim)**. At s10 they are replaced by the operator-in-the-loop shapes on this slice's own machinery: a long-running check **parks and ALERTS the operator with the kill/extend decision** (ODB + operator verdict; the hard ceiling stays as the fail-safe backstop behind the prompt — the J1-composition is an s10 design question, named now); the static gate is **replaced by the live prompt**. The empty-allowlist floor itself is NOT sunset — only how approvals arrive. **s10 does not close while the silent auto-kill or the static-only gate survives.** Record: `step2-plan/SITREP-…-025643`.
- **s10 — the comms spine** *(re-cut per VP F3 — the prior single comms slice bundled a scheduler hard-trigger with a cross-domain schema surface)*: the **minimum A-gate path end to end** — minimal ODB render + capture (bounded choices, `agent_enum_pick` buttons) → park → **validated operator reply** → **local re-observe on wake** (the observe-gate re-fired on resume — no away-token machinery, m-6 annotation A; decision-④'s token re-observe stays step-(d)) → **exactly-once wake** → **deterministic resummon commands** (timer-fired commands carry content-hash keys — seat + decision + cadence-slot — so a crash-refire dedupes through the A-2 replay machinery, m-7 note ③). Prereq: the **Q6×Q4 `record_kind` widening reconcile resolved before this PLAN** (the first ODB consumer). **Exit-test leg 3 (a parked lane wakes on reply) lives here.**
*(Sequencing note, 2026-07-13: the ratified `s9 ∥ s11` was serialized to **s9 → s11** by the operator. Not codependent — s11 consumes nothing s9 produces; the reverse soft edge [bucket A/D routing reads the `held`/`mixed` rollups] favors s9-first. Serializing DISSOLVES the m-2 registry collision edge [s9's BOUNCE-CLASS-UX lands, then s11's `stale_schema` builds on it] and halves m-2's peak load — the seat just compacted for that load. With the operator as single hand-relay transport, parallel bought context-switching, not real concurrency. Mechanics: the prematurely-cut **`s11-dispatch` and `s11-8a-joint-review` were STRUCK** (operator, same day) — the dispatch's `∥`/collision-edge framing went stale on serialization, and pre-staging a not-yet-live slice's relays is redundant when the slice is fully specified HERE and the routing history is in the INDEX; both are RE-CUT FRESH when s9 closes, against post-s9 reality. **`s11-oq2-ceiling` [m-5] STANDS carried NOW** — sequencing-independent, m-5 untouched by s9, its slow ceiling ruling ripens free during the s9 build.)*
- **s11 — the comms thicken:** the full 7-state FSM surface + the B/C/D bucket projections (A lands with the spine) · the **elaborate-more fork** (a parked-lane fork — Step-2; live-lane interjection stays Step-3, m-6 annotation B; **OQ-2 resolved before this slice's fork build**) · **CTO ruling (2026-07-10, closing m-6 annotation C):** a design/grill gate lands as an A-bucket gate **eligible for the elaborate-more fork — never silently force-compressed to a bare ODB brief**; the §5(b) no-compress guarantee holds via the fork in Step-2, and the auto-routed meeting surface is Step-3/4 · **8a hardening** (the design-item-5 contract, m-2 confirm + m-6.implementer review first) · the remaining bucket/fixture matrix · ③ known-A NF fixture registered.
- **step-exit (after s11):** the roadmap test run live on the dogfood store (all three legs) + the INV-CATALOG red-battery demonstration + battery green, uncached.

*(This decomposition is the proposal for `step2-plan`; the formal VP-gated decomposition may re-cut boundaries. Vertical-slice rationale: one observed relay end-to-end before any taxonomy thickens.)*

## Exit gate — HARDENED (baseline + adversarial, Step-1 discipline)
**Baseline (ROADMAP:100-101):** one **failed** observe-as-send case (a false "done" rejected pre-send) · one **passing** send with conductor-captured evidence · a parked lane **wakes on reply**.
**Promoted into the gate:**
- **Fabricated-done family:** lane-supplied observed-field forgery REJECTED typed (the s5-b (h) guard, channel-keyed) · `EVIDENCE_TARGET: E2` with a failing suite → `rejected` naming the failing predicate · clean-tree predicate vs dirty-observed veto · `slot_in` immutability / re-tag-to-escape leg.
- **Decision-② family (two-axis — never conflate no-vantage with machinery-fault):** authority-class + `record_integrity ∈ {self_reported, mixed}` → `held` + escalated, **with the `mixed` leg exercised explicitly** (m-3 §13:220; F-1) · non-authority + unobservable → accepted + labeled `self_reported` + `degradation_notes` · observed-fail → `rejected` in both classes · **observe-machinery internal fault/timeout**: authority → `held`, non-authority → `rejected`/author-return (the NF-S6 two-axis disposition, m-7 `:156`).
- **Executor-isolation NF (m-7-hosted):** a suite-class check probes from inside the executor and **proves absence** of store/config/outbox handles and signing material.
- **Egress fixture pair (decision ⑤):** model-name inside a conductor-generated operator-facing ODB passes the confidentiality class; the same content on any other path blocks; fail-closed default (`other`→block). Fixture-scoped; the chokepoint stays dormant.
- **park/wake crash legs:** crash mid-park / mid-wake → exactly-once wake, no stranded lane (extends the Step-1 liveness contract to the resummon timers) · a parked A-gate across a schema bump (8a) migrates-then-validates at wake or is explicitly dispositioned — never silently dropped.
- **INV-CATALOG tripwire demo:** deliberately weaken one law on a scratch branch → the battery goes red **naming the law**.
- **I-PH** swept over every new surface (observe bounces, ODB render, FSM/projection outputs — no store/config/socket path anywhere seat-visible).

## Design items owed at/before PLAN (`step2-prep` — bounded amendments via the ritual, NOT re-design)
*Pair intake verdicts folded 2026-07-10 — every item now carries its owner-proposed shape; the shapes bind at their named slice's PLAN, not here.*
1. **Layer-activation semantics** (m-2+m-7) — **SHAPE AGREED**: the flag is a member under m-7's pinned **engine-config composite digest** (CQ-4b); activation = an **operator-authored §7 `config_change` record, restart-effective** (no hot reload — locked §7); A-1's stable-schema digest bounces every pre-activation form with `re-render` (correctly); pre-activation records are never re-validated (immutability). m-2 owns the `layer_present:observe` atom semantics + the "governed act, never a code default" rule; m-7 owns the digest home + load-time flip. The s5 dormancy sweep mirrors as the activation tests. **Lands s8.** **Same config family, added at the s7 plan gate (VP s7-F3): the INV-CATALOG catalog artifact becomes a §7-digest-pinned member — member/home confirmed by m-7+m-2 before the s8 PLAN; the registered "governed like `registry.json`" property completes only when this carry lands (s7 ships the convention + red battery, not the claim).** **Added at the s7 F1/F2 fold review (m-7.planner note, 2026-07-10): when the catalog pins, the row-6 discovery's OWN boundary — the AST walk roots and the two-file seat-egress boundary set — is governed beside the other censuses (a new egress FILE, e.g. a second server binary, needs a census entry to be discovered; the discovery's boundary must be a visible, amendable fact, not an invisible literal).** **Added at the s8 grammar placement confirm (`s8-grammar-confirm-m7/…-234643`): `s8-design-m7-config` also carries the formal §3 STEP-4.5 AMENDMENT — the profile-aware completeness stage (post-observe, pre-§4-commit, the last in-courier gate) with m-7's four hosting constraints as its content (pure/deterministic over record+pinned-config · failure = one terminal typed reject through the single-pivot path · read-only over the step-3 classification and step-4 observe outputs · profile determination is m-2 grammar, the engine owns only order/purity/single-outcome). m-2 designs against the confirmed placement now; the locked §3 changes only via this amendment, never silently.**
2. **Executor isolation** (m-3+m-7) — **SEAM SPLIT AGREED**: m-3 owns the requirement + the suite-class check-registry contract + the adversarial probe intent (a check that *tries* to reach store/config/outbox/signing and must fail); m-7 owns the executor host + the NF absence-set fixture (§8.4 probe-from-inside idiom). Boundary shape: the executor receives only candidate bytes + declared inputs, returns a typed verdict, never writes. **Claim ceiling travels: tool/handle-surface isolation, D5 residual stated** — OS-grade sandboxing would be a grilled design *addition*, never silently implied. Build-gates E2; **lands s8.**
3. **`scope_paths` co-sign** (m-2+m-3, m-1 fidelity) — **s9 PLAN BLOCKER (VP F1 — reopened; intent agreed, contract NOT yet enforceable).** m-2 owns the slot; m-3 owns the `⊆` check; but before the s9 PLAN may consume it, the pair must co-sign the producer/reader contract with four pins: **(a)** the canonical value = the **accepted PLAN/dispatch ancestor record** (never the work-record); **(b)** how the observer resolves that value **through lineage** at check time; **(c)** a **typed refusal** for an implementing-lane override/self-widen attempt; **(d)** the missing/ambiguous-source disposition once the field is active. m-1 fidelity on the channel/lineage key; a **self-widen negative fixture** required in the exit set. Until co-signed, `diff_paths ⊆ scope_paths` stays **struck** (m-3-F7) — the F-2 conditional holds.
4. **Q6×Q4 `record_kind` ODB-widening MAJOR vs MINOR** — stays the **master reconcile**, resolved **BEFORE the first ODB-consuming slice PLAN (= before the s10 PLAN, VP F3)**, not "at" it. m-2's position on record: system-enum exhaustive-switch widening = MAJOR.
5. **8a parked-gate-across-schema-bump** (m-6+m-2) — **PARTIALLY CO-SIGNED; the full contract stays OPEN (VP F2/r3-F1 — evidence boundary exact).** **The co-signed floor (both pairs):** wake = **migrate-then-validate** (§9 read-time migrator registry, then current-form validation); un-migratable → **`held`/escalated — never silently dropped, never auto-resolved**. **The m-6-proposed set, NOT yet co-signed:** the **`stale_schema`** reason token · the bounded-choice set **frozen at park time** · the choice-set-changed **bounce-as-stale + RE-ISSUE** branch — m-6 itself flagged the branch consequential and reserved it for m-6.implementer adversarial review. Before the consuming slice PLAN locks 8a: **m-2 confirms the frozen-choice/migration interaction (+ the reason token), and the m-6.implementer design review runs** — one joint 8a review may co-sign all three together. m-2 owns migrator/version-negotiation; m-6 owns the FSM wake-branch + re-issue + `held` surface; the exit-gate 8a leg is the acceptance test.
6. **OQ-2 elaborate-more fork posture** (m-6→m-5 COORD; non-blocking) — m-6 recommendation on record: the fork **inherits the parent's context under a fresh read-only collaboration posture** (`interactive` + advisory/review_checkpoint + read-only ceiling; it must not mutate the parked lane). m-5 owns the final ceiling call. Resolve before the s11 fork build.
7. **`GRILL_REQUIRED` FieldSpec row** (m-6-F6) — **RESOLVED 2026-07-10: STRIKE for Step-2**, a recorded scope shrink (m-6 ruling): the meeting-lane route binds on the locked `phase` atom alone (s5-fidelity Q1); nothing in Step-2 consumes the row; it returns as an additive MINOR row when the Step-3/4 meeting surface (or non-DESIGN-phase grill routing) actually consumes it. **m-6-F6 closed.**
8. **red→green semantic gameability** — **DISPOSITIONED (m-3)**: (i) *mandatory, s9* — truthful labeling: the bugfix observe stamps red→green as a **differential-class signal, never semantic proof** (the honest floor; no kickoff/plan may claim otherwise); (ii) *optional* — the semantic claim stays agent-asserted + cross-lane review-gated under the already-locked §6:125 boundary. Doc/labeling only; no mechanism change.
9. **The adjudication rung (NEW — the confusion-firewall directive, operator-ratified 2026-07-11; owner m-3; lands s9):** the fuzzy-claim majority gains an independently-adjudicated evidence class — a cheap INDEPENDENT model reads the claim against courier-captured real artifacts and judges consistency; **medium-strength BY DESIGN, labeled as exactly that, never proof** (fabrication/confident error must survive an independent skeptic; cost moves free→real). Fits the locked registry shape as-is (a vetted parameterized check → CheckVerdict under the executor's isolation/timeout/verdict machinery); does NOT touch the R2 law (R2 bars the PRODUCING model's identity as a gate input — the judge's VERDICT is an evidence artifact like a test result; the distinction stated so nobody conflates). **NAMED CONSTRAINT: a cloud-judge call is an EXTERNAL SEND — the rung designs under the ratified egress fence** (a local judge, or design-now-activate-at-the-away-bridge). s8 stays deterministic-only (E1+E2 as dispatched); the thin-slice proof rides a FRESH store per the directive's steer.
10. **The turn-baseline fence (NEW — registered by the Row-3 ruling `s8-row3-precision/PLAN-orchestrator-planner-20260712-165010.md`; owners m-3+m-7; lands s9):** the conductor turn-entry snapshot of the governed lane + allowed-path policy that upgrades the claimless-report base floor from the honest `Degraded`/`self_reported`/E0 degrade (the s8 Option-2 posture) to a complete, attributable E1 predicate. Spec basis = m-3 §13 (the closed Authority-partitioned phase→predicate table + the pinned porcelain grammar) + the Option-1 sketch in `s8-row3-precision/SITREP-planner-20260712-165000.md`. **Constraint carried verbatim from the approving review: this is NEW commit-loop machinery — it gets its own m-7-inclusive design + review before any build; the §13 approval pre-approves nothing.** On landing, the claimless floor upgrades E0→E1 through the standing amendment path.

## Owed carries (inherited, typed — the materialize-first rule applies)
**F-S7-R2-COLGRAIN (registered 2026-07-10; found by the s7 fidelity pass — m-2 + m-4 independently convergent):** the live predicate grammar compiles `any_row:routing_assignments.chosen_model` while validating only the parent array (`internal/fieldspec/predicate.go:136-152`; `routing_assignments` is gate-referenceable) — a model-identity **column** predicate reaches a compiled predicate, so the **C1 column-grain carry's trigger is already met** and the guard is due now, not Step-3. Route: a bounded production lane (m-2-owned mechanism + red-first fixtures; m-4 co-sign; m-7 fidelity; staffing = operator ruling, B10-shape m-2 pair recommended) → then the s7 row-3 any_row negatives fold (test-only) → m-2 + m-4 re-confirm. s7 integration holds until resolved.
**s10-close carries (2026-07-13, consolidated from `s10-build-exit/SITREP-planner-20260713-125905`):** the s11 design cell — non-Accepted-resolution/blocked-prompter re-prompt semantics + the claimless authority-floor Held edge at resolution (m-3+m-6, J1-adjacent) · m-7 A-2 ledger — pinned run-verdict cache lacks invalidation (transient-fault replay) + two accepted residuals (re-entrant callHandler depth; clean-shutdown dropped handler result) · m-6 G4 (s11) — resummon cadence defaults (1h/class, explicit interim) belong on the operator-config surface · the s11 cleanup card (9 items: generic prompter · shared soft-expiry arbiter · one ODB builder · tables-snapshot prompter lookups · drop per-emit tables.Build · shared system→operator envelope builder ×5 · ContentHash/GateID prefix decoupling · finalizeRun preserve-flag ownership · genesis reverse-ladder growth) · standing shelves unchanged (side-effecting EXECUTION beside the OS sandbox · the 8a branch). `OI-S6-BOUNCE-CLASS-UX` (→ s9 m-2 touch) · `OI-S6-ENVELOPE-KEY-HYGIENE` (→ s9 m-1/m-2 seam touch) · fixtures ③/⑤ un-registered (→ s11 / s9) · relaunch ops notes (pre-allowlist `mcp__frank__*`; hosted-seat `tools/list_changed` staleness caveat — re-read schema after any re-render bounce) · step-(d) away-bridge + C1/C2 Step-3 routing carries stay shelved (recorded, untouched by Step-2).

## Pre-flight ops (operator)
1. **Baseline commit in `frank/` — an operator PRECONDITION completed before the s7 branch/slice, not s7 scope (VP F4).** The precise state: `frank/main@a1bc6d45ac5c` (the s6 CLOSE fold commit; peeled tag `s6-close` = `6a1198af6e20`, one behind) carrying the publication sweep as **38 tracked changes + 1067 untracked files (1105 expanded entries** — the untracked bulk is the co-published `master-docs/` + `.relays/` trees; the collapsed "42 paths" undercounted). Gate: inventory the expanded set → land it as **one baseline commit** → run the **full uncached repository battery at that commit** → record its SHA as the **s7 `BASE`** → `git status` clean before any s7 dispatch. (The publish clone is a separate tree and unaffected — and per the release-separation ruling below, this commit stays in the private dev repo.) **Inventory DONE (2026-07-10, read-only):** 38 tracked changes (the README rewrite · the `.gitignore` deletion un-hiding `.relays/` · 29 sprint-doc de-versioning edits · `registry.json` + 5 test/replay publication edits) + 1067 untracked (540 `.relays/` + 525 `master-docs/` + `LICENSE` + `NOTICE`) = **1105 expanded entries, all publication-sweep content, nothing unexpected**. **✔ GATE SATISFIED (2026-07-10, operator-directed):** landed as one commit — **s7 `BASE` = `1d3e92c`** — full uncached battery at it: **24 pkgs ok / 0 FAIL, `go vet` clean, status clean**. **Remote system (operator ruling, executed):** `frank/` origin = the new **private `iwnlcern/frank-dev`** (main + all close tags pushed); a pre-push guard in `frank/` mechanically refuses any push toward the public `iwnlcern/frank` URL; the release tree (renamed `~/frank-release`) alone carries the public-facing remote.
2. **Dogfood relaunch:** rides Step-2's first live store (s8's spine store; a fresh blessed store per the §7-apply pattern). The relaunch gate — "the F11 pattern must not livelock" — is now a passing test in the battery. **s8 GENESIS CONDITION (m-7 fidelity, 2026-07-10, s7a):** the dogfood store's genesis initializes from the **`s7a-fieldspec-v5`** registry bytes and records the exact composite `config.Load(...).Digest`; any already-initialized store adopts those bytes only through an accepted operator `config_change` carrying the recomputed composite digest before restart; direct replacement after genesis stays Phase-0 fail-closed.
2a. **FLAKE-SOCKET-PAR (registered 2026-07-10; non-blocking; found at s7a fidelity):** `TestS6IPHSeatMintReplyCarveOutsScoped` and `TestConfigChangeProjectionsCarryNoMemberBytes` fail intermittently under **parallel** full-suite runs (each passes in isolation; the serialized `-p=1` uncached suite passes) — pre-existing socket-startup concurrency instability, not in the s7a diff. Disposition owed at s8 (fix the fixtures or pin the battery's parallelism); stays visible to every integration review until dispositioned. *Scope note (VP s7a re-review): this entry covers the two socket-startup fixtures ONLY — the reconnect double-close panic first mis-grouped here is the SEPARATE runtime defect below (2b); the two entries stay distinct unless later evidence unifies the roots.* **Third member (2026-07-10, s7b diagnosis `s7b-dispatch/SITREP-planner-…-233611`): `TestLawPathHygiene`'s live capture in `test/invariants`** — the same fixed-deadline + per-run-build class (20s ctx · 5s socket wait · per-run `go build`); dispositioned by the s7b follow-up grant (`…-234637`: package-cached build, ctx ≥30s, socket ≥15s).
2d. **CRASHPOINT-KILL-RETURN (registered + dispositioned-in-flight 2026-07-10; mechanism record = `s7b-dispatch/SITREP-planner-…-233611`):** `crashpoint.Hit` fires `syscall.Kill(getpid, SIGKILL)` and RETURNS (`internal/crashpoint/crashpoint.go:64-66`) — asynchronous signal delivery lets the crashing thread execute further syscalls under load, so a `pre_rename` child can commit the rename BEFORE dying (the observed crashed-child-plus-committed-file signature; loaded-parallel-only; the `before_rename` leg specifically). Fix in the s7b follow-up: block forever after the kill (`select {}`) — the process state at delivery becomes byte-exactly the state at the Hit, at any load. Production-tree test-hook code; explicitly in-fence for the granted delta only.
2b. **OI-S7A-CLOSE-ONCE-RACE (registered 2026-07-10; found by the VP's s7a final-byte re-review `…-154020`; owner m-7; PRE-EXISTING, not an s7a regression — proven on unchanged `main@1d3e92c`):** `Client.Close` and `Client.readLoop` each perform an unsynchronized select/default check then `close(c.done)` (`internal/channel/server.go:519-525, 555-562`) — two concurrent closers can both observe the channel open and both close it: a **production double-close panic on the client lifecycle**, reachable via MCP reconnect (`cmd/frank-mcp/TestShimReconnectsAndRetriesSingleCallAfterConnectionLoss` panicked one fresh parallel full-suite run; `go test -race -count=20 ./cmd/frank-mcp -run '^TestShimReconnectsAndRetriesSingleCallAfterConnectionLoss$'` FAILS on both `2bc0763` AND `1d3e92c`, once through `Close:523`, once through `readLoop:560`). **Disposition gate: BEFORE the s8 dogfood opens live MCP channels.** Acceptance shape (VP-pinned): one idempotent close owner/primitive; the reconnect test green under focused `-race` repetition; full battery rerun. Honest evidence rule until then: full-suite results are reported as their observed sequence (e.g., "parallel attempt 1 panic · parallel attempt 2 pass · serialized pass"), never flattened to an unqualified pass. Visible to every integration review until dispositioned.
2c′. **S7A-TRAIL-FINDINGS (registered 2026-07-10; the m-2.planner close-out read, master-concurred — product evidence, TRANSPORT-FINDINGS' successor in miniature):** the s7a arc exercised the full build lifecycle (PLAN authoring · mechanism choice · a three-round pair loop · delegated dispatch · adversarial IMPL review · fold review) with **design and code judgment sound throughout** — and **every failure was at the mechanical trail-integrity layer**: the orphan parent, the false byte-identical claim, the DISPATCH_ID collision (all pair-side, all caught *before* action), the master-side authorization-shape gap (caught *after* an irreversible push), atop the `one_by_id` earliest-wins footgun class. The consistent finding: **the file-relay protocol is fragile precisely at trail integrity, holding by humans + a post-hoc linter what should hold by construction — the exact thesis of the product.** Conductor-assigned ids + computed, gate-enforced lineage make each of these structurally impossible. Feeds: the CYCLE-PLAYBOOK friction log at cycle close, and the **s8 dogfood** as its inherited lesson (the dogfood's evaluation should name which of these classes the conductor demonstrably kills).
2c. **S7A-MERGE-TRAIL-DEFECT (2026-07-10; DISPOSITIONED same day; defect owned by master):** the s7a merge-grant relay (`s7a-merge-gate/MERGE-GATE-…-155633`) conveyed the operator's real in-session grant in prose only — no recognized authorization field — so the executor's post-action claim-aware lint correctly failed the dispatch-root lineage ("merge claim without an earlier recognized authorization"). The merge itself (`54420dbc`) was fully authorized in fact, VP-final-approved, and master-verified; it stands. Disposition (`MERGE-GATE-…-160704`, operator countersign requested): the corrective record carries `HUMAN_MERGE_AUTHORIZATION` explicitly post-hoc; **the dispatch-root ordering failure is accepted as a permanent honest artifact** (backdating refused); **CONVENTION PINNED: every merge-grant relay carries the recognized positive field at grant time** (`HUMAN_MERGE_AUTHORIZATION: granted — <context>`) before any executor acts — part of the grant's minimum shape, exactly like the bare token for `DISPATCH IMPL`. The executor's conduct (disclose, no proxy repair, block the follow-on) is the model case. **Operator countersigned ("ratified", 2026-07-10) — INCIDENT CLOSED.**
3. **Ratifications (operator, 2026-07-10) — PARTIAL:** the egress-stays-fixture-scoped ruling ✔ **RATIFIED** · this kickoff (r4) as plan-of-record ✔ **RATIFIED** · the naming ruling (s7… / step2-prep / step2-plan) ✔ **RATIFIED** · the **s7–s11 slice proposal ✔ RATIFIED (2026-07-10, second round)** with the final order + staffing: **s7 lean** (no slice team — CTO+m-7 execute directly, m-1/m-2/m-4 fidelity by relay, VP plan-gate kept) → **s8 fresh team** → **s10 fresh team** (the comms spine pulled before s9 — it depends only on s8 + the Q6×Q4 reconcile, and after s8+s10 all three step-exit legs are live and demo-able) → **s9 ∥ s11 continuation teams** (the charter's at-most-two allowance; the m-2 registry is the named collision edge, the m-2 guide arbitrates) → step-exit. Cut-lines and review gates exactly as VP-approved; only order, staffing, and s7 ceremony moved. Slice numbers stay semantic identities (s9 = evidence thicken), not calendar order.
4. **NEW OPERATOR RULING (2026-07-10) — release separation:** releases live on a **separate release tree/branch**; **`frank/` is developer-only and stays private** (never gets a public remote). The publish tree (currently the separate release directory backing the private `iwnlcern/frank` remote and its v1.0.0 release) is the only public-facing surface; the pre-s7 baseline commit lands in the private dev repo only, and publication parity is maintained by the release flow, not by `frank/main`.
5. **NEW OPERATOR RULINGS (2026-07-11) — the two s8 design-fork elections (both grill-surfaced; both land on the designed defaults):** **(i) F5 config-load claims = fork (a), lean shape-only** (operator: "stamp a") — `config.Load` claims exactly *bytes-match-the-authorized-§7-history + member/provenance SHAPE*, stating "shape, not review" in those words (FX-CFG-4 pins the claim text); owner fidelity on catalog changes remains witnessed by the relay/design-review trail, never by the loader; attestation-at-load stays un-built — it would re-open the D3-shelved crypto-identity territory (ephemeral-session seats have no durable identity to attest with) and is electable later only by its own grill. **(ii) Executor containment = the v1 handle-surface ceiling** (operator: "we'll keep an OS sandbox in the back somewhere for a future feature, the first option is good enough for now") — the F4-scoped claim stands (no provided handles + no writes through the provided surface; the same-uid ambient residual STATED, never claimed away; matches the standing D5 posture); **the OS sandbox is SHELVED as a recorded future feature** — a separately-grilled enforcement upgrade slotting under the unchanged spawn/CheckVerdict contract; natural trigger (guidance, not a binding tripwire): the first E2 check pointed at a repo we don't author. Effect: `s8-design-m7-executor` r4's lock condition is **satisfied → lock effective**; `s8-design-m7-config`'s lock now awaits only the r6 folds (A1 version carrier · A2 reader-capability gate) + m-7.implementer re-review. Records: `s8-design-m7-config/SITREP-orchestrator-planner-20260711-130825.md` · `s8-design-m7-executor/SITREP-orchestrator-planner-20260711-130830.md`.
6. **NEW OPERATOR RULING (2026-07-11) — the CONFUSION-FIREWALL DIRECTIVE RATIFIED (the cross-domain scope amendment; text of record = `ARCHITECTURE.md` § CROSS-DOMAIN SCOPE AMENDMENT):** threat = confusion/hallucination NOT malice (ceremony re-importing the adversary cut on sight) · evidence = deterministic checks + independent cheap-model adjudication (medium-strength, labeled) + honest labeling · architecture = extensible seams with NO home-grown security primitives, governed by **Rail A** (additive/open where ignoring loses only detail; closed/fail-closed where ignore-unknown changes the MEANING of acceptance) + **Rail B** (cut by FUNCTION not flavor — drift/crash-safety/history-truth/provenance machinery stays, claims confusion-graded) · REDUCES scope (no speculative frameworks) · positioning leads with the confident-hallucination catch. **All four s8 locks verified SURVIVING by function analysis** (`frank-threat-model-scope/SITREP-orchestrator-planner-20260711-162826.md`); the s8 dispatch stands + addendum (SEQ-1 fresh-store steer + the Rails as build-time review criterion); the adjudication rung = design item 9 (s9). Trail: scribe `…-162331` (m-2, provenance respected) · assessment `…-162826` · operator "ratified" 2026-07-11.

*Standing rule: every new protocol deviation appends to `master/PROTOCOL-DEVIATIONS.md` as it is made.*
