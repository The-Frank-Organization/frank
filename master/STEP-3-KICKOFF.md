# STEP-3 KICKOFF — Model-agnostic runtime + routing execution (the SPINE first)

> **⚠ SUPERSEDED IN PART (2026-07-15) — read the ratified reframe first.** The architecture-of-record for Step-3 is now **`master/STEP-3-ARCH-AMENDMENT.md`** (operator-ratified at SHA-256 `2d240eb6…`, VP-approved `step3-arch-packet/063000`). It **supersedes this kickoff's conductor-hosted framing** across **BOTH §§1–3 AND §§5–8** (not merely §§1–3):
> - **§§1–3 SUPERSEDED** — the conductor-hosted provider-egress, the m-7 credential contract, the m-4/m-2 in-Step-3 routing-record, and the "m-9 runs ON m-7" placement. Under the reframe: **the conductor is one isolated relay service, NOT the app hub**; provider connectivity is **app-side** (m-8 connector + m-10 control plane + m-9 worker; new domain m-10); credentials are m-8-owned/m-1-governed app-side; the provider-egress **mechanism** is app-side (policy stays m-3).
> - **§5 SUPERSEDED in part** — **V2 second-provider portability and V3 routing execution DEFER to Step-4**; Step-3 = **V1 only** (the one-governed-turn MVP, single pinned **app-side run manifest**, no routing execution).
> - **§6 SUPERSEDED** — the pre-lock amendment set is re-cut: **m-7 credential → re-owned to m-8**; **m-4/m-2 routing-record → deferred to Step-4**; the new coordinated first stage is **m-10 boundary + the m-5 ceiling-host amendment**, then m-8/m-9, then m-3 egress + the m-8-owned connector-credential contract.
> - **§7/§8 — the "spine is non-terminal" framing is re-cut** (Step-3 scope is the MVP; benchmark/spawn/steer are Step-4), but the **T4-token gate mechanics (§8) SURVIVE** unchanged (and are strengthened: no code token until an executable T4 round-trip has actually run through the live store — `master/FRANK-HARDENING-BACKLOG.md` RULE-1).
> - **SURVIVING content:** the vertical-first **V1 goal**, the **E3 live-provider evidence floor**, and the **T4-token gate** — all re-homed app-side.
>
> This kickoff's **locked hash `983508fc…` is preserved as the historical lock** (this banner is the sanctioned supersession pointer; the superseded bytes below are retained as the record, not operative).

**Status: LOCKED 2026-07-14 — OPERATOR-RATIFIED (org expansion + vertical-first scope + kickoff, in-session) + VP-CO-SIGNED after THREE revise rounds (approve `step3-prep/RECONCILE-orchestrator-reviewer-20260714-222000`, at SHA-256 `cf480081…239e76`; this post-co-sign status edit is the VP-authorized bounded update, operative §0–§8 bytes + pre-flight obligations then unchanged; **§6 subsequently AMENDED for parallel authoring — a review-driven VP-concurred amendment, `step3-audit-reconcile`, B14 — the resulting exact-byte hash is recorded in the transition-check fold relay `step3-audit-reconcile/RECONCILE-orchestrator-planner-20260714-235500`**). Charter amended (org chart + domains); m-8/m-9 charters authored; design-only AUDIT boots issued **per seat** (`master-boot-m-8-planner/…-223500`, `master-boot-m-8-implementer/…-223510`, `master-boot-m-9-planner/…-223520`, `master-boot-m-9-implementer/…-223530`). Both AUDITs discharged §6 step 1 (pair-complete, VP-accepted `step3-audit-reconcile/…-234500`); **§6 amended for parallel authoring** (`step3-audit-reconcile`); PROCEED-TO-DESIGN pending the VP's narrow transition check. **Step-3 design phase is OPENING.** This is the Step-3 plan-of-record, the role `STEP-2-KICKOFF.md` played for Step-2. Brainstormed live with the operator; adversarially reviewed by the VP across three rounds — **r1** (`step3-prep/RECONCILE-orchestrator-reviewer-20260714-212000`, six directional folds) + **r2** (`…-214500`, four bounded contract-consistency corrections against the *locked bytes*: provider-egress + credential amendments don't-yet-exist [§1]; the `chosen_model` amendment is already-mandatory-not-conditional [§3]; manual-m-x-relay scoped to pre-build + the T4 relaunch roster must add the m-x PM [§0/§8]; the spine exit needs an E3 live-evidence floor + two real provider paths [§5]). **r3** (`…-220000`, two stale-clause consistency fixes: §6 step 4 → an unconditional three-leg OWNER-AMENDMENTS lifecycle step that must close before lock; V1 "pre-request egress" → the exact §1 final-authorization contract; §1's "after translation" phrasing corrected to "pre-translation check"). All three rounds folded; no scope change, no re-ratification required (VP r2/r3). Returned for exact-byte co-sign.

## 0. Scope + the operating model this runs under
- **Scope of THIS push = the Step-3 SPINE**, not the full step ([[operator-fulltime-speedrun]]). The spine proves one thing: *frank can drive and govern a real model turn, through a frank-owned provider contract, and route into it.* **The spine exit is explicitly NON-TERMINAL for Step-3** — benchmark, native governed spawn, steer-at-boundary, and soft-interrupt/cancel-redeliver remain Step-3 (§7 carry ledger).
- **Operating model = the T4 tier on frank** (`CYCLE-PLAYBOOK` Part F): an independent slice orchestrator team owns local detail-design+plan+impl against an **m-x-authored spec-of-record**, escalating up through master to the m-x planner. The team runs **ON frank as courier** (carriage + observe live at `~/frank-live`). **Manual m-x relaying is scoped to the PRE-BUILD AUDIT/DESIGN phase ONLY (VP re-review #3)** — the *activated* T4 build phase requires the full escalation path `slice→master→owning m-x PM→back` to run on frank per Part F, so the relaunch roster (§8) must put the owning m-x PM on frank before the first T4 code token, or the loop would stop at the exact boundary the tier exists to exercise.
- **The greenfield wrinkle, resolved (VP #6):** m-8/m-9 are greenfield — no Step-0 design, no locked contracts — so **they cannot issue a build spec while their own domain contracts are open.** They first act as ordinary **design owners** (audit → design → grill → review → lock), and only *after* lock do they act as PMs to a T4 build team. "This is not a flaw in Part F. The PM role begins when an owner has a spec to manage." So **Step-3 opens with a design phase before any T4 build**, like c1–c3 did for the conductor.

## 1. Decision (B), as folded — port INVARIANTS + FIXTURES, build a frank-owned contract
Operator-ruled (B): use pi/opencode as prior art, build frank's implementation fresh with governance seams from line one. **VP fold #1: pi/opencode are reference corpora + conformance-fixture sources, NOT the normative interface.** The local pi shape embeds assumptions frank cannot inherit silently — caller-supplied `apiKey`/headers/env, arbitrary `onPayload` mutation, pi's own retry/timeout/stream/error protocol (`references/pi/packages/ai/src/types.ts:113-188,301-313,703-729`); opencode likewise merges factual model metadata with runtime options (`references/opencode/.../provider.ts:1029-1054`).
- **frank owns the normative provider contract.** The m-8/m-9 design packet specifies, as frank's own contract: request · normalized event · tool-call · reasoning-replay · usage · finish/error · cancellation · retry/idempotency · timeout/backpressure · partial-stream semantics — with a named **governance/conformance matrix** (pi/opencode become fixtures that must pass frank's contract, not define it).
- **Trusted-side boundaries are non-negotiable** — but **the surfaces to enforce them DO NOT EXIST YET, and two owner-authored amendments are hard pre-build gates (VP re-review #1).** The locked m-3 egress gate is the dormant away-email **local-outbox** scanner whose classifier flags ordinary `claude-`/`gpt-`/`model_name=` bytes (`frank/internal/egress/rules.go:22-43`) — it would reject every model call and **cannot** be placed in front of a provider request as-is; and m-7's locked config defines **no** provider credential/endpoint/secret/rotation/redaction contract (`m-7 design:107-125,130-132`). Therefore, **before any m-8/m-9 design-lock or build:**
  - **(a) an m-3 owner-authored review-driven amendment** for a **provider-request egress class** (distinct from away-email; do NOT inherit `egress_blocked` behavior or the model-name confidentiality rule), hosted by m-7, consumed by m-8/m-9 — defining whether authorization is a **final-wire gate at the governed send boundary** or a deliberately-specified pre/post pair; **no adapter mutation may occur after the final authorization point** (translation, compatibility handling, endpoint binding, and authentication all happen *after* a **pre-translation check**, so a pre-translation check alone is NOT a sufficient final-wire guarantee).
  - **(b) an m-7 owner-authored trusted-config/credential amendment** (m-1 trust/secret-boundary review + m-8 consumer review): secret sourcing · endpoint allowlisting/binding · rotation · redaction · and the rule that **credential values never enter catalog, snapshots, records, seat surfaces, or evidence artifacts.**
  - **(c) fail-closed negatives (named exit legs):** planted secret/PII or a disallowed endpoint ⇒ **zero provider send**; translation cannot mutate after final authorization; adapter, endpoint, credential, and pinned lane must agree. No pi-shaped escape callback may bypass either boundary.

## 2. The org expansion — stand up Division II (the founding chart's runtime domains)
Per `master-org-decomp` (2026-06-28) Division II. **Design-only boot** (VP #3): AUDIT may run concurrently across m-8/m-9; **interface locks and all implementation must NOT.** Add domain charters + amend the standing org chart before staffing.

| New pair | Domain | Owns | Does NOT own |
|---|---|---|---|
| **m-8** | **Provider Adapters** | provider wire translation · normalized provider events · **the factual lane catalog** (schema + rows) · provider conformance fixtures | credentials · egress policy · routing judgment · authority enforcement |
| **m-9** | **Model Runtime** *(VP-renamed from "Runtime Core" — the boundary is in the name)* | the model-turn / session / context state machine · governed *requests* to tool execution | m-7's process/concurrency/serialized-commit-loop/recovery/trusted-config/seat-guardrail · m-5's authority ceiling — **a tool call parsed by m-9 stays INERT until the existing trusted authority/tool-exposure path authorizes it** |

Unchanged: **m-7** remains the trusted host + sequencer; **m-1/m-3** retain the TCB/identity + egress/observation policies consumed by m-8/m-9. **Master+VP own cross-domain reconciliation; they do not absorb either new domain.** Division III (TUI · Email UX · UI/UX · Platform Plumbing) stays Step-4+.

## 3. Ownership map + the consumer-lock set (single-writer; VP #3/#4)
**One artifact, one governed writer — co-ownership rejected.**
- **m-8** owns the **factual lane catalog**: stable lane identity, provider/API/compatibility facts, supported modalities/tool/reasoning/stream behavior, limits, time-stamped prices, source/provenance, adapter-contract version.
- **m-4** (existing) owns the **policy overlay**: capability buckets/priors, basis, recommendation policy, justified deviation, route selection, benchmark/admission policy — **keyed TO m-8 lane IDs, never writing m-8 rows.** m-4 also PMs the routing slice (V3).
- **m-3** owns **qualification evidence** (e.g. canary observations); m-4 decides how current evidence affects admission/routing.
- **m-1/m-7** own their existing trust/config boundaries.

**The consumer-lock set is NOT only m-8↔m-4.** It is: **m-8↔m-1/m-3/m-7 · m-8↔m-9 · m-8↔m-4 · m-9↔m-5/m-7.** Each is an interface-lock-first seam (as m-1↔m-2 was for c1); Master+VP lock the joins.

**MANDATORY m-4/m-2 routing-record amendment (VP re-review #2 — the condition is ALREADY MET at the bytes, so this is unconditional, not "if").** m-4's *locked* record says the Step-1 API/record does not change at Step-3, records one model-valued **`chosen_model`**, and binds buckets by **model membership** (`chosen_model ∈ members(declared_bucket)`) — not provider/serving/compat lane identity (`m-4 design:164-166,205-218,41,93-106,128-136`); the live FieldSpec row exposes `chosen_model` and no provider/serving/compat/lane reference (`frank/internal/fieldspec/registry.json:175`). So an exact `{model_id, provider_id, serving_profile_id, compat_mode}` execution choice **cannot be replay-complete** without either silently redefining `chosen_model`/bucket-membership or selecting provider/serving *after* the recorded decision — both violate the no-silent-widen / no-silent-fallback rule. **Therefore, BEFORE any m-8/m-9 lock or runtime build: a mandatory m-4 owner-authored review-driven amendment + m-2 FieldSpec review.** The owners choose the representation (canonical lane reference or explicit lane tuple), but the accepted route record must **bind the exact executable lane + snapshot** while preserving R2's non-gate-referenceability ([[fence-row-vs-owner-path]]).

## 4. The catalog treatment (light, but pinned; VP #5)
Approved: a **seeded** table, **models.dev-shaped import boundary**, **auto-discovery DEFERRED** (YAGNI on drift until unit-count warrants), and kept **out of the heavy config-member ceremony** unless the design finds a trusted-load reason. **Rejected: my coarse key + the "bookkeeping therefore unpinned" inference.**
- **Durable lane key ≥ `{model_id, provider_id, serving_profile_id, compat_mode}`** — model producer/vendor is metadata; the adapter-contract/version is bound. (My `{vendor, model, serving}` omitted provider-vs-producer + compat_mode, which changes tool/stream/reasoning behavior.)
- **Minimum pinning:** schema/version · source + observation/effective-time for drift-prone facts · canonical digest · immutable lane ID · **the exact catalog/policy snapshot-or-digest recorded on the route/run.**
- **Dynamic reliability → a separately-evidenced qualification record (m-3), not a static spec-sheet claim.** No secrets/credential values in catalog bytes.
- **R2 still holds:** model/lane identity remains **non-gate-referenceable payload** ([[m4-routing-record-shape]]) — reproducible execution + payload provenance ≠ making model identity a gate predicate.

## 5. The spine decomposition — VERTICAL-FIRST (VP #2; supersedes the adapter-first cut)
Three outcomes, sequenced so the load-bearing contract (can frank drive+govern a turn?) is proven first — avoiding the writer-with-no-reader risk the founding review prohibited (`master-org-decomp/…-031820:24-26`). **After design-lock:**
1. **V1 — the vertical governed turn:** one *pinned* lane through one adapter + the minimal m-9 turn loop — **provider-request egress per §1 (final authorization at the governed send boundary, or the locked pre/post design, with NO post-authorization mutation) · streamed-output observation · authority-ceiling enforced BEFORE any tool execution.** A static test route selects the lane; the outcome is **one real governed end-to-end turn.**
2. **V2 — portability:** add the **second provider** through the *same* contract + conformance suite — now the **"≥2 providers through one interface" claim is EARNED.**
3. **V3 — routing execution:** execute an **accepted m-4 routing record** into the exact pinned lane descriptor, and **prove no silent fallback.**

**Spine exit — with a LIVE-EVIDENCE FLOOR, not just fixtures (VP re-review #4):**
- **V1 and V2 each require an E3 live call against a real external provider endpoint** — in addition to the deterministic **E2** conformance/failure fixtures. Mocks/aliases satisfy neither claim: the spine claims to prove the credential/endpoint/wire/streaming/cancellation/egress abstraction, so it must actually exercise it.
- **V2 must exercise TWO independently-bound provider paths** — two catalog aliases routed through the same effective endpoint/protocol do **not** earn the ≥2 claim.
- **Named negative exit legs (fail-closed):** egress reject ⇒ **zero network send** · above-ceiling tool call ⇒ **zero execution** · absent/invalid exact lane at V3 ⇒ typed **`routing_unavailable`** or **`human_decision_required`**, never fallback.
- Plus: the m-8/m-9/m-4 contracts locked + consumer-reviewed; the catalog seeded+pinned; every outbound request provider-egress-gated (§1). **Explicitly non-terminal for Step-3.**

## 6. The pre-build design sequence (the gate to any T4 build; VP #6)
"Compressed" removes idle routing, not design substance. Before a T4 slice team receives a spec:
1. **AUDIT** — m-8 + m-9 each produce an audit/promotion matrix against pi, opencode, the landed frank interfaces, and the locked m-x contracts.
2. **DESIGN** — each planner authors its DESIGN; each paired implementer runs adversarial DESIGN-REVIEW.
3. **GRILL** — each hard-to-reverse cross-domain design gets a durable grill + **`GRILL_LOCK_ID`** (the live operator conversation is decision-input, NOT a substitute for the recorded grill).
4. **OWNER AMENDMENTS + CONSUMER REVIEW (all three unconditional; all must CLOSE before step 5 may lock m-8/m-9):**
   - **m-3** authors + reviews the **provider-request-egress amendment** (§1a) — m-7 host, m-8/m-9 consumer review.
   - **m-7** authors + reviews the **trusted-config/credential amendment** (§1b) — m-1 secret-boundary review, m-8 consumer review.
   - **m-4** authors + reviews the **exact-lane routing-record amendment** (§3) — m-2 FieldSpec review.
   - Plus the standing consumer-seam reviews (m-1/m-3/m-4/m-5/m-7 over the named m-8/m-9 seams). *(No "if §3 requires it" — the routing amendment is unconditional per §3.)*
5. **RECONCILE + LOCK** — Master+VP reconcile the joint boundary and lock each owner document (**not before all three step-4 amendment reviews close**); the owners then publish the specs-of-record.
6. **T4 BUILD** — only then may a T4 slice team own local detail-design/plan/impl under Part F's escalation triggers, vertical-first per §5.

**§6 AMENDMENT — PARALLEL AUTHORING (review-driven, CTO+VP sequencing authority, 2026-07-14; `step3-audit-reconcile` r-VP concurrence; NO product/secret/risk election, no fresh operator ratification).** Steps 2–4 are **authored concurrently**, not serially: after the AUDIT reconcile, the m-8/m-9 **DESIGN (step 2/3)** and the three owner-amendment **draft/audit/design lanes (step 4)** run in parallel. Bounding rules (unchanged lock gate):
- **Parallel authoring is NOT parallel locking.** An owner amendment may **not** reach lock/close on assumptions its consumer design has not yet exposed — the relevant m-8/m-9 DESIGN + DESIGN-REVIEW + GRILL outputs must **feed its consumer packet BEFORE the amendment's final review**.
- **No m-8/m-9 lock (step 5) occurs until ALL THREE owner amendments, their paired adversarial reviews, AND the named consumer confirmations close.** The step-5 gate is untouched; only step-2/3/4 *authoring* is parallelized.

## 7. The full Step-3 carry ledger (the spine does NOT exhaust Step-3)
Named so nothing disappears under "deferred past the spine":
- **Fuller Step-3 (roadmap `:104-116`):** the benchmark **score** (m-4 + runtime + adapters) — distinct from the **later-release adaptive feedback loop**; the **native governed agent-spawn primitive** (frank's own agent-teams — the Step-5 recursion substrate); **steer-at-boundary** + **soft-interrupt/cancel-redeliver** (the interjection mechanism).
- **From the Step-2 close (by dispatch ID):** T5 (elaborate-more fork, g2) · T10 (re-prompt/claimless-`held`, dc) · the item-2 soft-expiry-arbiter design cell (m-7+m-3) · the s9 queue (T7/T8 · C-1 · R-1 · AO-1 · B1 scope_paths §F m-1).
- **Named pre-build OWNER AMENDMENTS (mandatory, gate m-8/m-9 lock — not deferred):** the m-4/m-2 routing-record amendment (§3) · the m-3 provider-request-egress amendment + the m-7 credential/trusted-config amendment (§1). These are pre-build design gates, listed here only so the ledger is complete.

## 8. Gate on the first T4 code token (VP #6.6)
The first T4 code token is gated on **BOTH**: (a) the charter-mandated **frank live relaunch/shakedown** — the store bring-up is done for master-carriage, but the relaunch **roster must include the T4 pair + the master router/arbitrator seat + the owning m-x PM seat on frank** (VP re-review #3), and the shakedown must **prove one full escalation round trip `T4→master→m-x→master→T4` with stamped provenance, no operator transport**; and (b) the **still-owed Part-F mechanics** — nested lineage across three tiers · authority-ceiling-at-spawn for the T4 team · master-down arbitration · **export from the live frank store to the durable `../master/relays/` trail** at close (`CYCLE-PLAYBOOK.md:387-396`, Part F.6).

## 9. Pre-flight (owed before the first boot)
1. **Operator ratification** of: the Division-II org expansion (m-8 Provider Adapters + m-9 Model Runtime) · the revised vertical-first scope · this kickoff as plan-of-record. **✔ RATIFIED in-session 2026-07-14** (m-9="Model Runtime" + vertical-first confirmed; org expansion + kickoff ratified).
2. **VP co-sign** of the folded kickoff. **✔ CO-SIGNED (approve `step3-prep/RECONCILE-orchestrator-reviewer-20260714-222000`, SHA-256 `cf480081…`)** — three revise rounds folded (`…-213000` r1; `…-215000` r2; `…-220500` r3; `…-221500` r4 admin).
3. On the co-sign → master amends the charter org chart + domains table, authors the m-8/m-9 domain charters, and cuts the boots into the pre-build design sequence (§6). **IN PROGRESS.**
