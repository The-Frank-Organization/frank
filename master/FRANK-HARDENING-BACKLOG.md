# frank Hardening Backlog — battle reports → the development ecosystem

**The principle (operator, 2026-07-15).** The master team **dogfoods the BASELINE PROTOCOL + NAIVE MANUAL RELAY**. Every friction, failure, and near-miss it hits is a **battle report**. Battle reports are **frank's hardening spec** — they harden frank from a trusted courier into a whole governed **development ecosystem**. *The team's friction IS the product roadmap.* Corroborated by the 2026-07-15 external audits, whose closing line is the same: *"the next improvement is not fewer checks — it's fewer duplicated representations of the same fact."* These items are **advisory input, adjudicated** (per the confusion-firewall discipline) — not auto-adopted.

## Battle reports (the source ledger)
- **BR-1 (2026-07-15) — external adversarial audit.** `references/orchestrator-team-audit-2026-07-15.md` + `references/orchestrator-team-workflow-audit-2026-07-15.md`. Two independent instances (Codex + Claude Fable 5 ×19 subagents) over ~3,000 relays (harness master + frank `.relays` + pdc/bivpak), zero-authority external advisories. **Thesis:** the org deviation works, the courier kernel works, **the SEAM between them — workflow enforcement — is prose, not mechanism; that seam is the unfinished product.** It corroborated the ratified reframe's ratify→fold→non-transitive-authority sequence.
- Prior in-team battle reports already live in `PROTOCOL-DEVIATIONS.md`, `RECONCILE.md`, and the s5 `TRANSPORT-FINDINGS-2026-07-06.md` ledger — this doc is their forward-looking product index.

## Adopted NOW as governance rules (option-a, operator 2026-07-15)
- **RULE-1 — T4-preflight before any code token.** No first T4 code token issues until an **executable** `T4 → master → domain-PM → master → T4` round-trip has actually **RUN through the live frank store** (not merely designed). Sharpens the kickoff §8 T4-token gate: *"runs on frank" must be operational fact, not prose* (BR-1 finding: genesis + observe only, no complete round-trip has run).
- **RULE-2 — gates-cannot-check queue, before m-x moves onto frank.** The operator's incidental **100%-relay read** is a real, unmechanized safety layer (it caught the s9 mis-seated token, the U1 rollback, the Step-3 framing correction). Moving m-x onto frank-as-courier **removes it silently.** Before that transition, ship a **"gates-cannot-check" queue** (staffing/role-independence · authority grants · locked-surface touches · unverifiable action-claims) so the human-review layer is *explicit*, not incidental.

## Hardening backlog — mechanization findings → frank features (ride into Step-3/4 design)
| # | Battle-report finding | frank feature | the prose-enforced gap |
|---|---|---|---|
| **H-1** | the mis-seated s9 token **linted CLEAN** (one seat = plan-reviewer + builder + owner) | **role-independence invariant** (reject an IMPL whose `TO` == the `FROM` of its governing PLAN-REVIEW) + **lint-at-submit with conductor-computed lineage** + **git-aware `ACTIONS_GIT_REF`** (the VP's manual SHA check → an invariant) | "gates unfakeable" is true only of observe-as-send; staffing/authorship/action-claims are socially enforced |
| **H-2** | INDEX is an overloaded prose log (1,263 rows, 600 status strings, malformed rows, dangling paths, ≥1 unindexed relay, no round field). **+ instance 2026-07-17:** cross-session filename-timestamp skew made a causally-correct review chain (`step3-mvp-design-m8` 030000 → 024129 → 024600) read as retro-dated — the operator had to query it; causality was provable only from `IN_REPLY_TO` + in-body hash verification | **INDEX as a DERIVED PROJECTION** from relay headers + a **structured `ROUND` field**; the conductor's own **receive-order commit stamping** kills the seat-clock-skew class outright (relays order by store commit, never by self-stamped filenames) | a hand-maintained index is fragile by construction |
| **H-3** | whole-document replication — the r13/r17 loops **and our own r1→r4 packet loops** re-syncing stale copies | **represent each decision once** `{decision_id, revision, digest}` | a rule in N sections = N stale surfaces |
| **H-4** | charter density caused a **load-bearing misread** (the reframe-triggering conductor-scope error) | **kernel/lore split** (standing law vs dated lore; the pdc RUNBOOK proves it achievable) | a dense charter braids law + lore in 15-line paragraphs; an ungoverned external-model answer was imported as input |
| **H-5** | root lint is RED and stays RED (processes the exempt index + historical lineage/merge-gap defects) | require the **active dispatch subtree root-green** before dispatch/merge + an **archive-exception manifest** | "locally clean, globally never green" equilibrium |
| **H-6** | evidence targets aren't hard floors (`target_gt_achieved` accepted) | **evidence target = hard floor** | a claim can under-deliver its own target |
| **H-7** | `FOLD_SCOPE`/merge not bound to the actual git diff | **bind `FOLD_SCOPE` + merge to the real git diff** | scope is asserted, not computed |
| **H-8** | CC fan-out grew 4.0 → 9.4 | **derive CC from the collision-edge graph** | CC is hand-maintained and drifting |
| **H-9** | post-hoc merge ratification (s10) treated as a normal path | **retire post-hoc merge ratification** | out-of-order authority |
| **H-10** | pdc/bivpak convergent reinventions | **backport**: `RESIDUALS.md` durable carrier · R-CLASS swept rules · the r2-then-fork round budget · ADDENDUM/lock-reopen | convergent prior art in a sibling team |
| **H-11** | the seam-13 tool-freshness knot (a heavy per-call *fresh preventive authorization* apparatus was assumed load-bearing, then dissolved on inspection) | **tool-governance model = audit-universal + capability-gate-only-the-irreversible + correctness-downstream**: every tool call → m-3 evidence (cheap 90%); coarse capability ceiling (m-5) whose sharp end is *irreversible* actions (external send / destructive op / force-push, where downstream detection is too late); *semantic correctness* is undecidable to pre-bound → caught downstream by independent seats + adversarial review (confusion-not-malice). Proposed to m-5 as a ceiling-role refinement (`step3-amend-m5-ceiling/…-145500`); routed for m-5 to accept/refine/reject. | the ceiling's preventive role was scoped wider than the confusion threat needs; prevention was reached for where detection is the tractable mechanism |
| **H-12** | Step-3 MVP `bash` runs with **ambient host/external/destructive authority** — no cwd confinement, no sandbox, no irreversibility gate (operator-accepted residual risk for the MVP; audit is evidence, not prevention) | **trusted tool executor / sandbox**: canonicalize + confine filesystem targets (reject symlink/`..`/absolute escapes), bound subprocess + network behavior, and **gate destructive/external operations** (human-gate the irreversible) — the enforcement that makes the H-11 capability model real for `bash` | a tool-name ceiling containing `bash` cannot separate reversible from irreversible capability; "cwd-scoped" is a false boundary without a real executor |
| **H-13** | frank's **agent-facing output ergonomics** aren't yet designed for token budget (the native conductor tool `relay.submit/project/read` output + the frank CLL) — external input **AXI** (`references/axi`, an installable `axi` skill), a *benchmarked* 10-principle standard for agent-driven CLIs (gh-axi 100%/$0.050/3-turns vs GitHub-MCP 87%/$0.148/6-turns) | **adopt the cheap MVP-appropriate principles in the m-9 native-tool DESIGN** (definitive empty states · pre-computed aggregates e.g. inbox counts / "0 relays" · truncation-with-`--full` for `read` bodies · structured errors on stdout); **Step-4 frank-CLI conformance** (TOON output ~40% cheaper than JSON · ambient-context session hook · content-first no-args · contextual next-step disclosure). **Convergences already locked** (validation): AXI fail-loud-not-drop ≈ reject-not-drop; idempotent-mutation ≈ replay-by-content-hash (`cmd/frank-mcp/mcp.go:182`); no-interactive-prompts ≈ operator-direct-route; `{accepted,rejected,held}`+`scrubError` ≈ structured errors | output ergonomics were treated as a build detail; a benchmarked standard shows token budget is a first-class design constraint for the agent-facing surface — orthogonal to the r4 amendment (not an architecture change) |

**These H-items are the workflow-enforcement layer = the Step-4+ product.** Surface each into the relevant Step-3/4 domain design (H-1/H-6/H-7 → m-2 forms + m-3 observe + m-1 lineage; H-2/H-3/H-8 → the record/projection model; H-4 → charter refactor; H-5 → the lint/CI harness).

## Hygiene done (2026-07-15)
- INDEX malformed rows **1075/1081/1085 escaped** (in-prose `|` → `/`); 11-column parse restored.
- INDEX line 676 "dangling path" = a **WITHDRAWN-relay record** (correct as history; resolved properly by H-2's projection, not a hand-patch).
- **Flagged, handled separately:** `frank/README.md:128-168` drift ("Step-2 next"; a stale MCP-honesty string) — a `frank/` build-repo doc pass (a git-repo edit, not a cwd governance doc).
| **H-14** | m-10 r13→r14 (2026-07-17, self-flagged in `step3-mvp-design-m10/SITREP-…-183000`): a routed cross-seam fold (`rejected_local`) was **shape-correct but UNREACHABLE** under an inherited timing sentence — caught only by the reviewer's REACHABILITY check ("does the new enum value have an emission path?") | **reachability-audit as a STANDARD review lens** for every enum/disposition/state addition: the reviewer proves an emission/consumption path exists at the folded bytes, not just shape-consistency; cheap, mechanical, catches the dead-member class | shape review passes dead contract members; nothing forces "can this value ever be emitted?" |
| **H-15** | the VP hand-caught a full class of census/staleness defects in one week (F71 missing-edges · F74 stale-thread · F75 stale-basis rows · F80 imported-token census · F81 carrier/lineage mixing) — every catch was mechanical in nature; **external proof of mechanizability**: jcode's task-DAG engine ships the same invariants as ENGINE RULES (`UncoveredSiblings` = a gate pass must address every in-scope node BY ID, blanket "all good" structurally rejected · `StaleGateScope` = a pass is void if scope grew after dispatch · artifact-or-nothing turn ends; `references/jcode-swarm-notes.md`, code-verified in `crates/jcode-plan/src/dag/`) | **conductor-computed census + staleness gates**: derive the confirmation-edge census from the dispatch graph (a close/lock relay must address every edge BY ID or be rejected at submit); bind confirmations to producer digests so a re-hash mechanically voids dependents (the F73 ladder as an engine rule, not VP prose); reject blanket confirmations structurally | the census, the staleness rules, and the edges-vs-files accounting live in VP review prose today — a reviewer-grade model re-derives them every round instead of the store enforcing them once |

## Battle report — external audit (GPT-5.6 Sol Pro via operator, 2026-07-20)

The operator fed frank-dev (`main@6e4d657`, incl. the vendored governance workspace) through GPT-5.6 Sol Pro for a full external audit. Verdict compressed: "not especially novel as multi-agent topology; materially differentiated as a commit-integrated governance plane"; "reference-monitor-like for the governed relay plane, not yet for the complete system"; the decisive risk = TCB fragmentation as authority spreads across m-7/m-8/m-9/m-10/executor/observer/build. Largely CORROBORATES our own recorded limitations (it cites our own honesty back at us: the §8b convention, the E3 convention-binding, the bash residual, the H-3/H-4 staleness class) — but contributes one verified code defect, several genuinely new mechanisms, and a claim-discipline vocabulary we adopt. Full text held by the operator; dispositions below. New PRIOR-ART sources landed in `master/PRIOR-ART.md` §5.

- **H-16 — the post-commit outcome relabeling defect (VERIFIED at the code, 2026-07-20; pre-T4 fix).** `internal/engine/loop.go:168-185`: post-commit failures (completeTurn / AfterGateResolution / AfterApprovalResolution / AfterAccepted) return `Rejected` carrying the already-committed relay ID; retry returns the persisted (possibly Accepted) state via `existingOutcomeForCommand` (:136,:299) — the same intake presents rejected-then-accepted. Fix shape (audit's, sound): split the outcome into two monotonic dimensions — `decision_state {accepted|rejected|held}` (never relabeled after commit) × `post_commit_state {complete|pending|failed|unknown}` — with post-commit failures becoming durable derived-work faults/obligations, not decision relabels. OWNER: m-7 (conductor core). GATE: a bounded fix lane pre-T4 (the T4 build wires new callers to this exact API), operator-opened; not a silent edit to the closed Step-2 engine.
- **H-17 — the TCB / effect census (master-owned; a stage-6 lock input).** One machine-readable table, one row per governed effect: effect_class · policy owner/artifact · decision point · enforcement point · exclusive-credential holder · freeze point · authorization + effect linearization points · outcome observer · canonical record · KNOWN BYPASS PATHS · failure/unknown semantics · replay mechanism. Most rows already exist as prose in the closed contracts; the census makes "governed" a bounded claim instead of a vibe. Master authors it with the stage-6 lock packet.
- **H-18 — A-class authority authored by the authority principal (Step-4).** Today operator ratifications are agent-authored + operator-cited (§8b — accepted under confusion-not-malice, and the audit concedes this). When hard-gate claims ship: A-class decisions arrive through the authenticated operator SEAT on the live courier (the mechanism already exists!) or an operator-held key signs the exact frozen decision {action, target, args-digest, policy/release, expiry, one-shot nonce}. Approval of a plan never transitively authorizes an action class.
- **H-19 — broker-stamped worker-generation provenance (Step-4; AMENDMENT-class, do not re-open now).** Keep FROM = the durable logical seat (the closed m-1 design is right); add trusted, broker/m-10-stamped metadata (mint_generation · worker_generation · turn_epoch · run/turn ids · release_digest) into the system envelope or a signed execution receipt, for forensics/replay. Touches closed m-1/m-7 contracts → post-lock amendment only.
- **H-20 — the signed-evidence ladder (Step-4+/release-binding hardening).** E3's semantic binding is strong but its observer is convention-bound (our own recorded limitation). The upgrade: DSSE envelope over the exact JCS E3 bytes + a workload identity (SPIFFE-class) for the observer + SLSA-style provenance for release artifacts. Standard mechanisms, not home-grown crypto — consistent with the no-home-grown-security rail; sequenced to when public claims harden.
- **H-21 — effect-descriptor authorization (merges INTO the Step-4 permission design; pairs the paperclip lane).** A tool name is a bundle (bash = fs+exec+net+creds+vcs+ipc). Authorize {action, resource, effect_class, args_digest, one_shot} descriptors; whole-harness execution mediation (agentsh-class placement) = the H-12 trusted-executor successor.
- **H-22 — trajectory/sequence-level policy (Step-4+).** Per-call policy misses read-confidential → summarize → send-external chains (Microsoft AGT names the same limitation). Frank's edge: evaluate over the SYSTEM-DERIVED causal graph, starting with deterministic counters/taint facts (external_sends, secrets_accessed, irreversible_writes, trust-domain crossings) — never AI judgment first.
- **H-23 — the derived artifact-lifecycle projection (sharpens H-2/H-3; pairs H-15).** Immutable digest-addressed artifact CONTENTS × machine-derived artifact LIFECYCLE (candidate/approved/ratified/operative/superseded, from accepted authority records); dashboards/status generated from the projection, never reconstructed from prose. The stale root-README the audit flagged is this class.
- **H-24 — formal modeling of the composed state machines (pre/during-T4 candidate).** TLA+/Alloy over the cross-component protocol (ticket consume × epoch fence × crash windows × provider freeze-authorize-attach-send × gate resolution), then model-derived fault scenarios into the Go crash/replay harness. The audit's invariant list is a usable seed; a background-fork task shape.
- **H-25 — topology + governance metrics.** Instrument per task/topology: human minutes, latency, tokens, relay count, gate wait, review rounds, unique-vs-false findings, post-done commits, held/unknown rates, stale-epoch rejections, duplicate-effect preventions, operator overrides. Data before any topology-router ambition (the science-of-scaling result: architecture-task alignment decides sign).
- **Adopted claim discipline (effective immediately, all outward text):** say "reference-monitor-like for the governed relay plane" · "confusion-resistant identity" · "locally observed evidence" · "one-shot tool authorization is design-locked" · "the MVP has an authority seam". Avoid: "first agent governance platform" · "unfakeable" unqualified · "complete provenance" · "exactly-once external effects" · "sandboxed/least-privilege" · "independent verification" (local model reviewers). Positioning of record: **"a durable governance plane between agent harnesses and execution sandboxes — identity, lineage, gate outcomes, and evidence status as system-derived records rather than agent-authored claims."**
- **The audit's end-to-end bar, adopted as the T4 exit shape:** ONE real effect (one provider send + one local write) demonstrating exclusive chokepoint · frozen request · policy/release-bound authorization · directly-attributable authority · one-shot consumption · crash-safe succeeded/failed/unknown · (signed evidence when H-20 lands) · no alternate path. This is what the Step-3 exit test should prove, said sharply.

- **T11 — the governed error relay to the operator (Step-4 carry; operator-raised in the stage-5 grill 2026-07-20, scoped by master `stage5-m10/045603`).** MVP answer = m-10's app-local loud alert (persistent unmissable surface alert + non-zero exit on run-terminal failures; the operator IS the app user). Step-4: the failure fact travels as a governed relay into the operator's normal frank surfaces, composing with m-6's gate→bucket/Owner-Decision-Brief machinery (a run-terminal failure is an ODB-class event). **The design heart, preserved verbatim from m-10's escalation: the failure that kills the worker kills the CARRIER** — so the relay half needs either a control-plane-held reporter credential (an m-1/F60 seat question, deliberately NOT settled in the MVP) or accepted may-not-arrive semantics + polling fallback. MVP invariant already owed (m-10 design statement): every run-terminal class is VISIBLE IN THE DURABLE E0 RECORD under m-3 r4's existing enum — the Step-4 relay upgrades DELIVERY of a recorded fact, never records a new one.

- **CORRECTION (2026-07-20, VP F88 — supersedes the "audit's end-to-end bar" sentence above as the T4 exit shape).** The single-effect bar overclaimed the ratified MVP (local effects incl. bash are NOT governed; F59 proves the exact structured call was authorized+invoked, not what ambient bash did; same-UID residual accepted). **The corrected THREE-LEG exit proof:** (1) **provider-send leg** — freeze → policy authorize → credential attach → send on the designated credentialed route, claimed as "no alternate DESIGNED CREDENTIALED provider path within the confusion-not-malice threat model" — never system-wide OS isolation; (2) **local-write leg** — exact F59 request binding + one-shot consume + invocation + crash-safe honest outcome — never an exclusive filesystem chokepoint or no-ambient-bash claim; (3) **composed governed-turn leg** — both records bound to the same run/turn/release evidence WITHOUT upgrading either leg's claim. **H-18 disposition (master's election, VP option 1): the T4 claim is NARROWED** to attributable requester/actor + named policy source + exact request binding — full H-18 (operator-seat/signed A-class authority) stays Step-4; the direct route's agent-authored citation remains E0 self-report, honestly labeled. Signed evidence = `not applicable/deferred` until H-20 — never counted as a passed property. **H-19 rider (VP): generation provenance is neither authority provenance nor attestation — state so wherever H-19 lands. H-23 assignment: master-owned, medium tier, Step-4 (before any lifecycle dashboard becomes authority-bearing). H-24: a bounded non-gating model (ticket × epoch × crash; authorize × send × restart) is authorized — counterexamples become fixtures; full formalization off the T4 critical path. H-25: stable raw IDs/counters land DURING T4 where retrofit risk is real; analysis/routing use = Step-4.**

- **S-1 (a STRENGTH, not a gap — recorded 2026-07-20 from the m-9 stage-4 flags):** Governance Decay (arXiv 2606.22528) empirically validates frank's governance-outside-the-summarizer architecture — authorization re-derived from durable state every dispatch means the paper's worst failure mode (compaction deletes a policy → violations) is structurally unreachable; only the confusion layer (model-visible objective/constraints) needs Tier-0 pinning, which the stage-4 worker design carries. Cite in outward claims under the adopted discipline (a relay/authorization-plane property, not a whole-harness sandbox claim).

- **H-26 — `-mint`'s unlocked writer (a live Step-2 code defect, surfaced by the VP's H-16 F99 review 2026-07-20; elected name-the-defect per `h16-outcome-split/SITREP-planner-20260721-002411`).** `frank/cmd/frank/main.go:112-122` dispatches `cfg.MintSeat` BEFORE `store.AcquireRoot`; `mintSeat` (`:577-600`) probes the socket, checks genesis, opens the binding, and mints WITHOUT the root lock — violating the locked s6 single-writer law (`conductor.lock` = phase −1, held for the writer lifetime; socket/PID state is diagnostic, never authority). Risk class: a mint racing a conductor start (or a second mint) on the same root. FIX PATH: the H-16 rev16 ceremony's lock helper is designed shared-shaped — converge `-mint` onto it in the H-16 IMPL lane under the same operator merge grant (one lane, two fixes) if the operator wills; otherwise its own micro-lane. OWNER: m-7. PRE-T4 preferred (same reasoning as H-16 itself).
