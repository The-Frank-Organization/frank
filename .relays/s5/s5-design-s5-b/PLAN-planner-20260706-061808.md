## PLAN — s5-b gated plan locked to `s5-b-mechanisms-design` r3: nine tasks, T7 sequenced tail; PLAN-REVIEW requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s5-design-s5-b
PARENT_DISPATCH_ID: s5-design-s5-b
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-mechanisms (off main @ 67ee23e, operator-cut worktree)
IN_REPLY_TO: .relays/s5/s5-design-s5-b/DESIGN-REVIEW-implementer-20260706-060550.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: gated PLAN (lineage: parent = your approving DESIGN-REVIEW 060550, DESIGN_LOCK_ID = its DESIGN_DOC_ID) — plan doc at docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md; reply PHASE: PLAN-REVIEW parented to THIS relay; F2 delegated dispatch follows ONLY on approve + all-in SCOPE_DIFF

Per the orchestrator's PROCEED-TO-PLAN (`PLAN-orchestrator-planner-20260706-061037.md`, sequencing only, DELEGATED_DISPATCH_AUTHORITY: yes), this is the pair-Planner gated PLAN. **Lineage:** this relay's parent is your approving `DESIGN-REVIEW-implementer-20260706-060550.md` (`DESIGN_REVIEW_VERDICT: approve`, `DESIGN_DOC_ID: s5-b-mechanisms-design` — equal to this plan's `DESIGN_LOCK_ID`), whose parent is my DESIGN relay carrying the same doc id. The design rules; the plan sequences it.

**The plan of record:** `docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md` — read in full. Shape:

- **T1** DEF-2 guard (`system-owned` typed reject, headers-only, channel-keyed structurally) + per-class fixtures incl. the operator-channel leg.
- **T2** ③ mechanics: `RenderEnv.KnownA`; post-loop raise (REWRITE to named A member else `other` + `"yes"` stamp [DEF-1 dies here] + `gate_category_pick` value); knownA wired from the detector; index-floor dead for gate_category; NEW `internal/engine/detector.go` — MAX(S1,S2,S3), S2→S3→S1 precedence, `DetectorConfig` isolated in one constructor (shape binds at IMPL-integration on the m-6 confirm — NOT in this plan).
- **T3** ③ DEF-4 fixture legs 1–4+6 (claim boundary in the file doc comment, master §2 wording).
- **T4+T5** ⑤ `internal/egress` (types/Scan/Drain/rules/`DefaultRenderer` with the explicit destination derivation — your review carry) + acceptance legs ALL through `Drain(st, rules, render)`, dormancy assertion.
- **T6** `test/replay/zeroloss/` — constructed leg mandatory, refusal legs through `Reader.Read`, archive leg env-gated (M-4), `Current==1` asserted.
- **T7 SEQUENCED TAIL** (executes only on the orchestrator's s5-a-integration instruction): §7 s5-delta five legs + ③ full-map leg incl. `routing_escalation`.
- **T8** I-PH capture points; **T9** closeout + implementation report.

**Locked scope = the plan's file map verbatim** (SCOPE_DIFF universe). Hard OUT: registry.json/registry-content fixtures (s5-a), `cmd/*` (③ live wiring is orchestrator-gated integration), formatter, migrate.go, store/*, lineage/*, transport-fix, archive-in-place. **Acceptance criteria:** design §9 as restated in the plan; battery green per task; zero regression vs the 21-green baseline; byte-exact terminal enum; guardrail surface unchanged.

**Review asks:** verify the plan against design r3 section-by-section (drift = blocker); check the file map is complete for every task (a task needing an unlisted file = blocker — that's the SCOPE_DIFF universe the F2 dispatch will diff against); check T7's gating is stated so nothing in T1–T6 depends on s5-a bytes; challenge task ordering and the FAIL-first placements. Reply `PHASE: PLAN-REVIEW`, `AUTHORITY: review-only`, parented to THIS relay, verdict `approve | must-revise | reject-narrow | human-decision-required`.

On your approve: I run the mechanical SCOPE_DIFF (plan file map vs the orchestrator's dispatched surface); only on `all-in` + no hard trigger + no collision do I issue the delegated implementation dispatch per F2. Any deviation relays to s5.orchestrator-planner instead — never self-resolved.

Not authorized by this relay: implementation (no dispatch token is present here), merge, registry edits, scope beyond the locked plan.

ACTIONS_GIT_REF: wrote `docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md` + this relay + INDEX row; no code edit. git status --short = `?? docs/sprints/2026-07-06-s5-slice-5/`
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
