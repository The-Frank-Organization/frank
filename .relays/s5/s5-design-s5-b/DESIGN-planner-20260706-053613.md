## DESIGN — s5-b bundle design complete (③ per master §2 composed ruling; DEF-2 three grains; ⑤ R-2; replay; §7 s5-delta; I-PH): formal DESIGN-REVIEW requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s5-design-s5-b
PARENT_DISPATCH_ID: s5-design-s5-b
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: s5-b-mechanisms-design
IN_REPLY_TO: .relays/s5/s5-design-s5-b/DESIGN-orchestrator-planner-20260706-052753.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: DESIGN-REVIEW request — challenge `docs/sprints/2026-07-06-s5-slice-5/designs/s5-b-mechanisms-design.md` (DESIGN_DOC_ID s5-b-mechanisms-design); reply PHASE: DESIGN-REVIEW parented to THIS relay with DESIGN_REVIEW_VERDICT

The s5-b design doc is written and on disk: **`docs/sprints/2026-07-06-s5-slice-5/designs/s5-b-mechanisms-design.md`** (DESIGN_DOC_ID `s5-b-mechanisms-design`, rev r1). It designs the full bundle against the specification of record:

- **③** (§1): detector = MAX(S1 A-floor, S2 referenced-gate lookup, S3 merge-split) injected engine-side via `RenderEnv.KnownA`; mechanics = committed-token REWRITE to the named A member (else `other`) + `gate_category_raised:"yes"` (DEF-1 folded) + `gate_category_pick` provenance (MR-1, row s5-a's); the raise becomes a post-loop step (B-absorb needs it); index-floor arithmetic deleted for gate_category; DEF-4 six-leg fixture incl. the claim-boundary negative. Config SHAPE binds at IMPL on the m-6.implementer confirm — gates integration, not our PLAN.
- **DEF-2** (§2): pre-loop typed REJECT (`system-owned`) on non-empty system/computed-owned headers, channel-keyed structurally (Validate = the submit path; conductor-internal writers bypass it), envelope asymmetry preserved; per-class fixtures incl. the operator-channel leg.
- **⑤** (§3): new `internal/egress` package (R-s5-4 call), `Scan`/`Drain` at the real outbox-walk call site, two-class rules with the single ODB `model_name`/operator carve-out keyed on conductor-set `Origin`, fail-closed, present-but-dormant with a structural dormancy assertion; pass + (a)/(b)/(c) + fail-closed legs.
- **Replay** (§4): `test/replay/zeroloss/` subpackage, store-path-parameterized, constructed leg mandatory + archive leg optional (`FRANK_S5_REPLAY_STORE`, M-4 pending), count/identity/canonical-wins over `migrate.Reader`, refusal legs at `Reader.Read` grain, `Current` stays 1 with the R-1 false-trigger named.
- **§7 s5-delta legs** (§5): five adapted legs over the real s5-a registry payload + your explicit genesis-count and forced-ValidateGenesis assertions (adopted); sequenced post-s5-a-integration (R-s5-6).
- **I-PH** (§6): all new surfaces through the existing sweep patterns; formatter untouched.

**Review asks (challenge hard — §12 lists my discretionary calls):** (i) the S2→S3→S1 member precedence tie-break; (ii) `RenderEnv` threading vs a new Validate param; (iii) the `system-owned` class token; (iv) the `internal/egress` locus; (v) the post-loop raise placement; (vi) DEF-2 covering `owner:computed` (M-3(g) kept the row computed — guard closes suppliability, not a row flip); (vii) the zeroloss subpackage split; (viii) `Origin.ConductorODB` as the carve-out key. Also verify the design against the ruled texts: master's `s5-escalations/RECONCILE-…-052214.md` §2 (③), the 052753 update relay (DEF-2 grains), R-2/Q4/Q11/[VP-W7], and R-s5-1..7 — any drift from a ruling is a blocker.

Reply as a file relay in this directory: `PHASE: DESIGN-REVIEW`, `AUTHORITY: review-only`, `PARENT_DISPATCH_ID` pointing to this relay, same `DESIGN_DOC_ID`, with `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`. On approve I file the design-complete SITREP to s5.orchestrator-planner and hold for PROCEED-TO-PLAN.

Not authorized by this relay: PLAN, IMPL, any code edit, any merge. No dispatch token is present here.

ACTIONS_GIT_REF: wrote `docs/sprints/2026-07-06-s5-slice-5/designs/s5-b-mechanisms-design.md` (design doc, tracked path, uncommitted — the sprint dir itself is not yet git-tracked, so status collapses to the dir) + this relay + INDEX row; no code edit. git status --short = `?? docs/sprints/2026-07-06-s5-slice-5/`
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
