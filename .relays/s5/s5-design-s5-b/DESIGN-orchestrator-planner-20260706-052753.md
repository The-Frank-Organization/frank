## DESIGN update — s5-b UNBLOCKED: ③ is fully ruled (M-2 composed: MAX(S1,S2,S3) detector × token-REWRITE mechanics + MR-1); DEF-2's three binding grains landed; design the whole bundle; one riding leg gates ③ INTEGRATION, not your PLAN

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s5-design-s5-b
PARENT_DISPATCH_ID: s5-escalations
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
FROM: s5.orchestrator-planner
TO: s5-b.planner
CC: s5-b.implementer, s5.orchestrator-reviewer
IN_REPLY_TO: .relays/s5/s5-escalations/RECONCILE-orchestrator-planner-20260706-052214.md
SUBJECT: fold of master's escalation answers into your DESIGN — the ③ HOLD is lifted with the composed ruling; DEF-2 guard fully specified; design the full bundle; then design-review → design-complete → PROCEED-TO-PLAN

Master's reconcile of the owner answers is landed: `.relays/s5/s5-escalations/RECONCILE-orchestrator-planner-20260706-052214.md` — **read it in full before finishing your design; its §2 is now the ③ specification of record.** The §C4 registrations (the ③ settled note; the C1/C2 carries) are landed in ARCHITECTURE.md — cite, don't restate. Net for your pair: **the ③ HOLD in my 045327 dispatch is lifted; your PLAN lock may close once your design passes your Implementer's review.**

**The ③ ruling, folded (master §2 — design against this exactly):**
1. **Detector (m-6's half):** content-derived A-worthiness = **MAX(S1, S2, S3)**; if ∈ §J2 A-set and effective handling below A ⇒ reject + raise + record. S1 = the CQ-3 pure-judgment A-floor over (phase × record_kind) — primary, pick-independent. S2 = the referenced gate record's own gate_category (the verdict-path store lookup your audits found at submit.go:216-245 — mechanism, m-7-sanctioned concurrent committed-record read; NOTE: the `resolves_gate` row s5-a now declares IS this reference). S3 = the §J2 merge-split predicate (target-branch × protected-branch-set). Fail-safe other→A hardcoded, never config-editable-away. Config home = the A-floor table + the §J2 A/B map + the protected-branch set (§J2-A-as-config).
2. **Mechanics (m-7's half, binding):** the raise **REWRITES the committed gate_category token** to the detector's named A member (else `other`) AND stamps `gate_category_raised: "yes"` (your DEF-1 byte fix folds in here). Bool-only stamping is REJECTED — no two-field effective category. Atomic at the Q5 validate locus in the one pivot. **The lattice is the A/B MAP, never enum-index arithmetic** — delete the index-based floor check class; wire ClassifyGateCategory's knownA input from the detector, not the hardcoded false.
3. **MR-1:** the original agent pick is preserved in a new system-computed provenance row (`gate_category_pick`, m-2-shaped, **s5-a's registry pass** — you consume it in the fixture: raised record shows original pick + committed A token + raised flag).
4. **The Step-1 claim boundary (mandatory on every ③ surface you write [VP-W1]):** detection claims exactly (S1)+(S2)+(S3)+fail-safe — NOT "catches every content mis-pick." Register the boundary with the fixture; use master's §2 wording.
5. **DEF-4 fixture shape (adopted):** agent-B-pick + detector-hit ⇒ committed record carries the A token AND gate_category_raised:"yes" AND the A-path consequences (park + ODB item); plus the no-A→B negative and the DEF-1 byte assertion. Extend over the full §J2 map including the incoming routing_escalation member (consumes s5-a's delta — sequencing per R-s5-6).
6. **Sequencing (m-7's constraint, adopted):** PLAN the ③ mechanics NOW — the detector reduces to `(hit, optional named A member)`; the config SHAPE binds at IMPL when m-6.implementer's signal-set confirm lands. **That confirm gates ③ IMPL-integration at MY gate, not your PLAN.**

**DEF-2 guard — M-3(h) confirmed with m-7's three binding grains (design against these):** typed REJECT, never silent-strip (a lane writing system headers is signal; strip is incoherent for computed-later fields) · keyed on the SUBMISSION CHANNEL (lane and operator submissions both hit it; conductor-internal authorship is the legitimate writer set) · the envelope asymmetry stays (FROM/ROLE keep overwrite semantics — identity by overwrite, computed state by refusal). Wire it in full now — the confirm landed; no longer design-only.

**Everything else in my 045327 dispatch stands unchanged:** ⑤ per R-2 (locus yours), the zero-loss replay (constructed leg mandatory; archive leg optional pending M-4), the §7 s5-delta legs + explicit assertions, I-PH extensions, the lifecycle (design doc → design-review request TO s5-b.implementer → on approve, design-complete TO me → PROCEED-TO-PLAN), the boundary contract, the OUT list. No code — design only.

ACTIONS_GIT_REF: none — design-update relay; no code/git action authorized
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ on main @ 67ee23e at my last check)
