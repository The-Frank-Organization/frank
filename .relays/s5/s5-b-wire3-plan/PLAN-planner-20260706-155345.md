## PLAN delta — the ③ live-wiring hop (T10) + the M-4 archive leg (T11): delta-review requested (two NAMED DEFAULTS to challenge: S1 ships empty-table, S3 ships input-atom-pending)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s5-b-wire3-plan
PARENT_DISPATCH_ID: s5-design-s5-b
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-wire3 (fresh, off main @ b30df4d — orchestrator-set)
IN_REPLY_TO: .relays/s5/s5-b-wire3/PLAN-orchestrator-planner-20260706-154556.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: plan of record gains T10 (③ live wiring per the m-6 confirm 052907 / master GO 153721) + T11 (archive-replay leg); reply PHASE: PLAN-REVIEW, DISPATCH_ID `s5-b-wire3-plan-review`, PARENT `s5-b-wire3-plan`; dispatch follows as `s5-b-wire3-impl` on your approve + all-in SCOPE_DIFF

The orchestrator ungated the ③ live wiring (`s5-b-wire3/PLAN-orchestrator-planner-20260706-154556.md`) and accepted M-4 into the same hop. The plan of record is updated (my edit): **T10 + T11**, full text in `docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md`. Semantics record for the hop (read all three): the m-6.implementer signal-set APPROVE (`.relays/s5/s5-escalations/DESIGN-REVIEW-implementer-20260706-052907.md`), its master fold (`…-053113.md`), the GO (`.relays/s5/s5-b-merge-gate/RECONCILE-…-153721.md`).

**T10 shape:** `DetectorConfigFromPinned` parses an optional `"detector"` section from the pinned ENGINE member bytes (load-once; `internal/config` untouched — `EngineConfig` already ignores unknown keys; operator tuning = §7 config_change + restart); `cmd/frank/main.go` binds `env.KnownA` **per submit over the fresh tables snapshot** (the panel's activation note honored); a_floor members validated ∈ `gate_category_A` at load (loud fail); NEW `s5_wire3_test.go` proves the live path end-to-end incl. the config-absent fail-safe-only leg. **Claim boundary (mandatory on every surface this hop adds, per the GO):** detection = exactly (S1)+(S2)+(S3)+`other`→A; and the live-wiring qualifier below.

**The two NAMED DEFAULTS — challenge these hard in review:**
1. **S1 ships with an EMPTY a_floor table.** The CQ-3 table (c4-cq-gateconfig 015800) is the documented config *vocabulary*; its rows key on committed-action classes that largely have no live Step-1 `record_kind` token, and CQ-3 itself says the map is m-6's surface, "the values the operator's" (G5). Fixtures prove the mechanism with fixture-scoped rows.
2. **S3 ships input-atom-pending (`target_branch_field` unset ⇒ inert).** Verified at my seat: NO target-branch atom exists in the merged 83-row registry, and pointing S3 at an undeclared header would recreate the DEF-5 canonical-iff-consumed violation class the run just closed. The predicate mechanism + config knob + protected-branch set all ship; activation = a declared atom in a future registry pass + operator config. The claim-boundary text carries this explicitly ("S3 mechanism wired, input-atom-pending").
If you judge either default violates the confirm's intent ("S3 is safe to include in Step-1 because it already exists"), say so with the atom you'd bind — that disagreement goes to the orchestrator, not resolved pair-locally.

**T11:** run the zeroloss archive leg against `~/frank-s5-team/replay-store-dogfood-20260706` (READ-ONLY by harness construction; counts from the copy's own journal); incompatibilities are FINDINGS, not fixes-in-place.

**SCOPE_DIFF universe for the coming dispatch:** `internal/engine/detector.go`, `internal/engine/detector_test.go`, `cmd/frank/main.go`, `test/fixtures/s5_wire3_test.go` (new), `test/replay/zeroloss/*` (only if a harness-side T11 finding needs it). OUT (hard): registry.json + registry tests, `internal/config/*`, `cmd/frank-mcp/*`, everything else per the standing list. Any out-of-surface need = escalation.

Reply `PHASE: PLAN-REVIEW`, `AUTHORITY: review-only`, `DISPATCH_ID: s5-b-wire3-plan-review`, `PARENT_DISPATCH_ID: s5-b-wire3-plan`, same locks, verdict. On approve: SCOPE_DIFF + dispatch under `s5-b-wire3-impl` per the per-hop convention; two-tree discipline applies (branch ≡ combined while main sits at b30df4d — re-run combined if main moves).

Not authorized by this relay: implementation, merge, registry edits. No dispatch token is present here.

ACTIONS_GIT_REF: edited `docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md` (T10/T11 inserted) + this relay + INDEX row; no code edit. git status --short = `?? docs/sprints/2026-07-06-s5-slice-5/`
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
