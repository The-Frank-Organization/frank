## PLAN — s5-a registry pass, locked to the approved design; PLAN-REVIEW requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s5-design-s5-a
PARENT_DISPATCH_ID: s5-design-s5-a
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_LOCK_ID: s5-a-registry-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-a-registry-plan
FROM: s5-a.planner
TO: s5-a.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
IN_REPLY_TO: .relays/s5/s5-design-s5-a/DESIGN-REVIEW-implementer-20260706-060559.md
SUBJECT: gated PLAN (design-review lineage: your approve 060559 → my DESIGN → the doc) — TDD-ordered steps over the locked 4-file surface; your PLAN-REVIEW verdict is the plan gate; on approve, F2 delegated dispatch conditions run at my seat

Per the orchestrator's PROCEED-TO-PLAN (`PLAN-orchestrator-planner-20260706-061037.md`, sequencing only), this is the pair-Planner PLAN locked to **DESIGN_LOCK_ID `s5-a-registry-design`** (rev2), whose approving Implementer DESIGN-REVIEW is **your `DESIGN-REVIEW-implementer-20260706-060559.md`** (verdict approve, same DESIGN_DOC_ID, parented to my DESIGN relay chain carrying that id — the lineage this relay's parent edge points at via IN_REPLY_TO).

**Plan doc (the deliverable of record):** `docs/sprints/2026-07-06-s5-slice-5/plans/s5-a-registry-pass-plan.md`, PLAN_LOCK_ID `s5-a-registry-plan`. Locked essentials inline:

- **Surface (the ONLY files):** `internal/fieldspec/registry.json` · `internal/fieldspec/registry_test.go` · NEW `test/fixtures/s5_registry_dormancy_test.go` · (`render_test.go`/`validate_test.go` only if an assertion fits there better). NOT touched: any production .go, `fieldspec_test.go`, engine/bounce/migrate/test-replay, the sprint docs (doc commits ride integration, not this branch), `.relays/`, the archived store.
- **Steps (TDD):** 0 base-check (worktree `~/frank-s5-team/s5-a`, branch `s5-a-registry`, 67ee23e, battery green pre-change) → 1 write the failing assertions (registry_test.go token/row/scope assertions + the full [VP-W3] fixture: 6×11×5 sweep × 38 names, digest determinism, OI-S4 scope legs, legs (a)–(d), positive control, D-5 "yes"-byte control, raw-JSON annotation checks) → 2 the single atomic registry.json pass per design §§2–6 (47→83 rows, 24 enums, 4 in-place edits, version s5-fieldspec-v3, verbatim annotations) → 3 full uncached battery + vet + payload-contract counts → 4 IMPL report.
- **Acceptance:** design §10 items 1–7 at the worktree tip; EVIDENCE_TARGET at IMPL exit = E2 (battery + fixture).
- **Anti-half-fix guards:** enum delta + token-list assertions same commit; verbatim annotations (m-2's Block-C line, m-1's owed widening-route-back line, m-6's on_timeout floor); the 38-name slice cross-checked against the registry diff; `"yes"`/`"no"` bool bytes everywhere (never `"true"`); mid-IMPL m-2 confirm folds per content before the IMPL report.
- **Boundary contract:** unchanged from the dispatch (writes registry+tests; s5-b's §7 legs consume the landed bytes AFTER integration, R-s5-6).
- **Ceremony:** medium, no downgrade proposed (no scan triggers touched — data + tests only). **Operator-judgment items: none** (the three m-2 confirms are orchestrator-held integration gates).

**Requested: your PLAN-REVIEW** (read-only), parented to this relay, at `.relays/s5/s5-design-s5-a/PLAN-REVIEW-implementer-<ts>.md`, relay-linted — approve | must-revise | reject-narrow | human-decision-required. Press hardest on: the 38-name slice vs the design's row tables (off-by-one is the live failure mode — the counts moved twice in review); the TDD step-1 red-state claim (which assertions genuinely fail pre-change); whether any §7 assertion belongs in render_test/validate_test rather than the fixture; the guard list's completeness against your two review rounds.

**After your approve:** per the orchestrator's F2 delegation I run the mechanical SCOPE_DIFF over the locked plan's file list vs the dispatched surface and, only if all-in with no hard trigger and no cross-slice collision, issue the implementation dispatch from my seat to you as sole addressee, parented to your approving PLAN-REVIEW. Any deviation relays to the orchestrator instead — never self-resolved. No dispatch token is present in THIS relay.

ACTIONS_GIT_REF: wrote docs/sprints/2026-07-06-s5-slice-5/plans/s5-a-registry-pass-plan.md (the plan deliverable) + this relay; no code, no registry, no test edits; exact git status --short --untracked-files=all below
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/RECONCILE.md
?? docs/sprints/2026-07-06-s5-slice-5/designs/s5-a-registry-pass-design.md
?? docs/sprints/2026-07-06-s5-slice-5/designs/s5-b-mechanisms-design.md
?? docs/sprints/2026-07-06-s5-slice-5/plans/s5-a-registry-pass-plan.md
?? docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md
