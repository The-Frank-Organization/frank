## RECONCILE — the four s5 audits reconciled; seven orchestrator rulings; the M-1..M-4 escalation bundle to master; DESIGN dispatched to both pairs on the unblocked scope

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: review-only
DISPATCH_ID: s5-reconcile-audits
PARENT_DISPATCH_ID: s5-resume
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: s5.orchestrator-planner
TO: master.orchestrator-planner
CC: s5.orchestrator-reviewer, s5-a.planner, s5-b.planner, operator
IN_REPLY_TO: .relays/s5/s5-audit-s5-a/AUDIT-planner-20260706-042008.md
SUBJECT: paired audits reconciled (high convergence, zero contradictions unresolved); FIVE new live defects/gaps found by the fresh team (the standing bar pays again); rulings R-s5-1..7; escalation bundle M-1 (blocks s5-a PLAN) + M-2 (blocks ③ PLAN) + M-3 confirm-batch + M-4 operator item; DESIGN dispatched on the unblocked scope

Inputs (all read in full, reconciled toward file:line evidence): s5-a.planner AUDIT-planner-20260706-042008 · s5-a.implementer AUDIT-implementer-20260706-041357 · s5-b.planner AUDIT-planner-20260706-041832 · s5-b.implementer AUDIT-implementer-20260706-041025. All four booted clean, ran independently, and carried their boot acks folded. The reviewer is online (boot ack 040910, no blocking findings).

### A. Reconciliation

**s5-a pair — AGREEMENT (both seats, independent E1):** registry baseline 47 rows @ s3-fieldspec-v2; every consumer-domain field ABSENT except the settled-known rows (GRILL pair :123-124, gate_category_raised :93, the F-GATE-2 owed rows, delivery_state/failing_edge); the routing_escalation delta supportable exactly as m-2 specified, byte-distinct (routing_unavailable / human_decision_required: zero occurrences), MINOR; the OI-S4 `*`-scope holds genesis + owed tokens; nested row_array COLUMNS are inexpressible in the live grammar (both independently: the single largest declaration blocker); record_kind gains no live member; predicate_result/veto/rank1 not persisted.

**s5-a — DIFFERENT COVERAGE, resolved toward evidence:** (1) planner's **D1** — EVIDENCE_TARGET has NO required_when at :90, so the [VP-W3] fixture leg (c) is unenforceable against live code as stated; the implementer asserted leg (c) without code-checking it. Resolved toward the planner's E1. (2) Both found ACTIONS_GIT_REF/FINAL_GIT_STATUS_SHORT render on Step-1 forms (required_when observe-gated, no visible_when) — the implementer names the fix (add visible_when), adopted into the confirm batch M-3(c). (3) Planner-unique finds adopted: the render-context constraint (visible_when atoms usable at render = phase/tier/seat/layer_present ONLY — field/any_row/record_kind_in are always-false at render, render.go:30-33,:54); `resolves_gate` consumed-but-undeclared (submit.go:70 — a live canonical-iff-consumed violation predating s5) → M-3(j); T1 (below).

**s5-b pair — AGREEMENT (both seats, independent E1):** ③ is partially built at exactly the ruled locus (classifier config-fed validate.go:176-187; raise stamp validate.go:61-66 post-stamp/pre-commit; load-once config) — but the knownA leg has NO production caller, the stamp path has ZERO test coverage, and the raise today fires only for unknown/`other`; ⑤ is greenfield-confirmed (grep-reproduced) with outbox PRODUCTION existing and NO drain — the drain IS the chokepoint to build (m-7 §9); §7's five adapted legs are generically covered by the s4 suite (do not rebuild; extend with the s5-registry-payload variants + explicit no-re-genesis and phase-0-chain assertions where the implementer showed the s4 assertions are indirect); Q4 mechanics identical in both reads (Current stays 1; new replay independent of harness.go's external oracle; R-1 tripwires named byte-for-byte).

**Cross-pair TRIPLE-CONFIRMED live defects (the fresh-team bar, again):**
- **DEF-1 (T2/EQ-4): the raise stamp writes `"true"` into a `["no","yes"]` enum** (validate.go:64 vs registry.json:36,:93) — a legal rendered `other` pick self-bounces on the conductor's own stamp. Found independently by s5-a.planner (code-trace) and both s5-b seats.
- **DEF-2 (T1/EQ-3): lane-suppliable system/computed headers.** ignorePayloadField skips owner:system/system_only at validation (validate.go:115-120) with NO submit-path guard (seat.Stamp sanitizes envelope only, binding.go:112-116) — a lane can write bytes into failing_edge and every new dormant system row; and owner:computed rows (gate_category_raised) are validated-but-suppliable. Directly contradicts Q5's "no lane supplies or suppresses it" until closed. Found by s5-a.planner (general) + both s5-b seats (the raised-flag instance).
- **DEF-3 (D1):** EVIDENCE_TARGET not actually required at Step-1 — the settled answers' premise ("stays Step-1-required") is not true of the live code.
- **DEF-4:** the ③ stamp path untested end-to-end (no fixture asserts a committed record carries the raise) — the owed fixture is genuinely owed.
- **DEF-5:** `resolves_gate` consumed-but-undeclared (pre-s5 canonical-iff-consumed violation).

### B. Orchestrator rulings (within my authority; no semantics self-authored)

- **R-s5-1 (version label):** `s5-fieldspec-v3` — monotonic counter continues across the file's history (s3-fieldspec-v2 → v3), slice provenance in the prefix. Provenance block updated in the same pass.
- **R-s5-2 (write surfaces, answers EQ-2 + Q-e):** `internal/fieldspec/*.go` CODE for ③-scoped mechanics (the raise extension, the DEF-1 byte fix, the DEF-2 validator guard if M-3(h) confirms) = **s5-b**. `registry.json` + `registry_test.go` (byte-exact token-list assertions are registry-content-coupled) + `render_test.go`/`validate_test.go` registry-content fixtures = **s5-a**. Any registry.json row/column change arising from s5-b findings rides s5-a's single pass. `fieldspec_test.go` (classifier grain) = s5-b.
- **R-s5-3 (DEF-1 assignment):** s5-b fixes the stamp byte to the row's own enum (`"yes"`), sequenced with/before the ③ fixture. Enum-conformance, not new semantics; listed in M-3 as FYI-confirm.
- **R-s5-4 (⑤ drain locus):** naming/placement of the drain function (internal/egress vs store/drain) is the s5-b pair's DESIGN decision inside the R-2 shape.
- **R-s5-5 (replay store source):** the constructed-store leg is MANDATORY (deterministic CI); the archived-copy leg is an optional second leg pending the operator (M-4). Harness built store-path-parameterized so both legs share one implementation.
- **R-s5-6 (sequencing):** s5-b's §7 s5-delta fixture legs consume s5-a's landed registry — they land after s5-a's registry pass integrates at my gate. All other s5-b work (⑤, replay, I-PH, DEF fixes, ③ once unblocked) parallelizes.
- **R-s5-7 (scope_paths):** NO row — defer to Step-2 (the discretion RECONCILE Q7 left me), adopting the planner's canonical-iff-consumed argument.

### C. Escalation bundle → master (hub-and-spoke; recommendations attached, none self-adopted)

- **M-1 (m-2 + your gate-interpretation; BLOCKS the s5-a PLAN lock, ~10 rows):** the lane-fillable consumer rows (routing cluster: routing_assignments, justified_deviation, deviation_reason_code, routing_record_kind, constraints; ODB agent slots: plain_language_change, why_now, tradeoffs_risks, recommendation, choices) fit NONE of the three dormancy idioms: not observe-owned (the closed layer_present vocabulary has no routing layer), and reserved-shape leaves them RENDERED as optional fields on every Step-1 form — which collides with the [VP-W3] exit bar "each consumer field ABSENT from every Step-1 rendered form surface." The render context supports only phase/tier/seat/layer_present atoms (E1: render.go:30-33,:54). Options: (i) m-2 blesses `visible_when+required_when {layer_present: observe}` as a pure STEP-GATE idiom over non-observe-owned consumer rows (renders nothing until a later fill-layer wires; semantically "a later layer", not "these are observe fields"); (ii) declared-live-shape (they render; the [VP-W3] enumerated field list excludes them with documented reasons — m-4 §3:164 does say seats write the routing records); (iii) a grammar extension (new layer value) — we recommend AGAINST in s5 (mechanism change, m-2-owned). **Recommendation: (i)**, falling back to (ii) with your explicit gate-bar re-scope.
- **M-2 (m-6 + m-7; BLOCKS the ③ portion of the s5-b PLAN):** the known-A detector's per-record INPUT SIGNAL is unspecified at code grain — Q5 ruled the SET (§J2-A-as-config) but not what marks a submission A-worthy when the pick is B. Code-grain candidates found by the pair: the referenced gate record's own gate_category at the verdict path (detectable from tables, submit.go:216-245) vs the monotonic-floor mechanism (unwired in production, and the index-based floor check cannot express [floor,A] over the A-first enum ordering). Second half of the same grain: does the raise rewrite the EFFECTIVE gate_category token to the A member, or stamp the bool only (current code stamps bool, leaves the token — m-2 §17.1's "[floor, A] affordance" reads render-side)? Both halves needed before the pair locks the ③ plan.
- **M-3 (m-2 confirm batch, with our recommendations):** (a) deviation_reason_code value-set placement — Q8 said "config, not registry", but registry.json IS the fieldspec config member and gate_category's values live in its named_enums; recommend mirroring the 7 defaults into named_enums with a config-sourced annotation. (b) add required_when to EVIDENCE_TARGET (fixes DEF-3; makes code match the settled premise). (c) add visible_when {layer_present: observe} to ACTIONS_GIT_REF + FINAL_GIT_STATUS_SHORT (they render today). (d) on_timeout: valueless reserved slot (default hold_and_resummon stays J1 policy). (e) OI-S4: remove `genesis` from the `*` scope now (certain); owed_item/owed_disposition per m-2's two options — recommend owner-typing now, owed-id-picker as a later engine item. (f) nested row_array columns: recommend NO grammar extension in s5 — top-level carriers + prose-documented columns + Step-3 enforcement carry (m-2 + m-4 confirm). (g) gate_category_raised stays owner:computed (the Q5 text "s5-a adds as owner:system" has a premise slip — the row EXISTS as computed, :93); the suppliability closes via (h), not a row flip. (h) DEF-2 closure: a validator rule rejecting non-empty system/computed-owned headers on lane submissions — recommend closing GENERALLY in s5-b (one rule + per-class fixtures; the honesty stakes are [VP-W1]-level); m-2/m-7 confirm. (i) surface_intent: m-2 §17.6 declares its computed home — our dispatch list omitted it; recommend s5-a declares it (same class as record_integrity); the posture value-enum stays m-5 config. (j) resolves_gate: recommend s5-a folds the missing row under m-2/m-7 confirm (registry-completeness is this slice's charter). (k) FYI: DEF-1 byte fix (`"true"`→`"yes"`).
- **M-4 (operator):** an operator-placed COPY of the archived dogfood store for the replay's optional second leg — propose-only; the constructed-store leg does not wait on it.

### D. Next steps
DESIGN dispatched to both pairs on the unblocked scope with explicit HOLD markers (M-1 rows for s5-a; ③ for s5-b): `.relays/s5/s5-design-s5-a/DESIGN-orchestrator-planner-20260706-045327.md` and `.relays/s5/s5-design-s5-b/DESIGN-orchestrator-planner-20260706-045327.md`. Master's M-1/M-2 answers unblock the held slices; the M-3 batch folds into the s5-a design as confirms land. PLAN gates unchanged (F2). Worktrees still requested: s5-a-registry, s5-b-mechanisms off main @ 67ee23e.

FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ on main @ 67ee23e; .relays/ gitignored)
ACTIONS_GIT_REF: none — review-only reconcile; the three relay files written this pass live under the gitignored .relays/ substrate
Next requested action: route M-1 (m-2 + gate ruling), M-2 (m-6/m-7), M-3 batch (m-2, + m-4/m-5/m-7 where marked), M-4 (operator); answers return as file relays. Everything else proceeds.
