## SITREP — m-7's M-2 half RULED: the raise REWRITES the token (the bool records provenance); + the two M-3 co-confirms (h: reject-not-strip; j: resolves_gate declaration must match the live consumption)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-escalations
PARENT_DISPATCH_ID: s5-escalations
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: s5-escalations/RECONCILE-orchestrator-planner-20260706-050217.md
FROM: m-7.planner
TO: master.orchestrator-planner
CC: operator, s5.orchestrator-planner, master.orchestrator-reviewer, m-6.planner, m-7.implementer
SUBJECT: M-2 raise mechanics — committed `gate_category` IS the effective (raised) value, atomic in the one pivot, lattice = the A/B map never enum index; composable with any m-6 signal-set ruling (their half CC'd); DEF-2 disposition = typed reject; DEF-5 consuming side confirmed with the detector interplay named

m-7's section. I re-read the cited code this session — `validate.go:55-75` (the live bool-only stamp writing `"true"`), `ignorePayloadField` (the DEF-2 skip), `submit.go:62-76` + `classifyVerdict` (`:216-245`), and the `ClassifyGateCategory` contract — so the rulings below bind to the code as it actually is.

### M-2, the m-7 half — RULED: the raise REWRITES the effective `gate_category` token; `gate_category_raised` records that the floor won.

**The committed `gate_category` IS the effective category.** When the detector fires over a B (or unlisted) pick, the validate stage rewrites the token — to the detector's named A member when m-6's signal set names one, else to `other` (the hardcoded `other`→A fail-safe) — and stamps `gate_category_raised: "yes"` (the row's own enum token, DEF-1's fix). Bool-only stamping is rejected, for the load-bearing reason: it makes the effective category a **function of two fields** — every consumer (the m-6 bucket router, the park/ODB path, telemetry, any future §J2 reader) must know the composition rule, and the first consumer that reads the raw token silently absorbs an A item into B — **the exact failure ③ exists to kill**, reintroduced as a read-side landmine. The single-truth grain runs all through the locked engine (one pivot, one truth, canonical wins), and the monotonic precedent already decides this: the committed `HUMAN_GATE_REQUIRED` value IS the MAX result — nobody stores "no" plus a raised-bool. The ③ carry's own wording ("raised to A + `gate_category` recorded", `ARCHITECTURE.md:478`) reads the same way.

**Mechanics constraints, binding:**
1. **Atomic at the Q5 locus:** token rewrite + bool stamp both happen in the validate stage inside the loop's critical section and land in the ONE pivot — no committed-as-B window ever exists, and post-commit nothing moves (immutability). Never render-side, never post-commit.
2. **The lattice is the A/B MAP, never enum index order.** Effective = A-side if (pick ∈ A-set) ∨ detector-hit ∨ pick-unlisted; else the pick stands. The pair's finding is correct — index arithmetic over the A-first ordering cannot express `[floor, A]` and is the bug class to **delete, not repair**. `ClassifyGateCategory`'s (class, raised) contract is the right shape; wire its `knownA` input from m-6's detector config instead of the hardcoded `false`, and act on its output.
3. **Composability with m-6's half (their signal-set ruling, whatever it is):** the mechanics take detector output as `(hit, optional named A member)` — referenced-gate-record category, content predicates à la the merge split, or both all reduce to that pair; token := named member if provided, else `other`. And the verdict-path table read the pair found (`submit.go:216-245`) is sanctioned input from the engine side: concurrent reads of committed immutable records at validate time are lock-free-safe (the same grain as the S3 conditional-render ruling).
4. **The original pick — flag to m-2, not designed here:** rewriting loses the agent's pick, which has real value (deviation-grain telemetry; record-grain proof of "never silently absorbed"). Whether a system-computed row preserves it (a `gate_category_pick` class of thing) is m-2's row decision — I recommend it as cheap and audit-closing, and note that if declined, `gate_category_raised` + the versioned detector config still permit approximate reconstruction.
5. **DEF-4 lands with the ruling:** the end-to-end fixture asserts agent-B-pick + detector-hit ⇒ the committed record carries the A token AND `gate_category_raised: "yes"` AND the A-path consequences (park + ODB item); plus the no-A→B negative and the DEF-1 byte assertion. Anything less re-opens the coverage hole DEF-4 names.

### M-3 (h) — DEF-2 general validator rule: CO-CONFIRMED, disposition = TYPED REJECT, keyed on the submission channel.

Three grains that make the rule right rather than merely present:
- **Reject, not silent-strip:** a lane writing system/computed-owned headers is *signal* (a confused or probing lane) — silently overwriting hides it; and strip semantics are incoherent for computed-later fields (`failing_edge` is computed at bounce time — what would stripping "restore"?). A typed violation naming the field also gives the honest recompose path (a seat that copy-pasted a projection drops the field and resubmits).
- **Keyed on the submission channel, not the field alone:** lane AND operator submissions both hit the rule (the operator authors content; the conductor computes computed fields) — conductor-internal record authorship (parks, outbox records, held records, system stamps) is the legitimate writer set and never passes through lane validation.
- **The envelope asymmetry is deliberate and stays:** `FROM`/`ROLE` keep the locked m-1 overwrite semantics (stamped values win, payload ignored byte-for-byte — NF-S1). The new reject rule is for **headers** with system/computed owners. Two different locked behaviors, one rationale: the seat never controls system truth — identity by overwrite (locked), computed state by refusal (this rule).

### M-3 (j) — `resolves_gate` fold: CO-CONFIRMED on the consuming side; the declaration must match the consumption I re-read.

The live consumption (`submit.go:62-76`, `classifyVerdict`) enforces: operator-channel verdict path; `PARENT_DISPATCH_ID` must equal the gate ref (dead-edge bounce otherwise); the referenced record must be an accepted gate-bearing record; one-shot via the already-resolved scan; wake targets the gate's author. The m-2 row must declare exactly that: **operator-seat-scoped in Step-1**, value = a relay-id reference whose fill constraint is "an accepted gate-bearing record" (m-2 shapes the constraint idiom). Two engine-side notes for the fold: (1) **detector interplay** — if m-6's M-2 half keys known-A detection on the referenced gate's category, this row is the reference the detector follows, so its declaration precision now carries ③ semantics, not just DEF-5 hygiene; (2) the code path keys on bare header presence (`resolves_gate != ""`) — post-fold, presence and the declared record class should be one thing (m-2's shaping; the engine consumes whatever single thing is declared).

**Sequencing note for the master reconcile:** my M-2 ruling is deliberately independent of m-6's signal-set half (constraint 3) — s5-b's ③ portion can PLAN against the mechanics now and bind the detector config shape when m-6's answer returns.

Not authorized / not claimed: section answers only — the m-6 signal-set half is theirs (CC'd); no m-2 row designed (flags only); no cross-domain reconcile (master's); no `frank/` write.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master-docs/master/relays .relays/s5/s5-escalations/SITREP-planner-20260706-051409.md` — run before handoff (result in inline pointer)
- code re-read this session at `main@67ee23e`: `internal/fieldspec/validate.go:55-75` (bool-only `"true"` stamp live), `ignorePayloadField` (`validate.go:115-120` class), `internal/engine/submit.go:62-76`, `:216-245` (classifyVerdict whole), `ClassifyGateCategory` contract via `fieldspec_test.go:64-73`
- locked/standing cites: `ARCHITECTURE.md:478` (③ carry wording), the monotonic HUMAN_GATE_REQUIRED precedent (s1 design D-5), s4-guide-q1 Q5 locus, s3-consult Q3 (concurrent committed-record reads), NF-S1 (envelope overwrite semantics)
- `git -C frank status --short` — untouched by this relay

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260706-051409; `frank/` read-only; no doc/design/code edits
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: master reconciles my M-2 half with m-6's signal-set half + m-2's M-1/M-3 answers and relays the settled bundle to s5; s5-b may PLAN the ③ mechanics against this ruling immediately (constraint 3 decouples it from the m-6 return).
