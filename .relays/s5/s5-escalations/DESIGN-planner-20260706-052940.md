## DESIGN — § m-1 answer: owed authoring = operator-only Step-1 scope (CONFIRM, two-axis) · `genesis` belongs in NO seat's scope, operator included (CONFIRM, remove from both)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s5-escalations
PARENT_DISPATCH_ID: s5-escalations
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — narrow record-class-intent confirm against locked contract + S2 record; no operator decision surfaced
GRILL_REQUIRED: no
FROM: m-1.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-1.implementer, m-2.planner, m-7.planner
DESIGN_DOC_ID: c1-design-m-1-trust-identity
BUNDLE_ID: s5-escalations
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: s5-escalations/RECONCILE-orchestrator-planner-20260706-052214.md

master — the § m-1 record-class confirms, answered as store/record-class owner from the locked contract + the S2 record + the live registry (@ frank `67ee23e`). Both are CONFIRMS of the reconcile's proposed direction; neither reopens c1 or amends the locked doc. m-1.implementer is CC'd for a light in-contract nod if you want pair rigor on the fold (the c4-cq CQ-6 pattern); nothing here blocks s5-a's PLAN per your §5.

### Q1 — owed authoring: CONFIRM operator-only, stated on the correct axis (two axes, don't conflate)

**Answer: yes — remove `owed_item`/`owed_disposition` from `*`; keep them `operator`-only. But record it as a Step-1 SCOPE posture, not permanent class semantics.** The class has two independent axes and S2 settled only one:

- **Provenance axis (S2-settled, unchanged):** owed records are **principal-authored via `submit`, never machinery-authored** — the S2 design's "owed-item: NONE — human/agent-authored only" (`s2-slice-2-design.md:80-82`; a record-authoring principal commits the disposition; no auto-completer) and the guide-rejected shape (ii) (engine-transcribed — "fakes provenance," `:154`). This is what "ride the ordinary submit path" in the S2 F-M1-1 block meant: channel-stamped, never `system`. It was never a class-open seat-scope claim.
- **Scope axis (newly expressible, ruled here):** at Step-1 the only observed authoring principal is the operator (Q2=(i): `OI-S1-F11-SWEEP` = `FROM=operator` via the operator channel, `:84`), and **`owed_disposition` is authority-bearing in effect** — it discharges an open obligation, and open owed items gate exit gates (`disposition_path` = the S2 exit gate). A lane discharging an obligation = clearing a gate obstacle; under "authority blocks before dispatch" + the S1 pessimistic-superset discipline, that scope must be operator-only until deliberately widened. `owed_item` creation is the conservative direction (adds debt, discharges nothing), but it pollutes the exit-gating open set if lane-spammable, and symmetric scoping is the simpler posture — operator-only for both.
- **The widening path (named so intent survives):** a future cycle giving named seats owed-authoring (e.g. a reviewer filing an owed follow-up — the upstream idiom) is a **registry/config scope amendment with m-1 route-back**, not a contract break. The class intent is "record-authoring principal, never machinery"; the Step-1 operator-only scope is the pessimistic floor on that intent, and fill-time enforcement (the option absent from non-operator forms) is the right grain — m-2's fill-time-authority reading is exactly faithful.

### Q2 — `genesis`: CONFIRM it belongs in NO seat's scope — not `*`, not `operator`

**Remove it from both.** Genesis is authored by `store.Init` machinery ONLY:

- My locked §6 conductor-internal provenance clause (the s2-amend fold, §0.e): genesis is `FROM = "system"` — "**never accepted from the public `submit` path**"; no channel binding may hold the name (`s2-slice-2-design.md:50` carries the same, m-1-prescribed).
- Therefore a rendered form offering `genesis` is **incoherent by construction**: any record a seat (operator included) submits is stamped from its channel binding — it can never carry `FROM: system`, so a submit-path "genesis" would be a seat-stamped record wearing a machinery token — a provenance lie the store would then serve as if it were init-grade. Fill-time removal is the right fix; the S4 F-S4-M1-1 line ("not synthetic store genesis") already blocks the same move on the config path.
- Consistency check that confirms the rule: `incident` and `gc_marker` — the other machinery-authored S2 classes — are **already absent** from the live form enum (`registry.json:72`). `genesis` surviving in the seat scopes is the anomaly, not the pattern. m-7's "store-genesis is never lane-authored" stance concurs on record.

### Adjacent flag (registry-pass owner's, not ruled here)

The same principle — *machinery tokens don't belong in any form scope; authority-bearing tokens get the pessimistic floor* — suggests two rows for m-2's s5-a pass to check while it's in there: `gate_resolution` currently sits in `*` while (j) states it operator-seat-scoped Step-1; and `disposition`'s scope should be checked against its class (if it is the quarantine-disposition act, it is loop-internal). Flag only; those classes are not mine to rule.

## Verification
- Live registry @ frank `67ee23e`: `internal/fieldspec/registry.json:72` (7-token enum; `incident`/`gc_marker` absent) + `:113` (`record_kind` `seat_scoped_enum`; `*` currently includes `genesis`, `owed_item`, `owed_disposition`, `gate_resolution`).
- S2 sources: `docs/sprints/2026-07-03-s2-slice-2/designs/s2-slice-2-design.md:50` (internal provenance, m-1-prescribed), `:80-84` (owed = principal-authored; Q2=(i) operator instance), `:154` (shape (ii) guide-rejected); s2 fidelity verdict `.relays/s2/s2-fidelity-m1/SITREP-implementer-20260704-034158.md` F-M1-1.
- Locked m-1: §6 conductor-internal provenance (s2-amend, pair-approved `s2-amend-m-1/DESIGN-REVIEW-implementer-20260704-035323`); §6 operator-address (iii); §5 "authority blocks before dispatch."
- `python3 ~/.claude/skills/tools/relay-lint.py .relays/s5/s5-escalations/DESIGN-planner-20260706-052940.md` — OK (below).

ACTIONS_GIT_REF: wrote this answer relay + appended `master-docs/master/relays/INDEX.md`; read-only probes of `frank/` (no edit); no c1 design-doc edit, no mechanism change, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; frank/ probed read-only @ 67ee23e)
Next requested action: master folds both confirms to s5-a's registry pass (owed rows → operator-only; `genesis` → removed from `*` AND `operator`); m-1.implementer may add a light in-contract nod if master wants pair rigor. m-1.planner holds — no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
