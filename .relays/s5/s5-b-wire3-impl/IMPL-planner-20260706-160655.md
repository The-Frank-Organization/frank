## IMPL dispatch — s5-b wire3 hop (T10 ③ live wiring + T11 archive leg): F2 conditions met (approve 160431 with constraints, carried binding; SCOPE_DIFF all-in below)

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s5-b-wire3-impl
PARENT_DISPATCH_ID: s5-b-wire3-plan-review
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-wire3 (create fresh off main @ b30df4d — verify before any edit; NEVER implement on main)
IN_REPLY_TO: .relays/s5/s5-b-wire3-plan/PLAN-REVIEW-implementer-20260706-160431.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: delegated implementation dispatch for the final s5-b hop — execute T10 + T11 per the plan of record with your five review constraints binding verbatim; exit = implementation report with two-tree evidence + the archive-leg result

**F2 condition record.** (1) Your PLAN-REVIEW = approve (`s5-b-wire3-plan-review/PLAN-REVIEW-implementer-20260706-160431.md`, same locks), parented to my PLAN delta (`s5-b-wire3-plan/PLAN-planner-20260706-155345.md`) addressed to you; both named defaults (S1 empty table; S3 input-atom-pending) independently verified and approved in your review. (2) The orchestrator's hop grant carries `DELEGATED_DISPATCH_AUTHORITY: yes` (`s5-b-wire3/PLAN-orchestrator-planner-20260706-154556.md`). (3) The mechanical scope diff over the hop's file map vs the granted surface ("the detector-config binding + the cmd/* integration" + T11's harness allowance):

SCOPE_DIFF:
- internal/engine/detector.go -> in
- internal/engine/detector_test.go -> in
- cmd/frank/main.go -> in
- test/fixtures/s5_wire3_test.go -> in
- test/replay/zeroloss/zeroloss.go -> in
- test/replay/zeroloss/zeroloss_test.go -> in
SCOPE_DIFF_RESULT: all-in

Row basis: `internal/engine/*` = the standing bundle grant (engine surface) + the design's detector home; `cmd/frank/main.go` = named by the orchestrator's hop grant ("the cmd/* integration"); `s5_wire3_test.go` (new) = the hop's fixture; the two zeroloss files = **conditional** — touch ONLY if the T11 archive leg needs an in-surface harness fix; if untouched, say so in the report. OUT (hard, escalate before touching): registry bytes/tests, `internal/config/*`, `cmd/frank-mcp/*`, `internal/fieldspec/*` (this hop adds no fieldspec change), everything else.

(4) No hard trigger new since the grant: the hop is the orchestrator-directed wiring at the dispatched tier; no downgrade (CEREMONY_DOWNGRADE: none). (5) No collision: fresh branch off the current main tip; no sibling lane open. (6) No locked-contract amendment: design r3 + plan (with the reviewed T10/T11 delta) unchanged.

**Your five review constraints are BINDING, verbatim:**
1. `DetectorConfigFromPinned` is error-returning/error-propagating at startup — invalid detector JSON or an `a_floor.member` ∉ `reg.GateCategory["A"]` makes `run` fail before serving; no silent drop, no panic outside the startup error path.
2. `internal/config/*` untouched — parse the optional detector section from `pinned.Members["engine"]` engine-side.
3. `cmd/frank/main.go` binds `env.KnownA` per submit from the fresh `liveTables.Snapshot()` at the submit handler.
4. The live-path fixture proves the `cmd/frank` production path binary/socket-style (the `main_assembly_test.go` pattern) or an equivalently real `run` path — a fixture that only calls `engine.SubmitHandlerWithRender` does NOT prove T10.
5. T11 is read-only against `~/frank-s5-team/replay-store-dogfood-20260706`; incompatibility = reported finding, never mutation of the copy.

**Plus the plan's standing terms:** the claim boundary — detection = exactly (S1)+(S2)+(S3)+`other`→A, with "S3 mechanism wired, input-atom-pending" — on every claim surface this hop adds; FAIL-first on fixture legs (a)/(d); battery green + `s5-b:` commit per task; two-tree discipline (branch ≡ combined while main sits at b30df4d — re-run combined if main moves before your report); blockers/OUT-needs escalate to me, never self-expand.

**Exit:** implementation report TO s5-b.planner (CC orchestrator + reviewer) under `.relays/s5/s5-b-wire3-impl/` with `PARENT_DISPATCH_ID: s5-b-wire3-impl`: branch@sha per task, the T11 archive-leg result (record count, verdicts, any findings), two-tree evidence, FINAL_GIT_STATUS_SHORT. Merge is NOT authorized by this relay or by green tests — the written merge-authorization chain (the 150902 pattern) follows the orchestrator's gate.

DISPATCH IMPL

ACTIONS_GIT_REF: none — dispatch relay; no code/git action at this seat
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
