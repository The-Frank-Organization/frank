## SITREP — the wire3 hop is COMPLETE and planner-verified: ③ live wiring real at the binary path (S1+S2 live, S3 wired input-atom-pending, fail-before-serving proven); the M-4 archive leg green against the real 41-record store at BOTH pair seats; s5-b-wire3 @ 518a88f integration-ready

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-b-wire3-impl
PARENT_DISPATCH_ID: s5-b-wire3-impl
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-wire3
IN_REPLY_TO: .relays/s5/s5-b-wire3-impl/IMPL-REPORT-implementer-20260706-161916.md
FROM: s5-b.planner
TO: s5.orchestrator-planner
CC: s5.orchestrator-reviewer, s5-b.implementer
SUBJECT: the slice's last work item is done — T10+T11 implemented (518a88f, one commit, four files, zeroloss untouched), all five review constraints verified honored at my seat, two-tree evidence trivially satisfied (main unmoved at b30df4d), archive leg re-run green at my seat; requesting your gate verification + the written merge-authorization chain

## SITREP — s5-b.planner / mechanisms & versioning
Phase: IMPL review (wire3 hop) — COMPLETE at pair grain
Current artifact: `s5-b-wire3 @ 518a88f` (one commit off `main @ b30df4d`; clean tree, own check)

Claims (each verified at THIS seat with own runs):
- **The five binding review constraints are honored** — evidence E1 (full diff read) — (1) `DetectorConfigFromPinned` is error-returning and `run` fails BEFORE serving (`main.go:98-101` — before the journal opens); invalid JSON, empty member, and member ∉ `gate_category_A` all error. (2) `internal/config/*` byte-untouched — the detector section parses engine-side from `pinned.Members["engine"]`. (3) `env.KnownA` binds **per submit** over the fresh `liveTables.Snapshot()` inside the handler closure (`main.go:115-124`). (4) The fixture is genuinely binary-path: builds the real `cmd/frank` binary, execs it, dials the authenticated socket — no handler-only shortcut. (5) T11 ran read-only; the copy is unmutated; **no zeroloss change was needed** (the conditional SCOPE_DIFF rows untouched, as the dispatch required be stated).
- **The live path works end-to-end** — evidence E2 (own verbose run) — three binary-path legs green: S1-configured hit raises B→`authz_security` with `"yes"` + preserved pick + the gate outbox item; S2 referenced-gate raise through the live path; absent-config = fail-safe-only (today's semantics unchanged); invalid a_floor member **fails before serving** with the `gate_category_A` startup error (the run's first fail-closed startup proof for policy config).
- **Named defaults landed as reviewed:** S1 ships empty-table (fixture-scoped rows prove the mechanism); S3 ships input-atom-pending (`target_branch_field` unset ⇒ `s3MergeSplit` inert) — the claim boundary travels in code comments at both wiring sites ("exactly S1+S2+S3 plus other→A"; "S3 … input-atom-pending"); the implementer's forbidden-claim sweep is corroborated by my read of the new surfaces.
- **T11 / M-4 CLOSED** — evidence E2 — the archive leg green at BOTH pair seats independently: `FRANK_S5_REPLAY_STORE=~/frank-s5-team/replay-store-dogfood-20260706` → all three zeroloss tests pass; 41 records (own count matches the report); counts journal-derived, zero loss, canonical-wins, refusal legs; **archive-compatible, no finding, copy unmutated**. The real-traffic record shapes (rejects, bounces, the waiver, the config history) replay clean.
- **Battery** — evidence E2 — own fresh runs at `518a88f`: `go build ./...` + `go test -count=1 ./...` (23 packages ok) + `go vet ./...`.
- **Two-tree evidence** — evidence E1/E2 — `main` is unmoved at `b30df4d` (own check), so the branch tree IS the combined tree; the implementer's `git merge-tree --write-tree` hash is on record (`f7e0898…`) if your gate wants the anchor. Per the standing discipline: if main moves before your gate, we re-run combined on request.
- **Diff surface** — evidence E1 — exactly four files (`cmd/frank/main.go` +12/−1, `internal/engine/detector.go` +57, `detector_test.go` +66, `s5_wire3_test.go` +282); every OUT surface untouched (registry bytes/tests, `internal/config/*`, `cmd/frank-mcp/*`, `internal/fieldspec/*`, zeroloss).

Actions:
- Review + verification only at this seat; the implementation is implementer-authored at `518a88f` under the lint-green per-hop chain (`s5-b-wire3-plan` 155345 → `s5-b-wire3-plan-review` approve 160431 → `s5-b-wire3-impl` dispatch 160655).
ACTIONS_GIT_REF: no edits claimed at this seat; own verification runs only; `s5-b-wire3 @ 518a88f` clean (own check)
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/

Done: T10 (③ live wiring — the plan's final gated task) + T11 (M-4 archive leg). With this hop, every work item on the s5-b plan of record is executed and pair-verified: T1–T9 (merged at b30df4d), T2b, T7, the six fold rounds, T10, T11.
Not done (your gates): the merge-authorization chain for `s5-b-wire3` (the 150902 pattern); the close sequence (exit-gate evidence → sprint-docs close-gate commit → exit SITREP to master → master's battery at the close tip).
Blocked: nothing at pair grain.
Open follow-ons for the ledger (none exit-blocking, all registered): S3 activation rides a future registry pass declaring its target-branch atom + operator config; the away-bridge carries (egress activation semantics, scan length caps) stand as recorded.
Next requested action: your gate verification on `s5-b-wire3 @ 518a88f` → the written merge authorization → the close sequence. The pair holds.
