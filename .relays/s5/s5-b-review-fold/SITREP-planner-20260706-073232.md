## Panel findings for fold — team-of-4 panel on s5-b-mechanisms@81e0551: four approves, ZERO blockers, THREE must-fixes (fold before merge-gate), optionals at your discretion; your fold report is the PHASE: REVIEW-FOLD artifact

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-b-review-fold
PARENT_DISPATCH_ID: s5-b-impl
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-mechanisms
IN_REPLY_TO: .relays/s5/s5-b-impl/IMPL-REPORT-implementer-20260706-071248.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: adversarial panel findings for fold — MF-1 raise-stamp-persists-on-rejected-record (apply raise at acceptance only + fixture) · MF-2 canonical-wins replay leg is inert (make load-bearing) · MF-3 drain sentinels missing from the I-PH sweep · optionals + T7 carry; write FOLD_SCOPE before any edit; targeted re-check follows, no full re-panel

**Panel record (review-panels.md):**
PANEL_CHOSEN: team-of-4
DEFAULT_ROLES_CHANGED: no
WHY_THIS_PANEL: non-trivial diff (16 files, +1796) of trust-critical governance mechanics; default panel; no team-of-5 idiomaticity trigger (Go on established repo idiom, not a refactor).
ROLES:
- security — DEF-2 bypass/suppression, ③ downgrade/evasion, carve-out reachability from bytes, I-PH leaks
- performance — hot-loop overhead, detector/tables races, unbounded scan work, drain semantics
- test-coverage — acceptance-leg proof at committed-record grain, false-green hunt, FAIL-first
- correctness — case-by-case conformance to design r3 + the ruled texts, diff-vs-file-map, byte-exact enums

**Verdicts:** security approve · performance approve · test-coverage approve · correctness approve. **No blocker.** Cross-lens contradiction resolved toward evidence at my seat: correctness F1 said "a bounced record never commits"; security F1 said rejected records persist — **security is right** (`loop.go:133` commits the handler's record regardless of DeliveryState; verified E1 at my seat). All panel evidence E1/E2; two lenses independently re-ran builds/tests.

### Must-fix (fold before the merge gate)

- **MF-1 (security F1 + correctness F1, merged; severity raised on the corrected mechanism).** `raiseGateCategory` runs before the in-Validate `PARENT_DISPATCH_ID` check (`validate.go:66-76`) and before the engine's lineage/record_kind/classify rejections (`submit.go:53,58,63-76`) — and rejected candidates PERSIST as terminal evidenced records. So a record that raises then bounces persists carrying `gate_category_raised:"yes"` (+ possibly a rewritten token and `gate_category_pick`) — a conductor stamp on non-accepted immutable evidence, contradicting design §1.3 "atomic at the validate locus / atomic with acceptance". Inert for every CURRENT consumer (park/outbox/tables/INDEX all gate on Accepted — both lenses verified), but it is dishonest persisted evidence in a trust courier and a trap for any future non-accepted-record reader.
  **Required change:** apply the raise mutations only on the acceptance path — either run `raiseGateCategory` after ALL reject paths (the engine sets Accepted at `submit.go:77`; raising there or equivalently stripping the three raise headers on every reject path both satisfy the design; pick the cleaner — the raise logic may stay in fieldspec, invoked where acceptance is decided), AND add a fixture leg: a detector-hit record that bounces (e.g. bad `PARENT_DISPATCH_ID`) persists WITHOUT `gate_category_raised`/`gate_category_pick`/rewritten token. FAIL-first.
- **MF-2 (test-coverage F1).** The canonical-wins replay leg is inert: `zeroloss.go:56-83` builds its id set from journal/redo + `records/` only and never reads the `outbox/` projection, so `corruptProjection(...)` cannot affect the replayed view — the assertion is true by construction (false-green on the exact contention design §4 names). **Required change:** make it load-bearing — assert the corrupted projection bytes still sit on disk WHILE the replayed view is canonical (record-beats-projection genuinely contended), or construct a genuine record/projection divergence. FAIL-first (the leg must fail if the view were projection-fed).
- **MF-3 (test-coverage F2).** Design §6.2 lists ⑤ drain diagnostics as a swept surface, but the `Drain` sentinel errors (`egress.go:91-98`: outbox-read-error, source-record-read-error, egress-render-error, …) are not in `s5_iph_test.go`'s swept outputs. **Required change:** add the sentinel strings to the sweep capture (1-2 lines).

### Optional (your discretion; none blocks)

- OPT-1 (performance F1): one comment at `egress.go:116` recording that Drain's all-or-nothing batch abort is intentional fail-closed, so the away-bridge activation reviewer doesn't read it as an oversight. (The skip-vs-fail-closed decision itself is activation-time, out of scope.)
- OPT-2 (performance F2): activation-time note only — scanned-length cap / byte-wise fold for `strings.ToLower` per-field copies. No change now.
- OPT-3 (test-coverage F4): synthetic-spec unit test isolating each of `systemOwnedHeader`'s four owner/fill branches. Cheap, nice, not required.

### Registered carries (NOT yours to do now)

- **T7 carry (correctness F3):** add an `gate_category ⊆ A∪B∪{other}` completeness assertion to the T7 full-map leg, so a future member mapped into neither set can't silently escape the rewrite. Lands with T7 at the integration gate.
- **Integration-gate note (security, sharpened; for the orchestrator via CC):** if the ③ detector is ever wired live BEFORE s5-a lands `gate_category_pick` (computed row), that provenance field is lane-forgeable (DEF-2 cannot guard a row that doesn't exist; the B-absorb branch doesn't overwrite it). R-s5-6 ordering must stay enforced.
- **Orchestrator-awareness (correctness F2):** the `validate_test.go` assertion flip (delivery_state `enum`→`system-owned`) is mechanics-grain and in-bounds per R-s5-2/the plan file map; noted for the record.
- Performance activation notes (drain poison-item semantics; per-submit detector snapshot rebuild pattern) — recorded for the away-bridge/integration cycle.

**Fold mechanics:** you act under the standing `s5-b-impl` dispatch, fold-in-only. Before ANY edit, write the `FOLD_SCOPE` artifact listing every file you will touch, each row `-> in` against MF-1/MF-2/MF-3 (+ any accepted optionals); `FOLD_SCOPE_RESULT: all-in`; any OUT row stops the fold — relay the deviation, do not edit. Expected surface: `internal/fieldspec/validate.go` (and/or `internal/engine/submit.go`), `test/fixtures/s5_gate_raise_test.go`, `test/replay/zeroloss/zeroloss.go`+`zeroloss_test.go`, `test/fixtures/s5_iph_test.go`, optionally `internal/egress/egress.go` (comment) — all inside the dispatched universe. Battery green + `s5-b:` commit per the standing constraints; fold report back to me (CC orchestrator + reviewer) with FOLD_SCOPE above ACTIONS_GIT_REF. I then run a targeted re-check of the three fixes only — the panel is NOT re-run (consolidation rules; no design/blast-radius change).

Merge remains NOT authorized — the merge gate stays with the operator/orchestrator; T7 stays gated.

ACTIONS_GIT_REF: none — findings relay; no code/git action at this seat
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
