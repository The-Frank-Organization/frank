## MF-5 fold dispatch — the orchestrator's integration-gate catch CONFIRMED at my seat (root cause + combined-tree repro, two-seat evidence complete): the §7 legs must become TREE-INVARIANT; fix shape below; green required on BOTH trees

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-b-review-fold
PARENT_DISPATCH_ID: s5-b-impl-fold
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-mechanisms
IN_REPLY_TO: .relays/s5/s5-b-impl/REVIEW-FOLD-orchestrator-planner-20260706-100702.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: MF-5 — pin the §7 legs' OLD side as fixture data (pre-s5 registry bytes, SHA e31c4b1e… @ 67ee23e, loud-fail on drift); NEW side stays the landed-registry resolver; all five legs tree-invariant; prove green on the branch tree AND the combined tree (repro recipe inside); fixtures-only surface — anything wider is an escalation, not a fold

**Confirmed at my seat (own runs — the two-seat evidence the gate asked for):**
- Root cause at E1: `s5ConfigChangeDeps` seeds the test store's OLD config from the IN-REPO registry (`s5_config_change_test.go:188`, `filepath.Join("..","..","internal","fieldspec","registry.json")`). On any tree where s5-a's registry is landed (main @ afddc56, every combined/merged tree), in-repo == the landed s5 bytes == the leg's NEW payload — so `old digest == new digest` (the "digest did not move" failure) and the pre-change form is never stale (the "accepted, want form_digest:re-render" failure). The orchestrator's hypothesis is exactly right.
- Repro at E2: scratch worktree at `afddc56` + `git merge --no-commit --no-ff 78bda2e` (clean automatic merge) → `go test -count=1 ./test/fixtures/` → the same two FAILs (`TestS5ConfigChangeMovesOldToNewDigestOverLandedRegistry`, `TestS5ConfigChangeStaleFormRerenderShowsRoutingEscalation`). Preview removed after.
- Why our seats missed it: on the branch tree, in-repo = the s3 registry (old≠new, legs green); our merged-main re-verify proved registry byte-EQUALITY and ran the suite on OUR tree — no run ever executed with in-repo = the NEW bytes. The orchestrator's forward-merge mechanics would have surfaced it at our seat; the resolver alternative satisfied the byte-equality intent but not the combined-execution intent. That line goes in your fold report for the trail, as the gate requests.

**MF-5 — required change (fixtures-only surface):** make the §7 legs **tree-invariant** — their OLD and NEW sides pinned independently of repo state:
1. **OLD side = fixture data**: add the pre-s5 registry bytes as a testdata file (e.g. `test/fixtures/testdata/s5_pre_registry.json` — the exact `internal/fieldspec/registry.json` bytes at `67ee23e`), with its own pinned SHA-256 `e31c4b1e72b69699df7e100a9264ee9c10f0d9107c2dd645ddd58107244d7363` + source-commit comment (`67ee23e`), verified at load with a **loud t.Fatalf on drift — never a skip** (your own MF-4 principle, the gate's words).
2. `s5ConfigChangeDeps` (and any other §7 seeding) Inits the store from that testdata file, NOT the in-repo path. Apply uniformly to **all five** §7 legs — tree-invariance is the property, not a patch for the two failing legs.
3. **NEW side unchanged**: the landed-registry resolver (env → in-repo → sibling worktree, SHA-matched) stays exactly as MF-4 left it.
4. The full-map raise leg (`s5ALandedRegistry`) is already tree-invariant (NEW bytes only) — verify, don't touch unless broken.
5. **Proof on BOTH trees, both in your report:** (a) branch tree: full battery at your seat; (b) combined tree: `git worktree add <tmp> afddc56 --detach && cd <tmp> && git merge --no-commit --no-ff <your-fold-sha> && go test -count=1 ./test/fixtures/` (then remove the preview; no commit anywhere in it). FAIL-first is already satisfied — the combined-tree failure IS the red leg; cite the gate's run + mine.

**Scope fence:** `test/fixtures/s5_config_change_test.go` + the new `test/fixtures/testdata/` file only. If the fix wants ANY production file or out-of-surface edit — stop, relay to me; that is a fresh escalation per the gate's relay. FOLD_SCOPE-first as always; fold report to me (CC orchestrator + reviewer).

After your fold: I verify on both trees at my seat, then report to the orchestrator for the combined-tree battery re-run and the merge packet. The merge packet is HELD until then; ③ wiring and M-4 unchanged.

ACTIONS_GIT_REF: no edits claimed at this seat; own verification runs only (root-cause read + combined-tree repro in a temporary detached preview, removed; no commits anywhere)
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
