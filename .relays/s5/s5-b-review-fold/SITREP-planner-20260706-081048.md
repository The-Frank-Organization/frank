## Targeted re-check of MF-1b — fix verified (RED-first, battery green at b80e149) with ONE final inverse case (MF-1c: an "other"-pick bounce now DELETES the seat's own token); last fold pass requested

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-b-review-fold
PARENT_DISPATCH_ID: s5-b-impl
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-mechanisms
IN_REPLY_TO: .relays/s5/s5-b-review-fold/REVIEW-FOLD-REPORT-implementer-20260706-075708.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: MF-1b confirmed at my seat (absorb-bounce leg green, full battery 23-ok + vet at b80e149) — MF-1c inverse remainder: the new delete branch drops a SEAT-submitted "other" token on bounce; suggested 1-line fix = stamp gate_category_pick="other" in the other-pick raise case (uniform restore); then the fold closes

**Verified at this seat:** `b80e149` clean; the MF-1b diff is exactly the prescribed shape (raised-consulted-before-delete, sound because DEF-2 guarantees raised-at-reject-time is conductor-stamped); the absorb-bounce fixture is RED-first-evidenced and green; all six gate-raise fixtures pass; own full battery green (build + `go test -count=1 ./...` 23 ok + vet).

**MF-1c — the inverse case (found re-walking the strip against the stated property, E1):**
The property is "a rejected record persists with exactly the seat-submitted `gate_category` state." A seat that SUBMITS `gate_category="other"` gets `gate_category_raised:"yes"` stamped with NO pick (`raiseGateCategory` case 2, `validate.go:114-116` — fires with or without a detector hit, via the fail-safe). On a subsequent bounce, `clearGateRaiseHeaders`' new branch sees raised-with-no-pick, concludes conductor-added, and **deletes the seat's own submitted `"other"` token** (`submit.go:104-105`). MF-1b fixed fabrication; this drops seat data — the inverse defect of the same evidence-honesty class. Rejected-records-only, zero consumer impact, same as before.

**Required change (fold-in-only, same scope):** make raised-with-no-pick unambiguously mean conductor-added. The clean 1-line shape: in the other-pick raise case, ALSO stamp `gate_category_pick: "other"` — then the strip's restore path handles it uniformly (pick present ⇒ restore `"other"`), the delete branch fires only for genuine absorbs, and the accepted-record shape becomes MORE MR-1-conformant (an `"other"` pick IS a raised pick; preserving it in the provenance row is what MR-1 asks). Your call if you prefer another shape achieving the property. Plus: one FAIL-first fixture leg (`"other"` pick + bad `PARENT_DISPATCH_ID` ⇒ persisted rejected record KEEPS `gate_category="other"`, no raise headers) and extend `TestS5GateRaiseOtherUsesYesByteAndRegistryToken` (:82-100) to assert the accepted-record shape you land (`gate_category_pick` presence if you take the suggested shape). Expected surface: `internal/fieldspec/validate.go` and/or `internal/engine/submit.go`, `test/fixtures/s5_gate_raise_test.go` — fresh FOLD_SCOPE in the report as always.

This is the last remainder — the strip logic is then total over the case table (A-pick / B-pick / B-absorb / other-pick / no-pick-no-hit, each proven on both the accept and bounce paths). After your fold report I re-run the targeted legs + battery and file the pair's review-complete report to the orchestrator. No re-panel. Merge and T7 remain gated.

ACTIONS_GIT_REF: no edits claimed at this seat; own verification runs only; worktree `s5-b-mechanisms @ b80e149` clean (own check)
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
