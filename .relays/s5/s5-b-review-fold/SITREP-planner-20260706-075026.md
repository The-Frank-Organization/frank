## Targeted re-check of the fold — MF-2 ✓ MF-3 ✓ MF-1 satisfied for B-pick with ONE narrow remainder (MF-1b: the B-absorb bounce leaves the conductor-ADDED token); one more fold pass requested

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-b-review-fold
PARENT_DISPATCH_ID: s5-b-impl
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-mechanisms
IN_REPLY_TO: .relays/s5/s5-b-review-fold/REVIEW-FOLD-REPORT-implementer-20260706-074050.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: fold verified at my seat (own battery green at a11e4bb; MF-2 load-bearing confirmed; MF-3 all six sentinels swept; MF-1 B-pick leg confirmed with RED-first) — MF-1b remainder: an absorb-raise that bounces persists the conductor-added gate_category markerless; 2-3 lines + one fixture leg, same FOLD_SCOPE surface

**Verified at this seat (not taken from your report):** worktree clean at `a11e4bb`; fold diff touches exactly the four FOLD_SCOPE files; own run `go build` + targeted suites + full `go test -count=1 ./...` (23 ok) + `go vet` — green. MF-2 is genuinely load-bearing now (the corrupted projection bytes are asserted on disk WHILE the replayed view stays canonical — the contention exists). MF-3 sweeps all six drain sentinels. MF-1's strip covers all five reject boundaries with the pick-restore, and the new bounce fixture is RED-first-evidenced.

**MF-1b — the narrow remainder (found by the targeted re-check, E1):**
`raiseGateCategory`'s absorb branch (`validate.go:108-113`, `hit && !picked`) sets `gate_category` to the detector member with NO `gate_category_pick`. On a subsequent bounce, `clearGateRaiseHeaders` (`submit.go:98-108`) finds `pick == ""`, restores nothing, deletes `gate_category_raised` + `gate_category_pick` — and the conductor-ADDED `gate_category` token survives on the persisted rejected record, markerless. The record then shows a category the seat never submitted, indistinguishable from a seat pick — the same dishonest-persisted-evidence class MF-1 closed, in its absorb instance. Inert for every current consumer (all gate on Accepted), same as before.

**Required change (fold-in-only, same scope):** a rejected record must persist with exactly the seat-submitted `gate_category` state — for the absorb case that means NO `gate_category` header at all. Implementation is your call; the obvious shape is to consult `gate_category_raised` before deleting it (raised `"yes"` ∧ no pick ⇒ the token was conductor-added ⇒ delete `gate_category` too — safe because DEF-2 rejects any lane-supplied `gate_category_raised` before the raise can run, so raised-at-reject-time is conductor-stamped by construction). Plus one FAIL-first fixture leg: absorb candidate (no `gate_category`) + detector hit + bad `PARENT_DISPATCH_ID` ⇒ persisted rejected record has NO `gate_category`, NO raise headers. Expected surface: `internal/engine/submit.go` (or `validate.go`), `test/fixtures/s5_gate_raise_test.go` — inside your existing FOLD_SCOPE universe; write a fresh FOLD_SCOPE in the fold report regardless.

**Noted, no action:** the pre-s5-a lane-forgeability of `gate_category_pick` (no registry row yet ⇒ DEF-2 can't guard it; a lane-supplied pick value could even steer the restore path on a bounce) remains fully covered by the REGISTERED integration-gate constraint — it dies the moment s5-a lands the row as computed. No code change now; the R-s5-6 ordering carries it.

After your MF-1b fold report: I re-run the targeted leg + battery, then file the pair's review-complete report to the orchestrator. No re-panel (no design/blast-radius change). Merge and T7 remain gated as before.

ACTIONS_GIT_REF: no edits claimed at this seat; own verification runs only; worktree `s5-b-mechanisms @ a11e4bb` clean (own check)
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
