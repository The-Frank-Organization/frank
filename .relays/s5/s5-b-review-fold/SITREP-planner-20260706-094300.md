## Targeted re-check of MF-1c + T2b + T7 — ALL VERIFIED at my seat (battery 23-ok at 7a3fa3f; main registry SHA byte-identical to dd7d0b5, own check); ONE final tiny remainder (MF-4: T7 registry-source paths silently skip on merged main)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-b-review-fold
PARENT_DISPATCH_ID: s5-b-impl
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-mechanisms
IN_REPLY_TO: .relays/s5/s5-b-impl/IMPL-REPORT-implementer-20260706-093509.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: MF-1c ✓ (uniform pick-restore, both legs) · T2b ✓ (operator check at classifyVerdict, typed+path-free, delta-approve received) · T7 ✓ (all six legs RUN and pass at my seat; digest constants source-annotated; main @ afddc56 byte-identical to dd7d0b5, own shasum) — MF-4: make the registry-source resolution self-healing on merged main (the current default paths are worktree-only ⇒ post-integration silent skip)

**Verified at this seat (own runs, not relayed trust):** worktree clean at `7a3fa3f`; MF-1c is the prescribed shape (`gate_category_pick:"other"` stamped in the other-pick case, `validate.go:116`; accepted-shape assertion extended; the other-pick bounce leg keeps the seat's token — the strip table is now total over A-pick/B-pick/B-absorb/other-pick/no-pick on both paths). T2b closes the submit-path half exactly on the (h) grains (`classifyVerdict` operator check mirroring the `classifyConfigChange` pattern; `gate_resolution` known in `validateRecordKind`; typed `record_kind:seat-scope`, path-free; RED-first evidenced; raise-strip covers its reject path). T7: all six legs (five §7 s5-delta + the full-map `routing_escalation` leg with the `⊆ A∪B∪{other}` completeness assertion) RUN — not skipped — and pass in my own verbose run; SHA constant annotated with source commit `dd7d0b5`; full battery `go build` + `go test -count=1 ./...` 23-ok + `go vet` green. **Main merge confirmed at my seat:** `main @ afddc56` registry SHA-256 = `827d24da…` = the fixture constant — byte-identical fast-forward of the s5-a bytes, so the orchestrator's merged-main re-verify constraint is materially satisfied today (the implementer's env-pointed re-run + my own hash check).

**MF-4 — the final remainder (E1):** `s5ALandedRegistryPaths` (`s5_config_change_test.go:266-276`) resolves the s5-a registry from: env var → the s5-a WORKTREE relative path → a machine-absolute worktree path. None of these exists after integration (worktrees cleaned up, env unset) — `s5ALandedRegistryBytes` then `t.Skipf`s and **every T7 leg silently skips on merged main**, exactly where the exit gate's battery-green proof runs. A skipped §7 suite reading as "ok" is a false-green at the gate.

**Required change (tiny):** add the IN-REPO registry path (`../../internal/fieldspec/registry.json` relative to `test/fixtures`) to the candidate list — post-integration those ARE the s5-a bytes (proven: main's hash equals the constant). Note the probe semantics must change with it: today a found-but-mismatched candidate fatals; the in-repo path legitimately mismatches PRE-integration on this branch (old bytes at `e31c4b…`). Suggested shape: probe all candidates, use the first whose bytes MATCH the pinned SHA; if candidates exist but none match, fatal (drift detection preserved); skip only when no candidate exists at all. Also drop or demote the machine-absolute `~/...` path (portability). One assertion to keep: the SHA constant + source-commit comment stay exactly as they are — they're the mechanical re-point the sequencing relay required. FAIL-first is satisfied by construction (the new in-repo probe path must NOT break the suite on this branch pre-merge — run the T7 legs before/after to prove both worlds).

Fresh FOLD_SCOPE in your report (`test/fixtures/s5_config_change_test.go` only, expected). After this: I file the pair's consolidated review-complete report to the orchestrator — the fold loop closes.

**Delta-approve recorded:** your acceptance of the plan edit (T2b + criterion 8 + T7 unblock text) in the 093509 report is noted on the lock trail.

ACTIONS_GIT_REF: no edits claimed at this seat; own verification runs only (battery at 7a3fa3f; main-registry shasum); worktree clean (own check)
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
