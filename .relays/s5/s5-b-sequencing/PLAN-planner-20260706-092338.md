## PLAN delta — sequencing folded: T2b (classifyVerdict operator-seat, acceptance criterion 8) added; T7 UNBLOCKED against dd7d0b5 read-only; one-line delta-approve requested with your next fold report; MF-1c remains first

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s5-b-sequencing
PARENT_DISPATCH_ID: s5-design-s5-b
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_LOCK_ID: s5-b-mechanisms-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s5-b-mechanisms-plan
BRANCH: s5-b-mechanisms
IN_REPLY_TO: .relays/s5/s5-b-sequencing/PLAN-orchestrator-planner-20260706-091736.md
FROM: s5-b.planner
TO: s5-b.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
SUBJECT: the orchestrator's sequencing relay (091736) folded into the plan of record — execute in this order: (1) MF-1c fold (outstanding), (2) T2b, (3) T7 against s5-a-registry @ dd7d0b5 read-only, (4) T9 refresh; delta-approve line requested in your next report

The plan of record `docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md` is updated (my edit, plan-owner surface) per the orchestrator's `s5-b-sequencing/PLAN-orchestrator-planner-20260706-091736.md`:

1. **T2b (new task; acceptance criterion 8):** `classifyVerdict` (`submit.go:216-245`) gains submit-path operator-seat enforcement for gate-resolution submissions (settled `resolves_gate` shape: operator-seat-scoped, paired with `gate_resolution` record_kind), on the (h) grains — typed reject, channel-keyed, envelope asymmetry untouched. Negative fixture: non-operator gate-resolution submission bounces at the submit path (not merely un-rendered). FAIL-first. This is an acceptance sharpening inside the granted engine surface — not new scope; the orchestrator's relay is the authority; **include a one-line delta-approve of the plan edit (T2b + criterion 8 + the T7 unblock text) in your next fold/IMPL report** so the lock trail stays clean.
2. **T7 UNBLOCKED:** s5-a is accepted at the orchestrator's gate at `s5-a-registry @ dd7d0b5`. Consume that branch **READ-ONLY** — registry bytes + expected digests only; never check out/merge it into ours, never edit it. Every hard-coded digest in the T7 legs must note its source commit (`dd7d0b5`) in a comment so the re-point to merged main is mechanical. Final assertions re-verify against merged main before our integration (orchestrator relays any pre-merge byte delta).
3. **Unchanged:** m-6 confirm still gates ③ INTEGRATION at the orchestrator's seat; M-4 gates only the optional archive leg; F2 conditions, SCOPE_DIFF universe (registry.json/registry_test.go OUT), and the OUT list stand.

**Execution order at your seat:** MF-1c first (my `s5-b-review-fold/SITREP-planner-20260706-081048.md` — still outstanding; the review-fold loop closes before new build work lands on top), then T2b, then T7 (the §7 s5-delta five legs + the full-§J2-map ③ leg incl. `routing_escalation` + the T7 carry: the `gate_category ⊆ A∪B∪{other}` completeness assertion), then the T9 closeout refresh (fresh battery, forbidden-claim sweep over the new surfaces, consolidated implementation report). All under the standing `s5-b-impl` dispatch: T2b/T7 are plan tasks under your existing implementation authority; the MF-1c fold keeps the FOLD_SCOPE discipline. Battery green + `s5-b:` commit per task; blockers/OUT-needs escalate as always.

Not authorized by this relay: merge (operator's), registry edits, ③ live wiring (`cmd/*`), scope beyond the locked plan + this orchestrator-directed delta. No dispatch token is present here — your implementation authority is the standing `s5-b-impl` dispatch.

ACTIONS_GIT_REF: edited `docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md` (T2b inserted; T7 gating text replaced with the unblock + constraints; acceptance criterion 8 appended) + this relay + INDEX row; no code edit. git status --short = `?? docs/sprints/2026-07-06-s5-slice-5/`
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/
