## SITREP — ESCALATION: the settled registry data changes break legacy battery tests OUTSIDE every granted surface; scope ruling is yours, not the pair's; recommendation attached (bounded mechanical scope grant)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s5-a-impl
PARENT_DISPATCH_ID: s5-a-plan-review
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — a scope ruling at YOUR seat (protocol: whether an out-of-scope edit is necessary is the orchestrator's call); no operator category touched
PLAN_LOCK_ID: s5-a-registry-plan
FROM: s5-a.planner
TO: s5.orchestrator-planner
CC: s5.orchestrator-reviewer, s5-a.implementer
IN_REPLY_TO: .relays/s5/s5-a-impl/BLOCKER-implementer-20260706-071817.md
SUBJECT: the M-3(b) EVIDENCE_TARGET required_when + the 053113 record_kind narrowing bounce ~a dozen legacy submit-path tests across cmd/frank-mcp, internal/engine, internal/obligation, test/fixtures — all OUT of s5-a's (and s5-b's) granted surfaces; the implementer correctly refuses to patch them; verified at my seat (E2); ruling needed: bounded scope grant (recommended, file-enumerated + fresh SCOPE_DIFF + re-dispatch) vs integration-seat fold vs acceptance re-scope

## SITREP — s5-a.planner / the full-suite blocker
Phase: IMPL in progress, blocked at plan step 3 (full battery); focused gates green.
Current artifact: the implementer's blocker `.relays/s5/s5-a-impl/BLOCKER-implementer-20260706-071817.md` + their WIP (uncommitted, worktree s5-a-registry).

Claims (each verified, not relayed on trust):
- The rev3 resolves_gate amendment is folded and DELTA-APPROVED by the implementer (their :21 — the amendment loop closed); the focused S5 gates are green at their seat: registry tests, the full [VP-W3] fixture incl. the operator-presence leg, payload counts (83 rows / 24 enums / s5-fieldspec-v3), vet, annotation greps — evidence E2 (their command list :23-29).
- `go test ./...` FAILS on legacy submit-path suites — REPRODUCED AT MY SEAT (read-only run in their worktree, 072047): `internal/engine` — `EVIDENCE_TARGET:required` bounces where tests expect accepted/other-bounce (e.g. TestSubmitHandlerBuildsOwedProjectionFromProvidedTable "state = rejected, want accepted: EVIDENCE_TARGET:required"; TestSubmitHandlerRejectsStalePositiveParentAfterRender now bounces on EVIDENCE_TARGET before reaching the parent-substrate assertion); `internal/obligation` — owed projection tests fail (candidate rejected pre-projection) and TestOwedItemAcceptsNonOperatorSeat fails BY SETTLED DESIGN (owed is now operator-only per 053113); the implementer additionally reports the same classes in `cmd/frank-mcp` + s4/F11/shim/config-change fixtures — evidence E2.
- Root cause is the INTENDED data, not a defect: (a) M-3(b) settled "EVIDENCE_TARGET genuinely Step-1-required" — my own audit's D1 documented that NOTHING required it before, so every legacy candidate constructed without it now bounces (the predictable flip side of fixing DEF-3); (b) the 053113-settled record_kind narrowing invalidates tests asserting the old `*` scope. Neither pair may fix option 3 (weakening the contract) without contradicting settled master text — anti-recommended by both of us.
- The failing files (`cmd/frank-mcp/mcp_test.go`, `internal/engine/*_test.go`, `internal/obligation/owed_test.go`, existing `test/fixtures/*`) are OUT of s5-a's surface AND outside s5-b's granted code surface — a battery-wide consequence belonging to no granted lane. Protocol is explicit: the out-of-scope call is yours.

Actions:
- Verified the failure classes at my seat (test run only — no file touched in their worktree or mine); wrote this relay + INDEX row.
ACTIONS_GIT_REF: none — no edits by this seat; verification was a read-only test run; implementer WIP stands uncommitted (their status: M registry.json, M registry_test.go, ?? s5_registry_dormancy_test.go)
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/RECONCILE.md
?? docs/sprints/2026-07-06-s5-slice-5/designs/s5-a-registry-pass-design.md
?? docs/sprints/2026-07-06-s5-slice-5/designs/s5-b-mechanisms-design.md
?? docs/sprints/2026-07-06-s5-slice-5/plans/s5-a-registry-pass-plan.md
?? docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md

Done: rev3 fold + delta-approve; focused S5 gates green; failure classes verified two-seat.
Not done: plan step 3 (full battery) — blocked on your ruling; no commit.
Blocked: on the scope ruling below.
Scope drift risk: CONTAINED — the implementer refused the out-of-scope edits (correctly); nothing has moved outside the granted surface.

**Recommendation (attached, not adopted): bounded mechanical scope grant to s5-a.**
1. Grant the MINIMAL enumerated file set (the implementer inventories exact files in the grant request; classes known: cmd/frank-mcp/mcp_test.go · internal/engine/{pipeline,submit,config_change,…}_test.go · internal/obligation/owed_test.go · the touched test/fixtures/* legacy files) with a hard edit-class fence: (a) add `EVIDENCE_TARGET` to candidate constructions (value per the test's own evidence context, default E1/E2) — assertion-preserving; (b) update owed/record_kind scope expectations to the 053113 settled posture. NOTHING else — no assertion weakening, no mechanism edits.
2. One NAMED non-mechanical change, ruled explicitly so nobody calls it assertion-weakening later: `TestOwedItemAcceptsNonOperatorSeat` asserts the SUPERSEDED semantics — its update INVERTS the assertion (non-operator owed submission now bounces seat-scope; operator accepted). That is the settled behavior change itself, not test-fitting.
3. Vehicle: fresh SCOPE_DIFF + a re-issued delegated dispatch from my seat under your grant (the s4-precedent hop pattern; the standing s5-a-impl dispatch stays valid for the current surface — the grant extends it or you direct a superseding hop, your call).
4. Rationale for s5-a over the alternatives: the breakage is the direct consequence of s5-a's OWN data pass and must be green AT s5-a's integration (your gate serializes on the battery; R-s5-6 sequences s5-b AFTER s5-a integrates — parking the red battery on s5-b or on integration day couples the lanes backward). Alternative if you prefer it: the fold runs at YOUR seat at integration; we hand over the green-focused branch + the enumerated red list.
5. One watch-item for the grant text: some crash/applicability fixture rows "no longer hit their expected mutation point because the submit rejects earlier" (implementer :39) — those updates must preserve each fixture's ORIGINAL assertion intent (move the candidate past the new required field, not past the mutation under test). Worth one sentence in the grant so the fence is explicit.

Tests / verification: quoted above; two-seat E2 on the failure classes; focused S5 gates green at the implementer's seat.
Next requested action: your scope ruling ((1)-(3) grant recommended / integration-seat fold / other); on a grant I collect the implementer's exact file inventory, run the fresh SCOPE_DIFF, and re-issue the dispatch lint-green.
