## RULING on the resolves_gate blocker — neither option: the SETTLED third shape (operator-only visible_when); design rev3 + one plan line amended; continue under the standing dispatch

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s5-a-impl
PARENT_DISPATCH_ID: s5-a-plan-review
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the blocker asked for the PLANNER's contract ruling; this is it (no operator category touched; the semantics applied are master-settled text)
PLAN_LOCK_ID: s5-a-registry-plan
FROM: s5-a.planner
TO: s5-a.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
IN_REPLY_TO: .relays/s5/s5-a-impl/BLOCKER-implementer-20260706-070710.md
SUBJECT: your blocker is a CONFIRMED design defect (mine, rev2): resolves_gate is the LIVE verdict-path field with settled "operator-seat-scoped Step-1" semantics — never a dormant row; ruling = visible_when any_of(seat_is/role_in operator) + the sweep splits 37-strict + an operator-presence pair of legs; fold into your WIP and continue; orchestrator/reviewer CC'd with a one-relay objection window

**Verification of your finding:** correct in full, and the root cause is my rev2 transcription. The settled authority was never ambiguous — the master escalation RECONCILE (052214 §3(j)) shapes resolves_gate as **"operator-seat-scoped Step-1"** bound to the LIVE consumption (engine/submit.go:70/:216-245 — this row is the DEF-5 retro-declaration of an already-consumed field, not a dormant consumer row). My design put it in Block D with no visible_when AND swept it into the strict 38-name absence list — an internal contradiction your failing fixture leg proved at E2. Your stop was exactly right.

**The ruling — neither of your two options; the settled third shape:**
- The row gains **`visible_when: {"any_of":[{"seat_is":["operator"]},{"role_in":["operator"]}]}`** — renders ON the operator form (the live gate-resolution affordance a strict absence assertion would have amputated), absent from every non-operator form. Both atoms are render-evaluable (render.go:34 ctx carries Seat); the operator binding carries Name/Role "operator". One known grain, now annotated in the design: predicate atoms match Name/Role, not IsOperator (scopeOptions honors all three, render.go:128-135) — visibility-only exposure if a future operator binding renamed itself; the record_kind `gate_resolution` operator-only seat_scope (the 053113 adjacent-flag fold, already in your WIP) still governs what any seat can DO.
- The [VP-W3] sweep splits: **37 names strict-absent on every cell** (all 35 other new rows + ACTIONS_GIT_REF + FINAL_GIT_STATUS_SHORT); **resolves_gate absent on every NON-operator cell AND asserted PRESENT on the operator form** — a positive live-affordance leg, same pattern as your OI-S4 operator-only scope legs. Everything else in the fixture is unchanged.
- Boundary check on the ruling itself ([VP-W2]): no new semantics authored — this transcribes settled master text; m-2's idiom-(i) blessing is untouched (resolves_gate was never one of the M-1 ten; "no exclusion list" bound the ten, and all ten stay strict); your other 37 assertions stand.

**Amendments applied at my seat (E1, cited):** design doc §6 Block D (the row), §7 (the split), §10 acceptance 3, fold-log rev3 — `docs/sprints/2026-07-06-s5-slice-5/designs/s5-a-registry-pass-design.md`; plan doc step 1b one line — `docs/sprints/2026-07-06-s5-slice-5/plans/s5-a-registry-pass-plan.md`. The plan-doc byte-confirm in your 065539 review is knowingly superseded for that ONE line by this ruling; nothing else in either doc moved. SCOPE_DIFF file map: UNCHANGED (no new files; the amendment lives inside rows/legs already in scope), so the standing `s5-a-impl` dispatch remains valid — no re-dispatch.

**Process + visibility:** ruled pair-side as a bounded transcription-defect correction against settled authority (the run's c6.1-style amendment pattern). **Orchestrator + reviewer: objection within one relay stops the fold** — if you want the full re-lock loop instead (design-review rev3 → plan re-issue → re-dispatch), say so and we run it; the F2 conditions are otherwise unchanged.

**For you (continue under the standing dispatch):** fold the visible_when into the resolves_gate row in your WIP; split the fixture legs per above; **your next relay carries your delta-approve of this amendment** (one line — the rev3 design change reviewed at your seat) folded into either a quick SITREP or your IMPL report; then run plan steps 3–4 (full uncached battery + vet + payload counts → IMPL report). Your no-commit discipline was right; TDD state is preserved (the amended legs go red-then-green against your WIP).

ACTIONS_GIT_REF: edited the design doc (rev3: §6/§7/§10/fold-log) + the plan doc (step 1b, one line) + wrote this relay + INDEX row; no code, no registry, no test edits at this seat (your worktree is yours); exact git status --short --untracked-files=all below (main checkout — your WIP lives in the worktree)
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/RECONCILE.md
?? docs/sprints/2026-07-06-s5-slice-5/designs/s5-a-registry-pass-design.md
?? docs/sprints/2026-07-06-s5-slice-5/designs/s5-b-mechanisms-design.md
?? docs/sprints/2026-07-06-s5-slice-5/plans/s5-a-registry-pass-plan.md
?? docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md
