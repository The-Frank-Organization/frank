## AUDIT dispatch — pair s5-a "registry & rows" (re-issued as a file relay per s5-resume; all fidelity holds SETTLED)

ROLE: Orchestrator Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: s5-audit-s5-a
PARENT_DISPATCH_ID: s5-resume
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: s5.orchestrator-planner
TO: s5-a.planner, s5-a.implementer
CC: s5.orchestrator-reviewer
IN_REPLY_TO: .relays/s5/s5-resume/PLAN-orchestrator-planner-20260706-034602.md
SUBJECT: paired INDEPENDENT read-only audits — live FieldSpec registry vs the four consumer domains' settled field sets; duplicate gate heavy; dormancy idiom per row; [VP-W3] fixture surface; OI-S4-TOKEN-SCOPE row; the routing_escalation delta (hold LIFTED) + attestation_source (O-2) now IN scope

**Mode change first.** The frank transport is stood down (see `.relays/s5/s5-resume/PLAN-orchestrator-planner-20260706-034602.md` + `master-docs/master/TRANSPORT-FINDINGS-2026-07-06.md`). Do NOT retry the frank MCP tools. Transport = file relays under `.relays/s5/`, the upstream relay discipline, `relay-lint` before every handoff, operator hand-relay. Your onboarding-pointer relay (frank relay-f38bc5a8/relay-9d80b50f) stands; this file supersedes the frank-held AUDIT dispatch.

**Who you are.** s5-a is the registry pair of the s5 slice-team (Slice-5 = consumer schemas, the LAST Step-1 slice). Your bundle, master-approved: SOLE registry.json writer — the four consumer domains' FieldSpec rows + dormancy predicates + the [VP-W3] negative dormancy fixture + the OI-S4-TOKEN-SCOPE row fold + the routing_escalation named_enums delta (hold LIFTED — exact delta below) + `attestation_source` (new, O-2). Pair s5-b owns engine/bounce/migrate/test-replay exclusively; the battery serializes at my gate.

**Read before auditing (both seats, independently):**
1. `.relays/s5/s5-fidelity/RECONCILE-orchestrator-planner-20260706-034602.md` — EVERY Q1–Q11 settled; it is your semantics authority for this audit. Key deltas vs your onboarding pointer: owed-#3 GRILL_REQUIRED is EMPTY both legs (confirm-only now); routing_escalation hold LIFTED; no Step-1 slot_in writer; scope_paths STRUCK (optional dormant home, your discretion, never live-required); authority_class is a BOOL; mixed-edge ⇒ held; no live record_kind widening; model_name = m-2/m-6-declared typed exempt-marked field (m-3 certifies three scan-side properties); the Q9 persisted subset = the m-3 §3.1 write-allowlist EXACTLY (predicate_result + veto NOT persisted; rank1_recommended_bucket derived, not persisted); authority_ceiling = type:object reserved-shape, the ceiling object ITSELF Step-1 F2-recordable; external_send NOT declared; accepts_interjection + template schema = m-5 config, not rows.
2. `.relays/s5/s5-resume/PLAN-orchestrator-planner-20260706-034602.md` — the mode change + adapted exit gate ([VP-W7] §7 → fixture legs; dogfood record closed early; everything else stands).
3. The s5-dispatch scope of record + my onboarding findings: frank store archived — the operative content is restated in this dispatch and the two files above; if you need the originals, ask me (the archive at `~/frank-archives/frank-team-store-s5-dogfood-20260706` is operator-held evidence — do NOT open it yourselves).
4. Design-of-record (read-only) under `master-docs/master/`: ARCHITECTURE.md §C4 (controlling per [VP-W4]) + §J2 (the landed routing_escalation A-member, 2026-07-06 annotation); m-2 doc §4/§5/§12 (row grammar — your grammar authority); m-3 §3/§5/§6; m-4 §5/§9/§10; m-5 §3/§7; m-6 §5.
5. Code @ the repository root, `main @ 67ee23e`: internal/fieldspec/{registry.json,registry.go,predicate.go,validate.go,render.go}; test/fixtures/ (F-GATE-2 owed-rows precedent; s4_config_change_test.go).

**Audit questions (each seat answers ALL, independently — do not coordinate; with file:line evidence):**
1. **Row inventory (duplicate gate — HEAVY).** For every consumer-domain field the settled answers name (m-3: the §3.1 write-allowlist set + evidence_integrity/record_integrity/achieved_evidence/target_gap_result/executable_claim_results/egress_scan_result/degradation_notes + attestation_source (O-2) + authority_class(bool); m-4: routing_decision columns — routing_assignments row_array grain, justified_deviation, deviation_reason_code (config-sourced slot), capability_prior_snapshot, template_ref, constraints, outcome_feedback_ref, the §9 observed atoms deviated_observed/bucket_binding_observed/declared_bucket; m-5: slot_in/seat_archetype/authority_ceiling reserved-shape; m-6: away_bridge_eligible + the ODB slots incl. the typed exempt-marked model_name): is a row ALREADY in the live registry.json (47 rows at 67ee23e — GRILL_REQUIRED and, per the settled answers, gate_category_raised (:93) already are), ABSENT, or PRESENT-but-divergent? Deliver a field-by-field table: field · owner-domain · status(exists/absent/conflict) · proposed row (all 12 grammar columns) · citation. 4-bucket verdict per §C4-owed item touched.
2. **The routing_escalation delta (verify-only, exact per m-2):** `named_enums.gate_category_A` append `"routing_escalation"`; `named_enums.gate_category` insert `"routing_escalation"` BEFORE `"other"` (other stays last); NO `gate_category_B` change; NO FieldSpec-row change (gate_category :92 / gate_category_raised :93 keep their enum_set refs). Verify the named-enum structures at those locations support exactly this delta; verify byte-distinctness from `routing_unavailable` (which must NOT be added) and from `human_decision_required`. Classification MINOR/additive.
3. **Dormancy treatment per row.** Assign each absent row one of the three idioms — `layer_present:observe` conjunction (live precedent: ACTIONS_GIT_REF/FINAL_GIT_STATUS_SHORT), reserved-shape-no-values (m2d §12/15: seat_archetype/authority_ceiling/constraints/template_ref/slot_in), config-sourced slot (gate_category precedent — applies to deviation_reason_code) — and flag any row fitting none as an escalation to me. Confirm each dormant row gates required_when AND visible_when the same way (the validate.go:31-39 vs render.go trap).
4. **Grammar traps.** Confirm: no proposed consumer row is owner:system-unvalidated by accident (validate.go ignorePayloadField); record_kind_in atom preferred where record-kind-gating applies (F-GATE-2 precedent); gate_referenceable defaults false + model_identity⇒false enforced (registry.go:162) — model_name gets model_identity:true; all m-5 rows gate_referenceable:false (m5d:236).
5. **[VP-W3] negative dormancy fixture surface.** Enumerate every Step-1 rendered form surface (seat × phase × tier render paths) the fixture must sweep, and the four legs (m2d AC17/NF-S5): (a) observe-owned requireds step-gated off; (b) non-observe requireds STILL block; (c) EVIDENCE_TARGET still Step-1-required; (d) layer_present never references model. The bar: enumerate the consumer-owned fields under test and prove each ABSENT from EVERY Step-1 rendered form surface, not merely un-required on one happy path.
6. **OI-S4-TOKEN-SCOPE row.** Audit record_kind's seat_scope `*` list (genesis + owed tokens currently fillable by any seat); state the two m-2 options the s4 RECONCILE names and which the registry data supports; the fold rides your registry pass under one m-2 confirm (via me → master, file mode).
7. **Registry-version label + §7 fixture-leg shape (s5-b boundary).** Confirm the version label (registry.json `version: s3-fieldspec-v2`) and propose its bumped value; state what your row additions imply for the §7 config_change FIXTURE legs (adapted [VP-W7]: operator-authorized record shape, old→new digest, no re-genesis, phase-0 genesis-chain acceptance, stale-form re-render + re-rendered success — against TEST stores; the live-store leg moved to the transport-fix relaunch). You do NOT build §7 legs (s5-b surface); you own the registry content they carry.

**Escalation rule (unchanged):** any NEW semantics question or idiom-less row → file relay TO s5.orchestrator-planner (CC s5.orchestrator-reviewer) under `.relays/s5/<your-dispatch>/`; m-x routing stays hub-and-spoke via master (now file relays via the operator). Never self-author semantics ([VP-W2]).

**OUT of scope (escalate before touching):** any code edit (read-only audit); engine/bounce/migrate/test-replay (s5-b's); the step-(d) away-bridge set (§C4 carry ledger controlling, [VP-W4]); the archived dogfood store; ANY transport-fix pre-work (the lineage/parenting/codec fixes are the next master cycle — do not patch the conductor); observe/routing/archetype EXECUTION (Step 2/3/4+).

**Boundary contract (s5-a writes / s5-b reads):** Writes: registry.json rows + named_enums delta + the dormancy fixture (s5_registry_*_test.go). Reads: the settled field semantics. Target entity: the FieldSpec registry (landed via §7 fixture legs at build time). Downstream consumer: s5-b's ③/⑤/I-PH fixtures consume your row ids/enums; Step-2 observe consumes the declared fields. Contract: exact 12-column rows per m-2 grammar. Proof: E2 fixtures at exit. No-consumer action: defer (canonical-iff-consumed — flag, don't add).

**Deliverable (each seat separately):** an AUDIT report relay-linted at `.relays/s5/s5-audit-s5-a/AUDIT-<role>-<YYYYMMDD-HHMMSS>.md` + a tracked copy of the reconciled inventory later at `docs/sprints/2026-07-06-s5-slice-5/audits/` (I fold after reconciling both seats). Include: the field-by-field table, 4-bucket verdicts, the delta verification, grammar-trap findings, fixture-surface enumeration, FINAL_GIT_STATUS_SHORT, every claim E1+ with file:line. IMPL worktree later: branch `s5-a-registry` off `main @ 67ee23e` (operator sets up; audit against the main checkout).

Not authorized by this relay: DESIGN, PLAN, IMPL, any edit, any merge. No dispatch token is present here.

ACTIONS_GIT_REF: none — read-only AUDIT dispatch; no code/git action authorized or taken
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ on main @ 67ee23e; .relays/ substrate gitignored)
