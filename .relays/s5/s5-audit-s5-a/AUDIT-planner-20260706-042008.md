## AUDIT — s5-a.planner: live FieldSpec registry vs the four consumer domains' settled field sets (boot ack folded in)

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: s5-audit-s5-a
PARENT_DISPATCH_ID: s5-resume
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: s5-a.planner
TO: s5.orchestrator-planner
CC: s5.orchestrator-reviewer
IN_REPLY_TO: .relays/s5/s5-audit-s5-a/AUDIT-orchestrator-planner-20260706-035446.md
SUBJECT: s5-a.planner AUDIT — all 7 questions answered; every consumer-domain field ABSENT from the live 47-row registry (grep-clean sweep); routing_escalation delta verified supportable + byte-distinct; two live code traps found (system-owned headers pass submit unvalidated; gate_category=other self-bounces via the "true"-vs-bool-enum stamp); four idiom escalations flagged; OI-S4-TOKEN-SCOPE = registry data supports owner-typing today, owed-id-picker needs engine support

### 0. Boot ack (s5-boot-s5-a-planner folded here per the boot relay)

- Identity: **s5-a.planner**, Planner seat of pair s5-a "registry & rows", RUN_ID s5, file-relay transport. No frank MCP tool touched; the archived dogfood store not touched.
- Skill loaded: `agent-pair-planner` + its `protocol.md`, in full.
- Read, in order: the team charter (`master-docs/CLAUDE.md`); `.relays/s5/s5-resume/PLAN-orchestrator-planner-20260706-034602.md` (mode change + adapted exit gate); `.relays/s5/s5-fidelity/RECONCILE-orchestrator-planner-20260706-034602.md` (Q1–Q11 settled — my semantics authority); my AUDIT dispatch `.relays/s5/s5-audit-s5-a/AUDIT-orchestrator-planner-20260706-035446.md`.
- Design-of-record read: `ARCHITECTURE.md` §C4/§C4.3 (incl. I-PH) + §J2 (the landed 2026-07-06 routing_escalation annotation, ARCHITECTURE.md:105-133); m-2 design §1–§18 in full (grammar authority); m-3 §1–§15; m-4 §2/§5/§7/§8/§9/§10; m-5 §3–§7; m-6 §2–§8. Code @ `frank/` `main @ 67ee23e`: internal/fieldspec/{registry.json, registry.go, predicate.go, validate.go, render.go, fieldspec.go}, internal/engine/submit.go, internal/seat/binding.go, internal/bounce/formatter.go, internal/migrate/migrate.go, cmd/frank/main.go, cmd/frank-mcp/mcp.go, test/fixtures/ (incl. s4_config_change_test.go), internal/fieldspec/*_test.go.
- Independence: no coordination with s5-a.implementer; no sibling relay read.

### 1. Q1 — Row inventory (duplicate gate — HEAVY)

**Registry ground truth:** 47 rows at `internal/fieldspec/registry.json` (counted mechanically: fields array length 47; 10 envelope + 13 header/body + 24 lineage/gating rows), version `s3-fieldspec-v2` (:2), 14 named enums. The 12 m-2 grammar columns (m-2 §4:50-67): id · layer · owner · type · enum_set · gate_referenceable · seat_scope · required_when · visible_when · fill_constraints · consumers · lineage_role — all representable by the live `FieldSpec` struct (registry.go:26-41; plus the live extra `model_identity` bool :34 and inline `options` :32).

**Headline: a grep of every consumer-domain field id named by the settled answers returns 0 hits in registry.json** — the four consumer field sets are wholly ABSENT except the rows the settled answers already said exist. Byte-distinctness precondition for Q2 also holds: `routing_escalation`, `routing_unavailable`, `human_decision_required` each appear 0 times.

**EXISTS (confirm-only; do not rebuild):**

| field | status | evidence |
|---|---|---|
| GRILL_REQUIRED | exists, conforming | registry.json:123 — bool, agent_enum_pick, monotonic, gate_referenceable true, consumers [form_renderer, form_validator, human_surface, lineage_engine]. Owed-#3 leg 1 confirmed EMPTY (RECONCILE Q1) |
| GRILL_LOCK_ID | exists, conforming | registry.json:124 — id_ref, lineage_role lock_id, required_when {all_of: [GRILL_REQUIRED==yes, DESIGN_LOCK_ID present]}. Owed-#3 leg 2 confirmed EMPTY. Fixtured (registry_test.go:51+) |
| gate_category | exists | registry.json:92 — enum_set gate_category, monotonic, gate_referenceable true |
| gate_category_raised | exists | registry.json:93 — owner computed, type bool, enum_set bool, computed_result, no seat_scope, gate_referenceable absent (false). Matches the Q5 settled shape (conductor stamps; stamping wired at validate.go:61-66). See trap T2 below |
| ACTIONS_GIT_REF / FINAL_GIT_STATUS_SHORT | exist | registry.json:111-112 — required_when {all_of:[{layer_present: observe}]} — the live idiom-A precedent. Note: owner agent_enum_pick + observed_value (Step-1 seat-filled); m-3 §12 types them owner:system at Step-2 — an ownership handover the Step-2 fold will owe, not s5 |
| EVIDENCE_TARGET | exists | registry.json:90 — agent intent, gate_referenceable true. See divergence D1 below (no live required_when at all) |
| delivery_state / failing_edge | exist | registry.json:95-96 — owner system, computed slots; delivery_state enum byte-exact {accepted, rejected, held} (registry.json:69, asserted registry_test.go:48) |
| HUMAN_GATE_REQUIRED | exists | registry.json:91 — monotonic bool, the MAX-raiser floor mechanism live (render.go:85-96, validate.go:58-60) |
| record_kind | exists | registry.json:113 — closed system enum, 7 members, exhaustive consumer validateRecordKind (engine/submit.go:124-160). Q6 scope below |
| slot_in (predicate atom) | exists as RESERVED ATOM only | predicate.go:36, :155-165 — a non-empty slot_in value list is load-REJECTED ("slot_in is reserved for Step-1"). No FieldSpec row. Matches RECONCILE Q3 (no Step-1 writer, reserved-shape) |

**ABSENT (the s5-a build surface) — proposed rows, all additive, every claim E1 vs the named doc lines.** Grammar-column shorthand: layer header unless noted; consumers per the owning doc; lineage_role none unless noted. Idiom column = Q3 assignment (A = layer_present:observe conjunction; B = reserved-shape-no-values; C = config-sourced slot; ESC = fits none, escalated §3).

**m-3 set (Q9 §3.1 write-allowlist exactly; + O-2):**

| field | proposed row (12-col essentials) | idiom | citation |
|---|---|---|---|
| achieved_evidence | owner system · enum · NEW named_enum achieved_evidence [E0,E1,E2,E3,E4] · fill observed_value · gate_ref false · required_when + visible_when both {layer_present: observe} | A | m-3 §3:50, §4:82-86. NOTE E0 is in range — the existing EVIDENCE_TARGET enum (E1–E4) cannot be reused |
| target_gap_result | owner system · enum · NEW named_enum [met, target_gt_achieved, not_applicable] · observed_value/computed_result · gate_ref false | A | m-3 §3:51, §4:82 |
| evidence_integrity | owner system · object (per-field map; value vocabulary = NEW named_enum [observed, self_reported], two-value, R3-locked) · observed_value · gate_ref false | A | m-3 §3:52, §6:120-122 |
| record_integrity | owner system · enum · NEW named_enum [observed, self_reported, mixed] · computed_result · gate_ref false | A | m-3 §6:123; m-2 §17.6:344 |
| executable_claim_results | owner system · row_array (per-claim result ∈ pass/fail/skipped/unsafe) · observed_value · gate_ref false | A | m-3 §3:54 |
| egress_scan_result | owner system · enum · NEW named_enum [pass, blocked, not_applicable] · observed_value · gate_ref false | A | m-3 §3:55, §7 |
| degradation_notes | owner system · text · observed_value · gate_ref false | A | m-3 §3:56 |
| attestation_source (O-2) | owner system · enum · NEW named_enum [conductor, operator] · observed_value · gate_ref false — a provenance marker, NOT a third evidence_integrity value | A | RECONCILE Q9(2); m-3 §4:86, §13:222 |
| authority_class | owner computed · type bool · enum_set bool · computed_result · gate_ref false — a BOOL per the m-2 correction (RECONCILE Q9(4)), = record_kind ∈ authority-set ∨ gate_category ∈ A; never model-keyed | A | m-2 §17.6:345; m-3 §3.2(d):63 |
| deviated_observed | owner system · bool · enum_set bool · computed_result · gate_ref false (NEVER gate-referenced — the silent-deviation veto reads it observe-side only) | A | m-2 §17.3:312; m-4 §2:88-96 |
| bucket_binding_observed | owner system · bool · enum_set bool · computed_result · gate_ref false | A | m-4 §9:331-332; m-3 §9:154 |
| predicate_result, veto | NO ROWS — not persisted (internal control-flow resolving to the terminal token) | — | RECONCILE Q9(1) |
| rank1_recommended_bucket | NO ROW — derived, not persisted | — | RECONCILE Q9(3) |

**m-4 set:**

| field | proposed row | idiom | citation |
|---|---|---|---|
| routing_assignments | owner seat_scoped_enum · row_array · gate_ref TRUE (required so the any_row predicate can name it — predicate.go:145 validates the ARRAY id; precedent SCOPE_DIFF registry.json:106) · seat_scope planner/orch-planner (+ operator conditional — INEXPRESSIBLE, see T3) | ESC | m-4 §5:208; m-2 §17.3:309 |
| justified_deviation | owner free_text · text · required_when {any_row: routing_assignments.declared_deviated, op ==, value true} | ESC (render-visibility) | m-4 §5:210; m-2 §5:99 |
| deviation_reason_code | owner agent_enum_pick · enum · same any_row required_when grain · value-set = config-sourced (PLACEMENT QUESTION Q-a below) | C | m-4 §5:211; RECONCILE Q8 |
| capability_prior_snapshot | owner system · object · computed_result · gate_ref false | A-adjacent (system, never rendered) | m-4 §4:190-192, §5:209 |
| routing_record_kind | owner agent_enum_pick · enum · own NEW named_enum [routing_decision] (+ reserved routing_deviation/routing_update NOT declared — a later release) | B | m-4 §5:199-207 |
| constraints | owner agent_enum_pick/free_text · object · reserved-shape, no values, non-gate | B | m-4 §5:212; m-2 §17.3:316 |
| template_ref | owner system · id_ref · set-when-template-spawned/null-otherwise · lineage_role none | B | m-4 §5:214; m-2 §17.3:317 |
| outcome_feedback_ref | owner system · id_ref · null-reserved for a later release · lineage_role none | B | m-4 §5:213; m-2 §17.3:318 |
| declared_bucket, chosen_model, pin_mode, declared_deviated, seat, role, task_tag | COLUMNS of routing_assignments rows, not top-level rows — the live grammar has no column specs (T3). chosen_model carries model_identity true at whatever grain it lands | ESC | m-2 §17.3:309; m-4 §5:208 |

**m-5 set:**

| field | proposed row | idiom | citation |
|---|---|---|---|
| slot_in (field home) | owner system · type string (opaque tag) · reserved-shape, NO values, conductor-classified at Step-2 acceptance · gate_ref false | B | m-3 §5.1:114-116; m-5 §3.1:52-54 (values stay m-5 config, NOT registry) |
| seat_archetype | reserved-shape opaque; per-assignment carriage is a routing_assignments COLUMN (T3); if a top-level home is wanted it is owner system (conductor-stamped per m-5-F1) · string · no values · gate_ref false (m-5 rows all gate_ref false, m5d:236 discipline) | B/ESC | m-2 §17.3:309; m-5 §3.2:56-58 |
| authority_ceiling | type object reserved-shape OPEN named-axis map {write, dispatch, tool} · owner system (conductor-stamped) · no schema-enumerated axis values · gate_ref false. The ceiling object ITSELF is Step-1 F2-recordable (RECONCILE Q10) — reserved-shape must not forbid a carried value | B/ESC | m-5 §5:82-97; m-2 AC15:240 |
| accepts_interjection, template schema | NO ROWS — m-5 archetype-registry CONFIG, not FieldSpec | — | RECONCILE Q10; m-5 §7:118-119 |

**m-6 set:**

| field | proposed row | idiom | citation |
|---|---|---|---|
| away_bridge_eligible | owner computed (m-6 policy-derived) · bool · enum_set bool · computed_result · gate_ref false. Row home only — the step-(d) away-bridge MECHANISM stays OUT ([VP-W4]) | A-adjacent (computed, never rendered) | m-6 §4:92 |
| completed_proof | owner system · evidence_ref · observed_value (m-3-sourced, never agent free-text) · gate_ref false | A | m-2 §17.2:292 |
| subject_ref | owner system · id_ref · system_only | B | m-2 §17.2:296 |
| model_name | owner system · string · **model_identity: true** (⇒ gate_referenceable false enforced at load, registry.go:162-164) · consumers [egress_gate, human_surface] · the typed exempt-marked ODB field. m-2/m-6 declare; m-3 certifies conductor-generated · non-lane-writable-via-tool-surface · typed-identifiable — see T1 for the honest limit of "non-lane-writable" today | A-adjacent (system) | m-2 §17.2:297; RECONCILE Q6; m-3 §7:135 |
| plain_language_change, why_now, tradeoffs_risks, recommendation | owner free_text · text — agent-fillable ODB slots | ESC | m-2 §17.2:290-294 |
| choices | owner agent_enum_pick · row_array | ESC | m-2 §17.2:295 |
| decision_deadline | owner system · string (no timestamp type in the live grammar — noted) · null-reserved | B | m-2 §17.2:299 |
| on_timeout | owner system · enum · reserved-shape; default hold_and_resummon is J1 POLICY, not a registry value — propose valueless slot, flag | B | m-2 §17.2:300; ARCHITECTURE §J1:95-98 |
| ODB record_kind member | NOT ADDED — no live record_kind widening in Step-1 (RECONCILE Q6); the ⑤ fixture consumes a fixture-scoped registry view | — | RECONCILE Q6 |
| scope_paths | RECOMMEND NO ROW — struck until Step-2 (sole consumer = Step-2 IMPL done-predicate); canonical-iff-consumed argues against even a dormant home; my discretion call = defer | — | RECONCILE Q7; m-3 §5:98 |

**Pre-existing inventory gap found (not s5-authored, flagged):** `resolves_gate` is mechanically consumed by the engine (engine/submit.go:70, :217-236; tables/tables.go:143) with NO registry row — a live canonical-iff-consumed violation predating s5. Not mine to self-author semantics for ([VP-W2]); question Q-d below.

**4-bucket verdict:**

PRIMARY_BUCKET: still-open
still-open: the entire consumer-schema row set above (~28 new rows + ~8 new named_enums), the routing_escalation named_enums delta, the [VP-W3] negative dormancy fixture, the OI-S4-TOKEN-SCOPE seat_scope fold, the version-label bump — none exists at 67ee23e (grep-clean).
already-closed: GRILL_REQUIRED + GRILL_LOCK_ID (owed-#3 EMPTY both legs, confirmed live at registry.json:123-124); gate_category_raised home (:93); the layer_present:observe idiom precedent (:111-112); delivery_state/failing_edge homes (:95-96); the slot_in reserved-atom guard (predicate.go:155-165); model_identity⇒gate_referenceable enforcement (registry.go:162-164); the §7 config-change mechanism + fixtures (engine/submit.go:63-68, :173+; test/fixtures/s4_config_change_test.go). Do not rebuild any of these.
product-overlapped: accepts_interjection + template schema (m-5 config); posture/human_mode + surface_intent value-vocabularies (m-5-owned, m-6-bound — NOT in my dispatch list; question Q-c); rank1_recommended_bucket, predicate_result, veto (not persisted); scope_paths (struck; recommend defer); the ODB record_kind live member (rides away-mode activation).
recommended-next: one registry pass (rows + enums + delta + OI-S4 fold + version bump) + the [VP-W3] fixture, AFTER the orchestrator rules the four escalations (§3) and the five questions (§8) — the escalations change row shape, so the PLAN should not lock before they resolve.

### 2. Q2 — the routing_escalation delta (verify-only): SUPPORTED, byte-distinct, MINOR/additive

- `named_enums.gate_category_A` (registry.json:37-46): flat 8-member string array — append `"routing_escalation"` → 9. Structure supports it trivially.
- `named_enums.gate_category` (registry.json:53-67): 13 members, `"other"` last (:66) — insert `"routing_escalation"` immediately before `"other"` → 14, other stays last. Supports it.
- `named_enums.gate_category_B` (registry.json:47-52): NO change — verified the delta touches nothing there.
- NO FieldSpec-row change: `gate_category` :92 and `gate_category_raised` :93 reference enum_set names (`gate_category`, `bool`) — enum-set indirection means membership changes need no row edit. Verified.
- Byte-distinctness: `routing_unavailable` and `human_decision_required` appear 0 times anywhere in registry.json (mechanical grep) — neither is being added, and the new token collides with nothing. `routing_unavailable` stays a route_dispatch outcome state (m-4 §10:361-372, ARCHITECTURE.md:117); `human_decision_required` stays the other→A disposition label (ARCHITECTURE.md:118, :123).
- Consumer mechanics post-delta: `ClassifyGateCategory` (validate.go:176-187) reads GateCategory["A"]/["B"] built from the named enums (registry.go:120-123) — routing_escalation classifies A, no raise, correct. `GateCategoryAuthorityBearing` (validate.go:189-197) includes it via the A-list. `enumTokens` for the gate_category field reads NamedEnums["gate_category"] (validate.go:122-127) — the insert makes the token submittable. Consistency A∪B∪{other} = 9+4+1 = 14 holds.
- Classification MINOR/additive confirmed: agent_enum_pick widening + total other→A fallback preserved (m-2 §9:126 compat contract); `migrate.Current` (= 1, migrate.go:11) untouched — no record-shape change.
- **Same-pass obligation found:** the byte-exact enum floor test asserts the exact token lists (internal/fieldspec/registry_test.go:34-46) — the delta MUST update those assertions in the same pass or the battery goes red. Scope note Q-e below.

### 3. Q3 — dormancy idiom per row + the validate/render trap

Idiom assignments are in the Q1 tables. The trap, precisely, from code:

- `Validate` evaluates `required_when` with PresentLayers = DefaultLayers() = {store, form, lineage} — observe false (validate.go:29, predicate.go:17-23), so idiom-A requireds are OFF in Step-1 ✓ (live precedent proven at predicate_test.go:64-77).
- `Render` evaluates ONLY `visible_when` (render.go:54) and never `required_when`; and `renderable` hides owner system/computed + fill system_only/computed_result rows unconditionally (render.go:50-53).
- **Consequence:** an agent-fillable row gated only by required_when STILL RENDERS as an optional field (live proof: ACTIONS_GIT_REF/FINAL_GIT_STATUS_SHORT render on Step-1 forms today — no visible_when, owner agent_enum_pick). So idiom A for any lane-fillable row must set required_when AND visible_when to the same layer_present:observe conjunction, exactly as the dispatch warns. All my idiom-A proposals above carry both.
- **Render-context constraint (new finding):** at render, the predicate fields map contains ONLY {PHASE, CEREMONY_TIER} and rows=nil (render.go:30-33, :54) — so a visible_when keying on record_kind_in, field:<id>, or any_row is ALWAYS-FALSE at render (silent permanent hiding). Usable visible_when atoms are: phase_in, ceremony_tier_gte, seat_is, role_in, layer_present, (reserved slot_in). This structurally limits the available dormancy idioms.

**ESCALATIONS — rows fitting none of the three idioms (per the dispatch rule, these are yours to rule):**

- **E-1 (routing agent-fillable cluster):** routing_assignments, justified_deviation, deviation_reason_code, routing_record_kind, constraints. Not observe-owned (routing ≠ observe layer; the closed layer_present vocabulary {store, form, lineage, observe} — m-2 §5:89, predicate.go:385-390 — has no routing layer, so idiom A is semantically wrong AND the locked atom set cannot express "routing-layer absent"). Reserved-shape-B leaves them RENDERED on every Step-1 form (empty-option/row fields — only `grant` gets empty-option removal, render.go:79-81). Two honest options: (i) declared-live-shape, predicate-dormant — they render (SCOPE_DIFF/FOLD_SCOPE precedent: phase-agnostic, always rendered), required_when fires only when a row declares deviation, honesty [VP-W1] preserved ("declared-not-observed"); note m-4 §3:164 says the routing record IS written by seats + the human launches, so live-shaped may be design-faithful; (ii) render-hidden — needs either a grammar extension (new layer value or phase gating) or m-2 ruling. I RECOMMEND (i) with the [VP-W3] fixture scoped per §5 below; but this is a semantics call I will not self-author ([VP-W2]).
- **E-2 (ODB agent-fillable slots):** plain_language_change, why_now, tradeoffs_risks, recommendation, choices. Same shape as E-1 with no phase/seat gate available (ODB attaches to bucket-A gate records, not a phase). Options as E-1; additionally these could defer entirely to away-mode activation (their only consumer is the m-6 ODB surface — canonical-iff-consumed tension with declaring now).
- **E-3 (per-row columns):** declared_bucket, chosen_model, pin_mode, declared_deviated, seat, role, task_tag, seat_archetype, authority_ceiling as COLUMNS — see T3.
- **E-4 (on_timeout default):** the J1 hold_and_resummon default is policy; whether the registry row carries a single-member enum or a valueless slot needs m-2/m-6 confirm.

### 4. Q4 — grammar traps

Confirmed clean:
- gate_referenceable defaults false (Go zero value; registry.go:33) and model_identity⇒gate_referenceable-false is load-enforced (registry.go:162-164); predicate references to non-flagged fields are parse-REJECTED (registry.go:197-209; proven live: predicate_test.go:79-93 rejects a gate_category_raised reference). model_name lands model_identity:true. All proposed m-5 rows gate_ref false.
- record_kind_in atom preferred where record-kind-gating applies — F-GATE-2 precedent live at registry.json:114-118 (owner/source/target_surface/disposition_path/disposes_owed all key required_when on record_kind_in); my proposals reuse it; the ⑤/ODB gating stays fixture-scoped (no live ODB member).
- X-* discipline: no proposed row is X-prefixed; the X-* consumers/lineage guard is load-enforced (registry.go:165-169).

**Live traps found (both E1 code-trace, neither s5-a's code surface to fix — route as you see fit):**

- **T1 — owner:system headers pass submit UNVALIDATED and UNSANITIZED.** `ignorePayloadField` skips every owner:system / fill system_only row at validation (validate.go:115-120, the `continue` at :34-36), `seat.Stamp` sanitizes only envelope FROM/ROLE (seat/binding.go:112-116), and SubmitHandler strips no headers (engine/submit.go:24-83). A lane can therefore submit bytes into failing_edge today — and into every NEW owner:system dormant row (record_integrity, authority_class via its computed variant, attestation_source, model_name, completed_proof, …) — and they persist on the accepted record verbatim. Owner:computed rows are worse-than-skipped: they are enum-validated but lane-SUPPLIABLE (nothing requires a computed field to be absent from a submission). Consequence for my scope: "dormant" is render-absent, not submit-rejected; per [VP-W1] no s5 text may claim the dormant rows are non-lane-writable — I have phrased the model_name row accordingly (the m-3 certification "non-lane-writable via the tool surface" is NOT yet true of the submit tool at 67ee23e and must not be claimed as live until a guard exists). The [VP-W3] fixture should pin CURRENT behavior honestly (render-absence) and the gap should be named to master/m-2 — likely a small validator rule (reject non-empty system-owned headers on lane submissions), which is engine/validator code, not registry data.
- **T2 — gate_category="other" self-bounces.** validate.go:61-66 stamps `gate_category_raised = "true"` on a raised pick, but the row's enum is bool ["no","yes"] (registry.json:36, :93); field order gate_category (:92) precedes gate_category_raised (:93), so the stamped "true" is enum-validated in the same pass and violates ("unknown gate_category_raised") — a legal, rendered `other` pick (baseOptions includes it) bounces itself. Verified by code-trace: ClassifyGateCategory("other", false) → ("A", true) raised (validate.go:176-187, "other" is in neither A nor B list). Adjacent to my delta (routing_escalation lands in A → classify clean → unaffected) but the ③ RAISE-ONLY owed fixture will hit this. Fix is one token ("true"→"yes") plus a fixture — engine/fieldspec code, s5-b or m-2-confirm territory, not registry data.
- **T3 — per-row column ownership + conditional seat-scope are INEXPRESSIBLE in the live grammar.** The FieldSpec struct (registry.go:26-41) has no column specs for row_array fields and no record-conditional seat_scope — but the settled m-2 §17.3:309 mirror requires per-column ownership INSIDE routing_assignments rows (planner proposes seat/role/seat_archetype; conductor stamps seat_archetype + authority_ceiling per-column, m-5-F1) and m-4-F4 widens authorship to operator on template_ref-BEARING records only. Neither is registry-data-expressible at 67ee23e. For Step-1 dormancy this is survivable (no writer), but the rows' honest declaration needs a ruling: prose-documented columns + Step-3 enforcement carry, or an m-2 grammar extension (not mine to author).
- **T4 — seat_scope on a row_array has no defined render semantics.** scopeOptions returns seat_scope values as OPTION lists (render.go:124-150) — meaningful for enums, meaningless for row_arrays. routing_assignments with a seat_scope map would render garbage options. Interacts with E-1/T3.

### 5. Q5 — [VP-W3] negative dormancy fixture surface

**The single Step-1 render chokepoint** is `Registry.Render` reached via: (1) the channel `Describe` tool (cmd/frank/main.go:246-270 — live conductor; seat = bound SeatMeta, phase/tier request-supplied, defaults SITREP/medium; grants = lineage.RealGrantState; parents = ActiveLineageCandidates); (2) the MCP schema projection of (1) (cmd/frank-mcp/mcp.go:197-202, re-render refresh :166-168, :221-236); (3) the validator's own digest re-render (validate.go:22-25). All three share one form universe, so the sweep enumerates Render inputs:

- Seat-pattern equivalence classes (from every live seat_scope map + render.go:124-154): `operator` (IsOperator) · `*.orchestrator-planner` · `*.planner` (grant-state OFF and ON — render.go:110-120 prunes grant) · role/name fallthrough to `*` (implementer, orchestrator-reviewer, arbitrary seats). Six classes.
- PHASE × TIER: 11 phases (registry.json:10-22) × 5 tiers (:34) — phase also flips the MERGE-GATE dispatch-merge pruning (render.go:117-119).
- Monotonic-floor variants (env.MonotonicFloors raised/unraised) alter options, not field presence — one representative leg suffices.
- Bar per consumer-owned field under test: `form.HasField(f) == false` (fieldspec.go:28-31) for EVERY (seat-class × phase × tier) cell — absence, not merely un-required. Digest stability across the sweep is a free extra assertion (render.go:164-183 keys on form+config+seat-pattern+phase+tier).
- Fields under test: the full absent-set from Q1 that lands owner system/computed (structurally absent via render.go:51 — assert anyway, it pins the idiom) PLUS every lane-fillable dormant row whose idiom the E-1/E-2 ruling makes render-hidden. If the ruling is declared-live-shape for the routing cluster, the fixture's enumerated field list EXCLUDES those and the fixture documents why (honesty [VP-W1]).

**The four legs (m2d AC17:242 / NF-S5):**
- (a) observe-owned requireds step-gated OFF: Validate a candidate lacking the entire idiom-A set under DefaultLayers ⇒ zero `required` violations for those fields (precedent: predicate_test.go:64-77).
- (b) non-observe requireds STILL block: SUBJECT (validate.go:40-42), GRILL_LOCK_ID dependent-required (registry.json:124), owed_item required fields (engine/submit.go:128-134) — all still bounce.
- (c) EVIDENCE_TARGET still Step-1-required — **DIVERGENCE D1: not currently enforceable.** The live row (:90) has NO required_when, and the validator enforces only required_when predicates + the SUBJECT special case — nothing requires EVIDENCE_TARGET at 67ee23e. As stated, leg (c) FAILS against live code. Either the fixture asserts the weaker truth (rendered + fillable + not step-gated) or the registry pass adds a required_when to EVIDENCE_TARGET (a behavior change needing m-2 confirm — question Q-b).
- (d) layer_present never references model: structural — predicateLayers is the closed {store, form, lineage, observe} set, parse-validated (predicate.go:192-199, :385-390); a negative parse fixture pins it.

### 6. Q6 — OI-S4-TOKEN-SCOPE

Live data (registry.json:113): record_kind seat_scope = operator: all 7 members; `*`: [genesis, owed_item, owed_disposition, gate_resolution, disposition, diagnostics] — genesis + the owed tokens are fillable by ANY seat today; only config_change is operator-only. validateRecordKind (engine/submit.go:124-160) has no genesis arm, so a seat-authored genesis record passes shape-validation.

The two m-2 options the s4 RECONCILE names (s4-exit-gate/RECONCILE-orchestrator-planner-20260705-231116.md:36): **owner-typing** (narrow the `*` seat-scope list toward operator) vs **owed-id-picker** (bind owed authoring to a conductor-supplied candidate set, the parent_picker pattern). What the registry DATA supports: owner-typing is a pure data edit today (remove tokens from the `*` list; optionally add an `*.orchestrator-planner` scope line — the AUTHORITY row :88 is the live precedent for that three-tier scope shape). The owed-id-picker needs a new fill_constraints mechanism in render/validate (fill_constraints vocabulary registry.go:246-256 has parent_picker only, wired to lineage candidates) — engine code, s5-b/m-7 surface, NOT a registry-pass item. Recommendation: fold owner-typing in my registry pass — narrow `*` by removing `genesis` (per VP+m-7: "incl. genesis") and move owed_item/owed_disposition authoring per whatever m-2 confirms; compat note — narrowing seat_scope narrows fill-time affordance, not the enum or record shape (old accepted records unaffected; replay is read-side), but the compat LABEL (MINOR?) is m-2's call, folded under the one m-2 confirm the dispatch routes via you to master.

### 7. Q7 — registry-version label + §7 fixture-leg shape (s5-b boundary)

- Live label: `"version": "s3-fieldspec-v2"` (registry.json:2). No mechanical consumer reads Registry.Version at 67ee23e (grep: no non-test reference) — it rides the config digest via raw bytes. Proposed bump: **`s5-fieldspec-v3`** (slice provenance + monotonic counter, the s3 pattern). Provenance block (:3-8) still says owner s3-form — propose a provenance update in the same pass (small; flag to m-2 confirm alongside the rest).
- `migrate.Current` = 1 (migrate.go:11) — UNTOUCHED: additive rows/enums are not a record-SHAPE change (RECONCILE Q4; m-2 §9:126). The Q4 deliverable set stands: (a) this label bump, (b) s5-b's zero-loss replay (count/identity/canonical-wins — my additions must keep all 47 existing rows byte-stable, which my table satisfies EXCEPT the OI-S4 seat_scope narrowing on :113 and any EVIDENCE_TARGET required_when — both named as deliberate, confirmed edits, so the replay leg should assert canonical-wins over old records, not registry-byte identity), (c) version-negotiation/refusal legs (migrate.go:55-66 ErrFutureVersion/ErrUnversioned precedent, migrate_test.go:51+). NO envelope migrator.
- §7 fixture legs my rows imply (adapted [VP-W7], TEST stores; s5-b builds, I supply the content): (1) operator-authorized record shape — record_kind=config_change is operator-only seat_scope (:113) and classifyConfigChange requires member + matching new_digest (engine/submit.go:63-68, :173-186); my pass changes the fieldspec member bytes ⇒ a new digest value the config_change record must carry. (2) old→new digest, no re-genesis — precedent TestRunningConfigUnchangedUntilRestart (s4_config_change_test.go:21). (3) phase-0 digest acceptance via the genesis chain on restart. (4) stale-form re-render: the form digest folds config_digest + form content (render.go:164-183) ⇒ every pre-change form digest goes stale ⇒ submit bounces `form_digest: re-render` (validate.go:23-25; precedent TestRestartWithNewRegistryBouncesStaleForm :210) ⇒ Describe re-render ⇒ re-rendered submit succeeds — and my new rendered fields (per the E-1/E-2 ruling) must appear in the re-rendered form, my absent ones must not. I do NOT build these legs; the registry content + expected digests + byte-exact enum assertions are my deliverable into them.

### 8. Questions for the orchestrator (blocking the PLAN lock, not the audit)

- Q-a: deviation_reason_code value-set placement — RECONCILE Q8 says the default vocab is "config, not registry", but the dispatch's idiom C names the gate_category precedent, whose values DO live in registry.json named_enums (registry.json IS the fieldspec config member). Valueless slot vs named_enums mirror of the 7 defaults (capability_gap … other)? One m-2 confirm requested.
- Q-b: EVIDENCE_TARGET Step-1-required (D1, §5(c)) — add required_when in my pass (behavior change, m-2 confirm) or weaken the fixture leg?
- Q-c: surface_intent + human_mode/posture value-enum homes (m-2 §17.6:346-347) are NOT in my dispatch list — deliberately out of s5-a scope, or an omission to fold? (I propose nothing absent your ruling.)
- Q-d: `resolves_gate` consumed-but-undeclared (T4 area; engine/submit.go:70) — fold a row into my pass under m-7/m-2 confirm, or register as an owed item?
- Q-e: write-scope: the enum delta requires updating internal/fieldspec/registry_test.go:34-46 assertions (byte-exact floor) in the same pass — registry_test.go is not named in my write surface (registry.json + s5_registry_*_test.go). Confirm it is in-bounds for the IMPL dispatch.
- Plus the four idiom escalations E-1..E-4 (§3) and the two trap routings T1/T2 (§4).

### 9. Boundary contract (restated from the dispatch; unchanged)

Writes: registry.json rows + named_enums delta + the dormancy fixture (s5_registry_*_test.go). Reads: the settled field semantics (RECONCILE Q1–Q11). Target entity: the FieldSpec registry (landed via s5-b's §7 fixture legs at build time). Downstream consumer: s5-b's ③/⑤/I-PH fixtures consume my row ids/enums; Step-2 observe consumes the declared fields. Contract: exact 12-column rows per m-2 grammar. Proof: E2 fixtures at exit. No-consumer action: defer (applied: scope_paths, rank1_recommended_bucket, predicate_result/veto, accepts_interjection, ODB record_kind member — all flagged, none added).

### 10. Risks / reject-or-narrow gates

- Do NOT lock a PLAN before E-1/E-2 (render-visibility of lane-fillable consumer rows) resolves — it changes ~10 rows' shape.
- Do NOT claim non-lane-writability for any dormant system row while T1 stands ([VP-W1] honesty; the m-3 model_name certification phrasing must stay "via the tool surface" qualified AND note the submit-path gap).
- The ③ owed fixture will trip T2 (`other` self-bounce) — sequence the T2 fix (wherever you route it) before or with ③.
- I-PH: every new named_enum token and row id lands in rendered forms/bounce text — all proposed ids are semantic names, no store/config/outbox path fragments; the bounce formatter's Field:Class-only shape (bounce/formatter.go:11-26) is preserved by construction since I add no formatter changes. The NF-S18/NF-S15 sweep extension over new rendered descriptions is s5-b's I-PH surface; my row ids are sweep-safe.

## Verification
- Every code claim: file:line against `frank/` `main @ 67ee23e`, working tree clean (below). Every semantics claim: the s5-fidelity RECONCILE (settled Q1–Q11), the named m-2/m-3/m-4/m-5/m-6 design-of-record lines, ARCHITECTURE.md §C4/§J2.
- Mechanical checks run read-only: registry row count (47), named-enum sizes, the 40-field absence grep (all 0), the routing_unavailable/human_decision_required absence grep (0).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root .relays/s5 <this file>` — run before handoff.

ACTIONS_GIT_REF: none — read-only audit; no code/git action taken; this relay file is the only artifact written (relay substrate, gitignored)
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ on main @ 67ee23e; verified post-audit)
Next requested action: orchestrator reconciles this against s5-a.implementer's independent audit; rules E-1..E-4, T1/T2 routing, and Q-a..Q-e (m-2 confirms via master, file mode); then the s5-a PLAN can lock.
