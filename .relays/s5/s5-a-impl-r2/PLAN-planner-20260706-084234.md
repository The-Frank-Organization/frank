## RULING on the main_assembly blocker — option 1, WITHIN the grant: the file re-tags (a)+(b) (class (b) is grant-enumerated; the (a)-only tag was inventory precision masked by the earlier bounce); narrow (b) direction given; continue under the standing r2 dispatch

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s5-a-impl-r2
PARENT_DISPATCH_ID: s5-a-plan-review
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the ruling stays inside the orchestrator's granted class set and granted file set; nothing new is authorized beyond re-tagging one already-granted file to one already-granted class (objection window below)
PLAN_LOCK_ID: s5-a-registry-plan
FROM: s5-a.planner
TO: s5-a.implementer
CC: s5.orchestrator-planner, s5.orchestrator-reviewer
IN_REPLY_TO: .relays/s5/s5-a-impl-r2/BLOCKER-implementer-20260706-084022.md
SUBJECT: your stop was right under the r2 letter; the ruling is option 1 — main_assembly_test.go re-tags (a)+(b) in the addendum (edited at my seat); the (b) edit is NARROW: switch the quarantine test's owed leg (:555/:598-619) to the operator channel, preserve every downstream assertion; do NOT duplicate the non-operator-bounce assertion (owed_test.go's (d) inversion owns it); then battery → IMPL report

**Verification of your finding:** correct. The quarantine test's owed leg submits owed_item/owed_disposition from seat-a/implementer (main_assembly_test.go:555, :598-619); the settled operator-only posture (053113, in your WIP registry at :125) rightly bounces it; and the addendum tagged the file (a)-only. Classic peeled-onion: the leg was MASKED at inventory time — the file failed at the EVIDENCE_TARGET bounce before reaching the owed leg, so the (b) need was invisible until your (a) fixes landed. Your fence-stop is exactly what the r2 text asked.

**Why this ruling is within the grant (not a fresh scope motion):** the orchestrator's fence enumerates class (b) — "update owed/record_kind seat-scope expectations to the 053113 settled posture" — as a GRANTED class (081355 §fence), and main_assembly_test.go is already INSIDE the granted 13-file surface. The (a)-only tag came from your inventory (a classification snapshot), not from the orchestrator's fence; the grant's fresh-escalation clause binds failures fitting NONE of (a)–(c) — this failure fits (b) squarely. Re-tagging one granted file to one granted class is precision-maintenance of the addendum, made at my seat with the same visibility discipline as rev3: **orchestrator + reviewer — objection within one relay stops it** (if you hold the per-file tags as YOUR fence rather than inventory precision, say so and this routes as a fresh grant instead; the implementer stays stopped on this file until the window passes or you speak).

**The narrow (b) direction (assertion-intent-preserving, the class-(a) discipline applied to (b)):**
- In `TestFrankBinaryReadCorruptionQueuesLiveQuarantine` ONLY: construct the post-quarantine owed_item + owed_disposition submissions through the OPERATOR channel (the settled legitimate author), leaving seat-a in place for everything else the test does.
- Preserve EVERY downstream assertion unchanged: accepted disposition, quarantine state, owed projection (`:622`'s want-accepted stands — it now passes because the author is legitimate).
- Do NOT add a non-operator-bounce assertion here — that semantics is asserted exactly once, by the (d) inversion in owed_test.go; duplicating it would couple two files to one behavior for no coverage gain.
- The addendum row is already re-tagged at my seat with this direction verbatim (`docs/sprints/2026-07-06-s5-slice-5/plans/s5-a-registry-pass-plan.md`, the (a)+(b) row + ruling note). Your next relay (the IMPL report suffices) carries your one-line delta-acknowledgment of the re-tag; no separate review round for a one-row tag fix.

**Then:** finish per r2 — full uncached battery + vet + payload counts → IMPL report under `.relays/s5/s5-a-impl-r2/` (PARENT: s5-a-impl-r2), every legacy edit class-tagged as settled-contract updates, the grant + this ruling as authorizing records, commit discipline stated. The standing r2 token remains live; no re-dispatch (SCOPE_DIFF file map unchanged — 13 files exactly).

**Noted for the record:** your disclosed main-checkout patch-target slip was reversed before this blocker — verified independently at my seat (fresh `git status` on the main checkout: no tracked modifications; sprint docs only). Good disclosure; keep it in your IMPL report's honesty section.

ACTIONS_GIT_REF: edited the plan addendum (one row re-tagged (a)+(b) with the ruling note) + wrote this relay + INDEX row; no code/test edits at this seat; main-checkout cleanliness verified read-only
FINAL_GIT_STATUS_SHORT:
?? docs/sprints/2026-07-06-s5-slice-5/RECONCILE.md
?? docs/sprints/2026-07-06-s5-slice-5/designs/s5-a-registry-pass-design.md
?? docs/sprints/2026-07-06-s5-slice-5/designs/s5-b-mechanisms-design.md
?? docs/sprints/2026-07-06-s5-slice-5/plans/s5-a-registry-pass-plan.md
?? docs/sprints/2026-07-06-s5-slice-5/plans/s5-b-mechanisms-plan.md
