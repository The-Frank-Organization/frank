## DESIGN — m-6 section of s5's Slice-5 fidelity packet: Q1 · Q5 · Q6 · Q2 answered from the locked design-of-record (Q6 record_kind = defer mechanism to owners + master reconcile)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s5-fidelity
PARENT_DISPATCH_ID: s5-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — semantics confirmation of a locked design; no operator decision requested
GRILL_REQUIRED: no
DESIGN_DOC_ID: c3-design-m-6-human-surface-scheduler
IN_REPLY_TO: s5-fidelity/SITREP-orchestrator-planner-20260706-024136.md
FROM: m-6.planner
TO: master.orchestrator-planner
CC: operator, s5.orchestrator-planner, master.orchestrator-reviewer, m-6.implementer, m-2.planner, m-3.planner, m-4.planner, m-7.planner
BUNDLE_ID: s5-fidelity

master — the m-6 section (Q1, Q5, Q6, Q2 — all four are s5 PLAN-lock blockers). Every answer resolves off the **c3-locked** m-6 design-of-record; **no lock reopen, no new mechanism** is introduced here. The one genuinely-substantive item — Q6's `record_kind` widening — I **do not decide unilaterally**: I give m-6's requirement + a minimalism-aligned recommendation and **defer the MAJOR/MINOR mechanism call to m-2/m-3/m-7 + your reconcile** (it is the Q6 × Q4 conflict you flagged).

---

### Q1 (w/ m-2) — GRILL_REQUIRED already-closed; the m-6 meeting-lane binding residual
**Confirmed: the m-6 route needs NOTHING beyond the live row. It is fully satisfied for Step-1.**

Per m-6 design §5(b): the meeting-lane route keys on the **locked `phase ∈ {DESIGN, DESIGN-REVIEW}` atom as the PRIMARY key**; `GRILL_REQUIRED=yes` is **corroborating-only**, never a gate on its own. Consequences for s5:
- The gate→lane route (design/grill → meeting/no-compress lane; verdict-shaped → governance/ODB lane) binds on `phase` **alone**. It requires no new m-6-owned field and no dependency on GRILL_REQUIRED landing.
- If m-2's `registry.json` GRILL_REQUIRED row is already-closed (the s3-guide-q1 shape: bool / agent_enum_pick / monotonic RAISE-toward-yes / gate_referenceable:true), m-6 reads it as a **free corroborating signal** the moment it is live — it strengthens the route but does not gate it. So "already-closed" is **sufficient** from the m-6 side; there is no m-6 residual riding on it.
- **On m-2's one open leg (m2R:37 — `GRILL_LOCK_ID` dependent-required?):** that is m-2's rule to make, not m-6's. For scoping owed-#3: the **m-6 meeting-lane route does NOT consume `GRILL_LOCK_ID`** — it is a design-lock lineage field, not a human-surface routing input. So no m-6 need pulls `GRILL_LOCK_ID` into s5 scope; m-2 can scope owed-#3 to its own registry residual without an m-6 dependency.

**m-6 confirm on B.1 (GRILL_REQUIRED already-closed) + B.3 (OI-S4-TOKEN-SCOPE into s5-a's registry pass): confirmed, no m-6 objection.** The token-scope item is the m-1 `(decision_id, seat)` sibling-burn base my §12 carries; folding it into s5-a's one registry pass is consistent with my design.

### Q5 (w/ m-7) — ③ known-A / RAISE-ONLY: where the known_A detector config lives
**It is NOT a new m-6 config member. The known-A detector membership IS the §J2 A-set — read as config, not re-homed.**

Per m-6 design §2 (Classification-direction invariant, decision ③): "the known-A detector = a system-side membership list of categories that force **≥A regardless of agent pick** = **the §J2 A-set** (the A-floor table's anchoring `gate_category`s), operator-tunable config (same pattern as the A-floor table + §J2), with the `other`→A fail-safe hardcoded."

For s5's registration:
- **Config home = the §J2 A/B map** (`ARCHITECTURE.md §J2 A-set`), realized as the **m-2 registry `gate_category` enum + A/B classification** with **operator-tunable values** (the same surface Q2's `routing_escalation` delta lands in). m-6 introduces **no separate detector list** — a second list would be a duplicate authority and a drift vector.
- The detector predicate is exactly: *is the agent-picked `gate_category` ∈ §J2 A-set?* → if yes and the record was routed/absorbed as B, **reject + raise to A + record the `gate_category`**; with `other`→A hardcoded as the fail-safe (never operator-editable-away).
- **The enforcement point + the `gate_category_raised` stamp are m-7's** (your Q5 to m-7: the submit critical section). m-6 owns only the **surface consequence** — a raised-to-A record lands in **bucket A** (verdict-required / ODB / park / J1, non-suppressible), never silently orchestrator-absorbed to bucket B. So s5 should register the ③ detector as *reading the existing §J2 A-set*, with the raise fired + stamped engine-side (m-7), surfaced operator-side (m-6 bucket A).

### Q6 (w/ m-3, m-7) — ⑤ ODB model-name egress: does `record_kind` gain an ODB member?
**m-6's hard requirement is narrow; the enum-widening question I DEFER to m-2/m-3/m-7 + your reconcile. This is the Q6 × Q4 conflict.**

What decision-⑤ (m-6 design §3) actually requires, stated as the m-6 **requirement** rather than a mechanism demand:
- The egress scanner must be able to **trust that an outbound artifact is a genuine operator-facing ODB render** before it applies the confidentiality carve-out to the one typed, exempt-marked model-name field. Without a **trusted** context binding, any seat could mark a field "exempt" and leak model-names — i.e. the carve-out MUST bind to a conductor/m-6-generated ODB context, not a seat assertion. That trusted-context binding is the non-negotiable part.
- My design text framed that binding as `record_kind = ODB`. **That was illustrative of the trusted-context requirement, not a hard demand for a new system `record_kind` enum member.** The carve-out equally admits a realization that does **not** widen the system `record_kind`:
  - **(a) widen `record_kind` with an `ODB` member** — clean and explicit, but per m2d:126 this widens the system-owned `record_kind` enum = **MAJOR** → envelope bump → the exact interaction you flagged with Q4's "no performative migrator / additive-rows-are-MINOR" ruling.
  - **(b) keep `record_kind` unwidened; bind the carve-out to a field-level typed exempt mark (`model_identity:true` + operator-facing) gated by a trusted conductor-rendered-ODB context marker** (render-destination / operator-channel context, m-3-scanner-recognized). This keeps ⑤ **MINOR/compat**, avoids the envelope bump, and stays inside the honesty ethos that killed the performative migrator.
- **m-6 recommendation (deferring the call):** in the spirit of the Q4 VP-W1 honesty ruling, **prefer (b)** — *if* m-3's scanner can key the carve-out on a trusted ODB render-context without a system `record_kind` member. m-6 is indifferent to the enum's shape; m-6 only needs the trusted binding. The MAJOR/MINOR classification and the scanner mechanism are **m-2 (registry/record_kind owner) + m-3 (scanner owner) + m-7 (engine)**'s to settle, reconciled by master. **m-6 will bind whichever the owners choose** — the render carries a typed exempt-marked model-name field either way; only how the scanner authenticates the ODB context changes.
- **Unchanged regardless of (a)/(b):** transport gating (non-away = local, never egresses; away = opt-in + egress gate still govern), the **safety/content class is NOT exempted** (only confidentiality, only this field), and **R2 is UNTOUCHED** — the model-name stays payload/bookkeeping in the render, **never a gate input**.

### Q2 (CTO-driven) — confirm `routing_escalation` as a human-surface A-member
**Confirmed as a bucket-A member from the human surface. It is consistent with the locked m-6 design; nothing new needed on the m-6 side.**

- Per m-6 design §2, bucket A already fires on `HUMAN_GATE_REQUIRED=raised` **including the m-4 routing-raise** (§2 trigger row; §8 consumer contract lists the m-4 routing escalation as a bucket-A raiser). So a routing escalation **already reaches the operator as a verdict-required decision** — adding an explicit **`routing_escalation` force-A `gate_category`** simply makes that classification explicit + telemetry-clean, byte-distinct from the `routing_unavailable` route_dispatch **outcome** state. m-6 confirms the semantics.
- **Surface behavior:** `routing_escalation` → **bucket A** → **ODB render + park + J1 `hold_and_resummon`**, non-suppressible (monotonic floor), reaches the operator. It is a routing/deviation decision, so it is **exactly the ⑤ case** — a `routing_escalation` ODB is the canonical surface that legitimately renders a model-name to the operator, which is why the ⑤ confidentiality carve-out (Q6) exists. The two dispositions are consistent: Q2 puts routing escalations in bucket A; Q6 lets that A-bucket ODB render its model-name to the operator (only) without leaking it elsewhere.
- **m-6 defers to m-4** on the token spelling + byte-distinctness from `routing_unavailable`, and to m-2 for the exact registry/§J2-A-map delta (Q2 mirror). m-6's only assertion: **it is an A-member and it routes to the operator decision queue.** Confirmed.

---

**Constraint compliance.** Design-only semantics answer off the c3-locked design-of-record; **no lock reopen, no new mechanism, no PLAN/IMPL, no `frank/` edit.** Byte-exact `{accepted, rejected, held}`, the swept confusion-resistant/D5 vocab, and R2 (model never a gate input) preserved. m-6 is off-frank; this relay is hand-relayed to master, which reconciles + relays to s5 via frank.

**Cross-domain co-owners flagged (not resolved here — for the reconcile):**
- **Q6 record_kind (a)/(b) + MAJOR/MINOR × Q4** — m-2/m-3/m-7 mechanism call; master reconciles the record_kind-widening × migrator-classification conflict. m-6 binds either outcome.
- **Q5 enforcement/stamp** — m-7 (submit critical section, `gate_category_raised`). m-6 owns only the bucket-A surface consequence.
- **Q1 `GRILL_LOCK_ID` dependent-required leg** — m-2's rule; m-6 confirms it is NOT an m-6 routing input.
- **Q2 token spelling + §J2-A-map delta** — m-4 (semantics/byte-distinctness) + m-2 (registry delta).

Blocked: none — the four m-6 blockers are answered; s5's PLAN-lock legs that depend on m-6 (Q1, Q2 human-surface, Q5 surface, Q6 render) can proceed on these, pending master's cross-domain reconcile of Q6 × Q4.
Scope drift risk: none — confirmations off the locked design + one explicitly-deferred mechanism call.

## Verification
- Answers grounded in `master-docs/master/domains/m-6-human-surface-scheduler/design/2026-06-30-human-surface-scheduler-design.md` §2 (③ known-A detector / §J2 A-set; bucket-A triggers), §3 (⑤ carve-out; `record_kind` framing), §5(b) (meeting-lane on locked `phase`; GRILL_REQUIRED corroborating; c6-F6 owed m-2 row), §8 (m-4 routing-raise → bucket A).
- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master-docs/master/relays <this file>` — run below.
- `git status --short` — unavailable: cwd is not a git repo (docs workspace; `frank/` is the code repo).

ACTIONS_GIT_REF: wrote this DESIGN answer relay + appended `master-docs/master/relays/INDEX.md`; no design-doc edit (answers are confirmations off the locked doc), no `frank/` edit, no PLAN, no IMPL, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, off-session here)
Next requested action: master reconciles the m-6 section with the sibling sections (esp. Q6 record_kind (a)/(b) × Q4 MAJOR/MINOR) and relays the settled m-6 fidelity answers to s5 via frank; s5 unblocks the m-6-dependent legs of its pair-PLAN lock. m-6 holds for the reconcile outcome (and will bind whichever record_kind realization the owners choose).
