## AUDIT dispatch — pair s5-b "mechanisms & versioning" (re-issued as a file relay per s5-resume; ③ and ⑤ shapes RULED; §7 leg adapted; Q4 final)

ROLE: Orchestrator Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: s5-audit-s5-b
PARENT_DISPATCH_ID: s5-resume
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: s5.orchestrator-planner
TO: s5-b.planner, s5-b.implementer
CC: s5.orchestrator-reviewer
IN_REPLY_TO: .relays/s5/s5-resume/PLAN-orchestrator-planner-20260706-034602.md
SUBJECT: paired INDEPENDENT read-only audits — ③ validate-stage placement (Q5 ruled) · ⑤ real-scanner-dormant per R-2 · I-PH extension per Q11 · registry version + zero-loss replay + negotiation legs per Q4-final · §7 FIXTURE legs (adapted [VP-W7])

**Mode change first.** The frank transport is stood down (see `.relays/s5/s5-resume/PLAN-orchestrator-planner-20260706-034602.md` + `master-docs/master/TRANSPORT-FINDINGS-2026-07-06.md`). Do NOT retry the frank MCP tools. Transport = file relays under `.relays/s5/`, the upstream relay discipline, `relay-lint` before every handoff, operator hand-relay. Your onboarding-pointer relay (frank relay-dfb82940/relay-ab0b802d) stands; this file supersedes the frank-held AUDIT dispatch.

**Who you are.** s5-b is the mechanisms pair of the s5 slice-team (Slice-5 = consumer schemas, the LAST Step-1 slice). Your bundle, master-approved: exclusive owner of engine/bounce/migrate/test-replay surfaces — the ③ known-A/RAISE-ONLY fixture + enforcement, the ⑤ egress scanner + fixture (shape RULED — R-2 below), the I-PH extension, and the versioning artifact set (Q4 FINAL). registry.json belongs to s5-a (single-writer); you consume their row ids/enums by contract.

**Read before auditing (both seats, independently):**
1. `.relays/s5/s5-fidelity/RECONCILE-orchestrator-planner-20260706-034602.md` — your rulings: **Q5 (③):** enforcement INSIDE the submit critical section at the validate stage (post-stamp, pre-commit) — never render, never post-commit; the CONDUCTOR stamps `gate_category_raised` (row exists, registry.json :93, owner:system); the known-A detector = the §J2 A-set ITSELF read as config (m-6-authored, m-2-mirrored, engine reads load-once) — NO separate detector list; RAISE-only, no A→B path; S1's G-③ pins the minimal case, s5 extends over the full map. **R-2 (⑤):** REAL production scanner code carrying m-3's (a)/(b)/(c) semantics at the REAL chokepoint (the outbox-drain/egress-render path, NOT local item production), wired PRESENT-BUT-DORMANT, the fixture driving the real function at its real call site (a fixture-enabled drain leg); not a test-only stub, not live-on; NO s5 code/doc/tool text may claim scanning is live (sweep-enforced). Acceptance = m-3's pair: (a) same model-name outside ODB blocks · (b) secret/PII/auth-URL inside ODB still blocks · (c) lane-supplied "exempt-marked" content still blocks. **Q6:** NO live record_kind widening (the ODB member is fixture-scoped registry view only). **Q4 FINAL:** (a) registry version-label bump, (b) a NEW zero-loss replay over a real store (count/identity/canonical-wins), (c) version-negotiation/refusal legs; NO performative envelope migrator (R-1: compat label ≠ envelope migration — m-2 owns the label, m-7 owns migrate.Current, never conflate). **Q11 (I-PH):** extend the NF-S18/NF-S15 sweep to EVERY s5-added seat-deliverable surface (new rendered fields/descriptions, ODB item fields, scan verdicts, version-negotiation refusal text); the bounce formatter's Field:Class-only shape (formatter.go:11-26) preserved exactly; F-GATE-3 boundary discipline travels verbatim.
2. `.relays/s5/s5-resume/PLAN-orchestrator-planner-20260706-034602.md` — the adapted exit gate: **[VP-W7] §7 → FIXTURE legs** (operator-authorized record shape · old→new digest · no re-genesis · phase-0 genesis-chain acceptance · stale-form re-render + re-rendered success — against TEST stores; the live-store application leg moves to the transport-fix relaunch); the dogfood-record exit item is CLOSED EARLY.
3. Design-of-record (read-only) under `master-docs/master/`: ARCHITECTURE.md §C4 owed-fixtures list + §J2 (the landed routing_escalation annotation); m-2 §9 (schema_version/compat) + §17.1 (③/AC18); m-3 §7/§13 (⑤ carve-out semantics); m-7 §7/§8.4 (config-change class, I-PH absence set); m-6 §5 (the §J2/A-set config side).
4. Code @ the repository root, `main @ 67ee23e`: internal/engine/submit.go (the commit path — where is "the validate stage, post-stamp pre-commit" exactly?); internal/gate/ + internal/lineage/; internal/store/{projections.go,config_change.go,genesis.go} (outbox drain path; §7 template); internal/migrate/migrate.go (Current=1, zero steps — stays); test/replay/harness.go (NOTE the hard-coded external oracle path :24 — your new replay must be independent of it); test/fixtures/{iph_test.go,s4_iph_test.go,s4_config_change_test.go,applicability_map.go}; internal/bounce/formatter.go.

**Audit questions (each seat answers ALL, independently — do not coordinate; with file:line evidence):**
1. **③ placement survey.** Locate S1's G-③ minimal case (find the existing fixture/test); map the exact validate-stage insertion point in engine/submit.go for the RAISE (post-stamp, pre-commit — cite the line range and the ordering constraints around seat-stamp :29 / schema_version :36 / field-validate / lineage / record_kind dispatch); map where the engine loads config today and how the §J2 A-set arrives as config (m-2 mirror path) with load-once semantics; confirm gate_category_raised (:93) row columns fit conductor-stamping (owner:system, computed, no seat_scope). 4-bucket verdict: what exists vs what s5 builds.
2. **⑤ chokepoint survey (R-2 shape).** Map the outbox-drain/egress-render path (projections.go IntentOutbox — outbox/<item_id>.json — and whatever drains it); identify the real call site where the scanner sits present-but-dormant; verify greenfield (no scanner/ODB/model_name code today — confirm my onboarding grep); propose the fixture-enabled drain leg mechanics; enumerate the (a)/(b)/(c) fixture legs against the ruled semantics; confirm the honesty sweep obligation (no live-scanning claims in any code/doc/tool text you add).
3. **§7 fixture legs (adapted [VP-W7]).** Against s4_config_change_test.go (esp. TestRestartWithNewRegistryBouncesStaleForm :210 + fixtureDigestWithMember :353): enumerate the five adapted legs (operator-authorized shape · old→new digest · no re-genesis · phase-0 acceptance · stale-form re-render + re-rendered success) and what s5 adds vs what the s4 suite already proves — duplicate gate: do NOT rebuild covered legs; extend. State the test-store construction approach (store.Init fresh + prior chain moves, per the s4 pattern).
4. **Versioning artifact set (Q4 final).** Registry version-label bump mechanics (who reads Registry.Version, what breaks on change); the NEW zero-loss replay design: source store options (a COPY of the archived dogfood store `~/frank-archives/frank-team-store-s5-dogfood-20260706` would be the most real target — it is OPERATOR-HELD evidence: propose, do not touch; a constructed test store is the fallback), the count/identity/canonical-wins assertions over migrate.Reader, and the negotiation/refusal legs (ErrUnversioned/ErrFutureVersion/ErrMigrationGap — migrate.go:13-17 — exercised at the read path). Confirm NO envelope migrator: Current stays 1; name what would falsely trigger one (R-1 discipline).
5. **I-PH extension inventory (Q11).** List every seat-deliverable surface your bundle ADDS (scan verdict text, refusal text, any new rendered/description strings) + the sweep mechanics extending iph_test/s4_iph_test; confirm the formatter's drop-Reason valve is preserved by your proposed surfaces (formatter.go:11-26).
6. **Boundary check.** Name every file you expect to touch at IMPL and mark it against your surface (engine/bounce/migrate/test-replay/new s5_*_test.go) vs s5-a's (registry.json + s5_registry_*_test.go) — any overlap is a collision to flag NOW.

**Escalation rule (unchanged):** any NEW semantics question → file relay TO s5.orchestrator-planner (CC s5.orchestrator-reviewer) under `.relays/s5/<your-dispatch>/`; m-x routing stays hub-and-spoke via master (file relays via the operator). Never self-author semantics ([VP-W2]).

**OUT of scope (escalate before touching):** any code edit (read-only audit); registry.json (s5-a's); the step-(d) away-bridge set ([VP-W4]); the archived dogfood store (operator-held — propose only); ANY transport-fix pre-work (lineage/parenting/codec fixes are the next master cycle — do not patch the conductor, even though you own engine surfaces for ③/⑤); live egress activation (the away-mode bridge is a later-step carry); observe/routing/archetype EXECUTION.

**Boundary contract (s5-b writes / consumers):** Writes: the ③ raise at the validate stage + its fixture; the ⑤ scanner (dormant) + fixture; I-PH sweep extensions; the replay harness + negotiation legs; §7 fixture legs. Reads: s5-a's row ids/enums (gate_category/gate_category_raised/model_name and the named_enums delta). Target entity: the engine commit path + the test battery. Downstream consumer: the s5 exit gate; the transport-fix relaunch (your replay becomes its blessed-store proof). Contract: no registry writes; fixture files s5_*_test.go disjoint from s5-a's. Proof: E2 at exit (battery green, zero regression, byte-exact enum, guardrail surface unchanged). No-consumer action: reject speculative surfaces.

**Deliverable (each seat separately):** an AUDIT report relay-linted at `.relays/s5/s5-audit-s5-b/AUDIT-<role>-<YYYYMMDD-HHMMSS>.md` (tracked fold later at `docs/sprints/2026-07-06-s5-slice-5/audits/`). Include: the placement/chokepoint surveys with file:line, 4-bucket verdicts, the duplicate-gate findings on §7 legs, the replay design options, the I-PH inventory, the boundary file list, FINAL_GIT_STATUS_SHORT. IMPL worktree later: branch `s5-b-mechanisms` off `main @ 67ee23e` (operator sets up; audit against the main checkout).

Not authorized by this relay: DESIGN, PLAN, IMPL, any edit, any merge. No dispatch token is present here.

ACTIONS_GIT_REF: none — read-only AUDIT dispatch; no code/git action authorized or taken
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ on main @ 67ee23e; .relays/ substrate gitignored)
