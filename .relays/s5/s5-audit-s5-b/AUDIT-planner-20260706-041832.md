## AUDIT — s5-b.planner independent read-only audit (boot ack folded in): ③ raise partially built at the validate stage; ⑤ drain greenfield-confirmed; §7 legs generically covered — s5 owes the s5-delta variants; Q4 set shaped; four escalation questions

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: s5-audit-s5-b
PARENT_DISPATCH_ID: s5-resume
RUN_ID: s5
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
FROM: s5-b.planner
TO: s5.orchestrator-planner
CC: s5.orchestrator-reviewer
IN_REPLY_TO: .relays/s5/s5-audit-s5-b/AUDIT-orchestrator-planner-20260706-035446.md
SUBJECT: s5-b.planner AUDIT report — placement/chokepoint surveys with file:line; 4-bucket verdicts; §7 duplicate-gate findings; replay design options; I-PH inventory; boundary file list + collision flags; EQ-1..EQ-4 escalations

### 0. Boot ack (folded per the boot relay)

I am **s5-b.planner**, Planner seat of pair s5-b "mechanisms & versioning", online for RUN_ID s5 in file-relay mode. Loaded: `agent-pair-planner` skill + `protocol.md` in full. Read: the team charter (`master-docs/CLAUDE.md`); `.relays/s5/s5-resume/PLAN-orchestrator-planner-20260706-034602.md` (mode change + [VP-W7] adapted exit gate); `.relays/s5/s5-fidelity/RECONCILE-orchestrator-planner-20260706-034602.md` (Q1–Q11 settled; my bundle's Q5/R-2/Q4-final/Q11 rulings); my AUDIT dispatch (`AUDIT-orchestrator-planner-20260706-035446.md`); design-of-record sections ARCHITECTURE.md §J2 + §C4 owed list, m-2 §9/§17.1/AC18, m-3 §7/§13, m-7 §7/§8.4/§9, m-6 §5. No frank MCP tool touched. Audit run independently — no coordination with s5-b.implementer.

Code base audited: `frank/` `main @ 67ee23e`, clean tree. Full battery run at this seat for baseline: `go build ./...` clean + `go test ./...` **21 packages ok, zero failures** (E2, this session).

---

### 1. ③ placement survey (Q5 ruled shape)

**What exists (duplicate/already-built gate — substantial):**

- **The classifier exists and is config-fed.** `fieldspec.ClassifyGateCategory(token, knownA)` at `internal/fieldspec/validate.go:176-187`: A-membership from `r.GateCategory["A"]`, B from `["B"]`, unknown token → `("A", raised=true)` (the `other`→A fail-safe), `knownA=true` → forced `("A", true)`. The A/B sets load from `named_enums.gate_category_A/_B` (`internal/fieldspec/registry.go:105-108` legacy-populate, `:115-126` view-populate) inside the fieldspec config member — i.e. **the §J2 A-set already arrives as config with no separate detector list**, exactly the Q5 ruling's shape.
- **A raise stamp exists at the validate stage.** `internal/fieldspec/validate.go:61-66`: inside `Registry.Validate`, when `gate_category` is present and classification returns raised, the conductor stamps `cand.Headers["gate_category_raised"] = "true"`. This sits inside the submit critical section: `engine/submit.go:47` calls `reg.Validate` **after** seat-stamp (`submit.go:29`) and `schema_version` stamp (`:36`), **before** lineage (`:53`), record_kind validate (`:58`), record_kind dispatch (`:63-76`), and before the single-threaded commit (`engine/loop.go:126-133` — handler then `st.Commit` inside the one loop turn; the raise is atomic with acceptance). Post-stamp, pre-commit — the ruled locus is ALREADY the code's locus.
- **Load-once config semantics confirmed.** `cmd/frank/main.go:93-97`: `config.Load` once at startup, `reg := pinned.Registry`; no reload path (proven by s4 `TestRunningConfigUnchangedUntilRestart`, `test/fixtures/s4_config_change_test.go:21-65`).
- **No lane affordance at render.** `render.go:51` excludes `owner: computed`/`computed_result` fields from every rendered form — `gate_category_raised` is never offered to a seat.
- **The existing minimal case.** I found **no end-to-end G-③ fixture under `test/fixtures/`** — grep for `gate_category` across `test/` hits only config_change enum bodies. The minimal case is pinned at unit grain only: `internal/fieldspec/fieldspec_test.go:64-74` (A/B/unknown/knownA=true classification) + `internal/fieldspec/predicate_test.go:83` (`gate_category_raised` as a predicate input). **The stamp path itself (`validate.go:64`) has zero test coverage anywhere** — no test asserts a committed record carries the raised flag.

**What s5-b builds (still-open):**

1. **The full-map raise**: today raised fires only for tokens outside A∪B (i.e. `other`/unknown). The ruled B-pick-over-known-A raise has no trigger: `ClassifyGateCategory`'s `knownA=true` leg has **no production caller** (`validate.go:62` hardcodes `false`).
2. **The known-A context input** — see EQ-1 below; the mechanism that tells the validate stage "this record is known-A" is not present and not code-grain-specified in my read materials.
3. **Conductor-only enforcement of the stamp.** `validate.go:115-120` `ignorePayloadField` skips only `owner=="system" || fill_constraints=="system_only"` — `gate_category_raised` (`owner:"computed"`, `fill:"computed_result"`, no seat_scope, registry.json:93) is NOT skipped, so **a lane-supplied `gate_category_raised: "yes"` header passes validation and commits today**. The conductor-stamp property (Q5: "no lane supplies or suppresses it") is not enforced. (E1: validate.go:115-120 + registry.json:93.)
4. **Byte-exactness defect in the existing stamp**: `validate.go:64` stamps `"true"`, but the row's `enum_set: "bool"` is `["no","yes"]` (registry.json:36). The stamped value is outside its own enum — must normalize to `"yes"` (or the ruled canonical byte) at IMPL.
5. **The end-to-end ③ fixture** (the §C4 owed row): B-pick/B-absorb over known-A ⇒ committed record raised to A + `gate_category_raised` recorded, never delivered as B; extended over the full §J2 map incl. the incoming `routing_escalation` member (s5-a's delta).

**Row-columns check (dispatch item):** registry.json:93 = `owner:"computed"`, `type:bool`, `enum_set:"bool"`, `fill_constraints:"computed_result"`, no `seat_scope`, `gate_referenceable` absent (defaults false — matches m-2 §17.1). **Discrepancy vs the Q5 ruling text**: the RECONCILE says "s5-a adds that FieldSpec row as owner:system" — the row already exists with `owner:"computed"`. Functionally, `computed` is render-excluded (render.go:51) but NOT validate-ignored (item 3 above); `system` would be both. Whether the fix is a row flip to `owner:system` (s5-a's registry surface) or a validate-side computed_result strip/reject (my engine surface) is a cross-pair seam — flagged as EQ-3, and the row question belongs in s5-a's single registry pass.

**4-bucket verdict (③):**

PRIMARY_BUCKET: still-open
still-open: the full-map B-over-known-A raise trigger; the known-A context input; lane-supplied computed-field enforcement; the "true"→"yes" byte fix; the end-to-end owed fixture (stamp path currently untested).
already-closed: config-fed A/B classifier incl. the knownA-forced leg (unit-tested); the validate-stage raise stamp for unknown-token/`other`; load-once config; render exclusion of computed fields. Do not rebuild — extend `validate.go:61-66` / the submit path.
product-overlapped: none — the raise mechanism overlaps no sibling bundle (registry ROW ownership is s5-a's; mechanism is mine).
recommended-next: implement the full-map raise at the existing validate-stage locus after EQ-1/EQ-2/EQ-3 are settled; fold the byte fix in; author the owed fixture over the post-s5-a registry.

---

### 2. ⑤ chokepoint survey (R-2 ruled shape)

**Outbox production path (exists):** derived outbox items are produced by `obligation.completeOutbox` (`internal/obligation/obligation.go:143-186`) for accepted gate records and `held` records — committed as an `outbox-<itemID>` record whose intent (`store.IntentOutbox`) writes `outbox/<item_id>.json` atomically (`internal/store/projections.go:76-81`). Rebuild/recovery re-derive the same writes (`projections.go:169-185` `canonicalProjectionIntents`; `internal/recover/recover_test.go:71-72`). The item shape is `gate.OutboxItem` (`internal/gate/derived.go:8-16`) and already carries `GateCategory`.

**The drain (absent — this IS the chokepoint s5-b builds):** no code anywhere reads `outbox/*.json` for egress — grep over `internal/`, `cmd/frank`, `cmd/frank-mcp`, `test/` shows only the producer, the projection writer, rebuild/recover, and test assertions. m-7 §9 names the design: "the conductor's local outbox, **drained by the conductor itself**, with every item passing the m-3 §7 egress/content gate (fail-closed, dormant until the m-6 away bridge)". So the real call site is a **new conductor-side drain/egress-render function**; the scanner sits at its item-processing point, wired present-but-dormant (dormant = no live caller activates the drain in Step-1; NOT a test-only stub — R-2's floor honored).

**Greenfield confirm (dispatch item):** grep `egress|model_name|ODB` over all `.go` = **zero hits**; the broader `scan` matches are only `bufio.Scanner` (`store.go:161-168`), the `scan_result_in` predicate atom (`predicate.go:38,166,329-335,400-404` — dormant predicate support over `scan_result`-family FIELDS, not a scanner), and one `t.Fatalf("projection scan…")` string. Your onboarding grep is reproduced: the scanner, ODB handling, and model-name logic are greenfield.

**Proposed fixture-enabled drain-leg mechanics (for PLAN):** a real `Drain`-shaped function (new file on my surface — e.g. `internal/store/drain.go` or a small `internal/egress` package; naming at PLAN) that: reads an outbox item + resolves its source record, runs the scanner carrying m-3's two-class semantics (confidentiality incl. model-names with the single typed ODB `model_name` carve-out at `destination == operator`; safety/content = secrets/PII/auth-URLs never exempted; fail-closed `other`→block; block ⇒ never-send + the non-terminal `egress_blocked` disposition per m-3 §7 — no new terminal token), and returns the verdict at the render-for-egress point. The ⑤ fixture drives **that function at that call site** by constructing a store with outbox items and invoking the drain leg directly (fixture-enabled = the test is the only activator; live activation stays the away-mode bridge carry).

**Fixture legs (acceptance = m-3's pair, R-2):** (a) the same model-name in a non-ODB item **blocks**; (b) a secret/PII/auth-URL inside an ODB item **still blocks** (safety class unexempted); (c) lane-supplied "exempt-marked" content **still blocks** (the exemption keys on conductor-generated typed context only). Q6 constraint honored: **no live `record_kind` widening** — registry.json:72 has no `ODB` member and `validateRecordKind` (`submit.go:124-157`) switches exhaustively/no-fallback, so the fixture uses a fixture-scoped registry VIEW only; the dormant scanner's ODB-identification binds to m-6's conductor-rendered-ODB typed context, not a live enum member (precision item for PLAN).

**Honesty sweep obligation — confirmed and accepted:** no code comment, doc line, tool description, or verdict string I add may claim scanning is live; the §9 D5-qualified "only egress the governance system offers" phrasing travels verbatim; all new verdict/refusal strings enter the I-PH inventory (§5 below).

PRIMARY_BUCKET: still-open
still-open: the drain function, the scanner, the (a)/(b)/(c)+drain-leg fixtures.
already-closed: outbox enqueue/projection/rebuild/recovery (do not rebuild; consume).
product-overlapped: none (live activation = away-bridge, explicitly OUT per [VP-W4]).
recommended-next: build per R-2 on new files; collision-free.

---

### 3. §7 fixture legs (adapted [VP-W7]) — duplicate-gate findings

Against `test/fixtures/s4_config_change_test.go` + the engine/store suites, the five adapted legs are **all generically covered already**:

1. **Operator-authorized record shape** — COVERED: `engine/config_change_test.go:19` `TestConfigChangeNonOperatorRejected` (+ enforcement `submit.go:170-172`; seat_scope registry.json:113 restricts `config_change` to operator).
2. **Old→new digest** — COVERED: `TestPayloadDigestIsClaimNotAuthority` (`s4_config_change_test.go:167-208`; recompute-don't-trust at `submit.go:180-186`) + `engine/config_change_test.go:40` digest-mismatch; helper `fixtureDigestWithMember` (`:353-365`).
3. **No re-genesis** — COVERED: `internal/store/store_test.go:260-261` (second `Init` ⇒ `ErrGenesisExists`; guard `genesis.go:32-34,80-82`).
4. **Phase-0 digest acceptance via the genesis chain** — COVERED: `ValidateGenesis`/`ExpectedConfigDigest` (`genesis.go:104-141`, walked at startup from `recover.go:31`) + `TestChainWalkLatestWins` (`store/config_change_test.go:64`) + the restart leg of the test below.
5. **Stale-form re-render + re-rendered success** — COVERED: `TestRestartWithNewRegistryBouncesStaleForm` (`s4_config_change_test.go:210-281`): config_change → restart → stale digest bounces `form_digest:re-render` (`:255-258`) → fresh digest accepted (`:267-280`).

**Duplicate gate verdict: do NOT rebuild any leg.** What s5 adds is the **s5-delta payload variant**: the s4 suite exercises the class with a synthetic provenance-marker mutation (`mutatedRegistryBody`, `:283-301`); the [VP-W7] owed legs run the same five-leg pattern with **the real s5-a registry** (version-label bump + `routing_escalation` enum delta + the new rows) as the config_change body against TEST stores — proving old→new digest over the real delta, restart acceptance, and the stale-form bounce/re-render where the re-rendered form shows the new enum membership. Test-store construction: the s4 pattern exactly — `store.Init` fresh (per-test temp root via the `newS4ShimHarness` pattern) + prior config_change chain moves; no live store exists to touch (it is archived, operator-held).

PRIMARY_BUCKET: already-closed (mechanism + generic legs) with a bounded still-open extension (the s5-registry-payload variants — new `s5_*_test.go`, consuming s5-a's landed registry; sequencing note in §6).

---

### 4. Versioning artifact set (Q4 final)

**(a) Registry version-label bump.** `Registry.Version` is `registry.go:11` (`json:"version"`), currently `"s3-fieldspec-v2"` (registry.json:2). **No production reader**: nothing reads `.Version` as a value anywhere in `internal/`, `cmd/`, or `test/`. What "breaks" on change is purely byte-level and is exactly the §7 story: member bytes change ⇒ `config.Digest` changes (`config.go:74-91`) ⇒ the genesis-chain expected digest must advance via an operator config_change (or fresh `Init` in test stores) or phase-0 fails closed (`genesis.go:104-123`) ⇒ `form_digest` changes (ConfigDigest is folded into the render digest, `render.go:164-183`) ⇒ stale forms bounce and re-render. The bump and the §3 legs are one integrated proof.

**(b) NEW zero-loss replay over a real store.** Design:
- **Source-store options.** Preferred: a **COPY of the archived dogfood store** `~/frank-archives/frank-team-store-s5-dogfood-20260706` — the most real target (real records incl. held/quarantined/config_change traffic from the run). It is **operator-held evidence: PROPOSE ONLY** — I request the operator (via you) place a copy at a test-accessible path; the harness should assert structural invariants + a count derived from the copy's own redo log (not hard-coded numbers), since I cannot and must not inspect the archive to pre-compute them. Fallback (and the deterministic CI leg regardless): a **constructed test store** — `store.Init` fresh + a committed record variety (accepted/rejected/held, gate + owed + config_change chain) per the s4 pattern. I recommend building the harness store-path-parameterized so both legs share one implementation.
- **Assertions over `migrate.Reader`** (`migrate.go:80-91`): **count** — every records/redo entry is readable through the versioned read path, zero records lost; **identity** — for `SourceVersion == Current` the view is the stored record unchanged (`Apply` returns as-is at `version == current`, `migrate.go:62`; assert envelope/headers/body equality); **canonical-wins** — where the store holds both a canonical record and a derived projection of it, the replay treats the record as authority (the `gate/derived_test.go:123-147` outbox-record-vs-projection pattern generalized).
- **Independence from the existing harness (dispatch note honored):** `test/replay/harness.go:24-25` hard-codes **external** oracle paths (the upstream export tree) for the relay-lint parity replay. The new zero-loss replay is a separate harness with **no dependency on those paths or `dispositions.json`** — it walks a store, nothing else.

**(c) Negotiation/refusal legs at the read path.** Existing coverage (duplicate gate): `migrate_test.go:51-69` proves `ErrFutureVersion`/`ErrUnversioned`/`ErrMigrationGap` — but at **`Apply()` grain with synthetic in-memory records**; `:14-49` exercises `Reader.Read` happy-path with a registered migrator; `:71-86` store-errors-win. s5 adds the refusal legs **through `Reader.Read` against real store records** (planted record files with `SchemaVersion` 0 / future / gap-inducing values), i.e. at the same surface the seat `read` tool uses (`cmd/frank/main.go:302`). Refusal TEXT is already partially I-PH-swept (`iph_test.go:43-45` asserts the three error strings are path-free) — extend, don't rebuild.

**NO envelope migrator — confirmed with the R-1 tripwire named.** `migrate.Current` stays `1` (`migrate.go:11`); `New()` registers zero steps (`:36-38`). What would FALSELY trigger one (the R-1 conflation): treating m-2's **compat label** (MINOR/additive for the enum delta + new dormant rows, per m2d:126 ignore-unknown) as if it were a **record-SHAPE change** — e.g. bumping `Current` to 2 because the registry version label changed or because FieldSpec rows landed, then registering a no-op 1→2 step. Registry contents are config-member bytes governed by §7 digests; the record envelope shape is untouched by every s5 deliverable. m-2 owns the label; m-7 owns `migrate.Current`; nothing in s5 crosses them.

PRIMARY_BUCKET: still-open (the bump, the zero-loss replay, the read-path refusal legs) atop already-closed seams (the migrate registry/chain/refusal mechanism, the Apply-grain tests) — extend, don't rebuild.

---

### 5. I-PH extension inventory (Q11)

Seat-deliverable surfaces my bundle ADDS, each entering the sweep:

1. **⑤ scan verdict/block strings** — the scanner's violation output. Constraint: route through `bounce.Format` shapes or equivalently Field:Class-only tokens; class tokens must embed no path, no config value, no store layout.
2. **⑤ drain-leg diagnostics** — any error the drain function surfaces (store-read failures etc.) reuses the existing safe-class pattern (`engine.safeReason`, `loop.go:208-210`).
3. **Version-negotiation refusal text** — the three migrate errors are already swept (`iph_test.go:40-46`); any NEW wrapper text the read/replay path adds joins that list.
4. **③ raise surface** — the raise itself adds a header value, not a rendered string; if any bounce leg exists at IMPL its output is Field:Class-shaped like every other violation.
5. **§7 s5-delta legs** — no new seat-deliverable strings expected (test-side); any new engine error class introduced gets swept.

**Sweep mechanics:** extend the existing NF-S18/NF-S15 pattern — `iph_test.go` `assertNoPathFamilies` (`:106-115`; families `/records`, `/staging`, `/outbox`, `/binding`, `operator-socket`) and the s4 matrix `assertNoS4IPHLeaks` (`s4_iph_test.go:234-246`; forbidden = store root, socket, credential, `config/`, `binding`, `seats.json`) — by feeding every new output class (scan verdicts, refusal wrappers, drain diagnostics) through the same assertions in new `s5_*_test.go` capture points.

**Formatter valve — confirmed preserved.** `bounce.Format` (`formatter.go:11-26`) emits `Field:Class` ONLY — `Violation.Reason` and `Bounce` detail are dropped by construction. All my proposed surfaces emit through it or mimic its shape; none requires adding Reason to the output (no formatter change). The F-GATE-3 boundary and stated-carve-out discipline travel verbatim into any doc/comment text.

---

### 6. Boundary check — IMPL file list (marked against surfaces)

| File | Mark |
|---|---|
| `internal/fieldspec/validate.go` (③ raise extension + computed-field enforcement) | **mine-with-flag** — the ruled validate-stage locus lives here, but the fieldspec `.go` package is not named in either pair's write list. EQ-2. |
| `internal/engine/submit.go` (known-A context threading, if EQ-1 lands engine-side) | mine (engine) |
| NEW `internal/store/drain.go` or `internal/egress/` (⑤ drain + scanner) | mine (engine-adjacent new files; zero collision) |
| `internal/bounce/formatter.go` | expected NO change; touch only if a new violation value-type is unavoidable (mine if so) |
| `internal/migrate/migrate.go` | NO change (Current stays 1); tests only |
| NEW `test/replay/` zero-loss harness file(s) | mine (test-replay); independent of `harness.go` |
| NEW `test/fixtures/s5_*_test.go` (③ fixture, ⑤ legs, §7 s5-delta legs, I-PH extension) | mine; filename-disjoint from s5-a's `s5_registry_*_test.go`; shared package helpers (`newS4ShimHarness`) consumed read-only |
| `internal/fieldspec/registry.json` | **NOT mine** (s5-a single-writer) — but two of my findings land there: the `gate_category_raised` owner column (EQ-3) rides s5-a's single registry pass |
| conductor transport code (`internal/lineage/`, parenting, codec) | NOT touched — transport-fix is the next master cycle; I own engine surfaces for ③/⑤ only |
| archived dogfood store | NOT touched — propose-only (§4b) |

**Collisions flagged NOW:** (1) `internal/fieldspec/validate.go` ownership — EQ-2; (2) the `gate_category_raised` row-column change (owner `computed`→`system`?) is my semantics but s5-a's file — must fold into their single registry pass; (3) my §7 s5-delta legs CONSUME s5-a's landed registry — sequencing dependency: those fixtures land after (or bundled behind) s5-a's registry commit; (4) shared `fixtures_test` package — name-disjoint files, read-only helper reuse.

---

### 7. Escalation questions (new-semantics — routed, not self-authored [VP-W2])

- **EQ-1 (blocking my ③ PLAN):** What is the known-A detector's **input signal** at submit time — i.e. what marks a submission A-worthy when the seat's pick is B? Code-grain candidates found: (i) the render-context monotonic floor (`RenderEnv.MonotonicFloors` — but it is unwired in production, `cmd/frank/main.go:114`, and the index-based floor check `validate.go:79-94` cannot express "[floor, A]" over the A-first `gate_category` ordering registry.json:53-67 — a B pick is never "below" an A floor by index, so the raise must be a distinct check regardless); (ii) the referenced gate's own `gate_category` at the verdict path (`classifyVerdict` reads the referenced gate record, `submit.go:216-245` — a B re-pick over an A-category gate is detectable from tables). m-2 §17.1 says the detector "marks a record A-worthy" but not from which atom. I need the ruled grain before locking a plan.
- **EQ-2:** Confirm `internal/fieldspec/*.go` **code** (not registry.json) is s5-b's write surface for the ③ raise, or direct me to hoist the extension into `internal/engine/` post-Validate.
- **EQ-3:** Lane-supplied `computed_result` fields pass validation today (`validate.go:115-120`; `gate_category_raised` has no seat_scope) — is closing this (validate-side strip/reject, or the s5-a row flip to `owner:system`) in ③ scope? It is required for Q5's "no lane supplies or suppresses it".
- **EQ-4:** The existing stamp writes `"true"` (`validate.go:64`) vs the row's `["no","yes"]` enum — confirm the canonical byte (`"yes"`?) and that normalizing the existing stamp is in ③ scope (byte-exact-enum floor).

### Evidence by claim
- Battery green baseline — E2 (own run this session, 21 packages ok).
- All placement/chokepoint/coverage claims — E1 (file:line cited inline throughout).
- Greenfield ⑤ — E1 (grep evidence, reproduced independently).
- Archived-store contents — E0 by design (operator-held; not inspected).

### Risks / reject-or-narrow gates
- ③ cannot proceed past PLAN without EQ-1; do not let either seat improvise the detector input.
- The lane-writable `gate_category_raised` gap (EQ-3) is a live confusion-vector in the shipped code — worth closing inside s5 regardless of which pair carries it.
- §7: rebuilding any generic leg would be pure duplication — the extension is payload-variant only.

ACTIONS_GIT_REF: no edits claimed; final git status --short = (empty — clean tree, frank/ main @ 67ee23e)
FINAL_GIT_STATUS_SHORT: none — clean tree
Next requested action: reconcile against s5-b.implementer's independent audit; rule EQ-1..EQ-4 (EQ-3's registry half to s5-a's pass); then PROCEED-TO-PLAN for the pair.
