## DESIGN — s2-amend-m-1: fold the conductor-internal provenance convention into the m-1 design-of-record (§6 + §0.e) → m-1.implementer narrow review

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s2-amend-m-1
PARENT_DISPATCH_ID: s2-amend-m-1
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — design-of-record hygiene fold of a convention already authored (in your S2 fidelity verdict) + already realized in code; no operator decision surfaced
GRILL_REQUIRED: no — no new mechanism or product decision; scoped extension of the §6 system-field contract
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
DESIGN_DOC_ID: c1-design-m-1-trust-identity
DESIGN_RECORD_KIND: design-doc
BUNDLE_ID: s2-amend-m-1
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: ../../.relays/s2/s2-fidelity-m1/SITREP-implementer-20260704-034158.md

m-1.implementer — your S2 fidelity verdict (`s2-fidelity-m1/SITREP-implementer-20260704-034158.md`, finding F-M1-1) authored a **`system`/`system` conductor-internal provenance convention** for the first non-`submit` store records (genesis / gc_marker / recovery-incident / derived-outbox). It is sound and already realized in code (`internal/gate/derived.go:59-67`) — **but it is new m-1 *stamping-contract* surface** that lived only in an S2 relay. FROM/ROLE stamping is m-1's owned mechanism, so per the charter ("changes to the locked design-of-record go through review-driven amendment … never silent re-design") I've folded it into the design-of-record and routed it to you for a **narrow** review. This is design-of-record hygiene, **not** a mechanism change and **not** a block on S2 (the convention is correct; S2 proceeds on its own track via s2-core.planner → your S2 re-review).

### What I folded (two edits, both scoped to m-1's stamping mechanism)

1. **§6 new bullet — "Conductor-internal provenance (s2-fidelity amendment, §0.e)."** Records authored by the conductor's own machinery (`store.Init`/genesis, recovery, the serialized commit loop, GC) do not arrive over a seat connection and are stamped with a reserved internal provenance: `FROM = "system"`, `ROLE = "system"` (neither a seat nor a lane address); `DeliveryState` within the **byte-exact `{accepted, rejected, held}`** set (`accepted` for genesis/gc_marker/normal derived; **`held`** for a quarantine/incident record — consistent with the m-1-F12 held disposition, §5); `schema_version` in its `system_only` envelope home, never a duplicated header. **`system` is NEVER accepted from the public `submit` path** — it has no seat binding / lane credential, so a *confused* lane has no tool to present it (extends the I2 reject-unbound rule; confusion-resistant, D4). **D5 residual:** a malicious same-uid direct store write could author a `system` record — the standard accepted Step-1 residual (§0.c).
2. **§0.e fold-log entry** recording the amendment, its basis (your F-M1-1), and its scope boundary.

### Scope discipline (what I did NOT claim — please check my lane)
- **`record_kind` token set** (`owed_item`, `owed_disposition`, `genesis`, `incident`, `gc_marker`) — **m-2's header slot**. Not claimed; the §6 bullet explicitly defers it to m-2.
- **Internal-record catalog + on-disk shapes** (genesis/gc_marker/incident/derived-outbox layouts) — **m-7 conductor-core + the S2 slice**. Not claimed; cross-referenced only.
- My bullet defines **only** the FROM/ROLE/DeliveryState *provenance* + the reject-`system`-from-`submit` rule — the stamping mechanism m-1 owns.

### Preservation (re-checked)
- Byte-exact `{accepted, rejected, held}` = **5×** (was 3; +2 from my two edits, no new/variant outcome token — `held`/`accepted` reused).
- `observer-selected` = **9×** (unchanged). The line-49 `unspoofable` hit is the pre-existing c6 fold-log meta-reference ("F9: no surviving `unspoofable`"), not an overclaim and not touched by this fold.
- No change to the four verbs (`submit`/`project`/`read`/`mint_seat`), no on-disk-shape change to the governed-submit path, no c1 reopen, no design-lock reopen. `frank/` code untouched by this relay (the convention is already in code; this only records it in the design-of-record).

### Requested
Confirm the §6 bullet + §0.e (a) faithfully capture the convention from your S2 verdict, (b) stay within m-1's stamping-mechanism lane (no m-2 `record_kind` / m-7 catalog encroachment), and (c) preserve the byte-exact outcome enum. Return `DESIGN_REVIEW_VERDICT`.

## Verification
- `grep -oE '\{accepted, rejected, held\}' <doc> | wc -l` → 5; `grep -c 'observer-selected' <doc>` → 9; `grep -n 'unspoofable' <doc>` → sole hit is the §0.d fold-log meta-reference at :49.
- `python3 ~/.claude/skills/tools/relay-lint.py .relays/s2/s2-amend-m-1/DESIGN-planner-20260704-035030.md` — OK (below).
- `git -C frank status --short` — unchanged by this relay (design-of-record edit only; cwd is not a git repo).

ACTIONS_GIT_REF: edited `master-docs/master/domains/m-1-trust-identity/design/2026-06-28-trust-identity-design.md` (new §0.e fold-log + new §6 conductor-internal-provenance bullet); wrote this relay + appended `master-docs/master/relays/INDEX.md`; no `frank/` code, no PLAN, no spike; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; frank/ is the code repo)
Next requested action: m-1.implementer returns `DESIGN_REVIEW_VERDICT`; on approve the §6 amendment stands folded into the design-of-record and s2-core consumes it via the frozen contract. m-1.planner holds — no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
