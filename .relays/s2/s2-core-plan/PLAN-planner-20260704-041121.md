## PLAN — gated PLAN r5: both narrow-review findings folded (state labels + spec-of-record pointer); narrow re-review re-requested (supersedes DISPATCH_ID s2-core-plan-lock-r4)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s2-core-plan-lock-r5
PARENT_DISPATCH_ID: s2-core-design-r2-review-implementer
RUN_ID: s2
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_LOCK_ID: s2-slice-2-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s2-slice-2-plan
IN_REPLY_TO: s2-core-plan/PLAN-REVIEW-implementer-20260704-040358.md
FROM: s2-core.planner
TO: s2-core.implementer
CC: s2.orchestrator-planner, operator
SUBJECT: narrow re-review — design r4 + plan r4 at main@845a7d1 fold your F1 (stale spec-of-record/review-object pointers) + F2 (over-finalized m-1 labels); m-1 fold mechanics untouched from r3; your approve parented to THIS id becomes the dispatch parent

Both findings verified correct by me before folding (F1: the plan header + Task 13.2 still cited pre-fold main@6e3b67f — a downstream packet could cite the wrong design version; F2: §4 said "m-1-APPROVED" while m-1's narrow re-review confirm doesn't exist yet — our own gate-state wording violated the claim-honesty discipline the doc enforces elsewhere). r4 = **main@845a7d1** (docs only; the r3 fold mechanics at da25aec untouched):

1. **F1 fold:** plan spec-of-record now names the current folded design rev with both anchors explicit (substantive m-1 folds = r3 @ main@da25aec; the r2 pair design-review approve on design r2 = main@6e3b67f **remains the lineage-gate parent** — review surface moves with folds, lineage parent does not); Task 13.2 re-aimed: initial m-1 verdict RECEIVED+FOLDED, remaining = the m-1 NARROW re-review of the folded lines (post-fold review object named), approve on record required pre-dispatch.
2. **F2 fold:** design §4 header + fold-log + all in-body labels normalized — ✔ items carry the **m-1-PRESCRIBED shape verbatim, pending m-1's narrow re-review confirm** (zero "m-1-approved"/"m-1-FIXED" labels remain — rg-verified); design status line carries the r4 entry + the explicit PENDING line.

Requested: your narrow `PHASE: PLAN-REVIEW` parented to `s2-core-plan-lock-r5`, scoped to the two label/pointer folds (the r3 mechanics you already traced stand unchanged). On approve: fold-completion SITREP to s2.orchestrator-planner → m-1 narrow re-review packet → m-1 approve → SCOPE_DIFF → delegated dispatch parenting your approving review. No dispatch token here.

ACTIONS_GIT_REF: main@845a7d1 (r4 label-fix commit, docs only; r3 fold = da25aec); this relay + its INDEX row under gitignored .relays/ (`.gitignore:1`)
FINAL_GIT_STATUS_SHORT: none — clean tree
