## PLAN — s2-slice-2-plan r1 locked to design r2 + PLAN-REVIEW request

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s2-core-plan-lock
PARENT_DISPATCH_ID: s2-core-design-r2-review-implementer
RUN_ID: s2
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_LOCK_ID: s2-slice-2-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s2-slice-2-plan
IN_REPLY_TO: s2-core-plan/PLAN-orchestrator-planner-20260704-023128.md
FROM: s2-core.planner
TO: s2-core.implementer
CC: s2.orchestrator-planner, operator
SUBJECT: PLAN-REVIEW request — s2-slice-2-plan r1 at main@7d9562c, locked to design r2 (DESIGN_LOCK_ID s2-slice-2-design, referencing GRILL_LOCK_ID s2-grill-s2-core); verdict requested; dispatch remains blocked on m-1 fidelity regardless of your verdict

**Lineage (design-review discipline):** this gated PLAN carries `DESIGN_LOCK_ID: s2-slice-2-design` + `DESIGN_RECORD_KIND: design-doc` and parents to the approving DESIGN-REVIEW (`s2-core-design-r2-review-implementer`, verdict approve, same DESIGN_DOC_ID). The design lock references `GRILL_LOCK_ID: s2-grill-s2-core` (design §8) per the PROCEED-TO-PLAN requirement. Authorizing sequencing relay: `s2-core-plan/PLAN-orchestrator-planner-20260704-023128.md`.

**The plan:** `docs/sprints/2026-07-03-s2-slice-2/plans/s2-slice-2-plan.md` r1 at main@7d9562c — 12 build tasks + the Task-13 process tail, house style (task-per-step, red→green commands, exact interfaces). Locked design, locked scope, file targets, boundary contract (design §5 carried), fixture-keyed acceptance criteria (all 19 S2-* ids task-mapped), out-of-scope fence, anti-half-fix guards (S1-assertion byte-identity; crashpoint name+site same-commit rule; claim pins as blockers-by-construction).

**Coverage highlights for your review:**
- PROCEED-TO-PLAN deliverable items 1–6 all present: fixture-to-task map (header) · SCOPE_DIFF file list (File structure, the decomposition of record) · crashpoint finalization per owning task + the applicability map as a reviewed committed artifact with owed-item/owed-disposition as explicit named rows (Task 12) · the S1 migration inventory with byte-identical-assertion rule (Task 7) · the operator's OI-S1-F11-SWEEP submit sequenced as an operator-executed IMPL step (Task 13.5, via the Task-11 `-operator-submit` client mode) · commit discipline (Global constraints).
- Dispatch conditions restated as plan gates: m-1 fidelity approve blocks dispatch (Task 13.2); README.md excluded from the file list, ASK routed instead (Task 13.3 — S1 ASK-1 precedent).
- Design fidelity: every D-1..D-9 decision has an implementing task; the r2 folds are load-bearing (Task 7 = the capability split with both-halves fixtures; Task 8 = the closed durable-fact source pair verbatim).

**Known judgment calls for your adversarial eye (surfaced, not hidden):**
1. Task 9 accepts `owed_item` submits from ANY seat (engine imposes no authorship gate) — the OI record's operator authorship is realized as a process step (Task 13.5), not an engine restriction; I read the guide's Q2 as provenance-honesty (record the real holder), not an engine-level operator-only rule, and an engine gate would fake an authority distinction the design doesn't own. Challenge if you read Q2 stricter.
2. Task 4 makes a legacy single-file `journal/intake.jsonl` a typed fail-closed error rather than silent migration (no real store exists — grill codebase row); challenge if you want silent-create-alongside.
3. Task 11 adds the `-operator-submit` client mode (smallest honest realization of the operator step: existing `DialAuthenticated`, no new verb/protocol); challenge if that's scope you'd rather see escalated.

Please return `PHASE: PLAN-REVIEW` parented to THIS relay with `PLAN_REVIEW` verdict (approve | must-revise). Even on approve, NO dispatch until the m-1 fidelity approve is on record (`s2-fidelity-m1`) + SCOPE_DIFF all-in per the standing conditions.

ACTIONS_GIT_REF: main@7d9562c (plan r1 commit, doc only); this relay + its INDEX row under gitignored .relays/ (`.gitignore:1`)
FINAL_GIT_STATUS_SHORT: none — clean tree
