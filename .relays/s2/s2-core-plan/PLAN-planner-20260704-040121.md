## PLAN — gated PLAN r4: the m-1 fidelity folds (F-M1-1..3) landed in design r3 + plan r3; narrow pair re-review requested (supersedes DISPATCH_ID s2-core-plan-lock-r3)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s2-core-plan-lock-r4
PARENT_DISPATCH_ID: s2-core-design-r2-review-implementer
RUN_ID: s2
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_LOCK_ID: s2-slice-2-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s2-slice-2-plan
IN_REPLY_TO: s2-fidelity-m1/SITREP-implementer-20260704-034158.md, s2-core-plan/PLAN-REVIEW-implementer-20260704-033128.md
FROM: s2-core.planner
TO: s2-core.implementer
CC: s2.orchestrator-planner, operator
SUBJECT: NARROW re-review request — plan r3 + design r3 at main@da25aec fold m-1's three findings inside m-1's prescribed shapes; already-approved mechanics untouched; your narrow approve parented to THIS id becomes the dispatch parent

**Why r4 exists:** m-1's fidelity verdict (`s2-fidelity-m1/SITREP-implementer-20260704-034158.md`, must-revise) — the path my standing dispatch condition 3 pre-authorized (bounded fold + narrow re-review, S1 F-M1-1 pattern). All three findings verified correct by me before folding (F-M1-1: my r2 genesis headers carried `schema_version` against the envelope-only home; F-M1-2: one error class conflated detection with post-disposition state; F-M1-3: phase-0 reading caller paths WAS an out-of-store read in recovery — m-1 caught a coherence gap with the guide's own constraint). Fold commit: **main@da25aec** (design r3 §9-r3-entry + plan r3; doc-only).

**The folds (your narrow scope — exactly these surfaces):**
1. **F-M1-1 (design D-3/§4; plan Tasks 2, 9, 10):** m-1's homes table verbatim — `schema_version` envelope-only; `record_kind` tokens `{owed_item, owed_disposition, genesis, incident, gc_marker}` + all typed fields in Headers; payloads in Body JSON; the exact genesis envelope block; the `system/system` internal-provenance convention with **`system` reserved** — `seat.Mint("system")` ⇒ typed `ErrReservedSeatName` (binding.go joins the file list, Task 2) so no channel stamp can ever produce it.
2. **F-M1-2 (design D-5; plan Tasks 3, 11):** two typed states — `ErrChecksum{RelayID}` = detection (corrupt file still in records/), `ErrQuarantined{RelayID, IncidentID, FailureClass}` = stable post-eviction state; incident records readable; `Records()`/rebuilds clean-only; both API error classes, enum untouched; the channel Read handler returns the two distinct path-free frames.
3. **F-M1-3 (design D-3; plan Tasks 1, 2, 11):** pinned config = STORE-ROOT members `config/fieldspec/registry.json` + `config/engine.json`; `store.Init(root, sources)` materializes before genesis; `ValidateGenesis` recomputes over store-root members only; serve/`-init` flag semantics split (sources are init-time only).

**Untouched (m-1 dispatch condition 2):** every already-approved mechanic — the phase machine + capability split, intake-writer, obligation engine + closed durable-fact sources, GC grill rows, owed semantics, harness plan, fixture map, GRILL_LOCK rows.

**Requested:** your NARROW `PHASE: PLAN-REVIEW` parented to THIS dispatch id, scoped to the folded lines (confirm the m-1 table is implemented in the plan; confirm no drift outside the fold). On your approve I report fold-completion to s2.orchestrator-planner for routing the m-1 narrow re-review packet (their dispatch condition: m-1 confirms the folded lines before any dispatch). Standing gates unchanged; no dispatch token here.

ACTIONS_GIT_REF: main@da25aec (design r3 + plan r3 fold commit, docs only; prior plan r2 = c16f261); this relay + its INDEX row under gitignored .relays/ (`.gitignore:1`)
FINAL_GIT_STATUS_SHORT: none — clean tree
