## SITREP — dispatch-precondition question routed: disposition of the superseded r2 PLAN's root-mode lint residue (current-chain clean vs full-root clean)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s2-core-plan
PARENT_DISPATCH_ID: s2-core-plan-review-implementer-r3
RUN_ID: s2
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the lint-residue disposition (orchestrator ruling or operator waiver) before delegated dispatch
IN_REPLY_TO: s2-core-plan/SITREP-implementer-20260704-033312.md
FROM: s2-core.planner
TO: s2.orchestrator-planner
CC: operator, s2-core.implementer
SUBJECT: r3 approve is on record and lineage-clean; the implementer's caveat (superseded r2 PLAN still errors in root mode) needs YOUR disposition before I treat the lint gate as satisfied; m-1 fidelity remains the other open gate

State (all E1/E2, read/run this session):
- Re-affirming PLAN-REVIEW approve on record: `s2-core-plan/PLAN-REVIEW-implementer-20260704-033128.md` (`s2-core-plan-review-implementer-r3`, parents my r3 gated PLAN, which parents the approving DESIGN-REVIEW — the full lineage-gate chain resolves; both r3 files exact-file AND root-mode OK).
- Residue (implementer caveat `…-033312.md`, my own root-mode run concurs — E2): `relay-lint --relay-root .relays/s2` still errors on the SUPERSEDED r2 gated PLAN (`s2-core-plan/PLAN-planner-20260704-030751.md`, design-lock parent edge — the defect your fix relay named). The file stays on disk per append-only discipline; nothing consumes it; my dispatch chain runs r3-only.

The question (not the pair's to answer): does the delegated-dispatch lint gate require FULL root-mode cleanliness, or current-chain cleanliness with the superseded relay explicitly dispositioned?

My reading, for your confirmation rather than my action: your fix relay (`…-032900.md` step 3) required "the r3 PAIR must be root-mode clean (INDEX noise excepted)" — which it is; the residue sits only on the superseded file. That wording suggests current-chain was your intended gate, with the r2 file's error being exactly what supersession looks like in an append-only root. If you confirm that reading (a ruling line suffices — no operator waiver needed under it), the lint gate is satisfied on record. If instead you require full-root cleanliness, the S1 precedent applies: an explicit operator waiver line for the named file (the s1-exit-gate lint-flag pattern), since rewriting or removing the superseded relay would break the append-only trail.

Standing state otherwise: m-1 fidelity verdict still pending (`s2-fidelity-m1/` = request only) — the substantive gate. On {your lint disposition + m-1 approve}: I run the mechanical SCOPE_DIFF (README row citing your fence ruling `…-032200.md`) and, on all-in, issue the delegated `DISPATCH IMPL` parenting `s2-core-plan-review-implementer-r3`. No dispatch token in this relay.

ACTIONS_GIT_REF: none — report-only routing; this relay + its INDEX row under gitignored .relays/ (`.gitignore:1`); no tracked-file edit
FINAL_GIT_STATUS_SHORT: none — clean tree
