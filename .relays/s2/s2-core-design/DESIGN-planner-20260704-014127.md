## DESIGN — s2-slice-2-design r1 authored (grill run, GRILL_LOCK folded) + DESIGN-REVIEW request

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s2-core-design-r1-review
PARENT_DISPATCH_ID: s2-core-design
RUN_ID: s2
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s2-slice-2-design
FROM: s2-core.planner
TO: s2-core.implementer
CC: s2.orchestrator-planner, operator
SUBJECT: DESIGN-REVIEW request — s2-slice-2-design r1 at main@594259a; grill run with the operator (GRILL_LOCK s2-grill-s2-core folded at §8); verdict requested per the lineage-gate discipline

The S2 design doc is authored and committed: `docs/sprints/2026-07-03-s2-slice-2/designs/s2-slice-2-design.md` (r1, main@594259a). This relay is the mechanical Template-I DESIGN-REVIEW request: please return a `PHASE: DESIGN-REVIEW` relay parented to THIS relay, carrying the same `DESIGN_DOC_ID: s2-slice-2-design`, with `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`.

**What the doc contains (map for your hunt):**
- D-1 reified recovery phase machine 0–4 (structural no-authority-before-open via a `Ready` capability; phase-boundary crashpoints; recovery reads only the store)
- D-2 single intake-writer + segmented FIFO (closes convergent finding F-2; 4 MiB rotation default — operator-grilled)
- D-3 genesis + pinned config (guide Q1=(a) + all four sharpenings; deterministic manifest digest; S2 config fixed at store creation)
- D-4 phase-0 diagnostics disposition (not error-exit)
- D-5 quarantine replaces fail-stop (closes convergent finding F-1, live path swept; crash-window healed by the obligation mechanism)
- D-6 canonical-sufficiency rule + runtime tables (new drafting-surfaced S1 finding: the outbox record is not canonical-sufficient — its item payload lives only in the redo intent; the fix licenses redo-segment GC)
- D-7 the generalized obligation projection, owed-item as the no-auto-completer instance (guide Q2=(i) + sharpenings; OI-S1-F11-SWEEP end-to-end)
- D-8 GC drained-segments-only, off-by-default (operator GRILL rows 1–2, quoted verbatim in §8); render compaction documented NOT touched
- D-9 harness extension with the class×point applicability map
- §3 fixture table S2-* mapped to gate lines G1–G6; §4 the 10-item m-1 proposal surface (extractable fidelity packet); §8 GRILL_LOCK `s2-grill-s2-core`; §9 claim-sweep note

**Grill compliance (dispatch r2):** the operator grill ran 2026-07-04 before this request; the two operator-owned decisions (GC scope = journals only + render-copies-not-touched documented; off-by-default + 4 MiB) are locked in §8 with verbatim quotes; Q1/Q2 entered as resolved-by-guide rows; codebase-answerable questions (no-grandfathering, quarantine location, config-in-digest) answered from code/locked text per the skill. Per the dispatch, no DESIGN_LOCK_ID is claimed yet — the lock is assembled after your review verdict, referencing GRILL_LOCK_ID s2-grill-s2-core.

**Suggested review lenses (your call):** locked-text fidelity of every D-section cite (m-7 :52/:56-58/:89-95/:109-111/:134-137/:172; m-1 §5/§6); whether any D-section authors policy that belongs to m-1/m-7 (the §4 PROPOSAL discipline held everywhere?); claim-boundary regressions (§9 sweep classes); the canonical-sufficiency finding's correctness (derived.go:101-114 — is the redo-intent-only payload claim right?); the applicability-map leg logic (does clean-completion-leg coverage honestly discharge "full class×point"?); under-specified seams an implementer couldn't build to; fixture-table bite vs the six gate lines.

**Hard constraints you should verify are satisfied or explicitly rejected-with-reason:** the nine from dispatch r1 (`DESIGN-orchestrator-planner-20260704-004400.md` §Hard constraints, binding by reference from r2) + the eight guide sharpenings (de-provision supplement `SITREP-orchestrator-planner-20260704-005315.md`).

ACTIONS_GIT_REF: docs/sprints/2026-07-03-s2-slice-2/designs/s2-slice-2-design.md created at main@594259a (my commit this session, message prefix "s2 DESIGN:"); this relay + its INDEX row under gitignored .relays/
FINAL_GIT_STATUS_SHORT: none — clean tree (post-commit `git status --short` empty)
