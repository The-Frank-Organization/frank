## DESIGN — s2-slice-2-design r2 (both must-revise findings folded) + DESIGN-REVIEW re-request

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s2-core-design-r2-review
PARENT_DISPATCH_ID: s2-core-design-r1-review-implementer
RUN_ID: s2
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
DESIGN_DOC_ID: s2-slice-2-design
IN_REPLY_TO: s2-core-design/DESIGN-REVIEW-implementer-20260704-014646.md
FROM: s2-core.planner
TO: s2-core.implementer
CC: s2.orchestrator-planner, operator
SUBJECT: re-review request — r2 at main@6e3b67f folds F1 (Ready/Diagnostics two-capability split) + F2 (durable-store-fact obligation source, closed enumeration); carry-forwards adopted; same DESIGN_DOC_ID

Both r1 findings were verified correct by me before folding (F1: my D-1 and D-4 did contradict each other against m-7 :89-95; F2: my own D-5/D-8 instances used the file-backed intents my D-7 definition excluded). r2 = `docs/sprints/2026-07-03-s2-slice-2/designs/s2-slice-2-design.md` at main@6e3b67f; fold-log at §9.

**The folds, for your re-check:**
1. **F1 → D-1 two-capability split:** `recover.Run` returns mode-typed `*engine.Ready` (all phases) or `*engine.Diagnostics` (phase-0 failure). `Submit`/intake-writer/commit-loop require `Ready` (unconstructible otherwise); the read-only registry (`project`/`read` + operator diagnostics) is constructible from either terminal state; channels open only in a terminal state, with m-7 :95's phases-1–3 "may serve reads" honored as permissive and exercised via the two terminal capabilities only (stated). D-4 names the capability; S2-V3 asserts BOTH halves in one fixture (read surface alive AND submit/writer/loop unconstructed); S2-PM2 names the split.
2. **F2 → D-7 source = durable store fact:** committed record is the normal case; the two file-backed intents (quarantine/ member; GC-marker naming still-present segments) are explicitly enumerated and the enumeration is CLOSED — a third non-record source is a design change, not an extension point.
3. **Carry-forwards adopted:** D-9 requires owed-item/owed-disposition as explicit named rows in the applicability-map artifact (mapping to the submit-accept path, visible, never folded into the broad class name); the §4 m-1 fidelity gate unchanged-hard (§6.3).

No other section changed; §8 GRILL_LOCK rows and every operator/guide decision untouched. Please return a `PHASE: DESIGN-REVIEW` relay parented to THIS relay, same `DESIGN_DOC_ID: s2-slice-2-design`, with your verdict.

ACTIONS_GIT_REF: design r2 = main@6e3b67f (r1 = 594259a; amend-fixed section ref); this relay + INDEX row under gitignored .relays/
FINAL_GIT_STATUS_SHORT: none — clean tree (post-commit `git status --short` empty)
