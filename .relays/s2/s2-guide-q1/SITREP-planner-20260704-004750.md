## SITREP — m-7 guide → s2: both readings CONFIRMED from locked text (Q1 = (a), pin-what-exists; Q2 = (i), operator-authored), each with the sharpenings that keep S3+ additive

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s2-guide-q1
PARENT_DISPATCH_ID: s1-dispatch
RUN_ID: master
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: .relays/s2/s2-guide-q1/SITREP-orchestrator-planner-20260704-004330.md
FROM: m-7.planner
TO: s2.orchestrator-planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.implementer
SUBJECT: Q1 — genesis pins a single digest over what exists; the CQ-4b composition arrives later via §7's config-change record, and NO section may carry an author who didn't author it. Q2 — the operator authors the owed-record via the operator channel; the decisive m-7 reason is that recovery may read ONLY the store

No lock amendment is needed for either answer; both come off the locked lines re-read this session (`master-docs/master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md:89-95, :109-111, :136`). De-provision both design sections on this relay.

### Q1 — CONFIRMED: (a). Genesis pins a single top-level digest over the config artifacts that exist at S2.

The locked §7/§10 properties at stake are exactly three, and (a) preserves all of them: **one top-level digest** in the genesis record (`:136`), **Phase-0 digest-match fail-closed** (`:90`, `:110`), and **config evolution = a committed store record carrying the new digest** (`:109`, the auditable append-only chain). The CQ-4b composed shape (per-domain sections + m-4 per-section version stamps) is a property of the artifact *once multiple domain-authored sections exist* — it is not a genesis-time format obligation, and §7's config-change record is precisely the sanctioned upgrade path for the artifact to grow into it at S3/S4. (b) is wrong for the reason you gave, which I'll state at its locked grain: the m-5 CQ-4b confirm fixed the section grain as **authored and stamped by its domain** — `registry.json` is data in the m-2-*locked shape*, but the m-2 pair did not author it; stamping a section "m-2-attributed" would fabricate exactly the per-domain change-attribution the section stamps exist to make trustworthy. Four sharpenings:

1. **Honest attribution for what IS pinned:** describe S2's pinned artifact as operator-ratified build config *in the m-2-locked shape* (source: the gated S1/S2 plans). No domain-author stamp until a domain authors a section through the master-team path.
2. **Phase-0 disposition, not error-exit:** mismatch/missing genesis ⇒ **serve read-only diagnostics, accept nothing, summon operator** (`:90`). Implement that disposition; a process that just exits non-zero fails the locked line (fail-closed ≠ fail-stop-silent — and note the neighbor rule at `:91`: quarantine, never brick).
3. **Deterministic digest input:** define the digest over a canonical serialization of the pinned artifact set (stable ordering, named members), so the S3 config-change record can extend the member set without ambiguity about what the old digest covered.
4. **Claim scope:** S2 may claim load-once / digest-verified / Phase-0-fail-closed **for the pinned artifacts only**, and must state that the CQ-4b composed artifact + per-section stamps land with the consumer sections (S3/S4). SWEEP the wording.

Yes — m-1 gets the genesis-record *layout* half in your fidelity packet; the genesis semantics above are m-7's and are answered here.

### Q2 — CONFIRMED: (i). The operator authors the OI-S1-F11-SWEEP owed-record through the operator channel.

Your provenance argument is right; here is the **decisive m-7-side reason, which is load-bearing beyond taste: recovery may read only the store.** The locked recovery machine (`:89-95`) derives everything from committed records — burn set from verdict-record presence, parked lanes from store rows, FIFO from `intake − outcomes`. Shape (ii) would put a **docs-file read inside the trusted recovery/genesis path** and have the engine mint an obligation record from out-of-store text: that breaks dumb-replay idempotence (recovery output would depend on a mutable file outside the store), violates the store-is-truth grain, and — the CQ-1-style honesty point — would fake a provenance the system doesn't have. An owed item is an obligation somebody holds; the record's `FROM` should be the holder.

Sharpenings:
1. **Channel + stamp:** `FROM=operator` on the operator-relay channel — the locked m-1 §6 first-class-operator model, the same authoring answer the c6.1 template-spawn convergence settled for records that predate any seat's authority. Not a synthetic system stamp.
2. **Payload = the typed record:** `{owner, source, target surface, disposition path}` with `source` citing both the s1 ledger entry (RECONCILE.md :160-161) and my deviation-1 ruling (`.relays/s1/s1-exit-gate/SITREP-planner-20260703-200827.md`), `target surface` = the F11 full class×point sweep on the existing harness, `disposition path` = the S2 exit gate.
3. **Projection semantics, VP-corrected scope verbatim:** `open = owed-record with no disposition-record`; the projection makes silent drop impossible for a **recorded** item only — materializing is an intake/triage act by a record-authoring principal, which is exactly *why* (ii) is wrong: the engine transcribing ledgers would blur the one boundary that makes the projection's guarantee honest. Your fixture plan (surfaces open → closes at the S2 exit gate with a disposition record) is the right proof.
4. **Sequencing note:** the submit happens during S2 IMPL once the operator channel is up — the owed-item *projection* must not gate on the record pre-existing genesis (an empty owed-projection at genesis is correct, not a bug).

Not authorized / not claimed: answers restate locked lines with citations; no lock amendment, no scope change, no `DISPATCH IMPL`, no code, no `frank/` write by this relay.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master-docs/master/relays .relays/s2/s2-guide-q1/SITREP-planner-20260704-004750.md` — run before handoff (result in inline pointer)
- locked lines re-read this session: m-7 design `:89-95` (recovery/Phase-0), `:109-111` (§7 config + CQ-4b + change-record path), `:136` (genesis contents); m-5 CQ-4b confirm grain per §7's recorded OBTAINED pointer
- `git -C frank status --short` — untouched by this relay

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260704-004750; no doc/design edits; no code/`frank/` writes
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` not written by this relay)
Next requested action: operator carries this to the s2 session; s2 de-provisions both design sections against these answers; the genesis-layout half rides the m-1 fidelity packet as planned.
