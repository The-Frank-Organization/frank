## SITREP — Task-13.5 operator execution kit: the OI-S1-F11-SWEEP submit (round-3 mint fold verified closed; the build is done — this step is yours)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s2-core-impl
PARENT_DISPATCH_ID: s2-core-review-r3-fold-report
RUN_ID: s2
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — this relay's whole content is the operator-executed step
IN_REPLY_TO: s2-core-impl/REVIEW-FOLD-implementer-20260704-144230.md
FROM: s2-core.planner
TO: operator
CC: s2.orchestrator-planner, s2-core.implementer
SUBJECT: round-3 verified closed at s2-core-impl@7e5d1a3 (my battery/vet + condition checks, E2); below: the exact commands + payload files for YOUR OI-S1-F11-SWEEP submit; hand me back the outcome line and I run the exit-gate pass

**Round-3 verification (recorded):** diff = exactly the two scoped files; admin-time guard live ("conductor is serving; -mint is admin-time only"); reserved-name + duplicate-mint + stdout-once + no-registry-entry legs in the fixture; battery green + vet clean at 7e5d1a3 — my runs. The pair's build work for S2 is COMPLETE pending your step + the gate.

### Your step (guide Q2=(i): the operator authors the owed record via the operator channel — never the pair)

Work from the implementation worktree; pick a store path OUTSIDE any repo (e.g. `$HOME/frank-s2-store`).

```bash
cd ~/.config/superpowers/worktrees/frank/s2-core-impl
STORE=$HOME/frank-s2-store

# 0. engine config (grill row 2 defaults)
cat > /tmp/engine.json <<'EOF'
{"gc_enabled": false, "segment_rotate_bytes": 4194304}
EOF

# 1. init (materializes config under the store root, writes genesis)
go run ./cmd/frank -root "$STORE" -init -registry internal/fieldspec/registry.json -engine-config /tmp/engine.json

# 2. mint YOUR operator credential (printed ONCE — copy it)
go run ./cmd/frank -root "$STORE" -mint operator -role operator -operator

# 3. serve (leave running in this terminal; do steps 4+ in a second one)
go run ./cmd/frank -root "$STORE"

# 4. the payload file — the typed owed record, guide-confirmed shape
cat > /tmp/oi-s1-f11-sweep.json <<'EOF'
{
  "envelope": {"to": "operator", "dispatch_id": "oi-s1-f11-sweep"},
  "headers": {
    "PHASE": "SITREP",
    "AUTHORITY": "report-only",
    "SUBJECT": "OI-S1-F11-SWEEP",
    "record_kind": "owed_item",
    "owner": "s1 (S2 slice)",
    "source": "docs/sprints/2026-07-03-s1-slice-1/RECONCILE.md:160-161 + .relays/s1/s1-exit-gate/SITREP-planner-20260703-200827.md",
    "target_surface": "F11 full class×point sweep",
    "disposition_path": "S2 exit gate"
  }
}
EOF

# 5. YOUR submit (paste the step-2 credential)
cd ~/.config/superpowers/worktrees/frank/s2-core-impl
go run ./cmd/frank -root "$STORE" -operator-submit /tmp/oi-s1-f11-sweep.json -credential <PASTE-CREDENTIAL>
```

**Hand back to me:** the printed outcome JSON (it carries `state` + `relay_id`) and the store path. Expected: `state: accepted`; the item then shows in `$STORE/projections/owed/OPEN.md`.

**Then, in order (mine → yours → mine):**
1. I run the S2 exit-gate pass at 7e5d1a3 (full fixture set, my E2), copy your committed owed record into `docs/sprints/2026-07-03-s2-slice-2/results/`, and confirm the gate report is final.
2. YOU submit the disposition (same commands, second payload — I will hand you the file with `disposes_owed` filled with your step-5 relay_id and the body citing `results/f11-sweep-report.md`). The open set must empty.
3. I copy the disposition record into results/, commit the evidence, and produce the exit-gate report + the master SITREP draft for the orchestrator. Merge/S2-close remain your separate gate (the orchestrator's MERGE-GATE relay will carry the branch → main mechanical truth).

ACTIONS_GIT_REF: none — verification + instruction relay; no source edits by me; reviewed 7e5d1a3 (fold commit, implementer-authored, scope-verified); this relay + its INDEX row under gitignored .relays/ (`.gitignore:1`)
FINAL_GIT_STATUS_SHORT: none — clean tree (main checkout; worktree clean at 7e5d1a3)
