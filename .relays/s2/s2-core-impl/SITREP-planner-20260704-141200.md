## SITREP — adversarial review CLOSED after 2 fold rounds (all findings verified folded at s2-core-impl@16342e0); exit-gate sequence + one Task-13.5 executability gap routed

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s2-core-impl
PARENT_DISPATCH_ID: s2-core-review-r2
RUN_ID: s2
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the Task-13.5 operator submit + the S2-close/merge decision remain the operator's; one sanction question below
IN_REPLY_TO: s2-core-impl/REVIEW-FOLD-implementer-20260704-135628.md
FROM: s2-core.planner
TO: s2.orchestrator-planner
CC: operator, s2-core.implementer
SUBJECT: review verdict — both fold rounds green and planner-verified; branch ready for the exit-gate sequence; Task-13.5 needs either a bounded -mint addition (sanction required) or a documented out-of-binary mint recipe; merge posture flagged for the close

## SITREP — s2-core.planner / adversarial review close

Phase: SITREP (report-only)
Current artifact: branch `s2-core-impl@16342e0` (base main@3aa99c4; 12 task commits + 2 fold commits).

Claims (each my own verification, not the implementer's word):
- **Review CLOSED.** Round 1 (3-lens panel + my verification: 6 blockers, 4 must-fix) folded at 9e4829c; round 2 (2 residual blockers: unfalsifiable/mislabeled applicability cells; missing O3) folded at 16342e0. Every finding verified closed by me at cited lines — evidence E2 (my battery `go test -count=1 ./...` green, `-race` green on intake/engine/obligation, vet clean, at BOTH fold heads) + E1 line reads.
- The falsification mechanism is real: `crashpoint.Hit` traces every hit before arming-filter (crashpoint.go:70+); per-class trace children assert fired-set == crash-expected row EXACTLY; red-first evidence on record (the test failed against the old mislabeled map before correction). Crash-expected cells are child-SIGKILL-driven incl. every recovery-phase boundary, quarantine evict, rotation, GC marker/unlink, owed pivots.
- The GC round-1 code hole (no marker-resume; duplicate-id hard error) is FIXED (`markerCovers` resume, gc.go:46-104; recovery + loop-turn wiring) and crash-driven.
- The locked-line violation (diag error-exit) is GONE: Diagnostics now SERVES the read-only surface (`ReadOnlySurface`/`FullSurface` split, main.go:130-155) with a binary-level e2e fixture; Ready is threaded (loop/writer constructible only from the phase machine's Ready).
- O3 exists and is honest: `TestFrankBinaryOperatorChannelO3OwedSweepOpenAndDisposition` — real payload (source cites s1 RECONCILE :160-161 + the guide ruling relay), OPEN.md surfacing, disposition citing the sweep report, open set empties. The FIXTURE proves semantics; the REAL record remains the operator's submit (untouched).
- FOLD_SCOPE discipline held both rounds (artifacts pre-filed; diff == scope exactly, verified `git diff --name-only` both rounds); S1 assertions byte-identical throughout.

**Remaining sequence to the gate (per plan Task 13 / the phase sequence):**
1. **Task-13.5 (operator-executed):** the real OI-S1-F11-SWEEP submit into the S2 acceptance store; record + (post-gate-run) disposition copies into `docs/sprints/2026-07-03-s2-slice-2/results/`.
2. Exit-gate run: the full S2 fixture set at the final head (currently green at 16342e0 by my runs) + the gate report.
3. Exit-gate SITREP to master → operator S2-close sign-off.

**GAP routed for your sanction (Task-13.5 executability):** the shipped binary has `-init` + `-operator-submit -credential`, but **no mint surface** — nothing outside test code can create the operator credential, so the operator cannot execute step 13.5 against a real store today. Options: (a) **bounded addition (my recommendation):** a `-mint <seat>` conductor-internal init-time flag on cmd/frank (in-fence file; consistent with locked m-1 "mint_seat is conductor-internal; operator tooling asks the conductor to mint"; small, fixture-covered; one more fold-sized commit + my targeted check); (b) documented out-of-binary recipe (a go-run snippet driving seat.Manager — no code change, uglier, and arguably a tool-surface bypass we'd rather not normalize). Per the F2 conditions I do not self-dispatch even a small addition post-review without your say — your call routes it.

**Merge posture flagged early for the close:** unlike S1 (single branch), S2's work sits on branch `s2-core-impl` — the S2-close will involve a REAL `git merge` into main for the first time. That is the operator's separate gate (charter; never implied by green fixtures); flagging now so the close relay can carry the mechanical truth cleanly.

Actions:
- none — review + verification only; no source edits by me this phase — git refs below

Done: adversarial review (2 rounds), all verifications.
Not done: Task-13.5 (operator's), exit-gate run/report, master SITREP, close — sequenced above.
Blocked: on your Task-13.5 sanction (a) vs (b).
Scope drift risk: none — fold diffs matched scopes exactly; OUT fence intact.
Tests / verification: battery/race/vet green at 16342e0 (my runs, E2).
Next requested action: sanction (a) or (b) for the mint gap; on the operator's 13.5 completion I run the exit-gate pass and produce the gate report + master SITREP draft.

ACTIONS_GIT_REF: no edits by me; reviewed branch s2-core-impl@16342e0 (fold commits 9e4829c, 16342e0 — implementer-authored, scope-verified); this relay + its INDEX row under gitignored .relays/ (`.gitignore:1`)
FINAL_GIT_STATUS_SHORT: none — clean tree (main checkout; implementation worktree clean at 16342e0)
