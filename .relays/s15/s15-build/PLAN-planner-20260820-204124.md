## PLAN — the s15 straight-through build order (the m-9 worker slice): fourteen tasks over the frozen bases, RED-first per task, fakes per MVP amendment §8, one end-of-slice adversarial review; one named out-of-fence seam routed (the cmd/frank-mcp m-2-module factor-out)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s15-build
PARENT_DISPATCH_ID: s15-build-token
RUN_ID: s15
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the terminal gate stays the operator MERGE-GATE (serialized s13 → s14 → s15); nothing requested here
GRILL_REQUIRED: no — GRILL_LOCK step3-mvp-stage4-m9-worker-grill-1 rides via the charter; no new operator choice is opened
FILED_AT_LOCAL: 20260820-204124
IN_REPLY_TO: frank/.relays/s15/s15-build-token/SITREP-planner-20260820-203301.md
FROM: s15.planner
TO: s15.implementer, master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
SUBJECT: the build task order + package layout + encoding decisions; per the ratified straight-through cadence this PLAN does NOT block on review (objections ride async; the binding adversarial gate is the end-of-slice review) — plus ONE routed seam ask on cmd/frank-mcp

## Basis (all previously re-hashed at this seat, zero drift — the readiness twin carries the full 19-alias list)
The three m-9 bases (A-M9-WORKER 70a2378d · A-M9-LIFECYCLE 72796bc5 · A-M9-DELTA 61833f63) AS FOLDED by the ratified D1-r11 layer — the r11 amendment (stamped-current c388f7dd over registered 7147a0c2, preimage machine-verified at de4dd0a under the ratified retirement-fold convention), the one-file writer (1e85e92e, hash-verified this act) and the checksum-reader (d803232c, hash-verified this act) — plus the consumed sibling contracts read lazily at their tasks. m-9's three opening notes bind: LIVE text at fold sites; the lifecycle :139 fresh-turn clause is NOT implemented (the folded worker text + D1-r11 govern); GRILL_LOCK §12 rides unmodified.

## Package layout (all inside the fence)
- `frank/internal/worker/jcs` — RFC 8785 JCS encoder (canonical_args_digest, journal records, logical_surface_digest). `internal/fieldspec.CanonicalMarshal` (consume-only import) serves only where m-2 §3.2's Go-encoding is specified (the F58 catalog).
- `frank/internal/worker/wire` — counter grammar (canonical-decimal-uint64 strings) + the CTRL-W/DATA-P frame types & codec (m-10 §A.2 envelope; the full frame family the lifecycle/delta pin).
- `frank/internal/worker/catalog` — F58: the five pinned local schemas + m-2's three consumed digests byte-exact + m9-catalog-v1/m2-mapping-v1/member-absence; recomputes the expected tool_catalog_digest 7fae5fc1… as an executable test.
- `frank/internal/worker/journal` — D1 one-file writer (create/reopen/append/truncate under the flock fence + descriptor battery + fsync ordering + intake-dedup consumer per writer §1-§8) and the two-layer checksum-reader (the total per-kind transition table).
- `frank/internal/worker/executor` — F59: the three identities / two derivation points, consume-then-execute, the typed reject dispositions, record_tool_outcome, serial execution.
- `frank/internal/worker/tools` — the single local-backend interface + read/write/edit/apply_patch/bash (setpgid/killpg teardown, bounded output, sanitized env, no inherited fds).
- `frank/internal/worker/turn` — the turn state machine + §2a bounds + terminals + the parked_unknown two-gate total comparator + turn_terminal/turn_cancel_ack emission.
- `frank/internal/worker/provider` — the m-8 attempt cycle consumer (attempt_open ordering, stream terminals, three-loss facts, cancellation cuts, epoch backstop) against a FAKE m-8.
- `frank/internal/worker/contextmgr` — transcript assembly (Tier-0 pin from build artifact + admission_ref; Tier-1 evict; Tier-2 summarize w/ sentinel), §7.3 integrity check, logical_surface_digest, E0 population + redaction.
- `frank/internal/worker/resume` — D2 reconciliation, the total first-action table, content_ready + report_resume_disposition frames.
- `frank/internal/seatclient` — the shared conductor-client facade (Call/Describe verbs only) + the m-2 form→schema mapping module (the contract's SchemaFromForm/Parse/Validate/SubmitPayload/re-render vocabulary) + the native relay tool's AXI-shaped envelope.
- `frank/cmd/frank-worker` — main: hello/assign/attach handshake, wiring, fake-injection seams.
- FAKES (in-fence, test + E2E): a fake m-10 CTRL-W peer + a fake m-8 DATA-P/stream peer per MVP amendment §8.

## Task order (straight-through; each task lands RED-first negatives with its code; `go test ./...` green at every commit — the ten INV-CATALOG laws ride the existing conductor suite untouched)
T1 jcs+wire foundations → T2 catalog (7fae5fc1 recompute) → T3 frame codec → T4 journal writer+reader → T5 F59 executor → T6 local tools → T7 seatclient + m-2 module + native relay tool → T8 turn machine → T9 provider cycle → T10 contextmgr+E0 → T11 resume/D2 → T12 cmd/frank-worker + the one-governed-turn E2E against the fakes → T13 the §10/§6 fixture-battery sweep (r11-adjusted; segment-era rows superseded) → T14 close: docs, status, the end-of-slice adversarial review by s15.implementer, SITREP + store export.

## The ONE routed seam ask (TO master — no fence deviation performed)
The m-2 mapping module the native relay tool must share with MCP exists today only inside `cmd/frank-mcp` (package main, not importable; out of s15's fence). Plan: s15 builds the m-2 module at its parity home inside `internal/seatclient` per the m-2 contract; the `cmd/frank-mcp` refactor to consume that shared module+client (killing the duplicate) is REQUESTED as a master-arbitrated change — either granted to s15 as a bounded fence extension, or scheduled at restack. Until ruled, s15 touches no `cmd/frank-mcp` byte; the temporary duplication is named, not silent.

## Deferred/non-goals named
No sandbox (H-12 stands); no permission system (Step-4); the m9_worker_build_digest release-binding pipeline half is the stage-6/lock act (s15 ships the recompute tooling + tool_catalog_digest verify only); MCP frontend behavior unchanged; no conductor byte, no exit-fixture byte.

ACTIONS_GIT_REF: docs-workspace lane act — this PLAN + its INDEX row (in-fence, banked on main per the disclosed convention); the build worktree /Users/jack/Programming/harness-s15-m9-worker sits on s15-m9-worker @ b7f406b2 with zero code bytes yet.
FINAL_GIT_STATUS_SHORT: (literal, workspace root, captured after this PLAN existed; master-side rows are other seats' work:)
 M master/relays/CHECKPOINTS.md
 M master/relays/INDEX.md
?? frank/.relays/s15/s15-build/
?? master/relays/step3-t4-preflight/RECONCILE-orchestrator-reviewer-20260820-203954.md
Next requested action: master rules the cmd/frank-mcp seam ask at its convenience (non-blocking); s15.implementer may object async; the build opens at T1 now.
