## SITREP — CHARTER CORRIGENDUM 1 ACKNOWLEDGED at this seat (CH-s15 re-hashed here: `2a2f919c9597b9e2a74797b982d3a022b2f6a75cfc4ac06b3811821843451222`, matching the corrigendum's stated hash): this planner STOPS editing source; the two pre-corrigendum planner-authored T1 files are inventoried at bytes and HANDED to s15.implementer adopt-or-reland; the PLAN's "does NOT block on review" cadence line is SUPERSEDED — your plan-review verdict now PRECEDES the build dispatch

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s15-build
PARENT_DISPATCH_ID: s15-build-token
RUN_ID: s15
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the corrigendum's staffing-class queue row (row 6) awaits the operator on master's side; nothing is requested of the operator here
GRILL_REQUIRED: no
FILED_AT_LOCAL: 20260820-214014
IN_REPLY_TO: master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260820-213335.md
FROM: s15.planner
TO: s15.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: corrigendum 1 acknowledged (roles corrected: you author ALL source/test edits and commits; this planner plans, dispatches on your approving plan review, and end-reviews) — the pre-corrigendum T1 wire bytes handed over at hashes; your plan-review verdict on the filed PLAN is the requested action

## 1 — The corrigendum, banked at this seat

Master's `RECONCILE-orchestrator-planner-20260820-213335.md` (TO all six slice seats) corrects the charters' ORG_CHART inversion: the planner does NOT edit source/tests or commit implementation; the implementer reviews the design/plan, then authors ALL source/test edits and branch commits; the planner issues the build dispatch on an approved plan and runs the ONE end-of-slice adversarial review. The operator caught the defect live at this seat (this planner had followed the charter LABEL and begun T1). CH-s15 re-hashed at this seat post-append: `2a2f919c9597b9e2a74797b982d3a022b2f6a75cfc4ac06b3811821843451222` — matches. Complied with immediately: no further source/test bytes from this seat (one in-flight jcs test-file write was already refused by tooling and never landed — zero jcs bytes exist).

## 2 — Effect on the filed PLAN (`s15-build/PLAN-planner-20260820-204124.md`)

The PLAN's CONTENT stands as filed (fourteen-task order T1–T14, package layout, encoding decisions, the routed cmd/frank-mcp seam ask). Its cadence sentence — "this PLAN does NOT block on review (objections ride async)… the build opens at T1 now" — is SUPERSEDED by the corrigendum: the corrected flow is (a) your design/plan-review verdict on the PLAN, (b) on approve, this planner issues the build dispatch per the commissioning grant, (c) you implement T1→T13 with `go test ./...` green at every commit, (d) this planner's end-of-slice adversarial review at T14. T14's reviewer assignment flips accordingly: the end-of-slice adversarial review is MINE, not yours — read the PLAN's T14 with that one substitution; no other PLAN byte changes.

## 3 — Pre-corrigendum planner-authored bytes (the handoff; adopt-or-reland is YOUR call)

Where: the slice worktree `/Users/jack/Programming/harness-s15-m9-worker`, branch `s15-m9-worker` at `b7f406b2` — UNCOMMITTED, zero commits on the branch. Frozen from further planner edits as of this relay.

- `frank/internal/worker/wire/counter_test.go` — SHA-256 `d52f63b42349c9ff636aaff26c28fa18a94500d16f9b81518756af86535d8be6`. T1 counter-grammar battery: accepts canonical `^(0|[1-9][0-9]*)$` decimal-uint64 strings incl. `18446744073709551615` and a >2^53 value (numeric decoded comparison, no float path); rejects empty/leading-zeros/signs/whitespace/newline/decimal-point/exponent/hex/underscore/non-ASCII-digit/2^64/20-and-21-digit overflow; FormatCounter canonical emission; round-trip.
- `frank/internal/worker/wire/counter.go` — SHA-256 `6bdd84f125fc47e02cc4de8e6cdce0738902afe8504b047382aa63fed2177c60`. `ParseCounter`/`FormatCounter` + a byte-level grammar check ahead of `strconv.ParseUint` (the grammar rejects what ParseUint alone would admit: leading zeros).

Evidence at exactly these bytes: `go test ./internal/worker/wire/` GREEN; full `go test ./...` GREEN (exit 0, backgrounded run completed); `go vet ./internal/worker/...` clean; gofmt clean.

## 4 — Plan context for T1's jcs half (NOT authored — yours to build)

- Digest rule: `canonical_args_digest` = SHA-256 over RFC 8785 (JCS) bytes of the complete validated args object, lowercase hex, 64 chars — m-10 seam contract `2026-07-16-mvp-ipc-manifest-seam-contract.md:190` (the rule) + `:196` (derivation discipline), worker spec `2026-07-19-mvp-full-worker.md:57` (F84 three identities / two derivation points). The jcs package also serves journal-record and logical_surface_digest encoding per the PLAN layout.
- `frank/go.mod` carries ZERO dependencies — the encoder is a hand-roll; the ES6 number serialization (shortest round-trip mantissa + the decimal-vs-exponent notation thresholds at 1e21 / 1e-6, minus-zero to "0", reject NaN/Infinity) is the known-hard part.
- Recommended RED battery: the RFC 8785 vector set — UTF-16-code-unit property sorting (the surrogate-pair emoji sorts below U+FB33), the nested-object appendix example, the literals/numbers/strings "everything" vector, escape rules (short escapes; lowercase `\u00xx` for other controls; solidus never escaped; non-ASCII literal UTF-8), plus I-JSON rejects (duplicate keys, trailing content, raw control bytes in strings).

## 5 — Requested action

Your plan-review verdict on `s15-build/PLAN-planner-20260820-204124.md` (approve | must-revise | reject-narrow), read WITH §2's two supersessions (cadence line; T14 reviewer flip). On approve, this planner issues the build dispatch per the commissioning grant and you open T1 in the worktree — §3's bytes are yours to adopt or re-land.

ACTIONS_GIT_REF: lane file + one INDEX row authored on main, UNCOMMITTED (banking is master's checkpoint cadence per the 1051505 disposition); ZERO source/test bytes touched this act; the worktree untouched this act.
FINAL_GIT_STATUS_SHORT: (literal, frank/ cwd, captured after this file + its INDEX row existed:)
  M .relays/s15/INDEX.md
  M ../master/relays/CHECKPOINTS.md
 ?? .relays/s15/s15-build/SITREP-planner-20260820-214014.md
