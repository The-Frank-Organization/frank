## PLAN — s15 build order r4 (the BOUNDED R7 FOLD; everything else carries byte-unchanged from the approved r3): T7 gains master's Option-1 authorization terms — the five preserved invariants + the no-credential-bytes-through-the-facade constraint + the three owed evidence legs; the T1–T6 closure banked at COPY-EXACT commit hashes (the escalation's §4 splice corrected); T7 resumes only after your fresh review verdict + the successor dispatch

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s15-build-4
PARENT_DISPATCH_ID: s15-impl-auth-escalation
RUN_ID: s15
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the blocking dependency is MASTER's durable fence ruling on `cmd/frank-mcp/**` (§2), not an operator gate; the pair's terminal gate stays the operator MERGE-GATE (serialized s13 → s14 → s15), unchanged and not requested here
GRILL_REQUIRED: no — GRILL_LOCK `step3-mvp-stage4-m9-worker-grill-1` rides via the charter unmodified; no new operator choice is opened
FILED_AT_LOCAL: 20260821-041558
IN_REPLY_TO: master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-033914.md
FROM: s15.planner
TO: s15.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-9.implementer
SUBJECT: successor PLAN r4 — the R7 fold, bounded: T7's Option-1 terms in plan bytes (five preserved invariants, deviation re-fires the stop BEFORE commit; the §2 facade constraint, fresh stop if credential bytes would pass through conduct; evidence legs a/b/c added to the T7 battery); the T1–T6 evidence list corrected copy-exact per R7 §4; your fresh verdict precedes the resume

## §0 — Supersession, basis, and the pair roles (closes S15-PR1-F4's role half)

**Supersedes** `s15-build-3/PLAN-planner-20260820-225026.md` (the r3 plan your review APPROVED at byte-bound lock `53034c7e3174143d2012c31f854f8567c035354d20c8364bcded5a13ffdec4b8`) **in full** — the approved bytes carry UNCHANGED except the bounded R7 fold marked below; nothing approved is reopened. **State this fold banks:** master's R3 ruled **BRANCH A** (`RECONCILE-orchestrator-planner-20260820-234501.md`) — the Branch-B arms throughout this document are MOOT, retained as historical text; the operative scope is the issued token's twelve-row all-in SCOPE_DIFF (`s15-impl/IMPL-planner-20260821-004512.md`). **T1–T6 are CLOSED on `s15-m9-worker`** — the copy-exact evidence list (correcting the escalation's §4 garbled T2/T3 splices per R7 §4; re-derived from the branch by `git rev-parse` this act): T1 `7470ffc014bfaf4c8080dd29aad28527818ee8a5` (jcs+counters) · T2 `d2f468ede9e085110649bd8eebb65b6b232bf477` (F58 catalog) · T3 `081c8abf030c649617143238b0d8768316403db0` (frame codec) · T4 `9c2512552ac4381519bdd67ad01152d4d05dbb61` (session journal) · T5 `dcad3c407dd724809d0c2647473026bb804259d3` (F59 executor) · T6 `c6038e218357622089899dddeeadbb4adae23810` (local tools). Remaining execution = T7 (HELD at the stop-line, resumes per §5) → T14.

**Roles (Charter Corrigendum 1, CH-s15 @ `2a2f919c9597b9e2a74797b982d3a022b2f6a75cfc4ac06b3811821843451222`):** s15.implementer authors ALL source/test edits and branch commits, RED-first per task, `go test ./...` green at every commit. s15.planner (this seat) plans, receives your plan-review verdict, issues the bare own-line implementation dispatch addressed ONLY to s15.implementer on approve (per the commissioning grant + the delegated-dispatch protocol, with exactly one `SCOPE_DIFF` returning `all-in`), stands by for blockers, and runs the ONE end-of-slice adversarial review at T14. The two pre-corrigendum planner-authored wire files (`counter.go` `6bdd84f1…`, `counter_test.go` `d52f63b4…`, verified untouched by your review) are adopted-or-relanded BY YOU as an IMPL act after dispatch — nothing in this PLAN pre-authorizes it.

**Basis (unchanged from r1; all re-hashed at this seat, zero drift; the readiness twin carries the 19-alias list):** the three m-9 bases (A-M9-WORKER `70a2378d` = `2026-07-19-mvp-full-worker.md` · A-M9-LIFECYCLE `72796bc5` = `2026-07-17-mvp-lifecycle-half.md` · A-M9-DELTA `61833f63` = `2026-07-22-relock-lane2-m9-delta.md`) AS FOLDED by the ratified D1-r11 layer (stamped-current `c388f7dd` over registered `7147a0c2`, preimage verified at `de4dd0a`), the one-file writer (`2026-07-27-writer-onefile.md`, `1e85e92e`) and the checksum-reader (`2026-07-26-checksum-reader-onefile.md`, `d803232c`); consumed sibling contracts read at their tasks (m-10 seam `2026-07-16-mvp-ipc-manifest-seam-contract.md`; m-8 `2026-07-17-mvp-provider-contract.md`; m-2 `2026-07-16-step3-mvp-form-schema-mapping.md` FROZEN rev5 `83d8e63e`). m-9's three opening notes bind: LIVE text at fold sites; the lifecycle `:139` fresh-turn clause is NOT implemented; GRILL_LOCK §12 rides unmodified. **The 215942 phrasing guidance binds all naming:** provider `reasoning` output items are OPAQUE BYTES round-tripped byte-preserved (never parsed/decoded/rendered — m-8's opacity rule); the D1 journal is OUR OWN agent's session-content log for crash-safe resume/replay fidelity; test/symbol names use schema vocabulary; a task that ever seems to need an item's interior ESCALATES as a spec question.

## §1 — The m-2 shared-module seam: BLOCKING, with the package/import split (closes S15-PR1-F1)

r1's "non-blocking seam ask" classification is WITHDRAWN — your F1 is right: the frozen m-2 contract's §2.1 3-way seam and §6 build obligations make shared consumption a completion requirement, not a restack afterthought. The plan now binds:

**Package split (replaces r1's undifferentiated `internal/seatclient`):**
- `frank/internal/seatclient/formschema` — the m-2 mapping module (m-2 remains sole semantic owner; the path token exercises mapping §2.1's build-lane adjustment, ownership/API/import-boundary normative and unchanged). Imports `internal/fieldspec` + `internal/record` ONLY. **Must not import `internal/channel`** (mapping §2.1).
- `frank/internal/seatclient/conduct` — the channel-using client facade (Call/Describe verbs over `internal/channel`). **Must not import `formschema`** (mapping §2.1's reciprocal).
- `frank/internal/worker/relaytool` — the m-9 NATIVE frontend: imports both and wires them (the only in-slice place a rendered form meets a transport call), realizing worker §4's AXI-shaped envelope.
- `cmd/frank-mcp` — the retained MCP frontend, which the contract obligates to become a consumer of the SAME module (mapping §2.1 "neither absorbed nor stranded"; §6 items 1/4/5/9 MCP halves) — **fence-branch-dependent, §2**.
- **Enforcement:** the mapping §2.1/§6-item-2 import-graph test, both directions + the not-re-trapped-in-`main` assertion, lands in-fence at `internal/seatclient/` (T7).

**The blocking rule:** master's durable ruling on the `cmd/frank-mcp/**` fence question MUST land before the implementation dispatch (the escalation is LIVE at relay-1d3d6a77/intake-000045, upgraded from r1's non-blocking phrasing; its Branch-B arm now carries the three-part condition above). The two branches, both fully specified in §2/§3-T7/§3-T13:
- **Branch A (fence extended):** the exact files `cmd/frank-mcp/{main.go, mcp.go, mcp_test.go, schema.go, schema_test.go, errors.go, README.md}` become IN; T7 includes the consumer refactor (module hoist per §6 item 1 with the flagged P-1/P-2/P-3/P-6 + MCP Layer-2 gate deltas, each with its own red test — behavior-preserving is NOT claimed where the contract mandates fail-closed deltas), the §6-item-4 dispatch-gate wiring, the §6-item-5 F-1/F-2 freshness choreography MCP half, and the §6-item-9 MCP parity adapter.
- **Branch B (extension denied/deferred) — dispatchable ONLY under an explicit THREE-PART SEMANTIC RULING (S15-PR2-F1):** a path denial alone cannot make the locked m-2 §6 obligations non-applicable; Branch B is valid only if master's returned ruling explicitly does ALL THREE: **(1)** narrows s15's COMPLETION CLAIM (not merely denies/defers the fence), **(2)** names the owner/dispatch/gate that must land the omitted mandatory MCP obligations (§6 items 1-consumer-half/4-MCP-wiring/5-MCP-half/9-MCP-adapter), and **(3)** requires T13/T14 to return the slice PARTIAL/HELD on that named obligation, never complete. A ruling lacking any of the three ⇒ **Branch A is the only valid completion path**. Under a conforming Branch-B ruling: ZERO `cmd/frank-mcp` bytes; the slice builds the module + in-module obligations + the native halves; the trapped duplicate in `cmd/frank-mcp` is disclosed, not silent.

## §2 — Scope inventory vs the charter fence (closes S15-PR1-F2)

Charter WRITE_FENCE (CH-s15 §Launch-base, exclusive): `frank/cmd/frank-worker/**` · `frank/internal/worker/**` · `frank/internal/seatclient/**` · `frank/.relays/s15/**`.

| Path this plan touches | Fence class | Disposition |
|---|---|---|
| `frank/internal/worker/**` (jcs, wire, catalog, journal, executor, tools, turn, provider, contextmgr, resume, relaytool, fake, FIXTURES.md, README.md) | IN | build surface |
| `frank/internal/seatclient/**` (formschema, conduct, import-graph test, README.md) | IN | build surface |
| `frank/cmd/frank-worker/**` (main + README.md) | IN | build surface |
| `frank/.relays/s15/**` | IN | lane governance |
| `frank/cmd/frank-mcp/**` (the seven files named in §1) | **OUT today** | Branch A: master grant converts EXACTLY these to IN. Branch B: zero touches AND valid only under the §1 three-part semantic ruling — an all-in path diff is necessary but NOT sufficient. NO dispatch until the ruling lands. |
| `frank/internal/appipc/**`, the conductor tree (`internal/channel`, `internal/fieldspec`, `internal/record`, …), all m-x design docs, `master/exit-fixtures/**` | CONSUME-ONLY / untouchable | imports + reads only; ZERO writes (§3-T13's explicit preservation) |
| `frank/go.mod` / `frank/go.sum` | ARBITRATION-ONLY | NO touch planned (zero new dependencies — the encoder set is hand-rolled); should any task surface a genuine dep need, work STOPS and master arbitrates first |

**Dispatch rule:** the implementation dispatch relay carries exactly one `SCOPE_DIFF` enumerating every touched path against this table under the branch master's ruling selects, and issues ONLY on `SCOPE_DIFF_RESULT: all-in`. No "integration hook," "at restack," or acceptance-needed phrasing converts OUT to IN.

## §3 — The fourteen tasks with executable acceptance (closes S15-PR1-F3)

Common to every task: (i) RED-first — the negative battery lands failing before the code; (ii) the task's E2 command green + full `go test ./...` green at every commit; (iii) `gofmt -l` empty + `go vet ./...` clean at every commit; (iv) commits on `s15-m9-worker` in the slice worktree only.

**T1 — encoding foundations (jcs + wire counters).**
(a) Clauses: m-10 seam §D.2 `:190` (canonical_args_digest = sha-256 over JCS bytes of the complete validated args object; lowercase hex, 64 chars) + `:196` (fresh derivation discipline); worker §5.2 (F84 three identities / two derivation points); lifecycle §2.7 (the uniform counter rule) + m-10 seam §A.2 (canonical-decimal-uint64 frame counters).
(b) Paths: `internal/worker/jcs/{jcs.go,jcs_test.go}` · `internal/worker/wire/{counter.go,counter_test.go}` (the handed-off pair — adopt or re-land, your call).
(c) RED battery: RFC 8785 vectors — UTF-16-code-unit property sorting incl. the surrogate-pair case; the nested appendix example; the literals/numbers/strings vector; escape rules (short escapes; lowercase `\u00xx` for other controls; solidus unescaped; non-ASCII literal); ES6 number serialization (1e21/1e-6 notation thresholds, minus-zero to `0`, shortest mantissa, NaN/Infinity reject); I-JSON rejects (duplicate keys, trailing content, raw in-string control bytes). Counter grammar: the handed-off accept/reject/round-trip battery (leading zeros, signs, whitespace, hex, underscores, non-ASCII digits, 2^64 overflow; >2^53 numeric comparison).
(d) Done: every vector green; digest output is 64-char lowercase hex and input-order-invariant. E2: `go test ./internal/worker/jcs/ ./internal/worker/wire/`.
(e) Boundary: none yet (pure functions); T5/T10 consume.
(f) Preserves: `internal/fieldspec/canonical.go` is consume-only and is NOT a JCS encoder — no edit, no claim over it.

**T2 — the F58 catalog.**
(a) worker §8.3 (the m-9-owned tool-catalog build); m-2 mapping §3.2 reference values consumed BYTE-EXACT (the producers are m-2's module — this package consumes digests, never re-derives semantics); m-10 seam §C.3 (the serve gate's canonical-SET equality over tool identity).
(b) `internal/worker/catalog/**`.
(c) RED: recompute `tool_catalog_digest` == `7fae5fc1…` as an executable test; member-absence negative (a missing member breaks set equality); the three consumed m-2 digests pinned as byte constants with a mismatch test; `m9-catalog-v1`/`m2-mapping-v1` version markers.
(d) E2: `go test ./internal/worker/catalog/`.
(e) Counterpart: the manifest-side constant via a fake m-10 fixture; proof = digest equality both directions.
(f) Preserves: zero m-2 doc/module bytes; the catalog never interprets args.

**T3 — the CTRL-W/DATA-P frame codec.**
(a) m-10 seam §A.2 (version-pinned envelope; canonical-decimal counters via T1); §A.3 backpressure posture consumed (honest and minimal — no invented flow control).
(b) `internal/worker/wire/frame.go` + tests (extends the T1 package).
(c) RED: malformed-frame classes (bad version, unknown kind, non-canonical counter, oversize), unknown-member reject, round-trip byte-exactness, `PARKED_ROW_MAX = 640 B` bound honored on parked rows (m-10 producer delta `:73`).
(d) E2: `go test ./internal/worker/wire/`.
(e) Counterpart: the fake m-10 CTRL-W peer (T12) and fake m-8 DATA-P peer speak exactly this codec; shared frame fixtures both directions.
(f) Preserves: `internal/appipc/**` consume-only.

**T4 — the D1 session-content log (writer + two-layer reader).**
(a) writer-onefile §1–§8 (location/identity/`create_auth_id` provenance; the per-run exclusive fence; genesis-create sole-path; the one uniform resume action + untrusted-tail truncation; append; cross-`generation_id` handoff no-new-file; crash recovery; the at-rest descriptor battery on the OPEN descriptor); checksum-reader-onefile §1–§8 (two-layer over physical seq position; the structural ordered algorithm; admission by record class; `round_last_seq` vs `marker_seq`; the first-match stream transition; layer-2 content-trust; THE TOTAL per-kind transition table; `re_derive` checkpointing + the genesis-fault disposition); delta §1.2/§1.3 (closed record union + canonical encoding/envelope), §1.5 (`round_marker` exact membership), §1.6/§1.6a (fence + descriptor battery), §1.9 (identity-exactness), §1.10 (content-before-outcome, marker-before-outcome), §1.11 (acceptance properties i–vi).
(b) `internal/worker/journal/**`.
(c) RED: second-writer fence violation; the crash table (torn tail, mid-append, marker-vs-outcome windows) with recovery to the valid prefix; descriptor-battery negatives (symlink swap, wrong identity); intake-dedup consumer; per-kind transition-table totality (every kind × every state has exactly one row — anti-vacuity legs included); durability-ordering violations detected.
(d) Done: acceptance properties (i)–(vi) each bound to a named test. E2: `go test ./internal/worker/journal/`.
(e) Boundary: writer and reader prove each other over shared crash fixtures in both directions.
(f) Phrasing binds: this is OUR OWN agent's transcript persistence for crash-safe resume/replay fidelity; provider output items it stores are opaque bytes, round-tripped byte-preserved, never parsed (`opaque_item_passthrough`-class test names).

**T5 — the F59 executor (the single settlement point).**
(a) worker §5 (single central settlement; §5.2 F84: frozen identity #1 at construction, fresh comparand #2 pre-consume on the four-field wire, fresh comparand #3 pre-invocation, all epochs equal on the commit path); m-10 seam §D.1–§D.4 (ticket shape, issue, exactly-once atomic consume, crash windows); lifecycle §2.5 (cancellation + EOF fail-closed); GRILL_LOCK §12 (uniform gate, NO carve-outs; sync-authz/async-record).
(b) `internal/worker/executor/**`.
(c) RED: mutation before derivation point A ⇒ `IDENTITY_MISMATCH`/ticket stays `ISSUED`; mutation before point B ⇒ ZERO invocation; double-consume rejected; the honest 4-token `authorize_reject` domain; `record_tool_outcome` two-member outcome domain (`executed` / `not_invoked_integrity_fault`); epoch-mismatch ⇒ attempt-inert; a parsed call stays INERT with no authority path (charter SCOPE line); serial execution (no concurrent settlement).
(d) E2: `go test ./internal/worker/executor/`.
(e) Counterpart: the fake m-10 ticket authority; proof = the §D fixture family run against it, incl. the crash-window UNKNOWN/PARTIAL rows.
(f) Preserves: NO permission-system invention — authority = the manifest constant m-10 hosts (Step-4 owns the rest); no carve-out for any tool incl. relay verbs.

**T6 — the five local tools.**
(a) worker §3 (single local-backend interface; read/write/edit/apply_patch/bash); GRILL_LOCK §12 (bash DOC-ONLY claims; the frozen setpgid/killpg process-group teardown; bounded output; sanitized env; no inherited fds; the escaper deferred to Step-4).
(b) `internal/worker/tools/**`.
(c) RED: teardown kills the whole process group incl. orphans; output bounds truncate honestly (marked, never silent); env sanitization (allowlist, no ambient secrets); fd non-inheritance; path handling negatives per tool (read/write/edit/apply_patch), apply_patch malformed-hunk rejects.
(d) E2: `go test ./internal/worker/tools/`.
(e) Counterpart: T5's executor is the only caller (interface conformance test).
(f) Preserves: H-12 stands — no sandbox exists or is claimed; the bash claim stays narrowed to the documented gap; bash ambient authority is the accepted MVP posture, restated not relitigated.

**T7 — seatclient: the m-2 mapping module + client facade + native relay tool (BRANCH-SENSITIVE).**
(a) mapping §2.1 (home/ownership/3-way seam), §2.2 (normative API: `SchemaFromForm`/`SubmitPayloadFromArguments`/re-render halves), §2.3 (mapping rules + the V-partition validator), §2.4 (re-render semantics), §3.2/§3.3 (F58 producers + version/fingerprint `306b3149…`), §4 (parity + the PV fixture family item 15), §6 obligations 1–10 with the lane split — in-slice always: module hoist target + import-boundary test (item 2) + P-rule enforcement (item 3) + `ValidateSubmitArguments` and friends (item 4) + F-1/F-2 freshness NATIVE half (item 5) + F58 producers consumed (item 6) + branch-inventory/coverage binding (item 7) + the Appendix-A runner reproducing the LOCKED expected bytes + fingerprint (item 8) + parity mapping half + NATIVE adapter (item 9) + the release-binding recomputation hook (item 10); worker §4 (the native relay tool's AXI-shaped envelope; MCP retained for foreign harnesses); lifecycle §1.1–§1.7 (seat receiver: capability surface, attach, credential hygiene, durable rediscovery, wake forwarding, broker error classes, reattach).
(b) `internal/seatclient/formschema/**` · `internal/seatclient/conduct/**` · `internal/worker/relaytool/**`; Branch A adds the §1-named `cmd/frank-mcp` files.
(c) RED: the import-graph test BOTH directions + not-re-trapped (fails on violation); P-1/P-2/P-3/P-6 typed error identities; duplicate-member + trailing-data pre-scan; unknown-member/reserved-key/CC-conflict rejects; V-partition stable-vs-volatile split; F-1 refresh-before-reject + F-2 refresh-on-rejection (native; +MCP on Branch A); Appendix-A locked vectors reproduced byte-exact (vectors consumed, NEVER re-authored); `union(exercises) == inventory` failing both directions; parity vectors across frontends (Branch A) or native-only with the exclusion named (Branch B); form_digest freshness (the submit-gotcha class) exercised against fake-conductor PV fixtures.
(d) E2: `go test ./internal/seatclient/... ./internal/worker/relaytool/` (+ `./cmd/frank-mcp/` on Branch A).
(e) Counterpart: fake-conductor PV family fixtures; m-2 stays sole semantic owner — ANY mapping-rule question escalates to master → m-2 (m-2-reviewed changesets move the version; this lane never edits mapping semantics).
(f) Preserves: Branch B = zero `cmd/frank-mcp` bytes, the trapped duplicate disclosed, and Branch B valid only under the §1 three-part ruling (the omitted MCP halves land with the master-named owner/dispatch, never silently dropped); `internal/channel` unedited (consume-only import from `conduct` only). **THE R7 FOLD (master's Option-1 authorization, `…-20260821-033914.md`, binding T7's resumed work):** the minimum `callWithReconnect` refactor routing its wire calls through the shared `internal/seatclient/conduct` facade is AUTHORIZED, under: **(R7-INV, all five verified in the planner's T14 end-review; deviation from ANY re-fires the stop BEFORE commit)** (1) the H-16 guard function AND its call site (`cmd/frank-mcp/mcp.go:178`/`:151`) byte-untouched; (2) `ensureClient` remains the SOLE credential acquirer and `main.go` credential sources byte-untouched; (3) the close → re-authenticate → ONE-retry choreography semantics preserved (same triggers, same retry count); (4) the scrubbed error classes preserved (no new error path carries credential material or widens error text); (5) the refactor confined to the granted seven-file fence at minimum surface (the reconnect path + the client-type seam it needs). **(R7-FACADE, fresh stop if violated):** `conduct` NEVER receives, stores, logs, or transports raw credential bytes — it takes an authenticated client and/or a dial/re-auth callback owned by `ensureClient`; if the facade signature turns out to require credential bytes THROUGH it, STOP before the edit — that is a new escalation, not a local shape choice. **(R7-EVID, three legs added to the T7 battery):** (a) a RED/green reconnect-choreography leg exercising close→re-auth→one-retry THROUGH the facade; (b) an H-16 guard REACHABILITY leg — the gate demonstrably RUNS on the refactored path (a green that never reaches the guard is a false green); (c) the frontend-parity leg — native + MCP through the SAME facade bytes.

**T8 — the turn state machine.**
(a) lifecycle §2.1 (one-active-turn, distinct from m-10's lease), §2.2 (the state machine over the §2a loop), §2.3 (the five turn terminals), §2.4 (§2a bounds as compiled constants — termination/resource safety, NOT a permission system), §2.6 (session-state locus — no second durable truth), §2.8 (replay-envelope custody); delta §2 (the content-ready RECEIPT joint frame); m-10 seam §B.4 (`turn_epoch` issue/increment/reject) + §D-4 (the parked-unknown disclosure: `turn_open{…, parked_unknown: […]}` command + `attempt_open_ok{…, parked_unknown: […]}` reply — both closed-shape, empty-array-never-absent; m-9's owner obligation = pre-work surfacing); the two-gate `parked_unknown` total comparator; `turn_terminal`/`turn_cancel_ack` emission. The lifecycle `:139` fresh-turn clause is NOT implemented (opening note; the folded text + D1-r11 govern).
(b) `internal/worker/turn/**`.
(c) RED: second-turn admission rejected; every §2a bound exhaustion reaches its named terminal; stale-epoch frames rejected inert; `parked_unknown` consumed at BOTH frames, empty AND non-empty fixtures, surfaced pre-work (the seam's owed consumer fixtures); cancel-ack exactly-once; receipt emission ordering per delta §2.
(d) E2: `go test ./internal/worker/turn/`.
(e) Counterpart: fake m-10 admission/lease frames; proof = the §D-4 fixture pair (empty/non-empty) consumed.
(f) Preserves: m-10's lease/scheduling never re-owned; `:139` expressly excluded.

**T9 — the provider attempt cycle (vs the fake m-8).**
(a) m-8 §1.1 (`LLMRequest` app-internal, NEVER a wire object), §1.2 (the normalized `m8.provider_event.v1` stream), §1.3 (attempt lifecycle + the two observation views; attempt_open ordering; the three loss facts), §1.4 (cancellation two-cut + chunk pacing), §2.3 (ONE attempt, no auto-retry — the suppression census); lifecycle §2.2/§2.3 (terminals fed by stream outcomes); the attempt-inert epoch backstop (r19 lock members).
(b) `internal/worker/provider/**`.
(c) RED: `attempt_open` ordering violations rejected; every stream-terminal class mapped to its turn-machine input; the three loss facts each produce their honest recorded outcome (never a fabricated success); both cancellation cuts; stale-epoch attempt inert; `response_item_roundtrip`/`opaque_item_passthrough` — every output item kind incl. `reasoning` items round-trips byte-preserved through worker state and the T4 log (never parsed/decoded/rendered/summarized; the opacity rule IS the test); no-retry (a failed attempt never self-repeats).
(d) E2: `go test ./internal/worker/provider/`.
(e) Counterpart: the fake m-8 DATA-P/stream peer (MVP amendment §8); shared frame fixtures with T3's codec.
(f) Preserves: zero wire/credential/translation bytes (m-8's §2/§3/§5 surface untouched); no egress.

**T10 — context management + E0.**
(a) worker §7 (the GOVERNED 3-tier compaction: Tier-0 PINNED from the build artifact + `admission_ref`; Tier-1 evict; Tier-2 summarize WITH sentinel — the Governance-Decay defense; §7.3 integrity check; `logical_surface_digest`); worker §6 (E0 population + redaction); GRILL_LOCK §12 (the compaction ratification).
(b) `internal/worker/contextmgr/**`.
(c) RED: pin immutability (no tier ever evicts/rewrites Tier-0); the sentinel present on every Tier-2 summary; §7.3 integrity failure ⇒ the honest fault disposition (never silent continue); `logical_surface_digest` stable across a no-op reassembly and changed on any surface change; E0 redaction negatives (secret-shaped members never populate E0).
(d) E2: `go test ./internal/worker/contextmgr/`.
(e) Counterpart: consumes T4's reader for transcript assembly (shared fixtures).
(f) Preserves: opaque items stay opaque through assembly; no provider-item interiors read.

**T11 — resume + D2 reconciliation.**
(a) delta §3 (D2 reconciliation-consume + the TWO time-scoped trust properties), §4 (the TOTAL first-action table, all five branches); reader §2.5 (resume boundaries + admission by record class); `2026-07-26-resume-prefix-oracle-m9.md` (the prefix oracle); `content_ready` + `report_resume_disposition` frames; the edited-session boundary (`909ba17b` rev4): edit surface = the m-9 journal ONLY, class-distinctness + non-promotion.
(b) `internal/worker/resume/**`.
(c) RED: all five first-action branches each reached by a fixture (totality — the carrier domain enumerated, not the fixture alphabet); trust-window violations rejected; prefix-oracle conformance; an edited session never promotes across the class boundary.
(d) E2: `go test ./internal/worker/resume/`.
(e) Counterpart: fake m-10 reconciliation frames.
(f) Preserves: m-1 governed records + m-10 settlement rows excluded from any edit surface.

**T12 — `cmd/frank-worker` + the one-governed-turn E2E.**
(a) lifecycle §1.2 (hello/assign/attach + capability acquisition per m-7 §2.10 / m-10 §B.1), §1.5 (wake forwarding, duplicate-safe); the MVP amendment §8 fake posture; the wiring of every prior task.
(b) `cmd/frank-worker/**` · `internal/worker/fake/**` (the fake m-10 CTRL-W peer + fake m-8 DATA-P peer, in-fence, test+E2E).
(c) RED: handshake failure classes (bad capability, wrong epoch, dead broker) each fail-closed; then the ONE HONEST GOVERNED TURN E2E — admission → model exchange (fake m-8) → a governed tool request through the full F59 path (fake m-10 tickets) → local tool effect → outcome recorded → transcript persisted → terminal — asserting the journal's replay-fidelity readback byte-matches.
(d) E2: `go test ./cmd/frank-worker/ ./internal/worker/fake/` + the named E2E test; full `go test ./...`.
(e) Counterpart: both fakes ARE the counterpart proof until siblings' seams are live (charter SCOPE).
(f) Preserves: no real provider egress; no real m-10/m-8 process claimed; E3 liveness belongs to the exit oracle, not this slice's claims.

**T13 — the fixture-battery sweep + traceability (r11-adjusted).**
(a) worker §10 (the carried fixture obligations), worker §6 rows where m-9-owed; delta §1.11 (acceptance conformance); m-2 §4 PV family (per §1's branch); the m-10 §D-4 owed consumer fixtures (T8).
(b) The traceability table at `frank/internal/worker/FIXTURES.md` (IN-FENCE) mapping every applicable row → the named test function; tests live in their task packages.
(c) RED: anti-vacuity legs — every claimed gate reached under its enclosing guard (the reachability rule); at least one intentionally-broken fixture per battery family proving the battery can fail.
(d) Done: every §10/§6-applicable row has a named green test OR a named non-silent disposition (segment-era rows SUPERSEDED by r11 listed with the superseding locus; Branch-B exclusions listed per §1 WITH the master-named owner/dispatch/gate cited and the affected rows returned PARTIAL/HELD, never complete) — no silent drops. E2: `go test ./...` + the table complete.
(e) Counterpart: the battery families exercise every fake seam built above.
(f) **Explicit preservation:** `master/exit-fixtures/**`, the frozen exit oracle (`d4580c52`), and RLBS-1 are UNTOUCHABLE — never read as build inputs, never edited, never re-derived; this slice's batteries are its OWN, distinct from the exit corpus.

**T14 — close-out.**
(a) The charter's return obligations + Corrigendum-1 flow + this PLAN's §5.
(b) IN-FENCE docs: `frank/cmd/frank-worker/README.md` · `frank/internal/worker/README.md` · `frank/internal/seatclient/README.md` (run/build/test + the seam map + the Branch-B exclusion list if selected); TRANSPORT vs EXPORT split (S15-PR2-F2): the pair's closing SITREP submitted via the seat channel to master is TRANSPORT only. The F.7.2 STORE EXPORT is a MASTER act this pair never authors: master exports the slice's store records VERBATIM (the relay JSONs, content-addressed by `relay_id`) into `frank/.relays/s15/store-export/` and cites the export in the close relay (CYCLE-PLAYBOOK F.7.2 `:431-437`; charter `:35`). The pair's return REQUESTS that export; a SLICE-CLOSE claim issues only on master's receipt citing the populated `store-export/` artifacts — absent it, close is labeled EXPRESSLY OUTSTANDING and not claimed. NO out-of-fence doc path exists or is invented.
(c/d) Done: full suite green + `gofmt -l` empty + `go vet` clean at the final commit; the FIXTURES.md table complete; THEN **this planner's end-of-slice adversarial review** (the ONE binding review; REVIEW-FOLD loop for blockers/must-haves per the skill), then the pair's return to master (restack + battery rerun + re-review ride the operator MERGE-GATE, serialized s13 → s14 → s15 — not claimed here). Under Branch B the return states the slice PARTIAL/HELD on the master-named MCP obligation per §1. The return REQUESTS master's F.7.2 export; slice close per (b)'s gate.
(e) Reviewer: `s15.planner`. (f) Preserves: banking of main-side lane bytes stays master's checkpoint cadence.

## §4 — Escalation scan (closes S15-PR1-F4's ceremony half)

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — F59 authorization/tickets; the credentialed seat client (capability hygiene per lifecycle §1.3)
- migration/backfill/destructive-write/canonical-data-repair: no — no database/data migration in the slice
- money/inventory/orders/planning/accounting/trust-critical-state: yes — governed tool authority + the durable session-content trust boundary
- AI-or-automation-acts-downstream: yes — model-requested local and relay tools produce effects
- worker/scheduler/queue/retry/async-side-effect: yes — worker lifecycle, async outcome recording, provider attempts
- cross-repo/service-contract/generated-schema/shared-API-event: yes — m-2/m-7/m-8/m-10 shared contracts; MCP/native parity
- user-visible-control-with-materializer/downstream-consumer: no — none introduced
- test-runtime-role-mismatch: yes — the pre-corrigendum planner-authored bytes require explicit implementer adoption/re-landing (§0)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the fence ruling pends; H-12's documented no-sandbox gap rides
ESCALATION_SCAN_RESULT: trigger-present
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested; production-risk stands
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none
PRE_SCAN_PRESSURE: none

## §5 — The path from here

1. Your fresh plan-review verdict on THIS r4 fold (approve | must-revise | reject-narrow) — R7 §5's pair discipline: the fold + your review PRECEDE the T7 resume.
2. On approve, this planner issues the successor implementation dispatch (s15-impl-2) parenting your approving review, SCOPE_DIFF unchanged (the twelve rows, all-in under the granted Branch A), the R7 terms riding — T7 resumes on its receipt; T1–T6 stay closed and are not re-dispatched.
3. You execute T7→T13 per this plan; the in-progress uncommitted T7 bytes (formschema at the locked Appendix-A fingerprint `306b3149…`, the transport-neutral conduct facade, the schema.go consumer reduction) remain YOURS to keep or rework under the R7 terms; T14 ends with my adversarial end-review (now incl. the five-invariant verification); any slice-close claim additionally waits on master's F.7.2 export receipt (T14(b)). Transport per the 020247 notice: hand-relayed file paths; the store stays dormant.

ACTIONS_GIT_REF: lane governance only — this r4 fold + one INDEX row, UNCOMMITTED on main (master's banking cadence); zero source/test bytes by this seat; the worktree untouched this act (the implementer's uncommitted T7 bytes left exactly as found).
FINAL_GIT_STATUS_SHORT: (literal, frank/ cwd, captured after this file + its INDEX row existed:)
  M .relays/s13/INDEX.md
  M .relays/s14/INDEX.md
  M .relays/s15/INDEX.md
  M docs/sprints/active/2026-08-20-s13-m10-module/plans/PL-s13-build-plan-20260820.md
  M ../master/relays/CHECKPOINTS.md
 ?? .relays/s13/s13-build-impl/SITREP-implementer-20260821-034045.md
 ?? .relays/s13/s13-build-plan/PLAN-REVIEW-implementer-20260821-040528.md
 ?? .relays/s13/s13-build-plan/PLAN-planner-20260821-035127.md
 ?? .relays/s13/s13-build-plan/PLAN-planner-20260821-041422.md
 ?? .relays/s14/s14-build/IMPL-planner-20260821-040410.md
