## AUDIT — s1-core.planner independent audit: Slice-1 spec surface vs greenfield frank/

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: s1-core-audit
PARENT_DISPATCH_ID: s1-dispatch
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
IN_REPLY_TO: s1-core-audit/AUDIT-orchestrator-planner-20260703-133046.md
FROM: s1-core.planner
TO: s1.orchestrator-planner
CC: operator
SUBJECT: Planner audit — still-open (greenfield confirmed); full spec-to-exit-gate map; frozen m-1/m-2 surface enumerated E1; three owed carries typed; replay corpus located; one scope-boundary question (S1-minimal recovery/FIFO line) to put to the guide

All paths below: `master/…` = master-docs/master/…; `x/…` = the archived upstream release tree; `m-1 :N` / `m-2 :N` / `m-7 :N` = line N of the m-1 / m-2 / m-7 locked design docs named in the supplement.

### 1. 4-bucket verdict

PRIMARY_BUCKET: still-open
still-open: the entire Slice-1 path (`mint → connect → render-MVP-form → submit → stamp → validate → lineage → append(crash-atomic) → project → deliver → gate-outbox`). frank/ contains zero source code — 3 commits, all docs/relay substrate (`git log --oneline`: 461fae0, acd34b4, 43e513e; tree = `docs/` + gitignored `.relays/` only). Evidence E2 (commands run this session).
already-closed: none in frank/ (no code exists). In the wider surface: the upstream relay-lint linter + its fixture matrix and the upstream store layout exist as shipped prior art — they are spec-designated PORT/REUSE inputs, not rebuild targets (see duplicate gate).
product-overlapped: none. No sibling team builds any part of this slice; the frozen m-1/m-2 contracts are consumed, not re-implemented (their owners gate our usage via the fidelity reviews).
recommended-next: proceed to pair DESIGN of the slice implementation (the code, not the contracts), carrying the design questions in §6; put Q-A (the S1-minimal recovery/durable-FIFO boundary, §6) to the m-7 guide early — it is the one place the exit gate and the scope-OUT list need a line drawn between them.

Duplicate/already-built gate (run honestly):
- Nothing to promote/enable/wire in frank/ — greenfield confirmed (E2).
- DO NOT COPY: the upstream `relay-lint.py` (62 checks) — the locked m-2 §10 dissolve/survive map (m-2 :131-179) dissolves ~33 prose checks under strict form-only submit (m-2 :118-120) and refactors ~16 into form-validation + ~13 into the lineage engine. Building the S1 validator by porting linter code would rebuild the thing the design deletes.
- REUSE AS SPEC'D: the upstream on-disk layout `<RELAY_ROOT>/<DISPATCH_ID>/<PHASE>-<ROLE>-<ts>.md` + append-only INDEX.md is explicitly retained (m-1 :135; layout unchanged per CQ-8, ARCHITECTURE.md:426, m-7 :83) — rendered `.md` becomes a derived projection of the canonical typed-envelope record.
- REUSE AS TEST INPUT: the upstream fixture matrix (§5 below) is the replay corpus, consumed read-only by the S1-minimal dissolved-linter replay.
- PRIOR-ART ONLY: jcode / claude-code / codex donor patterns cited throughout the locked m-7 design (§14 GRILL_SOURCE) — port principles, never code (charter Cardinal #3).

### 2. Spec-to-exit-gate map (every ROADMAP exit-gate line → spec source + fixture idea)

Sprint ROADMAP exit gate = docs/sprints/2026-07-03-s1-slice-1/ROADMAP.md:64-71; identical surface in the charter dispatch (.relays/s1/s1-dispatch/PLAN-orchestrator-planner-20260703-130634.md:59-66) and STEP-1-KICKOFF.md:39-48. m-7 fixture ids F1–F11 = m-7 :170-172; NF-S1..S18 = m-7 §12 seam matrix (:149-168).

| # | exit-gate line | spec source (E1) | testable fixture idea |
|---|---|---|---|
| B1 | accepted only through the conductor | m-2 §4 step 5 accepted-only invariant (m-2 :76); m-1 submit = sole governed write path (m-1 :125); m-2 §8 form-only submit (m-2 :118-120) | consumer-side: deliver/project serves only `accepted` records; no code path renders a non-committed candidate; hand-authored markdown dropped into the store dir is never served as a record (D5-residual-stated) |
| B2 | FROM system-stamped, not lane-supplied | m-1 I2 (m-1 :100-109); m-1 :126 step 1; m-2 :27 (ROLE same channel); NF-S1 (m-7 :151) | submit with payload `FROM`/`ROLE` set to a victim seat ⇒ stamped values win byte-for-byte; submit over unbound channel ⇒ reject, nothing staged |
| B3 | form/lint validation before delivery | m-2 §4 step 4 pre-append validation (m-2 :75-76); m-1 :127 step 2; m-7 §3 step 2 (m-7 :70) | a required-set/enum/seat-scope failure yields one terminal `rejected` evidenced record + bounce naming the failing field/edge; the candidate is never delivered |
| B4 | a gate produces a local outbox item | m-7 §9 local-outbox-only (m-7 :130-132); m-2 §17.2 ODB sub-schema (m-2 :285-303); gate fields m-2 §17.1 | one HUMAN_GATE-bearing accepted record ⇒ exactly one local outbox/ODB item, store-visible; drain/egress scan stays dormant (m-7 :132 — egress gate is Step-2 m-3) |
| A1 | forged FROM rejected | = B2 sources; m-1 attack enumeration (a)–(d) (m-1 :103-108) | NF-S1 first two legs (payload lie inert; unbound reject); plus new-connection self-declare ⇒ reject (m-1 :106 case c) |
| A2 | forbidden enum absent-then-rejected | m-2 fill-time authority (m-2 :35, :41); NF-S3 (m-7 :153) | schema introspection: pair-seat rendered `submit` schema contains no forbidden option; hand-crafted MCP call supplying it anyway ⇒ rejected at authoritative validation |
| A3 | invalid parent rejected | m-1 parent_picker (m-1 :139, :126); m-2 lineage gate over `persisted ∪ {candidate}` (m-2 :75-76); lineage-bounce ergonomics STEP-1-KICKOFF.md:52 | free-typed parent ∉ conductor-derived candidate set ⇒ reject; reason field distinguishes parent-unknown-recompose from parent-invalid-dead-edge (reason text, not a new state) |
| A4 | duplicate-sibling double-accept killed (actually exercised) | m-7 §2.4 (m-7 :63-64); NF-S2/F1 (m-7 :152, :172) | two concurrent submits/verifies consuming the same one-shot authority enqueued together ⇒ exactly one wins; loser fail-closes typed; replay after restart still burned |
| C1 | kill -9 mid-commit ⇒ exactly-once + re-issued wake | m-7 §4 pivot (m-7 :78-85); F3 pre-pivot, F4 post-pivot (m-7 :172); wake re-issue m-7 §8.3/§11 | crash before rename ⇒ no committed record, staging cleaned, intake re-enqueued once; crash after rename ⇒ projections rebuilt, authority not re-consumed, wake re-issued; no duplicate either way |
| C2 | kill -9 mid-delivery ⇒ exactly-once + re-issued wake | m-7 §8.3 pipe=nudge (m-7 :121-122); intake/outcome referencing m-7 §2.2 (m-7 :56-58) | crash between commit and pipe write ⇒ recovery re-issues the nudge; recipient receives exactly one record via project() regardless |
| C3 | crash after intake-fsync ⇒ no lost intake | m-7 §2.2 (m-7 :57); F9 (m-7 :172) | kill -9 with N enqueued, k outcomes reached ⇒ recovery re-enqueues exactly N−k in arrival order |
| C4 | crash before/after atomic rename ⇒ presence=committed | m-7 §4 (m-7 :81, :84); F10 fsync-ordering (m-7 :172); ARCHITECTURE.md:423-426 | simulated power-cut between record-fsync and rename, and between rename and projection writes ⇒ store converges correctly both times |
| C5 | corrupt-projection rebuild from canonical | m-7 §4 projections-derived (m-7 :82); recovery phase 2 shape (m-7 :92) | corrupt/delete INDEX.md or a rendered `.md` or a mailbox ⇒ rebuild from canonical records; canonical wins unconditionally |
| C6 | replayed intake-id ⇒ no double-emission | m-7 §2.2 clear-on-pop atomic with outcome (m-7 :58); F9/F11 (m-7 :172) | re-present an intake entry whose `intake_id` has an outcome ⇒ zero re-emission; every outcome record references its `intake_id` |
| R1 | S1-minimal dissolved-linter replay | m-2 §10a dissolve map (m-2 :135-149); STEP-1-KICKOFF.md:45 (S1-scoped, VP RECONCILE); corpus §5 below | replay harness runs the MVP-FieldSpec-covered subset of the historical failure corpus through the MVP validator; each is caught-or-genuinely-obsolete; report names the uncovered remainder as S3 |
| P1 | I-PH: no canonical store path in any seat-facing output | ARCHITECTURE.md:463 (I-PH) + :482 (owed fixture); m-7 §8.4 absence set (m-7 :124-125); G(iii)/F6 shape (m-7 :168) | grep every seat-deliverable surface — bounces/error text/projections/delivery payloads, every fixture's outputs included — for canonical store/config/outbox/operator-channel paths ⇒ zero hits |
| L1 | liveness: inbox durable, pipe write = nudge | m-7 §8.3 (m-7 :121-122); STEP-1-KICKOFF.md:47, :51 | kill/park the recipient seat, deliver, reconnect ⇒ record present via project(); drop a wake ⇒ recovery/re-nudge path re-issues; no parked lane sleeps forever on a lost wake |
| W1 | park/wake: gated lane parks (consumes nothing) + wakes on operator verdict | m-7 §11 (m-7 :139-143); NF-S13 (m-7 :163); m-1 operator address (m-1 :142-145) | a gate-bearing accepted record parks the lane; parked lane consumes nothing; operator verdict record commits through the loop ⇒ one pipe write wakes it; kill -9 while parked ⇒ state restored from store records alone |

Spec gaps: none at the line-item level — every exit-gate line maps to locked spec. One scope-BOUNDARY ambiguity (not a gap) escalated as Q-A in §6.

### 3. Frozen contract surfaces we consume (verbatim, E1)

m-1 = the m-1 trust/identity design-of-record (2026-06-28):
- :124 `mint_seat(seat_address) → connection_credential` — conductor-only, spawn-time; binds seat ↔ {connection, credential}; mailbox provisioned.
- :125 `submit(relay_body) over seat_connection → relay_id` — "the **sole *governed* write path**"; candidate held in-courier, "no persisted `submitted` limbo state".
- :126-128 the three submit steps — (1) resolve+stamp: "reject if unbound"; stamp `FROM`/`ROLE` from the binding; system envelope `relay_id`, `DISPATCH_ID`, `timestamp`, `schema_version`, `certification = null`; `PARENT` via parent_picker; `TO`/`CC` via recipient_picker. (2) pre-append validation: (a) m-2 form-validation, (b) m-2 lineage gate over `persisted ∪ {candidate}`; (c) observe hook RESERVED Step-2. (3) atomic commit: one atomic append of `accepted` + INDEX + deliver, single transaction, TOCTOU-closed; on fail one atomic terminal `rejected` evidenced record + bounce; internal fault on authority ⇒ `held`. Outcome set byte-exact `{accepted, rejected, held}`.
- :129 "**Step-1 = store + form + lineage**" — observe gate + observed-field fill land Step-2; "Step-1 records carry no observe gate."
- :130 `project(seat_connection) → [relays addressed to caller]` — reads only own mailbox (own `TO`/`CC`).
- :131 `read(relay_id) → {record, schema_version}` — canonical immutable records; store is migration-agnostic.
- :135 on-disk: reuse the upstream layout `<RELAY_ROOT>/<DISPATCH_ID>/<PHASE>-<ROLE>-<ts>.md` + append-only `INDEX.md`; each `.md` a rendered projection; canonical object = the typed envelope; records immutable + append-only.
- :138 system_only set: `FROM`, `ROLE`, `relay_id`, `DISPATCH_ID`, `timestamp`, `schema_version`, `certification` (null-reserved).
- :139 parent_picker: conductor supplies candidate set + default; agent selects within it; free-typed parent rejected.
- :141 recipient_picker: `TO`/`CC` sender-selected, courier-validated ∈ minted address space.
- :142-145 `operator` = first-class minted address: (i) mailbox/projection, (ii) recipient_picker member, (iii) operator-FROM stamped via the operator-relay channel, never a lane credential.
- :89-93 DI-1..DI-5 (Step-1 I1 guardrail-borne, not store ACL — :89); :95-109 I1/I2 proofs (confusion-resistant, D5 residual stated).
- PLAN carries we inherit: :225-227 submit as one atomic conductor-owned commit (TOCTOU); :228-229 seat-credential lifecycle (generation/rotation/revocation); :254-259 operator-relay channel isolation (lanes have no write access to it).

m-2 = the m-2 forms/determinism design-of-record (2026-06-28):
- :21-27 three-layer envelope (envelope/headers/body); ROLE system-owned envelope, read-only projection in header view.
- :29 one-FROM rule — no header `From:`; FROM lives only in the system-owned envelope.
- :33-39 owner model `{system | seat_scoped_enum | agent_enum_pick | free_text}` + hybrids `parent_picker`/`recipient_picker`/`monotonic` (floor = MAX across raisers).
- :49-67 the FieldSpec shape (id, layer, owner, type, enum_set, gate_referenceable default false, seat_scope, required_when, visible_when, fill_constraints, consumers, lineage_role) — the envelope we must instantiate, not redesign.
- :71-77 render/validate flow steps 1–6; :75 step 4 pre-append (a) form-validation (b) lineage gate, candidate in-courier; :76 step 5 one atomic `accepted` commit; `held` carve-out (consumed only by gate/escalation/scheduler/operator machinery); lineage has blocking edges only for authority-bearing records; :77 markdown = view/export only.
- :83-91 bounded predicate vocabulary (closed atom set + all_of/any_of/not; `layer_present` atom); :95-97 CQ-1 step-gate — observe-owned requireds gated off in Step-1; hard guardrail: every system_only/lineage/form-owned required stays fully required.
- :101 `slot_in` = RESERVED atom, no Step-1 values, no predicate branches on a concrete slot.
- :118-120 §8 legacy strictness — form-only submit; hand-authored markdown read-only history, never submittable.
- :125-126 `schema_version` stamped; breaking-change contract (the `{accepted, rejected, held}` enum is byte-exact-locked at three).
- :131-179 §10 dissolve/survive map — the replay's semantic key: ~33 prose-only checks DISSOLVE (:135-149), ~16 survive as form-validation (:151-161), ~13 survive as the lineage engine (:163-172); :177 DISPATCH-token authority survives as seat_scoped_enum + phase-scoped field.
- :241 AC16 `delivery_state` closed `{accepted, rejected, held}`; `held` narrowed to internal-fault-on-authority + CQ-2 fail-closed; ordinary human-gate parking is `accepted`, not `held`.
- :243 AC18 known-A/RAISE-ONLY: `gate_category` monotonic-toward-A; "the m-7 known-A/RAISE-ONLY NF fixture is a **registered owed artifact** gated to Step-1 build."

m-7 hosting spec (consumed as the engine shape we implement) = the m-7 design doc: §2.1-2.4 (single intake-writer; one commit thread; concurrent lock-free reads; the §2.4 by-construction claim scoped exactly), §3 pipeline order, §4 Package-A rename pivot (presence=committed; projections derived via redo journal; fsync-before-rename, dir-fsync-after), §6 fault→`held` one-compound-record one-pivot (m-7 :100), §8 guardrail (registry exactly {submit, project, read}; submit schema IS the rendered m-2 form; absence set :124-125), §13 F1–F11 (:172 — incl. F9 no-stale-re-emission, F11 one-pivot-per-mutation), §16 claim-sweep classes (semantics not vocabulary; writability-token family included).

### 4. Owed §C4 carries — typed owed-item records

1. Code-layer interface-guardrail enforcement — {owner: s1-core (build), m-7 (spec home); source: ARCHITECTURE.md:433-436 + :477 ledger + STEP-1-KICKOFF.md:57 ("the one genuinely new build item") + m-7 :116; target surface: frank's per-seat MCP tool registry — exactly `{submit, project, read}`, raw store/config/outbox/operator-channel paths + config values absent from every seat surface; disposition path: S1 DESIGN → PLAN names G(i)/G(ii)/NF-S18 fixtures → IMPL → S1 exit gate (E2)}.
2. I-PH path-hygiene fixture — {owner: s1-core (build), m-7-hosted, m-1/m-2 honor it in store + bounce/reason text; source: ARCHITECTURE.md:463 (§C4.3 I-PH) + :482 (owed ledger entry, external-review 2026-07-03) + STEP-1-KICKOFF.md:11; target surface: every seat-delivered surface frank emits (bounce messages, error text, rendered projections, delivery payloads); disposition path: S1 PLAN names the grep-based negative fixture (G(iii)/F6 shape, m-7 :168) covering every fixture's outputs → S1 exit gate row P1}.
3. ③ known-A / RAISE-ONLY NF fixture, guardrail-adjacent portion — {owner: s1-core (the guardrail-adjacent portion only), m-7-side per the ledger, flagged owed by m-2 c6-fix + m-6; source: ARCHITECTURE.md:478 + m-2 :243 (AC18) + m-2 §17.1 (m-2-F1 fold, m-2 :381); target surface: the rendered-form affordance constraint `[floor, A]` + the validate-path monotonic-toward-A raise with `gate_category_raised` recorded, at whatever grain the MVP FieldSpec carries `gate_category`; disposition path: S1 DESIGN decides the S1 grain (Q-C in §6, guide-checked) → PLAN fixture → S1 exit gate; the full ③ fixture remains a Step-1-build-wide owed item beyond the S1 portion}.

Note for the map's completeness: the §C4 ledger's other owed items (⑤ ODB model-name egress fixture, R2 gate_referenceable per-column negatives, `GRILL_REQUIRED` FieldSpec row, `routing_escalation` §J2 member — ARCHITECTURE.md:479-481, :473) are Step-1-build-wide or step-(d) carries the charter does NOT assign to S1 (charter :72 names exactly the three above). No action in S1 beyond not contradicting them.

### 5. Replay corpus — LOCATED

<relay-lint tools>/relay-lint-fixtures/ — 244 fixture .md files across 14 category dirs (claude, lint-test, probes, addressing, identity, merge, merge-token, lineage, fold, design-review, content, orch-review, p9, rowtruth), with the expected-outcome manifest in its README.md (pass/fail per fixture, incl. --relay-root tree cases) and the checker `tools/check-relay-lint-fixtures.py` beside `tools/relay-lint.py` (the 62-check linter m-2 §10 classifies line-by-line). Evidence E2 (`find | wc -l` this session). This is the concrete historical-failure set: each fail-fixture encodes one historical lint-failure shape; the S1-minimal replay selects the subset whose checks the MVP FieldSpec covers (§10a/§10b classes), runs them through the MVP validator, and reports caught-or-genuinely-obsolete (obsolete = dissolved-by-construction under form-only submit, e.g. fence/row/bare-token grammar cases that a typed envelope cannot express). The full ~33-check dissolve validation stays S3 (STEP-1-KICKOFF.md:45).

### 6. Design questions (capture only — decisions belong to DESIGN; Q-A also needs the guide)

- Q-A (scope boundary — escalate to m-7 guide at DESIGN start): the exit gate requires crash-after-intake-fsync, replayed-intake-id, corrupt-projection rebuild, and re-issued wake (rows C3, C5, C6) — i.e. a working intake journal + a minimal recovery pass — while the scope-OUT list defers "full recovery phases 0–4 + FIFO durability" to S2. Where exactly is the S1-minimal line? Our reading: S1 needs the intake journal + the §4 pivot + a dumb-replay recovery sufficient for the crash matrix (staging cleanup, projection rebuild, intake−outcomes re-enqueue, binding-table restore), WITHOUT genesis/quarantine/GC/segment-rotation/the full phase-0-to-4 machinery (genesis + GC are explicitly S2-OUT). The plan must draw this line precisely; asking the guide to confirm the reading beats discovering it at the plan gate.
- Q-B (runtime/language + process shape): the locked docs prescribe topology, not implementation language — one conductor process, single intake-writer task, one commit thread, MCP server, per-seat stdio/bearer channels, pipe-write wake (m-7 §1/§2/§8). "Riding existing runtimes" constrains the SEATS, not the conductor. DESIGN picks the conductor's language/stack + MCP server library; the §17 non-re-cut invariants (loop, pivot, store shape, 3-verb surface) are the fixed points.
- Q-C (MVP FieldSpec concrete set): the m-2 envelope fixes the shape; DESIGN picks the minimum field set the slice needs — candidate floor from this read: the system envelope (:138 set + TO/CC + PARENT), PHASE/AUTHORITY-class header enums sufficient for seat-scope + one forbidden-option fixture (A2), one gate field pair (`HUMAN_GATE_REQUIRED` monotonic + `gate_category` at whatever grain Q-C/③ needs), `delivery_state`+`failing_edge` computed slots, and the `X-` overflow namespace. The choice fixes the replay subset (R1) — pick fields to maximize covered §10a/§10b classes per unit of build.
- Q-D (crash-fixture mechanics): F3/F4/F9/F10/F11 fix the shapes; DESIGN picks the injection mechanism for deterministic kill -9 at named points (e.g. conductor-internal test-only crash-point hooks keyed by env/config absent from any seat surface, vs external syscall-boundary sweep per m-7 §19.2). I-PH applies to fixture outputs too (row P1).
- Q-E (mint/connect/park-wake S1 minima): mint_seat conductor-internal at attach (m-1 :124, m-7 §8.5); connect = channel binding + credential re-present on re-attach (persisted binding table, m-7 §11 — CQ-6 base); park/wake minimal = W1 row semantics + the operator verdict channel (m-1 :142-145 operator address, verdict stamped via operator channel). DESIGN decides the S1 operator-channel realization (it must exist for W1/B4) and which seat runtime(s) the E2 fixtures drive (a scripted test seat suffices for E2; lane-qualification probes for real runtimes are operator-gated spikes, m-7 :122 — not S1 gates).

### 7. Boundary-contract assessment

The dispatch's locked boundary contract is consistent with the frozen surfaces read; no writer-with-no-reader or reader-with-no-writer found. Writes: append-only store records (terminal byte-exact `{accepted, rejected, held}`) + one local outbox/ODB item — consumers: seat inboxes via project() (B1/L1), the exit-gate fixtures, the operator mailbox (W1). Reads: the m-1 verbs + m-2 envelope exactly as enumerated in §3. Target entity: the committed relay + rebuilt projection — the crash matrix (C1–C6) is precisely target-entity proof. The outbox item's S1 consumer is the exit-gate fixture + operator surface; the egress-scan drain stays dormant (m-7 :132) — stated, not over-claimed. Honesty framing verified against the locked claim boundary: only the serialized-loop kill (§2.4) and constrained-grammar R2 are operationally live claims in S1; everything else recorded-not-enforced; D5 residual must be stated beside every exclusivity-shaped claim in anything seat- or user-facing we write, per the §16 sweep classes (semantics + writability-token family, not just vocabulary).

### 8. Risks / reject-or-narrow gates

- Pivot-shape risk (guide-gate #5): if the S1 commit path commits any mutation class via more than one rename, or makes INDEX/projections co-canonical, S2 re-architecture is forced — DESIGN must carry F11 one-pivot-per-mutation from the first line of code.
- Enum drift risk (guide-gate #4): any synonym (`bounced`, `submitted`) appearing as a value token anywhere fails NF-S16; byte-exact from day one.
- Claim-sweep risk: S1 docs/code comments/tool descriptions are seat- or user-facing surfaces — the §16 sweep classes apply to them; cheapest to write compliant than to scrub.
- Scope creep risk: Q-A is the only place the exit gate pulls toward an OUT item; resolved by drawing the guide-confirmed minimal line, never by absorbing S2 recovery wholesale.
- No reject/narrow trigger found: the dispatch bundle is buildable against the locked spec as chartered.

### 9. Evidence & protocol fields

Evidence by claim: all spec claims E1 (file:line cited in place); repo-state and corpus-location claims E2 (git/find/ls commands run this session); no E0 claims knowingly asserted.

Questions for Implementer (reconcile): does your independent read of the scope-OUT vs crash-matrix boundary match Q-A? Did you locate any additional failure corpus beyond the fixture matrix (e.g. organic failed relays in the master trail) worth including in the replay set? Do you concur the migrator-registry machinery (m-2 §9) is out of S1 (absent from the IN list; `schema_version` stamping only)?

ACTIONS_GIT_REF: no edits to any tracked file; writes this session = this relay file + one INDEX.md row under gitignored .relays/s1/ (boot-ACK relay earlier in session, same substrate); read-only elsewhere incl. all master/ and extracted/ paths
FINAL_GIT_STATUS_SHORT: none — clean tree (fresh `git status --short` run at 20260703-140226, empty output, immediately before authoring this relay; only subsequent writes are this file + its INDEX row, both gitignored)
