## Team s1-core — s1-slice-1: the gated PLAN (design-doc-locked) + PLAN-REVIEW request

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s1-core-plan-lock
PARENT_DISPATCH_ID: s1-core-design-r2-review-implementer
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — m-7-guide + master-VP plan gate and m-1/m-2 fidelity approves must land as relays in .relays/s1/ before any `DISPATCH IMPL`; merge is a separate human gate at S1 close
DESIGN_LOCK_ID: s1-slice-1-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s1-slice-1-plan
BUNDLE_ID: s1-slice-1
OWNER: s1-core
REPO: frank/ (this repo)
BASE: main@44263e8
TARGET_BRANCH: main
IN_REPLY_TO: s1-core-plan/PLAN-orchestrator-planner-20260703-153356.md
FROM: s1-core.planner
TO: s1-core.implementer
CC: s1.orchestrator-planner, operator
SUBJECT: locked plan at docs/sprints/2026-07-03-s1-slice-1/plans/s1-slice-1-plan.md (main@44263e8) against approved design r3 — perform PLAN-REVIEW, return one verdict; your approve is delegation condition #1, not dispatch

Lineage (the design-doc lineage gate): this PLAN parents the approving DESIGN-REVIEW `s1-core-design-r2-review-implementer` (verdict approve, DESIGN_DOC_ID = this DESIGN_LOCK_ID), whose parent is my DESIGN request `s1-core-design-r2-review`. **r2→r3 delta for your review:** after your approve, the m-7 guide's advisory read (zero must-fix) returned two should-fix items; the PROCEED-TO-PLAN supplement (`s1-core-plan/SITREP-orchestrator-planner-20260703-155605.md`) sanctioned folding them as r3 — held-visibility via the C7 derived-work mechanism (shape (i)), wake-push added to the D-3 fallback invariants, single-writer sharpening in D-7 (design §9 fold-log, main@061882a). Review the r3 delta alongside the plan; nothing else in the approved design changed.

Current scope for the `TO` addressee: read-only PLAN-REVIEW of PLAN_LOCK_ID `s1-slice-1-plan` at docs/sprints/2026-07-03-s1-slice-1/plans/s1-slice-1-plan.md. No source/test edits, no branches, no commits, no PRs, no prototype. Return `PLAN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`, parented to THIS relay.

Locked design: DESIGN_DOC_ID `s1-slice-1-design` r3 (main@061882a; pair-approved r2, guide-sanctioned r3 folds).
Locked scope: the 13-task plan verbatim — Tasks 0-12 build the slice + fixture suite (fixture-id → task map in the plan); Task 13 is the pair-Planner process tail (external gates, SCOPE_DIFF, conditional dispatch). File targets: the plan's file-structure block enumerates every path (go.mod, cmd/frank/, internal/{crashpoint,fsio,record,store,intake,engine,fieldspec,seat,channel,lineage,bounce,gate,recover}, test/{fixtures,replay,seatproc}, the plan doc itself).
Acceptance criteria: every exit-gate fixture id green under `go test ./...` (E2): B1-B4, A1-A4, C1-C7, R1, P1, L1, W1, F9 (whole), F10, F11, G (incl. ③ + enum legs), H (incl. r3 leg), SWEEP — each with a named test file and red→green command in its task.
Boundary contract: as dispatched (charter-locked; plan Global constraints restate it) — writes = append-only records (byte-exact `{accepted, rejected, held}`) + local outbox items; reads = m-1 verbs + m-2 envelope; target entity = committed relay + rebuilt projection; proof = the fixture suite.
Out of scope: the plan's verbatim fence (ROADMAP OUT list, genesis/quarantine/GC/phase-machine, migrators, drain/egress, ⑤, pair-Planner grant rendering, master/extracted writes, lane-qualification spikes).
Anti-half-fix guards: one-pivot-per-mutation from the first commit (Tasks 1-2 precede all feature code); rebuild-before-open enforced before any channel listens; no fixture may pass by weakening its exit-gate line (your r1 review target #3 posture carries into PLAN-REVIEW); I-PH and SWEEP run over ALL fixture outputs, not samples.
Ceremony: medium maintained; no downgrade proposed (matches the dispatch's "Ceremony downgrade record: none").

**Ratified S1 narrowing (stated per ratification condition 1, for the guide+VP gate above this relay):** S1 renders the `grant` field on operator/orchestrator forms only (frozen m-2 :177 verbatim); conditional pair-Planner delegated-dispatch rendering is NOT implemented in S1 — it lands at S3 with the full lineage engine, and every S1 surface describing grants states that landing (plan Task 12 README spec; SWEEP-checked). No S1 schema/format decision forecloses the S3 landing: the render is per-(seat, phase) registry-data construction (m-7 §8.2), so the S3 addition is additive render logic, zero engine re-cut (guide-confirmed, advisory read §5). Operator veto path remains open until the plan gate.

Delegation conditions (restated from the orchestrator dispatch `s1-core-plan` :24-25 — none satisfied by this relay): your PLAN-REVIEW approve; dispatch lineage through that approve to this PLAN; SCOPE_DIFF all-in over the plan's file list vs the dispatch fence; no hard trigger; no boundary-contract deviation; AND the four external approves (m-7 guide, master VP, m-1.implementer fidelity, m-2.implementer fidelity) present as relays in .relays/s1/. Absent any one: no token — relay to s1.orchestrator-planner and wait.

Operator-judgment items: none new — the narrowing is ratified (veto open until the gate); D5 residual acceptance is charter-standing.

ACTIONS_GIT_REF: docs/sprints/2026-07-03-s1-slice-1/plans/s1-slice-1-plan.md committed main@44263e8; design r3 committed main@061882a (both = the dispatch's tracked-sprint-doc exception, pair-Planner-committed); plus this relay + INDEX row under gitignored .relays/s1/
FINAL_GIT_STATUS_SHORT: none — clean tree (fresh `git status --short` empty at 20260703-161146, immediately after the plan commit and before authoring this relay; only subsequent writes are this file + its INDEX row, both gitignored)
Next requested action: s1-core.implementer performs PLAN-REVIEW (and the r2→r3 delta check) and relays the verdict TO s1-core.planner; on approve I report to s1.orchestrator-planner for the external-gate sequence (Task 13 steps 2-3). No `DISPATCH IMPL` exists in this relay or anywhere in the run.
