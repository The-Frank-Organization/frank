## Team s1-core — s1-slice-1: gated PLAN r3 (F-M1-1 fold) + NARROW pair re-review request

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s1-core-plan-lock-r3
PARENT_DISPATCH_ID: s1-core-design-r2-review-implementer
RUN_ID: s1
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — m-1's approve on this revised surface is the last outstanding external gate; guide/VP/m-2 approves stand (orchestrator routing `s1-core-plan/SITREP-orchestrator-planner-20260703-171643.md`)
DESIGN_LOCK_ID: s1-slice-1-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s1-slice-1-plan
BUNDLE_ID: s1-slice-1
OWNER: s1-core
REPO: frank/ (this repo)
BASE: main@50b2b94
TARGET_BRANCH: main
IN_REPLY_TO: s1-core-plan/SITREP-orchestrator-planner-20260703-171643.md
FROM: s1-core.planner
TO: s1-core.implementer
CC: s1.orchestrator-planner, operator
SUBJECT: NARROW re-review of the F-M1-1 fold only (plan r3 + design r5, main@50b2b94) — your r2 approve stands for everything else; one verdict, then I report for the m-1 re-route

Lineage: parent = the approving DESIGN-REVIEW `s1-core-design-r2-review-implementer` (design-doc gate edge, unchanged; DESIGN_LOCK_ID now r5). This relay supersedes `s1-core-plan-lock-r2/PLAN-planner-20260703-162535.md` as the current gated PLAN; ALL r2-relay content not named below carries forward verbatim, and per the orchestrator's routing your `…-162820` approve stands for everything outside this fold.

Narrow fold under review (the m-1 fidelity block F-M1-1, shape (b) — chosen because the design defines no remint/recycle, exactly m-1's stated condition for (b); plus the guide's non-blocking key-scheme nit):
1. Design r5 (§11 fold-log): D-3 gains the single-credential-generation invariant — `mint_seat` on a bound seat rejects typed, NO second credential, binding table unchanged, existing credential keeps resolving; no code path creates a stale/parallel credential; unbound credentials reject before staging; rotation/revocation = later m-1 slice, additive on this base. D-7 gains the one-line key parenthetical (`gate_record_ref` IS `source_record_ref` with `source_kind=gate`).
2. Plan r3: Task 5 interfaces now carry `ErrSeatAlreadyBound` + the invariant; Task 5 step 1 gains the E2 lifecycle legs (double-Mint ⇒ typed reject + byte-identical binding table + original credential resolves + exactly-one-credential scan; unbound-reject shared with B2).
3. NOT weakened (the orchestrator's carry-forward watch-surfaces, verbatim intact): F11 "exactly one pivot per mutation class"; P1 captures push frames + tool descriptions, not just bounce/error text.

Current scope for the `TO` addressee: read-only NARROW PLAN-REVIEW of the fold above (plan r3 + design r5 delta at main@50b2b94). No source/test edits, no branches, no commits, no PRs. Return `PLAN_REVIEW_VERDICT` parented to THIS relay.

Delegation conditions: unchanged (orchestrator dispatch `s1-core-plan` :24-25); m-1's approve on this revised surface replaces the blocked leg; SCOPE_DIFF (README.md row citing the fence-amendment relay `…-170259`) runs only after all four approves are relays in .relays/s1/. No dispatch token exists in this relay or anywhere in the run.

ACTIONS_GIT_REF: design r5 + plan r3 committed main@50b2b94 (tracked sprint docs per dispatch exception); plus this relay + INDEX row under gitignored .relays/s1/
FINAL_GIT_STATUS_SHORT: none — clean tree (fresh `git status --short` empty at 20260703-172222, immediately after the fold commit and before authoring this relay; only subsequent writes are this file + its INDEX row, both gitignored)
Next requested action: s1-core.implementer narrow re-review → verdict TO s1-core.planner; on approve I report to s1.orchestrator-planner for the m-1.implementer narrow re-route, then hold for the final approve.
