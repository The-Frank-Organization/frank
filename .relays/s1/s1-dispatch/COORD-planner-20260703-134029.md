## COORD — m-7 guide → s1: your onboarding context-brief (read this + your dispatch first; you boot with zero governance context)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s1-dispatch
PARENT_DISPATCH_ID: s1-dispatch
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: c4-design-m-7-conductor-core
IN_REPLY_TO: s1-dispatch/PLAN-orchestrator-planner-20260703-130634.md
FROM: m-7.planner
TO: s1.orchestrator-planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: guide context-brief for the fresh s1 session — who you are, what frank is, the absolute-path spec surface, conventions, and how to reach me

You are a **fresh session booting in ``** — the governance workspace's charter does NOT auto-load for you, and your dispatch's relative pointers (`master/...`) don't resolve from your cwd. This brief, from your **m-7 guide**, gives you everything "up here." Read it together with your charter dispatch (absolute path below), then act.

### 1. Who you are
- **`s1.orchestrator-planner`** — Orchestrator Planner of the **s1 slice-team**, a self-contained orchestrator-team instance (own reviewer + pairs, structure/granularity yours to decide). Suggested `RUN_ID: s1`.
- **Mission:** plan + implement **Slice-1** of the frank Step-1 build — the brutally-small end-to-end relay path (full scope in your dispatch).
- **Method:** invoke **`/orchestrator-planner`** — it carries the relay protocol + `sprint-doc-setup`. **Your relays + sprint docs live in `frank/`** (its `docs/sprints/<date>-<slice>/` tree + gitignored `.relays/<RUN_ID>/`), NOT in the governance trail. Code lands in `frank/` on `main` (empty repo, 0 commits — your first commits).

### 2. What frank is (one paragraph)
**frank** = a thin trusted courier for governed multi-agent dev teams. Its core (the "conductor") is an MCP server that agent sessions (seats) connect to; seats reach exactly **`submit` / `project` / `read`** — the conductor stamps identity from the channel (`FROM` is system-stamped, never lane-supplied), validates form + lineage, commits crash-atomically to an append-only store with byte-exact terminal outcomes **`{accepted, rejected, held}`**, and delivers via projection. Step-1 removes the human operator-as-transport. It is **provenance + transport, not verified work** ("done" is `self_reported` in Step-1) — keep that honesty in code and docs.

### 3. The structure above you (context, not your management chain to re-create)
A standing **master** governance team designed frank and dispatched you: CTO (`master.orchestrator-planner`, who wrote your dispatch), VP reviewer (`master.orchestrator-reviewer`), and seven domain pairs `m-1…m-7`. The human **operator** (Jack) hand-carries relay files between sessions — that's how any relay "arrives." Your slice consumes three locked domain designs: **m-1** (trust/identity: store + channel-stamped FROM), **m-2** (forms: FieldSpec envelope + validation), **m-7** (conductor-core: the engine — my domain; I am your guide). *(Historical docs may say `pcode/` — that's `frank/`'s old name, renamed 2026-07-03.)*

### 4. Your read-only spec surface (ABSOLUTE paths; do not edit any of these — escalate spec problems to master, never self-amend a locked design)
1. **Your charter dispatch (read first):** `.relays/s1/s1-dispatch/PLAN-orchestrator-planner-20260703-130634.md` — scope IN/OUT, exit gate, boundary contract, gates.
2. **Step-1 kickoff (build strategy + hardened exit gate):** `master-docs/master/STEP-1-KICKOFF.md`
3. **The engine spec:** `master-docs/master/ARCHITECTURE.md` — §C4.1 (engine), §C4.2 (seam matrix), §C4.3 (claim boundary + the **I-PH** path-hygiene invariant), plus the owed Step-1 fixture ledger just above §C5.
4. **The m-7 conductor-core design-of-record (DESIGN-LOCKED):** `master-docs/master/domains/m-7-conductor-core/design/2026-07-01-conductor-core-design.md` — for S1: §2 (process model), §3 (commit pipeline), §4 (rename-pivot crash-atomic commit; presence=committed), §6 (fault→`held`), §8 (interface guardrail + wake), §12 (seam matrix), §13 (fixtures F1–F11), §16 (claim-sweep rules).
5. **Frozen consumed contracts:** m-1 `master-docs/master/domains/m-1-trust-identity/design/2026-06-28-trust-identity-design.md` · m-2 `master-docs/master/domains/m-2-forms-determinism/design/2026-06-28-form-schema-design.md`. Build **against** them; their `.implementer` seats fidelity-review your **usage** before your `DISPATCH IMPL` is live.
6. **Sequencing context:** `master-docs/ROADMAP.md` (Step-1).
7. **For the S1-minimal dissolved-linter replay:** the shipped upstream baseline (historical lint failures) is under the upstream protocol release corpus (not vendored).
8. **My guide-gate checklist (pre-published so you can build your PLAN to it):** `.relays/s1/s1-dispatch/SITREP-planner-20260703-133102.md`.

### 5. Non-negotiables (the ones slice-teams most easily get wrong)
- **No code before the plan-gate:** your S1 PLAN is gated by **me (m-7 guide) + the VP** before any `DISPATCH IMPL`. Scaffolding your sprint substrate in `frank/` is allowed now; building the conductor is not.
- **Byte-exact enum:** terminal outcomes are exactly `{accepted, rejected, held}` — no synonyms, `bounced` is not a value token.
- **Scope fence:** the OUT list in your dispatch (full recovery, durable FIFO, GC/genesis, full FieldSpec registry, 62-check refactor, consumer schema fields, observe, routing) is deferred — if a task seems to need one, **escalate to master; do not expand**.
- **Claim honesty:** confusion-resistance is **tool-mediated** (removes affordance, not access); **D5** — a same-uid shell-bearing seat can bypass — is an accepted residual and must be stated next to any exclusivity-shaped claim ("only writer", "sole egress", …). Only the **serialized-loop double-accept kill** (and constrained-grammar R2) may be presented as operationally live in S1.
- **I-PH:** no seat-facing output — bounces and errors included — may contain a canonical store/config/outbox path.
- **Owed carries materialize-first:** the code-layer guardrail enforcement, the I-PH fixture, and the ③ known-A/RAISE-ONLY portion each get a typed owed-item record `{owner, source, target surface, disposition path}` before you treat them as covered.

### 6. How to reach me (your domain guide)
Write a relay file addressed `TO: m-7.planner` (from your own address; never author another seat's `FROM`), tell the operator, and they carry it to my session. I answer domain questions strictly from the locked design-of-record; if your question needs a locked contract amended, I escalate it to master rather than answer around it. When your S1 PLAN is ready, return it the same way for the guide+VP gate.

### 7. Suggested first actions
(1) Read your dispatch + the kickoff; (2) invoke `/orchestrator-planner` and scaffold your sprint/relay substrate in `frank/` via `sprint-doc-setup`; (3) decide your team structure; (4) draft the S1 PLAN against §4-listed specs and my gate checklist; (5) relay the PLAN to `m-7.planner` for the gate.

Not authorized / not claimed: this brief grants nothing — no `DISPATCH IMPL`, no code, no merge, no scope change; all authority derives from your dispatch and the gates named in it. Where this brief and your dispatch could ever disagree, the dispatch wins.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master-docs/master/relays .relays/s1/s1-dispatch/COORD-planner-20260703-134029.md` — run before handoff (result in inline pointer)
- every absolute path in §4 verified present on disk this session (ls/grep)
- `git -C frank status --short --branch` — `## No commits yet on main`

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created this relay + one INDEX.md row timestamped 20260703-134029; no doc/design edits; no code/`frank/` writes
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = empty, no commits on main)
Next requested action: operator hands the fresh s1.orchestrator-planner session this brief + the dispatch (both absolute paths); s1 boots via /orchestrator-planner in frank/ and proceeds per §7.
