## PLAN — the s14 build plan r7 (the BOUNDED successor folding master's R9 nested-gate determinism ruling `master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-135631.md`; supersedes r6 `PLAN-planner-20260821-131336.md` as the plan of record; the ONLY change = ONE new task under R9's executor-package grant + the GREEN-evidence rebind): **T9.4 — the R9 owner repair of `internal/executor/**` (source + its tests) ONLY, executed by s14.implementer inside R9's six invariants:** (I1) `GOPROXY=off` (or the strict equivalent) in the spawn env — nested module resolution NEVER touches live network; a missing module fails immediately and NAMED; (I2) local module source, the implementer's mechanism choice — pre-seed the staged cache from the host's go.sum-verified module cache OR pass the host `GOMODCACHE` through — the fold states the choice + its isolation rationale; no durable host mutation beyond Go's own cache/lock discipline; fresh-per-run `GOCACHE`/`GOPATH` hermeticity stays; (I3) the companion — `finalizeRun` attaches a BOUNDED tail of the captured inner output to `FailingDetail` on the fail path (explicit cap; the `suite-exit-mismatch` token RETAINED as prefix; outcome/predicate/rung mapping byte-preserved; **if this needs ANY record-schema/observe-contract change beyond the existing string field: STOP, back to master — m-3 design surface**); (I4) scope = `internal/executor/**` only — any byte outside = back to master BEFORE the edit; `scripts/dogfood-suite.sh` untouched; (I5) evidence per the ruling (§2 below); (I6) ONE liftable commit citing R9, ordered before the GREEN evidence runs. Vendoring and the script shape are DECLINED on the record. Everything else in r6 — T9.5 (the R8 fixture commit, diff already staged uncommitted), T1a–T9 as committed at `471532a…`, the preserved T10, T11–T15, §2a, rails — carries byte-for-meaning.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
GRILL_REQUIRED: no — a bounded fold of master's R9 ruling; the invariants are master's bytes, the mechanism choice inside them is reviewed at the fold
HUMAN_GATE_REQUIRED: no — queue row 12 rides the merge briefs on the operator's cadence (blocking nothing mechanically per master's filing); the operator MERGE-GATE stays terminal
FILED_AT_LOCAL: 20260821-140427
IN_REPLY_TO: master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-135631.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: plan r7 — R9 folded: T9.4 the executor determinism repair (six invariants) before T9.5's GREEN evidence; plan-review requested

## §1 — The exact delta over r6 (complete; nothing else moves)

1. **NEW task T9.4** (sequenced FIRST among the pending commits): the R9 repair per I1–I6 above. Commit order from T9 HEAD: **T9.4 (R9 executor commit) → T9.5 (R8 fixture commit) → the GREEN evidence runs → T10** — both repairs land before any GREEN run so the evidence is produced under the deterministic gate.
2. **The fence table gains one row (R9-granted):** `frank/internal/executor/**` -> in, FOR THE T9.4 COMMIT ONLY (thereafter consume-only again; a later needed touch = fresh escalation).
3. **The GREEN evidence ladder (R9-I5 + R8-C4, verbatim-bound):** (i) a spawn-env DETERMINISM leg — offline resolution proven (`GOPROXY=off` + the local source; a nested run resolves modules with zero network); (ii) a DETAIL-SURFACING leg — a forced inner red's output tail appears in `FailingDetail`, capped, token-prefixed; (iii) T9.5's THREE consecutive nested-suite greens (the isolated repaired fixture); (iv) R8-C4's TWO consecutive full batteries. All cited in the lane fold; the fold also states the I2 mechanism choice + isolation rationale and routes the repair diff FYI to m-3.planner.
4. **Every other r6/r5/r4/r3/r2 byte carries byte-for-meaning** — R8's conditions stand unchanged (600s backstop, C2 discrimination, the one-file fixture grant), the R6 directive law, the §2a matrix, the task boundary (seventeen + the R8 fixture repair + the R9 executor repair, both ruled insertions).

## §2 — Your review ask (this relay's gate)

PLAN-REVIEW verdict on the bounded fold only: (a) T9.4 carries R9's I1–I6 exactly, incl. the I3 STOP condition; (b) the commit order (repairs before evidence); (c) the evidence ladder binding; (d) the one-commit-scoped fence grant; (e) no other r6 byte moved. On APPROVE I issue the successor dispatch; you execute T9.4 → T9.5 → the evidence ladder → T10 → T11–T15.

ACTIONS_GIT_REF: docs-workspace disk action — this successor plan + one s14 INDEX row (in-fence). No source byte by this seat, no commit, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** + the implementer's preserved uncommitted T9.5/T10 bytes on the code worktree; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer files the PLAN-REVIEW verdict on r7 TO s14.planner; on APPROVE the successor dispatch issues and T9.4 executes from T9 HEAD `471532a…`.
