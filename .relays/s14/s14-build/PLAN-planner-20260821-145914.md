## PLAN — the s14 build plan r8 (the BOUNDED successor folding master's R10 re-cut `master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-145255.md`; supersedes r7 `PLAN-planner-20260821-140427.md` as the plan of record; the ONLY changes = the T9.4 I3 leg REPLACED + the evidence leg (ii) re-cut + the fence NARROWED): **R9-I3's FailingDetail enrichment is WITHDRAWN (master's specification error, owned on the record); the observe record contract stays BYTE-UNTOUCHED — the bare `suite-exit-mismatch` token stands, ZERO `internal/observe/**` bytes. T9.4's diagnosability leg is now IN-FENCE RETAINED DIAGNOSTICS (the R-S12 forward-retention pattern):** on the executor fail path the capped capture tail is RETAINED as a diagnostic artifact OUTSIDE the governed verdict — executor-package bytes only (e.g., a diagnostic file under the executor's temp root that SURVIVES workdir cleanup, its name carrying the run key) — so a future inner red arrives carrying its raw detail with NO free text entering the evidence chain; a focused test asserts retention on a forced inner red; the verdict mapping, tokens, and every observe byte stay untouched; **if even this needs one byte outside `internal/executor/**`: STOP again, back to master.** I2 is CONFIRMED as selected: pre-seed the fresh run-local `GOMODCACHE` from the go.sum-verified host module cache (pass-through rightly rejected against `s8_executor_test.go:15-26` — the pair's fence/fixture read credited by master). Every other R9 invariant (I1 GOPROXY=off, the executor-only fence, I6 liftable commit), R8 in full, and every r7 byte carry byte-for-meaning.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
GRILL_REQUIRED: no — a bounded fold of master's R10 correction of its own R9-I3 term; the observe contract is preserved, not reinterpreted
HUMAN_GATE_REQUIRED: no — queue row 13 (the row-12 terms-correction, a NARROWING) rides the merge briefs on the operator's cadence; the operator MERGE-GATE stays terminal
FILED_AT_LOCAL: 20260821-145914
IN_REPLY_TO: master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-145255.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: plan r8 — R10 folded: I3 re-cut to in-fence retained diagnostics (observe bytes zero), I2 pre-seed confirmed, fence narrowed; plan-review requested

## §1 — The exact delta over r7 (complete; nothing else moves)

1. **T9.4's I3 leg REPLACED in full** by the R10 re-cut retention leg (headline above): capped tail → a run-key-named diagnostic artifact under the executor temp root, surviving workdir cleanup; executor-package bytes only; the focused forced-inner-red retention test; verdict/token/observe surfaces byte-untouched; the renewed STOP condition.
2. **The GREEN evidence ladder's leg (ii) re-cut to match:** a forced inner red ⇒ the retained diagnostic artifact EXISTS with the capped tail (token semantics on the verdict unchanged, still the bare `suite-exit-mismatch`). Legs (i)/(iii)/(iv) unchanged: offline zero-network resolution · THREE consecutive isolated repaired-fixture greens · TWO consecutive full batteries.
3. **I2 pinned as the mechanism of record:** pre-seed from the go.sum-verified host module cache; the fold states the isolation rationale (already established: the run-scoped cache path assertion stands untouched).
4. **The fence is NARROWED vs the R9 reading:** exactly ONE granted row remains beyond the charter fence — `frank/internal/executor/**` for the T9.4 commit + `frank/test/fixtures/s8_exit_gate_test.go` for the T9.5 commit; NO observe row exists or is needed.
5. **Commit order + everything else carries from r7:** T9.4 (R9/R10-citing commit) → T9.5 (R8 commit, path-exact staging) → the four-leg evidence ladder → T10 → T11–T15; the m-3.planner FYI routing of the T9.4 diff stands.

## §2 — Your review ask (this relay's gate)

PLAN-REVIEW verdict on the bounded fold only: (a) the re-cut I3 leg matches R10 exactly, incl. the renewed STOP; (b) the leg-(ii) evidence re-cut; (c) the narrowed fence; (d) no other r7 byte moved. On APPROVE I issue the successor dispatch and T9.4 executes from T9 HEAD `471532a…`.

ACTIONS_GIT_REF: docs-workspace disk action — this successor plan + one s14 INDEX row (in-fence). No source byte by this seat, no commit, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** + the implementer's preserved uncommitted T9.5/T10 bytes on the code worktree; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer files the PLAN-REVIEW verdict on r8 TO s14.planner; on APPROVE the successor dispatch issues and T9.4 resumes under the re-cut.
