## PLAN — the s14 build plan r6 (the BOUNDED successor folding master's R8 fixture-capacity ruling `master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-130934.md`; supersedes r5 `PLAN-planner-20260821-035141.md` as the plan of record; the ONLY change = ONE new pre-T10 task under R8's one-file fence grant): **T9.5 — the R8 owner repair of `test/fixtures/s8_exit_gate_test.go`, executed by s14.implementer, exactly R8's five conditions:** (C1) the deadline byte `150*time.Second → 600*time.Second`, exactly (a hang backstop, not a timing assertion; no exit-gate semantic pins 150s); (C2) discrimination — the deadline-exhaustion path names the capacity boundary (ctx.Err + elapsed) and the false-done mismatch Fatalf records the nested suite's observed color + elapsed; NO assertion weakened or removed, every Accepted/Rejected expectation byte-unchanged; (C3) nothing else in the file moves (the `1ac14a0` remedy semantics — nested-scope guard, reachability assert — byte-untouched); (C4) RED = the three recorded failing runs (already on the trail at `061012`); GREEN owed in the fold = the repaired fixture in TWO consecutive full batteries + ONE isolated run, cited; (C5) the repair is its OWN commit, message citing R8, cleanly liftable, committed BEFORE T10 — then T10 commits under the restored gate. Everything else in r5 — T1a–T9 as committed (T9 HEAD `471532a…`), the preserved T10 bytes, T11–T15, §2a, acceptance, rails, the R6 directive law — carries byte-for-meaning. NOTED from R8, no plan action: run 2 is REGRADED not-a-conductor-defect (the nested suite was factually red, the conductor correctly accepted a true claim, the fixture failed loudly); the R-S12 flake tripwire is named and NOT fired (different shape); the domain-owner objection window (m-7/m-3, before the s13 merge brief) and queue row 11 ride master's process, not this pair's.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
GRILL_REQUIRED: no — a bounded fold of master's R8 ruling; the repair conditions are master's bytes, not pair design
HUMAN_GATE_REQUIRED: no — queue row 11 rides the merge briefs on the operator's cadence (blocking nothing mechanically per master's filing); the operator MERGE-GATE stays terminal
FILED_AT_LOCAL: 20260821-131336
IN_REPLY_TO: master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-130934.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: plan r6 — R8 folded: T9.5 the one-file fixture repair (600s + discriminating detail, own commit, before T10) under the granted fence; plan-review requested

## §1 — The exact delta over r5 (complete; nothing else moves)

1. **NEW task T9.5** (sequenced between committed T9 and the preserved T10): the R8 repair per C1–C5 above, on exactly `frank/test/fixtures/s8_exit_gate_test.go` — the ONE out-of-fence file R8's grant licenses, for exactly these bytes; any byte beyond C1+C2 in that file = out of grant = stop and route up.
2. **The fence table gains one row (R8-granted):** `frank/test/fixtures/s8_exit_gate_test.go` -> in, FOR THE T9.5 COMMIT ONLY (thereafter consume-only again; a later needed touch = fresh escalation).
3. **T10's commit condition restated:** T10 commits only after T9.5's GREEN evidence (two consecutive full batteries + one isolated) exists and is cited in the lane fold.
4. **Cross-slice note (R8 §2, informational):** s13/s15 may lift the exact repair-commit bytes on their branches; divergence at restack = arbitration stop (R4a/R6 law unchanged). Nothing for this pair to do beyond keeping the commit cleanly liftable (C5).
5. **Every other r5/r4/r3/r2 byte carries byte-for-meaning**, including the seventeen-task boundary (T9.5 is a ruled insertion, not a scope change — the count reads seventeen + the R8 repair).

## §2 — Your review ask (this relay's gate)

PLAN-REVIEW verdict on the bounded fold only: (a) T9.5 matches R8's C1–C5 exactly; (b) the fence grant is carried as one-commit-scoped; (c) T10's commit condition; (d) no other r5 byte moved. On APPROVE I issue the successor dispatch; you execute T9.5, then commit T10 under the restored gate, then proceed T11–T15.

ACTIONS_GIT_REF: docs-workspace disk action — this successor plan + one s14 INDEX row (in-fence). No source byte by this seat, no commit, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** + the implementer's preserved uncommitted T10 bytes on the code worktree; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer files the PLAN-REVIEW verdict on r6 TO s14.planner; on APPROVE the successor dispatch issues and T9.5 executes from T9 HEAD `471532a…`.
