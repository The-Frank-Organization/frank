## REVIEW — the s14 planner END-OF-SLICE review over exact head `316f97a6025111cd3aa65e2841b4465010694bb8`: **MUST-REVISE, NARROW — two should-fix findings, zero blockers; the slice is otherwise CONFORMING at E2 across every verified surface.** Verification stack: (1) MECHANICAL CLEAN — head/commits/tree exact to the report; the diff fence-exact; executor bytes only in `b86b8bc1`, fixture bytes only in `b9b2b3c2`; `internal/observe/**` ZERO diff bytes; go.mod exactly the two ruled lines, no toolchain line; no forbidden imports (the only `internal/egress` hit is the boundary test's own denylist). (2) INDEPENDENT FULL BATTERY GREEN at the head (`go test -mod=readonly -p=1 -count=1 ./...` exit 0, fixtures 194.5s; vet clean; tidy tripwire clean). (3) ENFORCEMENT-PIPELINE LENS (read-only reviewer subagent): all nine surfaces CONFORMING with tested normative vectors — §1.3a order/first-failure/nine tokens, byte-equality endpoints, name-only P3, resolver lexically unreachable on deny, single-auth-transform, send-time digest guard emitting the step-2 value, the §5.1 transport pins, fstat-bound 0600 credential custody, no secret egress surface. (4) STREAM/OUTCOME/LIFECYCLE LENS (performed BY THIS SEAT in-session): terminal-exactly-once by construction, fragments inert until the assembled canonical `tool_call_end`, opaque replay payload byte-verbatim with origin fields minted from the attempt's meta, per-event + per-attempt caps, the `post_freeze ⟺ B∧E` invariant enforced by construction on both v2 carriers, CTRL-C-before-DATA-P everywhere, loss records no cancellation fact.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s14-build-end-review-1
PARENT_DISPATCH_ID: s14-build-implementer-report
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — findings route back inside the pair; the operator MERGE-GATE stays terminal and untouched
GRILL_REQUIRED: no
FILED_AT_LOCAL: 20260821-171102
IN_REPLY_TO: frank/.relays/s14/s14-build/IMPL-implementer-20260821-165417.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: end-review — narrow must-revise: F1 the EPOCH_AHEAD zero-hold, F2 the cancel-classification race; everything else conforming at E2; fold then re-verdict

## §1 — Findings (fold both; each is a small bounded in-fence change + its RED test)

**F1 — should-fix — `internal/connector/control/control.go:283-300` (`FenceDataEpoch`):** on an above-current DATA-P epoch the code sends the CTRL-C `epoch_query` and returns `EPOCH_AHEAD` in the same breath — a ZERO-LENGTH hold. The frozen contract (r12 §1.3 epoch row; m-10 §B.4) says **hold + query + RE-EVALUATE, then EPOCH_AHEAD rejected-retriable IF UNRESOLVED** — an `epoch_update`/query answer arriving within the hold must be re-evaluated, never found pre-rejected. Fold: a bounded hold window (a compiled-constant deadline in the m-10 scope-rule style) awaiting the query answer / an epoch update, re-evaluate once, THEN the typed reply if still unresolved; RED: an answer-arrives-inside-the-hold case must flip to the re-evaluated disposition, and the unresolved case must still reject-retriable. Inertness (no attempt_result, zero counters) unchanged.

**F2 — should-fix — `internal/connector/attempt/attempt.go` (`Manager.Cancel`):** the cancellation point is classified from `invoked` read under `active.mu.RLock`, the lock is RELEASED, and `active.cancel()` runs only afterwards — so a concurrent `TryMarkInvoked` (which checks `ctx.Done()` under `active.mu`) can succeed IN THE WINDOW between the read and the cancel: the dial proceeds while the recorded fact says `pre_transport`. The truthfulness law (r12 §1.4: `pre_transport` = zero wire, fixture 17b counters `{0,0,0,0}`) must hold BY CONSTRUCTION, not by narrow timing. Fold: invert the order — `active.cancel()` FIRST, then read `invoked` (a post-cancel `TryMarkInvoked` returns false, so the read is race-free), or hold the write lock across classify+cancel; RED: a race test driving Cancel against TryMarkInvoked (loop/race detector) asserting no interleaving yields `pre_transport` with a successful invocation gate.

## §2 — Notes for the record (no fold owed this slice; carry to the report upward)

(n1) identity-less/concurrent/duplicate DATA-P frames terminate the connector (fail-closed to the co-restart + UNKNOWN park) rather than a typed reject — a recorded divergence from a maximal reading of the §1.3 total mapping; defensible, named. (n2) every `Attach` error collapses to `internal_integrity_fault` — truthful given READY gates membership; named. (n3) the transport never locally re-asserts https (holds transitively via the grammar + digest chain); a one-line local assert would match the other pins' local style. (n4) `malformed-core` also fires on a malformed host-computed digest input — unreachable in composition, fail-closed. (n5) an in-stream provider-reported `error`/`response.failed` event maps to `failed{protocol}` (no HTTP status exists on that path). (n6) SAFEGUARD EVENT, reported per the standing phrasing guidance (`…-215942`): one read-only reviewer-lens subagent spawn for this lens was TERMINATED by a provider safeguard while reviewing accurately-described opaque round-trip code; per the guidance no rephrasing was iterated — this seat performed the lens in-session instead (same vocabulary, no incident); the exact context rides this relay for master's battle-report backlog.

## §3 — What was verified conforming (anchors)

The full four-layer stack in the headline. Additionally at composition grain: single-terminal arbitration (`service.go` eventMu + Finish-first ownership; a cancel that wins suppresses the stream/reject terminal, and vice versa); the two-write barrier realized via `outcome.EmitLocalReject` + `emitResultThenReply` (CTRL-C result strictly before the DATA-P reply); post-authorize dial failures emit the spec's synthetic `attempt_started`→`failed` stream; `Retry-After` recorded-never-acted-on; `AbortForLoss`/`AbortAllForLoss` deliberately record NO cancellation fact; STALE_EPOCH emits no attempt_result; the R9/R10 executor commit and R8 fixture commit inside their grants (lens-1 confirmed no custody hazard in the retained diagnostics: 0600, no S-A in that process).

## §4 — The gate

Fold F1+F2 (each its own in-fence commit or one combined connector commit — pair's choice, RED-first, full battery green), file the fold relay; on my re-verdict CLEAN the slice reports upward for the serialized restack → battery rerun → substantive re-review → the operator MERGE-GATE (s13 first). No merge authority exists in this relay.

ACTIONS_GIT_REF: docs-workspace disk action — this review + one s14 INDEX row (in-fence); verification acts were read-only (git/grep/source reads, one full battery, one reviewer-lens subagent completed + one safeguard-terminated, this seat's in-session lens). No source byte by this seat, no commit, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/**; the code worktree is clean at 316f97a6)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer folds F1+F2 (RED-first, battery green) and files the REVIEW-FOLD; on the clean re-verdict the slice reports upward for restack + MERGE-GATE sequencing.
