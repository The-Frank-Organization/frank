## IMPL — THE s14 SUCCESSOR IMPLEMENTATION DISPATCH over approved plan r7 (supersedes `IMPL-planner-20260821-131808.md` as the live dispatch; same delegated authority, its conditions evidenced: the commission grant + the approving `s14-build-plan-review-7` at verdict `approve` over exact r7 SHA-256 `2f1ab1f2627eac98ecaa9c1ef107fb870f52dd75e8d61edd213b96dc397cfdba`): s14.implementer RESUMES from committed T9 HEAD `471532a151762d496186f2daee70b21fdd71062f` against the plan of record `frank/.relays/s14/s14-build/PLAN-planner-20260821-140427.md` (r7). Sequence: **T9.4** — the R9 executor-determinism repair (`internal/executor/**` only, one liftable R9-citing commit, invariants I1–I6: GOPROXY=off; go.sum-verified local module source with the choice + isolation rationale stated in the fold; capped token-prefixed FailingDetail tail with the I3 schema-change STOP; fresh GOCACHE/GOPATH hermeticity kept) → **T9.5** — the distinct R8-citing fixture commit (the diff already present UNSTAGED in the worktree; PATH-EXACT staging, each commit tree verified — the reviewer's non-operative-imprecision note bound as law here) → **the four-part GREEN ladder in order** (offline zero-network resolution leg · forced-inner-red capped-detail leg · THREE consecutive isolated repaired-fixture greens · TWO consecutive full `go test -mod=readonly -p=1 -count=1 ./...` batteries) → **T10 commits under the restored deterministic gate** → T11–T15 straight-through per exact r2 §2/§2a. The fold routes the T9.4 diff FYI to m-3.planner. Stop-and-route-up on any non-executor T9.4 byte, script edit, network-capable resolution, non-verified module source, unbounded detail, changed verdict mapping, schema/observe change, combined repair commit, failed evidence leg, or carried tripwire. Hand-relay transport; every byte and commit YOURS (CORRIGENDUM 1).

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build-plan-review-7
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — implementation only; the operator MERGE-GATE stays terminal in the serialized s13 → s14 → s15 order; queue rows 7+9+11+12 ride the merge briefs
FILED_AT_LOCAL: 20260821-142737
IN_REPLY_TO: frank/.relays/s14/s14-build/PLAN-REVIEW-implementer-20260821-142139.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: the successor implementation dispatch — T9.4 executor repair, T9.5 fixture commit, the four-part GREEN ladder, then T10 and T11–T15; all authority conditions evidenced in lineage

## §1 — The mechanical scope diff (restated as protocol requires; TWO one-commit granted rows)

SCOPE_DIFF (each surface the locked plan touches -> vs the dispatch in-scope list):
- frank/cmd/frank-connector/** -> in (charter fence)
- frank/internal/connector/** (jcs · nfccheck · frame · stream · all connector packages) -> in (charter fence)
- frank/.relays/s14/** -> in (charter fence; relay lane)
- frank/docs/sprints/active/2026-08-20-s14-m8-connector/** -> in (R1 fence addition; may remain explicitly unused)
- frank/go.mod + frank/go.sum (the R4/R6 ruled bytes, committed at T1b) -> in (ruled; the R6 tripwire stands on every later tidy)
- frank/internal/executor/** (source + its tests) -> in FOR THE T9.4 COMMIT ONLY (the R9 grant, I1–I6 exactly; thereafter consume-only again)
- frank/test/fixtures/s8_exit_gate_test.go -> in FOR THE T9.5 COMMIT ONLY (the R8 grant, C1+C2 bytes exactly; thereafter consume-only again)
- everything else in the workspace -> OUT (a needed touch = escalation by construction, never an edit)
SCOPE_DIFF_RESULT: no-deviation

## §2 — Dispatch terms

The prior dispatch §2 terms (`IMPL-planner-20260821-131808.md`) carry verbatim, with r7 §1 governing T9.4, the commit order (repairs before evidence), and the four-part GREEN ladder. Straight-through cadence, RED-first, full battery + vet + `-mod=readonly` green at every commit, the §2a matrix, the ten INV-CATALOG laws + the frozen oracle untouchable, H-12, the escalation rails, and the end-of-slice review sequence all stand.

## §3 — The token

DISPATCH IMPL

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch + one s14 INDEX row (in-fence). No source byte by this seat, no commit by this seat (both repairs, T10, and every later commit are the implementer's), no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** + the implementer's preserved uncommitted T9.5/T10 bytes on the code worktree; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer executes T9.4 → T9.5 → the GREEN ladder → T10 → T11–T15; progress rides the lane per the cadence.
