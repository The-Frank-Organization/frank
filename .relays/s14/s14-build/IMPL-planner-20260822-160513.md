## IMPL — THE s14 RESTACK DISPATCH (master's sequencing word `master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260822-160031.md`, issued on the s13 merge receipt `f8427bd3…`, carried verbatim under the approved plan's own §3 sequence [restack + rerun + re-review before the gate]): s14.implementer RESTACKS `s14-m8-connector` (at `7f91f99b8b8e97e52f1d8115df1e014c5affba9a`) onto THEN-CURRENT `main` at execution (name the exact tip hash in the return twin), reruns the locked gate set at the restacked bytes, and returns the twin; I substantively re-review any changed reviewed byte; master authors the MERGE-GATE brief on the return (rows 4/6/7/8/9/11/12/13/14 + 15 as then-pending). MERGE-GATE stays the operator's; no merge authority exists here.

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build-plan-review-8
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the operator's decision arrives at the s14 brief; nothing merges under this dispatch
FILED_AT_LOCAL: 20260822-160513
IN_REPLY_TO: master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260822-160031.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, s13.planner, s15.planner
SUBJECT: the restack dispatch — onto execution-time main under master's conflict law; rerun the locked gates; return the twin for my re-review and master's brief

## §1 — The mechanical scope diff (the restack act's surfaces, per the word)

SCOPE_DIFF (each surface this act touches -> vs the dispatch in-scope list):
- the `s14-m8-connector` branch history (the rebase/restack operation itself) -> in (the word §1)
- frank/internal/executor/executor.go + executor_test.go + frank/test/fixtures/s8_exit_gate_test.go + s8_executor_test.go -> in, IDENTICAL-BYTE AUTO-RESOLUTION ONLY (the word §2a: main via s13's carriage and your branch carry identical bytes; RE-CITE the four carriage-v4 hashes at the restacked tip; ANY non-identity = the arbitration stop)
- frank/go.mod + frank/go.sum -> in, RESOLVE TO MAIN'S BYTES ONLY (the word §2b: main's post-s13 superset; then prove the union — `go mod verify` + `go mod tidy -diff` SILENT; a non-silent tidy-diff = the arbitration stop, never a local fix)
- frank/.relays/s14/** -> in (the word §2c: main-canonical for banked files + append-only UNION for the INDEX, divergence reported)
- frank/.relays/s13/** + s13's sprint tree -> in AS MAIN-WINS-SILENTLY ONLY (the word §2d: our branch does not touch them)
- frank/docs/sprints/active/2026-08-20-s14-m8-connector/** -> in per the R15 identity-verified class rule (the word §2e: both staged sides hash to banked revisions or STOP)
- ANY other source conflict -> OUT = the arbitration stop, route up unchanged (the word §2f)
SCOPE_DIFF_RESULT: no-deviation

## §2 — The rerun + the return (the word §§3-5, verbatim obligations)

RERUN at the restacked tip: the full ordinary battery (`go test -mod=readonly -p=1 -count=1 ./...`) + `go vet ./...`; the count-100/race suites over the fold surface per our own gate definitions (transport count-100; the executor owner cut + race; control/attempt/service race-20); `go mod verify` + `go mod tidy -diff` silent; the four-hash recheck (`executor.go` `05b529fc…` · `executor_test.go` `858e28cf…` · `s8_executor_test.go` `93f1e94b…` · `s8_exit_gate_test.go` `c339bc0f…`). THE RETURN = one twin TO s14.planner: the restacked tip hash, the main tip it stacked onto, the conflict census (file + resolution + hashes where the class rules required them), the gate evidence, the four-hash citation, and an explicit statement whether ANY reviewed byte changed in the restack (expected: none — say so if so). The appipc-adoption evaluation named at plan r3 §1.4 runs here: EVALUATE and report (adoption itself, if warranted, is a change to reviewed bytes — report first, never silently author). Literal `git status --short` in the twin.

## §3 — The token

DISPATCH IMPL

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch + one s14 INDEX row (in-fence). No source byte by this seat, no branch act by this seat (the restack and every resolution are the implementer's), no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/**; the code worktree is clean at 7f91f99b)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer executes the restack under the conflict law, reruns the locked gates, and returns the twin TO s14.planner; my re-review then master's MERGE-GATE brief follow.
