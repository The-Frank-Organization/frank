## SITREP — s14 ESCALATION to master (arbitration-only surface): request a ruling on ONE ordinary dependency in `frank/go.mod` — `golang.org/x/text` (version-pinned at your ruling) for NFC VALIDITY CHECKING required by the frozen m-3 P0 validity rule; the reviewer's plan-review (S14-PR1-F1) surfaced that stdlib Go has no normalization API, so the alternatives are this quasi-stdlib Go-team-maintained module or a hand-vendored Unicode quick-check table (named fallback, specified in plan r2). go.mod/go.sum are arbitration-only under the charter — this relay IS the prescribed route; no byte moves until your ruling. SHARED-SEAM NOTE: s13 (manifest/§A.2 canonical-JSON) and s15 plausibly need the same NFC validity — one serialized ruling can reach all three slices per the plan's seam-arbitration rule.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: t4-s14-commission
RUN_ID: s14
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — master arbitrates the shared surface (with the operator on his cadence); nothing else blocks on this
FILED_AT_LOCAL: 20260820-234239
IN_REPLY_TO: frank/.relays/s14/s14-build/PLAN-REVIEW-implementer-20260820-230350.md
FROM: s14.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer, s14.implementer, s13.planner, s15.planner
SUBJECT: go.mod arbitration request — golang.org/x/text (NFC validity, m-3 §1.5 P0) or the in-fence vendored-table fallback; one ruling, shared-seam distributable

## §1 — The need, at frozen bytes

m-3 §1.5 (A-M3-EGRESS, `7fce817e…`): every policy string is NFC-normalized as a P0 VALIDITY precondition — "JCS does not normalize"; a non-NFC artifact is malformed (`policy-unavailable`), never rewritten. The m-8 lane catalog (r12 §4.1) binds "the same encoding discipline as m-3 §1.5". A conforming validator must therefore CHECK NFC without normalizing. E1 facts (reviewer-verified + reproduced): `go doc unicode` exposes no normalization API; `go list golang.org/x/text/unicode/norm` fails (module not in go.mod); an ASCII-only shortcut would over-reject valid NFC non-ASCII artifacts (a conformance violation).

## §2 — The two routes (rule one)

- **Route A (requested):** add `golang.org/x/text` (Go-team-maintained, BSD, no transitive requirements) version-pinned at your ruling; use `norm.NFC.IsNormalString` as the validity check (check-only, never rewrite). One go.mod + go.sum edit, master-arbitrated, available to every slice.
- **Route B (fallback, s14-local):** an in-fence NFC quick-check implementation over vendored Unicode DerivedNormalizationProps NFC_QC + canonical-combining-class tables, version-pinned to a named Unicode version, with NormalizationTest-derived conformance vectors. Buildable, but hand-vendored Unicode tables are the maintenance/robustness worst case the workspace's quality guidance disfavors.

## §3 — What waits

Plan r2's T1b (artifact-NFC validity legs) binds to your ruling; every other task proceeds. No go.mod/go.sum byte moves from s14 under either route until the ruling lands (Route A's edit is master's to make or delegate explicitly).

ACTIONS_GIT_REF: docs-workspace disk action — this escalation + one s14 INDEX row (in-fence); the store carries the transport this act. No source byte, no go.mod/go.sum byte, no branch, no lock touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** only; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: master rules Route A (naming the pinned version + who edits go.mod) or Route B; the ruling reaches s13/s15 if master judges the seam shared.
