## IMPL — THE s14 SUCCESSOR IMPLEMENTATION DISPATCH (re-issued over approved plan r4 per the same delegated authority; supersedes the r3-parented dispatch `IMPL-planner-20260821-020810.md` as the live dispatch): s14.implementer RESUMES the build from clean `s14-m8-connector@3257ec35f1b82874c2b0fa382c17d5e71107307e` (T1a stands as committed) against the plan of record `frank/.relays/s14/s14-build/PLAN-planner-20260821-024735.md` (r4, SHA-256 `7440e4d679b5c62a658b4571918a591e6b21f5bb16759965767947207fe3c177`, approved at `s14-build-plan-review-4` verdict `approve`): next act = the ONE r4-shaped T1b commit (`internal/connector/nfccheck` predicate + the ruled `go.mod` bytes + tidy-generated `go.sum`; RED already on the trail, GREEN = tidy-no-op + `-mod=readonly` + vet + full battery on the committed tree), then T1c and T2–T15 straight-through per exact r2 §2/§2a. All r3-dispatch terms carry except the T1b sequencing, which r4 replaces. Hand-relay transport; every source/test byte and commit YOURS (CORRIGENDUM 1).

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build-plan-review-4
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — implementation only; the operator MERGE-GATE stays terminal in the serialized s13 → s14 → s15 order; queue row 7 rides the merge brief
FILED_AT_LOCAL: 20260821-025143
IN_REPLY_TO: frank/.relays/s14/s14-build/PLAN-REVIEW-implementer-20260821-024921.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: the successor implementation dispatch — resume T1b from 3257ec35 under approved plan r4; all authority conditions evidenced in lineage

## §1 — The mechanical scope diff (unchanged from the r3 dispatch; restated as protocol requires)

SCOPE_DIFF (each surface the locked plan touches -> vs the dispatch in-scope list):
- frank/cmd/frank-connector/** -> in (charter fence)
- frank/internal/connector/** (jcs · nfccheck · frame · all connector packages) -> in (charter fence)
- frank/.relays/s14/** -> in (charter fence; relay lane)
- frank/docs/sprints/active/2026-08-20-s14-m8-connector/** -> in (R1 fence addition; may remain explicitly unused)
- frank/go.mod (EXACTLY: `go 1.25` + `require golang.org/x/text v0.41.0`, landing IN the T1b consumer commit per r4) -> in (R4 + R4a ruled bytes, branch-only)
- frank/go.sum (EXACTLY what `go mod tidy` generates, same commit) -> in (R4 ruled)
- everything else in the workspace -> OUT (a needed touch = escalation by construction, never an edit)
SCOPE_DIFF_RESULT: no-deviation

## §2 — Dispatch terms

The r3-dispatch §2 terms (`IMPL-planner-20260821-020810.md`) carry verbatim with one substitution: term 2's first-acts sentence is replaced by r4 §1 (the one-commit T1b shape; no blank import, no retention shim, no waived gate). Straight-through cadence, RED-first, full battery + vet + `-mod=readonly` tests green at every commit, the §2a matrix governing every fixture claim, the ten INV-CATALOG laws + the frozen oracle untouchable, H-12, the escalation rails, and the end-of-slice review sequence all stand unchanged.

## §3 — The token

DISPATCH IMPL

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch + one s14 INDEX row (in-fence). No source byte by this seat, no branch act by this seat (the worktree and every commit are the implementer's), no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** only; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer lands the r4-shaped T1b commit and proceeds T1c → T15 straight-through; progress rides the lane per the cadence.
