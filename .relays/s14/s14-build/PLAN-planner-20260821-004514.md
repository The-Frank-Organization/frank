## PLAN — the s14 m-8 connector build plan r3 (the append-only successor folding the narrow must-revise `s14-build-plan-review-2` finding S14-PR2-F1; supersedes r2 `PLAN-planner-20260820-234459.md` as the plan of record): the master rulings are now FOLDED CLOSED, not conditional — **R4** (`…-234501` R4: ROUTE A ONLY — `golang.org/x/text@v0.41.0` pinned, check-only `norm.NFC.IsNormalString`, s14 edits `go.mod`/`go.sum` ON ITS BRANCH to exactly the ruled bytes; Route B REMOVED from operative text, declined on the standing no-home-grown-security rule; one x/text version cross-slice) + **R4a** (`…-003714`: s14 AUTHORIZED to set the `go 1.25` directive on its own branch to IDENTICAL ruled bytes — an intentional convergent overlap with s13, serialized restack as the reconciliation proof, any DIVERGENT directive at restack = arbitration stop, full-battery-at-every-commit stands) + **R1** (`…-234501` R1: the sprint-doc fence addition `frank/docs/sprints/active/2026-08-20-s14-m8-connector/**` carried — non-relay artifacts live there, never under `frank/.relays/s14/`). **SEVENTEEN tasks** (T1a + T1b + T1c + T2–T15; r2's "16" corrected). Everything else carries from r2 byte-for-meaning (the §2a fixture boundary matrix, the T-breakdown, acceptance, rails). On your APPROVE I issue the build dispatch; you cut the worktree branch `s14-m8-connector` from LAUNCH_BASE `main@b7f406b2` and author every commit; I end-review at completion.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build-plan-review-2
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
GRILL_REQUIRED: no — the successor folds two master rulings + one review finding; every mechanism remains a byte-cited pointer into the frozen spec or a ruled arbitration
HUMAN_GATE_REQUIRED: no — the fence/dep grants ride queue row 7 (PENDING the operator's cadence, blocking nothing mechanically per master's filing); the pair's terminal gate is the operator MERGE-GATE, unchanged
FILED_AT_LOCAL: 20260821-004514
IN_REPLY_TO: frank/.relays/s14/s14-build/PLAN-REVIEW-implementer-20260820-235513.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: plan r3 — R4/R4a/R1 folded closed (exact dependency + directive recipe, RED/GREEN commands, fence path carried), seventeen tasks; plan-review requested

## §0 — Governing bytes (r2 §0 carried unchanged, PLUS the three rulings now closed)

The r2 §0 basis set carries verbatim: A-M8-PROVIDER r12 `4b670a79…` · A-M8-BE r7 `734e44b7…` · A-M3-EGRESS + A-M3-SCHEMAV · A-M10-SEAM · A-M1-SECRET · A-M9-WORKER + A-JB-M9/M3/CARRIAGE · the riding phrasing guidance (opaque round-trip; escalate-never-rephrase). NEW, closed: **R1/R2/R3/R4** = `master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260820-234501.md` (SHA-256 `17ea6b3bd3fc2c6db7c69a15e0a1548988131ed439912a74eaeb0074c7307455`, the reviewer's reproduced hash) and **R4a** = `master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-003714.md`; store carriage `relay-ef90da12989a02626edc5fcd` (intake-000062) + `relay-3d71cd4dacbe25117531daad` (intake-000065).

## §1 — Standing constraints (r2 §1 carried, items 1 and 1a REWRITTEN per the rulings)

1. **Write fence (as ruled):** `frank/cmd/frank-connector/**` + `frank/internal/connector/**` + `frank/.relays/s14/**` + **the R1 sprint-doc addition `frank/docs/sprints/active/2026-08-20-s14-m8-connector/**`** (non-relay working artifacts — design notes, plan working copies — live THERE, never under the relay root; if the build produces none, the path is carried explicitly unused; any move of an existing non-relay artifact out of the relay root cites the recursive `relay-lint --relay-root frank/.relays/s14 --no-freshness` clean in the moving seat's next twin). `appipc`/`seatclient`/conductor tree consume-only; everything else out-of-fence.
   **1a. The ruled dependency + directive edit (R4 + R4a, CLOSED — the exact s14-owned bytes, editable ONLY on the `s14-m8-connector` branch):**
   - `frank/go.mod`: the directive line `go 1.22` → **`go 1.25`** (R4a: identical bytes to s13's ruled edit — an intentional convergent overlap; NOT a collision), plus **`require golang.org/x/text v0.41.0`**.
   - `frank/go.sum`: exactly what `go mod tidy` generates for that require set — no hand edits.
   - **No other go.mod/go.sum line moves.** At restack: identical directive bytes merge cleanly; ANY divergent directive or dependency line = an ARBITRATION STOP through master, never a silent resolve.
   - **RED (pre-edit, cite in the task log):** `go list golang.org/x/text/unicode/norm` fails (module absent); with `go 1.22` + the require, `go mod tidy` rewrites the directive and `go test -mod=readonly ./...` refuses (`go: updates to go.mod needed`) — the reviewer's E1/E2 reproduction.
   - **GREEN (post-edit):** `go mod tidy` is a no-op (clean diff); `go build ./...` + `go test -mod=readonly ./...` green; `go vet ./...` clean; the full existing battery green at the same commit (full-battery-at-every-commit stands on the branch).
   - The cross-slice one-version rule rides: any slice needing NFC validity uses x/text at THIS v0.41.0 — no second version, no vendored fallback anywhere.
2. **No conductor byte moves.** The ten INV-CATALOG laws + the full existing battery green at every commit; byte-exact `{accepted, rejected, held}` untouched; the frozen oracle + RLBS-1 + `master/exit-fixtures/**` untouchable.
3. **m8.* types app-internal** (r12 §1.1): no conductor-payload serializer may exist (fixture 13a); never import `internal/egress`; never invoke the model-name classifier.
4. **Framing seam note (named restack item, carried):** the §A.2 frame codec in-fence under `internal/connector/frame`; at restack evaluate adopting `appipc` consume-only; any needed `appipc` change or post-restack shared-seam change is escalated, never silently authored.
5. **H-12 stands**; bash-originated egress is ungoverned and out of scope (r12 §0.4).
6. **Compiled constants** (tunable values, unremovable names): `FRAME_MAX`, `SEND_DEADLINE`, queue depths, `HEALTH_INTERVAL`, `HANDSHAKE_DEADLINE`, `TTFB_DEADLINE`, `STALL_DEADLINE`, `ATTEMPT_STREAM_MAX`.
7. **Straight-through cadence:** task-by-task, no per-task review relays; RED-first negatives per task; ONE end-of-slice adversarial review (yours) then my end-review; literal `git status --short` in every final report.

## §2 — Task breakdown (SEVENTEEN tasks: T1a/T1b/T1c + T2–T15; r2's text carried byte-for-meaning except T1b, which is now ruling-closed)

- **T1a — module skeleton + PURE RFC 8785 JCS** (`internal/connector/jcs`): canonical numbers, escaping, unique object keys, member ordering, strings PRESERVED byte-for-byte — no normalization, no rewriting, no global NFC rejection. RED: non-canonical inputs, duplicate keys; GREEN: the frozen byte vectors (the 255-byte m-3 example among them).
- **T1b — artifact-NFC VALIDITY (RULING-CLOSED, R4/R4a):** FIRST ACT of this task = the §1.1a go.mod/go.sum/directive edit with its RED/GREEN commands, as its own commit. Then the check-only predicate via `golang.org/x/text/unicode/norm` — `norm.NFC.IsNormalString`, applied AT exactly the schema loci whose frozen contracts require NFC (the m-3 §1.5 policy validator [T3]; the r12 §4 catalog validator [T2]); rejects non-NFC (⇒ that artifact's malformed token), never rewrites accepted bytes, never runs on surfaces whose schemas do not require it. RED: a non-NFC policy string ⇒ `policy-unavailable`; a non-NFC catalog string ⇒ READY refused; a valid NFC non-ASCII artifact ACCEPTED (the over-rejection negative).
- **T1c — frame codec** (`internal/connector/frame`, §A.2/§A.3): uint32_be length prefix, `FRAME_MAX`, the `{v, chan, type, seq, re, run_id, turn_epoch, body}` envelope, counter-string grammar `^(0|[1-9][0-9]*)$` < 2^64 with numeric compare, unknown-type ⇒ reject-and-record, unknown-field-ignore on CTRL-C types, per-sender monotonic seq, bounded send queue + blocking write + `SEND_DEADLINE` fault. RED: oversized frame, malformed counter, unknown type.
- **T2–T15 — carried from r2 byte-for-meaning** (`PLAN-planner-20260820-234459.md` §2, immutable predecessor): T2 lane catalog (NFC via T1b) · T3 policy load (NFC via T1b; the 255-byte example ⇒ `ca364710…`) · T4 authorize evaluator (§1.3a total order) · T5 `LLMRequest` intake · T6 translate + E digest · T7 freeze + B digest + census · T8 attach + S-A custody (resolution lexically unreachable on deny) · T9 send + the pinned no-replay transport + instrumented counters · T10 SSE + `m8.provider_event.v2` (opaque round-trip) · T11 CTRL-C lifecycle + epoch authority · T12 outcome reporting (six dispositions, `refusal_stage`, the two-write barrier) · T13 cancellation · T14 `cmd/frank-connector` main · T15 battery close (13a, 14, the fake-provider E2E, the matrix sweep).

## §2a — THE FIXTURE BOUNDARY MATRIX — carried from r2 UNCHANGED (`PLAN-planner-20260820-234459.md` §2a; the reviewer confirmed F2 closes on it). Acceptance binds to it: LOCAL legs production-realized E2; PRODUCER legs green and labeled; RESTACK/INTEG named to the concurrent-branch rerun + s16; SIBLING legs never claimed. r12's "no local fixture claims a cross-lane row" preserved verbatim.

## §3 — Acceptance (r2 §3 carried, plus the ruling legs)

Every §2a LOCAL leg production-realized E2 with its RED leg driven first + every PRODUCER leg green and LABELED, per-leg, citing the matrix; the §1.1a dependency/directive commit with its RED/GREEN command outputs cited; `go vet` + full `go test ./...` green at every commit; zero out-of-fence bytes (the fence now includes the R1 sprint-doc path — used or explicitly unused); the named seam items (framing-vs-appipc; the convergent go-directive overlap + restack proof) restated in the final report; your ONE end-of-slice adversarial review, then my end-review, then restack + rerun + re-review + the operator MERGE-GATE (queue row 7's operator disposition rides the merge brief).

## §4 — Escalation rails (unchanged from r2)

Spec-base defect / better way / locked-contract touch / fence deviation / cross-slice collision / amendment need ⇒ UP through master; serialized seam arbitration for live-sibling seams (any divergent shared-line byte at restack = arbitration stop); the gates-cannot-check queue is LAW; safeguard-fires-on-accurate-phrasing ⇒ STOP and report up.

## §5 — Your review ask (this relay's gate)

PLAN-REVIEW verdict on the S14-PR2-F1 fold: (a) R4/R4a closed-not-conditional — the exact §1.1a byte recipe + RED/GREEN commands, Route B absent from every operative sentence; (b) the R1 fence path carried with the non-relay-artifact rule (explicitly-unused permitted); (c) the seventeen-task count; (d) that T2–T15 + §2a carry from r2 without semantic drift. On APPROVE I issue the literal direct implementation dispatch; on must-revise I fold and re-route.

ACTIONS_GIT_REF: docs-workspace disk action — this successor plan + one s14 INDEX row. No source byte, no go.mod/go.sum byte (the §1.1a edit is the IMPLEMENTER's first T1b commit on the branch, post-dispatch), no branch, no lock, no shared path touched by this relay.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** only; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer files the PLAN-REVIEW verdict on r3 TO s14.planner; on APPROVE the build dispatch issues and the worktree branch opens.
