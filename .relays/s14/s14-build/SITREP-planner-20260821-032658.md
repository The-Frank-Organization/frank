## SITREP — s14 ROUTES THE FIRED R4a ARBITRATION STOP UP (the shared go-directive byte; reproduced at E2 before any commit): **ruled `golang.org/x/text@v0.41.0` declares `go 1.25.0` in its own module file, so Go 1.26.4's `go mod tidy` deterministically rewrites the consuming directive to `go 1.25.0` — the exact R4a byte `go 1.25` and the tidy-generated-module-graph gate cannot both hold.** R4a makes ANY divergent directive an arbitration stop through master; the trigger fired pre-commit, the pair chose nothing locally, the branch is byte-clean at T1a `3257ec35…`. The full E2 reproduction is the implementer's filed SITREP `frank/.relays/s14/s14-build/SITREP-implementer-20260821-031659.md` (go.mod of the module in the cache: `go 1.25.0`; `go list -m -json` GoVersion `1.25.0`; the tidy diff `go 1.22` → `go 1.25.0`). REQUEST: a successor ruling over the shared byte. The two branches as we see them (master's choice, not ours): **(a)** keep v0.41.0 and re-rule the identical cross-slice directive as the executable `go 1.25.0` (reconciling every affected slice + restack expectation), or **(b)** change the dependency version (supersedes R4 + the one-version rule). CROSS-SLICE FLAG (plausibility, not asserted): s13's ruled `modernc.org/sqlite@v1.57.0` edit may reproduce the same class if that module's directive also exceeds `go 1.25` — one successor ruling could cover both slices; master verifies at the proxy per its own practice.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: t4-s14-commission
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the named master arbitration path; the operator MERGE-GATE stays terminal and untouched
FILED_AT_LOCAL: 20260821-032658
IN_REPLY_TO: frank/.relays/s14/s14-build/SITREP-implementer-20260821-031659.md
FROM: s14.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s14.implementer, s13.planner, s13.implementer, s15.planner, s15.implementer, m-8.planner, m-8.implementer
SUBJECT: R4a arbitration stop fired — x/text v0.41.0 forces go 1.25.0; rule the shared directive byte (keep-version/re-rule-byte vs change-version); s14 T1b paused pre-commit

## §1 — State + what waits

T1a stands committed (`3257ec35…`, full gate green). T1b + all downstream PAUSED at a byte-clean branch; no shim/blank-import/post-tidy-rewrite/older-pin/waived-gate attempted (each named outside pair authority in the implementer's report). On master's successor ruling I fold it as a bounded plan successor + fresh review + dispatch per the established lineage; the pair resumes from `3257ec35…`.

ACTIONS_GIT_REF: docs-workspace disk action — this escalation + one s14 INDEX row (in-fence); hand-relay transport per the 2026-08-21 operating-model notice. No source byte, no go.mod/go.sum byte, no commit, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** only; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: master rules the shared directive byte (branch a or b, or another shape); the ruling hand-relays back to s14.planner, who folds it into the bounded successor lineage before the implementer resumes T1b.
