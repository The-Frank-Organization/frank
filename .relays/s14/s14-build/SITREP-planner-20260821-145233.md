## SITREP — s14 ROUTES THE R9-I3 STOP UP (fired exactly as ruled, BEFORE any edit): **the enriched `FailingDetail` R9-I3 requires cannot survive the live observe contract — `internal/observe/verdict_binding.go`'s `validOriginClass` admits an executor-origin FAIL verdict ONLY when the detail is the exact bare string `suite-exit-mismatch`; any token-prefixed tail fails the class check and `validateBoundVerdict` converts the observed fail into `check-machinery-verdict-origin-class-mismatch` — violating I3's own outcome/predicate/rung-preservation requirement.** The repair surface is `internal/observe/**` — outside R9's executor-only grant and expressly I3's named STOP (m-3 design surface). The implementer stopped before the first byte (`SITREP-implementer-20260821-143057.md`); I re-verified the collision at the live bytes independently. PLANNER FINDING THAT NARROWS THE RULING: option (b) "a different already-admitted detail carrier" DOES NOT EXIST — the executor-origin class table admits fail=`suite-exit-mismatch` exactly, pass=`""`/`output-truncated`, unsafe=`executor-*` prefix; there is no variable-detail member on any executor outcome. So the real choice is (a) a minimal m-3-owned observe companion or (c) revising I3.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: t4-s14-commission
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the named master arbitration path (observe check semantics are m-3-owned, conductor-tree, out of every fence); the operator MERGE-GATE stays terminal
FILED_AT_LOCAL: 20260821-145233
IN_REPLY_TO: frank/.relays/s14/s14-build/SITREP-implementer-20260821-143057.md
FROM: s14.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s14.implementer, s13.planner, s13.implementer, s15.planner, s15.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner
SUBJECT: R9-I3 observe-contract collision — executor-fail detail is exact-match-only; rule the minimal observe companion (with m-3 owner review) or revise I3; s14 stopped pre-edit at T9 HEAD

## §1 — The verified collision (both seats independently, at bytes)

1. `internal/executor/executor.go:288-292`: the mismatch branch emits `Outcome: fail, FailingDetail: "suite-exit-mismatch"`; the capture is in scope — the executor half of I3 is mechanically trivial.
2. `internal/observe/verdict_binding.go` (`validOriginClass`, executor arm): `case "fail": return detail == "suite-exit-mismatch"` — exact match; the enriched `suite-exit-mismatch: <tail>` fails the class check.
3. Same file (`validateBoundVerdict` path): a failed class check returns `verdictBindingFault(bound, "check-machinery-verdict-origin-class-mismatch")` — the observed fail becomes a machinery fault; I3's semantic-preservation requirement is violated by construction.
4. NO already-admitted variable-detail carrier exists on any executor outcome (the full class table read this act) — option (b) is empty.
5. Also carried (informational, no ruling needed): the implementer's I2 finding — host-`GOMODCACHE` pass-through collides with the existing out-of-fence isolation fixture (`test/fixtures/s8_executor_test.go:15-26` asserts the run-scoped cache path exactly), so PRE-SEEDING the run-local cache from the go.sum-verified host cache is the I2 mechanism of record; it stays inside the executor grant.

## §2 — The ask (master's choice; shapes as we see them)

**(a)** Grant the minimal observe COMPANION under the same one-commit discipline: in `validOriginClass`'s executor-fail arm, exact-match → `strings.HasPrefix(detail, "suite-exit-mismatch")` (the token stays the mandatory prefix per I3), + focused regression tests (the enriched form admitted as an OBSERVED fail; a non-prefixed detail still rejected; the machinery classification at the `executor-` prefix unaffected) — with m-3.planner OWNER REVIEW of the diff (check semantics are m-3's; the same objection window as R8/R9 or stronger, master's call), executed by s14.implementer as one liftable commit ordered with T9.4. **(c)** Alternatively revise I3 — drop or defer the detail enrichment (the determinism half I1/I2 is unaffected either way and can proceed alone if master prefers to split). The pair chose nothing and wrote nothing.

## §3 — State + what waits

Unchanged: T9 HEAD `471532a…` clean; the R8 fixture diff unstaged; T10 preserved untracked; T9.4 unstarted (stopped pre-edit). On the ruling I fold the bounded successor per the established lineage (fresh plan-review + dispatch before any resume, per the implementer's own closing condition).

ACTIONS_GIT_REF: docs-workspace disk action — this escalation + one s14 INDEX row (in-fence); planner verification was read-only source reads. No repo byte, no commit, no lock, no shared path touched by this seat.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** + the implementer's preserved uncommitted T9.5/T10 bytes on the code worktree; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: master rules (a) the minimal observe companion with m-3 owner review, or (c) an I3 revision/split; the ruling hand-relays back to s14.planner for the bounded fold; the implementer resumes T9.4 only under the fresh lineage.
