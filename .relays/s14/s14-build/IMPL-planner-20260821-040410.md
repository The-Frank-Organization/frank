## IMPL — THE s14 SUCCESSOR IMPLEMENTATION DISPATCH over approved plan r5 (supersedes `IMPL-planner-20260821-025143.md` as the live dispatch; same delegated authority, its conditions evidenced: the commission grant + the approving `s14-build-plan-review-5` at verdict `approve` over exact r5 SHA-256 `6eeb92c473104a4593154dc3f4653154bb2407b934e22df3fb3b1a1a007e5e1e`): s14.implementer RESUMES from clean `s14-m8-connector@3257ec35f1b82874c2b0fa382c17d5e71107307e` against the plan of record `frank/.relays/s14/s14-build/PLAN-planner-20260821-035141.md` (r5). Next act = the ONE T1b commit: `internal/connector/nfccheck` (check-only `norm.NFC.IsNormalString`, RED-first) + the R6-ruled `go.mod` bytes (directive `go 1.25.0` + `require golang.org/x/text v0.41.0`) + `go.sum` exactly as `go mod tidy` generates. GREEN on the committed tree: tidy no-op · NO `toolchain` line · exact `go 1.25.0` · `go build ./...` · `go test -mod=readonly ./...` · `go vet ./...` · the full battery. Then T1c → T2–T15 straight-through per exact r2 §2/§2a. The R6 §1(iv) tripwire is standing law: any tidy divergence = STOP before commit, route the exact bytes UP. All prior dispatch terms carry except where r5 supersedes. Hand-relay transport; every source/test byte and commit YOURS (CORRIGENDUM 1).

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build-plan-review-5
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — implementation only; the operator MERGE-GATE stays terminal in the serialized s13 → s14 → s15 order; queue rows 7+9 ride the merge brief
FILED_AT_LOCAL: 20260821-040410
IN_REPLY_TO: frank/.relays/s14/s14-build/PLAN-REVIEW-implementer-20260821-040009.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: the successor implementation dispatch — resume T1b from 3257ec35 under approved plan r5 (R6 byte go 1.25.0); all authority conditions evidenced in lineage

## §1 — The mechanical scope diff (restated as protocol requires; the R6 byte updated)

SCOPE_DIFF (each surface the locked plan touches -> vs the dispatch in-scope list):
- frank/cmd/frank-connector/** -> in (charter fence)
- frank/internal/connector/** (jcs · nfccheck · frame · all connector packages) -> in (charter fence)
- frank/.relays/s14/** -> in (charter fence; relay lane)
- frank/docs/sprints/active/2026-08-20-s14-m8-connector/** -> in (R1 fence addition; may remain explicitly unused)
- frank/go.mod (EXACTLY: the directive `go 1.25.0` + `require golang.org/x/text v0.41.0`, landing IN the T1b consumer commit) -> in (R4 + R6 ruled bytes, branch-only)
- frank/go.sum (EXACTLY what `go mod tidy` generates, same commit) -> in (R4/R6 ruled)
- everything else in the workspace -> OUT (a needed touch = escalation by construction, never an edit)
SCOPE_DIFF_RESULT: no-deviation

## §2 — Dispatch terms

The prior dispatch §2 terms (`IMPL-planner-20260821-025143.md`) carry verbatim, with the r5 §1 delta governing the T1b bytes and the R6 tripwire added as standing law. Straight-through cadence, RED-first, full battery + vet + `-mod=readonly` green at every commit, the §2a matrix governing every fixture claim, the ten INV-CATALOG laws + the frozen oracle untouchable, H-12, the escalation rails, and the end-of-slice review sequence all stand.

## §3 — The token

DISPATCH IMPL

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch + one s14 INDEX row (in-fence). No source byte by this seat, no branch act by this seat (the worktree and every commit are the implementer's), no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** only; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer lands the r5-shaped T1b commit and proceeds T1c → T15 straight-through; progress rides the lane per the cadence.
