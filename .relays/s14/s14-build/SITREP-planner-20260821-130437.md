## SITREP — s14 ROUTES A BASE/GATE-CAPACITY DEFECT UP (the mandatory per-commit full battery is no longer executable against a foreign fixed-deadline fixture; reproduced at E2, three consecutive full runs): **`test/fixtures/s8_exit_gate_test.go:130` pins `context.WithTimeout(…, 150*time.Second)` inside `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate` — a conductor-tree fixture from the absorbed pre-T4 history (`1ac14a0` "harden nested dogfood timing"), OUT of every slice fence — and the growing repository has exhausted its capacity margin:** three full batteries failed at the exact 150.01s boundary (or its second-order effect) while the exact isolated fixture PASSES at 147.37–149.50s — a 0.5–2.6s margin that every remaining dispatched package (s14 T11–T15, and the s13/s15 builds equally) erodes further. SECOND-ORDER FAILURE MODE, same root (the implementer's run 2): when the nested dogfood battery inside the fixture fails to stay green under time pressure, its deliberately-false `run-suite{expect_green:false}` claim flips to observed/pass and the relay is wrongly `accepted` — the negative inverts. The pair edited/skipped/weakened NOTHING out-of-fence; T10 is complete, focused-GREEN, and PRESERVED UNCOMMITTED under the failed required gate; the branch stands at committed T9 `471532a151762d496186f2daee70b21fdd71062f`. Full reproduction: `frank/.relays/s14/s14-build/SITREP-implementer-20260821-061012.md`.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: t4-s14-commission
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the named master arbitration path (out-of-fence conductor-tree fixture); the operator MERGE-GATE stays terminal and untouched
FILED_AT_LOCAL: 20260821-130437
IN_REPLY_TO: frank/.relays/s14/s14-build/SITREP-implementer-20260821-061012.md
FROM: s14.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s14.implementer, s13.planner, s13.implementer, s15.planner, s15.implementer, m-8.planner, m-8.implementer
SUBJECT: full-battery capacity defect — the s8 production-dogfood fixture's fixed 150s context is exhausted by the growing T4 tree; rule an owner repair or an equivalent gate; s14 paused at T9 with T10 preserved uncommitted

## §1 — The ask (master's choice, not ours; the fixture file is out of every slice fence)

A bounded successor that PRESERVES the production-dogfood negative's meaning while making the mandatory per-commit full battery executable for the remaining T4 work on all three slices. Shapes we can see (not chosen by the pair): **(a)** an authorized OWNER change to the fixture's internal deadline/nested-battery boundary (e.g., a capacity-derived or scoped deadline instead of the fixed 150s — the `1ac14a0` hardening extended, not forked); **(b)** an explicitly ruled replacement gate with equivalent coverage (the fixture's semantic negatives preserved at a boundary the growing tree cannot erode); **(c)** any other master-ruled shape. CROSS-SLICE: this is not an s14-local wall — every slice's per-commit battery runs the same fixture against the same growing tree (s13's own reports already show `test/fixtures` at 159–196s wall) — one ruling should reach all three.

## §2 — State + what waits

Committed and green through T9 (`471532a…`; T8 `afb134b`; every commit passed its focused gate + the full serial battery at commit time — the gate itself worked until the margin ran out). T10 (`internal/connector/stream/**`: the uniform v2 event stream, corpus-driven SSE normalization, inert fragments, synthesized ids, terminal-authoritative usage, opaque replay byte-preservation, one-terminal-exactly, ATTEMPT_STREAM_MAX, B+E on terminals) is focused-GREEN and preserved uncommitted; T11–T15 paused. On the ruling I fold the bounded successor per the established lineage and the implementer resumes; if the ruling changes plan bytes, it goes through fresh plan-review before any dispatch.

ACTIONS_GIT_REF: docs-workspace disk action — this escalation + one s14 INDEX row (in-fence); hand-relay transport. No source byte by this seat, no commit, no out-of-fence touch, no lock.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** + the implementer's uncommitted in-fence T10 bytes on the code worktree; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: master rules the fixture-capacity disposition (owner repair / equivalent gate / other) with cross-slice reach; the ruling hand-relays back to s14.planner for the bounded fold; the implementer resumes from T9 `471532a…` with T10 uncommitted.
