## PLAN — the s14 build plan r5 (the BOUNDED successor folding master's R6 ruling `master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-033642.md` over the fired R4a arbitration stop; supersedes r4 `PLAN-planner-20260821-024735.md` as the plan of record; the ONLY change is the ruled directive spelling): **the T1b commit's `go.mod` directive byte is `go 1.25.0`** (R6 §1: the executable byte, identical on every consuming branch — s13 and s14; x/text v0.41.0 AND modernc.org/sqlite v1.57.0 both declare `go 1.25.0`, master's own proxy + clean-room tidy reproduction of record). `golang.org/x/text v0.41.0` stands unchanged (branch (b) declined). NEW TRIPWIRE RESTATED FROM R6 §1(iv): if any future `go mod tidy` emits a `toolchain` line or ANY directive byte other than `go 1.25.0`, that is a NEW divergent byte = the same arbitration stop, fire-before-commit. Everything else in r4 — the one-commit T1b shape (nfccheck consumer + ruled go.mod bytes + tidy-generated go.sum), T1a as committed at `3257ec35…`, T1c, T2–T15 + §2a per exact r2, acceptance, rails, the seventeen tasks — carries byte-for-meaning.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
GRILL_REQUIRED: no — a one-byte-class fold of master's R6 successor ruling; no mechanism, version, fence, or fixture boundary moves
HUMAN_GATE_REQUIRED: no — R6 rides queue row 9 for the operator's cadence (blocking nothing mechanically per master's filing); the operator MERGE-GATE stays terminal
FILED_AT_LOCAL: 20260821-035141
IN_REPLY_TO: master/relays/step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-033642.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: plan r5 — R6 folded: the T1b directive byte is go 1.25.0 (versions stand, no toolchain line, divergence tripwire restated); plan-review requested

## §1 — The exact delta over r4 (complete; nothing else moves)

1. **r4 §1.1(b) ruled `go.mod` bytes now read:** the directive `go 1.22` → **`go 1.25.0`** + `require golang.org/x/text v0.41.0` (R6 §1(ii)). `go.sum` stays exactly as `go mod tidy` generates (R6 §1(iii)).
2. **GREEN unchanged in shape, now executable:** on the committed T1b tree — `go mod tidy` is a no-op (clean diff) · `go build ./...` · `go test -mod=readonly ./...` · `go vet ./...` · the full battery. The implementer's two E2 reproductions (the tidy-prunes and the 1.25.0-rewrite evidence) stand as the RED trail.
3. **The R6 §1(iv) tripwire carried as standing law:** any tidy-emitted `toolchain` line or any directive byte ≠ `go 1.25.0` = arbitration stop before commit, routed UP unchanged.
4. **Every other r4/r3/r2 byte carries byte-for-meaning** — including the one-NFC-source rule, the one-commit T1b shape, the restack join (identical bytes merge clean; divergence = stop), and the seventeen-task boundary.

## §2 — Your review ask (this relay's gate)

PLAN-REVIEW verdict on the bounded fold only: (a) the directive byte matches R6 exactly; (b) the tripwire is carried; (c) no other r4 byte moved. On APPROVE I issue the successor dispatch and you resume T1b from clean `3257ec35…`.

ACTIONS_GIT_REF: docs-workspace disk action — this successor plan + one s14 INDEX row (in-fence). No source byte, no go.mod/go.sum byte by this seat, no branch act, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** only; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer files the PLAN-REVIEW verdict on r5 TO s14.planner; on APPROVE the successor dispatch issues and T1b resumes from `3257ec35…`.
