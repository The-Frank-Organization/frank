## PLAN — the s14 m-8 connector build plan (the pair-local locked plan, routed to s14.implementer for plan-review per charter CORRIGENDUM 1): build `frank/cmd/frank-connector` + `frank/internal/connector/**` to the FROZEN m-8 spec-of-record (provider contract r12 `4b670a79…` + B/E addendum r7 `734e44b7…`) and its consumed bases, 15 tasks straight-through, RED-first negatives, stdlib-only (go.mod UNTOUCHED — any dependency need is an arbitration escalation, never an edit). On your APPROVE verdict I issue the build dispatch; you cut the worktree branch `s14-m8-connector` from LAUNCH_BASE `main@b7f406b2` and author every commit; I end-review at completion.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: t4-s14-commission
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
GRILL_REQUIRED: no — the plan executes frozen bases; it designs nothing new (every mechanism below is a byte-cited pointer into the frozen spec)
HUMAN_GATE_REQUIRED: no — the pair's terminal gate (the operator MERGE-GATE, serialized s13 → s14 → s15) is unchanged and not requested here
FILED_AT_LOCAL: 20260820-223912
IN_REPLY_TO: master/relays/t4-s14-commission/PLAN-orchestrator-planner-20260820-174746.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: the s14 build plan — 15 tasks over the frozen m-8 bases; plan-review requested; the build dispatch issues on your APPROVE

## §0 — Governing bytes (verified this session, zero drift; read them, never this plan, when in doubt)

- **A-M8-PROVIDER** r12 @ `4b670a79…` — THE spec: `LLMRequest`/events (§1), `freeze→authorize→attach→send` (§2), S-A custody (§3), lane catalog (§4), owned client (§5), E0 feed (§6), F63 grain (§7), fixtures 1–17b (§8).
- **A-M8-BE** r7 @ `734e44b7…` — the v2 carriers (`m8.provider_event.v2` / `m8.dataP_reply.v2` / `m8.attempt_result.v2`), P1 presence by the freeze boundary, `refusal_stage` discriminator, E recipe (lowered-tools digest), fixtures §6.1–11.
- **A-M3-EGRESS** + **A-M3-SCHEMAV** — the P0–PS–P1–P2–P3–P4 authorize predicates in the §1.3a total order (nine-token closed deny enum), §1.5 policy bytes/digest (incl. the 255-byte normative fixture, `policy_digest = ca364710…`), §1.6 endpoint grammar + normative vectors, E0 v2 census (ids/digests/enums/timestamps ONLY).
- **A-M10-SEAM** — §A.2 framing (uint32_be length + canonical-JSON, `FRAME_MAX` 4 MiB, counters as canonical-decimal STRINGS), §A.3 bounded/blocking/fault-on-deadline queues, §B.1 `hello`→`connector_assign`(7 fields)→verify→`connector_ready` + READY-gate ordering, §B.4 generation-blind epoch authority (m-10-sourced only), CTRL-EOF fail-closed, `attempt_result` six-disposition closed enum, co-restart (no solo connector restart).
- **A-M1-SECRET** — S-A custody: one 0600 m-8-private credential file, opaque `credential_ref` (1.4a: secret-inert + authority-inert), resolution ONLY inside the post-authorize attach, hardening rows 2/3/5/6.
- **A-M9-WORKER** (consumed seam) + **A-JB-M9/M3/CARRIAGE** — B-carriage: m-8 computes `frozen_core_digest` at freeze, emits the STEP-2 authorized value (never the step-5 recomputation), downstream copies verbatim; pre-freeze rejects carry NO digest (absence is the signal — never a placeholder); loss cuts owe NO positive non-emission record (documented MVP limit).
- Riding law: the phrasing guidance (`step3-t4-preflight/RECONCILE-…-215942`) — schema vocabulary throughout; provider `reasoning` output items are OPAQUE BYTES round-tripped byte-preserved, never parsed/decoded/rendered/logged; test/symbol names in schema vocabulary (`opaque_item_passthrough`, `response_item_roundtrip`), and if any task ever seems to need the inside of an item, STOP and escalate (it would contradict the opacity rule).

## §1 — Standing constraints (all tasks)

1. **Write fence:** `frank/cmd/frank-connector/**` + `frank/internal/connector/**` + `frank/.relays/s14/**` ONLY. `appipc`/`seatclient`/conductor tree consume-only; `go.mod`/`go.sum` arbitration-only — **plan pin: stdlib-only, zero new dependencies, go.mod byte-untouched** (JCS implemented in-fence; toolchain go1.26.4 matches the §5.1 pin citations).
2. **No conductor byte moves.** The ten INV-CATALOG laws + the full existing battery stay green at every commit; byte-exact `{accepted, rejected, held}` untouched; the frozen oracle + RLBS-1 + `master/exit-fixtures/**` untouchable.
3. **m8.* types app-internal** (r12 §1.1): no conductor-payload serializer for them may exist (fixture 13a); the connector never imports `internal/egress` (m-3 §1.1 non-inheritance) and never invokes the model-name classifier.
4. **Framing seam note (named restack item):** the §A.2 frame codec is implemented IN-FENCE under `internal/connector/frame` byte-conformant to the m-10 contract. s13 owns the shared `appipc` types package (not present at LAUNCH_BASE). At restack (s13 merges first), evaluate adopting `appipc` as a consume-only import; any needed `appipc` change = escalation to master (serialized seam arbitration). Do NOT define types outside the fence.
5. **H-12 stands**; bash-originated egress is ungoverned and out of scope (r12 §0.4); no sentence in code/docs/tests claims otherwise.
6. **Compiled constants** (m-10 scope rule — tunable values, unremovable names): `FRAME_MAX`, `SEND_DEADLINE`, queue depths, `HEALTH_INTERVAL`, `HANDSHAKE_DEADLINE`, `TTFB_DEADLINE`, `STALL_DEADLINE`, `ATTEMPT_STREAM_MAX`.
7. **Straight-through cadence:** task-by-task, no per-task review relays; RED-first negatives per task; ONE end-of-slice adversarial review (yours) then my end-review; literal `git status --short` in every final report.

## §2 — Task breakdown (build order; each task = code + its RED-first fixture legs)

- **T1 — module skeleton + JCS + frame codec.** `internal/connector/jcs` (RFC 8785: NFC strings, sorted unique keys, canonical numbers) with the m-3 §1.5 and r12 §4 encoding vectors; `internal/connector/frame` (§A.2: uint32_be length prefix, `FRAME_MAX`, `{v, chan, type, seq, re, run_id, turn_epoch, body}` envelope, counter-string grammar `^(0|[1-9][0-9]*)$` < 2^64, numeric compare, unknown-type ⇒ reject-and-record, unknown-field-ignore on CTRL-C types, per-sender monotonic seq, bounded send queue + blocking write + `SEND_DEADLINE` fault). RED: oversized frame, malformed counter, unknown type, non-canonical JSON.
- **T2 — lane catalog** (`m8.lane_catalog.v1`, r12 §4): types, load, validator A (schema-shape) + B (semantic: sorted/unique by `lane_id`, 4-axis uniqueness, id grammar, §1.6 endpoint validity, lowercase auth header + `denied_header_names` membership cross-check, closed method enum, RFC3339 fields), JCS digest. **Opacity rule:** no code path branches on `lane_id`/`serving_profile_id` bytes (structural test over the authorize/translate call graph). RED: fixture 8's full negative set ⇒ READY refused. Seeded one-row MVP content is CONFIG (the seed supplies the row; concrete values never hardcoded).
- **T3 — egress policy load** (m-3 §1.5): P0 validity (exact-JCS-stored-bytes check — no re-serialization; NFC; closed schema; `method_by_lane` present ⇒ malformed; sorted duplicate-free sets; lowercase `denied_header_names`; §1.6-valid allowlist entries), digest vs `connector_assign.policy_digest` BEFORE any authorize decision. RED: fixture 9 + the m-3 Annex C unsorted/duplicated negative. GREEN: the 255-byte normative example reproduces `ca364710…`.
- **T4 — authorize evaluator** (m-3 §1.3/§1.3a): P0→PS→P1→P2→P3→P4 in the total order, FIRST failure = THE token, nine-token closed enum, byte-equality endpoint compare (never substring), name-only P3 check, lane-catalog fact as sole method authority (P4), NO epoch re-check here (single owner — m-10's gate + the T11 DATA-P fence). RED: the normative multi-failure vector (⇒ `lane-mismatch`, never a later token); per-token determinism.
- **T5 — `LLMRequest` intake** (`m8.llm_request.v1`, r12 §1.1): closed schema, unknown field ⇒ `malformed_request`; the five input_item kinds; `tool_result.content` UTF-8 string; **replay envelope scope check PRE-translate** (`origin_provider_lane_id`==lane AND `origin_turn_id`==turn ⇒ else `replay_scope_violation`, zero transport, terminal row); lane-capability mismatch (⇒ `lane_capability_mismatch` before freeze); legacy `replay_payload` field ⇒ `malformed_request`. RED: fixture 15's four legs + fixture 16 totality.
- **T6 — translate** (r12 §5.2, compiled versioned compat profile `openai-responses`): pure deterministic function (`LLMRequest` × lane facts × profile → exact wire body bytes); tools lowering per addendum §3.2 (five-member census `{type:"function", name, description, parameters, strict}`, `description` present-empty never omitted, `strict` from the lane fact, order preserved, **present-empty `tools: []`** as the sole zero-tools form); `store:false` fixed; `reasoning_replay`: ONLY `envelope.payload` unwraps verbatim into the wire item — opaque bytes, never the wrapper. Computes **E** (`provider_lowered_tools_digest` = JCS-SHA-256 of the lowered `tools` value) at freeze. RED/GREEN: fixture 11 determinism property; addendum §6.5/6/7 (zero-tools, missing-member-fails, order/strict/description sensitivity).
- **T7 — freeze** (r12 §2.1): `frozen_core` five members (lowercase unique sorted non-auth headers; duplicate names ⇒ freeze refused ⇒ `rejected_local(internal_integrity_fault)` at `refusal_stage=pre_freeze`); `frozen_core_digest` = JCS-SHA-256 (**B**); the wire-field census (frozen `user-agent` literal `frank-connector/<build>`; suppressed `accept-encoding`; derived-censused `host`/`content-length`/`connection: close`). RED: fixture 3 mutation harness + F12 distinct-digest vectors.
- **T8 — attach + S-A custody** (r12 §3, m-1 §1): `m8.credentials.v1` file (0600 verify at open ⇒ else typed `credential-file-invalid` fail-closed at spawn), closed schema, `credential_ref` grammar/membership/duplicate checks at bootstrap; **resolution lexically unreachable on any denied/rejected path** (resolve only inside step-4 attach, keyed by exactly the authorized envelope's ref); exactly ONE added auth header (name from the lane's auth fact — in `denied_header_names`, so it cannot exist in the core). RED: fixtures 1 (all nine tokens ⇒ zero transport) + 2 (resolver never invoked, per-token) + the m-1 reference-without-authority negative.
- **T9 — send + the owned transport** (r12 §2.2 step 5 + §2.3 + §5.1): digest re-check (mismatch ⇒ `rejected_local(internal_integrity_fault)` at `refusal_stage=post_freeze`, emitting the STEP-2 authorized B); body-hash verify; the pinned transport (HTTP/1.1 only + empty `TLSNextProto`, `DisableKeepAlives=true`, `GetBody=nil`, `DisableCompression=true`, `Proxy: nil`, `CheckRedirect` ⇒ error, https-only); instrumented `DialContext` + connection wrapper counting `{dial_attempts, connections_established, request_write_started, request_write_completed}` below the encoder; `TTFB_DEADLINE`/`STALL_DEADLINE` composed with the cancel context; `Retry-After` recorded never acted on. RED: fixture 4's per-cut counter vectors incl. ALPN-never-h2 capture + pool-absence + redirect ⇒ `failed{protocol}`.
- **T10 — SSE parse + the normalized event stream** (`m8.provider_event.v2`, r12 §1.2 + addendum §1.1): the block grammar (start/delta/end; terminal exactly-once; errors-in-stream), tool-call fragments INERT until `tool_call_end` (assembled+validated; scratch never leaves m-8), synthesized `block_id`/`tool_call_id`, usage extraction (interleaved progress-only; terminal `completed.usage` authoritative), unknown finish ⇒ `failed{protocol}`, oversized single event ⇒ `failed{frame_overflow}`, `ATTEMPT_STREAM_MAX`, chunk pacing (fan-out only, block boundaries survive, every frame ≤ `FRAME_MAX`); **provider reasoning items = opaque round-trip**: `reasoning_end.replay_envelope` minted at emission (`{origin_provider_lane_id, origin_turn_id, payload: base64 verbatim}`), payload never parsed/logged/surfaced; B+E digest members on the three terminals ONLY. RED: fixture 10 corpus + `opaque_item_passthrough` byte-compare.
- **T11 — CTRL-C lifecycle + epoch authority** (m-10 §B.1/§B.4, r12 §5.3): argv = three artifact paths (paths are not secrets); load order (credential file → catalog → policy); first frame `hello{pid, build_info}`; `connector_assign` seven-field verify (loaded-policy hash == `policy_digest`; loaded-catalog hash == `lane_catalog_digest`; pinned lane ∈ catalog; `credential_ref` ∈ entries) ⇒ `connector_ready{run_id, turn_epoch}` — **READY-gate: zero DATA-P accept, zero provider-send path before the ack**; epoch cache advances ONLY on m-10-sourced values (bootstrap + `epoch_update`, monotonic within that stream); DATA-P fence: below-current ⇒ `STALE_EPOCH` (typed reply, NO `attempt_result`, zero everything), above-current ⇒ hold + CTRL-C query ⇒ `EPOCH_AHEAD` rejected-retriable (never peer adoption); `ping`/`pong`; `shutdown` graceful; **CTRL EOF ⇒ fail-closed exit** (no further DATA-P accept/authorize/send); generation-blind (no generation_id anywhere). RED: fixtures 5, 6, 17 (attempt-inertness both legs).
- **T12 — outcome reporting** (`m8.attempt_result.v2` + `m8.dataP_reply.v2`, r12 §1.3 + addendum §1): the TOTAL disposition mapping (six closed dispositions, each at its outcome's OWN terminal boundary); `rejected_local` two-write order (**CTRL-C write happens-before the DATA-P reply write** — the owner-real R14-F1 barrier; the reversed-order mutation MUST fail); `refusal_stage ∈ {pre_freeze, post_freeze}` on every reject reply + the reject dispositions, with the invariant `post_freeze ⟺ B∧E present` (validatable, never self-certifying — the classifier never inspects digest members); `egress_denied{token}` carries B+E (post-freeze by construction); one-way (no reply expected, no `cancellation_id` carried); ids/epochs/dispositions/tokens/digests ONLY on CTRL-C. RED: fixture 16 + addendum §6.1/6.9/6.11.
- **T13 — cancellation** (r12 §1.4): typed `cancel_attempt{attempt_id, turn_epoch}` current-epoch intent ⇒ abort HTTP context; honest split — `cancelled(pre_transport)` (zero wire, counters `{0,0,0,0}`, m-8-view-only) vs `cancelled(post_invocation)` (wire crossed, two-view); **raw DATA-P closure / crashed context NEVER emits `cancelled`** (channel loss parks UNKNOWN upstream — m-8 emits nothing); duplicate/delayed equivalence over `{attempt_id, reported turn_epoch, cancel_point}` with the historical epoch. RED: fixture 17b legs (a)–(f).
- **T14 — `cmd/frank-connector` main:** wire T1–T13 into the process: private runtime dir, close-on-exec re-mark, spawn sequence, sanitized posture (no secret in argv/env; crash-dump scrub posture per m-1 rows 2/3/5/6), single incarnation (no solo-restart logic — co-restart is m-10's). RED: fixture 12's m-8-realized sentinel-secret legs (sentinel absent from every frame/event/log/typed error/core/envelope/digest).
- **T15 — battery close + fixtures 13a/14 + conformance sweep:** the package-boundary import test (13a); the on-wire capture fixture 14 (capture server + connection tap: emitted request == authorized envelope + exactly the one auth header + the censused fields, nothing uncensused; body hash == `body_sha256`; auth value provably added at Attach); a fake-provider E2E run (spawn → hello/assign/ready → one governed `LLMRequest` → stream → terminals, all dispositions driven); full-repo battery green (INV-CATALOG laws + existing tests untouched); the addendum §6 eleven fixtures swept complete.

## §3 — Acceptance (the end-of-slice bar)

Every r12 §8 fixture (1–17b) + every addendum §6 fixture (1–11) implemented with its RED leg driven first and cited in the final report; `go vet` + full `go test ./...` green at every commit; zero out-of-fence bytes (verify with `git status --short` + the fence list); the two named seam items (framing-vs-appipc restack note; go.mod untouched) restated in the final report; your ONE end-of-slice adversarial review, then my end-review, then the restack + rerun + re-review + operator MERGE-GATE per the concurrent-branch contract.

## §4 — Escalation rails (charter restated)

Spec-base defect / better way / locked-contract touch / fence deviation / cross-slice collision / any amendment need ⇒ UP through master (the m-8 pair guides by relay from outside; serialized seam arbitration for anything touching a live sibling's seam). The gates-cannot-check queue is LAW. If a provider safeguard fires on accurately-described opaque round-trip work: STOP, report the exact context up through master — never iterate phrasings.

## §5 — Your review ask (this relay's gate)

PLAN-REVIEW verdict on: (a) task decomposition completeness vs the frozen bases; (b) the in-fence framing decision + restack note (§1.4); (c) the stdlib-only/go.mod-untouched pin; (d) fixture coverage mapping (§2 ↔ r12 §8 + addendum §6); (e) anything the plan invents rather than cites (must be zero). On APPROVE I issue the build dispatch; on must-revise I fold and re-route.

ACTIONS_GIT_REF: docs-workspace disk action — this plan file + one s14 INDEX row (in-fence `frank/.relays/s14/**`); the store carries the transport twin this act. No source byte, no branch, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s14/
Next requested action: s14.implementer files the PLAN-REVIEW verdict TO s14.planner; on APPROVE the build dispatch + token issue and the worktree branch opens.
