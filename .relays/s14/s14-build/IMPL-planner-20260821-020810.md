## IMPL — THE s14 DIRECT IMPLEMENTATION DISPATCH (issued under the commission's delegated dispatch authority, its conditions now ALL met and evidenced: the t4-s14-commission grant [boot round-trip complete — boots relay-a2ebc783/relay-8eed8a94, readiness relay-7d3000b8, the PM return relay-31a6c009] + the implementer's APPROVING plan-review `s14-build-plan-review-3` at verdict `approve` over exact plan r3 SHA-256 `84205d26f88aba475ecf15b39972f3fa47e9ea8f6e36b82b1e6e274f65d887a2`): s14.implementer is dispatched to BUILD the plan of record `frank/.relays/s14/s14-build/PLAN-planner-20260821-004514.md` — seventeen tasks (T1a/T1b/T1c + T2–T15, with T2–T15 + §2a defined by exact r2 `PLAN-planner-20260820-234459.md`), straight-through, RED-first, on the worktree branch `s14-m8-connector` cut from LAUNCH_BASE `main@b7f406b2afc20fc2ba7ffa2420b5c3a8828f62f9`, every source/test byte and every commit YOURS (CORRIGENDUM 1). Transport note: hand-relay is the courier (operator 2026-08-21, `step3-t4-preflight/RECONCILE-orchestrator-planner-20260821-020247.md`) — file your relays in the lane; no store acts.

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build-plan-review-3
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — implementation only; MERGE stays the operator's MERGE-GATE in the serialized s13 → s14 → s15 order, and queue row 7's disposition rides the merge brief
FILED_AT_LOCAL: 20260821-020810
IN_REPLY_TO: frank/.relays/s14/s14-build/PLAN-REVIEW-implementer-20260821-012712.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: the literal implementation dispatch — build plan r3 on the s14-m8-connector worktree; all authority conditions evidenced in lineage

## §1 — The mechanical scope diff (protocol-required before a delegated dispatch; list, not judge)

SCOPE_DIFF (each surface the locked plan touches -> vs the dispatch in-scope list):
- frank/cmd/frank-connector/** -> in (charter fence)
- frank/internal/connector/** (jcs · nfccheck · frame · all connector packages) -> in (charter fence)
- frank/.relays/s14/** -> in (charter fence; relay lane)
- frank/docs/sprints/active/2026-08-20-s14-m8-connector/** -> in (R1 fence addition, `…-234501` R1; may remain explicitly unused)
- frank/go.mod (EXACTLY: the directive `go 1.22` -> `go 1.25` + `require golang.org/x/text v0.41.0`) -> in (R4 + R4a ruled bytes, branch-only)
- frank/go.sum (EXACTLY what `go mod tidy` generates; no hand edits) -> in (R4 ruled)
- everything else in the workspace -> OUT (a needed touch = escalation by construction, never an edit)
SCOPE_DIFF_RESULT: no-deviation

## §2 — Dispatch terms (all from the approved plan; none new)

1. Build to plan r3 §§0–5 exactly; where r3 summarizes, exact r2 §2/§2a governs T2–T15 and the fixture boundary matrix. Spec doubt resolves at the frozen bases (r12 `4b670a79…` + addendum `734e44b7…` + the consumed contracts), never at the plan's paraphrase; a spec-base defect escalates UP, never a local fix.
2. First acts: cut the worktree branch `s14-m8-connector` from `main@b7f406b2`; T1b's first commit = the §1.1a ruled dependency/directive edit with its RED/GREEN command outputs cited in the task log.
3. Straight-through cadence: task-by-task, no per-task review relays; RED-first negatives; full battery + `go vet` + `go test ./...` green at every commit; the ten INV-CATALOG laws untouched; byte-exact `{accepted, rejected, held}`; the frozen oracle + `master/exit-fixtures/**` untouchable; H-12 stands.
4. The §2a matrix governs every fixture claim: LOCAL legs production-realized E2; PRODUCER legs labeled; RESTACK/INTEG named; SIBLING legs never claimed.
5. Completion: your ONE end-of-slice adversarial review filed in the lane, then my end-review; then restack + battery rerun + substantive re-review; the operator MERGE-GATE is terminal and not yours or mine to claim.
6. Escalation rails per the charter + plan §4; any divergent shared-line byte at restack = arbitration stop; safeguard-fires-on-accurate-phrasing = STOP and report up; hand-relay transport (file paths, no store).

## §3 — The token

DISPATCH IMPL

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch + one s14 INDEX row (in-fence). No source byte by this seat, no branch by this seat (the worktree + every commit are the implementer's from here), no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** only; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer cuts the worktree branch and opens the build at T1a; progress rides the lane per the straight-through cadence.
