## PLAN — the s14 m-8 connector build plan r2 (the append-only successor folding the must-revise PLAN-REVIEW `s14-build-plan-review-1`, findings S14-PR1-F1 + S14-PR1-F2; supersedes `PLAN-planner-20260820-223912.md` as the plan of record): build `frank/cmd/frank-connector` + `frank/internal/connector/**` to the FROZEN m-8 spec-of-record (provider contract r12 `4b670a79…` + B/E addendum r7 `734e44b7…`) and its consumed bases — 16 tasks straight-through (T1 split into T1a pure-JCS + T1b artifact-NFC validity per F1), RED-first negatives, stdlib-only EXCEPT the ONE master-arbitrated dependency question now ON THE TRAIL (escalation `SITREP-planner-20260820-234239.md`, store intake this act; go.mod stays byte-untouched by s14 under every route), and the NEW §2a per-fixture boundary matrix (per F2) that acceptance now binds to — local completion claims m-8-owned legs + fake-counterpart producer assertions ONLY; cross-lane legs are named sibling/restack/integration predicates, never claimed locally. On your APPROVE I issue the build dispatch; you cut the worktree branch `s14-m8-connector` from LAUNCH_BASE `main@b7f406b2` and author every commit; I end-review at completion.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build-plan-review-1
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
GRILL_REQUIRED: no — the successor folds the two review findings; every mechanism remains a byte-cited pointer into the frozen spec
HUMAN_GATE_REQUIRED: no — the pair's terminal gate (the operator MERGE-GATE, serialized s13 → s14 → s15) is unchanged; the go.mod ruling is master's arbitration on his cadence
FILED_AT_LOCAL: 20260820-234459
IN_REPLY_TO: frank/.relays/s14/s14-build/PLAN-REVIEW-implementer-20260820-230350.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: plan r2 — F1 closed (JCS/NFC split + the arbitration-or-fallback strategy on the trail), F2 closed (the §2a fixture boundary matrix binds acceptance); plan-review requested

## §0 — Governing bytes (unchanged from r1; verified this session, zero drift)

- **A-M8-PROVIDER** r12 @ `4b670a79…` — THE spec: `LLMRequest`/events (§1), `freeze→authorize→attach→send` (§2), S-A custody (§3), lane catalog (§4), owned client (§5), E0 feed (§6), F63 grain (§7), fixtures 1–17b (§8).
- **A-M8-BE** r7 @ `734e44b7…` — the v2 carriers, P1 presence by the freeze boundary, `refusal_stage` discriminator, E recipe, fixtures §6.1–11.
- **A-M3-EGRESS** + **A-M3-SCHEMAV** — P0–PS–P1–P2–P3–P4 in the §1.3a total order (nine-token closed enum), §1.5 policy bytes/digest (the 255-byte normative fixture ⇒ `ca364710…`), §1.6 endpoint grammar + vectors, E0 v2 census (ids/digests/enums/timestamps ONLY).
- **A-M10-SEAM** — §A.2 framing (`FRAME_MAX` 4 MiB, counter-string grammar), §A.3 bounded/blocking/fault-on-deadline, §B.1 `hello`→`connector_assign`(7)→verify→`connector_ready` + READY-gate, §B.4 generation-blind m-10-sourced epoch authority, CTRL-EOF fail-closed, `attempt_result` six-disposition enum, co-restart.
- **A-M1-SECRET** — S-A custody: one 0600 m-8-private credential file, opaque `credential_ref` (1.4a), resolution ONLY inside the post-authorize attach, hardening rows 2/3/5/6.
- **A-M9-WORKER** + **A-JB-M9/M3/CARRIAGE** — B-carriage: compute at freeze, emit the STEP-2 authorized value, downstream copies verbatim; pre-freeze rejects carry NO digest; loss cuts owe NO positive non-emission record.
- Riding law: the phrasing guidance — schema vocabulary; provider `reasoning` output items are OPAQUE BYTES round-tripped byte-preserved, never parsed/decoded/rendered/logged; escalate (never rephrase) if a task ever seems to need the inside of an item.

## §1 — Standing constraints (all tasks; F1-adjusted at item 1)

1. **Write fence:** `frank/cmd/frank-connector/**` + `frank/internal/connector/**` + `frank/.relays/s14/**` ONLY; `appipc`/`seatclient`/conductor tree consume-only; `go.mod`/`go.sum` arbitration-only. **Dependency pin (F1-revised):** zero dependencies except as the master arbitration rules — the escalation `s14-build/SITREP-planner-20260820-234239.md` (on the store this act) requests Route A (`golang.org/x/text`, version-pinned at the ruling, `norm.NFC.IsNormalString` check-only) with Route B (in-fence vendored Unicode NFC_QC quick-check tables, version-pinned, NormalizationTest conformance vectors) as the named fallback. **s14 edits no go.mod/go.sum byte under either route** (Route A's edit is master's to make or explicitly delegate). T1b binds to the ruling; every other task proceeds ruling-independent.
2. **No conductor byte moves.** The ten INV-CATALOG laws + the full existing battery green at every commit; byte-exact `{accepted, rejected, held}` untouched; the frozen oracle + RLBS-1 + `master/exit-fixtures/**` untouchable.
3. **m8.* types app-internal** (r12 §1.1): no conductor-payload serializer may exist (fixture 13a); never import `internal/egress`; never invoke the model-name classifier.
4. **Framing seam note (named restack item):** the §A.2 frame codec implemented IN-FENCE under `internal/connector/frame`, byte-conformant to the m-10 contract; at restack (s13 merges first) evaluate adopting `appipc` consume-only; any needed `appipc` change = escalation (serialized seam arbitration); any post-restack shared-seam change is escalated, never silently authored (reviewer condition, accepted).
5. **H-12 stands**; bash-originated egress is ungoverned and out of scope (r12 §0.4).
6. **Compiled constants** (tunable values, unremovable names): `FRAME_MAX`, `SEND_DEADLINE`, queue depths, `HEALTH_INTERVAL`, `HANDSHAKE_DEADLINE`, `TTFB_DEADLINE`, `STALL_DEADLINE`, `ATTEMPT_STREAM_MAX`.
7. **Straight-through cadence:** task-by-task, no per-task review relays; RED-first negatives per task; ONE end-of-slice adversarial review (yours) then my end-review; literal `git status --short` in every final report.

## §2 — Task breakdown (build order; each task = code + its RED-first fixture legs; the §2a matrix governs every fixture claim)

- **T1a — module skeleton + PURE RFC 8785 JCS** (`internal/connector/jcs`): canonical numbers, escaping, unique object keys, member ordering, **strings PRESERVED byte-for-byte — no normalization, no rewriting, no global NFC rejection** (m-3 §1.5: "JCS does not normalize"). RED: non-canonical inputs, duplicate keys; GREEN: the frozen byte vectors (the 255-byte m-3 example among them).
- **T1b — artifact-NFC VALIDITY** (`internal/connector/nfccheck` or the Route-A import): a check-only predicate applied AT the schema loci whose frozen contracts require NFC (the m-3 §1.5 policy validator [T3]; the r12 §4 catalog validator [T2]) — **rejects non-NFC (⇒ that artifact's malformed token), never rewrites accepted bytes, never runs on surfaces whose schemas do not require it.** Mechanism per the master ruling (Route A `norm.NFC.IsNormalString` | Route B vendored quick-check + conformance vectors). RED: a non-NFC policy string ⇒ `policy-unavailable`; a non-NFC catalog string ⇒ READY refused; a valid NFC non-ASCII artifact ACCEPTED (the over-rejection negative).
- **T1c — frame codec** (`internal/connector/frame`, §A.2/§A.3): uint32_be length prefix, `FRAME_MAX`, the `{v, chan, type, seq, re, run_id, turn_epoch, body}` envelope, counter-string grammar `^(0|[1-9][0-9]*)$` < 2^64 with numeric compare, unknown-type ⇒ reject-and-record, unknown-field-ignore on CTRL-C types, per-sender monotonic seq, bounded send queue + blocking write + `SEND_DEADLINE` fault. RED: oversized frame, malformed counter, unknown type.
- **T2 — lane catalog** (`m8.lane_catalog.v1`, r12 §4): types, load, validator A (schema-shape) + B (semantic: sorted/unique `lane_id`, 4-axis uniqueness, id grammar, §1.6 endpoint validity, lowercase auth header + `denied_header_names` membership cross-check, closed method enum, RFC3339 fields, NFC via T1b), JCS digest. Opacity: no code path branches on `lane_id`/`serving_profile_id` bytes (structural call-graph test). RED: fixture 8's negatives ⇒ READY refused. Seeded one-row content is CONFIG.
- **T3 — egress policy load** (m-3 §1.5): P0 validity (exact-stored-bytes JCS check — no re-serialization; NFC via T1b; closed schema; `method_by_lane` present ⇒ malformed; sorted duplicate-free sets; lowercase `denied_header_names`; §1.6-valid allowlist), digest vs `connector_assign.policy_digest` BEFORE any authorize decision. RED: fixture 9 + Annex C. GREEN: the 255-byte example ⇒ `ca364710…`.
- **T4 — authorize evaluator** (m-3 §1.3/§1.3a): P0→PS→P1→P2→P3→P4 total order, FIRST failure = THE token, nine-token closed enum, byte-equality endpoint compare, name-only P3, lane-fact method authority (P4), NO epoch re-check here. RED: the normative multi-failure vector; per-token determinism.
- **T5 — `LLMRequest` intake** (r12 §1.1): closed schema, unknown field ⇒ `malformed_request`; five input_item kinds; `tool_result.content` UTF-8 string; replay envelope scope check PRE-translate (⇒ `replay_scope_violation`); `lane_capability_mismatch` before freeze; legacy `replay_payload` ⇒ `malformed_request`. RED: fixture 15 legs + fixture 16 totality.
- **T6 — translate** (r12 §5.2, compiled `openai-responses` profile): pure deterministic function; tools lowering per addendum §3.2 (five-member census, `description` present-empty, `strict` from the lane fact, order preserved, present-empty `tools: []` sole zero-tools form); `store:false`; `reasoning_replay`: ONLY `envelope.payload` unwraps verbatim — opaque bytes. Computes **E** at freeze. RED/GREEN: fixture 11; addendum §6.5/6/7 producer halves.
- **T7 — freeze** (r12 §2.1): `frozen_core` five members (duplicate header names ⇒ `rejected_local(internal_integrity_fault)` @ `pre_freeze`); **B** = JCS-SHA-256; the wire-field census (frozen `user-agent`, suppressed `accept-encoding`, derived-censused `host`/`content-length`/`connection: close`). RED: fixture 3 + F12 vectors.
- **T8 — attach + S-A custody** (r12 §3, m-1 §1): `m8.credentials.v1` 0600 file (else `credential-file-invalid` fail-closed at spawn), closed schema, `credential_ref` grammar/membership/duplicate checks at bootstrap; **resolution lexically unreachable on any denied/rejected path**; exactly ONE added auth header. RED: fixtures 1 + 2 + the m-1 reference-without-authority negative.
- **T9 — send + the owned transport** (r12 §2.2 step 5, §2.3, §5.1): digest re-check (mismatch ⇒ `rejected_local(internal_integrity_fault)` @ `post_freeze`, emitting the STEP-2 authorized B); the pinned transport (HTTP/1.1 only + empty `TLSNextProto`, `DisableKeepAlives=true`, `GetBody=nil`, `DisableCompression=true`, `Proxy: nil`, redirects ⇒ error, https-only); instrumented `DialContext` + connection wrapper counting `{dial_attempts, connections_established, request_write_started, request_write_completed}`; `TTFB_DEADLINE`/`STALL_DEADLINE` composed with the cancel context; `Retry-After` recorded never acted on. RED: fixture 4 per-cut vectors + ALPN-never-h2 + pool-absence.
- **T10 — SSE parse + the normalized event stream** (`m8.provider_event.v2`): block grammar (terminal exactly-once, errors-in-stream), tool-call fragments INERT until `tool_call_end`, synthesized ids, usage extraction (terminal `completed.usage` authoritative), unknown finish ⇒ `failed{protocol}`, oversized event ⇒ `failed{frame_overflow}`, `ATTEMPT_STREAM_MAX`, chunk pacing (fan-out only, ≤ `FRAME_MAX`); reasoning items opaque round-trip (`reasoning_end.replay_envelope` minted at emission; payload never parsed/logged/surfaced); B+E on the three terminals only. RED: fixture 10 corpus + `opaque_item_passthrough` byte-compare.
- **T11 — CTRL-C lifecycle + epoch authority** (m-10 §B.1/§B.4, r12 §5.3): argv artifact paths; load order (credential → catalog → policy); `hello{pid, build_info}`; seven-field `connector_assign` verify ⇒ `connector_ready{run_id, turn_epoch}`; READY-gate (zero DATA-P accept / provider-send path pre-ack); epoch cache m-10-sourced only (monotonic within the update stream); DATA-P fence (`STALE_EPOCH` / hold + CTRL-C query ⇒ `EPOCH_AHEAD`, never peer adoption); `ping`/`pong`; `shutdown`; CTRL EOF ⇒ fail-closed exit; generation-blind. RED: fixtures 5, 6, 17 (m-8-side legs per the §2a matrix).
- **T12 — outcome reporting** (`m8.attempt_result.v2` + `m8.dataP_reply.v2`): the TOTAL six-disposition mapping at each outcome's OWN terminal boundary; `rejected_local` two-write order (CTRL-C write happens-before the DATA-P reply write; the reversed-order mutation MUST fail); `refusal_stage` on every reject reply + reject dispositions with the invariant `post_freeze ⟺ B∧E present` (classifier never inspects digest members); `egress_denied{token}` carries B+E; one-way; ids/epochs/dispositions/tokens/digests ONLY. RED: fixture 16 + addendum §6.1/6.9/6.11 producer halves.
- **T13 — cancellation** (r12 §1.4): typed `cancel_attempt{attempt_id, turn_epoch}` ⇒ abort; `cancelled(pre_transport)` (zero wire, `{0,0,0,0}`) vs `cancelled(post_invocation)` (wire crossed); raw closure NEVER emits `cancelled`; duplicate equivalence `{attempt_id, reported turn_epoch, cancel_point}`. RED: fixture 17b m-8-view legs per the matrix.
- **T14 — `cmd/frank-connector` main:** wire T1–T13; private runtime dir, close-on-exec re-mark, spawn sequence, sanitized posture (m-1 rows 2/3/5/6), single incarnation (no solo-restart logic). RED: fixture 12's m-8-realized sentinel legs.
- **T15 — battery close:** fixture 13a (package-boundary + no-serializer); fixture 14 (on-wire capture: emitted request == authorized envelope + the one auth header + censused fields, nothing uncensused; auth value provably added at Attach); a fake-provider + fake-counterpart E2E run (spawn → hello/assign/ready → one governed `LLMRequest` → stream → every disposition driven); full-repo battery green; the §2a matrix's LOCAL + PRODUCER columns swept complete and the report citing it per-leg.

## §2a — THE FIXTURE BOUNDARY MATRIX (F2 — acceptance binds here; r12's "no local fixture claims a cross-lane row" preserved verbatim)

Leg classes: **LOCAL** = s14/m-8 production-realized E2 · **PRODUCER** = s14 producer output or fake-counterpart contract assertion (labeled as such, never claimed as the sibling's proof) · **RESTACK/INTEG** = named restack or s16-integration obligation (rerun gate: the concurrent-branch contract's battery-rerun-at-restack + the s16 composed-app/exit suite) · **SIBLING** = m-2/m-3/m-7/m-9/m-10-owned; s14 must not claim it.

| fixture | LOCAL (s14 claims) | PRODUCER (labeled assertion) | RESTACK/INTEG | SIBLING (never claimed) |
|---|---|---|---|---|
| r12 1 denied-sends-nothing | all nine tokens, fake transport counter=0 | — | rerun at restack | — |
| r12 2 attach-after-authorize | instrumented resolver never invoked, per-token | — | rerun | — |
| r12 3 frozen-core-immutable | digest equality + mutation harness | — | rerun | — |
| r12 4 transport-does-not-retry | all per-cut counter vectors, ALPN capture, GetBody-nil, pool-absence | — | rerun | — |
| r12 5 stale/ahead epoch | m-8 fence behavior both legs | fake m-10 CTRL-C answers the query leg | s16: real m-10 counterpart | m-10's `attempt_open` primary gate |
| r12 6 ctrl-eof-fail-closed | exit + zero further authorize/send | fake CTRL-C EOF | s16 real supervision | m-10 co-restart disposition |
| r12 7 endpoint grammar | the full m-3 vector list vs both validators | — | rerun | grammar ownership stays m-3 |
| r12 8 catalog validation | all negatives + id-opacity structural test | — | rerun | — |
| r12 9 policy load | non-canonical ⇒ `policy-unavailable`; digest mismatch ⇒ READY refused | fake `connector_assign` supplies the comparand | s16 real manifest | m-10's manifest-side freeze equality |
| r12 10 wire conformance | corpus-driven assembly/terminals/timeouts/overflow legs | recorded fixture corpora (prior-art-derived) | **build-time re-record against the real dialect = s16/live-E3 territory (credentialed)** | — |
| r12 11 determinism | translate() property test | — | rerun | — |
| r12 12 sentinel-secret | m-8 frames/events/logs/typed errors/core/envelope/digests | sentinel + process-under-test contributed | — | m-9 prompt/tool-output legs · m-10 launch-path legs · annex-row-9 conductor records |
| r12 13a serializer-absence | package-boundary import test | — | — | — |
| r12 13b conductor entry points | — | sentinel type set contributed | — | the m-7/m-2 cross-lane harness |
| r12 14 on-wire capture | capture server + tap; envelope equality; attach-point proof | — | rerun | — |
| r12 15 replay-scope | all four legs (violations + verbatim re-emit + legacy field) | — | rerun | — |
| r12 16 local-reject totality | every reject_reason; zero stream/resolver/transport; the two-write ordering barrier + reversed-order negative | fake m-10 receives the CTRL-C write | s16 real row close | m-10's terminal `REJECTED_LOCAL` row |
| r12 17 epoch attempt-inertness | typed replies; zero `attempt_result`; zero counters | fake m-10 for the query; "no row created" asserted at the fake | s16 real rows | m-10's row/retirement machinery |
| r12 17b cancellation | legs (a)(b)(d) m-8-view: dispositions, counters, one-way, no-`cancelled`-on-raw-closure; leg (e) m-8's own never-`phase=failed` supply | fake m-9 cancel intent; fake m-10 row assertions | s16 two-view reconciliation | m-10 `CANCELLED`/`UNKNOWN` rows · m-9 `stream_cancelled`/`turn_cancelled` + E0 population · leg (f) retirement/reap races |
| add §6.1 P1 presence | per-row presence/absence on every m-8 carrier | — | rerun | — |
| add §6.2 P2a vs P2b | P2a counter cuts | an observer-recipe SELF-CHECK over the fixture-14 tap (labeled producer assertion, never m-3's E3) | — | P2b decidability = m-3's evaluator |
| add §6.3 B emission identity | emitted B == the internal mutation-guard value | — | rerun | — |
| add §6.4 B observer negatives | — | the six exclusion-recipe negatives run as producer self-checks over the local tap | — | the real observer derivation = m-3 |
| add §6.5 E determinism | cross-run/platform property | — | rerun | — |
| add §6.6 E zero-tools | producer emits present-`[]`; missing-member FAILS the field-set check | observer-half self-check labeled | — | m-3's evaluator half |
| add §6.7 E field-set/order/strict | producer sensitivity legs | observer-half self-check labeled | — | m-3's evaluator half |
| add §6.8 E independence | never sourced from m-9 | — | — | — |
| add §6.9 version decodability | one uniform v2 per channel; presence == P1 | fake-consumer parse of every reachable message | **the carriage-fold contract: m-9/m-10 parsers upgraded = their slices' obligation, checked at s16** | m-9/m-10 parser realities |
| add §6.10 payload/secret absence | sentinel sweep over both digest fields + inputs | — | rerun | — |
| add §6.11 refusal_stage | the total (reason × stage) decode; both crafted invariant-violation negatives; classifier never inspects digest members | fake-classifier keying legs labeled | — | m-3's real classifier keying |

## §3 — Acceptance (the end-of-slice bar; F2-rebound)

**Local completion = every §2a LOCAL leg production-realized E2 with its RED leg driven first, + every PRODUCER leg green and LABELED as producer/fake-counterpart assertion in the final report, per-leg, citing this matrix.** Cross-lane closure is NOT claimed: the RESTACK/INTEG column rides the concurrent-branch contract (batteries rerun at restack; the s16 composed-app + exit suite) and the SIBLING column belongs to its owners. `go vet` + full `go test ./...` green at every commit; zero out-of-fence bytes; the named seam items (framing-vs-appipc; the go.mod ruling disposition) restated in the final report; your ONE end-of-slice adversarial review, then my end-review, then restack + rerun + re-review + the operator MERGE-GATE.

## §4 — Escalation rails (unchanged)

Spec-base defect / better way / locked-contract touch / fence deviation / cross-slice collision / amendment need ⇒ UP through master; serialized seam arbitration for live-sibling seams; the gates-cannot-check queue is LAW; safeguard-fires-on-accurate-phrasing ⇒ STOP and report up.

## §5 — Your review ask (this relay's gate)

PLAN-REVIEW verdict on the two folds: (a) F1 — the T1a/T1b split (string-preserving JCS; check-only artifact-NFC at exactly the requiring loci; the arbitration-or-fallback strategy with the escalation on the trail and T1b alone binding to it); (b) F2 — the §2a matrix's completeness and honesty (every r12 §8 + addendum §6 leg classified; acceptance rebound). Everything else carries from r1 unchanged. On APPROVE I issue the build dispatch (T1b's NFC legs conditioned on the master ruling if still pending); on must-revise I fold and re-route.

ACTIONS_GIT_REF: docs-workspace disk action — this successor plan + one s14 INDEX row + the arbitration escalation filed this act (`SITREP-planner-20260820-234239.md`, store-transported). No source byte, no go.mod/go.sum byte, no branch, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** only; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer files the PLAN-REVIEW verdict on r2 TO s14.planner; on APPROVE the build dispatch issues and the worktree branch opens.
