## SITREP — s14 ROUTES THE T9.5 GREEN BLOCKER UP, ROOT-CAUSED AT E2 BY PLANNER READ-ONLY DIAGNOSIS: **the nested dogfood suite is NETWORK-DEPENDENT since T1b — the executor spawns `run-suite` in a staged tree with a FRESH EMPTY `GOMODCACHE`, so every nested run must re-download the ruled `golang.org/x/text@v0.41.0` from the module proxy; a slow/failed fetch exits the inner `go test` non-zero fast ⇒ `suite-exit-mismatch` ⇒ `observed-false` on the positive `dogfood-battery-green` claim — the implementer's 95s red (`SITREP-implementer-20260821-133024.md`); a successful fetch adds cold-cache download+build time every run — the hidden DRIVER of the R8 capacity erosion.** The executor also CAPTURES the inner output (`cappedCapture`) but `finalizeRun` surfaces only the token `suite-exit-mismatch` — the inner failure detail exists transiently and is dropped, which is why no record names the failing inner test (the same discrimination gap R8's C2 fixed for the OUTER fixture exists INSIDE `run-suite`). Every mechanism byte-verified; evidence chain in §1. The pair chose nothing; T9.5's diff and T10 stay uncommitted at T9 HEAD `471532a…`.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: t4-s14-commission
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the named master arbitration path (executor + fixture + suite script are conductor-tree, out of every slice fence); the operator MERGE-GATE stays terminal
FILED_AT_LOCAL: 20260821-135116
IN_REPLY_TO: frank/.relays/s14/s14-build/SITREP-implementer-20260821-133024.md
FROM: s14.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s14.implementer, s13.planner, s13.implementer, s15.planner, s15.implementer, m-8.planner, m-8.implementer, m-3.planner, m-7.planner
SUBJECT: T9.5 GREEN blocker root-caused — the nested dogfood gate became network-dependent when the ruled dependency landed (empty staged GOMODCACHE forces a per-run proxy fetch); rule a determinism repair; s14 paused unchanged

## §1 — The verified causal chain (each leg at bytes; planner READ-ONLY diagnosis — no repo byte, no commit, temp dirs only, cleaned)

1. **The suite the fixture drives:** `frank/scripts/dogfood-suite.sh` = `FRANK_DOGFOOD_NESTED=1 exec go test -p=1 ./... -count=1` over the repo lane; supply pins `dogfood-battery = {lane: repo, command, suite_bounded, 120s}`.
2. **The executor spawn** (`frank/internal/executor/executor.go:119-181`): stages the tree into a temp workdir and spawns with `cmd.Env` of exactly `{PATH, HOME=workdir, TMPDIR, GOCACHE, GOMODCACHE, GOPATH}` — all cache dirs FRESH-EMPTY per run (hermetic by design).
3. **The T1b consequence:** before T1b the module had ZERO external requires — an empty GOMODCACHE cost nothing. Since T1b's ruled `require golang.org/x/text v0.41.0`, every nested run must resolve it from the empty cache ⇒ a live proxy fetch per run.
4. **Faithful reproduction (executor-shaped env, empty caches, temp dirs):** `go test ./internal/connector/nfccheck` under that env prints `go: downloading golang.org/x/text v0.41.0` and passes in ~7.5s for ONE package — the fetch is real, per-run, and network-bound. With my normal warm-cache shell the FULL nested suite is GREEN in 72.06s wall on the exact worktree bytes (fixtures 47.2s) — confirming the tree itself is healthy and the red is environmental.
5. **The red path:** a slow/failed fetch ⇒ inner `go test` exits non-zero quickly ⇒ `finalizeRun` (executor.go:279-292) maps exit≠expect to `Outcome: fail, FailingDetail: suite-exit-mismatch` ⇒ the conductor honestly records `dogfood-battery-green: observed-false` ⇒ the fixture's positive leg fails at :312 — the implementer's 95.02s red, ~505s under the repaired 600s backstop.
6. **The diagnosis gap:** the inner stdout/stderr IS captured (`cappedCapture`, executor.go:183-185) and then dropped — no verdict/record field carries it; the R-S12-shape question cannot even be checked from the record. (The R8-C2 discrimination principle, applied one level down.)

## §2 — The ask (master's choice; all candidate surfaces are conductor-tree, out of every slice fence)

Rule a determinism repair for the nested gate so a governed suite claim never depends on live network state. Shapes we can see (not chosen by the pair): **(a)** executor owner change — pre-seed the staged `GOMODCACHE` from the host cache (or pass it through read-only) + `GOPROXY=off` in the spawn env, making the nested run offline-deterministic while keeping hermetic caches for build artifacts; **(b)** vendor the module (`go mod vendor` at the repo root — self-contained tree, nested runs need no cache at all; a root `vendor/` dir is a shared-surface act with restack consequences for all three slices); **(c)** suite-script change under the same owner logic. COMPANION (either shape): surface the captured inner output tail into the `run-suite` failure detail (the §1.6 gap) so a future inner red self-identifies — the R8-C2 principle inside the check. CROSS-SLICE: the same nondeterminism sits under s13's battery today (their tree carries the same go.mod since restack-time) and under every future dependency any slice adds.

## §3 — State + what waits

Unchanged from the implementer's report: T9 HEAD `471532a…` committed clean; T9.5's exact R8 one-file diff uncommitted; T10 preserved uncommitted; T11–T15 paused; no battery run counted toward R8-C4's GREEN evidence (the failed isolated run is recorded; the two-full-battery obligation stands untouched). On the ruling I fold the bounded successor per the established lineage.

ACTIONS_GIT_REF: docs-workspace disk action — this escalation + one s14 INDEX row (in-fence); planner diagnosis was read-only (git worktree list/status, source reads, two nested-suite reproductions — one warm-shell, one executor-env in temp dirs, cleaned); no repo byte, no commit, no lock, no shared path touched by this seat.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** + the implementer's preserved uncommitted T9.5/T10 bytes on the code worktree; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: master rules the nested-gate determinism repair (shape a/b/c or other) + the inner-detail surfacing companion; the ruling hand-relays back to s14.planner for the bounded fold; the implementer resumes T9.5's three-run GREEN under a deterministic gate.
