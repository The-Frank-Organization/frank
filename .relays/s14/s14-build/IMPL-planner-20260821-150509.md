## IMPL — THE s14 SUCCESSOR IMPLEMENTATION DISPATCH over approved plan r8 (supersedes `IMPL-planner-20260821-142737.md` as the live dispatch; same delegated authority, its conditions evidenced: the commission grant + the approving `s14-build-plan-review-8` at verdict `approve` over exact r8 SHA-256 `5ea4a77a83874542e4dba78fde8e6f61e16d740e1b49c48b0933474219535911`): s14.implementer RESUMES from committed T9 HEAD `471532a151762d496186f2daee70b21fdd71062f` against the plan of record `frank/.relays/s14/s14-build/PLAN-planner-20260821-145914.md` (r8). Sequence: **T9.4** — the R9/R10 executor commit (`internal/executor/**` only; I1 GOPROXY=off; I2 pre-seed the fresh run-local GOMODCACHE from the go.sum-verified host cache; the R10 re-cut retention leg — a bounded PRIVATE diagnostic artifact under the resolved executor TempRoot, named by the already-computed manifest/run key, surviving workdir cleanup, containing the capped capture TAIL; the observe contract byte-untouched, the bare `suite-exit-mismatch` verdict token stands) → **T9.5** — the distinct path-exact R8 fixture commit → **the four-leg GREEN ladder** (offline zero-network resolution · forced-inner-red RETENTION leg: artifact exists with the capped tail AND the verdict stays byte-compatible with the closed token contract · THREE consecutive isolated repaired-fixture greens · TWO consecutive full batteries) → **T10 commits** → T11–T15 straight-through per exact r2 §2/§2a. **THE REVIEWER'S FAITHFUL-IMPLEMENTATION NOTES ARE DISPATCH LAW:** "capped tail" = the LAST bounded bytes (not the current cappedCapture head-on-truncation shape); the retained file uses private permissions; no unbounded or filename-embedded child text; deterministic per manifest key (coalesced/replayed execution never multiplies the diagnostic). The renewed STOP: any non-executor T9.4 byte, observe/token/schema/script byte, network-capable resolution, unverified cache source, unbounded/non-private diagnostic, combined repair commit, or failed evidence leg ⇒ stop and route up. The fold routes the T9.4 diff FYI to m-3.planner. Hand-relay transport; every byte and commit YOURS (CORRIGENDUM 1).

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s14-build
PARENT_DISPATCH_ID: s14-build-plan-review-8
RUN_ID: s14
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — implementation only; the operator MERGE-GATE stays terminal in the serialized s13 → s14 → s15 order; queue rows 7+9+11+12+13 ride the merge briefs
FILED_AT_LOCAL: 20260821-150509
IN_REPLY_TO: frank/.relays/s14/s14-build/PLAN-REVIEW-implementer-20260821-150245.md
FROM: s14.planner
TO: s14.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-8.implementer
SUBJECT: the successor implementation dispatch — T9.4 under the R10 re-cut (retention-not-enrichment), T9.5, the four-leg ladder, then T10 and T11–T15; all authority conditions evidenced in lineage

## §1 — The mechanical scope diff (restated as protocol requires; the two one-commit grants; NO observe row exists)

SCOPE_DIFF (each surface the locked plan touches -> vs the dispatch in-scope list):
- frank/cmd/frank-connector/** -> in (charter fence)
- frank/internal/connector/** (jcs · nfccheck · frame · stream · all connector packages) -> in (charter fence)
- frank/.relays/s14/** -> in (charter fence; relay lane)
- frank/docs/sprints/active/2026-08-20-s14-m8-connector/** -> in (R1 fence addition; may remain explicitly unused)
- frank/go.mod + frank/go.sum (the R4/R6 ruled bytes, committed at T1b) -> in (ruled; the R6 tripwire stands on every later tidy)
- frank/internal/executor/** (source + its tests; + the runtime diagnostic artifact under TempRoot) -> in FOR THE T9.4 COMMIT ONLY (the R9 grant as R10-narrowed; thereafter consume-only again)
- frank/test/fixtures/s8_exit_gate_test.go -> in FOR THE T9.5 COMMIT ONLY (the R8 grant, C1+C2 bytes exactly; thereafter consume-only again)
- frank/internal/observe/** -> OUT — expressly ZERO bytes (R10; the closed token firewall stands)
- everything else in the workspace -> OUT (a needed touch = escalation by construction, never an edit)
SCOPE_DIFF_RESULT: no-deviation

## §2 — Dispatch terms

The prior dispatch §2 terms (`IMPL-planner-20260821-142737.md`) carry verbatim, with r8 §1 governing T9.4's re-cut I3 leg, the leg-(ii) evidence re-cut, and the narrowed fence. Straight-through cadence, RED-first, full battery + vet + `-mod=readonly` green at every commit, the §2a matrix, the ten INV-CATALOG laws + the frozen oracle untouchable, H-12, the escalation rails, and the end-of-slice review sequence all stand.

## §3 — The token

DISPATCH IMPL

ACTIONS_GIT_REF: docs-workspace disk action — this dispatch + one s14 INDEX row (in-fence). No source byte by this seat, no commit by this seat (both repairs, T10, and every later commit are the implementer's), no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT: (fence-scoped: s14-owned dirt = frank/.relays/s14/** + the implementer's preserved uncommitted T9.5/T10 bytes on the code worktree; sibling-lane rows belong to s13/s15/master)
 M frank/.relays/s14/INDEX.md
?? frank/.relays/s14/s14-build/
Next requested action: s14.implementer executes T9.4 → T9.5 → the GREEN ladder → T10 → T11–T15; progress rides the lane per the cadence.
