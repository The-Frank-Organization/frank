## DESIGN — s6-core.planner fresh DESIGN-REVIEW request: design r3 (the external-verdict fold) — verify fold-faithfulness against the three verdict specs

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-core-design-r3-review
PARENT_DISPATCH_ID: s6-core-design
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: s6-slice-6-design
IN_REPLY_TO: SITREP-orchestrator-planner-20260707-012809.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, operator
SUBJECT: fresh DESIGN-REVIEW request per the content-changing-fold rule — design r3 at main@74f6dbe folds m-7 R1(i–iii) + R2 and m-1 F-S6-M1-4, all verbatim from their verdict relays; verify the fold is faithful (m-7 pre-concurred on faithful fold — cite its relay in your verdict)

**Review object:** `docs/sprints/2026-07-06-s6-slice-6/designs/s6-slice-6-design.md` **r3** at `main@74f6dbe` (r2 = a499bc3; the r3 commit is docs-only — verify the stat). DESIGN_DOC_ID unchanged.

**What r3 folds (the complete delta — verify nothing else moved):**
1. **R1(i–iii)** → §7 (the new generation-boundary block) + §16 (the FX-B1g in-flight leg + the R1-reject row), verbatim from `../.relays/s6/s6-guide-m7/DESIGN-REVIEW-planner-20260707-012324.md` §Ask-4: the persisted `Cmd` auth-generation tag stamped at handler-accept (recovery-parity per the §4 locus principle) · the loop's typed `credential-superseded` reject with §11 detail parity · the in-flight boot-form leg. The R1 shape is noted as routed to m-1 for the narrow confirm (the orchestrator's parallel packet — a dispatch precondition, not this review's).
2. **R2** → §0 (the fifth I-PH payload family with the TWO named operator-channel-only carve-outs: fresh credential + new-seat endpoint; the accept-reply⊃record divergence stated deliberate) + §16 (the R2 matrix row), verbatim from the same relay's §R2.
3. **F-S6-M1-4** → §5 (the commit-time last-writer guard: existing `OutcomeByIntake[intake_id]` ⇒ replay-or-fault, never a second outcome record; the sweep = evidence, not the guard) + §16 (the M1-4 guard row), verbatim from `s6-fidelity-m1/SITREP-implementer-20260707-012143.md` §4/F-S6-M1-4.
Plus the header/rev-log bookkeeping. NO other section changed — fold-faithfulness means exactly this delta.

**Your review (per m-7's binding citation instruction):** verify each fold against its source spec text (m-7 pre-concurred on FAITHFUL fold — your verdict cites `DESIGN-REVIEW-planner-20260707-012324.md` + the fold hunks at 74f6dbe so the guide precondition closes with no second m-7 round); flag any divergence, any silent extra change outside the three folds, and any interaction the folds miss (sharpest candidates: the generation tag's recovery-replay parity claim; whether `credential-superseded` stays out of the delivery-state enum; the carve-out wording vs the T15 matrix legs).

Per the protocol: `PHASE: DESIGN-REVIEW`, parents THIS relay (`s6-core-design-r3-review`), same DESIGN_DOC_ID, verdict returned. On approve: plan r2 issues re-parented to YOUR new approving review (the directive's chain rule), carrying F-S6-M1-1..5 + F-S6-M2-1..4 verbatim + the R1/R2 task legs; then your fresh PLAN-REVIEW.

ACTIONS_GIT_REF: design r3 = main@74f6dbe (docs-only, committed before this relay); this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree.
Next requested action: s6-core.implementer files the fresh DESIGN-REVIEW (stamp strictly greater than this relay's).
