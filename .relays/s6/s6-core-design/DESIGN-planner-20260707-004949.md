## DESIGN — s6-core.planner DESIGN-REVIEW request: the s6 build design r2 (grill folded) to the pair Implementer for the adversarial grill

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-core-design-r2-review
PARENT_DISPATCH_ID: s6-core-design
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the grill's operator gate is exercised and folded (§18)
DESIGN_DOC_ID: s6-slice-6-design
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, operator
SUBJECT: DESIGN-REVIEW request — s6-slice-6-design r2 at main@a499bc3 (GRILL_LOCK s6-grill-s6-core folded as §18; six operator rows resolved); verdict {approve | must-revise | reject-narrow | human-decision-required} per the protocol, parented to THIS relay, same DESIGN_DOC_ID

**Review object:** `docs/sprints/2026-07-06-s6-slice-6/designs/s6-slice-6-design.md` **r2** at `main@a499bc3` (r1 = e13a6f4; both commits docs-only — verify the stat yourself). DESIGN_DOC_ID `s6-slice-6-design`.

**What r2 is:** the build design/decomposition against the co-signed amendment set — module map (§1), per-cluster design §2–§13, the registry pass §14, seam boundary contracts §15, the complete fixture table §16, PLAN ordering §17, the folded GRILL_LOCK §18 (`s6-grill-s6-core` — six operator rows: re-mint = binding replacement · F11 replay = sequence re-drive two-legs-one-claim · lock = flock/phase −1 · counter = segment header line · roster = the B-1.3 seven fields · the [VP-W3] seven-transport-rows/boot-fields-separate reading), and the ten-constraint compliance map §19.

**Review request (the standing adversarial grill — your D5 recast):** attack the decomposition, not the locked set. Specifically invited:
1. **Spec fidelity:** any §2–§14 mechanism that deviates from, weakens, or silently extends a locked contract (the set rules; deviations escalate, never fold silently).
2. **The dispatch's ten constraints** (§19 map): any constraint that does NOT actually land where claimed — silence fails the review both ways.
3. **Seam halves (§15):** any half re-owned instead of consumed; the re-mint seam and the waiver-row m-2 expressibility flag (§8) get your sharpest read.
4. **Fixture completeness (§16):** any set/constituent obligation with no row; any row whose asserts overstate (the F11 two-legs-one-claim wording; "un-bounceable"; the lock's D5 line).
5. **Ordering (§17):** any dependency inversion (D-2 before the rejection-detail consumers; registry pass placement; B-1 needing §7 generations).
6. **The grill fence:** §18 must not re-open any master-grilled or pair-locked contract — check the resolved-row list against the set.
7. **Correctness holes of the kind six fresh teams keep finding** — the stamp-locus-in-shared-handler claim (§4: recovery parity), the serve-time projection filter (§6), the coalescing reply-path (§5), the force-close-on-remint (§7).

Per the protocol: your review relay is `PHASE: DESIGN-REVIEW`, read-only, parents to THIS relay (`s6-core-design-r2-review`), carries `DESIGN_DOC_ID: s6-slice-6-design`, returns `DESIGN_REVIEW_VERDICT`. On approve I file the completion SITREP to the orchestrator (carrying your relay id + the GRILL_LOCK id) and HOLD for PROCEED-TO-PLAN.

ACTIONS_GIT_REF: design doc commits main@e13a6f4 (r1) + main@a499bc3 (r2), both docs-only; this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree.
Next requested action: s6-core.implementer files the DESIGN-REVIEW under this DISPATCH_ID's thread (stamp strictly greater than this relay's).
