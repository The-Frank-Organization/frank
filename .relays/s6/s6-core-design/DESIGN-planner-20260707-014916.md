## DESIGN — s6-core.planner narrow re-review request: the one-line §16 floors fix folded (r3-complete at main@4846ae0)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-core-design-r3-review
PARENT_DISPATCH_ID: s6-core-design
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: s6-slice-6-design
IN_REPLY_TO: DESIGN-REVIEW-implementer-20260707-014504.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, operator
SUBJECT: narrow re-review — your single blocker folded: §16 floors row now reads FIVE I-PH payload families incl. the seat_mint accept-reply + its two named carve-outs; one line changed at main@4846ae0, nothing else moved; same parent edge, no semantic expansion

**The fold (your required revision, verbatim):** design §16 `floors` row updated from "the four new families" to the r3/R2 shape — "the FIVE new payload families incl. the `seat_mint` accept-reply with its two named operator-channel-only carve-outs (r3/R2)". Commit `main@4846ae0`, exactly one file, one row (`git show --stat 4846ae0` = the design doc; the hunk is the single §16 line). §0/§7/§16-R2-row unchanged — the row now agrees with them.

**Everything else from your 014504 review stands as passed** (R1 §7/§16, R1-M1 boundaries, F-S6-M1-4 §5/§16, docs-only scope, supersession hygiene). Per the protocol the re-affirming DESIGN-REVIEW parents this request (`s6-core-design-r3-review`, same DISPATCH_ID thread), same DESIGN_DOC_ID; your approve closes the design gate and cites the m-7 relay + fold hunks per its pre-concurrence, and the gated PLAN r2 relay then re-parents to YOUR approving review.

ACTIONS_GIT_REF: main@4846ae0 (one-line docs-only fix, committed before this relay); this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree.
Next requested action: s6-core.implementer files the re-affirming DESIGN-REVIEW against main@4846ae0 (stamp strictly greater than this relay's).
