## DESIGN — s6-core.planner fresh DESIGN-REVIEW request r3-COMPLETE (SUPERSEDES `DESIGN-planner-20260707-013417.md`, unconsumed): the fold now includes R1-M1-1..4 per the m-1 confirm + orchestrator supplement

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-core-design-r3-review
PARENT_DISPATCH_ID: s6-core-design
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: s6-slice-6-design
IN_REPLY_TO: SITREP-orchestrator-planner-20260707-013623.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, operator
SUBJECT: fresh DESIGN-REVIEW request, r3-complete at main@b6579ef — the 013417 request superseded UNCONSUMED (the m-1 R1-confirm + supplement landed mid-pass; R1-M1-1..4 folded into the same r3, no extra round per the directive); verify fold-faithfulness against ALL FOUR verdict specs

**Supersession note (the s4 convention — supersede in place, never rename/delete):** `DESIGN-planner-20260707-013417.md` was filed before the m-1 R1-confirm (`s6-fidelity-m1/SITREP-implementer-20260707-013244.md`) and the orchestrator supplement (`…-013623.md`) landed; no review consumed it. This request replaces it; the review object advances from 74f6dbe to **b6579ef**.

**Review object:** `docs/sprints/2026-07-06-s6-slice-6/designs/s6-slice-6-design.md` **r3-complete at `main@b6579ef`** (fold commits: 74f6dbe + b6579ef, both docs-only — verify the stats; b6579ef also carries the plan-r2 pre-fold, reviewed separately at the PLAN gate, not here).

**The complete r3 delta to verify (fold-faithfulness = exactly this, nothing else moved):**
1. **m-7 R1(i–iii)** → §7 + §16 (`…s6-guide-m7/DESIGN-REVIEW-planner-20260707-012324.md` §Ask-4): the persisted `Cmd` auth-generation tag · the `credential-superseded` typed reject with §11 parity · the FX-B1g in-flight leg.
2. **m-1 R1-M1-1..4** → the same §7 block (`SITREP-implementer-20260707-013244.md` §Required-carry): conductor-stamped-at-authenticated-accept, pivot-ref-or-genesis-sentinel, never-credential-material, intake-provenance-never-derivation-input · typed TERMINAL disposal before any accepted append + stale non-submit = credential invalidation NOT lifecycle gating (current-credential seats keep read/project) · the in-flight leg's class = `credential-superseded` + the never-a-marker negative assertion · the six R1-M1 route-backs verbatim.
3. **m-7 R2** → §0 + §16: the fifth I-PH family, two named operator-channel-only carve-outs, the accept-reply⊃record divergence stated deliberate.
4. **m-1 F-S6-M1-4** → §5 + §16: the commit-time last-writer guard (replay-or-fault, never a second outcome record; the sweep = evidence, not the guard).

**Your review:** verify each fold against its source spec text; your verdict cites the m-7 relay + the fold hunks (its pre-concurrence closes the guide gate on faithful fold) and the m-1 confirm relay (its boundaries close the m-1 gate). Sharpest candidates: `credential-superseded` stays a rejection class, never a delivery state; the tag's absence from accepted-record content as designed (not just as fixtured); the non-submit refusal wording vs the never-lifecycle-gate rule; the carve-out scoping vs the §16 matrix rows.

Per the protocol: `PHASE: DESIGN-REVIEW`, parents THIS relay (`s6-core-design-r3-review`), same DESIGN_DOC_ID, verdict returned. On approve: the gated PLAN r2 relay issues re-parented to YOUR new approving review, locking plan r2 (already pre-folded at b6579ef with all thirteen external rows); then your fresh PLAN-REVIEW = the plan gate; then {SCOPE_DIFF all-in · no trigger/collision/amendment/OUT} ⇒ the delegated dispatch. No external round-trips remain (the supplement's closed-form gate state).

ACTIONS_GIT_REF: design r3-complete + plan r2 pre-fold = main@74f6dbe + main@b6579ef (docs-only, committed before this relay); this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree.
Next requested action: s6-core.implementer files the fresh DESIGN-REVIEW against b6579ef (stamp strictly greater than this relay's).
