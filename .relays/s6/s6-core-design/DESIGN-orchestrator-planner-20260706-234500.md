## DESIGN — s6-core build design/decomposition against the LOCKED amendment set (ten reconciled-audit constraints; two ratified decomposition items; fixture table with explicit obligation mapping)

ROLE: Orchestrator Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-core-design
PARENT_DISPATCH_ID: s6-core-audit
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no — the hard-to-reverse decisions were operator-grilled at master (`GRILL-LOCK-parenting-fork-2026-07-06`); the s6-dispatch stamps GRILL_REQUIRED: no ("completed at design"); this DESIGN is build-decomposition against a locked spec, so the s2 trigger profile (unsettled hard-to-reverse choices) does not bind — any decision found in-design that WOULD meet a grill trigger escalates to me instead
FROM: s6.orchestrator-planner
TO: s6-core.planner
CC: s6-core.implementer, s6.orchestrator-reviewer, operator
SUBJECT: DESIGN dispatch — produce the s6 build design (module design + fixture table + decomposition) against the co-signed set; DESIGN-REVIEW request goes TO s6-core.implementer per the protocol; ten binding constraints from the reconciled audits; the design PROPOSES m-1/m-2-surface shapes for fidelity review at completion, never locks them alone

**What this is.** The s6 DESIGN phase: `s6-core.planner` authors the build design against the LOCKED spec (the set r3 + constituents — design-of-record, read-only). The design does NOT re-decide anything the set locks; it decomposes HOW the locked contracts land in this codebase: module-level design, the complete fixture table, the ordering/decomposition the PLAN will lock, and explicit proposals on the surfaces the fidelity seats must review. Superpowers brainstorming owns the how; the lineage chain owns the review: your design doc (DESIGN_DOC_ID `s6-slice-6-design`, in `docs/sprints/2026-07-06-s6-slice-6/designs/`) → your DESIGN-REVIEW request **TO s6-core.implementer** (me/operator CC only) → its verdict → your completion SITREP to me → PROCEED-TO-PLAN.

**Reconciliation of your paired audits (both read at my seat; full agreement, zero contradictions, zero spec-gap escalations):** your two Named items are **RATIFIED as design inputs** (neither reopens a lock — my concurring read is in the ledger entry); the implementer's D-1/B-1-seed/threat-point depth is adopted alongside your sharper F9/§B/F3/F13 mechanics (your `writer.go`/`tables.ContentHash` cites spot-verified at my seat). Read the sibling audit now — the independence fence lifted at reconciliation; both audits are design inputs.

### The ten binding constraints (from the reconciled audits; each lands verbatim or the design says why not — silence fails the review)

1. **Promote, never rebuild:** `ParseTyped` `address_list` is the codec's decode seed (§2.1 unifies, not rewrites); per-field `DigestExempt` promotes to the `conductor_supplied_volatile` class; `tables.OutcomeByIntake` = the A-2 replay lookup; dormant `tables.ContentHash` is WIRED, not duplicated; `writer.hashes` = the in-flight coalescing seed; the channel per-credential `active` index = `bound_now`; the redacted-read view = the roster/audit-scope output pattern; the `resolves_gate`/`disposes_owed` idiom = the `retracts` effective-state template; the S1/S2 crash harness + crashpoint registry + S3 replay harness + the s5 env-gated archive leg = the fixture machinery; `seat.Stamp` = the PARENT stamp-locus idiom; `bounce.Format` stays the single formatter (D-2's gap is carriage).
2. **F9 lands at the `writer.go` grain:** durable id counter per A-2's own spec (segment-header high-water mark; recovery = `max(all segments)+1`) — the **GC-drained-segments + restart id-reuse leg joins FX-A2c**; outcome replay via the wired content-hash→outcome lookup; in-flight duplicate coalescing; the dead `journal.Append` `len()+1` id path is deleted or unified — no second id judge survives.
3. **§B gates the REBUILD path:** `RebuildProjections` currently re-applies canonical intents for ALL records regardless of delivery state — the polluted-archive fixture must prove filtering on a rebuilt store (m-1 §E carry-forward verbatim), not only fresh commits; turn context/`WokenOn` derives from accepted-state, never the raw mailbox tail.
4. **F13 fixtures name the live still-bouncing tokens:** `disposition` + `diagnostics` (the `gate_resolution` case landed post-`67ee23e` — the class, not the instance, is the target); `validateRecordKind` reduces to layer 3 after `reg.Validate` layers 1–2; the `genesis` seat-scope negative rides per m-2 §7.3.
5. **D-2 lands engine-side** (the `Outcome` carries the same `bounce.Format` detail for EVERY rejection class, byte-equal to the recorded Body) **and the shim's `re-render` pattern-match hack retires with it** — no per-class branches survive anywhere, shim included.
6. **Re-mint choreography decomposed explicitly (ratified item 1):** credential replacement as `seat_mint` derived work; old-credential rejection at auth; binding-table update semantics — all within m-1 §F/§F.1's locked generation semantics; PROPOSED in the design and routed to the m-1 fidelity packet (the §13.3 credential-lifecycle carry; the S1 shape-(b) lock's own wording anticipated the remint op). FX-B1g is the executable proof.
7. **F11 replay = two legs, one claim (ratified item 2):** the archived-traffic re-drive proves the recorded pattern completes without livelock; the GRILL_LOCK concurrent-accept fixture proves the race class; the design states both, overstating neither; the traffic re-drive COMPOSES with the read-integrity zeroloss leg, never duplicates it.
8. **The fixture table maps every obligation explicitly:** all 18 FX rows + the GRILL_LOCK three + m-1 §E families + m-2 §7/§11.5 — AND the m-1 §F.6 activation fixtures stated as landing via FX-B1c/f/g so §F.6 cannot fall between gate families. Red-first is the build discipline the PLAN inherits.
9. **Claim pins beside every relevant surface:** "at-least-once intake, exactly-once EFFECT" (never unqualified exactly-once); "un-bounceable boot" = structural absence of the digest/parent/divergence classes only (never immunity from auth/admission rejection); activation = liveness bookkeeping, never identity-strength; hint fallback ≠ validation (intent + outcome + flag is the honest triple); the lock = confusion-resistant ops, D5 same-uid residual verbatim; `charter_loaded` self_reported. **No-perf fence inside edited files:** the known hot-path rescans (per-submit `tables.Build`; `gate.Complete` scans) sit in files this slice edits and are NOT touched while we're in there.
10. **Enum/verb/I-PH threat points designed against by name:** roster + audit views = `project` payload parameters (the violation shape = a new `ToolSet` entry); `boot-required` = a rejection class, never a delivery state; the A-4 loser refusal = a typed process exit, never a delivery state; `held` stays off seat default projections; I-PH fixture classes over the four new payload families (roster rows · `boot-required` per-field detail · lock-refusal diagnostic · hint flags) extending the existing grep-matrix pattern; the enum-floor guard (`sweep_test.go`) extends over every new output surface.

### Seams (state each half; consume, never re-own)
Parenting three-way (m-1 semantics · m-7 stamp locus/turn context · m-2 un-render + hint rows) · F14 dual-cite ([VP-W2]: m-1 invariant / m-7 choreography) · F12↔F17 (m-1 record class / m-2 row typings) · Envelope.To + digest (m-2 projection truth / m-7 stamp + A-1) · boot (m-1 §F owns activation; m-7 B-1 transient classification; m-2 B-2 form). The design PROPOSES the m-1-surface shapes (re-mint, lock content, waiver rows, activation derivation host) and m-2-surface shapes (the seven rows, `seat_mint`/`waiver_retraction` row shapes, boot fields, codec export) — fidelity packets route at design-completion per the s3/s4 pattern; nothing store/registry-shaped locks on the pair's approval alone.

**OUT (restated; name-and-escalate):** Step-2 observe · Step-3 routing execution · engine perf work of ANY kind · new seat verbs · federation · dogfood-in-slice · any governance-doc edit · any locked-contract change (amendment path only).

**Watchpoints riding (restate in the doc):** [VP-W2] FX-B1g in the exit gate · [VP-W3] EXACTLY seven registry rows, no activation-marker row, activation DERIVED-ONLY · fallback-never-bounce with the empirical why carried into the fixtures · the step-exit operator legs never simulated.

**Deliverable:** design doc at `docs/sprints/2026-07-06-s6-slice-6/designs/s6-slice-6-design.md` (DESIGN_DOC_ID `s6-slice-6-design`), tracked, committed docs-only; the review chain as above; completion SITREP to me carrying the approving review's id. Boundary contract: the seam table above IS the contract surface; state `Writes/Reads/Contract/Proof` per seam in the doc. Operator-judgment items: expected none — flag any found.

ACTIONS_GIT_REF: none — design dispatch authored; no code/tracked-doc edit by this relay (the reconciliation ledger entry is a separate tracked docs-only edit committed in its own right).
FINAL_GIT_STATUS_SHORT: docs/sprints/2026-07-06-s6-slice-6/RECONCILE.md modified (the paired-audit reconciliation entry, this session, committed with this dispatch's issue); no code paths touched.
Next requested action: operator hand-relays this dispatch to s6-core.planner; the pair runs the design + review chain; completion SITREP returns to me; PROCEED-TO-PLAN follows.
