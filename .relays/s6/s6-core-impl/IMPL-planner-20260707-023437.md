## IMPL — s6-core.planner delegated implementation dispatch (re-issued on the corrected chain): s6-slice-6-plan r2 — all standing F2 conditions verified with evidence; SCOPE_DIFF all-in

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s6-core-impl
PARENT_DISPATCH_ID: s6-core-plan-review-implementer-r2-approve-v2
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no for this dispatch — the downstream human gates stand: live-seat designation, the step-exit operator legs, s6-close/merge (merge never implied by green fixtures)
DESIGN_LOCK_ID: s6-slice-6-design
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s6-grill-s6-core
PLAN_LOCK_ID: s6-slice-6-plan
IN_REPLY_TO: PLAN-REVIEW-implementer-20260707-022237.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, s6.orchestrator-reviewer, operator
SUBJECT: delegated implementation dispatch — execute s6-slice-6-plan r2 (main@2903d84) task-by-task on branch `s6-transport-impl` off main@2903d84, under Superpowers executing-plans; red-first per task; the thirteen external rows + three route-back lists bind; any deviation stops and escalates. Re-issue note: supersedes the VOIDED `IMPL-planner-20260707-021548.md` (token neutralized pre-relay at the lint gate); the chain now parents your lint-readable re-affirming approve

**The standing F2 conditions, verified with evidence before this dispatch:**
1. **Implementer PLAN-REVIEW approve:** `s6-core-plan-review-implementer-r2-approve-v2` (20260707-022237, `PLAN_REVIEW_VERDICT: approve` + bare `VERDICT: approve` — the lint-readable re-affirmation of the substantive 021255 approve), parenting my gated PLAN `s6-core-plan-lock-r2` (015313 + the narrow revision 020443), which parents the approving DESIGN-REVIEW `s6-core-design-r3-review-implementer` (015138) — the full lineage chain resolves in root-mode lint (verified on THIS relay before handoff).
2. **The three external verdicts on record in `.relays/s6/`:** m-1 approve-conditional (`s6-fidelity-m1/SITREP-implementer-20260707-012143.md`) + m-1 R1-confirm (`…-013244.md`) · m-2 approve-conditional (`s6-fidelity-m2/SITREP-implementer-20260707-012246.md`) · m-7 must-revise pre-concurred, discharged by the approving r3 DESIGN-REVIEW citing its relay + fold hunks (per `s6-core-design/SITREP-orchestrator-planner-20260707-013623.md`: gate state closed-form, no external round-trips remain). All thirteen condition rows + all three route-back lists ride the plan's Global constraints verbatim.
3. **SCOPE_DIFF: all-in** (below) vs the locked plan's file list (the File map + the task Files lines the reviews approved).
4. **No hard trigger beyond the chartered, externally-gated scope** (the slice's trust-critical surfaces are exactly what the three domain gates + the operator step-exit govern; ceremony stays the dispatched medium tier — no downgrade proposed, so no downgrade record is owed) · **no cross-slice collision** (s6 is the only active build; the frozen s2 store and the live team store are untouched by every task) · **no locked-contract amendment** (the set rules; every fold arrived via its owner's verdict) · **no OUT touch** (the plan's verbatim fence; name-and-escalate stands).

SCOPE_DIFF:
- internal/fieldspec/{canonical.go, render.go, validate.go, fieldspec.go, registry.json, *_test.go} -> in
- internal/lineage/{lineage.go, lineage_test.go} -> in
- internal/engine/{submit.go, loop.go, parent_stamp.go (new), admission.go (new), *_test.go} -> in
- internal/intake/{journal.go, writer.go, *_test.go} -> in
- internal/store/{store.go, projections.go, lock.go (new), lock_test.go (new), *_test.go} -> in
- internal/recover/{recover.go, recover_test.go} -> in
- internal/seat/{binding.go, binding_test.go} -> in
- internal/channel/{server.go, *_test.go} -> in
- internal/tables/{tables.go, *_test.go} -> in
- internal/obligation/ (T11 derived-work wiring ONLY, per the plan's T11 Files line — the File-map summary omits the row; the task line is the approved authority; gate.Complete internals stay untouched per the no-perf fence) -> in
- cmd/frank/main.go -> in
- cmd/frank-mcp/{mcp.go, errors.go, *_test.go} -> in
- test/fixtures/ new s6_*.go files (codec/parenting/projection/intake/waiver/lock/mint/lifecycle/iph) -> in
- test/fixtures/{f11_test.go, applicability_map.go, sweep_test.go} -> in
- test/fixtures/ + internal *_test.go legacy PARENT-header updates (T6 ONLY: move to parent_hint or drop; every touched test itemized in the task log; no assertion weakening) -> in
- test/replay/dogfood/dogfood_test.go (new) -> in
- docs/ops.md -> in
- cmd/frank-mcp/README.md -> in
- docs/sprints/2026-07-06-s6-slice-6/results/step-exit-procedure.md (new) -> in
SCOPE_DIFF_RESULT: all-in

Everything not rowed above is OUT: root README (no falsified line at plan time; T15.5 re-check ⇒ fence ASK on surprise, never a silent edit), the frozen `$HOME/frank-s2-store`, the live team store, every governance doc, and the OUT-fence list whole.

**Execution contract:** branch `s6-transport-impl` off `main@2903d84`; execute under Superpowers `executing-plans`, Tasks 1–16 in plan order; red-first per exit-gate fixture with evidence in the task log; battery (`go test -count=1 ./...` + `go vet ./...`) green before every commit, `-race` where the task names it; commit-per-task `s6:` prefix. **Stop-and-escalate (to me, thence the orchestrator) on:** any route-back trigger from any of the three lists; any file outside the SCOPE_DIFF rows (no entailed exception — relay and wait); any locked-contract or design-of-record conflict discovered mid-task; any OUT-fence pressure. Blocker relays and inline questions come to me; I do not direct implementation.

**After the tasks:** your completion report under this DISPATCH_ID (branch@sha, per-task commit list, red-first evidence pointers, battery output) → the adversarial panel + REVIEW-FOLD loop (pre-filed FOLD_SCOPE per the protocol) → the exit-gate pass → the gate report. Merge is a separate operator gate; nothing here implies it.

DISPATCH IMPL

ACTIONS_GIT_REF: none — dispatch relay only; no code/tracked-doc edit by this relay (the plan/design commits are recorded in their own right; this relay + INDEX row live under gitignored `.relays/s6/`).
FINAL_GIT_STATUS_SHORT: none — clean tree at main@2903d84.
Next requested action: s6-core.implementer acknowledges take-up under this DISPATCH_ID and begins Task 1 on the branch.
