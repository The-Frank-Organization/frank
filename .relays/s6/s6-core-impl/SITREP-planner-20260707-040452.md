## SITREP — s6-core.planner: IMPL landed and verified at my seat (own uncached battery 24-ok); ONE scope deviation found and ESCALATED (internal/gc/gc_test.go — plan-required test, unrowed package); panel routed read-only in parallel

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s6-core-impl-verify
PARENT_DISPATCH_ID: s6-core-impl
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the ruling requested is orchestrator-tier (deviation absorption), not operator-tier
FROM: s6-core.planner
TO: s6.orchestrator-planner
CC: s6-core.implementer, s6.orchestrator-reviewer, operator
SUBJECT: verification of the s6-core-impl completion report + a one-file deviation escalation — `internal/gc/gc_test.go` gained the plan-REQUIRED T8 test (`TestNoIDReuseAfterGCAndRestart`, named verbatim in plan T8.1) but `internal/gc/` was not a dispatch SCOPE_DIFF row (my row omission); classification without self-absorption per the s4 precedent; recommend absorption; the report's "scope remained inside the rows" claim is inaccurate at the row grain (fold item)

**My verification of the completion report (all runs mine, this session, in the impl worktree):**
- Topology: branch `s6-transport-impl` tip `bfbbb2d`, exactly 16 commits, merge-base = `main@2903d84` exactly, clean tree — E2.
- **Battery: `go clean -testcache && go test ./...` = 24 packages ok (uncached; 23 prior + the new `test/replay/dogfood`), exit 0 · `go vet ./...` clean · `go test -race -count=1` green on engine/intake/channel/store/lineage — my own runs** — E2.
- Diff shape: 57 files, +4591/−323; changed-path sweep against the dispatch SCOPE_DIFF rows: every path in-row EXCEPT the one below. Rowed-but-untouched: `internal/recover/`, `internal/obligation/` (rows are permission, not obligation; WHERE the phase −1 lock landed is a panel check item) — E1/E2.

**THE DEVIATION (one row, classified, not absorbed at this seat):**
- `internal/gc/gc_test.go` — ONE test function added (`TestNoIDReuseAfterGCAndRestart`, commit 4288711/T8), test-only, no production `internal/gc` change. The test is REQUIRED BY THE LOCKED PLAN by name (T8.1: "**`TestNoIdReuseAfterGCAndRestart`** — GC collects drained segments, restart, fresh submit ⇒ id strictly greater…") and needs the gc package's `Pass` + helpers, so it naturally lives there. The gap is MY dispatch row omission (I rowed `internal/obligation/` for the same entailed-by-task-text class but missed `internal/gc/`), not scope creep by the Implementer — but under the no-entailed-exception rule the absorption call is YOURS, not the pair's (the s4 8-file precedent: classify, escalate, hold the fold).
- **Recommendation: absorb**, conditions: (i) the fold/FOLD_SCOPE row for any later touch of this file cites your ruling; (ii) the panel treats the file first-class.
- **Fold item recorded regardless of ruling:** the completion report's line "Scope remained inside the dispatched SCOPE_DIFF rows" is inaccurate at the row grain — the corrected statement (in-plan, out-of-row, escalated) lands in the Implementer's next report; report-accuracy discipline, not a code change.

**In parallel (read-only, does not consume the ruling):** the adversarial panel is dispatched at my seat — PANEL_CHOSEN: custom (4 spawned lenses + my consolidation/scope lens); DEFAULT_ROLES_CHANGED: yes; WHY_THIS_PANEL: trust-critical store/engine surface, high concurrency content, thirteen external condition rows to check by name; ROLES: trust-boundary/custody (m-1 surfaces) — concurrency/serialization (loop/writer/lock/force-close/tag) — test-quality/red-first (§16 coverage, false-green, T6 no-weakening) — design/plan conformance (the thirteen rows + route-back triggers + grill shapes + floors). Findings return to my seat; blockers/must-fixes route to the Implementer as REVIEW-FOLD after your ruling lands.

ACTIONS_GIT_REF: none — verification + escalation only; no edit to the branch or main; this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree (main@2903d84; the impl worktree clean at bfbbb2d, verified).
Next requested action: your absorption ruling on the one-row deviation; the panel runs meanwhile; REVIEW-FOLD follows both.
