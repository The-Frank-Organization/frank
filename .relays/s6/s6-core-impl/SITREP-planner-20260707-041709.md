## SITREP — s6-core.planner panel consolidation: 4 lenses returned (2× must-fix converge on ONE substantive gap — the re-mint crash window); its FIX SHAPE crosses m-1's locked surfaces ⇒ routed to you for the m-1 (+m-7) consult; the REVIEW-FOLD for everything else files in parallel

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s6-core-impl-panel
PARENT_DISPATCH_ID: s6-core-impl
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the consult requested is domain-owner-tier (m-1, possibly m-7), routed through you per the standing pattern
FROM: s6-core.planner
TO: s6.orchestrator-planner
CC: s6-core.implementer, s6.orchestrator-reviewer, operator
SUBJECT: panel verdicts {trust must-fix · concurrency approve · tests must-fix · design must-fix}; the ONE cross-cutting substantive finding (re-mint crash window: recovery cannot distinguish rotated-vs-pending for an existing seat, so an interrupted re-mint leaves the OLD credential resolving) needs an m-1 ruling on the fix shape BEFORE its fold edit — three options framed with my recommendation; all other findings fold pair-side now

**PANEL record:** PANEL_CHOSEN custom (4 spawned read-only lenses + my consolidation); DEFAULT_ROLES_CHANGED yes; WHY: trust-critical store/engine surface, 13 named external condition rows, high concurrency content. Verdicts: trust-boundary **must-fix** · concurrency **approve** · test-quality **must-fix** · design-conformance **must-fix**. Broad conformance INDEPENDENTLY confirmed by the lenses: grill-locked shapes verbatim in code (flock phase −1 at main.go:87 before store.Open/recovery; the exact 7+2+1+2 registry diff with NO activation-marker row and ORCH_REVIEW_WAIVER removed; roster seven fields; segment-header counter with legacy tolerance); all deleted paths gone (comma-splits, ActiveLineageCandidates, silent-drop, readback hack, len+1 ids); route-back boundary sweep PASS (no persisted marker/generation column/credential history; AuthGeneration json:"-"; no new verb or delivery state; Envelope.To never overrides a present decoded header); T6 legacy updates mechanical, zero assertion weakening; `-race` + real-child kill-9 legs green.

**THE FINDING NEEDING YOUR ROUTING (trust-1 ≡ design-1, independently converged; verified at my seat by my own read of `cmd/frank/main.go:535-554`):**
An accepted RE-mint that crashes between the pivot commit and the `MintOrReplace` derived step is never completed at recovery: `completeMissingSeatMintBindings` skips any seat with `CredentialsFor != 0` — which is every existing seat — because the binding table (correctly, per F-S6-M1-1) carries no state distinguishing "rotation applied" from "rotation pending." Consequence: post-restart the OLD credential still Resolves (project/read live; submits bounded to `credential-superseded` by the generation supersede — the compensating control). Fresh-mint recovery is correct and fixtured. This contradicts the design's unqualified "old credential dies at Resolve" + "crash ⇒ recovery completes idempotently" for the re-mint case, and sits exactly on m-1's F-S6-M1-1 route-back line ("old re-mint credentials usable after derived-work completion") + the §13.3 credential-lifecycle carry.

**Fix-shape options for the m-1 packet (my framing; the ruling is theirs):**
- **(A) — RECOMMENDED: binding-row completion provenance.** The binding row gains ONE field: the `seat_mint` pivot relay-id the current credential realizes. Recovery completes iff `binding.pivot_ref ≠ the seat's latest accepted pivot`. Idempotent, derivation-consistent (a completion KEY per the locked S2 derived-work idiom "keyed by the record" — not a generation counter, not credential history, not multi-credential). BUT it is a binding-table SHAPE change — the standing hard-stop; m-1 must rule.
- **(B) honest residual, no shape change:** document that re-mint rotation is not crash-atomic; the old credential survives such a crash bounded to read-only by the generation supersede; remedy = operator re-mints again. Weakest: leaves a live-forever read-only zombie credential; the design/ops claims must be re-worded.
- **(D) rotate-before-commit:** fail-closed direction (a crash yields a dead-unknown credential, remedy = re-mint) with NO shape change — but it inverts the locked pivot-first derived-work discipline (m-7's mechanism + m-1's §F.1 generation-boundary keyed to the COMMITTED pivot), so it needs both owners' eyes if considered at all.
Whatever lands: the red-first pinning fixture (SIGKILL between pivot commit and binding replacement ⇒ old credential dead / rotation completed at recovery) is REQUIRED with it (both lenses demand it; FX-A3b covers fresh mint only).

**Folding pair-side NOW (the parallel REVIEW-FOLD, filed with this relay):** the M1-4 guard as a real commit-boundary mechanism covering ALL append paths incl. `faultOutcome` + the missing named `TestCommitGuardBlocksSecondOutcome` (concurrency-1 upgraded at my seat: M1-4's text says ANY outcome append; tests-3 confirms the named test absent) · boot-admission routing scoped to pre-active only (trust-2 ≡ design-2, upgraded at my seat: an ACTIVE seat submitting boot fields + any other header is falsely rejected `non-boot-before-active`, contradicting locked §12 — a false-reject channel, the F10 lesson class) · the two vacuous sweep floors rewritten over real bytes (tests-1/2 — false-greens proven by the lens's own runs) · the missing named negatives + under-tested legs (tests-4/5: FX-B1e multi-field smuggle; FX-B1f exactly-once unit legs — `generation.go` has NO test file; `TestTagNeverInAcceptedRecords`; `TestStaleNonSubmitRefusalIsNotLifecycleGating`) · the FX-A4b takeover side (tests-6: kill-9-then-TAKEOVER + auditable record + alias-path legs — only the refusal side exists) · the report-accuracy correction (your ruling condition 4). Optionals at Implementer discretion listed in the fold relay.

ACTIONS_GIT_REF: none — consolidation + routing only; no branch/main edit; this relay + the fold relay + INDEX rows under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree (main@2903d84; impl worktree clean at bfbbb2d).
Next requested action: route the re-mint fix-shape packet to m-1 (and m-7 if option D stays on the table); the pair folds the parallel set meanwhile; the re-mint edit waits on the ruling.
