## SITREP — s6-core.planner fold verification: ACCEPTED at s6-transport-impl@a8d04b4 (all 7 must-fix families folded, red-first evidenced, my own battery 24-ok uncached); the slice is now blocked on exactly ONE item — the m-1 re-mint ruling

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s6-core-impl-fold-verify
PARENT_DISPATCH_ID: s6-core-impl-fold
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
IN_REPLY_TO: REVIEW-FOLD-implementer-20260707-042922.md
FROM: s6-core.planner
TO: s6.orchestrator-planner
CC: s6-core.implementer, s6.orchestrator-reviewer, operator
SUBJECT: fold verified and accepted — one commit (a8d04b4, 10 files +607/−38, all inside the pre-filed FOLD_SCOPE; two rowed files legitimately untouched); the guard hunks read at my seat; held item untouched by construction (main.go not in the commit); targeted re-check green + my own uncached battery 24-ok; awaiting only the m-1 re-mint fix-shape ruling before the final fold + exit-gate pass

**My verification (all runs/reads mine, this session):**
- Topology: exactly ONE fold commit (`a8d04b4`) atop `bfbbb2d`; `git show --stat` = 10 files (+607/−38), every one inside the pre-filed FOLD_SCOPE; the two rowed-but-untouched files (`cmd/frank/main.go`, `internal/tables/generation.go`) are permission-not-obligation — E2.
- **Held item untouched BY CONSTRUCTION:** `cmd/frank/main.go` absent from the commit entirely; `completeMissingSeatMintBindings`/`completeSeatMintBinding`/`seat.Manager` shapes untouched — E2.
- Hunks read at my seat (E1): item 1 — the `existingOutcome` guard now fronts EVERY append path (`supersededCredentialOutcome` + both `faultOutcome` branches); a post-commit `AfterAccepted` panic now REPLAYS the just-committed real outcome instead of minting a second record (M1-4 verbatim: replay-or-fault, never a second outcome). Item 2 — `looksLikeBoot` DELETED; admission gated on `env.PreActive` only; boot rows non-renderable on active forms (the §12 conformance restored). Items 3/4 — the floors now scan real enum sources + real sealed record bytes + drive the real served socket for audit/roster views, with planted-leak scanner proofs. Item 5 — the named negatives landed (`TestTagNeverInAcceptedRecords`, `TestStaleNonSubmitRefusalIsNotLifecycleGating`, multi-field smuggle legs, `generation_test.go` exactly-once units). Item 6 — real-child kill-9 TAKEOVER leg + the store-visible TAKEOVER diagnostic + symlink-alias one-winner, at both the unit and frank-process grains. Item 7 — the corrected scope statement present, citing the absorption ruling.
- Red-first: RED evidence recorded per production-touching item (1/2/6) in the fold report; items 3/4 are floor rewrites (no production change to be red against); item 5 partly pins already-present behavior post-1/2 — consistent with the findings.
- **Battery at a8d04b4 (mine):** `go test -count=1 ./...` exit 0, **24 packages ok** (uncached clean-cache run also green) · `go vet` clean · `-race` green on engine/tables/store (+channel in the Implementer's run) — E2.
- Targeted re-check per protocol (no full panel re-run — blast radius small, hunks verified): the narrow fold suites the report names, plus my independent runs above. No design change, no locked-contract touch, no new scope row consumed.

**Standing state — the slice is blocked on exactly ONE item:** the re-mint crash-window fix shape (my packet `SITREP-planner-20260707-041709.md`, routed to you for m-1). On the ruling: the pair folds it under its own FOLD_SCOPE with the SIGKILL-between-pivot-and-rotation fixture red-first; then the exit-gate pass (fixtures + floors + my verification at the final tip), the gate report to you, and the operator's step-exit + merge gates.

ACTIONS_GIT_REF: none — verification only; no edit to the branch or main; this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree (main@2903d84; impl worktree clean at a8d04b4, verified).
Next requested action: the m-1 ruling on the re-mint fix shape; everything else at this seat is ready for the exit-gate pass behind it.
