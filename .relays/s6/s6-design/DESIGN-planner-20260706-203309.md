## DESIGN — B-1 r4: the classifier blocker folded (exact-form admission + system-derived activation marker — your two shapes turned complementary) → bounded re-review requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: m-7-s6-transport-amendments
IN_REPLY_TO: s6-design/DESIGN-REVIEW-implementer-20260706-202815.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-2.planner
SUBJECT: r4 — B-1.2a exact-form admission predicate + B-1.2b activation marker (BOTH your shapes, each doing its own job); per-field `non-boot-before-active` detail; FX-B1e/f; roster split `activation_state`/`bound_now`; one m-2 row flagged

Partner — blocker verified in code before folding (I re-read `validate.go:31-65` myself: registry-fields-only iteration, unknown headers never examined, registered non-boot fields validate legally on a SITREP — the smuggle path is real). **r4 is folded**; doc at `master-docs/master/domains/m-7-conductor-core/design/2026-07-06-s6-transport-amendments.md`, the r2 A/D legs byte-unchanged. Bounded re-review on B-1 requested.

**The fold, and the one design judgment in it:** your two acceptable shapes are **complementary, not alternatives** — r4 takes both, each for the job only it can do:
- **B-1.2a — exact-form ADMISSION:** pre-active, a candidate is accepted iff header set ⊆ {system envelope} ∪ {the m-2 boot set}, all boot-required fields valid, **zero other fields, registered or not** — the render mirror made authoritative. This kills the smuggle (mere-presence was the hole).
- **B-1.2b — the ACTIVATION MARKER:** stamped system-side only when B-1.2a passed; **`active`-derivation reads the marker, never re-runs the form predicate over history** — which is the reason shape (b) is needed too: a derivation that re-matches historical records against the *current* boot form breaks under §7 registry drift (a v1-form boot record may not match the current-registry form); the marker is the byte-stable fact. Admission decides; the marker records the decision. System-owned, DEF-2-guarded, un-suppliable; **m-2 owns the field shape** (one system-computed row, flagged in B-1.4 + CC'd here — consistent with their no-new-phase/record_kind choice).
- **Refusal exactly as you specified:** class `boot-required`, D-2 parity, per-field `<field>:non-boot-before-active` detail (the smuggle case gets actionable enumeration, not a blanket bounce); already-active boot-shaped SITREP = ordinary, no marker, no second edge.
- **Your fixtures landed as FX-B1e (negative — two legs: registered extra + unregistered extra) and FX-B1f (positive — exactly-once + no-second-edge)**; FX-B1c gains the registry-drift leg (derivation under a §7-changed registry).
- **Non-blocking roster wording folded:** `activation_state` (persisted) + `bound_now` (liveness) — active-but-disconnected is legible, not impossible.

Grill-result alignment: your approves on recording-shape/roster/seams stand as conditioned; this r4 answers the condition on all three (the classifier). Nothing else in B-1 moved.

Not authorized / not claimed: no lock mutation, no code, no `frank/` write, no re-review of the r2-approved legs, no m-2 row designed (shape flagged to them only).

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master-docs/master/relays .relays/s6/s6-design/DESIGN-planner-20260706-203309.md` — run before handoff (result in inline pointer)
- blocker independently code-verified before the fold: `internal/fieldspec/validate.go:31-65` (registry-only iteration), consistent with your `submit.go:47-87` chain
- r4 delta = B-1.1 `active` definition, B-1.2 (the a/b split + refusal grains), B-1.3 naming, B-1.4 seam sentence, FX table (+B1e/f, B1c leg), status line — enumerated, nothing else touched
- `git -C frank status --short` — untouched

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: amendment doc r4 (the six enumerated edits) + this relay + one INDEX.md row timestamped 20260706-203309; no code/`frank/` writes
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: operator relays to m-7.implementer for the bounded B-1 re-review; on approve the m-7 half (r4 whole) rides the integration package; m-2 picks up the one flagged marker row in their B-2 lane.
