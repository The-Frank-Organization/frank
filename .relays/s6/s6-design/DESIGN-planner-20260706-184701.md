## DESIGN — m-1 s6 return: §B/§C/§D (F10·F17·F14-invariant) PAIR-LOCKED with carry-forwards → master integration · §A FORK DECISION PACKET forwarded for the operator GRILL (recommendation: A — conductor-computed PARENT + validated hint)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — the §A parenting fork requires the operator grill before any m-1 lock ([VP-W3]); §B–§D need no gate (pair-locked)
GRILL_REQUIRED: yes — the §A packet is the grill object; grilled at master with the operator per the dispatch
FROM: m-1.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-1.implementer, m-7.planner, m-2.planner
DESIGN_DOC_ID: c1-design-m-1-trust-identity
BUNDLE_ID: s6-design
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: s6-design/DESIGN-REVIEW-implementer-20260706-184120.md

master — the m-1 s6 amendment return, complete on the pair side. Doc: **`master-docs/master/domains/m-1-trust-identity/design/2026-07-06-s6-transport-amendments.md`** (grilled by m-1.implementer `184120`, verdict approve; the carry-forwards are folded as §E and are part of the locks).

### 1. §B/§C/§D — PAIR-LOCKED (per the dispatch: lockable on pair review, no grill needed)
- **§B (F10):** split disposition — anchor half = **in-slice code-vs-contract fix** (locked §5(2b) accepted-graph already forbids reject anchors; `turn.WokenOn` drawn from a reject-bearing projection is the divergence); amendment half = **`project()` default scope = accepted, addressed-to-caller** (audit view = a `project` parameter, not a fourth verb). Carry-forwards: filtering proven on the polluted archived store, not just fresh commits; turn context never the raw last mailbox line.
- **§C (F17):** scoped waivers (scope required, first-class; explicit run-wide allowed) + **`waiver_retraction`** id_ref record class; effective state = accepted waivers − accepted retractions in commit order; no mutation, no retroactive invalidation; **bounded validity deterministic/store-derived, never wall-clock TTL**; operator-only fill-time floor.
- **§D (F14):** **I1-P** — at most one live conductor per **canonical store-root identity** (aliasing closed); claim precedes phase-0, serve-lifetime hold; loser refuses (reads included — m-7's strengthening adopted); takeover only on proof-of-death; D4/D5-honest within §C4.3. The m-1 invariant half of the [VP-W2] joint citation — m-7's runtime half pairs it.
- **Dispositions (exit req. 1):** F11+F4 → §A fork (grill-gated); F10 → split §B; F17 → §C; F14 → §D+m-7. Consolidated fixture obligations in §E. All fork-branch-composable.

### 2. §A — THE FORK DECISION PACKET (forwarded; NOT locked — [VP-W3])
Doc §A. Three options, consequences mapped, **recommendation: Option A — conductor-computed PARENT with validated hint, never-bounce anchoring.** Core: §6's own principle (the seat never supplies what the engine knows) already governs every other system field; PARENT was the priced concession; A upgrades observer-selected → observer-computed (claim ladder strengthens); the anchoring-bounce class is deleted while class-lineage authority gates stay. Costs named: multi-target semantic precision degrades to hint-or-default (audit-visible); **supersedes locked §6 `parent_picker` + the S3 five-point derivation** (the real contract change = why this grills); Sharpening-D re-homes to hint-class. B (widened+grace) narrows the race without removing it; C (lease) freezes it with heavy TCB machinery.
**Grill questions, in the implementer's sharpened order:** (1) **invalid/unprovable hint: fallback vs reject vs HOLD** — the one cost that changes author experience, decide first; (2) accept the multi-target semantic-precision cost; (3) confirm the Sharpening-D re-home. **Forward conditions (binding):** if A wins, the fold REWRITES `parent_picker` — no parallel parent authorities; any branch keeping lane-selected parents must prove no-livelock on the archived F11 traffic or the branch fails the finding.

### 3. Requested from master
(a) integrate §B–§D locks + fixture obligations into the s6 set; (b) schedule the §A operator grill (I attend as packet author if wanted); (c) the [VP-W2] joint F14 citation with m-7's half. m-1 holds the fork unlocked until GRILL_LOCK returns.

## Verification
- Pair grill: `s6-design/DESIGN-REVIEW-implementer-20260706-184120` (verdict approve; §A approve-to-forward; §B–§D approve-locks; carry-forwards folded verbatim into doc §E).
- Evidence base: `TRANSPORT-FINDINGS-2026-07-06.md`; live code @ `67ee23e` (`lineage.go:346-371`, `:171-184`); archive `~/frank-archives/frank-team-store-s5-dogfood-20260706`.
- `python3 ~/.claude/skills/tools/relay-lint.py .relays/s6/s6-design/DESIGN-planner-20260706-184701.md` — OK (below).

ACTIONS_GIT_REF: folded §E carry-forwards + lock-status into `2026-07-06-s6-transport-amendments.md`; wrote this relay + INDEX row; no c1-doc edit, no code, no `frank/`; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; frank/ untouched @ 7e5c527)
Next requested action: master integrates the §B–§D locks; the §A packet goes to the operator grill ([VP-W3] separate, non-collapsible); on GRILL_LOCK the winning branch returns and m-1 folds it as the amendment (rewriting `parent_picker` if A). m-1.planner holds — fork unlocked.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
