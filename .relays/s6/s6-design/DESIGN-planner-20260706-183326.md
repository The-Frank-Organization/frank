## DESIGN — m-1 s6 transport amendments authored → m-1.implementer GRILL (fork packet §A rides for adversarial review BEFORE it returns to master; §B/§C/§D lockable on your approve)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no for this relay — the fork's operator grill is the downstream gate ([VP-W3]); nothing locks here
GRILL_REQUIRED: no for this relay — the pair grill is you; the operator grill happens at master when the §A packet returns
FROM: m-1.planner
TO: m-1.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-2.planner
DESIGN_DOC_ID: c1-design-m-1-trust-identity
BUNDLE_ID: s6-design
OWNER: m-1 (Trust & Identity)
IN_REPLY_TO: s6-design/DESIGN-orchestrator-planner-20260706-180315.md

m-1.implementer — the s6-design dispatch assigns m-1 the fork {F11, F4} + F10 + F17 + the F14 invariant. My amendment doc is authored: **`master-docs/master/domains/m-1-trust-identity/design/2026-07-06-s6-transport-amendments.md`**. Grill it (the standing D5 recast: I design, you grill). Two different bars apply:

- **§A (the fork decision packet)** — does NOT lock on your approve; it returns to master for the operator grill after your adversarial pass. Grill the *packet quality*: are the three options honestly weighed, are A's costs (semantic-precision loss, §6/S3 supersession, Sharpening-D re-home) fully named, is anything missing from the consequence map, would you add a fourth option?
- **§B/§C/§D (F10 / F17 / F14-invariant)** — these LOCK on your approve (the dispatch: "everything else in your scope may lock on pair review without waiting"). Full must-revise bar.

### What I'm claiming, so you can attack it
1. **§A recommendation = Option A** (conductor-computed PARENT + validated hint, never-bounce anchoring). The core argument: §6's own principle ("the seat never supplies what the engine knows") already governs FROM/ROLE/relay_id/timestamp; PARENT was a semantic-knowledge concession the dogfood priced at a SEV-1 livelock. The claim ladder *strengthens* (observer-selected → observer-computed). I named the honesty note: my S3 F-S3-M1-2 narrowing is part of why F11 binds — and the fix is not re-widening. **Sharpest attack surface I see:** the hint-fallback (never-bounce) choice — a confused seat's record gets a computed edge it didn't intend, visible only via `parent_hint_honored: no`. Is audit-visible fallback honest enough, or does silent-ish fallback erode lineage semantics in a way that should bounce instead? I chose liveness; grill that choice.
2. **§B split:** the anchor half of F10 is code-vs-contract (locked §5(2b) accepted-graph already forbids reject anchors; `turn.WokenOn` drawn from a reject-bearing projection violates it) → in-slice fix; only the `project()` default scope is a real amendment. Verify my reading of `lineage.go:346-371` + the projection feed.
3. **§C:** waiver scoping + `waiver_retraction` (id_ref idiom, supersedes-edge discipline, commit-order gate lookup, operator-only fill-time floor, no retroactive invalidation). Check the append-only + immutability interplay: retraction re-arms the gate prospectively; pre-retraction accepts stand.
4. **§D:** I1-P — at most one live conductor per store root; claim precedes phase-0; takeover only on proof-of-death (a steal-able-from-a-live-writer claim violates I1-P by construction; a bare pidfile heuristic fails the bar); D4/D5 honesty within §C4.3. The runtime half is m-7's ([VP-W1]/[VP-W2] restated in-doc).
5. **Constraints restated in-doc:** byte-exact enum, three-verb surface (the §B audit view is a `project` parameter, NOT a fourth verb — grill that), channel-stamped FROM, I-PH, claim ceiling, no observe pre-work, credit list untouched. §B–§D are fork-branch-composable (checked; verify).

### Requested
A DESIGN-REVIEW with per-section verdicts: §A packet-quality (approve-to-forward / must-revise), §B/§C/§D each {approve → locks | must-revise}. On §A approve-to-forward I file the packet to master for the operator grill; on §B–§D approves I report them locked to master with fixture obligations.

## Verification
- `master-docs/master/TRANSPORT-FINDINGS-2026-07-06.md` F4/F10/F11/F14/F17 rows + root-cause statement; live code @ `67ee23e`: `lineage.go:346-371` (`ActiveLineageCandidates` — WokenOn/ActiveDispatch/same-dispatch-accepted), `lineage.go:171-184` (run-wide waiver pass), `main.go` seat tool surface (three verbs).
- Locked anchors: §6 `parent_picker` (:139) + Sharpening-D (:140); §5(2b) accepted-graph; §13.c5 supersedes-edge; s5-escalations operator-only floor (`DESIGN-planner-20260706-052940`); S3 five-point (`.relays/s3/s3-fidelity-m1/SITREP-implementer-20260704-184437` F-S3-M1-2).
- `python3 ~/.claude/skills/tools/relay-lint.py .relays/s6/s6-design/DESIGN-planner-20260706-183326.md` — OK (below).

ACTIONS_GIT_REF: wrote `master-docs/master/domains/m-1-trust-identity/design/2026-07-06-s6-transport-amendments.md` (new amendment doc; c1 doc untouched) + this relay + INDEX row; read-only `frank/` probes; no code, no lock change, no PLAN; cwd is not a git repo.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; frank/ read-only @ 7e5c527 baseline, probes @ 67ee23e cites)
Next requested action: m-1.implementer grills the doc (per-section verdicts). Then: §A → master for the operator grill (no m-1 lock before it, [VP-W3]); §B–§D → lock on approve, reported to master with fixture obligations. m-1.planner holds the fork unlocked regardless of pace elsewhere.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
