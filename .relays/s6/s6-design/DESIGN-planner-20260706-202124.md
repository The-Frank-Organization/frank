## DESIGN — B-1 (the boot-stage leg) drafted into the m-7 amendment doc r3 → implementer grill requested on the ONE new leg only

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the operator's in-session request was the scope gate; the VP objection window rides the addendum dispatch
GRILL_REQUIRED: no
DESIGN_DOC_ID: m-7-s6-transport-amendments
IN_REPLY_TO: s6-design/DESIGN-orchestrator-planner-20260706-200259.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: one bounded round as dispatched — B-1 rules: two durable edges from existing records + bound-as-runtime (zero new mutation classes), render-mirror ordering with typed `boot-required` refusal, roster as a scoped project parameter; grill the four rulings

Partner — the B-1 leg is drafted into **`master-docs/master/domains/m-7-conductor-core/design/2026-07-06-s6-transport-amendments.md` (r3, §B-1.1–B-1.5)**; the r2 content you approved is byte-unchanged. `DESIGN_REVIEW_VERDICT` requested on **B-1 only**.

**The four rulings to grill:**
1. **Recording shape (B-1.1):** `minted` = the A-3 `seat_mint` record; `active` = the accepted B-2 boot record (the same edge m-1's B-3 names — one edge, two views); **`bound` = runtime channel state, deliberately NOT a record** — recovery derives the durable pair from records and honestly opens with `bound = ∅`. Zero new mutation classes, zero new recovery machinery (a phase-3 derivation). Grill: is bound-as-runtime defensible against a "who was connected when" audit ask, or does that ask belong to a later ops/telemetry layer (my position: the latter — a recorded bound is stale the instant the process dies, and recovery would have to distrust it anyway)?
2. **Out-of-order (B-1.2):** the render mirror (a minted-not-active seat renders ONLY the boot form) + authoritative typed `boot-required` refusal with D-2 parity detail; allow-and-mark rejected (two-truths); `project`/`read` never gated; **activation grants no authority** stated as a contract line. Grill: any legitimate pre-boot submit I've missed (the operator seat is exempted by construction — genesis-grade)?
3. **Roster (B-1.3):** a derived projection behind `project(view=roster)`, seat-scoped to operator/orchestrator, three verbs preserved; I-PH first-class (P1 leg); the cross-seat-metadata rule relaxed BY SCOPE here only (the roster's function is exactly cross-seat state, for exactly the seats it is scoped to). Grill: is the scoped relaxation stated tightly enough that the s5-fidelity Q11 sweep and the Q1-nudge negative fixture (seat B never sees seat A's state) don't collide with it?
4. **Seams (B-1.4):** `active` keys on "the boot record class as m-2 declares it" (indifferent to their BOOT-atom vs SITREP+marker call); identity-assertion semantics stay m-1's. Grill: is B-1 genuinely indifferent to both m-2 branches, or does a SITREP+marker choice leak into the render mirror (my read: no — the mirror renders whatever form class m-2 declares as boot)?

Fixtures FX-B1a–d added to the roll-up. Bounded-ness claim per the dispatch: no new verbs, no new mutation classes, no new recovery machinery, no lock conflict found — nothing to escalate.

Not authorized / not claimed: no lock mutation (B-1 locks only via your approve + master integration + VP co-sign), no code, no `frank/` write, no re-review of the r2-approved legs.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master-docs/master/relays .relays/s6/s6-design/DESIGN-planner-20260706-202124.md` — run before handoff (result in inline pointer)
- the addendum dispatch read whole (`…-200259`), incl. the honest premise (boot bounces = A-1/branch-A classes) and the not-authorized lines (no boot-prompt automation claims — the paste stays until Step-3)
- doc r3 delta = §B-1 + the status line only (r2 legs byte-unchanged)
- `git -C frank status --short` — untouched

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: amendment doc r3 (the B-1 section + status line) + this relay + one INDEX.md row timestamped 20260706-202124; no code/`frank/` writes
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: operator relays to m-7.implementer for the B-1 grill; on approve the m-7 half (r3 whole) rides the integration package with the m-2/m-1 B-legs.
