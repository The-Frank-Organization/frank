## DESIGN — m-4 BLOCKING confirm on the Sharpening-D re-home: CONFIRMED, one consumer-certification condition (legible unverified-citation flag); R2 strengthened, no m-4 FieldSpec change

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — surface-certification of an already-grilled+locked decision (GRILL-LOCK-parenting-fork-2026-07-06); the operator ruled "m-4 first"; answered directly to master per that sequencing, m-4.implementer CC'd with a dissent window
GRILL_REQUIRED: no
DESIGN_DOC_ID: c2-design-m-4-routing-policy
IN_REPLY_TO: s6-design/RECONCILE-orchestrator-planner-20260706-195300.md
FROM: m-4.planner
TO: master.orchestrator-planner
CC: operator, master.orchestrator-reviewer, m-1.planner, m-4.implementer
BUNDLE_ID: s6-design

**Verdict: CONFIRM the Sharpening-D re-home to hint-class, conditioned on one consumer-certification item (the legible
unverified-citation flag, below).** Direction approved; no m-4 record-shape change; R2 strengthened, not merely preserved.

**Process note.** Blocking clause, operator ruled "m-4 first, answer before anything else." The decision is already
grilled + locked (operator + CTO, `GRILL-LOCK-parenting-fork-2026-07-06.md`); my role is to **certify my consumer
surface**, not re-litigate the fork. Because the re-home *strengthens* R2 and touches no m-4 FieldSpec, I answer
directly to master per the "m-4 first" sequencing rather than gating on a pair round. **m-4.implementer is CC'd with
an explicit dissent window** — raise before m-1 folds the clause if the audit-flag condition is under-specified.

---

### The surface, located precisely

From m-4's side, the m-1 §6 "Sharpening-D" citation — *how a dispatch cites the routing-decision relay that placed
it* — is the carriage for what my §5:216 labels **`lineage_role: routing_ref` on the referencing dispatch** (the
back-reference from a work-dispatch to the separate seat-stamped routing-decision relay, per my Q-C ruling). Confirming
that is the consumer surface in question; if m-1 reads the mapping differently, flag it — but this is the field the
re-home moves.

### Why CONFIRM — three checks pass, plus the empirical basis

1. **Q-C intact; routing record byte-unchanged.** The routing decision stays a **separate seat-stamped relay** (not a
   dispatch header). Re-homing the *citation carriage* changes **no m-4 FieldSpec row** (§5 untouched) and does not
   alter the routing record's provenance (seat-stamped `FROM`, lineage-accepted). ✔ Byte-unchanged as claimed.
2. **R2 strengthened, not just preserved.** Hint-class = **recorded-verbatim / verified-when-provable / never
   authority**. A citation that can *never* be authority can never be read as a gate input — so it categorically
   cannot smuggle a model-derived predicate into any gate (§2:82 "no `model_*` predicate ever enters the schema
   gate"). The re-home closes an even-by-accident path. ✔ Strengthens.
3. **No m-4 mechanism relies on bounce-on-fail.** I checked for a real downside: nothing m-4-owned needs the
   back-citation to be verified-or-bounce. The deviation check (§2) reads the routing record's **own** fields
   (`declared_deviated`, `declared_bucket`, snapshot); the Step-3 router consumes the routing record, not the
   dispatch's back-citation. So **fallback-never-bounce removes no m-4 capability** — it only changes what happens to
   an *unprovable* citation (flag, don't bounce), which the audit already proved is the correct disposition (zero true
   bad picks in the entire recorded history; refusing them punishes only correct behavior). ✔

### The one condition (m-4 consumer-certification — must fold with the clause)

**The unverified-citation flag must be legible on m-4's consumable surface.** When a `routing_ref` citation is
unprovable, the record must carry the **verbatim citation + an explicit `routing_ref_honored: no` / unverified
marker** (parallel to m-1's `parent_hint_honored: no` in the submit response), so replay/audit can distinguish a
**verified** citation from an **unverified** one. This is not optional decoration — the seat-stamped, persisted,
**auditable** routing artifact is m-4's whole §11 contribution; a silent conflation of verified and unverified
citations would erase exactly the audit legibility the record exists to provide. Two sub-points:
- The marker is **hint-class / audit-only — NOT a gate input** (no R2 concern; it is an observability field on the
  consumable record, same rails as the citation itself).
- "Legible" = present on the **accepted, consumable** record m-4's audit/replay reads — not merely in a transient log.

**Net:** CONFIRM the re-home; the m-1 §A fold may include the Sharpening-D clause **provided** the unverified-citation
flag lands per the condition above. No amendment to any m-4 FieldSpec; the change lives in m-1's §6 carriage plus the
one audit-marker on the consumable record.

## Verification
- `grep -n "lineage_role: routing_ref" design.md` → §5:216 (the dispatch→routing-record citation = the surface being re-homed).
- `grep -nE "no .model_.* predicate|R2 by construction" design.md` → §2:80-82 (R2 the re-home strengthens).
- `grep -n "auditable" design.md` → §11 (the audit-legibility contract the condition protects).
- Read `master-docs/master/GRILL-LOCK-parenting-fork-2026-07-06.md` premise: branch A + fallback-never-bounce + zero-true-bad-picks audit — the basis I certify against.
- No `frank/` edit, no m-1 §6 edit (m-1's fold), no code, no PLAN.

ACTIONS_GIT_REF: wrote this relay + appended `master-docs/master/relays/INDEX.md`; no design-doc edit (m-4 §5 unchanged by the re-home), no m-1/`frank/`/code edit, no PLAN.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace); `frank/` main @ `7e5c527` (tag `s5-close`), untouched.
Next requested action: master notes the m-4 CONFIRM (conditioned on the legible unverified-citation flag); m-1 folds §A including the Sharpening-D clause with that flag; master assembles the integrated amendment set → VP co-sign. m-4.implementer may raise dissent on the condition before the m-1 fold lands. m-4.planner holds.
