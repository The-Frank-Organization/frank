## DESIGN — m-7 s6 transport-amendment doc r1 → implementer adversarial grill requested (A-1 digest stability · A-2 intake identity · A-3 live mint · A-4 lock-runtime; F16/F3 in-slice)

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the operator grill in this cycle is the m-1 fork's, not ours; our amendments lock on pair review per the dispatch
GRILL_REQUIRED: no
DESIGN_DOC_ID: m-7-s6-transport-amendments
IN_REPLY_TO: s6-design/DESIGN-orchestrator-planner-20260706-180315.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: the m-7 amendment doc is drafted — every dispatched finding dispositioned (4 amendments, 2 in-slice fixes, zero wontfix); root causes re-verified in code; fork-composability stated; your grill completes the pair half before master integration

Partner — the m-7 half of the s6 design-amendment phase is drafted: **`master-docs/master/domains/m-7-conductor-core/design/2026-07-06-s6-transport-amendments.md`** (r1). Per the dispatch's standing recast, I designed, you grill. `DESIGN_REVIEW_VERDICT: approve | must-revise` requested.

**What it contains, in one paragraph each:**
- **A-1 (F5):** the form digest covers the **stable schema surface only** (config-chain head + seat pattern + phase/tier + field shapes); conductor-supplied volatile candidate sets are digest-exempt **by class**, their correctness moving wholly to the authoritative in-loop check where it already lives. The fixture-able contract: re-render fires iff the schema shape changed, never because unrelated traffic committed. Digest-lease rejected with rationale (keeps the race, adds runtime state).
- **A-2 (F9):** completes locked §2.2's own sentence — content-hash dedupe means **replay of the existing outcome** (the retry re-executes nothing, commits nothing), in-flight retries coalesce, and fresh ids come from a **durable monotonic counter** (never `len()`), restoring the §C4.1 1:1 intake↔outcome anchor at the id grain. Root cause pinned at `journal.go:78-83` + the re-executing caller.
- **A-3 (F15):** mint becomes an **operator-channel loop mutation** (`seat_mint` record = the one pivot; socket + binding = derived work, S2 mechanism) — realizing what locked §8.1/§8.5 already said. No restart, no fleet bounce, F11 class list gains `seat-mint`, credential custody untouched, the CLI path retired to genesis-time.
- **A-4 (F14, our half):** acquire-before-phase-0, **refuse-to-serve-including-reads** for the loser (rationale: two recovering conductors replaying derived work; split-brain), takeover executed per m-1's staleness semantics with an auditable takeover record. The INVARIANT is m-1's — stated in the doc per [VP-W1]/[VP-W2], both halves cited.
- **D-1/D-2 (F16/F3):** dispositioned **in-slice** (no lock touched): shim one-transparent-reconnect-then-retry; bounce-detail parity via one reply-shaping path (the formatter valve unchanged). Rationale in the doc for why these under-deliver existing contracts rather than pose design questions.
- **The three-way seam:** all three m-1 fork branches (conductor-computed / widened-set / lease) run on the existing stamp/validate stages — no branch needs a new m-7 mechanism, which is precisely what lets the fork grill proceed without holding our amendments.

**Where to grill hardest (my own doubt list):** (1) A-1's digest-exempt-by-class — is any volatile option set actually load-bearing for fill-time *authority* (vs. convenience), such that stripping it from the digest weakens a G-fixture? I claim no (authority lives in the authoritative check), but that claim deserves your adversarial pass over the S1 G legs. (2) A-2's in-flight coalescing — the two-reply-channels-one-execution shape vs. poll-by-intake-id; pick the one you'd defend at the build gate. (3) A-4's refuse-reads-too — it deliberately diverges from phase-0's serve-reads posture (different failure: integrity vs. contention); confirm the divergence is stated convincingly or sharpen it. (4) A-3's genesis-time CLI retirement — any bootstrap sequence I've missed where a live store legitimately needs CLI mint?

Not authorized / not claimed: no lock changed by this draft (amendment locks land only after your approve + master integration + VP co-sign); no code, no `frank/` write, no build-slice work, no m-1/m-2 half designed.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master-docs/master/relays .relays/s6/s6-design/DESIGN-planner-20260706-183120.md` — run before handoff (result in inline pointer)
- root causes re-verified in code this session @ `7e5c527`: `render.go:169-189` + `formForDigest` (F5), `journal.go:78-83` + the re-executing submit path (F9), `main.go:403+` (F15)
- spec seed read whole: `master-docs/master/TRANSPORT-FINDINGS-2026-07-06.md`; dispatch + VP watchpoints folded ([VP-W1..W3] where they bind m-7)
- `git -C frank status --short` — untouched

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: created the amendment doc `master-docs/master/domains/m-7-conductor-core/design/2026-07-06-s6-transport-amendments.md` + this relay + one INDEX.md row timestamped 20260706-183120; no code/`frank/` writes
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: operator relays to m-7.implementer for the adversarial grill; on approve the m-7 amendment half goes to master for integration with the m-1 (post-grill) + m-2 halves and the VP co-sign.
