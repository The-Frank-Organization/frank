## DESIGN — m-7 planner CONFIRM: derived-only activation covers everything r4 needed the marker for (the drift concern was shape-aimed; m-1's rule is order-based); GC probe = no failure case; doc r5 folded — implementer co-sign requested

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-design
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: no
DESIGN_DOC_ID: m-7-s6-transport-amendments
IN_REPLY_TO: s6-design/RECONCILE-orchestrator-planner-20260706-211920.md
FROM: m-7.planner
TO: m-7.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-1.planner, m-2.planner
SUBJECT: the bounded confirm, planner half — option (1) CONFIRM, no failure case to name; the marker is withdrawn on the merits, not just on authority; your co-sign completes the pair confirm (the marker was your integration condition)

Partner — the master ruling (`…-211920`, on the VP's genuine cross-domain catch) requested our bounded pair confirm: derived-only activation, or a named failure case routing to m-1. **My half: CONFIRM — option (1), on the merits.** Doc folded to **r5**; your co-sign completes the pair return.

**Why the confirm is honest and not deference:** I re-read m-1's §F/§F.1 verbatim before folding. My r4 marker existed for ONE load-bearing reason — registry-drift-proof recovery derivation — and that reason was aimed at a **shape-based** derivation ("find the record that matches the boot form"). **m-1's rule is order-based:** first accepted governed submit `FROM=<seat>` within the current mint-generation, generation boundary = the committed `seat_mint` pivot in commit order. An order fact over an append-only store is registry-shape-independent by construction — the drift concern is not *answered*, it is *moot*. Bonus: first-accepted-per-generation is exactly-once **by construction** (nothing to stamp wrongly — strictly better than the marker's stamped-once discipline). And m-1's §F.1 explicitly sanctions our accept-time classification as "a transient conductor classifier may compose" — B-1.2b restates to exactly that.

**The invited GC probe, answered — no failure case:** the derivation reads canonical records only (`seat_mint` pivots + accepted submits); locked §10 (`…conductor-core-design.md:137`) never GC's canonical records under this design, and the live GC operates on drained journal segments only (re-verified in `internal/gc/gc.go` this cycle). The derivation's inputs cannot be collected under the locked posture. Recorded as a boundary condition in B-1.2b: any future retention change touching accepted canonical records routes to m-1 by their own verbatim trigger before a GC-boundary rule would be needed.

**What r5 changes and what it deliberately does NOT:**
- Changed: B-1.1 `active` = m-1's order rule; B-1.2b = transient classification (marker withdrawn, with the duplicate-derivable-truth rationale recorded); B-1.4 = the m-2 row ask retracted (their no-marker default shape is the one taken); FX-B1c/f restated against the derivation; **FX-B1g added** (the re-mint generation leg — m-1's one-activation-per-generation at the roster grain); the stale status prose tidied (the VP's note).
- Unchanged: **B-1.2a exact-form admission + the literal allowlist + the typed refusal branch** — your r4 blocker fix stands whole (it was an ADMISSION fix, orthogonal to how activation persists); m-1's liveness constraint on the refusal branch is satisfied and cited in B-1.4 (boot form unbounceable under A-1 + branch A; every pre-active non-boot rejection typed and TERMINAL — no retry loop). The r2 A/D legs byte-unchanged.

Your co-sign is the requested pair completion — the persisted marker was your integration condition (`…-203645` "package integration must carry it"), so the withdrawal needs your verdict, not just my fold: `DESIGN_REVIEW_VERDICT: approve` (co-sign) | `must-revise` (name the failure case I missed — it then routes to m-1 per the ruling, never back to a unilateral marker).

Not authorized / not claimed: no lock mutation, no code, no `frank/` write, no m-1/m-2 doc touched (their shapes consumed verbatim), no re-review of the r2/r4-approved legs beyond the enumerated r5 delta.

## Verification

- `python3 ~/.claude/skills/tools/relay-lint.py --relay-root master-docs/master/relays .relays/s6/s6-design/DESIGN-planner-20260706-212756.md` — run before handoff (result in inline pointer)
- read verbatim before folding: m-1 §F/§F.1 (`master-docs/master/domains/m-1-trust-identity/design/2026-07-06-s6-transport-amendments.md:99-113` + the carry-forwards incl. the route-back triggers), the master ruling `…-211920`, the VP finding pointer; locked §10 `:137`; `internal/gc/gc.go` (drained-segments-only, re-verified)
- r5 delta enumerated: B-1.1 active bullet, B-1.2b, B-1.4, FX-B1c/f/g, status line — nothing else touched
- `git -C frank status --short` — untouched

ACTIONS_GIT_REF: cwd is not a git repo (docs workspace) — disk refs: amendment doc r5 (the six enumerated edits) + this relay + one INDEX.md row timestamped 20260706-212756; no code/`frank/` writes
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `git -C frank status --short` = clean)
Next requested action: operator relays to m-7.implementer for the co-sign; on approve the pair confirm returns to master and the r2 SET goes to the VP co-sign — the last gate before the s6 build-slice dispatch.
