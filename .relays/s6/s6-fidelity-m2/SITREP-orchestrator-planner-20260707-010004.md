## m-2 FIDELITY PACKET — s6 design r2: codec/registry/boot-form/render/validate surfaces (review object at `main@a499bc3`); the [VP-W3] registry reading + the waiver render-absence question are the headline asks

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s6-fidelity-m2
PARENT_DISPATCH_ID: s6-core-design-r2-review-implementer
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: s6-slice-6-design
FROM: s6.orchestrator-planner
TO: m-2.implementer
CC: master.orchestrator-planner, s6.orchestrator-reviewer, operator, s6-core.planner
SUBJECT: m-2 fidelity review request (operator-carried via the master hub) — review object = `docs/sprints/2026-07-06-s6-slice-6/designs/s6-slice-6-design.md` r2 @ `main@a499bc3`, the m-2-surface sections; verdict = a dispatch precondition; two named questions routed to you by domain ownership

**Context.** The s6 build design against your codec amendment is pair-approved (lineage chain complete; GRILL_LOCK `s6-grill-s6-core` folded). Per the s6-dispatch, m-2 holds fidelity on the codec, the registry pass, the boot form, and every render/validate surface. The pair's PLAN cannot dispatch before your verdict is on record in `.relays/s6/s6-fidelity-m2/`.

**Review object:** design §2 (the ONE codec: `ParseTyped` `address_list` exported as the single decode; the lineage comma-splits deleted; delivery silent-drop → engine invariant fault; decoded header TO/CC = recipient truth for EVERY projection per your §2.4 rev1) · §3 (A-1 digest: volatile-by-class `conductor_supplied_volatile`, your `recipient_picker` DigestExempt half) · §9 (F13 three-layer: `validateRecordKind` reduced to layer 3; `disposition`/`diagnostics` fixtures; the `genesis` seat-scope negative) · §11 (D-2 detail shape: `Field:Class` parity, byte-equal reply↔record) · §12/B-2 (the boot form: SITREP + `charter_loaded`/`dispatch_status`, lifecycle-gated render, exact-form admission consuming m-7's B-1.2a literal allowlist) · §14 (the registry pass whole).

**Specific asks:**
1. **The [VP-W3] registry reading (operator-grilled §18 row ⑥; confirmed at my seat; flagged to you per the grill's own commitment):** the pass lands the SEVEN transport rows (`parent_hint` · `parent_hint_honored` · `parent_provenance` · `routing_ref_honored` · `rationale` · `waiver_scope` · `retracts`) + the TWO B-2 boot fields + one `dispatch_status` named enum + record classes `waiver_retraction`/`seat_mint` + the `ORCH_REVIEW_WAIVER` `"*"` retirement — reading "exactly seven" as fencing the TRANSPORT row set against the activation-marker's reintroduction, with the boot fields your §11.2's separately-named additive-MINOR obligation. The guarded negative stays absolute (no activation-marker row). Confirm this is your registry-pass shape; MINOR compat class throughout; R-1 (no envelope migrator) held.
2. **The waiver-row render-absence question (design §8 routes it to you by domain ownership):** the F17 waiver record's rows are operator-only by record-authorship (your §4 rev1). Can fill-time ABSENCE of the waiver rows on non-operator forms be expressed in the current FieldSpec grammar (e.g. `visible_when`/seat-scope composition), or is submit-path rejection the s6 floor? The design claims only the floor until you rule; the pair's PLAN carries your answer verbatim.
3. `seat_mint` + `waiver_retraction` row shapes (§7/§8/§14): operator-only seat_scope via the record-kind map, per-kind required fields at layer 3 — faithful to your three-layer split?
4. B-2 (§12): lifecycle-gated render (the boot form as the ONLY pre-active render), the exact-form admission mirror, `charter_loaded` self_reported and never a gate input, the shared-vocab-untouched negative — faithful to your §11?

Per the s3/s4 pattern: narrow-re-review-vs-verbatim-carry is your call; your route-back triggers bind either way.

ACTIONS_GIT_REF: none — review-request packet only; no code/tracked-doc edit by this relay.
FINAL_GIT_STATUS_SHORT: none — clean tree at relay-write time.
Next requested action: operator carries this packet via the master hub; your verdict relay lands in `.relays/s6/s6-fidelity-m2/`.
