## REVIEW-FOLD — s6-core.planner → Implementer: the gate-record fold (two docs-only F-GATE folds · two OI materializations · the gate-record doc carrying my precise redrive classification, INVERTING master's crude split: honored 0 / fallback 9 / no-hint 5 — the claim-bearing 0 parent-class stands)

ROLE: Planner
PHASE: REVIEW-FOLD
AUTHORITY: fold-in-only
DISPATCH_ID: s6-gateday-fold
PARENT_DISPATCH_ID: s6-core-impl-remint-ruling
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
IN_REPLY_TO: SITREP-orchestrator-planner-20260708-021139.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, s6.orchestrator-reviewer, operator
SUBJECT: fold the gate record on `s6-transport-impl` per the orchestrator's dispositions (`s6-gateday/SITREP-orchestrator-planner-20260708-021139.md`) — docs-only: the F-GATE-s6-2 digest wording + the F-GATE-s6-4 hosted-seat caveat + the two typed OWED ITEMS in results/ + the gate-record doc (content below verbatim, incl. my reply-level redrive classification); FOLD_SCOPE pre-filed citing the directive; battery = the no-regression floor

**The fold (all docs-only; no code path):**

1. **F-GATE-s6-2** — `docs/sprints/2026-07-06-s6-slice-6/results/step-exit-procedure.md`: the `new_digest` wording corrected to the composite chain digest (`config.Digest` over ALL members, not the single member's hash), with the operator-refusal-until-shown episode credited (its HOLD was the custody discipline working).
2. **F-GATE-s6-4** — `docs/ops.md` + the procedure's relaunch note: the hosted-seat caveat verbatim — hosted seats do not consume `tools/list_changed`; after ANY re-render bounce, re-read the schema rather than trusting the cached const.
3. **OI-S6-BOUNCE-CLASS-UX** — new `docs/sprints/2026-07-06-s6-slice-6/results/OI-S6-BOUNCE-CLASS-UX.md`, typed: {owner: the relaunch/next m-2 touch; source: gate-day finding F-GATE-s6-1 + the operator's two-bounce diagnosis round + the orchestrator disposition relay; target_surface: requiring `PHASE`/`CEREMONY_TIER` or folding absent-context into the typed-required class — a registry/validate contract change OUTSIDE the co-signed set (the fence held at the finish line); disposition_path: m-2 rules the shape (require-flag vs class fold), then a bounded build touch; the operator may author the live-store owed record post-relaunch (the s2 Q2=(i) pattern)}.
4. **OI-S6-ENVELOPE-KEY-HYGIENE** — new `docs/sprints/2026-07-06-s6-slice-6/results/OI-S6-ENVELOPE-KEY-HYGIENE.md`, typed: {owner: next m-1/m-2 seam touch; source: gate-day finding F-GATE-s6-3 (the walk seat credited as finder); target_surface: extending the DEF-2 guard to envelope-layer `system_only` keys — hygiene, not a bypass (identity held, NF-S1); disposition_path: the next m-1/m-2 seam touch rules + bounded build}.
5. **The gate-record doc** — new `docs/sprints/2026-07-06-s6-slice-6/results/gate-record.md`, carrying VERBATIM: (a) master's evidence set + stated caveats (legs (a)/(c) + setup master-scaffolding-driven, operator-authored §7/mints/designations; redrive run-1's 12 rejects = pre-dance-client evidence; run-2 JSON = the artifact of record; the walk seat's `project → null` = an empty inbox, not a pre-active artifact); (b) the four F-GATE dispositions per the orchestrator relay; (c) **my precise redrive classification, verbatim below**; (d) the honesty rail (transport/provenance-only; done-state + `record_integrity` self_reported).

**The precise redrive classification (my analysis of record — from the COMMITTED step-store records for all 14 reply relay-ids, not the client counters; land verbatim in the gate-record doc):**

> Reply-level classification of `step-exit-dogfood-redrive.json` (run 2, the artifact of record), read from the committed records at the step store: **accepted 14/14 · parent-class bounces 0 · hint-carrying 9 → honored 0, fallback 9 · no-hint 5 → engine default**. Master's client-side `hint_honored: 9` was the crude split its own caveat flagged: every hint-carrying record actually carries `parent_hint_honored: no` with the verbatim hint preserved (intent + outcome + flag all present on each record — the GRILL_LOCK triple). **Zero honored is correct engine behavior under the redrive's normalization, not a prover deficiency:** the hints were ARCHIVED s5 relay-ids, and the re-driven records carry fresh ids on the fresh step store, so no archived id can exist in that store's accepted graph — every hint is honestly unprovable, and fallback-never-bounce carried all 14 to acceptance (including the four cross-namespace `park-relay-…` hints from the bad-picks audit). On a live store where hints cite local ids the prover resolves them (the G-2 hint-honored fixture is the executable proof). Stamped provenance: 13× `woken_on`; 1 record (re-drive of `relay-3728794d…`) committed with NO parent edge and no provenance — the probe seat's turn context was empty at that instant, the firstDefined chain exhausted, and anchoring stayed live (accepted, no bounce; class gates demanded nothing of a SITREP) — the transport-is-always-live property at its boundary. **The F11 claim does not depend on the split: 0 parent-class is the claim-bearing number.**

**Discipline:** FOLD_SCOPE pre-filed BEFORE any edit, rows citing the orchestrator directive (`SITREP-orchestrator-planner-20260708-021139.md`); expected rows: the procedure doc, `docs/ops.md`, the three new results/ files, + relay/INDEX substrate. `internal/gc/gc_test.go` not expected (its ruling citation stands if touched). Docs-only ⇒ the battery re-run at the fold tip is the no-regression floor, not a new-code gate; `git diff --check` clean. Fold report under this DISPATCH_ID with FOLD_SCOPE above ACTIONS_GIT_REF.

FOLD_SCOPE:
- none -> in
FOLD_SCOPE_RESULT: all-in

The scope block above is the structural no-edit form: this relay is the findings DIRECTIVE and touches no file; your executing fold pre-files the real FOLD_SCOPE per the Discipline section.

ACTIONS_GIT_REF: none — fold directive only; no edit by this relay (my classification analysis was read-only over `~/frank-s6-gate/results/` + `~/frank-s6-step-store/records/`).
FINAL_GIT_STATUS_SHORT: none — clean tree (main@bd66a97; impl worktree clean at 1f6cd08).
Next requested action: s6-core.implementer folds; on its report I verify and file the pair's gate record to the orchestrator — the held master SITREP releases on that chain.
