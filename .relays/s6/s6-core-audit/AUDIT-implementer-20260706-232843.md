## AUDIT - s6-core.implementer independent audit: transport-fix surface at s5-close

ROLE: Implementer
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: s6-core-audit-implementer
PARENT_DISPATCH_ID: s6-core-audit
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
FROM: s6-core.implementer
TO: s6.orchestrator-planner
CC: operator, s6.orchestrator-reviewer
SUBJECT: AUDIT - independent implementer pass over the co-signed s6 transport-fix set against the s5-close code surface

Independence statement: I read the addressed orchestrator AUDIT dispatch first and did not read or coordinate with the sibling `s6-core.planner` audit before filing. When checked, `.relays/s6/s6-core-audit/` contained only `AUDIT-orchestrator-planner-20260706-232500.md`.

Scope and baseline: read-only audit only. Current `frank` HEAD is `79238ca871e99061db8989dc35f4e07a6dc0fe9e`; `s5-close` is `7e5c527713d1450ea044d56c15b4e82ae2f36c5e`. The ahead diff is docs-only: `docs/sprints/2026-07-06-s6-slice-6/ROADMAP.md` and `docs/sprints/2026-07-06-s6-slice-6/RECONCILE.md`. Executable code audited here is therefore the s5-close surface.

### 1. Spec-to-exit-gate map

- Whole-set scope is grounded: the sprint ROADMAP names the authoritative set and constituents at `docs/sprints/2026-07-06-s6-slice-6/ROADMAP.md:3-6`, constrains the one-line boundary at `:8-9`, and lists every IN cluster at `:11-12`. The integrated set maps those same clusters by finding and boot leg at `../master/S6-AMENDMENT-SET-2026-07-06.md:26-48`, names the seams at `:50-57`, and rolls build obligations at `:59-64`.
- GRILL_LOCK three are grounded: branch A, fallback-never-bounce, and fixture obligations are at `../master/GRILL-LOCK-parenting-fork-2026-07-06.md:13-16` and `:33-36`. These feed ROADMAP exit fixture line `docs/sprints/2026-07-06-s6-slice-6/ROADMAP.md:18`.
- m-7 FX-A1a..FX-B1g are grounded: A/D fixtures at `../master/domains/m-7-conductor-core/design/2026-07-06-s6-transport-amendments.md:72-86`; B-1 fixtures, including FX-B1g, at `:119-129`. ROADMAP exit line `:18` and set line `../master/S6-AMENDMENT-SET-2026-07-06.md:62` correctly pull them in.
- m-1 section E and section F.1 are grounded: carry-forwards and consolidated fixture obligations sit at `../master/domains/m-1-trust-identity/design/2026-07-06-s6-transport-amendments.md:79-89`; activation carry-forwards and route-back triggers at `:105-113`. ROADMAP exit line `:18` and set line `:62` pull the m-1 set in.
- m-2 fixture set is grounded: codec/recipient projection/record_kind/parent/waiver/recipient_picker fixtures are at `../master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md:109-116`; B-2 boot fixtures at `:166-169`. ROADMAP exit line `:18` and set line `:62` pull them in.
- E2 floors are grounded: ROADMAP `:19` requires full uncached battery, byte-exact enum, three-verb surface, and I-PH sweep. The set restates these constraints at `../master/S6-AMENDMENT-SET-2026-07-06.md:66-68`; the domain docs restate them at m-1 `:10`, m-2 `:103-105`, and m-7 `:5`.
- Step-exit is grounded: ROADMAP `:20` names the fixed-conductor live legs; the master dispatch has the same at `../.relays/s6/s6-dispatch/PLAN-orchestrator-planner-20260706-221040.md:37-41`; the base Step-1 exit test referenced as "ROADMAP:83-85" is in `../ROADMAP.md:83-90`. No gate gap after resolving that pointer.
- Honesty is grounded: ROADMAP `:21` and set `:68` carry transport/provenance-only wording, `self_reported` done/record integrity, and no ceiling creep. m-2 boot honesty for `charter_loaded` is at `../master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md:145-149` and `:163-169`; m-1 activation non-upgrade is at `../master/domains/m-1-trust-identity/design/2026-07-06-s6-transport-amendments.md:95-103`.

Gate/spec verdict: no ungrounded exit gate found. No current design-of-record obligation lacked an exit-gate hook. The only initially suspicious pointer, `ROADMAP:83-85`, is grounded in the repository-level roadmap lines cited above, not the condensed sprint ROADMAP.

### 2. Root-cause re-verification in code

- F6/F7 four-judge divergence is still present. Render has no special `recipient_picker`; TO/CC are typed as `address_list` with `recipient_picker` at `internal/fieldspec/registry.json:95-97`, but `renderField` falls through to `baseOptions` for unhandled constraints at `internal/fieldspec/render.go:65-82`, yielding no recipient candidates. Validator canonicalizes `address_list` through JSON parse plus canonical marshal at `internal/fieldspec/canonical.go:36-41` and `:63-74`. Lineage still raw comma-splits TO and CC at `internal/lineage/lineage.go:282-291` and `:432-438`. Delivery parses TO/CC via `ParseTyped` but silently returns only `Envelope.To` on parse failure at `internal/store/projections.go:122-141`; rendered relay and index rows also print only `Envelope.To` at `:110-113`.
- F11/F4 candidate mechanics are still present. `PARENT_DISPATCH_ID` is still lane-fillable `parent_picker` in registry at `internal/fieldspec/registry.json:95`; render emits parent candidates/default at `internal/fieldspec/render.go:68-72`; validation re-checks against current `env.ParentCandidates` and bounces outside-lineage at `internal/fieldspec/validate.go:69-75`; lineage candidates are `WokenOn`, `ActiveDispatch`, and accepted records in that dispatch at `internal/lineage/lineage.go:346-371`. `turnContextForSeat` derives `WokenOn` from the raw mailbox tail at `cmd/frank/main.go:389-402`, and `Project` reads mailbox lines without accepted filtering at `internal/store/store.go:151-172`.
- F5 digest churn is still open. `RenderEnv` has config digest, known-A, parent candidates, and floors only at `internal/fieldspec/render.go:11-16`; the digest hashes the rendered form plus config/seat/phase/tier at `:169-187`, and the only structural stripping is per-field `DigestExempt` at `:190-199`. `parent_picker` sets that flag at `:68-72`; there is no class-level `conductor_supplied_volatile` treatment and no recipient picker implementation.
- F9 is partially improved but still open at the chartered grain. The older `Journal.Append` dedupes by content hash but returns an existing intake id without replaying an existing outcome and mints fresh ids from `len(entries)+1` at `internal/intake/journal.go:70-83`. The active `Writer` reconstructs a durable next counter from max existing ids at `internal/intake/writer.go:49-67`, which is reusable, but duplicate content still creates a `Job` for execution under the existing intake id at `:110-124`; there is no outcome replay or in-flight coalescing. Recovery still reasons as `intake - outcomes` at `internal/intake/journal.go:249-276`.
- F13 remains open. Registry membership/seat scope exist at `internal/fieldspec/validate.go:54-61`, but `validateRecordKind` is still a second hardcoded membership judge at `internal/engine/submit.go:143-178`, allowing only owed kinds, config_change, and gate_resolution; authorized registry tokens such as `disposition` and `diagnostics` fall into `unknown record_kind`.
- F17/F12 remain open. The reviewer visibility gate still accepts any accepted operator record with non-empty `ORCH_REVIEW_WAIVER`, run-wide and forever, at `internal/lineage/lineage.go:170-183`. Registry still carries the old `ORCH_REVIEW_WAIVER` `"*"` header at `internal/fieldspec/registry.json:134`; no `waiver_retraction`, `rationale`, `waiver_scope`, or `retracts` rows were present in the registry grep.
- F14 store lock remains open. `Store` has only a process-local mutex at `internal/store/store.go:38-40`; `Open` creates directories and returns without acquiring an interprocess store-root claim at `:43-57`. `Commit` serializes only inside the process at `:60-97`.
- F15/A-3 live mint remains open. `run` handles `-mint` before store open/server startup at `cmd/frank/main.go:72-85`; `mintSeat` refuses if the socket is live and mints directly through `seat.Manager` at `:405-425`. No `seat_mint` record path or live loop mutation exists.
- F16/D-1 is product-overlapped but not closed. The channel client dials once at `internal/channel/server.go:421-430`, and `request` returns errors on write failure, context, or closed connection at `:502-526`. The MCP shim closes its cached client on a failed tool call at `cmd/frank-mcp/mcp.go:157-164`, so the following call can reconnect, but it does not transparently reconnect and retry the same failed call once.
- F3/D-2 remains open. Rejected records do get detail in the record body for field and lineage bounces at `internal/engine/submit.go:47-64`, but the reply `Outcome` carries only state, relay id, intake id, and optional reason at `internal/engine/loop.go:24-29`; successful commit replies omit the recorded rejection detail at `:143-147`. The MCP shim has a re-render special-case that reads the rejected record at `cmd/frank-mcp/mcp.go:221-237`, but that is not the single parity path required for every rejection class.
- B-1 lifecycle/roster has reusable transport pieces, not lifecycle semantics. The channel server tracks active credentials at `internal/channel/server.go:99-109`, rejects duplicate active auth at `:263-275`, and removes the active entry on close at `:217-225`. It also supports per-seat push at `:166-178`. But `Project` ignores arguments at `cmd/frank/main.go:298-303`, the three exposed tools remain submit/project/read at `:274-279` and MCP `cmd/frank-mcp/mcp.go:321-348`, and there is no roster view or derived mint/bound/active table.
- B-2 boot form is not implemented. PHASE contains `SITREP` and no `BOOT` at `internal/fieldspec/registry.json:10-21`, which matches the spec decision, but `charter_loaded` and `dispatch_status` are absent from the registry grep; no lifecycle-gated boot-only render/admission path exists.
- B-3 derived activation is not implemented. There is no `seat_mint` record class in registry (`internal/fieldspec/registry.json:84` record_kind enum and `:125` seat_scope), no lifecycle derivation over mint generation plus first accepted governed submit, and no roster activation record ref.

### 3. Four-bucket verdicts by IN cluster

- Branch-A parenting: still-open. Already-closed: none at branch-A grain. Product-overlapped: `ActiveLineageCandidates`/`turnContextForSeat` can seed server-side stamp context but currently feed lane options. Recommended-next: remove rendered PARENT, accept optional hint fields, stamp PARENT at commit from accepted turn context, and keep class-lineage gates. Primary bucket: still-open.
- Codec: still-open. Already-closed: validator/delivery share `ParseTyped` for canonical `address_list`. Product-overlapped: `DeliveryRecipients` already has a parse helper and per-recipient push path consumes it. Recommended-next: export one codec, make render/gate/delivery/projection call it, delete raw comma-splits, and treat parse failure after validation as an invariant fault. Primary bucket: still-open.
- A-1 digest: still-open. Already-closed: per-field `DigestExempt` exists. Product-overlapped: parent picker exemption is the local proof shape. Recommended-next: promote exemption from per-field flag to a conductor-supplied volatile class and add recipient/dynamic option surfaces. Primary bucket: still-open.
- A-2 intake: still-open. Already-closed: writer reconstructs durable monotonic `next` from max seen ids. Product-overlapped: content-hash map and serialized writer are good seeds. Recommended-next: add outcome replay and in-flight duplicate coalescing; preserve unique intake-to-outcome invariant. Primary bucket: still-open.
- A-3 live mint: still-open. Already-closed: `seat.Manager.Mint` and credential custody exist. Product-overlapped: admin CLI gives a genesis-time fallback. Recommended-next: move live mint to an operator-authored `seat_mint` loop record; derive endpoint/binding completion from the committed record. Primary bucket: still-open.
- A-4 plus section D lock: still-open. Already-closed: intra-process store mutex only. Product-overlapped: none sufficient. Recommended-next: acquire canonical store-root lock before recovery/open serving, typed path-free refusal for live loser including reads, proof-of-death takeover, alias-path fixture. Primary bucket: still-open.
- Section B projection/anchor: still-open. Already-closed: `read` by relay id exists; mailbox projection exists. Product-overlapped: redacted read and project plumbing are usable. Recommended-next: default project to accepted addressed records, add explicit audit scope parameter, derive turn anchors from accepted graph only. Primary bucket: still-open.
- Section C waivers plus retraction: still-open. Already-closed: no durable scoped waiver model. Product-overlapped: owed disposition/id_ref pattern can be reused for `retracts`. Recommended-next: replace header flag with operator-only waiver record carrying rationale/scope plus `waiver_retraction`, evaluated by commit order. Primary bucket: still-open.
- F13 three-layer record_kind: still-open. Already-closed: registry membership and seat-scope already run before the hardcoded switch. Product-overlapped: per-kind owed/config/gate checks are reusable. Recommended-next: reduce `validateRecordKind` to layer-3 required-field checks and stop rejecting registry-authorized tokens. Primary bucket: still-open.
- D-1 shim reconnect: product-overlapped but still-open. Already-closed: MCP shim drops a failed cached client so a later call can reconnect. Product-overlapped: `ensureClient`/`closeClient` and credential-file handling are reusable. Recommended-next: on first failed call over a previously live connection, reconnect and retry that same call once, then surface typed unreachable. Primary bucket: still-open.
- D-2 bounce reply detail: still-open. Already-closed: rejected records record formatted detail. Product-overlapped: MCP re-render detection shows one narrow read-back path but is not general. Recommended-next: include the same formatted detail in `Outcome` for every rejected committed record. Primary bucket: still-open.
- B-1 lifecycle plus roster: still-open. Already-closed: channel active map, duplicate-active guard, and per-seat push exist. Product-overlapped: those directly seed `bound_now` and nudge behavior; redaction pattern seeds roster hygiene. Recommended-next: derive minted/active from records, bound from active channels, add scoped `project(view=roster)`, pre-active admission check, and lifecycle fixtures. Primary bucket: still-open.
- B-2 boot form: still-open. Already-closed: using existing `SITREP` token and no `BOOT` enum is already correct. Product-overlapped: form render/validate machinery exists. Recommended-next: add `charter_loaded` and `dispatch_status`, lifecycle-gated boot render, exact-form pre-active admission, and boot-required typed rejections. Primary bucket: still-open.
- B-3 derived activation: still-open. Already-closed: no persisted activation marker exists in current registry, which is correct. Product-overlapped: accepted record tables and seat binding records can drive derivation. Recommended-next: derive active as first accepted governed submit after current mint-generation pivot, never from read/project/rejected/held/addressed traffic. Primary bucket: still-open.
- Registry pass: still-open. Already-closed: delivery_state enum is byte-exact and no activation-marker row is present. Product-overlapped: existing registry/seat-scope mechanics fit the change. Recommended-next: add exactly seven rows (`parent_hint`, `parent_hint_honored`, `parent_provenance`, `routing_ref_honored`, `rationale`, `waiver_scope`, `retracts`), record classes `waiver_retraction` and `seat_mint`, retire `ORCH_REVIEW_WAIVER`, and keep no envelope migrator. Primary bucket: still-open.

### 4. Duplicate/promote inventory

- Promote `fieldspec.ParseTyped`/canonical marshal as the address-list codec seed; do not write a second parser. The delete target is the raw comma split and delivery silent-drop behavior.
- Promote `Field.DigestExempt` as the mechanical seed for A-1, but change the rule to class-based conductor volatility. Do not enumerate only today's field ids.
- Promote `intake.Writer`'s serialized request path, content-hash map, and recovered max-id counter; add replay/coalescing rather than a second journal.
- Promote existing crash/replay harnesses for A-2/A-3/A-4/F11 legs; the archived dogfood store belongs in the replay fixture path, not a hand-built new harness.
- Promote `store.DeliveryRecipients` and `channel.Server.PushTo` for multi-recipient delivery once codec truth is unified.
- Promote `server.active` and duplicate-active auth rejection as the runtime `bound_now` seed, not as durable lifecycle truth.
- Promote `redactedConfigChangeRead` as the pattern for scoped roster/audit outputs with no credential/path material.
- Promote `seat.Manager.Mint` and current credential custody; the change is the commit-loop call site and canonical record, not credential format.
- Promote owed disposition/id_ref handling (`disposes_owed`, `OwedOpenProjectionIntent`) for waiver retraction semantics.
- Promote `PendingDeliveryFor`/`PendingDeliverySeats` only as recovery-nudge helpers; do not confuse pending mailbox presence with accepted projection truth.

### 5. Fidelity-surface enumeration

- m-1 fidelity packet: PARENT stamp semantics; accepted-graph anchor rule; default project accepted scope and audit scope; waiver scope/retraction/effective-state semantics; store-root identity, lock timing, loser reads refusal, and proof-of-death takeover; activation edges and generation boundaries; roster exposure limits; credential custody and D4/D5 wording.
- m-2 fidelity packet: one `address_list` codec and canonical projection truth; recipient picker render; digest-exempt render class for volatile recipient/options display; registry seven rows plus two record classes and old waiver-header retirement; three-layer `record_kind`; boot form rows and no shared-vocab expansion; I-PH field/class shape for codec, boot, hint, and waiver bounces.
- m-7 guide packet: commit-loop PARENT stamping; intake writer outcome replay/coalescing and durable counter; live mint mutation and derived endpoint completion; store lock acquisition before recovery; D-1 shim retry; D-2 reply shaping; lifecycle derivation/recovery; roster `project` parameter; active channel binding; no new seat verbs; step-exit live legs.

### 6. Claim-boundary probes

- Byte-exact enum risk points: registry delivery_state is exactly `accepted`, `rejected`, `held` at `internal/fieldspec/registry.json:71`; do not add lifecycle/boot states to delivery_state. `record_integrity` remains observed/self_reported/mixed at `:75`; do not relabel done-state as observed.
- Three-verb surface risk points: server `ToolSet` is submit/project/read/describe internally at `internal/channel/server.go:24-31`; seat description returns only submit/project/read at `cmd/frank/main.go:274-279`; MCP exposes only submit/project/read at `cmd/frank-mcp/mcp.go:321-348`. Roster and audit must be project parameters, not new verbs.
- I-PH new surfaces: roster path-free output is seat/role/state/relay id/timestamp only; boot-required detail should enumerate `<field>:non-boot-before-active`; lock refusal must not expose raw store/socket paths; hint fallback detail is a field/class plus `parent_hint_honored`, not a validation claim.
- Honesty ceiling: activation is liveness bookkeeping, not identity-strength upgrade; hint fallback is not hint validation; store lock is confusion-resistant ops with D5 same-uid residual; `charter_loaded` is self_reported; done-state and `record_integrity` remain self_reported where Step-2 observe is absent.
- VP-W3 activation-marker probe: current registry grep found no activation-marker row and no new s6 rows/classes; the design-of-record explicitly removed the persisted activation marker at `../master/S6-AMENDMENT-SET-2026-07-06.md:6-10`, declares no marker row in boot/activation seams at `:57`, and says the registry pass has exactly seven named rows with no activation-marker row at `:61`. Historical relays still contain stale marker language, but current r3 set plus r5 m-7 text override them; treat broad historical hits as a watchpoint, not as authority.
- No-perf fence: latency was exonerated at `../master/TRANSPORT-FINDINGS-2026-07-06.md:66-68`; ROADMAP marks engine performance work of any kind OUT at `docs/sprints/2026-07-06-s6-slice-6/ROADMAP.md:14-15`. A-1/D-1 may improve perceived UX by removing bounces/retry choreography, but they do not authorize optimization work.

### 7. Battery and status proof

- Incoming dispatch lint: `python3 ~/.claude/skills/tools/relay-lint.py .relays/s6/s6-core-audit/AUDIT-orchestrator-planner-20260706-232500.md` -> OK.
- Own relay lint after write: `python3 ~/.claude/skills/tools/relay-lint.py .relays/s6/s6-core-audit/AUDIT-implementer-20260706-232843.md` -> OK.
- Baseline/current proof: `git rev-parse HEAD s5-close` -> `79238ca871e99061db8989dc35f4e07a6dc0fe9e` and `7e5c527713d1450ea044d56c15b4e82ae2f36c5e`; `git diff --name-only s5-close..HEAD` -> ROADMAP/RECONCILE docs only.
- Fresh uncached battery: `go clean -testcache && go test ./...` passed. Result shape: 23 `ok` packages and 2 packages with no test files (`cmd/frank`, `test/seatproc`).
- Status before relay write: `git status --short` empty; branch line `## main`.

ACTIONS_GIT_REF: read-only audit; no source/test/tracked-doc edits. Wrote `.relays/s6/s6-core-audit/AUDIT-implementer-20260706-232843.md` and appended the matching `.relays/s6/INDEX.md` row; `.relays/` is gitignored relay transport.
FINAL_GIT_STATUS_SHORT: none - clean tree (tracked files clean; relay artifacts are gitignored by design).
Next requested action: orchestrator-planner reconciles this independent audit with the sibling audit, then issues any DESIGN-phase dispatch from the reconciled packet.
