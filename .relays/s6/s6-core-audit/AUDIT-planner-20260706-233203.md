## AUDIT — s6-core.planner independent audit: the transport-fix surface at `s5-close` (all root causes reproduced at my own cites; 15 clusters still-open; zero spec-gap escalations; two named design-decomposition items)

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: s6-core-audit
PARENT_DISPATCH_ID: s6-dispatch
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
FROM: s6-core.planner
TO: s6.orchestrator-planner
CC: operator, s6.orchestrator-reviewer
SUBJECT: s6-core.planner AUDIT return — every cited root cause reproduced in live code (several at sharper cites than the spec's); spec-to-exit-gate map complete with zero gaps; all 15 IN clusters still-open at the chartered grain with a rich promote-don't-rebuild inventory; the re-mint choreography and the F11 traffic-replay interleaving named as design-decomposition items (not escalations)

**Independence statement:** I have not read, listed, or opened `s6-core.implementer`'s audit relay (or any draft of it) before filing this. My only exposure to that seat this run is its boot ACK, filed before the AUDIT dispatch existed. All line cites below are from my own reads this session at `main@e9ed6ab` (code surface = tag `s5-close` = `7e5c527`; `git diff --stat s5-close..HEAD` = the two s6 sprint docs only — my run).

---

### 1. Spec-to-exit-gate map (ROADMAP §Exit gate ↔ the set + constituents)

| gate line | spec ground | verdict |
|---|---|---|
| GRILL_LOCK three (F11 dogfood replay · hint honored/fallback · concurrent-accept-no-parent-bounce) | `GRILL-LOCK-parenting-fork-2026-07-06.md` §Design-lock impact (fixture bullet) + m-1 §A.4 + m-1 §E consolidated list | grounded |
| FX-A1a..FX-B1g (18) | m-7 r5 fixture roll-up (11 rows: A1a/A1b/A2a/A2b/A2c/A3a/A3b/A4a/A4b/D1/D2) + B-1.5 (7 rows: B1a..B1g) = 18 exactly — my count | grounded |
| m-1 §E set | m-1 §E "Fixture obligations (consolidated)" — six families (F11 replay · hint legs · polluted-archive projection filter + audit scope · reject-never-anchor · waiver scope/retraction/re-arm/non-operator · lock race/alias/kill-9/typed-refusal) | grounded |
| m-2's set | m-2 §7.1–7.6 + §11.5 (codec round-trip/full-set · archive relays one-encoding · three-layer incl. genesis negative · parent no-divergence · waiver-as-record · recipient_picker DigestExempt · boot renders-pre-active/un-bounceable · shared-vocab negative · self_reported label) | grounded |
| E2 floors (battery · byte-exact enum · three-verb · I-PH new surfaces) | set §Constraints held + m-1/m-7/m-2 constraint blocks; enum floor currently guarded by `test/fixtures/sweep_test.go:28` (the only `"bounced"` grep hit in the tree is the guard itself — my run) | grounded |
| Step-exit legs (a)–(d) | top-level `ROADMAP.md:83-85` (exit test) + the :86-90 operator ruling (in-step; fixed conductor; §7-apply first act; F11 regression leg) + set §Build-slice obligations "step-exit test" row | grounded |
| Honesty lines | set §Constraints held + findings §Honesty ceiling + m-2 §11.5 (`charter_loaded` self_reported) | grounded |

**No gate line without spec ground; no spec obligation without a gate line.** One mapping note (not a gap): m-1 §F.6's four activation fixtures land via the m-7 FX-B1c/B1f/B1g equivalents — the DESIGN fixture table should state that mapping explicitly so §F.6 can't fall between "m-1 §E set" and "FX-B1*". Ops non-fixture (pre-allowlist `mcp__frank__*`) rides gate 3's parenthetical — an operator-side checklist line, not a fixture.

### 2. Root-cause re-verification in code (my own cites; deltas vs the spec's cites named)

| finding | reproduced at my cites | notes / deltas |
|---|---|---|
| F6/F7 four judges | Render: registry rows `TO`/`CC` carry `fill_constraints: recipient_picker` (my registry dump), but `render.go:65-88` renderField has **no `recipient_picker` case** → default branch → `baseOptions` → nil (no enum_set/options) — the form offers 0 recipients. Validator: `validate.go:127-137` → `canonical.go:36-40,62-75` exact canonical round-trip required. Lineage gate: `lineage.go:282-292` (`addressedTo`) + `:432-439` (`addressedInHeader`) raw `strings.Split(",")`. Delivery: `projections.go:122-142` — `addressListHeaders` parse-fail → `:135-136 if !ok { return recipients }` = **silent drop to `Envelope.To` only**. `Envelope.To` = one verbatim string (`record.go` Envelope). | fully reproduced |
| F11/F4 | `lineage.go:346-372 ActiveLineageCandidates` (WokenOn + ActiveDispatch + same-dispatch accepted); `cmd/frank/main.go:389-403 turnContextForSeat` — `WokenOn` = **last raw mailbox line** (`store.go:151-173 Project` reads the mailbox file verbatim, no state filter); bounce sites `validate.go:69-76` (`active-lineage-empty` / `outside-active-lineage`) | fully reproduced |
| rejects-can-anchor (F10/§B) | Live path: rejects get nil intents (`submit.go` reject returns + `store.go:80-82` auto-intents only for Accepted) — BUT `projections.go:38-46 RebuildProjections` applies `canonicalProjectionIntents` for **ALL records regardless of delivery state** → every recovery re-floods mailboxes with rejects → `Project` serves them → a reject can be `WokenOn`. Exactly m-1 §B's carry-forward ("the archived s5 store already holds rejected relay ids in mailbox files") — the pollution writer is the REBUILD, not the live commit. | sharper cite than spec: the fixture must gate the rebuild path, not just fresh commits (m-1 §B already requires this) |
| F5 | `render.go:169-200 digestRenderedForm` hashes the full form; `formForDigest :190-200` strips options only for `DigestExempt` fields; the ONLY DigestExempt field is `parent_picker` (`render.go:68-72`); `seat_allowed_values` recipient/kind sets + monotonic floors are digest-covered | flag-vs-class confirmed |
| F9 | **The live surface is `writer.go`, not `journal.go`** (delta vs the spec's `journal.go:78-83` cite): `writer.go prepare()` — content-hash hit reuses the intake id **and still enqueues the Job** (`Run()` pushes to the loop unconditionally on ack success) → full pipeline re-execution per retry → multiple outcome records per intake id. Id mint = `w.next`, **in-memory**, seeded `max(surviving journal entries)+1` at writer construction — NOT durable: GC removes drained segments (`gc.go Pass` → `os.Remove`), so GC + restart can reuse historical ids. `journal.Append`'s `len(entries)+1` path has **zero live callers** (my grep: writer-only submit path) — a second, dead-path id judge to delete or unify, not the live defect site. No 1:1 guard anywhere: `store.go:60-97 Commit` never checks intake uniqueness; `tables.OutcomeByIntake` exists (`tables.go:136-138`) but nothing consults it as a guard. | reproduced with a materially sharper mechanism map |
| F13 | `submit.go:143-179 validateRecordKind` switch cases: ""/owed_item/owed_disposition/config_change/gate_resolution; default `:176-178` → "unknown". Registry (my dump): record_kind enum = 7 members; seat_scope operator=[6 kinds], `*`=[diagnostics]; **`disposition` (operator-offered) and `diagnostics` (offered to every seat) still bounce `unknown` at s5-close**. Delta vs the findings text: `gate_resolution` gained a case since `67ee23e` — the finding's specific token is fixed, its class is not. Layers 1–2 already live in `reg.Validate` (`validate.go:54-58` enum membership; `:59-61` seat-scope — `genesis` correctly in no scope → seat-scope reject today). The m-2 §2.6 claim structure verified true in code. | reproduced + a useful delta for fixtures |
| F17 | `lineage.go:170-184 checkReviewerVisibility` — ANY accepted operator record with non-empty `ORCH_REVIEW_WAIVER` passes, run-wide, forever; no scope read; record_kind enum has no retraction class. Registry row: `type: text`, `seat_scope: {operator: ["*"]}`, and the mis-typed `lineage_role: routing_ref` m-2 §4 flags — all verified in my registry dump | fully reproduced |
| F14 | grep `flock|Flock|lockfile|LOCK` across `internal/store`, `internal/recover`, `cmd/frank` = **zero hits**; `recover.go` runs phases 0–4 (crashpoints `recovery_post_phase0..4` at :41-101) with no claim step — A-4's "phase −1" slot genuinely absent | fully reproduced |
| F15 | `main.go:405-426 mintSeat` — refuses when the socket is live (`:413-414`); enrollment = restart. **Plus my finding (see §Named items):** `seat/binding.go:68-91 Mint` typed-rejects an existing seat (`ErrSeatAlreadyBound`, the S1 shape-(b) lock) — **no re-mint path exists at all**, yet FX-B1g requires re-mint/generation behavior | reproduced + gap named |
| F16 | `cmd/frank-mcp/errors.go` `errConnectionLost = "shim:connection-lost"`; `scrubError` maps EOF/broken-pipe/closed-conn → it; `mcp.go:157-165 handleToolCall` closes the client on call error and returns the scrubbed class; the NEXT call re-dials via `ensureClient` (`mcp.go:266-286`) — the two-call UX, exactly | fully reproduced |
| F3 | **Two layers, sharper than the spec's framing:** (i) `loop.go:143-147` — the Outcome for a handler-level reject carries `{State, RelayID, IntakeID}` and **no detail** (detail lives only in the rejected record's Body via `bounce.Format`); (ii) the shim back-fills detail for exactly ONE class — `mcp.go:221-241 submitNeedsReRender` reads the rejected record and pattern-matches `form_digest`+`re-render`, then `reRenderResult :243-264` synthesizes a violations payload. So the observed re-render/lineage parity gap is engine-Outcome + shim-hack shaped; D-2's "one reply-shaping path" lands engine-side and the shim's pattern-match hack should retire with it (DESIGN note) | reproduced with a build consequence |

Nothing the spec asserts about the code failed to reproduce. Two spec cites are imprecise in a build-relevant way (F9 live surface = writer.go; F3 = Outcome carriage + shim hack) — folded into DESIGN inputs, not escalations.

### 3. 4-bucket verdicts (one per IN cluster)

All fifteen clusters: **PRIMARY_BUCKET: still-open** at the chartered grain. Per-cluster still-open evidence + the already-closed sub-mechanisms to promote:

1. **Branch-A parenting** — still-open (`parent_picker` live in registry + render; no commit-time PARENT stamp). Promote: `seat.Stamp` NF-S1 idiom (the stamp locus template), `turnContextForSeat` plumbing (re-pointed to accepted-graph state).
2. **The codec** — still-open (no exported decode; four judges live). Promote: `ParseTyped` `address_list` (`canonical.go:36-40`) IS the single decode's seed — §2.1 says unify, not rewrite.
3. **A-1 digest** — still-open at the class grain; the exemption MECHANISM is already-closed at flag grain (`DigestExempt` + `formForDigest`) — promote flag→class (`conductor_supplied_volatile`).
4. **A-2 intake** — still-open (replay absent; counter volatile; re-enqueue-on-dedupe live). Promote: `tables.OutcomeByIntake` (live, populated every commit) as the replay lookup; the dormant `tables.ContentHash` map (exists at `tables.go:23,:43,:100`, never populated — wire it, don't rebuild); `writer.hashes` as the in-flight dedupe seed.
5. **A-3 live mint** — still-open (admin-time only). Promote: `seat.Manager.Mint` (mechanism unchanged, call-site pivots), the `config_change` compound-record + operator-authorship pattern (`submit.go:185-210`) as the `seat_mint` template, the derived-work-completion mechanism (`obligation.CompleteAuto` + `CompletionIndex`) for the endpoint leg, F11 applicability map gains the class (s4 `config-change` precedent).
6. **A-4 + §D lock** — still-open (zero lock machinery; recovery unclaimed).
7. **§B projection/anchor** — still-open both halves (unfiltered `Project`; rebuild pollution; raw mailbox-tail `WokenOn`). Promote: the redacted-read view pattern (`main.go:335-375`) as the audit-scope/view precedent.
8. **§C waivers + retraction** — still-open (presence-only gate; no record class). Promote: the `resolves_gate`/`disposes_owed` already-resolved idiom (`submit.go:154-171`) is the retraction-effective-state template; `tables.Waivers` (`tables.go:151-153`) is the gate-lookup seed.
9. **F13 three-layer** — still-open at the unification grain (delete `validateRecordKind`'s membership judging, keep layer 3); layers 1–2 already-closed inside `reg.Validate` (verified above) — the build is a REDUCTION plus per-kind rows for the new classes, not new machinery.
10. **D-1** — still-open (close-then-redial = two calls; no in-call transparent retry).
11. **D-2** — still-open (Outcome carries no detail; the shim hack covers one class).
12. **B-1 lifecycle + roster** — still-open (no lifecycle tables/roster/admission predicate). Promote: the server's per-credential `active` index (`server.go:106,:266-274`) IS `bound_now`'s runtime seed; recovery phase-3 is B-1.1's stated derivation slot.
13. **B-2 boot form** — still-open (no boot fields; no lifecycle-gated render).
14. **B-3 derived activation** — still-open (no derivation anywhere). Promote: `tables.Build`/OnCommit as the fold host.
15. **Registry pass** — still-open (my dump: none of the seven rows exist; record_kind enum lacks `waiver_retraction`/`seat_mint`; the `"*"` waiver row live with the mis-typed `lineage_role`).

already-closed: none at the chartered grain (sub-mechanisms enumerated above). product-overlapped: none. recommended-next: **DESIGN** (the build design/decomposition against the locked set).

### 4. Duplicate/promote-don't-rebuild inventory (all dispatch candidates audited + five additions)

Every dispatch-named candidate verified real and reusable: crash harness + crashpoint registry (`crashpoint.Names`, child-SIGKILL in `f11_test.go`, `applicability_map.go`) · S3 replay harness (`test/replay/harness.go` drives `engine.SubmitHandler`) · **the s5 archive leg** (`test/replay/zeroloss/zeroloss_test.go:66-78 TestReplayOptionalArchiveStore`, env-gated `FRANK_S5_REPLAY_STORE`; archive verified on disk: 41 records at `~/frank-archives/frank-team-store-s5-dogfood-20260706`) — NOTE: zeroloss is a READ-integrity replay; the F11 regression fixture additionally needs a TRAFFIC re-drive (reconstruct submit sequences from the archived records/journal and re-submit against the fixed conductor) — composes with, does not duplicate, the zeroloss leg · `DigestExempt` mechanism · `ParseTyped address_list` · `PushTo` per-recipient + redacted-read view · `seat.Manager`/`mintSeat` · derived-work completion · owed id_ref idiom. **My additions:** `tables.OutcomeByIntake` (live) · `tables.ContentHash` (dormant — wire) · `writer.hashes` · `tables.Waivers` · the channel `active` credential index (= `bound_now`). **Red-flag check: nothing in the set requires rebuilding any of these.** `bounce.Format` is already the single formatter — D-2's gap is carriage (Outcome), not formatting.

### 5. Fidelity-surface enumeration (packet-routing input)

- **m-1** (store/lineage/waiver/lock/activation): PARENT stamp semantics + the three hint rows' anchor meaning · §B `project()` default scope + accepted-graph turn context + the REBUILD-path filter (`projections.go:38-46` is a store-semantics change) · §C waiver class/retraction/commit-order gate lookup · §D I1-P + canonical root identity · B-3 activation derivation + §F.1 derives-only list · **re-mint/binding-table credential replacement** (supersedes the S1 shape-(b) single-generation posture per its own "if S1 has no remint op" framing; m-1 §13.3 credential-lifecycle carry — named below) · the A-2 1:1 intake↔outcome invariant at the store/commit grain.
- **m-2** (codec/registry/boot-form/render/validate): the exported codec + all four judge call-sites · the registry pass (exactly seven rows + `waiver_retraction`/`seat_mint` enum members + `"*"` header retirement + the two boot fields + the `seat_mint` row shape) · `recipient_picker` implementation · F13 `validateRecordKind` reduction to layer 3 · A-1 digest-input class semantics (shared half with m-7) · B-2 lifecycle-gated render · D-2 detail shape (`Field:Class` parity).
- **m-7 guide** (engine/loop/lifecycle/runtime — most of the diff): A-1 digest input · A-2 writer/counter/replay/re-enqueue · A-3 mint-as-loop-mutation + derived endpoint work + F11 class row · A-4 phase −1 claim + refusal + takeover choreography · D-1 shim retry · D-2 Outcome carriage · B-1 lifecycle tables + B-1.2a admission allowlist + roster projection + recovery re-derivation · per-seat turn-context maintenance + the commit-time PARENT stamp locus.

### 6. Claim-boundary probes

- **(a) enum + three-verb threat points:** roster/audit views must land as `project` payload parameters — the concrete risk site is `channel.ToolSet`/`names()` (`server.go:364-389` returns exactly submit/project/read; adding a tool entry is the violation shape). B-1's `boot-required` is a rejection CLASS, never a fourth delivery state; the A-4 loser's refusal is a process exit, never a delivery state; `held` stays off the seat default projection under §B. Enum floor guard already exists (`sweep_test.go:28`); extend its surface sweep over the new outputs.
- **(b) I-PH fixture class for the NEW surfaces:** extend the existing grep-matrix pattern (`test/fixtures/*iph*`) over four new payload families — roster rows (names/roles/states/relay ids/timestamps only), `boot-required` per-field detail (`<field>:non-boot-before-active` — field ids only), the lock-refusal diagnostic (typed, holder identity per m-1's lock-content semantics, no path), hint flags (`parent_hint*` carry relay ids only). D-2's byte-equality fixture (reply detail ≡ recorded Body) inherits path-freedom from `bounce.Format` (the drop-Reason valve is already fixture-proven at `bounce/formatter_test.go`).
- **(c) over-claim temptations:** activation is liveness bookkeeping, never an identity-strength upgrade (§F.1 ceiling — roster wording is the risk surface); hint fallback is NOT hint validation — never phrase a fallback-stamped record as having a "verified" parent (intent+outcome+flag is the honest triple); the lock is confusion-resistant ops protection — same-uid can delete any lock, D5 residual verbatim (§D.4); A-2 stays "at-least-once intake, exactly-once EFFECT" — never unqualified exactly-once (the S2 pin); "un-bounceable boot" = structural absence of the digest/parent/divergence classes post-A-1/branch-A, not immunity from auth or admission rejections.
- **(d) [VP-W3] grep:** `activation.marker|activation_marker` over `docs/ internal/ test/ cmd/` — the only hits are the s6 ROADMAP's own prohibition clauses. No surviving marker language in any build-plan input. Clean.
- **(e) no-perf fence misread risks:** A-1's "removes the race class" is a liveness/correctness claim — not license to touch anything for speed; the known hot-path rescans (per-submit `tables.Build` in `main.go process()`; `gate.Complete` store scans — the s2 F-3/F-P1 lineage) sit inside files this slice edits and must NOT be optimized while we're in there; D-1's single retry is UX, not latency work; "the engine is most of the diff" licenses the set's changes only.

### 7. Battery + status proof

Boot-run citable per the dispatch: **same code surface** — my own uncached run this session at `main@e9ed6ab` (`go clean -testcache && go test ./...` = exit 0, 23 `ok` packages + 2 no-test-file), with `git diff --stat s5-close..HEAD` = the two s6 sprint docs only, so the tested surface is `s5-close` exactly — E2, my runs. Enum-floor grep re-run this audit pass (§6a). No re-run needed between boot and this relay: no code commit landed (fresh status below).

### Named items (neither is an escalation; both are DESIGN-phase decomposition inputs)

1. **Re-mint choreography is mechanism-underspecified relative to FX-B1g.** The SEMANTICS are fully locked (generation = committed `seat_mint` pivot in commit order; fresh activation per generation; §F.1). But today `Mint` typed-rejects existing seats (`ErrSeatAlreadyBound` — the S1 shape-(b) lock, which itself anticipated a future remint op), and no constituent specs the remint mechanics: credential replacement as `seat_mint` derived work, old-credential rejection at auth, binding-table update semantics. DESIGN must decompose this explicitly and it routes to the m-1 fidelity packet (credential-lifecycle carry §13.3). Not a spec contradiction.
2. **The F11 traffic-replay fixture needs an interleaving grain decision.** The livelock is a RACE (foreign accepts between render and submit); a serial re-drive of the archived 41 records proves anchor validity but not race-freedom. The GRILL_LOCK's third fixture (concurrent-accept-during-submit) covers the race synthetically; DESIGN should state that the archived-traffic leg proves "the recorded pattern completes without livelock" while the concurrent fixture proves the race class — two legs, one claim, no overstatement.

Duplicate/already-built gate: run (§3/§4) — nothing to rebuild; promote inventory attached.
Boundary contract: not applicable at AUDIT (read-only); the §5 seam halves feed the DESIGN contracts.
Operator-judgment items: none at this phase.
Risks / reject-or-narrow gates: none reject-shaped; the two Named items are the watch surfaces.
Questions for the orchestrator: none blocking; confirm at reconciliation that the two Named items ride the DESIGN dispatch as constraints rather than re-opening any lock (my reading: they don't touch locked text).

ACTIONS_GIT_REF: none — read-only audit; no source/test/tracked-doc edit; this relay + one INDEX row written under gitignored `.relays/s6/`.
RELAY_LINT: exact-file passed — `python3 ~/.claude/skills/tools/relay-lint.py <this file>` = OK; relay-root run with `--relay-root .relays/s6` returned only the lint-exempt `INDEX.md` header noise plus OK for this file.
FINAL_GIT_STATUS_SHORT: none — clean tree.
