## PLAN — s6-core.planner gated PLAN: s6-slice-6-plan r1 locked to the approved design; PLAN-REVIEW requested from the pair Implementer (the F2 plan gate)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s6-core-plan-lock
PARENT_DISPATCH_ID: s6-core-design-r2-review-implementer
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no for this relay — remaining human gates: the three external verdicts precede any dispatch; live-seat designation + step-exit legs + s6-close are downstream operator gates
DESIGN_LOCK_ID: s6-slice-6-design
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s6-grill-s6-core
PLAN_LOCK_ID: s6-slice-6-plan
IN_REPLY_TO: PLAN-orchestrator-planner-20260707-010002.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, s6.orchestrator-reviewer, operator
SUBJECT: gated PLAN — s6-slice-6-plan r1 at main@fe55082 (16 tasks per design §17; red-first; grill-locked shapes verbatim; the three review watchpoints bound as task criteria); your PLAN-REVIEW = the plan gate; parent edge = the approving DESIGN-REVIEW, kept across ALL revisions

**Lock chain:** this PLAN consumes `DESIGN_LOCK_ID: s6-slice-6-design` (r2 @ main@a499bc3, GRILL_LOCK §18 `s6-grill-s6-core`) and parents the approving DESIGN-REVIEW `s6-core-design-r2-review-implementer` (verdict approve, same DESIGN_DOC_ID, 20260707-005333). Per the s2 lesson restated in PROCEED-TO-PLAN: this parent edge is KEPT across all plan revisions.

**Plan of record:** `docs/sprints/2026-07-06-s6-slice-6/plans/s6-slice-6-plan.md` **r1 = main@fe55082** (docs-only commit). Locked scope = its File map (the SCOPE_DIFF universe); locked ordering = Tasks 1–16 (design §17 sequence with D-2 early and the PARENT registry flip landing atomically inside Task 6 — the one-logical-pass/two-commits sequencing note stated in Task 3); acceptance criteria = the exit-gate mapping section; out-of-scope = the verbatim fence (root README included — no falsified claim line at plan time, re-checked at T15.5 with an EARLY fence-ASK stop rule).

**Bound into the plan (verify these in your review):**
1. The grill-locked shapes verbatim (§18): re-mint = binding replacement (T11) · flock phase −1 (T10) · segment-header high-water (T8) · roster seven fields (T12) · F11 two-legs-one-claim (T14/T6) · the registry reading 7+2+1+2 with NO activation-marker row (T3, negative fixture-guarded at T3.1/T15).
2. The three DESIGN-REVIEW watchpoints as task criteria: custody HANDOFF wording + fidelity-eyes stop rule (T11) · waiver fill-time-absence floor wording (T9) · the GC+restart id-reuse leg red-first (T8.1, named test).
3. Red-first on every exit-gate fixture with evidence in the task log; battery + vet green per task; `-race` where named; commit-per-task `s6:` prefix.
4. Boundary contract = design §15 seam table (each task names its consumed halves; nothing re-owned).
5. External-verdict fold rule: the m-1/m-2/m-7 packet verdicts bind on arrival verbatim (the s4 F-S4-M1 pattern); a must-revise re-reviews only the named surface; **no `DISPATCH IMPL` before all three approves are on record in `.relays/s6/`** — quoted inert, per the PROCEED-TO-PLAN conditions.

**Your PLAN-REVIEW (the F2 plan gate):** `PHASE: PLAN-REVIEW`, read-only, parented to THIS relay (`s6-core-plan-lock`), verdict `{approve | must-revise | reject-narrow | human-decision-required}`. Sharpest-read invitations: task-order dependency holes (the T3/T6 registry split; T5 before the detail consumers; T8 before T13's retry-safety claim; T11 generations before T12); any task whose fixture set under-covers its design section; any wording that overstates (the T14 claim line; "un-bounceable"; the lock's D5 line); the legacy-test update surface in T6 (itemized, bounded, no assertion-weakening).

**After your approve:** I hold for the three external verdicts; on {approve ×3 · SCOPE_DIFF all-in vs the File map · no trigger/collision/amendment/OUT} the delegated dispatch issues from this seat parenting your approving PLAN-REVIEW, restating the conditions with evidence.

ACTIONS_GIT_REF: plan doc r1 = main@fe55082 (docs-only, committed before this relay); this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree.
Next requested action: s6-core.implementer files the PLAN-REVIEW under this thread (stamp strictly greater than this relay's).
