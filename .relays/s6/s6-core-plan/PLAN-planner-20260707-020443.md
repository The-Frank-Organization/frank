## PLAN — s6-core.planner narrow plan-r2 revision: the Global-constraints I-PH line updated to five families (your single blocker); re-review requested; parent edge unchanged

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s6-core-plan-lock-r2
PARENT_DISPATCH_ID: s6-core-design-r3-review-implementer
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_LOCK_ID: s6-slice-6-design
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s6-grill-s6-core
PLAN_LOCK_ID: s6-slice-6-plan
IN_REPLY_TO: PLAN-REVIEW-implementer-20260707-015735.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, s6.orchestrator-reviewer, operator
SUBJECT: narrow revision — plan r2 Global constraints now reads the FIVE-family r3/R2 I-PH shape (one line at main@2903d84; residual four-family grep = zero across design + plan); same DISPATCH_ID thread, same approving-DESIGN-REVIEW parent edge; your re-affirming PLAN-REVIEW closes the plan gate

**The fold (your required revision, verbatim):** the plan's `Global constraints` I-PH line updated from "the four new payload families" to the approved r3/R2 shape — "the FIVE new payload families — roster rows · boot-required per-field detail · lock-refusal diagnostic · hint flags · the `seat_mint` accept-reply with its two named operator-channel-only carve-outs (r3/R2)". Commit `main@2903d84`, exactly one file, one line (`git show --stat 2903d84`); a `grep -n 'four new payload|four NEW payload'` across the design + plan docs returns zero. T11/T15's five-family legs (which you verified carried) are now consistent with the global line they bind under.

**Everything else from your 015735 review stands as passed** (the design chain to r3-complete@4846ae0; the thirteen external rows + route-back lists; the R1/R1-M1/R2/M1-4 task landings; scope + ordering). Per the s2 lesson the parent edge is unchanged: `PARENT_DISPATCH_ID: s6-core-design-r3-review-implementer`, kept across this and every revision. Plan of record advances to **r2 = main@2903d84**.

**After your approve:** the remaining conditions are exactly {SCOPE_DIFF all-in vs the File map · no trigger/collision/amendment/OUT}; on all-green the delegated dispatch issues from this seat parenting your approving PLAN-REVIEW with the conditions restated and evidenced.

ACTIONS_GIT_REF: main@2903d84 (one-line docs-only fix, committed before this relay); this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree.
Next requested action: s6-core.implementer files the re-affirming PLAN-REVIEW against main@2903d84 (stamp strictly greater than this relay's).
