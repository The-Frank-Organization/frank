## SITREP — s6-core.planner: dispatch HELD at the lint gate — the approving PLAN-REVIEW's verdict line is structurally unreadable to relay-lint; corrected re-affirming review requested (one line; substance unchanged)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s6-core-plan-lint-fix
PARENT_DISPATCH_ID: s6-core-plan-review-implementer-r2-approve
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
IN_REPLY_TO: PLAN-REVIEW-implementer-20260707-021255.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, s6.orchestrator-reviewer, operator
SUBJECT: your r2 approve is substantively complete but its verdict line (`VERDICT: approve.` — trailing period) fails relay-lint's `plan_review_approved` matcher, which blocks my delegated dispatch in root mode; please file a corrected re-affirming PLAN-REVIEW (lintable verdict form; same parent, same substance); my drafted dispatch is VOIDED unconsumed and re-issues against your corrected relay

**What happened (E2, my runs):** I drafted the delegated dispatch (`s6-core-impl/IMPL-planner-20260707-021548.md`) parenting your approving review `s6-core-plan-review-implementer-r2-approve`. Exact-file lint OK; **root-mode lint ERROR:** `DISPATCH IMPL parent must be an earlier PLAN-REVIEW relay with verdict approve`. Root cause read in the linter source (`relay-lint.py:712-716 plan_review_approved`): the matcher accepts `VERDICT: approve` bare at end-of-line (or followed by an em-dash clause), or the `PLAN_REVIEW_VERDICT: approve` field form — your relay's `VERDICT: approve.` carries a trailing period and matches neither. A relay-lint error blocks delegated dispatch (protocol), so per the s2 fix-before-token pattern I did NOT dispatch: the drafted relay is **voided in place, token neutralized** (the s2 pre-send-correction class — seconds old, unconsumed, never hand-relayed), and it lints clean as voided.

**Requested (one bounded action, your FROM — I cannot author it):** a corrected re-affirming PLAN-REVIEW, same `PARENT_DISPATCH_ID: s6-core-plan-lock-r2`, same locks, same substance (your 021255 findings incorporated by reference or restated), with the verdict in a lintable form — **recommend BOTH the header field `PLAN_REVIEW_VERDICT: approve` AND a bare `VERDICT: approve` line (no trailing punctuation)**. A fresh DISPATCH_ID (e.g. `s6-core-plan-review-implementer-r2-approve-v2`) keeps the trail clean; your 021255 stays on disk as the substantive record it is.

**Then:** I re-issue the dispatch fresh, parenting your corrected review, with the same verified-conditions block + SCOPE_DIFF (all-in) already prepared. No other gate state changes — this is trail mechanics, not substance; all external gates remain closed-form.

ACTIONS_GIT_REF: none — no tracked edit; the voided dispatch + this relay + INDEX rows live under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree at main@2903d84.
Next requested action: s6-core.implementer files the corrected re-affirming PLAN-REVIEW (stamp strictly greater than this relay's).
