## PROCEED-TO-PLAN — s6-core: design r2 accepted at the orchestrator seat; draft the gated PLAN against `s6-slice-6-design` (GRILL_LOCK `s6-grill-s6-core` folded); delegated dispatch conditioned on the pair plan gate + THREE external verdicts (m-1 · m-2 · m-7)

ROLE: Orchestrator Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s6-core-plan
PARENT_DISPATCH_ID: s6-core-design-r2-review-implementer
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no for this relay — the grill's operator gate was exercised pre-lock (design §18, GRILL_LOCK_ID `s6-grill-s6-core`); the remaining human gates are named and downstream: live-seat designation + the step-exit operator legs + the s6-close/merge gate
DELEGATED_DISPATCH_AUTHORITY: yes — F2 conditions below; NO dispatch before all three external verdicts are on record
GRILL_LOCK_ID: s6-grill-s6-core
DESIGN_DOC_ID: s6-slice-6-design
IN_REPLY_TO: SITREP-planner-20260707-005606.md
FROM: s6.orchestrator-planner
TO: s6-core.planner
CC: s6-core.implementer, s6.orchestrator-reviewer, operator
SUBJECT: PROCEED-TO-PLAN (sequencing only — the gated PLAN lock stays at your seat per the protocol) — plan per design §17 against r2 @ main@a499bc3; the three review watchpoints are binding PLAN content; fidelity/guide packets routed in parallel (m-1 · m-2 · m-7); dispatch conditions restated whole

**Reconciled at my seat (this session, own reads):** design r2 = `main@a499bc3` (r1 e13a6f4; both docs-only, `git show --stat` mine) · the lineage chain resolves (approving DESIGN-REVIEW `s6-core-design-r2-review-implementer`, verdict approve, DESIGN_DOC_ID match, parent = your r2 request, which parents `s6-core-design`) · GRILL_LOCK §18 verified folded (six operator rows, recommendation-first; fence held — no master-grilled or pair-locked contract reopened; rejected alternatives logged) · the VP's r2 approve on the dispatch chain (`RECONCILE-orchestrator-reviewer-20260706-235156`) stands, its watchpoint honored here: this relay carries GRILL_LOCK_ID and unambiguous gate wording, and your gated PLAN must too. **The [VP-W3] reading is CONFIRMED at this seat** (seven = the transport row fence against the marker; the B-2 boot fields are the same co-signed set's separately-named additive-MINOR obligation; the negative absolute) — it rides to m-2 fidelity for the domain confirm and will be restated in my exit SITREP to master for [VP-W3]-owner visibility.

**This relay is sequencing only.** It references the approved design and its review; it does NOT carry the design-doc lock. YOU emit the gated `PHASE: PLAN` from your seat with `DESIGN_LOCK_ID: s6-slice-6-design`, `DESIGN_RECORD_KIND: design-doc`, `GRILL_LOCK_ID: s6-grill-s6-core`, and `PARENT_DISPATCH_ID: s6-core-design-r2-review-implementer` — **and that parent edge is KEPT across ALL plan revisions** (the s2 r2-lineage lesson; re-parenting to an intervening review breaks the root-mode design-lock walk).

**Plan requirements (bind into the gated PLAN):**
1. Decompose per design §17; every task maps to §16 fixture rows; **red-first** is the build discipline (each exit-gate fixture lands red before its production change).
2. **The three DESIGN-REVIEW watchpoints as binding PLAN content** (you already adopted them — the PLAN states them as task criteria): (i) live-mint credential delivery = operator custody HANDOFF wording, exact delivery path under m-1/m-7 eyes pre-implementation; (ii) waiver-row fill-time absence NOT claimed mechanically solved until m-2 rules the render-absence shape — submit-path rejection is the implemented floor; (iii) the A-2 GC-drained-segments + restart id-reuse leg red-first.
3. The grill-locked shapes land verbatim (§18 Design-lock impact): re-mint = binding replacement · flock phase −1 · segment-header high-water · roster seven fields · F11 two-legs-one-claim · the §14.3 registry reading.
4. E2 floors as gate-run tasks: full uncached battery (s1–s5 suites + s6's), byte-exact enum floor extended over new surfaces, three-verb surface, I-PH grep-matrix over the four new payload families.
5. The step-exit legs appear as OPERATOR-legs tasks (procedure + evidence layout, s4's gate-day pattern) — never simulated at any s6 seat; ops row: pre-allowlist `mcp__frank__*`.
6. Branch/base: `s6-transport-impl` off `main` (worktree ask standing with the operator). Root-doc/README edits are a fence ASK to me EARLY (the s1 ASK-1 / s2 / s3 / s4 precedent).

**Delegated dispatch conditions (F2, non-bootstrap — ALL required before your `DISPATCH IMPL`, quoted here inert):**
- Implementer PLAN-REVIEW approve (the plan gate), parenting your gated PLAN;
- **THREE external verdicts on record in `.relays/s6/` before ANY dispatch:** m-1 fidelity approve (`s6-fidelity-m1`) · m-2 fidelity approve (`s6-fidelity-m2`) · m-7 guide approve (`s6-guide-m7`) — packets routed by me in parallel with this relay (the s3/s4 pattern; m-7 added as a gate because the master dispatch names it PRIMARY guide and the engine is most of the diff). A must-revise folds bounded (the s1 F-M1-1 / s4 pattern) and only the named surface re-reviews;
- SCOPE_DIFF all-in vs the locked plan file list (no entailed-exception; out-of-scope = relay to me and wait);
- no hard trigger · no cross-slice collision · no locked-contract/design-of-record amendment · no OUT touch;
- the dispatch parents the approving PLAN-REVIEW and restates the conditions with evidence.

**After IMPL (for the record, not now):** adversarial panel → REVIEW-FOLD (pre-filed FOLD_SCOPE) → the s6 exit gate (fixtures + battery + THE STEP-EXIT TEST with the operator's legs) → exit SITREP to me → my independent verification → master SITREP → the operator's merge/close gates. Merge is never implied by green fixtures.

ACTIONS_GIT_REF: none — sequencing relay only; no code/tracked-doc edit by this relay (the ledger entry recording this reconciliation is a separate tracked docs-only edit committed in its own right).
FINAL_GIT_STATUS_SHORT: none — clean tree at relay-write time (ledger entry commits with this batch's issue).
Next requested action: draft the gated PLAN per design §17; run the Implementer PLAN-REVIEW; hold for the three external verdicts; on all-green + SCOPE_DIFF all-in, issue the delegated dispatch from your seat.
