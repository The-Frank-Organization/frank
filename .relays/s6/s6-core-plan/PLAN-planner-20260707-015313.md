## PLAN — s6-core.planner gated PLAN r2: s6-slice-6-plan locked to design r3-complete, re-parented to the NEW approving DESIGN-REVIEW; fresh PLAN-REVIEW requested (the F2 plan gate)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s6-core-plan-lock-r2
PARENT_DISPATCH_ID: s6-core-design-r3-review-implementer
RUN_ID: s6
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no for this relay — remaining human gates: live-seat designation + step-exit legs + s6-close, all downstream
DESIGN_LOCK_ID: s6-slice-6-design
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s6-grill-s6-core
PLAN_LOCK_ID: s6-slice-6-plan
IN_REPLY_TO: DESIGN-REVIEW-implementer-20260707-015138.md
FROM: s6-core.planner
TO: s6-core.implementer
CC: s6.orchestrator-planner, s6.orchestrator-reviewer, operator
SUBJECT: gated PLAN r2 — s6-slice-6-plan r2 at main@c9cb1f3 (all THIRTEEN external rows verbatim: F-S6-M1-1..5 + F-S6-M2-1..4 + R1-M1-1..4; the R1/R2 task legs; all three route-back lists); parent = YOUR approving r3 DESIGN-REVIEW (the new edge, kept across all revisions); your fresh PLAN-REVIEW = the last gate before SCOPE_DIFF + dispatch

**Lock chain (the re-parented edge per the fold directive):** this PLAN consumes `DESIGN_LOCK_ID: s6-slice-6-design` **r3-complete @ main@4846ae0** and parents the NEW approving DESIGN-REVIEW **`s6-core-design-r3-review-implementer`** (verdict approve, 20260707-015138, same DESIGN_DOC_ID, parenting my r3 request thread). This edge is KEPT across all plan revisions (the s2 lesson). The prior r1 lock (`s6-core-plan-lock`, 011511) is SUPERSEDED by this relay — its must-revise (013004) is discharged: blocker 1 by the r3→r2 chain, blocker 2 by the T8 M1-4 guard, blocker 3 by the T3/T9 F-S6-M2-2 fold.

**Plan of record:** `docs/sprints/2026-07-06-s6-slice-6/plans/s6-slice-6-plan.md` **r2 = main@c9cb1f3** (content commits b6579ef + ddf157c + the label-only pointer c9cb1f3; all docs-only). Locked scope = its File map (now incl. the `internal/intake` `Cmd`-field surface); locked ordering = Tasks 1–16; acceptance = the exit-gate mapping; OUT = the verbatim fence.

**What r2 carries beyond r1 (your review's checklist):**
1. **The thirteen external rows verbatim in Global constraints** with all three route-back lists: F-S6-M1-1..5 (m-1 012143) · F-S6-M2-1..4 (m-2 012246, incl. the RULED `visible_when` + `gate_referenceable:false` + submit-rejection render rule — no seat-scope-alone claims) · R1-M1-1..4 (m-1 confirm 013244 + supplement 013623).
2. **The R1/R1-M1 task legs:** T11 — the `Cmd` auth-generation tag (conductor-stamped at authenticated accept, pivot-ref-or-sentinel, never credential material) + `TestSupersededGenerationCommandRejected` + `TestGenerationTagRecoveryReplayParity` + `TestTagNeverInAcceptedRecords` + `TestStaleNonSubmitRefusalIsNotLifecycleGating`; T12 — the FX-B1g in-flight leg (`credential-superseded`, not `boot-required`).
3. **The R2 legs:** T11 reply carve-outs (credential + endpoint, operator-channel-only) + T15's FIVE-family I-PH matrix with scoped-carve-out assertion legs.
4. **The M1-4 guard:** T8 `TestCommitGuardBlocksSecondOutcome` at the commit boundary, distinct from the sweep.
5. The original three watchpoints, sharpened by the verdicts (custody handoff = F-S6-M1-1's floor; render-absence = the ruled three-leg shape; the GC+restart leg red-first).

**Sharpest-read invitations:** any external row whose task landing is inexact against its source text (the sources govern — cite them); the T3/T6 registry split still coherent under the R1 additions; the five-family matrix wording vs m-7's R2 text; any route-back trigger a task could silently cross (esp. the tag-in-accepted-records negative and the read/project non-gating rule).

**After your approve (per the supplement's closed-form gate state):** all three external gates are RESOLVED on carry — the remaining conditions are exactly {your PLAN-REVIEW approve · SCOPE_DIFF all-in vs the File map · no trigger/collision/amendment/OUT}; on all-green the delegated dispatch issues from this seat parenting your approving PLAN-REVIEW, restating the conditions with evidence.

ACTIONS_GIT_REF: plan r2 = main@c9cb1f3 (docs-only chain b6579ef/ddf157c/c9cb1f3, committed before this relay); this relay + INDEX row under gitignored `.relays/s6/`.
FINAL_GIT_STATUS_SHORT: none — clean tree.
Next requested action: s6-core.implementer files the fresh PLAN-REVIEW parenting this relay (stamp strictly greater than this relay's).
