## DESIGN — m-2 s6 transport-codec amendment → m-2.implementer DESIGN-REVIEW: ONE canonical envelope codec (F6/F7/F2/F13/F12); consumes m-1's parenting fork; no c1 reopen

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-amend-m-2
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — pair DESIGN-REVIEW request; the operator/VP gates are the m-1 fork grill + the integrated VP co-sign, not this pair leg
GRILL_REQUIRED: no — my amendment consumes the m-1 parenting fork (§3), it does not decide it; the hard-to-reverse fork grill is m-1's at master. This leg is the standard pair adversarial DESIGN-REVIEW
DESIGN_DOC_ID: s6-amend-m-2-transport-codec
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: .relays/s6/s6-design/DESIGN-orchestrator-planner-20260706-180315.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: DESIGN-REVIEW request — the m-2 transport-codec amendment (kills the render↔validate↔gate↔deliver divergence CLASS with one codec); grill the codec unification + the fork-consumer contract + the constraints-preserved claims

**Request.** Grill the m-2 s6 amendment: `master-docs/master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md` (DESIGN_DOC_ID `s6-amend-m-2-transport-codec`). It **amends** c1 §2/§4/§12 via the review-driven path (folded only on VP co-sign) — **no c1 reopen**. Baseline `frank@7e5c527`; the four divergent judges were mapped at this seat against live code (cited in §1).

**The thesis (kill the class, not the instance).** Root in my layer = **four independent decoders of the recipient list**: render (`recipient_picker` unimplemented → 0 candidates), validator (canonical JSON), lineage gate (`strings.Split(",")` raw), delivery (canonical JSON, silent-drops to `Envelope.To` on parse-fail) — plus `Envelope.To` as a single verbatim mailbox. **One codec** (one canonical `address_list` form + one decode function all four judges call) dissolves F2/F6/F7/F13 as instances; F12 is a small typing fix.

**Per-finding disposition (all "design amendment"; none wontfix):**
- **F6** (silent drop, SEV-1) → single codec; delete the raw-comma-split (`lineage.go:286,433`) + the silent-drop (`projections.go:135-136`); `Envelope.To` becomes a **projection** of the canonical Header `TO` list.
- **F7** (CC deadlock, SEV-1) → the lineage reviewer-visibility gate (`lineage.go:170-184`) decodes via the codec ⇒ **one encoding satisfies both** typing and visibility (the `relay-c13dc32f`/`relay-1f99aadf` deadlock is gone).
- **F2** (dead-edge parent, SEV-2) → **m-1 fork seam** (§3): render offers a parent only if the lineage gate accepts it; **dissolves entirely** if the fork lands on conductor-computed PARENT (`system_only` ⇒ not rendered ⇒ nothing to diverge, exactly as ROLE/FROM). Designed composable with either branch.
- **F13** (validator rejects advertised tokens, SEV-2) → membership single-sourced from the registry `enum_set`; `submit.go:143 validateRecordKind`'s hardcoded switch (`default→"unknown"` at `:177` rejects advertised `genesis`/`disposition`/`diagnostics`/`config_change`) reads the enum_set, keeping only per-kind required-field checks. (A validation-source unification, not an enum widening — no `:126` bump.)
- **F12** (waiver typing, SEV-3) → **m-1 F17 seam** (§4): `ORCH_REVIEW_WAIVER` (`seat_scoped_enum` `["*"]`, presence-only) retyped to carry `free_text` rationale; the carrier (waiver-record field vs header) settles with m-1's F17 record class.

**Where I want the hardest grill:**
1. **The `Envelope.To` = projection rule (§2.4):** is deriving `Envelope.To` from the canonical Header list (m-2 defines the rule; m-7's loop stamps it) sound, or does it hide a delivery/index dependency I've missed?
2. **The fork-consumer contract (§3):** is "render offers a parent only if the gate accepts" genuinely branch-agnostic, and is the conductor-computed dissolution claim (`system_only` ⇒ not rendered) exact against `render.go renderable()`?
3. **F13 membership-vs-required-field split (§2.6):** does routing `validateRecordKind` membership through the enum_set while keeping per-kind field checks preserve every s4-wire/s5 owed-record guarantee?
4. **Constraints-preserved (§6):** is the codec truly interpretation-only — byte-exact `{accepted,rejected,held}`, the 3-verb surface, I-PH, and the confusion-resistance ceiling all untouched (no by-construction creep)?

**Boundary (grill this too):** the FORK (m-1), F17 scoping/retraction (m-1), `Envelope.To` stamping + digest lease (m-7), F3/F4/F5/F9/F11/F14/F15/F16 — named as seams in §8, not claimed. `recipient_picker` render (§2.5) is the one in-amendment build fix (realizes a locked §4 fill_constraint never implemented).

On `approve` I hold and SITREP master (design complete) for the integrated VP co-sign; the m-1 fork grill proceeds separately and my §3 consumes whichever branch it locks.

ACTIONS_GIT_REF: wrote the amendment design doc `master-docs/master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md` + this DESIGN relay + an INDEX row; read-only probes of `frank/` (no edit); no `frank/` change, no c1 design-doc edit (the amendment folds into c1 only on VP co-sign), no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, probed read-only @ 7e5c527)
Next requested action: m-2.implementer returns `PHASE: DESIGN-REVIEW` (`DESIGN_REVIEW_VERDICT`) on `s6-amend-m-2-transport-codec`; on approve, m-2.planner SITREPs master (design complete, held for integration + VP co-sign). No build, no c1 fold, no fork lock in this leg.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
