## SITREP — m-2 s6 transport-codec amendment COMPLETE + pair-approved (r2 `approve`); held for master integration + VP co-sign; the seams m-2 declares for the integrated set

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s6-amend-m-2
PARENT_DISPATCH_ID: s6-amend-m-2-review-r2
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — completion report of a pair-approved amendment design; the human/VP gates (integrated co-sign; the m-1 fork grill) remain upstream
GRILL_REQUIRED: no
DESIGN_DOC_ID: s6-amend-m-2-transport-codec
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: .relays/s6/s6-amend-m-2/DESIGN-REVIEW-implementer-20260706-190100.md
FROM: m-2.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-2.implementer
SUBJECT: m-2 amendment design-complete + pair-approved (implementer `190100` r2) — ONE canonical envelope codec (F6/F7/F2/F13/F12); held for the integrated VP co-sign; seams named for m-1 (fork, F17) + m-7 (Envelope.To stamp, digest lease)

**Design-complete + pair-approved.** m-2.implementer verdict `approve` (`190100`, r2) on `s6-amend-m-2-transport-codec` — the three r1 blockers folded without widening the s6 boundary. Per the pair lifecycle I hold and do **not** self-advance; returning the amendment for **master integration of the three amendment docs + the seam text → VP co-sign**. No c1 fold until that co-sign.

**Approved amendment (`master-docs/master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md`).** The central move: **ONE canonical `address_list` codec** (one decode function) consumed by render, typed-validate, the lineage gate, and delivery projection — killing the "one envelope, many judges" divergence *class*, not each instance. Per-finding, all *design amendment*:
- **F6** silent recipient drop → single decode; raw-comma-split (`lineage.go:286,433`) + silent-drop (`projections.go:135-136`) deleted.
- **F7** CC deadlock → the reviewer-visibility gate decodes via the codec ⇒ one encoding satisfies typing + visibility.
- **F2** dead-edge parent → **consumes the m-1 fork** (§3); dissolves under conductor-computed PARENT (`system_only` ⇒ not rendered), else render-candidate source == lineage-accept source.
- **F13** advertised-token rejects → **three-layer** authorization: membership (`enum_set`) / seat-scope / per-kind required-fields; `validateRecordKind` reduced to layer 3 after `reg.Validate`; `genesis` stays seat-scope-rejected (my own s5 M-3(e)).
- **F12** waiver typing → bound to m-1's **F17 operator-only waiver record**; m-2 types the `rationale`/`waiver_scope`/`retracts` rows; the `"*"` header retired.

**r1→r2 fold (all three blockers verified correct, incl. two that caught me contradicting my own s5 rulings):** F13 membership-vs-authorization conflation; `Envelope.To`-as-primary leaving render/index showing fewer recipients than delivery; F12's inexpressible free-text-header branch. §10 fold-log carries the detail.

**Seams m-2 declares (for your integration + the cross-pair set) — each names the m-2 half + the owner of the other half:**
1. **F2 parent seam (← m-1 fork):** m-2 = render/validate (render offers a parent only if the gate accepts); m-1 = the fork decision (conductor-computed vs widened candidate set). My §3 is branch-agnostic and can co-sign without waiting on the fork grill.
2. **F12 ↔ F17 (← m-1):** m-2 = the `rationale`/`waiver_scope`/`retracts` **field typings**; m-1 = the operator-only waiver **record class + scope/retraction semantics** (m-1 `2026-07-06-s6-transport-amendments.md:44-52`). Neither locks in isolation.
3. **`Envelope.To` projection + digest (→ m-7):** m-2 = the projection rule (Header list is recipient truth for every projection) + `recipient_picker` render marked `DigestExempt`; m-7 = the commit-loop stamp of `Envelope.To` + the digest **lease** (F5). My `DigestExempt` render is the m-2 contribution to the F5 churn class.

**Boundary held (unchanged):** interpretation-only; byte-exact `{accepted, rejected, held}`, the `submit`/`project`/`read` surface, channel-stamped FROM, and I-PH all untouched (no by-construction creep; the claim ceiling stays tool-mediated confusion-resistance, D5 residual). Not claimed: the FORK, F17 record class, `Envelope.To` stamp, digest lease, F3/F4/F9/F11/F14/F15/F16.

Blocked: no — m-2's amendment leg is complete; awaiting master integration + VP co-sign. The m-1 fork grill proceeds separately; my §3 consumes whichever branch it locks.
Scope drift risk: none.

Tests / verification: E1 design against read code (`frank@7e5c527`); the fixture obligations (§7) are the build-slice's owed artifacts (codec round-trip + full-set-in-every-projection; three-layer record_kind incl. the genesis-rejected negative; waiver-as-F17-record; recipient_picker digest-exempt); implementer `approve` `190100`.

ACTIONS_GIT_REF: wrote this completion SITREP + appended `master-docs/master/relays/INDEX.md`; the approved amendment doc + rev1 relay are the prior artifacts; read-only `frank/` probes; no `frank/` edit, no c1 fold, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo, probed read-only @ 7e5c527)
Next requested action: master integrates the m-1/m-2/m-7 amendment docs + the seam text and routes the integrated set to the VP for co-sign; the m-1 fork returns via its decision-packet → operator grill → GRILL_LOCK; then master dispatches the s6 build slice. m-2.planner holds — no c1 fold, no self-advance.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
