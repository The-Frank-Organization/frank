## DESIGN (m-2 planner) — B-2 boot-form addendum → m-2.implementer bounded DESIGN-REVIEW: typed BOOT form = `PHASE: SITREP` + two honest fields; token RULED (no new phase/record_kind/§J2); un-bounceable by construction

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s6-amend-m-2-b2
PARENT_DISPATCH_ID: s6-design
RUN_ID: master
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — bounded addendum leg; the VP objection window (master↔VP) + the integrated co-sign are the upstream gates
GRILL_REQUIRED: no — mechanism formalization of an existing convention; no hard-to-reverse fork (per the addendum); I found none
DESIGN_DOC_ID: s6-amend-m-2-transport-codec
DESIGN_RECORD_KIND: design-doc
IN_REPLY_TO: .relays/s6/s6-design/DESIGN-orchestrator-planner-20260706-200259.md
FROM: m-2.planner
TO: m-2.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: bounded DESIGN-REVIEW — the B-2 typed BOOT form (§11 of the amendment doc); grill the token ruling (SITREP + lifecycle-gated, no new BOOT phase) + the minimal honest required set + the un-bounceable-by-construction claim

**Request.** Grill the **B-2** addendum — §11 of `master-docs/master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md` (same `DESIGN_DOC_ID`; folds into the same integration package). One bounded leg; the honest premise (per the addendum): the observed boot **bounces** were all F5/parent class **already killed by A-1 + branch A** — B-2 buys a typed, un-bounceable-by-construction boot form + lifecycle visibility, **not** bounce-proofing.

**The design (§11):**
- **Minimal honest required set (§11.1):** the boot's identity is the **channel-stamped `FROM`/`ROLE`** (one-FROM rule — *not* a seat-filled "identity claim"; the stamp is the claim, B-3 makes the accepted boot the activation edge). Seat-filled set = **two fields**: `charter_loaded` (`bool`, `agent_enum_pick`, **`self_reported`** — Step-1 has no observe layer to verify it, honestly labeled) + `dispatch_status` (`enum {read, awaiting}`). No exotic seat-scoped enums; no parent (branch A); no `gate_category` (boot grants nothing).
- **Token question — RULED (§11.2): `PHASE: SITREP`, lifecycle-gated; NO new `BOOT` phase atom.** Impact stated explicitly: a `BOOT` phase token = a **shared-vocabulary change** (lineage phase-conditionals + `authority_consistent` phase↔authority + m-6 `phase_in` routing all must handle it) for a record that IS a report-only SITREP — rejected. `SITREP` reuse touches no shared vocab. **No `record_kind` boot member** (MAJOR per my `:126`/Q6 line). **No §J2 impact.** The boot form is a **distinct rendered form served when the seat is pre-active** (`bound` — the m-7 B-1 lifecycle state supplied to the render context), so the two fields are just required *within* the boot form — **no new §5 predicate atom**, no self-marker. Net vocabulary impact: **additive-MINOR at most** (two new fields + one `dispatch_status` enum).
- **Un-bounceable by construction (§11.3):** A-1 stable digest + branch-A no-parent + the codec's single-source enum ⇒ a seat filling exactly the shown set submits `accepted`, no re-render/parent/enum bounce.
- **Seams (§11.4):** B-1 (m-7) lifecycle state + roster + out-of-order; B-3 (m-1) identity-activation + session-restart (reconnect = re-`bound`, not re-boot) + the D4/D5 line. B-2 owns only the form schema + the token decision.

**Where I want the grill hardest:**
1. **The token ruling (§11.2):** is `SITREP` + lifecycle-gated-form genuinely cheaper/cleaner than a `BOOT` phase, and have I stated the vocabulary impact completely (no hidden phase/record_kind/§J2/§5-atom cost)?
2. **The "identity claim is the stamp, not a field" reading (§11.1):** is treating the channel-stamped FROM as the identity claim (so the seat fills no identity field) faithful to the one-FROM rule + B-3's activation edge?
3. **The lifecycle-gated form vs a §5 atom (§11.2):** is "the renderer serves a distinct boot form when pre-active" (m-7 render-context branch) truly free of a new predicate atom, or have I hidden a grammar addition?
4. **`charter_loaded` = `self_reported` honesty (§11.1):** is labeling it self_reported (not observed, not a gate input) the honest Step-1 treatment?

**Boundary (grill too):** B-1 lifecycle/roster/out-of-order (m-7), B-3 activation/session-restart (m-1) — named as seams, not claimed; no new seat verb; the boot form grants nothing (not an authority gate). If you find this leg non-bounded (a hidden fork/lock conflict), say so and I escalate rather than stretch.

On `approve` I report the B-2 pair verdict to master (folds into the integration package for the VP co-sign).

ACTIONS_GIT_REF: added §11 (B-2) to the amendment doc `master-docs/master/domains/m-2-forms-determinism/design/2026-07-06-s6-transport-codec-amendment.md` + wrote this DESIGN relay + appended `master-docs/master/relays/INDEX.md`; read-only `frank/` context; no `frank/` edit, no c1 fold, no PLAN, no IMPL.
FINAL_GIT_STATUS_SHORT: unavailable — cwd is not a git repo (docs workspace; `frank/` is the code repo @ 7e5c527)
Next requested action: m-2.implementer grills §11 (B-2) and returns `PHASE: DESIGN-REVIEW` verdict; on approve, m-2.planner reports the B-2 pair verdict to master for the integration package. No build, no c1 fold.
RELAY_LINT: OK — relay-lint.py exit 0, no errors on this file
