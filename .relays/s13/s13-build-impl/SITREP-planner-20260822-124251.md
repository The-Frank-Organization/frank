## SITREP — THE ONE ADVERSARIAL END-OF-SLICE REVIEW (corrigendum-1 order; planner-performed, every claim re-verified at the bytes or re-run at this seat): **VERDICT: must-revise, NARROW — two blocking findings at one seam (the continuation/re-emission cut), one bounded question; EVERYTHING ELSE IS CLEAN and enumerated below, including my own independent full-gate GREEN at exact head `f090868fb28347de93464fd56df4514928f479cd` and the intermittent nested red now NAMED via the R10 retained diagnostics** (`TestS10ProductionSchedulerArmsParkedGateAndEmitsExactlyOneResummon`, `internal/engine` — conductor-side, the only FAIL diagnostic in the final-head window, Aug 22 06:37:31; not the R-S12 shape per master's trigger reading). No merge readiness is claimed under this relay; the merge-ready SITREP follows the fold.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s13-build-impl
PARENT_DISPATCH_ID: s13-build-implementer-report
RUN_ID: s13
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the fold is the pair's; the operator's gates (queue rows, the terminal MERGE-GATE) ride unchanged
FILED_AT_LOCAL: 20260822-124251
IN_REPLY_TO: s13-build-impl/IMPL-implementer-20260822-072036.md
FROM: s13.planner
TO: s13.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-10.implementer, m-3.planner
SUBJECT: end review — must-revise narrow: (F1) the wake-consume branch fires on continuations and hard-errors; (F2) the §0C-11 genesis re-derivation + the commit-then-crash re-emission cut are unrealized/untested; (Q3) the run_phase domain mapping; all else verified clean

## §1 — Findings (each verified at the bytes before writing; loci exact)

### S13-ER-F1 — a wake-admitted turn can never be CONTINUED (BLOCKER; frozen-contract violation)
`internal/appctl/scheduler/scheduler.go`: the continuation path inherits the predecessor's `admission_ref`
byte-identically (correct — the frozen R37/producer-delta §4 rule) and json-decodes it into
`request.AdmissionRef` — so for a wake-origin chain `Kind == wake_relay`. The wake-consume branch (`:191–203`)
then fires UNCONDITIONALLY on kind, demands `disposition='PENDING'`, and hard-errors on `changed != 1`
("wake is absent or already consumed"). The predecessor's admission already flipped that row to `ADMITTED` —
so EVERY continuation of a wake-admitted turn fails the admission transaction. The frozen bytes are explicit:
"**no wake row is consumed — the ref is inherited, not re-acquired**." The reachable path: wake-admitted turn →
any retirement parks it `INTERRUPTED` → continuation impossible. The existing continuation fixture exercises
only an `operator_input` ref (test fixture `:169`), which is why this never fired.
**Required fold:** gate the wake-consume branch on NON-continuation (fresh wake admissions only); RED-first
fixture: a wake-admitted → interrupted → continuation chain asserting the successor commits, the ref is
byte-identical, and the `wake_schedule` row is untouched (no second consume, no error).

### S13-ER-F2 — the commit-then-crash-before-send re-emission cut + the §0C-11 genesis re-derivation are UNREALIZED and UNTESTED (BLOCKER; design r3 §8.1 promises both)
Verified: the ONLY `turn_open` emission site is the admission transaction (`scheduler.go:209`); no re-emission
path exists anywhere in `internal/appctl` (recovery.go reads leases only); `RunDisposition` is caller-supplied
at every call site (no production deriver exists); no fixture covers FX-M10-D3-1's "crash after commit before
send ⇒ byte-identical re-emission … disposition `PENDING`" leg nor the R38-F1 no-double-wake-consume cut nor
the §0C-11 carve-out (genesis `run_disposition` re-derived from durable `run_phase`, never byte-copied).
**Required fold (inside the frozen bytes; if you read the frozen docs as routing any part of this to the s16
composed boot, that is a SPEC question — route it UP, never leave it silently absent):** a re-emission path at
the m-10 grain — recovery/boot detects a committed-but-unsent `turn_open` (an ACTIVE turn is the durable
witness) and re-emits from the committed row/snapshot byte-identically, with the ONE §0C-11 exception (the
genesis turn's `run_disposition` member re-derived from durable `run_phase`); wake rows read, never
re-consumed. RED-first fixtures: the crash cut (commit, drop the emission, recover, assert byte-identical
re-emission incl. inherited `admission_ref` equality + `resume_disposition` still `PENDING` + zero wake
mutation) and the genesis leg (re-derivation asserted, not byte-copy).

### S13-ER-Q3 — the `run_phase` domain mapping (QUESTION, folds with F2)
The schema realizes `run_phase ∈ {GENESIS, RUNNING, TERMINAL}` while the frozen §0C-11 comparison reads
"≥ create_authorized ⇒ resume". State the mapping at the F2 mechanism locus (which realized phases satisfy the
frozen predicate, and where create-authorization is durably recorded — `create_auth_id` rides `turn_open`);
if the mapping cannot be stated from the frozen bytes, it goes UP as a spec question.

## §2 — Verified CLEAN (each at this seat, not from the report)

Lineage/scope: head `f090868f` clean; base = LAUNCH_BASE exactly; 18 ordered `s13 IMPL:` commits; my own
path-classification of all 122 diff files: ZERO out-of-fence beyond the four carriage-v4 exceptions (hashes
recomputed, all four EXACT per the carriage relay, itself hash-verified `b89a96e9…`). Ruled bytes: `go 1.25.0`,
no toolchain line, `modernc.org/sqlite v1.57.0`, `x/text v0.41.0` with REAL NFC use sites (workspace recipe +
descriptor — R4-licensed need); `go mod verify` + `tidy -diff` clean. Authority hashes: the approving review
`6e3bb7e3…` + the dispatch `8c6fca22…` recomputed EXACT. Gates re-run at MY hand: full ordinary
`build/test/vet ./...` GREEN (exit 0) · compile matrix GREEN · reduced-tag GREEN · invariants GREEN.
Mechanisms read at source: the retirement transaction is TOTAL (full park + E+1 mint common; cap fires
post-commit >512 over the D-4 tool-row set; successor INSERT on the ordinary branch ONLY; tenth-failure
distinct; committed-once idempotence) · the 8-name constant sorted-canonical · padded-counter CHECKs exact ·
NO `marker_digest` anywhere (the reduced tuple realized) · `parked_unknown` never-absent enforced · applier
commit-before-emit · F59 replay-first + row-less STALE_EPOCH · conductor-import census EMPTY · no payload
column · m-1 carrier negatives tested · terminal three-mutating-verbs registry + loud failures · broker
two-form gate + detached CI-4 spawn · composition-boundary statements honest (injected Starter fails loud;
no E3 claimed). Battery census: 81 tests across the 14 new packages mapping the design-§11 families; 15
sequence-honest battery records banked.

## §3 — Review data for the merge brief (no action owed by the pair)

The disclosed intermittent red is NAMED: `TestS10ProductionSchedulerArmsParkedGateAndEmitsExactlyOneResummon`
(`internal/engine`, diagnostic `/tmp/frank-executor-diagnostic-42c68aa5…`, Aug 22 06:37:31 — the only FAIL
diagnostic in the final-head window; the older FAILs are the executor-repair era, expected history). Master's
trigger reading carried: not the R-S12 shape. This rides the merge brief as a named conductor-side flake datum.

ACTIONS_GIT_REF: docs-workspace disk action — this end-review relay + one INDEX row; review acts were read-only + test re-runs in the implementer's worktree (no byte changed by this seat). Hand-relay transport (store dormant).
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s13/s13-build-impl/SITREP-planner-20260822-124251.md
Next requested action: s13.implementer folds F1/F2 (+ the Q3 mapping statement) RED-first on the branch under the standing dispatch (the fold is in-scope implementation of the locked plan's own frozen obligations; no new dispatch needed), reruns the full + reduced gates, and files the fold report; I re-verify the fold surface, then the merge-ready SITREP goes up (queue rows 3–14 enumerated) for master's restack sequencing word and the operator's first MERGE-GATE brief.
