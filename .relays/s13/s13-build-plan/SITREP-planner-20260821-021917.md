## SITREP — ONE RULING REQUESTED UP THE CHAIN (the plan-review's S13-PR-R1-F1 route-up, BLOCKING the s13 build dispatch): **the delegated-dispatch predicate vs the recorded ESCALATION_SCAN triggers.** The r1 PLAN relay truthfully records seven `yes` scan rows (`ESCALATION_SCAN_RESULT: trigger-present`) while stating the grant's dispatch condition "no hard trigger" — the reviewing implementer correctly refuses to let an approving review convert factually-true `yes` rows into "no hard trigger." REQUESTED: master's row-by-row disposition — are these seven classes FRESH dispatch blockers, or already DISPOSITIONED by the commissioned baseline (the production-risk commission `t4-s13-commission`, the ratified plan's s13 row, the charter's straight-through cadence with the operator MERGE-GATE as the terminal control, and the R1/R2 rulings)? The pair binds the returned disposition row-by-row in the r2 PLAN; NO token issues before it. Hand-relay transport per the 020247 notice; no store act.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s13-build-plan
PARENT_DISPATCH_ID: s13-build-plan-review
RUN_ID: s13
CEREMONY_TIER: large
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — master rules under the standing escalation ladder; the operator is CC'd context; the terminal MERGE-GATE is unchanged and not requested
FILED_AT_LOCAL: 20260821-021917
IN_REPLY_TO: s13-build-plan/PLAN-REVIEW-implementer-20260821-021351.md
FROM: s13.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-10.planner, m-10.implementer, s13.implementer
SUBJECT: F1 route-up — disposition the seven recorded scan triggers against the commissioned baseline so the pair's dispatch predicate is mechanically consistent; no token before your return

## The exact trigger set (each row factually `yes` for this build; recorded at `s13-build-plan/PLAN-planner-20260821-012742.md`)

1. authz/secrets — opaque credential-reference orchestration, the F59 authorization host, broker-control custody
   (all FROZEN spec surfaces this slice REALIZES; no secret bytes by schema).
2. migration/destructive-write — store genesis + forward-only user_version migrations + integrity-refusal (frozen §F).
3. trust-critical state — leases/epochs/tickets/attempts (the slice's entire subject matter).
4. AI-automation-downstream — the supervisor's automated lifecycle/retirement/backoff dispositions (frozen §B).
5. worker/scheduler/queue/retry — the scheduler, wake chain, G-2 backoff, bounded queues (frozen §B/§E).
6. cross-service contract — appipc as the owned-shared types package + the locked frame families (charter fence row).
7. user-visible-control — the §10 operator terminal surface (state-only by frozen design).

## The question, stated precisely

The pair reads these as INTRINSIC to the commissioned scope — every row names a surface the operator-ratified plan
and the frozen m-10 design set EXPLICITLY commission this pair to build, at production-risk ceremony, ceremony
UNdowngraded, with the operator MERGE-GATE terminal — i.e., dispositioned at commission, not fresh triggers arising
mid-flight. But that reading converts scan rows into dispatch permission, which is exactly what the reviewer (and
the standing B4 deviation's hard-trigger rule) says a pair may not do for itself. So: **rule each row DISPOSITIONED-
AT-COMMISSION or FRESH-BLOCKER (with its required act).** The r2 PLAN will cite your ruling row-by-row and state the
dispatch predicate as: implementer PLAN-REVIEW approve + this ruling bound + no NEW trigger arising after it.

## Status (report-only)

The plan-review's other findings (F2 T0 file-set/import determinism · F3 the stale store-export step vs your 020247
suspension · F4 three unmapped battery legs · F5 fake-counterpart construction owners · F6 the boundary-contract
block + design-§12 exclusions) are pair-local and are being folded into the plan artifact now; the r2 PLAN relay
files only after your F1 return, per the reviewer's required sequence. No source/branch/worktree/commit exists;
no token exists anywhere in this root.

ACTIONS_GIT_REF: docs-workspace disk action — this SITREP + one INDEX row + the in-progress plan-artifact F2–F6 folds (in-fence); hand-relay transport only (store dormant per 020247). No source byte, no branch, no token.
FINAL_GIT_STATUS_SHORT:
 M frank/.relays/s13/INDEX.md
 M frank/.relays/s14/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s13/s13-build-plan/PLAN-REVIEW-implementer-20260821-021351.md
?? frank/.relays/s13/s13-build-plan/SITREP-planner-20260821-021917.md
?? frank/.relays/s14/s14-build/IMPL-planner-20260821-020810.md
Next requested action: master dispositions the seven rows (row-by-row) and hand-relays the ruling TO s13.planner (CC s13.implementer); the r2 PLAN then files for fresh PLAN-REVIEW binding it.
