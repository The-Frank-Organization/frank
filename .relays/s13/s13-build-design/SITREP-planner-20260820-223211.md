## SITREP — TWO RULINGS REQUESTED UP THE CHAIN (both surfaced by the r1 design review S13-DR-R1-F1/F3; neither is this pair's to decide): **(E-0) the sprint-doc location/fence conflict** — the charter's relay-root sprint-doc grammar collides with recursive relay-root lint (the r1 artifacts fail it in place), and the standard `frank/docs/sprints/**` home is out of this pair's write fence; **(E-1) frank's FIRST external dependency + a possible Go-floor change** — `modernc.org/sqlite` declares `go 1.25.0` at every release v1.50.0–v1.57.0 while frank declares `go 1.22` (+ the README floor line), so the driver cannot land as a bare go.mod row without a baseline decision. Design r2 (@ 84444951…) folds the review and presents the exact options; the r2 review proceeds in parallel; store code (plan T4+) gates on E-1; the artifacts move only on E-0. No code byte moves under this relay.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s13-build-design
PARENT_DISPATCH_ID: s13-build-design-review
RUN_ID: s13
CEREMONY_TIER: large
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — master rules both (routing owners as master sequences); the operator is CC'd context
FILED_AT_LOCAL: 20260820-223211
IN_REPLY_TO: s13-build-design/DESIGN-REVIEW-implementer-20260820-221508.md
FROM: s13.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-10.planner, m-10.implementer, s13.implementer
SUBJECT: two build-gating rulings — (E-0) where s13's sprint-doc artifacts legally live; (E-1) the SQLite driver route: v1.57.0 + go-floor 1.22→1.25, or the floor-preserving v1.36.1

## E-0 — the sprint-doc location/fence arbitration (F1)

Facts: the r1 design + plan artifacts live at `frank/.relays/s13/docs/{designs,plans}/` (in-fence per the charter's
"relay root … sprint-doc-setup grammar" line); `relay-lint --relay-root .relays/s13` recursively treats them as
relay payloads and ERRORS on both (missing relay headers) — the exact-file lints of every actual relay + the INDEX
pass. The standard home `frank/docs/sprints/active/2026-08-20-s13-m10-module/**` (the s12 precedent, outside the
recursive lint surface) is OUT OF FENCE for this pair as chartered.
Options: **(a) RECOMMENDED — authorize `frank/docs/sprints/active/2026-08-20-s13-m10-module/**` as an s13 fence
addition** (matches s12; zero lint interaction; the branch commits it at T0); (b) an explicitly authorized
lane-docs convention + a linter mechanism (not ours to invent). The artifacts move ONLY on the ruling; acceptance
proof after the move = `relay-lint --relay-root .relays/s13 --no-freshness` clean.

## E-1 — the driver dependency + Go-floor route (F3; go.mod/go.sum are arbitration-only)

Facts (verified live at the module proxy this session; evidence commands in design §3): frank `go.mod:3` = `go
1.22`; `README.md:46` advertises "Requires Go 1.22+"; `modernc.org/sqlite` declares `go 1.25.0` for ALL of
v1.50.0–v1.57.0, ≥`go 1.23.0` from v1.37.0 (and v1.36.2/.3); the newest release satisfying the current floor is
**v1.36.1 (`go 1.21`)**. Installed toolchain here: go1.26.4.
Options: **(Route A, recommended in design §3) `modernc.org/sqlite@v1.57.0` + frank `go` directive 1.22 → 1.25
+ the README floor-line edit by the ruled owner** (the README is out of this pair's fence; this pair edits go.mod/
go.sum ONLY to the ruling's exact bytes) — current driver line, at the price of a support-floor change that is
master's/the operator's to make; **(Route B, floor-preserving) pin `modernc.org/sqlite@v1.36.1`** — no baseline
change, an older driver line. Either way the R-M10-SQLITE-DRIVER floor (WAL + synchronous=FULL · private-file ·
sole-writer · forward-only migration · transactions) is unweakened and fixture-backed (design §3).

## Status (report-only)

The r1 design review (`DESIGN-REVIEW-implementer-20260820-221508.md`, must-revise, S13-DR-R1-F1..F4) is folded as
design r2 @ `84444951f56ff9ace316205e23245435d4fb1bc3e48368425e5fae7d1e50d06f` (filed to s13.implementer for the
fresh review in this same act); F2 (the cap-terminal no-successor branch) and F4 (the complementary build selector
+ executable compile-negative harness) are folded locally — only E-0/E-1 need rulings. Plan T0–T3 are dispatchable
on plan-approve without them; T4+ (store) gates on E-1; the docs-bank commit follows E-0.

ACTIONS_GIT_REF: docs-workspace disk action — this SITREP + the r2 DESIGN relay + two INDEX rows + the in-place design/plan r2 folds (all in-fence, untracked until the branch opens); the store twins of both relays. No source byte, no branch, no lock, no shared path touched.
FINAL_GIT_STATUS_SHORT:
 M frank/.relays/s13/INDEX.md
 M frank/.relays/s13/docs/designs/DS-s13-m10-module-20260820.md
 M frank/.relays/s13/docs/plans/PL-s13-build-plan-20260820.md
 M frank/.relays/s15/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s13/s13-build-design/DESIGN-REVIEW-implementer-20260820-221508.md
?? frank/.relays/s13/s13-build-design/SITREP-planner-20260820-223211.md
?? frank/.relays/s15/s15-build-2/
?? frank/.relays/s15/s15-build/PLAN-REVIEW-implementer-20260820-220110.md
Next requested action: master rules E-0 and E-1 (routing m-10/operator as owners as master sequences) and relays the rulings TO s13.planner (CC s13.implementer). The pair proceeds meanwhile on the r2 design review + the unblocked plan surface.
