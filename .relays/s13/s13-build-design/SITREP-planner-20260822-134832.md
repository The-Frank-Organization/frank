## SITREP — the r4 review round + the ONE master act it needs: **the pair concurs, at independently verified bytes, that the fold's source fence must extend by exactly `frank/cmd/frank-app/main_test.go` — please rule.** The r4 amendment (@ `1941544c…`) came back MUST-REVISE NARROW (`DESIGN-REVIEW-implementer-20260822-133853.md`: 3 blocking, 0 non-blocking; every other §17 clause verified matching the owner-final ruling). I re-verified all three findings at my own hand before this relay. **S13-DR-R4-F1 (the master act):** the ruled literal lowercase `run_phase` domain rejects the raw SQL seed `run_phase='RUNNING'` at `cmd/frank-app/main_test.go:66` (exact head `f090868f…`) the moment `store/schema.go:11` carries the corrected CHECK; retaining uppercase aliases violates Q-A, skipping `./cmd/frank-app` violates the locked full gate, and no in-fence byte can rewrite an out-of-fence test literal. **My completeness proof for the one-file claim (three structurally different searches over the whole worktree):** (1) token `run_phase` — sole out-of-fence hit = `main_test.go:66`; (2) Go double-quoted phase literals `"RUNNING"/"GENESIS"/"TERMINAL"` — same sole hit; (3) every `INSERT INTO runs` site — same sole hit; `test/fixtures` (the carriage-v4 files) carries zero, so those stay byte-exact as ruled. The implementer's pre-edit census (`s13-build-impl/SITREP-implementer-20260822-133654.md`, FOLD_SCOPE_RESULT deviation-present, the file marked OUT, no edit made) reached the same single file independently. **F2/F3 need NO master act** — they restore column/source specificity already present in the owner ruling + its approving review (F2: bind the initial-path record literally as `runs.session_log_path`; F3: every later/continuation admission transaction COPIES the once-minted run-scoped nonce from the durable admission lineage into the new `turns.create_auth_id` — never remints, never trusts a caller value — per the approving review's "each turn admission row persists the run-scoped stable nonce"); I carry both in r5. **Sequencing on your word:** r5 = STATUS/§17 successor bytes only (F1's one-file fence exception named in §17.6 + F2 + F3) → pair review → corrected all-in FOLD_SCOPE → RED-first fixtures → the joint F1+F2 fold → gates → fold report → my re-review. Nothing moves until the fence word returns.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s13-build-design
PARENT_DISPATCH_ID: s13-build-impl
RUN_ID: s13
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a one-test-file fence extension inside the slice's own command surface; no operator decision, no frozen/ratified byte
GRILL_REQUIRED: no
FILED_AT_LOCAL: 20260822-134832
IN_REPLY_TO: s13-build-design/DESIGN-REVIEW-implementer-20260822-133853.md
FROM: s13.planner
TO: master.orchestrator-planner
CC: s13.implementer, master.orchestrator-reviewer, operator, m-10.planner, m-10.implementer, m-3.planner
SUBJECT: r4 must-revise narrow (3 findings, all pair-verified); ONE master act requested — extend the fold fence by exactly frank/cmd/frank-app/main_test.go; F2/F3 are mine in r5; the fold holds on your word

ACTIONS_GIT_REF: docs-workspace disk action — this relay + one INDEX row; verification read-only (worktree greps at head f090868f, ruling/review byte checks). No source, branch, commit, token, or design byte under this relay; r4 stays the standing design bytes until the r5 successor.
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s13/s13-build-design/SITREP-planner-20260822-134832.md
Next requested action: master.orchestrator-planner rules on the one-file fence extension (or supplies a source-backed contradiction); on the word, s13.planner authors r5 for pair review; the implementer's corrected all-in FOLD_SCOPE and the RED-first F1/F2 fold follow the approval. No self-advance.
