## RECONCILE — THE §1.7 IDENTITY DOMAIN RULED: branch (b) ADOPTED — the identity bind is the IMPLEMENTATION SUBTREE, with the census-at-merged-object leg UNCONDITIONAL and a NON-GOVERNANCE-DRIFT STOP RAIL; rung 6 (the MERGE-GATE brief) FILES NOW

**Verified at master's own invocation before ruling:** the candidate `e6f28798…`'s merge-base with `origin/main` is `b43fcbfc…`; the implementation-subtree diff (`frank/{cmd,internal,test,go.mod,go.sum}`) from that base to current main is EXACTLY ZERO paths; every advance path is governance (`master/**` + `frank/.relays/**`). The pair's premise is correct and structural: on a courier whose governance root lives in the merged repository, whole-tree identity between candidate and merged object is UNATTAINABLE BY CONSTRUCTION — each relay filing, including the escalation itself, advances main. Branch (a) is a Zeno race; rejected as the general rule.

**THE RULING (binds rungs 6–7 and the r10 close record):**
1. **The identity domain** = the implementation subtree: the merged object's `frank/cmd`, `frank/internal`, `frank/test`, `frank/go.mod`, `frank/go.sum` trees must equal the candidate `e6f28798…`'s, verified by tree-hash comparison at the merged object (not by absence-of-conflict).
2. **The census leg does NOT narrow:** the tagged census + the plain suite RERUN AT THE MERGED OBJECT unconditionally, expected 64/0/64 + zero failures; any deviation is a STOP regardless of subtree identity.
3. **The STOP rail (branch (a) preserved as the exception, not the rule):** at grant-execution time the executor re-sweeps the drift from `b43fcbfc…` to then-current main; if ANY path outside the governance classes (`master/**`, `frank/.relays/**`, the charter symlink pair) appears, the merge HALTS and a fresh restack round applies — that would be foreign implementation bytes, a STOP on any reading. Master re-runs the same sweep at post-merge verification.
4. **The brief states the ruled domain explicitly** — the operator grants over the implementation-subtree bind + the unconditional merged-object census + the stop rail, in those words.

**ON THIS RULING: rung 6 files immediately** — the planner's MERGE-GATE brief TO the operator with the EXECUTOR (s16a.implementer) IN TO, the queue enumerated, `HUMAN_MERGE_AUTHORIZATION` requested in the operator's own words; the grant executes as the PR #1 merge under §1–3 above, the PR flipping ready-for-review at the brief. The pair holds everything else; master's r10 close record + F.7.2 naming (the root + the closing hash) land on the merged object per the standing sequence.

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: report-only
DISPATCH_ID: s16a-close-ruling
PARENT_DISPATCH_ID: s16a-close
RUN_ID: s16a
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — rung 6's MERGE-GATE brief files on this ruling; the operator's grant is the terminal gate (this relay itself opens no operator choice)
IN_REPLY_TO: s16a-close/SITREP-planner-20260826-142247.md
FROM: master.orchestrator-planner
TO: s16a.planner, s16a.implementer
CC: master.orchestrator-reviewer, operator, m-7.planner, m-10.planner, m-9.planner, m-8.planner
SUBJECT: §1.7 identity domain RULED — implementation subtree bind (tree-hash equality at the merged object) + unconditional merged-object census + the non-governance-drift STOP rail (branch (a) = the exception); verified at master (zero subtree paths in the drift); the MERGE-GATE brief files now with the executor in TO

ACTIONS_GIT_REF: engine-lane governance act — drafted at .engine/drafts/master.orchestrator-planner/ and submitted through relay submit; verification was read-only (fetch + merge-base + two diff sweeps); no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16a/INDEX.md
 M master/relays/CHECKPOINTS.md
?? SITREP-implementer-20260826-141100.md.tmp
?? frank/.relays/s16a/s16a-close/
?? frank/.relays/s16a/s16a-end/
