## SITREP — ESCALATION UP (the D1 coda's two owner-contract conflicts, found by the r18 review `s16a-build/PLAN-REVIEW-implementer-20260825-220254.md`; m-7 rules, m-10 CC'd for the touched appipc/store surfaces): the coda's D1 as ruled is NOT EXECUTABLE against current bytes on two independent grounds, both verified at the loci by the reviewer. **F1 — THE LOCK-NAMESPACE CONFLICT:** the owner contract pins ONE lock family end-to-end — controller `fcntl F_SETLK`, broker `F_GETLK`, holder-PID == connected-peer-PID before token/generation (`…transport-broker.md:192-200`) — but the PRODUCTION controller holds `syscall.Flock(LOCK_EX|LOCK_NB)` (`internal/appctl/brokerclient/session.go:192-213`, the s13-era client the WP3 build correctly answered): `F_GETLK` cannot see an `flock()` lock, so the ruled verifier would FAIL an honest controller and a broker-only probe would regress the real CT-C09 while appearing fail-closed. The ask: rule EITHER (i) the controller CONVERTS to the pinned `F_SETLK` namespace — with the realization constraints the reviewer names (stable inode; the fcntl same-process-close hazard; Linux/Darwin) — the verifier then as ruled; OR (ii) verifier AND holder move together to an owner-ruled alternative (noting `flock` exposes no holder-PID query). The D1 sizing ("one file's handshake path") does not survive (i); m-7 re-sizes. **F2 — WHAT "RECORDED" REQUIRES AT THE CODA:** the contract reserves "recorded" for durable m-10-store commit through the broker event/ack protocol (`:227-255`; the `control_handover` outcome set incl. `adopted`, `:244`) — but `control_handover` is UNREGISTERED in `appipc/msgs_broker.go:63-81`, the store client accepts only `boundary_cut`/`epoch_installed` (`brokerclient/store.go:78-80`), and the server has no event sequence/resend/ack machinery (`server.go:22-173`; `Serve` blocks serially in-session). The ask: rule EITHER the full durable path lands in the coda (the in-fence writer, the closed schema, the durable reader/ack, resend/identity tests, the session-ownership proof that a verified greater generation replaces and closes the old session) — a materially larger coda — OR an EXPLICIT owner narrowing of the coda's recording obligation (stating what is recorded now vs deferred-with-registry-row, and whether accepted ADOPTION is recorded). NOT ESCALATED — F3 is mine: r19 will explicitly authorize the seam-predicate replacements the float ruling already implies (CT-D01 flipped from accept-1.5 to byte-verbatim arbitrary-precision integers incl. >2^53 + typed refusals for `-0`/fraction/exponent/float at every encode/digest entry; CT-D02 from source-grep to the executed exhaustive vector corpus with per-vector byte-equality), named separately from module-test updates, anti-vacuity preserved. **STATE:** D2/D3 and ALL of WP4's substance are review-ACCEPTED and wait only on r19; the pair holds build bytes; the PR stays DRAFT; on the joined ruling, PLAN r19 binds it verbatim + F3 and goes to fresh review.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16a-wp34-d1-esc
PARENT_DISPATCH_ID: s16a-build-plan-review-12
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a chartered contract arbitration (master → m-7; m-10 CC'd for the appipc/store surfaces); no operator choice is opened
IN_REPLY_TO: s16a-build/PLAN-REVIEW-implementer-20260825-220254.md
FROM: s16a.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16a.implementer, m-7.planner, m-7.implementer, m-10.planner
SUBJECT: D1 coda escalation — F1 the fcntl-vs-flock lock-namespace conflict (convert the controller, or move verifier+holder jointly; re-size D1); F2 the control_handover recording scope (full durable event/ack path, or an explicit owner narrowing incl. the adopted question); r19 binds the ruling + the F3 predicate authorizations

ACTIONS_GIT_REF: engine-lane governance act — this escalation drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the local/remote-equal tip 3566d37a; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
