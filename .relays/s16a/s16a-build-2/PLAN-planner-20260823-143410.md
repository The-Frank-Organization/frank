## PLAN r2 — WP1: the 63-test RED seam-contract battery (SUCCESSOR to `s16a-build/PLAN-planner-20260823-141142.md` @ `aa9e5b01…`, superseding it; the implementer's two hold findings of `s16a-build/SITREP-implementer-20260823-143058.md` executed — the B/E ledger question routed to master PRE-TOKEN, the pre-existing-battery command made exact — plus the filed-parent condition bound. Every other r1 decision byte is preserved; the diffs are §3.8, §5, §7.2, §9.)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-2
PARENT_DISPATCH_ID: s16a-build
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the operator's next gate is the terminal MERGE-GATE; the pre-token asks live with master (the B/E disposition) and the checkpoint cadence (banking)
GRILL_REQUIRED: no — the design is master's ratified plan r5 + the promoted r9 basis + the charter; no new design decision is opened at this seat
FILED_AT_LOCAL: 20260823-143410
IN_REPLY_TO: s16a-build/SITREP-implementer-20260823-143058.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-8.planner, m-9.planner, m-7.planner
SUBJECT: WP1 plan r2 — the r1 battery unchanged in substance; B/E coverage escalated to master pre-token (`s16a-build/SITREP-planner-20260823-143331.md`); exact package-exclusion command pinned; PLAN-REVIEW proceeds once these bytes are checkpoint-banked

## 1. Locked design (pointers, never paraphrase — on conflict the pinned bytes govern)
- The plan of record: `master/STEP-3-T4-S16A-PLAN.md` r5 @ `71bf6e35…` §2 WP1 — verbatim authority for this WP.
- The decomposition basis: `master/STEP-3-T4-SEAM-CONSISTENCY-2026-08-22-r9.md` @ `acfd358a…` §2 — THE row ledger. Each test binds its row's ACCEPTANCE cell (`CT-xxx`) as joined in the ninth column; never a prose summary.
- The charter: `master/subteams/s16a-conformance/CHARTER.md` @ `2f20768e…` (cardinal rules; write fence; close bar).
- The contract bases: the charter's BASIS_HASHES (all re-verified drift-none at both seats, my readiness `2e5608be…`), with `A-M10-SEAM` read AS AMENDED BY the WP0(c) amendment `b0450777…` for C01/C02/C04.
- The four PM read-first returns (READ THESE BEFORE writing the rows they flag — m-10 `75fc5358…` · m-8 `5294d52c…` · m-9 `67158281…` · m-7 `4db70ebc…`), folded below as §4's normative test-design law.

## 2. Scope (exact; everything else is out)
- Create branch `s16a-conformance` from LAUNCH_BASE `ff1193d7e63b52a085eeea486055ae9f2c971896` (branch-only; a separate worktree, e.g. `harness-s16a-conformance`, is recommended mechanics — implementer's call, disclosed in the first IMPL SITREP).
- ONE new Go test package `frank/test/seam/` (inside the module `github.com/jackli/frank`; the Go internal-package rule admits `internal/appipc` + `internal/appctl/*`, `internal/connector/*`, `internal/worker/*` imports from there).
- 63 test functions — one per NON-excluded r9 §2 row: `G01–G19` (19), `A01–A19` (19), `B01–B11` (11), `C01–C10` (10), `D01, D02, D04, D05` (4). `CT-D03` is NOT written (the sole excluded row).
- ONE census tool in `frank/test/seam/` (script or `go run`-able helper) deriving the row→GREEN/RED table from `go test -json` output — the launch census is SCRIPT-DERIVED, never hand-tallied.
- The WP1-close SITREP (implementer → planner → master) carrying the census artifact; then the pair HOLDS for the PM fidelity round (master routes it; one relay per PM confirming each row's test BINDS its obligation leg).
- In-fence file surface for WP1: `frank/test/seam/**` and `frank/.relays/s16a/**` ONLY. `frank/go.mod`/`go.sum` are arbitration-only — WP1 needs no new dependency; needing one = stop and escalate.

## 3. Test-design laws (each is a charter/plan/PM obligation, not style)
1. **Contracts, never code.** Every assertion derives from the pinned bases (as amended) and the row's CT cell. Reading current module source to learn "what it does" is legitimate only for locating exported surfaces to probe; an expectation sourced from current behavior instead of the contract is the defect class this battery exists to kill.
2. **Compile-clean, RED-by-assertion.** The whole package MUST compile and run at the launch base. A gate row's nonconformance must surface as a test FAILURE (assertion/`t.Fatal`), NEVER a compile error — a compile break silences the other 62 rows. Concretely: probe missing struct members via wire-bytes/JSON round-trips or reflection (field lookup fails → `t.Fatalf` → RED); probe never-built pieces via behavior (C09: dial through the EXISTING `internal/appctl/brokerclient` — no listener answers → RED; A12/C07: drive the existing entry points and assert the contract-required effect, absent → RED). Never reference an identifier that does not exist at base.
3. **RED means FAIL, never skip.** No `t.Skip` in any gate row — a skipped test binds nothing (vacuity). Every expected-RED row fails with a message naming the row ID and the missing/mismatched obligation.
4. **Anti-vacuity is law.** A gate row GREEN at first contact is a FINDING (the test binds nothing, or the ledger row is wrong): STOP on that row, file it in the WP1-close SITREP for routing up — never silently re-write the test to force RED, and never count it a pass. Symmetrically: every G row RED at base is a finding (regression pin broken or test misbound).
5. **Reachability preconditions named.** Each test reaches its gate through the enclosing path the contract names (the gate-claims-need-reachability law); a test that asserts a gate it never reaches is vacuous.
6. **Binding visible for the fidelity round.** Each test function carries a doc comment: row ID · the CT cell reference (r9 §2 row) · governing selector(s) · which §4 flag(s) apply. The PM fidelity round verifies BINDING; make it checkable from the test text.
7. **One test per row.** Multiple assertions inside a row's test are fine; a 64th test function for coverage that fits NO row is not — the one candidate (the B/E carriage note) is ALREADY escalated to master (§5); any further no-row coverage discovered during the build is likewise routed up, never written.
8. **Existing batteries stay green — exact command.** At every commit, from `frank/`: `go test -p=1 -count=1 $(go list ./... | grep -v '^github.com/jackli/frank/test/seam$')` stays green (all pre-existing packages; the ONLY exclusion is the seam package itself). The seam battery runs separately: `go test -count=1 ./test/seam/ -run 'TestCT_'` (expected RED at base; the census tool consumes its `-json` form). The seam package is the gate instrument, exempt from green-at-every-commit until WP5, where the full unexcluded battery `go test -p=1 -count=1 ./...` must be green.

## 4. The twelve PM read-first flags → row bindings (normative; read the four returns in full first)
- **m-10/1 → C01, C02, C04:** the WP0(c) amendment `b0450777…` is NORMATIVE — C01 the closed `build_info{version, commit, built_at}` (each required nonempty UTF-8 ≤64 B; unknown member rejected; total ≤256 B; a string or fourth member REJECTED); C02 `epoch_query` is ONE-WAY, answered by the EXISTING `epoch_update` (no new reply type; unregistered query type fails closed); C04 `refusal_stage` present IFF `rejected_local`, closed two-token set `{pre_freeze, post_freeze}`, absent on every other disposition. Bind the amendment's registered shapes, not pre-amendment seam text, not any realization.
- **m-10/2 → all m-10 rows; A19 sharpest:** LIVE UNSTRUCK lines govern in all five m-10 docs. A19's ceiling is `appipc.MaxToolCallsPerTurn` (128 production / 4 reduced) at the F59 issue gate — the worker carries NO local constant; a test greening against a worker-side 64 tests a dropped bound.
- **m-10/3 → a prohibition, not a test:** row-2b B/E digests ride ONLY the DATA-P `m8.dataP_reply.v2` channel; `attempt_result`/`provider_attempts` do NOT carry them post-amendment. Do NOT write a test reading row-2b B/E off CTRL-C persistence (an unregistered dependency). The related m-8 carried note is the §5 escalation.
- **m-8/1 → B01, B02:** read r12 + `A-M8-BE` TOGETHER; the uniform-v2 output-channel obligation lives ONLY in the addendum (`:47/:51/:132/:153`). The B-row acceptance is "v1 producer rejected, v2 accepted" — bound against the addendum. A test written from r12 alone passes a v1 producer and misses the obligation.
- **m-8/2 → B08 (+ C04 corollary):** TWO distinct closed enums, tested separately by name — `disposition` (6-member: `{sent_completed, denied(<token>), rejected_local(<reject_reason>), transport_failed, unknown, cancelled(<cancel_point>)}`) and the m-8-owned `reject_reason` (4-member, nested inside `rejected_local`). A single "six vs four" enum test is a category error.
- **m-8/3 → B04 (and any row touching loss/cancel terminals):** eligibility ≠ decidability. `transport_failed`/`cancelled` are CONDITIONAL on wire observability — a fresh-dial `{1,0,0,0}` or nothing-written `{1,1,≤1,0}` cut is NOT eligible ⇒ `unknown`. Rows 9/10 (DATA-P/connector loss, worker crash) yield NO carrier and NO row-of-record — assert absence there, never a carrier/outcome token. No wire-compare assertion on a non-`{1,1,1,1}` cut.
- **m-9/1 → B01, B02, B07, B10:** worker r11 `06a6d273…` NEWEST bytes are the spec — §2.2 uniform v2 event+reply parsers (no mixed stream), §6.4's THREE absence classes, §10's v2 fixtures (version-decodability; planted-B byte-verbatim on BOTH paths such that a reply-only implementation FAILS; E-independence). Read the r8→r11 review chain (`051616`/`053731`/`055450`→`061204`) before writing these. Live replacement text governs; struck/fenced bytes are history.
- **m-9/2 → B05:** the request leg STAYS v1 — `m8.llm_request.v1` is not versioned by the addendum (output channels only). Do not "upgrade" the request leg; B05's target is r12 §1.1.
- **m-9/3 → A08:** the polarity is FLIPPED (r3): the worker's NON-emission of `generation_id` on consume/outcome wires CONFORMS; CP is the non-conforming side. CT-A08 asserts the four-field `consume_ticket{ticket_id, turn_epoch, canonical_tool_name, canonical_args_digest}` carries NO generation member AND m-10 binds generation/run from the channel-assigned identity (seam `:225`), never from frame content.
- **m-9/4 → A05:** epochs/counters are STRINGS on the wire (canonical-decimal-uint64 grammar, lifecycle §2.7); the worker's uint64 bodies are the nonconformance to CATCH, not the shape to preserve.
- **m-9/5 → B11:** binds the WHOLE lifecycle §2.4 bounds-set — max attempts/turn · max tool calls/turn COUNTING denied authorizes with the row-less `authorize_reject{turn_budget_exhausted}` ceiling terminal · max wall-clock · m-8's stream cap at its site · max captured tool output with an HONEST `truncated` flag — not one constant.
- **m-7/1 → C09:** READ ORDER: study rev8 `64f9136e…` is a governed ADDITIVE DELTA over byte-frozen r11 `9331ea88…` — where the study names an r11 clause THE STUDY GOVERNS, and its supersessions CUT machinery a reader of r11 alone would wrongly expect (no transition ledger/crossing rows/`completed-before-install`/five-row recovery matrix; no `crossing_count` in `epoch_installed`; `Describe`'s crossing-record posture = telemetry-residual). CT-C09 answers the EXISTING client: `internal/appctl/brokerclient/session.go` is the dial the (future) server must accept; `appipc/msgs_broker.go` carries the wire vocabulary. At base: dial → no listener → RED.

## 5. The B/E carriage question — ESCALATED, pre-token (supersedes r1 §5)
The m-8 carried note (B/E producer-first digest carriage into `provider_attempts`, `A-M10-CARRIAGE` `cd17db32…`) fits NO r9 row's CT cell — both seats' independent finding. It is now ROUTED TO MASTER as a locked-surface/ledger class hit: `s16a-build/SITREP-planner-20260823-143331.md` (three dispositions offered: bind-in-row / ledger act / deferred-with-carrier). BINDING: the BUILD TOKEN does not issue before master's disposition is on the trail. If the ruling changes battery scope (a proper ledger act), a PLAN r3 carries it; if bind-in-row or deferred, this r2 stands and the battery is exactly 63.

## 6. Boundary contract
Writes: `frank/test/seam/**` (63 CT tests + census tool) on branch `s16a-conformance`; relay files under `frank/.relays/s16a/**`.
Reads: the pinned contract bases (as amended); the three modules' exported surfaces (probe targets only).
Target entity: the launch census (row→state table, script-derived) — the WP1 deliverable of record.
Downstream consumer: the PM fidelity round (binding verification per row) · WP2/WP3/WP4 (the battery is their gate) · WP5's r10 close census (same instrument at the merged object).
Contract: test names `TestCT_<rowID>` (optional descriptive suffix after a further underscore); doc-comment binding block per §3.6; census format one line per row `<rowID> <GREEN|RED>` + a summary count.
Proof: E2 — the battery run + census at the launch base, command output in the WP1-close SITREP.
No-consumer action: n/a — every consumer above is chartered.

## 7. Acceptance criteria (WP1 done means exactly this)
1. Branch `s16a-conformance` exists at/from `ff1193d7…` exactly; `git merge-base` proves it; ONLY `frank/test/seam/**` (+ lane relays) differ from base.
2. The seam package compiles; `go vet ./test/seam/` clean; the pre-existing batteries green by the EXACT §3.8 command (`go test -p=1 -count=1 $(go list ./... | grep -v '^github.com/jackli/frank/test/seam$')` from `frank/`).
3. Exactly 63 `TestCT_` functions; their row-ID set EQUALS the 63 non-excluded r9 row IDs (the census tool verifies the name↔row bijection mechanically — missing or extra rows are census errors).
4. Script-derived census at the launch base: all and only G01–G19 GREEN; all 44 gate rows RED, each failing on its named obligation. Every deviation (gate GREEN / G RED) is a per-row FINDING in the WP1-close SITREP — battery halted rows stay as-written pending routing; no silent test rewrites.
5. Every test carries the §3.6 binding doc comment; the twelve §4 flags demonstrably applied to their rows.
6. The WP1-close SITREP filed with: census artifact, findings (or none), and the fidelity-round request; then the pair HOLDS. No WP2 byte before the fidelity round returns through master.

## 8. Out of scope (a touch = escalation by construction)
Any production/source byte anywhere; any fix (WP2), build (WP3), extraction (WP4); `frank/go.mod`/`go.sum`; `master/**`; `frank/docs/**`; closed slice lanes `frank/.relays/s13|s14|s15/**`; the exit oracle + every `master/exit-fixtures/**` byte; any conforming-side edit; any dependency addition; any E3/composition/exit claim.

## 9. Sequencing after your review (the token's full precondition set, restated exactly)
Your PLAN-REVIEW verdict TO me (`approve | must-revise | reject-narrow | human-decision-required`), parented to THIS relay's dispatch — issued only once these r2 bytes are checkpoint-BANKED (tracked in a commit), per your filed-parent condition; the banking ask rides the escalation relay and the operator's checkpoint cadence. The token then issues only when ALL of: (a) your PLAN-REVIEW approve is filed and banked-or-bankable on the trail; (b) master's disposition of the §5 escalation is on the trail and does not demand a PLAN r3; (c) SCOPE_DIFF over the locked plan returns all-in. On the token: you open the branch and build WP1 straight-through (task order and mechanics inside WP1 are yours; RED-first is inherent here — the battery IS the RED). Objections and better-ways ride the review; spec-base defects found while writing tests are escalations UP through master to the owning PM, never local fixes.

ACTIONS_GIT_REF: docs-lane disk action — this PLAN r2 + the sibling escalation `s16a-build/SITREP-planner-20260823-143331.md` + two INDEX rows under `frank/.relays/s16a/`; no source, test, branch, or commit byte (the branch opens only on the token).
FINAL_GIT_STATUS_SHORT: (shared literal capture for this two-relay act, taken after both files + rows existed, inserted before lint:)
 M frank/.relays/s16a/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16a/s16a-build-2/
?? frank/.relays/s16a/s16a-build/
