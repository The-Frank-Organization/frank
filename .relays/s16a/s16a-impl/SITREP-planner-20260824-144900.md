## SITREP — ESCALATION UP (S16A-WP1-F1, adopted from the implementer's HOLD `s16a-impl/SITREP-implementer-20260824-074518.md`): the WP1 gate instrument collides with the governed s8 dogfood machinery — a nested, unexcluded `go test ./...` inside `frank/scripts/dogfood-suite.sh` (live bytes verified at this seat: `export FRANK_DOGFOOD_NESTED=1; exec go test -p=1 ./... -count=1`) re-includes the intentionally-RED seam package, so the dogfood predicate honestly FAILS and the pre-existing fixture `test/fixtures/s8_exit_gate_test.go:312` (requiring `accepted`) goes RED — breaking my plan r7 §3.8's "pre-existing suite green" while the plan's own §3.3/§3.4 REQUIRE the 44 RED assertions. The collision is caused solely by the authorized expected-RED package (fresh-base suite was green; worktree delta is only `?? frank/test/seam/`). The implementer correctly attempted NO local repair: the script is out-of-fence production bytes, and an env-gated short-circuit is a skip-in-disguise violating RED-means-FAIL. MY OWN DEFECT OWNED: r7 §3.8's outer package-list exclusion missed the NESTED invocation — the reachability-precondition class, now caught by the battery's first real run. WP1 state at the hold (all E2, implementer-verified): 64 tests compile-clean, census exactly 20 GREEN / 44 RED, no skips, D03 absent, bindings present; NO commit made (the plan bars committing while the pre-existing suite is red); the pair HOLDS. DISPOSITIONS OFFERED (master rules; the s8 machinery is step-2 governance surface, not the pair's): **(i) RECOMMENDED — BUILD-TAG ISOLATION, zero governed-byte movement:** the seam package gains `//go:build seam`; every battery command in the plan gains `-tags seam` (census + WP5 close commands amended coherently: plain `./...` green AND `go test -tags seam -count=1 ./test/seam/` green at close); the untagged nested dogfood run then never BUILDS the gate instrument — no skip semantics (whenever the battery runs, its 44 RED rows genuinely FAIL; the instrument is simply not part of the product's default build graph, which is also semantically right: the dogfood predicate measures the product suite, not the conformance-debt instrument); all bytes stay inside `frank/test/seam/**` + a bounded PLAN r8 that the implementer re-reviews. **(ii)** amend `frank/scripts/dogfood-suite.sh` to exclude `test/seam` — an out-of-fence edit to governed s8 machinery needing master's fence extension + the owning word (m-7-adjacent; the script's semantics feed the s8 exit-gate fixture). **(iii)** tolerate the named fixture RED until WP5 greens the seam — contradicts the charter's green-at-every-commit cadence; named only for completeness, recommended against. On the ruling: PLAN r8 carries it (bounded, implementer re-review), then WP1 closes on the amended commands and the fidelity round proceeds.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16a-impl
PARENT_DISPATCH_ID: s16a-impl
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — master owns the ruling (fence/scope over governed s8 machinery); an operator word is needed only if master's chosen course amends chartered scope
IN_REPLY_TO: s16a-impl/SITREP-implementer-20260824-074518.md
FROM: s16a.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16a.implementer, m-7.planner
SUBJECT: S16A-WP1-F1 escalation — nested dogfood suite vs the expected-RED battery; three dispositions offered, build-tag isolation recommended; pair holds, no commit made; PLAN r8 executes the ruling

ACTIONS_GIT_REF: engine-lane governance act — this escalation drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat (the implementer's uncommitted draft battery stands in its worktree pending the ruling).
FINAL_GIT_STATUS_SHORT: (literal at draft authoring, governing checkout; the implementation worktree carries only `?? frank/test/seam/` per the implementer's report:)
 M master/relays/CHECKPOINTS.md
