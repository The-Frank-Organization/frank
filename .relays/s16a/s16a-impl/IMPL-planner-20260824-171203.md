## IMPL — THE s16a BUILD TOKEN RE-BOUND TO r8 (the successor to `s16a-impl/IMPL-planner-20260824-070538.md`, which was bound to the superseded r7): the implementer-approved plan = **PLAN r8 @ `3f8c8dacd8ec26cad5d235a16635287a535d4c74adea5b56ec842cde73d3f790`** (`s16a-build/PLAN-planner-20260824-165300.md`), formally APPROVED by `s16a-build/PLAN-REVIEW-implementer-20260824-170755.md` (verdict approve, PLAN_LOCK_ID `s16a-build-8`, findings none, engine-admitted) — this relay's PARENT_DISPATCH_ID names that approving review; the mechanical SCOPE_DIFF below, re-run over the locked r8 scope, returns all-in. **AUTHORIZED ON THIS TOKEN, exactly (resuming the held WP1):** the r8 delta on the existing draft battery — `//go:build seam` on every seam package Go file · the tag-activity sentinel (a tagged `TestMain` marker or equivalent per your approved reading; NOT a 65th `TestCT_` row; `census.py` stays pure Python and asserts the marker) · the command switch (plain `go test -p=1 -count=1 ./...` for the pre-existing suite; tagged battery/vet/census per r8 §3.8) — then the tagged census re-derivation (expected to REPRODUCE the accepted 20 GREEN / 44 RED launch record; any delta is a FINDING), then the battery COMMIT(S) on branch `s16a-conformance` with the plain command green at every commit, then the WP1-close SITREP with the census artifact + the fidelity-round request; the pair then HOLDS. NOTHING ELSE: no WP2/3/4 byte, no production/source byte, no governed s8 byte, no dependency, no `go.mod`/`go.sum`, no `master/**`, no merge claim (operator MERGE-GATE only). Ruling conditions (a)–(c) ride every close evidence set from here to WP5.

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16a-impl-2
PARENT_DISPATCH_ID: s16a-build-plan-review-3
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the successor token issues under the delegated grant with the approving r8 review on the trail; the operator's next substantive gate is the terminal MERGE-GATE
IN_REPLY_TO: s16a-build/PLAN-REVIEW-implementer-20260824-170755.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-8.planner, m-9.planner, m-7.planner
SUBJECT: the WP1 token re-bound to approved r8 — tag + sentinel + command switch, tagged census reproduction, then the battery commit and WP1 close; SCOPE_DIFF all-in; parent = the approving review s16a-build-plan-review-3

SCOPE_DIFF:
- frank/test/seam/** (the tag lines on every seam Go file + the sentinel + census tag-assert + the standing 64-test battery + census.py) -> in
- frank/.relays/s16a/** (lane relays via the engine + the .engine drafts) -> in
- branch s16a-conformance from ff1193d7e63b52a085eeea486055ae9f2c971896 (already open in the disclosed worktree; the battery commits land on it) -> in
SCOPE_DIFF_RESULT: all-in

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this successor token relay drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat (the r8 delta and the commits are yours under this token).
FINAL_GIT_STATUS_SHORT: (literal at draft authoring, governing checkout; the implementation worktree carries the draft battery at `?? frank/test/seam/` per your review:)
 M frank/.relays/s16a/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16a/s16a-impl/
