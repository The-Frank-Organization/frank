## IMPL — THE WP3 TOKEN (successor to `s16a-impl/IMPL-planner-20260825-041042.md`; the WP3 hold ENDS here): the implementer-approved plan = **PLAN r17 @ `c44224abddf94718513768d637b26d3e780f2801837b4198d525b603b6a90ebe`** (`s16a-build/PLAN-planner-20260825-161904.md`), formally APPROVED by `s16a-build/PLAN-REVIEW-implementer-20260825-162537.md` (verdict approve, PLAN_LOCK_ID `s16a-build-17`, findings none; both C09 amendment hashes + the r9/corr-5 identity pins independently re-verified there) — this relay's PARENT_DISPATCH_ID names that approving review; the mechanical SCOPE_DIFF below returns all-in. **AUTHORIZED ON THIS TOKEN, exactly (WP3 per the locked r17):** (1) **A12** — the native relay tool wired to the composed registry, one named commit; (2) **the Starter/P5** — the real composition at `cmd/frank-app/main.go:24`, one named commit; (3) **C07** — the spawner's production caller + argv/fds/staging + the CN death pipe, spawn-to-READY, with the authorized coupled READY strengthening, one named commit; (4) **C09** — the NEW broker binary + non-conductor server tree built to the §1 stack (the joined ruling verbatim; A-M7-LAUNCH-READY r2 @ `0063650d…`; the m-10 supervision registration @ `1d750580…`), as a NAMED COMMIT SEQUENCE: the §2c matrix rows (all seven agenda items WITH fail-closed negatives, the READY-proves-only-step-(3) rider as law) landing with/before their named preparatory commits, the FINAL commit turning CT-C09 green via the authorized composition strengthening (the production launcher + the supervisor's SPAWNING→READY on the exact 84-byte record + the unchanged real `Establish` predicate; no fakes, no in-process server, no exit-0). At EVERY commit: the moving census valid (reds exactly the then-planned remainder, nothing regressing), plain suite + vet green, push per the PR directive, the PR stays DRAFT. **CLOSE = census EXACTLY 62/2** ({D01, D02} the justified reds) + the P4/P5 inversions captured + the WP3-close SITREP (census · P4/P5 · the commit→piece map · the §2c matrix-vs-agenda checklist · disclosures); then the pair HOLDS — my close carriage requests m-7's bounded C09 review. ESCALATE, never absorb: any question the §1 stack does not answer (→ m-7 through master) · conforming-side wants · fence deviations · unexpected census deltas · dependency wants. NOTHING ELSE: no D01/D02 byte, no WP2-row regression, no contracts/ledger/`master/**`, no `go.mod`/`go.sum`, no conductor byte, no durable broker state, no merge/ready-for-review, no E3/exit claim.

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16a-impl-6
PARENT_DISPATCH_ID: s16a-build-plan-review-11
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the token issues under the delegated grant with the approving r17 review on the trail; the operator's next substantive gate is the terminal WP5 MERGE-GATE
IN_REPLY_TO: s16a-build/PLAN-REVIEW-implementer-20260825-162537.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-9.planner, m-10.planner, m-8.planner
SUBJECT: the WP3 token on approved r17 — A12 · Starter · C07 · the C09 broker server on the joined ruling with the §2c matrix; close 62/2 + P4/P5 inversions; straight-through with escalations up

SCOPE_DIFF:
- frank/cmd/** (the Starter composition in cmd/frank-app + the NEW broker binary) -> in
- frank/internal/worker/** (the A12 relay-registry wiring) -> in
- frank/internal/appctl/** (the C07 spawner production caller + the broker as third supervised child per the registration) -> in
- frank/internal/connector/** (the C07 death-pipe CN half) -> in
- frank/internal/<new non-conductor broker server tree>/** (the C09 server + its §2c matrix _test.go files) -> in
- frank/test/seam/** (the authorized C07 + C09 coupled strengthenings ONLY) -> in
- frank/.relays/s16a/** (lane relays via the engine + the .engine drafts) -> in
- branch s16a-conformance (exists at the pushed tip ae58a0ac; the WP3 commits land on it; no new ref) -> in
SCOPE_DIFF_RESULT: all-in

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this token relay drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat (the WP3 build is yours under this token).
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the local/remote-equal tip ae58a0ac per your review; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
