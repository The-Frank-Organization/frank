## IMPL — THE TOKEN RE-BOUND TO r9 (the WP1 battery fold; successor to `s16a-impl/IMPL-planner-20260824-171203.md`): the implementer-approved plan = **PLAN r9 @ `def789764a2b5d8e8c9fff19161747665bfe1b00693a0ccaf2c981b8bb761603`** (`s16a-build/PLAN-planner-20260824-213626.md`), formally APPROVED by `s16a-build/PLAN-REVIEW-implementer-20260824-214918.md` (verdict approve, PLAN_LOCK_ID `s16a-build-9`, findings none, engine-admitted; work-list arithmetic confirmed: 30 unique does-not-bind rows + G04/G08 = 32 primary changed rows, + the B09/A16/C06 riders = 35 changed test functions, no new row, D03 absent) — this relay's PARENT_DISPATCH_ID names that approving review; the mechanical SCOPE_DIFF below returns all-in. **AUTHORIZED ON THIS TOKEN, exactly:** the r9 §2 fold on the committed battery — every strengthening per the NAMING PM's return verbatim (the returns govern at their bytes), your review's approved A02 locus reading BINDING (rejection proved BEFORE the journal-open path by a pre-journal control-flow witness; filesystem non-creation alone is insufficient), B08 additive (m-8's membership legs remain), G04/G08 forward-fixed, the riders taken — then the fold COMMIT(S) on `s16a-conformance` (plain `go test -p=1 -count=1 ./...` green at every commit), the refreshed tagged script-derived census (expected 20 GREEN / 44 RED; D04 the named watch-row; ANY delta per-row justified, never silent; a post-fold first-contact gate GREEN = ledger question routed UP), and the fold-close SITREP (census + delta justifications + rider dispositions); the pair then HOLDS for the m-9/m-10 bounded re-confirms through master. NOTHING ELSE: no WP2 byte (the `stream_lost` minting fix and the A02 admission-locus FIX are WP2 targets — the fold only strengthens their TESTS), no production/source byte, no contract/ledger byte, no dependency, no `go.mod`/`go.sum`, no new test row, no merge claim.

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16a-impl-3
PARENT_DISPATCH_ID: s16a-build-plan-review-4
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the successor token issues under the delegated grant with the approving r9 review on the trail; the operator's next substantive gate is the terminal MERGE-GATE
IN_REPLY_TO: s16a-build/PLAN-REVIEW-implementer-20260824-214918.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-8.planner, m-7.planner
SUBJECT: the fold token re-bound to approved r9 — 35 changed test functions per the PM returns verbatim; strict A02 locus witness; census 20/44 expected with D04 watched; then fold commit, refreshed census, fold-close SITREP, hold for re-confirms

SCOPE_DIFF:
- frank/test/seam/** (the 35 changed test functions per r9 §2 + any helper/fake-seam support code inside the package) -> in
- frank/.relays/s16a/** (lane relays via the engine + the .engine drafts) -> in
- branch s16a-conformance (exists at 720bdd68; the fold commits land on it; no new ref) -> in
SCOPE_DIFF_RESULT: all-in

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this successor token relay drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat (the fold edits and commits are yours under this token).
FINAL_GIT_STATUS_SHORT: (literal at draft authoring, governing checkout; the implementation worktree is clean at 720bdd68 per your review:)
 M frank/.relays/s16a/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16a/s16a-build/PLAN-planner-20260824-213626.md
