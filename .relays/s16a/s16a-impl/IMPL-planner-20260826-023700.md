## IMPL — THE CODA + WP4 TOKEN (successor to `s16a-impl/IMPL-planner-20260825-163020.md`): the implementer-approved plan = **PLAN r19 @ `a9b630f9f7d8c67749093c0f704dfd5ca9e8000a0421f5589a8a79ef4706dbab`** (`s16a-build/PLAN-planner-20260826-022447.md`), formally APPROVED by `s16a-build/PLAN-REVIEW-implementer-20260826-023155.md` (verdict approve, PLAN_LOCK_ID `s16a-build-19`, findings none; the joined D1 ruling re-hashed there at `f5072ff0…`; the D01/D02 re-cut REDs reproduced pre-implementation; the Darwin floor cross-built) — this relay's PARENT_DISPATCH_ID names that approving review; the mechanical SCOPE_DIFF below returns all-in. **AUTHORIZED ON THIS TOKEN, exactly, IN PARALLEL per the standing ruling:** **(A) the WP3 coda, ~3 named commits, CONVERSION BEFORE PROBE pinned** — (1) the controller conversion to the `fcntl F_SETLK` family under the five ruled constraints incl. the one-descriptor invariant test and the blocking/bounded adoption acquisition; (2) the broker verification half (peer-PID shim · the probe after-accept-before-generation · concurrent accept + verified live-session replacement + old-session close · the four typed in-memory outcomes with per-outcome fail-closed negatives · the honest-controller end-to-end positive); D2's 32-CSPRNG/64-hex mint riding where you sequence it; the F2 narrowing LAW (no durable recording, no `adopted` recording, no m-10 byte, nothing labeled "recorded"); CT-C09 GREEN at every coda commit, NO census delta expected — deviations stated. **(B) WP4** — the ONE shared canonicalizer (refuse-everywhere, the ruled clause verbatim) REPLACING the three JCS implementations, removed not shadowed; the exhaustive D02 vector corpus; the F3 re-cut CT-D01/CT-D02 landing red-then-green for the contracts' reasons; coupled module-test updates only where refuse-everywhere changes wrongly-pinned behavior, each named in the close report. At EVERY commit: the moving census valid, plain suite + vet green, push per the PR directive, the PR DRAFT. **CLOSE = census EXACTLY 64 GREEN / 0 RED / 64** + the close report (census · commit→item map · the named coupled updates · the D3 sixth-locus disclosure · the c4 acquisition choice named · deviations); then the pair HOLDS — my carriage requests m-7's bounded coda review and reports the all-green battery; WP5's plan follows from this seat. ESCALATE, never absorb: any m-10 recording byte want · dependency wants · D03/`fieldspec` touches · contract questions · unexpected census deltas. NOTHING ELSE: no merge/ready-for-review, no CI/CD, no E3/exit claim.

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16a-impl-7
PARENT_DISPATCH_ID: s16a-build-plan-review-13
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the token issues under the delegated grant with the approving r19 review on the trail; the operator's next substantive gate is the terminal WP5 MERGE-GATE
IN_REPLY_TO: s16a-build/PLAN-REVIEW-implementer-20260826-023155.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-7.implementer, m-10.planner, m-9.planner, m-8.planner
SUBJECT: the coda + WP4 token on approved r19 — fcntl conversion-before-probe under the five constraints, the verification half with typed outcomes, D2, the refuse-everywhere extraction + D02 corpus + F3 re-cuts; close 64/0; parallel, straight-through, escalations up

SCOPE_DIFF:
- frank/internal/appctl/brokerclient/** (the controller conversion + the one-descriptor invariant test) -> in
- frank/internal/broker/** (the verification half + its negatives/positive + platform-tagged helpers) -> in
- frank/cmd/frank-app/** (the D2 mint fix) -> in
- frank/internal/<the new shared canonicalizer package>/** (D01) + the replaced JCS files in appipc/connector/worker (removal, not shadowing) -> in
- frank/internal/** coupled module-test updates ONLY where refuse-everywhere changes wrongly-pinned behavior (each named) -> in
- frank/test/seam/** (the F3 CT-D01/CT-D02 re-cuts + any authorized coupled updates) -> in
- frank/.relays/s16a/** (lane relays via the engine + the .engine drafts) -> in
- branch s16a-conformance (exists at the pushed tip 3566d37a; the commits land on it; no new ref) -> in
SCOPE_DIFF_RESULT: all-in

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this token relay drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat (the coda + WP4 build is yours under this token).
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the local/remote-equal tip 3566d37a per your review; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
