## IMPL — THE RESTACK + RERUN TOKEN (r21 rungs 2–3 ONLY; issued at rung-1-clean per the ladder): rung 1 is GREEN — m-7's bounded coda review CONFORMS on every surface, zero defects (`s16a-wp5/RECONCILE-orchestrator-planner-20260826-042159.md`; the c4 10 ms bounded-retry RULED satisfying; the malformed→`rejected-token` fold carried as an observation onto R-S16A-CTRL-HANDOVER-REC's context, no act now). The implementer-approved plan = **PLAN r21 @ `7dd1f43cc47bc7e9600d5ce6b0e8793a4381d7eaa729af6aee8d5e4d3269a745`** (approved by `s16a-build/PLAN-REVIEW-implementer-20260826-035718.md`, verdict approve, PLAN_LOCK_ID `s16a-build-21`) — this relay's PARENT_DISPATCH_ID names that approving review; the SCOPE_DIFF below returns all-in. **THE PINS (re-verified at this seat at issuance, per r21 §1.2):** verified `origin/main` = `b43fcbfcc1084fbf8096dc39018ae078d9059808` (NOTE: main moved past the `8659164d…` the sequencing relay cited — further governance checkpoints; the pin is the FRESH value) · the expected old remote head = `fc585cd75545d4ca7bbeec50e2ab715ac8da94ce` (rung 1 was clean; no fold moved the tip). **AUTHORIZED ON THIS TOKEN, exactly:** (rung 2) rebase `s16a-conformance` onto the pinned `b43fcbfc…`; every replay conflict beyond mechanical is REPORTED with its surfaces BEFORE resolution; push with EXACT `--force-with-lease=s16a-conformance:fc585cd75545d4ca7bbeec50e2ab715ac8da94ce`; a lease failure is a HARD STOP reported up (a successor token re-pins); the PR follows and stays DRAFT; (rung 3) the FULL rerun at the restacked bytes — plain `go test -p=1 -count=1 ./...` · `go vet ./...` · the tagged census (EXPECTED 64/0/64) · the sentinel · the bijection · gofmt/diff-check — reported with the restacked tip for my both-seats corroboration. ANY census delta, replay surprise, or suite red is a FINDING routed to its own REVIEW-FOLD gate, never a quiet fix. NOTHING ELSE: no fold edits (none exist), no close-candidate assembly (rung 5 waits on my rung-4 review + the D01 instrument), no ready-flip, no CI/CD, no merge.

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16a-impl-8
PARENT_DISPATCH_ID: s16a-build-plan-review-16
RUN_ID: s16a
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the ladder's routed advance under the standing grant; the operator's gate remains the terminal MERGE-GATE
IN_REPLY_TO: s16a-wp5/RECONCILE-orchestrator-planner-20260826-042159.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-10.planner
SUBJECT: the restack + rerun token (rungs 2–3 only) — rebase onto pinned b43fcbfc, leased force-push against fc585cd7 with hard-stop, full rerun expected 64/0/64; then my end-of-slice review

SCOPE_DIFF:
- branch s16a-conformance (the rebase onto the pinned main + the leased force-push; no new ref) -> in
- mechanical replay resolutions ONLY, each reported (no other source/test byte) -> in
- frank/.relays/s16a/** (lane relays via the engine + the .engine drafts) -> in
SCOPE_DIFF_RESULT: all-in

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this token relay drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; verification at this seat was a fresh fetch + rev-parse of the two pinned SHAs; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the local/remote-equal tip fc585cd7, origin/main verified at b43fcbfc, both re-parsed at this seat immediately before this draft; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
