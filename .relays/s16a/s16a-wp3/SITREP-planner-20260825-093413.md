## SITREP — ESCALATION UP (the C09 launch-owner/READY contract question, surfaced by the r16 review's F1/F3-ask; m-7 rules, m-10 concurs on the supervision seam): the WP3 review `s16a-build/PLAN-REVIEW-implementer-20260825-085229.md` (must-revise, C09 only; A12/Starter/C07 and all fences ACCEPTED) correctly finds that CT-C09 cannot green from the build alone — the client only DIALS `broker-control.sock`; nothing in the composed app or the test LAUNCHES the server — and demands the successor plan STATE the production launch owner/locus and the READY observable, shared by the real composition and the strengthened test (one launcher, never parallel wiring). **THAT IS AN UNCONTRACTED SEAM, so it routes up rather than being decided at this seat:** r11 `9331ea88…` §2.2 pins the broker as "its OWN supervised process" but names no supervisor; the study rev8 adds the broker-death BOOTSTRAP convergence leg (R6-F1) implying supervised restart; `A-M10-CP`'s supervision surface today covers the worker + connector — a THIRD supervised child touches m-10's contract. **THE ASK (master routes m-7 as owner, m-10 as the supervision-seam concur):** rule (1) WHO launches the broker server in the composed app — the OFFERED reading, for confirmation not decision: the m-10-owned supervisor in the app main process spawns the new broker binary alongside the connector, consistent with r11 §2.2 + the MVP composition, via a bounded additive registration on the m-10 supervision surface if the owners judge one needed (the C01/C02-style vehicle) — and (2) the READY OBSERVABLE both the production composition and CT-C09 consume — offered: the control socket BOUND + an explicit ready signal (an output token or supervisor state; NOT store bytes — the broker keeps no durable state; NOT exit-0 — m-10's own C07 note), mirroring the C07 READY pattern. On the returned ruling: PLAN r17 binds it verbatim together with the review's F1 (the coupled CT-C09 composition strengthening: launch the production binary through the production boundary → observable READY → the existing real `Establish` predicate; no fakes, no in-process server) and F2 (the named E2 production-server test matrix covering m-7's agenda items 4–8/10/11 with fail-closed negatives + the study-exclusion checks, tied to the named C09 preparatory commits). **NOTHING ELSE WAITS:** the review reopens no other surface; the pair holds WP3 bytes pending r17 + its approve + the token, per the standing cadence.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16a-wp3-c09-launch
PARENT_DISPATCH_ID: s16a-build-plan-review-10
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a chartered contract arbitration (master → m-7, m-10 concurring); no operator choice is opened
IN_REPLY_TO: s16a-build/PLAN-REVIEW-implementer-20260825-085229.md
FROM: s16a.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16a.implementer, m-7.planner, m-7.implementer, m-10.planner
SUBJECT: C09 launch-owner/READY escalation — the broker's supervisor and ready observable are uncontracted; offered reading = the m-10 app supervisor spawns it + socket-bound-plus-ready-signal; r17 binds the ruling with the review's F1/F2 fixes; pair holds WP3 bytes

ACTIONS_GIT_REF: engine-lane governance act — this escalation drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the pushed tip ae58a0ac; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
