## PLAN r9 — THE WP1 BATTERY FOLD (bounded to `frank/test/seam/**`; executes master's fidelity ruling `s16a-wp1/RECONCILE-orchestrator-planner-20260824-212550.md` — the four PM returns ARE the fold spec, adopted as filed; this plan enumerates the work list and STATES the expected post-fold census per the ruling's §2. r8 remains the executed WP1 record; its laws — contracts-never-code, compile-clean RED-by-assertion, no skips, anti-vacuity STOP-and-report, binding doc comments, one-test-per-row, the tagged commands and conditions (a)–(c) — RIDE UNCHANGED. No WP2 byte: the m-8 live catch (fresh-dial `stream_lost` minting) and the A02 admission-locus fact are WP2 FIX TARGETS, credited not folded.)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-9
PARENT_DISPATCH_ID: s16a-wp1-fold
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the chartered pre-WP2 fold; the operator's next gate stays the terminal MERGE-GATE
IN_REPLY_TO: s16a-wp1/RECONCILE-orchestrator-planner-20260824-212550.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-8.planner, m-7.planner
SUBJECT: WP1 fold plan — ~32 row-namings strengthened per the naming PM verbatim (m-9's 22 + m-10's 10, rows A13/A14 shared) + G04/G08 pin forward-fixes; expected post-fold census 20/44 unchanged (D04 the one named watch-row); then fold commit, refreshed census, m-9/m-10 bounded re-confirms, WP2 opens

## 1. The fold spec (pointers; the returns govern at their bytes — on any wording delta between this plan and a return, THE RETURN WINS for its named row-half)
- The ruling: `s16a-wp1/RECONCILE-orchestrator-planner-20260824-212550.md` (adopts the returns as filed; rules A19/D04 re-cut, B10 re-cut, G04/G08 folded; the census contract).
- m-9's return: `master/relays2/s16a-wp1-fidelity/SITREP-planner-20260824-211301.md` @ `1e88c0a6…` — NORMATIVE for the 22 rows of §2.1–§2.4 below (his §2 classes + per-row fixes; his §1 riders).
- m-10's return: `…/SITREP-planner-20260824-211304.md` @ `ebf2c194…` — NORMATIVE for the 10 rows of §2.5 + the two pin findings (his §2 items 1–10, §3).
- m-8's return: `…/SITREP-planner-20260824-211444.md` @ `5f9e8d79…` — no fold rows; his B10/C02 observations are folded via m-9/m-10's rulings; his enum-membership legs on B08 STAND (see §2.1 note).
- m-7's return: `…/SITREP-planner-20260824-211344.md` @ `e8d197f0…` — no fold rows (C09 + A12-registry-leg bind); his twelve flag dispositions stand as the WP2 server-build agenda.
- All four hashes re-verified at this seat this act. Basis, laws, fences: PLAN r8 @ `3f8c8dac…` rides unchanged beneath this fold.

## 2. The work list (every edit inside `frank/test/seam/**`; the row's strengthened predicate must be the CONTRACT's acceptance, reached at run)
**2.1 m-9 class 1 — fourteen grep tests become behavioral predicates via the in-tree fake seams (m-9 §2 names the shape per row): A01 · A04 · A06 · A07 · A09 · A11 · B01 · B02 · B03 · B05 · B06 · B07 · B08 · B11.** Comment-stuffing must be impossible: the gate REACHED at run (e.g. A01 fake-M10 receives `genesis_committed` after journal durability; A09 mismatched descriptor ⇒ ZERO invocation; A11 withheld reply ⇒ typed fail-closed code; B01/B02 v2-accept/v1-reject at the live parser; B07 scripted terminal usage recorded; B11 the WHOLE lifecycle §2.4 bounds-set exercised behaviorally — over-limit loops per bound + an oversized output with an honest `truncated`). NOTE on B08: m-8's exact-membership legs for the two closed enums are RIGHT-GRANULARITY and REMAIN; the fold ADDS the worker-consumption behavioral leg (unknown-token fail-closed; total consumption) per m-9 — both PMs' halves satisfied, no deletion.
**2.2 m-9 class 2 — capacity becomes effect: A03 · A13(W) · A14.** One effect leg each (A03 journal lands at the app-chosen path; A13 the disclosed identity consumable at the S-4 read; A14 one rendered identity byte-compared against the manifest encoding) — merged with m-10's A13/A14 halves in §2.5.
**2.3 m-9 class 3 — under-constrained/wrong-reason: A02 · A05 · A10 · A12.** A02: rejection SEMANTICS + LOCUS (valid-nonce allowed control stays; the invalid nonce asserts NO journal genesis attempted — admission-locus per S-D1 §4b), NEVER a message substring. A05: a body round-trip on the executor CTRL-W wire + one canonical-decimal grammar negative (leading zero). A10: one demux round-trip per token through the executor's reply path (all six/four branches REACHED). A12: assert the relay registry / fake conductor RECEIVED exactly the read (count==1), not run-completion.
**2.4 m-9 class 4 — B10 re-cut (the m-9+m-8 joint finding):** DELETE the stdlib probe; drive an invalid-UTF-8 opaque item THROUGH the worker's item assembly/carriage (`runtime.go:197`'s path) and assert byte-exact arrival (the A-M8-BE byte-fidelity leg). A fully fixed worker must flip it GREEN — the census's exit meaning restored.
**2.5 m-10's ten (his §2 verbatim): A08** add the same `GenerationID`-absence check on `f59.OutcomeRequest` (types.go:73 still carries it — the added leg is RED today) · **A13(CP)** the closed `void_reason` enum membership + the W unknown-token fail-closed consumption leg · **A14** pin the expected key set FROM the manifest identity keys (`manifest.go:199-202`) + the recomputability leg (`tool_impl_ref` recomputed byte-exact from a manifest fixture) · **A19** the worker carries NO local bound (constant GONE; ceiling handling = consuming `authorize_reject{turn_budget_exhausted}`) — the equality predicate DELETED (master: the dropped-duplicate hazard) · **C02** add the receiver-effect leg (a query on a fixture CTRL-C yields the current `epoch_update`) + the unregistered-fails-closed leg (m-8's note, m-10's column) · **C03** add the decode→row leg + the unknown-token fail-closed leg; the `rejected_local` case gains its C04-iff members (kills the named forward wrong-reason red) · **C04** add the G20-style round-trip (submit `rejected_local` with `refusal_stage` → `QueryAttempt` → token verbatim on the row + NULL elsewhere) · **C05** behavioral mirror (refs the connector's validator refuses — absent/bad-grammar/duplicate per seam `:65` — fed to manifest admission, refusal asserted) · **C08** manifest fixture → assign construction → byte-equality of the three digests + a mutated-manifest control · **D04** predicate re-cut to the CONTRACT's observable: no peer package references the reduced table/tag AND no registered wire body carries a limits member (the A19-equality predicate deleted — it was the wrong row's predicate entirely).
**2.6 The two green-pin forward-fixes (master's ruling item 2):** **G04** hello fixture → the full closed `{version, commit, built_at}` body (valid under the CURRENT decoder and the amended registry — stays GREEN both sides of C01's fix). **G08** re-pinned to ITS OWN CT (the one-shot/replay MATRIX behavior); the raw uppercase constant-VALUE equalities on `DuplicateConsume`/`NO_REPLY` are DROPPED from G08 — wire-token values belong to A10's row (per m-10 §3: G08 must not demand bytes that contradict A10's registered lowercase). G08 stays GREEN now and after A10's WP2 fix; m-10's re-confirm verifies the surface split.
**2.7 PM-named riders on BINDING rows (in the returns, non-blocking, take-with-the-fold):** B09 one malformed≠stale input leg (m-9 §1) · A16 the Decode-side half of the iff (m-10 §1 residual) · C06 the epoch-divergence leg (m-10 §1). Binding rows are otherwise UNTOUCHED (master: settled, not re-reviewed).

## 3. The census contract (the ruling's §2, stated)
Expected post-fold census: **20 GREEN / 44 RED / 64 TOTAL — UNCHANGED.** Every strengthened gate row stays RED for the CONTRACT's reason at the current tree (spot-derivations: A08's added leg red at `types.go:73`; A02's no-genesis-at-admission red at the journal-open locus; B10's worker-path red at `runtime.go:197`); G04/G08/all pins stay GREEN. **The one named watch-row: D04** — its re-cut behavioral predicate's current-tree color must be DERIVED at the fold run; expected RED per the ledger's MISMATCH disposition; if it greens at first contact, that is a LEDGER question routed UP through master (anti-vacuity law), never a silent pass. Any OTHER color delta from 20/44 is likewise a per-row justified finding in the fold-close SITREP — never silent. The tagged census tool, sentinel, and commands are unchanged from r8.

## 4. Sequencing (the ruling's §4 path)
Your PLAN-REVIEW verdict TO me, parented to `s16a-build-9`, engine-submitted. On approve: I submit the successor IMPL relay re-binding the token to r9 (parent = your approving review; SCOPE_DIFF re-run, expected all-in — the fold adds no path outside `frank/test/seam/**`). Then: the fold commit(s) on `s16a-conformance` (plain `go test -p=1 -count=1 ./...` green at every commit; one logical fold, commit granularity yours) → the refreshed tagged script-derived census → the fold-close SITREP (census + any per-row delta justifications + the §2.7 rider dispositions) → I carry it UP; master routes the TWO bounded re-confirms (m-9 + m-10, their CHANGED rows only; m-8/m-7 owe nothing) → on master's joined carriage back, **WP2 opens**. The pair HOLDS WP2 bytes until that carriage. Spec-base defects found while strengthening stay escalations UP, never local fixes.

## 5. Out of scope (touch = escalation by construction)
Any production/source byte (incl. the `stream_lost` minting fix and every WP2 target); any contract/ledger/`master/**` byte; `go.mod`/`go.sum`; governed s8 machinery; WP3/WP4; any conforming-side edit; any new row (the 64-row bijection is invariant); merge (operator-only).

ACTIONS_GIT_REF: engine-lane governance act — this PLAN r9 drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; the four return hashes re-verified read-only at this seat; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: (literal at draft authoring, governing checkout; the implementation worktree is clean at 720bdd68:)
 M master/relays/CHECKPOINTS.md
 M master/relays2/INDEX.md
