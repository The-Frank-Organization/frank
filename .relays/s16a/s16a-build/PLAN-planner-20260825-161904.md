## PLAN r17 — WP3: THE NEVER-BUILT FOUR (BOUNDED SUCCESSOR to r16 `s16a-build/PLAN-planner-20260825-084308.md` @ `1954b7b72386951359e9e337b5914c06c6a422f7e57d7c5f6d2f3c81b2e95943`, superseding it; executes the must-revise `s16a-build/PLAN-REVIEW-implementer-20260825-085229.md` [C09 only; A12/Starter/C07 + all fences accepted] by BINDING the joined C09 ruling `s16a-wp3/RECONCILE-orchestrator-planner-20260825-095002.md` VERBATIM with its two pair-approved amendment instruments — m-7 A-M7-LAUNCH-READY r2 @ `0063650dfd55def3e08c47ff78831e332902e35fbe2d8379596d0bf2c89101be` + the m-10 broker-supervision registration @ `1d7505805506254459732d023f78a6e6cee23c4f7f60779bac56ee7185ba1d48`, both re-hashed at this seat — and folding the review's F1 [the coupled CT-C09 composition strengthening] and F2 [the named E2 server matrix]. ONE routing-premise error owned at this seat for the record: my escalation asserted r11 names no broker supervisor — r11 §2.1 `:92` pins m-10 as launcher/supervisor; the absence claim lacked its second search class; the genuinely uncontracted half was m-10's two-child supervision surface, now registered. The identity basis is also re-pinned CURRENT per the join carriage. Diffs from r16: the §1 pointers, the §1 C09 stack, §2.4, the NEW §2c matrix, §4.2; everything else r16-verbatim per the review's accepted list.)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-17
PARENT_DISPATCH_ID: s16a-build-plan-review-10
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — WP3 is the chartered next package on the closed WP2; the operator's next gate remains the terminal WP5 MERGE-GATE (the PR stays DRAFT)
IN_REPLY_TO: s16a-wp3/RECONCILE-orchestrator-planner-20260825-161514.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-9.planner, m-10.planner, m-8.planner
SUBJECT: WP3 plan r17 — C09 rebuilt on the joined ruling (m-10-supervisor launch, the 84-byte BROKER_READY -> SPAWNING->READY observable, the F2 server matrix over m-7's agenda); both amendment hashes cited; A12/Starter/C07 verbatim; census 62/2; m-7's bounded C09 review at the close carriage

## 1. Locked design (pointers; the pinned bytes govern)
- The plan of record **r9 @ `051c31f378895a1da4c923933a69112dd86fac04c3ac82f5ed51b763eb9faab9`** (the current identity successor per the join carriage; §2 WP3 the verbatim authority; §4's D03-gate note stands — the broker server INSIDE s16a does NOT trigger the D03 backlog gate) + the charter through CORRIGENDUM 5 @ `b5aee083ede4716bd1f2ab55a24b985463654dbc547250049a5ed40c2f3ec516` (fence; the frank-service-decomposition law). The re-certified lock set per `s16a-lock-recert3/…-101003`: RE-CERT 3 @ `6a768023…` · RLBS-3 @ `289a0da0…` · worker r12 @ `63f5c49d…` — all re-hashed at this seat, all matching.
- **C09's spec-of-record (m-7, the PM):** `A-M7-TRANSPORT` r11 @ `9331ea88…` (byte-frozen; §2.1 `:92` pins m-10 as the broker's launcher/supervisor; §2.2 its own supervised process) AS GOVERNED-ADDITIVELY-AMENDED by the study rev8 @ `64f9136e…` (the read order is law: where the study names an r11 clause THE STUDY GOVERNS; no crossing machinery, no `crossing_count`, `Describe` = telemetry-residual; build the study's shapes — `state_proposal`/`state_proposal_result` with malformed staged at the CORRELATION boundary · the ORDERED total disposition table with the BOOTSTRAP branch · the two-form assign gate on the canonical trusted tuple · NO durable broker state) AND by the TWO pair-approved C09 instruments, cited at hash and consumed never restated: **A-M7-LAUNCH-READY r2 @ `0063650d…`** (step-(3b): the READY record = the 84-byte `BROKER_READY` + 0x20 + `nonce=` + 64-lowercase-hex + 0x0A line on inherited stdout — the FIRST AND ONLY fresh-start lifecycle token; fail-closed first-line parsing; adoption emits NOTHING; outside the §2.11 event/ack family) and **the m-10 broker-supervision registration @ `1d750580…`** (the broker as THIRD supervised child: config-home-only launch · the §2.10 CI-1 spawn ritual, token + `control_generation` durably minted BEFORE spawn via ONE inherited pipe FD · adoption-FIRST with exact counters · NO parent-death kill · consumption of the 84-byte record fail-closed into **SPAWNING→READY, the ONE composition observable CT-C09 and production SHARE** · G-2 totality two→three, five broker failure classes each incrementing once · broker death APART from §A.1, no-cascade-by-default · the `m10-broker-spawn` census row). The joined ruling `s16a-wp3/RECONCILE-orchestrator-planner-20260825-095002.md` is BOUND VERBATIM — its five launch constraints and its READY exclusions (never exit-0, never store bytes, never socket-file existence, never deeper states) govern. Unsuperseded r11 core binds: CI-1 attach + the three-token `attach_result` set · §2.8/F64 per-operation fence with durable settlement for the three effectful F59 calls · F60/F66 custody. **m-7's agenda items 4–8/10/11 are the §2c matrix and the close-review scope.**
- A12/C07/Starter: the r9 rows + evidence cells (`runtime.go:219` the registry; `supervisor/spawn.go` + `cmd/frank-connector/main.go` the spawn/death-pipe pair; `cmd/frank-app/main.go:24` the nil) + the ratified 8-name constant for `relay.*` + `A-M10-SEAM`/`A-M10-CP` for the supervision surfaces.
- The battery at `ae58a0ac…` is the acceptance instrument: CT-A12 (the registry receives exactly the read, count==1) · CT-C07 (spawn the real connector to READY) · CT-C09 (the EXISTING `brokerclient.Establish` dial answered) — all PM-confirmed binding; their greens come from the BUILD, never from test edits.

## 2. Scope (the four pieces; everything else out)
1. **A12 (W):** the native relay tool wired — `relaytool.New` gains its production caller in the worker's composed registry; the provider peer's single embedding made live; `relay.*` dispatch reaches the relay registry from `executor.Prepare`'s invoker path.
2. **The composed Starter (app `cmd/`):** the `nil` at `cmd/frank-app/main.go:24` replaced by the REAL composition (the P5 predicate's subject); the app main process composes the m-10 module surfaces per `A-M10-CP`.
3. **C07 (CP spawner + CN death pipe):** `supervisor/spawn.go` gains its production caller — argv/fds/artifact staging per the seam — and the connector's death pipe wired (the CN half); spawn-to-READY under CT-C07. m-10's care note (their fidelity §1): pin READY OBSERVABLY (an output token or store state) rather than exit-0 alone — if the build provides the observable, the coupled CT-C07 STRENGTHENING is authorized (a strengthening per the PM's own note, never a weakening; disclosed in the close).
4. **C09 (the broker server):** a NEW separate binary under `frank/cmd/` (its own supervised process; name per the m-7 docs' vocabulary) + its server internals in a NEW non-conductor tree under `frank/internal/`, built to §1's C09 stack. **Launch (F1, per the ruling):** the m-10 supervisor spawns it through the PRODUCTION supervision boundary under the registration's exact semantics (spawn ritual, adoption-first, no-parent-death-kill); production and CT-C09 consume the SAME launcher and the SAME observable — the supervisor's SPAWNING→READY on the 84-byte record. **The coupled CT-C09 composition strengthening is AUTHORIZED:** the test launches the production binary through that boundary, waits for SPAWNING→READY, then runs the EXISTING real `brokerclient.Establish` predicate unchanged (`err == nil && session != nil`, the real socket, the real applier/control-generation advance). NO fake listener, NO in-client/in-process server, NO exit-0 readiness, NO predicate weakening. NO conductor byte, NO durable broker state.
- In-fence surfaces: `frank/cmd/**` · `frank/internal/**` · `frank/test/seam/**` (coupled strengthenings per §2.3's C07 authorization AND §2.4's C09 authorization) · `frank/.relays/s16a/**`. `go.mod`/`go.sum` stay arbitration-only — a dependency want = stop and escalate.

## 2c. The C09 server matrix (F2 — named E2 tests, tied to the named C09 preparatory commits; review prose is never the proof)
New production-server tests (in the server tree's own `_test.go` files) + the strengthened seam composition test cover EVERY m-7 agenda item, each with its fail-closed negative leg; m-7 guides the shapes and rules them at the chartered close review. **m-7's rider is LAW: the 84-byte READY token evidences NOTHING beyond step (3)** — suspension/fencing/disposition evidence comes from the contract's own channels, never from readiness.
- **(4) `Describe` telemetry-residual:** Describe serves its contract WITHOUT any crossing-record obligation (the residual form only; a crossing-machinery expectation ANYWHERE is itself the defect — the study-exclusion check: no transition-ledger/crossing/`completed-before-install`/recovery-matrix/`crossing_count` shape in the server or its tests).
- **(5) `state_proposal`/`state_proposal_result`:** the valid pair round-trips; a MALFORMED proposal is staged at the CORRELATION boundary — no result frame without a valid correlation (R7-F1); negative: malformed ⇒ no result, connection posture per the study.
- **(6) the ORDERED total disposition table:** every disposition branch exercised IN ORDER incl. the BOOTSTRAP branch as the broker-death convergence leg (R6-F1); negative: an out-of-table state fails closed.
- **(7) the two-form assign gate:** both forms accepted keyed by the CANONICAL trusted tuple (R3-F1); negative: a wrong-tuple assign rejected.
- **(8) NO durable broker state:** the broker writes NOTHING durable (no store bytes, no files beyond its socket in the runtime dir); `epoch_state` CONSUMED never written; negative: the observables prove absence, two structurally different checks.
- **(10) §2.8/F64 per-operation fence + settlement:** the fence exercised for ALL FOUR operations UNWEAKENED; durable settlement observed for the three effectful F59 relay calls through the app store applier; negative: an unfenced/duplicate operation fails closed.
- **(11) F60/F66 seat-credential custody:** the seat credential never leaves the broker — no credential bytes on any wire, reply, log, or exposed surface; negative custody legs per the m-1 boundary.
Each matrix row lands with (or before) a NAMED C09 preparatory commit; the FINAL C09 commit turns CT-C09 green with the composition strengthening. A matrix question the §1 stack does not answer routes UP to m-7 through master, never a local reading.

## 3. Laws (the standing set, WP3-shaped)
1. **Row-per-commit, build-grain:** A12 · the Starter · C07 = one named commit each (the Starter commit is named `Starter/P5`); C09 = a NAMED COMMIT SEQUENCE (a new binary legitimately takes preparatory commits) whose FINAL commit turns CT-C09 green — at every commit the moving census stays valid with reds exactly the then-planned remainder, the plain suite + vet green, no pin or fixed row regressing.
2. **The P4/P5 inversion evidence:** at WP3 close, the evidence-pack supplement commands (P4 zero-caller; P5 nil-Starter — `master/evidence/seam-check-2026-08-22/`) re-run with the OPPOSITE expectation and their output captured in the close SITREP; if a command's locus moved, say so and show the equivalent, never silently substitute.
3. **Escalations UP, never sideways:** every C09 question the §1 pins do not answer routes through master to m-7; a conforming-side or out-of-fence want stops; contract questions stop. The registrations in flight (A14 · B10 carrier) do not gate WP3.
4. Pushes ride the PR directive: push after each banked commit; refresh the PR description at WP3 close; the PR stays DRAFT; no merge (operator WP5).

## 4. Acceptance at WP3 close
1. CT-A12, CT-C07, CT-C09 GREEN for the contracts' reasons; census EXACTLY **62/2** with {D01, D02} the justified reds; no other color delta anywhere in the ladder.
2. The P4/P5 inversions captured per §3.2; the four pieces EXIST as the plan of record names them (the composed binary starts; the spawner reaches READY; the server answers the real dial through the production launch path; `relay.*` reaches the registry); the FULL §2c matrix GREEN with its negatives, each row tied to its named commit.
3. Plain suite + vet + sentinel + bijection green/loud/intact at every commit and at close, both seats' evidence; worktree clean; tip pushed; PR refreshed.
4. The WP3-close SITREP: census · the P4/P5 captures · the commit→piece map · the C09 build-vs-agenda checklist (§1's flags) · any coupled strengthening disclosed; then the pair HOLDS — my close carriage requests **m-7's bounded C09 review** (their stated agenda) through master; WP4 opens on my carriage unless master holds it on that review.

## 5. Out of scope (touch = escalation by construction)
D01/D02 (WP4) · any WP2-row byte (closed; regressions are defects) · contracts/ledger/`master/**` · `go.mod`/`go.sum` · governed s8 machinery · exit fixtures · conductor internals (`internal/conductor/**` or equivalent — the broker is NOT conductor growth) · merge/ready-for-review · E3/exit claims.

## 6. Sequencing after your review
Your PLAN-REVIEW verdict TO me, parented to `s16a-build-17`, engine-submitted. On approve: the successor IMPL relay carries the WP3 token (parent = your approving review; SCOPE_DIFF at that dispatch). Straight-through per the standing cadence; B-story: nothing here reopens WP2.

ACTIONS_GIT_REF: engine-lane governance act — this PLAN r17 drafted at .engine/drafts/s16a.planner/ and submitted through relay submit (r16 superseded, standing as history); the two amendment instruments + the five current identity pins re-hashed at this seat this act, all matching; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the pushed tip ae58a0ac per both seats' checks; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
