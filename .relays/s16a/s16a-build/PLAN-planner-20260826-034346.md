## PLAN r20 — WP5: THE CLOSE (the plan of record r9 §2 WP5 verbatim authority; opens on the ALL-GREEN battery — 64/0/64 at pushed tip `fc585cd75545d4ca7bbeec50e2ab715ac8da94ce`, corroborated both seats. WP5 is the close ladder, mostly gates and evidence, few bytes: the m-7 coda review return → the RESTACK → the rerun batteries → MY end-of-slice adversarial review → the r10 close-record evidence set for master's ledger act → the store export per master's ruling → the operator MERGE-GATE brief. The close bar is the charter's, EXACT: all 44 gate rows + 20 AGREE pins GREEN by their acceptance cases at both seats at the analyzed object; no carry substitute; the operator's grant — executor in TO, `HUMAN_MERGE_AUTHORIZATION` verbatim — EXECUTES as the PR #1 merge.)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-20
PARENT_DISPATCH_ID: s16a-wp34-close
RUN_ID: s16a
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — this plan requests nothing of the operator YET; the ladder ENDS at the operator's MERGE-GATE, reached only through every prior rung
IN_REPLY_TO: s16a-impl/SITREP-implementer-20260826-033857.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-9.planner, m-10.planner, m-8.planner
SUBJECT: WP5 plan — the close ladder: m-7 coda return → restack + rerun → the planner's end review → the r10 evidence set + registrations cited → store export → the MERGE-GATE brief; production-risk tier; no byte before its rung

## 1. The ladder (strictly ordered; each rung gates the next; deviations route UP)
1. **The m-7 coda review returns** (routed by master per my close carriage). Findings fold under the standing cadence BEFORE the restack (a fold after the restack re-reviews at the restacked bytes).
2. **The RESTACK (your act, one operation):** rebase `s16a-conformance` onto then-current `main`; resolve nothing silently — any conflict beyond mechanical replay is REPORTED with the conflicting surfaces before resolution; force-push the restacked branch (the PR follows); the PR stays DRAFT.
3. **The RERUN at the restacked bytes (your act):** the FULL evidence set — plain `go test -p=1 -count=1 ./...` + `go vet ./...` + the tagged census (EXPECTED 64/0/64; any delta is a finding, not a fix) + the sentinel + the bijection + gofmt/diff-check — at BOTH seats (yours in the worktree, mine at my own invocation on your restack report).
4. **MY end-of-slice adversarial review (the chartered ONE review; my act):** the changed-reviewed-bytes rule — commits whose bytes CHANGED in the restack get substantive re-review; the whole slice gets the adversarial pass (read-only; reviewer lenses as needed). Blockers/must-haves fold by you under REVIEW-FOLD discipline; then a bounded re-verify.
5. **The r10 close-record EVIDENCE SET (your act, assembled; master's ledger act consumes it):** the script-derived census at the reviewed object, row→STATUS table form (every non-excluded row GREEN; D03 the sole exclusion) · the commit→row/piece map across all WPs · the carried disclosure set (the SIX no-production-caller loci · the named coupled-test updates · the transient classifications) · the THREE registration instruments CITED at their hashes (A14 · B10-carrier · D01-clause — if any is not yet folded by its owner, the close record NAMES the gap and master rules whether it blocks) · the deferral rows named (R-S16A-FLOAT-DEFER · R-S16A-CTRL-HANDOVER-REC · D03's backlog carrier).
6. **The store export per F.7.2** — in the engine era master rules the form (my carriage asked); execute master's ruling verbatim.
7. **THE MERGE-GATE BRIEF (my act, TO the operator with the EXECUTOR in TO):** the close bar demonstrated · the census · the evidence pointers · the queue enumerated · the ask for `HUMAN_MERGE_AUTHORIZATION: approved — <the operator's words>`; the grant EXECUTES as the PR #1 merge (the executor per the grant's TO). NO merge claim before that relay; NO ready-for-review flip before the close set is filed (the PR directive's law).

## 2. Fences (unchanged, restated for the close)
No new feature byte; the only authorized edits are review-fold fixes (FOLD_SCOPE discipline) and mechanical restack resolutions (reported). The exit oracle + `master/exit-fixtures/**` UNTOUCHABLE; NO exit claim, NO E3, NO composition-on-the-live-courier claim, NO RELEASE-BINDING — all s16 (the charter's H-12 line stands: the bash claim stays invocation-context-narrowed, the sandbox forgone = the standing pre-external-use blocker, restated in the brief). `go.mod`/`go.sum` arbitration-only. The five WP2/WP3/WP4 packages are CLOSED — a regression found at any rung is a finding, never a quiet fix.

## 3. Acceptance (the slice is DONE when)
The operator's MERGE-GATE grant is on the trail with the executor in TO and the merge executed as granted; the r10 close record is accepted by master at the analyzed object with every non-excluded row GREEN; the store export is filed per the ruling; the PR is merged (and the branch state finalized per the operator's word). Anything less is not close.

## 4. Sequencing after your review
Your PLAN-REVIEW verdict TO me, parented to `s16a-build-20`, engine-submitted. On approve: rung 1 is already in flight via my carriage; the restack token (the successor IMPL relay, parent = your approving review) issues when rung 1 returns clean — the token authorizes rungs 2–3 + any fold edits from rungs 1/4 under FOLD_SCOPE; rungs 4–7 are review/evidence/gate acts in their lanes. Escalations UP as ever.

ACTIONS_GIT_REF: engine-lane governance act — this PLAN r20 drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the local/remote/PR-equal tip fc585cd7 per both seats' checks; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
