## PLAN r21 — WP5: THE CLOSE (BOUNDED SUCCESSOR to r20 `s16a-build/PLAN-planner-20260826-034346.md` @ `3625980402d6ee2d6cecdb007887abd22b335b4f16f02b3134a118b7e9bc0fd6`, superseding it; executes the must-revise `s16a-build/PLAN-REVIEW-implementer-20260826-035308.md` — ALL FIVE findings closed: F1 the r10 record moves POST-MERGE to the merged object per the plan of record `:31/:44/:60` with a pre-merge CLOSE CANDIDATE feeding the brief; F2 fold authority de-circularized — every finding gets its own addressed REVIEW-FOLD + pre-edit FOLD_SCOPE, no token pre-authorizes a later fold; F3 the three registration instruments become PREREQUISITES with affirmative discharge evidence for the original five residuals; F4 the restack pins verified SHAs with exact `--force-with-lease` and STOP-on-lease-failure, and ready-for-review is POSITIVELY authorized inside the MERGE-GATE grant; F5 the production-risk handoff fields are IN this plan. Master's sequencing confirmation `s16a-wp5/RECONCILE-orchestrator-planner-20260826-035119.md` is folded: A14 + the B10 carrier ALREADY DISCHARGED at the WP2-close registration round, only the D01 clause pends (routed, dispatch `s16a-wp5-gates`); F.7.2 SATISFIED BY CONSTRUCTION — the close record names the engine root + the closing workspace commit.)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-21
PARENT_DISPATCH_ID: s16a-build-plan-review-14
RUN_ID: s16a
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — this plan requests nothing of the operator yet; the ladder ends at the MERGE-GATE brief whose grant (merge + the ready-flip + branch finalization, executor in TO) is the operator's
IN_REPLY_TO: s16a-build/PLAN-REVIEW-implementer-20260826-035308.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-9.planner, m-10.planner, m-8.planner
SUBJECT: WP5 plan r21 — close-candidate pre-merge / r10 post-merge at the merged object; per-finding REVIEW-FOLD gates; the three registrations prerequisite (two discharged, D01-clause pending); leased restack with pinned SHAs; ready-flip inside the grant; production-risk fields present

## 1. The ladder (strictly ordered; each rung gates the next; deviations route UP)
1. **m-7's coda review returns** (routed by master). CLEAN → rung 2's token issues. FINDINGS → I file the addressed REVIEW-FOLD relay carrying them; you fold under pre-edit `FOLD_SCOPE` (all-in; an OUT row stops); rerun the gates; m-7 re-reviews the fold; THEN rung 2's token. No token pre-authorizes any fold.
2. **The RESTACK (under its own successor IMPL token, parent = this plan's approving review, issued only at rung-1-clean):** the token PINS the verified `main` SHA (currently `8659164ddfbfd36fbe7d98baa3ba4fdef05877f7` per master + the reviewer; re-verified at issuance) and the expected old remote head (`fc585cd75545d4ca7bbeec50e2ab715ac8da94ce`, or the post-fold tip). Rebase; every replay conflict beyond mechanical is REPORTED with its surfaces BEFORE resolution; push with exact `--force-with-lease=s16a-conformance:<the pinned old head>`; a lease failure is a HARD STOP reported up (a successor token re-pins). The PR follows the push and stays DRAFT.
3. **The RERUN at the restacked bytes (same token):** the FULL set at BOTH seats — plain `go test -p=1 -count=1 ./...` · `go vet ./...` · the tagged census (EXPECTED 64/0/64) · the sentinel · the bijection · gofmt/diff-check. Any delta is a FINDING routed to its own REVIEW-FOLD gate, never a quiet fix.
4. **MY end-of-slice adversarial review (the chartered ONE):** substantive re-review of every commit whose BYTES CHANGED in the restack + the whole-slice adversarial pass (read-only; reviewer lenses as needed). Blockers/must-haves → MY addressed REVIEW-FOLD relay → your fold (pre-edit `FOLD_SCOPE: all-in`, the artifact before the action) → my bounded re-verify. Optionals are your discretion per protocol.
5. **THE CLOSE CANDIDATE (pre-merge; assembled by you, corroborated by me):** at the reviewed restacked object — the script-derived census in row→STATUS form (every non-excluded row GREEN; D03 the sole exclusion) · the commit→row/piece map across all WPs · the carried disclosure set (the SIX no-production-caller loci · the named coupled-test updates · the transient classifications) · **PREREQUISITE registration instruments, all three CITED AT HASH:** A14 @ `67acb760…` (DISCHARGED, approve `s16a-wp2-close-reg/DESIGN-REVIEW-implementer-20260825-090741`) · the B10 carrier at worker r12 @ `63f5c49d…` §7.1a (DISCHARGED, approve `…-091915`) · the D01 float clause at ITS FOLDED HASH (pending `s16a-wp5-gates`; **the candidate CANNOT file until this instrument exists** — no naming-the-gap substitute) · **affirmative discharge evidence for the ORIGINAL FIVE R-S16A residuals:** R-S16A-M8BE-BASIS (the WP0(a) basis bind, plan §3) · R-S16A-M9-V2-FOLD (worker r11 pair-approved `061204`) · R-S16A-C01-BUILD-INFO + R-S16A-C02-EPOCH-QUERY + R-S16A-C04-REFUSAL-STAGE (the WP0(c) amendment `b0450777…` four-gates + the C01/C02/C04 rows GREEN in the census) · the registered DEFERRAL rows named AS CARRIES, never as discharge (R-S16A-FLOAT-DEFER · R-S16A-CTRL-HANDOVER-REC · the D03 backlog carrier) · **F.7.2 per master's ruling:** the candidate names `frank/.relays/s16a` as the export of record + the closing workspace commit hash.
6. **THE MERGE-GATE BRIEF (my act, TO the operator with the EXECUTOR in TO):** the close candidate · the queue enumerated · H-12 restated · and the EXPLICIT three-part ask: (a) `HUMAN_MERGE_AUTHORIZATION: approved — <the operator's words>`; (b) the READY-FOR-REVIEW flip POSITIVELY AUTHORIZED and ASSIGNED to the executor as the grant's first step (the flip happens only under the grant, immediately before the merge); (c) the branch-finalization word. The grant EXECUTES as: ready-flip → PR #1 merge, by the executor the grant's TO names. NO CI/CD, no flip, no merge before the grant.
7. **POST-MERGE r10 (the F1 fix; the plan of record's own bar):** after the executed merge — the executor's merge report pins the MERGE COMMIT + tree identity vs the candidate object; the census is RERUN AT THE MERGED OBJECT at both seats (64/0/64); I corroborate and carry; **master files the FINAL r10 at the merged object and DECLARES the close.** A pre-merge r10 claim is prohibited; if the merged tree differs from the candidate tree, that is a finding routed up before any close claim.

## 2. Fences
No new feature byte; the only authorized edits are REVIEW-FOLD fixes under their gates and mechanical restack resolutions (reported). The exit oracle + `master/exit-fixtures/**` untouchable; NO exit claim, NO E3, NO composition-on-the-live-courier claim, NO RELEASE-BINDING (all s16); H-12 stands. `go.mod`/`go.sum` arbitration-only. The closed packages regress only into findings, never quiet fixes.

## 3. Boundary contract (F5)
Writes: the restacked branch + PR #1 (you) · the close-candidate evidence set (you) · the REVIEW-FOLD artifacts (both, in lane) · the merge execution (the grant's executor) · the final r10 (master).
Reads: the reviewer (me) at rung 4 · master at rungs 5/7 · the operator at rung 6 · merged-`main` consumers thereafter.
Target entity: the merged object with the 64/0/64 census bound to it; the r10 close record.
Contract: the ladder's rung ordering; the grant's three-part scope; the census format.
Proof: E2 per rung at both seats + the post-merge identity/census bind.
No-consumer action: any rung whose consumer cannot act (a missing instrument, a failed lease, a census delta) STOPS the ladder and routes up.

## 4. Operator-judgment inventory (F5; all land AT the brief, none before)
The merge grant itself · the ready-flip authorization + executor assignment · branch finalization · any residual-risk acceptance the candidate discloses (the queue, the deferral rows, H-12). Nothing else in this plan asks the operator anything.

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — the close merges credential-adjacent governed code; no new authority byte in WP5 itself
- migration/backfill/destructive-write/canonical-data-repair: yes — the restack replaces remote branch history; leased force-push with pinned SHAs and hard-stop is the control
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the governed close and merge authority are trust-critical
- AI-or-automation-acts-downstream: no — no downstream automation is authorized by this plan
- worker/scheduler/queue/retry/async-side-effect: no — evidence and gates only
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the PR, the master ledger, the owner registrations, and the lane meet at the close
- user-visible-control-with-materializer/downstream-consumer: no
- test-runtime-role-mismatch: no — both-seat E2 and the merged-object identity bind are required
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the disclosed deferral rows and H-12 are residual risks, enumerated for the operator AT the brief
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested (the tier is production-risk, the maximum); the scan's residual rows land in the brief
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none — no downgrade or risk acceptance is requested by this plan

## 5. Sequencing after your review
Your PLAN-REVIEW verdict TO me, parented to `s16a-build-21`, engine-submitted. On approve: HOLD for rung 1 (m-7's return); at rung-1-clean I run SCOPE_DIFF and issue the restack token (rungs 2–3 ONLY); rungs 4–7 proceed under their own gates as §1 orders. Escalations UP as ever.

ACTIONS_GIT_REF: engine-lane governance act — this PLAN r21 drafted at .engine/drafts/s16a.planner/ and submitted through relay submit (r20 superseded, standing as history); no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the local/remote/PR-equal tip fc585cd7 per both seats' checks; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
