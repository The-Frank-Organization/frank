## PLAN r19 — THE WP3 CODA + WP4, IN PARALLEL (BOUNDED SUCCESSOR to r18 `s16a-build/PLAN-planner-20260825-213247.md` @ `33da00b57256b8fb89201896e38fc42b2129276796c82151599171d92e60b4b7`, superseding it; executes the must-revise `s16a-build/PLAN-REVIEW-implementer-20260825-220254.md` — F1/F2 resolved by the JOINED D1 RULING `s16a-wp34/RECONCILE-orchestrator-planner-20260826-021752.md` [m-7 rules, m-10's concur covers both branches], BOUND VERBATIM in the rewritten §2: the controller CONVERTS to the pinned fcntl family under FIVE constraints, the F2 recording obligation is NARROWED [verification half NOW; durable recording + `adopted` DEFER on the already-filed registry row R-S16A-CTRL-HANDOVER-REC, no durable-truth gap], D1 re-sized to ~3 commits with CONVERSION-BEFORE-PROBE pinned; F3 executed at this seat in §3's explicit CT-D01/CT-D02 predicate re-cuts. Everything the review accepted — D2, D3, the extraction, refuse-everywhere, the 64/0 close, every fence — stands r18-verbatim.)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-19
PARENT_DISPATCH_ID: s16a-build-plan-review-12
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — both work items ride master's joined carriage; the operator's next gate remains the terminal WP5 MERGE-GATE
IN_REPLY_TO: s16a-wp34/RECONCILE-orchestrator-planner-20260826-021752.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-7.implementer, m-10.planner, m-9.planner, m-8.planner
SUBJECT: plan r19 — the coda per the joined D1 ruling (fcntl conversion under five constraints -> broker verification half; recording deferred on R-S16A-CTRL-HANDOVER-REC; ~3 commits conversion-before-probe) ∥ WP4 with the F3 predicate re-cuts; close 64/0; one review, one token

## 1. Locked design (pointers; the pinned bytes govern)
- The joined WP34 carriage `s16a-wp34/RECONCILE-orchestrator-planner-20260825-213040.md` (the parallel ruling; rows R-S16A-FLOAT-DEFER + R-S16A-D01-CLAUSE-REG) AND the joined D1-coda ruling `s16a-wp34/RECONCILE-orchestrator-planner-20260826-021752.md` (m-7's `…d1/SITREP-planner-20260825-222836.md` + m-10's concur `…-222808.md`; row R-S16A-CTRL-HANDOVER-REC) — BOTH BOUND VERBATIM; on any wording delta the ruling relays govern.
- The float ruling: `master/relays2/s16a-wp34-gates/DESIGN-planner-20260825-212005.md` §1 (m-10, the IPC grammar owner; the definition of record until the WP4-close registration instrument).
- m-7's review: `…/SITREP-planner-20260825-212416.md` (the D1/D2 pins and sizes; the r11 §2.10 study-§(a) re-pins that D1 realizes).
- The identity basis stands current (plan r9 @ `051c31f3…`; charter through corr-5 @ `b5aee083…`); the WP3 close at `3566d37a…` is the launch state; the standing laws of r17 (moving census, plain suite + vet at every commit, coupled tests only where a fix's contract-conformant behavior requires them — never a weakening, pushes per the PR directive with the PR DRAFT, escalations UP) ride unchanged.

## 2. Part A — the WP3 CODA (the joined D1 ruling verbatim; ~3 named commits, ordering PINNED: conversion BEFORE probe — a probe landing first fails the honest controller)
- **Commit (1) — the controller CONVERSION** (`internal/appctl/brokerclient/session.go` lock helpers → the pinned `fcntl F_SETLK` family) under the ruling's FIVE constraints, all binding: (c1) `broker-control.lock` opened `O_CREAT|O_RDWR`, NEVER unlinked by either side; exclusive whole-file write lock; (c2) the close-releases hazard: exactly ONE descriptor to the lock file for the controller lifetime, no second in-process open/close — PROVEN by an FX-TB-16-style invariant test, never assumed; (c3) platform floor = Linux `F_GETLK`+`SO_PEERCRED` / Darwin `F_GETLK`+`LOCAL_PEERPID` (`l_pid` the holder query on both; no third platform); (c4) the adoption-path acquisition is blocking `F_SETLKW` under the context deadline OR a bounded retry loop — your choice, named in the close report; the instant-fail-only `LOCK_NB` posture is NOT acceptable there.
- **Commit (2) — the broker VERIFICATION half** (`internal/broker/server.go`): the peer-PID shim (`SO_PEERCRED`/`LOCAL_PEERPID`) + the `F_GETLK` probe in the ruled ORDER — (c5) probe AFTER accept, BEFORE any token/generation evaluation — + CONCURRENT accept with LIVE-SESSION REPLACEMENT (a handshake passing probe + token + strictly-greater generation replaces and closes the old session; the serial `Serve` posture cannot stand) + the FOUR typed outcomes `{adopted, rejected-lock, rejected-token, rejected-generation}` computed at the broker and surfaced IN-MEMORY (handshake reply/error + supervisor-visible) with a fail-closed negative for EACH + the positive end-to-end (an honest controller through the REAL converted client passes).
- **The F2 NARROWING is law:** durable `control_handover` recording DEFERS IN ITS ENTIRETY on registry row R-S16A-CTRL-HANDOVER-REC (already filed by master; the C01-split registration + applier-chokepoint widening + m-10's three constraints apply verbatim at discharge); `adopted` defers with it — NO durable-truth gap: the controller's generation-advance transaction already commits `{control_token, control_generation, minted_at}` durably before connecting. NO m-10 byte moves at the coda. A log/in-memory-slice/test-hook is NOT claimed as recording — the in-memory outcomes are surfaced as outcomes, never labeled recorded.
- **D2** (review-accepted): the fresh-spawn mint (`cmd/frank-app/main.go:233`/`:294-300`) to 32 OS-CSPRNG bytes as 64 lowercase hex — rides the coda wherever you sequence it.
- **Gates:** CT-C09 stays GREEN at EVERY coda commit; plain suite + tagged census green as ever; NO census color delta expected — any deviation STATED. On your coda-close report I carry it up; master routes m-7's bounded review of the converted-lock + probe + replacement work, as chartered.
- **D3 — a DISCLOSURE line, no code:** `broker.NewCore`/`Core.Invoke` joins the carried no-production-caller set as the SIXTH locus in the WP5 close record's disclosures.

## 3. Part B — WP4 (the shared-primitive extraction; the last two rows)
- **D01 — ONE canonicalizer:** a NEW shared package (in-fence under `frank/internal/`) REPLACES the three JCS implementations (`appipc/jcs.go` · `connector/jcs/jcs.go` · `worker/jcs/jcs.go`) — all three modules consume the one implementation; the replaced files' duplicated logic is REMOVED, not shadowed. **The float clause VERBATIM (the ruling §1):** a JSON number is ADMITTED IFF it matches the strict integer token grammar `-?(0|[1-9][0-9]*)` with `-0` additionally REFUSED, emitted BYTE-VERBATIM (never through any binary float type); EVERYTHING ELSE is a TYPED FAIL-CLOSED refusal, IDENTICALLY at the encode and digest paths — the current one-way asymmetry (and the connector's silent >2^53 rewrite the owner verified live) dies with the extraction. CT-D01 turns GREEN in the extraction's completing commit.
- **D02 — ONE shared frame-codec vector suite** pinning byte-equality across the three codecs, EXHAUSTIVE over the suite (the sampled three-exponent agreement superseded); CT-D02 GREEN in its commit. D03 stays untouched backlog.
- **The F3 seam-predicate re-cuts (authorized HERE, named separately from module-test updates; the anti-vacuity law preserved):** (a) **CT-D01** is REPLACED — the committed accept-`1.5` predicate contradicts the float ruling; the re-cut proves byte-verbatim arbitrary-precision integer carriage (including values above 2^53) AND the typed fail-closed refusal for `-0`, fraction, exponent, and Go float values, at EVERY applicable encode AND digest entry of the one extracted canonicalizer as consumed by all three modules. (b) **CT-D02** is REPLACED — the committed source-grep executes nothing; the re-cut EXECUTES the exhaustive shared vector corpus against all three codecs and requires byte-equality for every vector. Merely greening the present predicates is NOT acceptance; both re-cuts are RED until the extraction lands, green for the contracts' reasons after.
- Coupled module-test updates ONLY where refuse-everywhere changes contract-conformant behavior the old tests wrongly pinned (each named in the close report); the WP2/WP3 rows must not regress.
- **Close bar: the census reads `64 GREEN / 0 RED / 64` — every deviation impossible to state silently** (an all-green battery is the WP5 precondition).

## 4. Acceptance
1. The coda commit set lands per §2 with no census delta; the WP4 commits land per §3 with D01/D02 green; at the final tip the census is EXACTLY 64/0/64, the plain suite + vet green, the sentinel loud, the bijection intact, worktree clean, tip pushed, PR refreshed and DRAFT.
2. The close report carries: the census · the commit→item map · the named coupled-test updates with their contract reasons · the D3 disclosure line (six loci) · any deviation stated. Then the pair HOLDS: my carriage requests m-7's coda re-confirm + reports WP4 close; **WP5's plan follows separately from this seat** (full battery both seats · the end-of-slice adversarial review · the r10 close record · the store export · the restack · the operator MERGE-GATE brief).

## 5. Out of scope (touch = escalation by construction)
D03 · `fieldspec.CanonicalMarshal` (the backlog's third implementation — NOT this extraction's surface) · contracts/ledger/`master/**` · `go.mod`/`go.sum` · conductor internals beyond the named broker handshake path · any WP2/WP3-row behavior change beyond the named coda pins · merge/ready-for-review · E3/exit claims.

## 6. Sequencing after your review
Your PLAN-REVIEW verdict TO me, parented to `s16a-build-19`, engine-submitted. On approve: the successor IMPL relay carries the token (parent = your approving review; SCOPE_DIFF at that dispatch). Parallel execution per master's ruling; commit order yours; escalations up as ever.

ACTIONS_GIT_REF: engine-lane governance act — this PLAN r19 drafted at .engine/drafts/s16a.planner/ and submitted through relay submit (r18 superseded, standing as history); the joined D1 ruling read at bytes this act; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the local/remote-equal tip 3566d37a per both seats' checks; governing checkout carries the operator-owned relay/CHECKPOINTS/RESIDUALS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
