## PLAN r16 — WP3: THE NEVER-BUILT FOUR (BUILD, not conformance; the plan of record r7 §2 WP3 verbatim authority; opens from the WP2 close at pushed tip `ae58a0ac…`, census 59/5 corroborated both seats). WP3 owns A12 and ALL of C07 and C09 exclusively: (i) the worker's peers WIRED (A12) · (ii) the composed Starter (the `nil` at `cmd/frank-app/main.go:24` replaced by the real composition) · (iii) the connector spawner C07 to READY · (iv) the broker SERVER C09 — m-7's surface, a SEPARATE supervised process/binary, NEVER conductor growth. Acceptance = CT-A12/CT-C07/CT-C09 GREEN + the P4/P5 zero-caller/nil predicates INVERTED; expected WP3-close census **62 GREEN / 2 RED** ({D01, D02} the sole reds, WP4's).

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-16
PARENT_DISPATCH_ID: s16a-wp2-close
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — WP3 is the chartered next package on the closed WP2; the operator's next gate remains the terminal WP5 MERGE-GATE (the PR stays DRAFT)
IN_REPLY_TO: s16a-wp2/SITREP-planner-20260825-084146.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-9.planner, m-10.planner, m-8.planner
SUBJECT: WP3 plan — A12 wiring · the composed Starter · C07 spawner-to-READY · the C09 broker server (own binary, study-governs-r11, m-7 guides); P4/P5 inversions at close; census 62/2; m-7's bounded C09 review requested at the close carriage

## 1. Locked design (pointers; the pinned bytes govern)
- The plan of record r7 @ `5fd00b98…` §2 WP3 + §4 D03-gate note (the broker server INSIDE s16a does NOT trigger the D03 backlog gate) + the charter as thrice-corrected (fence; the frank-service-decomposition law).
- **C09's spec-of-record (m-7, the PM):** `A-M7-TRANSPORT` r11 @ `9331ea88…` (byte-frozen; §2.2 pins the broker as its OWN supervised process) AS GOVERNED-ADDITIVELY-AMENDED by the study rev8 @ `64f9136e…` — THE READ ORDER IS LAW (m-7's `211344` + `135947`): where the study names an r11 clause THE STUDY GOVERNS; its supersessions CUT machinery a reader of r11 alone would wrongly build (NO transition ledger/crossing rows/`completed-before-install`/five-row recovery matrix — the clean-cut epoch boundary, NO-H-24; no `crossing_count` in `epoch_installed`; `Describe` = telemetry-residual). BUILD the study's shapes: the `state_proposal`/`state_proposal_result` wire pair with malformed staged at the CORRELATION boundary (R7-F1) · the ORDERED total disposition table with the BOOTSTRAP branch as the broker-death convergence leg (R6-F1) · the two-form assign gate keyed by the canonical trusted tuple (R3-F1) · **the broker keeps NO durable state** (m-10's store is the writer; the broker consumes `epoch_state`). Unsuperseded r11 core binds: CI-1 attach + the three-token `attach_result` producer set · the §2.8/F64 per-operation fence UNWEAKENED for all four operations with durable settlement for the three effectful F59 relay calls · F60/F66 seat-credential custody. **m-7's C09 review agenda (their §4 flags 4–8, 10, 11) is the build's checklist and the close-review scope.**
- A12/C07/Starter: the r9 rows + evidence cells (`runtime.go:219` the registry; `supervisor/spawn.go` + `cmd/frank-connector/main.go` the spawn/death-pipe pair; `cmd/frank-app/main.go:24` the nil) + the ratified 8-name constant for `relay.*` + `A-M10-SEAM`/`A-M10-CP` for the supervision surfaces.
- The battery at `ae58a0ac…` is the acceptance instrument: CT-A12 (the registry receives exactly the read, count==1) · CT-C07 (spawn the real connector to READY) · CT-C09 (the EXISTING `brokerclient.Establish` dial answered) — all PM-confirmed binding; their greens come from the BUILD, never from test edits.

## 2. Scope (the four pieces; everything else out)
1. **A12 (W):** the native relay tool wired — `relaytool.New` gains its production caller in the worker's composed registry; the provider peer's single embedding made live; `relay.*` dispatch reaches the relay registry from `executor.Prepare`'s invoker path.
2. **The composed Starter (app `cmd/`):** the `nil` at `cmd/frank-app/main.go:24` replaced by the REAL composition (the P5 predicate's subject); the app main process composes the m-10 module surfaces per `A-M10-CP`.
3. **C07 (CP spawner + CN death pipe):** `supervisor/spawn.go` gains its production caller — argv/fds/artifact staging per the seam — and the connector's death pipe wired (the CN half); spawn-to-READY under CT-C07. m-10's care note (their fidelity §1): pin READY OBSERVABLY (an output token or store state) rather than exit-0 alone — if the build provides the observable, the coupled CT-C07 STRENGTHENING is authorized (a strengthening per the PM's own note, never a weakening; disclosed in the close).
4. **C09 (the broker server):** a NEW separate binary under `frank/cmd/` (its own supervised process; name per the m-7 docs' vocabulary) + its server internals in a NEW non-conductor tree under `frank/internal/` — answering `brokerclient`'s CI-1 handshake at `broker-control.sock`, built to §1's spec stack. `appipc/msgs_broker.go` carries the wire vocabulary (consume, don't fork). NO conductor byte, NO durable broker state.
- In-fence surfaces: `frank/cmd/**` · `frank/internal/**` · `frank/test/seam/**` (coupled strengthenings ONLY per §2.3's authorization) · `frank/.relays/s16a/**`. `go.mod`/`go.sum` stay arbitration-only — a dependency want = stop and escalate.

## 3. Laws (the standing set, WP3-shaped)
1. **Row-per-commit, build-grain:** A12 · the Starter · C07 = one named commit each (the Starter commit is named `Starter/P5`); C09 = a NAMED COMMIT SEQUENCE (a new binary legitimately takes preparatory commits) whose FINAL commit turns CT-C09 green — at every commit the moving census stays valid with reds exactly the then-planned remainder, the plain suite + vet green, no pin or fixed row regressing.
2. **The P4/P5 inversion evidence:** at WP3 close, the evidence-pack supplement commands (P4 zero-caller; P5 nil-Starter — `master/evidence/seam-check-2026-08-22/`) re-run with the OPPOSITE expectation and their output captured in the close SITREP; if a command's locus moved, say so and show the equivalent, never silently substitute.
3. **Escalations UP, never sideways:** every C09 question the §1 pins do not answer routes through master to m-7; a conforming-side or out-of-fence want stops; contract questions stop. The registrations in flight (A14 · B10 carrier) do not gate WP3.
4. Pushes ride the PR directive: push after each banked commit; refresh the PR description at WP3 close; the PR stays DRAFT; no merge (operator WP5).

## 4. Acceptance at WP3 close
1. CT-A12, CT-C07, CT-C09 GREEN for the contracts' reasons; census EXACTLY **62/2** with {D01, D02} the justified reds; no other color delta anywhere in the ladder.
2. The P4/P5 inversions captured per §3.2; the four pieces EXIST as the plan of record names them (the composed binary starts; the spawner reaches READY; the server answers the real dial; `relay.*` reaches the registry).
3. Plain suite + vet + sentinel + bijection green/loud/intact at every commit and at close, both seats' evidence; worktree clean; tip pushed; PR refreshed.
4. The WP3-close SITREP: census · the P4/P5 captures · the commit→piece map · the C09 build-vs-agenda checklist (§1's flags) · any coupled strengthening disclosed; then the pair HOLDS — my close carriage requests **m-7's bounded C09 review** (their stated agenda) through master; WP4 opens on my carriage unless master holds it on that review.

## 5. Out of scope (touch = escalation by construction)
D01/D02 (WP4) · any WP2-row byte (closed; regressions are defects) · contracts/ledger/`master/**` · `go.mod`/`go.sum` · governed s8 machinery · exit fixtures · conductor internals (`internal/conductor/**` or equivalent — the broker is NOT conductor growth) · merge/ready-for-review · E3/exit claims.

## 6. Sequencing after your review
Your PLAN-REVIEW verdict TO me, parented to `s16a-build-16`, engine-submitted. On approve: the successor IMPL relay carries the WP3 token (parent = your approving review; SCOPE_DIFF at that dispatch). Straight-through per the standing cadence; B-story: nothing here reopens WP2.

ACTIONS_GIT_REF: engine-lane governance act — this PLAN r16 drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: implementation worktree clean at the pushed tip ae58a0ac per both seats' checks; governing checkout carries the operator-owned relay/CHECKPOINTS dirt plus daemon rendering for this filing — the engine ledger is the admission authority
