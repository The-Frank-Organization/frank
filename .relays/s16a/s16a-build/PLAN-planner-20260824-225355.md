## PLAN r10 — THE WP1 CODA (two master-ruled fixes, one commit; bounded to `frank/test/seam/**`; executes `s16a-wp1/RECONCILE-orchestrator-planner-20260824-224707.md` VERBATIM — the joined re-confirms found 30/32 changed rows BIND at `4d0ff554…`, the two residuals are RULED with the owning PMs' own words adopted, recorded in ledger ADDENDUM A-2 @ `899157bd9de7ea166bc968ba1edd1bb0b9855207996f6b0a106baedc404fcd29` (hash re-verified at this seat this act); NO further re-confirm round is owed on the coda; **WP2 is PRE-AUTHORIZED to open on the coda census confirmation** — exactly 21/43/64 — with no master round-trip.)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-10
PARENT_DISPATCH_ID: s16a-wp1-coda
RUN_ID: s16a
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — both coda fixes are the owning PMs' words, master-adopted; the operator's next gate remains the terminal WP5 MERGE-GATE
IN_REPLY_TO: s16a-wp1/RECONCILE-orchestrator-planner-20260824-224707.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-8.planner, m-7.planner
SUBJECT: WP1 coda plan — the D04 skip-set boundary fix (expected red→GREEN) + the A09 scripted mismatched-descriptor grant (stays red for the contract's reason); one commit; census expectation EXACTLY 21/43/64 at both seats; WP2 opens on the coda-close confirmation

## 1. The two fixes (the ruling's §Ruling-1/§Ruling-2 text governs on any wording delta)
1. **CT-D04 (in `shared_test.go`, the D04 predicate only):** the peer-scan confinement boundary is m-10-INTERNAL — extend the committed skip set `{.git, internal/appipc, test/seam}` to m-10's OWN trees (`internal/appctl` + the app `cmd/` tree); the peer scope = the non-m-10 modules (worker/connector) + the registered-wire-body limits census. Master verified: zero `frank_test_reduced_limits` references under `internal/worker/**`/`internal/connector/**`; the corrected predicate GREENS today — the row's obligation was met by construction when A19's one-bound ruling landed, and the corrected scan + wire census stand as D04's PERMANENT regression predicate. The reduced-build divergence concern rides CT-A19's still-red row alone.
2. **CT-A09 (in A09's OWN test function; NO shared-helper byte):** script A09's own `probe.control.authorizeReply` — granted, with a MISMATCHED (and/or absent) effect descriptor — replacing the unmodified default probe whose granted descriptor-less reply ten other rows require to succeed (the mutually-unsatisfiable shape master verified at `app_worker_test.go:137-143`). Observables stay: descriptor-field-presence + `writes==0` + a typed reason. A09 remains a gate RED, now red for the CONTRACT's reason; its green is a WP2 worker-side act.

## 2. Acceptance (the coda is done when exactly this holds)
1. ONE commit on `s16a-conformance` atop `4d0ff554…`; committed diff touches ONLY the A09 test function + the D04 skip-set lines (both in `frank/test/seam/**`); worktree clean after.
2. The tagged census re-derived at YOUR seat AND (on your coda-close SITREP) corroborated at mine: **EXACTLY `21 GREEN / 43 RED / 64 TOTAL`** — the SOLE color delta is D04 red→green; A09's red names the descriptor reason. Any other delta = deviation, route UP before any further byte.
3. Plain `go test -p=1 -count=1 ./...` green at the commit; the untagged sentinel still fails loudly; `go vet -tags seam ./test/seam/` clean; the 64-row bijection and D03-absence unchanged.
4. Your coda-close SITREP files the census + the diff shape; per master's pre-authorization, **WP2 OPENS on that confirmation** — no master round-trip. (WP2 BYTES still wait on the WP2 plan + token: my next act on your coda-close is the WP2 PLAN over the non-conforming-side fixes; the A-2 carries — the A14 derivation-recipe registration at WP2-close, m-9's five rigidity notes, m-10's care notes — ride into it.)

## 3. Out of scope (touch = escalation by construction)
Any production/source byte (every WP2 fix incl. A09's worker-side fail-closed, the `stream_lost` minting, the A02 locus); any other test row or shared helper; contracts/ledger/`master/**`; `go.mod`/`go.sum`; governed s8 machinery; push/PR/merge. r8's laws + the tagged commands + conditions (a)–(c) ride unchanged.

## 4. Sequencing
Your PLAN-REVIEW verdict TO me, parented to `s16a-build-10`, engine-submitted. On approve: the successor IMPL relay re-binds the token (parent = your approving review; SCOPE_DIFF expected all-in), you land the coda commit + census + coda-close SITREP, and WP2 opens per §2.4. Deviations route UP; the ruling's census gate is exact.

ACTIONS_GIT_REF: engine-lane governance act — this PLAN r10 drafted at .engine/drafts/s16a.planner/ and submitted through relay submit; A-2's hash re-verified read-only; no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: (literal at draft authoring, governing checkout; the implementation worktree is clean at 4d0ff554:)
 M master/relays/CHECKPOINTS.md
 M master/relays2/INDEX.md
?? master/STEP-3-T4-SEAM-CONSISTENCY-2026-08-22-r9-ADDENDUM-A2.md
?? master/relays2/s16a-wp1-reconfirm/SITREP-planner-20260824-224219.md
?? master/relays2/s16a-wp1-reconfirm/SITREP-planner-20260824-224250.md
