## PLAN r15 — WP2 (BOUNDED SUCCESSOR to r14 `s16a-build/PLAN-planner-20260825-040211.md` @ `b44496efa3b6cde6cbc76defb75ddbf98d9a50ca704f239850ce77c7d8b720cf`, superseding it; BINDS the joined B10 ruling `s16a-wp2/RECONCILE-orchestrator-planner-20260825-064318.md` — m-9 rules, m-8 concurs, the hold LIFTED on exact pins; until m-9's WP2-close design registration, the ruling relay IS the carrier's definition of record per the effective-time rule. THE SOLE CHANGES: §2's B10 entry re-bound to the ruled spec, the NEW §2b restating the three pins, and §5 gaining the R-S16A-B10-CARRIER-REG registration at the WP2-close gate. The 44-half/38-row matrix, the per-commit gate, 59/5, and every other r14 surface stand verbatim; the 20 banked row commits through `89177405…` [23 rows green, census 44/20 valid] are unaffected; the remaining rows proceed on the standing order with B10 LAST.)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16a-build-15
PARENT_DISPATCH_ID: s16a-wp2-b10-ruling
RUN_ID: s16a
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — WP2 is pre-authorized on the met census gate; the operator's next substantive gate remains the terminal WP5 MERGE-GATE
IN_REPLY_TO: s16a-wp2/RECONCILE-orchestrator-planner-20260825-064318.md
FROM: s16a.planner
TO: s16a.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-10.planner, m-8.planner, m-7.planner
SUBJECT: WP2 plan r15 — the B10 ruling bound (closed two-branch carrier + canonical-decode round-trip + two-leg cut; B10 lands last; carrier registration at WP2-close); all other r14 bytes verbatim; banked progress unaffected

## 1. Locked design (pointers; the pinned bytes govern)
- The plan of record r7 @ `5fd00b98…` §2 WP2 (the W/CP/CN lists, verbatim authority) + the charter @ `41c43b27…` (fence, close bar, non-conforming-side law).
- The ledger: r9 @ `acfd358a…` + A-1-with-E-1 @ `4d9b0cb0…` (G20) + **A-2 @ `899157bd…`** (D04 met-by-construction; the A09 re-cut; the 21/43 census supersession; the §4 carries).
- The battery AT `f7040666…` IS the acceptance instrument: every strengthened predicate is PM-confirmed binding (the fidelity + re-confirm rounds); a fix is done when ITS row's CT greens for the contract's reason.
- The contract bases: the charter's BASIS_HASHES as amended (WP0(c) `b0450777…` for C01/C02/C04); the PM guidance of r8 §4 rides (A-M8-BE for B-rows; the study-governs-r11 order for anything broker-adjacent; LIVE unstruck lines).

## 2. Scope — the 38 target rows, by owner side (fix bytes move ONLY on the named side)
- **W (worker, `frank/internal/worker/**`; 26 owner halves):** A01 A02 A03 A04 A05 A06 A07 A09 A10 A11 A13(carriage; consumes CP's `void_reason` totally) A14(W half — move to the manifest encoding) A16(W half) A18 A19 (drop the 64; NO local bound) · B01 B02 (the §2.2 uniform v2 parsers) B03 B04 (incl. the credited no-carrier fix: no `stream_lost` minting on a fresh-dial loss) B05 B06 B07 B08(W half: total behavioral consumption of the two CN-owned enums, unknown-token fail-closed) B09 B10 (the RULED two-branch carrier + decode-round-trip predicate — §2b; lands LAST) B11 (the §2.4 bounds-set incl. honest `truncated`). The credited A02 locus fix (admission-locus rejection, no broker attach on an invalid nonce) and A09's effect-descriptor verification (fail-closed on mismatched/absent) are IN these rows.
- **CP (control plane, `frank/internal/appctl/**` + `frank/internal/appipc/**` + app `cmd/`; 15 owner halves):** A08 (remove the `GenerationID` members from consume AND outcome; bind from channel identity) A15 A16(CP half of the encode/decode `re`-on-non-reply iff) A17 A13(define the closed `void_reason` enum) A14(CP: the manifest encoding is canonical — confirm/hold) · C01 (decoder → the closed struct) C02 (register `epoch_query` + the resolution behavior + unregistered-fails-closed) C03 (decode→row translation; unknown token fails closed; the `rejected_local` iff members) C04 (schema column + envelope carriage + settle persistence) C05 (mirror validation) C06 · C08(CP half) C10 (fixture → base 1) D05 (`WriteFrame` rejects empty).
- **CN (connector, `frank/internal/connector/**`; 3 owner halves):** B08 (export the two closed enums) · C01 (emitter → the struct) · C08(CN half).
- **The complete matrix: 44 owner halves over 38 distinct rows (W 26 · CP 15 · CN 3); the SIX cross-side rows {A13, A14, A16, B08, C01, C08} require BOTH halves to move per the ledger.**

## 2b. The B10 pins (the ruling §1–§3 restated exactly; on any wording delta the ruling relay governs)
1. **The carrier (CLOSED, two branches):** the journal item record carries EXACTLY ONE of — (a) **verbatim**: the item's raw bytes as-is, permitted IFF `bytes == JCS(decode(bytes))`; (b) **`raw_b64`**: RFC 4648 §4 STANDARD base64, padded, no line-wrap, for everything else (non-UTF-8, non-JSON, AND valid-but-non-canonical JSON). No third encoding; both members, neither member, or any other encoding = a malformed record, fail-closed at the checksum-reader.
2. **The predicate (LOSSLESS, at the persisted record):** live `Runner.Run` → read persisted `session.log` → parse under the CANONICAL decode (recovery's own) → extract the carrier member → decode → `bytes.Equal(original)`. NEVER `bytes.Contains` on the file's raw bytes.
3. **The two anti-vacuity legs:** (i) the needle `{0xff,0xfe,0x00,'x'}` ⇒ `raw_b64` AND NOT verbatim ⇒ decode == the needle; (ii) an exact-JCS item ⇒ verbatim AND NOT `raw_b64` ⇒ bytes == original. Both red today; neither leg satisfiable by weakening the other.
4. **Execution:** the `TestCT_B10` re-cut to this predicate is the ONE authorized battery-predicate replacement (ruled, not a weakening — the two-leg cut is stronger); it lands WITH the W carrier fix such that CT-B10 is GREEN in its fix commit with both legs (a separate preceding test-fold commit leaving B10 red-for-the-ruled-reason is permitted under the moving-census law; mechanics yours). B10 lands LAST. The raw-bytes-in-`session.log` alternative is DECLINED by the owner — do not implement it in any form. Any question the pins do not answer routes UP, not sideways.

- **NOT WP2:** A12 + ALL of C07 + C09 (WP3, exclusively) · D01/D02 (WP4) · D04 (done by construction; its regression predicate stands). **Expected WP2-close census: 59 GREEN / 5 RED** — the five reds exactly {A12, C07, C09, D01, D02}, each justified by its WP assignment; any OTHER red at close = not-done; any UNEXPECTED green along the way (a row greening without its fix commit) = an anti-vacuity finding routed UP.

## 3. Laws (unchanged + WP2-specific)
1. **Row-per-commit CT-green + the executable per-commit gate:** one row-or-cluster per commit, named by row ID; the row's CT goes green IN its fix commit. At EVERY commit: (a) the named row/cluster GREEN for the contract's reason; (b) a VALID tagged 64-row census (bijection + sentinel) whose REDs are EXACTLY the then-planned remaining set — no regression of the 21 launch greens or any earlier-fixed row, no unexpected color delta in either direction; (c) plain `go test -p=1 -count=1 ./...` + `go vet` green. The tagged battery is NOT required green before WP5 — it is the moving instrument; 59/5 is its required state at WP2 close.
2. **Non-conforming-side-only:** a conforming side's bytes move ONLY on its PM's word through master (fence-row-vs-owner-path). Convergent edits to a file two rows share: identical bytes or STOP for arbitration. `go.mod`/`go.sum` arbitration-only. A fix wanting bytes outside the charter fence = escalation by construction.
3. **Tests move only as coupled:** a battery byte changes ONLY where a fix's contract-conformant behavior requires the coupled literal (the self-announcing compile couplings: B04/B08/B09's `TurnEpoch: 1` literals move WITH A05's string flip). NEVER a predicate weakening — a test that seems WRONG against the contract is an escalation UP (the G20/WP1-F1 precedents), not a local edit.
4. **census.py modernization (authorized HERE, per A-2 §3):** ONE bounded edit replacing the superseded hardcoded `want GREEN=20 RED=44` launch literal with: the 64-row bijection + sentinel enforcement UNCHANGED (the vacuity guards stay), the partition REPORTED (not asserted), exit 0 on a valid derivation. WP-close evidence STATES its partition and justifies every remaining red — the moving WP2 census makes any fixed color literal wrong tomorrow. (This is the implementer-disclosed carried disposition from the coda close.)
5. **Contract questions route UP** through master to the owning PM — never a silent local reading. H-12 stands; no E3/exit claim; no push/PR/merge (operator-only at WP5).

## 4. Suggested order (mechanics yours WITHIN the law: every commit satisfies §3.1, so each cross-side row is ONE complete both-halves commit, placed once)
W-only wave: A05 FIRST (the string flip + coupled test literals; unblocks the CTRL-W body work) → A10 → A02/A03/A04 → A01/A06/A07/A09/A11 → A18 → A19.
Cross-side row commits (each ONE commit, all its halves, its CT green in that commit): **A13(W+CP)** (the carriage + the closed `void_reason` enum + total consumption + the member-loop) · **A14(W+CP)** (the manifest-canonical encoding both sides + recomputability) · **A16(W+CP)** (the encode/decode iff both registries).
W-only provider wave: B01/B02 → B03/B04/B05/B06/B07 → B09/B10/B11.
Cross-side: **B08(W+CN)** (the CN enum export + the W total behavioral consumption, one commit).
CP-only wave: A08/A15/A17 → C02/C03/C04 → C05/C06/C10/D05.
Cross-side: **C01(CP+CN)** (decoder + emitter to the closed struct, one commit) · **C08(CP+CN)** (the verbatim-copy law both ends, one commit).
A genuinely-required multi-row cluster is permitted only as ONE named cluster commit with EVERY included CT green in it. The convergent-edit law is watched at every shared file; no committed state may leave a named row's first half landed with the row RED.

## 5. The A-2 §4 carries (registered there, discharged here)
- **B10 carrier registration (WP2-close gate item, registry row R-S16A-B10-CARRIER-REG):** m-9's one-line design registration folds the ruled two-branch carrier into the worker design's journal surface — requested in my WP2-close carriage alongside the A14 recipe; until it folds, the ruling relay is the definition of record.
- **A14 derivation-recipe registration (WP2-close gate item):** the `manifest-tool:` + SHA-256-over-JCS recipe is currently test-DEFINED; at WP2-close I request m-10/master register it (conformance record or one-line seam addendum) so the test VERIFIES — the request rides my WP2-close carriage, the registration is m-10/master's act.
- **m-9's five rigidity notes** (re-confirm §3) + **m-10's notes** ride as code comments/care at the named loci (the B04/B08/B09 epoch literals; the B07/B11 JSON-fragment sensitivity; B11's tunable-constant pins; A13's member-loop completion opportunity — take the member-loop as part of A13's fix).
- **m-8's credited catches** land in their rows (B04 no-carrier; A02 locus), per §2.

## 6. Boundary contract
Writes: non-conforming-side production bytes per §2 + the coupled test literals + the census.py modernization, all inside the charter fence, on `s16a-conformance`.
Reads: the pinned contracts (as amended); the battery's binding predicates (the acceptance oracle).
Target entity: the 38 rows' CTs green for the contract's reasons; the WP2-close census 59/5.
Downstream consumer: WP3 (builds on conformant modules) · WP4 (extraction) · WP5's r10 close record · the A14 registration request.
Proof: E2 per commit (the row's CT + full suites) + the WP2-close evidence set at BOTH seats (tagged census + plain suite + module batteries), per ruling condition (a).
No-consumer action: n/a — chartered.

## 7. Acceptance at WP2 close
1. All 38 §2 rows GREEN by their PM-confirmed predicates, each greened in its named fix commit; the census reads EXACTLY 59/5 with the five reds justified {A12,C07,C09→WP3; D01,D02→WP4}; the 21 launch greens (20 pins + D04) never regressed.
2. Plain suite + vet + the untagged sentinel + the 64-row bijection intact at every commit and at close, at BOTH seats.
3. No conforming-side byte moved without its PM's word on the trail; no fence deviation; every contract question that arose is on the trail with its ruling.
4. The WP2-close SITREP: the census + per-red justification + the commit→row map + the A14 registration request + the disclosed dispositions (incl. the census.py modernization and the s8 dogfood transient from the coda close, carried for master's awareness); then the pair HOLDS for my carriage UP and the WP3 plan.

## 8. Out of scope (touch = escalation by construction)
A12/C07/C09 (WP3) · D01/D02 (WP4) · any conforming-side byte absent its PM's word · contracts/ledger/`master/**` · `go.mod`/`go.sum` · governed s8 machinery · exit fixtures · push/PR/merge · any E3/composition/exit claim.

## 9. Sequencing after your review
Your PLAN-REVIEW verdict TO me, parented to `s16a-build-15`, engine-submitted. On approve: the successor IMPL relay carries the WP2 token (parent = your approving review; SCOPE_DIFF at that dispatch over §2's named trees). Straight-through build per the standing cadence — blockers and contract questions ride UP as they arise; the chartered end review is WP5's.

ACTIONS_GIT_REF: engine-lane governance act — this PLAN r15 drafted at .engine/drafts/s16a.planner/ and submitted through relay submit (r14 superseded, standing as history); no source, test, branch, or commit byte from this seat.
FINAL_GIT_STATUS_SHORT: (literal at draft authoring, governing checkout; the implementation worktree is clean at 89177405 per the hold report:)
 M master/relays/CHECKPOINTS.md
