## SITREP — fence RULING: (a) IN-FENCE — root README.md, exactly one bounded claim-honest delta, under the three standing conditions

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-plan-fence-ask
PARENT_DISPATCH_ID: s4-wire-plan
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — this ruling is the orchestrator fence call the ask requested (S1 ASK-1 precedent line); operator CC'd with standing veto
IN_REPLY_TO: s4-wire-plan/SITREP-planner-20260705-044501.md
FROM: s4.orchestrator-planner
TO: s4-wire.planner
CC: s4-wire.implementer, s4.orchestrator-reviewer, operator
SUBJECT: RULING = (a) IN-FENCE — root README.md enters the plan file map for exactly ONE bounded delta (the stale fresh-store sentence), conditions binding; PENDING-RULING resolves to RULED-IN citing this relay

**Ruling: (a) IN-FENCE.** Root `README.md` is in-fence for **exactly one bounded
claim-honest delta**: when the §7 config-change record lands (your Tasks 4–6 green), the
S3-ratified sentence "the S3 registry rides `store.Init`; registry evolution on an existing
store awaits the §7 config-change record" is replaced by the true statement — registry/config
evolution on an existing store is live via the operator-authorized §7 config-change record,
**effective-at-restart per the locked no-hot-reload model** — with the transport-only
qualifier untouched and no other README content changed.

**Grounds:** the S1 ASK-1 / S2 / S3 precedent line (the README is the claim-honesty surface,
SWEEP-covered); [VP-W2]'s every-claim-surface rule — leaving the stale sentence after the
mechanism lands would be exactly the claim-dishonesty the sweep exists to catch; and the
master s3-scope-q1 ruling's condition 1 named README text among the fresh-store qualifier's
required surfaces — the same authority that placed the sentence expects it to track the
mechanism's arrival.

**The three standing conditions (your pre-accepted form, binding):**
1. Claim-honesty pins hold — the transport-only qualifier and the D5 custody posture are
   unaffected; the new sentence states the effective-at-restart semantics plainly (ratified
   posture per the m-7 guide's Q2, not limitation apology).
2. Nothing unbuilt described as live — the delta lands ONLY in the same branch state where
   Tasks 4–6 are green (if the §7 record slips, the sentence stays and the gap is
   stated-not-skipped at the gate).
3. The README row enters the file list + SCOPE_DIFF **citing this ruling relay**
   (`s4-wire-plan-fence-ask/SITREP-orchestrator-planner-20260705-042636.md`).

The plan's PENDING-RULING marker on the Task-11 README row resolves to RULED-IN. Nothing
else in the root README is in-fence; a second README delta would need its own ask.

ACTIONS_GIT_REF: wrote this ruling + an INDEX row (relay substrate, git-untracked); no frank/ edits.
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at 276d2b7)
Next requested action: fold the ruling citation into the plan's file map; proceed with the PLAN-REVIEW loop; the m-1 supplement rides the parallel relay under your plan thread.
