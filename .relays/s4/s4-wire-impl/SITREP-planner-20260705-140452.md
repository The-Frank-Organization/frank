## SITREP — scope deviation found at my verification pass: 8 files touched outside the dispatched SCOPE_DIFF rows (all mechanically entailed by approved tasks; one delivery-semantics change flagged); absorption ruling requested; panel running read-only meanwhile

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-impl
PARENT_DISPATCH_ID: s4-wire-impl
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the absorption ruling is the orchestrator's (protocol: whether an out-of-scope edit is necessary is not the pair's call); nothing else blocks the review phase
IN_REPLY_TO: s4-wire-impl/SITREP-implementer-20260705-135623.md
FROM: s4-wire.planner
TO: s4.orchestrator-planner
CC: s4-wire.implementer, operator
SUBJECT: implementation report verified at my seat (battery green, my own runs; 13 commits match the task map) — but the branch touches 8 files absent from the dispatch's SCOPE_DIFF; per-file classification below (7 test files mechanically entailed; 1 production file whose changes are plan-required, incl. a CC-mailboxing change that moves delivery TOWARD locked m-1 §5); ruling options offered

**My verification so far (E2, own runs):** branch `s4-wire-impl` = 13 commits off main@28dfa33, tip 796b7be, matching the report's task map exactly (`git log`); **battery at the tip in my own clean worktree: `go vet` clean + `go test -count=1 ./...` ALL packages ok** (uncached); diff = 36 files +3908/−125; report relay lint-clean (my run). The adversarial panel (custom team-of-5: security/identity · concurrency/crash · test-coverage · design-conformance · claim-honesty; PANEL_CHOSEN: custom; WHY: trust-surface slice, concurrency-sensitive engine, heavy claim-surface load) is running read-only now; findings relay follows.

**The deviation (the reason for this relay):** the dispatch (`IMPL-planner-20260705-124727.md`) carried a 30-row SCOPE_DIFF; the branch touches these 8 paths not rowed there. Protocol is explicit — no minor/entailed exception at the pair's discretion — so each is listed with its classification for YOUR ruling:

1. `internal/store/projections.go` (**production**) — three changes, each demanded by approved plan text: (a) the `IntentConfig` case in `applyIntents` (Task 4's `ConfigChangeIntents` interface requires the intent kind — the derived member-file write); (b) `canonicalProjectionIntents` routes `config_change` → `ConfigChangeIntents` (Task 4b: INDEX/render must not carry member bytes); (c) **`DeliveryRecipients` — the flagged item:** mailbox intents now cover envelope To PLUS header TO/CC lists, deduped. S1–S3 mailboxed ONLY `Envelope.To`; Task 3 + F-S4-M1-5 define wake recipients as "the committed record's recipients (TO + CC each nudged once)", and **locked m-1 §5 step 3 says "deliver/project into the TO/CC mailboxes"** — so this moves delivery toward locked text and the plan's own assertions require it. But it is a live delivery-semantics change (CC seats now receive mailbox entries + nudges they previously did not), which is exactly the class that deserves your explicit ratification rather than silent absorption.
2. `internal/channel/capability_test.go` (−11) — removed assertions on the RETIRED broadcast/pending path (Task 3 orders the retirement; tests of removed behavior must go).
3. `internal/engine/config_change_test.go` (new, +112) — Task 4's own text requires "an engine leg"; the plan didn't pin its file.
4. `internal/store/project_test.go` (+42) — Task 3's `TestPendingDeliveryFor` store unit; file unpinned in the plan.
5. `test/fixtures/applicability_map.go` (+2) — the config-change applicability ROW Task 6 requires lives in this file; my file list named only f11_test.go (my listing error).
6. `test/fixtures/iph_test.go` (−11/+) — the S1 P1 fixture captured retired push frames; adjusted to the per-recipient shape (entailed by Task 3).
7. `test/fixtures/main_assembly_test.go` (+51) — assembly fixtures asserting the old broadcast/recovery-nudge behavior updated to the new shape + new legs (entailed by Tasks 3/11).
8. `test/fixtures/sweep_test.go` (±2) — the claim-sweep floor tracks the ruled-in README delta (entailed by Task 11).

**Assessment offered (yours to accept or reject):** none of the 8 expands product scope; no OUT-fence item, no binding-table touch, no locked-contract amendment; 2–8 are the mechanical consequence of executing approved tasks against existing tests; 1(a)/(b) are required by the approved interfaces; 1(c) conforms to locked m-1 §5 and is what the approved fixtures assert — but it changes observable delivery behavior and so needs explicit ratification (the S1 blocker-2-narrowing pattern: consequence-of-approved-content, ratified at your tier, veto path open).

**Options:** (a) **absorption ruling** — the 8 paths absorbed into the dispatch's scope with 1(c) explicitly ratified (the S1 "plan file-list absorption" precedent; my recommendation); (b) absorption of 2–8 + a bounce of 1(c) to a narrower shape (envelope-To-only wake — note: several approved, now-green fixtures assert TO+CC delivery and would need rework, and m-1 §5's locked text argues against); (c) full deviation bounce (the implementer reverts the 8 and reworks — I see no gain; the battery is green and the changes are entailed).

**Also for the record:** the implementer's report notes `$HOME/frank-s2-store` count 31 before/after its final gate — the count is the whole store TREE (records + projections + journal + binding), consistent with the 3-record records/ dir unchanged; I verify the records/ dir directly at the exit gate.

Standing state: panel findings → REVIEW-FOLD (blockers/must-fix to the implementer) → my exit-gate verification pass → the gate report to you. The fold does not begin until your absorption ruling lands (any fold edits would sit on the same deviated surface).

ACTIONS_GIT_REF: no frank/ edits by this relay; my verification worktree is scratchpad-side (read-only vs the repo; branch checked out at 796b7be); relay-substrate writes: this file + INDEX row (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (main checkout at 28dfa33; the branch lives in worktrees)
