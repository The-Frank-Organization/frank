## SITREP — F-GATE-1 fix direction → s4-wire.implementer: serverInfo.version + the class-pinning handshake-envelope fixture; protocolVersion = NOTE, not change; fold-sized, existing delegated scope

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-impl
PARENT_DISPATCH_ID: s4-wire-impl
RUN_ID: s4
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — rides the standing delegated authority (in-scope file cmd/frank-mcp/mcp.go + the existing fixture file); the operator resumes the gate on green
IN_REPLY_TO: s4-wire-impl/SITREP-orchestrator-planner-20260705-185759.md
FROM: s4-wire.planner
TO: s4-wire.implementer
CC: s4.orchestrator-planner, operator
SUBJECT: land F-GATE-1 on s4-wire-impl — (1) non-empty constant version string in serverInfo (my own E1 confirm: initializeResult at 7dc5f92 builds name-only); (2) schema-validate the WHOLE initialize envelope in-battery (pin the class); (3) protocolVersion pinned answer = NOTE in your report, no behavior change; battery + report the commit

**The defect (three seats now confirmed E1/E3):** `cmd/frank-mcp/mcp.go` `initializeResult()` — `serverInfo` lacks the MCP-required `version`; live Claude Code zod-rejects at initialize (13ms, host log definitive — master's gate-day diagnosis, orchestrator-verified, and my own `git show` this session).

**Direction (the orchestrator's three items, resolved where left to us):**
1. **Fix:** add `"version"` to `serverInfo` as a **non-empty constant string** — use a plain literal (e.g. the slice tag `"0.4.0"` or your call), NOT a dynamic/build-injected value (no host-visible config value; keep the I-PH surface inert).
2. **Pin the class:** extend `test/fixtures/s4_shim_test.go` (in-scope, no new scope row needed per the orchestrator) with a handshake-envelope validation fixture: `initialize` response has `protocolVersion` non-empty string, `capabilities.tools` of the declared shape, `serverInfo.name` AND `serverInfo.version` both non-empty strings; assert as exact-shape validation (marshal→structural check), not substring greps — the class is envelope drift, catch all of it. **Run-FAIL-first against the current code** (it must go red on the missing version before the fix).
3. **protocolVersion (master's non-blocking #1) — resolved as NOTE, no behavior change this gate:** the design (§11) pins the version the shim implements and refuses unknown-major politely; a blind echo of the client's requested version would over-claim capabilities we haven't implemented. Keep the pinned `2024-11-05` answer; record in your report that the pinned-answer posture is deliberate (clients downgrade per MCP negotiation); if a live host rejects on version at resume, that becomes a new gate finding and we address it with evidence rather than pre-emptively mid-gate.
4. **Discipline:** fold-sized — if you touch ONLY `cmd/frank-mcp/mcp.go` + `test/fixtures/s4_shim_test.go`, no FOLD_SCOPE artifact is required (the orchestrator's lane note); anything beyond those two files ⇒ FOLD_SCOPE artifact-before-action citing the relevant authority. Battery + `go vet` green; one commit; report the commit hash + the red-first evidence to me, CC the orchestrator + operator (the operator rebuilds only frank-mcp and resumes at Stage 4/5).

**Kept-positive gate findings (carried for the record, no action):** the honesty line live in all three tool descriptions; typed path-free `auth:invalid-credential` at first live contact; the real rendered form served with `additionalProperties:false`. I-PH + the honesty rail held live.

ACTIONS_GIT_REF: none — no edits made; direction only (my verification = `git show` reads); relay-substrate writes: this file + INDEX row (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (main checkout at a95701d)
