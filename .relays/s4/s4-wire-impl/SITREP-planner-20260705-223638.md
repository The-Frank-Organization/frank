## SITREP — F-GATE-2 fold direction on the m-2 confirm (record_kind_in atoms adopted) → s4-wire.implementer; PLUS one surface-discrepancy note for the orchestrator: m-2's probe read MAIN — the branch's record_kind row differs (F-S4-M1-1 change), so m-2's routed hygiene item needs re-grounding

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-impl
PARENT_DISPATCH_ID: s4-wire-impl
RUN_ID: s4
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the fold rides the standing delegated authority per the orchestrator's F-GATE-2 disposition ("on m-2's confirm the fold lands under pair review"); the two m-2-routed flags are the orchestrator's queue, not fold content
IN_REPLY_TO: ../.relays/s4/s4-wire-impl/SITREP-planner-20260705-224500.md
FROM: s4-wire.planner
TO: s4-wire.implementer
CC: s4.orchestrator-planner, operator, m-2.planner
SUBJECT: land the five owed FieldSpec rows per the m-2 confirm — record_kind_in predicate form ADOPTED (my call on m-2's recommendation: these are the registry's first record_kind-keyed predicates, the canonical atom sets the precedent and is live at predicate.go:37/:166); fixtures + FOLD_SCOPE artifact-before-action; §orchestrator note below on the main/branch registry divergence under m-2's routed items

### To the implementer — the fold (m-2 confirm on record: `../.relays/s4/s4-wire-impl/SITREP-planner-20260705-224500.md`)

1. **The five rows, exactly as confirmed:** `owner`/`source`/`target_surface`/`disposition_path` = `{layer: header, owner: free_text, type: text, fill_constraints: free_text, lineage_role: none, gate_referenceable: false, required_when: {all_of: [{record_kind_in: ["owed_item"]}]}}`; `disposes_owed` = same but `type: id_ref`, `required_when: {all_of: [{record_kind_in: ["owed_disposition"]}]}`. **The `record_kind_in:` atom form is ADOPTED** (m-2's canonical-consistency recommendation — precedent-setting first record_kind predicates; the atom is implemented on our branch). Render is UNSCOPED (the m-2-confirmed lean — the only locked-supported shape for free-text fields).
2. **Fixtures (run-FAIL-first):** the operator's rendered form (describe-grade) presents all four on a `record_kind=owed_item` fill context and `disposes_owed` on `owed_disposition` — assert at the rendered-Form grain (the fields present in the form; required_when firing per the predicate legs); required_when enforcement legs through validate (missing field ⇒ typed `required` violation naming the field); a no-record_kind submit does NOT require them (the predicate gates exactly as the engine does); the discharged OI records on the live store are untouched (read-only assert if cheap, else state).
3. **Discipline:** FOLD_SCOPE artifact-before-action (the F-GATE-1 two-file exemption was that finding's own lane, not standing) — expected rows: `internal/fieldspec/registry.json` (+ the fixture files you choose; name them); anything beyond ⇒ stop. Battery + `go vet` green; one commit; report with red-first evidence. On your report: my targeted verify, then the FORMAL exit-gate record files.

### For the orchestrator (CC) — the surface-discrepancy note + m-2's two routed items

**m-2's confirm is grounded on MAIN's registry** (their cites: `registry.json:112` = `owner: system / fill_constraints: system_only`, no seat_scope). **The s4 BRANCH changed that row** under F-S4-M1-1's sanctioned path (my earlier branch probe: `record_kind` = `owner: seat_scoped_enum`, `fill_constraints: seat_allowed_values`, `seat_scope: {operator: [...+config_change], "*": [the six]}` — the m-1-prescribed operator-scoping expressed via the S3 machinery; the tests lens verified no lane-authority broadening at E1). Consequences for the two items m-2 routed to you:
- **The owed-authoring hygiene item transforms:** m-2 reasoned "record_kind is system-derived, not a seat form-pick" — true on main, NOT on the branch, where record_kind IS a seat form-pick and `*` seats can pick `owed_item`/`owed_disposition` (guarded only by the five required-field bounces once this fold lands). m-2's own fill-time-authority thesis then points at a cheaper, machinery-supported gate than their m-7-intake framing: **narrowing the owed tokens out of the `*` list (operator-only), via the seat-scope machinery the branch already uses.** That would touch existing approved scope data (S2/S3-era token availability) — NOT fold content, NOT self-executed; it is your call whether to (i) rule it now as a one-line hygiene narrowing with m-2+m-1 visibility, (ii) send it back through m-2 against the branch surface, or (iii) record it as an owed item for s5. My input: the S2 Q2=(i) precedent (owed records are operator-authored) argues the narrowing is conformance, not new policy — but the F-S4-M1-1 no-broadening check was argued on "`*` unchanged", so ANY `*` edit deserves m-1's eyes too.
- **The two future options** (address-typing `owner`; an owed-id picker for `disposes_owed`) ride to your queue as m-2 framed them — nothing for this fold.
- Suggest m-2 receives the branch-surface note (CC'd here) so their routed items don't carry the stale grounding forward.

ACTIONS_GIT_REF: none — no edits made; direction + reporting only; relay-substrate writes: this file + INDEX row (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (main checkout at 670688c)
