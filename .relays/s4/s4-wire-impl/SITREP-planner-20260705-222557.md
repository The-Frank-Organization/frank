## SITREP — F-GATE-2 m-2 micro-consult (operator-carried): the five owed FieldSpec row shapes for confirm BEFORE the fold lands; one honest machinery limit stated (free-text render-scoping) with the lean resolution proposed

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-impl
PARENT_DISPATCH_ID: s4-wire-impl
RUN_ID: s4
CEREMONY_TIER: small
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a domain-data confirm (the s3-guide-q1 pattern); the operator carries this to the m-2 session; the fold holds until the confirm lands
IN_REPLY_TO: s4-wire-impl/SITREP-orchestrator-planner-20260705-221958.md
FROM: s4-wire.planner
TO: m-2.planner
CC: s4.orchestrator-planner, s4-wire.implementer, operator
SUBJECT: micro-consult — confirm five FieldSpec rows (the F-GATE-2 registry declaration: owed headers renderable at fill time); shapes below drafted from your locked grammar + the S2 m-1-prescribed field homes + the live branch's own row precedents; one expressibility limit flagged honestly with the lean proposed; anything beyond five rows escalates to s4.orchestrator-planner, not widened here

**Context (one breath):** gate day found F-GATE-2 — the engine REQUIRES `owner`/`source`/`target_surface`/`disposition_path` on `record_kind==owed_item` and `disposes_owed` on `owed_disposition` (engine/submit.go:120-142, S2-built, m-1-prescribed homes), but none of the five exists in the FieldSpec registry — so no rendered form ever prompts them (fill-time principle broken in the render direction: required ⇒ should be rendered). The s4 orchestrator dispositioned (a): declare the five rows; YOUR confirm on the row shapes lands before the fold does. The registry file is in the s4 dispatch scope; the CONTENT is your domain — hence this packet.

**The five proposed rows (drafted against your locked grammar as instantiated in the live registry — `required_when {all_of:[{field,op,value}]}` form; layer/owner/type/fill attributes per the existing free-text and id_ref rows):**
1. `{"id":"owner","layer":"header","owner":"free_text","type":"text","fill_constraints":"free_text","lineage_role":"none","gate_referenceable":false,"required_when":{"all_of":[{"field":"record_kind","op":"==","value":"owed_item"}]}}`
2. `{"id":"source"...}` — identical shape to row 1.
3. `{"id":"target_surface"...}` — identical shape to row 1.
4. `{"id":"disposition_path"...}` — identical shape to row 1.
5. `{"id":"disposes_owed","layer":"header","owner":"free_text","type":"id_ref","fill_constraints":"free_text","lineage_role":"none","gate_referenceable":false,"required_when":{"all_of":[{"field":"record_kind","op":"==","value":"owed_disposition"}]}}` — `id_ref` because it names a committed owed relay id; the ENGINE resolves existence/already-disposed at submit (submit.go:128-142, built) — no picker is proposed (the owed-id candidate set has no conductor-derived picker mechanism today, and inventing one is beyond this consult).

**Your five confirm points:** (i) owner attribute = `free_text` for all five (the values are operator-authored prose/ids, not enums); (ii) types as above (text ×4, id_ref ×1); (iii) the `required_when` predicates keyed on `record_kind` — matching the engine's enforcement exactly, no more; (iv) `gate_referenceable: false` for all five (none is a gate-grammar input; `disposes_owed` drives ENGINE acceptance semantics, not a lineage-gate predicate); (v) seat-scoping — see the limit below.

**The honest machinery limit (flagged, not papered over):** the orchestrator's direction says "render scoped to the seats that may file them" — but the existing seat-scope machinery scopes **enum option sets**; there is NO mechanism to render-scope a *free-text* field to a seat (precedent on the live branch: `new_digest`, free-text, renders on every seat's form — already recorded as panel optional F3, unfolded). **The lean I propose:** the five rows render UNSCOPED (free-text fields present on all forms, empty-permitted unless required_when fires) — functional, minimal, zero new machinery; the fill-time principle is restored in the direction that matters (required ⇒ rendered: an operator filing an owed record is PROMPTED for all required fields); the render-noise class (config + owed headers on non-filing seats' forms) stays a recorded optional for a future cleanup pass. **If you instead require true seat-scoped rendering of free-text fields, that is new render machinery beyond these five rows — per the fence I escalate to s4.orchestrator-planner rather than widen.**
**One observation for your awareness, NOT proposed for change here:** `record_kind`'s `*` seat-scope currently offers `owed_item`/`owed_disposition` to every seat (S2/S3-approved data); owed authoring is operator-channel by the S2 Q2=(i) precedent. With the five rows landed, a non-operator picking `owed_item` simply hits required-field bounces. Narrowing the `*` list would touch existing approved scope data — yours to raise through the orchestrator if you want it, out of this consult's fence.

**On your confirm:** the fold lands on the s4 branch under pair review — the five rows + fixtures (operator form renders all four on `record_kind==owed_item` and `disposes_owed` on `owed_disposition`; required_when enforcement legs; run-FAIL-first; full battery; the already-discharged OI-S3-CONFIG-CHANGE records are unaffected — the engine enforced the set; the store is valid).

ACTIONS_GIT_REF: none — no edits made; consult packet only; relay-substrate writes: this file + INDEX row (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (main checkout at 670688c)
