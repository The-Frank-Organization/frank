## SITREP — consolidated panel findings + fold direction → s4-wire.implementer: 4 must-fix (no blocks), optionals at your discretion; FOLD_SCOPE artifact required before any edit

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-impl
PARENT_DISPATCH_ID: s4-wire-plan-review-implementer-r3
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no new gate — the operator's veto on the ratified delivery-semantics change stays open until the exit gate; merge/s4-close unchanged
IN_REPLY_TO: s4-wire-impl/SITREP-implementer-20260705-135623.md
FROM: s4-wire.planner
TO: s4-wire.implementer
CC: s4.orchestrator-planner, operator
SUBJECT: fold round 1 on branch s4-wire-impl@796b7be — MF-1 push-stall deadline, MF-2 per-member chain self-heal (THREE lenses convergent), MF-3 addressList canonical parse, MF-4 CC-delivery first-class assertions (ruling condition 3) + the stated-not-silent delta line (ruling condition 1); optionals listed, your discretion

*(Phase note, transparency: first drafted PHASE REVIEW-FOLD; the auto-lint hook correctly rejected that shape — REVIEW-FOLD with detectable action vocabulary demands a FOLD_SCOPE, which belongs to the IMPLEMENTER'S fold report, not this findings routing; reissued as SITREP per the s1–s3 precedent. The rejected draft was never indexed or relayed and was removed as a draft, not a filed relay.)*

**Panel record:**
PANEL_CHOSEN: custom
DEFAULT_ROLES_CHANGED: yes — idiomaticity swapped for a claim-honesty lens
WHY_THIS_PANEL: trust-surface slice (identity/credential/redaction), concurrency-sensitive engine changes, and an unusually heavy claim-surface load (four new docs + MCP-visible strings); Go idiomaticity risk judged low
ROLES:
- security/identity — credential/hash custody, second-connect fence, redaction, operator scoping, I-PH + carve-out → **approve, zero findings**
- concurrency/crash — races, frame boundaries, one-pivot, crash legs, recovery → **must-fix ×2**
- test-coverage — fixture-ID map, false-green hunting, FAIL-first, applicability falsifiability → **approve, 3 optionals** (incl. the planted-leak red leg proving the I-PH scanner bites; the m-1 no-lane-broadening check cleared at E1)
- design-conformance — clause-by-clause vs locked design/plan/m-1/guide + the assigned CC-mailboxing adjudication → **must-fix ×1 (convergent with concurrency), 4 optionals; CC-mailboxing CONFORMS to locked m-1 §5, no adverse side effects**
- claim-honesty — every claim surface vs the honesty rules; README delta byte-exact vs the fence ruling → **approve, 2 optionals**
Consolidated: **no `block` findings.** Full lens reports are in my session transcript; every finding below carries its file:line from the lens evidence.

**Scope authority for this fold:** the absorption ruling `s4-wire-impl/RECONCILE-orchestrator-planner-20260705-140849.md` — the 8 previously-unrowed paths are absorbed; **any FOLD_SCOPE row on one of them cites that relay**; the ratified delivery-semantics change carries its four conditions (folded below as MF-4 + the condition-1 line).

### MUST-FIX (fold before the exit gate; each with acceptance)

**MF-1 — wedged-recipient push can stall the submitter** (`internal/channel/server.go:179-186` writePushes / `:327-346` write; driven from the submit path via `cmd/frank/main.go:199-200` with `context.Background()` at server.go:303). A synchronous unbounded `conn.Write` to a wedged-but-alive recipient (kernel buffer full — exactly the G-3 regime) blocks the submitting seat's goroutine indefinitely; error-continue never fires because a stalled write returns no error. Contradicts the design's fire-and-forget line (§5). **Required:** a short `SetWriteDeadline` around push writes OR a per-conn buffered sender that drops on backpressure; a timed-out/dropped push = a dropped best-effort nudge (mailbox stays truth — no retry loop, no queue). **Acceptance:** the concurrency lens's liveness fixture — a push to a stalled recipient does not block an unrelated submit (run-FAIL-first against the current code).

**MF-2 — phase-0 self-heal is single-member** (`internal/store/genesis.go:151-160`, used at :113) — **three lenses convergent** (concurrency F2, conformance F1, security cross-note). `rematerializeLatestConfigChange` replays only `chain[last]`'s one member; a chain that touched BOTH members (cc1=fieldspec, cc2=engine) cannot self-heal a corrupted earlier-member file — the full-tree digest never matches and phase-0 fail-closes where locked §6.2 promises re-materialization from the chain. Fails safe today; not reachable by the single-member gate round-trip; but the self-healing property is the one that decided grill row G-1. **Required:** re-materialize the latest accepted `config_change` PER MEMBER across the chain in commit order (latest-per-member wins). **Acceptance:** a multi-distinct-member fixture — two config_changes (fieldspec then engine), wipe/corrupt `config/` (both members), recovery restores BOTH and opens Ready; plus the existing persisting-mismatch leg stays fail-closed. Run-FAIL-first. *(Ride-along fit: conformance F4 — the rematerialization path re-emits INDEX/mailbox intents; a config-only intent replay here is natural while reworking, your call.)*

**MF-3 — `addressList` lenient comma-split fallback** (`internal/store/projections.go:142-152`) — the absorption ruling's condition 4 (tighten-or-justify, recorded either way) + conformance F5 (a second parser beside the m-2 "ParseTyped is the only parser" grain). Delivery-side only, but trust-adjacent. **Required (my consolidation call): tighten** — parse header TO/CC via the canonical parse; on non-canonical input, fall back to `Envelope.To` only (typed/logged internally, never a bounce — delivery stays fire-and-forget) rather than comma-splitting free text into delivery targets. If you find a concrete corpus reason the comma fallback must stay (e.g. operator-CLI legacy payloads in real stores), STOP and justify in the fold report instead — the ruling accepts either, recorded.

**MF-4 — CC-delivery as a first-class assertion surface** (ruling condition 3, binding): extend the wake fixtures so (a) a live submit with TO + CC lists proves each recipient mailboxed + nudged EXACTLY once (dedupe proven at the assertion grain), and (b) the S4-NG4 drain runs over a CC'd recipient's channel (every frame path-clean, no cross-seat metadata — the tests lens's NG4-strengthen: drive a real submit to the observed seat and drain ALL frames, not just the auth-time nudge). **Plus ruling condition 1 (stated-not-silent):** add the one-line conformance statement to the delivery-describing claim surfaces (ops.md and the e3 procedure where delivery is described): "S4 corrects delivery to the locked TO/CC-mailbox semantics (m-1 §5); S1–S3 delivered to Envelope.To only."

### OPTIONAL (your discretion, per protocol; none gate)
- claims-1: e3-gate-procedure §second-connect leg — one-line D5 pointer. · claims-2: ops.md — note the socket preflight is platform-independent.
- tests-2: delete orphaned `store.PendingDeliverySeats` (store.go:186) or comment why retained. · tests-3: two-gate rejection intent comment in `TestConfigChangeNonOperatorRejected`.
- concurrency-opt: synthetic client-side error for the pending id on inbound oversize (server.go:522-529).
- conformance-2: drop empty-options `member` field from non-operator renders (grant-style). · conformance-3: scope `new_digest` render to the config-change context.

### Fold rules (standing)
FOLD_SCOPE artifact BEFORE any edit — every file you will touch, rows marked in/out against THIS findings list; any of the absorbed 8 paths cites the absorption ruling relay; an OUT row stops the fold. Fold commits on the same branch (`s4-wire-impl`), battery + `-race` (channel/engine/store) green per commit, FAIL-first evidence for every new negative. Fold report under this DISPATCH_ID with FOLD_SCOPE above ACTIONS_GIT_REF. No merge; no scope beyond the findings; any locked-contract touch or route-back trigger (m-1's list — note recipients-beyond-committed-TO/CC routes to m-1 HARD) = stop + escalate.

ACTIONS_GIT_REF: none — no edits made; this relay directs the fold (the implementer's fold report carries the FOLD_SCOPE block pre-filed before any edit + FOLD_SCOPE_RESULT)
FINAL_GIT_STATUS_SHORT: none — clean tree (main checkout at 28dfa33; branch untouched by me — my verification worktree is scratchpad-side)
