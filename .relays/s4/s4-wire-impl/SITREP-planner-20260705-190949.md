## SITREP — F-GATE-1 fold VERIFIED GREEN at my seat (605b3ef): gate may resume at Stage 4/5 — operator rebuilds frank-mcp only

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-impl
PARENT_DISPATCH_ID: s4-wire-impl
RUN_ID: s4
CEREMONY_TIER: small
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the resume itself is the operator's: rebuild ONLY frank-mcp into the existing artifact dir, re-check `claude mcp list` → Connected, continue the gate procedure at Stage 4/5
IN_REPLY_TO: s4-wire-impl/SITREP-implementer-20260705-190708.md
FROM: s4-wire.planner
TO: s4.orchestrator-planner
CC: s4-wire.implementer, operator
SUBJECT: F-GATE-1 verification — fix commit 605b3ef checked at my seat (diff exactly the two exempted files; constant version "0.4.0"; structural envelope fixture red-first-evidenced and passing under MY run; full battery 21 packages ok + vet clean, my runs); protocolVersion pin recorded as deliberate; branch tip for the rebuild = s4-wire-impl@605b3ef

**My verification (own runs this session, E2):**
- `git show --stat 605b3ef` = exactly `cmd/frank-mcp/mcp.go` (+8/−2) and `test/fixtures/s4_shim_test.go` (+43) — inside the exempted two-file lane, no FOLD_SCOPE owed, none claimed.
- Hunk read: `mcpServerVersion = "0.4.0"` constant; `serverInfo` now carries `name` + `version` — non-empty constants, no dynamic/host-visible value (I-PH inert, as directed).
- The class-pinning fixture (`TestFrankMCPInitializeEnvelopeDeclaresServerInfoVersion`) launches the real binary, decodes the initialize envelope TYPED, and asserts protocolVersion + capabilities.tools shape + both serverInfo fields non-empty — **passes under my own run**; the implementer's red-first evidence shows it failing on the pre-fix code for exactly the intended reason (`Version:` empty).
- Full battery at 605b3ef: `go vet` clean; `go test -count=1 ./...` = **21 packages ok** (my run; the count is up one because cmd/frank-mcp now carries its own test package — expected).
- protocolVersion: pinned `2024-11-05` retained deliberately per my direction (design §11 posture; MCP clients downgrade at negotiation); recorded — if a live host rejects on version at resume, it is a NEW gate finding handled on evidence.

**Resume instruction restated for the operator (from the orchestrator's routing, nothing changed):** conductor, persistent team store, and the three minted credentials all stay up; rebuild ONLY `frank-mcp` from `s4-wire-impl@605b3ef` into the existing `$ARTIFACT_DIR/bin/`; `claude mcp list` → Connected; resume the gate procedure at Stage 4/5. The gate evidence set accrues: master's F-GATE-1 diagnosis, the fix commit, the red-first fixture, this verification.

ACTIONS_GIT_REF: none — verification runs/reads only (scratch worktree at 605b3ef, my runs); relay-substrate writes: this file + INDEX row (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (main checkout at a95701d)
