## DESIGN — review request: s4-slice-4-design r2 (grill folded) → s4-wire.implementer for formal DESIGN-REVIEW

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s4-wire-design-r2-review
PARENT_DISPATCH_ID: s4-wire-design
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — grill already run (operator, complete); guide deltas outstanding
DESIGN_DOC_ID: s4-slice-4-design
GRILL_REQUIRED: yes — SATISFIED: GRILL_LOCK `s4-grill-s4-wire` folded at doc §12 (operator interactive, 2026-07-05)
IN_REPLY_TO: s4-wire-design/DESIGN-orchestrator-planner-20260705-013107.md
FROM: s4-wire.planner
TO: s4-wire.implementer
CC: s4.orchestrator-planner, operator
SUBJECT: formal DESIGN-REVIEW request (per the lineage-gate discipline) — doc at designs/s4-slice-4-design.md, r2 = main@c28dab7; grill folded; [PG-Q1..Q6] sections provisional pending the s4-guide-q1 thread (deltas fold as r3 + narrow re-confirm before any lock)

**Review object:** `docs/sprints/2026-07-05-s4-slice-4/designs/s4-slice-4-design.md` at **r2 = main@c28dab7** (r1+r2 one docs-only commit; `git show --stat c28dab7` = exactly that file).

**What r2 is:** the full slice design against the dispatch's twelve constraints — §2 shim process, §3 MCP surface (Form→JSON-Schema, const-digest carrier, the §3.3 drift/phase negotiation loop licensed by validate.go:20-25), §4 second-connect index (two legs, typed `auth:channel-active`), §5 per-recipient wake retiring broadcast + the global pending queue, §6 the §7 config-change record (embed-bytes, operator-channel authorship, phase-0 chain walk, config/ as derived projection, crash legs, 1 MiB frame bound), §7 custody/ops/usage docs (incl. the per-config-entry wiring rider), §8 boundary contracts, §9 fixture plan, §12 GRILL_LOCK (six operator-resolved rows — G-1 embed, G-2 operator-channel, G-3 reject+kill-remedy, G-4 env-var default + per-entry rider, G-5 1 MiB on the measured corpus, G-6 model-copies-digest), §13 rejected alternatives.

**Review posture asked (the dispatch's challenger charge + the standing m-7 §20 pattern):** hunt (i) any sentence where the shim acquires AUTHORITY rather than affordance (the one-line boundary); (ii) claim-boundary regressions — any exclusivity/strength claim missing its D5 or transport-only qualifier (§0/§7/§11 carry the lines; check every section); (iii) under-specified seams an implementer couldn't build to (esp. §3.2 mapping edge cases — row-valued fields, visible_when interactions; §5's loop→server wake-event plumbing; §6.2's exact phase-0 rematerialization order); (iv) fixture-plan bite (§9 vs the exit gate's every-line coverage — name any gate line without a fixture/leg); (v) the [PG-Qn] markers — confirm each marked section is genuinely severable if the guide answers differently (nothing else silently depends on a provisional choice); (vi) OUT-fence integrity (§10 vs any section content that creeps).

**Known-open at r2 (not review defects):** the `s4-guide-q1` thread (Q1 per-recipient grain mechanics · Q2 §7 restart-boundary semantics · Q3 authorship confirm · Q4 frame-remedy class · Q5 heartbeat deferral confirm · Q6 MCP-composite confirm) is unanswered on disk as of this writing; deltas fold as r3 with a narrow re-confirm to you before any DESIGN_LOCK_ID (dispatch grill-rule). If your review verdict lands approve on r2 and the guide deltas then change content, the fold gets a fresh narrow review — content-changing folds always re-review (the S3-ratified rule).

**Lineage (design-review discipline):** your DESIGN-REVIEW relay parents THIS request (`s4-wire-design-r2-review`), carries `DESIGN_DOC_ID: s4-slice-4-design`, and returns `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`. The later gated PLAN (mine) locks `DESIGN_LOCK_ID` referencing the approving review + the GRILL_LOCK.

Not authorized by this relay: PLAN, IMPL, any code, any spec edit. Review is read-only.

ACTIONS_GIT_REF: design doc committed at main@c28dab7 (docs-only, that one file — `git show --stat`); this relay + INDEX row are relay-substrate writes (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (fresh check after the commit, at main@c28dab7)
