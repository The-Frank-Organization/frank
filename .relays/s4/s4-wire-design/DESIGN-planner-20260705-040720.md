## DESIGN — narrow re-review request: s4-slice-4-design r3 (F1+F2 folded) → s4-wire.implementer

> **SUPERSESSION / TIMESTAMP CORRECTION (2026-07-05, s4-wire.planner):** this file supersedes
> `DESIGN-planner-20260705-041500.md` — CONTENT IDENTICAL below the header; the ONLY correction is the
> filename timestamp. The original was stamped 041500 by planner error (invented forward stamp); its true
> creation time is 04:07:20 (file mtime evidence), which is what this filename now carries. The wrong stamp
> inverted the trail's causal order (the approving DESIGN-REVIEW 040925, really written 04:11:33, sorted
> BEFORE the request it answers), failing the root-mode design-lineage walk. The superseded file
> remains on disk (append-only); nothing else consumed it by filename except the review's IN_REPLY_TO
> (display-only, not a gate input).

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s4-wire-design-r3-review
PARENT_DISPATCH_ID: s4-wire-design-r2-review-implementer
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
DESIGN_DOC_ID: s4-slice-4-design
GRILL_REQUIRED: yes — SATISFIED (GRILL_LOCK `s4-grill-s4-wire`, doc §12, untouched by r3)
IN_REPLY_TO: s4-wire-design/DESIGN-REVIEW-implementer-20260705-035327.md
FROM: s4-wire.planner
TO: s4-wire.implementer
CC: s4.orchestrator-planner, operator
SUBJECT: narrow re-review per your must-revise — r3 = main@2ef9437 (one docs-only commit, 38+/18−, that file only); F1 guide-fold complete (every required item landed in the body, all PG markers resolved), F2 carrier rule closed at §3.2; scope = your three named focus areas

**Review object:** `docs/sprints/2026-07-05-s4-slice-4/designs/s4-slice-4-design.md` at **r3 = main@2ef9437** (`git show --stat 2ef9437` = exactly that file; r2 = c28dab7 unchanged beneath it).

**F1 — folded, item by item (your required-revision list → where it landed):**
- Guide answer into the body, not only the rev log → §5 (Q1: the three load-bearing properties verbatim — store-derived nudge-on-auth, fire-and-forget + mailbox truth, no-cross-seat-metadata), §6.1 (Q3: the second-provenance-path reasoning + the binding no-extra-approval-loop note), §6.2 (Q2: record-pivot-then-derived-bytes as a derived-intent class; chain from committed records ONLY; mismatch-anywhere ⇒ fail-closed-serving-reads), §6.4 (Q4: typed refusals BOTH directions), §4 (Q5: deferral + two conditions), §3.3/§3.5/§5/§10 (Q6: composite confirmed; list_changed mapping; poll-first-by-locked-design; the two shim guardrails).
- PG markers → all resolved in place (grep of the body is clean; remaining `[PG-…]` strings sit only in the historical rev-log/GRILL_LOCK record).
- §5 + fixtures: no-cross-seat-metadata negative → **S4-NG4**.
- §6.4 + fixtures: both-direction typed refusal incl. outbound `project`/`read` growth with narrower-re-scope hint → **S4-FR2**; the **ceiling-value disclosure carve-out** recorded as a named §8.4 exemption, with the I-PH matrix asserting it covers exactly that one value.
- §4/§7/§9: written wedged-host remedy (ops-surface deliverable) + kill-host→reconnect→recovery fixture → **S4-SC3**.
- §3/§8/§9: the verbatim-schema guardrail (no shim-side reshaping re-adding withheld affordances) folded at §3.5 and load-bearing in the §3.2 rule below.
- Seat-occupancy paragraph → landed at §7 (exposition of the locked occupancy model + the Step-1 wiring-time residual; no scope change — no new mechanism, no new surface; flagging per your instruction so you can confirm that reading).

**F2 — the carrier rule, closed at §3.2:** every FieldSpec type maps to a **string-typed** property (full table for the live set: text/enum/bool/id_ref/row_array/address_list + ParseTyped's object). Structured types are exposed **as canonical-JSON strings** — the S3 locked carrier riding through unchanged; the value the host writes is byte-for-byte what lands in `Headers map[string]string`; `ParseTyped` remains the ONLY parser/validator; a mis-encoding gets the conductor's typed field-named violation through the normal result path. Native-JSON exposure + shim-side canonicalization is rejected on your own grounds (§13: second canonical-encoder in the trusted bridge; split violation-surfacing; digest-proves-host-visible-schema broken). Fixture **S4-SCH2** covers round-trip, mis-encoding bounce, and honest schema description.

**Narrow scope (your Required Next Review Shape):** (i) guide-answer fold completeness; (ii) the structured carrier rule; (iii) boundary-contract + OUT-fence preservation (§8/§10 — Q6's confirm is folded into the §10 line; no other change).

**Lineage:** your re-review parents THIS request (`s4-wire-design-r3-review`), same `DESIGN_DOC_ID: s4-slice-4-design`. On approve, the approving review + GRILL_LOCK feed the DESIGN_LOCK; the completion SITREP then goes to s4.orchestrator-planner. Your must-revise is not consumed as any lock, per its own instruction.

Not authorized: PLAN, IMPL, any code, any spec edit.

ACTIONS_GIT_REF: design doc r3 committed at main@2ef9437 (docs-only, one file, 38+/18−); this relay + INDEX row are relay-substrate writes (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (fresh check after the commit, at main@2ef9437)
