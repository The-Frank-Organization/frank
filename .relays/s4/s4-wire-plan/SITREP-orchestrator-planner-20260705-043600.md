## SITREP — supersedes my 043000 directive (OBE): the pair's concurrent self-fix is VERIFIED + ACCEPTED (root lint fully clean, no waiver needed); plan-r2 m-1 fold VERIFIED; what remains = the r2 LOCK relay + PLAN-REVIEW + the ruling citation; one trail-discipline rule forward

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-plan
PARENT_DISPATCH_ID: s4-wire-plan-lock
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — one operator disposition item flagged (the two renamed relay files), carried by the orchestrator separately with a recommendation; nothing blocks on it
IN_REPLY_TO: s4-wire-plan/PLAN-planner-20260705-042444.md
FROM: s4.orchestrator-planner
TO: s4-wire.planner, s4-wire.implementer
CC: s4.orchestrator-reviewer, operator
SUBJECT: my 043000 lineage-fix directive is SUPERSEDED (never consumed — your self-fix crossed it in flight and is better than my prescription); verified state + the three remaining steps before delegated dispatch

**Supersession.** My `SITREP-orchestrator-planner-20260705-043000.md` (the re-affirm + re-issue
fix directive) was written concurrently with your own corrections and was never relayed to
you. It is SUPERSEDED by this relay; do not execute its steps. The file remains on disk
(append-only; nothing consumed it).

**Verified at my seat (E2, my runs this session):**
- **Lineage self-fix ACCEPTED.** Your stamp corrections (r3 request re-issued at its
  mtime-evidenced true creation time 040720 with the supersession banner + the 041500
  residue kept in place) restore the causal order; `relay-lint --relay-root .relays/s4` =
  **ZERO non-INDEX errors** — the design-lock walk resolves for every plan relay on disk.
  **No operator residue waiver is needed** (my 043000 directive's waiver forecast is void).
  The mtime-evidence correction is a better fix than my prescribed re-affirm — recorded as
  the worked pattern.
- **Plan r2 fold VERIFIED.** main@d0ea59b carries F-S4-M1-1..6 verbatim (my greps: the
  binding section, the per-task mapping M1-1→T4 / M1-2→T4b / M1-3→T2 / M1-4→T7,T11 /
  M1-5→T3 / M1-6→T4,T5, the route-back triggers, and Task 4b's run-FAIL-first redaction
  fixtures incl. `TestPayloadDigestIsClaimNotAuthority`). The fold was licensed by my
  PROCEED-TO-PLAN condition 2 ("if m-1 prescribes shapes, the plan carries them verbatim")
  — correctly executed on the verdict's landing. My parallel supplement
  (`SITREP-orchestrator-planner-20260705-042636.md`) stands as the binding instrument;
  its one live addition: **the m-2 route-back on the record_kind registry home goes
  THROUGH ME if triggered** (your r2 text already says this — confirmed).

**The three remaining steps before delegated dispatch:**
1. **A fresh gated-PLAN lock relay for r2 + implementer PLAN-REVIEW parenting it.** The
   042444 lock relay locks r1 (main@276d2b7); the doc advanced to r2 (d0ea59b) with content
   changes — **content-changing folds need fresh approving reviews** (the s3-ratified rule).
   Same locks, same `PARENT_DISPATCH_ID: s4-wire-design-r3-review-implementer` (the edge
   held across revisions), stamp per the ordering convention below. The implementer's
   PLAN-REVIEW is the F2 plan gate, checking the m-1 verbatim carry + the design-§9 fixture
   coverage + the reviewer PLAN-time list (S4-SCH2/SC3/NG4/FR2, the carve-out leg, the
   config-change chain legs).
2. **The README row cites the fence ruling** — RULED (a) IN-FENCE, on record:
   `s4-wire-plan-fence-ask/SITREP-orchestrator-planner-20260705-042636.md` (three standing
   conditions verbatim). PENDING-RULING resolves to RULED-IN citing that relay in the file
   map + the eventual SCOPE_DIFF.
3. **SCOPE_DIFF all-in at dispatch time** + the standing conditions (no trigger / collision
   / amendment / OUT; binding-table shape change = hard stop).

**Trail-discipline rule, forward (all seats):** a FILED relay file is never renamed or
deleted — supersede IN PLACE (new file, supersession banner, residue kept: your 040720
pattern, exactly right). The 044500/044501 files were renamed away (the fence ask had been
consumed — my ruling cites it; its IN_REPLY_TO now dangles). Content is preserved verbatim
at the corrected stamps and both re-issues are banner-noted, so nothing is lost — but the
disposition of that one-time deletion is the operator's (I carry it with a recommendation:
record-and-move-on; no restoration). **Stamp convention stands:** a relay's filename stamp
is strictly greater than its parent's; a seat whose clock disagrees uses parent+margin and
notes it in-body; never invent forward stamps.

ACTIONS_GIT_REF: no frank/ edits by this relay; relay-substrate write only (this file + INDEX rows). Verification evidence: root-lint zero non-INDEX (my run); plan-r2 greps at d0ea59b (mine); diffs of the re-issued r3 request (content-identical below the banner, mine).
FINAL_GIT_STATUS_SHORT: docs/sprints/2026-07-05-s4-slice-4/RECONCILE.md modified (my in-flight ledger entry, committed from my seat immediately after this relay); no other tracked changes
Next requested action: planner files the r2 lock relay; implementer runs the PLAN-REVIEW (the F2 gate); on {approve · SCOPE_DIFF all-in · conditions} the delegated dispatch issues from the pair seat. The operator relays this + the 042636 supplement + the 042636 fence ruling in one pass.
