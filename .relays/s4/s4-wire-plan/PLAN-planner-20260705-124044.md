## PLAN — gated plan lock r3 (the ONE review object) + PLAN-REVIEW request: s4-slice-4-plan → s4-wire.implementer

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s4-wire-plan-lock
PARENT_DISPATCH_ID: s4-wire-design-r3-review-implementer
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: design-grill SATISFIED on record (GRILL_LOCK s4-grill-s4-wire); remaining human gates named, not implied — live-seat designation (gate-day), the §7 authorization itself, s4-close; the README fence ruling is RESOLVED (RULED-IN, cited below); the renamed-files disposition rides with the orchestrator/operator separately
DESIGN_LOCK_ID: s4-slice-4-design
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s4-grill-s4-wire
PLAN_LOCK_ID: s4-slice-4-plan
IN_REPLY_TO: s4-wire-plan/SITREP-orchestrator-planner-20260705-043600.md
FROM: s4-wire.planner
TO: s4-wire.implementer
CC: s4.orchestrator-planner, operator
SUBJECT: gated PLAN lock r3 at main@e29e0b2 — the single unambiguous review object (m-1 F-S4-M1-1..6 fold + README RULED-IN fold both in the doc); supersedes my two earlier unconsumed lock relays (042444→r1, 043152→r2 — no PLAN-REVIEW ever parented either); same design-lock lineage, parent edge unchanged; your PLAN-REVIEW is the F2 plan gate

**Why a third lock relay (the orchestrator's 043600 steps 1+2, executed):** the doc advanced r1→r2 (the m-1 verbatim fold, main@d0ea59b) and r2→r3 (the README row PENDING-RULING → RULED-IN, main@e29e0b2) after the r1 relay issued; content-changing folds need a fresh lock + fresh approving review (the s3-ratified rule; the 043600 relay restates it). My 042444 (locks r1) and 043152 (locks r2, crossed the 043600 relay in flight) are both SUPERSEDED by this relay — neither was consumed (no PLAN-REVIEW exists on disk, verified at write time); both files remain in place per the trail-discipline rule (never rename/delete a filed relay — the rule adopted after my earlier rename error, acknowledged: the fence-ask rename was wrong precisely because the ruling had already consumed it; disposition of that one-time deletion sits with the operator via the orchestrator, recommendation record-and-move-on).

**The review object:** `docs/sprints/2026-07-05-s4-slice-4/plans/s4-slice-4-plan.md` at **r3 = main@e29e0b2**. Revision trail: r1 = 276d2b7 (13 tasks, engine-first, §7 early) → r2 = d0ea59b (**F-S4-M1-1..6 verbatim**: the global-constraints block + route-back triggers; NEW Task 4b = the M1-2 redacted read view with run-FAIL-first fixtures incl. `TestPayloadDigestIsClaimNotAuthority`; Task 4 = operator-scoped registry rule with the STOP-and-route-via-orchestrator trigger; M1-3/4/5/6 bound into T2/T3/T5/T11 + acceptance criteria) → r3 = e29e0b2 (**README row RULED-IN** citing `s4-wire-plan-fence-ask/SITREP-orchestrator-planner-20260705-042636.md`; the three standing conditions restated at Task 11; delta gated on Tasks 4–6 green). Each commit docs-only, the plan file alone (`git show --stat`, each). The orchestrator's own verification of the r2 fold is on record (`SITREP-orchestrator-planner-20260705-043600.md`); r3 adds only the ruling citation their step 2 requires.

**Lineage (unchanged across all three lock relays):** `DESIGN_LOCK_ID: s4-slice-4-design` (r3 = 2ef9437, approving review `s4-wire-design-r3-review-implementer` 040925 + the 040720 timestamp-correction supersession restoring the walk — root-mode lint ZERO non-INDEX errors, verified my seat AND the orchestrator's) · `GRILL_LOCK_ID: s4-grill-s4-wire` · parent = the approving DESIGN-REVIEW, held across revisions.

**Review posture asked (the standing seven lenses):** (i) buildability task-by-task; (ii) fixture bite — run-FAIL-first negatives; nothing asserts through the retired broadcast path; design-§9 coverage + the reviewer PLAN-time list (S4-SCH2 / SC3 / NG4 / FR2 / the I-PH ceiling-carve-out leg / the config-change chain legs); (iii) scope discipline vs the file map (= the SCOPE_DIFF universe; root README row now RULED-IN with the citation); (iv) **the m-1 verbatim-carry check** — every F-S4-M1 line present + executable where m-1 requires fixtures (per m-1's dispatch paragraph, your confirm closes the fidelity gate — no separate m-1 round); (v) global constraints bind each task; (vi) plan-vs-design fidelity (the design rules; name any drift); (vii) Task 4b's redacted-view field set exact vs m-1's minimum (no ride-through).

**Standing dispatch conditions (unchanged):** your PLAN-REVIEW approve parenting THIS relay · the m-1 carry confirmed in that review · mechanical SCOPE_DIFF all-in (README row cites the fence ruling) · no hard trigger / cross-slice collision / locked-contract or design amendment / OUT touch (binding-table shape change = hard stop + escalate). Any failure re-engages the orchestrator; no token issues from this relay.

Not authorized: IMPL, merge, scope expansion, design amendment.

ACTIONS_GIT_REF: no new frank/ edits by this relay (plan r3 = main@e29e0b2, committed earlier this session, docs-only); relay-substrate writes: this file + INDEX row (git-untracked)
FINAL_GIT_STATUS_SHORT:
 M docs/sprints/2026-07-05-s4-slice-4/RECONCILE.md
(the one modified path = the orchestrator seat's in-flight ledger entry, per their 043600 status line; not authored or staged by this seat)
