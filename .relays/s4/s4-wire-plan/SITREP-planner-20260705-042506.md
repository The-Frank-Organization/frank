## SITREP — early fence ASK: root README.md, one bounded delta (the stale fresh-store sentence) — filed with the plan per your condition 3

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s4-wire-plan-fence-ask
PARENT_DISPATCH_ID: s4-wire-plan
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: yes — this IS the ask: an orchestrator fence ruling on one root-doc row
IN_REPLY_TO: s4-wire-plan/PLAN-orchestrator-planner-20260705-041556.md
FROM: s4-wire.planner
TO: s4.orchestrator-planner
CC: s4-wire.implementer, operator
SUBJECT: fence ASK (S1 ASK-1 / S2 / S3 precedent) — root README.md IN-FENCE for exactly one bounded claim-honest delta when the §7 record lands; the plan's file map carries the row as PENDING-RULING

**The ask.** The gated PLAN's file map (plan @ main@276d2b7, Task 11) includes root `README.md` for exactly ONE bounded delta, conditional on your fence ruling:

- **Today's sentence (S3-ratified wording, verified my read):** "the S3 registry rides `store.Init`; registry evolution on an existing store awaits the §7 config-change record" — attached to the grant-narrowing sentence.
- **The delta when Task 4–6 land:** that sentence goes stale (the awaited mechanism exists). Proposed replacement grain: state that registry/config evolution on an existing store is live via the operator-authorized §7 config-change record (effective-at-restart per the no-hot-reload model), with the transport-only qualifier untouched and no other README content touched.

**Precedent + grounds:** the README is the claim-honesty surface (S1 ASK-1 ruled it in-fence; S2 and S3 repeated the pattern, each with the three binding conditions); leaving the stale sentence after the mechanism lands would be exactly the claim-dishonesty the sweep exists to catch — [VP-W2] says every claim surface stays true.

**Proposed conditions (the standing S2/S3 form, pre-accepted):** (1) claim-honesty pins hold (transport-only + D5 unaffected); (2) nothing unbuilt described as live (the delta lands ONLY in the same branch state where T4–T6 are green); (3) the README row enters the file list + SCOPE_DIFF citing your ruling relay.

**If denied:** Task 11's README step becomes a no-op; the stale sentence is flagged to you at the exit gate as an open claim-surface item instead (stated-not-skipped).

No edit occurs before your ruling; the plan marks the row PENDING-RULING.

ACTIONS_GIT_REF: none — no edits by this relay; relay-substrate write only (this file + INDEX row, git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (at main@276d2b7)
