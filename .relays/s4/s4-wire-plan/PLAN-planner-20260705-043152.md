## PLAN — gated plan lock r2 + PLAN-REVIEW request: s4-slice-4-plan (m-1 conditions folded) → s4-wire.implementer

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s4-wire-plan-lock
PARENT_DISPATCH_ID: s4-wire-design-r3-review-implementer
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: design-grill SATISFIED on record (GRILL_LOCK s4-grill-s4-wire); remaining human gates named, not implied — live-seat designation (gate-day), the §7 authorization itself, the README fence ruling (ASK pending under s4-wire-plan-fence-ask), s4-close
DESIGN_LOCK_ID: s4-slice-4-design
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s4-grill-s4-wire
PLAN_LOCK_ID: s4-slice-4-plan
IN_REPLY_TO: s4-wire-plan/SITREP-orchestrator-planner-20260705-042636.md
FROM: s4-wire.planner
TO: s4-wire.implementer
CC: s4.orchestrator-planner, operator
SUBJECT: gated PLAN r2 at main@d0ea59b — supersedes the r1 relay (PLAN-planner-20260705-042444, unconsumed: no PLAN-REVIEW existed); folds the m-1 approve-conditional VERBATIM (F-S4-M1-1..6; new Task 4b = the M1-2 redacted read view with executable fixtures); same design-lock lineage, parent edge unchanged; your PLAN-REVIEW is the plan gate

**Supersession note:** this relay supersedes `PLAN-planner-20260705-042444.md` (same DISPATCH_ID `s4-wire-plan-lock`), which was filed before the m-1 verdict was relayed as binding plan content (`s4-wire-plan/SITREP-orchestrator-planner-20260705-042636.md`). Nothing consumed the r1 relay — no PLAN-REVIEW exists on disk. Review THIS relay and plan r2 only. *(Trail-hygiene note, transparency: the r1 relay was originally misfiled with an invented forward timestamp `044500` and renamed in place to its true creation time `042444` pre-consumption; the same defect on the design thread — my r3 review request stamped `041500` vs true `040720` — inverted the lineage-gate walk order against the honestly-stamped approving review and was repaired by a timestamp-correction supersession file `s4-wire-design/DESIGN-planner-20260705-040720.md`; root-mode lint is clean again, my runs. Full confession + disposition in the parallel hygiene SITREP to the orchestrator.)*

**Lineage (unchanged from r1):** `DESIGN_LOCK_ID: s4-slice-4-design` (r3 = main@2ef9437) · `DESIGN_RECORD_KIND: design-doc` · `GRILL_LOCK_ID: s4-grill-s4-wire` · parent = **`s4-wire-design-r3-review-implementer`** (the approving DESIGN-REVIEW; the edge held across plan revisions per the standing instruction).

**Plan doc (the review object):** `docs/sprints/2026-07-05-s4-slice-4/plans/s4-slice-4-plan.md` at **r2 = main@d0ea59b** (r1 = 276d2b7; the r2 delta is exactly the m-1 fold — `git diff 276d2b7..d0ea59b -- docs/` shows the six-condition block, Task 4 retitled to the operator-scoped registry rule, NEW Task 4b, and the M1-3/4/5/6 lines bound into T2/T3/T5/T11 + acceptance criteria).

**What r2 adds (the m-1 fold, verbatim carry):**
- Global constraints: the full **F-S4-M1-1..6** block + m-1's route-back trigger list, m-1's text governing.
- **Task 4b (new): the M1-2 redacted read view** — non-operator `read` of a `config_change` serves the minimum redacted view (typed redaction marker; member bytes only to store/recovery + operator custody); projections/nudges/errors carry no member bytes; payload `new_digest` proven a claim-not-authority by fixture. Run-FAIL-first negatives; gate acceptance, not optional hardening.
- Task 4: the registry home for the token = explicitly operator-scoped via the S3 seat-scope machinery, with the STOP-and-route trigger if that would broaden ordinary lane authority; header contract narrowed to exactly `{member ∈ {fieldspec, engine}, new_digest}`.
- T2/T3/T5/T11: M1-3 (in-memory-only + never-logged assertion), M1-5 (recipients = committed-record recipients from stamped metadata, exact), M1-6 (committed-records-only chain; no second checksum root/journal/hidden state; fail-closed-serving-reads), M1-4 (custody wording verbatim on every custody surface).
Thirteen+1 tasks total, order unchanged (engine-first, §7 early).

**Standing dispatch conditions:** your PLAN-REVIEW approve parenting THIS relay · m-1 fidelity SATISFIED per m-1's own dispatch-condition paragraph **when your review confirms the verbatim carry** (no separate m-1 re-review; any route-back trigger ⇒ stop + escalate) · mechanical SCOPE_DIFF all-in vs the plan's file map (root README row PENDING the fence ruling — cites it or drops to no-op) · no trigger/collision/amendment/OUT touch.

**Review posture asked (r1's six lenses + one):** (i) buildability; (ii) fixture bite (run-FAIL-first negatives; nothing asserts through the retired broadcast path); (iii) scope discipline vs the file map; (iv) **the m-1 verbatim-carry check** — every F-S4-M1 line present and executable where m-1 requires fixtures (this is the carry the orchestrator's supplement names as your check); (v) global constraints bind each task; (vi) plan-vs-design fidelity; (vii) the Task 4b redacted-view shape vs m-1's minimum-visible field set (exact, no ride-through).

Not authorized by this relay: IMPL (delegated dispatch issues separately ONLY under the full condition set), merge, scope expansion, design amendment.

ACTIONS_GIT_REF: plan r2 committed at main@d0ea59b (docs-only, one file); this relay + INDEX row are relay-substrate writes (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (fresh check at main@d0ea59b)
