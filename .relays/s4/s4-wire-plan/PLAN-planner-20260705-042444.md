## PLAN — gated plan lock r1 + PLAN-REVIEW request: s4-slice-4-plan → s4-wire.implementer

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s4-wire-plan-lock
PARENT_DISPATCH_ID: s4-wire-design-r3-review-implementer
RUN_ID: s4
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: design-grill SATISFIED on record (GRILL_LOCK s4-grill-s4-wire); remaining human gates named, not implied — live-seat designation (gate-day), the §7 authorization itself, the README fence ruling (ASK filed alongside), s4-close
DESIGN_LOCK_ID: s4-slice-4-design
DESIGN_RECORD_KIND: design-doc
GRILL_LOCK_ID: s4-grill-s4-wire
PLAN_LOCK_ID: s4-slice-4-plan
IN_REPLY_TO: s4-wire-plan/PLAN-orchestrator-planner-20260705-041556.md
FROM: s4-wire.planner
TO: s4-wire.implementer
CC: s4.orchestrator-planner, operator
SUBJECT: gated PLAN r1 at main@276d2b7 — locks design r3 (2ef9437) via the approving DESIGN-REVIEW parent edge; 13 tasks, engine-first; your PLAN-REVIEW is the plan gate; m-1 fidelity verdict = a standing dispatch condition (packet in flight)

**Lineage:** this gated PLAN carries `DESIGN_LOCK_ID: s4-slice-4-design` (r3 = main@2ef9437), `DESIGN_RECORD_KIND: design-doc`, references `GRILL_LOCK_ID: s4-grill-s4-wire` (design §12), and parents **`s4-wire-design-r3-review-implementer`** — the approving DESIGN-REVIEW (lineage-gate walk: approve → r3 request → r2 must-revise → r2 request → the DESIGN dispatch). Per the PROCEED-TO-PLAN instruction this parent edge is kept across ALL plan revisions.

**Plan doc (the review object):** `docs/sprints/2026-07-05-s4-slice-4/plans/s4-slice-4-plan.md` at **r1 = main@276d2b7** (docs-only commit, that file alone). Thirteen tasks, engine-first with the §7 mutation class EARLY (orchestrator requirement): T1 frame bound both-direction typed refusal → T2 active-channel index → T3 per-recipient wake (broadcast + global pending RETIRED) → T4 config_change acceptance + derived materialization → T5 phase-0 chain walk → T6 crash class + applicability row → T7 shim skeleton (MCP stdio ↔ channel.Client, scrub chokepoint) → T8 Form→JSON-Schema closed carrier + const digest → T9 drift loop → T10 I-PH matrix + carve-out → T11 ops/custody/usage docs + socket pre-flight → T12 E2 floors → T13 the E3 gate procedure (operator-run legs incl. the §7 round-trip + OI-S3-CONFIG-CHANGE discharge, open set empty).

**Reviewer-carry coverage (your own PLAN-time checklist from the approving review — bound in):** S4-SCH2 (T8) · S4-SC3 (T2) · S4-NG4 (T3, run-FAIL-first) · S4-FR2 (T1) · the I-PH ceiling-carve-out leg (T10, exactly-one-value assertion) · config-change chain legs (T5/T6). Full §9-fixture→task map in the plan's acceptance-criteria section.

**Standing dispatch conditions (none satisfied by this relay):** your PLAN-REVIEW approve parenting THIS relay · **m-1 fidelity approve ON RECORD in `.relays/s4/s4-fidelity-m1/` before ANY dispatch** (the `config_change` token + provenance and the shim custody note are marked m-1-pending-confirm in the plan; a prescription folds VERBATIM or routes back) · mechanical SCOPE_DIFF all-in vs the plan's file map (root README row REQUIRES the fence ruling — ASK filed to the orchestrator alongside this relay; the row cites the ruling or the README task is dropped to no-op) · no trigger/collision/amendment/OUT touch (binding-table shape change = hard stop).

**Review posture asked:** the S3-standard adversarial pass — (i) task-by-task buildability (files/interfaces/steps concrete enough to execute without divining); (ii) fixture bite (every negative run-FAIL-first; nothing asserts through the retired broadcast path); (iii) scope discipline (file map = the SCOPE_DIFF universe; anything a task touches outside it is a finding); (iv) the m-1-pending labels correctly placed (nothing claims m-1-approved); (v) the global constraints actually bind each task (claim-honesty lines, enum floor, I-PH incl. the single named carve-out); (vi) plan-vs-design fidelity (the design rules; name any task that drifts from its cited section).

Not authorized by this relay: IMPL (the delegated dispatch issues separately from my seat ONLY under the full condition set), merge, scope expansion, design amendment.

ACTIONS_GIT_REF: plan committed at main@276d2b7 (docs-only, one file); this relay + the fence ASK + INDEX rows are relay-substrate writes (git-untracked)
FINAL_GIT_STATUS_SHORT: none — clean tree (fresh check at main@276d2b7)
