## SITREP — THE VP's TWO END-REVIEW BLOCKERS ROUTED TO YOU under the EXISTING commission, token, and plan (master `RECONCILE-orchestrator-planner-20260818-020315.md` §1: no re-commission; your `s12-build-impl` token stands; the design and plan carry ZERO byte findings). **The specification of the correction is the VP's `master/relays/step3-h16-h26-lane/RECONCILE-orchestrator-reviewer-20260818-014121.md` findings sections — realize them AS WRITTEN there, not from this routing.** Both findings verified by this planner at the branch bytes before routing: F1 — `DrainClassG` (`loop.go:742-755`) runs Class-G + blind retries only, NO §4a evidence fold, while `completeMissingSeatMintBindings` skips unresolved-marker pivots — a normal restart can serve while canonical bytes fold `unknown` though `realized_mint_ref` proves realization; F2 — `process` (`loop.go:154-163`) calls the signal-less `retryClassGIfDirty()` (`:704-708`, error discarded) then UNCONDITIONALLY `retryAllBlind()` — dirty Class-G does not gate blind Class-D work (the mint path's own `ClassGDirty()` check at `:425-431` shows the blind branch is the unguarded one). Your fold report (with its FOLD_SCOPE artifact BEFORE any edit) is the next relay; MERGE-GATE stays HELD.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s12-build
PARENT_DISPATCH_ID: s12-build-impl
RUN_ID: s12
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the correction runs under the standing token; the operator's terminal MERGE-GATE stays held and is not requested
FILED_AT_LOCAL: 20260818-021247
IN_REPLY_TO: master/relays/step3-h16-h26-lane/RECONCILE-orchestrator-planner-20260818-020315.md
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 f1de9e91e2c7d7c87faa471886d186295bbf7251acae54e98919c97e1ade9696
DESIGN_LOCK_ID: h16-outcome-split-design
BRANCH: s12-h16-fix
TARGET_BRANCH: main (operator-only merge; gate HELD)
FROM: s12.planner
TO: s12.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
BUNDLE_ID: m-7-conductor-core
SUBJECT: fold the VP's F1/F2 exactly as `014121` states them + the two named regressions + the uncontended full-suite pass + forward capture bindings; return the refreshed evidence set

## §1 — The two blockers (the VP's required corrections, quoted for convenience; `014121` governs)

**F1 (rev21 `:152-157`/`:84-87`/T4):** add the design's pre-serve EVIDENCE FOLD to the normal startup barrier — for a matching open non-blind mint attempt whose `realized_mint_ref` equals the pivot, commit exactly ONE validated `realized-undelivered` terminal, rebuild/publish the fold BEFORE serving, preserve the no-re-execution rule. Regression: a PROCESS-LEVEL crash/restart cut at `MintOrReplace`-persisted / cursor-advance-NOT-committed — assert normal startup writes one terminal, folds `failed`, never mints again, and opens NO channel before that proof completes.
**F2 (rev21 `:58-65`/`:39-44`/`:159-162`):** NO Class-D work — blind OR caller-present hooks, and no cursor advance — while the Class-G retry remains dirty; proceed only after a SUCCESSFUL Class-G pass. Regression: a persistently failing Class-G callback plus an open gate/approval item — assert zero hook invocations and zero cursor advances until Class-G succeeds, then ordered healing and idempotency.

## §2 — The evidential requirements (binding on the return)

1. **The UNCONTENDED full-suite pass:** the VP's three contended executions could not reproduce the green claim (the nested dogfood battery recursively runs `go test -p=1 ./...` and is contention-sensitive). Coordinate the quiet window with me before the run — I run NOTHING while you run the suite (say the word and I hold; I will not execute any test/build command in either worktree until you report the window closed) — and STATE the contention conditions inside the capture.
2. **Capture bindings, honored FORWARD:** every NEW capture records in its content the command, exit status, source identity (branch tip hash), and timestamp. Existing append-only captures stay exactly as filed — no rewrites.
3. **The return set (the VP's list, verbatim):** the new branch tip · the exact diff fence over `663e30d9…` · the two focused regressions · the uncontended full-suite result · the invariant result · clean status.

## §3 — Fences (unchanged)

Edits confined to `frank/` on branch `s12-h16-fix` in your worktree; straight-through; RED-first for both regressions; the ten INV-CATALOG laws green at every commit; byte-exact tokens per the plan; the frozen oracle, `master/exit-fixtures/`, S8 precedence, H-12 untouchable. If either fix surfaces a genuine rev21 design-byte defect: STOP, route up (the void rail) — never a silent reinterpretation. Your FOLD_SCOPE artifact precedes any edit per the protocol; expected surfaces are `internal/engine/loop.go`, `cmd/frank/main.go` (startup barrier seam), the new regression fixtures, and nothing else without a deviation relay.

ACTIONS_GIT_REF: docs-workspace disk action — this routing relay + one INDEX row; no `frank/` byte by this seat (the fold is yours). One hook-rejected draft of this same routing (`REVIEW-FOLD-planner-20260818-021138.md`, refused at write-time lint for phase/fold-scope shape, never indexed or relayed) was removed and refiled as this SITREP.
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
 M master/relays/INDEX.md
?? frank/.relays/s12/s12-build/SITREP-planner-20260818-021247.md
?? master/relays/step3-h16-h26-lane/RECONCILE-orchestrator-planner-20260818-020315.md
(literal, captured immediately before this file existed; the master rows are master's own banked/unbanked writes, untouched by this seat)
Next requested action: s12.implementer writes the FOLD_SCOPE, executes F1+F2 with their RED-first regressions, coordinates the quiet window, and returns the §2.3 evidence set TO s12.planner; I verify independently (in my own quiet window, after yours) and return the refreshed merge-decision packet to master for VP re-review. MERGE-GATE stays HELD.
