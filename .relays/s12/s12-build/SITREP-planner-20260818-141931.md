## SITREP — THE CORRECTED WHOLE RETURNED FOR VP RE-REVIEW: **m-3's ruling is executed in full and twice-verified — both flakes are CLOSED pre-merge with no documented-flake residue, the uncontended gate is GREEN in two independent quiet windows at the new tip `s12-h16-fix@1ac14a019f4122a7e43a7f2d17c465ba331e1985`, and the block branch did NOT fire** (the flake-2 mechanism is CONFIRMED `executor-timeout` — an `executor-*` load class, m-3's own hypothesis: the 1731ms sentinel doubled as the live subprocess budget and the 1.74s failure sat on it; no gate-path fault, no product-side race). The remedy commit is TEST-ONLY (six fixture files; zero product/policy/design bytes; `executor.go:116`'s 120s cap and the 150s context UNMOVED). F1/F2 stay closed. **The pair's recommendation stands: MERGE-READY.** Remaining: VP re-review → the operator's terminal MERGE-GATE.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s12-build
PARENT_DISPATCH_ID: s12-build-review-fold
RUN_ID: s12
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — on the VP's re-review pass, the lane's next transition is the operator-only MERGE-GATE (`HUMAN_MERGE_AUTHORIZATION` before any merge claim)
FILED_AT_LOCAL: 20260818-141931
IN_REPLY_TO: s12-build/REVIEW-FOLD-implementer-20260818-044833.md
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 f1de9e91e2c7d7c87faa471886d186295bbf7251acae54e98919c97e1ade9696
DESIGN_LOCK_ID: h16-outcome-split-design
BRANCH: s12-h16-fix
BASE: 663e30d99d61d89ec1958ba432389727bcf7c15f
TARGET_BRANCH: main (operator-only merge; gate HELD)
FROM: s12.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-3.planner, operator, s12.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: m-3's four conditions + the §3 diagnosis protocol executed and planner-verified; the return set complete; merge-ready re-recommended

## §1 — Remedy 1 executed under m-3's four conditions (each verified by me at the bytes)

(1) **The narrowing in the battery's own bytes, EXECUTABLE:** `test/fixtures/s12_nested_scope_test.go` — the authoritative 13-fixture outer-run-only registry with one reason per test, m-3's attestation sentence as a CHECKED CONSTANT (byte-compared in-test), and an assertion that every listed fixture calls the one shared `s12DogfoodNested()` decision (the wiring is test-enforced, not convention). (2) **The reachability argument ON the bytes:** the exit-gate test routes through the SAME decision and `s12AssertOuterDogfoodReachability(t)` immediately after it (`s8_exit_gate_test.go:124-127`) — reaching the outer body proves the variable was unset in the shared parent environment, licensing every same-guard fixture's outer execution; the argument is a checked non-empty constant. (3) **The margin re-measured in numbers** (§3 below). (4) **The anti-precedent carried:** option (b) expressly declined — the 120s executor cap (`executor.go:116`) and the 150s context are unmoved and this ruling is not citable for moving them.

## §2 — Remedy 2: the diagnosis confirmed m-3's hypothesis; the block branch did not fire

The implementer's bounded load reproduction (`batteries/implementer-flake-diagnosis-20260818-044741.txt`) reproduced the public `isolation-probe`/`observe-machinery-fault` shape and exposed `Timing:"timeout"` + `FailingDetail:"executor-timeout"` — an `executor-*` MachineryFault by construction, exactly m-3's sentinel-doubles-as-budget mechanism (the 1.74s failure on the 1731ms live budget). No gate-path (`gate.go:111`) fault and no product-side race was observed — the block branch was evaluated and NOT taken. Licensed test-only remedy applied: the sentinel is now the unique **17.311s** value; `timeoutSentinel.String()` remains in the forbidden-leak list (both sentinel properties preserved — verified at `s8_adversarial_test.go:42/:102`); the raw-verdict diagnostic is retained. No byte inside `internal/observe` or `internal/executor` moved.

## §3 — The return set (two independent verifiers; every capture forward-bound)

- **New tip:** `1ac14a019f4122a7e43a7f2d17c465ba331e1985` (`test: harden nested dogfood timing` — exactly six test-fixture files, +148/−4); worktree clean (verified).
- **Exact fence:** `663e30d9…..1ac14a01…` = 19 commits, 46 files, ZERO outside `frank/` (my own count); `git diff --binary` sha256 `f5242e9b…` (recomputed by me, matches the implementer's declaration).
- **Uncontended full gate, implementer:** `GOWORK=off go test ./... -count=1` exit 0, 169.85s, preflight none (`batteries/implementer-forward-verify-20260818-044741.txt`). **Planner, independent quiet window:** exit 0, 172.19s, fixtures 169.57s (`batteries/planner-verify2-full-uncontended-20260818-141516.txt`, sha256 `384b9218…`).
- **Nested leg + the condition-3 margins:** implementer 52.45s; **planner 50.72s** ⇒ headroom **99.28s vs the UNCHANGED 150s context** and **69.28s vs the UNCHANGED 120s cap** (`batteries/planner-verify2-nested-uncontended-20260818-141827.txt` — the capture carries the arithmetic; sha256 `1ee30389…`). Pre-remedy the same leg was 57.32s with near-zero total headroom; the restored margin is measured, not hoped.
- **Invariants:** ten INV-CATALOG laws PASS in both verifiers' runs.
- **Clean status:** the implementation worktree `git status --short` empty (verified).

## §4 — State

F1/F2: closed at `8ca99650…`, untouched by the remedy commit. Flakes 1+2: CLOSED pre-merge per m-3's ruling, no flake residue rides the merge. The operator's `194914` lint waiver stands (the two immutable r1 errors remain the only routed residue). NOT claimed: merge, push, PR, deploy; the live courier unmoved; H-12, the frozen oracle, and the four future-tense registry discharges stand. Remaining sequence: **VP re-review of this corrected whole → the operator's terminal MERGE-GATE.**

ACTIONS_GIT_REF: docs-workspace disk action — this return + one INDEX row + my two forward-bound capture files under `frank/.relays/s12/batteries/`; no `frank/` source/test byte by this seat; the remedy evidence rides branch `s12-h16-fix@1ac14a019f4122a7e43a7f2d17c465ba331e1985` (the implementer's commit).
FINAL_GIT_STATUS_SHORT:
 M frank/.relays/s12/INDEX.md
 M master/relays/CHECKPOINTS.md
 M master/relays/INDEX.md
?? frank/.relays/s12/batteries/planner-verify2-full-uncontended-20260818-141516.txt
?? frank/.relays/s12/batteries/planner-verify2-nested-uncontended-20260818-141827.txt
?? frank/.relays/s12/s12-build/SITREP-planner-20260818-141931.md
(literal, captured immediately before this file existed; the implementer's fold artifacts were banked by master's checkpoint cadence between filings; the master rows are master's own writes)
Next requested action: master routes the VP re-review over this corrected whole (m-3 CC'd per their ruling's return path); on its pass, the operator's terminal MERGE-GATE decides. The pair holds; no s12 seat merges.
