## PLAN — the s12 build plan **r3** (the RULINGS fold over r2; r2 `194700` and its re-review request are SUPERSEDED): both `194616` asks are owner-ruled and relayed by master `202541` — **Task 4/9's registry+carrier realization = m-2's `201520` §1 (1a–1d), realized byte-exactly; Task 10's byte = `mint-predecessor-mismatch` at rev21 `:89`** — and the spec of record REBINDS to **rev21 @ `cc8bcff3f4d04d45eb7cc4250714022f4050ab95bddae55f12fe17bdd7003d05`** (the void-rail one-byte-run §4a delta, pair-approved at 0 blocking `202647`; re-hashed from disk by this seat this act). ALL plan gates are CLEAR; the plan artifact revs in place to r3 @ `aebf1920…`. Routed TO s12.implementer for re-review. Before dispatch, TWO gates remain: your approve + the operator's `194914` waiver disposition. No dispatch token exists in this relay.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s12-build-plan-3
PARENT_DISPATCH_ID: step3-h16-h26-lane
RUN_ID: s12
CEREMONY_TIER: large
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — this transition (your re-review) needs no fresh operator decision; the operator's open items stand unchanged: the `194914` r1-waiver disposition (gates dispatch, not this review) and the terminal OPERATOR-ONLY MERGE-GATE
GRILL_REQUIRED: no — the design is FROZEN at rev21 (operator-grilled lineage carried; the rev21 delta is pair-approved); this plan executes it and changes no design byte
FILED_AT_LOCAL: 20260817-204945
ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — credential mint/quarantine/recovery and operator authority surfaces (rev21 §4a/§4c)
- migration/backfill/destructive-write/canonical-data-repair: yes — legacy pivot upgrade anchors, redo completeness proof, binding repair (§4a)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — canonical relay + credential truth are trust-critical state
- AI-or-automation-acts-downstream: yes — recovery/retry machinery performs automated canonical and binding actions
- worker/scheduler/queue/retry/async-side-effect: yes — Class-G/Class-D retries, scheduler arming, post-commit effects (§3/§6)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — fieldspec registry, Outcome wire shape, auth refusal contract are shared interfaces
- user-visible-control-with-materializer/downstream-consumer: yes — offline ceremony + state-only consumers have materialized downstream effects (§8)
- test-runtime-role-mismatch: no — real-process fixtures where role fidelity matters (locks, ceremony, raw-client legs)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: no — the two formerly unruled surfaces are now owner-ruled (m-2 `201520` §1; rev21 `:89`); no residual is accepted locally; the r1 lint residue is the operator's open `194914` decision, not a scope item
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no ceremony downgrade is requested; the commission's large tier stands undowngraded
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none
DESIGN_LOCK_ID: h16-outcome-split-design
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 aebf19203f330a8aa22d7cec23e6d5b2221b6a1907051049af0dfe37b148a421
BUNDLE_ID: m-7-conductor-core
OWNER: s12
REPO: frank/ (workspace subtree)
BASE: workspace main at worktree creation — Task 0 records the exact sha (main moves under master's checkpoint cadence; the r2 pin 51ddc6fb is superseded)
BRANCH: s12-h16-fix (worktree-hosted per Task 0)
TARGET_BRANCH: main (operator-only merge)
IN_REPLY_TO: master/relays/step3-h16-h26-lane/RECONCILE-orchestrator-planner-20260817-202541.md
FROM: s12.planner
TO: s12.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: plan r3 @ aebf1920… — the rulings folded byte-faithfully, the rev21 rebind executed, all task gates clear; re-review requested on the r3 delta

## §1 — The design binding (rev21)

The design lock identity is unchanged (`h16-outcome-split-design`); the digest floats forward to **rev21 @ `cc8bcff3…`** per the amendment rule, this relay acknowledging. The rev21 delta is the Ask-2 ruling made design-bytes: rev20 `:89`'s unnamed "with a typed class" now names **`mint-predecessor-mismatch`** (token count exactly 2 — the `:89` locus + the fold-log — m-2-verified and re-verified by this seat); everything else is byte-identical to the pair-approved rev20 (m-2's section split proves it; m-7.implementer's fresh rev21 review APPROVED at 0 blocking, `…202647`). I re-hashed rev21 from disk this act: exact. The isolated-lock header shape from r2 §1 carries unchanged (no in-root design phase; the external lineage is master-root-resident; routed lint target-clean).

## §2 — The r3 delta (everything else in r2 carries verbatim)

1. **The rev21 rebind:** the plan's spec paragraph, basis pins, Task 0 hash check (`cc8bcff3…`), token list (+ `mint-predecessor-mismatch`), rails, battery-map heading, and exit criteria all re-bound; a full reference sweep removed every stale rev20 binding (rev20 remains named only as the superseded predecessor).
2. **MUST-CHECK 1 → RULED, gate CLEAR:** Task 4 realizes m-2's `201520` §1a/§1b/§1d byte-exactly (four enum members spelled symbol-for-symbol; `seat_scope.operator` += `{mint-chain-anchor, attempt_resolution}`; `*` untouched; NO seat entry for the `derived-work-*` classes; the marker string + downgrade step stay this pair's per §1d). Task 9 realizes §1c: BOTH §5 operator verbs commit as `attempt_resolution`-class records, the §4b validator typing the verb BY THE RESOLVED TARGET'S TYPE (park ⇒ reopen, `resolves` ONLY; marker ⇒ disposition, `disposition` + `evidence_ref` REQUIRED; the `ParseSeatMintBody`-precedent required-member discipline; unresolvable `resolves` ⇒ `unknown-target`; NO new §4b class or byte). The authority cited in code/test comments is m-2's relay, not master's summary.
3. **MUST-CHECK 7 → RULED, gate CLEAR:** Task 10's RED leg asserts `mint-predecessor-mismatch` exactly, a fixed writer-stamped `failing_edge` value, NO enum registration (m-2's standing 8a ruling).
4. **BASE re-pinned** to worktree-creation time (Task 0 records the sha) — workspace `main` moves under master's checkpoint cadence, which also BANKS this lane's relay substrate on `main` (observed at `2cb9a0c`/`414ee38`); consequently **Task 16's substrate commit is REMOVED** (the branch carries code only; the r2 ROADMAP declaration is corrected to match). The end review reads the shared-tree relay root.
5. **Dispatch preconditions, restated:** your `approve` under `s12-build-plan-review-3` parented to THIS relay + `SCOPE_DIFF` all-in + the operator's `194914` waiver disposition realized. Nothing else is outstanding: both rulings are landed, rev21 is pair-approved, and every task gate is clear at plan grain.

## §3 — Scope for the TO addressee

Read-only PLAN-REVIEW of plan r3 @ `aebf19203f330a8aa22d7cec23e6d5b2221b6a1907051049af0dfe37b148a421` (path: `frank/docs/sprints/active/2026-08-17-s12-h16-fix/plans/PL-s12-build-plan-20260817.md`) — the r3 delta above is the review surface; your r2 fold surface (the five F-folds) carries into it unchanged except as §2 names. Verify the ruling fidelity yourself at the authorities' bytes (m-2 `201520` §1; rev21 `:89` at the verified hash; master `202541`). Return `PLAN_REVIEW_VERDICT` TO s12.planner under `s12-build-plan-review-3`, parented to THIS relay. No dispatch token exists in this relay or anywhere in this root.

ACTIONS_GIT_REF: docs-workspace disk action — this r3 PLAN relay + one INDEX row + the in-place plan-doc r3 fold + the ROADMAP rev21/status updates (uncommitted by this seat; master's checkpoints bank the substrate); no `frank/` source/test byte, no branch, no commit.
FINAL_GIT_STATUS_SHORT:
 M master/domains/m-2-forms-determinism/README.md
 M master/relays/CHECKPOINTS.md
 M frank/.relays/s12/INDEX.md
 M frank/docs/sprints/active/2026-08-17-s12-h16-fix/ROADMAP.md
 M frank/docs/sprints/active/2026-08-17-s12-h16-fix/plans/PL-s12-build-plan-20260817.md
?? frank/.relays/s12/s12-build/PLAN-planner-20260817-204945.md
(the two `master/` modifications are foreign concurrent writes present before this act, untouched by this seat; the s12 modifications + the new relay are exactly this act's writes over the master-banked baseline)
Next requested action: s12.implementer re-reviews r3 under `s12-build-plan-review-3` TO s12.planner. On approve I run SCOPE_DIFF; the `DISPATCH IMPL` then waits only on the operator's `194914` waiver disposition. On must-revise, an r4 follows. No self-advance.
