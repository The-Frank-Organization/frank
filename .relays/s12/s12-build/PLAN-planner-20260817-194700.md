## PLAN — the s12 build plan **r2** (the bounded F1–F5 fold over r1, per your must-revise `192631`; r1 and its relay `191828` are SUPERSEDED): the plan artifact is revved in place to r2 @ sha256 `103fce4e…`, the active-root lint shape is corrected and PROVEN routed-clean, `type: string` is restored literal, Task 4 gains the live version-gate consumers, the F2/F4 route-ups are FILED to master in this same act (Tasks 4, 7–15 gate on ruling 1; Task 10 on ruling 2), and Task 0 moves to the mandatory isolated-worktree procedure. Routed TO s12.implementer for re-review. No dispatch token exists in this relay.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s12-build-plan-2
PARENT_DISPATCH_ID: step3-h16-h26-lane
RUN_ID: s12
CEREMONY_TIER: large
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — this transition (your re-review) needs no fresh operator decision; the standing terminal gate is unchanged: merge is OPERATOR-ONLY via a future `PHASE: MERGE-GATE` relay carrying `HUMAN_MERGE_AUTHORIZATION`
GRILL_REQUIRED: no — the design was operator-grilled through its own r16 cycle and is FROZEN at rev20; this plan executes it and changes no design byte
FILED_AT_LOCAL: 20260817-194700
ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — credential mint/quarantine/recovery and operator authority surfaces (rev20 §4a/§4c)
- migration/backfill/destructive-write/canonical-data-repair: yes — legacy pivot upgrade anchors, redo completeness proof, binding repair (§4a)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — canonical relay + credential truth are trust-critical state
- AI-or-automation-acts-downstream: yes — recovery/retry machinery performs automated canonical and binding actions
- worker/scheduler/queue/retry/async-side-effect: yes — Class-G/Class-D retries, scheduler arming, post-commit effects (§3/§6)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — fieldspec registry, Outcome wire shape, auth refusal contract are shared interfaces
- user-visible-control-with-materializer/downstream-consumer: yes — offline ceremony + state-only consumers have materialized downstream effects (§8)
- test-runtime-role-mismatch: no — real-process fixtures where role fidelity matters (locks, ceremony, raw-client legs)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — two surfaces are unruled and HELD on master (the F2/F4 route-ups); no residual is accepted locally
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no ceremony downgrade is requested; the commission's large tier stands undowngraded
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none
DESIGN_LOCK_ID: h16-outcome-split-design
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 103fce4e6c9380c6a9dd557f2644e2c1dbb7aaafa5b759843ecdd33f886ea509
BUNDLE_ID: m-7-conductor-core
OWNER: s12
REPO: frank/ (workspace subtree)
BASE: main@51ddc6fb4f0ff4395c1629d6f3492639d4ca827c
BRANCH: s12-h16-fix (worktree-hosted per Task 0)
TARGET_BRANCH: main (operator-only merge)
IN_REPLY_TO: s12-build/PLAN-REVIEW-implementer-20260817-192631.md
FROM: s12.planner
TO: s12.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: plan r2 @ 103fce4e… — your five blockers folded exactly as found or routed exactly as required; re-review requested on the fold surface

## §1 — The design binding, stated plainly (the F1 fold)

The design lock is `h16-outcome-split-design` = m-7's FROZEN rev20 at `master/domains/m-7-conductor-core/design/2026-07-20-h16-outcome-split.md`, sha256 `e09fab09cdcc2a6025fd42f06b02e573a3e091542f9eb44cebe42a7fa5ff7de9`, re-verified from disk again this act. Its approval lineage is MASTER-ROOT-RESIDENT: the m-7 pair approve (`step3-h16-h26-lane/DESIGN-REVIEW-implementer-20260817-125908.md`, approve, zero findings at this hash), the m-1 confirm (`…132139`), the m-2 confirm chain (`133642`→`140244`, pair-closed), the Master+VP stage-3 join, and the commissioning PLAN `…184653` (this relay's parent edge) that binds it to this pair under the `DD-v29-master-authority-20260809` gate. This root deliberately carries NO in-root design phase and NO `DESIGN_RECORD_KIND` claim: no same-owner design record exists here to describe, s12 may not proxy-file m-7's relays, may not author a DESIGN over a frozen spec, and may not self-declare an override — so the header carries the bare lock identity (the protocol's isolated-lock reading) and this section carries the true external lineage. Verified: routed lint over this root (`--relay-root frank/.relays/s12`) reports ZERO errors on this shape — both F1 errors are gone (the annotated lock value that broke doc-id matching is also corrected; the design hash lives here and in the plan header, where the void rail already binds it). If you read the isolated-lock shape as insufficient for this topology, that is a kit/topology question for master — say so and I escalate it; I do not dispatch over your objection.

## §2 — The fold map (each finding → its exact fold)

1. **F1 → folded + proven.** Header: bare `DESIGN_LOCK_ID`; no record-kind claim; `PLAN_ARTIFACT`/`PLAN_SHA256` replaced by the protocol-blessed annotated `PLAN_LOCK_ID` (identity + integrity in the lock value; location: `frank/docs/sprints/active/2026-08-17-s12-h16-fix/plans/PL-s12-build-plan-20260817.md`, revved in place to r2 — the r1 digest binding floats forward via this acknowledging relay, per the amendment rule). The in-root `plans/` locus was tested and REJECTED for cause: any artifact inside the relay root is scanned as a relay and header-fails; the probe-3 locus (`frank/plans/`) would put a plan doc at the frank subtree top level. Routed lint: clean (§1).
2. **F2 → contested pins retracted + routed UP.** `type: string` restored LITERAL (your live-row evidence verified: `registry.json:161-173`); the four-vs-two token question is NOT locally decided — the route-up is FILED as `s12-build/SITREP-planner-20260817-194616.md` Ask 1 (TO master, CC m-2.planner/m-7.planner). Plan Tasks 4 and 7–15 GATE on the ruling; MUST-CHECK 1 records the default reading as input to master, executed only as ruled.
3. **F3 → folded whole.** Verified at the bytes (`config.go:317-328` v8-ceiling reader marker; `:425-436` transition allowlist ending v7→v8; `genesis.go:28-51` marker-keyed downgrade steps; `registry_test.go:15-28` exact version+provenance assertions). Task 4 now owns: the v9 reader-marker acceptance, the `v8 → v9` transition pair, the new genesis v9 downgrade step (exact strings incl. the post-ruling enum line, `preserveSource` mirrored), the fresh-v9-init proof battery, historical v8 fixture preservation against pinned bytes, and the EXACT pinned provenance map (flat string map with the `"supersedes": "s10-fieldspec-v8"` successor-relation entry). The file structure and battery map carry the additions.
4. **F4 → routed UP + Task 10 held.** The predecessor-mismatch byte is the same SITREP's Ask 2 (m-7/m-2 disposition via master); MUST-CHECK 7 records the hold; Task 10's gate line forbids any locally chosen byte and binds the RED leg to the ruled token.
5. **F5 → folded whole.** `HUMAN_GATE_REQUIRED: no` with the terminal gate in prose (above); the trigger-present scan carried WITHOUT any downgrade claim (the large tier stands); the plan-level boundary contract added (plan §Boundary contract: Writes/Reads/Target entity/Downstream consumer/Contract/Proof/No-consumer action); Task 0 now invokes `superpowers:using-git-worktrees` — the worktree hosts branch `s12-h16-fix`, the shared tree stays on `main`, captures/relays write to the shared-tree relay root by absolute path, and the substrate commit moves to Task 16 (ROADMAP declarations updated to match).

## §3 — Scope for the TO addressee + the standing conditions

Read-only PLAN-REVIEW of plan r2 @ `103fce4e6c9380c6a9dd557f2644e2c1dbb7aaafa5b759843ecdd33f886ea509` — per your own Next-requested-action, the fold surface: the five folds above, the reshaped MUST-CHECK register (pins 1 and 7 = holds; 2 and 3 = corrected/expanded; 4–6 your confirms, carried), and the r2 header shape. All r1 content not named in the fold map carries forward verbatim (task decomposition, battery map, global constraints, exit criteria + the two r2 additions: ruling-gate compliance and routed-lint-clean-at-dispatch-and-end-review). Return `PLAN_REVIEW_VERDICT` TO s12.planner under `s12-build-plan-review-2`, parented to THIS relay. Delegated dispatch conditions unchanged from the commission: your approve + `SCOPE_DIFF` all-in + no hard trigger beyond the commission's own weighed surface; dispatch reaches ONLY the unblocked tasks until the two rulings land (the gates are in the plan bytes). No dispatch token exists in this relay or anywhere in this root.

ACTIONS_GIT_REF: docs-workspace disk action — this r2 PLAN relay + the companion SITREP `194616` + two INDEX rows + the in-place plan-doc r2 fold + the ROADMAP update (all untracked; substrate commits at Task 16); no `frank/` source/test byte, no branch, no commit; sandbox lint experiments confined to /tmp.
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s12/
?? frank/docs/sprints/active/
Next requested action: s12.implementer re-reviews the fold surface and returns the verdict under `s12-build-plan-review-2` TO s12.planner. On approve, I run SCOPE_DIFF and dispatch the unblocked surface (Tasks 0–3, 5, 6 buildable pre-ruling; the gated tasks hold); on must-revise, an r3 follows. Master's two rulings land independently via the companion SITREP. No self-advance.
