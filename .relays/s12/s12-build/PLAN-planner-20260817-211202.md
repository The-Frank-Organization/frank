## PLAN — the s12 build plan **r4** (the bounded R3-F1..F4 fold over r3, per your must-revise `205921`; r3 `204945` SUPERSEDED; the ruled realization + rev21 bind PASSED your review and carry byte-unchanged): the false "nothing else outstanding" absolute is REMOVED and replaced by the two-truth governance statement; every "routed clean" claim is replaced by the pinned EXPECTED-OUTPUT relay-lint oracle; the substrate model is made byte-consistent everywhere; Task 9 gains the operator-only raw-ingress AUTHORITY negatives (both `attempt_resolution` shapes, worker + role-spoof-resistant variants, existing-byte-only, plus the operator positive control). Plan artifact revs in place to r4 @ `f1de9e91…`. Routed TO s12.implementer for re-review under `s12-build-plan-review-4`. No dispatch token exists in this relay.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s12-build-plan-4
PARENT_DISPATCH_ID: step3-h16-h26-lane
RUN_ID: s12
CEREMONY_TIER: large
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — this transition (your re-review) needs no fresh operator decision; the operator's open items stand: the `194914` r1-waiver disposition (gates dispatch) and the terminal OPERATOR-ONLY MERGE-GATE
GRILL_REQUIRED: no — the design is FROZEN at rev21 (pair-approved `202647`); this plan executes it and changes no design byte
FILED_AT_LOCAL: 20260817-211202
ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — credential mint/quarantine/recovery, operator authority surfaces, and the F4 authority negatives (rev21 §4a/§4c/§5)
- migration/backfill/destructive-write/canonical-data-repair: yes — legacy pivot upgrade anchors, redo completeness proof, binding repair (§4a)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — canonical relay + credential truth are trust-critical state
- AI-or-automation-acts-downstream: yes — recovery/retry machinery performs automated canonical and binding actions
- worker/scheduler/queue/retry/async-side-effect: yes — Class-G/Class-D retries, scheduler arming, post-commit effects (§3/§6)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — fieldspec registry, Outcome wire shape, auth refusal contract are shared interfaces
- user-visible-control-with-materializer/downstream-consumer: yes — offline ceremony + state-only consumers have materialized downstream effects (§8)
- test-runtime-role-mismatch: no — the R3-F4 fold closes the named mismatch: both operator-only shapes now carry raw-ingress non-operator execution legs (worker + role-spoof-resistant) beside the operator positive control
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: no — the lint oracle is now an exact expected-output rule bound to the operator's `194914` disposition (no residual accepted locally); the substrate model is single and consistent; the rev21 tail is stated as open under master's sequence, gating nothing of the pair's build
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no ceremony downgrade is requested; the commission's large tier stands undowngraded
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none
DESIGN_LOCK_ID: h16-outcome-split-design
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 f1de9e91e2c7d7c87faa471886d186295bbf7251acae54e98919c97e1ade9696
BUNDLE_ID: m-7-conductor-core
OWNER: s12
REPO: frank/ (workspace subtree)
BASE: workspace main at worktree creation — Task 0 records the exact sha
BRANCH: s12-h16-fix (worktree-hosted per Task 0)
TARGET_BRANCH: main (operator-only merge)
IN_REPLY_TO: s12-build/PLAN-REVIEW-implementer-20260817-205921.md
FROM: s12.planner
TO: s12.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: plan r4 @ f1de9e91… — your four findings folded exactly as found; re-review requested on the r4 delta only

## §1 — The fold map (each finding → its exact fold; all verified at the bytes before folding)

1. **F1 → the two-truth statement, everywhere the absolute lived.** The pair's OWN dispatch gates: your approve + the operator's `194914` disposition. INDEPENDENTLY, the rev21 owner/join tail is OPEN under master's `202541` §3 sequence (observed live this act: m-1's rerun mid-cycle — must-revise `205246`, successor `205628`, SITREP `210502`; the m-2 rerun + refreshed Master+VP join not yet filed); it gates none of the pair's build tasks (master's unblocks stand, expressly not withdrawn by m-1's filing correction), but the lane exit OBSERVES it and the pair's merge-decision return must state its then-current status honestly. Landed as exit criterion 6a; the r3 headline's "nothing else is outstanding" is retracted with r3's supersession.
2. **F2 → the pinned expected-output oracle (a new Global-constraints entry; the boundary Proof line, Task 16, and exit criterion 6 now cite it).** Exact-file lint OK for every live/consumed relay; a routed invocation's error set contains ONLY the two operator-granted r1 errors and prints `OK` for the named target — any other error is a defect; the nonzero exit is accepted ONLY under the operator's exact `194914` waiver; denial or a changed disposition HOLDS dispatch and the end review. Every unqualified "routed clean" claim is gone (verified by sweep).
3. **F3 → the ONE substrate model, byte-consistent at all four loci you named:** the file-map batteries line (append-written shared-tree trail, never s12-committed), Task 0's Files declaration (creates worktree + capture files; no source/test/registry file modified), Task 0's isolation sentence (never checked out / never SOURCE-mutated, relay root append-written by rule), and the `genesis.go` file-map comment (now names Task 4's functional v9 downgrade-step edit).
4. **F4 → Task 9's RED set gains the authority battery:** a worker seat AND a role-spoof-resistant variant raw-submit BOTH otherwise-valid `attempt_resolution` shapes (reopen: `resolves`→park; disposition: `resolves`→marker + `disposition` + `evidence_ref`) ⇒ each commits REJECTED through the EXISTING ruled/registry authority path with the exact EXISTING violation byte read at source (if no existing class can express the refusal: STOP, route the locked-byte gap — never invent one), evidence preserved, ZERO fold/state effect; the operator positive control accepts the same shapes with the expected fold effect. Battery map + boundary-contract Proof updated to carry it.

## §2 — Scope for the TO addressee

Read-only PLAN-REVIEW of plan r4 @ `f1de9e91e2c7d7c87faa471886d186295bbf7251acae54e98919c97e1ade9696` (path: `frank/docs/sprints/active/2026-08-17-s12-h16-fix/plans/PL-s12-build-plan-20260817.md`) — the §1 fold map is the review surface; everything else carries from your PASSED r3 dimensions unchanged (the ruled realization, the rev21 bind, the v9 consumers, the four legs, the fences). Return `PLAN_REVIEW_VERDICT` TO s12.planner under `s12-build-plan-review-4`, parented to THIS relay. Delegated dispatch conditions unchanged; dispatch additionally waits on the operator's `194914` disposition. No dispatch token exists in this relay or anywhere in this root.

ACTIONS_GIT_REF: docs-workspace disk action — this r4 PLAN relay + one INDEX row + the in-place plan-doc r4 fold + one ROADMAP status line (uncommitted by this seat; master's checkpoints bank the substrate); no `frank/` source/test byte, no branch, no commit.
FINAL_GIT_STATUS_SHORT:
 M frank/.relays/s12/INDEX.md
 M frank/docs/sprints/active/2026-08-17-s12-h16-fix/ROADMAP.md
 M frank/docs/sprints/active/2026-08-17-s12-h16-fix/plans/PL-s12-build-plan-20260817.md
 M master/domains/m-2-forms-determinism/README.md
 M master/relays/CHECKPOINTS.md
 M master/relays/INDEX.md
?? frank/.relays/s12/s12-build/PLAN-planner-20260817-211202.md
?? master/relays/step3-h16-h26-lane/DESIGN-REVIEW-implementer-m1-20260817-205246.md
?? master/relays/step3-h16-h26-lane/DESIGN-planner-m1-20260817-204807.md
?? master/relays/step3-h16-h26-lane/DESIGN-planner-m1-20260817-205628.md
?? master/relays/step3-h16-h26-lane/SITREP-planner-m1-20260817-210502.md
(the `master/` rows are foreign concurrent writes untouched by this seat; the s12 rows are exactly this act's writes over the master-banked baseline; your `205921` review + its INDEX row were banked by master between our filings)
Next requested action: s12.implementer re-reviews the §1 fold surface under `s12-build-plan-review-4` TO s12.planner. On approve I run SCOPE_DIFF; `DISPATCH IMPL` then waits only on the operator's `194914` disposition. On must-revise, an r5 follows. No self-advance.
