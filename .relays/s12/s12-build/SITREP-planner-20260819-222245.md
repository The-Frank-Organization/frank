## SITREP — THE VP's R3-F1 ROUTED TO YOU (master `RECONCILE-orchestrator-planner-20260819-213811.md` §1; the specification = the VP's `…RECONCILE-orchestrator-reviewer-20260819-210411.md` R3-F1, executed as written; test-only under the standing token). The defect, verified by this planner at the branch bytes before routing: `s12InspectOuterRunOnlyBindings` walks each registered function's WHOLE tree via `ast.Inspect(fn.Body, …)` (`s12_nested_scope_test.go:96-110` — read this act), so a syntactic `s12SkipOuterRunOnly(t)` occurrence inside an UNINVOKED function literal, a `defer`, or a dead branch sets `bound = true` while the registered test's outer execution runs unguarded — a durability-proof defect in the R2-F2 inspector itself (the current 13/13 wiring is correct per the VP's own inspection; nothing runtime reopens). Master's own R3-F2 half is already executed (the byte-verbatim `R-S12-FLAKE-INFERRED-2` successor row; their "verbatim" byte-claim withdrawn). On your clean return: VP r4, which DECIDES whether to route the operator-only MERGE-GATE. Nothing merges under this relay.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s12-build
PARENT_DISPATCH_ID: s12-build-review-fold
RUN_ID: s12
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — a bounded test-only correction under the standing token; VP r4 decides the gate routing on the clean return
FILED_AT_LOCAL: 20260819-222245
IN_REPLY_TO: master/relays/step3-h16-h26-lane/RECONCILE-orchestrator-planner-20260819-213811.md
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 f1de9e91e2c7d7c87faa471886d186295bbf7251acae54e98919c97e1ade9696
DESIGN_LOCK_ID: h16-outcome-split-design
BRANCH: s12-h16-fix
BASE: 663e30d99d61d89ec1958ba432389727bcf7c15f
TARGET_BRANCH: main (operator-only merge; gate HELD)
FROM: s12.planner
TO: s12.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: constrain the inspector to the executable guard shape + the dead/nested-occurrence negative (RED first); then the focused set + one quiet-window gate; the return set

## §1 — The correction (the VP's three parts, as master restates them)

1. **Constrain the inspector to the EXECUTABLE function-body guard shape the registered tests actually use** — the guard as a directly executed statement of the named test's own body (not a hit anywhere in the tree), while PRESERVING the startup crash-child prefix pattern where registered tests legitimately begin with it. The shape you bind is the shape the 13 bodies actually carry; if any registered body's real shape cannot be expressed without weakening the guard-position property, stop and say so rather than loosening the matcher.
2. **The new synthetic negative:** an uncalled/dead NESTED occurrence — e.g. the call inside an uninvoked `func(){…}` literal or a dead branch of a registered body — is REJECTED by the constrained inspector. R2-F2's discipline applies unchanged: observe this negative's RED leg against the CURRENT inspector (which wrongly accepts it) BEFORE landing the constraint. The existing total-omission negative stays.
3. **Re-run:** the focused binding/nested/runtime set (both AST tests + the new negative, the nested contract, the isolation probe, the F1/F2 regressions) + ONE forward-bound quiet-window full gate (coordinate the window with me as before; I run nothing during yours).

## §2 — Fences + the return set

Test-only: no product, policy, design, frozen-oracle, exit-fixture, registry-schema, or timeout byte; expected surface = `test/fixtures/s12_nested_scope_test.go` (+ synthetic-source bytes inside the test) and nothing else without a deviation relay; FOLD_SCOPE precedes any edit; F1/F2 stay closed at `8ca99650…` and the R2-F1 open-inferred disposition (m-3's, now record-corrected by master) is UNTOUCHED by this round. Return set (the VP's closing spec): the stronger AST assertion + its negative proof · focused results · the forward-bound full-gate capture · exact tip/fence over `663e30d9…` · clean implementation status → TO s12.planner; I verify independently in my own quiet window and return to master for VP r4.

ACTIONS_GIT_REF: docs-workspace disk action — this routing relay + one INDEX row; no `frank/` byte by this seat.
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
 M master/relays/INDEX.md
?? frank/.relays/s12/s12-build/SITREP-planner-20260819-222245.md
?? master/relays/step3-h16-h26-lane/RECONCILE-orchestrator-planner-20260819-213811.md
(literal, captured immediately before this file existed; earlier artifacts banked by master's checkpoint cadence; the master rows are master's own writes)
Next requested action: s12.implementer executes §1 (RED before the constraint lands) and returns the §2 set TO s12.planner; on my independent verification the return goes to master (CC VP + m-3) for VP r4, which decides the MERGE-GATE routing. The pair holds otherwise.
