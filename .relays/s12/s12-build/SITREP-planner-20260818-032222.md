## SITREP — THE s12 CORRECTION RETURN, honest on every item: **the VP's F1 and F2 are CLOSED and planner-verified at `s12-h16-fix@8ca99650019f2ddd12d47237e1abb32fbd5895e8`** (both fixes read at the bytes; both focused regressions PASS in my own runs; the ten INV-CATALOG laws PASS; the fence exact: 18 commits, 43 paths, all `frank/`; worktree clean) — **but the required "uncontended full-suite pass" CANNOT be honestly certified as a STABLE property at this branch, and the pair routes THAT item UP with a measured diagnosis instead of farming a green.** The implementer's uncontended green (`review-fold-full-green-uncontended-20260818-022414.txt`, bound to the tip, contention stated) is genuine; my two subsequent uncontended planner runs each failed ONCE, in two DIFFERENT S8 executor-family tests, and my measurements show why: **s12's suite growth consumed the dogfood battery's timing margin** (base: the whole test = 121.5s against its fixed 150s context — 28.5s headroom pre-s12; tip: each nested `go test -p=1 ./...` leg = 57.3s warm ×2 + binary build + init/submits ⇒ near-zero headroom, cold-cache or ambient load exceeds it), and the executor's PRODUCTION policy hard-caps suite budgets at 120s (`internal/executor/executor.go:116`) so no config-only budget raise exists. The second flake (`TestS8AdversarialExecutorIsolationAndVerdictIPHIntegrated`, one `observe-machinery-fault` in a full-suite run) is LOAD-COUPLED: 5/5 green focused at tip, green at base. No product-code defect was found in either flake. MERGE-GATE stays HELD; the remedy is not the pair's to choose.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s12-build
PARENT_DISPATCH_ID: s12-build-review-fold
RUN_ID: s12
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — master routes the flake-remedy ruling (the operator's terminal MERGE-GATE stays held and is not requested here)
FILED_AT_LOCAL: 20260818-032222
IN_REPLY_TO: s12-build/REVIEW-FOLD-implementer-20260818-022818.md
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 f1de9e91e2c7d7c87faa471886d186295bbf7251acae54e98919c97e1ade9696
DESIGN_LOCK_ID: h16-outcome-split-design
BRANCH: s12-h16-fix
BASE: 663e30d99d61d89ec1958ba432389727bcf7c15f
TARGET_BRANCH: main (operator-only merge; gate HELD)
FROM: s12.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s12.implementer, m-3.planner, m-7.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: F1/F2 closed and twice-verified; the uncontended-green item routed up with measurements — the dogfood battery's margin is structurally consumed and the executor's 120s cap is a locked production surface

## §1 — The VP's required return set (each item, with my independent verification)

1. **The new branch tip:** `8ca99650019f2ddd12d47237e1abb32fbd5895e8` (the bounded five-file correction commit, +310/−3, over the prior tip; worktree `git status --short` clean — verified by me).
2. **The exact diff fence:** `663e30d9…..8ca99650…` = 18 commits, 43 changed paths, ZERO outside `frank/` (my own `git diff --name-only` count); `git diff --check` silent.
3. **The two focused regressions, planner-run:** `TestH16StartupFoldsRealizedMintEvidenceBeforeServing` PASS (0.71s) · `TestH16DirtyClassGBlocksBlindClassDUntilSuccessfulPass` PASS (0.10s). The fixes verified at the bytes: `DrainClassG` now runs `foldRealizedMintEvidence()` after the successful Class-G pass and before `retryAllBlind` (deterministic sort, evidence-gated, one validated `realized-undelivered` per matching open attempt, committed through the ordinary derived writer pre-serve); `retryClassGIfDirty()` returns `!ClassGDirty()` and `process` gates `retryAllBlind()` on it — no blind Class-D work while dirty; caller-present retains its own check.
4. **The invariant result, planner-run:** the ten INV-CATALOG laws PASS (`go test ./test/invariants -run '^TestLaw' -count=1`).
5. **The uncontended full-suite result — REPORTED WHOLE, not summarized to a verdict:** the implementer's pass (`022414` capture: quiet window stated, preflight none, tip-bound, exit 0, fixtures 184.6s) is genuine. My two uncontended verification runs (captures with forward bindings: `planner-verify-full-green-uncontended-20260818-030628.txt` sha256 `3e112ea4…`; `planner-verify-full-rerun-uncontended-20260818-031506.txt` sha256 `e837393c…`) each failed once: run A — `TestS8ProductionDogfoodRejectsFalseDoneAndNamesPredicate` at its 150s context (`production false suite submit: context deadline exceeded`); run B — exit-gate PASSED, `TestS8AdversarialExecutorIsolationAndVerdictIPHIntegrated` failed once (`observe-machinery-fault`, 1.74s, not a timeout). Every capture states its contention conditions.

## §2 — The diagnosis (all numbers measured this act; no product-code defect found)

**Flake 1 — the dogfood battery's margin is structurally consumed.** The test (`s8_exit_gate_test.go:129`) wraps ONE fixed 150s context around: a full `frank` binary build + init/serve + several submits + TWO nested executor suite runs (`scripts/dogfood-suite.sh` = `FRANK_DOGFOOD_NESTED=1 exec go test -p=1 ./... -count=1`, each under the executor's 120s budget). Measured: at BASE the entire test = **121.54s** (headroom 28.5s); at TIP each nested leg = **57.32s warm-cache** (×2 = 114.6s before build/init/submits). s12's ~6k added test lines (correct, required batteries) grew the nested suite so the test now fits ONLY with a warm build cache on an idle machine — my run A followed the fold commit (cold rebuild inside the nest) and exceeded the deadline; the VP's three executions hit the same wall. **The executor's policy cap is production code:** `suite.Timeout > 120s ⇒ executor-timeout-policy-refused` (`executor.go:116`) — raising budgets is a locked-surface decision, not a fixture tweak.
**Flake 2 — load-coupled, tip-only-observed, not deterministic:** the adversarial isolation probe faulted once during a full parallel-package run at tip; 5/5 PASS focused at tip; PASS at base. Consistent with executor subprocess spawn sensitivity under the heavier tip suite's parallel load; needs its own bounded diagnosis if master wants it pinned.

## §3 — The disposition options (master/VP rule; the pair executes)

(a) **Guard the heavyweight process-level batteries from the NESTED run** (extend the existing `FRANK_DOGFOOD_NESTED` early-return pattern — the exit-gate test already uses it — to the slow s12 crash-cut/ceremony fixtures): test-only bytes, restores the margin, but NARROWS what "dogfood-battery green" attests — an S8 evidence-battery semantics change the pair may not self-rule. (b) **Raise the executor suite-budget cap + the test context:** touches the locked production policy surface (`executor.go:116`, m-3-adjacent) — rail (d), owning pair + master. (c) **De-couple the two nested legs into separate contexts / pre-built binary reuse:** test-infrastructure rework, larger. (d) **Accept-and-document the margin flake:** priced against the team's flakiness standard — the pair recommends AGAINST. The pair recommends (a) as smallest-honest IF the owners accept the attestation narrowing, with the narrowing stated in the battery's own bytes; we hold for the ruling either way.

## §4 — State

F1/F2: CLOSED at `8ca99650…`, twice-verified, regressions green. The lane's remaining open items: this flake ruling → the refreshed VP re-review → the operator's terminal MERGE-GATE. H-12, the frozen oracle, the four future-tense registry discharges, and the held gate all stand. The pair holds; nothing further moves without the ruling.

ACTIONS_GIT_REF: docs-workspace disk action — this return + one INDEX row + my two capture files under `frank/.relays/s12/batteries/` (forward-bound: command, exit, tip, timestamp, contention in content); one throwaway detached read-only worktree at the BASE commit created and removed for the §2 base measurements; no `frank/` source/test byte by this seat, no branch/commit/merge.
FINAL_GIT_STATUS_SHORT:
 M frank/.relays/s12/INDEX.md
 M master/relays/CHECKPOINTS.md
 M master/relays/INDEX.md
?? frank/.relays/s12/batteries/planner-verify-full-green-uncontended-20260818-030628.txt
?? frank/.relays/s12/batteries/planner-verify-full-rerun-uncontended-20260818-031506.txt
?? frank/.relays/s12/s12-build/SITREP-planner-20260818-032222.md
(literal, captured immediately before this file existed; the earlier fold-return artifacts were banked by master's checkpoint cadence between filings; the master rows are master's own writes)
Next requested action: master (with the VP, and m-3 as the executor/evidence-machinery owner) rules the §3 disposition; on the ruling the pair executes the bounded remedy under the standing token and re-runs the uncontended gate; then VP re-review → the operator's MERGE-GATE. The pair holds.
