## SITREP — THE m-3 FLAKE RULING ROUTED TO YOU under the standing token (master `RECONCILE-orchestrator-planner-20260818-042202.md` adopts it whole): **the specification is m-3's `master/relays/step3-h16-h26-lane/DESIGN-planner-m3-20260818-040900.md` — execute its §2 (four bound conditions) and §3 (the pinned diagnosis protocol) AS WRITTEN.** TEST-ONLY bytes are licensed; NO product byte, NO policy value (`executor.go:116`'s 120s cap and the 150s test context are EXPRESSLY unmoved and this ruling is anti-precedent for moving them), no design lock; F1/F2 stay closed at `8ca99650…`. **The BLOCK branch is real: if the flake-2 diagnosis shows the fault fired WITHOUT any `executor-*` detail — from the gate's own paths (e.g. `gate.go:111` write-allowlist machinery) or any product-side race — STOP: the merge blocks, the finding returns to master + m-3.planner, and nothing else in this order proceeds.** Routing note from my own capture: `planner-verify-full-rerun-uncontended-20260818-031506.txt` does NOT name the `executor-*` detail (zero matches), so §3's bounded load reproduction is needed to identify it. MERGE-GATE stays HELD.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s12-build
PARENT_DISPATCH_ID: s12-build-review-fold
RUN_ID: s12
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the remedies run under the standing token; the operator's terminal MERGE-GATE stays held and is not requested
FILED_AT_LOCAL: 20260818-042436
IN_REPLY_TO: master/relays/step3-h16-h26-lane/RECONCILE-orchestrator-planner-20260818-042202.md
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 f1de9e91e2c7d7c87faa471886d186295bbf7251acae54e98919c97e1ade9696
DESIGN_LOCK_ID: h16-outcome-split-design
BRANCH: s12-h16-fix
TARGET_BRANCH: main (operator-only merge; gate HELD)
FROM: s12.planner
TO: s12.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner
BUNDLE_ID: m-7-conductor-core
SUBJECT: execute m-3's §2 option-(a) under its four conditions + the §3 diagnosis-then-licensed-remedy; then the uncontended gate re-run; the return set below

## §1 — Remedy 1 (flake 1; m-3 §2, master §1.1 — conditions 1–3 realized IN THE BYTES)

Extend the existing `FRANK_DOGFOOD_NESTED` early-return pattern to the heavyweight process-level s12 batteries (the crash-cut/ceremony/startup fixtures whose re-execution inside the nested workload re-proves nothing about the gate). In the fixture source, ONE authoritative narrowing note carrying: (i) the enumerated outer-run-only list with one line of WHY each; (ii) m-3's exact attestation sentence — "nested-green attests the courier's gate adjudication over the nested suite; the process-level crash/ceremony cuts are proven ONLY by the outer run."; (iii) the reachability argument ON the bytes — the exit-gate test carries the SAME guard, so the exit-gate body having executed proves `FRANK_DOGFOOD_NESTED` was unset in the outer invocation's shared parent environment, which licenses every same-guard fixture's execution (never leave the guard's own reachability implicit). Condition 3: the post-remedy forward-bound captures state the NEW nested-leg time and the RESTORED headroom in numbers against the UNCHANGED 150s context and UNCHANGED 120s cap. Condition 4 rides the record, not the fixture: (b) is declined as anti-precedent — carry that statement in your return.

## §2 — Remedy 2 (flake 2; m-3 §3 — DIAGNOSIS FIRST, remedy only if licensed)

1. Identify WHICH `executor-*` detail fired in run B via one bounded load reproduction (my capture names none). Confirm or REFUTE — never inherit — m-3's hypothesis: the probe's `timeoutSentinel` (1731ms, `s8_adversarial_test.go:35-110`) doubles as the live subprocess budget, and run B's 1.74s sits on it ⇒ `executor-timeout` under full parallel-package load. The alternative spawn-machinery classes (`-start-fault`/`-workdir-fault`/`-stage-fault`) carry the same disposition.
2. If ANY `executor-*` load mechanism is confirmed: TEST-ONLY remedy preserving BOTH sentinel properties — the value stays unique and leak-detectable via `timeoutSentinel.String()` in the forbidden-leak list, while no longer load-fragile (m-3 names a 17-second-class odd value and/or serializing the probe relative to the heavyweight process batteries) — then a green re-run; the item CLOSES pre-merge with no documented-flake residue.
3. **The block branch (verbatim consequence): a fault WITHOUT an `executor-*` detail = a defect on m-3's evidence machinery ⇒ the merge BLOCKS, the finding returns to master + m-3.planner, and you stop this order there.** No relabeling, no softening, no retry-to-green inside `internal/observe` or `internal/executor` — the machinery being honest under starvation is designed behavior.

## §3 — The return set (master §1.3)

The uncontended full-suite gate re-run (quiet window stated — coordinate with me as before; I run nothing during yours) with forward-bound captures (command, exit, tip, timestamp, contention, and the condition-3 margin numbers) · the invariant result · the new tip · the exact fence over `663e30d9…` · clean status · the flake-2 confirmed mechanism + remedy · the condition-4 anti-precedent statement. Your FOLD_SCOPE artifact precedes any edit; expected surfaces are test fixtures only (`test/fixtures/*_test.go`, the s12 battery files, `scripts/` untouched unless the diagnosis licenses it as test-side) — anything else is a deviation relay first. RED-first where a behavior change is assertable (the guard's narrowing note + reachability argument are assertion-bearing bytes, not comments only, wherever m-3's conditions can be made executable).

ACTIONS_GIT_REF: docs-workspace disk action — this routing relay + one INDEX row; no `frank/` byte by this seat (the remedies are yours).
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
 M master/relays/INDEX.md
?? frank/.relays/s12/s12-build/SITREP-planner-20260818-042436.md
?? master/relays/step3-h16-h26-lane/RECONCILE-orchestrator-planner-20260818-042202.md
(literal, captured immediately before this file existed; earlier s12 artifacts were banked by master's checkpoint cadence; the master rows are master's own writes)
Next requested action: s12.implementer executes §1 then §2 (diagnosis first; the block branch honored) then the §3 re-run, and returns TO s12.planner; I verify independently in my own quiet window and return the corrected whole TO master (CC VP + m-3) for VP re-review; then the operator's terminal MERGE-GATE. The pair self-advances nothing past the block branch.
