## PLAN — the s12 build plan r1, adopting FROZEN rev20 (`e09fab09…`) as the spec of record: 17 tasks realizing the lane's four legs (H-16 · H-26 · H-16-REG · R-INIT-UNLOCKED) + the rev20-bound cross-cutting obligations, straight-through, RED-first, E2. Routed TO s12.implementer for ADVERSARIAL PLAN-REVIEW. No dispatch token exists in this relay; implementation starts only on a later `DISPATCH IMPL` after your approve, under the commission's delegated conditions.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s12-build-plan
PARENT_DISPATCH_ID: step3-h16-h26-lane
RUN_ID: s12
CEREMONY_TIER: large
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — the standing terminal gate: merge is OPERATOR-ONLY via a future `PHASE: MERGE-GATE` relay carrying `HUMAN_MERGE_AUTHORIZATION`; no fresh operator decision is requested by this relay itself
GRILL_REQUIRED: no — the design was operator-grilled through its own r16 cycle and is FROZEN at rev20; this plan executes it and changes no design byte
FILED_AT_LOCAL: 20260817-191828
DESIGN_LOCK_ID: h16-outcome-split-design @ sha256 e09fab09cdcc2a6025fd42f06b02e573a3e091542f9eb44cebe42a7fa5ff7de9
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s12-h16-fix-plan
PLAN_ARTIFACT: PL-s12-build-plan-20260817
PLAN_SHA256: e30a8af89555f28e7c5f3bd53afae652cb1ea2b0954a35ccdb2c8eff4b477d51
BUNDLE_ID: m-7-conductor-core
OWNER: s12
REPO: frank/ (workspace subtree)
BASE: main@51ddc6fb4f0ff4395c1629d6f3492639d4ca827c
BRANCH: s12-h16-fix
TARGET_BRANCH: main (operator-only merge)
FROM: s12.planner
TO: s12.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator
SUBJECT: the s12 build plan r1 at plan-artifact sha256 e30a8af8… — adversarial plan-review requested; six MUST-CHECK interpretation pins are your named review surface

## §1 — Lineage (why this design-doc PLAN carries no pair DESIGN-REVIEW parent in this root)

rev20 is NOT this pair's design and was NOT designed here: it is m-7's frozen spec of record, pair-approved in the parent lane (`master/relays/step3-h16-h26-lane/DESIGN-REVIEW-implementer-20260817-125908.md`, approve, zero findings, at the exact hash), owner-confirmed by m-1 (`…132139`, approve) and m-2 (the `133642`→`140244` chain, closed at pair grain), joined by Master+VP at stage 3, and bound to this pair by the commissioning PLAN `master/relays/step3-h16-h26-lane/PLAN-orchestrator-planner-20260817-184653.md` (the `DD-v29-master-authority-20260809` commissioning gate; `PARENT_DISPATCH_ID` above names it). This pair's charter forbids re-designing it (CH-s12-h16-fix, the exact-hash void rail). I re-verified the hash from disk this session before consuming a byte: `e09fab09…` exact. Your PLAN-REVIEW therefore reviews the PLAN's fidelity to rev20 — never rev20 itself; a defect you find in rev20's own bytes files as an escalation TO master, and the affected leg HOLDS.

## §2 — What is routed for review

The plan artifact of record: `frank/docs/sprints/active/2026-08-17-s12-h16-fix/plans/PL-s12-build-plan-20260817.md` @ sha256 `e30a8af8…` (17 tasks, task 0–16; battery→task map total over rev20 §10; global constraints; exit criteria). Sprint substrate: `…/ROADMAP.md` (declarations: tracked relay root `frank/.relays/s12/`, batteries dir, branch discipline).

Named review surface, beyond ordinary plan review:
1. **The six MUST-CHECK pins** (plan §MUST-CHECK): (1) the `record_kind` realization — four enum tokens, two operator seat-scope entries, the reopened verb riding the disposition class; (2) registry `type: text` as the string carrier m-2's confirm means; (3) the `s12-fieldspec-v9` marker + successor-relation shape; (4) `genesis` as the fixed predecessor token; (5) ceremony verb spelling latitude; (6) `-bless`/`operatorSubmit` census-report-only exclusion. Each is my interpretation where the ruled set leaves spelling to the build — confirm, contest, or mark human-decision-required per pin. A pin you judge BEYOND the commission's ruled set is an escalation finding, not a local fix.
2. **Task ordering/dependency soundness** (locks → validator → registry → outcome core → Class-G → derived work → chain → upgrade → quarantine → ceremony → retry → matrix → sweep).
3. **Battery totality**: every rev20 §10 battery + the widening word's three `-init` legs bound exactly once, no silent narrowing (the 48-case unit stated so no shape can be omitted).
4. **Scope-fence exactness**: the file-structure block vs the commission's §1; anything you find the plan touching outside it is a blocker.

## §3 — Conditions restated (binding on both seats)

Straight-through build: task-by-task, NO per-task review relays; ONE end-of-slice adversarial review by you at completion; RED-first with sequence-honest FILE-captured batteries; `go test ./...` + the ten INV-CATALOG laws green at every commit; byte-exact `{accepted, rejected, held}`; branch `s12-h16-fix` only; merge OPERATOR-ONLY. Escalation rails: registry/schema beyond the ruled set → master BEFORE work; locked-contract changes → owning pair + master; rev20 byte defects → master (void rail). The frozen exit oracle (`d4580c52…`) and `master/exit-fixtures/` are untouchable. H-12 stands.

Delegated dispatch (the commission's grant to s12.planner): I issue `DISPATCH IMPL` under `s12-build-impl` ONLY after your PLAN-REVIEW returns `approve` parented to THIS relay, with a mechanical `SCOPE_DIFF` returning `all-in` against the plan's file structure and no hard trigger. Your verdict relay: `PHASE: PLAN-REVIEW`, `DISPATCH_ID: s12-build-plan-review`, `PARENT_DISPATCH_ID: s12-build-plan`, filed in `frank/.relays/s12/s12-build/`, TO s12.planner.

ACTIONS_GIT_REF: docs-workspace disk action, all NEW untracked files (nothing committed; the Implementer commits the substrate on branch `s12-h16-fix` at Task 0): this relay + `frank/.relays/s12/INDEX.md` (+ its row) + `frank/docs/sprints/active/2026-08-17-s12-h16-fix/{ROADMAP.md, plans/PL-s12-build-plan-20260817.md}`. No existing byte edited; no `frank/` source/test byte; no branch created (Implementer's Task 0 act).
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s12/
?? frank/docs/sprints/active/
(the `master/relays/CHECKPOINTS.md` modification is a pre-existing foreign change present at session start, untouched by this seat; the two `??` trees are exactly this act's new files)
Next requested action: s12.implementer performs the adversarial PLAN-REVIEW (read rev20 IN FULL at the verified hash first — your charter's boot order governs) and returns `PLAN_REVIEW_VERDICT` TO s12.planner under `s12-build-plan-review`. On approve, I run SCOPE_DIFF and issue the IMPL dispatch; on must-revise, an `-2` successor follows. No self-advance; no code byte moves before the token.
