## SITREP — TWO RULINGS REQUESTED UP THE CHAIN (the plan-review's R2-F2 + R2-F4 route-ups, both registry/locked-contract surfaces this pair may NOT decide locally): **(1) the H-16-REG `record_kind` realization** — the commission's word is "the two new `record_kind` tokens" while rev20 defines FOUR committed record classes that all need enum membership under the live validation; **(2) the `mint_predecessor` chain-tip mismatch rejection byte** — rev20 `:89` requires a typed class but names no token, and m-2's confirmed class set does not cover it. Tasks 4 and 7–15 of the s12 plan GATE on ruling (1); Task 10 GATES on ruling (2); Tasks 0–3, 5, 6 are unblocked. No code byte moves under this relay.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s12-build
PARENT_DISPATCH_ID: s12-build-plan-review
RUN_ID: s12
CEREMONY_TIER: large
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — master rules (routing m-2/m-7 as they own); the operator is CC'd context; the terminal MERGE-GATE is unchanged and not requested
FILED_AT_LOCAL: 20260817-194616
IN_REPLY_TO: s12-build/PLAN-REVIEW-implementer-20260817-192631.md
FROM: s12.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, m-2.planner, m-7.planner, s12.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: two build-blocking rulings requested — the record_kind token realization (commission §1.3 cardinality vs rev20's four record classes) and the unruled predecessor-mismatch rejection byte (rev20 :89)

## Ask 1 — the `record_kind` realization (commission §1.3; blocks plan Tasks 4 and 7–15)

The commission rules "the two new `record_kind` tokens (`mint-chain-anchor` + the disposition class, operator-seat-scoped per the `gate_resolution` precedent; writer/ceremony-stamped classes ride the genesis empty-`seat_scope` precedent)". rev20 defines FOUR record classes this build must COMMIT as canonical records: `mint-chain-anchor` (§4a), `attempt_resolution` (§3a/§5), `derived-work-attempt` and `derived-work-transition` (§3a). Evidence that writer-stamped classes still need enum membership: the live registry's `record_kind` enum carries `genesis` (writer-authored) and grew by the writer classes `odb`/`resummon_command` at v7→v8 (`internal/store/genesis.go:38-48`, the downgrade table's exact enum lines) — so all four new classes appear to need enum entries, and the question is how the commission's "two tokens" maps onto them.

s12's recorded DEFAULT reading (E1, from the cited bytes; NOT self-executed): the enum gains all four members spelled exactly as rev20 spells them; "the two new tokens" = the two OPERATOR-SEAT-SCOPED additions, `seat_scope.operator` += `{mint-chain-anchor, attempt_resolution}` (the `gate_resolution` precedent); `derived-work-attempt`/`derived-work-transition` carry NO seat_scope entry (the `genesis` precedent — the commission's "writer/ceremony-stamped classes" clause); the operator's §5 `reopened` verb rides the `attempt_resolution` class's validated header set (rev20 §5's one-form language — "the designated PHASE/SUBJECT + validated header set for `attempt_resolution`/`reopened`" — and m-2's single disposition form, their `133642` §4.1), with the §4b commit-time validator typing the verbs. The alternative reading — operators submit `derived-work-transition`-class records directly for reopens — needs a THIRD operator seat-scope entry, which s12 reads as beyond the ruled set either way.

Requested: master's ruling (with m-2's guidance as the registry owner) on the exact enum-member set, the exact `seat_scope` deltas, and the `reopened` carrier. The s12 plan's Task 4 will realize the ruling byte-exactly and cite the ruling relay.

## Ask 2 — the predecessor-mismatch rejection byte (rev20 `:89`; blocks plan Task 10)

rev20 §4a: a `seat_mint` candidate whose implied predecessor is not the current chain tip "commits REJECTED with a typed class — impossible via the single writer except under tampering/divergence". No token is named there, and the m-2-confirmed class set (`duplicate-resolution` · `conflicting-resolution` · `stale-resolution` · `unknown-target` · `anchor-target-resolved` · `retry-authority-delta`, their `133642` §3) does not cover this case. The byte is an externally visible `failing_edge` surface — a locked-contract choice the pair must not improvise (the reviewing implementer's F4, concurred).

Requested: the ruled token (m-7 as spec author + m-2 as class owner, routed as master sequences). If the disposition is that rev20 itself must carry the byte, the void rail governs and m-7 authors the successor; s12's Task 10 holds either way until a token is ruled, and its RED leg will assert the ruled byte exactly.

## Status (report-only)

The plan-review (`s12-build/PLAN-REVIEW-implementer-20260817-192631.md`, must-revise, five findings) is folded as plan r2 + the r2 PLAN relay filed in this same act; all five findings verified at the bytes before folding. The two asks above are the review's F2/F4 route-ups, filed as the reviewing implementer required. Build tasks 0–3, 5, 6 are dispatchable on plan-approve without these rulings; 4 and 7–15 gate on Ask 1; 10 gates on Ask 2.

ACTIONS_GIT_REF: docs-workspace disk action — this SITREP + the r2 PLAN relay + two INDEX rows + the in-place plan-doc r2 fold + the ROADMAP declaration update (all untracked/uncommitted; the substrate commits on the branch at Task 16 per the worktree discipline); no `frank/` source/test byte, no branch, no commit.
FINAL_GIT_STATUS_SHORT:
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s12/
?? frank/docs/sprints/active/
Next requested action: master rules Ask 1 and Ask 2 (routing m-2/m-7 as owners), relays the rulings TO s12.planner (CC s12.implementer). s12 proceeds meanwhile only within the unblocked plan surface and holds the gated tasks.
