## SITREP — THE R3-F1 RETURN, verified: **the inspector is now constrained to the EXECUTABLE guard shape and the durability defect is CLOSED at `s12-h16-fix@ba3f76082ad1fea18b667f5fae0d250c119ad339`** (one test file, +93/−18; the recursive `ast.Inspect` walk is GONE — zero occurrences, verified — replaced by strict positional shape checks: statement zero must be the exact directly executed `if s12SkipOuterRunOnly(t) { return }` with no Init/no Else/single-return, and the one startup-crash fixture must carry its exact `FRANK_H16_MINT_CRASH_CHILD` prefix at statement zero with the guard at statement one). The NEW negative (`…RejectsNestedIneffectiveCall`: the only helper call inside an uninvoked function literal) had its RED observed against the OLD wrongly-accepting inspector before the constraint landed, and is rejected at the landed bytes; the total-omission negative carries. **My independent runs: 7/7 focused PASS (both AST negatives, the binding, the nested contract, the isolation probe, the F1/F2 regressions), the full gate exit 0 in my own quiet window, ten INV-CATALOG laws PASS.** Ready for VP r4, which decides the MERGE-GATE routing. Nothing merges under this relay.

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s12-build
PARENT_DISPATCH_ID: s12-build-review-fold
RUN_ID: s12
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: yes — on the VP's r4 pass, the lane's next transition is the operator-only MERGE-GATE (its routing is VP r4's decision, not this relay's request)
FILED_AT_LOCAL: 20260819-225523
IN_REPLY_TO: s12-build/REVIEW-FOLD-implementer-20260819-224442.md
PLAN_LOCK_ID: s12-h16-fix-plan @ sha256 f1de9e91e2c7d7c87faa471886d186295bbf7251acae54e98919c97e1ade9696
DESIGN_LOCK_ID: h16-outcome-split-design
BRANCH: s12-h16-fix
BASE: 663e30d99d61d89ec1958ba432389727bcf7c15f
TARGET_BRANCH: main (operator-only merge; gate HELD)
FROM: s12.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, m-3.planner, operator, s12.implementer
BUNDLE_ID: m-7-conductor-core
SUBJECT: R3-F1 closed and twice-verified; the return set complete; the lane stands ready for VP r4

## §1 — The return set (the VP's closing spec, every item verified by me)

1. **The stronger AST assertion + its negative proof:** the constrained `s12InspectOuterRunOnlyBindings` (positional shape matching at `s12_nested_scope_test.go` — `Body.List[0]`/`[guardIndex]`, the guard's exact executable form, the crash-child prefix special case for exactly `TestH16StartupFoldsRealizedMintEvidenceBeforeServing`); the new `TestS12OuterRunOnlyRegistryBindingRejectsNestedIneffectiveCall` (RED observed pre-constraint per R2-F2's discipline: `nested ineffective s12SkipOuterRunOnly(t) call was accepted`; now rejected as `missing executable … guard`); the existing omission negative carried.
2. **Focused results:** 7/7 PASS in my own run — the real-source binding, both negatives, the nested contract, the isolation probe, and the F1/F2 runtime regressions (nothing runtime reopened).
3. **The forward-bound full-gate captures:** implementer `implementer-r3-forward-verify-20260819-224413.txt` @ `b3e89fd0…` (hash verified; exit 0, fixtures 181.96s, contention preflight empty) AND planner `planner-verify4-full-uncontended-20260819-225221.txt` @ `12bd79f2…` (my own quiet window, exit 0, fixtures 167.55s, real 169.54s). Ten INV-CATALOG laws PASS in both seats' runs.
4. **Exact tip/fence:** `ba3f76082ad1fea18b667f5fae0d250c119ad339` over `663e30d9…` = 21 commits, 46 files, ZERO outside `frank/` (my count); `git diff --binary` sha256 `2897bc63…` recomputed, matches; the landed commit touches exactly one test file.
5. **Clean status:** the implementation worktree `git status --short` empty (verified).

## §2 — The lane's standing state (nothing else moved)

F1/F2 closed at `8ca99650…` (untouched). The R2-F1 flake item rides per m-3's owner disposition with master's byte-verbatim record correction (`R-S12-FLAKE-INFERRED-2`) — untouched by this round. The operator's `194914` lint waiver stands; the two immutable r1 errors remain the only routed residue. The four registry discharges stay future-tense at merge; H-12, the frozen oracle `d4580c52…` + RLBS-1, the live s11 courier, and the HELD MERGE-GATE all stand. NOT claimed: merge, push, PR, deploy.

ACTIONS_GIT_REF: docs-workspace disk action — this return + one INDEX row + my forward-bound capture under `frank/.relays/s12/batteries/`; no `frank/` byte by this seat; the R3 evidence rides branch `s12-h16-fix@ba3f76082ad1fea18b667f5fae0d250c119ad339` (the implementer's commit).
FINAL_GIT_STATUS_SHORT:
 M frank/.relays/s12/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s12/batteries/planner-verify4-full-uncontended-20260819-225221.txt
?? frank/.relays/s12/s12-build/FOLD_SCOPE-implementer-20260819-223742.md
?? frank/.relays/s12/s12-build/REVIEW-FOLD-implementer-20260819-224442.md
?? frank/.relays/s12/s12-build/SITREP-planner-20260819-222245.md
?? frank/.relays/s12/s12-build/SITREP-planner-20260819-225523.md
(literal, captured immediately before this file existed; the master row is master's own write)
Next requested action: master routes VP re-review r4 over this corrected whole; VP r4 decides whether to route the operator-only MERGE-GATE. The pair holds; no s12 seat routes or exercises the gate.
