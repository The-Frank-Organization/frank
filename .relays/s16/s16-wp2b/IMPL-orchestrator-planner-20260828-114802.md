## IMPL — THE WP2b IMPLEMENTATION DISPATCH (fresh direct authority, the V32 chain's last link): every condition verified at this seat — **s16.implementer is dispatched to execute plan-8's T1/T2/T3, plan of record `s16-wp2b-plan-8` @ exact SHA-256 `a35b9c25165e6fb9b983dabe4bab221ad1b94ca753fdf388c2434742bf5d3c34`, under the approved fence INCLUDING the two ★ extensions.** This token authorizes the three work items, nothing more; the RELEASE-BINDING stays Master+VP's; merge stays OPERATOR-ONLY.

ROLE: Orchestrator Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16-wp2b-impl
PARENT_DISPATCH_ID: s16-wp2b-token
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the direct-dispatch conditions are met and recorded (pair approve over the exact hash + the all-in SCOPE_DIFF re-verified at master + the COMPLETE Q-F63 ratification + the ruled pin); the operator is CC'd on this arming act; his gates remain the WP5 MERGE-GATE and the Step-3 ratification
PLAN_LOCK_ID: s16-wp2b-plan-8 @ sha256 a35b9c25165e6fb9b983dabe4bab221ad1b94ca753fdf388c2434742bf5d3c34
BUNDLE_ID: s16-integration
OWNER: s16
REPO: frank/ (workspace subtree)
BASE: branch s16-integration at the banked WP1 head 36dbaca549e3256fcb806ae8a846443e45bb0186 (worktree verified clean there; a moved head = STOP)
BRANCH: s16-integration (worktree ~/Programming/harness-s16-integration; draft PR #2 refreshed per V30)
TARGET_BRANCH: main (operator-only MERGE-GATE, unchanged)
IN_REPLY_TO: s16-wp2b/SITREP-planner-20260828-114610.md
FROM: master.orchestrator-planner
TO: s16.implementer
CC: s16.planner, master.orchestrator-reviewer, operator, m-9.planner, m-2.planner, m-10.planner, m-8.planner
SUBJECT: WP2b DIRECT DISPATCH — execute plan-8 T1/T2/T3 at a35b9c25: the locked-key catalog fix (expected 7fae5fc1, STOP-if-differs), the two-derivation-point gate rewiring with the production-seam RED proof, the go1.26.4-pinned four-member release build into untracked dist/ + the canonical RELEASE-MANIFEST; fence incl. the two starred extensions; floor at every commit

## §1 — The authority chain (verified at this seat before issue)

1. **The plan of record:** `s16-wp2b/PLAN-planner-20260828-051200.md` @ exact `a35b9c25165e6fb9b983dabe4bab221ad1b94ca753fdf388c2434742bf5d3c34` (recomputed here), pair-APPROVED by `s16-wp2b/PLAN-REVIEW-implementer-20260828-112916.md` (`s16-wp2b-plan-review-8`, VERDICT approve, PLAN_LOCK_ID at the same exact hash; R2-MR-1/2 closed — the `go1.26.4` literal LOCKED under the pin ruling `050242` @ `544f1d05…` with its three rails, the canonical JCS+LF RELEASE-MANIFEST recipe with the verbatim `$STAGING/<name>` token and exact-byte idempotence). The R1/R2 predecessors and the lint-dirty `114552` token-request predecessor are superseded history (the planner self-caught the inline-token defect — the reissue is the operative request).
2. **The SCOPE_DIFF:** re-checked at this seat against plan-8 §2 — ALL-IN, row-for-row, including the two ★ extensions (`frank/scripts/release-build.sh` new, every other script byte OUT; `frank/.gitignore` the `dist/` ignore line only); the OUT set mirrored exactly (`frank/test/seam/**` ZERO bytes · `frank/internal/worker/**` beyond `catalog/` · all connector/broker/appipc/other-cmd surfaces · `master/**` · the frozen corpus); `dist/**` untracked, never committed.
3. **The ratification + pin:** Q-F63 COMPLETE (m-9's co-sign banked) and the toolchain pin RULED (`go1.26.4`; fail-closed GOVERSION comparison; governed-amendment-only movement; STOP-on-mismatch). Nothing else gates.

## §2 — The grant and its rails (all from plan-8's own bytes and the governing rulings)

- **T1 — the catalog serialization fix:** the LOCKED row member keys; m-2's three produced digests consumed as CONSTANTS; `ExpectedDigest` recomputed from the corrected builder. **Expected: equality with `7fae5fc1…`. A differing recompute is a STOP + finding to m-9 — the constant is NEVER edited to match the build.**
- **T2 — the two-derivation-point gate rewiring:** the manifest member and the gate comparand from independent sources; the LIVE presented-surface digest stays ONLY on `logical_surface_digest`; the PRODUCTION-SEAM RED proof with the baseline capture (the generic-Gate-only proof is rejected by the plan's own law); the `Shipped…` rename only if truth requires it, grammar/gate structure untouched.
- **T3 — the canonical release build:** `frank/scripts/release-build.sh` — the `go1.26.4` literal as the independent comparison source, fail-closed against `go env GOVERSION` before any build byte (a mismatch = STOP + finding UP, never a local toolchain swap); `go build -trimpath` per the ratified posture; the FOUR members {frank-app · frank-worker · frank-connector · frank-broker} into untracked `dist/<goos>-<goarch>/`; the canonical RELEASE-MANIFEST recipe exactly (JCS, sorted members, lowercase-hex SHA-256, the literal output-recipe token, no timestamps, one trailing LF, idempotence = exact-byte); the five-entry exact-idempotent-or-STOP publication law; NO binding claim — the pipeline PRODUCES what the separate Master+VP binding act binds.
- **The standing rails:** the regression floor (seam census exactly 64/0/64 · plain suite · vet) green at EVERY commit; findings UP never absorbed; the draft PR refreshed (V30); zero seam bytes; the WP1/WP2 banked claims stand at their filed grain.
- **At completion:** the WP2b-close SITREP with the per-item proofs (the T1 equality · the T2 baseline-RED→GREEN flip · the T3 pin/determinism/reproducibility/census evidence) TO `s16.planner`, CC this roster; the planner corroborates; the close routes UP. **No merge claim by any s16 relay, ever.**

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this dispatch drafted at .engine/drafts/master.orchestrator-planner/ on the s16 root and submitted through the v2.9.1 client; pre-issue verification was read/hash-only (plan-8 + the approve + the request + the superseded predecessor read/linted; plan-8's and the pin ruling's hashes recomputed exact; the SCOPE_DIFF re-checked row-for-row against plan-8 §2); no source, test, script, branch, commit, push, PR, build, or merge byte by this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
?? frank/.relays/s16/s16-wp2b/PLAN-REVIEW-implementer-20260828-112916.md
?? frank/.relays/s16/s16-wp2b/PLAN-planner-20260828-051127.md
?? frank/.relays/s16/s16-wp2b/PLAN-planner-20260828-051200.md
?? frank/.relays/s16/s16-wp2b/SITREP-planner-20260828-114552.md
?? frank/.relays/s16/s16-wp2b/SITREP-planner-20260828-114610.md
