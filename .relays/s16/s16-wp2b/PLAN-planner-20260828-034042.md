## PLAN — WP2b REISSUE (s16-wp2b-plan-7): R1-MR-1..3 closed — the T2 RED leg goes THROUGH the production composition seam with the pre-change baseline captured; the toolchain pin is an exact literal with an independent comparison and the owner blessing ROUTED with the token request; T3 publication is fail-closed two-staging non-destructive with an exact final census; T1 and every carried clause preserved whole

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2b-plan-7
PARENT_DISPATCH_ID: s16-wp2b-plan-review-6
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the token remains master's fresh direct dispatch (which is asked to also ratify the §3-T3 toolchain literal); the RELEASE-BINDING stays the separate Master+VP act; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — the reissue realizes the same banked owner rulings; the ONE routed judgment (the toolchain literal) goes UP with the token request, never decided here
IN_REPLY_TO: s16-wp2b/PLAN-REVIEW-implementer-20260828-032120.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-2.planner, m-10.planner, m-8.planner
SUBJECT: WP2b plan-7 — R1-MR-1 closed (the RED leg drives the NAMED production derivation seam extracted from productionStarter.Start, joint-proves member/tool_set-from-ExpectedIdentities + comparand-from-ExpectedDigest + divergence⇒ErrServeGate, with the pre-change baseline-RED captured; a hand-built Gate/BuildInput is declared insufficient, the generic manifest_test leg cited as the dodge it must not be); R1-MR-2 closed (proposed canonical pin = the literal go1.26.4, independently compared via `go env GOVERSION`, RATIFICATION ROUTED to master in the token request; the effective-input set enumerated fixed-vs-recorded); R1-MR-3 closed (two fresh staging builds → digest-equality → deterministic RELEASE-MANIFEST → publish-only-verified; existing dist target: exact-idempotent-or-STOP, never delete/overwrite; the exact five-entry final census law)

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential, capability, custody, or authorization surface changes (evidence = §5)
- migration/backfill/destructive-write/canonical-data-repair: no — T3 is fail-closed NON-destructive BY SPECIFICATION now: staging-only builds, publish-only-verified, an existing target is exact-idempotent-or-STOP, never deleted or overwritten (evidence = §3 T3)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — F58/F63 identity values and the serve-gate comparand are trust-critical (evidence = §3)
- AI-or-automation-acts-downstream: yes — the corrected gate and the pipeline outputs feed the Master+VP binding act and later runs (evidence = §4)
- worker/scheduler/queue/retry/async-side-effect: yes — the composed battery re-runs as the T2 verification instrument; the four-member build executes (evidence = §3)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — F58's locked encoding, the A14 vector, and the ratified F63 set are cross-owner contracts realized under the owners' exact terms (evidence = §1/§3)
- user-visible-control-with-materializer/downstream-consumer: yes — dist/ + RELEASE-MANIFEST feed the binding act (evidence = §3 T3)
- test-runtime-role-mismatch: no — the T2 regression is REQUIRED to drive the production derivation seam, with the generic-Gate path expressly declared insufficient and the pre-change baseline captured (evidence = §3 T2)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the two named fence-extension rows stand, and the toolchain-literal judgment is ROUTED to master rather than taken (evidence = §2/§3 T3)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — The governing basis (unchanged from plan-6; carried whole)

The WP2b opening `s16-wp2b/RECONCILE-orchestrator-planner-20260828-023804.md`; the owner rulings read in full — m-9's Q-CATALOG `master/relays2/s16-wp2-owners/SITREP-planner-20260828-022110.md` · m-2's keys-only proof `…DESIGN-planner-20260828-022111.md` · m-10's tautology finding + two-derivations law `…SITREP-planner-20260828-022057.md` · m-8's ratified Q-F63 half `…SITREP-planner-20260828-022252.md` · m-9's landed co-sign `master/relays2/s16-wp2-disp/SITREP-planner-20260828-024556.md`; the banked WP1 object at head `36dbaca5…` (draft PR #2); the regression floor at every commit; the pre-verified consumption facts (the seam helper's by-reference consumption at `frank/test/seam/helpers_test.go:466`; the runtime self-check `runtime.go:134`; `frank/scripts/` existing; no `frank/.gitignore`). The review of record `s16-wp2b/PLAN-REVIEW-implementer-20260828-032120.md` over plan-6 @ `688bc0f6…`: T1 review-clean and carried; R1-MR-1..3 verified at this seat before this reissue (the generic gate-mismatch leg confirmed at `internal/appctl/manifest/manifest_test.go` — `driftedGate.ShippedToolCatalogDigest` ⇒ `ErrServeGate`, exactly the dodge T2's leg must not be; `go.mod:3` = `go 1.25.0` minimum with NO toolchain directive — no existing pin source).

## §2 — Scope and the EXACT fence (unchanged from plan-6; the SCOPE_DIFF comparand; ★ = the extension)

`frank/internal/worker/catalog/**` · `frank/cmd/frank-app/**` · `frank/internal/appctl/manifest/**` (ONLY the possible `Shipped…` rename; grammar/gate structure untouched) · `frank/test/composed/**` · ★ `frank/scripts/release-build.sh` (new; every other script byte OUT) · ★ `frank/.gitignore` (new; the `dist/` ignore line only) · `frank/go.mod`/`frank/go.sum` arbitration-only · the relay root via the engine. Explicitly OUT: `frank/test/seam/**` (ZERO bytes; by-reference propagation verified) · `frank/internal/worker/**` beyond `catalog/` · all connector/broker/appipc/other-cmd surfaces · `master/**` · the frozen corpus · every m-x design/lock byte. `dist/**` untracked, never committed.

## §3 — The three work items (T1 carried verbatim; T2/T3 per the R1 corrections)

- **T1 — the catalog serialization fix (REVIEW-CLEAN; carried verbatim from plan-6):** the F58 rows serialize under the LOCK's row member keys `{canonical_name, tool_schema_digest, tool_impl_catalog_version, form_schema_mapping_version?}`, absence-encoded on locals, present on the three relay rows, sorted by `canonical_name` byte order, m-2 §3.2 encoding/JCS, SHA-256; the member VALUES do not move (m-2's three produced digests stay source constants — no render, no store read, provably; m-9's five local digests and both version markers as pinned); `ExpectedDigest` RECOMPUTED. **Expected: equality with `7fae5fc1dd8f91c48828beaf0cfba45a1da4c297bf82f790ec2912b0a168c9d4`; THE STOP LAW verbatim: a differing recompute is a STOP + finding to m-9 — the constant is NEVER edited to whatever the build emits.** The package test asserts the constant equals the LITERAL doc pin AND `Digest(ExpectedIdentities())` reproduces it. The A14 manifest keys stay a different surface. The seam battery follows by reference with zero seam bytes; the runtime self-check follows.
- **T2 — the two-derivation-point wiring fix (R1-MR-1 closed — the PRODUCTION-SEAM RED leg):** extract the composition's derivation into a NAMED production helper in `cmd/frank-app` — the shape: `productionToolIdentity()` (or its replacement) returns the manifest `tool_set` + the member digest derived from `catalog.ExpectedIdentities()` (the REAL schema digests · `m9-catalog-v1` · `m2-mapping-v1` on relay rows — the fabricated `sha256("schema:"+name)`/`"mvp-v1"` values GONE), and a SEPARATE named site in `productionStarter.Start`'s path injects the gate comparand from `catalog.ExpectedDigest` — two independent derivation points, exactly m-10's law. **The regression (in `cmd/frank-app`'s package test, in-fence) drives the PRODUCTION helper/path — it must JOINTLY prove: (i) the manifest member + `tool_set` derive from `catalog.ExpectedIdentities()`; (ii) the gate comparand enters from `catalog.ExpectedDigest`; (iii) a FORCED DIVERGENCE AT THAT PRODUCTION SEAM (the member's derivation tampered relative to the injected pin) returns `manifest.ErrServeGate`. Hand-constructing unrelated `BuildInput`/`Gate` values is INSUFFICIENT — the generic leg already exists at `internal/appctl/manifest/manifest_test.go` (`driftedGate` ⇒ `ErrServeGate`) and proves nothing about the composition. THE BASELINE-RED CAPTURE: the new regression is run against the PRE-change production wiring first and its failing leg recorded (at the tautological wiring, divergence at the seam is UNDETECTABLE — the gate passes; the capture shows exactly that), then GREEN after the fix — the RED→GREEN flip rides the close SITREP.** The LIVE presented-surface digest stays ONLY on `logical_surface_digest`; the composed battery + WP1-evidence assertions update to the STATIC member value, honestly labeled.
- **T3 — the canonical release pipeline (R1-MR-2 + R1-MR-3 closed):** `frank/scripts/release-build.sh`, the canonical build step.
  - **The toolchain pin (R1-MR-2):** the canonical pin is the EXACT LITERAL **`go1.26.4`** carried IN THE SCRIPT as the independent comparison source, checked fail-closed against `go env GOVERSION` (a mismatch aborts before any build). **The literal is a routed judgment, NOT taken here: the token request asks master (m-8/m-9 in CC) to RATIFY `go1.26.4` — the toolchain this slice's entire evidence trail was built and three-seat-verified under — in the dispatch; if master names a different pin, the script carries that literal instead; no build before the ratified dispatch either way.** The effective-input set, enumerated: FIXED by the script — `GOFLAGS=""` (explicitly set), the literal `-trimpath`, the literal package/output arguments (`go build -trimpath -o <staging>/<name> ./cmd/<name>` for exactly the four members); RECORDED in the RELEASE-MANIFEST — the pinned toolchain literal + the verified `GOVERSION`, `GOOS`, `GOARCH`, `CGO_ENABLED` (as effective), the full build command line, and the per-binary lowercase-hex SHA-256s.
  - **Fail-closed non-destructive publication (R1-MR-3):** the script builds the full four-member set TWICE into TWO FRESH staging directories (under a script-created temp area, never `dist/` directly); compares corresponding per-binary SHA-256s — ANY inequality aborts (the determinism proof, observable); generates the RELEASE-MANIFEST deterministically from the verified digests; then PUBLISHES only the verified set into `dist/<goos>-<goarch>/` under the census law: **if the target directory does not exist, create + publish; if it exists and already equals the EXACT verified set (five entries, byte-equal), leave unchanged (idempotent PASS); otherwise STOP with a finding — NO deletion, NO overwrite, ever.** The final census must equal exactly `{frank-app, frank-worker, frank-connector, frank-broker, RELEASE-MANIFEST}`; an extra or missing entry is a failure. `frank` (F65) and `frank-mcp` (excluded) are NOT members; NO binding claim anywhere — the pipeline PRODUCES what the separate Master+VP binding act will bind; the four-member manifest grammar stays Q-CARRIER at the binding act.

## §4 — Acceptance criteria (plan-6's, with the R1 corrections folded)

1. T1: recompute AND constant both equal the literal `7fae5fc1…`; the STOP law honored if not; zero seam bytes; census 64/0/64; runtime self-check green.
2. T2: the PRODUCTION-seam regression jointly proves the three legs with the PRE-change baseline-RED captured and the post-change GREEN shown (the flip in the close SITREP); the fabricated identity values gone; the live digest only on `logical_surface_digest`; the composed run passes with member = pin.
3. T3: the ratified toolchain literal verified fail-closed; the two-staging double-build digest-equality proof green; the published census exactly the five entries with the idempotent-or-STOP law honored; no binding claim.
4. The regression floor green at EVERY commit; the draft PR refreshed (V30); findings UP never absorbed; WP1/WP2's banked claims stand (forward-only).
5. The WP2b-close SITREP: per-item evidence, the T1 equality, the T2 flip, the T3 manifest + determinism + census proofs, the honest not-done, findings; then my corroboration and the close routes UP.

## §5 — Out of scope (carried whole from plan-6)

Any "release bound"/"binding-ready" claim; the RELEASE-BINDING act and the Q-CARRIER grammar; any edit to the worker design doc, the `:144` pin, or ANY m-x design/lock byte; `frank/test/seam/**` (zero bytes); every §2-OUT surface; deleting or overwriting any existing `dist/` content (the STOP law); live/external E3, admissibility, exit legs (WP3); budget/conformance (WP4); gap register/battle report/close (WP5); merge (operator-only); new dependencies; any push beyond the slice branch + draft PR.

## §6 — Sequencing (the V32 law; the ONE routed judgment named)

Your PLAN-REVIEW verdict TO me, parented to this relay, over this plan's exact hash. On approve: I run the mechanical `SCOPE_DIFF` against §2 (the ★ extension rows listed) and route the TOKEN REQUEST UP — **asking master to (a) issue the fresh direct dispatch TO `s16.implementer` carrying the approved fence INCLUDING the extension, and (b) RATIFY the §3-T3 toolchain literal (`go1.26.4` proposed) in that dispatch.** Implementation begins only on that dispatch; no catalog, app, manifest, test, script, gitignore, dist, build, branch, push, PR, binding, or merge act before it. Spec-base defects found while realizing are escalations UP to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (the review read in full; the generic gate leg + go.mod pin state verified at the banked head); no source, test, script, branch, commit, push, PR, merge, build, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit; the untracked workspace-root `.codex-s16-wp2b-plan-review.md` is the implementer seat's scratch file, not this seat's:)
 M frank/.relays/s16/INDEX.md
 M master/relays2/INDEX.md
?? .codex-s16-wp2b-plan-review.md
?? frank/.relays/s16/s16-wp2b/PLAN-REVIEW-implementer-20260828-032120.md
?? frank/.relays/s16/s16-wp2b/PLAN-planner-20260828-024847.md
?? master/relays2/s16-wp2-disp/SITREP-planner-20260828-024556.md
