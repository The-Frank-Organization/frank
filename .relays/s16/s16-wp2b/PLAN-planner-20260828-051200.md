## PLAN — WP2b REISSUE (s16-wp2b-plan-8): R2-MR-1/2 closed — the toolchain literal `go1.26.4` is a LOCKED plan input under master's durable ruling (rails bound), and the RELEASE-MANIFEST is a fully-specified CANONICAL byte artifact (no staging paths, no timestamps, exact-byte idempotence); T1/T2, the publication law, and every carried closure preserved whole

*(Same-dispatch corrected successor to `s16-wp2b/PLAN-planner-20260828-051127.md`, plan content IDENTICAL: the predecessor's FINAL_GIT_STATUS_SHORT was captured before a master-side checkpoint banked the listed rows, so it was stale at its own submit instant — the recurring capture-vs-bank race; the field below is the corrected fresh literal. THIS file is the reviewable plan of record; the predecessor stands as history.)*

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2b-plan-8
PARENT_DISPATCH_ID: s16-wp2b-plan-review-7
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the token remains master's fresh direct dispatch on the V32 chain; the RELEASE-BINDING stays the separate Master+VP act; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — the reissue realizes the banked owner rulings + master's durable pin ruling; no judgment remains open at this seat
IN_REPLY_TO: s16-wp2b/PLAN-REVIEW-implementer-20260828-034510.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-2.planner, m-10.planner, m-8.planner
SUBJECT: WP2b plan-8 — R2-MR-1 closed (the pin RULED durably at `s16-wp2b/RECONCILE-orchestrator-planner-20260828-050242.md`: the exact literal go1.26.4, carried in the script as the independent comparand, fail-closed vs GOVERSION, governed-amendment-only movement, STOP-on-mismatch — ONE implementation contract, no float); R2-MR-2 closed (the canonical RELEASE-MANIFEST recipe: JCS-canonical JSON, members sorted by name, a literal output-recipe token, no timestamps, one trailing LF, idempotence = exact-byte); T1 review-clean carried, T2's production-seam RED leg carried, the non-destructive five-entry publication law carried

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential, capability, custody, or authorization surface changes (evidence = §5)
- migration/backfill/destructive-write/canonical-data-repair: no — T3 is fail-closed non-destructive by specification: staging-only builds, publish-only-verified, exact-idempotent-or-STOP, never delete/overwrite (evidence = §3 T3)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — F58/F63 identity values and the serve-gate comparand are trust-critical (evidence = §3)
- AI-or-automation-acts-downstream: yes — the corrected gate and the pipeline outputs feed the Master+VP binding act and later runs (evidence = §4)
- worker/scheduler/queue/retry/async-side-effect: yes — the composed battery re-runs as the T2 verification instrument; the four-member build executes (evidence = §3)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — F58's locked encoding, the A14 vector, and the ratified F63 set are cross-owner contracts realized under the owners' exact terms (evidence = §1/§3)
- user-visible-control-with-materializer/downstream-consumer: yes — dist/ + the canonical RELEASE-MANIFEST feed the binding act (evidence = §3 T3)
- test-runtime-role-mismatch: no — the T2 regression drives the production derivation seam with the generic-Gate path declared insufficient and the pre-change baseline captured (evidence = §3 T2)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the two named fence-extension rows stand (the dispatch carries them explicitly); no judgment floats: the pin is ruled, the recipe is fixed (evidence = §2/§3 T3)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — The governing basis (plan-7's, plus the pin ruling)

The WP2b opening `s16-wp2b/RECONCILE-orchestrator-planner-20260828-023804.md`; the owner rulings read in full (m-9 Q-CATALOG `master/relays2/s16-wp2-owners/SITREP-planner-20260828-022110.md` · m-2's keys-only proof `…DESIGN-planner-20260828-022111.md` · m-10's tautology + two-derivations law `…SITREP-planner-20260828-022057.md` · m-8's ratified Q-F63 half `…SITREP-planner-20260828-022252.md` · m-9's co-sign `master/relays2/s16-wp2-disp/SITREP-planner-20260828-024556.md` — the ratification COMPLETE); **master's durable pin ruling `s16-wp2b/RECONCILE-orchestrator-planner-20260828-050242.md`: the canonical F63 toolchain = the exact literal `go1.26.4`, with the three rails — locked plan input with the script as the independent comparison source fail-closing against `go env GOVERSION`; movement only by governed amendment through the ratifying chain, never toolchain drift; a build-time GOVERSION mismatch = STOP + finding UP, never a local toolchain swap.** The two prior reviews (`032120` over plan-6 @ `688bc0f6…`; `034510` over plan-7 @ `a22d270b…`): T1 review-clean; R1-MR-1 closed (the production-seam RED leg); the R1-MR-3 publication law closed and carried. The banked WP1 object at head `36dbaca5…` (draft PR #2); the regression floor at every commit; the pre-verified consumption facts (the seam helper by-reference at `helpers_test.go:466`; the runtime self-check `runtime.go:134`; `frank/scripts/` existing; no `frank/.gitignore`; the generic gate leg at `internal/appctl/manifest/manifest_test.go` = the declared-insufficient dodge; `go.mod:3` = minimum only).

## §2 — Scope and the EXACT fence (unchanged; the SCOPE_DIFF comparand; ★ = the extension)

`frank/internal/worker/catalog/**` · `frank/cmd/frank-app/**` · `frank/internal/appctl/manifest/**` (ONLY the possible `Shipped…` rename; grammar/gate structure untouched) · `frank/test/composed/**` · ★ `frank/scripts/release-build.sh` (new; every other script byte OUT) · ★ `frank/.gitignore` (new; the `dist/` ignore line only) · `frank/go.mod`/`frank/go.sum` arbitration-only · the relay root via the engine. Explicitly OUT: `frank/test/seam/**` (ZERO bytes) · `frank/internal/worker/**` beyond `catalog/` · all connector/broker/appipc/other-cmd surfaces · `master/**` · the frozen corpus · every m-x design/lock byte. `dist/**` untracked, never committed.

## §3 — The three work items (T1/T2 carried verbatim from plan-7; T3 with the R2 closures)

- **T1 — the catalog serialization fix (REVIEW-CLEAN; carried verbatim):** the F58 rows serialize under the LOCK's row member keys `{canonical_name, tool_schema_digest, tool_impl_catalog_version, form_schema_mapping_version?}`, absence-encoded on locals, present on the three relay rows, sorted by `canonical_name` byte order, m-2 §3.2 encoding/JCS, SHA-256; member VALUES unmoved (m-2's three produced digests stay source constants, provably no render/store read; m-9's five local digests + both version markers as pinned); `ExpectedDigest` RECOMPUTED. **Expected: equality with `7fae5fc1dd8f91c48828beaf0cfba45a1da4c297bf82f790ec2912b0a168c9d4`; THE STOP LAW verbatim: a differing recompute is a STOP + finding to m-9 — the constant is NEVER edited to whatever the build emits.** The package test asserts the constant equals the LITERAL doc pin AND `Digest(ExpectedIdentities())` reproduces it. The A14 manifest keys stay a different surface. The seam battery follows by reference (zero seam bytes); the runtime self-check follows.
- **T2 — the two-derivation-point wiring fix (carried verbatim from plan-7):** extract the composition's derivation into a NAMED production helper in `cmd/frank-app` — the manifest `tool_set` + member digest derived from `catalog.ExpectedIdentities()` (the REAL values; the fabricated `sha256("schema:"+name)`/`"mvp-v1"` values GONE), the gate comparand injected at a SEPARATE named site in `productionStarter.Start`'s path from `catalog.ExpectedDigest`. **The regression (in `cmd/frank-app`'s package test) drives the PRODUCTION helper/path and JOINTLY proves: (i) member + `tool_set` from `catalog.ExpectedIdentities()`; (ii) the comparand from `catalog.ExpectedDigest`; (iii) forced divergence AT THAT PRODUCTION SEAM returns `manifest.ErrServeGate`. Hand-built `BuildInput`/`Gate` values are INSUFFICIENT (the generic leg exists at `manifest_test.go` and proves nothing about the composition). THE BASELINE-RED CAPTURE: the regression runs against the PRE-change wiring first, its failing leg recorded (at the tautology, seam divergence is undetectable — the gate passes), then GREEN after; the flip rides the close SITREP.** The LIVE presented-surface digest stays ONLY on `logical_surface_digest`; the composed battery + WP1-evidence assertions update to the STATIC member value, honestly labeled.
- **T3 — the canonical release pipeline (R2-MR-1 + R2-MR-2 closed):** `frank/scripts/release-build.sh`.
  - **The toolchain pin (LOCKED; R2-MR-1):** the script carries the exact literal **`go1.26.4`** (master's durable ruling `050242` — the ratifying citation rides the script's header comment) as the independent comparison source and FAIL-CLOSES before any build byte: `go env GOVERSION` must equal the literal, else abort + finding UP (never a local toolchain swap). ONE implementation contract; the pin moves only by governed amendment through the ratifying chain. The effective-input set: FIXED by the script — `GOFLAGS=""` (explicitly set), the literal `-trimpath`, the literal build command `go build -trimpath -o "$STAGING/<name>" ./cmd/<name>` for exactly the four members; RECORDED in the manifest per the canonical recipe below.
  - **The canonical RELEASE-MANIFEST (R2-MR-2):** ONE canonical JSON artifact named `RELEASE-MANIFEST.json`, byte-fixed as: a single JSON object, serialized in JCS canonical form (RFC 8785 — sorted member names, no insignificant whitespace, UTF-8), followed by EXACTLY ONE trailing LF. Members, closed: `"build_command_template"` = the literal string `go build -trimpath -o $STAGING/<name> ./cmd/<name>` (the RECIPE TOKEN `$STAGING` verbatim — NO absolute or invocation-specific path ever enters the artifact); `"cgo_enabled"` = the verified effective value as a string; `"goarch"`, `"goos"` = the verified values; `"goflags"` = `""`; `"goversion_verified"` = the build-time `go env GOVERSION` output (which the fail-close guarantees equals the pin); `"members"` = an array of exactly four objects `{"name": <binary name>, "sha256": <lowercase-hex digest>}` SORTED BY `name` byte order; `"toolchain_pin"` = `"go1.26.4"`. NO timestamps, NO paths, NO hostnames, NO other invocation-specific byte. A repeat invocation on the same environment with byte-identical binaries reproduces the artifact BYTE-EXACTLY; **idempotence at the dist/ target is EXACT-BYTE comparison of this canonical artifact and the four binaries — no semantic comparison.**
  - **Fail-closed non-destructive publication (carried from plan-7):** build the full four-member set TWICE into TWO FRESH staging directories under a script-created temp area (never `dist/` directly); compare corresponding per-binary SHA-256s — ANY inequality aborts; generate the canonical manifest from the verified digests; publish only the verified set into `dist/<goos>-<goarch>/`: absent target ⇒ create + publish; existing target equal to the EXACT verified five-entry set (byte-equal) ⇒ leave unchanged (idempotent PASS); anything else ⇒ STOP with a finding — NO deletion, NO overwrite. The final census must equal exactly `{frank-app, frank-worker, frank-connector, frank-broker, RELEASE-MANIFEST.json}`; extras/missing = failure. `frank` (F65) and `frank-mcp` (excluded) are NOT members; NO binding claim anywhere; the four-member manifest `build_digests` grammar stays Q-CARRIER at the binding act.

## §4 — Acceptance criteria

1. T1: recompute AND constant both equal the literal `7fae5fc1…`; the STOP law honored if not; zero seam bytes; census 64/0/64; runtime self-check green.
2. T2: the PRODUCTION-seam regression jointly proves the three legs with the PRE-change baseline-RED captured and the post-change GREEN shown; the fabricated identity values gone; the live digest only on `logical_surface_digest`; the composed run passes with member = pin.
3. T3: the fail-closed pin check green at `go1.26.4`; the two-staging double-build digest-equality proof green; the canonical `RELEASE-MANIFEST.json` byte-reproducible on a repeat invocation (shown); the published census exactly the five entries with the idempotent-or-STOP law honored; no binding claim.
4. The regression floor green at EVERY commit; the draft PR refreshed (V30); findings UP never absorbed; WP1/WP2's banked claims stand (forward-only).
5. The WP2b-close SITREP: per-item evidence, the T1 equality, the T2 flip, the T3 pin/determinism/reproducibility/census proofs, the honest not-done (no binding, no WP3), findings; then my corroboration and the close routes UP.

## §5 — Out of scope (carried whole)

Any "release bound"/"binding-ready" claim; the RELEASE-BINDING act and the Q-CARRIER grammar; any edit to the worker design doc, the `:144` pin, or ANY m-x design/lock byte; any pin movement outside the governed-amendment chain; `frank/test/seam/**` (zero bytes); every §2-OUT surface; deleting or overwriting any existing `dist/` content; live/external E3, admissibility, exit legs (WP3); budget/conformance (WP4); gap register/battle report/close (WP5); merge (operator-only); new dependencies; any push beyond the slice branch + draft PR.

## §6 — Sequencing (the V32 law)

Your PLAN-REVIEW verdict TO me, parented to this relay, over this plan's exact hash. On approve: I run the mechanical `SCOPE_DIFF` against §2 (the ★ extension rows listed) and route the TOKEN REQUEST UP — implementation begins ONLY on master's fresh direct dispatch TO `s16.implementer` carrying the approved fence INCLUDING the extension (the ratification and the pin ruling are both COMPLETE; nothing else gates the dispatch). No catalog, app, manifest, test, script, gitignore, dist, build, branch, push, PR, binding, or merge act before it. Spec-base defects found while realizing are escalations UP to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (the pin ruling read in full; the carried closures cross-checked against both reviews); no source, test, script, branch, commit, push, PR, merge, build, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
?? frank/.relays/s16/s16-wp2b/PLAN-planner-20260828-051127.md
