## PLAN — WP2b (s16-wp2b-plan-6): the THREE ruled work items — the catalog locked-key serialization fix (expected `7fae5fc1…`, STOP-if-differs) · the two-derivation-point F58 gate wiring fix (the tautology killed, RED-leg-proven) · the canonical `-trimpath` four-member release pipeline into `dist/` + RELEASE-MANIFEST; the V32 law governs — no code byte before master's fresh direct dispatch

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2b-plan-6
PARENT_DISPATCH_ID: s16-wp2b-open
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the token remains master's fresh direct dispatch; the RELEASE-BINDING stays the separate Master+VP act; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — the three items realize the banked owner rulings (m-9's Q-CATALOG lead ruling, m-2's keys-only proof, m-10's two-derivations law, the ratified+co-signed Q-F63 set); no design decision is opened at this seat
IN_REPLY_TO: s16-wp2b/RECONCILE-orchestrator-planner-20260828-023804.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-9.planner, m-2.planner, m-10.planner, m-8.planner
SUBJECT: WP2b plan-6 — T1 catalog fix (locked long keys, constants-stay-constants, recompute ⇒ 7fae5fc1 with the never-edit-the-constant STOP law; the seam helper and runtime self-check follow BY REFERENCE, zero seam bytes — verified); T2 wiring fix (member from the composition's own derivation over the shipped identity vector, comparand injected from catalog.ExpectedDigest, the anti-tautology RED regression mandatory, battery/evidence expectations updated honestly); T3 the ratified pipeline (scripts/release-build.sh + frank/.gitignore = the NAMED fence extension; four members; double-build digest-equality proof; no binding claim); on your approve + all-in SCOPE_DIFF the token request routes UP (m-9's co-sign has landed, the dispatch may issue)

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential, capability, or custody surface is touched by any item (evidence = §5)
- migration/backfill/destructive-write/canonical-data-repair: no — no store schema, data repair, or destructive operation; the constants/wiring changes are source-level (evidence = §5)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — F58/F63 identity values and the serve-gate binding are trust-critical state feeding the binding act and WP3 applicability (evidence = §3/§4)
- AI-or-automation-acts-downstream: yes — the corrected gate and pipeline outputs are consumed by the Master+VP binding act and later runs (evidence = §4)
- worker/scheduler/queue/retry/async-side-effect: yes — the composed battery re-runs as the T2 verification instrument (evidence = §5 T2)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — F58's locked encoding, the A14 manifest keys, and the gate comparand are cross-owner contracts realized here under the owners' exact rulings (evidence = §3)
- user-visible-control-with-materializer/downstream-consumer: yes — the release pipeline's dist/ + RELEASE-MANIFEST are the binding act's inputs (evidence = §5 T3)
- test-runtime-role-mismatch: no — T2's regression makes the gate bind the real surfaces; every expectation update is to the STATIC contract value, honestly labeled (evidence = §5 T2)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the fence EXTENDS beyond plan-5 §2.4 by the two NAMED rows (the release script + frank/.gitignore); the extension rides the SCOPE_DIFF and master's dispatch carries it explicitly (evidence = §2)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — The governing basis (cite by path; on conflict the owners' bytes govern)

- The WP2b opening `s16-wp2b/RECONCILE-orchestrator-planner-20260828-023804.md` (the three work items + the V32 law + the rails).
- **The owner rulings (all read in full at this seat):** m-9's Q-CATALOG lead ruling `master/relays2/s16-wp2-owners/SITREP-planner-20260828-022110.md` (§1(a)–(d): `7fae5fc1…` is the contract comparand derived from the FULLY STATIC locked inputs; the LOCK's row keys govern F58 — the A14 manifest keys are a DIFFERENT surface, never unified; live `form_digest` is contract-FORBIDDEN in F58, it belongs to `logical_surface_digest`/INV-E1; the fix sequence with the never-edit-the-constant STOP law) · m-2's keys-only PROOF `…DESIGN-planner-20260828-022111.md` (BOTH digests reconstructed byte-exact from IDENTICAL member values — long keys ⇒ `7fae5fc1…`, shipped short tags ⇒ `151a7e61…`; zero value drift; the §2 recipe repeatable at any seat) · m-10's consumer confirm + tautology finding `…SITREP-planner-20260828-022057.md` (`main.go:126/:134` one variable on both sides of `gate.go:57` — the F84 tautological-comparand class, the gate VACUOUS on this member; the TWO-INDEPENDENT-DERIVATIONS law; structural acceptance: pin-equality against an injected comparand, only the wiring moves, plus a rename if "Shipped" needs one) · m-8's Q-F63 half `…SITREP-planner-20260828-022252.md` RATIFIED at `s16-wp2-disp` + **m-9's CO-SIGN `master/relays2/s16-wp2-disp/SITREP-planner-20260828-024556.md` (landed; the ratification is COMPLETE — the four-member set {frank-app · frank-worker · frank-connector · frank-broker}, `-trimpath` + pinned-toolchain build-instance identity, `dist/<goos>-<goarch>/` + RELEASE-MANIFEST, `frank`=F65/`frank-mcp` excluded)**.
- The standing state: the banked WP1 object at head `36dbaca5…` (branch `s16-integration`, draft PR #2); the WP2 evidence package + corroboration (NON-BINDING, as filed); plan-5(WP1) @ `e64e6077…` whose §2.4 fence this plan EXTENDS by exactly two named rows (§2); the regression floor (seam 64/0/64 + plain suite + vet at every commit).
- **Pre-verified consumption facts (this seat, at the banked head):** `frank/test/seam/helpers_test.go:466` consumes `workercatalog.ExpectedDigest` BY REFERENCE on BOTH sides of its manifest/gate construction — the corrected constant propagates with ZERO seam bytes; `frank/internal/worker/runtime/runtime.go:134` self-checks `Digest(ExpectedIdentities()) == ExpectedDigest` — both sides move together under T1; `frank/scripts/` exists (`dogfood-suite.sh`, untouched); NO `frank/.gitignore` exists (T3 creates it).

## §2 — Scope and the EXACT fence (the SCOPE_DIFF comparand; ★ = the extension beyond plan-5 §2.4)

- `frank/internal/worker/catalog/**` (T1: the builder serialization + the recomputed constant + the package tests).
- `frank/cmd/frank-app/**` (T2: the two-derivation wiring + the real identity-vector derivation).
- `frank/internal/appctl/manifest/**` (T2: ONLY the possible `ShippedToolCatalogDigest` rename m-10's acceptance names; the member grammar and gate structure are UNTOUCHED).
- `frank/test/composed/**` (T2/T3 verification: the anti-tautology regression, the honest expectation updates, the pipeline double-build check if test-shaped).
- ★ `frank/scripts/release-build.sh` (T3 — the new canonical build script; `dogfood-suite.sh` and every other script byte OUT).
- ★ `frank/.gitignore` (T3 — created, carrying the `dist/` ignore; nothing else).
- `frank/go.mod`/`frank/go.sum` arbitration-only (no new dependency; needing one = stop and escalate) · the relay root via the engine.
- **Explicitly OUT:** `frank/test/seam/**` — ZERO bytes (the by-reference propagation is verified; ANY seam byte = STOP, the SEAM-BATTERY-S16-G03-1 era rails stand) · `frank/internal/worker/**` beyond `catalog/` (the `runtime.go:134` self-check follows by reference — no edit) · the four T9 connector loci and all of `internal/connector/**` (WP2b touches none) · `frank/internal/broker/**`, `frank/internal/appipc/**`, `frank/cmd/{frank,frank-broker,frank-connector,frank-worker,frank-mcp}/**` · `master/**` · the frozen exit corpus · every m-x design/lock byte (the doc pin `:144` and the shipped constant relation is realized in CODE; the lock is NEVER edited from this lane).
- `dist/**` is BUILD OUTPUT: untracked (via ★ `.gitignore`), never committed, not a repository surface.

## §3 — The three work items (mechanics yours; the contracts are the owners' — bind their exact terms)

- **T1 — the catalog serialization fix (`internal/worker/catalog`):** the F58 identity rows serialize under the LOCK's row member keys — `{canonical_name, tool_schema_digest, tool_impl_catalog_version, form_schema_mapping_version?}` — absence-encoded on the five locals, present (`m2-mapping-v1`) on the three relay rows, sorted by `canonical_name` byte order, m-2 §3.2 canonical encoding/JCS, SHA-256. The member VALUES do not move: m-2's three produced digests stay source CONSTANTS (never a render, never a store read — they already are; keep it PROVABLY so: the package imports no store/render surface), m-9's five local digests and both version markers stay as pinned. `ExpectedDigest` is then RECOMPUTED from the corrected builder. **Expected outcome: equality with `7fae5fc1dd8f91c48828beaf0cfba45a1da4c297bf82f790ec2912b0a168c9d4` — m-2's §2 reconstruction PROVES this outcome for the correct fix. THE STOP LAW, verbatim: if the corrected recompute differs from the pin, that is a STOP + finding to m-9 (the defect would be in the §8.3 pinned arithmetic, returning as a bounded owner successor) — the constant is NEVER edited to whatever the build emits.** The package test asserts the constant equals the LITERAL doc pin (an independent comparand, not a re-derivation — the anti-tautology discipline applies to the test too) AND that `Digest(ExpectedIdentities())` reproduces it. The A14 manifest keys (`name`/`schema_digest`/…) stay exactly where they are — a DIFFERENT surface (m-9 §1(b)); nothing unifies them.
- **T2 — the two-derivation-point wiring fix (`cmd/frank-app` + the possible manifest rename):** the manifest's `tool_catalog_digest` member AND its `tool_set` derive from the COMPOSITION'S OWN derivation over the SHIPPED identity vector (`catalog.ExpectedIdentities()` mapped to the A14 manifest members — the real schema digests, `m9-catalog-v1`, `m2-mapping-v1` on relay rows — KILLING the fabricated `sha256("schema:"+name)`/`"mvp-v1"` values); the GATE comparand is INJECTED from the shipped pin `catalog.ExpectedDigest` — TWO INDEPENDENT DERIVATION POINTS, so `gate.go:57` binds again (m-10's structural acceptance: only the wiring moves; rename `Shipped…` only if the name stops being true). The LIVE presented-surface digest stays ONLY on `logical_surface_digest` (attempt grain, INV-E1) — out of the manifest member, out of the gate. **The anti-tautology RED regression is MANDATORY:** a test proving a manifest member ≠ the injected pin FAILS the serve gate (the gate's binding made observable — without this leg the fix ships unproven). The composed battery and the WP1-evidence assertions update to the STATIC member value (`7fae5fc1…` post-T1) — in-scope, honestly labeled as expectation updates tracking the ruled contract, never as evidence rewrites.
- **T3 — the canonical release pipeline (the ratified shape; ★ the fence extension):** `frank/scripts/release-build.sh` — the canonical build step: asserts the PINNED Go toolchain (the recorded version must equal the live `go version`; a mismatch fails the build), `go build -trimpath` per `GOOS/GOARCH`, emitting the FOUR ratified F63 members {`frank-app` · `frank-worker` · `frank-connector` · `frank-broker`} into `dist/<goos>-<goarch>/`, plus the RELEASE-MANIFEST (the toolchain version · the exact build flags · per-binary lowercase-hex SHA-256). **The build-instance identity proof: the script (or the composed-package check) runs the build TWICE on the same environment and asserts per-binary digest equality — the `-trimpath`+pinned-toolchain determinism made observable, not asserted.** `frank` (F65) and `frank-mcp` (excluded) are NOT members. **NO binding claim anywhere — the pipeline PRODUCES what the SEPARATE Master+VP binding act will bind; the manifest four-member `build_digests` grammar (the broker member) is Q-CARRIER, m-10's at the binding act — NOT realized here.**

## §4 — Acceptance criteria (WP2b done means exactly this)

1. T1: the corrected builder's recompute AND the package constant BOTH equal the literal `7fae5fc1…`; the STOP law honored if not; the seam battery follows by reference with ZERO seam bytes (census stays 64/0/64); the runtime self-check green.
2. T2: the RED regression proves the gate fails on a mismatched member; the composed run passes with member = pin = `7fae5fc1…`; the fabricated identity values are gone from the manifest `tool_set` (the real static vector present); the live digest appears ONLY on `logical_surface_digest`.
3. T3: `dist/<goos>-<goarch>/` carries exactly the four members + the RELEASE-MANIFEST; the double-build digest-equality proof green; no binding claim in any output or relay.
4. The regression floor green at EVERY commit; the draft PR refreshed (V30); every finding routed UP never absorbed; WP1/WP2's banked claims stand at their filed grain (forward-only).
5. The WP2b-close SITREP (implementer → planner, CC master + the owner roster): per-item evidence (commands + values), the T1 equality shown, the T2 RED leg shown, the T3 manifest + determinism proof shown, the honest not-done (no binding, no WP3), findings if any; then my corroboration and the close routes UP.

## §5 — Out of scope (a touch = escalation by construction)

Any "release bound"/"binding-ready" claim; the RELEASE-BINDING act and the Q-CARRIER manifest grammar (Master+VP / m-10 at the binding act); any edit to the worker design doc, the `:144` pin, or ANY m-x design/lock byte; `frank/test/seam/**` (zero bytes); every surface §2 lists OUT; live/external E3, admissibility, exit legs (WP3); budget/conformance (WP4); gap register/battle report/close (WP5); merge (operator-only); new dependencies; any push beyond the slice branch + draft PR.

## §6 — Sequencing (the V32 law)

Your PLAN-REVIEW verdict TO me, parented to this relay, over this plan's exact hash. On approve: I run the mechanical `SCOPE_DIFF` against §2 (the extension rows listed explicitly) and route the TOKEN REQUEST UP — **implementation begins ONLY on master's fresh direct dispatch TO `s16.implementer` carrying the approved fence INCLUDING the ★ extension** (the opening's own instruction; m-9's Q-F63 co-sign has LANDED, so the one stated pre-dispatch condition is met). No code byte, no session byte, no script byte before that dispatch. Spec-base defects found while realizing are escalations UP to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this plan drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (the four owner rulings + the co-sign read in full; the seam by-reference consumption, the runtime self-check, the scripts dir, and the absent .gitignore verified at the banked head); no source, test, script, branch, commit, push, PR, merge, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M master/relays2/INDEX.md
?? master/relays2/s16-wp2-disp/SITREP-planner-20260828-024556.md
