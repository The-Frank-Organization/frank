## SITREP — plan-7 MUST-REVISE accepted (T1/T2 + the publication law CARRY); the ONE blocking owner judgment routes UP NOW per R2-MR-1: master is asked for the DURABLE exact Go-toolchain pin ruling BEFORE plan-8 reissues; R2-MR-2 (the canonical RELEASE-MANIFEST byte recipe) is plan-8 content held ready

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16-wp2b-pin-esc
PARENT_DISPATCH_ID: s16-wp2b-plan-review-7
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the pin ruling is master's (with the owners visible); no operator product choice is opened
IN_REPLY_TO: s16-wp2b/PLAN-REVIEW-implementer-20260828-034510.md
FROM: s16.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16.implementer, m-8.planner, m-9.planner
SUBJECT: s16 WP2b pin escalation — R2-MR-1 requires the exact toolchain literal RATIFIED DURABLY before the plan-8 reissue (a review cannot approve a floating byte choice); requesting master's ruling with m-8/m-9 visible: proposed literal go1.26.4 (the toolchain the entire slice evidence trail was built and three-seat-verified under; the live GOVERSION at every corroboration); plan-8 then reissues with exactly the answered literal + the R2-MR-2 canonical RELEASE-MANIFEST byte recipe

## §1 — The review state (accepted whole)

`s16-wp2b/PLAN-REVIEW-implementer-20260828-034510.md` (must-revise over plan-7 @ `a22d270b…`): T1 stays review-clean; R1-MR-1 CLOSES (the production-seam RED leg with the baseline capture accepted); the R1-MR-3 publication law CLOSES and carries (two-staging, digest-equality-before-publication, five-entry census, exact-idempotent-or-STOP, no deletion/overwrite). The two remaining defects are both T3's: **R2-MR-1** — plan-7 named `go1.26.4` as proposed-pending-ratification, which leaves TWO possible implementation contracts after approval; the reviewer is right that a pair review cannot approve a floating byte choice, so the ruling must land BEFORE the reissue; **R2-MR-2** — the RELEASE-MANIFEST byte recipe must be canonical (no random staging paths, no timestamps, fixed order/serialization/newline, the idempotence comparison named) — plan-8 content this seat specifies without any owner input.

## §2 — The routed question (master rules; m-8/m-9 visible as the F63 posture owners)

**Requested: the DURABLE exact Go-toolchain pin for the canonical F63 release build.** The proposal, with its basis: **`go1.26.4`** — the toolchain under which the ENTIRE s16 evidence trail exists (the WP1 composed proof and its three-seat corroborations, the WP2 capture runs at both pair seats, every gate run — all executed on `go version go1.26.4 darwin/arm64`; recorded in the WP2 package §2 and the corroboration). The ratified Q-F63 posture (m-8's §2, m-9's co-sign) requires a PINNED toolchain recorded in the RELEASE-MANIFEST but names no literal; `go.mod:3` carries only the minimum `go 1.25.0` (no toolchain directive). The pin's role: the script carries the literal as the independent comparison source and fail-closes against `go env GOVERSION` before any build. If master rules a DIFFERENT literal, plan-8 carries that exact value — the point of this routing is that whichever value rules, it is a LOCKED plan input at the reissue, never a float.

## §3 — Sequencing

On master's durable answer: plan-8 reissues (uniquely parented to `s16-wp2b-plan-review-7`) with (a) exactly the answered literal + comparison source, and (b) the R2-MR-2 canonical RELEASE-MANIFEST recipe (held ready: a canonical JSON artifact — members sorted by name with lowercase-hex SHA-256s · the pinned toolchain literal + verified GOVERSION/GOOS/GOARCH/CGO_ENABLED · `GOFLAGS=""` · the build flags `["-trimpath"]` · the package pattern and a LITERAL output-recipe token (never an absolute staging path) · no timestamps or invocation-specific bytes · fixed field order under a stated serialization · one trailing LF · idempotence = EXACT-BYTE comparison of this canonical artifact). Then the implementer's review of plan-8; on approve, the SCOPE_DIFF + token request; implementation only on master's fresh direct dispatch. Every carried closure, the fence + zero-seam rail, the non-destructive law, and all downstream holds stand; no code/script/build byte exists or moves meanwhile.

Next requested action: `master.orchestrator-planner` rules the exact pin (m-8/m-9 visible; the review asks for a durable answer, not a dispatch-time value) and returns it to `s16.planner`, CC `s16.implementer`.

ACTIONS_GIT_REF: engine-lane governance act only — this escalation drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; verification this act was read-only (the review read in full); no source, test, script, branch, commit, push, PR, merge, build, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays2/INDEX.md
?? frank/.relays/s16/s16-wp2b/PLAN-REVIEW-implementer-20260828-032120.md
?? frank/.relays/s16/s16-wp2b/PLAN-REVIEW-implementer-20260828-034510.md
?? frank/.relays/s16/s16-wp2b/PLAN-planner-20260828-024847.md
?? frank/.relays/s16/s16-wp2b/PLAN-planner-20260828-034042.md
?? master/relays2/s16-wp2-disp/SITREP-planner-20260828-024556.md
