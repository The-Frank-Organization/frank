## PLAN — WP2 REISSUE (s16-wp2-plan-4): the F63/F65 identity-evidence preparation — R3-MR-1..4 closed (the temp binaries labeled NON-BINDING run witnesses with the missing canonical build-pipeline artifact set ROUTED to master; the decoder eliminated — the shim emits the manifest fields it already holds; a closed positive non-secret capture allowlist with a credential canary; mechanically safe teardown); every carried correction, routed question, and hold preserved

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2-plan-4
PARENT_DISPATCH_ID: s16-wp2-plan-review-3
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E3
HUMAN_GATE_REQUIRED: no — the pair prepares evidence; the RELEASE-BINDING is Master+VP's separate act; the newly-routed canonical-artifact question is master's to direct; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — bounded execution corrections; FOUR routed questions now stand (§3.4 finding · §3.6 config grain · §3.3 broker classification · §3.3R the canonical build-pipeline artifact set), all master's/the owners', never decided here
IN_REPLY_TO: s16-wp2/PLAN-REVIEW-implementer-20260828-005113.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner, m-2.planner, m-3.planner
SUBJECT: WP2 plan-4 — R3-MR-1 closed by the review's path (1): the run-built binaries are NON-BINDING same-run witnesses (no determinism asserted; the cross-run digest divergence of non-trimpath random-root builds DISCLOSED as expected and as the motivation), the F63-required canonical build-pipeline artifact set ROUTED to master, the source-substitution suggestion DELETED; R3-MR-2 closed (no decoder helper — the shim emits tool_catalog_digest + tool_set from the manifest bytes the test already reads at turn_test.go:286-312); R3-MR-3 closed (the closed positive capture allowlist: five binaries + three configs + two shim-emitted evidence files, NO store/binding/broker/credential surface, plus the in-process credential canary); R3-MR-4 closed (mktemp -d creation, recorded absolute target, non-empty/non-symlink/owner/prefix guards, quoted `rm -rf -- "$SESS"`, post-check on the recorded literal)

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — the §3a capture set is a CLOSED POSITIVE ALLOWLIST containing no credential-bearing surface (no conductor `binding/`, no broker home, no credential artifact, no key material), enforced by an allowlist assertion + the in-process credential canary (§3a.2v) (evidence = §3a)
- migration/backfill/destructive-write/canonical-data-repair: no — the only recursive deletion is the §3a.5 guarded, quoted, recorded-target teardown of the session root this plan itself created (evidence = §3a.5)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the F63/F65 identities determine the release and relay-exchange evidence applicability (evidence = §1/§3)
- AI-or-automation-acts-downstream: yes — Master+VP and the WP3 applicability gate consume the package (evidence = §4)
- worker/scheduler/queue/retry/async-side-effect: yes — the §3a capture run executes the five-process composed runtime once (evidence = §3a)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — F58/F63/F65 are locked cross-owner contracts; row 4 carries the LIVE inequality finding and §3.3R the artifact-pipeline gap (evidence = §3.4/§3.3R)
- user-visible-control-with-materializer/downstream-consumer: yes — the release-binding and applicability evaluators consume the exact vector (evidence = §4)
- test-runtime-role-mismatch: no — the same-run join holds (shim-hashed = executed), and NO determinism or binding grade is claimed for the run-built witnesses (evidence = §3.3/§3a)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the canonical-artifact set, the broker classification, the config grain, and the GOMODCACHE residue reading are ROUTED/declared, never inferred; no scope expansion is exercised under this plan (evidence = §3.3R/§2)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — Basis (carried; the token class unchanged)

- Plan r7 @ `044fe4cc…` §1(ii)/§2 WP2; NO "release bound" claim ever; `tool_catalog_digest` mechanically verified; F65 SEPARATE. The banked object: head `36dbaca5…` · tree `c31ac08e…` · base `77f8c9db…` · draft PR #2; plan-5 @ `e64e6077…`.
- The three reviews of record (`001759` · `003433` · `005113` over plan-3 @ `314f48cd…`); each R3 finding re-verified at this seat before this reissue (F63's exact-built-artifact/reproducible-release-digest requirement at the MVP amendment + m-8 §7 `m8_build_digest` + m-9 §8.2 `m9_worker_build_digest`; the Go internal-package placement rule; `internal/seat/binding.go`'s clear-JSON credential persistence at `binding/seats.json`; the unguarded-delete class).
- **Token class:** ZERO repository/product/worktree bytes move; NO implementation token is requested; trigger-present governs ceremony only. The §3.3R routing explicitly does NOT exercise any scope expansion — if master rules path (2) (a durable artifact store), THAT ruling carries its own fresh direct dispatch; nothing here anticipates it.

## §2 — Scope: the write classification (carried from plan-3; unchanged in substance)

- Repository/product/worktree writes: NONE outside `frank/.relays/s16/**`. The worktree verified at exact `36dbaca5…` pre-run (moved head = STOP); clean-tree proofs BEFORE and AFTER.
- The enumerated ephemeral class, all inside `$SESS` (§3a.5's creation/teardown discipline): the module copy + shim; `GOCACHE=$SESS/gocache`, `GOTMPDIR=$SESS/gotmp`; the capture run's private runtime root (the test's own `/tmp/frank-s16-e3-*`, removed by the test's own cleanup) and `$SESS/capture/**`; the auxiliary `frank-mcp` build output. GOMODCACHE: the user's, READ-consumed; any module write = disclosed residue (unexpected ⇒ STOP). Effective `go env` values recorded.

## §3a — THE CAPTURE RUN (R3-MR-2/3/4 closed; one run of record; the closed allowlist)

1. **Session creation (R3-MR-4):** `SESS=$(mktemp -d "${TMPDIR:-/tmp}/s16-wp2-evidence.XXXXXX")`; the RESOLVED ABSOLUTE path recorded immediately in the report and reused literally thereafter.
2. **The copy + shim:** `git archive 36dbaca5 frank | tar -x -C "$SESS/src"` (pure object-store read; the copy carries NO git metadata — noted for §3.3's `-buildvcs` disclosure). The shim modifies `$SESS/src/frank/test/composed/turn_test.go` ONLY (full diff verbatim in the report), additively: (i) after `buildProductionBinaries` — copy the five binaries to `$SESS/capture/bin/` + SHA-256, logging `name → executed-path → digest`; (ii) after `initializeConductor` — copy the exact three config inputs (`engine.json` · the copy's `internal/fieldspec/registry.json` · the copy's `test/invariants/catalog.v1.json`) to `$SESS/capture/config/`; (iii) inside `assertPersistedAppEvidence` (which already holds `manifest_bytes`, `turn_test.go:286-312`) — decode via the module's own decoder IN-TEST and write `$SESS/capture/manifest-fields.json` carrying EXACTLY the two non-secret members `{tool_catalog_digest, tool_set}` plus the manifest digest for join, and write the RUN_IDENTITY line to `$SESS/capture/identity.txt`; **NO separate decoder helper exists — R3-MR-2's placement problem is dissolved, and NO sqlite/state-directory copy is made**; (iv) no assertion, build command, or execution path changes — the test must PASS WHOLE; (v) **the credential canary:** the shim, holding the run's minted credential values in-process, ASSERTS before test end that no captured file's bytes contain any of them (and never persists the credentials themselves); the assertion result rides the report.
3. **The CLOSED POSITIVE capture allowlist (R3-MR-3):** `$SESS/capture/` contains EXACTLY: `bin/frank` · `bin/frank-app` · `bin/frank-broker` · `bin/frank-worker` · `bin/frank-connector` · `config/engine.json` · `config/registry.json` · `config/catalog.v1.json` · `manifest-fields.json` · `identity.txt` — TEN entries. The shim asserts the capture tree equals this list exactly (an extra or missing path fails the run). NO conductor `binding/` surface, NO broker home, NO credential or connector-credential artifact, NO key material, NO store tree, NO journal is captured — none is needed: every state-derived evidence value crosses as a shim-emitted FIELD, not a copied store byte.
4. **Execution:** `cd "$SESS/src/frank" && GOCACHE="$SESS/gocache" GOTMPDIR="$SESS/gotmp" go test -count=1 ./test/composed -run TestWholeAppGovernedTurnProducesJoinedE3Evidence -v` — hashed = executed by the same-run shim join (carried from plan-3).
5. **Teardown (R3-MR-4):** guards on the RECORDED absolute `$SESS` before deletion — exists · non-empty · not a symlink · owned by the current user · matches the literal prefix `${TMPDIR:-/tmp}/s16-wp2-evidence.` — then `rm -rf -- "$SESS"` (quoted, `--`-guarded); the post-delete existence check runs against the recorded literal path, never a recomputed variable or glob. The test's own cleanup removes its runtime root; both clean-tree captures close the run.

## §3 — The evidence package (rows carried; R3-MR-1's corrections applied)

1. **Source identity (E1):** head `36dbaca5…` · tree `c31ac08e…` · the five subtree ids · base lineage · PR #2 head OID/state.
2. **Toolchain + build environment (E1):** `go version`; the `go env` set; the capture run's EFFECTIVE recipe stated as it truly is — the test's plain `go build -o`, no `-trimpath`, `-buildvcs` at its default IN A TREE WITHOUT GIT METADATA (so no VCS stamp), absolute session paths embedded ⇒ **the outputs are RUN-SCOPED bytes**.
3. **The artifact matrix — NON-BINDING same-run witnesses (R3-MR-1, path (1)):** the five executed binaries' digests from the §3a shim, each row labeled **`NON-BINDING RUN WITNESS`**: they prove exactly WHICH bytes the composed E3 run executed (the same-run join), and NOTHING about durability, determinism, or F63 binding grade — no determinism is asserted, and **the expected CROSS-RUN digest divergence of non-trimpath random-root builds is DISCLOSED up front** (it is the mechanical demonstration of WHY the routed §3.3R gap exists). Classes carried: frank-app/worker/connector = F63-candidate SLOTS (their binding-grade artifacts do not yet exist — §3.3R); frank = the F65 slot, separate; frank-broker = classification ROUTED; frank-mcp = auxiliary, non-executed, hashed under a stated plain recipe. **The plan-3 suggestion that source identity may substitute for exact build digests is DELETED — F63 has no such branch.**
   - **§3.3R — the ROUTED canonical-artifact question (new, to master):** F63 requires exact digests of the BUILT app-main/m-9-worker/m-8-connector artifacts produced by the canonical build pipeline (m-8 §7 `m8_build_digest`; m-9 §8.2 `m9_worker_build_digest`), or one transitively reproducible release digest — and NO canonical T4 build-pipeline artifact set or durable artifact location EXISTS in the banked object. The package therefore CANNOT be binding-ready on its own; master is asked to direct the missing piece (the review's path (2) shape: a deterministic canonical recipe + a durable content-addressed artifact location/lifecycle preserving the exact bytes through the binding act and the later bound run — a scope expansion needing owner shape + a fresh direct dispatch; it composes with the row-8 carrier question and the row-4 finding, all plausibly one directed WP2b/WP3-adjacent task). Until that returns, WP2's pair half delivers everything deliverable read-only and states this gap as its headline routed item.
4. **`tool_catalog_digest` — the THREE-comparator verification (carried whole):** (a) the SHIPPED m-9 registry digest `151a7e61…` (E2, derivation + green test); (b) the LOCKED expected `7fae5fc1…` (E1); (c) the manifest/gate vector from the §3a.2(iii) shim-emitted `manifest-fields.json` (E3). Three distinct inputs; the reproduced inequalities are a FINDING/STOP ROUTED UP (m-9 · m-2 · m-10 · master); never normalized by the pair; the names-only hash only as the labeled policy identity.
5. **The composed-run identity set (E3):** the §3a run's `identity.txt` verbatim + `manifest-fields.json` — one run of record.
6. **F65 — separate (E3 at the invocation rows):** the executed `frank` witness digest (labeled as row 3) + the conductor source surfaces at head (E1) + the three captured config files hashed individually + as an ordered vector, WITH the path-bearing members named (the engine config embeds the run-scoped module path — disclosed; cross-run comparison is shape-wise). The GRAIN question stays ROUTED (master/m-7); row 6 = delivered-with-routed-question.
7. **Module closure (E1):** `go.mod`/`go.sum` blob ids + `go.sum`'s SHA-256.
8. **The carrier question (routed, carried):** how the bound values enter the composed run's manifest — now explicitly COMPOSING with §3.3R and the row-4 finding at the same locus family; the binding act directs, the pair executes under a fresh direct dispatch.

## §4 — Deliverable, corroboration, sequencing

- **Deliverable:** ONE evidence SITREP (implementer → planner, CC this roster): every §3 row at its stated grain; the §3a shim diff verbatim + the path→digest log + the allowlist and canary assertions' results; the §2 proofs (clean trees · teardown against the recorded target · any residue disclosure); the row-4 FINDING and the FOUR routed questions (§3.3R headline). E3 at the composed-runtime rows; E1/E2 elsewhere; NOT WP3's external E3.
- **Planner corroboration:** rows 1/2/7 + 4(a)/(b) re-derived fully; the §3a run REPEATED independently at this seat — the deterministic values (config-file digests modulo the named path-bearing members · `manifest-fields.json` values · the identity-set members that are run-independent · the catalog comparators) must MATCH, and a mismatch in any value claimed run-independent is a STOP; the witness binary digests are EXPECTED to differ across runs (disclosed) and are compared only for the same-run join's internal consistency.
- **Acceptance:** every §3 row per its own terms; the §3a joining proof + allowlist/canary results present; the row-4 finding + all four routed questions stated; the §2 classification proven; the corroboration filed; the package UP. The BINDING is not an acceptance row, and the package is explicitly NOT claimed binding-ready (§3.3R).
- **Sequencing:** your PLAN-REVIEW verdict TO me, parented to this relay. On approve: the record `SCOPE_DIFF` (relay root only); NO token request; you prepare; I corroborate; the package + findings + routed questions go UP.

## §5 — Out of scope (carried whole)

Any "release bound" or "binding-ready" claim; ANY repository/product/worktree byte outside `frank/.relays/s16/**`; any pair-authored repair of the row-4 inequality, the row-8 carrier, or the §3.3R artifact gap (all wait on owner/master direction + fresh direct dispatches); any durable artifact store or persistent build output (path (2) is master's to direct, not this plan's to exercise); the RELEASE-BINDING act; the F65 grain and broker-classification rulings; live/external E3, admissibility, exit legs (WP3); budget/conformance (WP4); gap register/battle report/close (WP5); merge (operator-only); every standing plan-5 §9 fence.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (`turn_test.go:286-312` re-read; the R3 citations verified); no source, test, branch, commit, push, PR, merge, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-001759.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-003433.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-005113.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-001107.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-002348.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-004522.md
