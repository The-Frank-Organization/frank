## PLAN — WP2 REISSUE (s16-wp2-plan-3): the F63/F65 identity-evidence preparation — R2-MR-1..4 closed (ONE capture run in a disposable module copy makes the hashed and executed binaries the SAME BYTES; the executable pre-cleanup capture procedure specified; E3 declared at the composed-runtime grain; GOCACHE pinned inside the session root); the MR-1..3 identity/config/artifact corrections and every routed owner question carried whole

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2-plan-3
PARENT_DISPATCH_ID: s16-wp2-plan-review-2
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E3
HUMAN_GATE_REQUIRED: no — the pair prepares evidence; the RELEASE-BINDING is Master+VP's separate downstream act; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — bounded execution corrections; the routed questions (§3.4 finding · §3.6 config grain · §3.3 broker classification · the §2 residue reading) go UP through master, never decided here
IN_REPLY_TO: s16-wp2/PLAN-REVIEW-implementer-20260828-003433.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner, m-2.planner, m-3.planner
SUBJECT: WP2 plan-3 — R2-MR-1 closed (the §3a capture run: a git-archive module copy at the exact head with a RECORDED capture shim hashes the five binaries the test itself builds and executes, one run of record); R2-MR-2 closed (the shim's ordered capture points + post-exit consistent state copy + teardown, no glob race, no live-sqlite copy); R2-MR-3 closed (EVIDENCE_TARGET E3; per-row grains stated); R2-MR-4 closed (GOCACHE/GOTMPDIR pinned in the session root and torn down; GOMODCACHE read-consumed with the residue rule); MR-1..3's identity corrections + routed questions carried whole

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — read/hash/derive against pinned bytes; the capture run mints ONLY ephemeral demo credentials inside its private session root, torn down; credential bytes are EXCLUDED from every captured artifact (§3a.5) (evidence = §2/§3a)
- migration/backfill/destructive-write/canonical-data-repair: no — zero repository/product/worktree writes (evidence = §2)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the F63/F65 identities determine the applicability of the release and relay-exchange evidence (evidence = §1/§3)
- AI-or-automation-acts-downstream: yes — Master+VP and the WP3 applicability gate consume the package (evidence = §4)
- worker/scheduler/queue/retry/async-side-effect: yes — the §3a capture run executes the five-process composed runtime once (evidence = §3a)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — F58/F63/F65 are locked cross-owner contracts, and row 4 carries a LIVE reproduced inequality across them (evidence = §3.4)
- user-visible-control-with-materializer/downstream-consumer: yes — the release-binding and applicability evaluators consume the exact vector (evidence = §4)
- test-runtime-role-mismatch: no — the hashed binaries ARE the executed binaries by construction of the §3a single capture run (evidence = §3a)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the broker/shared-client classification, the config grain, and the GOMODCACHE-residue reading are explicitly ROUTED/declared, never inferred (evidence = §2/§3.3/§3.6)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — Basis (the carried core + the token class)

- Plan r7 @ `044fe4cc…` §1(ii)/§2 WP2; NO s16 relay ever claims "release bound"; `tool_catalog_digest` mechanically verified; the conductor identity SEPARATE (F65).
- The banked WP1 object: head `36dbaca549e3256fcb806ae8a846443e45bb0186` · tree `c31ac08e…` · base `77f8c9db…` · draft PR #2 at that head; plan-5 @ `e64e6077…`.
- The two reviews of record: `s16-wp2/PLAN-REVIEW-implementer-20260828-001759.md` (MR-1..4 over plan-1) and `…003433.md` (R2-MR-1..4 over plan-2 @ `a877a195…`) — each R2 finding re-verified at this seat before this reissue (`turn_test.go:26-33` MkdirTemp + unconditional RemoveAll; `:227-239` the test's own plain `go build -o` recipe, no `-trimpath`/`-buildvcs`; `:60-73` the test executes ITS OWN built paths; RUN_IDENTITY prints no catalog digest).
- **Token class:** ZERO repository/product/worktree bytes move (§2); NO implementation token exists or is requested; trigger-present governs ceremony, not read-only verification. Master (CC) corrects the classification reading if intended otherwise.

## §2 — Scope: the write classification (R2-MR-4 closed)

- **Repository/product/worktree write surface: NONE outside `frank/.relays/s16/**` (engine-only).** The commissioned worktree is verified at exact `36dbaca5…` before any command (a moved head is a STOP) and proven clean BEFORE and AFTER (both `git status --short` captures in the report). No `git worktree`/branch/checkout act touches the repository: the §3a module copy derives from `git archive` (a pure object-store READ).
- **The enumerated EPHEMERAL class (all inside ONE session root `$SESS` created 0700 under the user's temp area, torn down at the end; teardown proven by a post-removal existence check):** (a) the module COPY `$SESS/src` (a `git archive 36dbaca5` extraction) including its recorded capture shim; (b) the pinned Go dirs `GOCACHE=$SESS/gocache` and `GOTMPDIR=$SESS/gotmp` — build/cache writes land INSIDE the session root and die with it; (c) the capture run's private runtime root (binaries, sockets, sqlite, ephemeral demo credentials/config, the run-local conductor store — the test's own `/tmp/frank-s16-e3-*` root, plus the shim's `$SESS/capture/**` copies); (d) the auxiliary `frank-mcp` build output. **GOMODCACHE is the user's, declared READ-CONSUMED:** dependencies are unchanged at the pinned head so no module write is expected; IF the run performs any module download/write, that is DISCLOSED in the report as persistent user-cache residue (and an unexpected one is a STOP) — complete teardown is claimed ONLY for `$SESS` and the test root, exactly as R2-MR-4 requires. Effective `go env` values recorded in the recipe.
- Read surface: the worktree at the pinned head (for hashes/lineage only — the capture run executes from the COPY); the pinned governance docs; `gh pr view`.

## §3a — THE CAPTURE RUN (R2-MR-1 + R2-MR-2 closed; ONE run of record joins rows 3/4c/5/6)

The mechanism is the review-permitted "temporary module copy + helper source" class, specified exactly; NO target-worktree source/test byte moves:

1. **The copy:** `git archive 36dbaca5 frank | tar -x -C $SESS/src` — the copy's `frank/` derives BY GIT from commit tree `c31ac08e…` (the derivation command recorded; git's archive-of-the-pinned-commit IS the fidelity proof; the report additionally records `git rev-parse 36dbaca5^{tree}:frank`).
2. **The capture shim (in the COPY ONLY; its FULL diff vs the archived bytes printed verbatim in the report):** a bounded additive modification of `$SESS/src/frank/test/composed/turn_test.go` (and, if needed, one small helper file beside it) that (i) after `buildProductionBinaries` returns, copies the five built binaries to `$SESS/capture/bin/` and SHA-256s them, logging `name → executed-path → digest`; (ii) after `initializeConductor` writes the three config inputs (`engine.json` · `internal/fieldspec/registry.json` · `test/invariants/catalog.v1.json` — the registry/catalog as the harness supplies them from the copy), copies those exact bytes to `$SESS/capture/config/`; (iii) AFTER the composed processes have exited and the test's own assertions have run — i.e., at the end of the test body, before `t.Cleanup`'s RemoveAll fires — copies the app-state sqlite (post-exit, quiescent: no live-WAL copy) and the run-local conductor store surfaces to `$SESS/capture/state/`, and copies the test's captured RUN_IDENTITY line to `$SESS/capture/identity.txt`; (iv) changes NO assertion, NO build command, NO execution path — the test must PASS WHOLE, the composed claim intact; (v) captures NO credential byte (the demo credential paths are excluded by name; the report shows the exclusion).
3. **Execution:** `cd $SESS/src/frank && GOCACHE=$SESS/gocache GOTMPDIR=$SESS/gotmp go test -count=1 ./test/composed -run TestWholeAppGovernedTurnProducesJoinedE3Evidence -v` — the binaries the test builds (ITS real recipe, plain `go build -o`, recorded as such — no `-trimpath` imposed, the effective recipe stated honestly) ARE the binaries it executes (`turn_test.go:60-73`) ARE the binaries the shim hashed: one run, same bytes, joined by the shim's path+digest log. R2-MR-1's requirement is met by construction.
4. **Row-4c extraction (post-run, from the captured quiescent state):** decode the persisted manifest bytes from `$SESS/capture/state/` via the product decoder (a tiny read-only helper program in `$SESS` invoking the module's own `manifest` package `DecodeFrozen`/store reader — the helper source and command printed verbatim); extract `tool_catalog_digest` + the full `tool_set`. NO claim that RUN_IDENTITY printed the field.
5. **Teardown:** the test's own cleanup removes its runtime root; the evidence values live in the report; `rm -rf $SESS` last; the post-removal check + both clean-tree captures close the run.

## §3 — The evidence package (rows carried from plan-2 with the R2 corrections; each row = value + EXACT command + caveat + its honest evidence grain)

1. **Source identity (E1):** head `36dbaca5…` (re-verified) · tree `c31ac08e…` · the five subtree ids at head · base lineage · PR #2 head OID/state.
2. **Toolchain + build environment (E1):** `go version`; `go env` for `GOOS GOARCH GOAMD64 CGO_ENABLED GOFLAGS GOVERSION GOCACHE GOTMPDIR GOMODCACHE`; the capture run's EFFECTIVE build recipe stated as it truly is (the test's plain `go build -o`, no `-trimpath`, `-buildvcs` unset ⇒ the toolchain default, recorded).
3. **The artifact matrix (E2, joined to the E3 run):** the FIVE executed binaries' digests = the §3a shim's captured values (frank-app: F63 candidate/m-10 · frank-worker: F63/m-9 · frank-connector: F63/m-8 · frank: **F65, separate** · frank-broker: classification ROUTED, F63-member vs shared-conductor-client material, master/m-7's call) — each row shows executed-path = hashed-path from the shim log. `frank-mcp` (E2, AUXILIARY, NON-executed): built separately under a stated plain recipe, hashed, labeled outside the binding set unless the binding act says otherwise. Reproducibility caveat per row (this-toolchain witnesses; the binding act may bind row 1 as primary — Master+VP's choice).
4. **`tool_catalog_digest` — the THREE-comparator verification (unchanged from plan-2; grains stated):** (a) the SHIPPED m-9 registry vector + digest from `internal/worker/catalog` (`ExpectedDigest` `151a7e61…`, `catalog.go:14-31`, + its green test) — E2; (b) the LOCKED expected pre-build vector digest `7fae5fc1…` — E1; (c) the manifest/gate vector from the §3a.4 captured persisted bytes — E3. Three DISTINCT inputs; **the reproduced inequalities (the app's names-only self-compared placeholder `3d42ed4c…` with fabricated `"schema:"+name` digests and `"mvp-v1"` versions vs (a); the landed-worker-serialization vs (b) lock question) are a FINDING/STOP ROUTED UP with the package (m-9 · m-2 · m-10 · master) — never an acceptance pass, never a pair-authored normalization; no pair byte "fixes" anything under this plan.** The names-only hash survives ONLY as the distinctly-labeled eight-name policy identity.
5. **The composed-run identity set (E3):** the §3a run's RUN_IDENTITY line verbatim (run-manifest · policy · lane-catalog · logical-surface · frozen-core · lowered-tools · trust-root · `release_identity=placeholder:<digest>`) + the §3a.4 extraction from the SAME run — one run of record, one identity.
6. **F65 — the conductor identity, SEPARATELY (E3 for the invocation rows):** the executed `frank` digest (§3a shim) + the conductor source surfaces at head (E1) + **the governing-config identity over the ACTUAL §3a invocation** — the three captured config files hashed individually + as an ordered vector. The GRAIN question (one digest vs a closed member vector; the authoritative F65 member set) stays ROUTED through master to m-7; row 6 delivers captured values + the routed question, NOT "complete".
7. **Module closure (E1):** `go.mod`/`go.sum` blob ids at head + `go.sum`'s SHA-256.
8. **The NAMED carrier question (routed, unchanged):** how the bound values enter the composed run's manifest (the placeholder locus `cmd/frank-app` — now COMPOSING with the row-4 finding at the same `productionToolIdentity` locus); the binding act directs; the pair executes under a fresh direct dispatch, plausibly in the WP3 round.

## §4 — Deliverable, verification, sequencing (E3-classed; the shape carried)

- **Deliverable:** ONE evidence SITREP (implementer → planner, CC this roster): every §3 row with command/value/caveat AT ITS STATED GRAIN; the §3a shim diff verbatim + its path+digest log; the §2 clean-tree and teardown proofs + any GOMODCACHE residue disclosure; the row-4 FINDING stated as such; the routed questions. **The package's evidence class is E3 at the composed-runtime rows (3/4c/5/6-invocation), E1/E2 at the static rows — this is the COMPOSED-runtime E3 only, NOT WP3's externally-observed live E3 and NOT its admissibility gate.**
- **Planner corroboration:** rows 1/2/7 re-derived fully; row 4(a)/(b) re-derived; rows 3/4c/5/6 corroborated by REPEATING the §3a capture run at my own invocation (a second independent run; digests of deterministic artifacts compared, run-scoped values compared shape-wise) — then the package + findings route UP together.
- **Acceptance:** every §3 row per its own terms (row 6 delivered-with-routed-question); the §3a joining proof present (shim diff + log; the test PASSED whole); the row-4 three-way comparison with the finding ROUTED; the §2 classification proven; the corroboration filed; the package UP. The BINDING is not an acceptance row.
- **Sequencing:** your PLAN-REVIEW verdict TO me, parented to this relay. On approve: the record `SCOPE_DIFF` (the relay root as the only repository write row); NO token request; you prepare; I corroborate; the package + findings route UP.

## §5 — Out of scope (carried whole from plan-2)

Any "release bound" claim; ANY repository/product/worktree byte outside `frank/.relays/s16/**` (the §3a copy/shim live ONLY under `$SESS` and die with it); any pair-authored repair of the row-4 inequality or the row-8 carrier; the RELEASE-BINDING act (Master+VP); the F65 grain and broker-classification rulings (master/m-7); live/external E3, admissibility, exit legs (WP3); budget/conformance (WP4); gap register/battle report/close (WP5); merge (operator-only); every standing plan-5 §9 fence, unchanged.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (`turn_test.go:26-38/:60-73/:227-239` + the runbook re-read at the pinned head); no source, test, branch, commit, push, PR, merge, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-001759.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-003433.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-001107.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-002348.md
