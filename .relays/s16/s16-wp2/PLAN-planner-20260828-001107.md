## PLAN — WP2: the F63/F65 release-identity EVIDENCE PREPARATION (obligation ii's pair half) — READ/EVIDENCE-ONLY, ZERO write surface outside the relay root (the no-token class, stated per master's WP2-open rule); the RELEASE-BINDING ITSELF stays the SEPARATE Master+VP act

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2-plan
PARENT_DISPATCH_ID: s16-wp2-open
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the pair prepares evidence; the RELEASE-BINDING is Master+VP's separate downstream act; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — WP2 executes plan r7 §1(ii)/§2's pair half against the banked WP1 object; no design decision is opened at this seat (the one carrier question is NAMED for the binding act at §3.8, not decided here)
IN_REPLY_TO: s16-wp2/RECONCILE-orchestrator-planner-20260828-000752.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-10.planner, m-3.planner
SUBJECT: WP2 plan — the identity-evidence package over the banked WP1 object 36dbaca5 (source/toolchain/binary/catalog/manifest-set/conductor-F65/module rows, each with its exact derivation command and honesty caveats); READ/EVIDENCE-ONLY with zero write surface outside frank/.relays/s16/** ⇒ per the WP2-open rule NO implementation token is needed; the runtime-carrier question for the bound identity is NAMED for the Master+VP binding act; your review, then the implementer prepares, I corroborate, master receives the package

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — read/hash/build-to-temp evidence derivation only; no credential surface is touched, read, or carried (evidence = §2/§3)
- migration/backfill/destructive-write/canonical-data-repair: no — zero writes outside the relay root (evidence = §2)
- money/inventory/orders/planning/accounting/trust-critical-state: no — no store, ticket, epoch, or record byte moves; the evidence READS the banked object (evidence = §2)
- AI-or-automation-acts-downstream: no — no governed turn is driven for its effects; the composed battery re-run is a verification instrument against the pinned head (evidence = §3.5)
- worker/scheduler/queue/retry/async-side-effect: no — no supervised process outlives its verification run (evidence = §3.5)
- cross-repo/service-contract/generated-schema/shared-API-event: no — no contract byte moves; digests are DERIVED from pinned bytes (evidence = §3)
- user-visible-control-with-materializer/downstream-consumer: no — the downstream consumer is the Master+VP binding act, which consumes the evidence RELAY, not a behavior change (evidence = §4)
- test-runtime-role-mismatch: no — every command runs against the exact banked head 36dbaca5 in the commissioned worktree (evidence = §3)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: no — the scope is the enumerated §3 rows; the carrier question is routed, not absorbed (evidence = §3.8)
ESCALATION_SCAN_RESULT: all-no
WHY_DOWNGRADE_IS_SAFE:
- no downgrade is made — the tier stays production-risk (the commission's own); this branch is used only because every scan row is honestly `no` for this read-only WP: zero writes outside the relay root, no runtime state mutated, no contract byte moved, direct verification commands throughout.

## §1 — Basis (pointers; on conflict the pinned bytes govern)

- Plan r7 @ `044fe4cc…` §1(ii) + §2 WP2: "the pair PREPARES release-identity evidence — the F63/F65 RELEASE-BINDING itself is a SEPARATE Master+VP act, never the pair's; `tool_catalog_digest` mechanically verified; the conductor identity bound separately (F65)." The acceptance law: no s16 relay claims "release bound," ever.
- The banked WP1 object (master's ack `s16-wp2/RECONCILE-orchestrator-planner-20260828-000752.md`, corroborated at THREE seats): branch `s16-integration`, head `36dbaca549e3256fcb806ae8a846443e45bb0186`, tree `c31ac08e…`, launch base `77f8c9db…`, draft PR #2 at that head; the WP1 plan of record plan-5 @ `e64e6077…`.
- The standing V32 law (the ack ¶WP2, restated): trigger-present ⇒ any WP2 byte moving under IMPLEMENTATION authority needs a fresh direct master dispatch; **a read/evidence-only WP2 whose plan shows ZERO write surface outside the relay root needs NO token — THIS PLAN IS THAT CLASS** (§2).
- Owner context consumed read-only: the m-10 F55/F63 pre-admission build-integrity gate + the manifest `ReleaseBinding` surface; m-9's F63-covered build-digest mentions; the interface lock RE-CERT 3 @ `6a768023…`; the composed-run identity set as printed by the T7 proof (incl. `trust_root_digest`; the release identity currently the LABELED placeholder — `cmd/frank-app/main.go:121`).

## §2 — Scope: READ/EVIDENCE-ONLY (the SCOPE_DIFF comparand)

- **Write surface: `frank/.relays/s16/**` via the engine ONLY** (the evidence report + this round's relays). ZERO bytes anywhere else: no source, no test, no commit, no push, no PR change, no worktree file, no `master/**` byte. Builds and test runs execute against the PINNED head with every output landing OUTSIDE the tree (`go build -o` into a temp dir; the worktree stays clean before and after, proven by `git status --short` in the report).
- Read surface: the commissioned worktree at exact `36dbaca5…` (verify HEAD first; a moved head is a STOP), the pinned governance docs, `gh pr view` (read-only).
- **No implementation token exists or is requested for this WP** (the §1 rule; the plan states its class exactly as master required). If ANY row turns out to need a byte outside the relay root, that row STOPS and routes UP — it is not executed under this plan.

## §3 — The evidence package (the rows the implementer prepares; each row = the value + the EXACT derivation command + any honesty caveat)

1. **Source identity (F63's object grain):** head `36dbaca5…` (re-verified at run time) · commit tree `c31ac08e…` · the FIVE implementation-subtree ids AT HEAD (`git rev-parse HEAD^{tree}:frank/cmd` etc. for cmd/internal/test/go.mod/go.sum) · launch-base lineage (`77f8c9db…`, ancestry command) · branch + draft PR #2 head OID (`gh pr view 2 --json isDraft,headRefOid,state`).
2. **Toolchain + platform identity:** the exact `go version` output (this authoring observed `go1.26.4 darwin/arm64` — the run re-states its own) + the host platform; stated so the binary digests are interpretable.
3. **Binary identity (all six `cmd/` targets: `frank` · `frank-app` · `frank-broker` · `frank-connector` · `frank-worker` · `frank-mcp`):** SHA-256 per binary, built at the pinned head with ONE stated recipe (`go build -trimpath -o <tempdir>/<name> ./cmd/<name>`, exact flags recorded). **Honesty caveat stated in the row:** Go binaries are reproducible only under an identical toolchain/platform/flags; the digests bind THIS toolchain's build, and the binding act may bind the source identity (row 1) as primary with binary digests as this-toolchain witnesses — that choice is Master+VP's, not the pair's.
4. **`tool_catalog_digest`, MECHANICALLY VERIFIED (the obligation's named check):** recompute `sha256(join(RatifiedToolNames, "\n"))` from `internal/appctl/manifest/manifest.go:19`'s constant at the head; assert equality with (a) the gate's shipped derivation in `cmd/frank-app` and (b) the composed run's printed manifest value. Any inequality is a FINDING UP, not a footnote.
5. **The composed-run manifest identity set:** re-run the composed battery at the pinned head (verification-instrument run; all state under a fresh private temp root, torn down after) and record the printed set — run-manifest digest · policy digest · lane-catalog digest · `logical_surface_digest` · `frozen_core_digest` · `provider_lowered_tools_digest` · `trust_root_digest` · the LABELED placeholder release identity (`placeholder:<digest>` verbatim, so the binding act sees exactly what it replaces).
6. **F65 — the conductor identity, SEPARATELY:** the conductor's own rows, distinct from the app-side set: the `cmd/frank` binary digest (row-3 recipe) + the conductor's source surfaces at the head (the `frank/internal` subtree id already in row 1; `cmd/frank` file blob ids) — packaged as its own section so the Master+VP act can bind F65 apart from F63 exactly as the obligation requires.
7. **Module closure:** the `go.mod`/`go.sum` blob ids at head (row 1's last two values restated in the module row) + `go.sum`'s own SHA-256 — the dependency-closure identity.
8. **The NAMED carrier question (routed, not decided):** the composed runtime currently derives its release identity from the labeled placeholder constant (`cmd/frank-app/main.go:121`); WP3's E3 must run BOUND to the exact release/manifest (plan r7 §1(iii); m-3's bound-identity precondition). The evidence package asks the Master+VP binding act to STATE how the bound values enter the composed run's manifest (the expected shape: a bounded code change replacing the placeholder with a binding-act-supplied input, executed by this pair under a fresh direct dispatch — plausibly folded into the WP3 plan round). The pair proposes nothing further and executes what the act directs.

## §4 — Deliverable, verification, and sequencing

- **Deliverable:** ONE evidence SITREP from `s16.implementer` TO `s16.planner` (CC master + the CC roster) carrying every §3 row with its command and output verbatim, the pre/post `git status --short` cleanliness proof, and the §3.8 routed question — the package the Master+VP binding act consumes. Evidence class: E2 (command-derived over pinned bytes; the composed-battery row is the same instrument WP1 banked, re-run as verification).
- **Planner corroboration:** on receipt I re-derive at my own invocation (the standing three-seat pattern): rows 1/2/4/7 fully; row 3 by digest re-derivation with the same recipe; row 5 by re-running the composed battery; row 6 by recompute. Then the reconciled package routes UP for the Master+VP F63/F65 act.
- **Acceptance (WP2's pair half done means exactly):** every §3 row present with command + value + caveat; the two row-4 equalities PROVEN; the worktree clean before/after; zero bytes outside the relay root; the §3.8 question carried; the corroboration filed; the package routed UP. The BINDING itself is explicitly NOT a WP2 acceptance row — it is Master+VP's act on their own cadence.
- **Sequencing:** your PLAN-REVIEW verdict TO me, parented to this relay, confirming (or contesting) the read/evidence-only class. On approve: I run the mechanical `SCOPE_DIFF` (the §2 comparand — the relay root is the only write row) for the record; NO token request follows (the §1/§2 class rule); you prepare the package; I corroborate; the package routes UP.

## §5 — Out of scope (a touch = escalation by construction)

Any "release bound" claim (the acceptance law — the words never appear in an s16 relay as a claim); ANY byte outside `frank/.relays/s16/**` (source, test, commit, push, PR, worktree file, `master/**`); the RELEASE-BINDING act itself (Master+VP); the placeholder-replacement code change (§3.8 — waits on the binding act's direction + a fresh direct dispatch); live/external E3, admissibility, exit legs (WP3); budget/conformance (WP4); the gap register/battle report/close (WP5); merge (operator-only); every standing fence of plan-5 §9 (unchanged and untouched by this read-only WP).

ACTIONS_GIT_REF: engine-lane governance act only — this WP2 plan drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (master's ack read in full; the toolchain version and cmd inventory observed in the worktree); no source, test, branch, commit, push, PR, merge, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M master/relays/CHECKPOINTS.md
