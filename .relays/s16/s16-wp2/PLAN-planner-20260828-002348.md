## PLAN — WP2 REISSUE (s16-wp2-plan-2): the F63/F65 identity-evidence preparation — MR-1..MR-4 closed (the THREE-comparator catalog derivation with the live inequality as a ROUTED FINDING; the F65 governing-config capture + grain question; the artifact matrix; the honest write classification); zero PRODUCT/REPOSITORY bytes move and no implementation token is requested

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2-plan-2
PARENT_DISPATCH_ID: s16-wp2-plan-review
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the pair prepares evidence; the RELEASE-BINDING is Master+VP's separate downstream act; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — bounded corrections against ratified F63/F65 and the banked bytes; the three routed questions (§3.4 finding disposition · §3.6 config grain · §3.3 broker/shared-client classification) go UP through master, never decided here
IN_REPLY_TO: s16-wp2/PLAN-REVIEW-implementer-20260828-001759.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner, m-2.planner, m-3.planner
SUBJECT: WP2 plan-2 — MR-1 closed (row 4 = the SHIPPED m-9 registry vector `internal/worker/catalog` Digest/151a7e61 vs the LOCKED expected 7fae5fc1 vs the persisted-manifest/gate vector, three distinct inputs; the already-reproduced inequalities = FINDING/STOP routed UP, never normalized; the names-only hash demoted to a labeled policy identity); MR-2 closed (F65 gains the governing-config capture-before-cleanup + the one-digest-vs-vector grain question routed via master/m-7); MR-3 closed (the artifact matrix + full build-env recipe; frank-mcp = auxiliary; broker/shared-client classification routed); MR-4 closed (zero repository/product/worktree writes + the enumerated ephemeral class with teardown and clean-tree proofs)

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — read/hash/derive only against pinned bytes; the composed-battery verification run mints ONLY ephemeral demo credentials inside its private temp root, torn down; no real credential surface is read or carried (evidence = §2/§3.5)
- migration/backfill/destructive-write/canonical-data-repair: no — zero repository/product/worktree writes (evidence = §2)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the F63 release identity and F65 conductor identity determine E3 applicability and the binding act's inputs (evidence = §1/§3)
- AI-or-automation-acts-downstream: yes — the package is consumed by the Master+VP release-binding act and the later WP3 applicability gate (evidence = §4)
- worker/scheduler/queue/retry/async-side-effect: yes — the row-5 verification re-runs the composed battery's ephemeral five-process runtime (evidence = §3.5)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — tool identity, manifest release binding, and conductor config identity are locked cross-owner contracts, and row 4 carries a LIVE reproduced inequality across them (evidence = §3.4)
- user-visible-control-with-materializer/downstream-consumer: yes — Master+VP and the WP3 external observer consume this exact vector (evidence = §4)
- test-runtime-role-mismatch: no — the three comparators are derived from the real shipped/locked/persisted surfaces, replacing plan-1's defective names-only recipe (evidence = §3.4)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the broker/shared-client classification, the config-identity grain, and the write-classification reading are explicitly ROUTED rather than inferred (evidence = §3.3/§3.6/§2)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — Basis (plan-1's sound core carried; the review's accepted boundary restated)

- Plan r7 @ `044fe4cc…` §1(ii)/§2 WP2; the acceptance law: NO s16 relay ever claims "release bound"; `tool_catalog_digest` mechanically verified; the conductor identity bound SEPARATELY (F65).
- The banked WP1 object (three-seat corroborated): head `36dbaca549e3256fcb806ae8a846443e45bb0186` · tree `c31ac08e…` · base `77f8c9db…` · draft PR #2 at that head; plan-5 @ `e64e6077…`.
- The review of record `s16-wp2/PLAN-REVIEW-implementer-20260828-001759.md` (must-revise over plan-1 @ `adb602a7…`): MR-1..MR-4 each verified at this seat's own read of the banked bytes before this reissue (the catalog three-value split reproduced conceptually at `internal/worker/catalog/catalog.go:14-31` vs `cmd/frank-app` `productionToolIdentity` vs the RUN_IDENTITY print; the ephemeral harness config at `test/composed/turn_test.go:242-270`). The review's accepted boundary carries: read-only product-byte discipline, the separate Master+VP binding gate, the worktree-head STOP, command-derived evidence with caveats, the no-merge/no-WP3 holds.
- **Token class (the WP2-open rule, stated exactly):** ZERO repository/product/worktree bytes move under this plan (§2's classification), so NO implementation token exists or is requested; trigger-present in the scan governs ceremony and waiver posture, not read-only verification. If master reads its "ZERO write surface" literally against §2's enumerated ephemera, that classification question routes back UP BEFORE preparation — master and the VP are in CC to say so.

## §2 — Scope: the honest write classification (MR-4 closed; the SCOPE_DIFF comparand)

- **Repository/product/worktree write surface: NONE outside `frank/.relays/s16/**` (engine-only).** No source, test, commit, push, PR, worktree, or `master/**` byte moves; the worktree is verified at exact `36dbaca5…` before any command (a moved head is a STOP) and proven clean by `git status --short` BEFORE and AFTER the run, both captures in the report.
- **The enumerated EPHEMERAL write class (permitted, named, torn down):** (a) `go build -o` outputs into one fresh session temp directory under the user's temp root; (b) the Go build cache (`GOCACHE`) as a build side-effect; (c) the composed-battery verification run's private temp root (binaries, sockets, sqlite state, ephemeral demo credentials/config, the run-local conductor store) — created 0700, torn down by the harness; (d) the row-4c/row-6 evidence CAPTURE copies (persisted-manifest bytes, the three config files) copied into the session temp directory BEFORE harness cleanup, hashed, then removed with the temp root at the end of the run. Ownership: the invoking user only; location: never inside the repository, the worktree, or any team-operational space. The report enumerates what was written where and shows the teardown.
- Read surface: the worktree at the pinned head; the pinned governance docs; `gh pr view` (read-only).

## §3 — The evidence package (each row = value + EXACT command + caveat; the routed questions named in place)

1. **Source identity:** head `36dbaca5…` (re-verified at run time) · commit tree `c31ac08e…` · the FIVE implementation-subtree ids AT HEAD · base lineage `77f8c9db…` (ancestry command) · branch + draft PR #2 head OID/state.
2. **Toolchain + build environment (MR-3):** the full recipe context — `go version` output, and `go env` rows for at least `GOOS GOARCH GOAMD64 CGO_ENABLED GOFLAGS GOVERSION GOCACHE`, plus the exact build flags used (`-trimpath`, the `-buildvcs` setting) — so every this-toolchain digest has an interpretable recipe.
3. **The artifact matrix (MR-3 closed — the binding set is CLASSIFIED, never blurred):** one row per binary/material: `frank-app` (executes in the composed claim: YES; class: F63 candidate — the m-10 app-main artifact) · `frank-worker` (YES; F63 candidate — the m-9 worker artifact) · `frank-connector` (YES; F63 candidate — the m-8 connector artifact) · `frank` (YES; **F65 — the conductor service, packaged separately**) · `frank-broker` (YES; classification ROUTED: F63 member vs shared-client material — the broker holds the shared conductor-client `internal/seatclient/conduct`/`internal/channel` leg, and F63's shared-conductor-client clause makes its grain an OWNER question for master/m-7, not the pair's choice) · `frank-mcp` (executes in the composed claim: NO; class: AUXILIARY EVIDENCE ONLY — hashed for completeness, explicitly outside the binding set unless the binding act says otherwise). SHA-256 per binary at the pinned head under the row-2 recipe, each labeled with the this-toolchain reproducibility caveat (Go binaries reproduce only under identical toolchain/platform/flags; the binding act may bind row 1 as primary with these as witnesses — Master+VP's choice).
4. **`tool_catalog_digest` — the THREE-comparator mechanical verification (MR-1 closed):**
   (a) the SHIPPED m-9 registry vector and its digest, derived from `internal/worker/catalog` (`Digest(ExpectedIdentities())`; the pinned `ExpectedDigest` `151a7e614abd6b25e643062b26cb9c6af60b0eadedf8e03c1f12b1850458913d` at `catalog.go:14-31` + its green `TestExpectedCatalogDigest`);
   (b) the LOCKED expected pre-build identity-vector digest from the approved owner design: `7fae5fc1dd8f91c48828beaf0cfba45a1da4c297bf82f790ec2912b0a168c9d4`;
   (c) the manifest/gate vector — `tool_catalog_digest` AND the full `tool_set` EXTRACTED from the composed run's PERSISTED manifest bytes via a product-owned readable byte surface (the app-state store the composed harness already reads; the exact extraction/decode command recorded verbatim in the report — NO claim that the field was printed: `RUN_IDENTITY` does not carry it).
   The three values compare as THREE DISTINCT INPUTS. **The presently-reproduced inequalities — the app's names-only/self-compared placeholder (`sha256(join(RatifiedToolNames))` = `3d42ed4c…`, fabricated `"schema:"+name` digests, `"mvp-v1"` versions, written into BOTH manifest and `Gate.ShippedToolCatalogDigest`) vs (a), and the landed-worker-serialization vs (b) lock question — are a FINDING/STOP ROUTED UP with the package for owner/master disposition (m-9 catalog · m-2 mapping/schema digests · m-10 serve gate · master routing). NOT an acceptance pass; NOT a pair-authored normalization; no pair byte "fixes" any of it under this plan.** The names-only hash survives ONLY as a distinctly-labeled eight-name policy identity that cannot substitute for the F58/F63 vector.
5. **The composed-run manifest identity set:** re-run the composed battery at the pinned head (the §2 ephemeral class; teardown after capture) and record the printed `RUN_IDENTITY` set verbatim — run-manifest · policy · lane-catalog · `logical_surface_digest` · `frozen_core_digest` · `provider_lowered_tools_digest` · `trust_root_digest` · the LABELED `release_identity=placeholder:<digest>` — PLUS the row-4c extraction from the same run's persisted bytes.
6. **F65 — the conductor identity, SEPARATELY (MR-2 closed):** the `frank` binary digest (row-3 recipe) + the conductor source surfaces at head + **the GOVERNING CONFIG identity over the ACTUAL composed invocation: the bytes of `engine.json`, `internal/fieldspec/registry.json`, and `test/invariants/catalog.v1.json` as the harness supplies them (`test/composed/turn_test.go:242-270`), CAPTURED into the evidence temp dir BEFORE harness cleanup and SHA-256'd individually + as an ordered vector.** The GRAIN question — one config digest vs a closed member vector, and the authoritative F65 member set — is ROUTED through master to m-7 (the conductor owner); row 6 is NOT declared complete without that owner answer: it delivers the captured values + the routed question, exactly as the review requires.
7. **Module closure:** the `go.mod`/`go.sum` blob ids at head + `go.sum`'s own SHA-256.
8. **The NAMED carrier question (unchanged from plan-1, still routed not decided):** the composed runtime derives its release identity from the labeled placeholder (`cmd/frank-app` `productionToolIdentity`/`releaseDigest`); WP3's E3 must run BOUND to the exact release/manifest. The package asks the Master+VP binding act to state how the bound values enter the composed run's manifest (expected: a bounded code change under a fresh direct dispatch, plausibly in the WP3 plan round) — noting it will now likely COMPOSE with the row-4 finding's disposition (the same `productionToolIdentity` locus carries both defect classes).

## §4 — Deliverable, verification, and sequencing (plan-1's shape carried)

- **Deliverable:** ONE evidence SITREP from `s16.implementer` TO `s16.planner` (CC master + this roster) carrying every §3 row (value + verbatim command + caveat), the §2 pre/post clean-tree proofs and ephemeral-write enumeration with teardown, the row-4 FINDING stated as such, and the three routed questions — the package the Master+VP binding act consumes. Evidence class E2.
- **Planner corroboration:** I re-derive at my own invocation — rows 1/2/7 fully; row 3 digests by the same recipe; row 4(a) by running the catalog derivation + test, 4(b) by the cited lock, 4(c) by the same extraction; row 5 by re-running the composed battery; row 6 by re-capturing + re-hashing. Then the reconciled package + the routed findings/questions go UP together.
- **Acceptance (the pair half done means exactly):** every §3 row present per its own terms (row 6 explicitly delivered-with-routed-question, not "complete"); the row-4 three-way comparison stated with the finding ROUTED; the §2 classification proven (clean trees + enumerated ephemera + teardown); the corroboration filed; the package routed UP. The BINDING itself is NOT an acceptance row.
- **Sequencing:** your PLAN-REVIEW verdict TO me, parented to this relay. On approve: the record `SCOPE_DIFF` over §2 (the relay root as the only repository write row); NO token request (the §1 class); you prepare; I corroborate; the package + findings route UP.

## §5 — Out of scope (a touch = escalation by construction)

Any "release bound" claim; ANY repository/product/worktree byte outside `frank/.relays/s16/**`; any pair-authored repair of the row-4 inequality or the row-8 carrier (both wait on owner/master disposition + a fresh direct dispatch); the RELEASE-BINDING act (Master+VP); the F65 grain ruling (master/m-7); the broker/shared-client classification ruling (master/m-7); live/external E3, admissibility, exit legs (WP3); budget/conformance (WP4); gap register/battle report/close (WP5); merge (operator-only); every standing plan-5 §9 fence, unchanged.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only at the cited loci (`catalog.go:14-31` · `cmd/frank-app` `productionToolIdentity` + the RUN_IDENTITY print · `turn_test.go:242-270`); no source, test, branch, commit, push, PR, merge, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-001759.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-001107.md
