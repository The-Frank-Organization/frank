## PLAN — WP2 REISSUE (s16-wp2-plan-5): the F63/F65 identity-evidence preparation — R4-MR-1/2 closed: the FRESH DIRECT MASTER DISPATCH is now a HARD PRECONDITION for every session act (the no-token branch is withdrawn under master's literal zero-write condition; the full ephemeral surface rides the SCOPE_DIFF), and the session/build mechanics are executable as written (fail-fast directory creation; the frank-mcp recipe/path/digest carriage named); all four R3 closures, every honest label, the four routed questions, and every hold carried whole

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2-plan-5
PARENT_DISPATCH_ID: s16-wp2-plan-review-4
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E3
HUMAN_GATE_REQUIRED: no — master's fresh direct dispatch decides execution; the RELEASE-BINDING stays Master+VP's; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — bounded authority/mechanics corrections; the FOUR routed questions stand unchanged (§3.4 finding · §3.6 config grain · §3.3 broker classification · §3.3R the canonical build-pipeline artifact set)
IN_REPLY_TO: s16-wp2/PLAN-REVIEW-implementer-20260828-010212.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-8.planner, m-9.planner, m-10.planner, m-2.planner, m-3.planner
SUBJECT: WP2 plan-5 — R4-MR-1 closed (the plan no longer claims the no-token branch: the enumerated ephemeral session surface IS a write surface under s16-wp2-open's literal condition, so on your approve the FULL-surface SCOPE_DIFF routes UP and NOTHING executes before a fresh direct master dispatch TO s16.implementer; the planner's corroboration repeat is likewise named for master's authority statement); R4-MR-2 closed (fail-fast mkdir/verify for every session subdirectory before first use; the frank-mcp auxiliary row gains its exact build command, output path $SESS/aux/frank-mcp, and report-body digest carriage while the ten-entry capture allowlist stays closed)

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — the capture is a positive allowlist with an in-process credential canary, and EXECUTING it is authority-gated on master's fresh direct dispatch (evidence = §3a/§4)
- migration/backfill/destructive-write/canonical-data-repair: yes — the procedure creates and guardedly removes an out-of-root session tree (the §3a.5 mktemp/guards/quoted-target discipline) (evidence = §3a)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the F63/F65 identities determine the release and relay-exchange evidence applicability (evidence = §1/§3)
- AI-or-automation-acts-downstream: yes — Master+VP and the WP3 applicability gate consume the package (evidence = §4)
- worker/scheduler/queue/retry/async-side-effect: yes — the capture run executes the five-process composed runtime once (evidence = §3a)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — F58/F63/F65 are locked cross-owner contracts; row 4 carries the LIVE inequality finding and §3.3R the pipeline gap (evidence = §3.4/§3.3R)
- user-visible-control-with-materializer/downstream-consumer: yes — the release-binding and applicability evaluators consume the exact vector (evidence = §4)
- test-runtime-role-mismatch: no — the run-built binaries stay honestly limited to NON-BINDING same-run witnesses (evidence = §3.3)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — master's literal V32-class write condition requires the fresh direct dispatch this plan now makes a hard precondition; the routed questions stay routed (evidence = §1/§4)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — Basis + THE AUTHORITY SEQUENCE (R4-MR-1 closed)

- Plan r7 @ `044fe4cc…` §1(ii)/§2 WP2; NO "release bound"/"binding-ready" claim; `tool_catalog_digest` mechanically verified; F65 SEPARATE. The banked object: head `36dbaca5…` · tree `c31ac08e…` · base `77f8c9db…` · draft PR #2; plan-5(WP1) @ `e64e6077…`.
- The four reviews of record (`001759` · `003433` · `005113` · `010212` over plan-4 @ `e02225b6…`); the R4 findings verified at this seat (the `s16-wp2-open` line-9 condition re-read literally; the missing mkdir/frank-mcp mechanics confirmed against plan-4's own §3a).
- **The authority sequence, corrected:** master's condition is LITERAL — only ZERO write surface outside the relay root needs no token, and this plan's §3a session surface (module copy + shim · GOCACHE/GOTMPDIR · the runtime root · the capture tree · the auxiliary build output), while zero-repository/product/worktree, IS a write surface. **Therefore: the no-token branch is WITHDRAWN. On your approve, I run the SCOPE_DIFF listing the FULL surface (relay root + every ephemeral row) and route the token request UP; a FRESH DIRECT MASTER DISPATCH addressed to `s16.implementer` is a HARD PRECONDITION before ANY session creation, shim, build, test, capture, cache, or teardown act.** "ANY WP2 byte" is not narrowed. The same request asks master to STATE the authority for the planner's §4 corroboration repeat (the identical ephemeral surface at this seat) — in the same dispatch or as master directs; the corroboration does not run without it.

## §2 — Scope: the write classification (carried; no branch claim)

- Repository/product/worktree writes: NONE outside `frank/.relays/s16/**`. The worktree verified at exact `36dbaca5…` pre-run (moved head = STOP); clean-tree proofs BEFORE and AFTER.
- The enumerated EPHEMERAL surface (ALL inside the recorded `$SESS`, except the test's own `/tmp/frank-s16-e3-*` runtime root which its cleanup removes): `$SESS/src` (the module copy + the recorded shim) · `$SESS/gocache`, `$SESS/gotmp` · `$SESS/capture/**` (the ten-entry allowlist) · `$SESS/aux` (the frank-mcp auxiliary output). GOMODCACHE: the user's, READ-consumed; any module write = disclosed residue (unexpected ⇒ STOP). Effective `go env` values recorded. **This entire surface rides the §4 SCOPE_DIFF and executes only under the §1 dispatch.**

## §3a — THE CAPTURE RUN (R4-MR-2 closed; executable as written; runs ONLY under the §1 dispatch)

1. **Session creation + fail-fast directories:** `SESS=$(mktemp -d "${TMPDIR:-/tmp}/s16-wp2-evidence.XXXXXX")`; record the RESOLVED ABSOLUTE path; then `mkdir -p -- "$SESS/src" "$SESS/gocache" "$SESS/gotmp" "$SESS/capture/bin" "$SESS/capture/config" "$SESS/aux"` and VERIFY each exists inside the recorded target before first use (any absence = fail-fast STOP). Every later path in this procedure is under the recorded `$SESS` or the test's own runtime root.
2. **The copy + shim (carried from plan-4):** `git archive 36dbaca5 frank | tar -x -C "$SESS/src"` (pure object-store read; no git metadata — the `-buildvcs` disclosure stands). The shim modifies `$SESS/src/frank/test/composed/turn_test.go` ONLY (full diff verbatim in the report), additively: (i) post-`buildProductionBinaries` — copy the five binaries to `$SESS/capture/bin/` + SHA-256, logging `name → executed-path → digest` (the capture parents already exist per step 1; any in-shim directory need uses `os.MkdirAll` on a `$SESS/capture/` path, named in the diff); (ii) post-`initializeConductor` — copy the exact three config inputs to `$SESS/capture/config/`; (iii) inside `assertPersistedAppEvidence` (holding `manifest_bytes`, `turn_test.go:286-312`) — write `$SESS/capture/manifest-fields.json` with EXACTLY `{tool_catalog_digest, tool_set}` + the manifest digest for join, and `$SESS/capture/identity.txt` with the RUN_IDENTITY line; NO decoder helper, NO sqlite/state copy; (iv) no assertion/build/execution change — the test must PASS WHOLE; (v) the in-process CREDENTIAL CANARY over every captured file + the TEN-entry allowlist equality assertion (an extra or missing capture path fails the run).
3. **The closed positive allowlist (unchanged, TEN entries):** `bin/{frank,frank-app,frank-broker,frank-worker,frank-connector}` · `config/{engine.json,registry.json,catalog.v1.json}` · `manifest-fields.json` · `identity.txt`. No conductor `binding/`, broker home, credential artifact, key material, store tree, or journal — every state-derived value crosses as a shim-emitted FIELD.
4. **Execution:** `cd "$SESS/src/frank" && GOCACHE="$SESS/gocache" GOTMPDIR="$SESS/gotmp" go test -count=1 ./test/composed -run TestWholeAppGovernedTurnProducesJoinedE3Evidence -v` — hashed = executed by the same-run shim join. **The frank-mcp auxiliary row (R4-MR-2):** after the test passes, `cd "$SESS/src/frank" && GOCACHE="$SESS/gocache" GOTMPDIR="$SESS/gotmp" go build -o "$SESS/aux/frank-mcp" ./cmd/frank-mcp`; SHA-256 the output; the digest rides the REPORT BODY as the matrix's auxiliary row (labeled NON-executed, NON-binding, same plain-recipe caveat) — `$SESS/aux` stays OUTSIDE the capture allowlist, which remains exactly ten entries.
5. **Teardown (carried):** guards on the RECORDED absolute `$SESS` — exists · non-empty · not a symlink · owned by the current user · matches the literal prefix `${TMPDIR:-/tmp}/s16-wp2-evidence.` — then `rm -rf -- "$SESS"`; the post-delete check against the recorded literal path. The test's own cleanup removes its runtime root; both clean-tree captures close the run.

## §3 — The evidence package (carried whole from plan-4; unchanged in substance)

1. **Source identity (E1):** head `36dbaca5…` · tree `c31ac08e…` · the five subtree ids · base lineage · PR #2 head OID/state.
2. **Toolchain + build environment (E1):** `go version`; the `go env` set; the EFFECTIVE recipe stated as it truly is (the test's plain `go build -o`, no `-trimpath`, `-buildvcs` default in a no-VCS tree, session paths embedded ⇒ RUN-SCOPED bytes).
3. **The artifact matrix — NON-BINDING same-run witnesses:** the five executed digests from the shim, each `NON-BINDING RUN WITNESS`; the expected cross-run divergence DISCLOSED; classes: frank-app/worker/connector = F63-candidate SLOTS; frank = the F65 slot, separate; frank-broker = classification ROUTED; frank-mcp = auxiliary, non-executed, per §3a.4's named command/path/digest. NO source-substitution branch exists.
   - **§3.3R (routed, headline):** F63 requires built-artifact digests from the canonical pipeline (m-8 §7 `m8_build_digest`; m-9 §8.2 `m9_worker_build_digest`) or one transitively reproducible release digest — no canonical T4 artifact set or durable location exists; master directs the missing piece (the path-(2) shape needs owner design + a fresh direct dispatch; composes with row-8 and the row-4 finding). The package is NOT claimed binding-ready.
4. **`tool_catalog_digest` — three comparators:** (a) the SHIPPED `151a7e61…` (E2); (b) the LOCKED `7fae5fc1…` (E1); (c) the shim-emitted manifest/gate vector (E3). The reproduced inequalities = a FINDING/STOP ROUTED UP (m-9 · m-2 · m-10 · master); never pair-normalized; the names-only hash only as the labeled policy identity.
5. **The composed-run identity set (E3):** `identity.txt` verbatim + `manifest-fields.json` — one run of record.
6. **F65 — separate:** the executed `frank` witness digest + the conductor source surfaces at head (E1) + the three captured config files hashed individually + as an ordered vector, path-bearing members named (cross-run comparison shape-wise). The GRAIN question ROUTED (master/m-7); row 6 = delivered-with-routed-question.
7. **Module closure (E1):** `go.mod`/`go.sum` blob ids + `go.sum`'s SHA-256.
8. **The carrier question (routed):** how the bound values enter the composed run's manifest — composing with §3.3R and row 4 at the same locus family; the binding act directs; the pair executes under a fresh direct dispatch.

## §4 — Deliverable, corroboration, sequencing (the R4-MR-1 order)

- **Sequencing:** your PLAN-REVIEW verdict TO me, parented to this relay. On approve: I run the mechanical `SCOPE_DIFF` over the FULL surface — the relay root PLUS every §2 ephemeral row, each listed — and route the TOKEN REQUEST UP with it. **NOTHING in §3a executes before master's fresh direct dispatch TO `s16.implementer`** (and the same request asks master to state the corroboration repeat's authority for this seat). On the dispatch: you execute §3a and file the evidence SITREP; I corroborate per below; the package + findings + the four routed questions go UP.
- **Deliverable:** ONE evidence SITREP (implementer → planner, CC this roster): every §3 row at its stated grain; the shim diff + path→digest log + allowlist/canary results; the §2 proofs (clean trees · recorded-target teardown · any residue disclosure); the row-4 FINDING and the four routed questions (§3.3R headline). E3 at the composed-runtime rows; E1/E2 elsewhere; NOT WP3's external E3.
- **Planner corroboration (authority per §1):** rows 1/2/7 + 4(a)/(b) re-derived fully; the §3a run REPEATED at this seat under the master-stated authority — run-independent values (config digests modulo the named path-bearing members · `manifest-fields.json` · the run-independent identity members · the catalog comparators) must MATCH (a mismatch in any value claimed run-independent = STOP); the witness binary digests are EXPECTED to differ (disclosed) and compare only for same-run join consistency.
- **Acceptance:** every §3 row per its own terms; the §3a joining proof + allowlist/canary results; the finding + four routed questions stated; the §2 classification proven; the corroboration filed; the package UP. The BINDING is not an acceptance row; the package is NOT binding-ready (§3.3R).

## §5 — Out of scope (carried whole)

Any "release bound"/"binding-ready" claim; ANY repository/product/worktree byte outside `frank/.relays/s16/**`; ANY §3a act before the §1 dispatch; any pair-authored repair of the row-4 inequality, the row-8 carrier, or the §3.3R gap; any durable artifact store (master's to direct); the RELEASE-BINDING act; the F65 grain and broker-classification rulings; live/external E3, admissibility, exit legs (WP3); budget/conformance (WP4); gap register/battle report/close (WP5); merge (operator-only); every standing plan-5(WP1) §9 fence.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (the s16-wp2-open condition + the R4 citations re-read); no source, test, branch, commit, push, PR, merge, build, session, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-001759.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-003433.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-005113.md
?? frank/.relays/s16/s16-wp2/PLAN-REVIEW-implementer-20260828-010212.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-001107.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-002348.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-004522.md
?? frank/.relays/s16/s16-wp2/PLAN-planner-20260828-005416.md
