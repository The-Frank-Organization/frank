## SITREP — T7 FINDING VERIFIED AND ROUTED UP: the real connector has NO authorized trust path to a demo-owned loopback TLS endpoint — an m-8 owner ruling (+ the exact-loci fence amendment that follows it) is required before T7's composed-runtime E3 can exist

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16-t7-esc-1
PARENT_DISPATCH_ID: s16-t7-tls-finding-1
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the requested act is the m-8 owner ruling routed by master; the operator's gates remain downstream (a fence amendment, once ruled, returns through the pair review + master's updated direct dispatch, not an operator act)
IN_REPLY_TO: s16-t7/SITREP-implementer-20260827-150334.md
FROM: s16.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16.implementer, m-8.planner, m-3.planner
SUBJECT: s16 T7 escalation — verified at both pair seats: endpoint.Validate is https-only, the production service defaults transport.NewClient() with no injectable trust root, default TLS walks system roots, Clearenv precedes transport construction, and every connector byte is outside the approved fence; requesting master route the bounded m-8 owner ruling (candidate shapes named, none chosen here); T7 + WP1-close HELD, the rest of WP1 continues in-grant

## §1 — The finding, independently verified at this seat this act

The implementer's T7 finding (`s16-t7/SITREP-implementer-20260827-150334.md`) reproduces at the pinned/launch bytes read at this seat:

- `internal/connector/endpoint/endpoint.go` `Validate` accepts ONLY `https://` (no scheme exception exists);
- `internal/connector/service/service.go` `New` defaults `config.Provider = transport.NewClient()` — the production binary reaches the zero-config constructor with no root-pool/trust parameter;
- `internal/connector/transport/transport.go` `newClient` defaults `tls.Config{MinVersion: TLS12}` (nil `RootCAs` ⇒ system roots); the configurable TLS path is package-private test machinery unreachable from `cmd/frank-connector`;
- `cmd/frank-connector/main.go` calls `os.Clearenv()` before `control.Bootstrap`/`service.New` — an inherited `SSL_CERT_FILE`-class override cannot survive to transport construction (and would be non-hermetic evidence anyway);
- plan-3 §2.4 (the approved fence) contains NO connector surface: `frank/internal/connector/**` and `cmd/frank-connector/**` are outside it, correctly — they are m-8's built domain.

Therefore the T7 requirement set — real `frank-connector` · full `freeze→authorize→attach→send` · demo-owned LOCAL endpoint · authenticated HTTPS · plain hermetic `go test` · zero out-of-fence bytes — is UNSATISFIABLE at the dispatched bytes. The implementer's refusal to reach for a fake connector, an HTTP carve-out, or a host trust-store mutation is exactly right (each would violate plan §3, the m-8 pinned wire posture, or hermeticity/authority bounds).

## §2 — The requested ruling (m-8's to make; candidate shapes NAMED, not chosen — the pair executes, never re-designs)

Requested from `m-8.planner` through master: rule the authorized mechanism by which the REAL connector trusts a demo-owned loopback TLS endpoint for the composed-runtime proof (and, downstream, the WP3 externally-observed E3 against the same loopback — flagged for m-3 in CC because the harness trust posture becomes E3-admissibility surface). Candidate shapes visible from the built bytes, for the owner to accept, reshape, or replace:

1. **A bounded non-secret trust-root ARTIFACT input** following the connector's existing artifact pattern (credential/catalog/policy paths under the private runtime dir): an optional test-CA PEM whose presence injects an explicit `RootCAs` pool into the production transport constructor — m-8-authored contract instrument (the broker-w precedent: the owner authors the bounded contract, the s16 pair realizes it under an amended fence), with the non-secret/never-logged/0600 custody terms m-8 deems fit and m-1's boundary untouched (a PUBLIC test CA certificate, no key material in the connector's hands).
2. **A bounded inherited-descriptor variant** of the same (the implementer's sketch): the trust root delivered over one inherited FD like the broker's token pipe, avoiding a new artifact path.
3. **An owner-ruled alternative** that preserves all of: real process, full wire machinery, loopback-only egress (H-12), hermetic plain `go test`, no host state mutation. If m-8 instead rules the https+system-roots posture untouchable even for the composed proof, the T7 proof definition itself needs a master-level acceptance re-cut — that choice is above this pair.

## §3 — The consequence chain, stated so the next steps are mechanical

(1) m-8's ruling lands as a pair-approved owner instrument (exact hash); (2) plan-3's fence gains EXACTLY the instrument's named loci via a bounded plan amendment at this seat, re-reviewed by `s16.implementer`; (3) master's updated direct dispatch extends the implementation authority to the amended fence (the standing rails: the token stays master's/the operator's while the scan is trigger-present); (4) the s16 pair realizes the instrument and T7 proceeds. Until (1)–(3): **T7's composed-runtime E3 and the WP1 close are honestly HELD**; every other WP1 surface continues under the existing grant — the implementer reports the composition commit `283676d3` pushed with the full gate green (seam 64/0/64 · plain suite incl. `test/fixtures` · vet) and the T8 loci audit flipped RED-at-cut → GREEN, exactly the anti-vacuity shape the plan demands.

Next requested action: `master.orchestrator-planner` routes §2 to `m-8.planner` (m-3 informed for the WP3 admissibility surface) and carries the ruling back to `s16.planner` + `s16.implementer`; this planner prepares the bounded plan amendment against the returned instrument.

ACTIONS_GIT_REF: engine-lane governance act only — this escalation drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; verification this act was read-only at the four named connector loci + the approved plan fence; no source, test, branch, commit, push, PR, merge, or code byte from this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16/s16-impl/SITREP-implementer-20260827-134558.md
?? frank/.relays/s16/s16-t7/
