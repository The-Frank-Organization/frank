## IMPL — THE V32 UPDATED DIRECT DISPATCH (the REPLACEMENT token; supersedes `s16-impl/IMPL-orchestrator-planner-20260827-133445.md`, whose token becomes history — it authorizes nothing further): every updated-route condition is satisfied and independently verified at this seat — **s16.implementer's implementation authority now runs on the approved plan-5 `s16-build-plan-5` @ exact SHA-256 `e64e6077bde125a51afd2294f65ad15ea236e817a3d1289a9de7fc28b13a61b3`, Tasks T1–T9, on the existing branch `s16-integration`, under plan-5's §2.4 fence AS AMENDED** (the four FILE-EXACT connector loci + locus-6 on the existing appctl/appipc rows; `endpoint.go` ZERO bytes, asserted at close; `control_test.go` and every other connector byte OUT). This token authorizes implementation, nothing more; merge stays OPERATOR-ONLY behind the WP5 MERGE-GATE.

ROLE: Orchestrator Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16-impl-2
PARENT_DISPATCH_ID: s16-v32-request
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the V32 direct-dispatch conditions are met and recorded (pair approve + all-in SCOPE_DIFF over the amended fence + the complete join-confirmed T7 instrument authority); the operator's gates are downstream (the WP5 MERGE-GATE and the Step-3 ratification), and the operator is CC'd on this arming act
PLAN_LOCK_ID: s16-build-plan-5 @ sha256 e64e6077bde125a51afd2294f65ad15ea236e817a3d1289a9de7fc28b13a61b3
BUNDLE_ID: s16-integration
OWNER: s16
REPO: frank/ (workspace subtree)
BASE: the existing branch s16-integration continues (cut verified under the superseded dispatch; no restack)
BRANCH: s16-integration (worktree ~/Programming/harness-s16-integration; DRAFT PR open per V30)
TARGET_BRANCH: main (operator-only MERGE-GATE)
IN_REPLY_TO: s16-v32/SITREP-planner-20260827-220011.md
FROM: master.orchestrator-planner
TO: s16.implementer
CC: s16.planner, master.orchestrator-reviewer, operator, m-8.planner, m-10.planner, m-3.planner
SUBJECT: V32 REPLACEMENT DISPATCH — execute plan-5 T1–T9 at e64e6077 on the existing s16-integration branch; the T9 trust-root realization runs LOCUS-EXACT (four connector files + locus 6; endpoint.go zero bytes proven by exact diff at close); the seven addendum fixtures land in test/composed; CT-G03 + the 64/0/64 floor rails carry unchanged; T7 unblocks under the corrected localhost endpoint term

## §1 — The authority chain (complete; every leg re-verified at this seat before issue)

1. **The plan of record:** `s16-build/PLAN-planner-20260827-214808.md` @ exact `e64e6077bde125a51afd2294f65ad15ea236e817a3d1289a9de7fc28b13a61b3` (recomputed here) — the R4-F1-shaped successor: plan-3 `2956b9c5…` §§1–10 byte-whole + the ★ T7 additions only. Pair-APPROVED by `s16-build/PLAN-REVIEW-implementer-20260827-215632.md` (`s16-build-plan-review-5`, verdict approve over that exact hash; R4-F1/F2 verified closed by the reviewer's own mechanical diff; the r4 MUST-REVISE `3d4d6aea…` superseded history).
2. **The SCOPE_DIFF:** the planner's mechanical diff (`s16-v32/SITREP-planner-20260827-220011.md` §2) re-run independently at this seat — ALL-IN against plan-5 §2.4 AS AMENDED; the four connector loci file-exact; `endpoint.go` correctly ABSENT from the write rows (an exclusion assertion, not a grant); no fence row unaccounted; `control_test.go`, every other `frank/internal/connector/**` byte, `frank/internal/seatclient/**`, `master/**`, and the frozen exit corpus touched by NO task.
3. **The T7 instrument authority, COMPLETE and join-confirmed:** the addendum r5 @ `cb154641…` (pair-approved `180445`; frozen r12 + A-M8-BE byte-unmoved) · m-10's six-point confirmation `204742` (= the vehicle-(a) ownership record for locus 6) · m-3's P1–P4 discharge `204725` · the corrected endpoint owner term (`https://localhost:<nondefault-port>/<path>`, SAN=localhost — master-ack `s16-t7-tls-ack/…165443`) · m-8's owner return `210815` concurring. Carried down at `s16-t7/RECONCILE-orchestrator-planner-20260827-210714.md`.
4. **The route:** V32 (the trigger-present scan bars any pair-issued token); this relay IS the fresh updated direct authority relay, addressed exactly to `s16.implementer`, carrying the approved amended fence.

## §2 — The grant and its rails

**Execute plan-5's Tasks T1–T9 in your own order and mechanics, straight-through** (T1–T2 and completed work stand — nothing re-executes for its own sake), under these rails, all from the plan's own bytes:

- Every write inside plan-5 §2.4's AMENDED fence; the connector work LOCUS-EXACT: `cmd/frank-connector/main.go` · `internal/connector/control/control.go` (the EXACT file) · `internal/connector/service/service.go` · `internal/connector/transport/transport.go` · locus 6 on the existing appctl/appipc rows. A byte in ANY other connector file — `endpoint.go` and `control_test.go` above all — is a fence deviation: STOP → UP. The `endpoint.go` zero-byte assertion is PROVEN by exact diff in the WP1-close SITREP.
- The addendum's terms govern where prose diverges (plan-5 §4 ★): additive+opt-in · fail-closed-to-production · transport-layer-only · non-secret public-CA-only · argv-before-Clearenv, the PATH never in the manifest/`connector_assign`/any conductor record · the §2 freeze-binding + XOR withhold · the READY-gated CA-qualified verification chain with the TRUST-SPECIFIC typed fault · §3 REPLACE-not-augment · the observer re-derivation rail. The SEVEN addendum fixtures (incl. fixture 4's observable-RED replacement control and fixture 6's observer re-derivation catch) land in `frank/test/composed/**` — never in the connector tree.
- T7 runs against the corrected endpoint term: `https://localhost:<nondefault-port>/<path>`, SAN=localhost, demo-owned loopback; the exact port/path a seeded lane-catalog fact under the frozen-lane check.
- The standing rails carry UNCHANGED from the superseded dispatch: the regression floor (seam census exactly 64/0/64 · plain suite `go test -p=1 -count=1 ./...` · `go vet ./...`) green at EVERY commit; the CT-G03 act ONLY per `SEAM-BATTERY-S16-G03-1`'s exact terms; `go.mod`/`go.sum` arbitration-only; frozen members untouchable; `master/**` master-only; findings route UP, never absorbed; the DRAFT PR refreshed per V30.
- At completion: the WP1-close SITREP per plan-5 §7 (now additionally carrying the T9 evidence: the locus-exact diff set, the `endpoint.go` zero-byte exact-diff proof, the seven fixture results, the m-3 capture map incl. the observer re-derivation) TO `s16.planner`, CC master; the pair reconciles; the planner routes the WP1 close + WP2 request UP. **No merge claim by any s16 relay, ever.**

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this replacement dispatch drafted at .engine/drafts/master.orchestrator-planner/ on the s16 root and submitted through the v2.9.1 client; pre-issue verification was read/hash-only (plan-5 + both reviews + the request + m-8's return read in full; plan-5's hash recomputed exact; the SCOPE_DIFF re-run against the amended fence; the full carrier set exact-file-linted); no source, test, branch, commit, push, PR, or merge byte by this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays/CHECKPOINTS.md
 M master/relays2/INDEX.md
?? frank/.relays/s16/s16-build/PLAN-REVIEW-implementer-20260827-214207.md
?? frank/.relays/s16/s16-build/PLAN-REVIEW-implementer-20260827-215632.md
?? frank/.relays/s16/s16-build/PLAN-planner-20260827-212142.md
?? frank/.relays/s16/s16-build/PLAN-planner-20260827-214808.md
?? frank/.relays/s16/s16-v32/
?? master/relays2/s16-t7-tls-confirm/SITREP-planner-20260827-210815.md
