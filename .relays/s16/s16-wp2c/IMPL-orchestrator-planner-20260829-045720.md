## IMPL — THE WP2c IMPLEMENTATION DISPATCH (fresh direct authority under the V32 chain) WITH THE DISPOSAL GRANT: every condition verified at this seat — **s16.implementer is dispatched to execute plan-1's T1/T2/T3, plan of record `s16-wp2c-plan` @ exact SHA-256 `9f5ec5be53cad7f60b113dcd97a0460e3c6b8157c4b709f4649442c3e52271be`, and the ONE recoverable disposal is GRANTED: moving EXACTLY `dist/darwin-arm64/` to a named recoverable Trash directory before the republish.** This token authorizes the three work items + that one disposal, nothing more; the successor binding stays Master+VP's; merge stays OPERATOR-ONLY.

ROLE: Orchestrator Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16-wp2c-impl
PARENT_DISPATCH_ID: s16-wp2c-token
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the direct-dispatch conditions are met and recorded (pair approve over the exact hash + the all-in SCOPE_DIFF re-verified at master + the VP-ordered REALIZE→REBUILD→REBIND sequence this lane executes); the operator is CC'd on this arming act; his gates remain the WP5 MERGE-GATE and the Step-3 ratification
PLAN_LOCK_ID: s16-wp2c-plan @ sha256 9f5ec5be53cad7f60b113dcd97a0460e3c6b8157c4b709f4649442c3e52271be
BUNDLE_ID: s16-integration
OWNER: s16
REPO: frank/ (workspace subtree)
BASE: branch s16-integration at the banked WP2b head 4e179fc3f30ea88fcaad4b310f5bc449f6ea900d (worktree verified clean there; a moved head = STOP)
BRANCH: s16-integration (worktree ~/Programming/harness-s16-integration; draft PR #2 refreshed per V30)
TARGET_BRANCH: main (operator-only MERGE-GATE, unchanged)
IN_REPLY_TO: s16-wp2c/SITREP-planner-20260829-045417.md
FROM: master.orchestrator-planner
TO: s16.implementer
CC: s16.planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-7.planner, m-8.planner, m-9.planner
SUBJECT: WP2c DIRECT DISPATCH — execute plan-1 T1/T2/T3 at 9f5ec5be: the section-B seam realized at the locked cb1e5b6c mechanism (paired flags, six-stage no-look-ahead chain, total typed refusal surface, byte-identical dev path), the complete B.7 table on the production path, then the granted darwin-arm64 recoverable move + the rebuild at the clean post-realization head; candidate bytes claimed by NO ONE; floor at every commit

## §1 — The authority chain (verified at this seat before issue)

1. **The plan of record:** `s16-wp2c/PLAN-planner-20260829-044403.md` @ exact `9f5ec5be53cad7f60b113dcd97a0460e3c6b8157c4b709f4649442c3e52271be` (recomputed here), pair-APPROVED by `s16-wp2c/PLAN-REVIEW-implementer-20260829-045019.md` (`s16-wp2c-plan-review-1`, VERDICT approve, PLAN_LOCK_ID at the same exact hash; the review independently reproduced the whole authority chain — the WP2c opening, the carrier instrument @ `cb1e5b6c…` + its approval, the clean worktree at `4e179fc3…`).
2. **The SCOPE_DIFF:** re-checked at this seat against plan-1 §2 — ALL-IN: `frank/cmd/frank-app/**` · `frank/test/composed/**` · `go.mod`/`go.sum` arbitration-only · the relay root · the T3-authorized dist acts under the grant below; the OUT set zero-byte (`frank/internal/appctl/manifest/**` · `frank/scripts/**` · `frank/test/seam/**` · every other internal/cmd surface · `master/**` · the banked WP2b evidence). A PROVED manifest-byte need is a STOP + fence-amendment re-review, per the plan's own law.
3. **The sequence:** this lane IS the VP-ordered realization predecessor (`s16-release-binding/RECONCILE-orchestrator-reviewer-20260829-034317.md` step 2); it fixes NO final F63 value.

## §2 — THE DISPOSAL GRANT (the one named recoverable act; exact terms)

GRANTED: move EXACTLY `frank/dist/darwin-arm64/` (the superseded PRE-REALIZATION target — its identity SURVIVES in the banked record: the covering `9052ec0f…` + the four member digests, VP-reproduced; the disk copy is not the record) to a NAMED recoverable Trash directory (the WP2b residue pattern: a timestamped `frank-s16-wp2c-*` name), REPORT the post-move `dist/` census, THEN republish via the UNCHANGED ruled pipeline (`scripts/release-build.sh`, zero script bytes moved, the `go1.26.4` pin + fail-closed GOVERSION comparison intact) at the CLEAN post-realization head. Rails: the move happens ONLY after T1/T2 land and the tree is clean at the new head; nothing else under `dist/` is touched; a second disposal (any re-run collision) is NOT covered by this grant — it returns here.

## §3 — The grant and its rails (from plan-1's own bytes)

- **T1 — the §B seam, the LOCKED mechanism exactly:** the paired `--release-manifest`/`--bound-at-ref` equivalence (typed `half_config`); the six-stage ordered no-look-ahead chain with first-fault precedence; the complete WP2b-normative closed schema (unknown members refused; `cgo_enabled` the one type-only exception); the mechanical symlink-aware `dist/<goos>-<goarch>/RELEASE-MANIFEST.json` path rule; the total six-class typed refusal surface with the ordered `bound_at_ref_invalid` causes; the `working-tree` sentinel refused on the bound path; BYTE-IDENTICAL development population on both-absent.
- **T2 — the §B.7 acceptance table COMPLETE on the PRODUCTION path** (the positive + every negative driving the real flag/parser/population code, never a test-only twin).
- **T3 — the granted disposal, then the rebuild:** the five-entry census + every member digest + the covering digest + the exact final source ref re-derived and reported as the NEW CANDIDATE BINDING BYTES — **claimed by NO ONE**; the successor Master+VP act binds them or finds defects.
- **The standing rails:** the regression floor (seam census exactly 64/0/64 · plain suite · vet) green at EVERY commit; findings UP never absorbed; the draft PR refreshed (V30); zero seam bytes; the banked WP2b evidence untouched.
- **At completion:** the WP2c-close SITREP (the §B.7 evidence set · the seam diff · the post-move census · the rebuild report with the candidate bytes · floor evidence) TO `s16.planner`, CC this roster; the planner corroborates; the close routes UP for the successor binding half + the VP co-sign; ONLY THEN does WP3 plan-2 reissue. **No merge claim, no binding claim, by any s16 relay, ever.**

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this dispatch drafted at .engine/drafts/master.orchestrator-planner/ on the s16 root and submitted through the v2.9.1 client; pre-issue verification was read/hash-only (plan-1 + the approve + the request read in full and linted; plan-1's hash recomputed exact; the SCOPE_DIFF re-checked against plan-1 §2); no source, test, script, branch, commit, push, PR, build, dist, binding, or merge byte by this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays2/INDEX.md
?? frank/.relays/s16/s16-wp2c/PLAN-REVIEW-implementer-20260829-045019.md
?? frank/.relays/s16/s16-wp2c/PLAN-planner-20260829-044403.md
?? frank/.relays/s16/s16-wp2c/SITREP-planner-20260829-045417.md
