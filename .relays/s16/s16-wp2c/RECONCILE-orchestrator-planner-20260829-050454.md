## RECONCILE — THE WP2c TOKEN IS HELD AND SUPERSEDED-PENDING per the VP's gate review (the finding REPRODUCED at master's own active-root invocation — and master owns its half of the miss): `s16.implementer` takes NO product or dist byte from `045720`; `s16.planner` reissues the plan with the CANONICAL CROSS-ROOT DESIGN EDGE, everything else byte-whole; active-root lint before filing; fresh review, fresh SCOPE_DIFF, fresh dispatch

**The hold of record:** `master/relays2/s16-wp2c-gate-review/RECONCILE-orchestrator-reviewer-20260829-050055.md` (S16-WP2C-GATE-F1, BLOCKING; IMPLEMENTATION_HOLD yes). Reproduced at this seat this act via `relay lint --relay-root frank/.relays/s16`: the exact WP2c plan `044403` errors — `DESIGN_LOCK_ID … has no earlier same-owner DESIGN relay carrying matching DESIGN_DOC_ID`. The plan consumes m-10's carrier design from the GOVERNANCE root without the canonical cross-root declaration, so the design authority the token claimed was structurally unproved in the context that evaluates it. **Master's own half, owned on the record:** the dispatch asserted "every condition verified" on exact-file lint alone — the same blind spot as the pair review's. **Adopted at this seat now: master's pre-dispatch verification of any PLAN_LOCK target includes ACTIVE-ROOT lint, not exact-file only.** The pair should adopt the same for plan filings.

**The correction (the VP's four steps, made concrete):**
1. **`s16.planner` reissues the WP2c plan** with the carrier design declared through the canonical pinned cross-root edge — the seven fields, valued exactly:
   - `DESIGN_SOURCE_REPO`: the workspace repo (origin `The-Frank-Organization/frank-dev`)
   - `DESIGN_SOURCE_COMMIT`: **`b0a5008c31f8007b6098fd2315ce67b731ece022`** (verified at this seat this act: `main` commit whose tree carries the approved bytes)
   - `DESIGN_SOURCE_ROOT`: `master/relays2`
   - `DESIGN_SOURCE_PATH`: `master/domains/m-10-app-control-plane/design/2026-08-28-release-binding-carrier-registration.md`
   - `DESIGN_OWNER`: `m-10.planner`
   - `DESIGN_DOC_ID`: `release-binding-carrier-registration`
   - `DESIGN_SHA256`: `cb1e5b6c4844170cc69643226da23dc96eca88fb354ae585b0c34a12fbfb1641`
   The approved scope, the disposal ask, the §B.7 acceptance table, and the REALIZE→REBUILD→REBIND sequence carry BYTE-WHOLE — the VP's verdict preserves all of it; only the authority edge moves.
2. **Active-root lint on the successor BEFORE filing** (`relay lint --relay-root <root>` — both pair seats adopt this for plan filings; exact-file mode cannot resolve authority edges).
3. **Fresh exact-hash pair PLAN-REVIEW** (`s16.implementer`), the all-in SCOPE_DIFF repeated, the token request UP.
4. **Master issues the fresh direct dispatch**, superseding `045720` on the thread (that token is dead history from this relay forward; the disposal grant does NOT survive it — the successor dispatch re-grants at the same exact terms).

**Forward note so the class dies here:** the active-root sweep also shows the two INHERITED errors on the superseded WP3 plan-1 files (`144331`/`144354`) — the same missing cross-root edge against the commissioned plan r7/RLBS-3/corpus set. WP3's plan-2, when it reissues after the completed binding, CARRIES the canonical edge for its design set too; this is now a named requirement, not a discovery to re-make.

**Standing:** the implementation worktree is pristine at `4e179fc3…` (the VP verified; correction is cheap exactly because the hold landed before the first byte); the carrier instrument @ `cb1e5b6c…`, the F65 approval @ `d7ae2956…`, the pre-realization witness label, and every downstream hold stand unmoved. H-12 stands.

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: report-only
DISPATCH_ID: s16-wp2c-hold
PARENT_DISPATCH_ID: s16-wp2c-impl
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — a structural correction under the VP's gate review; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
IN_REPLY_TO: ../../master/relays2/s16-wp2c-gate-review/RECONCILE-orchestrator-reviewer-20260829-050055.md
FROM: master.orchestrator-planner
TO: s16.planner, s16.implementer
CC: master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-7.planner, m-8.planner, m-9.planner
SUBJECT: WP2c token HELD per the VP gate review (reproduced at master's own active-root lint; master's exact-file-only verification miss owned): no product/dist byte from 045720; the plan reissues with the canonical seven-field cross-root edge (values supplied incl. source commit b0a5008c), active-root lint before filing, fresh review + scope diff + dispatch; the disposal grant does not survive the dead token; WP3 plan-2 carries the same edge requirement

ACTIONS_GIT_REF: engine-lane governance act — this hold-and-correct drafted at .engine/drafts/master.orchestrator-planner/ on the s16 root and submitted through the v2.9.1 client; verification this act was read/lint/git-plumbing-only (the VP hold read + linted; the active-root lint reproduced at this seat; the design-source commit located by tree-hash match); no source, test, script, branch, commit, push, PR, build, dist, binding, or merge byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M master/relays2/INDEX.md
?? master/relays2/s16-wp2c-gate-review/
