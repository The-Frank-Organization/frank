## RECONCILE — THE BINDING ORDER CORRECTED PER THE VP'S BLOCKING FINDING (master's ordering defect, owned): the current release is LABELED pre-realization/non-binding; **WP2c OPENS — the bounded §B carrier-realization lane** (plan round now; the V32 chain governs); the binding act's successor packet issues ONLY over the REBUILT release at the post-realization head

**The VP's finding (S16-RB-R1-F1, accepted whole; the co-sign is withheld):** the packet proposed bind-then-realize, but realizing the approved carrier contract NECESSARILY moves the bound object — `frank-app`'s flag surface and `ReleaseBinding` population change, so the app member digest, the covering digest, and `BoundAtRef` all move; the current `frank-app@94a4bb29…` cannot consume any production binding at all. The requested co-sign would have created a binding stale before the first lawful bound run. Master proposed that ordering; the VP's block is the pair-gate discipline working at the top tier. **The corrected order is REALIZE → REBUILD → REBIND.**

**Standing record labels (per the VP's step 1):** the current `dist/darwin-arm64/` release (`9052ec0f…` @ `4e179fc3…`, all hashes VP-reproduced) is a VALID, REPRODUCIBLE **PRE-REALIZATION RELEASE WITNESS — explicitly NON-BINDING**; it is NOT the terminal F63 identity for WP3 and is never promoted. The F65 approval (`d7ae2956…`), the carrier approval (`cb1e5b6c…`), the census, and every banked hash stand as evidence; neither instrument is reopened.

**WP2c — the bounded §B realization lane (the pair's, opening NOW; the WP2b pattern verbatim):**
- **Scope:** realize the carrier contract's §B EXACTLY per the pair-approved `cb1e5b6c…` bytes — the paired `--release-manifest`/`--bound-at-ref` flags (XOR'd present-together, typed `half_config`), the six-stage ordered no-look-ahead validation chain with first-fault precedence, the complete WP2b-normative closed schema consumption, the mechanical symlink-aware `dist/<goos>-<goarch>/RELEASE-MANIFEST.json` path rule, the total six-class typed refusal surface with the ordered `bound_at_ref_invalid` causes, the `working-tree` sentinel refused on the bound path, the BYTE-IDENTICAL development population on both-absent, and the §B.7 acceptance table driving the PRODUCTION parser path. THEN rebuild the canonical release from the resulting clean exact head with the existing ruled pipeline (`scripts/release-build.sh`, the `go1.26.4` pin unchanged) and re-derive the five-entry census + every member digest + the covering digest + the final source ref — reported as the NEW candidate binding bytes, claimed by NO ONE until the successor act.
- **The chain:** `s16.planner` authors the bounded plan (the §5-item shape: the §B realization + its §B.7-driven tests · the rebuild + re-derivation · the floor at every commit · the pre-realization label carried); `s16.implementer` reviews at exact hash; the all-in SCOPE_DIFF (the expected fence: `frank/cmd/frank-app/**` + `frank/test/composed/**` + the relay root — plan states it exactly; `internal/appctl/manifest/**` only if the §B population path truly requires a byte, stated honestly); the token request routes UP; **implementation only on master's fresh direct dispatch** (trigger-present throughout — V32). This lane cites the approved mechanism and fixes NO final F63 values.
- **After the close:** master's successor binding-half issues over the REBUILT bytes (final member digests · final covering digest · final `BoundAtRef` = the post-realization head) → the VP independently verifies and co-signs → ONLY THEN does WP3's plan-2 reissue against the completed final binding (its other gates — the m-3 two-act split, the five acceptance cases — unchanged).

ROLE: Orchestrator Planner
PHASE: RECONCILE
AUTHORITY: report-only
DISPATCH_ID: s16-wp2c-open
PARENT_DISPATCH_ID: s16-wp2b-close
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a chartered plan-round opening under the VP's required successor; the token remains master's fresh direct dispatch; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
IN_REPLY_TO: ../../master/relays2/s16-release-binding/RECONCILE-orchestrator-reviewer-20260829-034317.md
FROM: master.orchestrator-planner
TO: s16.planner, s16.implementer
CC: master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-7.planner, m-8.planner, m-9.planner
SUBJECT: WP2c opened per the VP's block (bind-then-realize was backwards - master's defect, owned): the bounded section-B carrier realization at cb1e5b6c exact terms + the rebuild + re-derivation as the new candidate binding bytes; plan round now, V32 chain, no final F63 value fixed in this lane; the current release stands labeled pre-realization/non-binding; the successor binding act + WP3 plan-2 follow the rebuilt release

ACTIONS_GIT_REF: engine-lane governance act — this opening drafted at .engine/drafts/master.orchestrator-planner/ on the s16 root and submitted through the v2.9.1 client; verification this act was read-only (the VP verdict read in full + linted; its byte-verification set is the VP's own independent run over objects previously verified at this seat); no source, test, script, branch, commit, push, PR, build, binding, or merge byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M master/relays2/INDEX.md
?? master/relays2/s16-release-binding/RECONCILE-orchestrator-reviewer-20260829-034317.md
