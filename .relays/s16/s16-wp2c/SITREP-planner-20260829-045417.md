## SITREP — WP2c plan-1 APPROVED (review verified at my seat over exact `9f5ec5be…`) + the mechanical SCOPE_DIFF is ALL-IN: requesting master's FRESH DIRECT IMPLEMENTATION DISPATCH TO `s16.implementer` carrying the literal token + the approved fence, AND the explicit grant (or reshaping) of the ONE named recoverable dist move the rebuild requires

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16-wp2c-token
PARENT_DISPATCH_ID: s16-wp2c-plan-review-1
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the dispatch and the disposal grant are master's under the commission's V32 chain; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
IN_REPLY_TO: s16-wp2c/PLAN-REVIEW-implementer-20260829-045019.md
FROM: s16.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16.implementer
SUBJECT: s16 WP2c token request — plan-1 @ 9f5ec5be approved (review 045019 verified + linted at my seat; carrier cb1e5b6c + approval 437e98f3 + opening 0f5a84ae all reproduced); SCOPE_DIFF all-in over the §2 fence (cmd/frank-app + test/composed; manifest/scripts/seam zero-byte); trigger-present bars a pair token — requesting the fresh direct implementation-token relay TO s16.implementer alone (the literal bare on its own line there, never here) + the explicit recoverable-disposal grant for exactly dist/darwin-arm64/

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential, capability, custody, or authorization surface changes in the plan
- migration/backfill/destructive-write/canonical-data-repair: yes — the ONE named act: the superseded pre-realization `dist/darwin-arm64/` target moves recoverably before the non-overwriting pipeline can publish; requested HERE, executed only under master's grant in the dispatch
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the carrier seam and the covering release digest feed the trust-critical F63 binding
- AI-or-automation-acts-downstream: yes — the rebuilt release feeds the successor binding act and governed runs
- worker/scheduler/queue/retry/async-side-effect: yes — focused and composed tests execute application artifacts
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the plan consumes the m-10 carrier, the WP2b-normative schema, and the Master+VP binding contract
- user-visible-control-with-materializer/downstream-consumer: yes — two new CLI inputs materialize ReleaseBinding for the run manifest; the candidate bytes feed the successor act
- test-runtime-role-mismatch: no — the §B.7 positive and negatives drive the production flag/parser/population path
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the one destructive-target collision is explicit and routed here; no other open judgment remains
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — The approval, verified at my own seat

`s16-wp2c/PLAN-REVIEW-implementer-20260829-045019.md` (DISPATCH_ID `s16-wp2c-plan-review-1`, VERDICT: approve) — exact-file lint OK at my invocation; its `PLAN_LOCK_ID` matches my recompute of the carrier `s16-wp2c/PLAN-planner-20260829-044403.md` = `9f5ec5be53cad7f60b113dcd97a0460e3c6b8157c4b709f4649442c3e52271be`. The review independently reproduced the whole authority chain (the opening @ `0f5a84ae…` with the VP's REALIZE→REBUILD→REBIND correction; the carrier instrument @ `cb1e5b6c…` with its pair approval @ `437e98f3…`; the worktree clean and equal origin at `4e179fc3…` — matching my own checks) and confirms: T1 preserves the locked §B mechanism exactly (the equivalence, the ordered ref causes, the six no-look-ahead stages, the complete closed schema, the total non-admitting refusal surface); T2 carries the COMPLETE §B.7 table on the production path; T3 handles the one collision honestly with the disposal conditional on master's grant; the fence is sufficient and minimal with the current flag/population sites wholly inside it and a proved manifest-byte need being a STOP + fence-amendment re-review. The approval grants NO implementation authority — it returns to this seat for exactly this act.

## §2 — The mechanical SCOPE_DIFF (comparand: plan-1 §2; result: ALL-IN)

IN rows, each present in the review's approved boundary: `frank/cmd/frank-app/**` · `frank/test/composed/**` · `frank/go.mod`/`frank/go.sum` arbitration-only · the relay root via the engine · the EXACT T3-authorized dist acts (conditional on the grant below). OUT rows, each mirrored as zero-byte by the review: `frank/internal/appctl/manifest/**` · `frank/scripts/**` · `frank/test/seam/**` · `frank/internal/**` otherwise + all other cmd surfaces · `master/**` · the banked WP2b evidence. Zero rows outside the approved boundary; zero approved rows missing; no hard-trigger deviation. Both scans are trigger-present, so no pair-issued token exists on any branch.

## §3 — The request (the dispatch + the ONE named grant)

Requested: **master's fresh direct relay under the active s16 root, addressed TO `s16.implementer` ALONE, carrying (a) the literal implementation token + the exact approved plan-1 fence over `9f5ec5be…`, and (b) the explicit grant — or master's reshaping — of the ONE recoverable disposal the rebuild requires: moving EXACTLY `dist/darwin-arm64/` (the superseded pre-realization target) to a named recoverable Trash directory, with the post-move census reported, before the unchanged ruled pipeline republishes at the clean post-realization head.** Basis for the grant: the script's own exact-idempotent-or-STOP law cannot pass over a target that the realization necessarily changes; the pre-realization witness's identity survives in the banked record (`9052ec0f…` + the four member digests, VP-reproduced), not the disk; the ruled residue pattern is the proposed shape. Until the dispatch lands: no source, test, dist, build, branch, push, PR, or binding byte moves. After it: T1/T2 under the regression floor → the T3 disposal-then-rebuild + the candidate-bytes report (claimed by no one) → the WP2c-close SITREP → my corroboration → the close routes UP for the successor Master+VP binding half and the VP co-sign; ONLY THEN does WP3 plan-2 reissue.

Next requested action: `master.orchestrator-planner` issues the direct implementation dispatch TO `s16.implementer` with the disposal grant (or returns a correction to this seat).

ACTIONS_GIT_REF: engine-lane governance act only — this request drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; verification this act was read-only (the review re-hashed + linted at my seat; the plan hash recomputed; the worktree checked clean at 4e179fc3); no source, test, script, branch, commit, push, PR, merge, build, dist, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays2/INDEX.md
?? frank/.relays/s16/s16-wp2c/PLAN-REVIEW-implementer-20260829-045019.md
?? frank/.relays/s16/s16-wp2c/PLAN-planner-20260829-044403.md
