## PLAN — WP2c REISSUE (s16-wp2c-plan-2): the S16-WP2C-GATE-F1 correction — the carrier design now declared through the CANONICAL SEVEN-FIELD CROSS-ROOT EDGE (values master-supplied, verified at my seat); the approved scope, the disposal ask, the §B.7 table, and the REALIZE→REBUILD→REBIND sequence carry BYTE-WHOLE; the bounded §B carrier realization EXACTLY per the pair-approved r3 @ `cb1e5b6c…` — the paired-flag consumption seam + the §B.7 acceptance table on the PRODUCTION parser path, then the rebuild at the clean post-realization head + the re-derived NEW CANDIDATE binding bytes (claimed by NO ONE until the successor Master+VP act); the pre-realization witness label carried; one named collision routed with the token request (the superseded dist target vs the script's no-overwrite law)

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp2c-plan-2
PARENT_DISPATCH_ID: s16-wp2c-hold
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the chartered WP2c plan round; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — the plan realizes the pair-approved carrier instrument's exact bytes and master's corrected REALIZE→REBUILD→REBIND order; it opens no new design decision
DESIGN_RECORD_KIND: design-doc
DESIGN_SOURCE_REPO: .
DESIGN_SOURCE_COMMIT: b0a5008c31f8007b6098fd2315ce67b731ece022
DESIGN_SOURCE_ROOT: master/relays2
DESIGN_SOURCE_PATH: master/domains/m-10-app-control-plane/design/2026-08-28-release-binding-carrier-registration.md
DESIGN_OWNER: m-10
DESIGN_DOC_ID: release-binding-carrier-registration
DESIGN_SHA256: cb1e5b6c4844170cc69643226da23dc96eca88fb354ae585b0c34a12fbfb1641
DESIGN_LOCK_ID: release-binding-carrier-registration r3 @ sha256 cb1e5b6c4844170cc69643226da23dc96eca88fb354ae585b0c34a12fbfb1641 (m-10 pair-approved, verdict relay 022929 @ 437e98f3…) + the WP2c opening s16-wp2c-open
IN_REPLY_TO: s16-wp2c/RECONCILE-orchestrator-planner-20260829-050454.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-10.planner, m-3.planner, m-7.planner, m-8.planner, m-9.planner
SUBJECT: s16 WP2c plan-2 (the gate-F1 reissue; scope byte-whole, only the authority edge + lineage move) — realize carrier §B at cb1e5b6c exact terms (paired --release-manifest/--bound-at-ref equivalence, six-stage no-look-ahead chain w/ first-fault precedence, complete WP2b-normative closed schema, symlink-aware dist path rule, total typed refusals w/ ordered ref causes, byte-identical dev population on both-absent, §B.7 table on the production path); then rebuild via the unchanged ruled pipeline + re-derive the candidate bytes; fence cmd/frank-app + test/composed only, manifest/** and scripts/** ZERO bytes; disposal of the superseded dist target routed with the token request

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — the seam consumes operator-passed values and published bytes; no credential surface moves
- migration/backfill/destructive-write/canonical-data-repair: yes — the rebuild requires disposing the SUPERSEDED pre-realization `dist/darwin-arm64/` target (else the script's own exact-idempotent-or-STOP law refuses); proposed as a RECOVERABLE move (the ruled residue pattern), authorization ROUTED with the token request, never taken by the pair alone
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the release-carrier seam and the candidate binding bytes are trust-critical acceptance surfaces
- AI-or-automation-acts-downstream: yes — the rebuilt bytes feed the successor binding act and WP3
- worker/scheduler/queue/retry/async-side-effect: yes — composed tests execute the application artifacts
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the realization consumes the m-10 carrier contract and the WP2b-normative schema by hash
- user-visible-control-with-materializer/downstream-consumer: yes — the successor Master+VP packet consumes the re-derived bytes
- test-runtime-role-mismatch: no — §B.7's own law: the positive drives the PRODUCTION flag/parser/population path, never a hand-built ReleaseBinding
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the one named collision (the superseded target vs the no-overwrite law) is routed, not waived; no other open judgment
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §0 — The reissue (S16-WP2C-GATE-F1; nothing of the approved scope moves)

*Same-dispatch correction note: the first plan-2 filing (`…050800`) carried the repo slug in `DESIGN_SOURCE_REPO`; the verifier resolves that field as a DIRECTORY (the repo toplevel), so the successor carries `.` — the consuming root's own workspace-repo toplevel; the second filing (`…050905`) then failed the authority-owner axis because the verifier derives the origin OWNER (`m-10`), not the dotted seat address, so this successor carries `DESIGN_OWNER: m-10`. Everything else is byte-identical to `050800` except this note and the fresh status tail. Both supplied-value corrections are mechanical verifier-authoritative fixes; the instrument, hash, commit, root, path, and doc id are unchanged.*

The token over plan-1 is DEAD HISTORY per the hold `s16-wp2c/RECONCILE-orchestrator-planner-20260829-050454.md`: plan-1 (`s16-wp2c/PLAN-planner-20260829-044403.md` @ `9f5ec5be…`) consumed m-10's carrier design from the governance root WITHOUT the canonical cross-root declaration, so the active-root lint could not prove the design authority (`DESIGN_LOCK_ID … has no earlier same-owner DESIGN relay carrying matching DESIGN_DOC_ID` — REPRODUCED at my own active-root invocation this authoring, alongside the two inherited WP3 plan-1 errors master names). This successor adds EXACTLY the seven-field edge above (each value verified at my seat: the repo field resolves to the workspace repo toplevel, whose origin is The-Frank-Organization/frank-dev; `b0a5008c…` is an ancestor of `main`; `git show b0a5008c…:<path>` hashes EXACTLY `cb1e5b6c…`) and moves the lineage to the hold; §§1–6 below are plan-1's bytes CARRIED WHOLE (the approved scope, the disposal ask, the §B.7 acceptance table, the REALIZE→REBUILD→REBIND sequence — the VP's verdict preserves all of it; only the authority edge moves). Both pair seats adopt the hold's practice: ACTIVE-ROOT lint before every plan filing, exact-file lint alone never again proves an authority edge. The disposal grant did NOT survive the dead token — the token request re-asks it and the successor dispatch re-grants at the same exact terms. The prior approval (`s16-wp2c-plan-review-1` @ over `9f5ec5be…`) is superseded-pending: a FRESH exact-hash review of THIS carrier is required.

## §1 — The governing basis (verified at my seat this authoring)

The WP2c opening `s16-wp2c/RECONCILE-orchestrator-planner-20260829-043807.md` (exact-file lint OK at my seat): the VP's blocking finding S16-RB-R1-F1 accepted — realizing the carrier NECESSARILY moves the bound object, so the order is **REALIZE → REBUILD → REBIND**; the current `dist/darwin-arm64/` release (covering digest `9052ec0f…` @ head `4e179fc3…`) stands labeled a VALID, REPRODUCIBLE **PRE-REALIZATION RELEASE WITNESS — explicitly NON-BINDING**, never promoted; this lane fixes NO final F63 value. **The instrument of record:** `master/domains/m-10-app-control-plane/design/2026-08-28-release-binding-carrier-registration.md` r3, re-hashed at my seat = `cb1e5b6c4844170cc69643226da23dc96eca88fb354ae585b0c34a12fbfb1641`, PAIR-APPROVED over that exact hash (`master/relays2/s16-wp3-carrier/DESIGN-REVIEW-implementer-20260829-022929.md` @ `437e98f3…`, verdict approve, lint OK at my seat). Its §A (Ruling 1) needs NO code: the covering `release_digest` arm is already lawful in the closed XOR grammar and transitively binds all four F63 members including the broker. Its §B (Ruling 2) is THE realization contract, consumed below BY REFERENCE — the r3 bytes at the pinned hash GOVERN; this plan quotes the load-bearing terms and re-derives nothing. The §B.3b normative schema input = the approved WP2b recipe `s16-wp2b/PLAN-planner-20260828-051200.md` @ `a35b9c25…` `:54`. The toolchain pin ruling (`go1.26.4`, `050242` @ `544f1d05…`) stands untouched. The banked WP1/WP2/WP2b claims stand at their filed grain; base = the banked head `4e179fc3…` (worktree verified clean there this authoring).

## §2 — Scope and the EXACT fence (the SCOPE_DIFF comparand)

IN: `frank/cmd/frank-app/**` (the §B seam: flags, validation chain, population, package tests) · `frank/test/composed/**` (the §B.7 fixtures that need the composed/dist-form surface; no weakening of any existing assertion) · `frank/go.mod`/`frank/go.sum` arbitration-only · the relay root via the engine. Explicitly OUT: **`frank/internal/appctl/manifest/**` ZERO bytes** — §B.6's own scope law (the serve gate's `validateRelease` XOR arms-law unchanged; both population fields `BoundAtRef`/`ReleaseDigest` already exist); if realization PROVES a manifest byte necessary, that is a STOP + a bounded fence-amendment round through re-review, never a silent edit · **`frank/scripts/**` ZERO bytes** (T3 RUNS the ruled pipeline unchanged) · `frank/test/seam/**` (ZERO bytes) · `frank/internal/**` otherwise + all other cmd surfaces · `master/**` (the instrument is m-10's; the fold rides m-10's queue) · the banked WP2b relay evidence (untouchable) · `dist/**` except EXACTLY the T3-authorized acts below.

## §3 — The work items (the §B terms by reference; first-fault precedence everywhere)

- **T1 — the §B consumption seam in `cmd/frank-app`, exactly the r3 bytes:** the two PAIRED optional flags `--release-manifest <path>` / `--bound-at-ref <ref>` beside `--state-dir` — present-together-or-absent-together (an EQUIVALENCE; "XOR" stays reserved for the `ReleaseBinding` arms law); exactly one present ⇒ the typed `half_config` refusal, no admission. Both-absent ⇒ the development population **BYTE-UNCHANGED** (`BoundAtRef: "working-tree"` + the labeled development digest — fail-closed-to-development). Both-present ⇒ the §B.4 ref checks FIRST (independent, before stage (i); total ordered precedence `empty → over_length → embedded_nul → invalid_utf8 → dev_sentinel`, one deterministic cause) then the SIX ordered stages WITHOUT look-ahead, FIRST-FAULT precedence: (i) read ⇒ `manifest_unreadable` · (ii) symlink-resolved path SHAPE `dist/<t1>-<t2>/RELEASE-MANIFEST.json` + tuple CAPTURE, no manifest field consulted ⇒ `path_form_violation` · (iii) the canonical round-trip via the ONE `canonicaljson` primitive + exactly one trailing LF ⇒ `non_canonical` (duplicates' ONE reachable class — top-level and nested) · (iv) the §B.3b COMPLETE closed schema (eight members exact; `cgo_enabled` the ONE recorded-not-value-validated exception; `goversion_verified == toolchain_pin` enforced; the pin literal CONSUMED from the locked ruling, never floated; four F63 members name-sorted) ⇒ `structural_violation` with the violated clause distinguishable · (v) tuple-BINDING against the now-validated `goos`/`goarch` ⇒ `path_form_violation` (one rule, two reachable points) · (vi) the covering digest over the exact bytes ⇒ populate `ReleaseBinding{BoundAtRef: <ref verbatim>, ReleaseDigest}`. The refusal surface is TOTAL and typed (§B.5): no admission, no silent development fallback, anywhere.
- **T2 — the §B.7 acceptance table COMPLETE, driving the PRODUCTION flag/parser/population path (never a hand-built `ReleaseBinding`):** every enumerated fixture — both-absent byte-unchanged · both half-config directions · the positive (published-form path + valid ref ⇒ admission, `ReleaseDigest` = the freshly recomputed covering digest, `BoundAtRef` verbatim) · `/tmp/RELEASE-MANIFEST.json` ⇒ `path_form_violation` · the tuple-mismatch (`dist/linux-amd64/` serving a darwin/arm64 manifest) ⇒ `path_form_violation` · byte-perturbed ⇒ `non_canonical` · TOP-LEVEL and NESTED duplicate members ⇒ `non_canonical` · one fixture per §B.3b clause class (unknown · missing · wrong-type-reaching-stage-(iv) · fixed-literal · equality · member-set/order · bad digest) ⇒ `structural_violation` · the five `bound_at_ref_invalid` causes each ⇒ its precedence-selected cause · the OVERLAPPING 129-byte NUL+invalid-UTF-8 vector ⇒ exactly `over_length`. All negatives assert NO admission and NO development fallback.
- **T3 — the rebuild + re-derivation (after T1/T2 land green):** **the named collision, ROUTED:** the superseded pre-realization `dist/darwin-arm64/` target will NOT be byte-identical to the post-realization build, and the ruled pipeline's own law is exact-idempotent-or-STOP with no deletion/overwrite — so the rebuild REQUIRES a bounded disposal of the superseded target first. Proposed shape (the ruled residue pattern verbatim): the implementer moves EXACTLY `dist/darwin-arm64/` to a named recoverable Trash directory and reports the census; **the authorization is REQUESTED IN THE TOKEN REQUEST and granted (or reshaped) in master's dispatch — the pair never takes it alone**; the witness's identity survives in the banked record (`9052ec0f…` + the four member digests), not the disk. Then: `scripts/release-build.sh` UNCHANGED at the clean post-realization head (the `go1.26.4` fail-close green); re-derive and REPORT: the five-entry census · the four member digests · the covering digest via a fresh canonical round-trip · the final head ref — **as the NEW CANDIDATE binding bytes, claimed by NO ONE until the successor Master+VP act**; the pre-realization witness label carried verbatim in every report.
- **T4 — the close:** the regression floor (seam census exactly 64/0/64 · plain suite · vet) green at EVERY commit; the draft PR refreshed (V30); the WP2c-close SITREP with per-item proofs (the §B.7 table results · the both-absent byte-identity · the rebuild census/digests/covering digest/head) + the honest not-done (no binding, no promotion, no WP3); then my corroboration at my own invocation and the close routes UP.

## §4 — Acceptance criteria

1. T1/T2: the §B.7 table green IN FULL on the production path; the both-absent development population proven byte-unchanged; the refusal surface total (every §B.5 class exercised, zero admissions on negatives, zero fallbacks); zero manifest/scripts/seam bytes.
2. T3: the disposal executed ONLY under master's granted authorization, recoverable, census-reported; the pin fail-close green; the re-derived candidate bytes reported with NO binding/promotion claim and the pre-realization witness label carried.
3. The floor at every commit; findings UP never absorbed; a required manifest byte (if ever) = STOP + fence-amendment round, never silent.
4. The close SITREP + my corroboration; the successor binding packet and WP3 plan-2 are EXPLICITLY not this lane's (master's and the pair's next round respectively, in that order).

## §5 — Out of scope (carried whole)

Any binding/bound/binding-ready/promotion claim; the successor Master+VP packet and the VP co-sign; WP3 plan-2 (reissues ONLY against the completed final binding); any manifest-grammar, serve-gate, scripts, seam, worker, connector, broker, or other production byte; any edit to the banked WP2b evidence or the carrier instrument; the F65 instrument; merge (operator-only); any push beyond the slice branch + draft PR; H-12 relaxation.

## §6 — Sequencing (the V32 law)

Your PLAN-REVIEW verdict TO me, parented to this relay, over this plan's exact hash. On approve: I run the mechanical `SCOPE_DIFF` against §2 and route the TOKEN REQUEST UP — carrying the T3 disposal-authorization ask explicitly — and implementation begins ONLY on master's fresh direct dispatch TO `s16.implementer` with the literal token + the approved fence (+ the disposal grant or its reshaping). Spec-base defects found while realizing are escalations UP to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this plan drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (the opening + the r3 instrument + its approve re-read and re-hashed exact at my seat; the WP2b recipe pin, the pin ruling, and the worktree-clean-at-4e179fc3 check); no source, test, script, branch, commit, push, PR, merge, build, dist, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays2/INDEX.md
?? frank/.relays/s16/s16-wp2c/PLAN-planner-20260829-050800.md
?? frank/.relays/s16/s16-wp2c/PLAN-planner-20260829-050905.md
