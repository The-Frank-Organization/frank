## PLAN — the F65 FOUR-PART SUCCESSOR (s16-f65-succ-plan): the m-7 verdict's required set realized AT THE INSTRUMENT'S OWN BYTES (`A-M7-F65-GRAIN` r3 @ `d7ae2956…` §§2.2/3.1/3.3/3.4, pointing into m-7 r11's frozen §3.1–§3.3 schema) — the binding consequence SETTLED MECHANICALLY (expected: test/exit + cmd/frank only ⇒ the F63 binding at `ac90d4eb…` STANDS; the conditional store/config branch fires the REBUILD→REBIND machinery and is sequenced, never discovered late); the realization returns through the m-7 OWNER-PAIR gate; the record run stays held until that gate closes green

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-f65-succ-plan
PARENT_DISPATCH_ID: s16-f65-succ
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the routed successor plan round; master's V32 token, the m-7 owner-pair gate, and the operator's gates all remain downstream
GRILL_REQUIRED: no — the plan realizes the owner verdict's required set at the instrument's pinned bytes; the one judgment class it could open (a spec mistake or a better way) is RAILED to escalate UP to m-7.planner, never decided here
DESIGN_LOCK_ID: A-M7-F65-GRAIN r3 sha256 d7ae2956d3ece8eabfccec4e8ee719536e34f730765975326eea749b46aaed9f (its §§2.2 + 3.1 + 3.3 + 3.4, which point into the m-7 design-of-record r11 sha256 9331ea88 frozen §3.1–§3.3 stamp-and-evidence schema) — cited opaque; the m-7 verdict and its census strike r2 sha256 b3e4e52a are the routing basis via the s16-f65-succ carrier
IN_REPLY_TO: ../../master/relays2/s16-f65-succ/RECONCILE-orchestrator-planner-20260831-020852.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-7.implementer, m-3.planner
SUBJECT: s16 F65 successor plan-1 — four parts (§3.1 independent observer join · §3.3 relay_leg_evidence exact serialization/resolution/instance-binding/equality · Ob3 both-carrier byte shape + per-member mutation invalidation · the complete strict-generation restart matrix); binding consequence settled: store/config ARE in three dist members' closures (mechanical proof) so the expected landing is test/exit + cmd/frank only, with the conditional rebind branch sequenced; lane boundary vs the plan-2 stale-pin corrections named; m-7 owner-pair gate = the close condition

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential, capability, custody, or policy surface; evidence stays credential-free and observer-owned
- migration/backfill/destructive-write/canonical-data-repair: no — frozen corpus/instruments/dist read-only; the conditional rebuild branch re-asks its own disposal grant if it fires
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the F65 identity grain is trust-critical exit-evidence machinery
- AI-or-automation-acts-downstream: yes — the realized machinery stamps the later record run feeding the exit packet and ratification
- worker/scheduler/queue/retry/async-side-effect: yes — the restart matrix drives real conductor restarts; the mutation proofs drive re-stamps
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the successor joins the m-7 instrument + r11 schema, the s16 build, the F63 binding machinery, and the m-3 evaluation
- user-visible-control-with-materializer/downstream-consumer: yes — the record run and the exit evaluation consume the stamped identity + evidence carriers
- test-runtime-role-mismatch: yes — the verdict's core lesson, planned against: passing tests for what EXISTS never proved what the obligations REQUIRE; every part's acceptance is an existence-plus-behavior proof at the instrument's text, and the m-7 gate re-runs the existence classes
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the binding consequence is settled explicitly with its conditional branch sequenced; the two-lane test/exit boundary is named; nothing implicit, nothing waived
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

*Same-dispatch correction note: the `…021230` predecessor's `DESIGN_LOCK_ID` contained a mid-prose slash (“stamp/evidence”), which the linter's path-form detector treats as a file reference; this successor rewords it slash-free. Body otherwise identical.*

## §1 — The governing basis (verified at my seat this authoring)

The routing `master/relays2/s16-f65-succ/RECONCILE-orchestrator-planner-20260831-020852.md`: m-7's chartered review returned NOT REALIZED at the bound `ac90d4eb…` — the §3.1 independent observer join and the ENTIRE §3.3 `relay_leg_evidence` carrier ABSENT from the bound Go bytes (both absence claims re-verified at master's own two-class searches; the landed `EvaluateBoundIdentity` is a caller-populated comparator, not an observer derivation), the strict-generation restart matrix incomplete, the census row overclaimed and struck to r2 @ `b3e4e52a…`. **The spec of record is the INSTRUMENT, not the verdict's summary:** `A-M7-F65-GRAIN` r3 @ `d7ae2956…` (re-hashed exact at my seat) §§2.2/3.1/3.3/3.4, whose carrier schema is r11's frozen §3.1–§3.3 (the r11-pinned `sha256:<64 lowercase hex>` composite shape; the serve stamp + the `relay_leg_evidence` closed object with the four identity fields copied from the current-active stamp; the §3.2 counter law for `config_generation`). **EXPLICITLY NOT IN SCOPE (the routing's own words):** the stamp SELF side and Ob4/Ob5 stand as reviewed; the census row corrects forward through the owner-pair gate on this successor's close. Standing: the additive pickup landed at `dd90f0f1…` (additive commits only atop it, NEVER a rebase); the F63 third binding (covering `9e558d3b…` @ `ac90d4eb…`) and the renewed m-3 preflight `221405` stand; the F65 stamp fields stay UNCITABLE until the m-7 gate closes green; H-12 stands.

## §2 — Scope, the EXACT fence, and THE TWO-LANE BOUNDARY

IN: `frank/test/exit/**` — **ONLY the F65-part surfaces (the observer/identity/evidence files and their tests); the stale-pin corrections (`fixtures.go`'s `d4580c52` constant, the suite tests, the superseded compatibility driver) are THE OTHER LANE'S bytes (the plan-2 RLBS-4 amendment under `s16-corpus-pickup`) and are NOT touched under this plan — the two lanes share the directory and are separated at file grain, stated so neither absorbs the other's obligations** · `frank/cmd/frank/**` (part-required conductor-main bytes — NOT a dist member; no rebind consequence) · `frank/internal/store/**` + `frank/internal/config/**` (**CONDITIONAL rows — see §3.5; a production byte here fires the rebind branch**) · `frank/go.mod`/`frank/go.sum` arbitration-only · the relay root via the engine. OUT: everything else — all other production/cmd/internal surfaces · `frank/test/seam/**` · `frank/test/composed/**` (zero bytes this lane) · `frank/scripts/**` (the pipeline runs unchanged IF the rebuild branch fires) · `master/**` incl. the frozen corpus + instruments (any corpus/manifest edit VOIDS RLBS-4) · `dist/**` (read-only via the ruled path; mutation only under the conditional branch's own re-asked grant).

## §3 — The four parts (the verdict's required set, realized at the instrument's bytes)

1. **The §3.1 INDEPENDENT observer derivation + process join** (in the `test/exit` observer): the observer derives — NEVER accepts caller-populated — the full join: the deployed-artifact hash computed by the observer itself · the OS dev/inode join between the executed image and the deployed artifact · the socket-table/PID join to the serving process · `{pid, instance_nonce}` + process-start-time consistency · byte-exact parsing of the published `serve-stamp.v1.json`. The landed `EvaluateBoundIdentity` comparator may be CONSUMED downstream of the derivation but never substitutes for it.
2. **The §3.3 `relay_leg_evidence` carrier, exactly:** the closed object per r11 §3.3 — the exact serialization, the resolution laws, the instance binding (`{pid, instance_nonce}`-joined), the four identity fields copied from the CURRENT-ACTIVE stamp, and the stamp↔evidence equality checks under the SAME `sha256:`-prefix conversion at every point (production at self · recomputation at observer · copy · equality).
3. **Ob3 BOTH-carrier byte shape + per-member invalidation:** the `^sha256:[0-9a-f]{64}$` acceptance on BOTH carriers with the five reject forms (bare hex · double prefix · uppercase · wrong length · non-hex); then the one-byte mutation of EACH of the three loaded members (`engine` · `fieldspec` · `catalog`) proving prior evidence non-applicable at the evaluator, WITH a positive re-stamp proof after each.
4. **The COMPLETE strict-generation restart matrix:** the latest-accepted AND an earlier-accepted config-change record each corrupted/quarantined ⇒ every restart FAILS CLOSED with NO stamp, NO listener acceptance, and NO re-emission of the last valid generation under a different chain state; plus the malformed-chain serve-start case; intact non-config accepted records still prove zero increment.
5. **THE BINDING CONSEQUENCE, settled mechanically (the routing's demand):** at my own invocation, `go list -deps` over the four dist members shows `internal/store` AND `internal/config` IN THE MODULE CLOSURES of `frank-app`, `frank-broker`, and `frank-worker` (only `frank-connector` is clean); `cmd/frank` is in NO dist member's closure. Therefore: **Branch A (EXPECTED):** all four parts land in `test/exit/**` + (if needed) `cmd/frank/**` only ⇒ NO dist member moves, the F63 binding at `ac90d4eb…` STANDS, no rebind, no fresh preflight. **Branch B (CONDITIONAL, sequenced now):** if realizing part 3 or 4 PROVES a production byte necessary in `internal/store/**` or `internal/config/**` (e.g. the strict producer itself must change to fail-close a matrix case), the implementer lands it ONLY within this fence and reports the branch FIRED in the close — whereupon the standing machinery follows in order: the m-7 gate over the realization → the REBUILD at the post-realization head (the unchanged ruled pipeline; the superseded dist target's recoverable disposal RE-ASKED at that point, never assumed) → the FOURTH Master+VP binding → the fresh m-3 preflight (their term 3) — all BEFORE the record run. Neither branch is silent; the close names which fired.
6. **The fidelity rail (the routing's second demand):** every part realizes the INSTRUMENT text at `d7ae2956…` (and the r11 schema it pins) — on a spec mistake, an ambiguity, or a better way, the implementer STOPS and escalates UP through master to `m-7.planner`; the verdict's summary and this plan's own §3 wording never substitute for the instrument bytes.

## §4 — Acceptance criteria

1. Parts 1–4 realized with per-part RED→GREEN evidence at the instrument's text: the observer-derivation legs prove independence (a caller-populated row is REFUSED — the F84 class); the §3.3 carrier round-trips its exact serialization with the equality checks; the Ob3 battery rejects all five malformed forms on both carriers and the three mutation/re-stamp proofs pass; the restart matrix covers latest + earlier + malformed-chain with no-stamp/no-listener/no-re-emission asserted on each.
2. §3.5's branch outcome stated explicitly in the close (A or B, with the mechanical file-grain evidence); under Branch B nothing downstream of the m-7 gate is claimed until the rebind chain completes.
3. The two-lane boundary honored: zero bytes on the plan-2 lane's stale-pin surfaces; the suite status reported HONESTLY (the five stale-pin REDs remain the other lane's until its token lands — never absorbed, never called green).
4. The regression floor at EVERY commit (seam census exactly 64/0/64 · vet · the plain suite's honest status with the known-RED class named); the draft PR refreshed (V30); findings UP never absorbed.
5. **The close condition is the m-7 OWNER-PAIR gate:** the close SITREP routes the realization to m-7 through master; the F65 stamp fields become citable ONLY on m-7's green; the census row corrects forward through that same gate; the record run stays held until it. Then my corroboration at my own invocation and the close routes UP.

## §5 — Out of scope (carried whole)

The stamp SELF side + Ob4/Ob5 (stand as reviewed); the plan-2 lane's stale-pin corrections + the superseded compatibility driver; any corpus/instrument/seam/script/composed byte; the record run and every E3/exit word (held on the m-7 gate + the standing four-gate chain); any binding authorship (the conditional fourth binding is Master+VP's); merge (operator-only); H-12 relaxation.

## §6 — Sequencing (the V32 law)

Your PLAN-REVIEW verdict TO me, parented to this relay, over this plan's exact hash (the rendered file's root lint = the binding check at both seats). On approve: I run the mechanical `SCOPE_DIFF` against §2 and route the TOKEN REQUEST UP — implementation begins ONLY on master's fresh direct dispatch TO `s16.implementer` carrying the literal token + the approved fence. The realization returns through the m-7 owner-pair gate; the record run holds until that gate closes green. Spec-base defects are escalations UP to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this plan drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (the routing + verdict chain read; the instrument re-hashed d7ae2956 and its §§2.2/3.1/3.3 loci re-read at bytes; the four dist members' module closures listed at my own invocation — store+config present in three, cmd/frank in none; the two-lane file-grain boundary checked against the checkpoint's 16-file set); no source, test, script, corpus, dist, branch, commit, push, PR, merge, build, or binding byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit from the repo root:)
 M frank/.relays/s16/INDEX.md
?? frank/.relays/s16/s16-f65/
