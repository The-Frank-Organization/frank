## PLAN-REVIEW — F65 four-part successor: APPROVE the exact `127e6c1f…` plan; all omitted owner-verdict obligations are restored, the two-lane file fence is enforceable, and the production/rebind consequence is explicit

ROLE: Implementer
PHASE: PLAN-REVIEW
AUTHORITY: review-only
DISPATCH_ID: s16-f65-succ-plan-review
PARENT_DISPATCH_ID: s16-f65-succ-plan
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — this is the bounded pair PLAN review; master's fresh direct implementation token, the m-7 owner-pair realization gate, any fourth binding, the m-3 preflight, and operator gates remain downstream
GRILL_REQUIRED: no — the plan realizes the exact owner instrument and routes any spec mistake, ambiguity, or better way upward; it opens no product-design choice
PLAN_LOCK_ID: s16-f65-succ-plan @ sha256 127e6c1ff7e36ed28f6aea0d98d19a2a8e4e522d78d68b42e3dbc3364d8e9d0a
IN_REPLY_TO: s16-f65/PLAN-planner-20260831-021322.md
FROM: s16.implementer
TO: s16.planner
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-7.implementer, m-3.planner
SUBJECT: approve F65 successor plan 127e6c1f — the independent observer join, exact relay-leg carrier, both-carrier mutation battery, and complete strict-generation restart matrix match the frozen instrument; the expected non-dist branch and conditional rebuild/rebind branch are mechanically sound
VERDICT: approve

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — the plan changes no credential, capability, custody, tenant, or policy boundary; evidence remains credential-free and observer-owned
- migration/backfill/destructive-write/canonical-data-repair: no — frozen corpus, instruments, and current dist stay read-only; any conditional dist replacement requires its own later grant
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the F65 conductor/config identity is trust-critical exit-evidence machinery
- AI-or-automation-acts-downstream: yes — the realized carriers feed a later governed record run and exit evaluation, neither authorized here
- worker/scheduler/queue/retry/async-side-effect: yes — the acceptance battery exercises actual restart/listener behavior and instance joins
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the work joins the m-7 r3 instrument, frozen r11 carrier schema, F63 binding machinery, and m-3 evaluation
- user-visible-control-with-materializer/downstream-consumer: yes — the later exit record consumes the stamp and relay-leg carrier
- test-runtime-role-mismatch: yes — this review checks required mechanism existence and behavior, not merely the already-passing subset
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the plan explicitly branches on dist-member movement, protects the neighboring stale-pin lane, and requires upward escalation rather than local reinterpretation
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## Exact carrier, authority, and design fidelity

This verdict covers `s16-f65/PLAN-planner-20260831-021322.md` at exact SHA-256 `127e6c1ff7e36ed28f6aea0d98d19a2a8e4e522d78d68b42e3dbc3364d8e9d0a`. The carrier is PLAN/plan-only, FROM `s16.planner`, TO this seat, parented to master's `s16-f65-succ` route, and contains no implementation token. Exact-file lint is clean under the daemon-matching v2.9.1 linter. Its `021230` same-dispatch predecessor remains inherited lint-red for the path-shaped lock prose; this corrected target adds no such finding.

The plan binds the approved `A-M7-F65-GRAIN` r3 bytes at `d7ae2956d3ece8eabfccec4e8ee719536e34f730765975326eea749b46aaed9f` and consumes r11 §§3.1–3.3 rather than paraphrasing them into a new contract. The four planned parts preserve every substantive owner-verdict obligation:

1. the observer independently derives the serving PID, socket/process join, loaded-image-to-deployed-artifact dev/inode join and hash, `{pid, instance_nonce}`, process-start-time consistency, and byte-exact serve-stamp parse; caller-populated comparison data is insufficient;
2. `relay_leg_evidence` is the closed r11 object with exact JCS carriage, deterministic unique record ordering, the E1 floor, governed record resolution and exchange/kind checks, current-active instance binding, and equality of all four copied identity fields;
3. both carriers enforce the single `sha256:` lowercase form, reject all five malformed shapes, and prove old evidence non-applicable after each of the three loaded-member mutations with a fresh positive re-stamp; and
4. latest and earlier committed accepted-config corruption/quarantine plus malformed-chain startup all prove no stamp, no listener acceptance, and no last-generation re-emission, while intact non-config records remain non-advancing.

The plan also preserves the instrument's explicit exclusions: the stamp SELF side and Ob4/Ob5 stand; the census corrects forward only through the m-7 owner-pair gate; F65 fields remain uncitable before that green gate; no E3, record-run, exit, merge, deployment, release, or ratification claim is pulled forward.

## File fence, feasibility, and binding consequence

The two `test/exit` lanes are separated at file grain: this plan owns only F65 observer/identity/evidence surfaces and excludes the RLBS-4 stale-pin constant, suite tests, and compatibility driver owned by `s16-corpus-pickup`. The conditional production fence is narrow: `internal/store/**` and `internal/config/**` may move only if realization proves that necessary; every other production/internal surface, frozen corpus, instrument, seam, composed, script, and dist byte remains out.

The binding split reproduces independently at current additive head `dd90f0f162c561e6fdf317e0724ac86b4ebc4340`: `go list -deps` puts both `internal/store` and `internal/config` in `frank-app`, `frank-broker`, and `frank-worker`, but not `frank-connector`; `cmd/frank` appears in none of the four release-member closures. Therefore Branch A can add F65 `test/exit` and conductor-main bytes without moving the F63 dist set, while any store/config production move fires Branch B. Branch B is sequenced correctly: m-7 realization gate first, then post-realization rebuild under a re-asked disposal grant, fourth Master+VP binding, and fresh m-3 preflight before the record run. The close must name A or B from the actual diff.

Feasibility evidence is proportionate and honest. Fresh focused tests pass for `./internal/store`, `./internal/config`, and `./cmd/frank`. Fresh `go test ./test/exit -count=1` is RED on exactly five top-level stale-pin tests: four old manifest-pin failures and the pre-existing untracked compatibility driver's old substrate pin. That is the neighboring plan-2 lane's known baseline, not a blocker to this plan and not green evidence for it. The successor requires per-part RED-to-GREEN proof plus the 64/0/64 seam census, vet, and the plain suite's literal status at every commit.

## Verdict and next gate

APPROVE the exact `127e6c1f…` plan. Its scope is bounded, its acceptance criteria are observable, its testing maps to the frozen owner obligations, and its conditional rebind consequence cannot be silently bypassed. The fidelity rail is binding: any spec mistake, ambiguity, or better way stops implementation and routes upward through master to `m-7.planner`.

This approval authorizes only the Planner's mechanical scope diff and token request upward. Implementation begins only on master's fresh direct dispatch TO `s16.implementer` carrying the literal token and approved fence. No source, test, script, corpus, instrument, dist, build, process, provider, branch, commit, push, PR, binding, E3, exit, merge, deployment, publication, release, external-use, or ratification action is authorized here.

ACTIONS_GIT_REF: read-only PLAN-REVIEW — exact-hashed and exact-file-linted the addressed plan; re-read the exact r3 and r11 §§3.1–3.3 carrier bytes; mechanically recomputed all four dist-member dependency closures; verified additive head and current worktree status; ran fresh focused production tests and the honest exit-package baseline; no product, test, script, corpus, instrument, dist, build, process, branch, commit, push, PR, binding, E3, exit, merge, deployment, publication, release, external-use, or ratification act
FINAL_GIT_STATUS_SHORT: (literal, captured immediately before drafting from the implementation worktree at `dd90f0f1…`; the untracked file is pre-existing work owned by the separate stale-pin lane:)
?? frank/test/exit/frozen_journal_compatibility_test.go
