## IMPL — THE F65 FOUR-PART SUCCESSOR DISPATCH (fresh direct authority, the V32 chain's last link): every condition verified at this seat — **s16.implementer is dispatched to execute the four parts of the successor plan, plan of record `s16-f65-succ-plan` @ exact SHA-256 `127e6c1ff7e36ed28f6aea0d98d19a2a8e4e522d78d68b42e3dbc3364d8e9d0a`, under the approved §2 fence with the TWO-LANE FILE-GRAIN BOUNDARY.** This token authorizes the four parts, nothing more; the realization returns through the m-7 OWNER-PAIR gate; any Branch-B rebuild/rebind needs its OWN re-asked grant; merge stays OPERATOR-ONLY.

ROLE: Orchestrator Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16-f65-impl
PARENT_DISPATCH_ID: s16-f65-token
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the direct-dispatch conditions are met and recorded (pair approve over the exact hash + the all-in SCOPE_DIFF re-verified at master + the active-root lint of the plan carrier OK at this seat); the operator is CC'd on this arming act; his gates remain the WP5 MERGE-GATE and the Step-3 ratification
PLAN_LOCK_ID: s16-f65-succ-plan @ sha256 127e6c1ff7e36ed28f6aea0d98d19a2a8e4e522d78d68b42e3dbc3364d8e9d0a
BUNDLE_ID: s16-integration
OWNER: s16
REPO: frank/ (workspace subtree)
BASE: branch s16-integration at the additive-pickup head dd90f0f162c561e6fdf317e0724ac86b4ebc4340 (worktree verified there this issue, sole untracked byte = the other lane's pre-existing driver); if the head has advanced by the `s16-corpus-pickup` lane's OWN file-grain-disjoint commits, proceed from the advanced head and NAME it in the close — ANY other movement = STOP
BRANCH: s16-integration (worktree ~/Programming/harness-s16-integration; draft PR #2 refreshed per V30; additive commits ONLY, never a rebase — the F63 binding at ac90d4eb protected)
TARGET_BRANCH: main (operator-only MERGE-GATE, unchanged)
IN_REPLY_TO: s16-f65/SITREP-planner-20260831-024917.md
FROM: master.orchestrator-planner
TO: s16.implementer
CC: s16.planner, master.orchestrator-reviewer, operator, m-7.planner, m-7.implementer, m-3.planner
SUBJECT: F65 DIRECT DISPATCH — execute the four-part successor at 127e6c1f: (1) the §3.1 independent observer derivation + process join, (2) the §3.3 relay_leg_evidence carrier at the frozen r11 schema laws, (3) the both-carrier byte-shape + per-member prior-evidence invalidation, (4) the complete strict-generation restart matrix; two-lane file-grain fence live; Branch A/B named at the close from the actual diff; the m-7 owner-pair gate is the return path

## §1 — The authority chain (verified at this seat before issue)

1. **The plan of record:** `s16-f65/PLAN-planner-20260831-021322.md` @ exact `127e6c1ff7e36ed28f6aea0d98d19a2a8e4e522d78d68b42e3dbc3364d8e9d0a` (recomputed here; ACTIVE-ROOT lint OK at this seat — the root's residual reds are the classified historical baseline plus the superseded `021230` predecessor), pair-APPROVED by `s16-f65/PLAN-REVIEW-implementer-20260831-022522.md` (`s16-f65-succ-plan-review`, VERDICT approve, PLAN_LOCK_ID at the same exact hash; the reviewer independently recomputed all four dist-member dependency closures, reproducing the binding split, and re-read the r3 instrument + the r11 §§3.1–3.3 frozen carrier bytes).
2. **The SCOPE_DIFF:** the planner's `024917` §2 restatement checked at this seat against the plan's §2 — ALL-IN: `frank/test/exit/**` at FILE GRAIN (F65 observer/identity/evidence surfaces ONLY; the stale-pin constant, suite tests, and compatibility driver are the `s16-corpus-pickup` lane's bytes — ZERO of them under this token) · `frank/cmd/frank/**` (non-dist-member) · `frank/internal/store/**` + `frank/internal/config/**` CONDITIONAL (a production byte fires Branch B) · `go.mod`/`go.sum` arbitration-only · the relay root via the engine; OUT mirrored exactly (all other production/cmd/internal surfaces · `test/seam/**` · `test/composed/**` · `scripts/**` · `master/**` incl. the frozen corpus + instruments — any corpus/manifest edit VOIDS RLBS-4 · `dist/**` read-only).
3. **Both scans trigger-present ⇒ V32:** no pair token exists on any branch of this work; this fresh direct dispatch TO the implementer alone is the only implementation authority.

## §2 — The grant and its rails (from the plan's own bytes and the m-7 verdict)

- **The four parts realize the INSTRUMENT'S text** (`A-M7-F65-GRAIN` r3 @ `d7ae2956…` §§2.2/3.1/3.3/3.4, pointing into the m-7 r11 frozen §3.1–§3.3 schema): (1) the independent observer derivation + process join — derived AT the observer (socket-table/PID, dev/inode, independent artifact hash, start-time consistency, byte-exact stamp parsing), never caller-populated; (2) the exact `relay_leg_evidence` serialization/resolution/instance-binding/equality laws as the reviewer spelled them from the frozen r11 bytes; (3) `^sha256:[0-9a-f]{64}$` in BOTH carriers + the five reject forms + the one-byte mutation of EACH of the three members with a positive re-stamp proof after each; (4) the complete latest/earlier restart matrix (no stamp, no listener acceptance, no last-generation re-emission, the malformed-chain serve-start case).
- **The fidelity rail:** a spec question, mismatch, or better-way is a STOP + escalation UP through master to `m-7.planner` — never a local re-interpretation; the m-7 gate will re-run the EXISTENCE search classes, so per-part proofs are existence-plus-behavior at the instrument text, per-part RED→GREEN.
- **The binding consequence:** the close MUST name Branch A or B from the ACTUAL diff. Branch A (test/exit + cmd/frank only) ⇒ the F63 binding at `ac90d4eb…` STANDS, no rebind. Branch B (any store/config production byte) ⇒ STOP after naming it — the rebuild runs only under its own re-asked grant, then the fourth binding (Master+VP) and the fresh m-3 preflight sequence; none of that is licensed by this token.
- **The standing rails:** the regression floor green at EVERY commit (the five stale-pin `test/exit` REDs are the NEIGHBORING lane's known baseline — state them, never absorb or fix them under this fence); additive commits only; findings UP never absorbed; the draft PR refreshed (V30); zero seam bytes.
- **At completion:** the close SITREP with the per-part proofs + the branch naming TO `s16.planner`, CC this roster; the planner corroborates; the return routes UP to the m-7 OWNER-PAIR gate (their review + adversarial pass); the F65 stamp fields become citable and the record run's gate closes ONLY on m-7's green. **No merge claim by any s16 relay, ever.**

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this dispatch drafted at .engine/drafts/master.orchestrator-planner/ on the s16 root and submitted through the v2.9.1 client; pre-issue verification was read/hash/lint-only (the plan + approve + request read; the plan hash recomputed exact; the active-root lint run with the residual reds classified; the SCOPE_DIFF re-checked row-for-row; the worktree head verified at dd90f0f1); no source, test, script, branch, commit, push, PR, build, or merge byte by this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit from the governance root:)
 M frank/.relays/s16/INDEX.md
 M master/relays2/INDEX.md
?? frank/.relays/s16/s16-f65/
(the s16-f65 relays are the pair's lane traffic, banked at master's checkpoint; the daemon owns the INDEX files)
