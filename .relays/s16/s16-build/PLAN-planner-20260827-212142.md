## PLAN — WP1 AMENDED REISSUE (s16-build-plan-4): the bounded T7 trust-root fold — the ONLY substantive delta vs the approved plan-3 @ `2956b9c5…` is the addendum-r5 instrument set (marked ★); everything else carries byte-whole in substance; the token remains master's V32 UPDATED direct dispatch

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-build-plan-4
PARENT_DISPATCH_ID: s16-build-plan-review-3
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E3
HUMAN_GATE_REQUIRED: no — the operator's next gates are the WP5 MERGE-GATE and the Step-3 ratification; the V32 updated dispatch is master's act after this plan's re-review
GRILL_REQUIRED: no — the delta realizes the pair-approved m-8 addendum r5 + its joined confirmations; no new design decision is opened at this seat
IN_REPLY_TO: s16-t7/RECONCILE-orchestrator-planner-20260827-210714.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner, m-2.planner, m-3.planner
SUBJECT: WP1 plan-4 — the T7 trust-root fold per master's carriage: the §2.4 fence gains EXACTLY the addendum's §5 loci (four exact connector surfaces + the two m-10 grammar touches; endpoint.go asserted untouched); §1/§4 gain the r5 instrument set by exact hash/carrier; T6/T7/T8 gain the realization obligations incl. all SEVEN §7 fixtures; everything else carries from approved plan-3; on your approve + my all-in SCOPE_DIFF over the amended fence, the token request routes UP for master's V32 updated dispatch

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — F59 authorization, the connection-scoped broker capability, the broker-held m-9 seat credential, provider-credential non-exposure; the trust-root artifact is NON-secret by contract (public CA bytes only; m-1 boundary untouched, addendum §0.4) but is trust-posture-bearing (evidence = charter + the m-7/m-8 custody laws + addendum §0/§2)
- migration/backfill/destructive-write/canonical-data-repair: no — no migration, repair, or destructive data operation is planned (evidence = §5 task list)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the conductor store, authority tickets, epoch fence, channel-stamped relay record, and now the freeze-bound trust comparand are trust-critical state (evidence = §3/§4/§6)
- AI-or-automation-acts-downstream: yes — a model-originated relay.submit causes a governed durable store append (evidence = §3)
- worker/scheduler/queue/retry/async-side-effect: yes — the composition supervises worker/connector/broker processes with attach retry and async IPC (evidence = §5 T2–T6)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — CTRL-W, DATA-P, broker-w, the provider request surfaces, and the trust_root_digest manifest/assign members are shared process contracts (evidence = §4/§5)
- user-visible-control-with-materializer/downstream-consumer: yes — operator `run start` (now optionally naming a trust root exactly as `credential_ref`) drives the composition; WP2–WP5 consume its outputs (evidence = §6)
- test-runtime-role-mismatch: no — the harness asserts the loopback call drawn from the lowered form of the actually presented eight-tool surface, over an AUTHENTICATED handshake per the addendum (evidence = §3/§4)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — H-12's accepted no-sandbox residual stands (and is STRUCTURALLY REINFORCED by the addendum's REPLACE semantics, fixture-4-bound); the CT-G03 act remains a protected-byte change under exact void rails (evidence = §4)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — Locked design (pointers, never paraphrase; on conflict the pinned bytes govern)

- The plan of record r7 @ `044fe4cc…` (§1(i), §2 WP1); the charter AS CORRIGENDUM'D @ `d6dd1949…` (the three pin corrections law: m-10 = live `b4d948d1…` AS AMENDED BY `b0450777…`; the m-7 three-instrument stack r11 `9331ea88…` + A-M7-LAUNCH-READY r2 `0063650d…` + study rev8 `64f9136e…`, study-governs read order; m-3 = preimage `009df607…` vs disk `7fce817e…`).
- Owner designs per BASIS_HASHES: m-9 worker r12 @ `63f5c49d…` + the m-9 delta @ `61833f63…` §6 · m-8 r12 @ `4b670a79…` + A-M8-BE @ `734e44b7…` (both byte-unmoved by the addendum) · m-2 mapping @ `83d8e63e…` + component contract @ `c3a8cd61…` · m-1 @ `7c8b09a6…` · interface lock RE-CERT 3 @ `6a768023…` (RE-CERTIFICATION 3 COMPLETE at worker r12) · RLBS-3 @ `289a0da0…` · the m-10 registrations (C09 `1d750580…` · A14 `67acb760…` · D01 `30b9d2bf…`).
- The two Q2 instruments (design-of-record from pair approval, folds queued): `A-M7-WORKER-WIRE` r4 @ `c75c469a…` (approve `s16-build-q12/DESIGN-REVIEW-implementer-20260827-050324`) · the `broker-w` registration @ `a1bddf1b…` (approve `…053826`).
- ★ **The T7 trust-root instrument set (the amendment's basis; every hash/carrier verified at this seat):**
  - the m-8 addendum `master/domains/m-8-provider-adapters/design/2026-08-27-trust-root-artifact-addendum.md` **r5 @ SHA-256 `cb154641a53600354f9a47b9305590503d1a59b951a113feea075c047e8ac5d0`** (re-hashed exact at this seat this authoring) — a governed additive delta over frozen r12, pair-APPROVED by `s16-t7-tls-inst/DESIGN-REVIEW-implementer-20260827-180445.md` over that exact hash;
  - the corrected endpoint owner term (master ack `s16-t7-tls-ack/RECONCILE-orchestrator-planner-20260827-165443.md`): `https://localhost:<nondefault-port>/<path>`, leaf SAN=`localhost`, non-443 port, path required — SHAPE, not a literal; the five other owner terms of the `s16-t7-tls/SITREP-planner-20260827-152357.md` ruling stand verbatim;
  - m-10's confirmation `master/relays2/s16-t7-tls-confirm/SITREP-planner-20260827-204742.md` — all six shape requirements SATISFIED at r5; **vehicle (a): that relay IS the citable m-10 ownership record** for the manifest `trust_root_digest` schema member + the registered `ConnectorAssignBody` member, realized by this build under this amended fence;
  - m-3's confirmation `master/relays2/s16-t7-tls-confirm/SITREP-planner-20260827-204725.md` — P1–P4 discharged as text at r5 (P1/P2/P4 with their own §7 fixtures); the declared injected-CA posture is the WP3-admissible shape; `trust_root_digest` joins the bound-identity set; the FORMAL E3-admissibility ruling remains a WP3 act;
  - master's carriage `s16-t7/RECONCILE-orchestrator-planner-20260827-210714.md` (the completed-authority declaration + this amendment's instruction).
- The joined Q1/Q2 answer set (normative, §4): master's carriage `s16-build/RECONCILE-orchestrator-planner-20260827-124550.md` · m-9 `s16-build-q12/SITREP-planner-20260827-032802.md` · m-2 `…DESIGN-planner-20260827-033927.md` · m-8 `…SITREP-planner-20260827-032915.md` · m-10 `…DESIGN-planner-20260827-032843.md` · m-9's Q2 confirm `…SITREP-planner-20260827-052148.md`.
- The CT-G03 authority: master `s16-ct-g03/RECONCILE-orchestrator-planner-20260827-130955.md` + the VP co-sign `…RECONCILE-orchestrator-reviewer-20260827-131637.md` — identity `SEAM-BATTERY-S16-G03-1`, terms bound at §4.
- The five PM ready-to-guide carriers of record: m-9 `…203121.md` · m-3 `…203227.md` · m-10 `…203233.md` · m-7 `…023135.md` · m-8 `…023313.md`.
- The regression floor: s16a close r10 @ `7bb643c8…` — seam battery 64/0/64 (script-derived census) + plain suite + `go vet`, green at every commit.
- The live build state this amendment joins: branch `s16-integration` cut at exact `77f8c9db…` (T1 report `s16-impl/SITREP-implementer-20260827-134558.md`); composition commit `283676d3` pushed gate-green under the existing `s16-impl` dispatch.

## §2 — Scope (exact; everything else is out)

1. **Branch + worktree + DRAFT PR:** as approved at plan-3 — the cut is DONE at exact `77f8c9db…` (verified); the DRAFT PR per V30; worktree `~/Programming/harness-s16-integration`.
2. **The composition (obligation i):** the six loci production-wired (§5), the Q1 assembly completion, the Q2 broker-w realization, ★ and the addendum-r5 trust-root realization (the T7 unblocking work).
3. **One honest governed turn, demonstrable** per §3 — eight tools presented, the loopback's call from the lowered set, ★ over an AUTHENTICATED loopback TLS session per the addendum.
4. **In-fence file surface (the EXACT AMENDED write fence — the SCOPE_DIFF comparand):** `frank/cmd/frank-worker/**` · `frank/cmd/frank-app/**` · `frank/cmd/frank-broker/**` · `frank/internal/appctl/**` · `frank/internal/broker/**` · `frank/internal/appipc/**` · `frank/internal/worker/**` · `frank/test/composed/**` · `frank/test/seam/agree_test.go` STRICTLY per `SEAM-BATTERY-S16-G03-1` · ★ **the addendum §5 loci EXACTLY, file-exact, never `internal/connector/**` wholesale:** `frank/cmd/frank-connector/main.go` (locus 1: the `--trust-root`-class argv flag → `Artifacts.TrustRootPath`, parsed BEFORE `os.Clearenv()`) · `frank/internal/connector/control/**` (locus 2: the optional `TrustRootPath` field + the §2 XOR/verify/fail-closed `connector_ready` withhold) · `frank/internal/connector/service/service.go` (locus 3: thread the verified pool via a trust-aware constructor variant) · `frank/internal/connector/transport/transport.go` (locus 4: `RootCAs: pool` when present, nil/system-roots otherwise) — **`frank/internal/connector/endpoint/**` is an ASSERTED-UNTOUCHED locus (locus 5): zero bytes; every OTHER `internal/connector` surface stays OUT** (locus 6, the manifest/`ConnectorAssignBody` grammar touches, rides the existing `frank/internal/appctl/**`/`frank/internal/appipc/**` rows under m-10's vehicle-(a) ownership record) · `frank/go.mod`/`frank/go.sum` arbitration-only · the relay root via the engine. `frank/internal/seatclient/**` explicitly OUT.

## §3 — The composed-turn definition (plan-3 §3 carried whole; ★ one addition)

Unchanged from the approved plan-3: real processes on the composed path; the presented/lowered eight-tool surface with the loopback's `relay.submit` drawn from the LOWERED set; the run-local live conductor with the broker-held seat credential; loopback-only provider egress (H-12); lane identity a run seed; the release identity an honest labeled placeholder until WP2.

★ **The authenticated loopback (the addendum realized):** the demo endpoint is the corrected owner-term shape `https://localhost:<nondefault-port>/<path>` with a leaf certificate SAN=`localhost` chaining to a demo-minted test CA; the REAL connector trusts it ONLY via the verified `TrustRootPath` artifact + freeze-bound `trust_root_digest` comparand (operator-named at `run start`, exactly like `credential_ref`); the verified pool REPLACES system roots, making H-12 a STRUCTURAL TLS property of the composed run (the connector physically cannot complete a trusted session to a real provider — bound by fixture 4's RED leg, never asserted in prose). `endpoint.go` and `os.Clearenv()` stay byte-untouched; the unconfigured production path stays byte-identical (nil `RootCAs`, wire-identical `connector_assign`).

## §4 — Composition laws (plan-3 §4 carried whole; ★ the addendum's terms added)

- **Fences:** FROZEN MEMBERS (the exit corpus `d4580c52…` + every digest-bound artifact): STOP → Master+VP on any touch. EVIDENCE INSTRUMENTS (`frank/test/seam/**`): outside the fence except the single `SEAM-BATTERY-S16-G03-1` act by its exact terms (the protected change, five landing conditions, void rails — bound verbatim from the co-sign as at plan-3 §4). `master/**` master-only. ★ The connector fence is LOCUS-EXACT per §2.4 — a byte in any other connector file (endpoint.go above all) is a fence deviation, STOP → UP.
- ★ **The addendum's terms (r5 @ `cb154641…` governs; where this plan's words diverge, the instrument does):** additive + opt-in (every clause inert unless a trust root is named) · fail-closed-to-production (absence ⇒ byte-for-byte today's system-roots path; no new required production field) · transport-layer only (the trust root enters NO `frozen_core`/digest/authorized-envelope/provider-wire/F12 state; `trust_root_digest` is the SOLE standalone comparand over the PEM bytes) · non-secret (public CA certificate bytes ONLY; any private-key block ⇒ fail-closed; m-1's boundary untouched) · hermetic (argv-before-`Clearenv` artifact path, NEVER env, NEVER host trust-store mutation; the PATH never enters the manifest, `connector_assign`, or any conductor record — only the digest is carried) · the §2 freeze-binding (manifest member present-IFF-configured, exact `[0-9a-f]{64}`, frozen at admission; `connector_assign` verbatim byte-copy ABSENT-never-empty; absence-is-STATED = system posture by rule; the XOR half-config ⇒ `connector_ready` withheld) · the READY-gated verification chain (ownership/mode → hash equality → parse → CA-qualified: `BasicConstraintsValid && IsCA` + `KeyUsageCertSign`-when-present → any failure ⇒ a TRUST-COMPARAND-SPECIFIC typed fault, never a generic withhold, + zero send) · the §3 REPLACE-not-augment injection (only `RootCAs` moves, only on the verified path) · the observer re-derivation rail (the WP3 observer re-derives the digest FRESH from the readable, byte-stable PEM — F84).
- **m-9's Q1 ruling + four guidance notes; m-2's canonical-fixed-point alias ruling + three fixture legs; m-8's two precisions (lowered-set carrier; pure-lowering/E-digest law, not K6); m-10's consumer-complete confirmation; the wire contract `c75c469a…` §§2–4 + registration `a1bddf1b…`; m-9's two Q2 consumption precisions; the standing readiness law incl. m-3's five E3-admissibility duties** — ALL carried verbatim from the approved plan-3 §4 (their text governs unchanged; not restated here to keep the delta bounded — the plan-3 bytes @ `2956b9c5…` are this plan's §4 substrate by reference, with the instrument-precedence rule intact).

## §5 — WP1 work breakdown (plan-3 §5 carried whole: T1 done · T2 LaunchWorker · T3 worker main · T3a delta-§6 assembly · T4 CTRL-W half · T5 broker-w + SEAM-BATTERY-S16-G03-1 act · T6 frank-app completion · T7 composed harness · T8 loci proof — each exactly as approved; ★ the T7-unblocking additions below)

- ★ **T9 — the trust-root realization (the addendum §5 loci EXACTLY):** locus 1 `cmd/frank-connector/main.go` (the argv flag, parsed before `Clearenv`); locus 2 `internal/connector/control/**` (the optional `Artifacts.TrustRootPath`; the XOR-consistency + verify + CA-qualification enforcement + trust-specific fail-closed withhold keyed by the `connector_assign` `trust_root_digest`); locus 3 `internal/connector/service/service.go` (the trust-aware constructor variant threading the pool); locus 4 `internal/connector/transport/transport.go` (`RootCAs: pool` present-else-nil); locus 5 `endpoint.go` ZERO BYTES (asserted; prove by diff absence at close); locus 6 the manifest `trust_root_digest` schema member + the registered `ConnectorAssignBody` member (m-10's surface under the vehicle-(a) ownership record `…204742`; realized on the existing appctl/appipc fence rows).
- ★ **T6 addition:** `run start` carries the operator-named trust root into the freeze (the manifest member) and the connector launch argv — named exactly as `credential_ref` is named; the path never enters a carrier.
- ★ **T7 additions:** the harness mints the test CA (`BasicConstraintsValid && IsCA`) + the `localhost`-SAN leaf; serves the corrected owner-term endpoint shape; provides the CA PEM as `TrustRootPath` + its digest as the manifest comparand; **and realizes ALL SEVEN addendum §7 fixtures in the composed battery as BUILD WORK:** (1) the T7 positive (authenticated full-wire loopback turn, hermetic green); (2) the production-default byte-identity pin; (3) the fail-closed negative set (mismatch · unparseable/empty/zero-cert · `!IsCA` leaf · IsCA-but-no-`KeyUsageCertSign` · private-key block · XOR half-config · named-but-absent); (4) the H-12 structural RED leg (a dial to any non-demo-CA endpoint FAILS TLS verification — observable RED); (5) absence-decidable + trust-cause-attributable; (6) the observer re-derivation catch (a fabricated manifest copy over honest bytes is CAUGHT); (7) the mixed-bundle WHOLE-reject (no partial pool).
- ★ **T8 addition:** the §5 loci join the loci-caller proof discipline — the trust-aware constructor path gains a production caller proven by the same mechanical audit (RED before the wiring, GREEN after).

## §6 — Boundary contract (plan-3 §6 carried whole; ★ additions)

Writes = §2.4's AMENDED fence. ★ Contract additions: the addendum r5 @ `cb154641…` by hash; the m-10 ownership record + m-3 precondition discharge by carrier. ★ Proof additions — E1/E2: the seven §7 fixtures green (the negatives explicitly enumerated in the close SITREP), the locus-5 zero-byte proof, the fixture-2 default-path pin; E3: the T7 composed-runtime proof now runs over the AUTHENTICATED loopback with the trust comparand on the frozen manifest and the identity set printed including `trust_root_digest`. The E3 remains the composed-runtime proof only; WP3's externally-observed E3 + m-3's formal admissibility ruling stay separate and unclaimed.

## §7 — Acceptance criteria (plan-3 §7 carried whole; ★ additions)

★ 10. The trust-root realization complete per the addendum: all SEVEN §7 fixtures green; `endpoint.go` proven byte-untouched; the production default proven byte-identical (fixture 2); the trust-specific fault causes attributable (fixture 5); the H-12 RED leg observable (fixture 4).
★ 11. The WP1-close SITREP additionally records: the addendum hash, the fixture evidence, the locus-exact diff footprint over the four connector files (proving no other connector byte), and `trust_root_digest`'s presence in the printed identity set.

## §8 — Settled readings (nothing open at this seat)

All plan-3 settled readings stand (SessionLogPath rides T6; the CT-G03 authority + `SEAM-BATTERY-S16-G03-1` terms; direct upstream dispatch only). ★ New settled: the T7 trust gap is closed by the completed owner authority — the amendment's §5-loci fence extension is exactly master's carriage instruction; no reading remains open.

## §9 — Out of scope (plan-3 §9 carried whole; ★ additions)

★ Every `frank/internal/connector/**` byte beyond the four named loci; `frank/internal/connector/endpoint/**` absolutely (the asserted-untouched locus); any trust-root use beyond the composed proof (no production trust-root ships configured); any env-var or host-trust-store mechanism; any private-key material in the artifact; any byte movement in the addendum r5 or its confirmations (the void rail — a needed change returns to m-8 through master).

## §10 — Sequencing after your review (the V32 route)

Your PLAN-REVIEW verdict TO me, parented to this relay, over this plan's exact hash. On approve: I run the mechanical `SCOPE_DIFF` against §2.4's AMENDED fence; on all-in, I route the token request UP — **implementation authority for the amended fence begins ONLY on master's V32 UPDATED direct dispatch TO `s16.implementer`** (per the carriage `210714`). Until that dispatch: NO connector byte moves; T7 and the WP1 close stay HELD; every other WP1 surface continues under the EXISTING `s16-impl` dispatch, whose fence is unchanged. Spec-base defects found while composing remain escalations UP through master to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this amended reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read/hash-only (the addendum r5 re-hashed exact `cb154641…`; the two confirmations + the carriage + the endpoint-term ack read in full); no source, test, branch, commit, push, PR, merge, or code byte from this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M master/relays/CHECKPOINTS.md
 M master/relays2/INDEX.md
?? master/relays2/s16-t7-tls-confirm/SITREP-planner-20260827-210815.md
