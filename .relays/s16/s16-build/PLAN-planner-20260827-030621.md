## PLAN — WP1: the composed app on the live courier (obligation i — the six no-production-caller loci made real; one honest governed turn end-to-end). Authored under the joined five-PM readiness (`s16-readiness/RECONCILE-orchestrator-planner-20260826-204449.md`) and filed on the rejoined five-of-five clean carrier chain (`s16-readiness/RECONCILE-orchestrator-planner-20260827-030435.md` — element 2 COMPLETE; the m-7/m-8 successors re-linted clean at this seat this act); the FIRST CODE TOKEN issues ONLY on s16.implementer's PLAN-REVIEW approve of this plan, per the commission grant.

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-build
PARENT_DISPATCH_ID: t4-s16-commission
RUN_ID: s16
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the operator's next gates are the WP5 MERGE-GATE and the Step-3 ratification; nothing is asked of the operator here
GRILL_REQUIRED: no — the design is master's ratified plan r7 + the corrigendum'd charter + the pinned owner designs; no new design decision is opened at this seat (the two named open items in §8 route to your review and, if live, UP — never to a local choice)
IN_REPLY_TO: s16-readiness/RECONCILE-orchestrator-planner-20260827-030435.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner, m-3.planner
SUBJECT: WP1 plan — branch from then-current main under the LAUNCH_BASE identity check; the six loci production-wired (LaunchWorker over NewSocketPair/SanitizeEnv, real frank-worker main, app CTRL-W half over f59.New, broker worker endpoint over NewCore/Invoke to the live conductor, wire.NewCodec transports, resume DecodeSettlementManifest); one honest governed turn demonstrated by a real-process E2 harness in a NEW test package; the PM guidance folded as §4 normative law; regression floor at every commit

## §1 — Locked design (pointers, never paraphrase; on conflict the pinned bytes govern)

- The plan of record: `master/STEP-3-S16-INTEGRATION-PLAN.md` r7 @ `044fe4ccefa3b55e16c897afecfe1f6bca9971a2a7c905aac804bd53713c726d` — §1(i) and §2 WP1 are this WP's verbatim authority; §1's acceptance law and §4's fence classes bind throughout.
- The charter AS CORRIGENDUM'D: `master/subteams/s16-integration/CHARTER.md` @ `d6dd1949491e2974ca6001017ef2c9e413f9fb381b49026a2b7a0f148f208642` (CORRIGENDUM 1, re-hashed at this seat this authoring). The three pin corrections are law: (a) the m-10 column = `A-M10-SEAM` LIVE @ `b4d948d1…` AS AMENDED BY `b0450777…` (`d2ce9831…` is the citable r40 history identity with NO live file — never recompute-hunt it); (b) the m-7 column is THREE instruments — r11 @ `9331ea88…` + `A-M7-LAUNCH-READY` r2 @ `0063650d…` + the study rev8 @ `64f9136e…`, and WHERE THE STUDY NAMES AN r11 CLAUSE THE STUDY GOVERNS (read order is law); (c) the m-3 pin `009df607…` is the ratified PREIMAGE — the live doc's disk bytes are `7fce817e…` under five licensed SUPERSEDED-BY markers; compare the marker-removal reconstruction, never the disk hash, and consume a marked span's successor.
- The remaining owner-design pins per the charter's BASIS_HASHES: m-9 worker r12 @ `63f5c49d…` (NEWEST bytes incl. §7.1a) · m-8 r12 @ `4b670a79…` + `A-M8-BE` @ `734e44b7…` (read TOGETHER) · m-2 @ `83d8e63e…` · m-1 @ `7c8b09a6…` · the interface lock RE-CERT 3 @ `6a768023…` · RLBS-3 @ `289a0da0…` · the m-10 registrations (C09 `1d750580…` · A14 `67acb760…` · D01 `30b9d2bf…`).
- The five PM ready-to-guide returns (READ IN FULL before the surfaces they flag; folded as §4 law): m-9 `master/relays2/s16-pm-readiness/SITREP-planner-20260826-203121.md` · m-7 `…203128.md` · m-8 `…203226.md` · m-3 `…203227.md` · m-10 `…203233.md`.
- The regression floor: the s16a close r10 @ `7bb643c8…` — the seam battery 64/0/64 by `go test -tags seam -json -count=1 ./test/seam/ -run 'TestCT_' | test/seam/census.py`, the plain suite `go test -p=1 -count=1 ./...`, and `go vet ./...`, green at every commit.

## §2 — Scope (exact; everything else is out)

1. **Branch + worktree + DRAFT PR:** cut branch `s16-integration` from then-current workspace main; the LAUNCH_BASE identity check AS WRITTEN in the charter — main ancestry ≥ `f007a9d65a449c01bb72df069df3286f992c68e4` AND the five implementation-subtree ids EXACT (`frank/cmd d6f565e7…` · `frank/internal 89b2b7db…` · `frank/test 09dd6a0e…` · `frank/go.mod 92c6e7b3…` · `frank/go.sum 887b5096…`) — verified at the cut and the EXACT CUT SHA stated in your branch-cut/first IMPL SITREP (at this authoring main is `342f3b9c…` and satisfies both; verify fresh at the cut). Work in the worktree `~/Programming/harness-s16-integration`. Open the DRAFT PR on origin at the FIRST WP1 commit (V30); it stays draft until the WP5 brief.
2. **The composition (the six loci made real, per obligation i):** wire each locus into the production path per §5's tasks. Verified at this authoring: all six have ZERO non-test callers (`f59.New` `internal/appctl/f59/types.go:125` · `wire.NewCodec` `internal/worker/wire/frame.go:75` · `resume.DecodeSettlementManifest` `internal/worker/resume/resume.go:84` · `supervisor.SanitizeEnv` `internal/appctl/supervisor/spawn.go:12` · `supervisor.NewSocketPair` `internal/appctl/supervisor/spawn.go:74` · `broker.NewCore`/`Core.Invoke` `internal/broker/core.go:60,93`), and no `LaunchWorker` analog exists anywhere in the repo.
3. **One honest governed turn, demonstrable:** a documented single command drives the real binaries end-to-end (§3's definition), and a NEW real-process E2 test package automates the same demonstration.
4. **In-fence file surface:** `frank/cmd/**` · `frank/internal/**` · `frank/test/**` (EXCEPT `frank/test/seam/**` — see §4 fences) · `frank/go.mod`/`frank/go.sum` (arbitration-only: WP1 needs NO new dependency; needing one = stop and escalate) · the relay root `frank/.relays/s16/**` via the engine only.

## §3 — The composed-turn definition (what "honest" and "live courier" mean here)

- **Real processes on the composed path:** `frank-app` (the m-10 module in the app main process) spawns and supervises `frank-broker`, `frank-connector`, and `frank-worker` through the production supervisor path; the worker runs `runtime.Runner.Run` (`internal/worker/runtime/runtime.go:119` — already exactly one governed turn by construction: one provider attempt, one tool call, journal close + recover check). No fake, probe, or scripted in-process peer stands anywhere ON this path — `internal/worker/fake/*` and the seam probes are test instruments and must not appear in the composed wiring.
- **The live courier:** a REAL `cmd/frank` conductor serve instance over a real store and unix socket, run-local to the demonstration (a fresh store initialized/minted for the run — NOT the team's operational `~/frank-live` store; polluting the team store with demo turns is out). The m-9 seat credential is BROKER-HELD (F60/F66; the parent charter's org row): the broker dials `channel.DialAuthenticated` and holds the conduct client; the worker's `relay.*` tool calls travel worker → broker worker-endpoint → `Core.Invoke` (connection-scoped capability, §4 m-7 law) → the conductor. The worker process NEVER holds or sees the conductor credential — the `SanitizeEnv` allow-list must exclude it by construction.
- **The demonstrated turn's tool call is `relay.submit`:** the one tool call originates at the provider boundary (`provider.ToolCallFromEvents` from the loopback provider's events through the REAL connector DATA-P path — not the `Provider.NextToolCall` local fallback), is F59-authorized (issue → consume → outcome via `f59.New`'s host on the app side), executes through the broker to the conductor, and leaves a COMMITTED relay record with a channel-stamped FROM in the conductor store. That record is m-3 capture point (c) made real.
- **Provider egress terminates at a controlled loopback endpoint** (m-8's return §1; H-12 law): the real connector runs its full `freeze→authorize→attach→send` machinery against a demo-owned local endpoint speaking the pinned lane's wire shape. NO third-party provider send exists in this slice; an intent to do genuine external egress would be an operator H-12 gate and a STOP.
- **Lane identity is a run seed** (m-8 §2): the composition derives pinned-lane facts from the frozen catalog + manifest bytes; no code parses meaning out of lane id bytes.
- **Release identity stays an honest placeholder at WP1:** `cmd/frank-app`'s current `releaseDigest` stub ("frank-mvp-development", `main.go:121`) may stand for WP1 ONLY, labeled placeholder in the demo output; WP2 replaces it with the pair-prepared F63/F65 identity evidence for the SEPARATE Master+VP binding act. No WP1 claim may call the release "bound".

## §4 — Composition laws (the PM guidance + fences folded as normative; each is an obligation, not style)

- **Fences (the charter's two classes, restated exactly):** (a) FROZEN MEMBERS — `master/exit-fixtures/STEP-3-EXIT-FIXTURES.json` @ `d4580c52…` and every digest-bound artifact: ANY edit VOIDS the freeze; STOP → bounded Master+VP route. (b) EVIDENCE INSTRUMENTS — `frank/test/seam/**` (the battery + census + partition): OUTSIDE this pair's write fence entirely; ZERO edits, ZERO new files there; the composed-turn harness lives in a NEW package (§5 T7). Battery green never discharges the three named instrument residuals. `master/**` is master-only.
- **m-7 (read r11 §2.10 + §2.3 THROUGH the study rev8):** attach returns exactly ONE of the three typed results with SUSPENSION PRECEDENCE FIRST (`broker:attach-suspended` for ANY suspension cause or the PREPARING barrier, evaluated BEFORE the tuple); the USE capability is minted per-generation AT attach, delivered only on the worker's own broker connection, CONNECTION-SCOPED (presenting on another connection rejects; re-attach mints fresh); the §2.4 per-operation fence and the four-op closed set already live in `Core.Invoke` — COMPOSE it, never re-implement beside it. Control-handover outcomes stay UNRECORDED (`R-S16A-CTRL-HANDOVER-REC`, RESIDUALS:73): if ANY WP1 byte would enter the deferred recording/schema/store machinery, the obligation fires and the work routes UP BEFORE the byte lands. The malformed-handshake→`rejected-token` fold is deliberate; carry it as-is.
- **m-8 (r12 + A-M8-BE read together):** connector-first launch ordering with the `connector_ready` gate (§5.3): the seven-field `connector_assign` → load/verify against the frozen comparands → `connector_ready`; no worker admission and no provider-send path before that ack. Credential custody per §3/m-1 1.4a: provider credentials resolve ONLY inside the post-authorize attach — the composition must not resolve or pass them elsewhere. A composed-path float refusal is the RULED fail-closed behavior (`R-S16A-FLOAT-DEFER`, RESIDUALS:71) — never "fix" it.
- **m-9 (worker r12 NEWEST bytes are the spec):** §7.1a's journal opaque-item carrier (verbatim-iff-exact-JCS | `raw_b64`, fail-closed malformed), the uniform v2 provider parsers (§2.2), §6.4's three absence classes; epochs/counters are canonical-decimal STRINGS on every wire; live replacement text is spec, struck/fenced bytes are history. The pending interface-lock worker-row re-cert at r12 is Master+VP's and does not gate this WP — but it is bookkept for the terminal gate.
- **m-10 (base-AS-AMENDED, never the base text alone):** all four registration/amendment instruments govern from pair approval with folds QUEUED — never wait on a fold. The C09 registration (`1d750580…`) carries the supervisor semantics the composition must consume: the two-counter separation (adoption advances `control_generation` exactly once durably before connect and NEVER increments G-2; only spawn rotates the token), the five broker failure classes each at their own disposition commit, adoption-first before any spawn, broker-apart-from-§A.1. The supervisor STATE is the ONE composition observable — never parallel wiring.
- **m-3 (the WP3 E3-admissibility preconditions, DESIGNED IN NOW — addressed one by one):**
  1. *Three capture points reachable by an external observer:* (a) the frozen request core at the connector boundary, capturable PRE-attach, with NO credential byte in any evidence surface; (b) the effect descriptor + the captured local invocation INCLUDING the env set on the worker side; (c) the committed relay record with the channel-stamped FROM, readable from the conductor store at the observer boundary. WP1's duty: the composition must make each point REACHABLE (a location an out-of-process observer can read without seat mediation) and the close SITREP maps each point to its composed location. WP1 does NOT build the observer.
  2. *Observer-owned output root:* the WP3 harness records from `observer_id=integration-harness` into a root the composed seats cannot write. WP1's duty: nothing in the composition may hard-wire evidence output into seat-writable space.
  3. *The F84 fresh-derivation rail:* capture points expose CAPTURED BYTES; every comparison fact is derived by the observer. WP1's duty: no composed surface pre-computes an "expected" digest into a capture slot (a byte-copied digest is the refused row class).
  4. *Unknown-HOLDS:* incomplete capture yields `unknown`, never a coerced pass/fail — a WP3-harness law; WP1 must not build any surface that silently defaults a missing capture.
  5. *Bound identities:* the ruling binds exact composed-artifact identities; a rebuilt artifact voids a historical E3. WP1's duty: the demo prints the identity set it ran under (manifest digest, tool-catalog digest, the placeholder release digest labeled as such) so WP2 can make the identity manifest a first-class harness input.

## §5 — WP1 work breakdown (tasks with file targets; mechanics inside each task are yours; task order is yours where dependencies permit)

- **T1 — branch cut + DRAFT PR.** Cut `s16-integration` per §2.1; verify ancestry + the five tree ids at the cut; first commit opens the DRAFT PR (V30). Deliverable: the branch-cut report lines in your first IMPL SITREP (exact cut SHA + the five verified ids).
- **T2 — `supervisor.LaunchWorker`** (new `internal/appctl/supervisor/worker.go` + test): the worker-process launch analog of `LaunchConnector` (`connector.go:46` is the shape precedent for handshake/lifecycle/close), REQUIRED to call the two loci: `NewSocketPair()` for the CTRL-W (and DATA-P, per the pinned seam contract) pairs and `SanitizeEnv` for the child env (allow-list explicitly excluding every credential variable). Note on shapes: `NewSocketPair` returns both sides as `net.Conn`; the child end reaches `Cmd.ExtraFiles` via the `*net.UnixConn.File()` dup — if you find the pinned contracts genuinely unrealizable over these loci, that is a spec-base escalation UP through master, never a silent switch to a parallel helper. Reuse the existing `lifecycle.go` machine, `DeathPipe`, `PrepareRuntimeDir`, and the C09 supervision semantics (§4 m-10).
- **T3 — real `cmd/frank-worker/main.go`** (replacing the 14-line stub): parse inherited fds; `os.Clearenv` posture per the connector precedent; build the framed CTRL-W/DATA-P transports over **`wire.NewCodec`** with the pinned message-spec registry; construct `runtime.Runner` with: the Control client half (T4's wire peer), the broker attach surface (worker's own broker connection), `Conductor` = the broker-endpoint adapter (T5), `tools.Backend` = the real local backend rooted in the run's workspace, and the resume path calling **`resume.DecodeSettlementManifest`** on a `resume` disposition's settlement-manifest bytes (the fresh-run demo leaves it unexercised at runtime but production-wired; its exercise arrives with the WP3 legs).
- **T4 — the app-side CTRL-W server half** (new package under `internal/appctl/`, e.g. `internal/appctl/workerctl/`): serve the worker's `runtime.Control` verbs over the app end of the socketpair — hello → `Assignment` (with `SessionLogPath` whose basename IS `journal.SessionLogName`; see the named mismatch in §8), `GenesisCommitted`, `ReportAttach`, `EvaluateProviderE0`, `TurnTerminal`, `WakeForward`, and the F59 authority verbs (`Authorize`/`Consume`/`RecordOutcome`) implemented over **`f59.New`** (`internal/appctl/f59/types.go:125`) with the manifest `Gate` (`gate.go:20`) enforcing the ratified 8-name constant (`manifest.go:19`). All frames through the T3 codec; all epochs/counters canonical-decimal strings (§4 m-9).
- **T5 — the broker worker endpoint over `broker.NewCore`/`Core.Invoke`:** wire the s16a-built broker server's worker endpoint so an attached worker's relay operations reach `Core.Invoke` with the connection-scoped capability minted at attach (§4 m-7), and `Core`'s `Conductor` caller is a `conduct.FromAuthenticated(channel.DialAuthenticated(socket, credential))` client against the live conductor (`internal/seatclient/conduct/client.go:36` — it structurally satisfies both `broker.Conductor` and the relay verbs). The broker config carries the conductor socket + the broker-held m-9 seat credential.
- **T6 — `frank-app` composition completion:** `run start` (`internal/appctl/terminal/terminal.go` StartRequest) currently ends at a connector READY probe and exits (`cmd/frank-app/main.go:144-163`, "s16a-wp3"); complete it to the full ordered composition: freeze/admit → broker (adoption-first) → connector (`connector_ready` gate) → worker (T2) → the turn runs → terminal settle → supervised shutdown. Fix the `SessionLogPath` composition defect (§8 item 1) to a worker-runtime-acceptable path (e.g. `<state-dir>/<runID>/session.log`). The supervisor STATE remains the one observable the demo asserts (§4 m-10).
- **T7 — the composed-turn E2 harness** (NEW package `frank/test/composed/`, plain `go test` — NOT under `frank/test/seam/**` and NOT `TestCT_`-named; it must not alter the battery instrument or its census): build the real binaries (`cmd/frank`, `cmd/frank-app`, `cmd/frank-broker`, `cmd/frank-connector`, `cmd/frank-worker`), stand up the run-local conductor (init store, mint the m-9 seat, serve) and the loopback provider endpoint (demo-owned, speaking the pinned lane's wire shape, returning a scripted completion whose events carry the `relay.submit` tool call), invoke `frank-app … run start …` exactly as an operator would (the CT-C07 invocation at `test/seam/connector_app_test.go:226` is the style precedent — cited, not copied into the battery), and assert the §6 proof set. Also document the same as a single manual command sequence (a `frank/test/composed/README.md` runbook) so the demonstration is operator-reproducible.
- **T8 — the loci-caller proof:** a mechanical audit (script or test in `frank/test/composed/`) that each of the six loci has a production (non-test) caller, emitting file:line evidence for the close SITREP. Anti-vacuity: this check must FAIL before your wiring lands (run it at the branch cut to prove RED) — a first-contact green is a finding routed UP.

## §6 — Boundary contract

- Writes: branch `s16-integration` — new/changed bytes ONLY in `frank/cmd/frank-worker/**`, `frank/cmd/frank-app/**`, `frank/internal/appctl/**` (supervisor worker launch + the CTRL-W half), `frank/internal/broker/**` (endpoint wiring only — `Core` semantics are pinned), `frank/internal/worker/**` (transport/wiring only — the runtime's governed-turn semantics are pinned m-9 surface), `frank/test/composed/**`; relay files under `frank/.relays/s16/**` via the engine.
- Reads: the pinned owner designs (§1, as corrected); the built s13/s14/s15/s16a surfaces.
- Target entity: the composed governed turn — real processes, live conductor record, F59-ticketed tool dispatch, journal `session.log` with resumable recovery.
- Downstream consumer: WP2 (identity evidence off this composition) · WP3 (m-3 ruling + the exit legs run against it) · WP4 (budget measurements + conformance cases on it) · WP5 (close + battle report).
- Contract: the §3 composed-turn definition + the §6 proof set below.
- Proof (E2): (1) the T7 harness green, asserting AT MINIMUM — supervisor state reaches its ready/settled observables (the C09 pattern, state-read only); `connector_ready` precedes any worker admission; the F59 ticket for the one tool call shows issue→consume→outcome in the `tickets` view; the conductor store contains the committed relay record with the channel-stamped worker-seat FROM; the worker journal exists at its runtime dir with `journal.Recover` = resumable; the demo output prints the §4-m-3-5 identity set; NO credential byte appears in any captured/evidence surface. (2) the T8 loci proof green with all six file:line rows. (3) the regression floor green at every commit (§1's three commands).
- No-consumer action: n/a — every consumer is chartered.

## §7 — Acceptance criteria (WP1 done means exactly this)

1. Branch cut verified per §2.1 with the exact cut SHA reported; DRAFT PR open from the first commit; every commit confined to §6 Writes.
2. All six loci carry production callers on the composed path, proven by T8's mechanical output (RED at cut → GREEN after wiring; the flip is in the close SITREP).
3. The composed-turn demonstration: the T7 harness passes AND the manual runbook reproduces it — one honest governed turn per §3, with every §6 proof row asserted, no fake on the composed path, provider egress loopback-terminated (H-12).
4. The m-3 capture-point map filed: the close SITREP addresses §4-m-3 points 1–5 each with the composed location or the design-out rationale — a missing row keeps WP1 open.
5. The regression floor: seam battery 64/0/64 (script-derived census), plain suite green, `go vet` clean — at every commit, and shown at the WP1-close commit.
6. Every finding (first-contact green, spec-base mismatch beyond §8's named items, fence pressure, locked-contract touch) filed and routed UP — none silently absorbed.
7. The WP1-close SITREP (implementer → planner, CC master) carrying: cut report, loci proof, demo evidence, capture map, floor evidence, findings, and the honest not-done list; then the pair reconciles and I route the WP1 close + the WP2 request through master.

## §8 — Named open items (state your reading in the PLAN-REVIEW; default = the stated disposition, deviations route UP)

1. **The `SessionLogPath` mismatch (a real composition defect, found at this authoring):** `cmd/frank-app/main.go:135` freezes `<state-dir>/<runID>.session` while the worker runtime REQUIRES basename `session.log` (`internal/worker/runtime/runtime.go:152` vs `internal/worker/journal/writer.go:18`). Disposition: fix on the app side in T6 (`<state-dir>/<runID>/session.log`-shaped), disclosed in the close SITREP. If your review reads any pinned m-10 byte as mandating the current app-side shape, it becomes a spec-base escalation UP instead.
2. **The empty provider tool advertisement:** `runtime.marshalProviderRequest` sends `Tools: []` (`runtime.go:310,330`) — the eight ratified names are never advertised on the provider wire; the scripted loopback reply supplies the `relay.submit` tool call regardless, so the composed demo is unaffected. Disposition: WP1 treats this as the built m-9 surface (pinned; not ours to change) and the demo relies on provider-events-carried tool calls per §3. If your review reads obligation (i)'s "honest turn" as requiring wire-advertised tools, that is a question UP through master to m-9, not a local edit.

## §9 — Out of scope (a touch = escalation by construction)

The F63/F65 RELEASE-BINDING and any "release bound" claim (WP2; Master+VP); live E3, admissibility, the observer harness, and any exit-leg or exit-aggregate claim (WP3); budget measurements (WP4); the five §1a conformance cases (WP4; their fixture shapes ride the ratified carrier's own §7 bytes BY REFERENCE); the gap register + battle report (WP5); `frank/test/seam/**` and every evidence-instrument byte; `master/**` and every governance doc; the frozen exit corpus; the control-handover recording machinery (the R-S16A-CTRL-HANDOVER-REC trigger); the benchmark utility demo (R-S16-UTILITY-DEMO, Step-4); any real external provider egress (H-12); any new module dependency; any push beyond the slice branch + its draft PR; merge (operator-only at WP5).

## §10 — Sequencing after your review

Your PLAN-REVIEW verdict TO me (`approve | must-revise | reject-narrow | human-decision-required`), parented to this relay, carrying your reading of §8's two items. On approve: I run the mechanical `SCOPE_DIFF` against §6 Writes and — conditions met per the commission grant (`DELEGATED_DISPATCH_AUTHORITY`, approve verdict, all-in, no hard trigger) — issue the IMPL relay carrying the first code token with `PARENT_DISPATCH_ID` = your approving PLAN-REVIEW. You then cut the branch and build WP1 straight-through; task mechanics and order inside §5 are yours. Spec-base defects found while composing are escalations UP through master to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this PLAN drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 relay client; authoring verification was read/grep/hash-only (loci caller census, LAUNCH_BASE tree ids, charter re-hash); no source, test, branch, commit, push, PR, merge, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M master/relays/CHECKPOINTS.md
