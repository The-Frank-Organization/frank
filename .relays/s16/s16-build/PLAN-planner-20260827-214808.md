## PLAN — WP1 REISSUE (s16-build-plan-5): the composed app on the live courier — R4-F1/F2 closed: plan-3's operative sections CARRIED BYTE-WHOLE below (every ★ block is an ADDITION; the only in-line substitutions are successor metadata) + the T7 trust-root fold at the addendum's exact §5 loci with `control.go` FILE-EXACT

*(Successor discipline, per R4-F1's required shape: this plan STARTS FROM the approved plan-3 at exact SHA-256 `2956b9c554ffc3d006404c5d7e372f33f1cf6920415ad2be54beb2b8fe37a4a4` and retains its operative §§1–10 byte-whole. The ONLY deviations are: (a) this title/preamble, the header lineage fields, and the SUBJECT/GRILL lines — successor metadata; (b) the ★-marked ADDITIONS carrying the master-authorized T7 amendment (`s16-t7/RECONCILE-orchestrator-planner-20260827-210714.md`: the §2.4 fence loci, the §1/§4 instrument basis, the T6/T7/T8-adjacent realization obligations); (c) the fresh ACTIONS/STATUS tail. No pre-existing clause is deleted, compressed, or rewritten; the mechanical diff against `2956b9c5…` shows additions only, outside the named metadata. R4-F2 is honored throughout: the connector-control locus is the exact file `frank/internal/connector/control/control.go`, never the package.)*

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-build-plan-5
PARENT_DISPATCH_ID: s16-build-plan-review-4
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E3
HUMAN_GATE_REQUIRED: no — the operator's next gates are the WP5 MERGE-GATE and the Step-3 ratification; the token, when its conditions are met, is a fresh DIRECT authority relay from master (or the operator), not an ask in this plan
GRILL_REQUIRED: no — the design is master's ratified plan r7 + the corrigendum'd charter + the pinned owner designs + the two Q2 instruments + the completed CT-G03 authority + the pair-approved addendum-r5 set; no new design decision is opened at this seat
IN_REPLY_TO: s16-build/PLAN-REVIEW-implementer-20260827-214207.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner, m-2.planner, m-3.planner
SUBJECT: WP1 plan-5 — R4-F1 closed (plan-3 §§1–10 carried byte-whole; the T7 fold rides ★ additions only) + R4-F2 closed (the connector-control locus is exact control.go; control_test.go and every other connector byte OUT); the r5 instrument set, the seven fixtures, T9, and the V32-updated-dispatch route as at plan-4 in substance; on your approve + my all-in SCOPE_DIFF over the amended fence, the token request routes UP for master's V32 updated dispatch

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — F59 authorization, the connection-scoped broker capability, the broker-held m-9 seat credential, provider-credential non-exposure (all acceptance-bearing; evidence = charter cardinal rules + the m-7/m-8 pinned custody laws)
- migration/backfill/destructive-write/canonical-data-repair: no — no migration, repair, or destructive data operation is planned (evidence = §5 task list)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the conductor store, authority tickets, epoch fence, and channel-stamped relay record are trust-critical state (evidence = §3/§6)
- AI-or-automation-acts-downstream: yes — a model-originated relay.submit causes a governed durable store append (evidence = §3)
- worker/scheduler/queue/retry/async-side-effect: yes — the composition supervises worker/connector/broker processes with attach retry and async IPC (evidence = §5 T2–T6)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — CTRL-W, DATA-P, the new broker-w family, and the provider request surfaces are shared process contracts (evidence = §4/§5)
- user-visible-control-with-materializer/downstream-consumer: yes — operator `run start` drives the composition; WP2–WP5 consume its outputs (evidence = §6)
- test-runtime-role-mismatch: no — the harness asserts the loopback call drawn from the lowered form of the actually presented eight-tool surface (evidence = §3/§4-m-8)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — H-12's accepted no-sandbox residual stands (operator-ratified at the interface lock); the CT-G03 seam act, though now fully authorized, remains a protected-byte change executed under exact void rails (evidence = §4/§5 T5)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade is requested; this plan runs at production-risk, the commission's own tier
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — Locked design (pointers, never paraphrase; on conflict the pinned bytes govern)

- The plan of record r7 @ `044fe4cc…` (§1(i), §2 WP1); the charter AS CORRIGENDUM'D @ `d6dd1949…` (the three pin corrections law: m-10 = live `b4d948d1…` AS AMENDED BY `b0450777…`; the m-7 THREE-instrument stack r11 `9331ea88…` + A-M7-LAUNCH-READY r2 `0063650d…` + study rev8 `64f9136e…`, study-governs read order; m-3 = preimage `009df607…` vs disk `7fce817e…`).
- Owner designs per BASIS_HASHES: m-9 worker r12 @ `63f5c49d…` + the m-9 delta @ `61833f63…` §6 · m-8 r12 @ `4b670a79…` + A-M8-BE @ `734e44b7…` · m-2 mapping @ `83d8e63e…` + component contract @ `c3a8cd61…` · m-1 @ `7c8b09a6…` · interface lock RE-CERT 3 @ `6a768023…` (RE-CERTIFICATION 3 COMPLETE at worker r12; no re-cert pending) · RLBS-3 @ `289a0da0…` · the m-10 registrations (C09 `1d750580…` · A14 `67acb760…` · D01 `30b9d2bf…`).
- The two Q2 instruments (re-hashed exact at this seat; design-of-record from pair approval, folds queued — build against base-AS-AMENDED): `A-M7-WORKER-WIRE` r4 @ `c75c469a9bd8712e362e5a2b2b492a44a6fd8b78d82ce7052e8a22164eb9296f` (approve `s16-build-q12/DESIGN-REVIEW-implementer-20260827-050324`) · the `broker-w` registration @ `a1bddf1b082a9a7d6ccf377941252d4e54394e4ae7b8e7ad30303a96be0cb796` (approve `…053826`).
- The joined Q1/Q2 answer set (normative, folded in §4): master's carriage `s16-build/RECONCILE-orchestrator-planner-20260827-124550.md` · m-9's Q1 ruling `master/relays2/s16-build-q12/SITREP-planner-20260827-032802.md` · m-2's confirmation + alias ruling `…DESIGN-planner-20260827-033927.md` · m-8's confirmation + two precisions `…SITREP-planner-20260827-032915.md` · m-10's confirmation `…DESIGN-planner-20260827-032843.md` · m-9's Q2 consumer confirm `…SITREP-planner-20260827-052148.md`.
- **The COMPLETED CT-G03 evidence-instrument authority (R2-F2 closed; the co-sign's exact terms GOVERN and are restated at §4/§5 T5):** owner legs = the two Q2 instruments above; master's bounded half `master/relays2/s16-ct-g03/RECONCILE-orchestrator-planner-20260827-130955.md`; **the VP co-sign `master/relays2/s16-ct-g03/RECONCILE-orchestrator-reviewer-20260827-131637.md` — instrument identity `SEAM-BATTERY-S16-G03-1`**, carried down by `s16-esc2/RECONCILE-orchestrator-planner-20260827-131741.md`.
- ★ **The T7 trust-root instrument set (the amendment's basis; every hash/carrier verified at this seat):** the m-8 addendum `master/domains/m-8-provider-adapters/design/2026-08-27-trust-root-artifact-addendum.md` **r5 @ SHA-256 `cb154641a53600354f9a47b9305590503d1a59b951a113feea075c047e8ac5d0`** (re-hashed exact at this seat; a governed additive delta over frozen r12 — r12/A-M8-BE bytes UNMOVED), pair-APPROVED by `s16-t7-tls-inst/DESIGN-REVIEW-implementer-20260827-180445.md` over that exact hash · the corrected endpoint owner term (master ack `s16-t7-tls-ack/RECONCILE-orchestrator-planner-20260827-165443.md`): `https://localhost:<nondefault-port>/<path>`, leaf SAN=`localhost`, non-443 port, path required — SHAPE, not a literal; the five other `152357` owner terms stand verbatim · m-10's confirmation `master/relays2/s16-t7-tls-confirm/SITREP-planner-20260827-204742.md` — all six shape requirements SATISFIED at r5; **vehicle (a): that relay IS the citable m-10 ownership record** for the manifest `trust_root_digest` schema member + the registered `ConnectorAssignBody` member, realized by this build under this amended fence · m-3's confirmation `master/relays2/s16-t7-tls-confirm/SITREP-planner-20260827-204725.md` — P1–P4 discharged as text at r5 (P1/P2/P4 with their own §7 fixtures); the declared injected-CA posture is the WP3-admissible shape; `trust_root_digest` joins the bound-identity set; the FORMAL E3-admissibility ruling remains a WP3 act · master's carriage `s16-t7/RECONCILE-orchestrator-planner-20260827-210714.md` (the completed-authority declaration + this amendment's instruction).
- The five PM ready-to-guide carriers of record: m-9 `master/relays2/s16-pm-readiness/SITREP-planner-20260826-203121.md` · m-3 `…203227.md` · m-10 `…203233.md` · m-7 `…20260827-023135.md` · m-8 `…20260827-023313.md` (the 203128/203226 predecessors are immutable history, not operative carriers).
- The regression floor: s16a close r10 @ `7bb643c8…` — seam battery 64/0/64 (`go test -tags seam -json -count=1 ./test/seam/ -run 'TestCT_' | test/seam/census.py`), plain suite `go test -p=1 -count=1 ./...`, `go vet ./...`, green at every commit.

## §2 — Scope (exact; everything else is out)

1. **Branch + worktree + DRAFT PR:** cut `s16-integration` from then-current main under the charter's LAUNCH_BASE identity check AS WRITTEN (ancestry ≥ `f007a9d6…` + the five exact subtree ids); the exact cut SHA stated in the branch-cut/first IMPL SITREP; worktree `~/Programming/harness-s16-integration`; DRAFT PR on origin at the FIRST commit (V30), draft until the WP5 brief.
2. **The composition (obligation i):** the six loci production-wired (§5), including the Q1 assembly completion and the Q2 broker-w wire realization. All six re-verified zero non-test callers at the plan-2 authoring; unchanged since. ★ The addendum-r5 trust-root realization (§5 T9) joins the composition scope under the amended fence.
3. **One honest governed turn, demonstrable** per §3 — the eight tools PRESENTED, the loopback's call drawn FROM the presented (lowered) set.
4. **In-fence file surface (the EXACT write fence — the SCOPE_DIFF comparand):** `frank/cmd/frank-worker/**` · `frank/cmd/frank-app/**` · `frank/cmd/frank-broker/**` · `frank/internal/appctl/**` · `frank/internal/broker/**` · `frank/internal/appipc/**` · `frank/internal/worker/**` · `frank/test/composed/**` (new) · **`frank/test/seam/agree_test.go` STRICTLY per `SEAM-BATTERY-S16-G03-1`: `TestCT_G03` only, the two mirrored `broker-w` list additions, nothing else — every other `frank/test/seam/**` byte stays outside the fence** · `frank/go.mod`/`frank/go.sum` arbitration-only (no new dependency; needing one = stop and escalate) · the relay root via the engine. `frank/internal/seatclient/**` explicitly OUT (consumed, never edited). ★ PLUS the addendum §5 loci EXACTLY, FILE-EXACT (R4-F2): `frank/cmd/frank-connector/main.go` (locus 1: the `--trust-root`-class argv flag → `Artifacts.TrustRootPath`, parsed BEFORE `os.Clearenv()`) · **`frank/internal/connector/control/control.go`** (locus 2 — the EXACT FILE: the optional `TrustRootPath` field + the §2 XOR/verify/fail-closed `connector_ready` withhold; `control_test.go` and every other byte of the package stay OUT) · `frank/internal/connector/service/service.go` (locus 3: thread the verified pool via a trust-aware constructor variant) · `frank/internal/connector/transport/transport.go` (locus 4: `RootCAs: pool` when present, nil/system-roots otherwise). `frank/internal/connector/endpoint/endpoint.go` is an ASSERTED-UNTOUCHED locus (locus 5): ZERO bytes, checked by exact diff at close — an exclusion, NOT a positive write grant. Every OTHER `frank/internal/connector/**` byte stays OUT. Locus 6 (the manifest `trust_root_digest` schema member + the registered `ConnectorAssignBody` member) rides the EXISTING `frank/internal/appctl/**`/`frank/internal/appipc/**` rows under m-10's vehicle-(a) ownership record.

## §3 — The composed-turn definition (unchanged from plan-2; carried whole)

- **Real processes on the composed path:** `frank-app` spawns/supervises `frank-broker`, `frank-connector`, `frank-worker` through the production supervisor path; the worker runs `runtime.Runner.Run` — one provider attempt, one tool call. No fake/probe/scripted in-process peer ON the path.
- **The presented tool surface:** the worker assembles and PRESENTS the eight ratified tools on `m8.llm_request.v1.tools[]`; the connector's `translate()` LOWERS them to the locked provider field set; the controlled loopback provider returns a tool call FROM the LOWERED presented set on the provider wire (1:1 by canonical name — the harness binds the lowered carrier or the canonical-name correspondence, never the inbound `tools[]` alone). The demonstrated call is `relay.submit` via `provider.ToolCallFromEvents`; the `NextToolCall` fallback is UNUSED on the demo path.
- **The live courier:** a run-local REAL `cmd/frank` conductor (fresh store, minted m-9 seat, unix socket) — never the team's operational store. The seat credential is BROKER-held (F60/F66); the worker never sees it; `relay.*` travels worker → broker-w wire → `Core.Invoke` (connection-scoped capability) → the conductor; the committed relay record with the channel-stamped FROM is m-3 capture point (c).
- **Provider egress terminates at the controlled loopback endpoint** (H-12); the real connector runs full `freeze→authorize→attach→send`. Lane identity is a run seed — no code parses lane-id bytes.
- **Release identity stays an honest placeholder at WP1**, labeled as such; WP2 replaces it (the F63/F65 binding is Master+VP's separate act).
- ★ **The authenticated loopback (the addendum realized):** the demo endpoint is the corrected owner-term shape `https://localhost:<nondefault-port>/<path>` with a leaf certificate SAN=`localhost` chaining to a demo-minted test CA; the REAL connector trusts it ONLY via the verified `TrustRootPath` artifact + freeze-bound `trust_root_digest` comparand (operator-named at `run start`, exactly like `credential_ref`); the verified pool REPLACES system roots, making H-12 a STRUCTURAL TLS property of the composed run (the connector physically cannot complete a trusted session to a real provider — bound by fixture 4's RED leg, never asserted in prose). `endpoint.go` and `os.Clearenv()` stay byte-untouched; the unconfigured production path stays byte-identical (nil `RootCAs`, wire-identical `connector_assign`).

## §4 — Composition laws (the fences + the joined answers + the CT-G03 terms folded as normative)

- **Fences:** FROZEN MEMBERS (the exit corpus `d4580c52…` + every digest-bound artifact): ANY edit voids the freeze — STOP → Master+VP. EVIDENCE INSTRUMENTS (`frank/test/seam/**`): outside the fence EXCEPT the single act `SEAM-BATTERY-S16-G03-1` authorizes, executed by its exact terms below. `master/**` master-only. Battery green never discharges the three named instrument residuals.
- **The CT-G03 authority's exact terms (the co-sign `131637` governs; bound as law here):** (i) the protected change = `frank/test/seam/agree_test.go`, `TestCT_G03` only — ONE `broker-w` member added to the appipc list and its MIRRORED member to the worker list; no comment, assertion, helper, other test, census script, fixture, or other seam byte; (ii) the FIVE landing conditions — same commit as BOTH production enum additions (values exactly `broker-w`); under the later DIRECT implementation dispatch + this plan's approved fence (the co-sign is NOT that dispatch); census exactly 64/0/64 + plain suite green + focused `TestCT_G03` pass at the landing commit; the WP1-close SITREP records the identity, the landing commit SHA, the post-change `frank/test/seam` tree hash, and the exact protected-file diff proving no other seam byte moved; the close record + WP5 basis cite the identity at both bound values; (iii) the VOID RAILS — any additional seam byte, a split-commit landing, an unbound/unreported identity value, any census/color/cardinality movement, or movement in either owner instrument VOIDS the authorization; a voided change returns through the full bounded route, never retained.
- **m-9's Q1 ruling + four guidance notes (`032802` §2):** (1) the FIVE-MEMBER FREEZE — the digest derives ONCE per attempt at first assembly, never recomputed across a Gate-2 reassembly window (a member move = a NEW attempt); (2) INV-E1 + description laws — a description change moves `logical_surface_digest` NOT `tool_catalog_digest`; relay-verb schemas are the LIVE render, never the F58 template or a baked copy; descriptions from the worker's own registry, `""`-never-absent, NO synthesis from schema-property annotations; (3) the refusal chain EXACT and terminal — totality/shape/alias failure while ASSEMBLING ⇒ NO digest · NO `attempt_open` · NO DATA-P · NO provider-attempt row ⇒ frozen `turn_terminal{turn_failed}` + `turn_receipt{terminal_recorded}`, NO retry; (4) floats not in play on this path.
- **m-2's alias ruling (`033927` §2):** normalization is CANONICAL-FIXED-POINT — normalize(n) = n for n ∈ the ratified 8-name set; the §3.1 table applies ONLY to non-canonical inputs (the bare-`read` alias row must NEVER touch the canonical local `read`). The three fixture legs BIND: (a) POSITIVE — the assembled canonical set incl. local `read` passes with `read` un-normalized (the anti-landmine leg); (b) ALIAS — a genuinely non-canonical name normalizes per §3.1 before the check; (c) REFUSAL — a genuinely partial/aliased/duplicated surface refuses down the §6 path.
- **m-8's two precisions (`032915`):** (a) the loopback returns from the LOWERED provider-wire set (folded into §3); (b) the governing law on the tool path is the PURE-LOWERING + E-digest discipline (r12 §5.2 + A-M8-BE §3.2: five-member lowered field set, `description` present-empty never omitted, `tools: []` present never absent, `strict` from the lane fact; `provider_lowered_tools_digest` = JCS over the LOWERED value at freeze) — NOT K6.
- **m-10 (`032843` §1):** the `attempt_open` digest consumer is COMPLETE at landed bytes — ZERO new m-10 bytes for Q1; the build produces the digest, never touches the consumer.
- **The wire contract (`c75c469a…` §§2–4 + the registration `a1bddf1b…`):** the five-frame closed `broker-w` family exactly as registered (envelope `chan: "broker-w"`; `re`-IFF-reply, `worker_push` reply-less; present-IFF member laws; membership-grade validation on both closed class enums); the §3a TOTAL outcome mapping (five rows top-down; response-plus-error DISCARDED; connection death = NO frame); the §4 close/error discipline (registry-decode failure closes; operation-grain failures never close; pre-attach operation ⇒ `broker:invalid-operation`); capability byte-equality is the BROKER's connection-scoped check, not the registry's. Where this plan's words and the instruments diverge, the instruments govern.
- **m-9's two Q2 consumption precisions (`052148` §2):** (i) consumption keys on the CLASS TOKEN whichever branch carries it — never fork behavior on the branch; (ii) the connection-death row mints NO frame BY LAW — recovery is the worker-layer §2.5 park + informed rediscovery (`project`/`read`) under a FRESH F59 ticket, never an expected wire frame, never an automatic re-send.
- **Standing readiness law (unchanged):** attach suspension-precedence; handover outcomes UNRECORDED (the R-S16A-CTRL-HANDOVER-REC trigger fires BEFORE any byte enters that machinery); malformed-handshake→`rejected-token` carried as-is; connector-first ordering + `connector_ready` gate; credentials resolve only inside post-authorize attach; composed-path float refusal is ruled behavior. **m-3's five E3-admissibility duties carry VERBATIM from plan-1 §4:** the three reachable capture points with the close-SITREP map; no evidence hard-wired into seat-writable space; capture points expose BYTES (F84); no silent defaulting of missing capture; the demo prints its identity set.
- ★ **The addendum's terms (r5 @ `cb154641…` governs; where this plan's words diverge, the instrument does):** additive + opt-in (every clause inert unless a trust root is named) · fail-closed-to-production (absence ⇒ byte-for-byte today's system-roots path; no new required production field) · transport-layer only (the trust root enters NO `frozen_core`/digest/authorized-envelope/provider-wire/F12 state; `trust_root_digest` is the SOLE standalone comparand over the PEM bytes) · non-secret (public CA certificate bytes ONLY; any private-key block ⇒ fail-closed; m-1's boundary untouched) · hermetic (argv-before-`Clearenv` artifact path, NEVER env, NEVER host trust-store mutation; the PATH never enters the manifest, `connector_assign`, or any conductor record — only the digest is carried) · the §2 freeze-binding (manifest member present-IFF-configured, exact `[0-9a-f]{64}`, frozen at admission; `connector_assign` verbatim byte-copy ABSENT-never-empty; absence-is-STATED = system posture by rule; the XOR half-config ⇒ `connector_ready` withheld) · the READY-gated verification chain (ownership/mode → hash equality → parse → CA-qualified: `BasicConstraintsValid && IsCA` + `KeyUsageCertSign`-when-present → any failure ⇒ a TRUST-COMPARAND-SPECIFIC typed fault, never a generic withhold, + zero send) · the §3 REPLACE-not-augment injection (only `RootCAs` moves, only on the verified path) · the observer re-derivation rail (the WP3 observer re-derives the digest FRESH from the readable, byte-stable PEM — F84). ★ The connector fence is LOCUS-EXACT per §2.4 — a byte in any other connector file (`endpoint.go` and `control_test.go` above all) is a fence deviation, STOP → UP.

## §5 — WP1 work breakdown (file targets; mechanics and order inside tasks are yours)

- **T1 — branch cut + DRAFT PR:** cut, verify ancestry + the five tree ids, report the exact cut SHA; first commit opens the draft PR.
- **T2 — `supervisor.LaunchWorker`** (new `frank/internal/appctl/supervisor/worker.go` + package test): calls **`NewSocketPair`** (child end to `ExtraFiles` via the `*net.UnixConn.File()` dup) and **`SanitizeEnv`** (allow-list excluding every credential variable); `DeathPipe`, `PrepareRuntimeDir`, the `lifecycle.go` machine, C09 supervision semantics. A genuinely unrealizable pinned shape = escalation UP, never a parallel helper.
- **T3 — real `cmd/frank-worker/main.go`:** inherited fds; `os.Clearenv` posture; CTRL-W/DATA-P framed transports over **`wire.NewCodec`**; the broker-w wire CLIENT (dial `broker_worker_endpoint` from the assign; `worker_attach` verbatim tuple; token-keyed result consumption per §4); `runtime.Runner` composed with the T4 Control client, the T5 conductor path, the real `tools.Backend`, and the resume path calling **`resume.DecodeSettlementManifest`** (production-wired; exercised at the WP3 legs).
- **T3a — the delta-§6 assembly completion (`frank/internal/worker/**`):** the eight-name two-array logical surface (m-2's LIVE `SchemaFromForm(form, digest)`/`ProjectSchema()`/`ReadSchema()` render for the three relay verbs; m-9's §8.3 pins for the five locals; the eight tool-level descriptions from the worker registry); canonical-fixed-point normalization + totality/ordering (byte-sorted) + the exact refusal chain; `logical_surface_digest` = SHA-256 over the JCS of the five-member object, derived once per attempt at first assembly; carried on the REGISTERED CTRL-W `attempt_open`; the eight tools presented on `m8.llm_request.v1.tools[]` (replacing the landed `Tools: []` at `runtime.go:310-330`). The m-2 fixture legs (a)/(b)/(c) land as package tests here.
- **T4 — the app-side CTRL-W server half** (new `frank/internal/appctl/workerctl/` or sibling): Assignment (SessionLogPath basename = `session.log`; the approved repair rides T6's producer side), `GenesisCommitted`, `ReportAttach`, `EvaluateProviderE0`, `TurnTerminal`, `WakeForward`, the F59 verbs over **`f59.New`** with the manifest `Gate` enforcing the 8-name constant; the `attempt_open` digest lands in the existing NOT-NULL store column.
- **T5 — the broker-w realization (`frank/internal/appipc/**` + `frank/internal/broker/**` + `cmd/frank-broker` as needed):** the new `Channel "broker-w"` constant BOTH sides + the five closed bodies registered per `a1bddf1b…` §2; the broker's worker LISTENER (`broker-worker.sock`, 0600); attach with suspension precedence + capability mint (64-hex, connection-scoped byte-equality at the broker); operations into **`broker.NewCore`/`Core.Invoke`** with the broker-authored `{broker_instance_nonce, op_seq}` identity; the §3a TOTAL mapping incl. the scrub-boundary classification into the closed nine; the §4 close discipline; `Core`'s `Conductor` = `conduct.FromAuthenticated(channel.DialAuthenticated(...))` against the live conductor. The §7 EIGHT production-registry contract tests are BUILD WORK under this task. **The CT-G03 act rides `SEAM-BATTERY-S16-G03-1` by its exact §4 terms: the two mirrored `TestCT_G03` list additions land in the SAME COMMIT as both production `broker-w` enum additions, with the census 64/0/64 + plain suite + focused `TestCT_G03` green at that commit, and the close-SITREP evidence set (identity, commit SHA, seam tree hash, protected-file diff) captured.**
- **T6 — `frank-app` composition completion:** `run start` to the full ordered composition (freeze/admit → broker adoption-first → connector `connector_ready` gate → worker → the turn → terminal settle → supervised shutdown); the SessionLogPath repair (`<state-dir>/<runID>/session.log`-shaped); supervisor STATE as the one observable. ★ Addition: `run start` carries the operator-named trust root into the freeze (the manifest `trust_root_digest` member) and the connector launch argv — named exactly as `credential_ref` is named; the PATH never enters a carrier.
- **T7 — the composed-turn E3 harness** (new `frank/test/composed/`, plain `go test`, never `TestCT_`-named): build the real binaries; run-local conductor (init/mint/serve) + the loopback provider endpoint (demo-owned, pinned-lane wire shape, returning the `relay.submit` call FROM the lowered presented set); invoke `frank-app … run start …` as an operator would; assert the §6 proof set; plus the manual runbook (`frank/test/composed/README.md`). ★ Additions: the harness mints the test CA (`BasicConstraintsValid && IsCA`) + the `localhost`-SAN leaf; serves the corrected owner-term endpoint shape; provides the CA PEM as `TrustRootPath` + its digest as the manifest comparand; **and realizes ALL SEVEN addendum §7 fixtures in the composed battery as BUILD WORK under the already-authorized `frank/test/composed/**` surface:** (1) the T7 positive (authenticated full-wire loopback turn, hermetic green); (2) the production-default byte-identity pin; (3) the fail-closed negative set (mismatch · unparseable/empty/zero-cert · `!IsCA` leaf · IsCA-but-no-`KeyUsageCertSign` · private-key block · XOR half-config · named-but-absent); (4) the H-12 structural RED leg (a dial to any non-demo-CA endpoint FAILS TLS verification — observable RED); (5) absence-decidable + trust-cause-attributable; (6) the observer re-derivation catch (a fabricated manifest copy over honest bytes is CAUGHT); (7) the mixed-bundle WHOLE-reject (no partial pool).
- **T8 — the loci-caller proof** (in `frank/test/composed/`): the mechanical six-loci production-caller audit with file:line evidence; RED at the branch cut (proven), GREEN after wiring; a first-contact green is a finding UP. ★ Addition: the addendum §5 write loci join the same proof discipline — the trust-aware constructor path gains a production caller proven by the same mechanical audit (RED before the wiring, GREEN after).
- ★ **T9 — the trust-root realization (the addendum §5 loci EXACTLY, file-exact per §2.4):** locus 1 `frank/cmd/frank-connector/main.go` (the argv flag, parsed before `Clearenv`); locus 2 **`frank/internal/connector/control/control.go`** (the optional `Artifacts.TrustRootPath`; the XOR-consistency + verify + CA-qualification enforcement + trust-specific fail-closed withhold keyed by the `connector_assign` `trust_root_digest`); locus 3 `frank/internal/connector/service/service.go` (the trust-aware constructor variant threading the pool); locus 4 `frank/internal/connector/transport/transport.go` (`RootCAs: pool` present-else-nil); locus 5 `frank/internal/connector/endpoint/endpoint.go` ZERO BYTES (asserted; proven by exact diff at close); locus 6 the manifest `trust_root_digest` schema member + the registered `ConnectorAssignBody` member (m-10's surface under the vehicle-(a) ownership record `…204742`; realized on the existing appctl/appipc fence rows). `control_test.go` and every other connector byte stay OUT; the seven fixtures live in `frank/test/composed/**` (T7), never in the connector tree.

## §6 — Boundary contract

- Writes: exactly §2.4's fence, on branch `s16-integration`; relay files via the engine.
- Reads: the §1 pinned set; the built s13/s14/s15/s16a surfaces; `frank/internal/seatclient/formschema` (consumed exported surface only).
- Target entity: the composed governed turn — real processes, live conductor record, F59-ticketed dispatch, presented-tool-surface provider attempt, journal with resumable recovery.
- Downstream consumer: WP2 (identity evidence) · WP3 (m-3 ruling + exit legs) · WP4 (budget + conformance) · WP5 (close + battle report).
- Contract: §3 + §4 + the two instruments and the CT-G03 authority by hash/id. ★ Plus the addendum r5 @ `cb154641…` by hash and the m-10 ownership record + m-3 precondition discharge by carrier.
- **Proof, split by evidence class:**
  - **E1/E2 (static + local test):** the T8 loci proof (six file:line rows, RED→GREEN); the T3a assembly fixture legs (a)/(b)/(c) green; the §7 wire-contract tests (all eight, incl. the class-closure negatives) green; the regression floor green at every commit — with the CT-G03 landing commit additionally showing the focused `TestCT_G03` pass and the census unchanged at 64/0/64. ★ Plus: the seven addendum fixtures green (the negatives explicitly enumerated in the close SITREP); the locus-5 zero-byte proof; the fixture-2 default-path byte-identity pin.
  - **E3 (composed runtime):** the T7 harness green, asserting AT MINIMUM — supervisor state reaches ready/settled (state-read only); `connector_ready` precedes worker admission; the eight tools presented and LOWERED, the loopback's `relay.submit` drawn from the lowered set; `logical_surface_digest` present on the attempt row; the F59 ticket issue→consume→outcome in the `tickets` view; the committed relay record with the channel-stamped worker-seat FROM in the conductor store; the worker journal recovers resumable; the identity set printed (placeholder release labeled); NO credential byte in any captured/evidence surface. ★ Plus: the turn completes over the AUTHENTICATED loopback with the trust comparand on the frozen manifest and `trust_root_digest` in the printed identity set.
  - This E3 is the COMPOSED-RUNTIME proof only; WP3's externally-observed live E3 and its m-3 admissibility gate are separate and unclaimed here.
- No-consumer action: n/a.

## §7 — Acceptance criteria (WP1 done means exactly this)

1. Branch cut verified + exact cut SHA reported; DRAFT PR from the first commit; every commit inside §2.4's fence.
2. All six loci production-called (T8 proof, RED→GREEN shown).
3. The delta-§6 assembly complete per §4's laws, the three alias fixture legs green, the refusal chain proven.
4. The broker-w wire realized per the two instruments, all EIGHT §7 contract obligations green (negatives included), and the CT-G03 landing evidence complete per the authority's condition 4 (identity `SEAM-BATTERY-S16-G03-1`, landing commit SHA, post-change seam tree hash, exact protected-file diff).
5. The composed-turn demonstration green at the §6 E3 proof set AND operator-reproducible via the runbook.
6. The m-3 capture-point map filed (the five §4 duties each addressed in the close SITREP).
7. The regression floor green at every commit.
8. Every finding (first-contact green, spec-base mismatch, fence pressure, locked-contract touch, any byte drawn toward the R-S16A-CTRL-HANDOVER-REC machinery, any pressure on a CT-G03 void rail) filed and routed UP — none silently absorbed.
9. The WP1-close SITREP (implementer → planner, CC master) carrying: cut report, loci proof, assembly + wire test evidence, the CT-G03 evidence set, demo evidence, capture map, floor evidence, findings, honest not-done list; then the pair reconciles and I route the WP1 close + WP2 request through master.
10. ★ The trust-root realization complete per the addendum: all SEVEN §7 fixtures green; `endpoint.go` proven byte-untouched; the production default proven byte-identical (fixture 2); the trust-specific fault causes attributable (fixture 5); the H-12 RED leg observable (fixture 4).
11. ★ The WP1-close SITREP additionally records: the addendum hash, the fixture evidence, the locus-exact diff footprint over EXACTLY the four connector write-loci files (`cmd/frank-connector/main.go` · `internal/connector/control/control.go` · `internal/connector/service/service.go` · `internal/connector/transport/transport.go` — proving no other connector byte, `control_test.go` and `endpoint.go` included), and `trust_root_digest`'s presence in the printed identity set.

## §8 — Settled readings (nothing remains open at this seat)

1. **SessionLogPath:** the app-side repair, approved at plan-review-1 and carried since — rides T6.
2. **CT-G03:** the charter's bounded route is COMPLETE (owner instruments + master `130955` + VP co-sign `131637`); the authority's exact terms are law at §4/§5 T5; my plan-2 carriage-suffices reading stands withdrawn.
3. **Dispatch authority:** trigger-present bars the delegated pair-token, per your R2-F3 and the co-sign's §Direct-token-route — settled at both orchestration seats; §10 below is the only token path.
4. ★ **The T7 trust gap:** closed by the completed owner authority (addendum r5 + the joined confirmations + master's carriage); the §5-loci fence extension is exactly master's carriage instruction, realized file-exact per your R4-F2.

## §9 — Out of scope (a touch = escalation by construction)

The F63/F65 RELEASE-BINDING and any "release bound" claim (WP2); live externally-observed E3, admissibility, the observer harness, exit legs, exit aggregate (WP3); budget (WP4); the five §1a conformance cases (WP4; carrier bytes BY REFERENCE); gap register + battle report (WP5); `frank/test/seam/**` beyond the exact SEAM-BATTERY-S16-G03-1 act; `master/**`; the frozen exit corpus; the control-handover recording machinery; the benchmark demo (R-S16-UTILITY-DEMO); real external provider egress (H-12); new dependencies; any push beyond the slice branch + draft PR; merge (operator-only); any byte movement in the two approved instruments or the CT-G03 authority's protected scope (the void rails). ★ Plus: every `frank/internal/connector/**` byte beyond the four named write loci — `frank/internal/connector/control/control_test.go` and `frank/internal/connector/endpoint/**` absolutely; any trust-root use beyond the composed proof (no production trust-root ships configured); any env-var or host-trust-store mechanism; any private-key material in the artifact; any byte movement in the addendum r5 or its confirmations (the void rail — a needed change returns to m-8 through master).

## §10 — Sequencing after your review (R2-F3 closed: direct dispatch is the ONLY token path)

Your PLAN-REVIEW verdict TO me, parented to this relay. On approve: I run the mechanical `SCOPE_DIFF` against §2.4's fence; on all-in, I route the TOKEN REQUEST up to master — **implementation begins ONLY upon a fresh direct authority relay from master (or the operator), addressed exactly to `s16.implementer`, carrying the literal implementation token and the approved fence** (the T4 three-token precedent governs the shape). No pair-issued token exists on this path. You then cut the branch and build WP1 straight-through under that dispatch; task mechanics and order are yours. Spec-base defects found while composing are escalations UP through master to the owning PM, never local fixes. ★ For the AMENDED fence specifically (the carriage `210714`): the required dispatch is **master's V32 UPDATED direct dispatch** extending the implementation authority to §2.4 as amended; until that dispatch NO connector byte moves, T7 and the WP1 close stay HELD, and every other WP1 surface continues under the EXISTING `s16-impl` dispatch, whose fence is unchanged.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ from the plan-3 bytes @ `2956b9c5…` with the ★ additions and successor metadata only, and submitted through the daemon-matching v2.9.1 client; authoring verification was read/hash-only; no source, test, branch, commit, push, PR, merge, or code byte from this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays/CHECKPOINTS.md
 M master/relays2/INDEX.md
?? frank/.relays/s16/s16-build/PLAN-REVIEW-implementer-20260827-214207.md
?? frank/.relays/s16/s16-build/PLAN-planner-20260827-212142.md
?? master/relays2/s16-t7-tls-confirm/SITREP-planner-20260827-210815.md
