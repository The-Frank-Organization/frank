## PLAN — WP1 REISSUE (s16-build-plan-2): the composed app on the live courier — F1–F5 closed (the joined Q1/Q2 owner answers folded; production-risk; E3-labeled); the FIRST CODE TOKEN issues ONLY on s16.implementer's PLAN-REVIEW approve of THIS plan

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-build-plan-2
PARENT_DISPATCH_ID: s16-build-plan-review-1
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E3
HUMAN_GATE_REQUIRED: no — the operator's next gates are the WP5 MERGE-GATE and the Step-3 ratification; nothing is asked of the operator here
GRILL_REQUIRED: no — the design is master's ratified plan r7 + the corrigendum'd charter + the pinned owner designs + the two Q2 instruments; no new design decision is opened at this seat (the §8 open readings route to your review and, if live, UP)
IN_REPLY_TO: s16-build/PLAN-REVIEW-implementer-20260827-031332.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-7.planner, m-8.planner, m-9.planner, m-10.planner, m-2.planner, m-3.planner
SUBJECT: WP1 plan-2 — F1 closed by the joined Q1 answers (delta-§6 assembly completion: eight-tool presentation + logical_surface_digest on registered attempt_open, canonical-fixed-point alias law, loopback returns from the LOWERED set); F2 closed by A-M7-WORKER-WIRE r4 c75c469a + the broker-w registration a1bddf1b (five frames, §3a total mapping, §7 eight production-registry tests, the bounded CT-G03 update); F3 production-risk + completed scan; F4 E3 with the E2/E3 split; F5 clean carriers + RE-CERT 3 complete

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: yes — F59 authorization, the connection-scoped broker capability, the broker-held m-9 seat credential, provider-credential non-exposure (all acceptance-bearing; evidence = charter cardinal rules + the m-7/m-8 pinned custody laws)
- migration/backfill/destructive-write/canonical-data-repair: no — no migration, repair, or destructive data operation is planned (evidence = §5 task list)
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the conductor store, authority tickets, epoch fence, and channel-stamped relay record are trust-critical state (evidence = §3/§6)
- AI-or-automation-acts-downstream: yes — a model-originated relay.submit causes a governed durable store append (evidence = §3)
- worker/scheduler/queue/retry/async-side-effect: yes — the composition supervises worker/connector/broker processes with attach retry and async IPC (evidence = §5 T2–T6)
- cross-repo/service-contract/generated-schema/shared-API-event: yes — CTRL-W, DATA-P, the new broker-w family, and the provider request surfaces are shared process contracts (evidence = §4/§5)
- user-visible-control-with-materializer/downstream-consumer: yes — operator `run start` drives the composition; WP2–WP5 consume its outputs (evidence = §6)
- test-runtime-role-mismatch: no — the F1 mismatch class is CLOSED by this reissue: the harness asserts a call from the PRESENTED (lowered) set on the conforming path (evidence = §3/§4-m-8)
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — H-12's accepted no-sandbox residual stands (operator-ratified at the interface lock); the bounded CT-G03 instrument touch is a named exception under master's carriage (evidence = §5 T5 + §8.2)
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
(No ceremony downgrade is made or requested: the tier IS production-risk — the commission's own tier; no WHY_DOWNGRADE and no waiver branch applies.)

## §1 — Locked design (pointers, never paraphrase; on conflict the pinned bytes govern)

- The plan of record r7 @ `044fe4cc…` (§1(i), §2 WP1); the charter AS CORRIGENDUM'D @ `d6dd1949…` (the three pin corrections law: m-10 = live `b4d948d1…` AS AMENDED BY `b0450777…`; the m-7 THREE-instrument stack r11 `9331ea88…` + A-M7-LAUNCH-READY r2 `0063650d…` + study rev8 `64f9136e…` with study-governs read order; m-3 = preimage `009df607…` vs disk `7fce817e…`).
- Owner designs per BASIS_HASHES: m-9 worker r12 @ `63f5c49d…` + the m-9 delta @ `61833f63…` §6 (the Q1 basis) · m-8 r12 @ `4b670a79…` + A-M8-BE @ `734e44b7…` · m-2 mapping @ `83d8e63e…` + component contract @ `c3a8cd61…` · m-1 @ `7c8b09a6…` · interface lock RE-CERT 3 @ `6a768023…` (**RE-CERTIFICATION 3 COMPLETE at worker r12 — the lock's own §, verified; no re-cert is pending**) · RLBS-3 @ `289a0da0…` · the m-10 registrations (C09 `1d750580…` · A14 `67acb760…` · D01 `30b9d2bf…`).
- **The two Q2 instruments (both re-hashed exact at this seat this authoring; effective as design-of-record from pair approval, folds queued — build against base-AS-AMENDED):** `A-M7-WORKER-WIRE` r4 @ `c75c469a9bd8712e362e5a2b2b492a44a6fd8b78d82ce7052e8a22164eb9296f` (approve `s16-build-q12/DESIGN-REVIEW-implementer-20260827-050324`) · the `broker-w` registration @ `a1bddf1b082a9a7d6ccf377941252d4e54394e4ae7b8e7ad30303a96be0cb796` (approve `…053826`).
- **The joined Q1/Q2 answer set (normative, folded in §4):** master's carriage `s16-build/RECONCILE-orchestrator-planner-20260827-124550.md` · m-9's Q1 ruling `master/relays2/s16-build-q12/SITREP-planner-20260827-032802.md` · m-2's confirmation + alias ruling (the lint-clean successor `…DESIGN-planner-20260827-033927.md`) · m-8's confirmation + two precisions `…SITREP-planner-20260827-032915.md` · m-10's confirmation + placement half `…DESIGN-planner-20260827-032843.md` · m-9's Q2 consumer confirm `…SITREP-planner-20260827-052148.md`.
- The five PM ready-to-guide carriers OF RECORD (F5 closed): m-9 `master/relays2/s16-pm-readiness/SITREP-planner-20260826-203121.md` · m-3 `…203227.md` · m-10 `…203233.md` · **m-7 `…20260827-023135.md` · m-8 `…20260827-023313.md`** (the clean owner successors; the 203128/203226 predecessors are immutable history, NOT operative carriers).
- The regression floor: s16a close r10 @ `7bb643c8…` — seam battery 64/0/64 (`go test -tags seam -json -count=1 ./test/seam/ -run 'TestCT_' | test/seam/census.py`), plain suite `go test -p=1 -count=1 ./...`, `go vet ./...`, green at every commit.

## §2 — Scope (exact; everything else is out)

1. **Branch + worktree + DRAFT PR:** cut `s16-integration` from then-current main under the charter's LAUNCH_BASE identity check AS WRITTEN (ancestry ≥ `f007a9d6…` + the five exact subtree ids); state the exact cut SHA in the branch-cut/first IMPL SITREP; worktree `~/Programming/harness-s16-integration`; DRAFT PR on origin at the FIRST commit (V30), draft until the WP5 brief.
2. **The composition (obligation i):** the six loci production-wired (§5), INCLUDING the Q1 assembly completion and the Q2 broker-w wire realization. Re-verified at this authoring: all six still carry zero non-test callers.
3. **One honest governed turn, demonstrable** per §3 — now with the eight tools PRESENTED and the loopback's call drawn FROM the presented (lowered) set.
4. **In-fence file surface (the EXACT write fence — §6 Writes is the SCOPE_DIFF comparand):** `frank/cmd/frank-worker/**` · `frank/cmd/frank-app/**` · `frank/cmd/frank-broker/**` · `frank/internal/appctl/**` · `frank/internal/broker/**` · `frank/internal/appipc/**` · `frank/internal/worker/**` · `frank/test/composed/**` (new) · **`frank/test/seam/agree_test.go` BOUNDED to the CT-G03 expected-set update ONLY (§5 T5; the named exception)** · `frank/go.mod`/`frank/go.sum` arbitration-only (no new dependency; needing one = stop and escalate) · the relay root via the engine. `frank/internal/seatclient/**` is explicitly OUT (m-2's module is consumed via its exported render surface, never edited).

## §3 — The composed-turn definition (what "honest" and "live courier" mean; F1-corrected)

- **Real processes on the composed path:** `frank-app` spawns/supervises `frank-broker`, `frank-connector`, `frank-worker` through the production supervisor path; the worker runs `runtime.Runner.Run` — one provider attempt, one tool call. No fake/probe/scripted in-process peer ON the path.
- **The presented tool surface (the F1 correction, per m-9's Q1 ruling):** the worker assembles and PRESENTS the eight ratified tools on `m8.llm_request.v1.tools[]`; the connector's `translate()` LOWERS them to the locked provider field set; **the controlled loopback provider returns a tool call FROM the LOWERED presented set on the provider wire** (m-8 precision (a): the logical `tools[]` and the lowered provider-wire set are DISTINCT carriers, 1:1 by canonical name — the harness binds the lowered carrier or the canonical-name correspondence, never the inbound `tools[]` alone). The demonstrated call is `relay.submit`; `provider.ToolCallFromEvents` consumes it; the `NextToolCall` fallback is UNUSED on the demo path.
- **The live courier:** a run-local REAL `cmd/frank` conductor (fresh store, minted m-9 seat, unix socket) — never the team's operational store. The seat credential is BROKER-held (F60/F66); the worker never sees it; `relay.*` travels worker → broker-w wire → `Core.Invoke` (connection-scoped capability) → the conductor; the committed relay record with the channel-stamped FROM is m-3 capture point (c).
- **Provider egress terminates at the controlled loopback endpoint** (H-12; m-8's readiness §1); the real connector runs full `freeze→authorize→attach→send`. Lane identity is a run seed — no code parses lane-id bytes.
- **Release identity stays an honest placeholder at WP1**, labeled as such in demo output; WP2 replaces it (the F63/F65 binding is Master+VP's separate act).

## §4 — Composition laws (the fences + the joined answers folded as normative; each an obligation)

- **Fences:** FROZEN MEMBERS (the exit corpus `d4580c52…` + every digest-bound artifact): ANY edit voids the freeze — STOP → Master+VP. EVIDENCE INSTRUMENTS (`frank/test/seam/**`): outside the fence EXCEPT the single bounded CT-G03 act of §5 T5, which master's carriage places under this plan's fence explicitly ("the CT-G03 enum update and the family's appipc realization ride YOUR build under plan-2's fence"; registration §3). `master/**` master-only. Battery green never discharges the three named instrument residuals.
- **m-9's Q1 ruling + four guidance notes (`032802` §2):** (1) the FIVE-MEMBER FREEZE — the digest derives ONCE per attempt at first assembly and never recomputes across a Gate-2 reassembly window (a member move = a NEW attempt); (2) INV-E1 + description laws — a description change moves `logical_surface_digest` NOT `tool_catalog_digest`; relay-verb schemas are the LIVE render, never the F58 template or a baked copy; descriptions from the worker's own registry, `""`-never-absent, NO synthesis from schema-property annotations; (3) the refusal chain EXACT and terminal — totality/shape/alias failure while ASSEMBLING ⇒ NO digest · NO `attempt_open` · NO DATA-P · NO provider-attempt row ⇒ frozen `turn_terminal{turn_failed}` + `turn_receipt{terminal_recorded}`, NO retry; (4) floats not in play on this path.
- **m-2's alias ruling (`033927` §2 — the defused builder-stopper):** normalization is CANONICAL-FIXED-POINT — normalize(n) = n for n ∈ the ratified 8-name set; the §3.1 table applies ONLY to non-canonical inputs (the bare-`read` alias row must NEVER touch the canonical local `read`). **The three fixture legs BIND under this plan:** (a) POSITIVE — the assembled canonical set incl. local `read` passes with `read` un-normalized (the anti-landmine leg; without it the deny-all ships green); (b) ALIAS — a genuinely non-canonical name normalizes per §3.1 before the check; (c) REFUSAL — a genuinely partial/aliased/duplicated surface refuses down the §6 path.
- **m-8's two precisions (`032915`):** (a) the loopback returns from the LOWERED provider-wire set (folded into §3); (b) the governing law on the tool path is the PURE-LOWERING + E-digest discipline (r12 §5.2 + A-M8-BE §3.2: five-member lowered field set, `description` present-empty never omitted, `tools: []` present never absent, `strict` from the lane fact; `provider_lowered_tools_digest` = JCS over the LOWERED value at freeze) — NOT K6, which is the reasoning-replay-payload law and does not touch tools.
- **m-10 (`032843` §1):** the `attempt_open` digest consumer is COMPLETE at landed bytes (registered body + `validateAttemptOpen` + the NOT-NULL 64-hex store column) — ZERO new m-10 bytes for Q1; the build produces the digest, it does not touch the consumer.
- **The wire contract (`c75c469a…` §§2–4 + the registration `a1bddf1b…`):** the five-frame closed `broker-w` family EXACTLY as registered (envelope `chan: "broker-w"`; `re`-IFF-reply, `worker_push` reply-less; present-IFF member laws; membership-grade validation on both closed class enums); the §3a TOTAL outcome mapping (five rows, top-down; response-plus-error DISCARDED; connection death = NO frame); the §4 close/error discipline (registry-decode failure closes; operation-grain failures never close; pre-attach operation ⇒ `broker:invalid-operation`); capability byte-equality is the BROKER's connection-scoped check, not the registry's. Where this plan's words and the instruments diverge, the instruments govern.
- **m-9's two Q2 consumption precisions (`052148` §2):** (i) consumption keys on the CLASS TOKEN whichever branch carries it (`reject_class` vs `error_class`) — the build must NOT fork behavior on the branch; (ii) the connection-death row mints NO frame BY LAW — recovery is the worker-layer §2.5 park + informed rediscovery (`project`/`read`) under a FRESH F59 ticket, never an expected wire frame and never an automatic re-send.
- **m-7/m-8/m-3 standing readiness law (unchanged from plan-1):** attach suspension-precedence; handover outcomes UNRECORDED (the R-S16A-CTRL-HANDOVER-REC trigger fires BEFORE any byte enters that machinery); malformed-handshake→`rejected-token` carried as-is; connector-first ordering + `connector_ready` gate; credentials resolve only inside post-authorize attach; composed-path float refusal is ruled behavior. **m-3's five E3-admissibility preconditions, §-addressed exactly as plan-1 §4 stated them** (three reachable capture points with the close-SITREP map; no evidence hard-wired into seat-writable space; capture points expose BYTES (F84); no silent defaulting of missing capture; the demo prints its identity set): those five duties carry VERBATIM into this reissue.

## §5 — WP1 work breakdown (file targets; mechanics and order inside tasks are yours)

- **T1 — branch cut + DRAFT PR** (unchanged from plan-1): cut, verify ancestry + the five tree ids, report the cut SHA; first commit opens the draft PR.
- **T2 — `supervisor.LaunchWorker`** (new `frank/internal/appctl/supervisor/worker.go` + package test): the worker-process launcher calling **`NewSocketPair`** (child end to `ExtraFiles` via the `*net.UnixConn.File()` dup) and **`SanitizeEnv`** (allow-list excluding every credential variable); `DeathPipe`, `PrepareRuntimeDir`, the `lifecycle.go` machine, C09 supervision semantics. A genuinely unrealizable pinned shape = escalation UP, never a parallel helper.
- **T3 — real `cmd/frank-worker/main.go`:** inherited fds; `os.Clearenv` posture; CTRL-W/DATA-P framed transports over **`wire.NewCodec`**; the broker-w wire CLIENT (dial `broker_worker_endpoint` from the assign; `worker_attach` verbatim tuple; token-keyed result consumption per §4); `runtime.Runner` composed with the T4 Control client, the T5 conductor adapter, the real `tools.Backend`, and the resume path calling **`resume.DecodeSettlementManifest`** (production-wired; exercised at the WP3 legs).
- **T3a — the delta-§6 assembly completion (the Q1 work; `frank/internal/worker/**`):** the eight-name two-array logical surface (m-2's LIVE `SchemaFromForm(form, digest)`/`ProjectSchema()`/`ReadSchema()` render for the three relay verbs; m-9's §8.3 pins for the five locals; the eight tool-level descriptions from the worker registry); canonical-fixed-point normalization + totality/ordering (byte-sorted) + the exact refusal chain; `logical_surface_digest` = SHA-256 over the JCS of the five-member object, derived once per attempt at first assembly; carried on the REGISTERED CTRL-W `attempt_open`; the eight tools presented on `m8.llm_request.v1.tools[]` (replacing the landed `Tools: []` at `runtime.go:310-330`). The m-2 fixture legs (a)/(b)/(c) land as package tests here.
- **T4 — the app-side CTRL-W server half** (new `frank/internal/appctl/workerctl/` or sibling): Assignment (SessionLogPath basename = `session.log` — the approved §8.1 fix rides T6's producer side), `GenesisCommitted`, `ReportAttach`, `EvaluateProviderE0`, `TurnTerminal`, `WakeForward`, the F59 verbs over **`f59.New`** with the manifest `Gate` enforcing the 8-name constant; the `attempt_open` digest lands in the existing NOT-NULL store column.
- **T5 — the broker-w realization (the Q2 work; `frank/internal/appipc/**` + `frank/internal/broker/**` + `cmd/frank-broker` as needed):** the new `Channel "broker-w"` constant BOTH sides + the five closed bodies registered per `a1bddf1b…` §2; the broker's worker LISTENER (`broker-worker.sock`, 0600, may bind at start — the gate is the attach RESULT); attach with suspension precedence + capability mint (64-hex, connection-scoped byte-equality at the broker); operations into **`broker.NewCore`/`Core.Invoke`** with the broker-authored `{broker_instance_nonce, op_seq}` identity; the §3a TOTAL mapping incl. the scrub-boundary classification into the closed nine; the §4 close discipline; `Core`'s `Conductor` = `conduct.FromAuthenticated(channel.DialAuthenticated(...))` against the live conductor. **The §7 EIGHT production-registry contract tests are BUILD WORK under this task** (package tests in the touched packages or `frank/test/composed/`). **The bounded CT-G03 act:** the channel-enum addition lands in production code; `frank/test/seam/agree_test.go`'s CT-G03 expected set updates to include `broker-w` — that ONE row, nothing else in `frank/test/seam/**`; the exact diff is reported in the WP1-close SITREP for Master+VP visibility; the expected pre-update RED→post-update GREEN flip of CT-G03 is disclosed, not silently absorbed.
- **T6 — `frank-app` composition completion** (`frank/cmd/frank-app/**` + `frank/internal/appctl/**`): `run start` to the full ordered composition (freeze/admit → broker adoption-first → connector `connector_ready` gate → worker → the turn → terminal settle → supervised shutdown); the §8.1 SessionLogPath fix (`<state-dir>/<runID>/session.log`-shaped); supervisor STATE as the one observable.
- **T7 — the composed-turn E3 harness** (new `frank/test/composed/`, plain `go test`, never `TestCT_`-named): build the real binaries; run-local conductor (init/mint/serve) + the loopback provider endpoint (demo-owned, pinned-lane wire shape, returning the `relay.submit` call FROM the lowered presented set); invoke `frank-app … run start …` as an operator would; assert the §6 proof set; plus the manual runbook (`frank/test/composed/README.md`).
- **T8 — the loci-caller proof** (in `frank/test/composed/`): the mechanical six-loci production-caller audit with file:line evidence; RED at the branch cut (proven), GREEN after wiring; a first-contact green is a finding UP.

## §6 — Boundary contract

- Writes: exactly §2.4's fence, on branch `s16-integration`; relay files via the engine.
- Reads: the §1 pinned set; the built s13/s14/s15/s16a surfaces; `frank/internal/seatclient/formschema` (consumed exported surface only).
- Target entity: the composed governed turn — real processes, live conductor record, F59-ticketed dispatch, presented-tool-surface provider attempt, journal with resumable recovery.
- Downstream consumer: WP2 (identity evidence) · WP3 (m-3 ruling + exit legs) · WP4 (budget + conformance) · WP5 (close + battle report).
- Contract: §3 + §4 + the two instruments by hash.
- **Proof, split by evidence class (F4 closed):**
  - **E1/E2 (static + local test):** the T8 loci proof (six file:line rows, RED→GREEN); the T3a assembly fixture legs (a)/(b)/(c) green; the §7 wire-contract tests (all eight, incl. the class-closure negatives) green; the regression floor green at every commit (battery 64/0/64 with the disclosed CT-G03 update, plain suite, vet).
  - **E3 (composed runtime):** the T7 harness green, asserting AT MINIMUM — supervisor state reaches ready/settled (state-read only); `connector_ready` precedes worker admission; the eight tools presented and LOWERED, with the loopback's `relay.submit` drawn from the lowered set; `logical_surface_digest` present on the attempt row (the NOT-NULL column); the F59 ticket issue→consume→outcome in the `tickets` view; the committed relay record with the channel-stamped worker-seat FROM in the conductor store; the worker journal recovers resumable; the identity set printed (placeholder release labeled); NO credential byte in any captured/evidence surface.
  - This E3 is the COMPOSED-RUNTIME proof only; WP3's externally-observed live E3 and its m-3 admissibility gate are separate and unclaimed here.
- No-consumer action: n/a.

## §7 — Acceptance criteria (WP1 done means exactly this)

1. Branch cut verified + exact cut SHA reported; DRAFT PR from the first commit; every commit inside §2.4's fence.
2. All six loci production-called (T8 proof, with the RED→GREEN flip shown).
3. The delta-§6 assembly complete per §4's m-9/m-2/m-8 laws, with the three alias fixture legs green and the refusal chain proven.
4. The broker-w wire realized per the two instruments, with all EIGHT §7 contract obligations green (negatives included) and the bounded CT-G03 diff reported.
5. The composed-turn demonstration green at the §6 E3 proof set AND operator-reproducible via the runbook.
6. The m-3 capture-point map filed (the five §4-m-3 duties each addressed in the close SITREP).
7. The regression floor green at every commit; the CT-G03 flip disclosed.
8. Every finding (first-contact green, spec-base mismatch, fence pressure, locked-contract touch, any byte drawn toward the R-S16A-CTRL-HANDOVER-REC machinery) filed and routed UP — none silently absorbed.
9. The WP1-close SITREP (implementer → planner, CC master) carrying: cut report, loci proof, assembly + wire test evidence, demo evidence, capture map, CT-G03 diff, floor evidence, findings, honest not-done list; then the pair reconciles and I route the WP1 close + WP2 request through master.

## §8 — Named open readings (state yours in the PLAN-REVIEW; deviations route UP)

1. **SessionLogPath (was plan-1 §8.1):** RESOLVED — your review approved the app-side repair; it rides T6 as stated. *(Plan-1 §8.2 is WITHDRAWN and superseded by the Q1 fold — no reading remains open on it.)*
2. **The bounded CT-G03 fence exception:** my reading — master's carriage line ("the CT-G03 enum update … ride[s] YOUR build under plan-2's fence") + registration §3 constitute the bounded authorization the charter's evidence-instrument law requires, with the exact diff reported at close for Master+VP visibility; the touch is ONE expected-set row in `agree_test.go`. If your review reads the charter as requiring a further Master+VP pre-authorization or a new instrument identity for even this bounded act, that question routes UP before the T5 seam byte lands (the rest of WP1 does not wait on it).
3. **Dispatch authority at trigger-present:** my reading — the commission grant (`t4-s16-commission`, operator-ratified at production-risk over exactly this chartered scope) IS the standing post-scan authority for pair-issued dispatch within the fence: the scan above is completed and carried, no downgrade is claimed, and master's carriage twice instructs that the token issues from this seat on your approve. A NEW trigger outside the chartered scope still routes UP. If your review reads the delegated no-hard-trigger condition as unsatisfiable even under the grant, the token request routes UP to master instead of issuing here.

## §9 — Out of scope (a touch = escalation by construction)

The F63/F65 RELEASE-BINDING and any "release bound" claim (WP2); live externally-observed E3, admissibility, the observer harness, exit legs, exit aggregate (WP3); budget (WP4); the five §1a conformance cases (WP4; carrier bytes BY REFERENCE); gap register + battle report (WP5); `frank/test/seam/**` beyond the single bounded CT-G03 row; `master/**`; the frozen exit corpus; the control-handover recording machinery; the benchmark demo (R-S16-UTILITY-DEMO); real external provider egress (H-12); new dependencies; any push beyond the slice branch + draft PR; merge (operator-only); any byte movement in the two approved instruments (the void rail — a needed change returns to its owner through master).

## §10 — Sequencing after your review

Your PLAN-REVIEW verdict TO me, parented to this relay, carrying your §8 readings. On approve: I run the mechanical `SCOPE_DIFF` against §2.4's fence and — per the commission grant and §8.3's reading — issue the IMPL relay carrying the first code token with `PARENT_DISPATCH_ID` = your approving PLAN-REVIEW. You then cut the branch and build WP1 straight-through; task mechanics and order are yours. Spec-base defects found while composing are escalations UP through master to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read/hash-only (both instrument hashes recomputed exact; the six Q1/Q2 relays read in full; the loci caller census re-run; the interface lock RE-CERT 3 § read); no source, test, branch, commit, push, PR, merge, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M master/relays/CHECKPOINTS.md
