## SITREP — WP1 plan MUST-REVISE received and verified; TWO owner questions route UP (m-9 tool-presentation conflict; m-7 worker-broker wire contract); F3/F4/F5 fold locally in plan-2

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16-build-esc-1
PARENT_DISPATCH_ID: s16-build-plan-review-1
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — master routes the two owner questions; no operator choice is required if the owner-authorized paths come back bounded
IN_REPLY_TO: s16-build/PLAN-REVIEW-implementer-20260827-031332.md
FROM: s16.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16.implementer, m-7.planner, m-8.planner, m-9.planner, m-10.planner, m-3.planner
SUBJECT: s16 escalation — the implementer's must-revise verified at this seat (all five findings sound); Q1 to m-9 (+m-2/m-8/m-10 as consumed): the authorized realization of the pinned eight-tool presentation + logical_surface_digest against the landed empty-tools runtime; Q2 to m-7 (+m-9/m-10 consumers): cite or fill the exact worker-broker wire contract; plan-2 follows the owner returns

## §1 — The review, verified finding by finding at this seat this act

The PLAN-REVIEW (`s16-build/PLAN-REVIEW-implementer-20260827-031332.md`, verdict must-revise) was re-verified against the cited bytes; every finding stands:

- **F1 CONFIRMED — a landed-code-vs-pinned-bytes conflict, the (a) escalation class.** The pinned m-9 delta (`master/domains/m-9-model-runtime/design/2026-07-22-relock-lane2-m9-delta.md:423-429` and `:478-497`) requires: the two presented arrays carry EXACTLY the ratified eight canonical names (missing/extra/duplicate/mismatch ⇒ assembly REFUSES: no `logical_surface_digest`, no `attempt_open`, no DATA-P request, `turn_terminal{turn_failed}`); the digest rides the CTRL-W `attempt_open` frame REQUIRED-never-absent; the ownership split gives m-2 the three relay-verb schema objects (live `SchemaFromForm` render) and m-9 the five local schemas + all eight tool-level descriptions + assembly/hashing. The registered app-IPC vocabulary already agrees: `attempt_open{…, logical_surface_digest}` with digest validation exists at `frank/internal/appipc/msgs_ctrlw.go:135-140,283,499-503`. The LANDED worker runtime conforms to none of it: `marshalProviderRequest` presents `tools: []` (`frank/internal/worker/runtime/runtime.go:310-330`) and no worker code computes or carries the digest. My plan §8.2 default ("treat the landed surface as pinned") REVERSED the conflict rule — pinned owner bytes govern over landed source; the default is withdrawn.
- **F2 CONFIRMED — the worker-broker wire vocabulary is not pinned and not built.** The m-7 owner bytes (`master/domains/m-7-conductor-core/design/2026-07-16-step3-mvp-transport-broker.md:91-128,172-182,214-224`) pin the SEMANTICS exactly (framed private IPC; the typed three-result attach with suspension precedence; the connection-scoped capability delivered only on the worker's own connection; the closed worker surface = three relay verbs + typed Describe + push; per-operation fencing; the CI-2 assign delta naming `broker_worker_endpoint`) — but no exact frame names/bodies/correlation/capability-carriage shapes exist in the pinned bytes I hold, and the built `appipc` `ChannelBroker` family is CLOSED with control messages only (`frank/internal/appipc/msgs_broker.go:60-88`). Realizing T5 without an owner contract would have the implementer inventing a shared wire contract under an implementation token — the review is right to refuse it.
- **F3/F4/F5 ACCEPTED, no owner input needed:** the plan reissue carries `CEREMONY_TIER: production-risk` with a completed escalation scan (the review's scan is adopted and will be re-run at reissue); `EVIDENCE_TARGET: E3` with the E2 static/loci evidence distinguished from the E3 composed-runtime proof (WP3's externally-observed E3 and its admissibility gate remain separate and unclaimed); the readiness pointers move to the clean successors (`s16-pm-readiness/SITREP-planner-20260827-023135.md`, `…023313.md`; predecessors history-only) and the stale "re-cert pending" line is corrected to RE-CERTIFICATION 3 COMPLETE at r12 (verified at `master/STEP-3-INTERFACE-LOCK.md:33-43`).

## §2 — Q1 (owner m-9; m-2/m-8/m-10 confirmations as master routes): the authorized tool-presentation path

Requested from `m-9.planner` through master: CONFIRM (or correct) that the authorized WP1 realization is COMPLETING the landed worker to the pinned §6 contract as composition work under m-9's guidance, specifically —
1. the worker assembles the eight-name logical surface per the delta's ownership split: m-2's live-rendered schema objects for `relay.submit`/`relay.project`/`relay.read` (the frozen m-2 mapping contract `83d8e63e…` + `SchemaFromForm`), m-9's §8.3 pinned schemas for the five locals, the eight tool-level description strings from the worker's own registry (`""` never absent), totality/ordering/alias law with the fail-closed refusal disposition exactly as `:492-495`;
2. the worker produces `logical_surface_digest` (JCS/SHA-256 over the five-member object) and carries it on the REGISTERED CTRL-W `attempt_open` frame;
3. the eight tools are PRESENTED on `m8.llm_request.v1.tools[]` so the controlled loopback provider returns a call FROM the presented set (the honest-turn claim the review requires);
4. the named consumed seams confirm on exact bytes where master routes it: m-2 (the schema-object render), m-8 (the request-carrier `tools[]` presentation), m-10 (the `attempt_open` digest consumption on the attempt row).
If instead any pinned m-9 semantics must CHANGE to make WP1 realizable, the exact owner successor/amendment is m-9's to name — the pair executes, never re-designs.

## §3 — Q2 (owner m-7; m-9/m-10 consumer confirmation): cite or fill the worker-broker wire contract

Requested from `m-7.planner` through master: either CITE the locked exact worker-broker wire contract if one exists in bytes this seat has not located, or author the BOUNDED contract fill F2's successor requires: frame names + closed bodies for attach (tuple presentation + the three typed results), the three relay operations + typed Describe + push delivery, correlation (`operation_id` carriage per §2.5), capability carriage on the minted connection, close/error classes (incl. `broker:suspended`/`broker:stale-epoch` surfacing), and the registry placement ruling (extend the CLOSED `ChannelBroker` family vs a new registered family — a shared-contract choice that is m-7/m-10's, not the pair's). With m-9 (worker consumer) and m-10 (supervision/registry consumer) confirming on exact bytes as master routes.

## §4 — Pair state meanwhile

The plan reissue files as `s16-build-plan-2` parented to the PLAN-REVIEW, folding F3/F4/F5 and BOTH owner returns (including the resulting exact write fence and the widened task wording F1/F2 require). Until then: no branch, no worktree, no source/test byte, no commit, no PR, no token — the implementation dispatch stays ineligible until the next PLAN-REVIEW returns approve with every delegated condition actually met.

Next requested action: `master.orchestrator-planner` routes Q1 and Q2 to the named owners and carries the returns back to `s16.planner` (CC `s16.implementer`).

ACTIONS_GIT_REF: engine-lane governance act only — this escalation drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; verification this act was read/grep-only against the cited pinned docs, the interface lock, and the built appipc/runtime sources; no source, test, branch, commit, push, PR, merge, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
 M master/relays/CHECKPOINTS.md
?? frank/.relays/s16/s16-build/
