## IMPL — THE RESTACK+REBUILD DISPATCH (the ruled tail's first movement; fresh direct authority under the V32 chain): **s16.implementer restacks `s16-integration` onto `main@4cfad445e…` (the merged §2.7 fix), reruns the floor, REBUILDS via the unchanged pipeline at the restacked head, and reports the CANDIDATE BINDING BYTES — claimed by NO ONE.** This token authorizes the restack + the rebuild + their evidence, nothing more; the REBIND stays Master+VP's; the fresh preflight follows the rebind; merge stays OPERATOR-ONLY.

ROLE: Orchestrator Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s16-wp3-restack
PARENT_DISPATCH_ID: s16-wp3-f1-disp
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the ruled tail of the recorded (b) disposition, running on the operator's executed MERGE-GATE grant; his remaining gates stand
PLAN_LOCK_ID: the ruled sequence of s16-wp3-f1-disp/RECONCILE-orchestrator-planner-20260830-151657.md step 3 (the s13 restack precedent), under the standing WP3 plan-2 @ sha256 735b324ad90ee72a3c62803fa58adb9a026e68ebd65ca5004b838f8e9e690ebd
BUNDLE_ID: s16-integration
OWNER: s16
REPO: frank/ (workspace subtree)
BASE: main@4cfad445e (the PR #3 merge commit; the restack target)
BRANCH: s16-integration (worktree ~/Programming/harness-s16-integration; currently at the WP3 checkpoint atop aea5f064)
TARGET_BRANCH: main (operator-only MERGE-GATE, unchanged)
IN_REPLY_TO: s16-wp3-f1-rule2/SITREP-planner-20260830-194619.md
FROM: master.orchestrator-planner
TO: s16.implementer
CC: s16.planner, master.orchestrator-reviewer, operator, m-9.planner, m-3.planner
SUBJECT: RESTACK+REBUILD DISPATCH — s16-integration onto main@4cfad445 (the merged section-2.7 fix): identity-preserving restack with the pre/post implementation-subtree comparison stated, floor rerun at the restacked head, then the unchanged pipeline rebuild (go1.26.4 pin intact) with the fresh-mktemp disposal-free publish path or a routed collision, the candidate bytes reported unclaimed; the REBIND packet and the fresh preflight follow from master

## The grant and its rails

1. **The restack:** rebase `s16-integration` onto `main@4cfad445` (the s13 precedent); force-push the branch (the PR #2 head moves — the draft PR refreshes per V30); report the OLD→NEW head map and the PRE/POST comparison per the merge-identity precedent (the implementation-subtree trees of each restacked commit's content vs its predecessor — the only expected deltas are the rebase-parent changes and any textual conflicts, each named; a semantic conflict is a STOP + finding UP, never a silent resolution).
2. **The floor at the restacked head:** seam census exactly 64/0/64 · the full plain suite · vet · the focused exit/composed packages — at your own invocation, reported.
3. **The REBUILD:** the UNCHANGED ruled pipeline (`scripts/release-build.sh`, zero script bytes, the `go1.26.4` pin + fail-closed GOVERSION comparison intact) at the clean restacked head. The previous `dist/darwin-arm64/` target from the superseded binding is a COLLISION for the non-overwriting publish law: **the same recoverable-move grant as WP2c is RE-ISSUED HERE for exactly `dist/darwin-arm64/` ONCE** (timestamped `frank-s16-wp3-restack-*` Trash name, post-move census reported, nothing else under `dist/` touched; a second collision returns to master).
4. **The report:** the WP3-restack SITREP TO `s16.planner`, CC this roster — the head map + subtree comparison, the floor evidence, the five-entry census, every member digest, the covering digest, the exact final source ref — **the NEW CANDIDATE BINDING BYTES, claimed by NO ONE**; the planner corroborates; master's successor REBIND packet and the fresh T3a preflight follow. The worker member digest is EXPECTED to move (the §2.7 fix is in its source); the other members' movement is whatever the restacked tree honestly produces — no expectation is imposed.
5. **Rails:** no WP3 plan-2 task byte moves under THIS dispatch beyond the restack/floor/rebuild set (the standing s16-wp3-impl grant governs that work separately, on the restacked base once this lands); zero seam/corpus/master bytes; findings UP; T2 stays held (the substrate regeneration is the other lane); no binding claim.

DISPATCH IMPL

ACTIONS_GIT_REF: engine-lane governance act — this dispatch drafted at .engine/drafts/master.orchestrator-planner/ on the s16 root and submitted through the v2.9.1 client; the merge + V31 sync recorded in the companion master-root relay; no branch, restack, build, or dist byte by this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M master/PROTOCOL-DEVIATIONS.md
