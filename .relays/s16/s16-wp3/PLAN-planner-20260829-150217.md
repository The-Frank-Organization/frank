## PLAN — WP3 REISSUE (s16-wp3-plan-2): the live-E3 + exit-run realization against the COMPLETED RELEASE-BINDING OF RECORD (covering `96e2cba9…` @ BoundAtRef `aea5f064…`, VP co-signed zero findings) — the five directed locked inputs folded: the canonical cross-root design edge · the bound record run with BOTH production flags · the m-3 TWO-ACT split (preflight before execution, FORMAL ruling post-capture) · the five named acceptance cases + RED controls · every plan-1 closure byte-whole; the F65 §5 realization lands inside this build under m-7's chartered review

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s16-wp3-plan-2
PARENT_DISPATCH_ID: s16-wp3-plan-review-1
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E3
HUMAN_GATE_REQUIRED: no — the plan round master opened; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
GRILL_REQUIRED: no — the plan realizes the frozen oracle, the banked rulings, and the ratified acceptance law; it opens no new design decision (the one owner judgment in scope — E3 admissibility — is ROUTED to its owner m-3, never decided here)
DESIGN_RECORD_KIND: design-doc
DESIGN_SOURCE_REPO: .
DESIGN_SOURCE_COMMIT: c45de9def6c72309d265d1bc9bd73f54f83c4550
DESIGN_SOURCE_ROOT: master/relays2
DESIGN_SOURCE_PATH: master/STEP-3-S16-INTEGRATION-PLAN.md
DESIGN_OWNER: master
DESIGN_DOC_ID: step3-s16-integration-plan
DESIGN_SHA256: 044fe4ccefa3b55e16c897afecfe1f6bca9971a2a7c905aac804bd53713c726d
DESIGN_LOCK_ID: step3-s16-integration-plan r7 (the edge above; §2 WP3, obligations iii–iv, the F2 acceptance law) with the co-governing locks cited opaque: RLBS-3 sha256 289a0da01d9ba654f10b01865d54d9174aa862ba5a3eec21439e0ca3ef4d93c3 · the frozen corpus sha256 d4580c52675038049471e2fd4ea813c42604b21b0032a9ba5f39fa794f972639 · the realized carrier sha256 cb1e5b6c4844170cc69643226da23dc96eca88fb354ae585b0c34a12fbfb1641 · the F65 instrument r3 sha256 d7ae2956d3ece8eabfccec4e8ee719536e34f730765975326eea749b46aaed9f · the binding of record (Master half 062854 + VP co-sign 065415)
IN_REPLY_TO: s16-wp3/RECONCILE-orchestrator-planner-20260829-145402.md
FROM: s16.planner
TO: s16.implementer
CC: master.orchestrator-planner, master.orchestrator-reviewer, operator, m-3.planner, m-7.planner, m-9.planner, m-10.planner
SUBJECT: s16 WP3 plan-2 (the directed reissue) — the frozen 8-leg/13-record suite by reference against the BOUND release (both production flags, verbatim ref + recomputed covering digest, no placeholder); m-3 two-act split (preflight pre-run, formal ruling post-capture, no closure before it); five named acceptance cases + RED controls; F65 §5 realization in-fence under m-7 chartered review; cross-root design edge carried; active-root lint clean before filing

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential, capability, custody, or authorization surface changes; the harness consumes the composed app's existing surfaces
- migration/backfill/destructive-write/canonical-data-repair: no — evidence runs only; the frozen corpus is zero-byte; fault injection acts on run-scoped harness state, never durable canonical data
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the exit verdicts and E3 records are the Step-3 acceptance's trust-critical evidence
- AI-or-automation-acts-downstream: yes — the exit rows feed the Master+VP exit packet and the operator's ratification
- worker/scheduler/queue/retry/async-side-effect: yes — the suite drives the composed worker/app/broker path including crash and duplicate-delivery legs
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the suite exercises the locked cross-owner contracts; the E3 record follows m-3's owned schema
- user-visible-control-with-materializer/downstream-consumer: yes — the record run's rows are consumed by the exit packet
- test-runtime-role-mismatch: no — the legs execute on the COMPOSED production path from the bound release artifacts; the harness is the evidence instrument, not a stand-in
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — a NEW test surface (frank/test/exit) enters the fence; the three named instrument residuals stand undischargeable by battery green; both are stated, neither waived
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

Authority note: plan-only — implementation starts ONLY on master's fresh direct dispatch per the V32 chain. Evidence-grade note: E3 is the RECORD run's target grade; the build/shakedown work beneath it is E2, and the split is stated per item.

## §0 — The reissue disposition (all four plan-review-1 findings closed; the binding of record cited verbatim)

Per the direction `s16-wp3/RECONCILE-orchestrator-planner-20260829-145402.md`: **R1-MR-3 and R1-MR-4 are RESOLVED UPSTREAM** — the carrier instrument @ `cb1e5b6c…` was realized at WP2c (the production consumption seam exists and is consumer-proven at the rebuilt candidate) and its §A accepted the covering `release_digest` arm binding all four F63 members transitively; the RELEASE-BINDING OF RECORD now EXISTS (Master half `master/relays2/s16-release-binding-2/…062854`, VP co-sign `…065415`, zero findings): **`BoundAtRef` = `aea5f064c7c0d80ac7c81649809ac66c0011382d` · covering `release_digest` = `96e2cba98f4847c3c6d7e0ae13192bf69d19e63523ddc3543c6ef8e282772a96` (over the exact 615-byte canonical manifest) · members `frank-app 708a631e…` / `frank-broker 40bc6c85…` / `frank-connector 1f40bd17…` / `frank-worker 975c19d6…` · F65 = `A-M7-F65-GRAIN` r3 @ `d7ae2956…` (equality-in-time at the WP3 run; no F65 run value exists yet BY DESIGN) · the pre-realization witness (`9052ec0f…` @ `4e179fc3…`) = labeled non-binding history.** **R1-MR-1 and R1-MR-2 are FOLDED HERE** (the two-act m-3 split at ★T3a/★T3b + the record-run gate rewrite at ★T4; the five named acceptance cases + RED controls at ★T5). The canonical cross-root design edge above carries the WP3 design set (verified at my seat: the plan file on disk AND at `main` commit `c45de9de…` both hash exactly `044fe4cc…`; the origin/approve chain lives at `master/relays2/s16-plan/` with `DESIGN_DOC_ID: step3-s16-integration-plan`, latest verdict approve over exact r7). Both pair seats hold the adopted practice: ACTIVE-ROOT lint clean before filing (this carrier was). §§1–6 below are plan-1's bytes with the ★-marked folds — every carried closure retained operative, none synopsized.

## §1 — The governing basis (verified at my seat this authoring)

The WP3 opening `s16-wp3/RECONCILE-orchestrator-planner-20260828-143329.md` (WP2b BANKED at master's third invocation; the plan round opened in parallel with the binding-act assembly; the dist path rule ADOPTED: any consuming act reads ONLY `dist/<goos>-<goarch>/RELEASE-MANIFEST.json`). The commissioned plan r7 §2 WP3 + the F2 acceptance law, quoted verbatim where it binds (§4 below). **RLBS-3** re-hashed exact at my seat: `master/STEP-3-EXIT-ORACLE-FREEZE-RELOCK-3.md` = `289a0da01d9ba654f10b01865d54d9174aa862ba5a3eec21439e0ca3ef4d93c3`, binding the frozen corpus `master/exit-fixtures/STEP-3-EXIT-FIXTURES.json` @ `d4580c52675038049471e2fd4ea813c42604b21b0032a9ba5f39fa794f972639` (99,270 B; 13 records / 32 governed turns / 100 tool calls; `common/workload-definition.json` @ `44f48808359b2b7c37423bf30fbe9f197000a6cc1d97b1a5d71c6458e00b813c`) and the interface lock RE-CERT 3 @ `6a768023…`. **The 13 exit records, the manifest's own explicit enumeration (membership is NEVER a prefix match):** `xit-gov-1` · `xit-dur-1` · `xit-dur-2` · `xit-dur-3` · `xit-dur-4` · `xit-dur-5` · `xit-crash-1` · `xit-inj-1` · `xit-ho-1` · `xit-op-1` · `neg.STALE_EPOCH` · `xit-edit-1` · `xit-edit-2`. **The EIGHT-leg shape** = r11 §6 as retirement-amended (the ratified six legs + (a) successor legitimacy — the TWO-arm conjunction: `positive` riding the regenerated `xit-dur-1` AND `neg.STALE_EPOCH` zero-work `{0,0}`; the leg passes only when both pass, resolved-unexpected ⇒ fail, unresolved ⇒ unknown + (b) the `xit-edit-1`/`xit-edit-2` pair as ONE indivisible discrimination witness). **The instruments of record** (evidence floor, NEVER a verdict): the battery @ `dd250a31…` · the cardinality partition @ `8ccc4a78…` · census v3 @ `72a8e1ac…`; the THREE named residuals (probe-C live-anchor · the unenforced content-duplicate · hard-link provenance) stand VP-concurred and are NOT dischargeable by battery green. **The m-3 basis:** r4 at the charter-adopted two-layer identity (ratified preimage `009df607…`; current file `7fce817e…` per register row 4-M3-SCHEMA) — the F62 E3 record is `m3.e3_observation.v2` (the STAGE6 two-schema amendment @ `9e874df8…`), authored by the SEPARATE observer (writer IS `observer_id`; a record surfacing any other way is `non_applicable(observer-mismatch)` — no self-supplied E3), stored OUTSIDE the conductor store, applicability bound to the exact identity vector (§3.4: any bound-digest mismatch ⇒ non-applicable); the T7-round confirm `master/relays2/s16-t7-tls-confirm/SITREP-planner-20260827-204725.md`: P1–P4 discharged as text at addendum r5, the declared injected-CA posture IS the admissible shape, **the formal E3-admissibility ruling stays a WP3 act** — routed below, never assumed. **The WP2b banked object:** head `4e179fc3…` (the corrected serve gate + the canonical `go1.26.4` release set at `dist/darwin-arm64/`). **H-12 stands throughout:** loopback only; no external/untrusted/multi-tenant use; no real third-party provider egress. ★ **The WP2c/binding additions to the basis:** the realized §B consumption seam at head `aea5f064…` (the paired `--release-manifest`/`--bound-at-ref` production flags; consumer-proven); the binding of record (§0's verbatim values); the F65 instrument r3 @ `d7ae2956…` (§2 store-owned loaded members `{engine, fieldspec, catalog}` + the `sha256:`-prefixed composite law + the STRICT generation walk; §5's five realization obligations land INSIDE THIS BUILD under m-7's chartered review); the standing dist path rule (consuming acts read ONLY `dist/<goos>-<goarch>/RELEASE-MANIFEST.json`).

## §2 — Scope and the EXACT fence (the SCOPE_DIFF comparand; ★ = the one new surface)

IN: ★ `frank/test/exit/**` (NEW — the exit-run harness, drivers, observer, evidence emission; the entire new surface) · `frank/test/composed/**` (composed-harness helper reuse/extension ONLY — no weakening of any existing assertion) · ★★ **the F65 realization rows, ONLY for the instrument's §5-obligated bytes, EACH under m-7's chartered review:** `frank/cmd/frank/**` (the serve-start stamp write + publication-before-accept) · `frank/internal/store/**` (the STRICT generation walk — the skip-tolerant `acceptedConfigChanges` gains its strict variant per §2.2, never described as landed) · `frank/internal/config/**` (the composite framing where §5.3 requires it); any byte inside these rows beyond the §5 obligations, or any F65 byte proving necessary OUTSIDE them (e.g. `internal/channel`/`internal/engine`), = STOP + a bounded fence-amendment round through re-review, never silent · `frank/go.mod`/`frank/go.sum` arbitration-only · the relay root via the engine. Explicitly OUT: **ALL OTHER production source — `frank/cmd/**` beyond `cmd/frank` + `frank/internal/**` beyond the two named rows: ZERO bytes** (a production defect surfaced by any leg = STOP + finding UP through master to the owning PM, never a local fix) · `frank/test/seam/**` (ZERO bytes) · `frank/scripts/**` · `master/**` INCLUDING the frozen corpus + the instruments of record (any corpus/manifest edit VOIDS RLBS-3; any instrument change needs bounded owner + Master+VP review + a NEW instrument identity) · every m-x design/lock byte · `dist/**` (READ-ONLY consumption, exclusively via the ruled path `dist/<goos>-<goarch>/`; ★ the BOUND release is NEVER rebuilt or mutated in this lane — any dist mutation = STOP).

## §3 — The work items

- **T1 — the hash-gated fixture loader (the fail-closed floor of everything):** the harness verifies, BEFORE any run byte: SHA-256(`master/exit-fixtures/STEP-3-EXIT-FIXTURES.json`) == `d4580c52…` AND SHA-256(`common/workload-definition.json` as the manifest locates it) == `44f48808…` — a mismatch ABORTS with a finding UP (the freeze is voided territory, never harness-absorbable). The record set is consumed as the manifest's own `exit_inventory.ids` enumeration verbatim (the §1 thirteen; never a prefix match); each record's execution contract (its `fixture_id` · `fault_injection_point` · `expected_canonical_rows` · `evidence_locator`, plus the manifest's `injection_overlay` + `limits_table_testonly` members where a row invokes them) is carried **BY REFERENCE: the frozen bytes at the pinned hash GOVERN each record's exact shape and disposition — the harness executes them, never re-derives them** (the §1a non-rederivation law, applied to the exit corpus).
- **T2 — the eight-leg realization on the composed path:** per-leg drivers in `frank/test/exit` over the WP1 composed-harness lineage (five processes, the authenticated loopback posture, the injected trust root per addendum r5), ★ **on the record run launching the PUBLISHED BOUND BINARIES from the ruled dist path with BOTH production flags (`--release-manifest` the published candidate path · `--bound-at-ref aea5f064…`) — the persisted frozen manifest MUST carry the verbatim ref + the independently recomputed covering digest; NO placeholder on the record run** — each leg's verdict computed by ITS OWN predicate from the frozen rows: the conjunction legs pass only WHOLE (leg (a): both arms; leg (b): both `xit-edit` records — one indivisible witness); per-leg verdict domain `PASS | FAIL | HOLD | unknown`, resolved-unexpected ⇒ FAIL, unresolved ⇒ unknown; the crash/duplicate legs (`xit-crash-1`, and every fault_injection_point row) drive REAL process kills / injected faults on the composed path per the frozen rows, run-scoped and non-destructive to any durable canonical data. **The claim ceilings ride every driver verbatim** (§4.3). Engineering shakedown runs during the build are PERMITTED and labeled **NON-RECORD: they claim NOTHING** — no E3, no leg verdict of record, no live word.
- ★ **T3a — the m-3 PREFLIGHT acceptance (pre-run; MY act, not a code byte):** with the token request I route UP through master to `m-3.planner` the CONCRETE record-run design for the owner's PREFLIGHT acceptance — the recorder boundary (a separate harness observer process authoring `m3.e3_observation.v2` records OUTSIDE the conductor store into an OBSERVER-OWNED output root the composed seats cannot write, the writer as `observer_id=integration-harness`, presented with recorder-boundary identity per §3.6 — no self-supplied E3) · the capture plan (the three capture points of ★T5) · the applicability inputs AS THE RUN WILL BIND THEM (the binding of record verbatim: covering `96e2cba9…` + `BoundAtRef aea5f064…` + the four member digests · the manifest/policy/lane/catalog digests · `trust_root_digest` · the F65 stamp fields at the tested instance, equality-in-time) · the per-leg evidence-locator plan. The record run starts ONLY after m-3's preflight acceptance returns; a returned correction folds as a plan amendment through the pair, never silently.
- ★ **T3b — the m-3 FORMAL ruling (post-capture; the owner's act over the exact bytes):** after the record run produces its IMMUTABLE captures and candidate `m3.e3_observation.v2` records, the exact bytes + context route UP to m-3 for the FORMAL E3-admissibility ruling — the ruling renders against the live run and its captures, never before them (m-3's own temporal law, verified at the sources). **NO E3 claim and NO exit closure of any kind proceeds before that post-capture ruling returns.**
- **T4 — the exit RECORD run + live E3 (triple-gated):** HARD PRECONDITIONS, all three, none waivable by the pair: **(a) the COMPLETED Master+VP RELEASE-BINDING act — ★ SATISFIED and cited (§0's binding of record; the record run consumes it as its release identity, both production flags, no placeholder)** · **(b) ★ m-3's returned PREFLIGHT acceptance (T3a) — the FORMAL ruling is NOT a run precondition but the post-capture closure gate (T3b): no E3/exit closure before it returns** · **(c) the T1 hash gate green at run time**. The run executes the thirteen records from the BOUND release artifacts via the ruled dist path on the composed loopback posture; every leg's verdict recorded ROW-BY-ROW; the E3 observation records authored by the observer per the ruled shape and inventoried; the battery/partition/census cited as the evidence FLOOR only. **HOLD and red rows are recorded HONESTLY and are NON-CLOSING** — each feeds the WP5 gap register; no re-run massaging, no leg dropped, no aggregate restated.

- ★ **T5 — the five NAMED m-3 readiness acceptance cases, executable, WITH their RED controls (R1-MR-2; the readiness return §2 governs by reference):** (1) **the capture-point map** — all three observer-reachable capture points exist and are proven reachable (the pre-attach FROZEN REQUEST CORE at the connector's freeze→authorize→attach→send boundary, credential-free; the worker EFFECT DESCRIPTOR + captured local invocation/env; the COMMITTED stamped relay record read from the conductor store at the observer boundary); (2) **the observer-owned output root** — composed seats CANNOT write it; RED control: a seat-write attempt is DENIED and the denial witnessed; (3) **the F84 fresh-derivation rail** — every comparison fact freshly derived by the observer from captured bytes; RED control: a copied-digest row is REFUSED; (4) **unknown-HOLDS** — an incomplete/gapped/undecodable capture yields `unknown` and the leg HOLDS; RED control: a deliberately truncated capture produces `unknown`/HOLD, never pass/fail; machines 2 and 5 stay NON-GATING and no s16 claim promotes them; (5) **bound-identity mutation invalidation** — per-member: mutating ANY bound member (each of the four F63 digests · manifest · policy · lane · catalog · trust root · the F65 stamp fields) makes a prior record non-applicable at the evaluator; RED control per member class.
- ★ **T6 — the F65 §5 realization (the instrument r3 @ `d7ae2956…` §§2–5 GOVERN BY REFERENCE; every conductor byte under m-7's CHARTERED REVIEW before the close claims it):** (1) the `config_generation` producer — the STRICT genesis-anchored walk per §2.2 (never the skip-tolerant helper's behavior), with the full fixture battery INCLUDING the strictness negatives (corrupt/quarantine the Nth and separately an EARLIER accepted config-change record ⇒ each restart FAILS CLOSED, no new stamp, zero listener acceptance, no ordinal regression/reuse); (2) the serve-start stamp write (`serve-stamp.v1.json`, atomic, publication BEFORE listener accept) + the observer join + the §3.4 census row; (3) the composite recomputation with the PRODUCTION algorithm + the §5.3 byte-shape battery (`^sha256:[0-9a-f]{64}$` in BOTH carriers; reject bare-hex/double-prefix/uppercase/wrong-length/non-hex) + the per-member one-byte mutation proofs (`engine`/`fieldspec`/`catalog`) with a positive re-stamp after each; (4) the mechanical source-closure capture (`go list -deps ./cmd/frank`, module-filtered) recorded with the `cmd/frank-mcp`-ABSENT proof; (5) alias-vs-loaded provenance labeling on every witness row. The §2.2 rail rides: any part the pair judges a spec CHOICE the frozen contract does not force routes UP rather than landing.

## §4 — Acceptance criteria

1. T1: both hash gates enforced fail-closed and demonstrated (a RED leg proving the abort on a byte-perturbed COPY outside `master/**` — the frozen file itself is never touched); the enumeration exactly the manifest's thirteen ids; zero corpus/instrument/production/seam bytes.
2. T2: all eight legs realized with per-leg predicates matching the frozen rows by reference; conjunction legs indivisible; shakedown output labeled NON-RECORD wherever it exists; ★ the RECORD run launches the published bound binaries with both production flags and its persisted frozen manifest carries the verbatim `BoundAtRef aea5f064…` + the independently recomputed covering `96e2cba9…` — a placeholder or hand-built binding on the record run is a FAILED criterion.
3. **The F2 law verbatim:** the Step-3 exit claim binds ALL EIGHT current legs — every required leg must PASS; a failed or HOLD leg keeps that gate OPEN; a ruled disposition belongs in the gap register and may route an explicit operator-visible amendment, but never satisfies the current gate. **The claim ceilings verbatim:** `xit-dur-1` proves the bounded frozen canonical record-CONTENT sequence matches — NEVER that the model resumed the same conversation; the `xit-edit` pair proves the DISPOSITION differs by record class — NO tamper-evidence claim, a `record_digest` mismatch is an integrity mismatch of UNKNOWN CAUSE, and no exit claim may assert frank labeled edited content untrusted.
4. ★ T5: the five named cases green WITH their RED controls witnessed; T3a's preflight acceptance returned BEFORE the record run; T3b's formal ruling returned BEFORE any E3/exit closure.
5. ★ T6: the five F65 §5 obligations realized to the instrument's letter, the conductor bytes m-7-review-approved before the close claims them; the strictness negatives and byte-shape battery green; the source-closure capture with the frank-mcp-absent proof filed.
6. NO exit claim, NO admissibility claim, NO binding-authorship claim by any s16 relay (★ CITING the completed binding of record verbatim is required truth, not a claim): m-3 rules admissibility; Master+VP author the RELEASE-BINDING, the exit packet, and the composite join (the app/provider E3 half PLUS m-7's F68 relay-leg E1/E2 half — an m-3 vertical E3 alone never satisfies the exit proof); Step-3 ratification is the operator's alone.
7. The regression floor green at EVERY commit (seam 64/0/64 · plain suite · vet); the draft PR refreshed (V30); findings UP never absorbed.
8. The WP3-close SITREP: the per-leg verdict table (all eight, row-by-row over the thirteen records), the E3 record inventory + custody statement, ★ the T3b formal-ruling status stated exactly (returned or awaited — the close cannot claim E3/exit closure without it), the gate-status honesty (open gates named as open), the not-done, every gap for WP5; then my corroboration at my own invocation and the close routes UP.

## §5 — Out of scope (carried whole)

The budget measurements (WP4, obligation v); the R-XIT-CONF five conformance cases (WP4, obligation vi — a DIFFERENT closed set, none an exit record); the gap register/battle report/close/MERGE-GATE (WP5); any "release bound"/"binding-ready"/"exit passed"/"E3 admissible" claim by the pair; the Q-CARRIER grammar (the binding act's); any corpus, oracle, instrument, seam, script, or m-x design byte; any production byte OUTSIDE the ★★ F65 rows (and inside them, outside the §5 obligations); any H-12 relaxation (the R-HARDLINK residual is REQUIRED before untrusted conditions and is not this slice's); any push beyond the slice branch + draft PR; merge (operator-only).

## §6 — Sequencing (the V32 law)

Your PLAN-REVIEW verdict TO me, parented to this relay, over this plan's exact hash (★ active-root lint at your seat per the adopted practice). On approve: I run the mechanical `SCOPE_DIFF` against §2 (the ★/★★ surfaces named) and route the TOKEN REQUEST UP — implementation begins ONLY on master's fresh direct dispatch TO `s16.implementer` carrying the literal token + the approved fence; ★ I file the T3a PREFLIGHT submission UP in the same act. T4 executes only when its three preconditions ALL stand (the binding of record is already cited; m-3's preflight acceptance arrives through master; the hash gate is the harness's own); ★ the close claims E3/exit closure only after T3b's post-capture formal ruling. Spec-base defects found while realizing are escalations UP to the owning PM, never local fixes.

ACTIONS_GIT_REF: engine-lane governance act only — this reissue drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; authoring verification was read-only (the reissue direction + binding co-sign chain read; the plan-r7 bytes verified at disk AND at main commit c45de9de both == 044fe4cc; the s16-plan origin/approve chain located with its DESIGN_DOC_ID; the F65 instrument r3 re-hashed d7ae2956 and its §§2–5 read; the cmd/frank module closure listed at the worktree; the draft active-root linted BEFORE filing); no source, test, script, branch, commit, push, PR, merge, build, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
