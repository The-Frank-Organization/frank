## SITREP — S16-WP3-F1 CORROBORATED AT THREE GRAINS at my own invocation (bytes · reproducer · floor) and ROUTED UP: the FROZEN edit-base journal substrate is payload-wrapped while the SHIPPED m-9 production reader requires the same members flattened top-level — three exit records (`xit-dur-2` + both `xit-edit` arms) are unexecutable BEFORE their faults; the disposition is m-9's design question with an RLBS-3 consequence fork (a corpus-side fix VOIDS the freeze and takes the bounded Master+VP re-freeze route); the pair HOLDS on T2, the green F65 checkpoint stands distinct

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16-wp3-f1
PARENT_DISPATCH_ID: s16-wp3-impl-finding-1
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — the plan-mandated finding routing; the disposition is the owning pair's through master (the freeze-void route, if taken, carries its own Master+VP act)
PLAN_LOCK_ID: s16-wp3-plan-2 @ sha256 735b324ad90ee72a3c62803fa58adb9a026e68ebd65ca5004b838f8e9e690ebd
BUNDLE_ID: s16-integration
IN_REPLY_TO: s16-wp3/SITREP-implementer-20260830-030636.md
FROM: s16.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16.implementer, m-9.planner, m-3.planner
SUBJECT: s16 WP3 finding routing — S16-WP3-F1 verified whole at my seat (frozen genesis nests run_id/run_manifest_digest/turn_epoch/create_auth_id under payload vs record.go:83 requiring them top-level, writer.go:179 emitting them top-level; the RED reproduces exactly; checkpoint 3122d523 green + fence-confined at my invocation); requesting master route the WHICH-SIDE-IS-RIGHT question to m-9 (m-3 visible: three predicates/locators unexecutable) — the worker journal design contract decides; a corpus-side correction is a freeze-VOID under RLBS-3's own law

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — the finding exposes no secret or authority byte
- migration/backfill/destructive-write/canonical-data-repair: no — everything read-only at both seats; no canonical, dist, corpus, or store byte moved
- money/inventory/orders/planning/accounting/trust-critical-state: yes — three trust-critical Step-3 exit records are blocked
- AI-or-automation-acts-downstream: yes — the affected rows feed the exit packet and the operator's ratification
- worker/scheduler/queue/retry/async-side-effect: yes — the mismatched reader is m-9's resume-journal production path
- cross-repo/service-contract/generated-schema/shared-API-event: yes — frozen corpus bytes vs the bound production reader contract
- user-visible-control-with-materializer/downstream-consumer: yes — m-3's evaluation cannot receive a valid observation from this substrate as it stands
- test-runtime-role-mismatch: no — the failure invokes the production `internal/worker/journal` reader directly over the exact frozen bytes, pre-fault
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — which side moves is an owner/design decision outside every s16 fence; nothing absorbed, nothing waived
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — The finding, corroborated at three grains at my own invocation

**At bytes (independent of the driver):** the frozen substrate `master/exit-fixtures/common/edit-base-journal.jsonl` (re-hashed exact `feb1bf6c…`; the corpus itself exact `d4580c52…`) opens with a PAYLOAD-WRAPPED genesis: `{"generation_id":"g-1","kind":"run_open","payload":{"create_auth_id":…,"run_id":"edit-r1","run_manifest_digest":…,"turn_epoch":"1"},…}`. The SHIPPED production reader (`frank/internal/worker/journal/record.go:83`) requires exactly those four members TOP-LEVEL for `run_open` and refuses at `:295` with `run_open lacks required member "run_id"`; the production WRITER (`writer.go:179`) emits `run_id` top-level — so the shipped read AND write sides agree with each other and disagree with the frozen authored substrate. This is a genuine frozen-carrier/production-contract mismatch, not a harness artifact.

**At the reproducer:** the implementer's retained driver (uncommitted, `frank/test/exit/frozen_journal_compatibility_test.go`) FAILS at my invocation with byte-identical output — `journal.Recover` ⇒ `{Disposition:degraded … FaultClass:genesis_fault GenesisFault:true Records:[]}`, decode cause `run_open lacks required member "run_id"` — BEFORE any fault injection. Consequence, per the frozen rows themselves: `xit-dur-2`'s anti-vacuity honest arm (the unmodified base must be `resumable` at marker seq 9) cannot go green, and both `xit-edit` arms bind the same authored substrate, so record class stops being their single independent variable. Three of the thirteen exit records are unexecutable as things stand; the record run cannot complete and does not start.

**At the floor:** the green checkpoint `3122d523…` == origin (verified); its 16-file set confined exactly to the fence (`cmd/frank/**` + `internal/config/**` + `internal/store/**` + new `test/exit/**`; zero seam/script/dist/master/other-production bytes); at my invocation the seam census is `GREEN=64 RED=0 TOTAL=64`, `go vet` PASS, and `./test/exit` is green EXCEPT the retained RED driver (proven by a skip-run); `cmd/frank` + `internal/store` + `internal/config` PASS; PR #2 draft at the exact head. The checkpoint carries the F65 realization but claims NO m-7 review — that chartered review remains owed before any close cites the conductor bytes, exactly as the implementer reported.

## §2 — The routed question (m-9 owns the contract; m-3 visible; the RLBS-3 fork stated)

**The question for `m-9.planner` (through master): which side matches the worker journal design contract of record (worker r12, incl. the §7.1a B10 journal-item carrier)** — (a) if the design pins the FLATTENED top-level shape the shipped reader/writer realize, then the frozen authored substrate was mis-authored and the correction is CORPUS-SIDE: under RLBS-3's own law ANY corpus edit VOIDS the freeze — the only path is STOP → a bounded Master+VP route → regeneration → a NEW freeze + re-lock (the r11 arc already anticipated exactly this class for `xit-dur-1`'s expected-prefix regeneration: fixtures author FRESH against the current genesis shape, no inheritance); (b) if the design pins the PAYLOAD-WRAPPED shape, the shipped reader diverges from its own contract and the correction is an m-9-owned PRODUCTION change under m-9's review (outside every s16 fence; reaching s16 only as a rebased basis through the standing chain). The pair takes NO position on which — the design bytes decide — and notes only the mechanical fact that the shipped writer and reader agree with each other. `m-3.planner` is visible because three predicates/locators become unexecutable until disposition (and the T3a preflight acceptance is UNTOUCHED: no §3 element changes; the record run simply waits).

## §3 — The pair's standing

T2 is HELD at the implementer's report and stays held; the RED driver stays deliberately uncommitted so the green checkpoint and the mismatch are never conflated; no local repair, translation, or corpus byte was or will be attempted at either seat (a harness translation would erase the exact mismatch being tested and violate the by-reference law). The rest of the granted build (the remaining leg drivers not bound to this substrate, the T5 cases, the F65 review-response work if m-7's review returns findings) can proceed in-grant unless master directs otherwise. Requested action: master routes §2 to `m-9.planner` (m-3 visible) and returns the disposition + any successor authority to the pair; the record run waits on it alongside its standing gates.

ACTIONS_GIT_REF: read-only corroboration + engine-lane governance act — re-hashed the corpus + substrate; read the frozen genesis line and the production reader/writer loci at bytes; ran the retained RED driver, the seam census, vet, the focused packages, and the skip-run at my own invocation; verified the checkpoint's head/upstream/file-set and PR #2; this relay drafted at .engine/drafts/s16.planner/ and submitted through the v2.9.1 client; no source, test, script, corpus, dist, branch, commit, push, PR, merge, or build-publication byte by this seat.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit from the repo root:)
 M frank/.relays/s16/INDEX.md
?? frank/.relays/s16/s16-wp3/SITREP-implementer-20260830-030636.md
