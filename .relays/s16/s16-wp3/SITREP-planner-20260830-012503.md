## SITREP — WP3 plan-2 APPROVED (review verified at my seat over exact `735b324a…`) + the ALL-IN SCOPE_DIFF + THE T3a PREFLIGHT PACKAGE, one act: requesting master's fresh direct implementation dispatch TO `s16.implementer` AND the routing of the preflight submission to `m-3.planner` (the record run starts only on m-3's preflight acceptance; the FORMAL ruling stays post-capture)

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16-wp3-token
PARENT_DISPATCH_ID: s16-wp3-plan-review-2
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the dispatch is master's under the commission's V32 chain and the preflight acceptance is m-3's owner act; the operator's gates remain the WP5 MERGE-GATE and the Step-3 ratification
IN_REPLY_TO: s16-wp3/PLAN-REVIEW-implementer-20260830-010856.md
FROM: s16.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16.implementer, m-3.planner, m-7.planner
SUBJECT: s16 WP3 token request — plan-2 @ 735b324a approved (review 010856 verified + linted at my seat; the reviewer's ledger-active reconstruction shows the operative carrier adds ZERO active-set findings); SCOPE_DIFF all-in over the §2 fence (exit harness + composed helpers + the three bounded F65 rows under m-7 chartered review); trigger-present bars a pair token — requesting the fresh direct implementation-token relay TO s16.implementer alone + master's routing of the §3 T3a preflight package to m-3.planner

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential, capability, custody, or authorization surface changes; evidence stays credential-free and observer-owned
- migration/backfill/destructive-write/canonical-data-repair: no — the frozen corpus and the bound dist are read-only; fault injection is run-scoped
- money/inventory/orders/planning/accounting/trust-critical-state: yes — F63/F65 identities and the E3/exit rows are trust-critical acceptance evidence
- AI-or-automation-acts-downstream: yes — the record run feeds the Master+VP exit packet and the operator's ratification
- worker/scheduler/queue/retry/async-side-effect: yes — the suite drives the composed app/worker/connector/broker/conductor path including process kills and duplicate delivery
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the plan joins the commissioned plan, the frozen oracle, the m-3 observation contract, the m-7 F65 instrument, the m-10 carrier, and the co-signed binding
- user-visible-control-with-materializer/downstream-consumer: yes — the immutable run records and leg rows are consumed by m-3 and the exit packet
- test-runtime-role-mismatch: no — the record run launches the published bound binaries with both production flags; no test-only substitute
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — V34 is the explicit self-retiring deviation; the inherited root findings stay immutable classified history; neither is waived or promoted
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — The approval, verified at my own seat

`s16-wp3/PLAN-REVIEW-implementer-20260830-010856.md` (DISPATCH_ID `s16-wp3-plan-review-2`, VERDICT: approve) — lint OK at my invocation; its `PLAN_LOCK_ID` matches my recompute of the operative carrier `s16-wp3/PLAN-planner-20260830-005514.md` = `735b324ad90ee72a3c62803fa58adb9a026e68ebd65ca5004b838f8e9e690ebd`. The review independently reproduced the whole basis (the seq 96→95→90 correction lineage; V34 at governance commit `1c983e5d…` with the Master ruling @ `d7c77d68…` and the VP concurrence @ `34255b93…` zero-findings; every governing hash — r7/RLBS-3/corpus/workload/carrier/F65-r3; the binding of record re-hashed including the 615-byte manifest and all four members; the bound worktree clean at `aea5f064…`, matching my own check this act) and closed all four R1 findings (MR-1 via T3a/T3b; MR-2 via T5; MR-3/MR-4 upstream and correctly consumed/encoded). Its honest lint posture is noted and adopted: the stock root sweep cannot finish over the immutable path-shaped predecessors; the reviewer's ledger-defined ACTIVE-SET reconstruction shows the operative carrier adds ZERO active-set findings and the nine inherited findings stay on the five classified historical carriers. Fresh E2 feasibility baselines were green at the reviewer's seat (the focused five-package battery; the `cmd/frank` closure with the `frank-mcp`-absent check). The approval grants NO implementation authority.

## §2 — The mechanical SCOPE_DIFF (comparand: plan-2 §2; result: ALL-IN)

IN rows, each present in the review's approved boundary: ★ `frank/test/exit/**` (new) · `frank/test/composed/**` (helper extension only) · ★★ the three bounded F65 rows — `frank/cmd/frank/**` + `frank/internal/store/**` + `frank/internal/config/**`, ONLY the instrument's §5-obligated bytes, EACH under m-7's chartered review · `frank/go.mod`/`frank/go.sum` arbitration-only · the relay root via the engine. OUT rows, each mirrored: every other production/cmd/internal surface · `frank/test/seam/**` · `frank/scripts/**` · `master/**` incl. the frozen corpus + instruments · `dist/**` read-only via the ruled path (the BOUND release never rebuilt or mutated in this lane). Zero rows outside the approved boundary; zero approved rows missing; any newly necessary OUT byte = STOP + bounded amendment + re-review. Both scans trigger-present ⇒ no pair token exists on any branch.

## §3 — The T3a PREFLIGHT package (for master's routing to `m-3.planner`; the owner accepts or reshapes BEFORE any record run)

Submitted for m-3's PRE-RUN acceptance per the two-act split (the FORMAL ruling stays post-capture over the exact immutable bytes — nothing here pre-approves a run):
1. **The recorder boundary:** a SEPARATE harness observer process authoring `m3.e3_observation.v2` records OUTSIDE the conductor store into an OBSERVER-OWNED output root the composed seats cannot write; the writer IS `observer_id=integration-harness` (the closed recorder registry); every row presented with recorder-boundary identity per §3.6 — no self-supplied E3, and a row recorded from seat-writable space is inadmissible regardless of content.
2. **The capture plan (the three points + the RED controls):** (a) the FROZEN REQUEST CORE at the connector's freeze→authorize→attach→send boundary, captured PRE-attach, credential-free, with machine 1 comparing an independently derived digest of the captured core; (b) the worker EFFECT DESCRIPTOR + the captured local invocation/env for machine 3's `env_digest` parity; (c) the COMMITTED relay record with the channel-stamped FROM, read from the conductor store AT the observer boundary, for machine 4. The T5 RED controls ride: seat-write denial · copied-digest refusal (the F84 fresh-derivation rail — every comparison fact freshly derived from captured bytes) · incomplete/gapped/undecodable capture ⇒ `unknown` + leg HOLD (machines 2/5 stay NON-GATING; no s16 claim promotes them) · per-member bound-identity mutation invalidation.
3. **The applicability inputs, as the record run will bind them:** the binding of record VERBATIM — covering `release_digest 96e2cba98f4847c3c6d7e0ae13192bf69d19e63523ddc3543c6ef8e282772a96` at `BoundAtRef aea5f064c7c0d80ac7c81649809ac66c0011382d`, members `frank-app 708a631e…` / `frank-broker 40bc6c85…` / `frank-connector 1f40bd17…` / `frank-worker 975c19d6…` — plus the run's manifest/policy/lane/catalog digests as frozen at launch, `trust_root_digest` (the declared injected-CA posture, r5's admissible shape, freeze-bound), and the F65 stamp fields at the tested conductor instance (equality-in-time; the strict-walk `config_generation` + `sha256:`-prefixed composite per the instrument, realized in-fence under m-7's chartered review before the run cites them).
4. **The per-leg evidence-locator plan:** the frozen manifest's own `evidence_locator` rows govern BY REFERENCE (the non-rederivation law); the observer's records inventory maps leg → locators → capture points; the run posture is loopback-only under H-12, honestly labeled evidence about the composed machinery under the operator-frozen test trust root, never production trust.
The ask: m-3's PREFLIGHT acceptance (or reshaping, which folds as a plan amendment through the pair). **The record run starts ONLY on that acceptance; E3/exit closure waits further on the post-capture FORMAL ruling.**

## §4 — The request

Requested of master, one act: **(a) the fresh direct relay under the active s16 root, addressed TO `s16.implementer` ALONE, carrying the literal implementation token + the exact approved plan-2 fence over `735b324a…`; (b) the §3 preflight package routed to `m-3.planner` for the pre-run acceptance.** Until the dispatch lands, no byte moves; the record run additionally waits on m-3's acceptance (and the close's E3/exit words wait on the post-capture ruling). After the dispatch: the harness + T5 cases + the F65 §5 realization build under the regression floor (the conductor bytes m-7-review-gated), shakedowns labeled NON-RECORD; then the record run on its gates; then the close + my corroboration route UP.

Next requested action: `master.orchestrator-planner` issues the direct implementation dispatch TO `s16.implementer` and routes §3 to `m-3.planner` (or returns a correction to this seat).

ACTIONS_GIT_REF: engine-lane governance act only — this request drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; verification this act was read-only (the review linted + its lock hash recomputed at my seat; the bound worktree re-checked clean at aea5f064); no source, test, script, branch, commit, push, PR, merge, build, dist, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit from the repo root:)
 M frank/.relays/s16/INDEX.md
?? frank/.relays/s16/s16-wp3/PLAN-REVIEW-implementer-20260830-010856.md
?? frank/.relays/s16/s16-wp3/PLAN-planner-20260830-005437.md
?? frank/.relays/s16/s16-wp3/PLAN-planner-20260830-005514.md
