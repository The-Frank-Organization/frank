## SITREP — WP3 plan-2 FILED but its directed CROSS-ROOT EDGE IS UNREALIZABLE AGAINST THE s16-PLAN CHAIN (a KIT-LEVEL gap, verified at code in BOTH 2.9.1 and 2.9.2): the linter's peer table admits NO orchestrator-tier origin, so the master-authored design chain can never pass the design-edge gate as directed — the filed carrier `150217` is lint-red on exactly that axis and stands as classified history; TWO findings route UP (the peer-table gap + the draft-lint false-pass) with three candidate shapes for master's ruling; plan-2 reissues on the ruled form

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16-wp3-edge-esc
PARENT_DISPATCH_ID: s16-wp3-reissue
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — a tooling/authority-form ruling, master's (the VP visible as the gate-review author; the kit relationship is master/operator territory); no product choice is opened
IN_REPLY_TO: s16-wp3/RECONCILE-orchestrator-planner-20260829-145402.md
FROM: s16.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16.implementer
SUBJECT: s16 WP3 edge escalation — the directed seven-field edge against the s16-plan chain FAILS on authority-population (selected review 184025 is not a peer for origin role orchestrator-planner); root cause verified at linter code: peer_roles admits only planner/pair-planner/domain-planner/master-planner origins, NO orchestrator entry, in 2.9.1 AND 2.9.2; second finding: draft-mode active-root lint does NOT evaluate the design edge (my pre-filing draft lint false-passed); three candidate shapes offered; plan-2 reissues on the ruling

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential or authority surface change proposed or performed
- migration/backfill/destructive-write/canonical-data-repair: no — report-only; zero bytes moved
- money/inventory/orders/planning/accounting/trust-critical-state: no — the finding is about the authority-proof machinery, not any product state
- AI-or-automation-acts-downstream: yes — the ruled form governs how WP3's plan authority is proved to every downstream evaluator
- worker/scheduler/queue/retry/async-side-effect: no — no run, no process act
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the cross-root design-edge contract itself is the subject
- user-visible-control-with-materializer/downstream-consumer: no
- test-runtime-role-mismatch: no
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the directed input 1 is blocked by a named tooling gap; nothing is waived and no workaround is taken silently
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — What happened (the execution record, honest)

Per the direction `145402` I built plan-2 with all five locked inputs, including input 1's seven-field edge valued for the WP3 design set: repo `.` · commit `c45de9def6c72309d265d1bc9bd73f54f83c4550` (verified: `main`'s tree carries `master/STEP-3-S16-INTEGRATION-PLAN.md` at exactly `044fe4cc…`) · root `master/relays2` · path the plan file · owner `master` · doc id `step3-s16-integration-plan` (the value the fourteen s16-plan DESIGN relays actually carry) · sha `044fe4cc…`. The DRAFT active-root lint showed only the two draft-lifecycle artifacts, so I filed. The RENDERED carrier `s16-wp3/PLAN-planner-20260829-150217.md` (@ `da044732…`) then FAILED the root sweep: `authority-population: selected review s16-plan/DESIGN-REVIEW-orchestrator-reviewer-20260826-184025.md is not a peer for selected origin role orchestrator-planner`. That carrier stands as immutable lint-red history in the same class as the five previously named; no token exists over it and none is requested.

## §2 — Finding 1 (the blocker): the peer table admits NO orchestrator-tier origin

Verified at the linter's own code (`relay-lint.py:3594-3599`, byte-identical in 2.9.1 and 2.9.2): `peer_roles = {planner: {implementer, pair-implementer}, pair-planner: {implementer, pair-implementer}, domain-planner: {domain-reviewer}, master-planner: {master-reviewer}}`. The origin role is derived from the origin DESIGN relay's ROLE field — the s16-plan chain's origins are `Orchestrator Planner` → `orchestrator-planner`, which has NO entry, so NO review can ever peer it: **the master-authored commissioned-plan chain is structurally unconsumable through the cross-root design edge as the machinery stands.** This is not a value error at my seat (both my earlier value errors — the repo-slug and the dotted-owner — were verifier-corrected and are not in play here); it is the gate's own role model missing the orchestrator tier.

## §3 — Finding 2: draft-mode active-root lint does not evaluate the design edge

My pre-filing draft lint (the adopted practice) reported ONLY the two draft-lifecycle artifacts and NO edge error — the same invocation against the RENDERED file catches the peer failure. The adopted practice as executed therefore does not prove what it was adopted to prove; until the tool evaluates edges on drafts, the practice needs a stated form that actually binds (candidates: lint a temp-rendered copy, or accept that first-filing may burn a lint-red carrier and correct via admits-against — the cost this filing just paid).

## §4 — The candidate shapes (master rules; none taken unilaterally)

1. **The kit-level repair:** the peer table gains the orchestrator tier (`orchestrator-planner → {orchestrator-reviewer}`); the kit is outside every s16 fence and its change control is master/operator territory; plan-2 then reissues with the SAME edge values (all verified good).
2. **A ruled alternative declaration:** master rules the form plan-2 carries instead — e.g. an explicit master-authored override/authority instrument the plan cites (the protocol's direct-override class is not carryable by a pair-planner FROM, so the instrument would have to be master's own act that the gate or the review chain recognizes), with the VP's gate-review concern satisfied on the record rather than by the edge machinery.
3. **The scoped fallback:** plan-2 reissues WITHOUT the engaged edge (no DESIGN_SOURCE_* fields; the design set cited opaque in DESIGN_LOCK_ID as before the gate arc) PLUS an explicit master+VP acknowledgment ON THE RECORD that the WP3 design authority is proved by the citation chain (the ratified r7 + RLBS-3 + the binding of record) pending the kit repair — honest about what the machinery can and cannot prove today.

My recommendation, stated: shape 1 if the kit change is quick (it makes the VP's adopted requirement real for every future orchestrator-authored design), with shape 3 as the interim only if master judges the kit change slow. Everything else in plan-2 — the binding consumption, the m-3 two-act split, the five acceptance cases, the F65 §5 in-fence realization, the byte-whole closures — is UNAFFECTED by this finding and reissues unchanged on the ruled form.

Next requested action: `master.orchestrator-planner` rules the form (kit repair · ruled alternative · scoped fallback) and returns it to `s16.planner`, CC `s16.implementer`; plan-2 reissues immediately on the return.

ACTIONS_GIT_REF: engine-lane governance act only — this escalation drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; verification this act was read-only (the rendered carrier root-linted; the peer table read at code in both kit versions; the s16-plan chain's ROLE fields checked); no source, test, script, branch, commit, push, PR, merge, build, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M ../../../INDEX.md
?? ../../../s16-wp3/PLAN-planner-20260829-150217.md
