## SITREP — WP3 plan-1 MUST-REVISE accepted WHOLE (all four findings verified at my seat); the TWO owner/master questions route UP NOW before any reissue: R1-MR-3 the production release-carrier seam (the placeholder at `main.go:141-150` has no input for a completed binding — the WP2 row-8/Q-CARRIER gap, now BLOCKING) and R1-MR-4 the four-member F63 set vs the CLOSED three-XOR-covering `BuildDigests`/E3-v2 vector (broker has no lawful slot); R1-MR-1/R1-MR-2 are accepted and held ready for plan-2, which reissues only on the returned rulings

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s16-wp3-carrier-esc
PARENT_DISPATCH_ID: s16-wp3-plan-review-1
RUN_ID: s16
CEREMONY_TIER: production-risk
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no — the routed questions are master's + the owners' (m-3/m-10 primary; m-8/m-9 visible as F63 member owners), composing with the already-assembling RELEASE-BINDING/Q-CARRIER act; no operator product choice is opened
IN_REPLY_TO: s16-wp3/PLAN-REVIEW-implementer-20260828-174123.md
FROM: s16.planner
TO: master.orchestrator-planner
CC: master.orchestrator-reviewer, operator, s16.implementer, m-3.planner, m-10.planner, m-9.planner, m-8.planner
SUBJECT: s16 WP3 carrier escalation — plan-1 must-revise verified whole; routing UP: (1) the production carrier for the bound release (placeholder releaseDigest sha256("frank-mvp-development") + BoundAtRef "working-tree" at main.go:141-150, no CLI/config seam; a test-only injection cannot prove the bound composed runtime) and (2) the F63 four-member set vs the closed BuildDigests {app_main, m9_worker, m8} XOR release_digest with NO broker member; both belong to the binding-act assembly already in motion; plan-2 folds R1-MR-1/2 and reissues on the rulings

ESCALATION_SCAN:
- authz/tenant/RLS/permissions/secrets: no — no credential or authority surface change is proposed or performed
- migration/backfill/destructive-write/canonical-data-repair: no — report-only; zero bytes moved
- money/inventory/orders/planning/accounting/trust-critical-state: yes — the release carrier and the E3 applicability vector are trust-critical acceptance surfaces
- AI-or-automation-acts-downstream: yes — the rulings shape the exit packet's evidence
- worker/scheduler/queue/retry/async-side-effect: no — no run, no process act in this relay
- cross-repo/service-contract/generated-schema/shared-API-event: yes — the collision spans m-3's closed E3 v2 vector, m-10's manifest grammar, and the ratified F63 member set
- user-visible-control-with-materializer/downstream-consumer: yes — the binding act consumes whatever form is ruled
- test-runtime-role-mismatch: yes — the finding itself: under the zero-production-byte fence no production path consumes the bound release, so a test-only manifest would not prove the claimed bound composed runtime
- broad-scope-expansion/ambiguous-product-semantics/residual-risk/live-verify-skip: yes — the two routed seams are open owner judgments; nothing is waived
ESCALATION_SCAN_RESULT: trigger-present
PRE_SCAN_PRESSURE: none
OPERATOR_WAIVER:
- status: none
- scan_presented_to_operator: no — no downgrade requested
- operator_reply_after_scan: none
- valid_waiver: no
WAIVED_RISK_ACCEPTANCE: none

## §1 — The review state (accepted whole; every finding verified at my own seat)

`s16-wp3/PLAN-REVIEW-implementer-20260828-174123.md` (must-revise over plan-1 @ `b976244e…`). My verification: **R1-MR-1** — m-3's own bytes state the temporal fact twice: "the FORMAL ruling renders at WP3 against the live run and its captures — this confirm discharges the PRECONDITION check only and pre-approves no run" (`s16-t7-tls-confirm/…204725`) and "nothing here pre-approves a run that has not happened" (`s16-t7-tls-inst/…154918`). Plan-1's T3/T4 had the formal ruling as a PRE-run gate — wrong order; the required split (pre-run owner PREFLIGHT acceptance of the concrete design/recorder boundary/capture plan/applicability inputs; post-capture FORMAL ruling over the exact immutable bytes; no E3/exit closure before it) is accepted. **R1-MR-2** — m-3's readiness return `s16-pm-readiness/SITREP-planner-20260826-203227.md` §2 enumerates the five preconditions verbatim (the three observer-reachable capture points: pre-attach frozen request core · worker effect descriptor + captured invocation/env · committed stamped relay at the observer boundary; the observer-owned output root `observer_id=integration-harness` the composed seats cannot write; the F84 fresh-derivation rail; unknown-HOLDS on incomplete capture with machines 2/5 non-gating; the bound-identity manifest with mutation invalidation). Plan-1 carried the observer shape but NOT these as executable acceptance cases — accepted; plan-2 makes them five NAMED cases with the RED controls (seat-write denial · copied-digest refusal · incomplete-capture unknown/HOLD · per-member bound-identity mutation). **R1-MR-3 and R1-MR-4 verified at bytes below and ROUTED — they are owner/master decisions, not foldable at this seat.**

## §2 — Routed question 1 (R1-MR-3): the production release-carrier seam

Verified at the banked head `4e179fc3…`: `frank/cmd/frank-app/main.go:141` hardcodes `releaseDigest := sha256Hex([]byte("frank-mvp-development"))` and `:150` writes `ReleaseBinding{BoundAtRef: "working-tree", ReleaseDigest: &releaseDigest}`; the flag surface (`main.go:44`) carries NO input for a completed binding. Consequence: under plan-1's zero-production-byte fence there is NO production path by which the composed app consumes the bound release identity — a test-only manifest injection would be exactly the test-runtime-role-mismatch the review names, and the "bound composed runtime" claim would be untrue. This is the WP2 row-8 carrier question (routed 2026-08-28 with the evidence package) surfacing as a BLOCKER. **Requested ruling:** either (a) the Q-CARRIER resolution at the binding act names an existing/ruled production consumption seam, or (b) master routes an owner-approved BOUNDED production realization (the carrier change at `cmd/frank-app` under m-10's grammar) as a SEPARATELY-FENCED predecessor act with its own review chain — the pair executes only under that instrument; plan-2 carries whichever form is ruled. A test-only substitute is not requested and would be refused at review.

## §3 — Routed question 2 (R1-MR-4): the F63 four-member set vs the closed applicability vector

Verified at bytes: `frank/internal/appctl/manifest/manifest.go:49-53` — `BuildDigests` is CLOSED at three members `{app_main_build_digest, m9_worker_build_digest, m8_build_digest}`, XOR the covering `release_digest` (`ReleaseBinding`, `:55-59`); m-3's closed `m3.e3_observation.v2` matrix admits the same forms. The ratified F63 canonical set is FOUR members `{frank-app, frank-worker, frank-connector, frank-broker}` (the WP2b RELEASE-MANIFEST carries all four digests). A broker digest has NO lawful slot in the closed form; the owner proposal called the covering form later-release; the pair can neither invent an encoding nor leave the broker outside applicability. **Requested ruling (master with m-3 + m-10; m-8/m-9 visible):** ONE owner-authorized closed form that mechanically binds EVERY ratified F63 member and is consumable by BOTH the production app and m-3's §3.4 evaluator — whether that is the covering `release_digest` computed over the canonical four-member RELEASE-MANIFEST identity (which the binding act already holds), a schema/grammar amendment adding the broker member, or another owner shape. A schema or production-contract change is a PREDECESSOR act through its owners, never exit-test discretion. This composes with the binding-act assembly already in motion (the m-7 F65 approve outstanding; Q-CARRIER directed with m-10).

## §4 — Sequencing

On the two returned rulings: plan-2 reissues uniquely parented to `s16-wp3-plan-review-1`, folding R1-MR-1 (the two-act m-3 split: preflight before execution, formal ruling post-capture over the exact bytes; the record run gated on RELEASE-BINDING + owner preflight + the runtime hash gate; no E3/exit closure before the post-capture ruling) and R1-MR-2 (the five named acceptance cases + RED controls) and carrying the ruled carrier + vector forms as LOCKED inputs; then the implementer's exact-hash review; on approve the SCOPE_DIFF + token request per V32. Every plan-1 closure the review names as carrying (the frozen-suite/hash/fence direction, the thirteen-id inventory, F2, the claim ceilings, HOLD/unknown non-closure, H-12, the dist path rule) is preserved whole. No byte of any kind moves meanwhile.

Next requested action: `master.orchestrator-planner` returns the two rulings (or folds them into the binding-act direction) to `s16.planner`, CC `s16.implementer` + the owners.

ACTIONS_GIT_REF: engine-lane governance act only — this escalation drafted at .engine/drafts/s16.planner/ and submitted through the daemon-matching v2.9.1 client; verification this act was read-only (the review + the two m-3 relays + the readiness return read in full; main.go:136-152 + manifest.go:45-59 read at the banked head; the flag surface checked); no source, test, script, branch, commit, push, PR, merge, build, binding, or code byte.
FINAL_GIT_STATUS_SHORT: (literal, captured immediately pre-submit:)
 M frank/.relays/s16/INDEX.md
?? frank/.relays/s16/s16-wp2b/SITREP-implementer-20260828-144120.md
?? frank/.relays/s16/s16-wp3/PLAN-REVIEW-implementer-20260828-174123.md
?? frank/.relays/s16/s16-wp3/PLAN-planner-20260828-144331.md
?? frank/.relays/s16/s16-wp3/PLAN-planner-20260828-144354.md
