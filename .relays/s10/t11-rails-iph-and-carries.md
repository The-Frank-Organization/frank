# s10 T11 Rails, I-PH, and explicit carries

## Rails A/B

| Surface | Rail A | Rail B |
|---|---|---|
| v8 interpreter-bearing kinds | CLOSED: old readers refuse marker-first | pass: owner-governed capability/successor remains |
| ODB choice capture | CLOSED: only the frozen choice set validates | pass: operator-FROM and one-shot checks remain |
| park/wake FSM | CLOSED: accepted A only; stale observation blocks wake | pass: durable derivation and crash replay remain |
| resummon | CLOSED: same cadence slot dedupes; summon never becomes verdict | pass: A-2 content-hash machinery remains |
| long-check expiry | CLOSED: no silent soft-expiry disposition | pass: absolute hard ceiling can only conservatively kill/block |
| side-effect approval | CLOSED: absent live operator allow is typed default-deny | pass: executor class/timeout refusals remain byte-untouched |

## I-PH census

New seat-visible families are the ODB headers/choices, park and resummon records, expiry/approval gates, and the validated wake record. Their payloads contain symbolic IDs, bounded enum choices, durations, and content hashes; no canonical store/config/outbox/socket path is rendered. The exhaustive `TestLawPathHygiene` census plus the s5/s6 path-family suites remain the executable guard. T11 runs them again in the final battery.

## Sunset boundary and registered carry

The live-prompt approval channel is proven to the gate; EXECUTION of side-effecting classes remains fail-closed refused at the executor — a future design round when a real side-effecting operation exists.

That execution path is intentionally outside s10 and the slice-exit gate. The s11 parked-across-schema freeze/re-issue branch, away bridge, E3/E4, and full bucket/TUI surfaces also remain unbuilt as the plan requires.
