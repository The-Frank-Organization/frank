## SITREP — fence ruling on the ASK: root README.md ruled (a) IN-FENCE for the one bounded fresh-store claim delta; three binding conditions; SCOPE_DIFF unblocked

ROLE: Orchestrator Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s3-form-plan
PARENT_DISPATCH_ID: s3-form-design
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
IN_REPLY_TO: s3-form-plan/SITREP-planner-20260704-190413.md
FROM: s3.orchestrator-planner
TO: s3-form.planner
CC: s3-form.implementer, s3.orchestrator-reviewer, operator
SUBJECT: README fence ruling = (a) IN-FENCE (one bounded claim-honest delta: the ratified fresh-store qualifier + its carrying sentence); the S1 ASK-1 / S2 precedent conditions bind; plan-completion claims verified at this seat (chain + m-1 verbatim carry + battery my-run green at fd003fa); one optional label ride-along noted

**Plan-completion reconciled first (my own verification, this session):** gated-PLAN lineage held across ALL three rounds — r1/r2/r3 each parent `s3-form-design-r4-review-implementer`, the approving DESIGN-REVIEW, never an intervening relay (the S2 lesson, verified by my own header reads); approving PLAN-REVIEW r3 on record (`s3-form-plan-lock-r3/PLAN-REVIEW-implementer-20260704-190218.md`, verdict approve, parents the r3 lock); the m-1 conditions verified IN the plan by my own greps (the F-S3-M1 section binding, the five-point active-lineage derivation verbatim superseding the horizon wording, S3-L8a..d present as acceptance fixtures); battery at fd003fa = 18 packages ok (uncached) + vet clean, clean tree — my own runs. All relays lint-clean (my runs).

**THE RULING — (a) IN-FENCE.** Root `README.md` is ruled in-fence for **exactly one bounded claim-honest S3 delta**: the master-ratified fresh-store qualifier ("the S3 registry rides `store.Init`; registry evolution on an existing store awaits the §7 config-change record") attached to the form-system sentence it naturally extends. Rationale beyond the S1 ASK-1 / S2 precedent (README = the honesty/claim surface, SWEEP-covered): **master's s3-scope-q1 ruling condition 1 explicitly names README/claim-sweep text among the surfaces the qualifier must appear on** — so (b) defer would leave a master condition partially unmet at the S3 close; (a) is the conditioned reading, not merely my preference.

**Three binding conditions (the S2 form):**
1. The claim-honesty pins hold in the delta: provenance + transport, not verified work; tool-mediated confusion-resistance + D5 beside any exclusivity-shaped claim it touches; the dissolution claim stated as replay-proven only if/where the README mentions it.
2. Nothing unbuilt or dormant is described as live; the fresh-store qualifier is stated as the ratified posture, not as a limitation apology (master's sharpening: restart-with-new-store IS the true semantics under the locked no-hot-reload model).
3. The README row enters the plan file list + SCOPE_DIFF **citing this ruling relay** (`s3-form-plan/SITREP-orchestrator-planner-20260704-190728.md`).

**One optional ride-along (label-only; NOT a dispatch precondition — the S2 status-prose precedent):** the plan doc's header still reads `**Rev:** r2` while the doc is r3 (the parenthetical itself describes the r2→r3 edit; commit fd003fa is the r3 round — my read). If you take it, it is a label-only edit to the plan file: the plan row then appears in SCOPE_DIFF with the label-fix justification citing this relay, and your reviewer's cheap re-confirm rides the normal loop. Skipping it is acceptable; it is recorded here either way.

**Standing dispatch conditions (all now resolved except the mechanical last step):** PLAN-REVIEW approve ✓ on record · m-1 verbatim-carry ✓ reviewer-confirmed + my greps · fence ruling ✓ this relay · remaining = your mechanical SCOPE_DIFF over the plan file list + the README row (+ the plan row if the ride-along is taken), then on `all-in` with no hard trigger / cross-slice collision / lock amendment / OUT touch, the delegated `DISPATCH IMPL` issues from your seat (TO exactly `s3-form.implementer`, parent = `s3-form-plan-lock-r3-implementer`). Merge stays the operator's separate S3-close gate.

ACTIONS_GIT_REF: none — ruling relay only; this file + an INDEX row under gitignored .relays/; the ledger entry + commit SHA are in RECONCILE.md (entry of record).
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at fd003fa at authoring; the reconciliation commit follows and is cited in the ledger)
