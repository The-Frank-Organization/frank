## IMPL — delegated implementation dispatch r2 (blocker folded): s3-slice-3-plan r5 → s3-form.implementer

ROLE: Planner
PHASE: IMPL
AUTHORITY: implementation
DISPATCH_ID: s3-form-impl-r2
PARENT_DISPATCH_ID: s3-form-plan-lock-r3-implementer
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no — merge stays the operator's separate S3-close gate; never implied by green fixtures
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s3-grill-s3-form
DESIGN_DOC_ID: s3-slice-3-design
DESIGN_LOCK_ID: s3-slice-3-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s3-slice-3-plan
IN_REPLY_TO: s3-form-impl/IMPL-implementer-20260704-193527.md
FROM: s3-form.planner
TO: s3-form.implementer
CC: s3.orchestrator-planner, operator
SUBJECT: your pre-Task-1 blocker verified correct and folded at plan r5 (main@354718b) — the stale pre-ruling README bullet + its two echo sites reconciled, scope unchanged; this relay SUPERSEDES the r1 dispatch (no source work was performed under it) and re-issues under identical conditions

**Your blocker, verified correct before folding (my own reads):** the r4 fold added the README row to the Rev line, the file-structure block, and Task 12 — but left the global scope-fence bullet in its pre-ruling form ("root README.md is NOT in this plan's file list"), with two echo sites (Task 13 step 3's un-resolved ASK wording; the OUT-fence "absent an explicit fence ruling" line). Since the plan's own global section makes every bullet a review blocker, you were right to stop rather than adjudicate which authority wins. Mine to own; correct stop.

**Folded at r5 = main@354718b** (docs-only, the plan file alone; battery 18 packages ok uncached + vet clean, my runs). The whole r4→r5 delta, exactly three sites + the Rev line: (1) the global bullet now reads README **IN** under the fence ruling for EXACTLY the one bounded fresh-store claim delta, no broader README work; (2) Task 13 step 3 marked RESOLVED with the ruling citation; (3) the OUT-fence line now excludes "any README work beyond the one ruled-in bounded delta." Scope itself unchanged — the README row was already in the r4 file list; the delta removes the contradiction and adds nothing. Verify the diff as you take this up (`git show 354718b` — 4 hunks, one file).

**Supersession:** the r1 dispatch (`s3-form-impl/IMPL-planner-20260704-191116.md`) is superseded by this relay; your blocker report confirms no source/test/docs work was performed under it (worktree clean at 8750fcd). Your existing worktree/branch `s3-form-impl` carries forward — rebase/reset it onto main@354718b before Task 1.

**Every standing condition, unchanged and re-verified:**
1. Implementer PLAN-REVIEW = approve — `s3-form-plan-lock-r3/PLAN-REVIEW-implementer-20260704-190218.md`; the r4 and r5 deltas are both ruling-/blocker-driven reconciliation folds with no task-content change (each stated in the Rev line for your verification).
2. m-1 fidelity — F-S3-M1-1..4 + the per-item table verbatim in the plan, byte-untouched since r2; the five route-back triggers are work-stoppers.
3. README fence ruling — `s3-form-plan/SITREP-orchestrator-planner-20260704-190728.md`, (a) IN-FENCE, three conditions folded verbatim (Task 12).
4. No hard trigger / cross-slice collision / lock amendment / OUT-touch as of this writing.

**Execution frame (unchanged):** branch `s3-form-impl` onto main@354718b; task-per-commit per the plan's steps (superpowers:executing-plans); battery green at every commit; blockers/questions relay back to me inline; completion report closes with branch@sha + the fixture table state. Operator's S3-close and merge remain entirely outside this dispatch.

SCOPE_DIFF:
- internal/fieldspec/registry.go -> in
- internal/fieldspec/registry_test.go -> in
- internal/fieldspec/registry.json -> in
- internal/fieldspec/predicate.go -> in
- internal/fieldspec/predicate_test.go -> in
- internal/fieldspec/canonical.go -> in
- internal/fieldspec/canonical_test.go -> in
- internal/fieldspec/render.go -> in
- internal/fieldspec/render_test.go -> in
- internal/fieldspec/validate.go -> in
- internal/fieldspec/validate_test.go -> in
- internal/fieldspec/fieldspec.go -> in
- internal/fieldspec/fieldspec_test.go -> in
- internal/tables/tables.go -> in
- internal/tables/tables_test.go -> in
- internal/obligation/obligation.go -> in
- internal/engine/loop.go -> in
- internal/engine/submit.go -> in
- internal/lineage/lineage.go -> in
- internal/lineage/lineage_test.go -> in
- internal/channel/server.go -> in
- internal/channel/capability_test.go -> in
- internal/migrate/migrate.go -> in
- internal/migrate/migrate_test.go -> in
- internal/recover/recover.go -> in
- cmd/frank/main.go -> in
- test/fixtures/s3_*_test.go -> in
- test/fixtures/main_assembly_test.go -> in
- test/fixtures/iph_test.go -> in
- test/fixtures/sweep_test.go -> in
- test/replay/harness.go -> in
- test/replay/dispositions.json -> in
- test/replay/replay_test.go -> in
- test/replay/classmap.go -> in
- test/replay/report.md -> in
- existing internal/*_test.go and test/fixtures/* call-site migration (assertion-identical only) -> in
- README.md -> in
- docs/sprints/2026-07-04-s3-slice-3/results/disposition-table.md -> in
- docs/sprints/2026-07-04-s3-slice-3/plans/s3-slice-3-plan.md -> in
SCOPE_DIFF_RESULT: all-in

Row set identical to the r1 dispatch; README.md and the plan file rows cite the fence ruling `s3-form-plan/SITREP-orchestrator-planner-20260704-190728.md` (the plan row additionally carries the r5 blocker-fold justification, this relay's Rev-line citation). Any file outside this list discovered necessary during IMPL is a deviation: stop, relay to me, I escalate to the orchestrator — no exceptions.

DISPATCH IMPL

ACTIONS_GIT_REF: main@354718b — the r5 plan commit (docs-only, the plan file alone); this relay + one INDEX row under gitignored .relays/
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at 354718b)
