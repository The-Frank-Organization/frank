## SITREP — Task 11 adjudication: NO narrowing (the FULL replay is the charter gate centerpiece — not this seat's to shrink); CONTINUE under the standing r2 dispatch; the classification source you asked for is the locked oracle; one scope-row regularization

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s3-form-impl-r2-task11
PARENT_DISPATCH_ID: s3-form-impl-r2
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s3-grill-s3-form
DESIGN_DOC_ID: s3-slice-3-design
DESIGN_LOCK_ID: s3-slice-3-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s3-slice-3-plan
IN_REPLY_TO: s3-form-impl-r2/IMPL-implementer-20260704-202056.md
FROM: s3-form.planner
TO: s3-form.implementer
CC: s3.orchestrator-planner, operator
SUBJECT: adjudication = your option (b), continue Task 11 on the existing branch under the standing r2 dispatch (no new token needed — Task 11 is inside its scope); the per-fixture expected-verdict source is check-relay-lint-fixtures.py's EXPECTED list, already named in the locked plan; fold engine_test.go into the enumerated lineage_test.go row

**Adjudication of your two options — (b), with no plan fold.** Option (a) — narrowing "FULL 243-fixture replay, both legs, zero uncovered" to the current representative artifact — is a narrowing of the S3 exit gate's CENTERPIECE (the master s3-dispatch line, [VP-W]-watched: "all 243 corpus fixtures caught-or-genuinely-obsolete ... no silent drops"). That is charter scope; neither pair seat can shrink it, and I will not route it upward either, because nothing blocks (b): the locked Task 11 already specifies everything you asked for.

**The classification source you requested exists and is named in the locked plan (Task 11, verbatim):** the oracle of record = `check-relay-lint-fixtures.py`'s `EXPECTED` list at the frozen upstream path — **146 entries** `(mode ∈ {file, root}, fixture-path, expected-exit)`: 68 exact-file + 78 relay-root entries; **96 expected-FAIL / 50 expected-OK**. That IS the per-fixture pass/fail mapping. (E2 on record from three independent seats this sprint: the checker runs 146/146 PASS against the frozen corpus.)

**What "continue Task 11" means, restated from the locked plan text — no new content, no discretion added:**
1. **The name-heuristic classifier DIES:** delete `test/replay/classmap.go` + the stale `test/replay/report.md`; the `uncovered-S3` disposition value and every assertion that expects it (your two cited `replay_test.go` asserts included) are removed, not relabeled.
2. **Build `test/replay/harness.go`:** for each of the 146 oracle entries, a typed-equivalent constructor drives the REAL path — `Render` → submit → registry validate → lineage — on a fresh test store; root-mode entries build their graphs by sequential submits, then the offending record submits last. Never a markdown submit path (§8 strictness; the corpus is read-only input).
3. **Fail side (96 entries):** each lands `caught` (the bounce class matches its `dispositions.json` row) or `genuinely-obsolete` (ground ∈ the closed vocabulary: the vanished hand-authored-markdown channel classes + §8 strict form-only submit + the one-channel ROLE/FROM stamp). **Zero uncovered — the bucket does not exist.** Your two named examples land here concretely: `scope-diff-row-array`-class fixtures ⇒ typed SCOPE_DIFF row_array violations via the canonical carrier (caught — dissolved-form); `fold/FD1-fold-edit-no-foldscope` ⇒ the REVIEW-FOLD FOLD_SCOPE required-when rule (caught — dissolved-form; the §10b :946-985 class the registry expresses).
4. **Pass side (50 entries):** the typed equivalent is ACCEPTED — the non-overblocking leg, asserted per entry.
5. **Observe-context rows** (the FINAL/ACTIONS-substance class): evaluated under the reconstructed `layer_present:observe = true` context; labeled EXACTLY "caught — surviving §10b rule, fires under the reconstructed `layer_present:observe` context"; excluded from the live-Step-1 tally by the generator's separate counters (plan global constraint — structural, not prose).
6. **`dispositions.json` completes code-first:** one row per relay-lint.py assertion anchor (the :840-848/:850-868/:870-873 addressed-token rows explicit); the generator asserts table-matches-inventory + report-matches-generated; `results/disposition-table.md` regenerates from it (S3-D1/D2); S3-D5's obsolete-ground vocabulary scan closes it.
Acceptance = S3-D1..D5 exactly as locked. The standing r2 dispatch (`IMPL-planner-20260704-193915.md`) already covers all of this — Task 11 is inside its SCOPE_DIFF; the token there remains the live authority; no reissue.

**One scope-row regularization (your final diff, my audit):** `internal/lineage/engine_test.go` is a NEW file not enumerated by any SCOPE_DIFF row (the row set names `internal/lineage/lineage_test.go`). The content is in-scope Task-7 testing in an in-scope package — the file NAME is the only mismatch. Fold its tests into `internal/lineage/lineage_test.go` (or a `_test.go` merge that leaves only enumerated files in `git diff --name-only main..HEAD`) as part of the continuation — keeping the final diff exactly inside the row set, no orchestrator round-trip needed. Noted and appreciated: your self-caught transient `owed.go` touch, restored before the report — correct handling; and the honest refusal to claim Task 11 green — exactly the claim discipline this gate exists for.

Also carried to the review panel (not blockers now): the plan's `test/fixtures/s3_*_test.go` naming was not used (S3-* fixtures apparently live inside existing enumerated files — acceptable against the row set; the panel checks fixture-id coverage against D-12 regardless).

Next requested action: continue on `s3-form-impl` from b4a1764 per the above; completion report closes with branch@sha + the full fixture table incl. S3-D1..D5 green and the classmap/report deletion in the diff.

ACTIONS_GIT_REF: none — adjudication/direction relay only; no tracked-file edit (this relay + one INDEX row under gitignored .relays/)
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ main at 354718b)
