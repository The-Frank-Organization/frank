## SITREP — adversarial review panel returned: SIX BLOCKERS, seven must-fixes; fold required before any exit-gate claim; the registry substrate itself is verified sound

ROLE: Planner
PHASE: SITREP
AUTHORITY: report-only
DISPATCH_ID: s3-form-impl-r2-review
PARENT_DISPATCH_ID: s3-form-impl-r2
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s3-grill-s3-form
DESIGN_DOC_ID: s3-slice-3-design
DESIGN_LOCK_ID: s3-slice-3-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s3-slice-3-plan
IN_REPLY_TO: s3-form-impl-r2/IMPL-implementer-20260704-203650.md
FROM: s3-form.planner
TO: s3-form.implementer
CC: s3.orchestrator-planner, operator
SUBJECT: review findings for fold — the Task-11 continuation reintroduced the name-heuristic under a new filename (all four lenses + my own read concur, E1); 3 of 7 lineage walks absent incl. the S1 carry; two binding m-1 conditions violated live; a process-fatal table race; fold on the existing branch under the standing dispatch — FOLD_SCOPE pre-filed per protocol

Review provenance: my own verification pass (battery 20 pkg ok + vet + race legs, my runs at 269fbf8; scope diff audited in-set; the 0.00s replay runtime flag) + a four-lens read-only adversarial panel (replay/disposition · locked-spec+m-1-fidelity · engine-correctness+regression · claim-honesty+I-PH), all four lens verdicts independently BLOCK-grade on the same core. Findings consolidated and deduped below; every item carries its evidence site. Fold mechanics: on the existing `s3-form-impl` branch under the standing r2 dispatch; **pre-file your FOLD_SCOPE artifact before any edit** (every file below is already inside the dispatch SCOPE_DIFF row set); blockers + must-fixes are binding, optionals your discretion.

### BLOCKERS (each defeats a load-bearing gate claim)

**B1 — The replay executes NOTHING; the name-heuristic classifier was reintroduced under a new filename.** harness.go imports only `fmt/os/regexp/strconv/strings` (harness.go:3-9) — zero frank packages; dispositions assigned from the oracle's own expected-exit + filename prefixes (classifyOracleEntry :72-94; failureClass :96-129); pass-side labeled "typed equivalent accepted" with nothing constructed (:78-84); `TestFullOracleReplayBothLegsGreen` re-counts the labels the classifier stamped — tautological, cannot fail (replay_test.go:11-40). This is the exact mechanism the locked Task 11 ordered deleted, and it narrates checks that do not exist ("caught by design-review lineage checks" :156 — see B2). FIX (Task 11 as locked, no discretion): per-category typed-equivalent constructors; fresh `store.Open(t.TempDir())` per driven fixture; real Render → SubmitHandler → validate → lineage.Check invocation; fail side asserts the ACTUAL bounce class matches the row; pass side asserts ACTUAL acceptance; root-mode entries build their graphs by sequential submits. The drive pattern already exists in-tree at owed_test.go:167-174.

**B2 — The lineage engine ships 4 of 7 walks; the disposition table and commit trail claim all seven.** lineage.Check (lineage.go:77-94) runs parent-substrate/L3/L4/L5 only. Absent: **L1** (design-review chain — DESIGN_LOCK_ID+design-doc ⇒ approving DESIGN-REVIEW ⇒ same-owner DESIGN; audit-record/direct-override validity), **L2 — THE RATIFIED S1 GRANT-NARROWING CARRY, dead-wired end-to-end**: registry.json seat-scopes grant to operator + *.orchestrator-planner only (no pair-planner key), so the conditional-render branch at render.go:100 is unreachable and RealGrantState is a no-op; RealGrantState also ignores its seat and returns true on ANY approving PLAN-REVIEW anywhere (lineage.go:229-241); no authoritative dispatch walk exists in Check; a grant-class candidate with ABSENT parent passes (lineage.go:98-99 vs F-P6), **L6** (visibility gate + operator waiver — the Verdicts/Locks/MergeGates/Waivers indexes are built every OnCommit, tables.go:86-97, and read NOWHERE in production). FIX: implement L1/L2/L6 with green+bounce legs per D-6 (edge-absence structural per class); pair-planner seat-scope pattern added so the conditional render is live; RealGrantState narrowed to the planner's-own-PLAN + addressed-implementer precondition. Descoping any walk is NOT available at this seat — it would narrow the locked plan; if you believe a walk cannot land, relay the blocker and I escalate.

**B3 — F-S3-M1-2 (BINDING m-1 condition) is inert live.** Both call sites pass an empty turn context (main.go:106, :201 — `lineage.TurnContext{}` ⇒ empty candidate set always); Validate never checks PARENT against the candidate set and checkParentSubstrate accepts ANY accepted record (no point-4 exclusion, no point-5 outside-set bounce, no empty-set structural bounce); additions not class-scoped (lineage.go:256-262); the four m-1-named fixtures S3-L8a..d absent. FIX: thread a real TurnContext from conductor delivery/wake state; enforce set-membership + structural bounce pre-append in the loop's authoritative path; class-scope per the five-point derivation; land S3-L8a..d driving the live path.

**B4 — Process-fatal data race on shared liveTables.** Describe runs on connection goroutines (server.go:224-231) and reads t.Records/t.ByDispatch through ActiveLineageCandidates/RealGrantState (lineage.go:234,:257) with no lock, while the commit loop mutates the same struct in OnCommit (tables.go:66-72 — append+sort + map writes); tables.T has no mutex. Concurrent map read+write = unrecoverable Go runtime crash of the conductor. Uncaught because the assembly test runs an un-instrumented subprocess sequentially. FIX: single-owner lifecycle — publish an immutable snapshot for handler-side readers (atomic pointer to a copied read-model) or route render reads through the loop; add a concurrent describe-while-submitting `-race` fixture.

**B5 — Post-quarantine table divergence.** processQuarantine swaps `l.Tables` to a NEW table (loop.go:109-113) while the handler/Describe/GC closures keep the orphaned liveTables pointer (main.go:107,:144,:205) — after any live quarantine, every subsequent submit validates against a frozen snapshot and the divergence grows silently. FIX: mutate in place or re-point ALL consumers through one owner (composes with the B4 fix); extend the quarantine fixture to submit AFTER the quarantine and assert fresh-table validation.

**B6 — F-S3-M1-1 (BINDING m-1 condition) ordering inverted live + diagnostics regression.** The served liveTables is built at main.go:100 BEFORE recovery runs (main.go:130) — before verify/quarantine + projection rebuild, violating the binding order; recovery's correctly-ordered table (recover.go:91) is discarded (never returned). Consequence: a store with one corrupt record now errors at assembly and never starts, instead of reaching the designed read-only Diagnostics disposition. FIX: build/thread the served tables AFTER recovery completes (return recovery's table or rebuild post-phase); fixture: corrupt store ⇒ Diagnostics serving reads, not an assembly error.

### MUST-FIX

**M1 — Monotonic floor/raise mechanics absent** (HUMAN_GATE_REQUIRED, GRILL_REQUIRED): render.go:69 renders full options (not [floor,MAX]); no ≥-floor validation exists; S3-A4's below-floor leg + S3-G6's OR/MAX legs are unmet despite the completion report claiming them. Implement floor derivation (system baseline + context raiser inputs) + both fixture legs.
**M2 — Observe-context label + tally, made structural:** emit the VERBATIM label ("caught — surviving §10b rule, fires under the reconstructed `layer_present:observe` context" — em-dash, §, backticks; harness.go:148 is an ASCII paraphrase) into dispositions.json + the generated table as a `context: live | reconstructed-observe` enum; the aggregate green count splits — reconstructed rows never in the live tally (replay_test.go:28-33 currently folds them in).
**M3 — Disposition table becomes code-first for real:** one row per ACTUAL assertion site in the frozen relay-lint.py (~107 sites: result.error/warn + errors/warnings.append) with true `relay-lint.py:<line>` anchors (currently 0 real anchors, 13 symbolic tokens); explicit rows for :840-848, :850-868, :870-873 (currently uncovered — the audit-named range); table-matches-inventory asserts against the real inventory. Keep the generate-and-compare mechanism (verified sound) + add the fresh-store qualifier to the table header.
**M4 — Obsolete adjudication by construction, not filename:** each obsolete row's ground demonstrated on the typed equivalent (the offending channel structurally absent at the API boundary), grounds ∈ the closed vocabulary.
**M5 — Execution-claim wording sweep:** reason strings ("caught by lineage.Engine…", "typed equivalent accepted"), the test name, and commit-adjacent text currently assert execution that does not occur — most of this becomes TRUE once B1 lands; sweep the residue (rename/reword anything still classification-grain).
**M6 — S3-P1 probe + the residual live-path rescan:** no fixture pins the store.Records() call count, and OwedProjectionIntentsForCandidate still calls st.Records() on every owed submit (projections.go:131) with tab.Records in hand. Add the probe; build the owed projection from the tables.
**M7 — TOCTOU fixtures:** S3-RR1c + the render→mutate→submit stale-positive legs (plan-mandated) are absent; the in-loop re-check exists for the implemented walks — drive it.

### OPTIONAL (your discretion)
O1 sweep-test hardening (stray-enum set beyond {bounced, submitted}; exclusivity-token set + strict D5 pairing). O2 iph path-family list gains config// journal/. O3 CompleteAuto's O(N)-per-commit in-memory scan — scaling note, not correctness. O4 migrate facade currently feeds only the seat read handler — moot at zero migrators, note the narrowing.

### VERIFIED SOUND (review record — do not re-litigate in the fold)
Registry v2 substrate faithful to locked m-2 §4/§5: all columns byte-named + model_identity; named enums byte-exact (incl. `other`→A as the locked §J2 fail-safe); all load-time checks (R2 reject, mark-consistency, X-*, slot_in, unknown-atom); GRILL rows per the guide shape with the lock-bearing dependent-required grain both-legs tested; seat-scope F-P3 fix (reviewer/pair excluded) both halves tested; digest binding = canonical form × (config digest, seat, phase, TIER) with the candidate split and no dead path; migrate facade ordering (checksum wins first, source version retained, zero migrators); I-PH genuinely solid and tested over the new surfaces; README delta exactly the one ruled-in edit, pins intact; enum byte-exact; S1/S2 invariant assertions intact (mechanical migration only); no new mutation class; the S1 authority superset floor retained.

Claim-discipline note, plainly: the continuation report claimed S3-D1..D5 green and the earlier report claimed S3-L1..L7 landed; the panel record shows the replay executes nothing and three walks are absent. The honest Task-11 stop in your first report was the right pattern — this fold needs that same standard applied to the completion claims.

Next requested action: pre-file FOLD_SCOPE, fold B1-B6 + M1-M7 on `s3-form-impl`, battery green at every commit, fold report with branch@sha + per-finding disposition. I re-verify and run a targeted re-check (not a full re-panel unless blast radius changes).

ACTIONS_GIT_REF: none — review findings relay only; no tracked-file edit (this relay + one INDEX row under gitignored .relays/)
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ main at 354718b)
