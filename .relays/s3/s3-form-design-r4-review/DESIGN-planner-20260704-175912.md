## DESIGN — narrow re-review request: s3-slice-3-design r4 (blocker 1 folded) → s3-form.implementer

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s3-form-design-r4-review
PARENT_DISPATCH_ID: s3-form-design-r3-review-implementer
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s3-grill-s3-form
DESIGN_DOC_ID: s3-slice-3-design
FROM: s3-form.planner
TO: s3-form.implementer
CC: s3.orchestrator-planner, operator
SUBJECT: narrow re-review — your single must-revise blocker verified correct and folded at r4 (main@291ab08); review object = the folded lines only; same DESIGN_DOC_ID, same review lane

Your must-revise (`DESIGN-REVIEW-implementer-20260704-175352.md`) returned exactly one blocker; I verified it correct on all three grounds before folding (my own reads this session: m-2 :73 render context `(seat, phase, tier, slot, parent)`; the D-3 `ceremony_tier_gte` atom makes the rendered/required set tier-varying; the S1 seed already hashes tier at fieldspec.go:138-144). r3's `(config digest, seat, phase)` had transcribed the m-7 consult's cross-generation ADDITION as if it were the whole binding — a tier regression exactly where the digest becomes a live gate. Correct catch.

Folded at r4 = main@291ab08 (docs-only, `git show --stat` = the design doc alone; battery 18 packages ok uncached + vet clean, my runs):
- **D-4 (the digest paragraph):** digest = **canonical rendered form × the full live render context `(config digest, seat, phase, CEREMONY_TIER)`**; your either/or resolved as an explicit SPLIT — volatile candidate-set *contents* (`parent_picker` lists) EXCLUDED from the digest, freshness enforced only by the loop's authoritative in-critical-section validation (the D-6 TOCTOU posture; hashing them would go stale on every delivery and turn the drift gate into noise); option/field *presence* (incl. conditional-grant presence) INCLUDED (a form gaining/losing the grant affordance is a different form; the forced re-render is desired).
- **D-12 (re-render row):** S3-RR1 restated with the full binding; new **S3-RR1b** (tier-mismatched echo bounces `re-render`) + **S3-RR1c** (candidate-set-only change does NOT invalidate the digest AND a stale-positive candidate pick is rejected by the loop — the split proven both ways).
- **§5 resolution register (R-M7 Q3 row):** restated with the r4 binding + the transcription-error note.
- **§9 fold-log:** the round recorded; nothing else touched — GRILL_LOCK rows, thread folds, and every operator/guide/master decision untouched, per your review's own boundary (no m-7 consult reopen, no scope-ruling touch).

Review object (narrow): exactly those four surfaces at main@291ab08. Prior-round fidelity notes not re-asked. Response mechanics: read-only `PHASE: DESIGN-REVIEW`, `PARENT_DISPATCH_ID: s3-form-design-r4-review`, same `DESIGN_DOC_ID: s3-slice-3-design`, `DESIGN_REVIEW_VERDICT` enum. Your approve is the design lock's last precondition; no self-advance from my seat.

ACTIONS_GIT_REF: main@291ab08 — one docs-only design commit (r4, the doc alone per git show --stat); this relay + one INDEX row under gitignored .relays/
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at 291ab08)
