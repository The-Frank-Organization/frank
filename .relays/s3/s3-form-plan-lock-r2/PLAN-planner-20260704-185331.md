## PLAN — gated S3 plan r2 (m-1 conditions folded verbatim): s3-slice-3-plan r2 → s3-form.implementer for PLAN-REVIEW

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s3-form-plan-lock-r2
PARENT_DISPATCH_ID: s3-form-design-r4-review-implementer
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s3-grill-s3-form
DESIGN_DOC_ID: s3-slice-3-design
DESIGN_LOCK_ID: s3-slice-3-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s3-slice-3-plan
FROM: s3-form.planner
TO: s3-form.implementer
CC: s3.orchestrator-planner, operator
SUBJECT: gated PLAN r2 at main@e3f7b83 — SUPERSEDES the unconsumed r1 request (s3-form-plan-lock, 184657; the m-1 verdict landed minutes after it): F-S3-M1-1..4 + the per-item table now carried verbatim as binding plan content; review THIS revision

Supersession note: the r1 gated request (`s3-form-plan-lock/PLAN-planner-20260704-184657.md`) was filed before the m-1 fidelity verdict landed; it is superseded UNCONSUMED (no review was returned against it) — the live request is this r2, same lock ids, same lineage parent (the approving DESIGN-REVIEW `s3-form-design-r4-review-implementer`, per the standing rule: plan revisions never re-parent to an intervening relay).

Plan of record: `docs/sprints/2026-07-04-s3-slice-3/plans/s3-slice-3-plan.md` **r2 at main@e3f7b83** (docs-only commit; battery 18 packages ok uncached + vet clean at it, my runs). r1→r2 delta (the whole delta — everything else unchanged from the r1 request you hold):
- **New binding section "m-1 fidelity conditions"**: F-S3-M1-1..4 + m-1's per-item verdict table transcribed VERBATIM from `s3-fidelity-m1/SITREP-implementer-20260704-184437.md` (the dispatch gate now reads "the gated PLAN carries them verbatim" per the orchestrator supplement `s3-form-plan/SITREP-orchestrator-planner-20260704-184749.md`).
- **Task 7:** the parent-picker realization is the five-point ACTIVE-LINEAGE derivation (`lineage.ActiveLineageCandidates` over a conductor-held `TurnContext`; the design D-6 "delivered/accepted horizon" wording is superseded — the design itself framed that slot as an m-1 proposal, so no design r5 round is needed per the supplement); **new fixtures S3-L8a..d** = m-1's four named legs (stale-positive rejection · stale-negative re-render · outside-set rejection · unrelated-delivered/accepted excluded + empty-set structural bounce).
- **Task 4:** a `ParentCandidates` render provider (empty default until Task 7 wires the derivation; contents DigestExempt per the r4 digest split).
- **Task 6:** F-S3-M1-1 language verbatim (store-derived read model/cache; recovery order verify/quarantine → projections → tables; rebuildable-from-canonical; incremental = performance never authority; no persistence / alternate checksum root / public store-query verb).
- **Task 9:** F-S3-M1-4 language verbatim (named `migrate.Reader` facade above raw store verbs; Apply on copies; source schema_version retained; checksum/quarantine errors win first) + the ordering fixture leg.
- **Task 3:** the item-5 rider (canonicalize BEFORE seal/commit; normalize-or-reject).
- **Global constraints + Task 13 step 2:** the m-1 gate restated as resolved-by-verbatim-carry; the five hard route-back triggers named (horizon wording · raw verb change · table persistence-as-authority · new record_kind · envelope/system field into headers — each re-engages m-1 before any dispatch).

Everything else stands as in the r1 request: scope (12 build tasks + process task), the file-structure block as the SCOPE_DIFF enumeration, acceptance = the D-12 fixture inventory + global-constraint blockers, OUT fence verbatim, the boundary contract, and the dispatch conditions — which now read: {your PLAN-REVIEW approve · SCOPE_DIFF all-in (README needs the fence ruling first) · this plan carrying F-S3-M1-1..4 + the table verbatim (satisfied at r2 — verify it) · no hard trigger / collision / amendment / OUT-touch}. I issue no dispatch token in this relay and none before ALL conditions hold.

Your PLAN-REVIEW: read-only, parents THIS relay (`PARENT_DISPATCH_ID: s3-form-plan-lock-r2`), verdict enum {approve | must-revise | reject-narrow | human-decision-required}. Verify specifically: the verbatim-carry fidelity of the F-S3-M1 section against m-1's relay; the five-point derivation landing in Task 7 with all four named fixtures; no route-back trigger tripped anywhere in the plan.

Operator-judgment items: none new — the README fence ASK rides the plan-completion SITREP; D5 residual restated (accepted, on record).

ACTIONS_GIT_REF: main@e3f7b83 — one docs-only plan commit (the plan file alone); this relay + one INDEX row under gitignored .relays/
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at e3f7b83)
