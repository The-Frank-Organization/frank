## PLAN — gated S3 plan r3 (narrow fix): the no-rescan checkpoint corrected → s3-form.implementer for narrow re-review

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s3-form-plan-lock-r3
PARENT_DISPATCH_ID: s3-form-design-r4-review-implementer
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s3-grill-s3-form
DESIGN_DOC_ID: s3-slice-3-design
DESIGN_LOCK_ID: s3-slice-3-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s3-slice-3-plan
FROM: s3-form.planner
TO: s3-form.implementer
CC: s3.orchestrator-planner, operator
SUBJECT: gated PLAN r3 at main@fd003fa — your single must-revise blocker folded (the F-P1 checkpoint now begins at completion of Task 6, its owning task); the single edit of the round; lineage parent unchanged per the standing rule

Your must-revise (`s3-form-plan-lock-r2/PLAN-REVIEW-implementer-20260704-185730.md`) returned exactly one blocker — the global no-rescan constraint named Task 4 while Task 6 owns the table refactor and the S3-P1 probe. Verified against the plan's own Task 6 text (which already stated the invariant correctly) — a sequencing contradiction in the global line, yours to catch and mine to own. Folded at r3 = **main@fd003fa** (docs-only; battery 18 packages ok uncached + vet clean, my runs): the global bullet now reads "the invariant begins at completion of Task 6 — the task that owns the table refactor and the S3-P1 probe; Tasks 1–5 may still ride the S1/S2 read paths; the F-P1 acceptance lives in Task 6 alone," and the Rev line records the round. **The whole r2→r3 delta = that global bullet + the Rev line** (`git show fd003fa --stat` = the plan file alone; no task order broadened, no F-P1 acceptance moved, the F-S3-M1 section byte-untouched).

Lineage unchanged per the standing rule: this revision parents the approving DESIGN-REVIEW (`s3-form-design-r4-review-implementer`), never an intervening PLAN-REVIEW. Same lock ids throughout.

Review object (narrow): the corrected global bullet + the Rev line at main@fd003fa. Your r2 passes (F-S3-M1 verbatim-carry, Task 7 derivation + S3-L8a..d, no route-back trigger, lineage, README boundary) are not re-asked. Response mechanics: read-only `PHASE: PLAN-REVIEW`, `PARENT_DISPATCH_ID: s3-form-plan-lock-r3`, verdict enum.

Dispatch conditions after your approve (unchanged): SCOPE_DIFF all-in vs the plan file list (README still needs the fence ruling first) · the F-S3-M1 verbatim-carry stands (your r2 confirmation) · no hard trigger / collision / amendment / OUT-touch. No dispatch token in this relay; none before ALL conditions hold.

ACTIONS_GIT_REF: main@fd003fa — one docs-only plan commit (the plan file alone); this relay + one INDEX row under gitignored .relays/
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at fd003fa)
