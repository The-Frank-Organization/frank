## DESIGN — review request: s3-slice-3-design r3 (grill-locked, thread-folded) → s3-form.implementer for DESIGN-REVIEW

ROLE: Planner
PHASE: DESIGN
AUTHORITY: design-only
DISPATCH_ID: s3-form-design-r3-review
PARENT_DISPATCH_ID: s3-form-design
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s3-grill-s3-form
DESIGN_DOC_ID: s3-slice-3-design
FROM: s3-form.planner
TO: s3-form.implementer
CC: s3.orchestrator-planner, operator
SUBJECT: formal DESIGN-REVIEW request — the S3 implementation design at r3 (main@8ee97cc): twelve constraints landed, operator grill folded (GRILL_LOCK §8), all three question threads de-provisioned; your adversarial review is the design lock's last precondition

Review object: `docs/sprints/2026-07-04-s3-slice-3/designs/s3-slice-3-design.md` at **main@8ee97cc** (r3). Revision trail: r1 = main@c149b71 (pre-grill provisional draft); r2 = main@03b7cb7 (operator grill folded — GRILL_LOCK `s3-grill-s3-form` at §8: Q1 typed-header carrier = canonical JSON-in-string; Q2 disposition-table = generated pair; S2-store freeze = stated consequence under planner judgment, operator-deferred); r3 = main@8ee97cc (the three thread folds — read the records: guide `../.relays/s3/s3-guide-q1/SITREP-planner-20260704-173000.md`, m-7 consult `../.relays/s3/s3-consult-m7/SITREP-planner-20260704-171546.md`, master ruling `../.relays/s3/s3-scope-q1/RECONCILE-orchestrator-planner-20260704-171608.md`, routed by the orchestrator's supplement `s3-form-design/SITREP-orchestrator-planner-20260704-172730.md`).

What to review (adversarially — the S2 bar):
1. **The twelve-constraint landing map (§3)** — every dispatch constraint landed or argued; silence on any fails the orchestrator's reconciliation.
2. **Fold fidelity** — the guide's GRILL_REQUIRED row folded verbatim (D-10; incl. `gate_referenceable: true` as the deliberate §4-default departure); the observe-context adjudication guardrails made STRUCTURAL, not prose (D-8 report vocabulary: context enum + excluded-from-live-tally by construction); the both-flags model-identity R2 leg (D-11 (e)) + the D-1 `ModelIdentity ⇒ GateReferenceable false` load-check; m-7's serving/render/TOCTOU grain (D-4/D-6); master's five DEFER conditions incl. the fresh-store qualifier ON the gate line (§1, D-12 row 1).
3. **The S3-owned bounded choice (D-10)** — GRILL_LOCK_ID dependent-required ADOPTED at the lock-bearing grain `all_of(field:GRILL_REQUIRED == true, field:DESIGN_LOCK_ID present)`; the argument against the naive grain (it would bounce legitimate grill-ORDERING dispatches — the s3-form-design dispatch itself is the counterexample). Challenge the grain if you see a hole.
4. **The engine shapes** — D-6 lineage rows vs the §10c map + protocol :88 edge-absence semantics; D-7 tables vs F-P1 (no per-submit full-store rescan survives); the S1 authority-superset kept as the never-narrowing floor; D-2 canonicalization rule (checksum aliasing).
5. **Boundary + honesty** — S4 expression-capacity line held everywhere (reserved-shape rows, no values); OUT list intact; claim-boundary vocabulary (tool-mediated confusion-resistance + D5) on every exclusivity-shaped line; the ratified restart-is-the-true-semantics wording (D-4).
6. **Fixture inventory (D-12)** — every exit-gate line has fixtures; the non-overblocking leg (50 expected-OK) first-class; S3-V6 positive wall fixture present.

Review mechanics (design-review discipline): your response is read-only `PHASE: DESIGN-REVIEW`, parents THIS relay (`PARENT_DISPATCH_ID: s3-form-design-r3-review`), carries the same `DESIGN_DOC_ID: s3-slice-3-design`, and returns `DESIGN_REVIEW_VERDICT: approve | must-revise | reject-narrow | human-decision-required`. Per the standing dispatch, the design lock and everything downstream (PLAN lineage) hang on your approve — no self-advance from my seat.

Battery state: 18 packages ok (uncached) + `go vet` clean at every design commit (c149b71, 03b7cb7, 8ee97cc — my own runs; all three commits are docs-only by `git show --stat`).

ACTIONS_GIT_REF: main@8ee97cc — design-doc commits c149b71 (r1), 03b7cb7 (r2), 8ee97cc (r3), each touching only docs/sprints/2026-07-04-s3-slice-3/designs/s3-slice-3-design.md; this relay + one INDEX row live under gitignored .relays/
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at 8ee97cc)
