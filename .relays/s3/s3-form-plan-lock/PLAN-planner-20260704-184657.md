## PLAN — gated S3 plan (design-doc locked): s3-slice-3-plan r1 → s3-form.implementer for PLAN-REVIEW

ROLE: Planner
PHASE: PLAN
AUTHORITY: plan-only
DISPATCH_ID: s3-form-plan-lock
PARENT_DISPATCH_ID: s3-form-design-r4-review-implementer
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E1
HUMAN_GATE_REQUIRED: no
GRILL_REQUIRED: yes
GRILL_LOCK_ID: s3-grill-s3-form
DESIGN_DOC_ID: s3-slice-3-design
DESIGN_LOCK_ID: s3-slice-3-design
DESIGN_RECORD_KIND: design-doc
PLAN_LOCK_ID: s3-slice-3-plan
FROM: s3-form.planner
TO: s3-form.implementer
CC: s3.orchestrator-planner, operator
SUBJECT: gated PLAN — plan r1 at main@e7c7e5b against locked design r4; lineage parents your approving DESIGN-REVIEW; your PLAN-REVIEW is the plan gate; dispatch stays blocked on m-1 fidelity regardless

Locked design consumed: `DESIGN_LOCK_ID: s3-slice-3-design` (doc r4 = main@291ab08; GRILL_LOCK `s3-grill-s3-form` §8). Per the lineage-gate discipline: this PLAN parents your approving DESIGN-REVIEW (`s3-form-design-r4-review-implementer`, verdict approve, same DESIGN_DOC_ID), which parents my r4 DESIGN request — the chain the root-mode gate walks. Per the PROCEED-TO-PLAN (`s3-form-plan/PLAN-orchestrator-planner-20260704-183832.md`): any narrow plan-fix round re-parents its lock to that SAME approving DESIGN-REVIEW, never an intervening PLAN-REVIEW.

Plan of record: `docs/sprints/2026-07-04-s3-slice-3/plans/s3-slice-3-plan.md` r1 at **main@e7c7e5b** (docs-only commit; battery 18 packages ok uncached + vet clean at it, my runs).

Scope (locked): 12 build tasks + the process task — registry v2 with all load-time checks (T1) · predicate evaluator (T2) · canonical carrier (T3) · render + full-context digest (T4) · validate + dialect deletion (T5) · runtime tables off the F-P1 hot path (T6) · the seven-row lineage engine + the S1 carry (T7) · describe-grade form serving (T8) · migrators + refusal legs (T9) · assembly e2e incl. S3-V6/RR2 (T10) · code-first disposition table + FULL replay both legs (T11) · regression/sweeps (T12). File list = the plan's "File structure" block verbatim (the future SCOPE_DIFF enumerates exactly it).

Acceptance criteria: the D-12 fixture inventory, mapped fixture-id → task in the plan header block; every S3 exit-gate line covered; hard blockers restated as plan global constraints (fresh-store qualifier on claim surfaces; replay-label guardrails structural; both-flags R2 column; D-10 lock-bearing grain; S3-P1 no-rescan probe; CQ-1 discipline; I-PH extension; S1+S2 assertion byte-identity with the ONE stated exception class — dying 6-enum unit tests migrate assertion-equivalent, flagged line-by-line).

Out of scope (verbatim in the plan): ROADMAP OUT list · §7 config-change record (master-ruled OUT, OI-S3-CONFIG-CHANGE carries it) · concrete chosen_model/bucket-proxy negatives (S4/m-4) · root README.md absent a fence ruling (Task 13 ASK) · m-1 shape decisions (PROPOSALS pending fidelity) · ../master, ../extracted.

Boundary contract:
Writes: append-only store records via the existing loop (no new mutation class); the v2 registry config member (store-root, fresh-store); generated results/ artifacts (disposition table, replay report); test fixtures.
Reads: the locked m-1 verbs + the pinned config; the frozen upstream corpus (read-only replay input).
Target entity: the registry-driven validate/lineage path + the committed disposition/replay gate artifacts.
Downstream consumer: seats via describe/submit/project/read; the S3 exit gate (E2); S4 slices consume the registry's expression capacity.
Contract: m-2 §4/§5 shapes byte-faithful; terminal enum unchanged; store verbs unchanged.
Proof: the D-12 fixture set (E2), S3-D1..D5 for the gate centerpiece.
No-consumer action: not applicable — every surface has a named consumer above.

Tests / verification target: `go test ./...` E2 umbrella + `-race` legs; every task carries red→green steps; battery green at every commit.

Dispatch conditions (restated from the PROCEED-TO-PLAN — none satisfied by this relay): your PLAN-REVIEW approve · SCOPE_DIFF all-in vs the plan file list (README needs a fence ruling first) · **m-1 fidelity approve ON RECORD in .relays/s3/** (packet routed by the orchestrator in parallel) · no hard trigger / collision / amendment / OUT-touch. I issue no dispatch token in this relay and none before ALL conditions hold.

Your PLAN-REVIEW: read-only, parents THIS relay (`PARENT_DISPATCH_ID: s3-form-plan-lock`), verdict enum {approve | must-revise | reject-narrow | human-decision-required}.

Operator-judgment items: none new — the README fence ASK rides the plan-completion SITREP to the orchestrator; D5 residual restated (accepted, on record).

ACTIONS_GIT_REF: main@e7c7e5b — one docs-only plan commit (the plan file alone); this relay + one INDEX row under gitignored .relays/
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at e7c7e5b)
