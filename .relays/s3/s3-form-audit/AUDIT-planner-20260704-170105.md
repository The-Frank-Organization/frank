## AUDIT — s3-form.planner independent audit: S1+S2 onboarding + S3 form-system mandate mapped onto the baseline

ROLE: Planner
PHASE: AUDIT
AUTHORITY: read-only
DISPATCH_ID: s3-form-audit-planner
PARENT_DISPATCH_ID: s3-form-audit
RUN_ID: s3
CEREMONY_TIER: medium
EVIDENCE_TARGET: E2
HUMAN_GATE_REQUIRED: no
FROM: s3-form.planner
TO: s3.orchestrator-planner
CC: operator
SUBJECT: planner AUDIT return — spec-to-gate map closed (no blocking gap); 62-check census closed with one unmapped line-range named; corpus census E2; six 4-bucket verdicts; six fresh-eyes fragility findings; m-1 touch list (8 surfaces); m-7 consult questions (4)

Independence statement: I have not read `AUDIT-implementer-20260704-162725.md` or any other seat's audit content; this return was produced solely from the locked spec, the ledgers, the source, and my own runs.

### 0. Onboarding evidence (I built neither S1 nor S2)

- Ledgers read whole (E1): s1 RECONCILE.md (all 12 entries incl. the blocker-2 narrowing ratification :60-74 and OI-S1-F11-SWEEP record :160-161), s1 design r5, s2 RECONCILE.md (all entries incl. merge execution + `s2-close`), s2 design r4 (D-1..D-9, GRILL_LOCK §8, m-1 homes §4).
- Locked spec read (E1): m-2 design-of-record whole (§1-§18); ARCHITECTURE §C4.1-4.3 + §J1/J2 + the §C4 owed ledger (:477-489); m-7 §6/§7/§8 (:100-143); m-1 thesis/invariants/§0.b/§0.e.
- Source read (E1): internal/{fieldspec (+registry.json), engine (submit/loop/ready), lineage, config, record, store (store/genesis), seat, bounce, obligation, gate, recover}, channel/server.go tool surface, cmd/frank/main.go wiring probes, test/replay whole.
- Battery (E2, my own runs this session, frank/ at 042fcd9, clean tree): `go test -count=1 ./...` = 18 packages ok (uncached); `go vet ./...` clean — matches your on-record baseline run.
- upstream oracle (E2, my own run): `check-relay-lint-fixtures.py` = 146/146 PASS.

### 1. Spec-to-exit-gate map (every exit-gate line → locked text; file:line)

| exit-gate line | locked grounding (E1) |
|---|---|
| Registry live end-to-end (real header vocabulary renders/validates/commits/projects) | m-2 §4 :49-77 (FieldSpec shape + render/validate flow steps 1-6), §8 :118-120 (strict form-only submit), AC1 :226, AC2 :227; m-7 §8.2 :118-119 (submit's input schema IS the rendered form); header vocabulary = the upstream protocol.md canonical schema + m-2 §12/§17 slots |
| Fill-time authority by negatives (forbidden options absent; forged/out-of-scope bounces pre-append) | m-2 §3 :31-43 (owner model + render semantics + courier re-validates on submit), §4 `seat_scope` :58, AC2 :227; pre-append seam §4 steps 4-5 :75-76; honest wording ARCH §C4.3 :468 |
| Disposition table all 62 + FULL replay + [VP-W] obsolete-adjudication | m-2 §10 :131-172 (class defs :133; 10a :135-149, 10b :151-161, 10c :163-172), §8 :120 ("strictness is what permits the deletion"), §11 :174-179, AC3 :228; the VP-W concrete-vanished-surface rule = s3-dispatch/ROADMAP verbatim, vocabulary grounded in §10a "Why safe" :149 |
| R2 negatives (`gate_referenceable`-per-column) | m-2 §4 :57 (first-class flag, m-2-F5), §5 R2 grammar-enforcement :99, AC14 :239; §C4 owed ledger :480 (the step-(d) carry the s3 mandate lands here) |
| GRILL_REQUIRED row present + rendered | §C4 :487 (m-6-F6: "m-2 declares the ported upstream GRILL_REQUIRED header's FieldSpec (owner/type/values)"); carrier shape m-2 §4 :49-67. NOTE: locked text assigns the declaration to m-2/S3 but fixes no owner/type/values — a named guide-confirm item (§10 below), not improvisation territory |
| schema_version + migrators + [VP-W] refusal/bounce legs | m-2 §9 :122-129 (stamp, additive/breaking contract, read-time migrator registry, zero migrators, submission-always-current :128), AC5 :230, §18 CQ-4b layering note :377 (config digest ≠ record schema_version); refusal legs verbatim in the ROADMAP gate; no-downgrade-migrator = ROADMAP (introducing one ⇒ scope-expansion escalation) |
| No regression (suites, enum byte-exact, I-PH, owed/recovery/FIFO/GC untouched) | S1 design §3 fixture set + S2 design §3 (S2-RE :123); enum m-2 §17.1 :278-282 + m-7 :104; I-PH ARCH :470 + :489; owed/recovery/FIFO/GC = S2 D-1/D-2/D-7/D-8 |
| materialize-first with teeth | S2 D-7 :72-84 (owed instance, no auto-completer) + the OI-S1-F11-SWEEP discharge precedent (s2 RECONCILE :122-131) |

**No exit-gate line is ungrounded — zero blocking spec gaps.** One coverage note: the mandate's **re-render/drift** item grounds in STEP-1-KICKOFF :53 + m-7 §8.2 (render advisory) — not in the m-2 doc — and has **no dedicated exit-gate line**; recommend the S3 PLAN carry an explicit fixture (registry change ⇒ stale-digest bounce carrying "re-render") so a mandate item can't fall between gate lines.

### 2. 62-check + corpus census (ground truth: relay-lint.py @ upstream release, 1448 lines)

**§10a arithmetic closes at exactly 33 named identifiers** (6+1+7+1+1+1+4+3+4+5 across the ten rows); §10b = 8 rows, §10c = 7 rows, whose stated expansions land at ~16/~13 — the tilded 62 total is consistent. **Every §10 line cite verified grounding** against the file: defs at :116/:128/:138/:142/:307/:648 (row 1); :555 (row 2); :154/:242/:291/:311/:345/:398/:407 (row 3); :173/:211/:157 (rows 4-6); :47/:374/:394/:578 (row 7); :464/:470/:482 (row 8); :622-:645 (row 9); :266/:662/:712/:721/:740 (row 10); §10b :88/:355/:455/:447/:498/:414/:591/:927-944/:946-985; §10c :1006-1071/:1157-1278, :1280-1334, :1336-1360, :685-782/:1362-1374, :1376-1394, :1075-1130/:1396-1400, :609/:785/:1402-1410. **No stale cite found.**

**One check-bearing range the §10 map does not cite — must get explicit disposition-table rows in S3 (E1):** `lint_file` **:840-868 + :870-873** — the addressed-token shape checks: `DISPATCH IMPL` TO-required / exactly-one / implementer-role (:840-848); `DISPATCH MERGE` MERGE-GATE phase-binding (:854), TO cardinality (:856-861), grantor-set operator|orchestrator|orchestrator-planner (:862-868); duplicate merge-verdict-line (:870-873). Their *semantics* are re-homed by m-2 §11.2 :177 (grant as `seat_scoped_enum`, phase-scoped) — so this is a **table-coverage gap, not a design gap** — but "no silent drops" means these lines need named rows (dissolve-into-fill-time-authority for grantor/phase halves + a surviving TO-cardinality form-validation rule). Helper defs :44-46, :250-264, :547-553 are uncited but their consuming checks are mapped (:355-371, :947, :1048) — coverage note only.

**Corpus census (E1 count + E2 oracle run):** 243 `.md` fixtures (README excluded) across exactly 14 category dirs: addressing 11 · claude 20 · content 17 · design-review 73 · fold 13 · identity 1 · lineage 17 · lint-test 7 · merge 31 · merge-token 15 · orch-review 26 · p9 2 · probes 4 · rowtruth 6. Oracle = 146 expected-verdict entries (68 exact-file + 78 relay-root, root entries spanning multi-file dirs); expected PASS-fixtures 50 / FAIL-fixtures 96; my run 146/146. Category→§10 class: {identity, probes-N2/N3, lint-test grammar, p9 continuations, content detached/dual-block} → 10a · {claude downgrade/enum/authority, content substance/absence, fold, rowtruth, addressing cardinality} → 10b (+ the :840-868 rows for addressing/merge-token) · {design-review, lineage, merge, merge-token lineage-half, orch-review} → 10c. The replay's caught-or-obsolete population = the FAIL side; the PASS side proves non-overblocking (a dimension the gate wording doesn't name — carry it: the new validation must also NOT reject the 50 expected-OK shapes' typed equivalents).

**classmap.go today (E1/E2):** report.md = 246 rows (243 corpus + 3 synthetic): caught 3 · obsolete-by-construction 22 · **uncovered-S3 221** — the S3 gate flips those 221 to executed caught-or-genuinely-obsolete.

### 3. Six 4-bucket verdicts

**V1 — Full FieldSpec registry.**
PRIMARY_BUCKET: still-open
still-open: the entire m-2 §4 column set — registry.json (:1-47) carries NONE of {layer, owner, type, enum_set, gate_referenceable, seat_scope, required_when, visible_when, fill_constraints, consumers, lineage_role}; the §5 bounded predicate evaluator (none exists anywhere — Validate is hardcoded, fieldspec.go:81-106); a typed carrier for row_array/object/id_ref fields (Headers is map[string]string, record.go:29); the per-field seat-scope matrix; serving the rendered form through the channel (server.go:209-212 serves tool names + static descriptions only — Render is never called outside tests, E1 grep).
already-closed: registry-as-pinned-config with genesis digest (config.go:29-83, genesis.go); Render/Validate/digest at library grain (fieldspec.go:54-106,138-150); `canGrant` seat-scoping (:108-110); `ClassifyGateCategory` other→A + known-A raise (:112-123); grant phase-scoping (:70-76).
product-overlapped: none — this IS m-2's domain; S4 consumer-field content stays out per the VP-W expression-capacity line.
recommended-next: DESIGN — registry data model + evaluator + typed-header representation decision + the channel form-serving seam (m-7 consult Q4).

**V2 — 62-check dissolution + disposition table.**
PRIMARY_BUCKET: still-open
still-open: the per-check table artifact for all 62 (incl. the :840-868 rows above); the retained-post-submit check engine; every §10c engine row beyond today's two S1 edges (see §4).
already-closed: bounce classes + central formatter (bounce/formatter.go); parent-resolve + one-shot verdict edges (lineage.go:43-64, submit.go:138-173); the pessimistic authority-bearing superset (lineage.go:21-41).
product-overlapped: none.
recommended-next: DESIGN the lineage engine over incrementally-maintained tables, not per-submit full-store rescans (finding F-P1).

**V3 — FULL dissolved-linter replay.**
PRIMARY_BUCKET: still-open
still-open: executed replay of all 243 (221 currently deferred by name-heuristic); the adjudication rule for observe-owned dissolved checks under the CQ-1 step-gate (probe (c) — the sharpest edge in this audit).
already-closed: the corpus walker + report-matches-generated artifact pattern (classmap.go:92-114, replay_test.go:25-34); the oracle as expected-verdict source; §8-preserving test-only import posture (S1 D-10).
product-overlapped: none.
recommended-next: DESIGN the harness against the real validate/lineage path with an explicit evaluation-context rule for step-gated checks; keep the pass-fixture non-overblocking leg.

**V4 — schema_version + migrator registry.**
PRIMARY_BUCKET: still-open
still-open: the migrator registry, read-time apply chain, v(n)→v(n+1) fixture, and the [VP-W] refusal/bounce legs (unknown/future, unversioned, mismatched — never silent coercion).
already-closed: `schema_version` stamped envelope-only since S1 (record.go:24; store.go:77-79 defaults 1; the m-1 F-M1-1 home).
product-overlapped: none.
recommended-next: DESIGN the read-path seam (m-2 read-side per §12 :185; m-1 fidelity on the exact wrap point — touch list §5.5).

**V5 — R2 per-column negatives + GRILL_REQUIRED row.**
PRIMARY_BUCKET: still-open
still-open: `gate_referenceable` as a registry column (absent from code entirely — E1 grep); the per-column negative fixtures (grammar rejects a predicate naming a non-gate-referenceable field — expressible in S3 without S4 field content: the fixture grain is the column mechanics, pin exact grain at DESIGN with the guide per §C4 :480's live-field-grain wording); the GRILL_REQUIRED FieldSpec row (token absent from code — E1 grep) with guide-confirmed owner/type/values.
already-closed: the known-A/RAISE-ONLY guardrail-adjacent portion (S1 G-fixture legs; ClassifyGateCategory).
product-overlapped: none.
recommended-next: DESIGN both; neither is expressible as data until V1's column set exists — sequence inside the registry work.

**V6 — Re-render/drift.**
PRIMARY_BUCKET: already-closed
already-closed: the digest mechanism + "re-render" bounce class exist (fieldspec.go:77,86-89,138-150) — but **dead-pathed on the live submit path**: SubmitHandler passes `formDigest=""` (submit.go:33), so the stale-digest check never fires outside direct library tests; no channel plumbing carries a client digest (E1 grep: no non-test caller).
still-open: wiring the digest through the served surface; correctness-under-a-CHANGING-registry fixtures (S1's registry never changes mid-store).
product-overlapped: none.
recommended-next: promote/wire the existing dead-pathed mechanism (do NOT rebuild) + the registry-change fixture; see also the §1 gate-coverage note.

### 4. Grant-narrowing carry (s1 RECONCILE :68-74) — disposition

**On the S3 build list by construction, but not as a named bullet:** the ratified narrowing lands "conditional pair-Planner delegated-dispatch rendering + the m-2 :167 lineage walk" in S3; the S3 exit gate reaches both only via the all-62 disposition table (the §10c dispatch-impl row) + the registry fill-time lines. Recommend the S3 DESIGN name the carry explicitly so it can't silently narrow. What the full §10c engine needs beyond today's lineage.Check (each row = new store-query shapes, feeding §5):
- design-review walk: design-doc PLAN → approving DESIGN-REVIEW (same DESIGN_DOC_ID) → same-owner DESIGN (m-2 §10c :166; relay-lint :1006-1071/:1157-1278);
- pair-Planner dispatch walk: grant=dispatch-impl → approving PLAN-REVIEW → that Planner's PLAN addressed to the implementer (:1280-1334) — **edge-absence must become a structural error for this class** (protocol :88); today lineage.Check passes on absent PARENT (lineage.go:47-49, correct for S1's edges only);
- non-addressee IMPL trap (:1336-1360); merge-claim lineage vs same-DISPATCH_ID MERGE-GATE grant (:685-782/:1362-1374); OUT→IN scope-flip drift over typed row-arrays (:1376-1394); orchestrator-review visibility gate + run-level operator waiver query (:1075-1130); the graph/index substrate (:609/:785/:1402-1410).
**New seam the carry forces (fresh-eyes):** conditional pair-Planner grant *rendering* means render consults lineage state ("does an approving PLAN-REVIEW exist for this seat") — today render is a pure function of (registry, seat, phase, tier). A render-time store read is BOTH an m-1 fidelity surface and an m-7 engine-seam question (consistency vs the serialized loop).

### 5. m-1 lineage/store-touch enumeration (the fidelity packet surface — VP watchpoint)

1. **New query shapes for the §10c engine:** by-DISPATCH_ID resolution, order-aware traversal, by-header filtered reads (record_kind, DESIGN_DOC_ID, grant, resolves_gate), accepted-graph views, run-level waiver existence. Today's whole read API = Records() full-scan-with-reverify + Read(relayID) + Project(seat) (store.go:105-173). Recommendation to carry: engine-side incrementally-maintained tables over the EXISTING verbs (no store-API change) — but the choice itself is m-1's to fidelity-review.
2. **PARENT movement:** today a free-typed header checked only when present (lineage.go:47); m-2-locked = envelope-layer `parent_picker` with conductor-derived candidate set + default (m-2 §12 :185). Any S3 movement (header→envelope home, candidate-set derivation, system default, free-typed reject) is the canonical m-1 fidelity trigger.
3. **Lineage-role fields:** homes for DESIGN_DOC_ID/DESIGN_LOCK_ID/DESIGN_RECORD_KIND/DESIGN_REVIEW_VERDICT/grant/merge-claim refs (envelope vs Headers per the S2 F-M1-1 homes-table precedent).
4. **record_kind taxonomy growth:** the precise authority-bearing set (m-2 §17.6 authority_class derivation) replacing the S1 pessimistic superset; new tokens follow the m-1-prescribed-homes pattern.
5. **schema_version migration read path:** where the migrator chain wraps store reads (Read/Records/Project consumers); refusal legs produce bounces, never mutations; m-1 serves versioned records, migration stays m-2 read-side (§12 :185).
6. **Registry config-member replacement:** store-root members + genesis digest (F-M1-3); a larger fieldspec member changes the digest ⇒ existing stores fail phase-0 (recover.go:31-39) — joined to m-7 consult Q1; m-1 on CC.
7. **Render-time lineage/candidate queries** (from §4) — store reads outside the commit loop; snapshot-consistency question.
8. **Rendered projections under the full envelope** — derived-only content change; canonical sufficiency (S2 D-6) preserved.

### 6. m-7 consult questions (consult, don't improvise — named exactly)

- **Q1 (escalation-shaped if mandatory):** the **§7 config-change record** (m-7 :109 "a legitimate config change is itself a committed store record") — S2 fixed config for the store's life and its design says verbatim "the §7 config-change record (S3)" (s2 design §1 :18), but the s3 mandate does not name it. Does S3 build it (registry replacement/evolution in existing stores), or is fresh `store.Init` the S3 posture with the record deferred again? If the former, that's engine-owned mechanism riding S3 — route the scope question via you to master/m-7 before DESIGN locks.
- **Q2:** member shape — one larger `fieldspec` member vs per-domain sections with per-section stamps (m-7 §7 :109 CQ-4b full author set); `configTarget` hardcodes exactly two member names (genesis.go:118-127), so any new member is engine code.
- **Q3:** the versioned registry's own version home (member content? genesis?) vs record `schema_version` — confirm the two-axis composition (m-2 §18 :377) for S3's concrete layout.
- **Q4:** the sanctioned way to serve the rendered form as `submit`'s input schema over the EXISTING per-seat socket protocol (tools/list is names-only today, server.go:209-212) without touching the OUT MCP live-adapter — m-7 owns the attach/channel surface.

### 7. Claim-boundary probes (answered from locked text)

- **(a) "genuinely obsolete" may cite exactly:** the vanished raw hand-authored-markdown text channel — the named §10a class (fences/inline-code, bare own-line tokens, FIELD:-block/row grammar, ambiguous continuations, detached rows, address string grammar) + §8 strict form-only submit :118-120, and for ROLE/FROM the one-channel stamp §2/§3 :27-34. A disposition needing any other ground rests on a design-of-record change ⇒ escalates to master before S3 close (VP-W, ROADMAP verbatim).
- **(b) fill-time-authority honest wording (pin for every negative fixture):** *tool-mediated confusion-resistance* — the forbidden option is absent from the rendered form (affordance removed, not access), the courier re-validates by set-membership on submit; **D5 residual stated beside every exclusivity-shaped claim**: a same-uid shell-routed seat — confused or malicious — bypassing the courier is the accepted residual (ARCH §C4.3 :468-470).
- **(c) CQ-1 required-set consequence + the replay's sharpest edge:** observe-owned registry rows (FINAL_GIT_STATUS_SHORT, ACTIONS_GIT_REF, achieved_evidence, the *_RESULT set — m-2 §5 :96) carry `all_of(required_when, layer_present:observe)` (:89, :95; AC17 :242) ⇒ **not required on any live Step-1 form**, while every system/lineage/form-owned required — `EVIDENCE_TARGET` included (:97) — stays required. Consequence the gate must adjudicate: upstream failure-fixtures of the read-only-FINAL/ACTIONS-substance class (§10b :927-944; e.g. claude/B5, content/E1/E2) **cannot bounce off a live Step-1 form**; they are NOT obsolete (no surface vanished — the check is step-gated) and are only honestly "caught" if the replay harness evaluates the predicate under an explicit `layer_present:observe = true` context, proving the dissolved rule exists and fires. Pin this adjudication rule at DESIGN with the guide; the {caught | genuinely-obsolete} vocabulary has no third bucket, and silent reclassification would breach VP-W.
- **(d) I-PH on new surfaces:** bounce.Format emits field:class only (formatter.go:11-26) — keep every new registry/predicate/migration error routed through it; **config.Load error strings embed member paths** (config.go:39 `%w`-wraps the path) — today reaching only admin-CLI/startup (operator-facing, the S2 `-mint` I-PH seat-grain precedent), but S3's registry-validation errors must never surface that class to a seat; extend the P1 grep family to registry errors + re-render bounce text.

### 8. Duplicate/already-built gate (named candidates confirmed/refuted)

- digest/re-render seed — **CONFIRM, dead-pathed** (V6): promote/wire, never rebuild.
- `canGrant` — **CONFIRM with a discrepancy (F-P3):** fieldspec.go:109 includes `orchestrator-reviewer` in the grant/merge-gated affordance set; the upstream authority set is {operator, orchestrator, orchestrator-planner} (relay-lint.py:59 LINEAGE_DIRECT_FROM_ROLES; :867 merge grantor set) and m-2 §11.2 :177 says "present only on the operator/orchestrator form". The S3 seat-scope matrix must exclude the reviewer seat (visibility, never grant authority); registry-data fix riding S3 — if the design defers it, record it as a typed owed item (the live S2 mechanism).
- corpus walker — **CONFIRM** (classmap.go:92-114 + the report-matches-generated pattern): reuse walk+artifact; replace the classifier with execution.
- S2 crash/battery machinery — **CONFIRM** (crashpoint registry + child-SIGKILL + applicability map, S2 D-9): reusable for any new S3 commit-path surface; S3 is mostly validate-path, so expect few new mutation classes (a config-change record, if Q1 lands, would be one).
- owed-item mechanism — **CONFIRM live** (obligation/owed.go; S2-O* fixtures green; OI-S1-F11-SWEEP discharged on a real store): use it for recording any S3 owed findings.
- `schema_version` stamped — **CONFIRM** (envelope-only, default 1). `held`/gate_category machinery — **CONFIRM** (loop.go:147-187 compound held; ClassifyGateCategory; park/outbox derivation).

### 9. Fresh-eyes fragility findings (load-bearing-but-fragile under a changing, larger registry)

- **F-P1 (top):** every submit re-reads and re-checksums the ENTIRE store from disk multiple times — lineage.Check (lineage.go:51), classifyVerdict (submit.go:146), the owed leg (submit.go:112), and obligation.CompleteAuto on every commit turn (loop.go:138 → obligation.go:118-123) with per-gate-record linear rescans (obligation.go:147,173). S2 built BuildTables but the live loop path does not ride them. Under the FULL replay + the heavier registry validate this O(N²)+ path is the hot path; design constraint: the §10c engine + obligation completion ride incrementally-maintained tables.
- **F-P2:** form-digest dead path (V6) — the re-render gate line has no live producer today.
- **F-P3:** `canGrant` reviewer membership (§8) — a fill-time-authority discrepancy vs locked text.
- **F-P4:** Validate() never enum-checks CEREMONY_TIER / EVIDENCE_TARGET / HUMAN_GATE_REQUIRED / gate_category tokens (fieldspec.go:81-106 covers PHASE/AUTHORITY only); an unknown gate_category is treated as gate-bearing (submit.go:92-94) and fail-safed to A downstream, never bounced. The registry-driven validate closes this by construction — name it so the replay's enum fixtures (claude/B9, C2) are caught by the real path, not a special case.
- **F-P5:** the rendered form is never served through the channel (server.go names + static descriptions only) — "renders … through frank" requires the wire-up; today's A2/G assertions are library introspection, not end-to-end.
- **F-P6:** absent-PARENT passes lineage (lineage.go:47-49) — correct for S1's edges; the §10c walks must make edge-absence structural per class (protocol :88).

### 10. Spec gaps, guide items, risks

- Blocking spec gaps: **none** — the §1 map closes.
- Guide (m-2) confirm items, via operator hand-relay at DESIGN: GRILL_REQUIRED row owner/type/values (§C4 :487 assigns, doesn't fix); the probe-(c) replay adjudication context for step-gated checks; the R2 fixture grain at live-field level (§C4 :480).
- m-7 consult items: §6 Q1-Q4 (Q1 escalation-shaped).
- Top risks to the gate centerpiece: probe-(c) unresolved (could stall the caught-or-obsolete adjudication late), F-P1 (replay wall-clock + hot-path correctness), the :840-868 unmapped rows (silent-drop hazard if the table is built §10-row-first instead of code-first).
- Reject/narrow gates: none recommended — all six items are genuinely S3-shaped; V6 narrows to promote+fixture.

Boundary contract: not applicable to this read-only audit (no writes; the S3 build boundary is the ROADMAP's verbatim one-line boundary, restated in my boot ACK). Operator-judgment items: none new — D5 residual restated (accepted, on record).

Evidence levels: E1 = every file:line cite above (m-2/ARCH/m-7/m-1 line refs, frank source, relay-lint.py, fixture counts); E2 = my own runs (battery 18 ok + vet clean at 042fcd9; oracle 146/146; disposition counts from report.md).

ACTIONS_GIT_REF: none — read-only audit; no tracked-file edit (this relay file + one INDEX row live under untracked-by-convention .relays/)
FINAL_GIT_STATUS_SHORT: none — clean tree (frank/ at 042fcd9)
